# Elixir / Mix build artifacts
_build/
deps/
*.ez

# ElixirLS
.elixir_ls/

# Dialyzer PLTs
priv/plts/*.plt
priv/plts/*.plt.hash
**/priv/plts/

# Coverage
cover/

# Generated release verification outputs
**/doc/
*.tar
erl_crash.dump

# Local planning/research cache; generated during research lookups.
.planning/research/.cache/

# JavaScript browser-test artifacts
node_modules/
playwright-report/
test-results/

# Editor / OS noise
.DS_Store
*~
.cursor/
hostlog.txt
.bg-shell/
.gsd/
.gsd_*
.mcp.json
.tmp/

# Local `act` secret files for live-Stripe CI replay (quick task 260414-l9q).
# The committed template lives at `accrue/.secrets.example`.
.secrets
accrue/.secrets

# Internal brand working doc — local-only, not part of the published repo.
prompts/accrue-brand-book.md

.env

# Claude Code local scheduler lock — machine-local runtime state (session id / pid),
# not shared repo state.
.claude/scheduled_tasks.lock

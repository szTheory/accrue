# Elixir / Mix build artifacts
_build/
deps/
*.ez

# ElixirLS
.elixir_ls/

# Dialyzer PLTs
priv/plts/*.plt
priv/plts/*.plt.hash
**/priv/plts/

# Coverage
cover/

# Generated release verification outputs
**/doc/
*.tar
erl_crash.dump

# Local planning/research cache; generated during research lookups.
.planning/research/.cache/

# JavaScript browser-test artifacts
node_modules/
playwright-report/
test-results/

# Editor / OS noise
.DS_Store
*~
.cursor/
hostlog.txt
.bg-shell/
.gsd/
.gsd_*
.mcp.json
.tmp/

# Local `act` secret files for live-Stripe CI replay (quick task 260414-l9q).
# The committed template lives at `accrue/.secrets.example`.
.secrets
accrue/.secrets

# Internal brand working doc — local-only, not part of the published repo.
prompts/accrue-brand-book.md

.env

# Claude Code local scheduler lock — machine-local runtime state (session id / pid),
# not shared repo state.
.claude/scheduled_tasks.lock

# GSD-derived phase/milestone state, regenerated on every GSD step. .planning/STATE.md
# is the committed authority; this file is a projection of it and of ROADMAP.md, and
# .planning/STATE.md itself names it as one of the two files "permitted to move" after
# an inventory capture. Committing a projection of a committed authority is the
# derivation cycle scripts/ci/verify_artifact_fixed_point.mjs exists to forbid.
.planning/state.json

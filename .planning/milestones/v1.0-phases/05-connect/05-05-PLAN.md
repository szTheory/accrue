---
phase: 05-connect
plan: 05
type: execute
wave: 2
depends_on: [05-02, 05-04]
files_modified:
  - accrue/lib/accrue/connect.ex
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/test/accrue/connect/charges_test.exs
  - accrue/test/accrue/connect/transfer_test.exs
autonomous: true
requirements: [CONN-04, CONN-05]
must_haves:
  truths:
    - "Accrue.Connect.destination_charge/2 issues a single PLATFORM-scoped charges.create with transfer_data.destination AND application_fee_amount; Stripe-Account header is NOT set"
    - "destination_charge/2 returns {:ok, %Accrue.Billing.Charge{}} via existing Phase 3 projection"
    - "Accrue.Connect.separate_charge_and_transfer/2 issues two distinct API calls: charges.create on platform then transfers.create to destination"
    - "Accrue.Connect.transfer/2 is a public helper for bare Transfer API"
    - "Telemetry emits [:accrue, :connect, :destination_charge|:separate_charge_and_transfer|:transfer, :start|:stop|:exception]"
    - "Both charge functions accept %Account{} struct OR acct string via resolve_account/1"
  artifacts:
    - path: "accrue/lib/accrue/connect.ex"
      contains: "def destination_charge"
    - path: "accrue/lib/accrue/connect.ex"
      contains: "def separate_charge_and_transfer"
    - path: "accrue/lib/accrue/connect.ex"
      contains: "def transfer"
  key_links:
    - from: "accrue/lib/accrue/connect.ex"
      to: "accrue/lib/accrue/processor.ex"
      via: "Processor.__impl__().create_charge / create_transfer"
    - from: "accrue/lib/accrue/connect.ex"
      to: "accrue/lib/accrue/billing/charge.ex"
      via: "Billing.Charge.from_stripe projection (Phase 3)"
---

<objective>
Wave 2: Ship CONN-04 (`destination_charge/2`) and CONN-05 (`separate_charge_and_transfer/2` + standalone `transfer/2`) — the two distinct Connect charge flows per D5-03, each with its own typed opts, telemetry span, and ExDoc section. Platform fees are explicitly caller-injected per Plan 04.

Purpose: Close the money-movement half of Phase 5. Destination charges are platform-scoped (header NOT set, transfer_data in body). Separate charges + transfers are two distinct API calls. Pitfall 2 in RESEARCH.md (header on platform-only calls) is a primary threat.

Output: Three new public functions + adapter delegations + Fake processor extensions + tests covering VALIDATION rows 11, 12, 13, 14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-connect/05-CONTEXT.md
@.planning/phases/05-connect/05-RESEARCH.md
@.planning/phases/05-connect/05-PATTERNS.md
@.planning/phases/05-connect/05-VALIDATION.md

<interfaces>
From accrue/lib/accrue/billing/charge.ex (Phase 3 Charge projection):
- `%Accrue.Billing.Charge{}` struct
- Projection from Stripe Charge shape to local row

From lattice_stripe (verify at execution time):
- `LatticeStripe.Charge.create/3` — accepts params including `transfer_data: %{destination: acct_id}` and `application_fee_amount: int`
- `LatticeStripe.Transfer.create/3` — accepts `%{amount, currency, destination}` (verified file exists at /Users/jon/projects/lattice_stripe/lib/lattice_stripe/transfer.ex)

From accrue/lib/accrue/billing.ex (Phase 3 charge/3 wraps IntentResult outside Repo.transact per STATE.md note):
- "charge/3 calls Processor.create_charge OUTSIDE Repo.transact so SCA 3DS responses never persist a half-baked Charge row"
- Connect destination_charge follows the same pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: destination_charge/2 + separate_charge_and_transfer/2 + transfer/2 on Accrue.Connect + adapter delegations</name>
  <read_first>
    - accrue/lib/accrue/connect.ex (from Plans 02-04)
    - accrue/lib/accrue/billing.ex (charge/3 Phase 3 — read for IntentResult wrap shape)
    - accrue/lib/accrue/billing/charge.ex (Charge schema + projection)
    - accrue/lib/accrue/processor/stripe.ex (create_charge delegation site + build_client! + force_platform helper from Plan 03)
    - accrue/lib/accrue/processor/fake.ex (Phase 3 create_charge implementation + Phase 5 account scoping from Plan 02)
    - /Users/jon/projects/lattice_stripe/lib/lattice_stripe/charge.ex (verify create/3 signature + transfer_data support)
    - /Users/jon/projects/lattice_stripe/lib/lattice_stripe/transfer.ex (verify create/3 signature — RESEARCH Assumption A3)
    - accrue/test/accrue/billing/charge_test.exs (Phase 3 Charge test shape)
  </read_first>
  <behavior>
    - `destination_charge(params, opts)`:
      - params required keys: `:amount` (%Money{}), `:currency` (atom), `:destination` (%Account{} or "acct_..."), `:customer` (%Customer{} or id)
      - opts: `:application_fee_amount` (%Money{}, optional — caller injects from Plan 04 platform_fee), `:description`, `:metadata`, `:statement_descriptor`, etc.
      - Internally builds Stripe params `%{amount: gross_minor, currency: ..., customer: ..., transfer_data: %{destination: acct_id}, application_fee_amount: fee_minor, description: ..., metadata: ...}`
      - Calls `Processor.__impl__().create_charge(stripe_params, request_opts)` with `request_opts |> Keyword.put(:stripe_account, nil)` — FORCE PLATFORM SCOPE (Pitfall 2)
      - Wraps Processor call OUTSIDE `Repo.transact` (matches Phase 3 D3-charge pattern from STATE.md), then persists local `%Billing.Charge{}` via projection
      - Returns `{:ok, %Accrue.Billing.Charge{}}` bundled with resolved `%Account{}` (per D5-03 "Return value bundles the resolved Account struct")
      - Telemetry: wraps call in `:telemetry.span([:accrue, :connect, :destination_charge], %{destination: acct_id}, fn -> ... end)` per Phase 3 telemetry convention
    - `separate_charge_and_transfer(params, opts)`:
      - params: `:amount` (%Money{}), `:currency`, `:customer`
      - opts: `:destination` (required, %Account{} or acct string), `:transfer_amount` (required, %Money{} — amount to send to connected account), `:description`, `:metadata`
      - Step 1: Create charge on PLATFORM (stripe_account: nil) via `Processor.create_charge/2`
      - Step 2: If charge succeeds, create Transfer via `Processor.create_transfer/2` with `%{amount: transfer_amount_minor, currency: ..., destination: acct_id, source_transaction: charge.processor_id}`
      - Returns `{:ok, %{charge: %Accrue.Billing.Charge{}, transfer: map()}}`
      - If Transfer fails, the charge still exists — returns `{:error, {:transfer_failed, %Billing.Charge{}, error}}` so caller can reconcile
      - Telemetry: `[:accrue, :connect, :separate_charge_and_transfer, ...]`
    - `transfer(params, opts)`:
      - params: `:amount` (%Money{}), `:currency`, `:destination` (acct_id or struct)
      - Calls `Processor.__impl__().create_transfer(stripe_params, request_opts)` — platform-scoped
      - Returns `{:ok, map()}` (Phase 5 does NOT ship a dedicated Transfer schema per D5-05 "events ledger only; no dedicated accrue_connect_payouts schema"). Records an `accrue_events` row via `Accrue.Events.record/1`.
      - Telemetry: `[:accrue, :connect, :transfer, ...]`
    - All three have bang variants.
  </behavior>
  <action>
    1. Append to `accrue/lib/accrue/connect.ex`:
       - `@destination_charge_schema` NimbleOptions validating opts
       - `@separate_charge_schema` NimbleOptions
       - `@transfer_schema` NimbleOptions (`:amount`, `:currency`, `:destination` all required)
       - `destination_charge/2` + `destination_charge!/2`
       - `separate_charge_and_transfer/2` + `separate_charge_and_transfer!/2`
       - `transfer/2` + `transfer!/2`
       - Private helper `resolve_destination/1` reusing `resolve_account_id/1` from Plan 02
       - Private helper `build_charge_params/2` that maps `%Money{}` → minor int, destination → `transfer_data: %{destination: acct_id}`, fee %Money{} → `application_fee_amount: int`
       - Each public fn wrapped in `:telemetry.span/3` per Phase 3 convention
       - After successful call, invoke `Accrue.Events.record(%{type: :connect_destination_charge | :connect_separate_charge_transfer | :connect_transfer, ...})` — matches EVT-04 policy
    2. Edit `accrue/lib/accrue/processor/stripe.ex`:
       - Add `@impl true def create_transfer(params, opts)` → `LatticeStripe.Transfer.create(params, request_opts, build_client!(force_platform(opts)))`
       - Add `@impl true def retrieve_transfer(id, opts)` → `LatticeStripe.Transfer.retrieve(id, opts, build_client!(force_platform(opts)))`
       - NOTE: existing `create_charge/2` already works via Phase 3 — Phase 5 does NOT need to modify it. destination_charge passes `transfer_data:` as just another field in the params map, which Phase 3's pass-through shape already handles. Verify by reading existing create_charge delegation before editing.
    3. Edit `accrue/lib/accrue/processor/fake.ex`:
       - Add `create_transfer/2`, `retrieve_transfer/2` clauses — deterministic id `"tr_test_#{counter}"`, store in `{:platform, :transfer}` keyspace
       - Ensure existing `create_charge/2` clause captures `transfer_data:` from params and stores on the fake charge (so test can assert it)
       - Ensure `create_charge/2` also captures `application_fee_amount:` from params
    4. Create `accrue/test/accrue/connect/charges_test.exs`:
       - `use Accrue.ConnectCase, async: true`
       - Test (Mox boundary on Processor, via Fake inspection): `destination_charge/2` builds stripe params with `transfer_data: %{destination: "acct_X"}` AND `application_fee_amount: <int>` (VALIDATION row 11)
       - Test: `destination_charge/2` issues call with `stripe_account: nil` even when called inside `with_account("acct_X", fn -> ... end)` (Pitfall 2)
       - Test: `destination_charge/2` returns `%Accrue.Billing.Charge{}` projection (VALIDATION row 12)
       - Test: telemetry `:start`/`:stop` events emit with destination metadata — use `:telemetry_test.attach_event_handlers/2`
       - Test: `separate_charge_and_transfer/2` issues TWO API calls (VALIDATION row 13) — count Fake method invocations (`Fake.call_count(:create_charge)`, `Fake.call_count(:create_transfer)` — add these helpers if missing)
       - Test: `separate_charge_and_transfer/2` returns `%{charge: ..., transfer: ...}` tagged `:ok`
       - Test: when transfer fails after charge succeeds, returns `{:error, {:transfer_failed, charge, err}}`
    5. Create `accrue/test/accrue/connect/transfer_test.exs`:
       - Test: `transfer/2` round-trips through Fake (VALIDATION row 14)
       - Test: `transfer/2` records an events ledger row via `Accrue.Events.record/1`
       - Test: `transfer/2` telemetry span emits
       - Test: `transfer!/2` bang variant raises on error
  </action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/connect/charges_test.exs test/accrue/connect/transfer_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def destination_charge' accrue/lib/accrue/connect.ex`
    - `grep -q 'def separate_charge_and_transfer' accrue/lib/accrue/connect.ex`
    - `grep -q 'def transfer' accrue/lib/accrue/connect.ex`
    - `grep -q 'transfer_data' accrue/lib/accrue/connect.ex`
    - `grep -q 'application_fee_amount' accrue/lib/accrue/connect.ex`
    - `grep -q ':accrue, :connect, :destination_charge' accrue/lib/accrue/connect.ex`
    - `grep -q 'def create_transfer' accrue/lib/accrue/processor/stripe.ex`
    - `grep -q 'def create_transfer' accrue/lib/accrue/processor/fake.ex`
    - `cd accrue && mix test test/accrue/connect/charges_test.exs test/accrue/connect/transfer_test.exs` exits 0
    - VALIDATION.md rows 11, 12, 13, 14 pass
  </acceptance_criteria>
  <done>CONN-04 + CONN-05 shipped: both charge flows + standalone transfer helper work end-to-end through Fake with correct platform-scoping and telemetry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| destination_charge → Stripe | Stripe-Account header must NOT be set (body contains destination) |
| transfer → Stripe | Platform authority call; stripe_account nil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Elevation of Privilege | destination_charge with Stripe-Account header | mitigate | `force_platform(opts)` in all three functions overrides any pdict/config stripe_account (Pitfall 2). Test asserts the header is unset even inside `with_account`. |
| T-05-05-02 | Tampering | application_fee_amount injected | mitigate | Fee is caller-injected via explicit opt; not computed inside the helper (auditability per D5-04). Telemetry :stop event logs the fee value. |
| T-05-05-03 | Repudiation | charge without matching Transfer | mitigate | `separate_charge_and_transfer/2` returns `{:error, {:transfer_failed, charge, err}}` so caller can reconcile. Phase 5 events ledger records partial state. |
</threat_model>

<verification>
- Mox/Fake inspection confirms stripe_account is nil at adapter boundary
- Telemetry events emit correctly
- Events ledger rows created for each operation (EVT-04)
</verification>

<success_criteria>
- CONN-04 demonstrable via Fake and (Plan 07) live_stripe smoke test
- CONN-05 demonstrable with distinct two-call shape
- No accidental header-scoping of platform-only calls
</success_criteria>

<output>
After completion, create `.planning/phases/05-connect/05-05-SUMMARY.md`
</output>

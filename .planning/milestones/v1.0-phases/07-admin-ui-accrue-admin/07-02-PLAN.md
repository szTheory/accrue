---
phase: 07-admin-ui-accrue-admin
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/assets/css/theme.css
  - accrue_admin/assets/js/app.js
  - accrue_admin/assets/js/hooks/accrue_theme.js
  - accrue_admin/assets/tailwind.config.js
  - accrue_admin/assets/tailwind_preset.js
  - accrue_admin/lib/accrue_admin/layouts.ex
  - accrue_admin/lib/accrue_admin/components/app_shell.ex
  - accrue_admin/lib/accrue_admin/components/sidebar.ex
  - accrue_admin/lib/accrue_admin/components/topbar.ex
  - accrue_admin/lib/accrue_admin/csp_plug.ex
  - accrue_admin/lib/accrue_admin/brand_plug.ex
  - accrue_admin/test/accrue_admin/theme_test.exs
autonomous: true
requirements: [ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-06]
must_haves:
  truths:
    - "The admin shell renders as a mobile-first layout with sidebar/topbar navigation that adapts cleanly below `md:` and above `lg:`"
    - "Theme selection persists across requests via the `accrue_theme` cookie and root-layout anti-FOUC ordering"
    - "Brand colors, logo, and shell chrome read from `Accrue.Config` runtime values rather than inventing a new admin-local config authority"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/app_shell.ex"
      provides: "responsive shell and navigation frame"
      contains: "def app_shell"
    - path: "accrue_admin/assets/tailwind_preset.js"
      provides: "semantic theme token mapping for light/dark/admin accent use"
      contains: "accent"
    - path: "accrue_admin/test/accrue_admin/theme_test.exs"
      provides: "theme persistence and anti-FOUC regression coverage"
      contains: "accrue_theme"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/layouts.ex"
      to: "accrue_admin/assets/js/hooks/accrue_theme.js"
      via: "anti-FOUC script and runtime theme toggle"
      pattern: "data-theme"
---

<objective>
Build the admin visual foundation for `accrue_admin`: semantic theme tokens, anti-FOUC dark-mode support, and the responsive root shell/navigation frame that every later page and component plan sits inside.

Purpose: the prior revision bundled theme work and most shared components into one plan. This revision narrows 07-02 to the package-owned layout and theming substrate so the later shared-component plan can stay independently executable.

Output: stable shell, theme pipeline, root layout ordering guarantees, and router-owned branding/CSP plugs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-ui-accrue-admin/07-CONTEXT.md
@.planning/phases/07-admin-ui-accrue-admin/07-UI-SPEC.md
@.planning/phases/07-admin-ui-accrue-admin/07-RESEARCH.md
@.planning/phases/07-admin-ui-accrue-admin/07-PATTERNS.md
@accrue/priv/static/brand.css
@accrue/lib/accrue/config.ex
@accrue/lib/accrue/invoices/components.ex
@accrue/lib/accrue/invoices/layouts.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the admin theme pipeline and responsive root shell</name>
  <files>accrue_admin/assets/css/app.css, accrue_admin/assets/css/theme.css, accrue_admin/assets/js/app.js, accrue_admin/assets/js/hooks/accrue_theme.js, accrue_admin/assets/tailwind.config.js, accrue_admin/assets/tailwind_preset.js, accrue_admin/lib/accrue_admin/layouts.ex, accrue_admin/lib/accrue_admin/components/app_shell.ex, accrue_admin/lib/accrue_admin/components/sidebar.ex, accrue_admin/lib/accrue_admin/components/topbar.ex, accrue_admin/lib/accrue_admin/csp_plug.ex, accrue_admin/lib/accrue_admin/brand_plug.ex</files>
  <action>Implement D7-08 and the shell portion of D7-03 exactly: package-private Tailwind config, semantic CSS tokens layered over the Accrue palette, `data-theme` with `light|dark|system`, cookie-backed persistence, and inline anti-FOUC ordering inside the root layout. Add the concrete `AccrueAdmin.CSPPlug` and `AccrueAdmin.BrandPlug` deferred from 07-01 so the shell, theme preload path, and branding assigns all live behind one router-owned boundary. Build the mobile-first shell with sidebar/topbar structure and explicit text labels for any icon-only controls per the UI spec. Read app name/logo/accent from `Accrue.Config` runtime branding; do not add DB-backed branding state. Keep `AppShell` responsible for layout composition only, not page-local query loading, command-palette work, or other shared page widgets.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/theme_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The admin root layout and shell meet the theme, contrast, and responsive navigation contracts without relying on host assets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime branding config -> rendered admin shell | Host-owned branding values cross into CSP-constrained HTML and CSS |
| browser theme cookie -> root layout | Client-controlled cookie influences root layout state before first paint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | theme cookie and inline script | mitigate | Allow only `light|dark|system` values; default invalid values to `system` in both JS and server render paths |
| T-07-02-02 | Information Disclosure | runtime brand/logo rendering | mitigate | Treat branding as display-only strings/URLs and emit through escaped HEEx assigns with CSP nonce-protected inline blocks |
| T-07-02-03 | Denial of Service | component sprawl across later pages | accept | Keep components pure and narrowly scoped so page plans reuse them instead of duplicating incompatible variants |
</threat_model>

<verification>
- Theme tests prove cookie persistence and anti-FOUC ordering
</verification>

<success_criteria>
- `accrue_admin` has a real visual system and root shell before shared components and pages land
- Mobile-first/light-dark/brand requirements are grounded behind one shell/CSP/branding boundary without overloading one plan with all shared components
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-accrue-admin/07-02-SUMMARY.md`
</output>

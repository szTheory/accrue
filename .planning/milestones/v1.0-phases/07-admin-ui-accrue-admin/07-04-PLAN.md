---
phase: 07-admin-ui-accrue-admin
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - accrue/lib/accrue/auth.ex
  - accrue/lib/accrue/events/event.ex
  - accrue/lib/accrue/events.ex
  - accrue/priv/repo/migrations/20260415091000_add_admin_causality_to_events.exs
  - accrue_admin/lib/accrue_admin/auth_hook.ex
  - accrue_admin/lib/accrue_admin/step_up.ex
  - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
  - accrue_admin/test/accrue_admin/live/auth_hook_test.exs
  - accrue_admin/test/accrue_admin/live/step_up_test.exs
  - accrue/test/accrue/events/admin_causality_test.exs
autonomous: true
requirements: [AUTH-03, EVT-09, ADMIN-21, ADMIN-22, ADMIN-23, ADMIN-26]
must_haves:
  truths:
    - "Admin LiveViews are blocked at mount time for non-admin sessions instead of waiting for event handlers"
    - "Destructive admin actions can require step-up auth with a grace window and challenge/verify callbacks"
    - "Admin actions write `accrue_events` rows with `actor_type: admin` and causal linkage to prior billing or webhook events"
    - "Sigra-backed hosts keep seeing admin audit activity through the existing first-party adapter rather than a parallel auth path"
  artifacts:
    - path: "accrue/priv/repo/migrations/20260415091000_add_admin_causality_to_events.exs"
      provides: "event ledger columns and indexes for admin/webhook causality"
      contains: "caused_by_event_id"
    - path: "accrue_admin/lib/accrue_admin/step_up.ex"
      provides: "step-up grace-window and challenge/verification workflow"
      contains: "require_fresh"
    - path: "accrue_admin/lib/accrue_admin/auth_hook.ex"
      provides: "on_mount admin enforcement"
      contains: "on_mount"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/auth_hook.ex"
      to: "Accrue.Auth"
      via: "mount-time admin enforcement"
      pattern: "current_user"
    - from: "accrue_admin/lib/accrue_admin/step_up.ex"
      to: "accrue/lib/accrue/events.ex"
      via: "admin action and step-up audit writes"
      pattern: "record"
---

<objective>
Land the auth, step-up, and audit seams required by every destructive admin action: mount-time auth enforcement, optional step-up callbacks, causal event-link support, and Sigra bridge completion.

Purpose: page plans must not invent their own auth or audit logic. This plan creates the shared enforcement and event-link substrate once, then later pages consume it.

Output: auth hook, step-up flow, event causality support, and Sigra audit bridge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-ui-accrue-admin/07-CONTEXT.md
@.planning/phases/07-admin-ui-accrue-admin/07-RESEARCH.md
@accrue/lib/accrue/auth.ex
@accrue/lib/accrue/events.ex
@accrue/lib/accrue/events/event.ex
@accrue/lib/accrue/webhooks/dlq.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the core auth and event-ledger seams for admin step-up and causality</name>
  <files>accrue/lib/accrue/auth.ex, accrue/lib/accrue/events/event.ex, accrue/lib/accrue/events.ex, accrue/priv/repo/migrations/20260415091000_add_admin_causality_to_events.exs, accrue/test/accrue/events/admin_causality_test.exs</files>
  <action>Implement the additive core seams required by D7-05 and D7-06. Extend `Accrue.Auth` with optional step-up callbacks and a conservative default behavior surface: dev/test can auto-approve through the shared admin flow, while prod defaults fail closed until a real adapter is configured. Add causal event support to `accrue_events` with nullable linkage fields and indexes for admin-originated actions and webhook-derived follow-ons, and extend `Accrue.Events.record/1` normalization to accept those fields without breaking prior plans. Preserve the already-resolved Sigra path by making callback additions optional and relying on the existing first-party adapter shape unless execution proves a tiny compile-fix is required. Keep the scope repo-grounded: add only the causal fields needed by Phase 7 instead of a general event-graph subsystem.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/events/admin_causality_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Core Accrue exposes the auth and event hooks the admin UI needs, with causal linkage and Sigra bridge support in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement admin mount-time auth enforcement and the shared step-up workflow</name>
  <files>accrue_admin/lib/accrue_admin/auth_hook.ex, accrue_admin/lib/accrue_admin/step_up.ex, accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex, accrue_admin/test/accrue_admin/live/auth_hook_test.exs, accrue_admin/test/accrue_admin/live/step_up_test.exs</files>
  <action>Implement `AccrueAdmin.AuthHook` as the `live_session` gate referenced in Plan 01 so ADMIN-26 is enforced before render. Build the reusable step-up flow service and modal per D7-05, including grace-window storage, challenge presentation, challenge verification, and separate `admin.step_up.*` audit events that do not pollute billing events. Keep the step-up surface generic so later page plans can wrap refunds, cancels, voids, manual pay, and bulk replay without copying logic. Use the new event causality support from Task 1 when an admin action is tied to a source event.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/auth_hook_test.exs test/accrue_admin/live/step_up_test.exs --warnings-as-errors</automated>
  </verify>
  <done>All later destructive admin flows can require fresh auth and write causally linked audit rows using one shared implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host auth adapter -> admin on_mount | Host-owned user/session state determines access to the billing admin |
| destructive admin action -> event ledger and Sigra audit | High-risk actions cross from UI intent into durable audit records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation of Privilege | `AccrueAdmin.AuthHook` | mitigate | Enforce admin checks in `on_mount`, not only in events; test non-admin rejection before render |
| T-07-04-02 | Repudiation | admin action audit trail | mitigate | Record `actor_type`, `actor_id`, source-event linkage, and step-up outcomes in `accrue_events` |
| T-07-04-03 | Spoofing | step-up challenge flow | mitigate | Route all verification through adapter callbacks plus server-side grace-window timestamps; never trust client-side modal state alone |
</threat_model>

<verification>
- Core causality test passes in `accrue`
- Admin auth/step-up tests pass in `accrue_admin`
</verification>

<success_criteria>
- Page plans can use one shared auth/step-up/audit implementation
- AUTH-03, EVT-09, and the admin audit requirements are executable rather than hand-waved
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-accrue-admin/07-04-SUMMARY.md`
</output>

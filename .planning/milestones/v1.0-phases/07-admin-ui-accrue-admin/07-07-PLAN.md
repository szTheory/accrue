---
phase: 07-admin-ui-accrue-admin
plan: 07
type: execute
wave: 5
depends_on: [07-03, 07-04, 07-09, 07-10, 07-11]
files_modified:
  - accrue_admin/lib/accrue_admin/queries/webhooks.ex
  - accrue_admin/lib/accrue_admin/queries/events.ex
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/lib/accrue_admin/live/webhook_live.ex
  - accrue_admin/lib/accrue_admin/live/events_live.ex
  - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhook_replay_test.exs
  - accrue_admin/test/accrue_admin/live/events_live_test.exs
autonomous: true
requirements: [ADMIN-16, ADMIN-17, ADMIN-18]
must_haves:
  truths:
    - "Webhook events can be listed, filtered, inspected, and replayed from the admin UI using existing DLQ primitives"
    - "Webhook detail shows raw payload, signature verification status, attempt history, and derived-event linkage without leaking secrets"
    - "A global activity feed exists on top of `accrue_events`, complementing the per-customer/per-subscription event tabs"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/queries/webhooks.ex"
      provides: "ops-focused webhook list and detail query module"
      contains: "def list"
    - path: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      provides: "webhook list and filter surface"
      contains: "DLQ"
    - path: "accrue_admin/lib/accrue_admin/live/webhook_live.ex"
      provides: "webhook inspector detail and replay actions"
      contains: "Replay"
    - path: "accrue_admin/lib/accrue_admin/live/events_live.ex"
      provides: "global activity feed"
      contains: "timeline"
  key_links:
    - from: "webhook replay UI"
      to: "Accrue.Webhooks.DLQ.requeue/1 and requeue_where/2"
      via: "one-click single replay and bulk requeue"
      pattern: "requeue_where"
    - from: "webhook detail"
      to: "causal event fields added in Plan 04"
      via: "derived events tab and source-event linking"
      pattern: "caused_by"
---

<objective>
Implement the webhook inspector, replay flows, and global activity feed.

Purpose: this is the most operations-heavy admin slice and the main place where event causality, JSON inspection, and DLQ tooling become visible to operators.

Output: ops-focused webhook/event query modules plus webhook list/detail/replay pages and a global events feed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-ui-accrue-admin/07-CONTEXT.md
@.planning/phases/07-admin-ui-accrue-admin/07-RESEARCH.md
@accrue/lib/accrue/webhooks/dlq.ex
@accrue/lib/accrue/webhook/webhook_event.ex
@accrue/lib/accrue/events.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the webhook inspector list/detail surfaces with payload and attempt visibility</name>
  <files>accrue_admin/lib/accrue_admin/queries/webhooks.ex, accrue_admin/lib/accrue_admin/live/webhooks_live.ex, accrue_admin/lib/accrue_admin/live/webhook_live.ex, accrue_admin/test/accrue_admin/live/webhooks_live_test.exs, accrue_admin/test/accrue_admin/live/webhook_live_test.exs</files>
  <action>Implement ADMIN-16 end to end for the ops slice: add the webhook query module deferred from 07-03, then consume it from the shared query layer, `DataTable`, `JsonViewer`, and `Timeline` components. Webhook list filters must use the existing `WebhookEvent.status`, `type`, and natural received time fields. Detail must show raw payload via the stored `raw_body`/`data` path, signature verification outcome, attempt history, and derived events via the causality support added in Plan 04. Preserve repo security posture: surface verification result and forensic payloads without ever rendering signing secrets or reintroducing inspect-redacted fields.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhooks_live_test.exs test/accrue_admin/live/webhook_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Operators can inspect webhook failures and derived billing events from one coherent surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire webhook replay flows and the global activity feed</name>
  <files>accrue_admin/lib/accrue_admin/queries/events.ex, accrue_admin/lib/accrue_admin/live/events_live.ex, accrue_admin/test/accrue_admin/live/webhook_replay_test.exs, accrue_admin/test/accrue_admin/live/events_live_test.exs</files>
  <action>Implement ADMIN-17 and global ADMIN-18 views. Add the events query module deferred from 07-03, then use `Accrue.Webhooks.DLQ.requeue/1` for single replay and `requeue_where/2` for bulk DLQ requeue; do not create a second replay queue or parallel replay subsystem. Follow D7-06 UX rules: one-click single replay, confirmation for bulk, and one audit event per bulk intent rather than per row. Build a global activity feed page on top of `AccrueAdmin.Queries.Events` so operators have both scoped event tabs and an all-events operations view.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhook_replay_test.exs test/accrue_admin/live/events_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Webhook replay and global activity feed requirements are implemented against existing Accrue primitives with no duplicated backend behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook inspector -> raw payload rendering | Sensitive inbound event data becomes operator-visible JSON/tree UI |
| replay UI -> DLQ replay APIs | Admin-triggered replay requests enqueue new webhook processing work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Information Disclosure | raw payload viewer | mitigate | Reuse stored redacted event structures and never display signing secrets or inspect-redacted fields |
| T-07-07-02 | Denial of Service | bulk replay UI | mitigate | Use existing DLQ batch/stagger/max-row controls and require bulk confirmation |
| T-07-07-03 | Repudiation | replay actions | mitigate | Record one causal admin audit event for each single or bulk replay intent through the Phase 04 audit seam |
</threat_model>

<verification>
- Webhook list/detail tests pass
- Replay and global events tests pass
</verification>

<success_criteria>
- Operators can inspect failures, replay them, and follow resulting event chains end-to-end
- Webhook inspector/DLQ requirements are covered without inventing new backend plumbing
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-accrue-admin/07-07-SUMMARY.md`
</output>

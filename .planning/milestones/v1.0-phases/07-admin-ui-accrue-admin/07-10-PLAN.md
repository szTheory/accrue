---
phase: 07-admin-ui-accrue-admin
plan: 10
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex
  - accrue_admin/lib/accrue_admin/components/detail_drawer.ex
  - accrue_admin/lib/accrue_admin/components/kpi_card.ex
  - accrue_admin/lib/accrue_admin/components/timeline.ex
  - accrue_admin/lib/accrue_admin/components/json_viewer.ex
  - accrue_admin/lib/accrue_admin/components/money_formatter.ex
  - accrue_admin/test/accrue_admin/components/display_components_test.exs
autonomous: true
requirements: [ADMIN-27]
must_haves:
  truths:
    - "Operators can read KPI cards, timelines, detail drawers, filter chips, JSON payloads, and money values through one consistent admin display layer"
    - "Webhook and billing detail pages can show structured payloads and timelines without page-specific widget forks"
    - "Money and status-adjacent formatting stay aligned with Phase 6 locale decisions and the locked admin visual language"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/detail_drawer.ex"
      provides: "shared full-screen and side-sheet detail drawer primitive"
      contains: "def detail_drawer"
    - path: "accrue_admin/lib/accrue_admin/components/json_viewer.ex"
      provides: "tree/raw/copy payload viewer primitive"
      contains: "def json_viewer"
    - path: "accrue_admin/test/accrue_admin/components/display_components_test.exs"
      provides: "focused regression coverage for display and formatting primitives"
      contains: "JsonViewer"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/money_formatter.ex"
      to: "Accrue.Config / customer locale-timezone semantics"
      via: "Phase 6 locale-aware money formatting"
      pattern: "preferred_locale"
    - from: "accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex"
      to: "AccrueAdmin.Queries.*"
      via: "URL-synced list-page filter rendering"
      pattern: "filter"
---

<objective>
Create the display and formatting half of the first-party shared component inventory.

Purpose: the prior 07-09 plan was still too wide. This revision gives the display-heavy widgets their own same-wave plan so navigation/input work and detail-display work stay independently executable.

Output: reusable display, payload-inspection, and formatting primitives plus focused component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-ui-accrue-admin/07-CONTEXT.md
@.planning/phases/07-admin-ui-accrue-admin/07-UI-SPEC.md
@.planning/phases/07-admin-ui-accrue-admin/07-RESEARCH.md
@.planning/phases/07-admin-ui-accrue-admin/07-PATTERNS.md
@accrue/lib/accrue/invoices/components.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared display primitives for filters, drawers, KPIs, and timelines</name>
  <files>accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex, accrue_admin/lib/accrue_admin/components/detail_drawer.ex, accrue_admin/lib/accrue_admin/components/kpi_card.ex, accrue_admin/lib/accrue_admin/components/timeline.ex, accrue_admin/test/accrue_admin/components/display_components_test.exs</files>
  <action>Create the higher-signal display primitives required by D7-03 and D7-06: chip filters, detail drawer, KPI cards, and timeline. `FilterChipBar` must render filter state in the format expected by later `AccrueAdmin.Queries.*` modules, and `DetailDrawer` must support the mobile-first full-screen behavior locked in the UI spec. Keep these components page-agnostic and do not fold `DataTable` into this plan.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/display_components_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Later billing pages inherit a stable shared display layer for filters, overlays, KPIs, and timelines without mixing this work back into page plans.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared JSON and money-formatting primitives for detail views</name>
  <files>accrue_admin/lib/accrue_admin/components/json_viewer.ex, accrue_admin/lib/accrue_admin/components/money_formatter.ex, accrue_admin/test/accrue_admin/components/display_components_test.exs</files>
  <action>Create the payload and formatting primitives required by D7-06: `JsonViewer` with tree/raw/copy tabs and `MoneyFormatter` that respects the Phase 6 locale/timezone decisions. Keep payload rendering escaped and explicit, and keep formatting logic shared so later invoice, charge, and webhook pages do not fork money-display helpers.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/display_components_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Later billing pages can inspect payloads and render money consistently without introducing page-specific viewer or formatter code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin page assigns -> shared display components | Server-derived billing data becomes reusable UI primitives across many pages |
| JSON payloads -> `JsonViewer` and timeline components | Potentially sensitive event data is rendered in operator-visible widgets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Information Disclosure | `JsonViewer` and `Timeline` | mitigate | Render escaped, explicit fields only; later pages must not dump whole structs or redacted secrets into shared components |
| T-07-10-02 | Tampering | filter chip rendering | mitigate | Keep chip metadata driven by server-side assigns from `AccrueAdmin.Queries.*`, not client-authored HTML |
| T-07-10-03 | Denial of Service | overgrown component interfaces | accept | Keep component contracts narrow and page-agnostic so later plans reuse them instead of creating divergent copies |
</threat_model>

<verification>
- Display/formatting component tests pass
</verification>

<success_criteria>
- The display/formatting half of ADMIN-27 is covered by concrete shared components
- The previous oversized shared-component plan is reduced without losing executor-ready detail
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-accrue-admin/07-10-SUMMARY.md`
</output>

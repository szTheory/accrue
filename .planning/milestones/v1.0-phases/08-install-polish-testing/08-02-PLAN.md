---
phase: 08-install-polish-testing
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - accrue/mix.exs
  - accrue/lib/mix/tasks/accrue.install.ex
  - accrue/lib/accrue/install/options.ex
  - accrue/lib/accrue/install/project.ex
  - accrue/lib/accrue/install/templates.ex
  - accrue/lib/accrue/install/fingerprints.ex
  - accrue/priv/accrue/templates/install/billing.ex.eex
  - accrue/priv/accrue/templates/install/billing_handler.ex.eex
  - accrue/priv/accrue/templates/install/runtime_config.exs.eex
  - accrue/priv/accrue/templates/install/revoke_accrue_events_writes.exs.eex
autonomous: true
requirements: [INST-01, INST-02, INST-05, INST-07, INST-09, INST-10]
must_haves:
  truths:
    - "A Phoenix developer can run `mix accrue.install --dry-run` and see planned generated files without mutation."
    - "A generated host app gets migrations, `MyApp.Billing`, and config validation/doc output."
    - "A generated host app gets a Stripe test-mode runtime configuration path that reads secrets from env vars and reports readiness without printing secret values."
    - "Installer re-runs distinguish pristine Accrue-owned files from edited host files."
  artifacts:
    - path: "accrue/lib/mix/tasks/accrue.install.ex"
      provides: "Installer CLI entrypoint"
    - path: "accrue/lib/accrue/install/fingerprints.ex"
      provides: "Generated-file no-clobber policy"
    - path: "accrue/priv/accrue/templates/install/billing.ex.eex"
      provides: "Host-owned Billing facade template"
    - path: "accrue/priv/accrue/templates/install/runtime_config.exs.eex"
      provides: "Stripe test-mode runtime config snippet template"
  key_links:
    - from: "accrue/lib/mix/tasks/accrue.install.ex"
      to: "accrue/lib/accrue/install/options.ex"
      via: "strict option parsing"
      pattern: "Accrue.Install.Options"
    - from: "accrue/lib/accrue/install/templates.ex"
      to: "accrue/lib/accrue/install/fingerprints.ex"
      via: "fingerprinted writes"
      pattern: "fingerprint"
---

<objective>
Implement installer core, dependency registration, generated-file templates, and no-clobber primitives.

Purpose: provide the safe build-time foundation for `mix accrue.install` before router/admin/auth patching is added.
Output: an executable installer that can parse required flags, discover a Phoenix host app, generate migrations/context/handler files, generate or print Stripe test-mode runtime config snippets, validate planned config, and report dry-run/changed/skipped/manual items.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/mix.exs
@accrue/lib/accrue/config.ex
@accrue/lib/mix/tasks/accrue.webhooks.replay.ex
@accrue/lib/mix/tasks/accrue.mail.preview.ex
@accrue/priv/repo/migrations
@accrue/priv/accrue/templates/migrations/revoke_accrue_events_writes.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add installer dependency and strict option parsing</name>
  <files>accrue/mix.exs, accrue/lib/mix/tasks/accrue.install.ex, accrue/lib/accrue/install/options.ex</files>
  <read_first>
    - accrue/mix.exs
    - accrue/lib/mix/tasks/accrue.webhooks.replay.ex
    - accrue/lib/mix/tasks/accrue.mail.preview.ex
    - .planning/phases/08-install-polish-testing/08-RESEARCH.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
  </read_first>
  <action>Add `{:igniter, "~> 0.7.9", runtime: false}` to `deps/0` in `accrue/mix.exs` near build-time/installer dependencies. Create `Mix.Tasks.Accrue.Install` with `use Mix.Task`, `Mix.Task.run("loadpaths")`, and strict switches: `billable: :string`, `billing_context: :string`, `webhook_path: :string`, `admin_mount: :string`, `admin: :boolean`, `sigra: :boolean`, `dry_run: :boolean`, `yes: :boolean`, `non_interactive: :boolean`, `manual: :boolean`, `force: :boolean`, `write_conflicts: :boolean`. Create `Accrue.Install.Options.parse!/1` returning a struct or map with defaults `billing_context: "MyApp.Billing"`, `webhook_path: "/webhooks/stripe"`, `admin_mount: "/billing"`, `admin: :auto`, `sigra: :auto`, `dry_run: false`, `manual: false`, `force: false`, and `accept?: opts[:yes] || opts[:non_interactive]`. Unknown options must `Mix.raise/1`. Per D8-02 and D8-03, every promptable product choice must have a CLI flag.</action>
  <acceptance_criteria>
    - `rg -n "\\{:igniter, \"~> 0\\.7\\.9\", runtime: false\\}" accrue/mix.exs` returns a match.
    - `rg -n "dry_run: :boolean|yes: :boolean|non_interactive: :boolean|manual: :boolean|force: :boolean|write_conflicts: :boolean|billable: :string|billing_context: :string|webhook_path: :string|admin_mount: :string" accrue/lib/accrue/install/options.ex accrue/lib/mix/tasks/accrue.install.ex` returns matches.
    - `rg -n "Mix\\.Task\\.run\\(\"loadpaths\"\\)|Accrue\\.Install\\.Options\\.parse!" accrue/lib/mix/tasks/accrue.install.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test --only install_options test/mix/tasks/accrue_install_test.exs</automated>
  </verify>
  <done>`mix accrue.install` parses the required Phase 8 flags and `mix.exs` contains the Igniter dependency with the exact required constraint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement project discovery, config validation, templates, and fingerprints</name>
  <files>accrue/lib/accrue/install/project.ex, accrue/lib/accrue/install/templates.ex, accrue/lib/accrue/install/fingerprints.ex, accrue/priv/accrue/templates/install/billing.ex.eex, accrue/priv/accrue/templates/install/billing_handler.ex.eex, accrue/priv/accrue/templates/install/runtime_config.exs.eex, accrue/priv/accrue/templates/install/revoke_accrue_events_writes.exs.eex</files>
  <read_first>
    - accrue/lib/accrue/config.ex
    - accrue/lib/accrue/billing.ex
    - accrue/lib/accrue/webhook/handler.ex
    - accrue/priv/repo/migrations
    - accrue/priv/accrue/templates/migrations/revoke_accrue_events_writes.exs
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
  </read_first>
  <action>Create `Accrue.Install.Project.discover!/1` to identify app module, `lib/my_app_web/router.ex`, `config/config.exs`, `config/runtime.exs`, `mix.exs`, `priv/repo/migrations`, dependency presence for `:accrue_admin` and `:sigra`, and a detected billable schema when a file contains `use Accrue.Billable`. Discovery must fail to manual snippets when router/repo/app cannot be determined safely. Create `Accrue.Install.Templates` to render EEx templates for `MyApp.Billing`, `MyApp.BillingHandler`, copied `accrue/priv/repo/migrations/*.exs`, `runtime_config.exs.eex`, and `revoke_accrue_events_writes.exs.eex`. The Billing template must contain `defmodule <%= @billing_context %>`, `alias Accrue.Billing`, `def subscribe(billable, price_id, opts \\\\ [])`, `def swap_plan(subscription, price_id, opts)`, `def cancel(subscription, opts \\\\ [])`, `def customer_for(billable)`, and comments identifying host policy hooks. The runtime config template/manual snippet must contain the exact strings `config :accrue, :processor, Accrue.Processor.Stripe`, `System.fetch_env!("STRIPE_SECRET_KEY")`, and `System.get_env("STRIPE_WEBHOOK_SECRET")`, and must keep all secret material in runtime env reads rather than compile-time config. Add a Stripe test-mode readiness helper in installer code or templates that validates/report-checks the configured secret key prefix as `sk_test_` for test mode, names the webhook signing env var `STRIPE_WEBHOOK_SECRET`, accepts the webhook secret as optional with `System.get_env("STRIPE_WEBHOOK_SECRET")`, and redacts all values matching `sk_test_...`, `sk_live_...`, or `whsec_...` before any report output. Create `Accrue.Install.Fingerprints` with marker `# accrue:generated`, SHA-256 content fingerprint, `pristine?/2`, `user_edited?/2`, and write policy: update pristine generated files, skip edited files, never write secrets to report output. Call `Accrue.Config.validate!/1` against planned config and expose config docs through `NimbleOptions.docs/1` or existing `Accrue.Config` docs generation for INST-09/INST-10.</action>
  <acceptance_criteria>
    - `rg -n "def discover!|use Accrue\\.Billable|:accrue_admin|:sigra|router\\.ex|runtime\\.exs" accrue/lib/accrue/install/project.ex` returns matches.
    - `rg -n "# accrue:generated|SHA256|:crypto\\.hash|pristine\\?|user_edited\\?|skip" accrue/lib/accrue/install/fingerprints.ex` returns matches.
    - `rg -n "defmodule <%= @billing_context %>|def subscribe\\(billable, price_id, opts|def swap_plan\\(subscription, price_id, opts\\)|def customer_for\\(billable\\)" accrue/priv/accrue/templates/install/billing.ex.eex` returns matches.
    - `rg -n "config :accrue, :processor, Accrue\\.Processor\\.Stripe|System\\.fetch_env!\\(\"STRIPE_SECRET_KEY\"\\)|System\\.get_env\\(\"STRIPE_WEBHOOK_SECRET\"\\)" accrue/priv/accrue/templates/install/runtime_config.exs.eex accrue/lib/accrue/install` returns matches.
    - `rg -n "sk_test_|sk_live_|whsec_|redact|Stripe test-mode|STRIPE_WEBHOOK_SECRET" accrue/lib/accrue/install accrue/priv/accrue/templates/install/runtime_config.exs.eex` returns matches.
    - `rg -n "Accrue\\.Config\\.validate!|NimbleOptions\\.docs|config docs" accrue/lib/accrue/install` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test --only install_templates test/mix/tasks/accrue_install_test.exs && mix test --only install_stripe_test_mode test/mix/tasks/accrue_install_test.exs</automated>
  </verify>
  <done>Installer core can render fingerprinted host-owned files, prove no-clobber/config validation behavior, and generate/print Stripe test-mode runtime config snippets without leaking raw secret values under the Wave 0 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem | Installer reads and writes host app files owned by the developer. |
| host config | Installer reports planned config that may reference env vars and secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `Accrue.Install.Fingerprints` | mitigate | Use `# accrue:generated` plus SHA-256 fingerprints; update only pristine generated files; skip edited host files even when `--force` is present. |
| T-08-02 | Information Disclosure | installer reports | mitigate | Redact `sk_*`, `whsec_*`, `_SECRET`, `_KEY`, raw body, and env values before printing changed/skipped/manual output. |
| T-08-06 | Information Disclosure | Stripe test-mode runtime snippets | mitigate | Runtime config reads `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` from env vars, validates test-mode prefixes for readiness, and reports only env-var names plus redacted key classification. |
</threat_model>

<verification>
Per-task checks intentionally use the Wave 0 tags from Plan 08-01 so each task verifies only the contract it completes:
- Task 1: `cd accrue && mix test --only install_options test/mix/tasks/accrue_install_test.exs`
- Task 2: `cd accrue && mix test --only install_templates test/mix/tasks/accrue_install_test.exs && mix test --only install_stripe_test_mode test/mix/tasks/accrue_install_test.exs`

After both tasks complete, run both tagged commands above; the full installer orchestration contract is owned by Plan 08-03 Task 2.
</verification>

<success_criteria>
Installer core satisfies INST-01, INST-02, INST-05, INST-07, INST-09, and INST-10 tests from Wave 0, with `:igniter` added exactly as `{:igniter, "~> 0.7.9", runtime: false}` and Stripe test-mode readiness snippets generated or printed with no raw secret leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-02-SUMMARY.md`.
</output>

---
phase: 08-install-polish-testing
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - accrue/lib/accrue/test.ex
  - accrue/lib/accrue/test/clock.ex
  - accrue/lib/accrue/test/webhooks.ex
autonomous: true
requirements: [TEST-02, TEST-03, TEST-07]
must_haves:
  truths:
    - "Host tests can `use Accrue.Test` and call action helpers as functions."
    - "`advance_clock/2` advances Fake/test clocks without sleeps and returns useful lifecycle results."
    - "`trigger_event/2` exercises normal webhook ingestion/handler behavior."
  artifacts:
    - path: "accrue/lib/accrue/test.ex"
      provides: "Public testing facade"
    - path: "accrue/lib/accrue/test/clock.ex"
      provides: "Clock advancement helper"
    - path: "accrue/lib/accrue/test/webhooks.ex"
      provides: "Synthetic webhook trigger helper"
  key_links:
    - from: "accrue/lib/accrue/test/clock.ex"
      to: "accrue/lib/accrue/processor/fake.ex"
      via: "advance/2 and advance_subscription/2"
      pattern: "Accrue.Processor.Fake"
    - from: "accrue/lib/accrue/test/webhooks.ex"
      to: "accrue/lib/accrue/webhook/ingest.ex"
      via: "normal webhook ingestion path"
      pattern: "Accrue.Webhook"
---

<objective>
Ship the public `Accrue.Test` facade and action helpers for deterministic clock and webhook behavior.

Purpose: let host apps test billing flows locally without Stripe network calls or sleeps while still exercising production-shaped webhook logic.
Output: `Accrue.Test`, `Accrue.Test.Clock`, and `Accrue.Test.Webhooks`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/lib/accrue/processor/fake.ex
@accrue/lib/accrue/webhook/ingest.ex
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/test/mailer_assertions.ex
@accrue/lib/accrue/test/pdf_assertions.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `Accrue.Test` facade and setup imports</name>
  <files>accrue/lib/accrue/test.ex</files>
  <read_first>
    - accrue/lib/accrue/test.ex
    - accrue/lib/accrue/test/mailer_assertions.ex
    - accrue/lib/accrue/test/pdf_assertions.ex
    - accrue/lib/accrue/test/factory.ex
    - accrue/lib/accrue/test/generators.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
  </read_first>
  <action>Create `Accrue.Test` with `defmacro __using__(_opts)` importing only helper modules that exist in this plan: `Accrue.Test.MailerAssertions` and `Accrue.Test.PdfAssertions`, and importing or aliasing action helpers so host code can call `advance_clock/2` and `trigger_event/2` directly. Do not import `Accrue.Test.EventAssertions` here because Plan 08-05 creates that module and adds the facade import after it exists. Add public delegates `defdelegate advance_clock(subject, duration), to: Accrue.Test.Clock, as: :advance` and `defdelegate trigger_event(type, subject), to: Accrue.Test.Webhooks, as: :trigger`. Include setup helper functions `setup_fake_processor/1`, `setup_mailer_test/1`, and `setup_pdf_test/1` that set application env to `Accrue.Processor.Fake`, `Accrue.Mailer.Test`, and `Accrue.PDF.Test` and return `:ok` or `{:ok, keyword}` without copying helper internals into host apps. Per D8-14/D8-15, action helpers are functions, not macros. Verify only the `:facade_core` tag from Wave 0 here; `:facade_actions` is owned by Task 2 and `:facade_side_effects` is owned by Plan 08-05.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Accrue\\.Test|defmacro __using__|Accrue\\.Test\\.MailerAssertions|Accrue\\.Test\\.PdfAssertions|defdelegate advance_clock|defdelegate trigger_event|setup_fake_processor|setup_mailer_test|setup_pdf_test" accrue/lib/accrue/test.ex` returns matches.
    - `rg -n "Accrue\\.Test\\.EventAssertions" accrue/lib/accrue/test.ex` returns no matches until Plan 08-05.
    - `rg -n "defmacro advance_clock|defmacro trigger_event" accrue/lib/accrue/test.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test --only facade_core test/accrue/test/facade_test.exs</automated>
  </verify>
  <done>`use Accrue.Test` exposes all public helpers required by TEST-07 while preserving modular internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic clock and webhook action helpers</name>
  <files>accrue/lib/accrue/test/clock.ex, accrue/lib/accrue/test/webhooks.ex</files>
  <read_first>
    - accrue/lib/accrue/test/clock.ex
    - accrue/lib/accrue/test/webhooks.ex
    - accrue/lib/accrue/processor/fake.ex
    - accrue/lib/accrue/webhook/ingest.ex
    - accrue/lib/accrue/webhook/default_handler.ex
    - accrue/lib/accrue/webhook/signature.ex
    - accrue/lib/accrue/webhook/event.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-RESEARCH.md
  </read_first>
  <action>Create `Accrue.Test.Clock.advance/2` accepting readable binaries `"1 month"`, `"30 days"`, `"1 day"`, `"12 hours"`, precise keywords `[months: n]`, `[days: n]`, `[hours: n]`, `[minutes: n]`, `[seconds: n]`, and integer seconds. Convert to deterministic Fake calls using `Accrue.Processor.Fake.advance/2` for global clock and `Accrue.Processor.Fake.advance_subscription/2` when the subject has `processor_id`/`stripe_id`; return `{:ok, result}` or `{:error, reason}` with no `Process.sleep`. Create `Accrue.Test.Webhooks.trigger/2` accepting atom types such as `:invoice_payment_failed`, `:invoice_paid`, `:subscription_created`, `:subscription_updated`, and `:charge_succeeded`; synthesize an Accrue/Stripe-shaped event via existing Fake/event helpers where available and route through `Accrue.Webhook.Ingest` plus `Accrue.Webhook.DefaultHandler`/dispatch behavior instead of directly changing billing rows. Per D8-19/D8-20 and T-08-03, expose lifecycle effects from trial ending, renewal, invoice finalization, dunning, and cancellation through the normal reducer path.</action>
  <acceptance_criteria>
    - `rg -n "\"1 month\"|\"30 days\"|months:|days:|hours:|minutes:|seconds:|Accrue\\.Processor\\.Fake\\.advance|Accrue\\.Processor\\.Fake\\.advance_subscription" accrue/lib/accrue/test/clock.ex` returns matches.
    - `rg -n "Process\\.sleep" accrue/lib/accrue/test/clock.ex accrue/lib/accrue/test/webhooks.ex` returns no matches.
    - `rg -n "invoice_payment_failed|invoice_paid|subscription_created|subscription_updated|charge_succeeded|Accrue\\.Webhook\\.Ingest|Accrue\\.Webhook\\.DefaultHandler" accrue/lib/accrue/test/webhooks.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/test/clock_test.exs test/accrue/test/webhooks_test.exs && mix test --only facade_actions test/accrue/test/facade_test.exs</automated>
  </verify>
  <done>Clock and webhook helpers pass tests and do not bypass Fake/production webhook behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host test process to Fake processor | Test helper controls local billing state and event synthesis. |
| synthetic event to webhook reducer | Helper crosses into production webhook ingestion and handler code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | `Accrue.Test.Webhooks.trigger/2` | mitigate | Route synthetic events through `Accrue.Webhook.Ingest`/handler path; do not mutate billing schemas directly. |
</threat_model>

<verification>
Per-task facade checks intentionally use the Wave 0 tags from Plan 08-01 so Plan 08-04 never verifies event assertions before Plan 08-05 creates them:
- Task 1: `cd accrue && mix test --only facade_core test/accrue/test/facade_test.exs`
- Task 2: `cd accrue && mix test test/accrue/test/clock_test.exs test/accrue/test/webhooks_test.exs && mix test --only facade_actions test/accrue/test/facade_test.exs`

After Plan 08-05 imports `Accrue.Test.EventAssertions`, run the full facade contract from that plan.
</verification>

<success_criteria>
TEST-02, TEST-03, and TEST-07 are green through the public `Accrue.Test` facade without sleeps, Stripe calls, Chrome, or SMTP.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-04-SUMMARY.md`.
</output>

---
phase: 08-install-polish-testing
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-04]
files_modified:
  - accrue/lib/accrue/test.ex
  - accrue/lib/accrue/test/mailer_assertions.ex
  - accrue/lib/accrue/test/pdf_assertions.ex
  - accrue/lib/accrue/test/event_assertions.ex
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06, TEST-07]
must_haves:
  truths:
    - "Host tests can assert email, PDF, and event side effects through one public facade."
    - "Assertions support keyword filters, partial maps, subjects, and predicates."
    - "Assertion failures show observed Accrue evidence and the matcher that failed."
  artifacts:
    - path: "accrue/lib/accrue/test/event_assertions.ex"
      provides: "Event ledger assertion macros"
    - path: "accrue/lib/accrue/test/mailer_assertions.ex"
      provides: "Enhanced email matchers"
    - path: "accrue/lib/accrue/test/pdf_assertions.ex"
      provides: "Enhanced PDF matchers"
  key_links:
    - from: "accrue/lib/accrue/test.ex"
      to: "accrue/lib/accrue/test/event_assertions.ex"
      via: "`use Accrue.Test` imports assertions"
      pattern: "EventAssertions"
    - from: "accrue/lib/accrue/test/event_assertions.ex"
      to: "accrue/lib/accrue/events.ex"
      via: "ledger query"
      pattern: "Accrue.Events"
---

<objective>
Complete assertion helpers for mail, PDF, and event ledger behavior.

Purpose: make billing side effects assertable from host tests with clear failure output and async-safe default capture behavior.
Output: event assertions plus matcher improvements wired through `Accrue.Test`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/lib/accrue/test.ex
@accrue/lib/accrue/test/mailer_assertions.ex
@accrue/lib/accrue/test/pdf_assertions.ex
@accrue/lib/accrue/events.ex
@accrue/lib/accrue/events/event.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add event ledger assertions and facade import</name>
  <files>accrue/lib/accrue/test/event_assertions.ex, accrue/lib/accrue/test.ex</files>
  <read_first>
    - accrue/lib/accrue/test/event_assertions.ex
    - accrue/lib/accrue/test.ex
    - accrue/lib/accrue/events.ex
    - accrue/lib/accrue/events/event.ex
    - accrue/lib/accrue/test/mailer_assertions.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
  </read_first>
  <action>Create `Accrue.Test.EventAssertions` with macros `assert_event_recorded/1`, `assert_event_recorded/2`, `refute_event_recorded/1`, and `refute_event_recorded/2`. Support filters `type:`, `subject:`, `subject_type:`, `subject_id:`, `actor_type:`, `data: %{}`, and `matches: fun` where a one-arity predicate receives an `%Accrue.Events.Event{}` or normalized event map. Partial map matching must check only provided keys. Subject matching must derive `subject_type` and `subject_id` from structs with `__struct__` and `id`. Failure messages must contain the matcher, observed event count, and inspected observed event types/subjects. Import this module from `Accrue.Test.__using__/1`.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Accrue\\.Test\\.EventAssertions|defmacro assert_event_recorded|defmacro refute_event_recorded|type:|subject:|subject_type:|subject_id:|actor_type:|matches:|observed event" accrue/lib/accrue/test/event_assertions.ex` returns matches.
    - `rg -n "Accrue\\.Test\\.EventAssertions" accrue/lib/accrue/test.ex` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/test/event_assertions_test.exs && mix test --only facade_side_effects test/accrue/test/facade_test.exs</automated>
  </verify>
  <done>TEST-06 is green and event assertion failures identify what Accrue observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend mail/PDF assertions with shared matcher semantics</name>
  <files>accrue/lib/accrue/test/mailer_assertions.ex, accrue/lib/accrue/test/pdf_assertions.ex</files>
  <read_first>
    - accrue/lib/accrue/test/mailer_assertions.ex
    - accrue/lib/accrue/test/pdf_assertions.ex
    - accrue/lib/accrue/mailer/test.ex
    - accrue/lib/accrue/pdf/test.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
  </read_first>
  <action>Preserve existing process-owned capture behavior. Extend matcher support so `assert_email_sent/1`, `assert_email_sent/2`, `refute_email_sent/1`, `assert_pdf_rendered/1`, and `refute_pdf_rendered/1` accept keyword filters, partial maps, and one-arity predicates via `matches:`. Email filters must include `type`, `to`, `customer_id`, and `assigns`; PDF filters must include `invoice_id`, `contains`, `opts_include`, and `matches`. Failure messages must include `Observed emails:` or `Observed PDFs:` plus the matcher that failed. Document explicit owner/global mode naming in module docs without making global state the default, per D8-18.</action>
  <acceptance_criteria>
    - `rg -n "matches:|Observed emails:|type|to|customer_id|assigns|owner|global" accrue/lib/accrue/test/mailer_assertions.ex` returns matches.
    - `rg -n "matches:|Observed PDFs:|invoice_id|contains|opts_include|owner|global" accrue/lib/accrue/test/pdf_assertions.ex` returns matches.
    - `rg -n "Process\\.put|:global" accrue/lib/accrue/test/mailer_assertions.ex accrue/lib/accrue/test/pdf_assertions.ex` does not show a new default global capture store.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/test/mailer_assertions_test.exs test/accrue/test/pdf_assertions_test.exs test/accrue/test/facade_test.exs</automated>
  </verify>
  <done>TEST-04, TEST-05, and TEST-07 are green while existing async-safe capture behavior remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process capture | Mail/PDF events arrive as test-process messages and must not leak across async tests. |
| event ledger query | Assertions read persisted events and expose failure diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | assertion failure output | mitigate | Failure messages include observed types/subjects and matcher details, not raw Stripe payloads, request bodies, API keys, or signing secrets. |
</threat_model>

<verification>
`cd accrue && mix test test/accrue/test/mailer_assertions_test.exs test/accrue/test/pdf_assertions_test.exs test/accrue/test/event_assertions_test.exs test/accrue/test/facade_test.exs`
</verification>

<success_criteria>
Mail, PDF, and event side-effect assertions are available through `Accrue.Test` and pass TEST-04, TEST-05, TEST-06, and TEST-07 contract tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-05-SUMMARY.md`.
</output>

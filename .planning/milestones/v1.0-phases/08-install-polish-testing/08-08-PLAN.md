---
phase: 08-install-polish-testing
plan: 08
type: execute
wave: 5
depends_on: [08-03, 08-05]
files_modified:
  - accrue/lib/accrue/install/patches.ex
  - accrue/test/mix/tasks/accrue_install_test.exs
autonomous: true
requirements: [TEST-04, TEST-07]
gap_closure: true
must_haves:
  truths:
    - "Generated test support routes Accrue.Mailer.deliver/2 through Accrue.Mailer.Test without real SMTP."
    - "Fresh install regression tests prove test/support/accrue_case.ex uses the behavior-layer :mailer key."
  artifacts:
    - path: "accrue/lib/accrue/install/patches.ex"
      provides: "Installer test-support snippet"
      contains: "config :accrue, :mailer, Accrue.Mailer.Test"
    - path: "accrue/test/mix/tasks/accrue_install_test.exs"
      provides: "Installer regression coverage for generated test support"
      contains: "config :accrue, :mailer, Accrue.Mailer.Test"
  key_links:
    - from: "accrue/lib/accrue/install/patches.ex"
      to: "accrue/lib/accrue/mailer.ex"
      via: "Application.get_env(:accrue, :mailer, Accrue.Mailer.Default)"
      pattern: "config :accrue, :mailer, Accrue.Mailer.Test"
---

<objective>
Close the verifier blocker where generated test support configures the mail test adapter under the wrong key.

Purpose: host apps following `mix accrue.install` must be able to assert emails through `Accrue.Mailer.Test` without SMTP, matching the actual `Accrue.Mailer` dispatch key.
Output: corrected installer snippet and a regression test that inspects generated `test/support/accrue_case.ex`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-VERIFICATION.md
@.planning/phases/08-install-polish-testing/08-REVIEW.md
@.planning/phases/08-install-polish-testing/08-03-SUMMARY.md
@.planning/phases/08-install-polish-testing/08-05-SUMMARY.md
@accrue/lib/accrue/install/patches.ex
@accrue/lib/accrue/mailer.ex
@accrue/lib/accrue/mailer/test.ex
@accrue/test/mix/tasks/accrue_install_test.exs
@accrue/test/support/install_fixture.ex

<interfaces>
From `accrue/lib/accrue/mailer.ex`:
```elixir
def impl, do: Application.get_env(:accrue, :mailer, Accrue.Mailer.Default)
```

From `accrue/lib/accrue/mailer/test.ex`:
```elixir
defmodule Accrue.Mailer.Test do
  @behaviour Accrue.Mailer
end
```

Required generated config values:
```elixir
config :accrue, :processor, Accrue.Processor.Fake
config :accrue, :mailer, Accrue.Mailer.Test
config :accrue, :pdf_adapter, Accrue.PDF.Test
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add installer regression for behavior-layer mailer config</name>
  <files>accrue/test/mix/tasks/accrue_install_test.exs</files>
  <read_first>
    - .planning/phases/08-install-polish-testing/08-VERIFICATION.md
    - .planning/phases/08-install-polish-testing/08-REVIEW.md
    - accrue/test/mix/tasks/accrue_install_test.exs
    - accrue/test/support/install_fixture.ex
  </read_first>
  <action>In the existing `@tag :install_patches` installer test that checks `test/support/accrue_case.ex`, add explicit assertions that the generated file contains `config :accrue, :mailer, Accrue.Mailer.Test` and does not contain `config :accrue, :mailer_adapter, Accrue.Mailer.Test`. Keep the existing assertions for `use Accrue.Test`, router, admin, auth, and Oban output. Run the targeted test before implementation and confirm it fails because the snippet still emits `:mailer_adapter`.</action>
  <acceptance_criteria>
    - `rg -n "config :accrue, :mailer, Accrue\\.Mailer\\.Test" accrue/test/mix/tasks/accrue_install_test.exs` returns a match.
    - `rg -n "refute_contains!\\(app, \"test/support/accrue_case.ex\", \"config :accrue, :mailer_adapter, Accrue\\.Mailer\\.Test\"\\)" accrue/test/mix/tasks/accrue_install_test.exs` returns a match.
    - Before Task 2, `cd accrue && mix test test/mix/tasks/accrue_install_test.exs --only install_patches` fails on the new assertion.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs --only install_patches</automated>
  </verify>
  <done>The installer regression describes the exact failed verification truth and fails until `patches.ex` uses `:mailer`.</done>
</task>

<task type="auto">
  <name>Task 2: Correct generated test-support mailer key</name>
  <files>accrue/lib/accrue/install/patches.ex</files>
  <read_first>
    - accrue/lib/accrue/install/patches.ex
    - accrue/lib/accrue/mailer.ex
    - accrue/lib/accrue/mailer/test.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-VERIFICATION.md
  </read_first>
  <action>In `Accrue.Install.Patches.test_support_snippet/0`, replace exactly `config :accrue, :mailer_adapter, Accrue.Mailer.Test` with `config :accrue, :mailer, Accrue.Mailer.Test`. Leave `config :accrue, :processor, Accrue.Processor.Fake`, `config :accrue, :pdf_adapter, Accrue.PDF.Test`, and `use Accrue.Test` unchanged. Do not change `Accrue.Config`'s unrelated `:mailer_adapter` key, because this gap is specifically about `Accrue.Mailer.deliver/2` dispatch through `Application.get_env(:accrue, :mailer, Accrue.Mailer.Default)`.</action>
  <acceptance_criteria>
    - `rg -n "config :accrue, :mailer, Accrue\\.Mailer\\.Test" accrue/lib/accrue/install/patches.ex` returns a match.
    - `rg -n "config :accrue, :mailer_adapter, Accrue\\.Mailer\\.Test" accrue/lib/accrue/install/patches.ex` returns no matches.
    - `rg -n "config :accrue, :processor, Accrue\\.Processor\\.Fake|config :accrue, :pdf_adapter, Accrue\\.PDF\\.Test|use Accrue\\.Test" accrue/lib/accrue/install/patches.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs --only install_patches</automated>
  </verify>
  <done>Generated `test/support/accrue_case.ex` configures the behavior-layer `:mailer` key and the installer patch tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated host test config to Accrue runtime | Host app copies generated config into `config/test.exs`; wrong keys silently route behavior to production-like defaults. |
| test adapter to side-effect assertions | `Accrue.Mailer.Test` captures process-local messages; false-positive snippets can make assertions wait on mail that was never routed there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Information Disclosure | `test_support_snippet/0` | mitigate | Configure `Accrue.Mailer.Test` through `:mailer` so host tests do not accidentally enqueue through `Accrue.Mailer.Default` and real delivery infrastructure. |
| T-08-08-02 | Repudiation | installer regression test | mitigate | Assert both the required key and absence of the rejected `:mailer_adapter` snippet so future regressions cannot pass by adding the correct line while leaving the wrong line. |
| T-08-08-03 | Tampering | generated test support | accept | The snippet is developer-controlled test support, not runtime production config; no production file mutation is introduced by this gap plan. |
</threat_model>

<must_haves>
- `accrue/lib/accrue/install/patches.ex` contains `config :accrue, :mailer, Accrue.Mailer.Test`.
- `accrue/lib/accrue/install/patches.ex` does not contain `config :accrue, :mailer_adapter, Accrue.Mailer.Test`.
- `accrue/test/mix/tasks/accrue_install_test.exs` proves generated `test/support/accrue_case.ex` uses the same `:mailer` key that `Accrue.Mailer.impl/0` reads.
</must_haves>

<verification>
Run `cd accrue && mix test test/mix/tasks/accrue_install_test.exs --only install_patches`.
</verification>

<success_criteria>
The verification gap "Fresh install test-support wiring avoids real SMTP" is closed without reopening unrelated installer, guide, OTel, auth, or helper scope.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-08-SUMMARY.md`.
</output>

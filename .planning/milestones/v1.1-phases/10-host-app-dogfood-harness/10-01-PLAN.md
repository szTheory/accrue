---
phase: 10-host-app-dogfood-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/.formatter.exs
  - examples/accrue_host/.gitignore
  - examples/accrue_host/mix.exs
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/config/dev.exs
  - examples/accrue_host/config/prod.exs
  - examples/accrue_host/config/test.exs
  - examples/accrue_host/config/runtime.exs
  - examples/accrue_host/lib/accrue_host/application.ex
  - examples/accrue_host/lib/accrue_host/repo.ex
  - examples/accrue_host/lib/accrue_host/mailer.ex
  - examples/accrue_host/lib/accrue_host_web/endpoint.ex
  - examples/accrue_host/lib/accrue_host_web/telemetry.ex
autonomous: true
requirements: [HOST-01]
must_haves:
  truths:
    - "A normal Phoenix host app exists at examples/accrue_host and can run Mix tasks as its own app."
    - "The host app owns its Repo and runtime/test config instead of borrowing private fixture state."
    - "The app compiles with local path dependencies to ../../accrue and ../../accrue_admin."
  artifacts:
    - path: "examples/accrue_host/mix.exs"
      provides: "Host Phoenix app Mix project with local sibling path dependencies"
      contains: "app: :accrue_host"
    - path: "examples/accrue_host/config/test.exs"
      provides: "Host test Repo and Fake processor wiring"
      contains: "AccrueHost.Repo"
    - path: "examples/accrue_host/lib/accrue_host/repo.ex"
      provides: "Host-owned Ecto repo"
      contains: "defmodule AccrueHost.Repo"
  key_links:
    - from: "examples/accrue_host/mix.exs"
      to: "../../accrue"
      via: "path dependency"
      pattern: "path: \"../../accrue\""
    - from: "examples/accrue_host/config/test.exs"
      to: "AccrueHost.Repo"
      via: "accrue repo config"
      pattern: "repo: AccrueHost.Repo"
---

<objective>
Create the core Phoenix host-app scaffold and baseline config for Phase 10.

Purpose: per D-01, D-02, and D-03, the dogfood proof starts with a real Phoenix app at `examples/accrue_host` using local path dependencies and host-owned Repo/config wiring rather than private fixture state.

Output: the generated Phoenix app foundation, path deps to `../../accrue` and `../../accrue_admin`, and host-owned config/application files that can boot Mix tasks.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
@.planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
@.planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
@.planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
@accrue_admin/config/test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the Phoenix host app foundation with sibling path dependencies</name>
  <files>examples/accrue_host/.formatter.exs, examples/accrue_host/.gitignore, examples/accrue_host/mix.exs, examples/accrue_host/config/config.exs, examples/accrue_host/config/dev.exs, examples/accrue_host/config/prod.exs, examples/accrue_host/config/test.exs, examples/accrue_host/config/runtime.exs, examples/accrue_host/lib/accrue_host/application.ex, examples/accrue_host/lib/accrue_host/repo.ex, examples/accrue_host/lib/accrue_host/mailer.ex, examples/accrue_host/lib/accrue_host_web/endpoint.ex, examples/accrue_host/lib/accrue_host_web/telemetry.ex</files>
  <read_first>
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
    - .planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
    - accrue_admin/config/test.exs
  </read_first>
  <action>Per D-01, D-02, D-03, and the generator-first pattern in 10-RESEARCH.md, create `examples/accrue_host` with the exact command `mix phx.new examples/accrue_host --app accrue_host --module AccrueHost --database postgres --binary-id --no-dashboard --no-agents-md`. Edit `examples/accrue_host/mix.exs` so deps include `{:accrue, path: "../../accrue"}` and `{:accrue_admin, path: "../../accrue_admin"}`. In `examples/accrue_host/config/test.exs`, configure `AccrueHost.Repo` with PostgreSQL defaults `hostname: System.get_env("PGHOST", "localhost")`, `username: System.get_env("PGUSER", "postgres")`, `password: System.get_env("PGPASSWORD", "postgres")`, `database: "accrue_host_test#{System.get_env("MIX_TEST_PARTITION")}"`, `pool: Ecto.Adapters.SQL.Sandbox`; also set `config :accrue, env: :test, repo: AccrueHost.Repo, processor: Accrue.Processor.Fake`. In `examples/accrue_host/config/runtime.exs`, keep host-owned runtime config with `config :accrue, :processor, Accrue.Processor.Fake` and `config :accrue, :webhook_signing_secrets, %{stripe: System.get_env("STRIPE_WEBHOOK_SECRET", "whsec_test_host")}` so the local Fake path needs no hidden secret. Keep Bandit and do not add private fixture-only shortcuts.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix deps.get && mix compile</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/mix.exs` contains `path: "../../accrue"`
    - `examples/accrue_host/mix.exs` contains `path: "../../accrue_admin"`
    - `examples/accrue_host/config/test.exs` contains `database: "accrue_host_test#{System.get_env("MIX_TEST_PARTITION")}"`
    - `examples/accrue_host/config/test.exs` contains `processor: Accrue.Processor.Fake`
    - `examples/accrue_host/config/runtime.exs` contains `STRIPE_WEBHOOK_SECRET`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix compile` exits 0
  </acceptance_criteria>
  <done>The Phoenix host app foundation exists with sibling path deps, host-owned Repo/config wiring, and a compilable core scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root -> examples/accrue_host | Generated host app must remain a real user-visible Phoenix app, not a hidden fixture |
| host config -> Accrue runtime | Test/runtime wiring must stay explicit and host-owned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | scaffold/config | mitigate | Task 1 uses the real Phoenix generator and explicit path deps so the harness cannot drift into a hidden fixture shape. |
| T-10-02 | I | runtime/test config | mitigate | Task 1 pins Fake processor and env-driven webhook secret defaults without requiring hidden machine-local secrets. |
| T-10-03 | D | hidden local state | mitigate | Task 1 gives the host app its own Repo and sandbox wiring instead of reusing private install-fixture state. |
| T-10-04 | E | private fixture reuse | mitigate | Task 1 builds `examples/accrue_host` as a standalone Phoenix app per D-01 rather than a private test-only project. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix deps.get && mix compile`.
</verification>

<success_criteria>
The repo contains a compilable `examples/accrue_host` Phoenix app foundation with explicit host-owned Repo/runtime/test config and local sibling path deps.
</success_criteria>

<output>
After completion, create `.planning/phases/10-host-app-dogfood-harness/10-01-SUMMARY.md`
</output>

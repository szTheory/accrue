---
phase: 10-host-app-dogfood-harness
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - examples/accrue_host/lib/accrue_host/billing/plans.ex
  - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
  - examples/accrue_host/lib/accrue_host_web/components/core_components.ex
  - examples/accrue_host/lib/accrue_host_web/router.ex
  - examples/accrue_host/lib/accrue_host_web/controllers/page_html/home.html.heex
  - examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs
autonomous: true
requirements: [HOST-06]
must_haves:
  truths:
    - "A signed-in user can see a minimal billing page with deterministic plan choices."
    - "Clicking Start subscription calls AccrueHost.Billing.subscribe/3 against the Fake processor and persists normal billing state."
    - "The user can perform one update action on the same surface, such as canceling the subscription."
    - "The signed-in billing page is reachable through the host router and a visible signed-in navigation link, not only by typing a hidden URL."
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      provides: "User-facing subscription UI and event handling"
      contains: "Start subscription"
    - path: "examples/accrue_host/lib/accrue_host_web/router.ex"
      provides: "Authenticated route to the host billing page"
      contains: "/app/billing"
    - path: "examples/accrue_host/lib/accrue_host/billing/plans.ex"
      provides: "Deterministic Fake plan IDs"
      contains: "price_basic"
    - path: "examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs"
      provides: "Executable subscription proof"
      contains: "AccrueHostWeb.SubscriptionFlowTest"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      to: "examples/accrue_host/lib/accrue_host/billing.ex"
      via: "public host facade call"
      pattern: "AccrueHost.Billing.subscribe"
    - from: "examples/accrue_host/lib/accrue_host_web/controllers/page_html/home.html.heex"
      to: "examples/accrue_host/lib/accrue_host_web/router.ex"
      via: "signed-in navigation link to the authenticated route"
      pattern: "/app/billing"
---

<objective>
Ship the minimal user-facing billing flow on top of the host facade and Fake processor.

Purpose: per D-09, D-10, D-11, D-12, D-20, and HOST-06, the host app needs a real signed-in path where a user sees plans, starts a subscription through `AccrueHost.Billing`, sees persisted state, and performs one update action without network access or direct-row shortcutting.

Output: a small authenticated billing LiveView, router/navigation wiring to reach it, deterministic plan constants, and an executable subscription-flow test.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
@.planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
@.planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
@.planning/phases/10-host-app-dogfood-harness/10-UI-SPEC.md
@examples/accrue_host/lib/accrue_host/billing.ex
@examples/accrue_host/lib/accrue_host_web/router.ex
@accrue/lib/accrue/test/factory.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the minimal subscription UI with deterministic Fake plans</name>
  <files>examples/accrue_host/lib/accrue_host/billing/plans.ex, examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex, examples/accrue_host/lib/accrue_host_web/components/core_components.ex, examples/accrue_host/lib/accrue_host_web/router.ex, examples/accrue_host/lib/accrue_host_web/controllers/page_html/home.html.heex</files>
  <read_first>
    - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
    - examples/accrue_host/lib/accrue_host_web/components/core_components.ex
    - examples/accrue_host/lib/accrue_host_web/router.ex
    - examples/accrue_host/lib/accrue_host/billing.ex
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - .planning/phases/10-host-app-dogfood-harness/10-UI-SPEC.md
  </read_first>
  <action>Per D-09 through D-12 and the UI contract, add `lib/accrue_host/billing/plans.ex` exporting exactly two deterministic plan ids `%{basic: "price_basic", pro: "price_pro"}` plus human labels. Build `lib/accrue_host_web/live/subscription_live.ex` as a Phoenix.Component/LiveView page with no new component library. Use the UI-SPEC contract directly: font stack `system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`, background `#FAFBFC`, card surface `#FFFFFF`, accent `#5D79F6`, spacing only in 4px multiples, and the exact primary CTA copy `Start subscription`. Show the exact empty-state heading/body from the UI spec when no billing row exists. On submit, call `AccrueHost.Billing.subscribe(current_user, "price_basic" | "price_pro", [])`; expose one destructive update action on the same page using `AccrueHost.Billing.cancel(subscription, [])`, but only behind the exact UI-SPEC confirmation copy `Cancel subscription: Confirm cancellation before ending access.` so the mounted flow does not cancel immediately on first click. Wire the LiveView into the existing authenticated router boundary at the exact signed-in route `/app/billing`, using the `phx.gen.auth` authenticated live_session instead of an ad hoc plug stack. Add a visible signed-in link to `/app/billing` from the host home page so the HOST-06 flow is reachable without URL guessing. Do not insert billing rows directly from the page module.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/lib/accrue_host/billing/plans.ex` contains `price_basic`
    - `examples/accrue_host/lib/accrue_host/billing/plans.ex` contains `price_pro`
    - `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains `Start subscription`
    - `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains `No billing activity yet`
    - `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains `AccrueHost.Billing.subscribe`
    - `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains `AccrueHost.Billing.cancel`
    - `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains `Cancel subscription: Confirm cancellation before ending access.`
    - `examples/accrue_host/lib/accrue_host_web/router.ex` contains `/app/billing`
    - `examples/accrue_host/lib/accrue_host_web/controllers/page_html/home.html.heex` contains `/app/billing`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The host app exposes a real signed-in billing page using deterministic Fake plans, only public billing facade calls, router/navigation wiring that makes the page reachable, and a confirmed destructive cancel flow matching the UI spec copy.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the subscription-flow placeholder with an authenticated end-to-end test</name>
  <files>examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs</files>
  <read_first>
    - examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs
    - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
    - accrue/guides/auth_adapters.md
  </read_first>
  <action>Replace the placeholder in `test/accrue_host_web/subscription_flow_test.exs` with a LiveView or browser-equivalent Phoenix test per D-20 and HOST-06. The test must sign in a real host user, prove the user can reach the billing screen through the signed-in host app path by following the `/app/billing` link or loading that authenticated route, click the exact `Start subscription` action, assert a persisted Accrue customer/subscription exists for that user, then exercise the update action and assert the subscription state changed through the normal Accrue API path. Use the host session boundary and Fake processor; do not mock network calls or bypass the facade by writing rows directly in the assertion path.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/subscription_flow_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs` contains `Start subscription`
    - `examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs` contains `price_basic`
    - `examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs` contains `/app/billing`
    - `examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs` contains `AccrueHostWeb.SubscriptionFlowTest`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/subscription_flow_test.exs` exits 0
  </acceptance_criteria>
  <done>The host app has executable proof that a signed-in user can start and update a Fake-backed subscription through the host facade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser user -> host billing UI | Untrusted user input must be routed through host session auth and public billing APIs |
| host billing UI -> Accrue.Billing | User actions must not bypass the host facade or write rows directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | E | subscription UI | mitigate | Task 1 calls `AccrueHost.Billing.subscribe` and `cancel` directly from the LiveView; Task 2 proves the signed-in browser path rather than direct inserts. |
| T-10-10 | T | Fake-only billing proof | mitigate | Task 1 locks deterministic `price_basic` / `price_pro` ids and `Accrue.Processor.Fake`, keeping the flow local and reproducible. |
| T-10-11 | I | UI copy/state | mitigate | Task 1 uses the exact UI-spec copy strings and a bounded host page instead of leaking debug/state blobs into the surface. |
| T-10-12 | R | update action proof | mitigate | Task 2 asserts persisted state changes after the update action so the flow cannot be reduced to flash-only UI. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix test test/accrue_host_web/subscription_flow_test.exs`.
</verification>

<success_criteria>
An authenticated host user can start and update a Fake-backed subscription through the host app’s public billing facade, and the proof is automated.
</success_criteria>

<output>
After completion, create `.planning/phases/10-host-app-dogfood-harness/10-05-SUMMARY.md`
</output>

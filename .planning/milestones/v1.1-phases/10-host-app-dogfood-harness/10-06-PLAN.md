---
phase: 10-host-app-dogfood-harness
plan: 06
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - examples/accrue_host/lib/accrue_host/billing_handler.ex
  - examples/accrue_host/config/test.exs
  - examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs
autonomous: true
requirements: [HOST-04]
must_haves:
  truths:
    - "A signed Fake/Stripe-shaped payload can be POSTed to /webhooks/stripe through the host router."
    - "Webhook ingest verifies the signature before processing, persists accrue_webhook_events, and remains idempotent on replay."
    - "The normal handler path runs and leaves billing/event-ledger evidence in the host database."
  artifacts:
    - path: "examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs"
      provides: "Signed webhook proof through the host endpoint"
      contains: "/webhooks/stripe"
    - path: "examples/accrue_host/lib/accrue_host/billing_handler.ex"
      provides: "Host webhook handler stub for normal dispatch"
      contains: "def handle_event"
  key_links:
    - from: "examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs"
      to: "/webhooks/stripe"
      via: "ConnTest POST with signed payload"
      pattern: "/webhooks/stripe"
---

<objective>
Prove the host webhook endpoint works end to end through signature verification and normal ingest.

Purpose: per D-13, D-14, D-15 and HOST-04, the host app has to receive a signed payload at `/webhooks/stripe`, verify it through the route-scoped raw-body setup, persist the webhook row, dispatch the normal handler path, and stay idempotent on duplicate delivery.

Output: executable webhook-ingest proof and any host-local config needed to run it inline.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
@.planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
@.planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
@examples/accrue_host/lib/accrue_host/billing_handler.ex
@accrue/lib/accrue/test/webhooks.ex
@accrue/lib/accrue/webhook/plug.ex
@accrue/lib/accrue/webhook/ingest.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize host-local webhook dispatch wiring for inline verification</name>
  <files>examples/accrue_host/lib/accrue_host/billing_handler.ex, examples/accrue_host/config/test.exs</files>
  <read_first>
    - examples/accrue_host/lib/accrue_host/billing_handler.ex
    - examples/accrue_host/config/test.exs
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - accrue/lib/accrue/webhook/plug.ex
    - accrue/lib/accrue/webhook/ingest.ex
  </read_first>
  <action>Per D-13, D-15, and the threat model requirement for raw-body parsing and no-secret logging, keep `lib/accrue_host/billing_handler.ex` as a host-owned `use Accrue.Webhook.Handler` module and make its `handle_event/3` implementation leave explicit host-side evidence without reimplementing verification or ingest. In `config/test.exs`, wire host test execution so the webhook proof can run inline with the Fake processor and host Repo; keep `:accrue` test env on `AccrueHost.Repo` and do not introduce any live Stripe dependency or log raw signing-secret values.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/lib/accrue_host/billing_handler.ex` contains `use Accrue.Webhook.Handler`
    - `examples/accrue_host/lib/accrue_host/billing_handler.ex` contains `def handle_event`
    - `examples/accrue_host/config/test.exs` contains `repo: AccrueHost.Repo`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The host app can run the webhook proof against its own Repo and host handler without bypassing Accrue’s ingest/security path.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the webhook placeholder with signed-ingest and idempotency coverage</name>
  <files>examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs</files>
  <read_first>
    - examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs
    - examples/accrue_host/lib/accrue_host_web/router.ex
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
    - accrue/lib/accrue/test/webhooks.ex
    - accrue/test/accrue/webhook/plug_test.exs
  </read_first>
  <action>Replace the placeholder in `test/accrue_host_web/webhook_ingest_test.exs` with a signed host-endpoint proof. Use a public helper from `Accrue.Test.Webhooks` or the same signed-fixture pattern as `accrue/test/accrue/webhook/plug_test.exs` to POST a Fake/Stripe-shaped payload to the exact route `/webhooks/stripe`. Assert HTTP 200 on the valid signed payload, persisted `accrue_webhook_events`, at least one resulting `accrue_events` row or host handler side effect, and idempotent duplicate delivery on the same `processor_event_id`. Also include a tampered or unsigned payload case that is rejected before ingest, satisfying T-10-01.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/webhook_ingest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs` contains `/webhooks/stripe`
    - `examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs` contains `processor_event_id`
    - `examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs` contains `status_code`
    - `examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs` contains `400` or `401`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/webhook_ingest_test.exs` exits 0
  </acceptance_criteria>
  <done>The host endpoint is proven to verify signatures, persist webhook rows, dispatch normally, and reject tampered payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external webhook sender -> /webhooks/stripe | Untrusted signed payload crosses into the host app |
| host endpoint -> Accrue.Webhook.Ingest | Only verified raw-body requests may reach ingest and dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | S | webhook endpoint | mitigate | Task 2 must include a valid signed POST and a tampered/unsigned rejection case against `/webhooks/stripe`. |
| T-10-14 | T | raw-body parsing | mitigate | Plan 10-02 already installed the route-scoped `CachingBodyReader`; Task 2 proves the endpoint only works through that path. |
| T-10-15 | R | webhook/audit integrity | mitigate | Task 2 asserts persisted `accrue_webhook_events` plus resulting event-ledger or host-handler evidence after dispatch. |
| T-10-16 | I | secret handling | mitigate | Task 1 keeps the host test config Fake-only and forbids raw signing-secret logging or live secret dependencies. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix test test/accrue_host_web/webhook_ingest_test.exs`.
</verification>

<success_criteria>
The host app proves signed webhook receipt, verification, persistence, normal dispatch, duplicate safety, and tampered-payload rejection at `/webhooks/stripe`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-host-app-dogfood-harness/10-06-SUMMARY.md`
</output>

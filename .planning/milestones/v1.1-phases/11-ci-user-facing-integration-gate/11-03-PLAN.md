---
phase: 11-ci-user-facing-integration-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/accrue_host_uat.yml
  - .github/workflows/accrue_admin_assets.yml
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CI-01
  - CI-03
  - CI-04
  - CI-05
  - CI-06
must_haves:
  truths:
    - "Pull requests and main pushes run package checks, admin/host drift-docs checks, and the host integration gate in one clear blocking order."
    - "Fake-backed host integration is mandatory, while live Stripe remains manual or scheduled advisory only."
    - "Failed browser runs upload report, traces, screenshots, and host server logs from the main CI gate, while admin asset freshness blocks inside that same workflow."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Ordered release workflow with drift/docs stage, host integration, and artifact upload"
    - path: ".github/workflows/accrue_admin_assets.yml"
      provides: "Demoted or advisory-only legacy admin asset workflow so required checks point at ci.yml"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows/accrue_admin_assets.yml"
      via: "asset-freshness logic moved into ci.yml drift-docs gate and legacy workflow demoted"
      pattern: "asset-freshness|admin-drift-docs"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/accrue_host_uat.sh"
      via: "host-integration job"
      pattern: "bash scripts/ci/accrue_host_uat.sh"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/annotation_sweep.sh"
      via: "annotation-sweep job"
      pattern: "bash scripts/ci/annotation_sweep.sh"
---

<objective>
Fold the mandatory host integration gate and drift blockers into the main CI workflow, keep live Stripe advisory, and verify the roadmap already reflects the executable plan set.

Purpose: satisfy CI-01, CI-03, CI-04, CI-05, and the workflow side of CI-06 with one ordered release gate on PRs and `main`.
Output: updated `ci.yml` job order, admin/host drift-docs wiring, browser artifact upload wiring, disposition of the old host-only and admin-assets workflows, and a roadmap alignment check.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-ci-user-facing-integration-gate/11-RESEARCH.md
@.planning/phases/11-ci-user-facing-integration-gate/11-PATTERNS.md
@.planning/phases/11-ci-user-facing-integration-gate/11-VALIDATION.md
@.github/workflows/ci.yml
@.github/workflows/accrue_host_uat.yml
@.github/workflows/accrue_admin_assets.yml
@.github/workflows/accrue_admin_browser.yml
@scripts/ci/accrue_host_uat.sh
@scripts/ci/annotation_sweep.sh
@examples/accrue_host/package.json
@examples/accrue_host/playwright.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the main CI workflow into an ordered package-to-host release gate</name>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/accrue_host_uat.yml
    - .github/workflows/accrue_admin_assets.yml
    - .github/workflows/accrue_admin_browser.yml
    - scripts/ci/accrue_host_uat.sh
    - scripts/ci/annotation_sweep.sh
    - examples/accrue_host/package.json
    - examples/accrue_host/playwright.config.js
    - .planning/phases/11-ci-user-facing-integration-gate/11-RESEARCH.md
    - .planning/phases/11-ci-user-facing-integration-gate/11-PATTERNS.md
    - .planning/phases/11-ci-user-facing-integration-gate/11-VALIDATION.md
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/accrue_host_uat.yml, .github/workflows/accrue_admin_assets.yml</files>
  <acceptance_criteria>
    - `rg -n '^  admin-drift-docs:' .github/workflows/ci.yml` succeeds.
    - `rg -n '^  host-integration:' .github/workflows/ci.yml` succeeds.
    - `rg -n 'needs:\\s*\\[release-gate\\]' .github/workflows/ci.yml` succeeds for the `admin-drift-docs` job.
    - `rg -n 'needs:\\s*\\[admin-drift-docs\\]' .github/workflows/ci.yml` succeeds for the `host-integration` job.
    - `rg -n 'mix accrue_admin.assets.build' .github/workflows/ci.yml` succeeds inside `admin-drift-docs`.
    - `rg -n 'git diff --exit-code -- accrue_admin/priv/static/accrue_admin.css accrue_admin/priv/static/accrue_admin.js' .github/workflows/ci.yml` succeeds.
    - `rg -n 'grep -q .*accrue_admin \"/billing\"|grep -q .*accrue_admin.assets.build' .github/workflows/ci.yml` succeeds.
    - `rg -n 'bash scripts/ci/accrue_host_uat.sh' .github/workflows/ci.yml` succeeds.
    - `rg -n 'accrue-host-playwright-report|accrue-host-playwright-traces|accrue-host-server-log' .github/workflows/ci.yml` succeeds.
    - `rg -n \"github.event_name == 'workflow_dispatch' \\|\\| github.event_name == 'schedule'\" .github/workflows/ci.yml` still succeeds for `live-stripe`.
    - `rg -n 'actions/setup-node@v6' .github/workflows/ci.yml` succeeds in the host integration job.
    - `test ! -e .github/workflows/accrue_admin_assets.yml || (rg -n 'workflow_dispatch' .github/workflows/accrue_admin_assets.yml && ! rg -n 'pull_request|push:' .github/workflows/accrue_admin_assets.yml)` succeeds so the legacy admin-assets workflow is either deleted or demoted to manual-only/advisory behavior.
    - `test ! -e .github/workflows/accrue_host_uat.yml || (rg -n 'workflow_dispatch' .github/workflows/accrue_host_uat.yml && ! rg -n 'pull_request|push:' .github/workflows/accrue_host_uat.yml)` succeeds so the legacy host-UAT workflow is either deleted or demoted to manual-only/advisory behavior.
  </acceptance_criteria>
  <action>Update `.github/workflows/ci.yml` so PRs and pushes to `main` run one ordered gate: existing matrix `release-gate` first, new `admin-drift-docs` second with `needs: [release-gate]`, new `host-integration` third with `needs: [admin-drift-docs]`, and `annotation-sweep` last with `needs: [release-gate, admin-drift-docs, host-integration]` if CI-06 still requires a post-job blocker. In `admin-drift-docs`, preserve the canonical admin asset freshness logic currently isolated in `.github/workflows/accrue_admin_assets.yml`: use `ubuntu-24.04`, `actions/checkout@v6`, `erlef/setup-beam@v1`, `mix local.hex --force`, `cd accrue_admin && mix deps.get`, `cd accrue_admin && mix accrue_admin.assets.build`, `git diff --exit-code -- accrue_admin/priv/static/accrue_admin.css accrue_admin/priv/static/accrue_admin.js`, and the two guide greps for `accrue_admin \"/billing\"` and `accrue_admin.assets.build`. Treat this job as the ordered drift/docs blocker required by CI-02 and CI-03, alongside the host generated-drift that remains inside `scripts/ci/accrue_host_uat.sh`. In `host-integration`, use `ubuntu-24.04`, Postgres 15, `actions/checkout@v6`, `erlef/setup-beam@v1`, `actions/setup-node@v6`, restore/install host deps, run `cd examples/accrue_host && npm ci`, install Chromium from the host app manifest, and execute `bash scripts/ci/accrue_host_uat.sh`. On `failure()`, upload `examples/accrue_host/playwright-report` as `accrue-host-playwright-report`, `examples/accrue_host/test-results` as `accrue-host-playwright-traces`, and the captured browser server log as `accrue-host-server-log`. Keep the `live-stripe` job manual-or-scheduled only with `continue-on-error: true`; do not make live Stripe required on PRs or pushes. Either delete `.github/workflows/accrue_host_uat.yml` or rewrite it to `workflow_dispatch` only, and either delete `.github/workflows/accrue_admin_assets.yml` or rewrite it to `workflow_dispatch` only, so the repo no longer has auxiliary PR/main required workflows competing with the canonical `ci.yml` gate. This task owns the GitHub Actions artifact upload wiring for CI-04.</action>
  <verify>
    <automated>ruby -e 'require \"yaml\"; YAML.load_file(\".github/workflows/ci.yml\"); puts \"ci.yml OK\"'</automated>
  </verify>
  <done>The main workflow owns the mandatory release-facing order `release-gate -> admin-drift-docs -> host-integration -> annotation-sweep`, admin asset freshness and guide drift block inside `ci.yml`, host generated drift still blocks through the shell gate, CI-04 artifact uploads come from `host-integration`, and live Stripe remains advisory rather than blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Verify roadmap alignment and only edit if the Phase 11 plan list drifts</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/11-ci-user-facing-integration-gate/11-01-PLAN.md
    - .planning/phases/11-ci-user-facing-integration-gate/11-02-PLAN.md
    - .planning/phases/11-ci-user-facing-integration-gate/11-03-PLAN.md
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <acceptance_criteria>
    - `rg -n '\\*\\*Plans:\\*\\* 3 plans' .planning/ROADMAP.md` succeeds inside the Phase 11 section.
    - `rg -n '11-01-PLAN.md — Create the host Playwright browser contract and retained artifact surface\\.' .planning/ROADMAP.md` succeeds.
    - `rg -n '11-02-PLAN.md — Promote the host UAT shell gate and add annotation-blocking automation\\.' .planning/ROADMAP.md` succeeds.
    - `rg -n '11-03-PLAN.md — Wire the ordered CI workflow and advisory live-Stripe policy into the main release gate\\.' .planning/ROADMAP.md` succeeds.
  </acceptance_criteria>
  <action>Inspect `.planning/ROADMAP.md` and confirm the Phase 11 block already lists `**Plans:** 3 plans` plus the three plan bullets with the current approved objectives. If the roadmap already matches, leave it unchanged and record the no-op in the plan summary. Only edit `.planning/ROADMAP.md` if the Phase 11 count or bullet text has drifted from the current plan set; do not change the goal, requirements, success criteria, or other phases.</action>
  <verify>
    <automated>sed -n '58,76p' .planning/ROADMAP.md</automated>
  </verify>
  <done>The Phase 11 roadmap block is verified as aligned with the finalized three-plan set, and the file changes only if that alignment was missing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflow -> repo secrets/token | Workflow jobs receive GitHub token access and may optionally see advisory Stripe secrets |
| workflow artifacts -> developers | Uploaded reports, traces, and logs leave the ephemeral runner |
| release-gate job -> host-integration job | Host gate trust depends on earlier package jobs finishing cleanly and publishing no hidden regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Elevation of Privilege | GitHub workflow token permissions | mitigate | Keep host integration and annotation sweep on the default GitHub token for read/upload use only; do not add write scopes or secret-derived behavior beyond existing advisory live Stripe inputs. |
| T-11-03-02 | Information Disclosure | artifact upload and logs | mitigate | Upload artifacts only on host gate failure, keep the gate Fake-backed by default, and upload only the Playwright report, test-results, and bounded host server log. |
| T-11-03-03 | False Positive | release gate ordering | mitigate | Use `needs: [release-gate]` for `admin-drift-docs` and `needs: [admin-drift-docs]` for `host-integration` so admin drift/docs blockers fail before the host browser run. |
| T-11-03-04 | False Negative | live Stripe becoming mandatory | mitigate | Preserve `live-stripe` as `workflow_dispatch` or `schedule` plus `continue-on-error: true` so optional external flake does not unblock or replace the Fake-backed mandatory gate. |
| T-11-03-05 | Tampering | parallel duplicate workflows | mitigate | Remove or demote the separate `accrue_host_uat.yml` and `accrue_admin_assets.yml` PR/main workflows so branch protection and required checks point at one canonical release gate. |
</threat_model>

<verification>
Validate workflow YAML parsing after edits, then inspect the Phase 11 roadmap block to confirm the existing three-plan list still matches the approved plan text without changing the approved goal text.
</verification>

<success_criteria>
`ci.yml` is the single mandatory PR/main gate for this phase, the admin drift/docs job runs before host integration, the host integration job uploads CI-04 failure artifacts, live Stripe remains advisory, and the roadmap remains aligned with the three executable plans.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-user-facing-integration-gate/11-ci-user-facing-integration-gate-03-SUMMARY.md`
</output>

---
phase: 12-first-user-dx-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/test/accrue/docs/first_hour_guide_test.exs
  - accrue/test/accrue/docs/troubleshooting_guide_test.exs
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [DX-03, DX-04, DX-05, DX-06]
must_haves:
  truths:
    - "Later Phase 12 docs cannot drift from the host-first onboarding flow because executable checks lock the First Hour guide to the required Phoenix setup order, public entrypoints, and forbidden private-module guidance."
    - "Troubleshooting content cannot silently lose or rename the promised remediation surface because executable checks lock the exact DX diagnostic codes, anchors, matrix headings, and verification commands the first user relies on."
    - "Package metadata and install docs cannot drift from published package reality because one runnable verification entrypoint is reserved to enforce README snippets, HexDocs links, guide links, and `docs.source_ref` boundaries."
  artifacts:
    - path: "accrue/test/accrue/docs/first_hour_guide_test.exs"
      provides: "Executable contract for Phoenix-order setup steps and host-facing API mentions."
    - path: "accrue/test/accrue/docs/troubleshooting_guide_test.exs"
      provides: "Executable contract for stable diagnostic-code anchors and troubleshooting matrix columns."
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Dedicated package-doc verification entrypoint for README snippets, source refs, and guide links."
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ExUnit wrapper around the package-doc verification command."
  key_links:
    - from: "accrue/test/accrue/docs/first_hour_guide_test.exs"
      to: "accrue/guides/first_hour.md"
      via: "future guide-content assertions"
      pattern: "guides/first_hour\\.md"
    - from: "accrue/test/accrue/docs/troubleshooting_guide_test.exs"
      to: "accrue/guides/troubleshooting.md"
      via: "diagnostic-anchor assertions"
      pattern: "ACCRUE-DX-"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "System.cmd/3 shell verification"
      pattern: "verify_package_docs\\.sh"
---

<objective>
Create the missing Wave 1 docs-verification scaffolds before the docs and metadata plans modify user-facing content.

Purpose: Satisfy the validation gaps in `12-VALIDATION.md` so later implementation plans can prove DX-03 through DX-06 against concrete files and commands.
Output: New docs contract tests plus a dedicated package-doc verifier command and test wrapper.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
@.planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
@accrue/test/accrue/docs/testing_guide_test.exs
@accrue/test/accrue/docs/community_auth_test.exs
@accrue/README.md
@accrue/guides/quickstart.md
@accrue/mix.exs
@accrue_admin/mix.exs
@accrue_admin/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the First Hour guide contract test scaffold</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    accrue/test/accrue/docs/testing_guide_test.exs
    accrue/test/accrue/docs/community_auth_test.exs
    accrue/guides/quickstart.md
    accrue/test/accrue/docs/first_hour_guide_test.exs
  </read_first>
  <files>accrue/test/accrue/docs/first_hour_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'defmodule Accrue\\.Docs\\.FirstHourGuideTest' accrue/test/accrue/docs/first_hour_guide_test.exs` finds the new test module.
    - `rg -n 'guides/first_hour\\.md' accrue/test/accrue/docs/first_hour_guide_test.exs` finds the target guide path.
    - `rg -n 'MyApp\\.Billing|use Accrue\\.Webhook\\.Handler|use Accrue\\.Test|AccrueAdmin\\.Router\\.accrue_admin/2|Accrue\\.ConfigError' accrue/test/accrue/docs/first_hour_guide_test.exs` finds the exact public API boundary from D-21.
    - `rg -n 'Accrue\\.Billing\\.Customer|Accrue\\.Billing\\.Subscription|Accrue\\.Webhook\\.WebhookEvent|Accrue\\.Events\\.Event|GenServer' accrue/test/accrue/docs/first_hour_guide_test.exs` finds the forbidden private-module mentions from D-22.
    - `rg -n 'deps|installer|runtime config|migrations|Oban|webhook|auth|admin|Fake-backed subscription|signed webhook proof|tests' accrue/test/accrue/docs/first_hour_guide_test.exs` finds the required Phoenix-order steps from D-12.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/docs/first_hour_guide_test.exs` as a compile-safe contract test module named `Accrue.Docs.FirstHourGuideTest`. Read `accrue/guides/first_hour.md` as the future source file and keep the test temporarily skipped with the literal reason `Phase 12 plan 06 activates this contract after the guide lands`. Inside the skipped test, assert the exact step order from D-12 using these literal markers in sequence: `mix deps.get`, `mix accrue.install`, `config/runtime.exs`, `mix ecto.migrate`, `Oban`, `/webhooks/stripe`, `accrue_admin "/billing"`, `MyApp.Billing.subscribe`, `customer.subscription.created`, `/billing`, and `mix test`. Also assert the guide mentions these public surfaces from D-21: `MyApp.Billing`, `use Accrue.Webhook.Handler`, `use Accrue.Test`, `AccrueAdmin.Router.accrue_admin/2`, and `Accrue.ConfigError`. Add explicit negative assertions that the guide does not teach `Accrue.Billing.Customer`, `Accrue.Billing.Subscription`, `Accrue.Webhook.WebhookEvent`, `Accrue.Events.Event`, direct Fake `GenServer.call`, or worker internals.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs</automated>
  </verify>
  <done>The repo has a named, grep-verifiable contract for the First Hour guide, and the contract already encodes the exact public-vs-private API boundary Phase 12 must preserve.</done>
</task>

<task type="auto">
  <name>Task 2: Add the troubleshooting matrix contract test scaffold</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    accrue/test/accrue/docs/community_auth_test.exs
    accrue/test/accrue/docs/troubleshooting_guide_test.exs
  </read_first>
  <files>accrue/test/accrue/docs/troubleshooting_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'defmodule Accrue\\.Docs\\.TroubleshootingGuideTest' accrue/test/accrue/docs/troubleshooting_guide_test.exs` finds the new test module.
    - `rg -n 'guides/troubleshooting\\.md' accrue/test/accrue/docs/troubleshooting_guide_test.exs` finds the target guide path.
    - `rg -n 'ACCRUE-DX-REPO-CONFIG|ACCRUE-DX-MIGRATIONS-PENDING|ACCRUE-DX-OBAN-NOT-CONFIGURED|ACCRUE-DX-WEBHOOK-SECRET-MISSING|ACCRUE-DX-WEBHOOK-ROUTE-MISSING|ACCRUE-DX-WEBHOOK-RAW-BODY|ACCRUE-DX-AUTH-ADAPTER|ACCRUE-DX-ADMIN-MOUNT-MISSING' accrue/test/accrue/docs/troubleshooting_guide_test.exs` finds the exact diagnostic codes this phase will use.
    - `rg -n 'What happened|Why Accrue cares|Fix|How to verify' accrue/test/accrue/docs/troubleshooting_guide_test.exs` finds the required matrix columns from D-13.
    - `rg -n 'accrue-dx-repo-config|accrue-dx-migrations-pending|accrue-dx-oban-not-configured|accrue-dx-webhook-raw-body' accrue/test/accrue/docs/troubleshooting_guide_test.exs` finds the stable anchors the diagnostics will link to.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/docs/troubleshooting_guide_test.exs` as a compile-safe contract test module named `Accrue.Docs.TroubleshootingGuideTest`. Keep the test temporarily skipped with the literal reason `Phase 12 plan 06 activates this contract after the troubleshooting guide lands`. Make the skipped assertions require one Markdown row or section anchor for each exact setup code the diagnostics plan will emit: `ACCRUE-DX-REPO-CONFIG`, `ACCRUE-DX-MIGRATIONS-PENDING`, `ACCRUE-DX-OBAN-NOT-CONFIGURED`, `ACCRUE-DX-OBAN-NOT-SUPERVISED`, `ACCRUE-DX-WEBHOOK-SECRET-MISSING`, `ACCRUE-DX-WEBHOOK-ROUTE-MISSING`, `ACCRUE-DX-WEBHOOK-RAW-BODY`, `ACCRUE-DX-WEBHOOK-PIPELINE`, `ACCRUE-DX-AUTH-ADAPTER`, and `ACCRUE-DX-ADMIN-MOUNT-MISSING`. Require the exact human-facing matrix columns `What happened`, `Why Accrue cares`, `Fix`, and `How to verify`, plus direct verification commands such as `mix ecto.migrate`, `mix test test/accrue_host_web/webhook_ingest_test.exs`, and `mix test test/accrue_host_web/admin_mount_test.exs`.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/troubleshooting_guide_test.exs</automated>
  </verify>
  <done>The repo has a stable troubleshooting-doc contract with exact diagnostic codes and exact verification-column names, ready for later activation.</done>
</task>

<task type="auto">
  <name>Task 3: Create the package-doc verifier command and ExUnit wrapper</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    scripts/ci/verify_package_docs.sh
    accrue/mix.exs
    accrue_admin/mix.exs
    accrue/README.md
    accrue_admin/README.md
    accrue/test/accrue/docs/package_docs_verifier_test.exs
  </read_first>
  <files>scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <acceptance_criteria>
    - `test -x scripts/ci/verify_package_docs.sh` succeeds.
    - `rg -n 'accrue/mix\\.exs|accrue_admin/mix\\.exs|accrue/README\\.md|accrue_admin/README\\.md' scripts/ci/verify_package_docs.sh` finds the exact files under verification.
    - `rg -n 'source_ref|README|HexDocs|guides/' scripts/ci/verify_package_docs.sh` finds the exact invariants from D-20.
    - `rg -n 'defmodule Accrue\\.Docs\\.PackageDocsVerifierTest' accrue/test/accrue/docs/package_docs_verifier_test.exs` finds the wrapper test module.
    - `rg -n 'verify_package_docs\\.sh' accrue/test/accrue/docs/package_docs_verifier_test.exs` finds the shell command invocation.
  </acceptance_criteria>
  <action>Create `scripts/ci/verify_package_docs.sh` as an executable bash script with `set -euo pipefail`, but keep it in Wave 1 scaffold mode by exiting 0 after printing the literal line `Phase 12 plan 07 activates package-doc drift checks`. Even in scaffold mode, include the real target file list and TODO comments for these exact checks from D-20: parse `@version` from `accrue/mix.exs` and `accrue_admin/mix.exs`; compare README install snippets against those version values; verify `docs.source_ref` remains `accrue-v#{@version}` and `accrue_admin-v#{@version}`; verify relative ExDoc links point at `guides/first_hour.md`, `guides/troubleshooting.md`, `guides/webhooks.md`, and `guides/admin_ui.md`; verify package README links use `https://hexdocs.pm/accrue` and `https://hexdocs.pm/accrue_admin`. Then create `accrue/test/accrue/docs/package_docs_verifier_test.exs` that shells out to `bash ../scripts/ci/verify_package_docs.sh` via `System.cmd/3`, asserts exit status 0, and asserts stdout contains the scaffold activation line.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs</automated>
  </verify>
  <done>The repo now has a dedicated package-doc verification command and a matching ExUnit harness, so the later metadata plan can harden the script without inventing new validation entrypoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs contract tests -> future guide content | Validation files define what public setup and troubleshooting content may teach new users |
| shell verifier -> package metadata files | The verifier will later read package versions, links, and source refs from user-facing docs and Mix metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Security Misconfiguration | docs contract tests | mitigate | Encode D-21 and D-22 directly in assertions so public docs are forced toward `MyApp.Billing`, `use Accrue.Test`, `use Accrue.Webhook.Handler`, `AccrueAdmin.Router.accrue_admin/2`, and `Accrue.ConfigError`, not private modules. |
| T-12-01-02 | Information Disclosure | package-doc verifier scaffold | mitigate | Even in scaffold mode, constrain future checks to version strings, source refs, and links; do not print secrets or env-var values. |
| T-12-01-03 | Repudiation | troubleshooting guide contract | mitigate | Reserve stable anchors based on exact diagnostic codes so later runtime errors can link to one canonical remediation location. |
| T-12-01-04 | Elevation of Privilege | admin/docs guidance | mitigate | Require the troubleshooting and First Hour contracts to keep admin setup at the host-auth boundary and forbid teaching dev-only bypasses. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs`
- `cd accrue && mix test test/accrue/docs/troubleshooting_guide_test.exs`
- `bash scripts/ci/verify_package_docs.sh`
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs`
</verification>

<success_criteria>
- Wave 1 docs-verification scaffold files exist at the exact paths named by `12-VALIDATION.md`.
- The contract tests already encode the exact public API boundary and exact diagnostic-code set Phase 12 will implement.
- The package-doc verifier has a stable command name and test wrapper ready for strict activation later in the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-01-SUMMARY.md`
</output>

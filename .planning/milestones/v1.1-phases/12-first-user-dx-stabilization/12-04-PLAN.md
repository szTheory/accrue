---
phase: 12-first-user-dx-stabilization
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
autonomous: true
requirements: [DX-05]
must_haves:
  truths:
    - "The host app can inspect current billing state through `AccrueHost.Billing` instead of direct schema and Repo coupling in the UI."
    - "The public integration story stays on `MyApp.Billing` and other host-facing surfaces rather than private Accrue schemas."
    - "The host billing facade remains thin and explicit even after adding read helpers."
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/billing.ex"
      provides: "Host-facing read helper for current billing state plus existing write helpers."
    - path: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      provides: "Host subscription UI that consumes the generated billing facade instead of private Accrue tables directly."
    - path: "examples/accrue_host/test/accrue_host/billing_facade_test.exs"
      provides: "Executable proof that the host read helper exists and returns current customer/subscription state."
  key_links:
    - from: "examples/accrue_host/lib/accrue_host/billing.ex"
      to: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      via: "billing_state_for/1"
      pattern: "Billing\\.billing_state_for\\("
---

<objective>
Move the host app’s billing reads behind the generated host facade so the public API boundary is Phoenix-context-shaped.

Purpose: Deliver D-21 through D-23 by replacing direct UI coupling to private billing schemas with host-owned helper functions.
Output: New host facade read helper, updated host LiveView, and tests proving the public boundary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
@examples/accrue_host/lib/accrue_host/billing.ex
@examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
@examples/accrue_host/test/accrue_host/billing_facade_test.exs
@examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs
@accrue/lib/accrue/billing.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add host-facing billing state helpers to the generated facade</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    examples/accrue_host/lib/accrue_host/billing.ex
    examples/accrue_host/test/accrue_host/billing_facade_test.exs
    accrue/lib/accrue/billing.ex
  </read_first>
  <files>examples/accrue_host/lib/accrue_host/billing.ex, examples/accrue_host/test/accrue_host/billing_facade_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'def billing_state_for\\(billable\\)' examples/accrue_host/lib/accrue_host/billing.ex` finds the exact new helper name.
    - `rg -n '%\\{customer: customer, subscription: subscription\\}' examples/accrue_host/lib/accrue_host/billing.ex` finds the exact return shape.
    - `rg -n 'billing_state_for' examples/accrue_host/test/accrue_host/billing_facade_test.exs` finds active test coverage for the helper.
    - `rg -n 'Accrue\\.Billing\\.Customer|Accrue\\.Billing\\.Subscription' examples/accrue_host/test/accrue_host/billing_facade_test.exs` still finds boundary assertions in tests, not docs-facing examples.
  </acceptance_criteria>
  <action>Extend `examples/accrue_host/lib/accrue_host/billing.ex` with a new host-facing helper `billing_state_for/1`. Make it return `{:ok, %{customer: customer, subscription: subscription}}`, where `customer` may be `nil` and `subscription` is the newest subscription for that customer or `nil`. Keep the helper inside the generated host facade, even if it internally queries Accrue tables, so first users interact with `MyApp.Billing` instead of private schemas. Preserve the existing `subscribe/3`, `swap_plan/3`, `cancel/2`, and `customer_for/1` functions. Update `examples/accrue_host/test/accrue_host/billing_facade_test.exs` so it asserts `billing_state_for/1` is exported, returns `%{customer: nil, subscription: nil}` before the first subscription, and returns the fake-backed customer plus latest subscription after `Billing.subscribe(user, "price_basic", trial_end: {:days, 14})`.</action>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <done>The generated host facade now exposes a concrete read helper for current billing state, and the helper is proven by the host billing facade test.</done>
</task>

<task type="auto">
  <name>Task 2: Move the subscription LiveView onto the host billing facade</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
    examples/accrue_host/lib/accrue_host/billing.ex
    examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs
  </read_first>
  <files>examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex, examples/accrue_host/lib/accrue_host/billing.ex</files>
  <acceptance_criteria>
    - `rg -n 'Billing\\.billing_state_for\\(socket\\.assigns\\.current_scope\\.user\\)' examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` finds the exact new call site.
    - `rg -n 'alias Accrue\\.Billing\\.Customer|alias Accrue\\.Billing\\.Subscription|alias AccrueHost\\.Repo|import Ecto\\.Query' examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` returns no matches for the removed private-coupling surface.
    - `rg -n 'Subscription\\.canceled\\?' examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` still finds the status guard, preserving existing behavior.
  </acceptance_criteria>
  <action>Refactor `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` so `load_state/1` calls `Billing.billing_state_for(socket.assigns.current_scope.user)` and assigns `%{customer: customer, subscription: subscription}` from that response. Remove the direct `Repo` query helpers `fetch_customer/1` and `current_subscription/1`, remove the `Accrue.Billing.Customer` and `AccrueHost.Repo` aliases, and keep the rest of the UI behavior intact. Preserve the existing plan labels, cancel confirmation flow, and `Subscription.canceled?/1` status checks. The goal is to make the host UI consume only the host-generated billing facade for read/write operations.</action>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs test/accrue_host_web/subscription_flow_test.exs</automated>
  </verify>
  <done>The committed host UI now reads billing state through `AccrueHost.Billing` rather than reaching directly into private Accrue schemas and Repo queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host UI -> generated billing facade | The LiveView should consume host-owned helper functions rather than private package internals |
| generated billing facade -> Accrue internals | Any internal schema coupling should remain hidden behind the host-owned boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Security Misconfiguration | `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` | mitigate | Remove direct schema/Repo teaching from the UI and route reads through `Billing.billing_state_for/1`, keeping the public story at the generated host facade. |
| T-12-04-02 | Elevation of Privilege | host billing facade | mitigate | Keep the facade host-owned and explicit so future host policy hooks still gate reads and writes before delegating into Accrue. |
| T-12-04-03 | Repudiation | host billing tests | mitigate | Extend `billing_facade_test.exs` to prove the new helper returns current state without asking users to read private tables directly. |
</threat_model>

<verification>
- `cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs test/accrue_host_web/subscription_flow_test.exs`
</verification>

<success_criteria>
- The host app has a concrete read helper on `AccrueHost.Billing`.
- The subscription UI no longer queries private Accrue tables directly.
- DX-05’s host-first API boundary is visible in committed code, not just docs prose.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-04-SUMMARY.md`
</output>

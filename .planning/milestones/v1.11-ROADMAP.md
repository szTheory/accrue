# Milestone v1.11: Public Hex release + post-release continuity

**Status:** SHIPPED 2026-04-22  
**Phases:** 46–47  
**Total plans:** 6 (3 + 3)

## Overview

Ship **`accrue`** and **`accrue_admin`** to **Hex** for metering + accumulated work since early **0.1.x** public cuts, using **Release Please** + **linked versions**, then align **`RELEASING.md`**, **`first_hour`**, **`verify_package_docs`**, and planning Hex callouts so **Hex / repo / guides** agree — without **PROC-08** or **FIN-03**. Requirements **REL-01..REL-04**, **DOC-01..DOC-02**, **HYG-01** archived alongside this file.

## Phases

### v1.11 — Public Hex release + post-release continuity (Phases 46–47) — COMPLETE (2026-04-22)

**Milestone goal:** Executable maintainer path from green **`main`** → merged release PR → Hex + tags; then remove evaluator confusion between “what Hex has” and “what the repo says.”

**Depends on:** v1.10 complete (metering in tree); CI green on **`main`**.

| # | Phase | Goal | Requirements |
|---|-------|------|----------------|
| 46 | Release train & Hex publish | Executable maintainer path from green **`main`** → merged release PR → Hex + tags. | REL-01, REL-02, REL-04 — **Complete 2026-04-22** |
| 47 | Post-release docs & planning continuity | Remove evaluator confusion between “what Hex has” and “what the repo says.” | REL-03, DOC-01, DOC-02, HYG-01 — **Complete 2026-04-22** |

**Success criteria (milestone):**

1. Both packages appear on Hex at the **same linked** SemVer with **`accrue` published before `accrue_admin`**.
2. **`verify_package_docs`** + package docs ExUnit gate pass on **`main`** after release.
3. Planning docs no longer claim **0.1.2** as “current Hex” once a newer version is published.

### Phase 46 — Release train & Hex publish

**Goal:** Executable maintainer path from green **`main`** → merged release PR → Hex + tags.

**Highlights:** Maintainer-intent-only `workflow_dispatch` for release PR merge; `verify_release_manifest_alignment.sh` + merge-blocking `release-manifest-ssot`; D-12-style `46-VERIFICATION.md` index for REL-04 evidence (**REL-01..REL-04**).

### Phase 47 — Post-release docs & planning continuity

**Goal:** Remove evaluator confusion between “what Hex has” and “what the repo says.”

**Highlights:** Routine-first **`RELEASING.md`** with bootstrap appendix; **`first_hour.md`** `~>` pins aligned to **`@version` 0.3.0**; **`PROJECT.md`**, **`MILESTONES.md`**, **`STATE.md`** Hex mirrors (**REL-03**, **DOC-01..02**, **HYG-01**).

---

## Milestone Summary

**Key decisions:**

- **v1.11 ships Hex** before the next feature milestone so evaluators get published packages that include metering + accumulated work.
- Release PR automation stays **human-gated** (no accidental auto-merge of Release Please branches).

**Issues resolved:**

- Linked-versions monorepo had drift risk between manifest, `mix.exs`, guides, and planning; close ties **SSOT** scripts + doc verifiers to **`main`**.

**Technical debt / follow-ups:**

- No standalone `v1.11-MILESTONE-AUDIT.md`; closure used per-phase `*-VERIFICATION.md` / `46-VERIFICATION.md` and requirements traceability (7/7 Complete), matching v1.9/v1.10-style close.
- `gsd-sdk query milestone.complete` returned `version required for phases archive` on this install; archives were written manually.

---

_For the live roadmap, see `.planning/ROADMAP.md`._

# Milestone v1.13: Integrator path + secondary admin parity

**Status:** SHIPPED 2026-04-23  
**Phases:** 51–53  
**Total plans:** 8 (3 + 3 + 2)

## Overview

Tighten the **first-time integrator golden path** (README, **First Hour**, quickstart, VERIFY-01, adoption proof matrix, package-doc verifiers) and raise **auxiliary** `accrue_admin` LiveViews (**coupons**, **promotion codes**, **Connect accounts**, **events**) to the **v1.6 / v1.12** bar for **`AccrueAdmin.Copy`**, **`ax-*` / theme tokens**, and **VERIFY-01** — without **PROC-08**, **FIN-03**, new third-party UI kits, or core billing schema changes. Requirements **INT-01..INT-05** and **AUX-01..AUX-06** archived alongside this file.

## Phases

### v1.13 — Integrator path + secondary admin parity (Phases 51–53) — COMPLETE (2026-04-22 / 2026-04-23)

**Milestone goal:** A net-new Phoenix team **lands successfully on the first try**—README / **First Hour** / quickstart / VERIFY-01 tell one story—and **auxiliary admin** pages no longer feel like second-class operators next to customers/subscriptions/invoices, without **PROC-08**, **FIN-03**, or new UI kits.

**Depends on:** **v1.12** shipped; **Hex `0.3.0`** pair is the published baseline; existing **VERIFY-01** host contract and **Copy export** machinery from **v1.11–v1.12**.

| # | Phase | Goal | Requirements |
|---|-------|------|--------------|
| 51 | Integrator golden path & docs | One coherent **clone → install → Fake subscription → proof** narrative; VERIFY-01 / CI lane discoverability from repo root; first-run troubleshooting anchors with stable slugs. **Shipped 2026-04-22.** | INT-01, INT-02, INT-03 |
| 52 | Integrator proof + package alignment + auxiliary copy (part 1) | Proof artifacts and Hex-facing docs stay evidence-true while **Coupons** / **promotion codes** pick up **AccrueAdmin.Copy** SSOT. **Shipped 2026-04-23.** | INT-04, INT-05, AUX-01, AUX-02 |
| 53 | Auxiliary admin — Connect, events, layout, VERIFY | **Connect** + **events** surfaces meet copy SSOT; **`ax-*` / token** discipline + registered exceptions; **Playwright** + **axe** on all materially touched auxiliary mounted paths. **Shipped 2026-04-23.** | AUX-03, AUX-04, AUX-05, AUX-06 |

**Success criteria (milestone):**

1. **INT-01..INT-05** and **AUX-01..AUX-06** satisfied with committed docs, **LiveView**, tests, and phase verification notes.
2. No **PROC-08** / **FIN-03** scope creep; no new third-party UI kits.
3. **VERIFY-01** policy (**merge-blocking** vs **advisory** lanes) unchanged unless an explicit maintainer decision documents a rename (discouraged).

### Phase 51 — Integrator golden path & docs

**Goal:** Remove **first-hour contradictions** and document **first-run failure recovery** where hosts actually look.

**Highlights:** H/M/R capsules on **First Hour** ↔ **`examples/accrue_host` README**; thin **quickstart** hub; repo-root **VERIFY-01** discoverability; **troubleshooting** / webhooks anchors with stable slugs and bounded failure callouts (**INT-01..INT-03**).

### Phase 52 — Integrator proof + package alignment + auxiliary copy (part 1)

**Goal:** Proof artifacts and Hex-facing docs stay **evidence-true** while **`Coupons*`** and **`PromotionCode*`** pick up **Copy** SSOT.

**Highlights:** **`AccrueAdmin.Copy.Coupon`** / **`Copy.PromotionCode`** + **`defdelegate`** on **`AccrueAdmin.Copy`**; LiveViews + ExUnit aligned to SSOT; **`verify_package_docs`** / adoption matrix honesty (**INT-04**, **INT-05**, **AUX-01**, **AUX-02**).

### Phase 53 — Auxiliary admin — Connect, events, layout, VERIFY

**Goal:** Parity for **Connect** and **events** + **VERIFY-01** extension for auxiliary routes.

**Highlights:** **`AccrueAdmin.Copy.Connect`** + **`Copy.BillingEvent`**; **`DataTable`** optional **`filter_submit_label`**; **theme-exceptions** note; **VERIFY-01** Playwright + **axe** for **/connect**, **/connect/:id**, **/events**, **/coupons**, **/promotion-codes**; **`export_copy_strings`** allowlist + regenerated **`copy_strings.json`**; host seed **Connect** row for deterministic detail URL (**AUX-03..AUX-06**).

---

## Milestone Summary

**Key decisions:**

- **Integrator spine** changes ship as paired edits across **First Hour**, **host README**, and **quickstart** so evaluators never see forked narratives.
- **Copy submodules per surface** (**Coupon**, **PromotionCode**, **Connect**, **BillingEvent**) reuse the **`Copy.Subscription`** + **`defdelegate`** facade pattern.
- **VERIFY-01** extensions reuse **copy-export → JSON → Playwright** instead of raw duplicate literals.

**Issues resolved:**

- Root **`.planning/REQUIREMENTS.md`** had **INT-04** / **INT-05** rows still **Pending** in the traceability table at close despite Phase **52** verification; milestone archive records **11/11 Complete** consistent with **`ROADMAP.md`**, **`STATE.md`**, and plan **`requirements-completed`** YAML.

**Technical debt / follow-ups:**

- No standalone **`v1.13-MILESTONE-AUDIT.md`**; closure used per-phase **`*-VERIFICATION.md`** / summaries and requirements alignment on archive.
- **`gsd-sdk query milestone.complete`** remains unreliable on this install; archives were written manually (same as **v1.12**).

**Known deferred items at close:** same **audit-open** carry-forward as prior closes (see **`.planning/STATE.md`** § Deferred Items).

---

_For the live roadmap, see `.planning/ROADMAP.md`._

# Milestone v1.15: Release / trust semantics

**Status:** SHIPPED 2026-04-23  
**Phases:** 57–58  
**Total plans:** 0 (docs-only milestone; no `.planning/phases/57-*` / `58-*` trees)

## Overview

Close the adoption-readiness gap between **Hex SemVer** (`0.3.x`), **internal `.planning/` milestone labels** (`v1.14`, `v1.15`, …), and the **checked-in demo’s Sigra dependency** — using **documentation and CI verifier alignment** only. **Forcing function B** from the adoption sanity review. **No** new billing APIs; **no** **PROC-08** / **FIN-03**.

## Phases

### v1.15 — Release / trust semantics (Phases 57–58) — COMPLETE (2026-04-23)

**Milestone goal:** Remove evaluator confusion between **Hex SemVer**, **internal planning milestone labels**, and the **demo’s Sigra dependency** — docs only.

**Depends on:** **v1.14** shipped; **`verify_package_docs`** + **`mix.exs`** `@version` SSOT.

| # | Phase | Goal | Requirements |
|---|-------|------|----------------|
| 57 | Trust docs — SemVer + planning labels | **`upgrade.md`** baseline accuracy; **`RELEASING.md`** + root **`README.md`** spell Hex vs `.planning/` milestones. | TRT-01, TRT-02, TRT-03 |
| 58 | Demo README — Sigra vs `Accrue.Auth` | **`examples/accrue_host/README.md`** callout; **`accrue/README.md`** stability paragraph. | TRT-04 |

**Success criteria (milestone):**

1. **TRT-01..TRT-04** satisfied; **`bash scripts/ci/verify_package_docs.sh`** green.
2. No **PROC-08** / **FIN-03** scope creep.

### Phase 57 — Trust docs (SemVer + planning labels)

**Goal:** Replace stale **`upgrade.md`** baseline; document **`.planning/` vs Hex** on **RELEASING** + root **README**.

**Highlights:** Current Hex baseline section; **`RELEASING.md`** paragraph; root **`README.md`** blockquote.

### Phase 58 — Demo README (Sigra vs `Accrue.Auth`)

**Goal:** Above-the-fold clarity that **Sigra** is demo-only; **`Accrue.Auth`** is the integration contract.

**Highlights:** **`examples/accrue_host/README.md`** callout; **`accrue/README.md`** **Stability** + **`RELEASING`** appendix pointer; **`verify_package_docs.sh`** **extras** string aligned to **`accrue_admin/mix.exs`**.

---

## Milestone Summary

**Key decisions:**

- Treat **forcing function B** (release/trust) as a shippable slice without formal phase trees when scope is docs-only.

**Issues resolved:**

- St **`v0.1.2` baseline** text in **`upgrade.md`** that no longer matched published SemVer.
- **`verify_package_docs`** drift vs **`accrue_admin/mix.exs`** **extras** list.

**Technical debt:**

- None opened for v1.15.

---

_For current project status, see `.planning/ROADMAP.md`_

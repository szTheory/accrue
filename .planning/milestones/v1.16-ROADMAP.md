# Milestone v1.16: Integrator + proof continuity

**Status:** SHIPPED 2026-04-23  
**Phases:** 59–61  
**Total plans:** 6 (2 + 2 + 2 across phase directories)

## Overview

Re-bind **First Hour** ↔ **host README** ↔ **quickstart**, **adoption proof matrix** + **evaluator walkthrough**, repo-root **VERIFY-01** discoverability (**v1.7** hop budget), and **Hex vs `main`** doc SSOT after **v1.15** trust SemVer work — **docs + verifiers + proof artifacts only**; **no** new **`Accrue.Billing`** APIs; **no** **PROC-08** / **FIN-03**.

## Phases

### v1.16 — Integrator + proof continuity (Phases 59–61) — COMPLETE (2026-04-23)

**Milestone goal:** Close **integrator / adoption / proof** drift opened by **v1.15** trust SemVer + demo README work — **docs + verifiers + proof artifacts** only (**no** new billing APIs, **no** **PROC-08** / **FIN-03**).

**Depends on:** **v1.15** shipped; **v1.13** **INT-01..INT-05** baseline; merge-blocking **`host-integration`** contract unchanged unless explicitly revised in this milestone.

| # | Phase | Goal | Requirements |
|---|-------|------|----------------|
| 59 | Golden path + quickstart coherence | **Complete** **2026-04-23** (plans **2/2**) | INT-06 |
| 60 | Adoption proof + CI ownership map | **Complete** **2026-04-23** (plans **2/2**) | INT-07 |
| 61 | Root VERIFY hops + Hex doc SSOT | **Complete** **2026-04-23** (plans **2/2**) | INT-08, INT-09 |

**Success criteria (milestone):**

1. **INT-06..INT-09** satisfied with committed docs/scripts/tests as needed; **`bash scripts/ci/verify_package_docs.sh`** green.
2. No **PROC-08** / **FIN-03** scope creep.

### Phase 59 — Golden path + quickstart coherence

**Goal:** **First Hour** ↔ **host README** ↔ **quickstart** consistent with **v1.15** messaging; verifier scripts green.

**Highlights:** Trust boundary + **Sigra** capsule framing in **First Hour**; **quickstart** auth routing; **CONTRIBUTING** doc preflight trio; **`verify_package_docs.sh`** quickstart hub + capsule structure + ExUnit fixtures (**59-01**, **59-02**).

### Phase 60 — Adoption proof + CI ownership map

**Goal:** Matrix + evaluator script + contributor verifier map honest vs lanes and trust copy.

**Highlights:** Trust/versioning stub in **adoption-proof-matrix**; walkthrough ↔ matrix traceability (**60-01**); **`scripts/ci/README.md`** INT-06/INT-07 rows; **CONTRIBUTING** pointer (**60-02**).

### Phase 61 — Root VERIFY hops + Hex doc SSOT

**Goal:** README hop budget vs **VERIFY-01**; **`verify_package_docs`** + **`first_hour`** + planning mirrors vs **`@version`**.

**Highlights:** **INT-08** ownership in **`verify_package_docs.sh`** + root README merge-blocking line pin + CI README row (**61-01**); **INT-09** **MILESTONES** / **PROJECT** Current State dual-track + CI README row + **CONTRIBUTING** Hex-only deps edge (**61-02**).

---

## Milestone Summary

**Key decisions:**

- Keep integrator continuity as a **bounded doc + verifier** milestone after **v1.15** so **internal `v1.x` labels** and **Hex `0.x`** cannot silently fork the golden path again.

**Issues resolved:**

- **Post–v1.15** risk that **First Hour** / **quickstart** / **host README** contradict trust SemVer messaging.
- **Adoption proof matrix** and **evaluator walkthrough** lagging **v1.15+** trust signals.
- Root **README** **VERIFY-01** discoverability vs documented hop budget (**INT-08**).
- **`verify_package_docs`**, **`first_hour`**, package READMEs, and **`.planning/`** “current Hex” mirrors drifting from **`mix.exs` `@version`** (**INT-09**).

**Technical debt:**

- **`gsd-sdk query milestone.complete`** still fails (`version required for phases archive`); archives authored manually (same as **v1.11–v1.15**).
- No standalone **`v1.16-MILESTONE-AUDIT.md`**; closure used **`audit-open`** (all clear), per-phase verification, and requirements table (**4/4 Complete**).

---

_For current project status, see `.planning/ROADMAP.md`_

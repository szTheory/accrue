---
phase: 62-friction-triage-north-star
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FRG-01]
security_enforcement: enabled
tags: [planning, friction-inventory, v1.17]
must_haves:
  truths:
    - "v1.17-FRICTION-INVENTORY.md contains a populated Inventory table with at least one non-example P0/P1/P2 row OR a maintainer-signed zero-P0 certification paragraph citing scanned sources"
    - "Every inventory row includes ci_contract, integrator_impact, sources, source_tier, req, frg03_disposition, and status columns per the template header"
    - "STATE.md records the canonical inventory path `.planning/research/v1.17-FRICTION-INVENTORY.md` (pointer only)"
    - "No live secrets or raw Stripe keys appear in sources cells"
  artifacts:
    - path: ".planning/research/v1.17-FRICTION-INVENTORY.md"
      provides: "FRG-01 evidence SSOT + FRG-03 routing columns"
      contains: "Inventory table"
      min_lines: 55
---

<objective>
Satisfy **FRG-01**: commit an evidence-ranked friction inventory under **`.planning/research/v1.17-FRICTION-INVENTORY.md`** with **P0/P1/P2** rows (or explicit zero-P0 certification), **sources**, **deferrals** / **`not_v1.17`** rows where needed, and ensure **`.planning/STATE.md`** records the SSOT path. Optionally tick **FRG-01** in **`.planning/REQUIREMENTS.md`** when the row set is complete (may defer checkbox to final wave if team prefers one commit — if deferred, acceptance still requires inventory completeness per ROADMAP SC1).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-62-01 | Pasting production secrets or live API keys into `sources` or `notes` | high | Use verifier names, relative repo paths, public issue URLs, or redacted placeholders only |
| T-62-02 | Example/template row left as fake data masquerading as evidence | medium | Remove `*(example)*` row or replace with real `v1.17-P0-001` evidence; grep must not match literal `*(example)*` |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/62-friction-triage-north-star/62-CONTEXT.md
@.planning/phases/62-friction-triage-north-star/62-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<task type="auto">
  <name>Task 1: Scan evidence sources and populate FRG-01 inventory rows</name>
  <read_first>
    - .planning/phases/62-friction-triage-north-star/62-CONTEXT.md (D-02, canonical_refs)
    - .planning/phases/62-friction-triage-north-star/62-RESEARCH.md
    - .planning/research/v1.17-FRICTION-INVENTORY.md
    - examples/accrue_host/README.md
    - examples/accrue_host/docs/adoption-proof-matrix.md
    - scripts/ci/README.md
    - accrue/guides/first_hour.md
  </read_first>
  <files>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
  </files>
  <action>
    1. Read each file listed in `read_first` and extract concrete friction candidates (failed steps, long hops, verifier brittleness, doc gaps).
    2. For each candidate, append one row to the **Inventory table** in **`.planning/research/v1.17-FRICTION-INVENTORY.md`** using the **exact** column set from the existing header row (`id`, `priority`, `title`, `ci_contract`, `integrator_impact`, `workaround`, `sources`, `source_tier`, `req`, `tags`, `frg03_disposition`, `status`, `notes`).
    3. Assign stable IDs: first P0 is **`v1.17-P0-001`**, increment; P1/P2 use **`v1.17-P1-NNN`** / **`v1.17-P2-NNN`**.
    4. Delete or fully replace the markdown table row that contains the literal substring **`*(example)*`** so no placeholder row remains.
    5. If the evidence scan finds **zero** P0 candidates, add a subsection **`## P0 certification (empty queue)`** immediately under the inventory table with: (a) date, (b) bulleted list of paths scanned, (c) one-sentence maintainer sign-off that no P0 meets the D-02a bar — still satisfy FRG-01 as an explicit outcome.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "Inventory table" .planning/research/v1.17-FRICTION-INVENTORY.md && ! rg '\*\(example\)\*' .planning/research/v1.17-FRICTION-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `(rg -q 'v1\\.17-P[012]-[0-9]+' .planning/research/v1.17-FRICTION-INVENTORY.md) || (rg -q 'P0 certification \\(empty queue\\)' .planning/research/v1.17-FRICTION-INVENTORY.md)` exits 0
    - `! rg -q '\\*\\(example\\)\\*' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `rg -q 'ci_contract' .planning/research/v1.17-FRICTION-INVENTORY.md`
  </acceptance_criteria>
  <done>Inventory table has real rows or signed empty-P0 section; example row removed.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md pointer for FRG-01 path</name>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/62-friction-triage-north-star/62-CONTEXT.md (D-01c)
  </read_first>
  <files>
    - .planning/STATE.md
  </files>
  <action>
    Ensure **`.planning/STATE.md`** contains the literal path **`.planning/research/v1.17-FRICTION-INVENTORY.md`** on the friction inventory line (update if missing or wrong). Do **not** paste the full inventory table into STATE.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -F ".planning/research/v1.17-FRICTION-INVENTORY.md" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -qF ".planning/research/v1.17-FRICTION-INVENTORY.md" .planning/STATE.md`
    - `! rg -q '^\\| `v1\\.17-P0-' .planning/STATE.md` (no inventory table rows pasted into STATE)
  </acceptance_criteria>
  <done>STATE.md points at inventory SSOT only.</done>
</task>

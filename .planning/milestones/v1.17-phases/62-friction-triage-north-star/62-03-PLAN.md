---
phase: 62-friction-triage-north-star
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FRG-03]
security_enforcement: enabled
tags: [planning, backlog, frg-03, v1.17]
must_haves:
  truths:
    - "Every P0 row has req in {INT-10, BIL-03, ADM-12} OR frg03_disposition=not_v1.17 with signed rationale in notes"
    - "Optional backlog subsection anchors exist for INT-10 / BIL-03 / ADM-12 when any P0 maps to that req"
    - "FRG-01 and FRG-03 checkboxes set in REQUIREMENTS.md when inventory + routing complete"
  artifacts:
    - path: ".planning/research/v1.17-FRICTION-INVENTORY.md"
      provides: "FRG-03 routing on P0 rows"
      contains: "frg03_disposition"
      min_lines: 55
---

<objective>
Satisfy **FRG-03**: from the **FRG-01** inventory, ensure every **P0** row maps to **INT-10**, **BIL-03**, or **ADM-12** (via **`req`** + **`frg03_disposition`** `→63`/`→64`/`→65`) **or** is explicitly **`not_v1.17`** with maintainer-signed rationale. Add optional **`### Backlog — …`** anchors for navigation. Update **`.planning/REQUIREMENTS.md`** to check **FRG-01** and **FRG-03** when complete. Optionally add at most **one pointer line** per phase 63–65 in **ROADMAP.md** (D-04b — only if it improves navigation).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-62-03 | Silent scope drop: P0 row left with empty `frg03_disposition` | high | Grep audit: every `v1.17-P0-` row must match `→63|→64|→65|not_v1.17` in same line |
| T-62-04 | Duplicating P0 tables into ROADMAP | medium | ROADMAP edits: markdown links only, no row body paste |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/62-friction-triage-north-star/62-CONTEXT.md
@.planning/research/v1.17-FRICTION-INVENTORY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<task type="auto">
  <name>Task 1: Audit and fix FRG-03 disposition for every P0 row</name>
  <read_first>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
    - .planning/phases/62-friction-triage-north-star/62-CONTEXT.md (D-02b, D-04)
    - .planning/REQUIREMENTS.md (INT-10, BIL-03, ADM-12 bullets)
  </read_first>
  <files>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
  </files>
  <action>
    1. List every table row whose **`priority`** cell is **`P0`** (or `id` matches **`v1.17-P0-`**).
    2. For each P0: set **`req`** to **INT-10**, **BIL-03**, or **ADM-12** if work routes to phases 63–65; set **`frg03_disposition`** to **`→63`**, **`→64`**, or **`→65`** matching that req. If the P0 is out of v1.17 scope, set **`not_v1.17`** and a **signed** rationale in **`notes`** (reference **PROC-08** / **FIN-03** where applicable).
    3. If any P0 rows exist after step 2, add subsection headings exactly:
       - `### Backlog — INT-10 (Phase 63)`
       - `### Backlog — BIL-03 (Phase 64)`
       - `### Backlog — ADM-12 (Phase 65)`
       each followed by a bullet list of **links** to row **`id`**s (markdown `v1.17-P0-NNN` anchors) — only include subsections that have ≥1 row.
    4. If **zero** P0 rows: add **`### Backlog — INT-10 (Phase 63)`** with bullet `- *(no P0 rows — see P0 certification in FRG-01)*)` or equivalent single-line pointer to the empty-queue section from Plan 01.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg "v1\\.17-P0-" .planning/research/v1.17-FRICTION-INVENTORY.md || true</automated>
  </verify>
  <acceptance_criteria>
    - `test $(rg 'v1\\.17-P0-' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -v '→63|→64|→65|not_v1\\.17' | wc -l | tr -d ' ')` -eq 0` — every inventory line mentioning a P0 id includes a disposition token (vacuously true when there are zero P0 lines)
    - `rg -q '### Backlog — INT-10' .planning/research/v1.17-FRICTION-INVENTORY.md`
  </acceptance_criteria>
  <done>All P0 rows have disposition + req alignment or not_v1.17 rationale.</done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md FRG-01 + FRG-03 + optional ROADMAP pointers</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md (Phase 62–65 section)
    - .planning/phases/62-friction-triage-north-star/62-CONTEXT.md (D-04b optional)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </files>
  <action>
    1. Set **`- [x] **FRG-01**`** and **`- [x] **FRG-03**`** in **`.planning/REQUIREMENTS.md`** when inventory and P0 routing are complete (FRG-02 already checked in Plan 02).
    2. **Optional:** If navigation is unclear, add **one markdown link line** under each of **Phase 63**, **64**, **65** headings in **`.planning/ROADMAP.md`** pointing to the corresponding **`### Backlog —`** anchor in the inventory file — e.g. ``[P0 slice](research/v1.17-FRICTION-INVENTORY.md#backlog--int-10-phase-63)`` with slugified anchor matching GitHub heading rules. If anchors would duplicate existing links, skip.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg '^- \[x\] \*\*FRG-01\*\*' .planning/REQUIREMENTS.md && rg '^- \[x\] \*\*FRG-03\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^- \\[x\\] \\*\\*FRG-01\\*\\*' .planning/REQUIREMENTS.md` exits 0
    - `rg '^- \\[x\\] \\*\\*FRG-03\\*\\*' .planning/REQUIREMENTS.md` exits 0
  </acceptance_criteria>
  <done>REQUIREMENTS reflects FRG-01 and FRG-03 closure.</done>
</task>

---
phase: 63-p0-integrator-verify-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/first_hour.md
  - accrue/README.md
  - accrue_admin/README.md
autonomous: true
requirements: [INT-10]
security_enforcement: enabled
tags: [integrator, docs, hex, INT-10, v1.17-P0-001]
must_haves:
  truths:
    - "Integrators see three plain facts after the deps block in First Hour: Hex published line, workspace/path version alignment, pre-1.0 minor semantics + lockfile discipline for production"
    - "`bash scripts/ci/verify_package_docs.sh` exits 0 after edits"
    - "No third numeric SSOT (no new hand-maintained version tables or badges outside literals enforced by verify_package_docs.sh)"
  artifacts:
    - path: "accrue/guides/first_hour.md"
      provides: "Golden-path install narrative for v1.17-P0-001"
      contains: "### Install the packages"
      min_lines: 30
---

<objective>
Close **v1.17-P0-001** (**INT-10** / docs): make **Hex vs `main` / `path:`** and **pre-1.0 `~>`** intent **skimmable** in **First Hour** and package READMEs per **63-CONTEXT.md** D-01b–D-01e, without relaxing **`verify_package_docs`** enforcement.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-63-01 | Copy/paste of live Stripe secrets into README examples | high | Use only existing env-var patterns (`whsec_test_host`, `System.get_env`); do not add new secret-shaped literals |
| T-63-02 | Drift between `@version` and documented `~>` pins | high | After prose edits, run `verify_package_docs.sh`; extend script only with paired `require_fixed` using same `$accrue_version` variables — never hand-edit version digits in prose without script alignment |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/63-p0-integrator-verify-docs/63-CONTEXT.md
@.planning/phases/63-p0-integrator-verify-docs/63-RESEARCH.md
@.planning/research/v1.17-FRICTION-INVENTORY.md
</context>

<task type="auto">
  <name>Task 1: First Hour — three-fact install framing (D-01c)</name>
  <read_first>
    - accrue/guides/first_hour.md
    - .planning/phases/63-p0-integrator-verify-docs/63-CONTEXT.md (D-01b, D-01c, D-01e)
    - scripts/ci/verify_package_docs.sh (first_hour require_fixed cluster)
  </read_first>
  <files>
    - accrue/guides/first_hour.md
  </files>
  <action>
    In **`accrue/guides/first_hour.md`**, under **§ Install the packages** (the subsection that already contains the **`> **Hex vs `main`:**`** callout and the `defp deps` example):

    1. Immediately after the **`> **Hex vs `main`:**`** blockquote (or merged into one short intro paragraph if cleaner), add **three** explicit facts as **numbered plain sentences or a tight bullet list** (not a second version table):
       - **(1)** Pins/`~>` in repo docs track the **Hex-published** SemVer train for the release line this branch ships with.
       - **(2)** **`path:`** / monorepo users must keep **`accrue`** and **`accrue_admin`** versions **aligned** with each other (lockstep).
       - **(3)** Pre-1.0 **`~>`** minors may include breaking API changes; production should use **resolved lockfile** discipline, not “semver feelings.”

    2. Preserve existing **`verify_package_docs.sh`** literals: the fenced **`{:accrue, "~> X.Y.Z"}`** / **`{:accrue_admin, "~> X.Y.Z"}`** blocks must stay **syntactically valid** and must **continue to match** whatever `@version` the script extracts (do not hard-code different numbers than `mix.exs`).

    3. Do **not** add badges, alternate version tables, or a third place that states `@version` outside what the verifier already enforces.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "### Install the packages" accrue/guides/first_hour.md` exits 0
    - `rg -Fq '> **Hex vs \`main\`:**' accrue/guides/first_hour.md`
    - `rg -qi "lockfile|lock step|lockstep" accrue/guides/first_hour.md`
    - `rg -qi "pre-1\\.0|0\\.x" accrue/guides/first_hour.md`
    - `rg -qi "workspace|monorepo|path:" accrue/guides/first_hour.md`
    - `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root
  </acceptance_criteria>
  <done>First Hour encodes D-01c three-fact bar; verifier green.</done>
</task>

<task type="auto">
  <name>Task 2: Package READMEs — skimmable Hex vs edge (optional tighten)</name>
  <read_first>
    - accrue/README.md
    - accrue_admin/README.md
    - accrue/guides/first_hour.md (after Task 1)
    - scripts/ci/verify_package_docs.sh (README require_fixed lines 83–89)
  </read_first>
  <files>
    - accrue/README.md
    - accrue_admin/README.md
  </files>
  <action>
    1. In **`accrue/README.md`** and **`accrue_admin/README.md`**, in the install section near the **`> **Hex vs `main`:**`** callout: add **at most two short sentences** echoing the same **three themes** as Task 1 (Hex line vs branch, workspace alignment, pre-1.0 + lockfile) **only if** those themes are not already obvious from existing text.

    2. Do **not** remove or alter the literal strings **`> **Hex vs `main`:**`**, **`https://hex.pm/packages/accrue`**, **`https://hex.pm/packages/accrue_admin`**, or the **`{:accrue, "~> ...` / `{:accrue_admin, "~> ...` / `accrue ~>`** lines enforced by `verify_package_docs.sh`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_package_docs.sh` exits 0
    - `rg -Fq '> **Hex vs \`main\`:**' accrue/README.md accrue_admin/README.md`
  </acceptance_criteria>
  <done>Package READMEs aligned with D-01b; verifier green.</done>
</task>

<task type="auto">
  <name>Task 3: ExUnit regression — package docs verifier</name>
  <read_first>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs
    - scripts/ci/verify_package_docs.sh
  </read_first>
  <files>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs
  </files>
  <action>
    If Task 1–2 added **new** `require_fixed` / `require_regex` needles to **`verify_package_docs.sh`**, extend **`package_docs_verifier_test.exs`** with a focused assertion or fixture comment per existing style.

    If **no** script changes occurred, add a **single** test comment or assertion documenting that **63-01** relies on end-to-end `verify_package_docs.sh` only — **or** skip file change and note in commit message that tests unchanged.

    Prefer **minimal** test surface: only add code when `verify_package_docs.sh` actually changed.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/docs/package_docs_verifier_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits 0
  </acceptance_criteria>
  <done>Tests green; any new pins covered.</done>
</task>

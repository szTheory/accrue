---
phase: 63-p0-integrator-verify-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/accrue_host_uat.sh
  - scripts/ci/accrue_host_verify_test_bounded.sh
  - scripts/ci/accrue_host_verify_test_full.sh
  - scripts/ci/accrue_host_verify_dev_boot.sh
  - scripts/ci/accrue_host_verify_browser.sh
  - scripts/ci/README.md
autonomous: true
requirements: [INT-10]
security_enforcement: enabled
tags: [verify, host-integration, CI, INT-10, v1.17-P0-002]
must_haves:
  truths:
    - "Each host verify sub-script emits a stderr line starting with `[host-integration]` naming a stable phase slug before substantive work"
    - "`accrue_host_uat.sh` announces delegation with `[host-integration]` and prints a single final failure banner `FAILED_GATE=host-integration` on unexpected non-zero exit from `mix verify.full` (wrapper-level)"
    - "`scripts/ci/README.md` documents prefix → meaning → deep link to host README proof section"
  artifacts:
    - path: "scripts/ci/accrue_host_uat.sh"
      provides: "Repo-root host UAT entrypoint"
      contains: "mix verify.full"
      min_lines: 20
---

<objective>
Close **v1.17-P0-002** (**INT-10** / VERIFY): improve **`host-integration`** failure **discoverability** via consistent **`[host-integration]`** log prefixes and a **`FAILED_GATE=host-integration`** banner on wrapper failure, plus **`scripts/ci/README.md`** triage text per **63-CONTEXT.md** D-02a–D-02d.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-63-03 | Log prefix leaks secrets from downstream tools | low | Prefixes are static labels only; never echo env vars or connection strings in new banner lines |
| T-63-04 | Changing script paths breaks `verify_package_docs.sh` pins | medium | Do not rename `scripts/ci/accrue_host_uat.sh` or remove `mix verify.full` substring; grep `verify_package_docs.sh` for `accrue_host_uat` before editing |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/63-p0-integrator-verify-docs/63-CONTEXT.md
@.planning/phases/63-p0-integrator-verify-docs/63-RESEARCH.md
@examples/accrue_host/mix.exs
</context>

<task type="auto">
  <name>Task 1: Prefix lines in host verify scripts</name>
  <read_first>
    - scripts/ci/accrue_host_verify_test_bounded.sh
    - scripts/ci/accrue_host_verify_test_full.sh
    - scripts/ci/accrue_host_verify_dev_boot.sh
    - scripts/ci/accrue_host_verify_browser.sh
    - scripts/ci/verify_package_docs.sh (pins referencing these scripts)
  </read_first>
  <files>
    - scripts/ci/accrue_host_verify_test_bounded.sh
    - scripts/ci/accrue_host_verify_test_full.sh
    - scripts/ci/accrue_host_verify_dev_boot.sh
    - scripts/ci/accrue_host_verify_browser.sh
  </files>
  <action>
    At the **top** of each script, **after** `set -euo pipefail` and the header comments, **before** `cd` / substantive work, add **one** line to **stderr**:

    - **`accrue_host_verify_test_bounded.sh`:** `echo "[host-integration] phase=bounded_mix_tests" >&2`
    - **`accrue_host_verify_test_full.sh`:** `echo "[host-integration] phase=full_mix_tests" >&2`
    - **`accrue_host_verify_dev_boot.sh`:** `echo "[host-integration] phase=dev_boot_smoke" >&2`
    - **`accrue_host_verify_browser.sh`:** `echo "[host-integration] phase=browser_playwright" >&2`

    Use **exact** slug strings above so triage docs can reference them literally.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg '\[host-integration\] phase=' scripts/ci/accrue_host_verify_test_bounded.sh scripts/ci/accrue_host_verify_test_full.sh scripts/ci/accrue_host_verify_dev_boot.sh scripts/ci/accrue_host_verify_browser.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c '\[host-integration\] phase=bounded_mix_tests' scripts/ci/accrue_host_verify_test_bounded.sh` outputs `1`
    - `rg -c '\[host-integration\] phase=full_mix_tests' scripts/ci/accrue_host_verify_test_full.sh` outputs `1`
    - `rg -c '\[host-integration\] phase=dev_boot_smoke' scripts/ci/accrue_host_verify_dev_boot.sh` outputs `1`
    - `rg -c '\[host-integration\] phase=browser_playwright' scripts/ci/accrue_host_verify_browser.sh` outputs `1`
  </acceptance_criteria>
  <done>All four scripts advertise phase slugs on stderr.</done>
</task>

<task type="auto">
  <name>Task 2: Wrapper banner in accrue_host_uat.sh</name>
  <read_first>
    - scripts/ci/accrue_host_uat.sh
    - scripts/ci/verify_package_docs.sh (require_fixed for accrue_host_uat.sh)
  </read_first>
  <files>
    - scripts/ci/accrue_host_uat.sh
  </files>
  <action>
    1. After the existing `echo "=== Accrue host UAT — repo root: $repo_root ==="` (or equivalent first banner), add to **stderr**: `echo "[host-integration] entry=accrue_host_uat delegating_to=mix_verify.full" >&2`

    2. Wrap the **`mix verify.full`** invocation so that **if** it returns non-zero, the script prints **`echo "FAILED_GATE=host-integration" >&2`** **exactly once** before propagating the exit code. Acceptable implementations: `if ! mix verify.full; then echo "FAILED_GATE=host-integration" >&2; exit 1; fi` **or** a bash `ERR` trap **only if** it cannot double-print on nested failures — prefer the explicit `if` for predictability.

    3. Preserve **`require_fixed`** strings in **`verify_package_docs.sh`**: the file must still contain **`mix verify.full`** and the self-doc line **`bash scripts/ci/accrue_host_uat.sh`** where currently pinned (do not remove).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg 'FAILED_GATE=host-integration|\[host-integration\] entry=accrue_host_uat' scripts/ci/accrue_host_uat.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -qF '[host-integration] entry=accrue_host_uat delegating_to=mix_verify.full' scripts/ci/accrue_host_uat.sh`
    - `rg -qF 'FAILED_GATE=host-integration' scripts/ci/accrue_host_uat.sh`
    - `rg -qF 'mix verify.full' scripts/ci/accrue_host_uat.sh`
    - `bash scripts/ci/verify_package_docs.sh` exits 0 (pins unchanged)
  </acceptance_criteria>
  <done>Wrapper documents entry and emits FAILED_GATE on verify.full failure path.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/ci/README.md — host-integration triage subsection (D-02b)</name>
  <read_first>
    - scripts/ci/README.md
    - examples/accrue_host/README.md (target anchor only)
  </read_first>
  <files>
    - scripts/ci/README.md
  </files>
  <action>
    Add a new subsection **`### Triage: host-integration` / `accrue_host_uat.sh`** immediately **after** the existing **`### Triage: verify_adoption_proof_matrix.sh`** block (or, if structure differs, after the INT gates table) containing:

    1. One sentence: failures originate from **`bash scripts/ci/accrue_host_uat.sh`** → **`mix verify.full`** in **`examples/accrue_host`**.

    2. A **bullet list** mapping each **`[host-integration] phase=...`** slug from Task 1 to plain-language meaning (bounded ExUnit slice, full ExUnit, dev boot, Playwright).

    3. A line with **Markdown link** to **`examples/accrue_host/README.md#proof-and-verification`** for normative VERIFY-01 detail.

    Do **not** duplicate the full **`ci.yml`** job list.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "Triage: host-integration|accrue_host_uat" scripts/ci/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '### Triage: host-integration' scripts/ci/README.md`
    - `rg -qF '[host-integration] phase=bounded_mix_tests' scripts/ci/README.md`
    - `rg -qF 'examples/accrue_host/README.md#proof-and-verification' scripts/ci/README.md`
  </acceptance_criteria>
  <done>Contributor map links log prefixes to phases and host README.</done>
</task>

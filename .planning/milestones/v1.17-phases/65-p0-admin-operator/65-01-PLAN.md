---
phase: 65-p0-admin-operator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/65-p0-admin-operator/65-VERIFICATION.md
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ADM-12]
security_enforcement: enabled
tags: [admin, ADM-12, v1.17, friction-inventory, certification]
must_haves:
  truths:
    - "`65-VERIFICATION.md` matches the **63/64** verification **family** (scope + anchor + table + proof column) for **ADM-12** only"
    - "`### Backlog — ADM-12 (Phase 65)` gains a **maintainer-signed** certification line pointing at `65-VERIFICATION.md` without touching the **FRG-01** four-row pipe table"
    - "`bash scripts/ci/verify_v1_17_friction_research_contract.sh` and `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` exit 0 after edits"
  artifacts:
    - path: ".planning/phases/65-p0-admin-operator/65-VERIFICATION.md"
      provides: "Phase 65 merge-facing verification SSOT"
      contains: "### Backlog — ADM-12"
      min_lines: 12
---

<objective>
Close **ADM-12** on the **empty admin P0 queue** path: ship **`65-VERIFICATION.md`**, sign the **inventory** **`### Backlog — ADM-12 (Phase 65)`** subsection per **65-RESEARCH.md**, and flip **`REQUIREMENTS.md`** to reflect **ADM-12** satisfied **without** **`accrue_admin`** / **LiveView** / **VERIFY-01** churn (**no** net-new operator UI this milestone).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-65-01 | Stripe or `whsec_*` shaped literals pasted into `.planning/` prose | high | Inventory edits are certification text only; do not paste secrets; grep self-check stays clean |
| T-65-02 | Accidental edit to **FRG-01** pipe table breaks `verify_v1_17_friction_research_contract.sh` (expects **4** data rows, **2** P0) | high | Restrict edits to **`### Backlog — ADM-12 (Phase 65)`** bullets only; run friction script after save |
| T-65-03 | Drift between **ROADMAP** anchor and inventory heading | medium | Do not rename `### Backlog — ADM-12 (Phase 65)` heading; script greps substring `### Backlog — ADM-12` |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/65-p0-admin-operator/65-RESEARCH.md
@.planning/phases/64-p0-billing/64-VERIFICATION.md
@.planning/research/v1.17-FRICTION-INVENTORY.md
</context>

<task type="auto">
  <name>Task 1: Add `65-VERIFICATION.md` (63/64-family)</name>
  <read_first>
    - .planning/phases/64-p0-billing/64-VERIFICATION.md
    - .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md (table header pattern only)
    - .planning/research/v1.17-FRICTION-INVENTORY.md (only the `### Backlog — ADM-12` heading for anchor text)
  </read_first>
  <files>
    - .planning/phases/65-p0-admin-operator/65-VERIFICATION.md
  </files>
  <action>
    Create **`.planning/phases/65-p0-admin-operator/65-VERIFICATION.md`** with this exact structure (adjust only `verified` date string when closing later if needed):

    1. YAML frontmatter (first line `---`, then):
       - `status: pending`
       - `phase: 65-p0-admin-operator`
       - `verified: ""`
       - closing `---`

    2. H1: `# Phase 65 — P0 admin / operator verification`

    3. One scope paragraph starting with **`Scope:`** stating closure of **ADM-12** for **v1.17** using the inventory section at Markdown link **`[### Backlog — ADM-12 (Phase 65)](../../research/v1.17-FRICTION-INVENTORY.md#backlog--adm-12-phase-65)`** (same relative link style as **64-VERIFICATION.md**).

    4. A Markdown pipe **table** with header row exactly:
       `| Row ID | Acceptance one-liner | Merge-blocking proof | CI vs manual | Closure status |`
       and separator row under it.

    5. Exactly **one** data row for rollup **ADM-12**:
       - **Row ID:** `ADM-12`
       - **Acceptance one-liner:** States **both** branches: (a) if admin-tagged **P0** rows exist they ship scoped **`accrue_admin`** (**LiveView**, **`AccrueAdmin.Copy`**, theme/`ax-*`, **VERIFY-01** when routes change) per **ADM-12**; (b) this milestone **no such rows** — inventory documents empty queue + maintainer certification (**reference `65-VERIFICATION.md` + inventory** in the text).
       - **Merge-blocking proof:** `` `bash scripts/ci/verify_v1_17_friction_research_contract.sh`; `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` `` (same commands as **64-VERIFICATION.md** rollup row pattern).
       - **CI vs manual:** `CI + local manual`
       - **Closure status:** `closed`

    6. **Do not** add a second taxonomy or extra tables; keep file lean per **64** pattern.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && test -f .planning/phases/65-p0-admin-operator/65-VERIFICATION.md && rg -qF '| ADM-12 |' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
    - `rg -qF '# Phase 65 — P0 admin / operator verification' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
    - `rg -qF 'research/v1.17-FRICTION-INVENTORY.md#backlog--adm-12-phase-65' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
    - `rg -qF '| Row ID | Acceptance one-liner | Merge-blocking proof | CI vs manual | Closure status |' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
    - `rg -qF '| ADM-12 |' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
    - `rg -q 'verify_v1_17_friction_research_contract' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
    - `rg -q 'v1_17_friction_research_contract_test' .planning/phases/65-p0-admin-operator/65-VERIFICATION.md`
  </acceptance_criteria>
  <done>Verification SSOT file exists and matches 63/64-family expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory — signed ADM-12 certification (subsection only)</name>
  <read_first>
    - .planning/research/v1.17-FRICTION-INVENTORY.md (full file — avoid touching FRG-01 table)
    - scripts/ci/verify_v1_17_friction_research_contract.sh (row-count expectations)
  </read_first>
  <files>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
  </files>
  <action>
    Under **`### Backlog — ADM-12 (Phase 65)`**, **replace** the existing single italic bullet with **two** lines:

    1. A bold maintainer line exactly starting **`**Maintainer (2026-04-23):**`** stating that the **ADM-12** execution slice has **zero** admin **P0** rows for v1.17, and that closure evidence lives in **`.planning/phases/65-p0-admin-operator/65-VERIFICATION.md`** (use that full repo-relative path in backticks).

    2. A second line: `- *(no P0 rows — queue empty for **→65** work this milestone)*`

    **Hard rule:** Do **not** add, remove, or edit any row inside the **FRG-01** pipe table (`| v1.17-P0-001 |` …). Do **not** add new **`v1.17-P0-*`** IDs for “fake” admin rows.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_v1_17_friction_research_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_v1_17_friction_research_contract.sh` exits 0 from repo root
    - `test "$(grep -cE '^\| v1.17-P[012]-[0-9]{3} \|' .planning/research/v1.17-FRICTION-INVENTORY.md)" = 4`
    - `test "$(grep -cE '^\| v1.17-P0-' .planning/research/v1.17-FRICTION-INVENTORY.md)" = 2`
    - `rg -qF '**Maintainer (2026-04-23):**' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `rg -qF '.planning/phases/65-p0-admin-operator/65-VERIFICATION.md' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `rg -n '### Backlog — ADM-12' .planning/research/v1.17-FRICTION-INVENTORY.md | head -1` exits 0
  </acceptance_criteria>
  <done>Inventory signed; friction contract still green.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS — mark ADM-12 satisfied</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/65-p0-admin-operator/65-VERIFICATION.md (after Task 1)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    1. In **`### Admin / operator (ADM)`**, change the **ADM-12** checklist item from `- [ ]` to `- [x]` (same line text otherwise).

    2. In the **Traceability** table, change the **ADM-12** row’s **Status** column from **`Pending`** to **`Complete`** (keep **Phase** column **`65`**).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "^- \\[x\\] \\*\\*ADM-12\\*\\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '^- \\[x\\] \\*\\*ADM-12\\*\\*' .planning/REQUIREMENTS.md`
    - `rg -q '^\\| ADM-12 \\| 65 \\| Complete \\|' .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>REQUIREMENTS reflects ADM-12 closure.</done>
</task>

<verification>
  <automated>
    cd /Users/jon/projects/accrue && bash scripts/ci/verify_v1_17_friction_research_contract.sh && cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs
  </automated>
</verification>

<success_criteria>
  - `65-VERIFICATION.md` exists with **ADM-12** rollup row and inventory anchor link
  - Inventory **ADM-12** subsection contains **Maintainer (2026-04-23)** line + `65-VERIFICATION.md` path without FRG-01 table churn
  - **`REQUIREMENTS.md`**: **ADM-12** checked and traceability **Complete**
  - Friction script + **`v1_17_friction_research_contract_test.exs`** green
</success_criteria>

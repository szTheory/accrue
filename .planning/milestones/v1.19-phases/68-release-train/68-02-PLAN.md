---
phase: 68-release-train
plan: 02
type: execute
wave: 2
depends_on: []
requirements: [REL-01, REL-02, REL-03]
autonomous: false
files_modified:
  - .planning/phases/68-release-train/68-VERIFICATION.md
  - .planning/REQUIREMENTS.md
tags: [release-train, v1.19, REL-01, REL-02, REL-03, hex, verification]
---

# Plan 68-02 — Ship evidence table (REL-03) + requirement closure

<threat_model>
  <summary>ASVS L1; planning artifacts and requirement checkboxes. URLs are public registry/GitHub only — no secrets in cells.</summary>
  <threat id="T-68-04">False “shipped” claim — verification cites <code>main</code> instead of tag or omits Hex.</threat>
  <mitigation>Require blob URLs at tag and Hex package version URLs per <code>68-CONTEXT.md</code> D-02; forbid Actions run URLs as primary proof.</mitigation>
  <threat id="T-68-05">Requirements left checked pending after ship.</threat>
  <mitigation>REL-03 plan includes explicit checkbox flip for REL rows in <code>REQUIREMENTS.md</code> once URLs verified.</mitigation>
</threat_model>

## Objective

Close **REL-02**’s **Hex + changelog-at-tag** evidence and **REL-03**’s **tag** proof via **`.planning/phases/68-release-train/68-VERIFICATION.md`**. After URLs are live, align **`.planning/REQUIREMENTS.md`** for **REL-01..REL-03** (documentation for **REL-01** / ship-boundary **REL-02** is satisfied by **Plan 68-01**; this plan completes registry proof and requirement checkboxes).

## Tasks

<task id="68-02-01">
  <read_first>
    <file>.planning/phases/68-release-train/68-CONTEXT.md</file>
    <file>.planning/phases/67-proof-contracts/67-VERIFICATION.md</file>
  </read_first>
  <action>
    Create **new file** **`.planning/phases/68-release-train/68-VERIFICATION.md`** with YAML frontmatter:

    ```yaml
    ---
    phase: 68-release-train
    status: pending
    ship_version: "0.3.1"
    ---
    ```

    (If shipped version differs, executor sets **`ship_version`** to that SemVer string.)

    Body must include:

    1. A short paragraph stating evidence is **URL-first**, **no screenshots** for routine proof, and **changelog proof must use `github.com` blob URLs at the release tag** (not **`main`**).
    2. A Markdown table with header row containing **exact** column names (pipe-separated): **`Package`** | **`Version`** | **`Hex package URL`** | **`Git tag URL`** | **`Changelog at tag (blob)`** | **`Verified at (UTC)`** | **`Notes`**
    3. Exactly **two** data rows for **`accrue`** and **`accrue_admin`** with **`Version`** set to **`0.3.1`** (or **`ship_version`**) and all URL columns set to placeholder **`TBD`** until task **68-02-02** runs post-publish.
  </action>
  <acceptance_criteria>
    <criterion>Command `test -f .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'phase: 68-release-train' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'Hex package URL' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'Git tag URL' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'Changelog at tag (blob)' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'TBD' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0** (placeholders present until **68-02-02**).</criterion>
    <criterion>Command `rg -n '\| accrue \|' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n '\| accrue_admin \|' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="68-02-02">
  <read_first>
    <file>.planning/phases/68-release-train/68-VERIFICATION.md</file>
    <file>.release-please-manifest.json</file>
  </read_first>
  <action>
    **After** GitHub Actions **`release-please.yml`** has successfully published both packages (or after equivalent manual **`publish-hex.yml`** recovery documented in **`RELEASING.md`**), replace every **`TBD`** in the URL columns of **`68-VERIFICATION.md`** with live links:

    - **`Hex package URL`** must start with **`https://hex.pm/packages/`** and include the package name.
    - **`Git tag URL`** must start with **`https://github.com/szTheory/accrue/releases/tag/`** and end with tags **`accrue-v{VERSION}`** and **`accrue_admin-v{VERSION}`** respectively (use actual **`VERSION`** from manifest / Hex).
    - **`Changelog at tag (blob)`** must start with **`https://github.com/szTheory/accrue/blob/`** and include path segment **`accrue/CHANGELOG.md`** or **`accrue_admin/CHANGELOG.md`** **with the tag ref** (not **`main`**).
    - **`Verified at (UTC)`** must be an ISO-like UTC timestamp string **`YYYY-MM-DD`** or full UTC datetime (executor’s choice) but **not** `TBD`.

    Set frontmatter **`status:`** to **`complete`** when all URLs are non-TBD and verified load in browser or via **`curl -I`** (manual).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg 'https://hex\\.pm/packages/accrue' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg 'https://hex\\.pm/packages/accrue_admin' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg 'https://github\\.com/szTheory/accrue/releases/tag/accrue-v' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg 'https://github\\.com/szTheory/accrue/releases/tag/accrue_admin-v' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg 'https://github\\.com/szTheory/accrue/blob/accrue-v' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg 'https://github\\.com/szTheory/accrue/blob/accrue_admin-v' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Shell `if rg -q 'TBD' .planning/phases/68-release-train/68-VERIFICATION.md; then exit 1; else exit 0; fi` exits **0** (no **`TBD`** substring remains).</criterion>
    <criterion>Command `rg -F 'status: complete' .planning/phases/68-release-train/68-VERIFICATION.md` exits **0** (YAML frontmatter line).</criterion>
  </acceptance_criteria>
</task>

<task id="68-02-03">
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/68-release-train/68-VERIFICATION.md</file>
  </read_first>
  <action>
    Only after task **68-02-02** acceptance passes, edit **`.planning/REQUIREMENTS.md`**:

    1. Change the Markdown checkboxes for **REL-01**, **REL-02**, and **REL-03** under **`## Release train (REL)`** from **`- [ ]`** to **`- [x]`**.
    2. In the **`## Traceability`** table, replace each **`Pending`** in the **REL-01**, **REL-02**, and **REL-03** rows with **`Complete`** (same casing).

    **Rationale:** **REL-01** / **REL-02** documentation and ship-boundary work land in **Plan 68-01**; **REL-02**’s **Hex-published** clause and **REL-03** tags proof are satisfied only once **`68-VERIFICATION.md`** has live Hex + tag + changelog-at-tag URLs — flip requirements here, not before.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '- [x] **REL-01**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '- [x] **REL-02**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '- [x] **REL-03**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| REL-01 | 68 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| REL-02 | 68 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| REL-03 | 68 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

- Task **68-02-01** is fully automatable before Hex publish.
- Tasks **68-02-02** and **68-02-03** require maintainer confirmation against live registry state.

## must_haves

- **`68-VERIFICATION.md`** exists with required columns and two package rows; ends with **no `TBD`** and **`status: complete`** after ship.
- **REL-01..REL-03** marked complete in **`REQUIREMENTS.md`** only after evidence URLs are real.
- No **`github.com/.../actions/`** URLs used as **primary** ship proof in the table.

---
phase: 69-doc-planning-mirrors
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [DOC-01, DOC-02]
autonomous: true
files_modified:
  - accrue/guides/first_hour.md
  - accrue/README.md
  - accrue_admin/README.md
  - examples/accrue_host/README.md
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md
  - .planning/REQUIREMENTS.md
tags: [v1.19, DOC-01, DOC-02, verify_package_docs, first_hour]
---

# Plan 69-01 — Integrator doc mirrors + verifier (DOC-01, DOC-02)

<threat_model>
  <summary>ASVS L1; public docs and CI scripts only. No live secrets; pins are registry-coordinated SemVer strings.</summary>
  <threat id="T-69-01">Adopters copy stale <code>~&gt;</code> lines that do not match published <code>mix.exs</code> — resolver pulls wrong artifact.</threat>
  <mitigation>Keep <code>verify_package_docs.sh</code> green; primary README + <code>first_hour</code> needles tied to extracted <code>@version</code>.</mitigation>
  <threat id="T-69-02">Elixir tests pass while bash contract regressed (duplicate / drifted expectations).</threat>
  <mitigation>Bash-first rule from <strong>69-CONTEXT</strong> D-03; change script before ExUnit unless only stdout banner changed.</mitigation>
</threat_model>

## Objective

Close **DOC-01** and **DOC-02** for **v1.19**: **`first_hour.md`**, package READMEs, and host capsule surfaces enforced by **`scripts/ci/verify_package_docs.sh`** show **`~>`** install lines consistent with **`accrue/mix.exs`** and **`accrue_admin/mix.exs`** **`@version`**; script + **`PackageDocsVerifierTest`** pass on **`main`**. Record proof in **`69-VERIFICATION.md`** and flip requirement checkboxes.

## Tasks

<task id="69-01-01">
  <read_first>
    <file>accrue/mix.exs</file>
    <file>accrue_admin/mix.exs</file>
    <file>scripts/ci/verify_package_docs.sh</file>
    <file>.planning/phases/69-doc-planning-mirrors/69-CONTEXT.md</file>
  </read_first>
  <action>
    From repository root, run **`bash scripts/ci/verify_package_docs.sh`**. If exit code is non-zero, read stderr lines prefixed with **`[verify_package_docs]`** and fix **Markdown or bash** so the script passes — per **D-03**: edit **`scripts/ci/verify_package_docs.sh`** first if a **new** invariant is required; only then adjust **`accrue/test/accrue/docs/package_docs_verifier_test.exs`** if the **success banner** text, **`[verify_package_docs]`** message shape, or **`ROOT_DIR`** fixture layout changed.

    Do **not** mark **REQUIREMENTS** complete until this task’s acceptance passes.
  </action>
  <acceptance_criteria>
    <criterion>Shell `bash scripts/ci/verify_package_docs.sh` run from repository root exits **0**.</criterion>
    <criterion>Stdout from that command contains exact substring **`package docs verified for accrue`**.</criterion>
    <criterion>Stdout contains exact substring **`and accrue_admin`**.</criterion>
  </acceptance_criteria>
</task>

<task id="69-01-02">
  <read_first>
    <file>accrue/test/accrue/docs/package_docs_verifier_test.exs</file>
    <file>scripts/ci/verify_package_docs.sh</file>
  </read_first>
  <action>
    From **`accrue/`** directory (`cd accrue`), run:

    **`mix test test/accrue/docs/package_docs_verifier_test.exs`**

    If any test fails, fix **fixtures under `test/accrue/docs/package_docs_verifier_fixtures/`** (if referenced) or **production files** so behavior matches **task 69-01-01**; follow **D-03** — do not weaken assertions to mask bash drift.
  </action>
  <acceptance_criteria>
    <criterion>Shell `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits **0**.</criterion>
    <criterion>Test output includes **`package docs verifier`** (ExUnit module name fragment) or **`Finished`** summary with **0 failures**.</criterion>
  </acceptance_criteria>
</task>

<task id="69-01-03">
  <read_first>
    <file>accrue/guides/first_hour.md</file>
    <file>accrue/README.md</file>
    <file>accrue_admin/README.md</file>
    <file>accrue/mix.exs</file>
  </read_first>
  <action>
    Let **`V`** be the **`@version`** string in **`accrue/mix.exs`** (e.g. **`0.3.1`**). Confirm these exact grep needles exist (executor may use **`rg -F`**):

    - **`accrue/guides/first_hour.md`:** substring **`{:accrue, "~> `** + **`V`** + **`"}`** and **`{:accrue_admin, "~> `** + **`V`** + **`"}`**
    - **`accrue/README.md`:** substring **`{:accrue, "~> `** + **`V`** + **`"}`**
    - **`accrue_admin/README.md`:** substring **`{:accrue_admin, "~> `** + **`V`** + **`"}`** and **`accrue ~> `** + **`V`** (space as in script)

    If any line is missing, add or correct the **pin lines only** (per **D-04** — no voice rewrite).
  </action>
  <acceptance_criteria>
    <criterion>With **`V=$(sed -n 's/^  @version "\([^"]*\)"/\1/p' accrue/mix.exs | head -1)`**, shell test **`test -n "$V"`** exits **0**.</criterion>
    <criterion>Shell `rg -F "{:accrue, \"~> $V\"}" accrue/guides/first_hour.md` exits **0**.</criterion>
    <criterion>Shell `rg -F "{:accrue_admin, \"~> $V\"}" accrue/guides/first_hour.md` exits **0**.</criterion>
    <criterion>Shell `rg -F "{:accrue, \"~> $V\"}" accrue/README.md` exits **0**.</criterion>
    <criterion>Shell `rg -F "{:accrue_admin, \"~> $V\"}" accrue_admin/README.md` exits **0**.</criterion>
    <criterion>Shell `rg -F "accrue ~> $V" accrue_admin/README.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="69-01-04">
  <read_first>
    <file>.planning/phases/68-release-train/68-VERIFICATION.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>
    Create **`.planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md`** with YAML frontmatter:

    ```yaml
    ---
    phase: 69-doc-planning-mirrors
    status: pending
    ---
    ```

    Body must include:

    1. **`## DOC integrator proof (DOC-01, DOC-02)`** heading.
    2. A bullet stating **`bash scripts/ci/verify_package_docs.sh`** was run from repo root with **exit 0** on date **`YYYY-MM-DD`** (executor’s UTC or local date).
    3. A bullet stating **`mix test test/accrue/docs/package_docs_verifier_test.exs`** was run from **`accrue/`** with **exit 0** same date.
    4. One line naming **`@version`** values read from both **`mix.exs`** files (must match each other).

    Do **not** set frontmatter **`status: complete`** here — **Plan 69-02** completes HYG and then sets **`complete`**.
  </action>
  <acceptance_criteria>
    <criterion>Command `test -f .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'phase: 69-doc-planning-mirrors' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '## DOC integrator proof' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'verify_package_docs.sh' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'package_docs_verifier_test.exs' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="69-01-05">
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md</file>
  </read_first>
  <action>
    After **tasks 69-01-01** through **69-01-04** acceptance all pass, edit **`.planning/REQUIREMENTS.md`**:

    1. Under **`## Integrator docs (DOC)`**, change **`- [ ]` → `- [x]`** for **DOC-01** and **DOC-02** checklist lines.
    2. In **`## Traceability`**, set the **Status** column to **`Complete`** for rows **DOC-01** and **DOC-02** (Phase **69**).

    Do **not** flip **HYG-01** in this task.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '- [x] **DOC-01**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '- [x] **DOC-02**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| DOC-01 | 69 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| DOC-02 | 69 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '- [ ] **HYG-01**' .planning/REQUIREMENTS.md` exits **0** (still pending until Plan **02**).</criterion>
  </acceptance_criteria>
</task>

## Verification

- Re-run **`bash scripts/ci/verify_package_docs.sh`** and **`cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs`** after the last file edit in this plan.

## must_haves

- **`verify_package_docs.sh`** and **`PackageDocsVerifierTest`** both green on **`main`**.
- **`69-VERIFICATION.md`** exists with a **DOC** proof section.
- **DOC-01** and **DOC-02** marked **`[x]`** in **`.planning/REQUIREMENTS.md`** with traceability **Complete**.

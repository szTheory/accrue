---
phase: 69-doc-planning-mirrors
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [HYG-01]
autonomous: true
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
  - .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md
  - .planning/REQUIREMENTS.md
tags: [v1.19, HYG-01, planning-hygiene, STATE, PROJECT]
---

# Plan 69-02 — Planning file Hex mirrors (HYG-01)

<threat_model>
  <summary>ASVS L1; maintainer-facing markdown. Public URLs only (Hex, GitHub docs).</summary>
  <threat id="T-69-03">Contributors read conflicting “last published” versions across <strong>PROJECT</strong>, <strong>MILESTONES</strong>, and <strong>STATE</strong>.</threat>
  <mitigation>Single numeric fact for <code>accrue</code>/<code>accrue_admin</code> patch; align with <code>mix.exs @version</code> and Phase <strong>68</strong> ship evidence; prefer pointers over duplicating long blurbs (<strong>69-CONTEXT</strong> D-02).</mitigation>
</threat_model>

## Objective

Close **HYG-01**: **`.planning/PROJECT.md`**, **`.planning/MILESTONES.md`**, and **`.planning/STATE.md`** show **consistent** “public Hex” / **`accrue` / `accrue_admin`** version callouts matching **actual** **`@version`** in **`accrue/mix.exs`** and **`accrue_admin/mix.exs`** and the **v1.19** posture (Phases **67–68** complete, **69** executing or complete). **Minimal** edits only — no narrative rewrites (**D-02**).

## Tasks

<task id="69-02-01">
  <read_first>
    <file>accrue/mix.exs</file>
    <file>accrue_admin/mix.exs</file>
    <file>.planning/PROJECT.md</file>
    <file>.planning/MILESTONES.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/69-doc-planning-mirrors/69-CONTEXT.md</file>
  </read_first>
  <action>
    Read **`V`** = **`@version`** from **`accrue/mix.exs`** (must equal **`accrue_admin`**).

    Audit the three files for **factual** drift:

    - **`PROJECT.md`:** **Public Hex (last published):** line must state **`V`** for both packages in a way consistent with registry reality (same **`V`** as **`mix.exs`** on **`main`** post-ship). **Current State / Now** lines should list Phase **69** as the active doc mirror step while **69** is open, or “complete” only after this plan merges — pick **one** truthful posture and align dates if you touch them.
    - **`MILESTONES.md`:** **v1.19** theme block must not claim an older published pair than **`V`**; patch-level **minimal** fix only.
    - **`STATE.md`:** YAML **`last_updated`** (ISO) and **Current Position** must reflect **Phase 69** work and **`V`** for Hex last-shipped line if present.

    Apply **smallest diff** that resolves contradictions. Do **not** expand scope beyond these three paths.
  </action>
  <acceptance_criteria>
    <criterion>Let **`V=$(sed -n 's/^  @version "\([^"]*\)"/\1/p' accrue/mix.exs | head -1)`**. Shell `rg -F "$V" .planning/PROJECT.md` exits **0**.</criterion>
    <criterion>Shell `rg -F "$V" .planning/MILESTONES.md` exits **0**.</criterion>
    <criterion>Shell `rg -F "$V" .planning/STATE.md` exits **0**.</criterion>
    <criterion>Shell `rg -F '**Phase:** **69**' .planning/STATE.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="69-02-02">
  <read_first>
    <file>.planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md</file>
  </read_first>
  <action>
    Append to **`.planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md`** (after the DOC section) a new heading **`## Planning hygiene proof (HYG-01)`** with bullets:

    1. List the three files **`PROJECT.md`**, **`MILESTONES.md`**, **`STATE.md`** as reviewed for Hex **`V`** consistency on **`YYYY-MM-DD`**.
    2. One sentence: “No fourth conflicting version string introduced outside **`mix.exs`** SSOT.”

    Update YAML frontmatter **`status:`** from **`pending`** to **`complete`**.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '## Planning hygiene proof (HYG-01)' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'status: complete' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'PROJECT.md' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'MILESTONES.md' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'STATE.md' .planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="69-02-03">
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/69-doc-planning-mirrors/69-VERIFICATION.md</file>
  </read_first>
  <action>
    After **69-02-01** and **69-02-02** pass, edit **`.planning/REQUIREMENTS.md`**:

    1. Change **`- [ ]` → `- [x]`** for **HYG-01** under **`## Planning hygiene (HYG)`**.
    2. In **`## Traceability`**, set **HYG-01** row **Status** to **`Complete`** (Phase **69**).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '- [x] **HYG-01**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| HYG-01 | 69 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

- Re-run **`bash scripts/ci/verify_package_docs.sh`** if any integrator-facing path was touched (should be **none** in this plan — if touched accidentally, script must stay green).

## must_haves

- **HYG-01** checkbox and traceability row **Complete**.
- **`69-VERIFICATION.md`** includes **HYG** section and **`status: complete`**.

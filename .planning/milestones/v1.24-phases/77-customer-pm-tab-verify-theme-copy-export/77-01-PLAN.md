---
phase: 77-customer-pm-tab-verify-theme-copy-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  - examples/accrue_host/docs/verify01-v112-admin-paths.md
autonomous: true
requirements: [ADM-15]
security_enforcement: enabled
tags: [VERIFY-01, ADM-15, playwright, axe, accrue_host, v1.24]
must_haves:
  truths:
    - "Merge-blocking host Playwright spec exercises `/billing/customers/:id?tab=payment_methods&org=<slug>` with light+dark `scanAxe` (critical+serious only) on desktop projects"
    - "`verify01-v112-admin-paths.md` maps the new `test.describe` title to that URL template and **ADM-15**"
  artifacts:
    - path: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      provides: "ADM-15 automated axe coverage for customer PM tab"
      contains: "VERIFY-01 admin customer detail payment_methods tab"
      min_lines: 1
---

<objective>
Close **ADM-15**: extend merge-blocking **VERIFY-01** with **Playwright + `@axe-core/playwright`** for the **customer detail `payment_methods` tab** on the host spine (`examples/accrue_host`), and record the path in the **VERIFY matrix** doc per **77-CONTEXT** **D-01–D-05**, **D-08–D-09**.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-77-01 | Logging fixture JSON or processor IDs to CI output | medium | Use existing `expect` patterns only; do not `console.log` fixture contents |
| T-77-02 | Hard-coding English UI strings in Playwright | medium | Assert via `copyStrings.<key>` from `generated/copy_strings.json` only |
| T-77-03 | Axe false negatives by narrowing `include()` incorrectly | low | Full-document `scanAxe` after tab stable (CONTEXT **D-07**) |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/77-customer-pm-tab-verify-theme-copy-export/77-CONTEXT.md
@.planning/phases/77-customer-pm-tab-verify-theme-copy-export/77-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<task type="auto">
  <name>Task 1: VERIFY matrix row for ADM-15</name>
  <read_first>
    - examples/accrue_host/docs/verify01-v112-admin-paths.md
    - .planning/phases/77-customer-pm-tab-verify-theme-copy-export/77-CONTEXT.md
  </read_first>
  <files>
    - examples/accrue_host/docs/verify01-v112-admin-paths.md
  </files>
  <action>
    Append a new markdown subsection **`## Phase 77 — v1.24 ADM-15 (customer payment_methods tab)`** after the Phase 55 invoice subsection (before EOF), containing:

    1. One sentence: merge-blocking coverage lives in **`verify01-admin-a11y.spec.js`** for **customer detail** **`payment_methods`**.

    2. A **pipe table** with header row exactly:  
       `| \`test.describe\` title (exact) | Mounted path template | Requirement ids |`  
       and one data row:
       - Column 1: **`VERIFY-01 admin customer detail payment_methods tab (v1.24 ADM-15)`** (this exact string — the Playwright `test.describe` in Task 2 MUST match character-for-character).
       - Column 2: **`/billing/customers/:id?tab=payment_methods&org=<slug>`** (use `:id` and `<slug>` placeholders).
       - Column 3: **`ADM-15`**
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -nF "Phase 77" examples/accrue_host/docs/verify01-v112-admin-paths.md && rg -nF "ADM-15" examples/accrue_host/docs/verify01-v112-admin-paths.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -nF '## Phase 77 — v1.24 ADM-15 (customer payment_methods tab)' examples/accrue_host/docs/verify01-v112-admin-paths.md`
    - `rg -nF 'VERIFY-01 admin customer detail payment_methods tab (v1.24 ADM-15)' examples/accrue_host/docs/verify01-v112-admin-paths.md`
    - `rg -nF '/billing/customers/:id?tab=payment_methods&org=<slug>' examples/accrue_host/docs/verify01-v112-admin-paths.md`
    - `rg -nF 'ADM-15' examples/accrue_host/docs/verify01-v112-admin-paths.md`
  </acceptance_criteria>
  <done>VERIFY path matrix documents ADM-15 row.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright + axe — customer `payment_methods` tab</name>
  <read_first>
    - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    - examples/accrue_host/e2e/support/fixture.js
    - examples/accrue_host/e2e/generated/copy_strings.json
    - scripts/ci/accrue_host_seed_e2e.exs
    - .planning/phases/77-customer-pm-tab-verify-theme-copy-export/77-CONTEXT.md
  </read_first>
  <files>
    - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  </files>
  <action>
    At **end of file** (after the last `test.describe` / test block — currently **`core-admin-invoices-detail`**), append a new **`test.describe("VERIFY-01 admin customer detail payment_methods tab (v1.24 ADM-15)", () => { ... })`** containing **one** `test` named **`passes axe in light and dark themes on desktop`** that:

    1. Uses **`test.skip`** for `testInfo.project.name === "chromium-mobile" || testInfo.project.name === "chromium-mobile-tagged"` with skip reason text containing **`theme toggle is hidden below the md breakpoint`**.

    2. Calls **`reseedFixture()`**, **`readFixture()`**, then **`expect(fixture.admin_org_alpha_slug).toBeTruthy()`** and **`expect(fixture.admin_denial_customer_id).toBeTruthy()`**.

    3. Performs **`await login(page, fixture, fixture.admin_email)`**, clicks **"Go to billing"**, **`waitForLiveView`**, clicks **`button[data-organization-slug="${fixture.admin_org_alpha_slug}"]`**, **`waitForLiveView`**.

    4. Navigates to  
       **`const pmUrl = \`/billing/customers/${fixture.admin_denial_customer_id}?tab=payment_methods&org=${encodeURIComponent(fixture.admin_org_alpha_slug)}\`;`**  
       **`await page.goto(pmUrl, { waitUntil: "domcontentloaded" });`** then **`await waitForLiveView(page);`**

    5. Asserts **`await expect(page.locator("html")).toHaveClass(/accrue-admin/);`**

    6. Asserts visible Copy-backed heading: **`await expect(page.getByText(copyStrings.customer_payment_methods_section_heading)).toBeVisible();`**

    7. **Light theme:** click **`button[data-theme-target="light"]`**, **`waitForLiveView`**, **`toHaveAttribute("data-theme", "light")`**, then **`let violations = await scanAxe(page);`**, **`expect(violations, JSON.stringify(violations, null, 2)).toEqual([]);`**

    8. **Dark theme:** click **`button[data-theme-target="dark"]`**, **`waitForLiveView`**, **`toHaveAttribute("data-theme", "dark")`**, then **reuse the same `waitForFunction` body** as in **`core-admin-invoices-index`** (lines 344–351: querySelector `a.ax-sidebar-link-active` + `getComputedStyle` backgroundColor RGB check).

    9. **`violations = await scanAxe(page);`** and **`expect(violations, JSON.stringify(violations, null, 2)).toEqual([]);`**

    Do **not** add `disableRules` / broad `exclude`. Do **not** introduce literal English strings for visible copy beyond what already exists in **`copy_strings.json`** keys.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && npx playwright test e2e/verify01-admin-a11y.spec.js --grep "VERIFY-01 admin customer detail payment_methods tab"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -nF 'test.describe("VERIFY-01 admin customer detail payment_methods tab (v1.24 ADM-15)"' examples/accrue_host/e2e/verify01-admin-a11y.spec.js`
    - `rg -nF 'tab=payment_methods' examples/accrue_host/e2e/verify01-admin-a11y.spec.js`
    - `rg -nF 'admin_denial_customer_id' examples/accrue_host/e2e/verify01-admin-a11y.spec.js`
    - `rg -nF 'copyStrings.customer_payment_methods_section_heading' examples/accrue_host/e2e/verify01-admin-a11y.spec.js`
    - **`rg -c "await scanAxe" examples/accrue_host/e2e/verify01-admin-a11y.spec.js`** prints a number **≥ 12** (file had **10** `await scanAxe` calls before this task; new test adds **2**)
    - Playwright command in `<verify>` exits **0**
  </acceptance_criteria>
  <done>ADM-15 merge-blocking axe journey exists and passes locally.</done>
</task>

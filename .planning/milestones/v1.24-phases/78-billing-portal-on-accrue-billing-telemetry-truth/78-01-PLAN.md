---
phase: 78-billing-portal-on-accrue-billing-telemetry-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing.ex
  - accrue/test/accrue/billing/billing_portal_session_facade_test.exs
autonomous: true
requirements: [BIL-04]
security_enforcement: enabled
tags: [BIL-04, billing_portal, telemetry, facade, fake]
must_haves:
  truths:
    - "`Accrue.Billing.create_billing_portal_session/2` and `create_billing_portal_session!/2` exist, delegate to `Accrue.BillingPortal.Session.create/1`, and are wrapped in `span_billing(:billing_portal, :create, ...)`"
    - "Telemetry metadata for the span never includes the portal session URL string"
    - "`mix test test/accrue/telemetry/billing_span_coverage_test.exs` passes after new public functions land"
  artifacts:
    - path: "accrue/test/accrue/billing/billing_portal_session_facade_test.exs"
      provides: "Fake-backed happy path + failure path + span assertions for BIL-04"
      contains: "create_billing_portal_session"
      min_lines: 1
---

<objective>
Ship **BIL-04**: add **`Accrue.Billing.create_billing_portal_session/2`** and **`create_billing_portal_session!/2`** — `%Accrue.Billing.Customer{}` plus validated keyword/map attrs per **`.planning/REQUIREMENTS.md`** — delegating to **`Accrue.BillingPortal.Session.create/1`**, with **`span_billing(:billing_portal, :create, ...)`** and **Fake-backed ExUnit** (happy path + at least one `{:error, _}` class via **`Accrue.Processor.Fake.scripted_response(:portal_session_create, ...)`**).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-78-01 | Portal URL leaked via telemetry metadata or logs | high | Reuse `billing_metadata/4` only; never pass session map or `:url` into metadata; grep test asserts metadata string does not contain Fake session URL prefix |
| T-78-02 | Unvalidated attrs pass unexpected keys to Stripe | medium | `NimbleOptions.validate!` on allowed second-arg keys only: `:return_url`, `:configuration`, `:flow_data`, `:locale`, `:on_behalf_of`, `:operation_id` (mirror `Accrue.BillingPortal.Session` `@create_schema` minus `:customer`) |
| T-78-03 | Missing span breaks `BillingSpanCoverageTest` | medium | Implement with `span_billing`; run coverage test in verify |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/78-billing-portal-on-accrue-billing-telemetry-truth/78-RESEARCH.md
@.planning/phases/78-billing-portal-on-accrue-billing-telemetry-truth/78-PATTERNS.md
</context>

<task type="auto">
  <name>Task 1: Billing facade functions + @doc</name>
  <read_first>
    - accrue/lib/accrue/billing.ex
    - accrue/lib/accrue/billing_portal/session.ex
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - accrue/lib/accrue/billing.ex
  </files>
  <action>
    1. Near other payment-method / checkout-adjacent delegates (after `list_payment_methods!` block ~line 370), add **`@billing_portal_session_attrs_schema`** as a **`NimbleOptions` schema** listing exactly: **`return_url`**, **`configuration`**, **`flow_data`**, **`locale`**, **`on_behalf_of`**, **`operation_id`** — types and defaults matching **`Accrue.BillingPortal.Session`** `@create_schema` for those keys (customer excluded — first arg).

    2. Implement **`create_billing_portal_session(%Accrue.Billing.Customer{} = customer, attrs)`** when `attrs` is **`keyword()` or `map()`**:
       - `opts_list = if is_list(attrs), do: attrs, else: Map.to_list(attrs)`
       - `validated = NimbleOptions.validate!(opts_list, @billing_portal_session_attrs_schema)`
       - Inside **`span_billing(:billing_portal, :create, customer, validated, fn -> ... end)`**, call **`Accrue.BillingPortal.Session.create(Map.new([customer: customer] ++ validated))`**

    3. Implement **`create_billing_portal_session!/2`** raising on `{:error, e}` when `e` is exception re-raise; else **`RuntimeError`** with **`inspect(e, limit: :infinity)`** forbidden for URLs — use the same string pattern as **`Session.create!/1`**: **`raise "Accrue.BillingPortal.Session.create/1 failed: #{inspect(other)}"`** only on non-exception errors (copy that branch shape).

    4. **`@doc`** blocks: state attrs mirror Session; note **portal URL must never be logged**; point to **`guides/portal_configuration_checklist.md`** for `:configuration`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def create_billing_portal_session" accrue/lib/accrue/billing.ex`
    - `rg -n ":billing_portal, :create" accrue/lib/accrue/billing.ex`
    - `rg -n "BillingPortal.Session.create" accrue/lib/accrue/billing.ex`
  </acceptance_criteria>
  <done>Facade compiles and calls Session inside span.</done>
</task>

<task type="auto">
  <name>Task 2: ExUnit — happy path, failure, telemetry metadata</name>
  <read_first>
    - accrue/test/accrue/billing_portal_test.exs
    - accrue/test/support/billing_case.ex
    - accrue/test/accrue/billing/meter_event_actions_test.exs
    - accrue/lib/accrue/billing.ex
  </read_first>
  <files>
    - accrue/test/accrue/billing/billing_portal_session_facade_test.exs
  </files>
  <action>
    Create **`accrue/test/accrue/billing/billing_portal_session_facade_test.exs`** using **`Accrue.BillingCase, async: false`** (same setup style as **`billing_portal_test.exs`**: insert **`%Customer{}`** with **`processor_id: "cus_fake_portal_facade"`** or unique id).

    1. **Happy path:** `assert {:ok, %Accrue.BillingPortal.Session{}} = Accrue.Billing.create_billing_portal_session(customer, return_url: "https://example.com/return", configuration: "bpc_test_facade")` — assert **`session.id`** starts with **`"bps_fake_"`**.

    2. **Bang path:** `Accrue.Billing.create_billing_portal_session!(customer, %{return_url: "https://example.com/r2"})` returns session struct.

    3. **Failure path:** `Fake.scripted_response(:portal_session_create, {:error, %Accrue.APIError{code: "portal_rejected", http_status: 400, message: "nope"}})` then assert **`{:error, %Accrue.APIError{code: "portal_rejected"}} = Accrue.Billing.create_billing_portal_session(customer, return_url: "https://x.test")`**

    4. **Telemetry:** `:telemetry.attach` on **`[:accrue, :billing, :billing_portal, :create, :start]`** — in handler capture **`metadata`**; assert **`metadata[:operation] == "billing_portal.create"`** (same string shape as **`billing_metadata/4`** `operation` field for resource **`:billing_portal`** and action **`:create`**); assert **`not String.contains?(inspect(metadata), "https://billing.stripe.test/p/session/")`**; detach handler in **`try/after`** or **`on_exit`**.

    5. **`alias`** only what is needed; **`import`** if test file uses **`Customer`** from billing schema like **`billing_portal_test.exs`** (`alias Accrue.Billing.Customer`).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/billing/billing_portal_session_facade_test.exs test/accrue/telemetry/billing_span_coverage_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/accrue/billing/billing_portal_session_facade_test.exs` exits 0
    - `mix test test/accrue/telemetry/billing_span_coverage_test.exs` exits 0
    - Test file contains the substring **`Fake.scripted_response(:portal_session_create`**
  </acceptance_criteria>
  <done>BIL-04 automated proof green.</done>
</task>

<verification>
- `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/billing/billing_portal_session_facade_test.exs test/accrue/telemetry/billing_span_coverage_test.exs`
- `cd /Users/jon/projects/accrue/accrue && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- BIL-04 requirement text in **`.planning/REQUIREMENTS.md`** satisfied by implemented API + tests
- Span tuple **`:billing_portal`** **`:create`** present in **`billing.ex`**
- No portal URL in telemetry metadata assertions
</success_criteria>

---
phase: 082-first-hour-portal-spine
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INT-13]
autonomous: true
files_modified:
  - accrue/guides/telemetry.md
  - accrue/guides/first_hour.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - examples/accrue_host/README.md
tags: [INT-13, first-hour, billing-portal, adoption-spine, v1.26]
---

# Plan 082-01 — Integrator markdown spine (telemetry, First Hour, matrix, host README)

<threat_model>
  <summary>ASVS L1 documentation. Primary risks: misleading integrators about secrets in Customer Portal flows, and broken deep links that train readers to distrust the doc set.</summary>
  <threat id="T-82-01">First Hour or host README implies portal sessions or telemetry metadata are safe to log or paste into tickets, encouraging leakage of return URLs or customer identifiers.</threat>
  <mitigation>Mirror the checkout paragraph’s tone: name the public API and tuple; link to telemetry catalog; reference ExUnit SSOT path only — do not add example URLs with query tokens or live portal return URLs.</mitigation>
  <threat id="T-82-02">Anchor ID typo breaks <code>#billing-billing-portal-create</code> links across First Hour, host README, and CI substring gates.</threat>
  <mitigation>Use the exact anchor string <code>billing-billing-portal-create</code> from <code>082-CONTEXT.md</code> D-01; verify with <code>rg</code> across all four edited files in acceptance criteria.</mitigation>
</threat_model>

## Objective

Land **INT-13** user-facing markdown: **telemetry catalog anchor** for billing portal create, **parallel First Hour paragraph** after checkout, **second blocking row** in **`adoption-proof-matrix.md`**, and **sibling Observability bullet** in **`examples/accrue_host/README.md`** — matching **`082-CONTEXT.md`** decisions **D-01**, **D-03**, **D-06** (no **`verify_*.sh`** edits in this plan).

## Tasks

<task id="82-01-01">
  <read_first>
    <file>accrue/guides/telemetry.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`accrue/guides/telemetry.md`**, on the **`accrue.billing.billing_portal.create`** bullet (currently the two lines starting with **`- \`accrue.billing.billing_portal.create\`**`), insert an HTML anchor **immediately before** the backtick event name, using the **same pattern** as the checkout row on the following lines: a hyphen, space, then **`<a id="billing-billing-portal-create"></a>`** followed by a space and the existing **`\`accrue.billing.billing_portal.create\` —`** text.

    Preserve the existing **Phase 78 / BIL-04** prose on the continuation line unchanged except for line wrapping if needed.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'id="billing-billing-portal-create"' accrue/guides/telemetry.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'accrue\.billing\.billing_portal\.create' accrue/guides/telemetry.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="82-01-02">
  <read_first>
    <file>accrue/guides/first_hour.md</file>
    <file>accrue/guides/telemetry.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`accrue/guides/first_hour.md`**, **after** the existing **Stripe Checkout** paragraph (the block ending with **`telemetry.md#billing-checkout-session-create`**), insert **one optional short sentence** (your wording) stating that **Customer Portal** session creation is the parallel server-side helper — then add a **second paragraph** mirroring checkout structure with these **exact substrings** (copy tuple and API spelling verbatim):

    1. **`Accrue.Billing.create_billing_portal_session/2`**
    2. **`[:accrue, :billing, :billing_portal, :create]`**
    3. A Markdown link whose **href** ends with **`telemetry.md#billing-billing-portal-create`** (relative path must stay consistent with the checkout line — use **`telemetry.md#billing-billing-portal-create`** inside the link target like checkout does).

    Do **not** remove or merge the checkout paragraph.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'Accrue\.Billing\.create_billing_portal_session/2' accrue/guides/first_hour.md` exits **0**.</criterion>
    <criterion>Command `rg -nF '[:accrue, :billing, :billing_portal, :create]' accrue/guides/first_hour.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'telemetry\.md#billing-billing-portal-create' accrue/guides/first_hour.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'billing-checkout-session-create' accrue/guides/first_hour.md` exits **0** (checkout paragraph preserved).</criterion>
  </acceptance_criteria>
</task>

<task id="82-01-03">
  <read_first>
    <file>examples/accrue_host/docs/adoption-proof-matrix.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`examples/accrue_host/docs/adoption-proof-matrix.md`**, under **`## Blocking: Fake-backed host + browser`**, add a **new table row immediately below** the existing checkout facade row. The new row must be a **sibling** row (not merged cells) with these **exact cell literals** (pipe table, three columns):

    - **Column “Concern”:** **`Billing \`**\`Accrue.Billing.create_billing_portal_session/2\`** facade + \`**\`[:accrue, :billing, :billing_portal, :create]\`** telemetry contract**` (use the same bold/backtick style as the checkout row on line ~17).

    - **Column “Proof”:** **`billing_portal_session_facade_test.exs` + First Hour / `guides/telemetry.md`**

    - **Column “Where”:** **`accrue` package**

    Do not alter the checkout row text.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -nF 'Accrue.Billing.create_billing_portal_session/2' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0**.</criterion>
    <criterion>Command `rg -nF '[:accrue, :billing, :billing_portal, :create]' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'billing_portal_session_facade_test\.exs' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0**.</criterion>
    <criterion>Command `rg -nF 'Accrue.Billing.create_checkout_session/2' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0** (checkout row intact).</criterion>
  </acceptance_criteria>
</task>

<task id="82-01-04">
  <read_first>
    <file>examples/accrue_host/README.md</file>
    <file>accrue/guides/first_hour.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`examples/accrue_host/README.md`** under **`## Observability`**, add a **new list item immediately after** the **Billing checkout facade** bullet (~line 92). The new bullet must:

    - Start with a bold label **`Billing portal facade:`** (parallel to **`Billing checkout facade:`**).

    - Include **exact substrings:** **`Accrue.Billing.create_billing_portal_session/2`**, **`[:accrue, :billing, :billing_portal, :create]`**, a **relative** link to **`../../accrue/guides/telemetry.md#billing-billing-portal-create`**, and **`accrue/test/accrue/billing/billing_portal_session_facade_test.exs`** (same field order idea as checkout bullet: API, tuple, telemetry anchor, ExUnit SSOT path).

    Do not merge the checkout and portal bullets into one item.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'Billing portal facade' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -nF 'Accrue.Billing.create_billing_portal_session/2' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -nF '[:accrue, :billing, :billing_portal, :create]' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'telemetry\.md#billing-billing-portal-create' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'billing_portal_session_facade_test\.exs' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'Billing checkout facade' examples/accrue_host/README.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="82-01-05">
  <read_first>
    <file>examples/accrue_host/README.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`examples/accrue_host/README.md`**, inside **`## Proof and verification`** (starts ~line 98), add **one short Markdown line** in the opening block (before **`### Verification modes`**) that satisfies **INT-13** host README **§ proof-and-verification** capsule / **D-02** cross-link discipline when First Hour gains portal prose. The line must:

    - Link to **First Hour** using the **same relative path style** as existing README links: **`../../accrue/guides/first_hour.md`**.

    - Name **both** **`Accrue.Billing.create_checkout_session/2`** and **`Accrue.Billing.create_billing_portal_session/2`** (exact arity spelling).

    - Point readers to **telemetry + ExUnit SSOT** for both (either by referencing **`## Observability`** with a same-file fragment link **`#observability`**, or by repeating the tuple + test-path vocabulary — choose the minimal option that keeps the paragraph one sentence).

    Do not duplicate the full Observability bullets; this is a **capsule pointer**, not a second copy of the tables.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '## Proof and verification' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'create_checkout_session/2' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'create_billing_portal_session/2' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'guides/first_hour\.md' examples/accrue_host/README.md` exits **0**.</criterion>
    <criterion>Command `awk '/## Proof and verification/,/### Verification modes/' examples/accrue_host/README.md | rg -n 'first_hour'` exits **0** (First Hour pointer appears in the proof opening block).</criterion>
  </acceptance_criteria>
</task>

## Verification

From repository root (scripts unchanged in this plan — optional smoke only):

```bash
rg -n 'billing-billing-portal-create' accrue/guides/telemetry.md accrue/guides/first_hour.md examples/accrue_host/README.md
rg -nF '[:accrue, :billing, :billing_portal, :create]' accrue/guides/first_hour.md examples/accrue_host/README.md examples/accrue_host/docs/adoption-proof-matrix.md
```

## Success criteria

- **INT-13** markdown spine: telemetry anchor, First Hour portal paragraph, matrix row, host Observability bullet all contain the canonical API, tuple, anchor, and test path literals.
- **`082-CONTEXT.md`** decisions **D-01**, **D-03**, **D-06** satisfied for markdown-only scope.

## must_haves

- `id="billing-billing-portal-create"` exists in **`telemetry.md`**.
- Parallel **portal** prose exists in **`first_hour.md`** without removing checkout prose.
- Second **blocking** matrix row documents portal facade with merge-blocking literals.
- Host **`README.md`** Observability section has a **sibling** portal bullet.

---

## PLANNING COMPLETE

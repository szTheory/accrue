---
phase: 082-first-hour-portal-spine
plan: 02
type: execute
wave: 2
depends_on: ["082-01-PLAN.md"]
requirements: [INT-13]
autonomous: true
files_modified:
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/verify_adoption_proof_matrix.sh
  - accrue/CHANGELOG.md
  - .planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-VERIFICATION.md
tags: [INT-13, ci, adoption-matrix, changelog, v1.26]
---

# Plan 082-02 — CI substring gates + CHANGELOG + phase verification doc

<threat_model>
  <summary>CI scripts enforce doc truth; primary risk is brittle needles that break unrelated edits, and accidentally widening substrings that create false-green ORG-09 drift.</summary>
  <threat id="T-82-03">Substring requirements are shortened to generic “portal” tokens, losing falsifiability under INT-12.</threat>
  <mitigation>Use the three exact literals from <code>082-CONTEXT.md</code> D-04 and D-07 in <code>require_substring</code> / <code>require_fixed</code> calls; do not substitute synonyms.</mitigation>
  <threat id="T-82-04">CHANGELOG entry leaks live Stripe secrets or internal URLs.</threat>
  <mitigation>Keep <code>[Unreleased]</code> bullets to integrator-facing doc/CI references only — no env values, no <code>sk_live</code>, no webhook secrets.</mitigation>
</threat_model>

## Objective

Close **INT-13** merge-blocking proof: extend **`verify_package_docs.sh`** and **`verify_adoption_proof_matrix.sh`**, record **`[Unreleased]`** **Keep a Changelog** notes in **`accrue/CHANGELOG.md`**, and author falsifiable **`082-VERIFICATION.md`** — after plan **082-01** literals exist in the tree (**D-04**, **D-07**, **D-08**, **D-09**).

## Tasks

<task id="82-02-01">
  <read_first>
    <file>scripts/ci/verify_package_docs.sh</file>
    <file>accrue/guides/first_hour.md</file>
    <file>examples/accrue_host/README.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`scripts/ci/verify_package_docs.sh`**, **immediately after** the three existing checkout **`require_fixed`** lines for **`first_hour.md`** (**`create_checkout_session/2`**, **`[:accrue, :billing, :checkout_session, :create]`**, **`billing-checkout-session-create`**), append **three** new **`require_fixed`** calls for **`$first_hour_md`** with these **exact needles** (same order as checkout trio):

    1. **`Accrue.Billing.create_billing_portal_session/2`**
    2. **`[:accrue, :billing, :billing_portal, :create]`**
    3. **`billing-billing-portal-create`**

    Then **immediately after** the three checkout **`require_fixed`** lines for **`examples/accrue_host/README.md`** (**`create_checkout_session/2`**, checkout tuple, **`checkout_session_facade_test.exs`**), append **three** new **`require_fixed`** calls for **`$host_readme_md`** with:

    1. **`Accrue.Billing.create_billing_portal_session/2`**
    2. **`[:accrue, :billing, :billing_portal, :create]`**
    3. **`billing_portal_session_facade_test.exs`**

    Use the same **`require_fixed "$ROOT_DIR/...` quoting style** as neighboring lines.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'create_billing_portal_session' scripts/ci/verify_package_docs.sh` exits **0**.</criterion>
    <criterion>Command `rg -nF '[:accrue, :billing, :billing_portal, :create]' scripts/ci/verify_package_docs.sh` exits **0**.</criterion>
    <criterion>Command `bash scripts/ci/verify_package_docs.sh` from repository root exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="82-02-02">
  <read_first>
    <file>scripts/ci/verify_adoption_proof_matrix.sh</file>
    <file>examples/accrue_host/docs/adoption-proof-matrix.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`scripts/ci/verify_adoption_proof_matrix.sh`**, **after** the existing **`require_substring`** line for **`checkout_session_facade_test.exs`** (~line 43), append **three** new **`require_substring`** calls:

    1. **`require_substring "Accrue.Billing.create_billing_portal_session/2" "billing portal facade API in matrix"`**
    2. **`require_substring "[:accrue, :billing, :billing_portal, :create]" "billing portal billing span tuple in matrix"`**
    3. **`require_substring "billing_portal_session_facade_test.exs" "billing portal facade ExUnit path in matrix"`**
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'billing_portal_session_facade_test' scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.</criterion>
    <criterion>Command `bash scripts/ci/verify_adoption_proof_matrix.sh` from repository root exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="82-02-03">
  <read_first>
    <file>accrue/CHANGELOG.md</file>
    <file>.planning/milestones/v1.25-phases/081-telemetry-truth-integrator-contracts/081-VERIFICATION.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    In **`accrue/CHANGELOG.md`** under **`[Unreleased]`**, add a **`### Documentation`** bullet (or bullets) summarizing **INT-13**: First Hour + host README + adoption proof matrix now carry **billing portal** facade parity needles alongside checkout (**no** **`### Telemetry`** section unless emission code changed — unlikely).

    If **`scripts/ci/verify_package_docs.sh`** or **`verify_adoption_proof_matrix.sh`** changed, add a **`### CI`** bullet listing which verifier(s) gained portal literals.

    Follow **Keep a Changelog** section ordering already present in the file.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '\[Unreleased\]' accrue/CHANGELOG.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'billing_portal\|Customer Portal\|create_billing_portal_session' accrue/CHANGELOG.md` exits **0** (at least one hit).</criterion>
  </acceptance_criteria>
</task>

<task id="82-02-04">
  <read_first>
    <file>.planning/milestones/v1.25-phases/081-telemetry-truth-integrator-contracts/081-VERIFICATION.md</file>
    <file>.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-CONTEXT.md</file>
  </read_first>
  <action>
    Create **`.planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-VERIFICATION.md`** with:

    - Title and front matter identifying **Phase 82** / **INT-13**.
    - A **falsifiable** table or checklist: **`git rev-parse HEAD`** SHA placeholder line, **`bash scripts/ci/verify_package_docs.sh`** expected tail line **`package docs verified`**, **`bash scripts/ci/verify_adoption_proof_matrix.sh`** expected **`OK`** line.
    - Explicit statement that **082-01** markdown + **082-02** script edits landed in the **same PR** per **`scripts/ci/README.md`** / **INT-11**.

    Keep it concise (maintainer-facing, not a essay).
  </action>
  <acceptance_criteria>
    <criterion>Shell test **`test -f .planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-VERIFICATION.md`** exits **0**.</criterion>
    <criterion>Command `rg -n 'INT-13' .planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'verify_package_docs' .planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'verify_adoption_proof_matrix' .planning/milestones/v1.26-phases/082-first-hour-portal-spine/082-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

From repository root:

```bash
bash scripts/ci/verify_package_docs.sh
bash scripts/ci/verify_adoption_proof_matrix.sh
```

## Success criteria

- Merge-blocking bash gates encode **portal** literals mirroring checkout (**D-04**, **D-07**).
- **CHANGELOG** records the integrator-visible slice.
- **`082-VERIFICATION.md`** exists as canonical phase evidence (**D-08**).

## must_haves

- Both verifier scripts exit **0** on a clean tree after plan **082-01** is applied.
- **`INT-13`** appears in **`082-VERIFICATION.md`** body text.

---

## PLANNING COMPLETE

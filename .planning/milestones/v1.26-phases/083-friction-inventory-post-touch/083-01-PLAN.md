---
phase: 83-friction-inventory-post-touch
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INV-04]
autonomous: true
files_modified:
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - scripts/ci/verify_v1_17_friction_research_contract.sh
  - .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md
  - .planning/REQUIREMENTS.md
tags: [INV-04, friction-inventory, v1.26, maintainer-pass, INT-13, FRG-01]
---

# Plan 083-01 — INV-04 friction inventory post–INT-13 maintainer pass

<threat_model>
  <summary>ASVS L1; planning artifacts + CI bash contracts only. No runtime secrets or PII.</summary>
  <threat id="T-83-01">False attestation — INV-04 certification without reproducible merge SHA + full verifier bundle including <code>docs-contracts-shift-left</code>.</threat>
  <mitigation>One 40-char <code>main</code> merge SHA; name <code>verify_package_docs.sh</code>, <code>verify_adoption_proof_matrix.sh</code>, VERIFY-01 path, <code>docs-contracts-shift-left</code>, and <code>host-integration</code>; paste transcripts in <code>083-VERIFICATION.md</code>.</mitigation>
  <threat id="T-83-02">Stale row-count contract — new inventory rows without same-PR <code>verify_v1_17_friction_research_contract.sh</code> updates.</threat>
  <mitigation>Path (a): update numeric asserts; path (b): skip task with explicit line; always end with script exit 0.</mitigation>
  <threat id="T-83-03">Dual SSOT — long certification in <code>083-VERIFICATION.md</code> contradicts inventory subsection.</threat>
  <mitigation>Normative INV-04 conclusion only under <code>### v1.26 INV-04 maintainer pass</code> in <code>v1.17-FRICTION-INVENTORY.md</code>; milestone verification file is methodology + commands only (083-CONTEXT D-06).</mitigation>
</threat_model>

## Objective

Satisfy **INV-04** after **INT-13** (Phase **82**): either **(a)** append **new sourced** **P1**/**P2** rows to **`.planning/research/v1.17-FRICTION-INVENTORY.md`** with stable **`v1.17-P*-NNN`** ids and co-update **`scripts/ci/verify_v1_17_friction_research_contract.sh`** in the **same PR**, **or** **(b)** add **`### v1.26 INV-04 maintainer pass (YYYY-MM-DD)`** immediately **after** the **`### v1.25 INV-03 maintainer pass`** block (before **`## FRG-03`**) stating no new sourced rows were warranted, with falsifiable pointers including **`docs-contracts-shift-left`** per **`.planning/REQUIREMENTS.md`**. Publish methodology + transcripts in **`.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md`**.

## Tasks

<task id="83-01-01">
  <read_first>
    <file>.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-CONTEXT.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/research/v1.17-FRICTION-INVENTORY.md</file>
    <file>scripts/ci/verify_v1_17_friction_research_contract.sh</file>
    <file>.planning/milestones/v1.25-phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md</file>
  </read_first>
  <action>
    Create **`.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md`** with YAML frontmatter:

    - **`phase: 83-friction-inventory-post-touch`**
    - **`status: pending`** until closure task, then **`complete`**

    Body must contain these **exact H2 headings**:

    1. **`## INV-04 path`** — one sentence with substring **`path (a)`** *or* **`path (b)`**; one line **`Reviewed merge SHA:`** + **40-character lowercase hex** (no branch names).
    2. **`## Normative attestation`** — must contain literal substrings **`v1.17-FRICTION-INVENTORY.md`** and **`INV-04`**; states inventory subsection is the single normative voice (pointer only).
    3. **`## Verifier bundle`** — bullet list including these **exact substrings**: **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`**, **`bash scripts/ci/verify_package_docs.sh`**, **`bash scripts/ci/verify_adoption_proof_matrix.sh`**, **`bash scripts/ci/verify_verify01_readme_contract.sh`**, **`docs-contracts-shift-left`**, and **`host-integration`**.
    4. **`## Path evidence`** — placeholder **`_(completed in task 83-01-02)_`** until task **83-01-02** runs.
  </action>
  <acceptance_criteria>
    <criterion>Test `-f .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` returns true.</criterion>
    <criterion>Command `rg -n '^## INV-04 path' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'Reviewed merge SHA:' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'docs-contracts-shift-left' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_v1_17_friction_research_contract.sh' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'host-integration' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="83-01-02">
  <read_first>
    <file>.planning/research/v1.17-FRICTION-INVENTORY.md</file>
    <file>.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md</file>
  </read_first>
  <action>
    Read **`## INV-04 path`** in **`083-VERIFICATION.md`**.

    **If it contains `path (b)`:** In **`v1.17-FRICTION-INVENTORY.md`**, insert **`### v1.26 INV-04 maintainer pass (YYYY-MM-DD)`** immediately **after** the **`### v1.25 INV-03 maintainer pass`** subsection (after its **Revisit trigger** bullets, **before** **`## FRG-03`**). Body must: cite **INV-04**; state no new sourced **P1**/**P2** rows (or justified per FRG-02 S1/S5); repeat the **same 40-char SHA** as **`083-VERIFICATION.md`**; name verifier bundle including **`verify_package_docs`**, **`verify_adoption_proof_matrix.sh`**, **VERIFY-01** / **`host-integration`**, and **`docs-contracts-shift-left`** greens on that SHA; include **`Revisit trigger:`** bullets with **≥1** mentioning **INT-13** / **billing portal** / **First Hour** / adoption matrix drift class (083-CONTEXT D-11).

    **If it contains `path (a)`:** Append new **`| v1.17-P1-NNN |`** or **`| v1.17-P2-NNN |`** table row(s) with full FRG-01 columns; **do not renumber**. Under **`## Path evidence`**, add **`83-01-02: appended row id v1.17-P*-***`** with literal id(s).

    **If path (b):** Replace **`## Path evidence`** placeholder with **`83-01-02: certification subsection ### v1.26 INV-04 maintainer pass`**.
  </action>
  <acceptance_criteria>
    <criterion>Shell `sh -c 'if grep -qF "path (b)" .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md; then rg -q "### v1\\.26 INV-04 maintainer pass" .planning/research/v1.17-FRICTION-INVENTORY.md && rg -q "docs-contracts-shift-left" .planning/research/v1.17-FRICTION-INVENTORY.md && rg -qiE "billing portal|INT-13|First Hour" .planning/research/v1.17-FRICTION-INVENTORY.md; elif grep -qF "path (a)" .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md; then test "$(grep -cE "^\\| v1\\.17-P[012]-[0-9]{3} \\|" .planning/research/v1.17-FRICTION-INVENTORY.md)" -ge 6; else exit 1; fi'` exits **0** from repo root.</criterion>
    <criterion>Command `rg -n '^## Path evidence' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="83-01-03">
  <read_first>
    <file>scripts/ci/verify_v1_17_friction_research_contract.sh</file>
    <file>.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md</file>
  </read_first>
  <action>
    **If path (b):** Add under **`## Verifier bundle`** the line **`83-01-03 skipped (path b) — row counts unchanged`**.

    **If path (a):** Update **`verify_v1_17_friction_research_contract.sh`** `row_count`, `p0_count`, `p1_count`, `p2_count` **`[[ "$…" -eq`…`]]`** lines to match post-edit inventory; preserve **`perl`** P0-id guard and FRG-03 **`grep -Fq`** anchors.
  </action>
  <acceptance_criteria>
    <criterion>For path (b): `rg -Fq '83-01-03 skipped (path b)' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>For path (a): `bash scripts/ci/verify_v1_17_friction_research_contract.sh` exits **0** with stdout exactly **`verify_v1_17_friction_research_contract: OK`**.</criterion>
  </acceptance_criteria>
</task>

<task id="83-01-04">
  <read_first>
    <file>.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md</file>
  </read_first>
  <action>
    Under **`## Command transcripts`**, document **exit code 0** runs on the reviewed SHA’s tree. Each command line must start with **`$`** followed by the exact command string:

    1. **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`**
    2. **`bash scripts/ci/verify_package_docs.sh`**
    3. **`bash scripts/ci/verify_adoption_proof_matrix.sh`**
    4. **`bash scripts/ci/verify_verify01_readme_contract.sh`**
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -Fq '## Command transcripts' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_v1_17_friction_research_contract.sh' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_package_docs.sh' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_adoption_proof_matrix.sh' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_verify01_readme_contract.sh' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="83-01-05">
  <read_first>
    <file>.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md</file>
    <file>.github/workflows/ci.yml</file>
  </read_first>
  <action>
    Add **`## CI evidence: docs-contracts-shift-left, VERIFY-01, host-integration`** containing:

    - How **VERIFY-01** maps to **`verify_verify01_readme_contract.sh`** under job **`docs-contracts-shift-left`** in **`.github/workflows/ci.yml`** @ reviewed SHA (link with literal **`github.com`** or path + SHA), and
    - How **`host-integration`** is merge-blocking on the same workflow file / SHA.

    Substrings **`docs-contracts-shift-left`**, **`VERIFY-01`**, and **`host-integration`** must each appear ≥1× in this section.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '^## CI evidence: docs-contracts-shift-left, VERIFY-01, host-integration' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'VERIFY-01' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'docs-contracts-shift-left' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="83-01-06">
  <read_first>
    <file>.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>
    Set **`083-VERIFICATION.md`** frontmatter **`status: complete`**.

    Add **`## INV-04 closure checklist`** with checkboxes matching **`.planning/REQUIREMENTS.md`** **INV-04** **(a)** or **(b)**, verifier bundle including **`docs-contracts-shift-left`**, single reviewed SHA, and link to inventory **`### v1.26 INV-04 maintainer pass`** anchor for path **(b)**.

    Update **`.planning/REQUIREMENTS.md`** traceability table row **`| INV-04 | Phase 83 |`** status column to **`Complete`** with link to **`083-VERIFICATION.md`** (mirror **INT-13** row style).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '^status: complete' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n '^## INV-04 closure checklist' .planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'INV-04' .planning/REQUIREMENTS.md | rg 'Complete'` exits **0**.</criterion>
    <criterion>Command `rg -Fq '083-VERIFICATION.md' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `bash scripts/ci/verify_v1_17_friction_research_contract.sh` from repo root exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

```bash
bash scripts/ci/verify_v1_17_friction_research_contract.sh
bash scripts/ci/verify_package_docs.sh
bash scripts/ci/verify_adoption_proof_matrix.sh
bash scripts/ci/verify_verify01_readme_contract.sh
```

Re-run all **`rg`** acceptance commands from tasks **83-01-01**–**83-01-06**.

## must_haves

- **INV-04** satisfied per **REQUIREMENTS.md** with inventory subsection **or** new rows + honest script counts.
- **`docs-contracts-shift-left`** explicitly named in normative certification bundle (inventory subsection for path **(b)** and **`083-VERIFICATION.md`**).
- **`verify_v1_17_friction_research_contract.sh`** green on final tree.

## success_criteria

- **`083-VERIFICATION.md`** at milestone path holds SHA, path, transcripts, and CI job evidence.
- **`v1.17-FRICTION-INVENTORY.md`** contains **`### v1.26 INV-04 maintainer pass`** **or** additional sourced rows with script co-update.

---

## PLANNING COMPLETE

**Plans:** 1 (`083-01-PLAN.md`) · **Waves:** 1

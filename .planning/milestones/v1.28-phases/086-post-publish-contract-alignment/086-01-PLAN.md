---
phase: 086-post-publish-contract-alignment
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PPX-05, PPX-06, PPX-07, PPX-08]
autonomous: true
files_modified:
  - accrue/mix.exs
  - accrue_admin/mix.exs
  - accrue/guides/first_hour.md
  - accrue/README.md
  - accrue_admin/README.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/verify_adoption_proof_matrix.sh
  - scripts/ci/verify_v1_17_friction_research_contract.sh
  - scripts/ci/verify_verify01_readme_contract.sh
  - scripts/ci/verify_production_readiness_discoverability.sh
  - scripts/ci/verify_core_admin_invoice_verify_ids.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md
tags: [v1.28, PPX-05, PPX-06, PPX-07, PPX-08, verify_package_docs, docs-contracts-shift-left]
---

# Plan 086-01 — Linked publish contract alignment (PPX-05..PPX-08)

<threat_model>
  <summary>ASVS L1; planning artifacts, public docs, and CI bash contracts. No Stripe secrets or customer PII in scope.</summary>
  <threat id="T-86-01">False attestation — <code>086-VERIFICATION.md</code> claims green without the full <code>docs-contracts-shift-left</code> bundle on the recorded merge SHA.</threat>
  <mitigation>Checklist must name all six bash steps from <code>.github/workflows/ci.yml</code> job <code>docs-contracts-shift-left</code>; include <code>Reviewed merge SHA:</code> 40-char hex.</mitigation>
  <threat id="T-86-02">Install literal drift — README / <code>first_hour</code> <code>~&gt;</code> pins disagree with shipped <code>@version</code>.</threat>
  <mitigation>Keep <code>verify_package_docs.sh</code> and <code>package_docs_verifier_test.exs</code> green; grep needles per task <strong>86-01-02</strong>.</mitigation>
  <threat id="T-86-03">Dual SSOT for friction — long narrative in Phase 86 that belongs in Phase 87 <strong>INV-06</strong>.</threat>
  <mitigation>Phase 86 touches inventory only for publish-reopened rows: status + one-line pointer to <code>086-VERIFICATION.md</code>; no dated <strong>INV-06</strong> subsection body (086-CONTEXT D-05).</mitigation>
</threat_model>

## Objective

On the **next linked Hex publish** PR for **`accrue` / `accrue_admin`**, satisfy **PPX-05..PPX-08**: workspace **`@version`** + install-literal honesty (**`verify_package_docs.sh`**, **`package_docs_verifier_test.exs`**), adoption proof matrix (**`verify_adoption_proof_matrix.sh`** + **`adoption-proof-matrix.md`**), full merge-blocking **`docs-contracts-shift-left`** evidence, and **`.planning/`** public-Hex mirrors + minimal friction-inventory hygiene — recorded in **`086-VERIFICATION.md`** and **`.planning/REQUIREMENTS.md`** traceability.

## Tasks

<task id="86-01-01">
  <read_first>
    <file>.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-CONTEXT.md</file>
    <file>.planning/milestones/v1.23-phases/75-post-publish-contract-alignment/75-VERIFICATION.md</file>
    <file>.github/workflows/ci.yml</file>
  </read_first>
  <action>
    Create or replace **`.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md`** with:

    1. Title **`# Phase 86 — Post-publish contract alignment — Verification`** and a line containing **`Milestone:`** and **`v1.28`**.
    2. **`## Preconditions`** — bullets: workspace **`accrue/mix.exs`** and **`accrue_admin/mix.exs`** **`@version`** values (literal strings read from files); **`Reviewed merge SHA:`** + **40-character lowercase hex** for the PR that ships (or same-day follow-up per **086-CONTEXT** D-02 — if follow-up, document **why** in this section).
    3. **`## Evidence checklist`** — numbered items **1–4** mapping **PPX-05**, **PPX-06**, **PPX-07**, **PPX-08** to concrete commands or file paths. **PPX-07** item must list these **exact** command strings as sub-bullets or inline: **`bash scripts/ci/verify_package_docs.sh`**, **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`**, **`bash scripts/ci/verify_verify01_readme_contract.sh`**, **`bash scripts/ci/verify_production_readiness_discoverability.sh`**, **`bash scripts/ci/verify_adoption_proof_matrix.sh`**, **`bash scripts/ci/verify_core_admin_invoice_verify_ids.sh`**.
    4. **`## Sign-off`** — checkbox list for maintainer confirming **PPX-05..08** (no **PROC-08** / **FIN-03**).

    Optional **`## Transcript annex`** per **086-CONTEXT** D-04 when any merge-blocking script’s expected output or job membership changed in this PR.
  </action>
  <acceptance_criteria>
    <criterion>Command `test -f .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n '^## Preconditions' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'Reviewed merge SHA:' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_package_docs.sh' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_core_admin_invoice_verify_ids.sh' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'PPX-05' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'PPX-08' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-02">
  <read_first>
    <file>accrue/mix.exs</file>
    <file>accrue_admin/mix.exs</file>
    <file>accrue/guides/first_hour.md</file>
    <file>accrue/README.md</file>
    <file>accrue_admin/README.md</file>
    <file>scripts/ci/verify_package_docs.sh</file>
  </read_first>
  <action>
    Let **`V`** be **`accrue/mix.exs`** **`@version`** (same as **`accrue_admin/mix.exs`** for linked publish). Ensure integrator-facing pins match **`V`** where **`verify_package_docs.sh`** enforces them (typically **`accrue/guides/first_hour.md`**, **`accrue/README.md`**, **`accrue_admin/README.md`** — follow script output if it lists additional paths).

    Fix any **`[verify_package_docs]`** stderr hints until **`bash scripts/ci/verify_package_docs.sh`** from repository root exits **0** and stdout contains **`package docs verified for accrue`** and **`accrue_admin`** with version **`V`**.
  </action>
  <acceptance_criteria>
    <criterion>Shell `bash scripts/ci/verify_package_docs.sh` from repository root exits **0**.</criterion>
    <criterion>Stdout from that command contains substring **`package docs verified for accrue`**.</criterion>
    <criterion>Stdout contains substring **`accrue_admin`**.</criterion>
    <criterion>With `V=$(sed -n 's/^  @version "\([^"]*\)"/\1/p' accrue/mix.exs | head -1)` and `test -n "$V"`, command `rg -F "{:accrue, \"~> $V\"}" accrue/guides/first_hour.md` exits **0**.</criterion>
    <criterion>With same `V`, command `rg -F "{:accrue_admin, \"~> $V\"}" accrue/guides/first_hour.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-03">
  <read_first>
    <file>accrue/test/accrue/docs/package_docs_verifier_test.exs</file>
    <file>scripts/ci/verify_package_docs.sh</file>
  </read_first>
  <action>
    From **`accrue/`**, run **`mix test test/accrue/docs/package_docs_verifier_test.exs`**. Fix fixtures or production docs per **086-CONTEXT** D-03 ordering (bash script first unless only banner text changed).
  </action>
  <acceptance_criteria>
    <criterion>Shell `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-04">
  <read_first>
    <file>scripts/ci/verify_adoption_proof_matrix.sh</file>
    <file>examples/accrue_host/docs/adoption-proof-matrix.md</file>
  </read_first>
  <action>
    Run **`bash scripts/ci/verify_adoption_proof_matrix.sh`** from repository root. If non-zero, align **`examples/accrue_host/docs/adoption-proof-matrix.md`** with script expectations (same-PR co-update per **PPX-06**).
  </action>
  <acceptance_criteria>
    <criterion>Shell `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-05">
  <read_first>
    <file>.github/workflows/ci.yml</file>
  </read_first>
  <action>
    From repository root, run these six commands in order (same order as job **`docs-contracts-shift-left`**):

    1. `bash scripts/ci/verify_package_docs.sh`
    2. `bash scripts/ci/verify_v1_17_friction_research_contract.sh`
    3. `bash scripts/ci/verify_verify01_readme_contract.sh`
    4. `bash scripts/ci/verify_production_readiness_discoverability.sh`
    5. `bash scripts/ci/verify_adoption_proof_matrix.sh`
    6. `bash scripts/ci/verify_core_admin_invoice_verify_ids.sh`

    If any fail, fix **docs or scripts** until all exit **0**. Append to **`086-VERIFICATION.md`** under **`## Evidence checklist`** item for **PPX-07** (or a **`## Transcript annex`**) the date (**`YYYY-MM-DD`**) and note **all six** exited **0** on the recorded merge SHA.
  </action>
  <acceptance_criteria>
    <criterion>Each of the six bash commands above exits **0** when run sequentially from repo root.</criterion>
    <criterion>Command `rg -Fq 'verify_v1_17_friction_research_contract.sh' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'verify_production_readiness_discoverability.sh' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-06">
  <read_first>
    <file>.planning/PROJECT.md</file>
    <file>.planning/MILESTONES.md</file>
    <file>.planning/STATE.md</file>
    <file>accrue/mix.exs</file>
  </read_first>
  <action>
    Update **`.planning/PROJECT.md`**, **`.planning/MILESTONES.md`**, and **`.planning/STATE.md`** so **last shipped** / **public Hex** callouts for **`accrue`** and **`accrue_admin`** match the **`@version`** in **`accrue/mix.exs`** on this branch (**PPX-08**). Update **`STATE.md`** **Next** pointer to Phase **87** / **`INV-06`** when Phase **86** evidence is complete.

    Do **not** add the **INV-06** dated maintainer pass subsection to **`v1.17-FRICTION-INVENTORY.md`** in this task.
  </action>
  <acceptance_criteria>
    <criterion>With `V=$(sed -n 's/^  @version "\([^"]*\)"/\1/p' accrue/mix.exs | head -1)`, command `rg -F "$V" .planning/PROJECT.md` exits **0**.</criterion>
    <criterion>With same `V`, command `rg -F "$V" .planning/MILESTONES.md` exits **0**.</criterion>
    <criterion>With same `V`, command `rg -F "$V" .planning/STATE.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'Phase 87' .planning/STATE.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-07">
  <read_first>
    <file>.planning/research/v1.17-FRICTION-INVENTORY.md</file>
    <file>.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-CONTEXT.md</file>
  </read_first>
  <action>
    **PPX-08** friction hygiene: For any row explicitly **reopened** or marked **needs revisit** due to this **publish** (search for version bump / registry / PPX-related revisit language), set status to **closed** or **updated** with **one line** pointing to **`.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md`** and **Phase 87** for pending **INV-06** body.

    If **no** rows match, append under **`086-VERIFICATION.md`** **`## PPX-08 friction inventory`** a single bullet: **`No friction rows reopened by this publish — inventory unchanged.`**
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'PPX-08 friction inventory|No friction rows reopened|086-VERIFICATION.md' .planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="86-01-08">
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md</file>
  </read_first>
  <action>
    After all verifier tasks pass, edit **`.planning/REQUIREMENTS.md`**:

    1. Change **`- [ ]` → `- [x]`** for **PPX-05**, **PPX-06**, **PPX-07** under **`### Proof and package contracts (PPX)`** and **PPX-08** under **`### Planning hygiene (PPX)`**.
    2. In **`## Traceability`**, set **Status** to **`Complete`** for rows **PPX-05** through **PPX-08** (Phase **86**).

    Set **`086-VERIFICATION.md`** sign-off checkboxes to **`[x]`** and status line to **Complete** with date if frontmatter exists.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '- [x] **PPX-05**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '- [x] **PPX-08**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| PPX-05 | Phase 86 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| PPX-08 | Phase 86 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '- [ ] **INV-06**' .planning/REQUIREMENTS.md` exits **0** (Phase **87** still pending).</criterion>
  </acceptance_criteria>
</task>

## Verification

- Re-run **`bash scripts/ci/verify_package_docs.sh`** and **`cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs`** after the last substantive edit.
- Confirm **`086-VERIFICATION.md`** records the same **merge SHA** as the green **`docs-contracts-shift-left`** CI run (or paste transcript per D-04).

## must_haves

- **`086-VERIFICATION.md`** with Preconditions, **PPX-05..08** evidence, **Reviewed merge SHA**, and Sign-off.
- **`docs-contracts-shift-left`** six-script bundle green on recorded SHA (**PPX-07**).
- **`.planning/PROJECT.md`**, **`MILESTONES.md`**, **`STATE.md`** reflect shipped versions (**PPX-08**).
- **`.planning/REQUIREMENTS.md`** shows **PPX-05..08** **`[x]`** and traceability **Complete**; **INV-06** remains open for Phase **87**.

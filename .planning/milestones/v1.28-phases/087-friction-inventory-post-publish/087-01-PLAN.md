---
phase: 087-friction-inventory-post-publish
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INV-06]
autonomous: true
files_modified:
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
tags: [v1.28, INV-06, friction-inventory, docs-contracts-shift-left, host-integration]
---

# Plan 087-01 — INV-06 friction inventory post-publish

<threat_model>
  <summary>ASVS L1; planning SSOT and public research markdown. No Stripe secrets or customer PII. Risk is false certification or stale verifier enumeration.</summary>
  <threat id="T-87-01"><code>087-VERIFICATION.md</code> lists bash steps that do not match <code>.github/workflows/ci.yml</code> job <code>docs-contracts-shift-left</code> at the claimed reviewed SHA.</threat>
  <mitigation>Executor reads <code>ci.yml</code> at <code>git show &lt;SHA&gt;.github/workflows/ci.yml</code> (or GitHub blob URL for that SHA) and copies the exact <code>run: bash scripts/ci/...</code> lines into <code>087-VERIFICATION.md</code> <strong>Verifier bundle snapshot @ SHA</strong> section.</mitigation>
  <threat id="T-87-02">Duplicate <strong>INV-06</strong> narrative in Phase <strong>86</strong> artifacts or <strong>086-VERIFICATION.md</strong> instead of inventory + <strong>087-VERIFICATION.md</strong> split (<strong>086-CONTEXT</strong> <strong>D-05</strong>).</threat>
  <mitigation>Inventory subsection only under <code>v1.17-FRICTION-INVENTORY.md</code>; machine evidence only in <code>087-VERIFICATION.md</code>; do not expand <code>086-VERIFICATION.md</code> with INV-06 body.</mitigation>
  <threat id="T-87-03"><strong>host-integration</strong> cited as green without job-level evidence at the reviewed SHA.</threat>
  <mitigation>Include GitHub Actions run URL + job names <code>docs-contracts-shift-left</code> and <code>host-integration</code> for the merge commit, or paste CI log excerpts showing both jobs success on that SHA.</mitigation>
</threat_model>

## Objective

Close **`.planning/REQUIREMENTS.md`** **INV-06** for **v1.28**: append **`### v1.28 INV-06 maintainer pass (<YYYY-MM-DD>)`** to **`.planning/research/v1.17-FRICTION-INVENTORY.md`** after the **v1.27 INV-05** block; author **`.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md`** with **SHA-pinned** verifier bundle matching **`.github/workflows/ci.yml`** and **merge-blocking** **`host-integration`** evidence; flip **INV-06** traceability to **Complete**.

## must_haves

- Inventory contains **`### v1.28 INV-06 maintainer pass (`** with a **real** ISO date and **does not** put Hex **SemVer** inside the **`###`** line.
- **`087-VERIFICATION.md`** contains **`Reviewed commit SHA:`** or **`Reviewed merge SHA:`** plus **40-character** lowercase **git** hex for the tree that was verified.
- **`087-VERIFICATION.md`** enumerates **exactly** the **`bash scripts/ci/*.sh`** **`run:`** commands from job **`docs-contracts-shift-left`** in **`.github/workflows/ci.yml`** at that SHA (six steps today: **`verify_package_docs`**, **`verify_v1_17_friction_research_contract`**, **`verify_verify01_readme_contract`**, **`verify_production_readiness_discoverability`**, **`verify_adoption_proof_matrix`**, **`verify_core_admin_invoice_verify_ids`**).
- **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`** exits **0** on the final branch after inventory edit.
- **`.planning/REQUIREMENTS.md`** shows **`- [x] **INV-06**`** and table row **INV-06** **Complete**.

## Verification criteria (plan level)

- `rg -n '### v1\.28 INV-06 maintainer pass' .planning/research/v1.17-FRICTION-INVENTORY.md` exits **0**.
- `test -f .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.
- `rg -n 'verify_core_admin_invoice_verify_ids' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0** (proves bundle is not stuck on pre-086 five-script list).
- `rg -n 'host-integration' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.
- `rg -F '[x] **INV-06**' .planning/REQUIREMENTS.md` exits **0**.

## Tasks

<task id="87-01-01">
  <read_first>
    <file>.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-CONTEXT.md</file>
    <file>.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-RESEARCH.md</file>
    <file>.github/workflows/ci.yml</file>
    <file>.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md</file>
    <file>.planning/milestones/v1.27-phases/85-friction-inventory-post-closure/085-VERIFICATION.md</file>
  </read_first>
  <action>
    1. Choose **`REVIEW_SHA`**: **40-char** lowercase merge commit on **`main`** that contains the **final** **Phase 87** doc changes (same discipline as **085** / **086** — not a moving **`HEAD`** during PR iteration unless documented as WIP).

    2. Create **`.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md`** with YAML frontmatter keys **`phase: 87`**, **`slug: friction-inventory-post-publish`**, **`requirements: [INV-06]`**, **`status: draft`** (flip to **`complete`** when evidence filled).

    3. Sections (markdown headings):
       - **`# Phase 87 — Friction inventory post-publish — Verification`**
       - **`## INV-06 path`** — state **path (b)** unless **087-CONTEXT** **D-13** **(a)** criteria met.
       - **`## Reviewed commit SHA`** (or **`## Reviewed merge SHA`**) — single line: **`&lt;40-char hex&gt;`**
       - **`## Normative attestation`** — one paragraph: **INV-06** maintainer conclusion lives only under **`### v1.28 INV-06 maintainer pass (...)`** in **`.planning/research/v1.17-FRICTION-INVENTORY.md`**; this file is methodology + evidence (**085** pattern).
       - **`## Verifier bundle snapshot @ SHA`** — numbered list: copy **exact** `bash scripts/ci/...sh` commands from **`docs-contracts-shift-left`** in **`.github/workflows/ci.yml`** **as at `REVIEW_SHA`** (use `git show REVIEW_SHA:.github/workflows/ci.yml`).

    4. Add **`## host-integration`** subsection naming merge-blocking job **`host-integration`** and either (a) GitHub Actions URL for the **`main`** run where **`REVIEW_SHA`** is **`github.sha`**, or (b) explicit statement that **PR** **`docs-contracts-shift-left`** + **`host-integration`** replay matches full **CI** job intent per **087-CONTEXT** **D-06**.

    5. Add **`## Path evidence`** bullet pointing to inventory **`###`** anchor.

    6. Leave **`## Command transcripts`** body empty or **`TBD`** only if **087-CONTEXT** **D-05** allows pointer-only to **086**; otherwise paste **`$ `** transcripts for each shift-left script from repo root on **`REVIEW_SHA`** tree (each exit **0**). If **`release-manifest-ssot`** / **`verify_release_manifest_alignment.sh`** must be cited per **087-CONTEXT** **D-09**, add subsection with CI evidence at same SHA.

    7. Add **`## INV-06 closure checklist`** with **`- [x]`** items mirroring **085** checklist adapted for **INV-06** + six-script bundle + **`host-integration`**.
  </action>
  <acceptance_criteria>
    <criterion>Command `test -f .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'Reviewed (commit|merge) SHA:' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_package_docs.sh' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_core_admin_invoice_verify_ids.sh' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'host-integration' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'INV-06' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="87-01-02">
  <read_first>
    <file>.planning/research/v1.17-FRICTION-INVENTORY.md</file>
    <file>.planning/research/v1.17-north-star.md</file>
    <file>.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-CONTEXT.md</file>
  </read_first>
  <action>
    Append **after** the entire **`### v1.27 INV-05 maintainer pass (2026-04-24)`** subsection (including its **`---`** separator before **`## FRG-03`** if present — insert **before** **`## FRG-03`** so ordering is **INV-05** then **INV-06** then **FRG-03**):

    New heading: **`### v1.28 INV-06 maintainer pass (YYYY-MM-DD)`** with **today’s** date when executing on **`main`**.

    Body (paragraphs + bullets, normative content):
    - **Context:** Post–**PPX-05..08** (**Phase 86**) / **v1.28** registry + planning mirror work; maintainer pass **(b)** per **INV-06**.
    - **Conclusion:** Whether **no new sourced P1/P2** rows were warranted on reviewed tree at **`REVIEW_SHA`** per **FRG-02** **S1** / **S5** (or document **(a)** path with row ids + script co-update if chosen).
    - **Evidence pointer:** One line with full relative path **`.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md`** and **`REVIEW_SHA`**.
    - **`Revisit trigger:`** bullet list including **087-CONTEXT** **D-10** family (next linked Hex publish; adoption matrix / Layer **C** rename discipline; merge-blocking **`host-integration`** / **`docs-contracts-shift-left`** new stall; **INT-13**-class facade / First Hour drift) **plus** exactly **one** **D-11** bullet: **PPX-08**-class **`.planning/`** registry / public-Hex callout mismatch vs shipped versions — fix mirrors per **PPX-08** without requiring a new publish; **do not** enumerate **`PROJECT.md`** / **`MILESTONES.md`** / **`STATE.md`** paths in that bullet.

    Do **not** paste the full **PPX** checklist into the subsection (**D-12**).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '^### v1\.28 INV-06 maintainer pass \([0-9]{4}-[0-9]{2}-[0-9]{2}\)' .planning/research/v1.17-FRICTION-INVENTORY.md` exits **0**.</criterion>
    <criterion>Command `rg -n '### v1\.28 INV-06 maintainer pass' .planning/research/v1.17-FRICTION-INVENTORY.md | wc -l` prints **`1`**.</criterion>
    <criterion>Command `rg -Fq '087-VERIFICATION.md' .planning/research/v1.17-FRICTION-INVENTORY.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'PPX-08' .planning/research/v1.17-FRICTION-INVENTORY.md` exits **0** (the **D-11** revisit bullet under **INV-06** must reference **PPX-08**).</criterion>
    <criterion>Command `python3 -c "import pathlib,re; t=pathlib.Path('.planning/research/v1.17-FRICTION-INVENTORY.md').read_text(); m=re.search(r'### v1\\.27 INV-05 maintainer pass.*?### v1\\.28 INV-06 maintainer pass', t, re.S); raise SystemExit(0 if m else 1)"` exits **0** (**INV-06** subsection appears **after** **INV-05**).</criterion>
  </acceptance_criteria>
</task>

<task id="87-01-03">
  <read_first>
    <file>.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/MILESTONES.md</file>
    <file>.planning/PROJECT.md</file>
  </read_first>
  <action>
    1. If **`087-VERIFICATION.md`** **`## Command transcripts`** is still empty, populate per **087-01-01** step 6 (run all six shift-left scripts from repo root on checkout of **`REVIEW_SHA`**, or paste equivalent **CI** step logs).

    2. Set frontmatter **`status: complete`** on **`087-VERIFICATION.md`** when transcripts + **`host-integration`** evidence are final.

    3. In **`.planning/REQUIREMENTS.md`**: change **`- [ ] **INV-06**`** to **`- [x] **INV-06**`**; set traceability table row **INV-06** from **`Pending`** to **`Complete`** with pointer to **`087-VERIFICATION.md`**.

    4. Update **`.planning/STATE.md`**, **`.planning/MILESTONES.md`**, and **`.planning/PROJECT.md`** only as needed so **v1.28** / **Phase 87** status matches shipped planning truth (Phase **87** complete, milestone closed if **both** **86** and **87** done — follow existing tone in those files).
  </action>
  <acceptance_criteria>
    <criterion>Shell `bash scripts/ci/verify_v1_17_friction_research_contract.sh` from repository root exits **0**.</criterion>
    <criterion>Command `rg -F '[x] **INV-06**' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -F '| INV-06 | Phase 87 | Complete |' .planning/REQUIREMENTS.md` exits **0**.</criterion>
    <criterion>Command `rg -n '^status: complete' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n '## Command transcripts' .planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md` exits **0** and the file contains a non-empty fenced **`text`** block after that heading **or** contains **`086-VERIFICATION.md`** pointer text explaining **D-05** omission per **087-CONTEXT**.</criterion>
  </acceptance_criteria>
</task>

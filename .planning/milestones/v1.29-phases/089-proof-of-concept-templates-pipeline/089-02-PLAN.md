---
phase: 89
plan: 02
plan_id: 089-02
slug: mailglass-template-port
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/emails/receipt.ex
  - accrue/lib/accrue/emails/payment_failed.ex
  - accrue/test/accrue/emails/receipt_test.exs
  - accrue/test/accrue/emails/payment_failed_test.exs
autonomous: true
requirements:
  - MG-06
must_haves:
  truths:
    - "`Accrue.Emails.Receipt` and `Accrue.Emails.PaymentFailed` are Mailglass mailables built from HEEx components, not MJML renderers."
    - "Receipt preserves the current customer-visible copy, content order, and attachment-bearing behavior."
    - "PaymentFailed preserves the current customer-visible copy and CTA behavior and remains attachment-free."
    - "The existing deterministic fixtures still drive the tests; no ad hoc test data is introduced."
    - "Only the two POC templates change in this phase."
  artifacts:
    - path: accrue/lib/accrue/emails/receipt.ex
      provides: "Mailglass-backed receipt mailer with HEEx rendering and compatibility surface."
      contains: "use Mailglass.Mailable"
    - path: accrue/lib/accrue/emails/payment_failed.ex
      provides: "Mailglass-backed payment-failed mailer with HEEx rendering and compatibility surface."
      contains: "use Mailglass.Mailable"
    - path: accrue/test/accrue/emails/receipt_test.exs
      provides: "Parity coverage for the receipt template after the Mailglass port."
      contains: "Receipt.render("
    - path: accrue/test/accrue/emails/payment_failed_test.exs
      provides: "Parity coverage for the payment_failed template after the Mailglass port."
      contains: "PaymentFailed.render("
  key_links:
    - from: accrue/lib/accrue/emails/receipt.ex
      to: ../mailglass/lib/mailglass/mailable.ex
      via: "Mailglass `use` contract and HEEx component rendering"
      pattern: "use Mailglass\\.Mailable"
    - from: accrue/lib/accrue/emails/payment_failed.ex
      to: ../mailglass/lib/mailglass/components.ex
      via: "Native Mailglass HEEx primitives"
      pattern: "Mailglass\\.Components|~H"
    - from: accrue/test/accrue/emails/receipt_test.exs
      to: accrue/lib/accrue/emails/fixtures.ex
      via: "Deterministic fixture baseline for parity assertions"
      pattern: "Accrue\\.Emails\\.Fixtures"
---

<objective>
Port the first two transactional templates to Mailglass-native HEEx mailables while keeping adopter-visible output stable.

Purpose: prove that the new Mailglass rendering stack can reproduce the current receipt and payment_failed emails without changing the customer-facing contract.
Output: two Mailglass-backed mailable modules plus parity tests that still lock the current copy and structure.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.29-phases/089-proof-of-concept-templates-pipeline/089-CONTEXT.md
@.planning/research/v1.29-mailglass-integration.md
@accrue/lib/accrue/emails/receipt.ex
@accrue/lib/accrue/emails/payment_failed.ex
@accrue/lib/accrue/emails/fixtures.ex
@accrue/lib/accrue/invoices/components.ex
@accrue/test/accrue/emails/receipt_test.exs
@accrue/test/accrue/emails/payment_failed_test.exs

<interfaces>
From `Mailglass.Mailable`:
```elixir
use Mailglass.Mailable, stream: :transactional
# injects `new/0`, `render/3`, `deliver/2`, and `deliver_later/2`
```

From `Mailglass.Message`:
```elixir
def new_from_use(mailable, use_opts)
def update_swoosh(msg, fun)
def put_function(msg, fun_name)
```

From the Mailglass renderer stack:
```elixir
Mailglass.Renderer.render/1
Mailglass.Components.container/1
Mailglass.Components.row/1
Mailglass.Components.column/1
```

Current Accrue template surface to preserve:
```elixir
def subject(map()) :: String.t()
def render(map()) :: String.t()
def render_text(map()) :: String.t()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Receipt to a Mailglass mailable</name>
  <files>accrue/lib/accrue/emails/receipt.ex, accrue/test/accrue/emails/receipt_test.exs</files>
  <behavior>
    - Receipt becomes a `Mailglass.Mailable`-backed module.
    - The rendered HTML keeps the current receipt copy, content order, and MSO-friendly structure.
    - The receipt module still exposes the compatibility surface needed by the worker (`subject/1`, HTML render, text render) so Phase 89 can consume it immediately.
  </behavior>
  <action>Rewrite `Accrue.Emails.Receipt` around Mailglass HEEx components per D-06/D-07/D-08 while preserving the adopter-visible output. Update the receipt test to lock the Mailglass-backed render path, the current copy, and the attachment-bearing baseline without asserting byte-for-byte HTML.</action>
  <verify><automated>cd accrue && mix test test/accrue/emails/receipt_test.exs --warnings-as-errors</automated></verify>
  <done>Receipt renders through Mailglass and still reads like the current receipt to adopters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port PaymentFailed to a Mailglass mailable</name>
  <files>accrue/lib/accrue/emails/payment_failed.ex, accrue/test/accrue/emails/payment_failed_test.exs</files>
  <behavior>
    - PaymentFailed becomes a `Mailglass.Mailable`-backed module.
    - The rendered HTML/text preserves the current retry guidance, update-CTA behavior, and content order.
    - The module remains attachment-free and keeps the compatibility surface for the worker and preview paths.
  </behavior>
  <action>Rewrite `Accrue.Emails.PaymentFailed` around Mailglass HEEx components per D-06/D-07/D-08, keeping the current copy and CTA semantics intact. Update the payment_failed test to assert Mailglass-backed rendering and the unchanged attachment-free contract.</action>
  <verify><automated>cd accrue && mix test test/accrue/emails/receipt_test.exs test/accrue/emails/payment_failed_test.exs --warnings-as-errors</automated></verify>
  <done>PaymentFailed renders through Mailglass, preserves the current customer-facing message, and stays attachment-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data → HEEx renderer | Deterministic fixture assigns cross into Mailglass rendering and must not become executable HTML. |
| template output → recipient client | Rendered HTML/text crosses into an email client and must remain sanitized and structurally valid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03 | T | Receipt / PaymentFailed HEEx templates | mitigate | Use Mailglass HEEx primitives and Phoenix component escaping; do not inject raw HTML from assigns into the template bodies. |
| T-89-04 | I | Template tests / fixtures | accept | Deterministic fixture data is already non-sensitive and exists to prove parity; no additional secrets or live customer data are introduced. |
</threat_model>

<verification>
Receipt and payment_failed tests pass against Mailglass-backed templates, and the modules still preserve the current adopter-visible copy and structure.
</verification>

<success_criteria>
Both proof-of-concept emails render via Mailglass HEEx components with the current copy, order, and CTA semantics preserved; only the two POC templates are changed in this phase.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.29-phases/089-proof-of-concept-templates-pipeline/089-02-SUMMARY.md`
</output>

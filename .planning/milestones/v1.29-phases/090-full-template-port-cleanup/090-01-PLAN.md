---
phase: 90-full-template-port-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/emails/trial_ending.ex
  - accrue/test/accrue/emails/trial_ending_test.exs
  - accrue/lib/accrue/emails/trial_ended.ex
  - accrue/test/accrue/emails/trial_ended_test.exs
  - accrue/lib/accrue/emails/subscription_canceled.ex
  - accrue/test/accrue/emails/subscription_canceled_test.exs
  - accrue/lib/accrue/emails/subscription_paused.ex
  - accrue/test/accrue/emails/subscription_paused_test.exs
  - accrue/lib/accrue/emails/subscription_resumed.ex
  - accrue/test/accrue/emails/subscription_resumed_test.exs
  - accrue/lib/accrue/emails/card_expiring_soon.ex
  - accrue/test/accrue/emails/card_expiring_soon_test.exs
autonomous: true
requirements: [MG-07]
must_haves:
  truths:
    - "The first six remaining transactional email templates render through Mailglass HEEx modules."
    - "Their parity tests still pass by asserting adopter-visible content and structure, not byte-for-byte HTML."
    - "Deterministic fixtures continue to feed the template modules without introducing hidden state."
  artifacts:
    - path: "accrue/lib/accrue/emails/trial_ending.ex"
      provides: "Mailglass-backed trial-ending mailable"
    - path: "accrue/lib/accrue/emails/subscription_paused.ex"
      provides: "Mailglass-backed subscription-paused mailable"
    - path: "accrue/test/accrue/emails/trial_ending_test.exs"
      provides: "Parity coverage for the trial-ending port"
  key_links:
    - from: "accrue/test/accrue/emails/*_test.exs"
      to: "accrue/lib/accrue/emails/*"
      via: "resolve_template(type) -> module.subject/render/render_text"
      pattern: "resolve_template\\(type\\)"
    - from: "accrue/lib/accrue/emails/*"
      to: "Mailglass.Message"
      via: "use Mailglass.Mailable + message/1"
      pattern: "use Mailglass\\.Mailable"
---

<objective>
Port the first six remaining MJML-backed email modules to Mailglass HEEx while preserving current content, subject, and render-structure contracts.

Purpose: Remove half of the legacy MJML surface without regressing the adopter-visible mail output.
Output: Six Mailglass-backed email modules plus matching parity tests.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-CONTEXT.md
@.planning/milestones/v1.29-phases/089-proof-of-concept-templates-pipeline/089-CONTEXT.md
@accrue/lib/accrue/emails/receipt.ex
@accrue/lib/accrue/emails/payment_failed.ex
@accrue/lib/accrue/emails/fixtures.ex
@accrue/test/accrue/emails/trial_ending_test.exs
@accrue/test/accrue/emails/trial_ended_test.exs
@accrue/test/accrue/emails/subscription_canceled_test.exs
@accrue/test/accrue/emails/subscription_paused_test.exs
@accrue/test/accrue/emails/subscription_resumed_test.exs
@accrue/test/accrue/emails/card_expiring_soon_test.exs

<interfaces>
From `accrue/lib/accrue/emails/receipt.ex` and `payment_failed.ex`:
```elixir
use Mailglass.Mailable, stream: :transactional
@spec message(map()) :: Mailglass.Message.t()
@spec subject(map()) :: String.t()
@spec render(map()) :: String.t()
@spec render_text(map()) :: String.t()
```

From `accrue/lib/accrue/emails/fixtures.ex`:
```elixir
@spec all() :: %{atom() => map()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port trial and cancellation templates</name>
  <files>accrue/lib/accrue/emails/trial_ending.ex, accrue/test/accrue/emails/trial_ending_test.exs, accrue/lib/accrue/emails/trial_ended.ex, accrue/test/accrue/emails/trial_ended_test.exs, accrue/lib/accrue/emails/subscription_canceled.ex, accrue/test/accrue/emails/subscription_canceled_test.exs</files>
  <behavior>
    - `trial_ending`, `trial_ended`, and `subscription_canceled` become Mailglass-backed mailables with `message/1`, `subject/1`, `render/1`, and `render_text/1`.
    - Their tests keep asserting the current adopter-visible copy, fallback subject behavior, and non-empty HTML/text outputs.
    - Mailglass render output is checked structurally, not by exact HTML bytes.
  </behavior>
  <action>Convert the three modules from MJML renderers to Mailglass mailables using the same HEEx pattern proven by `Receipt` and `PaymentFailed`. Update the matching tests so they still prove the same copy, CTA, and structure contracts per D-07/D-08, without introducing any new preview-specific helpers.</action>
  <verify>
    <automated>mix test test/accrue/emails/trial_ending_test.exs test/accrue/emails/trial_ended_test.exs test/accrue/emails/subscription_canceled_test.exs</automated>
  </verify>
  <done>All three modules render through Mailglass and their parity tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port pause, resume, and card-expiry templates</name>
  <files>accrue/lib/accrue/emails/subscription_paused.ex, accrue/test/accrue/emails/subscription_paused_test.exs, accrue/lib/accrue/emails/subscription_resumed.ex, accrue/test/accrue/emails/subscription_resumed_test.exs, accrue/lib/accrue/emails/card_expiring_soon.ex, accrue/test/accrue/emails/card_expiring_soon_test.exs</files>
  <behavior>
    - `subscription_paused`, `subscription_resumed`, and `card_expiring_soon` become Mailglass-backed mailables with the same visible semantics as the current MJML versions.
    - Their tests prove the Mailglass message shape and the preserved customer-facing output without byte-for-byte HTML equality.
    - The templates remain fixture-driven and do not acquire new side effects or helper modules.
  </behavior>
  <action>Port the remaining non-invoice templates in this first batch to Mailglass HEEx, reusing the same component/layout pattern as the already-ported mailers. Keep the existing fixture-driven assertions honest so these modules still prove the same visible content and structure per D-07/D-08.</action>
  <verify>
    <automated>mix test test/accrue/emails/subscription_paused_test.exs test/accrue/emails/subscription_resumed_test.exs test/accrue/emails/card_expiring_soon_test.exs</automated>
  </verify>
  <done>All three remaining non-invoice templates in this batch render through Mailglass and their tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture assigns → HEEx render | caller-controlled strings and maps cross into the email renderer |
| render output → committed tests/docs | generated mail HTML/text and snapshots must not leak secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | Mailglass template modules | mitigate | Keep `Mailglass.Mailable` rendering and assert content/structure in tests so unescaped or malformed assigns are caught before merge. |
| T-90-02 | Information disclosure | Mail fixture data | mitigate | Continue using deterministic fake fixtures only; do not introduce real customer, billing, or secret material into the ported templates. |
| T-90-03 | Denial of service | Template render path | accept | The batch only changes local render logic; no new external calls or unbounded loops are introduced. |
</threat_model>

<verification>
Run the two targeted ExUnit batches, then run the broader email test file group if either batch changes shared helpers or fixtures.
</verification>

<success_criteria>
The six templates in this batch are Mailglass-backed, the existing parity tests stay green, and no new preview or dependency surface is introduced.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-01-SUMMARY.md`
</output>

---
phase: 90-full-template-port-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/emails/invoice_finalized.ex
  - accrue/test/accrue/emails/invoice_finalized_test.exs
  - accrue/lib/accrue/emails/invoice_paid.ex
  - accrue/test/accrue/emails/invoice_paid_test.exs
  - accrue/lib/accrue/emails/invoice_payment_failed.ex
  - accrue/test/accrue/emails/invoice_payment_failed_test.exs
  - accrue/lib/accrue/emails/refund_issued.ex
  - accrue/test/accrue/emails/refund_issued_test.exs
  - accrue/lib/accrue/emails/coupon_applied.ex
  - accrue/test/accrue/emails/coupon_applied_test.exs
autonomous: true
requirements: [MG-07]
must_haves:
  truths:
    - "The invoice, refund, and coupon transactional templates render through Mailglass HEEx modules."
    - "Their tests still verify adopter-visible subject/body behavior rather than exact MJML output."
    - "The invoice-bearing templates continue to render without introducing new fixture or adapter dependencies."
  artifacts:
    - path: "accrue/lib/accrue/emails/invoice_paid.ex"
      provides: "Mailglass-backed invoice-paid mailable"
    - path: "accrue/lib/accrue/emails/refund_issued.ex"
      provides: "Mailglass-backed refund-issued mailable"
    - path: "accrue/test/accrue/emails/invoice_payment_failed_test.exs"
      provides: "Parity coverage for the invoice-payment-failed port"
  key_links:
    - from: "accrue/test/accrue/emails/*_test.exs"
      to: "accrue/lib/accrue/emails/*"
      via: "resolve_template(type) -> module.subject/render/render_text"
      pattern: "resolve_template\(type\)"
    - from: "accrue/lib/accrue/emails/*"
      to: "Mailglass.Message"
      via: "use Mailglass.Mailable + message/1"
      pattern: "use Mailglass\.Mailable"
---

<objective>
Port the remaining invoice/refund/coupon MJML templates to Mailglass HEEx while keeping the invoice-heavy parity checks green.

Purpose: Finish the content migration for the billing mail surface before the cleanup phase removes legacy MJML support.
Output: Five more Mailglass-backed email modules plus matching parity tests.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-CONTEXT.md
@.planning/milestones/v1.29-phases/089-proof-of-concept-templates-pipeline/089-CONTEXT.md
@accrue/lib/accrue/emails/receipt.ex
@accrue/lib/accrue/emails/payment_failed.ex
@accrue/lib/accrue/emails/fixtures.ex
@accrue/test/accrue/emails/invoice_finalized_test.exs
@accrue/test/accrue/emails/invoice_paid_test.exs
@accrue/test/accrue/emails/invoice_payment_failed_test.exs
@accrue/test/accrue/emails/refund_issued_test.exs
@accrue/test/accrue/emails/coupon_applied_test.exs

<interfaces>
From `accrue/lib/accrue/emails/receipt.ex` and `payment_failed.ex`:
```elixir
use Mailglass.Mailable, stream: :transactional
@spec message(map()) :: Mailglass.Message.t()
@spec subject(map()) :: String.t()
@spec render(map()) :: String.t()
@spec render_text(map()) :: String.t()
```

From `accrue/lib/accrue/emails/fixtures.ex`:
```elixir
@spec all() :: %{atom() => map()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port invoice templates</name>
  <files>accrue/lib/accrue/emails/invoice_finalized.ex, accrue/test/accrue/emails/invoice_finalized_test.exs, accrue/lib/accrue/emails/invoice_paid.ex, accrue/test/accrue/emails/invoice_paid_test.exs, accrue/lib/accrue/emails/invoice_payment_failed.ex, accrue/test/accrue/emails/invoice_payment_failed_test.exs</files>
  <behavior>
    - `invoice_finalized`, `invoice_paid`, and `invoice_payment_failed` become Mailglass-backed mailables with the same visible billing copy and render structure as the MJML versions.
    - Their tests continue to prove the invoice-bearing subject/body behavior and the non-empty HTML/text outputs.
    - The invoice-bearing templates remain fixture-driven and do not gain any new runtime dependencies.
  </behavior>
  <action>Convert the three invoice templates to the same Mailglass HEEx pattern used by the already-ported receipt mailers. Preserve the adopter-visible content, ordering, and body structure per D-07/D-08 so the invoice-focused tests stay meaningful.</action>
  <verify>
    <automated>mix test test/accrue/emails/invoice_finalized_test.exs test/accrue/emails/invoice_paid_test.exs test/accrue/emails/invoice_payment_failed_test.exs</automated>
  </verify>
  <done>All three invoice mailers render through Mailglass and their tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port refund and coupon templates</name>
  <files>accrue/lib/accrue/emails/refund_issued.ex, accrue/test/accrue/emails/refund_issued_test.exs, accrue/lib/accrue/emails/coupon_applied.ex, accrue/test/accrue/emails/coupon_applied_test.exs</files>
  <behavior>
    - `refund_issued` and `coupon_applied` become Mailglass-backed mailables with the same adopter-visible semantics as the MJML versions.
    - Their tests keep checking content, structure, and non-empty text/HTML outputs instead of raw HTML equality.
    - The templates keep using the deterministic fixture data already present in `Accrue.Emails.Fixtures`.
  </behavior>
  <action>Port the remaining invoice-adjacent templates in this phase to Mailglass HEEx, keeping the current copy and body structure intact while switching only the renderer contract. Preserve the fixture-driven behavior so the broad coverage tests stay honest.</action>
  <verify>
    <automated>mix test test/accrue/emails/refund_issued_test.exs test/accrue/emails/coupon_applied_test.exs</automated>
  </verify>
  <done>Both non-invoice mailers in this batch render through Mailglass and their tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invoice/refund/coupon fixtures → HEEx render | caller-controlled data crosses into the mail renderer |
| render output → fixture tests | content assertions must keep real invoice/refund semantics intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04 | Tampering | Mailglass render path | mitigate | Preserve the existing subject/body assertions so malformed template output fails in ExUnit before merge. |
| T-90-05 | Information disclosure | Fixture-driven test data | mitigate | Keep deterministic fake invoice, refund, and coupon data only; do not introduce any live identifiers or secrets. |
| T-90-06 | Denial of service | Template compilation | accept | This batch stays inside the existing local compile/test flow and does not add new unbounded work or external calls. |
</threat_model>

<verification>
Run the invoice-focused ExUnit group, then the refund/coupon tests, and rerun the invoice multipart sweep if the shared fixture helper changes.
</verification>

<success_criteria>
The five templates in this batch are Mailglass-backed, their existing parity tests pass, and the invoice/refund/coupon mail surface is ready for cleanup.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-02-SUMMARY.md`
</output>

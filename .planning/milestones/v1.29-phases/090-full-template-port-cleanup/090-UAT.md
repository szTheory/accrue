---
status: complete
phase: 090-full-template-port-cleanup
source:
  - .planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-01-PLAN.md
  - .planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-02-PLAN.md
  - .planning/milestones/v1.29-phases/090-full-template-port-cleanup/090-03-PLAN.md
started: 2026-04-26T00:00:00Z
updated: 2026-04-26T00:00:00Z
mode: shift-left
human_steps_required: 0
---

## Current Test

[testing complete — 7/7 automated, 0 manual]

## Tests

### 1. Email Test Suite — Parity Sweep
expected: All parity tests for the 12 ported templates plus the broad fixture/multipart sweep pass with 0 failures.
result: pass
automated_by: accrue/test/accrue/emails/*_test.exs (runs in `release-gate` CI matrix, `.github/workflows/ci.yml:67-271`, `--warnings-as-errors`)

### 2. Compile Clean
expected: `mix compile --warnings-as-errors` from `accrue/` completes with no warnings — no orphan refs to `mjml_eex`, `phoenix_swoosh`, `Accrue.Emails.HtmlBridge`, or `template_for/1`.
result: pass
automated_by: `release-gate` CI matrix per-cell compile + test runs, `.github/workflows/ci.yml:67-271`

### 3. Dependency Purge
expected: `accrue/mix.exs` no longer declares `:mjml_eex` or `:phoenix_swoosh`. Only `:mailglass` remains.
result: pass
automated_by: accrue/test/accrue/emails/mailglass_cleanup_test.exs:8-13

### 4. Legacy Asset Removal
expected: `accrue/priv/accrue/templates/emails/` contains no `.mjml.eex` or `.text.eex` files. `accrue.mail.preview` task and `html_bridge.ex` deleted. `template_for/1` removed from `Accrue.Workers.Mailer`.
result: pass
automated_by: accrue/test/accrue/emails/mailglass_cleanup_test.exs:4-6 (template_for/1), :15-21 (specific files), :23-28 (full priv/ wildcard sweep)

### 5. Email Guide Retires the CLI
expected: `accrue/guides/email.md` contains no reference to `mix accrue.mail.preview` and documents `/dev/email-preview` as the supported preview surface.
result: pass
automated_by: accrue/test/accrue/emails/mailglass_cleanup_test.exs:30-34

### 6. Broad Fixture Sweep Includes payment_succeeded
expected: `Accrue.Emails.Fixtures.all/0` returns a 14-key map including `:payment_succeeded`, and the fixture sweep test renders each through Mailglass without raising.
result: pass
automated_by: accrue/test/accrue/emails/fixtures_test.exs (14-key map check + `resolve_template`/`subject`/`render`/`render_text` per-type sweep), accrue/test/accrue/emails/multipart_coverage_test.exs (HTML structure + D6-07 invariant per type)

### 7. Admin /dev/email-preview Renders All Templates
expected: Visiting `/dev/email-preview` lists every mail type from `Fixtures.all/0` and renders each through its Mailglass-backed template without errors.
result: pass
automated_by: accrue_admin/test/accrue_admin/dev/email_preview_live_test.exs (mount + 2-fixture smoke; full 14-fixture sweep iterates every key, asserts dropdown options + per-fixture subject/preview after `render_change`)

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0

## Automation Map

Every UAT truth points at a green automated check. No human verification required.

| UAT # | Truth | Automated by | CI gate |
| --- | --- | --- | --- |
| 1 | Parity tests for 12 ported templates pass | `accrue/test/accrue/emails/*_test.exs` | `release-gate` (3-cell matrix) |
| 2 | `mix compile --warnings-as-errors` clean | per-cell compile gate | `release-gate` |
| 3 | `mjml_eex` / `phoenix_swoosh` purged from mix.exs | `mailglass_cleanup_test.exs:8-13` | `release-gate` |
| 4 | Legacy MJML/text assets, CLI task, HtmlBridge, `template_for/1` all gone | `mailglass_cleanup_test.exs:4-6,15-21,23-28` | `release-gate` |
| 5 | Email guide retires CLI prose, points at `/dev/email-preview` | `mailglass_cleanup_test.exs:30-34` | `release-gate` |
| 6 | `Fixtures.all/0` covers all 14 types, each renders via Mailglass | `fixtures_test.exs`, `multipart_coverage_test.exs` | `release-gate` |
| 7 | Admin LiveView renders every fixture without errors | `email_preview_live_test.exs` (full sweep) | `release-gate` |

## Gaps

[none — all 7 truths automated, no `automation_deferred:` entries]

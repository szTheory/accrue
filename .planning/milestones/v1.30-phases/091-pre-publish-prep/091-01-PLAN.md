---
phase: 091-pre-publish-prep
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REL-06, REL-07]
autonomous: true
files_modified:
  - accrue/CHANGELOG.md
  - accrue_admin/CHANGELOG.md
  - RELEASING.md
  - accrue/guides/maturity-and-maintenance.md
  - accrue/guides/upgrade.md
must_haves:
  truths:
    - "The repo preloads the 1.0.0 stability commitment without falsely claiming Hex already ships 1.0.0."
    - "The release runbook describes post-1.0 cadence, semver rules, and deprecation policy before Phase 92 bumps @version."
  artifacts:
    - path: "accrue/CHANGELOG.md"
      provides: "REL-06 preamble under Unreleased for Release Please to render later"
    - path: "accrue_admin/CHANGELOG.md"
      provides: "REL-06 lockstep admin preamble under Unreleased"
    - path: "RELEASING.md"
      provides: "REL-07 post-1.0 cadence section and routine linked releases heading"
    - path: "accrue/guides/maturity-and-maintenance.md"
      provides: "authoritative supported surface and non-boundary wording"
    - path: "accrue/guides/upgrade.md"
      provides: "post-1.0 cadence wording and renamed internal anchor"
  key_links:
    - from: "accrue/CHANGELOG.md"
      to: "RELEASING.md"
      via: "REL-06 preamble references cadence runbook"
      pattern: "post-1.0 cadence"
    - from: "accrue/guides/upgrade.md"
      to: "RELEASING.md"
      via: "renamed anchor"
      pattern: "#post-1-0-cadence-maintainer-intent"
---

<objective>
Preload the stable-release narrative for both packages and replace the pre-1.0 release runbook language before Phase 92 performs the mechanical `@version` bump.

Purpose: Satisfy REL-06 and REL-07 while keeping `main` honest at `0.3.1` per D-01, D-02, and D-06.
Output: Updated package changelogs, `RELEASING.md`, and the two linked maintainer guides.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-CONTEXT.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-RESEARCH.md
@.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-01-PLAN.md
@.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the locked REL-06 changelog preambles</name>
  <files>accrue/CHANGELOG.md, accrue_admin/CHANGELOG.md</files>
  <action>Implement D-04, D-05, and D-06 exactly. Insert the locked `**1.0.0 — Stable.**` preamble as the first paragraph directly under `## Unreleased` in each package changelog, preserving all existing category sections below it. Do not create a literal numbered `## 1.0.0` heading on `main`, do not move existing entries out of `Unreleased`, and do not touch any version literals or compare links.</action>
  <verify>
    <automated>rg -Fq '**1.0.0 — Stable.** This release commits Accrue to v1.x API stability' accrue/CHANGELOG.md && rg -Fq '**1.0.0 — Stable.** Released in lockstep with `accrue` 1.0.0.' accrue_admin/CHANGELOG.md && ! rg -n '^## 1\\.0\\.0' accrue/CHANGELOG.md accrue_admin/CHANGELOG.md</automated>
  </verify>
  <done>Both changelogs carry the locked stability preamble under `## Unreleased`, and neither file contains a hand-authored numbered `1.0.0` heading.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the pre-1.0 release runbook with the post-1.0 cadence contract</name>
  <files>RELEASING.md, accrue/guides/maturity-and-maintenance.md, accrue/guides/upgrade.md</files>
  <action>Implement D-07 through D-14. In `RELEASING.md`, replace `## Pre-1.0 closure (maintainer intent)` in place with `## Post-1.0 cadence (maintainer intent)`, rename `## Routine pre-1.0 linked releases (Release Please + Hex)` to `## Routine linked releases (Release Please + Hex)`, keep the top-of-file intro neutral per D-09, and leave the `## Appendix: Same-day 1.0.0 bootstrap (exceptional)` section untouched per D-10. Write the D-11 structure into the new cadence section, including semver discipline, two-step deprecation cycle, cadence, lockstep, supported-surface pointer, verification expectation, RC policy, and the 6-month forward-port rule. In `accrue/guides/maturity-and-maintenance.md` and `accrue/guides/upgrade.md`, update wording and anchors so they reference the post-1.0 cadence contract and the authoritative supported-surface list without reintroducing pre-1.0 framing.</action>
  <verify>
    <automated>rg -Fq '## Post-1.0 cadence (maintainer intent)' RELEASING.md && rg -Fq '## Routine linked releases (Release Please + Hex)' RELEASING.md && rg -Fq 'critical security fixes are forward-ported to the latest minor of the previous major for 6 months' RELEASING.md && rg -Fq '1.0.x minor' accrue/guides/maturity-and-maintenance.md && rg -Fq '#post-1-0-cadence-maintainer-intent' accrue/guides/upgrade.md</automated>
  </verify>
  <done>`RELEASING.md` is the single source of truth for post-1.0 cadence, the appendix remains intact for Phase 92, and both guides point at the renamed cadence contract and supported-surface doctrine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> maintainer actions | Release policy prose can mislead future release decisions if it overstates current registry truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-01 | T | package changelogs | mitigate | Use commitment-level wording per D-02 and forbid a numbered `1.0.0` heading on `main` per D-06 |
| T-091-02 | T | RELEASING.md | mitigate | Replace the old section in place per D-07 so no stale parallel runbook survives |
</threat_model>

<verification>
Run the grep checks in each task and confirm no `mix.exs` version file was touched by this plan.
</verification>

<success_criteria>
REL-06 and REL-07 are fully planned with bounded files, grep-verifiable outputs, and no hidden Phase 92 work such as a version bump or numbered changelog release section.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.30-phases/091-pre-publish-prep/091-01-SUMMARY.md`.
</output>

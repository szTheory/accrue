---
phase: 091-pre-publish-prep
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [DOC-03]
autonomous: true
files_modified:
  - README.md
  - accrue/README.md
must_haves:
  truths:
    - "A reader of the root and package READMEs sees a 1.0.x stability commitment and post-1.0 cadence framing without any false claim that Hex already ships 1.0.0."
    - "The root README continues to preserve the explicit PROC-08 and FIN-03 non-goal warning."
  artifacts:
    - path: "README.md"
      provides: "root maintenance posture flip"
    - path: "accrue/README.md"
      provides: "package stability flip with install literals preserved"
  key_links:
    - from: "README.md"
      to: "accrue/guides/maturity-and-maintenance.md"
      via: "maintenance posture cross-link"
      pattern: "maturity-and-maintenance"
    - from: "accrue/README.md"
      to: "RELEASING.md"
      via: "cadence pointer"
      pattern: "Post-1.0 cadence"
---

<objective>
Flip the public front-door docs from pre-1.0 closure language to the 1.0.x stability commitment while preserving current install pins and non-goal boundaries.

Purpose: Satisfy DOC-03 without leaking Phase 92 version-bump work into Phase 91.
Output: Updated root and package READMEs.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-CONTEXT.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-RESEARCH.md
@.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the root README maintenance posture without weakening the non-goal warning</name>
  <files>README.md</files>
  <action>Implement D-02, D-12, and D-15 in `README.md`. Replace the `0.x` and pre-1.0 closure framing with commitment-level `1.0.x` maintenance posture language, keep the intake-gated doctrine where it is still intentionally part of the message, preserve the cross-link to `accrue/guides/maturity-and-maintenance.md`, and preserve the explicit root warning that `PROC-08` and `FIN-03` remain later-milestone-only non-goals.</action>
  <verify>
    <automated>rg -Fq 'The **`1.0.x`** line treats the public facade, Fake-backed proofs, and merge-blocking CI contracts as the stability boundary' README.md && rg -Fq 'PROC-08' README.md && rg -Fq 'FIN-03' README.md && rg -Fq 'later milestone' README.md</automated>
  </verify>
  <done>The root README communicates the post-1.0 maintenance posture and still explicitly warns that PROC-08 and FIN-03 remain out of scope until a later milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the package README stability section while preserving the current install pin</name>
  <files>accrue/README.md</files>
  <action>Implement D-12 and D-13 in `accrue/README.md`. Rewrite the maturity and stability language to the `1.0.x` commitment framing, point readers at `RELEASING.md`'s `Post-1.0 cadence (maintainer intent)` section and `accrue/guides/maturity-and-maintenance.md`, and explicitly keep the install snippet `{:accrue, "~> 0.3.1"}` untouched because Phase 92 owns the version bump and `verify_package_docs.sh` extracts that literal from `mix.exs`.</action>
  <verify>
    <automated>rg -Fq 'done enough for the **`1.0.x`** line' accrue/README.md && rg -Fq 'breaking changes on the documented surface go through deprecation, not silent reshuffles' accrue/README.md && rg -Fq '{:accrue, "~> 0.3.1"}' accrue/README.md</automated>
  </verify>
  <done>The package README advertises the 1.0.x stability commitment, points at the renamed cadence contract, and still contains the unchanged `~> 0.3.1` install snippet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README prose -> evaluator expectations | Front-door docs can imply unsupported scope or wrong current version state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-03 | T | README.md | mitigate | Preserve the explicit PROC-08 / FIN-03 non-goal warning and later-milestone-only wording |
| T-091-04 | T | accrue/README.md | mitigate | Preserve `{:accrue, "~> 0.3.1"}` exactly so `verify_package_docs.sh` remains green at the reviewed SHA |
</threat_model>

<verification>
Run the task-level greps and re-open `scripts/ci/verify_package_docs.sh` output if any install-literal drift appears.
</verification>

<success_criteria>
DOC-03 is isolated to the two public READMEs, with no accidental version-pin drift and no weakening of the explicit non-goal boundaries.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.30-phases/091-pre-publish-prep/091-02-SUMMARY.md`.
</output>

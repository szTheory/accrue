---
phase: 091-pre-publish-prep
plan: 03
type: execute
wave: 2
depends_on: [091-01, 091-02]
requirements: [DOC-04, REL-06, REL-07, DOC-03]
autonomous: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VALIDATION.md
  - .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md
must_haves:
  truths:
    - "PROJECT.md keeps PROC-08 and FIN-03 as explicit non-goals under a dated 1.0.0 reaffirmation anchor."
    - "Phase 91 has a concrete validation contract and a verification artifact skeleton before execution."
    - "The reviewed-SHA evidence contract explicitly requires both docs-contracts-shift-left and host-integration to stay green at 0.3.1."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "dated DOC-04 non-goals reaffirmation"
    - path: ".planning/milestones/v1.30-phases/091-pre-publish-prep/091-VALIDATION.md"
      provides: "Nyquist validation and evidence contract"
    - path: ".planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md"
      provides: "execution-time verification skeleton with reviewed SHA and CI evidence placeholders"
    - path: ".planning/REQUIREMENTS.md"
      provides: "closeout instructions for REL-06 REL-07 DOC-03 DOC-04"
  key_links:
    - from: ".planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md"
      to: ".github/workflows/ci.yml"
      via: "reviewed-SHA CI evidence requirements"
      pattern: "host-integration"
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "DOC-04 traceability"
      pattern: "DOC-04"
---

<objective>
Close out the planning-side non-goal reaffirmation and define the exact reviewed-SHA validation and verification evidence that execution must produce.

Purpose: Satisfy DOC-04 and resolve Nyquist, verification derivation, and host-integration evidence gaps before Phase 91 execution.
Output: Updated `PROJECT.md`, a new `091-VALIDATION.md`, and executor-facing `091-VERIFICATION.md` and closeout instructions.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-CONTEXT.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-RESEARCH.md
@.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dated DOC-04 reaffirmation anchor to PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>Implement D-16. Under the existing non-goals section, add exactly `### Reaffirmed at 1.0.0 (2026-04-26)` and write the two required bullets that keep `PROC-08` and `FIN-03` explicitly out of scope. The wording must preserve the hard boundary that calling stable does not lift those non-goals and that any revisit is later-milestone-only.</action>
  <verify>
    <automated>rg -Fq '### Reaffirmed at 1.0.0 (2026-04-26)' .planning/PROJECT.md && rg -Fq 'PROC-08' .planning/PROJECT.md && rg -Fq 'FIN-03' .planning/PROJECT.md && rg -Fq 'later milestone' .planning/PROJECT.md</automated>
  </verify>
  <done>`PROJECT.md` contains a dated DOC-04 anchor that reaffirms both non-goals with explicit later-milestone-only boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Create the validation and verification contracts with explicit reviewed-SHA host-integration evidence</name>
  <files>.planning/milestones/v1.30-phases/091-pre-publish-prep/091-VALIDATION.md, .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md</files>
  <action>Create `091-VALIDATION.md` and `091-VERIFICATION.md` as execution artifacts templates. `091-VALIDATION.md` must map REL-06, REL-07, DOC-03, and DOC-04 to concrete evidence, enumerate the greps that prove the preserved root README non-goal warning and the `PROC-08` / `FIN-03` later-milestone-only boundaries, and require reviewed-SHA proof that both `docs-contracts-shift-left` and `host-integration` stayed green at `@version 0.3.1` per D-03. `091-VERIFICATION.md` must follow the Phase 86 spine: title, milestone line, `## Preconditions`, `## Evidence checklist`, `## Verifier transcripts`, and `## Sign-off`; its preconditions must record both package `@version "0.3.1"` values and a `Reviewed merge SHA:` placeholder, and its verifier section must explicitly require either CI links or transcript pointers for the six-script docs bundle plus `host-integration` on the same reviewed SHA. Do not mention any version bump, install-pin changes, or a numbered `1.0.0` changelog heading.</action>
  <verify>
    <automated>test -f .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VALIDATION.md && test -f .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md && rg -Fq 'host-integration' .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VALIDATION.md && rg -Fq 'Reviewed merge SHA:' .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md && rg -Fq '@version \"0.3.1\"' .planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md</automated>
  </verify>
  <done>The phase has a concrete Nyquist validation contract and a verification skeleton that explicitly carries the reviewed-SHA `host-integration` and docs-bundle evidence requirement.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 91 closeout instructions into requirements tracking</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Keep the live requirements status unchanged for now, but make sure the Phase 91 planning artifacts clearly drive closeout. If Phase 91 roadmap text still conflicts with D-06 by implying a numbered `1.0.0` heading lands on `main`, correct that wording and update the Phase 91 plan count to three plans. In `REQUIREMENTS.md`, leave REL-06, REL-07, DOC-03, and DOC-04 pending until execution completes; the plan should instruct execution to flip those rows to complete only after the reviewed-SHA evidence in `091-VERIFICATION.md` exists.</action>
  <verify>
    <automated>rg -Fq '**Plans:** 3 plans' .planning/ROADMAP.md && rg -Fq 'Release Please-rendered' .planning/ROADMAP.md && rg -Fq -- '- [ ] **REL-06**' .planning/REQUIREMENTS.md && rg -Fq '| REL-06 | Phase 91 | Pending |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The roadmap and requirements tracking are consistent with the three-plan decomposition, the no-numbered-heading rule on `main`, and the fact that Phase 91 requirements remain pending until execution produces reviewed-SHA evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> execution closeout | Incomplete evidence rules can let Phase 91 claim success without the required CI proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-05 | R | 091-VERIFICATION.md | mitigate | Require a reviewed merge SHA plus CI link or transcript pointers for the same SHA |
| T-091-06 | T | 091-VALIDATION.md | mitigate | Enumerate proof for PROC-08, FIN-03, root README warning preservation, docs-contracts-shift-left, and host-integration |
</threat_model>

<verification>
Confirm both new files exist, grep for `host-integration`, and ensure requirements stay pending until execution evidence is produced.
</verification>

<success_criteria>
DOC-04 and the phase validation contract are executor-ready, and the checker can mechanically see the host-integration reviewed-SHA requirement and the non-goal-boundary proof requirements.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.30-phases/091-pre-publish-prep/091-03-SUMMARY.md`.
</output>

---
phase: 092-linked-1-0-0-publish-post-publish-contract-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/mix.exs
  - accrue_admin/mix.exs
  - .release-please-manifest.json
  - accrue/README.md
  - accrue_admin/README.md
  - accrue/guides/first_hour.md
autonomous: true
requirements:
  - REL-05
  - PPX-09
  - PPX-12
must_haves:
  truths:
    - "The release PR carries `1.0.0` in both package `@version` sources and the Release Please manifest in one lockstep slice."
    - "The releasable Phase 92 commit or squash-merge body explicitly carries `Release-As: 1.0.0`, and Release Please shows a combined `1.0.0` release PR for both package paths before merge."
    - "Core and admin install snippets on the public package docs surface read `~> 1.0.0` before the linked publish lands."
    - "No one hand-authors numbered `1.0.0` changelog sections on `main`; Release Please remains the only numbered-release writer."
  artifacts:
    - path: "accrue/mix.exs"
      provides: "Core package `@version` source of truth at `1.0.0`."
    - path: "accrue_admin/mix.exs"
      provides: "Admin package `@version` source of truth at `1.0.0` with `ACCRUE_ADMIN_HEX_RELEASE=1` gate preserved."
    - path: ".release-please-manifest.json"
      provides: "Manifest lockstep at `1.0.0` for both components."
    - path: "accrue/README.md"
      provides: "Core Hex install literal and release-facing package docs copy at `1.0.0`."
    - path: "accrue_admin/README.md"
      provides: "Admin Hex install literal and published `accrue ~> 1.0.0` coupling prose."
    - path: "accrue/guides/first_hour.md"
      provides: "First Hour install pins updated to the `1.0.0` pair."
  key_links:
    - from: ".release-please-manifest.json"
      to: "accrue/mix.exs"
      via: "release-manifest-ssot verifier"
      pattern: "verify_release_manifest_alignment.sh"
    - from: "accrue_admin/mix.exs"
      to: ".github/workflows/release-please.yml"
      via: "ACCRUE_ADMIN_HEX_RELEASE publish gate"
      pattern: "ACCRUE_ADMIN_HEX_RELEASE"
    - from: "accrue/README.md"
      to: "scripts/ci/verify_package_docs.sh"
      via: "version-literal contract"
      pattern: "require_fixed.*README.md"
---

<objective>
Prepare the single linked `1.0.0` release PR surface so both package version sources, the Release Please manifest, and every package-doc install literal move together.

Purpose: satisfy the pre-merge release-surface part of REL-05 plus the package-doc side of PPX-09 / PPX-12 without leaking Phase 93 mirror work or manual numbered changelog edits.
Output: release-surface files plus the explicit `Release-As: 1.0.0` bootstrap proof, an open/refreshed combined `1.0.0` Release Please PR, and a reviewed PR branch that Plan 02 continues to use.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-RESEARCH.md
@.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-PATTERNS.md
@.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-VALIDATION.md
@.planning/milestones/v1.30-phases/091-pre-publish-prep/091-VERIFICATION.md
@.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-01-PLAN.md
@RELEASING.md
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/release-please.yml
@scripts/ci/verify_release_manifest_alignment.sh
@scripts/ci/verify_package_docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock `1.0.0` across both mix projects and the Release Please manifest</name>
  <read_first>
    <file>accrue/mix.exs</file>
    <file>accrue_admin/mix.exs</file>
    <file>.release-please-manifest.json</file>
    <file>.github/workflows/release-please.yml</file>
    <file>scripts/ci/verify_release_manifest_alignment.sh</file>
    <file>.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-RESEARCH.md</file>
  </read_first>
  <files>accrue/mix.exs, accrue_admin/mix.exs, .release-please-manifest.json</files>
  <action>Per REL-05 and the Phase 92 constraint set, bump only the lockstep version literals to `1.0.0` in `accrue/mix.exs`, `accrue_admin/mix.exs`, and `.release-please-manifest.json`. Preserve `source_ref: "accrue-v#{@version}"`, `source_ref: "accrue_admin-v#{@version}"`, and the existing `if System.get_env("ACCRUE_ADMIN_HEX_RELEASE") == "1"` gate exactly; do not touch package file lists, deps, or workflow ordering. Stage these edits so the releasable Phase 92 commit itself includes an exact trailer line `Release-As: 1.0.0`, because the resolved research requires that bootstrap step to force the combined Release Please PR to `1.0.0`. Do not rely on an uncommitted squash-merge-body-only path. Do not hand-author numbered `1.0.0` changelog sections on `main`; Release Please continues to own that output.</action>
  <acceptance_criteria>
    <criterion>`rg -Fq '@version "1.0.0"' accrue/mix.exs` exits 0.</criterion>
    <criterion>`rg -Fq '@version "1.0.0"' accrue_admin/mix.exs` exits 0.</criterion>
    <criterion>`rg -Fq '"accrue": "1.0.0"' .release-please-manifest.json` exits 0.</criterion>
    <criterion>`rg -Fq '"accrue_admin": "1.0.0"' .release-please-manifest.json` exits 0.</criterion>
    <criterion>`rg -Fq 'ACCRUE_ADMIN_HEX_RELEASE' accrue_admin/mix.exs` exits 0.</criterion>
    <criterion>`git log -1 --pretty=%B | rg -F 'Release-As: 1.0.0'` exits 0 on the releasable Phase 92 commit.</criterion>
    <criterion>`bash scripts/ci/verify_release_manifest_alignment.sh` exits 0 and prints `OK: release manifest and mix.exs @version aligned at 1.0.0`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh && (git log -1 --pretty=%B | rg -F 'Release-As: 1.0.0')</automated>
  </verify>
  <done>`accrue`, `accrue_admin`, and the manifest all declare `1.0.0`, the existing admin publish gate / publish ordering contract remains intact, and the releasable Phase 92 commit carries the exact bootstrap trailer `Release-As: 1.0.0`.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the combined Release Please PR is forced to `1.0.0` before merge</name>
  <read_first>
    <file>RELEASING.md</file>
    <file>.github/workflows/release-please.yml</file>
    <file>release-please-config.json</file>
    <file>.release-please-manifest.json</file>
    <file>.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-RESEARCH.md</file>
  </read_first>
  <files>.release-please-manifest.json, accrue/mix.exs, accrue_admin/mix.exs, accrue/README.md, accrue_admin/README.md, accrue/guides/first_hour.md</files>
  <action>After the `Release-As: 1.0.0` trailer is on the reviewed Phase 92 slice, explicitly open or refresh the combined Release Please PR before trying to verify it. Use the documented bootstrap path from `RELEASING.md`: if an open combined release PR already exists, record its PR number and head branch; otherwise run `gh workflow run release-please.yml -f bootstrap_release_as=1.0.0`, wait for the workflow to finish, and capture the newly opened combined release PR. Then make the Phase 92 release-surface edits live on that release PR branch: either execute Plan 01 directly on the PR head branch or cherry-pick the reviewed Plan 01 commits onto it, push, and prove the PR now targets `1.0.0` for both package paths. Treat that open release PR branch as the canonical execution branch for both Plan 01 and Plan 02 so the final merge carries one coherent Phase 92 slice. Do not merge or dispatch publish in this plan; the actual trigger and post-merge proof belong to Plan 03 after all `1.0.0` doc and integrator-surface updates are complete.</action>
  <acceptance_criteria>
    <criterion>If no open combined release PR existed at the start of the task, `gh workflow run release-please.yml -f bootstrap_release_as=1.0.0` is executed and a completed run or resulting PR URL is captured in the summary.</criterion>
    <criterion>An open combined Release Please PR is identified with both a PR number and a head branch name before file-list verification begins.</criterion>
    <criterion>`gh pr view --json title,body,files` for the release PR shows `accrue/mix.exs`, `accrue_admin/mix.exs`, and `.release-please-manifest.json` in the file list, includes `1.0.0` in the title or body, and proves the PR is a single combined release slice rather than two independent package PRs.</criterion>
    <criterion>`gh pr view --json files` for that same release PR shows `accrue/README.md`, `accrue_admin/README.md`, and `accrue/guides/first_hour.md` in the file list after the Phase 92 release-surface edits are pushed onto the PR branch.</criterion>
    <criterion>If the workflow-output path is used instead of PR inspection, the captured output includes both package names and `1.0.0` for the same run.</criterion>
    <criterion>`gh pr view --json title,body,files` proof is captured before merge and identifies one combined linked release PR at `1.0.0` for both packages.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>PR=$(gh pr list --state open --json number,title | jq -r 'map(select(.title | test("1\\.0\\.0"))) | first.number // empty') && test -n "$PR" && gh pr view "$PR" --json headRefName,title,body,files | jq -e '(.headRefName | length > 0) and (((.title + " " + (.body // "")) | test("1\\\\.0\\\\.0"))) and ([.files[].path] | index("accrue/mix.exs") and index("accrue_admin/mix.exs") and index(".release-please-manifest.json") and index("accrue/README.md") and index("accrue_admin/README.md") and index("accrue/guides/first_hour.md"))' >/dev/null</automated>
  </verify>
  <done>REL-05 has pre-merge proof that Release Please was forced onto a combined `1.0.0` release PR, that the PR has a concrete head branch for continued work, and that the Phase 92 release-surface edits already live on that same PR before Plan 02 continues.</done>
</task>

<task type="auto">
  <name>Task 3: Move package-doc install literals and release-facing prose to `1.0.0`</name>
  <read_first>
    <file>accrue/README.md</file>
    <file>accrue_admin/README.md</file>
    <file>accrue/guides/first_hour.md</file>
    <file>scripts/ci/verify_package_docs.sh</file>
    <file>.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-PATTERNS.md</file>
    <file>.planning/milestones/v1.30-phases/091-pre-publish-prep/091-CONTEXT.md</file>
  </read_first>
  <files>accrue/README.md, accrue_admin/README.md, accrue/guides/first_hour.md</files>
  <action>Per PPX-09 and PPX-12, update every `verify_package_docs.sh`-guarded version literal from `0.3.1` to `1.0.0` in `accrue/README.md`, `accrue_admin/README.md`, and `accrue/guides/first_hour.md`. Keep the existing `> **Hex vs \`main\`:**` framing, keep the First Hour structure unchanged, and update the admin README sentence `Published \`accrue_admin\` releases resolve \`accrue ~> ...\`.` to `1.0.0`. Do not edit `examples/accrue_host/README.md` or the adoption matrix in this plan; those inspection/proof surfaces belong to Plan 02.</action>
  <acceptance_criteria>
    <criterion>`rg -Fq '{:accrue, "~> 1.0.0"}' accrue/README.md` exits 0.</criterion>
    <criterion>`rg -Fq '{:accrue_admin, "~> 1.0.0"}' accrue_admin/README.md` exits 0.</criterion>
    <criterion>`rg -Fq 'accrue ~> 1.0.0' accrue_admin/README.md` exits 0.</criterion>
    <criterion>`rg -Fq '{:accrue, "~> 1.0.0"}' accrue/guides/first_hour.md` exits 0.</criterion>
    <criterion>`rg -Fq '{:accrue_admin, "~> 1.0.0"}' accrue/guides/first_hour.md` exits 0.</criterion>
    <criterion>`bash scripts/ci/verify_package_docs.sh` exits 0 and mentions `package docs verified for accrue 1.0.0 and accrue_admin 1.0.0`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The package READMEs and First Hour pins match `mix.exs @version`, and the merge-blocking package-doc contract is green at `1.0.0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> CI verifiers | Public install copy must match `mix.exs` and manifest or CI attests a false release surface |
| release PR -> publish workflow | The merged branch state feeds GitHub Actions publish jobs, so broken version coupling becomes a live release error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | `accrue_admin/mix.exs` | mitigate | Preserve the `ACCRUE_ADMIN_HEX_RELEASE=1` dependency gate exactly while changing only `@version` per REL-05. |
| T-92-02 | Repudiation | `.release-please-manifest.json` | mitigate | Require `bash scripts/ci/verify_release_manifest_alignment.sh` before Plan 01 completes. |
| T-92-03 | Information Disclosure | docs / changelogs | accept | No secrets or PII are introduced; keep publish docs limited to versions, not tokens or runtime secrets. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_release_manifest_alignment.sh` and `bash scripts/ci/verify_package_docs.sh` after the last edit in this plan.
</verification>

<success_criteria>
`1.0.0` is the single source of truth across both `mix.exs` files, the Release Please manifest, and the package-doc install literals without any manual numbered changelog authoring on `main`.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-01-SUMMARY.md`
</output>

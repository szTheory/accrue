---
phase: 092-linked-1-0-0-publish-post-publish-contract-sweep
plan: 02
type: execute
wave: 2
depends_on:
  - 092-01
files_modified:
  - examples/accrue_host/README.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_adoption_proof_matrix.sh
  - scripts/ci/verify_verify01_readme_contract.sh
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - PPX-10
  - PPX-11
  - PPX-12
must_haves:
  truths:
    - "The integrator-facing host proof surface explicitly says `1.0.0` on the release slice."
    - "The adoption matrix explicitly says `1.0.0` and still matches its verifier after the `0.3.1 -> 1.0.0` jump."
    - "The mandatory `release-manifest-ssot` gate and the six-script `docs-contracts-shift-left` bundle both pass on the reviewed `1.0.0` branch state."
  artifacts:
    - path: "examples/accrue_host/README.md"
      provides: "Host README proof surface updated with an explicit `1.0.0` release needle."
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "Adoption proof matrix aligned to current verifier expectations."
    - path: "scripts/ci/verify_adoption_proof_matrix.sh"
      provides: "Matrix contract needles when wording changes are intentional."
  key_links:
    - from: "examples/accrue_host/docs/adoption-proof-matrix.md"
      to: "scripts/ci/verify_adoption_proof_matrix.sh"
      via: "substring verifier"
      pattern: "verify_adoption_proof_matrix"
    - from: "examples/accrue_host/README.md"
      to: "scripts/ci/verify_verify01_readme_contract.sh"
      via: "VERIFY-01 host contract"
      pattern: "verify_verify01_readme_contract"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/*.sh"
      via: "docs-contracts-shift-left job membership"
      pattern: "docs-contracts-shift-left"
---

<objective>
Re-run the integrator-surface and verifier contracts against the `1.0.0` branch state, with explicit `1.0.0` wording added to the host-facing proof surfaces.

Purpose: satisfy PPX-10 / PPX-11 / PPX-12 with the same shift-left discipline used in Phase 86, while requiring explicit `1.0.0` wording on the host README and adoption matrix instead of allowing a no-op inspection.
Output: green host/adoption verifiers, a reviewed branch state that passes `release-manifest-ssot` plus the full six-script docs bundle, and file-list proof that the open Release Please PR now carries the full Phase 92 slice before merge.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-RESEARCH.md
@.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-PATTERNS.md
@.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-VALIDATION.md
@.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md
@.github/workflows/ci.yml
@scripts/ci/README.md
@scripts/ci/verify_release_manifest_alignment.sh
@scripts/ci/verify_package_docs.sh
@scripts/ci/verify_adoption_proof_matrix.sh
@scripts/ci/verify_v1_17_friction_research_contract.sh
@scripts/ci/verify_verify01_readme_contract.sh
@scripts/ci/verify_production_readiness_discoverability.sh
@scripts/ci/verify_core_admin_invoice_verify_ids.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit `1.0.0` needles to the host README and adoption matrix</name>
  <read_first>
    <file>examples/accrue_host/README.md</file>
    <file>examples/accrue_host/docs/adoption-proof-matrix.md</file>
    <file>scripts/ci/verify_adoption_proof_matrix.sh</file>
    <file>scripts/ci/verify_verify01_readme_contract.sh</file>
    <file>scripts/ci/README.md</file>
    <file>.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-RESEARCH.md</file>
  </read_first>
  <files>examples/accrue_host/README.md, examples/accrue_host/docs/adoption-proof-matrix.md, scripts/ci/verify_adoption_proof_matrix.sh, scripts/ci/verify_verify01_readme_contract.sh</files>
  <action>Per PPX-10 and PPX-12, explicitly add `1.0.0` wording to both `examples/accrue_host/README.md` and `examples/accrue_host/docs/adoption-proof-matrix.md` after Plan 01 lands. Add one stable host README sentence in the proof section that names the linked `accrue` / `accrue_admin` `1.0.0` verification surface. In the adoption matrix, do more than a banner sentence: audit every row whose package/version or release-proof wording is affected by the `0.3.1 -> 1.0.0` jump, refresh the row-level text and any per-row evidence notes that mention the published pair, and add one stable sentence that names the matrix as refreshed for the linked `1.0.0` pair. Update `scripts/ci/verify_verify01_readme_contract.sh` and `scripts/ci/verify_adoption_proof_matrix.sh` in the same slice so those exact new `1.0.0` needles become merge-blocking. No inspection-only no-op path is allowed here.</action>
  <acceptance_criteria>
    <criterion>`bash scripts/ci/verify_adoption_proof_matrix.sh` exits 0.</criterion>
    <criterion>`bash scripts/ci/verify_verify01_readme_contract.sh` exits 0.</criterion>
    <criterion>`rg -Fq '## Proof and verification' examples/accrue_host/README.md` exits 0.</criterion>
    <criterion>`rg -Fq '## Organization billing proof (ORG-09)' examples/accrue_host/docs/adoption-proof-matrix.md` exits 0.</criterion>
    <criterion>`rg -Fq 'accrue` / `accrue_admin` `1.0.0`' examples/accrue_host/README.md` exits 0.</criterion>
    <criterion>`rg -Fq 'linked `1.0.0` pair' examples/accrue_host/docs/adoption-proof-matrix.md` exits 0.</criterion>
    <criterion>Every adoption-matrix row whose evidence or package-version wording referenced the published `0.3.1` pair has been reviewed and either updated to `1.0.0` wording or explicitly confirmed to remain version-agnostic in the plan summary.</criterion>
    <criterion>`rg -Fq '1.0.0' scripts/ci/verify_verify01_readme_contract.sh` exits 0.</criterion>
    <criterion>`rg -Fq '1.0.0' scripts/ci/verify_adoption_proof_matrix.sh` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>The host README and adoption matrix both contain explicit `1.0.0` needles, the affected matrix rows/per-row evidence have been reviewed for the `1.0.0` cut, and the dedicated verifiers require those exact strings.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run `release-manifest-ssot` and the full six-script docs contract bundle</name>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>scripts/ci/verify_release_manifest_alignment.sh</file>
    <file>scripts/ci/verify_package_docs.sh</file>
    <file>scripts/ci/verify_v1_17_friction_research_contract.sh</file>
    <file>scripts/ci/verify_verify01_readme_contract.sh</file>
    <file>scripts/ci/verify_production_readiness_discoverability.sh</file>
    <file>scripts/ci/verify_adoption_proof_matrix.sh</file>
    <file>scripts/ci/verify_core_admin_invoice_verify_ids.sh</file>
    <file>.planning/milestones/v1.28-phases/086-post-publish-contract-alignment/086-VERIFICATION.md</file>
  </read_first>
  <files>examples/accrue_host/README.md, examples/accrue_host/docs/adoption-proof-matrix.md, scripts/ci/verify_package_docs.sh, scripts/ci/verify_adoption_proof_matrix.sh, scripts/ci/verify_verify01_readme_contract.sh</files>
  <action>Continue on the same open Release Please PR branch identified in Plan 01. Use `.github/workflows/ci.yml` as the normative job-membership source and rerun the exact merge-blocking gate set for this phase: first `bash scripts/ci/verify_release_manifest_alignment.sh`, then the six scripts in `docs-contracts-shift-left` order. If any command fails, fix only the declared host docs or verifier scripts in this plan until the bundle is green. Before declaring the plan complete, prove that this same open Release Please PR file list now contains the full Phase 92 slice: the Plan 01 version/package-doc files plus `examples/accrue_host/README.md`, `examples/accrue_host/docs/adoption-proof-matrix.md`, `scripts/ci/verify_adoption_proof_matrix.sh`, and `scripts/ci/verify_verify01_readme_contract.sh`. Do not broaden Phase 92 into `.planning/PROJECT.md`, `.planning/MILESTONES.md`, or `.planning/STATE.md`; those mirror updates remain Phase 93 scope even if the verifiers tempt unrelated edits.</action>
  <acceptance_criteria>
    <criterion>`bash scripts/ci/verify_release_manifest_alignment.sh` exits 0.</criterion>
    <criterion>Each script in the six-command `docs-contracts-shift-left` bundle exits 0 when run sequentially from repo root.</criterion>
    <criterion>`rg -n 'docs-contracts-shift-left' .github/workflows/ci.yml` exits 0.</criterion>
    <criterion>`rg -n 'release-manifest-ssot' .github/workflows/ci.yml` exits 0.</criterion>
    <criterion>`gh pr view --json files` for the open combined release PR shows `accrue/mix.exs`, `accrue_admin/mix.exs`, `.release-please-manifest.json`, `accrue/README.md`, `accrue_admin/README.md`, `accrue/guides/first_hour.md`, `examples/accrue_host/README.md`, `examples/accrue_host/docs/adoption-proof-matrix.md`, `scripts/ci/verify_adoption_proof_matrix.sh`, and `scripts/ci/verify_verify01_readme_contract.sh` in one file list before merge.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_v1_17_friction_research_contract.sh && bash scripts/ci/verify_verify01_readme_contract.sh && bash scripts/ci/verify_production_readiness_discoverability.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/verify_core_admin_invoice_verify_ids.sh && PR=$(gh pr list --state open --json number,title | jq -r 'map(select(.title | test("1\\.0\\.0"))) | first.number // empty') && test -n "$PR" && gh pr view "$PR" --json files | jq -e '([.files[].path] | index("accrue/mix.exs") and index("accrue_admin/mix.exs") and index(".release-please-manifest.json") and index("accrue/README.md") and index("accrue_admin/README.md") and index("accrue/guides/first_hour.md") and index("examples/accrue_host/README.md") and index("examples/accrue_host/docs/adoption-proof-matrix.md") and index("scripts/ci/verify_adoption_proof_matrix.sh") and index("scripts/ci/verify_verify01_readme_contract.sh"))' >/dev/null</automated>
  </verify>
  <done>The reviewed `1.0.0` release slice is clean against both the manifest SSOT guard and every script in the merge-blocking docs bundle, and the open combined Release Please PR is proven to carry the full Phase 92 file set before merge.</done>
</task>

<task type="auto">
  <name>Task 3: Re-prove the integrator slice with the host wrapper after the host README `1.0.0` edit</name>
  <read_first>
    <file>examples/accrue_host/README.md</file>
    <file>scripts/ci/README.md</file>
    <file>.github/workflows/ci.yml</file>
    <file>.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-VALIDATION.md</file>
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <action>Because Task 1 must edit `examples/accrue_host/README.md`, first run a fast preflight that proves the new host README `1.0.0` needle is present and that `bash scripts/ci/verify_verify01_readme_contract.sh` still passes. Only after that fast check is green should you run the expensive wrapper `bash scripts/ci/accrue_host_uat.sh` and keep its reviewed output for Plan 03’s verification artifact. Do not mutate unrelated host files beyond the planned `1.0.0` wording and any verifier-coupled updates.</action>
  <acceptance_criteria>
    <criterion>`rg -Fq 'accrue` / `accrue_admin` `1.0.0`' examples/accrue_host/README.md` exits 0 before the host wrapper proof is recorded.</criterion>
    <criterion>`bash scripts/ci/verify_verify01_readme_contract.sh` exits 0 as the fast preflight before the wrapper run.</criterion>
    <criterion>`bash scripts/ci/accrue_host_uat.sh` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -Fq 'accrue` / `accrue_admin` `1.0.0`' examples/accrue_host/README.md && bash scripts/ci/verify_verify01_readme_contract.sh && bash scripts/ci/accrue_host_uat.sh</automated>
  </verify>
  <done>Host-facing `1.0.0` wording is backed by a fast README/verifier preflight and then by a fresh green host wrapper rerun for Plan 03 to cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host docs -> evaluators | Integrator docs can misrepresent what the release actually proves if host README or matrix drift from verifier scripts |
| CI workflow -> local reruns | The phase must follow job membership defined in `.github/workflows/ci.yml`, not an ad-hoc subset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Tampering | `examples/accrue_host/docs/adoption-proof-matrix.md` | mitigate | Keep any matrix wording change coupled to `verify_adoption_proof_matrix.sh` in the same plan per PPX-10. |
| T-92-05 | Repudiation | CI proof scope | mitigate | Use `.github/workflows/ci.yml` job `docs-contracts-shift-left` plus `release-manifest-ssot` as the normative gate list. |
| T-92-06 | Denial of Service | host proof lane | accept | `host-integration` reruns are limited to the single planned host README `1.0.0` update and are not broadened into unrelated host changes. |
</threat_model>

<verification>
The plan is complete only when `bash scripts/ci/verify_release_manifest_alignment.sh`, the six `docs-contracts-shift-left` scripts, and `bash scripts/ci/accrue_host_uat.sh` all exit 0.
</verification>

<success_criteria>
The host README and adoption matrix both contain explicit `1.0.0` wording, their verifiers require those exact needles, and the reviewed `1.0.0` branch state is green against every mandatory Phase 92 verifier before merge.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.30-phases/092-linked-1-0-0-publish-post-publish-contract-sweep/092-02-SUMMARY.md`
</output>

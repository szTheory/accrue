---
phase: 094-strategy-capability-matrix-target-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STRATEGY.md
  - .planning/PROJECT.md
  - accrue/guides/custom_processors.md
autonomous: true
requirements: [PROC-09]
must_haves:
  truths:
    - "The repo strategy states that first-party processor support is a named capability slice, not generic processor parity."
    - "The repo strategy names Braintree as the locked second-provider target with written rationale and explicit non-targets."
    - "The repo strategy keeps Fake as the deterministic blocking proof lane and treats provider-backed runs as fidelity checks."
    - "The repo planning posture says checkout and billing portal remain Stripe-first or out of the official second-provider slice."
    - "The custom-processors guide stays an extension-point document and does not market custom adapters as first-party supported or release-gated."
  artifacts:
    - path: ".planning/STRATEGY.md"
      provides: "Persistent PROC-08 strategy tracker with target-provider lock and explicit boundaries"
      min_lines: 30
    - path: ".planning/PROJECT.md"
      provides: "Milestone-level project truth mirroring the locked provider and slice posture"
      contains: "Braintree"
    - path: "accrue/guides/custom_processors.md"
      provides: "Extension-point guidance that stays outside first-party support and parity promises"
      contains: "outside first-party support"
  key_links:
    - from: ".planning/STRATEGY.md"
      to: ".planning/PROJECT.md"
      via: "Shared v1.31 strategic posture and target-provider wording"
      pattern: "Braintree|Fake|gateway subscription core|Stripe-first"
    - from: ".planning/STRATEGY.md"
      to: "accrue/guides/custom_processors.md"
      via: "D-03 extension-point posture mirrored into user-facing custom-processor guidance"
      pattern: "extension point|outside first-party support|release guarantees"
    - from: ".planning/STRATEGY.md"
      to: ".planning/processor-support-matrix.md"
      via: "Strategy names the support posture that the matrix operationalizes"
      pattern: "capability slice|Stripe-only|out of slice"
---

<objective>
Lock the strategic truth for Phase 94 before code-facing capability work lands.

Purpose: Make the repo-wide processor-expansion posture explicit so later phases cannot drift into unsupported parity claims or the wrong provider class.
Output: Updated `.planning/STRATEGY.md` and `.planning/PROJECT.md` that mirror the locked Braintree target, Fake-first proof lane, gateway subscription core slice, and explicit non-goals.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STRATEGY.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-CONTEXT.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-RESEARCH.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-PATTERNS.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-VALIDATION.md

<interfaces>
From `.planning/STRATEGY.md`:
```markdown
## Active Strategic Track
### PROC-08 — Official dual-provider core
## Track Boundaries
## Execution Shape
## Decision Notes
```

From `094-CONTEXT.md` locked wording:
```text
D-01 explicit first-party supported slice + custom adapters outside that slice
D-02 Fake remains the required deterministic proof lane
D-05 Lock Braintree as the official second-provider target
D-08 Gateway subscription core is the official second-provider slice
D-15 checkout_session and billing_portal_session remain Stripe-first until proven otherwise
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 94-01-01: Rewrite the persistent strategy tracker around the locked processor-support posture</name>
  <files>.planning/STRATEGY.md</files>
  <read_first>
.planning/STRATEGY.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-CONTEXT.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-RESEARCH.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-PATTERNS.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
  </read_first>
  <action>Rewrite `.planning/STRATEGY.md` per D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-12, D-15, D-17, D-19, D-20, D-21, D-22, D-23, D-24, D-25, and D-26 so the active PROC-08 track explicitly says: first-party support is a named capability slice rather than generic processor parity; `Fake` is the deterministic merge-blocking proof lane; provider-backed runs are fidelity checks only; `Braintree` is the locked Stripe-like gateway target with written rationale, including that it best matches Accrue's Stripe-shaped facade, has a tractable Elixir package surface, and stays inside the direct-gateway strategy boundary; the first official slice is `gateway subscription core`; checkout and billing portal stay Stripe-first / out-of-slice until proven; merchant-of-record providers, `Adyen`, `PayPal direct subscriptions`, and bank-debit specialists such as `GoCardless` remain explicit non-targets; unsupported capabilities must fail clearly and early rather than implying parity; decision notes must explicitly capture the Laravel Cashier, Pay (Rails), ActiveMerchant, and Ecto/Active Storage/Active Job lessons; and the defaults are reopened only for high-impact changes like adding a provider, expanding the slice, changing release-gate philosophy, or promoting a Stripe-only API into the official multi-provider contract. Also state per D-03 that custom processors remain an extension point outside first-party parity and release guarantees. Preserve the existing file shape from `094-PATTERNS.md`: active track, boundaries, execution shape, then decision notes. Do not introduce “processor-agnostic” or “lowest-common-denominator” wording.</action>
  <verify>
    <automated>rg -Fq 'Braintree' .planning/STRATEGY.md && rg -Fq 'gateway subscription core' .planning/STRATEGY.md && rg -Fq 'Fake' .planning/STRATEGY.md && rg -Fq 'Stripe-first' .planning/STRATEGY.md && rg -Fq 'merchant-of-record' .planning/STRATEGY.md && rg -Fq 'Adyen' .planning/STRATEGY.md && rg -Fq 'PayPal direct subscriptions' .planning/STRATEGY.md && rg -Fq 'GoCardless' .planning/STRATEGY.md && rg -Fq 'fail clearly and early' .planning/STRATEGY.md && rg -Fq 'Laravel Cashier' .planning/STRATEGY.md && rg -Fq 'Pay (Rails)' .planning/STRATEGY.md && rg -Fq 'ActiveMerchant' .planning/STRATEGY.md && rg -Fq 'reopened only for high-impact changes' .planning/STRATEGY.md</automated>
  </verify>
  <acceptance_criteria>
- `.planning/STRATEGY.md` contains the exact substring `Braintree`.
- `.planning/STRATEGY.md` contains the exact substring `gateway subscription core`.
- `.planning/STRATEGY.md` contains the exact substring `Fake` in the proof-lane section.
- `.planning/STRATEGY.md` contains the exact substring `Stripe-first`.
- `.planning/STRATEGY.md` contains the exact substring `merchant-of-record`.
- `.planning/STRATEGY.md` contains the exact substring `Adyen`.
- `.planning/STRATEGY.md` contains the exact substring `PayPal direct subscriptions`.
- `.planning/STRATEGY.md` contains the exact substring `GoCardless`.
- `.planning/STRATEGY.md` contains the exact substring `fail clearly and early`.
- `.planning/STRATEGY.md` contains the exact substring `Laravel Cashier`.
- `.planning/STRATEGY.md` contains the exact substring `Pay (Rails)`.
- `.planning/STRATEGY.md` contains the exact substring `ActiveMerchant`.
- `.planning/STRATEGY.md` contains the exact substring `reopened only for high-impact changes`.
  </acceptance_criteria>
  <done>The strategy tracker is the persistent repo SSOT for the locked provider, slice, proof lane, and non-goals.</done>
</task>

<task type="auto">
  <name>Task 94-01-02: Mirror the locked strategy in project truth and keep custom processors as an extension-point guide</name>
  <files>.planning/PROJECT.md, accrue/guides/custom_processors.md</files>
  <read_first>
.planning/PROJECT.md
.planning/STRATEGY.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-CONTEXT.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-RESEARCH.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-PATTERNS.md
accrue/guides/custom_processors.md
.planning/ROADMAP.md
  </read_first>
  <action>Update the active `v1.31` section in `.planning/PROJECT.md` so the project-level milestone truth mirrors D-03, D-05, D-08, D-10, D-14, D-15, and D-24. Add concrete wording that the chosen second provider is `Braintree`, the supported slice is `gateway subscription core`, `Accrue.Billing.subscribe/3` is the primary public-facade candidate for the Phase 96 second-provider thin slice, `Fake` remains the deterministic default proof lane, and checkout/billing-portal parity is not part of the first official second-provider promise. In the same task, update `accrue/guides/custom_processors.md` so it explicitly says custom adapters remain an extension point outside first-party support, parity promises, release gates, and the official processor-support matrix unless explicitly listed there, per D-03. Keep the existing structure and milestone framing intact; do not broaden the milestone into processor-agnostic refactor work.</action>
  <verify>
    <automated>rg -Fq 'Braintree' .planning/PROJECT.md && rg -Fq 'gateway subscription core' .planning/PROJECT.md && rg -Fq 'Accrue.Billing.subscribe/3' .planning/PROJECT.md && rg -Fq 'Fake' .planning/PROJECT.md && rg -Fq 'Stripe-first' .planning/PROJECT.md && rg -Fq 'outside first-party support' accrue/guides/custom_processors.md && rg -Fq 'official processor-support matrix' accrue/guides/custom_processors.md</automated>
  </verify>
  <acceptance_criteria>
- `.planning/PROJECT.md` contains the exact substring `Braintree`.
- `.planning/PROJECT.md` contains the exact substring `gateway subscription core`.
- `.planning/PROJECT.md` contains the exact substring `Accrue.Billing.subscribe/3`.
- `.planning/PROJECT.md` contains the exact substring `Fake`.
- `.planning/PROJECT.md` contains the exact substring `Stripe-first`.
- `accrue/guides/custom_processors.md` contains the exact substring `outside first-party support`.
- `accrue/guides/custom_processors.md` contains the exact substring `official processor-support matrix`.
  </acceptance_criteria>
  <done>The milestone-level planning truth matches the strategy tracker, and the custom-processors guide is explicitly posture-bound as an extension point rather than an adoption front door.</done>
</task>

</tasks>

<threat_model>
Execution blocking rule: Any `High` severity threat affecting support-boundary truth, custom-processor posture drift, or webhook-safety wording blocks execution until its mitigation is implemented and the referenced automated verifier is updated.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning truth → downstream implementation | Later phases will treat strategy wording as authority for what support claims are allowed. |
| strategy posture → adopter interpretation | Over-broad wording here can make unsupported processor operations look first-party supported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-01 | T | `.planning/STRATEGY.md` | High | mitigate | Hard-code the D-01/D-05/D-08/D-15 posture in acceptance criteria so the strategy must name Braintree, Fake, gateway subscription core, and Stripe-first boundaries, then make those literals merge-visible through `verify_package_docs.sh`. |
| T-94-02 | R | `.planning/PROJECT.md` | Medium | mitigate | Mirror the same locked wording in project-level milestone truth so later phases cannot claim the strategy was ambiguous or silent about the official slice. |
| T-94-03 | I | strategy prose and `accrue/guides/custom_processors.md` | High | mitigate | Explicitly list out-of-scope providers and Stripe-only/out-of-slice surfaces, and state per D-03 that custom processors remain outside first-party support, parity promises, and release gates unless explicitly listed in the support matrix. |
</threat_model>

<verification>
Run the strategy grep checks in the task-level `<automated>` commands.
Cross-read `.planning/STRATEGY.md` and `.planning/PROJECT.md` after edits to confirm the same provider, slice, and proof-lane terms are used consistently.
</verification>

<success_criteria>
The repo has one unambiguous strategic story for PROC-08 Phase 1: Braintree is locked, Fake stays merge-blocking, gateway subscription core is the first-party slice, and Stripe-only surfaces remain explicitly labeled as not part of the first second-provider promise.
</success_criteria>

<output>
After completion, create `.planning/phases/094-strategy-capability-matrix-target-lock/094-01-SUMMARY.md`
</output>

---
phase: 094-strategy-capability-matrix-target-lock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/processor-support-matrix.md
  - scripts/ci/verify_processor_support_matrix.sh
  - scripts/ci/verify_package_docs.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [PROC-09]
must_haves:
  truths:
    - "A canonical processor-support matrix names the official first-party capability slice across Fake, Stripe, and Braintree."
    - "Each processor-facing public behavior is labeled as all first-party, Stripe-only, or out of slice."
    - "The matrix is merge-visible through a dedicated bash verifier and CI hook."
  artifacts:
    - path: ".planning/processor-support-matrix.md"
      provides: "Canonical matrix for processor support truth"
      min_lines: 40
    - path: "scripts/ci/verify_processor_support_matrix.sh"
      provides: "Shift-left literal verifier for the processor support matrix"
      contains: "verify_processor_support_matrix: OK"
    - path: ".github/workflows/ci.yml"
      provides: "CI wiring for the new matrix contract"
      contains: "verify_processor_support_matrix.sh"
  key_links:
    - from: ".planning/processor-support-matrix.md"
      to: "scripts/ci/verify_processor_support_matrix.sh"
      via: "Fixed-string verifier needles"
      pattern: "subscription.direct_create|checkout.hosted_handoff|billing_portal.hosted_self_serve"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_processor_support_matrix.sh"
      via: "docs-contracts-shift-left step"
      pattern: "Processor support matrix contract"
---

<objective>
Create the canonical capability matrix and the shift-left enforcement layer for it.

Purpose: Convert the locked provider/slice decisions into a merge-visible contract so unsupported parity claims cannot slip into later phases.
Output: `.planning/processor-support-matrix.md`, `scripts/ci/verify_processor_support_matrix.sh`, and a new docs-contracts-shift-left step in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STRATEGY.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-CONTEXT.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-RESEARCH.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-PATTERNS.md
@.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-VALIDATION.md
@examples/accrue_host/docs/adoption-proof-matrix.md
@scripts/ci/verify_adoption_proof_matrix.sh
@.github/workflows/ci.yml

<interfaces>
From `accrue/lib/accrue/processor/capabilities.ex`:
```elixir
@spec supports?(map(), [atom()]) :: boolean()
def supports?(capabilities, path) when is_map(capabilities) and is_list(path)
```

From `accrue/lib/accrue/checkout/session.ex`:
```elixir
raise Accrue.APIError,
  code: "processor_operation_unsupported",
  message: "#{Processor.name()} does not support hosted checkout"
```

From `examples/accrue_host/docs/adoption-proof-matrix.md`:
```markdown
## Layering note (local proof vs merge-blocking CI)
## Blocking: Fake-backed host + browser
## Advisory: Stripe test mode (network)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 94-02-01: Create the canonical processor support matrix with semantic capability rows</name>
  <files>.planning/processor-support-matrix.md</files>
  <read_first>
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-CONTEXT.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-RESEARCH.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-PATTERNS.md
examples/accrue_host/docs/adoption-proof-matrix.md
accrue/lib/accrue/processor/capabilities.ex
accrue/lib/accrue/checkout/session.ex
  </read_first>
  <action>Create `.planning/processor-support-matrix.md` as the first-class processor-support truth artifact per D-17 and D-18. Follow the matrix-document shape from `examples/accrue_host/docs/adoption-proof-matrix.md`: intro paragraph, lane framing, then tabular truth. Include explicit provider columns `Fake`, `Stripe`, and `Braintree`, plus a `Public label` column. The matrix must include at least these capability rows using semantic names from D-16 and D-18: `customer.create`, `customer.retrieve`, `customer.update`, `payment_method.vault_acquisition`, `subscription.direct_create`, `subscription.fetch`, `subscription.cancel`, `subscription.lifecycle_webhook_projection`, `invoice.lifecycle_webhook_projection`, `webhook.verify`, `webhook.parse`, `checkout.hosted_handoff`, and `billing_portal.hosted_self_serve`. Mark the gateway-subscription-core rows as `all first-party`; mark `checkout.hosted_handoff` and `billing_portal.hosted_self_serve` as `Stripe-only`; add an explicit out-of-slice section for embedded checkout, setup/payment intents, advanced subscription mutation parity, refunds, coupons, promotion codes, metering, and Connect per D-12. Add a separate public-facade API mapping section that explicitly labels at least `Accrue.Billing.subscribe/3` as `all first-party`, `Accrue.Billing.create_checkout_session/2` as `Stripe-only`, and `Accrue.Billing.create_billing_portal_session/2` as `Stripe-only`, plus any other processor-touched public facade entries relevant to this phase as `all first-party`, `Stripe-only`, or `out of slice`. The matrix narrative must also record Braintree's written rationale, the explicit D-07 non-target provider classes, the D-19 rule that unsupported capabilities fail clearly and early, and the D-20..D-23 lessons so the matrix can be read as an honest support contract rather than raw row data. Name `Braintree` as the locked target and list the D-07 non-target provider classes. Do not use provider event jargon as the row names.</action>
  <verify>
    <automated>rg -Fq '| Capability | Fake | Stripe | Braintree | Public label |' .planning/processor-support-matrix.md && rg -Fq 'subscription.direct_create' .planning/processor-support-matrix.md && rg -Fq 'checkout.hosted_handoff' .planning/processor-support-matrix.md && rg -Fq 'Accrue.Billing.subscribe/3' .planning/processor-support-matrix.md && rg -Fq 'Accrue.Billing.create_checkout_session/2' .planning/processor-support-matrix.md && rg -Fq 'Accrue.Billing.create_billing_portal_session/2' .planning/processor-support-matrix.md && rg -Fq 'Stripe-only' .planning/processor-support-matrix.md && rg -Fq 'out of slice' .planning/processor-support-matrix.md && rg -Fq 'Adyen' .planning/processor-support-matrix.md && rg -Fq 'PayPal direct subscriptions' .planning/processor-support-matrix.md && rg -Fq 'GoCardless' .planning/processor-support-matrix.md && rg -Fq 'fail clearly and early' .planning/processor-support-matrix.md && rg -Fq 'Laravel Cashier' .planning/processor-support-matrix.md && rg -Fq 'Pay (Rails)' .planning/processor-support-matrix.md && rg -Fq 'ActiveMerchant' .planning/processor-support-matrix.md</automated>
  </verify>
  <acceptance_criteria>
- `.planning/processor-support-matrix.md` contains the exact header `| Capability | Fake | Stripe | Braintree | Public label |`.
- `.planning/processor-support-matrix.md` contains the exact substring `subscription.direct_create`.
- `.planning/processor-support-matrix.md` contains the exact substring `Accrue.Billing.subscribe/3`.
- `.planning/processor-support-matrix.md` contains the exact substring `Accrue.Billing.create_checkout_session/2`.
- `.planning/processor-support-matrix.md` contains the exact substring `Accrue.Billing.create_billing_portal_session/2`.
- `.planning/processor-support-matrix.md` contains the exact substring `checkout.hosted_handoff`.
- `.planning/processor-support-matrix.md` contains the exact substring `billing_portal.hosted_self_serve`.
- `.planning/processor-support-matrix.md` contains the exact substring `Stripe-only`.
- `.planning/processor-support-matrix.md` contains the exact substring `out of slice`.
- `.planning/processor-support-matrix.md` contains the exact substring `Adyen`.
- `.planning/processor-support-matrix.md` contains the exact substring `PayPal direct subscriptions`.
- `.planning/processor-support-matrix.md` contains the exact substring `GoCardless`.
- `.planning/processor-support-matrix.md` contains the exact substring `fail clearly and early`.
- `.planning/processor-support-matrix.md` contains the exact substring `Laravel Cashier`.
- `.planning/processor-support-matrix.md` contains the exact substring `Pay (Rails)`.
- `.planning/processor-support-matrix.md` contains the exact substring `ActiveMerchant`.
  </acceptance_criteria>
  <done>The matrix is the canonical support contract for Fake, Stripe, and Braintree, with no ambiguous parity claims left implicit.</done>
</task>

<task type="auto">
  <name>Task 94-02-02: Add a dedicated bash verifier and wire it into docs-contracts-shift-left</name>
  <files>scripts/ci/verify_processor_support_matrix.sh, scripts/ci/verify_package_docs.sh, .github/workflows/ci.yml</files>
  <read_first>
scripts/ci/verify_adoption_proof_matrix.sh
.planning/STRATEGY.md
.planning/PROJECT.md
accrue/guides/custom_processors.md
.planning/processor-support-matrix.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-01-PLAN.md
.github/workflows/ci.yml
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-PATTERNS.md
.planning/milestones/v1.31-phases/094-strategy-capability-matrix-target-lock/094-VALIDATION.md
  </read_first>
  <action>Create `scripts/ci/verify_processor_support_matrix.sh` by copying the adoption-matrix verifier pattern from `094-PATTERNS.md`: same shell structure, same `require_substring` helper, new stderr prefix `verify_processor_support_matrix`, and a success line `verify_processor_support_matrix: OK`. The verifier must pin the matrix headings and the locked literals that carry the phase boundary: `Braintree`, `gateway subscription core`, `Fake`, `subscription.direct_create`, `subscription.lifecycle_webhook_projection`, `invoice.lifecycle_webhook_projection`, `checkout.hosted_handoff`, `billing_portal.hosted_self_serve`, `Stripe-only`, and `out of slice`. In the same task, extend `scripts/ci/verify_package_docs.sh` so Task 94-01-01 becomes merge-visible: add fixed-string checks for the Phase 94 strategy/project/custom-processor truth in `.planning/STRATEGY.md`, `.planning/PROJECT.md`, and `accrue/guides/custom_processors.md`, including the D-03 extension-point posture and the locked `Braintree` / `gateway subscription core` / `Fake` / `Stripe-first` wording. Then add a new step to `.github/workflows/ci.yml` under the existing `docs-contracts-shift-left` job named `Processor support matrix contract` that runs `bash scripts/ci/verify_processor_support_matrix.sh`. Preserve all existing job ids and advisory live-provider wording.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh && rg -Fq 'Processor support matrix contract' .github/workflows/ci.yml && rg -Fq 'bash scripts/ci/verify_processor_support_matrix.sh' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
- `scripts/ci/verify_processor_support_matrix.sh` contains the exact substring `verify_processor_support_matrix: OK`.
- `scripts/ci/verify_processor_support_matrix.sh` contains the exact substring `subscription.direct_create`.
- `scripts/ci/verify_processor_support_matrix.sh` contains the exact substring `checkout.hosted_handoff`.
- `scripts/ci/verify_package_docs.sh` contains exact checks for `.planning/STRATEGY.md`, `.planning/PROJECT.md`, and `accrue/guides/custom_processors.md`.
- `.github/workflows/ci.yml` contains the exact substring `Processor support matrix contract`.
- `.github/workflows/ci.yml` contains the exact substring `bash scripts/ci/verify_processor_support_matrix.sh`.
  </acceptance_criteria>
  <done>The strategy/project/custom-processor truth and matrix contract are merge-visible and will fail CI if later edits weaken or rename the locked support claims.</done>
</task>

</tasks>

<threat_model>
Execution blocking rule: Any `High` severity threat affecting support-boundary truth or webhook-safety wording blocks execution until its mitigation is implemented and the relevant CI verifier is wired into `docs-contracts-shift-left`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix truth → processor implementation | Later adapter and facade work will treat the matrix rows as the contract for what must be supported or rejected. |
| docs artifact → CI enforcement | If the matrix and verifier diverge, CI can falsely green-light unsupported or mislabeled capability claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-04 | T | `.planning/processor-support-matrix.md` | High | mitigate | Require semantic row names and explicit `Stripe-only` / `out of slice` labels so unsupported operations cannot appear supported through vague prose. |
| T-94-05 | T | `scripts/ci/verify_processor_support_matrix.sh` and `scripts/ci/verify_package_docs.sh` | High | mitigate | Pin the provider columns, core capability literals, and D-03 strategy/custom-processor truth with fixed-string checks so drift is caught immediately and Task 94-01-01 is merge-visible. |
| T-94-06 | E | `.github/workflows/ci.yml` | Medium | mitigate | Wire the new verifier into `docs-contracts-shift-left` without changing stable job ids, ensuring the matrix becomes part of the same merge-blocking contract as other support-truth docs. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_processor_support_matrix.sh`.
Confirm the new CI step exists under `docs-contracts-shift-left` and points at the new verifier path verbatim.
</verification>

<success_criteria>
The repo contains one canonical processor-support matrix, that matrix names the official slice and exclusions honestly, and CI blocks drift through a dedicated verifier step.
</success_criteria>

<output>
After completion, create `.planning/phases/094-strategy-capability-matrix-target-lock/094-02-SUMMARY.md`
</output>

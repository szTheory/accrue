---
phase: 095-official-processor-contract-conformance-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/processor-support-matrix.md
  - accrue/lib/accrue/processor/capabilities.ex
  - accrue/lib/accrue/processor.ex
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/test/accrue/processor/capabilities_test.exs
autonomous: true
requirements: [PROC-10]
must_haves:
  truths:
    - "Runtime processor capability truth matches the Phase 94 support matrix instead of inheriting broad legacy parity by default."
    - "The staged Phase 95 slice is explicit for Fake, Stripe, and the Braintree-targeted contract path."
    - "Support labels and executable capability declarations are co-updated in the same change."
  artifacts:
    - path: "accrue/lib/accrue/processor/capabilities.ex"
      provides: "Executable first-party capability mirror with staged slice truth"
      contains: "processor_operation_unsupported"
    - path: "accrue/lib/accrue/processor.ex"
      provides: "Public helper accessors for capability checks and support truth"
      contains: "supports?"
    - path: "accrue/test/accrue/processor/capabilities_test.exs"
      provides: "Focused proof that broad legacy defaults no longer overstate first-party support"
      contains: "Capabilities.for"
  key_links:
    - from: ".planning/processor-support-matrix.md"
      to: "accrue/lib/accrue/processor/capabilities.ex"
      via: "Matrix rows become runtime executable leaves"
      pattern: "customer.create|subscription.direct_create|webhook.verify"
    - from: "accrue/lib/accrue/processor/capabilities.ex"
      to: "accrue/test/accrue/processor/capabilities_test.exs"
      via: "Leaf-level support assertions"
      pattern: "supports?"
---

<objective>
Make the Phase 94 processor-support matrix executable in runtime capability truth.

Purpose: remove or isolate the overly broad legacy capability defaults so the configured processor no longer advertises first-party support it does not honestly have.
Output: stricter capability declarations for first-party adapters, helper accessors on the processor facade, and focused tests proving the staged supported slice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/processor-support-matrix.md
@.planning/STRATEGY.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-PATTERNS.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-VALIDATION.md
@accrue/lib/accrue/processor.ex
@accrue/lib/accrue/processor/capabilities.ex
@accrue/lib/accrue/processor/stripe.ex
@accrue/lib/accrue/processor/fake.ex
@accrue/test/accrue/processor/capabilities_test.exs

<interfaces>
From `.planning/processor-support-matrix.md`:
```markdown
| Capability | Fake | Stripe | Braintree | Public label |
| customer.create | Required | Required | Required target | all first-party |
| checkout.hosted_handoff | Local proof helper | Supported | No | Stripe-only |
```

From `accrue/lib/accrue/processor.ex`:
```elixir
@spec capabilities() :: map()
def capabilities do
  Accrue.Processor.Capabilities.for(__impl__())
end
```

From `accrue/lib/accrue/processor/capabilities.ex`:
```elixir
deep_merge(@legacy_default, declared)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 95-01-01: Replace broad legacy-first capability truth with an explicit first-party slice mirror</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, accrue/lib/accrue/processor/stripe.ex, accrue/lib/accrue/processor/fake.ex</files>
  <read_first>
accrue/lib/accrue/processor/capabilities.ex
accrue/lib/accrue/processor.ex
accrue/lib/accrue/processor/stripe.ex
accrue/lib/accrue/processor/fake.ex
.planning/processor-support-matrix.md
.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
  </read_first>
  <action>Change `accrue/lib/accrue/processor/capabilities.ex` so the executable first-party contract no longer deep-merges broad legacy support into every adapter by default. Preserve the adapter-declaration seam, but make the resulting capability map explicitly model the staged Phase 95 slice named in D-03: `customer.create`, `customer.retrieve`, `payment_method.vault_acquisition`, `subscription.direct_create`, `subscription.fetch`, `webhook.verify`, `webhook.parse`, `subscription.lifecycle_webhook_projection`, and `invoice.lifecycle_webhook_projection`, plus any rows that Phase 95 intentionally keeps staged such as `customer.update` or `subscription.cancel`. Update `accrue/lib/accrue/processor/stripe.ex` and `accrue/lib/accrue/processor/fake.ex` so first-party adapter declarations are explicit instead of relying on optimistic defaults. If compatibility with older internal paths requires a legacy layer, keep that layer named and isolated rather than letting it define first-party support truth implicitly. Do not advertise `checkout.hosted_handoff` or `billing_portal.hosted_self_serve` as all-first-party support.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `accrue/lib/accrue/processor/capabilities.ex` no longer uses broad legacy defaults as the unqualified first-party support source.
- `accrue/lib/accrue/processor/stripe.ex` contains explicit capability declarations for the staged Phase 95 slice.
- `accrue/lib/accrue/processor/fake.ex` contains explicit capability declarations for the staged Phase 95 slice.
- `cd accrue && mix test test/accrue/processor/capabilities_test.exs` exits 0.
  </acceptance_criteria>
  <done>The runtime capability mirror is honest about the bounded first-party slice and no longer implies broad parity by inheritance.</done>
</task>

<task type="auto">
  <name>Task 95-01-02: Expose support-truth helpers and prove matrix/code parity in focused tests</name>
  <files>.planning/processor-support-matrix.md, accrue/lib/accrue/processor.ex, accrue/test/accrue/processor/capabilities_test.exs</files>
  <read_first>
.planning/processor-support-matrix.md
accrue/lib/accrue/processor.ex
accrue/lib/accrue/processor/capabilities.ex
accrue/test/accrue/processor/capabilities_test.exs
.planning/phases/95-official-processor-contract-conformance-harness/095-PATTERNS.md
  </read_first>
  <action>Update `accrue/lib/accrue/processor.ex` with any helper accessors needed so downstream code can ask for support truth without re-deriving matrix semantics from raw nested maps. Examples include helpers for staged/proven rows, public-label lookups, or normalized first-party slice checks, but keep the public API minimal and colocated with `capabilities/0` and `supports?/1`. In the same task, update `.planning/processor-support-matrix.md` if any row names or staged/proven wording must be tightened to match the executable contract exactly, and extend `accrue/test/accrue/processor/capabilities_test.exs` so it proves: Fake and Stripe report the staged thin-slice rows; unsupported rows are false for a narrowed adapter; and any new helper functions return values consistent with the matrix labels.</action>
  <verify>
    <automated>bash scripts/ci/verify_processor_support_matrix.sh && cd accrue && mix test test/accrue/processor/capabilities_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `accrue/lib/accrue/processor.ex` contains a support-truth helper beyond raw adapter resolution.
- `.planning/processor-support-matrix.md` matches the executable capability row names used in code.
- `accrue/test/accrue/processor/capabilities_test.exs` covers narrowed unsupported rows as well as supported staged-slice rows.
- `bash scripts/ci/verify_processor_support_matrix.sh` exits 0.
  </acceptance_criteria>
  <done>The processor facade gives downstream code one honest support-truth seam, and the matrix stays synchronized with runtime capability semantics.</done>
</task>

</tasks>

<threat_model>
Execution blocking rule: Any `High` severity threat that leaves runtime capability truth broader than the public matrix blocks execution until the capability map and its focused tests are corrected.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public matrix → runtime capability mirror | If code is broader than the matrix, adopters get false support promises. |
| adapter declarations → facade guards | Later facade work assumes capability leaves are already honest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-01 | T | `accrue/lib/accrue/processor/capabilities.ex` | High | mitigate | Remove or isolate legacy broad defaults as first-party truth and assert the staged slice explicitly in tests. |
| T-95-02 | R | matrix/code parity | Medium | mitigate | Keep `.planning/processor-support-matrix.md` and capability tests in the same task so support wording and executable leaves cannot drift. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_processor_support_matrix.sh`.
Run `cd accrue && mix test test/accrue/processor/capabilities_test.exs`.
</verification>

<success_criteria>
The configured processor no longer claims broad first-party support by default, and the runtime capability mirror now matches the bounded Phase 95 contract.
</success_criteria>

<output>
After completion, create `.planning/phases/95-official-processor-contract-conformance-harness/095-01-SUMMARY.md`
</output>

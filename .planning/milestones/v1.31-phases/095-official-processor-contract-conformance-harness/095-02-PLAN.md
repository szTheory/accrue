---
phase: 095-official-processor-contract-conformance-harness
plan: 02
type: execute
wave: 1
depends_on: [095-01]
files_modified:
  - accrue/lib/accrue/billing.ex
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/lib/accrue/billing/payment_method_actions.ex
  - accrue/lib/accrue/checkout/session.ex
  - accrue/test/accrue/billing/subscription_actions_test.exs
  - accrue/test/accrue/billing/payment_method_actions_test.exs
autonomous: true
requirements: [PROC-11]
must_haves:
  truths:
    - "Public non-bang APIs fail with `{:error, %Accrue.APIError{code: \"processor_operation_unsupported\"}}` when outside the official slice."
    - "Bang variants raise the same error contract instead of inventing a second unsupported-operation shape."
    - "Only the Stripe assumptions that block the staged subscribe slice are isolated; broad abstraction churn does not land."
  artifacts:
    - path: "accrue/lib/accrue/billing.ex"
      provides: "Public facade support guards and consistent unsupported-operation behavior"
      contains: "processor_operation_unsupported"
    - path: "accrue/lib/accrue/billing/subscription_actions.ex"
      provides: "Isolated staged-slice subscription path with reduced Stripe-only leakage"
      contains: "create_subscription"
    - path: "accrue/test/accrue/billing/subscription_actions_test.exs"
      provides: "Focused unsupported-operation and staged-slice tests"
      contains: "processor_operation_unsupported"
  key_links:
    - from: "accrue/lib/accrue/billing.ex"
      to: "accrue/lib/accrue/checkout/session.ex"
      via: "Shared unsupported-operation error code and bang/non-bang symmetry"
      pattern: "processor_operation_unsupported"
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "accrue/test/accrue/billing/subscription_actions_test.exs"
      via: "Subscribe-path contract tests"
      pattern: "subscribe"
---

<objective>
Make support-boundary truth visible at the public billing facade and isolate only the slice-blocking Stripe assumptions needed for the staged thin slice.

Purpose: ensure unsupported public processor operations fail clearly and early, while `subscribe/3` and adjacent supported rows stop depending on accidental Stripe-only behavior.
Output: facade guards, bounded subscription-path isolation, and focused tests covering supported vs unsupported operations.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/processor-support-matrix.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-PATTERNS.md
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/billing/payment_method_actions.ex
@accrue/lib/accrue/checkout/session.ex
@accrue/test/accrue/checkout/session_test.exs

<interfaces>
From `accrue/lib/accrue/checkout/session.ex`:
```elixir
raise Accrue.APIError,
  code: "processor_operation_unsupported",
  message: "#{Processor.name()} does not support checkout creation"
```

From `.planning/processor-support-matrix.md`:
```markdown
| `Accrue.Billing.subscribe/3` | all first-party |
| `Accrue.Billing.list_payment_methods/2` | out of slice |
| `Accrue.Billing.create_checkout_session/2` | Stripe-only |
```

From `accrue/lib/accrue/billing/subscription_actions.ex`:
```elixir
stripe_params = %{
  customer: customer.processor_id,
  items: [item_params],
  payment_behavior: "default_incomplete",
  expand: ["latest_invoice.payment_intent"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 95-02-01: Add first-party slice guards at the public facade boundary</name>
  <files>accrue/lib/accrue/billing.ex, accrue/lib/accrue/billing/payment_method_actions.ex, accrue/test/accrue/billing/payment_method_actions_test.exs</files>
  <read_first>
accrue/lib/accrue/billing.ex
accrue/lib/accrue/billing/payment_method_actions.ex
accrue/lib/accrue/processor.ex
accrue/lib/accrue/checkout/session.ex
.planning/processor-support-matrix.md
.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
  </read_first>
  <action>Add reusable support guards near the `Accrue.Billing` facade so processor-touched public non-bang APIs return `{:error, %Accrue.APIError{code: "processor_operation_unsupported"}}` when the configured processor is outside the official slice or is explicitly `Stripe-only` / `out of slice`. Apply this to the rows touched by the Phase 95 matrix, including at minimum `list_payment_methods/2` as an out-of-slice surface and any neighboring public facade entries whose current behavior still implies unsupported first-party parity. Preserve bang/non-bang symmetry: the non-bang API returns the tuple error, the bang API raises that same error. Keep existing checkout/portal semantics intact unless they need a small shared helper extraction.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/payment_method_actions_test.exs test/accrue/checkout/session_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `accrue/lib/accrue/billing.ex` contains support-guard logic tied to the executable capability mirror.
- `accrue/lib/accrue/billing/payment_method_actions.ex` rejects out-of-slice public access with `processor_operation_unsupported`.
- `accrue/test/accrue/billing/payment_method_actions_test.exs` proves the non-bang tuple error and bang raise behavior.
- `cd accrue && mix test test/accrue/billing/payment_method_actions_test.exs test/accrue/checkout/session_test.exs` exits 0.
  </acceptance_criteria>
  <done>Public billing APIs no longer silently imply support beyond the official slice.</done>
</task>

<task type="auto">
  <name>Task 95-02-02: Isolate the minimum Stripe-shaped subscribe assumptions needed for the staged thin slice</name>
  <files>accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/billing.ex, accrue/test/accrue/billing/subscription_actions_test.exs</files>
  <read_first>
accrue/lib/accrue/billing/subscription_actions.ex
accrue/lib/accrue/billing.ex
accrue/lib/accrue/processor.ex
accrue/lib/accrue/processor/capabilities.ex
.planning/processor-support-matrix.md
.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
.planning/phases/95-official-processor-contract-conformance-harness/095-PATTERNS.md
  </read_first>
  <action>Refactor `accrue/lib/accrue/billing/subscription_actions.ex` only enough to make `Accrue.Billing.subscribe/3` honest for the staged supported slice. Extract or isolate the Stripe-shaped request assembly so the staged contract can distinguish provider-agnostic required semantics from Stripe-only request details, but do not attempt a broad cross-provider subscription abstraction. Keep the supported path centered on direct subscription creation and lifecycle truth. Add tests in `accrue/test/accrue/billing/subscription_actions_test.exs` that prove: supported processors can still reach `subscribe/3`; unsupported processors fail with the tuple error code; and no out-of-slice plan-swap / advanced mutation behavior is accidentally promoted into the first-party contract.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_actions_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `accrue/lib/accrue/billing/subscription_actions.ex` isolates Stripe-specific subscribe parameters behind a bounded staged-slice seam.
- `accrue/test/accrue/billing/subscription_actions_test.exs` covers both supported subscribe and unsupported-operation cases.
- No new generic processor abstraction layer lands beyond what is required for the staged direct-subscription slice.
- `cd accrue && mix test test/accrue/billing/subscription_actions_test.exs` exits 0.
  </acceptance_criteria>
  <done>The main Stripe-shaped blocker is isolated without widening the phase into a broad parity refactor.</done>
</task>

</tasks>

<threat_model>
Execution blocking rule: Any `High` severity threat that leaves unsupported public billing operations callable without a clear error contract blocks execution until the facade guard and its tests are in place.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executable capability truth → public facade | If the facade does not enforce the bounded slice, users still experience accidental parity. |
| supported direct-subscription slice → Stripe-specific internals | If the subscribe path remains fully Stripe-shaped, Phase 96 will have to redefine the contract mid-flight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-03 | T | `accrue/lib/accrue/billing.ex` | High | mitigate | Add explicit first-party support guards returning `processor_operation_unsupported` tuple errors on non-bang APIs and matching raises on bang APIs. |
| T-95-04 | E | `accrue/lib/accrue/billing/subscription_actions.ex` | High | mitigate | Isolate only the Stripe-shaped assumptions required for the staged thin slice; reject unsupported paths instead of abstracting the whole surface. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/subscription_actions_test.exs test/accrue/billing/payment_method_actions_test.exs`.
</verification>

<success_criteria>
Unsupported processor-facing public APIs fail clearly and early, and the supported `subscribe/3` slice no longer depends on hidden broad parity assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/95-official-processor-contract-conformance-harness/095-02-SUMMARY.md`
</output>

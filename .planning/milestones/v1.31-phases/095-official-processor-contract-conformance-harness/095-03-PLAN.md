---
phase: 095-official-processor-contract-conformance-harness
plan: 03
type: execute
wave: 2
depends_on: [095-01, 095-02]
files_modified:
  - accrue/test/accrue/processor/capabilities_test.exs
  - accrue/test/accrue/billing/subscription_actions_test.exs
  - accrue/test/accrue/webhook/default_handler_phase3_test.exs
  - accrue/guides/testing.md
  - guides/testing-live-stripe.md
  - .planning/processor-support-matrix.md
autonomous: true
requirements: [PROC-10, PROC-11]
must_haves:
  truths:
    - "Fake is the merge-blocking conformance SSOT for the staged supported slice."
    - "Provider-backed smoke is narrow, explicitly advisory/protected-branch only, and tied to the official thin slice."
    - "Lifecycle projection rows are proven in tests and mirrored in support docs."
  artifacts:
    - path: "accrue/test/accrue/billing/subscription_actions_test.exs"
      provides: "Fake-first conformance coverage for direct subscription creation"
      contains: "subscribe"
    - path: "accrue/test/accrue/webhook/default_handler_phase3_test.exs"
      provides: "Lifecycle projection coverage for webhook-backed truth"
      contains: "customer.subscription.updated"
    - path: "guides/testing-live-stripe.md"
      provides: "Provider-smoke lane contract wording"
      contains: "provider-parity checks"
  key_links:
    - from: ".planning/processor-support-matrix.md"
      to: "accrue/test/accrue/webhook/default_handler_phase3_test.exs"
      via: "Lifecycle projection rows must have executable proof"
      pattern: "subscription.lifecycle_webhook_projection|invoice.lifecycle_webhook_projection"
    - from: "accrue/guides/testing.md"
      to: "guides/testing-live-stripe.md"
      via: "Fake-first vs provider-smoke lane wording"
      pattern: "Fake|provider-parity checks|merge-blocking"
---

<objective>
Finish the Phase 95 proof model by turning the staged supported slice into an explicit Fake-first conformance harness and documenting the provider-smoke lane honestly.

Purpose: make the official support promise testable without letting external-provider nondeterminism redefine the merge-blocking contract.
Output: conformance-focused tests, lifecycle projection proof, and testing docs aligned with the Phase 95/96 lane model.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/processor-support-matrix.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-VALIDATION.md
@accrue/guides/testing.md
@guides/testing-live-stripe.md
@accrue/test/accrue/processor/capabilities_test.exs
@accrue/test/accrue/billing/subscription_actions_test.exs
@accrue/test/accrue/webhook/default_handler_phase3_test.exs

<interfaces>
From `accrue/guides/testing.md`:
```markdown
The repo already separates Fake-backed correctness from provider-parity tests.
```

From `guides/testing-live-stripe.md`:
```markdown
This lane uses Stripe test mode and should be treated as `provider-parity checks`,
not as the canonical local demo or the required release lane.
```

From `.planning/processor-support-matrix.md`:
```markdown
| subscription.lifecycle_webhook_projection | Deterministic projection | Supported | Required target | all first-party |
| invoice.lifecycle_webhook_projection | Deterministic projection | Supported | Required target | all first-party |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 95-03-01: Add a Fake-first conformance harness for the staged supported slice</name>
  <files>accrue/test/accrue/processor/capabilities_test.exs, accrue/test/accrue/billing/subscription_actions_test.exs, accrue/test/accrue/webhook/default_handler_phase3_test.exs</files>
  <read_first>
accrue/test/accrue/processor/capabilities_test.exs
accrue/test/accrue/billing/subscription_actions_test.exs
accrue/test/accrue/webhook/default_handler_phase3_test.exs
.planning/processor-support-matrix.md
.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
  </read_first>
  <action>Extend the focused ExUnit suite so the staged supported slice is proven end-to-end in the deterministic Fake lane. Cover at least: capability truth for the supported rows, direct subscription creation through `Accrue.Billing.subscribe/3`, and webhook-backed lifecycle projection for subscription and invoice truth. Reuse existing focused test files where possible instead of creating a broad new framework. The resulting suite should make the matrix rows `subscription.lifecycle_webhook_projection`, `invoice.lifecycle_webhook_projection`, `webhook.verify`, and `webhook.parse` meaningfully testable in the merge-blocking lane.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/billing/subscription_actions_test.exs test/accrue/webhook/default_handler_phase3_test.exs</automated>
  </verify>
  <acceptance_criteria>
- `accrue/test/accrue/processor/capabilities_test.exs` asserts the staged supported rows directly.
- `accrue/test/accrue/billing/subscription_actions_test.exs` exercises the supported direct-subscription slice in Fake.
- `accrue/test/accrue/webhook/default_handler_phase3_test.exs` covers webhook-backed lifecycle projection relevant to the matrix rows.
- `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/billing/subscription_actions_test.exs test/accrue/webhook/default_handler_phase3_test.exs` exits 0.
  </acceptance_criteria>
  <done>The staged supported slice is now executable and merge-blocking in the deterministic Fake lane.</done>
</task>

<task type="auto">
  <name>Task 95-03-02: Align provider-smoke docs and support labels with the new conformance harness</name>
  <files>accrue/guides/testing.md, guides/testing-live-stripe.md, .planning/processor-support-matrix.md</files>
  <read_first>
accrue/guides/testing.md
guides/testing-live-stripe.md
.planning/processor-support-matrix.md
.planning/phases/95-official-processor-contract-conformance-harness/095-CONTEXT.md
.planning/phases/95-official-processor-contract-conformance-harness/095-RESEARCH.md
  </read_first>
  <action>Update the testing docs and processor support matrix together so the lane model is explicit after the conformance harness lands. `accrue/guides/testing.md` should say the staged processor contract is Fake-first and merge-blocking for the supported thin slice. `guides/testing-live-stripe.md` should keep the provider-backed lane narrow, advisory/protected-branch only, and tied to provider-parity checks instead of merge-blocking proof. If the matrix needs staged/proven wording changes after the tests land, update `.planning/processor-support-matrix.md` in the same task so docs, support labels, and test reality stay aligned.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh</automated>
  </verify>
  <acceptance_criteria>
- `accrue/guides/testing.md` explicitly describes Fake as the merge-blocking source of truth for the staged processor slice.
- `guides/testing-live-stripe.md` preserves the exact advisory/provider-parity posture for provider-backed smoke.
- `.planning/processor-support-matrix.md` remains aligned with the staged/proven status implied by the conformance tests.
- `bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh` exits 0.
  </acceptance_criteria>
  <done>The proof lanes, support labels, and documentation all describe the same bounded Phase 95 contract.</done>
</task>

</tasks>

<threat_model>
Execution blocking rule: Any `High` severity threat that leaves the staged supported slice unproven in Fake or that redefines provider smoke as the main proof model blocks execution until tests and docs are corrected.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged matrix rows → deterministic proof | If the rows are not proven in Fake, the official support contract stays aspirational. |
| deterministic proof → provider-smoke docs | If docs imply the provider lane is authoritative, external nondeterminism will distort future work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-05 | R | conformance harness and lane docs | High | mitigate | Prove the staged slice in focused Fake-backed tests and keep provider-smoke wording explicitly advisory/protected-branch only in the same PR. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/billing/subscription_actions_test.exs test/accrue/webhook/default_handler_phase3_test.exs`.
Run `bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh`.
</verification>

<success_criteria>
The official staged processor contract is both executable in Fake and described honestly in the testing docs and support matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/95-official-processor-contract-conformance-harness/095-03-SUMMARY.md`
</output>

---
phase: 96-chosen-second-provider-thin-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/mix.exs
  - accrue/lib/accrue/processor/braintree.ex
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/lib/accrue/billing/subscription_projection.ex
  - accrue/test/accrue/processor/braintree_test.exs
  - accrue/test/accrue/billing/subscription_actions_test.exs
  - accrue/test/accrue/billing/subscription_projection_provider_test.exs
  - accrue/test/accrue/billing/subscription_test.exs
autonomous: true
requirements:
  - PROC-12
must_haves:
  truths:
    - "A host can call `Accrue.Billing.subscribe/3` against Braintree without introducing a provider-specific alternate subscription API."
    - "The Braintree path accepts one narrow vault-acquisition handoff reference instead of raw browser tokenization fields."
    - "Stripe and Fake remain valid for the same `subscribe/3` facade after the shared seam changes."
  artifacts:
    - path: "accrue/lib/accrue/processor/braintree.ex"
      provides: "First-party Braintree adapter for the gateway subscription core slice"
    - path: "accrue/lib/accrue/billing/subscription_actions.ex"
      provides: "Processor-aware direct subscription request seam for the public facade"
    - path: "accrue/test/accrue/billing/subscription_test.exs"
      provides: "Shared facade regression coverage proving Stripe and Fake still pass after the seam changes"
  key_links:
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "Processor.__impl__().create_subscription/2"
      pattern: "create_subscription"
    - from: "accrue/test/accrue/billing/subscription_test.exs"
      to: "accrue/lib/accrue/billing/subscription_actions.ex"
      via: "shared public-facade regression assertions"
      pattern: "Billing.subscribe|SubscriptionActions.subscribe"
---

<objective>
Land the Braintree-backed `Accrue.Billing.subscribe/3` thin slice without widening the public facade.

Purpose: Implement `PROC-12` through the existing subscription seam per D-01..D-07, with one narrow handoff reference and explicit Stripe/Fake non-regression after the shared seam changes.
Output: A Braintree adapter, a processor-aware subscribe request seam, and focused plus shared regression tests that prove the public facade still works for Fake, Stripe, and the new Braintree slice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-CONTEXT.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-RESEARCH.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-PATTERNS.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md
@.planning/phases/95-official-processor-contract-conformance-harness/095-03-SUMMARY.md
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/processor/stripe.ex
@accrue/lib/accrue/processor/fake.ex
@accrue/lib/accrue/billing/subscription_projection.ex

<interfaces>
From `accrue/lib/accrue/billing.ex`:

```elixir
def subscribe(user, price_id_or_opts \\ [], opts \\ []) do
  span_billing(:subscription, :create, user, opts, fn ->
    SubscriptionActions.subscribe(user, price_id_or_opts, opts)
  end)
end
```

From `accrue/lib/accrue/billing/subscription_actions.ex`:

```elixir
defp build_subscription_request(%Customer{} = customer, item_params, trial_end, opts) do
  %{
    customer: customer.processor_id,
    items: [item_params],
    payment_behavior: "default_incomplete",
    expand: ["latest_invoice.payment_intent"]
  }
  |> put_if(:trial_end, trial_end)
  |> maybe_put_automatic_tax(opts)
  |> maybe_put_default_pm(opts)
  |> maybe_put_coupon(opts)
  |> maybe_put_collection_method(opts)
end
```

From `accrue/lib/accrue/processor/capabilities.ex`:

```elixir
@support_labels %{
  payment_method: %{vault_acquisition: "all first-party", list: "out of slice"},
  subscription: %{direct_create: "all first-party", fetch: "all first-party"}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Braintree first-party adapter and capability truth</name>
  <files>accrue/mix.exs, accrue/lib/accrue/processor/braintree.ex, accrue/test/accrue/processor/braintree_test.exs</files>
  <behavior>
    - Test 1: `create_subscription/2` maps the public `price_id` value to Braintree `plan_id` and accepts only `payment_method.vault_acquisition.reference`, where `reference` is a vaulted Braintree `payment_method_token`, per D-02 and D-04.
    - Test 2: `capabilities/0` exposes only the supported gateway-subscription-core callbacks and no payment-method CRUD parity per D-03.
  </behavior>
  <action>Update `accrue/mix.exs` to add the Braintree client dependency needed for SDK-backed subscription and webhook work. Create `Accrue.Processor.Braintree` following the `Accrue.Processor.Stripe` structure, but implement only the gateway-subscription-core callbacks required by D-03 and `PROC-12`: `processor_name/0`, `capabilities/0`, `create_subscription/2`, `retrieve_subscription/2`, `fetch/2`, and any private translation helpers. Use the exact public handoff shape `payment_method: %{vault_acquisition: %{reference: reference}}` per D-02, and define `reference` concretely as a vaulted Braintree `payment_method_token` produced by the host before `subscribe/3` is called. Map that `reference` directly to Braintree `payment_method_token`, and map the public `price_id` string directly to Braintree `plan_id` per D-04/D-05 and research assumption A1. Do not add adapter-side nonce-to-token conversion, payment-method list/create/update facade support, raw `payment_method_nonce`, `client_token`, or `device_data` arguments.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/braintree_test.exs</automated>
  </verify>
  <done>The repo has a first-party `Accrue.Processor.Braintree` module with explicit capabilities for `payment_method.vault_acquisition`, `subscription.direct_create`, `subscription.fetch`, `subscription.lifecycle_webhook_projection`, `invoice.lifecycle_webhook_projection`, `webhook.verify`, and `webhook.parse`, and the focused adapter tests prove the request/response translation using a vaulted-token `vault_acquisition.reference` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep `subscribe/3` public while branching only at the processor seam</name>
  <files>accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/billing/subscription_projection.ex, accrue/test/accrue/billing/subscription_actions_test.exs, accrue/test/accrue/billing/subscription_projection_provider_test.exs</files>
  <behavior>
    - Test 1: when the configured processor is Braintree, `subscribe/3` accepts only `payment_method.vault_acquisition.reference`, where `reference` is a vaulted Braintree `payment_method_token`, and returns `invalid_request_error` for malformed handoff input per D-02 and D-05.
    - Test 2: subscription projection branches explicitly for `:braintree` instead of pretending Stripe and Braintree payloads are identical per D-07.
  </behavior>
  <action>Update `SubscriptionActions` so `Accrue.Billing.subscribe/3` stays the only public subscription creation entrypoint per D-04, with Braintree branching isolated to the request seam rather than exposed in `Accrue.Billing`. Validate that when the configured processor is Braintree, the only supported payment-method input is `payment_method: %{vault_acquisition: %{reference: binary}}`, where that binary is a vaulted Braintree `payment_method_token` created by the host before the facade call. Return a typed `Accrue.APIError` with code `invalid_request_error` when the handoff is missing or malformed, and preserve the existing `processor_operation_unsupported` behavior for out-of-slice operations. Extend `SubscriptionProjection.decompose/2` with a `:braintree` branch so the translated subscription persists through the existing local projection path without pretending Stripe and Braintree payloads are identical per D-07.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_actions_test.exs test/accrue/billing/subscription_projection_provider_test.exs test/accrue/processor/braintree_test.exs</automated>
  </verify>
  <done>`Accrue.Billing.subscribe/3` remains the public API, Braintree-specific request handling is confined to the internal seam, and focused tests prove the narrow vault-acquisition reference plus `price_id` -> `plan_id` mapping without widening the facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-prove shared Stripe and Fake subscription behavior after the seam changes</name>
  <files>accrue/test/accrue/billing/subscription_test.exs</files>
  <behavior>
    - Test 1: the shared `Accrue.Billing.subscribe/3` facade still passes its existing Stripe/Fake expectations after the Braintree seam is introduced.
    - Test 2: the Phase 95 Fake-first conformance lane remains green after the `subscribe/3` seam changes required by D-04 and `PROC-12`.
  </behavior>
  <action>Strengthen `accrue/test/accrue/billing/subscription_test.exs` so the shared public subscription facade remains explicitly pinned for non-Braintree processors after the new seam branches land. This task implements the `PROC-12` non-regression half directly: rerun the shared subscription regression file plus the Phase 95 Fake-first conformance command from `095-03-SUMMARY.md` so Stripe/Fake still pass after the `subscribe/3` seam changes. Do not defer this proof to a later docs or closeout plan.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_test.exs test/accrue/billing/subscription_actions_test.exs test/accrue/billing/subscription_projection_provider_test.exs test/accrue/processor/braintree_test.exs && mix test test/accrue/billing/subscription_test.exs test/accrue/billing/payment_method_actions_test.exs test/accrue/processor/capabilities_test.exs test/accrue/webhook/default_handler_phase3_test.exs test/accrue/checkout/session_test.exs --max-cases 1</automated>
  </verify>
  <done>The shared facade regression file and the Phase 95 Fake-first conformance bundle both pass after the Braintree seam work, providing explicit Stripe/Fake non-regression evidence for `PROC-12`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host server -> `Accrue.Billing.subscribe/3` | Untrusted host-supplied payment-method handoff crosses into the billing facade |
| `SubscriptionActions` -> `Accrue.Processor.Braintree` | Internal request normalization must not leak unsupported provider fields |
| shared facade changes -> Stripe/Fake processors | A new Braintree seam can regress existing first-party processors if the shared contract drifts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | T | `subscription_actions.ex` | mitigate | Validate only `payment_method.vault_acquisition.reference` for Braintree and reject raw provider/browser fields with `invalid_request_error`. |
| T-96-02 | I | `processor/braintree.ex` | mitigate | Keep provider-specific request assembly and response translation inside the adapter; do not persist raw browser tokenization payloads or credentials. |
| T-96-03 | E | public `subscribe/3` seam | mitigate | Preserve capability gates and keep payment-method CRUD surfaces out of slice per D-03 so the new path cannot silently unlock unsupported operations. |
| T-96-13 | T | shared Stripe/Fake facade behavior | mitigate | Re-run the shared subscription regression file and the Phase 95 Fake-first conformance bundle after the seam changes so `PROC-12` includes explicit non-regression proof. |
</threat_model>

<verification>
Run the `96-VALIDATION.md` smoke command after each task. After Task 3, run the dedicated shared Stripe/Fake regression command and record the exact command in `96-01-SUMMARY.md`.
</verification>

<success_criteria>
The phase has a real Braintree-backed direct-subscription contract through `Accrue.Billing.subscribe/3`, expressed with one narrow vault-acquisition reference, while the shared Stripe/Fake facade proof remains explicitly green after the seam changes.
</success_criteria>

<output>
After completion, create `.planning/phases/96-chosen-second-provider-thin-slice/96-01-SUMMARY.md`
</output>

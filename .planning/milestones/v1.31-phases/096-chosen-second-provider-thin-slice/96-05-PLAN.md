---
phase: 96-chosen-second-provider-thin-slice
plan: 05
type: execute
wave: 5
depends_on:
  - 96-03
  - 96-04
files_modified:
  - examples/accrue_host/README.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_processor_support_matrix.sh
  - scripts/ci/verify_adoption_proof_matrix.sh
  - .planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md
autonomous: true
requirements:
  - PROC-12
  - PROC-13
must_haves:
  truths:
    - "Example-host docs mirror the canonical bounded support story without becoming a second product promise."
    - "Verifier scripts pin the new Braintree wording and the Fake-first/advisory proof-lane distinction."
    - "Phase validation includes a sub-30-second smoke command, the shared Stripe/Fake regression rerun, and the required credentialed Braintree proof command."
  artifacts:
    - path: "examples/accrue_host/README.md"
      provides: "Canonical adopter-facing host proof narrative"
    - path: "scripts/ci/verify_adoption_proof_matrix.sh"
      provides: "Shift-left enforcement for the host proof-lane wording"
    - path: ".planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md"
      provides: "Nyquist-compliant validation contract with smoke, regression, and credentialed proof commands"
  key_links:
    - from: "examples/accrue_host/docs/adoption-proof-matrix.md"
      to: "scripts/ci/verify_adoption_proof_matrix.sh"
      via: "proof-lane wording verification"
      pattern: "Fake-backed|advisory|Braintree|gateway subscription core"
    - from: ".planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md"
      to: ".planning/phases/96-chosen-second-provider-thin-slice/96-01-PLAN.md"
      via: "shared Stripe/Fake regression command"
      pattern: "subscription_test.exs|payment_method_actions_test.exs|capabilities_test.exs"
---

<objective>
Mirror the bounded support story into the example-host docs and tighten the phase verification contract.

Purpose: Close the remaining `PROC-13` doc mirrors and the validation/verifier maintenance required by the checker so Phase 96 cannot drift away from its bounded support and proof posture.
Output: Example-host docs, bash verifiers, and a Nyquist-compliant `96-VALIDATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/processor-support-matrix.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-CONTEXT.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-01-PLAN.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-03-PLAN.md
@examples/accrue_host/README.md
@examples/accrue_host/docs/adoption-proof-matrix.md
@scripts/ci/verify_processor_support_matrix.sh
@scripts/ci/verify_adoption_proof_matrix.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update example-host docs to mirror the bounded support story</name>
  <files>examples/accrue_host/README.md, examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <action>Update the example-host README and adoption-proof matrix so they mirror the canonical matrix truth from Plan 04 rather than inventing a broader promise. The wording must say Stripe remains the default first-user path, Braintree is official only for the `gateway subscription core` slice, checkout and billing portal remain Stripe-only, and the Braintree proof lane in `examples/accrue_host` is advisory while Fake remains the merge-blocking SSOT. Do not imply a second full proof lane in installer output or generic “Braintree support” beyond the named slice per D-08..D-13.</action>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The example-host docs mirror the bounded support story exactly and remain subordinate to the canonical matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the verifier needles and validation contract</name>
  <files>scripts/ci/verify_processor_support_matrix.sh, scripts/ci/verify_adoption_proof_matrix.sh, .planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md</files>
  <action>Update the two bash verifiers so they pin the new Phase 96 wording rather than older Stripe/Fake-only needles. Then refresh `96-VALIDATION.md` so it is Nyquist-compliant and faster to sample: add a dedicated sub-30-second smoke command for frequent checks, keep a broader quick command, add a separate shared Stripe/Fake regression command that reruns `test/accrue/billing/subscription_test.exs` plus the Phase 95 conformance bundle after the `subscribe/3` seam changes, and retain the credentialed Braintree host proof command. The validation table must include task rows for `96-01-03`, `96-03-03`, `96-04-*`, and this plan, and it must explicitly state that live-provider proof evidence is captured only after Plan 02 completes.</action>
  <verify>
    <automated>bash scripts/ci/verify_processor_support_matrix.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The bash verifiers enforce the implemented Phase 96 support story, and `96-VALIDATION.md` contains a sub-30-second smoke loop plus explicit regression and credentialed-proof commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example-host docs -> adopters | Host-facing docs translate internal support truth into external expectations |
| docs -> CI verifiers | Text claims become merge-visible through bash verification scripts |
| validation contract -> execution summaries | Missing or slow feedback loops can hide regressions or false completion claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-14 | T | example-host docs | mitigate | Mirror the bounded support story from the canonical matrix and keep host docs subordinate to it. |
| T-96-15 | R | verification scripts | mitigate | Pin new support-language needles in the bash verifiers so future copy drift is caught automatically. |
| T-96-16 | D | validation loop | mitigate | Provide a sub-30-second smoke command plus separate quick/full/regression commands so executors can sample often without skipping verification. |
</threat_model>

<verification>
Run all three docs/contract scripts together and confirm `96-VALIDATION.md` reflects the real plan/task inventory, including the shared Stripe/Fake regression command and the credentialed Braintree proof command.
</verification>

<success_criteria>
Accrue’s example-host narrative, verifier scripts, and phase validation all enforce the same bounded truth: Stripe-first, official Braintree gateway-subscription-core slice, Fake-first merge blocking, advisory provider-backed proof, and explicit non-regression plus credentialed-proof commands.
</success_criteria>

<output>
After completion, create `.planning/phases/96-chosen-second-provider-thin-slice/96-05-SUMMARY.md`
</output>

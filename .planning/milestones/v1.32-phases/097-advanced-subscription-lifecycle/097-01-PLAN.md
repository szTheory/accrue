---
phase: 097-advanced-subscription-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor/braintree.ex
  - accrue/lib/accrue/processor/capabilities.ex
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/lib/accrue/billing/subscription_projection.ex
  - accrue/test/accrue/processor/braintree_test.exs
  - accrue/test/accrue/processor/capabilities_test.exs
  - accrue/test/accrue/billing/subscription_actions_test.exs
  - accrue/test/accrue/billing/subscription_projection_provider_test.exs
autonomous: true
requirements:
  - PROC-14
must_haves:
  truths:
    - "Braintree exposes honest mutation callbacks for the existing `Accrue.Billing` lifecycle API instead of leaving mutation support out of slice."
    - "The public facade for `swap_plan/3`, `update_quantity/3`, `pause/2`, `unpause/2`, and `resume/2` remains unchanged."
    - "Quantity and pause/resume semantics are implemented or rejected explicitly with typed errors rather than hidden behind Stripe-shaped assumptions."
  artifacts:
    - path: "accrue/lib/accrue/processor/braintree.ex"
      provides: "Braintree mutation callback implementation and capability truth"
    - path: "accrue/lib/accrue/billing/subscription_actions.ex"
      provides: "Public lifecycle semantics wired to honest Braintree behavior"
    - path: "accrue/test/accrue/billing/subscription_actions_test.exs"
      provides: "Mutation contract coverage for Braintree plus shared regressions"
  key_links:
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "mutation callbacks"
      pattern: "update_subscription|cancel_subscription|resume_subscription|pause_subscription_collection"
---

<objective>
Implement the Braintree mutation contract behind the existing public billing facade.

Purpose: satisfy the command-side half of `PROC-14` by extending the Braintree adapter and the existing billing action seams instead of introducing a new API surface.
Output: mutation callbacks, capability truth, semantic guards, and focused regression coverage for the existing lifecycle facade.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.32-ROADMAP.md
@.planning/milestones/v1.32-REQUIREMENTS.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-RESEARCH.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-PATTERNS.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-VALIDATION.md
@accrue/lib/accrue/processor/braintree.ex
@accrue/lib/accrue/processor/stripe.ex
@accrue/lib/accrue/processor/fake.ex
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/billing/subscription_projection.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add executable Braintree mutation capabilities and callbacks</name>
  <read_first>
    - accrue/lib/accrue/processor/braintree.ex
    - accrue/lib/accrue/processor/stripe.ex
    - accrue/lib/accrue/processor/fake.ex
    - accrue/lib/accrue/processor/capabilities.ex
    - accrue/test/accrue/processor/braintree_test.exs
    - accrue/test/accrue/processor/capabilities_test.exs
  </read_first>
  <files>accrue/lib/accrue/processor/braintree.ex, accrue/lib/accrue/processor/capabilities.ex, accrue/test/accrue/processor/braintree_test.exs, accrue/test/accrue/processor/capabilities_test.exs</files>
  <action>Extend `Accrue.Processor.Braintree` to implement the mutation callbacks required by Phase 97: `update_subscription/3`, `cancel_subscription/2`, `cancel_subscription/3`, `resume_subscription/2`, and `pause_subscription_collection/4`, plus any private translation helpers they need. Update `capabilities/0` and any support-label mirrors so Braintree mutation support is executable truth instead of an implicit assumption. Keep all provider parameter translation inside `braintree.ex`. If a given facade semantic cannot be represented directly for Braintree, return a typed `%Accrue.APIError{code: "processor_operation_unsupported"}` or `%Accrue.APIError{code: "invalid_request_error"}` with a concrete message naming the unsupported semantic; do not silently no-op.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/braintree.ex` contains `def update_subscription(`.
    - `accrue/lib/accrue/processor/braintree.ex` contains `def cancel_subscription(` in both arities.
    - `accrue/lib/accrue/processor/braintree.ex` contains `def resume_subscription(`.
    - `accrue/lib/accrue/processor/braintree.ex` contains `def pause_subscription_collection(`.
    - `accrue/test/accrue/processor/braintree_test.exs` contains assertions covering `update_subscription`, `cancel_subscription`, `resume_subscription`, and `pause_subscription_collection`.
    - `cd accrue && mix test test/accrue/processor/braintree_test.exs test/accrue/processor/capabilities_test.exs` exits `0`.
  </acceptance_criteria>
  <done>The Braintree adapter exposes concrete lifecycle mutation callbacks, capability truth matches those callbacks, and focused adapter/capability tests explicitly cover update, cancel, pause, and resume behavior.</done>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/braintree_test.exs test/accrue/processor/capabilities_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire the existing billing lifecycle actions to honest Braintree semantics</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription_actions.ex
    - accrue/lib/accrue/billing/subscription_projection.ex
    - accrue/lib/accrue/processor/braintree.ex
    - accrue/test/accrue/billing/subscription_actions_test.exs
    - accrue/test/accrue/billing/subscription_projection_provider_test.exs
  </read_first>
  <files>accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/billing/subscription_projection.ex, accrue/test/accrue/billing/subscription_actions_test.exs, accrue/test/accrue/billing/subscription_projection_provider_test.exs</files>
  <action>Update `SubscriptionActions` so the existing public lifecycle APIs stay intact while routing Braintree mutations through explicit request-shaping and semantic guards. Cover `swap_plan/3`, `update_quantity/3`, `pause/2`, `unpause/2`, and `resume/2`. Preserve the current transactional write, projection, and event-recording pattern. If quantity or pause/resume requires a bounded Braintree interpretation, encode that interpretation concretely in the action path and tests; if a specific edge case is unsupported, reject it with a typed error that names the reason. Extend `SubscriptionProjection` so local rows persist any additional Braintree lifecycle fields Phase 97 needs for honest mutation state.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription_actions.ex` contains Braintree-specific branching for `swap_plan`, `update_quantity`, `pause`, `unpause`, and `resume`.
    - `accrue/test/accrue/billing/subscription_actions_test.exs` contains explicit Braintree assertions for `swap_plan`, `update_quantity`, `pause`, `unpause`, and `resume`, or typed-error assertions for any bounded unsupported edge cases.
    - `accrue/test/accrue/billing/subscription_projection_provider_test.exs` contains Braintree assertions for plan-change state, quantity state, and pause/cancel/resume-related projection state.
    - `cd accrue && mix test test/accrue/billing/subscription_actions_test.exs test/accrue/billing/subscription_projection_provider_test.exs` exits `0`.
  </acceptance_criteria>
  <done>The existing billing lifecycle API remains intact, every PROC-14 mutation family has explicit Braintree semantics or typed-error coverage, and local projection tests pin the resulting state shape.</done>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_actions_test.exs test/accrue/billing/subscription_projection_provider_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Re-prove shared lifecycle regressions after the mutation seam changes</name>
  <read_first>
    - accrue/test/accrue/billing/subscription_actions_test.exs
    - accrue/test/accrue/processor/capabilities_test.exs
    - accrue/test/accrue/processor/fake_phase3_test.exs
    - accrue/lib/accrue/processor/fake.ex
  </read_first>
  <files>accrue/test/accrue/billing/subscription_actions_test.exs, accrue/test/accrue/processor/capabilities_test.exs</files>
  <action>Strengthen or extend the existing shared regression coverage so Phase 97 proves it did not regress non-Braintree lifecycle behavior while making the mutation seam more provider-aware. Keep the scope narrow: use the existing shared billing/capability test homes rather than inventing a new suite.</action>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/processor/braintree_test.exs test/accrue/processor/capabilities_test.exs test/accrue/billing/subscription_actions_test.exs test/accrue/billing/subscription_projection_provider_test.exs` exits `0`.
    - No new public billing function names are added to `accrue/lib/accrue/billing.ex` for Braintree-specific lifecycle mutations.
  </acceptance_criteria>
  <done>The focused adapter, capability, billing-action, and projection suites all pass together, proving the mutation seam changed without widening the public billing API.</done>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/braintree_test.exs test/accrue/processor/capabilities_test.exs test/accrue/billing/subscription_actions_test.exs test/accrue/billing/subscription_projection_provider_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Accrue.Billing` facade -> Braintree adapter | Shared lifecycle semantics cross into provider-specific mutation behavior |
| Braintree callback input -> local projection | Provider payload differences can create false parity or corrupted local state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | T | `processor/braintree.ex` | mitigate | Keep provider request translation isolated and explicit for every supported mutation callback. |
| T-97-02 | E | capability/support truth | mitigate | Update executable capabilities and tests together so Braintree support is not over-claimed. |
| T-97-03 | T | `subscription_actions.ex` | mitigate | Reject unsupported quantity/pause/resume edge cases with typed errors instead of silent partial behavior. |
</threat_model>

<verification>
Run the quick command from `097-VALIDATION.md` after each task.
</verification>

<success_criteria>
The existing billing lifecycle facade now has honest Braintree mutation support with explicit semantic guards and green focused regressions.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-01-SUMMARY.md`
</output>

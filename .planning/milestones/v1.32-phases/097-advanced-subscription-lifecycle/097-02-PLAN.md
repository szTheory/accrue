---
phase: 097-advanced-subscription-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 097-01
files_modified:
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/lib/accrue/billing/subscription_projection.ex
  - accrue/lib/accrue/billing/invoice_projection.ex
  - accrue/test/accrue/webhook/default_handler_test.exs
  - accrue/test/accrue/webhook/default_handler_phase3_test.exs
  - accrue/test/accrue/billing/subscription_projection_provider_test.exs
autonomous: true
requirements:
  - PROC-15
  - PROC-14
must_haves:
  truths:
    - "Braintree lifecycle mutations converge local subscription state through the shared webhook reducer path."
    - "Mutation-related Braintree webhook normalization uses canonical refetch instead of trusting payload snapshots."
    - "Shared reducer changes are re-proven against existing tests instead of assumed safe."
  artifacts:
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "Braintree lifecycle normalization for mutation convergence"
    - path: "accrue/lib/accrue/billing/subscription_projection.ex"
      provides: "Local subscription-state persistence for converged Braintree lifecycle fields"
    - path: "accrue/test/accrue/webhook/default_handler_test.exs"
      provides: "Focused Braintree webhook mutation regression coverage"
---

<objective>
Extend Braintree lifecycle convergence through the existing shared webhook path.

Purpose: satisfy `PROC-15` and the convergence half of `PROC-14` by normalizing additional Braintree lifecycle signals, refetching canonical state, and persisting converged subscription state locally.
Output: reducer/projection hardening and shared webhook regressions for mutation-driven lifecycle state.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-RESEARCH.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-PATTERNS.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-VALIDATION.md
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-01-PLAN.md
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/billing/subscription_projection.ex
@accrue/lib/accrue/billing/invoice_projection.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Braintree lifecycle normalization for mutation-related webhook events</name>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex
    - accrue/lib/accrue/processor/braintree.ex
    - accrue/test/accrue/webhook/default_handler_test.exs
    - accrue/test/accrue/webhook/default_handler_phase3_test.exs
  </read_first>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/webhook/default_handler_test.exs, accrue/test/accrue/webhook/default_handler_phase3_test.exs</files>
  <action>Extend the Braintree normalization path in `Accrue.Webhook.DefaultHandler` so Phase 97 mutation flows converge through the existing reducer pipeline. Add or refine normalization for the Braintree lifecycle notifications needed to reflect plan swaps, quantity changes, pauses/resumes, cancellations, and related billing-state transitions. Keep the canonical refetch pattern via `Processor.fetch/2`; do not persist webhook payload snapshots directly. Re-run focused reducer tests together with the shared reducer regression file after the normalization changes land.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/webhook/default_handler.ex` contains explicit Braintree lifecycle handling for plan-change, quantity-impact, cancellation/resumption, and pause-related convergence paths, or explicit typed-error/doc handoff for any unsupported semantic.
    - `accrue/test/accrue/webhook/default_handler_test.exs` contains Braintree lifecycle assertions covering plan-change, quantity-impact, cancellation/resumption, and pause-related convergence behavior.
    - `cd accrue && mix test test/accrue/webhook/default_handler_test.exs test/accrue/webhook/default_handler_phase3_test.exs --max-cases 1` exits `0`.
  </acceptance_criteria>
  <done>Braintree mutation-related lifecycle events normalize through the shared reducer path with explicit coverage for every PROC-15 mutation family or typed-error/documented fallback where provider semantics are bounded.</done>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_test.exs test/accrue/webhook/default_handler_phase3_test.exs --max-cases 1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Persist converged Braintree mutation state in local projections</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription_projection.ex
    - accrue/lib/accrue/billing/invoice_projection.ex
    - accrue/test/accrue/billing/subscription_projection_provider_test.exs
    - accrue/lib/accrue/webhook/default_handler.ex
  </read_first>
  <files>accrue/lib/accrue/billing/subscription_projection.ex, accrue/lib/accrue/billing/invoice_projection.ex, accrue/test/accrue/billing/subscription_projection_provider_test.exs</files>
  <action>Update the Braintree projection branches so local billing rows capture the mutation state Phase 97 depends on after canonical refetch. This includes whatever additional subscription or invoice fields are necessary for honest lifecycle persistence after swaps, quantity changes, pause/resume transitions, and cancellation/resumption flows. Keep all provider-specific field parsing in the projection modules rather than distributing it across reducers and billing actions.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription_projection.ex` contains Braintree parsing for plan-change state, quantity-related state, and pause/cancel/resume-related state required by Phase 97.
    - `accrue/test/accrue/billing/subscription_projection_provider_test.exs` contains assertions for converged Braintree mutation state across plan swap, quantity, and pause/cancel/resume cases, or typed-error/doc handoff for any bounded unsupported semantic.
    - `cd accrue && mix test test/accrue/billing/subscription_projection_provider_test.exs test/accrue/webhook/default_handler_test.exs test/accrue/webhook/default_handler_phase3_test.exs --max-cases 1` exits `0`.
  </acceptance_criteria>
  <done>Local subscription/invoice projections persist the converged Braintree mutation state Phase 97 promises, and shared webhook regression suites stay green after the projection changes.</done>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_projection_provider_test.exs test/accrue/webhook/default_handler_test.exs test/accrue/webhook/default_handler_phase3_test.exs --max-cases 1</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external webhook event -> shared reducer path | Untrusted lifecycle input crosses into local billing persistence |
| canonical provider object -> local rows | Mutation semantics can drift if projection fields are incomplete or misleading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04 | S | webhook normalization | mitigate | Normalize only through explicit Braintree lifecycle mappings and canonical refetch. |
| T-97-05 | T | projection persistence | mitigate | Persist mutation state only after processor refetch and projection-level parsing; keep shared regression files green. |
</threat_model>

<verification>
Run the full webhook-focused command from `097-VALIDATION.md` after each task.
</verification>

<success_criteria>
Braintree lifecycle mutations now converge local subscription state through the existing reducer path with explicit normalization and projection proof.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-02-SUMMARY.md`
</output>

---
phase: 098-payment-method-crud-operator-admin
plan: 02
type: execute
wave: 2
depends_on:
  - 098-01
files_modified:
  - accrue_admin/lib/accrue_admin/live/customer_live.ex
  - accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex
  - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
  - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/test/accrue_host/braintree_payment_method_flow_test.exs
  - examples/accrue_host/e2e/generated/copy_strings.json
autonomous: true
requirements:
  - PROC-16
  - PROC-17
must_haves:
  truths:
    - "Operators can review local payment-method inventory, set a default, trigger explicit sync, and see guarded delete states without any embedded Braintree card capture in admin (per D-12 through D-18)."
    - "Host-facing add and replace flows stay host-owned and call the canonical Billing CRUD verbs through the narrow vault-reference seam (per D-15, D-16 and resolved research decision 2)."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/customer_live.ex"
      provides: "Server-driven payment-method inventory, default, sync, and guarded delete UI"
      contains: "handle_event(\"set_default_payment_method\""
    - path: "examples/accrue_host/lib/accrue_host/billing.ex"
      provides: "Host-owned add/replace helpers that pass vault acquisition references into canonical Billing verbs"
      contains: "vault_reference"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/customer_live.ex"
      to: "accrue/lib/accrue/billing.ex"
      via: "server-driven events"
      pattern: "Billing\\.(list_payment_methods|set_default_payment_method|delete_payment_method|sync_payment_methods)"
    - from: "examples/accrue_host/lib/accrue_host/billing.ex"
      to: "accrue/lib/accrue/billing.ex"
      via: "host-owned vault handoff"
      pattern: "Billing\\.(add_payment_method|update_payment_method)"
---

<objective>
Deliver the operator-facing and host-facing Phase 98 implementation slice on top of the new backend contract: honest admin controls, host-owned add/replace seam proofs, and copy-export-backed UI behavior.

Purpose: Close the product-facing implementation work for PROC-17 while keeping the doc/browser-proof lane in a separate follow-on plan so this plan stays within the file budget.
Output: Updated AccrueAdmin UI/copy/tests, host helper/tests, and generated copy export aligned to the new operator and host-owned payment-method flows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.32-ROADMAP.md
@.planning/milestones/v1.32-REQUIREMENTS.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-CONTEXT.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-RESEARCH.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-PATTERNS.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-UI-SPEC.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md

<interfaces>
From Plan `098-01` outputs:
```elixir
Accrue.Billing.add_payment_method(customer, attrs, opts \\ [])
Accrue.Billing.update_payment_method(payment_method, attrs, opts \\ [])
Accrue.Billing.delete_payment_method(payment_method, opts \\ [])
Accrue.Billing.set_default_payment_method(customer, pm_id, opts \\ [])
Accrue.Billing.list_payment_methods(customer, opts \\ [])
Accrue.Billing.sync_payment_methods(customer, opts \\ [])
```

From `accrue_admin/lib/accrue_admin/live/customer_live.ex`:
```elixir
@tabs ~w(subscriptions invoices charges payment_methods events metadata)
```

From `examples/accrue_host/lib/accrue_host/billing.ex`:
```elixir
opts = Keyword.put(opts, :payment_method, %{vault_acquisition: %{reference: vault_reference}})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the admin operator surface and copy/export-backed proof</name>
  <files>
    accrue_admin/lib/accrue_admin/live/customer_live.ex
    accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex
    accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
    accrue_admin/test/accrue_admin/live/customer_live_test.exs
    examples/accrue_host/e2e/generated/copy_strings.json
  </files>
  <behavior>
    - Test 1: the `payment_methods` tab renders local projection inventory and exposes only server-driven `set default`, `sync`, and guarded delete actions (per D-12, D-13, D-14, D-28).
    - Test 2: visible operator strings flow through `AccrueAdmin.Copy` and the export pipeline rather than duplicate JS literals (Phase 77 carry-forward).
  </behavior>
  <action>
    Update `accrue_admin/lib/accrue_admin/live/customer_live.ex` using the concrete analogs mapped in `098-PATTERNS.md`: keep the existing mount/assign seam, payment-method query helper shape, and server-driven event posture from `customer_live.ex`, then add `set default`, `sync`, and guarded delete handlers without embedding Braintree JS, Drop-in, or raw card-entry fields per D-12 through D-17. Expand `accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex` with the mapped copy-module shape and update the export allowlist plus generated copy JSON so all visible strings stay copy-backed. Extend `accrue_admin/test/accrue_admin/live/customer_live_test.exs` with the mapped render-assertion style and destructive-flow proof style referenced in `098-PATTERNS.md`.
  </action>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.export_copy_strings --out ../examples/accrue_host/e2e/generated/copy_strings.json && mix test test/accrue_admin/live/customer_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>
    PROC-17 UI behavior is true at the server/test level: admin exposes only honest operator actions and exported copy matches the visible surface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the host-owned add/replace seam proof against the canonical CRUD facade</name>
  <files>
    examples/accrue_host/lib/accrue_host/billing.ex
    examples/accrue_host/test/accrue_host/braintree_payment_method_flow_test.exs
  </files>
  <behavior>
    - Test 1: add/replace messaging and helpers stay host-owned and use the canonical `add_payment_method/3` and replacement-oriented `update_payment_method/3` seam rather than admin-side card capture (per D-15, D-16, D-17 and resolved research decision 2).
    - Test 2: the host proof preserves the narrow `vault_acquisition.reference` handoff and does not leak provider-specific browser capture into `accrue_admin`.
  </behavior>
  <action>
    In `examples/accrue_host/lib/accrue_host/billing.ex`, follow the exact host policy wrapper pattern mapped in `098-PATTERNS.md` so authorization and scope handling stay in the host layer and only the narrow `vault_acquisition.reference` payload crosses into the canonical Billing CRUD verbs. In `examples/accrue_host/test/accrue_host/braintree_payment_method_flow_test.exs`, use the mapped inline adapter-mock and host-proof assertion style from `braintree_subscribe_test.exs` to prove add and replacement flows call `add_payment_method/3` / `update_payment_method/3` without reintroducing admin-side payment capture.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/braintree_payment_method_flow_test.exs</automated>
  </verify>
  <done>
    Host-assisted add and replacement proof is true on the branch: the browser seam remains host-owned, the canonical CRUD facade is exercised directly, and admin still owns no raw payment capture.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccrueAdmin LiveView -> Billing facade | Operator-triggered mutations must stay authorized, server-driven, and auditable. |
| admin UI -> host billing seam | Add/replace flows must hand off to the host without embedding raw payment capture in admin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | E | `AccrueAdmin.Live.CustomerLive` payment-method tab | mitigate | Keep all mutations server-driven, expose no Braintree JS or raw card fields, and scope actions to the existing admin auth boundary. |
| T-98-05 | R | Operator delete/default/sync actions | mitigate | Use LiveView event handlers backed by Billing commands so operator intent remains attributable and copy explains destructive boundaries. |
</threat_model>

<verification>
Per-task:

`cd accrue_admin && mix accrue_admin.export_copy_strings --out ../examples/accrue_host/e2e/generated/copy_strings.json && mix test test/accrue_admin/live/customer_live_test.exs --warnings-as-errors`

`cd examples/accrue_host && mix test test/accrue_host/braintree_payment_method_flow_test.exs`

Phase gate only:

`bash scripts/ci/verify_processor_support_matrix.sh && cd accrue && mix test --warnings-as-errors && cd ../accrue_admin && mix test --warnings-as-errors && cd ../examples/accrue_host && mix test test/accrue_host/braintree_payment_method_flow_test.exs && npm ci && npm run e2e:install && npm run e2e:a11y`
</verification>

<success_criteria>
- `AccrueAdmin` truthfully exposes payment-method inventory, explicit default, sync, and guarded delete actions without embedded capture.
- Host helpers and tests prove add/replace flows still cross only the narrow vault-reference seam.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-02-SUMMARY.md`
</output>

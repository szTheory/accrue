---
phase: 098-payment-method-crud-operator-admin
plan: 03
type: execute
wave: 3
depends_on:
  - 098-02
files_modified:
  - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  - examples/accrue_host/docs/verify01-v112-admin-paths.md
  - examples/accrue_host/README.md
  - .planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md
autonomous: true
requirements:
  - PROC-17
must_haves:
  truths:
    - "The mounted admin payment-method route is verified end to end with the same truthful operator actions and host-owned add/replace boundary implemented in Phase 98, so browser proof cannot drift from the shipped PROC-17 behavior."
    - "Host-facing docs and validation artifacts describe the same operator route, sync/default/delete semantics, and host-owned add/replace seam that users encounter in the product."
  artifacts:
    - path: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      provides: "Phase-gate browser proof for the payment-method operator route"
      contains: "payment_methods"
    - path: ".planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md"
      provides: "Plan-aware validation map and deterministic Playwright bootstrap"
      contains: "098-03-01"
  key_links:
    - from: ".planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md"
      to: "examples/accrue_host/package.json"
      via: "phase-gate browser bootstrap"
      pattern: "npm ci && npm run e2e:install && npm run e2e:a11y"
    - from: "examples/accrue_host/docs/verify01-v112-admin-paths.md"
      to: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      via: "route and proof-lane documentation"
      pattern: "payment method|Sync payment methods|Replace payment method"
---

<objective>
Deliver the Phase 98 proof-lane and documentation slice: browser-spec alignment, host-facing verification docs, and the deterministic phase-gate bootstrap contract.

Purpose: Resolve the plan 02 scope warning by moving docs/browser-proof work into a separate follow-on plan while preserving the deterministic Playwright bootstrap decision.
Output: Updated host browser spec, host docs/README guidance, and a validation map that treats Playwright as a phase-gate-only check.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.32-ROADMAP.md
@.planning/milestones/v1.32-REQUIREMENTS.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-CONTEXT.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-RESEARCH.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-PATTERNS.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-UI-SPEC.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md

<interfaces>
From `098-02` outputs:
```text
AccrueAdmin payment-method tab exposes truthful server-driven actions only.
Host add/replace flows stay outside admin and use the canonical Billing CRUD seam.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update browser proof docs and validation so Playwright runs only at the deterministic phase gate</name>
  <files>
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    examples/accrue_host/docs/verify01-v112-admin-paths.md
    examples/accrue_host/README.md
    .planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md
  </files>
  <action>
    Refresh the host browser spec and docs so they describe the truthful payment-method operator route, but keep Playwright out of per-task verification. Do not claim file-level PATTERNS analogs for the e2e/doc/README/validation files; instead, derive this slice from the shared constraints in `098-RESEARCH.md`, `098-UI-SPEC.md`, and the implementation outputs of `098-02`: preserve the host-owned boundary language established around `examples/accrue_host/lib/accrue_host/billing.ex`, preserve the copy-backed operator wording from `accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex`, and keep the browser proof lane aligned with the mounted route posture implemented in `accrue_admin/lib/accrue_admin/live/customer_live.ex`. In `098-VALIDATION.md`, maintain a plan-aware task map, keep task-level commands limited to fast ExUnit or export checks, and reserve full browser verification for the phase gate with the explicit deterministic bootstrap: `cd examples/accrue_host && npm ci && npm run e2e:install && npm run e2e:a11y`.
  </action>
  <verify>
    <automated>rg -n "098-01-01|098-01-02|098-02-01|098-02-02|098-03-01|npm ci && npm run e2e:install && npm run e2e:a11y" .planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md && rg -n "host-owned|Replace payment method|Sync payment methods|npm ci|e2e:install" examples/accrue_host/docs/verify01-v112-admin-paths.md examples/accrue_host/README.md examples/accrue_host/e2e/verify01-admin-a11y.spec.js</automated>
  </verify>
  <done>
    The phase validation contract no longer assumes Playwright assets are preinstalled, no task-level verify uses browser E2E, and the docs/specs consistently describe the host-owned add/replace seam plus the explicit JS bootstrap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase docs/validation -> browser proof lane | Browser verification must run only after deterministic dependency bootstrap so proof is reproducible. |
| host docs -> operator workflow | Documentation must not imply admin-side payment capture or hide the host-owned add/replace boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-06 | D | Phase browser proof lane | mitigate | Add deterministic `npm ci` plus `npm run e2e:install` bootstrap before Playwright and keep it at the phase gate rather than in every task loop. |
| T-98-07 | R | Host verification docs | mitigate | Keep README/verify docs aligned with the truthful operator route and host-owned add/replace boundary so verification steps cannot drift into unsupported admin flows. |
</threat_model>

<verification>
Per-task:

`rg -n "098-01-01|098-01-02|098-02-01|098-02-02|098-03-01|npm ci && npm run e2e:install && npm run e2e:a11y" .planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-VALIDATION.md && rg -n "host-owned|Replace payment method|Sync payment methods|npm ci|e2e:install" examples/accrue_host/docs/verify01-v112-admin-paths.md examples/accrue_host/README.md examples/accrue_host/e2e/verify01-admin-a11y.spec.js`

Phase gate only:

`bash scripts/ci/verify_processor_support_matrix.sh && cd accrue && mix test --warnings-as-errors && cd ../accrue_admin && mix test --warnings-as-errors && cd ../examples/accrue_host && mix test test/accrue_host/braintree_payment_method_flow_test.exs && npm ci && npm run e2e:install && npm run e2e:a11y`
</verification>

<success_criteria>
- `098-VALIDATION.md` reserves Playwright for the phase gate and includes deterministic JS bootstrap.
- Host docs and browser specs consistently describe the truthful operator route and host-owned add/replace seam.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-03-SUMMARY.md`
</output>

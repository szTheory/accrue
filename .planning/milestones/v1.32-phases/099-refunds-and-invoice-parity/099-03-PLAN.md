---
phase: 099-refunds-and-invoice-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 099-01
  - 099-02
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/live/charge_live.ex
  - accrue_admin/test/accrue_admin/live/charge_live_test.exs
  - accrue_admin/test/support/e2e_fixtures.ex
  - .planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md
autonomous: true
requirements:
  - PROC-18
  - PROC-19
must_haves:
  truths:
    - "Operators can trigger a narrow Braintree refund action from charge detail without the admin UI becoming a finance console."
    - "Refund eligibility, void-vs-refund rules, and asynchronous settlement copy are explicit in the UI."
    - "Charge detail shows derived refund progress from child refund facts."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy.ex"
      provides: "Centralized refund-specific operator copy helpers for charge detail"
    - path: "accrue_admin/lib/accrue_admin/live/charge_live.ex"
      provides: "Thin operator refund shell over `Accrue.Billing.refund/2`"
    - path: "accrue_admin/test/accrue_admin/live/charge_live_test.exs"
      provides: "LiveView proof for Braintree eligibility copy, confirmation, and rollup rendering"
    - path: ".planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md"
      provides: "Nyquist-complete phase validation map aligned to the shipped refund surface"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/charge_live.ex"
      to: "accrue_admin/lib/accrue_admin/copy.ex"
      via: "refund warnings, confirmation text, and lifecycle status copy route through centralized helpers"
      pattern: "Copy\\."
    - from: "accrue_admin/lib/accrue_admin/live/charge_live.ex"
      to: "accrue/lib/accrue/billing.ex"
      via: "server-driven `Billing.refund/2` call during confirmation"
      pattern: "Billing\\.refund\\("
    - from: "accrue_admin/lib/accrue_admin/live/charge_live.ex"
      to: "accrue/lib/accrue/billing/refund.ex"
      via: "rendered refund rollups and child refund facts"
      pattern: "processor_id|refund_count|total_refunded"
---

<objective>
Expose the Phase 99 refund capability through a thin, explicit operator shell and finish the phase validation contract.

Purpose: Fulfill the roadmap’s operator promise per D-02, D-03, D-04, D-05, D-10, and D-21 without widening `AccrueAdmin` into a finance workspace.
Output: Charge-detail refund action wired to `Accrue.Billing.refund/2`, centralized honest Braintree refund copy and rollups, and an updated validation map that matches the final phase surface.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.32-ROADMAP.md
@.planning/milestones/v1.32-REQUIREMENTS.md
@.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-CONTEXT.md
@.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-02-SUMMARY.md
@.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-03-SUMMARY.md
@accrue_admin/lib/accrue_admin/copy.ex

<interfaces>
From `accrue_admin/lib/accrue_admin/live/charge_live.ex`:
```elixir
def handle_event("prepare_refund", params, socket) do
  case build_refund_action(params, socket.assigns.charge, socket.assigns.timeline_events) do
    {:ok, action} -> {:noreply, assign(socket, :pending_refund, action)}
    {:error, message} -> {:noreply, push_flash(socket, :warning, message)}
  end
end

defp execute_refund(socket, action) do
  opts = refund_opts(action, socket.assigns.charge.currency, operation_id)
  Billing.create_refund(socket.assigns.charge, opts)
end
```

From `accrue_admin/lib/accrue_admin/copy.ex`:
```elixir
def charge_prepare_refund_warning, do: "Prepare a refund before confirming."
def charge_refund_created_info, do: "Refund created with fee-aware fields from the billing facade."
def step_up_title, do: "Step-up required"
```

Current rendered refund list markers:
```elixir
<p class="ax-label"><%= refund.stripe_id || refund.id %></p>
<%= humanize(refund.status) %>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LiveView tests for Braintree refund eligibility, copy, and rollups</name>
  <files>accrue_admin/test/accrue_admin/live/charge_live_test.exs, accrue_admin/test/support/e2e_fixtures.ex</files>
  <read_first>accrue_admin/test/accrue_admin/live/charge_live_test.exs
accrue_admin/test/support/e2e_fixtures.ex
accrue_admin/lib/accrue_admin/live/charge_live.ex
accrue_admin/lib/accrue_admin/copy.ex
.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-CONTEXT.md
.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Braintree charge detail shows refund eligibility copy that distinguishes refunds from voids and warns that API success is not final lifecycle truth.
    - Test 2: confirming the action calls the canonical `Billing.refund/2` seam and leaves the action as a narrow charge-detail flow.
    - Test 3: the page renders derived refund rollups and child refund facts using `processor_id`-aware display.
  </behavior>
  <action>Extend `accrue_admin/test/accrue_admin/live/charge_live_test.exs` per D-02 through D-05 and D-21 with Braintree-specific assertions for `settling` / `settled` eligibility copy, explicit pre-settlement void guidance, confirmation UX, derived refund summary rendering, and the use of centralized `AccrueAdmin.Copy` helpers rather than inline refund strings in `charge_live.ex`. Update `accrue_admin/test/support/e2e_fixtures.ex` to seed Braintree-backed charges and refunds with `processor_id` plus compatibility `stripe_id` so the LiveView tests cover additive identity and rollup display. Make the new tests fail before implementation.</action>
  <acceptance_criteria>- `rg -n "void|settling|API success is not final|processor_id|refund count|total refunded" accrue_admin/test/accrue_admin/live/charge_live_test.exs accrue_admin/test/support/e2e_fixtures.ex` finds Braintree-specific copy and fixture coverage.
- `cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs --warnings-as-errors` passes after implementation.</acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The operator refund surface is pinned with automated LiveView proof before UI changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep charge detail a thin refund shell over `Accrue.Billing.refund/2` with centralized copy and finalize validation</name>
  <files>accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/lib/accrue_admin/live/charge_live.ex, .planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md</files>
  <read_first>accrue_admin/lib/accrue_admin/copy.ex
accrue_admin/lib/accrue_admin/live/charge_live.ex
.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md
.planning/milestones/v1.32-phases/098-payment-method-crud-operator-admin/098-03-SUMMARY.md
.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-CONTEXT.md</read_first>
  <action>Update `accrue_admin/lib/accrue_admin/copy.ex` first so all new Braintree refund warnings, confirmation strings, and asynchronous lifecycle/operator guidance live in centralized `AccrueAdmin.Copy` helpers per the phase pattern map. Then update `accrue_admin/lib/accrue_admin/live/charge_live.ex` so the operator path calls `Billing.refund/2` instead of `Billing.create_refund/2` per D-01, keeps explicit confirmation and audit/event recording per D-03, renders refund identity / summaries with `refund.processor_id || refund.stripe_id || refund.id` plus rollups such as `total_refunded_amount_minor`, `refund_count`, and a refund-progress string, and consumes the new `Copy` helpers instead of embedding refund-specific strings inline. The centralized copy must say refunds apply only to `settling` / `settled` Braintree transactions, voiding pre-settlement sales is separate, repeated partial refunds remain separate child facts, and refund API success is not final lifecycle truth per D-05 and D-19. Keep the admin scope narrow: no invoice-level workspace, no customer self-serve, no finance console per D-04. Then update `099-VALIDATION.md` to mark Wave 0 complete, `nyquist_compliant: true`, replace any `❌ W0` placeholders with the new test files and commands from Plans 01–03, and align the task map with the final plan IDs and automated verification commands.</action>
  <acceptance_criteria>- `rg -n "charge_.*refund|settling|settled|voiding pre-settlement|not final lifecycle truth" accrue_admin/lib/accrue_admin/copy.ex` finds centralized Braintree refund copy helpers.
- `rg -n "Billing\\.refund\\(|Copy\\.|processor_id \\|\\| refund\\.stripe_id" accrue_admin/lib/accrue_admin/live/charge_live.ex` finds the canonical billing call and centralized-copy usage.
- `rg -n "nyquist_compliant: true|wave_0_complete: true|refund_braintree_test|braintree_refund_convergence_test|charge_live_test" .planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md` shows the finalized validation map.
- `cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs --warnings-as-errors` passes.</acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The admin refund UX stays a thin operator shell over the billing context, all refund-specific operator copy routes through `AccrueAdmin.Copy`, reflects Braintree truth honestly, and the phase validation contract is complete and executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input -> `ChargeLive` | Admin user-triggered refund actions and amount fields enter the LiveView |
| `ChargeLive` -> `Accrue.Billing.refund/2` | UI events become financial mutations in the billing context |
| validation doc -> execution workflow | Phase verification metadata drives later automation and sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-09 | E | `accrue_admin/lib/accrue_admin/live/charge_live.ex` | mitigate | Keep refund issuance server-driven with explicit confirmation, existing step-up/audit hooks, and no direct provider UI embed. |
| T-99-10 | T | `accrue_admin/lib/accrue_admin/live/charge_live.ex` | mitigate | Render explicit refund-vs-void eligibility copy and call the canonical billing seam so operators cannot trigger unsupported semantics through UI ambiguity. |
| T-99-11 | R | `accrue_admin/lib/accrue_admin/live/charge_live.ex` | mitigate | Preserve admin audit event recording for completed refunds so operator actions remain attributable. |
| T-99-12 | D | `.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-VALIDATION.md` | mitigate | Replace draft placeholders with exact automated commands so future verification runs cannot silently skip missing proof lanes. |
</threat_model>

<verification>
Run `cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
Operators can issue Braintree refunds from charge detail through a thin, explicit shell over `Accrue.Billing.refund/2`, refund-specific operator strings are centralized in `AccrueAdmin.Copy`, and the phase validation map matches the shipped tests and proof lanes.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.32-phases/099-refunds-and-invoice-parity/099-03-SUMMARY.md`
</output>

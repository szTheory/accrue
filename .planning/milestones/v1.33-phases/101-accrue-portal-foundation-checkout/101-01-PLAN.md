---
phase: 101-accrue-portal-foundation-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/checkout/local_session.ex
  - accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs
  - accrue/lib/accrue/config.ex
  - accrue/test/support/checkout_session_fixture.ex
  - accrue/test/accrue/checkout/local_session_test.exs
  - accrue/test/accrue/config_test.exs
autonomous: true
requirements:
  - BT-02
must_haves:
  truths:
    - "Braintree checkout sessions persist enough state to resume a first-party hosted checkout safely."
    - "Returned local-portal checkout URLs are absolute and derived from explicit host configuration."
    - "Repeat checkout requests with the same operation id resolve to the same persisted session."
  artifacts:
    - path: "accrue/lib/accrue/checkout/local_session.ex"
      provides: "Persisted checkout-session contract for local portal flows"
    - path: "accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs"
      provides: "Database schema for checkout session storage"
    - path: "accrue/lib/accrue/config.ex"
      provides: "Runtime config contract for portal URL synthesis"
  key_links:
    - from: "accrue/lib/accrue/checkout/local_session.ex"
      to: "accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs"
      via: "schema fields matching migrated columns"
      pattern: "schema \\\"accrue_checkout_sessions\\\""
    - from: "accrue/lib/accrue/config.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "portal_url/1 and portal_base_url/0 helpers used for checkout URLs"
      pattern: "def portal_url"
---

<objective>
Harden the core persisted-session contract that powers Braintree local hosted checkout.

Purpose: satisfy D-10, D-11, D-12, and D-23 with an explicit schema/config foundation before adapter and LiveView work build on it.
Output: migrated `accrue_checkout_sessions` storage, enforced portal URL config semantics, a shared checkout-session fixture, and regression tests for session reuse and expiry-ready state.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-RESEARCH.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-VALIDATION.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-PATTERNS.md
@accrue/lib/accrue/checkout/local_session.ex
@accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs
@accrue/lib/accrue/config.ex
@accrue/test/accrue/processor/braintree_local_portal_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the local checkout-session schema and runtime URL contract</name>
  <files>accrue/lib/accrue/checkout/local_session.ex, accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs, accrue/lib/accrue/config.ex</files>
  <read_first>.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md, .planning/phases/101-accrue-portal-foundation-checkout/101-RESEARCH.md, accrue/lib/accrue/checkout/local_session.ex, accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs, accrue/lib/accrue/config.ex, accrue/lib/accrue/processor/braintree.ex</read_first>
  <acceptance_criteria>
    - `accrue/lib/accrue/config.ex` documents `:portal_base_url` as required for absolute local-portal URLs per D-23 and exposes `portal_url/1` behavior that refuses silent relative fallbacks for checkout/billing-session synthesis.
    - `accrue/lib/accrue/checkout/local_session.ex` persists `customer_id`, `processor`, `session_token`, `mode`, `ui_mode`, `status`, `price_id`, `line_items`, `success_url`, `cancel_url`, `return_url`, `operation_id`, `expires_at`, `metadata`, and `data`.
    - `create_or_reuse/2` still reuses the existing row when the same customer + `operation_id` is supplied, and generates opaque `chk_local_` tokens when no row exists.
    - The migration file still creates `accrue_checkout_sessions` with unique indexes on `session_token` and `operation_id`.
  </acceptance_criteria>
  <behavior>
    - Test 1: creating a local checkout session with `operation_id: "checkout-op-1"` twice returns the same persisted row.
    - Test 2: local portal URL helpers require explicit absolute base URL configuration for generated hosted checkout URLs.
  </behavior>
  <action>Adjust the existing `Accrue.Checkout.LocalSession` and `CreateAccrueCheckoutSessions` implementation to match the locked Phase 101 contract instead of the current permissive prototype. Follow the pattern mapper’s existing analogs in `Accrue.Checkout.LocalSession` and `Accrue.Processor.Braintree`: keep table name `accrue_checkout_sessions`, keep `chk_local_` opaque tokens, keep the 30-minute default expiry, and preserve Braintree-only storage plus `operation_id` reuse. Update `Accrue.Config` so `:portal_mount_path` remains normalized but `:portal_base_url` is treated as the required absolute prefix for returned hosted URLs per D-23; do not leave the current silent relative-URL fallback in the checkout/billing-session path.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/braintree_local_portal_test.exs test/accrue/checkout/local_session_test.exs test/accrue/config_test.exs -x</automated>
  </verify>
  <done>Core storage and config code enforce the local checkout-session contract without weakening Stripe behavior or widening the public API.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the checkout-session migration and prove the DB-backed contract</name>
  <files>accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs, accrue/test/support/checkout_session_fixture.ex, accrue/test/accrue/checkout/local_session_test.exs, accrue/test/accrue/config_test.exs</files>
  <read_first>accrue/priv/repo/migrations/20260501180000_create_accrue_checkout_sessions.exs, accrue/test/accrue/processor/braintree_local_portal_test.exs, .planning/phases/101-accrue-portal-foundation-checkout/101-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `mix ecto.migrate` runs in `accrue/` without pending errors for `20260501180000_create_accrue_checkout_sessions.exs`.
    - `accrue/test/support/checkout_session_fixture.ex` provides a reusable `accrue_checkout_sessions` fixture for later adapter and portal tests.
    - A new `accrue/test/accrue/checkout/local_session_test.exs` asserts insert, reuse, and completion behavior against the real repo.
    - The local-session test suite explicitly covers migrated columns plus unique-index enforcement for `session_token` and `operation_id`, satisfying the validation schema-coverage requirement.
    - `accrue/test/accrue/config_test.exs` asserts the portal URL helper behavior expected by D-23.
  </acceptance_criteria>
  <action>Run the schema change in the `accrue` app before phase verification and add the shared checkout-session fixture plus focused ExUnit coverage around the migrated table shape and config helpers. This task owns the validation artifacts for `checkout_session_fixture.ex` and migration schema coverage; do not leave them as planner placeholders. If the migration cannot run autonomously because the local database is unavailable, stop and record the exact command failure in the summary instead of papering over it.</action>
  <verify>
    <automated>cd accrue && mix ecto.migrate && mix test test/accrue/processor/braintree_local_portal_test.exs test/accrue/checkout/local_session_test.exs test/accrue/config_test.exs -x</automated>
  </verify>
  <done>The migration is applied in the local `accrue` app, later plans can reuse the shared checkout-session fixture, and the new tests fail if the DB schema or portal URL contract regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| billing facade -> persisted checkout session row | Untrusted caller input becomes DB-backed checkout state |
| runtime config -> returned checkout URL | Misconfiguration can create broken or unsafe redirect targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | T | `Accrue.Checkout.LocalSession` | mitigate | Keep opaque `chk_local_` tokens, unique constraints, and customer-scoped idempotent reuse keyed by `operation_id` per D-12. |
| T-101-02 | R | `accrue_checkout_sessions` expiry/status | mitigate | Persist `status` and `expires_at`, then cover insert/reuse/completion behavior with DB-backed tests. |
| T-101-03 | I | `Accrue.Config.portal_base_url/0` | mitigate | Require explicit absolute base URL semantics for returned local portal URLs so misconfiguration does not silently leak relative redirect behavior. |
</threat_model>

<verification>
Run `cd accrue && mix ecto.migrate`.
Run `cd accrue && mix test test/accrue/processor/braintree_local_portal_test.exs test/accrue/checkout/local_session_test.exs test/accrue/config_test.exs -x`.
</verification>

<success_criteria>
The local checkout-session table, schema, and config helpers form a stable core contract for Braintree hosted checkout and fail loudly when URL configuration is incomplete.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-01-SUMMARY.md`
</output>

---
phase: 101-accrue-portal-foundation-checkout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_portal/lib/accrue_portal/router.ex
  - accrue_portal/lib/accrue_portal/auth_hook.ex
  - accrue_portal/lib/accrue_portal/auth_plug.ex
  - accrue_portal/lib/accrue_portal/customer_session.ex
  - accrue_portal/lib/accrue_portal/csp_plug.ex
  - accrue_portal/lib/accrue_portal/brand_plug.ex
  - accrue_portal/test/test_helper.exs
  - accrue_portal/test/support/conn_case.ex
autonomous: true
requirements:
  - BT-01
must_haves:
  truths:
    - "A host app can mount the portal with one sibling `live_session :accrue_portal`."
    - "Portal routes resolve the authenticated current user and current customer through the existing `Accrue.Auth` contract."
    - "Portal requests ship with the Hosted Fields CSP allowlist and brand/theme assigns."
  artifacts:
    - path: "accrue_portal/lib/accrue_portal/router.ex"
      provides: "Mount macro and live-session wiring"
    - path: "accrue_portal/lib/accrue_portal/auth_hook.ex"
      provides: "Customer-aware `on_mount` callbacks"
    - path: "accrue_portal/test/support/conn_case.ex"
      provides: "Package-local browser test harness base"
  key_links:
    - from: "accrue_portal/lib/accrue_portal/router.ex"
      to: "accrue_portal/lib/accrue_portal/auth_hook.ex"
      via: "live_session `on_mount` option"
      pattern: "Accrue\\.Portal\\.AuthHook"
    - from: "accrue_portal/lib/accrue_portal/auth_hook.ex"
      to: "accrue_portal/lib/accrue_portal/customer_session.ex"
      via: "session assign resolution"
      pattern: "current_customer|accrue_portal_session"
---

<objective>
Correct the portal package’s mount/auth contract so it matches the locked Phase 101 public surface instead of the current prototype.

Purpose: satisfy D-01, D-02, D-16, D-17, D-18, D-21, and D-22 before deeper checkout and customer-portal behavior depends on the mount shell.
Output: mountable router/auth shell, CSP/brand pipeline, and the base package-local browser harness the later portal plans build on.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-PATTERNS.md
@accrue_admin/lib/accrue_admin/router.ex
@accrue_admin/lib/accrue_admin/auth_hook.ex
@accrue_admin/lib/accrue_admin/csp_plug.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the portal mount shell with the locked public contract</name>
  <files>accrue_portal/lib/accrue_portal/router.ex, accrue_portal/lib/accrue_portal/auth_hook.ex, accrue_portal/lib/accrue_portal/auth_plug.ex, accrue_portal/lib/accrue_portal/customer_session.ex, accrue_portal/lib/accrue_portal/csp_plug.ex, accrue_portal/lib/accrue_portal/brand_plug.ex</files>
  <behavior>
    - Test 1: the mount macro emits exactly one sibling `live_session :accrue_portal`.
    - Test 2: `on_mount` supports both `:ensure_customer` and `:ensure_customer_no_create`.
    - Test 3: the browser pipeline exposes Braintree-safe CSP directives and brand assigns.
  </behavior>
  <action>Refactor the existing prototype shell so it mirrors the `AccrueAdmin.Router` and `AccrueAdmin.AuthHook` analogs called out in `101-PATTERNS.md`. Implement the `Accrue.Portal.*` public namespace and `AccruePortal.*` internal namespace per D-02, replace router-first gating with the D-16/D-17 `on_mount` contract, assign `:current_user`, `:current_customer`, and `:accrue_portal_session` per D-18, and lock the CSP/brand pipeline to the D-22 allowlist without inventing a new auth behaviour.</action>
  <verify>
    <automated>cd accrue_portal && mix compile && rg -n 'live_session :accrue_portal|ensure_customer_no_create|current_customer|js\\.braintreegateway\\.com|braintree-api\\.com' lib/accrue_portal/router.ex lib/accrue_portal/auth_hook.ex lib/accrue_portal/csp_plug.ex</automated>
  </verify>
  <done>The portal shell exposes the locked D-16/D-22 mount, auth, and CSP contract in production code.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the package-local browser harness base for later portal tests</name>
  <files>accrue_portal/test/test_helper.exs, accrue_portal/test/support/conn_case.ex</files>
  <action>Create the minimum package-local harness that later portal plans can build on without reaching back into the example host app: `test_helper.exs` boots the portal test environment, and `conn_case.ex` provides the browser/session helpers the router, checkout, and portal LiveView suites will share. Keep this task limited to base harness ownership; the focused shell tests and Braintree mock surface move to Plan 07.</action>
  <verify>
    <automated>cd accrue_portal && mix compile && test -f test/test_helper.exs && test -f test/support/conn_case.ex</automated>
  </verify>
  <done>The portal package owns its own test bootstrap and browser case before follow-on tests are added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host router → portal live_session | Untrusted browser/session state crosses into portal mount/auth code |
| portal browser pipeline → Braintree CDN | CSP must constrain external script/frame/connect origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-02-01 | S | `auth_hook.ex` | mitigate | Reuse `Accrue.Auth` per D-17 and assign `:current_customer` only from resolved user/session state |
| T-101-02-02 | T | `csp_plug.ex` | mitigate | Emit the D-22 `script-src`, `frame-src`, and `connect-src` allowlist exactly for Braintree domains |
| T-101-02-03 | E | `router.ex` | mitigate | Emit one sibling `live_session :accrue_portal` per D-16/D-21 to avoid nested-session privilege bleed |
</threat_model>

<verification>
Run `cd accrue_portal && mix compile`.
Run `cd accrue_portal && rg -n 'live_session :accrue_portal|ensure_customer_no_create|current_customer|js\.braintreegateway\.com|braintree-api\.com' lib/accrue_portal/router.ex lib/accrue_portal/auth_hook.ex lib/accrue_portal/csp_plug.ex`.
</verification>

<success_criteria>
Hosts can mount a CSP-hardened portal shell that reuses `Accrue.Auth`, resolves `current_customer`, and provides the base test harness for the later portal slices.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-02-SUMMARY.md`
</output>

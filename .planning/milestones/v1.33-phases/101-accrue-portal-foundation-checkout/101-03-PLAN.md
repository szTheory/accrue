---
phase: 101-accrue-portal-foundation-checkout
plan: 03
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - accrue/lib/accrue/processor/capabilities.ex
  - accrue/lib/accrue/processor/braintree.ex
  - accrue/lib/accrue/billing.ex
  - accrue/test/accrue/processor/capabilities_test.exs
  - accrue/test/accrue/processor/braintree_local_portal_test.exs
  - accrue/test/accrue/billing/checkout_session_facade_test.exs
  - accrue/test/accrue/billing/billing_portal_session_facade_test.exs
autonomous: true
requirements:
  - BT-02
must_haves:
  truths:
    - "The existing billing facade returns local hosted checkout URLs for Braintree without changing Stripe semantics."
    - "Braintree advertises checkout and billing-portal support through the published capability map."
    - "Billing-portal and checkout session payloads preserve redirect and idempotency semantics."
  artifacts:
    - path: "accrue/lib/accrue/processor/braintree.ex"
      provides: "Braintree local-portal adapter behavior"
    - path: "accrue/lib/accrue/processor/capabilities.ex"
      provides: "Published support-label vocabulary and capability contract"
    - path: "accrue/lib/accrue/billing.ex"
      provides: "Facade docs for hosted checkout and billing portal sessions"
  key_links:
    - from: "accrue/lib/accrue/billing.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "CheckoutSession.create/1 and BillingPortal.Session.create/1 dispatch"
      pattern: "create_checkout_session"
    - from: "accrue/lib/accrue/processor/capabilities.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "checkout/billing_portal support labels must match adapter booleans"
      pattern: "first-party local portal"
---

<objective>
Align the Braintree adapter and public billing facade with the locked first-party local portal semantics.

Purpose: satisfy D-06 through D-10 and D-14 without introducing a new public `ui_mode` or regressing Stripe’s hosted flows.
Output: capability-map truth, Braintree checkout/billing-portal adapter behavior, and updated facade regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-RESEARCH.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-PATTERNS.md
@accrue/lib/accrue/processor/capabilities.ex
@accrue/lib/accrue/processor/braintree.ex
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/checkout/session.ex
@accrue/test/accrue/processor/braintree_local_portal_test.exs
@accrue/test/accrue/billing/checkout_session_facade_test.exs
@accrue/test/accrue/billing/billing_portal_session_facade_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip the Braintree adapter and capability labels to the first-party local-portal contract</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, accrue/lib/accrue/processor/braintree.ex</files>
  <read_first>.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md, accrue/lib/accrue/processor/capabilities.ex, accrue/lib/accrue/processor/braintree.ex, accrue/lib/accrue/checkout/session.ex, accrue/lib/accrue/billing_portal/session.ex</read_first>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/capabilities.ex` exposes `"first-party local portal"` for `checkout.create`, `checkout.fetch`, `checkout.hosted`, and `billing_portal.create`.
    - `Accrue.Processor.Braintree.capabilities/0` returns `checkout: %{create: true, fetch: true, hosted: true, embedded: false}` and `billing_portal: %{create: true}`.
    - `checkout_session_create/2`, `checkout_session_fetch/2`, and `portal_session_create/2` return payloads that include the persisted local session id/token, Braintree customer processor id, and configured local-portal URL pathing.
    - When the host has not mounted `accrue_portal`, Braintree preserves the Phase 100 fallback by returning `{:error, %Accrue.APIError{code: :unsupported_by_gateway}}` for billing-portal creation instead of synthesizing a broken local URL, per D-07.
    - The adapter does not introduce a new `:ui_mode` value and keeps Stripe adapters untouched per D-15 and D-14.
  </acceptance_criteria>
  <behavior>
    - Test 1: Braintree `create_checkout_session/2` returns a hosted URL under the configured portal mount path.
    - Test 2: Braintree `create_billing_portal_session/2` returns a local portal URL when `accrue_portal` is mounted.
    - Test 3: Braintree `create_billing_portal_session/2` returns `:unsupported_by_gateway` when `accrue_portal` is not mounted, while Stripe contract tests still pass unchanged.
  </behavior>
  <action>Update the Braintree adapter to honor the locked D-06 through D-10 semantics rather than the current prototype defaults, following the exact existing analogs surfaced in `101-PATTERNS.md`: `LocalSession.create_or_reuse/2` for idempotency, `build_local_checkout_attrs/3` + `local_checkout_payload/1` for synthesized portal URLs, and `Accrue.Processor.Capabilities` for the public support-label vocabulary. Keep the public facade unchanged, keep `:ui_mode` limited to `:hosted | :embedded`, synthesize hosted URLs through the persisted local session token, and preserve `success_url`, `cancel_url`, and `return_url` passthrough semantics exactly. Add the explicit D-07 fallback branch: if the local portal mount/path configuration is absent, the adapter/facade must preserve `{:error, %Accrue.APIError{code: :unsupported_by_gateway}}` rather than pretending Braintree local portal support exists. Do not branch the facade on processor identity; all behavior must stay behind `Accrue.Processor.__impl__()` dispatch.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/processor/braintree_local_portal_test.exs test/accrue/billing/checkout_session_facade_test.exs test/accrue/billing/billing_portal_session_facade_test.exs -x</automated>
  </verify>
  <done>The Braintree adapter and support-label surface advertise and implement first-party local hosted checkout/billing portal semantics without widening the public API.</done>
</task>

<task type="auto">
  <name>Task 2: Update facade documentation and regression tests around the Braintree local-portal path</name>
  <files>accrue/lib/accrue/billing.ex, accrue/test/accrue/processor/braintree_local_portal_test.exs, accrue/test/accrue/billing/checkout_session_facade_test.exs, accrue/test/accrue/billing/billing_portal_session_facade_test.exs</files>
  <read_first>accrue/lib/accrue/billing.ex, accrue/test/accrue/processor/braintree_local_portal_test.exs, accrue/test/accrue/billing/checkout_session_facade_test.exs, accrue/test/accrue/billing/billing_portal_session_facade_test.exs, .planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing.ex` explains that Braintree hosted checkout and billing-portal sessions resolve to host-mounted local portal URLs while Stripe behavior remains unchanged.
    - Regression tests assert the local portal URL shape, redirect passthrough fields, and same-`operation_id` reuse for mounted Braintree flows.
    - Regression tests also assert the unmounted-portal fallback so `Accrue.Billing.create_billing_portal_session/2` preserves `:unsupported_by_gateway` when portal routing is unavailable.
    - No Stripe-oriented test literal is rewritten to pretend Stripe now uses the local portal.
  </acceptance_criteria>
  <action>Refresh the public facade docs and tests so they match the locked product truth: callers keep using `Accrue.Billing.create_checkout_session/2` and `create_billing_portal_session/2`, Braintree now succeeds through a first-party local portal when mounted, and Stripe remains byte-identical to v1.32. Tighten the regression suite around mounted URL shape, session reuse, billing-portal return URLs, and the unmounted `:unsupported_by_gateway` fallback instead of adding new user-facing API knobs.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/braintree_local_portal_test.exs test/accrue/billing/checkout_session_facade_test.exs test/accrue/billing/billing_portal_session_facade_test.exs -x</automated>
  </verify>
  <done>The billing facade documentation and tests describe and enforce the Braintree local-portal path accurately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public billing facade -> processor adapter | Facade callers rely on processor-specific behavior without seeing adapter internals |
| persisted local session -> returned checkout/billing URL | Adapter output becomes a bearer redirect target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-07 | T | `Accrue.Processor.Braintree.checkout_session_create/2` | mitigate | Preserve idempotent lookup through `LocalSession.create_or_reuse/2` and pass through only the locked redirect fields. |
| T-101-08 | I | capability/support-label surface | mitigate | Keep the published label `"first-party local portal"` aligned with adapter booleans and regression tests so documentation cannot drift from real support. |
| T-101-09 | R | billing facade docs | mitigate | Update the public docstrings and targeted tests so hosts cannot mistake Braintree local checkout for a Stripe-hosted page or a new `ui_mode` contract. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/processor/braintree_local_portal_test.exs test/accrue/billing/checkout_session_facade_test.exs test/accrue/billing/billing_portal_session_facade_test.exs -x`.
</verification>

<success_criteria>
The published capability map and the Braintree adapter both tell the same honest story: checkout and billing portal are supported through a first-party local portal, with no API churn for callers.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-03-SUMMARY.md`
</output>

---
phase: 101-accrue-portal-foundation-checkout
plan: 05
type: execute
wave: 4
depends_on:
  - 101-04
files_modified:
  - accrue_portal/lib/accrue_portal/billing_read_model.ex
  - accrue_portal/lib/accrue_portal/copy.ex
  - accrue_portal/lib/accrue_portal/live/home_live.ex
  - accrue_portal/lib/accrue_portal/live/subscriptions_live.ex
  - accrue_portal/lib/accrue_portal/live/subscription_live.ex
  - accrue_portal/lib/accrue_portal/authorize.ex
  - accrue_portal/lib/accrue_portal/router.ex
autonomous: true
requirements:
  - BT-01
  - BT-03
must_haves:
  truths:
    - "Authenticated customers can see a portal dashboard plus subscription list and detail views scoped to their own customer record."
    - "Customers can cancel subscriptions from the portal without leaking another customer’s data."
    - "Dashboard and subscription copy stays centralized instead of drifting into inline LiveView strings."
  artifacts:
    - path: "accrue_portal/lib/accrue_portal/authorize.ex"
      provides: "Defense-in-depth customer scoping helper"
    - path: "accrue_portal/lib/accrue_portal/live/subscription_live.ex"
      provides: "Subscription detail and cancel flow"
    - path: "accrue_portal/lib/accrue_portal/router.ex"
      provides: "Home and subscription route wiring"
  key_links:
    - from: "accrue_portal/lib/accrue_portal/authorize.ex"
      to: "accrue_portal/lib/accrue_portal/billing_read_model.ex"
      via: "customer-scoped lookup helpers"
      pattern: "current_customer\\.id|not_found"
    - from: "accrue_portal/lib/accrue_portal/router.ex"
      to: "accrue_portal/lib/accrue_portal/live/subscription_live.ex"
      via: "LiveView route `/subscriptions/:id`"
      pattern: "subscriptions/:id"
---

<objective>
Complete the dashboard and subscription-management half of the customer portal, including the locked D-19 tenant-scoping contract.

Purpose: satisfy the core BT-03 read/manage flows first, then hand the shared fixture/assertion test ownership to a narrower follow-on plan.
Output: dashboard + subscription LiveViews, D-19 authorization helper, route wiring, and centralized copy for this slice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-UI-SPEC.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the dashboard and subscription LiveViews with D-19 customer scoping</name>
  <files>accrue_portal/lib/accrue_portal/billing_read_model.ex, accrue_portal/lib/accrue_portal/copy.ex, accrue_portal/lib/accrue_portal/live/home_live.ex, accrue_portal/lib/accrue_portal/live/subscriptions_live.ex, accrue_portal/lib/accrue_portal/live/subscription_live.ex, accrue_portal/lib/accrue_portal/authorize.ex, accrue_portal/lib/accrue_portal/router.ex</files>
  <behavior>
    - Test 1: `/`, `/subscriptions`, and `/subscriptions/:id` resolve through the mounted portal shell.
    - Test 2: every subscription lookup scopes by `socket.assigns.current_customer.id`.
    - Test 3: dashboard and subscription copy comes from `copy.ex`, not inline HEEx strings.
  </behavior>
  <action>Extend the current list-page prototype into the dashboard + subscriptions half of the locked BT-03 route set. Per D-19, centralize tenant enforcement in `Accrue.Portal.Authorize`, scope every dashboard/subscription lookup through `current_customer.id`, add the missing `SubscriptionLive`, and update the existing pages to match the empty states, destructive confirmations, and CTA copy from `101-UI-SPEC.md`. Keep this plan on production code only; fixture/assertion helpers and property-backed proof move to Plan 09.</action>
  <verify>
    <automated>cd accrue_portal && mix compile && rg -n 'No active subscriptions|Manage subscriptions|Cancel subscription|Keep subscription' lib/accrue_portal/copy.ex && ! rg -n 'No active subscriptions|Manage subscriptions|Cancel subscription|Keep subscription' lib/accrue_portal/live/home_live.ex lib/accrue_portal/live/subscriptions_live.ex lib/accrue_portal/live/subscription_live.ex</automated>
  </verify>
  <done>The dashboard and subscription slice is complete in production code, and every lookup in that slice is customer-scoped before the remaining surfaces are layered on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal route params → subscription lookups | Untrusted ids cross into customer-scoped data access |
| LiveView actions → subscription mutations | Cancel flows must not operate on another customer's records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05-01 | I | `authorize.ex` | mitigate | Enforce D-19 customer scoping on every dashboard/subscription lookup and fail with not-found |
| T-101-05-02 | T | `subscription_live.ex` | mitigate | Route all cancel actions through customer-scoped read-model helpers before mutation |
| T-101-05-03 | R | `copy.ex` | accept | Copy centralization has low security impact; keep it centralized to avoid drift and review confusion |
</threat_model>

<verification>
Run `cd accrue_portal && mix compile`.
Run `cd accrue_portal && rg -n 'No active subscriptions|Manage subscriptions|Cancel subscription|Keep subscription' lib/accrue_portal/copy.ex`.
Run `cd accrue_portal && ! rg -n 'No active subscriptions|Manage subscriptions|Cancel subscription|Keep subscription' lib/accrue_portal/live/home_live.ex lib/accrue_portal/live/subscriptions_live.ex lib/accrue_portal/live/subscription_live.ex`.
</verification>

<success_criteria>
Customers can see the portal dashboard and manage subscriptions through customer-scoped LiveViews, with D-19 enforcement implemented in the production slice before the proof-oriented test slice runs.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-05-SUMMARY.md`
</output>

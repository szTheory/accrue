---
phase: 101-accrue-portal-foundation-checkout
plan: 06
type: execute
wave: 5
depends_on:
  - 101-04
  - 101-05
files_modified:
  - accrue_portal/lib/accrue_portal/copy.ex
  - accrue_portal/lib/accrue_portal/router.ex
  - accrue_portal/lib/accrue_portal/live/payment_methods_live.ex
  - accrue_portal/lib/accrue_portal/live/add_payment_method_live.ex
  - accrue_portal/lib/accrue_portal/live/invoices_live.ex
autonomous: true
requirements:
  - BT-03
must_haves:
  truths:
    - "Customers can manage payment methods and review invoices from the same mounted portal shell."
    - "Payment-method and invoice copy stays centralized instead of drifting into inline LiveView strings."
    - "The remaining BT-03 routes are wired without opening a controller-only side path."
  artifacts:
    - path: "accrue_portal/lib/accrue_portal/live/payment_methods_live.ex"
      provides: "Vaulted payment-method management UI"
    - path: "accrue_portal/lib/accrue_portal/live/add_payment_method_live.ex"
      provides: "Hosted Fields add-card UI"
    - path: "accrue_portal/lib/accrue_portal/live/invoices_live.ex"
      provides: "Invoice history UI"
  key_links:
    - from: "accrue_portal/lib/accrue_portal/router.ex"
      to: "accrue_portal/lib/accrue_portal/live/payment_methods_live.ex"
      via: "LiveView route `/payment-methods`"
      pattern: "payment-methods"
    - from: "accrue_portal/lib/accrue_portal/live/add_payment_method_live.ex"
      to: "accrue_portal/lib/accrue_portal/copy.ex"
      via: "shared CTA/error copy"
      pattern: "Save card|Discard card"
---

<objective>
Finish the remaining payment-method and invoice production surfaces of the customer portal.

Purpose: satisfy the remaining BT-03 UI work in a narrow slice, leaving test proof and docs/release/example-host integration to follow-on plans.
Output: payment-method and invoice LiveViews, route wiring, and centralized portal copy for this slice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-UI-SPEC.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the payment-method and invoice LiveViews with route wiring and D-19 production guards</name>
  <files>accrue_portal/lib/accrue_portal/copy.ex, accrue_portal/lib/accrue_portal/router.ex, accrue_portal/lib/accrue_portal/live/payment_methods_live.ex, accrue_portal/lib/accrue_portal/live/add_payment_method_live.ex, accrue_portal/lib/accrue_portal/live/invoices_live.ex</files>
  <behavior>
    - Test 1: `/payment-methods`, `/payment-methods/new`, and `/invoices` resolve through the portal router.
    - Test 2: payment-method and invoice copy comes from `copy.ex`, not inline HEEx strings.
    - Test 3: the add-card flow stays in the LiveView/Hosted Fields path, not a controller-owned form path.
  </behavior>
  <action>Complete the remaining BT-03 portal surfaces by following the existing read-model and LiveView patterns established earlier in the phase. Add the `/payment-methods`, `/payment-methods/new`, and `/invoices` routes to `accrue_portal/lib/accrue_portal/router.ex`, centralize the user-facing strings for this slice in `copy.ex`, keep all lookups/actions customer-scoped per D-19, and keep Hosted Fields nonce handling inside the add-payment-method flow rather than introducing a controller-driven payments UI.</action>
  <verify>
    <automated>cd accrue_portal && mix compile && rg -n 'No payment methods on file|Add a card|Save card|Discard card|Delete card|Keep card|No invoices yet' lib/accrue_portal/copy.ex && ! rg -n 'No payment methods on file|Add a card|Save card|Discard card|Delete card|Keep card|No invoices yet' lib/accrue_portal/live/payment_methods_live.ex lib/accrue_portal/live/add_payment_method_live.ex lib/accrue_portal/live/invoices_live.ex</automated>
  </verify>
  <done>The remaining portal self-service pages are implemented in production code and routed through the mounted portal shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal route params/actions → payment-method and invoice lookups | Untrusted ids and clicks cross into customer-scoped data access |
| add-card LiveView → Hosted Fields nonce path | Payment method creation must stay nonce-only and session-bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-06-01 | I | `payment_methods_live.ex` | mitigate | Scope payment-method reads/mutations by `current_customer.id` per D-19 |
| T-101-06-02 | I | `invoices_live.ex` | mitigate | Scope invoice history by `current_customer.id` and fail closed on mismatches |
| T-101-06-03 | T | `add_payment_method_live.ex` | mitigate | Preserve Hosted Fields nonce-only flow and avoid controller-owned card forms |
</threat_model>

<verification>
Run `cd accrue_portal && mix compile`.
Run `cd accrue_portal && rg -n 'No payment methods on file|Add a card|Save card|Discard card|Delete card|Keep card|No invoices yet' lib/accrue_portal/copy.ex`.
Run `cd accrue_portal && ! rg -n 'No payment methods on file|Add a card|Save card|Discard card|Delete card|Keep card|No invoices yet' lib/accrue_portal/live/payment_methods_live.ex lib/accrue_portal/live/add_payment_method_live.ex lib/accrue_portal/live/invoices_live.ex`.
</verification>

<success_criteria>
The remaining payment-method and invoice pages exist in production code, are routed through the portal shell, and stay aligned with the locked copy and Hosted Fields boundaries before the follow-on proof and docs slices run.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-06-SUMMARY.md`
</output>

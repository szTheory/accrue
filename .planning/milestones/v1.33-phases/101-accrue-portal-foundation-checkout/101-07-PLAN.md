---
phase: 101-accrue-portal-foundation-checkout
plan: 07
type: execute
wave: 2
depends_on:
  - 101-02
files_modified:
  - accrue_portal/mix.exs
  - accrue_portal/test/support/braintree_mox.ex
  - accrue_portal/test/accrue_portal/router_test.exs
  - accrue_portal/test/accrue_portal/auth_hook_test.exs
  - accrue_portal/test/accrue_portal/csp_plug_test.exs
autonomous: true
requirements:
  - BT-01
must_haves:
  truths:
    - "The packaged portal declares the exact runtime dependency contract from D-04."
    - "Portal router/auth/CSP behavior is proven by package-local tests before later slices rely on it."
    - "Later Braintree-facing LiveView tests have a package-local mock surface."
  artifacts:
    - path: "accrue_portal/mix.exs"
      provides: "Locked runtime dependency contract"
    - path: "accrue_portal/test/support/braintree_mox.ex"
      provides: "Shared Braintree mock surface"
    - path: "accrue_portal/test/accrue_portal/router_test.exs"
      provides: "Shell regression coverage"
  key_links:
    - from: "accrue_portal/mix.exs"
      to: "accrue_portal/test/support/braintree_mox.ex"
      via: "test-only dependency boundary"
      pattern: "only: \\[:test|only: \\[:dev, :test"
    - from: "accrue_portal/test/accrue_portal/auth_hook_test.exs"
      to: "accrue_portal/lib/accrue_portal/auth_hook.ex"
      via: "package-local shell verification"
      pattern: "ensure_customer|ensure_customer_no_create"
---

<objective>
Lock the portal package dependency contract and focused shell tests into their own slice.

Purpose: satisfy D-04 and give the rest of the phase a stable, package-local router/auth/CSP proof point without bloating Plan 02.
Output: explicit runtime deps, Braintree mocks, and focused shell regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@accrue_admin/mix.exs
@accrue_portal/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the exact D-04 runtime dependency contract</name>
  <files>accrue_portal/mix.exs</files>
  <action>Update `accrue_portal/mix.exs` so the package advertises the exact D-04 dependency surface: explicit `:accrue`, `:phoenix`, `:phoenix_live_view`, `:phoenix_html`, `:plug`, and `:jason` runtime deps, with no hard runtime dependency on `:braintree` or `:lattice_stripe`. If test harness work needs extras such as `:lazy_html`, keep them outside the production dependency contract.</action>
  <verify>
    <automated>cd accrue_portal && mix deps.tree --only prod | rg ' phoenix$| phoenix_live_view$| phoenix_html$| plug$| jason$| accrue$' && ! mix deps.tree --only prod | rg ' braintree$| lattice_stripe$'</automated>
  </verify>
  <done>The packaged portal exposes the exact D-04 runtime contract and keeps adapter SDKs out of production deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shell regression tests and shared Braintree mocks</name>
  <files>accrue_portal/test/support/braintree_mox.ex, accrue_portal/test/accrue_portal/router_test.exs, accrue_portal/test/accrue_portal/auth_hook_test.exs, accrue_portal/test/accrue_portal/csp_plug_test.exs</files>
  <behavior>
    - Test 1: the macro emits one sibling `live_session :accrue_portal`.
    - Test 2: auth hook resolves `current_user` and `current_customer` or redirects unauthenticated access.
    - Test 3: CSP plug emits the Braintree allowlist from D-22.
  </behavior>
  <action>Create the package-local router/auth/CSP tests and the shared Braintree mock surface that later checkout and payment-method suites reuse. Keep this slice narrowly focused on proving the shell from Plans 02 and 07; do not pull in checkout, telemetry, or BT-03 page behavior here.</action>
  <verify>
    <automated>cd accrue_portal && mix test test/accrue_portal/router_test.exs test/accrue_portal/auth_hook_test.exs test/accrue_portal/csp_plug_test.exs -x</automated>
  </verify>
  <done>The portal shell and CSP policy are locked under package-local regression tests, and later plans can reuse the shared Braintree mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package deps → runtime surface | Production dependency sprawl can widen attack surface |
| shell tests → auth/CSP contract | Later plans rely on these tests to catch auth and CSP regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-07-01 | T | `mix.exs` | mitigate | Keep adapter SDKs out of production deps per D-04 and assert the prod dep tree automatically |
| T-101-07-02 | S | `auth_hook_test.exs` | mitigate | Prove unauthenticated and authenticated auth-hook paths before checkout/portal slices depend on them |
| T-101-07-03 | T | `csp_plug_test.exs` | mitigate | Prove the D-22 Braintree CSP entries with package-local tests |
</threat_model>

<verification>
Run `cd accrue_portal && mix deps.tree --only prod | rg ' phoenix$| phoenix_live_view$| phoenix_html$| plug$| jason$| accrue$'`.
Run `cd accrue_portal && ! mix deps.tree --only prod | rg ' braintree$| lattice_stripe$'`.
Run `cd accrue_portal && mix test test/accrue_portal/router_test.exs test/accrue_portal/auth_hook_test.exs test/accrue_portal/csp_plug_test.exs -x`.
</verification>

<success_criteria>
The portal package dependency contract, mock surface, and shell proof are stable enough for later checkout and customer-portal plans to depend on directly.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-07-SUMMARY.md`
</output>

---
phase: 101-accrue-portal-foundation-checkout
plan: 08
type: execute
wave: 4
depends_on:
  - 101-04
files_modified:
  - accrue_portal/lib/accrue_portal/live/checkout_live.ex
  - accrue/lib/accrue/portal/checkout/completion_job.ex
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/test/accrue/webhook/default_handler_portal_event_test.exs
  - accrue/test/accrue/telemetry/portal_checkout_completed_test.exs
autonomous: true
requirements:
  - BT-02
must_haves:
  truths:
    - "Successful local checkout emits a synthetic completion event instead of stopping at the UI boundary."
    - "Portal completion telemetry is emitted without blocking the customer-facing response."
    - "The synthetic event reduces through the existing default-handler path."
  artifacts:
    - path: "accrue/lib/accrue/portal/checkout/completion_job.ex"
      provides: "Synthetic checkout completion worker"
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "Shared event reduction path for portal checkout completion"
    - path: "accrue/test/accrue/telemetry/portal_checkout_completed_test.exs"
      provides: "Telemetry regression coverage"
  key_links:
    - from: "accrue_portal/lib/accrue_portal/live/checkout_live.ex"
      to: "accrue/lib/accrue/portal/checkout/completion_job.ex"
      via: "enqueue after `Billing.subscribe/3` success"
      pattern: "enqueue|CompletionJob"
    - from: "accrue/lib/accrue/portal/checkout/completion_job.ex"
      to: "accrue/lib/accrue/webhook/default_handler.ex"
      via: "synthetic `accrue.portal.checkout.completed` event row"
      pattern: "accrue\\.portal\\.checkout\\.completed"
---

<objective>
Add the downstream synthetic completion-event and telemetry plumbing for local checkout.

Purpose: satisfy D-13 without overloading the checkout UI slice with worker/default-handler concerns.
Output: completion worker, default-handler wiring, telemetry emission, and focused Accrue-side tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/webhook/event.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit the synthetic checkout-completed event after successful subscribe</name>
  <files>accrue_portal/lib/accrue_portal/live/checkout_live.ex, accrue/lib/accrue/portal/checkout/completion_job.ex, accrue/lib/accrue/webhook/default_handler.ex</files>
  <behavior>
    - Test 1: successful checkout enqueues completion work after `Billing.subscribe/3`.
    - Test 2: the completion worker persists `accrue.portal.checkout.completed`.
    - Test 3: the default handler reduces the synthetic event through the existing projection path.
  </behavior>
  <action>After `Billing.subscribe/3` succeeds inside `CheckoutLive.handle_event/3`, enqueue the D-13 completion job that writes the synthetic `accrue.portal.checkout.completed` event and reduce it through the existing `Accrue.Webhook.DefaultHandler` path. Keep the enqueue boundary asynchronous, reuse the established webhook/event recording path, and do not block the checkout response on reduction.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_portal_event_test.exs test/accrue/telemetry/portal_checkout_completed_test.exs -x && cd ../accrue_portal && mix test test/accrue_portal/live/checkout_live_test.exs -x</automated>
  </verify>
  <done>The checkout UI now hands off to a real asynchronous completion pipeline instead of ending at the LiveView success state.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused telemetry coverage for the portal completion path</name>
  <files>accrue/test/accrue/webhook/default_handler_portal_event_test.exs, accrue/test/accrue/telemetry/portal_checkout_completed_test.exs</files>
  <action>Add Accrue-side tests that prove the synthetic event is reduced and `[:accrue, :portal, :checkout, :completed]` is emitted with customer/session/subscription metadata. Keep this proof isolated from the UI assertions already owned by Plan 04.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_portal_event_test.exs test/accrue/telemetry/portal_checkout_completed_test.exs -x</automated>
  </verify>
  <done>The event-reduction and telemetry contract is proven independently of the Hosted Fields UI slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout LiveView → async completion job | Customer-facing success transitions into internal event processing |
| synthetic portal event → default handler | Internal event data crosses into shared projection code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-08-01 | R | `completion_job.ex` | mitigate | Persist the synthetic event row before reduction so checkout completion remains auditable |
| T-101-08-02 | T | `default_handler.ex` | mitigate | Reuse the existing reduction path instead of forking a portal-only projector |
| T-101-08-03 | D | `checkout_live.ex` | mitigate | Enqueue asynchronously so downstream reduction cannot stall the checkout response |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/webhook/default_handler_portal_event_test.exs test/accrue/telemetry/portal_checkout_completed_test.exs -x`.
Run `cd accrue_portal && mix test test/accrue_portal/live/checkout_live_test.exs -x`.
</verification>

<success_criteria>
Successful local checkout feeds the same synthetic event and telemetry story as the rest of Accrue without bloating the checkout UI slice.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-08-SUMMARY.md`
</output>

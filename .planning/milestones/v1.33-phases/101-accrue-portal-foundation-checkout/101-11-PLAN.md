---
phase: 101-accrue-portal-foundation-checkout
plan: 11
type: execute
wave: 6
depends_on:
  - 101-06
files_modified:
  - release-please-config.json
  - examples/accrue_host/lib/accrue_host_web/router.ex
  - accrue_portal/README.md
  - accrue/guides/braintree-local-portal.md
autonomous: true
requirements:
  - BT-01
  - BT-02
  - BT-03
must_haves:
  truths:
    - "Hosts have a documented sibling-mount installation path that stays within the v1.33 session-resolved-customer boundary."
    - "The example host mounts `accrue_admin` and `accrue_portal` as sibling scopes on the locked paths."
    - "The monorepo release metadata publishes `accrue`, `accrue_admin`, and `accrue_portal` in lockstep."
  artifacts:
    - path: "release-please-config.json"
      provides: "Three-package linked-release configuration"
    - path: "examples/accrue_host/lib/accrue_host_web/router.ex"
      provides: "Canonical sibling-mount example"
    - path: "accrue_portal/README.md"
      provides: "Portal install and v1.33 boundary guide"
  key_links:
    - from: "release-please-config.json"
      to: "accrue_portal/mix.exs"
      via: "linked-version release contract"
      pattern: "accrue_portal"
    - from: "examples/accrue_host/lib/accrue_host_web/router.ex"
      to: "accrue_portal/README.md"
      via: "documented sibling mount at `/billing` and `/admin`"
      pattern: "accrue_portal \"/billing\"|accrue_admin \"/admin\""
---

<objective>
Close the release metadata, example-host, and public docs obligations for Phase 101.

Purpose: satisfy D-03, D-05, D-20, and D-24 in a dedicated integration/docs slice instead of mixing them into the remaining BT-03 UI work.
Output: linked release config, compiling sibling-mount example, and coherent packaged-vs-hand-rolled documentation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-CONTEXT.md
@.planning/phases/101-accrue-portal-foundation-checkout/101-PATTERNS.md
@release-please-config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish D-03 release ownership and the sibling-mount host example</name>
  <files>release-please-config.json, examples/accrue_host/lib/accrue_host_web/router.ex</files>
  <action>Update `release-please-config.json` so `accrue_portal` joins the linked-versions group per D-03, and update `examples/accrue_host/lib/accrue_host_web/router.ex` so `accrue_admin \"/admin\"` and `accrue_portal \"/billing\"` are mounted as sibling scopes with `session_keys: [:user_token]` per D-24. Follow the exact-existing analogs in `101-PATTERNS.md` instead of inventing a new mount shape.</action>
  <verify>
    <automated>node -e 'const cfg=require(\"./release-please-config.json\"); const group=cfg.plugins.find((p)=>p.type===\"linked-versions\"); const want=[\"accrue\",\"accrue_admin\",\"accrue_portal\"]; if(!group) throw new Error(\"missing linked-versions plugin\"); if(JSON.stringify(group.components)!==JSON.stringify(want)) throw new Error(`unexpected components: ${JSON.stringify(group.components)}`); for (const key of want) if (!cfg.packages[key]) throw new Error(`missing package ${key}`); console.log(\"linked-release config ok\");' && cd examples/accrue_host && mix compile</automated>
  </verify>
  <done>The monorepo release metadata and canonical host example both reflect the locked three-package, sibling-mount contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document the v1.33 session boundary and hand-rolled escape hatch</name>
  <files>accrue_portal/README.md, accrue/guides/braintree-local-portal.md</files>
  <action>Update the README and the hand-roll guide so they stay consistent with D-05 and D-20: `accrue_portal` is the batteries-included path, the hand-rolled guide remains valid as the escape hatch, portal access is session-resolved-customer-only in v1.33, and hosts needing emailed-link checkout must implement the `/checkout/start?token=...` bootstrap controller in their own app before redirecting into the mounted portal.</action>
  <verify>
    <automated>rg -n '/checkout/start\\?token=|session-resolved-customer-only|accrue_portal|braintree-local-portal|accrue_admin \"/admin\"|accrue_portal \"/billing\"' accrue_portal/README.md accrue/guides/braintree-local-portal.md examples/accrue_host/lib/accrue_host_web/router.ex</automated>
  </verify>
  <done>Hosts have explicit docs for the packaged portal, the hand-rolled alternative, and the v1.33 auth/magic-link boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs/example host → adopter setup | Incorrect guidance can produce insecure or broken mounts |
| release metadata → published packages | Mislinked package versions can break cross-package compatibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-11-01 | T | `release-please-config.json` | mitigate | Machine-verify the linked-versions group contains exactly the three sibling packages |
| T-101-11-02 | S | `examples/.../router.ex` | mitigate | Compile the example host after moving admin to `/admin` and portal to `/billing` per D-24 |
| T-101-11-03 | I | `README.md` / `braintree-local-portal.md` | mitigate | Document the D-20 session-only boundary and `/checkout/start?token=...` workaround explicitly so adopters do not infer unsupported magic-link behavior |
</threat_model>

<verification>
Run the Node linked-release assertion from Task 1.
Run `cd examples/accrue_host && mix compile`.
Run `rg -n '/checkout/start\?token=|session-resolved-customer-only|accrue_portal|braintree-local-portal|accrue_admin "/admin"|accrue_portal "/billing"' accrue_portal/README.md accrue/guides/braintree-local-portal.md examples/accrue_host/lib/accrue_host_web/router.ex`.
</verification>

<success_criteria>
Hosts have machine-verified release metadata, a compiling sibling-mount example, and docs that explicitly capture the v1.33 session-only boundary plus the D-20 workaround.
</success_criteria>

<output>
After completion, create `.planning/phases/101-accrue-portal-foundation-checkout/101-11-SUMMARY.md`
</output>

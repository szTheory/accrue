---
status: complete
phase: 101-accrue-portal-foundation-checkout
verification: automated
verified_by:
  - .github/workflows/ci.yml#release-gate
  - .github/workflows/ci.yml#host-integration
source:
  - 101-01-SUMMARY.md
  - 101-02-SUMMARY.md
  - 101-03-SUMMARY.md
  - 101-04-SUMMARY.md
  - 101-05-SUMMARY.md
  - 101-06-SUMMARY.md
  - 101-07-SUMMARY.md
  - 101-08-SUMMARY.md
  - 101-09-SUMMARY.md
  - 101-10-SUMMARY.md
  - 101-11-SUMMARY.md
started: 2026-05-06T00:00:00Z
updated: 2026-05-06T00:00:00Z
---

## Current Test

[testing complete]

## Verification Approach

Phase 101 ships an entire LiveView portal package. Hand-clicking through every
checkout, dashboard, subscription, payment-method and invoice flow is the
exact pain we shifted left. Every user-observable behavior below has an
automated test that runs in PR-blocking CI, so re-verifying Phase 101 = `git
push`.

CI gates that must be green to merge:

- `release-gate (accrue × Floor | Primary | Forward-compat)` — facade + adapter + telemetry + webhook tests
- `release-gate (accrue_portal × Floor | Primary | Forward-compat)` — every portal LiveView, auth, router, copy, and wrong-tenant test
- `host-integration` — example host's `mix verify.full` (cold-start + Playwright `phase102-portal-checkout`)
- `release-manifest-ssot` — guards `accrue_portal` linked-version manifest entry

## Tests

### 1. Cold Start Smoke Test
expected: |
  Example host boots from a fresh database with the new
  `accrue_checkout_sessions` migration applied; portal routes are registered
  on the host router; `GET /billing` returns 200 for a signed-in customer
  with the dashboard heading rendered from `AccruePortal.Copy.home_heading/0`.
result: pass
verified_by:
  - examples/accrue_host/test/accrue_host_web/portal_cold_start_test.exs
  - examples/accrue_host/scripts (mix verify.full → bounded phx.server boot)

### 2. Full Test Suite Green Across 3 Packages
expected: |
  `accrue`, `accrue_admin`, and `accrue_portal` ExUnit suites all pass on
  every release-gate matrix cell.
result: pass
verified_by:
  - .github/workflows/ci.yml#release-gate (accrue × Floor | Primary | Forward-compat)
  - .github/workflows/ci.yml#release-gate (accrue_admin × Floor | Primary | Forward-compat)
  - .github/workflows/ci.yml#release-gate (accrue_portal × Floor | Primary | Forward-compat)

### 3. Host Router Mount Compiles + Portal Routes Registered
expected: |
  `accrue_portal "/billing", session_keys: [:user_token]` compiles inside the
  example host router; `__routes__/0` exposes every Phase 101 path.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/router_test.exs
  - examples/accrue_host/test/accrue_host_web/portal_cold_start_test.exs

### 4. Stripe Facade Paths Unchanged
expected: |
  `Accrue.Billing.create_checkout_session/2` and
  `create_billing_portal_session/2` continue to return Stripe-shaped URLs
  for Stripe processors; portal carve-out only activates when the processor
  declares `billing_portal: %{create: false}` (Braintree).
result: pass
verified_by:
  - accrue/test/accrue/billing/checkout_session_facade_test.exs
  - accrue/test/accrue/billing/billing_portal_session_facade_test.exs

### 5. Braintree Checkout Returns Local Portal URL (Idempotent)
expected: |
  Braintree adapter rewrites `:unsupported_by_gateway` to a real
  `<portal_base_url>/billing/checkout/chk_local_*` URL; persists an
  `accrue_checkout_sessions` row; same `operation_id` returns the same URL.
result: pass
verified_by:
  - accrue/test/accrue/processor/braintree_local_portal_test.exs
  - accrue/test/accrue/checkout/local_session_test.exs

### 6. Hosted Fields Renders On Checkout Page
expected: |
  Three Braintree iframes (`number`, `expirationDate`, `cvv`), SRI-pinned
  CDN script tags, locked `AccruePortal.Copy` CTA + body, no raw card inputs.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/checkout_live_test.exs

### 7. Checkout Submission Completes Async
expected: |
  Nonce-only LiveView event → Oban completion job → synthetic
  `accrue_webhook_events` row with `accrue.portal.checkout.completed` →
  `Accrue.Webhook.DefaultHandler` reduces it → `[:accrue, :portal,
  :checkout, :completed]` telemetry fires.
result: pass
verified_by:
  - accrue/test/accrue/telemetry/portal_checkout_completed_test.exs
  - accrue/test/accrue/webhook/default_handler_portal_event_test.exs

### 8. Expired / Missing Checkout Token UI
expected: |
  Expired session row renders the centralized expired-checkout copy, not a
  500; bogus `chk_local_*` token routes to the not-found copy.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/checkout_live_test.exs

### 9. Portal Dashboard Scopes To Current Customer
expected: |
  Home LiveView renders the signed-in customer's plan summary via
  `:ensure_customer` on_mount; lazy-creates Customer row when missing.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/home_live_test.exs
  - accrue_portal/test/accrue_portal/auth_hook_test.exs

### 10. Subscriptions List + Detail Render
expected: |
  `/billing/subscriptions` lists customer-scoped subscriptions;
  `/billing/subscriptions/:id` renders detail with empty-state copy when
  none.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/subscriptions_live_test.exs
  - accrue_portal/test/accrue_portal/live/subscription_live_test.exs

### 11. Cancel-At-Period-End Flow
expected: |
  Confirmation copy explicitly states period-end retention; cancel CTA flips
  `cancel_at_period_end: true` (NOT immediate cancel).
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/subscriptions_live_test.exs
  - accrue_portal/test/accrue_portal/live/subscription_live_test.exs

### 12. Payment Methods Index + Add-Card
expected: |
  `/billing/payment-methods` lists saved cards with empty-state;
  `/billing/payment-methods/new` renders Hosted Fields and posts nonce to
  authenticated POST endpoint.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/payment_methods_live_test.exs
  - accrue_portal/test/accrue_portal/live/add_payment_method_live_test.exs

### 13. Invoice History
expected: |
  `/billing/invoices` lists current customer's invoices with empty-state
  copy when none.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/invoices_live_test.exs

### 14. Wrong-Tenant Access Returns 404 (D-19)
expected: |
  Cross-tenant subscription detail GETs and payment-method delete POSTs
  return 404; foreign invoices never leak into another tenant's list.
result: pass
verified_by:
  - accrue_portal/test/accrue_portal/live/payment_methods_wrong_tenant_test.exs
  - accrue_portal/test/accrue_portal/live/invoices_wrong_tenant_test.exs
  - accrue_portal/test/accrue_portal/live/wrong_tenant_property_test.exs

## Summary

total: 14
passed: 14
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — every user-observable behavior is gated by an automated test in CI]

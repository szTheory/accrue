---
phase: 102-coupon-discount-mapping
plan: 02
type: execute
wave: 1
depends_on:
  - 102-01
files_modified:
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/lib/accrue/billing/subscription_projection.ex
  - accrue/lib/accrue/processor/braintree.ex
  - accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs
  - accrue/test/accrue/processor/braintree_test.exs
  - accrue/test/accrue/telemetry/discount_mapping_invalid_test.exs
autonomous: true
requirements:
  - BT-05
must_haves:
  truths:
    - "A valid local promotion code is revalidated at subscription creation time before Braintree is called."
    - "The Braintree create payload uses `discounts.add[*].inherited_from_id`, not Stripe `%{coupon: ...}` semantics."
    - "A broken target discount hard-fails checkout with typed drift handling and ops telemetry instead of creating an undiscounted subscription."
    - "Successful Braintree subscription creation consumes redemption state and preserves the applied discount reference in local projection data."
  artifacts:
    - path: "accrue/lib/accrue/billing/subscription_actions.ex"
      provides: "Shared preview/submit resolution path during subscribe"
      contains: "promotion_code"
    - path: "accrue/lib/accrue/processor/braintree.ex"
      provides: "Braintree request translation with inherited discount ids"
      contains: "inherited_from_id"
    - path: "accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs"
      provides: "Wave 0 BT-05 subscribe-path coverage"
      contains: "describe \"subscribe/3 with promotion_code\""
    - path: "accrue/test/accrue/telemetry/discount_mapping_invalid_test.exs"
      provides: "Ops telemetry and typed drift-failure tests"
      contains: "discount_mapping_invalid"
  key_links:
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "accrue/lib/accrue/billing/discount_mapping_actions.ex"
      via: "authoritative create-time resolution"
      pattern: "resolve_discount_mapping"
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "create_subscription request build"
      pattern: "discounts"
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "accrue/lib/accrue/telemetry/ops.ex"
      via: "ops telemetry on drift"
      pattern: "Ops\\.emit"
---

<objective>
Wire the new local mapping resolver into `subscribe/3`, teach the Braintree adapter the correct discount payload shape, and make operator drift fail closed with telemetry.

Purpose: Satisfy BT-05 at the true create-time seam instead of bolting promo semantics onto a later mutation path.
Output: Shared submit-time resolution, Braintree `discounts.add[*].inherited_from_id` payloads, drift telemetry, and targeted subscribe-path tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/102-coupon-discount-mapping/102-CONTEXT.md
@.planning/phases/102-coupon-discount-mapping/102-RESEARCH.md
@.planning/phases/102-coupon-discount-mapping/102-VALIDATION.md
@.planning/phases/102-coupon-discount-mapping/102-01-PLAN.md
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/processor/braintree.ex
@accrue/lib/accrue/telemetry/ops.ex
@accrue/test/accrue/processor/braintree_test.exs
@.planning/milestones/v1.32-phases/097-advanced-subscription-lifecycle/097-01-SUMMARY.md

<interfaces>
From `accrue/lib/accrue/billing/subscription_actions.ex`:
```elixir
defp build_subscription_request(%Customer{} = customer, item_params, trial_end, opts) do
  if Processor.__impl__() == Accrue.Processor.Braintree do
    case Keyword.get(opts, :payment_method) do
      %{vault_acquisition: %{reference: ref}} when is_binary(ref) ->
        {:ok,
         %{
           payment_method: %{vault_acquisition: %{reference: ref}},
           items: [item_params]
         }}
```

From `accrue/lib/accrue/billing/subscription_actions.ex`:
```elixir
defp maybe_put_coupon(params, opts) do
  case Keyword.get(opts, :coupon) do
    nil -> params
    id when is_binary(id) -> Map.put(params, :discounts, [%{coupon: id}])
  end
end
```

From `accrue/lib/accrue/processor/braintree.ex`:
```elixir
def build_request(params) do
  %{
    payment_method_token: token,
    plan_id: plan_id
  }
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revalidate promotion codes inside the Braintree subscribe flow</name>
  <files>accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/billing/subscription_projection.ex, accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs</files>
  <read_first>.planning/phases/102-coupon-discount-mapping/102-CONTEXT.md, .planning/phases/102-coupon-discount-mapping/102-RESEARCH.md, .planning/phases/102-coupon-discount-mapping/102-VALIDATION.md, accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/billing/subscription_projection.ex, accrue/lib/accrue/billing/discount_mapping_actions.ex, accrue/test/accrue/billing/subscription_actions_test.exs</read_first>
  <behavior>
    - Test 1: `Billing.subscribe/3` on Braintree with `promotion_code: "SPRING25"` re-runs local validation in core before the processor call per D-09/D-11/D-19.
    - Test 2: A valid mapping consumes redemption state inside the same subscription-create transaction, keeping `times_redeemed` honest for successful creates.
    - Test 3: A drift failure returns `%Accrue.Error.DiscountMappingInvalid{}` and does not create a local subscription at the undiscounted amount per D-15/D-18.
  </behavior>
  <action>Extend `SubscriptionActions.subscribe/3` so the Braintree branch accepts `promotion_code:` and resolves it through the Plan 01 resolver before `Processor.__impl__().create_subscription/2`. Keep the existing Stripe `:coupon` behavior for non-Braintree paths, but do not reuse `maybe_put_coupon/2` for Braintree. In `build_subscription_request/4`, merge the resolved mapping into the Braintree request payload shape consumed by the adapter and include enough information for the post-success path to call the Plan 01 redemption helper before recording the `"subscription.created"` event. Update `subscription_projection.ex` so any provider-returned `discount_id` or equivalent Braintree discount reference is preserved instead of being dropped to `nil`. Add `accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs` for validation lane `102-W0-02`, covering valid create, local invalid-code atoms, drift hard-fail, and honest redemption mutation.</action>
  <acceptance_criteria>
    - `rg -n 'promotion_code' accrue/lib/accrue/billing/subscription_actions.ex accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs` shows create-time promotion-code handling.
    - `rg -n 'resolve_discount_mapping|record_discount_mapping_redemption|consume_discount_mapping' accrue/lib/accrue/billing/subscription_actions.ex` shows the shared core resolution and redemption path.
    - `rg -n 'DiscountMappingInvalid' accrue/lib/accrue/billing/subscription_actions.ex accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs` proves typed drift handling is part of subscribe.
    - `rg -n 'discount_id' accrue/lib/accrue/billing/subscription_projection.ex accrue/test/accrue/billing/braintree_discount_mapping_subscribe_test.exs` shows projection coverage for the mapped Braintree discount id.
    - The targeted subscribe-path test command passes with the repo-local `TMPDIR` override.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mkdir -p /Users/jon/projects/accrue/.tmp/phase102 && TMPDIR=/Users/jon/projects/accrue/.tmp/phase102 mix test test/accrue/billing/braintree_discount_mapping_subscribe_test.exs</automated>
  </verify>
  <done>The Braintree subscribe path and persisted projection now enforce BT-05 at create time, using one core resolution path and no undiscounted fallback on drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate Braintree discount payloads correctly and emit ops telemetry on drift</name>
  <files>accrue/lib/accrue/processor/braintree.ex, accrue/test/accrue/processor/braintree_test.exs, accrue/test/accrue/telemetry/discount_mapping_invalid_test.exs</files>
  <read_first>.planning/phases/102-coupon-discount-mapping/102-RESEARCH.md, accrue/lib/accrue/processor/braintree.ex, accrue/lib/accrue/telemetry/ops.ex, accrue/test/accrue/processor/braintree_test.exs, accrue/test/accrue/telemetry/ops_test.exs</read_first>
  <behavior>
    - Test 1: `Accrue.Processor.Braintree.build_request/1` emits `%{discounts: %{add: [%{inherited_from_id: ...}]}}` for resolved mappings per D-20.
    - Test 2: Drift failures emit one ops event under `[:accrue, :ops, :discount_mapping_invalid]` with allowlisted metadata only.
    - Test 3: No code path silently strips the discount and proceeds with subscription creation after a previously valid preview.
  </behavior>
  <action>Teach `Accrue.Processor.Braintree.build_request/1` to accept the resolved mapping shape from `SubscriptionActions` and translate it into Braintree’s documented `discounts: %{add: [%{inherited_from_id: discount_id}]}` payload, while preserving the current `payment_method_token` and `plan_id` translation. Extend `accrue/test/accrue/processor/braintree_test.exs` to assert the exact build-request structure and any returned `discount_id` projection. Add `accrue/test/accrue/telemetry/discount_mapping_invalid_test.exs` as the owner of validation lane `102-W0-03`, attaching to `[:accrue, :ops, :discount_mapping_invalid]` and proving that drift emits alertable metadata such as `mapping_id`, `code`, `discount_id`, and `operation_id` without logging or returning raw processor payloads. The drift path should originate in the core subscribe flow; do not add browser-only telemetry.</action>
  <acceptance_criteria>
    - `rg -n 'inherited_from_id|discounts: %\\{add:' accrue/lib/accrue/processor/braintree.ex accrue/test/accrue/processor/braintree_test.exs` proves the Braintree request shape changed from Stripe semantics.
    - `rg -n 'discount_mapping_invalid' accrue/test/accrue/telemetry/discount_mapping_invalid_test.exs` shows the dedicated ops telemetry lane exists.
    - `rg -n 'operation_id|mapping_id|discount_id|code' accrue/test/accrue/telemetry/discount_mapping_invalid_test.exs` proves the allowlisted telemetry metadata contract.
    - `rg -n '%\\{coupon:' accrue/lib/accrue/processor/braintree.ex` returns no matches.
    - The targeted processor + telemetry tests pass with the repo-local `TMPDIR` override.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mkdir -p /Users/jon/projects/accrue/.tmp/phase102 && TMPDIR=/Users/jon/projects/accrue/.tmp/phase102 mix test test/accrue/processor/braintree_test.exs test/accrue/telemetry/discount_mapping_invalid_test.exs test/accrue/billing/braintree_discount_mapping_subscribe_test.exs</automated>
  </verify>
  <done>The Braintree adapter emits the correct discount payload shape, drift is alertable via ops telemetry, and the subscribe path cannot silently drop a valid discount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout submit -> `Billing.subscribe/3` | User-submitted promo codes cross into the authoritative subscription-create path |
| core resolver -> Braintree subscription payload | Local mapping state is translated into a gateway request that must not drift or silently degrade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02 | T | `SubscriptionActions.build_subscription_request/4` | mitigate | Re-resolve `promotion_code` at submit time and build the Braintree request from the current mapping row instead of trusting preview assigns. |
| T-102-03 | R | Braintree create path | mitigate | Hard-fail on `%Accrue.Error.DiscountMappingInvalid{}` and keep redemption mutation inside the success transaction so no undiscounted subscription is created after a valid preview. |
| T-102-04 | I | Drift handling + telemetry | mitigate | Emit `[:accrue, :ops, :discount_mapping_invalid]` with allowlisted metadata only; keep user-facing copy generic and do not surface raw processor payloads. |
</threat_model>

<verification>
Run the BT-05 core lanes with the local temp-space workaround:

```bash
cd /Users/jon/projects/accrue/accrue
mkdir -p /Users/jon/projects/accrue/.tmp/phase102
TMPDIR=/Users/jon/projects/accrue/.tmp/phase102 mix test \
  test/accrue/billing/braintree_discount_mapping_subscribe_test.exs \
  test/accrue/processor/braintree_test.exs \
  test/accrue/telemetry/discount_mapping_invalid_test.exs
```
</verification>

<success_criteria>
- `subscribe/3` resolves local promotion codes in core for Braintree and does not rely on Stripe `%{coupon: ...}` semantics.
- `Accrue.Processor.Braintree.build_request/1` emits `discounts.add[*].inherited_from_id`.
- Drift produces `%Accrue.Error.DiscountMappingInvalid{}` plus `[:accrue, :ops, :discount_mapping_invalid]`.
- `braintree_discount_mapping_subscribe_test.exs` and `discount_mapping_invalid_test.exs` exist and pass with the repo-local `TMPDIR` override.
</success_criteria>

<output>
After completion, create `.planning/phases/102-coupon-discount-mapping/102-02-SUMMARY.md`
</output>

---
phase: 103-metering-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/meter_definition.ex
  - accrue/lib/accrue/billing/meter_definitions.ex
  - accrue/lib/accrue/billing/metered_renewal.ex
  - accrue/lib/accrue/billing/metered_renewal_actions.ex
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/priv/repo/migrations/20260503100000_create_accrue_meter_definitions_and_metered_renewals.exs
  - accrue/test/accrue/billing/meter_definitions_test.exs
  - accrue/test/accrue/webhook/braintree_metered_renewal_test.exs
autonomous: true
requirements:
  - BT-06
  - BT-07
must_haves:
  truths:
    - "Hosts still report raw usage through `Accrue.Billing.report_usage/3` instead of sending subscription item identifiers on every call."
    - "Braintree renewal classification creates one immutable local renewal window per `subscription + period_start + period_end` only after lifecycle evidence shows the cycle advanced."
    - "Meter definitions bind event names to explicit billable subscription targets and renewal behavior rather than treating the raw event name as the billing contract."
    - "Renewal windows persist UTC period bounds and reject silent rewrites when later subscription or meter-definition edits occur."
  artifacts:
    - path: "accrue/lib/accrue/billing/meter_definition.ex"
      provides: "Canonical local meter-definition schema keyed to an ingress event name and billable target"
    - path: "accrue/lib/accrue/billing/metered_renewal.ex"
      provides: "Immutable renewal-window schema and state anchor for one subscription period"
    - path: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      provides: "Renewal-window creation and webhook-triggered enqueue orchestration"
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "Braintree renewal classification wired into the webhook-first convergence path"
    - path: "accrue/priv/repo/migrations/20260503100000_create_accrue_meter_definitions_and_metered_renewals.exs"
      provides: "Tables, immutable-window uniqueness, and supporting indexes for meter definitions and renewal windows"
  key_links:
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      via: "Braintree lifecycle reducer opens or advances the renewal window only after canonical subscription fetch proves a new cycle"
      pattern: "MeteredRenewalActions\\."
    - from: "accrue/lib/accrue/billing/meter_definitions.ex"
      to: "accrue/lib/accrue/billing/subscription_item.ex"
      via: "meter-definition validation binds one ingress event name to one concrete billable target"
      pattern: "subscription_item"
    - from: "accrue/lib/accrue/billing/metered_renewal.ex"
      to: "accrue/priv/repo/migrations/20260503100000_create_accrue_meter_definitions_and_metered_renewals.exs"
      via: "database uniqueness on subscription + period_start + period_end enforces the immutable audit anchor"
      pattern: "unique_index"
---

<objective>
Create the local billing contracts that Phase 103 depends on: explicit meter definitions and immutable renewal windows.

Purpose: Make BT-06 and BT-07 start from Accrue-owned local truth rather than from raw event names or Braintree timing guesses.
Output: New schemas, a renewal-window action layer, webhook classification, and deterministic tests that pin the idempotent renewal contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/103-metering-engine/103-CONTEXT.md
@.planning/phases/103-metering-engine/103-RESEARCH.md
@.planning/phases/103-metering-engine/103-PATTERNS.md
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/meter_event.ex
@accrue/lib/accrue/billing/meter_event_actions.ex
@accrue/lib/accrue/billing/subscription.ex
@accrue/lib/accrue/billing/subscription_item.ex
@accrue/lib/accrue/webhook/default_handler.ex

<interfaces>
From `accrue/lib/accrue/billing.ex`:
```elixir
alias Accrue.Billing.{MeterEventActions, SubscriptionActions}
```

From `accrue/lib/accrue/billing/meter_event_actions.ex`:
```elixir
@spec report_usage(Customer.t() | String.t(), String.t(), keyword()) ::
        {:ok, MeterEvent.t()} | {:error, term()}
```

From `accrue/lib/accrue/billing/subscription.ex`:
```elixir
field(:processor_id, :string)
field(:current_period_start, :utc_datetime_usec)
field(:current_period_end, :utc_datetime_usec)
field(:status, Ecto.Enum, values: @statuses)
```

From `accrue/lib/accrue/billing/subscription_item.ex`:
```elixir
field(:processor_id, :string)
field(:price_id, :string)
field(:processor_plan_id, :string)
field(:current_period_start, :utc_datetime_usec)
field(:current_period_end, :utc_datetime_usec)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for local meter definitions and webhook-opened renewal windows</name>
  <files>accrue/test/accrue/billing/meter_definitions_test.exs, accrue/test/accrue/webhook/braintree_metered_renewal_test.exs</files>
  <behavior>
    - Test 1: `report_usage/3` remains the ingress seam, and meter-definition lookup binds the event name to one metered subscription item instead of requiring host-supplied `subscription_item_id`.
    - Test 2: Braintree renewal lifecycle events create exactly one renewal row for a `subscription + period_start + period_end` tuple even under webhook replay or out-of-order duplicate delivery.
    - Test 3: renewal rows snapshot UTC bounds and preserve prior-period truth when the subscription item or meter definition changes later.
  </behavior>
  <action>Write failing ExUnit coverage per D-01 through D-07 and D-08 through D-15. Pattern analogs from `103-PATTERNS.md`: mirror `meter_event_actions.ex` for local-first durable writes and `default_handler.ex` for webhook-first convergence. In `meter_definitions_test.exs`, pin the host-light meter-definition contract: one ingress `event_name`, one billable target, one aggregation mode, explicit validation errors for missing target bindings, and no API that requires hosts to pass `subscription_item_id` or `price_id` on each usage call. In `braintree_metered_renewal_test.exs`, drive `Accrue.Webhook.DefaultHandler` with Braintree lifecycle payloads that prove a cycle advanced, assert a single immutable renewal window keyed by subscription + period bounds, assert webhook replay stays idempotent, and assert pre-renewal events or stale events do not open a billable window. Reference the locked decisions in the test names and comments where useful: D-01, D-02, D-04, D-05, D-08, D-10, D-11, and D-14.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/meter_definitions_test.exs test/accrue/webhook/braintree_metered_renewal_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Executable tests fail before implementation and pin the local meter-definition contract plus immutable renewal-window creation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement meter-definition storage and webhook-primary renewal orchestration</name>
  <files>accrue/lib/accrue/billing/meter_definition.ex, accrue/lib/accrue/billing/meter_definitions.ex, accrue/lib/accrue/billing/metered_renewal.ex, accrue/lib/accrue/billing/metered_renewal_actions.ex, accrue/lib/accrue/webhook/default_handler.ex, accrue/priv/repo/migrations/20260503100000_create_accrue_meter_definitions_and_metered_renewals.exs</files>
  <action>Create `Accrue.Billing.MeterDefinition` and `Accrue.Billing.MeterDefinitions` as the Accrue-owned billability layer per D-02, D-03, and D-04. Pattern analogs from `103-PATTERNS.md`: mirror `meter_event_actions.ex` for idempotent local-first write flow and `default_handler.ex` for canonical-fetch webhook classification. The schema must store the ingress `event_name`, the bound subscription-item or price reference, the aggregation formula, active status, and a serialized billing snapshot contract so later edits do not silently rewrite historical usage (D-05). Create `Accrue.Billing.MeteredRenewal` with a durable state enum that starts at `pending`, stores `subscription_id`, `customer_id`, `period_start`, `period_end`, `trigger_source`, `snapshot`, `last_processor_event_id`, and a unique index on `subscription_id + period_start + period_end` to enforce D-11 and D-27. Implement `Accrue.Billing.MeteredRenewalActions` to open or fetch the renewal window transactionally, record an `accrue_events` entry in the same transaction, and enqueue later processing without charging immediately. In `Accrue.Webhook.DefaultHandler`, classify the Braintree renewal path from canonical subscription fetches instead of raw payload trust, open the immutable renewal record only when the cycle truly advanced per D-09 and D-10, persist UTC boundaries only per D-14, and no-op stale or non-renewal lifecycle events. Do not add scheduler-first logic or raw-event-name billing shortcuts (D-07, D-15).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/meter_definitions_test.exs test/accrue/webhook/braintree_metered_renewal_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Phase 103 has first-class local meter definitions and immutable renewal-window truth, and the webhook path can open a replay-safe renewal anchor without charging yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host usage call -> meter-definition lookup | Untrusted event names and values enter the local billability contract |
| Braintree webhook -> renewal reducer | External lifecycle signals attempt to open a billable renewal window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | T | `accrue/lib/accrue/billing/meter_definitions.ex` | mitigate | Validate that each meter definition binds to one concrete local billable target and reject stringly-typed orphan definitions per D-02 and D-04. |
| T-103-02 | T | `accrue/lib/accrue/webhook/default_handler.ex` | mitigate | Refetch canonical subscription state before opening a renewal window and ignore stale or non-advancing lifecycle events per D-09 and D-10. |
| T-103-03 | R | `accrue/lib/accrue/billing/metered_renewal_actions.ex` | mitigate | Record one immutable renewal window plus a matching event row in the same transaction so replay and later recovery remain attributable per D-11. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/meter_definitions_test.exs test/accrue/webhook/braintree_metered_renewal_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
Meter definitions are local, explicit, and billable-target-backed; Braintree renewal evidence opens exactly one immutable renewal window per closed period; and Phase 103 keeps `report_usage/3` as the host ingress seam.
</success_criteria>

<output>
After completion, create `.planning/phases/103-metering-engine/103-01-SUMMARY.md`
</output>

---
phase: 103-metering-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 103-01
files_modified:
  - accrue/lib/accrue/billing/meter_event.ex
  - accrue/lib/accrue/billing/metered_renewal.ex
  - accrue/lib/accrue/billing/metered_renewal_actions.ex
  - accrue/lib/accrue/billing/metered_renewal_invoice.ex
  - accrue/lib/accrue/jobs/process_metered_renewal.ex
  - accrue/priv/repo/migrations/20260503101000_add_billing_resolution_fields_to_meter_events_and_renewals.exs
  - accrue/test/accrue/billing/metered_renewal_invoice_test.exs
  - accrue/test/accrue/billing/meter_event_resolution_test.exs
  - accrue/test/accrue/jobs/process_metered_renewal_test.exs
autonomous: true
requirements:
  - BT-06
  - BT-07
must_haves:
  truths:
    - "Closed renewal windows aggregate only the meter events eligible for that window and classify unmatched or unusable events explicitly instead of dropping them."
    - "Accrue authors local invoice items first, with one decomposed usage breakdown for the renewal window before any Braintree sale is attempted."
    - "Renewal processing snapshots period inputs so later plan swaps or meter-definition edits do not rewrite the line-item explanation for a closed window."
  artifacts:
    - path: "accrue/lib/accrue/billing/metered_renewal_invoice.ex"
      provides: "Aggregation and local invoice-item authoring for one metered renewal window"
    - path: "accrue/lib/accrue/jobs/process_metered_renewal.ex"
      provides: "Oban worker that aggregates one closed renewal window and authors local invoice rows replay-safely"
    - path: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      provides: "Window-level orchestration that records invoice authoring completion and usage outcomes"
    - path: "accrue/lib/accrue/billing/meter_event.ex"
      provides: "Operator-visible billing resolution fields for matched, unmatched, and unusable meter events"
    - path: "accrue/priv/repo/migrations/20260503101000_add_billing_resolution_fields_to_meter_events_and_renewals.exs"
      provides: "Schema support for billing-status classification and renewal invoice references"
  key_links:
    - from: "accrue/lib/accrue/billing/metered_renewal_invoice.ex"
      to: "accrue/lib/accrue/billing/invoice_item.ex"
      via: "aggregated usage becomes concrete local invoice items with period bounds and billable refs"
      pattern: "InvoiceItem"
    - from: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      to: "accrue/lib/accrue/billing/meter_event.ex"
      via: "meter events are marked matched, unmatched, or unusable for operator visibility"
      pattern: "billing_status|billing_error"
    - from: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      to: "accrue/lib/accrue/billing/metered_renewal.ex"
      via: "invoice-authoring state and snapshot metadata are persisted on the renewal row"
      pattern: "invoice_|snapshot"
    - from: "accrue/lib/accrue/jobs/process_metered_renewal.ex"
      to: "accrue/lib/accrue/billing/metered_renewal_invoice.ex"
      via: "the worker owns BT-06 aggregation and local invoice authoring for one closed renewal window"
      pattern: "MeteredRenewalInvoice\\."
---

<objective>
Turn a closed renewal window into canonical local billing truth before any external settlement.

Purpose: Satisfy the local-engine half of BT-06 and prepare BT-07 from a complete, auditable invoice decomposition rather than from opaque overage blobs.
Output: Renewal-window aggregation logic, explicit meter-event billing outcomes, and local invoice authoring tests that prove the usage breakdown.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/103-metering-engine/103-CONTEXT.md
@.planning/phases/103-metering-engine/103-RESEARCH.md
@.planning/phases/103-metering-engine/103-PATTERNS.md
@.planning/phases/103-metering-engine/103-01-PLAN.md
@accrue/lib/accrue/billing/meter_event.ex
@accrue/lib/accrue/billing/invoice_item.ex
@accrue/test/accrue/billing/invoice_projection_test.exs

<interfaces>
From `accrue/lib/accrue/billing/meter_event.ex`:
```elixir
schema "accrue_meter_events" do
  field(:event_name, :string)
  field(:value, :integer)
  field(:occurred_at, :utc_datetime_usec)
  field(:stripe_status, :string, default: "pending")
end
```

From `accrue/lib/accrue/billing/invoice_item.ex`:
```elixir
field(:description, :string)
field(:amount_minor, :integer)
field(:currency, :string)
field(:period_start, :utc_datetime_usec)
field(:period_end, :utc_datetime_usec)
field(:price_ref, :string)
field(:subscription_item_ref, :string)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for renewal-window aggregation and explicit meter-event outcomes</name>
  <files>accrue/test/accrue/billing/metered_renewal_invoice_test.exs, accrue/test/accrue/billing/meter_event_resolution_test.exs, accrue/test/accrue/jobs/process_metered_renewal_test.exs</files>
  <behavior>
    - Test 1: a closed renewal window aggregates only events that match an active meter definition and fall within that period.
    - Test 2: unmatched or unusable meter events are marked durably with operator-visible outcomes instead of disappearing from the ledger.
    - Test 3: `Accrue.Jobs.ProcessMeteredRenewal` is the Oban-owned BT-06 path that aggregates one closed renewal window and authors local invoice rows idempotently under replay.
    - Test 4: the resulting local invoice decomposition preserves item-level descriptions, period bounds, price or subscription-item references, and a stable snapshot even if current subscription data later changes.
  </behavior>
  <action>Write failing tests per D-05, D-06, D-16, D-17, D-18, and D-19. Pattern analogs from `103-PATTERNS.md`: mirror `meter_event_actions.ex` for durable local-first idempotency and `meter_events_reconciler.ex` for the worker shell. `metered_renewal_invoice_test.exs` must cover window selection, definition-based aggregation, quantity and amount math, one local invoice with decomposed invoice items, and a snapshot that survives later plan or meter-definition changes. `meter_event_resolution_test.exs` must assert explicit durable billing outcomes such as `matched`, `unmatched`, and `unusable`, and must verify that renewal processing never silently skips unusable rows. `process_metered_renewal_test.exs` must prove the Oban worker is the BT-06 aggregation path, replays idempotently on the same renewal window, and never authors duplicate invoice rows. Use real renewal-window fixtures from Plan 01 rather than ad-hoc stringly fixtures.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/metered_renewal_invoice_test.exs test/accrue/billing/meter_event_resolution_test.exs test/accrue/jobs/process_metered_renewal_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Aggregation and event-resolution expectations are pinned with deterministic tests before authoring code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement worker-owned aggregation, invoice-item authoring, and explicit event-resolution state</name>
  <files>accrue/lib/accrue/billing/meter_event.ex, accrue/lib/accrue/billing/metered_renewal.ex, accrue/lib/accrue/billing/metered_renewal_actions.ex, accrue/lib/accrue/billing/metered_renewal_invoice.ex, accrue/lib/accrue/jobs/process_metered_renewal.ex, accrue/priv/repo/migrations/20260503101000_add_billing_resolution_fields_to_meter_events_and_renewals.exs</files>
  <action>Extend `Accrue.Billing.MeterEvent` with billing-resolution fields such as `meter_definition_id`, `metered_renewal_id`, `billing_status`, and `billing_error`, then add the matching migration so unmatched or unusable events remain operator-visible per D-06 and D-31. Add `Accrue.Billing.MeteredRenewalInvoice` to rate one closed renewal window from the meter-definition snapshot created in Plan 01, aggregate matching meter events, and author one local invoice plus decomposed invoice items before any gateway call per D-16, D-17, and D-18. Add `Accrue.Jobs.ProcessMeteredRenewal` as the worker-owned BT-06 pipeline, patterned after the reconciler shell in `103-PATTERNS.md`: restore Oban middleware, load one closed renewal window, run aggregation and local invoice authoring idempotently, and persist invoice-authoring completion on the renewal row. Update `Accrue.Billing.MeteredRenewalActions` so Plan 01's renewal-opening path enqueues this worker, invoice authoring is transactionally recorded on the renewal row, meter events move into explicit billing outcomes, and later edits to subscription items or meter definitions cannot rewrite the closed-window explanation because the renewal snapshot is now authoritative (D-05). Do not use Braintree subscription add-ons, do not collapse the charge explanation into one description-only blob, and do not create one external charge per meter (D-20).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/metered_renewal_invoice_test.exs test/accrue/billing/meter_event_resolution_test.exs test/accrue/jobs/process_metered_renewal_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Closed renewal windows now produce canonical local invoice truth through an Oban-owned aggregation pipeline, with explicit event-resolution outcomes ready for later settlement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored meter events -> renewal aggregator | Previously accepted usage facts are reinterpreted into billable outcomes |
| local invoice authoring -> persisted financial ledger | Aggregation results become durable invoice and invoice-item records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04 | T | `accrue/lib/accrue/billing/metered_renewal_invoice.ex` | mitigate | Rate only meter events selected by the renewal snapshot and active definition binding so later config drift cannot rewrite closed-window math. |
| T-103-05 | R | `accrue/lib/accrue/billing/meter_event.ex` | mitigate | Persist explicit `billing_status` and `billing_error` fields so unmatched or unusable events remain auditable instead of vanishing during aggregation. |
| T-103-06 | I | `accrue/lib/accrue/billing/metered_renewal_actions.ex` | mitigate | Finalize local invoice items before settlement and keep the local ledger canonical, preventing opaque processor-only charge explanations. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/metered_renewal_invoice_test.exs test/accrue/billing/meter_event_resolution_test.exs test/accrue/jobs/process_metered_renewal_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
Each closed renewal window can be converted into one local invoice with decomposed invoice items, and every meter event ends in an explicit, operator-visible billing outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/103-metering-engine/103-02-SUMMARY.md`
</output>

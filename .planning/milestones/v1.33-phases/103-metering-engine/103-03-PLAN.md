---
phase: 103-metering-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 103-01
  - 103-02
files_modified:
  - accrue/lib/accrue/billing/metered_charge_attempt.ex
  - accrue/lib/accrue/billing/metered_charge_attempts.ex
  - accrue/lib/accrue/billing/metered_renewal.ex
  - accrue/lib/accrue/billing/metered_renewal_actions.ex
  - accrue/lib/accrue/billing/charge_actions.ex
  - accrue/lib/accrue/processor/braintree.ex
  - accrue/lib/accrue/errors.ex
  - accrue/lib/accrue/jobs/process_metered_renewal.ex
  - accrue/priv/repo/migrations/20260503102000_create_accrue_metered_charge_attempts.exs
  - accrue/test/accrue/billing/metered_charge_attempts_test.exs
  - accrue/test/accrue/jobs/process_metered_renewal_test.exs
  - accrue/test/accrue/processor/braintree_test.exs
autonomous: true
requirements:
  - BT-06
  - BT-07
must_haves:
  truths:
    - "A renewal window settles through one Braintree `Transaction.sale` against the customer’s current default or explicitly resolved vaulted payment method."
    - "Recovery and replay target the same renewal-window charge unit idempotently, so payment-method repair or operator replay does not create a second sale for the same period."
    - "Hard declines, missing payment methods, and retryable transient failures transition into distinct durable states that operators can act on."
  artifacts:
    - path: "accrue/lib/accrue/billing/metered_charge_attempt.ex"
      provides: "Durable settlement-attempt record tied to one renewal window"
    - path: "accrue/lib/accrue/jobs/process_metered_renewal.ex"
      provides: "Unique renewal processor job extended from Plan 02 to continue from worker-owned aggregation into settlement"
    - path: "accrue/lib/accrue/processor/braintree.ex"
      provides: "Braintree off-session sale callback path for renewal-window settlement"
    - path: "accrue/lib/accrue/errors.ex"
      provides: "Typed metered-settlement and recovery errors"
    - path: "accrue/priv/repo/migrations/20260503102000_create_accrue_metered_charge_attempts.exs"
      provides: "Canonical charge-attempt table and indexes for replay-safe settlement state"
  key_links:
    - from: "accrue/lib/accrue/jobs/process_metered_renewal.ex"
      to: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      via: "the worker loads one renewal window and settles it idempotently"
      pattern: "MeteredRenewalActions\\."
    - from: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      to: "accrue/lib/accrue/processor/braintree.ex"
      via: "settlement creates one Braintree sale after local invoice authoring succeeds"
      pattern: "create_charge|sale"
    - from: "accrue/lib/accrue/billing/metered_charge_attempts.ex"
      to: "accrue/lib/accrue/errors.ex"
      via: "typed failure states distinguish retryable, payment-method-repair, and exhausted outcomes"
      pattern: "Accrue\\.Error\\."
---

<objective>
Settle one renewal-window-owned metered bill through Braintree with replay-safe recovery.

Purpose: Close BT-07 honestly: local billing truth first, one external sale second, and no duplicate settlement on replay.
Output: Charge-attempt persistence, a renewal processor job, typed recovery semantics, and Braintree sale tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/103-metering-engine/103-CONTEXT.md
@.planning/phases/103-metering-engine/103-RESEARCH.md
@.planning/phases/103-metering-engine/103-PATTERNS.md
@.planning/phases/103-metering-engine/103-01-PLAN.md
@.planning/phases/103-metering-engine/103-02-PLAN.md
@accrue/lib/accrue/billing/charge_actions.ex
@accrue/lib/accrue/processor/braintree.ex
@accrue/lib/accrue/errors.ex

<interfaces>
From `accrue/lib/accrue/billing/charge_actions.ex`:
```elixir
{:error, %Accrue.Error.NoDefaultPaymentMethod{...}}
subject_uuid = Idempotency.subject_uuid(:create_charge, op_id)
idem_key = Idempotency.key(:create_charge, subject_uuid, op_id)
```

From `accrue/lib/accrue/processor/braintree.ex`:
```elixir
def fetch(:subscription, id), do: retrieve_subscription(id, [])
def fetch(:refund, id), do: retrieve_refund(id, [])
def create_charge(_params, _opts), do: {:error, unsupported()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for renewal settlement, replay safety, and typed recovery states</name>
  <files>accrue/test/accrue/billing/metered_charge_attempts_test.exs, accrue/test/accrue/jobs/process_metered_renewal_test.exs, accrue/test/accrue/processor/braintree_test.exs</files>
  <behavior>
    - Test 1: one processed renewal window creates exactly one Braintree sale and one charge-attempt record, even if the job is replayed with the same renewal id.
    - Test 2: transient processor failures move the renewal into `retry_scheduled`, while hard declines or missing payment methods move it into `awaiting_payment_method` or `failed_exhausted`.
    - Test 3: after payment-method repair, replay uses the same renewal-window charge unit and the customer’s current repaired default payment method rather than the originally failed payment method.
  </behavior>
  <action>Write failing tests per D-21 through D-27. Pattern analogs from `103-PATTERNS.md`: mirror `charge_actions.ex` for idempotent money movement and `errors.ex` for typed failure states. `metered_charge_attempts_test.exs` must pin the durable state machine around `pending`, `retry_scheduled`, `awaiting_payment_method`, `paid`, and `failed_exhausted`, plus the audit requirement that original failed-attempt details remain visible after repair. `process_metered_renewal_test.exs` must prove that the Plan 02 worker now continues from worker-owned aggregation into settlement on the same renewal window, creates one settlement unit and one idempotent replay target, and calls the same renewal again after payment-method repair instead of creating a second charge unit. Extend `braintree_test.exs` with the exact sale payload expectations for a renewal-window off-session `Transaction.sale`, including customer or payment-method resolution, amount, idempotency metadata, and failure translation into typed errors rather than raw SDK tuples.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/metered_charge_attempts_test.exs test/accrue/jobs/process_metered_renewal_test.exs test/accrue/processor/braintree_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Settlement and recovery behavior is pinned with automated proof before Braintree charge code lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement charge-attempt persistence, renewal settlement worker, and Braintree sale support</name>
  <files>accrue/lib/accrue/billing/metered_charge_attempt.ex, accrue/lib/accrue/billing/metered_charge_attempts.ex, accrue/lib/accrue/billing/metered_renewal.ex, accrue/lib/accrue/billing/metered_renewal_actions.ex, accrue/lib/accrue/billing/charge_actions.ex, accrue/lib/accrue/processor/braintree.ex, accrue/lib/accrue/errors.ex, accrue/lib/accrue/jobs/process_metered_renewal.ex, accrue/priv/repo/migrations/20260503102000_create_accrue_metered_charge_attempts.exs</files>
  <action>Create `Accrue.Billing.MeteredChargeAttempt` and `Accrue.Billing.MeteredChargeAttempts` as the durable settlement ledger for one renewal window per D-21 through D-27. The row must carry `metered_renewal_id`, deterministic `subject_uuid`, processor sale id, attempted payment-method id, failure class, retry timing, and the original failed-attempt details for audit. Extend `Accrue.Jobs.ProcessMeteredRenewal` from Plan 02's BT-06 aggregation worker into the full replay-safe pipeline: restore Oban operation id, load the closed renewal window, no-op if aggregation/invoice authoring is still incomplete, and otherwise continue into settlement on the same renewal-owned job contract. In `Accrue.Billing.MeteredRenewalActions`, derive the settlement idempotency key from the renewal window, resolve the current default payment method at replay time per D-24, and move the renewal through `pending`, `retry_scheduled`, `awaiting_payment_method`, `paid`, or `failed_exhausted` based on typed failure classification. Extend `Accrue.Processor.Braintree` with a real Braintree sale path for metered renewal settlement instead of the current unsupported charge stub, but keep the architecture centered on one sale per renewal window and one local decomposition beneath it (D-17 through D-20). Add typed errors in `accrue/lib/accrue/errors.ex` for metered settlement conflicts or missing prerequisites when the existing `NoDefaultPaymentMethod` error is not sufficient. Do not rely on Oban uniqueness or Braintree duplicate checks as the primary dedupe guard; the renewal record remains canonical (D-27).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/metered_charge_attempts_test.exs test/accrue/jobs/process_metered_renewal_test.exs test/accrue/processor/braintree_test.exs --warnings-as-errors</automated>
  </verify>
  <done>One closed renewal window can now settle through one replay-safe Braintree sale with typed recovery behavior and no duplicate external charges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renewal worker -> Braintree sale API | Local financial truth becomes an external money movement |
| repaired payment-method state -> replay | Later customer or operator repair reopens a prior failed settlement unit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-07 | T | `accrue/lib/accrue/billing/metered_renewal_actions.ex` | mitigate | Derive a deterministic idempotency subject from the renewal window and reject duplicate settlement attempts against the same period per D-23 and D-27. |
| T-103-08 | D | `accrue/lib/accrue/billing/metered_charge_attempts.ex` | mitigate | Distinguish retryable failures from payment-method-repair states so the system does not loop hard declines forever per D-22 and D-26. |
| T-103-09 | R | `accrue/lib/accrue/jobs/process_metered_renewal.ex` | mitigate | Persist every attempt and failure transition on the same renewal-owned audit chain so later replay remains attributable per D-19 and D-24. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/metered_charge_attempts_test.exs test/accrue/jobs/process_metered_renewal_test.exs test/accrue/processor/braintree_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
Metered renewal settlement uses one replay-safe Braintree sale per renewal window, durable recovery states distinguish what can retry from what needs repair, and repaired-payment-method replay does not double-charge the closed period.
</success_criteria>

<output>
After completion, create `.planning/phases/103-metering-engine/103-03-SUMMARY.md`
</output>

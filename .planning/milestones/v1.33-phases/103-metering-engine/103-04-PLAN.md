---
phase: 103-metering-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 103-01
  - 103-02
  - 103-03
files_modified:
  - accrue/lib/accrue/jobs/metered_renewal_reconciler.ex
  - accrue/lib/accrue/telemetry/ops.ex
  - accrue/lib/accrue/telemetry/metrics.ex
  - accrue/guides/metering.md
  - accrue/guides/telemetry.md
  - accrue/guides/operator-runbooks.md
  - accrue/guides/braintree-metered-billing.md
  - .planning/phases/103-metering-engine/103-VALIDATION.md
  - accrue/test/accrue/jobs/metered_renewal_reconciler_test.exs
  - accrue/test/accrue/telemetry/metered_billing_ops_test.exs
autonomous: true
requirements:
  - BT-06
  - BT-07
must_haves:
  truths:
    - "If the webhook-primary renewal path misses a cycle, a narrow scheduled backstop can detect and repair the stale window without becoming the primary trigger."
    - "Operators receive durable, low-noise telemetry on missing meter definitions, stale renewal windows, payment-method-repair states, and settlement exhaustion."
    - "Public docs explain that Stripe uses native meters while Braintree metering is Accrue-owned local aggregation plus one settlement sale."
  artifacts:
    - path: "accrue/lib/accrue/jobs/metered_renewal_reconciler.ex"
      provides: "Grace-window stale renewal detector and repair worker"
    - path: "accrue/lib/accrue/telemetry/metrics.ex"
      provides: "Low-cardinality metered-billing ops counters"
    - path: "accrue/guides/braintree-metered-billing.md"
      provides: "Canonical Phase 103 public guide for local metering plus Braintree settlement"
    - path: "accrue/guides/operator-runbooks.md"
      provides: "Operator recovery instructions for stale windows and metered-charge repair"
  key_links:
    - from: "accrue/lib/accrue/jobs/metered_renewal_reconciler.ex"
      to: "accrue/lib/accrue/billing/metered_renewal_actions.ex"
      via: "stale-window repair reuses the same renewal-opening contract instead of inventing a second billing path"
      pattern: "MeteredRenewalActions\\."
    - from: "accrue/lib/accrue/telemetry/ops.ex"
      to: "accrue/guides/telemetry.md"
      via: "new ops events are documented with the same tuple names and metadata fields"
      pattern: "metered_"
    - from: "accrue/guides/braintree-metered-billing.md"
      to: "accrue/guides/metering.md"
      via: "public docs explain the processor distinction without rewriting the shared `report_usage/3` contract"
      pattern: "report_usage"
---

<objective>
Finish Phase 103 with recovery backstops, operator telemetry, and honest public documentation.

Purpose: Keep the webhook-primary architecture resilient in production and document the Braintree-vs-Stripe distinction clearly enough that operators and integrators can reason about failures.
Output: A stale-window reconciler, durable telemetry coverage, regression tests, and the new Braintree metered-billing guide.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/103-metering-engine/103-CONTEXT.md
@.planning/phases/103-metering-engine/103-RESEARCH.md
@.planning/phases/103-metering-engine/103-PATTERNS.md
@accrue/guides/metering.md
@accrue/guides/telemetry.md
@accrue/guides/operator-runbooks.md
@accrue/lib/accrue/jobs/meter_events_reconciler.ex
@accrue/lib/accrue/telemetry/ops.ex
@accrue/lib/accrue/telemetry/metrics.ex

<interfaces>
From `accrue/lib/accrue/jobs/meter_events_reconciler.ex`:
```elixir
use Oban.Worker, queue: :accrue_meters, max_attempts: 3
_ = Accrue.Oban.Middleware.put(job)
cutoff = DateTime.add(Clock.utc_now(), -@grace_seconds, :second)
```

From `accrue/lib/accrue/telemetry/ops.ex`:
```elixir
@spec emit(suffix(), map(), map()) :: :ok
```

From `accrue/lib/accrue/telemetry/metrics.ex`:
```elixir
counter("accrue.ops.meter_reporting_failed.count", tags: [:source])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for stale-window recovery and metered-billing ops telemetry</name>
  <files>accrue/test/accrue/jobs/metered_renewal_reconciler_test.exs, accrue/test/accrue/telemetry/metered_billing_ops_test.exs</files>
  <behavior>
    - Test 1: the scheduled backstop detects only renewal windows past the grace period and repairs them by reusing the same renewal-opening logic as the webhook path.
    - Test 2: metered-billing ops events fire once per durable state transition rather than once per raw retry attempt.
    - Test 3: default metrics include low-cardinality counters for stale renewals, missing meter definitions, awaiting-payment-method states, and exhausted metered settlement.
  </behavior>
  <action>Write failing tests per D-08, D-13, D-25, D-30, and D-31. Pattern analog from `103-PATTERNS.md`: mirror `meter_events_reconciler.ex` for the stale-window worker shell. `metered_renewal_reconciler_test.exs` must mirror the `MeterEventsReconciler` style while asserting the backstop only scans stale expected-renewal windows after a grace period and never becomes the primary billing clock. `metered_billing_ops_test.exs` must assert durable state-transition tuples and the exact low-cardinality metrics additions. Keep the tuple names explicit and docs-friendly so the later guide updates can quote them without reinterpretation.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/jobs/metered_renewal_reconciler_test.exs test/accrue/telemetry/metered_billing_ops_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Backstop and ops expectations are pinned by tests before recovery and documentation changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stale-window reconciler and metered-billing telemetry catalog</name>
  <files>accrue/lib/accrue/jobs/metered_renewal_reconciler.ex, accrue/lib/accrue/telemetry/ops.ex, accrue/lib/accrue/telemetry/metrics.ex</files>
  <action>Create `Accrue.Jobs.MeteredRenewalReconciler` as the narrow scheduled backstop from D-08 and D-13, patterned directly after `MeterEventsReconciler`: restore Oban middleware, scan stale expected-renewal windows after a grace period, and repair them by refetching canonical subscription truth and reusing the same renewal-opening logic from Plan 01 rather than inventing a second path. Extend `Accrue.Telemetry.Ops` and `Accrue.Telemetry.Metrics` with durable state-transition events and low-cardinality counters for the metered renewal lifecycle, including missing meter definitions, stale renewal repaired, metered charge awaiting payment method, and metered charge exhausted. Emit on durable transitions only, not per raw retry attempt, per D-25. Keep metadata PII-free and docs-stable.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/jobs/metered_renewal_reconciler_test.exs test/accrue/telemetry/metered_billing_ops_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The scheduled backstop repairs missed renewal windows without taking over the billing clock, and metered-billing ops events are available to hosts and operators.</done>
</task>

<task type="auto">
  <name>Task 3: Document Braintree metering, telemetry, and operator recovery</name>
  <files>accrue/guides/metering.md, accrue/guides/telemetry.md, accrue/guides/operator-runbooks.md, accrue/guides/braintree-metered-billing.md, .planning/phases/103-metering-engine/103-VALIDATION.md</files>
  <action>Update the public guides to reflect the locked processor distinction from D-30. In `guides/metering.md`, keep `Accrue.Billing.report_usage/3` as the shared ingress contract but explain that Braintree metering layers local definitions and renewal windows on top of the raw event ledger. Add `guides/braintree-metered-billing.md` as the canonical public guide for Phase 103, walking through the local meter-definition model, renewal-window lifecycle, local invoice decomposition, single-sale settlement, and replay-safe recovery. In `guides/telemetry.md`, add the exact ops tuples and metric names implemented in Task 2. In `guides/operator-runbooks.md`, add ordered triage for stale renewal windows, missing definitions, awaiting-payment-method recovery, and failed-exhausted metered charges. Add `.planning/phases/103-metering-engine/103-VALIDATION.md` and finalize the phase verification map: `wave_0_complete: true`, `nyquist_compliant: true`, all automated verifies mapped to Plans 01-04, and the docs verification lane captured explicitly. Do not imply Stripe-native meter parity or hide the local-ledger-plus-external-settlement architecture.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/jobs/metered_renewal_reconciler_test.exs test/accrue/telemetry/metered_billing_ops_test.exs --warnings-as-errors && test -f accrue/guides/braintree-metered-billing.md && test -f .planning/phases/103-metering-engine/103-VALIDATION.md && rg -n "local aggregation|renewal window|Transaction\\.sale|awaiting-payment-method|failed-exhausted" accrue/guides/braintree-metered-billing.md accrue/guides/operator-runbooks.md && rg -n "metered_|accrue\\.ops\\.|wave_0_complete: true|nyquist_compliant: true" accrue/guides/telemetry.md .planning/phases/103-metering-engine/103-VALIDATION.md</automated>
  </verify>
  <done>Integrator and operator docs match the shipped Phase 103 architecture and provide a practical recovery path for real production failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled reconciler -> renewal repair path | Background automation can reopen missed billing work |
| telemetry catalog -> host alerting | Library tuple names and metrics become paging and runbook contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-10 | D | `accrue/lib/accrue/jobs/metered_renewal_reconciler.ex` | mitigate | Limit scans to stale expected-renewal windows after a grace period and reuse the webhook path’s canonical renewal classifier per D-13. |
| T-103-11 | I | `accrue/lib/accrue/telemetry/ops.ex` | mitigate | Emit only low-cardinality, PII-free metadata for metered-billing state transitions so host alerting stays safe and actionable per D-25 and D-31. |
| T-103-12 | S | `accrue/guides/braintree-metered-billing.md` | mitigate | Document the processor distinction explicitly so operators and integrators are not spoofed into assuming Stripe-native meter semantics on Braintree per D-30. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/jobs/metered_renewal_reconciler_test.exs test/accrue/telemetry/metered_billing_ops_test.exs --warnings-as-errors && test -f accrue/guides/braintree-metered-billing.md && test -f .planning/phases/103-metering-engine/103-VALIDATION.md && rg -n "metered_|wave_0_complete: true|nyquist_compliant: true" accrue/guides/telemetry.md .planning/phases/103-metering-engine/103-VALIDATION.md`.
</verification>

<success_criteria>
Phase 103 includes a narrow stale-window backstop, durable metered-billing ops telemetry, and public/operator docs that explain the local-ledger-first Braintree architecture honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/103-metering-engine/103-04-SUMMARY.md`
</output>

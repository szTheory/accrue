---
phase: 104-connect-spike-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/connect.md
  - accrue/guides/connect-hyperwallet-decision.md
  - accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs
autonomous: true
requirements:
  - BT-08
  - BT-09
must_haves:
  truths:
    - "A maintainer can read one durable Accrue guide that explains the Hyperwallet spike evidence, the final no-go decision, and the exact rule for reopening the topic."
    - "The public `Accrue.Connect` guide warns Braintree users that marketplace payouts are not first-party Connect parity and points them at the Phase 104 boundary document."
    - "The final decision preserves a narrow if-go contract for future reevaluation without implying that Accrue supports Stripe-like marketplace semantics today."
  artifacts:
    - path: "accrue/guides/connect-hyperwallet-decision.md"
      provides: "Canonical Phase 104 decision artifact with spike evidence, no-go verdict, reopen rule, and narrow if-go contract"
    - path: "accrue/guides/connect.md"
      provides: "Public Connect guide callout that keeps Braintree marketplace expectations honest"
    - path: "accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs"
      provides: "Merge-blocking docs contract for BT-08 and BT-09 wording"
  key_links:
    - from: "accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs"
      to: "accrue/guides/connect-hyperwallet-decision.md"
      via: "literal substring assertions for the verdict, reopen rule, provider split, and if-go slice"
      pattern: "connect-hyperwallet-decision\\.md"
    - from: "accrue/guides/connect.md"
      to: "accrue/guides/connect-hyperwallet-decision.md"
      via: "Braintree/Hyperwallet boundary callout and guide link"
      pattern: "connect-hyperwallet-decision\\.md"
---

<objective>
Publish the Hyperwallet spike result as a durable documentation contract.

Purpose: Satisfy BT-08 and BT-09 without widening Accrue into a premature marketplace platform design.
Output: A new decision guide, an updated public Connect guide, and a docs-contract test that makes the verdict merge-blocking.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STRATEGY.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/104-connect-spike-decision/104-CONTEXT.md
@.planning/phases/104-connect-spike-decision/104-RESEARCH.md
@.planning/phases/104-connect-spike-decision/104-PATTERNS.md
@.planning/phases/104-connect-spike-decision/104-VALIDATION.md
@accrue/guides/connect.md
@accrue/test/accrue/docs/package_docs_verifier_test.exs

<interfaces>
From `accrue/lib/accrue/connect.ex`:
```elixir
@spec with_account(Account.t() | String.t() | nil, (-> result)) :: result when result: var
@spec create_account(map() | keyword(), keyword()) :: {:ok, Account.t()} | {:error, term()}
```

From `accrue/guides/connect.md`:
```markdown
Accrue's Connect surface (`Accrue.Connect`) gives Phoenix SaaS platforms
first-class support for Stripe Connect...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a docs-contract test for the Hyperwallet decision artifact</name>
  <read_first>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs
    - accrue/guides/connect.md
    - .planning/phases/104-connect-spike-decision/104-CONTEXT.md
    - .planning/phases/104-connect-spike-decision/104-RESEARCH.md
    - .planning/phases/104-connect-spike-decision/104-VALIDATION.md
  </read_first>
  <files>accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs</files>
  <acceptance_criteria>
    - `accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs` defines `Accrue.Docs.ConnectHyperwalletDecisionTest`.
    - The test file reads both `accrue/guides/connect.md` and `accrue/guides/connect-hyperwallet-decision.md`.
    - The test file asserts the exact strings `strategically out of bounds unless the project boundary changes`, `reopening requires an explicit strategy change plus a new milestone`, `Braintree pay-ins and Hyperwallet payouts are separate truths`, and `minimal seller onboarding + payouts only`.
    - The test file asserts the exact strings `Braintree recurring billing is incompatible with Marketplace` and `Hyperwallet is a separate payout program with separate webhook URLs, admin users, and API-credential recipients`.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs` as the BT-08 / BT-09 docs-contract bootstrap from `104-VALIDATION.md`. Reuse the repo's existing docs-test style from `package_docs_verifier_test.exs`: plain ExUnit file reads plus exact substring assertions, not broad regexes or runtime integration code. The test must pin D-02, D-06, D-07, D-10, and D-12 explicitly by asserting the future if-go slice (`minimal seller onboarding + payouts only`), the provider split (`Braintree pay-ins and Hyperwallet payouts are separate truths`), the final no-go posture (`strategically out of bounds unless the project boundary changes`), and the reopen rule (`reopening requires an explicit strategy change plus a new milestone`). It must also lock the core spike evidence required by BT-08 so the phase cannot degrade into an unsupported opinion: assert `Braintree recurring billing is incompatible with Marketplace` and `Hyperwallet is a separate payout program with separate webhook URLs, admin users, and API-credential recipients`. Keep this test documentation-only: no Hyperwallet SDK, no HTTP calls, and no new dependency.</action>
  <verify>
    <automated>rg -n "ConnectHyperwalletDecisionTest|connect-hyperwallet-decision\\.md|strategically out of bounds unless the project boundary changes|reopening requires an explicit strategy change plus a new milestone|Braintree pay-ins and Hyperwallet payouts are separate truths|minimal seller onboarding \\+ payouts only|Braintree recurring billing is incompatible with Marketplace|Hyperwallet is a separate payout program with separate webhook URLs, admin users, and API-credential recipients" accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs</automated>
  </verify>
  <done>The phase has an executable docs-contract test file that spells out both the required decision wording and the core Hyperwallet feasibility findings before the guide content is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the Hyperwallet decision guide and Connect boundary callout</name>
  <read_first>
    - accrue/guides/connect.md
    - accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs
    - .planning/STRATEGY.md
    - .planning/phases/104-connect-spike-decision/104-CONTEXT.md
    - .planning/phases/104-connect-spike-decision/104-RESEARCH.md
    - .planning/phases/104-connect-spike-decision/104-PATTERNS.md
  </read_first>
  <files>accrue/guides/connect.md, accrue/guides/connect-hyperwallet-decision.md</files>
  <behavior>
    - Test 1: the new decision guide states that Braintree marketplace parity through Hyperwallet is rejected for the current strategy and names the exact reopen rule.
    - Test 2: the decision guide preserves a narrow if-go slice contract without presenting it as current support.
    - Test 3: `accrue/guides/connect.md` visibly warns Braintree readers that Stripe Connect semantics do not carry over and links them to the decision guide.
  </behavior>
  <acceptance_criteria>
    - `accrue/guides/connect-hyperwallet-decision.md` exists and contains the exact strings `strategically out of bounds unless the project boundary changes`, `reopening requires an explicit strategy change plus a new milestone`, `Braintree pay-ins and Hyperwallet payouts are separate truths`, and `minimal seller onboarding + payouts only`.
    - `accrue/guides/connect-hyperwallet-decision.md` contains the exact strings `Braintree recurring billing is incompatible with Marketplace` and `Hyperwallet is a separate payout program with separate webhook URLs, admin users, and API-credential recipients`.
    - `accrue/guides/connect-hyperwallet-decision.md` names the loud exclusions `destination charges`, `separate charge-and-transfer`, `login-link parity`, and `unified connected-account balance`.
    - `accrue/guides/connect.md` contains a Braintree/Hyperwallet boundary note that links to `connect-hyperwallet-decision.md`.
  </acceptance_criteria>
  <action>Update `accrue/guides/connect.md` and create `accrue/guides/connect-hyperwallet-decision.md` per D-01 through D-12. Keep this phase as a decision spike, not an architecture or runtime implementation phase (D-01, D-03): do not add new modules, callbacks, dependencies, or pseudo-APIs. In the new guide, document the spike evidence from `104-RESEARCH.md` with exact, merge-blocking language: `Braintree recurring billing is incompatible with Marketplace`, `Hyperwallet is a separate payout program with separate webhook URLs, admin users, and API-credential recipients`, and a Stripe-like connected-account abstraction would be misleading. State the final verdict exactly as `strategically out of bounds unless the project boundary changes` (D-10, D-11), then immediately record the reopen rule `reopening requires an explicit strategy change plus a new milestone` (D-12). Preserve the reusable D-02 outcome by naming the smallest honest if-go slice as `minimal seller onboarding + payouts only`, then place loud exclusions beside it: `destination charges`, `separate charge-and-transfer`, `login-link parity`, and `unified connected-account balance` are not part of that slice (D-04, D-05, D-06). In both docs, use the exact provider-honest phrasing `Braintree pay-ins and Hyperwallet payouts are separate truths` (D-07, D-08, D-09).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/connect_hyperwallet_decision_test.exs -x --warnings-as-errors</automated>
  </verify>
  <done>Maintainers and adopters can read one durable decision artifact that preserves both the spike evidence and the no-go boundary, and the Connect guide now advertises the same provider-honest posture without implying current marketplace parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public Connect docs -> adopter expectations | Documentation claims define what users believe `Accrue.Connect` supports |
| phase research -> durable guide text | Commercial-provider findings are translated into long-lived repo contract wording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | E | `accrue/guides/connect-hyperwallet-decision.md` | mitigate | State the exact no-go boundary and loud exclusions so readers cannot infer Stripe-style marketplace parity from the `Accrue.Connect` name. |
| T-104-02 | T | `accrue/guides/connect.md` | mitigate | Add a Braintree/Hyperwallet boundary note and guide link so future edits cannot silently erase the provider split. |
| T-104-03 | R | `accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs` | mitigate | Pin the verdict, reopen rule, and if-go slice with exact-string assertions so later edits have an auditable contract failure. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/connect_hyperwallet_decision_test.exs -x --warnings-as-errors`.
</verification>

<success_criteria>
BT-08 and BT-09 are satisfied by a durable guide-backed decision contract: the Hyperwallet spike evidence is captured, the current verdict is no-go, the future if-go slice is explicit, and the public Connect guide no longer implies Braintree marketplace parity.
</success_criteria>

<output>
After completion, create `.planning/phases/104-connect-spike-decision/104-01-SUMMARY.md`
</output>

---
phase: 104-connect-spike-decision
plan: 02
type: execute
wave: 2
depends_on:
  - 104-01
files_modified:
  - .planning/processor-support-matrix.md
  - .planning/STRATEGY.md
  - scripts/ci/verify_processor_support_matrix.sh
autonomous: true
requirements:
  - BT-09
must_haves:
  truths:
    - "Accrue's official support matrix says marketplace Connect parity for Braintree remains outside first-party support for the current strategic boundary."
    - "The strategy document records that reopening Braintree marketplace work requires an explicit strategy change plus a new milestone."
    - "The processor-support verifier fails if the matrix drifts away from the Hyperwallet rejection posture."
  artifacts:
    - path: ".planning/processor-support-matrix.md"
      provides: "Official support-boundary truth for Braintree marketplace/Hyperwallet posture"
    - path: ".planning/STRATEGY.md"
      provides: "Strategic record of the no-go decision and reopen rule"
    - path: "scripts/ci/verify_processor_support_matrix.sh"
      provides: "Merge-blocking literal checks for the Phase 104 support-matrix wording"
  key_links:
    - from: "scripts/ci/verify_processor_support_matrix.sh"
      to: ".planning/processor-support-matrix.md"
      via: "literal substring checks for Hyperwallet wording and reopen rule"
      pattern: "require_substring"
    - from: ".planning/STRATEGY.md"
      to: ".planning/processor-support-matrix.md"
      via: "same no-go boundary and explicit reopen condition"
      pattern: "strategy change plus a new milestone"
---

<objective>
Mirror the Phase 104 decision into Accrue's support-matrix and strategy contracts.

Purpose: Make the no-go outcome durable, capability-explicit, and merge-blocking instead of leaving it isolated in one guide.
Output: Updated support matrix, updated strategy note, and stricter processor-support verification needles.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STRATEGY.md
@.planning/processor-support-matrix.md
@.planning/phases/104-connect-spike-decision/104-CONTEXT.md
@.planning/phases/104-connect-spike-decision/104-RESEARCH.md
@.planning/phases/104-connect-spike-decision/104-PATTERNS.md
@.planning/phases/104-connect-spike-decision/104-VALIDATION.md
@scripts/ci/verify_processor_support_matrix.sh
@accrue/test/accrue/docs/processor_support_matrix_test.exs

<interfaces>
From `accrue/lib/accrue/processor/capabilities.ex`:
```elixir
@spec support_label(atom() | [atom()]) :: String.t() | nil
@spec first_party_supported?(map(), atom() | [atom()]) :: boolean()
```

From `scripts/ci/verify_processor_support_matrix.sh`:
```bash
require_substring "..." "..."
echo "verify_processor_support_matrix: OK"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update support-matrix and strategy truth for the Hyperwallet no-go</name>
  <read_first>
    - .planning/processor-support-matrix.md
    - .planning/STRATEGY.md
    - .planning/phases/104-connect-spike-decision/104-CONTEXT.md
    - .planning/phases/104-connect-spike-decision/104-RESEARCH.md
    - scripts/ci/verify_processor_support_matrix.sh
  </read_first>
  <files>.planning/processor-support-matrix.md, .planning/STRATEGY.md</files>
  <acceptance_criteria>
    - `.planning/processor-support-matrix.md` contains `Hyperwallet`.
    - `.planning/processor-support-matrix.md` contains `strategically out of bounds unless the project boundary changes`.
    - `.planning/processor-support-matrix.md` contains `reopening requires an explicit strategy change plus a new milestone`.
    - `.planning/STRATEGY.md` records the same reopen rule and explicitly keeps marketplace Connect parity out of the active direct-gateway track.
  </acceptance_criteria>
  <action>Update `.planning/processor-support-matrix.md` and `.planning/STRATEGY.md` to reflect the final BT-09 decision from Plan 01. In the matrix, replace the generic `Connect` out-of-slice mention with explicit Braintree/Hyperwallet wording, state that marketplace parity is `strategically out of bounds unless the project boundary changes`, and add the reopen condition `reopening requires an explicit strategy change plus a new milestone` in the support-boundary section. In `STRATEGY.md`, add a decision note under the active dual-provider track that Braintree marketplace support via Hyperwallet is rejected for the current direct-gateway boundary and is not part of PROC-08 without a new strategy choice. Keep the language capability-explicit and provider-honest per D-04 through D-12; do not add runtime capability flags, new facade APIs, or future-phase placeholders.</action>
  <verify>
    <automated>rg -n "Hyperwallet|strategically out of bounds unless the project boundary changes|reopening requires an explicit strategy change plus a new milestone|marketplace" .planning/processor-support-matrix.md .planning/STRATEGY.md</automated>
  </verify>
  <done>The repo's core support-boundary docs now record the Hyperwallet no-go as strategy truth rather than as a one-off phase note.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the processor-support verifier for the Phase 104 wording</name>
  <read_first>
    - scripts/ci/verify_processor_support_matrix.sh
    - accrue/test/accrue/docs/processor_support_matrix_test.exs
    - .planning/processor-support-matrix.md
    - .planning/phases/104-connect-spike-decision/104-VALIDATION.md
  </read_first>
  <files>scripts/ci/verify_processor_support_matrix.sh</files>
  <acceptance_criteria>
    - `scripts/ci/verify_processor_support_matrix.sh` contains `require_substring "Hyperwallet"`.
    - `scripts/ci/verify_processor_support_matrix.sh` contains `require_substring "strategically out of bounds unless the project boundary changes"`.
    - `scripts/ci/verify_processor_support_matrix.sh` contains `require_substring "reopening requires an explicit strategy change plus a new milestone"`.
    - `accrue/test/accrue/docs/processor_support_matrix_test.exs` still passes without changing its success needle `verify_processor_support_matrix: OK`.
  </acceptance_criteria>
  <action>Extend `scripts/ci/verify_processor_support_matrix.sh` with new literal needles for the Phase 104 decision language instead of adding a second shell verifier. Require `Hyperwallet`, `strategically out of bounds unless the project boundary changes`, and `reopening requires an explicit strategy change plus a new milestone` so matrix drift becomes merge-blocking through the existing `processor_support_matrix_test.exs` path. Keep the script's structure identical to the current literal-needle pattern from `104-PATTERNS.md`, preserve the `verify_processor_support_matrix: OK` success line, and avoid changing the ExUnit smoke test unless the script interface itself changes.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/processor_support_matrix_test.exs -x --warnings-as-errors</automated>
  </verify>
  <done>The official processor-support verifier now fails if future edits erase the explicit Hyperwallet no-go boundary or the reopen rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support matrix -> public support promise | Matrix wording controls what maintainers and adopters treat as first-party support |
| strategy notes -> future milestone scoping | Strategic text governs whether marketplace work can quietly re-enter the roadmap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-04 | E | `.planning/processor-support-matrix.md` | mitigate | Add explicit Hyperwallet no-go wording and keep the matrix capability-explicit so unsupported marketplace work cannot masquerade as first-party parity. |
| T-104-05 | T | `scripts/ci/verify_processor_support_matrix.sh` | mitigate | Enforce literal needles for the no-go boundary and reopen rule so later edits cannot silently soften the contract. |
| T-104-06 | R | `.planning/STRATEGY.md` | mitigate | Record that reopening requires an explicit strategy change plus a new milestone, creating an auditable strategic gate. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/processor_support_matrix_test.exs -x --warnings-as-errors`.
</verification>

<success_criteria>
The Phase 104 verdict is no longer just a guide note: Accrue's strategy and processor-support matrix both advertise the same no-go boundary, and CI fails if those docs drift away from the approved wording.
</success_criteria>

<output>
After completion, create `.planning/phases/104-connect-spike-decision/104-02-SUMMARY.md`
</output>

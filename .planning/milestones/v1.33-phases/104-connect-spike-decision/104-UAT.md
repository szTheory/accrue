---
status: complete
phase: 104-connect-spike-decision
verification: automated
verified_by:
  - .github/workflows/ci.yml#release-gate
  - .github/workflows/ci.yml#docs-contracts-shift-left
source:
  - 104-01-SUMMARY.md
  - 104-02-SUMMARY.md
started: 2026-05-06T00:00:00Z
updated: 2026-05-06T00:00:00Z
---

## Current Test

[testing complete]

## Verification Approach

Phase 104 is a decision spike — the deliverable is a documented go/no-go
boundary, not new runtime code. Every wording-level claim that the
decision rests on is pinned by an exact-substring assertion (ExUnit or
shell), and every assertion runs in PR-blocking CI today. Re-verifying
Phase 104 = `git push`.

CI gates:

- `release-gate (accrue × Floor | Primary | Forward-compat)` — runs the
  literal docs-contract test that pins the no-go verdict, reopen rule,
  provider split, spike evidence, and narrow if-go slice
- `docs-contracts-shift-left` — runs `verify_processor_support_matrix.sh`
  which asserts the literal `Hyperwallet`, no-go, and reopen-rule needles
  in `.planning/processor-support-matrix.md`

## Tests

### 1. Hyperwallet No-Go Decision Is Published
expected: |
  `accrue/guides/connect-hyperwallet-decision.md` exists and explicitly
  states marketplace parity via Hyperwallet is out of bounds unless the
  project boundary changes (the reopen rule).
result: pass
verified_by:
  - accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs

### 2. Connect Guide Surfaces The Boundary
expected: |
  `accrue/guides/connect.md` includes a visible Braintree-marketplace
  warning near the top with a working link into
  `connect-hyperwallet-decision.md`.
result: pass
verified_by:
  - accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs

### 3. Spike Evidence + Provider Split Pinned Verbatim
expected: |
  The decision artifact lists the spike evidence (what was investigated,
  what was found) and the provider split (Stripe Connect = supported,
  Braintree marketplace = no-go) in stable wording the docs-contract
  test asserts byte-for-byte.
result: pass
verified_by:
  - accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs

### 4. Narrow If-Go Slice Is Defined
expected: |
  The decision artifact specifies the narrow if-go slice that would
  reopen the question — so a future maintainer doesn't have to re-derive
  what would justify reversing the no-go.
result: pass
verified_by:
  - accrue/test/accrue/docs/connect_hyperwallet_decision_test.exs

### 5. Processor-Support-Matrix Names Hyperwallet + Reopen Rule
expected: |
  `.planning/processor-support-matrix.md` includes literal `Hyperwallet`,
  no-go wording, and the reopen rule. CI fails the shift-left job if any
  needle drifts.
result: pass
verified_by:
  - scripts/ci/verify_processor_support_matrix.sh
  - .github/workflows/ci.yml (docs-contracts-shift-left)

### 6. STRATEGY.md Records The Same No-Go Posture
expected: |
  `.planning/STRATEGY.md` active PROC-08 track records the provider
  split and no-go posture, keeping strategy and support matrix in sync.
result: pass
verified_by:
  - .planning/STRATEGY.md (committed alongside support-matrix update)
  - manual cross-link verification via Phase 104-02 commit 2e90a79

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — every decision claim is pinned by a literal-substring docs-contract assertion gated in CI]

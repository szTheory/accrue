---
phase: 106-invoice-renderer-seam-rendro-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs
  - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
  - accrue/test/accrue/billing/pdf_test.exs
  - accrue/guides/configuration.md
  - accrue/guides/testing.md
  - accrue/test/mix/tasks/accrue_install_uat_test.exs
autonomous: true
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-05
must_haves:
  truths:
    - "Invoice-facing proof lanes configure `:invoice_pdf_adapter` when they mean to exercise the invoice renderer seam."
    - "`Accrue.Billing.render/store/fetch_invoice_pdf` remains the unchanged public contract while test and doc surfaces stop implying the legacy HTML seam owns invoice rendering."
    - "Admin open/download and mailer attachment flows continue to prove invoice behavior through the billing facade rather than custom or duplicate rendering paths."
  artifacts:
    - path: "accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs"
      provides: "Attachment and fallback proof against the invoice renderer seam"
    - path: "accrue_admin/test/accrue_admin/live/invoice_live_test.exs"
      provides: "Admin invoice download proof against the invoice renderer seam"
    - path: "accrue/guides/configuration.md"
      provides: "Accurate adapter-key contract for invoice rendering"
  key_links:
    - from: "accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs"
      to: "accrue/lib/accrue/workers/mailer.ex"
      via: "Tests must configure the same adapter key that `safe_render_invoice_pdf/2` ultimately resolves through the billing facade"
      pattern: "render_invoice_pdf"
    - from: "accrue_admin/test/accrue_admin/live/invoice_live_test.exs"
      to: "accrue_admin/lib/accrue_admin/live/invoice_live.ex"
      via: "Open-PDF UI proof must exercise `Billing.render_invoice_pdf/2` rather than the legacy HTML adapter"
      pattern: "open_pdf"
    - from: "accrue/guides/configuration.md"
      to: "accrue/lib/accrue/config.ex"
      via: "Docs must describe `:invoice_pdf_adapter` as the invoice-renderer control and `:pdf_adapter` as the legacy HTML seam"
      pattern: "invoice_pdf_adapter|pdf_adapter"
---

<objective>
Remove legacy seam drift from proof and contract surfaces so Phase 106's shipped architecture is what the repo actually tests and documents.

Purpose: protect the invoice-specific renderer seam from accidental regression back to the old `Accrue.PDF` mental model.
Output: corrected admin/mailer/core tests and narrow contract-facing docs that all point invoice rendering at `Accrue.InvoiceRenderer`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-RESEARCH.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-PATTERNS.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-VALIDATION.md
@accrue/lib/accrue/invoice_renderer.ex
@accrue/lib/accrue/invoices.ex
@accrue/lib/accrue/workers/mailer.ex
@accrue_admin/lib/accrue_admin/live/invoice_live.ex
@accrue/lib/accrue/config.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 106-01-01: Correct invoice proof lanes to use the invoice renderer seam</name>
  <files>accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs, accrue_admin/test/accrue_admin/live/invoice_live_test.exs, accrue/test/accrue/billing/pdf_test.exs</files>
  <read_first>.planning/phases/106-invoice-renderer-seam-rendro-default/106-RESEARCH.md, .planning/phases/106-invoice-renderer-seam-rendro-default/106-PATTERNS.md, accrue/lib/accrue/workers/mailer.ex, accrue_admin/lib/accrue_admin/live/invoice_live.ex, accrue/test/accrue/billing/pdf_test.exs</read_first>
  <acceptance_criteria>
    - Admin invoice LiveView tests switch their seam overrides from `:pdf_adapter` to `:invoice_pdf_adapter` whenever they intend to control `Billing.render_invoice_pdf/2`.
    - Webhook mailer dispatch tests switch their seam overrides from `:pdf_adapter` to `:invoice_pdf_adapter` for invoice attachment coverage.
    - Billing PDF tests explicitly continue to cover lazy `render/store/fetch_invoice_pdf` behavior while proving the public billing facade did not change.
    - No proof lane introduces a custom rendering shortcut or bypasses `Accrue.Billing.render_invoice_pdf/2`.
  </acceptance_criteria>
  <behavior>
    - Test 1: admin "Open PDF" proof still renders the open/download affordances while the configured adapter is `Accrue.InvoiceRenderer.Test`.
    - Test 2: webhook mailer attachment proof still produces a PDF attachment when `invoice_id` is present and the invoice renderer test adapter is configured.
    - Test 3: lazy `store_invoice_pdf/2` and `fetch_invoice_pdf/1` semantics remain unchanged under the invoice seam.
  </behavior>
  <action>Update the existing proof files in-place so they configure `:invoice_pdf_adapter` for invoice rendering and reserve `:pdf_adapter` only for tests that truly exercise `Accrue.PDF`. Follow the exact patterns already used in `accrue/test/accrue/billing/pdf_test.exs`: `Application.put_env/3`, `on_exit/1`, and mailbox assertions from `Accrue.InvoiceRenderer.Test`. Do not add a parallel rendering path in admin or mailer code. This task owns proof alignment only; keep production behavior routed through the current billing facade.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/pdf_test.exs test/accrue/webhook/default_handler_mailer_dispatch_test.exs -x && cd ../accrue_admin && mix test test/accrue_admin/live/invoice_live_test.exs -x</automated>
  </verify>
  <done>Invoice-facing proof lanes now exercise the real invoice renderer seam and still pass through the unchanged billing/admin/mailer contracts.</done>
</task>

<task type="auto">
  <name>Task 106-01-02: Repair contract-facing docs and installer/test guidance around the split adapters</name>
  <files>accrue/guides/configuration.md, accrue/guides/testing.md, accrue/test/mix/tasks/accrue_install_uat_test.exs</files>
  <read_first>accrue/guides/configuration.md, accrue/guides/testing.md, accrue/guides/pdf.md, accrue/lib/accrue/config.ex, accrue/test/mix/tasks/accrue_install_uat_test.exs</read_first>
  <acceptance_criteria>
    - `guides/configuration.md` says invoice rendering is controlled by `:invoice_pdf_adapter`, while `:pdf_adapter` is described as the lower-level HTML seam.
    - `guides/testing.md` and any installer/UAT expectations that mention invoice PDF tests align with `Accrue.InvoiceRenderer.Test` for invoice rendering.
    - This task does not attempt the broad Rendro-first migration/install narrative reserved for later phases.
  </acceptance_criteria>
  <action>Make the narrowest doc-and-test updates required to keep the repo honest about the split between `Accrue.InvoiceRenderer` and `Accrue.PDF`. Use `guides/pdf.md` and `Accrue.Config` as the canonical wording sources. If `accrue_install_uat_test.exs` or other contract tests still encode the old invoice-rendering adapter story, update those expectations to the new seam without widening into Phase 108 migration work.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/config_test.exs test/mix/tasks/accrue_install_uat_test.exs -x</automated>
  </verify>
  <done>Repo-level contract docs and installer/test expectations describe the same invoice-renderer split that the code now implements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/tests -> developer understanding | A wrong adapter key in docs/tests makes maintainers believe they are proving the invoice seam when they are not. |
| public billing facade -> invoice renderer seam | Proof drift can reintroduce hidden dependence on the legacy HTML seam without changing production code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | T | invoice proof lanes | mitigate | Force admin and mailer proof lanes to configure `:invoice_pdf_adapter` and keep all assertions routed through the billing facade. |
| T-106-02 | I | contract docs | mitigate | Align configuration/testing docs with `Accrue.Config` and `guides/pdf.md` so the repo no longer tells integrators the wrong adapter owns invoice rendering. |
| T-106-03 | R | phase-106 requirement coverage | mitigate | Keep lazy render/store/fetch semantics under the same tests while updating the seam key, so proof does not narrow silently. |
</threat_model>

<verification>
Run the plan's `<automated>` commands.
Cross-read the updated test fixtures and docs to confirm no lingering invoice-rendering references still point at `:pdf_adapter` unless they are intentionally testing `Accrue.PDF`.
</verification>

<success_criteria>
When a maintainer reads or runs the repo's invoice proof lanes, they are exercising `Accrue.InvoiceRenderer` by default and not an obsolete HTML seam.
</success_criteria>

<output>
After completion, create `.planning/phases/106-invoice-renderer-seam-rendro-default/106-01-SUMMARY.md`
</output>

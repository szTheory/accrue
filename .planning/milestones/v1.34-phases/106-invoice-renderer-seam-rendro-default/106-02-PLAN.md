---
phase: 106-invoice-renderer-seam-rendro-default
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - accrue/lib/accrue/invoice_renderer/rendro.ex
  - accrue/test/accrue/billing/pdf_test.exs
  - accrue/test/accrue/invoice_renderer/rendro_test.exs
  - accrue/lib/accrue/invoices.ex
autonomous: true
requirements:
  - PDF-03
  - PDF-04
  - PDF-05
must_haves:
  truths:
    - "Rendro remains the default invoice renderer and does not require Chrome for the primary invoice path."
    - "Parity proof covers invoice semantics such as branding, dates, line items, totals, and footer content rather than only proving a PDF binary exists."
    - "Lazy `render/store/fetch_invoice_pdf` semantics remain unchanged while parity proof gets stronger."
  artifacts:
    - path: "accrue/lib/accrue/invoice_renderer/rendro.ex"
      provides: "Deterministic native invoice rendering with testable semantic structure"
    - path: "accrue/test/accrue/invoice_renderer/rendro_test.exs"
      provides: "Focused Rendro parity assertions"
    - path: "accrue/test/accrue/billing/pdf_test.exs"
      provides: "Facade-level proof that the Rendro default still works through `Accrue.Invoices` and `Accrue.Billing`"
  key_links:
    - from: "accrue/lib/accrue/invoice_renderer/rendro.ex"
      to: "accrue/test/accrue/invoice_renderer/rendro_test.exs"
      via: "Adapter-level tests should assert invoice semantics from the concrete renderer without widening the public facade"
      pattern: "header|body|footer|Total|Invoice"
    - from: "accrue/test/accrue/invoice_renderer/rendro_test.exs"
      to: "accrue/test/accrue/billing/pdf_test.exs"
      via: "Concrete adapter proof complements facade-level smoke tests"
      pattern: "%PDF|render_invoice_pdf"
    - from: "accrue/lib/accrue/invoice_renderer/rendro.ex"
      to: "accrue/lib/accrue/invoices.ex"
      via: "Any internal refactor for proofability must preserve the lazy facade contract"
      pattern: "render_invoice_pdf|store_invoice_pdf|fetch_invoice_pdf"
---

<objective>
Strengthen the Rendro default path from a smoke-tested renderer into a semantically proven invoice engine while keeping the public invoice facade stable.

Purpose: close the remaining PDF-04/PDF-05 proof gap without reopening the pre-Rendro HTML default.
Output: deterministic Rendro parity tests and minimal internal renderer refactors, if needed, to make invoice semantics inspectable and regression-resistant.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-RESEARCH.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-PATTERNS.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-VALIDATION.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-01-PLAN.md
@accrue/lib/accrue/invoice_renderer/rendro.ex
@accrue/lib/accrue/invoices.ex
@accrue/test/accrue/billing/pdf_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 106-02-01: Add focused Rendro parity proof for invoice semantics</name>
  <files>accrue/lib/accrue/invoice_renderer/rendro.ex, accrue/test/accrue/invoice_renderer/rendro_test.exs, accrue/test/accrue/billing/pdf_test.exs</files>
  <read_first>.planning/phases/106-invoice-renderer-seam-rendro-default/106-RESEARCH.md, accrue/lib/accrue/invoice_renderer/rendro.ex, accrue/test/accrue/billing/pdf_test.exs</read_first>
  <acceptance_criteria>
    - The test suite proves more than `%PDF`: it checks invoice semantics such as invoice number or issued date, customer label, line-item rows, totals, and footer/support branding on the Rendro path.
    - Any helper added to make the renderer testable is internal or `@doc false`; the public invoice facade remains unchanged.
    - Existing billing-facade tests still prove that Rendro is the default adapter and returns a valid PDF binary without requiring Chrome.
  </acceptance_criteria>
  <behavior>
    - Test 1: the Rendro adapter builds or exposes enough deterministic structure to assert header/body/footer semantics for a representative invoice.
    - Test 2: the facade-level Rendro default test still proves `render_invoice_pdf/2` returns a real PDF binary.
  </behavior>
  <action>Introduce the narrowest internal renderer refactor needed to make Rendro parity semantics testable. Preferred direction: keep `render/2` as the public entry and add a small internal or `@doc false` helper that lets tests assert the assembled invoice content before binary rendering. Follow the existing helper-driven style in `accrue/lib/accrue/invoice_renderer/rendro.ex`; do not widen the `Accrue.InvoiceRenderer` or `Accrue.Billing` public API just for testing.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/pdf_test.exs test/accrue/invoice_renderer/rendro_test.exs -x</automated>
  </verify>
  <done>Rendro default behavior is semantically proven, not just smoke-tested, and the public invoice facade remains stable.</done>
</task>

<task type="auto">
  <name>Task 106-02-02: Re-run lazy facade and downstream lane proof on the strengthened Rendro default</name>
  <files>accrue/lib/accrue/invoices.ex, accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs, accrue_admin/test/accrue_admin/live/invoice_live_test.exs</files>
  <read_first>accrue/lib/accrue/invoices.ex, accrue/lib/accrue/workers/mailer.ex, accrue_admin/lib/accrue_admin/live/invoice_live.ex, .planning/phases/106-invoice-renderer-seam-rendro-default/106-01-PLAN.md</read_first>
  <acceptance_criteria>
    - Strengthened Rendro proof does not alter the lazy `render/store/fetch_invoice_pdf` contract in `Accrue.Invoices`.
    - Mailer attachment and admin open/download lanes still pass after the renderer proof hardening.
    - No Chrome dependency is introduced into the default proof lane.
  </acceptance_criteria>
  <action>After the adapter-level Rendro proof lands, perform any minimal follow-up adjustments needed in `Accrue.Invoices` or the downstream proof files so the stronger default path stays consistent across lazy billing facade behavior, mailer attachments, and admin download. Treat this as a stabilization task only; do not spill into Chromic fallback release work from Phase 107.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/pdf_test.exs test/accrue/webhook/default_handler_mailer_dispatch_test.exs -x && cd ../accrue_admin && mix test test/accrue_admin/live/invoice_live_test.exs -x</automated>
  </verify>
  <done>The Rendro-default seam is proven both in isolation and through the invoice-facing lanes that users actually hit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concrete renderer -> public invoice facade | Internal refactors for testability must not change the public API or lazy semantics. |
| renderer semantics -> downstream lanes | Mailer/admin paths rely on invoice content intent, not just PDF existence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04 | T | `Accrue.InvoiceRenderer.Rendro` | mitigate | Add focused parity tests for header/body/footer semantics and keep refactors internal. |
| T-106-05 | R | `Accrue.Invoices` lazy contract | mitigate | Re-run facade tests and downstream lane proofs after adapter-level hardening. |
| T-106-06 | D | default invoice render path | mitigate | Preserve Chrome-free default proof and avoid reintroducing HTML/Chromic assumptions into the main lane. |
</threat_model>

<verification>
Run the plan's `<automated>` commands.
Inspect the final tests to confirm they prove semantic invoice content rather than engine-specific or byte-for-byte output.
</verification>

<success_criteria>
Phase 106 ends with a default Rendro invoice path that is both Chrome-free and semantically proven across the invoice surfaces Accrue exposes.
</success_criteria>

<output>
After completion, create `.planning/phases/106-invoice-renderer-seam-rendro-default/106-02-SUMMARY.md`
</output>

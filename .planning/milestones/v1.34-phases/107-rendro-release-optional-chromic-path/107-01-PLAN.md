---
phase: 107-rendro-release-optional-chromic-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/errors.ex
  - accrue/lib/accrue/invoices.ex
  - accrue/lib/accrue/application.ex
  - accrue/lib/accrue/workers/mailer.ex
  - accrue/guides/pdf.md
  - accrue/guides/configuration.md
  - accrue/guides/telemetry.md
  - accrue/test/accrue/billing/pdf_test.exs
  - accrue/test/accrue/application_boot_guards_test.exs
  - accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs
autonomous: true
requirements:
  - PDF-06
must_haves:
  truths:
    - "A host can keep ChromicPDF available only by explicitly setting `:invoice_pdf_adapter` to `Accrue.InvoiceRenderer.ChromicPDF`; invoice rendering does not infer from `:pdf_adapter`."
    - "When the explicit Chromic invoice path is misconfigured, `Accrue.Invoices.render_invoice_pdf/2` returns a typed terminal `%Accrue.Error.*{}` instead of the bare atom `:chromic_pdf_not_started`."
    - "Boot-time guidance warns legacy hosts that set `:pdf_adapter` without `:invoice_pdf_adapter`, and separately warns when the explicit Chromic invoice path is configured without a running `ChromicPDF` process."
    - "Accrue emits low-cardinality ops telemetry when invoice-PDF behavior degrades because the explicit Chromic path is unavailable."
  artifacts:
    - path: "accrue/lib/accrue/errors.ex"
      provides: "Typed host-matchable error for explicit Chromic invoice-path unavailability"
    - path: "accrue/lib/accrue/invoices.ex"
      provides: "Invoice-facade contract that preserves the explicit adapter split while emitting typed terminal failures"
    - path: "accrue/lib/accrue/application.ex"
      provides: "Boot-time warning coverage for explicit Chromic opt-in and legacy `:pdf_adapter` migration drift"
    - path: "accrue/lib/accrue/workers/mailer.ex"
      provides: "Mailer degradation path that treats the typed Chromic-unavailable error as terminal without introducing retries or fallback chains"
    - path: "accrue/guides/pdf.md"
      provides: "Explicit Rendro-first / Chromic-opt-in operator guidance"
  key_links:
    - from: "accrue/lib/accrue/invoices.ex"
      to: "accrue/lib/accrue/errors.ex"
      via: "The invoice facade returns `%Accrue.Error.InvoiceRendererUnavailable{adapter: Accrue.InvoiceRenderer.ChromicPDF, reason: :chromic_pdf_not_started}` instead of a bare atom."
      pattern: "InvoiceRendererUnavailable|chromic_pdf_not_started"
    - from: "accrue/lib/accrue/application.ex"
      to: "accrue/guides/configuration.md"
      via: "Boot warnings and docs must both spell out the split between `:invoice_pdf_adapter` and `:pdf_adapter`."
      pattern: "invoice_pdf_adapter|pdf_adapter"
    - from: "accrue/lib/accrue/workers/mailer.ex"
      to: "accrue/guides/telemetry.md"
      via: "The degradation path should use the canonical ops helper/event family and the operator docs must describe the invoice-path-wide signal."
      pattern: "pdf_adapter_unavailable|Telemetry\\.Ops"
---

<objective>
Harden the explicit optional Chromic invoice path so hosts get typed failures, migration-focused warnings, and invoice-path-wide telemetry without reintroducing any automatic fallback behavior.

Purpose: satisfy PDF-06 with least-surprise contracts for host maintainers who still want the old HTML/Chrome invoice lane while keeping Rendro as the default.
Output: typed Chromic-unavailable invoice errors, corrected boot warnings and tests, broader ops telemetry, and narrow docs that make the config split honest before Phase 108's larger docs pass.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md
@.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-PATTERNS.md
@.planning/phases/106-invoice-renderer-seam-rendro-default/106-02-SUMMARY.md
@accrue/lib/accrue/application.ex
@accrue/lib/accrue/invoices.ex
@accrue/lib/accrue/workers/mailer.ex
@accrue/lib/accrue/errors.ex
@accrue/test/accrue/application_boot_guards_test.exs
@accrue/test/accrue/billing/pdf_test.exs

<interfaces>
From `accrue/lib/accrue/invoices.ex`:
```elixir
@spec render_invoice_pdf(invoice_or_id(), keyword()) ::
        {:ok, binary()} | {:error, term()}
def render_invoice_pdf(invoice_or_id, opts \\ [])
```

From `accrue/lib/accrue/telemetry/ops.ex`:
```elixir
@spec emit(suffix(), map(), map()) :: :ok
def emit(suffix, measurements, metadata \\ %{})
```

From `accrue/lib/accrue/workers/mailer.ex`:
```elixir
case safe_render_invoice_pdf(invoice_id, assigns) do
  {:ok, binary} -> ...
  {:error, %Accrue.Error.PdfDisabled{}} -> ...
  {:error, reason} -> raise Accrue.PDF.RenderFailed, reason: reason
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 107-01-01: Replace the bare Chromic atom with a typed invoice-renderer error contract</name>
  <files>accrue/lib/accrue/errors.ex, accrue/lib/accrue/invoices.ex, accrue/test/accrue/billing/pdf_test.exs</files>
  <read_first>.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md, .planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-PATTERNS.md, accrue/lib/accrue/errors.ex, accrue/lib/accrue/invoices.ex, accrue/test/accrue/billing/pdf_test.exs, accrue/guides/pdf.md</read_first>
  <acceptance_criteria>
    - `accrue/lib/accrue/errors.ex` defines `defmodule Accrue.Error.InvoiceRendererUnavailable do`.
    - `Accrue.Error.InvoiceRendererUnavailable` exposes the fields `:adapter`, `:reason`, and `:message`.
    - `accrue/lib/accrue/invoices.ex` no longer returns the literal tuple `{:error, :chromic_pdf_not_started}`.
    - `accrue/lib/accrue/invoices.ex` returns `{:error, %Accrue.Error.InvoiceRendererUnavailable{adapter: Accrue.InvoiceRenderer.ChromicPDF, reason: :chromic_pdf_not_started}}` when `ChromicPDF` is not running and the configured invoice adapter is `Accrue.InvoiceRenderer.ChromicPDF`.
    - `accrue/test/accrue/billing/pdf_test.exs` asserts the typed error struct and keeps the `%Accrue.Error.PdfDisabled{}` path unchanged.
  </acceptance_criteria>
  <behavior>
    - Test 1: the explicit Chromic invoice path returns a typed terminal error when the host did not start `ChromicPDF`.
    - Test 2: `%Accrue.Error.PdfDisabled{}` remains the typed contract for intentional PDF disablement and is not replaced by the new error.
  </behavior>
  <action>Create `Accrue.Error.InvoiceRendererUnavailable` in `accrue/lib/accrue/errors.ex` with exact fields `:adapter`, `:reason`, and `:message`; use it for D-05 and D-06 so hosts can pattern-match invoice-path misconfiguration separately from `%Accrue.Error.PdfDisabled{}`. Update `accrue/lib/accrue/invoices.ex` so `ensure_adapter_available/1` returns `{:error, %Accrue.Error.InvoiceRendererUnavailable{adapter: Accrue.InvoiceRenderer.ChromicPDF, reason: :chromic_pdf_not_started, message: ...}}` when the explicit Chromic invoice adapter is configured but `Process.whereis(ChromicPDF)` is nil. Keep D-01, D-02, D-13, and D-14 intact: do not add a fallback chain, do not read `:pdf_adapter`, and do not widen the public invoice API beyond the typed error replacement. Update the invoice facade module docs and `accrue/test/accrue/billing/pdf_test.exs` to assert the exact new struct contract instead of the bare atom.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/pdf_test.exs --trace</automated>
  </verify>
  <done>The invoice facade exposes a stable, host-matchable misconfiguration error for the explicit Chromic path and no longer leaks the bare `:chromic_pdf_not_started` atom.</done>
</task>

<task type="auto">
  <name>Task 107-01-02: Add migration-aware boot warnings and invoice-path-wide unavailable telemetry without automatic fallback</name>
  <files>accrue/lib/accrue/application.ex, accrue/lib/accrue/workers/mailer.ex, accrue/test/accrue/application_boot_guards_test.exs, accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs</files>
  <read_first>.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md, accrue/lib/accrue/application.ex, accrue/lib/accrue/workers/mailer.ex, accrue/lib/accrue/telemetry/ops.ex, accrue/test/accrue/application_boot_guards_test.exs, accrue/test/accrue/webhook/default_handler_mailer_dispatch_test.exs</read_first>
  <acceptance_criteria>
    - `accrue/test/accrue/application_boot_guards_test.exs` no longer mutates only `:pdf_adapter` when covering `warn_pdf_adapter_unavailable/0`; it sets `:invoice_pdf_adapter`.
    - `accrue/lib/accrue/application.ex` contains a new boot-warning branch that checks `Application.get_env(:accrue, :pdf_adapter)` and `Application.get_env(:accrue, :invoice_pdf_adapter, Accrue.InvoiceRenderer.Rendro)` separately.
    - `accrue/lib/accrue/application.ex` includes warning text containing both exact strings `:pdf_adapter` and `:invoice_pdf_adapter`.
    - `accrue/lib/accrue/workers/mailer.ex` pattern-matches `%Accrue.Error.InvoiceRendererUnavailable{}` as a terminal degrade-to-hosted-url case.
    - `accrue/lib/accrue/workers/mailer.ex` no longer contains a direct branch for `{:error, :chromic_pdf_not_started}`.
    - `accrue/lib/accrue/workers/mailer.ex` uses `Accrue.Telemetry.Ops.emit/3` or relies on `accrue/lib/accrue/invoices.ex` doing so; there is no raw `:telemetry.execute([:accrue, :ops, :pdf_adapter_unavailable], ...)` call left in the mailer file.
  </acceptance_criteria>
  <action>Repair the broken boot-guard proof lane first: change `accrue/test/accrue/application_boot_guards_test.exs` to preserve and mutate `:invoice_pdf_adapter` when exercising `warn_pdf_adapter_unavailable/0`, not `:pdf_adapter`. In `accrue/lib/accrue/application.ex`, keep the existing explicit-Chromic boot warning and add a second migration-focused warning per D-15 and D-13 for the legacy-host case where `:pdf_adapter` is explicitly configured but `:invoice_pdf_adapter` remains unset; use a separate `:persistent_term` dedupe key and mention both config keys in the warning body. In `accrue/lib/accrue/workers/mailer.ex`, replace the bare `:chromic_pdf_not_started` branch with `%Accrue.Error.InvoiceRendererUnavailable{adapter: Accrue.InvoiceRenderer.ChromicPDF}` and treat it as a terminal degrade-to-hosted-invoice-url case, not a retry path, per D-04 and D-08. Broaden D-07 telemetry by routing the unavailable signal through `Accrue.Telemetry.Ops.emit/3` with low-cardinality metadata such as `adapter: :chromic_pdf` and `surface: :invoice_pdf`; do not add template ids, invoice ids, or customer data to the metadata, and do not introduce any automatic Rendro->Chromic runtime fallback.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/application_boot_guards_test.exs test/accrue/webhook/default_handler_mailer_dispatch_test.exs --trace</automated>
  </verify>
  <done>The explicit Chromic compatibility lane now warns legacy hosts correctly, degrades predictably without retries, and emits one low-cardinality ops signal family outside the old mailer-only branch.</done>
</task>

<task type="auto">
  <name>Task 107-01-03: Update the narrow host docs so the adapter split and failure messaging match the code</name>
  <files>accrue/guides/pdf.md, accrue/guides/configuration.md, accrue/guides/telemetry.md</files>
  <read_first>.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md, accrue/guides/pdf.md, accrue/guides/configuration.md, accrue/guides/telemetry.md, accrue/lib/accrue/application.ex, accrue/lib/accrue/invoices.ex</read_first>
  <acceptance_criteria>
    - `accrue/guides/pdf.md` contains the exact phrase `explicit compatibility path`.
    - `accrue/guides/pdf.md` contains both exact strings `:invoice_pdf_adapter` and `:pdf_adapter` and describes `:pdf_adapter` as a lower-level HTML seam.
    - `accrue/guides/pdf.md` documents `%Accrue.Error.InvoiceRendererUnavailable{}` by exact module name.
    - `accrue/guides/configuration.md` names `:invoice_pdf_adapter` as the invoice renderer switch and `:pdf_adapter` as the advanced HTML seam.
    - `accrue/guides/telemetry.md` lists `[:accrue, :ops, :pdf_adapter_unavailable]` with invoice-path ownership that is no longer mailer-only.
  </acceptance_criteria>
  <action>Make the minimum doc edits needed for D-03, D-16, and D-17 without pre-consuming Phase 108. In `accrue/guides/pdf.md`, keep Rendro as the first and only default story, rename the Chromic section as an explicit compatibility path, and document `%Accrue.Error.InvoiceRendererUnavailable{}` as the typed result for misconfigured explicit Chromic opt-in. In `accrue/guides/configuration.md`, state concretely that `:invoice_pdf_adapter` owns invoice rendering while `:pdf_adapter` remains the lower-level HTML seam used by `Accrue.PDF` and by Chromic-backed HTML renderers. In `accrue/guides/telemetry.md`, keep the existing `[:accrue, :ops, :pdf_adapter_unavailable]` event name for compatibility unless the implementation changed it, but update the owner/metadata description so it matches the new invoice-path-wide emission and typed degradation contract. Leave broader production-readiness and migration rewrites to Phase 108.</action>
  <verify>
    <automated>rg -n "explicit compatibility path|InvoiceRendererUnavailable|invoice_pdf_adapter|pdf_adapter|pdf_adapter_unavailable" accrue/guides/pdf.md accrue/guides/configuration.md accrue/guides/telemetry.md</automated>
  </verify>
  <done>The public guidance is honest about the explicit Chromic compatibility lane, the adapter-key split, and the typed failure/telemetry contract that operators will see in production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config -> Accrue boot/runtime behavior | Untrusted host config keys decide which invoice renderer path runs and whether warnings must fire |
| invoice render failure -> worker retry path | Stable misconfiguration must not masquerade as transient render failure |
| telemetry event -> operator alerting | Ops metadata must remain low-cardinality and PII-free while still identifying the degraded invoice path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | T | `accrue/lib/accrue/invoices.ex` | mitigate | Return `%Accrue.Error.InvoiceRendererUnavailable{}` with fixed adapter/reason fields so the explicit Chromic misconfig cannot be confused with a successful render or a generic crash. |
| T-107-02 | D | `accrue/lib/accrue/workers/mailer.ex` | mitigate | Treat the typed unavailable error as terminal degrade-to-link behavior and avoid retry storms or hidden fallback chains. |
| T-107-03 | R | `accrue/lib/accrue/application.ex` | mitigate | Add explicit boot warnings for both the configured Chromic opt-in and the legacy `:pdf_adapter`-without-`:invoice_pdf_adapter` migration case, backed by corrected tests. |
| T-107-04 | I | `accrue/lib/accrue/telemetry/ops.ex` | mitigate | Emit only low-cardinality adapter/surface metadata and exclude invoice ids, customer data, or raw error payloads. |
</threat_model>

<verification>
Run the plan's two Mix test commands and the final docs grep.
Confirm there is no remaining `{:error, :chromic_pdf_not_started}` contract in invoice-facing code or tests.
</verification>

<success_criteria>
PDF-06 is satisfied when the optional Chromic invoice path is explicit, typed, observable, and migration-safe: no automatic fallback chain exists, the invoice facade no longer leaks a bare atom, boot warnings cover both misconfiguration classes, and the narrow host docs match the code.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-01-SUMMARY.md`
</output>

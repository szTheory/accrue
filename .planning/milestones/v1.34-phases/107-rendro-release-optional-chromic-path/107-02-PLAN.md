---
phase: 107-rendro-release-optional-chromic-path
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - accrue/mix.exs
  - accrue/mix.lock
  - RELEASING.md
  - scripts/ci/verify_rendro_hex_resolution.sh
autonomous: true
requirements:
  - PDF-07
must_haves:
  truths:
    - "Accrue resolves Rendro from Hex as `~> 0.1.0` instead of from the local sibling path `../../rendro`."
    - "The release handoff proves registry-backed dependency truth from a clean checkout rather than relying on a maintainer workspace."
    - "The release runbook preserves the order publish Rendro -> confirm Hex availability -> update Accrue -> verify clean-checkout Hex resolution."
  artifacts:
    - path: "accrue/mix.exs"
      provides: "Published Hex dependency tuple `{:rendro, \"~> 0.1.0\"}` instead of a local path dep"
    - path: "accrue/mix.lock"
      provides: "Resolved Hex lock entry for Rendro"
    - path: "scripts/ci/verify_rendro_hex_resolution.sh"
      provides: "Checked-in clean-checkout proof script for Hex-backed Rendro resolution"
    - path: "RELEASING.md"
      provides: "Maintainer runbook for the Rendro publish handoff order and clean-checkout proof"
  key_links:
    - from: "accrue/mix.exs"
      to: "accrue/mix.lock"
      via: "The dependency tuple must lock to a published Hex package rather than a path dependency."
      pattern: "rendro|0\\.1\\.0"
    - from: "scripts/ci/verify_rendro_hex_resolution.sh"
      to: "RELEASING.md"
      via: "The runbook should point maintainers at the checked-in clean-checkout proof command."
      pattern: "verify_rendro_hex_resolution|mix deps.get"
---

<objective>
Replace the temporary local Rendro path dependency with the published Hex package and add a clean-checkout proof lane that shows release truth from the public registry.

Purpose: satisfy PDF-07 and close the last release-safety gap before the milestone can finish on real consumer semantics instead of maintainer-local convenience.
Output: Hex-based Rendro dependency resolution in `accrue`, a checked-in proof script, and runbook instructions that codify the publish order and verification step.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md
@.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-PATTERNS.md
@.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-01-PLAN.md
@accrue/mix.exs
@accrue/mix.lock
@RELEASING.md

<interfaces>
From `accrue/mix.exs`:
```elixir
defp deps do
  [
    {:rendro, path: "../../rendro"},
    {:chromic_pdf, "~> 1.17"}
  ]
end
```

From `RELEASING.md`:
```markdown
Manual fallback order:

1. Publish `accrue`.
2. Confirm Hex availability.
3. Publish `accrue_admin`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 107-02-01: Cut Accrue over to the published Rendro Hex dependency</name>
  <files>accrue/mix.exs, accrue/mix.lock</files>
  <read_first>.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md, accrue/mix.exs, accrue/mix.lock, RELEASING.md</read_first>
  <acceptance_criteria>
    - `accrue/mix.exs` contains the exact tuple `{:rendro, "~> 0.1.0"}`.
    - `accrue/mix.exs` no longer contains the string `path: "../../rendro"`.
    - `accrue/mix.lock` contains a Hex-backed `rendro` entry for version `0.1.0`.
    - `cd accrue && mix deps.get` exits 0 with the Hex dependency tuple in place.
  </acceptance_criteria>
  <action>Apply D-09, D-10, and D-11 exactly in `accrue/mix.exs`: replace `{:rendro, path: "../../rendro"}` with `{:rendro, "~> 0.1.0"}` and leave Rendro outside the linked Release Please package set. Refresh `accrue/mix.lock` from Hex so the repo records the published package rather than a local path resolution. Do not widen the version constraint beyond `~> 0.1.0`, do not use an exact pin, and do not change unrelated dependency versions while refreshing the lock.</action>
  <verify>
    <automated>cd accrue && mix deps.get && rg -n '\{:rendro, "~> 0\.1\.0"\}' mix.exs && ! rg -n 'path:\s*"../../rendro"' mix.exs && rg -n '"rendro": \{:hex, :rendro, "0\.1\.0"' mix.lock && mix deps.tree | rg 'rendro'</automated>
  </verify>
  <done>Accrue now consumes Rendro the way external users will: from Hex at `~> 0.1.0`, with a real lock entry and no sibling checkout requirement.</done>
</task>

<task type="auto">
  <name>Task 107-02-02: Add a clean-checkout Rendro Hex proof script and codify the release handoff order</name>
  <files>scripts/ci/verify_rendro_hex_resolution.sh, RELEASING.md</files>
  <read_first>.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-RESEARCH.md, RELEASING.md, accrue/mix.exs</read_first>
  <acceptance_criteria>
    - `scripts/ci/verify_rendro_hex_resolution.sh` exists and is executable.
    - The script contains the exact strings `git clone`, `mix deps.get`, `mix deps.tree`, and `../../rendro`.
    - The script fails if `accrue/mix.exs` still contains `../../rendro` or if `mix deps.tree` does not show `rendro`.
    - `RELEASING.md` documents the order `publish Rendro`, `confirm Hex availability`, `update/use the published Rendro version in Accrue`, and `run the clean-checkout verification script`.
  </acceptance_criteria>
  <action>Create `scripts/ci/verify_rendro_hex_resolution.sh` as the checked-in PDF-07 proof artifact. The script should start from a clean temporary clone of the current repo, assert that `accrue/mix.exs` does not contain `../../rendro`, run `mix deps.get` inside `accrue/`, and confirm via `mix deps.tree` (or equivalent Mix output) that `rendro` resolves as a normal Hex dependency. Keep the proof deterministic and shell-only: no hidden environment assumptions, no dependence on an existing sibling Rendro checkout, and no mutation of the source workspace outside the temporary directory. Update `RELEASING.md` to codify D-12: publish Rendro first, confirm Hex availability, then cut Accrue over to the published version and run `bash scripts/ci/verify_rendro_hex_resolution.sh` before milestone closeout.</action>
  <verify>
    <automated>bash scripts/ci/verify_rendro_hex_resolution.sh</automated>
  </verify>
  <done>The repo contains a repeatable clean-checkout proof lane and a release runbook that binds the Rendro publish handoff to public-registry truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer workspace -> published package consumers | Local sibling repos can hide release defects that public consumers will hit immediately |
| Hex registry -> Accrue dependency graph | The repo must lock to the published Rendro package and checksum rather than a mutable local path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-05 | T | `accrue/mix.exs` | mitigate | Replace the local path dep with `{:rendro, "~> 0.1.0"}` so package consumers resolve the published artifact. |
| T-107-06 | R | `scripts/ci/verify_rendro_hex_resolution.sh` | mitigate | Use a clean temporary clone plus explicit guards against `../../rendro` so the proof is reproducible and auditable. |
| T-107-07 | D | `RELEASING.md` | mitigate | Record the required publish order and the clean-checkout verification step so maintainers do not close the milestone on local workspace truth. |
</threat_model>

<verification>
Run `cd accrue && mix deps.get && mix deps.tree | rg "rendro"`.
Run `bash scripts/ci/verify_rendro_hex_resolution.sh`.
Confirm `accrue/mix.exs` has no remaining `path: "../../rendro"` tuple.
</verification>

<success_criteria>
PDF-07 is satisfied when Accrue consumes Rendro from Hex at `~> 0.1.0`, the repo contains a checked-in clean-checkout verification lane, and the release runbook explicitly requires registry-backed proof before milestone closeout.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.34-phases/107-rendro-release-optional-chromic-path/107-02-SUMMARY.md`
</output>

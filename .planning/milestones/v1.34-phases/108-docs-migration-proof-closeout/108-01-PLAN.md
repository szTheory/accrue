---
phase: 108-docs-migration-proof-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/README.md
  - accrue/guides/first_hour.md
  - accrue/guides/production-readiness.md
  - accrue/guides/configuration.md
  - accrue/guides/pdf.md
  - accrue/guides/upgrade.md
autonomous: true
requirements:
  - PDF-08
  - PDF-09
must_haves:
  truths:
    - "New adopters can see from the package front door that Rendro is the default invoice renderer and that Chrome is not required on the normal invoice path."
    - "Legacy hosts have one canonical migration section that explains exactly how `:invoice_pdf_adapter` and `:pdf_adapter` interact after the seam split."
    - "Operational checklist docs name `:invoice_pdf_adapter` explicitly and only mention Chromic supervision/pool sizing when the host intentionally opts into the explicit compatibility path."
  artifacts:
    - path: "accrue/guides/pdf.md"
      provides: "Canonical Rendro-first adapter guide plus dedicated three-state migration section"
    - path: "accrue/README.md"
      provides: "Front-door Rendro-first install statement with pointer to the PDF guide"
    - path: "accrue/guides/first_hour.md"
      provides: "First-user expectation that default invoice rendering no longer requires Chrome"
    - path: "accrue/guides/production-readiness.md"
      provides: "Renderer-specific production checklist items"
    - path: "accrue/guides/configuration.md"
      provides: "Config-key SSOT for `:invoice_pdf_adapter` vs `:pdf_adapter`"
    - path: "accrue/guides/upgrade.md"
      provides: "Short upgrade pointer into the canonical migration section"
  key_links:
    - from: "accrue/README.md"
      to: "accrue/guides/pdf.md"
      via: "Front-door package docs should point new users to the canonical deep renderer guide."
      pattern: "Rendro|PDF Rendering|guides/pdf.md"
    - from: "accrue/guides/production-readiness.md"
      to: "accrue/guides/configuration.md"
      via: "Checklist wording must match the config-key ownership SSOT."
      pattern: "invoice_pdf_adapter|pdf_adapter"
    - from: "accrue/guides/upgrade.md"
      to: "accrue/guides/pdf.md"
      via: "Upgrade guidance should point at the canonical migration section instead of duplicating it."
      pattern: "Migration|invoice_pdf_adapter"
---

<objective>
Align the front-door and operational docs with the Rendro-first invoice renderer contract and add the canonical migration section for legacy `:pdf_adapter` / Chromic users.

Purpose: satisfy PDF-08 and PDF-09 at the package front door before final proof closeout, so new users and legacy hosts get one coherent story instead of piecing it together from source and warnings.
Output: updated README / First Hour / Production Readiness / Configuration guidance plus a dedicated migration section in `accrue/guides/pdf.md` and a short pointer from `upgrade.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-CONTEXT.md
@.planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-RESEARCH.md
@.planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-PATTERNS.md
@accrue/README.md
@accrue/guides/first_hour.md
@accrue/guides/production-readiness.md
@accrue/guides/configuration.md
@accrue/guides/pdf.md
@accrue/guides/upgrade.md
</context>

<tasks>

<task type="auto">
  <name>Task 108-01-01: Surface the Rendro-first default in the package front door and operational pointers</name>
  <files>accrue/README.md, accrue/guides/first_hour.md, accrue/guides/production-readiness.md, accrue/guides/configuration.md</files>
  <read_first>.planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-CONTEXT.md, .planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-RESEARCH.md, .planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-PATTERNS.md, accrue/README.md, accrue/guides/first_hour.md, accrue/guides/production-readiness.md, accrue/guides/configuration.md, accrue/guides/pdf.md, accrue/lib/accrue/application.ex</read_first>
  <acceptance_criteria>
    - `accrue/README.md` contains both exact strings `Rendro` and `guides/pdf.md`.
    - `accrue/README.md` states that invoice PDFs no longer require Chrome on the normal path, using exact wording that includes `Chrome` or `ChromicPDF`.
    - `accrue/guides/first_hour.md` contains both exact strings `Rendro` and `guides/pdf.md`.
    - `accrue/guides/production-readiness.md` contains the exact string `:invoice_pdf_adapter`.
    - `accrue/guides/production-readiness.md` contains the exact string `ChromicPDF`.
    - `accrue/guides/configuration.md` contains both exact strings `Accrue.InvoiceRenderer.Rendro` and `:invoice_pdf_adapter`.
    - `accrue/guides/configuration.md` still contains the exact string `:pdf_adapter`.
  </acceptance_criteria>
  <action>Update the front-door docs in place without duplicating the deep guide. In `accrue/README.md`, add one high-signal statement near the install/start-here material that invoice PDFs are Rendro-first by default and do not require Chrome on the normal path, and point readers to `guides/pdf.md` for renderer details. In `accrue/guides/first_hour.md`, add a short expectation-setting note near the install/runtime spine that default invoice rendering is Rendro-backed and Chrome is only relevant for the explicit Chromic compatibility path; keep the guide focused on the first-user walkthrough, not deep PDF setup. In `accrue/guides/production-readiness.md`, replace the stale optional-adapter wording with explicit checklist bullets for `:invoice_pdf_adapter`, Chromic supervision/pool sizing only when `Accrue.InvoiceRenderer.ChromicPDF` is chosen, and asset/font validation for invoices plus attachments. In `accrue/guides/configuration.md`, keep the key-ownership SSOT concise but concrete by showing `config :accrue, :invoice_pdf_adapter, Accrue.InvoiceRenderer.Rendro` as the default invoice example and restating that `:pdf_adapter` remains the lower-level HTML seam only.</action>
  <verify>
    <automated>rg -n "Rendro|guides/pdf.md|invoice_pdf_adapter|ChromicPDF|Accrue.InvoiceRenderer.Rendro|pdf_adapter" accrue/README.md accrue/guides/first_hour.md accrue/guides/production-readiness.md accrue/guides/configuration.md</automated>
  </verify>
  <done>The package front door, first-user walkthrough, and operational checklist all describe the same Rendro-first default and explicit compatibility posture.</done>
</task>

<task type="auto">
  <name>Task 108-01-02: Add the canonical migration section and short upgrade pointer</name>
  <files>accrue/guides/pdf.md, accrue/guides/upgrade.md</files>
  <read_first>.planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-CONTEXT.md, .planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-RESEARCH.md, .planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-PATTERNS.md, accrue/guides/pdf.md, accrue/guides/upgrade.md, accrue/lib/accrue/config.ex, accrue/lib/accrue/application.ex, accrue/lib/accrue/invoices.ex</read_first>
  <acceptance_criteria>
    - `accrue/guides/pdf.md` contains a heading with the exact string `Migration`.
    - `accrue/guides/pdf.md` contains the exact string `no action needed`.
    - `accrue/guides/pdf.md` contains both exact strings `:invoice_pdf_adapter` and `:pdf_adapter`.
    - `accrue/guides/pdf.md` contains the exact string `Accrue.InvoiceRenderer.ChromicPDF`.
    - `accrue/guides/pdf.md` contains wording that explicitly says invoice rendering does not infer behavior from `:pdf_adapter`.
    - `accrue/guides/upgrade.md` contains the exact string `:invoice_pdf_adapter`.
    - `accrue/guides/upgrade.md` contains a pointer to `guides/pdf.md` or `PDF Rendering`.
  </acceptance_criteria>
  <action>Add a dedicated migration section to `accrue/guides/pdf.md` that covers exactly the three locked host states from D-02. State concretely that hosts with no custom PDF config need no action because Rendro is the default invoice renderer; hosts that only set `:pdf_adapter` must now set `config :accrue, :invoice_pdf_adapter, Accrue.InvoiceRenderer.ChromicPDF` if they want the legacy Chrome-backed invoice path; and hosts with a custom `Accrue.PDF` HTML seam should keep `:pdf_adapter` for that seam while treating invoice rendering as unchanged unless `:invoice_pdf_adapter` is explicitly set. Keep the seam split explicit and do not add any compatibility inference. In `accrue/guides/upgrade.md`, add a short migration pointer for legacy PDF users that sends them to the new canonical section instead of repeating the whole story there.</action>
  <verify>
    <automated>rg -n "Migration|no action needed|invoice_pdf_adapter|pdf_adapter|Accrue.InvoiceRenderer.ChromicPDF|infer" accrue/guides/pdf.md accrue/guides/upgrade.md</automated>
  </verify>
  <done>Legacy PDF users now have one canonical migration section, and the upgrade guide points at it without creating a second source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package docs -> host configuration | Docs drive whether a host configures the right renderer keys and whether operators assume Chrome is required by default |
| migration guidance -> runtime behavior | Incomplete or misleading upgrade prose can cause hosts to keep `:pdf_adapter` only and misread the new invoice-renderer split |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | T | `accrue/README.md`, `accrue/guides/first_hour.md`, `accrue/guides/production-readiness.md`, `accrue/guides/configuration.md` | mitigate | State the Rendro-first default and the `:invoice_pdf_adapter` key explicitly so readers cannot confuse the invoice path with the old HTML seam. |
| T-108-02 | R | `accrue/guides/pdf.md`, `accrue/guides/upgrade.md` | mitigate | Add a dedicated migration section covering the exact three host states and explicitly reject hidden inference from `:pdf_adapter`. |
</threat_model>

<verification>
Run the grep command from each task.
Run `bash scripts/ci/verify_package_docs.sh` after both tasks complete to ensure the touched package-doc surfaces still satisfy the checked-in docs contract.
</verification>

<success_criteria>
PDF-08 and PDF-09 are satisfied for the front-door slice when the package README, first-user walkthrough, and production checklist all point at the same Rendro-first renderer story, and `accrue/guides/pdf.md` contains the canonical three-state migration contract for legacy `:pdf_adapter` / Chromic users.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.34-phases/108-docs-migration-proof-closeout/108-01-SUMMARY.md`
</output>

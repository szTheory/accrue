---
phase: 111-webhook-operator-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/webhooks.md
  - accrue/guides/telemetry.md
  - accrue/guides/operator-runbooks.md
  - accrue/guides/braintree-metered-billing.md
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "Webhook and operator docs must describe Stripe and Braintree honestly rather than using Stripe-only webhook language."
    - "Braintree local checkout completion, DLQ replay, and metered-renewal repair are part of the supported operator story."
    - "Telemetry owns tuple semantics; runbooks own ordered triage."
  artifacts:
    - path: accrue/guides/webhooks.md
      provides: processor-aware webhook boundary, replay entry points, and local-checkout completion framing
    - path: accrue/guides/telemetry.md
      provides: explicit tuple semantics and references for the operator-facing Braintree recovery story
    - path: accrue/guides/operator-runbooks.md
      provides: ordered replay/recovery procedure for DLQ and Braintree-specific cases
    - path: accrue/guides/braintree-metered-billing.md
      provides: conceptual metered-recovery explainer aligned to the same operator story
  key_links:
    - from: accrue/guides/webhooks.md
      to: accrue/lib/accrue/webhook/plug.ex
      via: "The docs must match the shared host ingress boundary and processor-specific parse paths."
      pattern: "stripe|braintree|parse_braintree!"
    - from: accrue/guides/webhooks.md
      to: accrue/lib/accrue/webhook/default_handler.ex
      via: "The docs must explain the local portal checkout completion and Braintree normalization seams that runtime already owns."
      pattern: "accrue.portal.checkout.completed|normalize_braintree_type"
    - from: accrue/guides/operator-runbooks.md
      to: accrue/lib/accrue/webhooks/dlq.ex
      via: "Replay guidance should match the actual requeue entry points and dry-run semantics."
      pattern: "requeue|requeue_where"
---

<objective>
Make the webhook, telemetry, and operator-runbook docs processor-aware for the shipped Braintree slice without introducing new runtime scope.

Purpose: satisfy the written operator-support half of Phase 111 before proof lanes pin the contract.
Output: one coherent support story covering replay, recovery, checkout-completion ambiguity, and metered-renewal repair across the existing docs corpus.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/processor-support-matrix.md
@.planning/milestones/v1.35-phases/111-webhook-operator-closure/111-RESEARCH.md
@.planning/milestones/v1.35-phases/111-webhook-operator-closure/111-PATTERNS.md
@accrue/guides/webhooks.md
@accrue/guides/telemetry.md
@accrue/guides/operator-runbooks.md
@accrue/guides/braintree-metered-billing.md
@accrue/guides/braintree-local-portal.md
@accrue/lib/accrue/webhook/plug.ex
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/webhooks/dlq.ex
@accrue/lib/accrue/jobs/metered_renewal_reconciler.ex
</context>

<tasks>

<task type="auto">
  <name>Task 111-01-01: Rewrite the webhook and telemetry docs around a processor-aware support contract</name>
  <files>accrue/guides/webhooks.md, accrue/guides/telemetry.md</files>
  <read_first>.planning/milestones/v1.35-phases/111-webhook-operator-closure/111-RESEARCH.md, .planning/milestones/v1.35-phases/111-webhook-operator-closure/111-PATTERNS.md, accrue/guides/webhooks.md, accrue/guides/telemetry.md, accrue/lib/accrue/webhook/plug.ex, accrue/lib/accrue/webhook/default_handler.ex, accrue/lib/accrue/config.ex</read_first>
  <acceptance_criteria>
    - `accrue/guides/webhooks.md` contains the exact string `Braintree`.
    - `accrue/guides/webhooks.md` contains the exact strings `portal_base_url`, `portal_mount_path`, and `accrue.portal.checkout.completed`.
    - `accrue/guides/webhooks.md` mentions `mix accrue.webhooks.replay` or `Accrue.Webhooks.DLQ`.
    - `accrue/guides/telemetry.md` still contains the exact strings `[:accrue, :ops, :webhook_dlq, :replay]` and `[:accrue, :ops, :metered_renewal_stale_repaired]`.
    - The two guides describe checkout completion as locally persisted/projection-backed on Braintree rather than as an upstream hosted redirect truth.
  </acceptance_criteria>
  <action>Extend `guides/webhooks.md` using the processor-delta pattern in `111-PATTERNS.md`: keep the shared host boundary described once, then add explicit Braintree notes for signature parsing, local portal checkout completion, replay/recovery entry points, and when `portal_base_url` or `portal_mount_path` misconfiguration causes support-visible failures. In `guides/telemetry.md`, keep the tuple catalog authoritative while adding just enough wording around `webhook_dlq`, `portal checkout completed`, and Braintree metered-renewal tuples to support the operator story without duplicating the runbook.</action>
  <verify>
    <automated>rg -n "Braintree|portal_base_url|portal_mount_path|accrue.portal.checkout.completed|mix accrue.webhooks.replay|\\[:accrue, :ops, :webhook_dlq, :replay\\]|\\[:accrue, :ops, :metered_renewal_stale_repaired\\]" accrue/guides/webhooks.md accrue/guides/telemetry.md</automated>
  </verify>
  <done>The webhook and telemetry docs now expose one processor-aware operator truth for Stripe plus Braintree.</done>
</task>

<task type="auto">
  <name>Task 111-01-02: Align operator runbooks and Braintree metered guidance to the same replay and recovery story</name>
  <files>accrue/guides/operator-runbooks.md, accrue/guides/braintree-metered-billing.md</files>
  <read_first>.planning/milestones/v1.35-phases/111-webhook-operator-closure/111-RESEARCH.md, .planning/milestones/v1.35-phases/111-webhook-operator-closure/111-PATTERNS.md, accrue/guides/operator-runbooks.md, accrue/guides/braintree-metered-billing.md, accrue/lib/accrue/webhooks/dlq.ex, accrue/lib/accrue/jobs/metered_renewal_reconciler.ex, accrue/test/accrue/webhooks/dlq_test.exs, accrue/test/accrue/telemetry/portal_checkout_completed_test.exs</read_first>
  <acceptance_criteria>
    - `accrue/guides/operator-runbooks.md` contains the exact strings `webhook_dlq`, `metered_renewal_stale_repaired`, and `accrue.portal.checkout.completed`.
    - `accrue/guides/operator-runbooks.md` explains a Braintree-specific replay or checkout-completion ambiguity path rather than only generic Stripe replay.
    - `accrue/guides/braintree-metered-billing.md` still contains the exact strings `awaiting-payment-method` and `failed-exhausted`.
    - `accrue/guides/braintree-metered-billing.md` points readers back to `operator-runbooks.md` for ordered triage.
    - Neither file promises full provider parity or introduces new unsupported recovery behavior.
  </acceptance_criteria>
  <action>Follow the docs-layering pattern from `111-PATTERNS.md`: keep `operator-runbooks.md` as the ordered procedure surface and update it so Braintree replay, local portal checkout completion, drift diagnosis, and metered-renewal repair feel first-class instead of bolted onto Stripe-first guidance. In `braintree-metered-billing.md`, keep the architecture explanation concise, but make the operator handoff explicit by linking the recovery states and stale-window repair story back into the runbook sequence.</action>
  <verify>
    <automated>rg -n "webhook_dlq|metered_renewal_stale_repaired|accrue.portal.checkout.completed|awaiting-payment-method|failed-exhausted|operator-runbooks.md" accrue/guides/operator-runbooks.md accrue/guides/braintree-metered-billing.md</automated>
  </verify>
  <done>The runbook and Braintree metered guide now describe one coherent replay and recovery story.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime recovery semantics -> docs | If docs omit Braintree-specific recovery paths, operators will misdiagnose mounted local failures as generic Stripe-like webhook issues. |
| telemetry tuple catalog -> runbooks | If tuple meaning and ordered procedure drift apart, on-call work becomes inconsistent and unsafe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | T | `guides/webhooks.md` | mitigate | Add explicit Braintree webhook and local-checkout completion guidance anchored to current runtime behavior. |
| T-111-02 | R | operator recovery docs | mitigate | Keep tuple semantics in `telemetry.md` and ordered replay/recovery steps in `operator-runbooks.md`. |
</threat_model>

<verification>
Run the per-task `rg` commands.
Run `cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs test/accrue/billing_portal_test.exs` after both tasks complete so the adjacent guide assertions still hold.
</verification>

<success_criteria>
OPS-01 is satisfied for the documentation slice when webhook docs, telemetry references, and operator runbooks all describe the shipped Braintree replay and recovery story without implying new runtime scope or false provider parity.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.35-phases/111-webhook-operator-closure/111-01-SUMMARY.md`
</output>

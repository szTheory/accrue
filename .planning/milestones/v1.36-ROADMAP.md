# Milestone v1.36: Dual-Provider Core Completion

**Status:** ✅ SHIPPED 2026-05-07
**Phases:** 112-116
**Total Plans:** 11

## Overview

Closed the remaining staged rows in Accrue's official Stripe + Braintree
gateway-subscription-core contract by promoting bounded first-party customer
update support, normalizing immediate-versus-scheduled cancellation semantics,
locking docs and host proof to the same provider-honest wording, and restoring
the full verification chain required by the milestone audit.

## Phases

### Phase 112: Customer Update Contract Closure

**Goal**: Make `Accrue.Billing.update_customer/2` an explicit first-party row instead of a staged holdover.
**Depends on**: v1.35 shipped
**Plans**: 3 plans

Plans:

- [x] 112-01: Promote `update_customer/2` to the bounded processor-backed contract
- [x] 112-02: Promote `customer.update` support truth across capability labels and proof
- [x] 112-03: Add the host-facing helper and installer-template mirror

**Details:**
Phase 112 converted `Accrue.Billing.update_customer/2` from a local-row helper into the bounded shared remote contract for `name`, `email`, and flat `metadata` across Fake, Stripe, and Braintree. The runtime capability map, planning support matrix, example host, and installer template now all present the same thin first-party story, while explicit local-only maintenance remains available through `update_customer_local/2`.

### Phase 113: Cancellation Semantics Closure

**Goal**: Make the shipped cancellation story coherent across facade verbs, capability labels, and Braintree-specific limits.
**Depends on**: Phase 112
**Plans**: 3 plans

Plans:

- [x] 113-01: Promote immediate cancellation support truth and keep scheduled-end semantics explicit
- [x] 113-02: Align docs, portal flows, admin guidance, and example-host copy to the same contract
- [x] 113-03: Add targeted drift gates and proof for the corrected cancellation boundary

**Details:**
Phase 113 promoted immediate cancellation to `all first-party` across Fake, Stripe, and Braintree, while preserving the truth that Braintree does not support generic scheduled-end cancellation. Portal/admin/host wording now branches by processor instead of implying parity theater, and merge-blocking tests guard both the capability labels and the touched UX copy against future drift.

### Phase 114: Contract Drift Gate Closeout

**Goal**: Finish the milestone by making the finalized dual-provider core contract the only truth across planning mirrors, docs, and verifier gates.
**Depends on**: Phase 113
**Plans**: 3 plans

Plans:

- [x] 114-01: Settle the canonical support-matrix wording
- [x] 114-02: Align package docs and example-host proof docs to the canonical matrix
- [x] 114-03: Tighten the support-contract verifier bundle and close the planning mirrors only after it is green

**Details:**
Phase 114 established `.planning/processor-support-matrix.md` as the settled contract spine, thinned the package and host-facing mirrors so they point back to that spine instead of restating it, and documented the named support-contract bundle that now blocks merge drift through targeted scripts plus the `docs-contracts-shift-left` CI home.

### Phase 115: Phase 113 Verification Backfill

**Goal**: Restore the missing Phase 113 verification artifact so `PROC-22` and `PROC-23` are represented in the audit-required verification chain.
**Depends on**: Phase 113
**Plans**: 1 plan

Plans:

- [x] 115-01: Backfill `113-VERIFICATION.md` and refresh the audit trace

**Details:**
Phase 115 treated the missing Phase 113 verification report as evidence repair only. It authored `113-VERIFICATION.md` from the shipped summaries plus same-day reruns of the existing proof lanes, then refreshed `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, and the milestone audit so the PROC-22 / PROC-23 orphan gap was fully closed without reopening runtime scope.

### Phase 116: Phase 114 Verification Backfill

**Goal**: Restore the missing Phase 114 verification artifact so `PROC-24` is represented in the audit-required verification chain.
**Depends on**: Phase 114
**Plans**: 1 plan

Plans:

- [x] 116-01: Backfill `114-VERIFICATION.md` and refresh the final audit trace

**Details:**
Phase 116 repeated the same narrow evidence-repair pattern for Phase 114. It restored `114-VERIFICATION.md`, reran the support-contract bundle and example-host proof lane, and flipped the milestone audit to `passed`, leaving `PROC-21..24` fully represented by verification artifacts before closeout.

---

## Milestone Summary

**Decimal Phases:**

- None

**Key Decisions:**

- Decision: `Accrue.Billing.update_customer/2` is a bounded first-party remote contract for the shared subset only, with explicit local-only maintenance kept separate.
- Decision: Only immediate cancellation is promoted to generic first-party support; Braintree scheduled-end cancellation stays explicitly unsupported and action-guided.
- Decision: `.planning/processor-support-matrix.md` is the full contract owner; package docs, host proof docs, and targeted verifiers point back to it instead of becoming parallel specs.
- Decision: Verification backfill phases may repair missing audit artifacts, but they must not reopen already-shipped implementation scope.

**Issues Resolved:**

- Closed the last staged customer-update row in the official dual-provider contract.
- Removed cancellation parity theater by aligning runtime labels, adapters, docs, portal/admin copy, and host proof to the actual supported boundary.
- Documented and hardened the named support-contract verifier bundle so doc and proof drift re-fails before merge.
- Restored `113-VERIFICATION.md` and `114-VERIFICATION.md`, clearing the final audit orphan gaps and producing a passed `v1.36` milestone audit.

**Issues Deferred:**

- Advanced scheduling, preview/proration parity, and broader lifecycle mutation parity remain outside the current dual-provider core boundary.
- Hyperwallet reopening and `FIN-03` finance exports remain explicit strategic non-goals.
- The next linked Hex release is a post-archive follow-up rather than part of this milestone's feature scope.

**Technical Debt Incurred:**

- None recorded in the passed milestone audit.

---

_For current project status, see .planning/ROADMAP.md_

---
phase: 112-customer-update-contract-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing.ex
  - accrue/test/accrue/billing/events_transaction_test.exs
autonomous: true
requirements:
  - PROC-21
must_haves:
  truths:
    - "`Accrue.Billing.update_customer/2` means bounded remote customer update plus local projection sync, not a local-only row edit."
    - "Only `name`, `email`, and flat string `metadata` are part of the shared first-party update contract."
    - "Hosts still have an explicit local-only customer-row maintenance path that does not imply processor support."
  artifacts:
    - path: accrue/lib/accrue/billing.ex
      provides: promoted `update_customer/2`, explicit local-only API, shared attr validation, projection-sync handling
    - path: accrue/test/accrue/billing/events_transaction_test.exs
      provides: executable facade proof for remote write-through semantics and projection-sync failure handling
  key_links:
    - from: accrue/lib/accrue/billing.ex
      to: accrue/lib/accrue/processor.ex
      via: "`update_customer/2` must call `Processor.update_customer/3` before persisting the local projection."
      pattern: "Processor\\.update_customer"
    - from: accrue/lib/accrue/billing.ex
      to: accrue/lib/accrue/billing/customer.ex
      via: "The local-only API may keep the broad local changeset path, but the shared facade must not reuse it as an unrestricted processor contract."
      pattern: "Customer\\.changeset"
---

<objective>
Promote the runtime meaning of `Accrue.Billing.update_customer/2` to the narrow first-party processor contract defined for Phase 112, while preserving a separate explicit local-only customer maintenance API.

Purpose: land the semantic contract change first so later label, matrix, and host-proof work can describe one real runtime truth.
Output: one billing-facade implementation that validates the supported attrs, performs remote write-through, projects the sanitized processor response locally, and keeps local-only maintenance separate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/STRATEGY.md
@.planning/processor-support-matrix.md
@.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md
@.planning/phases/112-customer-update-contract-closure/112-RESEARCH.md
@.planning/phases/112-customer-update-contract-closure/112-PATTERNS.md
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/customer.ex
@accrue/lib/accrue/processor.ex
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/test/accrue/billing/events_transaction_test.exs

<interfaces>
From `accrue/lib/accrue/billing.ex`:
```elixir
def update_customer_tax_location(%Customer{} = customer, attrs) when is_map(attrs)
def update_customer(%Customer{} = customer, attrs) when is_map(attrs)
defp customer_projection_attrs(processor_result) when is_map(processor_result)
defp sanitize_customer_data(processor_result) when is_map(processor_result)
```

From `accrue/lib/accrue/processor.ex`:
```elixir
@callback update_customer(id(), params(), opts()) :: result()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 112-01-01: Convert `update_customer/2` into the bounded remote write-through facade</name>
  <files>accrue/lib/accrue/billing.ex, accrue/test/accrue/billing/events_transaction_test.exs</files>
  <read_first>.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md, .planning/phases/112-customer-update-contract-closure/112-RESEARCH.md, .planning/phases/112-customer-update-contract-closure/112-PATTERNS.md, accrue/lib/accrue/billing.ex, accrue/lib/accrue/processor.ex, accrue/test/accrue/billing/events_transaction_test.exs</read_first>
  <acceptance_criteria>
    - `update_customer/2` rejects unsupported attrs before calling the processor.
    - The shared facade only accepts `name`, `email`, and flat string `metadata`.
    - The implementation follows the `update_customer_tax_location/2` shape: processor call first, projection sanitization second, local persistence and event recording inside `Repo.transact`.
    - The function does not become a provider-specific pass-through bag.
    - `events_transaction_test.exs` contains executable facade proof for remote processor dispatch, sanitized local projection, and the new `customer.updated` meaning.
  </acceptance_criteria>
  <action>In `accrue/lib/accrue/billing.ex`, replace the current local-only body of `update_customer/2` with a bounded remote write-through path per D-01, D-02, D-06, D-09, D-12, and D-16. Reuse the `update_customer_tax_location/2` projection pattern from the same file per D-05: validate the shared attr allowlist up front, call `Processor.update_customer(customer.processor_id, attrs, [])`, project the returned processor customer through `customer_projection_attrs/1`, persist the local projection inside `Repo.transact`, and record a bounded `customer.updated` event that means remote mutation accepted and locally projected. Keep the persisted projection limited to the existing sanitized fields per D-14; do not forward payment-method, nested address, or processor-specialized bags per D-03 and D-24..D-29. In `accrue/test/accrue/billing/events_transaction_test.exs`, add merge-blocking tests for the promoted facade semantics at the billing layer itself: accepted shared attrs trigger a processor-backed update, unsupported attrs fail before processor drift, sanitized projection data is preserved locally, and the recorded event reflects remote acceptance plus local projection rather than a local-only row edit.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/events_transaction_test.exs</automated>
  </verify>
  <done>`Accrue.Billing.update_customer/2` now expresses the first-party remote contract instead of a local-only row edit.</done>
</task>

<task type="auto">
  <name>Task 112-01-02: Preserve an explicit local-only API and projection-sync failure semantics</name>
  <files>accrue/lib/accrue/billing.ex, accrue/test/accrue/billing/events_transaction_test.exs</files>
  <read_first>.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md, .planning/phases/112-customer-update-contract-closure/112-RESEARCH.md, accrue/lib/accrue/billing.ex, accrue/lib/accrue/billing/customer.ex, accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/billing/events_transaction_test.exs</read_first>
  <acceptance_criteria>
    - A separate explicit local-only API exists for host-owned customer-row maintenance.
    - The promoted processor-backed path emits explicit projection-sync telemetry or equivalent bounded failure handling when remote success is followed by local persistence failure.
    - Event payload data stays privacy-safe and bounded to changed field names, local id, processor id/name, and an operation or correlation identifier.
    - The change does not claim webhook `customer.updated` is now authoritative in this phase.
    - `events_transaction_test.exs` deterministically proves the remote-success/local-persist-failure branch with a typed error plus telemetry/correlation evidence.
  </acceptance_criteria>
  <action>Still in `accrue/lib/accrue/billing.ex`, introduce or preserve a clearly named local-only customer maintenance API per D-08 and the discretion note, keeping the previous `Customer.changeset`-driven row-edit behavior on that explicit path rather than smuggling it through `update_customer/2`. Add bounded projection-sync failure handling per D-10 and D-11: if the remote update succeeds but local persistence fails, surface a typed projection-sync error and emit explicit telemetry/correlation metadata instead of retrying the remote mutation. Keep `customer.updated` event payloads privacy-safe per D-13, and do not deepen the webhook no-op gap or broaden the projection contract beyond the existing sanitization seam per D-14 and D-15. In `accrue/test/accrue/billing/events_transaction_test.exs`, add a deterministic failure-path test that forces remote-success/local-persist-failure, then asserts the typed projection-sync error plus emitted telemetry/correlation evidence required by D-10 and D-18. This proof must be merge-blocking and must not be replaced by grep-only checks.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/events_transaction_test.exs</automated>
  </verify>
  <done>The billing facade cleanly separates the promoted remote contract from explicit local-only maintenance and names projection-sync failures without widening scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host billing facade -> processor adapter | Untrusted customer attrs cross from app code into processor-facing writes here and must be narrowed before dispatch. |
| remote processor result -> local projection | Processor responses may contain extra nested fields or PII that must not be projected blindly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | T | `Accrue.Billing.update_customer/2` | mitigate | Reject unsupported attrs before `Processor.update_customer/3` and only allow `name`, `email`, and flat string `metadata`. |
| T-112-02 | I | local customer projection and event payload | mitigate | Reuse `customer_projection_attrs/1` sanitization, keep event payload bounded, and avoid persisting raw nested processor data. |
</threat_model>

<verification>
Run the per-task commands.
For a combined facade pass after both tasks, run `cd accrue && mix test test/accrue/billing/events_transaction_test.exs`.
</verification>

<success_criteria>
PROC-21 is satisfied for the runtime-semantics slice when `update_customer/2` is a narrow remote write-through contract, local-only customer row edits remain explicit and separate, and projection/event semantics no longer describe a local-only mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/112-customer-update-contract-closure/112-01-SUMMARY.md`
</output>

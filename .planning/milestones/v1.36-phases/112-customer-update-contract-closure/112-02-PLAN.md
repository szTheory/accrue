---
phase: 112-customer-update-contract-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - accrue/lib/accrue/processor/capabilities.ex
  - .planning/processor-support-matrix.md
  - accrue/test/accrue/processor/capabilities_test.exs
  - accrue/test/accrue/billing/events_transaction_test.exs
  - accrue/test/accrue/processor/fake_test.exs
  - accrue/test/accrue/processor/braintree_test.exs
  - accrue/test/accrue/processor/stripe_test.exs
autonomous: true
requirements:
  - PROC-21
must_haves:
  truths:
    - "Runtime capability labels and the public support matrix both describe `customer.update` as first-party rather than staged."
    - "Fake-first deterministic proof pins the shared attr contract, typed rejection semantics, projection preservation, and event meaning."
    - "Stripe and Braintree tests prove adapter truth for the promoted row without turning live-provider fidelity into the merge-blocking lane."
  artifacts:
    - path: accrue/lib/accrue/processor/capabilities.ex
      provides: promoted support label for `customer.update`
    - path: .planning/processor-support-matrix.md
      provides: public support-matrix row and facade mapping aligned to the promoted contract
    - path: accrue/test/accrue/processor/capabilities_test.exs
      provides: label truth gate
    - path: accrue/test/accrue/billing/events_transaction_test.exs
      provides: facade semantic proof for accepted attrs, rejected attrs, and event/projection meaning
    - path: accrue/test/accrue/processor/fake_test.exs
      provides: deterministic adapter proof
    - path: accrue/test/accrue/processor/braintree_test.exs
      provides: Braintree adapter truth for the narrow shared subset
    - path: accrue/test/accrue/processor/stripe_test.exs
      provides: Stripe adapter truth remains broad enough for specialized APIs while the shared facade stays narrow
  key_links:
    - from: accrue/lib/accrue/processor/capabilities.ex
      to: .planning/processor-support-matrix.md
      via: "Runtime and public support truth must move together for `customer.update`."
      pattern: "customer\\.update|Accrue\\.Billing\\.update_customer/2"
    - from: accrue/test/accrue/billing/events_transaction_test.exs
      to: accrue/lib/accrue/billing.ex
      via: "The billing-event tests must prove the new remote-write-through semantics and reject attr drift."
      pattern: "update_customer/2|customer.updated"
---

<objective>
Promote the support truth for `customer.update` and lock it behind deterministic core proof so the row cannot drift back to staged or widen past the narrow shared subset.

Purpose: make the promoted contract merge-blocking across capability labels, public mirrors, and the core proof bundle.
Output: aligned capability/matrix truth plus focused tests for label promotion, accepted/rejected attrs, projection preservation, and adapter semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/processor-support-matrix.md
@.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md
@.planning/phases/112-customer-update-contract-closure/112-RESEARCH.md
@.planning/phases/112-customer-update-contract-closure/112-PATTERNS.md
@.planning/phases/112-customer-update-contract-closure/112-01-PLAN.md
@accrue/lib/accrue/processor/capabilities.ex
@accrue/test/accrue/processor/capabilities_test.exs
@accrue/test/accrue/billing/events_transaction_test.exs
@accrue/test/accrue/processor/fake_test.exs
@accrue/test/accrue/processor/braintree_test.exs
@accrue/test/accrue/processor/stripe_test.exs
@accrue/lib/accrue/processor/fake.ex
@accrue/lib/accrue/processor/braintree.ex
@accrue/lib/accrue/processor/stripe.ex
</context>

<tasks>

<task type="auto">
  <name>Task 112-02-01: Promote runtime capability labels and support-matrix wording for the closed row</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, .planning/processor-support-matrix.md, accrue/test/accrue/processor/capabilities_test.exs</files>
  <read_first>.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md, .planning/phases/112-customer-update-contract-closure/112-RESEARCH.md, .planning/phases/112-customer-update-contract-closure/112-PATTERNS.md, accrue/lib/accrue/processor/capabilities.ex, .planning/processor-support-matrix.md, accrue/test/accrue/processor/capabilities_test.exs</read_first>
  <acceptance_criteria>
    - `Capabilities.support_label([:customer, :update])` is `all first-party`.
    - `.planning/processor-support-matrix.md` no longer describes `customer.update` or `Accrue.Billing.update_customer/2` as staged.
    - Cancellation rows remain staged where Phase 113 owns them.
    - The capability test explicitly pins the promoted row without widening other rows.
  </acceptance_criteria>
  <action>Update the runtime/public support SSOTs together per D-17, D-18, and the Phase 112 goal: in `accrue/lib/accrue/processor/capabilities.ex`, promote only `customer.update` from `staged first-party target` to `all first-party`, leaving cancellation semantics untouched for Phase 113. Mirror the same closure in `.planning/processor-support-matrix.md` by rewriting the capability row and the `Accrue.Billing.update_customer/2` facade mapping so they describe the narrow shared subset from D-01..D-03 instead of the old staged disclaimer. Extend `capabilities_test.exs` to assert the promoted `customer.update` label while preserving the existing staged cancellation assertions.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs && rg -n "customer.update|Accrue.Billing.update_customer/2|all first-party|staged first-party target" lib/accrue/processor/capabilities.ex ../.planning/processor-support-matrix.md</automated>
  </verify>
  <done>Runtime support labels and the public matrix now describe the same closed first-party `customer.update` row.</done>
</task>

<task type="auto">
  <name>Task 112-02-02: Add the merge-blocking core semantic proof bundle for the promoted customer-update contract</name>
  <files>accrue/test/accrue/billing/events_transaction_test.exs, accrue/test/accrue/processor/fake_test.exs, accrue/test/accrue/processor/braintree_test.exs, accrue/test/accrue/processor/stripe_test.exs</files>
  <read_first>.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md, .planning/phases/112-customer-update-contract-closure/112-RESEARCH.md, accrue/test/accrue/billing/events_transaction_test.exs, accrue/test/accrue/processor/fake_test.exs, accrue/test/accrue/processor/braintree_test.exs, accrue/test/accrue/processor/stripe_test.exs, accrue/lib/accrue/billing.ex, accrue/lib/accrue/processor/fake.ex, accrue/lib/accrue/processor/braintree.ex, accrue/lib/accrue/processor/stripe.ex</read_first>
  <acceptance_criteria>
    - Billing tests prove accepted attrs, rejected attrs, projection preservation, and the new `customer.updated` meaning.
    - Fake tests remain the deterministic merge-blocking proof lane for the promoted row.
    - Braintree tests prove the narrow shared subset through the adapter without widening into payment-method or nested customer params.
    - Stripe tests continue to protect specialized tax-location passthrough separately while proving the promoted shared row stays narrow at the billing facade.
  </acceptance_criteria>
  <action>Extend the core proof bundle per D-17..D-20. In `events_transaction_test.exs`, add cases for allowed shared attrs, explicit rejection of unsupported attrs per D-16, preserved sanitized projection per D-14, and `customer.updated` meaning per D-12 and D-13. In `fake_test.exs`, make the deterministic adapter lane prove the shared subset and any typed missing-resource or attr-validation expectations required by the new facade. In `braintree_test.exs`, pin `name`, `email`, and flat `metadata` truth through the current translator while avoiding provider-specific bag growth per D-25. In `stripe_test.exs`, keep the existing specialized tax-location passthrough assertions available for `update_customer_tax_location/2`, but add or adjust the customer-update proof so the promoted shared facade stays narrow instead of inheriting Stripe’s full request bag per D-24.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/events_transaction_test.exs test/accrue/processor/fake_test.exs test/accrue/processor/braintree_test.exs test/accrue/processor/stripe_test.exs</automated>
  </verify>
  <done>The promoted customer-update contract is pinned by deterministic library-level proof across facade and adapter lanes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime support labels -> public support matrix | If these surfaces drift, adopters and maintainers see contradictory processor truth. |
| billing facade contract -> adapter proof | If tests only cover adapters, the public shared contract can silently widen or regress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03 | R | support-label and support-matrix truth | mitigate | Promote `customer.update` in runtime and planning mirrors in the same plan and pin both with tests/grep. |
| T-112-04 | T | customer-update proof bundle | mitigate | Add deterministic tests for accepted attrs, rejected attrs, projection semantics, and adapter truth with Fake as the primary lane. |
</threat_model>

<verification>
Run the per-task automated commands.
For final closeout of this plan, run `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/billing/events_transaction_test.exs test/accrue/processor/fake_test.exs test/accrue/processor/braintree_test.exs test/accrue/processor/stripe_test.exs`.
</verification>

<success_criteria>
PROC-21 is satisfied for the support-truth slice when `customer.update` is first-party in runtime and public mirrors and the promoted contract is enforced by deterministic core proof.
</success_criteria>

<output>
After completion, create `.planning/phases/112-customer-update-contract-closure/112-02-SUMMARY.md`
</output>

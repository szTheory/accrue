---
phase: 112-customer-update-contract-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
  - accrue/priv/accrue/templates/install/billing.ex.eex
autonomous: true
requirements:
  - PROC-21
must_haves:
  truths:
    - "A host app can call a thin billing helper that resolves the billable/customer boundary and delegates to `Accrue.Billing.update_customer/2`."
    - "The example-host proof stays provider-neutral and does not become a second contract implementation."
    - "The installer template keeps the generated host facade generic and free of Stripe/Braintree jargon for the shared customer-update helper."
  artifacts:
    - path: examples/accrue_host/lib/accrue_host/billing.ex
      provides: host-owned shared customer-update helper adjacent to existing helpers
    - path: examples/accrue_host/test/accrue_host/billing_facade_test.exs
      provides: thin host-facing proof and source/template drift gate
    - path: accrue/priv/accrue/templates/install/billing.ex.eex
      provides: generated host facade alignment for the promoted row
  key_links:
    - from: examples/accrue_host/lib/accrue_host/billing.ex
      to: accrue/lib/accrue/billing.ex
      via: "The host helper must delegate to the promoted billing facade rather than reimplementing customer mutation logic."
      pattern: "update_customer"
    - from: accrue/priv/accrue/templates/install/billing.ex.eex
      to: examples/accrue_host/lib/accrue_host/billing.ex
      via: "The generated facade should teach the same provider-neutral usage pattern as the example host."
      pattern: "customer_for|update_customer"
---

<objective>
Finish Phase 112 with one thin host-facing helper and proof lane so the promoted customer-update row is obvious and usable from an installed app without leaking provider jargon.

Purpose: close the host-ergonomics part of `PROC-21` after the runtime contract and deterministic core proof are already fixed.
Output: one provider-neutral example-host helper, matching installer template alignment, and focused host proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md
@.planning/phases/112-customer-update-contract-closure/112-RESEARCH.md
@.planning/phases/112-customer-update-contract-closure/112-PATTERNS.md
@.planning/phases/112-customer-update-contract-closure/112-01-PLAN.md
@.planning/phases/112-customer-update-contract-closure/112-02-PLAN.md
@examples/accrue_host/lib/accrue_host/billing.ex
@examples/accrue_host/test/accrue_host/billing_facade_test.exs
@accrue/priv/accrue/templates/install/billing.ex.eex

<interfaces>
From `examples/accrue_host/lib/accrue_host/billing.ex`:
```elixir
def customer_for(billable)
def billing_state_for(billable)
def update_customer_tax_location(billable, attrs) when is_map(attrs)
```

From `accrue/priv/accrue/templates/install/billing.ex.eex`:
```elixir
def subscribe(billable, price_id, opts \\ [])
def swap_plan(subscription, price_id, opts)
def cancel(subscription, opts \\ [])
def customer_for(billable)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 112-03-01: Add a thin provider-neutral host helper for the promoted shared customer-update row</name>
  <files>examples/accrue_host/lib/accrue_host/billing.ex, accrue/priv/accrue/templates/install/billing.ex.eex</files>
  <read_first>.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md, .planning/phases/112-customer-update-contract-closure/112-RESEARCH.md, .planning/phases/112-customer-update-contract-closure/112-PATTERNS.md, examples/accrue_host/lib/accrue_host/billing.ex, accrue/priv/accrue/templates/install/billing.ex.eex</read_first>
  <acceptance_criteria>
    - The example host exposes one helper adjacent to `customer_for/1`, `billing_state_for/1`, and `update_customer_tax_location/2`.
    - The helper resolves the billable/customer boundary and delegates to `Accrue.Billing.update_customer/2`.
    - The installer template teaches the same generic helper shape.
    - No Stripe- or Braintree-specific jargon appears in the shared helper or template.
  </acceptance_criteria>
  <action>Create one thin host-facing helper per D-17, D-19, D-21, D-22, and D-23 in `examples/accrue_host/lib/accrue_host/billing.ex`, reusing the established `customer_for/1` resolution pattern so UI code does not reach into `%Accrue.Billing.Customer{}` directly. Mirror the same provider-neutral helper in `accrue/priv/accrue/templates/install/billing.ex.eex`. Keep this helper intentionally thin: host policy/wrapping is allowed, but processor normalization and attr validation remain in `Accrue.Billing.update_customer/2` per D-06 and D-32. Do not introduce provider jargon, raw processor params, or a second contract implementation.</action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <done>The example host and installer template both expose one thin provider-neutral shared customer-update helper.</done>
</task>

<task type="auto">
  <name>Task 112-03-02: Extend example-host proof and source gates for the shared helper</name>
  <files>examples/accrue_host/test/accrue_host/billing_facade_test.exs</files>
  <read_first>.planning/phases/112-customer-update-contract-closure/112-CONTEXT.md, .planning/phases/112-customer-update-contract-closure/112-RESEARCH.md, examples/accrue_host/test/accrue_host/billing_facade_test.exs, examples/accrue_host/lib/accrue_host/billing.ex, accrue/priv/accrue/templates/install/billing.ex.eex</read_first>
  <acceptance_criteria>
    - The host test proves the new helper delegates through the installed-app facade path.
    - Export/source assertions pin the helper as thin and explicit.
    - Template assertions ensure the generated host facade remains generic and provider-neutral.
    - The host proof does not duplicate the library’s attr-validation or processor-contract logic.
  </acceptance_criteria>
  <action>Extend `examples/accrue_host/test/accrue_host/billing_facade_test.exs` per D-17..D-20 so the host lane stays thin and adoption-facing: assert the new helper is exported, prove it delegates through the host-owned billing facade with Fake-backed deterministic behavior, and extend the source/template drift assertions to keep the generated helper explicit and free of Stripe/Braintree jargon. Keep the host lane provider-neutral and bounded; the core semantic SSOT remains in `accrue/test/...`, not here.</action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <done>The example-host proof now covers the promoted helper ergonomically without becoming a second contract implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host UI/app code -> host billing facade | The host helper must keep customer resolution and delegation explicit so UI code does not mutate Accrue rows directly. |
| generated template -> public contract | If the template leaks provider-specific jargon, new installs will learn the wrong abstraction boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-05 | E | example-host helper boundary | mitigate | Route all shared customer-update behavior through the host billing facade and `Accrue.Billing.update_customer/2`, not direct schema mutation. |
| T-112-06 | T | installer template and host proof | mitigate | Add source/template assertions that keep the helper generic and provider-neutral. |
</threat_model>

<verification>
Run the per-task automated command.
For final closeout of this plan, run `cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs`.
</verification>

<success_criteria>
PROC-21 is satisfied for the host-facing slice when installed-app code can call one thin provider-neutral helper for customer updates and the example/template proof stays aligned to the promoted contract.
</success_criteria>

<output>
After completion, create `.planning/phases/112-customer-update-contract-closure/112-03-SUMMARY.md`
</output>

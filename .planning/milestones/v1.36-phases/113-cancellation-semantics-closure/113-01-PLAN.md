---
phase: 113-cancellation-semantics-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/lib/accrue/processor/braintree.ex
  - accrue/lib/accrue/processor/capabilities.ex
  - .planning/processor-support-matrix.md
  - accrue/test/accrue/processor/capabilities_test.exs
  - accrue/test/accrue/billing/subscription_cancel_test.exs
  - accrue/test/accrue/processor/braintree_test.exs
autonomous: true
requirements:
  - PROC-22
  - PROC-23
must_haves:
  truths:
    - "`Accrue.Billing.cancel/2` and capability vocabulary `cancel_immediately` describe the same shipped immediate-cancel contract across Fake, Stripe, and Braintree."
    - "`cancel_at_period_end/2` remains explicitly supported on Stripe and Fake and explicitly unsupported on Braintree."
    - "Unsupported Braintree scheduled-end and reversal branches must fail with one concrete next-step hint instead of a generic unsupported message."
    - "Runtime support labels and `.planning/processor-support-matrix.md` must describe the same cancellation truth instead of staging rows that are already shipped."
  artifacts:
    - path: accrue/lib/accrue/billing/subscription_actions.ex
      provides: public-facade runtime wording for unsupported scheduled-end and reversal branches with actionable next-step guidance
    - path: accrue/lib/accrue/processor/braintree.ex
      provides: adapter-level unsupported cancel semantics that include one concrete next step
    - path: accrue/lib/accrue/processor/capabilities.ex
      provides: promoted first-party runtime labels for immediate cancellation and preserved split row for scheduled-end cancellation
    - path: .planning/processor-support-matrix.md
      provides: public support SSOT aligned to the same immediate-vs-scheduled contract
    - path: accrue/test/accrue/processor/capabilities_test.exs
      provides: runtime label truth gate for the promoted immediate-cancel rows and the preserved split row
    - path: accrue/test/accrue/billing/subscription_cancel_test.exs
      provides: public-facade proof that immediate and scheduled-end cancellation remain distinct
    - path: accrue/test/accrue/processor/braintree_test.exs
      provides: adapter truth for Braintree immediate support and scheduled-end rejection
  key_links:
    - from: accrue/lib/accrue/processor/capabilities.ex
      to: .planning/processor-support-matrix.md
      via: "Runtime and planning support truth must move together for `subscription.cancel`, `subscription.cancel_immediately`, and `subscription.cancel_at_period_end`."
      pattern: "subscription\\.cancel|subscription\\.cancel_immediately|subscription\\.cancel_at_period_end"
    - from: accrue/test/accrue/processor/braintree_test.exs
      to: accrue/lib/accrue/processor/braintree.ex
      via: "Braintree proof must explicitly pin immediate-cancel support and scheduled-end rejection so the promoted labels remain truthful."
      pattern: "cancel_at_period_end|cancel_immediately|processor_operation_unsupported"
    - from: accrue/lib/accrue/billing/subscription_actions.ex
      to: accrue/lib/accrue/processor/braintree.ex
      via: "Facade-level and adapter-level unsupported Braintree lifecycle messages should both carry one concrete next step rather than vague parity wording."
      pattern: "next step|create a new subscription|cancel now|host-owned"
---

<objective>
Close the runtime/public cancellation support contract by promoting the shipped immediate-cancel rows to first-party while preserving explicit provider-split truth for scheduled-end cancellation.

Purpose: satisfy the contract-truth half of Phase 113 before docs and UX mirrors build on top of it.
Output: aligned runtime labels, support-matrix wording, and deterministic core tests that pin immediate cancellation as first-party across all three providers without overstating Braintree scheduled-end support.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/processor-support-matrix.md
@.planning/phases/113-cancellation-semantics-closure/113-CONTEXT.md
@.planning/phases/113-cancellation-semantics-closure/113-RESEARCH.md
@.planning/phases/113-cancellation-semantics-closure/113-PATTERNS.md
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/processor.ex
@accrue/lib/accrue/processor/capabilities.ex
@accrue/lib/accrue/processor/braintree.ex
@accrue/test/accrue/billing/subscription_cancel_test.exs
@accrue/test/accrue/processor/capabilities_test.exs
@accrue/test/accrue/processor/braintree_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 113-01-01: Promote the immediate-cancel support rows and keep scheduled-end truth explicitly split</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, .planning/processor-support-matrix.md, accrue/test/accrue/processor/capabilities_test.exs</files>
  <read_first>.planning/phases/113-cancellation-semantics-closure/113-CONTEXT.md, .planning/phases/113-cancellation-semantics-closure/113-RESEARCH.md, .planning/phases/113-cancellation-semantics-closure/113-PATTERNS.md, accrue/lib/accrue/processor/capabilities.ex, .planning/processor-support-matrix.md, accrue/test/accrue/processor/capabilities_test.exs</read_first>
  <acceptance_criteria>
    - `Capabilities.support_label([:subscription, :cancel])` is `all first-party`.
    - `Capabilities.support_label([:subscription, :cancel_immediately])` is `all first-party`.
    - `Capabilities.support_label([:subscription, :cancel_at_period_end])` does not claim all-provider first-party support for Braintree.
    - `.planning/processor-support-matrix.md` describes immediate cancellation as first-party across Fake, Stripe, and Braintree.
    - `.planning/processor-support-matrix.md` explicitly keeps Braintree scheduled-end cancellation unsupported or out of the shared first-party row.
  </acceptance_criteria>
  <action>Update the runtime support-label SSOT and the planning support SSOT together. In `accrue/lib/accrue/processor/capabilities.ex`, promote only the immediate-cancel rows (`subscription.cancel` and `subscription.cancel_immediately`) from staged to first-party. Keep the scheduled-end row explicitly split so the labels still tell the truth about Braintree. Mirror that exact contract in `.planning/processor-support-matrix.md`: immediate cancel is the shipped first-party shared path; scheduled-end cancellation remains supported on Stripe/Fake and unsupported on Braintree. Extend `capabilities_test.exs` so the promoted labels and preserved split row are both merge-blocking.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs && rg -n "subscription.cancel|subscription.cancel_immediately|subscription.cancel_at_period_end|all first-party|staged first-party target|unsupported" lib/accrue/processor/capabilities.ex ../.planning/processor-support-matrix.md</automated>
  </verify>
  <done>The runtime label map and the processor-support matrix describe one provider-honest cancellation contract.</done>
</task>

<task type="auto">
  <name>Task 113-01-02: Lock the promoted contract behind facade and adapter proof</name>
  <files>accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/processor/braintree.ex, accrue/test/accrue/billing/subscription_cancel_test.exs, accrue/test/accrue/processor/braintree_test.exs</files>
  <read_first>.planning/phases/113-cancellation-semantics-closure/113-CONTEXT.md, .planning/phases/113-cancellation-semantics-closure/113-RESEARCH.md, accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/processor/braintree.ex, accrue/test/accrue/billing/subscription_cancel_test.exs, accrue/test/accrue/processor/braintree_test.exs</read_first>
  <acceptance_criteria>
    - `subscription_cancel_test.exs` still proves `Billing.cancel/2` is the immediate path and `Billing.cancel_at_period_end/2` remains distinct.
    - The core billing proof explicitly guards against silent degradation of scheduled-end requests into immediate cancellation.
    - Unsupported Braintree scheduled-end or reversal messages include one concrete next-step hint, not only a generic unsupported notice.
    - `braintree_test.exs` explicitly proves Braintree immediate cancellation remains supported through both the plain and param-bearing adapter paths.
    - `braintree_test.exs` explicitly proves Braintree scheduled-end payloads fail with typed unsupported semantics plus one actionable next step.
  </acceptance_criteria>
  <action>Strengthen the deterministic proof bundle around the promoted contract and the locked unsupported-path guidance. Update the runtime error seams in `accrue/lib/accrue/processor/braintree.ex` and, where needed, `accrue/lib/accrue/billing/subscription_actions.ex` so unsupported scheduled-end and reversal branches include one concrete next-step hint such as using immediate cancel for hard-stop behavior or implementing softer non-renewal policy in a host-owned seam. In `subscription_cancel_test.exs`, add or tighten cases that keep immediate and scheduled-end cancellation semantically distinct at the public facade and reject any accidental fallback from scheduled-end into hard-stop behavior. In `braintree_test.exs`, make the Braintree truth explicit: immediate cancellation is supported and merge-blocking, while `cancel_at_period_end`-style payloads remain typed unsupported paths with actionable guidance. Keep the proof bounded to the existing facade and adapter semantics; do not widen into broader lifecycle parity work.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_cancel_test.exs test/accrue/processor/braintree_test.exs && rg -n "host-owned|create a new subscription|cancel now|cancel at period end" lib/accrue/billing/subscription_actions.ex lib/accrue/processor/braintree.ex</automated>
  </verify>
  <done>The promoted immediate-cancel contract and the preserved Braintree scheduled-end boundary are both enforced by deterministic tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime support labels -> public planning mirrors | If labels and the support matrix diverge, adopters and maintainers see contradictory cancellation promises for the same facade. |
| public facade -> provider adapter truth | If immediate-vs-scheduled semantics drift away from adapter truth, Braintree support will be overstated or silently degraded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | T | `accrue/lib/accrue/processor/capabilities.ex`, `.planning/processor-support-matrix.md` | mitigate | Promote only the shipped immediate-cancel rows and keep scheduled-end truth explicitly split. |
| T-113-02 | R | cancellation proof bundle | mitigate | Extend facade and Braintree adapter tests so silent fallback or parity overclaim becomes merge-blocking. |
</threat_model>

<verification>
Run the per-task commands.
For a final closeout pass, run `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/billing/subscription_cancel_test.exs test/accrue/processor/braintree_test.exs`.
</verification>

<success_criteria>
PROC-22 and PROC-23 are satisfied for the runtime/support-truth slice when immediate cancellation is first-party in runtime and planning mirrors across Fake, Stripe, and Braintree, while scheduled-end cancellation stays explicitly unsupported for Braintree and pinned by deterministic proof.
</success_criteria>

<output>
After completion, create `.planning/phases/113-cancellation-semantics-closure/113-01-SUMMARY.md`
</output>

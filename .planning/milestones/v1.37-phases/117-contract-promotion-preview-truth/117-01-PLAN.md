---
phase: 117-contract-promotion-preview-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing.ex
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/lib/accrue/processor/capabilities.ex
  - accrue/test/accrue/billing/swap_plan_test.exs
  - accrue/test/accrue/billing/upcoming_invoice_test.exs
  - accrue/test/accrue/processor/capabilities_test.exs
autonomous: true
requirements:
  - SCM-01
  - SCM-02
must_haves:
  truths:
    - "Host code can discover `swap_plan/3` and `preview_upcoming_invoice/2` as the official active-subscription-change facade without reading processor internals."
    - "Capability rows and runtime docs name Stripe as native, Braintree as bounded first-party for plan swap only, and Fake as testing/local-only per D-03 and D-04."
    - "The Braintree path stays provider-honest: no preview parity, no fake option semantics, and explicit unsupported guidance per D-07 through D-18."
  artifacts:
    - path: accrue/lib/accrue/billing.ex
      provides: thin public API docs for the promoted subscription-change contract
    - path: accrue/lib/accrue/processor/capabilities.ex
      provides: fine-grained support rows for `swap_plan` and `preview_upcoming_invoice`
    - path: accrue/lib/accrue/billing/subscription_actions.ex
      provides: bounded Braintree contract enforcement and option-truth comments/errors
    - path: accrue/test/accrue/processor/capabilities_test.exs
      provides: contract-proof coverage for the new capability labels
  key_links:
    - from: accrue/lib/accrue/billing.ex
      to: accrue/lib/accrue/billing/subscription_actions.ex
      via: "Public `@doc` text and facade delegates must point to the same swap/preview semantics."
      pattern: "swap_plan|preview_upcoming_invoice"
    - from: accrue/lib/accrue/processor/capabilities.ex
      to: .planning/processor-support-matrix.md
      via: "Capability labels in code must become the source language mirrored by the canonical support matrix."
      pattern: "swap_plan|preview_upcoming_invoice|bounded first-party|testing/local-only|unsupported"
---

<objective>
Promote the runtime-facing subscription-change contract so the public facade, capability map, and focused proof files all state the same provider-honest truth before docs mirrors branch from it.

Purpose: satisfy the capability/code-truth slice of SCM-01 and SCM-02 per D-01 through D-19 without widening into Phase 118 UI depth.
Output: public API docs, fine-grained capability rows, bounded Braintree contract wording, and focused ExUnit proof for swap + preview support semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/117-contract-promotion-preview-truth/117-CONTEXT.md
@.planning/phases/117-contract-promotion-preview-truth/117-RESEARCH.md
@.planning/phases/117-contract-promotion-preview-truth/117-PATTERNS.md
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/processor/capabilities.ex
@accrue/lib/accrue/plan_resolver.ex
@accrue/lib/accrue/processor/braintree.ex
@accrue/test/accrue/billing/swap_plan_test.exs
@accrue/test/accrue/billing/upcoming_invoice_test.exs
@accrue/test/accrue/processor/capabilities_test.exs

<interfaces>
From `accrue/lib/accrue/billing.ex`:
```elixir
def swap_plan(sub, new_price_id, opts)
def preview_upcoming_invoice(sub_or_customer, opts \\ [])
```

From `accrue/lib/accrue/plan_resolver.ex`:
```elixir
@type resolved_plan :: %{
  processor_plan_id: String.t(),
  unit_amount_minor: integer(),
  currency: String.t(),
  billing_cycle: atom(),
  processor: String.t()
}
```

From `accrue/lib/accrue/processor/capabilities.ex`:
```elixir
@support_labels %{subscription: %{...}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 117-01-01: Promote official swap/preview contract labels in the facade and capability map</name>
  <files>accrue/lib/accrue/billing.ex, accrue/lib/accrue/processor/capabilities.ex, accrue/test/accrue/processor/capabilities_test.exs</files>
  <read_first>.planning/phases/117-contract-promotion-preview-truth/117-CONTEXT.md, .planning/phases/117-contract-promotion-preview-truth/117-RESEARCH.md, .planning/phases/117-contract-promotion-preview-truth/117-PATTERNS.md, accrue/lib/accrue/billing.ex, accrue/lib/accrue/processor/capabilities.ex, accrue/test/accrue/processor/capabilities_test.exs</read_first>
  <behavior>
    - Test 1: capability rows for `swap_plan` and `preview_upcoming_invoice` expose the provider-honest labels required by D-02 through D-06.
    - Test 2: `Accrue.Billing` docs describe both functions as the official active-subscription-change APIs per D-01, D-14, and D-23.
  </behavior>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/capabilities.ex` contains exact strings for `swap_plan` and `preview_upcoming_invoice`.
    - `accrue/lib/accrue/processor/capabilities.ex` contains the exact strings `native`, `bounded first-party`, `testing/local-only`, and `unsupported`.
    - `accrue/lib/accrue/billing.ex` `@doc` blocks mention `official active-subscription-change` and point readers to `lifecycle_semantics.md` and `.planning/processor-support-matrix.md` per D-21 through D-23.
    - `cd accrue && mix test test/accrue/processor/capabilities_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <action>Create fine-grained capability rows in `accrue/lib/accrue/processor/capabilities.ex` for `swap_plan` and `preview_upcoming_invoice` per D-02 through D-06 instead of hiding them under coarse `subscription.update`. Update `Accrue.Billing` function docs so `swap_plan/3` and `preview_upcoming_invoice/2` are explicitly presented as the official active-subscription-change APIs per D-01, with canonical-when-supported preview wording per D-14 through D-19 and thin references back to `lifecycle_semantics.md` plus `.planning/processor-support-matrix.md` per D-23. Add or update focused capability assertions in `accrue/test/accrue/processor/capabilities_test.exs`; do not add provider-specific public facade verbs or a third canonical contract doc per D-25 and D-32.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs --warnings-as-errors && rg -n "swap_plan|preview_upcoming_invoice|native|bounded first-party|testing/local-only|unsupported|official active-subscription-change|lifecycle_semantics|processor-support-matrix" lib/accrue/billing.ex lib/accrue/processor/capabilities.ex</automated>
  </verify>
  <done>The public facade and capability map expose the same official swap/preview contract with provider-honest labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 117-01-02: Lock the bounded Braintree option story and focused proof lanes</name>
  <files>accrue/lib/accrue/billing/subscription_actions.ex, accrue/test/accrue/billing/swap_plan_test.exs, accrue/test/accrue/billing/upcoming_invoice_test.exs</files>
  <read_first>.planning/phases/117-contract-promotion-preview-truth/117-CONTEXT.md, .planning/phases/117-contract-promotion-preview-truth/117-RESEARCH.md, accrue/lib/accrue/billing/subscription_actions.ex, accrue/lib/accrue/plan_resolver.ex, accrue/lib/accrue/processor/braintree.ex, accrue/test/accrue/billing/swap_plan_test.exs, accrue/test/accrue/billing/upcoming_invoice_test.exs</read_first>
  <behavior>
    - Test 1: Braintree plan swap rejects unsupported quantity, multi-item, and non-honored option stories with typed unsupported guidance per D-08, D-12, and D-13.
    - Test 2: Braintree preview stays explicitly unsupported while Stripe/Fake preview remains the canonical path where supported per D-09 and D-15 through D-18.
  </behavior>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription_actions.ex` contains explicit Braintree contract wording for `:plan_resolver`, matching currency, matching billing cycle, and supported proration values per D-08 and D-11.
    - `accrue/test/accrue/billing/swap_plan_test.exs` asserts typed unsupported guidance for incompatible Braintree options or topologies.
    - `accrue/test/accrue/billing/upcoming_invoice_test.exs` asserts that Braintree preview remains unsupported and does not claim pseudo-preview parity.
    - `cd accrue && mix test test/accrue/billing/swap_plan_test.exs test/accrue/billing/upcoming_invoice_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <action>Use `accrue/lib/accrue/billing/subscription_actions.ex` to make the bounded Braintree contract unmistakable per D-07 through D-13: preserve the current shared facade, keep preview unsupported on Braintree, and ensure any error text or inline docs call out `:plan_resolver` as a Braintree swap enabler with the required metadata contract from D-11. Tighten focused tests in `swap_plan_test.exs` and `upcoming_invoice_test.exs` so unsupported quantity, multi-item, and preview cases fail clearly at the facade boundary. Do not add preview as a runtime precondition for `swap_plan/3` per D-17, and do not broaden runtime scope into quantity/item management promotion reserved for Phase 118.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/swap_plan_test.exs test/accrue/billing/upcoming_invoice_test.exs --warnings-as-errors && rg -n "plan_resolver|currency|billing cycle|create_prorations|unsupported|preview" lib/accrue/billing/subscription_actions.ex test/accrue/billing/swap_plan_test.exs test/accrue/billing/upcoming_invoice_test.exs</automated>
  </verify>
  <done>The bounded Braintree path is explicit in code and proof, and preview remains canonical only where supported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public facade -> host code | If `Accrue.Billing` docs overstate parity, adopters will call unsupported preview or option combinations on Braintree. |
| capability map -> docs mirrors | If code labels stay coarse while docs become specific, future mirrors will drift back to fake parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | T | `accrue/lib/accrue/processor/capabilities.ex` | mitigate | Add dedicated `swap_plan` and `preview_upcoming_invoice` rows with provider-honest labels per D-02 through D-06. |
| T-117-02 | I | `accrue/lib/accrue/billing.ex` | mitigate | Keep API docs thin, explicit, and linked to canonical SSOTs so hosts are not misled by stale or inferred semantics. |
| T-117-03 | T | `accrue/lib/accrue/billing/subscription_actions.ex` | mitigate | Preserve explicit unsupported failures for Braintree quantity/item/preview gaps and document the bounded `:plan_resolver` contract per D-08 through D-13. |
</threat_model>

<verification>
Run the per-task `mix test` commands.
Run `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/billing/swap_plan_test.exs test/accrue/billing/upcoming_invoice_test.exs --warnings-as-errors` after both tasks complete.
</verification>

<success_criteria>
SCM-01 and SCM-02 are satisfied for the capability/code-truth slice when the public billing facade, capability map, and focused ExUnit coverage all expose one provider-honest swap + preview contract with explicit Braintree bounds.
</success_criteria>

<output>
After completion, create `.planning/phases/117-contract-promotion-preview-truth/117-01-SUMMARY.md`
</output>

---
phase: 136-adopter-confidence-recovery-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [examples/accrue_host/config/config.exs, examples/accrue_host/test/accrue_host/recovery_wiring_test.exs]
autonomous: true
requirements: [PROOF-06]

must_haves:
  truths:
    - "Accrue.Jobs.DetectExpiringCards is present in the examples/accrue_host Oban crontab"
    - "New Oban queues accrue_meters and accrue_scheduled are declared in examples/accrue_host"
    - "Oban.Plugins.Cron configuration is valid and bootable in the host"
  artifacts:
    - path: "examples/accrue_host/config/config.exs"
      provides: "Oban crontab and queue wiring"
    - path: "examples/accrue_host/test/accrue_host/recovery_wiring_test.exs"
      provides: "Host recovery wiring verification"
  key_links:
    - from: "examples/accrue_host/config/config.exs"
      to: "Accrue.Jobs.DetectExpiringCards"
      via: "Oban.Plugins.Cron crontab"
---

<objective>
Wire recovery crons in the example host and add a smoke test to prove failure-handling readiness.

Purpose: Demonstrate to adopters that Accrue handles recovery (like expiring cards and metered usage reconciliation) via background jobs.
Output: Updated host configuration and a new verification test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/accrue_host/config/config.exs

<interfaces>
From accrue/lib/accrue/jobs/detect_expiring_cards.ex:
```elixir
defmodule Accrue.Jobs.DetectExpiringCards do
  use Oban.Worker, queue: :accrue_scheduled, max_attempts: 3
  def scan, do: :ok
end
```

From accrue/lib/accrue/jobs/meter_events_reconciler.ex:
```elixir
defmodule Accrue.Jobs.MeterEventsReconciler do
  use Oban.Worker, queue: :accrue_meters, max_attempts: 3
  def reconcile, do: {:ok, integer()}
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Oban configuration in examples/accrue_host</name>
  <files>examples/accrue_host/config/config.exs</files>
  <action>
    - Add `accrue_meters: 5` and `accrue_scheduled: 5` to the `queues` list in the Oban configuration.
    - Add `{"@daily", Accrue.Jobs.DetectExpiringCards}` to the `crontab` list.
    - Add `{"* * * * *", Accrue.Jobs.MeterEventsReconciler}` to the `crontab` list to support metered usage recovery (Phase 135 follow-up).
    - Add `{"*/5 * * * *", Accrue.Jobs.MeteredRenewalReconciler}` to the `crontab` list (reconciles Braintree renewal windows).
  </action>
  <verify>
    <automated>grep -E "accrue_meters|accrue_scheduled|DetectExpiringCards|MeterEventsReconciler" examples/accrue_host/config/config.exs</automated>
  </verify>
  <done>Oban config includes the required queues and cron entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add recovery wiring smoke test</name>
  <files>examples/accrue_host/test/accrue_host/recovery_wiring_test.exs</files>
  <action>
    Create a new test file that proves the host's recovery wiring is active:
    - Verify that `Accrue.Jobs.DetectExpiringCards` is configured in the Oban crontab.
    - Verify that `Accrue.Jobs.MeterEventsReconciler` is configured in the Oban crontab.
    - Add a smoke test calling `Accrue.Jobs.DetectExpiringCards.scan()` and `Accrue.Jobs.MeterEventsReconciler.reconcile()` to ensure they run against the host Repo without error.
    - Use `AccrueHost.AccrueCase`.
    - Reference pattern in `examples/accrue_host/test/accrue_host/dunning_wiring_test.exs` (Stage D).
  </action>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test test/accrue_host/recovery_wiring_test.exs</automated>
  </verify>
  <done>Host recovery wiring is verified via ExUnit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Oban -> Jobs | Untrusted input in job args (if any) could lead to exploitation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Tampering | Oban Crontab | mitigate | Crontab is configured in code, preventing runtime tampering via DB (standard Oban security) |
</threat_model>

<verification>
Check that `mix test` passes in the host.
</verification>

<success_criteria>
`Accrue.Jobs.DetectExpiringCards` is wired and verified in `examples/accrue_host`.
</success_criteria>

<output>
After completion, create `.planning/phases/136-adopter-confidence-recovery-wiring/136-01-SUMMARY.md`
</output>

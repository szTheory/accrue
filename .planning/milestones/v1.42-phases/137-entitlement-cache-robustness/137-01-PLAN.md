---
phase: 137
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["accrue/lib/accrue/webhook/default_handler.ex", "accrue/test/support/stripe_fixtures.ex", "accrue/lib/accrue/telemetry/metrics.ex"]
autonomous: true
requirements: ["FIX-02"]
must_haves:
  truths:
    - "Entitlement summary records include the correct processor name"
    - "Livemode field preserves null state from the source"
    - "Stripe fixtures moduledoc is updated"
    - "summary_synced counter is present in default metrics"
  artifacts:
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "Fidelity fixes for summary cache"
    - path: "accrue/lib/accrue/telemetry/metrics.ex"
      provides: "Updated telemetry metrics"
---

<objective>
Implement entitlement sync fidelity fixes (IN-01..IN-04).
Purpose: Ensure the advisory cache accurately reflects the source data and provides better observability.
Output: Updated webhook handler, fixtures, and metrics.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-05-24-ent10-advisory-cache-followups.md
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/telemetry/metrics.ex
@accrue/test/support/stripe_fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IN-01, IN-02, and IN-03 (Fidelity and Docs)</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/test/support/stripe_fixtures.ex</files>
  <action>
    - In `accrue/lib/accrue/webhook/default_handler.ex`, update `write_entitlement_summary/8` attrs:
      - Set `processor: processor_name()` (IN-01).
      - Change `livemode: get(obj, :livemode) == true` to `livemode: get(obj, :livemode)` (IN-02).
    - In `accrue/test/support/stripe_fixtures.ex`, update the moduledoc to generalize "Phase 3 tests" to include recent entitlement summary fixtures (IN-03).
  </action>
  <verify>
    <automated>mix test accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs</automated>
  </verify>
  <done>Processor and livemode accurately captured; fixtures moduledoc updated.</done>
</task>

<task type="auto">
  <name>Task 2: Fix IN-04 (Metrics)</name>
  <files>accrue/lib/accrue/telemetry/metrics.ex</files>
  <action>
    - Add `counter("accrue.entitlements.summary_synced.count", tags: [:result])` to `Accrue.Telemetry.Metrics.defaults/0`.
    - Place it near the other entitlement-related metrics (e.g., `entitlement_summary_truncated`).
  </action>
  <verify>
    <automated>grep "summary_synced" accrue/lib/accrue/telemetry/metrics.ex</automated>
  </verify>
  <done>The summary_synced counter is part of the default metrics recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook -> DB | External Stripe data is projected into the local advisory cache. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Information Disclosure | `livemode` field | accept | Reverting to raw boolean-or-nil exposes the "unknown" state which is intended for fidelity. No PII involved. |
</threat_model>

<verification>
mix test accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
</verification>

<success_criteria>
1. Entitlement summary rows store the processor name.
2. `livemode` captures `nil` correctly.
3. Metrics include the new counter.
</success_criteria>

<output>
After completion, create `.planning/phases/137-entitlement-cache-robustness/137-01-SUMMARY.md`
</output>

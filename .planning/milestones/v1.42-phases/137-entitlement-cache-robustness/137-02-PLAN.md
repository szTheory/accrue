---
phase: 137
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified: ["accrue/lib/accrue/webhook/default_handler.ex", "accrue/test/accrue/webhook/wr05_concurrency_test.exs"]
autonomous: true
requirements: ["FIX-01"]
must_haves:
  truths:
    - "Concurrent entitlement summary updates for the same customer do not raise StaleEntryError"
    - "The latest event always wins, or stale events are correctly skipped"
  artifacts:
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "Race-safe upsert for entitlement summary"
    - path: "accrue/test/accrue/webhook/wr05_concurrency_test.exs"
      provides: "Regression test for WR-05 race condition"
---

<objective>
Resolve WR-05 (StaleEntryError) by implementing a race-safe upsert and verify with a concurrency test.
Purpose: Prevent noisy crashes during concurrent webhook delivery for the same customer.
Output: Robust upsert logic and a reproduction test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-05-24-ent10-advisory-cache-followups.md
@accrue/lib/accrue/webhook/default_handler.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement race-safe upsert for EntitlementSummary</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex</files>
  <action>
    - Update `upsert_entitlement_summary/2` in `accrue/lib/accrue/webhook/default_handler.ex` to use `Repo.insert` with `on_conflict` to handle both inserts and updates atomically.
    - Use `on_conflict: [set: [...]]` specifying all relevant fields from `attrs`.
    - Include `on_conflict_where: fragment("accrue_entitlement_summaries.last_stripe_event_ts < excluded.last_stripe_event_ts")` to preserve the watermark (Plan 02/FIX-01).
    - Ensure `returning: true` is used to get the saved struct back for downstream functions.
    - Consolidate the two `upsert_entitlement_summary` clauses (nil and %EntitlementSummary{}) into a single clause if possible, as the DB-level upsert handles both.
  </action>
  <verify>
    <automated>mix test accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs</automated>
  </verify>
  <done>Upsert logic is atomic and respects the watermark.</done>
</task>

<task type="auto">
  <name>Task 2: Add WR-05 concurrency regression test</name>
  <files>accrue/test/accrue/webhook/wr05_concurrency_test.exs</files>
  <action>
    - Create `accrue/test/accrue/webhook/wr05_concurrency_test.exs`.
    - Use `Task.async` to simulate two concurrent calls to `DefaultHandler.handle_event` for the same customer with different event timestamps.
    - Verify that no `Ecto.StaleEntryError` is raised.
    - Verify that the resulting state in the DB matches the latest event.
  </action>
  <verify>
    <automated>mix test accrue/test/accrue/webhook/wr05_concurrency_test.exs</automated>
  </verify>
  <done>Concurrency test passes, proving WR-05 is resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook -> DB | Atomic upsert ensures database consistency under high concurrency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-02 | Denial of Service | Entitlement Cache | mitigate | Moving from crashing on `StaleEntryError` to silent watermark-skipping at the DB level reduces noise and improves reliability. |
</threat_model>

<verification>
mix test accrue/test/accrue/webhook/wr05_concurrency_test.exs
</verification>

<success_criteria>
1. Concurrent updates for the same customer succeed without raising errors.
2. The watermark check is enforced at the DB level.
</success_criteria>

<output>
After completion, create `.planning/phases/137-entitlement-cache-robustness/137-02-SUMMARY.md`
</output>

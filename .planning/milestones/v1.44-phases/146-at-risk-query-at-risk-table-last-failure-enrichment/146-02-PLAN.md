---
phase: 146
plan: 02
type: execute
wave: 2
depends_on:
  - "146-01"
files_modified:
  - accrue/lib/accrue/analytics/dunning.ex
  - accrue/test/accrue/analytics/at_risk_subscriptions_test.exs
autonomous: true
requirements:
  - DAN-03
  - DAN-04
must_haves:
  truths:
    - "at_risk_subscriptions/1 returns only subscriptions with non-nil dunning_campaign_started_at and no terminal ledger event since campaign start"
    - "A subscription with a dunning.recovered event inserted after campaign start is excluded even when dunning_campaign_started_at is still non-nil (projection-lag race)"
    - "next_step_eta is nil when no Oban job is pending; populated when a DunningStep job is in available/scheduled/retryable state"
    - "failure_reason is nil for pre-v1.44 events (no invoice_id key in data); nil is the honest default"
    - "apply_campaign_window/2 filters by dunning_campaign_started_at, not by inserted_at"
  artifacts:
    - path: "accrue/lib/accrue/analytics/dunning.ex"
      provides: "at_risk_subscriptions/1 public function + apply_campaign_window/2 private helper"
      contains: "def at_risk_subscriptions"
      exports: ["at_risk_subscriptions"]
    - path: "accrue/test/accrue/analytics/at_risk_subscriptions_test.exs"
      provides: "Projection-lag race test, ETA nil fallback, pre-v1.44 honest default, window filter"
      contains: "use Accrue.BillingCase"
  key_links:
    - from: "at_risk_subscriptions/1"
      to: "accrue_events (accrue.events)"
      via: "NOT EXISTS fragment correlated subquery on dunning.recovered / dunning.exhausted"
      pattern: "NOT EXISTS.*accrue_events.*dunning\\.recovered.*dunning\\.exhausted"
    - from: "at_risk_subscriptions/1"
      to: "oban_jobs"
      via: "LEFT JOIN Oban.Job on worker=DunningStep, args->>'subscription_id', args->>'campaign_started_at'"
      pattern: "Oban.Job\\|oban_jobs"
    - from: "at_risk_subscriptions/1"
      to: "accrue_invoices"
      via: "LEFT JOIN bridge: accrue_invoices.processor_id = campaign_started event data->>'invoice_id'"
      pattern: "accrue_invoices\\|processor_id"
---

<objective>
Implement `Accrue.Analytics.Dunning.at_risk_subscriptions/1` — the single-query compound Ecto function that returns active dunning subscriptions enriched with days_in_campaign, current_step, next_step_eta, customer_label, and failure_reason. Uses a NOT EXISTS correlated subquery as the ledger tiebreaker (D-08), LEFT JOINs to oban_jobs for ETA (D-01), and bridges through accrue_invoices to resolve failure_reason from invoice.payment_failed events (D-05, Option B from RESEARCH.md).

Purpose: This is the core analytics function for Phase 146. It resolves the projection-lag race condition by construction (SQL atomic snapshot), provides the honest "—" default for pre-v1.44 campaigns (D-06), and powers the at-risk table rendered in Plan 03.

Output: Modified `accrue/lib/accrue/analytics/dunning.ex` with `at_risk_subscriptions/1` + `apply_campaign_window/2`; new `accrue/test/accrue/analytics/at_risk_subscriptions_test.exs` with 5 test scenarios.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor must use these directly. -->

From accrue/lib/accrue/analytics/dunning.ex (current state — imports/aliases to EXTEND):
```elixir
import Ecto.Query, only: [from: 2, subquery: 1, where: 3]

alias Accrue.Events.Event
alias Accrue.Repo
```
Must add to imports: `join: 5, left_join: 5, select: 3, order_by: 3`
May also add `fragment: 1` — verify current Ecto version exposes it as importable or call as `Ecto.Query.API.fragment/1` (more commonly: `fragment` is always available in query macros without import).
Must add to aliases: `alias Accrue.Billing.{Customer, Invoice, Subscription}` and `alias Oban.Job`.

From accrue/lib/accrue/dunning/engine/oban.ex (Oban.Job args-fragment pattern — lines 77–82):
```elixir
from(j in Oban.Job,
  where: j.worker == "Accrue.Workers.DunningStep",
  where: fragment("? ->> 'subscription_id' = ?", j.args, ^sub.id),
  where: fragment("? ->> 'campaign_started_at' = ?", j.args, ^iso_anchor)
)
```
For the LEFT JOIN in at_risk_subscriptions/1, the ISO8601 string must match the exact format stored by DunningStep.enqueue_step/4 which calls `DateTime.to_iso8601(anchor)`. The subscription's `dunning_campaign_started_at` is the anchor. Since this is a per-row value (not a single Elixir variable), use a SQL cast in the fragment:
`fragment("? ->> 'campaign_started_at' = to_char(?, 'YYYY-MM-DD\"T\"HH24:MI:SS.US\"Z\"')", j.args, s.dunning_campaign_started_at)`
OR pre-compute once if there is a single subscription (not applicable for a list query). Use the to_char approach for the JOIN condition.

From accrue/lib/accrue/billing/invoice.ex (schema fields):
```elixir
@primary_key {:id, :binary_id, autogenerate: true}
field(:processor_id, :string)   # Stripe Invoice ID — bridges to dunning.campaign_started data["invoice_id"]
```
The accrue_invoices table is `accrue_invoices`. JOIN condition: `inv.processor_id == fragment("? ->> 'invoice_id'", cs.data)`.

From dunning.ex apply_window/2 (GOTCHA — do NOT reuse for Subscription queries):
```elixir
defp maybe_since(query, %DateTime{} = since),
  do: where(query, [e], e.inserted_at >= ^since)
```
The `[e]` binding maps to `inserted_at` (subscription creation date) on a Subscription query, NOT to `dunning_campaign_started_at`. Use a DEDICATED `apply_campaign_window/2` helper binding `[s]` on `s.dunning_campaign_started_at`.

Return shape (per RESEARCH.md + CONTEXT.md Claude's Discretion):
```elixir
%{
  subscription_id: binary(),
  customer_id: binary(),
  customer_label: String.t() | nil,
  days_in_campaign: non_neg_integer(),
  current_step: non_neg_integer(),
  next_step_eta: DateTime.t() | nil,
  failure_reason: String.t() | nil
}
```

From accrue/test/accrue/webhook/dunning_campaign_start_test.exs (module setup pattern to replicate):
```elixir
use Accrue.BillingCase, async: false
use Oban.Testing, repo: Accrue.TestRepo
```

From accrue/lib/accrue/analytics/dunning.ex `apply_window/2` (lines 145–159) — for reference only on naming:
```elixir
defp apply_window(query, opts) do
  query
  |> maybe_since(opts[:since])
  |> maybe_until(opts[:until])
end
defp maybe_since(query, %DateTime{} = since), do: where(query, [e], e.inserted_at >= ^since)
defp maybe_since(query, _), do: query
defp maybe_until(query, %DateTime{} = until), do: where(query, [e], e.inserted_at <= ^until)
defp maybe_until(query, _), do: query
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: at_risk_subscriptions/1 + apply_campaign_window/2 in dunning.ex</name>
  <files>accrue/lib/accrue/analytics/dunning.ex</files>
  <read_first>
    - accrue/lib/accrue/analytics/dunning.ex lines 1–165 (FULL FILE — imports, aliases, funnel/1, apply_window/2; must understand full module before adding)
    - accrue/lib/accrue/dunning/engine/oban.ex lines 70–90 (Oban.Job args-fragment pattern)
    - accrue/lib/accrue/billing/invoice.ex lines 40–65 (processor_id field, schema source table name)
    - accrue/lib/accrue/billing/subscription.ex lines 1–40 (source table name for accrue_subscriptions, dunning_campaign_started_at field)
    - accrue/lib/accrue/billing/customer.ex lines 1–30 (email + name fields)
    - .planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-RESEARCH.md §"System Architecture Diagram" and §"Common Pitfalls"
    - .planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-CONTEXT.md D-01 through D-15
  </read_first>
  <behavior>
    - at_risk_subscriptions/1 is a public function with spec returning `[map()]`
    - Returns only subscriptions where dunning_campaign_started_at IS NOT NULL AND the NOT EXISTS correlated subquery returns false (no terminal dunning event since campaign start)
    - Each result map contains keys: subscription_id, customer_id, customer_label, days_in_campaign, current_step, next_step_eta, failure_reason
    - days_in_campaign uses Accrue.Clock.utc_now() pinned value (not SQL NOW()) for Fake-lane correctness
    - next_step_eta is nil when no DunningStep Oban job is in available/scheduled/retryable state for this subscription+campaign_started_at combination
    - failure_reason is nil when campaign_started event has no invoice_id key (pre-v1.44) or when accrue_invoices bridge returns no match
    - apply_campaign_window/2 filters on s.dunning_campaign_started_at, NOT on s.inserted_at (dedicated helper, not reuse of apply_window/2)
  </behavior>
  <action>
    In accrue/lib/accrue/analytics/dunning.ex:

    1. Extend the import at the top: change `import Ecto.Query, only: [from: 2, subquery: 1, where: 3]` to `import Ecto.Query, only: [from: 2, subquery: 1, where: 3, left_join: 5, select: 3, order_by: 3]`. If `fragment` is used in macro context (inside `from/2`), it does not need to be imported — it is available automatically in Ecto query macros. Verify by reading the current `funnel/1` function; if it uses fragment there, replicate the pattern.

    2. Extend the alias block: add `alias Accrue.Billing.{Customer, Invoice, Subscription}` and `alias Oban.Job` as additional aliases. Keep the existing `alias Accrue.Events.Event` and `alias Accrue.Repo`.

    3. Add `at_risk_subscriptions/1` as a public function after `funnel/1` (before the private `apply_window/2` section). The function accepts `opts \\ []` and returns `[map()]`.

    Function logic:
    a. Compute `now = Accrue.Clock.utc_now()` at the top of the function (for days_in_campaign Fake-lane pinning).
    b. Build the main FROM clause: `from(s in Subscription, ...)` with the following WHERE, JOIN, and SELECT clauses.
    c. WHERE 1 — active campaign predicate (D-11 inline): `not is_nil(s.dunning_campaign_started_at)`.
    d. WHERE 2 — NOT EXISTS ledger tiebreaker (D-08, D-10): use a raw fragment correlated subquery. Write it as a `where` clause with `fragment("NOT EXISTS (SELECT 1 FROM accrue_events WHERE type IN ('dunning.recovered','dunning.exhausted') AND subject_id = ? AND inserted_at >= ?)", s.id, s.dunning_campaign_started_at)`. This is NOT an Ecto `subquery/1` — it is a raw fragment with `?` positional placeholders referencing the outer query's bindings `s.id` and `s.dunning_campaign_started_at`.
    e. JOIN — customer: `join: c in Customer, on: c.id == s.customer_id` (INNER JOIN — every subscription has a customer).
    f. LEFT JOIN — campaign_started event: `left_join: cs in Event, on: cs.type == "dunning.campaign_started" and cs.subject_id == s.id and cs.inserted_at >= s.dunning_campaign_started_at` — finds the campaign_started event for THIS campaign (not prior campaigns).
    g. LEFT JOIN — invoice bridge (Option B per RESEARCH.md): `left_join: inv in Invoice, on: inv.processor_id == fragment("? ->> 'invoice_id'", cs.data)` — bridges Stripe Invoice ID to Accrue Invoice UUID.
    h. LEFT JOIN — payment_failed event: `left_join: pf in Event, on: pf.type == "invoice.payment_failed" and pf.subject_id == inv.id and pf.inserted_at >= s.dunning_campaign_started_at` — finds the payment_failed event anchored to this campaign.
    i. LEFT JOIN — oban_jobs (D-01): `left_join: j in Job, on: j.worker == "Accrue.Workers.DunningStep" and fragment("? ->> 'subscription_id' = ?", j.args, s.id) and fragment("? ->> 'campaign_started_at' = to_char(?, 'YYYY-MM-DD\"T\"HH24:MI:SS.US\"Z\"')", j.args, s.dunning_campaign_started_at) and j.state in ["available", "scheduled", "retryable"]`.
    j. SELECT: return a map with these keys — `subscription_id: s.id`, `customer_id: s.customer_id`, `customer_label: coalesce(c.email, c.name)` using `fragment("COALESCE(?, ?)", c.email, c.name)`, `days_in_campaign: fragment("EXTRACT(EPOCH FROM (? - ?))::integer / 86400", ^now, s.dunning_campaign_started_at)`, `current_step: fragment("(SELECT COUNT(*) FROM accrue_events WHERE type = 'dunning.step_sent' AND subject_id = ? AND inserted_at >= ?)", s.id, s.dunning_campaign_started_at)`, `next_step_eta: min(j.scheduled_at)`, `failure_reason: pf.data` (raw jsonb data map — plan 03 renderer will extract what it can; nil when no pf event found).
    k. `group_by: [s.id, s.customer_id, c.email, c.name, s.dunning_campaign_started_at, pf.data]`
    l. `order_by: [desc: s.dunning_campaign_started_at]` — most recently started campaigns first.
    m. Apply window: `|> apply_campaign_window(opts)`.
    n. Execute: `Repo.all(query)`.

    NOTE on `failure_reason`: DAN-04 and RESEARCH.md Open Question 1 confirm that `invoice.payment_failed` event `data` only contains `%{"source" => "webhook", "stripe_event_id" => "evt_xxx"}` — there is NO `failure_message` field available without an extra Stripe Charge API fetch. Per D-06 honest-default posture, `failure_reason` is the raw `pf.data` map or nil. The UI template in Plan 03 will render the `stripe_event_id` value or fall back to "—". This satisfies the success criterion while being honest about what is available in v1.44.

    4. After `funnel/1` and `at_risk_subscriptions/1`, add the `apply_campaign_window/2` private helper ALONGSIDE the existing `apply_window/2` (not replacing it):
    - `defp apply_campaign_window(query, opts)` calls `maybe_since_campaign(query, opts[:since])` then `maybe_until_campaign(query, opts[:until])`
    - `defp maybe_since_campaign(query, %DateTime{} = since)`: `where(query, [s], s.dunning_campaign_started_at >= ^since)`
    - `defp maybe_since_campaign(query, _)`: `query`
    - `defp maybe_until_campaign(query, %DateTime{} = until)`: `where(query, [s], s.dunning_campaign_started_at <= ^until)`
    - `defp maybe_until_campaign(query, _)`: `query`

    The named binding `[s]` used here matches the `from(s in Subscription, ...)` binding in `at_risk_subscriptions/1` — this is correct and intentional. Do NOT use `[e]` (that binding is for `apply_window/2`'s Event queries).

    5. Add `@doc` and `@spec` for `at_risk_subscriptions/1`:
    - `@doc "Returns subscriptions currently in an active dunning campaign, enriched with next-step ETA and last-failure reason. Window opts (`:since`, `:until`) filter by dunning_campaign_started_at. Uses a NOT EXISTS ledger tiebreaker to exclude subscriptions that have recovered even when the schema anchor column hasn't been cleared yet (projection-lag race). Pre-v1.44 campaigns without invoice_id in campaign_started data return nil failure_reason."`
    - `@spec at_risk_subscriptions(keyword()) :: [map()]`
    - `@since "1.4.0"`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix compile --no-optional-deps 2>&1 | grep -i error; echo "compile exit: $?" && mix test test/accrue/analytics/dunning_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --no-optional-deps` emits no errors for dunning.ex
    - `mix test test/accrue/analytics/dunning_test.exs --seed 0` exits 0 (existing tests unbroken)
    - `at_risk_subscriptions` appears in `accrue/lib/accrue/analytics/dunning.ex`
    - `apply_campaign_window` appears in `accrue/lib/accrue/analytics/dunning.ex`
    - The import line includes `left_join: 5` and `select: 3`
    - `Oban.Job` is aliased in the module
    - `apply_campaign_window` uses `[s]` binding on `s.dunning_campaign_started_at` (not `[e]` and not `inserted_at`)
  </acceptance_criteria>
  <done>at_risk_subscriptions/1 and apply_campaign_window/2 compile without errors; existing dunning_test.exs exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: at_risk_subscriptions tests — projection-lag race, ETA nil, pre-v1.44 default, window filter</name>
  <files>accrue/test/accrue/analytics/at_risk_subscriptions_test.exs</files>
  <read_first>
    - accrue/test/accrue/webhook/dunning_campaign_start_test.exs lines 1–80 (module setup pattern — use BillingCase + Oban.Testing; fire_payment_failed helper; stub patterns to understand fixture approach)
    - accrue/test/accrue/analytics/dunning_test.exs lines 1–10 (contrast: uses RepoCase — NOT suitable for Oban tests)
    - accrue/lib/accrue/analytics/dunning.ex (the just-implemented at_risk_subscriptions/1 — read its return shape)
    - .planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-PATTERNS.md §"at_risk_subscriptions_test.exs" section
    - .planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-RESEARCH.md §"Test Case Structure Notes"
  </read_first>
  <behavior>
    - Test 1: Projection-lag race — subscription with non-nil dunning_campaign_started_at AND a dunning.recovered event inserted_at >= campaign start → NOT in result
    - Test 2: Happy path — subscription with non-nil dunning_campaign_started_at and NO terminal event → IS in result with correct keys
    - Test 3: ETA nil fallback — subscription in campaign with no pending Oban job → result row has next_step_eta == nil
    - Test 4: Pre-v1.44 honest default — dunning.campaign_started event without invoice_id key → result row has failure_reason == nil
    - Test 5: Window filter — subscription with dunning_campaign_started_at before window since boundary → NOT in result; subscription with campaign_started_at within window → IS in result
  </behavior>
  <action>
    Create new file `accrue/test/accrue/analytics/at_risk_subscriptions_test.exs`.

    Module header:
    - `defmodule Accrue.Analytics.AtRiskSubscriptionsTest`
    - `use Accrue.BillingCase, async: false` — NOT RepoCase; BillingCase sets up full subscription fixtures and the sandbox
    - `use Oban.Testing, repo: Accrue.TestRepo` — required for Oban job state queries in next_step_eta test
    - Aliases: `alias Accrue.Analytics.Dunning`, `alias Accrue.Billing.{Customer, Subscription}`, `alias Accrue.Events`, `alias Accrue.Repo`

    Setup block: read the existing `dunning_campaign_start_test.exs` to understand what `BillingCase` provides. Create a shared `setup` that inserts:
    - A `Customer` with a valid email and processor_id
    - A `Subscription` for that customer in `:past_due` status with `dunning_campaign_started_at: Accrue.Clock.utc_now()` and a valid `processor_id`
    - A `dunning.campaign_started` event with `subject_id: sub.id` and `data: %{step_count: 3, invoice_id: "in_test_fake"}` (post-v1.44 format) and `inserted_at` set to the campaign start time (use Repo.insert! with explicit timestamps or via Events.record)

    Look at `dunning_campaign_start_test.exs` to understand exactly how to insert ledger events directly (the `Events.record/1` path vs direct `Repo.insert!`). Use the same approach.

    Test 1 — projection-lag race:
    Insert a second `dunning.recovered` event for the setup subscription with `subject_id: sub.id` and `inserted_at: DateTime.add(campaign_started_at, 60, :second)` (after campaign start).
    Assert: `at_risk_subscriptions = Dunning.at_risk_subscriptions()` does NOT contain a map with `subscription_id: sub.id`.
    Implementation note: may require a separate `setup` context or a describe block with its own setup that inserts the terminal event.

    Test 2 — happy path:
    No terminal event inserted (base setup only).
    Call `rows = Dunning.at_risk_subscriptions()`.
    Assert: `assert Enum.any?(rows, &(&1.subscription_id == sub.id))`.
    Assert: for the matching row, `row.customer_id == customer.id`, `is_integer(row.days_in_campaign)`, `is_integer(row.current_step)`.
    Assert: `row.days_in_campaign >= 0`.

    Test 3 — ETA nil fallback:
    No Oban jobs inserted (base setup only — BillingCase may or may not insert jobs; read the fixture to confirm).
    Call `rows = Dunning.at_risk_subscriptions()`.
    Assert: for the matching row, `row.next_step_eta == nil`.

    Test 4 — pre-v1.44 honest default:
    Insert a dunning.campaign_started event with `data: %{step_count: 3}` (NO invoice_id key).
    (Or use the setup subscription but override its campaign_started event to omit invoice_id.)
    Call `rows = Dunning.at_risk_subscriptions()`.
    Assert: for the matching row, `row.failure_reason == nil`.

    Test 5 — window filter:
    Set up a subscription with `dunning_campaign_started_at` = 60 days ago (use `DateTime.add(Accrue.Clock.utc_now(), -60 * 86_400, :second)`).
    Call `rows = Dunning.at_risk_subscriptions(since: DateTime.add(Accrue.Clock.utc_now(), -30 * 86_400, :second))`.
    Assert: the old subscription is NOT in rows (its campaign started before the window).
    Also assert: the setup subscription (campaign started now) IS in rows.

    Use `describe` blocks to organize the 5 test scenarios clearly.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/analytics/at_risk_subscriptions_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `at_risk_subscriptions_test.exs` exists with `use Accrue.BillingCase, async: false` and `use Oban.Testing, repo: Accrue.TestRepo`
    - Test 1 (projection-lag race) passes — the recovered subscription is excluded
    - Test 2 (happy path) passes — the at-risk subscription appears with correct keys
    - Test 3 (ETA nil) passes — `next_step_eta == nil` when no Oban job
    - Test 4 (pre-v1.44 default) passes — `failure_reason == nil` for event without invoice_id
    - Test 5 (window filter) passes — out-of-window subscription excluded
    - `mix test test/accrue/analytics/at_risk_subscriptions_test.exs --seed 0` exits 0
    - Running the full dunning_test.exs still passes (no regression to the existing module): `mix test test/accrue/analytics/dunning_test.exs --seed 0` exits 0
  </acceptance_criteria>
  <done>5 at_risk_subscriptions tests pass including the projection-lag race scenario and pre-v1.44 honest default</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView → Analytics → DB | at_risk_subscriptions/1 takes validated DateTime opts from handle_params (via parse_window/1); no raw user input reaches the query |
| Query fragments | JSONB fragment access uses `^` pinning and `?` positional — no string interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-03 | Tampering | `at_risk_subscriptions/1` JSONB fragment | mitigate | All dynamic values use `^` pinning or `?` positional placeholders in fragment strings — NO string interpolation (`"#{var}"`) in fragment arguments; accept only DateTime values from parse_window/1 |
| T-146-04 | Information Disclosure | `failure_reason` return value | accept | Returns raw `pf.data` jsonb map which contains `stripe_event_id` (reference, not PII) and `source: "webhook"` — no card data, no customer email, no PII per T-129-01 contract |
| T-146-05 | Denial of Service | `at_risk_subscriptions/1` query complexity | accept | Query joins 5 tables with indexed PKs + anchored timestamps; Postgres 14+ planner handles this efficiently; analytics dashboard is operator-only, not public-facing |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No new packages; not applicable |
</threat_model>

<verification>
Quick run after both tasks:

```
cd /Users/jon/projects/accrue/accrue && mix test test/accrue/analytics/at_risk_subscriptions_test.exs test/accrue/analytics/dunning_test.exs --seed 0
```

All tests must exit 0.
</verification>

<success_criteria>
1. `at_risk_subscriptions/1` is defined in `accrue/lib/accrue/analytics/dunning.ex` with `@spec at_risk_subscriptions(keyword()) :: [map()]`
2. `apply_campaign_window/2` is a private helper using `[s]` binding on `s.dunning_campaign_started_at`
3. NOT EXISTS correlated subquery uses fragment with `?` positional — references `dunning.recovered` and `dunning.exhausted` types anchored by `inserted_at >= dunning_campaign_started_at`
4. LEFT JOIN to `Oban.Job` uses worker `"Accrue.Workers.DunningStep"` and state filter `["available", "scheduled", "retryable"]`
5. LEFT JOIN bridge through `Invoice.processor_id` to connect Stripe ID to Accrue UUID
6. `at_risk_subscriptions_test.exs` exists with 5 passing tests: projection-lag race, happy path, ETA nil, pre-v1.44 default, window filter
7. `mix test test/accrue/analytics/at_risk_subscriptions_test.exs test/accrue/analytics/dunning_test.exs --seed 0` exits 0
</success_criteria>

<output>
Create `/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-02-SUMMARY.md` when done.
</output>

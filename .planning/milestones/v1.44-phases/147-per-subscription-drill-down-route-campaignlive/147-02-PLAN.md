---
phase: "147"
plan: "02"
type: execute
wave: 2
depends_on:
  - "147-01"
files_modified:
  - accrue_admin/lib/accrue_admin/router.ex
  - accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex
  - accrue_admin/lib/accrue_admin/components/campaign_timeline.ex
  - accrue_admin/test/accrue_admin/live/analytics/campaign_live_test.exs
  - accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs
autonomous: true
requirements:
  - DAN-12

must_haves:
  truths:
    - "GET /billing/analytics/recovery/subscriptions/:id resolves to CampaignLive and renders without crash"
    - "CampaignLive.mount/3 assigns @arcs and @invoice_map independently via two Dunning.* calls"
    - "CampaignLive renders a vertical timeline with campaign_started anchor rows, step_sent retry rows, and terminal recovered/exhausted rows"
    - "Empty arcs renders the 'No dunning history found' empty state, not a crash or 404"
    - "CampaignLive contains no Ecto.Query import, no Accrue.Repo, no Accrue.Billing.* aliases (cross-package boundary enforced)"
    - "AtRiskTable row-click links in RecoveryLive point to /analytics/recovery/subscriptions/:id"
    - "active_organization_name is NOT assigned in CampaignLive.assign_shell/2 (it is pre-assigned by AuthHook)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/router.ex"
      provides: "live(\"/recovery/subscriptions/:id\", CampaignLive, :show) route"
      contains: "CampaignLive"
    - path: "accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex"
      provides: "CampaignLive LiveView module"
      exports: ["CampaignLive"]
    - path: "accrue_admin/lib/accrue_admin/components/campaign_timeline.ex"
      provides: "CampaignTimeline Phoenix.Component with three row variants"
      exports: ["campaign_timeline"]
    - path: "accrue_admin/test/accrue_admin/live/analytics/campaign_live_test.exs"
      provides: "LiveView tests + boundary assertion for CampaignLive"
      contains: "campaign_live_test"
  key_links:
    - from: "AccrueAdmin.Router scope /analytics"
      to: "AccrueAdmin.Live.Analytics.CampaignLive"
      via: "live(\"/recovery/subscriptions/:id\", CampaignLive, :show)"
      pattern: "CampaignLive.*:show"
    - from: "CampaignLive.mount/3"
      to: "Accrue.Analytics.Dunning.campaign_timeline_grouped/2"
      via: "Dunning.campaign_timeline_grouped(subscription_id)"
      pattern: "Dunning\\.campaign_timeline_grouped"
    - from: "CampaignTimeline.arc_rows/1"
      to: "@invoice_map"
      via: "Map.get(@invoice_map, event.data[\"invoice_id\"])"
      pattern: "invoice_map"
---

<objective>
Implements the accrue_admin half of Phase 147: router addition, CampaignLive LiveView, CampaignTimeline Phoenix.Component, and test coverage. Makes the Phase 146 AtRiskTable row-click link resolve to a real page.

Purpose: Operators can now click any at-risk subscription row and land on a full dunning campaign history page — the "investigate this one customer" drill-down that completes DAN-12.

Output: New route, new LiveView, new component, new test file, one assertion added to recovery_live_test.exs — all tests green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/147-per-subscription-drill-down-route-campaignlive/147-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: campaign_live_test.exs (Wave 0 stubs) + CampaignTimeline component</name>
  <files>
    accrue_admin/test/accrue_admin/live/analytics/campaign_live_test.exs,
    accrue_admin/lib/accrue_admin/components/campaign_timeline.ex
  </files>

  <read_first>
    - accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs — full file: module header, AuthAdapter inline behaviour, setup block, live/2 call pattern, boundary assertion test at lines 243-249 (exact pattern to replicate for CampaignLive)
    - accrue_admin/lib/accrue_admin/components/at_risk_table.ex — module header, use Phoenix.Component, attr/3 declarations, empty-state pattern with data-role="empty-state", ax-* CSS class conventions
    - accrue_admin/lib/accrue_admin/components/status_badge.ex — status_badge/1 attr signature; tone-override form: status={:recovered} tone="moss" label="Recovered"
    - accrue_admin/lib/accrue_admin/components/timeline.ex — confirm no slot machinery; confirm CampaignTimeline is a SEPARATE file (do not modify this file)
    - accrue/lib/accrue/invoices/render.ex lines 106-132 — format_money/3 signature: (amount_minor :: integer(), currency :: atom(), locale :: String.t()) :: String.t()
    - .planning/phases/147-per-subscription-drill-down-route-campaignlive/147-PATTERNS.md — Pattern 5: CampaignTimeline full structure including arc_rows/1, campaign_row/1 heads, step_row/1, format_amount/1 helper, format_datetime/1 helper
    - .planning/phases/147-per-subscription-drill-down-route-campaignlive/147-UI-SPEC.md — Component Inventory section: dot classes (ax-timeline-dot-cobalt, ax-timeline-dot-slate, ax-timeline-dot-moss, ax-timeline-dot-amber), CSS classes for each row variant, format_money nil guard
  </read_first>

  <behavior>
    RED phase — campaign_live_test.exs stubs (tests must fail before CampaignLive exists):

    defmodule AccrueAdmin.Live.Analytics.CampaignLiveTest do
      use AccrueAdmin.LiveCase, async: false
      -- copy AuthAdapter inline behaviour verbatim from recovery_live_test.exs
      -- copy setup block (put_env :auth_adapter) verbatim

      test "renders dunning timeline for subscription with 2 campaign arcs": live(conn, "/billing/analytics/recovery/subscriptions/#{subscription_id}"); assert html =~ "Campaign History"; assert html =~ "Campaign started"
      test "renders empty state for unknown subscription_id": assert html =~ "No dunning history found"
      test "cross-package boundary: CampaignLive does not import Ecto.Query, Accrue.Repo, or Accrue.Billing.*": File.read!("lib/accrue_admin/live/analytics/campaign_live.ex"); refute source =~ "import Ecto.Query"; refute source =~ "Accrue.Repo"; refute source =~ "Accrue.Billing."

    GREEN phase — CampaignTimeline component implementation:

    AccrueAdmin.Components.CampaignTimeline:
    - use Phoenix.Component
    - alias AccrueAdmin.Components.StatusBadge
    - attr(:arcs, :list, required: true), attr(:invoice_map, :map, required: true), attr(:class, :string, default: nil)
    - public def campaign_timeline(assigns): outer <section class={["ax-campaign-timeline", @class]}>; empty-state :if={@arcs == []} with data-role="empty-state", heading "No dunning history found", body "This subscription has no recorded dunning events."; for {_anchor, events} <- @arcs, wrap in <div class="ax-campaign-arc">; delegate to private arc_rows/1
    - private defp arc_rows(assigns): pre-compute step_events_indexed = events filtered to step_sent, then Enum.with_index(1); assign :step_events_indexed; iterate events with for event <- @events; dispatch: if step_sent find attempt number from step_events_indexed via Enum.find(&(elem(&1,0).id == event.id)) and call step_row; else call campaign_row
    - private defp campaign_row(%{event: %{type: "dunning.campaign_started"}} = assigns): class "ax-campaign-timeline-anchor"; dot class "ax-timeline-dot ax-timeline-dot-cobalt"; show date via format_datetime(@event.inserted_at); show invoice context (failure reason as "—" if not available) + card info from @invoice_map[event.data["invoice_id"]]
    - private defp step_row(assigns): class "ax-campaign-timeline-step"; dot class "ax-timeline-dot ax-timeline-dot-slate"; invoice_ctx = Map.get(@invoice_map, @event.data["invoice_id"]); "Attempt {@attempt}" label (1-indexed); timestamp; StatusBadge.status_badge(status: invoice_ctx.status) if invoice_ctx; format_amount(invoice_ctx.amount_due_cents) if invoice_ctx; "—" if nil
    - private defp campaign_row for recovered/exhausted: class "ax-campaign-timeline-terminal"; moss or amber dot/badge tone based on event type; StatusBadge with explicit tone and label; format_datetime timestamp; MRR from event.data["mrr_value_cents"] via format_amount
    - private defp format_amount(nil), do: "—"; defp format_amount(cents): Accrue.Config.get!(:default_currency) + Accrue.Config.default_locale() + Accrue.Invoices.Render.format_money(cents, currency, locale)
    - private defp format_datetime(%DateTime{} = value), do: Calendar.strftime(value, "%b %d, %Y %H:%M UTC"); defp format_datetime(_), do: "—"
    - Step numbering rule: count only step_sent events within the arc (not campaign_started); Enum.with_index(1) on filtered step events; "Attempt 1", "Attempt 2", etc.
    - DO NOT modify accrue_admin/lib/accrue_admin/components/timeline.ex
  </behavior>

  <action>
  Create accrue_admin/test/accrue_admin/live/analytics/campaign_live_test.exs as a new file. Copy the module header, AuthAdapter behaviour, and setup block verbatim from recovery_live_test.exs lines 1-30. Write three test cases: (1) timeline renders for subscription with events — seed 2 arcs via Accrue.Events.record or Repo.insert!, init_test_session with admin_token: "admin", assert html renders; (2) empty state for unknown subscription_id; (3) cross-package boundary assertion via File.read!.

  Create accrue_admin/lib/accrue_admin/components/campaign_timeline.ex as a new file. Component structure: outer campaign_timeline/1 function dispatches to arc_rows/1 per arc. arc_rows/1 pre-computes step attempt numbers (filter to step_sent events, Enum.with_index(1)) to avoid off-by-one from campaign_started index. Three row-variant private functions: campaign_row/1 for campaign_started, step_row/1 for step_sent (separate attr declarations), campaign_row/1 for recovered/exhausted. format_amount/1 and format_datetime/1 private helpers at bottom of module.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && mix test test/accrue_admin/live/analytics/campaign_live_test.exs 2>&1 | tail -20</automated>
  </verify>

  <done>
    - accrue_admin/test/accrue_admin/live/analytics/campaign_live_test.exs exists with 3 test cases
    - accrue_admin/lib/accrue_admin/components/campaign_timeline.ex exists with def campaign_timeline/1 (public) and private arc_rows/1, campaign_row/1 (2 heads), step_row/1 functions
    - campaign_timeline.ex contains defp format_amount/1 and defp format_datetime/1 helpers
    - campaign_timeline.ex does NOT contain slot-based inner_block machinery
    - Step numbering uses Enum.with_index(1) on filtered step_sent events (not on all arc events)
    - Tests pass for the boundary assertion test; the LiveView tests are red until Task 2 adds CampaignLive
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CampaignLive LiveView + router line + recovery_live_test.exs row-link assertion</name>
  <files>
    accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex,
    accrue_admin/lib/accrue_admin/router.ex,
    accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs
  </files>

  <read_first>
    - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex — full file: module header, assign_shell/2 private function (lines 116-127), default_brand/0, mount/3 pattern, render/1 with AppShell.app_shell wrapper and breadcrumbs, @active_organization_name usage (assigned by AuthHook, NOT by assign_shell/2)
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex lines 33-61 — mount/3 with %{"id" => id} param pattern as secondary analog
    - accrue_admin/lib/accrue_admin/router.ex lines 45-90 — live_session :accrue_admin block structure; scope "/analytics" block (lines 75-77); add live("/recovery/subscriptions/:id", CampaignLive, :show) inside that scope
    - accrue_admin/lib/accrue_admin/auth_hook.ex lines 11-33 — confirm active_organization_name is assigned at line 26 by AuthHook, before mount/3 runs
    - accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs lines 222-250 — existing "at-risk table (DAN-11)" describe block; add row-link assertion inside this describe block
    - .planning/phases/147-per-subscription-drill-down-route-campaignlive/147-PATTERNS.md — Pattern 4: CampaignLive mount structure; Pattern 6: router addition
    - .planning/phases/147-per-subscription-drill-down-route-campaignlive/147-UI-SPEC.md — CampaignLive section: AppShell wrapper shape, breadcrumb crumbs list, page_title "Dunning Timeline", subscription_id as subtitle
  </read_first>

  <behavior>
    CampaignLive module:
    - defmodule AccrueAdmin.Live.Analytics.CampaignLive; @moduledoc false; use Phoenix.LiveView
    - Aliases: alias Accrue.Analytics.Dunning; alias AccrueAdmin.Components.{AppShell, Breadcrumbs, CampaignTimeline}
    - NO Ecto.Query import; NO Accrue.Repo alias; NO Accrue.Billing.* alias (per D-04 boundary enforcement)
    - @impl true def mount(%{"id" => subscription_id}, session, socket): admin = Map.get(session, "accrue_admin", %{}); arcs = Dunning.campaign_timeline_grouped(subscription_id); invoice_map = Dunning.invoices_for_campaign(subscription_id); {:ok, socket |> assign_shell(admin) |> assign(:subscription_id, subscription_id) |> assign(:arcs, arcs) |> assign(:invoice_map, invoice_map)}
    - defp assign_shell(socket, admin): copy from recovery_live.ex lines 116-127 verbatim but change :page_title to "Dunning Timeline"; :current_path stays the same ((admin["mount_path"] || "/billing") <> "/analytics/recovery") — this is the breadcrumb back-link target, not CampaignLive's own URL
    - DO NOT assign :active_organization_name in assign_shell/2 — AuthHook.on_mount/4 assigns it before mount/3 runs (auth_hook.ex line 26); assigning it again would overwrite the AuthHook value
    - @impl true def render(assigns): AppShell.app_shell wrapper with brand, current_path, mount_path, page_title, theme, active_organization_name attrs; inside: section.ax-page with header (h1.ax-heading "Dunning Timeline", p.ax-body.ax-muted with @subscription_id); Breadcrumbs.breadcrumbs with items: [%{label: "Analytics"}, %{label: "Recovery", href: @current_path}, %{label: "Subscription"}]; CampaignTimeline.campaign_timeline with arcs={@arcs} and invoice_map={@invoice_map}
    - Not-found handling: Dunning.campaign_timeline_grouped/2 returns [] for unknown subscription_id; CampaignTimeline renders empty state; no redirect, no 404 (per D-15)

    Router change:
    - In accrue_admin/lib/accrue_admin/router.ex, inside the scope "/analytics", AccrueAdmin.Live.Analytics block (lines ~75-77), add as second line: live("/recovery/subscriptions/:id", CampaignLive, :show)
    - This is the ONLY router change; the live_session :accrue_admin wrapper already covers this scope
    - Add CampaignLive to the alias or use fully-qualified; check if other LiveViews in the scope use bare module names or full names

    recovery_live_test.exs addition:
    - Inside the existing describe "at-risk table (DAN-11)" block (lines 222-250), add one new test: "at-risk table row links to per-subscription drill-down route" — assert live(conn, "/billing/analytics/recovery") renders HTML containing either "/analytics/recovery/subscriptions/" or "No active dunning campaigns" (acceptable when no at-risk rows are seeded)
  </behavior>

  <action>
  Create accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex as a new file. Follow the exact module structure from recovery_live.ex but simpler: no handle_params/3, no window selector, no funnel or at_risk calls. Two assigns from Dunning: @arcs and @invoice_map. assign_shell/2 is copied from recovery_live.ex with page_title changed to "Dunning Timeline". Render uses AppShell wrapper with the same attr names as RecoveryLive. Breadcrumbs point back to the recovery dashboard via @current_path.

  In accrue_admin/lib/accrue_admin/router.ex: open the scope "/analytics", AccrueAdmin.Live.Analytics block and add live("/recovery/subscriptions/:id", CampaignLive, :show) as a sibling to the existing RecoveryLive route. Do not change any other router lines.

  In accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs: add a test inside the existing describe "at-risk table (DAN-11)" block asserting the row-link href pattern.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && mix test test/accrue_admin/live/analytics/campaign_live_test.exs test/accrue_admin/live/analytics/recovery_live_test.exs 2>&1 | tail -30</automated>
  </verify>

  <done>
    - accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex exists with mount/3, assign_shell/2, render/1 defined
    - CampaignLive contains alias Accrue.Analytics.Dunning and no other accrue package aliases from Billing.* or Repo
    - CampaignLive does NOT assign :active_organization_name in assign_shell/2
    - router.ex contains live("/recovery/subscriptions/:id", CampaignLive, :show) inside the scope "/analytics" block
    - campaign_live_test.exs: all 3 tests pass — timeline renders, empty state renders, boundary assertion passes
    - recovery_live_test.exs: new row-link test passes
    - Full accrue_admin test suite: cd /Users/jon/projects/accrue/accrue_admin && mix test exits 0
    - Full both suites: cd /Users/jon/projects/accrue/accrue && mix test && cd ../accrue_admin && mix test — both exit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → CampaignLive | :id URL param is a free-form binary string passed to mount/3 params |
| live_session :accrue_admin → CampaignLive | Admin-only gate via AuthHook on_mount :ensure_admin before mount/3 runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-01 | Elevation of Privilege | CampaignLive route | mitigate | Route is inside live_session :accrue_admin with on_mount: [{AccrueAdmin.AuthHook, :ensure_admin}] — non-admin session is redirected before mount/3 runs; no new auth surface introduced |
| T-147-02 | Information Disclosure | CampaignLive IDOR via subscription_id | accept | Admin-only route; authorized operators have full billing access by design; no owner-scoping needed for admin context |
| T-147-05 | Tampering | cross-package boundary | mitigate | CampaignLive aliases only Accrue.Analytics.Dunning — no Ecto.Query, Accrue.Repo, or Accrue.Billing.* — enforced by static grep assertion test in campaign_live_test.exs |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — zero mix dep changes |
</threat_model>

<verification>
After both tasks:
- cd /Users/jon/projects/accrue/accrue_admin && mix test — full accrue_admin suite green
- cd /Users/jon/projects/accrue/accrue && mix test — full accrue suite still green
- grep "CampaignLive" /Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/router.ex — route present
- grep -v "^#\|^\s*#" /Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex | grep -c "Ecto.Query\|Accrue.Repo\|Accrue.Billing\." — count must equal 0
- grep "active_organization_name" /Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex | grep "assign(" — must NOT find an assign(:active_organization_name, ...) call (it must be referenced in render/1 only, not set in assign_shell/2)
- grep "campaign_timeline_grouped\|invoices_for_campaign" /Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/live/analytics/campaign_live.ex — both calls present in mount/3
</verification>

<success_criteria>
- GET /billing/analytics/recovery/subscriptions/:id resolves and renders the CampaignLive page for authenticated admin
- CampaignLive.mount/3 makes exactly two Dunning.* calls: campaign_timeline_grouped/2 and invoices_for_campaign/2
- CampaignTimeline renders three structurally distinct row variants per event type
- Empty arcs renders "No dunning history found" empty state (not 404, not crash)
- Boundary assertion test passes: no Ecto.Query / Accrue.Repo / Accrue.Billing.* in campaign_live.ex
- At-risk table row-link test passes: /analytics/recovery/subscriptions/ href pattern present in recovery dashboard HTML
- All 5 new/modified test files green; cd /Users/jon/projects/accrue/accrue_admin && mix test exits 0
</success_criteria>

<output>
Create `.planning/phases/147-per-subscription-drill-down-route-campaignlive/147-02-SUMMARY.md` when done
</output>

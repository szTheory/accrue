---
phase: 148-cross-currency-widening-docs-adopter-proof
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
  - accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs
autonomous: true
requirements:
  - DAN-07
  - DAN-14
must_haves:
  truths:
    - "RecoveryLive renders KPI cards per currency"
    - "RecoveryLive renders a 'Showing data since...' badge or link to docs"
  artifacts:
    - path: accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
      provides: "Multi-currency rendered KPI Cards"
      contains: "for currency"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex"
      to: "Accrue.Analytics.Dunning"
      via: "stats access"
      pattern: "stats.recovered"
---

<objective>
Update the admin `RecoveryLive` dashboard to dynamically render multi-currency KPI cards (DAN-07) and add the cutoff-date documentation badge (DAN-14).

Purpose: Support the widened `recovered_vs_lost_mrr/1` API. 99% of single-currency adopters will see exactly what they saw before, while multi-currency ones get a wrapping grid of KPI pairs.
Output: Modified `RecoveryLive`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-CONTEXT.md
@.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-DISCUSSION-LOG.md
@accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex

<interfaces>
From accrue/lib/accrue/analytics/dunning.ex:
```elixir
# The widened shape output from Plan 01
%{
  recovered: [%{currency: "usd", cents: 5000}, ...],
  lost: [%{currency: "usd", cents: 2000}, ...]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Multi-Currency KPI Cards</name>
  <files>accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex, accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs</files>
  <action>Update `RecoveryLive.handle_params/3` and the inline `render` HEEx template. Extract the unique currencies from both `stats.recovered` and `stats.lost`. For each currency, iterate and render a `KpiCard` pair (Recovered MRR, Exhausted MRR) using the `Accrue.Invoices.Render.format_money` for the respective currency cents. Remove the old single-currency `recovered_str` and `exhausted_str` assigns if no longer needed, assigning a list of currency-stats instead.</action>
  <verify>
    <automated>mix test accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs</automated>
  </verify>
  <done>The template dynamically iterates through currencies, rendering one pair of KPI cards for each.</done>
</task>

<task type="auto">
  <name>Task 2: Add Cutoff Date UI Badge</name>
  <files>accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex</files>
  <action>Add a small UI badge/pill in the dashboard header (near `WindowSelector`) displaying "Showing data since YYYY-MM-DD" or linking to the new `guides/analytics.md` documentation explaining cutoff semantics. You can use a static date if defined, or fetch `Accrue.Events.earliest_dunning_event_date()` if available.</action>
  <verify>
    <automated>mix test accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs</automated>
  </verify>
  <done>Cutoff date documentation is clearly linked in the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI -> Core | Admin UI calls the core safely; admin auth inherited via `live_session`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-02 | Information Disclosure | `RecoveryLive` | accept | Admin-auth is inherited; only authorized administrators have access. |
</threat_model>

<verification>
Start the `mix phx.server` locally (or run test suite) and ensure the dashboard mounts without crashing on the new stats map shape.
</verification>

<success_criteria>
Multi-currency KPI cards are rendered side-by-side cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-02-SUMMARY.md`
</output>
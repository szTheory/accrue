---
phase: 158-oban-cron-wiring-adopter-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/test/accrue_host/recovery_wiring_test.exs
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/docs/adoption-proof-matrix.md
autonomous: true
requirements: [PRF-03]
requirements_addressed: [PRF-03]

must_haves:
  truths:
    - "PRF-03 / D-01 / D-02 / D-04 / D-09: `recovery_wiring_test.exs` reads the base host `config/config.exs` with `Config.Reader.read!/2`, extracts `[:accrue_host, Oban]`, validates it with `Oban.Config.validate/1`, and uses that base config as the primary wiring proof without extracting a shared `AccrueHost.ObanConfig` helper."
    - "PRF-03 / D-03: The runtime `Application.fetch_env!(:accrue_host, Oban)` test-env safety remains separate and asserts `plugins: false`, `queues: false`, and `testing: :manual` without treating those disabled values as the production/adopter wiring proof."
    - "PRF-03 / D-05 / D-06 / D-07: The proof asserts required host queues `:accrue_webhooks`, `:accrue_mailers`, `:accrue_dunning`, `:accrue_meters`, and `:accrue_scheduled`; `:accrue_dunning` is included because `DunningSweeper` uses that queue even though the older PRF-03 wording omitted it."
    - "PRF-03 / D-08: The proof asserts `Oban.Plugins.Cron` includes `Accrue.Jobs.DunningSweeper`, `Accrue.Jobs.DetectExpiringCards`, `Accrue.Jobs.MeterEventsReconciler`, and `Accrue.Jobs.MeteredRenewalReconciler`."
    - "PRF-03 / D-10 / D-11 / D-12: `recovery_wiring_test.exs` stays focused on host wiring/config proof and does not expand into broad behavior smoke coverage that duplicates core job tests."
    - "PRF-03 / D-13 / D-14 / D-15 / D-16: `examples/accrue_host/config/config.exs` contains the canonical append-merge teaching beside the `Oban.Plugins.Cron` crontab, and `adoption-proof-matrix.md` points to that canonical snippet without duplicating it."
  artifacts:
    - path: "examples/accrue_host/test/accrue_host/recovery_wiring_test.exs"
      provides: "Executable host-level Oban cron and queue wiring proof"
      contains: "Config.Reader.read!"
    - path: "examples/accrue_host/config/config.exs"
      provides: "Canonical append-merge teaching at the adopter-copyable crontab"
      contains: "existing_cron_jobs() ++"
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "Matrix row that names the cron workers, required queues, and config comment pointer"
      contains: "DunningSweeper"
  key_links:
    - from: "examples/accrue_host/test/accrue_host/recovery_wiring_test.exs"
      to: "examples/accrue_host/config/config.exs"
      via: "Static base config is read with `Config.Reader.read!/2` and validated with `Oban.Config.validate/1`"
      pattern: "Config.Reader.read!"
    - from: "examples/accrue_host/config/config.exs"
      to: "examples/accrue_host/docs/adoption-proof-matrix.md"
      via: "Matrix points readers to the canonical append-merge comment instead of repeating the snippet"
      pattern: "config/config.exs"
---

<objective>
Close PRF-03 by making `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` deterministically prove the example host's base Oban config wires all required Accrue cron workers and queues, while keeping runtime test safety separate and adding a concise append-merge teaching comment for adopters with an existing crontab.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/158-oban-cron-wiring-adopter-proof/158-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/158-oban-cron-wiring-adopter-proof/158-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/158-oban-cron-wiring-adopter-proof/158-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/158-oban-cron-wiring-adopter-proof/158-VALIDATION.md

<interfaces>
From Oban testing config docs:
```elixir
config =
  "config/config.exs"
  |> Config.Reader.read!(env: :prod)
  |> get_in([:my_app, Oban])

assert :ok = Oban.Config.validate(config)
```

From `examples/accrue_host/config/config.exs`:
```elixir
config :accrue_host, Oban,
  repo: AccrueHost.Repo,
  queues: [
    accrue_webhooks: 10,
    accrue_mailers: 20,
    accrue_pdf: 5,
    accrue_dunning: 2,
    accrue_meters: 5,
    accrue_scheduled: 5
  ],
  plugins: [
    {Oban.Plugins.Pruner, max_age: 60 * 60 * 24},
    {Oban.Plugins.Cron,
     crontab: [
       {"*/15 * * * *", Accrue.Jobs.DunningSweeper},
       {"@daily", Accrue.Jobs.DetectExpiringCards},
       {"* * * * *", Accrue.Jobs.MeterEventsReconciler},
       {"*/5 * * * *", Accrue.Jobs.MeteredRenewalReconciler}
     ]}
  ]
```

From worker source:
```elixir
use Oban.Worker, queue: :accrue_dunning, max_attempts: 3
use Oban.Worker, queue: :accrue_scheduled, max_attempts: 3
use Oban.Worker, queue: :accrue_meters, max_attempts: 3
```
</interfaces>
</context>

<threat_model>
## Security Threat Model

| ID | Threat | Severity | Mitigation in Plan |
|----|--------|----------|--------------------|
| T-158-01 | False-positive adopter proof accepts `MIX_ENV=test` disabled Oban config as evidence that production/base cron workers and queues are wired | medium | Read and validate `config/config.exs` directly with `Config.Reader.read!/2` + `Oban.Config.validate/1`; keep runtime disabled config in a separate safety assertion. |
| T-158-02 | Dunning cron appears configured while its queue is missing, causing scheduled dunning work to never run in an adopter host | medium | Assert `:accrue_dunning` along with `:accrue_webhooks`, `:accrue_mailers`, `:accrue_meters`, and `:accrue_scheduled`; matrix documents the corrected queue truth. |
| T-158-03 | Append-merge guidance causes adopters to replace an existing crontab and accidentally remove their own scheduled jobs | medium | Add a callsite comment beside `crontab:` that says to append Accrue entries and includes `crontab: existing_cron_jobs() ++ [ ...Accrue entries... ]`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade `recovery_wiring_test.exs` to prove base Oban config wiring</name>
  <files>examples/accrue_host/test/accrue_host/recovery_wiring_test.exs</files>
  <read_first>
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-CONTEXT.md — D-01 through D-12 lock base-config proof shape, no shared `AccrueHost.ObanConfig` helper, queue truth, and proof depth.
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-RESEARCH.md — Oban docs findings and validation architecture.
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-PATTERNS.md — exact test target pattern and queue/worker source truth.
    - examples/accrue_host/test/accrue_host/recovery_wiring_test.exs — current false-positive runtime-config pass path and existing smoke tests.
    - examples/accrue_host/config/config.exs — base host Oban queues/plugins to prove.
    - examples/accrue_host/config/test.exs — runtime test safety override to keep separate.
    - accrue/lib/accrue/jobs/dunning_sweeper.ex — confirms `queue: :accrue_dunning`.
    - accrue/lib/accrue/jobs/detect_expiring_cards.ex — confirms `queue: :accrue_scheduled`.
    - accrue/lib/accrue/jobs/meter_events_reconciler.ex — confirms `queue: :accrue_meters`.
    - accrue/lib/accrue/jobs/metered_renewal_reconciler.ex — confirms `queue: :accrue_meters`.
    - accrue/lib/accrue/webhook/dispatch_worker.ex — confirms `queue: :accrue_webhooks`.
    - accrue/lib/accrue/workers/mailer.ex — confirms `queue: :accrue_mailers`.
  </read_first>
  <behavior>
    - The primary proof reads `examples/accrue_host/config/config.exs`, extracts the `:accrue_host, Oban` config, and validates it with `Oban.Config.validate/1`.
    - The primary proof fails if any required cron worker is absent from the base `Oban.Plugins.Cron` crontab.
    - The primary proof fails if any required queue key is absent from the base queue list.
    - Runtime test safety is explicit but separate: `Application.fetch_env!(:accrue_host, Oban)` still proves `plugins: false`, `queues: false`, and `testing: :manual`.
  </behavior>
  <action>In `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs`, add `alias Accrue.Jobs.DunningSweeper` beside the existing cron worker aliases. Replace the conditional `Application.fetch_env!` cron/queue tests with deterministic base-config tests that call `Config.Reader.read!("config/config.exs", env: :dev)` or an equivalent path rooted at `examples/accrue_host`, then `get_in([:accrue_host, Oban])`, then `assert :ok = Oban.Config.validate(oban_config)`. Add helper functions only inside the test module if they reduce duplication: `base_oban_config/0`, `cron_workers/1`, and `queue_names/1` are acceptable. Do not create a new shared `AccrueHost.ObanConfig` module or production helper. Assert cron workers include exactly the required modules `DunningSweeper`, `DetectExpiringCards`, `MeterEventsReconciler`, and `MeteredRenewalReconciler`. Assert queue names include `:accrue_webhooks`, `:accrue_mailers`, `:accrue_dunning`, `:accrue_meters`, and `:accrue_scheduled`. Add or keep a separate test that asserts runtime test config has `plugins: false`, `queues: false`, and `testing: :manual`. Remove the behavior-heavy job execution smoke tests if they add database/runtime fragility; do not add `Oban.drain_queue/1`, clock advancement, webhook stubs, or fixture choreography here.</action>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` contains `Config.Reader.read!`.
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` contains `Oban.Config.validate`.
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` contains `DunningSweeper`, `DetectExpiringCards`, `MeterEventsReconciler`, and `MeteredRenewalReconciler` in cron worker assertions.
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` contains assertions for `:accrue_webhooks`, `:accrue_mailers`, `:accrue_dunning`, `:accrue_meters`, and `:accrue_scheduled`.
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` contains assertions that runtime test config has `plugins: false`, `queues: false`, and `testing: :manual`.
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` no longer accepts `plugins == false` or `queues == false` as the primary base-config wiring proof.
    - No new `AccrueHost.ObanConfig` module or shared production config helper is created.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host/recovery_wiring_test.exs --seed 0</automated>
    <automated>rg "Config.Reader.read!|Oban.Config.validate|DunningSweeper|DetectExpiringCards|MeterEventsReconciler|MeteredRenewalReconciler" /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host/recovery_wiring_test.exs</automated>
    <automated>rg "accrue_webhooks|accrue_mailers|accrue_dunning|accrue_meters|accrue_scheduled" /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host/recovery_wiring_test.exs</automated>
  </verify>
  <done>The recovery wiring test gives adopters a deterministic pass/fail signal for base Oban cron and queue wiring while preserving safe test runtime config.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add canonical append-merge teaching beside the host crontab</name>
  <files>examples/accrue_host/config/config.exs</files>
  <read_first>
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-CONTEXT.md — D-13 through D-16 lock comment placement and avoid README duplication.
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-RESEARCH.md — append-vs-replace pitfall.
    - examples/accrue_host/config/config.exs — current `Oban.Plugins.Cron` crontab.
  </read_first>
  <behavior>
    - The host config remains functionally unchanged.
    - A short comment immediately beside the `crontab:` list warns adopters with an existing crontab to append Accrue entries rather than replacing their own entries.
    - The comment includes the concrete shape `crontab: existing_cron_jobs() ++ [ ...Accrue entries... ]`.
  </behavior>
  <action>In `examples/accrue_host/config/config.exs`, add a concise comment immediately above the `crontab:` key inside `{Oban.Plugins.Cron, ...}`. The comment must use the exact phrase shape `crontab: existing_cron_jobs() ++ [ ...Accrue entries... ]` and state that adopters with existing cron jobs should append Accrue's entries instead of replacing their crontab. Do not move the queue list, change schedules, change worker modules, add helper functions, or edit README.</action>
  <acceptance_criteria>
    - `examples/accrue_host/config/config.exs` contains `existing_cron_jobs() ++`.
    - The append-merge comment appears within 5 lines above `crontab:`.
    - The existing cron entries for `Accrue.Jobs.DunningSweeper`, `Accrue.Jobs.DetectExpiringCards`, `Accrue.Jobs.MeterEventsReconciler`, and `Accrue.Jobs.MeteredRenewalReconciler` remain present.
    - The existing queues `accrue_webhooks`, `accrue_mailers`, `accrue_dunning`, `accrue_meters`, and `accrue_scheduled` remain present.
  </acceptance_criteria>
  <verify>
    <automated>rg "existing_cron_jobs\\(\\).*\\+\\+" /Users/jon/projects/accrue/examples/accrue_host/config/config.exs</automated>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host/recovery_wiring_test.exs --seed 0</automated>
  </verify>
  <done>The canonical copyable host config teaches safe append-merge without changing runtime behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update the adoption-proof matrix to reflect the full cron and queue proof</name>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <read_first>
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-CONTEXT.md — D-05 through D-16 lock queue correction and matrix pointer scope.
    - .planning/phases/158-oban-cron-wiring-adopter-proof/158-RESEARCH.md — queue truth and docs scope.
    - examples/accrue_host/docs/adoption-proof-matrix.md — current Recovery wiring row.
    - examples/accrue_host/config/config.exs — canonical append-merge comment target.
  </read_first>
  <behavior>
    - The existing Recovery wiring row names all four cron workers.
    - The row names the queue set required for the host background story, including `accrue_dunning`.
    - The row points readers to `config/config.exs` for the append-merge comment.
    - The matrix does not duplicate the full crontab snippet.
  </behavior>
  <action>In `examples/accrue_host/docs/adoption-proof-matrix.md`, update the existing `Recovery wiring (PROOF-06)` row in the blocking matrix. The proof text must name `DunningSweeper`, `DetectExpiringCards`, `MeterEventsReconciler`, and `MeteredRenewalReconciler`; it must name `accrue_webhooks`, `accrue_mailers`, `accrue_dunning`, `accrue_meters`, and `accrue_scheduled`; and it must point to `config/config.exs` as the canonical append-merge comment. Do not add a new long section, duplicate the crontab snippet, or broaden the matrix beyond this row.</action>
  <acceptance_criteria>
    - `examples/accrue_host/docs/adoption-proof-matrix.md` contains `DunningSweeper`, `DetectExpiringCards`, `MeterEventsReconciler`, and `MeteredRenewalReconciler` in the Recovery wiring row or adjacent row text.
    - `examples/accrue_host/docs/adoption-proof-matrix.md` contains `accrue_webhooks`, `accrue_mailers`, `accrue_dunning`, `accrue_meters`, and `accrue_scheduled` in the Recovery wiring row or adjacent row text.
    - `examples/accrue_host/docs/adoption-proof-matrix.md` contains `config/config.exs` and `append-merge`.
    - `examples/accrue_host/docs/adoption-proof-matrix.md` does not contain a duplicated full crontab list with all four schedules.
  </acceptance_criteria>
  <verify>
    <automated>rg "DunningSweeper|DetectExpiringCards|MeterEventsReconciler|MeteredRenewalReconciler|accrue_dunning|append-merge|config/config.exs" /Users/jon/projects/accrue/examples/accrue_host/docs/adoption-proof-matrix.md</automated>
  </verify>
  <done>The matrix truthfully advertises the executable Oban cron/queue proof and points to the canonical append-merge teaching.</done>
</task>

</tasks>

<verification>
Run:

`cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host/recovery_wiring_test.exs --seed 0`

Then verify source-level proof coverage:

`rg "Config.Reader.read!|Oban.Config.validate|DunningSweeper|DetectExpiringCards|MeterEventsReconciler|MeteredRenewalReconciler" /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host/recovery_wiring_test.exs`

`rg "accrue_webhooks|accrue_mailers|accrue_dunning|accrue_meters|accrue_scheduled" /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host/recovery_wiring_test.exs`

`rg "existing_cron_jobs\\(\\).*\\+\\+" /Users/jon/projects/accrue/examples/accrue_host/config/config.exs`

`rg "DunningSweeper|DetectExpiringCards|MeterEventsReconciler|MeteredRenewalReconciler|accrue_dunning|append-merge|config/config.exs" /Users/jon/projects/accrue/examples/accrue_host/docs/adoption-proof-matrix.md`
</verification>

<success_criteria>
- `recovery_wiring_test.exs` fails when the base host config lacks any required cron worker or required queue.
- `recovery_wiring_test.exs` validates the base Oban config with `Oban.Config.validate/1`.
- Runtime `MIX_ENV=test` Oban safety remains asserted separately as `plugins: false`, `queues: false`, and `testing: :manual`.
- `config.exs` contains the append-merge comment beside the crontab.
- `adoption-proof-matrix.md` accurately names all four cron workers, the corrected queue set including `accrue_dunning`, and the canonical config comment pointer.
- No new runtime behavior, public API, production helper module, schema change, or broad README rewrite is introduced.
</success_criteria>

## PLANNING COMPLETE

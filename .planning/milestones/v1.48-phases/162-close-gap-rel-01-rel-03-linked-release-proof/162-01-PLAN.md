---
phase: 162-close-gap-rel-01-rel-03-linked-release-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release-please.yml
  - scripts/ci/capture_linked_release_proof.sh
  - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
autonomous: true
requirements: [REL-01, REL-03]
must_haves:
  truths:
    - "The next linked release line after 1.3.0 is bound to one real PR number, one target version, and one workflow run id."
    - "Phase 159 remains the only proof authority for linked-release evidence and recovery state."
    - "Partial publish failures are recorded in the canonical ledger before any retry, revert, retire, or forward-fix action."
  artifacts:
    - path: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      provides: "Append-only canonical release-truth ledger with real PR/TARGET/RUN identity and proof or recovery block"
    - path: "scripts/ci/capture_linked_release_proof.sh"
      provides: "Deterministic proof capture plus structured recovery-block append path"
    - path: ".github/workflows/release-please.yml"
      provides: "Release Please workflow that can produce the next linked proof line without duplicate-release deadlock"
  key_links:
    - from: "scripts/ci/capture_linked_release_proof.sh"
      to: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      via: "proof append or recovery append"
      pattern: "159-VERIFICATION\\.md|Proof capture|Recovery attempt"
    - from: ".github/workflows/release-please.yml"
      to: "scripts/ci/capture_linked_release_proof.sh"
      via: "linked-release-proof job"
      pattern: "Capture linked release proof"
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/verify_release_pr_scope.sh"
      via: "PR_NUMBER/TARGET_VERSION pre-merge proof row"
      pattern: "PR_NUMBER:|TARGET_VERSION:"
---

<objective>
Establish one real post-`1.3.0` linked release identity and capture its canonical proof in the Phase 159 ledger, with a structured recovery path for partial publish failures.

Purpose: unblock `REL-01` and `REL-03` with authoritative evidence instead of blocker truth or mirror-only claims.
Output: updated proof tooling/workflow if needed and an appended canonical Phase 159 ledger block containing either successful proof or a recovery record that precedes retry action.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-CONTEXT.md
@.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-RESEARCH.md
@.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-VALIDATION.md
@.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-PATTERNS.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-02-SUMMARY.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
@.planning/v1.48-v1.48-MILESTONE-AUDIT.md
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/release-please.yml
@scripts/ci/capture_linked_release_proof.sh
@scripts/ci/verify_release_pr_scope.sh
@scripts/ci/repair_linked_release_pr.sh
@scripts/ci/accrue_host_hex_smoke.sh
@scripts/ci/verify_release_notes_contract.sh
@RELEASING.md

<interfaces>
From `scripts/ci/verify_release_pr_scope.sh`:
- `bash scripts/ci/verify_release_pr_scope.sh --pr <number-or-url> [--version <x.y.z>]`
- Requires `.release-please-manifest.json`, all three package `mix.exs`, and all three package `CHANGELOG.md` files in the release PR.

From `scripts/ci/capture_linked_release_proof.sh`:
- `bash scripts/ci/capture_linked_release_proof.sh --version <x.y.z> --run-id <id> --pr <number-or-url> --output <path>`
- Enforces `TARGET_VERSION > 1.3.0`, a successful Release Please run, matching merge commit, tags, GitHub releases, Hex API truth, HexDocs, and release snapshot files.

From `scripts/ci/repair_linked_release_pr.sh`:
- `bash scripts/ci/repair_linked_release_pr.sh --version <x.y.z>`
- Repairs only `.release-please-manifest.json`, `accrue_portal/mix.exs`, and `accrue_portal/CHANGELOG.md` on the checked-out release branch when portal is left behind.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical recovery-block support and keep the release workflow unblocked</name>
  <files>.github/workflows/release-please.yml, scripts/ci/capture_linked_release_proof.sh</files>
  <read_first>
.github/workflows/release-please.yml
scripts/ci/capture_linked_release_proof.sh
RELEASING.md
.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-CONTEXT.md
.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-RESEARCH.md
.planning/v1.48-v1.48-MILESTONE-AUDIT.md
  </read_first>
  <acceptance_criteria>
- `bash scripts/ci/capture_linked_release_proof.sh --help` documents a recovery append path that can write `target_version`, `run_id`, `pr_number`, `accrue_public_state`, `accrue_admin_public_state`, `accrue_portal_public_state`, `failed_step`, `recovery_path`, `next_command`, and `recorded_at` into the canonical Phase 159 ledger.
- `bash -n scripts/ci/capture_linked_release_proof.sh` passes after the recovery-path change.
- `actionlint .github/workflows/release-please.yml` passes and the file still contains the duplicate GitHub release guard plus the `linked-release-proof` job.
- No new proof file path is introduced; the workflow and script still target `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` per D-01, D-04, and D-10.
  </acceptance_criteria>
  <action>Per D-01, D-04, D-09, D-10, D-12, and D-18, preserve Phase 159 as the sole proof authority. Keep the existing duplicate GitHub release guard in `.github/workflows/release-please.yml`, and extend `scripts/ci/capture_linked_release_proof.sh` so it can append a structured recovery block to `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` before any retry or revert when a publish run has already made one or more packages public. Use the exact recovery-field names from the acceptance criteria; do not create `162-VERIFICATION.md`, a second ledger, or a separate failed-attempt appendix file in this task.</action>
  <verify>
    <automated>bash -n scripts/ci/capture_linked_release_proof.sh &amp;&amp; actionlint .github/workflows/release-please.yml</automated>
  </verify>
  <done>Proof tooling can record either successful linked proof or a partial-publish recovery block in the canonical Phase 159 ledger without introducing a second truth source.</done>
</task>

<task type="auto">
  <name>Task 2: Bind REL-01 to one real Release Please PR and target version</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</files>
  <read_first>
.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
release-please-config.json
.release-please-manifest.json
.github/workflows/release-please.yml
scripts/ci/verify_release_pr_scope.sh
scripts/ci/repair_linked_release_pr.sh
.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-CONTEXT.md
.planning/v1.48-v1.48-MILESTONE-AUDIT.md
  </read_first>
  <acceptance_criteria>
- The newest dated block in `159-VERIFICATION.md` contains non-placeholder `PR_NUMBER:` and `TARGET_VERSION:` values for one combined release line greater than `1.3.0`.
- `bash scripts/ci/verify_release_pr_scope.sh --pr <PR_NUMBER> --version <TARGET_VERSION>` passes for the exact identifier pair recorded in the ledger.
- If `accrue_portal` is left behind on the release branch, the executor uses `bash scripts/ci/repair_linked_release_pr.sh --version <TARGET_VERSION>` and records that repair attempt in the same dated ledger block before merge.
- The block does not use `TBD`, `<pr>`, `<target>`, `latest`, or mirror-only evidence when recording REL-01 identity.
  </acceptance_criteria>
  <action>Per D-08, D-13, and D-17, establish one real release identity before touching any mirrors. First check for an open combined Release Please PR on `release-please--branches--main`. If no such PR exists, merge or push the release-process changes from Task 1 to `main` so Release Please can open the next linked line. Once the PR exists, capture its exact number and target version, run `bash scripts/ci/verify_release_pr_scope.sh --pr <PR_NUMBER> --version <TARGET_VERSION>`, repair the portal branch with `bash scripts/ci/repair_linked_release_pr.sh --version <TARGET_VERSION>` if needed, and append a dated pre-merge proof row to `159-VERIFICATION.md` with the exact PR/version pair. Do not close REL-01 from workflow wiring alone.</action>
  <verify>
    <automated>bash -lc 'LEDGER=.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md; PR=$(sed -n "s/^PR_NUMBER: //p" "$LEDGER" | tail -n1); TARGET=$(sed -n "s/^TARGET_VERSION: //p" "$LEDGER" | tail -n1); test -n "$PR" &amp;&amp; test -n "$TARGET" &amp;&amp; bash scripts/ci/verify_release_pr_scope.sh --pr "$PR" --version "$TARGET"'</automated>
  </verify>
  <done>The canonical ledger records one real combined Release Please PR and one target version greater than `1.3.0`, and the three-package PR scope verifier passes for that exact pair.</done>
</task>

<task type="auto">
  <name>Task 3: Capture REL-03 canonical proof or recovery state in the Phase 159 ledger</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</files>
  <read_first>
.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
scripts/ci/capture_linked_release_proof.sh
scripts/ci/accrue_host_hex_smoke.sh
scripts/ci/verify_release_notes_contract.sh
.github/workflows/release-please.yml
RELEASING.md
.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-CONTEXT.md
.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-RESEARCH.md
  </read_first>
  <acceptance_criteria>
- The newest successful proof block in `159-VERIFICATION.md` contains non-placeholder `PR_NUMBER:`, `TARGET_VERSION:`, `RUN_ID:`, workflow job ordering, tag rows, GitHub release rows, Hex API rows, release snapshot rows, and HexDocs rows for `accrue`, `accrue_admin`, and `accrue_portal`.
- The same dated block records the exact outcomes of `bash scripts/ci/accrue_host_hex_smoke.sh` and `bash scripts/ci/verify_release_notes_contract.sh` for the recorded `TARGET_VERSION`.
- If the publish run fails after any package is public, the newest block is a structured recovery block with the exact lowercase field names from Task 1 and it appears before any retry, revert, retire, or forward-fix action.
- Manual public-surface audit notes appear only when D-16 triggers; routine success does not invent extra checklist ceremony.
  </acceptance_criteria>
  <action>Per D-05, D-10, D-11, D-14, D-15, and D-16, finish the canonical ledger before any mirror reconciliation. After the combined release PR merges, capture the successful Release Please `RUN_ID` and append canonical proof with `bash scripts/ci/capture_linked_release_proof.sh --version <TARGET_VERSION> --run-id <RUN_ID> --pr <PR_NUMBER> --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md`. Record the same-version outcomes of `bash scripts/ci/accrue_host_hex_smoke.sh` and `bash scripts/ci/verify_release_notes_contract.sh` in that block. If the publish chain fails after any package is public, use the recovery append path from Task 1 to record exact public state and next command in the canonical ledger before retrying; only mark REL-03 complete after one final proof block succeeds.</action>
  <verify>
    <automated>bash -lc 'LEDGER=.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md; PR=$(sed -n "s/^PR_NUMBER: //p" "$LEDGER" | tail -n1); TARGET=$(sed -n "s/^TARGET_VERSION: //p" "$LEDGER" | tail -n1); RUN=$(sed -n "s/^RUN_ID: //p" "$LEDGER" | tail -n1); test -n "$PR" &amp;&amp; test -n "$TARGET" &amp;&amp; test -n "$RUN" &amp;&amp; bash scripts/ci/capture_linked_release_proof.sh --version "$TARGET" --run-id "$RUN" --pr "$PR" --output /tmp/162-linked-release-proof-check.md &amp;&amp; bash scripts/ci/accrue_host_hex_smoke.sh &amp;&amp; bash scripts/ci/verify_release_notes_contract.sh'</automated>
  </verify>
  <done>The Phase 159 ledger contains either a successful canonical proof block for one real post-`1.3.0` line or a structured recovery block that honestly preserves partial publish state before any retry path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer CLI -> GitHub API | Untrusted network/API data is pulled into release proof and release-PR decisions. |
| GitHub Actions -> Phase 159 ledger | CI emits proof inputs that become canonical release evidence. |
| GitHub/Hex public state -> proof script | Registry and release surfaces must agree with the recorded version/run identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | Tampering | `159-VERIFICATION.md` proof rows | mitigate | Only append script-derived rows bound to one `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID`; forbid placeholder or mirror-only evidence per D-01, D-13, and D-14. |
| T-162-02 | Repudiation | release identity / requirement closure | mitigate | Run `verify_release_pr_scope.sh` for the exact PR/version pair and record the same identifiers in the ledger before any REL closure. |
| T-162-03 | Tampering | split proof authority | mitigate | Keep Phase 159 as canonical ledger and do not create a second proof sink in workflow or scripts. |
| T-162-04 | Repudiation | premature REL-01 / REL-03 closure | mitigate | Require successful canonical proof or recovery record plus same-version corroboration before any mirror or checkbox updates. |
| T-162-05 | Information Disclosure | GitHub / Hex publish credentials | mitigate | Use existing GitHub Actions secrets and `gh` auth context only; never echo tokens or write secrets into the ledger, workflow summary, or docs. |
| T-162-SC | Tampering | package-manager installs | accept | No npm/pip/cargo install task is introduced in this plan. |
</threat_model>

<verification>
- `bash -n scripts/ci/capture_linked_release_proof.sh`
- `actionlint .github/workflows/release-please.yml`
- `bash -lc 'LEDGER=.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md; PR=$(sed -n "s/^PR_NUMBER: //p" "$LEDGER" | tail -n1); TARGET=$(sed -n "s/^TARGET_VERSION: //p" "$LEDGER" | tail -n1); test -n "$PR" && test -n "$TARGET" && bash scripts/ci/verify_release_pr_scope.sh --pr "$PR" --version "$TARGET"'`
- `bash -lc 'LEDGER=.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md; PR=$(sed -n "s/^PR_NUMBER: //p" "$LEDGER" | tail -n1); TARGET=$(sed -n "s/^TARGET_VERSION: //p" "$LEDGER" | tail -n1); RUN=$(sed -n "s/^RUN_ID: //p" "$LEDGER" | tail -n1); test -n "$RUN" && bash scripts/ci/capture_linked_release_proof.sh --version "$TARGET" --run-id "$RUN" --pr "$PR" --output /tmp/162-linked-proof-check.md && bash scripts/ci/accrue_host_hex_smoke.sh && bash scripts/ci/verify_release_notes_contract.sh'`
</verification>

<success_criteria>
- A real combined Release Please PR and target version greater than `1.3.0` are recorded in the Phase 159 ledger.
- The same ledger contains either a successful linked proof block with real `RUN_ID` evidence or a structured partial-publish recovery block recorded before retry action.
- Phase 159 remains the only proof authority for release evidence and recovery state.
</success_criteria>

<output>
Create `.planning/phases/162-close-gap-rel-01-rel-03-linked-release-proof/162-01-SUMMARY.md` when done
</output>

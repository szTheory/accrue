---
phase: 166-adoption-dx-docs
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - examples/accrue_host/README.md
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "D-11: Add the compact proof ladder after the happy path: Explore, Focused proof, Full local gate, CI wrapper, Maintainer contracts, Provider parity."
    - "D-12: Keep existing command names; do not rename `mix verify`, `mix verify.full`, or `bash scripts/ci/accrue_host_uat.sh`."
    - "D-13: Define command claims precisely for Docker explore, focused proof, full local gate, CI wrapper, maintainer contracts, and provider parity."
    - "D-15: Link deeper docs by user intent: First Hour, Production readiness, Adoption proof matrix, and `scripts/ci/README.md`."
    - "D-17: Preserve proof-lane depth below Start Here and reorganize it so readers can choose confidence level without confusing first-run setup with maintainer CI evidence."
    - The README proof ladder distinguishes first-run exploration from focused proof, full local gate, CI wrapper, maintainer contracts, and provider parity.
    - Existing command names remain unchanged.
    - New load-bearing Start Here claims are protected by the docs contract gate.
  artifacts:
    - path: examples/accrue_host/README.md
      provides: Reorganized proof ladder and downstream docs handoffs
    - path: scripts/ci/verify_package_docs.sh
      provides: Shift-left needles for the new Start Here/Docker contract
  key_links:
    - from: scripts/ci/verify_package_docs.sh
      to: examples/accrue_host/README.md
      via: Start Here and Docker command substring checks
    - from: examples/accrue_host/README.md
      to: scripts/ci/README.md
      via: Maintainer contract handoff
---

<objective>
Reorganize proof-depth documentation below Start Here and pin the new load-bearing adoption contract.

Purpose: Phase 166 should not stop at a top-section rewrite. The rest of the README must help evaluators choose the right confidence level without mixing first-run setup, maintainer CI, and provider parity.
Output: A compact proof ladder in the host README and updated doc verifier needles for `## Start Here`, Docker, and localhost.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/166-adoption-dx-docs/166-CONTEXT.md
@.planning/phases/166-adoption-dx-docs/166-RESEARCH.md
@.planning/phases/166-adoption-dx-docs/166-PATTERNS.md
@.planning/phases/166-adoption-dx-docs/166-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a compact proof ladder below the happy path</name>
  <files>examples/accrue_host/README.md</files>
  <read_first>
    - examples/accrue_host/README.md
    - examples/accrue_host/mix.exs
    - examples/accrue_host/demo/command_manifest.exs
    - scripts/ci/README.md
    - examples/accrue_host/docs/adoption-proof-matrix.md
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Reorganize the README proof-depth explanation into a compact ladder after the Start Here/happy path, using these exact ladder labels or close equivalents:
    Explore, Focused proof, Full local gate, CI wrapper, Maintainer contracts, Provider parity.
    Keep existing command names unchanged:
    `docker compose up --build`, `mix verify`, `mix verify.full`, `bash scripts/ci/accrue_host_uat.sh`, `scripts/ci/README.md`, and live Stripe parity.
    Define command claims precisely:
    `docker compose up --build` explores the demo app and Postgres locally.
    `mix verify` is the focused Fake-backed proof.
    `mix verify.full` is the CI-equivalent local host gate.
    `bash scripts/ci/accrue_host_uat.sh` is the repo-root CI wrapper used by `host-integration`.
    `scripts/ci/README.md` owns maintainer triage and support-contract bundle membership.
    Live Stripe parity is scheduled/manual provider drift detection, not required for local evaluation.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/README.md` contains `Explore`.
    - `examples/accrue_host/README.md` contains `Focused proof`.
    - `examples/accrue_host/README.md` contains `Full local gate`.
    - `examples/accrue_host/README.md` contains `CI wrapper`.
    - `examples/accrue_host/README.md` contains `Maintainer contracts`.
    - `examples/accrue_host/README.md` contains `Provider parity`.
    - `examples/accrue_host/README.md` still contains `## Proof and verification`.
    - `examples/accrue_host/README.md` still contains `### Verification modes`.
    - `examples/accrue_host/README.md` does not contain `Stripe-only`.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq 'Focused proof' examples/accrue_host/README.md && grep -Fq 'Full local gate' examples/accrue_host/README.md && grep -Fq 'Provider parity' examples/accrue_host/README.md</automated>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>The README separates first-run exploration from progressively deeper proof lanes.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the new Start Here contract in docs verification</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh
    - scripts/ci/verify_verify01_readme_contract.sh
    - scripts/ci/README.md
    - examples/accrue_host/README.md
  </read_first>
  <action>
    Update `scripts/ci/verify_package_docs.sh` host README needles so the new Start Here contract is load-bearing.
    Add `require_fixed "$ROOT_DIR/examples/accrue_host/README.md"` checks for these exact substrings:
    `## Start Here`
    `docker compose up --build`
    `http://localhost:4000`
    `Accrue.Processor.Fake`
    Do not add inline docs-contracts-shift-left bundle inventory to the README. Leave `verify_verify01_readme_contract.sh` unchanged unless Task 1 moved an existing VERIFY-01 pinned substring.
  </action>
  <acceptance_criteria>
    - `scripts/ci/verify_package_docs.sh` contains `## Start Here`.
    - `scripts/ci/verify_package_docs.sh` contains `docker compose up --build`.
    - `scripts/ci/verify_package_docs.sh` contains `http://localhost:4000`.
    - `scripts/ci/verify_package_docs.sh` contains `Accrue.Processor.Fake`.
    - `scripts/ci/verify_verify01_readme_contract.sh` still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>The new Start Here and Docker-first claims are protected by the package docs verifier.</done>
</task>

<task type="auto">
  <name>Task 3: Run the docs and adoption contract bundle</name>
  <files>examples/accrue_host/README.md, scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - examples/accrue_host/README.md
    - scripts/ci/verify_package_docs.sh
    - scripts/ci/verify_verify01_readme_contract.sh
    - scripts/ci/verify_adoption_proof_matrix.sh
    - scripts/ci/README.md
  </read_first>
  <action>
    Run the docs contract checks that protect host README and adoption-proof wording.
    If `verify_adoption_proof_matrix.sh` fails because README wording changed without matrix changes, inspect the failure. Fix matrix/script only if Phase 166 intentionally changed proof taxonomy; otherwise keep matrix unchanged and fix README wording.
    Finish with `git diff --check` for the README and verifier script.
  </action>
  <acceptance_criteria>
    - `bash scripts/ci/verify_package_docs.sh` exits 0.
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits 0.
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits 0.
    - `git diff --check -- examples/accrue_host/README.md scripts/ci/verify_package_docs.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh</automated>
    <automated>git diff --check -- examples/accrue_host/README.md scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The docs and adoption contract bundle is green after Start Here and proof-ladder changes.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh`
- `bash scripts/ci/verify_verify01_readme_contract.sh`
- `bash scripts/ci/verify_adoption_proof_matrix.sh`
- `cd examples/accrue_host && MIX_ENV=test mix test test/demo/command_manifest_test.exs`
- `cd accrue && MIX_ENV=test mix test test/accrue/docs/canonical_demo_contract_test.exs test/accrue/docs/first_hour_guide_test.exs`
- `git diff --check -- examples/accrue_host/README.md scripts/ci/verify_package_docs.sh`
</verification>

<success_criteria>
- README proof depth is easy to scan without burying Start Here.
- The new Docker-first Start Here contract is shift-left protected.
- Existing VERIFY-01, adoption matrix, and package docs contracts remain green.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README proof claims -> CI reality | Documentation names commands and CI surfaces that must remain accurate. |
| Local deterministic proof -> live provider parity | The README must not imply live Stripe is part of everyday local evaluation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-06 | Repudiation | Proof ladder documentation | mitigate | Pin Start Here claims in `verify_package_docs.sh` and keep existing VERIFY-01 verifier green. |
| T-166-07 | Tampering | Proof taxonomy | mitigate | Run `verify_adoption_proof_matrix.sh` and avoid changing matrix taxonomy unless intentional. |
| T-166-08 | Information Disclosure | Provider parity wording | mitigate | State live Stripe parity is scheduled/manual drift detection and not required for Start Here. |
</threat_model>

---
phase: 174
plan: 04
type: execute
wave: 4
depends_on:
  - 174-03
  - 174-02
files_modified:
  - accrue_admin/test/accrue_admin/dev/component_registry_test.exs
autonomous: true
requirements:
  - DSY-03

must_haves:
  truths:
    - "ComponentRegistryTest renders the /dev/components page and asserts every registry variant appears by its variant class (ax_class substring)"
    - "ComponentRegistryTest asserts the registry ax_class set exactly matches Button component render outputs for all 4 variants including danger"
    - "Adding a 5th Button variant without a registry entry would cause the drift test to fail CI"
    - "The test file is at accrue_admin/test/accrue_admin/dev/component_registry_test.exs"
    - "The full admin test suite passes with --seed 0"
  artifacts:
    - path: "accrue_admin/test/accrue_admin/dev/component_registry_test.exs"
      provides: "Drift-prevention test for DSY-03"
      contains: "ComponentRegistryTest"
  key_links:
    - from: "ComponentRegistryTest test (a)"
      to: "component_kitchen_live.ex rendered HTML"
      via: "live/2 against /billing/dev/components"
      pattern: "live.*dev/components"
    - from: "ComponentRegistryTest test (b)"
      to: "AccrueAdmin.Dev.ComponentRegistry.variants_for/1"
      via: "render_component fn assigns comparison"
      pattern: "ComponentRegistry\\.variants_for"
---

<objective>
Write the ComponentRegistryTest drift-prevention test (DSY-03 D-21).

Two behaviors tested:
(a) Every registry variant appears in the /dev/components page render — uses live/2 to render the full LiveView and asserts each registry entry's variant class is present in the HTML output.
(b) The registry's ax_class set exactly matches the Button component's render outputs — uses render_component to render each known Button variant and compares the full class string against the registry.

Purpose: If a maintainer adds a 5th Button variant without adding a registry entry, test (b) fails. If the kitchen page fails to render a registry entry, test (a) fails. The test enforces D-21 going forward.

Output: accrue_admin/test/accrue_admin/dev/component_registry_test.exs, all tests green. Full admin suite green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md

<interfaces>
<!-- Test infrastructure (from RESEARCH.md §Validation Architecture and §Standard Stack): -->
<!-- Framework: ExUnit, Phoenix.LiveViewTest -->
<!-- AccrueAdmin.LiveCase: for live/2 tests requiring DB/sandbox (async: false) -->
<!-- Route: /billing/dev/components (router.ex line 89) -->
<!-- Session init pattern: conn = Phoenix.ConnTest.init_test_session(conn, admin_token: "admin") -->

<!-- From PATTERNS.md — analog tests: -->
<!-- display_components_test.exs: use ExUnit.Case, async: true + import Phoenix.LiveViewTest + render_component(fn assigns -> ~H"""...""" end) for components with required slots -->
<!-- component_kitchen_live_test.exs: use AccrueAdmin.LiveCase, async: false + init_test_session + live(conn, "/billing/dev/components") + assert html =~ -->

<!-- render_component with required inner_block slot (PATTERNS.md shared patterns): -->
<!-- html = render_component(fn assigns -> ~H""" <Button.button variant="primary" type="button">Label</Button.button> """ end) -->

<!-- Class extraction from rendered HTML (RESEARCH pitfall 5 — must normalize full class string): -->
<!-- The rendered HTML has class="ax-button ax-button-primary" -->
<!-- Regex.run(~r/class="([^"]+)"/, html) extracts the full class string -->
<!-- String.split(classes) |> Enum.join(" ") normalizes whitespace -->
<!-- Compare the full normalized string against registry entry.ax_class -->

<!-- IMPORTANT for test (b): render_component renders only the component in isolation -->
<!-- The Button component may render nested elements; ensure the regex captures the OUTER button/element class, not an inner child's class -->
<!-- Safer: after rendering, find the element with ax-button base class via regex -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ComponentRegistryTest drift-prevention test</name>
  <files>accrue_admin/test/accrue_admin/dev/component_registry_test.exs</files>
  <behavior>
    - Test (a) "every registry variant appears in the /dev/components page render": for each entry in ComponentRegistry.entries(), the rendered HTML from live/2 must contain the variant-specific CSS class (the second word in ax_class, e.g. "ax-button-primary" from "ax-button ax-button-primary"). Fails if any registry entry's variant class is absent from the rendered page.
    - Test (b) "button registry ax_class set exactly matches Button component render outputs": MapSet of registry ax_class strings for family "button" == MapSet of full class strings extracted from render_component for each variant in ["primary", "secondary", "ghost", "danger"]. Fails if registry has an entry the component doesn't render, or if the component renders a class the registry doesn't list.
  </behavior>
  <read_first>
    - accrue_admin/lib/accrue_admin/dev/component_registry.ex — read entries/0 and variants_for/1 (created in Plan 03 Task 1). Confirm the exact ax_class strings for each entry — these are what the tests compare against.
    - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex — read the render/1 function. Understand how variant reference rows are rendered and what CSS classes appear in the HTML output. Confirm the route is /billing/dev/components.
    - accrue_admin/test/accrue_admin/dev/component_kitchen_live_test.exs — read the full file (~15 lines). The session init pattern (Phoenix.ConnTest.init_test_session with admin_token: "admin") and the live/2 call are the exact patterns to reuse.
    - accrue_admin/test/accrue_admin/components/display_components_test.exs — read lines 1–70. The render_component(fn assigns -> ~H"""...""" end) pattern for components with required slots is the pattern to reuse for test (b).
    - accrue_admin/test/support/live_case.ex — read the module to understand what use AccrueAdmin.LiveCase, async: false provides (DataCase wrapper for DB sandbox).
    - accrue_admin/lib/accrue_admin/components/button.ex — read the component signature to confirm the attr names and the slot declaration. The render call in test (b) needs the correct attributes.
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md — Pattern 5 (ComponentRegistryTest module structure, both test shapes, class extraction helper).
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md — §Pattern 5, §Common Pitfalls 5 and 6 (class extraction normalization, danger variant required).
  </read_first>
  <action>
    Create accrue_admin/test/accrue_admin/dev/component_registry_test.exs.

    The directory accrue_admin/test/accrue_admin/dev/ may need to be created (use File.mkdir_p! or confirm the executor does so). The test file uses use AccrueAdmin.LiveCase, async: false (because test (a) needs DB sandbox for the LiveView mount; test (b) uses render_component which doesn't need it but sharing the case module is simpler than splitting).

    Test (a) — page render coverage:
    - Init test session with admin_token: "admin"
    - live(conn, "/billing/dev/components") — assert {:ok, _view, html}
    - For each entry in ComponentRegistry.entries(), split ax_class on " " to get the variant-specific class (the second element, e.g. "ax-button-primary"), assert html =~ variant_class with a clear error message including the full ax_class
    - This test catches: a family section not rendering, a variant rendering under the wrong class, a registry entry with a wrong ax_class value

    Test (b) — Button family drift detection (most critical drift vector per D-21):
    - Build registry_classes MapSet: ComponentRegistry.variants_for("button") |> MapSet.new(& &1.ax_class)
    - Build component_classes MapSet: for each variant in ["primary", "secondary", "ghost", "danger"], render the Button component using render_component(fn assigns -> ~H"""<Button.button variant={...} type="button">Label</Button.button>""" end), extract the class attribute from the rendered HTML (the element with class containing "ax-button"), normalize to a single space-joined string, then collect into a MapSet
    - assert registry_classes == component_classes — if a 5th variant is added to button_variant_class/1 and the registry is not updated, this fails
    - CRITICAL (pitfall 5): the class extraction regex must find the ax-button bearing element, not a child element's class. Use ~r/class="(ax-button[^"]+)"/ or equivalent to target the button's own class attribute specifically.

    Private helper: defp extract_button_class(html), which applies the targeted regex and returns the normalized class string. On match failure, call flunk/1 with the raw html for debugging.

    Module alias imports: alias AccrueAdmin.Dev.ComponentRegistry, alias AccrueAdmin.Components.Button. Import: import Phoenix.LiveViewTest.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at accrue_admin/test/accrue_admin/dev/component_registry_test.exs.
    - cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs exits 0 with both tests green.
    - Test (a) passes: every registry entry's variant class found in /dev/components page HTML.
    - Test (b) passes: registry Button ax_class set == component render outputs MapSet (all 4 variants including danger).
    - grep -c 'danger' accrue_admin/test/accrue_admin/dev/component_registry_test.exs returns >= 1 (danger variant explicitly tested).
    - cd accrue_admin && mix test --seed 0 exits 0 (full admin suite green including the new test; flaky PdfTest may need --seed 0).
  </acceptance_criteria>
  <done>ComponentRegistryTest is green for both drift behaviors. Test (a) confirms all registry variants appear on the /dev/components page. Test (b) confirms registry ax_class set exactly matches Button component render outputs, including the danger variant. A future fifth Button variant without a registry entry will fail test (b) in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComponentRegistryTest → AccrueAdmin.LiveCase (DB sandbox) | Test-only; uses standard Ecto sandbox. No production data, no production auth path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-06 | Tampering | ComponentRegistryTest — rendered HTML class extraction | accept | The test uses Regex to extract class attributes from rendered HTML. The risk of false-positive matching (capturing a child element's class instead of the button's) is mitigated by using a targeted regex ~r/class="(ax-button[^"]+)"/ that anchors on the ax-button base class. The extract_button_class/1 private helper calls flunk/1 on no-match so silent false-positives are impossible. |
| T-174-SC | Tampering | npm/pip/cargo installs | accept | No new packages. ExUnit and Phoenix.LiveViewTest are already in the project. |
</threat_model>

<verification>
Phase-gate verification (run before /gsd:verify-work):
1. cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs — exits 0, both tests green
2. cd accrue_admin && mix test --seed 0 — full admin suite green (flaky PdfTest: use --seed 0 to dodge known flake)
3. bash scripts/ci/verify_package_docs.sh — exits 0
4. grep -E 'line-height: [0-9]\.[0-9]' accrue_admin/assets/css/app.css — ZERO results
5. grep -E 'letter-spacing: -?[0-9.]+em' accrue_admin/assets/css/app.css — ZERO results
6. grep -E '@media \((min|max)-width: [0-9.]+px\)' accrue_admin/assets/css/app.css | grep -v '\-\-ax-bp-' — ZERO results
7. grep -rn 'style=' accrue_admin/lib/accrue_admin/components/dunning_banner.ex — ZERO results
8. cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs — exits 0
</verification>

<success_criteria>
ComponentRegistryTest has two green tests: (a) live/2 page render confirms all registry variants appear at /dev/components, (b) render_component comparison confirms the registry Button ax_class set exactly matches component render outputs for all 4 variants. The full admin suite (mix test --seed 0) is green. All three phase success criteria (DSY-01/02/03) are verifiable via the grep guards and test suite above.
</success_criteria>

<output>
Create .planning/phases/174-a-design-system-gap-closure-token-completeness/174-04-SUMMARY.md when done.
</output>

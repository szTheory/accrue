---
phase: 175
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/test/accrue_admin/live/event_live_test.exs
  - accrue_admin/test/accrue_admin/router_test.exs
  - accrue_admin/lib/accrue_admin/queries/invoices.ex
  - accrue_admin/lib/accrue_admin/queries/subscriptions.ex
  - accrue_admin/lib/accrue_admin/queries/charges.ex
autonomous: true
requirements:
  - IA-03
  - IA-04
  - IA-06

must_haves:
  truths:
    - "InvoicesLive can filter by multiple statuses (open,uncollectible) without crashing"
    - "SubscriptionsLive can filter by multiple statuses (past_due,canceling) without crashing"
    - "ChargesLive can filter by status string (failed) unchanged or via same multi-value path"
    - "GET /charges returns 302 redirect to /payments (test asserts, code may not exist yet)"
    - "EventLive test file exists as scaffolding ready for Wave 3"
  artifacts:
    - path: "accrue_admin/test/accrue_admin/live/event_live_test.exs"
      provides: "Scaffold test for EventLive — IA-04 coverage (failing with NotFoundError until Wave 3 ships)"
    - path: "accrue_admin/lib/accrue_admin/queries/invoices.ex"
      provides: "Multi-status filter support: comma-separated status string split into OR clause"
    - path: "accrue_admin/lib/accrue_admin/queries/subscriptions.ex"
      provides: "Multi-status filter support: comma-separated status string dispatched to existing named clauses"
  key_links:
    - from: "invoices_live.ex handle_params default push_patch"
      to: "Queries.Invoices.list/1 :status filter"
      via: "decode_filter → filter_query → :status key"
      pattern: "filter_status.*status"
---

<objective>
Wave 0: Resolve the two open questions from RESEARCH.md before any list-screen work begins.

1. Extend query modules (Invoices, Subscriptions, Charges) to support multi-value status filtering via comma-separated strings (e.g. "open,uncollectible"). The current single-atom status clause crashes on a comma-joined value.

2. Scaffold the Wave-0 test files that RESEARCH.md flagged as missing: event_live_test.exs (for IA-04 EventLive) and router_test.exs redirect assertions (for IA-06).

Anti-churn justification: persona-job (Finance Ops/Recovery) requires multi-status queues; without this fix, work-queue defaults in Wave 2 would produce ArgumentError at runtime.

Purpose: Unblock Waves 2 and 3; prevent test failures from accumulating during execution.
Output: Extended query modules + two test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/175-b-persona-driven-ia-spine/175-CONTEXT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md
@.planning/phases/175-b-persona-driven-ia-spine/175-VALIDATION.md
</context>

<interfaces>
<!-- Queries.Behaviour.normalize_string/1 returns nil or a trimmed string. -->
<!-- Invoices filter_query {:status, status} clause (line 123):
     where(query, [invoice, _], invoice.status == ^String.to_existing_atom(status))
     PROBLEM: "open,uncollectible" → ArgumentError. Must split and use in clause. -->
<!-- Subscriptions filter_status/2 dispatch (lines 129-143):
     Dispatches single string to named Billing.Query.* functions.
     PROBLEM: "past_due,canceling" misses all named clauses → falls to catch-all with rescues ArgumentError.
     Must split on comma and apply each named clause with OR logic. -->
<!-- Charges {:status, status} clause (line 93):
     where(query, [charge, _], charge.status == ^status)
     Charge status is a plain string (not atom). Single value only.
     "failed" alone works; no multi-value needed for the charges queue.
     Extend for consistency (split+OR) but it is not strictly required for Wave 2. -->
<!-- Router @default_on_mount (router.ex line 9):
     @default_on_mount [{AccrueAdmin.AuthHook, :ensure_admin}]
     NavBadgeHook is added here in Wave 1. -->
<!-- Existing router_test.exs: already exists in accrue_admin/test/accrue_admin/router_test.exs -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend query modules for multi-status filtering</name>
  <files>
    accrue_admin/lib/accrue_admin/queries/invoices.ex,
    accrue_admin/lib/accrue_admin/queries/subscriptions.ex,
    accrue_admin/lib/accrue_admin/queries/charges.ex,
    accrue_admin/test/accrue_admin/queries/query_modules_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/queries/invoices.ex (full — filter_query {:status} clause on line 123)
    accrue_admin/lib/accrue_admin/queries/subscriptions.ex (full — filter_status/2 clauses lines 129-143)
    accrue_admin/lib/accrue_admin/queries/charges.ex (full — {:status} clause on line 93)
    accrue_admin/test/accrue_admin/queries/query_modules_test.exs (existing tests to extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md (§4 Pitfall 6 — multi-status)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (invoices_live.ex default_queue_status shape)
  </read_first>
  <behavior>
    - Test: Invoices.decode_filter(%{"status" => "open,uncollectible"}) returns %{status: "open,uncollectible"}
    - Test: Invoices.list(filter: %{status: "open,uncollectible"}) does not raise ArgumentError
    - Test: Subscriptions.decode_filter(%{"status" => "past_due,canceling"}) returns %{status: "past_due,canceling"}
    - Test: Subscriptions.list(filter: %{status: "past_due,canceling"}) does not raise
    - Test: Charges.list(filter: %{status: "failed"}) still works (single-value unchanged)
  </behavior>
  <action>
    In Invoices.filter_query, replace the {:status, status} clause with a helper that splits on "," and builds an OR query. When the string contains no comma, apply as before (single-atom). When comma-split produces multiple values, use Ecto.Query.where with dynamic OR: each split value is converted via String.to_existing_atom and combined with or_where. Wrap the whole clause in a rescue ArgumentError that returns the query unchanged (same as the existing rescue on line 136).

    In Subscriptions.filter_query, replace the {:status, status} dispatch with a helper that splits on "," and maps each value through the existing filter_status/2 named clauses (which already call Billing.Query.active/past_due/etc.). For multi-value, fold the results as `or_where` fragments. Single-value path remains unchanged (no regression).

    In Charges.filter_query, extend the {:status, status} clause to split on "," and apply an `in` query when multiple values are present, falling back to single equality. Charge status is a plain string, so no atom conversion needed.

    In query_modules_test.exs, add describe blocks for multi-status filter handling. Use existing test factory/fixture patterns from the file — do not import new helpers.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --seed 0 -x</automated>
  </verify>
  <done>All three query modules accept comma-separated status strings without raising. Tests green. Single-value paths still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold missing test files (event_live_test + router redirect assertions)</name>
  <files>
    accrue_admin/test/accrue_admin/live/event_live_test.exs,
    accrue_admin/test/accrue_admin/router_test.exs
  </files>
  <read_first>
    accrue_admin/test/accrue_admin/live/webhook_live_test.exs (analog for detail LiveView test structure)
    accrue_admin/test/accrue_admin/router_test.exs (existing file — add redirect tests)
    .planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md (§Validation Architecture, Wave 0 Gaps)
    .planning/phases/175-b-persona-driven-ia-spine/175-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
    Create accrue_admin/test/accrue_admin/live/event_live_test.exs as a scaffold with the module declaration, use AccrueAdminWeb.ConnCase (or Phoenix.LiveViewTest — match the pattern in webhook_live_test.exs), and two pending/skipped test stubs:
    (a) "GET /events/:id renders EventLive detail" — tagged @tag :pending; no implementation yet.
    (b) "EventLive Related card links to source webhook and affected entity" — tagged @tag :pending.
    The file must compile cleanly (no undefined references). Mark the module with @moduledoc false.

    In the existing router_test.exs, add a describe "/charges redirects" block with two assertions:
    (a) GET /billing/charges redirects 302 to /billing/payments — assert conn.status == 302 and get_resp_header(conn, "location") contains "/payments".
    (b) GET /billing/charges/:id redirects 302 to /billing/payments/:id — same pattern.
    These tests will FAIL until Wave 2 ships the redirect controller + router changes. Tag them @tag :pending or use assert_raise/ExUnit.Assertions.flunk with a clear message that this unblocks Wave 2 verification. Do NOT skip them permanently — they are the Wave 2 gate.

    NOTE: the redirect tests may fail at route resolution before Wave 2. Use @tag :pending so the scaffold compiles and is ready. Wave 2 plan removes the :pending tag.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/event_live_test.exs test/accrue_admin/router_test.exs --seed 0 2>&1 | grep -E "passed|pending|excluded"</automated>
  </verify>
  <done>Both test files compile cleanly. event_live_test.exs has 2 pending stubs. router_test.exs has 2 pending redirect assertions. Existing router tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query filter input | `status` param from URL query string passed through decode_filter → filter_query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-01-01 | Tampering | Invoices/Subscriptions filter_query :status | mitigate | Comma-split values are each wrapped in String.to_existing_atom + rescue ArgumentError; no user-controlled SQL injection possible via Ecto parameterized queries |
| T-175-01-02 | Information Disclosure | Multi-status query returning wrong-owner records | accept | Scope filters (scope_query/2) are applied before filter_query in the query chain; multi-status change only affects the WHERE clause, not the owner scope |
| T-175-01-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs test/accrue_admin/live/event_live_test.exs test/accrue_admin/router_test.exs --seed 0
- All existing query tests still pass
- event_live_test.exs compiles and has pending stubs (not compile errors)
</verification>

<success_criteria>
Invoices, Subscriptions, and Charges query modules accept comma-separated status values without raising. Two test scaffolds exist and compile cleanly. Wave 2 (work-queue defaults) can now be implemented with confidence.
</success_criteria>

<output>
Create `.planning/phases/175-b-persona-driven-ia-spine/175-01-SUMMARY.md` when done.
</output>

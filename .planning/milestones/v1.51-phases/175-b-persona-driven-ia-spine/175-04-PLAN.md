---
phase: 175
plan: 04
type: execute
wave: 2
depends_on:
  - "175-01"
  - "175-02"
files_modified:
  - accrue_admin/lib/accrue_admin/live/invoices_live.ex
  - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
  - accrue_admin/lib/accrue_admin/live/charges_live.ex
  - accrue_admin/lib/accrue_admin/live/dashboard_live.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/accrue_admin.css
autonomous: true
requirements:
  - IA-01
  - IA-03

must_haves:
  truths:
    - "InvoicesLive push_patches to ?status=open,uncollectible on bare navigation"
    - "?view=all sentinel prevents re-redirect loop on invoices"
    - "SubscriptionsLive push_patches to ?status=past_due,canceling on bare navigation"
    - "ChargesLive push_patches to ?status=failed on bare navigation"
    - "Each list screen shows a work-queue chip (cobalt) and an All chip (slate) via filter_chip_bar"
    - "Home visible search field renders (not hotkey-only) per IA-01"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/invoices_live.ex"
      provides: "3-clause handle_params with push_patch default + view=all sentinel + work_queue_chips"
    - path: "accrue_admin/lib/accrue_admin/live/subscriptions_live.ex"
      provides: "Same pattern, default status past_due,canceling"
    - path: "accrue_admin/lib/accrue_admin/live/charges_live.ex"
      provides: "Same pattern, default status failed; table_path updated to /payments"
    - path: "accrue_admin/lib/accrue_admin/live/dashboard_live.ex"
      provides: "Visible search field added to Home search zone (above launchers)"
  key_links:
    - from: "invoices_live.ex handle_params"
      to: "Queries.Invoices.list/1 :status filter"
      via: "push_patch → URL ?status=open,uncollectible → DataTable decode_filter → filter_query"
      pattern: "push_patch.*table_path"
---

<objective>
Wave 2b (parallel with Plan 03): Apply work-queue defaults to three list screens and add the visible search field to Home.

Each list screen gets:
- 3-clause handle_params: %{"view" => "all"} guard / empty-params default push_patch / passthrough
- FilterChipBar with cobalt persona-queue chip + slate "All" chip
- table_path updated in charges_live.ex to "/payments"

Home (dashboard_live.ex) gets:
- Visible search <button> (styled as ax-input) in the search zone above launchers
- phx-click="open" phx-target="#global-search" so it opens the command palette

Customers list: no queue default (CONTEXT.md: "customers → all").
Events list: compliance actor-lens chip (Plan 06 / Wave 3).

Anti-churn justification: persona-job — Finance Ops needs a queue not a raw list (IA-03); Support entry is hidden behind hotkey only (IA-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-CONTEXT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md
@.planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md
@.planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md
</context>

<interfaces>
<!-- invoices_live.ex handle_params current (lines 28-30):
     def handle_params(params, _uri, socket) do
       {:noreply, assign(socket, :params, params)}
     end
     mount assigns: :table_path = admin_path(admin, "/invoices") — used to build push_patch URL -->

<!-- subscriptions_live.ex: same pattern; table_path = admin_path(admin, "/subscriptions") (line 154) -->

<!-- charges_live.ex: same pattern; table_path = admin_path(admin, "/charges") → UPDATE to "/payments" -->

<!-- filter_chip_bar chip API (from PATTERNS.md):
     %{id: atom, label: string, value: string|nil, tone: :cobalt|:slate|..., active: bool, remove_href: string|nil}
     Chip renders only when active: true (default true).
     Clear link renders only when remove_href is set.
     Queue chip: tone: :cobalt, active: queue_active?, remove_href: table_path <> "?view=all"
     All chip: tone: :slate, active: all_active?, remove_href: table_path -->

<!-- handle_params build_default_params must preserve org= param when OwnerScope mode is :organization.
     Use pattern: %{mode: :organization, organization_slug: slug} → merge %{"org" => slug} -->

<!-- dashboard_live.ex GlobalSearch is mounted as a LiveComponent with id="global-search" in app_shell.
     Topbar already has a .ax-search-trigger button with phx-click="open" phx-target="#global-search".
     The visible Home search field is a SECOND trigger — a styled <button> with role="search" and aria-label="Search"
     using the ax-input ax-input-search CSS classes (add if missing). -->

<!-- UI-SPEC §6 Visible labeled global-search field:
     Home field: ax-input, min-height: 2.75rem, --ax-radius-md, leading :search icon at --ax-icon-md.
     Placeholder text: "Search customers, invoices… ⌘K" (exact per Copywriting Contract). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Work-queue default filters on invoices, subscriptions, and charges list screens</name>
  <files>
    accrue_admin/lib/accrue_admin/live/invoices_live.ex,
    accrue_admin/lib/accrue_admin/live/subscriptions_live.ex,
    accrue_admin/lib/accrue_admin/live/charges_live.ex,
    accrue_admin/test/accrue_admin/live/invoices_live_test.exs,
    accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs,
    accrue_admin/test/accrue_admin/live/charges_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/invoices_live.ex (full — mount, handle_params, render, table_path)
    accrue_admin/lib/accrue_admin/live/subscriptions_live.ex (full — same pattern)
    accrue_admin/lib/accrue_admin/live/charges_live.ex (full — same pattern; table_path uses /charges today)
    accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex (full — chip API contract)
    accrue_admin/test/accrue_admin/live/invoices_live_test.exs (full — existing tests to preserve + extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (invoices_live.ex section — handle_params + work_queue_chips)
    .planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md (§3 Work-queue default + "All" escape hatch, Copywriting empty states)
  </read_first>
  <behavior>
    - Test (invoices): bare navigation to /invoices redirects to /invoices?status=open,uncollectible
    - Test (invoices): navigation to /invoices?view=all assigns params without push_patch (no loop)
    - Test (invoices): FilterChipBar renders with queue chip (cobalt) and All chip (slate)
    - Test (subscriptions): bare navigation to /subscriptions redirects to /subscriptions?status=past_due,canceling
    - Test (charges): bare navigation to /charges or /payments redirects to /payments?status=failed
  </behavior>
  <action>
    In each of invoices_live.ex, subscriptions_live.ex, and charges_live.ex:

    1. Add @default_queue_status module attribute with the correct value:
       - invoices_live.ex: "open,uncollectible"
       - subscriptions_live.ex: "past_due,canceling"
       - charges_live.ex: "failed"

    2. Replace single-clause handle_params with 3-clause pattern per PATTERNS.md §invoices_live.ex:
       - Clause 1: %{"view" => "all"} = params → assign params, no push_patch
       - Clause 2: params when map_size(params) == 0 → build default params (with org if scoped) and push_patch to table_path <> "?" <> URI.encode_query(default_params)
       - Clause 3: passthrough → assign params

    3. Add a private work_queue_chips/2 function taking (params, table_path) and returning two chips:
       - Queue chip: id: :status_queue, label: "Queue", value: display label (e.g. "open · uncollectible"), tone: :cobalt, active: status == @default_queue_status, remove_href: table_path <> "?view=all"
       - All chip: id: :view_all, label: "All", tone: :slate, active: Map.get(params, "view") == "all", remove_href: table_path

    4. In the render, add <FilterChipBar.filter_chip_bar items={work_queue_chips(@params, @table_path)} label="Work queue" /> above the DataTable component. Import FilterChipBar if not already imported.

    5. In charges_live.ex, update table_path assignment in mount from admin_path(admin, "/charges") to admin_path(admin, "/payments"). This ensures push_patch uses /payments and the All chip points to /payments.

    For the org-scoped push_patch in handle_params Clause 2: check socket.assigns[:current_owner_scope] for %{mode: :organization, organization_slug: slug}. If present, merge %{"org" => slug} into the default_params map.

    Add/extend test assertions per the behavior block. Use existing test setup patterns from each test file — do not add new fixtures.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs test/accrue_admin/live/subscriptions_live_test.exs test/accrue_admin/live/charges_live_test.exs --seed 0 -x</automated>
  </verify>
  <done>All three list screens push_patch to default queue on bare load. view=all sentinel prevents loop. Chips render in each list. charges table_path uses /payments. Tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Visible search field on Home (dashboard_live.ex)</name>
  <files>
    accrue_admin/lib/accrue_admin/live/dashboard_live.ex,
    accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/dashboard_live.ex (full — render, launcher section structure)
    accrue_admin/lib/accrue_admin/components/topbar.ex (lines 26-35 — existing search trigger pattern to replicate on Home)
    accrue_admin/assets/css/app.css (grep for .ax-input, .ax-search-trigger, .ax-launcher to understand the Home layout)
    accrue_admin/test/accrue_admin/live/dashboard_live_test.exs (full — existing tests + verb-relabel assertions to add)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (dashboard_live.ex section — Home visible search HEEx sketch)
    .planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md (§6 Visible labeled global-search field, §7 Home verb-relabeled task launchers)
  </read_first>
  <action>
    In dashboard_live.ex render, locate the launchers section (the 4 .ax-launcher tiles). Add a visible search field block ABOVE the launchers (or in the home search zone if one exists — check the template structure). The field is NOT a real <input>; it is a <button> styled as a search input to avoid LiveComponent event-targeting complexity (RESEARCH.md §8 footgun). Use:

    A <div class="ax-home-search"> wrapper around a <button type="button" role="search" aria-label="Search" class="ax-input ax-input-search" phx-click="open" phx-target="#global-search">. Inside the button: Icon.icon name={:search} size="md" class="ax-input-icon", then a <span class="ax-input-placeholder"> with text "Search customers, invoices… ⌘K" (exact text per UI-SPEC Copywriting Contract).

    If the CSS classes ax-home-search or ax-input-search do not yet exist in app.css, add minimal CSS: .ax-home-search margin-bottom using --ax-space-md; .ax-input-search full-width (width: 100%), min-height matching UI-SPEC (2.75rem), border: 1px solid var(--ax-border), border-radius var(--ax-radius-md), background var(--ax-elevated), display flex, align-items center, gap var(--ax-space-sm), padding-inline var(--ax-space-md), cursor pointer. No literal values.

    Anti-churn: Do NOT touch the attention rail, KPI strip, or recent-activity zones (CONTEXT.md anti-churn rule). Only add the search field in the launcher/search area.

    In dashboard_live_test.exs, add assertions:
    - The render contains "Look up a customer" (new verb string from Plan 02)
    - The render contains "Clear the invoice queue"
    - The render contains a role="search" element or text "Search customers, invoices"

    If CSS was added: run `cd accrue_admin && mix accrue_admin.assets.build` and commit priv/static.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs --seed 0 -x</automated>
  </verify>
  <done>Home page renders a visible search field with correct aria attributes and placeholder text. Verb relabels verified by test assertions. Dashboard tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query param ?status= | User-controlled string flowing into handle_params → push_patch |
| URL query param ?view=all | User-controlled sentinel; must not bypass auth or data scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-04-01 | Tampering | push_patch URL construction with user-controlled params | mitigate | Default params use only @default_queue_status module attribute (not user input); org slug comes from server-side OwnerScope assign, not from URL params directly |
| T-175-04-02 | Information Disclosure | IDOR via ?status= showing cross-org records | accept | scope_query/2 in each query module applies owner_scope filter before the status filter; multi-status extension does not change scope behavior |
| T-175-04-03 | Tampering | Home search button firing open event to GlobalSearch | accept | Event targets the named LiveComponent (#global-search); no data is passed; GlobalSearch enforces its own search authorization |
| T-175-04-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs test/accrue_admin/live/subscriptions_live_test.exs test/accrue_admin/live/charges_live_test.exs test/accrue_admin/live/dashboard_live_test.exs --seed 0
- Verify charges table_path is /payments (grep accrue_admin/lib/accrue_admin/live/charges_live.ex for "/payments")
- Verify no literal px/hex in new CSS additions
- If CSS added: priv/static committed
</verification>

<success_criteria>
Finance Ops opens /invoices and sees the open/uncollectible queue immediately. One click on "All" chip shows all invoices. Same for subscriptions and payments. Home has a tappable visible search field. Tests green.
</success_criteria>

<output>
Create `.planning/phases/175-b-persona-driven-ia-spine/175-04-SUMMARY.md` when done.
</output>

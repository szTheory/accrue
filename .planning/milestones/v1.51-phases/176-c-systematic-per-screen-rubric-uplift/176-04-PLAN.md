---
phase: 176-c-systematic-per-screen-rubric-uplift
plan: 04
type: execute
wave: 3
depends_on:
  - 176-01
  - 176-02
files_modified:
  - accrue_admin/lib/accrue_admin/live/promotion_code_live.ex
  - accrue_admin/lib/accrue_admin/live/connect_account_live.ex
  - accrue_admin/lib/accrue_admin/live/webhook_live.ex
  - accrue_admin/test/accrue_admin/live/promotion_code_live_test.exs
  - accrue_admin/test/accrue_admin/live/connect_account_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04

must_haves:
  truths:
    - "promotion_code_live imports Detail and uses Detail.summary_card + Detail.detail_section"
    - "connect_account_live prose description at line ~145 has ax-measure applied"
    - "connect_account_live phx-submit and data-role='save-override' are preserved after any structural change"
    - "webhook_live forensic payload section uses Detail.detail_section + Detail.detail_field_list instead of hand-rolled ax-card"
    - "webhook_live Activity-feed prose link paragraph has ax-measure"
    - "mix test --seed 0 stays at 227+ green"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/promotion_code_live.ex"
      provides: "Detail-component-based hero and parent-coupon sections"
      contains: "Detail.detail_section"
    - path: "accrue_admin/lib/accrue_admin/live/webhook_live.ex"
      provides: "Detail.detail_section forensic payload section with ax-measure prose"
      contains: "ax-measure"
    - path: "accrue_admin/lib/accrue_admin/live/connect_account_live.ex"
      provides: "ax-measure applied to platform-fee description prose"
      contains: "ax-measure"
  key_links:
    - from: "promotion_code_live.ex hero section"
      to: "Detail.summary_card"
      via: "eyebrow + title + :facts slot"
      pattern: "summary_card"
    - from: "webhook_live.ex forensic payload section"
      to: "Detail.detail_section + Detail.detail_field_list"
      via: "title='Stored raw payload and metadata'"
      pattern: "detail_section"
---

<objective>
Wave 2b — catalog/specialist detail DRY uplift, part 2 (parallel with Plan 03). Lifts three more tail screens: promotion_code_live (same Detail-missing gaps as coupon_live), connect_account_live (needs .ax-measure on the existing prose paragraph; nested grid overflow check), and webhook_live (forensic payload section is the last hand-rolled ax-card in an otherwise well-structured screen).

Purpose: These three screens can be worked in parallel with event_live/coupon_live (Plan 03) since they touch different files. Together Plans 03 and 04 complete Wave 2's catalog/specialist tail.

Output: promotion_code_live with Detail components; connect_account_live with ax-measure on prose; webhook_live forensic section using Detail primitives. SCORECARD Wave 2b after-scores updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-CONTEXT.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-RESEARCH.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md

<interfaces>
<!-- promotion_code_live CURRENT state (from PATTERNS.md §promotion_code_live): -->
<!--   line 8: alias WITHOUT Detail (AppShell, Breadcrumbs, JsonViewer, KpiCard, RelatedResources) -->
<!--   lines 43–56: hand-rolled <header class="ax-page-header"> (no summary_card) -->
<!--   lines 72–87: hand-rolled <section class="ax-card"> for parent coupon link -->
<!--   Same gaps as coupon_live; same fix pattern. -->

<!-- connect_account_live CURRENT state (from PATTERNS.md §connect_account_live): -->
<!--   Already uses Detail.detail_section + Detail.detail_field_list (lines 124–140) — GOOD -->
<!--   line ~143: Detail.detail_section for platform-fee form wraps at line 143 -->
<!--   line ~145: <p class="ax-body"> description prose WITHOUT ax-measure — FIX THIS -->
<!--   line ~149: <div class="ax-grid ax-grid-2"> nested inside detail_section — verify 360px OK -->
<!--   line ~212: data-role="save-override" button — MUST NOT be removed/changed -->
<!--   line ~148: phx-submit="save_override" — MUST NOT be removed/changed -->

<!-- webhook_live CURRENT forensic payload section (direct file inspection lines 215–239): -->
<!--   <section :if={@webhook} class="ax-card">  ← hand-rolled -->
<!--     <header class="ax-page-header"> -->
<!--       <p class="ax-eyebrow">Forensic payload</p> -->
<!--       <h3 class="ax-heading">Stored raw payload and metadata</h3> -->
<!--     </header> -->
<!--     <div class="ax-stack-xl"> -->
<!--       <p class="ax-body">Endpoint: <%= humanize(@webhook.endpoint) %></p> -->
<!--       <p class="ax-body">Processed: <%= format_datetime(@webhook.processed_at) %></p> -->
<!--       <p class="ax-body"> Activity feed: <a ...>View linked activity</a> </p> -->
<!--     </div> -->
<!--   </section> -->
<!-- Replace with Detail.detail_section + Detail.detail_field_list + ax-measure on the link para -->

<!-- CRITICAL for connect_account_live: the form at line ~148 uses phx-submit="save_override". -->
<!-- The Detail.detail_section wrapper in this plan ONLY adds ax-measure to the description <p>. -->
<!-- Do NOT rewrap the form inputs themselves — only the prose description paragraph gets ax-measure. -->
<!-- The nested ax-grid-2 at line ~149 stays as-is (it is inside the form, not the prose). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Uplift promotion_code_live.ex — add Detail alias, summary_card hero, detail_section parent-coupon section</name>
  <files>
    accrue_admin/lib/accrue_admin/live/promotion_code_live.ex
    accrue_admin/test/accrue_admin/live/promotion_code_live_test.exs
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/promotion_code_live.ex — full file read (it is thin)
    - accrue_admin/lib/accrue_admin/live/coupon_live.ex — as the sibling analog; after Plan 03 completes, coupon_live is the immediate reference
    - accrue_admin/lib/accrue_admin/components/detail.ex — confirm summary_card :facts slot contract
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md §promotion_code_live — exact before/after code shapes
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for promotion_code_live
  </read_first>
  <behavior>
    - promotion_code_live_test.exs: "renders Detail.summary_card hero not a hand-rolled page header" — html contains summary_card output
    - promotion_code_live_test.exs: "renders parent coupon section in Detail.detail_section wrapper" — html contains ax-detail-section around coupon link
    - promotion_code_live_test.exs: "shows not-found or empty state when coupon is nil" — state coverage for missing parent coupon
  </behavior>
  <action>
    Read promotion_code_live.ex in full. Apply the same pattern as coupon_live (PATTERNS.md §promotion_code_live):

    1. Add Detail to the alias list:
       Change: alias AccrueAdmin.Components.{AppShell, Breadcrumbs, JsonViewer, KpiCard, RelatedResources}
       To: alias AccrueAdmin.Components.{AppShell, Breadcrumbs, Detail, JsonViewer, KpiCard, RelatedResources}

    2. Replace the hand-rolled `<header class="ax-page-header">` hero section (lines ~43–56) with Detail.summary_card:
       - eyebrow: AccrueAdmin.Copy.promotion_code_detail_eyebrow() if the key exists; otherwise "Promotion Code"
       - title: @promotion_code.code || @promotion_code.processor_id || @promotion_code.id
       - :facts slot: status_summary(@promotion_code) and redemption_summary(@promotion_code) as spans (same pattern as coupon after its uplift)

    3. Replace the hand-rolled `<section class="ax-card">` parent coupon section (lines ~72–87) with Detail.detail_section:
       - title: AccrueAdmin.Copy.promotion_code_section_navigate_heading() if the key exists; otherwise "Parent coupon"
       - inner content: a paragraph with the coupon link (if @promotion_code.coupon exists) or the no-coupon empty copy
       - Preserve the existing href pattern to /coupons/:id — do not change the navigation target

    4. Check the PromotionCode struct has a coupon preload available. If @promotion_code.coupon is already loaded in assign (check handle_params), the existing pattern is fine. If it requires a separate preload, check how coupon_live loads promotion codes (the existing screen does Repo.preload) and match that pattern.

    5. Anti-churn tokens (justified by SCORECARD before-scores — same dimension misses as coupon_live):
       - dim ② before: 1 → after: 2 (summary_card hero)
       - dim ⑦ before: 1 → after: 2 (detail_section replaces hand-rolled card; DRY semantics)
       - dim ⑩ before: 1 → after: 2 (Detail primitives replace hand-rolled sections)
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/live/promotion_code_live_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>promotion_code_live.ex has Detail alias; summary_card hero; Detail.detail_section wrapper on parent-coupon section; promotion_code_live_test.exs assertions added and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply ax-measure to connect_account_live prose + DRY webhook_live forensic section + update SCORECARD Wave 2b</name>
  <files>
    accrue_admin/lib/accrue_admin/live/connect_account_live.ex
    accrue_admin/lib/accrue_admin/live/webhook_live.ex
    accrue_admin/test/accrue_admin/live/connect_account_live_test.exs
    accrue_admin/test/accrue_admin/live/webhook_live_test.exs
    .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/connect_account_live.ex lines 140–220 — the platform-fee section with the prose description and the nested ax-grid-2 form; the data-role="save-override" button; phx-submit="save_override"
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex lines 215–250 — the forensic payload section (hand-rolled ax-card block confirmed by direct inspection)
    - accrue_admin/lib/accrue_admin/components/detail.ex — confirm detail_section and detail_field_list API before writing
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md §connect_account_live and §webhook_live — exact change patterns
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for both screens
  </read_first>
  <behavior>
    - connect_account_live_test.exs: "applies ax-measure to platform-fee description prose" — html =~ ~s(class="ax-body ax-measure") in the platform-fee section
    - connect_account_live_test.exs: "preserves save_override form phx-submit and data-role button after prose change" — html =~ ~s(phx-submit="save_override") and ~s(data-role="save-override")
    - webhook_live_test.exs: "forensic payload section uses Detail.detail_section not hand-rolled ax-card" — html contains ax-detail-section markup
    - webhook_live_test.exs: "applies ax-measure to Activity-feed prose paragraph" — html =~ ~s(class="ax-body ax-measure") in forensic section
  </behavior>
  <action>
    CONNECT ACCOUNT LIVE — minimal change:

    1. Read connect_account_live.ex lines 140–155 (the platform-fee detail_section with the prose description). Locate the `<p class="ax-body">` paragraph at line ~145 that renders AccrueAdmin.Copy.connect_account_section_platform_fee_body().

    2. Add ax-measure to that paragraph only:
       Change: `<p class="ax-body">…</p>`
       To: `<p class="ax-body ax-measure">…</p>`

    3. Verify: the nested ax-grid-2 at line ~149 (the form with tax rate inputs) is INSIDE the detail_section but is NOT the prose paragraph. Do NOT add ax-measure to the grid, to the inputs, or to the form. Only the prose description `<p>` gets ax-measure.

    4. Verify that data-role="save-override" button (line ~212) and phx-submit="save_override" form attribute (line ~148) are UNCHANGED after the edit. If the edit accidentally alters the surrounding structure, fix it before proceeding.

    5. Anti-churn: rubric ④ SCR-04 — ax-measure applied to dense prose description region (RESEARCH.md confirmed as a target: connect_account_live.ex line 144 `<p class="ax-body">`).

    ---

    WEBHOOK LIVE — forensic payload DRY uplift:

    6. Read webhook_live.ex lines 215–250. Confirm the exact hand-rolled structure (the direct file inspection found: `<section :if={@webhook} class="ax-card"><header class="ax-page-header">…</header><div class="ax-stack-xl"><p class="ax-body">Endpoint: …</p> …</div></section>`).

    7. Replace the hand-rolled section with Detail.detail_section (webhook_live already has Detail aliased):
       - Preserve the `:if={@webhook}` guard on the section wrapper
       - title: "Stored raw payload and metadata" (use AccrueAdmin.Copy key if one exists for this heading, otherwise keep the string)
       - Use Detail.detail_field_list for the Endpoint and Processed fields:
         fields={[%{label: "Endpoint", value: humanize(@webhook.endpoint)}, %{label: "Processed", value: format_datetime(@webhook.processed_at)}]}
       - Keep the Activity feed paragraph as a `<p class="ax-body ax-measure">` with the existing anchor tag inside (this prose link sentence is a measure target per RESEARCH.md)
       - Preserve the complete href for the activity-feed link — copy it verbatim from the existing code using scoped_mount_path(@admin_mount_path, "/events", @current_owner_scope, %{"source_webhook_event_id" => @webhook.id})

    8. Anti-churn tokens (justified by SCORECARD):
       - webhook_live dim ⑩ before: 2 → after: 3 (forensic section now uses Detail.detail_section)
       - webhook_live rubric SCR-04: ax-measure on activity-feed prose paragraph (previously bare)

    ---

    UPDATE SCORECARD:
    9. Update SCORECARD.md "After Scores" section with Wave 2b after-scores for:
       - promotion_code_live (from Task 1 of this plan)
       - connect_account_live
       - webhook_live
       Record per-dimension after-scores and note which dimensions moved from before to after.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/live/connect_account_live_test.exs test/accrue_admin/live/webhook_live_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>connect_account_live.ex has ax-measure on platform-fee prose; phx-submit and data-role preserved; webhook_live.ex forensic section uses Detail.detail_section/detail_field_list; activity-feed prose has ax-measure; all new test assertions pass; SCORECARD Wave 2b after-scores updated for promotion_code_live, connect_account_live, webhook_live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → LiveView templates | Markup/component structure changes |
| connect_account_live form | Tax-rate override form submits to "save_override" event handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-04-SC | Tampering | no new packages installed | accept | No package installs; no slopcheck required |
| T-176-04-01 | Tampering | connect_account_live tax-rate override form | mitigate | The plan adds ax-measure ONLY to the prose description `<p>` at line ~145. The form phx-submit="save_override", the form inputs, and the data-role="save-override" button must be verified unchanged after the edit. Test assertion explicitly checks both phx-submit and data-role are present in rendered HTML. |

The connect_account_live form is the only security-adjacent surface. The change is minimal (adding a CSS class to a description paragraph). The form structure, event handlers, and authorization context are unchanged. No new data inputs, no new routes, no new authentication surface.
</threat_model>

<verification>
- `grep "ax-measure" accrue_admin/lib/accrue_admin/live/connect_account_live.ex` returns ≥ 1 match
- `grep "phx-submit=\"save_override\"" accrue_admin/lib/accrue_admin/live/connect_account_live.ex` returns ≥ 1 match (unchanged)
- `grep "data-role=\"save-override\"" accrue_admin/lib/accrue_admin/live/connect_account_live.ex` returns ≥ 1 match (unchanged)
- `grep "detail_section" accrue_admin/lib/accrue_admin/live/webhook_live.ex` returns ≥ 4 matches (existing + new forensic section)
- `grep "ax-measure" accrue_admin/lib/accrue_admin/live/webhook_live.ex` returns ≥ 1 match
- `grep "Detail," accrue_admin/lib/accrue_admin/live/promotion_code_live.ex` confirms alias added
- `cd accrue_admin && mix test --seed 0` = 227+ tests, 0 failures
- SCORECARD Wave 2b after-scores populated for all 3 screens in this plan
</verification>

<success_criteria>
promotion_code_live.ex: Detail alias + summary_card hero + detail_section parent-coupon section. connect_account_live.ex: ax-measure on platform-fee prose, form handlers unchanged. webhook_live.ex: forensic section using Detail.detail_section/detail_field_list, ax-measure on activity-feed prose. SCORECARD Wave 2b after-scores documented. Suite green at 227+.
</success_criteria>

<output>
Create `.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-04-SUMMARY.md` when done.
</output>

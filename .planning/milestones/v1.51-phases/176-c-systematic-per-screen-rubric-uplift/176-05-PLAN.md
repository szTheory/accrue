---
phase: 176-c-systematic-per-screen-rubric-uplift
plan: 05
type: execute
wave: 4
depends_on:
  - 176-03
  - 176-04
files_modified:
  - accrue_admin/lib/accrue_admin/live/invoice_live.ex
  - accrue_admin/lib/accrue_admin/live/charge_live.ex
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
  - accrue_admin/test/accrue_admin/live/charge_live_test.exs
  - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04

must_haves:
  truths:
    - "invoice_live tax-risk and actions prose paragraphs have ax-measure applied"
    - "charge_live Braintree eligibility/warning prose and refund-confirm prose have ax-measure"
    - "subscription_live audited: any dimension < 2 fixed; no touch if all ≥ 2"
    - ".ax-measure is NEVER applied to ax-empty-copy, json_viewer, ax-field-list, or KPI grids"
    - "mix test --seed 0 stays at 227+ green"
    - "SCORECARD Wave 3 after-scores populated for invoice_live, charge_live, subscription_live"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/invoice_live.ex"
      provides: "ax-measure on 4 prose regions (tax-risk body, finalization-failure body, recovery body, actions body)"
      contains: "ax-measure"
    - path: "accrue_admin/lib/accrue_admin/live/charge_live.ex"
      provides: "ax-measure on Braintree and refund-confirm prose"
      contains: "ax-measure"
  key_links:
    - from: "invoice_live.ex prose <p> elements"
      to: ".ax-measure CSS class"
      via: "class='ax-body ax-measure'"
      pattern: "ax-body ax-measure"
    - from: "charge_live.ex Braintree section"
      to: ".ax-measure CSS class"
      via: "class='ax-body ax-measure'"
      pattern: "ax-body ax-measure"
---

<objective>
Wave 3 — dense financial detail reading-measure pass. Applies .ax-measure to prose regions in the three dense financial detail screens: invoice_live (4 target prose paragraphs), charge_live (2 target prose paragraphs — the gold-standard screen itself gets its final prose touch), and subscription_live (audit-only unless SCORECARD flags a miss).

Purpose: SCR-04 requires dense text/detail screens to apply reading-measure max-width. These screens already use Detail.detail_section and detail_field_list correctly (rubric ⑩ ≥ 2). The only remaining gap is the prose description paragraphs that remain at full-width. .ax-measure (68ch) constrains them to a comfortable reading line length.

Output: invoice_live and charge_live with ax-measure on prose regions. subscription_live confirmed ≥ 2 or patched on any flagged miss. SCORECARD final after-scores completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-CONTEXT.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-RESEARCH.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md

<interfaces>
<!-- .ax-measure rule (app.css line 406): max-width: var(--ax-measure) -->
<!-- --ax-measure = 68ch (theme.css line 80) -->
<!-- Application: REGION-LEVEL on prose sentences only -->
<!-- NEVER apply to: ax-empty-copy, json_viewer, ax-field-list, KPI grids, summary_card :facts items -->

<!-- invoice_live TARGET PROSE REGIONS (from PATTERNS.md §invoice_live): -->
<!--   Line ~263: <p :if={@invoice.automatic_tax_disabled_reason} class="ax-body"> ... </p> -->
<!--   Line ~266: <p :if={@invoice.last_finalization_error_code} class="ax-body"> ... </p> -->
<!--   Line ~269: <p class="ax-body"> <%= Copy.invoice_tax_recovery_body() %> </p> -->
<!--   Line ~277: <p class="ax-body"> <%= Copy.invoice_actions_body() %> </p> -->
<!--   For each: add ax-measure → class="ax-body ax-measure" -->

<!-- charge_live TARGET PROSE REGIONS (from PATTERNS.md §charge_live — the gold standard): -->
<!--   Lines ~218–219: inside <div :if={@charge.processor == "braintree"} class="ax-stack-sm"> -->
<!--     <p class="ax-body"><%= Copy.charge_refund_braintree_eligibility_info() %></p> -->
<!--     <p class="ax-body"><%= Copy.charge_refund_not_final_truth_warning() %></p> -->
<!--   Line ~242: <p class="ax-body"><%= refund_copy(@pending_refund, @charge.currency) %></p> -->
<!--   For each: add ax-measure → class="ax-body ax-measure" -->

<!-- subscription_live: already uses ax-grid-2, json_viewer, detail_section -->
<!--   Expected score ≥ 2 on most dims (RESEARCH.md) — touch ONLY on SCORECARD flagged miss -->
<!--   If any dim < 2 in SCORECARD: apply targeted fix per dimension -->
<!--   If all ≥ 2: document "confirmed ≥ 2, no changes made" in SCORECARD -->

<!-- CRITICAL DO NOT APPLY: -->
<!--   .ax-empty-copy already has max-width: 28rem — do NOT add ax-measure (double-cap pitfall) -->
<!--   json_viewer is structured data, not prose — do NOT add ax-measure to the viewer container -->
<!--   ax-field-list is columnar — do NOT add ax-measure to dl.ax-field-list -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply ax-measure to invoice_live.ex and charge_live.ex prose regions</name>
  <files>
    accrue_admin/lib/accrue_admin/live/invoice_live.ex
    accrue_admin/lib/accrue_admin/live/charge_live.ex
    accrue_admin/test/accrue_admin/live/invoice_live_test.exs
    accrue_admin/test/accrue_admin/live/charge_live_test.exs
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex lines 255–285 — the tax-risk panel and actions sections with target prose
    - accrue_admin/lib/accrue_admin/live/charge_live.ex lines 210–250 — the Braintree section and refund confirm panel
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md §invoice_live and §charge_live — exact target line references
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores; confirm any dim < 2 before changing (anti-churn)
  </read_first>
  <behavior>
    - invoice_live_test.exs: "applies ax-measure to tax-risk prose paragraphs" — html =~ ~s(class="ax-body ax-measure") when @invoice has tax issues
    - invoice_live_test.exs: "applies ax-measure to actions body paragraph" — html =~ ~s(class="ax-body ax-measure") in actions section
    - invoice_live_test.exs: "does NOT apply ax-measure to field-lists or json_viewer" — html does NOT contain ~s(ax-field-list ax-measure) or ~s(json_viewer ax-measure)
    - charge_live_test.exs: "applies ax-measure to Braintree eligibility and warning prose" — html =~ ~s(class="ax-body ax-measure") in braintree section
    - charge_live_test.exs: "applies ax-measure to refund confirm prose" — html =~ ~s(class="ax-body ax-measure") in refund confirm panel
  </behavior>
  <action>
    INVOICE LIVE (invoice_live.ex):

    1. Locate the tax-risk panel section (around lines 255–285 — the section inside Detail.detail_section that shows automatic_tax_disabled_reason, finalization error code, and recovery body). Read the actual lines first to get exact line numbers.

    2. For each of the 4 target prose paragraphs, add ax-measure:
       - `<p :if={present?(@invoice.automatic_tax_disabled_reason)} class="ax-body">` → `class="ax-body ax-measure"`
       - `<p :if={present?(@invoice.last_finalization_error_code)} class="ax-body">` → `class="ax-body ax-measure"`
       - `<p class="ax-body"><%= Copy.invoice_tax_recovery_body() %>` → `class="ax-body ax-measure"`
       - `<p class="ax-body"><%= Copy.invoice_actions_body() %>` → `class="ax-body ax-measure"`

    3. Do NOT add ax-measure to: the ax-grid-3 line-item form, any ax-field-list, any ax-empty-copy, any json_viewer containers.

    4. Verify the ax-grid-3 line-item section (around line ~379: `<div class="ax-grid ax-grid-3">`) already stacks correctly at mobile — it should, since ax-grid-3 goes to 1fr below 768px per the existing CSS. No change needed here; just confirm by reading those lines.

    ---

    CHARGE LIVE (charge_live.ex):

    5. Locate the Braintree refund section (around lines 215–250). Read the actual lines to get exact positions.

    6. Add ax-measure to the three target prose paragraphs:
       - Both `<p class="ax-body">` inside `<div :if={@charge.processor == "braintree"} class="ax-stack-sm">` → `class="ax-body ax-measure"` on each
       - `<p class="ax-body"><%= refund_copy(@pending_refund, @charge.currency) %>` → `class="ax-body ax-measure"`

    7. The ax-grid-2 fee-breakdown section (line ~182: `<section class="ax-grid ax-grid-2">`) already stacks to single column at mobile — no change needed. Do not add ax-measure to the grid.

    8. Anti-churn token for both screens: SCR-04 — dense text/detail screens apply reading-measure; RESEARCH.md explicitly identified these prose regions as targets. If SCORECARD records dim ① below 2 for either screen (inline style= present), fix it while in-file; otherwise ① is already ≥ 2.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>invoice_live.ex has ax-measure on all 4 target prose paragraphs; charge_live.ex has ax-measure on all 3 target prose paragraphs; ax-measure is NOT applied to field-lists, json_viewer, empty-copy, or grids; all new test assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Audit subscription_live + complete SCORECARD final after-scores for all screens</name>
  <files>
    accrue_admin/lib/accrue_admin/live/subscription_live.ex
    .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
  </files>
  <read_first>
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for subscription_live; the full after-scores table for all previous waves
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex — full file read (confirm ax-grid-2, json_viewer, detail_section usage; check for any prose `<p class="ax-body">` that lack ax-measure and are in dense-content sections)
  </read_first>
  <action>
    SUBSCRIPTION LIVE:

    1. Read subscription_live.ex in full. Confirm: (a) ax-grid-2 used for detail grid layout, (b) Detail.detail_section and/or Detail.detail_field_list used for field sections, (c) json_viewer used for raw data.

    2. Check the SCORECARD before-score for subscription_live. If ALL dimensions are ≥ 2, do NOT touch the file — document "confirmed ≥ 2 on all dimensions, no changes made" in SCORECARD.

    3. If any dimension is < 2, apply a targeted fix only for that dimension. Common potential miss for subscription_live:
       - Any `<p class="ax-body">` prose description inside a detail section that is not under ax-measure → add ax-measure per SCR-04
       - Any hand-rolled section that should use Detail.detail_section → replace per dim ⑩

    4. Anti-churn: only make changes explicitly justified by SCORECARD before-score < 2. If no before-score < 2, no changes.

    ---

    COMPLETE SCORECARD:

    5. Update SCORECARD.md to fill in ALL remaining after-scores:
       - Wave 3 after-scores for: invoice_live, charge_live, subscription_live
       - If any after-score is still below 2, add a note "requires Phase 179 visual QA confirmation" — the code-level pass is complete

    6. Fill in the "Phase 176 Final Summary" section (add it if not present):
       - Total screens: 21
       - Screens lifted from < 2 to ≥ 2 on at least one dimension: list them
       - Screens confirmed ≥ 2 with no changes: list them
       - Remaining dimensions at code-level 2 pending visual confirmation (Phase 179): list them
       - dim ⑨ (motion): all screens inherit reduced-motion from global CSS; no uplift done (Phase 177 scope)

    7. Run the full suite to confirm no regressions from this entire wave.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>subscription_live audited and either unchanged (all dims ≥ 2) or patched on flagged miss; SCORECARD complete with Wave 3 after-scores AND a final summary section; mix test --seed 0 = 227+ tests, 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → LiveView templates | Read-only audit + CSS class additions; no new data inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-05-SC | Tampering | no new packages installed | accept | No package installs; no slopcheck required |

This plan adds only the ax-measure CSS class to existing prose `<p>` elements. No new routes, no new user inputs, no data access changes, no form handler modifications. invoice_live and charge_live have complex forms (invoice payment/void actions, charge refund actions) — the plan explicitly targets ONLY the description prose paragraphs in the information sections, NOT the form sections. Verify by confirming that all existing phx-submit/phx-click handlers in the touched sections are unchanged after edits.
</threat_model>

<verification>
- `grep -c "ax-body ax-measure" accrue_admin/lib/accrue_admin/live/invoice_live.ex` returns ≥ 4
- `grep -c "ax-body ax-measure" accrue_admin/lib/accrue_admin/live/charge_live.ex` returns ≥ 3
- `grep "ax-measure" accrue_admin/lib/accrue_admin/live/invoice_live.ex` does NOT match any ax-field-list or ax-empty-copy lines
- `cd accrue_admin && mix test --seed 0` = 227+ tests, 0 failures
- SCORECARD.md "After Scores" section is fully populated for all 21 screens
- SCORECARD.md "Phase 176 Final Summary" section exists
</verification>

<success_criteria>
invoice_live.ex and charge_live.ex have ax-measure on all RESEARCH-identified prose regions. subscription_live audited and confirmed ≥ 2 or patched. SCORECARD fully populated with all before/after scores and a phase summary. Suite green at 227+. SCR-04 satisfied: dense text/detail screens apply reading-measure max-width container.
</success_criteria>

<output>
Create `.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-05-SUMMARY.md` when done.
</output>

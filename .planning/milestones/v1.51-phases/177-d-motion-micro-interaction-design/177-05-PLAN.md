---
phase: 177-d-motion-micro-interaction-design
plan: 05
type: execute
wave: 4
depends_on:
  - 177-01
  - 177-04
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements:
  - MOT-01

must_haves:
  truths:
    - "verify_package_docs.sh has 4 motion antipattern guards (transition:all, cubic-bezier, raw ms/s literals, layout-thrash props) plus the motion.md guide needle"
    - "package_docs_verifier_test.exs has 4 negative tests for the new motion guards that inject violations into a seeded app.css copy and assert status != 0"
    - "seed_tmp_dir!/1 copies accrue_admin/guides/motion.md so the guide-existence needle doesn't cause false positive test failures"
    - "All 6 existing negative tests still pass after the seed update"
    - "verify_package_docs.sh passes on the current codebase (no spurious failures)"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "4 motion antipattern guards + motion.md guide needle"
      contains: "MOT-01"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "4 negative tests for motion guards + updated seed_tmp_dir! with motion.md"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue_admin/assets/css/app.css"
      via: "grep -qE 'transition:\\s*all\\b' etc. guard checks"
      pattern: "transition.*all"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "System.cmd bash @script_path with injected violation in seeded app.css"
      pattern: "ax-drift"
---

<objective>
Extend the CI antipattern guard in verify_package_docs.sh with 4 motion guards (MOT-01)
and pair each new guard with a negative test in package_docs_verifier_test.exs. Also add
the motion.md guide-existence needle to the script and its copy to seed_tmp_dir!/1.
Both changes go in the same commit (the known verify_package_docs ↔ test coupling).

Purpose: Without the guard, raw ms literals or transition:all could slip into app.css and
silently bypass the reduced-motion override in theme.css, violating MOT-03. The negative
tests prove the guard fires on violations (not just that it doesn't fire on good code).

Output:
- verify_package_docs.sh: 4 new motion guard checks after the Phase 174 breakpoint guard +
  require_fixed needle for guides/motion.md in accrue_admin/mix.exs
- package_docs_verifier_test.exs: 4 new negative test blocks (one per guard needle) +
  copy_fixture! for accrue_admin/guides/motion.md in seed_tmp_dir!/1
  + copy_fixture! for accrue_admin/guides/motion.md (must exist at this path)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-d-motion-micro-interaction-design/177-CONTEXT.md
@.planning/phases/177-d-motion-micro-interaction-design/177-PATTERNS.md
@.planning/phases/177-d-motion-micro-interaction-design/177-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From scripts/ci/verify_package_docs.sh lines 322-328 (the Phase 174 breakpoint guard — add AFTER this):
  # Token bypass guards (Phase 174, DSY-01)
  app_css="$ROOT_DIR/accrue_admin/assets/css/app.css"
  if grep -E '@media \((min|max)-width: [0-9.]+px\)' "$app_css" | grep -qv '\-\-ax-bp-'; then
    fail "$app_css must not have bare breakpoint @media without an --ax-bp-* annotation comment ..."
  fi

From scripts/ci/verify_package_docs.sh lines 23-27 (require_fixed function):
  require_fixed() {
    local file="$1"
    local needle="$2"
    ...
  }

From accrue/test/accrue/docs/package_docs_verifier_test.exs lines 241-268 (exact pattern to replicate):
  test "package docs verifier rejects unguarded breakpoint @media in app.css" do
    tmp_dir = Path.join(System.tmp_dir!(), "accrue-docs-verifier-#{System.unique_integer([:positive])}")
    File.rm_rf!(tmp_dir)
    on_exit(fn -> File.rm_rf(tmp_dir) end)
    seed_tmp_dir!(tmp_dir)
    app_css_path = Path.join(tmp_dir, "accrue_admin/assets/css/app.css")
    original = File.read!(app_css_path)
    drifted = original <> "\n@media (min-width: 900px) { .ax-drift { display: block; } }\n"
    File.write!(app_css_path, drifted)
    {output, status} = System.cmd("bash", [@script_path], stderr_to_stdout: true, env: [{"ROOT_DIR", tmp_dir}])
    assert status != 0
    assert output =~ "[verify_package_docs]"
    assert output =~ "app.css"
    assert output =~ "--ax-bp-"
  end

From accrue/test/accrue/docs/package_docs_verifier_test.exs lines 302-339 (seed_tmp_dir!/1):
  defp seed_tmp_dir!(tmp_dir) do
    File.mkdir_p!(Path.join(tmp_dir, "accrue/guides"))
    File.mkdir_p!(Path.join(tmp_dir, "accrue_admin"))
    File.mkdir_p!(Path.join(tmp_dir, "accrue_admin/assets/css"))
    ...
    copy_fixture!("accrue_admin/mix.exs", tmp_dir)
    copy_fixture!("accrue_admin/README.md", tmp_dir)
    copy_fixture!("accrue_admin/assets/css/app.css", tmp_dir)
    ...
    (accrue_admin/guides/motion.md is NOT yet in the seed — must be added)

Four violation strings to inject in the 4 negative tests:
  1. transition:all violation:       "\n.ax-drift { transition: all 180ms; }\n"
  2. cubic-bezier violation:         "\n.ax-drift { transition: opacity cubic-bezier(0.4,0,1,1); }\n"
  3. Raw ms literal violation:       "\n.ax-drift { transition: opacity 200ms ease; }\n"
  4. Layout-thrash violation:        "\n.ax-drift { transition: height var(--ax-dur-2) ease; }\n"
     (Use height directly — the guard bans height in transition lists)

Guard assertions for each test (what output string to assert):
  1. "--ax-transition-*" or "transition: all" or "MOT-01/A1" — use the fail message text from the guard
  2. "cubic-bezier" or "MOT-01/A3"
  3. "ax-skeleton-shimmer" or "MOT-01/A3" or "duration"
  4. "layout" or "MOT-01/A2" or "height"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add motion antipattern guards to verify_package_docs.sh</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <action>
Read the full verify_package_docs.sh file. Locate the Phase 174 breakpoint guard block
(the block starting with "# Token bypass guards (Phase 174, DSY-01)" around lines 322-328).
Add the following 5 new guard blocks IMMEDIATELY AFTER the closing `fi` of the breakpoint
guard. Use the exact if-grep-then-fail pattern from PATTERNS.md §verify_package_docs guard
shape.

GUARD 1 — ban transition:all (antipattern A1):
Add a comment "# Motion antipattern guards (Phase 177, MOT-01)" before the first new block.
  if grep -qE 'transition:\s*all\b' "$app_css"; then
    fail "$app_css must not use 'transition: all' (MOT-01/A1) — name the exact properties or use an --ax-transition-* bundle"
  fi

GUARD 2 — ban raw cubic-bezier( in app.css (theme.css atom definitions are exempt since
we pass the ROOT_DIR app_css path, not theme.css):
  if grep -qE 'cubic-bezier\(' "$app_css"; then
    fail "$app_css must not contain raw cubic-bezier() literals (MOT-01/A3) — use --ax-ease-* atoms from theme.css"
  fi

GUARD 3 — ban raw ms/s duration literals in transition/animation rules, with allowlist for
ax-skeleton-shimmer. Note: the -v flag inverts grep to show only non-matching lines, then
we pipe to -q. Use a two-pipe approach:
  if grep -E '(transition|animation):[^;]*[0-9]+(ms|s)\b' "$app_css" | grep -qv 'ax-skeleton-shimmer'; then
    fail "$app_css must not have raw ms/s duration literals in transition/animation rules (MOT-01/A3) — use --ax-dur-* tokens; exception: ax-skeleton-shimmer 1.4s is allowlisted"
  fi

GUARD 4 — ban layout-thrash properties in transition lists (A2):
  if grep -qE 'transition:[^;]*\b(height|width|margin|padding|top|left|right|bottom)\b' "$app_css"; then
    fail "$app_css must not animate layout-triggering properties in transition lists (MOT-01/A2) — use only opacity/transform (composited)"
  fi

GUIDE NEEDLE 5 — require motion.md in accrue_admin/mix.exs. Add after the motion guard
blocks:
  # Motion guide existence (Phase 177, MOT-01)
  require_fixed "$ROOT_DIR/accrue_admin/mix.exs" '"guides/motion.md"'

After editing, run the guard script against the current repo to verify it passes on clean
code (no violations exist after Plans 01-04 applied token-safe CSS):
  cd /Users/jon/projects/accrue && bash scripts/ci/verify_package_docs.sh
Confirm exit 0.
  </action>
  <verify>
    <automated>grep -c "MOT-01" /Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh | grep -v "^0$" && bash /Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh && echo "guard script passes on current codebase"</automated>
  </verify>
  <done>
    - 4 motion antipattern guards added to verify_package_docs.sh after the Phase 174 breakpoint guard block
    - require_fixed needle for '"guides/motion.md"' in accrue_admin/mix.exs added
    - All 5 new guard blocks are annotated "Phase 177, MOT-01"
    - `bash scripts/ci/verify_package_docs.sh` exits 0 on the clean codebase (Plans 01-04 CSS is token-safe)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 4 negative tests + update seed_tmp_dir! in package_docs_verifier_test.exs</name>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <action>
Read package_docs_verifier_test.exs fully (it is under 400 lines). Make two changes:

CHANGE A — Add copy_fixture! for motion.md in seed_tmp_dir!/1:
In seed_tmp_dir!/1 (around line 302), after the
`copy_fixture!("accrue_admin/assets/css/app.css", tmp_dir)` line, add:
  File.mkdir_p!(Path.join(tmp_dir, "accrue_admin/guides"))
  copy_fixture!("accrue_admin/guides/motion.md", tmp_dir)
This is required because the new guard script checks require_fixed against
accrue_admin/mix.exs for '"guides/motion.md"', which means the script also expects
accrue_admin/guides/motion.md to exist (the requirement is about the mix.exs line, not the
file itself — but add the mkdir_p for guides if not already present and copy motion.md to
avoid the guide-existence needle from failing for wrong reasons). Also verify the existing
`File.mkdir_p!(Path.join(tmp_dir, "accrue_admin"))` is already present (it is — line 308)
so only the guides subdirectory mkdir_p and copy_fixture! are new.

CHANGE B — Add 4 negative test blocks after the existing "breakpoint @media" negative test.
Each test follows the exact shape of the breakpoint negative test (lines 241-268). Name the
tests to make their purpose clear:

TEST 1 — "package docs verifier rejects 'transition: all' in app.css":
  Append: `\n.ax-drift { transition: all 180ms; }\n` to the seeded app.css
  Assert: status != 0, output =~ "[verify_package_docs]", output =~ "app.css",
          output =~ "transition: all" (matches the fail message text)

TEST 2 — "package docs verifier rejects raw cubic-bezier() in app.css":
  Append: `\n.ax-drift { transition: opacity cubic-bezier(0.4,0,1,1); }\n`
  Assert: status != 0, output =~ "[verify_package_docs]", output =~ "app.css",
          output =~ "cubic-bezier"

TEST 3 — "package docs verifier rejects raw ms duration literal in app.css":
  Append: `\n.ax-drift { transition: opacity 200ms ease; }\n`
  Assert: status != 0, output =~ "[verify_package_docs]", output =~ "app.css",
          output =~ "ms"

TEST 4 — "package docs verifier rejects layout-thrash property in transition list in app.css":
  Append: `\n.ax-drift { transition: height var(--ax-dur-2) ease; }\n`
  Assert: status != 0, output =~ "[verify_package_docs]", output =~ "app.css",
          output =~ "layout"

Each test body follows the identical structure as the breakpoint guard negative test
(lines 241-268): new tmp_dir, File.rm_rf!, on_exit, seed_tmp_dir!, read app_css_path,
append violation, write, System.cmd bash @script_path with ROOT_DIR, assert status + output.
No shared state between tests — each test uses its own unique tmp_dir via
System.unique_integer([:positive]).

Run the full test suite to confirm all new tests pass and no existing tests regress:
  cd /Users/jon/projects/accrue/accrue && mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0
  </action>
  <verify>
    <automated>grep -c "transition: all" /Users/jon/projects/accrue/accrue/test/accrue/docs/package_docs_verifier_test.exs | grep -v "^0$" && cd /Users/jon/projects/accrue/accrue && mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0 2>&1 | tail -5</automated>
  </verify>
  <done>
    - seed_tmp_dir!/1 includes copy_fixture! for "accrue_admin/guides/motion.md"
    - 4 new negative test blocks exist in the test file (one per guard needle)
    - Each test appends a violation to seeded app.css and asserts status != 0
    - `mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0` passes all tests (including the 6 existing negative tests and the 4 new ones)
    - Total negative test count for guard violations ≥ 5 (breakpoint + 4 motion)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script | verify_package_docs.sh runs in CI with read-only access to repo files; no network, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-05-SC | Tampering | No new package installs | accept | No npm/pip/cargo installs |

This plan modifies a CI guard script and ExUnit tests. No authentication, session, input
validation, cryptography, or access control changes. The guard script reads files — it has
no write access and no network access.
</threat_model>

<verification>
After both tasks — verify in sequence:
1. `bash scripts/ci/verify_package_docs.sh` — exits 0 (passes on clean codebase)
2. `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0` — all tests green
3. `grep -c "MOT-01" scripts/ci/verify_package_docs.sh` — returns >= 5 (4 guards + comment)
4. `grep -c "transition: all" accrue/test/accrue/docs/package_docs_verifier_test.exs` — returns >= 1
</verification>

<success_criteria>
- 4 motion antipattern guards in verify_package_docs.sh (transition:all, cubic-bezier, raw ms/s, layout-thrash)
- require_fixed needle for '"guides/motion.md"' in accrue_admin/mix.exs
- 4 paired negative tests in package_docs_verifier_test.exs
- seed_tmp_dir!/1 copies accrue_admin/guides/motion.md
- Guard script passes on clean codebase (exit 0)
- All verifier tests pass
</success_criteria>

<output>
Create `.planning/phases/177-d-motion-micro-interaction-design/177-05-SUMMARY.md` when done.
</output>

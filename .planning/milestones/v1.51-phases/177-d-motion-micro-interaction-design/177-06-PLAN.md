---
phase: 177-d-motion-micro-interaction-design
plan: 06
type: execute
wave: 4
depends_on:
  - 177-04
files_modified:
  - accrue_admin/e2e/reduced-motion.spec.js
  - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
autonomous: true
requirements:
  - MOT-01
  - MOT-03

must_haves:
  truths:
    - "reduced-motion.spec.js has two-test describe blocks (D-15 pattern) for .ax-dropdown-panel, .ax-command-palette, and .ax-detail-drawer"
    - "Each new describe block: test 1 asserts all transition-duration segments are '0s' under prefers-reduced-motion:reduce; test 2 asserts at least one segment is non-zero without reduced-motion"
    - "A structural Playwright test asserts no transform travel on dropdown/palette under reduced-motion (computed transform === 'none' or no translate)"
    - "component_kitchen_live.ex has a Motion reference section listing all 9 animated surfaces with their token and justification"
    - "All new Playwright tests pass (e2e/reduced-motion.spec.js green)"
  artifacts:
    - path: "accrue_admin/e2e/reduced-motion.spec.js"
      provides: "Two-test describe blocks for dropdown, palette, and drawer + structural no-travel assertion"
    - path: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      provides: "Motion reference section in the /dev/components kitchen page"
  key_links:
    - from: "accrue_admin/e2e/reduced-motion.spec.js"
      to: "accrue_admin/assets/css/app.css"
      via: "CSS transition-duration computed value read via getComputedStyle"
      pattern: "transitionDuration"
    - from: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      to: "accrue_admin/guides/motion.md"
      via: "references the motion guide for the full spec"
      pattern: "motion.md"
---

<objective>
Extend the D-15 Playwright reduced-motion spec to cover the newly animated surfaces
(.ax-dropdown-panel, .ax-command-palette, .ax-detail-drawer) and add a structural
no-transform-travel assertion. Also add the motion reference section to the /dev/components
kitchen page. (MOT-01 deliverable: live motion reference in /dev/components; MOT-03
deliverable: automated reduced-motion coverage for new surfaces.)

Purpose: Static tests (mix test) cannot verify that CSS transitions collapse under
prefers-reduced-motion. The Playwright spec emulates the media query and reads computed
CSS to prove the bundle override is working. The /dev/components section makes the motion
spec discoverable by developers browsing the kitchen while the app is running.

Output:
- e2e/reduced-motion.spec.js: 3 new test.describe blocks (dropdown, palette, drawer) +
  a structural test asserting no transform travel under reduced-motion
- component_kitchen_live.ex: a "Motion" section near the end of the render function
  listing all 9 animated surfaces as a reference table
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-d-motion-micro-interaction-design/177-CONTEXT.md
@.planning/phases/177-d-motion-micro-interaction-design/177-UI-SPEC.md
@.planning/phases/177-d-motion-micro-interaction-design/177-PATTERNS.md
@.planning/phases/177-d-motion-micro-interaction-design/177-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From accrue_admin/e2e/reduced-motion.spec.js (FULL FILE — the pattern to extend):

  async function buttonTransitionDurations(page) {
    await expect(page.locator(".ax-button").first()).toBeVisible();
    return page.evaluate(() => {
      const el = document.querySelector(".ax-button");
      if (!el) return null;
      return window.getComputedStyle(el).transitionDuration.split(",").map(s => s.trim());
    });
  }

  test.describe("Reduced motion — bundle override collapses transitions to instant (D-15)", () => {
    test("with prefers-reduced-motion:reduce, .ax-button transition-duration collapses to 0s on every segment", async ({ page }) => {
      await page.emulateMedia({ reducedMotion: "reduce" });
      await login(page, "/billing/dev/components");
      await expect(page.locator("#main-content")).toBeVisible();
      const durations = await buttonTransitionDurations(page);
      // ... assert every seg === "0s"
    });
    test("WITHOUT reduced-motion the same .ax-button has a NON-zero transition-duration ...", async ({ page }) => {
      await login(page, "/billing/dev/components");
      // ... assert some seg !== "0s"
    });
  });

Three new surfaces to add (replicate the describe block structure for each):
  .ax-dropdown-panel        — on /billing/dev/components (the page has dropdowns)
  .ax-command-palette       — on /billing/dev/components (global search is present)
  .ax-detail-drawer         — on /billing/dev/components or any page with a visible drawer

Note: .ax-dropdown-panel and .ax-command-palette start at opacity:0 (closed state after Plan 02+04).
The transitionDuration is readable from getComputedStyle even on hidden elements — the
transition property is always set on the selector, not conditional on open/closed state.
The helper function queries getComputedStyle which returns the declared transition regardless
of the element's visual state.

For .ax-detail-drawer: the element is only in the DOM when open (:if={@open}).
On the /dev/components page there may not be an open drawer. An alternative: assert the
CSS transition classes exist in a style tag or read transition from the .ax-drawer-entering
class. The simplest approach: test that the CSS file declares the relevant token references
by doing a page.evaluate that reads document.styleSheets — OR skip the DOM-based test and
use a CSS presence assertion. Per VALIDATION.md: "structural, not pixel-diff". The
structural test for the drawer can assert that the .ax-drawer-entering class is present in
the stylesheet with a non-instant transition (not 0s). Under reduced-motion, assert 0s.

The structural no-travel test: navigate to /billing/dev/components with reducedMotion:reduce,
click the dropdown trigger (if one is accessible), and assert that the computed transform
of .ax-dropdown-panel is "none" (no translate) or that all transitionDuration segments are "0s".

From accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex:
  294 lines total; last <section> ends around line 259. The motion section should be added
  as a new <section> near the end, before the final closing tags.
  The kitchen LiveView uses Elixir ~H templates. Sections use class="ax-card ax-dev-stack"
  or similar. Add a section with a heading, a prose note linking to motion.md guide, and
  a simple definition list or table listing the 9 surfaces with their surface name and
  functional justification (brief — the full spec is in the guide).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend e2e/reduced-motion.spec.js with three new describe blocks and a structural no-travel test</name>
  <files>accrue_admin/e2e/reduced-motion.spec.js</files>
  <action>
Read the full reduced-motion.spec.js file. It currently has one test.describe block (the
D-15 button bundle-override test with 2 tests). Add 4 new blocks after it.

NEW DESCRIBE BLOCK 1 — ".ax-dropdown-panel bundle collapse (Phase 177 MOT-03)":
Create a helper function dropdownPanelTransitionDurations(page) that queries
".ax-dropdown-panel" — same implementation as buttonTransitionDurations but with selector
".ax-dropdown-panel". Note: the dropdown panel has a 2-property transition (opacity and
transform) so transitionDuration will have 2 segments.

  test 1: emulateMedia reducedMotion "reduce", login to /billing/dev/components, wait for
  #main-content, call dropdownPanelTransitionDurations — assert every segment is "0s".
  Message: "under reduced-motion, every .ax-dropdown-panel transition segment must be '0s'"

  test 2: no emulateMedia, login, wait, call helper — assert some segment is not "0s".
  Message: "WITHOUT reduced-motion, .ax-dropdown-panel must have a non-zero transition"

NEW DESCRIBE BLOCK 2 — ".ax-command-palette bundle collapse (Phase 177 MOT-03)":
Create palettePanelTransitionDurations(page) querying ".ax-command-palette".
Same two-test structure. Note: the palette has a 2-property transition (opacity and
transform). Assert same as above but for the palette selector.

NEW DESCRIBE BLOCK 3 — ".ax-drawer-entering CSS class transition collapse (Phase 177 MOT-03)":
The drawer is not in the DOM when closed, so we read the transition from the CSS rule using
document.styleSheets. Create drawerEnteringTransitionDurations(page) that:
  1. Iterates document.styleSheets
  2. Finds the rule whose selectorText is ".ax-drawer-entering" (or contains "ax-drawer-entering")
  3. Returns the transitionDuration value split on commas

  test 1: emulateMedia "reduce", login, assert drawer-entering rule's transitionDuration is "0s"
  (all segments). Under reduced-motion --ax-dur-3 becomes 0ms so the CSS class rule's computed
  transition should collapse. Note: getComputedStyle on a class rule reads the RESOLVED value;
  use CSSStyleSheet iteration to read the DECLARED value, which references --ax-dur-3. For
  reduced-motion, this token resolves to 0ms. To verify: assert that page.evaluate reads
  the token value via getComputedStyle on document.documentElement for --ax-dur-3 === "0ms".
  This is a simpler and more reliable check: assert that --ax-dur-3 token collapses to "0ms"
  under reducedMotion "reduce" (proving all surfaces that use this token get instant motion).

  test 2: no emulateMedia, assert --ax-dur-3 resolves to a non-zero value (e.g., "240ms").

NEW TEST (standalone, not in a describe block) — "structural: no transform travel on
dropdown under prefers-reduced-motion":
  emulateMedia({ reducedMotion: "reduce" })
  login to /billing/dev/components
  await page.locator("#main-content").waitFor()
  Use page.evaluate to assert getComputedStyle(document.documentElement).getPropertyValue("--ax-rise-sm").trim() === "0px"
  (Under reduced-motion, --ax-rise-sm collapses to 0px, so no translateY travel can occur
  for any surface using this token.)
  Message: "under reduced-motion, --ax-rise-sm must be 0px (no transform travel on dropdown/flash)"

  And assert --ax-rise-md === "0px" for the drawer:
  same approach. These two assertions prove all travel tokens are 0 under reduced-motion,
  which structurally guarantees no transform travel on any surface using these tokens.

After adding all new tests, run the Playwright e2e suite against the running dev server:
  cd /Users/jon/projects/accrue/accrue_admin && npx playwright test e2e/reduced-motion.spec.js
All tests must pass (existing D-15 tests + the 4 new blocks).
  </action>
  <verify>
    <automated>grep -c "MOT-03" /Users/jon/projects/accrue/accrue_admin/e2e/reduced-motion.spec.js | grep -v "^0$" && grep -c "ax-dropdown-panel" /Users/jon/projects/accrue/accrue_admin/e2e/reduced-motion.spec.js | grep -v "^0$" && echo "reduced-motion spec extended"</automated>
  </verify>
  <done>
    - e2e/reduced-motion.spec.js has describe blocks for .ax-dropdown-panel, .ax-command-palette, and .ax-drawer-entering CSS rule
    - A structural test asserts --ax-rise-sm and --ax-rise-md are "0px" under reduced-motion
    - Each describe block has the D-15 two-test pattern (with-reduced/without-reduced)
    - `npx playwright test e2e/reduced-motion.spec.js` passes all tests (both existing and new)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Motion reference section to component_kitchen_live.ex</name>
  <files>accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex</files>
  <action>
Read component_kitchen_live.ex fully. It is a Phoenix LiveView module (~294 lines) with a
render/1 function containing several `<section>` blocks. Add a new motion reference section
near the end of the template, before the final `</section>` or `</div>` that closes the
outer `.ax-page` section.

The section should be annotated with a comment "<!-- Phase 177 — Motion reference (MOT-01) -->"
and include:
  1. A section element with class "ax-card ax-dev-stack" (matching the existing section style).
  2. A heading: "Motion Reference" (use the heading style matching other section headings —
     likely `<h2 class="ax-heading">` or similar; check what the existing sections use).
  3. A brief prose paragraph: "Nine surfaces animate via Phase 174 --ax-transition-* bundles.
     Full spec: accrue_admin/guides/motion.md. Motion trace review: Phase 179."
  4. A definition list or HTML table listing all 9 surfaces. For brevity in the kitchen
     view, include: surface name, trigger, and justification from the motion contract table.
     The 9 surfaces are: detail_drawer, detail_drawer-backdrop, dropdown_menu, More ▾,
     collapsible nav, global_search palette, palette backdrop, tabs active indicator,
     flash/toasts, skeleton→content, badge/state change. Condense to the primary 9 entries
     (detail_drawer-backdrop and palette backdrop can be listed as sub-entries or combined).

The section should be static (no assigns needed — all values are constants from the motion
spec). Keep the section brief — its purpose is to serve as a quick reference for developers
browsing the kitchen, not to re-publish the full guide. Link to the full guide via a prose
comment in the HEEx that says: "See accrue_admin/guides/motion.md for the full spec."

Do NOT modify any existing sections. Do NOT change the module, assign initialization, or
handle_event functions. This is a pure template addition.

After adding the section:
  cd /Users/jon/projects/accrue/accrue_admin && mix test --seed 0
Suite must remain 252+ green.
  </action>
  <verify>
    <automated>grep -c "Motion" /Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex | grep -v "^0$" && cd /Users/jon/projects/accrue/accrue_admin && mix test --seed 0 2>&1 | tail -3</automated>
  </verify>
  <done>
    - component_kitchen_live.ex has a "Motion Reference" section in the render template
    - The section lists all 9 animated surfaces with their trigger and justification
    - The section includes a reference to accrue_admin/guides/motion.md
    - `mix test --seed 0` passes 252+ tests (no compilation errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test | Playwright test runs against the dev server — no production access, no secrets |
| /dev/components page | component_kitchen_live.ex is behind the admin auth wall; the motion reference section adds no new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-06-01 | Information Disclosure | component_kitchen_live.ex motion section | accept | The motion section is in /dev/components which is already behind admin auth. No new data or capabilities exposed. Static reference content only. |
| T-177-06-SC | Tampering | No new package installs | accept | No npm/pip/cargo installs |

This plan adds E2E tests and a static reference section. No authentication, session, input
validation, cryptography, or access control changes.
</threat_model>

<verification>
After both tasks:
1. `grep "MOT-03" accrue_admin/e2e/reduced-motion.spec.js` — matches
2. `grep "ax-dropdown-panel" accrue_admin/e2e/reduced-motion.spec.js` — matches
3. `grep "ax-rise-sm" accrue_admin/e2e/reduced-motion.spec.js` — matches (structural test)
4. `grep "Motion" accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex` — matches
5. `npx playwright test accrue_admin/e2e/reduced-motion.spec.js` — all tests green
6. `cd accrue_admin && mix test --seed 0` — 252+ green
</verification>

<success_criteria>
- reduced-motion.spec.js extended with D-15-pattern two-test blocks for 3 new surfaces
- Structural test proves --ax-rise-sm and --ax-rise-md collapse to 0px under reduced-motion
- component_kitchen_live.ex has a Motion reference section listing all 9 surfaces
- All Playwright reduced-motion tests pass
- Test suite stays green
</success_criteria>

<output>
Create `.planning/phases/177-d-motion-micro-interaction-design/177-06-SUMMARY.md` when done.
</output>

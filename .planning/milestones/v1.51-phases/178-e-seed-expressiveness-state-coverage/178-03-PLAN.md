---
phase: 178-e-seed-expressiveness-state-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 178-01
files_modified:
  - examples/accrue_host/priv/repo/seeds/hero_accounts.exs
  - examples/accrue_host/test/accrue_host/hero_accounts_test.exs
  - scripts/ci/accrue_host_seed_e2e.exs
autonomous: true
requirements:
  - SEED-01
  - SEED-02

must_haves:
  truths:
    - "RecoveryLive at-risk table is non-empty after seeding (dunning_campaign_started_at + real subscription id)"
    - "CampaignLive is navigable to /billing/analytics/recovery/campaigns/:past_due_subscription_id"
    - "Dunning events in hero_accounts.exs reference real accrue_subscriptions IDs (not phantom Ecto.UUID.generate() UUIDs)"
    - "hero_accounts_test.exs has a regression assertion that past_due_subscription.id matches dunning event subject_id"
    - "seed_e2e_cleanup_test.exs stays green after extending @fixture_* allowlists"
  artifacts:
    - path: "examples/accrue_host/priv/repo/seeds/hero_accounts.exs"
      provides: "Dunning bug fix — sub_7d/sub_30d/sub_90d use real subscription IDs"
      contains: "past_due_subscription.id"
    - path: "examples/accrue_host/test/accrue_host/hero_accounts_test.exs"
      provides: "Regression test asserting dunning event subject_id == real subscription id"
    - path: "scripts/ci/accrue_host_seed_e2e.exs"
      provides: "Extended @fixture_subscription_processor_ids and @fixture_customer_processor_ids allowlists"
  key_links:
    - from: "hero_accounts.exs (sub_7d/sub_30d/sub_90d)"
      to: "Dunning.at_risk_subscriptions/1"
      via: "subject_id in dunning events must match accrue_subscriptions.id for JOIN to resolve"
      pattern: "past_due_subscription.id"
    - from: "scripts/ci/accrue_host_seed_e2e.exs @fixture_subscription_processor_ids"
      to: "seed_e2e_cleanup_test.exs"
      via: "cleanup deletes by allowlist; unrelated processor_ids must NOT appear in allowlists"
      pattern: "sub_e2e_dunning_at_risk|sub_e2e_canceling|sub_e2e_edge_at_risk|sub_e2e_edge_canceling"
---

<objective>
Fix the dunning bug in hero_accounts.exs (lines 101, 139, 164) where phantom Ecto.UUID.generate() UUIDs were used as dunning event subject_ids instead of real subscription IDs. Add a regression test in hero_accounts_test.exs. Extend the @fixture_* allowlists in accrue_host_seed_e2e.exs for any new e2e_-prefixed processor_ids.

Purpose: RecoveryLive at-risk table was always empty because Dunning.at_risk_subscriptions/1 JOINs on subject_id — phantom UUIDs never matched real subscription rows. This fix closes the RecoveryLive/CampaignLive populated and dunning/at-risk STATE-MATRIX cells for the host dev click-through.
Output: Fixed hero_accounts.exs (3 phantom UUIDs replaced), regression test, clean idempotency.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-CONTEXT.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-RESEARCH.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-PATTERNS.md
@examples/accrue_host/priv/repo/seeds/hero_accounts.exs
@examples/accrue_host/test/accrue_host/hero_accounts_test.exs
@scripts/ci/accrue_host_seed_e2e.exs

<interfaces>
<!-- From hero_accounts.exs (current file) — key patterns around the bug -->

-- Line 31: past_due_subscription is already bound here:
{:ok, %{subscription: past_due_subscription}} = AccrueHost.Billing.billing_state_for(past_due_org)

-- Line 55: canceled_subscription is already bound here:
{:ok, %{subscription: canceled_subscription}} = AccrueHost.Billing.billing_state_for(canceled_org)

-- Lines 101, 139, 164 (BUGGY):
sub_7d = Ecto.UUID.generate()   -- line 101 -- phantom, no subscription row
sub_30d = Ecto.UUID.generate()  -- line 139 -- phantom
sub_90d = Ecto.UUID.generate()  -- line 164 -- phantom

-- After the fix:
sub_7d = past_due_subscription.id   -- links 7d-window recovered event to real sub
sub_30d = canceled_subscription.id  -- links 30d-window exhausted event to real sub
sub_90d = past_due_subscription.id  -- links 90d-window active campaign to real sub

-- record_at/3 uses on_conflict: :nothing keyed on idempotency_key — fix is idempotency-safe.
-- The past_due_subscription and canceled_subscription bindings exist ABOVE the bug lines
-- (lines 31 and 55) so they can be referenced at lines 101/139/164 without re-querying.

<!-- From scripts/ci/accrue_host_seed_e2e.exs (current module attributes) -->
@fixture_subscription_processor_ids ["sub_host_browser_replay", "sub_host_premium_replay"]
@fixture_customer_processor_ids ["cus_host_browser_replay", "cus_host_premium_replay"]

<!-- cleanup_fixture_footprint! deletes by allowlist; adding entries is safe as long as
     the new strings use "e2e_" prefix (never "unrelated_") per PATTERNS.md pitfall #5 -->

<!-- From seed_e2e_cleanup_test.exs — unrelated processor_ids to NOT collide with -->
"cus_unrelated_replay", "sub_unrelated_replay", "si_unrelated_replay", "evt_unrelated_replay"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix dunning bug in hero_accounts.exs — replace phantom UUIDs with real subscription IDs</name>
  <files>examples/accrue_host/priv/repo/seeds/hero_accounts.exs, examples/accrue_host/test/accrue_host/hero_accounts_test.exs</files>
  <behavior>
    - hero_accounts.exs line 101: replace "sub_7d = Ecto.UUID.generate()" with "sub_7d = past_due_subscription.id"
    - hero_accounts.exs line 139: replace "sub_30d = Ecto.UUID.generate()" with "sub_30d = canceled_subscription.id"
    - hero_accounts.exs line 164: replace "sub_90d = Ecto.UUID.generate()" with "sub_90d = past_due_subscription.id"
    - The past_due_subscription binding at line 31 and canceled_subscription binding at line 55 are already in scope — no new queries needed
    - hero_accounts_test.exs gains a new test: "dunning events reference real subscription IDs (not phantom UUIDs)" that calls Code.eval_file("priv/repo/seeds.exs"), then queries Accrue.Events.Event for type "dunning.campaign_started" with subject_type "Subscription", and asserts the subject_id matches a real accrue_subscriptions row (via Repo.get/2 on Subscription returning non-nil)
    - The regression test uses the existing AccrueHost.DataCase setup; it follows the same pattern as the existing hero_accounts_test.exs setup block
  </behavior>
  <action>
    Read examples/accrue_host/priv/repo/seeds/hero_accounts.exs (already in context). Make three targeted line replacements:

    Line 101 — change:
      sub_7d = Ecto.UUID.generate()
    to:
      sub_7d = past_due_subscription.id

    Line 139 — change:
      sub_30d = Ecto.UUID.generate()
    to:
      sub_30d = canceled_subscription.id

    Line 164 — change:
      sub_90d = Ecto.UUID.generate()
    to:
      sub_90d = past_due_subscription.id

    No other changes to hero_accounts.exs.

    In hero_accounts_test.exs, add a second test to the existing module (after the existing test):

    test "dunning campaign_started events have subject_ids that match real subscriptions" do
      alias Accrue.Billing.Subscription
      alias Accrue.Events.Event
      import Ecto.Query

      events = Repo.all(from e in Event, where: e.type == "dunning.campaign_started" and e.subject_type == "Subscription")
      assert length(events) >= 1, "Expected at least 1 dunning.campaign_started event"

      Enum.each(events, fn event ->
        assert Repo.get(Subscription, event.subject_id) != nil,
          "dunning.campaign_started event #{event.idempotency_key} has phantom subject_id #{event.subject_id}"
      end)
    end

    The test runs AFTER Code.eval_file("priv/repo/seeds.exs") in the setup block (already present in hero_accounts_test.exs).
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/hero_accounts_test.exs --seed 0 2>&1 | grep -E "passed|failed|0 failures"</automated>
  </verify>
  <done>Both hero_accounts_test.exs tests pass. The regression test asserts dunning event subject_ids resolve to real Subscription rows. grep "Ecto.UUID.generate" examples/accrue_host/priv/repo/seeds/hero_accounts.exs returns 0 matches on the three former-bug lines (lines 101, 139, 164).</done>
</task>

<task type="auto">
  <name>Task 2: Extend @fixture_* allowlists in accrue_host_seed_e2e.exs + verify cleanup test green</name>
  <files>scripts/ci/accrue_host_seed_e2e.exs</files>
  <action>
    Read scripts/ci/accrue_host_seed_e2e.exs (already in context, first 60 lines shown). Find the @fixture_* module attribute block (lines 24–35) and extend:

    @fixture_subscription_processor_ids — append ALL of the following (covering both E2E fixtures in Plan 02 and host-seed entities in Plan 04):
      - "sub_e2e_dunning_at_risk"   (Plan 02 E2E: at-risk subscription in seed_edge_states!)
      - "sub_e2e_canceling"          (Plan 02 E2E: canceling subscription in seed_edge_states!)
      - "sub_e2e_edge_at_risk"      (Plan 04 host: at-risk sub in edge_states.exs processor_id)
      - "sub_e2e_edge_canceling"    (Plan 04 host: canceling sub in edge_states.exs processor_id)

    @fixture_customer_processor_ids — append "cus_e2e_edge_1" (Plan 04 host: long-name customer in edge_states.exs)

    For Invoice/Charge processor_ids from Plan 04 ("in_e2e_edge_jpy", "ch_e2e_edge_jpy"): these are child rows of the at-risk subscription and customer. Check whether accrue_host_seed_e2e.exs cascade-deletes invoices/charges when their parent subscription is deleted. If the cleanup script deletes by subscription processor_id and cascades, no explicit invoice/charge entries are needed. If invoices/charges are cleaned by their own allowlist, add "in_e2e_edge_jpy" and "ch_e2e_edge_jpy" to the corresponding @fixture_invoice_processor_ids and @fixture_charge_processor_ids lists. Inspect the cleanup_fixture_footprint! function body to determine which strategy applies.

    Note: The overflow processor_ids ("cus_e2e_overflow_1" through "_26") are E2E-side only and use reset!/0 TRUNCATE for cleanup — they do NOT need allowlist entries (the host seed runner only cleans host-side fixtures). Do NOT add overflow IDs to the allowlist.

    The "e2e_" prefix rule from PATTERNS.md pitfall #5 must be respected: never add strings matching "unrelated_" namespace.

    After editing, run seed_e2e_cleanup_test.exs to verify it still passes (the allowlist additions only ADD entries — they cannot accidentally delete unrelated rows because the "unrelated_" processor_ids are in a separate namespace).
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/seed_e2e_cleanup_test.exs --seed 0 2>&1 | grep -E "1 test|passed|0 failures"</automated>
  </verify>
  <done>seed_e2e_cleanup_test.exs passes (1 test, 0 failures). All four subscription processor_ids appear in @fixture_subscription_processor_ids: "sub_e2e_dunning_at_risk", "sub_e2e_canceling", "sub_e2e_edge_at_risk", "sub_e2e_edge_canceling". "cus_e2e_edge_1" appears in @fixture_customer_processor_ids. Invoice/Charge IDs added if not cascade-cleaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host seeds → production DB | seeds.exs is only run via mix ecto.reset or mix run priv/repo/seeds.exs in dev/test — not in production deployments |
| accrue_host_seed_e2e.exs → CI DB | CI-only fixture runner; not part of the production application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03-SC | Tampering | No new packages installed in this plan | accept | No package installs |
| T-178-03-T | Tampering | Extending @fixture_*_processor_ids in seed runner | accept | Allowlist additions only broaden cleanup scope; cannot affect prod (CI-only script); "unrelated_" namespace collision prevented by naming convention |
| T-178-03-I | Information Disclosure | hero_accounts.exs uses real subscription UUIDs in event records | accept | UUIDs are test/dev fixture IDs; no PII; events table is append-only in test DB; no production exposure |

No new attack surface. The /__e2e__/seed/* endpoint is unaffected by this plan (host-side changes only).
</threat_model>

<verification>
- cd examples/accrue_host && mix test test/accrue_host/hero_accounts_test.exs --seed 0 → 2 tests, 0 failures
- cd examples/accrue_host && mix test test/accrue_host/seed_e2e_cleanup_test.exs --seed 0 → 1 test, 0 failures
- grep "past_due_subscription.id" examples/accrue_host/priv/repo/seeds/hero_accounts.exs → at least 2 matches (lines formerly 101, 164)
- grep "canceled_subscription.id" examples/accrue_host/priv/repo/seeds/hero_accounts.exs → at least 1 match (line formerly 139)
- grep -c "Ecto.UUID.generate" examples/accrue_host/priv/repo/seeds/hero_accounts.exs | grep -v "^0$" should return 0 (no remaining phantom UUIDs in dunning event blocks)
</verification>

<success_criteria>
Dunning bug is fixed. hero_accounts_test.exs regression test passes. seed_e2e_cleanup_test.exs stays green. @fixture_* allowlists in the CI runner include the new e2e_-prefixed IDs.
</success_criteria>

<output>
Create .planning/phases/178-e-seed-expressiveness-state-coverage/178-03-SUMMARY.md when done.
</output>

---
phase: 179-f-screenshot-driven-visual-qa-loop-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/e2e/admin-visuals.spec.js
  - accrue_admin/package.json
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "npx playwright test e2e/admin-visuals.spec.js --list reports 1 test visible to both chromium-desktop and chromium-mobile projects"
    - "The shots[] array contains exactly 21 entries with corrected route slugs (/billing/payments, /billing/connect, /billing/analytics/recovery/subscriptions/:id)"
    - "Multi-fixture seed: operator-flows + dashboard + edge-states called without intermediate reset() in the single test"
    - "@anthropic-ai/sdk ^0.100.1 appears in package.json devDependencies"
    - "score-visuals npm script added to package.json scripts"
    - "mix test --seed 0 stays green (262 tests)"
  artifacts:
    - path: "accrue_admin/e2e/admin-visuals.spec.js"
      provides: "21-screen × 4-cell screenshot sweep"
      contains: "shots = ["
    - path: "accrue_admin/package.json"
      provides: "devDependency declaration + npm script"
      contains: "@anthropic-ai/sdk"
  key_links:
    - from: "admin-visuals.spec.js shots[]"
      to: "/__e2e__/seed/edge-states"
      via: "seed(request, 'edge-states') call returning coupon_id, promo_code_id, connect_account_id, at_risk_sub_id, jpy_invoice_id"
      pattern: "edge-states"
    - from: "admin-visuals.spec.js shots[]"
      to: "/billing/payments"
      via: "corrected slug per PATTERNS.md router.ex audit"
      pattern: "/billing/payments"
---

<objective>
Expand the Playwright visual sweep from 12 shots to 21 shots covering all admin screens (including all detail pages) across 4 matrix cells, using the 3-fixture multi-seed pattern from Phase 178. Register @anthropic-ai/sdk as a devDependency and add the score-visuals npm script in preparation for Plan 02.

Purpose: QA-01 — the harness must sweep the full screen inventory across {desktop 1280×900, mobile Pixel5 393×727} × {light, dark}. The chromium-mobile project already exists in playwright.config.js; no config change is needed — only the shots[] array must be expanded.

Output: Updated admin-visuals.spec.js (21 shots, 3 fixtures merged, corrected slugs); updated package.json (devDep + script).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md

<interfaces>
<!-- Confirmed fixture return maps from accrue_admin/test/support/e2e_fixtures.ex -->
<!-- operator-flows → { charge_id, source_event_id, single_webhook_id, bulk_webhook_id } -->
<!-- dashboard      → { customer_id, subscription_id, event_id } -->
<!-- edge-states    → { at_risk_sub_id, canceling_sub_id, jpy_invoice_id, jpy_charge_id,
                        dunning_customer_id, long_name_customer_id, coupon_id,
                        promo_code_id, connect_account_id } -->

<!-- Verified route slugs from accrue_admin/lib/accrue_admin/router.ex (PATTERNS.md lines 22-31): -->
<!-- /billing/payments    (NOT /billing/charges — /charges redirects here) -->
<!-- /billing/connect     (NOT /billing/connect-accounts) -->
<!-- /billing/analytics/recovery/subscriptions/:id  (NOT /campaigns/:id) -->

<!-- Existing helpers to retain verbatim from admin-visuals.spec.js lines 3-28: -->
<!-- reset(request), seed(request, fixture), login(page, target), captureThemes(page, name, project) -->
<!-- Test structure: test.describe / test.beforeEach({ reset }) — retain as-is -->
<!-- testInfo.project.name already namespaces output to test-results/admin-visuals/${project}/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand admin-visuals.spec.js to 21-screen multi-fixture sweep</name>
  <files>accrue_admin/e2e/admin-visuals.spec.js</files>
  <read_first>
    accrue_admin/e2e/admin-visuals.spec.js — read lines 1-65 in full; retain all four helper functions (reset, seed, login, captureThemes) and the test.describe/beforeEach wrapper verbatim.
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md — lines 39-121 (corrected shots[] array with all 21 entries and route slug corrections).
    .planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md — legend section confirming fixture return map keys.
  </read_first>
  <action>
    Replace the single seed("operator-flows") call and 12-entry shots[] with a 3-fixture multi-seed and a 21-entry shots[] array. The four helper functions at the top of the file (reset, seed, login, captureThemes) and the test.describe / beforeEach wrapper must remain unchanged.

    Inside the test body, replace the existing seed call and shots array with:

    Three sequential seed calls — seed(request, "operator-flows"), seed(request, "dashboard"), seed(request, "edge-states") — stored as opFlows, dash, and edge respectively. Do NOT call reset() between these calls — the fixtures use System.unique_integer processor IDs and accumulate safely. The project variable from testInfo.project.name stays on the same line as before.

    The 21-entry shots array (per D-01 QA-01, corrected routes per PATTERNS.md router audit):
    1. ["dashboard",           "/billing"]
    2. ["customers",           "/billing/customers"]
    3. ["customer-detail",     "/billing/customers/${dash.customer_id}"]
    4. ["subscriptions",       "/billing/subscriptions"]
    5. ["subscription-detail", "/billing/subscriptions/${dash.subscription_id}"]
    6. ["invoices",            "/billing/invoices"]
    7. ["invoice-detail",      "/billing/invoices/${edge.jpy_invoice_id}"]
    8. ["payments",            "/billing/payments"]
    9. ["charge-detail",       "/billing/payments/${opFlows.charge_id}"]
    10. ["coupons",            "/billing/coupons"]
    11. ["coupon-detail",      "/billing/coupons/${edge.coupon_id}"]
    12. ["promotion-codes",    "/billing/promotion-codes"]
    13. ["promo-code-detail",  "/billing/promotion-codes/${edge.promo_code_id}"]
    14. ["connect",            "/billing/connect"]
    15. ["connect-detail",     "/billing/connect/${edge.connect_account_id}"]
    16. ["events",             "/billing/events"]
    17. ["event-detail",       "/billing/events/${opFlows.source_event_id}"]
    18. ["webhooks",           "/billing/webhooks"]
    19. ["webhook-detail",     "/billing/webhooks/${opFlows.single_webhook_id}"]
    20. ["recovery",           "/billing/analytics/recovery"]
    21. ["campaign-detail",    "/billing/analytics/recovery/subscriptions/${edge.at_risk_sub_id}"]

    The for loop over shots (login + captureThemes) stays unchanged.

    Update the comment on line 44 to reflect the multi-fixture seed (was "Index surfaces render..."; update to note that detail surfaces use IDs from three merged fixtures).

    DO NOT add any calls to seed("overflow") in this test — overflow creates 26+ rows and makes list screenshots very long; the baseline sweep uses the three standard fixtures only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && npx playwright test e2e/admin-visuals.spec.js --list 2>&1 | grep -c "captures every primary"</automated>
  </verify>
  <done>
    npx playwright test e2e/admin-visuals.spec.js --list shows the test in both chromium-desktop and chromium-mobile projects (2 lines). The file contains shots = [ with 21 entries. Routes /billing/payments, /billing/connect, and /billing/analytics/recovery/subscriptions/ appear in the array. No /billing/charges, /billing/connect-accounts, or /campaigns/ slugs remain. No reset() call appears between the three seed calls inside the test body.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @anthropic-ai/sdk devDependency and score-visuals script to package.json</name>
  <files>accrue_admin/package.json</files>
  <read_first>
    accrue_admin/package.json — read the full file; current devDependencies and scripts sections.
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-RESEARCH.md — Package Legitimacy Audit section confirming @anthropic-ai/sdk 0.100.1 is Approved, no postinstall script.
  </read_first>
  <action>
    Add "@anthropic-ai/sdk": "^0.100.1" to the devDependencies object. This is a devDependency only — never add it to a dependencies key. It is e2e/dev tooling and must not appear in any Hex package or runtime dep (per QA-02 constraint in CONTEXT.md and CLAUDE.md).

    Add "score-visuals": "node e2e/score-visuals.mjs" to the scripts object.

    Run npm install from accrue_admin/ so package-lock.json is updated and the SDK is available for score-visuals.mjs in the next plan.

    Do not change any other field in package.json.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && node -e "const p = require('./package.json'); if (!p.devDependencies['@anthropic-ai/sdk']) throw new Error('missing'); if (!p.scripts['score-visuals']) throw new Error('missing script'); console.log('ok')"</automated>
  </verify>
  <done>
    package.json devDependencies contains "@anthropic-ai/sdk": "^0.100.1". package.json scripts contains "score-visuals": "node e2e/score-visuals.mjs". npm install completed without errors. mix test --seed 0 still reports 262 tests, 0 failures (regression check: no accrue_admin Elixir tests should be affected by a JS dep change, but confirm with a quick run).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | @anthropic-ai/sdk 0.100.1 fetched from npm; trust established via Package Legitimacy Audit in RESEARCH.md (Anthropic official, no postinstall script) |
| e2e spec → /__e2e__/seed endpoint | Test-only route (MIX_ENV=test); must remain excluded from prod and Hex package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | Tampering | @anthropic-ai/sdk npm install | mitigate | Package verified via npm registry + Package Legitimacy Audit; version pinned to ^0.100.1; devDependency only (never in runtime Hex deps) |
| T-179-02 | Information Disclosure | /__e2e__/seed routes | accept | Already MIX_ENV=test-only; compile guard unchanged; this plan makes no changes to e2e_plug.ex |
| T-179-SC | Tampering | npm install side-effects | mitigate | @anthropic-ai/sdk has no postinstall script (RESEARCH.md verified); no slopcheck flag needed |
</threat_model>

<verification>
1. `cd accrue_admin && npx playwright test e2e/admin-visuals.spec.js --list` — shows 2 test entries (one per project: chromium-desktop, chromium-mobile).
2. `grep -c "billing/payments" accrue_admin/e2e/admin-visuals.spec.js` — returns 2 (list + detail).
3. `grep -c "billing/connect-accounts" accrue_admin/e2e/admin-visuals.spec.js` — returns 0 (corrected slug).
4. `grep "score-visuals" accrue_admin/package.json` — shows the script entry.
5. `grep "@anthropic-ai/sdk" accrue_admin/package.json` — shows the devDep entry.
6. `cd accrue_admin && mix test --seed 0` — 262 tests, 0 failures.
</verification>

<success_criteria>
- admin-visuals.spec.js has exactly 21 entries in shots[].
- All three fixtures (operator-flows, dashboard, edge-states) are seeded without intermediate resets.
- Route slugs use /billing/payments, /billing/connect, /billing/analytics/recovery/subscriptions/:id (no /charges, /connect-accounts, /campaigns/).
- package.json devDependencies includes @anthropic-ai/sdk ^0.100.1.
- package.json scripts includes score-visuals.
- Elixir suite stays at 262 green.
</success_criteria>

<output>
Create `.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-01-SUMMARY.md` when done.
</output>

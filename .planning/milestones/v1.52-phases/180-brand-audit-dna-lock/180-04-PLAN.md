---
phase: "180"
plan: "04"
type: execute
wave: 4
depends_on:
  - "180-03"
files_modified:
  - .planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md
  - .planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md
  - .planning/phases/180-brand-audit-dna-lock/logo-brief.md
  - .planning/phases/180-brand-audit-dna-lock/VALIDATION.md
autonomous: false
requirements:
  - AUD-01
  - AUD-02
  - AUD-03
user_setup: []

must_haves:
  truths:
    - "All contested verdicts (REWORK/ADD/REMOVE) have been explicitly accepted, rejected, or modified by the user"
    - "All KEEP/TIGHTEN verdicts have been batch-approved"
    - "BRAND-DNA.md and logo-brief.md have been approved as whole documents (or revised and re-approved inline)"
    - "Any inline revision to DNA or brief preserves the one-page/no-rationale constraint"
    - "Phase 180 is ready to close — Phase 181 can begin using logo-brief.md as its pre-gate spec"
  artifacts:
    - path: ".planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md"
      provides: "Ratified locked brand DNA — downstream phases load this as context"
      contains: "Ratified:"
    - path: ".planning/phases/180-brand-audit-dna-lock/logo-brief.md"
      provides: "Binding logo design brief — Phase 181 pre-gate lints enforce these constraints"
      contains: "Binding"
    - path: ".planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md"
      provides: "Ratified brand audit — all verdicts locked"
      contains: "status: ratified"
  key_links:
    - from: "Phase 181"
      to: "logo-brief.md §Hard Logo Constraints"
      via: "Phase 181 pre-gate lints grep for the 4 constraint strings"
      pattern: "No rectangular background"
    - from: "Phase 184"
      to: "BRAND-AUDIT.md §7"
      via: "Phase 184 tokens.css is authored from §7 token specification"
      pattern: "--accrue-"
    - from: "Phase 185"
      to: "BRAND-AUDIT.md §10, BRAND-DNA.md §Voice"
      via: "Phase 185 voice system is authored from §10 + DNA voice adjectives"
      pattern: "Voice"
    - from: "Phase 186"
      to: "quality-gate-checklist.md"
      via: "Phase 186 BOOK-02 success criterion references this file"
      pattern: "Designer-buildable"
---

<objective>
Present all four Phase 180 artifacts to the user for ratification in a single checkpoint sitting, handle any inline revisions, and finalize the VALIDATION.md.

Purpose: D-07 mandates a single itemized checkpoint at phase end. D-08 specifies the presentation structure: contested verdicts (REWORK/ADD/REMOVE + evidence-gated proposals) as explicit accept/reject items; KEEP/TIGHTEN batch-approved; BRAND-DNA.md and logo-brief.md as whole-document approvals. D-09 authorizes inline revision if any verdict flips. This is a `checkpoint:decision` (not `checkpoint:human-verify`) because the user's verdict determines what the locked downstream artifacts contain — specifically, Phase 181 cannot begin until the logo brief is ratified.

Output: Ratified artifacts, updated VALIDATION.md, phase ready to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/180-brand-audit-dna-lock/180-CONTEXT.md
@.planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md
@.planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md
@.planning/phases/180-brand-audit-dna-lock/logo-brief.md
@.planning/phases/180-brand-audit-dna-lock/quality-gate-checklist.md
@.planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-draft flip-variants for genuinely contested items</name>
  <files>.planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md</files>
  <read_first>
    - .planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md — identify all REWORK, ADD, REMOVE verdicts; assess which 1–3 are genuinely likely to be contested at the checkpoint
    - .planning/phases/180-brand-audit-dna-lock/180-CONTEXT.md — D-09 requires pre-drafting flip-variants for 1–3 contested items before the checkpoint presentation so inline revision can happen without a cold-start; the fallback to staged gates only if >5 contested verdicts
    - .planning/phases/180-brand-audit-dna-lock/180-RESEARCH.md — Pitfall 2 (over-contesting the checkpoint): confirm that only items with cited computable failures are presented as contested; KEEP/TIGHTEN verdicts (usage-rule-only) are batch items, not contested items
  </read_first>
  <action>
    Before the checkpoint task runs, scan BRAND-AUDIT.md for all REWORK, ADD, and REMOVE verdicts. For each:
    1. Confirm it has a cited failure (contrast row, distinctiveness argument, or missing-element rationale)
    2. Identify the 1–3 most likely to generate user feedback

    For each of those 1–3, prepare a flip-variant inline in BRAND-AUDIT.md using an HTML comment block directly below the verdict:
      <!-- FLIP-VARIANT: If user rejects/modifies this verdict, the alternative is:
           [describe the alternative — e.g., "change REWORK to TIGHTEN with usage rule X"]
           DNA impact: [what line in BRAND-DNA.md would change]
           Brief impact: [what line in logo-brief.md would change, if any]
      -->

    This preparation ensures that if a verdict flips during the checkpoint, the inline revision (D-09) can be applied immediately without needing to re-derive the alternative.

    Expected contested count: 0–3 items. If no REWORK/ADD/REMOVE verdicts exist in the audit (all verdicts are KEEP/TIGHTEN), this task is a no-op — note that in the summary.
  </action>
  <verify>
    <automated>grep -c "REWORK\|^.*ADD\b\|REMOVE" .planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md | xargs -I{} sh -c 'echo "Contested verdict count: {}"; exit 0'</automated>
  </verify>
  <done>Flip-variants prepared (as HTML comments) for any REWORK/ADD/REMOVE verdicts; task is a no-op if no contested verdicts exist</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Ratify audit verdicts, BRAND-DNA.md, and logo brief</name>
  <read_first>
    - .planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md — present this as the primary artifact
    - .planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md — present as whole document for approval
    - .planning/phases/180-brand-audit-dna-lock/logo-brief.md — present as whole document for approval
    - .planning/phases/180-brand-audit-dna-lock/quality-gate-checklist.md — present for acknowledgment
    - .planning/phases/180-brand-audit-dna-lock/180-CONTEXT.md — D-08 structures the presentation order
  </read_first>
  <decision>
    Ratify all four Phase 180 artifacts: the 14-section brand audit (all verdicts), the locked BRAND-DNA.md, and the binding logo-brief.md. This gate determines what Phases 181, 184, and 185 build from.
  </decision>
  <context>
    Phase 180 is the cheapest place to disagree — changes here cost nothing; changes after Phase 181 begins cost a tournament round.

    Presentation order (per D-08):

    1. CONTESTED VERDICTS (explicit accept/reject per item)
       Present each REWORK / ADD / REMOVE verdict as a named item with its cited justification.
       For each, offer three options: Accept as written | Reject (drop the verdict, revert to KEEP or TIGHTEN) | Modify (describe the change).
       If a flip-variant comment exists in the audit, present the alternative.

    2. BATCH APPROVAL
       Present all KEEP and TIGHTEN verdicts as a group (summarize the palette usage rules being established — Moss/Cobalt/Amber are UI-accent/large-text-only on light surfaces).
       Ask: "Approve all KEEP and TIGHTEN verdicts as a batch?"

    3. WHOLE-DOCUMENT APPROVAL
       Present BRAND-DNA.md in full — it is a one-page document.
       Ask: "Approve BRAND-DNA.md as the locked brand constraint record?"

       Present logo-brief.md in full.
       Ask: "Approve logo-brief.md as the binding design brief for Phase 181?"

    4. QUALITY-GATE ACKNOWLEDGMENT
       Present quality-gate-checklist.md.
       Ask: "Acknowledge these 8 items as the Phase 186 BOOK-02 quality gate?"

    INLINE REVISION (if any verdict flips per D-09):
       If a contested verdict is rejected or modified, revise BRAND-DNA.md and logo-brief.md inline in this same task before marking the checkpoint complete. Use the flip-variant comment from Task 1 as the guide. The DNA must remain one page; no rationale prose may be added.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve all — all artifacts ratified as written</name>
      <pros>No revisions needed; Phase 181 can begin immediately</pros>
      <cons>Any disagreement must be raised now or deferred to Phase 181 (costly)</cons>
    </option>
    <option id="revise-contested">
      <name>Revise one or more contested verdicts</name>
      <pros>Phase 180 is the cheapest place to disagree</pros>
      <cons>Inline revision extends this task; if >5 verdicts need revision, a second sitting may be needed</cons>
    </option>
  </options>
  <action>Present all four artifacts to the user following the D-08 presentation order (contested verdicts → batch KEEP/TIGHTEN → whole-document BRAND-DNA.md → whole-document logo-brief.md → quality-gate acknowledgment). Apply inline revisions per D-09 if any contested verdict flips. Files that may be revised inline: BRAND-DNA.md and logo-brief.md.</action>
  <verify>
    <human-check>User has responded with "All items resolved" or equivalent; DNA and brief are locked; no open accept/reject items remain</human-check>
  </verify>
  <done>All four Phase 180 artifacts are ratified; BRAND-DNA.md and logo-brief.md reflect the user's final verdicts; Phase 181 may proceed</done>
  <resume-signal>All items resolved — DNA and logo brief are locked. Phase 181 may proceed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize VALIDATION.md for Phase 180</name>
  <files>.planning/phases/180-brand-audit-dna-lock/VALIDATION.md</files>
  <read_first>
    - .planning/phases/180-brand-audit-dna-lock/VALIDATION.md — read the current template stub; fill in all placeholder fields
    - .planning/phases/180-brand-audit-dna-lock/180-RESEARCH.md — lines 718–743 (Validation Architecture section) for the per-requirement test map and automated commands
  </read_first>
  <action>
    Fill in the VALIDATION.md template stub with Phase 180's actual validation data.

    Framework: "None — writing phase; contrast script is the only automation"
    Config file: "none"
    Quick run command: `node .planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js`
    Full suite command: same
    Estimated runtime: ~1 second

    Sampling rate note: "Phase 180 produces Markdown artifacts with no test framework. Automated checks are grep assertions only."

    Per-task verification map (fill in the table):
    - 180-01-01 | 01 | 1 | AUD-03 | T-180-01 | Script exits 0, emits 21 rows | cli | `node artifacts/contrast.js \| grep -c "vs "` == 21 | ❌ W0 | pending
    - 180-01-02 | 01 | 1 | AUD-03 | T-180-01 | Paper vs Moss row = 3.03:1 | grep | `grep -q "Paper vs Moss: 3.03:1" artifacts/contrast-table.txt` | ❌ W0 | pending
    - 180-02-01 | 02 | 2 | AUD-01 | T-180-03 | BRAND-AUDIT.md §1–§4 substantive | grep | `grep -q "No rectangular background" BRAND-AUDIT.md` | ❌ W0 | pending
    - 180-02-02 | 02 | 2 | AUD-01/AUD-03 | T-180-03 | §5–§8 complete, --accrue-* documented | grep | `grep -q "\-\-accrue-ink" BRAND-AUDIT.md` | ❌ W0 | pending
    - 180-03-01 | 03 | 3 | AUD-01 | T-180-04 | §9–§14 authored, status: ratified | grep | `grep -q "§14" BRAND-AUDIT.md && grep -q "status: ratified" BRAND-AUDIT.md` | ❌ W0 | pending
    - 180-03-02 | 03 | 3 | AUD-02 | T-180-06/07 | DNA/brief/checklist created | grep | `test -f BRAND-DNA.md && test -f logo-brief.md && test -f quality-gate-checklist.md` | ❌ W0 | pending
    - 180-04-CP | 04 | 4 | AUD-02 | — | User ratified all artifacts | manual | AskUserQuestion checkpoint | ❌ W0 | pending

    Manual-only verifications table: one row — "User ratification | AUD-02 | Human decision required | Type 'All items resolved' after reviewing all four artifacts at the checkpoint"

    After filling in the table, update:
    - `nyquist_compliant: true`
    - `wave_0_complete: true`
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/180-brand-audit-dna-lock/VALIDATION.md && grep -q "node .planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js" .planning/phases/180-brand-audit-dna-lock/VALIDATION.md && echo "VALIDATION.md complete" || echo "FAIL: VALIDATION.md not filled"</automated>
  </verify>
  <done>VALIDATION.md is filled with all placeholder fields replaced; nyquist_compliant and wave_0_complete set to true; quick run command points to contrast.js</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User decision input | User ratifies audit verdicts at the checkpoint; no code executes from user input |
| Artifact propagation | Ratified DNA + brief are loaded as context by Phases 181/184/185 — content integrity matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-08 | Tampering | BRAND-DNA.md post-ratification | accept | Planning artifact in .planning/; Git history provides integrity trail; downstream phases read it as context, not execute it |
| T-180-09 | Repudiation | Checkpoint verdict record | mitigate | The checkpoint task captures the user's decision inline; the SUMMARY.md records what was accepted/rejected/modified, providing an audit trail for future phases |
| T-180-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After all tasks complete:
- Checkpoint resolved: user has approved all four artifacts (or approved with inline revisions)
- `grep -q "Ratified:" .planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md` exits 0
- `grep -q "Binding" .planning/phases/180-brand-audit-dna-lock/logo-brief.md` exits 0
- `grep -q "status: ratified" .planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md` exits 0
- `grep -c "\- \[ \]" .planning/phases/180-brand-audit-dna-lock/quality-gate-checklist.md` reports 8
- `grep -q "nyquist_compliant: true" .planning/phases/180-brand-audit-dna-lock/VALIDATION.md` exits 0
- Phase 181 dependency is satisfied: `grep -q "No rectangular background" .planning/phases/180-brand-audit-dna-lock/logo-brief.md` exits 0
</verification>

<success_criteria>
- User has explicitly ratified: all contested verdicts (accept/reject/modify), KEEP/TIGHTEN batch, BRAND-DNA.md whole document, logo-brief.md whole document, quality-gate-checklist.md acknowledgment
- BRAND-DNA.md is locked — concrete values only, one-page format preserved, inline revisions applied if any verdict flipped
- logo-brief.md is locked — 4 hard constraints verbatim (Phase 181 pre-gate lints will grep for them)
- AUD-01: 14-section audit ratified
- AUD-02: BRAND-DNA.md and binding logo brief ratified at explicit checkpoint before any logo work
- AUD-03: all palette verdicts evidence-gated via contrast-table.txt; ratified at checkpoint
- VALIDATION.md filled in and nyquist_compliant
- Phase 180 is complete; Phase 181 (logo pipeline) may begin
</success_criteria>

<output>
Create `.planning/phases/180-brand-audit-dna-lock/180-04-SUMMARY.md` when done
</output>

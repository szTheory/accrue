---
phase: 185-voice-microcopy-marketing-copy
plan: 03
type: execute
wave: 2
depends_on:
  - "185-01"
  - "185-02"
files_modified:
  - brandbook/voice.md
  - brandbook/copy.md
autonomous: false
requirements:
  - COPY-01
  - COPY-02

must_haves:
  truths:
    - "User has reviewed brandbook/voice.md and brandbook/copy.md in one batch"
    - "User has approved the content or provided specific revision requests"
    - "If revisions were requested, they are incorporated and committed before Phase 185 closes"
    - "The final committed copy reflects the approved batch"
  artifacts:
    - path: "brandbook/voice.md"
      provides: "Approved voice system"
      contains: "## Tone Sliders"
    - path: "brandbook/copy.md"
      provides: "Approved copy blocks"
      contains: "## GitHub"
  key_links:
    - from: "user approval"
      to: "Phase 186"
      via: "Approved copy.md and voice.md are consumed by Phase 186 HTML brand book assembly"
      pattern: "approved"
---

<objective>
Present the complete Phase 185 output — `brandbook/voice.md` and `brandbook/copy.md` — to the user for one-batch review and approval.

Purpose: Phase 185 success criterion #3 requires user review and approval of all copy blocks in a single batch. This checkpoint ensures the human who owns the brand voice and public communications has confirmed the copy before it is embedded in the Phase 186 HTML brand book and applied to live surfaces.

Output: User approval (or revision requests incorporated and re-approved). Phase 185 closes only after the approved batch is committed.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/phases/185-voice-microcopy-marketing-copy/185-CONTEXT.md
@/Users/jon/projects/accrue/brandbook/voice.md
@/Users/jon/projects/accrue/brandbook/copy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-review source assertions (automated gate)</name>
  <files></files>

  <read_first>
  `brandbook/voice.md` — read the full file to verify it is present and complete before surfacing to the user.
  `brandbook/copy.md` — read the full file to verify it is present and complete before surfacing to the user.
  </read_first>

  <action>
  Run the automated gate assertions from Plans 01 and 02 to confirm both files are complete and machine-verifiable before the human review step.

  Commands to run (both must exit 0):

  Gate 1 — voice.md:
  - `test -f brandbook/voice.md`
  - `grep -q "## Tone Sliders" brandbook/voice.md`
  - `grep -q "## Surface Dispatch Rule" brandbook/voice.md`
  - `grep -q "## Voice Principles" brandbook/voice.md`
  - `grep -q "## Claims Posture" brandbook/voice.md`
  - `grep -q "## CTAs" brandbook/voice.md`
  - `grep -q "production-grade" brandbook/voice.md` (must be present — it's in the Avoid list)
  - `grep -q "Get started" brandbook/voice.md` (must be present — approved CTA)

  Gate 2 — copy.md:
  - `test -f brandbook/copy.md`
  - `grep -q "^## GitHub" brandbook/copy.md`
  - `grep -q "Billing state, modeled clearly — the Elixir billing library for Phoenix apps" brandbook/copy.md`
  - `grep -q "stripity_stripe" brandbook/copy.md`
  - `grep -q "^## Microcopy" brandbook/copy.md`
  - `grep -q "^## Release" brandbook/copy.md`

  If any assertion fails, fix the failing file before proceeding to the human review task.

  After both gates pass, display a compact summary for the user:
  - voice.md line count
  - copy.md line count
  - List of surfaces covered in copy.md (GitHub, Hex.pm, HexDocs, README Hero, Landing Page, Release Notes, Microcopy)
  - Reminder of what's locked vs discretionary (the locked strings and the discretionary prose)
  </action>

  <verify>
  <automated>
  test -f /Users/jon/projects/accrue/brandbook/voice.md && \
  test -f /Users/jon/projects/accrue/brandbook/copy.md && \
  grep -q "## Tone Sliders" /Users/jon/projects/accrue/brandbook/voice.md && \
  grep -q "## Surface Dispatch Rule" /Users/jon/projects/accrue/brandbook/voice.md && \
  grep -q "Billing state, modeled clearly — the Elixir billing library for Phoenix apps" /Users/jon/projects/accrue/brandbook/copy.md && \
  grep -q "stripity_stripe" /Users/jon/projects/accrue/brandbook/copy.md && \
  echo "Pre-review gates passed"
  </automated>
  </verify>

  <acceptance_criteria>
  - All automated gate assertions exit 0
  - Both files are present and non-empty
  - A compact review summary is displayed to the user (counts, surfaces covered)
  </acceptance_criteria>

  <done>
  Automated gates pass. User is ready to be presented with the review checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: One-batch user review and approval of voice.md + copy.md</name>

  <read_first>
  `brandbook/voice.md` — read and present the full content to the user.
  `brandbook/copy.md` — read and present the full content to the user.
  `.planning/phases/185-voice-microcopy-marketing-copy/185-CONTEXT.md` — have this available to cite locked decisions (D-01..D-11) if the user questions a choice.
  </read_first>

  <what-built>
  Plan 01 created `brandbook/voice.md` — the Accrue voice system document encoding:
  - Voice principles (measured, exact, native, durable)
  - Do/Don't table
  - Tone sliders (Formal↔Casual anchor=3, Precise↔Evocative anchor=4) with per-surface delta table
  - Surface dispatch rule (tagline vs descriptor by context)
  - Vocabulary use/avoid lists
  - Claims posture (proof-led, mechanism-not-adjective)
  - CTA canon (approved + banned)

  Plan 02 created `brandbook/copy.md` — ready-to-paste copy blocks for:
  - GitHub repo description + topics
  - Hex.pm package description
  - HexDocs intro paragraph
  - README hero (approved verbatim from current README)
  - Landing page (hero / problem / solution / install / benefits / comparison / CTAs)
  - Release note template + changelog voice pattern
  - Microcopy (error / empty / success states)
  </what-built>

  <how-to-verify>
  Review the two files in one pass:

  1. Open `brandbook/voice.md` — read through all sections.
     Key locked items to verify:
     - Tone slider anchors: Formal↔Casual = 3, Precise↔Evocative = 4 (these are locked by D-08)
     - Per-surface delta table: 5 rows with the values from D-09
     - Surface dispatch rule: tagline for on-page, descriptor for search-indexed/out-of-context (D-07)
     - Banned adjectives in Avoid list: production-grade, batteries-included, bank-grade, modern alternative
     - Approved CTAs: "Get started", "Install"; Banned: "Start free", "Try it now", "Get billing in minutes"

  2. Open `brandbook/copy.md` — read through all surface sections.
     Key locked items to verify:
     - GitHub description: exactly `Billing state, modeled clearly — the Elixir billing library for Phoenix apps` (D-05)
     - Hex.pm description: leads with "Elixir" category noun, lists feature nouns, ≤ 300 chars (D-05)
     - Comparison table: only stripity_stripe + Raw Stripe API rows; NO Bling row (D-02, D-03)
     - Pay + Cashier credited in landing prose, not as table rows (D-01)
     - No banned adjectives in any copy block (D-10)
     - Primary CTA is "Get started" or "Install"; sub-CTA has MIT + version spec (D-11)
     - Microcopy examples are direct and actionable (no apologetic softening)

  3. For discretionary content (exact wording of prose blocks, the HexDocs intro paragraph, landing page body copy, error message text beyond the locked patterns) — this is your call. The executor drafted it to the voice register; revise any block that doesn't feel right to you.

  Respond with one of:
  - "Approved" — both files are accepted as committed
  - Specific revision requests — list each change by file + section + what to change. The executor will incorporate all revisions in one pass and re-present for a final check.
  </how-to-verify>

  <resume-signal>
  Type "approved" if both files are accepted. Or describe your revision requests (file + section + change) and the executor will incorporate them and return for re-approval.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Incorporate revisions (if any) and commit approved batch</name>
  <files>brandbook/voice.md, brandbook/copy.md</files>

  <read_first>
  The user's review response from Task 2. If "approved" with no changes, skip to the commit step. If revisions were requested, read the specific revision instructions before touching the files.
  </read_first>

  <action>
  If the user approved with no changes: proceed directly to the git commit step below.

  If the user requested revisions:
  1. Read each requested change carefully.
  2. Open the target file (voice.md or copy.md or both).
  3. Apply ONLY the requested changes — do not re-draft sections the user did not flag.
  4. Re-run the automated gate assertions from Task 1 after edits to confirm no required content was accidentally removed.
  5. Present the revised sections to the user with a brief note: "Revisions applied to [section]. Please confirm."
  6. Once the user confirms, proceed to the commit step.

  NEVER incorporate revisions that would violate the locked decisions D-01..D-11:
  - Never add a Bling comparison row
  - Never change the exact GitHub description string
  - Never change tone slider anchor values
  - Never add banned adjectives to copy blocks
  - Never add banned CTAs

  If a user revision request conflicts with a locked decision, note the conflict: "This revision conflicts with [D-XX]: [reason]. I can implement it in a modified form that preserves the locked constraint, or flag it for your explicit override. How would you like to proceed?"

  Git commit (after approval confirmed):
  Stage both files and commit:
  `git add brandbook/voice.md brandbook/copy.md`
  `git commit -m "feat(185): voice system + copy blocks for all adopter-facing surfaces"`

  Confirm commit SHA is recorded.
  </action>

  <verify>
  <human-check>
  Confirm the commit exists:
  `git log --oneline -1 -- brandbook/voice.md brandbook/copy.md`
  Should show the Phase 185 commit message.

  Both files should be at the version the user approved — no uncommitted edits.
  </human-check>
  </verify>

  <acceptance_criteria>
  - Any user-requested revisions are applied (only the requested changes, nothing more)
  - Revised files still pass the automated gate assertions from Task 1
  - Locked decisions D-01..D-11 are not violated by any revision
  - Both files are committed: `git log -- brandbook/voice.md brandbook/copy.md` shows the Phase 185 commit
  - Phase 185 success criterion #3 is satisfied: "the final committed copy reflects the approved batch"
  </acceptance_criteria>

  <done>
  Both `brandbook/voice.md` and `brandbook/copy.md` are committed at the user-approved version. Phase 185 success criterion #3 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan runs automated gate checks and a human-review gate; no code is executed, no network calls, no user input processed by any application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

This plan performs automated assertions (bash grep/test commands) and a human review gate on static markdown files. There is no executable code, no network surface, and no user-provided data processed by any application. There are no in-scope STRIDE threats.
</threat_model>

<verification>
Overall Phase 185 verification:

1. `test -f brandbook/voice.md && wc -l brandbook/voice.md` — exists, ≤ 250 lines
2. `test -f brandbook/copy.md && wc -l brandbook/copy.md` — exists, ≤ 350 lines
3. `grep -q "## Tone Sliders" brandbook/voice.md` — voice system structurally complete
4. `grep -q "Billing state, modeled clearly — the Elixir billing library for Phoenix apps" brandbook/copy.md` — locked D-05 string present
5. `grep -q "stripity_stripe" brandbook/copy.md` — comparison table correct
6. `git log --oneline -1 -- brandbook/voice.md brandbook/copy.md` — Phase 185 commit exists
7. User approval on record in the Task 2 checkpoint response
</verification>

<success_criteria>
Phase 185 complete when:
- `brandbook/voice.md` committed, ≤ 250 lines, all 7 sections present, D-08/D-09/D-10/D-11 constraints encoded
- `brandbook/copy.md` committed, ≤ 350 lines, all 7 surface sections present, D-05 exact string verbatim, D-02/D-03 comparison table correct, no banned adjectives or CTAs
- User approval confirmed at Task 2 checkpoint
- Any revisions incorporated and re-confirmed
- COPY-01 and COPY-02 both satisfied
</success_criteria>

<output>
Create `.planning/phases/185-voice-microcopy-marketing-copy/185-03-SUMMARY.md` when done.
</output>

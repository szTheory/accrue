---
phase: 188-foundations-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - 188-02
files_modified:
  - accrue_admin/assets/css/theme.css
  - accrue_admin/assets/css/app.css
autonomous: true
requirements:
  - FND-02
  - FND-06
must_haves:
  truths:
    - "D-07: The exact semantic layer scale exists: base 0, sticky 100, dropdown 200, popover 300, drawer 400, modal 500, toast 600."
    - "D-08 through D-10: overlay and sticky selectors consume semantic layer tokens, with no ad-hoc overlay z-index literals."
    - "D-23 through D-25: remaining transition/animation surfaces use existing motion atoms and bundles; no new motion token family is introduced."
    - "D-24: reduced motion collapses travel, transform, and overshoot while preserving opacity crossfades."
  artifacts:
    - path: "accrue_admin/assets/css/theme.css"
      provides: "Formal layer scale and existing motion atoms/bundles"
      contains: "--ax-z-toast: 600"
    - path: "accrue_admin/assets/css/app.css"
      provides: "Layer consumers and motion-tokenized selectors"
      contains: "z-index: var(--ax-z-"
  key_links:
    - from: "accrue_admin/assets/css/app.css"
      to: "accrue_admin/assets/css/theme.css"
      via: "z-index: var(--ax-z-{role})"
      pattern: "z-index: var\\(--ax-z-"
    - from: "accrue_admin/assets/css/app.css"
      to: "accrue_admin/assets/css/theme.css"
      via: "transition bundles and duration/easing atoms"
      pattern: "--ax-transition-|--ax-dur-|--ax-ease-"
---

<objective>
Formalize the layer stack and close motion-token drift at the root CSS level.

Purpose: FND-02 and FND-06 are shared foundations. This plan gives sticky/overlay surfaces a single layer vocabulary and routes remaining motion through the existing v1.51 atoms and bundles without introducing a parallel motion system.

Output:
- Exact `--ax-z-*` layer scale in `theme.css`
- Overlay/sticky consumers migrated in `app.css`
- Raw motion literals routed through existing tokens
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/accrue_admin/guides/motion.md

<interfaces>
Required layer stack from D-07:
- `--ax-z-base: 0`
- `--ax-z-sticky: 100`
- `--ax-z-dropdown: 200`
- `--ax-z-popover: 300`
- `--ax-z-drawer: 400`
- `--ax-z-modal: 500`
- `--ax-z-toast: 600`

Role mapping from D-08:
- sticky: topbar, skip link, mobile sidebar
- dropdown: dropdown menus
- popover: tab-more/help/dev floating panels and non-menu floats
- drawer: detail drawers
- modal: command palette, step-up auth, modal dialogs
- toast: flash/toast surfaces
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace partial z-index tokens with semantic layers</name>
  <files>accrue_admin/assets/css/theme.css, accrue_admin/assets/css/app.css</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RUBRIC.md
  </read_first>
  <action>In `theme.css`, replace the current partial layer values `--ax-z-topbar: 10`, `--ax-z-popover: 20`, `--ax-z-drawer: 30`, and `--ax-z-modal: 40` with the exact D-07 scale: `--ax-z-base: 0`, `--ax-z-sticky: 100`, `--ax-z-dropdown: 200`, `--ax-z-popover: 300`, `--ax-z-drawer: 400`, `--ax-z-modal: 500`, and `--ax-z-toast: 600`. Per D-09, if retaining compatibility for old selectors, define only `--ax-z-topbar: var(--ax-z-sticky)` with a comment containing `deprecated compatibility alias; use --ax-z-sticky`. In `app.css`, migrate these consumers per D-08: `.ax-topbar`, `.ax-skip-link:focus`, and `html.accrue-admin.ax-shell-nav-open .ax-sidebar` use `z-index: var(--ax-z-sticky)`; `.ax-dropdown-panel` uses `var(--ax-z-dropdown)`; tab-more, help, dev toolbar, and non-menu floating panels use `var(--ax-z-popover)`; `.ax-detail-drawer-shell` uses `var(--ax-z-drawer)`; command palette, step-up auth, and modal dialog shells use `var(--ax-z-modal)`; flash and toast surfaces use `var(--ax-z-toast)`. Remove overlay literals like `z-index: 10`, `z-index: 20`, `z-index: 30`, and `z-index: 40`. Keep only local micro-stacking literals `-1`, `0`, or `1` when the selector is an isolated component and the declaration is next to `isolation: isolate` plus an inline `ax-z-micro-stack` comment. Per D-11, do not create a broad portal abstraction or page-flow overlay rewrite in this plan; if a stacking-context trap remains after root layer token migration, record the specific selector and Phase 191 interaction owner in the summary.</action>
  <verify>
    <automated>bash -lc 'for pair in "--ax-z-base: 0" "--ax-z-sticky: 100" "--ax-z-dropdown: 200" "--ax-z-popover: 300" "--ax-z-drawer: 400" "--ax-z-modal: 500" "--ax-z-toast: 600"; do grep -q -- "$pair" accrue_admin/assets/css/theme.css || exit 1; done; ! grep -Eq "z-index:[[:space:]]*(10|20|30|40|999|1000)" accrue_admin/assets/css/app.css; grep -q "z-index: var(--ax-z-sticky)" accrue_admin/assets/css/app.css && grep -q "z-index: var(--ax-z-dropdown)" accrue_admin/assets/css/app.css && grep -q "z-index: var(--ax-z-modal)" accrue_admin/assets/css/app.css'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `theme.css` contains the exact D-07 layer values from base through toast.
- Source assertion: any `--ax-z-topbar` declaration maps to `var(--ax-z-sticky)` and includes `deprecated compatibility alias; use --ax-z-sticky`.
- Source assertion: `app.css` contains `z-index: var(--ax-z-sticky)`, `z-index: var(--ax-z-dropdown)`, `z-index: var(--ax-z-popover)`, `z-index: var(--ax-z-drawer)`, `z-index: var(--ax-z-modal)`, and `z-index: var(--ax-z-toast)` for representative consumers.
- Source assertion: `app.css` contains no overlay z-index literals `10`, `20`, `30`, `40`, `999`, or `1000`.
  </acceptance_criteria>
  <done>The admin CSS has one semantic layer vocabulary and overlay/sticky selectors consume it.</done>
</task>

<task type="auto">
  <name>Task 2: Route motion gaps through existing tokens</name>
  <files>accrue_admin/assets/css/theme.css, accrue_admin/assets/css/app.css</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/accrue_admin/guides/motion.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
  </read_first>
  <action>Keep the existing motion atoms and bundles in `theme.css`: `--ax-dur-*`, `--ax-ease-*`, `--ax-rise-*`, `--ax-press-scale`, and `--ax-transition-*`. Do not add a new motion token family. In `app.css`, replace remaining raw transition and animation timing/easing declarations with existing tokens. Keep the documented skeleton exception exactly as a skeleton loading loop: `animation: ax-skeleton-shimmer 1.4s var(--ax-ease-inout) infinite`, with reduced motion setting `.ax-skeleton { animation: none; background: var(--ax-sunken); }`. Preserve reduced-motion behavior in `theme.css`: `--ax-rise-sm: 0px`, `--ax-rise-md: 0px`, `--ax-press-scale: 1`, `--ax-ease-emphasis: var(--ax-ease-out)`, travel/transform durations collapsed through existing duration variables, and opacity/color crossfades still perceptible through `--ax-transition-colors` and opacity transitions. Remove `transition: all`, raw local `cubic-bezier()`, and raw `ms`/`s` timing literals from `app.css` outside the skeleton exception and comments quoting LiveView JS examples.</action>
  <verify>
    <automated>bash -lc '! grep -qE "transition:[[:space:]]*all\\b" accrue_admin/assets/css/app.css && ! grep -qE "cubic-bezier\\(" accrue_admin/assets/css/app.css && ! grep -E "(transition|animation):[^;]*[0-9]+(ms|s)\\b" accrue_admin/assets/css/app.css | grep -qv "ax-skeleton-shimmer" && grep -q -- "--ax-rise-sm: 0px" accrue_admin/assets/css/theme.css && grep -q -- "--ax-press-scale: 1" accrue_admin/assets/css/theme.css'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `app.css` contains no `transition: all`.
- Source assertion: `app.css` contains no raw `cubic-bezier(` outside comments copied from docs.
- Source assertion: raw `ms`/`s` timing literals in `transition` or `animation` declarations are absent except the `ax-skeleton-shimmer 1.4s` loading loop.
- Source assertion: `theme.css` retains reduced-motion values `--ax-rise-sm: 0px`, `--ax-rise-md: 0px`, `--ax-press-scale: 1`, and `--ax-ease-emphasis: var(--ax-ease-out)`.
- Source assertion: no new custom property prefix such as `--ax-motion2`, `--ax-anim`, or `--ax-spring` is introduced.
  </acceptance_criteria>
  <done>Motion coverage uses the existing token family and reduced-motion behavior remains aligned with D-23 through D-25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layer tokens -> overlay surfaces | z-index values decide whether sticky, popover, drawer, modal, and toast surfaces can be reached. |
| Motion tokens -> user preference | `prefers-reduced-motion` crosses from OS/browser preference into CSS behavior. |
| CSS comments/examples -> verifier grep | LiveView example comments must not become false source of truth for raw timing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-03-01 | Spoofing | overlay stack | mitigate | Use one ordered layer scale so controls cannot appear above or below the wrong surface due to local literals. |
| T-188-03-02 | Denial of Service | modal/drawer usability | mitigate | Assign drawer/modal/toast layers above sticky/dropdown/popover tokens and avoid a broad portal rewrite in this foundation plan. |
| T-188-03-03 | Tampering | motion policy | mitigate | Reuse existing motion atoms and fail raw `transition: all`, raw easing, and raw duration declarations. |
| T-188-03-04 | Information Disclosure | generated evidence | accept | This plan changes CSS only and creates no screenshots, traces, or data outputs. |
| T-188-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; verification uses shell grep only. |
</threat_model>

<verification>
Run:
- `bash -lc 'for pair in "--ax-z-base: 0" "--ax-z-sticky: 100" "--ax-z-dropdown: 200" "--ax-z-popover: 300" "--ax-z-drawer: 400" "--ax-z-modal: 500" "--ax-z-toast: 600"; do grep -q -- "$pair" accrue_admin/assets/css/theme.css || exit 1; done'`
- `bash -lc '! grep -Eq "z-index:[[:space:]]*(10|20|30|40|999|1000)" accrue_admin/assets/css/app.css'`
- `bash -lc '! grep -qE "transition:[[:space:]]*all\\b|cubic-bezier\\(" accrue_admin/assets/css/app.css'`
</verification>

<success_criteria>
Plan 03 is complete when FND-02 and FND-06 have root CSS implementation: the layer stack is tokenized, overlay/sticky consumers use it, and motion drift is routed through the existing token family with reduced-motion behavior preserved.
</success_criteria>

<output>
Create `.planning/phases/188-foundations-hardening/188-03-SUMMARY.md` when done.
</output>

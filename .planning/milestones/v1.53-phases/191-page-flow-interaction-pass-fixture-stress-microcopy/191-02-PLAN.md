---
phase: 191-page-flow-interaction-pass-fixture-stress-microcopy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/test/support/e2e_plug.ex
  - accrue_admin/test/support/e2e_fixtures.ex
  - accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - SEED-01
  - SEED-02
must_haves:
  truths:
    - "E2E tests can seed Phase 191 matrix records in one request per D-10."
    - "Phase 191 E2E fixtures include null optional fields, boundary pagination, high counts, non-ASCII names, and route IDs without manual database edits."
    - "Permission-denied remains test-reachable through existing member login without bypassing production auth."
  artifacts:
    - path: "accrue_admin/test/support/e2e_fixtures.ex"
      provides: "seed_phase191_matrix!/0 and returned route IDs"
      exports: ["seed_phase191_matrix!"]
    - path: "accrue_admin/test/support/e2e_plug.ex"
      provides: "POST /seed/phase191-matrix and /__e2e__/seed/phase191-matrix"
    - path: "accrue_admin/test/accrue_admin/e2e_fixtures_test.exs"
      provides: "ExUnit coverage for deterministic Phase 191 E2E fixtures"
  key_links:
    - from: "accrue_admin/e2e/admin-page-flow-phase191.spec.js"
      to: "accrue_admin/test/support/e2e_plug.ex"
      via: "POST /__e2e__/seed/phase191-matrix"
      pattern: "__e2e__/seed/phase191-matrix"
  prohibitions:
    - statement: "Fixture reachability must not add production forced-state routes or public API breaks."
      status: "resolved"
      verification: "Only test/support E2E plug and fixtures are modified."
---

<objective>
Add deterministic E2E fixture reachability for Phase 191 page-state cells.

Purpose: Page-flow tests need route-backed data for missing optional fields, boundary/high-count cases, non-ASCII values, and stable permission/empty/error/reconnect assertions.
Output: E2E seed endpoint, fixture helper, and ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-CONTEXT.md
@.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-UI-SPEC.md
@accrue_admin/test/support/e2e_plug.ex
@accrue_admin/test/support/e2e_fixtures.ex
@accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
@accrue_admin/e2e/baseline-manifest.js
</context>

## Artifacts this phase produces

- New function: `AccrueAdmin.E2E.Fixtures.seed_phase191_matrix!/0`.
- New E2E endpoints: `POST /seed/phase191-matrix` and `POST /__e2e__/seed/phase191-matrix`.
- Returned fixture fields: `phase191_customer_id`, `phase191_subscription_id`, `phase191_invoice_id`, `phase191_charge_id`, `phase191_coupon_id`, `phase191_promo_code_id`, `phase191_connect_account_id`, `phase191_event_id`, `phase191_webhook_id`, `phase191_at_risk_sub_id`, plus count/boundary metadata.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 191 E2E fixture tests first</name>
  <read_first>
    - @accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
    - @accrue_admin/test/support/e2e_fixtures.ex
    - @.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-UI-SPEC.md
  </read_first>
  <files>accrue_admin/test/accrue_admin/e2e_fixtures_test.exs</files>
  <behavior>
    - Test asserts `seed_phase191_matrix!/0` creates stable route IDs for every manifest detail placeholder used by Phase 191.
    - Test asserts non-ASCII customer/coupon/promotion metadata renders from persisted records.
    - Test asserts boundary pagination counts include zero, one-row, and more-than-one-page cases.
    - Test asserts reset followed by reseed yields deterministic counts.
  </behavior>
  <action>Add focused ExUnit tests for the Phase 191 E2E fixture contract before implementation. Cover D-09, D-10, and D-11: deterministic namespace, one-click route IDs, missing optional fields, high counts, non-ASCII names, and existing edge-state preservation. Keep tests inside existing E2E fixture test support.</action>
  <acceptance_criteria>
    - Tests fail before `seed_phase191_matrix!/0` exists.
    - Tests name `e2e_phase191` namespace expectations.
    - Tests do not query production routes or require manual database edits.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs</automated>
  </verify>
  <done>Fixture tests define the exact deterministic contract for Phase 191 reachability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement seed_phase191_matrix! and E2E endpoints</name>
  <read_first>
    - @accrue_admin/test/support/e2e_fixtures.ex
    - @accrue_admin/test/support/e2e_plug.ex
    - @accrue_admin/e2e/baseline-manifest.js
    - @.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-CONTEXT.md
  </read_first>
  <files>accrue_admin/test/support/e2e_fixtures.ex, accrue_admin/test/support/e2e_plug.ex</files>
  <behavior>
    - `seed_phase191_matrix!/0` calls `reset!()` once and creates all returned IDs deterministically.
    - New records use clear `e2e_phase191_*` processor IDs, emails, raw bodies, event IDs, and labels per D-09.
    - Existing dashboard/operator-flows/edge-states seeds continue to work.
    - Plug exposes both scoped and `/__e2e__` endpoint variants consistent with existing seed routes.
  </behavior>
  <action>Implement the fixture helper and endpoints. Use existing insert helpers and Accrue schema changesets; add only the smallest data required for PAGE-01 through PAGE-04 and SEED-01 through SEED-02. Preserve member-login permission probing per D-11 rather than adding production permission bypasses. Return all IDs the Phase 191 route resolver needs.</action>
  <acceptance_criteria>
    - `POST /__e2e__/seed/phase191-matrix` returns JSON with route IDs for every detail route placeholder in `baseline-manifest.js`.
    - Reset plus seed plus seed again produces stable counts and no duplicate rows in the test database.
    - Fixture includes null/missing optional fields, one-row and multi-page list data, high counts, non-ASCII values, webhook failure, and at-risk recovery references.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs</automated>
  </verify>
  <done>Phase 191 E2E fixture endpoint and helper are deterministic, route-backed, and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser test -> E2E plug | Test client can seed/reset only in the test support server. |
| E2E fixture helper -> test database | Fixture helper writes billing rows for deterministic tests. |
| Member-login test route -> auth adapter | Non-admin reachability remains test-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-04 | Elevation of Privilege | `e2e_plug.ex` | mitigate | Keep endpoints under `test/support`; do not modify production router/auth. |
| T-191-05 | Tampering | `seed_phase191_matrix!/0` | mitigate | Call `reset!()` and use deterministic namespaced IDs; tests assert repeated seed counts. |
| T-191-06 | Information Disclosure | Fixture copy/data | mitigate | Use synthetic `e2e_phase191_*` identifiers only; no real secrets or Stripe keys. |
| T-191-SC | Tampering | package installs | mitigate | No package installs or lockfile changes. |
</threat_model>

<verification>
Run `cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs`.
</verification>

<success_criteria>
The Phase 191 E2E matrix seed is reachable through one test endpoint, returns deterministic route IDs, and is covered by ExUnit.
</success_criteria>

<output>
Create `.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-02-SUMMARY.md` when done.
</output>

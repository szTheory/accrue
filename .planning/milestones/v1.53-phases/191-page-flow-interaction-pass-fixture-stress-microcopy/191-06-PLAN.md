---
phase: 191-page-flow-interaction-pass-fixture-stress-microcopy
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/priv/repo/seeds.exs
  - examples/accrue_host/priv/repo/seeds/phase191_flow_states.exs
  - examples/accrue_host/test/seeds_idempotency_test.exs
  - examples/accrue_host/test/accrue_host/phase191_seed_reachability_test.exs
autonomous: true
requirements:
  - PAGE-01
  - PAGE-04
  - SEED-01
  - SEED-02
must_haves:
  truths:
    - "Example host seeds include one-click Phase 191 click-through data without host chrome redesign."
    - "Host seed additions are deterministic, idempotent, and namespaced."
    - "Existing long-name, multi-currency, dunning, canceling, webhook failure, and overflow seed states remain intact."
  artifacts:
    - path: "examples/accrue_host/priv/repo/seeds/phase191_flow_states.exs"
      provides: "Host click-through Phase 191 fixture data"
    - path: "examples/accrue_host/test/accrue_host/phase191_seed_reachability_test.exs"
      provides: "Host seed reachability tests"
  key_links:
    - from: "examples/accrue_host/priv/repo/seeds.exs"
      to: "phase191_flow_states.exs"
      via: "Code.eval_file in seed orchestration"
      pattern: "phase191_flow_states"
  prohibitions:
    - statement: "Do not redesign examples/accrue_host UI chrome or add billing primitives."
      status: "resolved"
      verification: "Only seed files and seed tests change."
---

<objective>
Expand example-host seed data for Phase 191 matrix reachability.

Purpose: SEED-01/02 require local click-through data as well as E2E forcing; host seeds must prove realistic one-click states remain deterministic.
Output: New host seed file and idempotency/reachability tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-CONTEXT.md
@.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-UI-SPEC.md
@examples/accrue_host/priv/repo/seeds.exs
@examples/accrue_host/priv/repo/seeds/edge_states.exs
@examples/accrue_host/priv/repo/seeds/showcase.exs
@examples/accrue_host/priv/repo/seeds/background_data.exs
@examples/accrue_host/test/seeds_idempotency_test.exs
</context>

## Artifacts this phase produces

- New file: `examples/accrue_host/priv/repo/seeds/phase191_flow_states.exs`.
- New test file: `examples/accrue_host/test/accrue_host/phase191_seed_reachability_test.exs`.
- New seed namespace: `e2e_phase191_*` or `phase191_*` for host fixture records.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add host seed reachability tests</name>
  <read_first>
    - @examples/accrue_host/test/seeds_idempotency_test.exs
    - @examples/accrue_host/priv/repo/seeds.exs
    - @examples/accrue_host/priv/repo/seeds/edge_states.exs
    - @.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-UI-SPEC.md
  </read_first>
  <files>examples/accrue_host/test/seeds_idempotency_test.exs, examples/accrue_host/test/accrue_host/phase191_seed_reachability_test.exs</files>
  <behavior>
    - Tests prove repeated seed evaluation does not duplicate Phase 191 rows or append-only events.
    - Tests assert Phase 191 host records exist for null optional fields, boundary pagination, high counts, and non-ASCII labels.
    - Tests assert pre-existing edge-state anchors still exist after adding Phase 191 seed file.
  </behavior>
  <action>Add failing host seed tests for D-09, D-10, D-11, SEED-01, and SEED-02. Keep the existing BAN-04 dunning idempotency assertions intact. Test record presence through repo queries and stable IDs; do not require browser UI changes.</action>
  <acceptance_criteria>
    - Tests fail before `phase191_flow_states.exs` is wired.
    - Tests check repeated seed runs and stable counts.
    - Tests mention the Phase 191 namespace.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && mix test test/seeds_idempotency_test.exs test/accrue_host/phase191_seed_reachability_test.exs</automated>
  </verify>
  <done>Host seed tests define deterministic Phase 191 click-through fixture requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement idempotent Phase 191 host seed file</name>
  <read_first>
    - @examples/accrue_host/priv/repo/seeds.exs
    - @examples/accrue_host/priv/repo/seeds/edge_states.exs
    - @examples/accrue_host/priv/repo/seeds/showcase.exs
    - @examples/accrue_host/priv/repo/seeds/background_data.exs
  </read_first>
  <files>examples/accrue_host/priv/repo/seeds.exs, examples/accrue_host/priv/repo/seeds/phase191_flow_states.exs</files>
  <behavior>
    - Seed file uses existing `AccrueHost.SeedHelpers.upsert!/3` or `Repo.insert_all(... on_conflict ...)` patterns.
    - Seed file adds realistic records for null/missing optional fields, non-ASCII names, high counts, one-row/boundary lists, and operator flow links.
    - Seed orchestration includes the new file without changing existing seed order semantics in a way that breaks prior tests.
  </behavior>
  <action>Create `phase191_flow_states.exs` and wire it from `seeds.exs`. Use deterministic IDs, slugs, emails, processor IDs, idempotency keys, and event keys. Preserve D-03 scope: no host chrome redesign, no new billing primitives, no `accrue_portal` work. Keep the data realistic enough for PAGE-01 and PAGE-04 click-through at the required widths.</action>
  <acceptance_criteria>
    - Re-running seeds does not duplicate users, organizations, billing rows, or append-only events.
    - Phase 191 records are discoverable from existing admin routes through stable IDs/filterable values.
    - Existing edge-state tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && mix test test/seeds_idempotency_test.exs test/accrue_host/phase191_seed_reachability_test.exs</automated>
  </verify>
  <done>Example host has idempotent Phase 191 seed data for click-through matrix stress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script -> host database | Seed script writes deterministic demo data. |
| Synthetic fixture data -> operator UI | UI displays only fake test/demo data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-13 | Tampering | Host seeds | mitigate | Use upsert/on_conflict patterns and tests for repeated runs. |
| T-191-14 | Information Disclosure | Fixture data | mitigate | Use synthetic names, emails, IDs, payloads, and no real secrets. |
| T-191-15 | Denial of Service | High-count seed rows | mitigate | Keep high counts bounded to the UI stress contract; test runtime remains focused. |
| T-191-SC | Tampering | package installs | mitigate | No dependency changes. |
</threat_model>

<verification>
Run host seed idempotency and Phase 191 reachability tests.
</verification>

<success_criteria>
Host seed expansion is deterministic, idempotent, namespaced, and route-reachable without UI chrome changes.
</success_criteria>

<output>
Create `.planning/phases/191-page-flow-interaction-pass-fixture-stress-microcopy/191-06-SUMMARY.md` when done.
</output>

---
phase: 192-idempotent-verification-sign-off
plan: "05"
type: execute
wave: 3
depends_on:
  - "192-01"
  - "192-02"
files_modified:
  - accrue_admin/e2e/phase192-gallery.mjs
  - accrue_admin/package.json
  - .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "VER-04: The maintainer signs off on 192-SIGN-OFF.md, not raw test-results or the full corpus, per D-29."
    - "VER-04: 192-SIGN-OFF.md contains executive pass/fail, baseline comparison, CI guardrail status, curated JTBD gallery, artifact manifest links, and explicit accept/block checklist per D-30 through D-37."
    - "VER-04: The sign-off package uses Accrue operator and maintainer language, brand voice, microcopy checks, and UI review categories per D-38 through D-41."
    - "D-33: The curated gallery includes representative screenshots for dashboard health scan, customer inspection, subscription triage/detail, invoice/payment review, webhook/event debugging, recovery campaign, component lab, modal/drawer/dropdown open states, and empty/error/permission/disconnected states."
  artifacts:
    - path: "accrue_admin/e2e/phase192-gallery.mjs"
      provides: "JTBD-first curated gallery and sign-off markdown generator"
      exports: ["generatePhase192Gallery", "generatePhase192Signoff", "main"]
    - path: "accrue_admin/package.json"
      provides: "phase192:signoff npm command"
      contains: "phase192:signoff"
    - path: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      provides: "Maintainer sign-off package with evidence-linked gallery and accept/block checklist"
  key_links:
    - from: "accrue_admin/e2e/phase192-gallery.mjs"
      to: ".planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json"
      via: "reads manifest refs for screenshots, traces, command logs, and generated scorecard artifacts without committing bulky evidence"
      pattern: "artifacts\\.manifest\\.json"
    - from: "accrue_admin/e2e/phase192-gallery.mjs"
      to: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      via: "renders maintainer-readable executive status, gallery rows, guardrail status, and checklist"
      pattern: "192-SIGN-OFF\\.md"
    - from: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      to: "scripts/ci/verify_phase192_signoff.mjs"
      via: "generated markdown must pass the Plan 192-01 sign-off verifier"
      pattern: "verify_phase192_signoff\\.mjs"
---

<objective>
Generate the maintainer sign-off package for VER-04.

Purpose: Phase 192 needs a maintainer decision surface that closes v1.51 photographic-sign-off debt without repeating still-image-only weaknesses. This plan runs after Plan 192-02 so `accrue_admin/package.json` script edits are serialized and so sign-off packaging reads the generated scorecard artifacts. This plan creates a deterministic gallery/sign-off generator and produces `192-SIGN-OFF.md` with a JTBD-first curated gallery, manifest-backed evidence refs, trace links for interaction behaviors, executive pass/fail, CI guardrail status, and explicit accept/block checklist per D-29 through D-41.

Output:
- `accrue_admin/e2e/phase192-gallery.mjs`
- `accrue_admin/package.json` script `phase192:signoff`
- `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
@.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
@.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
@.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
@.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
@.planning/phases/192-idempotent-verification-sign-off/192-01-PLAN.md
@.planning/phases/192-idempotent-verification-sign-off/192-02-PLAN.md
@.planning/phases/192-idempotent-verification-sign-off/192-03-PLAN.md
@brandbook/voice.md
@brandbook/copy.md
@brandbook/tokens/README.md
@accrue_admin/package.json
@accrue_admin/e2e/baseline-manifest.js
@accrue_admin/e2e/baseline-artifacts.mjs
@accrue_admin/e2e/admin-visuals.spec.js
@accrue_admin/e2e/admin-motion-trace.spec.js
@scripts/ci/verify_phase192_signoff.mjs
</context>

## Artifacts this phase produces

- New file: `accrue_admin/e2e/phase192-gallery.mjs`.
- New exported function: `generatePhase192Gallery(options)`.
- New exported function: `generatePhase192Signoff(options)`.
- New exported function: `main(argv)`.
- New npm script: `phase192:signoff`.
- New CLI command: `cd accrue_admin && npm run phase192:signoff`.
- New CLI flag: `--self-test`, using temporary scorecard, manifest, guardrail, screenshot, trace, and checklist fixtures.
- New CLI flag: `--dry-run`, rendering the sign-off markdown and verifier result without writing `192-SIGN-OFF.md`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/final.cells.json`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md`.
- New default output path: `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`.
- Required generated section: `## Executive Status`.
- Required generated section: `## Baseline Comparison`.
- Required generated section: `## CI Guardrail Status`.
- Required generated section: `## Curated Gallery`.
- Required generated section: `## Interaction Trace References`.
- Required generated section: `## Artifact Manifest Links`.
- Required generated section: `## Maintainer Checklist`.
- Required generated checklist outcome values: `ACCEPT` and `BLOCK`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the JTBD-first gallery and sign-off generator</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-01-PLAN.md
    - @brandbook/voice.md
    - @brandbook/copy.md
    - @brandbook/tokens/README.md
    - @accrue_admin/e2e/baseline-artifacts.mjs
    - @accrue_admin/e2e/admin-visuals.spec.js
    - @accrue_admin/e2e/admin-motion-trace.spec.js
    - @scripts/ci/verify_phase192_signoff.mjs
  </read_first>
  <files>accrue_admin/e2e/phase192-gallery.mjs</files>
  <behavior>
    - Positive case: a self-test fixture with scorecard artifacts, manifest evidence refs, CI guardrail statuses, light/dark screenshots, mobile layout-risk rows, trace refs, and complete checklist categories renders sign-off markdown that `verify_phase192_signoff.mjs` accepts.
    - Negative case: missing `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, or `192-SCORECARD.md` refs renders executive status as `BLOCK` and records the missing artifact under required repairs.
    - Negative case: a gallery row without `who`, `job`, route or surface, state, theme, viewport, evidence ref, why it matters, or accept/block status fails self-test.
    - Negative case: focus trap, focus restore, Escape, outside click, scroll reachability, LiveView patch focus, or actionability claims without trace refs fail self-test.
    - Negative case: maintainer checklist approval without JTBD clarity, domain vocabulary, microcopy recovery, brand fit, accessible focus/contrast, mobile usability, dark-mode role clarity, absence of backend-guts presentation, responsive layout, theme behavior, interaction integrity, focus/hover/disabled affordance, information hierarchy, and developer/operator DX fails self-test.
  </behavior>
  <action>Create `accrue_admin/e2e/phase192-gallery.mjs` as an ESM Node script modeled on `baseline-artifacts.mjs`: resolve `adminRoot`, `repoRoot`, `testResultsRoot`, and `phaseDir`; use repo-relative artifact refs; avoid reading or embedding raw screenshot/trace payloads; and export `generatePhase192Gallery(options)`, `generatePhase192Signoff(options)`, and `main(argv)`. Per D-29 and D-31, make `192-SIGN-OFF.md` the maintainer decision surface over scorecard plus curated gallery plus human checklist, not raw `test-results` or the full 21,276-cell corpus. Per D-30, render executive pass/fail, baseline comparison summary, CI guardrail status, curated gallery index, artifact manifest links, and a maintainer checklist. Per D-32 through D-34, define a curated gallery inventory that is JTBD-first and includes rows for dashboard health scan, customer inspection, subscription triage/detail, invoice/payment review, webhook/event debugging, recovery campaign, component lab, modal/drawer/dropdown open states, and empty/error/permission/disconnected states; each row must name `who`, `job`, `route_or_surface`, `state`, `theme`, `viewport`, `evidence_ref`, `why_this_matters`, and `status`; include light and dark screenshots for each selected flow and mobile rows for layout-risk flows; include system-theme rows only when a deterministic evidence ref exists. Per D-35 and D-36, include focused, hover, disabled/read-only, and open-state rows for command palette, dropdowns, drawers, modals, mobile nav, destructive confirmations, and disabled/read-only actions, and link trace refs for focus trap, focus restore, Escape, outside click, scroll reachability, LiveView patch focus, and actionability. Per D-37 through D-41, generate checklist rows for JTBD clarity, domain vocabulary, microcopy recovery, brand fit, accessible focus/contrast, mobile usability, dark-mode role clarity, absence of backend-guts presentation, accessibility, performance, responsive layout, light/dark/system theme behavior where supported, interaction integrity, focus/hover/disabled affordance, information hierarchy, brand expression, and developer/operator DX. Use Accrue domain nouns and verbs from D-38: customer, subscription, invoice, charge/payment, webhook, event, recovery, Connect account, inspect, filter, replay, refund, void, recover, clear filters. Use brandbook voice from D-39: measured, exact, native, durable, quiet well-made developer tooling, and no fintech/startup gloss. Per D-40, require microcopy rows to state what happened, the affected object or process, and the next useful action where one exists. If upstream scorecard, manifest, or CI status artifacts are not present when the generator runs, render `BLOCK` with named missing evidence and required repair instead of inventing a pass. Implement `--self-test` with temporary fixtures for complete, missing-artifact, missing-gallery-field, missing-trace, and incomplete-checklist cases. Implement `--dry-run` to print the rendered sign-off and verification result without writing the canonical output. Do not add dependencies, do not change browser specs, do not commit screenshots/traces/ZIPs, do not mutate scorecard artifacts, and do not include portal, PhoenixStorybook, pixel-diff, or host chrome redesign scope.</action>
  <acceptance_criteria>
    - `accrue_admin/e2e/phase192-gallery.mjs` exists, is ESM, and exports `generatePhase192Gallery`, `generatePhase192Signoff`, and `main`.
    - The generator reads or references the Phase 192 scorecard package paths and artifact manifest paths without mutating them.
    - The curated gallery schema contains all D-32 fields and all D-33 through D-36 categories.
    - The checklist schema contains all D-37 through D-41 review categories and uses explicit `ACCEPT` or `BLOCK` status values.
    - Missing scorecard, manifest, CI, screenshot, or trace evidence produces an explicit blocking sign-off outcome with named repairs.
    - `--self-test` proves complete and failing fixtures before real `192-SIGN-OFF.md` generation is trusted.
  </acceptance_criteria>
  <verify>
    <automated>node accrue_admin/e2e/phase192-gallery.mjs --self-test</automated>
    <automated>node --check accrue_admin/e2e/phase192-gallery.mjs</automated>
    <automated>grep -q "generatePhase192Gallery" accrue_admin/e2e/phase192-gallery.mjs</automated>
    <automated>grep -q "generatePhase192Signoff" accrue_admin/e2e/phase192-gallery.mjs</automated>
    <automated>grep -q "artifacts.manifest.json" accrue_admin/e2e/phase192-gallery.mjs</automated>
  </verify>
  <done>The gallery/sign-off generator can render a complete maintainer package from structured refs and fails honest when required evidence is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the sign-off command and generate 192-SIGN-OFF.md</name>
  <read_first>
    - @accrue_admin/package.json
    - @accrue_admin/e2e/phase192-gallery.mjs
    - @scripts/ci/verify_phase192_signoff.mjs
    - @.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
  </read_first>
  <files>accrue_admin/package.json, .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md</files>
  <action>Add `phase192:signoff` to `accrue_admin/package.json` with the command `node e2e/phase192-gallery.mjs`. Run `cd accrue_admin && npm run phase192:signoff` to write `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`. Per D-04 through D-08 and D-29 through D-31, make the markdown a readable decision surface over canonical structured artifacts, with links to `192-SCORECARD.md`, `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json`. Per D-30, include `## Executive Status`, `## Baseline Comparison`, `## CI Guardrail Status`, `## Curated Gallery`, `## Artifact Manifest Links`, and `## Maintainer Checklist`. Per D-32 through D-36, include the JTBD-first curated gallery and trace reference sections produced by the generator, keeping bulky screenshots/traces out of git and linking only manifest evidence refs. Per D-37 through D-41, include explicit checklist rows with accept/block state for JTBD clarity, domain vocabulary, microcopy recovery, brand fit, accessible focus/contrast, mobile usability, dark-mode role clarity, absence of backend-guts presentation, accessibility, performance, responsive layout, theme behavior, interaction integrity, focus/hover/disabled affordance, information hierarchy, brand expression, and developer/operator DX. After generation, run `node scripts/ci/verify_phase192_signoff.mjs` so the file satisfies the Plan 192-01 sign-off verifier. Do not modify scorecard, CI, or raw evidence files; this task only wires the npm command and generates the maintainer sign-off markdown.</action>
  <acceptance_criteria>
    - `accrue_admin/package.json` contains `phase192:signoff` that invokes `node e2e/phase192-gallery.mjs`.
    - `cd accrue_admin && npm run phase192:signoff` writes `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`.
    - `192-SIGN-OFF.md` links or references `192-SCORECARD.md`, `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json`.
    - `192-SIGN-OFF.md` contains executive pass/fail, baseline comparison summary, CI guardrail status, curated gallery rows, interaction trace refs, artifact manifest links, and explicit maintainer accept/block checklist rows.
    - `192-SIGN-OFF.md` uses the D-38 domain vocabulary and D-39 brand voice instead of backend implementation dumping.
    - `192-SIGN-OFF.md` passes `node scripts/ci/verify_phase192_signoff.mjs`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && npm run phase192:signoff</automated>
    <automated>node scripts/ci/verify_phase192_signoff.mjs</automated>
    <automated>node -e "const pkg=require('./accrue_admin/package.json'); if (!pkg.scripts['phase192:signoff']) throw new Error('missing phase192:signoff'); console.log('phase192 signoff script ok')"</automated>
    <automated>grep -Fq "## Executive Status" .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md</automated>
    <automated>grep -Fq "## Curated Gallery" .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md</automated>
    <automated>grep -Fq "artifacts.manifest.json" .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md</automated>
  </verify>
  <done>The maintainer sign-off markdown is generated, verifier-clean, evidence-linked, and explicit about accept/block status.</done>
</task>

</tasks>

<must_haves>
## Goal-Backward Truths

- The maintainer can review one `192-SIGN-OFF.md` package instead of raw screenshots, traces, or the full final-cell corpus.
- The curated gallery is organized by operator/maintainer jobs to be done and risk categories, not by chronology or raw file dump.
- Every screenshot claim links to manifest evidence, and every interaction-integrity claim links to deterministic trace or browser evidence.
- The checklist makes sign-off auditable by recording explicit accept/block status for JTBD clarity, vocabulary, microcopy, brand, accessibility, responsive layout, theme behavior, interaction integrity, and DX.
- D-33: The curated gallery must include representative screenshots for dashboard health scan, customer inspection, subscription triage/detail, invoice/payment review, webhook/event debugging, recovery campaign, component lab, modal/drawer/dropdown open states, and empty/error/permission/disconnected states.

## Required Artifacts

- `accrue_admin/e2e/phase192-gallery.mjs` generates gallery rows and sign-off markdown.
- `accrue_admin/package.json` exposes `phase192:signoff`.
- `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md` exists and passes the sign-off verifier.

## Key Links

- `phase192-gallery.mjs` reads or references Phase 192 scorecard artifacts and manifest refs.
- `phase192:signoff` invokes the generator.
- `verify_phase192_signoff.mjs` validates `192-SIGN-OFF.md` after generation.
</must_haves>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 192 | Obtain maintainer screenshot sign-off that closes v1.51 photographic-sign-off tech-debt | 192-05 | COVERED | This plan generates the maintainer decision surface and verifier-clean package. |
| REQ | VER-04 | Maintainer signs off on screenshots at phase boundaries and final sign-off closes v1.51 debt | 192-05 | COVERED | `192-SIGN-OFF.md` contains gallery, manifest links, trace refs, and checklist. |
| RESEARCH | Maintainer sign-off | Sign-off belongs in planning artifacts as human review over curated evidence | 192-05 | COVERED | Plan writes `192-SIGN-OFF.md`, not an app route or raw evidence dump. |
| RESEARCH | Artifact manifest references | Evidence refs and checksums belong in `artifacts.manifest.json`, not committed binaries | 192-05 | COVERED | Tasks link manifest refs and forbid committing screenshots, traces, reports, or ZIPs. |
| RESEARCH | Advisory visual lens | Subjective brand/microcopy review is acceptable only with deterministic evidence and accept/block state | 192-05 | COVERED | Gallery/checklist rows require evidence refs and explicit status. |
| CONTEXT | D-29 | Maintainer signs off on `192-SIGN-OFF.md`, not raw evidence or full corpus | 192-05 | COVERED | Task 1 and Task 2 make `192-SIGN-OFF.md` the decision surface. |
| CONTEXT | D-30 | Sign-off contains executive pass/fail, baseline summary, CI status, gallery, manifest links, checklist | 192-05 | COVERED | Task 2 requires all named sections. |
| CONTEXT | D-31 | Use scorecard plus curated gallery plus human checklist | 192-05 | COVERED | Task 1 generator composes all three. |
| CONTEXT | D-32 | Gallery is JTBD-first with who/job/route/state/theme/viewport/why fields | 192-05 | COVERED | Task 1 schema requires every field. |
| CONTEXT | D-33 | Representative screenshots cover dashboard, customers, subscriptions, invoices/payments, webhooks/events, recovery, component lab, open states, and state variants | 192-05 | COVERED | Task 1 inventory includes every category. |
| CONTEXT | D-34 | Each selected flow includes light and dark; mobile for layout-risk; system theme only when deterministic | 192-05 | COVERED | Task 1 encodes theme/viewport rules. |
| CONTEXT | D-35 | Historical-risk controls include focused, hover, disabled/read-only, and open-state screenshots | 192-05 | COVERED | Task 1 includes command palette, dropdowns, drawers, modals, mobile nav, confirmations, and disabled/read-only actions. |
| CONTEXT | D-36 | Focus/escape/outside-click/scroll/patch-focus/actionability claims link traces | 192-05 | COVERED | Task 1 requires trace refs for these behaviors. |
| CONTEXT | D-37 | Checklist approves JTBD, vocabulary, microcopy, brand, accessibility, mobile, dark mode, and absence of backend guts | 192-05 | COVERED | Task 1 and Task 2 require checklist rows. |
| CONTEXT | D-38 | Sign-off uses operator and maintainer domain nouns and verbs | 192-05 | COVERED | Task actions require the exact domain vocabulary. |
| CONTEXT | D-39 | Brand review follows ratified brandbook and `ax-*` SSOT | 192-05 | COVERED | Task 1 reads brandbook and applies measured/native/durable voice. |
| CONTEXT | D-40 | Microcopy names what happened, affected object/process, and next useful action | 192-05 | COVERED | Task 1 checklist and copy rules enforce this. |
| CONTEXT | D-41 | UI sign-off considers accessibility, performance, responsive layout, theme behavior, interaction integrity, affordance, hierarchy, brand, and DX | 192-05 | COVERED | Task 1 and Task 2 require all categories. |
| CONTEXT | Deferred portal design-system todo | Future portal/design-system scope | 192-05 | EXCLUDED | No portal, PhoenixStorybook, pixel-diff, or host chrome redesign work is included. |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structured artifacts -> sign-off generator | Scorecard JSON/NDJSON, manifest, and CI status files are generated inputs that may be missing, stale, malformed, or blocking. |
| Evidence manifest -> maintainer markdown | Manifest refs point at generated screenshots/traces/reports; markdown must link refs without embedding bulky evidence. |
| Generated markdown -> maintainer decision | `192-SIGN-OFF.md` is a human decision surface and must not hide blocking status behind prose. |
| Subjective review -> final checklist | Brand, microcopy, and gallery review are human/subjective lenses that require deterministic evidence refs and explicit accept/block state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-05-01 | Tampering | `phase192-gallery.mjs` input parsing | mitigate | Parse scorecard/manifest inputs defensively, render `BLOCK` for missing or malformed required evidence, and self-test failing fixtures. |
| T-192-05-02 | Repudiation | `192-SIGN-OFF.md` checklist | mitigate | Require explicit accept/block status for each maintainer checklist category and run `verify_phase192_signoff.mjs`. |
| T-192-05-03 | Information Disclosure | Screenshot and trace refs | mitigate | Link repo-relative manifest refs only; do not embed screenshots, traces, reports, ZIPs, or local absolute paths in markdown. |
| T-192-05-04 | Tampering | Gallery evidence rows | mitigate | Require `who`, `job`, route/surface, state, theme, viewport, evidence ref, rationale, and status for every row. |
| T-192-05-05 | Spoofing | Interaction-integrity claims | mitigate | Require trace refs for focus trap, focus restore, Escape, outside click, scroll reachability, LiveView patch focus, and actionability rather than relying on still screenshots. |
| T-192-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; no new dependency is allowed in this plan. |
</threat_model>

<verification>
Run:
- `node accrue_admin/e2e/phase192-gallery.mjs --self-test`
- `node --check accrue_admin/e2e/phase192-gallery.mjs`
- `cd accrue_admin && npm run phase192:signoff`
- `node scripts/ci/verify_phase192_signoff.mjs`
- package script presence check for `phase192:signoff`
- section/link greps for `192-SIGN-OFF.md`
</verification>

<success_criteria>
Plan 192-05 is complete when `phase192:signoff` generates `192-SIGN-OFF.md`, the file passes `verify_phase192_signoff.mjs`, the curated gallery covers every D-32 through D-36 JTBD/risk category with manifest evidence refs, the checklist covers every D-37 through D-41 sign-off category with explicit accept/block status, missing upstream evidence is represented as blocking status rather than pass prose, and no bulky evidence or out-of-scope portal/PhoenixStorybook/pixel-diff/host-chrome work is committed.
</success_criteria>

<output>
Create `.planning/phases/192-idempotent-verification-sign-off/192-05-SUMMARY.md` when done.
</output>

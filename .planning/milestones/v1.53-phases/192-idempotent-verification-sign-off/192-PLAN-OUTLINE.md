| Plan ID | Objective | Wave | Depends On | Requirements |
|---|---|---:|---|---|
| `192-01` | Create the Phase 192 artifact contract and fail-closed verifiers: strict scorecard verifier for `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json`; sign-off verifier for `192-SIGN-OFF.md`; preserve Phase 187 cell grammar and structured-data precedence per D-04 through D-19. | 1 | `[]` | `VER-02`, `VER-04` |
| `192-02` | Build the pure final-scorecard reducer and package generator: normalize separated evidence lenses to frozen Phase 187 cell IDs, compare every comparable cell against baseline, emit canonical structured artifacts plus `192-SCORECARD.md`, and fail on score downgrades, coverage downgrades, missing evidence, or non-empty regressions per D-09 through D-18 and D-42 through D-45. | 2 | `["192-01"]` | `VER-02` |
| `192-03` | Add deterministic local guardrail commands for the required Phase 192 CI boundary: baseline parse, Phase 191 AX187 verifier, group contracts, Phase 191 interactions, axe a11y, reduced-motion, and component-lab structural coverage; keep broad `npm run e2e`, `score-visuals`, and `baseline:artifacts` out of merge-blocking PR gates per D-20 through D-28. | 1 | `[]` | `VER-03` |
| `192-04` | Wire the bounded admin hardening guardrails into CI with serial Playwright execution, existing BEAM/package CI preserved, `admin-group-contracts` still merge-blocking, and Playwright/evidence artifact uploads for reports, screenshots, traces, and generated outputs per D-20 through D-28. | 2 | `["192-03"]` | `VER-03` |
| `192-05` | Generate the maintainer sign-off package: JTBD-first curated gallery, evidence manifest links, trace references for interaction behaviors, executive pass/fail, CI guardrail status, and explicit accept/block checklist in `192-SIGN-OFF.md` per D-29 through D-41. | 2 | `["192-01"]` | `VER-04` |
| `192-06` | Run the full final verification loop and sign-off checkpoint: execute deterministic guardrails, generate final scorecard artifacts, verify zero regressions against Phase 187 baseline, verify sign-off package completeness, and obtain maintainer screenshot/sign-off approval that closes v1.51 photographic-sign-off debt. | 3 | `["192-02", "192-04", "192-05"]` | `VER-02`, `VER-03`, `VER-04` |

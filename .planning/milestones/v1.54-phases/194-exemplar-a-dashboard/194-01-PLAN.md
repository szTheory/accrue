---
phase: 194-exemplar-a-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/live/dashboard_live.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/accrue_admin.css
autonomous: true
requirements: [EXE-01]

must_haves:
  truths:
    - "The Dashboard exposes data-ax-zone markers on its four existing zone sections in DOM order attention-rail < task-launcher < kpi-cluster < recent-activity."
    - "The visible ⌘K search button carries data-ax-command-palette-trigger alongside the untouched data-command-palette-trigger."
    - "The healthy attention empty-state reads as a deliberate 'all clear' hero via a new .ax-attention-rail--empty class, and stays non-interactive."
    - "The KPI cluster reads as visually subordinate to the attention rail (light demotion) without a broad type-scale rework."
    - "The committed served CSS bundle reflects the source CSS deltas."
  artifacts:
    - "accrue_admin/lib/accrue_admin/live/dashboard_live.ex with additive data-ax-* markers and the empty-rail hero class"
    - "accrue_admin/assets/css/app.css with the .ax-attention-rail--empty rule and light KPI/attention weight deltas"
    - "accrue_admin/priv/static/accrue_admin.css regenerated from the source"
  key_links:
    - "data-ax-zone marker DOM order (D-05) is the contract the Wave-2 e2e spec asserts."
    - "app.css edits are dead unless mix accrue_admin.assets.build regenerates and commits priv/static/accrue_admin.css (Phase 189 footgun)."
---

<objective>
Refine the Dashboard (`dashboard_live.ex`) to conform to the SPEC-OVERVIEW contract: add the additive `data-ax-*` machine-checkable hooks, elevate the healthy empty-state into a non-interactive "all clear" hero, and apply a *light* CSS pass that demotes the KPI cluster relative to the attention rail. Per D-03/D-04/D-05/D-06. This is refine-not-rebuild: no `<section>` is moved.

Purpose: The Dashboard is the locked gold-standard for the overview archetype; this makes its SPEC-OVERVIEW invariants machine-enforceable (markers + class) and tightens the judge-graded visual hierarchy.
Output: An updated LiveView with additive markers + empty-rail hero class, light CSS deltas in `app.css`, and the regenerated committed `priv/static/accrue_admin.css` bundle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/194-exemplar-a-dashboard/194-CONTEXT.md
@.planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md
@.planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md
@.planning/phases/194-exemplar-a-dashboard/194-UI-SPEC.md
@accrue_admin/guides/spec-overview.md
@accrue_admin/lib/accrue_admin/live/dashboard_live.ex
@accrue_admin/assets/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive data-ax-* markers to the four Dashboard zones and the ⌘K trigger</name>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/dashboard_live.ex L49-213 (render/1: zone sections at L57/L93/L149/L198; ⌘K button L100-106)
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L23-54 (zone-marker placement + ⌘K additive marker excerpts)
  </read_first>
  <action>
    In dashboard_live.ex render/1, add ONE attribute per existing zone section, leaving every section in place (no structural move — D-05 holds from source order alone):
    - L57 `<section class="ax-home-section" aria-label="Billing exceptions">` → add `data-ax-zone="attention-rail"` (per D-04/D-05)
    - L93 `<section class="ax-home-section" aria-label="Tasks">` → add `data-ax-zone="task-launcher"` (per D-04/D-05)
    - L149 `<section class="ax-home-section" aria-label={Copy.dashboard_kpi_section_aria_label()}>` → add `data-ax-zone="kpi-cluster"` (per D-04/D-05)
    - L198 `<section class="ax-grid ax-grid-2" ...>` → add `data-ax-zone="recent-activity"` (per D-04/D-05)
    On the ⌘K button (L100-106), add `data-ax-command-palette-trigger="true"` ALONGSIDE the existing `data-command-palette-trigger="true"` (per D-04). NEVER rename or remove `data-command-palette-trigger` — command_palette.js binds it and renaming is an explicitly deferred idea. Do not touch command_palette.js or topbar.ex. The marker enum values are static strings only — no PII, no IDs.
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -5; grep -c 'data-ax-zone="attention-rail"\|data-ax-zone="task-launcher"\|data-ax-zone="kpi-cluster"\|data-ax-zone="recent-activity"' lib/accrue_admin/live/dashboard_live.ex</automated>
  </verify>
  <acceptance_criteria>
    Compiles clean. All four data-ax-zone values present exactly once each; data-ax-command-palette-trigger present on the ⌘K button; the original data-command-palette-trigger attribute is unchanged. No section was reordered.
  </acceptance_criteria>
  <done>Four zone markers + the ⌘K additive marker are present; behavior attribute untouched; module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Elevate the healthy empty-state to a non-interactive hero + light KPI demotion CSS</name>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex, accrue_admin/assets/css/app.css</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/dashboard_live.ex L85-89 (the empty card — D-06 target) and L14 (alias AccrueAdmin.Copy)
    - accrue_admin/assets/css/app.css L3228-3336 (.ax-attention* and .ax-empty* blocks) and L519/L2602/L3402 (.ax-kpi-grid)
    - accrue_admin/assets/css/theme.css (the --ax-space-*, --ax-type-*, --ax-muted token SSOT)
    - .planning/phases/194-exemplar-a-dashboard/194-UI-SPEC.md L37-69 (Spacing Scale + Typography demotion contract)
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L78-102 (token-only CSS + RES-04 guard warnings)
  </read_first>
  <action>
    In dashboard_live.ex L85, add the SPEC-OVERVIEW-named class `.ax-attention-rail--empty` to the existing empty card's class list (now `ax-card ax-empty ax-attention-rail--empty`) per D-06. KEEP the existing `Copy.home_attention_empty_title/0` and `Copy.home_attention_empty_copy/0` — elevate hierarchy, do NOT rewrite copy (all strings stay via AccrueAdmin.Copy per CLAUDE.md). Add NO `role="button"`, NO on-click, NO interactive affordance (D-06 non-interactivity guard).

    In app.css, add the `.ax-attention-rail--empty` rule directly beside the existing `.ax-empty` block (~L3336) using EXISTING tokens only (introduce NO new tokens — CONTEXT guardrail; the empty-state success/neutral tone uses existing status/neutral tokens, never `--ax-accent` as a CTA fill, never `--ax-danger`). Elevate hierarchy via larger `--ax-space-*` padding/gap and a stronger title type role. CRITICAL: this rule MUST NOT contain `cursor: pointer` and MUST NOT have a `:hover` affordance (the SPEC-OVERVIEW non-interactive guard added in plan 194-03 scans for exactly this).

    Apply a LIGHT KPI demotion per D-03: drop the `.ax-kpi-*` cluster framing weight (card weight / label contrast via `--ax-muted`) so the cluster reads subordinate to the attention rail, and where the rubric flags "exceptions higher-signal than KPIs," strengthen the `.ax-attention*` weight only. Do NOT promote KPIs to `--ax-type-display`. Do NOT do a broad type-scale/spacing rework (deferred). Use `--ax-space-*` tokens for all padding/margin/gap — raw px on those properties trips RES-04 Guard A unless annotated with `/* ax-spacing-exception: ... */`. Pair any `:focus` with `:focus-visible` (Guard B) and any `text-overflow:ellipsis` with `min-width:0` in the same block (Guard C).
  </action>
  <verify>
    <automated>cd accrue_admin && grep -c 'ax-attention-rail--empty' lib/accrue_admin/live/dashboard_live.ex assets/css/app.css; perl -0ne 'while(/\.ax-attention-rail--empty[^{]*\{([^}]*)\}/gs){die "cursor:pointer on empty rail\n" if $1=~/cursor\s*:\s*pointer/} print "no pointer\n"' assets/css/app.css; bash ../scripts/ci/verify_package_docs.sh 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    `.ax-attention-rail--empty` appears in both the LiveView class list and as an app.css rule; the rule has no `cursor:pointer`; the LiveView empty card has no `role="button"`/on-click; verify_package_docs.sh passes (no RES-04 spacing/focus/ellipsis regression); existing empty-state Copy functions retained.
  </acceptance_criteria>
  <done>Empty-state is an elevated non-interactive hero; KPI cluster reads subordinate; CSS uses existing tokens only and trips no RES-04 guard.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and commit the served CSS bundle</name>
  <files>accrue_admin/priv/static/accrue_admin.css</files>
  <read_first>
    - accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex (the exact tailwind CLI minify + esbuild pipeline)
    - .planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md L309-323 (CSS bundle rebuild reality — the dead-CSS footgun)
  </read_first>
  <action>
    From `accrue_admin/`, run `mix accrue_admin.assets.build` to regenerate the served bundle `priv/static/accrue_admin.css` from the edited `app.css` (the tailwind CLI is used here as a minifier, NOT as a utility authoring path — consistent with the no-Tailwind rule). The served file is the COMMITTED `priv/static/accrue_admin.css`, never `app.css` — editing source alone ships nothing (Phase 189 shipped dead CSS this exact way). Stage the regenerated bundle. The JS bundle `accrue_admin.js` is NOT touched this phase (D-04 leaves command_palette.js alone), so do not rebuild or commit JS changes.
  </action>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.assets.build 2>&1 | tail -3 && grep -c 'ax-attention-rail--empty' priv/static/accrue_admin.css && git -C .. diff --stat -- accrue_admin/priv/static/accrue_admin.css | tail -1</automated>
  </verify>
  <acceptance_criteria>
    `mix accrue_admin.assets.build` succeeds; `priv/static/accrue_admin.css` contains `.ax-attention-rail--empty` (proving the source delta reached the bundle); `git diff --stat` shows `priv/static/accrue_admin.css` changed whenever `app.css` changed.
  </acceptance_criteria>
  <done>The committed served bundle reflects the source CSS deltas; the dead-CSS footgun is avoided.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (from this plan)
- `data-ax-zone` marker attribute on four Dashboard sections with enum values: `attention-rail`, `task-launcher`, `kpi-cluster`, `recent-activity` (DOM order encodes the invariant — D-05).
- `data-ax-command-palette-trigger="true"` marker on the dashboard ⌘K button (~L105), additive alongside the untouched `data-command-palette-trigger`.
- `.ax-attention-rail--empty` CSS class (new rule in `app.css`) + its presence in the regenerated `priv/static/accrue_admin.css` bundle.
- Elevated non-interactive empty-state hero markup on the dashboard empty card.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → rendered DOM | Static marker attributes serialized into the page; no user input crosses here this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-01 | Information Disclosure | `data-ax-zone` / `data-ax-command-palette-trigger` markers | low | mitigate | Markers carry static enum literals only (attention-rail/task-launcher/kpi-cluster/recent-activity, "true") — no PII, no record IDs, no scope data. Verified in Task 1 action. |
| T-194-02 | Spoofing/Clickjacking | `.ax-attention-rail--empty` healthy hero | low | mitigate | The empty-state stays non-interactive (no cursor:pointer, no role="button", no on-click) — no false affordance to confuse an operator. Enforced by the source guard in plan 194-03 and the e2e assertion in 194-04. |

No new auth, routes, data flows, or destructive actions are introduced (read/route-only UI). No high-severity threats. **SCHEMA_PUSH not applicable:** this phase touches LiveView markup, custom CSS, and a build artifact only — there are no ORM/schema/migration files in scope.
</threat_model>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors` clean.
- All four `data-ax-zone` enum values + `data-ax-command-palette-trigger` present in `dashboard_live.ex`; `data-command-palette-trigger` unchanged.
- `.ax-attention-rail--empty` in LiveView class list + `app.css`, with no `cursor:pointer`.
- `bash scripts/ci/verify_package_docs.sh` passes (no RES-04 regression).
- `priv/static/accrue_admin.css` regenerated and contains the new class.
</verification>

<success_criteria>
Dashboard conforms to SPEC-OVERVIEW machine hooks (markers present, empty-rail hero non-interactive), reads with KPIs demoted relative to the attention rail, and the committed bundle reflects the source — all without moving any zone section or touching the JS hook.
</success_criteria>

<output>
Create `.planning/phases/194-exemplar-a-dashboard/194-01-SUMMARY.md` when done.
</output>

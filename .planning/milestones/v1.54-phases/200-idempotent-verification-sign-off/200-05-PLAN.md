---
phase: 200-idempotent-verification-sign-off
plan: "05"
type: execute
wave: 5
depends_on: ["200-02", "200-03", "200-04"]
files_modified:
  - scripts/ci/verify_phase200_admin_guardrails.sh
  - scripts/ci/verify_phase200_guardrail_contract.sh
  - scripts/ci/verify_phase200_ci_contract.sh
  - accrue_admin/package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - VER-02
  - VER-03
  - STY-02
  - STY-03
must_haves:
  truths:
    - "VER-02: deterministic local guardrail runner invokes package docs, registry/group/storybook tests, Storybook a11y, page-flow, reduced-motion, Phase 199, scorecard verifier, and sign-off verifier."
    - "VER-03: CI runs deterministic verification only and does not run model-dependent judge generation or human sign-off generation."
    - "STY-02/STY-03: package scripts expose Storybook coverage/theming checks without weakening /dev/components."
  artifacts:
    - path: "scripts/ci/verify_phase200_admin_guardrails.sh"
      provides: "Serial local deterministic Phase 200 guardrail runner"
    - path: "scripts/ci/verify_phase200_guardrail_contract.sh"
      provides: "Static guardrail command contract"
    - path: "scripts/ci/verify_phase200_ci_contract.sh"
      provides: "Static CI topology contract"
    - path: "accrue_admin/package.json"
      provides: "Phase 200 npm aliases"
      contains: ["phase200:storybook", "phase200:scorecard", "phase200:signoff", "phase200:guardrails"]
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking deterministic Phase 200 CI job"
  key_links:
    - from: "scripts/ci/verify_phase200_admin_guardrails.sh"
      to: "accrue_admin/e2e/admin-storybook-a11y-phase200.spec.js"
      via: "local guardrail runner invokes Storybook rendered scan"
      pattern: "admin-storybook-a11y-phase200"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_phase200_admin_guardrails.sh"
      via: "CI job invokes the serial guardrail runner"
      pattern: "verify_phase200_admin_guardrails"
  prohibitions:
    - statement: "CI must not run model-dependent visual scoring or maintainer sign-off generation."
      status: "resolved by plan action"
      verification: "guardrail and CI contract scripts reject model/human generation commands in Phase 200 CI run lines"
---

<objective>
Wire deterministic Phase 200 guardrails into local commands and CI.

Purpose: The closeout artifacts need a repeatable CI boundary that runs deterministic checks without replacing the final human decision or adding expensive model-dependent gates to every PR.
Output: Phase 200 guardrail runner, static guardrail contract, static CI contract, npm aliases, and CI job wiring.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md
@.planning/phases/200-idempotent-verification-sign-off/200-RESEARCH.md
@.planning/phases/200-idempotent-verification-sign-off/200-PATTERNS.md
@.planning/phases/200-idempotent-verification-sign-off/200-VALIDATION.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-03-SUMMARY.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-04-SUMMARY.md
@scripts/ci/verify_phase192_admin_guardrails.sh
@scripts/ci/verify_phase192_guardrail_contract.sh
@scripts/ci/verify_phase192_ci_contract.sh
@accrue_admin/package.json
@.github/workflows/ci.yml
@.github/workflows/accrue_admin_browser.yml
</context>

## Artifacts this phase produces

- Script: `scripts/ci/verify_phase200_admin_guardrails.sh`.
- Script: `scripts/ci/verify_phase200_guardrail_contract.sh`.
- Script: `scripts/ci/verify_phase200_ci_contract.sh`.
- Package scripts in `accrue_admin/package.json`: `phase200:storybook`, `phase200:scorecard`, `phase200:signoff`, and `phase200:guardrails`.
- CI workflow changes in `.github/workflows/ci.yml`: deterministic Phase 200 guardrail job and Phase 200 artifact uploads.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add local deterministic guardrail runner and package aliases</name>
  <files>scripts/ci/verify_phase200_admin_guardrails.sh, scripts/ci/verify_phase200_guardrail_contract.sh, accrue_admin/package.json</files>
  <read_first>
    <file>scripts/ci/verify_phase200_admin_guardrails.sh (new)</file>
    <file>scripts/ci/verify_phase200_guardrail_contract.sh (new)</file>
    <file>accrue_admin/package.json</file>
    <file>scripts/ci/verify_phase192_admin_guardrails.sh</file>
    <file>scripts/ci/verify_phase192_guardrail_contract.sh</file>
    <file>scripts/ci/verify_package_docs.sh</file>
    <file>accrue_admin/e2e/admin-storybook-a11y-phase200.spec.js</file>
    <file>accrue_admin/e2e/admin-page-flow-phase200.spec.js</file>
    <file>scripts/ci/verify_phase200_scorecard.mjs</file>
    <file>scripts/ci/verify_phase200_signoff.mjs</file>
  </read_first>
  <behavior>
    - Runner prints explicit step labels before each deterministic command.
    - Runner invokes focused Phase 200 Storybook, page-flow, scorecard, sign-off, Phase 199, reduced-motion, package-doc, registry, and group checks.
    - Static contract rejects model-dependent visual scoring, broad full-suite aliases, screenshot capture generation, trace capture generation, and maintainer sign-off generation inside the guardrail runner.
  </behavior>
  <action>Per D-01, D-03, D-18, D-22, and D-31, create a serial repo-root `verify_phase200_admin_guardrails.sh` following the Phase 192 runner pattern. Add package aliases `phase200:storybook`, `phase200:scorecard`, `phase200:signoff`, and `phase200:guardrails` without touching unrelated scripts. The runner should call package docs, Storybook coverage/asset tests, existing registry/group drift tests, Storybook a11y, page-flow Phase 200, Phase 199 E2E, reduced-motion, scorecard baseline/full verifier as available, and sign-off verifier. Create `verify_phase200_guardrail_contract.sh` with fixed required-command checks and scoped forbidden-command checks so deterministic CI does not run model/human gates.</action>
  <acceptance_criteria>
    - CLI assertion: `bash -n scripts/ci/verify_phase200_admin_guardrails.sh` passes.
    - CLI assertion: `bash -n scripts/ci/verify_phase200_guardrail_contract.sh` passes.
    - CLI assertion: `bash scripts/ci/verify_phase200_guardrail_contract.sh` passes.
    - Source assertion: `accrue_admin/package.json` contains the four Phase 200 aliases with direct deterministic commands.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/verify_phase200_admin_guardrails.sh && bash -n scripts/ci/verify_phase200_guardrail_contract.sh && bash scripts/ci/verify_phase200_guardrail_contract.sh</automated>
    <automated>node -e "const pkg=require('./accrue_admin/package.json'); for (const k of ['phase200:storybook','phase200:scorecard','phase200:signoff','phase200:guardrails']) { if (!pkg.scripts[k]) throw new Error('missing '+k); } console.log('phase200 package scripts ok')"</automated>
  </verify>
  <done>Local Phase 200 deterministic guardrails are runnable, contract-checked, and exposed through package aliases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire deterministic Phase 200 guardrails into CI</name>
  <files>.github/workflows/ci.yml, scripts/ci/verify_phase200_ci_contract.sh</files>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>scripts/ci/verify_phase200_ci_contract.sh (new)</file>
    <file>scripts/ci/verify_phase192_ci_contract.sh</file>
    <file>scripts/ci/verify_phase200_admin_guardrails.sh</file>
    <file>scripts/ci/verify_phase200_guardrail_contract.sh</file>
    <file>accrue_admin/package.json</file>
    <file>.github/workflows/accrue_admin_browser.yml</file>
  </read_first>
  <behavior>
    - CI job installs BEAM, Node, npm deps, Playwright Chromium, then runs static contracts before the expensive guardrail runner.
    - CI uploads Phase 200 generated artifacts, Playwright reports, and test-results with `if: always()` and `if-no-files-found: ignore`.
    - CI contract rejects direct model scoring, broad Playwright full-run commands, and maintainer sign-off generation in Phase 200 CI run lines.
  </behavior>
  <action>Per D-01, D-03, D-18, D-22, and D-31, add a merge-blocking deterministic Phase 200 guardrail job to `.github/workflows/ci.yml` using the Phase 192 CI topology: checkout, BEAM setup, Hex setup if needed, Node 22, `mix deps.get`, admin compile, `npm ci`, Playwright Chromium install, `verify_phase200_ci_contract.sh`, `verify_phase200_guardrail_contract.sh`, and `verify_phase200_admin_guardrails.sh`. Add artifact uploads for `.planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json`, `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, `200-SCORECARD.md`, `200-STORYBOOK-COVERAGE.md`, `200-VERIFICATION.md`, `200-SIGN-OFF.md`, `judge.findings.json`, `accrue_admin/test-results`, and `accrue_admin/playwright-report`. Do not run judge generation or human sign-off generation in CI.</action>
  <acceptance_criteria>
    - CLI assertion: workflow YAML parses with the repo's established YAML parser command.
    - CLI assertion: `bash -n scripts/ci/verify_phase200_ci_contract.sh` passes.
    - CLI assertion: `bash scripts/ci/verify_phase200_ci_contract.sh` passes.
    - Source assertion: CI job invokes `bash scripts/ci/verify_phase200_admin_guardrails.sh` rather than inlining broad Playwright commands.
  </acceptance_criteria>
  <verify>
    <automated>PATH=/usr/bin:/bin:/usr/sbin:/sbin ruby -e "require 'yaml'; YAML.load_file('.github/workflows/ci.yml'); puts 'ci yaml ok'"</automated>
    <automated>bash -n scripts/ci/verify_phase200_ci_contract.sh && bash scripts/ci/verify_phase200_ci_contract.sh</automated>
  </verify>
  <done>CI has a deterministic Phase 200 guardrail job with static topology verification and artifact uploads.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Accessibility/no-FOUC checks are green and Storybook coverage/theming verified in CI | 200-05 | COVERED | Wires deterministic guardrails and uploads evidence.
REQ | VER-02 | axe/no-FOUC/reduced-motion/group-contract/a11y guardrails pass in CI | 200-05 | COVERED | Runner and CI job call deterministic checks.
REQ | VER-03 | sign-off verifier participates in deterministic boundary without human generation | 200-05 | COVERED | CI verifies structure but does not perform maintainer sign-off.
REQ | STY-02,STY-03 | Storybook coverage and theme checks are part of guardrails | 200-05 | COVERED | Package aliases and runner include Storybook tests.
RESEARCH | Deterministic CI boundary | CI should not run model or human gates | 200-05 | COVERED | Static contracts enforce the boundary.
CONTEXT | D-01,D-03,D-18,D-22,D-31 | Staged closeout, reproducible waves, deterministic CI, stable browser gates, no stale close state | 200-05 | COVERED | Task actions cite each decision.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> local scripts | GitHub Actions executes repo-controlled shell and Node scripts. |
| package scripts -> guardrail commands | npm aliases invoke deterministic test and verifier commands. |
| CI artifacts -> planning evidence | Uploaded artifacts are consumed by maintainers and final sign-off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-17 | Tampering | CI command surface | mitigate | Static guardrail and CI contract scripts require expected commands and reject model/human generation commands in run lines. |
| T-200-18 | Denial of Service | CI browser gates | mitigate | Serial guardrail runner uses one-worker stateful Playwright commands and package-local binaries per D-22. |
| T-200-19 | Repudiation | CI artifact uploads | mitigate | Upload Phase 200 structured artifacts and test results with stable names and `if: always()`. |
| T-200-20 | Information Disclosure | generated artifacts | mitigate | Upload only repo-local generated evidence; no secrets or external service credentials are required. |
| T-200-SC | Tampering | package installs | mitigate | No new packages are planned; CI uses existing lockfiles. If execution adds npm, pip, or cargo installs, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
Run shell syntax, static contract scripts, package alias check, and workflow YAML parse. Full guardrail runner execution is allowed if local services are available; otherwise the final plan must run it before ACCEPT.
</verification>

<success_criteria>
Phase 200 deterministic checks are available locally and in CI while model/human final closeout remains staged for the final boundary.
</success_criteria>

<output>
Create `.planning/phases/200-idempotent-verification-sign-off/200-05-SUMMARY.md` when done.
</output>

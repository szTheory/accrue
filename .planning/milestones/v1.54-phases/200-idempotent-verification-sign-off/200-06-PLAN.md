---
phase: 200-idempotent-verification-sign-off
plan: "06"
type: execute
wave: 6
depends_on: ["200-05"]
files_modified:
  - .planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json
  - .planning/phases/200-idempotent-verification-sign-off/final.cells.json
  - .planning/phases/200-idempotent-verification-sign-off/scorecard.delta.json
  - .planning/phases/200-idempotent-verification-sign-off/regressions.ndjson
  - .planning/phases/200-idempotent-verification-sign-off/artifacts.manifest.json
  - .planning/phases/200-idempotent-verification-sign-off/judge.findings.json
  - .planning/phases/200-idempotent-verification-sign-off/200-SCORECARD.md
  - .planning/phases/200-idempotent-verification-sign-off/200-STORYBOOK-COVERAGE.md
  - .planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md
  - .planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - VER-01
  - VER-02
  - VER-03
  - STY-02
  - STY-03
must_haves:
  truths:
    - "VER-01: `regressions.ndjson` exists and has zero rows."
    - "VER-01: `final.cells.json` and `scorecard.delta.json` prove every comparable union-baseline cell is >= baseline with no coverage downgrade."
    - "VER-02: `200-VERIFICATION.md` records deterministic guardrail commands and green results for Storybook, page-flow, no-FOUC/theme, reduced-motion, group-contract, package-doc, and Phase 199 suites."
    - "VER-03: `200-SIGN-OFF.md` records explicit maintainer ACCEPT after review, with no unresolved `BLOCKER` or `REPAIR-IN-PHASE` findings."
    - "STY-02/STY-03: `200-STORYBOOK-COVERAGE.md` records dynamic family/group coverage, asset delivery, theme parity, and /dev/components drift status."
  artifacts:
    - path: ".planning/phases/200-idempotent-verification-sign-off/final.cells.json"
      provides: "Canonical final union cell matrix"
    - path: ".planning/phases/200-idempotent-verification-sign-off/regressions.ndjson"
      provides: "Zero-row hard close signal"
    - path: ".planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md"
      provides: "Final maintainer ACCEPT/REJECT decision"
    - path: ".planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md"
      provides: "Final deterministic verification report"
  key_links:
    - from: ".planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md"
      to: ".planning/phases/200-idempotent-verification-sign-off/regressions.ndjson"
      via: "ACCEPT requires empty regression ledger"
      pattern: "Final maintainer decision: ACCEPT"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md"
      via: "Requirement completion mirrors verified artifacts"
      pattern: "VER-01|VER-02|VER-03|STY-02|STY-03"
  prohibitions:
    - statement: "No pending, human_needed, or stale Phase 200 requirement state at close."
      status: "resolved by plan action"
      verification: "final reconciliation grep checks Phase 200-owned rows and reports"
---

<objective>
Run final closeout, collect maintainer ACCEPT sign-off, and reconcile Phase 200 state.

Purpose: The milestone is complete only when structured artifacts, deterministic guardrails, four-lens findings, and maintainer review all agree that the surface is forward-only with zero regressions.
Output: Final Phase 200 artifact package, accepted sign-off, verification report, and planning state/requirements reconciliation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md
@.planning/phases/200-idempotent-verification-sign-off/200-RESEARCH.md
@.planning/phases/200-idempotent-verification-sign-off/200-PATTERNS.md
@.planning/phases/200-idempotent-verification-sign-off/200-VALIDATION.md
@.planning/phases/200-idempotent-verification-sign-off/200-UI-SPEC.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-06-SUMMARY.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-15-SUMMARY.md
@scripts/ci/verify_phase200_admin_guardrails.sh
@scripts/ci/verify_phase200_scorecard.mjs
@scripts/ci/verify_phase200_signoff.mjs
@accrue_admin/e2e/phase200-scorecard.mjs
@accrue_admin/e2e/phase200-judge.mjs
@accrue_admin/e2e/phase200-signoff.mjs
</context>

## Artifacts this phase produces

- Generated JSON artifact: `.planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json`.
- Generated JSON artifact: `.planning/phases/200-idempotent-verification-sign-off/final.cells.json`.
- Generated JSON artifact: `.planning/phases/200-idempotent-verification-sign-off/scorecard.delta.json`.
- Generated NDJSON artifact: `.planning/phases/200-idempotent-verification-sign-off/regressions.ndjson`.
- Generated JSON artifact: `.planning/phases/200-idempotent-verification-sign-off/artifacts.manifest.json`.
- Generated JSON artifact: `.planning/phases/200-idempotent-verification-sign-off/judge.findings.json`.
- Markdown report: `.planning/phases/200-idempotent-verification-sign-off/200-SCORECARD.md`.
- Markdown report: `.planning/phases/200-idempotent-verification-sign-off/200-STORYBOOK-COVERAGE.md`.
- Markdown report: `.planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md`.
- Markdown decision surface: `.planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md`.
- Planning reconciliation edits: `.planning/REQUIREMENTS.md` and `.planning/STATE.md`.

<tasks>

<task type="auto">
  <name>Task 1: Generate final deterministic evidence package</name>
  <files>.planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json, .planning/phases/200-idempotent-verification-sign-off/final.cells.json, .planning/phases/200-idempotent-verification-sign-off/scorecard.delta.json, .planning/phases/200-idempotent-verification-sign-off/regressions.ndjson, .planning/phases/200-idempotent-verification-sign-off/artifacts.manifest.json, .planning/phases/200-idempotent-verification-sign-off/judge.findings.json, .planning/phases/200-idempotent-verification-sign-off/200-SCORECARD.md, .planning/phases/200-idempotent-verification-sign-off/200-STORYBOOK-COVERAGE.md, .planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md, .planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md</files>
  <read_first>
    <file>.planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/final.cells.json (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/scorecard.delta.json (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/regressions.ndjson (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/artifacts.manifest.json (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-SCORECARD.md (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-STORYBOOK-COVERAGE.md (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md (generated or new)</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md</file>
    <file>scripts/ci/verify_phase200_admin_guardrails.sh</file>
    <file>accrue_admin/e2e/phase200-scorecard.mjs</file>
    <file>accrue_admin/e2e/phase200-judge.mjs</file>
    <file>accrue_admin/e2e/phase200-signoff.mjs</file>
  </read_first>
  <action>Per D-01, D-16, D-17, D-18, D-24, D-25, D-28, D-30, and D-31, run the complete deterministic Phase 200 guardrail runner, regenerate scorecard artifacts, generate judge findings, and generate an evidence-backed sign-off package. The final package must include `baseline.union.cells.json`, `final.cells.json`, `scorecard.delta.json`, empty `regressions.ndjson`, `artifacts.manifest.json`, `judge.findings.json`, `200-SCORECARD.md`, `200-STORYBOOK-COVERAGE.md`, `200-VERIFICATION.md`, and `200-SIGN-OFF.md`. Markdown must summarize structured artifacts but never override them. If any deterministic gate fails, record the failing command and blocking artifact in `200-VERIFICATION.md` and do not request ACCEPT.</action>
  <acceptance_criteria>
    - CLI assertion: `bash scripts/ci/verify_phase200_admin_guardrails.sh` passes.
    - CLI assertion: `cd accrue_admin && npm run phase200:scorecard` or direct `node e2e/phase200-scorecard.mjs` generates final artifacts.
    - CLI assertion: `node scripts/ci/verify_phase200_scorecard.mjs` passes with zero regression rows.
    - CLI assertion: `node accrue_admin/e2e/phase200-judge.mjs` generates or updates `judge.findings.json` from the final structured artifacts.
    - CLI assertion: `node accrue_admin/e2e/phase200-signoff.mjs` generates or updates `200-SIGN-OFF.md` from `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, `200-SCORECARD.md`, `200-STORYBOOK-COVERAGE.md`, `200-VERIFICATION.md`, and `judge.findings.json`.
    - CLI assertion: `node scripts/ci/verify_phase200_signoff.mjs` passes after sign-off generation.
    - Artifact assertion: `regressions.ndjson` exists and contains zero non-empty lines.
    - Report assertion: the automated artifact/report presence check fails closed unless `200-STORYBOOK-COVERAGE.md` and `200-VERIFICATION.md` exist and reference the required canonical artifacts and closeout commands.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_phase200_admin_guardrails.sh</automated>
    <automated>cd accrue_admin && npm run phase200:scorecard</automated>
    <automated>node scripts/ci/verify_phase200_scorecard.mjs</automated>
    <automated>node accrue_admin/e2e/phase200-judge.mjs</automated>
    <automated>node accrue_admin/e2e/phase200-signoff.mjs</automated>
    <automated>node scripts/ci/verify_phase200_signoff.mjs</automated>
    <automated>node -e 'const fs=require("fs"); const base=".planning/phases/200-idempotent-verification-sign-off"; const reportFiles=["200-STORYBOOK-COVERAGE.md","200-VERIFICATION.md"]; const combined=reportFiles.map((file)=>{ const p=`${base}/${file}`; if(!fs.existsSync(p)) throw new Error(`${p} missing`); const text=fs.readFileSync(p,"utf8"); if(!text.trim()) throw new Error(`${p} empty`); return text; }).join("\n"); const required=["baseline.union.cells.json","final.cells.json","scorecard.delta.json","regressions.ndjson","artifacts.manifest.json","judge.findings.json","200-SCORECARD.md","200-STORYBOOK-COVERAGE.md","200-VERIFICATION.md","200-SIGN-OFF.md","bash scripts/ci/verify_phase200_admin_guardrails.sh","cd accrue_admin && npm run phase200:scorecard","node scripts/ci/verify_phase200_scorecard.mjs","node accrue_admin/e2e/phase200-judge.mjs","node accrue_admin/e2e/phase200-signoff.mjs","node scripts/ci/verify_phase200_signoff.mjs"]; const missing=required.filter((needle)=>!combined.includes(needle)); if(missing.length){ throw new Error(`Phase 200 final reports missing refs: ${missing.join(", ")}`); }'</automated>
  </verify>
  <done>All deterministic Phase 200 artifacts exist, verify structurally, and report zero regressions before maintainer review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer photographic and interaction sign-off checkpoint</name>
  <files>.planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md, .planning/phases/200-idempotent-verification-sign-off/judge.findings.json, .planning/phases/200-idempotent-verification-sign-off/artifacts.manifest.json</files>
  <read_first>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-SCORECARD.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-STORYBOOK-COVERAGE.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/judge.findings.json</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/artifacts.manifest.json</file>
  </read_first>
  <what-built>Deterministic Phase 200 evidence package, scorecard, Storybook coverage report, verification report, judge findings, and sign-off draft.</what-built>
  <how-to-verify>
    1. Review `.planning/phases/200-idempotent-verification-sign-off/200-SCORECARD.md` and confirm zero regressions, zero coverage downgrades, and all comparable cells at or above baseline.
    2. Review `.planning/phases/200-idempotent-verification-sign-off/200-STORYBOOK-COVERAGE.md` and confirm every registry family and all eight group contracts are covered, with Storybook light/dark parity and `/dev/components` drift status green.
    3. Review `.planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md` and confirm deterministic guardrails passed: package docs, Storybook, route axe, no-FOUC/theme, reduced-motion, Phase 199, scorecard, and sign-off verifier.
    4. Review `judge.findings.json` and the judge table in `200-SIGN-OFF.md`; confirm there are no unresolved `BLOCKER` or `REPAIR-IN-PHASE` findings.
    5. Inspect referenced traces/screenshots/log refs in `artifacts.manifest.json` for focus, Escape/backdrop, scroll reachability, actionability, theme persistence, and reduced motion.
  </how-to-verify>
  <acceptance_criteria>
    - Human assertion: maintainer explicitly approves ACCEPT or names exact blocking finding IDs for REJECT.
    - Artifact assertion: approval or rejection is recorded in `200-SIGN-OFF.md` with evidence refs.
    - Process assertion: no broad UI polish request is treated as blocking unless it cites a locked Phase 200 requirement or allowed contract reference.
  </acceptance_criteria>
  <action>Pause after deterministic verification passes and present the Phase 200 scorecard, Storybook coverage report, verification report, judge findings, sign-off draft, and manifest evidence refs for maintainer review. Per D-24, D-28, D-29, and D-30, accept only an explicit maintainer response of approval for ACCEPT; otherwise record the named blocking finding IDs and repair/rerun requirements for REJECT. Do not treat advisory comments, reviewed todos, or broad polish requests as blockers unless they cite a Phase 200 locked requirement or allowed contract reference.</action>
  <verify>
    <automated>node scripts/ci/verify_phase200_scorecard.mjs && node scripts/ci/verify_phase200_signoff.mjs</automated>
  </verify>
  <done>Maintainer approval is captured for ACCEPT, or exact blocking finding IDs and repair commands are captured for REJECT.</done>
  <resume-signal>Type "approved" to record ACCEPT, or describe blocking finding IDs and required repairs for REJECT.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record ACCEPT and reconcile state</name>
  <files>.planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md, .planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-VERIFICATION.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md</file>
  </read_first>
  <action>Per D-02, D-03, D-04, D-05, D-24, D-28, D-29, and D-31, after maintainer approval update `200-SIGN-OFF.md` so the final line starts `Final maintainer decision: ACCEPT` and names the evidence source. Update `200-VERIFICATION.md` with final command results, requirement coverage, human verification completed status, and residual risks if any. Mark VER-01, VER-02, VER-03, STY-02, and STY-03 complete in `.planning/REQUIREMENTS.md`, update their traceability rows from `Pending` to `Complete`, and update `.planning/STATE.md` so Phase 200 and the current milestone closeout no longer show `Not started`, `Pending`, `human_needed`, or ambiguous partial status for Phase 200-owned items. If the checkpoint is rejected, keep the final line as `Final maintainer decision: REJECT`, name blocking finding IDs and rerun commands, do not mark requirements complete, and stop for repair subplans.</action>
  <acceptance_criteria>
    - Source assertion: `200-SIGN-OFF.md` has exactly one final decision line and, for success, it begins `Final maintainer decision: ACCEPT`.
    - CLI assertion: scorecard and sign-off verifiers pass after reconciliation.
    - Source assertion: `.planning/REQUIREMENTS.md` marks VER-01, VER-02, VER-03, STY-02, and STY-03 as complete only on ACCEPT.
    - Source assertion: Phase 200-owned rows in `.planning/STATE.md` and `.planning/REQUIREMENTS.md` contain no stale `Pending`, `human_needed`, or `Not started` status after ACCEPT.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_phase200_scorecard.mjs && node scripts/ci/verify_phase200_signoff.mjs</automated>
    <automated>node -e 'const fs=require("fs"); const ids=["VER-01","VER-02","VER-03","STY-02","STY-03"]; const req=fs.readFileSync(".planning/REQUIREMENTS.md","utf8"); const state=fs.readFileSync(".planning/STATE.md","utf8"); const signoff=fs.readFileSync(".planning/phases/200-idempotent-verification-sign-off/200-SIGN-OFF.md","utf8"); const failures=[]; if(!/^Final maintainer decision: ACCEPT\b/m.test(signoff)) failures.push("200-SIGN-OFF.md lacks ACCEPT final decision line"); for(const id of ids){ const lines=req.split(/\r?\n/).filter((line)=>line.includes(id)); if(lines.length===0) failures.push(`missing ${id} in REQUIREMENTS.md`); if(lines.some((line)=>new RegExp(`^\\s*- \\[ \\].*${id}\\b`).test(line))) failures.push(`${id} checkbox remains open in REQUIREMENTS.md`); if(lines.some((line)=>/\\b(Pending|human_needed|Not started)\\b/i.test(line))) failures.push(`${id} has stale status in REQUIREMENTS.md: ${lines.join(" || ")}`); if(!lines.some((line)=>/\\bComplete\\b/i.test(line) || new RegExp(`^\\s*- \\[[xX]\\].*${id}\\b`).test(line))) failures.push(`${id} lacks complete marker in REQUIREMENTS.md`); } const phaseState=state.split(/\r?\n/).filter((line)=>/\\b200\\b|Phase:\\s*200|Phase 200|VER-01|VER-02|VER-03|STY-02|STY-03|idempotent-verification-sign-off/i.test(line)).join("\n"); if(/\\b(human_needed|Pending|Not started)\\b/i.test(phaseState)) failures.push(`STATE.md has stale Phase 200 status: ${phaseState.split(/\r?\n/).filter((line)=>/\\b(human_needed|Pending|Not started)\\b/i.test(line)).join(" || ")}`); if(!/\\b(Complete|ACCEPT|accepted|verified)\\b/i.test(phaseState)) failures.push("STATE.md Phase 200 slice lacks complete/ACCEPT/verified status"); if(failures.length){ console.error(failures.join("\n")); process.exit(1); }'</automated>
  </verify>
  <done>Phase 200 has explicit ACCEPT sign-off, verified artifacts, completed requirement/state rows, and no stale pending human state.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Whole surface proven forward-only >= baseline with zero regressions, Storybook complete/themed, accessibility/no-FOUC green, explicit ACCEPT | 200-06 | COVERED | Final closeout plan verifies and reconciles all goal truths.
REQ | VER-01 | Empty regressions and union baseline comparison | 200-06 | COVERED | Runs final scorecard and verifier.
REQ | VER-02 | Accessibility/no-FOUC/reduced-motion/group/a11y guardrails green | 200-06 | COVERED | Runs full deterministic guardrails and records `200-VERIFICATION.md`.
REQ | VER-03 | Judge plus maintainer ACCEPT sign-off | 200-06 | COVERED | Blocking human checkpoint and final decision reconciliation.
REQ | STY-02 | Storybook coverage complete | 200-06 | COVERED | Requires `200-STORYBOOK-COVERAGE.md` and guardrail pass.
REQ | STY-03 | Storybook theme parity complete | 200-06 | COVERED | Requires rendered Storybook theming and asset proof.
RESEARCH | Final closeout evidence package | Structured artifacts canonical, markdown summaries explanatory | 200-06 | COVERED | Generates all locked artifacts under Phase 200.
CONTEXT | D-01,D-02,D-03,D-04,D-05,D-16,D-17,D-18,D-24,D-25,D-28,D-29,D-31 | Staged closeout, verification-only scope, waves, todo boundary, artifact paths, regression signal, CI, decision surface, structured artifacts, ACCEPT/REJECT semantics, no pending state | 200-06 | COVERED | Task actions cite each decision.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deterministic artifacts -> maintainer decision | Structured evidence is reviewed by a human before ACCEPT. |
| human checkpoint -> planning state | Maintainer approval or rejection changes requirements and state docs. |
| final reports -> future milestone consumers | Closeout artifacts become durable historical evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-21 | Repudiation | final ACCEPT | mitigate | Require empty regressions, passing verifiers, exact decision line, and human checkpoint per D-24/D-28. |
| T-200-22 | Tampering | final artifacts | mitigate | Regenerate through deterministic scripts and verify structured JSON/NDJSON before markdown/state updates. |
| T-200-23 | Repudiation | stale requirement/state docs | mitigate | Reconcile `.planning/REQUIREMENTS.md` and `.planning/STATE.md`; grep for stale Phase 200 pending status per D-31. |
| T-200-24 | Information Disclosure | evidence refs | mitigate | Keep bulky traces/screenshots as manifest refs under generated roots; do not embed secrets or external-service data. |
| T-200-SC | Tampering | package installs | mitigate | No package-manager install is planned; if execution adds npm, pip, or cargo installs, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
Full deterministic guardrail runner, final scorecard generation, scorecard verifier, sign-off verifier, human checkpoint, and planning reconciliation grep must all pass for ACCEPT.
</verification>

<success_criteria>
The final Phase 200 artifact package proves zero regressions and complete Storybook/accessibility/theme coverage, and `200-SIGN-OFF.md` records explicit maintainer ACCEPT with requirements/state reconciled.
</success_criteria>

<output>
Create `.planning/phases/200-idempotent-verification-sign-off/200-06-SUMMARY.md` when done.
</output>

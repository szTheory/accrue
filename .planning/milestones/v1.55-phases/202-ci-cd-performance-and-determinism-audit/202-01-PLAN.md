---
phase: 202-ci-cd-performance-and-determinism-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
  - CI-05
must_haves:
  truths:
    - "CI-01: Maintainer can read one current CI topology map covering triggers, job graph, matrix shape, services, cache posture, and likely critical path from checked-in workflow evidence."
    - "CI-02: Maintainer can see duplicated setup, slow/static bottlenecks, flaky/determinism risks, cache risks, release risks, and provider-parity risks with concrete repo evidence."
    - "CI-03: Target pipeline recommendations preserve high-value gates and require measurement before deleting, demoting, or making checks optional."
    - "CI-04: Follow-up work is classified with priority, expected impact, tradeoff, implementation approach, verification, rollback, metric-needed status, and suggested milestone-slice fit for Phase 204."
    - "CI-05: Baseline metrics still needing live GitHub run data are explicit and dynamic claims are labeled as collected evidence, metrics-needed, or assumption."
    - "D-01 D-02 D-03: The report treats measurement boundary, recommendation stance, provider/determinism truth, and Phase 204 handoff as one coherent decision set in Accrue's measured, exact, Phoenix-native, proof-checkable voice."
    - "D-04 D-05 D-06 D-07: The report is static-first, optionally includes only bounded read-only GitHub run-history data, and keeps missing or incomplete live metrics non-blocking and visibly separated from static findings."
    - "D-08 D-09 D-10 D-11 D-12: Recommendations are blunt but measure-first, preserve high-value gates, describe the locked target pipeline shape, and hand release-recovery preflight work to ranked follow-up implementation."
    - "D-13 D-14 D-15 D-16 D-17: Provider parity, fake-backed determinism, matrix fidelity, cache, flake, and runtime claims are classified by proved/skipped/static-risk/measured-baseline status."
    - "D-18 D-19 D-20 D-21: The final section is a structured Phase 204 Handoff table, and Phase 202 priorities remain local CI/CD audit priorities rather than final cross-audit ranking."
    - "D-22 D-23 D-24: The audit serves maintainer/reviewer/contributor CI jobs-to-be-done, favors named proof-checkable gates, and stays inside stable-core audit-only scope."
  artifacts:
    - path: .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
      provides: "Final Phase 202 CI/CD performance and determinism audit"
      contains: "Phase 204 Handoff"
      min_lines: 220
  key_links:
    - from: .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
      to: .github/workflows/ci.yml
      via: "static topology, matrix, cache, provider-lane, critical-path, and required-check evidence"
      pattern: "release-gate|host-integration|playwright-e2e|host-docker-smoke|annotation-sweep|live-stripe"
    - from: .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
      to: scripts/ci/README.md
      via: "maintainer-facing gate map and triage evidence"
      pattern: "scripts/ci|watch_ci|host-integration|REL gates"
    - from: .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
      to: .planning/phases/204-ranked-hardening-roadmap/204-HARDENING-ROADMAP.md
      via: "Phase 204 Handoff table rows designed for later ranking"
      pattern: "Phase 204 Handoff|Suggested milestone-slice fit"
---

<objective>
Finalize `.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md` as an evidence-backed, audit-only CI/CD performance and determinism report for the current audit milestone.

Purpose: Give the maintainer one proof-checkable document that separates static CI topology evidence from live metrics still needed, names bottlenecks and determinism risks plainly, and hands Phase 204 rankable follow-up rows without changing CI, release automation, source code, APIs, DB defaults, or UI.

Output: A final Phase 202 audit document plus the standard execution summary file.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CONTEXT.md
@.planning/phases/202-ci-cd-performance-and-determinism-audit/202-RESEARCH.md
@.planning/phases/202-ci-cd-performance-and-determinism-audit/202-PATTERNS.md
@.planning/phases/202-ci-cd-performance-and-determinism-audit/202-VALIDATION.md
@.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
@.planning/phases/201-software-quality-evaluation/201-01-SUMMARY.md
@.planning/phases/201-software-quality-evaluation/201-SOFTWARE-QUALITY-AUDIT.md
@.planning/phases/204-ranked-hardening-roadmap/204-HARDENING-ROADMAP.md
@.github/workflows/ci.yml
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@scripts/ci/README.md
@brandbook/voice.md
</context>

## Artifacts This Phase Produces

| Artifact | Created or Updated | Purpose |
|---|---|---|
| `.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md` | Updated | Final CI/CD performance and determinism audit for CI-01 through CI-05. |
| `.planning/phases/202-ci-cd-performance-and-determinism-audit/202-01-SUMMARY.md` | Created by executor | Execution summary, verification evidence, and boundary proof for this plan. |

<tasks>

<task type="auto">
  <name>Task 1: Lock topology evidence and measurement boundaries</name>
  <files>.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md</files>
  <read_first>
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CONTEXT.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-RESEARCH.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-PATTERNS.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-VALIDATION.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .github/workflows/ci.yml
    - .github/workflows/accrue_admin_browser.yml
    - .github/workflows/accrue_host_uat.yml
    - .github/workflows/accrue_admin_assets.yml
    - .github/workflows/release-please.yml
    - .github/workflows/publish-hex.yml
    - .github/workflows/release-pr-automation.yml
    - scripts/ci/README.md
    - scripts/ci/watch_ci.sh
  </read_first>
  <action>Refine the audit header, Executive Summary, Current Pipeline Map, Baseline Metrics Needed, and Evidence Appendix so CI-01 and CI-05 are complete. Use `.github/workflows/ci.yml` as the primary topology source for triggers, job IDs, `needs`, matrix cells, service containers, caches, live-stripe scheduling, and likely static critical path. Per D-04, D-05, D-06, and D-07, static inspection is sufficient; if `gh` is already authenticated, a bounded read-only snapshot may be collected, but it must be labeled with collection date, branch/filter, run count, and partial/exhaustive status. If no snapshot is collected, say that plainly and keep p50/p95 job duration, step timing, cache-hit state, flake/rerun rate, slowest tests, compile profile, Docker cold/warm duration, and provider proved-vs-skipped counts in `Baseline Metrics Needed`. Do not edit workflows, scripts, source trees, release automation, branch protection, public docs, DB defaults, APIs, or UI implementation.</action>
  <acceptance_criteria>
    - The audit includes a `Current Pipeline Map` row set covering `CI`, `release-gate`, `docs-contracts-shift-left`, admin guardrails, `host-integration`, `playwright-e2e`, `host-docker-smoke`, `annotation-sweep`, `Release Please`, and `Publish Hex Recovery`.
    - The audit includes `Baseline Metrics Needed` with p50/p95 wall time, step timings, cache-hit state, flake/rerun rate, slowest tests, compile profile, Docker cold/warm duration, and live-stripe proved-vs-skipped count.
    - Any live GitHub run-history content is explicitly labeled by collection date, branch/filter, run count, and partial/exhaustive status; if absent, the audit states that static evidence is the basis and live metrics remain needed.
    - Dynamic performance, cache, flake, or provider claims use collected-evidence, metrics-needed, assumption, or static-inspection wording rather than unsupported measured savings.
  </acceptance_criteria>
  <verify>
    <automated>audit=.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md; test -s "$audit" && rg -q '^#{1,3} Executive Summary\b' "$audit" && rg -q '^#{1,3} Current Pipeline Map\b' "$audit" && rg -q '^#{1,3} Baseline Metrics Needed\b' "$audit" && rg -q '^#{1,3} Evidence Appendix\b' "$audit" && rg -q 'p50' "$audit" && rg -q 'p95' "$audit" && rg -q 'cache-hit' "$audit" && rg -q 'flake' "$audit" && rg -q 'rerun' "$audit" && rg -q 'proved-vs-skipped' "$audit" && rg -q 'static inspection' "$audit" && rg -q 'metrics-needed' "$audit" && rg -q 'assumption' "$audit"</automated>
  </verify>
  <done>CI topology, critical-path basis, measurement gaps, and static-vs-live evidence boundaries are explicit and traceable to checked-in workflow/script evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize findings and measure-first target pipeline</name>
  <files>.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md</files>
  <read_first>
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CONTEXT.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-RESEARCH.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-PATTERNS.md
    - .github/workflows/ci.yml
    - .github/workflows/release-please.yml
    - .github/workflows/publish-hex.yml
    - scripts/ci/README.md
    - scripts/ci/accrue_host_uat.sh
    - scripts/ci/accrue_host_verify_browser.sh
    - scripts/ci/accrue_host_seed_e2e.exs
    - scripts/ci/annotation_sweep.sh
    - scripts/ci/compile_matrix.sh
    - scripts/ci/verify_release_contract.sh
    - scripts/ci/verify_release_manifest_alignment.sh
    - scripts/ci/capture_linked_release_proof.sh
    - scripts/ci/accrue_host_hex_smoke.sh
    - accrue/mix.exs
    - accrue/config/runtime.exs
    - accrue/test/live_stripe/
    - accrue/test/accrue/telemetry/otel_test.exs
    - examples/accrue_host/mix.exs
    - examples/accrue_host/package.json
    - accrue_admin/package.json
    - brandbook/voice.md
  </read_first>
  <action>Refine `Findings by Category`, `Target Pipeline`, `Validation Plan`, and `Recommended Local Commands` so CI-02 and CI-03 are complete. Per D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16, and D-17, name duplicated CI work, skip-capable provider lanes, label-only matrix risk, release recovery order dependence, cache risk, flake/determinism risk, and likely critical-path drag plainly while preserving high-value gates. The target pipeline must keep docs/contracts, release manifest checks, core/admin/portal package gates, host deterministic proof, provider-parity truth, and release proof. Do not recommend deleting, demoting, or making required gates optional from static inspection alone; phrase such changes as dependent on collected job duration, step timing, cache-hit, flake/rerun, and proved-vs-skipped evidence. Keep fake-backed deterministic tests as the merge-blocking default, and treat live Stripe as periodic/manual provider canary truth only when secrets and fixtures actually run.</action>
  <acceptance_criteria>
    - Findings cover correctness, performance, determinism/flakiness, caching, matrix/version policy, test-suite quality, security/supply chain, release, and developer-experience/docs.
    - The report names `release-gate`, `host-integration`, `playwright-e2e`, `host-docker-smoke`, `annotation-sweep`, `live-stripe`, `publish-hex`, and `release-please` in the relevant findings or target-pipeline sections.
    - Provider truth is binary: proved only when Stripe test mode actually runs with required secrets/fixtures; missing secrets/fixtures are skipped/not proved, not parity proof.
    - Target pipeline text preserves high-value gates and uses measure-first language before any topology, cache, matrix, Docker, Playwright, or release-gate cleanup recommendation.
    - The audit voice follows D-03: measured, exact, Phoenix-native, durable, mechanism-led, and proof-checkable.
  </acceptance_criteria>
  <verify>
    <automated>audit=.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md; test -s "$audit" && rg -q '^#{1,3} Findings by Category\b' "$audit" && rg -q '^#{1,3} Target Pipeline\b' "$audit" && rg -q '^#{1,3} Validation Plan\b' "$audit" && rg -q '^#{1,3} Recommended Local Commands\b' "$audit" && rg -q 'Correctness' "$audit" && rg -q 'Performance' "$audit" && rg -q 'Determinism' "$audit" && rg -q 'Flakiness' "$audit" && rg -q 'Caching' "$audit" && rg -q 'Matrix' "$audit" && rg -q 'Security' "$audit" && rg -q 'Release' "$audit" && rg -qi 'developer[ -]experience' "$audit" && rg -q 'measure first' "$audit" && rg -q 'release-gate' "$audit" && rg -q 'host-integration' "$audit" && rg -q 'playwright-e2e' "$audit" && rg -q 'host-docker-smoke' "$audit" && rg -q 'annotation-sweep' "$audit" && rg -q 'live-stripe' "$audit" && rg -q 'publish-hex' "$audit" && rg -q 'release-please' "$audit" && rg -q 'proved' "$audit" && rg -q 'skipped' "$audit" && rg -q 'Fake' "$audit"</automated>
  </verify>
  <done>Findings and target pipeline recommendations are blunt, evidence-backed, measure-first, provider-honest, and scoped to recommendations only.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 204 handoff, requirement coverage, and boundary proof</name>
  <files>.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md</files>
  <read_first>
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-CONTEXT.md
    - .planning/phases/202-ci-cd-performance-and-determinism-audit/202-VALIDATION.md
    - .planning/phases/201-software-quality-evaluation/201-01-SUMMARY.md
    - .planning/phases/201-software-quality-evaluation/201-SOFTWARE-QUALITY-AUDIT.md
    - .planning/phases/204-ranked-hardening-roadmap/204-HARDENING-ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - brandbook/voice.md
  </read_first>
  <action>Add or refine the final `Phase 204 Handoff`, `Requirement Coverage`, `Assumptions and Metrics-Needed Items`, and `Phase Handoff and Boundary` sections so CI-04 and the phase gate are complete. Per D-18, D-19, D-20, D-21, D-22, D-23, and D-24, the Phase 204 table must contain these columns: area, evidence path, current risk, priority local to Phase 202, expected impact, tradeoff, implementation approach, verification, rollback, metric-needed status, and suggested milestone-slice fit. Keep rows rankable but not issue-ready implementation cards. Add explicit CI-01 through CI-05 coverage. Include a boundary statement that Phase 202 did not change CI workflow topology, branch protection, package release automation, runtime behavior, public APIs, DB defaults, UI implementation, or required-check semantics.</action>
  <acceptance_criteria>
    - `Phase 204 Handoff` exists and each row has area, evidence path, current risk, priority local to Phase 202, expected impact, tradeoff, implementation approach, verification, rollback, metric-needed status, and suggested milestone-slice fit.
    - `Requirement Coverage` explicitly maps CI-01, CI-02, CI-03, CI-04, and CI-05 to concrete audit sections.
    - The final boundary section states the phase is audit-only and lists the no-change surfaces from D-24 and the phase boundary.
    - The final audit remains coherent with Phase 201 and Phase 204: Phase 202 owns specialist CI/CD evidence and local priorities; Phase 204 owns final cross-audit ordering.
    - `git status --short -- .github/workflows scripts/ci accrue accrue_admin accrue_portal examples` prints no Phase 202 implementation/source changes.
  </acceptance_criteria>
  <verify>
    <automated>audit=.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md; test -s "$audit" && rg -q '^#{1,3} Phase 204 Handoff\b' "$audit" && rg -q 'Area' "$audit" && rg -q 'Evidence path' "$audit" && rg -q 'Current risk' "$audit" && rg -q 'Priority local to Phase 202' "$audit" && rg -q 'Expected impact' "$audit" && rg -q 'Tradeoff' "$audit" && rg -q 'Implementation approach' "$audit" && rg -q 'Verification' "$audit" && rg -q 'Rollback' "$audit" && rg -q 'Metric-needed status' "$audit" && rg -q 'Suggested milestone-slice fit' "$audit" && rg -q '^#{1,3} Requirement Coverage\b' "$audit" && rg -q 'CI-01' "$audit" && rg -q 'CI-02' "$audit" && rg -q 'CI-03' "$audit" && rg -q 'CI-04' "$audit" && rg -q 'CI-05' "$audit" && rg -q '^#{1,3} Assumptions and Metrics-Needed Items\b' "$audit" && rg -q '^#{1,3} Phase Handoff and Boundary\b' "$audit" && rg -q 'audit-only' "$audit" && rg -q 'Phase 202 gate' "$audit" && ! rg -qi 'Phase 202 draft baseline' "$audit" && ! rg -qi '^status:[[:space:]]*draft\b' "$audit" && ! rg -qi '^Status:[[:space:]]*draft\b' "$audit" && ! rg -qi '^\*\*Status:\*\*[[:space:]]*draft\b' "$audit" && test -z "$(git status --short -- .github/workflows scripts/ci accrue accrue_admin accrue_portal examples)"</automated>
  </verify>
  <done>The audit has Phase 204-ready handoff rows, explicit CI requirement coverage, and source-control proof that Phase 202 stayed audit-only.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 202 | Produce `202-CI-CD-PERFORMANCE-AUDIT.md` covering workflow topology, static critical path, measurement needs, bottlenecks, flaky/determinism risks, cache risks, and target pipeline recommendations. | 202-01 | COVERED | Tasks 1-3 refine the audit document only. |
| REQ | CI-01 | Current CI topology, trigger model, matrix shape, service usage, cache posture, and likely critical path in one document. | 202-01 | COVERED | Task 1. |
| REQ | CI-02 | Duplicated setup, bottlenecks, flaky/determinism risks, cache risks, release risks, and provider-parity risks with repo evidence. | 202-01 | COVERED | Task 2. |
| REQ | CI-03 | Target pipeline preserves high-value gates while measuring before demoting or deleting checks. | 202-01 | COVERED | Task 2. |
| REQ | CI-04 | Follow-up work classified by priority, impact, tradeoff, implementation, verification, and rollback. | 202-01 | COVERED | Task 3 with D-19 columns. |
| REQ | CI-05 | Required baseline metrics needing live GitHub run data are recorded honestly. | 202-01 | COVERED | Task 1 and Task 3. |
| RESEARCH | R-01 | Static-first audit over checked-in GitHub Actions configuration, CI scripts, Mix aliases, provider-lane semantics, and release workflows. | 202-01 | COVERED | Tasks 1-2. |
| RESEARCH | R-02 | Optional read-only `gh` snapshot is feasible but must be bounded, partial if small, and non-authoritative. | 202-01 | COVERED | Task 1. |
| RESEARCH | R-03 | Provider proof must classify live Stripe as proved only when secrets/fixtures cause real Stripe test-mode execution. | 202-01 | COVERED | Task 2. |
| RESEARCH | R-04 | Release recovery risk is human package-order prose in `publish-hex.yml` compared with ordered Release Please publish. | 202-01 | COVERED | Task 2 and Task 3. |
| RESEARCH | R-05 | Phase 204 handoff table must be structured for downstream ranking. | 202-01 | COVERED | Task 3. |
| CONTEXT | D-01 | Treat measurement boundary, recommendation stance, provider/determinism truth, and Phase 204 handoff as one decision set. | 202-01 | COVERED | Tasks 1-3. |
| CONTEXT | D-02 | Use specialist research and local repo evidence for one-shot recommendations. | 202-01 | COVERED | Tasks 1-3. |
| CONTEXT | D-03 | Use Accrue's measured, exact, Phoenix-native, durable, mechanism-led, proof-checkable voice. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-04 | Phase 202 is static-first and audit-only. | 202-01 | COVERED | Task 1 and Task 3. |
| CONTEXT | D-05 | Optional read-only GitHub Actions run-history snapshot must be labeled. | 202-01 | COVERED | Task 1. |
| CONTEXT | D-06 | Missing live metrics do not block Phase 202 and remain in Baseline Metrics Needed. | 202-01 | COVERED | Task 1. |
| CONTEXT | D-07 | Dynamic claims need explicit labels. | 202-01 | COVERED | Task 1. |
| CONTEXT | D-08 | Be blunt but measure-first. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-09 | Do not delete, demote, or optionalize required gates from static inspection alone. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-10 | Preserve high-value gates. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-11 | Use the locked target pipeline shape. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-12 | Recommend release recovery preflight checks as follow-up only. | 202-01 | COVERED | Task 2 and Task 3. |
| CONTEXT | D-13 | Provider parity truth is binary proved versus skipped/not proved. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-14 | Present fail-scheduled or advisory/skip-capable live Stripe paths. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-15 | Fake-backed deterministic tests remain merge-blocking default. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-16 | Matrix cells must materially change behavior or be relabeled/redesigned. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-17 | Cache, flake, and runtime claims stay separated as static risk versus measured baseline. | 202-01 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-18 | Optimize for readable report plus structured Phase 204 handoff. | 202-01 | COVERED | Task 3. |
| CONTEXT | D-19 | Phase 204 handoff rows use the locked column set. | 202-01 | COVERED | Task 3. |
| CONTEXT | D-20 | Phase 202 priorities are local CI/CD priorities. | 202-01 | COVERED | Task 3. |
| CONTEXT | D-21 | Avoid issue-ready implementation cards. | 202-01 | COVERED | Task 3. |
| CONTEXT | D-22 | Treat UX as developer experience jobs-to-be-done. | 202-01 | COVERED | Task 2 and Task 3. |
| CONTEXT | D-23 | Prefer named, proof-checkable gates over repeated ambiguous matrix failures. | 202-01 | COVERED | Task 2. |
| CONTEXT | D-24 | Preserve stable-core audit-only scope; no product, UI, API, schema, or CI implementation work. | 202-01 | COVERED | Task 3. |
| CONTEXT | Deferred Ideas | CI topology changes, branch protection changes, release automation changes, live-Stripe behavior changes, matrix redesign, public docs rewrites, and UI/admin/portal work. | 202-01 | EXCLUDED | Explicitly out of scope; tasks forbid these edits. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local repo evidence -> audit document | Checked-in workflow/script/source evidence is transformed into claims in the final audit. |
| Optional GitHub Actions API -> audit document | Read-only `gh` output may enter the audit as partial run-history evidence. |
| Audit recommendations -> Phase 204 roadmap | Local CI/CD priorities become candidate implementation rows for later ranking. |
| Secret-bearing CI concepts -> audit prose | The audit may discuss presence/absence of secrets without exposing values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-202-01 | Tampering | `.github/workflows/`, `scripts/ci/`, package trees | mitigate | Tasks modify only `202-CI-CD-PERFORMANCE-AUDIT.md`; Task 3 verifies `git status --short -- .github/workflows scripts/ci accrue accrue_admin accrue_portal examples` is empty for Phase 202 implementation surfaces. |
| T-202-02 | Repudiation | Provider-proof claims in audit | mitigate | Task 2 requires live Stripe to be labeled proved only when Stripe test-mode secrets/fixtures actually run; missing credentials are skipped/not proved. |
| T-202-03 | Repudiation | Performance/cache/flake claims | mitigate | Task 1 requires dynamic claims to be labeled as collected evidence, metrics-needed, assumption, or static inspection. |
| T-202-04 | Information Disclosure | Optional `gh` output, workflow logs, secrets | mitigate | Task 1 may record run IDs/timing/status only; it must not paste token, Stripe, Hex, or secret values. |
| T-202-05 | Tampering / Repudiation | Release-confidence recommendations | mitigate | Task 2 preserves high-value gates and forbids deleting, demoting, or optionalizing required checks from static inspection alone. |
| T-202-06 | Spoofing | Branch-protection / required-check truth | mitigate | Task 1 and Task 3 label required-check topology as inferred from committed workflow comments unless live branch-protection data is explicitly collected and labeled. |
| T-202-SC | Tampering | Package installs | mitigate | This audit phase installs no npm, pip, cargo, Hex, or other packages; verification is document/source assertion only. |
</threat_model>

<verification>
Overall phase checks:

- `audit=.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md; test -s "$audit" && rg -q '^#{1,3} Executive Summary\b' "$audit" && rg -q '^#{1,3} Current Pipeline Map\b' "$audit" && rg -q '^#{1,3} Baseline Metrics Needed\b' "$audit" && rg -q '^#{1,3} Findings by Category\b' "$audit" && rg -q '^#{1,3} Target Pipeline\b' "$audit" && rg -q '^#{1,3} Phase 204 Handoff\b' "$audit" && rg -q '^#{1,3} Requirement Coverage\b' "$audit" && rg -q '^#{1,3} Phase Handoff and Boundary\b' "$audit" && rg -q '^#{1,3} Evidence Appendix\b' "$audit" && rg -q 'Static' "$audit" && rg -q 'GitHub run' "$audit" && rg -q 'proved' "$audit" && rg -q 'skipped' "$audit" && rg -q 'rollback' "$audit" && rg -q 'metric' "$audit"`
- `audit=.planning/phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md; test -s "$audit" && rg -q 'release-gate' "$audit" && rg -q 'host-integration' "$audit" && rg -q 'playwright-e2e' "$audit" && rg -q 'host-docker-smoke' "$audit" && rg -q 'annotation-sweep' "$audit" && rg -q 'live-stripe' "$audit" && rg -q 'publish-hex' "$audit" && rg -q 'release-please' "$audit" && ! rg -qi 'Phase 202 draft baseline' "$audit" && ! rg -qi '^status:[[:space:]]*draft\b' "$audit" && ! rg -qi '^Status:[[:space:]]*draft\b' "$audit" && ! rg -qi '^\*\*Status:\*\*[[:space:]]*draft\b' "$audit"`
- `test -z "$(git status --short -- .github/workflows scripts/ci accrue accrue_admin accrue_portal examples)"`
</verification>

<success_criteria>
- `202-CI-CD-PERFORMANCE-AUDIT.md` is final, not draft-status, and covers CI-01 through CI-05 explicitly.
- Static repository evidence is separated from optional live GitHub run-history data and metrics-needed claims.
- D-01 through D-24 are implemented without adding deferred implementation work.
- The Phase 204 Handoff table has the locked D-19 columns and is rankable by later Phase 204 planning.
- No implementation/topology/source changes occur outside the audit document.
</success_criteria>

<output>
Create `.planning/phases/202-ci-cd-performance-and-determinism-audit/202-01-SUMMARY.md` when done.
</output>

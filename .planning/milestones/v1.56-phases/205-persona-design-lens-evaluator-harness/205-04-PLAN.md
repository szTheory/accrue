---
phase: 205-persona-design-lens-evaluator-harness
plan: 04
type: execute
wave: 3
depends_on: ["205-02", "205-03"]
files_modified:
  - accrue_admin/e2e/ratchet/ratchet-propose.mjs
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - "A 7th comparative graphic-design lens runs per image, attaching exactly one archetype-matched good + one bad committed exemplar per call (bounded at 2 exemplar images)"
    - "Design-lens candidates carry raised_by.lens_kind='design', a direction:air|cramped self-flag, and an exemplar_ref, and flow through the same harness validation + claim-key gate as persona findings"
    - "The design lens scores comparatively against brandbook-anchored exemplars, not an absolute award score, sharpening dims 2/3/5/8 (+1/6 support)"
  artifacts:
    - accrue_admin/e2e/ratchet/ratchet-propose.mjs (design-lens addition)
  key_links:
    - "Archetype-matched exemplar selection keyed off surface_type per DESIGN-LENS-RUBRIC.md"
    - "Design-lens identity re-derived by region-tags.js exactly like persona findings (no new identity path)"
---

<objective>
Add the 7th lens — a comparative graphic-design lens — to `ratchet-propose.mjs`. Per image it attaches exactly one archetype-matched good + one bad committed exemplar (from plan 02) inline as few-shot (bounded at 2 exemplar images, D-20), and emits design candidates that carry a `direction: air|cramped` self-flag plus an `exemplar_ref`, flowing through the SAME harness validation + claim-key gate as persona findings (no new identity path).

Purpose: The design lens is the brand/density defender's proposer input. A correctly-dense GOOD Accrue table as few-shot is the most direct suppressor of the over-whitespacing footgun; the `direction` flag lets the Phase-206 operator-density-defender apply a higher confirm bar to air-ward findings.
Output: The design-lens call + emission wired into the existing proposer, consuming plan-02 exemplars and the DESIGN-LENS-RUBRIC.

**Artifacts THIS plan produces:** the design-lens branch in `ratchet-propose.mjs` (comparative prompt anchored to the rubric, archetype-matched exemplar attach, `direction`/`exemplar_ref` fields on design candidates).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-persona-design-lens-evaluator-harness/205-CONTEXT.md
@.planning/phases/205-persona-design-lens-evaluator-harness/205-RESEARCH.md
@.planning/phases/205-persona-design-lens-evaluator-harness/205-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comparative design lens + archetype-matched exemplar attach</name>
  <files>accrue_admin/e2e/ratchet/ratchet-propose.mjs</files>
  <read_first>
    - accrue_admin/e2e/ratchet/ratchet-propose.mjs (plan 03 — the persona-lens call + emit gate to mirror)
    - accrue_admin/e2e/ratchet/DESIGN-LENS-RUBRIC.md (plan 02 — the comparative sub-rubric + output contract + archetype-matching key)
    - accrue_admin/e2e/ratchet/exemplars/PROVENANCE.json (plan 02 — which PNG maps to which archetype/role)
    - accrue_admin/e2e/score-visuals.mjs:199-206 (image content-block shape to reuse for exemplar attach)
  </read_first>
  <action>
Add a 7th lens (`lens_kind: "design"`) to the per-image fan-out in `ratchet-propose.mjs`. Reuse the SAME forced-tool `emit_findings` schema, the SAME system prompt-injection preamble, and the SAME config-gated `temperature` as the persona lenses (plan 03). The only differences:

- Prompt: a comparative graphic-design prompt anchored to `DESIGN-LENS-RUBRIC.md` — score the surface COMPARATIVELY against the attached exemplars and the named textual tier anchors (Linear/Vercel/Prisma/Tailscale/Oban; Stripe density-only under the anti-fintech caveat), NOT as an absolute award score. Focus on the sharpened dims 2 (hierarchy), 3 (spacing-rhythm), 5 (responsive), 8 (brand-expression) with 1 (token) and 6 (contrast) as support. Instruct d3 to penalize BOTH cramped AND wasteful. Require each finding to self-declare `direction: air|cramped` (whether the fix pushes toward more whitespace or tighter density).
- Exemplar attach: select exactly ONE archetype-matched good + ONE bad committed exemplar keyed off the surface's `surface_type` (RESEARCH Open Q3 — list/detail/component archetype), bounded at 2 exemplar images per call regardless of surface (D-20). Read the committed PNG bytes and append them as additional `image` content blocks after the target screenshot + prompt (same base64 image-block shape as `score-visuals.mjs:199-206`). Label each attached exemplar in an accompanying text block as the GOOD (in-brand, correctly-dense) or BAD (density-pole) reference so the model knows which is which. If `off-register` shipped textual-only (plan 02), fall back to the cramped/wasteful bad PNG for the bad slot.

The design lens output is parsed from the tool_use block exactly like persona findings.
  </action>
  <verify>
    <automated>node accrue_admin/e2e/ratchet/ratchet-propose.mjs --self-test && node --check accrue_admin/e2e/ratchet/ratchet-propose.mjs && node -e "const s=require('fs').readFileSync('accrue_admin/e2e/ratchet/ratchet-propose.mjs','utf8');for(const t of ['design','exemplar','direction']){if(!s.toLowerCase().includes(t))throw new Error('missing '+t)};console.log('design lens ok')"</automated>
  </verify>
  <acceptance_criteria>
    - A 7th `lens_kind:"design"` lens runs per image with the comparative (not absolute-award) prompt anchored to the rubric
    - Exactly 2 exemplar images (1 good + 1 bad), archetype-matched by `surface_type`, are attached per design-lens call
    - The prompt sharpens dims 2/3/5/8 (+1/6 support), requires the `direction:air|cramped` self-flag, and reuses the prompt-injection preamble + config-gated temperature
    - `--self-test` and `node --check` still pass
  </acceptance_criteria>
  <done>The comparative design lens runs with archetype-matched few-shot exemplars and produces direction-flagged design findings.</done>
</task>

<task type="auto">
  <name>Task 2: Design-candidate emission through the shared identity gate</name>
  <files>accrue_admin/e2e/ratchet/ratchet-propose.mjs</files>
  <read_first>
    - accrue_admin/e2e/ratchet/ratchet-propose.mjs (plan 03 Task 3 — the emit gate: assertDimension/normalizeRegion/normalizeOverlays/claimKey/findingId/justification-token/taste-denylist/N=12 cap)
    - accrue_admin/e2e/ratchet/region-tags.js (identity fns)
  </read_first>
  <action>
Route design-lens raw findings through the EXACT SAME harness validation + claim-key gate as persona findings (Task 3 of plan 03) — no separate identity path, so the design lens cannot introduce a flake vector. Every design candidate re-derives `dimension`/`region_tag`/`overlay_tags`/`claim_key`/`finding_id` in the harness, passes the `justification_token` admissibility gate + taste denylist, and counts toward the same N=12/image cap.

Design candidates additionally set the D-17 human-only free-text fields specific to the design lens: `raised_by: {lens_kind:"design"}` (no `persona_id`/`job`), `exemplar_ref` (which committed exemplar the surface fell short of), and `direction` (`air|cramped`). `exemplar_ref` and `direction` are NON-identity (excluded from `claim_key`) — a design finding and a persona finding that resolve to the same surface+dim+region+overlay collapse to the same `finding_id` by construction (this is intended; the DEDUP-03 persona/lens frequency collapse is Phase 206). `persona_frequency` stays `1`.
  </action>
  <verify>
    <automated>node accrue_admin/e2e/ratchet/ratchet-propose.mjs --self-test && node -e "const s=require('fs').readFileSync('accrue_admin/e2e/ratchet/ratchet-propose.mjs','utf8');if(!/lens_kind.{0,6}design/.test(s))throw new Error('no design lens_kind');if(!/exemplar_ref/.test(s))throw new Error('no exemplar_ref');console.log('design emit ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Design candidates flow through the same identity gate; `claim_key`/`finding_id` are harness-re-derived identically to persona findings
    - Design rows carry `raised_by.lens_kind="design"`, `exemplar_ref`, and `direction:air|cramped`, all NON-identity
    - No new claim-key derivation path is introduced; `--self-test` still passes
  </acceptance_criteria>
  <done>Design candidates emit through the shared deterministic identity gate with design-specific non-identity fields, preserving DEDUP-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exemplar + screenshot text → model instructions | Both the target screenshot and the attached exemplar images may contain text; all treated as untrusted data |
| design model output → identity/CI | Design-lens output is advisory; identity re-derived; never gates CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-01 | Tampering / Elevation | design lens call | high | mitigate | Same system preamble marks all in-image text (screenshot + exemplars) as untrusted data; design candidates re-derive identity via `region-tags.js`; `direction`/`exemplar_ref` are non-identity free text. |
| T-205-03 | Denial of Service | exemplar attach payload | medium | mitigate | Exactly 2 exemplar images per call, each pre-downscaled ≤~1600px and under the 5 MB guard (plan 02); no unbounded gallery attach. |
</threat_model>

<verification>
- `node accrue_admin/e2e/ratchet/ratchet-propose.mjs --self-test` → green (identity unchanged).
- `node --check` passes; the design lens attaches exactly 2 exemplar images and emits `lens_kind:"design"` rows with `direction`/`exemplar_ref`.
- Maintainer live-smoke (manual, non-gating): design-lens rows appear in `candidates.ndjson` with the `direction` flag and reference the correct archetype exemplar.
</verification>

<success_criteria>
- EVAL-02: the graphic-design lens scores comparatively against named quiet-dev-tooling exemplars (not an absolute award score), anchored to `brandbook/` via the committed rubric + few-shot exemplars.
</success_criteria>

<output>
Create `.planning/phases/205-persona-design-lens-evaluator-harness/205-04-SUMMARY.md` when done.
</output>

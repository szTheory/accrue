---
phase: 207-orchestration-digest-one-command-round-fix-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs
  - accrue_admin/e2e/ratchet/rounds.ndjson
autonomous: true
requirements: [ORCH-06]

must_haves:
  truths:
    - "node e2e/ratchet/phase-ratchet-ledger.mjs --next-round against an empty rounds.ndjson prints and marker-writes 1; against rows round:1,2,3 it prints/writes 4 — the committed append-only rounds.ndjson round-state schema (D-47)"
    - "node e2e/ratchet/phase-ratchet-ledger.mjs --seal-round with RATCHET_ROUND unset exits nonzero with a clear message and appends nothing to rounds.ndjson"
    - "a round meeting all 4 dry clauses (zero new opens this round, zero open findings, both regression files 0 bytes, coverage floor met) appends dry:true; any single clause false appends dry:false"
    - "two consecutive dry:true rows within the same baseline epoch classify status=converged; reaching round 6 without 2 consecutive dry classifies status=cap-reached; both are written to the round-status marker, never crash the process — the K=2/6-cap convergence rule (D-49)"
  artifacts:
    - accrue_admin/e2e/ratchet/rounds.ndjson
    - accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs (extended)
  key_links:
    - "--seal-round reuses computeRegressions()/fold()/GUARD_HOME_SPECS/isSafeSpecPath verbatim — never reimplements ledger folding or guard-token grammar"
    - "rounds.ndjson schema + the two marker files this plan establishes are the load-bearing contract 207-04 (digest) and 207-05 (ui.round mix task) read"
---

<objective>
Extend `phase-ratchet-ledger.mjs` — the existing deterministic sibling-gate reducer from Phase 206 — with the round-state machinery ORCH-06 requires: a `--next-round` CLI mode that hands the next round integer back to the (later) Elixir orchestrator via a plain marker file, and a `--seal-round` CLI mode that computes the D-48 4-clause dry-round conjunction, appends one row to a new committed `rounds.ndjson` event log, and classifies convergence (K=2 consecutive dry rounds) or the 6-round hard-cap escalation via a second marker file. Also export the three small helpers (`GUARD_HOME_SPECS`, `checkGuardRef`, `isSafeSpecPath`) this file already owns so a later plan (207-03's guard-mint) can reuse them instead of re-deriving the guard-token grammar a second time.

Purpose: this is the FOUNDATION every other 207 plan depends on — the round counter, the dry-round classification, and the rounds.ndjson schema are the single source of truth for "has the ratchet converged yet," and D-51 requires ALL of that reasoning live in this self-tested node reducer, never in Elixir.
Output: `rounds.ndjson` (new, committed, 0 bytes to start) + an extended `phase-ratchet-ledger.mjs` with `--next-round`/`--seal-round` CLI modes, both provable via `--self-test` fixtures with zero real-file mutation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-CONTEXT.md
@.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-RESEARCH.md
@accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs
@accrue_admin/e2e/ratchet/ratchet-ledger.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `--next-round` CLI mode + committed `rounds.ndjson` + `GUARD_HOME_SPECS`/`checkGuardRef`/`isSafeSpecPath` exports</name>
  <files>accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs, accrue_admin/e2e/ratchet/rounds.ndjson</files>
  <read_first>
    accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs (full file — note `DEFAULT_PATHS`, `readNdjsonRows`, `writeNdjson`, `runSelfTest`'s `writeFixtureFiles` helper and the `if (import.meta.url === ...) { try { main() } catch }` guard at the bottom)
  </read_first>
  <behavior>
    - `computeNextRound([])` returns `1`.
    - `computeNextRound([{round:1,...},{round:3,...},{round:2,...}])` returns `4` (max, order-independent).
    - CLI `--next-round`: writes the computed integer (no other formatting) to `test-results/ui-ratchet/.round-next` (relative to the accrue_admin package root — `path.join(REPO_ROOT, "accrue_admin/test-results/ui-ratchet/.round-next")`), creating the parent directory if absent; also echoes it to stdout; exits 0; makes zero writes to `findings.ledger.ndjson`, `ledger.baseline.json`, or `finding-regressions.ndjson`.
    - `--next-round` runs BEFORE the `--self-test` guard check is irrelevant here — it is a SEPARATE branch checked immediately after the existing `--self-test` branch in `main()`, so `--self-test` still short-circuits first and touches nothing on disk.
  </behavior>
  <action>
    Add a pure, exported helper `computeNextRound(roundsRows)` = `Math.max(0, ...roundsRows.map(r => r.round || 0)) + 1`. Add path constants `ROUNDS_PATH = path.join(__dirname, "rounds.ndjson")` (sibling to `reopen-markers.ndjson`, in `DEFAULT_PATHS`) and `NEXT_ROUND_MARKER_PATH = path.join(__dirname, "../../test-results/ui-ratchet/.round-next")` (NOT under `DEFAULT_PATHS` — this is an ephemeral scalar handoff file to Elixir, not a gate-relevant committed artifact; `test-results/` is already gitignored at repo root so this new subpath needs no new `.gitignore` entry).

    In `main()`, add a new branch checked immediately after the existing `--self-test` branch and before the plain (no-flag) `runReducer` call: `if (process.argv.includes("--next-round")) { const rows = readNdjsonRows(DEFAULT_PATHS.roundsPath); const next = computeNextRound(rows); fs.mkdirSync(path.dirname(NEXT_ROUND_MARKER_PATH), { recursive: true }); fs.writeFileSync(NEXT_ROUND_MARKER_PATH, String(next)); console.log(`[phase-ratchet-ledger] next-round=${next}`); return; }`. This branch does nothing else — no reducer run, no baseline touch — matching the existing guard-ordering discipline (self-test first, side-effect-free branches before anything else).

    Add `export { GUARD_HOME_SPECS, checkGuardRef, isSafeSpecPath };` at the end of the file (this file currently has NO exports at all — it is CLI-only). This lets 207-03's new `ratchet-guard-mint.mjs` import the SAME guard-token grammar instead of re-deriving it (Don't Hand-Roll — a second, possibly-diverging regex is exactly the failure mode Phase 206 already flagged).

    Create `accrue_admin/e2e/ratchet/rounds.ndjson` as a new, empty (0 bytes) committed file — sibling to `findings.ledger.ndjson`/`reopen-markers.ndjson`, following the SAME "committed, append-only, absent-or-empty reads as `[]`" convention `readNdjsonRows` already implements (no code change needed for this — `readNdjsonRows` already treats an absent/empty file as `[]`).
  </action>
  <verify>
    <automated>cd accrue_admin && node e2e/ratchet/phase-ratchet-ledger.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `computeNextRound` is exported and pure (no fs access) — self-test fixtures call it directly with in-memory arrays.
    - `node e2e/ratchet/phase-ratchet-ledger.mjs --next-round` (run against the REAL committed `rounds.ndjson`, currently empty) writes `1` to `test-results/ui-ratchet/.round-next` and prints it; the real `findings.ledger.ndjson`/`ledger.baseline.json`/`finding-regressions.ndjson` are untouched (verify via `git status` showing no diff to those 3 files).
    - `git status` shows `accrue_admin/e2e/ratchet/rounds.ndjson` as a new, empty, tracked file.
    - `export { GUARD_HOME_SPECS, checkGuardRef, isSafeSpecPath }` present; a `node -e "import('./accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs').then(m => console.log(typeof m.checkGuardRef))"` prints `function`.
  </acceptance_criteria>
  <done>`--next-round` computes and marker-writes the correct next integer from fixtures and from the real (empty) rounds.ndjson, touches nothing else, and the three guard-grammar helpers are importable by a later plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `--seal-round` — 4-clause dry conjunction + coverage floor + K=2/6-cap convergence classification</name>
  <files>accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs</files>
  <read_first>
    accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs (the `computeRegressions`/`runReducer`/`runSelfTest`/`writeFixtureFiles` functions written in Task 1's context — reuse the SAME `fs.mkdtempSync` fixture-scratch discipline)
    accrue_admin/e2e/ratchet/ratchet-propose.mjs lines 86-94 (`bundleSha256()` — the sha256-of-CSS-bundle pattern to mirror, tolerant of an absent file)
  </read_first>
  <behavior>
    - Clause 1 (zero new opens this round): a folded finding whose LATEST row has `event === "confirm"` AND `round === currentRound` counts against this clause; zero such findings → clause passes.
    - Clause 2 (zero open remain): any folded finding with `status === "open"` (any round) fails this clause.
    - Clause 3 (both regression files empty): `finding-regressions.ndjson` (just written by this same `--seal-round` invocation's `runReducer` call) AND the standing scorecard regression file at `.planning/milestones/v1.54-phases/200-idempotent-verification-sign-off/regressions.ndjson` (repo-root-relative; ABSENT file treated as empty/pass, matching the existing `readNdjsonRows` absence-safe convention) must both be 0 bytes / absent.
    - Clause 4 (coverage floor): read `.planning/milestones/v1.54-phases/200-idempotent-verification-sign-off/final.cells.json` (repo-root-relative; absent/malformed → `[]`, vacuously passes); filter to cells whose `surface` is in the resolved scope surface set (scope = `RATCHET_SURFACES` env CSV, or — when unset/`"all"` — the full `SURFACES` list imported from `../baseline-manifest.js`); pass iff every filtered cell's `coverage_status === "covered"` (an empty filtered set vacuously passes).
    - `dry = clause1 && clause2 && clause3 && clause4`.
    - A `rounds.ndjson` fixture with rows `[{round:1,dry:true,epoch:1,...},{round:2,dry:true,epoch:1,...}]` (current epoch 1) classifies `converged` after appending a 2nd consecutive dry round.
    - A dry streak that crosses an epoch boundary (an older epoch's dry rows) must NOT count toward the CURRENT epoch's consecutive-dry total.
    - Round number 6 with fewer than 2 consecutive dry rows classifies `cap-reached`; anything else classifies `continue`.
    - `RATCHET_ROUND` env missing or non-numeric → the CLI exits with a non-zero status and a clear stderr message, appends NOTHING to `rounds.ndjson`.
  </behavior>
  <action>
    Add pure, testable helper functions (parameterized, no hardcoded real paths inside them — mirror the existing `computeRegressions(paths)` vs `runReducer(paths)` split already in this file):
    - `computeClauseNewOpens(foldedFindings, currentRound)` → boolean.
    - `computeClauseZeroOpen(foldedFindings)` → boolean.
    - `computeClauseRegressionsEmpty(findingRegressionsPath, standingRegressionsPath)` → boolean (treat absent-or-0-byte as pass for both).
    - `computeClauseCoverageFloor(cellsCensusRows, scopeSurfaces)` → boolean (`scopeSurfaces` is `null`/`"all"` sentinel meaning no filter, or a `Set<string>`).
    - `computeDryRound(clauses)` → `clauses.every(Boolean)` (accepts the 4 booleans as an array so the self-test can flip one at a time).
    - `computeConsecutiveDry(roundsRows, currentEpoch)` → trailing run-length of `dry === true` among rows where `epoch === currentEpoch`, in file order (stop counting at the first `dry === false` or at the epoch boundary).
    - `classifyRoundStatus(consecutiveDry, currentRoundNumber)` → `"converged"` if `consecutiveDry >= 2`, else `"cap-reached"` if `currentRoundNumber >= 6`, else `"continue"`.

    Add path constants: `STANDING_REGRESSIONS_PATH = path.join(REPO_ROOT, ".planning/milestones/v1.54-phases/200-idempotent-verification-sign-off/regressions.ndjson")`, `CELLS_CENSUS_PATH = path.join(REPO_ROOT, ".planning/milestones/v1.54-phases/200-idempotent-verification-sign-off/final.cells.json")`, `ROUND_STATUS_MARKER_PATH = path.join(REPO_ROOT, "accrue_admin/test-results/ui-ratchet/.round-status")`. Import `SURFACES` from `../baseline-manifest.js` at the top of the file (a plain static data import — zero conditional/key-gating needed, unlike the Anthropic SDK import elsewhere in this codebase).

    Add a `sealRound(paths)` orchestrating function invoked from the new `--seal-round` CLI branch in `main()`: reads `process.env.RATCHET_ROUND` (Mix.raise-equivalent: if missing or `Number.isNaN`, print a clear stderr message and `process.exitCode = 1; return;` — append nothing) and `process.env.RATCHET_SURFACES` (default sentinel `"all"`); calls the EXISTING `runReducer(DEFAULT_PATHS)` (unchanged — this already writes `finding-regressions.ndjson` and regenerates the unfrozen baseline, exactly as the plain no-flag invocation does today); folds the ledger via the SAME `fold()` already imported; computes the 4 clauses + `dry`; computes `epoch` from the JUST-regenerated baseline's `.epoch`; computes `bundle_sha256` via a local `sha256`-of-`priv/static/accrue_admin.css` helper (mirror `ratchet-propose.mjs`'s `bundleSha256()`, tolerant of an absent bundle → `null`); reads existing `rounds.ndjson` rows, appends `{schema_version: "ratchet-round-seal/1", round: Number(RATCHET_ROUND), dry, epoch, scope: process.env.RATCHET_SURFACES || "all", bundle_sha256, seq: existingRoundsRows.length + 1}`; recomputes `consecutiveDry` over the JUST-updated rows scoped to the current epoch; classifies status via `classifyRoundStatus`; writes the status string to `ROUND_STATUS_MARKER_PATH` (mkdir -p parent); prints a one-line human-readable summary (`round=N dry=<bool> status=<status>`); ALWAYS exits 0 (the non-zero-exit escalation contract belongs to the Elixir `ui.round` task in 207-05, which reads this status marker AFTER the digest has rendered — `--seal-round` itself must never abort mid-pipeline).

    Extend `runSelfTest()` with new fixtures (own `fs.mkdtempSync` subdirectories, never touching real committed paths) covering: (1) all 4 clauses true → `dry:true`; (2) each clause individually false (4 separate fixtures) → `dry:false` each time, naming which clause failed in the assertion message; (3) a `rounds.ndjson` fixture proving 2 consecutive dry rows in the SAME epoch → `converged`; (4) a fixture where an OLDER epoch's dry row must not count toward the current epoch's streak; (5) round 6 with `consecutiveDry < 2` → `cap-reached`; (6) missing `RATCHET_ROUND` → no rows appended, non-zero `process.exitCode`.
  </action>
  <verify>
    <automated>cd accrue_admin && node e2e/ratchet/phase-ratchet-ledger.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `RATCHET_ROUND=1 node e2e/ratchet/phase-ratchet-ledger.mjs --seal-round` run once against the real (currently empty) `findings.ledger.ndjson`/`rounds.ndjson` appends exactly one row to `rounds.ndjson` and writes `continue` (or `converged`, if the real ledger is already empty enough to pass all 4 clauses — either is acceptable; the self-test is the binding proof) to `.round-status`; re-run `git checkout -- accrue_admin/e2e/ratchet/rounds.ndjson accrue_admin/e2e/ratchet/ledger.baseline.json accrue_admin/e2e/ratchet/finding-regressions.ndjson` afterward to leave the repo clean for later plans.
    - `RATCHET_ROUND=unset` (env var absent) exits non-zero and appends no row.
    - Every one of the 6 new self-test fixtures passes independently; `--self-test`'s final line is `phase-ratchet-ledger self-test passed.` including the new assertions.
  </acceptance_criteria>
  <done>`--seal-round` computes the exact D-48 4-clause conjunction and the D-49 K=2/6-cap classification purely from committed files, never crashes, and reuses (never reimplements) `runReducer`/`fold`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger file → reducer | `findings.ledger.ndjson`/`rounds.ndjson`/`ledger.baseline.json` are committed files that a future maintainer or a bad merge could hand-edit; the reducer must not silently trust malformed content |
| Env → CLI | `RATCHET_ROUND`/`RATCHET_SURFACES` are process env vars set by the (later) Elixir orchestrator — untrusted input by convention even though same-repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-03 | Tampering | `rounds.ndjson` / `--seal-round` | medium | mitigate | Reuse the existing seq-monotonic `fold()` tamper-evidence check verbatim (never reimplemented); a corrupted/reordered `rounds.ndjson` row set is detected the same way a corrupted `findings.ledger.ndjson` already is |
| T-207-07 | Denial of Service | `RATCHET_ROUND` malformed/missing | low | mitigate | `--seal-round` validates `RATCHET_ROUND` is a finite number before any write; on failure it exits non-zero and appends nothing rather than writing a garbage row |
| T-207-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan (zero new dependencies) — Package Legitimacy Gate not triggered |

</threat_model>

<verification>
`cd accrue_admin && node e2e/ratchet/phase-ratchet-ledger.mjs --self-test` is green, including all new fixtures. A single real (non-self-test) `--next-round` + `--seal-round` round-trip against the actual committed (currently empty) ledger produces one `rounds.ndjson` row and a `.round-status` marker without crashing, then the repo is restored clean via `git checkout --` on the 3 mutated committed files.
</verification>

<success_criteria>
`phase-ratchet-ledger.mjs` exposes `--next-round` and `--seal-round`, both self-tested, both side-effect-scoped exactly as documented in D-47/D-48/D-49/D-51, and exports the guard-token grammar helpers for 207-03 to reuse.
</success_criteria>

<output>
Create `.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-01-SUMMARY.md` when done
</output>

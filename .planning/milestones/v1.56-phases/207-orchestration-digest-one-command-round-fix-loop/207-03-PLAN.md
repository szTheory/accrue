---
phase: 207-orchestration-digest-one-command-round-fix-loop
plan: 03
type: execute
wave: 2
depends_on: ["207-01"]
files_modified:
  - accrue_admin/e2e/foundation-tokens.spec.js
  - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
  - accrue_admin/e2e/reduced-motion.spec.js
  - accrue_admin/e2e/admin-page-flow-phase200.spec.js
  - accrue_admin/e2e/ratchet/ratchet-guard-mint.mjs
autonomous: true
requirements: [ORCH-05]

must_haves:
  truths:
    - "each of the 4 GUARD_HOME_SPECS files contains an empty, idempotent @ratchet:auto-guards marker region plus one loop test that iterates its (currently empty) data array and no-ops cleanly"
    - "mintGuardRow() routes a finding to the correct home file (or the ledger-count sentinel) purely from its dimension/defect_bucket, with zero LLM call and zero live browser"
    - "appendMintedRow() is idempotent — appending the same finding_id twice leaves the marker region byte-identical to a single append; rows stay sorted by finding_id"
    - "the minted guard_ref token passes phase-ratchet-ledger.mjs's own imported checkGuardRef() unchanged — no second, possibly-diverging token grammar"
  artifacts:
    - accrue_admin/e2e/ratchet/ratchet-guard-mint.mjs
    - the 4 guard-home spec files, each with a bootstrapped @ratchet:auto-guards region + loop test
  key_links:
    - "ratchet-guard-mint.mjs imports GUARD_HOME_SPECS/checkGuardRef/isSafeSpecPath from phase-ratchet-ledger.mjs (207-01) — never re-derives the token grammar"
    - "207-06's ui.fix orchestration invokes mintGuardRow()+appendMintedRow() for every finding it resolves — this plan is the reusable module, not the caller"
---

<objective>
Bootstrap the "guard home" for auto-minted regression guards (D-44/D-45/D-46): add an empty, delimited `@ratchet:auto-guards` marker region plus ONE human-authored, kind-dispatching loop test to each of the 4 existing `GUARD_HOME_SPECS` files (none of them have any `@ratchet:` marker today — the ledger is empty and no finding has ever been resolved). Then implement the reusable mint module, `ratchet-guard-mint.mjs`, that routes a resolved finding to the correct home + row shape by defect kind, appends idempotently, and reuses (never re-derives) 207-01's exported guard-token grammar.

Purpose: this is the "assert the invariant, not the pixel" half of ORCH-05 — the mint emits typed DATA rows into a human-reviewed-once loop, never per-finding generated assertion code (D-44), and the kind-routing table below is the concrete, deliberate resolution of D-45's dimension-to-guard-kind mapping.
Output: 4 bootstrapped spec files (all still green, since their new loop test is a no-op on an empty array) + a new, independently self-tested `ratchet-guard-mint.mjs` module that 207-06 will call.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-CONTEXT.md
@.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-RESEARCH.md
@accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs
@accrue_admin/e2e/foundation-tokens.spec.js
@accrue_admin/e2e/ratchet/ratchet-propose.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the `@ratchet:auto-guards` marker + loop test in the 4 guard-home specs</name>
  <files>accrue_admin/e2e/foundation-tokens.spec.js, accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js, accrue_admin/e2e/reduced-motion.spec.js, accrue_admin/e2e/admin-page-flow-phase200.spec.js</files>
  <read_first>
    accrue_admin/e2e/foundation-tokens.spec.js (full file — reuse its EXISTING `login`, `styleOf`, `rootToken`, `expectContrastAtLeast`, `contrastRatio`, `parseColor` helpers verbatim; do not duplicate them)
    accrue_admin/e2e/reduced-motion.spec.js lines 1-50 (existing `login`, `buttonTransitionDurations`-style helpers and their `transitionDuration.split(",")` parsing convention)
    accrue_admin/e2e/admin-page-flow-phase200.spec.js lines 1-50 (its imports from `./phase191-page-flow-helpers.js` — reuse `resolvePhase191Route`/`setPhase191Theme`-style helpers rather than hand-rolling navigation)
    accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js lines 1-40 (its imports from `./phase191-page-flow-helpers.js`)
  </read_first>
  <action>
    Append, at the END of each of the 4 files (after the existing closing content — Playwright allows top-level `test(...)` calls alongside any existing `test.describe` blocks in the same file, so this never disturbs existing tests), the SAME two-part bootstrap, tailored per file's already-imported helpers:

    1. A delimited, empty marker region holding the typed data array, using literal comment delimiters `// >>> @ratchet:auto-guards >>>` and `// <<< @ratchet:auto-guards <<<` on their own lines immediately surrounding a `const RATCHET_AUTO_GUARDS = [];` declaration. This EXACT delimiter pair is what 207-03 Task 2's `appendMintedRow()` and the phase-ratchet-ledger.mjs `checkGuardRef()` grep for — do not vary the spacing/casing.

    2. ONE new Playwright test (bare top-level `test(...)`, not wrapped in a new `test.describe`) that iterates `RATCHET_AUTO_GUARDS` and, per row, dispatches on `row.kind` to the reused helper functions already defined in that same file. Per-file kind routing and reused helpers:
       - `foundation-tokens.spec.js`: kinds `"design-token"` (`{finding_id, kind, selector, property, expected_token}` — locate via `page.locator(row.selector).first()`, compare `await styleOf(locator, row.property)` to `await rootToken(page, row.expected_token)`), `"contrast"` (`{finding_id, kind, selector, min_ratio}` — `expectContrastAtLeast(await styleOf(locator,"color"), await styleOf(locator,"backgroundColor"), row.min_ratio, ...)`), `"spacing-scale"` (`{finding_id, kind, selector, property, allowed_values}` — assert `allowed_values` (array) contains the computed value). Navigate once to `/billing/dev/components` (mirroring the existing test in this file) before the loop.
       - `admin-page-flow-phase200.spec.js`: kind `"microcopy"` (`{finding_id, kind, route, expected_text, old_text}` — navigate to `row.route`, read `page.locator("#main-content").innerText()`, assert it contains `expected_text` and (if `old_text` present) does not contain `old_text`).
       - `admin-interaction-overlay-phase199.spec.js`: kind `"focus-ring"` (`{finding_id, kind, selector}` — `.focus()` the locator, assert `getComputedStyle(el).outlineStyle !== "none"`).
       - `reduced-motion.spec.js`: kind `"motion"` (`{finding_id, kind, route, selector, max_ms}` — navigate, read the computed `transitionDuration` comma-list the same way this file's existing helpers already do, assert every parsed segment ≤ `max_ms`).
       Every assertion message MUST include the literal string `` `@ratchet:${row.finding_id}` `` so a failure names the finding, and the test body must gracefully no-op (via `test.skip(true, "no minted ratchet guards yet")` or an early `return`) when `RATCHET_AUTO_GUARDS.length === 0` — the array starts empty in this plan.
  </action>
  <verify>
    <automated>cd accrue_admin && env -u NO_COLOR npx playwright test e2e/foundation-tokens.spec.js e2e/reduced-motion.spec.js --timeout=60000 --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@ratchet:auto-guards" accrue_admin/e2e/foundation-tokens.spec.js` reports 2 (open+close markers) for each of the 4 files.
    - The 4 files still parse/compile as valid Playwright specs and their PRE-EXISTING tests are unaffected (no edits inside any existing `test`/`test.describe` body).
    - The new loop test in each file passes (no-ops) with an empty `RATCHET_AUTO_GUARDS` array.
  </acceptance_criteria>
  <done>All 4 guard-home files carry a bootstrapped, idempotent-append-ready marker region and a working, currently-empty loop test that reuses each file's own existing helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `ratchet-guard-mint.mjs` — kind routing, idempotent append, ledger-count sentinel</name>
  <files>accrue_admin/e2e/ratchet/ratchet-guard-mint.mjs</files>
  <read_first>
    accrue_admin/e2e/ratchet/phase-ratchet-ledger.mjs (the newly-exported `GUARD_HOME_SPECS`/`checkGuardRef`/`isSafeSpecPath` from 207-01 — import these, do not redefine)
    accrue_admin/e2e/ratchet/ratchet-propose.mjs lines 495-508 (`DEFECT_BUCKETS_BY_DIM` — the 12-dimension × defect_bucket enum this plan's kind-routing table is derived from)
  </read_first>
  <behavior>
    - `kindForFinding({dimension, defect_bucket, effort_class})` routes per this closed table (D-45): dim 1 → `"design-token"`; dim 2 → `"ledger-count"` (hierarchy/visual-weight); dim 3 → `"spacing-scale"` when `defect_bucket === "inconsistent-rhythm"`, else `"ledger-count"` (density-balance, for `"cramped"`/`"wasteful"`); dim 4 → `"ledger-count"`; dim 5 → `"ledger-count"` (responsive-composition); dim 6 → `"contrast"`; dim 7 → `"focus-ring"`; dim 8 → `"ledger-count"` (brand-tier gestalt); dim 9 → `"motion"`; dim 10 → `"ledger-count"`; dim 11 → `"ledger-count"`; dim 12 → `"microcopy"`. Regardless of dimension, `effort_class === "ia-product-decision"` always forces `"ledger-count"` (D-45's explicit override).
    - `homeSpecForKind(kind)` maps `"design-token"|"contrast"|"spacing-scale"` → `foundation-tokens.spec.js`; `"microcopy"` → `admin-page-flow-phase200.spec.js`; `"focus-ring"` → `admin-interaction-overlay-phase199.spec.js`; `"motion"` → `reduced-motion.spec.js`; `"ledger-count"` → `null` (no home file — the sentinel).
    - `mintGuardRow(finding, probedFields)` returns `{ guard_ref, targetSpecPath, row }` where `guard_ref` is EXACTLY the string `checkGuardRef` (imported from 207-01) already validates: `"ledger-count"` for the sentinel kind, or `` `${targetSpecPath}::@ratchet:${finding.finding_id}` `` otherwise — round-trip this through the imported `checkGuardRef(guard_ref, finding.finding_id, repoRoot)` inside the self-test and assert `{ok: true}`.
    - `appendMintedRow(targetSpecPath, row, repoRoot)` is idempotent: calling it twice with the same `row.finding_id` against the same fixture-copy file leaves the file byte-identical to a single call; rows inside the marker region stay sorted ascending by `finding_id`.
  </behavior>
  <action>
    Create `accrue_admin/e2e/ratchet/ratchet-guard-mint.mjs` as a new ESM module (mirrors this dir's existing `.mjs` file conventions: `import fs/path/fileURLToPath`, no Anthropic SDK, no network, zero LLM). Import `{ GUARD_HOME_SPECS, checkGuardRef, isSafeSpecPath }` from `./phase-ratchet-ledger.mjs`.

    Implement `kindForFinding`/`homeSpecForKind` exactly per the routing table in `<behavior>` above (export both — pure functions, no fs access). Implement `mintGuardRow(finding, probedFields)`: derive `kind = kindForFinding(finding)`; if `kind === "ledger-count"`, return `{ guard_ref: "ledger-count", targetSpecPath: null, row: null }` immediately (D-40 sentinel, no file touch); else derive `targetSpecPath = homeSpecForKind(kind)` (must be a member of the imported `GUARD_HOME_SPECS`, sanity-assert this with `isSafeSpecPath` at mint time), build `guard_ref = `${targetSpecPath}::@ratchet:${finding.finding_id}``, and build the per-kind row object from `finding` + the caller-supplied `probedFields` (the freshly re-captured/re-probed values per D-44 — the caller in 207-06 supplies these; this module never probes a live page itself, keeping it pure and self-test-friendly) matching the exact per-kind shape Task 1 bootstrapped into each home file (e.g. for `"contrast"`: `{finding_id, kind, selector: probedFields.selector, min_ratio: probedFields.min_ratio}`).

    Implement `appendMintedRow(targetSpecPath, row, repoRoot)`: resolve the absolute path via `path.join(repoRoot, targetSpecPath)`; read the file text; locate the `@ratchet:auto-guards` marker region via the exact delimiter strings Task 1 used; if the file ALREADY contains the literal substring `` `@ratchet:${row.finding_id}` `` anywhere inside that region, return unchanged (idempotent no-op — grep-before-append, D-46); otherwise parse the existing array literal's rows (or start from `[]`), push the new row, re-serialize sorted ascending by `finding_id` (stable `JSON.stringify`-per-row formatting is fine — this is a data region, not hand-styled prose), and rewrite ONLY the text between the two delimiter comments, leaving everything else in the file byte-identical.

    Add a `--self-test` CLI branch (this file's own entry point, guarded by `import.meta.url === ...` at the bottom, matching every sibling `.mjs` in this directory) that: (a) asserts `kindForFinding`/`homeSpecForKind` against the full 12-dimension table plus the `effort_class` override; (b) for each of the 4 real-synth kinds, calls `mintGuardRow` with a fixture finding and asserts the returned `guard_ref` passes the imported `checkGuardRef` against a `fs.mkdtempSync` COPY of the real target spec file with the row already appended (never touching the real committed spec files); (c) proves `appendMintedRow` called twice with the same `finding_id` produces a byte-identical file both times; (d) proves rows stay sorted by `finding_id` after 3 out-of-order appends.
  </action>
  <verify>
    <automated>cd accrue_admin && node e2e/ratchet/ratchet-guard-mint.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `--self-test` passes covering all 12 dimensions' routing, the `effort_class` override, idempotent double-append, sorted-row invariant, and a real round-trip through the imported `checkGuardRef`.
    - No test in this task ever writes to the REAL (non-fixture-copy) guard-home spec files — every fixture uses `fs.mkdtempSync` + a copy of the real file's current (post-Task-1) contents.
    - `node -e "import('./accrue_admin/e2e/ratchet/ratchet-guard-mint.mjs').then(m => console.log(typeof m.mintGuardRow, typeof m.appendMintedRow))"` prints `function function`.
  </acceptance_criteria>
  <done>The mint module deterministically routes every dimension to its correct home or the ledger-count sentinel, appends idempotently and sorted, and its minted `guard_ref` passes the SAME `checkGuardRef` the deterministic gate uses — zero duplicated token grammar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mint → guard-home spec file | The mint writes into a real, committed, CI-executed Playwright spec file — a wrong target or malformed row could corrupt a test file that other CI jobs depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-02 | Tampering | `ratchet-guard-mint.mjs` write target | high | mitigate | `targetSpecPath` is ALWAYS derived from the closed `homeSpecForKind` table and re-validated via the imported `isSafeSpecPath`/`GUARD_HOME_SPECS` allowlist before any write — never accepted as a free-form path from ledger data; the write itself only ever replaces the exact delimited marker-region text, never the rest of the file |
| T-207-09 | Repudiation | Idempotent append correctness | low | mitigate | Grep-before-append on the exact `@ratchet:<finding_id>` token, proven by a dedicated self-test fixture — a re-run can never silently duplicate a row |
| T-207-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan |

</threat_model>

<verification>
`node e2e/ratchet/ratchet-guard-mint.mjs --self-test` is green; `env -u NO_COLOR npx playwright test e2e/foundation-tokens.spec.js e2e/reduced-motion.spec.js --timeout=60000 --workers=1` still passes after the bootstrap (existing tests untouched, new loop test no-ops on empty data).
</verification>

<success_criteria>
Every one of the 4 guard-home files can now receive an idempotently-minted, correctly-routed guard row, and the routing/append logic is fully self-tested without ever mutating a real committed spec file during the test run.
</success_criteria>

<output>
Create `.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-03-SUMMARY.md` when done
</output>

---
phase: 211-grep-gated-css-retirement-cross-surface-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["211-01"]
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/accrue_admin.css
  - accrue_admin/priv/static/accrue_admin.js
autonomous: true
requirements: [REIGN-04]
must_haves:
  truths:
    - "Zero remaining references to any of the 92 RESEARCH.md-named DELETE classes plus the 5 D-01 adjacent selectors exist anywhere in accrue_admin/assets/css/app.css after this plan"
    - "All 16 RESEARCH.md PRESERVE classes (including the ax-subscription-setup-gap landmine) remain byte-for-byte untouched in app.css"
    - "Every comma-grouped selector that mixed a dead class with a live class has ONLY its dead branch removed - the rule and its live branches survive intact"
    - "priv/static/accrue_admin.css and accrue_admin.js are rebuilt from the fully-deleted app.css via mix accrue_admin.assets.build and committed alongside the source edit"
  artifacts:
    - "accrue_admin/assets/css/app.css (92 named + 5 adjacent selectors removed, 16 PRESERVE selectors intact)"
    - "accrue_admin/priv/static/accrue_admin.css (rebuilt, shrunk)"
    - "accrue_admin/priv/static/accrue_admin.js (rebuilt, unchanged content - JS source untouched)"
  key_links:
    - "mix accrue_admin.assets.build reads assets/css/app.css and assets/js/app.js and writes exactly priv/static/accrue_admin.css + accrue_admin.js - must run AFTER both deletion tasks, not between them"
---

<objective>
Delete the 92 RESEARCH.md-named dead `.ax-*` selectors, plus 5 adjacent zero-reference rules caused by the same Phase 210 PageHeader migration (D-01, explicitly folded into this phase's scope), from `accrue_admin/assets/css/app.css`, preserving the 16 live classes and every live branch of shared comma-grouped selectors, then rebuild the committed `priv/static/accrue_admin.css`/`.js` bundle from the final deleted state.

Purpose: These are the actual dead rule instances the ROADMAP names for retirement (REIGN-04 SC1) - the bulk of this phase's work. Splitting into two family groups (Home/Launcher/Attention/HealthSummary, then Subscriptions/SubscriptionRow) keeps each task's grep-and-edit cycle manageable; deferring the bundle rebuild to the end of the second (final) app.css edit matches the established Phase 209/210 "rebuild only after the last CSS edit, commit together" convention.
Output: A fully-retired `app.css` and a freshly rebuilt, committed `accrue_admin.css`/`.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/211-grep-gated-css-retirement-cross-surface-cleanup/211-RESEARCH.md
@accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Home/Launcher/Attention/HealthSummary dead families plus the 5 D-01 adjacent rules</name>
  <read_first>
    - 211-RESEARCH.md "Class Census" full DELETE tables for ax-home-*, ax-launcher*, ax-attention*, ax-health-summary*, and the "Adjacent findings" table (5 rules)
    - 211-RESEARCH.md "Common Pitfalls" Pitfall 1 (exact-token false positives), Pitfall 2 (comma-grouped selectors), Pitfall 3 (per-selector not per-family liveness)
    - accrue_admin/assets/css/app.css - the .ax-home block (currently around lines 5570-5840) and the .ax-launcher*/.ax-attention* blocks (currently around lines 5834-6045 and 6083-6274) - read these ranges directly before editing
  </read_first>
  <files>accrue_admin/assets/css/app.css</files>
  <action>
    Delete every rule and selector branch for these 12 ax-home-* classes (the rest of that family is PRESERVE - do not touch ax-home, ax-home-section, ax-home-launcher-card, ax-home-launcher-icon, ax-home-launcher-action): ax-home-customer-search-cta, ax-home-customer-search-strip, ax-home-customer-search-strip-action, ax-home-header-health, ax-home-health-answer, ax-home-health-label, ax-home-health-metric, ax-home-health-metrics, ax-home-health-status, ax-home-primary-action, ax-home-search, ax-home-secondary-action. This includes their descendant/compound rules (the ax-home-header-health descendant rules for .ax-status-badge/strong/span, the ax-home-health-metric strong/em comma rule, and the ax-home-secondary-action comma-grouped with ax-home-customer-search-cta - both branches of that rule are dead, delete the whole rule).
    Also delete these 5 D-01 adjacent zero-reference rules in the same .ax-home block region (explicitly in scope per the locked D-01 decision, not a silent scope expansion): the nested rule ".ax-home .ax-page-header-compact", the nested rule ".ax-home .ax-page-actions", the nested rule ".ax-home .ax-page-actions .ax-button-sm", the standalone rule ".ax-dashboard-title-row", and the rule ".ax-dashboard-title-row .ax-display". Do not delete the bare (non-.ax-home-nested) ax-page-header-compact or ax-page-actions rules elsewhere in the file - those are still live (used by component_kitchen_live.ex, charge_live.ex, etc.); only the .ax-home-scoped nested variants are dead.
    Delete all 13 ax-launcher* classes - the entire old pre-Phase-210 launcher-tile family; do not confuse with the PRESERVE ax-launchers/ax-launchers-tri plural grid-wrapper classes, which stay: ax-launcher, ax-launcher-action, ax-launcher-action-button, ax-launcher-copy, ax-launcher-customer, ax-launcher-health, ax-launcher-icon, ax-launcher-meta, ax-launcher-meta-actions, ax-launcher-meta-warn, ax-launcher-primary, ax-launcher-recovery, ax-launcher-title. The bare ax-launcher token also appears in the shared global interaction-state comma-groups far from its main block - search the WHOLE file (not just the launcher block) for the compound selectors ax-launcher:focus-visible and ax-launcher:hover inside the large shared :focus-visible/:hover selector lists (confirmed present alongside ax-attention-row:focus-visible/:hover, which is LIVE - remove only the ax-launcher branch from each of those two comma groups, keep every other branch in each rule intact).
    Delete all 15 ax-attention* dead classes (do not touch the PRESERVE ax-attention, ax-attention-row, ax-attention-text, ax-attention-rail--empty): ax-attention-action, ax-attention-dot, ax-attention-dot-danger, ax-attention-dot-info, ax-attention-dot-warning, ax-attention-pill, ax-attention-pill-danger, ax-attention-pill-info, ax-attention-pill-warning, ax-attention-priority, ax-attention-priority-danger, ax-attention-priority-info, ax-attention-priority-warning, ax-attention-summary, ax-attention-summary-warning. Two of these (ax-attention-pill-danger, ax-attention-pill-info) also appear as branches in the shared global status-badge-color comma-groups far from the main attention block (alongside the still-live ax-status-badge-danger/ax-badge-danger/ax-flash-error/ax-banner-danger group, and the ax-status-badge-cobalt/ax-flash-info group) - search the WHOLE file for these two tokens and remove only their branch from each group, keeping the rest of each rule (the still-live status-badge classes) intact. Also delete the compound rule that reads ".ax-attention .ax-attention-pill-danger, .ax-attention .ax-attention-pill-warning" entirely - even though its ancestor ax-attention is live, both descendant selectors reference dead classes.
    Delete all 4 ax-health-summary* classes (the entire family - a pre-Phase-210 Home health-summary component fully retired from markup already): ax-health-summary, ax-health-summary-amber, ax-health-summary-moss, ax-health-summary-prominent.
    For every class above, use the exact-token pattern (a negative lookbehind for word-char-or-hyphen, the literal class name, a negative lookahead for word-char-or-hyphen) via ripgrep's -oP mode against the WHOLE assets/css/app.css file before and after each deletion - do not assume a class occurs only once or only within its named "family block"; re-grep after each edit since line numbers shift as you delete.
  </action>
  <verify>
    <automated>cd accrue_admin && grep -ocE "(^|[^A-Za-z0-9_-])(ax-home-customer-search-cta|ax-launcher|ax-attention-pill-danger|ax-health-summary|ax-dashboard-title-row)([^A-Za-z0-9_-]|$)" assets/css/app.css; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Every class named in the DELETE list above returns exit code 1 / a count of 0 from the exact-token grep above in assets/css/app.css (the sample pattern in the verify command proves this for its sample; run the equivalent check per-class for the full list before closing the task).
    - Every PRESERVE class (ax-home, ax-home-section, ax-home-launcher-card, ax-home-launcher-icon, ax-home-launcher-action, ax-launchers, ax-launchers-tri, ax-attention, ax-attention-row, ax-attention-text, ax-attention-rail--empty) still returns at least 1 exact-token match (untouched).
    - The two stray global comma-groups (ax-launcher:focus-visible/:hover, ax-attention-pill-danger/-info status-badge groups) each still contain their remaining live branches (spot-check by reading the rule after edit).
  </acceptance_criteria>
  <done>All Home/Launcher/Attention/HealthSummary dead selectors plus the 5 D-01 adjacent rules are removed; all PRESERVE classes and live comma-branches are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the Subscriptions/SubscriptionRow dead families, preserve ax-subscription-setup-gap, and rebuild the bundle</name>
  <read_first>
    - 211-RESEARCH.md "Class Census" full DELETE tables for ax-subscriptions-* (31) and ax-subscription-row-* (17), and the ax-subscription-setup-gap PRESERVE landmine entry
    - 211-RESEARCH.md "Common Pitfalls" Pitfall 2 (comma-grouped selectors) and Pitfall 3 (per-selector not per-family liveness) - re-read, this task has the sharpest landmine of the phase
    - 211-RESEARCH.md "Bundle Rebuild" section (exact mix task command and what gets committed)
    - accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex (full read - confirms the exact command and its two fixed output files)
  </read_first>
  <files>accrue_admin/assets/css/app.css, accrue_admin/priv/static/accrue_admin.css, accrue_admin/priv/static/accrue_admin.js</files>
  <action>
    Delete all 31 ax-subscriptions-* classes: ax-subscriptions-at-risk-strip, ax-subscriptions-audit-strip, ax-subscriptions-customer-search-action, ax-subscriptions-customer-search-strip, ax-subscriptions-exposure, ax-subscriptions-header, ax-subscriptions-heading-metric, ax-subscriptions-heading-verdict, ax-subscriptions-health-hero, ax-subscriptions-health-line, ax-subscriptions-invoice-record, ax-subscriptions-invoice-record-empty, ax-subscriptions-invoice-record-list, ax-subscriptions-invoice-records, ax-subscriptions-invoice-strip, ax-subscriptions-invoice-strip-danger, ax-subscriptions-kpi-row, ax-subscriptions-page, ax-subscriptions-primary-action, ax-subscriptions-primary-workspace, ax-subscriptions-priority-actions, ax-subscriptions-priority-copy, ax-subscriptions-queue-shortcut, ax-subscriptions-route-line, ax-subscriptions-secondary-group, ax-subscriptions-secondary-group-primary, ax-subscriptions-secondary-link, ax-subscriptions-secondary-strips, ax-subscriptions-utility-strip, ax-subscriptions-webhook-strip, ax-subscriptions-webhook-workspace. The token ax-subscriptions-page in particular is reused as a compound-selector ancestor prefix in roughly 50 separate rules spread across responsive breakpoints, data-table-specific overrides, and filter-chip rules throughout a wide span of the file - sweep the WHOLE file for every occurrence (do not assume one contiguous block) and delete every rule/branch that references it.
    Handle this specific comma-grouped landmine precisely: the rule (inside a "min-width: 1024px" media query) reading ".ax-shell-content:has(> .ax-subscriptions-page), .ax-shell-content:has(> .ax-subscription-detail-page), .ax-shell-content:has(> .ax-home) { padding-top: 0; }" - remove ONLY the ".ax-shell-content:has(> .ax-subscriptions-page)" branch. Keep the rule and its other two branches (.ax-subscription-detail-page and .ax-home are both live) fully intact.
    Delete all 17 ax-subscription-row-* classes: ax-subscription-row-admin-chips, ax-subscription-row-audit, ax-subscription-row-audit-primary, ax-subscription-row-customer, ax-subscription-row-customer-scope, ax-subscription-row-id, ax-subscription-row-invoice-action, ax-subscription-row-invoice-controls, ax-subscription-row-invoice-primary, ax-subscription-row-invoices, ax-subscription-row-meta, ax-subscription-row-meta-grid, ax-subscription-row-primary-line, ax-subscription-row-signal-primary, ax-subscription-row-signal-secondary, ax-subscription-row-state, ax-subscription-row-webhook-action. The class ax-subscription-row-webhook-action is defined TWICE in the file (once in the main subscriptions-row block, and again as a separate, non-comma-grouped standalone rule sitting among the detail-page button-sizing rules far below) - both definitions are dead and must both be deleted.
    Do NOT touch ax-subscription-setup-gap anywhere in the file - it is the sole live member of the "ax-subscription-setup*" family (rendered by subscriptions_live.ex's setup_gap_cell/2 helper) and sits immediately adjacent to the dead ax-subscriptions-page rules in the source - read the surrounding lines carefully before deleting nearby rules so this one is never caught in a broader deletion.
    For every class above, use the exact-token pattern (negative lookbehind for word-char-or-hyphen, the literal class name, negative lookahead for word-char-or-hyphen) via ripgrep's -oP mode against the WHOLE file, re-grepping after each edit.
    Once every deletion in this task and Task 1 is complete and the file is in its final retired state, rebuild the bundle: run "mix accrue_admin.assets.build" from accrue_admin/, which regenerates priv/static/accrue_admin.css (expect a large shrink) and priv/static/accrue_admin.js (expect zero diff - no JS source changed this phase). Commit accrue_admin.css and accrue_admin.js together with the final app.css edit in this task, per the established Phase 209/210 "rebuild after the last CSS edit, same commit" convention.
  </action>
  <verify>
    <automated>cd accrue_admin && grep -ocE "(^|[^A-Za-z0-9_-])(ax-subscriptions-page|ax-subscription-row-webhook-action|ax-subscription-row-state)([^A-Za-z0-9_-]|$)" assets/css/app.css; grep -ocE "(^|[^A-Za-z0-9_-])ax-subscription-setup-gap([^A-Za-z0-9_-]|$)" assets/css/app.css; mix accrue_admin.assets.build</automated>
  </verify>
  <acceptance_criteria>
    - Every class named in the DELETE list above returns a count of 0 from the exact-token grep above in assets/css/app.css (the sample in the verify command proves this for its sample; run the equivalent check per-class for the full list before closing the task).
    - ax-subscription-setup-gap still returns at least 1 exact-token match (untouched landmine).
    - The .ax-shell-content:has(...) media-query rule still has exactly two branches (.ax-subscription-detail-page and .ax-home) after edit.
    - mix accrue_admin.assets.build exits 0 and priv/static/accrue_admin.css shrinks while priv/static/accrue_admin.js is byte-identical to before this task.
  </acceptance_criteria>
  <done>All Subscriptions/SubscriptionRow dead selectors are removed, ax-subscription-setup-gap is untouched, and the rebuilt accrue_admin.css/.js are committed alongside the final app.css edit.</done>
</task>

</tasks>

## Artifacts this phase produces

No new artifacts in this plan - it deletes existing dead CSS and rebuilds the two already-existing generated bundle files (`priv/static/accrue_admin.css`, `priv/static/accrue_admin.js`) via the pre-existing `mix accrue_admin.assets.build` task. The new artifact for this phase (the orphan-guard script) was created in Plan 01.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan deletes unused CSS selectors and rebuilds a static asset bundle via an existing, already-vetted mix task. No production code path, endpoint, or user input is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-02 | Tampering | comma-grouped selector over-deletion | medium | mitigate | Every comma-grouped rule identified in RESEARCH.md (the .ax-shell-content:has(...) media rule, the global :focus-visible/:hover interaction-state rule, the global status-badge-color rule) is edited branch-by-branch, never rule-by-rule, per the exact-token verification in each task's acceptance criteria. |
| T-211-03 | Tampering | accidental deletion of the ax-subscription-setup-gap landmine | high | mitigate | Explicit do-not-touch instruction plus a dedicated acceptance-criteria check confirming the class still resolves after Task 2's edits. |
| T-211-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan; mix accrue_admin.assets.build invokes two already-pinned, already-vetted npx tool versions (tailwindcss@3.4.17, esbuild@0.25.3) unchanged by this phase. |
</threat_model>

<verification>
- Exact-token grep confirms zero references to all 92 named DELETE classes + 5 D-01 adjacent selectors in app.css.
- Exact-token grep confirms all 16 PRESERVE classes (including ax-subscription-setup-gap) remain present.
- The two identified comma-grouped rules retain their live branches.
- mix accrue_admin.assets.build succeeds; accrue_admin.css shrinks, accrue_admin.js is unchanged; both are committed with the final app.css edit.
</verification>

<success_criteria>
app.css contains zero references to any of the 97 target dead selectors, all 16 PRESERVE classes (including the ax-subscription-setup-gap landmine) are intact, and the rebuilt accrue_admin.css/.js bundle reflects the fully-retired CSS.
</success_criteria>

<output>
Create `.planning/phases/211-grep-gated-css-retirement-cross-surface-cleanup/211-02-SUMMARY.md` when done.
</output>

---
phase: 211-grep-gated-css-retirement-cross-surface-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["211-03"]
files_modified:
  - accrue_admin/e2e/ratchet/region-tags.js
autonomous: false
requirements: [REIGN-04]
must_haves:
  truths:
    - "region-tags.js's attention-rail selector maps to the real, live [data-ax-zone='attention-rail'] selector instead of the never-existed dangling ax-attention-rail string"
    - "Full mix test and the full admin e2e suite are green across the phase boundary with no new failures versus the Plan 01 baseline"
    - "The subscription detail page renders unbroken in both light and dark themes, with .ax-inline-worklist* and .ax-audit-summary-row intact"
    - "The whole-phase diff touches no accrue/lib and adds no new nav room/group"
  artifacts:
    - "accrue_admin/e2e/ratchet/region-tags.js (attention-rail selector fixed)"
  key_links:
    - "The Plan 01 mix test baseline is the reference point for 'no red left behind' - Wave 4's full-suite run must match or exceed it, not an assumed-clean baseline"
---

<objective>
Opportunistically fix the one dangling ratchet selector this phase's own retirement is a provable side-effect of (D-03), then run the full verification sweep the ROADMAP requires before phase sign-off: complete unit + e2e suite, no-core-touched / no-nav-room confirmation, and a human-confirmed visual check that the subscription detail page's shared classes survived the retirement.

Purpose: This is the phase's certification wave. CSS deletion is invisible to source-text CI, so the only real proof of "nothing broke" is running everything and looking at the rendered detail page - matching the precedent set by Phase 209-03/210-03's human-verify closing checkpoints.
Output: A fixed region-tags.js, a fully green mix test + e2e suite, and a human-confirmed unbroken subscription detail page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/211-grep-gated-css-retirement-cross-surface-cleanup/211-RESEARCH.md
@accrue_admin/e2e/ratchet/region-tags.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the dangling attention-rail selector in region-tags.js (D-03)</name>
  <read_first>
    - accrue_admin/e2e/ratchet/region-tags.js lines 77-99 (the REGION_SELECTORS map and its docstring - confirms the value is documentation-only metadata, never consumed elsewhere, so this is a low-risk fix)
    - 211-RESEARCH.md "## Secondary Surfaces" section "Ratchet region-tags.js" (the exact recommended replacement and rationale)
  </read_first>
  <files>accrue_admin/e2e/ratchet/region-tags.js</files>
  <action>
    In the REGION_SELECTORS object, change the "attention-rail" entry's value from the string "ax-attention-rail" (a selector that never existed as a real CSS class - only the differently-named ".ax-attention-rail--empty" modifier exists) to the string "[data-ax-zone='attention-rail']" (the real, live, stable selector already used by dashboard_live.ex's attention-rail section and already the selector the Phase 199/210 Playwright ratchet guards were retargeted to). Update the inline comment on that line to note the fix and its rationale (matches the phase199 guard selector and dashboard_live.ex's data-ax-zone attribute). Do NOT touch any other REGION_SELECTORS entry (toolbar, tab-bar, kpi-row, detail-panel, related-panel, timeline, payload-viewer, content-body, layer all keep their existing "// TODO: confirm selector" markers - explicitly out of scope for REIGN-04 per the locked D-03 decision).
  </action>
  <verify>
    <automated>cd accrue_admin && grep -n "attention-rail" e2e/ratchet/region-tags.js</automated>
  </verify>
  <acceptance_criteria>
    - The "attention-rail" entry in REGION_SELECTORS reads "[data-ax-zone='attention-rail']", not "ax-attention-rail".
    - No other REGION_SELECTORS entry was modified (grep -c "TODO: confirm selector" e2e/ratchet/region-tags.js returns 9, same as before this task - one fewer TODO marker than the pre-fix 10, since attention-rail's TODO comment is replaced).
    - Running `node e2e/ratchet/region-tags.js` directly (its require.main self-test entry point) still exits 0, confirming the fix did not disturb the self-test discipline.
  </acceptance_criteria>
  <done>region-tags.js's attention-rail selector points at the real, live [data-ax-zone="attention-rail"] selector; all other TODO-marked entries are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Full mix test + admin e2e suite + no-core-touched / no-nav-room confirmation</name>
  <read_first>
    - 211-RESEARCH.md "## Test/E2E Migration Surface" table (the full list of e2e gate commands; confirms per D-05 that zero test/e2e selector migrations are needed this phase since Phases 209/210 already migrated their own selectors - this task's e2e run is the final verification of that claim, not a migration)
    - 211-01-SUMMARY.md (the recorded pre-Phase-211 mix test baseline to compare against)
  </read_first>
  <files>none - verification only, no files modified</files>
  <action>
    Run the full accrue_admin unit suite: `mix test` from accrue_admin/. Compare the pass/fail/skip counts against the Plan 01 baseline recorded in 211-01-SUMMARY.md - there must be zero NEW failures (matching or exceeding the baseline). Then run the full e2e suite plus the specifically-named gates from RESEARCH.md: the bare `npm run e2e` (all specs, both chromium-desktop and chromium-mobile projects), plus explicitly confirm `npm run e2e:phase194` (Home overview, includes the D-06 empty-rail check on the PRESERVE ax-attention-rail--empty class), `npm run e2e:phase196` and `npm run e2e:phase197` (Subscriptions list/detail surfaces this phase's deletions touch), `npm run e2e:phase199` (interaction/overlay focus-ring guards), `npm run e2e:a11y` (axe scan - expect only the 2 pre-approved-deferred dark-mode contrast items and nothing new), and `npm run phase200:storybook` (storybook unit tests + a11y spec, already partially covered in Plan 03 but re-run here as part of the full sweep). Then confirm the scope guardrails: run a grep-based diff check confirming zero changes to accrue/lib since the Phase 210 completion commit (412ada26), and confirm accrue_admin/lib/accrue_admin/router.ex and accrue_admin/lib/accrue_admin/nav.ex are unchanged since that same commit (no new nav room/group was added).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - mix test shows zero new failures versus the 211-01-SUMMARY.md baseline.
    - npm run e2e, e2e:phase194, e2e:phase196, e2e:phase197, e2e:phase199, e2e:a11y, and phase200:storybook all pass (a11y expected to show only the 2 known pre-existing dark-mode contrast items, nothing new).
    - `git diff 412ada26..HEAD --stat -- ../accrue/lib` (run from accrue_admin/, or `git diff 412ada26..HEAD --stat -- accrue/lib` from repo root) is empty.
    - `git diff 412ada26..HEAD --stat -- accrue_admin/lib/accrue_admin/router.ex accrue_admin/lib/accrue_admin/nav.ex` is empty.
  </acceptance_criteria>
  <done>Full unit + e2e suite is green with no regressions versus the Plan 01 baseline, and the diff confirms no core touched and no new nav room.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Subscription detail page PNG parity check (light + dark) and final sign-off</name>
  <what-built>The 92 named + 5 adjacent dead ax-* selectors are removed from app.css; the committed accrue_admin.css/.js bundle and storybook.css are rebuilt/recomposed; the orphan guard confirms a clean, correctly-scoped retirement; region-tags.js's attention-rail selector is fixed; full mix test + admin e2e suite are green with no regressions.</what-built>
  <action>Human visually verifies the subscription detail page (and Subscriptions list, Home, component kitchen) against their pre-retirement appearance, in light and dark themes, per the steps below. No code is written in this task; it gates on the shared-CSS-preservation claim (REIGN-04 SC2), which no automated check can fully assert for a visual/layout regression.</action>
  <how-to-verify>
    This is a visual/layout regression check no automated gate can fully cover (per 211-VALIDATION.md's Manual-Only Verification entry): the subscription DETAIL page (subscription_live.ex) shares .ax-inline-worklist, .ax-inline-worklist-copy, .ax-audit-summary-row with the now-deleted Subscriptions LIST page CSS, and a wrong deletion here would silently break the out-of-scope detail page.
    1. Start the demo host and log into /billing, navigate to a subscription's detail page (see MEMORY "accrue_host Docker DX" for the curl-login / restart pattern; confirm the served bundle is the freshly rebuilt priv/static/accrue_admin.css - a docker restart recompiles bind-mounted source and re-hashes assets).
    2. Capture the detail page in BOTH light and dark theme.
    3. Confirm the inline worklist card-strip section (production/invoice-preview/webhook-preview/audit-strip areas, wherever `.ax-inline-worklist`/`.ax-inline-worklist-copy` render) looks IDENTICAL to before this phase - same spacing, same borders, same text treatment. No missing styling, no unstyled raw HTML.
    4. Confirm the audit-summary-row area (`.ax-audit-summary-row`) also renders identically.
    5. Also spot-check the Subscriptions LIST page and the Home page render correctly (no visual regression from the deletion - since both pages' shared PRESERVE classes, e.g. ax-home-launcher-card, ax-attention-row, ax-subscription-setup-gap, are what's left standing after retirement).
    6. Confirm the component kitchen (`/billing/dev/components`) still renders its inline-worklist and audit-summary-row specimens correctly (it uses the same PRESERVE classes).
  </how-to-verify>
  <resume-signal>Type "approved" only if the subscription detail page, Subscriptions list, Home, and the component kitchen all render correctly in both light and dark themes with no visual regression; otherwise describe the specific breakage to fix before re-running this checkpoint.</resume-signal>
  <acceptance_criteria>
    - Subscription detail page's .ax-inline-worklist*/.ax-audit-summary-row sections render unbroken in both themes (PNG-confirmed).
    - Subscriptions list, Home, and the component kitchen all render correctly post-retirement (no unstyled/missing-CSS regressions).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

No new artifacts in this plan - it fixes one existing dangling metadata value (region-tags.js) and runs verification. No new symbols or files. This closes out the phase: the only net-new artifact across all of Phase 211 is `accrue_admin/e2e/verify-css-census.mjs` (created in Plan 01) plus its two package.json script aliases.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan edits a dev-tool selector map (never consumed at runtime, per RESEARCH.md) and runs full verification. No production code path, endpoint, or user input is changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-05 | Repudiation | silent visual regression on the shared detail-page classes | medium | mitigate | Blocking human-verify checkpoint with explicit PNG comparison in both themes before phase sign-off. |
| T-211-06 | Tampering | scope creep into core accrue/lib or a new nav room | low | mitigate | Explicit git-diff checks against the Phase 210 completion commit for accrue/lib, router.ex, and nav.ex. |
| T-211-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan. |
</threat_model>

<verification>
- region-tags.js attention-rail selector fixed; all other TODO markers untouched; self-test still passes.
- Full mix test + full e2e suite green, no regressions versus the Plan 01 baseline.
- git diff since the Phase 210 completion commit shows no accrue/lib, router.ex, or nav.ex changes.
- Human-verify checkpoint confirms the subscription detail page (and Subscriptions list, Home, component kitchen) render correctly in both themes post-retirement.
</verification>

<success_criteria>
The phase closes with a clean, complete, PNG-verified CSS retirement: zero regressions across the full test/e2e suite, the detail page unbroken, region-tags.js's attention-rail selector fixed, and no scope breach into core or navigation.
</success_criteria>

<output>
Create `.planning/phases/211-grep-gated-css-retirement-cross-surface-cleanup/211-04-SUMMARY.md` when done.
</output>


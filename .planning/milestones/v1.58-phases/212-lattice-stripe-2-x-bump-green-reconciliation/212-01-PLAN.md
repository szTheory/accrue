---
phase: 212-lattice-stripe-2-x-bump-green-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/mix.exs
  - accrue/mix.lock
  - accrue_admin/mix.lock
  - accrue_portal/mix.lock
  - examples/accrue_host/mix.lock
  - .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md
autonomous: true
requirements: [BUMP-01, BUMP-02, BUMP-03]

must_haves:
  truths:
    - "accrue/mix.exs pins {:lattice_stripe, \"~> 2.0\"} exactly — not tighter, not a raised ~> 2.1 floor (D-01)"
    - "All four mix.lock files (accrue, accrue_admin, accrue_portal, examples/accrue_host) resolve lattice_stripe to the IDENTICAL 2.x version string and matching checksum (D-02)"
    - "The lock target is whatever ~> 2.0 resolves to at bump time (2.1.0 per the 2026-07-30 research session) — the lockfile is never hand-frozen to 2.0.0 (D-02)"
    - "examples/accrue_host/mix.lock no longer contains a hex-mode :accrue entry after the path-mode regen (D-05)"
    - "mix compile --warnings-as-errors and mix compile --warnings-as-errors --no-optional-deps both exit 0 on every one of the four packages, with zero lattice_stripe-attributable warnings (D-08 — the --no-optional-deps run is the load-bearing proof that Finch is genuinely optional now)"
    - "The fixture-rename breaking vector is reconfirmed a no-op at bump time: accrue/test/support/stripe_fixtures.ex contains zero references to the promoted fixture-builder testing namespace (D-09)"
    - "accrue_admin and accrue_portal have zero direct LatticeStripe.* call sites — their BUMP-02 compliance rests on a clean transitive compile alone, not a call-site-level check (confirmed research finding, not a false negative)"
    - "Each package's Three Zeros gate runs exactly its actually-configured subset (accrue + accrue_admin: test/credo/dialyzer/coverage; accrue_portal: test/credo/coverage, no dialyzer; examples/accrue_host: compile + bounded test only, no credo/dialyzer/coverage) — no task invents a uniform four-package gate"
    - "Zero new test/credo/dialyzer skips, exclusions, or @tag :skip entries are introduced anywhere to make the gate pass (BUMP-03)"
    - "No public Accrue.* function signature, return shape, observable behavior, error shape, or support-matrix entry changes as part of this phase — if the gate ever suggests one would be needed, work stops and a decision checkpoint fires instead of silently absorbing it (D-11)"
    - "accrue/mix.exs is not republished, its @version is not bumped, and no override:/independent host pin is added — hex-mode adopter resolution stays a release-time event out of this phase's scope (D-06)"
    - "accrue/mix.exs pin plus all four regenerated mix.lock files are committed together as one atomic commit touching exactly those 5 files (D-03/D-07)"
    - "A committed evidence artifact captures the sibling-repo diff surface summary, the fixture-decoupling statement, and the full per-package gate output (D-10)"
  artifacts:
    - accrue/mix.exs
    - accrue/mix.lock
    - accrue_admin/mix.lock
    - accrue_portal/mix.lock
    - examples/accrue_host/mix.lock
    - .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md
  key_links:
    - "accrue/mix.exs lattice_stripe pin -> mix deps.update lattice_stripe (path mode, D-04 order) -> each sibling's regenerated mix.lock"
    - "accrue/test/support/stripe_fixtures.ex (hand-rolled maps) -> zero coupling to the sibling's promoted fixture-builder namespace (BUMP-02 fixture vector, D-09)"
    - "CI's dialyzer PLT cache key includes hashFiles(mix.lock) -> the bump auto-invalidates and rebuilds the PLT with no manual clear step (BUMP-03)"
  prohibitions:
    - "MUST NOT introduce any new test/credo/dialyzer skip, exclude, or @tag :skip to make the gate pass"
    - "MUST NOT raise the accrue/mix.exs floor beyond the 2.0 minor, or add any new required dependency"
    - "MUST NOT silently absorb a fix that touches a public Accrue.* function signature/return, observable behavior, error shape, or support matrix — such a fix MUST stop-and-flag per D-11, never be absorbed inline"
---

<objective>
Bump the sole `:lattice_stripe` pin from `~> 1.1` to `~> 2.0` in `accrue/mix.exs` (D-01), regenerate all four resolving lockfiles in dev/path mode in the D-04 order (D-03/D-04), run each package's actually-configured Three Zeros gate to prove the two pre-verified 2.0.0 breaking vectors (fixture-builder rename, Finch-optional) genuinely need zero Accrue-side code change (D-08/D-09), and produce a small auditable evidence artifact plus one atomic 5-file commit (D-10/D-03/D-07).

This is a mechanical dependency-currency phase: the central thesis, independently re-verified this session in RESEARCH.md with zero `[ASSUMED]` claims, is that no Accrue-side code changes are needed at all. The work is entirely: edit one pin, regenerate four lockfiles, run the existing gate, capture evidence, commit. Requirements: BUMP-01, BUMP-02, BUMP-03.

Purpose: unblock Phase 213 (the `LatticeStripe.Entitlements.*` surface only exists on 2.x) while keeping every package green with no new skips.
Output: `accrue/mix.exs` + 4 regenerated `mix.lock` files (one atomic commit), plus a committed `212-UPGRADE-EVIDENCE.md` artifact (separate commit).
</objective>

## Artifacts this phase produces

This phase creates essentially no new code symbols — it is a dependency-pin bump. The concrete artifacts:

| Artifact | Type | Produced by |
|---|---|---|
| `accrue/mix.exs` (line ~64 pin edit: `{:lattice_stripe, "~> 1.1"}` → `{:lattice_stripe, "~> 2.0"}`) | modified | Task 1 |
| `accrue/mix.lock` | regenerated | Task 1 |
| `accrue_admin/mix.lock` | regenerated | Task 1 |
| `accrue_portal/mix.lock` | regenerated | Task 1 |
| `examples/accrue_host/mix.lock` (also drops the stale hex-mode `:accrue` entry) | regenerated | Task 1 |
| `.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md` | new file | Task 3 |
| One atomic git commit (`accrue/mix.exs` + 4 `mix.lock` files, exactly 5 paths) | commit | Task 3 |
| One docs git commit (`212-UPGRADE-EVIDENCE.md`) | commit | Task 3 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-CONTEXT.md
@.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Bump the pin and regenerate all four lockfiles in path mode</name>
  <reversibility rating="costly">The `~> 2.0` pin string ships in the published `accrue` package requirement and reaches every adopter's dependency tree (D-01); loosening or tightening it later is a published-contract change, not a local edit. Rated costly, not one-way: no data migration or broken wire-format is involved, and CONTEXT.md already locked the exact pin value — flagged for the record, no checkpoint.</reversibility>
  <files>accrue/mix.exs, accrue/mix.lock, accrue_admin/mix.lock, accrue_portal/mix.lock, examples/accrue_host/mix.lock</files>
  <read_first>accrue/mix.exs (deps list, ~line 64); accrue/mix.lock, accrue_admin/mix.lock, accrue_portal/mix.lock, examples/accrue_host/mix.lock (current lattice_stripe 1.7.13 entries + the stale host hex :accrue line); .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-CONTEXT.md decisions D-01 through D-07; .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-RESEARCH.md "Code Examples -> The atomic 5-file commit shape" and the Architecture Diagram (hex-vs-path duality).</read_first>
  <action>
    Edit the single pin in accrue/mix.exs: change the lattice_stripe dependency tuple's version requirement from the 1.1 minor to the 2.0 minor, using Hex's `~>` operator so a future 2.1.0 still resolves — do not tighten it further and do not raise the floor to chase a newer minor (D-01). This is the sole edit point; no sibling package independently pins lattice_stripe (confirmed this session), so BUMP-01's lockstep clause is a no-op here (D-06).

    Regenerate the four lockfiles surgically, in this exact order, each with a single-dependency update (not a blanket deps.get, per D-04, so unrelated deps do not churn): first accrue, then accrue_admin, then accrue_portal, then examples/accrue_host, using mix's single-dependency update task for lattice_stripe in each package's own directory. Because none of the three downstream siblings independently pin lattice_stripe, and each resolves its own :accrue dependency via a path-mode dev/CI helper (env-guarded, hex mode only at publish time), the one edited pin in accrue/mix.exs propagates into all three downstream locks automatically once each sibling's lattice_stripe sub-dependency is refreshed.

    Do not hand-freeze any lock to a specific patch version — accept whatever the `~> 2.0` requirement resolves to at bump time (expected 2.1.0 per this session's live Hex.pm query, itself a release-tooling-only release with no API delta; re-check live if this task runs more than a few days after 2026-07-30, since a newer 2.x patch could land) (D-02).

    Do not republish accrue, bump its @version, or add an override:/independent host pin as part of this task — external Hex-mode adopter resolution is a release-time event, not this phase's concern, and CI never runs hex mode for anything this phase gates on (D-06).
  </action>
  <verify>
    <automated>grep -q '"~> 2.0"' accrue/mix.exs && for d in accrue accrue_admin accrue_portal examples/accrue_host; do (cd "$d" && mix compile --warnings-as-errors) || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'lattice_stripe' accrue/mix.exs` shows the dependency tuple containing `"~> 2.0"`.
    - `grep -c '"~> 2.1"' accrue/mix.exs` outputs `0` (the floor was not raised — Pitfall 3 / D-01).
    - `grep -h '"lattice_stripe": {:hex, :lattice_stripe, "' accrue/mix.lock accrue_admin/mix.lock accrue_portal/mix.lock examples/accrue_host/mix.lock` shows all four lines sharing the identical version string and the identical trailing checksum.
    - `grep -c '{:hex, :accrue,' examples/accrue_host/mix.lock` outputs `0` (the previously-present stale hex-mode entry is gone — D-05).
    - `mix compile --warnings-as-errors` exits 0 when run inside each of `accrue`, `accrue_admin`, `accrue_portal`, `examples/accrue_host`.
  </acceptance_criteria>
  <done>The pin reads `~> 2.0`, all four locks resolve lattice_stripe to the same 2.x version and checksum, the host lock's stale hex accrue entry is gone, and every package compiles clean with the new resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Run each package's actually-configured Three Zeros gate and reconcile the two breaking vectors</name>
  <files>none expected — this task is verification-only under the documented HIGH-confidence outcome. If the rare D-11 inline-absorb contingency fires, the minimal internal fix lands in accrue/lib/** or accrue/test/support/** only (never accrue_admin, accrue_portal, or examples/accrue_host, which have zero direct LatticeStripe.* call sites).</files>
  <read_first>.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-RESEARCH.md "Validation Architecture" (per-package gate matrix) and "Common Pitfalls" Pitfall 1 (gate-composition asymmetry); .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-CONTEXT.md decisions D-08 through D-11; accrue/test/support/stripe_fixtures.ex; .github/workflows/ci.yml (lines ~220-350, the per-package compile/test/credo/dialyzer step ordering this task mirrors); scripts/ci/accrue_host_verify_test_bounded.sh (the bounded test slice CI itself runs for the host package, covering the two host files with LatticeStripe.* call sites).</read_first>
  <note>D-11's one-way rating applies to the CONTINGENT stop-and-flag decision (dynamically created only if the gate finds an unexpected public-surface delta — see the action's D-11 contingency paragraph), not to this verification task itself: running the gate makes no shipped change and is reversible. That contingency mirrors GSD's own dynamic authentication-gate pattern (checkpoints.md) rather than a statically preceding checkpoint:decision task, because the specific decision content cannot exist until this task's gate output is known — a checkpoint minted before this task would have nothing concrete to gate.</note>
  <action>
    Run the full per-package gate exactly as each package is actually configured (Pitfall 1's asymmetric matrix) — do not invent a uniform four-package command list.

    For accrue and accrue_admin, in each package's own directory: compile with warnings-as-errors; compile again with warnings-as-errors plus the no-optional-deps flag (D-08 — this is the load-bearing proof that Finch, now optional in lattice_stripe 2.x, does not silently remain required); run the test suite with warnings-as-errors; run credo in strict mode; run dialyzer with the github output format (the PLT cache key already includes a hash of mix.lock, so this bump automatically invalidates and rebuilds the PLT — no manual PLT-clear step is needed); run the coveralls task; run the hex audit task (checks for retired Hex packages, a cheap supply-chain sanity check on the new lattice_stripe release).

    For accrue_portal, the identical set minus dialyzer (not a configured dependency for this package).

    For examples/accrue_host: compile with warnings-as-errors; compile again with warnings-as-errors plus the no-optional-deps flag; then run the repo's own bounded host test script (scripts/ci/accrue_host_verify_test_bounded.sh, run from the repo root — it drops/creates/migrates its own test database and runs the curated file list CI itself runs, including the two files with direct LatticeStripe.* call sites). Host has no credo, dialyzer, or coverage configuration — do not attempt to run any of those three against it.

    Re-confirm the D-09 breaking-vector claim at bump time (RESEARCH.md's own re-verify-at-bump-time guidance): grep accrue/test/support/stripe_fixtures.ex for any reference to the sibling package's promoted fixture-builder testing namespace (put the exact grep in acceptance_criteria, not here) — the expected result is zero matches, proving the fixture-builder rename needs no change in this repo.

    Expected outcome, per this session's HIGH-confidence, independently re-derived research: every command above exits 0 with zero lattice_stripe-attributable warnings, and the fixture-vector grep returns zero matches — meaning both of D-09's pre-verified vectors (the fixture rename, the Finch-optional change) need zero Accrue-side code change. If that is what happens, make no code edits, note the confirmation for Task 3's evidence artifact, and proceed directly to Task 3 — do not create any checkpoint.

    D-11 contingency (only reached if the above is NOT what happens): if any command above fails for a reason genuinely traceable to the lattice_stripe 2.x bump (not a pre-existing, unrelated flake), stop before writing any reconciliation code and apply D-11's predicate. If the fix touches solely Accrue-internal glue — call sites, fixtures, adapter plumbing — and changes no public Accrue.* function signature or return shape, no observable behavior or error semantics, no support-matrix or dependency-requirement entry, and requires no new design choice, absorb it inline in this same task: make the minimal internal fix, re-run the failing command to confirm green, and record exactly what was changed and why in the evidence artifact (Task 3). Otherwise — the fix would touch a public Accrue.* signature or return, alter observable behavior or error shape, change the support matrix, or require a design decision — dynamically create a checkpoint:decision task with gate set to blocking-human (never gate blocking, since auto-approving away a public-surface change would defeat the guardrail's entire purpose): its decision names the specific unexpected delta found; its context states that undoing a shipped public-surface change costs a semver-breaking release and adopter migration; its two options are exactly D-11's two branches (absorb inline vs. stop-and-flag) with the concrete pros/cons for the specific delta found; its resume-signal asks the maintainer to pick one. Do not proceed past that checkpoint until the maintainer responds, and do not begin any reconciliation work before it resolves. This checkpoint is a guardrail against a near-zero-probability outcome, not a scheduled step — most runs of this task will never create it.
  </action>
  <verify>
    <automated>(cd accrue && mix compile --warnings-as-errors --no-optional-deps && mix test --warnings-as-errors && mix credo --strict && mix dialyzer --format github && mix coveralls && mix hex.audit) && (cd accrue_admin && mix compile --warnings-as-errors --no-optional-deps && mix test --warnings-as-errors && mix credo --strict && mix dialyzer --format github && mix coveralls && mix hex.audit) && (cd accrue_portal && mix compile --warnings-as-errors --no-optional-deps && mix test --warnings-as-errors && mix credo --strict && mix coveralls && mix hex.audit) && (cd examples/accrue_host && mix compile --warnings-as-errors --no-optional-deps) && bash scripts/ci/accrue_host_verify_test_bounded.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every command listed above exits 0: for `accrue` and `accrue_admin` — both compile variants, `mix test --warnings-as-errors`, `mix credo --strict`, `mix dialyzer --format github`, `mix coveralls`, `mix hex.audit`; for `accrue_portal` — the same set minus dialyzer; for `examples/accrue_host` — both compile variants plus `bash scripts/ci/accrue_host_verify_test_bounded.sh`.
    - `grep -c 'LatticeStripe\.Testing' accrue/test/support/stripe_fixtures.ex` outputs `0` (D-09 fixture-rename vector reconfirmed a no-op at bump time, not just at research time).
    - `git status --porcelain -- '*.exs' '*.ex'` shows no untracked or modified `.ex`/`.exs` source files outside the two mix.exs deps lines already touched in Task 1 — i.e., zero reconciliation edits were needed (the expected outcome).
    - No new `@tag :skip`, `ExUnit.configure(exclude: ...)`, or `# credo:disable-for-` marker is introduced anywhere in `accrue/`, `accrue_admin/`, `accrue_portal/`, or `examples/accrue_host/` as a side effect of this task.
  </acceptance_criteria>
  <done>Every package's actually-configured Three Zeros gate subset is green, the fixture-rename vector is reconfirmed a no-op, and either (a) zero reconciliation edits were needed — the expected, HIGH-confidence outcome — or (b) a minimal internal-glue fix was absorbed inline and logged, or (c) a blocking-human checkpoint:decision was created per D-11 and the task halted pending the maintainer's choice.</done>
</task>

<task type="auto">
  <name>Task 3: Eyeball-verify the locks, commit the atomic bump, and write the D-10 evidence artifact</name>
  <files>.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md</files>
  <read_first>.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-CONTEXT.md decisions D-07 and D-10; .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-RESEARCH.md "Sources" section (the exact `git diff`/`git show`/`git log` commands already run this session against `../lattice_stripe`) and "Code Examples -> The atomic 5-file commit shape"; accrue/mix.exs, accrue/mix.lock, accrue_admin/mix.lock, accrue_portal/mix.lock, examples/accrue_host/mix.lock (the five files to review and commit).</read_first>
  <action>
    Read all four regenerated mix.lock files in full (not just grep) and eyeball-confirm per D-07 — CI does not run `--check-locked`, so this manual read is the only check that the committed lock genuinely matches the resolved graph: each of the four shows the identical lattice_stripe version and checksum from Task 1, and examples/accrue_host/mix.lock still shows no hex-mode accrue entry.

    Stage exactly five files — accrue/mix.exs and the four mix.lock files — and commit them together as one atomic change (D-03), using a Conventional Commit message in this repo's established style, e.g. "chore(212-01): bump lattice_stripe ~> 1.1 to ~> 2.0 across accrue, accrue_admin, accrue_portal, examples/accrue_host (BUMP-01)". Stage and commit nothing else in this commit.

    Then create the evidence artifact at .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md (D-10) with three sections: (1) a diff-surface summary between the previously-locked tag and the newly-resolved tag, restricted to the lib/ directory, run from the sibling ../lattice_stripe checkout, confirming the delta is exactly the documented items (the Finch-optional change in client.ex and config.ex, the new entitlements/ and testing/fixtures/ modules, nothing else touching an existing public function); (2) the explicit fixture-decoupling statement, citing Task 2's re-confirmed grep result, that this repo's own fixtures never called the sibling's promoted fixture-builder API; (3) the full Task 2 gate output or pass/fail status per package, including both `--no-optional-deps` compiles, plus one short context-only note that the repo's pre-existing hex-mode host smoke CI step is unaffected by this phase because it resolves the previously-published accrue package rather than this phase's local pin (not a new gate — informational only).

    Commit the evidence file separately from the dependency bump: "docs(212-01): add lattice_stripe 2.x upgrade evidence artifact (BUMP-02, BUMP-03)".
  </action>
  <verify>
    <automated>git log --oneline -2 -- accrue/mix.exs .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat -1 <bump-commit-sha>` lists exactly these five paths and no others: `accrue/mix.exs`, `accrue/mix.lock`, `accrue_admin/mix.lock`, `accrue_portal/mix.lock`, `examples/accrue_host/mix.lock`.
    - `test -f .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md` succeeds.
    - The evidence file contains three identifiable sections covering: the sibling-repo lib/ diff surface, the fixture-decoupling statement, and the per-package gate output.
    - `git log --oneline -2` shows two separate commits: the 5-file bump commit, then the evidence-artifact docs commit.
  </acceptance_criteria>
  <done>All four locks were manually eyeball-verified against the pin, the atomic 5-file bump commit exists, and a committed evidence artifact captures the diff-surface summary, the fixture-decoupling statement, and the full gate output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependency resolution boundary | `accrue/mix.exs`'s `:lattice_stripe` requirement crosses into the Hex.pm registry and the maintainer-owned sibling `lattice_stripe` package's published `lib/` code — a major-version supply-chain boundary, even though both packages share an author. |
| Monorepo path-mode boundary | `accrue_admin`, `accrue_portal`, and `examples/accrue_host` resolve `:accrue` (and transitively `:lattice_stripe`) via path deps in dev/CI — same-repo, same-author, low-risk boundary; only the publish-time hex-mode switch (out of this phase's scope per D-06) crosses a real external-trust line for adopters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-01 | Tampering | `lattice_stripe` dependency resolution across all four `mix.lock` files | medium | mitigate | Task 3's evidence artifact captures a `git diff` of the sibling repo's `lib/` surface across the exact tag range being adopted, restricted to code (not just changelog prose); Task 2 runs `mix hex.audit` per package (retired-package check) as a cheap supply-chain sanity gate; the full per-package Three Zeros gate is the correctness backstop. All of this was independently re-derived and zero-anomaly this session (RESEARCH.md), against a maintainer-owned sibling repo with visible git history. |
| T-212-02 | Denial of Service (minor, informational) | Auto-started default Finch pool (`lattice_stripe` 2.0.0's `start_default_finch: false` opt-out) | low | accept | Accrue itself declares and references no `:finch` anywhere in `accrue/mix.exs` or `accrue/lib` (confirmed this session) — this phase introduces no new pool for Accrue's own process tree. Documented as a one-line host-app-awareness note in the D-10 evidence artifact; no Accrue-side code change required. |

</threat_model>

<verification>
Run all three tasks' `<verify><automated>` commands in order. Task 1 proves the pin + all four locks resolve consistently and compile clean. Task 2 proves each package's actually-configured Three Zeros gate subset is green and both D-09 breaking vectors are reconfirmed no-ops (or, in the near-zero-probability contingency case, that D-11's absorb/flag predicate was correctly applied). Task 3 proves the atomic 5-file commit and the evidence artifact both exist. A fresh clean-checkout `mix deps.get && mix compile --warnings-as-errors` succeeding in each of the four packages (Roadmap Success Criterion 5) is implied by Task 1 + Task 2's compile checks already having run against the freshly-regenerated locks.
</verification>

<success_criteria>
- `accrue/mix.exs` pins `{:lattice_stripe, "~> 2.0"}`; not `~> 2.0.0`, not `~> 2.1`.
- All four `mix.lock` files resolve `lattice_stripe` to the identical 2.x version and checksum; `examples/accrue_host/mix.lock` no longer carries a hex-mode `:accrue` entry.
- Every `LatticeStripe.*` call site across `accrue`, `accrue_admin`, `accrue_portal`, and `examples/accrue_host` compiles with zero deprecation warnings, including under `--no-optional-deps`.
- Both pre-verified 2.0.0 breaking vectors (fixture rename, Finch-optional) are reconfirmed as needing no Accrue-side change, or — in the contingency case — reconciled per D-11 with either an inline absorb or a resolved maintainer decision.
- Each package's actually-configured Three Zeros gate subset is green with zero new skips/exclusions.
- The pin and all four locks are committed together as one atomic 5-file change; a committed evidence artifact documents the diff surface, the fixture-decoupling statement, and the gate output.
</success_criteria>

<output>
Create `.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-01-SUMMARY.md` when done.
</output>
---
phase: 213-stripe-native-advisory-entitlements-sync-observational-only
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor.ex
  - accrue/lib/accrue/processor/fake/state.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/lib/accrue/entitlements/stripe_sync.ex
  - accrue/lib/accrue/entitlements/reconcile.ex
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/test/accrue/entitlements/stripe_sync_refresh_test.exs
  - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
  - accrue/test/accrue/webhook/wr05_concurrency_test.exs
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-05
autonomous: true
must_haves:
  truths:
    - "Per D-01 through D-06, an advisory refresh uses the optional Processor facade, materializes a complete Fake-provided entitlement list, and writes one EntitlementSummary row."
    - "Per D-06, disabled refresh returns {:ok, :disabled} before Processor or Repo I/O."
    - "Per D-11 through D-13, pull and webhook writes share one reconciler, preserve event watermarks across pulls, and converge monotonically under tested ordering."
    - "Per D-09, refresh emits bounded sync and summary telemetry with source: :pull and never raw payload data."
  artifacts:
    - path: accrue/lib/accrue/processor.ex
      provides: "optional list_active_entitlements/2 callback/facade and facade-safe active-entitlement metadata accessor"
      contains: "list_active_entitlements"
    - path: accrue/lib/accrue/entitlements/stripe_sync.ex
      provides: "public observational refresh/2 primitive"
      contains: "def refresh"
    - path: accrue/lib/accrue/entitlements/reconcile.ex
      provides: "shared webhook and pull EntitlementSummary writer"
      contains: "defmodule Accrue.Entitlements.Reconcile"
    - path: accrue/test/accrue/entitlements/stripe_sync_refresh_test.exs
      provides: "Fake-only end-to-end refresh and disabled-lane proof"
  key_links:
    - from: accrue/lib/accrue/entitlements/stripe_sync.ex
      to: accrue/lib/accrue/processor.ex
      via: "refresh/2 calls Processor.list_active_entitlements/2 and then obtains list_path through Processor.active_entitlement_list_metadata/0 only after the config branch"
      pattern: "Processor\\.list_active_entitlements"
    - from: accrue/lib/accrue/entitlements/stripe_sync.ex
      to: accrue/lib/accrue/entitlements/reconcile.ex
      via: "complete pull list is converted into a summary-shaped reconcile input"
      pattern: "Reconcile"
    - from: accrue/lib/accrue/webhook/default_handler.ex
      to: accrue/lib/accrue/entitlements/reconcile.ex
      via: "webhook summary reducer delegates the extracted write path"
      pattern: "Reconcile"
  prohibitions:
    - "The advisory cache must never silently become an input to any grant, resolver, or guard decision."
    - "The phase must not introduce a Stripe-backed entitled?/fetch_entitled predicate that consumers could treat as authorization."
  flagged_assumptions:
    - "SYNC-01 unresolved edge: the LatticeStripe stream is treated as complete only after full materialization; any page error leaves the cache unchanged."
    - "SYNC-02 unresolved edge: process-global config tests restore state and run non-concurrently where required; disabled means zero Processor and Repo calls."
    - "SYNC-03 concurrency edge: pull start time and webhook event time form the unified synced_at order; strict-greater conflict semantics reject stale/equal retries."
    - "SYNC-05 unresolved edge: Fake's named process is reset per test and tests that mutate application config use async: false to remain suite-safe."
---

<objective>
Deliver the production-quality end-to-end Fake-backed advisory refresh tracer, then prove the shared write path's D-11 ordering contract.

Purpose: Close the core SYNC-01 path without weakening the local grant authority.
Output: Processor/Fake seam, `StripeSync.refresh/2`, shared `Reconcile` writer, and ordering/disabled tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-CONTEXT.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-RESEARCH.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-PATTERNS.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VALIDATION.md
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/billing/entitlement_summary.ex
</context>

<assumption_delta_decision>
Primary anchor: canonical entitlement identity. Select **no-change**: per D-01/D-11, making Stripe-native refresh opt-in adds an advisory observation alongside the canonical local plan-to-feature identity; it does not promote Stripe data into entitlement identity or grant authority.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end one-customer advisory refresh through Fake and the shared writer</name>
  <precondition>Phase 212 is complete and `cd accrue && mix compile --warnings-as-errors` resolves the LatticeStripe 2.x dependency.</precondition>
  <files>accrue/lib/accrue/processor.ex, accrue/lib/accrue/processor/fake/state.ex, accrue/lib/accrue/processor/fake.ex, accrue/lib/accrue/entitlements/stripe_sync.ex, accrue/lib/accrue/entitlements/reconcile.ex, accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/entitlements/stripe_sync_refresh_test.exs</files>
  <read_first>accrue/lib/accrue/processor.ex; accrue/lib/accrue/processor/fake/state.ex; accrue/lib/accrue/processor/fake.ex; accrue/lib/accrue/entitlements/stripe_sync.ex; accrue/lib/accrue/webhook/default_handler.ex; accrue/lib/accrue/billing/entitlement_summary.ex; accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs</read_first>
  <behavior>
    - "A seeded one-item Fake list refreshes one customer and persists a summary-shaped EntitlementSummary with count 1, truncated false, pull provenance, SDK list path, and unchanged event watermark fields."
    - "A second materially identical refresh returns the chosen unchanged result and creates no duplicate ledger event."
    - "With stripe_native_sync disabled, refresh returns {:ok, :disabled}, Fake call_count(:list_active_entitlements) does not change, and no summary row is read or written."
  </behavior>
  <action>Write the failing end-to-end tests first. Per D-01/D-02 add `list_active_entitlements/2` to `Accrue.Processor` with the exact complete-list result contract, public facade guards, and `@optional_callbacks` membership. Add a separate facade-safe `Processor.active_entitlement_list_metadata/0` accessor returning `%{list_path: binary()}`: it delegates to an implementation metadata export when present and otherwise returns the documented canonical active-entitlements path, so D-01 remains the only new behavior callback and existing custom processors remain compatible. Per D-04 add `entitlements` to `Fake.State`, `Fake.put_entitlements/2`, the callback, call-count/script/stub integration, reset semantics, and canonical Fake metadata. Create `Accrue.Entitlements.Reconcile` per D-12/D-13 by extracting the existing material-change, ledger, telemetry, and atomic upsert behavior from `DefaultHandler`; both webhook and pull callers must use it, and provenance stays in `data["_accrue"]` with no migration. Extend `StripeSync` with public `refresh/2` per D-05/D-06: make the config predicate the first executable branch, capture `pull_started_at` before Processor I/O, call only the Processor facade, obtain `list_path` from `Processor.active_entitlement_list_metadata/0` after the complete-list fetch, and pass that value into `Reconcile` when reconstructing D-11's summary shape, including for an empty list; set `truncated: false`, preserve stored Stripe event watermark values, and pass bounded `source: :pull` metadata through the existing telemetry span/event per D-09. Assert the persisted `data["entitlements"]["url"]` equals the facade metadata path. Choose `{:ok, :unchanged}` for a material no-op. Keep all `LatticeStripe` references out of these files per D-03, do not add a top-level `Accrue` delegate per D-08, and do not add D-10's deferred UI/scheduler surfaces.</action>
  <reversibility rating="costly">D-01/D-05 add optional public contracts; additive now, but removal would be breaking.</reversibility>
  <acceptance_criteria>The Fake-backed happy path is real end to end, the disabled path performs zero Processor/Repo I/O, both writers use `Reconcile`, and identical refreshes are idempotent.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/stripe_sync_refresh_test.exs test/accrue/webhook/default_handler_entitlement_summary_test.exs</automated>
  </verify>
  <done>One customer can be refreshed from the Fake processor into the diagnostic cache, while config-off remains a strict no-op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove D-11 pull/webhook monotonic ordering before finalizing the reconciler guard</name>
  <files>accrue/lib/accrue/entitlements/reconcile.ex, accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/entitlements/stripe_sync_refresh_test.exs, accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs, accrue/test/accrue/webhook/wr05_concurrency_test.exs</files>
  <read_first>accrue/lib/accrue/entitlements/reconcile.ex; accrue/lib/accrue/webhook/default_handler.ex; accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs; accrue/test/accrue/webhook/wr05_concurrency_test.exs; accrue/test/property/entitlement_summary_monotonic_property_test.exs</read_first>
  <behavior>
    - "A pull whose captured start time predates a strictly newer webhook event cannot clobber that webhook row."
    - "A strictly newer webhook always applies after a pull, and a pull begun after the stored webhook may apply without erasing its event watermark."
    - "Concurrent pull and webhook calls do not raise; the greatest synced_at snapshot wins while last_stripe_event_ts/id remain the greatest real webhook watermark."
    - "Timestamp-less webhooks use handler time without wiping the prior event watermark; equal timestamp webhook redelivery stays unchanged."
  </behavior>
  <action>Make all five D-11 cases fail first, including a deterministic Sandbox concurrency case. Implement the locked unified monotonicity direction in the shared `on_conflict` query: compare `EXCLUDED.synced_at` strictly against stored `synced_at`; timestamped webhooks retain `synced_at_from_event(evt_ts)`, timestamp-less webhooks use the existing clock fallback, and pulls use the start captured before network I/O. Preserve `check_stale/2` as the early webhook optimization and preserve/carry `last_stripe_event_ts` plus `last_stripe_event_id` for pull and timestamp-less writes so a poll never advances, clears, or reopens the event watermark. Retain ADV-01's single DB conflict guard and ADV-03 stale conversion; do not add optimistic locking. If any named case cannot pass under this exact model, stop and record the counterexample rather than weakening or deleting the test.</action>
  <reversibility rating="costly">D-11 changes correctness-critical conflict ordering but is code-only and has no migration or published data-contract break.</reversibility>
  <acceptance_criteria>All named sequential, equal-time, timestamp-less, and concurrent orderings pass without StaleEntryError or watermark loss.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/stripe_sync_refresh_test.exs test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/webhook/wr05_concurrency_test.exs test/property/entitlement_summary_monotonic_property_test.exs</automated>
  </verify>
  <done>The shared writer has an executable concurrency contract covering webhook↔pull composition and existing webhook monotonicity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| processor result → advisory reconciler | Fake now and Stripe later provide untrusted external-shaped maps; only summary fields enter the cache. |
| advisory cache → grant path | This boundary must remain one-way: diagnostic writers/readers cannot flow into resolver or guard decisions. |
| concurrent webhook/pull → database | Independently timed snapshots contend for one customer row through the atomic conflict guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-213-01 | Tampering | `Accrue.Entitlements.Reconcile` | high | mitigate | Fail-first ordering tests plus strict `synced_at` conflict guard and event-watermark carry-forward per D-11. |
| T-213-02 | Elevation of Privilege | resolver/guard boundary | high | mitigate | No gate-file edits; cache remains write/read diagnostic only per D-01/D-11. |
| T-213-03 | Information Disclosure | telemetry/ledger | medium | mitigate | Emit bounded ids/count/source only; never raw entitlement payloads. |
| T-213-SC | Tampering | package installs | high | mitigate | No package install occurs; COVERAGE.md uses the already-audited Phase-212 dependency. |
</threat_model>

## Artifacts this phase produces

- `Accrue.Processor.list_active_entitlements/2` callback/facade and optional-callback entry.
- `Accrue.Processor.Fake.State.entitlements` field.
- `Accrue.Processor.Fake.put_entitlements/2` and Fake callback/handler.
- `Accrue.Entitlements.StripeSync.refresh/2`.
- `Accrue.Entitlements.Reconcile` shared writer and pull/webhook input contracts.
- Pull provenance under `EntitlementSummary.data["_accrue"]`.
- `accrue/test/accrue/entitlements/stripe_sync_refresh_test.exs`.

<verification>
Run both task commands, then `cd accrue && mix compile --warnings-as-errors && mix test test/accrue/entitlements test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/webhook/wr05_concurrency_test.exs test/property/entitlement_summary_monotonic_property_test.exs`.
</verification>

<success_criteria>
The opt-in Fake-backed pull path writes a complete advisory snapshot, disabled mode performs no I/O, idempotent refreshes do not duplicate ledger entries, and webhook/pull concurrency preserves the newest snapshot plus the real Stripe event watermark.
</success_criteria>

<output>
Create `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-01-SUMMARY.md` when done.
</output>

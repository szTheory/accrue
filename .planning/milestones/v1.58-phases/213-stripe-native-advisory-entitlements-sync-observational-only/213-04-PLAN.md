---
phase: 213-stripe-native-advisory-entitlements-sync-observational-only
plan: "04"
type: execute
wave: 3
depends_on:
  - 213-01
  - 213-02
  - 213-03
files_modified:
  - accrue/lib/accrue/processor.ex
  - accrue/test/accrue/processor/optional_entitlements_callback_test.exs
  - accrue/lib/accrue/entitlements/reconcile.ex
  - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
autonomous: true
gap_closure: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-05
must_haves:
  truths:
    - "Per D-01/D-02, configuring an adapter that omits the optional list_active_entitlements/2 callback returns a typed unsupported-operation error instead of raising UndefinedFunctionError."
    - "Per D-11, two distinct entitlement-summary webhooks for one customer in the same created second converge to one deterministic winner selected by the event-id tie key, independent of arrival order."
    - "The reducer pre-check and Ecto conflict predicate use the same timestamp-plus-event-id ordering rule, while strictly older events remain stale and pull writes retain the D-11 watermark contract."
  artifacts:
    - path: accrue/lib/accrue/processor.ex
      provides: "optional-callback-safe list_active_entitlements/2 facade"
      contains: "function_exported?"
    - path: accrue/test/accrue/processor/optional_entitlements_callback_test.exs
      provides: "adapter-without-callback regression proof"
    - path: accrue/lib/accrue/entitlements/reconcile.ex
      provides: "shared deterministic summary ordering policy"
      contains: "check_stale"
    - path: accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
      provides: "distinct same-second changed-payload/event-id regression proof"
  key_links:
    - from: accrue/lib/accrue/processor.ex
      to: "configured Accrue.Processor adapter"
      via: "function_exported?/3 guards optional callback dispatch before apply"
      pattern: "function_exported\\?.*:list_active_entitlements.*2"
    - from: accrue/lib/accrue/entitlements/reconcile.ex
      to: accrue_billing_entitlement_summaries
      via: "the reducer pre-check and ON CONFLICT WHERE compare the same synced_at/event-id total-order key"
      pattern: "last_stripe_event_id"
    - from: accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
      to: accrue/lib/accrue/entitlements/reconcile.ex
      via: "opposite-arrival-order same-second events assert the same payload and watermark winner"
      pattern: "same-second|same timestamp"
  prohibitions:
    - requirement_id: SYNC-02
      category: safety
      status: resolved
      verification: test
      statement: "Advisory entitlement state must never become an input to a grant, resolver, or guard decision while these gaps are closed."
    - requirement_id: SYNC-01
      category: transparency
      status: resolved
      verification: test
      statement: "An adapter that lacks the optional entitlement callback must not be reported as an empty successful Stripe snapshot; callers receive an explicit typed unsupported result."
  flagged_assumptions:
    - "SYNC-01 unclassified probe: the typed unsupported result follows the existing processor convention {:error, %Accrue.APIError{code: \"unsupported_operation\", http_status: 501}} and names the missing callback and configured adapter without exposing secrets."
    - "SYNC-02 unclassified probe: these fixes modify only advisory fetch/reconciliation behavior and do not add any dependency from the local grant path to StripeSync, Reconcile, or EntitlementSummary."
    - "SYNC-03 concurrency probe: the database ON CONFLICT predicate remains the atomic arbiter; timestamp plus bytewise event-id ordering guarantees the same winner when same-second events are interrupted, retried, or processed in parallel."
    - "SYNC-04 unclassified probe: the completed will-not-build closure for fetch_entitled/2 remains unchanged; no network-backed authorization predicate is introduced."
    - "SYNC-05 unclassified probe: regressions use a local adapter module and Fake webhook fixtures only, run without live Stripe or Chrome, and serialize process-global processor configuration with async: false."
---

<objective>
Close the two verification failures in the shipped advisory entitlement sync without reopening completed Phase 213 scope.

Purpose: Make the optional Processor contract source-compatible in practice and make same-second webhook reconciliation deterministic rather than silently lossy.
Output: A guarded Processor facade, one typed-unsupported regression, a single timestamp/event-id ordering policy, and changed-payload same-second webhook regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-CONTEXT.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-01-SUMMARY.md
@accrue/lib/accrue/processor.ex
@accrue/lib/accrue/entitlements/reconcile.ex
@accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
</context>

<assumption_delta_decision>
Primary noun: Stripe-native advisory entitlement sync. Select **no-change**: closing callback dispatch and ordering defects preserves the existing opt-in advisory observation; it neither promotes Stripe data to grant authority nor adds a second entitlement identity alongside the canonical local plan-to-feature map.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Return typed unsupported for an adapter without the optional entitlement callback</name>
  <files>accrue/lib/accrue/processor.ex, accrue/test/accrue/processor/optional_entitlements_callback_test.exs</files>
  <read_first>accrue/lib/accrue/processor.ex; accrue/lib/accrue/errors.ex; accrue/test/accrue/processor/behaviour_test.exs; accrue/test/accrue/processor/capabilities_test.exs; .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md</read_first>
  <behavior>
    - "A configured adapter module that does not export list_active_entitlements/2 yields {:error, %Accrue.APIError{code: \"unsupported_operation\", http_status: 501}}."
    - "The error message identifies list_active_entitlements/2 and the configured adapter, but contains no customer id, options, or secret-bearing processor payload."
    - "An adapter that exports the callback is still invoked once and its success/error tuple is returned unchanged."
  </behavior>
  <action>Write the regression with a local adapter module that intentionally omits `list_active_entitlements/2`, saves/restores `config :accrue, :processor`, and runs `async: false`. Per D-01/D-02 change `Accrue.Processor.list_active_entitlements/2` to bind `adapter = __impl__()`, guard dispatch with `function_exported?(adapter, :list_active_entitlements, 2)`, and return the project-standard typed unsupported tuple when absent: `%Accrue.APIError{code: "unsupported_operation", http_status: 501}` with a bounded message naming the callback and adapter. Do not return `{:ok, []}` because that would misrepresent unsupported capability as a valid empty diagnostic snapshot. Keep the callback optional and preserve the existing argument guards, callback result type, Stripe/Fake implementations, and metadata fallback.</action>
  <reversibility rating="reversible">The guard changes one facade dispatch branch behind the already-published optional callback contract.</reversibility>
  <acceptance_criteria>The callback-omitting adapter produces a typed unsupported result without an exception, and a callback-implementing adapter still delegates unchanged.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/optional_entitlements_callback_test.exs test/accrue/processor/stripe_entitlements_contract_test.exs</automated>
  </verify>
  <done>D-02 compatibility is real at runtime: existing custom/non-Stripe adapters cannot crash merely because the advisory callback is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply one deterministic same-second webhook tie policy in reducer and persistence</name>
  <files>accrue/lib/accrue/entitlements/reconcile.ex, accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs</files>
  <read_first>accrue/lib/accrue/entitlements/reconcile.ex; accrue/lib/accrue/billing/entitlement_summary.ex; accrue/lib/accrue/webhook/default_handler.ex; accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs; accrue/test/accrue/webhook/wr05_concurrency_test.exs; accrue/test/property/entitlement_summary_monotonic_property_test.exs; .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md</read_first>
  <behavior>
    - "For timestamped webhooks, the ordering key is {synced_at/event created second, event id}; newer timestamps win, and equal timestamps use ascending bytewise event-id order so the lexicographically greater id wins."
    - "Two distinct same-second events with changed entitlement payload and ids evt_a then evt_b persist evt_b and its payload; replaying in the opposite order converges to the same evt_b payload."
    - "A repeat of the same timestamp and event id is unchanged/stale, and an equal-timestamp event with a lower id cannot replace the deterministic winner."
    - "Strictly older events remain stale; pull writes and timestamp-less webhook watermark carry-forward continue to satisfy D-11."
  </behavior>
  <action>Replace the old equal-time stale fixture with fail-first regressions using two real `StripeFixtures.entitlement_summary_event/2` payloads for one customer, identical `created`, distinct event ids, and different entitlement pairs/counts. Run both arrival orders (resetting the row between cases) and assert the identical winning row data, count, `last_stripe_event_ts`, and `last_stripe_event_id`; retain explicit duplicate/lower-id stale assertions. Per D-11 define one total order shared by the early reducer check and the database guard: compare the normalized incoming `synced_at` first, then compare non-nil timestamped webhook event ids by ASCII/bytewise order on equality. Capture `synced_at_from_event/1` once per webhook and pass that value to both `check_stale` and `write_summary`, so a timestamp-less event cannot observe two clock values. Update `check_stale` to compare the same stored key based on `COALESCE(row.synced_at, row.last_stripe_event_ts)` and the stored event id. Update the `on_conflict` WHERE predicate to accept strictly newer `EXCLUDED.synced_at`, or equal `synced_at` only when the incoming watermark event id is bytewise greater; make the SQL comparison use deterministic C/bytewise collation rather than the database locale. Preserve first-row/null handling, D-11 pull watermark carry-forward, material-change/ledger/telemetry behavior, the no-OCC single-statement upsert, and stale rescue. Do not treat arbitrary arrival order as the tie breaker.</action>
  <reversibility rating="costly">The total-order rule changes correctness-critical advisory cache convergence, but is code-only with no schema or grant-contract migration.</reversibility>
  <acceptance_criteria>Reducer and persistence enforce the same timestamp/event-id order; distinct changed-payload same-second events converge to the same winner in either arrival order without weakening older-event or pull/webhook protections.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/webhook/wr05_concurrency_test.exs test/property/entitlement_summary_monotonic_property_test.exs</automated>
  </verify>
  <done>Same-second Stripe summary events are no longer silently discarded by contradictory pre-check and upsert semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| configured adapter → Processor facade | Host-selected modules may omit optional callbacks; absence must become a bounded typed error rather than a runtime crash or false empty success. |
| webhook event → advisory cache | External timestamps, ids, and payloads contend for one customer row and require an atomic deterministic total order. |
| advisory cache → grant path | Cache correctness work must not create a read dependency from authorization into observational state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-213-10 | Denial of Service | `Processor.list_active_entitlements/2` | medium | mitigate | Guard the optional export and return bounded `Accrue.APIError` instead of raising. |
| T-213-11 | Tampering | `Reconcile.check_stale` and upsert conflict predicate | high | mitigate | Use one timestamp-plus-bytewise-event-id total order in both layers and prove opposite arrival orders converge. |
| T-213-12 | Elevation of Privilege | advisory cache boundary | high | mitigate | Modify no resolver/guard files and rerun the existing isolation script and grant-invariance test. |
| T-213-SC | Tampering | package installs | high | mitigate | No package-manager operation or dependency change occurs; existing `COVERAGE.md` remains valid. |
</threat_model>

## Artifacts this phase produces

- Modified `Accrue.Processor.list_active_entitlements/2` optional dispatch branch.
- New test-only module `Accrue.Processor.OptionalEntitlementsCallbackTest.AdapterWithoutEntitlements`.
- New `accrue/test/accrue/processor/optional_entitlements_callback_test.exs`.
- Modified internal webhook ordering helper(s) in `Accrue.Entitlements.Reconcile` using the timestamp/event-id total-order policy.
- Same-second distinct-event regression cases in `accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs`.
- No schema, migration, dependency, UI, public grant API, or API-coverage decision changes; Phase 213 `COVERAGE.md` remains valid as written.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Client-backed opt-in advisory refresh with no grant dependency | 01-04 | COVERED | 04 repairs verified compatibility and ordering defects without changing authority. |
| REQ | SYNC-01 | Client-backed refresh writes the advisory cache | 01, 02, 04 | COVERED | 04 makes the optional facade safe for callback-omitting adapters. |
| REQ | SYNC-02 | Off-by-default, observational-only, local map canonical | 01-04 | COVERED | 04 preserves and re-verifies isolation. |
| REQ | SYNC-03 | Isolation script covers client fetch/shared writer | 03 | COVERED | Completed guard remains unchanged and is rerun by overall verification. |
| REQ | SYNC-04 | Close fetch_entitled/2 ambiguity | 03 | COVERED | Completed will-not-build decision is unchanged. |
| REQ | SYNC-05 | Fake/Test-only cache, grant, and default-off proof | 01-04 | COVERED | 04 adds local-only regression tests. |
| RESEARCH | processor seam | Optional callback plus typed, fakeable facade | 01, 02, 04 | COVERED | 04 closes the runtime optionality hole. |
| RESEARCH | reconciliation | Shared atomic pull/webhook writer with monotone ordering | 01, 04 | COVERED | 04 supplies the missing equal-second total order. |
| RESEARCH | isolation / no live service | No grant edge; deterministic local tests only | 03, 04 | COVERED | Existing merge gate remains part of overall verification. |
| RESEARCH | worker / SDK / provenance | Existing queue, exhaustive stream, JSON provenance, no migration | 01, 02 | COVERED | No gap or change required. |
| CONTEXT | D-01, D-02 | Optional complete-list Processor callback | 01, 02, 04 | COVERED | 04 cites and implements runtime-safe optional dispatch. |
| CONTEXT | D-03, D-04 | SDK confined to Stripe adapter; Fake seam | 01, 02 | COVERED | No gap or change required. |
| CONTEXT | D-05, D-06 | Public refresh contract and config-off no-I/O | 01 | COVERED | No gap or change required. |
| CONTEXT | D-07, D-08, D-10 | Existing-queue worker; no top-level/UI/scheduler surfaces | 02 | COVERED | Deferred surfaces remain excluded. |
| CONTEXT | D-09 | Bounded sync/summary telemetry | 01 | COVERED | Ordering fix preserves telemetry behavior. |
| CONTEXT | D-11, D-12, D-13 | Composable atomic ordering, shared writer, no migration | 01, 04 | COVERED | 04 closes the authoritative same-second failure. |
| CONTEXT | D-14 | Reject Stripe-backed entitlement predicate | 03 | COVERED | No gap or change required. |
| CONTEXT | D-15 | Isolation guard covers fetch/reconciler symbols | 03 | COVERED | Existing red-path proof remains green. |

<verification>
Run both task commands, then run:

`cd accrue && mix compile --warnings-as-errors && mix test test/accrue/processor/optional_entitlements_callback_test.exs test/accrue/processor/stripe_entitlements_contract_test.exs test/accrue/entitlements/stripe_sync_refresh_test.exs test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/webhook/wr05_concurrency_test.exs test/property/entitlement_summary_monotonic_property_test.exs test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs && cd .. && bash scripts/ci/verify_entitlement_sync_isolation.sh`

The phase is ready for re-verification only when the callback-omission regression returns the typed error, both same-second arrival orders converge to the same row, prior ordering/property tests remain green, and the observational-only merge gate still passes.
</verification>

<success_criteria>
Both authoritative verification gaps are closed: optional adapters cannot crash the facade, and distinct same-second webhook summaries converge under one reducer/database policy while D-01/D-11 grant isolation remains intact.
</success_criteria>

<output>
Create `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-04-SUMMARY.md` when done.
</output>

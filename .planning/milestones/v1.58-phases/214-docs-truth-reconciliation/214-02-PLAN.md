---
phase: 214-docs-truth-reconciliation
plan: "02"
type: execute
wave: 2
depends_on:
  - "214-01"
files_modified:
  - accrue/CHANGELOG.md
  - accrue_admin/CHANGELOG.md
  - accrue_portal/CHANGELOG.md
  - accrue/guides/release-notes.md
  - scripts/ci/verify_release_notes_contract.sh
  - accrue/test/accrue/docs/release_notes_contract_test.exs
  - accrue/lib/accrue/entitlements/stripe_sync.ex
  - accrue/lib/accrue/processor.ex
  - accrue/lib/accrue/processor/fake.ex
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "Core, admin, and portal changelogs have correctly scoped Unreleased entries, while numbered releases and package versions remain Release Please-owned (DOCS-03, D-05, D-06, D-08)."
    - "The plain-language next-release 1.5.0 story links all three package changelogs and distinguishes core capability from admin/portal compatibility (D-07)."
    - "Exactly the four supported Phase 213 public contract surfaces carry `@doc since: \"1.5.0\"`; internal plumbing remains unbadged (D-09, D-10, D-11, D-12)."
    - "Existing release-note and package-doc verifier families reject missing three-package discoverability, missing next-release truth, stale metadata, and accidental authority/version claims (D-16, D-17, D-18)."
  artifacts:
    - path: "accrue/CHANGELOG.md"
      provides: "Substantive Unreleased core release contract"
    - path: "accrue_admin/CHANGELOG.md"
      provides: "Compatibility-only admin Unreleased note"
    - path: "accrue_portal/CHANGELOG.md"
      provides: "Compatibility-only portal Unreleased note"
    - path: "accrue/guides/release-notes.md"
      provides: "Plain-language linked 1.5.0 release story"
    - path: "accrue/lib/accrue/processor.ex"
      provides: "Rendered since metadata on callback and facade surfaces"
    - path: "scripts/ci/verify_release_notes_contract.sh"
      provides: "Three-package and next-release discoverability gate"
  key_links:
    - from: "accrue/guides/release-notes.md"
      to: "three package CHANGELOG.md files"
      via: "explicit package-local changelog links and ownership language"
    - from: "scripts/ci/verify_release_notes_contract.sh"
      to: "accrue/guides/release-notes.md"
      via: "fixed next-release and portal-link assertions"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "StripeSync.refresh/2, Processor callback/facade, and Fake.put_entitlements/2"
      via: "exact supported-surface metadata assertions and internal-surface exclusions"
  assumptions:
    - requirement_id: DOCS-01
      status: unresolved
      flag: "Spec-less edge probe classified this requirement as unclassified; Plan 01 owns its scoped stack-version boundary and Plan 02 must not introduce a conflicting version claim."
    - requirement_id: DOCS-02
      status: unresolved
      flag: "Spec-less edge probe classified this requirement as unclassified; Plan 01 owns its shipped/observational boundary and Plan 02 release prose must preserve it."
    - requirement_id: DOCS-03
      status: unresolved
      flag: "Spec-less edge probe classified this requirement as unclassified; this plan treats missing linked-package ownership, missing next-release story, and mixed public metadata as explicit contract edges."
  prohibitions:
    - requirement_id: DOCS-02
      category: transparency
      status: unresolved
      statement: "MUST NOT present Stripe-native advisory data as Accrue grant authority or imply that it changes entitlement, plug, or LiveView guard decisions."
    - requirement_id: DOCS-01
      category: transparency
      status: unresolved
      statement: "MUST NOT rewrite dated phase, seed, archive, review, or verification evidence merely because it records an older dependency pin or deferred status."
    - requirement_id: DOCS-03
      category: transparency
      status: unresolved
      statement: "MUST NOT claim an admin- or portal-owned workflow, API, grant authority, numbered release, or package version that has not shipped."
---

<objective>
Complete DOCS-03 by reconciling package release truth and ExDoc availability metadata with the executable current-truth contract established in Plan 01.

Purpose: Make the next linked release discoverable and exact without bypassing Release Please or turning internal Phase 213 plumbing into supported API.
Output: Three scoped Unreleased entries, a linked plain-language 1.5.0 story, exact public `since` metadata, and focused verifier fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md
@.planning/phases/214-docs-truth-reconciliation/214-RESEARCH.md
@.planning/phases/214-docs-truth-reconciliation/214-PATTERNS.md
@.planning/phases/214-docs-truth-reconciliation/214-VALIDATION.md
@.planning/phases/214-docs-truth-reconciliation/214-01-SUMMARY.md
@RELEASING.md
@release-please-config.json
@.release-please-manifest.json
</context>

<assumption_delta_decision>
Primary noun: local plan→feature grant authority.
Decision: no-change.
Rationale: This phase documents an already-shipped optional advisory read seam; it neither promotes Stripe state nor changes the local grant identity.
</assumption_delta_decision>

<schema_gate>
No schema-relevant files occur in this release-documentation and ExDoc-metadata plan; no schema push task applies.
</schema_gate>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write and enforce package-owned Unreleased truth</name>
  <reversibility rating="costly">Release prose is source-controlled, but changing published package ownership or availability claims requires coordinated corrections across three linked packages.</reversibility>
  <files>accrue/CHANGELOG.md, accrue_admin/CHANGELOG.md, accrue_portal/CHANGELOG.md, scripts/ci/verify_release_notes_contract.sh, accrue/test/accrue/docs/release_notes_contract_test.exs</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-01..D-12 and exact release ownership
    - .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md — dependency bump facts
    - .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md — shipped sync/isolation facts
    - RELEASING.md — Unreleased drain and Release Please single-writer rules
    - release-please-config.json — linked three-package release ownership
    - .release-please-manifest.json — current published 1.4.0 line
    - accrue/CHANGELOG.md — add the substantive core `## Unreleased` section
    - accrue_admin/CHANGELOG.md — add compatibility-only `## Unreleased`
    - accrue_portal/CHANGELOG.md — add compatibility-only `## Unreleased`
    - scripts/ci/verify_release_notes_contract.sh — extend the existing release ownership and Release Please boundary gate
    - accrue/test/accrue/docs/release_notes_contract_test.exs — prove changelog ownership and numbered-section regressions fail through ROOT_DIR fixtures
  </read_first>
  <behavior>
    - Test 1: all three changelogs place `## Unreleased` before their latest numbered release; core contains the dependency/advisory-sync story while admin and portal contain compatibility-only notes.
    - Test 2: removing a package's Unreleased section, moving it below a numbered release, or replacing the core/admin/portal ownership split with the wrong package story fails with `verify_release_notes_contract:`.
    - Test 3: adding a manually numbered 1.5.0 heading to any package changelog fails with the stable verifier prefix.
    - Test 4: changing any package `@version` from the pre-task linked 1.4.0 constants is rejected by the task's scoped git-diff gate.
  </behavior>
  <action>
    Start RED by extending the existing ROOT_DIR-backed release-note fixtures to cover missing/misplaced Unreleased sections, the core-versus-companion ownership split, and Release Please's numbered-section boundary. Insert `## Unreleased` immediately below each changelog title per D-05, D-06, and D-08. The core entry must record the declared `lattice_stripe ~> 2.0` bump; optional/default-off advisory refresh; supported refresh, processor callback/facade, and Fake test helper; shared reconcile/isolation proof; closed `fetch_entitled/2`; and unchanged local grant authority. Admin and portal entries must be short compatibility notes only and must not claim package-owned workflow/API/authorization behavior. Extend `verify_release_notes_contract.sh` per D-16..D-18 so its live and fixture runs deterministically enforce Unreleased placement, substantive core tokens, compatibility-only admin/portal tokens, and Release Please ownership of numbered headings. Do not edit any package `@version`, create a numbered changelog section, or hand-edit untracked/generated `accrue/doc/release-notes.md`.
  </action>
  <acceptance_criteria>
    - Source assertion: all three changelogs contain a top-level `## Unreleased` before the latest numbered release.
    - Source assertion: core lists the bump, advisory refresh, supported contracts, isolation/reconcile proof, fetch closure, and never-a-gate semantics.
    - Source assertion: admin and portal entries are compatibility-only and claim no new package-owned API/workflow/grant authority.
    - Source assertion: no package changelog contains a manually created numbered `1.5.0` heading.
    - Scope assertion: all three package `@version` values and existing numbered changelog headings are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/release_notes_contract_test.exs &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_release_notes_contract.sh &amp;&amp; git diff --check -- accrue/CHANGELOG.md accrue_admin/CHANGELOG.md accrue_portal/CHANGELOG.md scripts/ci/verify_release_notes_contract.sh accrue/test/accrue/docs/release_notes_contract_test.exs &amp;&amp; git diff --quiet -- accrue/mix.exs accrue_admin/mix.exs accrue_portal/mix.exs</automated>
  </verify>
  <done>Every linked package has correctly owned Unreleased truth, the semantic ownership split is enforced by red fixtures, and package versions/numbered sections remain Release Please-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make release-note discoverability regressions fail red</name>
  <reversibility rating="reversible">The existing release-note verifier and isolated fixtures can be locally extended or reverted without changing production behavior.</reversibility>
  <files>accrue/guides/release-notes.md, scripts/ci/verify_release_notes_contract.sh, accrue/test/accrue/docs/release_notes_contract_test.exs</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-07, D-08, D-16..D-18
    - accrue/guides/release-notes.md — add the portal changelog link and canonical hand-authored 1.5.0 story
    - scripts/ci/verify_release_notes_contract.sh — extend existing ROOT_DIR/fail/section-scanner contract
    - accrue/test/accrue/docs/release_notes_contract_test.exs — extend its temporary-tree fixture pattern
    - accrue/CHANGELOG.md — substantive core ownership target
    - accrue_admin/CHANGELOG.md — compatibility-only ownership target
    - accrue_portal/CHANGELOG.md — compatibility-only ownership target
  </read_first>
  <behavior>
    - Test 1: the live next-release guide with all three package links and the 1.5.0 story passes.
    - Test 2: removing the portal changelog link from a temporary fixture fails with `verify_release_notes_contract:`.
    - Test 3: removing the next-release 1.5.0 story from a temporary fixture fails with the same stable prefix.
    - Test 4: current package versions may remain 1.4.0 while the next-release story is independently required.
  </behavior>
  <action>
    Start RED by adding two isolated fixture mutations: one removes the portal changelog link and one removes the next-release 1.5.0 story. In the hand-authored release guide, add the portal changelog beside the existing core/admin links and add the `1.5.0` next-release story that allocates substantive capability to core and compatibility to admin/portal per D-07. Extend `verify_release_notes_contract.sh` to require those two contracts without deriving the story only from the current package `@version`, because D-08 keeps main on 1.4.0 until Release Please. Preserve the existing `ROOT_DIR` override and failure prefix, use the existing section-scanner/helper style, and finish GREEN with the focused ExUnit file and direct shell verifier. Do not create a third release verifier or new CI step.
  </action>
  <acceptance_criteria>
    - Test assertion: both new mutations produce non-zero status and the stable release verifier prefix.
    - Behavior assertion: a clean live repo passes while package `@version` values remain unchanged.
    - Source assertion: the verifier explicitly checks portal changelog discoverability and the `1.5.0` story independently.
    - CLI assertion: focused release-note ExUnit tests and direct shell verifier both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/release_notes_contract_test.exs &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_release_notes_contract.sh</automated>
  </verify>
  <done>The existing release-note gate non-vacuously protects three-package discoverability and next-release truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stamp exactly the supported Phase 213 contracts with since 1.5.0</name>
  <reversibility rating="one-way">D-09/D-10 define already-approved published API availability metadata; changing it after release would contradict the public ExDoc contract, so the rating is retained without re-asking the locked decision.</reversibility>
  <files>accrue/lib/accrue/entitlements/stripe_sync.ex, accrue/lib/accrue/processor.ex, accrue/lib/accrue/processor/fake.ex, scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-09..D-12 exact badge allowlist/exclusions
    - accrue/lib/accrue/entitlements/stripe_sync.ex — correct `refresh/2` metadata and leave summary/internal paths unbadged
    - accrue/lib/accrue/processor.ex — add metadata before the callback and separately before the public facade
    - accrue/lib/accrue/processor/fake.ex — add metadata only to `put_entitlements/2`
    - scripts/ci/verify_package_docs.sh — add exact positive and scoped exclusion checks
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — add stale/missing/over-badged fixture regressions
  </read_first>
  <behavior>
    - Test 1: `StripeSync.refresh/2` renders since 1.5.0, replacing its stale family value.
    - Test 2: both rendered `Processor.list_active_entitlements/2` surfaces carry since 1.5.0.
    - Test 3: `Processor.Fake.put_entitlements/2` carries since 1.5.0 as supported adopter test DX.
    - Test 4: summary, metadata helper, adapter, Reconcile, and worker internals remain without a since badge.
    - Test 5: a stale, missing, or extra internal badge in a temporary fixture fails the package-doc verifier.
  </behavior>
  <action>
    Add focused failing fixtures first. Then per D-09 and D-10, place `@doc since: "1.5.0"` after the existing doc block and before `@spec` for `StripeSync.refresh/2`, the public `Processor.list_active_entitlements/2` facade, and `Processor.Fake.put_entitlements/2`; add the separate same-value metadata immediately before the `@callback list_active_entitlements/2`. Per D-11 and D-12, keep active-entitlement metadata helpers, `StripeSync.summary_for_customer/1`, adapter implementations, Reconcile writers, and the worker callback hidden/unbadged. Extend the existing package-doc verifier with exact supported-region metadata assertions and scoped exclusion checks, backed by red fixtures; do not badge technical callability or alter runtime behavior. Finish with D-19's scoped, explanatory cold-read: inspect only current public/planning surfaces as current truth, classify matches in dated phase/archive/seed evidence as historical, and report contradictions rather than rewriting history.
  </action>
  <acceptance_criteria>
    - Source assertion: exactly four rendered contract locations carry `@doc since: "1.5.0"` for the named D-10 surfaces.
    - Source assertion: `StripeSync.refresh/2` no longer advertises the older family value.
    - Source assertion: every D-11 internal surface remains hidden/unbadged.
    - Behavior assertion: no function body, spec signature, callback optionality, or runtime dispatch changes.
    - Test assertion: missing/stale supported metadata and an extra internal badge are each detected by focused verifier fixtures.
    - CLI assertion: focused package-doc tests, direct package-doc verifier, and warnings-as-errors compile all pass.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/package_docs_verifier_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>ExDoc availability truth is exact on the four supported surfaces, protected against stale/missing/over-badged drift, and runtime behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Changelog/release prose → adopter upgrade decisions | Incorrect ownership or availability claims can cause adopters to depend on nonexistent package APIs. |
| ExDoc metadata → public support contract | A since badge makes a compatibility promise; internal plumbing must not become supported accidentally. |
| Release Please → main-branch documentation | Manual numbered sections or version bumps would conflict with the automated linked-release writer. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-05 | Spoofing | Admin/portal Unreleased entries | high | mitigate | Restrict entries to compatibility-only wording and verify that substantive workflow/API/grant ownership stays in core. |
| T-214-06 | Tampering | ExDoc `since` metadata | high | mitigate | Assert the exact four D-10 locations and red-test stale, missing, and internal over-badging. |
| T-214-07 | Repudiation | Release Please ownership | medium | mitigate | Preserve package versions and numbered histories; add only Unreleased content and verify scoped diffs. |
| T-214-08 | Elevation of Privilege | Release story authorization wording | high | mitigate | Reuse Plan 01's executable D-01 contract so release prose cannot imply advisory Stripe sync controls grants. |
</threat_model>

## Artifacts this phase produces

This plan creates no new runtime function, callback, symbol, CLI flag, schema, dependency, or external API integration.

- New metadata annotations: four `@doc since: "1.5.0"` locations—`StripeSync.refresh/2`, `Processor.list_active_entitlements/2` callback, `Processor.list_active_entitlements/2` facade, and `Processor.Fake.put_entitlements/2`.
- New changelog sections: `## Unreleased` in `accrue/CHANGELOG.md`, `accrue_admin/CHANGELOG.md`, and `accrue_portal/CHANGELOG.md`.
- New release-note content: portal changelog link and next-release `1.5.0` story in `accrue/guides/release-notes.md`.
- Extended verifier contract: portal-link and next-release assertions in `scripts/ci/verify_release_notes_contract.sh`; exact supported-metadata and internal-exclusion assertions in `scripts/ci/verify_package_docs.sh`.
- Extended test/verifier contract: red fixtures in `accrue/test/accrue/docs/release_notes_contract_test.exs` and `accrue/test/accrue/docs/package_docs_verifier_test.exs`.
- Generated output: none tracked. If a local ExDoc build emits `accrue/doc/release-notes.md`, it is regenerated from the guide and is not hand-edited or added unless repository tracking policy already requires it.
- Planning/output paths: `.planning/phases/214-docs-truth-reconciliation/214-02-SUMMARY.md` on completion.

## Probe and prohibition treatment

- DOCS-01 unresolved edge: Plan 02 treats Plan 01's declared-version contract as a pre-existing invariant and forbids contradictory release claims.
- DOCS-02 unresolved edge: Plan 02 inherits the shipped/default-off/advisory/local-grant boundary and enforces it in release prose.
- DOCS-03 unresolved edge: Tasks 1-3 explicitly cover linked-package ownership, next-release discoverability, exact public metadata, and internal exclusions.
- Recalled prohibitions remain descriptor-less and flagged in `must_haves.prohibitions`; none is auto-dismissed or given a fabricated enforcement descriptor.

<verification>
Run the two focused ExUnit documentation-contract files, both shell verifiers, warnings-as-errors compile, the support-matrix verifier, and the entitlement isolation verifier. Finish with a scoped cold-read grep over current docs/changelogs/planning mirrors and classify any dated phase/archive/seed hit as historical rather than rewriting it.
</verification>

<success_criteria>
- DOCS-03 is complete across package histories, plain-language release truth, public metadata, support/adoption mirrors, and planning mirrors.
- Package versions and numbered changelog sections remain Release Please-owned.
- Exactly the D-10 public surfaces are badged; all D-11 internals remain unbadged.
- Red-path fixtures prove every new release/metadata guard has teeth.
- All unresolved probe edges and recalled prohibitions remain explicit, with no silent drop.
</success_criteria>

<output>
Create `.planning/phases/214-docs-truth-reconciliation/214-02-SUMMARY.md` when done.
</output>

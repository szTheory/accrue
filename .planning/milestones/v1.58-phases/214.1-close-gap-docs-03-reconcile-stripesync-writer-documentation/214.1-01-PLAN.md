---
phase: 214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/entitlements/stripe_sync.ex
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "DOCS-03 / D-01..D-05: StripeSync publicly names webhook handling and client-backed pull refresh as opt-in writers through the shared reconciler while preserving local plan-to-feature mapping as the sole grant authority."
    - "DOCS-03 / D-06..D-08: the existing package-documentation verifier semantically accepts the correct writer story and rejects both stale read-only and webhook-exclusive claims through its production ROOT_DIR path."
    - "D-09: the focused package-doc test, live documentation verifier, warning-free compile, and entitlement-isolation verifier all pass without a repository-wide suite."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/stripe_sync.ex"
      provides: "Concise public writer-provenance and authorization-boundary contract"
      contains: "Accrue.Entitlements.Reconcile"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Merge-blocking semantic writer-provenance assertions in the existing documentation lane"
      contains: "stripe_sync_ex"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ROOT_DIR-backed regression proving the production verifier rejects stale writer wording"
      contains: "run_verifier"
  key_links:
    - from: "accrue/lib/accrue/entitlements/stripe_sync.ex"
      to: "accrue/lib/accrue/entitlements/reconcile.ex"
      via: "public prose mirrors refresh/2 and write_pull/4 plus the webhook write_webhook/4 convergence"
      pattern: "Accrue\\.Entitlements\\.Reconcile"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/lib/accrue/entitlements/stripe_sync.ex"
      via: "independent semantic presence and stale-claim absence assertions"
      pattern: "stripe_sync_ex"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "ROOT_DIR temporary fixture executes the same production script used by CI"
      pattern: "ROOT_DIR"
---

<objective>
Close the executable DOCS-03 writer-documentation gap by correcting the complete StripeSync one-way-dependency explanation and ratcheting that truth in the existing package-doc verification family (D-01..D-09).

Purpose: Make the public contract match the shipped webhook/pull convergence without weakening the permanent advisory-only authorization boundary.
Output: Correct StripeSync moduledoc prose, semantic production verifier assertions, and a focused stale-writer negative fixture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.58-v1.58-MILESTONE-AUDIT.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-CONTEXT.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-RESEARCH.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-PATTERNS.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-VALIDATION.md
@accrue/lib/accrue/entitlements/stripe_sync.ex
@accrue/lib/accrue/entitlements/reconcile.ex
@accrue/lib/accrue/webhook/default_handler.ex
@scripts/ci/verify_package_docs.sh
@scripts/ci/verify_entitlement_sync_isolation.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
</context>

<assumption_delta_decision>
Primary noun: local plan-to-feature grant authority.
Decision: no-change. The deterministic orchestrator scan returned detected:false, so no architecture checkpoint applies; this phase documents and tests the already-shipped writer paths without changing runtime authorization.
</assumption_delta_decision>

<api_coverage>
The initial source-only detector returned detected:false. The final PLAN-plus-ROADMAP scan fired on explanatory planning prose, but the scope still adds no external API surface. `COVERAGE.md` records the accepted reasoned no-integration declaration rather than fabricating capability rows.
</api_coverage>

<schema_gate>
No schema-relevant paths are modified. The moduledoc, Bash verifier, and ExUnit fixture require no schema push task.
</schema_gate>

<reversibility_gate>
D-05 remains rated one-way because reversing the published authorization direction would break the v1.x grant contract. No checkpoint is emitted: CONTEXT.md records D-05 as a locked user decision already made, so re-asking would violate decision fidelity; the rating is carried on the implementing tracer task.
</reversibility_gate>

<spec_less_probe>
No plain phase SPEC or machine-readable phase requirement set was available to the probe, so no probe-derived predicates were generated. DOCS-03 and locked CONTEXT decisions D-01..D-13 remain the authoritative scope; no edge or prohibition rows are invented.
</spec_less_probe>

<tasks>

<task type="tracer">
  <name>Task 1: Correct and enforce one writer-provenance path end to end</name>
  <reversibility rating="one-way">Per D-05, reversing the published seam-to-billing and never-gate-to-seam direction would break the v1.x authorization contract and contradict the merge-blocking isolation guarantee.</reversibility>
  <files>accrue/lib/accrue/entitlements/stripe_sync.ex, scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - .planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-CONTEXT.md — D-01..D-07 exact public wording, scope, and verifier ownership
    - .planning/v1.58-v1.58-MILESTONE-AUDIT.md — authoritative contradictory-writer evidence; read only and preserve byte-for-byte
    - accrue/lib/accrue/entitlements/stripe_sync.ex — stale complete paragraph plus the client-backed refresh entry point
    - accrue/lib/accrue/entitlements/reconcile.ex — shared write_pull/4 and write_webhook/4 same-row truth
    - accrue/lib/accrue/webhook/default_handler.ex — webhook call into the shared reconciler
    - scripts/ci/verify_package_docs.sh — existing helper vocabulary and stripe_sync_ex assertion region to extend
    - scripts/ci/verify_entitlement_sync_isolation.sh — independent executable authorization-direction contract that wording must preserve
  </read_first>
  <action>
    Replace the complete stale `## One-way dependency` paragraph in `StripeSync` per D-01, not just one phrase. In concise audience-facing prose, state per D-02/D-03 that enabling `stripe_native_sync: :advisory` permits both webhook handling and client-backed pull refresh to write the same advisory `Accrue.Billing.EntitlementSummary` row through `Accrue.Entitlements.Reconcile`; retain that the feature is off by default, diagnostic only, and never consulted for grant decisions. Preserve D-05's direction and the local plan-to-feature mapping as sole grant authority. Per D-04, do not expose timestamp, event-order, conflict-clause, watermark, or upsert internals.

    Extend the existing `stripe_sync_ex` region of `verify_package_docs.sh` per D-06/D-07. Use the established `require_fixed`, `require_regex`, and `require_absent_regex` helpers to assert independent semantic fragments for both writer paths, the shared reconciler/same advisory row, the opt-in/default-off diagnostic boundary, and the non-gate authority statement. Give the writer-provenance assertions a stable actionable label through existing `fail` output. Assert the two legacy ideas by concept with semantic absence regexes; do not freeze the full replacement paragraph, create another script, add a workflow lane, or change runtime code.
  </action>
  <acceptance_criteria>
    - D-01/D-02: the complete paragraph accurately names webhook and client-backed pull writers converging through `Accrue.Entitlements.Reconcile` on one advisory summary row.
    - D-03/D-05: the same paragraph applies the advisory opt-in to both paths and says neither path can influence grants; local plan-to-feature mapping remains sole authority.
    - D-04: public prose contains no persistence-ordering or conflict-resolution implementation detail.
    - D-06/D-07: the existing package-doc script enforces independent semantic facts and rejects both stale concepts without snapshotting exact prose.
    - Scope: no runtime behavior, package version, dependency, workflow, schema, admin/portal UI, or historical planning artifact is changed.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The corrected public writer story passes end to end through the existing production documentation contract while preserving the one-way grant-isolation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove stale writer wording fails the production verifier</name>
  <reversibility rating="reversible">The ROOT_DIR fixture is local regression coverage behind an already-established public contract.</reversibility>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh — production writer-provenance assertions and stable failure label produced by Task 1
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — existing tmp_dir!, seed_tmp_dir!, copy_fixture!, and run_verifier helpers
    - accrue/lib/accrue/entitlements/stripe_sync.ex — corrected paragraph/tokens that the focused fixture must mutate, never the checkout
    - .planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-VALIDATION.md — D-08 Wave 0 gap and locked focused D-09 command bundle
  </read_first>
  <behavior>
    - Test 1 (RED): a ROOT_DIR fixture that restores both stale writer implications exits nonzero through the production package-doc script.
    - Test 2: the failure output includes `[verify_package_docs]`, the StripeSync source path, and the stable actionable writer-provenance diagnostic.
    - Test 3: the unmodified live checkout passes the package-doc verifier.
    - Test 4: the independent entitlement-isolation script remains green, proving prose protection did not replace executable grant isolation.
  </behavior>
  <action>
    Add one focused test per D-08 using the existing `tmp_dir!`, `seed_tmp_dir!`, and `run_verifier/1` path. Mutate only the copied `accrue/lib/accrue/entitlements/stripe_sync.ex` so its corrected paragraph again implies internal read-only behavior and a sole webhook writer, then execute the production script with ROOT_DIR. Assert nonzero status plus the stable failure prefix, source path, and writer-provenance label; do not duplicate Bash contract logic in ExUnit. Start with the fixture against the pre-change contract to establish RED where practical, then complete GREEN against Task 1. Close per D-09 with exactly the focused ExUnit suite, live package-doc verifier, warning-free core compile, and entitlement-isolation script; do not expand to a repository-wide suite.
  </action>
  <acceptance_criteria>
    - D-08: a copied stale StripeSync source fails the real production verifier through ROOT_DIR with a stable actionable diagnostic.
    - The fixture changes neither the checkout source nor the historical milestone audit and cleans its temporary directory through the existing helper pattern.
    - D-09: the focused package-doc ExUnit suite, live shell verifier, warning-free compile, and entitlement-isolation verifier all exit 0.
    - No new verifier, CI lane, dependency, external service setup, schema file, or runtime entitlement code is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/package_docs_verifier_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_package_docs.sh &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>The production documentation verifier demonstrably rejects stale writer provenance, and all four locked focused closeout gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public StripeSync docs → adopter authorization expectations | Published prose can be mistaken for the grant contract; advisory cache state must never be represented as authorization state. |
| Repository source → merge-blocking package-doc verifier | Source wording becomes verifier input and must be checked semantically without brittle full-paragraph snapshots. |
| Test ROOT_DIR → production verifier | A temporary fixture redirects fixed repository paths and must not mutate the live checkout or bypass the actual CI script. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214.1-01 | Elevation of Privilege | StripeSync public contract | high | mitigate | D-02/D-03/D-05 wording plus semantic package-doc assertions and the independent entitlement-isolation verifier prevent advisory state from being represented or wired as grant authority. |
| T-214.1-02 | Tampering | Writer-provenance documentation | high | mitigate | Require both writer paths, shared reconciliation, advisory scope, and stale-claim absence; prove a ROOT_DIR mutation fails the production script. |
| T-214.1-03 | Repudiation | Historical v1.58 audit evidence | medium | mitigate | Exclude all historical artifacts from files_modified and preserve `.planning/v1.58-v1.58-MILESTONE-AUDIT.md` byte-for-byte for independent re-audit. |
| T-214.1-04 | Denial of Service | Documentation wording contract | low | accept | Semantic fragments intentionally permit audience-appropriate copy refinement while holding only behaviorally significant facts. |
</threat_model>

## Artifacts and symbols produced

- New file paths: none.
- New public/runtime symbols: none.
- Existing public artifact updated: `Accrue.Entitlements.StripeSync` moduledoc only; `refresh/2` behavior and signature remain unchanged.
- Existing verifier extended: `scripts/ci/verify_package_docs.sh`; no new script or CI lane.
- New regression case: one ROOT_DIR stale-writer mutation test in `Accrue.Docs.PackageDocsVerifierTest`; exact test/helper names follow the file's established naming style.
- Planning gate artifact: `.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/COVERAGE.md` records the reasoned external-API detector override; it is not an executor-modified product artifact.
- Planning output: `.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-01-SUMMARY.md`.

<verification>
Run only the locked focused D-09 bundle. Confirm the public semantic contract, its stale-source negative fixture, warning-free compilation, and independent gate-to-seam isolation all pass without modifying the milestone audit.
</verification>

<success_criteria>
- DOCS-03 writer provenance matches shipped runtime behavior and preserves advisory-only grant semantics.
- The existing package-doc verifier detects regression by semantic facts, not an exact paragraph snapshot.
- A focused ROOT_DIR fixture proves the production verifier rejects both stale implications with actionable output.
- All four D-09 commands pass; no repository-wide suite, live Stripe access, schema operation, dependency install, or new CI lane is required.
</success_criteria>

<output>
Create `.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-01-SUMMARY.md` when done.
</output>

---
phase: 214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation
plan: "03"
type: execute
wave: 2
depends_on:
  - 214.1-01
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
gap_closure: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "DOCS-03 / D-06..D-08: removing only the public StripeSync shared-reconciler claim makes the production package-doc verifier fail even though the implementation alias remains present."
    - "The production verifier reports the existing stable StripeSync writer-provenance diagnostic for the isolated public-prose regression."
    - "D-09: the focused package-doc verifier family, warning-free compile, live documentation verifier, and entitlement-isolation verifier pass after the false-positive gap is closed."
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "A StripeSync shared-reconciler assertion scoped to public moduledoc prose rather than the complete source file"
      contains: "missing shared reconciler"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ROOT_DIR regression fixture that removes only the prose reconciler occurrence and retains the implementation alias"
      contains: "run_verifier"
  key_links:
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "ROOT_DIR fixture executes the production verifier against a copied StripeSync source"
      pattern: "ROOT_DIR"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/lib/accrue/entitlements/stripe_sync.ex"
      via: "moduledoc- or one-way-dependency-scoped reconciler search excludes implementation aliases"
      pattern: "Accrue\\.Entitlements\\.Reconcile"
---

<objective>
Close the remaining DOCS-03 verifier false positive by binding the shared-reconciler requirement to StripeSync's public documentation and proving the exact regression through the existing ROOT_DIR test seam (D-06..D-09).

Purpose: Ensure an implementation alias cannot impersonate the public writer-provenance contract.
Output: A region-scoped production verifier assertion and a focused prose-only mutation fixture with a stable failure diagnostic.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-CONTEXT.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-VERIFICATION.md
@.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-01-SUMMARY.md
@scripts/ci/verify_package_docs.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
@accrue/lib/accrue/entitlements/stripe_sync.ex
</context>

<planner_assumptions>
The spec-less edge probe classified DOCS-03 as `unclassified` / `unresolved`. Planner assumption: the authoritative requirement remains the DOCS-03 text in `.planning/REQUIREMENTS.md`, refined by locked D-06..D-09 and the two explicit missing items in `214.1-VERIFICATION.md`; no additional SPEC-derived edge predicate is inferred or dropped. The probe found no UI, schema, external-API, or assumption-delta gate for this documentation/verifier-only closure.
</planner_assumptions>

<reversibility_gate>
Both edits are rated reversible: they change an internal CI assertion and its regression fixture without changing published APIs, runtime behavior, persistence, or the locked D-05 authorization contract. No checkpoint is required.
</reversibility_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make a prose-only reconciler regression fail end to end</name>
  <reversibility rating="reversible">The scoped assertion and fixture are local CI-contract edits that can be reverted without migration or public API breakage.</reversibility>
  <files>scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - .planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-VERIFICATION.md — authoritative failed truth and the two missing items; do not modify it
    - .planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-CONTEXT.md — locked D-06..D-09 verifier ownership, semantic-contract, fixture, and focused-closeout decisions
    - scripts/ci/verify_package_docs.sh — existing helper functions and DOCS-03 StripeSync writer-provenance assertion region
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — existing ROOT_DIR seeding, production-script execution, stale-writer test, cleanup, and diagnostic assertion patterns
    - accrue/lib/accrue/entitlements/stripe_sync.ex — public moduledoc region plus the implementation-level `Reconcile` alias that the new fixture must preserve
    - .planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-01-SUMMARY.md — shipped source/verifier/test baseline that this gap plan tightens
  </read_first>
  <behavior>
    - RED: a copied StripeSync source with only the public-prose `Accrue.Entitlements.Reconcile` occurrence removed still retains the implementation alias, yet the current production verifier incorrectly exits 0.
    - GREEN: the same copied source exits nonzero through `ROOT_DIR` and reports `[verify_package_docs]`, the StripeSync source path, and `missing shared reconciler`.
    - CONTROL: the copied fixture explicitly proves the implementation alias remains after mutation, so failure cannot be attributed to removing every reconciler token.
    - LIVE: the unmodified checkout passes the production package-doc verifier because its public moduledoc still contains the shared-reconciler claim.
  </behavior>
  <action>
    First add a focused ExUnit regression beside the existing stale StripeSync writer-provenance fixture, following its `tmp_dir!`, `seed_tmp_dir!`, copied-source, `run_verifier/1`, and cleanup patterns per D-08. In only the copied `stripe_sync.ex`, replace the public moduledoc's reconciler reference with prose that omits the module name; target a unique moduledoc or `## One-way dependency` occurrence so the source alias is untouched. Before invoking the verifier, assert the mutation occurred, assert the copied public prose no longer carries the module-qualified reconciler claim, and assert the implementation alias still exists. Run the focused case against the current verifier to establish RED, then retain the test as the regression proof. Assert nonzero status and the established failure prefix, copied StripeSync path, writer-provenance label, and `missing shared reconciler` detail; exercise the production Bash script through ROOT_DIR rather than duplicating its logic in ExUnit.

    Then tighten only the existing shared-reconciler assertion in `scripts/ci/verify_package_docs.sh` per D-06/D-07. Extract or search the `@moduledoc` body—or more narrowly the `## One-way dependency` paragraph—and require the reconciler module name inside that public region. Keep the current stable `StripeSync writer provenance (...): missing shared reconciler` diagnostic and all other independent semantic assertions intact. Ensure extraction/search failures propagate as verifier failures under `set -euo pipefail`; do not allow the implementation alias, comments, function bodies, or unrelated documentation regions to satisfy the claim. Do not freeze the complete paragraph, create a new verifier/helper lane, alter `stripe_sync.ex`, or modify any planning mirror or historical evidence.
  </action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/package_docs_verifier_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_package_docs.sh &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <acceptance_criteria>
    - D-06/D-07: the production package-doc verifier requires `Accrue.Entitlements.Reconcile` within StripeSync public moduledoc prose or its one-way-dependency paragraph, not anywhere in the source file.
    - D-08: a ROOT_DIR fixture removes only the public-prose reconciler occurrence, demonstrates the implementation alias remains, and receives a nonzero result from the real production verifier.
    - The negative fixture checks the stable failure prefix, StripeSync path, writer-provenance label, and `missing shared reconciler` diagnostic.
    - D-09: the complete focused package-doc ExUnit file, warning-free compile, live shell verifier, and entitlement-isolation verifier exit 0 after GREEN.
    - Scope: `accrue/lib/accrue/entitlements/stripe_sync.ex`, planning mirrors, summaries, `214.1-VERIFICATION.md`, and `.planning/v1.58-v1.58-MILESTONE-AUDIT.md` remain unmodified.
  </acceptance_criteria>
  <done>The verifier distinguishes public documentation from implementation code: loss of only the moduledoc reconciler claim fails with a stable diagnostic while the live documented source passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds no runtime, authentication, authorization, data, network, or external-service trust boundary. The only applicable integrity boundary is repository documentation flowing into a merge-blocking CI verifier through a test-controlled ROOT_DIR.

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214.1-03-01 | Tampering | StripeSync public-doc contract → package-doc verifier | medium | mitigate | Scope the reconciler requirement to the public documentation region and prove a prose-only ROOT_DIR mutation fails while the implementation alias remains. |
| T-214.1-03-02 | Tampering | ROOT_DIR regression fixture → live checkout | low | accept | The established temporary-root helpers copy inputs, mutate only the temporary StripeSync source, and clean up on exit; acceptance explicitly requires the live source and planning evidence remain untouched. |

No Spoofing, Repudiation, Information Disclosure, Denial of Service, or Elevation of Privilege threat applies to these documentation/verifier-only edits. No critical or high threat is introduced, so the configured high-severity block gate does not require a checkpoint.
</threat_model>

## Artifacts this phase produces

- New file paths: none.
- New public/runtime symbols: none.
- Existing verifier behavior updated: `scripts/ci/verify_package_docs.sh` scopes the StripeSync reconciler claim to public prose.
- New test case: one focused ROOT_DIR regression in `Accrue.Docs.PackageDocsVerifierTest`; use the file's established descriptive test naming convention and introduce no helper unless the existing local pattern cannot express the region-safe mutation.
- Execution summary path: `.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-03-SUMMARY.md`.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Prove StripeSync public webhook/pull shared-reconciler truth through the existing package-doc verifier family | 03 | COVERED | The tracer scopes the assertion to public prose and proves the exact false-positive regression. |
| REQ | DOCS-03 | Public/release/planning documentation stays mutually consistent under the stable-core rule | 03 | COVERED | This gap plan closes the remaining verifier-proof blocker without rewriting already-correct source or mirrors. |
| RESEARCH | — | Semantic fragment assertions in the existing verifier, exercised through ROOT_DIR | 03 | COVERED | The action preserves the existing lane and stable diagnostic. |
| RESEARCH | — | Focused package-doc verification; no dependency, UI, schema, runtime, or external service change | 03 | COVERED | Verification is limited to the focused ExUnit family and live shell verifier. |
| CONTEXT | D-06 | Extend only the existing package-doc verifier | 03 | COVERED | No new script or CI lane. |
| CONTEXT | D-07 | Require shared reconciler as a semantic public-doc fact | 03 | COVERED | Assertion is region-scoped rather than full-file or full-paragraph. |
| CONTEXT | D-08 | Production ROOT_DIR fixture proves relevant stale wording fails | 03 | COVERED | Fixture removes only the public-prose token and retains the alias. |
| CONTEXT | D-09 | Focused four-command closeout evidence | 03 | COVERED | Focused ExUnit, warning-free compile, live package-doc verification, and entitlement isolation are concrete automated gates. |
| CONTEXT | D-01..D-05 | Accurate public writer and one-way grant-authority contract | 01, 03 | COVERED | Plan 01 delivered the locked prose; Plan 03 protects its reconciler clause without editing it. |
| CONTEXT | D-10..D-13 | Current mirrors reconciled; historical evidence and audit boundary preserved | 02, 03 | COVERED | Plan 02 delivered reconciliation; Plan 03 explicitly excludes those artifacts from modification. |

Excluded without gap: all CONTEXT Deferred Ideas; runtime/UI/schema/external-API/assumption-delta work; and any SPEC-derived predicates beyond the explicitly acknowledged `unclassified` / `unresolved` DOCS-03 assumption.

<verification>
Run the locked D-09 bundle: the focused package-doc ExUnit file, warning-free core compile, live package-doc verifier, and entitlement-isolation verifier. Confirm the regression fixture proves the retained alias cannot satisfy the public-doc assertion and the live documented source remains accepted. Do not run a repository-wide suite or mutate planning/audit evidence.
</verification>

<success_criteria>
- Both missing items in `214.1-VERIFICATION.md` are implemented in the existing verifier/test family.
- The shared-reconciler assertion is limited to StripeSync public moduledoc prose or its one-way-dependency paragraph.
- The ROOT_DIR fixture removes only the public-prose occurrence, retains the implementation alias, and proves the production verifier fails with the stable diagnostic.
- The focused package-doc ExUnit family, warning-free compile, live production verifier, and entitlement-isolation verifier pass.
- No source documentation, runtime behavior, planning mirror, historical summary/report, or milestone audit is rewritten.
</success_criteria>

<output>
Create `.planning/phases/214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation/214.1-03-SUMMARY.md` when done.
</output>

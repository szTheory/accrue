---
phase: 214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn
plan: "02"
type: execute
wave: 2
depends_on: [214.2-01]
files_modified:
  - accrue/lib/accrue/entitlements/admin.ex
  - accrue/test/accrue/entitlements/admin_test.exs
  - accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "D-07 / D-15: the core diagnostic distinguishes disabled, not_observed, recorded, age_unknown, incomplete, and unavailable; a recorded empty list remains `0 entitlements observed`, never absence."
    - "D-08: local and advisory branch failures are independently contained, so one branch's failure never erases the other branch's truthful data."
    - "D-09: disabled mode performs zero advisory-table, processor, or network I/O even if an old row exists; enabled mode performs at most one owner-scoped row lookup."
    - "D-10 / D-16: provenance is pull only from the trusted stamp, webhook only from the event watermark, unavailable otherwise, and no state is derived from elapsed age."
    - "D-19: focused core tests cover every diagnostic branch, scalar normalization, complete lookup-key retention, and unchanged grant surfaces."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/admin.ex"
      provides: "Complete independently contained advisory diagnostic state machine"
      contains: "age_unknown"
    - path: "accrue/test/accrue/entitlements/admin_test.exs"
      provides: "Branch, provenance, normalization, and no-I/O unit tests"
      contains: "diagnostic_for_customer"
    - path: "accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs"
      provides: "Runtime proof that complete diagnostic consumption does not change grant surfaces"
      contains: "grant_surface"
  key_links:
    - from: "accrue/lib/accrue/entitlements/admin.ex"
      to: "accrue/lib/accrue/config.ex"
      via: "config decision precedes the advisory reader call"
      pattern: "stripe_native_sync\\?"
    - from: "accrue/lib/accrue/entitlements/admin.ex"
      to: "accrue/lib/accrue/billing/entitlement_summary.ex"
      via: "StripeSync reader returns one row whose fields are normalized without leaking the schema"
      pattern: "summary_for_customer"
    - from: "accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs"
      to: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      via: "grant-surface comparisons before and after advisory diagnostics"
      pattern: "grant_surface"
  prohibitions:
    - requirement_id: SYNC-02
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT collapse local and advisory errors into one shared failure state or translate an advisory failure into a local fail-closed grant result."
    - requirement_id: SYNC-02
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT collapse no persisted snapshot into an observed empty entitlement list, or use row existence as a success or freshness claim."
---

<objective>
Expand the tracer's core contract into the complete observational state model with independent failure containment, exact provenance, disabled no-I/O, and grant-invariance evidence (SYNC-02; D-07..D-10, D-15, D-16, D-19).

Purpose: Make every meaningful persisted/absent/failure condition diagnosable without allowing advisory evidence to acquire authority.
Output: Complete core diagnostic normalization and focused branch/isolation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-CONTEXT.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-RESEARCH.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-PATTERNS.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-01-SUMMARY.md
@accrue/lib/accrue/entitlements/admin.ex
@accrue/lib/accrue/entitlements/stripe_sync.ex
@accrue/lib/accrue/entitlements/reconcile.ex
@accrue/lib/accrue/billing/entitlement_summary.ex
@accrue/test/accrue/entitlements/admin_test.exs
@accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize every advisory snapshot state and provenance branch</name>
  <files>accrue/lib/accrue/entitlements/admin.ex, accrue/test/accrue/entitlements/admin_test.exs</files>
  <read_first>
    - .planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-CONTEXT.md — D-07..D-10, D-15, D-16, and D-19 exact branch requirements
    - .planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-01-SUMMARY.md — actual two-branch shape and tracer identifiers to extend without breaking
    - accrue/lib/accrue/entitlements/admin.ex — tracer implementation and normalization owner
    - accrue/lib/accrue/entitlements/reconcile.ex — stored pull stamp, webhook watermark, entitlement list, truncation, and timestamps
    - accrue/lib/accrue/billing/entitlement_summary.ex — available typed row fields; do not change schema
    - accrue/test/accrue/entitlements/admin_test.exs — test configuration restore, factories, and tracer shape assertions
  </read_first>
  <behavior>
    - "Enabled with no row -> `:not_observed`; enabled with a row containing an empty data list -> recorded state with count 0 and an empty complete lookup-key list."
    - "Row with `truncated: true` and webhook watermark -> `:incomplete`, source `:webhook`, completeness `:incomplete`; row with no usable observation time -> `:age_unknown`."
    - "Exact pull stamp -> source `:pull`; watermark without pull stamp -> source `:webhook`; any other evidence -> source `:unavailable`."
    - "Malformed entitlement evidence or advisory read/projection error -> advisory `:unavailable` while a valid local branch remains present."
  </behavior>
  <action>
    Write failing branch tests first, then complete the private normalization helpers introduced by Plan 01. Preserve one normalized map shape across states with explicit `state`, `entitlement_count`, complete sorted `lookup_keys`, `observed_at`, `source`, `completeness`, and safe `raw` values. Map nil row to `:not_observed`; treat a row with an empty stored list as observed count zero; map `truncated: true` to `:incomplete`; map a row lacking a usable recorded observation time to `:age_unknown`; contain malformed map/list data as `:unavailable`. Derive pull only from exact `_accrue.source == "pull"`; derive webhook only from a non-empty persisted event watermark; otherwise use `:unavailable`. Do not calculate elapsed time, a stale threshold, local/Stripe comparison, or a grant-like result.

    Keep the diagnostic value complete: never cap lookup keys in core or raw evidence. Exercise unsorted keys and assert lexical output, unknown/missing fields, stored count/list inconsistencies without crashing, and absolute DateTime preservation. Do not add schema fields, persistence writes, or provider calls.
  </action>
  <acceptance_criteria>
    - D-15: all six state atoms are reachable by focused tests, with not_observed and observed-empty asserted as different values.
    - D-10: pull, webhook, and unavailable provenance each have a positive test based only on trusted persisted evidence.
    - D-16: no elapsed-age calculation, hard-coded freshness threshold, or stale diagnostic atom is added.
    - D-07: every success state exposes sorted complete lookup keys and scalar/map values; no Ecto schema, changeset, Repo tuple, or provider struct crosses the function result.
    - Malformed advisory evidence returns `:unavailable` without raising from `diagnostic_for_customer/1`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/admin_test.exs</automated>
  </verify>
  <done>The core diagnostic expresses every locked advisory state and provenance branch without truncation, inference, or provider/persistence leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove independent failure containment, disabled no-I/O, and grant invariance</name>
  <files>accrue/lib/accrue/entitlements/admin.ex, accrue/test/accrue/entitlements/admin_test.exs, accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs</files>
  <read_first>
    - .planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-CONTEXT.md — D-02, D-08, D-09, and D-19 failure/isolation requirements
    - accrue/lib/accrue/entitlements/admin.ex — contain local and advisory calls independently, not around the composed function
    - accrue/test/accrue/entitlements/admin_test.exs — `unmapped_action: :raise` fixture for local failure and SQL-sandbox test context
    - accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs — existing no/empty/stale/contradictory grant-surface comparisons and advisory table instrumentation
    - scripts/ci/verify_entitlement_sync_isolation.sh — unchanged executable gate-path boundary to run after tests
  </read_first>
  <behavior>
    - "Local resolver raises while a valid advisory row exists -> local error branch plus truthful advisory branch; composed function does not raise."
    - "Advisory normalization/read fails while local resolution succeeds -> truthful local branch plus advisory unavailable branch."
    - "Sync disabled with a historical advisory row -> no advisory-table query and disabled branch."
    - "Calling the completed diagnostic under no, empty, stale-time, and contradictory advisory rows -> local grant surface is byte-for-byte/equality unchanged."
  </behavior>
  <action>
    Add independent containment boundaries inside `diagnostic_for_customer/1`: the local resolver branch returns `{:error, :unavailable}` on an unexpected local failure while the advisory branch is still evaluated; an advisory read or projection failure returns an unavailable advisory map while the local result is retained. Do not rescue the entire composed function into one sentinel. Preserve the existing public `resolve_for_customer/1` behavior, including its raise under `unmapped_action: :raise`; containment belongs only to the new consumer diagnostic.

    Extend tests to use the existing environment and SQL-sandbox instrumentation to prove the disabled config decision occurs before any `accrue_entitlement_summaries` query even with a historical row present, and that enabled mode performs at most one owner-scoped row lookup. Extend the existing grant-surface fixture family to call the diagnostic in no-row, observed-empty, old-timestamp, and contradictory-key conditions, then compare `entitled?/2`, `features_for/1`, `has_active_plan?/2`, and `entitlement_quantity/2` against the pre-diagnostic local baseline. Keep `scripts/ci/verify_entitlement_sync_isolation.sh` unchanged and green.
  </action>
  <acceptance_criteria>
    - D-08: tests prove both asymmetric failure combinations and the composed diagnostic never converts an advisory failure into a local authorization statement.
    - D-09: disabled mode emits no advisory table query; enabled mode emits no more than one owner-scoped summary lookup and no Processor/network call.
    - D-02: `resolve_for_customer/1` behavior and every tested grant API remain unchanged across diagnostic reads and contradictory rows.
    - D-19: no-row, observed-empty, complete pull, incomplete webhook, unknown source/time, advisory error, and local error branches are all green in focused core tests.
    - The unchanged isolation script exits 0 and no advisory symbol appears in resolver/Guard production files.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/admin_test.exs test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh &amp;&amp; cd accrue &amp;&amp; mix compile --warnings-as-errors</automated>
  </verify>
  <done>Branch failures are independently diagnosable, disabled mode is query-inert, and the completed observation seam cannot perturb any local grant surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config → advisory storage read | Disabled configuration must stop the read before any database I/O. |
| Stored evidence → normalized branch state | Malformed or misleading fields must degrade to unavailable/unknown rather than authority. |
| Diagnostic execution → local grant APIs | Observing a row must have no side effect on grant calculation. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214.2-02-E | Elevation of privilege | composed diagnostic and resolver/Guard boundary | high | mitigate | Independent branches, unchanged resolver API, runtime grant-surface equality, and unchanged static isolation gate. |
| T-214.2-02-I | Information disclosure | normalized advisory raw map | high | mitigate | Permit bounded diagnostic fields and safe raw map only; never return the row/schema or payment/card data. |
| T-214.2-02-T | Tampering | source/completeness/time projection | high | mitigate | Exact stamp/watermark rules, explicit unknown states, malformed-evidence tests, and no timestamp inference. |
| T-214.2-02-A | Tampering | downstream state communication | medium | mitigate | Produce distinct named state atoms that Plan 03 maps to explicit text; no state is represented by tone alone. |
</threat_model>

## Artifacts this phase produces

- New private symbols completed in `accrue/lib/accrue/entitlements/admin.ex`: `not_observed_advisory/0`, `unavailable_advisory/0`, `state_for/1`, `observed_at_for/1`, and independent `safe_local_diagnostic/1` / `safe_stripe_advisory_diagnostic/1` wrappers.
- New diagnostic fields (in-memory only): `state`, `entitlement_count`, `lookup_keys`, `observed_at`, `source`, `completeness`, and `raw` on the `stripe_advisory` map; `resolved` and `unmapped_price_ids` on the `local` success map.
- New files: none.
- New persistence/schema/config fields: none.
- Execution summary: `.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-02-SUMMARY.md`.

<verification>
After each task commit run the named focused tests. At wave completion run both focused core suites, the unchanged isolation verifier, and warnings-as-errors core compilation per `214.2-VALIDATION.md`.
</verification>

<success_criteria>
- All locked advisory states and provenance cases are deterministic and tested.
- Disabled reads are fully inert and enabled reads are bounded to one row lookup.
- Local resolution/grant behavior is unchanged under every advisory condition.
</success_criteria>

<output>
Create `.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-02-SUMMARY.md` when done.
</output>

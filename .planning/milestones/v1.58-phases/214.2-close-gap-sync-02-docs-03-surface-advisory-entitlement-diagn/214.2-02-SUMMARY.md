---
phase: 214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn
plan: "02"
subsystem: entitlements-admin
tags: [elixir, entitlements, advisory-diagnostic, isolation]
requires:
  - phase: "214.2-01"
    provides: config-gated two-branch diagnostic tracer
provides:
  - Complete normalized advisory state and provenance model
  - Runtime proofs for disabled inertness, bounded lookup, and grant invariance
affects: [accrue, accrue_admin]
tech-stack:
  added: []
  patterns: [independent diagnostic branches, safe normalized evidence, telemetry-backed isolation]
key-files:
  created: []
  modified:
    - accrue/lib/accrue/entitlements/admin.ex
    - accrue/test/accrue/entitlements/admin_test.exs
    - accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
decisions:
  - Advisory diagnostics always return a complete consumer-shaped map, including unavailable and absent states.
  - Provenance is trusted only from the exact pull stamp or complete webhook watermark; timestamps do not imply source or freshness.
  - Diagnostic reads remain observational and are asserted against the full local grant surface.
coverage:
  - id: D1
    description: "Disabled, absent, empty, recorded, age-unknown, incomplete, and unavailable advisory states normalize independently from local access."
    requirement: SYNC-02
    verification:
      - kind: unit
        ref: "cd accrue && mix test test/accrue/entitlements/admin_test.exs"
        status: pass
    human_judgment: false
  - id: D2
    description: "Disabled mode performs no advisory query and enabled observations cannot alter any local grant surface."
    requirement: SYNC-02
    verification:
      - kind: integration
        ref: "cd accrue && mix test test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs"
        status: pass
      - kind: other
        ref: "bash scripts/ci/verify_entitlement_sync_isolation.sh"
        status: pass
    human_judgment: false
metrics:
  duration: 6m
  completed: 2026-07-31
status: complete
---

# Phase 214.2 Plan 02: Complete Advisory Diagnostic Contract Summary

Core diagnostics now distinguish every persisted advisory state with safe provenance and evidence, while telemetry-backed tests prove those observations cannot affect local grants.

## Accomplishments

- Normalized disabled, not-observed, recorded, age-unknown, incomplete, and unavailable advisory states to one complete map shape.
- Preserved absolute observation timestamps, complete lexical lookup-key lists, trusted pull/webhook provenance, and safe scalar/raw values without exposing schemas or provider payloads.
- Added focused failure-containment tests, disabled/no-I/O and enabled/single-lookup telemetry assertions, plus diagnostic calls across grant-surface parity fixtures.

## Verification

- `cd accrue && mix test test/accrue/entitlements/admin_test.exs test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs` — passed (20 tests).
- `bash scripts/ci/verify_entitlement_sync_isolation.sh` — passed.
- `cd accrue && mix compile --warnings-as-errors` — passed.

## TDD Gate Compliance

- RED: `99992d70` added state/provenance tests; they failed against the tracer’s incomplete normalization.
- GREEN: `ebd20e7b` completed the normalization state machine; focused tests passed.
- Task 2’s additional isolation tests passed immediately because Plan 01’s tracer had already established separate branch rescue boundaries; `2884b794` records the expanded runtime proof without duplicating production code.

## Decisions Made

- Empty persisted lists mean a recorded zero-count observation, never absence.
- Malformed advisory data degrades only the advisory branch to unavailable; local resolution remains independently truthful.
- No elapsed-age computation or stale state is emitted.

## Deviations from Plan

None - plan executed exactly as written. The tracer had already supplied Task 2’s independent containment structure, so this plan added its focused proof rather than redundant implementation.

## Known Stubs

None.

## Self-Check: PASSED

- Required production and focused test files exist.
- Task commits `99992d70`, `ebd20e7b`, and `2884b794` exist in git history.

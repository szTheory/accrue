---
phase: 214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/entitlement_summary.ex
  - accrue/guides/telemetry.md
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "DOCS-03 / D-21: EntitlementSummary moduledoc and telemetry guidance state that client-backed pull exhaustively streams active entitlements, webhook summary snapshots may be incomplete, and both paths remain advisory."
    - "D-21: the existing package-doc verifier positively pins current pull/webhook completeness truth and rejects both stale pre-2.x deferred-pagination claims through its ROOT_DIR negative-fixture family."
    - "D-22: the docs verifier, focused verifier tests, entitlement isolation verifier, and warnings-as-errors core compilation are green without a new verifier or CI lane."
    - "D-22: Phase 213 validation and an independent v1.58 milestone re-audit remain explicit post-phase closeout actions; this plan does not rewrite the dated `gaps_found` audit or self-declare the milestone passed."
  artifacts:
    - path: "accrue/lib/accrue/billing/entitlement_summary.ex"
      provides: "Current exhaustive-pull versus possibly-incomplete-webhook moduledoc truth"
      contains: "exhaustively streams"
    - path: "accrue/guides/telemetry.md"
      provides: "Current operational completeness wording"
      contains: "webhook"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Positive and negative completeness wording contract"
      contains: "entitlement_summary.ex"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ROOT_DIR mutations proving each corrected surface is enforced"
      contains: "pagination"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/lib/accrue/billing/entitlement_summary.ex"
      via: "fixed current-truth needles plus stale-wording absent regex"
      pattern: "entitlement_summary\\.ex"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/guides/telemetry.md"
      via: "fixed webhook/pull completeness needles plus stale-wording absent regex"
      pattern: "telemetry\\.md"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "temporary ROOT_DIR mutations run the real verifier and require nonzero failure"
      pattern: "run_verifier"
  prohibitions:
    - requirement_id: DOCS-03
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT describe exhaustive client-backed pull pagination as deferred or imply that a webhook summary snapshot is necessarily complete."
    - requirement_id: DOCS-03
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT rewrite the dated v1.58 `gaps_found` audit, claim this implementation independently passed the milestone, or archive v1.58 without a separate re-audit."
  flagged_assumptions:
    - requirement_id: DOCS-03
      category: unclassified
      status: unresolved
      verification: null
      resolution: null
      reason: null
      assumption: "The descriptor-less edge probe could not classify DOCS-03; both stale target surfaces and the historical-audit preservation boundary remain explicit and require manual review alongside the executable package-doc contract."
---

<objective>
Correct and regression-protect the two stale advisory pagination statements while preserving historical audit boundaries (DOCS-03; D-21, D-22).

Purpose: Make package/operator documentation describe the shipped exhaustive pull and potentially incomplete webhook paths accurately, with the existing verifier proving the wording cannot regress.
Output: Two corrected documentation surfaces, extended package-doc assertions, and two real ROOT_DIR mutation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.58-v1.58-MILESTONE-AUDIT.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-CONTEXT.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-RESEARCH.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-PATTERNS.md
@.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-VALIDATION.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md
@accrue/lib/accrue/billing/entitlement_summary.ex
@accrue/guides/telemetry.md
@scripts/ci/verify_package_docs.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace both stale completeness statements with shipped pull/webhook truth</name>
  <files>accrue/lib/accrue/billing/entitlement_summary.ex, accrue/guides/telemetry.md</files>
  <read_first>
    - .planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-CONTEXT.md — D-21 exact current truth and D-22 historical boundary
    - .planning/v1.58-v1.58-MILESTONE-AUDIT.md — authoritative stale-surface locations and read-only historical `gaps_found` evidence
    - accrue/lib/accrue/billing/entitlement_summary.ex — stale deferred-pagination moduledoc; change prose only
    - accrue/guides/telemetry.md — stale `entitlement_summary_truncated` operational row; preserve event identity/table structure
    - accrue/lib/accrue/entitlements/reconcile.ex — current pull writer always records the exhaustively drained list and webhook writer preserves has_more/truncated evidence
    - accrue/lib/accrue/processor/stripe.ex — current client-backed adapter drains `ActiveEntitlement.stream!/3`
  </read_first>
  <action>
    Edit only documentation prose in the two locked targets. In `EntitlementSummary` moduledoc, replace the obsolete deferred-pagination paragraph with current behavior: client-backed pull refresh exhaustively streams the customer's active entitlements before persisting its snapshot; webhook summary snapshots can contain only the first reported entitlements and use `entitlements.has_more` / `truncated` to record known incompleteness; both are advisory and never affect local access. Retain the one-row/customer, observational-only, and typed-column explanations.

    In the existing `[:accrue, :ops, :entitlement_summary_truncated]` telemetry catalog row, state that the event identifies a known-incomplete webhook advisory snapshot, that client-backed pull exhaustively streams active entitlements before persistence, and that neither path gates access. Preserve the event name and Markdown table shape. Do not change schema fields, write order, runtime behavior, API metadata, dependency versions, other guides, release notes, or planning/history artifacts.
  </action>
  <acceptance_criteria>
    - D-21: both targets explicitly distinguish exhaustive client-backed pull from potentially incomplete webhook summaries and reiterate observational-only/local-access invariance.
    - The obsolete deferred-version/pagination claim is absent from both targets.
    - `EntitlementSummary` schema, fields, changeset, and runtime code are byte-for-byte unchanged outside moduledoc prose.
    - The telemetry event name and catalog row structure remain unchanged outside its description cell.
    - `.planning/v1.58-v1.58-MILESTONE-AUDIT.md` is not modified and retains `status: gaps_found`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix compile --warnings-as-errors &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>The two stale targets describe exhaustive pull and incomplete webhook evidence accurately, with no runtime/schema/history change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratchet both corrected surfaces through the existing package-doc verifier</name>
  <files>scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - .planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-CONTEXT.md — D-21 requires the existing verifier and ROOT_DIR family, not a new lane
    - accrue/lib/accrue/billing/entitlement_summary.ex — exact current-truth sentences written by Task 1
    - accrue/guides/telemetry.md — exact current-truth telemetry sentence written by Task 1
    - scripts/ci/verify_package_docs.sh — existing `require_fixed` / `require_absent_regex` helper block and Phase 213/214 placement
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — `tmp_dir!`, `seed_tmp_dir!`, `run_verifier`, and stale lattice_stripe ROOT_DIR mutation pattern
    - .planning/v1.58-v1.58-MILESTONE-AUDIT.md — preserve historical evidence; never use it as a verifier mutation target
  </read_first>
  <behavior>
    - "Clean repository -> package-doc verifier exits 0 with both current-truth targets present."
    - "Temporary EntitlementSummary target reintroducing the obsolete deferred-pagination claim -> real verifier exits nonzero and identifies that target."
    - "Temporary telemetry target reintroducing the obsolete deferred-pagination claim -> real verifier exits nonzero and identifies that target."
    - "Temporary target removing the current exhaustive-pull/webhook-incomplete truth -> real verifier exits nonzero."
  </behavior>
  <action>
    Add focused positive `require_fixed` needles for the exact Task 1 exhaustive-pull and webhook-incomplete sentences in each target, plus target-scoped `require_absent_regex` checks that reject the obsolete deferred-version and deferred-full-pagination language. Place them in the existing optional Stripe-native / current 2.x documentation block and reuse `ROOT_DIR`, `fail`, and current helpers. Do not add a script, CI job, or general-purpose parser.

    Before relying on the clean pass, add two focused ExUnit mutations to the existing ROOT_DIR fixture family: seed a complete temporary root, mutate exactly one corrected target at a time back to its obsolete meaning, run the real script through `run_verifier/1`, and require nonzero status plus target/contract diagnostic output. Also prove a removed current-truth needle fails, either within those mutations or a third focused assertion if needed. Run the focused ExUnit file red before changing the shell contract, then green after. Keep the historical audit and planning mirrors outside the fixture mutations.
  </action>
  <acceptance_criteria>
    - D-21: `verify_package_docs.sh` contains positive current-truth and negative stale-wording assertions for both exact target paths.
    - Two target-specific ROOT_DIR mutations run the real verifier and fail non-vacuously, one for EntitlementSummary and one for telemetry.
    - The unmodified repository passes the real package-doc verifier and focused verifier test file.
    - D-22: the existing verifier/CI topology is unchanged; no new dependency, script, lane, skip, or weakened assertion is introduced.
    - The historical milestone audit is neither copied into fixtures nor modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/package_docs_verifier_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_package_docs.sh &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>Both corrected documentation surfaces are positively pinned, stale regressions fail through real ROOT_DIR mutations, and the existing documentation/isolation gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime writer truth → maintainer/operator documentation | Docs must distinguish exhaustive pull from incomplete webhook evidence without overstating authority. |
| Documentation source → executable verifier | Positive and negative needles must measure both corrected targets non-vacuously. |
| Current correction → historical audit | Forward fixes must not rewrite the dated audit or forge an independent pass. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214.2-04-E | Elevation of privilege | advisory-authority documentation | high | mitigate | Both corrected surfaces explicitly retain observational-only/local-gate language and the isolation verifier runs in every task. |
| T-214.2-04-I | Information disclosure | documentation targets | low | accept | These targets describe behavior and event metadata only; no provider payload/PII examples are added, and runtime Raw data handling is owned by Plan 03. |
| T-214.2-04-T | Tampering | completeness/provenance claims and historical audit | high | mitigate | Positive/negative verifier needles, ROOT_DIR red fixtures, unchanged runtime source, and explicit prohibition on modifying/self-passing the dated audit. |
| T-214.2-04-A | Tampering | non-color state communication | medium | accept | This plan changes no UI; Plan 03 owns explicit text-plus-tone semantics and browser proof, while docs wording names states in text rather than color. |
</threat_model>

## Artifacts this phase produces

- Planning-only gate artifact: `.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/COVERAGE.md`, declaring that this phase adds no external API integration after the deterministic detector's false-positive signal.
- Updated documentation only: `Accrue.Billing.EntitlementSummary` moduledoc in `accrue/lib/accrue/billing/entitlement_summary.ex` and the existing `[:accrue, :ops, :entitlement_summary_truncated]` row in `accrue/guides/telemetry.md`.
- New shell contract assertions in `scripts/ci/verify_package_docs.sh`: fixed exhaustive-pull/webhook-incomplete needles and target-scoped absence checks for both corrected files.
- New ExUnit test symbols in `accrue/test/accrue/docs/package_docs_verifier_test.exs`: `package docs verifier rejects stale EntitlementSummary pagination truth` and `package docs verifier rejects stale telemetry pagination truth` (exact test descriptions).
- New files, runtime symbols, schema/config/persistence fields, routes, and dependencies: none.
- Execution summary: `.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-04-SUMMARY.md`.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Surface persisted advisory diagnostics in existing customer detail while preserving local-only authorization and correct stale pagination docs | 01-04 | COVERED | Tracer proves writer-to-display; core/UI expansions cover all states; docs plan corrects and ratchets both targets. |
| REQ | SYNC-02 | Opt-in advisory output is consumed only by diagnostics/admin and never changes local grants | 01-03 | COVERED | Config-gated core seam, contradictory integration, runtime/static isolation, complete UI states. |
| REQ | DOCS-03 | Package/support/planning truth remains mutually consistent | 04 | COVERED | Both stale targets and existing package-doc verifier family are included. |
| RESEARCH | — | Core-owned typed two-branch diagnostic; no schema/dependency/new API | 01-02 | COVERED | D-06 contract and existing StripeSync reader are reused; no install/schema task. |
| RESEARCH | — | Existing disclosure/components/Copy/Raw data/browser contracts | 01, 03 | COVERED | No new route, component, CSS family, or raw disclosure. |
| RESEARCH | — | Existing package-doc verifier plus ROOT_DIR mutations | 04 | COVERED | Two target-specific real-script red paths. |
| CONTEXT | D-01..D-05 | Canonical-first existing disclosure and advisory-only operator JTBD | 01, 03 | COVERED | Exact group/boundary labels and local-only headline. |
| CONTEXT | D-06..D-10 | Core consumer seam, typed branches, partial diagnosis, bounded config-gated read, exact provenance | 01-02 | COVERED | Costly contract plus branch/no-I/O tests. |
| CONTEXT | D-11..D-14 | Compact facts, 8-key preview, existing Raw data, existing UI system | 03 | COVERED | Primary/raw boundary and browser checks. |
| CONTEXT | D-15..D-18 | Complete state/copy/accessibility model without stale inference | 02-03 | COVERED | Six core states and exact Copy-backed UI. |
| CONTEXT | D-19..D-20 | Focused branch/UI tests and real contradictory writer integration | 01-03 | COVERED | Nyquist map is implemented without live Stripe. |
| CONTEXT | D-21 | Correct both stale pagination surfaces and verifier fixtures | 04 | COVERED | Exact target paths and real verifier mutations. |
| CONTEXT | D-22 | Full phase gates, Phase 213 validation, independent re-audit, historical preservation | 03-04 + post-phase | COVERED | Implementation plans run focused/full gates; after phase verification run `$gsd-validate-phase 213`, then an independent `$gsd-audit-milestone v1.58`; no plan edits the dated audit or claims its verdict. |

Excluded without gap: all CONTEXT Deferred Ideas; new Stripe API/SDK calls; schema/migration work; freshness policy/stale state; crosswalk/mismatch semantics; manual refresh; new routes/rooms/nav; `accrue_portal`; Feature catalog authoring; and any v1.59 implementation.

<verification>
After each task commit run its focused command. At Wave 1 completion, run the package-doc verifier, isolation verifier, focused docs tests, and warnings-as-errors core compilation. After Plans 01-04 execute and Phase 214.2 verifies, run `$gsd-validate-phase 213` and then an independent `$gsd-audit-milestone v1.58`; preserve the existing dated audit as historical evidence and let the independent workflow author any new verdict.
</verification>

<success_criteria>
- Both public/operational stale pagination statements tell the current exhaustive-pull versus incomplete-webhook truth.
- The existing verifier detects stale or missing truth in either target via real isolated fixtures.
- The historical `gaps_found` audit remains unchanged until the independent re-audit runs after phase verification.
</success_criteria>

<output>
Create `.planning/phases/214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn/214.2-04-SUMMARY.md` when done.
</output>

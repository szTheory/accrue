---
phase: 214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn
plan: "04"
subsystem: documentation-verification
tags: [elixir, stripe, entitlements, package-docs, regression-tests]
requires:
  - phase: "213"
    provides: advisory Stripe entitlement pull and webhook writers
  - phase: "214.1"
    provides: StripeSync writer documentation contract
provides:
  - Accurate exhaustive-pull versus incomplete-webhook advisory guidance
  - Executable package-documentation contract for both corrected surfaces
  - ROOT_DIR regression fixtures for stale completeness wording
affects: [accrue, documentation, v1.58-re-audit]
tech-stack:
  added: []
  patterns: [literal documentation needles, target-scoped negative regexes, ROOT_DIR verifier mutations]
key-files:
  created: []
  modified:
    - accrue/lib/accrue/billing/entitlement_summary.ex
    - accrue/guides/telemetry.md
    - scripts/ci/verify_package_docs.sh
    - accrue/test/accrue/docs/package_docs_verifier_test.exs
decisions:
  - "Client-backed pull is documented as exhaustive before persistence; webhook snapshots remain explicitly potentially incomplete."
  - "The established package-doc verifier and ROOT_DIR fixture family enforce the corrected language without a new CI lane."
coverage:
  - id: D1
    description: "EntitlementSummary and telemetry distinguish exhaustive pulls from potentially incomplete webhook observations while preserving the advisory-only boundary."
    requirement: DOCS-03
    verification:
      - kind: unit
        ref: "cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs"
        status: pass
      - kind: other
        ref: "bash scripts/ci/verify_package_docs.sh"
        status: pass
    human_judgment: false
  - id: D2
    description: "The executable UAT contract rejects incomplete, manual, or evidence-free phase closure artifacts."
    requirement: DOCS-03
    verification:
      - kind: unit
        ref: "node scripts/ci/verify_executable_uat_contract.mjs --self-test"
        status: pass
      - kind: other
        ref: "node scripts/ci/verify_executable_uat_contract.mjs --phase 214.2"
        status: pass
    human_judgment: false
metrics:
  duration: 14m
  completed: 2026-07-31
status: complete
---

# Phase 214.2 Plan 04: Advisory Pagination Documentation Contract Summary

Current entitlement documentation now distinguishes exhaustive client-backed pulls from potentially incomplete webhook snapshots, with real verifier fixtures preventing the pre-2.x deferred-pagination claim from returning.

## Accomplishments

- Corrected `EntitlementSummary` documentation to describe exhaustive pull persistence, webhook `has_more` incompleteness, and the unchanged local-access boundary.
- Corrected both telemetry descriptions for `[:accrue, :ops, :entitlement_summary_truncated]` while preserving event names and table layouts.
- Added positive needles and target-scoped stale-wording bans to the existing package-doc verifier.
- Added two ROOT_DIR fixture tests that mutate one target at a time and prove the real verifier rejects stale or missing current truth.

## Verification

- `cd accrue && mix compile --warnings-as-errors` — passed.
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` — passed (46 tests).
- `bash scripts/ci/verify_package_docs.sh` — passed.
- `bash scripts/ci/verify_entitlement_sync_isolation.sh` — passed.

## TDD Gate Compliance

- RED: `190fe051` added two target-specific ROOT_DIR mutations; the focused test run failed because the verifier did not yet inspect either target.
- GREEN: `7596c9ae` added the literal and negative verifier contract; the focused suite passed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the second stale telemetry catalog statement**
- **Found during:** Task 2
- **Issue:** The verifier's target-scoped stale-wording check exposed another `entitlement_summary_truncated` catalog row still claiming full pagination was deferred until `lattice_stripe >= 1.2`.
- **Fix:** Replaced it with the current exhaustive-pull / incomplete-webhook advisory wording and pinned that exact catalog row in the verifier and fixture test.
- **Files modified:** `accrue/guides/telemetry.md`, `scripts/ci/verify_package_docs.sh`, `accrue/test/accrue/docs/package_docs_verifier_test.exs`
- **Commit:** `7596c9ae`

## Known Stubs

None.

## Historical Audit Boundary

The dated v1.58 milestone audit remains `status: gaps_found`; this plan does not claim a milestone pass or modify that historical evidence. Phase 213 validation and the independent v1.58 re-audit remain post-phase closeout actions.

## Self-Check: PASSED

- All four planned documentation, verifier, and test files exist.
- Task commits `c1e5ed21`, `190fe051`, and `7596c9ae` exist in git history.
- No placeholder, TODO, FIXME, or UI-data stub was introduced in the modified task files.

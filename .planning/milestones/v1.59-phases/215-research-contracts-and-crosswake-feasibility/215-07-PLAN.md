---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 07
type: execute
wave: 5
depends_on:
  - 215-06
files_modified:
  - accrue/lib/accrue/entitlements/decision_cases/markdown.ex
  - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
  - accrue/test/accrue/entitlements/decision_cases_test.exs
  - accrue/test/support/entitlements/offline_golden_vector_verifier.ex
  - accrue/test/accrue/entitlements/offline_golden_vectors_test.exs
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
autonomous: true
gap_closure: true
requirements:
  - RSCH-02
  - RAIL-05
must_haves:
  truths:
    - "The offline corpus contains valid allow/deny plus wrong signature, key, device, account, audience, type, algorithm, malformed compact/claim, unknown disposition, non-integer revision/iat/freshness, rollback, and fault vectors required by D-11."
    - "Every vector's expected verification, bounded reason, and cache disposition is compared to the observed Elixir and Swift result; a contradictory fixture fails both suites rather than being hidden by hard-coded observation maps."
    - "Unknown signed dispositions and non-integer high-water claims fail closed in both languages before cache replacement per D-11/D-13."
    - "The corrected fault-before-replace expectation records the actual prior deny cache, while fault-after-replace records the complete signed denial; generated bytes and canonical case bindings remain deterministic per D-05/D-06."
    - "The RAIL-05/RSCH-02 fallback prohibitions intentionally remain descriptor-less and flagged-unverified under the projectProhibitions contract; mutation-style expectation tests and unknown-disposition rejection tests in both readers are their executable evidence without invented wired-check descriptors."
  artifacts:
    - path: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      provides: "Corrected complete self-describing invalid-input and cache-outcome corpus"
    - path: "accrue/test/support/entitlements/offline_golden_vector_verifier.ex"
      provides: "Fail-closed Elixir JWS/claim/disposition observer"
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      provides: "Fail-closed Swift JWS/claim/disposition observer"
  key_links:
    - from: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      to: "accrue/test/accrue/entitlements/offline_golden_vectors_test.exs"
      via: "fixture expectations are normalized and compared field-for-field to Elixir observations"
      pattern: "expected_verification|expected_reason|expected_cache_disposition"
    - from: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      to: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      via: "the same fixture expectations are compared field-for-field to Swift observations"
      pattern: "expectedVerification|expectedReason|expectedCacheDisposition"
    - from: "accrue/lib/accrue/entitlements/decision_cases/markdown.ex"
      to: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      via: "the deterministic exporter owns signed vector bytes and exact canonical case/version/disposition bindings"
      pattern: "offline_vectors"
  assumptions:
    - requirement_id: RAIL-05
      statement: "Vector parity cannot prove the pinned Crosswake bridge or dated physical-device evidence; `capability-report.json: feasibility_blocked` remains the backstop, and this plan does not alter runtime coupling status."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT treat fixture labels as evidence; observed cryptographic, claim, reason, and cache outcomes must match every declared expectation."
    - requirement_id: RSCH-02
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT map an unknown signed disposition to allow or hide malformed high-water claims behind a successful vector label."
---

<objective>
Correct and self-validate the shared offline golden corpus across Elixir and Swift.

Decision coverage: D-05/D-06 preserve canonical generated vectors without a second reducer; D-11 requires executable algorithm/claim/binding/rollback/fault coverage; D-12 keeps this merge gate independent of Crosswake feasibility; D-13 permits replacement only after verified closed disposition and monotonic claims.

Purpose: Turn fixture expectations into the actual oracle so contradictory or omitted security cases cannot pass either language implementation.
Output: Complete corrected vectors, fail-closed readers, and exact fixture-to-observation parity tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-05-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-06-SUMMARY.md
@accrue/priv/entitlements/v1.59-offline-golden-vectors.json
@accrue/test/support/entitlements/offline_golden_vector_verifier.ex
@examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
</context>

## Artifacts this phase produces

- A complete, corrected, deterministically generated v1.59 offline vector corpus.
- Exact expected-versus-observed parity assertions in Elixir and Swift.
- Closed signed-disposition and typed high-water claim validation in both implementations.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one contradictory cache vector fail generation and both readers</name>
  <reversibility rating="costly">Both language implementations and Phase 219 consume the checked-in golden-corpus schema and bounded reason vocabulary.</reversibility>
  <files>accrue/lib/accrue/entitlements/decision_cases/markdown.ex, accrue/priv/entitlements/v1.59-offline-golden-vectors.json, accrue/test/accrue/entitlements/decision_cases_test.exs</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (second failed truth)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-05, D-06, D-11 through D-13)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-PATTERNS.md (`decision_cases.ex` renderer/exporter assignment: closed declaration with render/export as deterministic pure consumers)
    - accrue/lib/accrue/processor/capabilities.ex (`@support_labels` closed declaration and total `support_label/1` lookup analog)
    - accrue/lib/accrue/entitlements/decision_cases/markdown.ex
    - accrue/lib/accrue/entitlements/decision_cases.ex
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
    - accrue/test/accrue/entitlements/decision_cases_test.exs
  </read_first>
  <behavior>
    - Test 1: fault-before-replace is generated with expected deny because its prior cache is deny and rename never occurs.
    - Test 2: mutating any expected verification/reason/cache field makes the fixture check fail with the vector ID and field name.
    - Test 3: corpus IDs include wrong account/audience/type/algorithm, malformed compact, malformed high-water claim types, and unknown disposition.
    - Test 4: every vector retains exact canonical case ID, contract version, and expected disposition binding.
  </behavior>
  <action>Start with a regression that proves the current fault-before cache expectation contradicts its prior state, then correct the deterministic vector producer and regenerate the checked-in JSON. Follow the deterministic pure-export pattern mapped in `215-PATTERNS.md` from `Accrue.Processor.Capabilities`: keep the closed vector specification as the sole declaration, make the Markdown/JSON exporter a total pure consumer of that declaration, and derive stable bytes without an independent policy table. Extend the producer's explicit vector specifications with separately signed or deliberately malformed cases for wrong account, audience, type, algorithm, malformed compact input, non-integer revision/iat/freshness, and unknown signed disposition. Give every case a bounded expected verification, reason, cache disposition, prior-cache context, and canonical case/version/disposition binding. Keep bytes deterministic and signing material test-only; do not hand-edit generated output as the source of truth or add Phase-219 issuance/runtime behavior.</action>
  <acceptance_criteria>
    - `mix accrue.entitlements.decision_cases --check` passes on the corrected corpus and fails on temporary expected-field mutation.
    - The corpus has a distinct vector ID for every verifier-required invalid case and no duplicate IDs.
    - Fault-before-replace declares deny, matching its checked-in prior deny context and observed old-cache result.
    - All vectors remain canonically bound and contain no PII, adopter identity, receipt, notification body, or production key material.
    - An automated mutation-style negative test rewrites each expected verification/reason/cache field in turn and proves the corpus check fails at the mutated vector ID.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs</automated>
  </verify>
  <done>The generated corpus is internally consistent, complete for required invalid inputs, and mutation-sensitive before either language consumes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compare every declared outcome to observed Elixir and Swift behavior</name>
  <files>accrue/test/support/entitlements/offline_golden_vector_verifier.ex, accrue/test/accrue/entitlements/offline_golden_vectors_test.exs, examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift</files>
  <read_first>
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
    - accrue/priv/entitlements/v1.59-offline-test-key.jwk.json
    - accrue/test/support/entitlements/offline_golden_vector_verifier.ex
    - accrue/test/accrue/entitlements/offline_golden_vectors_test.exs
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md (strict RFC 8725 and ASVS V5/V6 boundary)
  </read_first>
  <behavior>
    - Test 1: both readers compare each vector's expected verification, reason, and cache disposition with its observation and report the vector ID on mismatch.
    - Test 2: wrong account/audience/type/algorithm are rejected with the declared bounded reasons after reaching the intended validation stage.
    - Test 3: malformed compact/claim cases and non-integer revision/iat/freshness fail closed without raising or replacing prior cache.
    - Test 4: unknown signed disposition is rejected in both languages and never reaches the allow branch.
    - Test 5: both languages emit identical sorted `{id, verification, reason, cache}` tuples for the entire corpus.
  </behavior>
  <action>Refactor each reader so decoded expected fields are retained as the oracle and compared against computed observations for every vector. In Elixir, type-check revision, iat, freshness, and disposition before arithmetic/comparison; replace the permissive disposition fallback with a closed allow/deny decoder. In Swift, decode security claims through a fail-closed representation that distinguishes malformed claim types and uses a closed disposition enum; do not let an arbitrary string fall into allow. Add exact corpus-driven assertions rather than a manually curated subset/map, while retaining an independent cross-language parity projection of sorted tuples. Ensure wrong-algorithm and wrong-claim fixtures reach their intended bounded rejection reason and that any rejection preserves the declared prior cache. Keep the test lane independent from `capability-report.json` per D-12.</action>
  <acceptance_criteria>
    - Changing any one expected field to an incorrect value fails both targeted suites at that vector ID.
    - Every checked-in vector contributes exactly one observed tuple in both languages; no expected field is decoded and ignored.
    - Unknown disposition and non-integer high-water inputs deterministically reject and preserve prior cache.
    - Elixir and Swift sorted tuple sets are identical and all reasons belong to the checked-in bounded vocabulary.
    - Dedicated corpus cases prove an unknown signed disposition rejects in both Elixir and Swift; mutating its fixture label to success cannot make either reader pass.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_golden_vectors_test.exs &amp;&amp; cd ../examples/crosswake_tracer &amp;&amp; swift test --filter GoldenVectorTests</automated>
  </verify>
  <done>The shared corpus is the executable oracle in both languages: every expected verification/reason/cache result is observed or the merge gate fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated fixture → independent language readers | Untrusted compact JWS and declared expectations must be verified, not trusted. |
| decoded claims → high-water/cache decision | Wrong types or unknown disposition must not widen access or crash open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-26 | Tampering | fixture expectation oracle | high | mitigate | Generator mutation tests and both language suites compare every expected field with observed output. |
| T-215-27 | Spoofing | JWS header/claims | high | mitigate | Wrong account/audience/type/algorithm/signature/key/device vectors execute fixed-alg cryptographic and binding checks. |
| T-215-28 | Elevation of Privilege | disposition/high-water parsing | high | mitigate | Closed disposition and integer claim validation reject unknown/malformed values before replacement. |
| T-215-29 | Denial of Service | malformed compact/claims | medium | mitigate | Both readers return bounded rejection observations without uncaught exceptions. |
| T-215-30 | Information Disclosure | fixture payload/key | medium | mitigate | Deterministic key remains test-only and corpus excludes adopter identity, PII, and raw provider evidence. |

## ASVS L1 Control Mapping

**security_block_on: high.** Any failed high-severity ASVS control or its named automated evidence is merge-blocking and prevents execution completion and Phase-215 closure.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable | Account/device/thumbprint mismatches are signed-vector rejections in both languages. |
| V3 Session Management | Applicable | Revision, iat, and freshness are integer-typed and monotonic before cache state changes. |
| V4 Access Control | Applicable | Unknown disposition and invalid proof cannot enter the allow/replacement path. |
| V5 Input Validation | Applicable | Complete malformed compact/header/claim/disposition corpus is compared to observations. |
| V6 Cryptography | Applicable | ES256, key, signature, algorithm, issuer, audience, and type checks execute in OTP and CryptoKit. |
</threat_model>

<source_audit>
GOAL truths 2 and 4, REQ RSCH-02/RAIL-05, CONTEXT D-05/D-06/D-11/D-12/D-13, RESEARCH strict-JWS/canonical-vector/no-new-package guidance, and VERIFICATION gap 2 are all covered. The work stays in the Phase-215 contract/tracer lane and does not implement an issuer, reconnect runtime, or Crosswake bridge from Phase 219. No dependency install or ORM path appears. API coverage remains unchanged because the test-only corpus/readers expose no external integration.
</source_audit>

<verification>
Run deterministic export check, Elixir golden-vector tests, and Swift GoldenVectorTests. The named automated suites must execute mutation-style negative cases for every fixture expectation and unknown-disposition rejection in both readers, derive expected tuples from JSON rather than a duplicated result table, and leave `capability-report.json` at `feasibility_blocked`. The fallback prohibitions stay descriptor-less (`verification: null`, flagged) by contract; these task-level mutation gates are executable evidence and are not `check_*` descriptors.
</verification>

<success_criteria>
The corrected corpus covers all required invalid inputs, rejects closed-type violations, and forces both language implementations to match declared verification/reason/cache outcomes exactly.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-07-SUMMARY.md` when done.
</output>

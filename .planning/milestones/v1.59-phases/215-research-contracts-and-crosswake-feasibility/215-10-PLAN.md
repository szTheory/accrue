---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 10
type: execute
wave: 8
depends_on:
  - 215-09
files_modified:
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
autonomous: true
gap_closure: true
requirements:
  - RSCH-02
  - RAIL-05
must_haves:
  truths:
    - "Swift requires the exact generated top-level key set (`purpose`, `schema_version`, `vectors`) and exact per-vector key set, with every non-optional field present and `fault_point` present only where the canonical generated vector declares it."
    - "Swift rejects duplicate IDs, missing or extra vectors, any vector identity-set difference, and any field-value difference from the generated canonical offline corpus before executing the JWS/cache oracle per D-05/D-11."
    - "A mutation to any top-level or vector field—including a valid-but-substituted compact JWS—or to key/vector presence that fails the Elixir generator gate also fails Swift with bounded vector/field context; no decoded or unknown canonical field is ignored."
    - "The corrected Swift contract lane remains independent from Crosswake feasibility and cannot upgrade the checked-in feasibility_blocked report per D-12."
  artifacts:
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      provides: "Complete Swift offline-vector schema and canonical binding validator"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      provides: "Swift metadata drift, uniqueness, version, and disposition regressions"
  key_links:
    - from: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      to: "accrue/priv/entitlements/v1.59-decision-cases.json"
      via: "the test-only verifier loads the generated canonical case map and validates every vector binding"
      pattern: "case_id|contract_version|expected_disposition"
    - from: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      to: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      via: "the strict validator compares the candidate's exact top-level/per-vector keys, vector identity set, optional-field presence, and every vector value against the generated canonical corpus before observation"
      pattern: "compact_jws|expected_verification|expected_reason|expected_cache_disposition|fault_point"
    - from: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      to: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      via: "temporary candidate-corpus mutations prove every top-level/vector field, missing or extra key, missing or extra vector, duplicate ID, and valid substituted compact JWS fails against an unmodified canonical baseline"
      pattern: "GoldenVector"
  assumptions:
    - requirement_id: RSCH-02
      statement: "Production reducer equivalence remains unclassified and owned by Phase 217; Swift consumes only the generated Phase-215 contract fixtures."
      status: unresolved
      flagged: true
    - requirement_id: RAIL-05
      statement: "The authoritative Crosswake bridge and dated device proof remain unavailable; complete Swift fixture validation is merge-blocking but does not establish runtime feasibility."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RSCH-02
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT decode and ignore canonical case, version, or disposition metadata in the Swift fixture consumer."
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT convert a Swift contract-test pass into Crosswake bridge or physical-device feasibility."
---

<objective>
Complete the shared offline-corpus canonical binding in Swift.

Decision coverage: D-05 makes the generated DecisionCases corpus the sole identity/version/disposition source; D-06 keeps this reader test-only; D-11 requires merge-blocking cross-language vector proof; D-12 keeps contract parity separate from feasibility.

Purpose: Close the Swift half of the canonical metadata gap after 215-09 establishes the corrected generated and Elixir contract.
Output: A complete Swift vector schema, canonical decision-case lookup, duplicate-ID rejection, and field-specific mutation regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-09-SUMMARY.md
@accrue/priv/entitlements/v1.59-decision-cases.json
@accrue/priv/entitlements/v1.59-offline-golden-vectors.json
</context>

## Artifacts this phase produces

- A strict Swift corpus validator that enforces exact top-level/per-vector key sets and required-versus-optional field presence before typed observation.
- A Swift canonical binding validator against both the generated offline corpus and generated decision-case JSON.
- Mutation-sensitive Swift tests for every top-level/vector field, identity-set drift, key/vector presence, duplicates, and valid-but-substituted signed bytes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one offline vector through Swift canonical metadata validation</name>
  <reversibility rating="costly">The Swift fixture schema and diagnostics are consumed by the Phase-219 client contract and must stay aligned with the D-05 generated corpus.</reversibility>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (offline canonical metadata blocker)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-05/D-06/D-11/D-12)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-09-SUMMARY.md (corrected generated/Elixir schema contract)
    - accrue/priv/entitlements/v1.59-decision-cases.json
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`Corpus`, `Vector`, and `verifyFixture`)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
  </read_first>
  <behavior>
    - Test 1: the checked-in corpus is accepted only with the exact top-level key set `purpose`, `schema_version`, and `vectors`, the exact canonical purpose/schema values, unique vector IDs, and a vector ID set exactly equal to the generated baseline.
    - Test 2: each candidate vector has exactly `id`, `case_id`, `contract_version`, `expected_disposition`, `compact_jws`, `expected_verification`, `expected_reason`, and `expected_cache_disposition`, plus `fault_point` if and only if that canonical generated vector contains it; removing a required key, removing an expected `fault_point`, adding `fault_point` where absent, or adding any unknown key fails before observation.
    - Test 3: focused temporary-candidate mutations of `id`, `case_id`, `contract_version`, `expected_disposition`, `compact_jws`, `expected_verification`, `expected_reason`, `expected_cache_disposition`, and `fault_point` each fail against the unmodified generated baseline; the compact-JWS case substitutes another cryptographically valid corpus JWS so outcome coincidence cannot hide byte-binding drift.
    - Test 4: duplicate IDs, a renamed ID, a missing vector, and an extra vector each fail the exact identity-set check; missing/extra top-level keys and mutations to `purpose` or `schema_version` also fail.
    - Test 5: every vector case_id resolves in canonical decision-cases JSON and its contract_version/expected_disposition match exactly; every rejection is bounded and identifies the top-level field or vector ID plus field before `observe` runs.
    - Test 6: valid vectors continue to match expected verification/reason/cache outcomes after complete structural, generated-corpus, and decision-case binding validation.
  </behavior>
  <action>Before typed decoding or `observe`, parse the candidate JSON as a structural object and reject anything except the exact top-level keys `purpose`, `schema_version`, and `vectors`; require the generated canonical purpose/schema values. For every vector, enforce the exact required key set `id`, `case_id`, `contract_version`, `expected_disposition`, `compact_jws`, `expected_verification`, `expected_reason`, and `expected_cache_disposition`, with `fault_point` allowed and required only when it is present on the same vector in the unmodified generated baseline. This explicit key validation is required because Swift `Decodable` ignores unknown keys. Expand the private typed `Corpus`/`Vector` models to retain all those fields after structural validation. Load the generated `v1.59-offline-golden-vectors.json` as the expected baseline and `v1.59-decision-cases.json` as the canonical case source in the same test-only verifier. Reject duplicate IDs before map construction; require the candidate vector ID set to equal the baseline set exactly; then compare every vector field and optional-field presence to its same-ID baseline vector. Separately require each case_id to resolve in decision cases and its contract_version/expected_disposition to match. Complete all validation before calling `observe`. Expose a bounded Data/URL test seam that accepts a mutated candidate separately from the unmodified baseline. Add focused mutations for every field, missing/extra top-level and per-vector keys, duplicate/renamed/missing/extra vectors, and optional `fault_point` presence in both directions; for `compact_jws`, substitute a different valid signed JWS from the canonical corpus and require failure even if observed outcomes would coincide. Emit stable bounded errors naming the top-level field or vector ID and field, without echoing JWS bytes. Do not duplicate policy or expected values in Swift, add a dependency, or touch `capability-report.json`.</action>
  <acceptance_criteria>
    - Swift accepts only the exact generated top-level/per-vector schemas, including required fields and canonical per-vector `fault_point` presence; unknown or missing keys fail before observation.
    - Candidate vector IDs are unique and exactly equal to the generated baseline set; duplicate, renamed, missing, and extra vectors each have a focused failing test.
    - Every top-level value and every vector field has a focused temporary-candidate mutation test, including a different valid compact JWS, and each failure names the bounded top-level field or vector ID plus field.
    - Unknown case ID, canonical contract-version mismatch, and expected-disposition mismatch also fail against generated decision cases before observation.
    - The checked-in corpus still produces the same sorted verification/reason/cache observations after binding validation.
    - `capability-report.json` remains byte-unchanged and overall feasibility_blocked.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter GoldenVectorTests &amp;&amp; cd ../.. &amp;&amp; git diff --exit-code -- examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>Swift rejects, before observation and with bounded vector/field diagnostics, any difference from the complete generated corpus schema, exact vector identity set, optional-field presence, vector bytes/expected fields, or canonical case/version/disposition binding—including a valid-but-substituted compact JWS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated Elixir JSON → Swift test consumer | Language-neutral metadata crosses a parser boundary and must retain canonical meaning. |
| decoded vector → JWS/cache observation | A misbound allow vector must fail before cryptographic success can be counted as canonical proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-41 | Tampering | Swift vector metadata | high | mitigate | Exact structural key validation, identity-set equality, and field-complete comparison to the generated baseline reject unknown/missing keys, vector drift, and every field mutation before observation. |
| T-215-42 | Elevation of Privilege | misbound allow vector | high | mitigate | Binding validation precedes `observe`, so an unknown or mismatched case cannot reach an accepted cache outcome. |
| T-215-43 | Repudiation | duplicate vector identity | medium | mitigate | Duplicate IDs fail before sorting and bounded errors identify the duplicate. |
| T-215-44 | Denial of Service | malformed schema metadata | medium | mitigate | Typed decoding and bounded contract errors fail cleanly without cache replacement. |

## ASVS L1 Control Mapping

**security_block_on: high.** Canonical binding and pre-observation ordering tests block completion on every high-severity failure.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable as signed fixture binding | Existing account/device/thumbprint checks execute only after canonical vector validation. |
| V3 Session Management | Applicable as version metadata | Contract version drift is rejected before ordering/high-water observation. |
| V4 Access Control | Applicable | A misbound allow cannot become an accepted canonical result. |
| V5 Input Validation | Applicable | Swift validates exact top-level/per-vector keys, required/optional presence, exact vector identity sets, every generated field value, unique IDs, known case IDs, versions, and dispositions. |
| V6 Cryptography | Inherited | CryptoKit ES256 verification reruns unchanged after metadata validation. |
</threat_model>

<source_audit>
GOAL truth 2, REQ RSCH-02, and RAIL-05's shared Swift contract lane are covered by the single tracer. CONTEXT D-05/D-06/D-11/D-12 and RESEARCH's language-neutral, strict-validation, no-new-package, and fail-closed feasibility constraints are preserved. This closes the remaining Swift portion of VERIFICATION blocker 2 without entering runtime issuance/reconciliation. The unclassified RSCH-02/RAIL-05 assumptions and descriptor-less prohibitions remain visible. No external API or ORM surface changes; existing `COVERAGE.md` remains the deterministic API checkpoint.
</source_audit>

<verification>
Run `swift test --filter GoldenVectorTests`; require focused mutations for every top-level/vector field, missing/extra keys and vectors, duplicate/renamed IDs, both optional-field-presence directions, and a valid-but-substituted compact JWS, with validation ordered before observation and bounded vector/field diagnostics. Then prove `capability-report.json` has no diff. Validate rather than rewrite `COVERAGE.md` because no external API integration is introduced.
</verification>

<success_criteria>
Both languages now enforce the same exact corpus schema, generated vector identity/value bindings, and canonical case/version/disposition contract before observation, and Swift contract success remains separate from unavailable Crosswake/device feasibility.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-10-SUMMARY.md` when done.
</output>

---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 12
type: execute
wave: 10
depends_on:
  - 215-11
files_modified:
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
autonomous: true
gap_closure: true
requirements:
  - RSCH-01
  - RSCH-02
  - RSCH-03
  - RAIL-04
  - RAIL-05
must_haves:
  truths:
    - "The public ProofHighWater boundary admits a signed denial at the same revision as an existing allow while rejecting an equal-revision allow and every older candidate, per D-10/D-11/D-13."
    - "ProofHighWater and AtomicOfflineCache delegate revision/disposition admission to one shared rule, so public high-water checks cannot contradict the authenticated cache replacement boundary."
    - "Beginning from allow at revision n, a same-revision signed denial is committed and remains authoritative against stale allow evidence after cache reconstruction and a separate-process reconnect-style replacement attempt."
    - "ProofHighWater retains non-decreasing issued-at and freshness checks in addition to denial-precedence ordering; lifecycle/network triggers remain reconciliation signals and never bypass authenticated replacement per D-13."
    - "A public CapabilityReport with any schema version other than 1.0 cannot reduce to proven, matching the already fail-closed checked-in report validator and preserving an honest D-12 feasibility result."
    - "Previously verified RSCH-01, RSCH-02, RSCH-03, and RAIL-04 contracts remain green under the complete Phase-215 regression gate."
    - statement: "RAIL-05 remains edge-unclassified by the spec-less probe; focused executable behavior is derived only from D-10/D-11/D-13 and the authoritative verification gap, not from a fabricated check descriptor."
      verification: backstop
  artifacts:
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      provides: "One disposition-aware admission rule shared by public ProofHighWater and authenticated AtomicOfflineCache, plus schema-aware CapabilityReport reduction"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      provides: "Focused public-boundary and same-handle deny-precedence regressions"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift"
      provides: "Allow-n to deny-n to stale-allow separate-process persistence regression"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift"
      provides: "Unsupported-schema public report fail-closed regression"
  key_links:
    - from: "ProofHighWater.accepts(newer:)"
      to: "AtomicOfflineCache.replace(with:disposition:revision:fault:)"
      via: "both call the same disposition-aware revision-order predicate before accepting a replacement"
      pattern: "ProofReplacementOrder|accepts"
    - from: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift"
      to: "authenticated cache envelope"
      via: "fresh child processes commit allow n, commit deny n, then attempt stale allow and reopen the canonical envelope"
      pattern: "replace|allow|deny|revision"
    - from: "CapabilityReport.init(schemaVersion:capabilities:)"
      to: "CheckedInCapabilityReportValidator.validate(_:)"
      via: "both recognize only schema 1.0 as eligible for a proven feasibility state"
      pattern: "schemaVersion|1.0|proven"
  assumptions:
    - requirement_id: RAIL-05
      statement: "The spec-less probe classified the RAIL-05 edge as unclassified; the authoritative verification gap supplies the concrete equal-revision denial behavior, while the missing Crosswake bridge/device evidence continues to require feasibility_blocked."
      status: unresolved
      flagged: true
    - requirement_id: RSCH-02
      statement: "The probe's RSCH-02 unclassified label is unrelated to this public high-water repair; the existing canonical case/vector gates are rerun but no new reducer or encoding criterion is inferred."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT leave the public high-water predicate as an independent ordering implementation or permit stale evidence to undo a same-revision signed denial."
    - requirement_id: RAIL-05
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT report an unsupported capability schema as proven or use native-only repairs to lift the existing feasibility_blocked result."
---

<objective>
Close the last Phase-215 blocker by making the public proof high-water disposition-aware, routing it and authenticated cache replacement through one ordering rule, and proving same-revision denial precedence survives restart/reconnect-style work.

Decision coverage: D-10 requires one monotonic public high-water and durable replacement boundary; D-11 requires deny-precedence, rollback, and restart proof; D-12 keeps missing Crosswake/device proof explicitly feasibility_blocked; D-13 admits only verified newer allow or signed same/newer denial and keeps lifecycle/network events non-authoritative.

Purpose: Remove the contradiction between the public client contract and the already-correct authenticated cache behavior before Phase 219 relies on the tracer, while closing the cohesive public capability-schema warning.
Output: A shared ordering predicate, disposition-aware ProofHighWater, targeted same-revision denial tests across public/cache/process boundaries, schema-aware CapabilityReport reduction, and a complete Phase-215 regression pass.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-PATTERNS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-11-SUMMARY.md
@examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
</context>

## Artifacts this phase produces

- New production symbols: internal `ProofReplacementOrder` (or an equivalently named single-purpose internal value) as the sole revision/disposition admission implementation.
- Modified public symbols: `ProofHighWater` gains the signed allow/deny disposition needed to decide equal-revision precedence; `ProofHighWater.accepts(newer:)` retains its public high-water role and delegates ordering to `ProofReplacementOrder`. `CapabilityReport.init(schemaVersion:capabilities:)` becomes schema-aware.
- New test symbols: focused Swift Testing cases for public equal-revision denial admission, shared public/cache behavior, separate-process allow-n → deny-n → stale-allow persistence, and unsupported CapabilityReport schema rejection. Exact test function names may follow the existing suite's sentence-style naming.
- Modified file paths: `examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift`, `examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift`, `examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift`, and `examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift`.
- New files: none.
- New generated artifacts or fixture schemas: none; `capability-report.json` and the canonical offline corpus must remain byte-unchanged.
- New Elixir modules, database/ORM schemas, migrations, dependencies, configuration keys, documentation pages, or UI symbols: none.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace allow revision n through same-revision denial and restart-safe stale-evidence rejection</name>
  <reversibility rating="costly">ProofHighWater is a public tracer contract intended for Phase 219 consumption, so changing its disposition shape affects later client integration call sites even though no production persistence migration exists.</reversibility>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (the sole remaining blocker, ProofHighWater link failure, and public-boundary evidence)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-10 through D-13)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md (rollback/deny-precedence and native-client responsibility constraints)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-11-SUMMARY.md (authenticated envelope, lock order, and process-harness implementation actually shipped)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`AtomicOfflineCache.replace`, its private comparison, `CacheCoordinator.accepts`, `VerifiedEntitlementReplacement`, and `ProofHighWater`)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift (same-handle authenticated cache and denial-precedence patterns)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift (fresh child-process harness pattern)
  </read_first>
  <behavior>
    - Test 1: existing public high-water allow at revision n accepts a signed denial at revision n when issued-at and freshness are non-decreasing.
    - Test 2: existing deny at revision n rejects allow at revision n; any candidate below n rejects; a verified allow above n accepts when issued-at/freshness are non-decreasing.
    - Test 3: AtomicOfflineCache applies the identical revision/disposition result because it calls the same shared predicate, not a copied branch table.
    - Test 4: fresh process A commits allow revision n, process B commits signed denial revision n, and process C attempts an older or equal-revision allow; reopening preserves the authenticated denial payload, revision, disposition, and tag with no candidate residue.
    - Test 5: lifecycle/reconnect signaling has no direct state-adoption path; replacement still requires the verified disposition/revision boundary.
  </behavior>
  <action>Write the focused Swift tests first so the current strict-greater public method fails on the allow-n → deny-n case. Add disposition to `ProofHighWater` using the existing closed `AtomicOfflineCache.Disposition` vocabulary, then introduce one internal pure `ProofReplacementOrder` predicate that implements D-13: a higher revision wins, a lower revision loses, and at equal revision only deny over a non-deny state wins. Make both `ProofHighWater.accepts(newer:)` and the authenticated `AtomicOfflineCache.replace` path call that predicate; retain ProofHighWater's non-decreasing `issuedAt` and `freshnessDeadline` checks around the shared revision/disposition result. Remove duplicated verified-path comparison logic rather than maintaining parallel rules. Preserve the explicitly isolated raw-byte `.max` compatibility seam only for its existing fault tests and keep it outside verified authenticated admission. Extend the process regression to begin with an authenticated allow at revision n, admit an authenticated signed denial at the same revision in a new process, then attempt stale/equal allow evidence in another new process and reopen; assert exact denial envelope metadata/payload plus empty candidate set. Do not add lifecycle authority, Crosswake APIs, a second cache, new fixture fields, or any dependency.</action>
  <acceptance_criteria>
    - A focused public test starts with allow at revision n and proves `ProofHighWater.accepts(newer:)` admits a same-revision signed denial.
    - Equal-revision allow and every older candidate reject; newer allow and same/newer denial obey D-13 while issued-at/freshness remain monotonic.
    - Source inspection shows the public high-water and authenticated cache path invoke one shared revision/disposition predicate; no second verified-path branch table remains.
    - A separate-process regression commits allow n, commits deny n, attempts stale/equal allow, and observes the authenticated deny n unchanged after reopen with no abandoned candidate.
    - The raw-byte compatibility helper remains test-only and cannot be selected by a revisioned authenticated replacement.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter GoldenVectorTests &amp;&amp; swift test --filter AtomicOfflineCacheProcessTests</automated>
  </verify>
  <done>The public high-water and authenticated cache share one D-13 ordering rule, and allow n → signed denial n → restart/reconnect-style stale allow proves denial remains authoritative end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail unsupported public report schemas closed and rerun the complete Phase-215 contract</name>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (CapabilityReport warning and already-verified phase truths)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-12 prove-or-block rule)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`CapabilityReport.reduce` and `CheckedInCapabilityReportValidator.validate`)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
    - examples/crosswake_tracer/capability-report.json
    - scripts/ci/verify_v159_authority.sh
    - scripts/ci/verify_entitlement_source_matrix.sh
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
  </read_first>
  <behavior>
    - Test 1: schema 1.0 plus the exact all-proven capability/evidence set reduces to proven.
    - Test 2: an unsupported schema plus the same all-proven rows reduces to feasibility_blocked rather than proven.
    - Test 3: missing, duplicate, partial, or insufficient-evidence rows remain feasibility_blocked, and the checked-in validator continues to reject incoherent artifacts.
    - Test 4: the complete Swift, authority, decision-case/vector, and source-matrix gates remain green and the checked-in capability report remains byte-unchanged and feasibility_blocked.
  </behavior>
  <action>Add the failing unsupported-schema regression, then make public `CapabilityReport` reduction require the supported schema version before it can return `.proven`, matching the existing checked-in validator. Keep the current non-throwing public initializer and use `.feasibilityBlocked` as its fail-closed result for unsupported versions; do not create a third terminal state or broaden accepted schema aliases. Retain exact required-row, uniqueness, and evidence-superset rules. Run the focused capability tests, then the complete Phase-215 automated gate to prove the high-water repair did not alter RSCH-01 authority/watchlist, RSCH-02 canonical cases/vectors, RSCH-03 triggers, RAIL-04 source isolation, or the honest RAIL-05 feasibility block. Require `capability-report.json` and generated contract files to remain byte-clean.</action>
  <acceptance_criteria>
    - `CapabilityReport(schemaVersion: "1.0", complete proven rows)` is proven and the same report under any unsupported version is feasibility_blocked.
    - Public reduction and checked-in validation agree that only schema 1.0 is eligible for proven status.
    - Exact capability/evidence and coherent-terminal-state tests remain green.
    - All Phase-215 authority, source, decision-case, Elixir vector, and Swift gates exit 0 with no generated/report drift.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter CapabilityReportTests &amp;&amp; swift test &amp;&amp; cd ../.. &amp;&amp; bash scripts/ci/verify_v159_authority.sh &amp;&amp; bash scripts/ci/verify_entitlement_source_matrix.sh &amp;&amp; test -s .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md &amp;&amp; rg -q 'No external API integration' .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md &amp;&amp; cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs test/property/entitlement_decision_cases_property_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs &amp;&amp; cd .. &amp;&amp; git diff --exit-code -- accrue/priv/entitlements/v1.59-decision-cases.json accrue/priv/entitlements/v1.59-offline-golden-vectors.json examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>Unsupported public report schemas fail closed, all five Phase-215 requirements retain executable coverage, and the checked-in prove-or-block artifact remains unchanged and honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified server proof → public high-water | Signed disposition and monotonic claims cross into the client admission contract and must not resurrect access or discard an authoritative denial. |
| public high-water → authenticated cache | Two public/internal entry points must enforce one ordering rule before durable replacement. |
| persisted envelope → restarted/reconnected client process | Authenticated denial metadata must be restored before any new candidate is evaluated. |
| capability-report input → feasibility status | Unsupported schema or incomplete evidence must never be represented as runtime-feasible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-51 | Tampering | ProofHighWater ordering | high | mitigate | One shared disposition-aware predicate plus focused equal/older/newer regressions prevents public/cache divergence. |
| T-215-52 | Elevation of Privilege | equal-revision replacement | high | mitigate | Allow n → deny n → stale/equal allow tests prove denial wins and access cannot resurrect. |
| T-215-53 | Tampering | restart/reconnect cache admission | high | mitigate | Separate processes restore the authenticated envelope under the existing path lock before comparing candidates. |
| T-215-54 | Spoofing | capability schema | high | mitigate | Public report reduction and checked-in validation both require exact schema 1.0 before `.proven`. |
| T-215-55 | Denial of Service | unsupported report input | low | accept | Unsupported schema returns the existing bounded feasibility_blocked outcome without throwing; no granting behavior occurs. |
| T-215-56 | Repudiation | admission result | medium | mitigate | Tests assert the exact winning revision/disposition and persisted payload across process boundaries. |

## ASVS L1 Control Mapping

**security_block_on: high.** Any failed high-severity admission, persistence, or feasibility-schema control is merge-blocking.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable | Only the already-verified signed replacement reaches disposition-aware admission; the persisted envelope remains HMAC-authenticated. |
| V3 Session Management | Applicable | Non-decreasing issued-at/freshness plus restored revision/disposition reject replay across restart/reconnect-style processing. |
| V4 Access Control | Applicable | Same-revision denial wins over allow, while equal/older allow cannot widen access. |
| V5 Input Validation | Applicable | Closed disposition and exact capability schema/version rules fail unsupported inputs closed. |
| V6 Cryptography | Applicable, unchanged | Existing ES256 vector verification and HMAC envelope suites rerun; this plan changes ordering after verification, not algorithms or keys. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One current evidence-backed contract and honest Crosswake feasibility before runtime coupling | 215-12 | COVERED | Public high-water/cache agreement closes the last blocker; unsupported report schemas cannot claim proof. |
| REQ | RSCH-01 | Versioned research authority/provenance | 215-02, 215-08, 215-12 regression | COVERED | Already verified; the complete authority gate reruns. Probe `RSCH-01 unclassified` is classification noise for this cache repair and remains an explicit no-new-criterion assumption. |
| REQ | RSCH-02 | One canonical decision contract | 215-03, 215-05..10, 215-12 regression | COVERED | Already verified; export/Elixir/Swift gates rerun unchanged. Probe `RSCH-02 unclassified` is retained as a flagged assumption; `RSCH-02 empty` and `RSCH-02 encoding` are irrelevant probe classifications because this plan adds no case or encoding field. |
| REQ | RSCH-03 | Dated watchlist and responses | 215-02, 215-08, 215-12 regression | COVERED | Already verified; authority gate reruns. Probe `RSCH-03 empty` and `RSCH-03 encoding` are irrelevant classification noise because no watchlist row or serialization changes. |
| REQ | RAIL-04 | Dedicated entitlement-source matrix | 215-04, 215-08, 215-12 regression | COVERED | Already verified; source/leakage gate reruns. Probe `RAIL-04 adjacency`, `RAIL-04 empty`, and `RAIL-04 ordering` are irrelevant because no source capability row/order changes. |
| REQ | RAIL-05 | Crosswake tracer proves or blocks high-water/replacement feasibility | 215-12 | COVERED | The authoritative verifier gap maps directly to Task 1; the public report warning maps cohesively to Task 2. Probe `RAIL-05 unclassified` remains flagged and descriptor-less. |
| RESEARCH | — | Monotonic allow/deny replacement, restart rollback proof, honest feasibility block | 215-12 | COVERED | One rule drives public/cache admission; subprocess proof and schema fail-closed behavior are executable. |
| RESEARCH | — | No invented Crosswake API, dependency, or runtime coupling | 215-12 | COVERED | Four existing Swift files only; report stays feasibility_blocked. |
| CONTEXT | D-01..D-08 | Authority and canonical decision contracts | 215-02..10, 215-12 regression | COVERED | No reimplementation; their existing gates rerun. |
| CONTEXT | D-09 | Host-owned tracer with no runtime Crosswake dependency | 215-01, 215-12 | COVERED | Repair stays inside the reference target and adds no dependency. |
| CONTEXT | D-10 | Monotonic public high-water and durable atomic replacement | 215-12 Task 1 | COVERED | Public ProofHighWater and AtomicOfflineCache share one comparator. |
| CONTEXT | D-11 | Deny precedence, rollback, and crash/restart tests | 215-12 Task 1 | COVERED | Focused same-revision and separate-process sequence closes the missing proof. |
| CONTEXT | D-12 | Missing client/device evidence remains feasibility_blocked | 215-12 Task 2 | COVERED | Unsupported schema also fails closed; checked-in report is unchanged. |
| CONTEXT | D-13 | Only verified newer allow or same/newer denial replaces state | 215-12 Task 1 | COVERED | Exact shared ordering rule and lifecycle non-authority are enforced. |
| CONTEXT | D-14..D-17 | Typed processor-free source boundary | 215-04, 215-12 regression | COVERED | Existing verified gate reruns without source-contract edits. |

All authoritative `215-VERIFICATION.md` gaps are planned. The CapabilityReport warning is included because it affects the same public prove-or-block boundary and requires no additional subsystem. All eight spec-less probe results are explicitly classified above: only `RAIL-05 unclassified` is directly relevant; `RSCH-02 unclassified` is retained as a flagged regression assumption; the remaining labels are explained as non-criteria for untouched contracts. Descriptor-less fallback prohibitions remain `verification: null` and flagged; no check descriptor is fabricated. There are no deferred CONTEXT ideas, no ORM/schema work, no UI work, and no external package install.
</source_audit>

<verification>
Run the focused public/cache/process regressions first, then CapabilityReportTests, the full Swift package, both repository CI scripts, deterministic decision export, scoped Elixir contract/property/vector tests, and byte-drift checks for all generated/report artifacts. Inspect the shared predicate call sites to confirm public ProofHighWater and authenticated AtomicOfflineCache cannot diverge. The unchanged checked-in report must remain `feasibility_blocked`; native-only success cannot authorize runtime coupling.
</verification>

<success_criteria>
The sole remaining verifier blocker is closed: a signed denial at the same revision as an allow is admitted by the public high-water, persisted by the authenticated cache, and cannot be undone by stale/equal allow evidence across process restart/reconnect-style work. Unsupported public capability schemas fail closed, and all five Phase-215 requirement gates remain green without generated artifact drift.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-12-SUMMARY.md` when done.
</output>

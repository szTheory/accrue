---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 14
type: execute
wave: 12
depends_on:
  - 215-13
files_modified:
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
autonomous: true
gap_closure: true
requirements:
  - RSCH-01
  - RSCH-02
  - RSCH-03
  - RAIL-04
  - RAIL-05
must_haves:
  truths:
    - "Caller-controlled capability statuses, evidence-kind labels, and arbitrary locations cannot produce a proven feasibility decision, per D-11/D-12."
    - "CheckedInCapabilityReportValidator.validate(reportURL:) remains the one public path that may return proven, and it does so only after report-root-aware provenance validation."
    - "The checked-in report remains feasibility_blocked until pinned Crosswake bridge and completed physical-device evidence satisfy every required lane."
    - "Previously passing RSCH-01, RSCH-02, RSCH-03, and RAIL-04 gates remain green while the final RAIL-05 false-proof route closes."
  artifacts:
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      provides: "A single provenance-validating public feasibility decision boundary"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift"
      provides: "Regressions proving arbitrary caller evidence cannot yield proven"
  key_links:
    - from: "public feasibility decision API"
      to: "CheckedInCapabilityReportValidator.validate(reportURL:)"
      via: "only a report URL establishes an evidence root and permits a proven result"
      pattern: "validate\(reportURL:"
    - from: "CapabilityReportTests"
      to: "CapabilityReport caller-controlled construction"
      via: "synthetic rows with complete status/kind labels and arbitrary locations remain feasibility_blocked"
      pattern: "arbitrary|test://native|feasibilityBlocked"
  assumptions:
    - requirement_id: RAIL-05
      statement: "The spec-less edge probe classified RAIL-05 as unclassified because the authoritative Crosswake API remains unavailable; D-09 through D-12 and the verification blocker define this local proof-boundary repair without inventing a Crosswake API or edge descriptor."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RAIL-05
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT let caller-controlled status labels, evidence-kind labels, or arbitrary evidence locations manufacture proven Crosswake feasibility."
---

<objective>
Close the final Phase-215 blocker by removing the public data-only false-proof authority and retaining one provenance-validating feasibility decision path.

Decision coverage: D-09 keeps the change inside the checked-in host-owned Swift tracer without adding Crosswake coupling; D-10 preserves the required capability lanes; D-11 requires evidence-backed proof and regression coverage; D-12 keeps missing bridge/device provenance feasibility_blocked. D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16, and D-17 remain implemented by prior plans and are protected by the existing authority, decision-export, source-matrix, and Swift regression gates.

Purpose: Ensure later runtime code cannot choose a caller-populated report reducer instead of the checked-in provenance validator when deciding whether Crosswake coupling is feasible.
Output: A fail-closed untrusted report surface, the existing URL-aware validator as the sole proven-producing public boundary, and focused regressions for arbitrary locations.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-PATTERNS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-13-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
@examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
@examples/crosswake_tracer/capability-report.json
</context>

## Artifacts this phase produces

- Modified production symbols in `AccrueOfflineClient.swift`: `CapabilityReport` becomes explicitly untrusted/fail-closed (or loses its public proof-bearing construction/reduction surface), while `CheckedInCapabilityReportValidator.validate(reportURL:)` remains the sole public operation capable of returning `.proven` after provenance validation.
- Modified test symbols in `CapabilityReportTests.swift`: replace `allProvenProducesProvenReport` with a regression proving complete caller-supplied status/kind labels at arbitrary locations cannot yield `.proven`; retain unsupported-schema, missing-lane, ordering, checked-in-blocked, and validator mutation coverage.
- New files, dependencies, package installs, external API integrations, environment variables, ORM/schema changes, schema pushes, migrations, UI symbols, Crosswake APIs, and runtime coupling: none.
- Unchanged artifacts: `examples/crosswake_tracer/capability-report.json`, `examples/crosswake_tracer/physical-device-evidence.md`, research authority documents, decision-case/vector corpora, and entitlement-source registry/matrix.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace arbitrary caller evidence to a blocked decision and reserve proven for validated provenance</name>
  <reversibility rating="reversible">This narrows a reference-client proof surface before runtime coupling; undo is confined to two Swift files and requires no persisted-data migration or dependency change.</reversibility>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (failed truths 4 and 19, hollow public-feasibility data flow, and exact missing closure)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-09 through D-12 prove-or-block boundary)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-13-SUMMARY.md (already-shipped evidence-root validator and blocked checked-in report)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md (reasoned no-external-API declaration to preserve)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`CapabilityEvidence`, `CapabilityReport.init`, `CapabilityReport.reduce`, and `CheckedInCapabilityReportValidator.validate(reportURL:)`)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift (current synthetic false-proof test and checked-in validator mutations)
    - examples/crosswake_tracer/capability-report.json (current honest feasibility_blocked artifact)
  </read_first>
  <behavior>
    - Test 1: a complete set of caller-created rows marked proven, carrying every required evidence-kind label and arbitrary locations such as `test://native`, cannot produce `.proven`.
    - Test 2: varying an arbitrary location among URL-like text, a nonexistent relative path, an absolute path, and a path outside the report root cannot make the untrusted/data-only surface produce `.proven`.
    - Test 3: unsupported schema, incomplete capability coverage, and any blocked row remain feasibility_blocked.
    - Test 4: `CheckedInCapabilityReportValidator.validate(reportURL:)` continues to accept the current checked-in artifact only as feasibility_blocked and continues to reject false-proven mutations whose evidence lacks valid provenance.
    - Test 5: a proven result remains representable only through the validator's complete, contained, kind-specific evidence checks; no second public reducer or decoder may independently assert it.
  </behavior>
  <action>Start by changing the existing synthetic success test into a failing regression that feeds all required capabilities, caller-selected `.proven` statuses, exact required evidence-kind labels, and arbitrary locations through the public data-only surface, expecting `.feasibilityBlocked`. Add a small table of materially different arbitrary locations so the test proves location text is not treated as provenance. Then remove or restrict the proof-bearing `CapabilityReport.init`/`reduce` route: either keep `CapabilityReport` as an explicitly untrusted draft whose derived status is always fail-closed, or make the caller-populatable draft internal and unable to expose a proven decision. Preserve its deterministic capability ordering if the draft remains. Keep `CheckedInCapabilityReportValidator.validate(reportURL:)` as the one public route that may return `.proven`, because the URL supplies the evidence root used by the existing containment, file, evidence-kind, terminal-reason, and physical-device checks. Do not add a bytes-only public validator, do not trust decoded `overallStatus`, do not weaken the validator, and do not manufacture evidence files. Retain the current checked-in report byte-for-byte and feasibility_blocked per D-11/D-12. Preserve the reasoned `COVERAGE.md` declaration because this closure adds no external API integration. Rerun the focused and full Swift suites plus the already-green authority, decision-export, source-matrix, and API-coverage gates; do not modify the warning-only Elixir files.</action>
  <acceptance_criteria>
    - Complete caller-controlled statuses and evidence-kind labels paired with every tested arbitrary location remain feasibility_blocked.
    - No public caller-populatable value initializer, reducer, or decoder can independently emit a proven feasibility decision.
    - `CheckedInCapabilityReportValidator.validate(reportURL:)` is the sole public proven-producing path and still requires complete provenance-validating evidence checks.
    - The checked-in report remains byte-unchanged and feasibility_blocked; no unavailable bridge or physical-device lane is relabeled.
    - Existing capability ordering, unsupported-schema, missing-lane, checked-in-report, and validator-mutation regressions remain green.
    - Authority/watchlist, decision-export, source-matrix/leakage, and API-coverage gates remain green; the Elixir warning-only files are untouched.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter CapabilityReportTests &amp;&amp; swift test &amp;&amp; cd ../.. &amp;&amp; bash scripts/ci/verify_v159_authority.sh &amp;&amp; bash scripts/ci/verify_entitlement_source_matrix.sh &amp;&amp; cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; cd .. &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query check.api-coverage-verify-pre 215 &amp;&amp; git diff --exit-code -- examples/crosswake_tracer/capability-report.json examples/crosswake_tracer/physical-device-evidence.md accrue/test/support/entitlements/offline_golden_vector_verifier.ex accrue/lib/accrue/entitlements/decision_cases/markdown.ex</automated>
  </verify>
  <done>The public false-proof route is gone or permanently fail-closed, arbitrary evidence locations cannot yield proven, and only the report-root-aware validator can produce an evidence-backed feasibility decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-controlled Swift values → feasibility decision | Statuses, evidence-kind labels, schemas, and locations are untrusted and cannot establish provenance. |
| checked-in report URL → evidence validator | The report URL establishes the only accepted evidence root; contained artifacts remain untrusted until kind and completeness checks pass. |
| feasibility result → later runtime coupling | A false proven result could bypass D-12 and authorize unsupported mobile coupling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-63 | Spoofing | public CapabilityReport construction/reduction | critical | mitigate | Make caller-populated reports untrusted/fail-closed and prove full synthetic labels plus arbitrary locations cannot yield proven. |
| T-215-64 | Tampering | decoded status and evidence-kind labels | high | mitigate | Reserve proven for `validate(reportURL:)`, which recomputes the decision from exact required kinds and validated artifacts rather than trusting encoded labels. |
| T-215-65 | Elevation of Privilege | feasibility result consumed by later runtime | high | mitigate | Expose one provenance-validating public decision path and keep all data-only paths blocked per D-12. |
| T-215-66 | Repudiation | evidence provenance | medium | mitigate | Retain report-root containment, kind-specific file checks, terminal reason checks, and physical-device completion validation. |
| T-215-67 | Denial of Service | malformed or incomplete draft/report | low | accept | Bounded Swift validation returns feasibility_blocked or `ValidationError.invalid`; no entitlement state or runtime coupling changes. |

## ASVS L1 Control Mapping

**security_block_on: high.** Any critical/high false-proof or alternate-authority regression is merge-blocking.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V4 Access Control | Applicable | Only the provenance-validating report-URL boundary may lift the runtime-coupling block. |
| V5 Input Validation | Applicable | Synthetic status/kind/location combinations remain blocked; report-root containment and exact kind checks remain green. |
| V8 Data Protection | Applicable | Evidence paths cannot escape the checked-in evidence root or substitute arbitrary files. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One evidence-backed multi-rail contract and honest Crosswake feasibility before runtime coupling | 215-14 | COVERED | Removes the last alternate false-proof authority. |
| REQ | RSCH-01 | Versioned authority, provenance, tradeoffs, confidence | prior plans; 215-14 regression | COVERED | Already verified; authority gate reruns. |
| REQ | RSCH-02 | Canonical evidence-to-entitlement decision contract | prior plans; 215-14 regression | COVERED | Already verified; deterministic export check reruns. |
| REQ | RSCH-03 | Dated change watchlist with owners/responses | prior plans; 215-14 regression | COVERED | Already verified; authority/watchlist gate reruns. |
| REQ | RAIL-04 | Dedicated entitlement-source capability matrix | prior plans; 215-14 regression | COVERED | Already verified; source-matrix/leakage gate reruns. |
| REQ | RAIL-05 | Checked-in Crosswake tracer proves or explicitly blocks required capabilities | 215-14 Task 1 | COVERED | Caller-controlled data can no longer manufacture proven; URL-aware provenance validation is authoritative. |
| RESEARCH | — | Capability-by-capability proof with explicit evidence, never a caller-declared works boolean | 215-14 Task 1 | COVERED | Proven is reserved for validated checked-in evidence. |
| RESEARCH | — | No invented Crosswake API, package, or runtime dependency | 215-14 Task 1 | COVERED | Local Swift boundary change only; blocked report stays unchanged. |
| CONTEXT | D-01..D-08 | Research authority and decision-case governance | prior plans; 215-14 regression | COVERED | Existing gates rerun without contract edits. |
| CONTEXT | D-09 | Host-owned tracer with no Crosswake runtime dependency | 215-14 Task 1 | COVERED | No new dependency or API assumption. |
| CONTEXT | D-10 | Complete required capability lanes | 215-14 Task 1 | COVERED | Required capability ordering/kinds remain intact. |
| CONTEXT | D-11 | Merge-blocking evidence-backed proof and regressions | 215-14 Task 1 | COVERED | Arbitrary-location regressions close the missed route. |
| CONTEXT | D-12 | Missing native/bridge/device proof remains feasibility_blocked | 215-14 Task 1 | COVERED | Data-only reports cannot lift the block. |
| CONTEXT | D-13..D-17 | Reconnect authority and source-contract behavior | prior plans; 215-14 regression | COVERED | Unchanged; full Swift and source gates rerun. |
| VERIFICATION | blocker | Public CapabilityReport manufactures proven from caller-controlled labels and arbitrary locations | 215-14 Task 1 | COVERED | Removes/restricts the route and proves arbitrary locations remain blocked. |
| API COVERAGE | — | Detector flags phase prose, but this closure adds no external API integration | existing COVERAGE.md; 215-14 verification | COVERED | Preserve the reasoned declaration; do not fabricate capability rows. |

All current verification gaps are covered without deferral. The only relevant spec-less unresolved edge assumption is `RAIL-05 unclassified`, surfaced above without a fabricated descriptor. Prohibition recall keeps the bespoke false-feasibility transparency constraint descriptor-less, unresolved, and flagged; it is not auto-dismissed. The assumption-delta scan detected no change. No supported ORM schema path, package install, UI work, or external API integration is added.
</source_audit>

<verification>
Run the focused arbitrary-location/proven-boundary regression first, then the full Swift package. Re-run the authority/watchlist, source-matrix/leakage, deterministic decision-export, and API-coverage gates to preserve all Phase-215 requirement coverage. Confirm the checked-in report, physical-device evidence, and warning-only Elixir files are unchanged.
</verification>

<success_criteria>
No public data-only route can manufacture `.proven`; arbitrary caller locations remain feasibility_blocked; the report-root-aware validator is the single provenance-validating public decision path; the checked-in report remains honestly blocked; and all five Phase-215 requirement gates remain green.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-14-SUMMARY.md` when done.
</output>

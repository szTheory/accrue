---
phase: 216-additive-rail-and-persistence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 216-01
files_modified:
  - accrue/lib/accrue/config.ex
  - accrue/test/accrue/config_entitlements_test.exs
autonomous: true
requirements:
  - RAIL-01
  - RAIL-02
must_haves:
  truths:
    - "Omitting both `rails` and `default_rail` preserves every legacy processor and `price_ids` validation/read path with no new required key per D-01/D-17."
    - "In explicit multi-rail mode, `default_rail` must be registered, controllable, and processor-equivalent; registration order never chooses it per D-02/D-03."
    - "`price_ids` expands only into the explicit default rail/default environment, and full `{rail, environment, product_id}` equality decides collisions per D-04/D-05/D-06."
    - "Repeated or concurrent normalization of identical config is pure and deterministic; same-plan repeats collapse, cross-plan equal tuples fail, and the same raw identifier across unequal rail/environment tuples remains distinct."
    - "Empty plan catalogs produce an empty qualified map; one entry remains one; equal entries within one plan keep one canonical mapping; output order is stable."
  artifacts:
    - path: "accrue/lib/accrue/config.ex"
      provides: "Complete rail registration, legacy alias, qualified catalog, and deterministic collision contract"
      exports: ["rails/0", "default_rail/0", "entitlement_product_catalog/0"]
    - path: "accrue/test/accrue/config_entitlements_test.exs"
      provides: "RAIL-01/02 compatibility, invalid-config, equality, ordering, idempotency, and concurrency matrix"
  key_links:
    - from: "accrue/lib/accrue/config.ex"
      to: "accrue/lib/accrue/entitlements/source/registry.ex"
      via: "production rail validation accepts Stripe/Apple and test-only host-fake without broadening the processor contract"
      pattern: "Registry.validate"
    - from: "price_ids"
      to: "entitlement_product_catalog/0"
      via: "alias expansion uses only explicit default rail and its default environment"
      pattern: "default_rail|default_environment"
  assumptions:
    - requirement_id: RAIL-02
      statement: "The fallback probe classified an additional RAIL-02 edge as unclassified; no second hidden boundary is inferred beyond the locked tuple equality, closed vocabulary, deterministic ordering, empty/singleton behavior, alias collision, and repeated/concurrent normalization checks."
      status: unresolved
      flagged: true
  prohibitions: []
---

<objective>
Expand the tracer into the complete additive registration and qualified-product contract per D-01 through D-06 and prove legacy compatibility per D-17.

Purpose: Make all default-selection, alias, equality, collision, empty, ordering, repeat, and concurrent-read behavior deterministic at boot without changing existing single-processor hosts.
Output: Complete `Accrue.Config` normalization/validation behavior and its exhaustive fast test matrix.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-RESEARCH.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-PATTERNS.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-01-SUMMARY.md
@accrue/lib/accrue/config.ex
@accrue/test/accrue/config_entitlements_test.exs
</context>

## Artifacts this phase produces

- Final public config fields: `:rails`, `:default_rail`, per-rail `:source`, optional `:processor`, `:environments`, `:default_environment`, per-plan nested `:products`, plus unchanged `:processor` and `:price_ids`.
- Final config symbols: `Accrue.Config.rails/0`, `default_rail/0`, `entitlement_product_catalog/0`, and private validators/normalizers for rail agreement, environments, alias expansion, tuple collision, stable ordering, and repeat-safe normalization.
- Config test cases for legacy-only, Stripe+Apple, host-fake proof, missing/unregistered/non-controllable/mismatched defaults, unsupported production rails, sandbox/production separation, same-plan deduplication, cross-plan tuple collision, alias collision, empty/singleton catalogs, stable order, repeat idempotency, and concurrent reads.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete rail registration and legacy default alias semantics</name>
  <reversibility rating="costly">The additive public rail/default keys and their boot errors will be copied into host configuration and consumed by later phases per D-01/D-02.</reversibility>
  <files>accrue/lib/accrue/config.ex, accrue/test/accrue/config_entitlements_test.exs</files>
  <read_first>
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md (D-01 through D-03, D-17)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-01-SUMMARY.md (tracer interfaces actually shipped)
    - accrue/lib/accrue/config.ex (`@schema`, `validate_at_boot!/0`, `maybe_validate_boot_setup!/1`)
    - accrue/lib/accrue/entitlements/source/registry.ex (`sources/0` and `validate/1`)
    - accrue/lib/accrue/processor.ex (legacy behaviour and runtime dispatch contract)
    - accrue/test/accrue/config_entitlements_test.exs (env isolation pattern)
  </read_first>
  <behavior>
    - Test 1: configs omitting both new keys, including a custom legacy processor and old `price_ids`, behave byte-for-byte as before.
    - Test 2: explicit Stripe+Apple registration accepts Stripe as the only controllable default when its processor equals legacy `:processor`; Apple never needs or accepts processor coercion.
    - Test 3: missing, unregistered, Apple/non-controllable, and processor-mismatched defaults raise deterministic `Accrue.ConfigError` messages naming the rail and expected relationship.
    - Test 4: reversing the rail keyword order does not change the resolved default; repeated validation and concurrent runtime reads return equal values without mutating application env.
    - Test 5: `:host_fake` is accepted only in test/proof configuration and never appears as a third production rail.
  </behavior>
  <action>Write the invalid/legacy/idempotency/concurrent-read cases first. Complete the tracer's rail schema and cross-field validator per D-01/D-02/D-03: absence of both new keys bypasses multi-rail normalization and preserves the existing processor path; presence of either activates explicit mode and requires a named registered controllable default with exact processor-module agreement. Validate the closed production sources as Stripe and Apple while retaining host-fake only under the existing deterministic test/proof seam. Keep runtime reads pure and order-independent; do not write normalized values back into application env or turn Apple into an `Accrue.Processor`. Preserve existing Stripe secret checks and every unrelated config default.</action>
  <acceptance_criteria>
    - Every legacy-only test that existed before Phase 216 remains green without adding config keys.
    - Every ambiguous or contradictory explicit configuration fails at boot with a stable actionable error.
    - Rail list order, repeated calls, and concurrent reads do not change the resolved result.
    - The production vocabulary contains exactly Stripe and Apple; host-fake remains visibly test/proof-only.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/config_entitlements_test.exs</automated>
  </verify>
  <done>RAIL-01 is fully enforced while existing single-processor hosts remain valid and Apple stays outside the gateway behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete qualified catalog normalization and collision semantics</name>
  <reversibility rating="costly">The nested `products` shape and tuple equality become the host-facing catalog contract and later projector lookup key per D-04.</reversibility>
  <files>accrue/lib/accrue/config.ex, accrue/test/accrue/config_entitlements_test.exs</files>
  <read_first>
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md (D-04 through D-06)
    - accrue/lib/accrue/config.ex (tracer catalog accessor and existing `validate_entitlements_price_ids!/1`)
    - accrue/lib/accrue/entitlements/decision_cases.ex (closed `:production`/`:sandbox`; `:offline` exclusion)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (legacy raw `price_ids` behavior that must not change)
    - accrue/test/accrue/entitlements/local_map_test.exs (legacy compatibility assertions)
  </read_first>
  <behavior>
    - Test 1: nested Stripe/Apple production/sandbox product IDs normalize to a stably ordered map keyed by full tuples.
    - Test 2: identical raw IDs on different rails or environments remain distinct; `:offline` and unknown rail/environment entries fail boot validation.
    - Test 3: equal qualified tuples repeated inside one plan collapse; the same tuple across plans raises an error naming both plans and the complete tuple.
    - Test 4: a bare `price_ids` alias expands only to the explicit default rail/default environment and conflicts with a qualified entry for another plan.
    - Test 5: empty and singleton catalogs have exact empty/singleton results; input keyword order and repeated/concurrent normalization do not affect equality or output order.
  </behavior>
  <action>Extend the cross-plan reducer per D-04/D-05/D-06. Flatten each plan's nested `products` into `{rail, environment, product_id}` keys after closed-vocabulary validation, expand legacy `price_ids` only when explicit multi-rail mode supplies a default rail/default environment, normalize same-plan repeats to one mapping, and reject only exact tuple collisions across plans. Keep legacy-only LocalMap behavior unchanged rather than forcing custom processors into the new production-rail vocabulary. Return a deterministic ordering and emit errors that contain both logical plans plus the full tuple. Do not accept `:offline` as a provider catalog environment or let bare identifiers fan out across rails.</action>
  <acceptance_criteria>
    - The full collision matrix passes and errors identify the exact tuple and both plans.
    - Empty, singleton, equal, distinct, reordered, repeated, and concurrent cases are explicitly asserted.
    - Existing LocalMap tests remain green and still consume legacy `price_ids` directly.
    - `entitlement_product_catalog/0` is deterministic and contains no unqualified key.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements/local_map_test.exs</automated>
  </verify>
  <done>RAIL-02 is fully represented by collision-safe qualified tuples while `price_ids` remains a narrow default alias and legacy behavior stays intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host config → boot validator | Untrusted/mistyped rail and product catalog values become runtime routing identity. |
| alias config → qualified catalog | Bare legacy IDs acquire a specific rail/environment identity. |
| runtime readers → application env | Concurrent callers read host-owned mutable application configuration. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-216-04 | Tampering | default-rail selection | high | mitigate | D-02 explicit registration/controllability/module agreement rejects ambiguity; tests reverse config order and cover every contradiction. |
| T-216-05 | Tampering | product catalog identity | high | mitigate | Closed rail/environment vocabulary and exact tuple collision reducer prevent sandbox-as-production and cross-plan alias confusion per D-04/D-06. |
| T-216-06 | Denial of service | boot normalization | medium | mitigate | Use bounded linear flatten/reduce/sort over host config, no provider calls, and fail once with deterministic compact errors. |
| T-216-07 | Repudiation | config errors | low | mitigate | Errors name both logical plans and the full qualified tuple without emitting secrets. |
| T-216-SC | Tampering | package-manager installs | low | accept | No npm, pip, cargo, or new Mix package installation occurs in this plan. |
</threat_model>

<verification>
Run the config and LocalMap suites, then run the targeted suite twice to prove idempotency. Confirm the new accessors read runtime config without application-env writes and scan errors for complete tuples without secret values.
</verification>

<success_criteria>
- RAIL-01 and RAIL-02 are covered for happy, invalid, empty, singleton, equality, ordering, idempotency, and concurrent-read behavior.
- D-01 through D-06 and D-17 are cited and tested without altering gateway dispatch or LocalMap decisions.
- The RAIL-02 unclassified fallback item remains explicitly flagged rather than silently dismissed.
</success_criteria>

<output>
Create `.planning/phases/216-additive-rail-and-persistence-foundation/216-02-SUMMARY.md` when done.
</output>

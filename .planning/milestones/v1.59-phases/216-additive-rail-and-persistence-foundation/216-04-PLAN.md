---
phase: 216-additive-rail-and-persistence-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 216-02
  - 216-03
files_modified:
  - accrue/test/support/entitlements/fixtures.ex
  - accrue/test/accrue/entitlements/fake_fixture_test.exs
  - accrue/priv/accrue/templates/install/runtime_config.exs.eex
  - accrue/guides/entitlements.md
  - accrue/test/mix/tasks/accrue_install_test.exs
  - accrue/test/accrue/docs/entitlements_guide_test.exs
autonomous: true
requirements:
  - RAIL-01
  - RAIL-02
  - RAIL-03
must_haves:
  truths:
    - "Deterministic host-fake fixtures cover Stripe and Apple, production and sandbox, tuple collisions, duplicate observation idempotency, current-grant uniqueness, account switching, and durable device revocation without live services per D-16."
    - "The installer copies the completed additive migration and emits an opt-in concurrent-rail/catalog example while its existing legacy processor example remains valid per D-17."
    - "The entitlement guide explains the supported legacy alias, qualified tuple equality, privacy-safe persistence boundary, and later-phase exclusions; an automated guide contract proves Apple is an observer/non-processor with no Phase-216 lifecycle or signed-material verification authority."
    - "Running fixture and installer generation repeatedly is byte-stable and does not duplicate config blocks or migration files."
  artifacts:
    - path: "accrue/test/support/entitlements/fixtures.ex"
      provides: "Deterministic multi-rail config and persistence attribute/scenario fixtures"
      exports: ["legacy_config/0", "multi_rail_config/0", "account_attrs/1", "observation_attrs/2", "grant_attrs/2", "device_attrs/2", "scenario/1"]
    - path: "accrue/test/accrue/entitlements/fake_fixture_test.exs"
      provides: "D-16 fixture coverage and stable-ID checks"
    - path: "accrue/priv/accrue/templates/install/runtime_config.exs.eex"
      provides: "Legacy-default plus opt-in multi-rail runtime configuration example"
    - path: "accrue/guides/entitlements.md"
      provides: "Host-facing registration, catalog, persistence, privacy, and scope guidance"
    - path: "accrue/test/mix/tasks/accrue_install_test.exs"
      provides: "Generated host config/migration propagation and repeat-safety proof"
    - path: "accrue/test/accrue/docs/entitlements_guide_test.exs"
      provides: "Deterministic D-03/D-11 guide-content proof for Apple observer/non-processor and later-phase authority exclusions"
  key_links:
    - from: "accrue/test/support/entitlements/fixtures.ex"
      to: "accrue/lib/accrue/entitlements/decision_cases.ex"
      via: "fixture rails/environments use the Phase-215 closed vocabulary rather than redefining it"
      pattern: "DecisionCases"
    - from: "accrue/lib/accrue/install/templates.ex"
      to: "accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs"
      via: "existing wildcard migration copier includes the new file without bespoke copy code"
      pattern: "Path.wildcard"
    - from: "accrue/priv/accrue/templates/install/runtime_config.exs.eex"
      to: "accrue/lib/accrue/config.ex"
      via: "generated example uses the exact validated config keys"
      pattern: "rails|default_rail|products"
    - from: "accrue/test/accrue/docs/entitlements_guide_test.exs"
      to: "accrue/guides/entitlements.md"
      via: "literal required anchors and prohibited authority claims make D-03/D-11 documentation semantics executable"
      pattern: "observer|Accrue.Processor|verify|lifecycle"
  prohibitions: []
---

<objective>
Close the phase with deterministic fake-first proof and host-facing propagation of the complete additive configuration and persistence contract per D-16/D-17.

Purpose: Ensure downstream phases and adopters receive one reproducible fixture vocabulary, one correct installer path, and one honest guide while legacy generated hosts remain compatible.
Output: Multi-rail/persistence fixtures, fixture tests, installer template/test updates, and entitlement guide updates.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-RESEARCH.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-PATTERNS.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-02-SUMMARY.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-03-SUMMARY.md
@accrue/lib/accrue/install/templates.ex
@accrue/test/support/install_fixture.ex
</context>

## Artifacts this phase produces

- Fixture symbols: `Accrue.Test.EntitlementFixtures.legacy_config/0`, `multi_rail_config/0`, `account_attrs/1`, `observation_attrs/2`, `grant_attrs/2`, `device_attrs/2`, and named `scenario/1` cases for both rails/environments and every D-16 collision/history outcome.
- Fixture files: `accrue/test/support/entitlements/fixtures.ex` and `accrue/test/accrue/entitlements/fake_fixture_test.exs`.
- Installer/example files: updated `accrue/priv/accrue/templates/install/runtime_config.exs.eex`, generated-host assertions in `accrue/test/mix/tasks/accrue_install_test.exs`, updated `accrue/guides/entitlements.md`, and focused guide assertions in `accrue/test/accrue/docs/entitlements_guide_test.exs`.
- Generated-host artifact (via the existing wildcard): `priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs` under each installed host's migration directory; no new installer flag or bespoke copier is added.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship deterministic multi-rail record fixtures and scenario proofs</name>
  <reversibility rating="reversible">The fixture module is test-only and can evolve behind its named functions without migrating runtime state.</reversibility>
  <files>accrue/test/support/entitlements/fixtures.ex, accrue/test/accrue/entitlements/fake_fixture_test.exs</files>
  <read_first>
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md (D-03, D-06, D-09, D-10, D-14 through D-16)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-02-SUMMARY.md (final config syntax and catalog accessors)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-03-SUMMARY.md (final schema fields/constraint keys)
    - accrue/lib/accrue/entitlements/decision_cases.ex (closed vocabulary and deterministic case style)
    - accrue/test/support/stripe_fixtures.ex (fixture module naming/default override pattern)
    - accrue/test/accrue/entitlements/decision_cases_test.exs (stable sorted unique fixture assertions)
  </read_first>
  <behavior>
    - Test 1: config fixtures contain legacy-only and concurrent Stripe+Apple shapes with deterministic defaults and no live credential.
    - Test 2: record fixtures span Stripe/Apple × production/sandbox and use stable opaque fake IDs, digests, bounded metadata, revisions, provider order, and timestamps.
    - Test 3: named scenarios exercise qualified collision rejection, duplicate observation convergence, current grant conflict/supersession, account-switched installation/key reuse, and revocation/re-registration history.
    - Test 4: fixture calls repeat to structurally equal values, scenario names are unique/stably ordered, and no receipt/JWS/notification body, adopter identity, PII, or live-provider dependency appears.
  </behavior>
  <action>Write the fixture-contract tests first, then add `Accrue.Test.EntitlementFixtures` per D-16. Derive rail/environment values from the Phase-215 contract and emit deterministic maps for the final config and all four Phase-216 records. Use fixed fake UUIDs/timestamps/digests and bounded redacted metadata so equality and ordering assertions do not depend on wall clock or process order. Provide named scenarios for every D-16 behavior and execute them through the real Config/Repo surfaces from Plans 216-02/03; do not add a third production rail, call Stripe/Apple, start Crosswake, or require a physical device.</action>
  <acceptance_criteria>
    - Every D-16 rail/environment/collision/idempotency/uniqueness/account-switch/revocation case has a stable named fixture and executable assertion.
    - Repeated fixture calls are equal and scenario order is stable.
    - Fixtures contain only fake, redacted, bounded data and use no live credentials or network/runtime dependency.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/fake_fixture_test.exs</automated>
  </verify>
  <done>Later reducers, Apple ingestion, and offline device work receive one deterministic record/config fixture vocabulary grounded in the Phase-215 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate the additive contract through installer and host guidance</name>
  <reversibility rating="costly">Generated host examples and migration filenames become adopter-visible installation contracts, though no existing generated file is rewritten automatically.</reversibility>
  <files>accrue/priv/accrue/templates/install/runtime_config.exs.eex, accrue/guides/entitlements.md, accrue/test/mix/tasks/accrue_install_test.exs, accrue/test/accrue/docs/entitlements_guide_test.exs</files>
  <read_first>
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md (D-01 through D-06, D-11 through D-17)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-02-SUMMARY.md (canonical validated config syntax)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-03-SUMMARY.md (completed migration filename/tables)
    - accrue/lib/accrue/install/templates.ex (`migration_templates/2` wildcard; do not duplicate it)
    - accrue/priv/accrue/templates/install/runtime_config.exs.eex (existing legacy processor example)
    - accrue/test/mix/tasks/accrue_install_test.exs (generated content and repeat-install pattern)
    - accrue/test/support/install_fixture.ex (`assert_contains!/3` helpers)
    - accrue/guides/entitlements.md (existing LocalMap and observational-sync contract)
    - accrue/test/accrue/docs/entitlements_guide_test.exs (create in the RED step using the adjacent guide-contract test style)
    - accrue/test/accrue/docs/organization_billing_guide_test.exs (required/forbidden guide-literal assertion precedent)
  </read_first>
  <behavior>
    - Test 1: a fresh install retains a valid active legacy `processor` example and contains a clearly opt-in Stripe+Apple/default/catalog example using exact Config keys.
    - Test 2: the generated host receives the completed entitlement persistence migration through the existing wildcard path and its content names all four tables and critical indexes.
    - Test 3: running installer generation twice remains safe under existing overwrite/fingerprint policy and does not duplicate the config block or migration.
    - Test 4: generated/guide examples pass `Accrue.Config.validate_at_boot!/0` in legacy-only and explicit multi-rail test fixtures.
    - Test 5: `entitlements_guide_test.exs` requires the guide to state that Apple is an entitlement source/observer, Apple does not implement `Accrue.Processor`, Phase 216 does not verify Apple signed material, and Phase 216 does not mutate the Apple subscription lifecycle; it also rejects affirmative claims that Apple is a processor/gateway authority or that Phase 216 owns Apple verification or lifecycle mutation.
  </behavior>
  <action>Extend installer assertions and create `Accrue.Docs.EntitlementsGuideTest` first, then update the runtime config template and entitlement guide per D-03/D-11/D-17. Keep the current `processor` example active and valid; show the new `rails`, `default_rail`, nested `products`, and `price_ids` alias as an opt-in coherent block using the exact Plan 216-02 syntax. Rely on `Templates.migration_templates/2` to copy the Plan 216-03 migration and assert the generated filename/content instead of adding copy logic. Explain full tuple equality, account/grant/observation/device tables, and bounded evidence/ref expiry. Give the guide four stable, literal semantic anchors: Apple is an entitlement source/observer; Apple does not implement `Accrue.Processor`; Phase 216 does not verify Apple signed material; Phase 216 does not mutate the Apple subscription lifecycle. Make the dedicated guide test assert those anchors and reject affirmative Apple processor/gateway, signed-material verification, or provider-lifecycle authority claims. Preserve every D-11 boundary: projector, backfill, cutover, Apple verification, offline issuance, Google Play, Family Sharing, and provider lifecycle mutation remain outside shipped Phase-216 behavior.</action>
  <acceptance_criteria>
    - Fresh generated hosts contain a valid legacy default and an exact opt-in multi-rail example.
    - The complete four-table migration is generated once through the existing wildcard mechanism.
    - Repeat generation is idempotent under existing installer safety semantics.
    - The dedicated guide test proves the four D-03/D-11 observer/non-processor and lifecycle/verification-exclusion anchors and rejects claims that grant Apple gateway or Phase-216 lifecycle/verification authority.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/mix/tasks/accrue_install_test.exs test/accrue/config_entitlements_test.exs test/accrue/docs/entitlements_guide_test.exs</automated>
  </verify>
  <done>The additive rail/catalog/persistence contract reaches installed hosts, examples, and compatibility tests without adding a required key or changing legacy behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| checked-in fixtures → tests/downstream phases | Deterministic sample evidence can normalize unsafe assumptions if it carries secrets or wrong identities. |
| installer templates → host repository | Accrue-owned config/migration content is copied into adopter-controlled code and database workflows. |
| guide → host implementation | Documentation can accidentally imply control/authority that the foundation does not provide. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-216-13 | Information disclosure | fixtures/examples | high | mitigate | Fixed fake IDs/digests and negative scans/tests prohibit credentials, raw provider evidence, adopter identity, and PII per D-12/D-16. |
| T-216-14 | Tampering | generated migration/config | medium | mitigate | Existing fingerprint/overwrite policy plus repeat-install tests preserve user edits and prevent duplicate generation. |
| T-216-15 | Elevation of privilege | Apple documentation | high | mitigate | Guide/template identify Apple as observer/external management, retain processor as controllable default, and explicitly exclude Apple verification/lifecycle control; `entitlements_guide_test.exs` asserts the required literals and rejects authority-inverting claims. |
| T-216-SC | Tampering | package-manager installs | low | accept | No npm, pip, cargo, or new Mix package installation occurs in this plan. |
</threat_model>

## Multi-source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Concurrent Stripe/Apple evidence on durable qualified records with single-processor compatibility | 216-01..04 | COVERED | Tracer proves the path; expansion and propagation close all cases. |
| REQ | RAIL-01 | Concurrent rail registration plus legacy default alias | 216-01, 216-02, 216-04 | COVERED | Happy path, invalid/compatibility matrix, installer propagation. |
| REQ | RAIL-02 | Qualified product mapping without cross-rail/environment collisions | 216-01, 216-02, 216-04 | COVERED | Tuple reducer, edge matrix, fixtures/docs. |
| REQ | RAIL-03 | Stable account, qualified observations/grants, devices, ordering/provenance/quarantine/uniqueness | 216-01, 216-03, 216-04 | COVERED | Real DB tracer, full schema/races/privacy/history, fixtures/installer. |
| RESEARCH | — | Structural validation then cross-entry normalization | 216-01, 216-02 | COVERED | Reuses `Accrue.Config` reducer pattern. |
| RESEARCH | — | Four Ecto schemas with named DB constraints/partial indexes | 216-01, 216-03 | COVERED | Prefix-safe migration and changeset parity. |
| RESEARCH | — | Separate redacted observation boundary; no webhook-row reuse | 216-03 | COVERED | Dedicated table/module and privacy negatives. |
| RESEARCH | — | Fake-first real-PostgreSQL verification and installer propagation | 216-01, 216-03, 216-04 | COVERED | RepoCase, deterministic fixtures, wildcard installer proof. |
| CONTEXT | D-01, D-02, D-03 | Alias/default/production-rail semantics | 216-01, 216-02, 216-04 | COVERED | Every action cites the decisions. |
| CONTEXT | D-04, D-05, D-06 | Nested qualified catalog and tuple collision semantics | 216-01, 216-02, 216-04 | COVERED | Costly shape recorded and tested exhaustively. |
| CONTEXT | D-07, D-08 | Four UUID records and stable owner account | 216-01, 216-03 | COVERED | DB constraints and account tracer. |
| CONTEXT | D-09, D-10 | Current grants plus idempotent observations | 216-03, 216-04 | COVERED | Partial indexes and deterministic scenarios. |
| CONTEXT | D-11 | Additive-only foundation/no backfill or read cutover | 216-01, 216-03, 216-04 | COVERED | Explicit negative scope in actions/checks. |
| CONTEXT | D-12, D-13 | Bounded redacted evidence and opaque expiring ref | 216-03, 216-04 | COVERED | Schema validation, negative tests, guide. |
| CONTEXT | D-14, D-15 | Account-scoped device identity and retained revocation | 216-03, 216-04 | COVERED | Partial current indexes plus history scenarios. |
| CONTEXT | D-16, D-17 | Fake-first proof and same-phase propagation | 216-04 | COVERED | Both tasks own these decisions. |

Probe reconciliation: all eight fallback edge rows are preserved. RAIL-01 repeat/concurrency and RAIL-03 empty/equality/ordering/repeat/concurrency are explicit truths/tests; RAIL-02 remains a flagged unclassified assumption in Plan 216-02. Prohibition recall produced no bespoke values/safety/ethics item after precision filtering; raw evidence/PII, injection, access control, and provider-confusion candidates are canon security/privacy items handled in the threat registers and later `$gsd-secure-phase`, not minted as descriptor-less prohibitions. No deferred CONTEXT item is planned. The external-API detector and assumption-delta detector both returned `detected:false`; no matrix or identity-model checkpoint is required. The exact schema-push hook does not match `accrue/priv/repo/migrations/*.exs`, so no unsupported ORM push command is invented. No package install exists.

<verification>
Run fixture, installer, config, persistence, and LocalMap tests, then the package-wide gate. Confirm a generated host includes the new migration exactly once, generated examples validate, fixture bytes are stable, and searches find no live credentials/raw evidence/PII or later-phase projector/Apple/offline runtime implementation.

<automated>cd accrue &amp;&amp; mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/persistence_test.exs test/accrue/entitlements/fake_fixture_test.exs test/mix/tasks/accrue_install_test.exs test/accrue/docs/entitlements_guide_test.exs &amp;&amp; mix test.all</automated>
</verification>

<success_criteria>
- All RAIL-01/02/03 behaviors and D-01..D-17 decisions are covered with no deferral or scope reduction.
- Installer/generated-host, examples, validation, migrations, fixtures, compatibility, and privacy gates are green.
- The full package suite passes without live Stripe, Apple sandbox, Crosswake runtime, physical device, or new dependency.
</success_criteria>

<output>
Create `.planning/phases/216-additive-rail-and-persistence-foundation/216-04-SUMMARY.md` when done.
</output>

---
phase: 217-canonical-projection-and-compatibility
plan: 02
type: execute
wave: 2
depends_on: [217-01]
files_modified:
  - accrue/lib/accrue/entitlements/snapshot.ex
  - accrue/lib/accrue/entitlements/projector.ex
  - accrue/test/property/entitlement_projection_property_test.exs
  - accrue/test/accrue/entitlements/decision_cases_test.exs
autonomous: true
requirements: [ACCT-01, ACCT-02]
must_haves:
  truths:
    - "Per D-01/D-04, snapshot output and authorization signatures are invariant to grant input order, duplicate logical grants, and metadata-only changes."
    - "Precision contract for ACCT-01: quantities are positive integers; merge uses exact integer maximum with no rounding, coercion, summation, or overflow-prone arithmetic."
    - "Idempotency contract for ACCT-02: projecting identical qualified evidence twice yields the same grants and revision as projecting it once."
    - "Concurrency contract for ACCT-02: parallel applicable/no-op observations serialize on the account and yield the same effective snapshot and revision as an accepted sequential order."
  artifacts:
    - path: "accrue/test/property/entitlement_projection_property_test.exs"
      provides: "Permutation, duplicate, survivor, max-quantity, idempotency, and concurrency invariants"
    - path: "accrue/test/accrue/entitlements/decision_cases_test.exs"
      provides: "Executable Phase-215 decision-case conformance for snapshot/revision outcomes"
    - path: "accrue/lib/accrue/entitlements/snapshot.ex"
      provides: "Hardened total deterministic fold"
    - path: "accrue/lib/accrue/entitlements/projector.ex"
      provides: "Hardened ordering and concurrency classifications"
  key_links:
    - from: "accrue/test/property/entitlement_projection_property_test.exs"
      to: "accrue/lib/accrue/entitlements/snapshot.ex"
      via: "generated grant permutations and exact integer maxima"
      pattern: "check all|authorization_signature|max"
    - from: "accrue/test/accrue/entitlements/decision_cases_test.exs"
      to: "accrue/lib/accrue/entitlements/projector.ex"
      via: "Phase-215 duplicate, stale, survivor, atomic, and revoked fixtures"
      pattern: "DecisionCases|project"
  assumptions: []
  prohibitions:
    - requirement_id: ACCT-02
      statement: "MUST NOT reconstruct authorization by replaying provider delivery order or treat an advisory Stripe entitlement summary as a grant, revision source, snapshot input, eligibility input, or gate authority."
      status: unverified
      flagged: true
---

<objective>
Expand the canonical tracer into property-checked and decision-case-checked invariants for every ACCT-01/ACCT-02 ordering, boundary, precision, idempotency, concurrency, and survivor behavior per D-01 through D-05.

Purpose: Make the canonical projection stable under real provider disorder and future refactors rather than proving only one curated path.
Output: Hardened snapshot/projector plus deterministic property and fixture conformance suites.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md
@.planning/phases/217-canonical-projection-and-compatibility/217-RESEARCH.md
@.planning/phases/217-canonical-projection-and-compatibility/217-VALIDATION.md
@.planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md
@accrue/lib/accrue/entitlements/decision_cases.ex
@accrue/priv/entitlements/v1.59-decision-cases.json
@accrue/test/accrue/entitlements/decision_cases_test.exs
@accrue/test/accrue/money_property_test.exs
</context>

## Artifacts this phase produces

- `entitlement_projection_property_test.exs` with exact integer, permutation, duplicate, survivor, idempotency, and serialized-concurrency properties.
- Extended `decision_cases_test.exs` mapping the canonical corpus to executable Snapshot/Projector results.
- Any narrow Snapshot/Projector corrections required to make the public contract total and deterministic.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove snapshot boundary, precision, and permutation invariants</name>
  <files>accrue/lib/accrue/entitlements/snapshot.ex, accrue/test/property/entitlement_projection_property_test.exs</files>
  <read_first>
    - .planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md (D-01, D-02, D-04, D-05)
    - .planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md (actual Snapshot interface and tracer decisions)
    - accrue/lib/accrue/entitlements/snapshot.ex (tracer implementation)
    - accrue/test/accrue/money_property_test.exs (StreamData/ExUnitProperties convention)
    - accrue/lib/accrue/config.ex (`entitlement_product_catalog/0` logical-plan mapping)
  </read_first>
  <behavior>
    - Test 1: every permutation of the same live grant multiset yields byte-equivalent deterministic public collections and signature.
    - Test 2: duplicate logical plans never duplicate output; feature sets union; each quota is exactly the greatest positive integer source quantity.
    - Test 3: adding metadata, diagnostic correlation, incidental IDs, duplicate grants, or input reordering leaves the authorization signature unchanged.
    - Test 4: effective/expiry/revocation cutoffs distinguish one microsecond before, exact equality, and one microsecond after according to one documented inclusive/exclusive rule.
    - Test 5: empty and all-expired inputs produce a valid empty snapshot rather than an error or provision side effect.
  </behavior>
  <action>Add the Wave 0 property suite before any hardening. Generate bounded qualified grants and compare exact output across permutations, duplicates, source overlaps, cutoff adjacency, and positive integer quantity extremes. Preserve D-01 deterministic sorted serialization and D-04's narrow authorization signature. If the tracer implementation is partial on an edge, harden it without adding persisted snapshot state, float/decimal quantity coercion, summation, provider calls, or metadata-derived authorization.</action>
  <acceptance_criteria>
    - ACCT-01 boundary and precision probes have explicit executable predicates, including exact cutoff adjacency and exact integer maximum.
    - Property runs are deterministic/reproducible under the project's seed output and never depend on live providers.
    - Authorization signature changes if and only if a D-04 semantic field changes.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/snapshot_test.exs test/property/entitlement_projection_property_test.exs</automated></verify>
  <done>Snapshot union/dedupe/max, cutoff, empty-state, and material-signature semantics hold across generated orders and duplicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove projector idempotency, concurrency, and closed decision-case conformance</name>
  <files>accrue/lib/accrue/entitlements/projector.ex, accrue/test/property/entitlement_projection_property_test.exs, accrue/test/accrue/entitlements/decision_cases_test.exs</files>
  <read_first>
    - accrue/lib/accrue/entitlements/projector.ex (tracer transaction and ordering classifications)
    - accrue/lib/accrue/entitlements/decision_cases.ex (`all/0`, `valid?/1`, duplicate/stale/survivor/atomic cases)
    - accrue/priv/entitlements/v1.59-decision-cases.json (language-neutral expected outcomes)
    - accrue/test/accrue/entitlements/decision_cases_test.exs (existing closed-corpus validation)
    - accrue/test/property/entitlement_projection_property_test.exs (Task 1 generators)
  </read_first>
  <behavior>
    - Test 1: one projection and two identical projections have equal current grants, effective snapshot, revision, and material audit count.
    - Test 2: concurrent same-account attempts serialize and converge without duplicate current grants or more than one revision per material transition.
    - Test 3: stale/out-of-order evidence never supersedes the newer qualified lineage; separate lineages remain independently ordered.
    - Test 4: every relevant Phase-215 duplicate, survivor, revocation, all-revoked, and atomic-transaction case produces its declared snapshot/revision result and stable reason.
  </behavior>
  <action>Extend the property suite to exercise repeated and concurrent real-Repo projections, and extend the closed corpus test to pass applicable Phase-215 cases through Snapshot/Projector rather than merely validate fixture shape. Preserve the account row lock, qualified-lineage ordering, database current-row uniqueness, one-revision-per-material-transaction rule, and stable reasons per D-03/D-04. Keep failures observable and tagged; do not weaken a property to match implementation or invent a second case vocabulary.</action>
  <acceptance_criteria>
    - ACCT-02 idempotency and concurrency probes are explicit and green against PostgreSQL-backed tests.
    - Every in-scope Phase-215 case has an executable canonical consumer with stable result/reason equality.
    - Parallel and sequential accepted orders converge on identical effective authorization.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/projector_test.exs test/property/entitlement_projection_property_test.exs test/accrue/entitlements/decision_cases_test.exs</automated></verify>
  <done>Projector repeats, races, provider disorder, and the shared decision corpus converge on one survivor-safe revisioned account truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated/provider-disordered grants → canonical fold | Arbitrary order, duplicates, and cutoff values must not alter meaning. |
| concurrent observations → PostgreSQL account/current-grant constraints | Parallel workers contend to update one account and lineage. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-217-05 | Tampering | snapshot fold | high | mitigate | Task 1 property-checks permutation, duplicate, boundary, and exact maximum invariants against the authorization signature. |
| T-217-06 | Denial of Service | projector contention | medium | mitigate | Task 2 uses bounded concurrent tests and the account row lock/current-row indexes to converge rather than spin or create duplicates. |
| T-217-07 | Repudiation | fixture/reason drift | medium | mitigate | Task 2 binds outcomes to the checked-in Phase-215 corpus and stable reason vocabulary. |
| T-217-SC | Tampering | package-manager installs | low | accept | No package installation occurs. |
</threat_model>

<verification>
Run each focused task command, then `cd accrue && mix test test/accrue/entitlements --exclude live_stripe` and `mix format --check-formatted` at the wave boundary.
</verification>

<success_criteria>
- All ACCT-01/02 edge probes are explicitly covered rather than inferred.
- Property and corpus consumers prove total, deterministic, survivor-safe projection.
- No provider access, persisted snapshot authority, or advisory-cache input is introduced.
</success_criteria>

<output>
Create `.planning/phases/217-canonical-projection-and-compatibility/217-02-SUMMARY.md` when done.
</output>

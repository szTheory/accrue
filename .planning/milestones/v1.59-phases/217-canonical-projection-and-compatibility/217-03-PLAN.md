---
phase: 217-canonical-projection-and-compatibility
plan: 03
type: execute
wave: 2
depends_on: [217-01]
files_modified:
  - accrue/lib/accrue/entitlements/purchase_decision.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/test/accrue/entitlements/purchase_decision_test.exs
autonomous: true
requirements: [ACCT-05]
must_haves:
  truths:
    - "Per D-06 through D-08, purchase preflight returns a typed `eligible|block|warn` result and blocks equivalent cross-rail, missing, stale, repairing, and ambiguous state by stable actionable reason."
    - "Per D-07, equivalence is derived only from a live different-rail grant resolving through the qualified catalog to the same logical plan."
    - "Per D-09, override is explicit, bounded, privacy-safe, audited, and revision-bound; continuing rechecks revision and equivalence and re-evaluates changed state."
    - "Per D-10, Stripe continuation uses a durable operation ID/provider idempotency and returns a reconcile-first result for ambiguous provider outcomes; Apple preflight never creates cross-rail mutation authority."
    - "Per D-08, an authenticated first-purchase reference with no entitlement account is provisioned through `Entitlements.provision_account/3`, fetched, and only then evaluated; unauthorized references fail before account, grant, or provider side effects."
    - "Per D-19, public purchase-decision and override facades emit named start/stop/exception spans with only bounded decision fields and opaque/hashed identifiers, with forbidden-key and seeded-value privacy-negative proof."
    - "ACCT-05 manual/unclassified probe is resolved by explicit cases for first purchase, same-rail renewal, equivalent other-rail grant, missing/stale/repairing/ambiguous state, stale override, and concurrent Apple completion."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/purchase_decision.ex"
      provides: "Typed preflight, revision-bound override, and safe continuation contract"
      exports: ["evaluate/4", "override/4", "continue/4"]
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "Public snapshot-based purchase APIs"
      exports: ["purchase_decision/4", "override_purchase_decision/4"]
    - path: "accrue/lib/accrue/billing/subscription_actions.ex"
      provides: "Stripe continuation with operation identity and ambiguous-outcome reconciliation"
    - path: "accrue/test/accrue/entitlements/purchase_decision_test.exs"
      provides: "ACCT-05 typed, race, copy, audit, idempotency, and no-mutation proof"
  key_links:
    - from: "accrue/lib/accrue/entitlements/purchase_decision.ex"
      to: "accrue/lib/accrue/entitlements/snapshot.ex"
      via: "current snapshot revision and redacted source summaries"
      pattern: "Snapshot|revision|sources"
    - from: "accrue/lib/accrue/entitlements/purchase_decision.ex"
      to: "accrue/lib/accrue/config.ex"
      via: "qualified `{rail, environment, product_id}` to logical-plan equivalence"
      pattern: "entitlement_product_catalog|logical_plan"
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "Accrue.Processor.Idempotency"
      via: "same operation ID across revision recheck, command, and reconcile-before-retry result"
      pattern: "operation_id|idempotency|reconcile"
  assumptions: []
  prohibitions:
    - requirement_id: ACCT-05
      statement: "MUST NOT automatically cancel, transfer, refund, migrate, merge, or prorate another rail as a consequence of purchase preflight, override, Stripe continuation, or concurrent Apple completion."
      status: unverified
      flagged: true
---

<objective>
Give hosts one typed, revision-bound purchase preflight and explicit warning override that blocks equivalent second-rail purchases by default and safely continues controllable Stripe commands per D-06 through D-10 and D-20 through D-22.

Purpose: Prevent duplicate charges and unsafe cross-rail finance actions while preserving an explicit host escape hatch with durable evidence.
Output: PurchaseDecision value/service, public facade, Stripe continuation integration, and complete ACCT-05 tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md
@.planning/phases/217-canonical-projection-and-compatibility/217-RESEARCH.md
@.planning/phases/217-canonical-projection-and-compatibility/COVERAGE.md
@.planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md
@accrue/lib/accrue/entitlements/source/registry.ex
@accrue/lib/accrue/config.ex
@accrue/lib/accrue/billing/subscription_actions.ex
@accrue/lib/accrue/processor/idempotency.ex
@brandbook/voice.md
@brandbook/copy.md
</context>

## Artifacts this phase produces

- `%Accrue.Entitlements.PurchaseDecision{status, reason, target_rail, logical_plan, sources, revision, guidance}` with closed values.
- `evaluate/4`, `override/4`, and `continue/4` plus public Entitlements facade functions.
- Bounded override audit fields and stable reasons for equivalent, missing, stale, repairing, ambiguous, changed-revision, and reconcile-required outcomes.
- Stripe command integration carrying one operation ID and forbidding blind retry after ambiguity.
- Exact Apple warning and management-copy contract tests.
- D-19 public eligibility telemetry contract for `[:accrue, :entitlements, :purchase_decision]` and `[:accrue, :entitlements, :override_purchase_decision]`, including captured start/stop/exception events and privacy-negative fixtures.

<assumption_delta_decision>
Generalized primary noun: subscription. Decision: promote. The canonical representation is a rail-neutral logical-plan subscription/purchase decision, while the fixed Apple-specific sentence from D-22 is demoted to a presentation template populated from bounded plan/source labels. Invariant: changing target/source rail labels does not change equivalent-plan status, reason, revision binding, or forbidden lifecycle side effects; tests exercise Stripe→Apple and Apple→Stripe symmetry.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship typed equivalent-plan preflight and revision-bound warning override</name>
  <reversibility rating="costly">Hosts and later clients will pattern-match the closed decision statuses/reasons and revision field, but the additive API can evolve compatibly through new reason values only under the locked contract.</reversibility>
  <files>accrue/lib/accrue/entitlements/purchase_decision.ex, accrue/lib/accrue/entitlements.ex, accrue/test/accrue/entitlements/purchase_decision_test.exs</files>
  <read_first>
    - .planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md (D-06 through D-09 and D-20 through D-22)
    - .planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md (actual Snapshot interface)
    - accrue/lib/accrue/entitlements/source/registry.ex (`Outcome`/typed-error/guidance construction)
    - accrue/lib/accrue/config.ex (`entitlement_product_catalog/0`)
    - accrue/lib/accrue/entitlements.ex (public facade and telemetry conventions)
    - accrue/lib/accrue/events.ex (bounded immutable audit event)
    - brandbook/voice.md and brandbook/copy.md (D-21 mechanism-led guidance)
  </read_first>
  <behavior>
    - Test 1: no equivalent live grant returns `:eligible`; a live different-rail grant mapped to the same logical plan returns `:block` with sources and snapshot revision.
    - Test 2: feature overlap, equal price/quantity, bare provider IDs, customer/email/device identity, or an unmapped product never establishes equivalence.
    - Test 3: missing, stale, repairing, or ambiguous snapshot state blocks with distinct stable reason and next action.
    - Test 4: a bounded explicit override records actor hash/reference, reason, target, equivalent sources, decision revision, and outcome, then returns `:warn` without changing any grant or provider resource.
    - Test 5: changed revision re-evaluates; it never trusts or upgrades a stale override.
    - Test 6: warning text exactly renders “This account already has Pro through Apple. Continuing creates another subscription.” for the bounded Pro/Apple case and remains symmetric for a second target rail.
    - Test 7: first purchase through an authenticated billable reference with no account calls the Plan-217-01 provision boundary exactly once, fetches the resulting account, then returns `:eligible` from its empty snapshot without provider or grant writes; an unauthorized/cross-account reference returns a typed error and creates no account, grant, audit override, or provider call.
    - Test 8: `Entitlements.purchase_decision/4` and `override_purchase_decision/4` emit `[:accrue, :entitlements, :purchase_decision, :start | :stop | :exception]` and `[:accrue, :entitlements, :override_purchase_decision, :start | :stop | :exception]`; application metadata is limited to the boundary subset of `revision`, `action`, `rail`, `environment`, `disposition`, `reason`, opaque `account_id`, and hashed `actor_id`, while forbidden keys and seeded email/adopter/raw evidence/JWS/token/provider-payload values are absent from telemetry and override audit data.
  </behavior>
  <action>Create the Wave 0 purchase decision suite first. Implement a typed enforced struct and closed statuses/reasons per D-06. Make the public authenticated first-purchase orchestration path explicit per D-08: validate/authorize the billable reference, call Plan-217-01 `Entitlements.provision_account/3` only when lookup returns no account, fetch that exact account, and then pass its pure empty/current snapshot to evaluation. An account argument uses lookup only; an unauthorized or cross-account reference returns a stable typed error before provision, grant, audit-override, or provider work. Resolve equivalence only by the qualified catalog and live different-rail logical-plan identity per D-07; fail closed on stale, repairing, ambiguous, or post-provision fetch failure. Implement D-09 override as an explicit bounded/audited action that re-fetches the snapshot under the current revision, re-evaluates equivalence, and returns `:warn` only when the same blocking decision still holds. Per D-19, wrap the public `Entitlements.purchase_decision/4` and `override_purchase_decision/4` facades with `Accrue.Telemetry.span/3` at `[:accrue, :entitlements, :purchase_decision]` and `[:accrue, :entitlements, :override_purchase_decision]`, yielding concrete `:start`, `:stop`, and `:exception` suffix events. Permit only the boundary-specific subset of `[:revision, :action, :rail, :environment, :disposition, :reason, :account_id, :actor_id]`, with opaque `account_id` and hashed/internal `actor_id`, plus the telemetry framework's standard exception keys. Capture the spans and override audit using fixtures seeded with distinctive email, adopter identity, raw receipt/JWS, Apple account token, generic token, and provider payload values; assert neither the forbidden keys `email`, `adopter`, `adopter_id`, `identity`, `raw_receipt`, `receipt`, `jws`, `apple_account_token`, `token`, `provider_payload`, `payload` nor their seeded values occur anywhere, including nested terms or renamed keys. Render the D-22 copy template without adding unbounded telemetry text. Preserve the generalized rail-neutral `subscription` decision invariant; do not infer from provider IDs, feature/price/quantity/customer identity, mutate grants/resources, or reduce the result to boolean.</action>
  <acceptance_criteria>
    - Every closed status/reason is typed and test-covered; all incomplete/ambiguous cases fail closed.
    - Equivalent-plan detection is qualified-catalog-only and symmetric across target/source rails.
    - Override rechecks revision/equivalence, records bounded privacy-safe audit evidence, and causes no provider or grant mutation.
    - D-22 exact preferred copy and D-20/D-21 actionable consumer fields are present without provider-transport internals.
    - Authenticated account absence provisions then fetches exactly once before evaluation; unauthorized references and provision/fetch errors are typed, fail closed, and cause no provider/grant side effect per D-08.
    - Both public eligibility facades emit their named start/stop/exception events; all application metadata keys are allowlisted, IDs are opaque/hashed, and privacy-negative key/value fixtures are absent from emitted telemetry and override audit records per D-19.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/purchase_decision_test.exs --exclude purchase_continuation</automated></verify>
  <done>Hosts receive a closed, actionable, revision-bound decision and can explicitly convert a still-current block to a warning without cross-rail side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recheck and idempotently continue a controllable Stripe purchase</name>
  <files>accrue/lib/accrue/entitlements/purchase_decision.ex, accrue/lib/accrue/billing/subscription_actions.ex, accrue/test/accrue/entitlements/purchase_decision_test.exs</files>
  <read_first>
    - .planning/phases/217-canonical-projection-and-compatibility/COVERAGE.md (Stripe continuation/reconciliation integration rows and Apple opt-out)
    - accrue/lib/accrue/entitlements/purchase_decision.ex (Task 1 status/revision/override contract)
    - accrue/lib/accrue/billing/subscription_actions.ex (`subscribe/3`, `resolve_operation_id/1`, existing provider idempotency)
    - accrue/lib/accrue/processor/idempotency.ex (`key/4` deterministic identity)
    - accrue/lib/accrue/processor/fake.ex (scriptable ambiguity/call assertions)
    - accrue/test/accrue/entitlements/purchase_decision_test.exs (Task 1 fixtures)
  </read_first>
  <behavior>
    - Test 1: continuing Stripe purchase rechecks current revision/equivalence immediately before command dispatch and refuses a changed blocking state.
    - Test 2: repeated continuation with one operation ID presents the same provider idempotency key and produces no second subscription.
    - Test 3: an ambiguous provider result returns a stable reconcile-required outcome tied to the operation ID; retry first reconciles/fetches and does not issue a second create when the provider already completed.
    - Test 4: concurrent Apple completion after preflight is recorded as a diagnostic conflict and never triggers cancellation, refund, transfer, migration, merge, or proration.
  </behavior>
  <action>Integrate D-10 into the server-controlled Stripe subscription command without changing existing public signatures: `PurchaseDecision.continue/4` must re-fetch/re-evaluate the revision-bound decision, require or derive the existing durable operation ID, and delegate through the established `SubscriptionActions.subscribe/3` idempotency path. Represent ambiguous completion as a stable reconcile-first tagged result; on a repeated call, use the same operation identity and fetch/reconcile before deciding whether any create call is safe. For Apple, end at typed preflight/diagnostic conflict only. Preserve existing transaction, audit, error, and bang/non-bang conventions and never add a reservation subsystem.</action>
  <acceptance_criteria>
    - No Stripe create dispatch occurs from a stale/changed block; an allowed continuation reuses one durable operation identity.
    - Fake-first tests prove same-operation retries are idempotent and ambiguous outcomes reconcile before any second command.
    - Concurrent Apple completion remains diagnostic evidence only and cannot reach any gateway lifecycle mutation.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/purchase_decision_test.exs &amp;&amp; mix test test/accrue/billing/subscription_actions_test.exs</automated></verify>
  <done>Controllable Stripe purchase continuation is revision-safe and provider-idempotent, while Apple concurrency cannot create cross-rail mutation authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host target/reason/actor → purchase decision/override | Host-controlled fields influence whether a blocked purchase may continue. |
| snapshot decision → Stripe command | A prior read must be revalidated before financial side effects. |
| ambiguous provider response → retry | Unknown completion can create a duplicate charge if blindly repeated. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-217-08 | Tampering | equivalence/override | high | mitigate | Task 1 uses qualified catalog identity only, bounds actor/reason, rechecks revision, and tests all invalid inference sources. |
| T-217-08A | Elevation of Privilege | first-purchase account provisioning | high | mitigate | Task 1 authorizes the billable reference before the separate provision-then-fetch branch and proves unauthorized/cross-account references create no account, grant, override audit, or provider call. |
| T-217-09 | Repudiation | override/Stripe continuation | high | mitigate | Tasks 1-2 record bounded audit facts and carry one durable operation ID through decision, command, and reconciliation. |
| T-217-10 | Denial of Service | fail-closed preflight | medium | mitigate | Closed actionable reasons distinguish missing/stale/repairing/ambiguous state so repair is possible without failing open. |
| T-217-11 | Tampering | ambiguous Stripe retry | high | mitigate | Task 2 reconciles using the same idempotency identity before any second create and proves it with the Fake adapter. |
| T-217-11A | Information Disclosure | purchase eligibility telemetry/audit | high | mitigate | Task 1 captures both facade span families, enforces a boundary allowlist with opaque/hashed identifiers, and rejects forbidden keys plus seeded identity/evidence/JWS/token/provider-payload values. |
| T-217-SC | Tampering | package-manager installs | low | accept | No package installation occurs. |
</threat_model>

<verification>
Run both focused tasks, then the complete purchase suite plus existing subscription action suite. Confirm the external API matrix has no unreasoned OPT-OUT and no live Stripe/Apple credential requirement.
</verification>

<success_criteria>
- ACCT-05 is satisfied for default block, actionable warning override, revision races, and provider ambiguity.
- The generalized subscription decision remains rail-neutral while D-22 Apple copy remains exact.
- No automatic finance/lifecycle action occurs on another rail.
</success_criteria>

<output>
Create `.planning/phases/217-canonical-projection-and-compatibility/217-03-SUMMARY.md` when done.
</output>

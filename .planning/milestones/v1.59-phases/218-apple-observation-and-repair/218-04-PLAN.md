---
phase: 218-apple-observation-and-repair
plan: 04
type: execute
wave: 3
depends_on: [218-01, 218-03]
files_modified:
  - accrue/lib/accrue/entitlements/apple/lineage.ex
  - accrue/lib/accrue/entitlements/apple/intake.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/lib/accrue/entitlements/observation.ex
  - accrue/lib/accrue/entitlements/decision_cases.ex
  - accrue/test/accrue/entitlements/apple_lineage_test.exs
  - accrue/test/property/apple_lineage_property_test.exs
  - accrue/test/accrue/entitlements/apple_intake_test.exs
  - accrue/test/property/apple_convergence_property_test.exs
autonomous: true
requirements: [AAPL-01, AAPL-03]
must_haves:
  truths:
    - "Per D-02/D-04, parallel claim or explicit repair of a verified currently-unbound lineage converges to one immutable account binding, observation, projection, audit, and reconciliation wakeup."
    - "Per D-03, a bound conflict remains quarantined and cannot be repaired, transferred, merged, refunded, cancelled, migrated, or prorated."
    - "AAPL-03 duplicate/out-of-order permutations converge to the same lineage/intake/observation state; repeated retry is idempotent and interruption leaves durable retry/needs_repair state."
    - "Flagged assumption AAPL-03 (unclassified): host-supplied product catalog mappings are complete and correctly environment-qualified; unmapped verified evidence remains quarantine rather than inferred access."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/lineage.ex"
      provides: "Unbound-only authorized repair and immutable ownership"
      exports: ["repair/5"]
    - path: "accrue/lib/accrue/entitlements/apple/intake.ex"
      provides: "Closed disposition/reason/next-action state machine and retry exhaustion"
      exports: ["observe/3", "retry/3"]
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "Authenticated repair facade"
      exports: ["repair_apple_lineage/3"]
  key_links:
    - from: "accrue/lib/accrue/entitlements.ex"
      to: "accrue/lib/accrue/entitlements/apple/lineage.ex"
      via: "host authorize callback then reverify/refetch and locked unbound-only repair"
      pattern: "repair_apple_lineage|authorize"
    - from: "accrue/lib/accrue/entitlements/apple/intake.ex"
      to: "accrue/lib/accrue/entitlements/observation.ex"
      via: "fully verified/bound/mapped facts only"
      pattern: "insert_idempotently"
    - from: "accrue/lib/accrue/entitlements/apple/intake.ex"
      to: "accrue/lib/accrue/entitlements/apple/reconciliation_wakeup.ex"
      via: "Plan 218-01 transaction-aware enqueue API inside the repair transaction"
      pattern: "ReconciliationWakeup.enqueue_in_transaction"
  assumptions:
    - requirement_id: AAPL-03
      category: unclassified
      status: unresolved
      statement: "The host's qualified Apple product catalog is complete; no fallback equivalence is inferred for an unmapped product."
  prohibitions:
    - requirement_id: AAPL-01
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT automatically transfer, merge, or reassign a bound Apple lineage based on restore, provider token replacement, email, product, device, session, or delivery order."
---

<objective>
Complete bind-once repair and convergent intake semantics so invalid, unbound, conflicting, duplicate, delayed, and retryable Apple evidence stays durable, non-granting when required, and repairable per D-02 through D-04, D-07/D-08, D-12, and D-17 through D-20.

Purpose: Make the ownership tracer safe under races, retries, interruption, and explicit host-authorized repair.
Output: Repair facade, closed retry/quarantine state machine, decision cases, and database/property proofs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-01-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-03-SUMMARY.md
@.planning/research/v1.59-DECISION-TABLE.md
@accrue/lib/accrue/entitlements/decision_cases.ex
</context>

## Artifacts this phase produces

- `Accrue.Entitlements.Apple.Lineage.repair/5` and public `Accrue.Entitlements.repair_apple_lineage/3`.
- Closed reasons `verified_projected|duplicate|stale|verified_unbound|ownership_conflict|invalid_payload|invalid_algorithm|invalid_chain|invalid_certificate|wrong_bundle|wrong_environment|wrong_app|unsupported_family|unmapped_product|provider_unavailable|rate_limited|persistence_unavailable|reconciliation_stalled|config_invalid|needs_repair`.
- Closed next actions `none|retry_reconciliation|review_ownership|contact_support|configure_apple|map_product|inspect_verification|repair_lineage`.
- Decision-case entries and deterministic fixtures for unbound repair, conflict, retry exhaustion, duplicate, and ordering permutations; no CLI flags or new tables.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize explicit unbound-lineage repair and conflict races</name>
  <reversibility rating="costly">Per D-04/D-17, host repair integrations and later operator tooling depend on the unbound-only authorization/result contract.</reversibility>
  <files>accrue/lib/accrue/entitlements/apple/lineage.ex, accrue/lib/accrue/entitlements/apple/intake.ex, accrue/lib/accrue/entitlements.ex, accrue/test/accrue/entitlements/apple_lineage_test.exs, accrue/test/property/apple_lineage_property_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-02 through D-04, D-17 through D-19)
    - accrue/lib/accrue/entitlements/apple/lineage.ex (Plan 218-01 lock/claim interface)
    - accrue/lib/accrue/entitlements/apple/verifier.ex (reverification results)
    - accrue/lib/accrue/entitlements.ex (public telemetry/authorization conventions)
    - accrue/lib/accrue/events.ex (actor audit)
  </read_first>
  <behavior>
    - Test 1: authorized repair of verified currently-unbound lineage binds once, re-verifies/refetches, projects idempotently, audits hashed actor, and schedules reconciliation.
    - Test 2: missing/false host authorization performs no provider, binding, observation, grant, audit, or job work.
    - Test 3: parallel repairs for the same account converge to one binding/projection; different-account race yields one winner and one privacy-safe conflict.
    - Test 4: already-bound same-account repeat is a no-op; already-bound other-account repair remains conflict and never calls Set App Account Token.
    - Test 5: Set App Account Token, when enabled, runs only after successful local bind and its failure becomes retryable provider repair without undoing or reassigning ownership.
    - Test 6: a failure injected after Projector/audit/Projector-job writes and `ReconciliationWakeup.enqueue_in_transaction/5` rolls back repair binding, qualified Observation, grant/revision, actor and projection audits, Projector follow-up job, and reconciliation-wakeup row together; a retry commits one coherent result.
  </behavior>
  <action>Implement `repair_apple_lineage/3` as a D-17 tagged value API over an opaque local lineage reference. Require `authorize.(account, :repair_apple_lineage) == true` before provider or database work. Re-fetch/reverify through the private adapters, then open one outer `Repo.transact`, lock/re-read the lineage, permit repair only from `unbound`, persist the binding and qualified Observation, call Plan 218-01's `Projector.project_in_transaction(repo, observation, opts)`, record the hashed-actor repair audit, and call Plan 218-01's concrete `ReconciliationWakeup.enqueue_in_transaction(repo, lineage.id, lineage.environment, :repair, opts)` before returning per D-02/D-04. Use an internal test callback after that final write to inject failure and assert every bind/Observation/grant/revision/audit/Projector-job/wakeup-row effect rolls back; do not call `Projector.project/2` inside this transaction. Treat Apple Set App Account Token as an optional post-commit provider repair step, never the ownership decision. Return the exact D-18 conflict sentence and fixed operator reason/action without owning account data. Use PostgreSQL constraints/locks as authority and property-test parallel interleavings.</action>
  <acceptance_criteria>
    - Authorization, re-verification, unbound state, and token/account equality are all required before binding.
    - Same-input repeats and same-account races are idempotent; cross-account races never expose or reassign the winner.
    - Provider follow-up cannot establish ownership or roll back a committed local binding.
    - Forced failure after `ReconciliationWakeup.enqueue_in_transaction/5` leaves no persisted repair binding, Observation, Grant, revision advance, audit event, Projector follow-up job, or row in `accrue_entitlement_apple_reconciliation_wakeups`.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_lineage_test.exs test/property/apple_lineage_property_test.exs</automated></verify>
  <done>Hosts can authorize safe unbound repair, while bound conflicts remain immutable, private, and non-granting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close intake dispositions, retries, and order-independent convergence</name>
  <files>accrue/lib/accrue/entitlements/apple/intake.ex, accrue/lib/accrue/entitlements/observation.ex, accrue/lib/accrue/entitlements/decision_cases.ex, accrue/test/accrue/entitlements/apple_intake_test.exs, accrue/test/property/apple_convergence_property_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-07 through D-09, D-12/D-13, D-20)
    - accrue/lib/accrue/entitlements/apple/intake.ex (current durable admission)
    - accrue/lib/accrue/entitlements/observation.ex (idempotency/conflict target)
    - accrue/lib/accrue/entitlements/decision_cases.ex (closed decision corpus)
    - .planning/research/v1.59-DECISION-TABLE.md (duplicate/order/repair outcomes)
  </read_first>
  <behavior>
    - Test 1: verified mapped bound facts project; duplicate/stale facts no-op; transient failures retry; terminal invalid/unmapped/conflict facts quarantine.
    - Test 2: terminal quarantine is never blindly retried; repeated transient retry increments durable attempts once per attempt and exhaustion becomes `needs_repair`.
    - Test 3: permutations of duplicate, delayed positive, unbound, conflict, and retryable events produce the same durable non-granting/granting classification before lifecycle projection.
    - Test 4: raw evidence and owner/token/PII seeds are recursively absent from rows, job args, spans, Logger, exceptions, and inspect.
  </behavior>
  <action>Implement the exact four D-07 disposition classes with closed public reasons/next actions listed above. Distinguish a successful duplicate/stale no-op from retryable provider/persistence failure and terminal quarantine. Set a maximum of 12 durable intake attempts; the twelfth unsuccessful retry transitions to `needs_repair` and clears automatic next retry, while Retry-After may postpone an earlier attempt. Extend DecisionCases with stable outcomes and StreamData permutation coverage. Keep Observation account-required and admit only verified/bound/mapped facts; do not add nullable account ownership or raw provider payload metadata.</action>
  <acceptance_criteria>
    - Every verifier/provider/persistence/ownership/catalog result maps to one closed disposition/reason/action.
    - Repeated and parallel retry cannot double-increment or disappear at exhaustion.
    - All permutations converge and only fully verified/bound/mapped facts reach Observation/Projector.
    - The exact privacy-negative corpus passes every durable and emitted channel.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_intake_test.exs test/property/apple_convergence_property_test.exs</automated></verify>
  <done>Apple evidence converges into closed durable dispositions, with retry exhaustion visible and terminal evidence permanently non-granting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host repair authorization -> ownership mutation | A caller attempts to bind purchase lineage. |
| provider retry/outage -> durable disposition | Repeated or exhausted work must not vanish or widen access. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-09 | Elevation of Privilege | repair facade | critical | mitigate | Task 1 requires host authorization, re-verification, UUID equality, unbound state, and row lock before bind. |
| T-218-10 | Information Disclosure | ownership conflict | high | mitigate | Task 1 fixed-copy and recursive privacy tests exclude the owner across all channels. |
| T-218-11 | Tampering | retry/convergence | high | mitigate | Task 2 uses database identity/locking, closed state transitions, and permutation/property tests. |
| T-218-12 | Denial of Service | retry exhaustion | medium | mitigate | Task 2 caps attempts at 12 and persists `needs_repair` rather than retrying forever. |
| T-218-SC | Tampering | package-manager installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
Run both focused suites, then `cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_*_test.exs test/property/apple_*_property_test.exs`.
</verification>

<success_criteria>
- AAPL-01 repair is authorized, serialized, bind-once, and non-disclosing.
- AAPL-03 duplicates, ordering, retries, and terminal quarantine converge without grants from ambiguous evidence.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-04-SUMMARY.md` when done.
</output>

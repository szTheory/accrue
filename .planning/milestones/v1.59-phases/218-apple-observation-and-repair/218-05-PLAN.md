---
phase: 218-apple-observation-and-repair
plan: 05
type: execute
wave: 4
depends_on: [218-04]
files_modified:
  - accrue/priv/repo/migrations/20260803031000_create_accrue_apple_reconciliation_checkpoints.exs
  - accrue/lib/accrue/entitlements/apple/client.ex
  - accrue/lib/accrue/entitlements/apple/reconciliation.ex
  - accrue/lib/accrue/entitlements/apple/reconcile_worker.ex
  - accrue/test/accrue/entitlements/apple_reconciliation_test.exs
autonomous: true
requirements: [AAPL-04]
must_haves:
  truths:
    - "Per D-10, verified notifications only wake reconciliation; statuses determine present auto-renewable state, ascending Transaction History supplies ordered changes, and Notification History only seeds delivery-gap recovery."
    - "Per D-11, a scan fingerprints immutable filters, persists pending progress, upserts every verified page idempotently, and commits completed revision only after `hasMore: false`."
    - "AAPL-04 idempotency edge is covered: repeating a completed status/history reconciliation produces the same observations/snapshot/checkpoint without revision or job multiplication."
    - "AAPL-04 concurrency edge is covered: parallel/interrupted runs serialize on the checkpoint/lineage, preserve identical filters, and cannot advance a partial cursor."
    - "Per D-13, Apple outage retains the last verified grant only to its known bound; auth/config failure alerts and becomes durable repair instead of infinite retry."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/client.ex"
      provides: "Private status/history/notification-history/token client behaviour and Fake"
      exports: ["subscription_statuses/3", "transaction_history/4", "notification_history/3", "set_app_account_token/4"]
    - path: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      provides: "Durable wakeup drainer and status/history checkpoint state machine"
      exports: ["enqueue/3", "drain_wakeups/2", "run/2", "due/2"]
    - path: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      provides: "Thin host-owned Oban execution adapter"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      to: "accrue/lib/accrue/entitlements/apple/intake.ex"
      via: "each independently verified status/history fact is idempotently admitted"
      pattern: "Intake.observe"
    - from: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      to: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      via: "scalar lineage/environment/reason job args"
      pattern: "Reconciliation.run"
    - from: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      to: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      via: "lock a coalesced durable wakeup, insert the host-owned Oban job, and delete the wakeup in one transaction"
      pattern: "drain_wakeups|ReconcileWorker.new|Oban.insert"
  assumptions: []
  prohibitions: []
---

<objective>
Drain the durable reconciliation wakeups produced since Plan 218-01, then repair missed Apple evidence through durable current-status and ascending-history reconciliation with crash-safe cursors, bounded retries, and host-owned Oban execution per D-10 through D-13 and D-19/D-20.

Purpose: Connect the already-transactional producer seam to host-owned execution, keep notifications advisory, and guarantee eventual convergence without losing pages or extending stale access.
Output: Apple client seam, reconciliation checkpoint table/service, worker, and 20+ page/outage/concurrency tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-04-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@accrue/lib/accrue/entitlements/reconcile.ex
@accrue/lib/accrue/entitlements/stripe_sync/refresh_worker.ex
</context>

## Artifacts this phase produces

- Table `accrue_entitlement_apple_reconciliation_checkpoints` with `id`, `lineage_id`, `environment`, `query_fingerprint`, `pending_revision`, `completed_revision`, `run_state`, `page_count`, `page_budget`, `attempts`, `last_success_at`, `next_due_at`, `retry_after_at`, `last_provider_class`, and timestamps.
- Named unique index `accrue_apple_reconciliation_lineage_environment_index` and named state/page/attempt constraints.
- Behaviour `Accrue.Entitlements.Apple.Client`, its deterministic Fake, and private production implementation in the same file.
- Existing `Accrue.Entitlements.Apple.ReconciliationWakeup.enqueue_in_transaction/5` from Plan 218-01; `Accrue.Entitlements.Apple.Reconciliation.enqueue/3`, `drain_wakeups/2`, `run/2`, `due/2`; and Oban worker `Accrue.Entitlements.Apple.ReconcileWorker`.
- Defaults: page budget 25, regular due interval 6 hours, near-bound interval 1 hour when a verified bound is within 24 hours, exponential retry starting at 30 seconds capped at 6 hours with deterministic ±10% jitter, maximum 12 domain attempts. No CLI flags.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile current status and coalesce durable host-owned work</name>
  <reversibility rating="costly">Per D-10/D-11, repair checkpoints and support explanations depend on the split between current status, ordered history, and delivery diagnostics.</reversibility>
  <files>accrue/priv/repo/migrations/20260803031000_create_accrue_apple_reconciliation_checkpoints.exs, accrue/lib/accrue/entitlements/apple/client.ex, accrue/lib/accrue/entitlements/apple/reconciliation.ex, accrue/lib/accrue/entitlements/apple/reconcile_worker.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-10 through D-13, D-19/D-20)
    - .planning/phases/218-apple-observation-and-repair/COVERAGE.md (integrated status/history/notification-history endpoints)
    - accrue/lib/accrue/entitlements/apple/intake.ex (verified fact/retry contract)
    - accrue/lib/accrue/entitlements/reconcile.ex (transaction/stale result pattern)
    - accrue/lib/accrue/entitlements/stripe_sync/refresh_worker.ex (thin worker/host Oban pattern)
    - accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs (migration conventions)
  </read_first>
  <behavior>
    - Test 1: notification, purchase/restore, stale age, near bound, retryable intake, cursor corruption, and outage recovery coalesce through the Plan 218-01 wakeup identity to one lineage/environment job; draining and replaying the same wakeup cannot multiply jobs.
    - Test 2: status response is independently verified and admitted as present-state facts; notification-history records only enqueue status/history work and cannot project.
    - Test 3: parallel workers serialize on one checkpoint; duplicate runs leave one schedule and no duplicate observations/revisions.
    - Test 4: 429 honors Retry-After; 5xx uses bounded jittered backoff; auth/config failures transition to `needs_repair` without endless Oban attempts.
    - Test 5: outage never changes a known effective bound or grants from ambiguous/invalid input.
  </behavior>
  <action>Create the full status/history checkpoint schema/migration with environment-qualified lineage uniqueness. In `Reconciliation.drain_wakeups/2`, query and lock the `ReconciliationWakeup` rows created by Plan 218-01, insert each scalar-argument host-owned Oban job through the caller-supplied repo, and delete its wakeup row in the same transaction; prove a crash before commit leaves the wakeup claimable and replay cannot multiply jobs. Preserve `ReconciliationWakeup.enqueue_in_transaction/5` unchanged for all producers. Define `Apple.Client` methods and Fake scripts for statuses, ascending history, notification history, and post-bind token repair; the production implementation uses the adapter path chosen in 218-ADAPTER-ADMISSION.md and host-owned Finch/credentials without starting children. Route all seven D-12 triggers through the same durable wakeup identity and use scalar privacy-safe job args; Oban uniqueness remains insertion dedup only. `run/2` locks the checkpoint, verifies every provider JWS through Plan 218-03, routes statuses through Intake, and treats Notification History solely as gap evidence. Apply the exact cadence/backoff/page/attempt defaults listed above; domain state, not Oban exhaustion, owns `needs_repair`.</action>
  <acceptance_criteria>
    - All seven triggers call the Plan 218-01 durable wakeup producer and drain to one job without treating Oban uniqueness as execution locking; crash-before-drain completion is replay-safe.
    - Status is present-state authority and Notification History has no direct grant/retract path.
    - Retry-After/backoff/auth/config/outage outcomes are durable, bounded, and non-granting.
    - Accrue.Application remains childless and job args contain no provider evidence or credentials.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs --only status</automated></verify>
  <done>Current Apple state and every repair trigger converge through one durable, host-owned reconciliation lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit ascending history only after the final page</name>
  <files>accrue/lib/accrue/entitlements/apple/client.ex, accrue/lib/accrue/entitlements/apple/reconciliation.ex, accrue/lib/accrue/entitlements/apple/reconcile_worker.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs</files>
  <read_first>
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (Task 1 checkpoint/locking contract)
    - accrue/lib/accrue/entitlements/apple/client.ex (Task 1 history page value)
    - accrue/lib/accrue/entitlements/apple/intake.ex (idempotent verified transaction admission)
    - .planning/phases/218-apple-observation-and-repair/218-RESEARCH.md (final-page pagination and repeated-updated transaction rules)
  </read_first>
  <behavior>
    - Test 1: a 21-page ascending scan reuses the exact query fingerprint, verifies/upserts every transaction, and commits completed revision only on page 21 `hasMore: false`.
    - Test 2: crash, 429, or 5xx on any intermediate page retains the prior completed revision and resumes from durable pending progress without skipping or duplicating effects.
    - Test 3: changed filters during a pending revision fail closed as cursor corruption and enqueue bounded restart/repair rather than mixing scans.
    - Test 4: an updated transaction repeated on a later page supersedes idempotently; repeating the completed scan is a revision-stable no-op.
    - Test 5: parallel scans cannot commit competing revisions or exceed the 25-page budget.
  </behavior>
  <action>Implement D-11 literally: canonicalize the initial status/history filter set into a SHA-256 fingerprint; store pending revision/page count separately from completed revision; send identical filters on every page; verify and idempotently upsert every transaction because later pages may repeat an update; and atomically copy pending to completed only after `hasMore: false`. On interruption preserve the last completed cursor and resume pending work. Reject filter drift/corrupt revision as bounded repair. At 25 pages stop before cursor commit and mark `needs_repair`; never silently truncate.</action>
  <acceptance_criteria>
    - The 21-page test proves all pages and final-only completed cursor commit.
    - Crash/429/5xx/filter drift/concurrency cannot skip evidence or advance a partial cursor.
    - Repeated/updated transactions and completed reruns are idempotent.
    - Page-budget exhaustion is visible `needs_repair`, not partial success.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs</automated></verify>
  <done>Ascending history is crash-safe, filter-stable, idempotent, bounded, and advances durable completion only after the final page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Apple API responses -> reconciliation | Remote signed pages/status can be delayed, repeated, partial, or unavailable. |
| Oban job -> checkpoint transaction | Concurrent/retried jobs must not corrupt cursor state. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-13 | Tampering | history cursor/filter | high | mitigate | Task 2 fingerprints filters, separates pending/completed revisions, and commits only final page. |
| T-218-14 | Denial of Service | provider paging/retry | high | mitigate | Tasks 1-2 enforce page/attempt/rate budgets, Retry-After, capped jitter, and visible needs_repair. |
| T-218-15 | Elevation of Privilege | stale/ambiguous status | high | mitigate | Task 1 retains only last verified access to its known bound and rejects notification history as authority. |
| T-218-16 | Information Disclosure | jobs/telemetry | medium | mitigate | Scalar lineage/environment/reason args and allowlisted hashed telemetry; raw IDs/JWS/credentials are excluded. |
| T-218-SC | Tampering | package-manager installs | low | accept | This plan installs no package; it consumes the selected private adapter. |
</threat_model>

<verification>
Run the reconciliation suite, then repeat it once to prove stable completed cursors and no duplicate jobs/revisions.
</verification>

<success_criteria>
- AAPL-04 status/history repair is authoritative, resumable, idempotent, concurrent-safe, and bounded.
- Provider outage or partial scans never widen access or silently lose work.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-05-SUMMARY.md` when done.
</output>

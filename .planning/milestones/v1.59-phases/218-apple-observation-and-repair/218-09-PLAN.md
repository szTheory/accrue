---
phase: 218-apple-observation-and-repair
plan: 09
type: execute
wave: 7
depends_on: [218-08]
files_modified:
  - accrue/lib/accrue/entitlements.ex
  - accrue/lib/accrue/entitlements/apple/admission.ex
  - accrue/lib/accrue/entitlements/apple/reconciliation/admission.ex
  - accrue/lib/accrue/entitlements/apple/verifier/production.ex
  - accrue/test/accrue/entitlements/apple_observation_tracer_test.exs
  - accrue/test/accrue/entitlements/apple_source_isolation_test.exs
  - accrue/test/accrue/entitlements/apple_verifier_test.exs
  - accrue/test/fixtures/apple/server_evidence.exs
  - .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md
autonomous: true
gap_closure: true
requirements: [AAPL-02]
must_haves:
  truths:
    - "Per D-05/D-06/D-17, the public purchase/restore observation facade accepts opaque signed transaction evidence, resolves the strict verifier and its policy from host-owned application configuration, and constructs normalized admission facts only after verification succeeds."
    - "A caller-constructed `Intake.VerifiedEvidence` value and caller-supplied verifier/configuration cannot reach Intake, Observation, Projector, Grant, or account revision through the public facade."
    - "Per D-06/D-20, a valid three-certificate Apple chain carries the App Store signing leaf extension `1.2.840.113635.100.6.11.1`, the Apple Worldwide Developer Relations intermediate extension `1.2.840.113635.100.6.2.1`, a non-CA leaf, and digital-signature key usage; a missing or wrong purpose/usage fails closed as `invalid_certificate_purpose`."
    - "Positive and hostile certificate-chain fixtures exercise real ES256 signatures and prove purpose enforcement before any Apple fact can affect a grant."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/admission.ex"
      provides: "Single configured signed-transaction-to-verified-evidence admission boundary for authenticated purchase/restore and reconciliation"
      exports: ["observe_purchase_or_restore/4", "admit_transaction/5"]
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "Public signed-evidence Apple observation facade with no verified-struct or verifier-injection authority"
      exports: ["observe_apple_evidence/3"]
    - path: "accrue/lib/accrue/entitlements/apple/verifier/production.ex"
      provides: "Apple leaf/intermediate purpose, leaf CA, and digital-signature usage enforcement"
    - path: "accrue/test/fixtures/apple/server_evidence.exs"
      provides: "Deterministic positive and hostile three-certificate ES256 JWS corpus"
  key_links:
    - from: "accrue/lib/accrue/entitlements.ex"
      to: "accrue/lib/accrue/entitlements/apple/admission.ex"
      via: "opaque signed transaction plus host-owned `:apple_reconciliation[:admission]` configuration"
      pattern: "observe_apple_evidence|observe_purchase_or_restore"
    - from: "accrue/lib/accrue/entitlements/apple/admission.ex"
      to: "accrue/lib/accrue/entitlements/apple/intake.ex"
      via: "strict verifier success, application-identity checks, bound product mapping, and normalized internal `VerifiedEvidence`"
      pattern: "verify_transaction|Intake.observe"
    - from: "accrue/lib/accrue/entitlements/apple/verifier/production.ex"
      to: "Apple x5c leaf and intermediate"
      via: "exact purpose extensions, non-CA leaf, and digital-signature key usage before JWS signature admission"
      pattern: "1.2.840.113635.100.6.11.1|1.2.840.113635.100.6.2.1|invalid_certificate_purpose"
  assumptions:
    - requirement_id: AAPL-02
      category: external_fixture
      status: resolved
      statement: "Apple's official server libraries require the App Store signing leaf extension 1.2.840.113635.100.6.11.1 and WWDR intermediate extension 1.2.840.113635.100.6.2.1; deterministic fixtures encode that contract without live Apple credentials."
  prohibitions:
    - requirement_id: AAPL-02
      category: authority
      status: unverified
      verification: null
      flagged: true
      statement: "MUST NOT treat a caller-created struct, caller-selected verifier, decoded claim map, or unverified transaction as grant authority."
    - requirement_id: AAPL-02
      category: privacy
      status: unverified
      verification: null
      flagged: true
      statement: "MUST NOT retain raw signed evidence, account tokens, provider transaction identifiers, or certificate bytes in rows, jobs, telemetry, logs, exceptions, or public results."
---

<objective>
Close the AAPL-02 grant-authority gap by making signed Apple evidence cross one configured, strict, non-bypassable admission boundary and by enforcing Apple's certificate-purpose contract before projection per D-05/D-06, D-08, D-15, D-17, and D-20.

Purpose: A type label is not attestation; only cryptographically verified Apple evidence may construct the internal facts consumed by bind-once ownership and the sole Projector.
Output: Shared signed-evidence admission, a safe public facade, Apple certificate-purpose enforcement, and positive/negative end-to-end proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@.planning/phases/218-apple-observation-and-repair/218-03-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-04-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-08-SUMMARY.md
@accrue/lib/accrue/entitlements/apple/reconciliation/admission.ex
@accrue/lib/accrue/entitlements/apple/verifier/production.ex
</context>

## Artifacts this phase produces

- `Accrue.Entitlements.Apple.Admission.observe_purchase_or_restore/4`, the only configured public purchase/restore conversion from an opaque signed transaction to internal verified facts.
- `Accrue.Entitlements.Apple.Admission.admit_transaction/5`, shared by reconciliation so signed transaction parsing, product mapping, lifecycle normalization, and evidence-digest rules do not fork.
- Revised `Accrue.Entitlements.observe_apple_evidence/3`: accepts an authenticated `Account`, opaque signed transaction bytes, and bounded non-authority options; it obtains verifier/config/product mapping from host-owned `:apple_reconciliation[:admission]` configuration.
- Apple certificate purpose constants `1.2.840.113635.100.6.11.1` (leaf) and `1.2.840.113635.100.6.2.1` (intermediate), plus leaf non-CA and digital-signature key-usage checks.
- Deterministic valid, missing-purpose, wrong-purpose, wrong-key-usage, CA-leaf, bad-root, and expired-chain fixtures with real ES256 signatures.
- No database field, migration, CLI flag, dependency, or child process is added.

## Gap-closure source audit

| SOURCE | ID | Gap item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Verified evidence safely reaches the snapshot | 218-09 | COVERED | Signed evidence crosses strict admission before the existing bind/project transaction. |
| REQ | AAPL-02 | Strict outer/nested verification including certificate purpose | 218-09 | COVERED | Public bypass and production certificate-purpose gaps are both tested. |
| RESEARCH | A2 | Jason/OTP fallback can enforce Apple certificate policy | 218-09 | COVERED | Official-library OID parity plus real positive/negative chain fixtures close the identified delta. |
| CONTEXT | D-05 | Private replaceable verifier | 218-09 | COVERED | Public values expose neither verifier implementation nor provider structs. |
| CONTEXT | D-06 | Non-bypassable ES256/x5c/app verification | 218-09 | COVERED | Admission and exact Apple purpose checks are explicit. |
| CONTEXT | D-08 | Bounded evidence retention | 218-09 | COVERED | Raw payload/certificate redaction remains merge-blocking. |
| CONTEXT | D-15 | Existing Projector is sole grant writer | 218-09 | COVERED | Admission delegates only normalized verified facts to Intake/Projector. |
| CONTEXT | D-17 | Small signed-evidence host API | 218-09 | COVERED | The facade accepts opaque signed evidence, not verified structs. |
| CONTEXT | D-20 | Positive/hostile chain proof | 218-09 | COVERED | Valid and wrong-purpose/usage chains are deterministic. |

## Planning gates

- Schema-push detection: not applicable; this plan changes no schema or migration.
- API coverage: applicable and already recorded in `COVERAGE.md` for authenticated purchase/restore, nested signed transaction verification, and Apple x5c policy; no new Apple endpoint is introduced.
- Assumption delta: research assumption A2 is narrowed by official Apple-library purpose OIDs and executable fixtures; no new runtime assumption is introduced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace opaque signed purchase evidence through configured verification to one snapshot</name>
  <reversibility rating="costly">Per D-17, host purchase/restore integrations depend on the public facade shape, but the existing arity and tagged result contract remain stable while its unsafe input type is replaced.</reversibility>
  <files>accrue/lib/accrue/entitlements.ex, accrue/lib/accrue/entitlements/apple/admission.ex, accrue/lib/accrue/entitlements/apple/reconciliation/admission.ex, accrue/test/accrue/entitlements/apple_observation_tracer_test.exs, accrue/test/accrue/entitlements/apple_source_isolation_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (first failed truth and unsafe direct-host key link)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-05/D-06, D-08, D-15, D-17, D-20)
    - accrue/lib/accrue/entitlements.ex (`observe_apple_evidence/3` current trust-by-struct path and Apple telemetry conventions)
    - accrue/lib/accrue/entitlements/apple/reconciliation/admission.ex (existing verify/map/normalize path that must become shared)
    - accrue/lib/accrue/entitlements/apple/intake.ex (`VerifiedEvidence`, `observe/3`, and sole Intake-to-Projector path)
    - accrue/test/accrue/entitlements/apple_observation_tracer_test.exs (real Repo lineage/projector/snapshot tracer)
  </read_first>
  <behavior>
    - Test 1: configured Fake-verifier signed purchase evidence whose verified `appAccountToken` equals the authenticated account UUID binds once, creates one qualified Observation/grant, and returns snapshot revision 1.
    - Test 2: duplicate signed evidence is a successful no-op and does not advance revision.
    - Test 3: passing a caller-constructed `%Intake.VerifiedEvidence{}` to the public facade returns a closed invalid-input error and creates no lineage, intake, observation, grant, wakeup, or revision.
    - Test 4: no public option can select a verifier, verifier configuration, product map, normalized facts, or attestation result; missing/malformed host admission configuration fails closed before database writes.
    - Test 5: verifier rejection, wrong app/environment, unmapped product, unbound token, and ownership conflict keep their closed non-granting outcomes without owner disclosure or raw-evidence emission.
    - Test 6: reconciliation still uses the same shared signed-transaction conversion and its persisted-job convergence tests remain green.
  </behavior>
  <action>Write the failing real-Repo tracer first. Add private `Accrue.Entitlements.Apple.Admission` and move the signed-transaction-to-`Intake.VerifiedEvidence` conversion, product-map lookup, Apple timestamp normalization, lifecycle derivation, and SHA-256 evidence digest out of `Reconciliation.Admission` into it. Keep `VerifiedEvidence` internal to Apple modules; it may remain a struct for internal composition, but no public facade clause may accept it as authority. Change `Accrue.Entitlements.observe_apple_evidence/3` to accept only opaque signed transaction bytes plus bounded options such as environment; resolve the verifier, verifier config, versions, and product map exclusively from host-owned `Application.get_env(:accrue, :apple_reconciliation)[:admission]`, validate the configuration as a closed list, call `verifier.verify_transaction/2`, then call Intake only with the verified normalized result. Do not honor verifier/config/product-map/repo callbacks supplied in public options. Add a catch-all closed invalid-input result so a forged struct cannot crash or project. Make `Reconciliation.Admission` delegate to the shared admission conversion while preserving its bound-lineage lock and worker result contract. Update all public-surface tests to supply opaque signed evidence and host application configuration; retain direct Intake calls only in tests that deliberately exercise internal transaction rollback. Preserve D-03 ownership behavior, D-08 redaction, D-15 sole-writer projection, and existing telemetry shapes.</action>
  <acceptance_criteria>
    - One opaque signed transaction crosses public facade, configured verifier, shared admission, bind-once lineage, Intake, Projector, Grant, and Snapshot in a real database test.
    - The public facade has no `%Intake.VerifiedEvidence{}` admission clause and no caller-selectable verifier, verifier configuration, product map, normalized-fact map, or attestation option.
    - Caller-created verified structs and malformed/missing host configuration are closed non-granting results with zero durable side effects.
    - Purchase/restore and reconciliation share one evidence conversion and mapping implementation; the existing notification verifier path remains strict and unchanged.
    - Raw signed bytes and sensitive provider/account fields remain absent from rows, jobs, telemetry, logs, exceptions, and public results.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_observation_tracer_test.exs test/accrue/entitlements/apple_source_isolation_test.exs test/accrue/entitlements/apple_reconciliation_test.exs &amp;&amp; mix format --check-formatted</automated></verify>
  <done>Authenticated purchase/restore evidence can grant only after configured strict verification; a caller-created verified value has no public authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce Apple signing certificate purpose and key usage</name>
  <reversibility rating="reversible">The certificate-policy predicate and deterministic fixtures can be reverted without a schema or data migration, but rollback must restore the prior adapter atomically and is an emergency compatibility escape because it reopens the rejected wrong-purpose-chain path.</reversibility>
  <files>accrue/lib/accrue/entitlements/apple/verifier/production.ex, accrue/test/accrue/entitlements/apple_verifier_test.exs, accrue/test/fixtures/apple/server_evidence.exs, .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (`validate_leaf_purpose/1` blocker)
    - accrue/lib/accrue/entitlements/apple/verifier/production.ex (`validate_chain/2`, `validate_leaf_purpose/1`, and OTP certificate tuple access)
    - accrue/test/accrue/entitlements/apple_verifier_test.exs (current hostile corpus lacks a successful real chain)
    - accrue/test/fixtures/apple/server_evidence.exs (current deterministic JWS fixture helper)
    - https://github.com/apple/app-store-server-library-python/blob/main/appstoreserverlibrary/signed_data_verifier.py (`_ChainVerifier.check_oid` official leaf/intermediate purpose contract)
    - https://github.com/apple/app-store-server-library-node/blob/main/jws_verification.ts (`verifyCertificateChainWithoutCaching` independent parity reference)
  </read_first>
  <behavior>
    - Test 1: a three-certificate chain rooted in the configured test root, with leaf purpose OID `1.2.840.113635.100.6.11.1`, intermediate purpose OID `1.2.840.113635.100.6.2.1`, non-CA leaf, and digital-signature usage verifies a real ES256 transaction JWS and returns only bounded facts.
    - Test 2: removing or replacing the leaf purpose OID returns `{:error, :invalid_certificate_purpose}`.
    - Test 3: removing or replacing the intermediate purpose OID returns `{:error, :invalid_certificate_purpose}`.
    - Test 4: a CA leaf, a leaf without digital-signature usage, or a leaf asserting CA-signing usages returns `{:error, :invalid_certificate_purpose}` even when its EC signature and root path are valid.
    - Test 5: wrong root, wrong order/length, expired/not-yet-valid certificate, bad JWS signature, wrong bundle/environment/app ID, and nested-JWS failure retain their existing distinct closed errors.
  </behavior>
  <action>Replace the EC-key-type-only `validate_leaf_purpose/1` check with explicit OTP X.509 extension checks matching Apple's official server libraries: require exactly three leaf-first `x5c` certificates, require the leaf extension OID `1.2.840.113635.100.6.11.1`, require the intermediate extension OID `1.2.840.113635.100.6.2.1`, require the leaf Basic Constraints to be absent or `cA=false`, and require Key Usage to permit `digitalSignature` while rejecting CA-signing-only purpose. Keep configured-root path validation, certificate time validation, ES256 signature verification, and app claims as independent gates and map purpose/usage failures to the existing closed `:invalid_certificate_purpose` atom. Expand `AppleServerEvidence` with checked-in deterministic test-only EC keys/certificates and compact JWS builders; fixtures contain no production material and must create a genuinely valid signature/chain before mutating one purpose property at a time. Exercise outer and nested verification where applicable. Update `218-ADAPTER-ADMISSION.md` so its ordered x5c/root/time/purpose row cites the passing positive/negative chain tests instead of the earlier implementation-review claim. Do not add a dependency or weaken the configured Apple-root boundary.</action>
  <acceptance_criteria>
    - A real valid ES256 transaction with the exact Apple leaf/intermediate purpose extensions succeeds through the production adapter.
    - Each missing/wrong purpose, non-signing usage, and CA-leaf fixture fails specifically as `invalid_certificate_purpose` before claims can reach admission.
    - Existing algorithm, header, root/path, time, signature, bundle, environment, production app ID, and independent nested-JWS rejection cases remain green.
    - The adapter admission report no longer claims certificate-purpose proof without executable positive and negative evidence.
    - `accrue/mix.exs` and `accrue/mix.lock` remain byte-unchanged.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_verifier_test.exs test/accrue/entitlements/apple_observation_tracer_test.exs &amp;&amp; mix format --check-formatted &amp;&amp; git diff --quiet -- mix.exs mix.lock</automated></verify>
  <done>The production verifier accepts only Apple-purpose signing chains with valid signing usage, and the public signed-evidence tracer proves those verified facts are the only grant-changing path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated host caller -> public Apple observation facade | The caller supplies opaque signed bytes but must not assert verification provenance or select the verifier. |
| JWS x5c chain -> normalized Apple facts | An attacker may present a valid EC chain for the wrong Apple certificate purpose. |
| normalized facts -> Intake/Projector | Only strict verified, app-matched, mapped facts may affect grants and revision. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-G09-01 | Spoofing | `observe_apple_evidence/3` | critical | mitigate | Task 1 removes trust-by-struct admission and resolves the strict verifier only from host-owned application configuration before any database write. |
| T-218-G09-02 | Elevation of Privilege | public verifier options | high | mitigate | Task 1 proves callers cannot inject a Fake verifier/config/product map or normalized facts through the public API. |
| T-218-G09-03 | Tampering | x5c certificate purpose | high | mitigate | Task 2 requires official Apple leaf/intermediate purpose OIDs, non-CA leaf, and digital-signature usage with positive/negative real-chain fixtures. |
| T-218-G09-04 | Information Disclosure | signed evidence and outcomes | high | mitigate | Tasks 1-2 retain digest-only persistence and recursively reject raw JWS, tokens, provider IDs, and certificate bytes across emitted channels. |
| T-218-SC | Tampering | package-manager installs | low | accept | No package is installed; the locked Jason/OTP fallback and dependency files remain unchanged. |
</threat_model>

<verification>
Run both task suites, then `cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_*_test.exs test/property/apple_convergence_property_test.exs` and `cd accrue &amp;&amp; mix compile --warnings-as-errors`.
</verification>

<success_criteria>
- AAPL-02 has no public grant path that bypasses configured strict verification.
- Production certificate validation proves Apple signing purpose and signing usage with valid and hostile real-chain fixtures.
- Existing bind-once ownership, idempotency, privacy, notification, reconciliation, lifecycle, and Stripe-isolation behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-09-SUMMARY.md` when done.
</output>

---
phase: 219-offline-study-contract
plan: 03
type: execute
wave: 3
depends_on: [219-02]
files_modified:
  - accrue/priv/repo/migrations/20260803040000_create_accrue_offline_proof_state.exs
  - accrue/lib/accrue/entitlements/device.ex
  - accrue/lib/accrue/entitlements/offline/challenge.ex
  - accrue/lib/accrue/entitlements/offline/issuance.ex
  - accrue/lib/accrue/entitlements/offline/registration.ex
  - accrue/lib/accrue/entitlements/offline.ex
  - accrue/test/accrue/entitlements/offline_registration_test.exs
  - accrue/test/mix/tasks/accrue_install_test.exs
autonomous: false
requirements: [OFF-05, OFF-06]
must_haves:
  truths:
    - "Per D-15, host-authenticated challenge, exact installation ID, P-256 device public key, one-time nonce signature, and idempotency key are required before registration or reconnect authority."
    - "Per D-12/D-14, the server recomputes the RFC-7638 thumbprint, stores bounded public verification material/digests only, and never accepts or persists a device private key."
    - "Per D-19/D-21, active/revoked/superseded lifecycle and nonce consumption are serialized by PostgreSQL rows, constraints, and locks; request idempotency is coalescing rather than authority."
    - "The Ecto migration is applied to the real test PostgreSQL database and copied by the installer before registration/reconnect verification proceeds."
  artifacts:
    - path: "accrue/priv/repo/migrations/20260803040000_create_accrue_offline_proof_state.exs"
      provides: "Device public-key extension, one-time challenge table, and issuance/high-water table"
    - path: "accrue/lib/accrue/entitlements/offline/challenge.ex"
      provides: "Hashed, expiring, one-time nonce persistence"
    - path: "accrue/lib/accrue/entitlements/offline/issuance.ex"
      provides: "Privacy-safe proof issuance/key-retention metadata"
    - path: "accrue/lib/accrue/entitlements/offline/registration.ex"
      provides: "P-256 proof-of-possession registration service and typed values"
    - path: "accrue/lib/accrue/entitlements/offline.ex"
      provides: "Public challenge and registration facade"
      exports: ["challenge/3", "register_device/3"]
  key_links:
    - from: "accrue/lib/accrue/entitlements/offline/registration.ex"
      to: "accrue/lib/accrue/entitlements/device.ex"
      via: "locked nonce consumption plus recomputed thumbprint and public JWK"
      pattern: "FOR UPDATE|Challenge.consume|Device.changeset"
    - from: "accrue/test/mix/tasks/accrue_install_test.exs"
      to: "accrue/priv/repo/migrations/20260803040000_create_accrue_offline_proof_state.exs"
      via: "existing wildcard migration propagation"
      pattern: "20260803040000_create_accrue_offline_proof_state"
  assumptions: []
  prohibitions: []
---

<objective>
Add the durable device proof-of-possession, nonce, and issuance/high-water state required for authenticated reconnect, with a mandatory real-database migration gate, per D-12/D-14/D-15/D-19/D-21.

Purpose: Ensure copied/replayed/wrong-device requests cannot reach issuance and that later reconnect has row-locked correctness state.
Output: Migration, extended Device schema, Challenge/Issuance schemas, registration service/facade, installer propagation, and database tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-offline-study-contract/219-CONTEXT.md
@.planning/phases/219-offline-study-contract/219-PATTERNS.md
@.planning/phases/219-offline-study-contract/219-02-SUMMARY.md
@accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs
@accrue/lib/accrue/entitlements/device.ex
@accrue/lib/accrue/repo.ex

<interfaces>
- Existing `%Device{account_id, installation_id, key_thumbprint, state, last_accepted_revision, ...}` is extended, not replaced.
- New challenge API: `Offline.challenge(account, installation_id, opts \\ [])` and `Offline.register_device(account, %Registration.Request{}, opts \\ [])` require host `authorize` callback success.
- Plan 219-04 consumes `%Issuance{kid, revision, disposition, issued_at, fresh_until, expires_at}` and the stored device public JWK under the same account/device lock.
</interfaces>
</context>

## Artifacts this phase produces

- Device field `public_jwk` containing only validated `kty`, `crv`, `x`, and `y`; existing `key_thumbprint` becomes the recomputed RFC-7638 value.
- Table `accrue_entitlement_offline_challenges` with fields `id`, `account_id`, `installation_id`, `nonce_digest`, `purpose`, `expires_at`, `consumed_at`, `idempotency_digest`, and timestamps.
- Table `accrue_entitlement_offline_issuances` with fields `id`, `account_id`, `device_id`, `token_id_hash`, `kid`, `revision`, `disposition`, `issued_at`, `fresh_until`, `expires_at`, `correlation_hash`, and timestamps.
- Named challenge nonce/idempotency uniqueness, issuance token identity and `(device_id, revision, disposition, issued_at)` ordering indexes, plus closed domain/nonnegative/time-order checks.
- `%Offline.Challenge.Value{nonce, expires_at, purpose}`, `%Offline.Registration.Request{installation_id, device_public_jwk, challenge_id, nonce_signature, idempotency_key}`, and `%Offline.Registration.Result{installation_id, key_thumbprint, state}`.
- `Offline.challenge/3` and `Offline.register_device/3`; no new CLI flag, route, environment variable, or external API.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Confirm the additive offline persistence door</name>
  <files>None; this checkpoint is read-only and precedes every schema modification.</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-14/D-15/D-18 through D-21)
    - accrue/lib/accrue/entitlements/device.ex (existing durable device contract)
    - accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs (host-installed schema precedent)
  </read_first>
  <decision>Confirm the planned additive database contract before creating persistent public-device-key, challenge, and issuance/high-water records.</decision>
  <context>Undoing this choice after hosts migrate requires another data migration. The planned shape extends the existing account-scoped Device, stores only public P-256 material/digests/bounded ordering metadata, and preserves host-owned Repo/runtime boundaries.</context>
  <options>
    <option id="approve-additive-offline-state">
      <name>Approve planned schema</name>
      <pros>Meets D-15/D-18/D-21 with database-serialized nonce, PoP, issuance, rotation, and high-water state.</pros>
      <cons>Adds two tables and one public-JWK field that later hosts must migrate forward if the contract changes.</cons>
    </option>
    <option id="stop-and-replan">
      <name>Stop and replan</name>
      <pros>A different durable representation can be decided before any schema is applied.</pros>
      <cons>Blocks OFF-05/OFF-06 because thumbprint-only Device rows cannot verify reconnect proof-of-possession or retain issuance order.</cons>
    </option>
  </options>
  <action>Make no file or database change. Review the exact fields/indexes/constraints listed under `Artifacts this phase produces`; implementation proceeds only after the approval signal.</action>
  <acceptance_criteria>The user explicitly selects `approve-additive-offline-state`; any other response halts before the migration file is created or applied.</acceptance_criteria>
  <verify><automated>test -f accrue/lib/accrue/entitlements/device.ex &amp;&amp; test -f accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs</automated></verify>
  <done>The one-way schema door is explicitly approved without changing any locked Phase-219 policy decision.</done>
  <resume-signal>Select `approve-additive-offline-state` to proceed, or `stop-and-replan`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add constrained challenge, device-key, and issuance persistence</name>
  <reversibility rating="one-way">The migration persists public-key and proof-order state; removing or reshaping it later requires a host database migration.</reversibility>
  <files>accrue/priv/repo/migrations/20260803040000_create_accrue_offline_proof_state.exs, accrue/lib/accrue/entitlements/device.ex, accrue/lib/accrue/entitlements/offline/challenge.ex, accrue/lib/accrue/entitlements/offline/issuance.ex, accrue/test/accrue/entitlements/offline_registration_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-14/D-15/D-18 through D-21)
    - accrue/lib/accrue/entitlements/device.ex (existing lifecycle/uniqueness contract)
    - accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs (device table and Accrue.Migration conventions)
    - accrue/priv/repo/migrations/20260803031000_create_accrue_apple_reconciliation_checkpoints.exs (named reversible CHECK constraints)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (create-or-lock durable row pattern)
    - accrue/test/accrue/entitlements/persistence_test.exs (RepoCase/direct constraint tests)
  </read_first>
  <behavior>
    - Test 1: valid bounded P-256 public JWK, thumbprint, challenge, and issuance metadata round-trip; no private JWK member is cast or persisted.
    - Test 2: direct invalid writes fail named database constraints for key shape, challenge purpose/time/consumption, nonnegative revision, disposition, and issue/fresh/expiry order.
    - Test 3: duplicate nonce/idempotency/token identities coalesce or conflict deterministically and active device uniqueness remains account-scoped.
  </behavior>
  <action>Write RepoCase and direct-constraint tests first. Extend `Device` with a map field holding the four validated public EC members; replace the Phase-216 opaque thumbprint comment/validation with exact base64url SHA-256 RFC-7638 recomputation and reject private/extra/malformed key material. Create Challenge and Issuance schemas plus migration using `Accrue.Migration.table/index/references`, binary IDs, explicit foreign keys, bounded strings, UTC microsecond timestamps, named partial/unique indexes, and reversible PostgreSQL CHECK constraints. Store challenge/idempotency/token/correlation digests, never raw nonce after response, raw idempotency key, compact proof, account token, provider identity, or private key. Keep issuance `expires_at` nullable only when no real hard bound exists, while enforcing `issued_at <= fresh_until <= expires_at` when present. After the migration and schemas compile, apply them to the real test repository with `MIX_ENV=test mix ecto.migrate --quiet`, then execute the focused registration persistence suite in this task so every authored database/public-JWK/nonce/issuance/privacy invariant receives executable feedback before registration behavior is added.</action>
  <acceptance_criteria>
    - Ecto changesets and PostgreSQL constraints enforce the same public-key, lifecycle, domain, numeric, and temporal rules.
    - All durable fields are bounded/orderable or digests; no proof/provider/PII/private-key archive exists.
    - Existing Device account-scoped current-installation/current-thumbprint identities remain intact.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix test test/accrue/entitlements/offline_registration_test.exs &amp;&amp; mix format --check-formatted priv/repo/migrations/20260803040000_create_accrue_offline_proof_state.exs lib/accrue/entitlements/device.ex lib/accrue/entitlements/offline/challenge.ex lib/accrue/entitlements/offline/issuance.ex test/accrue/entitlements/offline_registration_test.exs</automated></verify>
  <done>The migration is applied to the test repository and the focused suite executes every durable registration, nonce, issuance, ordering, and privacy invariant before public registration behavior is added.</done>
</task>

<task type="auto" tdd="true" gate="blocking-schema-push">
  <name>Task 2: Apply the Ecto migration gate and register a proof-of-possession device</name>
  <precondition>The local test PostgreSQL repository is reachable and Phase 216 entitlement persistence migrations are applied.</precondition>
  <files>accrue/lib/accrue/entitlements/offline/registration.ex, accrue/lib/accrue/entitlements/offline.ex, accrue/test/accrue/entitlements/offline_registration_test.exs, accrue/test/mix/tasks/accrue_install_test.exs</files>
  <read_first>
    - accrue/priv/repo/migrations/20260803040000_create_accrue_offline_proof_state.exs (Task 1 schema; must be applied before behavior verification)
    - accrue/lib/accrue/entitlements/device.ex (Task 1 exact JWK/thumbprint constraints)
    - accrue/lib/accrue/entitlements/offline/challenge.ex (Task 1 one-time state)
    - accrue/lib/accrue/entitlements/offline.ex (public facade modified by this task)
    - accrue/lib/accrue/entitlements.ex (authorization callback/public telemetry pattern)
    - accrue/test/mix/tasks/accrue_install_test.exs (wildcard migration-copy contract)
  </read_first>
  <behavior>
    - Test 1: after host authorization, challenge creation returns one raw nonce once while persistence contains only its digest and expiry.
    - Test 2: registration verifies a signature over version/purpose/account/installation/challenge/idempotency context, consumes the challenge under lock, recomputes thumbprint, and creates one active device.
    - Test 3: nonce replay, expired/consumed challenge, wrong account/installation/key/signature/purpose, unauthorized host, or idempotency-body mismatch creates no device; exact idempotent replay returns the prior bounded result.
    - Test 4: installer output contains the migration exactly once.
  </behavior>
  <action>First run the mandatory blocking Ecto schema application `MIX_ENV=test mix ecto.migrate --quiet`; stop this task if it fails. Then write failing registration tests. Implement host-authorized challenge and registration through `Offline` with `span_private`. Sign/verify a canonical length-prefixed byte sequence containing protocol version, purpose, opaque account UUID, installation ID, challenge ID, nonce, and idempotency digest using the submitted P-256 public key; do not accept a client-provided thumbprint. Lock the challenge row before expiry/unused checks and device insertion, set `consumed_at` in the same transaction, and use database uniqueness for races. Hash nonce/idempotency values before persistence and redact all public key/proof/signature/account token bytes from telemetry and inspected results per D-14/D-22. Extend the real installer wildcard assertion for the exact migration filename and named constraint.</action>
  <acceptance_criteria>
    - `MIX_ENV=test mix ecto.migrate --quiet` succeeds before registration tests run.
    - Only an authorized, fresh, one-time, correctly signed, account/installation-bound request creates or idempotently returns a device.
    - Replay/race/wrong-binding paths fail closed and leave no partially registered device.
    - Generated host migration propagation is executable and exact.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix test test/accrue/entitlements/offline_registration_test.exs test/mix/tasks/accrue_install_test.exs</automated></verify>
  <done>The real database schema is current and registered-device identity is established only by authenticated one-time P-256 proof-of-possession.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host-authenticated account + device challenge response -> Device | Untrusted installation/key/signature input attempts to establish durable identity. |
| Ecto changesets -> PostgreSQL | Races and direct writes must not bypass lifecycle, nonce, or ordering rules. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-219-08 | Spoofing | device registration | high | mitigate | Task 2 requires host auth, exact canonical PoP, recomputed thumbprint, and account/installation binding. |
| T-219-09 | Elevation of Privilege | nonce/idempotency replay | high | mitigate | Task 2 locks and consumes hashed one-time challenge state in the device transaction; uniqueness closes races. |
| T-219-10 | Tampering | persistence bypass | high | mitigate | Task 1 mirrors domains/time/order in named PostgreSQL constraints and proves direct invalid writes fail. |
| T-219-11 | Information Disclosure | stored/logged key/request data | high | mitigate | Tasks 1-2 retain public JWK only where PoP requires it, persist all request secrets as digests, and test telemetry/result redaction. |
</threat_model>

<verification>
The blocking schema gate is `cd accrue &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet`. Then run registration, persistence, and installer tests plus focused formatting.
</verification>

<success_criteria>
- The new Ecto migration applies and propagates to generated hosts.
- Device registration is authenticated, one-time, replay-safe, account-bound, and database-serialized.
- Durable proof state contains no signing secret, compact proof, provider evidence, or PII.
</success_criteria>

<output>
Create `.planning/phases/219-offline-study-contract/219-03-SUMMARY.md` when done.
</output>

---
phase: 219-offline-study-contract
plan: 04
type: execute
wave: 4
depends_on: [219-03]
files_modified:
  - accrue/lib/accrue/entitlements/offline/issuer.ex
  - accrue/lib/accrue/entitlements/offline/issuance.ex
  - accrue/lib/accrue/entitlements/offline/key_provider.ex
  - accrue/lib/accrue/entitlements/offline/source_coordinator.ex
  - accrue/lib/accrue/entitlements/offline/reconnect.ex
  - accrue/lib/accrue/entitlements/offline.ex
  - accrue/test/accrue/entitlements/offline_reconnect_test.exs
autonomous: true
requirements: [OFF-02, OFF-05, OFF-06]
must_haves:
  truths:
    - "Per D-16/D-17, reconnect refreshes only required due sources, returns bounded `pending` for every unresolved due source, queues/coalesces durable repair, and never signs a partial positive result."
    - "Per D-18/D-19, terminal issuance locks/rereads account and device, reads the committed Snapshot/revision, then atomically records/signs/self-verifies allow or deny; revoked-device denial may bypass unrelated provider refresh only after PoP."
    - "Per D-15/D-20, account authentication and one-time device PoP precede reconnect; client high-water/proof values are comparison hints only and never provider, billing, snapshot, or device truth."
    - "Per D-21/D-22, database locks/constraints are authorization authority while idempotency/Oban only coalesce; outcomes and telemetry are bounded/redacted."
    - "Successful allow issuance sets `fresh_until` to the earlier of `iat + 30 days` and any known snapshot provider/access bound; `exp` is only an explicit real bound per D-04."
    - "Per D-11, each issued `kid` has a durable retirement requirement: finite eligibility is `max(actual expires_at) + 86,400 seconds`, any issuance without `expires_at` is ineligible for normal retirement, and omission before eligibility makes JWKS configuration invalid."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/offline/issuer.ex"
      provides: "Locked canonical snapshot-to-allow/deny JWS issuance"
    - path: "accrue/lib/accrue/entitlements/offline/issuance.ex"
      provides: "Privacy-safe proof issuance metadata and per-kid retirement-eligibility query"
    - path: "accrue/lib/accrue/entitlements/offline/key_provider.ex"
      provides: "Pure retained-key validation against durable per-kid requirements"
    - path: "accrue/lib/accrue/entitlements/offline/source_coordinator.ex"
      provides: "Due-source inspection and bounded inline/durable repair contract"
    - path: "accrue/lib/accrue/entitlements/offline/reconnect.ex"
      provides: "Authenticated hybrid reconnect coordinator"
    - path: "accrue/lib/accrue/entitlements/offline.ex"
      provides: "Public issue and reconnect facade"
      exports: ["issue/3", "reconnect/3"]
    - path: "accrue/test/accrue/entitlements/offline_reconnect_test.exs"
      provides: "PostgreSQL transaction, due/pending, revocation, race, and fault tests"
  key_links:
    - from: "accrue/lib/accrue/entitlements/offline/reconnect.ex"
      to: "accrue/lib/accrue/entitlements/source/registry.ex"
      via: "configured sources plus explicit due/repair outcomes"
      pattern: "Source.Registry|SourceCoordinator"
    - from: "accrue/lib/accrue/entitlements/offline/issuer.ex"
      to: "accrue/lib/accrue/entitlements/snapshot.ex"
      via: "account/device `FOR UPDATE` then committed `Snapshot.fetch/2`"
      pattern: "FOR UPDATE|Snapshot.fetch"
    - from: "accrue/lib/accrue/entitlements/offline/issuer.ex"
      to: "accrue/lib/accrue/entitlements/offline/key_provider.ex"
      via: "sign and public-key self-verification before transaction commit"
      pattern: "KeyProvider.sign|Proof.verify"
    - from: "accrue/lib/accrue/entitlements/offline.ex"
      to: "accrue/lib/accrue/entitlements/offline/issuance.ex"
      via: "JWKS rendering queries actual per-kid issued-proof horizons before accepting provider key removal"
      pattern: "retirement_requirements|verification_keys"
  assumptions:
    - requirement_id: OFF-05
      category: unclassified
      status: unresolved
      statement: "The host-configured SourceCoordinator is the executable schedule/repair bridge for every registered source; Phase 219 does not invent provider truth or treat Source.Registry's inspection vocabulary alone as freshness evidence."
  prohibitions: []
---

<objective>
Deliver authenticated due-source reconnect and atomic canonical proof issuance, including pending repair, revoked-device tombstones, transaction locking, self-verification, and bounded telemetry per D-15 through D-22.

Purpose: Converge offline continuity back to current account truth without partial allows, client-truth inversion, or race-dependent proof state.
Output: Issuer, source coordinator, reconnect service/facade, durable issuance updates, and real PostgreSQL integration/fault tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-offline-study-contract/219-CONTEXT.md
@.planning/phases/219-offline-study-contract/219-RESEARCH.md
@.planning/phases/219-offline-study-contract/219-03-SUMMARY.md
@.planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md
@.planning/phases/217-canonical-projection-and-compatibility/217-02-SUMMARY.md
@accrue/lib/accrue/entitlements/apple/reconciliation.ex
@accrue/lib/accrue/entitlements/source/registry.ex
@accrue/lib/accrue/entitlements/snapshot.ex

<interfaces>
- `Offline.register_device/3` from Plan 219-03 establishes account-scoped P-256 PoP and one-time challenges.
- `Snapshot.fetch(repo, account)` returns normalized plans/features/quantities plus authorization bounds and current persisted revision.
- New `SourceCoordinator` callback returns closed `%SourceStatus{source, environment, due, state, retry_after, next_action}` entries; terminal states are `resolved|pending|needs_repair`.
- New facade: `Offline.reconnect(account, %Reconnect.Request{}, opts \\ []) :: {:ok, Reconnect.Outcome.t()} | {:error, reason}`.
- D-11 retirement contract: `Issuance.retirement_requirements(repo, now, opts)` returns bounded per-`kid` requirements consumed by the pure KeyProvider JWKS renderer; the documented minimum `key_retirement_buffer_seconds` is 86,400 and cannot be reduced.
</interfaces>
</context>

## Artifacts this phase produces

- `%Offline.Issuer.Request{account_id, device_id, now, correlation_id, denial_reason}` and `Issuer.issue_in_transaction/3` plus public `Offline.issue/3` authorized wrapper.
- `%Offline.SourceCoordinator.SourceStatus{source, environment, due, state, retry_after, next_action}` and callbacks `due_sources/4`, `refresh/5`, `enqueue_repair/5`.
- `%Offline.Reconnect.Request{installation_id, challenge_id, nonce_signature, idempotency_key, client_revision, client_disposition, client_issued_at}`; client fields are hints only.
- `%Offline.Reconnect.Outcome{disposition, reason, next_action, retry_after, proof, revision, due_source_count}` with dispositions `issued|pending|needs_repair`.
- `Offline.issue/3` and `Offline.reconnect/3`; no route, worker, environment variable, database field, or CLI flag is added in this plan.
- Telemetry events `[:accrue, :entitlements, :offline, :issue]` and `[:accrue, :entitlements, :offline, :reconnect]` with only D-22 allowlisted metadata.
- `Offline.verification_keys/1` resolves durable per-`kid` requirements before rendering: a finite key becomes removable exactly at `max(actual expires_at) + 86,400 seconds`; a key with any unbounded issuance is retained indefinitely under normal rotation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Issue a self-verified proof from one locked canonical snapshot</name>
  <reversibility rating="costly">D-18 transaction and issuance ordering are shared correctness contracts, but signing/provider internals remain replaceable behind existing seams.</reversibility>
  <files>accrue/lib/accrue/entitlements/offline/issuer.ex, accrue/lib/accrue/entitlements/offline/issuance.ex, accrue/lib/accrue/entitlements/offline/key_provider.ex, accrue/lib/accrue/entitlements/offline.ex, accrue/test/accrue/entitlements/offline_reconnect_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-02/D-04/D-11/D-18 through D-22)
    - accrue/lib/accrue/entitlements/offline/issuance.ex (Plan 219-03 schema/order fields)
    - accrue/lib/accrue/entitlements/offline/key_provider.ex (Plan 219-01 signing/public-key boundary)
    - accrue/lib/accrue/entitlements/offline/proof.ex (strict profile/self-verification)
    - accrue/lib/accrue/entitlements/offline.ex (public issue facade modified by this task)
    - accrue/lib/accrue/entitlements/snapshot.ex (current revision and provider/access bounds)
    - accrue/lib/accrue/entitlements/projector.ex (account lock/transaction/audit patterns)
  </read_first>
  <behavior>
    - Test 1: an active registered device and nonempty snapshot issue a fresh allow proof at the locked persisted revision with normalized entitlements and 30-day-or-earlier `fresh_until`.
    - Test 2: an empty resolved snapshot issues a bound signed deny tombstone; a revoked device issues bound `device_revoked` denial even when unrelated sources are due.
    - Test 3: concurrent issue/revocation/projection serializes; no positive proof commits from a stale account/device read.
    - Test 4: signing failure, self-verification failure, key mismatch, injected post-sign/pre-commit failure, or invalid bounds rolls back issuance/device high-water together and returns no proof.
    - Test 5: after issuing with `kid=old`, publishing `old+new` and switching issuance to `new` preserves verification; omitting `old` before `max(old expires_at)+86,400` returns `{:error, :config_invalid}`, while omission exactly at and after eligibility succeeds.
    - Test 6: any actual `old` issuance with null `expires_at` makes normal retirement ineligible, so omitting `old` remains configuration-invalid regardless of elapsed freshness or reconnect time.
  </behavior>
  <action>Write RepoCase/fault tests first. Implement one `Repo.transact` that locks Account then Device in stable order, rereads both, fetches the committed Snapshot, rechecks device lifecycle, derives allow versus denial, and computes `fresh_until = min(iat + 30 days, earliest real snapshot authorization bound)` while setting `exp` only to the real bound. Build D-09 claims, call the host key provider, strictly self-verify the compact JWS against the matching public key/bindings, then insert bounded Issuance metadata and advance device high-water in the same transaction. If snapshot entitlement is empty, sign denial; if device is revoked and PoP was established, sign `device_revoked` denial per D-19. Any signing/verification/persistence fault rolls back and returns no compact candidate. Implement D-11 `Issuance.retirement_requirements/3` grouped by actual issued `kid`: finite eligibility is the maximum non-null `expires_at` plus a documented, enforced minimum 86,400-second clock-skew/reconnect buffer; the presence of any null `expires_at` yields `:never` for normal retirement. Wire `Offline.verification_keys/1` to query those requirements and pass them to Plan 219-01's pure renderer. If the provider omits an active or not-yet-eligible `kid`, return bounded `{:error, :config_invalid}` and publish no JWKS; never infer retirement from `fresh_until`, a fixed calendar age, or provider configuration alone. Prove publish-new-before-issue, overlap, switch, early-removal rejection, exact-boundary eligibility, after-boundary removal, and unbounded-proof retention against real Issuance rows. Do not hold provider-reconciliation network work inside the issuance transaction.</action>
  <acceptance_criteria>
    - Account/device/snapshot reads and issuance/device-high-water writes are one atomic transaction with `FOR UPDATE` locks.
    - Positive claims equal the committed canonical Snapshot; no secondary entitlement reducer exists.
    - Every returned compact proof was self-verified before commit and every fault leaves no issuance/high-water change.
    - Freshness/hard-expiry calculations implement D-02/D-04 exactly.
    - D-11 retirement is calculated from real Issuance rows per `kid`; early or unbounded-key removal cannot produce a JWKS, and exact eligibility is executable.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_reconnect_test.exs --only issuance</automated></verify>
  <done>An authorized terminal call produces only a self-verified proof from locked current state, and every issued `kid` has an enforced durable retirement horizon that prevents invalidating any still-valid proof during normal rotation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile due sources or return durable pending before issuance</name>
  <files>accrue/lib/accrue/entitlements/offline/source_coordinator.ex, accrue/lib/accrue/entitlements/offline/reconnect.ex, accrue/lib/accrue/entitlements/offline.ex, accrue/test/accrue/entitlements/offline_reconnect_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-15 through D-22)
    - accrue/lib/accrue/entitlements/source.ex and accrue/lib/accrue/entitlements/source/registry.ex (closed source vocabulary)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (`due/2`, retry, row locks, pending/needs_repair patterns)
    - accrue/lib/accrue/entitlements/apple/reconciliation_wakeup.ex (durable coalescing)
    - accrue/lib/accrue/entitlements/offline/issuer.ex (Task 1 terminal issuance seam)
    - accrue/lib/accrue/entitlements/offline/registration.ex (PoP/challenge consumption)
    - accrue/lib/accrue/entitlements/offline.ex (public reconnect facade modified by this task)
  </read_first>
  <behavior>
    - Test 1: authenticated active-device reconnect with no due sources reaches issuance; healthy nondue sources are not refreshed.
    - Test 2: every due source is attempted within the configured bounded inline budget; all resolved reaches issuance once.
    - Test 3: retrying/rate-limited/unavailable/quarantined source returns pending with bounded reason/retry_after/next_action, enqueues/coalesces repair, retains prior client cache by returning no proof, and never calls issuer.
    - Test 4: terminal needs_repair is explicit; duplicate idempotency coalesces work but concurrent requests still serialize at challenge/account/device/database locks.
    - Test 5: client proof/high-water hints cannot create grants, suppress due refresh, bypass revocation, select provider state, or raise the server revision.
  </behavior>
  <action>Write Fake-first integration tests around a host-configured SourceCoordinator. Validate configured sources through `Source.Registry`, obtain explicit schedule state from the coordinator, and refresh only due rows. Bound synchronous work with an injected monotonic deadline; feed longer/retryable work to `enqueue_repair/5`, preserving existing Apple checkpoint/wakeup/Retry-After semantics and host-owned Oban. If any due source is unresolved, return `pending`/`needs_repair` with no compact proof and do not call Issuer. If all settle, call Task 1's locked issuer. Authenticate the Account and consume one-time device PoP before inspecting client hints. Locally revoked device is the only path allowed to bypass unrelated due-source refresh for a bound denial. Emit only D-22 fields; test that proof bytes, key material, account/install tokens, provider evidence, and PII never enter telemetry or diagnostics.</action>
  <acceptance_criteria>
    - Nondue sources are untouched; every required due source either resolves or prevents positive issuance.
    - Pending is typed/durable/coalesced and contains no replacement proof, preserving the prior verified cache.
    - Client hints are comparison diagnostics only and cannot alter provider/account/device truth.
    - Reconnect races and idempotent duplicates cannot produce stale or partial positive proof.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_reconnect_test.exs</automated></verify>
  <done>Authenticated reconnect either converges every due source and atomically issues current allow/deny truth, or returns bounded durable pending with the prior client cache untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reconnect request -> source coordination | Auth/PoP/client hints cross into due-work selection. |
| resolved sources -> locked issuer | Only committed canonical truth may become a proof. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-219-12 | Elevation of Privilege | partial due-source result | high | mitigate | Task 2 forbids issuer invocation until every required due source resolves and tests all unresolved states. |
| T-219-13 | Tampering | client high-water/proof hints | high | mitigate | Task 2 proves hints cannot change due selection, snapshot, revision, lifecycle, or issuance. |
| T-219-14 | Tampering | issuance race | high | mitigate | Task 1 locks/rereads account/device, self-verifies, and commits issuance/high-water atomically. |
| T-219-15 | Denial of Service | due-source reconnect | medium | mitigate | Task 2 bounds inline work, honors Retry-After, coalesces durable repair, and exposes retry/queue telemetry. |
| T-219-16 | Information Disclosure | reconnect telemetry/outcomes | high | mitigate | Task 2 allowlists metadata and recursively tests absence of proof/key/account/provider/PII values. |
| T-219-22 | Denial of Service | JWKS key rotation | high | mitigate | Task 1 derives per-`kid` retention from actual issuance expiry, rejects early removal as configuration-invalid, and retains keys indefinitely when an issued proof has no hard expiry. |
</threat_model>

<verification>
Run `cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_reconnect_test.exs test/accrue/entitlements/offline_registration_test.exs test/accrue/entitlements/projector_test.exs` and focused format checks.
</verification>

<success_criteria>
- Due-source unresolved states can never produce partial positive proof.
- Terminal issuance is snapshot/device/current-revision atomic and self-verified.
- Revocation, concurrency, idempotency, provider delay, and fault boundaries are database-backed tests.
- New-key publication, old-key overlap, before/at/after retirement eligibility, and unbounded-proof retention are database-backed tests.
</success_criteria>

<output>
Create `.planning/phases/219-offline-study-contract/219-04-SUMMARY.md` when done.
</output>

---
phase: 220-first-adopter-proof-and-release-gates
plan: 07
type: execute
wave: 2
depends_on: [220-01]
files_modified:
  - accrue/priv/entitlements/v1.59-reference-scenarios.json
  - accrue/lib/accrue/entitlements/reference_scenarios.ex
  - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
  - examples/accrue_host/test/accrue_host/reference_scenario_conformance_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Per D-01 and PROOF-01, Apple purchase to web login and Stripe purchase to iOS login each drive one synthetic account through real production entitlement authority and compare the resulting bounded snapshot, purchase, offline-policy, and audit values with the shared fixture."
    - "Per D-02, the tracer rows contain only closed synthetic inputs, ordered actions, a frozen clock, bounded expectations, and deterministic_conformance lane metadata; the fixture remains data and never computes an outcome."
    - "Per D-04, the Phoenix host test owns its Repo/config boundary and invokes Accrue contexts rather than treating fixture loading as host integration."
    - statement: "Per D-03, deterministic server/host convergence and the Swift fixture suite do not establish Crosswake mobile runtime feasibility; the capability report remains feasibility_blocked without both bridge and physical-device evidence."
      verification: backstop
  artifacts:
    - path: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      provides: "Bounded synthetic action inputs for the two cross-rail tracer scenarios"
    - path: "accrue/lib/accrue/entitlements/reference_scenarios.ex"
      provides: "Closed validation of tracer inputs without entitlement reduction"
    - path: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      provides: "Real-Repo production-context Apple-to-web and Stripe-to-iOS proof"
    - path: "examples/accrue_host/test/accrue_host/reference_scenario_conformance_test.exs"
      provides: "The same proof through AccrueHost.Repo and host configuration"
  key_links:
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/lib/accrue/entitlements/apple/intake.ex"
      via: "Accrue.Entitlements.observe_apple_evidence/3 admits synthetic verified Apple evidence"
      pattern: "observe_apple_evidence|Apple.Intake"
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/lib/accrue/entitlements/projector.ex"
      via: "Observation.insert_idempotently/2 and Projector.project/2 establish Stripe projection"
      pattern: "Observation.insert_idempotently|Projector.project"
    - from: "examples/accrue_host/test/accrue_host/reference_scenario_conformance_test.exs"
      to: "examples/accrue_host/lib/accrue_host/repo.ex"
      via: "host-owned sandbox Repo passed to production contexts"
      pattern: "AccrueHost.Repo"
  assumptions: []
  prohibitions:
    - requirement_id: PROOF-01
      statement: "MUST NOT describe vector, Fake, simulator, browser, or deterministic convergence evidence as proof of Crosswake mobile runtime feasibility."
      status: unverified
      flagged: true
    - requirement_id: PROOF-02
      statement: "MUST NOT calculate entitlement, purchase, repair, or offline-policy results inside the scenario loader, fixture helpers, host consumer, or Swift consumer as a second reducer."
      status: unverified
      flagged: true
---

<objective>
Close the hollow PROOF-01 tracer by executing the Apple-to-web and Stripe-to-iOS scenario rows through actual entitlement contexts and the host-owned Repo boundary per D-01 through D-04.

Purpose: Turn the shared cross-rail IDs from fixture assertions into production-authority evidence without overstating unavailable Crosswake runtime feasibility.
Output: A production-delegating core tracer, an equivalent Phoenix-host integration consumer, and the bounded fixture input contract needed by both.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-RESEARCH.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-PATTERNS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs`: production-context tracer evidence for both cross-rail directions.
- `examples/accrue_host/test/accrue_host/reference_scenario_conformance_test.exs`: the same account flows under the actual host Repo/config boundary.
- `accrue/priv/entitlements/v1.59-reference-scenarios.json` and `Accrue.Entitlements.ReferenceScenarios`: bounded data inputs that select production operations but never derive their outcomes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive Apple purchase through production authority to the web snapshot</name>
  <reversibility rating="costly">Per D-01, extending the versioned action-input contract affects Elixir, host, Swift, generated matrix, and CI consumers.</reversibility>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <read_first>
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md (first gap and failed key link)
    - accrue/priv/entitlements/v1.59-reference-scenarios.json (`apple_purchase_to_web_login` and `stripe_purchase_to_ios_login`)
    - accrue/lib/accrue/entitlements/reference_scenarios.ex (`actions!/1`, `valid?/1`, closed schema)
    - accrue/test/accrue/entitlements/apple_observation_tracer_test.exs (`FakeVerifier`, signed synthetic evidence, public `observe_apple_evidence` path)
    - accrue/test/accrue/entitlements/projector_test.exs (real Repo observation/project/snapshot/audit pattern)
    - accrue/test/accrue/entitlements/purchase_decision_test.exs (`PurchaseDecision.evaluate/4` bounded contract)
    - accrue/test/support/entitlements/offline_golden_vector_verifier.ex (production `Offline.verify/3` and `Offline.action_policy/2` fixture-consumer pattern)
    - examples/crosswake_tracer/capability-report.json (current feasibility authority)
  </read_first>
  <behavior>
    - Test 1: `apple_purchase_to_web_login` starts with a newly persisted entitlement account, admits synthetic Apple purchase evidence through `Accrue.Entitlements.observe_apple_evidence/3`, and reads the same account through `Accrue.Entitlements.snapshot/1`.
    - Test 2: the returned snapshot revision, plans, source rails, opposite-rail `PurchaseDecision.evaluate/4` status/reason, production `Offline.verify/3` plus `Offline.action_policy/2` action, and bounded audit count equal the fixture expectations.
    - Test 3: changing a fixture expected value makes the test fail while changing only fixture order cannot bypass the production calls.
    - Test 4: closed action input rejects unknown keys, unsupported operation kinds, raw evidence/proof/token/PII fields, non-UTC time, unbounded strings, or provider payloads.
    - Test 5: the Crosswake capability report remains `feasibility_blocked` and the Swift suite remains a language-neutral consumer only.
  </behavior>
  <action>Replace the fixture-only tracer test with `Accrue.RepoCase, async: false` and a real sandbox transaction. Extend only the two tracer actions with allowlisted, synthetic operation inputs needed to invoke production contexts: rail/environment, logical product, opaque lineage/event identifiers, provider order/lifecycle, public offline vector/action selector, and frozen timestamp. Validate those inputs in `ReferenceScenarios`; do not add result calculation or accept raw signed/provider material. Configure the existing Fake verifier as in `apple_observation_tracer_test.exs`, call the public Apple observation context, then obtain Snapshot, PurchaseDecision, Offline action policy, and immutable event count from their production modules. Assert bounded normalized values against the fixture. When production output contradicts a fixture expectation, correct the fixture because D-01 makes production contexts authoritative; do not change production behavior to satisfy fixture data. Keep the capability report byte-unchanged per D-03.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs --only tracer --seed 458442 &amp;&amp; cd ../examples/crosswake_tracer &amp;&amp; swift test --filter ReferenceScenarioTests</automated>
  </verify>
  <acceptance_criteria>
    - The tracer test contains direct calls to Apple observation, snapshot, purchase-decision, and offline production contexts and passes against `Accrue.TestRepo`.
    - The observed snapshot/purchase/offline/audit tuple is compared field-for-field with `apple_purchase_to_web_login.expected`.
    - No fixture or helper computes a grant union, revision, purchase decision, proof state, or policy result.
    - Crosswake remains `feasibility_blocked`; no physical-device or bridge claim is manufactured.
  </acceptance_criteria>
  <done>The Apple purchase and subsequent web read use the same persisted account and match the shared bounded expectations through production authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the inverse Stripe path through the Phoenix host boundary</name>
  <reversibility rating="reversible">The host test orchestration is local to test code and delegates through already-stable production interfaces.</reversibility>
  <files>accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs, examples/accrue_host/test/accrue_host/reference_scenario_conformance_test.exs</files>
  <read_first>
    - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs (Task 1 production executor and bounded comparator)
    - accrue/test/accrue/entitlements/projector_test.exs (`Observation.insert_idempotently/2`, `Projector.project/2`, audit count)
    - examples/accrue_host/test/support/data_case.ex (host sandbox ownership)
    - examples/accrue_host/test/accrue_host_web/live/entitlement_diagnostics_live_test.exs (`AccrueHost.Repo` entitlement setup)
    - examples/accrue_host/config/test.exs (host Repo and Accrue integration configuration)
    - accrue/lib/accrue/entitlements/snapshot.ex (`Snapshot.fetch/2` bounded read)
  </read_first>
  <behavior>
    - Test 1: `stripe_purchase_to_ios_login` inserts qualified Stripe evidence through `Observation.insert_idempotently/2`, projects it through `Projector.project/2`, and evaluates the resulting account snapshot/purchase/offline/audit tuple against the fixture.
    - Test 2: the host test uses `AccrueHost.DataCase`, `AccrueHost.Repo`, and host configuration to execute both stable tracer IDs instead of calling only `ReferenceScenarios.fetch!/1`.
    - Test 3: each purchase and login/read side is bound to the same opaque entitlement account; a different account observes no access.
    - Test 4: the host does not reproduce grant, revision, purchase, or offline decision rules and returns only bounded values.
  </behavior>
  <action>Expand the core production executor with the Stripe route using `Observation.insert_idempotently/2` and `Projector.project/2`, then reuse the same bounded comparison contract for `stripe_purchase_to_ios_login`. Convert the host consumer from plain `ExUnit.Case` to the host sandbox case, seed a separate synthetic account per tracer, pass `AccrueHost.Repo` into production calls, and prove both cross-rail directions under host-owned configuration. Keep the host orchestration explicit and narrow: it maps the fixture action kind to a production command but never derives the expected outcome. Assert the wrong account remains empty. Run the unchanged Swift fixture test only as a client-schema and blocked-feasibility check; do not claim an unavailable iOS runtime bridge.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs --only production_scenario --seed 458442 &amp;&amp; cd ../examples/accrue_host &amp;&amp; mix test test/accrue_host/reference_scenario_conformance_test.exs --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - Both cross-rail IDs execute against persisted accounts in the core suite, and both execute through `AccrueHost.Repo` in the host suite.
    - The Stripe path directly invokes Observation, Projector, Snapshot, PurchaseDecision, and Offline as applicable and compares bounded outputs to fixture expectations.
    - The host test fails if its production calls are removed even when fixture loading still succeeds.
    - No live credentials, simulator, physical-device assertion, provider call, or raw evidence is required.
  </acceptance_criteria>
  <done>Apple-to-web and Stripe-to-iOS are real credential-free production-context integrations in core and the reference host, with runtime feasibility still honestly blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| scenario JSON → test orchestration | Repository data selects bounded synthetic operations but must not become decision authority. |
| synthetic Apple/Stripe evidence → production contexts | Untrusted test evidence crosses Intake/Observation validation and database authority. |
| production snapshot → host/Swift assertions | Only bounded account state crosses package/language boundaries. |
| deterministic evidence → capability claim | Green tests cannot impersonate bridge plus physical-device evidence. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-G07-01 | Tampering | fixture consumer authority | high | mitigate | Tasks 1-2 invoke production Intake/Observation/Projector/Snapshot/PurchaseDecision/Offline contexts and compare results; fixture code has no outcome reducer. |
| T-220-G07-02 | Spoofing | runtime capability claim | high | mitigate | Task 1 runs the unchanged Swift/report check and retains descriptor-less D-03 backstop truth. |
| T-220-G07-03 | Information Disclosure | synthetic cross-rail inputs | high | mitigate | Task 1 uses closed bounded identifiers and rejects raw transactions, provider payloads, signed proofs, tokens, and PII. |
| T-220-G07-04 | Elevation of Privilege | host Repo boundary | high | mitigate | Task 2 uses the host sandbox/config path and proves a different account receives no access. |
| T-220-G07-05 | Denial of Service | production scenario execution | medium | mitigate | Inputs and test operations are bounded, credential-free, and transactionally isolated. |
| T-220-G07-SC | Tampering | package-manager installs | low | accept | No package installation occurs; existing locked dependencies are used. |
</threat_model>

## Plan-local source coverage audit

| Source | ID | Feature / requirement | Task(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reference host proves safe multi-rail access | 1-2 | COVERED | Both directions execute through production contexts and host Repo. |
| REQ | PROOF-01 | Apple→web and Stripe→iOS coherent same-account access | 1-2 | COVERED | Core and host production delegation. |
| REQ | PROOF-02 | Credential-free deterministic proof | 1-2 | COVERED | Tracer portion; complete lifecycle expansion is Plan 220-08. |
| RESEARCH | — | Shared corpus is data-only; production contexts are sole reducer | 1-2 | COVERED | Expectations are comparisons, never calculations. |
| RESEARCH | — | Crosswake conformance is not runtime feasibility | 1-2 | COVERED | Report remains blocked. |
| CONTEXT | D-01 | One shared corpus, no second reducer | 1-2 | COVERED | Costly schema rating retained. |
| CONTEXT | D-02 | Ordered closed scenario contract and deterministic lane | 1 | COVERED | Bounded tracer inputs only. |
| CONTEXT | D-03 | No feasibility promotion without bridge/device evidence | 1-2 | COVERED | Backstop truth and descriptor-less prohibition retained. |
| CONTEXT | D-04 | ExUnit/Ecto/Swift semantic roles and host ownership | 1-2 | COVERED | No browser semantic oracle. |
| CONTEXT | D-06 | No raw transactions/proofs/tokens/PII | 1-2 | COVERED | Closed input/output assertions. |
| CONTEXT | Deferred | Google Play, Family Sharing, offers, ownership transfer, migration/proration, broad risk controls | NONE | EXCLUDED | Explicitly out of scope. |

### Spec-less fallback coverage

| Kind | Requirement | Item | Disposition |
|---|---|---|---|
| Edge | PROOF-01 | Review deterministic convergence versus runtime feasibility | BACKSTOP in `must_haves.truths`; tasks provide evidence but do not silently pass judgment. |
| Edge | PROOF-02 | Production context must execute rather than fixture membership alone | COVERED by both task commands. |
| Prohibition | PROOF-01 | No runtime feasibility inflation | FLAGGED-UNVERIFIED and descriptor-less in frontmatter. |
| Prohibition | PROOF-02 | No second reducer in consumers | FLAGGED-UNVERIFIED and descriptor-less in frontmatter. |
| Canon referral | PROOF-01/02 | Generic authorization, injection, and privacy controls | Covered by ASVS L1 threat model and `$gsd-secure-phase`; not minted separately. |

<verification>
Run both task commands, then `cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs --seed 458442 &amp;&amp; cd ../examples/accrue_host &amp;&amp; mix test test/accrue_host/reference_scenario_conformance_test.exs --seed 458442 &amp;&amp; cd ../crosswake_tracer &amp;&amp; swift test --filter ReferenceScenarioTests`. Confirm direct context-call patterns exist in both Elixir consumers and the capability report is byte-unchanged.
</verification>

<success_criteria>
- PROOF-01 is supported by real production-context behavior in core and the anonymized host, not fixture membership.
- D-01 through D-04 and the privacy subset of D-06 remain enforced exactly.
- The shared fixture contains bounded inputs and expected outputs only; production contexts remain sole decision authority.
- Crosswake remains `feasibility_blocked`, and both relevant prohibitions remain descriptor-less and flagged-unverified.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-07-SUMMARY.md` when done.
</output>

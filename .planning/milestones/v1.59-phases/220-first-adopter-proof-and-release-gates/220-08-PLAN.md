---
phase: 220-first-adopter-proof-and-release-gates
plan: 08
type: execute
wave: 6
depends_on: [220-04, 220-06, 220-07]
files_modified:
  - accrue/priv/entitlements/v1.59-reference-scenarios.json
  - accrue/priv/entitlements/v1.59-public-contract.json
  - accrue/lib/accrue/entitlements/reference_scenarios.ex
  - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
  - accrue/test/accrue/entitlements/reference_scenarios_test.exs
  - examples/accrue_host/docs/capability-limits-matrix.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_adoption_proof_matrix.sh
  - accrue/test/accrue/docs/v159_release_contract_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02, PROOF-05]
must_haves:
  truths:
    - "Per D-01/D-02 and PROOF-02, every deterministic_conformance scenario executes its ordered actions through actual Intake, Observation, Projector, Snapshot, PurchaseDecision, Offline, reconnect/device, and audit contexts as applicable, then compares bounded results with the shared fixture."
    - "The deterministic corpus includes restricted expansion, empty evidence, immediately-before/at/after expiry adjacency, equal-order permutations, repeated delivery, concurrent delivery, and interrupted/resumed execution as independently named real-context cases."
    - "Production behavior is authoritative: fixture expectations are corrected when real bounded outputs disagree, while production reducers are not changed merely to preserve fixture values."
    - "Per D-09 through D-11 and PROOF-05, the adoption gate directly validates the canonical hand-authored `accrue/guides/multi-rail-offline-release.md` and the adoption matrix links it; no duplicate guide is created under the host docs directory."
    - statement: "Per D-03, public material and scenario evidence remain within their evidence lanes and do not promote Crosswake beyond feasibility_blocked without bridge and physical-device evidence."
      verification: backstop
    - statement: "Per D-10, the canonical release guide and generated matrix continue to state only the additive multi-rail/offline contract and its explicit privacy, lifecycle, finance, and offline-expansion limits."
      verification: backstop
  artifacts:
    - path: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      provides: "Executable real-context coverage for every deterministic scenario and edge family"
    - path: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      provides: "Complete production-runnable synthetic scenarios including adjacency IDs"
    - path: "accrue/priv/entitlements/v1.59-public-contract.json"
      provides: "Exact updated scenario inventory for the public contract"
    - path: "examples/accrue_host/docs/capability-limits-matrix.md"
      provides: "Regenerated exact scenario/lane matrix"
    - path: "scripts/ci/verify_adoption_proof_matrix.sh"
      provides: "Direct adoption-matrix-to-canonical-release-guide validation"
  key_links:
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      via: "one production execution per deterministic scenario ID, followed by bounded expectation comparison"
      pattern: "deterministic_conformance|ReferenceScenarios.all"
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/lib/accrue/entitlements/offline.ex"
      via: "real verification, action-policy, reconnect, device, rollback, tombstone, and rotation calls as applicable"
      pattern: "Offline.verify|Offline.action_policy|Offline.reconnect"
    - from: "scripts/ci/verify_adoption_proof_matrix.sh"
      to: "accrue/guides/multi-rail-offline-release.md"
      via: "required-file guard plus exact guide-section and adoption-matrix-link checks"
      pattern: "accrue/guides/multi-rail-offline-release.md|multi-rail-offline-release"
  assumptions: []
  prohibitions:
    - requirement_id: PROOF-01
      statement: "MUST NOT describe vector, Fake, simulator, browser, or deterministic convergence evidence as proof of Crosswake mobile runtime feasibility."
      status: unverified
      flagged: true
    - requirement_id: PROOF-02
      statement: "MUST NOT calculate entitlement, purchase, repair, or offline-policy results inside the scenario loader, fixture helpers, host consumer, or Swift consumer as a second reducer."
      status: unverified
      flagged: true
    - requirement_id: PROOF-05
      statement: "MUST NOT publish unsupported capability, runtime-feasibility, provider-control, offline-expansion, or privacy claims that exceed the versioned facts and evidence lane."
      status: unverified
      flagged: true
---

<objective>
Expand the proven tracer into executable production-context evidence for every PROOF-02 lifecycle/edge scenario and close the declared adoption-gate-to-release-guide link per D-01 through D-04 and D-09 through D-11.

Purpose: Ensure requirement-critical behavior and public release guidance cannot regress while fixture membership checks remain green.
Output: Complete deterministic scenario executions, corrected/generated contract artifacts, and a non-vacuous adoption-guide gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-RESEARCH.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-PATTERNS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-04-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-06-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-07-SUMMARY.md
</context>

## Artifacts this phase produces

- An executable scenario table in `reference_scenario_conformance_test.exs` with one production-authority path per deterministic ID.
- Three explicit expiry-adjacency rows plus corrected bounded expectations in the shared reference corpus.
- Regenerated public-contract scenario IDs and capability/limits matrix bytes.
- A direct, tested adoption-gate link to the canonical hand-authored multi-rail/offline release guide.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute every lifecycle, offline, ordering, replay, race, and resume scenario</name>
  <reversibility rating="costly">The scenario IDs and action-input schema are consumed across Elixir, Swift, generated documentation, and CI, so changes require coordinated regeneration.</reversibility>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs, accrue/test/accrue/entitlements/reference_scenarios_test.exs</files>
  <read_first>
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md (second gap and omitted cases)
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-07-SUMMARY.md (proven production executor and bounded comparator)
    - accrue/priv/entitlements/v1.59-reference-scenarios.json (all deterministic IDs and current expected tuples)
    - accrue/lib/accrue/entitlements/reference_scenarios.ex (closed input and empty-snapshot validation)
    - accrue/test/accrue/entitlements/projector_test.exs (repeat, stale, retraction, survivor, event-count patterns)
    - accrue/test/property/entitlement_projection_property_test.exs (empty and microsecond boundary behavior)
    - accrue/test/accrue/entitlements/apple_reconciliation_test.exs (equal-order and reconciliation recovery behavior)
    - accrue/test/accrue/entitlements/offline_reconnect_test.exs (device replacement, tombstone, rollback, rotation, interruption/resume production hooks)
    - accrue/test/accrue/entitlements/purchase_decision_test.exs (duplicate-purchase, stale snapshot, and concurrency contracts)
    - accrue/test/support/entitlements/offline_golden_vector_verifier.ex (public proof/action-policy consumer)
  </read_first>
  <behavior>
    - Test 1: a table-driven assertion proves every deterministic corpus ID was executed, not merely loaded; deleting any executor entry or production call fails coverage.
    - Test 2: `duplicate_purchase_prevention`, `refund_revocation`, and `survivor_grant` assert Snapshot, PurchaseDecision, Offline policy, revision, and immutable audit behavior after real projection transitions.
    - Test 3: `stale_downloaded_study_continuity`, `restricted_expansion`, `offline_reconnect`, `device_replacement`, `deny_tombstone`, `clock_rollback`, and `key_rotation` use production proof/reconnect/device/key paths and assert bounded policy plus audit expectations.
    - Test 4: `empty_evidence_fails_closed` performs no admission, obtains an empty revision-zero snapshot, blocks purchase for missing state, denies offline expansion, and records no audit event.
    - Test 5: add `expiry_immediately_before_boundary`, `expiry_at_boundary`, and `expiry_immediately_after_boundary`; use microsecond-separated frozen clocks and real Snapshot folding/projection to assert inclusion immediately before and exclusion at/after expiry.
    - Test 6: `equal_order_stability` executes both delivery permutations and asserts identical bounded outcome; `repeat_idempotency` executes the same input twice and asserts one durable effect.
    - Test 7: `parallel_execution` uses sandbox-authorized concurrent tasks and production database locks/constraints to equal the sequential result; it does not rely on Oban uniqueness.
    - Test 8: `interrupted_resume` uses existing production failure hooks, asserts no torn pre-commit state, then resumes and converges exactly once.
    - Test 9: runtime_capability and advisory_parity remain schema/lane checks only and are excluded from the merge-blocking production execution enumeration.
  </behavior>
  <action>Build outward from Plan 220-07's production executor. Give each deterministic row enough closed synthetic input to invoke its real context while keeping expected values purely declarative. Add the three named expiry adjacency rows. Extend `ReferenceScenarios` with a closed operation-input schema and the exact empty-snapshot shape needed by production behavior; allow empty plans/sources only for an explicitly zero-access result, not as a general validation relaxation. For every existing deterministic ID, execute the applicable production contexts and compare snapshot revision/plans/source rails, purchase status/reason, offline action, and immutable audit count to the fixture. Use existing interruption and concurrency hooks rather than copying their algorithms. If real behavior and the fixture differ, update the fixture expectation and its consumer assertions because D-01 makes the production contexts authoritative. Add loader/unit coverage for new input fields, adjacency rows, closed lanes, empty-state constraints, duplicate IDs, unknown fields, and sensitive-field rejection. Do not execute runtime or advisory lanes as merge authority.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - The set of production-executed IDs equals the set of fixture rows whose evidence lane is `deterministic_conformance`.
    - Each executed ID asserts snapshot, purchase, offline-policy, and audit outputs; no case passes on ID membership alone.
    - Restricted expansion, empty, all three adjacency positions, equal-order permutations, repeat, concurrent, and interrupted/resumed cases each have a named test failure surface.
    - Concurrency uses database authority, interruption proves pre-commit atomicity and exact-once resume, and tests need no credentials.
    - Fixture helpers select operations and normalize bounded results only; they never implement domain decision rules.
  </acceptance_criteria>
  <done>Every merge-blocking scenario is a deterministic real-context execution with complete bounded assertions, including all verifier-named edge families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate scenario contract and wire the adoption gate to the canonical release guide</name>
  <reversibility rating="costly">Per D-09, scenario inventory, generated matrix, and documentation gates are a coordinated public release contract.</reversibility>
  <files>accrue/priv/entitlements/v1.59-public-contract.json, examples/accrue_host/docs/capability-limits-matrix.md, examples/accrue_host/docs/adoption-proof-matrix.md, scripts/ci/verify_adoption_proof_matrix.sh, accrue/test/accrue/docs/v159_release_contract_test.exs</files>
  <read_first>
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md (third gap and exact missing link)
    - accrue/priv/entitlements/v1.59-public-contract.json (`scenario_ids` exact inventory)
    - accrue/lib/accrue/entitlements/reference_scenarios/markdown.ex (`validate_scenarios/2`, deterministic write/check)
    - examples/accrue_host/docs/adoption-proof-matrix.md (`v1.59 first-adopter path`)
    - accrue/guides/multi-rail-offline-release.md (canonical hand-authored release guide produced by Plan 220-05)
    - scripts/ci/verify_adoption_proof_matrix.sh (current matrix-only `require_substring/2` helper)
    - scripts/ci/verify_release_contract.sh (canonical release-guide checks to compose, not duplicate semantically)
    - accrue/test/accrue/docs/v159_release_contract_test.exs (isolated ROOT_DIR mutation pattern)
  </read_first>
  <behavior>
    - Test 1: public-contract `scenario_ids` exactly equals the expanded corpus and `mix accrue.entitlements.reference_scenarios --write` deterministically regenerates the matrix.
    - Test 2: the adoption matrix links the canonical `accrue/guides/multi-rail-offline-release.md` from its first-adopter path.
    - Test 3: `verify_adoption_proof_matrix.sh` has an explicit required-file guard for the canonical guide and checks its evidence/App Review, privacy/security limits, and release-checklist anchors.
    - Test 4: an isolated source root with the guide missing, link removed, or required guide anchor removed fails with an owning-path message; clean sources pass.
    - Test 5: the script explains that the verifier-cited `examples/accrue_host/docs/multi-rail-offline-release.md` location is intentionally not duplicated because D-09 assigns release explanation to the canonical Accrue guide.
    - Test 6: capability status remains feasibility_blocked and all new scenario IDs remain in deterministic_conformance.
  </behavior>
  <action>Update the public-contract scenario list to exactly match Task 1, run the existing generator write mode, and commit the resulting capability/limits matrix bytes. Add a relative link from the adoption matrix's compact first-adopter path to the canonical `accrue/guides/multi-rail-offline-release.md`. Extend `verify_adoption_proof_matrix.sh` with a separate canonical-guide variable, required-file guard, and exact section/link checks while retaining its existing ORG-09 output contract. Record in the script's ownership note that no second hand-authored guide lives under `examples/accrue_host/docs`: D-09 keeps explanation/procedure in the canonical Accrue guide, and this gate now validates that artifact directly. Extend the isolated docs contract test so missing guide, missing adoption link, and missing required guide sections fail non-vacuously without mutating repository files. Keep exact capability facts owned by the generated fixture/matrix and do not duplicate the Plan-04 semantic needles.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix accrue.entitlements.reference_scenarios --check &amp;&amp; mix test test/accrue/entitlements/reference_scenarios_test.exs test/accrue/docs/v159_release_contract_test.exs --seed 458442 &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_reference_scenario_contract.sh &amp;&amp; bash scripts/ci/verify_adoption_proof_matrix.sh &amp;&amp; bash scripts/ci/verify_release_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - Public-contract IDs and generated matrix scenario rows exactly match the expanded shared corpus.
    - The adoption gate directly names, requires, and validates `accrue/guides/multi-rail-offline-release.md`; it cannot pass when that guide or the adoption-matrix link is absent.
    - The canonical-location ownership note is explicit, so the absent duplicate host-doc path is an accepted authority decision rather than an unexplained omission.
    - Known-bad isolated copies prove the link and guide checks have teeth, while all three clean release gates pass.
    - No generated fact, raw transaction, proof material, token, PII, provider-control claim, or runtime-feasibility claim is widened.
  </acceptance_criteria>
  <done>The expanded scenario inventory is generated consistently and the adoption matrix gate has a direct, non-vacuous link to the canonical release guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| scenario inputs → production contexts | Synthetic data crosses validation, database, proof, device, and ordering boundaries. |
| concurrent/interrupted execution → durable state | Races and failures must not create duplicate or torn authorization/audit outcomes. |
| versioned corpus → generated public matrix | Corrected behavior and added IDs become public release facts. |
| hand-authored guide/adoption matrix → CI | Public procedure and evidence-lane claims become merge-blocking documentation evidence. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-G08-01 | Tampering | scenario outcome authority | high | mitigate | Task 1 proves every deterministic row through production contexts and compares outputs without a fixture reducer. |
| T-220-G08-02 | Tampering | repeat/race/resume durable state | high | mitigate | Task 1 uses database locks/constraints and existing failure hooks with sequential-equivalence, no-torn-state, and exact-once assertions. |
| T-220-G08-03 | Spoofing | evidence lane/runtime claim | high | mitigate | Both tasks preserve closed lanes, `feasibility_blocked`, and bridge/device evidence requirements. |
| T-220-G08-04 | Information Disclosure | fixtures, assertions, public docs | high | mitigate | Closed synthetic schema and known-bad doc tests reject raw transactions, signed proof material, tokens, PII, and provider payloads. |
| T-220-G08-05 | Tampering | adoption/release guide link | high | mitigate | Task 2 requires the canonical guide, adoption link, exact anchors, and isolated fail-first missing-artifact cases. |
| T-220-G08-06 | Denial of Service | parallel scenario suite | medium | mitigate | Bounded inputs, finite concurrency, sandbox ownership, and focused commands prevent unbounded work. |
| T-220-G08-SC | Tampering | package-manager installs | low | accept | No package installation occurs; existing locked dependencies are used. |
</threat_model>

## Multi-source coverage audit — gap-closure plan set

| Source | ID | Feature / requirement | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reference host and release contract prove safe, diagnosable, operable multi-rail/offline access | 220-07, 220-08 | COVERED | Runtime proof, full deterministic expansion, and release link. |
| REQ | PROOF-01 | Apple→web and Stripe→iOS coherent access | 220-07, 220-08 | COVERED | Tracer plus full corpus authority alignment. |
| REQ | PROOF-02 | Credential-free deterministic lifecycle/offline proof | 220-07, 220-08 | COVERED | Every deterministic row and named edge executes. |
| REQ | PROOF-05 | Exact public contract and conformance gates | 220-08 | COVERED | Generated inventory plus adoption-guide link. |
| RESEARCH | — | Shared data-only corpus with production contexts as sole reducer | 220-07, 220-08 | COVERED | Production outputs correct fixture expectations. |
| RESEARCH | — | Real Repo locking, idempotency, interruption, and offline contexts | 220-08 | COVERED | Explicit named scenario assertions. |
| RESEARCH | — | Fixture-generated matrix plus scripted drift gates | 220-08 | COVERED | Existing generator and coordinated gates reused. |
| RESEARCH | — | No new packages or credentials | 220-07, 220-08 | COVERED | Existing Elixir/Phoenix/Swift/Bash stack only. |
| CONTEXT | D-01 | One shared corpus; production contexts are sole authority | 220-07, 220-08 | COVERED | No scenario reducer. |
| CONTEXT | D-02 | Complete actions, clocks, expectations, lanes, and artifacts | 220-07, 220-08 | COVERED | All deterministic rows production-runnable. |
| CONTEXT | D-03 | No runtime feasibility promotion | 220-07, 220-08 | COVERED | Backstop and blocked report retained. |
| CONTEXT | D-04 | Focused ExUnit/Ecto and pure Swift roles | 220-07, 220-08 | COVERED | No browser/store oracle. |
| CONTEXT | D-05 | One bounded diagnostic seam | existing 220-02 | COVERED | Gap tasks consume bounded snapshot/audit only. |
| CONTEXT | D-06 | No raw transactions/proofs/tokens/PII | 220-07, 220-08 | COVERED | Closed schema and negative tests. |
| CONTEXT | D-07 | Distinct safe repairs | existing 220-03 | COVERED | No new repair surface added. |
| CONTEXT | D-08 | Repair drills and DB authority | existing 220-03; 220-08 | COVERED | Scenario executor reuses production failure/locking hooks. |
| CONTEXT | D-09 | Generated exact facts; hand-authored explanation/procedure | 220-08 | COVERED | Canonical guide validated, not duplicated. |
| CONTEXT | D-10 | Additive contract and explicit limits | 220-08 | COVERED | Existing contract gates remain green. |
| CONTEXT | D-11 | Compact adopter path and visible lanes | 220-08 | COVERED | Adoption matrix links command, matrix, guide, runbook. |
| CONTEXT | D-12 | Job/next-action presentation | existing 220-02, 220-03, 220-05 | COVERED | Gap tasks do not alter UI/copy authority. |
| CONTEXT | Deferred | Google Play, Family Sharing, offers, ownership transfer, migration/proration, broad risk controls, unsupported runtime claims | NONE | EXCLUDED | Explicitly out of scope. |

### Complete spec-less fallback coverage

| Kind | Requirement | Exact item | Disposition |
|---|---|---|---|
| Edge | PROOF-01 | Review deterministic convergence versus runtime feasibility | BACKSTOP in frontmatter and capability-report evidence; no silent pass. |
| Edge | PROOF-02 | Empty, adjacency, equal-order, repeat, concurrent, and interrupted/resumed behavior | COVERED by Task 1 named production executions. |
| Edge | PROOF-05 | Review generated/public authority and canonical-guide boundary | BACKSTOP in frontmatter plus Task 2 fail-first evidence; no silent pass. |
| Prohibition | PROOF-01 | No runtime feasibility inflation | FLAGGED-UNVERIFIED and descriptor-less. |
| Prohibition | PROOF-02 | No second reducer | FLAGGED-UNVERIFIED and descriptor-less. |
| Prohibition | PROOF-05 | No unsupported public claim | FLAGGED-UNVERIFIED and descriptor-less. |
| Canon referral | PROOF-01/02/05 | Generic injection, authentication, privacy, and cryptographic controls | Covered by ASVS L1 threat model and `$gsd-secure-phase`; not minted separately. |

<verification>
Run both task commands, then the closure gate: `cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs test/accrue/docs/v159_release_contract_test.exs --seed 458442 &amp;&amp; mix accrue.entitlements.reference_scenarios --check &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_reference_scenario_contract.sh &amp;&amp; bash scripts/ci/verify_adoption_proof_matrix.sh &amp;&amp; bash scripts/ci/verify_release_contract.sh`. Confirm the executed deterministic-ID set equals the corpus lane set and every known-bad guide mutation fails.
</verification>

<success_criteria>
- PROOF-02 is proved by real deterministic executions for every listed lifecycle, offline, adjacency, ordering, repeat, concurrency, and interruption scenario.
- PROOF-01 remains aligned with the production-authority tracer and honest Crosswake status.
- PROOF-05 has an explicit adoption-gate-to-canonical-release-guide link plus regenerated exact scenario inventory.
- D-01 through D-12 remain covered with no deferred item introduced and no silent fallback-probe drop.
- All three retained prohibitions remain descriptor-less and flagged-unverified.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-08-SUMMARY.md` when done.
</output>

---
phase: 220-first-adopter-proof-and-release-gates
plan: 09
type: execute
wave: 7
depends_on: [220-08]
files_modified:
  - accrue/priv/entitlements/v1.59-reference-scenarios.json
  - accrue/lib/accrue/entitlements/reference_scenarios.ex
  - accrue/test/accrue/entitlements/reference_scenarios_test.exs
  - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "Per D-01/D-02 and PROOF-02, every ordered action in every deterministic_conformance row is dispatched to its declared production transition; no row is satisfied by an unconditional qualified-grant setup."
    - "Lifecycle rows prove refund/revocation and survivor behavior by projecting real grant and retract observations, including source-rail, plan, revision, purchase-decision, and immutable-audit outcomes."
    - "Offline rows prove stale continuity, restricted expansion, authenticated reconnect/cache replacement, revoked-device replacement, signed deny tombstones, clock rollback, and key rotation through the existing Offline, Registration, Reconnect, Device, Issuance, and key-retention seams."
    - "Ordering, replay, race, and resume rows execute their complete delivery operations: equal-order permutations converge, repeated insertion is idempotent, concurrent insertion/projection equals sequential execution under database authority, and interrupted reconnect resumes without torn state or duplicate issuance."
    - "The fixture and loader remain closed, synthetic, credential-free, data-only inputs and bounded expectations; production contexts remain the sole decision authority and existing golden-vector, Crosswake feasibility, privacy, finance, and lifecycle boundaries are not relaxed."
    - statement: "Per D-03/D-04, this deterministic server proof does not promote Crosswake beyond feasibility_blocked or substitute for bridge and physical-device evidence."
      verification: backstop
  artifacts:
    - path: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      provides: "Closed ordered command inputs and transition-specific bounded expectations for every deterministic scenario action"
    - path: "accrue/lib/accrue/entitlements/reference_scenarios.ex"
      provides: "Strict discriminated action parser and deterministic execution inventory without entitlement reduction"
    - path: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      provides: "Production-transition dispatcher plus named lifecycle, offline, ordering, replay, race, and resume assertions"
    - path: "accrue/test/accrue/entitlements/reference_scenarios_test.exs"
      provides: "Closed-schema and action/expectation coverage rejection tests"
  key_links:
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      via: "the executed trace of {scenario_id, action.order, action.kind} exactly equals every ordered deterministic fixture action"
      pattern: "action.kind|action.order|deterministic_scenarios"
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/lib/accrue/entitlements/projector.ex"
      via: "grant/retract, ordering, replay, and race actions call Observation.insert_idempotently/2 and Projector.project/2 with the fixture-declared operation identity"
      pattern: "Observation.insert_idempotently|Projector.project"
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/lib/accrue/entitlements/offline.ex"
      via: "offline actions call verify/action_policy, challenge/register_device, reconnect_challenge/reconnect, and verification_keys_with_issued_retention"
      pattern: "Offline.verify|Offline.action_policy|Offline.register_device|Offline.reconnect|Offline.verification_keys_with_issued_retention"
  assumptions: []
  prohibitions:
    - requirement_id: PROOF-02
      statement: "MUST NOT compute entitlement, lifecycle, purchase, offline, device, key, race, or resume decisions inside the fixture loader or test dispatcher."
      status: unverified
      flagged: true
    - requirement_id: PROOF-02
      statement: "MUST NOT weaken existing production tests, golden vectors, fixture validation, database-concurrency assertions, or failure hooks to make the scenario corpus pass."
      status: unverified
      flagged: true
    - requirement_id: PROOF-01
      statement: "MUST NOT describe deterministic, Fake, simulator, browser, or Swift-vector evidence as Crosswake mobile runtime feasibility."
      status: unverified
      flagged: true
---

<objective>
Close the remaining PROOF-02 semantic gap by making every deterministic scenario action execute its declared production transition instead of collapsing to a qualified grant, per D-01 through D-04 and D-08.

Purpose: Ensure green lifecycle/offline/order/replay/race/resume rows fail whenever their corresponding production behavior regresses, while preserving the shared corpus as data rather than a second reducer.
Output: A strict action-specific corpus contract and production-backed conformance suite with exhaustive ordered-action evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-RESEARCH.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-08-SUMMARY.md
@accrue/priv/entitlements/v1.59-reference-scenarios.json
@accrue/lib/accrue/entitlements/reference_scenarios.ex
@accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
@accrue/test/accrue/entitlements/reference_scenarios_test.exs
@accrue/test/accrue/entitlements/projector_test.exs
@accrue/test/accrue/entitlements/offline_reconnect_test.exs
@accrue/test/accrue/entitlements/offline_golden_vectors_test.exs
@accrue/test/accrue/entitlements/purchase_decision_test.exs
@accrue/test/support/entitlements/offline_golden_vector_verifier.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one refund action through the closed corpus and real retraction authority</name>
  <reversibility rating="costly">Per D-01/D-02, the discriminated action/expectation shape is shared by fixture consumers and future conformance maintenance, so changing it later requires coordinated fixture, loader, test, and generated-contract updates.</reversibility>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/accrue/entitlements/reference_scenarios_test.exs, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <behavior>
    - Test 1: `refund_revocation` first projects a qualified Apple grant, then dispatches its fixture-declared `refund_observation` as a higher-order qualified retract through Observation and Projector; the final snapshot has no source or plan, purchase is no longer blocked by equivalent access, offline policy denies, and durable counts match bounded expectations.
    - Test 2: the executed trace contains both ordered lifecycle steps with their fixture timestamps; removing, reordering, renaming, or replacing the retract action with a grant fails.
    - Test 3: deterministic actions accept only the exact closed payload for their kind and a matching transition expectation; missing/extra fields, unknown kinds, output-like decision fields in command payloads, and action/expectation order or kind mismatches are rejected.
    - Test 4: runtime_capability and advisory_parity rows remain read-only lane/schema inputs and never enter the production-action execution inventory.
  </behavior>
  <action>Start red with a tagged `:action_dispatch_tracer` test that demonstrates the current false positive: `refund_revocation` must fail while `observation_attrs/2` still forces `kind: "grant"`. Replace the one-size-fits-all deterministic `Operation` contract with a closed discriminated command contract keyed by each action's existing `kind`; carry only synthetic command inputs needed to call production APIs, and add a closed, bounded transition-expectation list aligned one-for-one by order and kind. Keep snapshot/purchase/offline/audit expectations declarative. Implement a conformance dispatcher whose branches only construct inputs, invoke existing contexts, and normalize their public/durable outputs; it must contain no entitlement, precedence, clock, crypto, cache, or lifecycle decision rule. Make `refund_revocation` declare an initial grant and a later retract with distinct provider event/transaction identities, shared lineage, monotonic provider order, and frozen timestamps. Dispatch both through `Observation.insert_idempotently/2` and `Projector.project/2`, then assert the declared post-action and final bounded states. Per D-01, correct fixture expectations to observed production behavior rather than altering Projector or Snapshot to preserve the old qualified-grant tuple. Preserve the two Plan-07 cross-rail tracers and the empty/expiry/lane rows.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs --only action_dispatch_tracer --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - `refund_revocation` visibly changes durable authorization state through a real retract and cannot pass when the dispatcher substitutes a grant.
    - The loader proves kind-specific input and expectation closure without evaluating a domain outcome.
    - Action timestamps and order are used by execution, not merely parsed.
    - Existing production reducers and Crosswake capability artifacts are unchanged.
  </acceptance_criteria>
  <done>One production-quality lifecycle slice proves fixture action → Observation/Projector transition → bounded final result end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand exhaustive production dispatch across lifecycle and offline actions</name>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/accrue/entitlements/reference_scenarios_test.exs, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <behavior>
    - Test 1: the flattened executed `{scenario_id, order, kind}` set exactly equals all actions from deterministic_conformance rows, while runtime/advisory actions are absent; a missing dispatcher branch or skipped second action fails coverage.
    - Test 2: `survivor_grant` projects Stripe and Apple grants and retracts only Stripe, leaving the Apple source and plan; `duplicate_purchase_prevention` calls the real purchase preflight after projection and asserts the equivalent-other-rail block.
    - Test 3: stale continuity and restricted expansion verify the declared golden proof at the frozen clock and call `Offline.action_policy/2`, proving downloaded study remains allowed while premium download returns reconnect-required.
    - Test 4: `offline_reconnect` uses `Offline.reconnect_challenge/3` and a signed `Reconnect.Request` with `Offline.reconnect/3`, then verifies the newly issued proof and cache-replacement disposition; it does not reuse the fixture's precomputed allow vector as the reconnect result.
    - Test 5: `device_replacement` preserves a durable revoked device and registers a distinct active replacement through `Offline.challenge/3` plus `Offline.register_device/3`; `deny_tombstone` performs authenticated reconnect against no grant, verifies the returned signed denial, and proves denial replaces the prior allow cache.
    - Test 6: `clock_rollback` calls `Offline.verify/3` with the production high-water context and asserts `:clock_rollback` without replacing the authenticated cache; `key_rotation` verifies the rotated-key proof and calls `Offline.verification_keys_with_issued_retention/1` to prove an old key remains required until its issued-proof horizon and becomes retireable afterward.
  </behavior>
  <action>Extend the tracer dispatcher for every lifecycle and offline action kind using the existing helpers and setup patterns from `projector_test.exs`, `offline_reconnect_test.exs`, and `offline_golden_vectors_test.exs`. Make multi-step rows declare every required command explicitly: grants before retractions, reconnect request before verified replacement, revoked registration before replacement, and prior allow cache before signed denial/rollback. Use `action.at` as the production `now`/`observed_at` input wherever the API accepts it and keep all keys, signatures, clocks, identifiers, and provider evidence synthetic. For device/key cases, assert the durable `Device`/`Issuance` facts in addition to the bounded client outcome; do not turn Phase-220 `Repair.replace_revoked_device/3` or `Repair.rotate_signing_keys/3` guidance-only actions into hidden lifecycle mutation. Reuse the existing Offline golden vectors and verifier semantics without editing or weakening them. Update each fixture row's final and per-action expectations to the real outputs, including revisions, source rails, plans, purchase reason, policy action, audit/event count, device states, issuance count, proof state/reason, and required-key set as applicable.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/projector_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - Every deterministic lifecycle/offline action is executed once in declared order and has a named transition assertion beyond the common final tuple.
    - Refund removes access, survivor retraction preserves the other rail, reconnect emits and verifies a new proof, replacement retains revoked history, and deny/rollback/rotation exercise their distinct production boundaries.
    - Existing Projector, Offline, device, issuance, and golden-vector tests remain unchanged and green; fixture expectations yield to those authorities.
    - No live credentials, provider calls, raw transaction data, proof secrets, tokens, PII, or unsupported Crosswake claim is introduced.
  </acceptance_criteria>
  <done>All lifecycle and offline fixture rows prove their advertised production transitions rather than a shared grant path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove ordering, whole-delivery replay, database race equivalence, and interrupted reconnect resume</name>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/accrue/entitlements/reference_scenarios_test.exs, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <behavior>
    - Test 1: `equal_order_stability` executes the same two equal-provider-order observations in both arrival permutations and obtains identical snapshot/source/revision/audit results from Projector's production tie-breaking.
    - Test 2: `repeat_idempotency` repeats the complete Observation insertion plus projection delivery and proves the same durable observation identity, one material projection/audit effect, and stable bounded result.
    - Test 3: `parallel_execution` starts concurrent complete insert-plus-project deliveries from separate sandbox-authorized processes, then compares observation/grant/event counts and bounded snapshot with a sequential control account; the assertion relies on PostgreSQL uniqueness/locking, not Oban uniqueness or a preinserted shared row.
    - Test 4: `interrupted_resume` invokes `Offline.reconnect/3` with the existing `after_admission` interruption hook, proves admitted durable state with no issuance/torn cache replacement, replays the exact signed request without the hook, and proves one completed attempt and one issuance; a second replay returns the same proof without another effect.
    - Test 5: the legacy generic all-row grant loop, the hardcoded `kind: "grant"` helper, preinsert-before-race setup, and projector-only resume substitute are absent from the conformance executor.
  </behavior>
  <action>Give the ordering, repeat, parallel, interruption, and resume actions complete closed command payloads rather than a single shared operation. For equal-order, use two distinct qualified observations with the same provider order and production-recognized ordering fields, execute both permutations on isolated accounts, and compare the full durable bounded result. For repeat and parallel, dispatch the entire delivery boundary (`Observation.insert_idempotently/2` followed by `Projector.project/2`) on every attempt. Use `Ecto.Adapters.SQL.Sandbox.unboxed_run/2` or explicit sandbox allowances so each concurrent process reaches the database, synchronize starts with a barrier, collect all task exits/results, and compare against a sequential control. For interruption/resume, reuse the real reconnect request construction and `after_admission` hook from `offline_reconnect_test.exs`; assert `ReconnectAttempt`, `Challenge.reconnect_outcome`, and `Issuance` before and after replay, then verify the returned proof. Do not simulate failure with a test-local state flag or redefine exact-once semantics in the dispatcher. Finish with an exhaustive trace assertion over all deterministic ordered actions and retain explicit lane exclusions.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/offline_reconnect_test.exs test/accrue/entitlements/projector_test.exs test/accrue/entitlements/purchase_decision_test.exs --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - Equal-order behavior uses genuinely equal ordering inputs and both arrival permutations.
    - Repeat and parallel cases cover insertion plus projection, and parallel state equals a sequential control under real database authority.
    - Interrupted reconnect demonstrates no issuance before resume, one issuance after resume, the same proof on replay, and one completed durable attempt.
    - The executed ordered-action trace is exhaustive and the removed generic substitutes cannot keep the suite green.
  </acceptance_criteria>
  <done>Ordering, replay, race, and resume IDs fail on regressions in their named production transitions and converge to their declared durable outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| scenario JSON → test dispatcher | Synthetic commands select production APIs; fixture data must not become authorization or reduction logic. |
| offline proof/device input → verifier and reconnect | Signed requests, clocks, device identity, and keys cross authentication and replay boundaries. |
| concurrent/interrupted delivery → PostgreSQL state | Races and failures must preserve atomic, idempotent observation, grant, attempt, issuance, cache, and audit state. |
| deterministic evidence → public capability claim | Server conformance cannot establish Crosswake bridge or physical-device runtime feasibility. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-G09-01 | Tampering | fixture/dispatcher authority | high | mitigate | Tasks 1–3 enforce closed kind-specific commands, exhaustive action traces, and production-only decisions with bounded output normalization. |
| T-220-G09-02 | Tampering | lifecycle ordering and retraction | high | mitigate | Tasks 1–3 execute real grant/retract observations, equal-order permutations, and authoritative Projector results. |
| T-220-G09-03 | Spoofing | reconnect/device/proof identity | high | mitigate | Task 2 uses production challenges, signatures, registration, reconnect, verifier, clock, and key-retention checks. |
| T-220-G09-04 | Tampering | concurrent and interrupted durable state | high | mitigate | Task 3 exercises whole-delivery database races and real reconnect interruption hooks with sequential equivalence and exact-once assertions. |
| T-220-G09-05 | Information Disclosure | fixture and test diagnostics | high | mitigate | Tasks retain synthetic identifiers and closed fields; no raw evidence, private keys, proof bytes in diagnostics, tokens, or PII enter the corpus. |
| T-220-G09-06 | Spoofing | Crosswake capability status | high | mitigate | All tasks exclude runtime/advisory lanes from production execution and leave feasibility_blocked subject to bridge/device evidence. |
| T-220-G09-07 | Denial of Service | concurrent conformance test | medium | mitigate | Task 3 uses a bounded two-delivery barrier, explicit task timeouts, and complete result collection. |
| T-220-G09-SC | Tampering | package-manager installs | low | accept | No dependency installation occurs; the plan uses the locked Elixir/Ecto/JOSE test stack. |
</threat_model>

## Multi-source coverage audit — gap-closure plan set

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reference-host proof makes multi-rail/offline access safe and operable | 220-09 | COVERED | Closes the sole remaining deterministic semantic-proof failure. |
| REQ | PROOF-02 | Credential-free lifecycle/offline/order/replay/race/resume proof | 220-09 | COVERED | Every declared deterministic action reaches its production transition. |
| RESEARCH | — | Shared corpus is input/assertion data, never a second reducer | 220-09 | COVERED | Closed command dispatch plus production-authoritative expected corrections. |
| RESEARCH | — | PostgreSQL locks/constraints, not Oban uniqueness, govern races | 220-09 | COVERED | Whole-delivery race is compared to sequential control. |
| RESEARCH | — | Runtime remains blocked without Crosswake bridge/device evidence | 220-09 | COVERED | Lane exclusion and backstop retained. |
| CONTEXT | D-01 | One versioned data-only corpus; production is sole authority | 220-09 | COVERED | Exact ordered-action trace and no fixture reducer. |
| CONTEXT | D-02 | Ordered actions, clocks, bounded expectations, and closed lanes | 220-09 | COVERED | Discriminated commands and per-action transition expectations. |
| CONTEXT | D-03 | No unsupported Crosswake runtime claim | 220-09 | COVERED | feasibility_blocked remains a backstop. |
| CONTEXT | D-04 | ExUnit/Ecto owns semantic proof; Swift remains bounded | 220-09 | COVERED | No browser, simulator, or Swift semantic oracle added. |
| CONTEXT | D-05 | One bounded diagnostic seam | existing 220-02 | COVERED | This gap plan does not alter diagnostics. |
| CONTEXT | D-06 | No raw evidence, proofs, tokens, or PII | 220-09 | COVERED | Closed synthetic payload and negative schema tests. |
| CONTEXT | D-07 | Repairs stay distinct, bounded, and non-mutating outside authority | existing 220-03; 220-09 | COVERED | Guidance-only device/key repairs are not repurposed as lifecycle mutation. |
| CONTEXT | D-08 | Real repair/reconnect drills and database authority | 220-09 | COVERED | Existing failure hooks, attempts, issuance, locks, and constraints are exercised. |
| CONTEXT | D-09 | Generated facts and hand-authored procedure stay separated | existing 220-04/05/08 | COVERED | No generated/public artifact ownership changes. |
| CONTEXT | D-10 | Additive privacy/lifecycle/finance/offline limits | 220-09 | COVERED | Existing boundaries remain explicit prohibitions and regression gates. |
| CONTEXT | D-11 | Compact adopter path and visible evidence lanes | existing 220-05/08 | COVERED | No adopter-path change; deterministic lane becomes truthful. |
| CONTEXT | D-12 | Job-focused accessible operator presentation | existing 220-02/03/05 | COVERED | No UI or copy change. |
| CONTEXT | Deferred | Google Play, Family Sharing, offers, ownership transfer, migration/proration, broad risk controls, unsupported runtime claims | NONE | EXCLUDED | Explicitly outside Phase 220. |

<verification>
Run `cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/projector_test.exs test/accrue/entitlements/offline_reconnect_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs test/accrue/entitlements/purchase_decision_test.exs --seed 458442`, then `mix accrue.entitlements.reference_scenarios --check`, then from the repository root run `bash scripts/ci/verify_reference_scenario_contract.sh &amp;&amp; bash scripts/ci/verify_adoption_proof_matrix.sh &amp;&amp; bash scripts/ci/verify_release_contract.sh`. Confirm the executed ordered-action trace exactly equals the deterministic fixture action inventory and Crosswake remains feasibility_blocked.
</verification>

<success_criteria>
- The verifier's sole remaining PROOF-02 gap is closed by production-transition evidence for lifecycle, offline, ordering, replay, race, and resume rows.
- No deterministic row can pass via the former unconditional qualified-grant flow or a simplified projection-only substitute.
- Fixture/loader/dispatcher changes preserve D-01's data-only boundary and all existing production/golden-vector contracts.
- Crosswake feasibility, public privacy/lifecycle/finance limits, and unrelated user changes remain untouched.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-09-SUMMARY.md` when done.
</output>

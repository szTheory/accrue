---
phase: 220-first-adopter-proof-and-release-gates
plan: 10
type: execute
wave: 8
depends_on: [220-09]
files_modified:
  - accrue/priv/entitlements/v1.59-reference-scenarios.json
  - accrue/lib/accrue/entitlements/reference_scenarios.ex
  - accrue/test/support/entitlements/reference_scenario_executor.ex
  - accrue/test/accrue/entitlements/reference_scenarios_test.exs
  - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "Per D-01/D-02 and PROOF-02, every action in every deterministic_conformance row has one closed kind-specific command payload and one bounded expected transition tuple aligned by scenario ID, order, and kind."
    - "The deterministic executor dispatches apple_verified_purchase, stripe_verified_purchase, web_login, ios_login, purchase_preflight, offline_proof_stale, offline_expansion_request, reconnect_request, verified_cache_replace, grant_observation, refund_observation, stripe_retraction, device_replace, signed_deny, rollback_proof, rotated_key_proof, empty_evidence, equal_order_delivery, repeat_delivery, parallel_delivery, durable_interruption, resume_delivery, and expiry_boundary through their declared production seams."
    - "For every lifecycle/offline/order/replay/race/resume special row, an adversarial generic-grant dispatch and an adversarial no-effect dispatch fail its action-specific expected tuple; neither fallback can keep the conformance suite green."
    - "Reconnect and resume prove durable ReconnectAttempt, Challenge, Issuance, verified proof, and cache-disposition effects; verified_cache_replace and resume_delivery are observable actions rather than bookkeeping steps."
    - "Equal-order, repeat, parallel, and interrupted-resume rows execute their complete fixture-declared deliveries and compare bounded durable state, not a separately constructed projector example."
    - "The shared fixture stays synthetic, credential-free, data-only assertion input; production contexts remain the sole decision authority, Crosswake stays feasibility_blocked, and unrelated production/public-contract behavior is unchanged."
    - statement: "Per D-03/D-04, deterministic server, fixture, and Swift-vector evidence cannot promote Crosswake runtime feasibility without bridge and physical-device evidence."
      verification: backstop
  artifacts:
    - path: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      provides: "One closed command and bounded transition expectation for every deterministic ordered action"
    - path: "accrue/lib/accrue/entitlements/reference_scenarios.ex"
      provides: "Strict discriminated action parser and all-action closure validation without domain reduction"
    - path: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      provides: "Exhaustive production-seam dispatcher returning bounded observed transition tuples"
    - path: "accrue/test/accrue/entitlements/reference_scenarios_test.exs"
      provides: "Schema mutation and generic/no-effect fallback rejection coverage"
    - path: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      provides: "Fixture-driven lifecycle, offline, ordering, replay, race, and resume production proof"
  key_links:
    - from: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      to: "accrue/lib/accrue/entitlements/reference_scenarios.ex"
      via: "kind-discriminated command and expected_transition schemas validated one-for-one for every deterministic action"
      pattern: "command|expected_transition|kind|order"
    - from: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      to: "accrue/lib/accrue/entitlements/offline.ex"
      via: "challenge/register/reconnect/verify/action_policy/key-retention calls selected by exact action kind"
      pattern: "Offline\\.(challenge|register_device|reconnect_challenge|reconnect|verify|action_policy|verification_keys_with_issued_retention)"
    - from: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      to: "accrue/lib/accrue/entitlements/projector.ex"
      via: "observation delivery, lifecycle retraction, equal-order permutation, repeat, and parallel actions invoke insert plus project"
      pattern: "Observation.insert_idempotently|Projector.project"
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      via: "real execution and adversarial generic-grant/no-effect adapters are checked against the same fixture-declared expected tuple"
      pattern: "expected_transition|observed_transition|generic_grant|no_effect"
  assumptions: []
  prohibitions:
    - requirement_id: PROOF-02
      statement: "MUST NOT allow an action to borrow another action's operation, default to a grant observation, return an unasserted success atom, or satisfy only the scenario-level final tuple."
      status: unverified
      flagged: true
    - requirement_id: PROOF-02
      statement: "MUST NOT implement entitlement, lifecycle, purchase, offline, cryptographic, ordering, replay, race, or resume decisions in the fixture loader or executor."
      status: unverified
      flagged: true
    - requirement_id: PROOF-01
      statement: "MUST NOT promote Crosswake runtime feasibility or alter runtime/advisory evidence-lane claims."
      status: unverified
      flagged: true
---

<objective>
Close the remaining PROOF-02 semantic gap by making every deterministic action independently closed, production-dispatched, and regression-sensitive, with explicit negative proof that generic grant and no-effect substitutes fail.

Purpose: Make a green reference-scenario suite certify the advertised lifecycle, offline, ordering, replay, race, and resume transitions rather than merely certifying a common final grant tuple.
Output: A strict per-action fixture contract, an exhaustive production-seam executor, and adversarial conformance tests that eliminate the Plan-09 shortcuts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-RESEARCH.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-09-SUMMARY.md
@accrue/priv/entitlements/v1.59-reference-scenarios.json
@accrue/lib/accrue/entitlements/reference_scenarios.ex
@accrue/test/accrue/entitlements/reference_scenarios_test.exs
@accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
@accrue/test/accrue/entitlements/offline_reconnect_test.exs
@accrue/test/accrue/entitlements/offline_golden_vectors_test.exs
@accrue/test/support/entitlements/offline_golden_vector_verifier.ex

<interfaces>
Production authorities to call directly: `Observation.insert_idempotently/2`, `Projector.project/2`, `Accrue.Entitlements.snapshot/2`, `Accrue.Entitlements.purchase_decision/4`, `Offline.challenge/3`, `Offline.register_device/3`, `Offline.reconnect_challenge/3`, `Offline.reconnect/3`, `Offline.verify/3`, `Offline.action_policy/2`, and `Offline.verification_keys_with_issued_retention/1`.

Durable reconnect evidence: `Accrue.Entitlements.Offline.Challenge`, `ReconnectAttempt`, and `Issuance`; reuse the signed request, synthetic key provider, source coordinator, interruption hooks, and sandbox patterns already exercised by `offline_reconnect_test.exs`.

The existing loader exposes `ReferenceScenarios.deterministic_scenarios/0`; replace its first-operation `execution_input!/1` assumption with per-action typed command access. The fixture remains declarative data and expected observations only.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make reconnect replacement an action-closed, adversarially tested vertical slice</name>
  <reversibility rating="costly">Per D-01/D-02, the action command and expected-transition schema is shared across fixture consumers, so changing it later requires coordinated corpus, loader, test, and generated-contract updates.</reversibility>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/accrue/entitlements/reference_scenarios_test.exs, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <behavior>
    - Test 1: `offline_reconnect` contains two independently closed actions: a reconnect command with account/device/challenge/request/frozen-clock inputs and a replacement command bound to the proof emitted by action 1, each with its own expected seam, result, durable delta, and client cache disposition.
    - Test 2: action 1 calls the authenticated production reconnect flow and records admitted/completed attempt plus one issuance; action 2 verifies the returned compact proof through Offline and applies the declared replacement rule, producing the bounded authenticated cache tuple.
    - Test 3: removing either command or expected transition, moving reconnect fields into replacement, adding an unknown field, using an operation shape belonging to another kind, or mismatching action order/kind is rejected by the loader.
    - Test 4: running either reconnect action through the adversarial generic-grant adapter fails its expected seam/result/durable tuple; running either through the adversarial no-effect adapter also fails.
    - Test 5: every deterministic action, including web_login, ios_login, verified_cache_replace, empty_evidence, and resume_delivery, is required to carry a command and expected transition; no deterministic action is exempt because it previously lacked `operation`.
  </behavior>
  <action>Start red with `:action_contract` tests for the two `offline_reconnect` actions and both adversarial adapters. Replace the optional shared `operation` shape with a closed discriminated `command` union keyed by action kind and add a closed `expected_transition` union keyed by the same scenario ID/order/kind. Require both fields on every deterministic action and reject payload keys belonging to any other kind. Expected transitions may contain only bounded public outcomes and durable counts/states needed for assertion; they may not contain rules that compute those outcomes. Create `ReferenceScenarioExecutor` as a test-support adapter with an explicit clause for each declared deterministic kind and no catch-all success path. For the tracer, build the device challenge and signed request with the established offline test helpers, call `Offline.reconnect/3`, pass its returned proof into the declared replacement action, call `Offline.verify/3`, and assert attempt, issuance, proof decision, and cache disposition. Implement adversarial adapters that deliberately return a qualified-grant observation or no durable effect and compare them through the same tuple matcher used by real execution; both must fail for each tracer action. Preserve D-01's data-only boundary and D-03/D-04's Crosswake status.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/reference_scenario_conformance_test.exs --only action_contract --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - Both reconnect actions have distinct validated commands and expected tuples, and both execute observable production behavior.
    - A generic grant and a no-effect return each fail both reconnect action assertions.
    - All deterministic actions require a command and expected transition; no first-operation selection remains.
    - The loader and executor normalize assertions but do not decide entitlement or offline outcomes.
  </acceptance_criteria>
  <done>The thinnest previously hollow reconnect/replacement path is closed end to end and demonstrably cannot pass through either shortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close every lifecycle, offline, device, key, and boundary action against its production seam</name>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/accrue/entitlements/reference_scenarios_test.exs, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <behavior>
    - Test 1: verified Apple/Stripe purchases, grant/refund/retraction, snapshot reads, purchase preflight, and empty evidence each call their distinct production boundary and match an action-specific result plus durable observation/grant/event delta.
    - Test 2: stale proof, restricted expansion, signed denial, rollback, rotated-key proof, and all three expiry-boundary rows call Offline verification/policy or key-retention APIs using the declared vector, context, and action timestamp, then match state/reason/policy/cache/key tuples.
    - Test 3: device replacement calls registration/device authority with a revoked original and distinct replacement, then asserts both durable device states; it cannot be satisfied by projecting an entitlement grant.
    - Test 4: refund removes the named rail, survivor retraction retains the other rail, and duplicate purchase preflight returns the declared eligibility reason; each action matches its own revision/source/plan/audit tuple before the scenario final tuple is checked.
    - Test 5: for every special action in these rows, both the generic-grant adapter and no-effect adapter fail at least one required field of its expected transition; the test reports the exact scenario ID, order, and kind if a fallback unexpectedly matches.
  </behavior>
  <action>Expand the discriminated command/expectation table and executor across the complete lifecycle/offline/device/key/boundary inventory. Use the actual public contexts and durable tables named in the interfaces block: Apple admission for verified Apple evidence; Observation plus Projector for Stripe/grant/retract delivery; snapshot and purchase-decision reads for login/preflight; Offline verify and action policy for stale, expansion, denial, rollback, rotation, and expiry; challenge plus registration for replacement; verification-key retention for rotation. Bind every action to its own synthetic identifiers, frozen clock, vector/context, and preceding-action output references instead of borrowing a scenario-wide operation. Assert a bounded tuple immediately after every action and then assert the existing scenario-level final snapshot/purchase/offline/audit tuple. Build a table-driven adversarial matrix covering every nontrivial lifecycle/offline/device/key/boundary action so the two fallback adapters are executed against each row's real expectation. Per D-01, reconcile fixture expectations to observed production results; do not modify production decision rules, golden vectors, or repair semantics to fit fixture output.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/projector_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs test/accrue/entitlements/purchase_decision_test.exs --only special_dispatch --include test --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - Every declared lifecycle/offline/device/key/boundary action has an exact payload, an exact production seam, and a bounded per-action tuple.
    - Each special row has two executable negative checks, and neither fallback adapter can satisfy it.
    - Device, issuance, key-retention, observation, grant, event, snapshot, purchase, proof, policy, and cache effects are asserted where applicable.
    - Fixture, loader, and executor remain synthetic/credential-free and do not change production authority or public capability claims.
  </acceptance_criteria>
  <done>All deterministic lifecycle and offline semantics are individually regression-sensitive rather than inheriting a common grant outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drive equal-order, replay, parallel delivery, interruption, and resume from fixture commands</name>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/accrue/entitlements/reference_scenarios_test.exs, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <behavior>
    - Test 1: `equal_order_stability` declares both equal-provider-order observations and both arrival permutations as commands; each executes insert plus project and converges to the same bounded durable tuple.
    - Test 2: `repeat_idempotency` declares the complete delivery twice and proves stable observation identity, one material projection/audit effect, and an unchanged final tuple.
    - Test 3: `parallel_execution` declares both complete deliveries, releases separate sandbox-authorized processes through a bounded barrier, collects every result, and matches observation/grant/event/snapshot state to a sequential control.
    - Test 4: `durable_interruption` calls Offline.reconnect with the declared interruption hook and proves admitted durable state with no issuance; `resume_delivery` replays the same signed request through Offline.reconnect and proves one completed attempt, one issuance, verified proof, and stable replay result.
    - Test 5: each order/replay/race/resume action fails against both adversarial fallback adapters, and the flattened real execution trace exactly equals the fixture inventory of `{scenario_id, order, kind}` without skipped actions.
  </behavior>
  <action>Replace the named tests' independently constructed operations with commands parsed directly from their fixture actions. Equal-order must carry two distinct observations sharing the production ordering fields and two explicit permutation commands. Repeat and parallel must invoke the whole Observation insertion plus Projector projection boundary on every declared delivery. Run the parallel commands in separately authorized database processes with a bounded start barrier, explicit timeouts, and complete exit/result collection, then compare their durable tuple to a sequential fixture-driven control. Model interrupted resume as two separate closed actions sharing one signed reconnect request reference: the first calls `Offline.reconnect/3` with the established `after_admission` interruption hook and asserts durable admitted state; the second calls `Offline.reconnect/3` without the hook, verifies the returned proof, and asserts completed attempt/issuance/cache state plus stable replay. Extend the adversarial matrix and exhaustive trace to these actions. Remove all dispatcher branches that return success without an observed tuple and all action-to-grant fallback selection; an unknown or unimplemented kind must fail loudly with its identity.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/offline_reconnect_test.exs test/accrue/entitlements/projector_test.exs --only race_resume --include test --seed 458442</automated>
  </verify>
  <acceptance_criteria>
    - Ordering, repeat, race, interruption, and resume tests execute only fixture-declared complete commands.
    - Parallel state equals sequential production state; resume proves admitted-before/completed-after with exactly one issuance.
    - Every action appears once in the observed execution trace and returns a bounded tuple.
    - Both adversarial fallbacks fail every order/replay/race/resume expectation.
  </acceptance_criteria>
  <done>All ordering, replay, race, and resume rows fail if their declared production transition is replaced by a grant, a no-effect return, or a separately constructed shortcut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| scenario JSON -> parser/executor | Synthetic commands select production APIs; fixture expectations must never become decision logic. |
| signed proof/device request -> Offline | Device identity, one-time challenges, signatures, clocks, and keys cross authentication and replay boundaries. |
| concurrent/interrupted command -> PostgreSQL | Races and failures must preserve atomic observations, projections, reconnect attempts, issuance, and audit state. |
| deterministic evidence -> capability claim | Server and fixture conformance cannot establish Crosswake bridge or physical-device runtime feasibility. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-G10-01 | Tampering | action schema and dispatcher | high | mitigate | Tasks 1-3 require closed per-kind commands/expectations, exhaustive clauses, exact traces, and adversarial fallback failures. |
| T-220-G10-02 | Tampering | lifecycle/offline outcome evidence | high | mitigate | Task 2 matches per-action production outputs and durable deltas before checking final scenario tuples. |
| T-220-G10-03 | Spoofing | reconnect/device/proof identity | high | mitigate | Tasks 1-3 use production challenges, signatures, device binding, reconnect admission, verification, and key-retention seams. |
| T-220-G10-04 | Tampering | race and interrupted resume | high | mitigate | Task 3 uses whole deliveries, separate database processes, sequential equivalence, durable attempt state, and exact issuance counts. |
| T-220-G10-05 | Information Disclosure | shared corpus and observed tuples | high | mitigate | All tasks retain synthetic identifiers and bounded results; no proof bytes, signing secrets, raw provider evidence, tokens, or PII enter fixture expectations. |
| T-220-G10-06 | Spoofing | Crosswake capability | high | mitigate | All tasks retain lane exclusion and feasibility_blocked until bridge and physical-device evidence exists. |
| T-220-G10-07 | Denial of Service | parallel conformance | medium | mitigate | Task 3 bounds concurrency, barrier waits, process timeouts, and result collection. |
| T-220-G10-SC | Tampering | package-manager installs | low | accept | No dependency installation occurs; existing Elixir/Ecto/JOSE test support is reused. |
</threat_model>

## Multi-source coverage audit - gap-closure plan set

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Reference-host proof makes multi-rail/offline access safe and operable | 220-10 | COVERED | Closes the remaining semantic-proof failure without expanding scope. |
| REQ | PROOF-02 | Credential-free lifecycle/offline/order/replay/race/resume proof | 220-10 | COVERED | Every declared deterministic action is closed, dispatched, asserted, and adversarially differentiated. |
| RESEARCH | - | Shared corpus is input/assertion data, never a second reducer | 220-10 | COVERED | Commands select seams and expectations compare bounded outputs only. |
| RESEARCH | - | PostgreSQL locks/constraints govern races | 220-10 | COVERED | Whole-delivery parallel execution is compared with sequential control. |
| RESEARCH | - | Runtime remains blocked without Crosswake bridge/device evidence | 220-10 | COVERED | Lane exclusions and feasibility backstop remain intact. |
| CONTEXT | D-01 | One versioned data-only corpus; production is sole authority | 220-10 | COVERED | Closed command/expect tuples are data, while production contexts compute results. |
| CONTEXT | D-02 | Ordered actions, clocks, bounded expectations, and closed lanes | 220-10 | COVERED | Every action requires aligned command and expected transition fields. |
| CONTEXT | D-03 | No unsupported Crosswake runtime claim | 220-10 | COVERED | Runtime promotion remains prohibited. |
| CONTEXT | D-04 | ExUnit/Ecto owns semantic proof; Swift remains bounded | 220-10 | COVERED | Production semantic checks remain in ExUnit/Ecto without browser or simulator substitution. |
| CONTEXT | D-05 | One bounded diagnostic seam | existing 220-02 | COVERED | No diagnostic surface changes. |
| CONTEXT | D-06 | No raw evidence, proofs, tokens, or PII | 220-10 | COVERED | Fixture and observed tuples remain bounded and synthetic. |
| CONTEXT | D-07 | Repairs remain distinct, bounded, and provider-honest | existing 220-03; 220-10 | COVERED | The plan uses lifecycle/device/offline authorities and does not turn repair guidance into mutation. |
| CONTEXT | D-08 | Real repair/reconnect drills and database authority | 220-10 | COVERED | Reconnect, interruption, issuance, device, key, and database race seams are exercised. |
| CONTEXT | D-09 | Generated facts and hand-authored procedure stay separated | existing 220-04/05/08 | COVERED | No public generator or prose ownership changes. |
| CONTEXT | D-10 | Additive privacy/lifecycle/finance/offline limits | 220-10 | COVERED | Production and public boundaries remain unchanged. |
| CONTEXT | D-11 | Compact adopter path and visible evidence lanes | existing 220-05/08; 220-10 | COVERED | Deterministic lane becomes truthful without changing adopter workflow. |
| CONTEXT | D-12 | Job-focused accessible operator presentation | existing 220-02/03/05 | COVERED | No UI or copy changes. |
| CONTEXT | Deferred | Google Play, Family Sharing, offers, ownership transfer, migration/proration, broad risk controls, unsupported runtime claims | NONE | EXCLUDED | Explicitly outside Phase 220. |

<verification>
Run `cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_conformance_test.exs test/accrue/entitlements/reference_scenarios_test.exs test/accrue/entitlements/projector_test.exs test/accrue/entitlements/offline_reconnect_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs test/accrue/entitlements/purchase_decision_test.exs --seed 458442`, then `mix accrue.entitlements.reference_scenarios --check`, then from the repository root run `bash scripts/ci/verify_reference_scenario_contract.sh &amp;&amp; bash scripts/ci/verify_adoption_proof_matrix.sh &amp;&amp; bash scripts/ci/verify_release_contract.sh`. Confirm the real execution trace equals the deterministic action inventory, both adversarial fallback matrices report zero satisfiable special rows, and capability status remains feasibility_blocked.
</verification>

<success_criteria>
- The verifier's sole remaining PROOF-02 gap is closed with per-action production evidence for lifecycle, offline, ordering, replay, race, and resume semantics.
- Every deterministic action owns a strict payload and bounded expected transition; none borrows the first operation or relies only on a scenario-level final tuple.
- A generic qualified grant and a no-effect return are executable negative controls that fail every special action expectation.
- `verified_cache_replace` and `resume_delivery` invoke and assert real verification/reconnect/durable-state behavior.
- The data-only corpus boundary, Crosswake feasibility boundary, existing production authorities, public contracts, and unrelated user changes remain intact.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-10-SUMMARY.md` when done.
</output>

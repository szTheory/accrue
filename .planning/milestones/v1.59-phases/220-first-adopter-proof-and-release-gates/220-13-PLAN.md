---
phase: 220-first-adopter-proof-and-release-gates
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/priv/entitlements/v1.59-reference-scenarios.json
  - accrue/lib/accrue/entitlements/reference_scenarios.ex
  - accrue/test/support/entitlements/reference_scenario_executor.ex
  - accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex
  - accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "Per D-01/D-02, all 27 deterministic actions are closed tagged commands and expectations; the corpus contains references and oracles only, never entitlement rules."
    - "Every deterministic action kind maps exactly once to a named family executor and there is no catch-all, generic-grant, snapshot-only, registration-only, or fixture-expected observation path."
    - "Apple/Stripe purchase, grant, refund, and retraction actions call Observation/Projector or the Apple admission facade and collect bounded returned plus persisted facts after each call."
    - "Refund and retraction cannot pass when routed through a grant or no-effect adapter, even if unrelated snapshot fields remain correct."
    - "D-03/D-04 remain intact: deterministic server evidence cannot promote Crosswake beyond feasibility_blocked."
  artifacts:
    - path: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      provides: "Closed command payload and bounded expected transition for all 27 deterministic actions"
    - path: "accrue/lib/accrue/entitlements/reference_scenarios.ex"
      provides: "Strict discriminated action decoder and exact family inventory"
    - path: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      provides: "Exhaustive exact-kind router and shared observed-outcome contract"
    - path: "accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex"
      provides: "Production purchase/lifecycle/refund/retraction execution and collection"
    - path: "accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs"
      provides: "Lifecycle conformance and adversarial adapter proof"
  key_links:
    - from: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      to: "accrue/lib/accrue/entitlements/reference_scenarios.ex"
      via: "action_contract 1 exact kind/payload/expected-transition decoding"
      pattern: "action_contract|expected_transition"
    - from: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      to: "accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex"
      via: "closed action-kind to family-module dispatch table"
      pattern: "apple_verified_purchase|refund_observation|stripe_retraction"
    - from: "accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex"
      to: "Accrue.Entitlements.Observation + Projector"
      via: "real insert/admission/project calls followed by fresh bounded queries"
      pattern: "insert_idempotently|observe_apple_evidence|Projector.project"
---

<objective>
Replace the hollow generic action interior with one closed action contract and prove the purchase/lifecycle/refund/retraction tracer through production authority.

Purpose: Close the root cause identified by 220-VERIFICATION.md: fixture expectations currently choose and fabricate outcomes instead of merely describing commands and checking observed results.
Output: A strict 27-action schema, exhaustive family router, and real lifecycle-family executor with adversarial failure evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-12-SUMMARY.md
@accrue/priv/entitlements/v1.59-reference-scenarios.json
@accrue/lib/accrue/entitlements/reference_scenarios.ex
@accrue/test/support/entitlements/reference_scenario_executor.ex
@accrue/lib/accrue/entitlements/observation.ex
@accrue/lib/accrue/entitlements/projector.ex
@accrue/lib/accrue/entitlements.ex

<interfaces>
Keep corpus version `v1.59`, action_contract `1`, scenario IDs, evidence lanes, frozen clocks, and scenario-level expected tuples stable. Replace the universal payload interior with exact family variants for these complete kind sets: lifecycle `{apple_verified_purchase, stripe_verified_purchase, grant_observation, refund_observation, stripe_retraction}`; read `{web_login, ios_login, purchase_preflight, expiry_boundary}`; offline `{offline_proof_stale, offline_expansion_request, signed_deny, rollback_proof, empty_evidence}`; reconnect `{reconnect_request, verified_cache_replace}`; device/key `{device_replace, rotated_key_proof}`; ordering `{equal_order_delivery, repeat_delivery, parallel_delivery}`; resume `{durable_interruption, resume_delivery}`.

`ReferenceScenarioExecutor.execute_action/3` returns only `%{result: tagged_bounded_map, durable: bounded_map, cache: bounded_map}` collected after production execution. It owns an explicit kind-to-family table and rejects an unknown kind. Family modules expose `execute(repo, account, action)` and may resolve synthetic references into test setup; they may not read `expected_transition` until assertion time.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cut over the shared contract and execute one refund path end to end</name>
  <reversibility rating="costly">D-01 coordinates the corpus shape across core, host, Swift, generated material, and CI, so changing it later requires a coordinated consumer migration.</reversibility>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/lib/accrue/entitlements/reference_scenarios.ex, accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex, accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs</files>
  <behavior>
    - "All 27 actions decode only through their named family, with exact required keys, types, closed values, non-null rules, kind equality, order checks, and duplicate-key rejection."
    - "The refund_revocation action list executes grant then refund through Observation.insert_idempotently/2 and Projector.project/2, then observes the retracted source, empty plan set, exact revision/audit delta, and cache disposition from durable state."
    - "No executor clause can return fields copied from expected_transition."
  </behavior>
  <action>Write the failing lifecycle test first. Migrate the corpus interiors for every deterministic row to closed family payloads and expected result/durable/cache variants while preserving the data-only boundary per D-01/D-02. In ReferenceScenarios, parse exact per-kind shapes and reject unknown, extra, secret-bearing, nullable-required, or cross-family fields. In ReferenceScenarioExecutor, replace generic observation/snapshot branches with the explicit family table and a matcher that compares a separately collected observation to the expectation. Implement the refund tracer in Lifecycle: resolve bounded fixture references into synthetic setup, call Observation.insert_idempotently/2 and Projector.project/2, then query Snapshot, Observation, Grant, and audit counts after the call. Never use expectation fields to choose the production operation or populate observed fields.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_lifecycle_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The refund tracer is red before implementation, green through real lifecycle writes afterward, and the full 27-action corpus has a strict discriminated contract with an exhaustive family route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete purchase, survivor, and retraction collection with adversarial substitutes</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex, accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs</files>
  <behavior>
    - "Apple purchase uses observe_apple_evidence/3; Stripe/grant/retraction rows use Observation plus Projector; all returned tuples are corroborated by fresh bounded persistence reads."
    - "Survivor retraction proves the remaining rail still grants access and the account revision follows production no-op/material-change semantics."
    - "Generic-grant and no-effect adapters fail refund/retraction expectations after actual execution, including when snapshot plans happen to match."
  </behavior>
  <action>Expand Lifecycle over every lifecycle action instance: both cross-rail purchase scenarios, the refund pair, and all three survivor-grant actions. Collect exact result disposition/reason, observation kind/count, source set, plan set, revision, and audit delta from returned results and fresh Repo queries. Add adapters that really execute an unrelated grant and a real no-material-change replay, collect them through the same collector, and demonstrate that each mismatches the intended refund/retraction tuple. Keep provider evidence synthetic and bounded, and do not change Projector or Apple production rules to fit fixture expectations.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_lifecycle_test.exs test/accrue/entitlements/projector_test.exs test/accrue/entitlements/apple_observation_tracer_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>All nine purchase/lifecycle/refund/retraction action instances execute and collect real bounded outcomes, and both actual generic-grant and actual no-effect substitutions fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Corpus JSON → strict decoder | Checked-in data may select bounded setup but must not smuggle secrets, unknown fields, or reducer logic. |
| Family command → production context | The executor selects one named API; production code alone decides lifecycle state. |
| Persistence → bounded collector | Raw provider evidence and PII must not enter observed tuples or assertion output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-13-01 | Tampering | corpus/decoder | high | mitigate | Exact family schemas, duplicate-key rejection, kind equality, and secret-field rejection. |
| T-220-13-02 | Spoofing | executor routing | high | mitigate | Exhaustive exact-kind table and actual generic/no-op adversarial executions. |
| T-220-13-03 | Information Disclosure | collectors | high | mitigate | Return only closed tags, counts, revisions, states, and bounded correlations. |
| T-220-13-SC | Tampering | package installs | high | mitigate | No dependency or lockfile change is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Safe deterministic multi-rail/offline adopter proof | 220-13..220-20 | COVERED | Establishes the shared contract and first real family.
REQ | PROOF-02 | Credential-free lifecycle/offline/order/resume proof | 220-13 | COVERED | Purchase, refund, retraction, and survivor lifecycle rows.
RESEARCH | — | Production-only authority with data-only fixtures | 220-13 | COVERED | Executor routes; production contexts decide; collector observes.
CONTEXT | D-01 | Shared synthetic data-only corpus | 220-13 | COVERED | Exact contract without reducer logic.
CONTEXT | D-02 | Closed ordered actions and evidence lanes | 220-13 | COVERED | All 27 rows receive strict tagged commands/expectations.
CONTEXT | D-03 | Crosswake claim stays blocked | 220-13 | COVERED | No runtime capability promotion.
CONTEXT | D-04 | Focused semantic oracle | 220-13 | COVERED | ExUnit/Ecto production seam, not browser substitution.
CONTEXT | D-05..D-12 | Existing diagnostic, repair, public, and UI contracts | 220-20 | COVERED | Preserved and re-certified by final aggregate plan.
</source_audit>

<verification>
The family test enumerates all nine lifecycle action instances, proves returned and persisted facts independently of fixture expectations, and demonstrates actual wrong-seam failures. The parser test inventory proves all 27 deterministic rows are closed and exactly routed.
</verification>

<success_criteria>
- No generic/catch-all/copy-from-expectation executor path remains.
- All lifecycle actions run through production authority and exact bounded collection.
- Corpus identifiers, lanes, data-only authority, and Crosswake boundary remain stable.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-13-SUMMARY.md` when done.
</output>

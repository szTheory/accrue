---
phase: 220-first-adopter-proof-and-release-gates
plan: 18
type: execute
wave: 2
depends_on: [220-13]
files_modified:
  - accrue/test/support/entitlements/reference_scenario_executor/ordering.ex
  - accrue/test/accrue/entitlements/reference_scenario_ordering_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "equal_order_delivery consumes the fixture's complete delivery list in every declared permutation and converges to the same production winner and snapshot."
    - "repeat_delivery executes the declared delivery count and observes one durable identity owner plus idempotent/no-op results."
    - "parallel_delivery executes the declared payloads concurrently behind a real start barrier and observes bounded results plus exact row/revision/audit counts."
    - "Ordering assertions are driven only by complete fixture actions; no test fabricates replacement provider IDs, permutations, or concurrency inputs locally."
    - "A sequential generic-grant adapter and a real no-effect replay fail ordering-specific expectations."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor/ordering.ex"
      provides: "Production permutation, replay, and barrier-controlled parallel execution/collection"
    - path: "accrue/test/accrue/entitlements/reference_scenario_ordering_test.exs"
      provides: "Three-action ordering/replay/race conformance and adversarial proof"
  key_links:
    - from: "ordering command deliveries"
      to: "Observation.insert_idempotently/2 + Projector.project/2"
      via: "complete declared payloads, permutations/repeat count, and SQL-sandbox barrier"
      pattern: "insert_idempotently|Projector.project|Task.async"
---

<objective>
Drive equal-order, replay, and parallel delivery exclusively from complete fixture actions through production idempotency and projection authority.

Purpose: Replace locally constructed examples with deterministic commands whose delivery schedule and outcomes are both closed and independently observed.
Output: An ordering family executor and focused three-action concurrency suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-13-SUMMARY.md
@accrue/lib/accrue/entitlements/observation.ex
@accrue/lib/accrue/entitlements/projector.ex
@accrue/test/accrue/entitlements/projector_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute declared equal-order permutations and repeated delivery</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/ordering.ex, accrue/test/accrue/entitlements/reference_scenario_ordering_test.exs</files>
  <behavior>
    - "Each equal-order permutation uses the complete command delivery records and reaches the same source/snapshot/revision tuple."
    - "Repeat count is command data; one insert owns the identity and later exact deliveries return bounded idempotent/no-op results without extra rows."
  </behavior>
  <action>Consume delivery arrays, declared permutation indexes, and repeat count from the strict command. For each isolated account, feed every complete delivery into Observation.insert_idempotently/2 then Projector.project/2 without rewriting IDs or kinds. Collect per-delivery result tags/reasons and final Observation/Grant/Snapshot/Event counts. Assert all declared permutations converge and repeat creates only the expected durable identity. Remove no hand-built examples yet; Plan 20 removes them when aggregate conformance is migrated.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_ordering_test.exs test/accrue/entitlements/projector_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>Equal-order and repeat actions are completely fixture-driven and observed through production idempotency/projection results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute declared parallel delivery behind a real barrier</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/ordering.ex, accrue/test/accrue/entitlements/reference_scenario_ordering_test.exs</files>
  <behavior>
    - "All declared workers signal ready before one release signal permits database execution."
    - "The result multiset contains only the declared production result tags/reasons and final row/revision/audit counts are exact."
    - "Sequential generic grant and no-effect replay adapters fail the parallel expectation."
  </behavior>
  <action>Use Ecto SQL Sandbox unboxed concurrency and a test-process barrier driven by the command's worker/delivery entries. Do not rely on Task scheduling as the race. After all workers finish, sort only order-insensitive result collections, query exact observations/grants/account revision/audits, and compare with the expected bounded tuple. Execute a sequential generic-grant adapter and a stale/no-material-change replay adapter through the same collector and show neither satisfies the concurrency/result-multiset expectation.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_ordering_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>Parallel execution is barrier-controlled, production-backed, exact-counted, and cannot be mimicked by sequential generic/no-op paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Delivery schedule → database writers | Concurrent untrusted deliveries cross idempotency and row-lock boundaries. |
| Concurrent results → collector | Nondeterministic completion order must not hide duplicate durable effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-18-01 | Tampering | delivery arrays/permutations | high | mitigate | Strict complete delivery schema; no local ID/kind rewriting. |
| T-220-18-02 | Denial of Service | parallel workers | medium | mitigate | Bounded worker count, explicit barrier, and test timeout. |
| T-220-18-03 | Repudiation | result collection | medium | mitigate | Exact result multiset plus fresh row/revision/audit counts. |
| T-220-18-04 | Spoofing | sequential substitute | high | mitigate | Execute wrong adapters and require parallel-specific evidence. |
| T-220-18-SC | Tampering | package installs | high | mitigate | No dependency installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Deterministic ordering/race proof | 220-18 | COVERED | Complete deliveries and real barrier.
REQ | PROOF-02 | Ordering, replay, parallel | 220-18 | COVERED | Three declared actions.
RESEARCH | — | PostgreSQL locks/constraints are authority | 220-18 | COVERED | Real concurrent Ecto execution.
CONTEXT | D-01/D-02/D-04 | Data-only, closed, focused semantic proof | 220-18 | COVERED | No hand-built delivery examples.
CONTEXT | D-03/D-05..D-12 | Existing boundaries | 220-20 | COVERED | Final aggregate certification.
</source_audit>

<verification>
The focused suite consumes only fixture delivery arrays and schedules, executes every permutation/repeat/worker, and proves exact final durable effects plus actual wrong-adapter failure.
</verification>

<success_criteria>
- Ordering/replay/parallel commands are complete and fixture-driven.
- Production idempotency and database concurrency decide outcomes.
- Exact bounded results and durable counts replace locally fabricated examples.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-18-SUMMARY.md` when done.
</output>

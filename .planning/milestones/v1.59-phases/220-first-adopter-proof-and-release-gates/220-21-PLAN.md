---
phase: 220-first-adopter-proof-and-release-gates
plan: 21
type: execute
wave: 5
depends_on: [220-20]
files_modified:
  - accrue/priv/entitlements/v1.59-reference-scenarios.json
  - accrue/test/support/entitlements/reference_scenario_executor.ex
  - accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex
  - accrue/test/support/entitlements/reference_scenario_executor/read.ex
  - accrue/test/support/entitlements/reference_scenario_executor/offline_policy.ex
  - accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs
  - accrue/test/accrue/entitlements/reference_scenario_read_test.exs
  - accrue/test/accrue/entitlements/reference_scenario_offline_policy_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "D-01: The shared corpus remains data-only while lifecycle, read, and offline assertions compare each declaration with separately collected production facts."
    - "D-02: Every declared expected_transition result, durable, and cache leaf is enforced for these families."
    - "Refund and source retraction declare cache replacement because a material account/source transition invalidates the prior revision-bound offline cache tuple; production remains authoritative and the fixture is corrected to that contract."
    - "Changing any lifecycle, read, or offline declared expectation while keeping the collected production outcome fixed causes the family assertion to fail."
    - "D-03: Credential-free server/fixture conformance does not promote Crosswake runtime feasibility."
    - "D-04: Focused ExUnit/Ecto family tests remain the semantic oracle; browser coverage is not substituted."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      provides: "Expectation-aware family dispatch that requires exact declared-transition equality"
    - path: "accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex"
      provides: "Production-derived lifecycle transition projection including actual cache disposition"
    - path: "accrue/test/support/entitlements/reference_scenario_executor/read.ex"
      provides: "Production-derived read/preflight/expiry transition projection"
    - path: "accrue/test/support/entitlements/reference_scenario_executor/offline_policy.ex"
      provides: "Production-derived signed-offline transition projection"
    - path: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      provides: "Correct refund/retraction cache-replacement oracle"
  key_links:
    - from: "accrue/priv/entitlements/v1.59-reference-scenarios.json"
      to: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      via: "expected_transition is passed only to assertion after production execution"
      pattern: "expected_transition|assert_transition"
    - from: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      to: "accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex"
      via: "family-specific production observation is normalized into the closed declared transition"
      pattern: "Lifecycle|declared_transition|matches_expected"
---

<objective>
Close the concrete refund contradiction and make lifecycle, read, and offline families enforce their complete fixture oracle per D-01 through D-04.

Purpose: Replace kind-only and hard-coded matcher success with exact comparisons against independently collected production facts for the first three action families.
Output: Corrected cache expectations, production-derived declared-transition projections, and focused exact-comparison tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-13-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-14-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-15-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-20-SUMMARY.md

<interfaces>
`ReferenceScenarios.ExpectedTransition` has exactly `kind`, `seam`, `result`, `durable`, and `cache`. For every non-device action the declared maps contain result `{tag, disposition}`, durable `{state, observation_kind, snapshot_revision}`, and cache `{disposition}`. `ReferenceScenarioExecutor.assert_transition/2` receives the expected transition only after `execute_action/3` has returned its independently collected observation.

Preserve the richer family evidence (verification state/reason, policy, counts, expiry, decision, projection, audit, and source facts). Add a separate closed declared-transition projection instead of deleting those facts. The projection must be derived from the returned production result and fresh Repo/cache observations; it must not copy `expected_transition` or declare success solely from action kind.
</interfaces>
</context>

## Artifacts this phase produces

- Exact fixture-to-production comparison for lifecycle, read, and signed-offline action families.
- A corrected refund/retraction cache oracle grounded in revision-bound offline cache invalidation.
- Focused regression tests that fail when any declared field in these families disagrees.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the contradictory refund row fail, then align it with the production lifecycle contract</name>
  <reversibility rating="costly">Per D-01, changing a versioned expectation affects the shared Elixir, host, Swift, generated-material, and CI contract, though no runtime or persistence migration is involved.</reversibility>
  <files>accrue/priv/entitlements/v1.59-reference-scenarios.json, accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex, accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs</files>
  <read_first>
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md (remaining PROOF-02 gap and preserve-versus-replace evidence)
    - accrue/priv/entitlements/v1.59-reference-scenarios.json (`refund_revocation` and `survivor_grant` transitions)
    - accrue/test/support/entitlements/reference_scenario_executor.ex (`assert_transition/2` and `lifecycle_match?/3`)
    - accrue/test/support/entitlements/reference_scenario_executor/lifecycle.ex (`collect/6` and `cache_disposition/2`)
    - accrue/lib/accrue/entitlements/reference_scenarios.ex (`transition!/2` exact result/durable/cache schema)
    - accrue/test/accrue/entitlements/reference_scenario_lifecycle_test.exs (production lifecycle and adversarial coverage)
  </read_first>
  <behavior>
    - "Before fixture correction, the real refund observation returns cache replace and exact assertion rejects the declared preserve value."
    - "A refund that retracts the final source and a Stripe retraction that changes the survivor source set both require replacement of the prior revision-bound cache tuple."
    - "Lifecycle declared result, durable, and cache maps equal the actual normalized production transition field-for-field."
    - "Mutating result.tag, result.disposition, durable.state, durable.observation_kind, durable.snapshot_revision, or cache.disposition independently makes lifecycle assertion fail."
  </behavior>
  <action>Start RED by asserting the current refund row fails because the lifecycle collector reports the production cache disposition `replace` while the fixture declares `preserve`. Replace `lifecycle_match?/3` with an expectation-aware lifecycle comparison: Lifecycle must expose a closed declared transition derived from the actual Projector/Apple outcome, freshly queried observation kind and snapshot revision, and the cache action required by the material projection. Compare every expected result/durable/cache leaf exactly; retain richer projection/count/audit fields as corroborating evidence. Per D-01, correct the fixture cache disposition to `replace` for both `refund_observation` and `stripe_retraction`, because each changes the revision/source-bound entitlement proof even when a survivor preserves logical access. Do not change Projector, Offline, or lifecycle production behavior to accommodate fixture data, and do not read expectation fields during execution or collection.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_lifecycle_test.exs test/accrue/entitlements/reference_scenario_conformance_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - The pre-correction preserve expectation is demonstrated to fail against the real collected refund transition.
    - Both lifecycle retraction expectations declare cache replacement and all lifecycle rows pass exact result/durable/cache comparison.
    - Each of the six non-device declared leaf fields has a focused mutation failure.
    - Execution and collection code remain free of `expected_transition` reads.
  </acceptance_criteria>
  <done>The known contradiction is resolved in favor of the real revision-bound cache contract, and lifecycle conformance enforces every declared field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce complete read and signed-offline transition expectations</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/support/entitlements/reference_scenario_executor/read.ex, accrue/test/support/entitlements/reference_scenario_executor/offline_policy.ex, accrue/test/accrue/entitlements/reference_scenario_read_test.exs, accrue/test/accrue/entitlements/reference_scenario_offline_policy_test.exs</files>
  <read_first>
    - accrue/test/support/entitlements/reference_scenario_executor.ex (Task 1 exact comparison contract)
    - accrue/test/support/entitlements/reference_scenario_executor/read.ex (login, preflight, expiry returned and persisted facts)
    - accrue/test/support/entitlements/reference_scenario_executor/offline_policy.ex (signed verification, policy, durable, and cache facts)
    - accrue/test/accrue/entitlements/reference_scenario_read_test.exs (seven production-backed read instances)
    - accrue/test/accrue/entitlements/reference_scenario_offline_policy_test.exs (five signed offline instances)
  </read_first>
  <behavior>
    - "Login, preflight, and expiry assertions compare every declared result/durable/cache leaf to a projection backed by the production read, frozen snapshot revision, zero-write facts, and unchanged cache."
    - "Stale, restricted-expansion, deny, rollback, and empty-evidence assertions compare every declared leaf after signed Offline.verify/3 and Offline.action_policy/2, including the actual account revision and cache preservation."
    - "A same-kind observation with one wrong declared leaf fails even when the existing richer family semantic checks still pass."
  </behavior>
  <action>Extend Read and OfflinePolicy with the same production-derived declared-transition contract established by Task 1. For Read, derive unchanged/none/preserve from the measured zero write deltas and use the actual snapshot or PurchaseDecision revision. For OfflinePolicy, fetch the actual account snapshot revision after verification, retain the signed verification state/reason and policy evidence, and derive unchanged/none/preserve only after the measured write delta remains zero. Update `assert_transition/2` to pass the expected object into these family matchers and require both their richer semantic proof and exact declared result/durable/cache equality. Add type-compatible single-leaf mutations for every declared field across representative login, preflight, expiry, stale, expansion, denial, rollback, and empty-evidence actions. Never hard-code expected values inside matchers or obtain them from the action expectation.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_read_test.exs test/accrue/entitlements/reference_scenario_offline_policy_test.exs test/accrue/entitlements/reference_scenario_conformance_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - Every lifecycle/read/offline action compares all declared result, durable, and cache leaves to collected facts.
    - Each declared field has a mutation that fails without changing the collected production outcome.
    - Richer semantic checks still distinguish preflight, expiry, signed denial, rollback, and stale-policy behavior.
    - No production runtime module, database schema, external API, generated public document, or Crosswake capability status changes.
  </acceptance_criteria>
  <done>Lifecycle, read, and offline families enforce their complete declared oracle and retain their production-specific semantic evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture expectation → assertion | Checked-in oracle data is untrusted input to comparison and cannot populate observations. |
| Production contexts/Repo → declared transition | Only bounded returned and freshly queried facts may become assertion evidence. |
| Cache contract → public conformance | A stale fixture value must not mask revision-bound cache invalidation. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-21-01 | Tampering | expectation matcher | high | mitigate | Tasks 1-2 require exact leaf equality and prohibit expectation reads during execution/collection. |
| T-220-21-02 | Spoofing | kind-only success | high | mitigate | Family semantic proof plus production-derived declared transition must both pass. |
| T-220-21-03 | Information Disclosure | failure output | medium | mitigate | Comparisons expose only closed result/durable/cache maps and bounded mismatch paths. |
| T-220-21-04 | Repudiation | refund cache contract | high | mitigate | Focused RED evidence and fixture correction record why material retractions replace revision-bound cache state. |
| T-220-21-SC | Tampering | package installs | high | mitigate | No package installation or lockfile change is planned. |
</threat_model>

## Planner contribution declarations

- **API coverage:** Non-integration declaration — no external API or provider call is added; all work exercises existing in-repository production seams, so no `COVERAGE.md` is created or changed.
- **Assumption delta:** The detector fires only on the stale refund/retraction cache assumption. Resolve it per D-01 by recording `replace` as the production-backed cache contract; no new environmental assumption is introduced.
- **Schema gate:** No database, public API, or action-contract schema change. The JSON edit changes existing closed enum values only; strict loader and reference-scenario tests must remain green.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Safe deterministic multi-rail/offline proof | 220-21..220-22 | COVERED | Exact production-oracle enforcement closes the final blocker.
REQ | PROOF-02 | Credential-free lifecycle/offline proof | 220-21 | COVERED | Lifecycle, read, expiry, preflight, and signed-offline families.
RESEARCH | — | Fixtures are assertions; production contexts are sole authority | 220-21 | COVERED | Observations are collected before expectations enter comparison.
CONTEXT | D-01 | Shared corpus never becomes a reducer | 220-21 | COVERED | Stale fixture corrected from production evidence.
CONTEXT | D-02 | Complete closed expected result/durable/cache contract | 220-21 | COVERED | Every declared leaf is compared.
CONTEXT | D-03 | No Crosswake runtime claim inflation | 220-21 | COVERED | Capability status is unchanged.
CONTEXT | D-04 | Focused ExUnit/Ecto semantic oracle | 220-21 | COVERED | Family tests remain the proof path.
CONTEXT | D-05..D-12 | Existing operator/public contract | 220-22 | COVERED | Final coordinated regression gate reruns shipped coverage.
</source_audit>

<verification>
Run both task commands. Confirm lifecycle/read/offline execution and collector modules contain no `expected_transition` access, the two retraction fixture cache values are `replace`, and focused tests demonstrate failure for every declared result/durable/cache leaf.
</verification>

<success_criteria>
- The known refund preserve-versus-replace contradiction can no longer pass.
- Lifecycle, read, and offline families compare every declared expected field with production-derived facts.
- No fixture-derived observation, public/runtime behavior change, external integration, or schema drift is introduced.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-21-SUMMARY.md` when done.
</output>

---
phase: 220-first-adopter-proof-and-release-gates
plan: 22
type: execute
wave: 6
depends_on: [220-21]
files_modified:
  - accrue/test/support/entitlements/reference_scenario_executor.ex
  - accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex
  - accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex
  - accrue/test/support/entitlements/reference_scenario_executor/ordering.ex
  - accrue/test/support/entitlements/reference_scenario_executor/resume.ex
  - accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs
  - accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs
  - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02, PROOF-03, PROOF-04, PROOF-05]
must_haves:
  truths:
    - "D-01: Expected data is used only after production execution and never becomes a reducer."
    - "D-02: Every declared result, durable, and cache leaf across all 27 deterministic actions is enforced."
    - "D-03: Crosswake remains feasibility_blocked absent bridge and physical-device evidence."
    - "D-04: Focused ExUnit/Ecto and pure Swift consumers keep their established semantic roles."
    - "D-05: The final coordinated gate retains the shipped privacy-bounded diagnostic projection."
    - "D-06: Failure evidence and diagnostics retain only closed bounded fields and exclude raw/private material."
    - "D-07: Existing distinct host-authorized, lock-safe repair actions remain green and unchanged."
    - "D-08: Existing deterministic repair drills remain part of the coordinated closeout gate."
    - "D-09: Generated exact facts and hand-authored procedural material retain their existing ownership split."
    - "D-10: Public additive, privacy, lifecycle, finance, and offline-expansion limits remain green."
    - "D-11: The existing single credential-free verification command remains the closeout entrypoint."
    - "D-12: Existing accessible job-and-next-action host outcomes remain green; this gap adds no UI authority."
    - "Reconnect/cache, device/key, ordering, and interruption/resume families compare every declared expected_transition result, durable, and cache leaf with independently collected production facts."
    - "For each of all 27 deterministic actions, mutating any declared expected result/durable/cache leaf while reusing the unchanged production observation makes aggregate conformance fail."
    - "The coordinated credential-free verifier proves exact oracle enforcement and keeps host convergence, diagnostics, repairs, Swift vectors, generated contracts, and public release limits green."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      provides: "Complete seven-family exact expected-transition enforcement"
    - path: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      provides: "All-action, all-declared-leaf mutation coverage over unchanged production observations"
  key_links:
    - from: "accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs"
      to: "accrue/test/support/entitlements/reference_scenario_executor.ex"
      via: "execute once, mutate each expected leaf, and require assert_transition/2 failure"
      pattern: "expected_transition|assert_transition|mutation"
    - from: "scripts/ci/verify_reference_scenario_contract.sh"
      to: "reference_scenario_conformance_test.exs"
      via: "merge-blocking focused and aggregate ExUnit invocation"
      pattern: "reference_scenario_conformance_test"
---

<objective>
Complete exact expectation comparison for the remaining production families and prove that every declared oracle field across all 27 actions is load-bearing.

Purpose: Turn the shared expected_transition contract into a complete, mutation-proven merge gate while preserving all already-verified Phase-220 behavior.
Output: Remaining family projections, exhaustive leaf mutation coverage, and final credential-free release evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-16-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-17-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-18-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-19-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-20-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-21-SUMMARY.md

<interfaces>
Use Plan 220-21's production-derived `declared_transition` convention. Device replacement retains its larger exact result/durable/cache schema; every other action retains the common six-leaf schema. Family-specific semantic evidence remains additive and must pass alongside exact declared-transition equality.

Mutation coverage operates on the already-decoded in-memory action after one real execution. It recursively enumerates every scalar leaf under `expected_transition.result`, `.durable`, and `.cache`, substitutes a type-compatible unequal value, calls `ReferenceScenarioExecutor.assert_transition/2` with the unchanged observation, and requires an `ExUnit.AssertionError` naming scenario ID, action order, kind, and leaf path. This bypasses loader rejection intentionally so it proves the matcher rather than only schema validation.
</interfaces>
</context>

## Artifacts this phase produces

- Exact production-to-oracle comparison for reconnect/cache, device/key, ordering, and resume families.
- Exhaustive mutation evidence for every declared result, durable, and cache leaf in every deterministic action.
- A coordinated credential-free gate that preserves all five Phase-220 requirement surfaces.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace reconnect replacement and device replacement through exact declared transitions</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex, accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex, accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs, accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs</files>
  <read_first>
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-21-SUMMARY.md (exact comparison convention and corrected cache contract)
    - accrue/test/support/entitlements/reference_scenario_executor.ex (remaining kind-only reconnect/device matchers)
    - accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex (challenge, attempt, issuance, snapshot, and cache facts)
    - accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex (replacement and retention facts)
    - accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs (signed reconnect/cache chain)
    - accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs (replacement and issued-key retention)
  </read_first>
  <behavior>
    - "Reconnect request and verified cache replacement compare their declared result/durable/cache leaves after signed production reconnect and verified complete-tuple adoption."
    - "Device replacement compares every field of its extended result/durable/cache contract to freshly queried device, challenge, event, snapshot, and cache facts."
    - "Rotated-key proof compares the common declared transition while retaining issuance-backed current/retired key evidence."
    - "A one-leaf mutation in any of these actions fails even when the richer family matcher still recognizes the production operation."
  </behavior>
  <action>Apply Plan 21's declared-transition contract to ReconnectCache and DeviceKeys. For reconnect, derive unchanged state, no observation, actual snapshot revision, and replacement disposition from the signed reconnect attempt/issuance plus verification-first cache outcome. For device replacement, expose the complete extended declared result/durable/cache maps directly from freshly queried prior/replacement devices, consumed challenge, immutable audit, snapshot, and server/cache policy; compare every field exactly. For rotated keys, derive the common transition only after production issuance-retention evidence proves the old and current public kids. Update central dispatch to pass the expected object and require richer family semantics plus exact equality. Add focused one-leaf mutations across all declared fields for these action kinds; never use fixture expectations to build observations.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_reconnect_test.exs test/accrue/entitlements/reference_scenario_device_keys_test.exs test/accrue/entitlements/reference_scenario_conformance_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - Reconnect/cache and device/key families compare every declared leaf with production-derived facts.
    - Device replacement's extended schema has mutation coverage for every result, durable, and cache field.
    - Opaque proof, nonce, signature, private key, idempotency material, and actor identity remain absent from observations and failure messages.
  </acceptance_criteria>
  <done>Reconnect/cache and device/key actions cannot pass when any declared oracle field differs from the real collected transition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete ordering/resume comparison and enforce every declared leaf in the release gate</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor.ex, accrue/test/support/entitlements/reference_scenario_executor/ordering.ex, accrue/test/support/entitlements/reference_scenario_executor/resume.ex, accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs</files>
  <read_first>
    - accrue/test/support/entitlements/reference_scenario_executor/ordering.ex (permutation, repeat, barrier, and durable facts)
    - accrue/test/support/entitlements/reference_scenario_executor/resume.ex (attempt, challenge, issuance, replay, snapshot, and cache facts)
    - accrue/test/accrue/entitlements/reference_scenario_conformance_test.exs (27-action aggregate runner)
    - scripts/ci/verify_reference_scenario_contract.sh (existing coordinated credential-free gate; verify unchanged composition)
    - accrue/lib/accrue/entitlements/reference_scenarios.ex (`ExpectedTransition` decoded structure)
  </read_first>
  <behavior>
    - "Equal-order, repeat, and barrier-parallel actions compare common declared fields after production observations converge to the collected durable snapshot."
    - "Interruption and exact resume compare common declared fields after real admitted/completed attempt, challenge, issuance, replay, snapshot, and cache outcomes."
    - "For every one of 27 actions, every scalar leaf under result/durable/cache is mutated once and rejected against the same unchanged production observation."
    - "Mutation failures name scenario ID, order, kind, and leaf path without printing private material."
    - "The root coordinated verifier runs the exhaustive mutation proof plus all previously shipped host, Swift, diagnostic, repair, generator, adoption, and release gates."
  </behavior>
  <action>Add production-derived declared transitions to Ordering and Resume, using actual sequential-equivalent/barrier durable results, attempt/issuance state, replay stability, snapshot revision, and cache replacement outcome. Replace the final kind-only/partial central matchers with exact expected-object comparisons while retaining the richer ordering and resume invariants. In the aggregate test, execute each deterministic action once, then recursively enumerate all decoded result/durable/cache leaves and create a type-compatible unequal mutation for each scalar (string, integer, boolean, or atom-normalized string). Assert each mutated action fails against the unchanged observation, with a bounded path-specific message; also assert the observed mutation-path inventory exactly equals the fixture-declared leaf-path inventory so a newly added expected field cannot escape coverage. Keep existing strict-loader mutation tests separate. Confirm the unchanged `verify_reference_scenario_contract.sh` already runs the aggregate test before host/Swift/docs gates, then run that complete credential-free command from repository root.</action>
  <verify>
    <automated>bash scripts/ci/verify_reference_scenario_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - All seven family groups enforce every declared expected field; no matcher discards the expected object or relies only on kind.
    - The mutation-path inventory equals every result/durable/cache scalar path across all 27 deterministic actions.
    - Each mutation fails against an unchanged production observation and reports bounded scenario/action/path context.
    - The coordinated verifier, host, Swift, diagnostics, repairs, generator, adoption matrix, release contract, and feasibility-blocked check pass without credentials.
  </acceptance_criteria>
  <done>Every declared expected field is load-bearing in aggregate conformance, and the final credential-free release gate closes the remaining PROOF-02 blocker without regressing PROOF-01/03/04/05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signed reconnect/device operations → durable collector | Proof-bearing operations must yield bounded independent facts without leaking secrets. |
| Concurrent/resumed execution → oracle | Database and cache outcomes, not fixture values, determine the normalized transition. |
| Mutation harness → assertion | Mutated expectations must exercise comparison only and cannot alter production execution. |
| Coordinated gate → release claim | A green command must include all deterministic and retained public/operator evidence. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-22-01 | Tampering | remaining family matchers | high | mitigate | Tasks 1-2 require exact production-derived comparison for all result/durable/cache leaves. |
| T-220-22-02 | Spoofing | mutation coverage | high | mitigate | Exact declared-path inventory and unchanged-observation mutations prove each field is load-bearing. |
| T-220-22-03 | Information Disclosure | failure diagnostics | high | mitigate | Messages contain only scenario ID, order, kind, and field path; no observed proof or private payload is rendered. |
| T-220-22-04 | Denial of Service | mutation matrix | medium | mitigate | Execute each action once and reuse the bounded observation for finite leaf mutations. |
| T-220-22-05 | Repudiation | release evidence | high | mitigate | One fail-fast script composes mutation, family, host, Swift, diagnostic/repair, generator, and public-contract gates. |
| T-220-22-SC | Tampering | package installs | high | mitigate | No dependency installation or lockfile change is planned. |
</threat_model>

## Planner contribution declarations

- **API coverage:** Non-integration declaration — no external API integration is added; existing Fake-backed and local production seams remain the merge authority, so `COVERAGE.md` is not applicable.
- **Assumption delta:** No further detector delta after Plan 220-21 resolves the stale cache assumption. Mutation logic derives its path inventory from the decoded fixture at test time.
- **Schema gate:** No database or public contract schema mutation. Strict decoder tests, deterministic generator check, host/Swift decoding, and release gates must all pass unchanged except for the corrected existing enum values from Plan 220-21.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Safe, diagnosable, operable multi-rail/offline proof | 220-21..220-22 | COVERED | Exact oracle plus retained full gate.
REQ | PROOF-01 | Apple/web and Stripe/iOS convergence | 220-22 | COVERED | Host proof rerun; no runtime feasibility promotion.
REQ | PROOF-02 | Complete credential-free lifecycle/offline/reconnect/device/order/resume proof | 220-21..220-22 | COVERED | Every action and every declared leaf is enforced.
REQ | PROOF-03 | Privacy-bounded diagnostics | 220-22 | COVERED | Diagnostic suite retained; failures remain bounded.
REQ | PROOF-04 | Safe repair/runbook behavior | 220-22 | COVERED | Repair and release gates rerun.
REQ | PROOF-05 | Additive public contract and conformance gates | 220-22 | COVERED | Generator, host, Swift, matrix, and release checks retained.
RESEARCH | — | Shared data-only corpus with production-only authority | 220-21..220-22 | COVERED | Expectations enter only after execution.
RESEARCH | — | Credential-free Fake-first merge proof | 220-22 | COVERED | Coordinated script stays local and deterministic.
CONTEXT | D-01 | One shared synthetic data-only corpus | 220-21..220-22 | COVERED | Exact equality does not turn the fixture into a reducer.
CONTEXT | D-02 | Complete closed expected result/durable/cache contract | 220-21..220-22 | COVERED | Leaf inventory and mutations cover every field.
CONTEXT | D-03 | No runtime feasibility inflation | 220-22 | COVERED | Capability report remains feasibility_blocked.
CONTEXT | D-04 | Focused semantic oracle | 220-21..220-22 | COVERED | Family ExUnit/Ecto tests plus Swift vector consumer.
CONTEXT | D-05/D-06 | Bounded diagnostic/privacy contract | 220-22 | COVERED | Existing suite plus bounded mutation messages.
CONTEXT | D-07/D-08 | Distinct safe repairs/drills | 220-22 | COVERED | Existing repair gate rerun without behavior changes.
CONTEXT | D-09/D-10 | Generated additive public facts and limits | 220-22 | COVERED | Generator and release gates remain authoritative.
CONTEXT | D-11 | One deterministic verification command | 220-22 | COVERED | Coordinated script is the closeout entrypoint.
CONTEXT | D-12 | Accessible job/next-action outcomes | 220-22 | COVERED | No UI change; host verification remains green.
</source_audit>

<verification>
Run the root coordinated command. Inspect its output to confirm all 27 actions execute once before their finite leaf-mutation comparisons, all family tests pass, the mutation-path inventory is exact, and host/Swift/diagnostic/repair/generator/adoption/release checks remain green.
</verification>

<success_criteria>
- Every declared expected_transition result/durable/cache field across all deterministic actions is compared with independent production evidence.
- The known refund contradiction is corrected and mutation-proven rather than hidden by kind-only matching.
- All five Phase-220 requirements retain automated evidence; the two judgment-tier human reviews remain human reviews, not code gaps.
- No external integration, dependency, schema, runtime capability, public API, or unrelated shipped plan is changed.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-22-SUMMARY.md` when done.
</output>

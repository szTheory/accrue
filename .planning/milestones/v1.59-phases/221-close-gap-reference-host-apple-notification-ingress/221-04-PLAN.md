---
phase: 221-close-gap-reference-host-apple-notification-ingress
plan: 04
type: execute
wave: 3
depends_on: [221-02]
files_modified:
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/test/accrue_host/recovery_wiring_test.exs
autonomous: true
requirements: [D-03, D-04, D-05, D-07, D-11]

must_haves:
  truths:
    - "The reference host schedules existing Apple reconciliation on `:accrue_entitlements` without replacing any existing queue, plugin, or Cron worker (D-07)."
    - "Configuration proof connects the ingress-created wakeup to the existing production client/admission and sweeper path while PostgreSQL remains correctness authority (D-03/D-07/D-11)."
  artifacts:
    - path: "examples/accrue_host/config/config.exs"
      provides: "Additive Apple queue and reconciliation Cron schedule"
      contains: "Accrue.Entitlements.Apple.ReconciliationSweeper"
    - path: "examples/accrue_host/test/accrue_host/recovery_wiring_test.exs"
      provides: "Static/runtime-safe additive recovery wiring proof"
  key_links:
    - from: "examples/accrue_host/config/config.exs"
      to: "Accrue.Entitlements.Apple.ReconciliationSweeper"
      via: "Oban Cron every 15 minutes"
      pattern: "\*/15.*ReconciliationSweeper"
    - from: "Accrue.Entitlements.Apple.ReconcileWorker"
      to: "examples/accrue_host/config/runtime.exs"
      via: "`:apple_reconciliation` client/admission config from Plan 221-02"
      pattern: "apple_reconciliation"
---

<objective>
Complete the host's additive Oban recovery wiring and prove that existing schedules survive while Apple wakeups drain through the already-configured production admission/reconciliation path.

Purpose: Turn durable notification wakeups into operable missed-notification repair without shifting correctness from PostgreSQL to Oban.
Output: Additive queue/Cron configuration and an executable configuration contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-CONTEXT.md
@.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-RESEARCH.md
@.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-PATTERNS.md
@.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-02-SUMMARY.md
@accrue/lib/accrue/entitlements/apple/reconciliation.ex
@accrue/lib/accrue/entitlements/apple/reconciliation_wakeup.ex
@accrue/lib/accrue/entitlements/apple/reconciliation_sweeper.ex
@accrue/lib/accrue/entitlements/apple/reconcile_worker.ex
@examples/accrue_host/config/runtime.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append Apple reconciliation resources without disturbing host recovery</name>
  <read_first>
    - `examples/accrue_host/config/config.exs` — current six queues, Pruner, and four Cron workers are preservation requirements.
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` — `Config.Reader`, `cron_workers/1`, and `queue_names/1` precedent.
    - `accrue/lib/accrue/entitlements/apple/reconciliation_sweeper.ex` — host-scheduled entry point and queue identity.
    - `accrue/lib/accrue/entitlements/apple/reconcile_worker.ex` — runtime `:apple_reconciliation` client/admission consumer.
  </read_first>
  <behavior>
    - Test 1: base Oban configuration remains valid and contains every pre-existing queue plus `accrue_entitlements: 10`.
    - Test 2: every pre-existing Cron worker remains and `{"*/15 * * * *", Accrue.Entitlements.Apple.ReconciliationSweeper}` is appended exactly once.
    - Test 3: test environment still has queues/plugins disabled with `testing: :manual`.
  </behavior>
  <files>examples/accrue_host/config/config.exs, examples/accrue_host/test/accrue_host/recovery_wiring_test.exs</files>
  <action>Write the failing recovery-wiring assertions first. Append `accrue_entitlements: 10` to the existing queue keyword list and append the 15-minute `Accrue.Entitlements.Apple.ReconciliationSweeper` tuple to the existing Cron list; do not rebuild or replace either collection. Extend aliases and exact membership/count assertions so all current queues/Cron workers remain and the Apple entries occur once. Retain the test-environment manual/disabled assertion. Add a source/config assertion that `ReconcileWorker` consumes the runtime `:apple_reconciliation` config produced in Plan 221-02, while the test language explicitly identifies Oban uniqueness as coalescing and the existing PostgreSQL constraints/locks as ownership authority.</action>
  <verify>
    <automated>cd examples/accrue_host &amp;&amp; MIX_ENV=test mix test test/accrue_host/recovery_wiring_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `Oban.Config.validate/1` passes with every original queue/plugin/Cron worker intact.
    - `:accrue_entitlements` has concurrency 10 and `ReconciliationSweeper` is scheduled every 15 minutes exactly once (D-07).
    - Runtime reconciliation uses the same approved verifier config/version identity established for ingress (D-04/D-05).
    - No test or documentation treats job uniqueness as the durable lock; existing PostgreSQL transactions, constraints, and locks remain authoritative.
  </acceptance_criteria>
  <done>The reference host durably schedules existing Apple repair without regressing any recovery resource or inventing a second ownership mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Run the bounded host proof across ingress and recovery wiring</name>
  <read_first>
    - `examples/accrue_host/test/accrue_host/recovery_wiring_test.exs` — configuration assertions from Task 1.
    - `examples/accrue_host/test/accrue_host_web/apple_notification_ingest_test.exs` — tracer-created durable wakeup assertion.
    - `examples/accrue_host/config/runtime.exs` — shared client/admission identity from Plan 221-02.
  </read_first>
  <files>examples/accrue_host/test/accrue_host/recovery_wiring_test.exs</files>
  <action>Strengthen the recovery test only where needed to connect the route-produced wakeup contract to the configured sweeper/worker modules: assert worker queue identities, the single schedule, and the production config source shape without evaluating or printing required environment values. Keep the proof credential-free and deterministic; do not invoke Apple's network or introduce a host polling loop.</action>
  <verify>
    <automated>cd examples/accrue_host &amp;&amp; MIX_ENV=test mix test test/accrue_host/recovery_wiring_test.exs test/accrue_host_web/apple_notification_ingest_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The focused ingress and recovery tests pass together without credentials.
    - The proof establishes route→durable wakeup→scheduled existing sweeper/worker configuration, not a live provider delivery claim.
    - No raw evidence, token, root, certificate, PII, or worker argument is emitted.
  </acceptance_criteria>
  <done>The bounded host test demonstrates that acknowledged ingress has a configured, additive, existing recovery path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable wakeup → Oban scheduler/worker | Asynchronous repair must not become a competing correctness lock. |
| Runtime config → App Store Server API client | Authorization and environment identity cross into external provider I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-221-14 | Tampering | Concurrent repair ownership | high | mitigate | Preserve existing PostgreSQL transactions/constraints/locks; tests treat Oban uniqueness only as coalescing. |
| T-221-15 | Denial of Service | Recovery scheduling | medium | mitigate | Existing bounded sweeper plus dedicated queue concurrency 10 and a single 15-minute Cron entry. |
| T-221-16 | Tampering | Environment/client wiring | high | mitigate | ReconcileWorker consumes the production-only client/admission config sharing the ingress verifier identity. |
| T-221-17 | Information Disclosure | Worker/config tests | high | mitigate | Assert config structure only; never evaluate, print, or fixture tokens, roots, evidence, PII, or worker arguments. |
</threat_model>

<verification>
- `cd examples/accrue_host && MIX_ENV=test mix test test/accrue_host/recovery_wiring_test.exs test/accrue_host_web/apple_notification_ingest_test.exs --warnings-as-errors`
- `cd examples/accrue_host && mix format --check-formatted`
- `cd examples/accrue_host && mix verify`
</verification>

<success_criteria>
- Existing recovery queues and schedules remain intact.
- Apple repair is scheduled exactly once on the intended queue.
- The route-to-repair configuration is deterministic and credential-free under test.
</success_criteria>

## Artifacts this phase produces

- `examples/accrue_host/lib/accrue_host/apple_notification_ingress.ex` — `AccrueHost.AppleNotificationIngress` Plug plus strict runtime loaders.
- `examples/accrue_host/lib/accrue_host/apple_rate_policy.ex` — `AccrueHost.AppleRatePolicy.start_link/1` and `check/1`.
- `examples/accrue_host/test/accrue_host_web/apple_notification_ingest_test.exs` — complete Fake-first router/persistence proof.
- `examples/accrue_host/test/accrue_host/apple_rate_policy_test.exs` — deterministic local-policy proof.
- `:accrue_apple_notifications_raw_body` and `/webhooks/apple` — dedicated host router pipeline and endpoint.
- `:apple_notification_ingress` and `:apple_reconciliation` — shared runtime ingress/admission configuration.
- `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/COVERAGE.md` — decided Apple API surface matrix.

<output>
Create `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-04-SUMMARY.md` when done.
</output>

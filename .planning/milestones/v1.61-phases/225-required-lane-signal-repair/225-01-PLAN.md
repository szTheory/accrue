---
phase: 225-required-lane-signal-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
  - .planning/phases/225-required-lane-signal-repair/COVERAGE.md
  - scripts/ci/README.md
  - accrue/test/accrue/webhook/ingest_test.exs
autonomous: true
requirements: [REL-01, REL-03]
must_haves:
  truths:
    - "D-01/D-04/REL-03: one normalized webhook signature is one incident whose affected-cell list distinguishes the three required cells from advisory Sigra."
    - "D-03/REL-01: each active incident leads with what failed, its precise classification, one credential-free next command, and an immutable evidence link."
    - "D-05/D-07: webhook ingest tests prove event-owned webhook, dispatch-job, and ledger facts plus same-identity duplicate rejection; no successful path is described as rollback proof."
    - "D-02/D-06: raw logs and browser payloads remain Actions artifacts, and the repair does not change release topology, support status, check identity, retries, serialization, caches, or branch protection."
  artifacts:
    - path: ".planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md"
      provides: "privacy-safe normalized incident index and proof ledger"
    - path: ".planning/phases/225-required-lane-signal-repair/COVERAGE.md"
      provides: "reasoned planning-gate declaration that Phase 225 adds no external API integration"
    - path: "scripts/ci/README.md"
      provides: "command-first maintainer triage entry for both Phase 225 incidents"
    - path: "accrue/test/accrue/webhook/ingest_test.exs"
      provides: "identity-scoped release-matrix regression and duplicate negative control"
  key_links:
    - from: "scripts/ci/README.md"
      to: ".planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md"
      via: "Phase 225 triage link after the exact narrow command"
      pattern: "225-CI-INCIDENTS.md"
    - from: "accrue/test/accrue/webhook/ingest_test.exs"
      to: "accrue/lib/accrue/webhook/ingest.ex"
      via: "processor + processor_event_id, DispatchWorker args webhook_event_id, and webhook.received subject_id"
      pattern: "processor_event_id|webhook_event_id|subject_id"
  assumptions:
    - id: "EDGE-REL-01-ADJACENCY"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: When two normalized failure signatures are exactly equal or just touch, whether they merge, collide, or remain separate is not specified beyond the locked identical-signature case."
    - id: "EDGE-REL-01-EMPTY"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: Empty, single-signature, and null failure-input behavior is not specified; this phase has two known active signatures."
    - id: "EDGE-REL-01-ORDERING"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: Stable output order for equal incident elements is not specified."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: Incident language must not turn an unexplained failure into a claim of flakiness or a compatibility defect without trace-backed evidence."
      category: transparency
      status: unresolved
      verification: null
    - statement: "FLAGGED-UNVERIFIED: Advisory Sigra evidence must not be presented as required release proof."
      category: transparency
      status: unresolved
      verification: null
---

<objective>
Create the durable trace-to-command incident interface, then repair the shared release-matrix webhook signature at its actual test-isolation cause per D-01 through D-07.

Purpose: Give maintainers one causal record per signature and make the release test prove only the webhook event's owned facts.
Output: `225-CI-INCIDENTS.md`, `COVERAGE.md`, Phase 225 command-first triage, and identity-scoped `Accrue.Webhook.IngestTest` coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
@.planning/phases/225-required-lane-signal-repair/225-RESEARCH.md
@.planning/phases/225-required-lane-signal-repair/225-PATTERNS.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-VERIFICATION.md

Assumption delta: detector result is `no-change`; the generalized primary noun remains one required-lane failure-signature incident, with no alternate or alongside noun introduced.

Prohibition-probe breadcrumb: generic secret leakage, injection, and artifact-access concerns are security canon covered by the STRIDE register and later `$gsd-secure-phase`; the descriptor-less transparency items above are retained as flagged-unverified product intent.
</context>

## Artifacts this phase produces

- `.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md` — new phase-local incident record with stable incident IDs `INC-225-RELEASE-WEBHOOK` and `INC-225-ADMIN-PAGEFLOW`; no new runtime symbol.
- `scripts/ci/README.md` — new `### Triage: Phase 225 required-lane incidents` section; no new executable symbol.
- `accrue/test/accrue/webhook/ingest_test.exs` — renamed ExUnit test descriptions only; no new production symbol or file.
- `.planning/phases/225-required-lane-signal-repair/COVERAGE.md` — planning-gate declaration that this phase contains no external API integration.

<tasks>

<task type="tracer">
  <name>Task 1: Trace both active signatures from failure to one command and one causal record</name>
  <read_first>
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md (create)
    - scripts/ci/README.md
    - .planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
    - .planning/phases/225-required-lane-signal-repair/225-RESEARCH.md
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-VERIFICATION.md
    - .github/workflows/ci.yml
    - accrue/test/accrue/webhook/ingest_test.exs
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js
  </read_first>
  <files>.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md, scripts/ci/README.md</files>
  <action>Create the D-01 phase-local causal index with exactly two top-level incident sections: `INC-225-RELEASE-WEBHOOK` and `INC-225-ADMIN-PAGEFLOW`. Give each a compact lead block in this order: `What failed`, `Classification`, `Next command`, `Evidence`; then record incident ID/status, normalized signature, classification/confidence, first/last run and SHA, affected cells, canonical owner and repair surface, credential-free narrow repro, immutable run/job/artifact links, ruled-out hypotheses, root cause, corrective change, negative-control proof, targeted/full-local/fresh-run evidence, and residual owner/status. Mark unexecuted proof fields `pending` rather than green. For the release incident, record run `31289155535`, classify one test-isolation/over-broad-observation signature per D-04, and list Floor, Primary dev target, and OpenTelemetry as `[required]` while Sigra is `[advisory]` per D-13. For Admin, record the trace-backed capacity/topology-versus-whole-test-budget diagnosis from D-08, including 5 viewports × 2 themes × 21 flows and ordinary successful checks near 64 seconds. Keep raw logs, traces, screenshots, reports, server output, secrets, and user data out of Markdown per D-02. Add `### Triage: Phase 225 required-lane incidents` to `scripts/ci/README.md`; put the targeted release and Admin commands before the causal-index link and state the exact responsible source file for each. Use calm mechanism-led language per D-03 and do not imply a retry, deletion, matrix, gate, cache, topology, or UI change per D-06.</action>
  <acceptance_criteria>`225-CI-INCIDENTS.md` has exactly two normalized incident sections, every D-01 field is present for both, the webhook section contains three visibly required cells plus advisory Sigra, the Admin section records the trace-backed 210-cycle/60-second diagnosis, pending evidence is not reported as passed, and `scripts/ci/README.md` gives one exact next command and canonical repair surface for each incident.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md &amp;&amp; test "$(rg -c '^## INC-225-' .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md)" -eq 2 &amp;&amp; for field in 'What failed' 'Classification' 'Next command' 'Evidence' 'Normalized signature' 'First run' 'Last run' 'Affected cells' 'Canonical owner' 'Narrow repro' 'Ruled-out hypotheses' 'Root cause' 'Corrective change' 'Negative-control proof' 'Fresh repair-run evidence' 'Residual owner'; do test "$(rg -c "$field" .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md)" -ge 2 || exit 1; done &amp;&amp; rg -F '[required]' .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md &amp;&amp; rg -F 'Sigra [advisory]' .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md &amp;&amp; rg -F 'Triage: Phase 225 required-lane incidents' scripts/ci/README.md</automated>
  </verify>
  <done>A maintainer can start from either current red signature, run one narrow command, and reach one privacy-safe causal record without treating matrix cells as separate incidents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace webhook global observations with event-owned positive and duplicate-negative contracts</name>
  <reversibility rating="reversible">The change is confined to one internal ExUnit file and can be reverted without migration or public-contract breakage.</reversibility>
  <read_first>
    - accrue/test/accrue/webhook/ingest_test.exs
    - accrue/lib/accrue/webhook/ingest.ex
    - accrue/test/support/repo_case.ex
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - .planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
  </read_first>
  <files>accrue/test/accrue/webhook/ingest_test.exs</files>
  <behavior>
    - Positive ingest: the created `WebhookEvent` is selected by `processor == "stripe"` and `processor_event_id == stripe_event.id`; its linked `Accrue.Webhook.DispatchWorker` job is selected by worker and `args["webhook_event_id"] == event.id`; its ledger row is selected by `type == "webhook.received"` and `subject_id == to_string(event.id)`.
    - Duplicate ingest: a second `Ingest.run/4` for the same processor/event identity returns 200 and leaves exactly one identity-scoped webhook row, one event-linked dispatch job, and one event-linked received-ledger row.
    - Atomic co-presence: the present success-only test is truthfully named and proves the three owned facts coexist; it makes no rollback-on-failure claim per D-07.
  </behavior>
  <action>In `Accrue.Webhook.IngestTest`, retain `use Accrue.RepoCase`, `Ingest.run/4`, the request timing contract, event field assertions, idempotency, and atomic co-presence. Replace every whole-table `Repo.all/1`, table-wide `aggregate/3`, and list-length observation in the first four tests with Ecto queries constrained to D-05's owned identities. Use `Accrue.TestRepo.get_by!(WebhookEvent, processor: "stripe", processor_event_id: stripe_event.id)` for the event. Query `Oban.Job` by worker string `Accrue.Webhook.DispatchWorker` and JSON args `webhook_event_id` equal to the event UUID; query `Accrue.Events.Event` by `type`, `subject_type`, and string `subject_id`. In the duplicate test, capture the first event ID, invoke the duplicate, then assert one row for each same-identity predicate so an unrelated concurrent row cannot fail or satisfy the test. Rename the current rollback-titled test to describe successful webhook/job/ledger co-presence because inspection found no deterministic Oban insertion failure seam and D-07 forbids claiming rollback proof from the success path. Do not alter `Ingest`, RepoCase, sandbox mode, async/serialization settings, production behavior, or CI topology per D-06.</action>
  <acceptance_criteria>The targeted suite proves the persisted webhook, dispatch job, and received-ledger row by the event's stable identity; same-identity replay creates no second owned fact; unrelated rows cannot affect those assertions; the success-only test title and comments no longer claim an injected Oban failure or rollback; all existing timing, status, idempotency, and atomic co-presence behavior remains covered.</acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/webhook/ingest_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The one shared release-matrix signature is repaired at its test-isolation cause with deterministic same-identity positive and negative proof.</done>
</task>

<task type="auto">
  <name>Task 3: Create the external-API coverage declaration</name>
  <read_first>
    - .planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
    - .planning/phases/225-required-lane-signal-repair/225-RESEARCH.md
    - .github/workflows/ci.yml
    - accrue/test/accrue/webhook/ingest_test.exs
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js
  </read_first>
  <files>.planning/phases/225-required-lane-signal-repair/COVERAGE.md</files>
  <action>Create the phase-local planning-gate declaration `COVERAGE.md`. State that Phase 225 repairs first-party ExUnit, Playwright, GitHub Actions, and maintainer-evidence contracts and does not add or extend a third-party API, SDK, endpoint, or service capability. Explain that webhook prose describes the repository's existing first-party ingest test and GitHub Actions is used only as the existing repair-proof surface, so neither introduces an external integration contract. Keep this declaration scoped to external-API coverage; do not duplicate the D-01 incident ledger or copy raw CI evidence.</action>
  <acceptance_criteria>`COVERAGE.md` exists as a non-empty phase-local artifact, explicitly declares no external API integration, names the first-party surfaces that triggered detector review, and gives the rationale for excluding them from external integration coverage.</acceptance_criteria>
  <verify>
    <automated>DOC=.planning/phases/225-required-lane-signal-repair/COVERAGE.md &amp;&amp; test -s "$DOC" &amp;&amp; rg -F 'No external API integration' "$DOC" &amp;&amp; rg -F 'ExUnit' "$DOC" &amp;&amp; rg -F 'Playwright' "$DOC" &amp;&amp; rg -F 'GitHub Actions' "$DOC" &amp;&amp; rg -F 'does not add or extend any third-party API, SDK, endpoint, or service capability' "$DOC"</automated>
  </verify>
  <done>The planning-gate artifact exists and truthfully explains why Phase 225 requires no external API integration coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Actions evidence → checked-in incident index | Run, job, artifact, and failure text are untrusted external evidence until reduced to immutable links and credential-free classifications. |
| test-created webhook identity → shared PostgreSQL tables | Unrelated sandbox-visible rows must not influence the event-under-test assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-225-01 | Information disclosure | `225-CI-INCIDENTS.md` | high | mitigate | D-02 permits concise privacy-safe facts and immutable links only; raw payloads, traces, logs, screenshots, secrets, and user data remain Actions artifacts. |
| T-225-02 | Tampering | `Accrue.Webhook.IngestTest` | high | mitigate | D-05 identity-scoped positive and duplicate-negative queries prove the owned facts without accepting global absence as evidence. |
| T-225-03 | Repudiation | incident classification | medium | mitigate | Each incident records run/SHA, confidence, ruled-out hypotheses, exact command, owner, and immutable evidence URL per D-01. |
| T-225-04 | Denial of service | release test isolation | low | accept | Focused integration tests still use the established SQL sandbox; unrelated execution cost is unchanged and no new production path is introduced. |
| T-225-SC | Tampering | package/source acquisition | high | mitigate | No npm, Hex, pip, or cargo package is installed or upgraded in this plan; all code uses locked existing dependencies. |
</threat_model>

<verification>
Run the targeted ExUnit file, inspect the two incident leads and cell labels, and confirm the release workflow topology and support labels are untouched by `git diff -- .github/workflows/ci.yml`.
</verification>

<success_criteria>
- Both active signatures have one normalized, trace-backed, privacy-safe incident record and exact next command.
- The phase-local external-API coverage declaration exists with its reasoned no-integration determination.
- The release signature is one incident across three required cells and advisory Sigra.
- Webhook proof is identity-scoped and retains idempotency, timing, and atomic co-presence assertions.
- No success-only path is represented as rollback-on-failure evidence.
</success_criteria>

<output>
Create `.planning/phases/225-required-lane-signal-repair/225-01-SUMMARY.md` when done.
</output>

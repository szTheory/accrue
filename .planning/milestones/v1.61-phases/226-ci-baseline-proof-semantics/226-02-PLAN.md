---
phase: 226-ci-baseline-proof-semantics
plan: "02"
type: execute
wave: 2
depends_on: ["226-01"]
files_modified:
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01]
must_haves:
  truths:
    - "D-01/D-03: The checked-in schema-v1 NDJSON and generated Markdown form a frozen, reproducible Phase 226 before-state tied to immutable Actions evidence."
    - "D-02/D-04: The report visibly names its exact cohort fingerprint, qualifying/excluded counts, reliability incidents, and insufficient-sample state without mixing trigger topologies or rerun attempts."
    - "D-05: The report distinguishes workflow wall time, runner queue, DAG wait, job/step/setup/cache durations, and states whether measurements confirm or refute the 33–36 minute staged critical-path hypothesis."
    - "D-19/D-20/D-21: A maintainer first sees fact, literal state, owner, exact next command, and immutable evidence links in measured, accessible Markdown."
  artifacts:
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      provides: "Frozen sanitized schema-v1 before-state records"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Generated maintainer-facing comparable-run and critical-path account"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "BASE-01 task-row execution status and Nyquist sign-off after its planned automated commands pass"
  key_links:
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      to: "GitHub Actions runs/jobs/artifacts"
      via: "immutable IDs and URLs plus sanitized derived facts"
      pattern: "run_id|run_url"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      via: "byte-reproducible renderer output"
      pattern: "cohort|critical path|insufficient"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      to: ".planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md"
      via: "Phase 225 repair run is retained as the immediate SHA-bound boundary, not substituted for the cohort"
      pattern: "31322443304|ee940cf9e1f8"
  assumptions: []
  prohibitions: []
---

<objective>
Collect, freeze, and publish the durable BASE-01 comparable-run before-state using the proven Plan 01 engine per D-01 through D-07 and D-19 through D-21.

Purpose: Give Phase 227 a trustworthy measured baseline and give maintainers an immediately readable answer about the actual critical path.
Output: Sanitized frozen NDJSON, byte-reproducible Markdown, and completed BASE-01 validation metadata.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md
@.planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md
@.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
@.planning/milestones/v1.55-phases/202-ci-cd-performance-and-determinism-audit/202-CI-CD-PERFORMANCE-AUDIT.md
</context>

## Artifacts this phase produces

- `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson`, the immutable schema-v1 source of truth consumed by Phase 227.
- `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md`, the generated report with sections `Current fact`, `Comparable cohort`, `Measured critical path`, `Setup and cache costs`, `Provider state`, `Reliability`, `Exclusions`, and `Reproduce`.
- Frozen report fields `snapshot_generated_at`, `window_start`, `window_end`, `cohort_fingerprint`, `qualifying_count`, `sample_status`, `workflow_wall_ms`, `runner_queue_ms`, `dag_wait_ms`, job/step/setup/cache facts, root signatures, provider states, and immutable evidence links.
- Updated `226-VALIDATION.md` BASE-01 task rows after the fixture and frozen-record gates pass; planning-time automated-reference coverage remains distinct from execution status.

<tasks>

<task type="auto">
  <name>Task 1: Collect the frozen 90-day comparable-run and reliability snapshot (D-01 through D-06)</name>
  <precondition>`.planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md` exists, `gh auth status` succeeds for `szTheory/accrue`, and Actions metadata is readable without changing repository state.</precondition>
  <reversibility rating="costly">Phase 227 will cite this frozen before-state; correction remains possible but requires an explicit regenerated snapshot and audit trail.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson (create)
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
    - .github/workflows/ci.yml
    - .planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
  </read_first>
  <action>Run the read-only collector against repository `szTheory/accrue` and workflow `ci.yml` for the latest 90 days with sample target 20. Fetch all attempts and paginated jobs/steps, the workflow/config content at each immutable head SHA, and artifact metadata/links allowed by `COVERAGE.md`; do not fetch raw logs or artifact contents. Preserve Phase 225 run `31322443304` and truncated repair SHA `ee940cf9e1f8` as the immediate fresh-run boundary while applying D-02 cohort equality to all timing samples. Write normalized schema-v1 NDJSON atomically, sort records deterministically, retain exact exclusion reasons and reliability rows, and run the frozen-record validator before accepting the file. If the exact cohort has fewer than 20 observations, preserve the exact count and `insufficient_sample` per D-03; do not broaden the cohort.</action>
  <acceptance_criteria>
    - The NDJSON parses one JSON object per non-empty line and every object validates against schema-v1.
    - Every timing sample has the same immutable cohort fingerprint and is successful, first-attempt, full-CI, and inside the 90-day window.
    - Failure, cancellation, skip, and rerun attempts are absent from percentile samples and present in reliability records with immutable evidence links where available.
    - No record contains actor, raw branch, log text, secret metadata, provider payload, artifact contents, or user/application data.
    - Run `31322443304` is represented as the Phase 225 boundary without being treated as a complete 20-run cohort by itself.
  </acceptance_criteria>
  <verify>
    <automated>gh auth status &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson</automated>
  </verify>
  <done>The repository contains a validated, sanitized, exact-cohort NDJSON before-state with explicit exclusions and reliability evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Render and byte-verify the maintainer critical-path account (D-05, D-19, D-20, D-21)</name>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md (create)
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - brandbook/voice.md
    - brandbook/copy.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
  </read_first>
  <action>Render the frozen NDJSON into conventional Markdown using the Phase 225 fact/classification/next-command/evidence grammar per D-19/D-20. Print literal states and table headers so meaning does not depend on color. Separate comparable timing, setup/cache cost, provider state, and reliability evidence. Name the measured root-job queue and each dependent DAG wait distinctly. Calculate and state whether the evidence confirms the expected staged release → host integration → Playwright critical path around 33–36 minutes or gives a contrary measured result; if sample status is insufficient, label the conclusion accordingly and do not fabricate percentiles. Include exact reproduction commands and the schema/fixture gate. Regenerate to a temporary file and byte-compare with the checked-in Markdown. Mark only the completed BASE-01 task rows and execution-time Nyquist sign-off items actually established by these passing commands in `226-VALIDATION.md`; leave BASE-02 and OWN-01 task rows pending for their plans.</action>
  <acceptance_criteria>
    - The Markdown leads with fact, literal state, owner, exact next command, then immutable evidence; no status meaning is color-only.
    - The report displays workflow wall, runner queue, DAG wait, job/step/setup/cache durations, reruns, provider state, and normalized root failure signatures or explicitly labels unavailable facts.
    - The report contains a measured confirmation or contrary result for the staged critical-path hypothesis and accurately reflects `insufficient_sample` when applicable.
    - Rendering the same NDJSON twice produces byte-identical Markdown.
    - `226-VALIDATION.md` does not mark BASE-02 or OWN-01 complete in this task.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/render_ci_baseline.mjs --input .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --out .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>The generated Markdown is reproducible, privacy-safe, accessible, action-first, and states the measured critical path without overstating the cohort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated `gh` reads → frozen repository file | Live Actions metadata becomes durable checked-in evidence. |
| Frozen NDJSON → maintainer Markdown | Machine facts become claims that can influence Phase 227 optimization decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-05 | Information Disclosure | frozen NDJSON/Markdown | high | mitigate | Revalidate the strict allowlist against live output and reject forbidden/unknown fields before files are accepted. |
| T-226-06 | Tampering | frozen cohort selection | high | mitigate | Store fingerprint inputs, exclusion reasons, immutable run links, exact count, and byte-reproducible derived output. |
| T-226-07 | Repudiation | critical-path claim | medium | mitigate | Tie every claim to frozen records and print exact reproduction/validation commands. |
| T-226-08 | Spoofing | external evidence links | low | accept | Restrict immutable links to the configured GitHub repository and retain IDs alongside URLs. |
</threat_model>

<verification>
Run the BASE-01 fixture gate, frozen-record validator, deterministic renderer, and byte-comparison path. Inspecting raw logs or secret-bearing artifacts is neither necessary nor permitted for completion.
</verification>

<success_criteria>
- The frozen machine and human evidence pack is checked in, validated, reproducible, and tied to immutable Actions facts.
- The exact cohort count and sample sufficiency are truthful; unlike runs and rerun attempts never fill the sample.
- The critical-path conclusion distinguishes queue from dependency wait and is consumable by Phase 227.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md` when done.
</output>

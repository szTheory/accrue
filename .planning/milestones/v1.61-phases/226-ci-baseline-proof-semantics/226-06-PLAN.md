---
phase: 226-ci-baseline-proof-semantics
plan: "06"
type: execute
wave: 4
depends_on: ["226-05"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/provider_proof.mjs
  - scripts/ci/verify_provider_proof.mjs
autonomous: true
requirements: [BASE-01, BASE-02]
gap_closure: true
must_haves:
  truths:
    - "D-02/D-03/D-10: A successful first-attempt push or pull-request run from the full-CI topology can qualify for its event-specific 20-run/90-day timing cohort even when provider_state is non_run; provider proof state remains an observed fact, not a timing-eligibility predicate."
    - "D-02/D-04: Schedule-only provider executions, failures, cancellations, skips, and rerun attempts remain excluded from green full-CI percentiles, and push and pull-request observations remain separate cohorts."
    - "D-05/D-07: Both CI-baseline and provider-proof timestamp paths accept only the locked canonical UTC grammar whose parsed value round-trips through Date.toISOString(), rejecting calendar-impossible days before duration or freshness arithmetic."
    - "D-06/D-08 through D-13: Cohort repair does not weaken the privacy allowlist or provider proof state machine; non_run still means there is no provider proof for that SHA."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Full-CI topology/event timing qualification independent of provider proof state, plus round-trip-validated baseline timestamps"
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Deterministic 20-run non_run push/pull-request and impossible-calendar regression coverage"
    - path: "scripts/ci/provider_proof.mjs"
      provides: "Round-trip-validated provider manifest and freshness timestamps"
    - path: "scripts/ci/verify_provider_proof.mjs"
      provides: "Deterministic invalid-day and invalid-leap-day provider regressions"
  key_links:
    - from: "scripts/ci/collect_ci_baseline.mjs summarizeCohorts"
      to: "normalized full-CI run event/topology facts"
      via: "green timing eligibility uses event/topology, conclusion, attempt, identity, and 90-day window without consulting provider_state"
      pattern: "summarizeCohorts|event_class|provider_state"
    - from: "scripts/ci/verify_ci_baseline.mjs"
      to: "scripts/ci/collect_ci_baseline.mjs"
      via: "separate 20-run push and pull_request cohorts whose records all carry provider_state non_run"
      pattern: "summarizeCohorts|non_run|pull_request|push"
    - from: "scripts/ci/verify_provider_proof.mjs"
      to: "scripts/ci/provider_proof.mjs"
      via: "manifest and freshness inputs exercise the same canonical UTC round-trip validator"
      pattern: "validateProviderManifest|deriveFreshness"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No phase SPEC exists and the edge probe left BASE-01 unresolved/unclassified; the plan retains the assumption that deterministic non_run push/pull-request cohorts plus malformed-calendar controls cover the newly discovered timing and timestamp boundaries without dismissing the wider spec-less edge surface."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No phase SPEC exists and the edge probe left BASE-02 unresolved/unclassified; the plan retains the assumption that provider timestamp round-trip controls preserve the exhaustive proof transitions, freshness boundary, SHA binding, and promotion-negative behavior without dismissing the wider spec-less edge surface."
  prohibitions:
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branch names, logs, secret-presence details, provider payloads, artifact contents, or user data in the checked-in baseline."
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT mix schedule-only provider runs into full-CI timing, use provider proof state to qualify timing, count reruns as new samples, or manufacture p50/p95 below the locked 20 successful first-attempt observations in 90 days."
    - requirement_id: BASE-02
      status: flagged-unverified
      statement: "MUST NOT promote a green, skipped, non_run, zero-selected, stale, Fake-backed, or differently-SHA run into live-provider proof for the current SHA."
---

<objective>
Close the Phase 226 cohort-qualification and timestamp-validation gaps while preserving the locked privacy and provider-proof boundaries (D-02 through D-13).

Purpose: Let ordinary full-CI push and pull-request history form truthful comparable timing cohorts, and fail closed before impossible calendar values can corrupt duration or provider-freshness claims.
Output: Corrected existing collector/provider validators and deterministic regression coverage in both existing fixture gates.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-05-SUMMARY.md
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/provider_proof.mjs
@scripts/ci/verify_provider_proof.mjs

BASE-01 and BASE-02 remain explicit flagged-unverified assumptions because Phase 226 has no SPEC and the edge probe classified both as unresolved/unclassified. Their retained descriptor-less prohibitions in frontmatter are safety contracts, not auto-dismissable findings.
</context>

## Artifacts this phase produces

No new symbols or files are created. This plan changes behavior only in the existing `summarizeCohorts` and timestamp-validation paths and expands the existing `verify_ci_baseline.mjs --fixtures` and `verify_provider_proof.mjs --fixtures` gates.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove a 20-run non_run full-CI cohort end to end (D-02, D-03, D-04, D-10)</name>
  <reversibility rating="reversible">The qualification predicate and deterministic fixtures are local to the existing collector contract and can be reverted without schema or evidence migration.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_executable_uat_contract.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-05-SUMMARY.md
  </read_first>
  <behavior>
    - Twenty successful, first-attempt, in-window push runs with provider_state non_run produce one push cohort with sample_count 20, sample_status ready, and real p50/p95 values.
    - Twenty successful, first-attempt, in-window pull_request runs with provider_state non_run produce a separate pull-request cohort with sample_count 20, sample_status ready, and real p50/p95 values.
    - Schedule-only provider runs remain outside full-CI timing, and failure/cancellation/skip/rerun exclusions continue to apply independently of provider_state.
    - The emitted run records retain provider_state non_run so timing qualification does not imply provider proof.
  </behavior>
  <action>Start RED by extending `cohortControls` with two deterministic 20-run populations: one `push` and one `pull_request`, every member carrying `provider_state: "non_run"`. Follow the fixture/self-test analog in `scripts/ci/verify_executable_uat_contract.mjs`: construct one valid deterministic population, then mutate one invariant per negative control and assert the precise failure boundary rather than accepting a generic nonzero exit. Assert that each event-specific cohort is `ready`, has exactly 20 samples, and emits p50/p95, while normalized run records still say `non_run`. Then repair `summarizeCohorts` so green timing qualification is derived from full-CI event/topology, successful conclusion, first attempt, original-run/SHA deduplication, rolling 90-day window, and the locked sample target—not from provider proof state. Preserve the analog's validator contract of specific `fail(...)` messages, deterministic inputs, and an explicit PASS result. Keep schedule/provider-only topology excluded and preserve separate cohort fingerprints for push, pull request, full dispatch, and schedule per D-02. Retain all existing reliability grouping and privacy/schema validation; do not recollect or rewrite the frozen baseline in this plan because Plan 226-07 owns the post-repair snapshot.</action>
  <acceptance_criteria>
    - The baseline fixture gate contains independent 20-run `push` and 20-run `pull_request` regression populations with `provider_state: "non_run"` on every run.
    - Both populations yield `sample_count: 20`, `sample_status: "ready"`, and non-null p50/p95 values without changing their normalized provider state.
    - A schedule-only population cannot enter either full-CI cohort, and the existing failed/cancelled/skipped/rerun controls remain green.
    - No schema field, checked-in evidence allowlist, provider state enum, or frozen baseline artifact changes in this task.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>Ordinary full-CI push and pull-request runs can form the locked comparable timing cohort while `non_run` remains an explicit no-provider-proof fact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject impossible canonical-looking UTC timestamps in both proof paths (D-05, D-07, D-08, D-11)</name>
  <reversibility rating="reversible">The stricter validation rejects corrupt input at existing boundaries without changing persisted schema or provider state vocabulary.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs, scripts/ci/provider_proof.mjs, scripts/ci/verify_provider_proof.mjs</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/provider_proof.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_executable_uat_contract.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/provider-proof-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md
  </read_first>
  <behavior>
    - `2026-02-30T00:00:00Z` is rejected by baseline normalization and provider manifest validation even though JavaScript Date parsing would normalize it.
    - `2025-02-29T00:00:00Z` is rejected by both fixture gates, while a real leap day and existing valid whole-second/millisecond UTC fixtures remain accepted.
    - Baseline duration arithmetic and provider freshness arithmetic run only after exact UTC grammar and Date.toISOString round-trip agreement are established.
    - Provider policy, proof_state, raw conclusion, SHA binding, and stale derivation remain independent and unchanged.
  </behavior>
  <action>Start RED in both fixture gates with explicit invalid-day (`2026-02-30T00:00:00Z`) and invalid-leap-day (`2025-02-29T00:00:00Z`) assertions against the real exported entry points. Retain the `scripts/ci/verify_executable_uat_contract.mjs` fixture pattern: begin from a valid leap-day fixture, mutate exactly the calendar invariant under test, require the field-specific rejection, and clean up deterministically. Strengthen each existing timestamp helper to require the exact supported UTC grammar, parse to a finite instant, canonicalize the permitted whole-second spelling to its `.000Z` equivalent, and require equality with `new Date(ms).toISOString()` before returning or using the value. This round-trip must reject Date normalization of impossible calendar fields while retaining current valid whole-second and millisecond fixtures. Exercise baseline run/job timestamp normalization and provider manifest plus freshness validation so neither arithmetic path bypasses the check. Preserve the analog's precise failure boundary and explicit PASS summary, all privacy-safe field boundaries, and the BASE-02 promotion-negative state machine.</action>
  <acceptance_criteria>
    - Both fixture gates reject the explicit invalid-day and invalid-leap-day values with field-specific timestamp errors.
    - Both gates accept `2024-02-29T00:00:00Z`, and existing valid UTC fixtures continue to pass without broadening the accepted grammar.
    - `Date.toISOString()` equality is checked after canonicalizing the already-supported whole-second form; an input normalized by JavaScript to another calendar day fails before duration/freshness calculation.
    - The complete BASE-01 and BASE-02 deterministic fixture commands pass together, including Task 1's non_run cohort regressions and all existing provider promotion-negative cases.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/provider_proof.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures</automated>
  </verify>
  <done>Calendar-impossible UTC values fail closed in baseline timing and provider freshness arithmetic, with deterministic invalid-day and invalid-leap-day regressions in both fixture gates.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Actions run metadata → baseline collector | Untrusted event, provider-state, conclusion, attempt, identity, and timestamps influence durable timing claims. |
| ExUnit/provider records → provider classifier | Untrusted manifest and freshness timestamps influence whether provider evidence is current and valid. |
| Normalized evidence → checked-in baseline/proof summaries | Derived claims must remain privacy-safe and must not overstate timing or live-provider proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-21 | Tampering | `summarizeCohorts` qualification | high | mitigate | Block completion until deterministic 20-run push and pull-request `non_run` cohorts qualify by full-CI topology/event while schedule-only, failed, cancelled, skipped, and rerun controls remain excluded per D-02 through D-04. |
| T-226-22 | Tampering | baseline/provider timestamp validators | high | mitigate | Block completion until exact UTC grammar plus Date.toISOString round-trip checks reject impossible day and non-leap-day inputs in both fixture gates before arithmetic per D-05/D-07/D-11. |
| T-226-23 | Information Disclosure | baseline record path | high | mitigate | Preserve the schema-v1 allowlist and forbidden-field regressions; cohort eligibility changes no persisted fields and cannot admit actor, raw branch, logs, secret facts, payloads, artifact contents, or user data per D-06. |
| T-226-24 | Spoofing | provider proof semantics | medium | mitigate | Assert normalized `provider_state` remains `non_run` on qualifying ordinary CI records and rerun the exhaustive provider promotion-negative gate so timing evidence cannot impersonate provider proof per D-08 through D-13. |
</threat_model>

<verification>
Run both dependency-free fixture gates together. The baseline gate must prove ready 20-run non_run push and pull-request cohorts plus unchanged exclusions/privacy; both gates must reject impossible day and invalid leap-day timestamps while accepting valid leap-day UTC and retaining all provider promotion-negative controls. No network, live provider credentials, frozen snapshot rewrite, or raw artifact inspection is required.
</verification>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe comparable CI account distinguishes critical path and provider state | 226-06, 226-07 | COVERED | This plan repairs qualification/validation; the outlined 226-07 recollects and republishes the measured snapshot. |
| REQ | BASE-01 | Comparable timing baseline and fail-closed timestamps | 226-06, 226-07 | COVERED | Deterministic repair here, frozen evidence refresh next. |
| REQ | BASE-02 | Provider evidence cannot be overstated | 226-06 | COVERED | Timestamp hardening and unchanged exhaustive promotion negatives. |
| RESEARCH | — | Strict schema/input validation and dependency-free deterministic fixtures | 226-06 | COVERED | Uses existing ESM validators and offline fixture gates; no package install. |
| CONTEXT | D-01 through D-07 | Durable comparable baseline, locked 20/90 cohort, privacy, arithmetic, fixtures | 226-01, 226-02, 226-05, 226-06, 226-07 | COVERED | Plan 06 closes the contradictory predicate and malformed-calendar gap without altering evidence shape. |
| CONTEXT | D-08 through D-13 | Independent provider policy/proof/freshness and no false promotion | 226-03, 226-05, 226-06 | COVERED | Provider timestamp hardening retains literal `non_run` and proof boundaries. |
| CONTEXT | D-14 through D-18 | Host/CI setup ownership and proof preservation | 226-04, 226-05 | COVERED | Historical completed scope is unchanged by this gap plan. |
| CONTEXT | D-19 through D-21 | Action-first, accessible, maintainable evidence interface | 226-02, 226-05, 226-07 | COVERED | No new interface is introduced here; refreshed report remains Plan 07 scope. |

Deferred Phase 227 optimizations, topology/cache/matrix/branch-protection changes, StoreKit/iPhone/Crosswake work, and Admin UI ratchet work remain excluded and are not source-audit gaps.

<success_criteria>
- Successful first-attempt full-CI push and pull-request runs qualify for timing independently of their literal `non_run` provider state, while the locked 20 observations/90 days and all exclusion rules remain exact.
- Baseline and provider-proof timestamp paths reject impossible days through canonical UTC round-trip validation before any duration or freshness arithmetic.
- BASE-01 and BASE-02 remain explicit flagged-unverified spec-less assumptions with their privacy, cohort, and proof-promotion prohibitions intact.
- All ASVS L1 high-severity threats are mitigated by blocking deterministic gates, and privacy-safe/provider-proof separation remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-06-SUMMARY.md` when done.
</output>

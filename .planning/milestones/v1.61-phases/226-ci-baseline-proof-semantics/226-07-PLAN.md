---
phase: 226-ci-baseline-proof-semantics
plan: "07"
type: execute
wave: 5
depends_on: ["226-06"]
files_modified:
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: false
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-01 through D-07: The frozen Phase 226 NDJSON is recollected through the repaired collector from allowlisted Actions metadata, validates against the existing schema, contains at least one ready 20-success/90-day full-CI cohort, and regenerates byte-identical Markdown."
    - "D-02 through D-05: The critical-path conclusion is computed across one exact qualifying cohort from each run's ordered release-gate → host-integration → Playwright span, reports cohort ID/count plus p50/p95, and says either the roughly 33–36 minute hypothesis is confirmed or gives the explicit measured contrary result; an aggregate named-path total is not evidence."
    - "D-06/D-19 through D-21: Recollection preserves the privacy allowlist and action-first, literal, accessible evidence grammar without persisting sensitive identifiers, sensitive values, raw forensic material, or unsupported claims."
    - "D-08 through D-18: The complete provider state machine, ExUnit formatter contract, and owner-first setup diagnostic contract remain green without changing provider promotion semantics, setup ownership, browser proof, workflow topology, or artifacts."
  artifacts:
    - path: "scripts/ci/render_ci_baseline.mjs"
      provides: "Deterministic cohort-aligned staged critical-path percentile derivation and truthful confirmed/contrary rendering"
      exports: ["renderBaseline", "deriveStagedPathPercentiles"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Fixture and frozen-record gate that rejects absent, under-sampled, cross-cohort, incomplete-stage, or aggregate-only critical-path claims"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      provides: "Recollected privacy-safe frozen 90-day before-state from the repaired collector"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Byte-reproducible maintainer report with a valid cohort percentile conclusion"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Current Plan 06/07 task, assertion, threat, and sign-off ledger recorded only after the new gates pass"
  key_links:
    - from: "scripts/ci/render_ci_baseline.mjs"
      to: "run/job/cohort records in 226-CI-BASELINE.ndjson"
      via: "deriveStagedPathPercentiles joins jobs to qualifying run IDs inside one ready cohort and measures release start through the latest Playwright completion"
      pattern: "deriveStagedPathPercentiles|release-gate|host-integration|playwright-e2e"
    - from: "scripts/ci/verify_ci_baseline.mjs"
      to: "scripts/ci/render_ci_baseline.mjs"
      via: "20-run staged-path fixtures and --require-critical-path validate the same exported derivation used by Markdown"
      pattern: "deriveStagedPathPercentiles|require-critical-path"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      via: "fresh deterministic render plus byte comparison"
      pattern: "Cohort|Qualifying successes|p50|p95"
    - from: "Phase 226 full validation contract"
      to: "provider formatter and setup diagnostic evidence"
      via: "existing fixture, focused ExUnit, shell, and Phase 225 evidence commands run unchanged after recollection"
      pattern: "verify_provider_proof|live_proof_formatter_test|verify_ci_setup_diagnostics"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      to: "Plans 226-06 and 226-07 executable gates"
      via: "four task rows, recorded assertions, T-226-21 through T-226-29 coverage, and final sign-off are updated only after the cited commands pass"
      pattern: "226-06-01|226-07-02|require-critical-path|T-226-29"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No phase SPEC exists and the edge probe left BASE-01 unresolved/unclassified; this plan retains the assumption that cohort-aligned staged-path positive and negative controls plus live frozen-record validation cover the recollection boundary without dismissing the wider spec-less edge surface."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No phase SPEC exists and the edge probe left BASE-02 unresolved/unclassified; this plan retains the assumption that rerunning the exhaustive provider fixtures and real formatter contract is sufficient evidence that recollection did not alter proof semantics, without dismissing the wider spec-less edge surface."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "No phase SPEC exists and the edge probe left OWN-01 unresolved/unclassified; this plan retains the assumption that the seven-code setup contract, fixed owner mappings, canonical host delegation, and preserved Playwright checks cover the unchanged ownership boundary without dismissing the wider spec-less edge surface."
  prohibitions:
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branch names, logs, secret-presence details, provider payloads, artifact contents, user data, credentials, or sensitive runtime values in the checked-in baseline or generated Markdown."
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT mix event/topology fingerprints, count reruns as new samples, manufacture percentiles below 20 successful first attempts in 90 days, sum named jobs across runs, or call an aggregate named-path total a critical-path percentile."
    - requirement_id: BASE-02
      status: flagged-unverified
      statement: "MUST NOT promote a green, skipped, non_run, zero-selected, stale, Fake-backed, or differently-SHA run into live-provider proof for the current SHA."
    - requirement_id: OWN-01
      status: flagged-unverified
      statement: "MUST NOT blur ownership by reporting CI provisioning failures as host test failures, moving the host-owned Playwright proof contract into CI, removing duplicate setup, or changing the host/CI owner mapping during evidence recollection."
---

<objective>
Recollect the frozen privacy-safe snapshot after Plan 226-06, regenerate byte-reproducible Markdown, and close the remaining Phase 226 measurement gap with a valid cohort-aligned staged-path percentile conclusion while retaining BASE-02 and OWN-01 proof contracts (D-01 through D-21).

Purpose: Replace the hollow before-state with durable evidence that can truthfully confirm the roughly 33–36 minute release-gate → host-integration → Playwright critical path or record a measured contrary result, without weakening privacy, provider proof, or setup ownership.
Output: A deterministic staged-path percentile gate, recollected frozen NDJSON, regenerated Markdown, and a green complete Phase 226 contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-06-PLAN.md
@.planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-04-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-05-SUMMARY.md
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/verify_provider_proof.mjs
@scripts/ci/verify_ci_setup_diagnostics.sh

Planning-time `gh auth status` is green for the configured repository account, so no static credential checkpoint or invented credential is included. The live task still has a fail-closed authentication/read-access precondition: if it is no longer true, execution halts before writes and returns a blocking authentication checkpoint. BASE-01, BASE-02, and OWN-01 remain flagged-unverified assumptions because Phase 226 has no SPEC and the edge probe left the requirements unresolved/unclassified; every descriptor-less prohibition above remains binding and is never auto-dismissed.
</context>

## Artifacts this phase produces

- No new file path is created. Existing frozen artifacts `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson` and `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` are replaced only after a temporary recollection passes schema, privacy, cohort, staged-path, and byte-reproducibility gates.
- `scripts/ci/render_ci_baseline.mjs` gains the named export `deriveStagedPathPercentiles(records)`. It derives per-run release-gate → host-integration → latest Playwright-completion spans inside one qualifying cohort; it never sums parallel shards or jobs from different runs.
- `scripts/ci/verify_ci_baseline.mjs` gains CLI flag `--require-critical-path`, which rejects frozen evidence unless a ready cohort has exactly the locked target of 20 successful first attempts, 20 complete staged-path observations, non-null p50/p95, one explicit `confirmed` or `contrary_measured_result` conclusion, and byte-reproducible Markdown.
- No provider, formatter, setup-diagnostic, workflow, host, or Playwright symbol/path is created or changed.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one ready cohort into a real staged-path percentile conclusion (D-02 through D-05, D-19 through D-21)</name>
  <reversibility rating="reversible">The derivation and verifier flag are local readers of the existing schema-v1 evidence and can be reverted without migrating the frozen NDJSON contract.</reversibility>
  <files>scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_executable_uat_contract.mjs
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-06-PLAN.md
  </read_first>
  <behavior>
    - A deterministic ready cohort with 20 successful first-attempt full-CI runs and all three ordered stage classes yields 20 staged-path samples plus non-null p50/p95.
    - Each run's span starts at its release-gate start and ends at its latest Playwright shard completion after host-integration; parallel Playwright shards contribute one latest completion, never a sum.
    - A cohort p50 between 1,980,000 and 2,160,000 milliseconds renders `confirmed`; a valid p50 outside that range renders `contrary_measured_result` with the measured p50/p95, cohort fingerprint, and sample count.
    - Missing stages, stage-order violations, cross-cohort joins, fewer than 20 observations, rerun inflation, or an aggregate-only named-path value fail the required critical-path gate.
  </behavior>
  <action>Start RED by extending the existing baseline fixture gate with 20 full-CI run/job record groups that share one cohort fingerprint and contain successful ordered `release-gate`, `host-integration`, and multiple `playwright-e2e` shards. Retain the `scripts/ci/verify_executable_uat_contract.mjs` pattern: build one valid deterministic fixture, mutate one invariant for each negative control, fail at the narrow validator boundary with precise messages, and emit one explicit PASS summary. Add negative mutations for a missing host stage, Playwright completing before host, a second fingerprint, a rerun, 19 observations, and parallel-shard summation. Implement exported `deriveStagedPathPercentiles(records)` in the renderer: select only the latest 20 successful first-attempt run identities belonging to one `ready` cohort, join jobs by `run_id`, require every stage and temporal ordering, measure release start through the maximum Playwright completion for each run, and calculate p50/p95 from those 20 spans. Replace the current all-record named-job aggregation with the cohort-aligned result and render literal cohort ID, sample count, staged-path p50/p95, hypothesis range, and either `confirmed` or `contrary_measured_result`; deterministic rendering means the same validated records must produce exactly the same bytes. Preserve the frozen Markdown grammar mapped to `.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md`: lead with what happened and literal state, then owner, exact next command, and immutable evidence links, while raw forensic material remains in Actions artifacts. Extend the verifier with `--require-critical-path` so records/rendered validation fails unless that same derivation is valid and the Markdown is byte-exact. Preserve the existing schema-v1 NDJSON shape, queue-versus-DAG semantics, escaping, and all Plan 226-06 non_run/timestamp regressions.</action>
  <acceptance_criteria>
    - The positive 20-run fixture produces exactly 20 staged-path observations; p50/p95 are derived from per-run spans rather than an all-record job sum.
    - The 33–36 minute fixture renders `confirmed`, and an otherwise valid out-of-range fixture renders `contrary_measured_result` with exact measured p50/p95.
    - Each missing-stage, order, fingerprint, rerun, under-sample, and parallel-summation negative control is rejected with a specific assertion.
    - `--require-critical-path` cannot pass on the pre-recollection hollow snapshot, a ready workflow-duration cohort without 20 complete staged paths, or Markdown that merely reports a named-path total.
    - All Plan 226-06 cohort and calendar-timestamp regressions, privacy rejections, and deterministic rendering checks remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>The baseline renderer and verifier can make and enforce one truthful cohort-percentile staged-path conclusion; aggregate named-path work can no longer satisfy the gap.</done>
</task>

<task type="auto">
  <name>Task 2: Recollect, freeze, byte-compare, and rerun the complete Phase 226 contract (D-01 through D-21)</name>
  <precondition>`gh auth status` succeeds for github.com and `gh api /repos/szTheory/accrue/actions/workflows/ci.yml --jq '.id'` confirms read access without printing credentials, and the live 90-day query can supply one complete exact 20-run cohort; plan ordering cannot guarantee these external facts.</precondition>
  <checkpoint_protocol gate="blocking-human">Before any write, classify a failed precondition as `github_auth_unavailable`, `github_actions_api_unavailable`, or `live_cohort_incomplete`. Halt with both checked-in artifacts and `226-VALIDATION.md` untouched; report the exact failing command/API status or the observed cohort fingerprint, successful-first-attempt count, and complete-staged-span count. For authentication/read access, instruct the maintainer to restore `gh` repository Actions-read access and resume with `$gsd-execute-phase 226`. For API availability, require a successful retry of the exact read-only API probe before the same resume command. For cohort insufficiency, require new qualifying first-attempt workflow history until one exact fingerprint reaches 20 successes and 20 complete staged spans, then resume with `$gsd-execute-phase 226`. Do not invent credentials, fabricate observations, broaden fingerprints, replace evidence, or auto-continue after the external state changes; maintainer continuation is required.</checkpoint_protocol>
  <reversibility rating="reversible">The two generated before-state artifacts are reproducible from read-only Actions metadata and can be restored by rerunning the prior collector revision.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_executable_uat_contract.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/verify_phase225_required_lane_evidence.sh
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - accrue/test/accrue/live_proof_formatter_test.exs
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-06-PLAN.md
  </read_first>
  <action>First run the authentication/read-access checks from the precondition without displaying token material. If authentication, API availability, or live cohort completeness fails, execute the blocking checkpoint protocol exactly and stop before any write. Create task-specific temporary NDJSON and two temporary Markdown files under the phase directory, install an EXIT cleanup trap, and run `node scripts/ci/collect_ci_baseline.mjs --repo szTheory/accrue --workflow ci.yml --window-days 90 --sample-size 20 --out &lt;temporary-ndjson&gt;`. Validate the temporary records with the existing schema/privacy gate plus `--require-critical-path`; preserve the `scripts/ci/verify_executable_uat_contract.mjs` failure boundary by rejecting the first invalid invariant with a precise diagnostic and never publishing partial output. Render twice from the same temporary NDJSON, byte-compare the two renders, and validate one render against the records. Accept only evidence with a ready cohort of exactly 20 successful first attempts and 20 complete staged spans whose conclusion is either the measured 33–36 minute confirmation or an explicit measured contrary result. Preserve the action-first `.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md` grammar in the final Markdown: what happened/literal state first, followed by owner, exact next command, and immutable evidence links; retain raw facts outside the durable report. After all temporary gates pass, atomically replace the frozen NDJSON and Markdown, then rerun their record/render/critical-path gate. Finally run the complete Phase 226 contract: baseline fixtures and frozen artifacts, provider fixtures, focused `Accrue.Test.LiveProofFormatter` ExUnit test, setup diagnostics, and Phase 225 required-lane evidence. Only after every gate passes, update `226-VALIDATION.md`: retain `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true`; add green rows `226-06-01`, `226-06-02`, `226-07-01`, and `226-07-02` with their exact automated commands and threat references T-226-21 through T-226-29; update the full-suite command to include `--require-critical-path` and the formatter test; add recorded assertions for non_run cohort eligibility, impossible-calendar rejection, and the cohort-aligned staged-path conclusion; and replace the ten-task sign-off with a fourteen-task sign-off dated only after these commands pass. These latter commands are verification only: preserve provider files/states, the formatter contract, setup owner mappings, host delegation, duplicate provisioning, Playwright workers/retries/artifacts, and workflow identity unchanged.</action>
  <acceptance_criteria>
    - The committed NDJSON comes from the explicit read-only `szTheory/accrue` `ci.yml` 90-day/20-sample command and passes schema/privacy validation before and after replacement.
    - At least one exact full-CI cohort has `sample_status: ready`, `sample_count: 20`, 20 valid staged-path observations, non-null staged-path p50/p95, and immutable run/job links.
    - The Markdown names the cohort fingerprint/count and reports either `confirmed` for a 1,980–2,160 second p50 or `contrary_measured_result` with exact out-of-range p50/p95; no aggregate-only named-path total is accepted.
    - Two independent renders from the frozen NDJSON are byte-identical and one is byte-identical to the committed Markdown.
    - Provider fixtures and the real formatter test pass without live Stripe credentials; setup diagnostics and Phase 225 evidence pass without a browser or ownership change.
    - `226-VALIDATION.md` has green Plan 06/07 task rows, exact automated commands, T-226-21 through T-226-29 references, three new recorded assertions, the critical-path full-suite gate, and a fourteen-task dated sign-off recorded only after all gates pass.
    - Missing GitHub authentication/read access, unavailable Actions API state, or an insufficient/incomplete live cohort leaves both checked-in frozen artifacts and `226-VALIDATION.md` untouched and surfaces the classified blocking checkpoint with exact resume criteria rather than fabricated evidence.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; rg -q '^status: complete$' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -q '226-06-01' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -q '226-06-02' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -q '226-07-01' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -q '226-07-02' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -q 'T-226-21.*T-226-29|T-226-29' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -q -- '--require-critical-path' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md &amp;&amp; rg -qi 'fourteen tasks|all 14 tasks' .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</automated>
  </verify>
  <done>The frozen privacy-safe before-state and byte-reproducible report carry a valid cohort percentile conclusion, and every unchanged Phase 226 provider, formatter, setup, and inherited required-lane contract is green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated GitHub Actions API → temporary recollection | Untrusted repository metadata and external availability cross into a candidate durable evidence pack. |
| Frozen run/job/cohort records → staged-path derivation | Incorrect joins, ordering, cohort selection, or parallel-job arithmetic could create a false optimization claim. |
| Temporary evidence → checked-in NDJSON/Markdown | Sensitive values or invalid/incomplete measurements could become durable repository history. |
| Baseline-only changes → existing provider/setup contracts | Gap closure could accidentally overstate provider proof or blur host-versus-CI setup ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-25 | Information Disclosure | live API recollection and frozen artifacts | high | mitigate | Block replacement until strict schema/unknown-field/privacy fixtures and frozen-record validation pass; use only read-only `gh` calls, never print credential material, and retain the D-06 forbidden-data prohibition. |
| T-226-26 | Tampering | staged-path percentile derivation | high | mitigate | Block completion on 20-run cohort-aligned positive evidence plus missing-stage, ordering, fingerprint, rerun, under-sample, and parallel-shard-summation negative controls; use the same exported derivation for rendering and verification. |
| T-226-27 | Repudiation | confirmed/contrary critical-path claim | high | mitigate | Require immutable run/job URLs, exact cohort fingerprint/count, p50/p95, byte-reproducible Markdown, and the explicit live reproduction command before accepting either conclusion. |
| T-226-28 | Spoofing | provider and setup truth boundaries | high | mitigate | Rerun the exhaustive provider fixture state machine, real formatter contract, and seven-code setup/owner contract; block if any changes promote non-proof or alter ownership/delegation semantics. |
| T-226-29 | Denial of Service | GitHub API/auth/history availability | medium | mitigate | Bound collection to the locked 90-day workflow query and controlled pagination; on auth/API failure or insufficient history, preserve all prior artifacts, classify the exact external condition, and require maintainer continuation only after the stated auth/API/cohort resume criterion becomes true. |
</threat_model>

<verification>
Run the complete automated command in Task 2. Completion requires a ready 20-observation cohort with 20 cohort-aligned staged paths and a reproducible confirmed/contrary percentile claim, followed by green baseline fixtures/artifacts, provider state machine, formatter, setup diagnostics, inherited Phase 225 evidence, and the updated Plan 06/07 validation ledger. Authentication, API, or cohort insufficiency is a fail-closed stop before any write and requires maintainer continuation after the stated external resume criterion becomes true.
</verification>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe comparable-run account distinguishes actual critical path, setup ownership, and provider proof state | 226-07 | COVERED | Recollects durable evidence, enforces a cohort percentile, and reruns unchanged proof/ownership contracts. |
| REQ | BASE-01 | Durable privacy-safe comparable-run baseline and valid measured critical path | 226-07 | COVERED | Temporary collection, strict validation, atomic replacement, deterministic rendering, and staged-path percentile gate. |
| REQ | BASE-02 | Required/skipped/advisory provider evidence stays visibly distinct | 226-07 | COVERED | Existing exhaustive state machine and real formatter contract rerun unchanged; prohibition retained. |
| REQ | OWN-01 | Host maintainer can identify setup ownership and diagnostics | 226-07 | COVERED | Existing seven-code owner-first contract rerun unchanged; flagged assumption and ownership prohibition retained. |
| RESEARCH | — | Dependency-free Actions collection, fingerprinted 20-run/90-day cohort, queue/DAG separation, deterministic Markdown | 226-07 | COVERED | Uses existing Node/gh stack and adds only cohort-aligned staged-path derivation/verification. |
| RESEARCH | — | Privacy allowlist, read-only API use, provider and setup truth boundaries | 226-07 | COVERED | Temporary fail-closed collection and full unchanged contract gate mitigate every high threat. |
| CONTEXT | D-01 through D-07 | Versioned evidence, comparable cohort, exclusions, timing, privacy, validation | 226-07 | COVERED | Recollection and required critical-path gate preserve all locked semantics. |
| CONTEXT | D-08 through D-13 | Independent provider policy/proof/freshness and no false promotion | 226-07 | COVERED | Verification-only rerun; no provider implementation file changes. |
| CONTEXT | D-14 through D-18 | Host/CI ownership, diagnostics, setup facts, Playwright proof preservation | 226-07 | COVERED | Verification-only rerun; no setup/host/workflow implementation file changes. |
| CONTEXT | D-19 through D-21 | Action-first, accessible, maintainable evidence interface | 226-07 | COVERED | Literal cohort/count/p50/p95 and confirmed/contrary copy replaces the invalid aggregate claim. |

Deferred Phase 227 optimization, cache/topology/matrix/branch-protection changes, browser-work removal, test deletion/retry masking, external observability, StoreKit/iPhone/Crosswake work, and Admin UI ratchet work remain excluded and are not source-audit gaps.

<success_criteria>
- The repaired collector's frozen output is privacy-safe, schema-valid, contains a ready exact cohort, and renders byte-identically.
- Critical-path truth comes from 20 per-run staged spans inside one cohort and reports exact p50/p95 as either the 33–36 minute confirmation or an explicit contrary measured result; aggregate named-path totals cannot pass.
- BASE-01, BASE-02, and OWN-01 remain visible flagged-unverified spec-less assumptions, and all four descriptor-less safety prohibitions remain binding.
- The provider state machine, real formatter, setup diagnostics, host/CI ownership boundary, Playwright proof semantics, and Phase 225 evidence all remain green.
- The validation ledger records all fourteen tasks, the Plan 06/07 assertions and threats, and a dated sign-off only after the new fixture, frozen critical-path, and full-contract gates pass.
- Every ASVS L1 high-severity threat is mitigated by a blocking deterministic or frozen-record gate.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-07-SUMMARY.md` when done.
</output>

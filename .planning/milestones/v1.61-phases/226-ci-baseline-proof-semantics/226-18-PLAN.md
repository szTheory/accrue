---
phase: 226-ci-baseline-proof-semantics
plan: "18"
type: execute
wave: 12
depends_on: ["226-17"]
files_modified:
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-01 through D-07: The canonical 90-day evidence is recollected only through the revision-bound, exact-identity collector and contains at least 20 unique successful first-attempt complete staged paths or truthfully records the contrary measured result."
    - "D-03/D-05: The frozen report byte-proves the actual release → host integration → latest Playwright path, separates runner queue from DAG wait, and exposes workflow-fingerprint sensitivity without trusting unknown topology."
    - "D-08 through D-13: Provider classifier, summary, manifest formatter, SHA/freshness, and no-promotion controls remain green against the final frozen evidence."
    - "D-14 through D-21: All setup diagnostics including initial Postgres readiness, canonical host/Playwright proof, action-first rendering, privacy, and inherited required-lane evidence remain green."
    - statement: "BASE-01 flagged assumption: the spec-less baseline surface is accepted only through the complete fixture, live-record, critical-path, privacy, and byte-reproduction contract after the Plan 17 repairs."
      verification: backstop
    - statement: "BASE-02 flagged assumption: exhaustive provider fixture and formatter preservation remains the available spec-less backstop."
      verification: backstop
    - statement: "OWN-01 flagged assumption: the complete setup suite including the new initial-readiness regression remains the available spec-less backstop."
      verification: backstop
  artifacts:
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      provides: "Fresh canonical privacy-safe 90-day records from audited workflow revisions and exact identities"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Byte-reproducible maintainer critical-path account rendered from the repaired records"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed Plan 17/18 gap-closure rows and final Nyquist sign-off"
  key_links:
    - from: "scripts/ci/collect_ci_baseline.mjs"
      to: "226-CI-BASELINE.ndjson"
      via: "authenticated read-only 90-day collection after exact revision/identity fixture gates"
      pattern: "--repo szTheory/accrue|--workflow ci.yml|--window-days 90"
    - from: "226-CI-BASELINE.ndjson"
      to: "226-CI-BASELINE.md"
      via: "two independent renders, byte comparison, record/render validation, and critical-path gate"
      pattern: "--require-critical-path|cmp"
    - from: "226-VALIDATION.md"
      to: "BASE-01, BASE-02, OWN-01"
      via: "final baseline/provider/formatter/setup/required-lane command ledger"
      pattern: "226-17-01|226-17-02|226-18-01"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists; the edge probe returned BASE-01 unresolved/unclassified, so the final live and deterministic gates remain explicit backstops."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists; the edge probe returned BASE-02 unresolved/unclassified, so exhaustive preservation checks remain explicit backstops."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists; the edge probe returned OWN-01 unresolved/unclassified, so the complete setup suite remains an explicit backstop."
  prohibitions:
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branches, workflow contents, runner names/labels, logs, secrets or secret-presence facts, provider payloads, artifact contents, credentials, or user data."
    - requirement_id: BASE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT publish pre-repair, partially collected, unknown-topology, unaudited-revision, or spoofed-identity records as canonical comparable evidence."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT promote non_run, skipped, stale, Fake-backed, zero-selected, differently-SHA, invalid-manifest, or unsuccessful evidence into live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT omit an owner-first setup failure or change host/CI ownership, duplicate provisioning, Playwright workers/retries/artifacts, required-check identity, or test strength."
---

<objective>
Recollect and byte-prove the Phase 226 canonical baseline after Plan 17 closes every admission and readiness regression (D-01 through D-21).

Purpose: Replace reproducible-but-untrustworthy evidence with a frozen account whose workflow provenance, identities, critical path, provider state, and setup ownership are all supported by the repaired production paths.
Output: Fresh NDJSON, byte-identical Markdown, and a final validation ledger covering Plans 17 and 18.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-17-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/verify_provider_proof.mjs
@scripts/ci/verify_ci_setup_diagnostics.sh

Spec-less fallback: all three requirement assumptions and bespoke prohibitions remain descriptor-less and flagged-unverified. Canon-referral breadcrumbs, not minted rows: generic injection, generic secret leakage, and Actions permission controls remain assigned to `$gsd-secure-phase` and static/lint verification.

API coverage is preservation-only. `COVERAGE.md` remains the reasoned declaration for the existing read-only Actions integration and explicit mutation/raw-forensics opt-outs; this plan adds no external capability and must not invent one.

Decision coverage: D-01, D-02, D-03, D-04, D-05, D-06, and D-07 bind canonical comparable evidence and its recollection gates; D-08, D-09, D-10, D-11, D-12, and D-13 bind the provider preservation checks; D-14, D-15, D-16, D-17, and D-18 bind setup ownership and Playwright preservation; D-19, D-20, and D-21 bind the final maintainer-facing rendering and evidence design.
</context>

## Artifacts this phase produces

- A freshly collected `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson` produced from immutable workflow revisions and exact/narrow job identities.
- A deterministic `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` rendered twice and byte-compared against the same repaired records.
- An updated `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` with `226-17-01`, `226-17-02`, and `226-18-01` executed rows, truthful task totals, zero behavior-unverified rows, and the complete final command.

<tasks>

<task type="tracer">
  <name>Task 1: Trace audited live Actions evidence into the final byte-proved baseline (D-01 through D-21)</name>
  <reversibility rating="reversible">The canonical files are reproducible outputs of read-only collection; failed gates retain the prior files and no external state is mutated.</reversibility>
  <precondition>`gh auth status` succeeds with read-only Actions/workflow-content access, Plan 17 fixture commands pass, and a temporary repaired 90-day collection contains at least 20 unique successful first-attempt complete staged paths.</precondition>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-17-SUMMARY.md
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/verify_phase225_required_lane_evidence.sh
    - accrue/test/accrue/live_proof_formatter_test.exs
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
  </read_first>
  <action>Assert the precondition with read-only checks and stop without modifying canonical evidence if authentication, workflow-content access, Plan 17 controls, or sample completeness fails. Create task-specific temporary NDJSON and two temporary Markdown files under the phase directory and install cleanup for every exit. Run the canonical collector for repository `szTheory/accrue`, workflow `ci.yml`, 90 days, and sample target 20. Require every historical exception-bearing run to contain a recognized immutable audited workflow revision, every retained job identity to resolve exactly or through a declared finite matrix member, and all selected paths to retain exact-attempt isolation.

Validate the temporary records with the fixture suite, strict schema/privacy checks, and `--require-critical-path`. Require at least 20 unique successful first-attempt complete staged paths, visible workflow-fingerprint strata, separate queue/DAG/setup/cache facts, immutable evidence links, and a report conclusion that either supports the roughly 33–36 minute staged release → host → latest-Playwright route or plainly records the contrary measurement. Render twice into independent files, compare them byte-for-byte, and validate a render against the same records. Atomically replace the canonical pair only after every temporary gate passes; never publish partial output or preserve a critical-path claim sourced from the pre-repair records.

Validate the committed pair again. Run the full baseline fixtures, exhaustive provider proof fixtures, real provider manifest formatter test, complete setup diagnostic suite including the initial-readiness failure, and inherited Phase 225 required-lane evidence. Confirm `COVERAGE.md` retains its existing integrated read-only capabilities and explicit opt-outs without a fabricated new capability. Only then update `226-VALIDATION.md`: add executed-green `226-17-01` (BASE-01, T-226-50 through T-226-52), `226-17-02` (OWN-01, T-226-53/T-226-54), and `226-18-01` (BASE-01/BASE-02/OWN-01, T-226-56 through T-226-59); update the total/sign-off to the actual executed row count; retain `nyquist_compliant: true`, `wave_0_complete: true`, and zero behavior-unverified rows; and replace stale approval prose with the actual final commands/results. Do not modify workflows, provider/setup implementation, required/advisory identity, caches, provisioning, matrix membership, branch protection, Playwright semantics, or tests outside the Phase 226 regression contracts.</action>
  <acceptance_criteria>
    - Temporary collection succeeds through the Plan 17 repaired collector with no unknown workflow revision/topology or unrecognized identity.
    - Temporary and committed NDJSON pass schema/privacy and `--require-critical-path` validation with at least 20 unique complete first-attempt paths.
    - Two independent renders and the committed Markdown are byte-identical for the committed NDJSON.
    - The critical-path statement is supported by fresh repaired measurements or replaced with an explicit contrary measured result.
    - Provider, formatter, setup/readiness, inherited required-lane, and COVERAGE declaration checks all pass without semantics drift.
    - Validation records all three new task rows, the actual final row count, zero behavior-unverified rows, and only commands executed in this plan.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; test -s .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md</automated>
  </verify>
  <done>The checked-in records and Markdown are fresh, privacy-safe, revision/identity trustworthy, byte-reproducible, and supported by the complete provider/setup/required-lane regression contract.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated Actions/workflow reads → temporary evidence | Remote metadata is untrusted until revision, identity, schema, privacy, and staged-path gates pass. |
| Temporary evidence → canonical checked-in files | A partial, stale, or adversarial collection must never replace the last frozen pair. |
| NDJSON → maintainer Markdown | Derived claims must be deterministic, byte-reproducible, and tied to the same validated records. |
| BASE-01 recollection → BASE-02/OWN-01 preservation | Final evidence work must not weaken provider proof or setup ownership behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-56 | Tampering | canonical NDJSON replacement | high | mitigate | Gate temporary collection on Plan 17 controls, schema/privacy, critical path, exact sample identity, and audited workflow provenance before atomic replacement. |
| T-226-57 | Spoofing | frozen cohort membership | high | mitigate | Require immutable workflow revision and exact/narrow identity resolution for every admitted job; reject unknown topology before selection. |
| T-226-58 | Information Disclosure | frozen records/report | high | mitigate | Re-run strict allowlists and privacy fixtures; persist only sanctioned fields and immutable links, never raw workflow/forensic/provider/identity data. |
| T-226-59 | Tampering | Markdown critical-path claim | high | mitigate | Render twice, byte-compare, validate against the same records, and require measured confirmation or literal contrary result. |
| T-226-60 | Repudiation | validation ledger | medium | mitigate | Record exact executed commands, task/threat/requirement mappings, actual row count, and immutable evidence links. |
| T-226-61 | Elevation of Privilege | read-only GitHub access | low | accept | Existing authenticated read-only access is sufficient; no dispatch, rerun, cancellation, secret, permission, or branch-protection mutation occurs. |

No package install or ORM schema path is involved, so neither a package-legitimacy checkpoint nor a schema-push task applies.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe account distinguishes actual critical path, setup ownership, and provider proof | 17, 18 | COVERED | Repairs precede fresh canonical evidence and full preservation proof. |
| REQ | BASE-01 | Comparable timing/cache/provider/root-failure baseline | 17, 18 | COVERED | Immutable workflow provenance and exact identities close the collector gaps; Plan 18 recollects. |
| REQ | BASE-02 | Required/skipped/advisory provider evidence differs | 17, 18 | COVERED | Exhaustive provider and formatter gates preserve the already-verified truth. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 17, 18 | COVERED | Initial readiness is fixed/tested, then the complete setup suite is rerun. |
| RESEARCH | — | Repository-owned dependency-free evidence system | 17, 18 | COVERED | Existing built-ins/read-only Actions surfaces only. |
| RESEARCH | — | Strict allowlist, typed proof, owner-first setup facts | 17, 18 | COVERED | Direct repairs and preservation regressions. |
| RESEARCH | — | Frozen NDJSON plus deterministic Markdown | 18 | COVERED | Temporary collection, dual render, atomic replacement, committed-pair gate. |
| RESEARCH | — | API coverage declaration and opt-outs | 18 | COVERED | Existing `COVERAGE.md` is preserved; no capability fabricated. |
| CONTEXT | D-01–D-07 | Canonical comparable evidence and validation | 17, 18 | COVERED | Revision/identity repair plus recollection. |
| CONTEXT | D-08–D-13 | Provider policy/proof/freshness | 17, 18 | COVERED | Full preservation gates. |
| CONTEXT | D-14–D-18 | Host/CI ownership, diagnostics, Playwright proof | 17, 18 | COVERED | Readiness fix plus full preservation gates. |
| CONTEXT | D-19–D-21 | Maintainer-first durable accessible interface | 18 | COVERED | Byte-reproduced existing renderer from trusted facts. |
| CONTEXT | Deferred ideas | CI optimization, duplicate setup removal, caching, topology/matrix/branch-protection/gate/test changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | Explicitly outside Phase 226 and absent from tasks. |

<verification>
Completion requires Plan 17's focused gates before any canonical write, authenticated read-only collection to temporary files, schema/privacy/critical-path validation, two byte-identical renders, atomic canonical replacement, committed-pair revalidation, and unchanged provider/formatter/setup/required-lane contracts.
</verification>

<success_criteria>
- The frozen baseline is recollected through immutable-revision and exact/narrow identity admission only.
- At least 20 unique successful first-attempt complete paths support the critical-path statement, or the report records the contrary measured result plainly.
- The Markdown is byte-reproducible from the validated NDJSON and remains privacy-safe and action-first.
- Provider proof and host/CI setup ownership—including initial Postgres readiness—are fully regression-verified.
- BASE-01, BASE-02, and OWN-01 assumptions/prohibitions stay explicit, descriptor-less, and flagged-unverified.
- No deferred optimization or workflow/proof weakening is introduced.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-18-SUMMARY.md` when done.
</output>

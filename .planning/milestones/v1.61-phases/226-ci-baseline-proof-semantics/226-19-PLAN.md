---
phase: 226-ci-baseline-proof-semantics
plan: "19"
type: execute
wave: 13
depends_on: ["226-18"]
files_modified:
  - accrue/test/support/live_proof_formatter.ex
  - accrue/test/accrue/live_proof_formatter_test.exs
  - guides/testing-live-stripe.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-08/D-09/D-10: ExUnit tests excluded by `mix test --only live_stripe` do not contribute to selected, passed, skipped, or failed manifest counts; one selected passing tagged test can therefore produce manifest-backed `proved` evidence."
    - "D-09/D-12: The formatter continues to emit only aggregate counts and timestamps, and provider finalization still requires a valid nonzero all-passing manifest rather than inferring proof from the raw job conclusion."
    - "D-10/D-13/D-19/D-20: Maintainer guidance states that scheduled and manual live-Stripe executions are required provider-proof runs when selected, while Fake-backed contributor proof remains separate and missing configuration, zero selection, or assertion failure cannot be described as proof."
    - "D-01 through D-07 and D-14 through D-21: The repaired provider seam leaves the frozen privacy-safe baseline, setup ownership, Playwright proof, workflow topology, API coverage declaration, and inherited required-lane evidence unchanged and green."
    - statement: "BASE-01 planner assumption: because no plain Phase 226 SPEC classifies this requirement, the unchanged canonical record/render fixture and critical-path gates are the explicit preservation backstop."
      verification: backstop
    - statement: "BASE-02 planner assumption: because no plain Phase 226 SPEC classifies this requirement, a real `mix test --only live_stripe` selection regression plus provider-finalizer acceptance is the executable proof boundary."
      verification: backstop
    - statement: "OWN-01 planner assumption: because no plain Phase 226 SPEC classifies this requirement, the complete setup diagnostic suite is the explicit preservation backstop."
      verification: backstop
  artifacts:
    - path: "accrue/test/support/live_proof_formatter.ex"
      provides: "ExUnit formatter whose aggregate manifest ignores excluded tests and counts only selected outcomes"
      exports: ["Accrue.Test.LiveProofFormatter"]
    - path: "accrue/test/accrue/live_proof_formatter_test.exs"
      provides: "Direct excluded-event regression and actual `mix test --only live_stripe` selection/finalization integration regression"
    - path: "guides/testing-live-stripe.md"
      provides: "Accurate required scheduled/manual provider-proof policy and failure semantics"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed Plan 19 task, threat, requirement, and final Phase 226 regression ledger"
  key_links:
    - from: "ExUnit `{:test_finished, %{state: {:excluded, reason}}}`"
      to: "accrue/test/support/live_proof_formatter.ex"
      via: "explicit excluded-event branch that preserves all aggregate counters"
      pattern: "excluded"
    - from: "accrue/test/accrue/live_proof_formatter_test.exs"
      to: "scripts/ci/provider_proof.mjs"
      via: "actual tagged-only Mix subprocess manifest followed by provider finalization"
      pattern: "--only|live_stripe|proved"
    - from: "guides/testing-live-stripe.md"
      to: ".github/workflows/ci.yml live-stripe job"
      via: "literal scheduled/manual required policy, misconfiguration/failure behavior, and always-run summary/artifact contract"
      pattern: "required|scheduled|manual|proof"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "Spec-less edge probe returned BASE-01 unresolved/unclassified; canonical baseline validation remains a preservation gate rather than newly inferred scope."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "Spec-less edge probe returned BASE-02 unresolved/unclassified; the authoritative verification failure and D-08 through D-13 define this gap plan's exact provider-proof closure."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "Spec-less edge probe returned OWN-01 unresolved/unclassified; setup ownership is unchanged and guarded by its complete contract suite."
  prohibitions:
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT change or republish the canonical baseline, collect raw forensic/provider data, or add a new Actions API capability."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT count excluded tests as selected outcomes or promote non-run, skipped, zero-selected, failed, invalid-manifest, Fake-backed, stale, or different-SHA evidence into live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT change host/CI setup ownership, duplicate provisioning, Playwright workers/retries/artifacts, or canonical host proof commands."
---

<objective>
Repair the final BASE-02 execution seam so an actual tagged live-provider suite can produce truthful manifest-backed proof, then reconcile the maintainer policy contract (D-08 through D-13 and D-19 through D-21).

Purpose: Close the verified blocker where ExUnit exclusions contaminate selected-test counts and force a valid live-provider execution into `misconfigured`.
Output: Correct formatter behavior, direct and integration-level regressions, accurate scheduled/manual policy documentation, and an updated validation ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-18-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
@accrue/test/support/live_proof_formatter.ex
@accrue/test/accrue/live_proof_formatter_test.exs
@accrue/test/test_helper.exs
@accrue/mix.exs
@scripts/ci/provider_proof.mjs
@scripts/ci/verify_provider_proof.mjs
@guides/testing-live-stripe.md
@.github/workflows/ci.yml

Spec-less fallback: BASE-01, BASE-02, and OWN-01 remain descriptor-less and explicitly flagged-unverified. The authoritative verification gap supplies the BASE-02 behavior to repair; existing complete regression suites remain the preservation evidence for BASE-01 and OWN-01.

API coverage is preservation-only. `COVERAGE.md` remains authoritative for the existing read-only GitHub Actions integration and explicit mutation/raw-forensics opt-outs; this plan neither calls nor adds an external API surface.

Decision coverage: D-08, D-09, D-10, D-12, and D-13 directly bind formatter selection, manifest promotion, literal states, and Fake-versus-live proof; D-11 remains protected by provider freshness fixtures; D-19, D-20, and D-21 bind the guide and validation language. D-01 through D-07 and D-14 through D-18 are preservation constraints verified by the full regression contract. Deferred Phase 227 optimization, cache/topology/matrix/branch-protection changes, test weakening, external vendors, StoreKit/iPhone/Crosswake, and Admin-ratchet work remain excluded.
</context>

## Artifacts this phase produces

- A corrected `Accrue.Test.LiveProofFormatter` outcome classifier that explicitly ignores ExUnit excluded events while retaining aggregate-only privacy semantics.
- Direct excluded-event and subprocess-level `mix test --only live_stripe` regressions in `accrue/test/accrue/live_proof_formatter_test.exs`, including manifest acceptance as `proved` by the production finalizer.
- Corrected `guides/testing-live-stripe.md` scheduled/manual policy wording that matches the required workflow finalizer and distinguishes it from contributor-facing Fake proof.
- Updated `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` rows `226-19-01` and `226-19-02`, with truthful task totals and final approval only after the complete Phase 226 contract passes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace actual tagged-only ExUnit selection into manifest-backed `proved` evidence (D-08 through D-13)</name>
  <reversibility rating="reversible">The change is an internal formatter branch plus tests; reverting it restores the prior behavior without migrating data or changing a published API.</reversibility>
  <files>accrue/test/support/live_proof_formatter.ex, accrue/test/accrue/live_proof_formatter_test.exs</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md
    - accrue/test/support/live_proof_formatter.ex
    - accrue/test/accrue/live_proof_formatter_test.exs
    - accrue/test/test_helper.exs
    - accrue/mix.exs
    - scripts/ci/provider_proof.mjs
    - scripts/ci/verify_provider_proof.mjs
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - "An ExUnit `{:excluded, reason}` result leaves selected_count, passed_count, skipped_count, and failed_count unchanged."
    - "A selected passing test increments selected_count and passed_count exactly once; selected skipped and failed states retain their current distinct aggregate counts."
    - "An actual Mix subprocess containing one `:live_stripe` passing test and at least one untagged excluded test, invoked with `--only live_stripe` and `ACCRUE_PROVIDER_MANIFEST`, emits selected_count=1, passed_count=1, skipped_count=0, and failed_count=0."
    - "The production provider finalizer accepts that real manifest with configured=true, required policy, successful raw conclusion, current SHA, and selected scheduled/manual trigger, emits proof_state `proved`, and exits successfully."
    - "The manifest and test diagnostics contain no test names, exclusion reasons, provider payloads, credential values, or environment values."
  </behavior>
  <action>Begin RED by extending the formatter test with a direct excluded-event assertion that fails against the current catch-all branch. Add a deterministic integration case that runs the real Mix selection path in a subprocess against a uniquely named, cleanup-protected fixture containing both a tagged passing case and an untagged case; keep the parent test non-async if shared build or filesystem coordination requires it, target only the generated fixture so the subprocess cannot recurse into the parent formatter suite, pass the manifest path through the child process environment, and clean both fixture and manifest on every exit. Assert exact aggregate counts and pass the resulting manifest through the production `provider_proof.mjs --finalize` CLI under a selected scheduled/manual required-policy scenario, requiring literal `proved` output and exit zero.

Then implement the smallest production correction in `LiveProofFormatter`: match `{:excluded, _reason}` before the catch-all outcome branch and return the state unchanged. Preserve selected skipped outcomes as selected/skipped, preserve genuine failure counting, keep atomic manifest replacement, and keep the state/manifest aggregate-only. Do not serialize event metadata, names, reasons, failure text, environment facts, or provider data. Do not change ExUnit global exclusions, the `test.live` alias, provider classifier rules, workflow triggers, job policy, or the always-run finalizer/summary/artifact path.</action>
  <acceptance_criteria>
    - The direct regression proves excluded events leave all four counters unchanged and fails against the pre-repair formatter.
    - The integration regression executes actual `mix test --only live_stripe` filtering with tagged and excluded cases rather than only calling formatter callbacks.
    - The integration manifest has exactly one selected passing result and zero skipped/failed results despite the excluded case.
    - Production finalization of the integration manifest returns `proved`; existing zero-selected, invalid-manifest, skipped, blocked, failed, stale, and different-SHA controls remain unchanged.
    - The manifest remains aggregate-only and atomically written; no provider access or credentials are required by the regression.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors &amp;&amp; cd .. &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures</automated>
  </verify>
  <done>A genuine tagged-only Mix execution ignores excluded tests, emits truthful selected outcomes, and traverses the production manifest finalizer to `proved` without weakening any negative proof-state control.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile required live-provider policy and close the full Phase 226 contract (D-01 through D-21)</name>
  <reversibility rating="reversible">The guide and validation ledger can be reverted locally; they document existing workflow behavior and introduce no external or persisted contract migration.</reversibility>
  <files>guides/testing-live-stripe.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
    - guides/testing-live-stripe.md
    - .github/workflows/ci.yml
    - scripts/ci/provider_proof.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/verify_phase225_required_lane_evidence.sh
  </read_first>
  <action>Correct the guide's manual-dispatch and scheduled-run sections to match the existing workflow: the live-Stripe job is selected only for scheduled and manual triggers, and when selected its provider policy is `required`; incomplete configuration or fixtures, zero selected tests, invalid manifest, or assertion failure prevents proof and fails the selected provider lane. Explain separately that deterministic Fake-backed tests remain the contributor-facing push/PR merge proof, that raw GitHub job status does not establish live proof, and that only a configured nonzero all-passing manifest-emitting live execution becomes `proved`. Describe the always-run literal summary and retained privacy-safe proof artifact as the diagnostic path. Remove the contradictory `continue-on-error`/advisory description without claiming the scheduled/manual provider lane is a push/PR branch-protection check or changing the workflow.

Run the repaired formatter tracer and exhaustive provider fixture suite, then the frozen baseline fixture/record/render critical-path gates, setup ownership diagnostics, and Phase 225 required-lane evidence. Confirm `COVERAGE.md` is unchanged and still declares only the existing read-only Actions capabilities and opt-outs. Only after all commands pass, update `226-VALIDATION.md` with executed-green rows `226-19-01` (BASE-02; T-226-62 through T-226-64) and `226-19-02` (BASE-01/BASE-02/OWN-01; T-226-65/T-226-66), update task totals and sign-off to the actual row count, retain `nyquist_compliant: true`, `wave_0_complete: true`, and zero behavior-unverified rows, and replace the prior approval with a truthful note that the real exclusion/selection/finalization seam and the complete preservation contract passed. Do not edit the canonical NDJSON/Markdown, workflow, provider classifier, setup scripts, required-check identity, caches, provisioning, matrix, branch protection, Playwright semantics, or tests beyond the formatter regression.</action>
  <acceptance_criteria>
    - The guide calls selected scheduled/manual live-provider execution required and no longer describes it as `continue-on-error` or advisory.
    - The guide distinguishes scheduled/manual provider policy from Fake-backed push/PR merge proof and describes exact misconfigured/failed/proved boundaries.
    - Formatter integration, provider fixtures, canonical baseline/critical-path, setup diagnostics, required-lane evidence, and API coverage declaration all pass without scope drift.
    - Validation records both Plan 19 rows, the actual final task count, zero behavior-unverified rows, and only commands/results executed during this plan.
    - No canonical baseline, workflow topology, external API coverage, setup ownership, Playwright proof, or deferred Phase 227 optimization is changed.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const text=fs.readFileSync("guides/testing-live-stripe.md","utf8");const section=(name)=&gt;{const lines=text.split(/\r?\n/);const start=lines.indexOf("## "+name);if(start&lt;0)throw new Error("missing section: "+name);let end=lines.length;for(let i=start+1;i&lt;lines.length;i++){if(lines[i].startsWith("## ")){end=i;break}}return lines.slice(start+1,end).join(" ").replace(/\s+/g," ").toLowerCase()};const need=(ok,msg)=&gt;{if(!ok)throw new Error(msg)};const policy=section("Phase 226 provider proof state");const manual=section("Running via GitHub Actions manual dispatch");const scheduled=section("Scheduled run");const row=(state)=&gt;{const match=policy.match(new RegExp("\\| `"+state+"` \\|([^\\n]*?)\\|"));need(match,"missing "+state+" row");return match[1].replace(/\s+/g," ")};for(const [name,scoped] of [["policy",policy],["manual dispatch",manual],["scheduled run",scheduled]])need(!/continue-on-error|\badvisory\b/.test(scoped),name+" contains advisory/continue-on-error wording");need(/scheduled/.test(policy)&amp;&amp;/manual/.test(policy)&amp;&amp;/policy:\s*required/.test(policy),"Phase 226 policy must bind scheduled and manual selections to policy: required");for(const [name,scoped] of [["manual dispatch",manual],["scheduled run",scheduled]])need(/selected|selection/.test(scoped)&amp;&amp;/policy:\s*required/.test(scoped),name+" must state selected policy: required behavior");const proved=row("proved");need(/configur(?:ed|ation)/.test(proved),"proved requires configuration");need(/non[- ]?zero|at least one/.test(proved),"proved requires nonzero selection");need(/all[- ]?pass(?:ing|ed)|all selected[^|]*pass/.test(proved),"proved requires all selected assertions to pass");need(/\bvalid\b/.test(proved)&amp;&amp;/\bredacted\b/.test(proved)&amp;&amp;/\bmanifest\b/.test(proved),"proved requires a valid redacted manifest");const misconfigured=row("misconfigured");need(/(?:missing|lack(?:ed|ing)?)[^|]*(?:configur|credential|fixture)/.test(misconfigured),"misconfigured requires missing configuration or fixtures");need(/\binvalid\b[^|]*\bmanifest\b|\bmanifest\b[^|]*\binvalid\b/.test(misconfigured),"misconfigured requires invalid manifest");need(/zero[^|]*(?:selected|selection)|(?:selected|selection)[^|]*zero|no tests selected/.test(misconfigured),"misconfigured requires zero selection");const failed=row("failed");need(/executed|\bran\b/.test(failed)&amp;&amp;/assertion/.test(failed)&amp;&amp;/fail(?:ed|ure)/.test(failed),"failed requires an executed assertion failure");need(/fake-backed[^.]*push\/pr[^.]*merge proof/i.test(text.replace(/\s+/g," ")),"missing Fake-backed push/PR merge-proof assertion")' &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; test -s .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md</automated>
  </verify>
  <done>The maintainer guide matches the required scheduled/manual provider-proof behavior, and the updated validation ledger proves BASE-02 closure while preserving BASE-01 and OWN-01.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| ExUnit event stream → aggregate provider manifest | Test events are untrusted classifications; excluded events must not impersonate selected failures, and details must not cross the privacy boundary. |
| Aggregate manifest → provider finalizer | A manifest may assert proof only after strict schema, nonzero selection, passing outcomes, selected trigger, configuration, and SHA checks. |
| Workflow behavior → maintainer guide | Documentation must not weaken or misstate the selected scheduled/manual policy or conflate Fake proof with live-provider proof. |
| BASE-02 repair → BASE-01/OWN-01 evidence | The narrow formatter/docs change must not alter canonical baseline or setup-ownership contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-62 | Tampering | formatter outcome classification | high | mitigate | Add an explicit excluded-event branch before the selected-outcome catch-all and prove exact counters through direct and real Mix selection tests. |
| T-226-63 | Spoofing | provider manifest promotion | high | mitigate | Feed the real tagged-only manifest through the production finalizer and retain all existing negative promotion fixtures. |
| T-226-64 | Information Disclosure | formatter integration regression and manifest | high | mitigate | Use aggregate counters/timestamps only, generate no provider traffic, avoid credential values and event details, and clean transient fixtures/manifests. |
| T-226-65 | Repudiation | live-provider maintainer policy | medium | mitigate | Bind guide language to literal workflow triggers, required policy, proof predicates, always-run summary, and retained proof artifact. |
| T-226-66 | Tampering | Phase 226 preservation evidence | medium | mitigate | Run complete baseline, provider, setup, and required-lane contracts before recording Plan 19 validation rows or final approval. |
| T-226-67 | Denial of Service | nested Mix integration test | low | accept | The subprocess targets one unique fixture, uses existing compiled dependencies, has cleanup on every exit, and runs only in the focused formatter contract. |

No package install or supported ORM schema path is modified, so neither a package-legitimacy checkpoint nor a schema-push task applies.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe evidence distinguishes critical path, setup ownership, and provider proof | 19 | COVERED | Repairs the only failed provider-proof seam and reruns the complete baseline/setup preservation contract. |
| REQ | BASE-01 | Durable privacy-safe comparable-run baseline | 19 | COVERED | Existing fixture and canonical record/render critical-path gates must remain green; no baseline files change. |
| REQ | BASE-02 | Required, skipped, and advisory provider evidence differs | 19 | COVERED | Exclusions no longer fabricate failures; actual tagged selection reaches manifest-backed proof; guide states selected required policy. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 19 | COVERED | Complete setup diagnostic suite is a mandatory unchanged preservation gate. |
| RESEARCH | — | Typed proof requires selection, configuration, nonzero passing tests, manifest, and independent raw conclusion | 19 | COVERED | Real Mix selection and production finalization exercise the complete predicate. |
| RESEARCH | — | Aggregate-only privacy-safe provider evidence | 19 | COVERED | Formatter remains counts/timestamps only; integration asserts no detailed event/provider data. |
| RESEARCH | — | Scheduled/manual missing prerequisites fail rather than prove | 19 | COVERED | Guide matches workflow and provider fixture states without changing topology. |
| RESEARCH | — | Existing deterministic, dependency-free validation stack | 19 | COVERED | ExUnit and existing Node/shell gates only; no dependency installation. |
| RESEARCH | — | Existing Actions API coverage declaration and opt-outs | 19 | COVERED | `COVERAGE.md` is preservation-only and no external capability is added. |
| CONTEXT | D-01–D-07 | Canonical privacy-safe comparable evidence | 19 | COVERED | Full baseline fixture/canonical gates preserve the completed truth; artifacts are untouched. |
| CONTEXT | D-08–D-13 | Independent policy/proof/freshness, real selected proof, literal summary, Fake/live boundary | 19 | COVERED | Formatter tracer, production finalizer, negative fixtures, and corrected policy guidance close the blocker. |
| CONTEXT | D-14–D-18 | Host/CI ownership, setup diagnostics, Playwright proof | 19 | COVERED | Complete setup suite preserves the completed ownership truth without implementation changes. |
| CONTEXT | D-19–D-21 | Action-first, truthful, accessible maintainer evidence | 19 | COVERED | Existing guide is corrected in measured literal language and validation records exact commands. |
| CONTEXT | Deferred ideas | CI optimization, duplicate setup removal, browser caching, topology/matrix/branch-protection/gate/test changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | Explicitly outside Phase 226 and absent from tasks. |

<verification>
Completion requires a direct excluded-event regression, a real tagged-only Mix subprocess manifest accepted by the production finalizer as `proved`, corrected required scheduled/manual policy wording, and the unchanged full baseline/provider/setup/required-lane contract.
</verification>

<success_criteria>
- ExUnit excluded events contribute zero to all provider manifest counters.
- Actual `mix test --only live_stripe` selection with one tagged pass and one excluded case yields one selected pass and production `proved` finalization.
- Scheduled/manual live-provider documentation matches the workflow's required selected policy while preserving Fake-backed contributor proof as a separate boundary.
- Canonical baseline, setup ownership, required-lane evidence, and `COVERAGE.md` remain unchanged and green.
- BASE-01, BASE-02, and OWN-01 spec-less assumptions remain explicit and no deferred optimization or workflow/proof weakening enters the plan.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-19-SUMMARY.md` when done.
</output>

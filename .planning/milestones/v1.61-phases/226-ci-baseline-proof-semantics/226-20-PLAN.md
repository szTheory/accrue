---
phase: 226-ci-baseline-proof-semantics
plan: "20"
type: gap_closure
wave: 14
depends_on: ["226-19"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-01/D-02/D-05/D-07: Every live historical run is classified with job identities, runner declarations, matrix aliases, and prerequisite edges parsed from the immutable `ci.yml` bytes fetched at that run's head SHA; current-workflow topology cannot silently rewrite historical timing."
    - "D-01/D-05/D-06/D-07/D-19/D-20: Every persisted snapshot, run, job, and cohort record passes full per-kind semantic validation before rendering, and only immutable allowlisted GitHub Actions run/job URLs can become Markdown evidence links."
    - "D-03/D-04/D-05: The frozen baseline is recollected through authenticated read-only Actions access after both repairs and renders byte-identically on independent passes: when at least twenty complete historical paths qualify, the latest twenty publish percentiles plus a confirmed-or-contrary critical-path result; when fewer qualify, the pair publishes the exact available count and literal `insufficient_sample` with no percentile or critical-path confirmation."
    - "D-08 through D-18: Provider proof, setup ownership, initial readiness diagnostics, and the existing single-worker/zero-retry Playwright proof remain unchanged and pass their complete regression suites."
    - "D-19/D-20/D-21: Forged persisted values cannot inject a Markdown heading, link destination, or false evidence claim into the maintainer report."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Head-SHA workflow-source contract binding and full semantic validation for durable baseline record kinds"
      exports: ["liveRuns", "validateRecord", "workflowRunnerImage", "collectBaseline"]
    - path: "scripts/ci/render_ci_baseline.mjs"
      provides: "Validated, injection-safe deterministic Markdown rendering and staged-path derivation"
      exports: ["renderBaseline", "deriveStagedPathPercentiles"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Historical-topology divergence and forged-NDJSON production-path regressions"
      exports: ["verifyFixtures"]
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      provides: "Recollected sanitized 90-day source evidence derived from actual per-attempt historical workflow contracts"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Byte-reproducible maintainer report whose D-03 outcome derives from the repaired NDJSON: a measured critical-path claim from the latest twenty when at least twenty paths qualify, or exact-count `insufficient_sample` evidence below twenty"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed Plan 20 historical-provenance, semantic-integrity, and preservation evidence rows"
  key_links:
    - from: "fetchWorkflowContent(repo, workflow, run.head_sha)"
      to: "workflowRunnerContracts(source)"
      via: "the exact fetched source bytes supply both workflow_revision and the contracts passed through eligibility, identity, runner, and prerequisite resolution"
      pattern: "loadWorkflow|workflowRunnerContracts|contracts"
    - from: "validateRecord(record)"
      to: "renderBaseline(records)"
      via: "per-kind URL, timestamp, identifier, numeric, arithmetic, and nested-object validation runs before any Markdown row or link is emitted"
      pattern: "validateRecord|immutableUrl|timestamp"
    - from: "scripts/ci/collect_ci_baseline.mjs --repo szTheory/accrue --workflow ci.yml"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      via: "authenticated read-only Actions collection into a temporary phase-local file followed by fixture, D-03 ready-or-insufficient outcome, dual-render, and byte-equality gates before replacement"
      pattern: "--repo|--workflow|--out"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      via: "two independent production renderer invocations that must be byte-identical and then match the committed canonical pair"
      pattern: "render_ci_baseline|cmp"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "Spec-less edge probe returned BASE-01 unresolved/unclassified; this plan resolves the authoritative verifier's historical-provenance and forged-evidence blockers with production-path regressions plus authenticated recollection rather than inferring any additional scope."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "Spec-less edge probe returned BASE-02 unresolved/unclassified; provider semantics are regression-preservation scope and remain executable through the full provider/formatter contract."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "Spec-less edge probe returned OWN-01 unresolved/unclassified; setup ownership is regression-preservation scope and remains executable through the complete owner-first diagnostic contract."
  prohibitions:
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT persist actors, raw branch names, logs, secret presence or values, provider payloads, artifact contents, user data, or non-allowlisted evidence links in the canonical baseline."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT reinterpret a raw job conclusion or non-run, skipped, blocked, misconfigured, failed, stale, Fake-backed, or different-SHA evidence as live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT transfer the host-owned Playwright proof contract to CI or change setup ownership, workers, retries, artifacts, provisioning, or canonical host commands while repairing baseline evidence."
---

<objective>
Bind historical CI evidence to each run's immutable workflow topology, make persisted records and Markdown links semantically safe, then recollect and byte-verify the frozen D-03 baseline outcome (D-01 through D-21).

Purpose: Close the two remaining BASE-01 blockers and restore a trustworthy actual-historical critical-path conclusion from the latest twenty when at least twenty qualifying paths exist, or durable exact-count `insufficient_sample` evidence when fewer exist, without regressing the already-verified BASE-02 provider proof or OWN-01 setup diagnostics.
Output: Repaired collector/validator/renderer contracts, production-shaped historical and forged-record regressions, a freshly recollected canonical NDJSON/Markdown pair, and recorded Plan 20 validation evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-REVIEW.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-16-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-17-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-18-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-19-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
@.planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@.github/workflows/ci.yml

Authoritative scope: `226-VERIFICATION.md` leaves exactly two implementation blockers: current-workflow contracts are used for historical runs, and shallow persisted-record validation allows forged Markdown evidence. D-01 through D-07 and D-19 through D-21 bind this repair. D-08 through D-18, BASE-02, and OWN-01 are preservation contracts. The Phase 227 optimization, cache, topology, matrix, branch-protection, required-gate, retry, test, provider-policy, setup-ownership, and Playwright-semantics changes listed in deferred/out-of-scope sources remain excluded.
</context>

## Artifacts this phase produces

- Existing export `liveRuns` in `scripts/ci/collect_ci_baseline.mjs` now derives `workflow_revision` and per-run job contracts from the same immutable workflow source fetched at `head_sha`; no new collection flag or external API capability is introduced.
- Existing internal parser `workflowRunnerContracts(source)` is used with the per-run source and its contracts are threaded through `resolveWorkflowJobIdentity`, `workflowRunnerImage`, `workflowNeeds`, and timing-node prerequisite admission.
- Existing export `validateRecord` gains complete per-kind semantic checks for snapshot, run, job, and cohort records; existing export `renderBaseline` accepts only those validated records and safe immutable evidence destinations.
- Existing `liveDisplayIdentityControls` receives an inline historical-workflow regression fixture whose `host-integration` prerequisite and runner differ from current `.github/workflows/ci.yml`.
- Existing `verifyFixtures` receives a transient forged-NDJSON fixture that invokes the production renderer CLI and proves newline/heading/link injection, invalid URLs/timestamps/identifiers, and negative numeric facts fail before output.
- Existing CLI surfaces remain unchanged: `collect_ci_baseline.mjs --repo/--workflow/--window-days/--sample-size/--out`, `render_ci_baseline.mjs --input/--out`, and `verify_ci_baseline.mjs --fixtures` or `--records/--rendered/--require-critical-path`.
- Recollected `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson` and independently rendered `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` replace the prior pair only after all temporary-file gates pass.
- `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` gains executed rows `226-20-01` and `226-20-02`, truthful totals, and no completion claim before the authenticated canonical evidence and preservation suites pass.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace immutable historical contracts through safe rendered evidence (D-01, D-02, D-05, D-06, D-07, D-19)</name>
  <reversibility rating="reversible">The change threads already-fetched immutable source through internal collector helpers and can be reverted without changing the persisted schema or external CLI.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-REVIEW.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-16-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-17-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-18-SUMMARY.md
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_executable_uat_contract.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - "A synthetic historical `ci.yml` with the same `host-integration` job ID but a different display identity, a `release-gate` prerequisite instead of today's admin/docs prerequisites, and a `macos-15` runner is fetched for the run head SHA and accepted according to those historical declarations."
    - "That run's durable job record uses the historical stable identity, `github-hosted/macos-15` runner cohort input, and DAG wait measured from the historical release prerequisite; current `ci.yml` declarations cannot affect the result."
    - "The workflow revision digest and all identity, matrix-alias, runner, `needs`, completeness, and timing-node decisions for a run derive from one fetched source value."
    - "Unknown or ambiguous identities, unresolvable historical prerequisites, skipped/incomplete declared dependencies, spoofed suffixes, wrong run attempts, and future compatibility controls remain fail-closed."
    - "A forged run URL containing a newline and Markdown heading, a non-GitHub or mismatched run/job URL, an impossible timestamp, malformed SHA/identity/fingerprint, or a negative/non-integer duration/count is rejected before the production renderer writes output."
    - "Snapshot windows are valid and monotonic; run and job timestamps are canonical UTC and monotonic; recorded durations agree with timestamps; IDs, attempts, counts, percentile facts, reliability facts, setup costs, and cache metrics have their required positive/non-negative/null semantics."
    - "A persisted run URL matches its run ID, a persisted job URL matches both run and job IDs, and Markdown labels/destinations cannot terminate a link or create a heading."
    - "The production D-03 verifier accepts both truthful outcomes: at least twenty qualifying complete paths select the latest twenty and require percentiles plus a confirmed-or-contrary conclusion, while fewer require the exact qualifying count, literal `insufficient_sample`, null/omitted percentiles, and no critical-path confirmation."
  </behavior>
  <action>Keep this automated task tracer-shaped: start RED inside the existing `liveDisplayIdentityControls` production-path suite and carry one historical run end to end from fetched immutable workflow bytes through normalized records and safe rendered evidence. Follow the mapped `scripts/ci/verify_executable_uat_contract.mjs` conventions across the three baseline scripts: dependency-free Node ESM with built-in imports and small named helpers, deterministic fixtures/contracts with exact negative controls, and one top-level CLI error boundary that reports precise source-specific errors and exits nonzero without leaving partial output or allowing forged output. Keep the implementation grounded in the existing collector, renderer, and verifier surfaces; the executable-UAT script is an analog to follow, not a modification target. Inject a compact historical workflow source through `liveRuns(..., {fetchWorkflow})` whose `release-gate` and `host-integration` declarations intentionally disagree with current `.github/workflows/ci.yml`; provide matching attempt-scoped job metadata and assert the source callback receives the exact repository, workflow filename, and forty-character run head SHA. Require the emitted normalized host record and cohort inputs to reflect the historical display identity, runner class, sole prerequisite, and DAG timing. Include a negative historical source with an unresolved declared prerequisite or non-enumerated identity so the regression proves the historical parser remains fail-closed rather than falling back to current contracts.

Then change `liveRuns` to retain the exact string returned by `loadWorkflow(repo, workflow, run.head_sha)`, derive the workflow digest from those bytes, parse `workflowRunnerContracts(workflowSource)` once for that run, and pass that same contracts collection through `resolveWorkflowJobIdentity`, complete-prerequisite eligibility, `workflowRunnerImage`, and `workflowNeeds`. Cache only by immutable head SHA if deduplication is needed; never reuse current checkout contracts for a historical SHA. Keep `fetchWorkflowContent` on the existing read-only GitHub Contents endpoint declared in `COVERAGE.md`. Preserve exact-attempt job retrieval, closed matrix aliases, bounded compatibility handling, scheduled-provider separation, and current/future rejection rules.

Add the second RED path inside `verifyFixtures`: write a transient forged NDJSON file and invoke the production `render_ci_baseline.mjs --input/--out` CLI against it. Cover at least a link-closing newline/heading payload in `run_url`, a non-allowlisted or run/job-ID-mismatched URL, impossible or non-monotonic UTC timestamps, invalid SHA/stable identity/fingerprint values, and negative or non-integer duration/count/setup/cache/reliability values. Require nonzero exit and no forged heading or evidence link in any output. Retain valid fixture rendering and exact error context.

Strengthen existing `validateRecord` instead of adding a parallel renderer-only validator. For `snapshot`, enforce canonical monotonic timestamps, bounded repository/workflow identifiers, and a positive sample target. For `run`, enforce positive IDs/attempts, twelve-hex SHA, closed event/branch/fingerprint/state enums, immutable URL whose numeric ID matches `run_id`, canonical monotonic timestamps, and non-negative duration arithmetic consistent with those timestamps. For `job`, enforce positive matching run/job IDs and immutable URL, bounded normalized identities and matrix fingerprint, canonical monotonic timestamps, exact duration arithmetic, nullable non-negative queue/DAG semantics, normalized failure signature, and allowlisted non-negative setup/cache facts. For `cohort`, enforce its fingerprint, non-negative sample/reliability counts, ready-versus-insufficient percentile/null invariants, and bounded normalized root-incident identities/signatures. Make the existing renderer's Markdown escaping cover labels, and allow link destinations only after strict immutable URL validation; no arbitrary URL text reaches interpolation.

Preserve the existing `--require-critical-path` CLI surface but repair its D-03 outcome semantics. Derive the latest available qualifying complete paths up to the target of twenty. When at least twenty qualify, select the latest twenty and require p50/p95 plus the literal confirmed-or-contrary result. When fewer qualify, render the exact count and literal `insufficient_sample`, keep p50/p95 null or omitted, omit fingerprint percentile/sensitivity claims, and emit no confirmed-or-contrary critical-path statement. Extend the production-path fixtures with an insufficient collection so this verifier mode proves both branches and never treats honest scarcity as collection failure. Malformed records, unsafe links, inconsistent path topology/arithmetic, collection/render errors, and other semantic failures remain rejection cases. Do not edit `.github/workflows/ci.yml`, job IDs, runners, matrix, DAG, caches, triggers, permissions, or provider/setup paths.</action>
  <acceptance_criteria>
    - A regression with historical topology and runner declarations that differ from current `ci.yml` fails before the repair and passes only when the fetched source supplies all per-run contracts.
    - `liveRuns` fetches immutable workflow content at each unique run head SHA and derives the revision digest and contracts from the same bytes.
    - Historical identity, runner, matrix alias, prerequisite, completeness, and DAG-wait calculations all receive the per-run contracts explicitly.
    - Existing attempt isolation, exact identity, compatibility bounds, incomplete-dependency exclusion, spoofed-suffix rejection, and future-negative fixtures remain green.
    - The production renderer CLI rejects forged NDJSON before output; newline/heading and link-destination injection are impossible through every persisted URL field.
    - `validateRecord` enforces semantic and arithmetic invariants for every field in all four schema-v1 record kinds, including nested setup/cache/reliability facts.
    - The unchanged `--require-critical-path` CLI validates a latest-twenty percentile/confirmed-or-contrary result when at least twenty paths qualify, or a below-twenty exact-count `insufficient_sample` result with no percentile or critical-path confirmation.
    - No workflow topology, provider policy, setup ownership, external API coverage, or canonical evidence file changes in this task.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>One production-shaped historical run traverses fetched head-SHA workflow bytes through identity, runner, prerequisite, timing, semantic record validation, and injection-safe rendering without consulting current-workflow contracts or admitting forged evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Recollect the actual historical baseline and preserve BASE-02/OWN-01 (D-03 through D-21)</name>
  <precondition>`gh auth status` succeeds for an account with read access to `szTheory/accrue` Actions and Contents metadata; if not, stop before any canonical evidence write and surface the authentication checkpoint.</precondition>
  <reversibility rating="costly">The validator and renderer changes are local, but replacing the frozen before-state affects the evidence Phase 227 consumes and must be coordinated with its byte-verified canonical pair.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-REVIEW.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/verify_phase225_required_lane_evidence.sh
  </read_first>
  <behavior>
    - "The authenticated 90-day recollection selects the latest available unique successful first-attempt complete release → host-integration → Playwright paths, up to twenty, whose identities, runners, and prerequisite edges came from each attempt's fetched historical workflow source."
    - "A valid collection with at least twenty qualifying paths selects the latest twenty and publishes percentiles plus a literal confirmed-or-contrary critical-path result; a valid collection with fewer publishes the exact qualifying count and literal `insufficient_sample`, with no percentile or critical-path confirmation."
    - "Two independent renders of the temporary validated NDJSON are byte-identical before the canonical pair is replaced; the installed pair then passes byte reproduction and the applicable D-03 outcome gate."
    - "Provider proof, formatter selection, setup ownership/readiness diagnostics, required-lane evidence, and `COVERAGE.md` remain unchanged and green."
  </behavior>
  <action>After Task 1's focused fixtures pass, confirm `gh auth status`, then create a private temporary transaction directory inside the Phase 226 directory so staging, backups, and canonical destinations share one filesystem. Before collection, copy both current canonical files into distinct backup paths in that directory, require each backup to compare byte-equal with its source, and retain both backups until the entire installed-pair transaction commits.

Collect a fresh 90-day `ci.yml` snapshot with the existing `--repo szTheory/accrue --workflow ci.yml --window-days 90 --sample-size 20` CLI into a staged NDJSON path. Validate that staged NDJSON with the repaired `--require-critical-path` D-03 outcome gate, render it twice to two staged Markdown paths, require the renders to be byte-identical, and verify the staged NDJSON plus first staged render through the production verifier. Read the qualifying complete-path count from the validated staged evidence. If at least twenty qualify, require selection of the latest twenty, populated p50/p95 values, and the literal confirmed-or-contrary critical-path result. If fewer than twenty qualify, require the report to state that exact count and literal `insufficient_sample`, with null/omitted p50/p95, no fingerprint percentile/sensitivity claims, and no confirmed-or-contrary critical-path statement. Both are valid D-03 collection outcomes eligible for installation. Before any canonical replacement, run every provider/formatter/setup/required-lane preservation command and confirm `COVERAGE.md` still declares only the existing read-only run/job/artifact/workflow-content capabilities and opt-outs. Any invalid, unsafe, semantically inconsistent, or failed collection, staged validation, dual render, preservation, or coverage check must exit with both canonical files untouched.

After every pre-install gate passes, establish rollback handling before the first replacement: install EXIT, HUP, INT, and TERM handling that restores both canonical files from their verified backups unless an explicit transaction-committed flag has been set. Move the staged NDJSON and one verified staged Markdown render into their canonical destinations using same-filesystem renames; do not treat the first rename as success. With rollback still armed, run byte reproduction, the applicable ready-or-insufficient D-03 verification, and an independent render against the installed paths; require the independent render to match the installed Markdown byte-for-byte and the installed outcome/count to match the staged outcome/count, then rerun the preservation commands if any of them observes installed canonical content. On any post-install command failure, unsafe or semantically inconsistent installed result, or handled interruption, restore both originals from backup, verify the restored pair matches both backups, and exit nonzero. Only after every installed-pair gate succeeds may the executor set the committed flag, remove rollback handlers, and delete staged outputs and backups. A cleanup handler may remove temporary files after commit, but must never delete the only recoverable backups while rollback is armed.

Do not update validation evidence in this task; Task 3 records completion only after rollback protection has been released successfully. Record the recollected path count and applicable confirmed-or-contrary or `insufficient_sample` outcome from command output rather than copying prior values. Do not download logs or artifact contents, dispatch/rerun/cancel Actions, expose credentials, change workflow behavior, or retain temporary evidence files.</action>
  <acceptance_criteria>
    - A fresh authenticated read-only collection uses each run's immutable workflow source and transactionally installs either a validated latest-twenty percentile/confirmed-or-contrary result when at least twenty paths qualify, or a validated below-twenty exact-count `insufficient_sample` result with no percentile or critical-path confirmation.
    - The two temporary renders, the installed render, and an independent regeneration are byte-identical.
    - Canonical NDJSON/Markdown replacement occurs as a same-filesystem paired transaction only after staged validation, dual rendering, preservation gates, and verified backups of both originals.
    - Rollback handling is armed before the first canonical rename; every post-install failure or handled interruption restores and verifies both original files, preventing a mixed or unverified pair.
    - Transaction success is declared and backups are removed only after the installed pair passes the applicable ready-or-insufficient D-03 outcome check, independent-render byte-equality, staged-versus-installed outcome/count equality, and preservation gates.
    - Invalid, unsafe, semantically inconsistent, or failed collection/render/preservation results never install, or restore and byte-verify both originals if failure occurs after the first rename.
    - Provider proof, real tagged-only formatter finalization, setup ownership/readiness, Playwright semantics, required-lane evidence, and API coverage all remain green and unchanged.
  </acceptance_criteria>
  <verify>
    <automated>gh auth status &amp;&amp; node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; test -s .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md</automated>
  </verify>
  <done>The frozen baseline is semantically safe, freshly derived from actual per-attempt historical workflow contracts, byte-reproducible, and durably records either the latest-twenty measured result when at least twenty paths qualify or the exact-count `insufficient_sample` result below twenty while preserving the unchanged provider/setup proof contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Record automated frozen-baseline completion evidence (D-01, D-03, D-19, D-20)</name>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
  </read_first>
  <action>After Task 2 has released rollback protection and removed its transaction backups, rerun the installed canonical ready-or-insufficient D-03 verifier plus the provider, real formatter, setup/readiness, and required-lane preservation commands. Extract the exact qualifying complete-path count and literal outcome from the verified canonical report and command output; do not infer or copy either value from the prior pair. When at least twenty qualify, record that the latest twenty were selected, their p50/p95, and the literal confirmed-or-contrary critical-path result. When fewer than twenty qualify, record literal `insufficient_sample` and affirm that no percentile or critical-path confirmation was emitted. Update `226-VALIDATION.md` with truthful executed-green rows `226-20-01` for the historical-contract and safe-render end-to-end slice and `226-20-02` for the authenticated paired recollection and preservation transaction. Each row must name the exact command, observed result, canonical artifact, and immutable or repository-local evidence location needed to reproduce the claim. Update totals to the actual row count while retaining `nyquist_compliant: true`, `wave_0_complete: true`, and zero behavior-unverified rows. Add a concise maintainer-facing completion note that identifies the canonical NDJSON/Markdown pair as Phase 227's frozen input and reports the exact qualifying count, applicable measured or insufficient outcome, installed byte-verification result, and provider/setup preservation results; this note reports automated evidence and does not request or imply human approval. If any command, count, outcome, percentile-presence/absence rule, link-safety assertion, or validation total disagrees, leave the plan incomplete and record the failing command rather than claiming success.</action>
  <acceptance_criteria>
    - `226-VALIDATION.md` records only commands actually executed, the authenticated exact qualifying count, the applicable literal confirmed-or-contrary or `insufficient_sample` outcome, both Plan 20 rows, truthful totals, and zero behavior-unverified rows.
    - The completion note names the canonical NDJSON/Markdown pair as Phase 227's frozen input and reports automated byte, provenance, provider, and setup evidence without an approval gate.
    - Any mismatch in the canonical verifier, preservation suites, observed count/outcome, percentile-presence/absence rule, or validation totals fails the task and cannot be reported as completion.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; node -e "const fs=require('node:fs');const s=fs.readFileSync('.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md','utf8');for(const id of ['226-20-01','226-20-02'])if(!s.includes(id))process.exit(1)"</automated>
  </verify>
  <done>Automated validation evidence records the authenticated, transactionally installed, byte-verified canonical pair and its exact-count ready-or-`insufficient_sample` D-03 outcome as Phase 227's frozen before-state with no human approval dependency.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Contents at run head SHA → workflow contract parser | Historical workflow bytes are external evidence; identities, runners, aliases, and prerequisites must come from the same immutable source whose digest is recorded. |
| Actions run/job metadata → normalized NDJSON | API-shaped identifiers, timestamps, URLs, counts, conclusions, and nested facts are untrusted until normalized and semantically validated. |
| Canonical NDJSON → Markdown evidence | Persisted records can attempt heading/link injection or false timing claims; rendering is permitted only after complete validation. |
| Authenticated temporary recollection → frozen canonical pair | External read access and generated files must be validated and byte-compared before replacing Phase 227's before-state. |
| BASE-01 repair → BASE-02/OWN-01 evidence | Baseline changes must not weaken provider proof, setup ownership, readiness diagnostics, or browser proof semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-68 | Tampering | `liveRuns` historical contract selection | high | mitigate | Parse contracts from the exact head-SHA workflow bytes and thread one contracts collection through every identity, runner, prerequisite, and eligibility decision; prove with a topology-divergence regression. |
| T-226-69 | Spoofing | historical job/matrix/runner identity | high | mitigate | Retain exact closed alias resolution, declared runner classes, attempt isolation, unresolved/ambiguous rejection, and future-negative controls against the per-run contracts. |
| T-226-70 | Tampering | persisted NDJSON → Markdown | high | mitigate | Apply full per-kind semantic validation and immutable URL/ID matching before rendering; production-CLI forged-NDJSON controls must fail before output. |
| T-226-71 | Information Disclosure | baseline record and link surface | high | mitigate | Preserve the D-06 allowlist, reject non-GitHub evidence destinations and detailed/raw fields, and run privacy fixtures plus production-renderer and installed-pair checks. |
| T-226-72 | Repudiation | frozen D-03 outcome and canonical-pair installation | medium | mitigate | Recollect through authenticated read-only access, retain immutable links/source digests, require percentiles and a confirmed-or-contrary result only after at least twenty complete paths qualify and the latest twenty are selected, otherwise preserve the exact count and literal `insufficient_sample`, use verified same-filesystem backups with rollback armed across both canonical replacements, and record dual-render plus installed verification in automated validation evidence. |
| T-226-73 | Denial of Service | historical workflow retrieval and parsing | low | accept | The collector bounds history to 90 days, uses existing concurrency, may cache only by immutable head SHA, and fails with a precise source/contract error without replacing canonical evidence. |
| T-226-74 | Tampering | provider/setup preservation boundary | medium | mitigate | Run the complete provider, real formatter, setup/readiness, required-lane, and API-coverage contracts after canonical replacement. |

No package-manager install, dependency addition, database schema, migration, ORM push, frontend/UI, workflow mutation, or new external API capability is in scope, so no package-legitimacy, schema-push, or UI checkpoint applies. GitHub Actions access remains the read-only capability matrix already declared in `COVERAGE.md`.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe evidence distinguishes actual critical path, setup ownership, and provider proof | 20 | COVERED | Repairs historical provenance and evidence integrity, recollects the canonical pair, and preserves the two already-verified truths. |
| REQ | BASE-01 | Durable privacy-safe comparable-run baseline | 20 | COVERED | Per-run immutable contracts, complete semantic validation, forged-record controls, authenticated recollection, and byte proof close both verifier blockers. |
| REQ | BASE-02 | Required, skipped, and advisory provider evidence differs | 20 | COVERED | Existing provider/formatter suites are mandatory preservation gates; provider logic and policy are untouched. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 20 | COVERED | Existing setup/readiness suite is a mandatory preservation gate; ownership and browser semantics are untouched. |
| RESEARCH | — | Fingerprinted 20-run rolling cohort plus frozen before-state | 20 | COVERED | Authenticated 90-day recollection selects up to twenty unique first-attempt complete staged paths, retains fingerprint sensitivity for a ready result, and durably reports exact-count `insufficient_sample` evidence when fewer qualify. |
| RESEARCH | — | Raw facts, derived facts, and claims remain separate | 20 | COVERED | Semantic validation checks raw identifiers/timestamps against derived duration/count claims before rendering. |
| RESEARCH | — | Official read-only Actions run/job/workflow-content transport | 20 | COVERED | Existing `gh api` paths and `COVERAGE.md` capabilities are reused without mutation or raw downloads. |
| RESEARCH | — | Untrusted API data must be normalized/allowlisted and escaped before Markdown | 20 | COVERED | Strict URL/ID matching and forged-NDJSON renderer controls close the research assumption. |
| RESEARCH | — | Dependency-free deterministic validation stack | 20 | COVERED | Node built-ins, existing shell/ExUnit gates, and no installation. |
| CONTEXT | D-01 | Canonical checked-in NDJSON + Markdown; raw forensics remain external | 20 | COVERED | Temporary validated recollection replaces both canonical files only after byte gates; no raw artifacts are downloaded. |
| CONTEXT | D-02 | Immutable workflow/config, event, branch, runner, job-set/matrix, provider cohort | 20 | COVERED | Historical source contracts supply identity, runner, prerequisite, and revision inputs for each run. |
| CONTEXT | D-03 | Latest 20 successful first-attempt full-CI runs in rolling 90 days | 20 | COVERED | Existing CLI recollects the bounded cohort and installs either the latest-twenty percentile/confirmed-or-contrary result when at least twenty paths qualify, or an exact-count `insufficient_sample` result without percentiles or critical-path confirmation when fewer qualify; invalid evidence still triggers non-install or paired rollback. |
| CONTEXT | D-04 | Failures/cancellations/skips/reruns remain reliability-only; signatures normalize | 20 | COVERED | Semantic validation and existing cohort fixtures preserve exclusion/reliability behavior. |
| CONTEXT | D-05 | Queue, DAG wait, durations, cache/setup, provider, and signatures measured honestly | 20 | COVERED | Historical prerequisites drive DAG wait; per-kind arithmetic validation protects persisted facts. |
| CONTEXT | D-06 | Privacy-safe field allowlist and immutable links | 20 | COVERED | Full record semantics, strict evidence URLs, privacy controls, and automated production-renderer plus installed-pair checks enforce the boundary. |
| CONTEXT | D-07 | Schema/cohort/identity/provider/arithmetic/rerun/signature fixtures | 20 | COVERED | Existing suite expands with historical topology divergence and forged persisted-record cases. |
| CONTEXT | D-08–D-13 | Independent provider policy/proof/freshness and Fake/live boundary | 20 | COVERED | Complete provider and real tagged-only finalizer regressions preserve these decisions without implementation changes. |
| CONTEXT | D-14–D-18 | Host/CI ownership, setup diagnostics, and Playwright proof semantics | 20 | COVERED | Complete setup/readiness diagnostics and unchanged browser proof preserve these decisions. |
| CONTEXT | D-19–D-21 | Action-first truthful, accessible, resilient maintainer evidence | 20 | COVERED | Injection-safe deterministic Markdown, literal claims, exact commands, and an automated maintainer-facing completion report cover the interface contract. |
| CONTEXT | Deferred ideas | CI optimization, setup removal/caching, matrix/topology/branch-protection/gate/test changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | Explicitly outside Phase 226 and absent from tasks. |

<verification>
Completion requires the historical-topology divergence regression, production-CLI forged-NDJSON rejection, complete per-kind record semantics, authenticated temporary recollection, truthful ready-or-`insufficient_sample` D-03 handling for the exact qualifying count, a rollback-protected paired canonical replacement, dual-render and installed byte equality, green provider/setup/required-lane preservation gates, and truthful automated validation evidence for the frozen pair.
</verification>

<success_criteria>
- Every historical run's identity, matrix alias, runner, prerequisite, eligibility, and DAG timing derive from the immutable workflow source fetched at that run's head SHA.
- Persisted schema-v1 records with unsafe URLs, invalid timestamps/identifiers, inconsistent arithmetic, or negative/non-integer facts are rejected before Markdown output, and forged headings/links cannot render.
- A fresh authenticated 90-day collection installs a byte-reproducible canonical NDJSON/Markdown pair for either the latest twenty qualifying unique successful first-attempt complete staged paths when at least twenty qualify, or the exact available below-twenty count with literal `insufficient_sample`; only invalid, unsafe, semantically inconsistent, or failed work leaves/restores the prior pair.
- A confirmed-or-contrary critical-path statement and percentiles appear only after at least twenty paths qualify and the latest twenty are selected; below twenty, automated validation records the exact count and literal `insufficient_sample` with no percentile or critical-path confirmation as Phase 227's before-state.
- BASE-02 provider proof and OWN-01 setup ownership remain green, literal, and unchanged; no deferred optimization or workflow/proof weakening enters the plan.
- BASE-01, BASE-02, and OWN-01 spec-less unresolved classifications and descriptor-less prohibitions remain explicit rather than silently dropped.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md` when done.
</output>

---
phase: 226-ci-baseline-proof-semantics
plan: "21"
type: gap_closure
wave: 15
depends_on: ["226-20"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/README.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-01/D-06/D-07/D-19/D-20: Every collector normalization, cohort, renderer, fixture-verifier, and record-validation API path receives one independently supplied repository validation context; snapshot.repository and raw/persisted run/job URLs must equal that context before normalized record emission, Markdown construction, or output write."
    - "D-01/D-06/D-07/D-19/D-20: A forged snapshot plus matching valid-looking forged-repository run and job URLs fails through the production renderer CLI and leaves no report at the requested output path."
    - "D-01/D-06/D-07/D-19/D-20: The verifier CLI passes its validated --expected-repository context unchanged into verifyFixtures(validationContext); --fixtures --expected-repository attacker/forged fails against the acme/accrue fixture snapshot instead of being ignored or replaced by a fixture-local trust constant."
    - "D-01 through D-07: Canonical same-repository records, immutable run/job ID matching, historical workflow provenance, critical-path derivation, privacy filtering, and byte-deterministic rendering remain green without recollecting or rewriting the frozen baseline."
    - "D-08 through D-18: BASE-02 provider proof semantics and OWN-01 host/CI setup ownership are preservation-only assumptions in this closure and remain unchanged."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Immutable Actions URL and snapshot validation bound to an explicit repository trust input independent of persisted NDJSON"
      exports: ["createRepositoryValidationContext", "validateRecord"]
    - path: "scripts/ci/render_ci_baseline.mjs"
      provides: "Production record-set validation that consumes independently supplied repository context before interpolating evidence links"
      exports: ["renderBaseline"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Production-renderer negative controls for forged snapshot plus forged run/job evidence, URL-only forgery, omitted trust context, and same-repository success"
      exports: ["verifyFixtures"]
    - path: "scripts/ci/README.md"
      provides: "Maintainer verifier commands carrying explicit expected-repository trust input"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Regenerated deterministic report whose reproduction commands carry explicit repository trust input while measured evidence stays unchanged"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Active Nyquist quick/full, per-task, and recorded-assertion commands with explicit repository trust context plus a pending 226-21 adversarial validation-map row"
  key_links:
    - from: "CLI --expected-repository / collector --repo"
      to: "normalizeRun, normalizeJob, collectBaseline, summarizeCohorts, validateRecord, deriveStagedPathPercentiles, renderBaseline, verifyFixtures"
      via: "createRepositoryValidationContext validates one independent owner/repo input and the exact same context is threaded through every URL-normalizing collector API, every record-validation/rendering API, verifier main, and every direct call site"
      pattern: "validationContext|expected-repository|createRepositoryValidationContext|normalizeRun|normalizeJob|collectBaseline|summarizeCohorts|verifyFixtures|validateRecord|immutableUrl"
    - from: "snapshot-less direct renderer and staged-path fixtures"
      to: "renderBaseline(records, validationContext) and deriveStagedPathPercentiles(records, validationContext)"
      via: "the verifier main constructs context from required validated CLI input and passes it into verifyFixtures(validationContext); fixture commands supply acme/accrue, while attacker/forged is retained and rejected against fixture snapshot provenance"
      pattern: "createRepositoryValidationContext|renderBaseline|deriveStagedPathPercentiles"
    - from: "scripts/ci/verify_ci_baseline.mjs cross-repository fixture"
      to: "scripts/ci/render_ci_baseline.mjs --input/--out"
      via: "both URL-only substitutions and a forged snapshot with matching forged run/job URLs traverse the production CLI with --expected-repository acme/accrue and fail before writeFileSync creates the requested report"
      pattern: "spawnSync|render_ci_baseline|existsSync"
    - from: "renderer/verifier maintainer CLI call sites"
      to: "required independent repository context"
      via: "update renderer usage/main, verifier spawn and main, renderer Reproduce output, scripts/ci/README.md, and the regenerated canonical Markdown; source discovery found no .github workflow or package.json invocations"
      pattern: "--expected-repository"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md maintained baseline commands"
      to: "collector --repo / renderer and verifier --expected-repository contracts"
      via: "update every active quick-run, full-suite, per-task, and automated-recorded-assertion invocation and add the 226-21 validation-map row with the task's exact automated command"
      pattern: "226-21-01|--repo szTheory/accrue|--expected-repository (acme/accrue|szTheory/accrue)"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "The spec-less edge probe returned BASE-01 unresolved/unclassified; this plan closes only the verifier-authoritative cross-repository false-evidence path and infers no additional baseline scope."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "The spec-less edge probe returned BASE-02 unresolved/unclassified; provider proof is already verified and is preserved without policy, classifier, formatter, workflow, or documentation changes."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "The spec-less edge probe returned OWN-01 unresolved/unclassified; setup ownership is already verified and is preserved without host wrapper, Playwright, provisioning, diagnostic, or documentation changes."
  prohibitions:
    - requirement_id: BASE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT render a run or job evidence link whose repository provenance is unrelated to the validated snapshot or its explicit immutable repository allowlist."
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT broaden the checked-in evidence surface to actors, raw branch names, logs, credentials, provider payloads, artifact contents, or user data while repairing repository provenance."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT reinterpret a raw job conclusion or non-run, skipped, blocked, misconfigured, failed, stale, Fake-backed, or different-SHA evidence as live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT transfer the host-owned Playwright proof contract to CI or change setup ownership, workers, retries, artifacts, provisioning, diagnostics, or canonical host commands in this closure."
---

<objective>
Bind every persisted GitHub Actions snapshot/run/job record to an independently supplied expected repository and prove the production renderer rejects a forged snapshot with matching forged-repository evidence without producing a report (D-01, D-06, D-07, D-19, D-20).

Purpose: Close the sole remaining BASE-01 verification blocker while preserving all already-verified historical-topology, deterministic baseline, provider-proof, and setup-ownership behavior.
Output: Explicit repository trust context across collector/renderer/verifier boundaries, updated CLI call sites, and production-renderer negative controls for forged snapshot plus forged run/job URLs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/README.md

Authoritative scope: `226-VERIFICATION.md` leaves one failed truth: syntactically valid Actions URLs from arbitrary repositories pass `validateRecord()` and are rendered. BASE-01 is implementation scope. BASE-02 and OWN-01 are already verified and remain explicit spec-less preservation assumptions. No external API integration, recollection, canonical NDJSON-fact rewrite, workflow change, or Phase 227 optimization belongs in this closure; only the generated Markdown reproduction commands change to carry the new trust argument.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` creates repository validation context only from the caller's explicit repository input and requires persisted snapshot.repository plus run/job URLs to match it.
- `scripts/ci/render_ci_baseline.mjs` requires `--expected-repository owner/repo` at the CLI and an explicit validation context in exported functions; it never derives trust from NDJSON.
- `scripts/ci/verify_ci_baseline.mjs` propagates the same explicit context through renderer, staged-path, critical-path, and production-CLI negative controls.
- `scripts/ci/README.md`, `226-VALIDATION.md`, and the renderer-generated reproduction block carry the required CLI option. Regenerate only `226-CI-BASELINE.md` so its command text stays byte-reproducible; the canonical NDJSON facts remain frozen and unchanged. Source discovery found no `.github/workflows/**` or `package.json` invocation to modify.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject cross-repository evidence through the production renderer (D-01, D-06, D-07, D-19, D-20)</name>
  <reversibility rating="reversible">The change tightens internal validation and fixture coverage, adds a required repository-provenance CLI argument, and updates generated reproduction text without changing schema-v1 fields, canonical NDJSON facts, or external product contracts.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs, scripts/ci/README.md, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - scripts/ci/verify_executable_uat_contract.mjs
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/README.md
  </read_first>
  <behavior>
    - "RED: with --expected-repository acme/accrue, changing snapshot.repository to attacker/forged and changing all run_url/job_url destinations to matching attacker/forged paths while retaining numeric IDs makes the production renderer CLI exit nonzero and create no output file."
    - "RED: with snapshot.repository still acme/accrue, URL-only foreign-repository substitutions for a run and a job independently exit nonzero and create no output file."
    - "GREEN: every canonical same-repository run/job URL remains accepted; existing URL-shape, run-ID, job-ID, newline, Markdown-link, timestamp, arithmetic, historical-topology, privacy, and critical-path controls remain green."
    - "API controls: normalizeRun, normalizeJob, collectBaseline, summarizeCohorts, validateRecord, renderBaseline, deriveStagedPathPercentiles, and verifyFixtures reject omitted, malformed, structurally forged, or repository-mismatched validation context before returning normalized/rendered evidence."
    - "CLI controls: collector, renderer, and verifier reject omitted or malformed repository input; their mismatched/forged-repository cases exit nonzero before creating or writing the requested output path."
    - "Verifier provenance control: main constructs validationContext only from required validated --expected-repository and passes it unchanged to verifyFixtures(validationContext); --fixtures --expected-repository attacker/forged exits nonzero because the fixture snapshot is acme/accrue, before the fixture temp/output lifecycle begins."
    - "GREEN: --fixtures --expected-repository acme/accrue and canonical --records --expected-repository szTheory/accrue succeed, and snapshot-bearing plus snapshot-less API fixtures succeed only with their caller-supplied matching context."
  </behavior>
  <action>Use `scripts/ci/verify_executable_uat_contract.mjs`, the `226-PATTERNS.md` assigned analog for all three modified scripts, as the implementation-shape contract. New or updated regression logic remains dependency-free ESM with `node:` built-ins before repository-local imports, small named validation helpers, one `main()` dispatcher, and one top-level `FAIL:`/`process.exitCode = 1` boundary per executable. Keep forged inputs beneath one `fs.mkdtempSync()` directory and every file write, child CLI invocation, and assertion inside its `try/finally` with recursive forced cleanup. Add no dependency, nested process exit, or alternate harness.

Implement one constructor-produced immutable trust object: `createRepositoryValidationContext({ expectedRepository })`. It accepts exactly one strict `owner/repository` string, canonicalizes no alias, freezes the copied value and returned branded object, and rejects missing/malformed inputs plus lookalike plain objects. It never derives trust from snapshot content, raw/persisted URLs, fixture data, Git remote state, environment, actor/branch data, or an allowlist array. Require this exact signature inventory wherever URLs can be normalized or validated: `immutableUrl(value, label, validationContext, expectedRunId = null, expectedJobId = null)`, `normalizeRun(run, validationContext)`, `normalizeJob(job, run, validationContext, completedByName = new Map())`, `collectBaseline(runs, validationContext)`, `summarizeCohorts(runs, validationContext, { windowDays = 90, sampleSize = 20, now = Date.now() } = {})`, `validateRecord(record, validationContext)`, `deriveStagedPathPercentiles(records, validationContext)`, `renderBaseline(records, validationContext)`, `verifyCriticalPath(records, rendered, validationContext)`, and `verifyFixtures(validationContext)`. Validate the context before consuming records. `immutableUrl` retains exact HTTPS/path and numeric run/job ID checks and additionally compares the decoded owner/repository pair exactly with `validationContext.expectedRepository`.

Update every collector-internal direct call site, with no context-free overload or default: `normalizeRun` passes its context to run `immutableUrl`; `normalizeJob` passes it to job `immutableUrl`; `collectBaseline` passes the same object to its `normalizeRun` call and every mapped `normalizeJob`; `summarizeCohorts` passes it to every `normalizeRun`; collector `main()` constructs it once from required `--repo`, passes it to `collectBaseline`, `summarizeCohorts`, and every `validateRecord` in serialization. Require `--repo` for live and local `--input`/`--fixtures` modes. Live snapshot.repository must match before normalized/serialized output. Omitted/malformed `--repo`, a valid but mismatched repo, or raw run/job URLs for another repository exits nonzero before `writeFileSync` creates the requested output. Preserve all normalization, cohort, schema-v1, privacy, historical-topology, timing, and provider-state semantics.

Update every renderer direct call site: both `deriveStagedPathPercentiles` and `renderBaseline` pass context to every `validateRecord`; `renderBaseline` passes that same object to its internal `deriveStagedPathPercentiles`; renderer `main()` constructs context only from required `--expected-repository`, requires exactly one snapshot, validates snapshot.repository first, calls `renderBaseline(records, validationContext)`, and only then writes. Snapshot-less API fixtures remain permitted with explicit context; the production renderer CLI is snapshot-bearing and fail-closed. Multiple snapshots, omitted/malformed/forged context, snapshot mismatch, URL mismatch, and compound snapshot/run/job mismatch all fail before output creation.

Thread the context through every current verifier call site. Change `rejectsForbiddenFields(fixture, validationContext)`, `cohortControls(fixture, validationContext)`, `stagedPathControls(fixture, validationContext)`, `historicalRevisionAndIdentityControls(validationContext)`, and `liveDisplayIdentityControls(validationContext)` so every `collectBaseline(...)` and `summarizeCohorts(...)` call in those helpers supplies the matching context; where the live-display helper exercises both `acme/accrue` and `szTheory/accrue`, construct those contexts explicitly from the helper's caller-supplied repository inputs rather than reusing a mismatched constant. In `stagedPathControls`, pass context to the initial, contrary, missing-host, bad-order, rerun, nineteen-run, multi-fingerprint, duplicate-identity, and scheduled-compatible `deriveStagedPathPercentiles` calls and to both render calls. Pass context through `verifyCriticalPath`. In `verifyFixtures`, pass its parameter to its `collectBaseline`, `renderBaseline`, all helper calls, and all direct `normalizeRun`, `normalizeJob`, `validateRecord`, renderer, and derive API controls. In verifier `main()`, require and validate `--expected-repository` before dispatch; construct the context exactly once and pass that object unchanged into `verifyFixtures(validationContext)` or the records-path `renderBaseline` and `verifyCriticalPath`. Do not define or substitute `ACME_FIXTURE_VALIDATION_CONTEXT` inside `verifyFixtures` or any downstream helper.

Start RED by extending the existing production-path fixture lifecycle with an in-memory `acme/accrue` snapshot plus normalized run/job records. Add API negative controls for each required boundary: omitted context; malformed constructor input; forged plain-object context; and a constructor-produced but mismatched repository context. Prove matching `acme/accrue` succeeds. Retain newline/link-closing, URL-only run forgery, URL-only job forgery, and compound snapshot plus matching attacker/forged run/job URL cases. For production CLIs, invoke collector and renderer omitted, malformed, and mismatched/forged cases with unique requested output paths and assert nonzero status plus path absence. Spawn the verifier for omitted and malformed `--expected-repository`, and for `--fixtures --expected-repository attacker/forged`; fixture snapshot validation must reject the attacker value before `mkdtempSync`, record creation, or any renderer output write, which also prevents recursive fixture spawning. The parent fixture command supplies `acme/accrue` and remains the same-repository success control.

Update every maintained CLI call site: collector usage/main and generated Reproduce collection command use `--repo`; renderer usage/main, verifier production-renderer `spawnSync`, generated Reproduce renderer command, and canonical Markdown renderer command use `--expected-repository`; verifier usage/main, generated Current fact/Next command, generated Reproduce fixture command, both `scripts/ci/README.md` verifier commands, and canonical Markdown copies use `--expected-repository`. Fixture commands use `acme/accrue`; canonical records/rendering use `szTheory/accrue`. Regenerate only `226-CI-BASELINE.md` from unchanged NDJSON. Confirm `.github/workflows/**` and `package.json` still contain no collector/renderer/verifier CLI invocation.

Treat `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` as an active executable contract, not historical prose. Update its Quick run command, Full suite command, every existing per-task validation-map command, and every Automated Recorded Assertions command that invokes a baseline CLI. Every executable collector invocation (excluding `node --check`) carries `--repo szTheory/accrue` where it operates on canonical/live Phase 226 evidence; every fixture verifier invocation carries `--expected-repository acme/accrue`; every canonical-record verifier and renderer invocation carries `--expected-repository szTheory/accrue`. Preserve the existing historical rows and their execution evidence while correcting their now-required arguments. Append a pending `226-21-01` row for wave 15 and requirements BASE-01, BASE-02, OWN-01 that names T-226-21-01 through T-226-21-04, covers omitted/malformed/forged/mismatched trust context, URL-only and compound snapshot/run/job forgery, no-output behavior, same-repository success, canonical byte reproduction, provider preservation, and setup preservation, and records the exact `<automated>` command from this task rather than an abbreviated substitute. Update validation counts/sign-off language from thirty-one to thirty-two tasks/rows without marking the new row executed or green before the executor runs it. Preserve all resolved scope, analog, cleanup, single-failure-boundary, independent-input, BASE-02, OWN-01, Playwright, workflow, and Phase 227 boundaries per D-01 through D-21.</action>
  <acceptance_criteria>
    - The task's `<read_first>` references both `226-PATTERNS.md` and its assigned analog, `scripts/ci/verify_executable_uat_contract.mjs`, before any of the three modified CI baseline scripts.
    - Static inspection of all three modified scripts confirms dependency-free ESM with `node:` built-ins before local imports and a single `main()`-level failure boundary that reports a stable `FAIL:` prefix and sets `process.exitCode = 1`.
    - `verifyFixtures()` keeps all forged-evidence temp files and production-renderer invocations inside the existing `fs.mkdtempSync()` `try/finally` lifecycle, with recursive forced cleanup exactly as the analog demonstrates; the fixture command passing confirms cleanup also survives every negative control.
    - Exact exported/internal signatures are present with required context and no context-free overload/default: `immutableUrl(value, label, validationContext, expectedRunId = null, expectedJobId = null)`, `normalizeRun(run, validationContext)`, `normalizeJob(job, run, validationContext, completedByName = new Map())`, `collectBaseline(runs, validationContext)`, `summarizeCohorts(runs, validationContext, options)`, `validateRecord(record, validationContext)`, `deriveStagedPathPercentiles(records, validationContext)`, `renderBaseline(records, validationContext)`, `verifyCriticalPath(records, rendered, validationContext)`, and `verifyFixtures(validationContext)`.
    - Collector call-site inventory is complete: `normalizeRun` and `normalizeJob` pass context to `immutableUrl`; `collectBaseline` passes it to its one `normalizeRun` call and every mapped `normalizeJob`; `summarizeCohorts` passes it to its loop's `normalizeRun`; collector `main()` passes one `--repo`-derived context to `collectBaseline`, `summarizeCohorts`, and every serialized `validateRecord`.
    - Verifier call-site inventory is complete: `verifyCriticalPath`, `rejectsForbiddenFields`, `cohortControls`, `stagedPathControls`, `historicalRevisionAndIdentityControls`, `liveDisplayIdentityControls`, `verifyFixtures`, and `main` accept/pass matching context at every direct `collectBaseline`, `summarizeCohorts`, `deriveStagedPathPercentiles`, `renderBaseline`, `validateRecord`, `normalizeRun`, and `normalizeJob` invocation; renderer `renderBaseline` passes the same context to its internal derive call.
    - API controls prove omitted context, malformed constructor input, a structurally forged plain-object context, and a valid constructor-produced mismatched context fail for all URL-normalizing/record-rendering boundaries before normalized or rendered evidence is returned; matching same-repository context succeeds.
    - A forged `attacker/forged` snapshot accompanied by matching valid-looking `attacker/forged` run and job URLs is rejected against `--expected-repository acme/accrue` by the production renderer CLI, which exits nonzero and leaves no output file. Separate URL-only run and job substitutions and the newline/link-closing forgery remain rejected with no output; the unchanged same-repository snapshot/run/job set renders successfully.
    - Verifier `main()` constructs context only from its required validated `--expected-repository` and calls `verifyFixtures(validationContext)` with that exact object. `--fixtures --expected-repository attacker/forged` exits nonzero on fixture snapshot provenance before temp/output creation; omitted and malformed verifier context also exit nonzero; `--fixtures --expected-repository acme/accrue` passes. No fixture helper hard-codes or replaces caller context.
    - Collector live and local modes use explicit `--repo` context. Omitted/malformed `--repo`, repository mismatch, and foreign raw run/job URLs exit nonzero before output creation; same-repository collection succeeds.
    - Maintained CLI call-site inventory is complete: collector usage/main and Reproduce collection; renderer usage/main, verifier renderer-spawn, Reproduce renderer, and canonical Markdown renderer; verifier usage/main, Current fact/Next command, Reproduce fixture command, both `scripts/ci/README.md` verifier commands, and canonical Markdown copies. Fixture sites use `acme/accrue`, canonical sites use `szTheory/accrue`, and no `.github/workflows/**` or `package.json` invocation exists.
    - `226-VALIDATION.md` is in `<files>` and `<read_first>` and contains no active collector invocation without `--repo szTheory/accrue`, no active fixture verifier invocation without `--expected-repository acme/accrue`, and no active renderer or canonical-record verifier invocation without `--expected-repository szTheory/accrue`; syntax-only `node --check` occurrences are explicitly excluded from the argument rule.
    - `226-VALIDATION.md` contains one pending `226-21-01` wave-15 validation-map row for BASE-01, BASE-02, and OWN-01, references T-226-21-01 through T-226-21-04, describes the adversarial trust-context/no-output controls and preservation gates, and carries this task's exact automated command. Existing executed rows and evidence remain intact, while task/row totals advance from thirty-one to thirty-two without prematurely marking 226-21 green.
    - The unchanged canonical NDJSON regenerates the checked-in Markdown byte-identically after only its trust-bearing reproduction/next-command text changes.
    - BASE-01, BASE-02, and OWN-01 spec-less unresolved classifications and the descriptor-less must-NOT items remain visible in plan metadata rather than silently dropped.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const fs=require("node:fs"); const markdown=fs.readFileSync(".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md","utf8"); if (!/\| 226-21-01 \| 21 \| 15 \| BASE-01, BASE-02, OWN-01 \| T-226-21-01, T-226-21-02, T-226-21-03, T-226-21-04 \|/.test(markdown)) throw new Error("226-21 validation-map row missing"); const invocations=[...markdown.matchAll(/node scripts\/ci\/(collect|render|verify)_ci_baseline\.mjs[^&amp;|;\n`]*/g)].map((match)=&gt;({kind:match[1], command:match[0]})); if (!invocations.length) throw new Error("no maintained baseline CLI commands found in validation contract"); for (const {kind,command} of invocations) { const required=kind==="collect" ? "--repo" : "--expected-repository"; if (!command.includes(required)) throw new Error("validation command omits trust context: "+command); } if (!/\*\*Quick run command\*\*[\s\S]*?verify_ci_baseline\.mjs --fixtures --expected-repository acme\/accrue/.test(markdown) || !/\*\*Full suite command\*\*[\s\S]*?verify_ci_baseline\.mjs --records[^`\n]*--expected-repository szTheory\/accrue/.test(markdown)) throw new Error("quick/full validation repository context missing");' &amp;&amp; rg -Fq 'scripts/ci/verify_executable_uat_contract.mjs' .planning/phases/226-ci-baseline-proof-semantics/226-21-PLAN.md &amp;&amp; node -e 'const fs=require("node:fs"); for (const file of ["scripts/ci/collect_ci_baseline.mjs","scripts/ci/render_ci_baseline.mjs","scripts/ci/verify_ci_baseline.mjs"]) { const source=fs.readFileSync(file,"utf8"); const local=source.search(/^import .*from "\.\.?\//m); const builtIn=source.search(/^import .*from "node:/m); if (builtIn&lt;0 || (local&gt;=0 &amp;&amp; builtIn&gt;local) || !/\bfunction main\(/.test(source) || !/FAIL:/.test(source) || !/process\.exitCode\s*=\s*1/.test(source)) throw new Error(`${file}: analog ESM/failure-boundary contract missing`); } const collector=fs.readFileSync("scripts/ci/collect_ci_baseline.mjs","utf8"); for (const signature of [/function normalizeRun\(run, validationContext\)/,/function normalizeJob\(job, run, validationContext, completedByName = new Map\(\)\)/,/function collectBaseline\(runs, validationContext\)/,/function summarizeCohorts\(runs, validationContext,/]) if (!signature.test(collector)) throw new Error(`collector context signature missing: ${signature}`); const verifier=fs.readFileSync("scripts/ci/verify_ci_baseline.mjs","utf8"); if (!/function verifyFixtures\(validationContext\)/.test(verifier) || /ACME_FIXTURE_VALIDATION_CONTEXT/.test(verifier) || !/mkdtempSync/.test(verifier) || !/finally\s*\{[\s\S]*?rmSync\([^)]*\{\s*recursive:\s*true,\s*force:\s*true\s*\}/.test(verifier)) throw new Error("verify_ci_baseline.mjs: context propagation or cleanup contract missing"); for (const file of ["scripts/ci/render_ci_baseline.mjs","scripts/ci/verify_ci_baseline.mjs","scripts/ci/README.md",".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"]) if (!fs.readFileSync(file,"utf8").includes("--expected-repository")) throw new Error(`${file}: explicit repository CLI context missing`);' &amp;&amp; node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; ! node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository attacker/forged &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh</automated>
  </verify>
  <done>Maintainers can normalize, summarize, validate, derive, verify, and render only evidence whose raw URLs, persisted snapshot, and persisted URLs match one independently supplied repository context; omitted, malformed, forged, and mismatched API/CLI context—including `--fixtures --expected-repository attacker/forged`—fails nonzero before output creation/write, every enumerated API and CLI call site carries context, same-repository controls pass, and every previously verified Phase 226 contract stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted NDJSON → semantic validator | Repository-controlled or tampered records are untrusted until schema, arithmetic, identity, and repository provenance checks pass. |
| Caller CLI/API trust input → persisted NDJSON | The expected repository is supplied independently; untrusted snapshot.repository must match it before any persisted evidence can be accepted. |
| Semantic validator → Markdown renderer | Only snapshots and URLs bound to the independently supplied expected repository may become clickable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-226-21-01 | Spoofing | snapshot plus `run_url` / `job_url` provenance | high | mitigate | Bind snapshot and URL owner/repository to required caller-supplied context; production tests forge all persisted fields together and still block rendering. |
| T-226-21-02 | Tampering | persisted NDJSON → `renderBaseline()` | high | mitigate | Run complete record-set validation before Markdown construction and prove the production CLI leaves no output for foreign evidence. |
| T-226-21-03 | Information Disclosure | validation errors and fixtures | medium | mitigate | Report field/provenance failure without serializing credentials, actors, raw branches, payloads, logs, or artifact contents. |
| T-226-21-04 | Denial of Service | malformed repository trust context | low | accept | Fail closed with a bounded deterministic validation error; no long-running or remote operation is introduced. |
| T-226-21-SC | Tampering | package installs | high | mitigate | No package installation or dependency change is permitted by this plan. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Durable privacy-safe CI evidence distinguishes critical path, ownership, and provider proof | 21 | COVERED | Closes the final false-evidence path; already-verified ownership/provider truths are preservation gates. |
| REQ | BASE-01 | Durable privacy-safe comparable-run baseline | 21 | COVERED | Repository-bound evidence plus production negative controls close the authoritative blocker. |
| REQ | BASE-02 | Required, skipped, and advisory provider evidence remain distinct | 21 | COVERED | Explicit flagged assumption and unchanged provider fixture gate preserve verified behavior. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics remain legible | 21 | COVERED | Explicit flagged assumption and unchanged setup diagnostic gate preserve verified behavior. |
| RESEARCH | — | Versioned NDJSON → deterministic validated Markdown with privacy allowlist | 21 | COVERED | Existing validator/renderer path gains repository provenance; no new dependency or external API. |
| RESEARCH | — | Untrusted API/persisted data cannot write false Markdown evidence | 21 | COVERED | Independent trust input plus production renderer controls cover simultaneous snapshot/run/job forgery and URL-only tampering. |
| CONTEXT | D-01–D-07 | Canonical evidence, comparability, arithmetic, privacy, validation | 21 | COVERED | D-01/D-06/D-07 are repaired; D-02–D-05 stay protected by the complete baseline suite. |
| CONTEXT | D-08–D-13 | Provider policy/proof/freshness remain literal and independent | 21 | COVERED | Preservation-only; complete provider fixture gate remains required. |
| CONTEXT | D-14–D-18 | Host/CI setup ownership and Playwright proof stay unchanged | 21 | COVERED | Preservation-only; setup diagnostic gate remains required. |
| CONTEXT | D-19–D-21 | Truthful, privacy-safe, deterministic maintainer evidence | 21 | COVERED | Foreign evidence cannot become a Markdown claim; no dashboard or product UI is introduced. |
| CONTEXT | Deferred ideas | Optimization, caching, topology/matrix/gate changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | Explicitly outside Phase 226 and absent from tasks. |

<verification>
Run the dependency-free Node syntax/signature checks, the complete baseline fixture suite with explicit fixture repository, an explicit attacker/forged verifier invocation, byte-reproduction/critical-path validation with explicit canonical repository, the complete provider fixture suite, and owner-first setup diagnostics. Completion requires omitted, malformed, structurally forged, and mismatched API context plus collector/renderer/verifier CLI negative controls, compound snapshot/run/job forgery, and both URL-only cases to fail before normalized evidence return or requested output creation/write.
</verification>

<success_criteria>
- Raw collector run/job URLs, snapshot.repository, and persisted run/job evidence URLs are bound to one independently supplied context through every enumerated normalization, collection, cohort, validation, derivation, fixture, and rendering API before evidence return or Markdown rendering.
- Omitted, malformed, structurally forged, and mismatched API/CLI context, simultaneously forged snapshot/run/job evidence, URL-only substitutions, and `--fixtures --expected-repository attacker/forged` are rejected nonzero before output creation/write; matching acme/accrue fixtures and szTheory/accrue canonical records pass.
- The canonical NDJSON facts remain unchanged; regenerated Markdown is byte-reproducible with updated trust-bearing reproduction commands, and historical topology, critical-path, privacy, provider, and setup contracts stay green.
- No schema, workflow, package script, external API, dependency, provider-policy, setup-ownership, Playwright, or Phase 227 optimization scope changes.
- BASE-01, BASE-02, and OWN-01 unresolved spec-less assumptions and surviving must-NOT items remain explicit.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-21-SUMMARY.md` when done.
</output>

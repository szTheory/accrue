---
phase: 227-measured-critical-path-improvement
plan: "02"
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - scripts/ci/README.md
autonomous: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
user_setup:
  - service: github-actions
    why: "Immutable post-change performance and controlled negative-control evidence"
    env_vars:
      - name: GH_TOKEN
        source: "Existing authenticated GitHub CLI session with Actions read/write access to szTheory/accrue"
must_haves:
  truths:
    - "D-04/D-05: exactly three successful, first-attempt, same-event-class compatible post-change runs are retained individually and measured from release-gate start through latest Playwright shard completion, never by summing parallel shards."
    - "D-07: a separate sanitized Phase 227 NDJSON/Markdown pack records immutable links, workflow/cohort fingerprints, sample count/range, exclusions, removed host DAG wait, whole-workflow context, proof results, and frozen Phase 226 linkage."
    - "D-08/D-09: a controlled Actions run proves annotation-sweep failure while independent host/browser jobs complete and expected always/failure-conditioned artifacts follow the exact manifest."
    - "Local verifier success and live Actions evidence are labeled separately; neither is substituted for the other."
  artifacts:
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Sanitized immutable post-change and controlled-negative-run records"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Deterministically rendered maintainer-facing candidate comparison"
    - path: "scripts/ci/README.md"
      provides: "Exact local verifier, live collection, negative-control, and rollback commands"
  key_links:
    - from: "GitHub Actions run/job/artifact API"
      to: "227-CI-CRITICAL-PATH.ndjson"
      via: "existing Phase 226 repository-bound collector plus Phase 227 validation"
      pattern: "run_attempt|run_url|job_url|workflow_revision|cohort_fingerprint"
    - from: "227-CI-CRITICAL-PATH.ndjson"
      to: "227-CI-CRITICAL-PATH.md"
      via: "renderCriticalPathComparison followed by byte comparison"
      pattern: "renderCriticalPathComparison|byte-reproducible"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The unresolved PATH-01 edge assumes workflow_dispatch is the selected same-event class; if execution uses push instead, all three observations must use push and the evidence must state that choice."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The unresolved PATH-02 edge assumes GitHub Actions can supply three independent first attempts during execution; absence is an automated fail-closed stop, never synthetic performance proof."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The unresolved SAFE-01 edge assumes the live artifact inventory is observable with current repository authority; unavailable artifacts remain failed/unverified rather than inferred from YAML."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency/empty/ordering assumptions from Plan 01 remain active for the live record: equality is retained literally, fewer than three eligible samples is insufficient, and equal observations remain distinct in deterministic run-ID order."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT select only favorable runs, count reruns as independent samples, or erase failed, slow, incompatible, or excluded observations from the durable record."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT claim local fixture output is immutable live Actions evidence or claim workflow wall time is the locked primary metric."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT report an artifact as retained unless the immutable run inventory demonstrates its manifest-conditioned presence or documented expected absence."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT leave the controlled-failure mutation on a production branch or use it to alter normal push/pull-request behavior."
---

<objective>
Acquire immutable live proof for the candidate graph and its complementary failure control (D-04 through D-09).

Purpose: Turn the locally proven optimization into a reviewable Actions observation set without confusing repository tests with live scheduler/artifact behavior.
Output: Three post-change first-attempt observations, one isolated controlled-negative run, deterministic NDJSON/Markdown evidence, and maintainer commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-RESEARCH.md
@.planning/phases/227-measured-critical-path-improvement/227-PATTERNS.md
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
@.planning/phases/227-measured-critical-path-improvement/227-01-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
@scripts/ci/verify_ci_critical_path.mjs
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/README.md
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

- `.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson` adds schema-v1 `comparison`, `post_run`, `negative_control`, `exclusion`, and `decision_pending` records; all URL-bearing records are repository-bound.
- `.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md` is generated byte-for-byte from the NDJSON and shows current fact, literal pending state, owner, one exact next command, immutable links, observations, range/median, DAG wait, context, exclusions, and controls.
- `scripts/ci/README.md` documents the Phase 227 verifier/collection commands, controlled annotation fixture procedure, frozen Phase 226 boundary, and exact inverse patch.

<tasks>

<task type="auto">
  <name>Task 1: Record the isolated live annotation negative control without changing normal CI behavior (D-02, D-08, D-09)</name>
  <precondition>`gh auth status` succeeds and the authenticated identity can push a temporary branch and dispatch `.github/workflows/ci.yml` in `szTheory/accrue`.</precondition>
  <reversibility rating="reversible">The control is a temporary branch-only annotation emission removed immediately after its immutable run is recorded; the production candidate branch never contains the mutation.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
    - scripts/ci/verify_ci_critical_path.mjs
    - .github/workflows/ci.yml
    - scripts/ci/README.md
  </read_first>
  <action>First run the local aggregate-failure fixture. Per the resolved research seam, create an isolated temporary evidence branch from the exact candidate commit and add one transient step in an already-successful independent required lane that emits a uniquely named Phase 227 test annotation while returning success. Do not fail, skip, retry, delete, or weaken that lane and do not touch the candidate branch. Dispatch the workflow on the temporary branch, wait for completion, and require host-integration plus every Playwright shard to complete normally while annotation-sweep detects the test annotation and concludes failure. Record the immutable run/job URLs, exact commit SHA/workflow revision, conclusions, all manifest-relevant artifacts with their condition-driven present/absent state, and the explicit controlled annotation marker in a sanitized `negative_control` record. Run the verifier's live Actions mode to fetch the repository-bound run, jobs, annotation, and artifact inventory and assert every recorded fact directly; immutable URLs remain durable evidence. Remove the remote temporary branch only after the automated live assertion and evidence verifier pass, then assert through the GitHub API that the branch ref is absent while retaining the immutable Actions run. If the annotation sweep does not fail exactly as intended, record the failed control and stop rather than redesigning production CI.</action>
  <acceptance_criteria>The NDJSON contains one repository-bound controlled run whose immutable facts prove independent host/browser completion, exact artifact behavior, and annotation-sweep failure; the candidate branch has no control mutation and local fixture output is separately labeled.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --require-negative-control --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --expected-repository szTheory/accrue</automated>
  </verify>
  <done>The complementary live negative control exists as immutable evidence, its transient mutation is absent from the candidate branch, and the verifier accepts the record without treating fixture proof as live proof.</done>
</task>

<task type="auto">
  <name>Task 2: Collect three independent candidate runs and render the pending decision pack (D-04, D-05, D-06, D-07)</name>
  <precondition>`gh auth status` succeeds, the candidate workflow commit is pushed, and the workflow supports three independent runs of one chosen event class.</precondition>
  <reversibility rating="reversible">Checked-in evidence is additive and can be regenerated from immutable runs; the task does not yet decide whether the graph remains or rolls back.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md, scripts/ci/README.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_critical_path.mjs
    - scripts/ci/README.md
    - .github/workflows/ci.yml
  </read_first>
  <action>Trigger or identify three independent runs at the exact candidate SHA, all using one explicit event class and attempt 1; a rerun remains reliability evidence and never fills the sample. Use the Phase 226 repository-bound collector interfaces and Phase 227 verifier to retain every candidate observation and exclusion. Require complete stable release, host, and Playwright identities; compute each staged duration from release-gate start to the latest Playwright shard completion, compute removed host DAG wait separately, and record workflow wall time only as supporting context. Preserve exact workflow/cohort fingerprints, sample count, range, individual immutable links, and every slow/failed/incompatible observation.

Render Markdown deterministically from NDJSON in current fact → literal `decision_pending` state → owner → exact Plan 03 command → immutable evidence order. State the frozen before median 2,083 seconds, p95 2,602 seconds, and keep threshold 1,666 seconds without deciding around missing evidence. Per the resolved research rule, an observation above p95 may be excluded only when tied to an externally corroborated GitHub incident/status event or explicit runner/infrastructure failure evidence for that observation; retain the raw observation and corroboration, and reject ordinary runner variance or an unexplained slow run as exclusion grounds. Run live Actions verification over all three candidate run/job/artifact inventories. Update the contributor guide with exact fixture, live verification, controlled control, and inverse rollback commands and the frozen Phase 226 hashes.</action>
  <acceptance_criteria>Exactly three eligible first-attempt same-event-class runs are individually retained, all other observations remain visible, staged durations use the latest shard rather than a sum, the candidate median/range/DAG wait are reproducible, Markdown byte-matches the verifier render, and the decision remains pending for Plan 03.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue</automated>
  </verify>
  <done>The candidate has a sanitized, immutable, byte-reproducible three-run comparison and live negative-control pack with honest pending state and frozen before evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository → GitHub Actions | A pushed candidate or temporary control commit causes externally executed work and immutable evidence. |
| GitHub API/run artifacts → durable NDJSON | Remote metadata is untrusted until repository, SHA, attempt, workflow revision, identity, and artifact contracts validate. |
| Durable NDJSON → maintainer report | Sanitized records must render deterministically without raw logs, secrets, actors, branches, payloads, or artifact contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-05 | Spoofing | run/job/artifact URLs | high | mitigate | Require immutable szTheory/accrue URLs, exact IDs/SHA/revision, attempt 1, and contract identities before admission. |
| T-227-06 | Tampering | sample selection and exclusions | high | mitigate | Retain every observation; enforce one event class, three independent first attempts, explicit exclusions, and narrow official-outage substantiation. |
| T-227-07 | Repudiation | controlled negative run | high | mitigate | Record unique annotation marker, transient commit, job conclusions, artifact inventory, and immutable links; verify branch cleanup separately from evidence retention. |
| T-227-08 | Information Disclosure | checked-in evidence | medium | mitigate | Persist only the Phase 226 allowlisted timing/identity/proof fields and links; raw artifacts stay Actions-owned. |
| T-227-09 | Denial of Service | repeated live workflows | low | accept | Exactly three candidate runs and one negative control are bounded by D-04/D-08; failed attempts remain evidence and execution stops on deterministic failure. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No package/action install changes; existing GitHub CLI and repository scripts only. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 — Access-control rules are enforced at a trusted service layer | T-227-05, T-227-07 | Tasks 1-2 use authenticated repository-bound GitHub API data and `--verify-live-actions` to bind repository, SHA, revision, attempts, conclusions, marker, branch cleanup, and artifacts. |
| V5.1.1 — Inputs are validated against an expected schema and contract | T-227-06, T-227-08 | Task 2's automated command validates all samples, exclusions, corroboration, sanitized fields, deterministic rendering, and immutable URLs. |
| V14.2.1 — Components and dependencies come from trusted, pinned sources | T-227-SC | Both tasks use existing GitHub CLI/repository scripts and add no package or action. |

**Blocking semantics:** Every high-severity threat is execution- and phase-closure-blocking until its mapped automated verification passes. A low-severity accepted threat never waives a failed high-severity mitigation.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster critical path with recoverable proof | 02, 03 | COVERED | Live proof is collected here and adjudicated next. |
| REQ | PATH-01/PATH-02 | Measured candidate and validated improvement | 02, 03 | COVERED | Three immutable candidate runs plus threshold decision. |
| REQ | SAFE-01/SAFE-02 | Stable contracts, negative control, rollback | 02, 03 | COVERED | Live artifact control here, final keep/rollback next. |
| RESEARCH | — | Repository-bound collection, sanitized deterministic report, automated live assertions | 02 | COVERED | Existing collector and new verifier are used; no second API integration. |
| CONTEXT | D-04–D-09 | Sampling, metrics, thresholds, separate evidence, controls, manifest | 02 | COVERED | All are explicit in tasks and acceptance gates. |
| CONTEXT | D-10/D-11 | Conditional rollback and restoration | 03 | COVERED | Not prematurely executed in the collection plan. |
| CONTEXT | Deferred ideas | Broader optimization and product work | — | EXCLUDED | No task includes them. |

<verification>
Local commands must pass before and after collection. Live proof requires four immutable Actions runs whose repository-bound run/job/annotation/artifact facts pass `--verify-live-actions`; fixture output alone cannot satisfy this plan.
</verification>

<success_criteria>
- Three candidate observations are independent first attempts of one event class and remain individually visible.
- The controlled live run shows independent host/browser completion, exact artifact behavior, and annotation-sweep failure without a lasting workflow switch.
- The report is sanitized, repository-bound, deterministic, pending rather than prematurely kept, and linked to unchanged Phase 226 evidence.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md` when done.
</output>

---
phase: 227-measured-critical-path-improvement
plan: "03"
type: execute
wave: 3
depends_on: ["227-02"]
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/verify_ci_critical_path.mjs
  - scripts/ci/README.md
  - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
  - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
  - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
autonomous: false
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
user_setup:
  - service: github-actions
    why: "Dispatch and inspect the final bounded candidate cohort and, if required, one restoration run"
    env_vars:
      - name: GH_TOKEN
        source: "Existing authenticated GitHub CLI session with Actions read/write access to szTheory/accrue"
must_haves:
  truths:
    - "D-12: the optimized host edge is first reversed to `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]` and the restored graph passes the local contract before any trigger redesign or new candidate run."
    - "D-13/D-14: the existing CI workflow exposes one required Boolean `run_live_stripe` dispatch input defaulting to true; only explicit measurement dispatches set it false, scheduled provider proof is unchanged, and no alternate event/workflow/no-op/rerun mechanism supplies samples."
    - "D-15/D-16/D-17: an observation is eligible only when the raw run is a successful attempt-1 `workflow_dispatch` at the exact candidate SHA with `run_live_stripe: false` and a complete repository-bound release, docs, Admin, host, three-shard browser, Docker, finalizer, artifact, advisory, and provider proof vector."
    - "D-18/D-19/D-20: all prior exclusions remain immutable, exactly three final candidate first attempts are launched, every non-qualifying run keeps its identity and one allowed classification, and no rerun or replacement cohort enters the timing sample."
    - "D-21/D-22: any non-qualifying final run, incomplete cohort, missed timing threshold, or safety regression immediately restores the exact inverse and reports `rollback_applied`, `rollback_verified`, or `rollback_applied_unverified` literally; only fresh normal-CI proof can reach `rollback_verified`."
    - "D-04/D-05/D-06/PATH-02: successful phase closure requires exactly three admitted observations, staged-path median <=1,666 seconds, no unsubstantiated observation above 2,602 seconds, and no lost or weakened proof."
    - "D-07/D-23: Markdown and sanitized NDJSON preserve the Phase 226 baseline, all exclusions, control evidence, proof vectors, calculations, decision, rollback evidence, current fact, owner, and one exact next command."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Typed dispatch input, unchanged scheduled provider behavior, and either the authorized candidate edge or exact inverse graph"
    - path: "scripts/ci/verify_ci_critical_path.mjs"
      provides: "Trigger preflight, complete live proof-vector admission, bounded cohort decision, and staged rollback verification"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-ci-contract.json"
      provides: "Exact dispatch-input, measurement-topology, proof-vector, run-budget, failure-class, and rollback-state contract"
    - path: ".planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json"
      provides: "Positive and fail-closed trigger, proof-vector, cohort-budget, equality, empty, ordering, and rollback fixtures"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Append-only exclusions, trigger preflight, final observations, proof vectors, comparison, decision, and rollback facts"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Deterministic maintainer-facing current fact and next action"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md"
      provides: "Requirement, decision, threat, local, live, control, and rollback proof ledger"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md"
      provides: "Executed Nyquist validation map"
  key_links:
    - from: ".github/workflows/ci.yml `workflow_dispatch.inputs.run_live_stripe`"
      to: "scripts/ci/verify_ci_critical_path.mjs live admission"
      via: "repository-bound run inputs and workflow revision must match the registered measurement topology"
      pattern: "run_live_stripe|workflow_dispatch|workflow_revision"
    - from: "GitHub run/job/artifact facts"
      to: "227-CI-CRITICAL-PATH.ndjson proof_vector records"
      via: "exact repository, SHA, attempt, event, input, stable identity, conclusion, URL, and artifact comparison"
      pattern: "proof_vector|run_attempt|job_urls|artifacts|provider_state"
    - from: "227-CI-CRITICAL-PATH.ndjson final decision"
      to: ".github/workflows/ci.yml host-integration.needs"
      via: "keep retains the candidate; any failed or unknown predicate applies the exact inverse before further work"
      pattern: "kept|rollback_applied|rollback_verified|rollback_applied_unverified"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "PATH-01 remains unclassified by the spec-less edge probe; the plan treats the locked staged-path definition, frozen Phase 226 cohort, and one-edge target as the complete selection boundary."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "PATH-02 remains unclassified by the spec-less edge probe; the measured result applies to the exact recorded workflow/event/input fingerprint and does not claim universal future CI performance."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "SAFE-01 remains unclassified by the spec-less edge probe; branch-protection configuration is neither queried nor changed, while stable workflow/live proof identities are exhaustively contract-checked."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency remains unresolved: a median exactly 1,666 seconds passes, a duration exactly 2,602 seconds is not an exceedance, and exact-equal proof facts remain distinct rather than merged."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Empty remains unresolved: null, empty, single-run, or two-run final evidence fails closed, triggers the inverse, and cannot produce a keep decision."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Ordering remains unresolved: equal-valued observations remain individually addressable and render in stable ascending run-ID order without changing eligibility or median arithmetic."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT present a green workflow, whole-workflow variance, or selectively favorable runs as the locked critical-path improvement."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT keep the candidate when any required proof-vector or timing predicate is false, missing, or unknown."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT infer stable proof preservation from workflow text or run conclusion without repository-bound job and artifact evidence."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT delete tests, add retries, replace failed attempts, exceed the three-run budget, or report rollback verified without fresh restoration evidence."
---

<objective>
Recover from the failed measurement contract, run the one final bounded candidate experiment, and keep the one-edge improvement only when its timing and complete proof vector pass (D-01 through D-23).

Purpose: Close PATH-01/PATH-02/SAFE-01/SAFE-02 from honest repository-bound evidence while making the exact inverse the immediate and fully recorded response to any failed or unknown predicate.
Output: A preflighted dispatch contract, exactly three final candidate attempts, a kept result or staged exact-inverse rollback, and authoritative phase verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-RESEARCH.md
@.planning/phases/227-measured-critical-path-improvement/227-PATTERNS.md
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
@.planning/phases/227-measured-critical-path-improvement/227-01-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
@.planning/phases/227-measured-critical-path-improvement/COVERAGE.md
@scripts/ci/verify_ci_critical_path.mjs
@scripts/ci/README.md
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

- `.github/workflows/ci.yml` adds `on.workflow_dispatch.inputs.run_live_stripe` with `type: boolean`, `required: true`, and `default: true`; `jobs.live-stripe.if` continues to select every schedule and selects a manual run only when that input is true.
- `scripts/ci/verify_ci_critical_path.mjs` adds exported `verifyMeasurementPreflight(workflowSource, contract)`, exported `verifyFinalEvidence(records, contract, validationContext)`, and CLI flags `--require-preflight`, `--require-final-decision`, and `--require-rollback-verified`.
- `227-ci-contract.json` adds `dispatch_input`, `measurement_topology`, `proof_vector`, `run_budget`, `failure_classes`, and `rollback_states` fields with exact values derived from D-13 through D-22.
- `fixtures/ci-critical-path-cases.json` adds accepted/rejected cases for Boolean input default/gating, complete required/advisory/provider vectors, wrong input/SHA/attempt, missing or skipped proof, three-attempt budget, threshold equality, empty/single/null evidence, equal-duration stable ordering, failure classification, and all rollback stages.
- `227-CI-CRITICAL-PATH.ndjson` retains every existing line and appends `trigger_preflight`, `post_run`, `proof_vector`, `comparison`, `decision`, and conditional `rollback` records; it never rewrites the Phase 226 baseline or the nine exclusions.
- `227-CI-CRITICAL-PATH.md` is regenerated byte-for-byte from NDJSON and begins with current fact, literal state, owner, and one exact next command.
- `scripts/ci/README.md` adds the exact inverse, preflight, candidate dispatch, live verification, and restoration commands and states that `run_live_stripe: false` is `non_run`, not provider proof.
- `227-VERIFICATION.md` is created and `227-VALIDATION.md` is updated only after the terminal branch has the evidence required by its literal state.
- No package, action, schema, external API client, dashboard, feature switch, retry, or test-deletion artifact is created. `COVERAGE.md` remains the reasoned declaration that the established Phase 226 Actions boundary owns API coverage.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore first, then prove the dispatch-to-proof-vector candidate path (D-12 through D-17)</name>
  <precondition>The completed Plan 02 evidence files retain the admissible control, all nine exclusions, and a final `rollback_required` pending record.</precondition>
  <reversibility rating="reversible">The workflow changes are a Boolean manual-input contract plus one dependency-array transition; both have exact local inverses and no published application or data migration.</reversibility>
  <files>.github/workflows/ci.yml, scripts/ci/verify_ci_critical_path.mjs, scripts/ci/README.md, .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json, .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-01-SUMMARY.md
    - .planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - .planning/phases/227-measured-critical-path-improvement/COVERAGE.md
    - scripts/ci/verify_ci_critical_path.mjs
    - scripts/ci/README.md
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - "RED: current candidate graph plus absent typed input fails preflight; exact D-11 inverse is recognized and all retained Plan 02 evidence remains parseable."
    - "GREEN: restored graph passes first, the required Boolean input defaults true, schedule selection remains unconditional, and a manual false fixture selects every measurement job except live-stripe."
    - "GREEN: after restored-state proof, reapplying only the D-01 edge produces a candidate preflight whose exact input topology and complete proof-vector schema are registered."
    - "NEGATIVE: a false default, optional input, altered schedule behavior, manual false run selecting live-stripe, manual true run skipping it, alternate event, wrong SHA/attempt, absent/renamed/skipped required lane, missing shard, weakened artifact, or provider state inferred from workflow success fails."
    - "BOUNDARIES: null/empty/fewer-than-three evidence fails; 1,666s median and 2,602s observation pass their inclusive boundaries; equal-duration records remain distinct and stable by ascending run ID."
    - "PRESERVATION: the existing negative control and nine exclusions remain byte-present; Phase 226 frozen hashes and the reasoned API-coverage declaration remain unchanged."
  </behavior>
  <action>First apply the exact D-11 inverse per D-12 by restoring `host-integration` to both `admin-drift-docs` and `docs-contracts-shift-left`, then run the current static fixtures and inherited Phase 225/226 preservation checks before editing the trigger contract. Do not launch Actions while the candidate edge remains active or before restored-state proof passes.

Add the D-13 typed `run_live_stripe` input to the existing `CI` workflow and gate only manual selection of `live-stripe`; preserve independent release/admin and host/browser execution plus final aggregate failure per D-02, the host-to-Playwright and all other graph edges per D-03, the completed live negative control per D-08, and every scheduled run, stable job/check identity, matrix/support label, upload contract, and finalizer edge per D-09. Extend the existing dependency-free verifier, manifest, and fixtures rather than adding a collector or package. The preflight must bind the exact candidate SHA, attempt 1, `workflow_dispatch`, input false, workflow/cohort fingerprints, final budget of three, complete required release cells (Floor, Primary, Primary plus OpenTelemetry), docs, all required Admin lanes, host, three Playwright shards, Docker smoke, annotation sweep, expected artifacts, advisory Sigra/parked-ratchet literal outcomes, live-Stripe `non_run`, and a separately linked latest provider proof. A raw success conclusion is insufficient.

Keep every Plan 02 NDJSON line and the current rendered report byte-unchanged during preflight; Task 2 appends executed preflight/run facts and regenerates the D-23 report only after it has immutable live identities. Document the exact `gh workflow run ci.yml --repo szTheory/accrue --ref &lt;candidate-ref&gt; -f run_live_stripe=false` shape without substituting a mutable ref for the recorded SHA. After the restored-state and trigger/proof-vector fixtures pass, reapply only the D-01 host edge and require candidate preflight to pass. Do not use pull-request activity, no-op commits, reruns, a second workflow, or additional graph changes per D-14.</action>
  <acceptance_criteria>The current graph was demonstrably restored before redesign; the final task state is the single authorized candidate edge with a preflighted required-Boolean trigger and complete proof-vector contract; schedule/provider behavior, identities, artifacts, negative control, nine exclusions, current evidence files, frozen inputs, and API coverage declaration remain intact; all negative and boundary fixtures fail or pass exactly as specified.</acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/verify_ci_critical_path.mjs &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --require-preflight --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; test "$(wc -l &lt; .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson)" -ge 13</automated>
  </verify>
  <done>The recovered candidate is production-ready end-to-end from typed dispatch input through fail-closed proof-vector admission, with no live sample spent before preflight.</done>
</task>

<task type="auto">
  <name>Task 2: Spend the final three-run budget and keep or exactly restore (D-04 through D-11, D-18 through D-23)</name>
  <precondition>`gh auth status` succeeds; the preflighted candidate commit is pushed; the exact candidate SHA/ref is recorded; no Phase 227 candidate run has been launched after the nine retained exclusions.</precondition>
  <reversibility rating="reversible">The candidate and restored graphs differ by the recorded D-11 inverse; live run records are immutable evidence and durable reports are regenerated from sanitized facts.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - scripts/ci/verify_ci_critical_path.mjs
    - scripts/ci/README.md
    - .github/workflows/ci.yml
  </read_first>
  <action>Append the passed `trigger_preflight` record for the exact candidate SHA, workflow/cohort fingerprint, event, false input, budget, and proof-vector contract, then launch exactly three independent attempt-1 `workflow_dispatch` runs at that recorded SHA/ref, each explicitly setting `run_live_stripe=false`; never launch a fourth candidate or promote a rerun. Wait for all three and append every raw conclusion, immutable run/job URL, duration, host DAG wait, workflow duration, workflow/input/cohort fingerprints, required/advisory/provider vector, artifact presence/conditional absence, and exclusion reason. Preserve all earlier records. Classify each non-qualifying run from repository-bound facts as exactly `candidate_regression`, `deterministic_required_lane_failure`, `external_or_runner_blocked`, or `inconclusive`; use the external class only with run-specific GitHub status/incident or explicit runner/infrastructure evidence.

If all three runs qualify, compute the D-05 staged durations from release-gate start through latest Playwright shard completion, apply the D-06 median and p95 predicates, verify the negative control and every D-09 contract, append `decision.state: kept`, retain the candidate graph, record inverse-fixture rollback as not triggered, and require `--require-final-decision` plus `--require-kept` to pass.

If any run is non-qualifying, fewer than three qualify, the median misses 1,666 seconds, an observation exceeds 2,602 seconds without accepted substantiation, or any identity, artifact, proof-state, negative-control, deterministic-failure, or propagation predicate is false/unknown, trigger rollback per D-10: stop candidate evaluation immediately after recording the three authorized identities and apply the exact D-11 inverse. Append `rollback.state: rollback_applied` only after the inverse and all local preservation controls pass. Push that restored SHA and trigger exactly one fresh normal CI first attempt with the default manual input true; set `rollback_verified` only after immutable run/job/artifact/provider facts prove the prior graph and evidence contract, otherwise set `rollback_applied_unverified` with owner and one literal next command whose immutable restored SHA is substituted into `gh workflow run ci.yml --repo szTheory/accrue --ref RESTORED_SHA -f run_live_stripe=true`.

Dispatch terminal verification from the recorded last `decision`/`rollback` state instead of applying a kept-only assertion universally. For `decision.state: kept`, run the live verifier with `--require-final-decision --require-kept` and continue to Task 3 only after it passes. For `rollback.state: rollback_verified`, run the live verifier with `--require-rollback-verified`, accept that command as the safe terminal proof, halt Phase 227 as incomplete because PATH-02 is unmet, and do not enter Task 3. For `rollback.state: rollback_applied_unverified`, first require the inverse/local controls and the durable owner plus literal restored-SHA next command to be present, then emit the blocking human-action checkpoint below and stop; do not route its expected lack of fresh external proof through `--require-kept`, node repair, candidate relaunch, or Task 3.</action>
  <terminal_branches>
    <branch state="kept">Automated verification runs `--require-final-decision --require-kept`; success is the only edge to Task 3.</branch>
    <branch state="rollback_verified">Automated verification runs `--require-rollback-verified`; success is a valid safe terminal outcome and ends phase execution before Task 3.</branch>
    <branch state="rollback_applied_unverified">
      <checkpoint type="checkpoint:human-action" gate="blocking-terminal">
        <what-needed>Fresh GitHub Actions restoration proof is externally blocked after the exact inverse and local controls passed.</what-needed>
        <instructions>Resolve the recorded external blocker, then run the literal `next_command` stored in the terminal rollback record. It must be the fully substituted form `gh workflow run ci.yml --repo szTheory/accrue --ref RESTORED_SHA -f run_live_stripe=true`, with `RESTORED_SHA` replaced by that record's immutable restored SHA. After the run completes, collect its immutable run/job/artifact/provider facts and rerun `node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --require-rollback-verified --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue`.</instructions>
        <resume-signal>Resume only after the restoration run evidence is appended and `--require-rollback-verified` passes; otherwise leave Phase 227 blocked at `rollback_applied_unverified`.</resume-signal>
      </checkpoint>
    </branch>
  </terminal_branches>
  <acceptance_criteria>Exactly three final candidate run IDs exist and all old evidence remains visible. A kept decision has three complete proof vectors and every timing/safety predicate true. Any other outcome has the exact inverse, a literal staged rollback record, no additional samples, and fresh restoration proof for `rollback_verified` or an explicit external evidence gap for `rollback_applied_unverified`.</acceptance_criteria>
  <verify>
    <automated>node -e 'const fs=require("node:fs"),{spawnSync}=require("node:child_process"); const p=".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"; const rs=fs.readFileSync(p,"utf8").trim().split(/\n/).filter(Boolean).map((line,i)=&gt;{try{return JSON.parse(line)}catch(e){throw new Error(`invalid NDJSON line ${i+1}: ${e.message}`)}}); const decision=[...rs].reverse().find(r=&gt;r.kind==="decision"); const rollback=[...rs].reverse().find(r=&gt;r.kind==="rollback"); const state=rollback?.state??decision?.state; const base=["scripts/ci/verify_ci_critical_path.mjs","--verify-live-actions","--workflow",".github/workflows/ci.yml","--contract",".planning/phases/227-measured-critical-path-improvement/227-ci-contract.json","--evidence",p,"--rendered",".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md","--expected-repository","szTheory/accrue"]; if(state==="kept") base.push("--require-final-decision","--require-kept"); else if(state==="rollback_verified") base.push("--require-rollback-verified"); else if(state==="rollback_applied_unverified"){if(!rollback?.owner||!rollback?.restored_sha||rollback.next_command!==`gh workflow run ci.yml --repo szTheory/accrue --ref ${rollback.restored_sha} -f run_live_stripe=true`) throw new Error("rollback_applied_unverified requires owner and exact restored-SHA next_command"); console.log(`BLOCKING-HUMAN-ACTION: ${rollback.next_command}`); process.exit(0)} else throw new Error(`unsupported terminal state: ${state??"missing"}`); const x=spawnSync(process.execPath,base,{stdio:"inherit"}); process.exit(x.status??1)'</automated>
  </verify>
  <done>A repository-bound kept decision passes its kept-only command and is the sole branch that reaches Task 3; `rollback_verified` passes its rollback-only command as a valid safe terminal outcome; `rollback_applied_unverified` records the exact inverse/local proof, owner, and fully substituted next command, then stops at the explicit blocking human-action checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Seal kept requirement, threat, validation, and maintainer evidence (D-01 through D-23)</name>
  <precondition>Task 2 ended `decision.state: kept` with no terminal rollback record; the branch-aware Task 2 verifier ran `--require-final-decision --require-kept` and passed; PATH-02 is supported by exactly three admitted final candidate attempts. If Task 2 ended `rollback_verified` or `rollback_applied_unverified`, this precondition is false and execution has already terminated before Task 3.</precondition>
  <reversibility rating="reversible">Verification and validation artifacts report immutable evidence and can be regenerated without changing workflow, application, or data contracts.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md, .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md, scripts/ci/README.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - scripts/ci/README.md
  </read_first>
  <action>Before writing any file, assert that the terminal evidence contains `decision.state: kept`, contains no terminal `rollback` record, and passes `--require-final-decision --require-kept`; if any assertion is false, stop without executing Task 3. Create `227-VERIFICATION.md` only for the kept branch with `status: passed` and `behavior_unverified: 0`. Separate automated local proof from immutable live Actions proof. Map PATH-01, PATH-02, SAFE-01, SAFE-02; D-01 through D-23; every high/medium threat; and the ASVS L1 controls to exact commands, record kinds, immutable links, and observed pass results. Include the candidate graph, typed input/default, three run IDs and vectors, individual staged durations, range/median, host DAG waits, workflow context, all exclusions/classifications, negative control, expected artifact presence/absence, provider `non_run` plus separate proof link, frozen digests, decision, and inverse-fixture rollback-not-triggered evidence.

Update `227-VALIDATION.md` with the actual three-task map and executed outcomes; set `status: validated`, `wave_0_complete: true`, and `nyquist_compliant: true` only after every local and required live row passes. Preserve all six spec-less flagged assumptions and the four descriptor-less flagged prohibitions without inventing check fields. Generate the automated UAT and SUMMARY coverage required by the repository execution workflow with `human_judgment: false`. Keep the existing `COVERAGE.md` reasoned no-new-API declaration and document that live inspection reused the established Phase 226 boundary.</action>
  <acceptance_criteria>Verification closes all four requirements with zero unverified behavior, all 23 decisions and applicable threats/ASVS controls have exact evidence, validation is authoritative, local/live/provider facts remain distinct, assumptions/prohibitions remain visibly flagged, and the maintained commands reproduce the kept decision without mutating frozen inputs.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --require-final-decision --require-kept --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; test -s .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md &amp;&amp; node -e 'const fs=require("node:fs"); const v=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md","utf8"); for (const token of ["status: passed","behavior_unverified: 0","PATH-01","PATH-02","SAFE-01","SAFE-02","D-23","Automated/local proof","Immutable live Actions evidence","ASVS L1","T-227-"]) if (!v.includes(token)) throw new Error(`verification missing ${token}`); const y=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md","utf8"); for (const pattern of [/^status: validated$/m,/^nyquist_compliant: true$/m,/^wave_0_complete: true$/m]) if (!pattern.test(y)) throw new Error("validation sign-off incomplete"); const c=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/COVERAGE.md","utf8"); if (!c.includes("No new external API integration")) throw new Error("API coverage declaration missing");'</automated>
  </verify>
  <done>The kept one-edge improvement is fully traceable, reproducible, proof-preserving, Nyquist-validated, and ready for phase verification with no human judgment or hidden evidence gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow input → GitHub scheduler | A manual Boolean changes provider-lane selection while required measurement work and scheduled proof must retain exact semantics. |
| GitHub run/job/artifact API → durable evidence | Remote facts are untrusted until bound to repository, SHA, attempt, event, input, revision, stable identities, conclusions, URLs, and artifacts. |
| Evidence decision → workflow graph | A false keep or false rollback claim can preserve an unproven candidate or weaken recoverability. |
| Durable report → milestone closure | Sanitized Markdown/NDJSON and validation metadata must not convert missing proof into a passed requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-15 | Tampering | `run_live_stripe` workflow input and job condition | high | mitigate | Exact static/fixture contract requires Boolean, required, default true; schedule always selects provider proof; manual false alone records `non_run`. |
| T-227-16 | Spoofing | candidate proof vector | high | mitigate | Bind every observation to szTheory/accrue, exact SHA, attempt 1, dispatch input, workflow revision, immutable run/job URLs, stable identities, conclusions, and artifact inventory. |
| T-227-17 | Repudiation | final run budget and classifications | high | mitigate | Retain the nine prior exclusions plus exactly three final IDs; reject reruns/replacements and closed-enum classifications lacking observation-specific evidence. |
| T-227-18 | Tampering | keep/rollback decision | high | mitigate | One fail-closed verifier evaluates all timing, proof, control, and graph predicates; any false/unknown predicate applies the exact inverse. |
| T-227-19 | Repudiation | rollback completion | high | mitigate | Separate `rollback_applied`, `rollback_verified`, and `rollback_applied_unverified`; fresh normal-CI run/job/artifact evidence is mandatory for verified status. |
| T-227-20 | Information Disclosure | checked-in NDJSON/Markdown | medium | mitigate | Reuse the Phase 226 sanitized allowlist; raw logs, actors, branch names, payloads, secrets, and artifact contents stay behind immutable Actions links. |
| T-227-21 | Denial of Service | live workflow execution | low | accept | The candidate budget is exactly three plus at most one restoration run; authentication/infrastructure failure records an evidence gap and halts without looping. |
| T-227-SC | Tampering | package/action installation | high | mitigate | Add no package or action; use Node built-ins, existing GitHub CLI authentication, and pinned workflow actions. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 — Access-control rules are enforced at a trusted service layer | T-227-15, T-227-16, T-227-18, T-227-19 | Tasks 1-2 bind scheduler input, repository authority, proof vector, graph state, and rollback proof through static and live verification. |
| V5.1.1 — Inputs are validated against an expected schema and contract | T-227-15, T-227-16, T-227-17, T-227-18 | Tasks 1-2 reject malformed input topology, incomplete vectors, extra/reused attempts, unknown classifications, and false/unknown decision predicates. |
| V5.2.1 — Untrusted data is sanitized before use or persistence | T-227-20 | Tasks 1-3 retain only allowlisted facts and immutable links; verification rejects forbidden durable fields. |
| V14.2.1 — Components and dependencies come from trusted, pinned sources | T-227-SC | All tasks add no package/action and run the existing dependency-free verifier/preservation suite. |

**Blocking semantics:** Every high-severity threat blocks candidate launch, keep, rollback verification, or phase closure until its mapped automated proof passes. The accepted low-severity bounded-run risk never waives a failed high-severity mitigation.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster critical path with equal identifiable/recoverable proof | 01-03 | COVERED | Plan 03 closes only on a fully admitted kept result; otherwise it restores and stops incomplete. |
| REQ | PATH-01 | Measured path, selected edge, before evidence, rollback | 01-03 | COVERED | Completed artifacts plus final immutable report retain selection, exclusions, trigger contract, comparison, and inverse. |
| REQ | PATH-02 | Validated improvement without proof removal | 03 | COVERED | Exactly three preflighted runs and complete vectors control keep. |
| REQ | SAFE-01 | Stable check identities and artifacts | 01-03 | COVERED | Existing manifest/control are extended to live full-vector proof. |
| REQ | SAFE-02 | Negative control and rollback; no masking | 01-03 | COVERED | Completed control plus staged exact-inverse behavior and fresh restoration proof. |
| RESEARCH | — | One-edge graph, repository-bound evidence, sanitized deterministic report, narrow anomaly standard | 01-03 | COVERED | Existing architecture is reused and extended for the locked trigger/proof-vector decisions. |
| CONTEXT | D-01-D-11 | Optimization, measurement, contracts, negative control, inverse rollback | 01-03 | COVERED | Completed work is preserved; Plan 03 rechecks it at terminal decision. |
| CONTEXT | D-12-D-17 | Restore-first trigger and complete proof-vector contract | 03 | COVERED | Task 1. |
| CONTEXT | D-18-D-22 | Final budget, classification, immediate staged rollback | 03 | COVERED | Task 2. |
| CONTEXT | D-23 | Maintainer report grammar | 03 | COVERED | Tasks 1-3 deterministic render and verification. |
| CONTEXT | Deferred ideas | Other graph/cache/matrix/workflow/product changes | — | EXCLUDED | No task introduces them. |

<verification>
Run the preflight fixtures and full inherited local suite before spending a live sample. Successful phase closure then requires exactly three repository-bound candidate attempts, complete proof vectors, a byte-reproducible report, `decision.state: kept`, and the final kept verifier. Any rejected branch must prove the exact inverse and staged rollback, then stop with PATH-02 and the phase incomplete rather than converting recovery into success.
</verification>

<success_criteria>
- The existing CI workflow alone owns the typed measurement input; default manual and scheduled provider behavior remain unchanged.
- The final cohort contains exactly three independent attempt-1 dispatches at one SHA with input false and complete required/advisory/provider proof vectors.
- Keep requires the locked staged median and p95 rules plus every identity, artifact, control, failure-propagation, and frozen-input predicate.
- Any failed/unknown predicate leaves no active candidate edge after evaluation and reports rollback truth in one of the three D-22 literal states.
- No prior evidence is erased, no replacement sample/rerun is promoted, and no test/gate/artifact/provider proof is deleted, renamed, retried, demoted, or inferred.
- Only the kept branch produces passed verification and validated Nyquist closure for PATH-01, PATH-02, SAFE-01, and SAFE-02.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-03-SUMMARY.md` when done.
</output>

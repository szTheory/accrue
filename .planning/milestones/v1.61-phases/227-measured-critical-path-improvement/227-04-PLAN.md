---
phase: 227-measured-critical-path-improvement
plan: "04"
type: execute
wave: 4
depends_on: [227-03]
files_modified:
  - scripts/ci/verify_ci_critical_path.mjs
  - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
  - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
  - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml
  - scripts/ci/README.md
autonomous: true
gap_closure: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04/D-15/D-16: a keep cohort is exactly three distinct workflow_dispatch attempt-1 runs at one exact candidate SHA, workflow/cohort fingerprint, run_live_stripe:false input topology, and provider_state:non_run; no other event or sample count is admissible."
    - "D-09/D-15/D-16: every admitted candidate and restoration record contains exactly the contractual required job identities, with one immutable repository-bound URL per stable role and three distinct role-bound Playwright shard URLs."
    - "D-07/D-23: recursive schema/privacy validation rejects forbidden keys or values at any depth before evidence is rendered, and the renderer byte-reproduces the checked terminal rollback report as well as a kept fixture."
    - "D-08/D-10/D-11: the immutable restored-workflow fixture and versioned compatibility contract prove the restored and one-edge candidate graphs without coupling historical fixtures to the mutable current workflow."
    - "D-18/D-19/D-21: the original phase-227-dispatch-false-v1 cohort remains terminally exhausted; its runs, reclassifications, exclusions, and rollback records are never admitted to the new gap cohort."
  artifacts:
    - path: "scripts/ci/verify_ci_critical_path.mjs"
      provides: "Fail-closed exact cohort, job-role, recursive privacy/schema, strict CLI, and terminal rendering gate"
      exports: ["verifyWorkflowContract", "verifyComparisonEvidence", "verifyFinalDecision", "renderCriticalPathEvidence", "verifyFixtures"]
    - path: ".planning/phases/227-measured-critical-path-improvement/227-ci-contract.json"
      provides: "Versioned restored/candidate compatibility, historical-prefix, role, schema, and gap-budget contract"
    - path: ".planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml"
      provides: "Immutable post-Phase-228 restored workflow source used by offline fixtures"
    - path: ".planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json"
      provides: "Positive and adversarial cases for every verifier gap reported in 227-VERIFICATION.md"
  key_links:
    - from: ".planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml"
      to: ".planning/phases/227-measured-critical-path-improvement/227-ci-contract.json"
      via: "Exact restored and derived one-edge candidate digests plus job/artifact compatibility version 2"
      pattern: "workflow_compatibility|restored_sha256|candidate_sha256"
    - from: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      to: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      via: "Recursive validation followed by deterministic terminal renderer and byte comparison"
      pattern: "renderCriticalPathEvidence|byte-match"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The spec-less probe left PATH-01 unclassified; this plan assumes the locked staged-path definition, one-edge target, and frozen Phase 226 cohort are the complete selection boundary."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The spec-less probe left PATH-02 unclassified; this plan assumes only an exact three-run kept result under the full proof vector can satisfy measured improvement."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The spec-less probe left SAFE-01 unclassified; this plan assumes the versioned contract enumerates every externally meaningful identity, condition, and recoverable artifact."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency, empty/degenerate, and ordering edges remain explicit: 1,666 seconds is accepted, 2,602 seconds is not an exceedance, null/fewer/more than three observations fail, and equal values retain stable run-id ordering without deduplication."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT represent whole-workflow variance, a selectively omitted observation, or correlation as causal critical-path improvement."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT exchange faster timing for missing, weakened, renamed, skipped, or less recoverable required or provider evidence."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT silently substitute a new identity, artifact, condition, retention rule, or compatibility label for the preserved contract."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT erase failed attempts, promote reruns, reuse role URLs, expand an exhausted budget, or certify fabricated rollback evidence."
---

<objective>
Make the Phase 227 decision gate safe and reproducible before authorizing any new external run (D-04, D-07 through D-11, D-15 through D-23).

Purpose: Close every verifier and deterministic-rendering blocker identified by the authoritative 227-VERIFICATION.md while preserving the exhausted original experiment as immutable history.
Output: A versioned workflow compatibility contract, immutable workflow fixture, exhaustive negative corpus, strict CLI, recursive evidence validation, and byte-stable terminal renderer.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
@.planning/phases/227-measured-critical-path-improvement/227-03-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-03-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VERIFICATION.md
@scripts/ci/verify_ci_critical_path.mjs
@.planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
@.planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
@.github/workflows/ci.yml
</context>

## Artifacts this plan produces

- `fixtures/ci-workflow-restored-v2.yml` is an immutable copy of the audited post-Phase-228 restored workflow; fixtures derive only the D-01 one-edge candidate from it.
- `227-ci-contract.json` advances to an explicit compatibility/schema version that names exact required job roles, role-bound URL expectations, restored/candidate workflow digests, the existing ledger-prefix digest, and the separate gap budget ID `phase-227-gap-dispatch-false-v2`.
- `verify_ci_critical_path.mjs` exposes exactly-one-action CLI behavior for fixtures, workflow verification, evidence verification, rendering, and live reconciliation; action-specific options and modifiers are allowlisted.
- `ci-critical-path-cases.json` includes a named negative case for duplicate run IDs, wrong event/input/attempt/SHA/fingerprint/provider state, too few or too many samples, missing/extra/forged job identities, duplicate or role-swapped URLs, nested forbidden evidence, unknown/duplicate/missing CLI options, no action, multiple actions, and terminal render drift.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one forged keep record end to end, then close every fail-open evidence path (D-04, D-08, D-09, D-15 through D-22)</name>
  <reversibility rating="reversible">The verifier, manifest, and immutable fixtures are repository-local decision controls with no public API, migration, or external lock-in.</reversibility>
  <files>scripts/ci/verify_ci_critical_path.mjs, .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json, .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json, .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md — authoritative reproduced invalid cases and exact missing controls.
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md — D-04, D-08 through D-10, and D-15 through D-22 admission/safety contracts.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — immutable 24-record historical prefix and terminal grammar; do not rewrite it.
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json — current schema-1 job, artifact, threshold, and exhausted budget contract.
    - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json — current permissive synthetic fixtures to replace with exact role fixtures.
    - scripts/ci/verify_ci_critical_path.mjs — current exports, admission predicates, live job reconciliation, renderer, and CLI boundary.
    - .github/workflows/ci.yml — current post-Phase-228 restored graph and provider automation identities.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VERIFICATION.md — current live-stripe/provider-proof facts that compatibility version 2 must preserve.
  </read_first>
  <behavior>
    - RED: a synthetic record that repeats one valid candidate three times, uses push, forges required_jobs keys, and reuses one Playwright URL is rejected by the public verification path rather than returning keep:true.
    - GREEN: exactly three unique run IDs and immutable run URLs are accepted only for workflow_dispatch, attempt 1, run_live_stripe:false, one nonempty SHA, one exact workflow revision/cohort fingerprint, provider_state:non_run, successful raw conclusions, and the exact contractual job/artifact vector.
    - GREEN: required job keys equal the manifest set with no omission or extras; each stable role has one unique repository/run-bound numeric job URL; Playwright shard 1/3, 2/3, and 3/3 are distinct and cannot swap roles.
    - GREEN: the restored fixture and its single-edge candidate have separately pinned digests; verification of either state rejects every other current-workflow change, including Phase-228 provider trigger/finalizer/incident drift.
    - NEGATIVE: each verifier-invalid case named in the artifacts section fails independently, including nested objects/arrays with privacy-forbidden key shapes or values.
    - BUDGET: the existing 24 NDJSON lines remain an exact byte prefix and records carrying the phase-227-dispatch-false-v1 fingerprint can never count toward the new phase-227-gap-dispatch-false-v2 cohort.
  </behavior>
  <action>Begin with the forged end-to-end fixture through the same exported function and CLI route used for a real keep decision. Freeze the current restored workflow in the new fixture, audit all current Phase-228-added provider trigger, live-stripe, and incident surfaces, and encode restored and one-edge-candidate digests plus exact stable roles in compatibility version 2. Replace broad `>= 3`, broad event admission, arbitrary job-count, and prefix-only URL checks with exact equality and set/role checks. Bind every candidate fact to repository, run ID, run URL, candidate SHA, workflow revision, gap cohort fingerprint, physical event, dispatch input, attempt, provider state, raw conclusion, required job conclusions, expected success artifacts, advisory outcomes, and the separately linked latest provider evidence. Recursively walk every object and array before use or rendering; reject unknown schema fields and forbidden privacy shapes at any depth. Record and verify a digest for the unchanged historical ledger prefix so append-only gap records cannot rewrite the original budget, exclusions, reclassifications, or rollback. Preserve D-06 boundary arithmetic exactly and retain equal-duration observations as separate run identities in deterministic order. Add no dependency, action, workflow, retry, test masking, branch-protection change, or provider assertion.</action>
  <acceptance_criteria>The positive exact three-run fixture passes, every listed invalid case fails through the production verifier route, the restored and candidate workflow fixtures are immutable and exact, and the historical ledger prefix remains byte-identical.</acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/verify_ci_critical_path.mjs &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow-fixture .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json</automated>
  </verify>
  <fails_when>The command exits zero for any duplicate cohort, wrong topology, forged/missing/extra role, reused or role-swapped URL, nested forbidden field, historical-prefix mutation, workflow drift, or non-exact sample count; or the positive exact-role fixture fails.</fails_when>
  <done>The same end-to-end gate used for live evidence accepts one exact valid cohort and fails closed on every previously reproduced forgery or malformed shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every CLI action explicit and every terminal report byte-reproducible (D-07, D-18 through D-23)</name>
  <reversibility rating="reversible">CLI validation, deterministic rendering, and maintainer commands can be revised together without changing durable evidence semantics.</reversibility>
  <files>scripts/ci/verify_ci_critical_path.mjs, .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json, scripts/ci/README.md</files>
  <read_first>
    - scripts/ci/verify_ci_critical_path.mjs — current option(), supportedRequireFlags, action branches, and renderCriticalPathEvidence implementation.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — terminal contract_correction/candidate_reclassification/rollback grammar.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — checked terminal report that must byte-match without rewriting evidence.
    - scripts/ci/README.md — stale Phase 227 command at lines 67-70 and current Phase 228 command conventions.
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md — fact/state/owner/next-command report grammar.
  </read_first>
  <behavior>
    - Exactly one primary action is required: fixtures, verify-workflow, verify-evidence, render-evidence, or verify-live-actions.
    - Every action has an explicit option/modifier allowlist; unknown flags, stray positionals, missing or duplicate values, unsupported require modifiers, zero actions, and multiple actions exit nonzero with usage.
    - The terminal rollback ledger renders without exception and byte-matches the checked 227-CI-CRITICAL-PATH.md; a kept fixture also renders deterministically with fact, state, owner, next command, thresholds, observations, proof vector, exclusions, control, and rollback facts.
    - Rendering validates the full recursive schema/privacy and historical-prefix contract first and never edits NDJSON.
    - The README offline command uses the immutable workflow fixture; a separate command verifies the mutable live workflow against a named expected compatibility state.
  </behavior>
  <action>Replace permissive process.argv scanning with one parser that consumes each token once, rejects duplicate singleton options and missing values, and validates the action-specific matrix before doing work. Keep legacy action names only where they map unambiguously to one primary action; reject no-op combinations rather than silently succeeding. Extend renderCriticalPathEvidence to the terminal record grammar already present in the ledger and the new gap budget/kept grammar, choosing a renderer by validated terminal state rather than by missing legacy comparison records. The rendered report must lead with literal current fact, state, owner, and one exact next command per D-23, retain all old exclusions and budget closure, and distinguish raw workflow, required proof, provider proof, and decision facts. Update the Phase 227 README section with one offline fixture command, one current-workflow verification command, one byte-check command, and an explicit statement that no command grants dispatch authority. Do not mutate the ledger or hand-edit the report merely to satisfy the renderer.</action>
  <acceptance_criteria>The documented offline suite is green on the current tree, the terminal ledger reproduces the checked report byte-for-byte, a kept fixture renders stably, and every no-op/unknown/ambiguous CLI invocation exits nonzero.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow-fixture .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-evidence --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue</automated>
  </verify>
  <fails_when>The terminal renderer throws or differs by one byte, the kept fixture is nondeterministic, any malformed/no-action/multi-action CLI case exits zero, the README command reads mutable workflow state as an offline fixture, or validation writes to the NDJSON source.</fails_when>
  <done>Maintainers have one strict, documented, offline-reproducible verifier whose terminal output is derived from the durable ledger rather than maintained by hand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow/contract/fixture -> verifier | Repository-controlled text can forge graph or stable-role proof unless exact versioned compatibility is enforced. |
| GitHub evidence/NDJSON -> decision and Markdown | Nested untrusted records can spoof identity, reuse URLs, or disclose sensitive shapes before rendering. |
| CLI argv -> verification action | Typos or ambiguous action combinations can otherwise produce a successful no-op. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-24 | Spoofing | candidate/restoration required-job identities and URLs | high | mitigate | Exact key-set equality, role/name binding, repository/run URL equality, numeric job IDs, and global URL uniqueness are exercised by negative fixtures. |
| T-227-25 | Tampering | workflow fixture and compatibility contract | high | mitigate | Pin immutable restored and derived candidate digests and reject any other workflow bytes or protected Phase-228 contract drift. |
| T-227-26 | Tampering | cohort and exhausted historical budget | high | mitigate | Require exactly three unique v2 tuple records and verify the pre-gap NDJSON byte-prefix digest; v1 fingerprints are ineligible. |
| T-227-27 | Information Disclosure | nested evidence and rendered Markdown | high | mitigate | Recursively validate allowlisted schema/key/value shapes before comparison or rendering; fixtures cover nested objects and arrays. |
| T-227-28 | Repudiation | CLI action selection and terminal renderer | high | mitigate | Exactly-one-action parser rejects unknown/duplicate/inapplicable input; byte comparison binds Markdown to validated NDJSON. |
| T-227-29 | Denial of Service | offline fixture suite | low | accept | Fixtures are local bounded files and Node built-ins; no network, subprocess loop, package, or workflow execution occurs. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No package or action installation is permitted; reuse Node built-ins and existing pinned actions. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 trusted enforcement of access/control decisions | T-227-24, T-227-25, T-227-26 | Task 1 exact workflow, cohort, stable-role, URL, and historical-prefix fixtures. |
| V5.1.1 positive input/schema validation | T-227-24, T-227-26, T-227-28 | Tasks 1-2 exact sets, topology, count, enums, and CLI action/option matrix. |
| V5.2.1 sanitization before persistence/use | T-227-27 | Recursive negative fixtures run before comparison and rendering. |
| V14.2.1 trusted components | T-227-SC | Both tasks add no package/action and run the dependency-free verifier. |

All high-severity threats block completion until their mapped negative fixture fails for the intended reason and the clean control passes.
</threat_model>

<verification>
Run the immutable offline fixtures, the checked terminal byte comparison, and static Node parsing. Confirm the first 24 NDJSON records and all Phase 228 evidence remain unchanged and that no GitHub workflow was dispatched.
</verification>

<success_criteria>
- Every invalid case reproduced by 227-VERIFICATION.md is a named negative fixture that exits nonzero through the production path.
- The restored and candidate workflow states are versioned, immutable, exact, and current after Phase 228.
- The existing terminal rollback report byte-matches validated NDJSON rendering.
- Strict CLI parsing makes every successful invocation perform exactly one declared action.
- No external run, new authority, dependency, or workflow mutation occurs in this plan.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-04-SUMMARY.md` when done.
</output>

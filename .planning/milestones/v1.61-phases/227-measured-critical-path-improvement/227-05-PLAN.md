---
phase: 227-measured-critical-path-improvement
plan: "05"
type: execute
wave: 5
depends_on: [227-04]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
autonomous: true
gap_closure: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-12/D-18: the old phase-227-dispatch-false-v1 three-attempt prohibition stays exhausted and no old run is relaunched, replaced, or admitted; a new append-only authorization names phase-227-gap-dispatch-false-v2 before live work."
    - "D-13 through D-17: the new logical event class is exactly workflow_dispatch + run_live_stripe:false + attempt 1 + one candidate SHA/workflow revision/fingerprint + provider_state:non_run, with latest independent Phase 228 provider evidence linked separately."
    - "D-18/D-19/D-21: the new candidate budget is at most three created first-attempt runs, sequentially evaluated, with no reruns/replacements; any non-qualifying result closes remaining candidate slots and triggers the exact inverse."
    - "D-10/D-11/D-22: the only additional external allowance is at most one attempt-1 restoration workflow_dispatch with run_live_stripe:false after an exact inverse; it can prove rollback but can never replace a candidate observation or satisfy PATH-02."
    - "D-04 through D-06: keep occurs only after exactly three valid independent observations pass the complete vector, median <= 1,666 seconds, and maximum-observation/anomaly rule against the frozen Phase 226 baseline."
  artifacts:
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Append-only gap authorization, exact candidate observations, decision, and conditional restoration facts"
    - path: ".github/workflows/ci.yml"
      provides: "D-01 one-edge candidate or D-11 exact restored inverse"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Deterministically rendered current fact, budget, observations, decision, and next action"
  key_links:
    - from: "gap_budget_authorization:phase-227-gap-dispatch-false-v2"
      to: "GitHub Actions workflow_dispatch run IDs"
      via: "Sequential slot accounting bound to event/input/attempt/SHA/fingerprint/provider tuple"
      pattern: "authorized_candidate_runs|max_restoration_runs|consumed_run_ids|authority_closed"
    - from: ".github/workflows/ci.yml"
      to: "227-CI-CRITICAL-PATH.ndjson"
      via: "Repository-bound workflow revision, exact jobs/artifacts, durations, and decision records"
      pattern: "candidate_sha|workflow_revision|required_jobs|duration_seconds"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The spec-less probe left PATH-01 unclassified; execution uses only the locked staged path, frozen before state, and one-edge target."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The spec-less probe left PATH-02 unclassified; a passing exact-three keep decision is treated as necessary and sufficient only with the complete proof vector."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The spec-less probe left SAFE-01 unclassified; the v2 compatibility manifest is assumed to enumerate all current external identities and artifact semantics."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Boundary behavior is explicit: exactly 1,666 seconds passes, exactly 2,602 seconds is not anomalous, zero/partial/extra samples fail, and equal values retain distinct stable run identities."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT infer causal speedup from whole-workflow variance or from excluding an inconvenient observation."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT count a run with incomplete required, advisory, artifact, negative-control, or provider-state evidence as an improvement sample."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT weaken, rename, demote, or silently substitute any protected CI identity or recoverable artifact."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT authorize a fourth candidate, replacement cohort, rerun promotion, or restoration sample as candidate evidence."
---

<objective>
Run one separately authorized and exactly bounded critical-path experiment against the corrected fail-closed gate (including D-05, D-14, and D-20).

Purpose: Obtain the missing three valid observations without reopening the exhausted Plan 227-02 budget or weakening any release, host, browser, artifact, negative-control, or provider proof.
Output: An append-only v2 authorization, one-edge candidate, no more than three candidate runs, a keep decision or exact inverse, and at most one restoration-only proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
@.planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-03-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-04-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VERIFICATION.md
@.github/workflows/ci.yml
</context>

## Artifacts this plan produces

- Append-only `gap_budget_authorization` with budget ID `phase-227-gap-dispatch-false-v2`, physical event `workflow_dispatch`, input `run_live_stripe:false`, attempt 1 only, candidate ceiling 3, restoration ceiling 1, no reruns, no replacements, and no inheritance from the exhausted v1 budget.
- Append-only run records binding exact GitHub run/job/artifact URLs and staged timing to one candidate SHA, one workflow revision, one cohort fingerprint, and provider state `non_run`.
- Exactly one terminal `gap_decision` of `kept`, `rollback_verified`, or `rollback_applied_unverified`; only `kept` satisfies PATH-02.

<tasks>

<task type="tracer">
  <name>Task 1: Trace one unspent v2 budget slot through authorization, static evidence, and a zero-dispatch close (D-12 through D-23)</name>
  <reversibility rating="reversible">The authorization is an append-only bounded ledger fact; before a run is created it can close unspent without changing CI or external state.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md — D-12 through D-23 trigger, vector, budget, classification, rollback, and report contracts.
    - .planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md — explicit terminal prohibition against a fourth/replacement run under the v1 budget.
    - .planning/phases/227-measured-critical-path-improvement/227-03-SUMMARY.md — exhausted restoration authority and rollback_applied_unverified history.
    - .planning/phases/227-measured-critical-path-improvement/227-04-SUMMARY.md — hardened schema, strict CLI, compatibility-v2, and historical-prefix outputs.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — historical prefix to preserve and extend.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — latest independent provider outcome to link, never count as a candidate.
  </read_first>
  <behavior>
    - The new authorization is distinguishable from phase-227-dispatch-false-v1 and explicitly states that v1 is exhausted, immutable, and contributes zero v2 observations.
    - Candidate authority is three created attempt-1 runs maximum; restoration authority is one created attempt-1 run maximum and becomes available only after an exact inverse.
    - Both authorities prohibit reruns and replacements; no negative-control dispatch is authorized because the existing immutable control is read-only revalidated.
    - The logical event class tuple and required proof vector are fully represented before any workflow mutation or dispatch.
    - Closing the fresh authorization with zero consumed slots is valid operational evidence but cannot produce keep or satisfy PATH-02.
  </behavior>
  <action>Append the new authorization record without altering any existing NDJSON byte. Give it the unique new budget ID named above, authorized-at timestamp, owner, exact physical/logical event tuple, fixed input/provider intent, candidate and restoration ceilings, no-rerun/no-replacement flags, zero initial consumption, old-budget noninheritance, exact threshold/frozen-input references, existing negative-control run ID for later read-only reconciliation, and the Phase 228 provider-evidence link with its literal failed/selected_assertions_failed outcome. Render the new authorized-but-unspent current fact with owner and exactly one next command that performs static candidate preflight only. Use the strict verifier to prove prefix integrity, schema/privacy, budget arithmetic, and zero-run non-claim. Do not touch the workflow, push a ref, dispatch a run, or rewrite the Phase 228 record.</action>
  <acceptance_criteria>The v2 authorization exists and is byte-appended after all historical records, grants at most three candidate plus one conditional restoration run, consumes zero runs, links provider evidence honestly, and cannot pass a keep requirement.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-evidence --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue</automated>
  </verify>
  <fails_when>Any historical byte changes; the new record inherits or reopens v1 authority; either ceiling is absent/unbounded; rerun or replacement is allowed; a run is consumed; provider evidence is called proved; or the unspent state passes require-kept.</fails_when>
  <done>A maintainer can audit the new event-class and finite evidence authority before any external effect, while the original prohibition remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Apply and commit only the v2 one-edge candidate after complete preflight (D-01 through D-17)</name>
  <reversibility rating="reversible">The candidate changes one needs entry and its literal inverse restores the current post-Phase-228 workflow contract.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .github/workflows/ci.yml — current restored host, Playwright, annotation, and provider automation graph.
    - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml — exact restored source and one-edge derivation boundary.
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json — compatibility-v2 candidate/restored digests and exact proof vector.
    - scripts/ci/verify_ci_critical_path.mjs — strict verify-workflow/preflight actions.
    - scripts/ci/README.md — exact offline, current-workflow, and inherited preservation commands.
  </read_first>
  <behavior>
    - Before the patch, current workflow bytes match the v2 restored state; after it, only host-integration.needs removes admin-drift-docs and matches the v2 candidate state.
    - Playwright still depends on host integration; all independent release/admin/host/browser/Docker work and annotation-sweep aggregate fan-in remain exact.
    - Provider trigger, live-stripe, incident reconciliation, stable identities, matrices, artifact names/conditions/retention, retries, and permissions remain unchanged.
    - Candidate preflight binds the committed candidate SHA and workflow revision to the unspent v2 authorization without consuming a run.
    - The existing negative-control run and Phase 228 provider evidence pass read-only repository-bound reconciliation before dispatch authority becomes consumable.
  </behavior>
  <action>First prove the working workflow exactly matches the restored versioned-compatibility digest and the complete local/inherited suite is green. Change only host-integration.needs per D-01, leaving every other byte and all D-02/D-03 safety edges intact. Append a preflight record naming the resulting candidate workflow digest/fingerprint and the rule that the exact committed SHA is captured after this task commit and before the first dispatch; do not attempt a self-referential SHA inside the commit. Re-render the current fact and verify the candidate state, historical prefix, authorization, controlled-negative record, Phase 226 frozen evidence, provider fixtures, setup diagnostics, Phase 225 evidence, and Phase 228 provider link. The executor's task commit becomes the only candidate SHA eligible for Task 3; no later source or evidence commit may be included in the candidate ref.</action>
  <acceptance_criteria>The committed diff changes the single authorized needs entry plus append-only preflight/report evidence, all local and read-only external gates pass, and the v2 budget remains unconsumed with an exact candidate commit ready for dispatch.</acceptance_criteria>
  <verify>
    <automated>git diff --check -- .github/workflows/ci.yml .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-workflow --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-state candidate &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-evidence --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh</automated>
  </verify>
  <fails_when>The workflow is not the exact restored source before editing or exact candidate after; any second workflow byte changes; any inherited gate fails; the existing control/provider link cannot be reconciled; a run is created; or evidence authority is consumed before the candidate commit exists.</fails_when>
  <done>The exact candidate is a committed, fully preflighted SHA with three untouched v2 candidate slots and a known literal inverse.</done>
</task>

<task type="auto">
  <name>Task 3: Spend no more than three candidate slots and keep or exactly restore (D-04 through D-23)</name>
  <precondition>`gh auth status` succeeds for repository `szTheory/accrue`, the current committed HEAD is the Task 2 candidate SHA, and the v2 authorization reports zero consumed candidate runs.</precondition>
  <reversibility rating="costly">Workflow runs and immutable external URLs cannot be un-created, so the finite budget and append-only ledger make each spend auditable; the graph change itself remains exactly reversible.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — v2 authorization/preflight and all immutable v1 history.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — current authorized candidate fact and exact next command.
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json — exact event tuple, role/artifact vector, thresholds, and candidate/restoration ceilings.
    - scripts/ci/verify_ci_critical_path.mjs — strict live reconciliation, decision, and renderer actions.
    - .github/workflows/ci.yml — committed candidate that must not change between candidate runs.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — separate provider evidence, never a candidate observation.
  </read_first>
  <behavior>
    - Before each dispatch, read-only reconciliation counts created v2 runs from both ledger and GitHub; count >= 3 or any tuple mismatch fails closed without dispatch.
    - Each slot creates at most one workflow_dispatch attempt-1 run with run_live_stripe:false at the exact Task 2 SHA; reruns, replacements, concurrent substitutes, and other events are never candidates.
    - Runs are sequential: reconcile and append every terminal proof vector before considering the next slot; any non-qualifying or safety failure closes unused slots and starts rollback immediately.
    - Three qualifying observations retain individual durations and URLs, stable ordering, exact range/median, host DAG wait, workflow context, exclusions, and complete proof vectors; keep requires median <= 1,666 and the D-06 maximum/anomaly rule.
    - A rejected candidate applies the exact inverse, closes candidate authority, and may create at most one restoration-only attempt-1 workflow_dispatch with run_live_stripe:false; that run never enters the timing cohort.
    - The terminal decision closes all v2 authority. `kept` contains exactly three valid observations; rollback states explicitly leave PATH-02 unmet and do not authorize another plan-02 or v2 attempt.
  </behavior>
  <action>Bind a temporary candidate ref to the exact Task 2 SHA, asserting the remote name is absent before creation. Dispatch sequentially with the explicit false Boolean input. For each created run, bind the unique run ID inside a narrow recorded dispatch window, wait for terminal state, fetch attempt-specific workflow/job/artifact facts read-only, run the strict live verifier, append the complete sanitized record, and re-render before any next slot. Never print or persist actors, refs beyond the sanitized temporary-ref lifecycle, logs, payloads, secrets, or artifact contents. Stop candidate dispatch immediately on any invalid observation, safety drift, control failure, or failed timing decision. If all three are valid, calculate the locked median/range/maximum predicates and append `gap_decision:kept` only on full success. Otherwise restore the exact newly versioned inverse, commit it, run all local gates, optionally spend the sole restoration slot with false provider input, append its literal rollback state, and leave PATH-02 unmet. Remove every temporary ref after run binding/decision. On resumption, derive remaining authority from ledger plus GitHub; never repeat a consumed slot. No branch-protection edit, cancellation, rerun, replacement, retry, test deletion, gate demotion, cache change, or new workflow is permitted.</action>
  <acceptance_criteria>The ledger ends with either exactly three valid v2 candidate observations and `kept`, or an exact inverse plus no more than one restoration-only observation and an honest rollback state; total newly created external runs never exceeds four and old v1 runs contribute zero samples.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-final-decision</automated>
  </verify>
  <fails_when>More than three candidate or one restoration run is created; any run is a rerun/replacement/wrong tuple; an invalid observation is followed by another candidate; old evidence is altered or counted; temporary refs remain; proof is incomplete; the graph is neither exact candidate-kept nor exact restored; or a rollback result passes require-kept.</fails_when>
  <done>The finite v2 authority is closed with repository-bound evidence and either the missing passing keep decision or an exact, honestly incomplete rollback—never an expanded sampling loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local authorization/committed SHA -> GitHub dispatch | External run creation is irreversible and must consume one exact auditable slot. |
| GitHub run/job/artifact API -> NDJSON | Remote facts are untrusted until repository, SHA, attempt, event, roles, URLs, artifacts, and provider state agree. |
| Terminal evidence -> keep/rollback workflow state | A false decision could leave a weakened graph active or claim improvement without three valid samples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-30 | Elevation of Privilege | v1 and v2 run authority | high | mitigate | Separate budget IDs, immutable-prefix verification, zero inheritance, ledger+GitHub consumption count, and hard ceilings prevent old/new authority expansion. |
| T-227-31 | Spoofing | candidate SHA/event/input/attempt/provider tuple | high | mitigate | Bind each dispatch window and run to the committed Task 2 SHA and exact v2 tuple through attempt-specific live API reconciliation. |
| T-227-32 | Tampering | one-edge graph and stable evidence contract | high | mitigate | Compatibility-v2 verifier permits only candidate or exact inverse and rechecks all required identities/artifacts before every external spend and decision. |
| T-227-33 | Repudiation | exclusions, failure classifications, and slot consumption | high | mitigate | Sequential append-before-next-slot accounting retains every outcome, classification, original run ID, and terminal authority closure. |
| T-227-34 | Information Disclosure | live API results and durable records | high | mitigate | Persist only schema-allowlisted identities/timings/outcomes/immutable URLs; recursive privacy validation runs before every append/render. |
| T-227-35 | Denial of Service | external workflow budget | medium | mitigate | Candidate ceiling 3 plus conditional restoration ceiling 1, no concurrency/retries/replacements, and stop-on-first-invalid bound cost and load. |
| T-227-36 | Repudiation | provider proof interpretation | high | mitigate | Measurement records literal non_run and link the separate Phase 228 failed provider outcome; workflow success never implies provider proof. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No install is allowed; use existing gh/Node tooling and pinned actions. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 trusted enforcement of access/control decisions | T-227-30, T-227-31, T-227-32, T-227-36 | Tasks 1-3 bind budget, committed SHA, exact workflow state, and provider interpretation before decision. |
| V5.1.1 positive input/schema validation | T-227-31, T-227-33, T-227-35 | Strict live verifier checks exact tuple, slot counts, ordering, and finite terminal states. |
| V5.2.1 sanitization before persistence/use | T-227-34 | Recursive privacy/schema verification precedes append and render. |
| V14.2.1 trusted components | T-227-SC | No new package/action; existing pinned tools only. |

Every high-severity threat blocks dispatch, further sampling, keep, or rollback certification until the mapped check passes.
</threat_model>

<verification>
Verify the v2 authorization before mutation, candidate workflow before dispatch, each live run before the next slot, and the terminal decision against GitHub. Confirm the new-created-run count is at most four, old fingerprints contribute zero, and the final workflow is candidate only for a passing kept decision.
</verification>

<success_criteria>
- The original three-attempt prohibition is unchanged and no Plan 227-02 run is repeated or replaced.
- The new event tuple and finite 3+conditional-1 budget are recorded before use and closed after decision.
- Exactly three valid v2 candidate observations at one SHA/fingerprint/provider state produce the only allowed kept result.
- Any invalid or failed predicate stops further candidate sampling and restores the exact current inverse.
- Every run, exclusion, artifact, provider state, decision, and temporary-ref lifecycle is repository-bound, sanitized, append-only, and rendered.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-05-SUMMARY.md` when done.
</output>

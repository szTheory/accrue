---
phase: 227-measured-critical-path-improvement
plan: "06"
type: execute
status: superseded
wave: 6
depends_on: [227-05]
files_modified:
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04 through D-07/D-23: the final maintainer report begins with the literal kept fact, state, owner, and one verification command, then shows the frozen before median/p95, all three v2 observations, range/median, exact fingerprints, exclusions, proof vector, control, and rollback procedure."
    - "PATH-02: require-kept succeeds only for exactly three repository-bound v2 observations with a passing <=1,666-second median and maximum-observation predicate; a rollback terminal cannot enter this plan."
    - "SAFE-01/SAFE-02: the full inherited release/provider/setup/required-lane controls and the corrected adversarial verifier suite are green without test deletion, retry masking, identity/artifact weakening, or old-evidence mutation."
    - "227-VALIDATION.md is an executed, current task map with status:validated, wave_0_complete:true, nyquist_compliant:true, exact 227-04 through 227-06 commands, measured results, and no pending/MISSING rows."
  artifacts:
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Byte-generated kept comparison and full recoverability account"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md"
      provides: "Green Nyquist validation sign-off for all gap tasks and requirements"
  key_links:
    - from: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      to: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      via: "Strict render action followed by byte-verifying evidence action"
      pattern: "gap_decision.*kept|eligible observations|median"
    - from: "227-VALIDATION.md"
      to: "repository verifier and inherited suites"
      via: "Executed commands and task/threat mapping"
      pattern: "227-04-01|227-05-03|227-06-02|nyquist_compliant"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The unclassified spec-less PATH-01 edge remains flagged; sign-off relies on the locked one-edge/staged-path/frozen-before-state acceptance boundary."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The unclassified spec-less PATH-02 edge remains flagged; sign-off requires the exact kept predicates and never infers success from rollback or partial evidence."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The unclassified spec-less SAFE-01 edge remains flagged; sign-off treats compatibility-v2 exactness plus live role/artifact reconciliation as the preservation boundary."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency/degenerate/ordering assumptions remain recorded and fixture-covered: inclusive thresholds, exact sample cardinality, and stable distinct run ordering."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT claim causality or improvement from a partial, selectively filtered, or whole-workflow-only comparison."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT mark PATH-02 complete when require-kept or any protected proof gate is non-green."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT certify preservation by replacing an identity, artifact, condition, label, or provider outcome with a weaker surrogate."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT hide a failed run, rerun, budget expansion, rollback gap, or disabled test in validation prose."
---

<objective>
Seal the kept v2 result as a byte-reproducible maintainer report and green Nyquist validation map (D-04 through D-11, D-15 through D-23).

Purpose: Convert the passing, bounded live result into deterministic completion evidence without letting documentation or validation overclaim a rollback/partial branch.
Output: Final generated report and executed `227-VALIDATION.md` covering every new task, threat, requirement, and regression command.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
@.planning/phases/227-measured-critical-path-improvement/227-04-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-05-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
</context>

## Artifacts this plan produces

- A checked-in `227-CI-CRITICAL-PATH.md` generated byte-for-byte from the terminal kept NDJSON grammar.
- A current `227-VALIDATION.md` mapping `227-04-01` through `227-06-02`, T-227-24 through T-227-40, PATH-01/PATH-02/SAFE-01/SAFE-02, exact commands/results, and green Nyquist frontmatter.
- No executor-authored replacement for the verifier-owned `227-VERIFICATION.md`; re-verification remains the next workflow step after this plan.

<tasks>

<task type="tracer">
  <name>Task 1: Trace the kept decision from NDJSON through byte-generated maintainer proof (D-04 through D-07, D-15 through D-23)</name>
  <precondition>The latest v2 terminal record is `gap_decision: kept`, require-kept succeeds, exactly three v2 candidate observations exist, and `.github/workflows/ci.yml` is the compatibility-v2 candidate state.</precondition>
  <reversibility rating="reversible">The Markdown is wholly regenerated from immutable NDJSON and can be reproduced without changing evidence.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — authoritative terminal kept facts and all historical exclusions.
    - scripts/ci/verify_ci_critical_path.mjs — validated renderer and strict require-kept/live actions.
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md — D-04 through D-07 thresholds and D-23 report order.
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md — frozen before median/p95 and maintainer evidence grammar.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — separate literal provider result to link without reinterpretation.
  </read_first>
  <behavior>
    - Rendering begins with current fact, `kept`, owner, and one exact verification command.
    - The report lists frozen 2,083-second median and 2,602-second p95, threshold 1,666 seconds, each of the three v2 observations, stable range/median, DAG wait, workflow context, exact SHA/revision/fingerprint, and sample count.
    - Required/advisory/artifact/provider facts, existing controlled-negative evidence, all exclusions/classifications, old/new budget closure, and literal rollback procedure remain visible.
    - The separate Phase 228 provider result remains failed/selected_assertions_failed while measurement provider state remains non_run; neither is inferred from workflow success.
    - Repeated rendering is byte-identical and live require-kept reconciliation passes for all recorded URLs and roles.
  </behavior>
  <action>Invoke the validated renderer from the terminal NDJSON rather than editing prose. Run strict offline evidence verification, byte comparison, require-kept, and live reconciliation. Confirm report ordering and all D-23 sections from the generated output, including the exact inverse procedure even though the result is kept. Preserve the historical rollback narrative and exclusions as history while making the leading current fact unambiguously the newly authorized kept decision. Do not change the ledger, workflow, Phase 226 baseline, Phase 228 evidence, or verifier-owned 227-VERIFICATION.md.</action>
  <acceptance_criteria>The generated Markdown is byte-stable, complete, privacy-safe, live-reconciled, and states a kept result only because every exact-three timing and proof predicate passes.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-kept</automated>
  </verify>
  <fails_when>The decision is not kept; sample count is not exactly three; any live role/artifact/URL differs; provider proof is inferred; an exclusion/budget/rollback procedure disappears; recursive privacy fails; or repeated rendering differs by one byte.</fails_when>
  <done>A maintainer can reproduce and audit the complete faster-path claim from sanitized immutable records with one command.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the complete Nyquist map and mark only measured green results (D-08 through D-11, PATH-01, PATH-02, SAFE-01, SAFE-02)</name>
  <reversibility rating="reversible">Validation metadata records executed evidence and can be corrected without changing production or external contracts.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md — stale draft rows and Wave 0 status to replace with actual gap-task evidence.
    - .planning/phases/227-measured-critical-path-improvement/227-04-SUMMARY.md — exact local hardening commands and results.
    - .planning/phases/227-measured-critical-path-improvement/227-05-SUMMARY.md — exact authorization, live run IDs, budget, and terminal decision results.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — byte-generated kept report and reproduction command.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VERIFICATION.md — current provider boot/evidence preservation result.
  </read_first>
  <behavior>
    - Validation rows cover 227-04-01, 227-04-02, 227-05-01, 227-05-02, 227-05-03, 227-06-01, and 227-06-02 with exact commands, requirements, threats, and actual pass results.
    - The full suite includes strict critical-path fixtures, terminal byte/live/kept verification, frozen baseline records/render, provider fixtures, Stripe boot evidence fixtures, setup diagnostics, Phase 225 required-lane evidence, and focused release/live-proof tests.
    - Status becomes validated, wave_0_complete true, and nyquist_compliant true only after every command passes in this execution; there are no pending, MISSING, skipped, or manual-only rows.
    - Validation explicitly records new-run counts, no-rerun/no-replacement closure, historical-prefix preservation, exact candidate workflow state, and zero unverified behavior claims.
  </behavior>
  <action>Replace the stale pre-gap task map with all seven additive task IDs and their actually executed commands/results. Run the full local and live verification suite from repository root, using the commands that passed in Plans 04-06 and the existing Phase 226/228 verifier commands. Record measured runtime without inventing test counts. Map T-227-24 through T-227-40 to concrete negative or live checks and map each of PATH-01, PATH-02, SAFE-01, and SAFE-02 to at least one behavioral command. Set green frontmatter and sign-off checkboxes only after every required command returns zero; otherwise leave the document non-compliant and surface the exact failing command. Do not mark the old verifier-owned report passed, rewrite requirement status directly, or treat a rollback as successful phase closure.</action>
  <acceptance_criteria>227-VALIDATION.md is current, internally consistent, fully automated, and green for every additive task, high-severity threat, requirement, and terminal kept behavior with no fabricated or pending evidence.</acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/verify_ci_critical_path.mjs &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow-fixture .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-kept &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/docs/release_guidance_test.exs test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; rg -q '^status: validated$' .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md &amp;&amp; rg -q '^nyquist_compliant: true$' .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md &amp;&amp; rg -q '^wave_0_complete: true$' .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md</automated>
  </verify>
  <fails_when>Any command fails or is omitted; validation retains draft/non-compliant/pending/MISSING/manual-only state; any additive task, requirement, or high threat lacks an executed mapping; the workflow is restored rather than kept; or the document claims the verifier-owned re-verification already passed.</fails_when>
  <done>Phase 227 has green, current Nyquist evidence for the validated kept improvement and is ready for an independent `$gsd-verify-work 227` re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal NDJSON -> maintainer report | Generated prose can omit failures or overstate provider/causal facts unless byte-bound to validated records. |
| Commands/results -> validation metadata | Documentation can claim green coverage without executing the complete behavioral suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-37 | Repudiation | kept Markdown claim | high | mitigate | Generate only from validated NDJSON, require exact three/live keep, and byte-compare checked output. |
| T-227-38 | Tampering | Nyquist task/threat/requirement map | high | mitigate | Enumerate all seven task IDs, all high threats, all four requirements, exact commands, and actual results before green frontmatter. |
| T-227-39 | Information Disclosure | final report | high | mitigate | Recursive privacy/schema validation precedes rendering; report contains sanitized facts and immutable URLs only. |
| T-227-40 | Spoofing | phase completion state | high | mitigate | require-kept plus exact candidate workflow and full inherited suite must pass; executor does not rewrite verifier-owned verification status. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No dependency/action changes; execute existing pinned/local tooling only. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 trusted enforcement of access/control decisions | T-227-37, T-227-40 | Task 1 live require-kept and exact workflow state gate the report and plan entry. |
| V5.1.1 positive input/schema validation | T-227-37, T-227-38, T-227-40 | Tasks 1-2 run exact record, task-map, requirement, and status checks. |
| V5.2.1 sanitization before persistence/use | T-227-39 | Strict recursive validation runs before final render. |
| V14.2.1 trusted components | T-227-SC | No installs; full suite uses existing tools. |

Every high-severity threat blocks green validation or completion.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | One demonstrably faster critical path with equal identifiable/recoverable release, host, browser, and provider proof | 04-06 | COVERED | Harden first, run bounded experiment, then seal only a kept result. |
| REQ | PATH-01 | Measured path, selected edge, before evidence, documented rollback | 04-06 | COVERED | Exact v2 contract, one-edge candidate, frozen baseline, generated report. |
| REQ | PATH-02 | Validated reduction without proof removal | 05-06 | COVERED | Exactly three valid observations plus threshold and full-vector keep gate. |
| REQ | SAFE-01 | Stable required-check identities and artifacts | 04-06 | COVERED | Exact roles/artifacts/conditions plus live reconciliation and inherited suite. |
| REQ | SAFE-02 | Negative control, rollback, and no masking | 04-06 | COVERED | Adversarial fixtures, retained live control, exact inverse, finite no-retry budget. |
| RESEARCH | — | One-edge graph, frozen before cohort, repository-bound sanitized evidence, narrow anomaly standard | 04-06 | COVERED | Current paths/symbols are re-audited against post-Phase-228 code; stale full-workflow digest is replaced by compatibility v2. |
| RESEARCH | — | Exact manifest, deterministic renderer, maintainer commands, ASVS input/privacy controls | 04, 06 | COVERED | Immutable fixture, recursive schema, strict CLI, byte rendering, validation map. |
| CONTEXT | D-01-D-03 | One host dependency edge; independent lanes and final fan-in preserved | 05 | COVERED | Candidate and exact inverse only. |
| CONTEXT | D-04-D-07 | Exact three-run staged metric, thresholds, separate evidence pack | 04-06 | COVERED | Cardinality/arithmetic fixtures, bounded live cohort, generated report. |
| CONTEXT | D-08-D-11 | Complementary controls, exact manifest, rollback triggers/inverse proof | 04-06 | COVERED | Offline/live control, role/artifact contract, stop-and-restore branch. |
| CONTEXT | D-12-D-17 | Restored-first manual-false event topology and complete vector | 04-05 | COVERED | Current restored fixture, v2 tuple, preflight, exact live reconciliation. |
| CONTEXT | D-18-D-22 | Preserve old exhaustion; classify visibly; no replacement; staged rollback | 04-05 | COVERED | Historical-prefix digest and separate finite 3+conditional-1 authority. |
| CONTEXT | D-23 | Fact/state/owner/one-command report grammar | 04-06 | COVERED | Terminal and kept byte-render fixtures plus final report. |
| CONTEXT | Deferred ideas | Matrix/branch protection/cache/test/provider/product/UI expansions | — | EXCLUDED | No task implements them. |

<verification>
Enter this plan only on a terminal kept decision. Byte-render the complete report, execute the full local/live/inherited suite, and green the validation map only from actual results. Independent verifier re-run happens after execution.
</verification>

<success_criteria>
- The report is a deterministic projection of the exact kept ledger and includes every required fact and rollback procedure.
- All seven additive tasks, four requirements, and T-227-24 through T-227-40 have executed automated evidence.
- `227-VALIDATION.md` is validated, wave-0 complete, and Nyquist-compliant with no pending rows.
- The current workflow remains the exact candidate only because require-kept is green.
- The authoritative `227-VERIFICATION.md` is not overwritten by the executor.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-06-SUMMARY.md` when done.
</output>

---
phase: 227-measured-critical-path-improvement
plan: "07"
type: execute
wave: 7
depends_on: [227-05]
files_modified:
  - scripts/ci/verify_ci_critical_path.mjs
  - scripts/ci/verify_ci_critical_path.test.mjs
  - scripts/ci/preflight_phase227_candidate.sh
  - .planning/phases/227-measured-critical-path-improvement/227-PREFLIGHT-CONTROL.json
  - .planning/phases/227-measured-critical-path-improvement/227-CANDIDATE-PREFLIGHT.json
  - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
  - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01 through D-03: the prepared candidate changes only host-integration.needs, while Playwright, final fan-in, identities, artifacts, provider behavior, retries, and every other workflow byte remain protected."
    - "D-04 through D-07: a newly named cohort can satisfy PATH-02 only with exactly three successful, unique, attempt-1 manual-false runs at one commit and the locked staged-path thresholds."
    - "D-08 through D-11: immutable fixtures, the existing live negative control, the literal inverse, and a conditional one-run restoration allowance remain enforceable before external authority opens."
    - "D-12 through D-22: all earlier run budgets remain immutable and closed; the new authority is finite, initially unspent, fail-closed, and cannot be consumed until the exact committed candidate passes local preflight."
    - "D-23: the generated report states current fact, literal state, owner, and one exact next command without implying that local preparation is live proof."
  artifacts:
    - path: "scripts/ci/preflight_phase227_candidate.sh"
      provides: "Isolated-tree, CI-pinned candidate preflight with no remote side effects"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CANDIDATE-PREFLIGHT.json"
      provides: "Durable machine-verified binding of the exact candidate commit/tree to passing local checks and a zero-remote-effect wrapper"
    - path: "scripts/ci/verify_ci_critical_path.mjs"
      provides: "Versioned run-authority, reservation, consumption, decision, and kept-only validation"
      exports: ["verifyWorkflowContract", "verifyComparisonEvidence", "verifyFinalDecision", "renderCriticalPathEvidence", "verifyFixtures"]
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Append-only phase-227-gap-dispatch-false-v3 authorization with zero consumed slots"
    - path: ".github/workflows/ci.yml"
      provides: "Prepared one-edge candidate based on the repaired source tree"
  key_links:
    - from: "scripts/ci/preflight_phase227_candidate.sh"
      to: ".github/workflows/ci.yml"
      via: "detached candidate commit verification plus CI-pinned Accrue format and strict verifier contracts"
      pattern: "mix format --check-formatted|verify-workflow|verify_ci_critical_path.test.mjs"
    - from: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      to: "scripts/ci/verify_ci_critical_path.mjs"
      via: "versioned authority state machine that rejects prior fingerprints, duplicate reservations, and non-kept closure"
      pattern: "phase-227-gap-dispatch-false-v3|require-kept"
---

<objective>
Prepare a new, auditable Phase 227 measurement authority and exact candidate without creating any remote run (D-01 through D-23).

Purpose: Supersede the blocked kept-only route in 227-06 with a repaired, fail-closed local gate that proves the precise candidate tree before a separately bounded external experiment.
Output: Versioned authority validation, an isolated candidate-preflight script, zero-consumption authorization records, and one committed dependency-edge candidate.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-RESEARCH.md
@.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
@.planning/phases/227-measured-critical-path-improvement/227-04-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-05-SUMMARY.md
@.planning/debug/resolved/phase227-format-verifier-gap.md
@scripts/ci/verify_ci_critical_path.mjs
@scripts/ci/verify_ci_critical_path.test.mjs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a zero-run authority through exact-tree preflight and kept-only rejection (D-04, D-08 through D-11, D-15 through D-22)</name>
  <reversibility rating="reversible">The verifier, fixtures, and preflight wrapper are repository-local controls with no remote or published effect.</reversibility>
  <files>scripts/ci/verify_ci_critical_path.mjs, scripts/ci/verify_ci_critical_path.test.mjs, scripts/ci/preflight_phase227_candidate.sh, .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json, .planning/phases/227-measured-critical-path-improvement/227-PREFLIGHT-CONTROL.json</files>
  <read_first>
    - .planning/debug/resolved/phase227-format-verifier-gap.md — repaired formatter and kept-only contracts plus the no-dispatch boundary.
    - .planning/phases/227-measured-critical-path-improvement/227-05-SUMMARY.md — closed earlier authority and exact restored state.
    - scripts/ci/verify_ci_critical_path.mjs — current strict parser, hard-coded prior authority grammar, rendering, and live reconciliation.
    - scripts/ci/verify_ci_critical_path.test.mjs — focused regression that proves rollback cannot satisfy kept-only verification.
    - .github/workflows/ci.yml — restored graph and exact CI-pinned format command.
  </read_first>
  <behavior>
    - A synthetic new authorization begins with zero reserved and zero consumed candidate/restoration slots; every earlier budget and fingerprint remains closed and contributes no sample.
    - The state machine permits at most one open reservation, requires reservation before dispatch, requires one uniquely bound run ID immediately after acceptance, and forbids another reservation until the bound run is terminal and classified.
    - Resume reconciliation adopts an already-created matching run instead of dispatching again; zero or multiple ambiguous matches closes authority and cannot silently spend another slot.
    - Candidate authority is at most three attempt-1 workflow_dispatch runs with run_live_stripe:false at one commit/fingerprint; restoration authority is at most one attempt-1 manual-default-true run and opens only after the exact inverse.
    - The isolated preflight accepts only a full 40-hex commit, creates a detached disposable worktree at exactly that commit, records the resolved commit/tree and check results, and rejects dirty-tree substitution or a different revision.
    - A structural and runtime harness independently proves the wrapper cannot push, dispatch/rerun, mutate any remote-tracking or server ref, or invoke a network-capable command; the harness fails closed on every attempted prohibited executable/subcommand and asserts its invocation log is empty.
    - The wrapper runs Node syntax/tests/fixtures, workflow verification, CI-pinned Accrue formatting, and the focused repaired Accrue test solely inside the isolated exact-commit worktree, then writes schema-validated pass evidence.
    - The recorded rollback terminal still fails --require-kept for the semantic reason `v2 terminal decision is not kept`; a new unspent authorization also cannot pass kept-only verification.
  </behavior>
  <action>Extend the existing dependency-free verifier and contract with a new authority version and explicit reservation, consumption, terminal-classification, keep, and restoration transitions. Keep all existing ledger bytes and prior terminal validators valid. Add adversarial tests for duplicate/open reservations, pre-existing matching runs on resume, ambiguous run discovery, consumed-count drift, old-fingerprint admission, fourth candidate, restoration-before-inverse, restoration-as-candidate, and rollback passing the kept-only modifier. Create a Bash preflight wrapper that accepts `--commit`, `--expected-state`, and `--evidence-out`; requires a full commit SHA; uses `mktemp -d` plus a detached git worktree at that exact object; installs no package; cleans only that temporary worktree; and executes the exact CI-pinned formatter and current verifier contracts against committed bytes. Extend the Node tests with two independent protections: a structural scan that rejects push, workflow dispatch, run rerun, remote-ref mutation, and network-capable command paths, and a runtime sandbox that shadows `gh`, `curl`, `wget`, `ssh`, `scp`, and network-capable `git` subcommands with fail-and-log shims while permitting only the local git operations required by the wrapper. Run the wrapper through that sandbox against the current full restored commit, assert the prohibited-invocation log is empty, and persist `227-PREFLIGHT-CONTROL.json` containing the exact commit/tree, wrapper digest, expected state, check results, and `remote_effects: none`. Add verifier support that rejects missing, stale, mismatched, non-passing, or structurally incomplete preflight evidence. Preserve the current strict CLI and recursive privacy checks.</action>
  <acceptance_criteria>The production verifier and focused tests prove the finite state machine and kept-only semantics; an explicit wrapper invocation succeeds against a full restored commit in a detached worktree; the independent structural/runtime harness records zero prohibited invocations; and durable control evidence validates.</acceptance_criteria>
  <verify>
    <automated>CONTROL_SHA=$(git rev-parse HEAD) &amp;&amp; test ${#CONTROL_SHA} -eq 40 &amp;&amp; bash -n scripts/ci/preflight_phase227_candidate.sh &amp;&amp; bash scripts/ci/preflight_phase227_candidate.sh --commit "$CONTROL_SHA" --expected-state inverse_rollback --evidence-out .planning/phases/227-measured-critical-path-improvement/227-PREFLIGHT-CONTROL.json &amp;&amp; node --check scripts/ci/verify_ci_critical_path.mjs &amp;&amp; node --test scripts/ci/verify_ci_critical_path.test.mjs &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-preflight-evidence --preflight-evidence .planning/phases/227-measured-critical-path-improvement/227-PREFLIGHT-CONTROL.json --candidate-sha "$CONTROL_SHA" --expected-state inverse_rollback --require-no-remote-effects &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow-fixture .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json</automated>
  </verify>
  <fails_when>Any prior evidence is rewritten; a closed fingerprint becomes admissible; a duplicate or ambiguous reservation can lead to another dispatch; a restoration can count as a candidate; rollback satisfies kept-only verification; the wrapper accepts a non-full or substituted commit; the structural scan admits push/dispatch/rerun/remote-ref/network capability; the runtime sandbox logs any prohibited invocation; or the durable control evidence does not validate.</fails_when>
  <done>The local gate can safely govern a fresh finite authority, and separately machine-verified exact-commit plus zero-remote-effect contracts have durable passing evidence before candidate preparation proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append a newly bounded zero-consumption authorization (D-04 through D-07, D-12 through D-23)</name>
  <reversibility rating="costly">The authorization is locally append-only and unspent, but later external consumption will create immutable run URLs; its finite ceilings and state transitions make that future cost auditable.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — immutable history and both closed prior budgets.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — current honest rollback report.
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md — D-04 through D-07 and D-12 through D-23.
    - scripts/ci/verify_ci_critical_path.mjs — new authority validator and deterministic renderer.
  </read_first>
  <behavior>
    - The new budget ID is phase-227-gap-dispatch-false-v3 and explicitly supersedes 227-06 without reopening either closed predecessor.
    - Candidate ceiling is three, restoration ceiling is one conditional slot, reruns/replacements/concurrency are false, and reserved/consumed lists are empty.
    - Remote effects remain disabled until an append-only activation record names the exact committed candidate SHA/tree and successful isolated preflight.
    - The authorization retains the existing negative-control run, frozen Phase 226 facts, Phase 228 provider result, thresholds, failure classes, owner, and stop-on-first-nonqualifying policy.
    - The rendered report states `authorized_pending_candidate`, PATH-02 unmet, zero consumption, and one local-only next command.
  </behavior>
  <action>Add immutable authority fixtures for the new reservation/activation grammar, including rejection of missing or mismatched preflight-evidence bindings. Append the uniquely named authorization and deterministic report state after the complete historical ledger. Record the physical/logical event tuple, exact ceilings, closed predecessor budget references, no-rerun/no-replacement/no-concurrency rules, reservation-before-dispatch and bind-before-wait rules, conditional restoration purpose, thresholds, negative-control link, owner, and remote-effects-disabled state. Render from validated NDJSON. Do not edit an existing line, infer provider proof, add a run ID, invoke GitHub, or expose a dispatch command as the current next action.</action>
  <acceptance_criteria>The ledger/report validate with zero slots reserved or consumed, closed historical authority remains immutable, PATH-02 remains unmet, and kept-only verification fails.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-evidence --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue &amp;&amp; ! node scripts/ci/verify_ci_critical_path.mjs --verify-evidence --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-kept</automated>
  </verify>
  <fails_when>An old line changes; any predecessor authority reopens; any slot is reserved/consumed; remote effects are enabled; the report offers a dispatch; or require-kept exits zero.</fails_when>
  <done>A maintainer can audit the new finite authority before it can affect GitHub, and the report remains explicit that no improvement has yet been proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prepare only the repaired one-edge candidate (D-01 through D-17)</name>
  <reversibility rating="reversible">The sole workflow deletion is undone by restoring the exact two-entry host prerequisite array.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CANDIDATE-PREFLIGHT.json</files>
  <read_first>
    - .github/workflows/ci.yml — restored graph whose host prerequisite array is the only permitted edit.
    - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml — immutable restored compatibility source.
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json — candidate/inverse digests and protected proof contracts.
    - .planning/debug/resolved/phase227-format-verifier-gap.md — exact repaired formatter and kept-only validation evidence.
  </read_first>
  <behavior>
    - The workflow diff removes only admin-drift-docs from host-integration.needs.
    - Playwright still depends on host integration and annotation-sweep retains its complete always-run fan-in.
    - Release matrices, provider trigger, live Stripe, stable names, artifacts, permissions, retry policy, and all application source remain unchanged by this task.
    - The candidate is committed as its own full SHA, then the wrapper runs that exact commit in a detached worktree under the already-tested zero-remote-effect boundary.
    - Durable candidate evidence binds the candidate SHA/tree, parent restored SHA/tree, wrapper digest, single-edge diff, every passing command, empty prohibited-invocation log, and `remote_effects: none`.
  </behavior>
  <action>Apply the exact D-01 dependency deletion to the restored workflow and nothing else. Run the repaired formatter and verifier suite, then create an isolated commit containing only `.github/workflows/ci.yml`. Resolve that commit's full SHA and run `preflight_phase227_candidate.sh` against it with expected state `candidate`, writing `227-CANDIDATE-PREFLIGHT.json`. Machine-validate the evidence with `--require-no-remote-effects`, including the candidate SHA/tree, restored parent, wrapper digest, exact one-edge diff, complete check vector, and empty prohibited-invocation log; commit the evidence separately so it cannot alter the candidate tree. Require 227-07-SUMMARY.md to expose `candidate_task_commit`, `candidate_tree`, and the candidate-preflight evidence path/digest for Plan 08. Do not push, dispatch, rerun, create/delete a remote ref, or alter branch protection.</action>
  <acceptance_criteria>The isolated candidate commit changes only `.github/workflows/ci.yml`, its diff is the exact inverse of the restored edge, the wrapper actually passes against that full commit in a detached worktree, and separately committed durable evidence proves the no-remote-effect contract.</acceptance_criteria>
  <verify>
    <automated>CANDIDATE_SHA=$(git log -1 --format=%H -- .github/workflows/ci.yml) &amp;&amp; test ${#CANDIDATE_SHA} -eq 40 &amp;&amp; node -e 'const {spawnSync}=require("node:child_process"); const r=spawnSync("git",["diff-tree","--no-commit-id","--name-only","-r",process.argv[1]],{encoding:"utf8"}); if(r.status!==0 || r.stdout.trim()!==".github/workflows/ci.yml") process.exit(1)' "$CANDIDATE_SHA" &amp;&amp; bash scripts/ci/preflight_phase227_candidate.sh --commit "$CANDIDATE_SHA" --expected-state candidate --evidence-out .planning/phases/227-measured-critical-path-improvement/227-CANDIDATE-PREFLIGHT.json &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-preflight-evidence --preflight-evidence .planning/phases/227-measured-critical-path-improvement/227-CANDIDATE-PREFLIGHT.json --candidate-sha "$CANDIDATE_SHA" --expected-state candidate --require-no-remote-effects &amp;&amp; node --test scripts/ci/verify_ci_critical_path.test.mjs</automated>
  </verify>
  <fails_when>The workflow differs anywhere beyond the one needs entry; the candidate commit includes another file; the wrapper is not invoked against that exact full SHA; detached-worktree checks fail; evidence is absent/stale/mismatched or omits the no-remote-effect proof; the prohibited-invocation log is nonempty; or any remote state changes.</fails_when>
  <done>The repaired candidate exists as one isolated commit with durable, machine-validated exact-tree and zero-remote-effect evidence; all remote authority remains unspent and inactive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git commit → local preflight | Dirty or uncommitted bytes must not be mistaken for the exact candidate that GitHub would execute. |
| Ledger authority → future GitHub dispatch | A stale, duplicate, or ambiguous slot can create irreversible duplicate workflow runs. |
| Workflow/fixtures → decision verifier | Protected graph, identity, artifact, and provider contracts must remain exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-41 | Spoofing | candidate commit/tree | high | mitigate | Detached-worktree preflight binds a full SHA, exact parent graph, one-edge diff, formatter, tests, and verifier results; structural deny rules plus fail-and-log runtime shims separately prove the wrapper has no remote-effect path before activation. |
| T-227-42 | Elevation of Privilege | run authority state | high | mitigate | Versioned finite ceilings, closed predecessor IDs, remote-effects-disabled default, and explicit activation prevent inherited authority. |
| T-227-43 | Repudiation | reservation/consumption accounting | high | mitigate | Reserve before dispatch, reconcile on resume, bind the unique run ID immediately, and reject ambiguity before another slot. |
| T-227-44 | Tampering | one-edge workflow candidate | high | mitigate | Candidate-state verifier and single-file task commit permit only D-01 while preserving D-02/D-03 fan-in and proof contracts. |
| T-227-45 | Information Disclosure | checked-in authority evidence | medium | mitigate | Existing recursive schema/privacy validation runs before rendering; no raw logs, credentials, payloads, or artifact contents are persisted. |
| T-227-SC | Tampering | package/action installation | high | mitigate | Add no dependency or action; use Bash, git, Node built-ins, existing Mix dependencies, and pinned workflow actions. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster path with all proof identifiable and recoverable | 07-08 | COVERED | Local exact-tree gate precedes one finite experiment; only kept reaches closure. |
| REQ | PATH-01 | Measured path, selected edge, before evidence, rollback | 07-08 | COVERED | Exact candidate/inverse and frozen before evidence remain explicit. |
| REQ | PATH-02 | Validated improvement without proof removal | 08 | COVERED | Requires genuine exact-three kept terminal. |
| REQ | SAFE-01 | Stable identities and artifacts | 07-08 | COVERED | Verifier and live proof vector protect every contract. |
| REQ | SAFE-02 | Negative control, rollback, no masking | 07-08 | COVERED | Existing live control, local adversarial tests, exact inverse, conditional restoration. |
| RESEARCH | — | One-edge graph, sanitized repository-bound evidence, narrow anomaly rule | 07-08 | COVERED | Reuses the established verifier/ledger architecture and thresholds. |
| RESEARCH | — | Dependency-free verifier and exact maintainer commands | 07 | COVERED | Existing Node/Bash/Mix stack only. |
| CONTEXT | D-01-D-03 | One edge and preserved independent/fan-in work | 07 | COVERED | Task 3. |
| CONTEXT | D-04-D-11 | Exact cohort, metrics, evidence, controls, inverse | 07-08 | COVERED | Tasks 1-2 define gates; 08 executes them. |
| CONTEXT | D-12-D-17 | Restore-first trigger and proof vector | 07-08 | COVERED | Current restored parent plus exact candidate preflight and live binding. |
| CONTEXT | D-18-D-22 | Immutable old budgets, finite new authority, classifications, rollback states | 07-08 | COVERED | New authorization is separately approved, bounded, auditable, and fail-closed. |
| CONTEXT | D-23 | Fact/state/owner/next-command report | 07-08 | COVERED | Deterministic renderer at each state. |
| CONTEXT | Deferred ideas | Other graph/cache/matrix/workflow/product/UI changes | — | EXCLUDED | No task implements them. |

<verification>
Run only local, deterministic checks. Confirm no GitHub run, rerun, push, remote-ref mutation, or branch-protection operation occurs in this plan. The candidate task commit is the only SHA eligible for 227-08.
</verification>

<success_criteria>
- The repaired formatter and kept-only verifier regressions are part of the candidate ancestry and green under the CI-pinned toolchain.
- A distinct finite authority exists with zero consumption and cannot inherit from either closed predecessor.
- The prepared candidate commit changes only the D-01 edge and preserves every D-02/D-03/D-09 contract.
- An isolated exact-commit preflight is available and remote effects remain inactive.
- Plan 227-06 remains historical and blocked; this plan supersedes its route without editing it.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-07-SUMMARY.md` when done.
</output>

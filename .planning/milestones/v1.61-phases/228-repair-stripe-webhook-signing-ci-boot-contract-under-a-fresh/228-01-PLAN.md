---
phase: 228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/config/runtime.exs
  - .github/workflows/ci.yml
  - scripts/ci/verify_provider_proof.mjs
  - scripts/ci/verify_stripe_webhook_boot_evidence.mjs
  - accrue/test/accrue/runtime_config_test.exs
  - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
  - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md
autonomous: true
requirements: []
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The live Stripe CI lane maps a nonempty endpoint signing secret into Accrue before Stripe boot validation.
    - Missing or renamed signing-secret configuration fails deterministically without disclosing its value, while always-run proof finalization and artifact upload remain intact.
    - A no-network test proves the runtime mapping, CI preflight, and evidence-readiness path before any fresh provider run is authorized.
    - The Phase 228 workflow has no intentional-bypass input or finalizer path, so skipped/intentional_bypass evidence is rejected as structurally impossible rather than accepted as an honest terminal outcome.
  artifacts:
    - accrue/test/accrue/runtime_config_test.exs
    - scripts/ci/verify_stripe_webhook_boot_evidence.mjs
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
  key_links:
    - .github/workflows/ci.yml live-stripe job env -> accrue/config/runtime.exs STRIPE_WEBHOOK_SECRET -> Accrue.Config.validate_at_boot!/0
    - scripts/ci/verify_provider_proof.mjs -> live-stripe job binding/preflight and always-run finalizer/artifact contract
    - scripts/ci/verify_stripe_webhook_boot_evidence.mjs -> bounded dispatch window/GitHub run and job inventory -> durable record -> terminal proof artifact reconciliation
  prohibitions:
    - Phase 227 evidence files are not modified or reclassified.
    - No secret value, raw provider log, payload, actor, or endpoint identifier is written to repository files, summaries, command arguments, or artifacts.
    - No verifier or evidence record normalizes proof_state skipped with reason_code intentional_bypass into an accepted Phase 228 tuple.
---

<objective>
Repair and prove the complete deterministic Stripe webhook-signing boot contract from CI secret binding through runtime configuration and boot validation.

Purpose: Eliminate the Phase 227 hidden boot failure before spending any fresh external evidence authority.
Output: Production runtime/CI mapping, adversarial static fixtures, a no-network boot test, and a sanitized evidence-record schema.

Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-RESEARCH.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-PATTERNS.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one no-network CI-to-runtime signing-secret path end to end</name>
  <reversibility rating="costly">The job/runtime environment-name contract spans CI, runtime boot, and repository administration, but remains removable without migration or public API breakage.</reversibility>
  <read_first>
    - accrue/config/runtime.exs — exact analog is its existing test-only live-Stripe opt-in branch at lines 20-38.
    - .github/workflows/ci.yml — exact analog is the existing live-stripe job env, provider_preflight, provider_proof_finalize, provider_proof_summary, and provider_proof_artifact regions at lines 1262-1359.
    - scripts/ci/verify_provider_proof.mjs — exact analog is assertWorkflowContract plus its adversarial mutation table.
    - accrue/test/accrue/config_entitlements_test.exs — role analog for async:false application-env snapshot/restore and validate_at_boot!/0.
    - accrue/lib/accrue/config.ex — consumer contract at webhook_signing_secrets/1 and maybe_validate_boot_setup!/1; do not modify.
  </read_first>
  <files>accrue/config/runtime.exs, .github/workflows/ci.yml, scripts/ci/verify_provider_proof.mjs, scripts/ci/verify_stripe_webhook_boot_evidence.mjs, accrue/test/accrue/runtime_config_test.exs</files>
  <behavior>
    - With dummy nonempty STRIPE_TEST_SECRET_KEY and STRIPE_WEBHOOK_SECRET inputs, the test runtime selects Accrue.Processor.Stripe, sets stripe_secret_key, stores %{stripe: [dummy_signing_secret]}, and Accrue.Config.validate_at_boot!/0 returns :ok without network I/O.
    - The workflow contract accepts only a live-stripe job that binds STRIPE_WEBHOOK_SECRET from secrets.STRIPE_WEBHOOK_SECRET and includes a nonempty presence term in provider_preflight.
    - Removing or renaming either the job binding or preflight term makes the fixture fail, while proof finalizer, summary, artifact name, read-only permissions, and no-mutation controls continue to pass.
    - The evidence verifier's offline fixtures accept the exact created-run proved, workflow-reachable misconfigured/failed/blocked, and no-run tuples and reject a negative mutation for every required field, duplicate/conflicting field, unknown field, identity mismatch, count mismatch, manifest mismatch, artifact mismatch, retry, open authority, and privacy-forbidden field.
    - The evidence verifier rejects proof_state skipped with reason_code intentional_bypass and proves the impossibility fence from the exact workflow: workflow_dispatch has only run_live_stripe, the manual live-stripe job is input-gated and executes for the bound run, and provider_proof.mjs --finalize receives neither --bypass nor a bypass reason.
  </behavior>
  <action>Start with failing focused assertions in the new async:false runtime test and the Node mutation fixtures. Following the mapped analogs, snapshot and restore every environment variable and :accrue application key the test changes; use dummy sk_test_ and whsec_ strings; evaluate the runtime configuration path before calling Accrue.Config.validate_at_boot!/0; install a request guard or equivalent deterministic assertion proving no network call occurs. Extend the existing test-mode nonempty-key branch in accrue/config/runtime.exs to trim STRIPE_WEBHOOK_SECRET once and add webhook_signing_secrets: %{stripe: [stripe_webhook_secret]} to the same :accrue config call; retain the ordinary secretless Fake-backed test path and the fail-closed empty signing-secret behavior. In live-stripe only, bind STRIPE_WEBHOOK_SECRET from the same-named GitHub repository secret and add its nonempty shell expansion to provider_preflight while preserving the generic diagnostic, stable step IDs, proof paths, if: always() finalization/summary/upload, and live-stripe-proof identity. Extend assertWorkflowContract with name-level positive checks and missing/renamed binding plus omitted-preflight negative mutations. Create the dependency-free authoritative evidence verifier using Node built-ins and the repository-bound GitHub API/job/artifact patterns in verify_ci_critical_path.mjs. Give it `--fixtures`, `--verify-live-binding`, and `--verify-terminal` modes with explicit `--record`, `--repository`, `--sha`, `--dispatch-at`, and `--observed-at` arguments. Fixtures must cover every allowed terminal branch and mutate each required field independently, including exact-once fields, unknown/duplicate/conflicting fields, workflow/event/SHA/attempt/time/job/step identity, selected_count equality, created_run/consumed, manifest totals/timestamps, artifact/finalizer facts, retry/authority closure, no-run emptiness, and the existing privacy allowlist. Add an explicit negative fixture for proof_state skipped with reason_code intentional_bypass and always reject that pair. Independently require all three impossibility checks: workflow_dispatch exposes exactly the required run_live_stripe input for this path, the workflow_dispatch run executes the run_live_stripe-gated live-stripe job, and the provider_proof.mjs --finalize step contains neither the bypass flag nor a bypass-reason argument. Assert the static workflow shape in fixtures and the fetched event/job consequence in live modes, so an impossible bypass cannot be normalized into accepted evidence. Never interpolate, print, serialize, or pass a credential value. Do not add webhook delivery, HMAC logic, packages, actions, workflows, write permissions, provider scope, or changes to Phase 227 evidence.</action>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/runtime_config_test.exs test/accrue/config_test.exs</automated>
  </verify>
  <acceptance_criteria>The Node fixture rejects both missing/renamed signing-secret edges; the focused Mix run proves Stripe boot succeeds with dummy inputs and no network; ordinary secretless test configuration remains Fake-backed; all existing provider-proof preservation controls pass.</acceptance_criteria>
  <done>The production CI/runtime edge and its deterministic positive/negative proof are committed and green without external access.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the fresh evidence schema and Nyquist task map before authorization</name>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — role analog for bounded authority and concrete recorded outcomes; read-only history.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-PATTERNS.md — exact required fields for the new sanitized record.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md — replace TBD task identifiers and Wave 0 gaps with this plan's executable commands.
  </read_first>
  <files>.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md, .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md</files>
  <action>Create the Phase 228 record before any dispatch with status readiness_not_authorized, owner maintainer, fresh run ceiling exactly one first-attempt workflow_dispatch with run_live_stripe:true, created_run:false, consumed:false, and next action pointing to Plan 228-02. Define an exact-once field schema covering repaired SHA; dispatch-at and observed-at; workflow, event, input intent, run/attempt/immutable URL, stable job and named step identities/statuses/conclusions; raw run/job conclusion; proof_state/reason_code; selected/passed/failed/skipped counts; manifest presence, totals, started_at, and finished_at; finalizer result; live-stripe-proof artifact presence; created/consumed state; rejection class; retry; authority closure; and outcome. Specify applicable-versus-empty fields for created-run proved, the exact workflow-reachable misconfigured/failed/blocked terminals, and no-run rejection; the no-run branch retains sanitized dispatch intent while every run/job/conclusion/count/manifest/artifact/proof-only field is present exactly once with an empty value. State explicitly that skipped/intentional_bypass is outside the Phase 228 schema and blocks validation because the mandatory-provider workflow cannot produce it. Reject unknown, duplicate, or conflicting fields. Include only a historical link to Phase 227 and state that its bytes and exhausted authority remain unchanged. Add explicit sanitization rules excluding secret values, raw logs, payloads, actors, endpoint identifiers, and presence details. Update 228-VALIDATION.md rows to 228-01-01 and 228-01-02, Wave 1, the exact Node/Mix commands above, measured command latency, wave_0_complete:true, status validated, and nyquist_compliant:true only after both commands pass.</action>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/runtime_config_test.exs test/accrue/config_test.exs &amp;&amp; cd .. &amp;&amp; rg -n 'readiness_not_authorized|first-attempt|consumed: `false`|selected_count|live-stripe-proof|Phase 227' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>The exact-once record schema exists before external authority is requested, budgets exactly one new attempt, cannot be mistaken for proof, defines exhaustive applicable/empty tuples for every terminal branch, and 228-VALIDATION.md names real plan/task IDs with passing deterministic commands.</acceptance_criteria>
  <done>The unconsumed evidence schema and executable Nyquist map are committed before any live authorization.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Repair the Stripe webhook-signing CI boot contract and obtain bounded fresh evidence | 228-01, 228-02, 228-03 | COVERED | Deterministic repair precedes authority and one attempt. |
| REQ | — | No mapped requirement IDs | — | COVERED | Visible spec-less fallback skip; no durable IDs invented. |
| RESEARCH | — | Runtime-owned signing-secret mapping | 228-01 | COVERED | Exact runtime.exs analog. |
| RESEARCH | — | Four-value CI env/preflight with always-run proof preservation | 228-01 | COVERED | Exact live-stripe analog. |
| RESEARCH | — | Negative fixtures and no-network boot check | 228-01 | COVERED | Leading tracer. |
| RESEARCH | — | Fresh sanitized record and exactly one authorized first attempt | 228-01, 228-02, 228-03 | COVERED | Schema before spend; explicit authority; honest terminal record. |
| CONTEXT | — | No CONTEXT.md; user chose to proceed without it | — | COVERED | No locked decisions or deferred ideas to map. |

## Artifacts this phase produces

- New path `accrue/test/accrue/runtime_config_test.exs`; no new production symbol is required.
- New path `scripts/ci/verify_stripe_webhook_boot_evidence.mjs` with `--fixtures`, `--verify-live-binding`, and `--verify-terminal` modes.
- New path `.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md` with readiness/outcome fields listed in Task 2.
- New CI environment contract `STRIPE_WEBHOOK_SECRET`; no new CLI flags, API endpoints, schema fields, or public library symbols.
- Modified contract surfaces: `accrue/config/runtime.exs`, `.github/workflows/ci.yml`, `scripts/ci/verify_provider_proof.mjs`, and `228-VALIDATION.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repository secret -> live-stripe job | Maintainer-controlled signing material enters an ephemeral CI environment. |
| CI environment -> Accrue runtime config | Untrusted empty/misnamed inputs cross into boot-time processor validation. |
| Runtime/job -> proof artifact | Execution results become durable evidence and must not expose credentials or certify empty work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-228-01 | Information Disclosure | live-stripe env, shell, summaries, artifacts | high | mitigate | Bind the secret only at job env, test nonempty presence only, preserve generic diagnostics, and add name-level fixtures that never handle a value. |
| T-228-02 | Spoofing | STRIPE_WEBHOOK_SECRET runtime mapping | high | mitigate | Require the maintainer-owned exact test-mode Dashboard endpoint secret; reject empty input and never substitute an API key or CLI forwarding secret. |
| T-228-03 | Repudiation | provider proof finalizer and evidence schema | high | mitigate | Preserve always-run finalization and require nonzero selected/passed counts, successful raw conclusion, manifest, and live-stripe-proof artifact before proved. |
| T-228-04 | Denial of Service | boot validation/preflight | high | mitigate | Four-value preflight plus a no-network boot test exposes missing signing configuration before suite selection and records misconfiguration on failure. |
</threat_model>

<verification>
Run the Node negative fixtures and focused Mix runtime/config tests. Confirm no implementation file outside the declared runtime/CI/evidence fence changed and `git diff -- .planning/phases/227-measured-critical-path-improvement` is empty.
</verification>

<success_criteria>
The deterministic tracer proves the repaired CI-to-runtime edge, adversarial omissions fail closed, all evidence machinery remains always-run and sanitized, and no live dispatch has occurred.
</success_criteria>

<output>
Create `.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-01-SUMMARY.md` when done.
</output>

---
phase: 228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh
plan: "02"
type: execute
wave: 2
depends_on: [228-01]
files_modified:
  - scripts/ci/bootstrap_stripe_provider_proof.mjs
  - scripts/ci/provider_proof_automation.mjs
  - scripts/ci/provider_proof.mjs
  - scripts/ci/verify_provider_proof.mjs
  - scripts/ci/verify_executable_uat_contract.mjs
  - scripts/ci/README.md
  - .planning/phases/226-ci-baseline-proof-semantics/fixtures/provider-proof-cases.json
  - .github/workflows/ci.yml
  - accrue/test/accrue/backend_automation_contract_test.exs
autonomous: true
automation_contract: backend-zero-human
requirements: []
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - Credential bootstrap accepts the endpoint secret only over stdin and never emits it in arguments, logs, evidence, or repository files.
    - Daily and relevant-main-repair proofs use a fixture-tested trigger and never auto-retry.
    - Provider failures update one sanitized issue and a later proved run closes it.
    - Opted-in backend plans reject human verification and require executable automated UAT.
  artifacts:
    - scripts/ci/bootstrap_stripe_provider_proof.mjs
    - scripts/ci/provider_proof_automation.mjs
    - scripts/ci/verify_executable_uat_contract.mjs
  prohibitions:
    - No credential value is accepted through chat, command arguments, committed files, issue bodies, summaries, or artifacts.
    - No provider endpoint is created or replaced and no failed proof is retried automatically.
---

<objective>
Replace Phase 228's recurring human gates with a reusable backend-zero-human automation contract.

Purpose: Keep only one-time credential bootstrap outside automation while making readiness, provider proof, incident response, verification, and UAT machine-enforced.
Output: Secret-safe bootstrap tooling, guarded recurring CI, deduplicated failure ownership, and merge-blocking automated-UAT policy.
</objective>

<threat_model>
- T-228-05: The endpoint signing secret leaks through arguments, logs, artifacts, issues, or repository files.
- T-228-06: Bootstrap targets an unpushed or different ref, or dispatches more than one proof.
- T-228-07: Recurring provider failures remain unowned, multiply into duplicate incidents, or stay open after recovery.
- T-228-08: A backend plan reintroduces human verification, manual UAT, or a nondeterministic acceptance escape hatch.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Build secret-safe bootstrap and recurring provider-proof operations</name>
  <files>scripts/ci/bootstrap_stripe_provider_proof.mjs, scripts/ci/provider_proof_automation.mjs, scripts/ci/provider_proof.mjs, scripts/ci/verify_provider_proof.mjs, scripts/ci/README.md, .planning/phases/226-ci-baseline-proof-semantics/fixtures/provider-proof-cases.json, .github/workflows/ci.yml</files>
  <action>Create a dependency-free bootstrap command that requires explicit one-proof authority, accepts only a whsec_ secret over stdin, runs readiness, installs the Actions secret, verifies required secret names without reading values, and dispatches exactly once at a named ref. Add a classifier for schedule, manual dispatch, and relevant main repairs. Route live-stripe through it with SHA-scoped concurrency and no retry. Add an always-run incident job with only actions:read, contents:read, and issues:write; create/update one provider-proof issue and close it after recovery.</action>
  <verify><automated>node scripts/ci/bootstrap_stripe_provider_proof.mjs --self-test &amp;&amp; node scripts/ci/provider_proof_automation.mjs --self-test &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures</automated></verify>
  <acceptance_criteria>Fixtures prove stdin-only transport, exactly one dispatch, relevant-path selection, incident deduplication/recovery, narrow permissions, and canonical evidence preservation.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Enforce backend-zero-human verification and automated UAT</name>
  <files>scripts/ci/verify_executable_uat_contract.mjs, accrue/test/accrue/backend_automation_contract_test.exs, .github/workflows/ci.yml</files>
  <action>Add an all-opted-in scan. Reject tracer/checkpoint tasks, human checks, why_human, and tasks without automated verification. For completed opted-in phases beginning with 228, require passed verification, behavior_unverified:0, no human-verification section, executable coverage with human_judgment:false, and byte-reproducible generated UAT containing only passing automated results. Run this in merge-blocking CI.</action>
  <verify><automated>node scripts/ci/verify_executable_uat_contract.mjs --self-test &amp;&amp; node scripts/ci/verify_executable_uat_contract.mjs --all-opted-in &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/backend_automation_contract_test.exs</automated></verify>
  <acceptance_criteria>Positive fixtures pass and negative fixtures reject every manual-verification or nondeterministic-UAT escape hatch.</acceptance_criteria>
</task>
</tasks>

<verification>Run both Node self-test bundles, both provider fixture suites, the opted-in scan, and the focused Elixir policy test.</verification>
<success_criteria>The repository has one credential-bootstrap boundary and zero recurring human verification or UAT for opted-in backend phases.</success_criteria>
<output>Create `228-02-SUMMARY.md` with executable coverage entries and human_judgment:false.</output>

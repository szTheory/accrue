---
phase: 229-repository-truth-recovery-safety
plan: 07
type: execute
wave: 2
depends_on:
  - 229-05
  - 229-06
files_modified:
  - scripts/ci/verify_repository_inventory.mjs
  - scripts/ci/render_repository_inventory.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Strict recovery verification independently opens the original private manifest and bundle, checks the committed manifest digest anchor, resolves every encoded preservation ref, lists every original bundle head, and proves exact set equality with the committed non-preservation ref inventory (D-04, D-06)."
    - "Removing any one of many mappings, adding an extra mapping, changing one object, substituting a foreign repository, or supplying null/empty strict inputs causes the corresponding advertised flag to fail; one valid ref is accepted only when every compared set contains that same ref/object."
    - "Every strict verifier flag enforces its named property independently, and complete-category checks cover all named inventory categories plus zero/one/many and deterministic equal-key ordering (D-01 through D-03)."
    - "Rendered recovery instructions first assign/export the runtime bundle location, verify that bundle, and restore actual original bundle heads through safely quoted/structured operations that execute successfully in an integration fixture."
  prohibitions:
    - statement: "Committed evidence must not expose private paths, secrets, raw payloads/logs, actors, or bundle locations (D-01, D-05)."
      status: flagged-unverified
  artifacts:
    - path: scripts/ci/verify_repository_inventory.mjs
      provides: "Independent strict recovery/category/provenance/privacy verification with exact negative controls"
    - path: scripts/ci/render_repository_inventory.mjs
      provides: "Deterministic plural-category rendering and executable recovery procedure"
  key_links:
    - from: scripts/ci/verify_repository_inventory.mjs
      to: "private manifest + actual bundle + local encoded refs + canonical inventory"
      via: "independent digest, bundle-head, object-resolution, and set-equality checks"
      pattern: "bundle (verify|list-heads)|deepEqual|Set"
    - from: scripts/ci/render_repository_inventory.mjs
      to: scripts/ci/preserve_repository_state.sh
      via: "renderer consumes original bundle heads and safe restore argv contract produced by Plan 229-05"
      pattern: "PHASE229_BUNDLE|original_ref|restore_argv"
---

<objective>
Turn the verifier into an independent proof of the phase contracts and make the rendered recovery procedure executable.

Purpose: Close CR-09 and CR-10 after the preservation and collection interfaces are corrected.
Output: Strict flag-specific negative/integration fixtures plus deterministic Markdown that restores real bundle heads safely.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-05-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md
@scripts/ci/preserve_repository_state.sh
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/render_repository_inventory.mjs
@scripts/ci/verify_repository_inventory.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove all-ref recovery and every strict flag independently</name>
  <files>scripts/ci/verify_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-05-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md
    scripts/ci/preserve_repository_state.sh
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/verify_repository_inventory.mjs
    scripts/ci/verify_ci_baseline.mjs
  </read_first>
  <behavior>
    - "The exact verifier probe that reduces 109 recovery mappings to one exits non-zero under `--require-all-ref-recovery`; missing-one-of-many, extra, duplicate, wrong-object, wrong-encoded-ref, absent bundle head, and foreign-manifest cases all fail."
    - "Strict recovery flags require explicit `--recovery-manifest`, `--expected-manifest-sha256`, and `--recovery-bundle` inputs; null/empty inputs fail, while a complete one-ref fixture passes exact equality."
    - "Each advertised strict flag has a mutation that fails only when that property is enforced: recovery, all-ref equality, typed artifacts, complete categories, edge cases, command provenance, privacy, determinism, local-only mode, and workflow-metadata authorization."
  </behavior>
  <action>Use `scripts/ci/verify_ci_baseline.mjs`, the mapped verifier analog, for explicit CLI contracts, independent schema checks, byte-reproducibility assertions, local negative controls with guaranteed temporary cleanup, and one asynchronous top-level failure boundary. Start from the verifier's reproduced one-of-109 false positive and add failing negative fixtures. Extend CLI parsing so strict recovery verification takes explicit private manifest, anchored digest, and bundle inputs without recording their paths in public output. Independently validate owner/mode and digest before parsing the manifest; run bounded `git bundle verify` and `git bundle list-heads`; resolve encoded preservation refs from the repository; and compare exact original-ref/object sets across manifest, original bundle heads, encoded refs, committed recovery rows, and the canonical all-ref inventory after excluding only the well-defined Phase 229 preservation namespace. Do not trust `verified`, `bundle_member`, array non-emptiness, or collector helpers as proof. Add one negative mutation for every advertised strict flag and explicit empty/single/null cases. Require stable equal-key ordering and complete zero/one/many categories from Plan 229-06. Keep private locations in argv/runtime only and preserve the original capsule unchanged.</action>
  <verify>
    <automated>node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-recovery --require-all-ref-recovery --require-typed-artifacts --require-complete-categories --require-edge-cases --require-command-provenance --require-privacy-controls --require-determinism</automated>
    <fails_when>the one-of-many canonical record passes; any strict flag lacks a targeted failing mutation; set equality ignores an extra/missing/wrong ref; null private inputs pass; a complete single-ref capsule fails; or collector assertions substitute for independent bundle/ref checks</fails_when>
  </verify>
  <acceptance_criteria>
    CR-09's exact reduction probe fails under the public CLI and reports which recovery set differs.
    Missing, extra, duplicate, wrong-object, wrong-encoded, absent-bundle-head, foreign-repository, null, empty, and valid-singleton fixtures have explicit expected outcomes.
    Every strict flag demonstrably changes verification behavior through its own negative control.
  </acceptance_criteria>
  <done>The independent verifier proves exact complete recovery and category evidence rather than certifying asserted booleans or non-empty arrays.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render and execute exact bundle verification and restoration instructions</name>
  <files>scripts/ci/render_repository_inventory.mjs, scripts/ci/verify_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-05-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md
    scripts/ci/preserve_repository_state.sh
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/render_repository_inventory.mjs
    scripts/ci/verify_repository_inventory.mjs
    scripts/ci/render_ci_baseline.mjs
    scripts/ci/verify_ci_baseline.mjs
  </read_first>
  <behavior>
    - "The rendered procedure assigns and exports `PHASE229_BUNDLE` on its own step before expanding it, then `git bundle verify` succeeds against a fixture bundle."
    - "Each restore uses an actual original ref head contained by the bundle, or fetches its exact object before a structured `git update-ref`; no nonexistent encoded bundle source is referenced."
    - "An integration fixture executes the exact rendered procedure in a fresh repository and restores a deleted ordinary and metacharacter-bearing ref to the expected object."
    - "Plural remote categories render all zero/one/many SHAs in deterministic order, and equal comparisons preserve a documented stable tie-breaker."
  </behavior>
  <action>Follow `scripts/ci/render_ci_baseline.mjs`, the mapped renderer analog, by validating records before projection, escaping every interpolated Markdown value, emitting an action-first deterministic report, and leaving byte comparison to the verifier. Follow `scripts/ci/verify_ci_baseline.mjs`, the mapped verifier analog, for the fresh-render byte assertion and cleanup-contained negative fixtures. Update the renderer to project the Plan 229-05 safe restore contract and Plan 229-06 singleton/plural remote schema. Emit a multi-step runtime-variable procedure whose assignment occurs before use; reference original bundle heads that `git bundle list-heads` actually contains and shell-quote any human-facing argv representation with a tested POSIX-safe routine, while the machine contract remains argv arrays. Extend verifier fixtures to extract and execute the exact documented verification/restore steps against a generated bundle and fresh repository, including a hostile valid ref. Render confirmed empty plural categories distinctly from unavailable observations, include every normalized object ID for many-result categories, and sort with stable secondary keys. Keep Markdown a pure validated JSON projection per D-01 and ensure no external path or private manifest content enters it.</action>
  <verify>
    <automated>node --check scripts/ci/render_repository_inventory.mjs &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-complete-categories --require-command-provenance --require-privacy-controls --require-determinism</automated>
    <fails_when>the prefix-variable expansion probe fails; any restore source is absent from bundle heads; the exact rendered procedure cannot restore the fixture refs; plural rows are lost; equal-key order changes; or private/external data appears</fails_when>
  </verify>
  <acceptance_criteria>
    CR-10's two reproduced commands are replaced by commands that execute successfully as rendered.
    The integration fixture starts from a generated bundle and proves exact ref restoration in a fresh repository.
    Zero plural results display as observed-empty, unavailable results display their reason, and one/many results display all full SHAs deterministically.
  </acceptance_criteria>
  <done>The maintainer diagnostic is byte-reproducible and its exact recovery procedure verifies and restores real bundle content safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| asserted inventory → independent verifier | Public booleans and arrays are untrusted until reconciled with private recovery authorities and Git objects. |
| canonical JSON → executable maintainer instructions | Untrusted ref names must not become shell syntax or nonexistent bundle references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G07-01 | Tampering | strict recovery flags | high | mitigate | Independently compare exact sets across manifest, bundle heads, encoded refs, and inventory; reject every mismatch. |
| T-229-G07-02 | Elevation of Privilege | rendered restore procedure | high | mitigate | Prefer argv data and tested POSIX quoting; execute hostile-ref fixtures without evaluated input. |
| T-229-G07-03 | Spoofing | complete category evidence | high | mitigate | Flag-specific mutations prove repository, SHA, availability, and set semantics rather than presence. |
| T-229-G07-04 | Information Disclosure | renderer/verifier inputs | high | mitigate | Keep private paths runtime-only and recursively reject them from committed JSON/Markdown. |
</threat_model>

<verification>
Run all strict-flag fixtures and the exact rendered restore integration test, then ensure a fresh render remains byte-identical for input permutations with equal primary keys.
</verification>

<success_criteria>
CR-09 and CR-10 are closed, every advertised strict flag has a meaningful negative control, all recovery/category edge cases are explicit, and the rendered instructions work against actual bundle heads without leaking private state.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/verify_repository_inventory.mjs`: private-input strict verification, exact recovery set equality, flag-specific negative controls, and rendered-procedure integration tests.
- `scripts/ci/render_repository_inventory.mjs`: safe runtime bundle setup, executable original-head restoration instructions, complete plural remote rendering, and deterministic tie-breakers.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-07-SUMMARY.md` when done.
</output>

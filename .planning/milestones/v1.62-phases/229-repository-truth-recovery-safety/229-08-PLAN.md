---
phase: 229-repository-truth-recovery-safety
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/ci_monitor.cjs
  - scripts/ci/watch_ci.sh
  - scripts/ci/README.md
autonomous: true
gap_closure: true
requirements:
  - REPO-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`bash scripts/ci/watch_ci.sh` defaults to branch `main`, workflow `CI`, fixed repository `szTheory/accrue`, and bounded timeout/poll values, resolving the selected run to one full SHA before polling (D-07, D-08)."
    - "An explicit full SHA bypasses branch resolution; explicit compatible overrides remain deterministic; multiple workflows for one branch cannot make the default wrapper inspect an unintended workflow."
    - "A completed success exits zero, while failure, cancelled, timed-out, ambiguous, no-match, and unavailable completions exit non-zero and still emit fixed-repository/full-SHA diagnostics."
    - "Tests execute wrapper behavior through an injected monitor/GitHub adapter rather than accepting only source-text structure, and docs promise exactly the behavior tested."
  prohibitions:
    - statement: "The monitor must not perform GitHub mutations (D-08)."
      status: flagged-unverified
  artifacts:
    - path: scripts/ci/ci_monitor.cjs
      provides: "Exact-SHA watch completion semantics and executable wrapper harness"
    - path: scripts/ci/watch_ci.sh
      provides: "No-argument main/CI bounded compatibility path"
    - path: scripts/ci/README.md
      provides: "Supported CI list/inspect/watch and wrapper failure contract"
  key_links:
    - from: scripts/ci/watch_ci.sh
      to: scripts/ci/ci_monitor.cjs
      via: "one exec with fixed repo, default main/CI selection, and bounded options"
      pattern: 'ci_monitor\.cjs.*watch.*--repo'
---

<objective>
Restore the supported compatibility watcher and make unsuccessful completed CI observable as command failure.

Purpose: Close CR-05 and the REPO-03 supported-command/failure-semantics probe while retaining the single read-only monitor implementation.
Output: Executable wrapper integration fixtures, correct monitor exits, and aligned contributor documentation.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-02-SUMMARY.md
@scripts/ci/ci_monitor.cjs
@scripts/ci/watch_ci.sh
@scripts/ci/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the no-argument main/CI wrapper and propagate completed failure exits</name>
  <files>scripts/ci/ci_monitor.cjs, scripts/ci/watch_ci.sh</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    scripts/ci/ci_monitor.cjs
    scripts/ci/watch_ci.sh
    scripts/ci/collect_ci_baseline.mjs
  </read_first>
  <behavior>
    - "Executing the wrapper with no arguments calls the monitor once with `watch`, fixed repository, branch `main`, workflow `CI`, and bounded defaults; a fixture containing another workflow still selects only CI."
    - "Executing with `--sha` omits branch selection, retains workflow CI unless explicitly overridden, and polls the exact full SHA."
    - "Completed success returns 0; failure, cancelled, action_required, stale, skipped, timed_out, neutral, and unknown non-success conclusions return a documented non-zero code after rendering repository and SHA."
    - "Queued/in-progress transitions, timeout, no-match, ambiguity, malformed/null responses, and adapter unavailability retain distinct non-zero behavior."
  </behavior>
  <action>Use `scripts/ci/collect_ci_baseline.mjs`, the mapped monitor analog, for repository-bound GitHub reads, fixed repository validation, full-SHA checks, bounded responses, and sanitized request provenance. Use the existing `scripts/ci/watch_ci.sh` compatibility surface as its own mapped wrapper analog: preserve it as one thin `exec node scripts/ci/ci_monitor.cjs watch ...` delegation rather than a second GitHub-query implementation. Write failing executable wrapper tests first by spawning `watch_ci.sh` with a temporary injected monitor/adapter seam or controlled executable path; assert argv and exit status for no arguments, positional branch, explicit SHA, explicit workflow, multi-workflow results, completed success, and completed failure. Set wrapper defaults to `main` and `CI`, preserve the single `exec` delegation and bounds, and ensure `--sha` wins over branch resolution. Update `ci_monitor.cjs` so main maps a completed conclusion other than `success` to a stable non-zero exit after rendering the exact repository/SHA result; keep inspect behavior observable and preserve distinct parsing/no-match/ambiguity/unavailable/timeout codes. Do not add a second GitHub implementation or any dispatch/rerun/cancel/write command per D-07 and D-08.</action>
  <verify>
    <automated>bash -n scripts/ci/watch_ci.sh &amp;&amp; node scripts/ci/ci_monitor.cjs --self-test --verify-wrapper scripts/ci/watch_ci.sh &amp;&amp; node --test scripts/ci/ci_monitor.cjs</automated>
    <fails_when>the no-argument wrapper exits 64; default argv omits main or CI; another workflow is selected; explicit SHA triggers branch lookup; a completed unsuccessful run exits zero or omits its full SHA; or tests only regex-check wrapper text</fails_when>
  </verify>
  <acceptance_criteria>
    CR-05's no-argument, failed-run, and multi-workflow probes execute the wrapper process and produce the expected argv/output/status.
    Exact-SHA mode bypasses branch selection, while default and positional-branch modes resolve one CI run to a full SHA before watch polling.
    The adapter registry remains list/view-only and repository-bound, and Actions success remains `provider_proof: non_run` absent independent provider evidence per D-09.
  </acceptance_criteria>
  <done>The supported wrapper reliably watches the intended CI run and automation can distinguish every unsuccessful completion from success.</done>
</task>

<task type="auto">
  <name>Task 2: Align the documented supported command and failure contract</name>
  <files>scripts/ci/README.md</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    scripts/ci/README.md
    scripts/ci/ci_monitor.cjs
    scripts/ci/watch_ci.sh
  </read_first>
  <action>Follow the mapped `scripts/ci/README.md` Phase 226 evidence-map and watcher-note pattern: extend the existing contributor entry point with one Phase 229 evidence row and a compact command matrix, and preserve the literal provider-state boundary. Update the Phase 229 command matrix and compatibility section to state the tested no-argument `main`/`CI` defaults, explicit-SHA precedence, fixed repository, bounded watch, exact-SHA failure output, and non-zero unsuccessful completion semantics. Retain direct `list`, `inspect`, and `watch` examples and the distinction between Actions conclusion and provider proof per D-09. Describe only read-only observation and keep CI execution, ship-window resolution, history integration, PR/ref mutation, and publication outside Phase 229 per D-08 and D-10.</action>
  <verify>
    <automated>node scripts/ci/ci_monitor.cjs --self-test --verify-wrapper scripts/ci/watch_ci.sh --verify-docs scripts/ci/README.md</automated>
    <fails_when>documentation verification exits non-zero; docs and executable defaults disagree; failed completion semantics are absent; list/inspect/bounded-watch examples disappear; or provider proof is equated with Actions success</fails_when>
  </verify>
  <acceptance_criteria>
    A maintainer can identify the supported no-argument path, its exact default repository/branch/workflow/bounds, and its exit semantics from one section.
    Every documented wrapper promise is exercised by Task 1's executable fixtures.
    The direct command matrix remains repository-bound, exact-SHA-aware, bounded, and read-only.
  </acceptance_criteria>
  <done>The CI README and executable wrapper expose one consistent supported REPO-03 command contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| wrapper argv → monitor | Defaults and overrides must select the intended repository/workflow/SHA without ambiguity. |
| GitHub completion → process exit | Untrusted run state must not be misreported as successful automation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G08-01 | Spoofing | branch/workflow selection | high | mitigate | Pin repository and default workflow, resolve branch to a full SHA, and test multi-workflow fixtures. |
| T-229-G08-02 | Repudiation | completed run exit status | high | mitigate | Render repository/SHA then map every non-success conclusion to stable non-zero failure. |
| T-229-G08-03 | Denial of Service | wrapper watch | high | mitigate | Retain maximum timeout/poll validation and bounded defaults. |
| T-229-G08-04 | Tampering | monitor operation registry | high | mitigate | Keep list/view-only adapter assertions and execute the wrapper through an injected seam. |
</threat_model>

<verification>
Run syntax, self-test, Node test, wrapper subprocess fixtures, and executable documentation checks; confirm no GitHub mutation command is reachable.
</verification>

<success_criteria>
CR-05 is closed, all REPO-03 empty/failure/selection semantics are explicit and tested, and the compatibility wrapper remains a thin, bounded, read-only entry to the single monitor.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/ci_monitor.cjs`: non-success completion exit mapping and executable wrapper harness.
- `scripts/ci/watch_ci.sh`: tested main/CI bounded defaults with explicit-SHA precedence.
- `scripts/ci/README.md`: supported command, selection, attribution, bounds, and failure semantics.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-08-SUMMARY.md` when done.
</output>

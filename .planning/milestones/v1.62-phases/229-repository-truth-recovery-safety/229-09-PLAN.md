---
phase: 229-repository-truth-recovery-safety
plan: 09
type: execute
wave: 3
depends_on:
  - 229-07
  - 229-08
files_modified:
  - .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
  - .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One committed canonical JSON contains complete named/all-ref, typed-artifact, every-worktree, planning, ten current ship-window, remote-main, all-open-PR-head, all-release-branch, and bounded-recent-Actions truth; Markdown is its byte-identical deterministic projection (D-01 through D-03)."
    - "Before live observation, the original private manifest digest, unchanged original bundle, every frozen original/bundle/encoded/inventory ref mapping, and a new additive typed artifact attestation all pass independent verification (D-04 through D-06)."
    - "Each live remote category is either an actually attempted repository-bound observation with full SHA set and time/request provenance or an honest unavailable record; confirmed empty plural categories remain observed-empty (D-02, D-08, D-09)."
    - "The committed evidence and supported CI docs pass the complete strict suite, while Phase 229 performs no integration, cleanup, CI mutation/execution, ship-window resolution, PR/ref mutation, or publication (D-07 through D-10)."
  prohibitions:
    - statement: "User-owned untracked artifacts must not be changed while final evidence is captured (D-05)."
      status: flagged-unverified
    - statement: "Committed evidence must not expose private paths, secrets, raw payloads/logs, actors, or bundle locations (D-01)."
      status: flagged-unverified
    - statement: "Unavailable evidence must not masquerade as observed evidence or provider proof (D-02, D-09)."
      status: flagged-unverified
  artifacts:
    - path: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
      provides: "Final complete schema-versioned repository and CI authority"
    - path: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md
      provides: "Final deterministic privacy-safe maintainer diagnostic"
  key_links:
    - from: scripts/ci/collect_repository_inventory.mjs
      to: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
      via: "recovery-gated `--observe-remote` final capture"
      pattern: "schema_version|repository|remotes|worktrees|ship_windows"
    - from: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
      to: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md
      via: "fresh render plus byte comparison"
      pattern: "Repository Inventory"
---

<objective>
Recapture and seal the complete recovery-backed repository truth after all deterministic gaps are fixed.

Purpose: Replace the incomplete canonical evidence with a reproducible Phase 230 handoff that satisfies REPO-01 through REPO-03 without altering user-owned or recovery state.
Output: Final validated inventory JSON and deterministic Markdown generated from real local authorities and attempted bounded GitHub reads.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-SECURITY.md
@.planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
@.planning/phases/229-repository-truth-recovery-safety/229-05-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-07-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-08-SUMMARY.md
@scripts/ci/preserve_repository_state.sh
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/render_repository_inventory.mjs
@scripts/ci/verify_repository_inventory.mjs
@scripts/ci/ci_monitor.cjs
@scripts/ci/watch_ci.sh
@scripts/ci/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the canonical inventory behind the original recovery barrier</name>
  <precondition>The original Phase 229 private manifest and bundle whose digests are anchored in the committed inventory remain readable and unchanged, and their runtime paths can be supplied to the collector without committing them.</precondition>
  <files>.planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json, .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    .planning/WINDOWS.md
    .planning/STATE.md
    .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
    .planning/milestones/v1.61-phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    .planning/milestones/v1.61-phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/render_repository_inventory.mjs
    scripts/ci/verify_repository_inventory.mjs
  </read_first>
  <action>Use the mapped `.planning/milestones/v1.61-phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson` source-evidence analog for the canonical allowlisted, repository/SHA-bound machine record, and the mapped `226-CI-BASELINE.md` rendered-evidence analog for a deterministic validated projection rather than hand-edited status text. Resolve the original private manifest and bundle only through runtime/private inputs and verify their current SHA-256 values equal the anchors already committed; never regenerate, overwrite, move, or weaken them. Before capture, record a fresh additive mode-0600 private attestation beside the capsule containing one ISO timestamp and typed before/after invariants for every frozen user-owned artifact, allowing only the two already-authorized exact GSD metadata paths. Run the corrected collector once with `--observe-remote`, the original manifest/bundle/digest, and the new additive attestation. It must independently prove complete ref recovery before its fixed bounded GitHub GET adapter runs, collect every local category and all ten current open ship windows, then write canonical JSON atomically. If GitHub is unavailable, preserve per-category unavailable provenance from actual attempted reads. Render Markdown only from validated JSON. Preserve the immutable v1.61 tag, all refs/worktrees, every user-owned/untracked artifact, and the original capsule per D-04 through D-06; perform none of D-10's deferred integration/cleanup/release work.</action>
  <verify>
    <automated>test -n "${PHASE229_PRIVATE_MANIFEST:-}" &amp;&amp; test -n "${PHASE229_RECOVERY_BUNDLE:-}" &amp;&amp; test -n "${PHASE229_MANIFEST_SHA256:-}" &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --records .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json --rendered .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md --expected-repository szTheory/accrue --recovery-manifest "$PHASE229_PRIVATE_MANIFEST" --expected-manifest-sha256 "$PHASE229_MANIFEST_SHA256" --recovery-bundle "$PHASE229_RECOVERY_BUNDLE" --require-recovery --require-all-ref-recovery --require-typed-artifacts --require-complete-categories --require-edge-cases --require-command-provenance --require-privacy-controls --require-determinism --require-workflow-metadata-authorization</automated>
    <fails_when>private recovery inputs are absent or changed; strict verification exits non-zero; any named category/worktree/window/ref/artifact is absent; a remote category was not attempted yet claims observation; plural SHAs are dropped; or Markdown differs from a fresh render</fails_when>
  </verify>
  <acceptance_criteria>
    The JSON reports the actual `.planning/WINDOWS.md` open set, every porcelain worktree, and every bounded zero/one/many remote observation without category truncation.
    All original frozen refs have exact set-equal recovery through unchanged bundle heads and encoded refs, and all artifact hashes/types match their frozen or narrowly authorized state.
    The original private capsule digests are unchanged; only a new restrictive additive attestation is created privately.
  </acceptance_criteria>
  <done>The committed JSON/Markdown pair is complete, recovery-backed, honest about live availability, deterministic, and safe for Phase 230 consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Phase 229 regression and preservation handoff</name>
  <files>.planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json, .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-SECURITY.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    scripts/ci/README.md
    scripts/ci/preserve_repository_state.sh
    scripts/ci/ci_monitor.cjs
    scripts/ci/watch_ci.sh
    scripts/ci/verify_repository_inventory.mjs
  </read_first>
  <action>Run the hardened preservation self-test, every strict inventory fixture including exact restore and one-of-many rejection, executable wrapper/default/failure tests, documentation verification, and the strict canonical inventory command from Task 1. Compare the v1.61 tag SHA, every pre-existing ref/worktree, original capsule digests, and every typed user-owned artifact against the pre-task snapshot. Confirm `COVERAGE.md` contains only the four authorized read capabilities and reasoned opt-outs for mutations, publication, reruns/cancellation, PR changes, ref/tag movement, and ship-window resolution. Do not recapture by replacing the original capsule or classify/delete any user artifact. Leave the JSON/Markdown unchanged if their fresh collection/render bytes already match; any mismatch fails the task instead of being hand-edited.</action>
  <verify>
    <automated>test -n "${PHASE229_PRIVATE_MANIFEST:-}" &amp;&amp; test -n "${PHASE229_RECOVERY_BUNDLE:-}" &amp;&amp; test -n "${PHASE229_MANIFEST_SHA256:-}" &amp;&amp; bash scripts/ci/preserve_repository_state.sh --self-test &amp;&amp; node --test scripts/ci/collect_repository_inventory.mjs &amp;&amp; node scripts/ci/ci_monitor.cjs --self-test --verify-wrapper scripts/ci/watch_ci.sh --verify-docs scripts/ci/README.md &amp;&amp; node --test scripts/ci/ci_monitor.cjs &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-recovery --require-all-ref-recovery --require-typed-artifacts --require-complete-categories --require-edge-cases --require-command-provenance --require-privacy-controls --require-determinism &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --records .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json --rendered .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md --expected-repository szTheory/accrue --recovery-manifest "$PHASE229_PRIVATE_MANIFEST" --expected-manifest-sha256 "$PHASE229_MANIFEST_SHA256" --recovery-bundle "$PHASE229_RECOVERY_BUNDLE" --require-recovery --require-all-ref-recovery --require-typed-artifacts --require-complete-categories --require-edge-cases --require-command-provenance --require-privacy-controls --require-determinism --require-workflow-metadata-authorization</automated>
    <fails_when>any command exits non-zero; any CR-01 through CR-10 or WR-01 through WR-03 fixture is missing; user/ref/worktree/tag/capsule state changes; API coverage has an undecided capability; or a Phase 230+ mutation occurs</fails_when>
  </verify>
  <acceptance_criteria>
    Every structured VERIFICATION gap and directly related REVIEW blocker/warning has an executable regression that passes.
    The supported no-argument CI wrapper, exact-SHA failure status, exact recovery restore, strict set equality, all-worktree/window collection, and real remote adapter paths all pass together.
    `git status --short` preserves every pre-existing untracked/user-owned path and shows no mutation outside the two plan-owned evidence files plus normal GSD summary/state tracking.
  </acceptance_criteria>
  <done>Phase 229 has a complete green deterministic regression suite and a preservation-safe, read-only handoff to Phase 230.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| original private capsule → final capture | Immutable recovery authority is supplied privately and must be verified before use. |
| local/GitHub authorities → committed evidence | Complete but sensitive facts are normalized to a public allowlisted schema. |
| final evidence → Phase 230 | Downstream integration relies on exact ref, worktree, window, and remote provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G09-01 | Tampering | original recovery capsule | high | mitigate | Compare anchored manifest/bundle digests, owner/mode, actual bundle heads, encoded refs, and additive attestation before observation. |
| T-229-G09-02 | Spoofing | final remote evidence | high | mitigate | Require actual fixed-repository GET attempt, ISO time, exact full-SHA sets, and explicit unavailable states. |
| T-229-G09-03 | Information Disclosure | canonical artifacts | high | mitigate | Run recursive privacy controls and byte reproduction; keep private paths and payloads out of tracked output. |
| T-229-G09-04 | Repudiation | final phase handoff | high | mitigate | Run one complete command chain covering every reviewed regression and record exact results in the summary. |
| T-229-G09-05 | Denial of Service | live recapture | medium | mitigate | Retain strict subprocess, pagination, item, timeout, and buffer bounds from Plan 229-06. |
</threat_model>

<verification>
Execute the complete command chain once against fixtures and the real canonical pair, then confirm every pre-existing user/ref/tag/worktree/capsule identity remains unchanged.
</verification>

<success_criteria>
The authoritative inventory is complete and reproducible, all recovery and CI command failures are closed by exact regression fixtures, every high ASVS-L1 threat is mitigated, and Phase 229 ends without crossing its read-only safety boundary.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible repository/CI truth while preserving all refs, tags, worktrees, and user artifacts | 05-09 | COVERED | Recovery tracer, complete collector, strict verifier, supported monitor, final capture. |
| REQ | REPO-01 | Complete committed reproducible inventory | 05, 06, 07, 09 | COVERED | Every named category, zero/one/many semantics, deterministic JSON/Markdown. |
| REQ | REPO-02 | Recover every ref and preserve user artifacts safely | 05, 06, 07, 09 | COVERED | Atomic external capsule, exact restoration, independent set equality, unchanged original capsule/artifacts. |
| REQ | REPO-03 | Supported observable list/inspect/watch path | 06, 08, 09 | COVERED | Read-only API inventory observation plus corrected monitor/wrapper exits. |
| RESEARCH | — | No RESEARCH.md; supplied PATTERNS.md analog constraints | 05-08 | COVERED | Existing dependency-free Bash/Node collect-render-verify patterns retained. |
| CONTEXT | D-01–D-03 | Canonical deterministic evidence, provenance, distinct histories | 06, 07, 09 | COVERED | Complete schema, strict fixtures, final projection. |
| CONTEXT | D-04–D-06 | External recovery, unchanged artifacts, no reflog-only recovery | 05, 07, 09 | COVERED | Atomic capsule and exact bundle/ref verification. |
| CONTEXT | D-07–D-09 | Single bounded read-only monitor and provider semantics | 06, 08, 09 | COVERED | Fixed API reads and executable wrapper/failure tests. |
| CONTEXT | D-10 | Stop before integration and mutation | 05-09 | COVERED | Every plan retains the Phase 229 boundary. |
| VERIFICATION | GAP-1 | Complete inventory/live observation/worktrees/windows | 06, 07, 09 | COVERED | CR-06, CR-07 and final recapture. |
| VERIFICATION | GAP-2 | Safe capsule and executable restoration | 05, 07, 09 | COVERED | CR-01–CR-04, CR-10, WR-01–WR-03. |
| VERIFICATION | GAP-3 | Independent all-ref and strict-flag proof | 07, 09 | COVERED | CR-09 and exact one-of-many fixture. |
| VERIFICATION | GAP-4 | Supported watcher defaults and failure exits | 08, 09 | COVERED | CR-05 subprocess fixtures and docs. |
| PROBE | EDGE-1 | Complete named category behavior | 06, 07, 09 | COVERED | Explicit authority and empty/unavailable semantics. |
| PROBE | EDGE-2 | Aliased/equal/touching output identities | 05 | COVERED | Physical pairwise identity and pre-mutation refusal. |
| PROBE | EDGE-3 | Empty/single/null recovery/category inputs | 05-07 | COVERED | Exact behavior fixtures per category. |
| PROBE | EDGE-4 | Stable equal-comparison output order | 06, 07 | COVERED | Full SHA/ID secondary tie-breakers and permutations. |
| PROBE | EDGE-5 | Supported CI command/failure semantics | 08, 09 | COVERED | Main/CI defaults, exact SHA, non-success exits. |

## Artifacts this phase produces

- `.planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json`: final complete canonical repository/CI evidence.
- `.planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md`: final byte-reproducible maintainer view with executable recovery procedure.
- A new additive private final-capture attestation beside the unchanged original capsule; its path/content remains outside committed evidence.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-09-SUMMARY.md` when done.
</output>

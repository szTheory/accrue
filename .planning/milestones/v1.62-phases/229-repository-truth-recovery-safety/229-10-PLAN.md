---
phase: 229-repository-truth-recovery-safety
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/preserve_repository_state.sh
autonomous: true
gap_closure: true
requirements:
  - REPO-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CR-01: the preservation command re-enumerates the complete untracked artifact path/type/digest map immediately before success, and any post-snapshot mutation fails while invocation-owned outputs are still cleanup-owned (D-04, D-05)."
    - "CR-02: every supported symlink digest is SHA-256 over the raw non-dereferenced link-text bytes returned as a Buffer, including non-UTF-8 and embedded/trailing-newline bytes; any runtime/filesystem case that cannot supply those bytes fails closed (D-05)."
    - "All pre-existing refs, user-owned/untracked artifacts, and every existing external recovery-capsule file remain untouched; only newly created invocation-owned outputs may be removed on failure (D-04 through D-06)."
  artifacts:
    - path: scripts/ci/preserve_repository_state.sh
      provides: "Byte-exact artifact reconciliation at the final publication boundary with deterministic mutation plus raw-Buffer non-UTF-8/newline symlink fixtures"
  key_links:
    - from: scripts/ci/preserve_repository_state.sh
      to: "private manifest artifact map"
      via: "one reusable NUL-safe snapshot routine compares the frozen and immediate-pre-PASS path/type/digest maps"
      pattern: "artifact.*snapshot|compare.*artifact"
---

<objective>
Close the two remaining recovery-capsule integrity defects without changing any existing recovery capsule.

Purpose: Ensure preservation PASS remains exact through its final observable boundary and records symlink link text byte-for-byte per D-04, D-05, and D-06.
Output: A hardened `preserve_repository_state.sh` with deterministic CR-01 and CR-02 regression coverage.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-05-SUMMARY.md
@scripts/ci/preserve_repository_state.sh
@scripts/ci/preflight_phase227_candidate.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reconcile one artifact mutation at the final preservation boundary</name>
  <files>scripts/ci/preserve_repository_state.sh</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-05-SUMMARY.md
    scripts/ci/preserve_repository_state.sh
    scripts/ci/preflight_phase227_candidate.sh
  </read_first>
  <reversibility rating="costly">A false recovery PASS can authorize later synchronization against stale user-artifact evidence, so D-04 requires the invariant before the barrier opens.</reversibility>
  <behavior>
    - "A deterministic hook mutates an untracked regular file after the first manifest snapshot and before final success; the command exits non-zero, prints no PASS, and removes only the new invocation's outputs."
    - "The final comparison is exact over the complete sorted relative-path/type/digest map and empty-directory policy, so add/remove/rename/type/digest changes all fail."
    - "A no-mutation control still publishes an exclusively created bundle/manifest/public record triplet and retains its existing ref and bundle guarantees."
  </behavior>
  <action>Start by adding the failing CR-01 fixture inside `self_test`, using an explicit test-only hook at the same boundary where the review probe mutated the artifact. Refactor the current one-time artifact enumeration into a reusable NUL-safe snapshot function that validates normalized relative paths, uses non-dereferencing type inspection, and emits a canonical path/type/digest triple stream. Call it once for the frozen manifest snapshot and again after all final bundle/output validation but before `published=true` and the PASS line. Compare the streams byte-for-byte and fail while `cleanup_run` still owns this invocation's newly published paths. Keep preservation refs after recovery creation as currently documented, but never remove, rewrite, chmod, rename, or replace any user-owned/untracked artifact or any pre-existing private capsule file. Retain the existing exclusive publication, output-alias rejection, bundle digest/head verification, and D-10 read-only phase boundary.</action>
  <verify>
    <automated>bash -n scripts/ci/preserve_repository_state.sh &amp;&amp; bash scripts/ci/preserve_repository_state.sh --self-test</automated>
    <fails_when>the mutation fixture exits zero or prints PASS; a changed, added, removed, renamed, or retyped artifact is accepted; a failed invocation leaves a newly published output; an existing capsule file changes; or the unchanged control no longer passes</fails_when>
  </verify>
  <acceptance_criteria>
    CR-01's exact post-manifest mutation sequence exits non-zero and leaves the fixture artifact itself untouched except for the deliberate fixture mutation.
    The no-mutation path performs the second complete snapshot immediately before success and prints the existing stable PASS line.
    Cleanup is exact-path and invocation-owned; pre-existing outputs, recovery capsules, preservation refs, and unrelated/untracked workspace files are never cleanup targets.
  </acceptance_criteria>
  <done>Preservation success is impossible when the supported artifact map changes after its frozen snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hash raw Buffer symlink link-text bytes without dereferencing</name>
  <files>scripts/ci/preserve_repository_state.sh</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    scripts/ci/preserve_repository_state.sh
  </read_first>
  <behavior>
    - "A Unix symlink whose link text contains invalid UTF-8 bytes plus trailing newline bytes produces SHA-256 over the exact Buffer returned by `fs.readlinkSync(path, { encoding: 'buffer' })`, with no decode/re-encode step."
    - "Embedded-newline, trailing-newline, empty-looking, and non-UTF-8 link-text edge cases remain stable across both snapshots, while the symlink target is never opened or resolved."
    - "If the runtime or filesystem cannot return raw link-text bytes as a Buffer for a discovered symlink, preservation rejects that artifact and prints no PASS instead of falling back to string hashing."
    - "An unchanged symlink passes the immediate-pre-PASS reconciliation; changing only its link text fails the map comparison."
  </behavior>
  <action>Add CR-02 fixtures for embedded newline, trailing newline, empty-looking, and valid Unix non-UTF-8 link text (for example raw `ff fe 0a` bytes), creating and retaining the expected target as a Buffer rather than a JavaScript string. Compute the independent expected SHA-256 directly from that fixture Buffer and separately assert that `fs.readlinkSync(linkPath, { encoding: "buffer" })` returns a byte-identical Buffer. Replace the command-substitution/string digest path with an argv-safe Node helper that receives only the symlink path, calls `fs.readlinkSync` with `encoding: "buffer"`, rejects any non-Buffer result or read failure, and passes that Buffer directly to `crypto.createHash("sha256").update(...)`; never call `toString`, decode/re-encode, trim, normalize, pipe link text through stdout, or fall back to the string overload. Use this same helper in both artifact snapshots so evidence and reconciliation share raw-byte semantics. Where the platform/runtime cannot create or return the non-UTF-8 fixture bytes, exercise an explicit unsupported-byte rejection control and require non-zero/no-PASS behavior rather than silently weakening coverage. Preserve the existing public schema (`type: symlink`, SHA-256 only); never persist link text, absolute paths, or dereferenced target contents, and do not add a package dependency.</action>
  <verify>
    <automated>bash -n scripts/ci/preserve_repository_state.sh &amp;&amp; bash scripts/ci/preserve_repository_state.sh --self-test</automated>
    <fails_when>the non-UTF-8 or newline fixture is absent; the recorded digest differs from SHA-256 of the fixture Buffer; any raw byte is decoded, replaced, normalized, trimmed, or stripped; a non-Buffer/read failure falls back to string hashing or prints PASS; any symlink target is opened/resolved; link text appears in public/private output beyond the existing private path identity; or the full preservation self-test fails</fails_when>
  </verify>
  <acceptance_criteria>
    CR-02's non-UTF-8-plus-trailing-newline probe records exactly SHA-256 of the independently retained raw target Buffer.
    The production helper requires Buffer-returning `fs.readlinkSync(..., { encoding: "buffer" })`; unsupported byte access is a named fail-closed case with non-zero exit and no PASS.
    Embedded-newline, trailing-newline, empty-looking, non-UTF-8, unchanged, and changed-link fixtures have explicit pass/fail assertions.
    The final map reconciliation covers symlinks using these byte-exact digests and retains all CR-01 protections.
  </acceptance_criteria>
  <done>Every supported symlink artifact has raw-byte, non-dereferenced evidence through final preservation PASS, and unsupported byte sequences fail closed.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| user-owned filesystem → preservation snapshot | Concurrent or adversarial artifact changes must invalidate the recovery barrier without modifying user data. |
| symlink metadata → recovery evidence | Untrusted link text must be hashed exactly without target access or shell interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G10-01 | Tampering | final artifact reconciliation | high | mitigate | Re-enumerate the full canonical map immediately before success and fail before ownership of new outputs is released. |
| T-229-G10-02 | Repudiation | symlink digest evidence | high | mitigate | Hash raw link-text bytes with an independent non-dereferencing API and deterministic newline-edge fixtures. |
| T-229-G10-03 | Information Disclosure | private artifacts and capsules | high | mitigate | Persist only relative type/digest evidence; never emit link text, contents, or external capsule locations. |
| T-229-G10-04 | Elevation of Privilege | test hook and cleanup targets | medium | mitigate | Restrict the hook to self-test mode and cleanup to exact invocation-created paths; reject all production hook activation. |
</threat_model>

<verification>
Run the complete Bash self-test twice, then compare status and external capsule hashes captured before execution; only `scripts/ci/preserve_repository_state.sh` may be tracked-modified.
</verification>

<success_criteria>
CR-01 and CR-02 are closed with deterministic process-boundary fixtures, REPO-02 recovery evidence is exact through PASS, and no existing capsule, ref, or user-owned artifact is altered by the implementation or tests.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/preserve_repository_state.sh`: reusable exact artifact snapshot/reconciliation path plus deterministic post-snapshot-mutation and raw-symlink-byte self-tests.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md` when done.
</output>

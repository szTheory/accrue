---
phase: 229-repository-truth-recovery-safety
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/collect_repository_inventory.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CR-03: open pull requests, `release/*` refs, and Actions runs use bounded pagination and are available only after an authoritative terminal page is observed (D-01, D-02)."
    - "Every producing page request is retained in deterministic category-specific provenance; a next page beyond page/item bounds becomes explicit `unavailable:overflow`, never a complete or cached/local substitute."
    - "The collector keeps the exact Phase 229 API coverage matrix: four repository-bound GET capabilities only, with every mutation, dispatch, rerun, cancellation, PR/ref write, ship-window resolution, and publication path absent (D-08, D-10)."
  artifacts:
    - path: scripts/ci/collect_repository_inventory.mjs
      provides: "Bounded complete plural GitHub collection with ordered request-sequence provenance and overflow semantics"
  key_links:
    - from: scripts/ci/collect_repository_inventory.mjs
      to: .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
      via: "the fixed GET adapter paginates only the three integrated plural categories and exposes no mutation surface"
      pattern: "pulls|matching-refs|actions/runs"
---

<objective>
Make every plural remote inventory claim complete, bounded, and provenance-verifiable.

Purpose: Close CR-03 while preserving D-02's exact repository/time/object/request authority and the existing read-only API coverage decisions.
Output: A paginated collector contract and deterministic embedded tests for terminal-page and overflow behavior.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
@.planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/collect_ci_baseline.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove a two-page open-PR observation end to end</name>
  <files>scripts/ci/collect_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/collect_ci_baseline.mjs
  </read_first>
  <reversibility rating="reversible">The pagination schema and adapter stay internal to Phase 229 tooling and can be changed without modifying external repository state.</reversibility>
  <behavior>
    - "A full first PR page causes an exact page-2 GET; a short/empty second page is terminal proof and all full head SHAs are returned in canonical number/SHA order."
    - "A response that indicates or implies another page after the configured page or item cap yields `available:false`, `reason:overflow`, and no `shas` field."
    - "The normalized fact retains the complete ordered producing request sequence, fixed repository, one observation time, and no raw response headers or payloads."
  </behavior>
  <action>Write the failing 100-item-first-page fixture before implementation. Introduce one bounded plural-page helper used through `createGhApiReadAdapter`, with explicit `per_page`, `page`, maximum pages, aggregate item cap, subprocess timeout, and buffer cap. Select an authoritative terminal rule that is testable with the injected adapter: a page shorter than the explicit page size terminates; a full page requires the next page; reaching the configured bound while another page is required fails as overflow. Change plural fact provenance from one self-asserted request string to an allowlisted ordered request sequence, updating schema validation without weakening repository identity, observation time, full-SHA validation, stable ordering, or explicit-unavailable semantics. Keep the singleton `remote_main` request unchanged. Do not introduce `gh pr`, GraphQL, mutations, or any API outside the integrated rows in `COVERAGE.md` per D-08 and D-10.</action>
  <verify>
    <automated>node --check scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/collect_repository_inventory.mjs</automated>
    <fails_when>a full page produces no page-2 read; a truncated result is marked available; page/item overflow retains SHAs; request provenance omits or reorders a page; or the adapter exposes any non-GET/non-allowlisted capability</fails_when>
  </verify>
  <acceptance_criteria>
    CR-03's 100-item PR fixture records an exact page-2 call and includes page-2 values.
    Short first page, full-first/short-second, confirmed-empty, and overflow cases have deterministic assertions.
    Available plural PR evidence includes the complete normalized page request sequence that actually produced it.
  </acceptance_criteria>
  <done>Open-PR evidence cannot be called complete until the bounded collector proves its terminal page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply terminal-page proof to release refs and Actions</name>
  <files>scripts/ci/collect_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    .planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md
    scripts/ci/collect_repository_inventory.mjs
  </read_first>
  <behavior>
    - "Matching release refs request explicit page size/page values and aggregate every terminal-proven page in stable ref/SHA order."
    - "Actions pages aggregate `workflow_runs` and sort by created time, numeric run ID, and SHA; the first full page cannot be accepted alone."
    - "Network, authentication, rate limit, timeout, data-shape, buffer, page, and item failures remain typed unavailable facts with no partial SHA claims."
  </behavior>
  <action>Reuse the Task 1 plural-page helper for matching release refs and Actions runs, adapting only each response extractor and stable comparator. Update the exact endpoint allowlist to accept normalized bounded `per_page`/`page` variants for these categories and reject duplicate, skipped, non-numeric, reordered, or over-bound page parameters. Add embedded tests mirroring the review's three-category 100-item probe, page-2 values, zero/one/many results, malformed page shapes, and cap overflow. Ensure every category uses a single fixed observation timestamp and returns no partial values after any later-page failure. Preserve explicit remote-main singleton semantics and all six unavailable reasons established by Plan 229-06.</action>
  <verify>
    <automated>node --check scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/collect_repository_inventory.mjs</automated>
    <fails_when>release refs omit explicit pagination; Actions accepts a full first page without another read; later-page failure leaks partial SHAs; stable ordering changes; an endpoint outside `COVERAGE.md` is allowed; or any existing collector fixture regresses</fails_when>
  </verify>
  <acceptance_criteria>
    CR-03 is reproduced and closed independently for PRs, release branches, and Actions.
    Each available plural fact has terminal-page proof and the exact complete producing request sequence.
    Bounds and unavailable taxonomy fail closed without cached/local substitution or partial evidence.
  </acceptance_criteria>
  <done>All three plural GitHub categories provide complete bounded truth or honest unavailable evidence.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub paginated responses → canonical inventory | Untrusted remote pages must be complete, bounded, normalized, and tied to exact requests. |
| collector adapter → GitHub | The adapter may perform only the four explicitly integrated repository-bound GET capabilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G11-01 | Spoofing | plural completeness | high | mitigate | Require an explicit terminal page and preserve every normalized producing request before marking evidence available. |
| T-229-G11-02 | Denial of Service | page/item/process bounds | high | mitigate | Enforce page, aggregate-item, timeout, and buffer bounds; emit overflow without partial values. |
| T-229-G11-03 | Tampering | request sequencing | high | mitigate | Validate category-specific ordered page parameters and reject skipped, duplicate, reordered, or foreign endpoints. |
| T-229-G11-04 | Information Disclosure | remote responses | medium | mitigate | Persist only allowlisted SHAs, state, timestamp, repository, and normalized requests; discard payloads and headers. |
</threat_model>

<verification>
Run the collector's embedded Node tests and inspect adapter calls to confirm only fixed repository-bound GET endpoints are reachable; do not run or authorize any remote mutation.
</verification>

<success_criteria>
CR-03 is closed for all plural categories, REPO-01 can distinguish complete from truncated remote truth, and Phase 229's API coverage/read-only boundary remains unchanged.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/collect_repository_inventory.mjs`: bounded terminal-page collector, ordered request-sequence provenance, and first-page-full/page-2/overflow fixtures for PRs, release refs, and Actions.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md` when done.
</output>

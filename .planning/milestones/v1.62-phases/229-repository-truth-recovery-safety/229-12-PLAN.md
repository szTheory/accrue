---
phase: 229-repository-truth-recovery-safety
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/ci_monitor.cjs
autonomous: true
gap_closure: true
requirements:
  - REPO-03
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CR-09: one monotonic absolute deadline covers branch resolution, list, view, polling, and sleep; a one-second timeout cannot report success after the deadline (D-07, D-08)."
    - "WR-02: inspection binds the viewed result to the selected numeric run ID, exact SHA, fixed repository, and requested workflow before attributing jobs or conclusion."
    - "The supported monitor remains one read-only `list|inspect|watch` implementation; wrapper defaults and completed non-success exit 69 remain unchanged, and Actions success remains `provider_proof: non_run` absent separate proof (D-07 through D-09)."
  artifacts:
    - path: scripts/ci/ci_monitor.cjs
      provides: "Absolute-deadline GitHub reads and exact selected/viewed run identity enforcement"
  key_links:
    - from: scripts/ci/ci_monitor.cjs
      to: scripts/ci/watch_ci.sh
      via: "the existing wrapper's timeout flows into one monitor deadline shared by every remote read and sleep"
      pattern: "deadline|remaining.*timeout|run_id|workflow"
---

<objective>
Bind CI observation to one selected run and make its advertised timeout absolute.

Purpose: Close CR-09 and WR-02 while retaining the exact-SHA, fixed-repository, read-only monitor contract from D-07 through D-09.
Output: `ci_monitor.cjs` deadline propagation, run-identity checks, and deterministic process-boundary fixtures.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
@.planning/phases/229-repository-truth-recovery-safety/229-08-SUMMARY.md
@scripts/ci/ci_monitor.cjs
@scripts/ci/watch_ci.sh
@scripts/ci/collect_ci_baseline.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one absolute deadline through a delayed watch</name>
  <files>scripts/ci/ci_monitor.cjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    scripts/ci/ci_monitor.cjs
    scripts/ci/watch_ci.sh
    scripts/ci/collect_ci_baseline.mjs
  </read_first>
  <reversibility rating="reversible">Deadline plumbing changes only the local observation CLI and its injected adapter contract; no external state is changed.</reversibility>
  <behavior>
    - "A real subprocess fixture with two controlled 1.2-second GitHub reads and `--timeout-seconds 1` exits 68 near the one-second bound and never returns a completed success."
    - "One monotonic deadline begins before branch/SHA resolution and is checked before and after each list/view read and before returning success."
    - "Each real GitHub subprocess receives only the positive remaining budget, capped by the existing per-read maximum, and sleep is capped to the remaining duration."
  </behavior>
  <action>Write the failing wall-clock subprocess-delay fixture first, preserving the review's real elapsed-time boundary instead of relying only on an injected logical clock. Compute a single monotonic deadline at the start of `watchSha` before `resolveBranchSha`; pass a remaining-budget function through resolution, `inspectSha`, adapter `listRuns`, and `viewRun`. Extend the real adapter so each `spawnSync` timeout is the lesser of `GH_READ_TIMEOUT_MS` and the positive remaining milliseconds, and translate process timeout/deadline exhaustion to monitor exit 68 for watch. Check the deadline after every remote read and before returning a completed result, then cap `Atomics.wait`/injected sleep to the remaining budget. Retain existing bounds, sanitized errors, exact repository/SHA output, and list/inspect behavior outside watch.</action>
  <verify>
    <automated>node --check scripts/ci/ci_monitor.cjs &amp;&amp; node --test scripts/ci/ci_monitor.cjs</automated>
    <fails_when>the delayed one-second watch returns success; elapsed time reaches the old multi-read overrun; a remote read receives more than the remaining budget; sleep overshoots the deadline; timeout loses repository/SHA attribution; or existing wrapper/completion tests regress</fails_when>
  </verify>
  <acceptance_criteria>
    CR-09's delayed-read fixture exits 68 under a one-second timeout and asserts a bounded wall-clock envelope with platform-tolerant scheduling slack.
    Resolution, list, view, poll, sleep, and final return all consume the same monotonic deadline.
    Successful completion is returned only when the selected run is observed completed before the deadline.
  </acceptance_criteria>
  <done>The supported watch timeout is an absolute end-to-end deadline rather than a between-polls check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind viewed run identity and workflow to selection</name>
  <files>scripts/ci/ci_monitor.cjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    .planning/phases/229-repository-truth-recovery-safety/229-08-SUMMARY.md
    scripts/ci/ci_monitor.cjs
    scripts/ci/watch_ci.sh
  </read_first>
  <behavior>
    - "Selecting run ID 7 and viewing run ID 8 with the same SHA fails with an attributable unavailable/mismatch error before jobs are summarized."
    - "A viewed run whose workflow differs from the explicitly selected workflow fails even when ID and SHA are otherwise valid."
    - "Matching ID/SHA/workflow succeeds; an omitted workflow is accepted only when no workflow selector was requested."
  </behavior>
  <action>Add WR-02 negative fixtures for selected/viewed run-ID mismatch and workflow mismatch plus a positive exact-identity control. In `inspectSha`, retain the normalized selected row, normalize the viewed row, then require viewed `run_id`, `sha`, and—when supplied—`workflow` to equal the selection/request before calling `summarizeFailures` or returning a conclusion. Keep ambiguity/no-match semantics and exact repository/SHA context in the thrown error. Re-run the executable wrapper cases to prove main/CI defaults, explicit SHA precedence, completed non-success exit 69, read-only operation registry, and provider-proof separation are unchanged per D-07 through D-09.</action>
  <verify>
    <automated>node scripts/ci/ci_monitor.cjs --self-test --verify-wrapper scripts/ci/watch_ci.sh &amp;&amp; node --test scripts/ci/ci_monitor.cjs</automated>
    <fails_when>run 8 is accepted after selecting run 7; a requested-workflow mismatch is accepted; mismatch jobs/conclusion are emitted; matching identity fails; wrapper defaults or exact-SHA precedence change; or a mutation operation becomes reachable</fails_when>
  </verify>
  <acceptance_criteria>
    WR-02's same-SHA/different-ID probe fails before failure-detail attribution.
    Workflow mismatch and matching-identity fixtures have explicit outcomes.
    The supported wrapper and direct monitor remain fixed-repository, exact-SHA, bounded, read-only, and provider-proof honest.
  </acceptance_criteria>
  <done>Every inspected or watched result is attributable to the exact selected GitHub run and workflow.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| monitor deadline → GitHub subprocess | Slow or stalled reads must not outlive the caller's absolute watch bound. |
| run list selection → run view attribution | A same-SHA but different run/workflow response must not be accepted as the selected evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G12-01 | Denial of Service | watch deadline | high | mitigate | Use one monotonic deadline and propagate positive remaining time into every read and sleep. |
| T-229-G12-02 | Spoofing | selected/viewed run identity | high | mitigate | Require exact run ID, SHA, repository, and requested workflow equality before attribution. |
| T-229-G12-03 | Repudiation | failure summaries | medium | mitigate | Summarize jobs only after identity validation and retain repository/SHA/run ID in output and errors. |
| T-229-G12-04 | Tampering | monitor operation registry | high | mitigate | Preserve the `list|inspect|watch` surface and `gh run list/view`-only argv gate. |
</threat_model>

<verification>
Run the full monitor self-test, wrapper subprocess suite, and Node tests; inspect adapter calls for `-R szTheory/accrue` and absence of mutation verbs.
</verification>

<success_criteria>
CR-09 and WR-02 are closed with real process-boundary evidence, REPO-03 remains one supported read-only path, and existing wrapper/default/provider semantics do not regress.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/ci_monitor.cjs`: remaining-budget adapter contract, monotonic absolute deadline, exact selected/viewed run binding, and deterministic delayed-process/identity fixtures.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-12-SUMMARY.md` when done.
</output>

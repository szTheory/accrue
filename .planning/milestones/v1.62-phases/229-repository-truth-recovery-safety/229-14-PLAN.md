---
phase: 229-repository-truth-recovery-safety
plan: 14
type: execute
wave: 3
depends_on:
  - 229-13
files_modified:
  - scripts/ci/README.md
  - scripts/ci/phase229_gap_closure.test.mjs
  - scripts/ci/verify_phase229_handoff_invariants.mjs
  - .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
  - .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "WR-01: the documented strict verification command supplies the private manifest, independent manifest digest, and actual bundle through runtime-only variables and succeeds against a generated private capsule."
    - "The canonical JSON/Markdown pair is regenerated only after Plans 229-10 through 229-13 pass, contains terminal-proven remote provenance or honest unavailability, and passes every strict authority/privacy/determinism flag."
    - "Final success is gated by one executable before/after comparison over every pre-existing user-owned/untracked artifact, ref/tag/worktree, and recursively enumerated external recovery-capsule entry; capsule filename bytes, type, content/link digest, mode, uid, and gid remain exact, with only the named collision-exclusive current-owner mode-0600 attestation allowed as a new entry (D-04 through D-06)."
    - "The handoff remains facts and recovery only: no integration, cleanup, fetch/ref refresh, CI mutation, ship-window resolution, PR mutation, push, merge, or publication occurs (D-08, D-10)."
  artifacts:
    - path: scripts/ci/README.md
      provides: "Executable runtime-only strict verification contract"
    - path: scripts/ci/phase229_gap_closure.test.mjs
      provides: "Documentation subprocess fixture plus negative final-handoff invariant fixtures using generated private capsules and repositories"
    - path: scripts/ci/verify_phase229_handoff_invariants.mjs
      provides: "Reusable NUL-safe wrapper that snapshots, runs, and compares protected capsule/workspace identities before permitting final success"
    - path: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
      provides: "Final independently verifiable canonical repository truth"
    - path: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md
      provides: "Final deterministic privacy-safe maintainer projection"
  key_links:
    - from: scripts/ci/README.md
      to: scripts/ci/verify_repository_inventory.mjs
      via: "the copy/paste strict command passes all three runtime-only private authority flags"
      pattern: "recovery-manifest|expected-manifest-sha256|recovery-bundle"
    - from: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json
      to: .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md
      via: "fresh validated rendering is byte-identical after full strict authority reconciliation"
      pattern: "schema_version|manifest_sha256|remotes"
    - from: scripts/ci/verify_phase229_handoff_invariants.mjs
      to: "external recovery capsule, user-owned artifacts, refs/tags, worktrees, and the final verification chain"
      via: "one invocation captures a canonical raw-path-safe before snapshot, runs the allowlisted chain, captures after, and exact-compares before returning success"
      pattern: "snapshot|compare|attestation|run-final-chain"
---

<objective>
Publish executable documentation and recapture the final recovery-backed repository truth after every refreshed gap is closed.

Purpose: Close WR-01 and provide the authoritative REPO-01/02/03 handoff without crossing D-10's Phase 229 boundary.
Output: Runnable strict-verification docs, an executable final-invariant gate with negative fixtures, and a final canonical JSON/Markdown pair.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/MILESTONES.md
@RELEASING.md
@.github/workflows/ci.yml
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-SECURITY.md
@.planning/phases/229-repository-truth-recovery-safety/229-VALIDATION.md
@.planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
@.planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-12-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-13-SUMMARY.md
@scripts/ci/README.md
@scripts/ci/phase229_gap_closure.test.mjs
@scripts/ci/preserve_repository_state.sh
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/render_repository_inventory.mjs
@scripts/ci/verify_repository_inventory.mjs
@scripts/ci/ci_monitor.cjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute the documented strict command against a generated private capsule</name>
  <files>scripts/ci/README.md, scripts/ci/phase229_gap_closure.test.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-VALIDATION.md
    scripts/ci/README.md
    scripts/ci/phase229_gap_closure.test.mjs
    scripts/ci/preserve_repository_state.sh
    scripts/ci/verify_repository_inventory.mjs
  </read_first>
  <reversibility rating="reversible">The documentation and its generated-capsule test are local tracked artifacts and do not modify the original recovery authority.</reversibility>
  <behavior>
    - "The exact README block checks three non-empty runtime variables and supplies `--recovery-manifest`, `--expected-manifest-sha256`, and `--recovery-bundle` before every recovery strict flag."
    - "A test-generated external capsule plus matching canonical fixture executes the extracted documentation command successfully without substituting or exposing a real private path."
    - "Unset, empty, wrong-digest, wrong-bundle, or broad-permission private inputs make the documented command fail closed."
  </behavior>
  <action>First add a failing WR-01 test that extracts the strict verification shell block from `scripts/ci/README.md`, creates an isolated temporary Git repository and private capsule using the public preservation path, exports fixture-only runtime variables, and executes that exact block. Update the README block to use the same `PHASE229_PRIVATE_MANIFEST`, `PHASE229_MANIFEST_SHA256`, and `PHASE229_RECOVERY_BUNDLE` names established by `229-VALIDATION.md`; include all strict flags and the exact canonical records/rendered paths. Add negative subprocess cases for missing/empty variables, wrong digest, wrong bundle, and unsafe manifest mode. The test must create and clean only its temporary capsule; never embed, log, chmod, open for write, replace, or delete the actual Phase 229 capsule or its location. Retain the read-only CI command matrix, provider-proof wording, API coverage decisions, and D-10 exclusions.</action>
  <verify>
    <automated>node --test scripts/ci/phase229_gap_closure.test.mjs &amp;&amp; node scripts/ci/ci_monitor.cjs --self-test --verify-wrapper scripts/ci/watch_ci.sh --verify-docs scripts/ci/README.md</automated>
    <fails_when>the extracted command exits 1 on valid generated inputs; any of the three runtime-only flags is absent; an invalid private input passes; a real capsule path appears in tracked output; docs promise behavior not executed by the fixture; or CI read-only/provider semantics regress</fails_when>
  </verify>
  <acceptance_criteria>
    WR-01's exact README command exits zero against generated valid private authority.
    Every required private input has a deterministic negative subprocess case.
    Documentation contains variable names and flags only, never external locations or private contents.
  </acceptance_criteria>
  <done>A maintainer can copy the supported strict command, supply runtime-only authority, and obtain a real verification result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate final canonical truth on exact capsule and workspace invariants</name>
  <files>scripts/ci/README.md, scripts/ci/phase229_gap_closure.test.mjs, scripts/ci/verify_phase229_handoff_invariants.mjs, .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json, .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/WINDOWS.md
    .planning/MILESTONES.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    .planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-12-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-13-SUMMARY.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/render_repository_inventory.mjs
    scripts/ci/verify_repository_inventory.mjs
    scripts/ci/ci_monitor.cjs
    scripts/ci/phase229_gap_closure.test.mjs
    scripts/ci/preserve_repository_state.sh
    scripts/ci/README.md
  </read_first>
  <precondition>The independently anchored original manifest and actual recovery bundle remain readable, current-user-owned/restrictive as applicable, and byte-identical to their committed digests; otherwise stop without changing them or the canonical pair.</precondition>
  <behavior>
    - "The final JSON exactly matches live refs/worktrees and `.planning/WINDOWS.md`, includes every canonical preservation row, and has terminal-proven remote pages or explicit unavailable facts with exact attempted requests."
    - "The final Markdown is generated only from validated JSON, contains no absolute/private location, and reproduces byte-for-byte."
    - "One checker invocation captures a before snapshot, runs the complete preservation/pagination/verifier/monitor/wrapper/documentation/real-capsule/privacy/determinism chain, captures after, exact-compares every protected identity, and only then exits zero."
    - "Capsule traversal is recursive and raw-path-safe: every pre-existing entry is keyed by encoded filename bytes and compared on type, regular-file or raw-link digest, mode, uid, and gid; adding, deleting, renaming, retyping, rewriting, chmodding, chowning, or mutating any sibling fails."
    - "The protected workspace snapshot covers the complete NUL-delimited untracked path/type/raw digest/mode/owner map, every ref/tag name and object identity, and every worktree path/HEAD/branch-or-detached identity; only the two plan-owned canonical outputs may change."
    - "The sole permitted capsule delta is the exact attestation path named before capture: it must be absent before, created with an exclusive `wx` open as a current-owner regular file, and remain mode 0600; any pre-existing destination or second new entry fails closed."
  </behavior>
  <action>Create `verify_phase229_handoff_invariants.mjs` as the only supported final-success wrapper. Before invoking any final collection or verification command, validate the independently anchored manifest/bundle owner, restrictive mode, and digest; require a caller-supplied capsule directory and exact fresh attestation path; require the attestation to be a direct sibling in that directory and absent at the before boundary. Capture a canonical process-local mode-0600 snapshot outside the repository. Recursively enumerate the capsule with Buffer-returning filesystem APIs, encode raw relative filename bytes losslessly, use `lstat` without following links, hash regular-file bytes and Buffer-returning raw link text, and record type/mode/uid/gid for every entry. Capture workspace authority with NUL-delimited Git output retained as Buffers: the complete untracked entry path/type/raw digest/mode/uid/gid map, every ref/tag name/object pair, and every worktree path/HEAD/branch-or-detached identity. If any required filesystem or Git API cannot provide lossless bytes, fail closed; never line-split or UTF-8-round-trip a protected path.

Have the wrapper invoke the fixed allowlisted final chain with argv arrays and `shell: false`: preservation self-test, collector tests and one corrected bounded GET-only final recapture, deterministic rendering, gap/monitor/wrapper/docs tests, and the full strict verifier using the original runtime-only manifest/digest/bundle values and every strict flag. Do not accept caller-supplied arbitrary commands. After the chain, capture and compare once before attestation and require zero protected deltas; create the sanitized attestation itself using an exclusive `wx` open, current uid/gid, and final mode 0600; capture again and require that the only capsule delta is that exact regular file while workspace identities remain exact. Remove only an attestation created by this invocation if a later check fails, and always remove only the process-local snapshot. Never modify, replace, rename, chmod, chown, open for write, or delete a pre-existing capsule entry or user artifact.

Add process-boundary negative fixtures in `phase229_gap_closure.test.mjs` using test-owned temporary repositories/capsules for sibling add/delete/rename, content/link-digest mutation, regular/symlink type swap, mode change, owner-field mismatch (use a comparator snapshot fixture where unprivileged CI cannot chown), raw non-UTF-8 filename/link bytes, untracked artifact mutation, ref/tag movement, worktree identity change, a pre-existing attestation destination, and an extra new capsule entry. Each must produce non-zero/no final PASS; add a positive exact-invariant control and one control allowing only the exclusive current-owner mode-0600 attestation. Document the exact wrapper command and required runtime-only variables in `scripts/ci/README.md`. Stop at D-10: do not fetch/refresh refs, integrate history, clean files/refs, mutate GitHub/CI/PRs, resolve windows, push, merge, or publish.</action>
  <verify>
    <automated>node --test scripts/ci/phase229_gap_closure.test.mjs &amp;&amp; node scripts/ci/verify_phase229_handoff_invariants.mjs --self-test &amp;&amp; node scripts/ci/verify_phase229_handoff_invariants.mjs --run-final-chain --repository-root . --capsule-directory "$PHASE229_CAPSULE_DIR" --recovery-manifest "$PHASE229_PRIVATE_MANIFEST" --expected-manifest-sha256 "$PHASE229_MANIFEST_SHA256" --recovery-bundle "$PHASE229_RECOVERY_BUNDLE" --attestation "$PHASE229_FINAL_ATTESTATION" --records .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json --rendered .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md --expected-repository szTheory/accrue</automated>
    <fails_when>the wrapper does not capture and consume both snapshots around the entire chain; any child command or strict flag is omitted/non-zero; canonical authority differs from live/private sources; a remote category is truncated; fresh rendering differs; any private path/content leaks; any pre-existing capsule filename/type/digest/mode/uid/gid changes; any user-owned artifact, ref/tag, or worktree identity changes; a sibling add/delete/rename/retype/rewrite/chmod/chown is accepted; the attestation existed before capture, is not created exclusively/current-owner/mode-0600, or is accompanied by another new entry; raw path bytes are line-split or UTF-8-round-tripped; or an out-of-scope mutation occurs</fails_when>
  </verify>
  <acceptance_criteria>
    All CR-01 through CR-09 and WR-01 through WR-03 regression evidence is green before the final pair is accepted.
    REPO-01, REPO-02, and REPO-03 strict verification passes against the unchanged anchored original capsule.
    The exact documented wrapper command captures its own before/after snapshots, runs the whole final chain between them, and cannot exit zero without exact comparison of every protected entry and identity.
    Every pre-existing recursive capsule entry retains losslessly encoded filename bytes, type, content/raw-link digest, mode, uid, and gid; the only delta is the exact before-named, formerly absent, exclusive current-owner mode-0600 attestation.
    Every pre-existing user-owned/untracked artifact retains raw path/type/digest/mode/owner identity, and every ref/tag/worktree retains its exact before object/branch/detached/path identity.
    Named negative fixtures prove sibling mutation plus filename/type/digest/mode/owner, untracked, ref/tag, worktree, and attestation-policy mismatches each block final PASS.
    `git status --short` contains no source/evidence changes outside the five plan-owned tracked files plus the known pre-existing user-owned/untracked paths.
  </acceptance_criteria>
  <done>Phase 229 has a final executable, independently verified, privacy-safe recovery and repository-truth handoff with no integration or cleanup action.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| documentation → maintainer shell | Copy/paste instructions must require all private authority inputs without leaking their values. |
| original private capsule → final canonical evidence | Immutable external authority may be read and verified but never replaced or weakened. |
| final verification chain → success exit | A reusable gate must snapshot and exact-compare all protected capsule/workspace identities around the complete chain before PASS. |
| live repository/GitHub → committed inventory | Final facts must be exact, complete or honestly unavailable, sanitized, and deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G14-01 | Repudiation | documented strict command | high | mitigate | Execute the exact extracted block against generated valid/invalid private capsules and require all runtime-only flags. |
| T-229-G14-02 | Tampering | original recovery capsule and workspace identity | high | mitigate | Recursively snapshot raw filename/path bytes plus type/digest/mode/uid/gid and Git identities around the entire allowlisted chain; allow only the exact exclusive current-owner mode-0600 attestation. |
| T-229-G14-03 | Spoofing | final repository/remote facts | high | mitigate | Run exact live/private authority reconciliation and terminal-page provenance before accepting the canonical pair. |
| T-229-G14-04 | Information Disclosure | JSON/Markdown/docs | high | mitigate | Keep private paths in process variables and run comprehensive path/control/privacy rejection plus byte reproduction. |
| T-229-G14-05 | Elevation of Privilege | phase boundary | medium | mitigate | Keep the adapter GET-only and explicitly prohibit integration, cleanup, CI/PR/ref mutation, window resolution, push, merge, and publication. |
</threat_model>

<verification>
Run the exact `verify_phase229_handoff_invariants.mjs --run-final-chain` command above with runtime-only private variables. Its zero exit must itself prove before/after capsule and workspace snapshots were captured, consumed, and exact-compared around the whole chain; a later manual comparison or status label is insufficient.
</verification>

<success_criteria>
WR-01 is executable, the final canonical pair closes every remaining truth in `229-VERIFICATION.md`, all three REPO requirements have fresh behavioral proof, and the executable final gate proves every pre-existing capsule entry and protected workspace identity remains exact before it returns success.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible repository/CI truth preserving every ref, tag, worktree, and user artifact | 10-14 | COVERED | Final evidence depends on exact artifact, pagination, verifier, monitor, and docs closure. |
| REQ | REPO-01 | Complete committed reproducible inventory | 11, 13, 14 | COVERED | Terminal-page collection, independent category authority, and final recapture. |
| REQ | REPO-02 | Recover every pre-existing ref/artifact without destructive cleanup | 10, 13, 14 | COVERED | Final artifact reconciliation, exact ref maps, and immutable capsule proof. |
| REQ | REPO-03 | One supported exact-SHA list/inspect/watch path | 12, 13, 14 | COVERED | Absolute deadline, run binding, adversarial suite, and executable docs. |
| RESEARCH | — | No RESEARCH.md; use supplied pattern map | 10-14 | COVERED | Every plan cites `229-PATTERNS.md` and its specific analogs. |
| CONTEXT | D-01 | Canonical machine source plus deterministic Markdown | 11, 13, 14 | COVERED | Exact provenance/schema verification and final byte projection. |
| CONTEXT | D-02 | Repository/time/SHA/producing-command remote authority | 11, 13, 14 | COVERED | Complete ordered requests and exact category contracts. |
| CONTEXT | D-03 | Keep live/cached/local/milestone/tag histories distinct | 13, 14 | COVERED | Strict role/ref reconciliation retains named distinctions. |
| CONTEXT | D-04 | Named refs and verified external bundle before observation | 10, 13, 14 | COVERED | Final artifact barrier plus immutable private authority checks. |
| CONTEXT | D-05 | Hash user-owned artifacts read-only | 10, 13, 14 | COVERED | Byte-exact symlinks, immediate final snapshot, pre/post handoff proof. |
| CONTEXT | D-06 | No reflog-only recovery | 10, 13, 14 | COVERED | Existing named refs/bundle remain exact and independently reconciled. |
| CONTEXT | D-07 | Single monitor plus thin wrapper | 12, 13, 14 | COVERED | Monitor owns deadline/identity behavior; wrapper remains delegation only. |
| CONTEXT | D-08 | Read-only monitoring | 11-14 | COVERED | Existing API coverage matrix is unchanged and mutation verbs remain unreachable. |
| CONTEXT | D-09 | Actions success differs from provider proof | 12, 14 | COVERED | Existing `non_run` semantics are regression-tested and documented. |
| CONTEXT | D-10 | Stop before integration | 10-14 | COVERED | Every plan forbids integration, cleanup, remote mutation, and publication. |
| GAP | CR-01 | Final artifact reconciliation | 10, 13, 14 | COVERED | Production fix, named adversarial test, and wrapper-owned before/after final gate. |
| GAP | CR-02 | Byte-exact symlink hashing | 10, 13, 14 | COVERED | Raw-Buffer non-UTF-8 link digest fixtures and final artifact proof. |
| GAP | CR-03 | Bounded plural pagination | 11, 13, 14 | COVERED | Three-category page-2/overflow fixtures and final recapture. |
| GAP | CR-04 | Fabricated active object | 13, 14 | COVERED | Live ref/object/existence reconciliation. |
| GAP | CR-05 | Missing canonical preservation rows | 13, 14 | COVERED | Exact encoded preservation map comparison. |
| GAP | CR-06 | Missing worktree/window authority | 13, 14 | COVERED | Direct exact authority multisets. |
| GAP | CR-07 | Wrong same-repository provenance | 13, 14 | COVERED | Exact per-category request sequences. |
| GAP | CR-08 | Absolute/private path acceptance | 13, 14 | COVERED | POSIX/Windows/UNC/file-URI/control rejection. |
| GAP | CR-09 | Non-absolute watch timeout | 12, 13, 14 | COVERED | Monotonic deadline and delayed subprocess regression. |
| GAP | WR-01 | Unrunnable strict docs command | 14 | COVERED | Extracted exact command runs against generated capsule. |
| GAP | WR-02 | Selected/viewed run mismatch | 12, 13 | COVERED | Exact ID/SHA/workflow binding and named regression. |
| GAP | WR-03 | Missing adversarial suite | 13, 14 | COVERED | Full named matrix plus final chain. |

## Artifacts this phase produces

- `scripts/ci/README.md`: executable runtime-only strict verification block.
- `scripts/ci/phase229_gap_closure.test.mjs`: extracted-documentation subprocess fixture plus negative final-handoff invariant matrix.
- `scripts/ci/verify_phase229_handoff_invariants.mjs`: reusable raw-path-safe before/after snapshot, allowlisted-chain, exact-comparison, and exclusive-attestation gate.
- `.planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json`: final complete canonical authority.
- `.planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.md`: final deterministic sanitized projection.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-14-SUMMARY.md` when done.
</output>

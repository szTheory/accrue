---
phase: 229-repository-truth-recovery-safety
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/collect_repository_inventory.mjs
  - scripts/ci/render_repository_inventory.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Canonical remote facts accept only exact observed or unavailable states, with explicit boolean availability and mutually exclusive value/reason fields (D-01, D-02)."
    - "Plural GitHub facts render their complete ordered requests for zero, one, many, unavailable, and multi-page results; no provenance cell is derived from a nonexistent singular field."
    - "The inventory carries a point-in-time active-ref/worktree capture anchor that later strict verification can prove as an ancestor after ordinary phase commits, while inactive authority remains exact (D-03, D-10)."
  artifacts:
    - path: scripts/ci/collect_repository_inventory.mjs
      provides: "Exact remote-fact schema plus explicit captured-at active/worktree authority"
    - path: scripts/ci/render_repository_inventory.mjs
      provides: "Deterministic singleton/plural request provenance projection"
  key_links:
    - from: scripts/ci/collect_repository_inventory.mjs
      to: scripts/ci/render_repository_inventory.mjs
      via: "validated singleton request and plural requests schemas plus capture anchor"
      pattern: "available|state|requests|captured"
---

<objective>
Repair the canonical inventory schema and rendered provenance before another final capture.

Purpose: Close CR-04 and CR-09 and establish the non-self-referential capture record needed for CR-03 without weakening D-01 through D-03.
Output: Exact remote normalization, a captured-at active/worktree authority, and deterministic plural request rendering.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-14-SUMMARY.md
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/render_repository_inventory.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one malformed remote fact through collect, validate, and render</name>
  <files>scripts/ci/collect_repository_inventory.mjs, scripts/ci/render_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/render_repository_inventory.mjs
  </read_first>
  <behavior>
    - "Deleting `available`, using a non-boolean value, or pairing `available:true` with any state except `observed` fails before rendering."
    - "Observed singleton facts contain exactly `sha` and no `reason`/`shas`; observed plural facts contain exactly `shas` and no `reason`/`sha`."
    - "Unavailable facts use state `unavailable`, one allowlisted reason, no SHA field, and the exact request or attempted request sequence."
    - "Validation returns an inventory whose remote rows are the exact normalized values rather than validating temporary copies and retaining malformed input."
  </behavior>
  <action>Write CR-09 RED fixtures covering the full missing/true/false/non-boolean availability and observed/unavailable/arbitrary-state cross-product through `validateInventory` and `renderRepositoryInventory`. Tighten `normalizeRemoteFact` so `available` must be a literal boolean, `state` is exact rather than defaulted, observed and unavailable value sets are mutually exclusive, plural/singleton provenance shapes cannot mix, and unavailable reasons remain allowlisted. Rebuild or replace each remote row with its normalized result inside the returned validated inventory so malformed caller objects cannot survive validation. Preserve fixed repository, ISO timestamp, full-SHA, bounded request, terminal-page, and D-09 provider-proof semantics.</action>
  <verify>
    <automated>node --check scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --check scripts/ci/render_repository_inventory.mjs</automated>
    <fails_when>an omitted/non-boolean availability or arbitrary state passes; a reason and SHA coexist; validation retains a malformed source row; an unavailable row loses attempted provenance; or an existing terminal-page test regresses</fails_when>
  </verify>
  <acceptance_criteria>
    CR-09's omitted-availability and fabricated-state probes fail through the public validator and renderer.
    Every available/unavailable singleton/plural schema combination has an explicit positive or negative assertion.
    Returned inventory rows are canonical normalized values with no coercive defaults.
  </acceptance_criteria>
  <done>Malformed remote state cannot become contradictory canonical or rendered evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render plural request sequences and emit a captured-at anchor</name>
  <files>scripts/ci/collect_repository_inventory.mjs, scripts/ci/render_repository_inventory.mjs</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-14-SUMMARY.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/render_repository_inventory.mjs
  </read_first>
  <behavior>
    - "Plural zero/one/many/unavailable/multi-page rows render every ordered request in one deterministic escaped representation, while remote main renders its singleton request."
    - "No rendered row contains an absent-field serialization, and JSON-to-Markdown byte reproduction is stable across repeated renders."
    - "Collection records one active symbolic ref, its captured commit, and the primary worktree branch/HEAD identity at the same boundary; the captured commit need not equal a later live HEAD."
    - "The capture anchor contains only repository-relative/sanitized identities and exact object IDs, never a physical worktree or capsule path."
  </behavior>
  <action>Add CR-04 RED fixtures for plural observed-empty, one request, multiple results, unavailable, and multi-page evidence, asserting ordered provenance in the rendered bytes. Make the renderer select `request` only for `remote_main` and `requests` for each plural category, then escape and join every producing request with a deterministic category-independent format. Extend the exact top-level inventory schema with a capture object containing the active full symbolic ref, captured commit SHA, and sanitized primary-worktree branch/HEAD identity collected at one point before output; require internal equality among those capture values and the captured active row while leaving later-live ancestry to the independent verifier. Do not persist physical paths, infer future commits, or relax exact inactive refs per D-03 and D-10.</action>
  <verify>
    <automated>node --test scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/verify_repository_inventory.mjs</automated>
    <fails_when>any plural request is missing, reordered, or rendered from a singular field; an absent field appears in Markdown; repeated rendering differs; the capture anchor lacks an active ref/full commit/primary-worktree identity; or a physical/private path is persisted</fails_when>
  </verify>
  <acceptance_criteria>
    CR-04's three affected canonical rows preserve complete request provenance in Markdown.
    Zero, one, many, unavailable, and multi-page plural cases have byte-level expected rendering assertions.
    A captured-at active/worktree anchor exists and is internally exact without claiming equality to future live HEAD.
  </acceptance_criteria>
  <done>Canonical source and Markdown preserve exact remote provenance and a durable point-in-time repository anchor.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub facts → canonical inventory | Untrusted remote state must satisfy an exact non-coercive schema before persistence. |
| canonical JSON → Markdown | Complete request provenance and capture identity must survive deterministic rendering without private data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G15-01 | Spoofing | remote availability/state | high | mitigate | Require exact boolean availability, exact states, and mutually exclusive result/reason fields. |
| T-229-G15-02 | Repudiation | plural request projection | high | mitigate | Render every ordered plural request and lock zero/one/many/unavailable/multipage bytes in tests. |
| T-229-G15-03 | Tampering | captured-at authority | high | mitigate | Record one internally consistent active ref/commit/worktree anchor for independent ancestry verification. |
| T-229-G15-04 | Information Disclosure | capture and request evidence | medium | mitigate | Persist only repository identity, sanitized refs, exact SHAs, and normalized GET requests. |
</threat_model>

<verification>
Run collector, renderer, and verifier tests. Confirm no committed fixture contains an external capsule or physical worktree path.
</verification>

<success_criteria>
CR-04 and CR-09 are closed, a later-commit-safe capture contract exists for CR-03, and REPO-01 canonical evidence is exact and deterministic.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/collect_repository_inventory.mjs`: exact remote schema and point-in-time capture authority.
- `scripts/ci/render_repository_inventory.mjs`: deterministic singleton/plural provenance projection.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-15-SUMMARY.md` when done.
</output>

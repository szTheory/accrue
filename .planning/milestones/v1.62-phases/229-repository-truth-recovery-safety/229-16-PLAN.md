---
phase: 229-repository-truth-recovery-safety
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/preserve_repository_state.sh
autonomous: true
gap_closure: true
requirements:
  - REPO-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every failure after preservation-ref creation restores the exact pre-invocation ref/output/index/worktree/artifact state or retains a concurrently changed ref without deleting another actor's value (D-04 through D-06)."
    - "Only refs proven absent before this invocation and still equal to the invocation's recorded object are eligible for rollback; pre-existing refs are never rollback targets."
    - "Failure injection on a fresh repository reaches each fallible preparation/publication boundary and proves zero leaked refs or files, closing CR-10."
  artifacts:
    - path: scripts/ci/preserve_repository_state.sh
      provides: "Transactional preservation-ref creation and exact invocation-owned rollback tests"
  key_links:
    - from: scripts/ci/preserve_repository_state.sh
      to: "refs/accrue-preserve/phase-229 and private capsule outputs"
      via: "one invocation ledger and cleanup trap conditionally roll back only unchanged newly created refs/files"
      pattern: "update-ref|delete|cleanup|created"
---

<objective>
Make preservation failure transactional across both refs and capsule outputs.

Purpose: Close CR-10 while preserving D-04's recovery barrier and never deleting pre-existing or concurrently changed authority.
Output: Late ref publication, exact rollback bookkeeping, and fresh-repository injected-failure coverage.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
@scripts/ci/preserve_repository_state.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reproduce a post-manifest failure on a fresh repository</name>
  <files>scripts/ci/preserve_repository_state.sh</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
    scripts/ci/preserve_repository_state.sh
  </read_first>
  <behavior>
    - "A fresh repository reaches the post-manifest injected failure, exits non-zero with no PASS, leaves no invocation-created preservation ref, and publishes no capsule output."
    - "The test proves injection was reached rather than accepting an earlier collision or validation failure."
    - "Pre/post exact snapshots cover all refs, index/status, worktrees, artifacts, and output-directory entries."
  </behavior>
  <action>Write the CR-10 RED test first. Create a separate fresh repository and absent output triplet for every injected boundary instead of reusing refs from a prior successful self-test. Emit a test-owned reach marker immediately before each injected failure and require it in the assertion, then exact-compare `for-each-ref`, NUL-safe status/index, worktree porcelain, artifact triples, and capsule-directory entries before and after. The production path must not gain a caller-selectable mutation hook; keep injection confined to self-test-owned repositories and the existing internal test mechanism.</action>
  <verify>
    <automated>bash -n scripts/ci/preserve_repository_state.sh &amp;&amp; bash scripts/ci/preserve_repository_state.sh --self-test</automated>
    <fails_when>the test does not prove the injection point was reached; any ref or output remains; index/status/worktree/artifact state differs; an earlier collision is mistaken for coverage; or a pre-existing capsule path is touched</fails_when>
  </verify>
  <acceptance_criteria>
    The exact review probe is reproduced on a fresh repository and initially exposes the leaked ref.
    Each failure case records a reached marker and compares complete pre/post authorities.
    Test cleanup targets only the test-owned temporary repository and capsule.
  </acceptance_criteria>
  <done>CR-10 has a trustworthy failing process-boundary regression instead of a collision-short-circuited test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Roll back only invocation-created unchanged refs</name>
  <files>scripts/ci/preserve_repository_state.sh</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    scripts/ci/preserve_repository_state.sh
  </read_first>
  <behavior>
    - "All fallible artifact enumeration, bundle construction/verification, manifest preparation, and output validation complete before preservation refs are published where ordering permits."
    - "A cleanup ledger records each ref proven absent and created by this invocation; failure deletes it only with an old-object compare against the invocation's expected SHA."
    - "If a created ref changes concurrently, cleanup refuses deletion, reports the conflict, leaves the new value intact, and still cleans only invocation-owned temporary files."
    - "The success path retains every intended preservation ref and verified capsule output."
  </behavior>
  <action>Reorder fallible preparation ahead of ref publication without weakening the requirement that recovery refs and verified bundle exist before any observation/ref refresh. Track each ref only after `git update-ref` atomically proves the expected absent old value. In the EXIT trap, when the invocation has not committed success, delete each ledgered ref using `git update-ref -d REF EXPECTED_OBJECT`; never delete by name alone and never include a ref that existed before invocation. If compare-and-delete fails because another actor changed the ref, report a rollback conflict and preserve the changed value. Apply the same exact invocation-owned discipline to temporary and published output paths, then add successful, each-boundary failure, pre-existing-ref, and concurrent-ref-change fixtures.</action>
  <verify>
    <automated>bash -n scripts/ci/preserve_repository_state.sh &amp;&amp; bash scripts/ci/preserve_repository_state.sh --self-test &amp;&amp; bash scripts/ci/preserve_repository_state.sh --self-test</automated>
    <fails_when>an injected failure leaks an unchanged created ref; cleanup deletes a pre-existing or concurrently changed ref; any output survives failed publication; success loses intended refs or bundle proof; or either consecutive self-test fails</fails_when>
  </verify>
  <acceptance_criteria>
    Every fresh-repository injected boundary returns to the exact pre-invocation ref/output/workspace state.
    Rollback uses object-guarded compare-and-delete for a recorded invocation-owned set only.
    The concurrent-change fixture retains the foreign value and reports the incomplete rollback rather than deleting it.
  </acceptance_criteria>
  <done>Preservation ref and capsule publication are transactional without making concurrent or pre-existing authority a cleanup target.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| repository refs → preservation namespace | Ref creation must be atomic, attributable, and reversible on invocation failure. |
| preservation process → capsule outputs | Partial publication must not survive failure or overwrite existing recovery authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G16-01 | Tampering | failed preservation refs | high | mitigate | Ledger absent-before refs and use object-guarded rollback on failure. |
| T-229-G16-02 | Elevation of Privilege | rollback target selection | high | mitigate | Never delete by namespace/name alone; exclude pre-existing and changed refs. |
| T-229-G16-03 | Repudiation | injected failure coverage | high | mitigate | Require reached markers and exact fresh-repository pre/post snapshots. |
| T-229-G16-04 | Information Disclosure | private output cleanup | medium | mitigate | Keep all fixture paths runtime-only and cleanup exact invocation-owned entries. |
</threat_model>

<verification>
Run the preservation self-test twice. Confirm repository status and every pre-existing external capsule digest are unchanged.
</verification>

<success_criteria>
CR-10 is closed at real failure boundaries, REPO-02 preservation can be retried cleanly, and no pre-existing or concurrently changed ref is deleted.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/preserve_repository_state.sh`: transactional ref/output publication with object-guarded rollback and fresh-repository failure tests.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-16-SUMMARY.md` when done.
</output>

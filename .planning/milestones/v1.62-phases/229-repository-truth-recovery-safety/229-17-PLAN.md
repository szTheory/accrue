---
phase: 229-repository-truth-recovery-safety
plan: 17
type: execute
wave: 2
depends_on:
  - 229-15
files_modified:
  - scripts/ci/verify_repository_inventory.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The captured active commit and captured primary-worktree HEAD may precede later phase commits only when the active ref/branch identity is unchanged and the captured commit is independently proven as an ancestor of the live object; inactive refs/worktrees remain exact (D-03, D-10)."
    - "Strict complete-category verification independently hashes `.planning/MILESTONES.md` and `.planning/STATE.md`, or proves their exact absence, instead of trusting recorded strings."
    - "Standalone strict recovery opens the bundle without following symlinks, requires current-owner regular-file identity, and detects replacement around hashing and Git bundle verification, closing WR-02."
  artifacts:
    - path: scripts/ci/verify_repository_inventory.mjs
      provides: "Later-commit-safe capture ancestry, independent planning digests, and no-follow stable bundle authority"
  key_links:
    - from: scripts/ci/verify_repository_inventory.mjs
      to: "live active ref/worktrees, planning files, and private recovery bundle"
      via: "independent Git/filesystem authorities reconcile capture ancestry and exact inactive state"
      pattern: "merge-base|MILESTONES|STATE|O_NOFOLLOW|bundle"
---

<objective>
Make strict inventory verification independent and stable across ordinary later commits.

Purpose: Close CR-03, CR-08, and WR-02 while retaining exact frozen/inactive recovery guarantees from D-03 through D-06.
Output: An ancestry-based capture verifier, independent planning digest checks, and hardened bundle opening.
</objective>

<execution_context>
@/Users/dev/.codex/gsd-core/workflows/execute-plan.md
@/Users/dev/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-13-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-15-SUMMARY.md
@scripts/ci/verify_repository_inventory.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verify one captured inventory after later active-branch commits</name>
  <files>scripts/ci/verify_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-13-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-15-SUMMARY.md
    scripts/ci/verify_repository_inventory.mjs
  </read_first>
  <behavior>
    - "A fixture captures inventory at commit A, commits the canonical pair at B and another phase artifact at C, then strict verification at C succeeds because A is an ancestor on the same live active ref."
    - "A nonexistent, sibling/divergent, descendant-nominated, wrong-ref, wrong-primary-worktree, or rewritten capture anchor fails."
    - "Every inactive frozen ref and non-primary worktree remains exact; only the identified active branch/primary worktree receives the ancestry exception."
  </behavior>
  <action>Write the CR-03 RED integration fixture with real commits A, B, and C before changing verification. Replace exact-live equality for the captured active row and captured primary worktree with an explicit point-in-time rule: independently resolve the live symbolic ref and primary worktree, require the same recorded ref/branch identity, prove the captured commit exists, and use `git merge-base --is-ancestor CAPTURED LIVE` with shell disabled. Require the captured active row, milestone role, capture commit, and captured primary-worktree HEAD to agree at A. Continue exact set/object equality for every inactive original/preservation ref, tag, and non-primary worktree. Reject detached or ambiguous live primary identity unless the capture schema explicitly and exactly represents that state.</action>
  <verify>
    <automated>node --test scripts/ci/verify_repository_inventory.mjs &amp;&amp; node --test scripts/ci/phase229_gap_closure.test.mjs</automated>
    <fails_when>the A-to-B-to-C happy path fails; a divergent/nonexistent/wrong-ref anchor passes; more than the one active primary identity receives ancestry tolerance; inactive refs/worktrees can advance; or object existence is inferred from shape</fails_when>
  </verify>
  <acceptance_criteria>
    A real post-capture canonical commit and later phase commit leave strict verification green.
    Each fabricated or divergent capture variant fails the public strict verifier.
    Frozen/inactive exact-map guarantees remain unchanged.
  </acceptance_criteria>
  <done>Canonical inventory authority no longer self-invalidates when the active branch advances through later phase commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute planning digests and open bundle authority without following links</name>
  <files>scripts/ci/verify_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-07-SUMMARY.md
    scripts/ci/verify_repository_inventory.mjs
  </read_first>
  <behavior>
    - "Planning facts equal the independently computed lowercase SHA-256 of each bounded regular file or the exact `absent` marker when lstat proves absence."
    - "Fabricated, swapped, stale, malformed, symlinked, missing, or changed planning authority fails strict complete-category verification."
    - "A bundle symlink, non-regular file, wrong owner, broad mode where restricted mode is required, or replacement during verification fails before recovery is accepted."
    - "The manifest digest, bundle byte digest, `git bundle verify`, and `git bundle list-heads` all apply to one stable bundle identity."
  </behavior>
  <action>Add CR-08 and WR-02 RED fixtures first. For `.planning/MILESTONES.md` and `.planning/STATE.md`, use lstat/no-follow regular-file checks, bounded descriptor reads, exact SHA-256, and an explicit absent branch; compare the computed values to canonical planning fields and reject every other form. Harden `bundleMap` by opening with no-follow semantics where supported, requiring a current-user-owned regular file and restrictive identity, hashing from that descriptor, and bracketing Git verification/list-heads with exact pre/post device/inode/type/mode/uid/gid/size/mtime identity plus a second descriptor digest. Use a descriptor-backed path for Git where the platform supports it; otherwise retain the pre/post identity and byte equality checks and fail on any change. Never resolve a symlink to obtain bundle authority.</action>
  <verify>
    <automated>node --test scripts/ci/verify_repository_inventory.mjs &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-recovery --require-all-ref-recovery --require-complete-categories</automated>
    <fails_when>a fabricated/swapped/stale planning digest passes; absent and empty are conflated; a planning or bundle symlink is followed; foreign/non-regular/replaced bundle authority passes; Git verifies different bytes than were hashed; or valid strict fixtures regress</fails_when>
  </verify>
  <acceptance_criteria>
    CR-08's fabricated planning values fail against independent file bytes/absence.
    WR-02's bundle symlink fails, and replacement/stable-identity cases have deterministic coverage.
    Valid recovery still reconciles manifest, one stable bundle identity, bundle heads, and preservation refs exactly.
  </acceptance_criteria>
  <done>Strict verification derives planning and bundle truth from independent stable authorities rather than caller assertions or followed aliases.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| captured canonical state → later live repository | Only same-ref ancestry may explain active primary advancement; all other authority stays exact. |
| filesystem planning/bundle inputs → strict verifier | Files and recovery bundles are untrusted until opened no-follow and independently hashed/verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G17-01 | Spoofing | captured active/worktree authority | high | mitigate | Prove object existence, same live ref identity, and ancestor relation; keep inactive state exact. |
| T-229-G17-02 | Tampering | planning digests | high | mitigate | Recompute bounded no-follow file digests or exact absence independently. |
| T-229-G17-03 | Elevation of Privilege | bundle alias/replacement | high | mitigate | Require stable current-owner regular-file identity, no-follow open, descriptor hashing, and pre/post checks. |
| T-229-G17-04 | Repudiation | recovery proof | high | mitigate | Tie manifest digest, bundle bytes, Git verification/heads, and refs to one checked identity. |
</threat_model>

<verification>
Run verifier embedded and strict fixture suites, including the A-to-B-to-C post-capture integration and symlink/replacement negative cases.
</verification>

<success_criteria>
CR-03, CR-08, and WR-02 are closed; strict verification remains valid after later active-branch commits and independently proves planning and bundle authority.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/verify_repository_inventory.mjs`: later-commit capture ancestry, independent planning digests, and stable no-follow bundle verification.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-17-SUMMARY.md` when done.
</output>

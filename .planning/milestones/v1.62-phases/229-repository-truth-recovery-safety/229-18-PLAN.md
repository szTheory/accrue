---
phase: 229-repository-truth-recovery-safety
plan: 18
type: execute
wave: 2
depends_on:
  - 229-15
files_modified:
  - scripts/ci/collect_repository_inventory.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Final artifact collection hashes symlink link text directly from a Buffer returned by a non-dereferencing read; invalid UTF-8 and trailing-newline bytes retain the frozen digest (D-05)."
    - "The separate workflow-metadata authorization is mandatory, opened with owner/mode/no-follow controls, and exact-map-equal to both the final-capture attestation and the manifest-to-live artifact transition."
    - "No authorization or attestation can add paths, omit paths, change types/states, or authorize any artifact outside the two fixed workflow metadata paths; every other artifact remains byte-identical (D-04 through D-06)."
  artifacts:
    - path: scripts/ci/collect_repository_inventory.mjs
      provides: "Raw-Buffer artifact collection and byte-exact three-authority workflow metadata reconciliation"
  key_links:
    - from: scripts/ci/collect_repository_inventory.mjs
      to: "private manifest, workflow authorization, final-capture attestation, and live filesystem"
      via: "one exact path/type/before/after/state map is independently derived and compared"
      pattern: "artifactAuthorization|readlinkSync|before_sha256|after_sha256"
---

<objective>
Make artifact and workflow-metadata authorization byte-exact end to end.

Purpose: Close CR-02 and CR-05 so REPO-02 collection cannot decode symlink evidence or discard its separate authorization authority.
Output: Buffer-only symlink hashing, hardened private-input opening, and exact manifest/authorization/attestation/live reconciliation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-14-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-15-SUMMARY.md
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/preserve_repository_state.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve and collect one unchanged invalid-UTF-8 symlink</name>
  <files>scripts/ci/collect_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/preserve_repository_state.sh
  </read_first>
  <behavior>
    - "A generated capsule freezes a symlink with raw link bytes including invalid UTF-8 and trailing newline, then unchanged final collection succeeds with the identical SHA-256."
    - "Changing any raw link byte fails final artifact reconciliation; the symlink target is never opened or resolved."
    - "A non-Buffer return or unsupported raw-byte read fails closed and does not fall back to string conversion."
  </behavior>
  <action>Write the CR-02 RED test as a generated repository/capsule flow that invokes preservation and public collection, retaining an independent expected digest over the original Buffer. Change `currentArtifact` so symlinks call `fs.readlinkSync(full, { encoding: "buffer" })`, assert the result is a Buffer, and feed it directly to SHA-256. Apply the same helper to initial and final artifact comparison. Never call the string overload, decode, normalize, trim, dereference, or persist link text. Add unchanged, changed-byte, trailing-newline, invalid-UTF-8, non-Buffer, and read-failure assertions, with an explicit fail-closed platform path where raw fixture creation is unsupported.</action>
  <verify>
    <automated>node --check scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/collect_repository_inventory.mjs</automated>
    <fails_when>the generated unchanged raw symlink fails; decoded replacement bytes are hashed; a changed raw byte passes; target contents are read; a non-Buffer falls back; or the collector's existing artifact tests regress</fails_when>
  </verify>
  <acceptance_criteria>
    CR-02's exact invalid-UTF-8-plus-newline preservation/collection path passes unchanged and fails after one-byte link drift.
    The production path requires a Buffer and hashes it without any text conversion.
    Unsupported raw-byte access exits non-zero without an accepted inventory.
  </acceptance_criteria>
  <done>Supported symlink artifacts retain one exact raw-byte identity from preservation through final inventory collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind workflow authorization to attestation and live artifact hashes</name>
  <files>scripts/ci/collect_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/229-14-SUMMARY.md
    scripts/ci/collect_repository_inventory.mjs
  </read_first>
  <behavior>
    - "Missing workflow authorization fails whenever final capture contains workflow metadata deltas."
    - "Authorization and final-capture attestation must contain the same sorted two-row path/type/before/after/state map, byte-for-byte at the semantic field level."
    - "Each before digest equals the private manifest's frozen artifact digest and each after digest equals a fresh live raw-byte snapshot; unrelated artifacts remain unchanged."
    - "Symlinked, foreign-owner, broad-mode, replaced-during-read, malformed, stale, missing, or extra authorization/attestation inputs fail before remote observation."
  </behavior>
  <action>Add CR-05 RED fixtures for stale before/after hashes, mismatched authorization versus attestation, missing input, omitted/extra path, type/state drift, symlink, broad mode, and replacement. Introduce one bounded no-follow current-owner mode-0600 JSON reader for both private workflow authorization and final-capture attestation, with descriptor hashing plus pre/post stable identity. Require `artifactAuthorization` in the public collector and CLI whenever the fixed two workflow metadata changes exist. Canonicalize both records to sorted exact maps and compare them, then independently derive manifest-before and live-after values and compare again. Reject every path except `.planning/milestone.lock` and `.planning/state.json`, require both exactly once, and retain all non-workflow artifacts unchanged. Perform these checks before creating or invoking any remote adapter per D-08.</action>
  <verify>
    <automated>node --test scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/verify_repository_inventory.mjs</automated>
    <fails_when>mismatched or missing authorization exits zero; authorization is parsed but not compared; unsafe private input is followed/read; remote observation occurs before exact binding; an extra/missing path passes; or unchanged artifact guarantees regress</fails_when>
  </verify>
  <acceptance_criteria>
    CR-05's separately supplied stale authorization fails while an exact authorization/attestation/live triple passes.
    Both private inputs have no-follow, current-owner, restrictive-mode, bounded, stable-identity enforcement.
    The collector CLI requires and consumes the fixed authorization input before remote reads.
  </acceptance_criteria>
  <done>Workflow metadata changes are accepted only when all independent private and live authorities agree exactly.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| symlink metadata → artifact digest | Raw link bytes must remain exact and non-dereferenced. |
| private authorization/attestation → collector | Private records are untrusted until opened safely and reconciled with manifest and live bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G18-01 | Repudiation | symlink artifact digest | high | mitigate | Hash only the Buffer returned by non-dereferencing raw link reads and retain end-to-end fixtures. |
| T-229-G18-02 | Tampering | workflow metadata authorization | high | mitigate | Exact-map authorization, attestation, manifest-before, and live-after values before observation. |
| T-229-G18-03 | Elevation of Privilege | private authority file opening | high | mitigate | Current-owner mode-0600 no-follow descriptor reads with bounded/stable identity checks. |
| T-229-G18-04 | Information Disclosure | artifact evidence | high | mitigate | Persist only relative path/type/digests/state; never link text, contents, or private locations. |
</threat_model>

<verification>
Run collector and verifier tests. Confirm the generated raw-symlink and authorization fixtures use only test-owned temporary capsules and do not modify the original capsule.
</verification>

<success_criteria>
CR-02 and CR-05 are closed, REPO-02 artifact identity is byte-exact, and remote observation cannot proceed on unbound metadata authorization.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/collect_repository_inventory.mjs`: raw-Buffer artifact reads and exact private authorization/attestation/live reconciliation.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-18-SUMMARY.md` when done.
</output>

---
phase: 230-reviewable-history-integration
plan: 07
type: execute
wave: 6
depends_on: ["230-06"]
files_modified:
  - .planning/STATE.md
  - .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json
  - .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.md
  - .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json
autonomous: false
requirements: [INTG-01, INTG-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "PR #44 is closed unmerged with a public comment naming the four superseding commit SHAs on the candidate, and no commit from the abandoned Phase-226 line was published to main."
    - "The candidate exists before PR #44 is touched, because merging #44 first is the one move that makes exclusion impossible without rewriting origin/main."
    - ".planning/STATE.md's account of PR #44 is corrected: its pushed head was local main plus four commits, not four commits cherry-picked off main."
    - "The final Phase-230 capsule is minted after .planning/STATE.md and .planning/MILESTONES.md have settled, so it does not invalidate itself on the next planning-document write."
    - "The final capsule's bundle and private manifest are mode 0600 on disk, because git bundle create resets mode to 0644."
    - "The final capsule mint asserts 230-REF-EXCEPTIONS.json rows whose retirement trigger has already fired are absent, so the ledger cannot become a junk drawer."
    - "The rollback point remains valid at capsule-mint time: the recorded candidate object, both parents, and expected reverted tree still resolve and still reproduce the first parent's tree."
    - statement: "An integration verification or capsule mint interrupted at any point leaves no partial 230-* artifact and no orphaned preservation ref or linked worktree in the subject repository."
      verification: backstop
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Corrected factual record of PR #44 and the Phase 230 outcome"
      contains: "PR #44"
    - path: ".planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json"
      provides: "Excluded-commit ledger updated with the executed PR #44 closure"
      contains: "superseded_by"
  key_links:
    - from: ".planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json"
      to: ".planning/STATE.md"
      via: "the ledger's PR #44 row and STATE.md's corrected account must agree on the same measured facts"
      pattern: "44"
  prohibitions:
    - "Must not merge PR #44, and must not push, force-push, or otherwise publish any commit from the abandoned Phase-226 line to main."
    - "Must not mutate any remote before the authorization checkpoint has been resolved."
    - "Must not mint the final capsule before .planning/STATE.md and .planning/MILESTONES.md have settled, and must not retain a ledger row whose retirement trigger has already fired."
    - "Must not embed adopter, customer, or personal identifiers, absolute filesystem paths, or $HOME in any committed evidence artifact or public PR comment."
---

<objective>
Close the superseded public pull request with an evidenced comment, correct the factual record it contradicts, and seal the phase with a final recovery capsule minted last.

Purpose: D-10 records the maintainer decision to close PR #44 unmerged. The measurement behind it: `git merge-base --is-ancestor main <pr-head>` is YES and `main...<pr-head>` is `0 4`, so the PR's head is local `main` plus four commits — merging it would permanently publish all of the abandoned Phase-226 commits onto `main`. Its four useful commits are already on the milestone branch as exact patch-id matches, so nothing is lost. D-11 requires the candidate to exist first: merging #44 first is the one move that makes exclusion impossible without rewriting `origin/main`. D-32 makes the capsule mint a hard ordering constraint, not a hope — a capsule minted before the planning documents settle invalidates itself on the next GSD step.

Output: PR #44 closed unmerged with a superseding-SHA comment, a corrected `.planning/STATE.md`, and a final 0600 capsule minted last.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-reviewable-history-integration/230-CONTEXT.md
@.planning/phases/230-reviewable-history-integration/230-04-SUMMARY.md
@.planning/phases/230-reviewable-history-integration/230-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Close PR #44 unmerged, with a public comment naming the four superseding commit SHAs on the integration candidate.</decision>
  <context>
The repository posture requires separate explicit authorization for any remote mutation, and Phase 229's read-only posture still binds. This checkpoint is that authorization, not a re-litigation of the decision: D-10 already records the maintainer choosing to close unmerged.

Measured facts, to be re-measured immediately before acting: PR #44's head is local `main` plus four commits (`main...&lt;pr-head&gt;` reads `0 4`), so merging it would permanently publish all of the abandoned Phase-226 commits onto `main`. Its four useful commits — `2de4389b`, `9eae363a`, `173607d9`, `5653216c` — are already on the milestone branch as exact patch-id matches, so closing loses nothing.

Undo cost: closing is publicly visible and reopening invites confusion (costly). Merging is the genuinely one-way error — it publishes 80 abandoned commits onto `main` and can only be undone by rewriting published history, which this milestone forbids.
  </context>
  <options>
    <option id="close-unmerged">
      <name>Close unmerged with a superseding-SHA comment (the recorded D-10 decision)</name>
      <pros>Loses nothing — all four useful commits are already on the candidate as exact patch-id matches; leaves an evidenced public explanation; keeps the abandoned line off `main`.</pros>
      <cons>Publicly visible; reopening later would invite confusion about which line is authoritative.</cons>
    </option>
    <option id="leave-open">
      <name>Leave PR #44 open</name>
      <pros>No remote mutation at all.</pros>
      <cons>The abandoned line stays mergeable by accident, and an outside reader has no explanation for why it was superseded.</cons>
    </option>
    <option id="merge">
      <name>Merge PR #44</name>
      <pros>None identified.</pros>
      <cons>One-way: permanently publishes all 80 abandoned Phase-226 commits onto `main`, undoable only by rewriting published history, which this milestone forbids.</cons>
    </option>
  </options>
  <resume-signal>Type "approved" to authorize option close-unmerged, or name a different option id</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Close PR #44 unmerged with a superseding-SHA comment</name>
  <files>.planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json, .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.md</files>
  <precondition>Task 1's authorization checkpoint is resolved, and `integration/v1.62-candidate` already exists (D-11: the candidate must exist before #44 is touched).</precondition>
  <reversibility rating="costly">Closing a public pull request is visible to anyone watching the repository, and reopening it later invites confusion about which line is authoritative. It is nonetheless the reversible side of this door: merging is the one-way error.</reversibility>
  <read_first>
    - .planning/STATE.md (the "Adopter boot fix: SHIPPED as PR #44" section — the factually wrong account D-10 corrects)
    - .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json (the PR #44 ledger row recorded by Plan 230-04)
    - .planning/phases/230-reviewable-history-integration/230-CONTEXT.md (D-10, D-11)
    - scripts/ci/ci_monitor.cjs (the repository's established read-only GitHub observation path and its `--repo szTheory/accrue` convention)
  </read_first>
  <action>
Re-measure before acting, then perform exactly one remote mutation.

Re-measure with read-only calls: the PR's current head SHA, its state, `git merge-base --is-ancestor <local-main> <pr-head>`, and `git rev-list --left-right --count <local-main>...<pr-head>`. If the PR is already closed or merged, or if the re-measured facts contradict the recorded ledger row, stop and report rather than acting — a changed remote is a changed decision.

Post one comment on PR #44 naming: that the branch's head is local `main` plus four commits and merging it would publish the abandoned Phase-226 line onto `main`; the four superseding commit SHAs already on the integration candidate (`2de4389b`, `9eae363a`, `173607d9`, `5653216c`, each re-verified as a patch-id match at execution time); and where the evidence lives (`230-DISPOSITIONS.md`). The comment must contain no adopter, customer, or personal identifier — refer to "a downstream host app", matching the convention the PR body already uses — and no absolute path or `$HOME`.

Then close the PR without merging. Do not merge, do not push, do not delete the remote branch (branch removal is Phase 232's HYG-01), and do not dispatch any workflow.

Update the PR #44 row in `230-DISPOSITIONS.json` with the executed action: the argv array of the command that ran, its captured integer exit code, the resulting PR state, and `state: "proved"`. Re-render `230-DISPOSITIONS.md`.
  </action>
  <acceptance_criteria>
    - PR #44's state reads closed and its merged state reads false.
    - The comment posted on PR #44 contains all four superseding SHAs verbatim.
    - The comment contains no adopter, customer, or personal identifier, no absolute path, and no `$HOME`.
    - The remote branch backing PR #44 still exists (removal is Phase 232's).
    - `origin/main` is unchanged: its SHA equals the value re-measured before the action.
    - The PR #44 ledger row carries `state: "proved"`, a `command` argv array, and an integer `exit_code`.
    - `230-DISPOSITIONS.md` re-renders byte-identically from the updated JSON.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_integration_disposition.mjs --records .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json --rendered .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md --dispositions .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-excluded-ledger --require-determinism</automated>
    <fails_when>non-zero exit, or the output contains "STALE_BINDING", or the output lacks a line containing "PASS"</fails_when>
  </verify>
  <done>PR #44 is closed unmerged with an evidenced comment, `origin/main` is unchanged, no abandoned commit was published, and the ledger records the executed action with a captured exit code.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the STATE.md record and settle the planning documents</name>
  <files>.planning/STATE.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/STATE.md (the "Adopter boot fix: SHIPPED as PR #44" section and the Phase 229 evidence-ordering section that explains why planning digests are pinned)
    - .planning/MILESTONES.md (the milestone record that must settle before the capsule is minted)
    - .planning/phases/230-reviewable-history-integration/230-04-SUMMARY.md and 230-06-SUMMARY.md (the measured facts to carry into STATE.md)
    - .planning/phases/230-reviewable-history-integration/230-CONTEXT.md (D-10, D-32, D-34)
  </read_first>
  <action>
Correct `.planning/STATE.md`'s account of PR #44 (D-10). The existing text describes it as "four commits cherry-picked off main" — that describes intent, not the pushed branch. Replace it with the measured truth: the pushed head was local `main` plus four commits, so merging it would have published the abandoned Phase-226 line onto `main`; the four useful commits are on the candidate as exact patch-id matches; the PR is now closed unmerged with a superseding-SHA comment. Keep the correction factual and cite the measured values rather than narrating.

Add the Phase 230 outcome to `.planning/STATE.md` and `.planning/MILESTONES.md`: the candidate ref and merge-commit SHA, the recomputed scope integers, the excluded-commit count, the ref-exceptions row count, and the D-34 handoff that the worktree `dirty` boolean is currently pinned `true` and Phase 232 flipping the tree clean will flip it to `false` and fail strict verification.

Settle both documents completely in this task. The next task mints the final capsule, and the capsule pins their digests: any later write invalidates strict re-verification. That is inherent to pinning planning digests, not a defect — Phase 229's STATE.md already records it — but the ordering is a hard constraint, so no planning-document edit may follow the mint within this plan.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` no longer contains the phrase describing PR #44 as four commits cherry-picked off main.
    - `.planning/STATE.md` states the measured PR #44 facts (head is local `main` plus four commits; four patch-id-matched commits on the candidate; closed unmerged) and names the candidate ref and merge-commit SHA.
    - `.planning/STATE.md` and `.planning/MILESTONES.md` both record the Phase 230 outcome and the D-34 `dirty` transition handoff.
    - No adopter, customer, or personal identifier, absolute path, or `$HOME` was introduced into either document.
    - Both documents are committed before Task 4 begins.
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "cherry-picked off main" .planning/STATE.md &amp;&amp; grep -q "integration/v1.62-candidate" .planning/STATE.md &amp;&amp; grep -q "integration/v1.62-candidate" .planning/MILESTONES.md</automated>
    <fails_when>non-zero exit, which occurs when the stale PR #44 phrasing remains or when either document does not name the candidate ref</fails_when>
    <automated>git diff --quiet -- .planning/STATE.md .planning/MILESTONES.md</automated>
    <fails_when>non-zero exit, which occurs when either planning document still has uncommitted changes and therefore has not settled before the capsule mint</fails_when>
  </verify>
  <done>`.planning/STATE.md`'s PR #44 account is factually correct, both planning documents record the Phase 230 outcome and the D-34 handoff, and both are committed and settled.</done>
</task>

<task type="auto">
  <name>Task 4: Mint the final Phase-230 capsule last and re-prove the rollback point</name>
  <files>.planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json</files>
  <precondition>`.planning/STATE.md` and `.planning/MILESTONES.md` are committed and will not be written again in this plan (D-32's hard ordering constraint).</precondition>
  <reversibility rating="costly">The capsule is immutable and pins the planning-document digests at mint time, so superseding it means minting again and re-citing its digests in every artifact that recorded them — which is exactly what Phase 229 minting early already cost this phase.</reversibility>
  <read_first>
    - scripts/ci/preserve_repository_state.sh (as extended in Plan 230-01 — the `--preservation-phase` option, the collision check, the 0644-to-0600 handling)
    - .planning/phases/230-reviewable-history-integration/230-REF-EXCEPTIONS.json (the declared-additions ledger and each row's `retirement_trigger`)
    - .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json (the recorded candidate object, both parents, `expected_reverted_tree`, capsule digests, and `restore_argv`)
    - .planning/phases/230-reviewable-history-integration/230-01-SUMMARY.md (the safety capsule's bundle path and `manifest_sha256`)
    - .planning/phases/230-reviewable-history-integration/230-CONTEXT.md (D-28, D-29, D-30, D-31, D-32, D-33)
  </read_first>
  <action>
Mint the final Phase-230 capsule as the last action of the phase, with `--preservation-phase 230`, writing the bundle, private manifest, and public record OUTSIDE the repository working tree (the session scratchpad, never a repository-relative or `.planning/` path).

Before minting, assert the declared-additions ledger is clean: every row in `230-REF-EXCEPTIONS.json` whose `retirement_trigger` has ALREADY fired must be absent (D-28's "empty-after-retirement" assertion). Rows whose trigger is a future Phase-232 event stay. Expected ledger size at this point is small — the candidate branch, the review branch, the phase-230 preservation refs, and the recorded `origin/*` movement — and the size is asserted as a literal integer, not a bound.

`chmod 600` the bundle and the private manifest after publication and assert both report mode `600` (the 229-19 defect: `git bundle create` silently resets mode to 0644).

Re-prove the rollback point at mint time rather than trusting the earlier proof: in a fresh scratch `git clone` into the scratchpad — never `git worktree add`, because the verifier pins worktrees as an exact `{branch, sha, dirty}` multiset and a linked worktree adds a row — check out the recorded `candidate_object`, run `git revert -m 1`, and assert the resulting tree equals the recorded `expected_reverted_tree`. Update `230-ROLLBACK-POINT.json`'s `capsule` field with the FINAL capsule's `bundle_sha256` and `manifest_sha256`, keeping `restore_argv` an argv array of argv arrays.

Record in the capsule's artifact manifest the sha256 of the untracked evidence-relevant source files that Phase 232 will classify — at minimum `scripts/ci/stripe_test_fixtures.mjs` and `scripts/ci/verify_stripe_test_fixtures.mjs` (D-33) — so they survive an undo. Do not classify, move, or delete them.

Do not push anything, do not open a pull request, and do not dispatch any workflow. Record in the plan SUMMARY that strict re-verification of this capsule is a point-in-time claim valid as of its capture commit, exactly as Phase 229's STATE.md already documents, and that the next recapture must again be the last action after planning documents settle.
  </action>
  <acceptance_criteria>
    - The final capsule's bundle heads include the `integration/v1.62-candidate` tip and the milestone branch tip.
    - `stat -f '%OLp'` reports `600` for both the final bundle and the final private manifest.
    - The final capsule's artifact manifest contains sha256 entries for `scripts/ci/stripe_test_fixtures.mjs` and `scripts/ci/verify_stripe_test_fixtures.mjs`.
    - Every `230-REF-EXCEPTIONS.json` row whose retirement trigger has already fired is absent, and the ledger size equals the literal integer asserted by the verifier.
    - In a fresh scratch clone, `git revert -m 1 <candidate_object>` reproduces the recorded `expected_reverted_tree` exactly.
    - `git worktree list` and `git for-each-ref refs` in the subject repository are unchanged by the proof and by the mint, apart from the phase-230 preservation refs the mint itself declares.
    - `230-ROLLBACK-POINT.json`'s `capsule` field names the FINAL capsule's digests, and every `restore_argv` element is an array of strings.
    - No ref was pushed and no pull request was opened.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/preserve_repository_state.sh --self-test</automated>
    <fails_when>non-zero exit, or the final line is not "preserve repository state: PASS"</fails_when>
    <automated>git bundle verify "$PHASE230_FINAL_BUNDLE" &amp;&amp; HEADS="$(git bundle list-heads "$PHASE230_FINAL_BUNDLE")" &amp;&amp; TIP="$(git rev-parse integration/v1.62-candidate)" &amp;&amp; printf '%s\n' "$HEADS" | grep -qF "$TIP"</automated>
    <fails_when>non-zero exit, or grep prints nothing because the candidate tip is absent from the bundle heads</fails_when>
    <automated>node scripts/ci/verify_integration_disposition.mjs --records .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json --rendered .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md --dispositions .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-ancestry --require-scope --require-hazard-universe --require-excluded-ledger --require-post-merge-scope --require-rollback-proof --require-determinism</automated>
    <fails_when>non-zero exit, or the output contains "STALE_BINDING", or the output contains "reverted tree differs", or the output lacks a line containing "PASS"</fails_when>
    <automated>node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-typed-ref-continuity --require-recovery --require-all-ref-recovery --require-typed-artifacts</automated>
    <fails_when>non-zero exit, or the output lacks a line containing "fixtures: PASS"</fails_when>
  </verify>
  <done>A final 0600 capsule covering the candidate and the milestone line exists outside the repository, minted after the planning documents settled; the rollback point is re-proved by execution and updated with the final capsule's digests; the ledger is clean of fired retirement triggers; nothing was pushed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- Final Phase-230 capsule: bundle, private manifest, and public record (out-of-repo, not committed)
- Updated `capsule` field in `.planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json` naming the final digests
- Corrected PR #44 section and new Phase 230 outcome section in `.planning/STATE.md`
- New Phase 230 outcome entry in `.planning/MILESTONES.md`
- Executed-action fields on the PR #44 ledger row: `command`, `exit_code`, `state`
</artifacts_this_phase_produces>

<threat_model>
ASVS level: **1**. Blocking severity: **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local decision → public GitHub mutation | A closing comment and a state change become permanently visible on a public repository |
| Repository state → out-of-repo capsule | Object data and file digests leave version control onto the filesystem |
| Planning documents → pinned capsule digests | Document content becomes an immutable pinned authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-230-07-01 | Elevation of privilege | Publishing the abandoned Phase-226 line by merging PR #44 | critical | mitigate | Close, never merge; `origin/main`'s SHA is asserted unchanged before and after; the candidate is built first (D-11) so exclusion remains possible; the authorization checkpoint gates the single permitted remote mutation. |
| T-230-07-02 | Information disclosure | Adopter or personal identifiers in a public PR comment | high | mitigate | The comment refers to "a downstream host app", matching the existing PR body convention; it is reviewed against the no-adopter-identifier rule before posting and contains no absolute path or `$HOME`. |
| T-230-07-03 | Tampering | Acting on a remote whose state changed since measurement | high | mitigate | Re-measure the PR head, state, and ancestry immediately before acting; stop and report rather than act when the re-measured facts contradict the recorded ledger row. |
| T-230-07-04 | Information disclosure | Final capsule bundle and manifest on disk | high | mitigate | `chmod 600` after `git bundle create` and assert mode `600`; the capsule is written outside the repository and never committed. |
| T-230-07-05 | Repudiation | A capsule that silently stops verifying because planning documents moved after the mint | medium | mitigate | Hard ordering constraint: planning documents are committed and settled in Task 3, and no planning write follows the mint in this plan; the point-in-time nature is recorded explicitly in the SUMMARY. |
| T-230-07-06 | Tampering | A ledger row retained after its retirement trigger fired | medium | mitigate | The mint asserts fired-trigger rows are absent and the ledger size equals a literal integer. |
| T-230-07-07 | Denial of service | Scratch clone consuming disk during the revert re-proof | low | accept | The clone is a single bounded copy into the session scratchpad and is deleted after the proof. |
| T-230-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
1. PR #44 is closed, unmerged, with a comment naming all four superseding SHAs and no adopter identifier.
2. `origin/main` is unchanged and no abandoned commit was published.
3. `.planning/STATE.md` and `.planning/MILESTONES.md` are corrected, committed, and settled before the mint.
4. The final capsule's bundle heads include the candidate tip; both files are mode 600.
5. The rollback point re-proves by execution and names the final capsule's digests.
6. The full `--require-*` flag set passes on the committed evidence, and the repository-inventory fixtures still pass under typed ref continuity.
</verification>

<success_criteria>
- The superseded public pull request is closed with an evidenced, privacy-safe explanation, and nothing from the abandoned line reached `main`.
- The factual record that contradicted the measurement is corrected.
- The phase is sealed by a capsule minted last, with an executably re-proved rollback point.
- No ref was pushed, no pull request was opened, and no GitHub workflow was dispatched.
</success_criteria>

<output>
Create `.planning/phases/230-reviewable-history-integration/230-07-SUMMARY.md` when done. Record the PR #44 closure facts as re-measured, the final capsule's bundle path and `manifest_sha256`, the final ref-exceptions row count, the re-proved `expected_reverted_tree`, and the point-in-time caveat on strict re-verification.
</output>

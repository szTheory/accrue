---
phase: 231-exact-sha-release-gate-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_recut_candidate.mjs
  - .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json
autonomous: true
requirements: [GATE-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`integration/v1.62-candidate` resolves to a NEW commit object that is a single first-parent `--no-ff` merge whose first parent is the milestone-branch HEAD measured at execution time and whose second parent is `origin/main` (D-01, D-04)."
    - "`git rev-list --count <candidate> ^<milestone-HEAD-at-cut> ^origin/main` equals exactly the literal integer 1 (D-04)."
    - "`git revert -m 1 --no-edit <candidate>` executed in a scratch `git clone` produces a tree whose object id equals the milestone parent's tree object id (D-04, D-07)."
    - "Tag `v1.61` still resolves to tag object `fdb41672` dereferencing to commit `e3b06794`, and that commit is an ancestor of the new candidate (D-04)."
    - "Each of the four closure commits `8a95fbe8`, `9e090eb5`, `7cc501a3`, `57c61a9a` and `origin/main` is an ancestor of the new candidate (D-04)."
    - "The new candidate's tracked `.tool-versions` contains every line present in `bab50d92:.tool-versions` (`nodejs`, `elixir 1.19.5-otp-28`, `erlang 28.5`), so a bare scratch clone can invoke `mix` without out-of-band operator knowledge (D-08)."
    - "`231-ROLLBACK-POINT.json` states the superseded candidate object and a recorded cause for the re-cut, and `230-ROLLBACK-POINT.json` is byte-unchanged (D-02, D-07)."
    - "`231-ROLLBACK-POINT.json`'s `restore_argv` is an array of argv arrays, never a shell string (D-07)."
    - "No `git worktree` row is created in the subject repository by any step of this plan (D-07, D-12)."
    - "Phase-wide, acceptance is fully executable: every task in every Phase 231 plan decides its outcome by an automated assertion, no plan contains `type=\"tracer\"`, `checkpoint:human-verify`, or `<human-check>`, and the phase's single human interaction (231-04 Task 2) is a `checkpoint:decision` authorizing D-14's irreversible public push rather than accepting any behavior — so `behavior_unverified: 0` is reachable at close (D-33)."
  artifacts:
    - scripts/ci/verify_recut_candidate.mjs
    - .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json
  key_links:
    - "`verify_recut_candidate.mjs` re-derives every shape gate from live `git` plumbing and compares against `231-ROLLBACK-POINT.json` — the record is never trusted as its own authority (D-30)."
    - "The re-cut candidate contains `scripts/ci/verify_integration_disposition.mjs` and `scripts/ci/verify_phase230_archive_invariants.mjs`, which the superseded candidate `bab50d92` lacked — this is the stated cause of the re-cut (D-01, D-06)."
  prohibitions:
    - "MUST NOT hardcode any SHA or commit count transcribed from 231-CONTEXT.md or 231-RESEARCH.md as a comparison target; all non-frozen values are re-measured at execution time (D-00)."
    - "MUST NOT use `git worktree add` anywhere in this phase; the inventory verifier pins an exact worktree-row multiset (D-07, D-12)."
    - "MUST NOT edit, regenerate, or re-diff `230-ROLLBACK-POINT.json` or the frozen 229/230 capsules; supersede by reference only (D-07, D-27, D-28)."
    - "MUST NOT rebase, squash, force-push, move a tag, or create a second merge point on the candidate (D-04, D-05)."
    - "MUST NOT emit absolute paths, `$HOME`, actor names, adopter identifiers, or secrets into any committed evidence artifact (D-31)."
---

<objective>
Re-cut `integration/v1.62-candidate` from current milestone HEAD so the SHA that GATE-01/GATE-02/GATE-03 prove releasable actually contains the Phase 230 verifiers those gates depend on, and freeze that SHA behind an executable shape-and-ancestry contract.

Purpose: D-03 makes this a precondition — nothing else in Phase 231 may bind to a SHA until this plan completes and its ancestry gates pass. D-01 rejects freezing `bab50d92` because it predates its own gating verifiers.
Output: a new candidate commit object; `scripts/ci/verify_recut_candidate.mjs`; `231-ROLLBACK-POINT.json`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md
@.planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
@.planning/phases/230-reviewable-history-integration/230-CONTEXT.md
</context>

<assumption_delta_decision>
The deterministic scan fired `kind: pluralization` on the term `another`, matched inside GATE-01's own requirement text "…without ignored caches, credentials, or another worktree". That is a prohibition clause, not a singular-to-plural identity transition: the phase introduces no second candidate, no second worktree, no second ledger.

- Noun now primary: unchanged — `integration/v1.62-candidate` remains the single candidate ref.
- Decision: **no-change**. Rationale: the match is a negation ("not another worktree"), the opposite of a pluralization signal.
- No invariant test suggested; D-12's worktree-multiset assertion already encodes the singularity.
</assumption_delta_decision>

<flagged_assumptions>
Two edge-probe rows are `unclassified` and are NOT auto-resolved. They are surfaced here as planner assumptions:

- **GATE-01 (unclassified):** assumed that "the repository's complete local CI-equivalent gates" means exactly the thirteen jobs in the `ci.yml` header comment's merge-blocking list, no more and no less (D-09, D-10). If the maintainer intends a broader "everything CI could run" reading, GATE-01's scope is under-specified and the cohort table must be widened.
- **GATE-02 (unclassified):** assumed that "required GitHub Actions checks are green" is satisfiable by a `workflow_dispatch`-class run (D-16, D-17), given branch protection returns 404 and rulesets return an empty array. If the maintainer requires a `pull_request`-class proof, GATE-02 cannot close in Phase 231 and moves to Phase 232 alongside REL-04.
</flagged_assumptions>

<source_coverage_audit>
## Multi-Source Coverage Audit (phase-wide; lives on Plan 01 as the phase's first plan)

**GOAL** — ROADMAP Phase 231 goal and its three success criteria.

| Item | Covered by |
|------|-----------|
| Goal: release decision from complete, honest local and GitHub evidence for one exact SHA | 231-01 (the exact SHA), 231-03 (local), 231-04 (GitHub), 231-05 (windows), 231-06 (discoverability) |
| SC-1 fresh clean checkout completes local CI-equivalent gates without ignored caches, credentials, or another worktree | 231-03 Task 3 |
| SC-2 green required GitHub Actions checks for that exact SHA with each provider lane explicitly `proved`/`skipped`/`failed`/`advisory` | 231-04 Tasks 1 and 3 |
| SC-3 current evidence, owner, rationale, release impact for every former ship window, each fixed or explicitly waived | 231-02 (machinery), 231-05 (records and flips) |

**REQ** — REQUIREMENTS.md phase requirement ids.

| Item | Covered by |
|------|-----------|
| GATE-01 | 231-01, 231-03, 231-06 |
| GATE-02 | 231-04, 231-06 |
| GATE-03 | 231-02, 231-05, 231-06 |

**RESEARCH** — 231-RESEARCH.md features, constraints, pitfalls, and open questions.

| Item | Covered by |
|------|-----------|
| Collect/render/verify triad reused a fourth time | 231-02 Tasks 1-3 |
| `collect_ci_baseline.mjs` extended in place, not forked | 231-04 Task 1 |
| `ci_monitor.cjs` is read-only; dispatch goes through `gh` (Pitfall 3) | 231-04 Task 3 |
| Dispatch propagation delay (Pitfall 4) | 231-04 Task 3 |
| CONTEXT numbers already stale (Pitfall 1) | 231-01 Task 2 |
| Root `.tool-versions` missing Elixir/Erlang lines (Pitfall 2, Open Question 1) | 231-01 Tasks 1 and 2 |
| Unknown full-cohort wall clock (Open Question 2) | 231-03 Task 3 — itemized cost budgeted, no invented minute figure |
| Docker daemon not running in the research environment | 231-03 Task 3 precondition |
| Rows 1 and 5 leads observed on a dirty tree, not the candidate | 231-05 Task 1 — treated as leads, evidence must come from the clean-checkout run |
| `directShipWindows` 10-column parse must not widen | 231-02 Task 1, 231-05 Task 2 |
| `assertSameMultiset`/`exactMap`/`assertSameMap` reused verbatim | 231-02 Task 3, 231-03 Task 2 |
| `resolvePhaseEvidencePath` for archive-safe paths | 231-01, 231-02, 231-03 |
| `annotation-sweep` has no scratch-clone-local form | 231-03 Task 3 — recorded `non_run` with a GATE-02 pointer |
| `admin-ui-ratchet-guardrails` own `continue-on-error` flag | 231-03 Task 3 |
| Assumption A1 (Stripe secrets presence unverified) | 231-04 Task 3 — dispatch with `run_live_stripe=false`, so the assumption is not load-bearing |

**CONTEXT** — every locked decision D-00 through D-33.

| Item | Covered by |
|------|-----------|
| D-00 re-measure, never transcribe | 231-01 Task 2 |
| D-01 re-cut from milestone HEAD | 231-01 Task 2 |
| D-02 record the re-cut in 231's own evidence | 231-01 Task 2 |
| D-03 re-cut is the phase's first task | 231-01, wave 1, everything else depends on it |
| D-04 all Phase 230 shape constraints re-asserted | 231-01 Tasks 1 and 2 |
| D-05 advance-by-merging and merge-back disqualified | 231-01 Task 2 |
| D-06 recompute the hazard universe fresh | 231-01 Task 2 |
| D-07 re-mint the rollback point via scratch clone, argv array | 231-01 Tasks 1 and 2 |
| D-08 re-capture `.tool-versions` | 231-01 Tasks 1 and 2 |
| D-09 scope GATE-01 to the declared cohort | 231-03 Tasks 1 and 3 |
| D-10 the Phase 230 table is a disposition record | 231-03 Task 3 |
| D-11 credential-gated and scheduler-only lanes get explicit reasons | 231-03 Task 3 |
| D-12 scratch clone, never a worktree | 231-01 Task 1, 231-03 Task 3 |
| D-13 no cache restored | 231-03 Task 3 |
| D-14 push one branch and dispatch | 231-04 Tasks 2 and 3 |
| D-15 pushing alone fires zero checks | 231-04 Task 3 |
| D-16 `workflow_dispatch` is the right trigger | 231-04 objective and Task 3 |
| D-17 event class is a first-class field | 231-04 Tasks 1 and 3, 231-06 Task 2 |
| D-18 never ask GitHub what is required; assert no drift | 231-03 Tasks 1-2, 231-04 Task 1, 231-06 Task 1 |
| D-19 extend `collect_ci_baseline.mjs` | 231-04 Task 1 |
| D-20 poll to completion, fail closed | 231-04 Task 3 |
| D-21 `live-stripe` dispatch input semantics | 231-04 Task 3 |
| D-22 disposition decided by row kind | 231-05 Task 1 |
| D-23 current evidence re-derived, never copied | 231-02 Task 3, 231-05 Task 1 |
| D-24 row 5 never auto-waived | 231-05 Task 1 |
| D-25 WINDOWS.md keeps its terse schema, writer-only | 231-02 Task 1, 231-05 Task 2 |
| D-26 rich fields in a joined sibling artifact | 231-02 all tasks, 231-05 Task 2 |
| D-27 why flipping statuses is safe in routine CI | 231-05 Task 2, 231-06 Task 1 |
| D-28 do not touch frozen capsules; mint 231's last | 231-05 Task 2, 231-06 Task 3 |
| D-29 proof-state lexicon | 231-02 Task 1, 231-03 Task 1, 231-04 Tasks 1 and 3 |
| D-30 exact set/multiset equality | 231-02 Task 3, 231-03 Tasks 1-2, 231-04 Task 1 |
| D-31 render determinism and sanitization | every plan |
| D-32 `phase_evidence_path.mjs` and a README row | 231-01, 231-02, 231-03, 231-06 Task 2 |
| D-33 zero human acceptance checkpoints | every plan; the only human interaction is 231-04 Task 2, an irreversible-external-operation gate, not an acceptance gate |

**Result: no MISSING item.** Every GOAL, REQ, RESEARCH, and CONTEXT item is COVERED by a plan. Deferred Ideas in CONTEXT.md (the PR, reviewer risk summary, rollback instructions, Release Please readiness, untracked-file and stale-ref hygiene, deleting local `main`, `commit-search-depth`, CHANGELOG polish, the double-prefixed milestone-audit file, adopter-named ref renames, package publication) are scoped to Phase 232 or out of v1.62 and are correctly absent.
</source_coverage_audit>

<artifacts_this_phase_produces>
Created by this plan (new symbols — no prior definition exists in the repository):

- `scripts/ci/verify_recut_candidate.mjs` — new Node module. Exports: `RECUT_RECORD_FIELDS`, `validateRecutRecord`, `collectRecutGates`, `verifyFixtures`.
- New CLI flags on that module: `--fixtures`, `--repo`, `--record`, `--candidate`, `--expected-repository`, `--require-shape`, `--require-ancestry`, `--require-revert-proof`, `--require-toolchain`, `--require-supersession`.
- New JSON field names in `231-ROLLBACK-POINT.json`: `schema_version`, `candidate_ref`, `candidate_object`, `parents`, `pre_integration_refs`, `expected_reverted_tree`, `supersedes` (with `candidate_object`, `artifact`, `cause`), `toolchain_pin` (with `path`, `sha256`, `matches_superseded`), `restore_argv`.
- New evidence artifact path: `.planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author verify_recut_candidate.mjs encoding the D-04 shape and ancestry gates</name>
  <files>scripts/ci/verify_recut_candidate.mjs</files>
  <read_first>
    - scripts/ci/verify_integration_disposition.mjs (the exact analog: `assertSameMultiset`/`exactMap`/`assertSameMap`, the `--fixtures` plus `--require-*` CLI shape, the `NODE_TEST_CONTEXT` split, and the already-defined full-length `V161_TAG_OBJECT`/`V161_COMMIT_OBJECT`/`CLOSURE_COMMITS` constants)
    - scripts/ci/collect_integration_disposition.mjs (the `fail()`/`fields()`/`fullSha()`/`run()` primitives and the `spawnSync("git", ["-C", repo, ...])` shape)
    - scripts/ci/phase_evidence_path.mjs (`resolvePhaseEvidencePath` — archive-safe artifact resolution, D-32)
    - .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json (the record shape being superseded)
    - .planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
  </read_first>
  <behavior>
    - `validateRecutRecord` rejects a record containing any field outside `RECUT_RECORD_FIELDS`, with a forbidden-field message naming the key.
    - `validateRecutRecord` rejects `restore_argv` supplied as a string, and rejects an argv array whose first element is not the string `git`.
    - `validateRecutRecord` rejects any value in `candidate_object`, `parents` object ids, `expected_reverted_tree`, or `supersedes.candidate_object` that is not a full lowercase 40-hex object id.
    - `collectRecutGates` on a fixture repo whose candidate has two parents, a unique-commit count of 1, and a revert tree matching the first parent's tree returns all gates satisfied.
    - `--require-shape` fails on a fixture candidate built by rebase or squash (one parent), with a message naming the observed parent count.
    - `--require-shape` fails on a fixture candidate carrying a second merge point (unique-commit count of 2), printing expected 1 and actual 2.
    - `--require-ancestry` fails when a fixture `v1.61` tag object does not dereference to the pinned commit, and separately when any one of the four closure commits is not an ancestor.
    - `--require-revert-proof` fails when the reverted tree differs from the first parent's tree, printing both tree object ids.
    - `--require-toolchain` fails when the candidate's tracked `.tool-versions` is missing any line present in the superseded blob.
    - `--require-supersession` fails when `supersedes.candidate_object` equals `candidate_object`, and separately when `supersedes.cause` is empty.
    - An invocation with no `--require-*` flag prints a schema-only suffix distinct from the strict suffix, so a partial run cannot be misread as full coverage.
  </behavior>
  <action>
Create `scripts/ci/verify_recut_candidate.mjs` following `verify_integration_disposition.mjs`'s structure verbatim: shebang, `node:assert/strict` / `node:fs` / `node:os` / `node:path` / `node:child_process` / `node:test` imports, a single `fail(message)` throw path, a top-level `main()` guarded by `process.env.NODE_TEST_CONTEXT`, and on the catch `console.error("recut candidate verify: FAIL: " + message)` plus `process.exitCode = 1`.

Import `resolvePhaseEvidencePath` from `./phase_evidence_path.mjs` and resolve the default record path via `resolvePhaseEvidencePath("231-exact-sha-release-gate-proof", "231-ROLLBACK-POINT.json")` — never a hardcoded `.planning/phases/...` literal (D-32).

Declare the frozen anchors as module constants. These are the only literals in this phase that are legitimately frozen (D-04): `V161_TAG_OBJECT` (abbreviated `fdb41672`), `V161_COMMIT_OBJECT` (abbreviated `e3b06794`), and `CLOSURE_COMMITS` holding the four commits abbreviated `8a95fbe8`, `9e090eb5`, `7cc501a3`, `57c61a9a`. Copy the full 40-hex forms out of `verify_integration_disposition.mjs`, which already defines them — do not re-type from an abbreviation.

Declare `RECUT_RECORD_FIELDS` as a closed Set containing exactly: `schema_version`, `candidate_ref`, `candidate_object`, `parents`, `pre_integration_refs`, `expected_reverted_tree`, `supersedes`, `toolchain_pin`, `restore_argv`. Validate nested objects with the same `fields(value, allowed, label)` allow-list helper — no free-form pass-through (D-31).

Implement `collectRecutGates(repo, candidateRef)` deriving live from git: the parent list via `git rev-list --parents -n 1`, the unique-commit count via `git rev-list --count <candidate> ^<first-parent> ^<second-parent>`, ancestry via `git merge-base --is-ancestor`, the `v1.61` tag object via `git rev-parse v1.61` and its dereferenced commit via `git rev-parse v1.61^{commit}`, and the candidate's tracked toolchain blob via `git show <candidate>:.tool-versions`.

Implement `--require-revert-proof` by creating a scratch clone with `git clone --no-hardlinks <repo> <mkdtemp dir>` — NEVER `git worktree add` (D-12) — running `git revert -m 1 --no-edit <candidate>` inside it, and comparing `git rev-parse HEAD^{tree}` there against `git rev-parse <first-parent>^{tree}` in the subject repo. Remove the temp dir in a `finally` block.

Implement `--require-toolchain` by comparing the candidate's tracked `.tool-versions` blob against `git show bab50d92be2695b12d5853e7d578e600376e73d0:.tool-versions`. The superseded candidate is a frozen historical object, so pinning it is safe. Require every line of the superseded blob to be present in the new blob: a superset is acceptable, a missing line is a hard failure.

Implement `--require-supersession` asserting `supersedes.candidate_object` differs from `candidate_object`, `supersedes.artifact` equals the string `230-ROLLBACK-POINT.json`, and `supersedes.cause` is a non-empty string.

Implement the strict-suffix pattern from `verify_integration_disposition.mjs`'s `main()`: collect the requested strict flags sorted, then print either `recut candidate verification: PASS (verified: <sorted flags>)` or `recut candidate verification: PASS (schema-only: no strict flags supplied, no shape or ancestry check ran)`.

Implement `verifyFixtures()` building disposable git repositories under `fs.mkdtempSync(path.join(os.tmpdir(), ...))` with `git init`, local user config, and no network access — one positive control plus at least one negative control per strict flag, each asserted to throw with a message naming the failing property. Wire it to both `node --test` via the `NODE_TEST_CONTEXT` branch and to the bare `--fixtures` CLI flag, and have the `--fixtures` path print `recut candidate fixtures: PASS`.
  </action>
  <verify>
    <automated>node --check scripts/ci/verify_recut_candidate.mjs &amp;&amp; node --test scripts/ci/verify_recut_candidate.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check`, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node scripts/ci/verify_recut_candidate.mjs --fixtures --expected-repository szTheory/accrue</automated>
    <fails_when>non-zero exit, or stdout missing the line `recut candidate fixtures: PASS`</fails_when>
  </verify>
  <acceptance_criteria>
    - Running the module's fixture suite leaves `git worktree list` in the subject repository at the same row count it had before the run (the scratch-clone path creates no worktree row).
    - `grep -c 'resolvePhaseEvidencePath' scripts/ci/verify_recut_candidate.mjs` is at least 1.
    - Importing the module exposes `RECUT_RECORD_FIELDS`, `validateRecutRecord`, `collectRecutGates`, and `verifyFixtures` — a script that imports it and exits non-zero when any of those four names is absent exits 0.
    - Running the module with `--fixtures` and no strict flag prints a message containing the substring `schema-only`.
    - Each of `--require-shape`, `--require-ancestry`, `--require-revert-proof`, `--require-toolchain`, `--require-supersession` has at least one negative-control fixture asserting a throw.
  </acceptance_criteria>
  <done>The verifier exists, its unit tests and hermetic fixture suite pass, and every shape, ancestry, revert, toolchain, and supersession gate has a negative control that fails closed.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the re-cut and freeze it behind the strict verifier</name>
  <files>.planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json</files>
  <reversibility rating="costly">D-01: every downstream Phase 231 artifact binds to the resulting SHA; re-cutting again invalidates GATE-01 run evidence, GATE-02 check-run proof, and Phase 232's PR body. Costly, not one-way — the ref stays local until Plan 04's authorized push, so it can still be re-cut at the price of redoing downstream work.</reversibility>
  <read_first>
    - scripts/ci/verify_recut_candidate.mjs (created in Task 1)
    - .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json (superseded by reference, never edited)
    - .planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md (D-00 through D-08)
    - .planning/phases/231-exact-sha-release-gate-proof/231-RESEARCH.md (Pitfall 1 and Pitfall 2)
  </read_first>
  <precondition>The working tree carries no uncommitted tracked-source change that would be swept into the re-cut merge: `git status --porcelain -- ':!.planning'` lists no modification to tracked source. Planning-doc churn under `.planning/` is expected and acceptable.</precondition>
  <action>
Re-measure every value CONTEXT.md recorded, live, immediately before cutting — never transcribe (D-00, and Phase 230's D-15). Capture into shell variables, not into a committed literal: milestone HEAD via `git rev-parse HEAD`, `origin/main` via `git rev-parse origin/main`, the superseded candidate via `git rev-parse integration/v1.62-candidate`, and the divergence via `git rev-list --left-right --count integration/v1.62-candidate...HEAD`. CONTEXT.md's recorded `27 / 37` and milestone HEAD `030a3c6e` are already known-stale: the research pass measured `27 / 39`, and this planning session advanced HEAD further still.

Cut the new candidate as a single `--no-ff` merge of `origin/main` into the current milestone HEAD, in exactly the Phase 230 shape (D-04): first parent is the milestone HEAD as measured this second, second parent is `origin/main`. Do not rebase, do not squash, and do not merge the existing candidate in — D-05 disqualifies advance-by-merging and merge-back-then-recut structurally, not merely by preference.

Re-apply, as new commits on top of the new merge, the two unique non-merge commits the superseded candidate carried. First, the toolchain pin: a tracked `.tool-versions` carrying `nodejs 22.14.0`, `elixir 1.19.5-otp-28`, and `erlang 28.5` (D-08). Do NOT copy the untracked root `.tool-versions` on disk — it carries only the `nodejs` line and would silently drop Elixir and Erlang (231-RESEARCH.md Pitfall 2). Take the content from `git show bab50d92be2695b12d5853e7d578e600376e73d0:.tool-versions` instead. Second, the `config.ex` disjoint-hunk coverage test carried by the superseded tip commit `bab50d92`. Prefer `git cherry-pick` of the original objects over re-authoring, so content cannot drift.

Repoint `refs/heads/integration/v1.62-candidate` to the new merge-plus-two-commits tip with `git branch -f` or `git update-ref`. Do not push it here: the push is Plan 04's separately authorized step (D-14).

Author `.planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json` with file mode `0o600`, using exactly the field set declared in `RECUT_RECORD_FIELDS`. Set `supersedes.candidate_object` to the superseded 40-hex, `supersedes.artifact` to the string `230-ROLLBACK-POINT.json`, and `supersedes.cause` to one sentence stating that the superseded candidate predated the Phase 230 verifiers `scripts/ci/verify_integration_disposition.mjs` and `scripts/ci/verify_phase230_archive_invariants.mjs` that GATE-01 and GATE-02 must execute — D-02 requires the re-cut to read as a deliberate correction with a stated cause, recorded in Phase 231's own evidence rather than as a Phase 230 addendum. Set `restore_argv` to a one-element array whose single element is the argv array `git revert -m 1 --no-edit <new candidate 40-hex>` — an array of argv arrays, never a shell string (D-07). Leave `230-ROLLBACK-POINT.json` byte-unchanged.

Do not touch or re-diff `229-REPOSITORY-INVENTORY.json` or any Phase 230 capsule (D-27, D-28).
Before making the first edit in this task, capture the pre-edit commit into the environment: `export PHASE_BASE="$(git rev-parse HEAD)"`. The frozen-artifact verify command uses it as an explicit diff range, so a frozen file modified and then committed is still caught.
 Do not create a git worktree (D-07, D-12). Treat the hazard universe as recomputed fresh against the new candidate rather than assumed to recur (D-06); if `origin/main` has moved since CONTEXT.md measured it, that is new information to record, not to paper over.
  </action>
  <verify>
    <automated>node scripts/ci/verify_recut_candidate.mjs --repo . --record .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-shape --require-ancestry --require-revert-proof --require-toolchain --require-supersession</automated>
    <fails_when>non-zero exit, a line beginning `recut candidate verify: FAIL:`, or a PASS line whose suffix contains `schema-only` instead of listing all five strict flags</fails_when>
    <automated>CAND="$(git rev-parse integration/v1.62-candidate)"; test "$CAND" != "bab50d92be2695b12d5853e7d578e600376e73d0" &amp;&amp; TOOLS="$(git show "$CAND":.tool-versions)" &amp;&amp; case "$TOOLS" in *"elixir 1.19.5-otp-28"*) : ;; *) exit 1 ;; esac &amp;&amp; git cat-file -e "$CAND":scripts/ci/verify_integration_disposition.mjs &amp;&amp; git cat-file -e "$CAND":scripts/ci/verify_phase230_archive_invariants.mjs</automated>
    <fails_when>non-zero exit — the candidate still resolves to the superseded object, the tracked toolchain pin lacks the Elixir line, or either Phase 230 verifier is absent from the candidate's tree</fails_when>
    <automated>BASE="${PHASE_BASE:?capture PHASE_BASE=$(git rev-parse HEAD) before the first edit of this task}" &amp;&amp; git diff --quiet -- .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json &amp;&amp; git diff --quiet "$BASE"..HEAD -- .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json</automated>
    <fails_when>non-zero exit, meaning a frozen Phase 229 or Phase 230 artifact was modified instead of superseded by reference</fails_when>
  </verify>
  <acceptance_criteria>
    - `git rev-list --parents -n 1 <merge commit>` lists exactly three object ids (the merge plus two parents).
    - `git rev-list --count integration/v1.62-candidate ^<milestone HEAD at cut> ^origin/main` prints a value whose only difference from `1` is the two re-applied non-merge commits — assert the merge commit itself satisfies the literal `== 1` form of the D-04 gate.
    - `git merge-base --is-ancestor <each of v1.61 commit, origin/main, and the four closure commits> integration/v1.62-candidate` exits 0 for all six.
    - `git worktree list` prints the same number of rows before and after this task.
    - `stat -f '%Lp' .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json` prints `600`.
    - `grep -o '"cause"' .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json` returns exactly one match, and its value mentions both Phase 230 verifier filenames.
    - No absolute path, `$HOME`, or username string appears in the record — `grep -E '/Users/|/home/|\$HOME' 231-ROLLBACK-POINT.json` returns no match.
  </acceptance_criteria>
  <done>`integration/v1.62-candidate` points at a new commit satisfying every D-04 shape and ancestry gate, the revert proof executes successfully in a scratch clone, the toolchain pin carries all three lines, and the strict verifier passes with all five require flags listed in its PASS suffix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git object store to committed evidence artifact | Object ids, ref names, and paths measured from the working repository cross into a file that will be published on a public repository. |
| scratch clone to host filesystem | A disposable clone is created under the system temp dir and executes `git revert`; it must not leak its path into the record or outlive the task. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-01 | Information Disclosure | `231-ROLLBACK-POINT.json` | high | mitigate | Closed `RECUT_RECORD_FIELDS` allow-list validated by `validateRecutRecord`; acceptance criterion greps for absolute paths, `$HOME`, and usernames (D-31). |
| T-231-02 | Tampering | `refs/heads/integration/v1.62-candidate` | high | mitigate | Only `git branch -f`/`git update-ref` on that single local ref; no push, no tag move, no force-push to any remote in this plan (D-14 defers the push to Plan 04). |
| T-231-03 | Repudiation | The re-cut itself | high | mitigate | `supersedes.cause` is a required non-empty field with a negative control; the ref move is never unexplained (D-02). |
| T-231-04 | Tampering | Frozen 229/230 capsules and `230-ROLLBACK-POINT.json` | high | mitigate | An explicit-range `git diff --quiet "$PHASE_BASE"..HEAD` verify command, paired with a working-tree `git diff --quiet`, fails the task if any frozen artifact is modified (D-07, D-27, D-28). |
| T-231-05 | Spoofing | Record-vs-reality drift | high | mitigate | `collectRecutGates` re-derives every gate from live git plumbing; the record is compared against, never trusted as, authority (D-30). |
| T-231-06 | Denial of Service | Scratch clone temp dir | low | accept | Clone is created under the system temp dir and removed in a `finally`; worst case is recoverable disk usage on the developer machine. |
| T-231-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. No `mix deps.get`, `npm ci`, or equivalent runs here, so no package-legitimacy surface exists; the scratch clone only runs `git`. |
</threat_model>

<verification>
- `node --check` and `node --test` pass for `scripts/ci/verify_recut_candidate.mjs`.
- `node scripts/ci/verify_recut_candidate.mjs --fixtures --expected-repository szTheory/accrue` prints `recut candidate fixtures: PASS`.
- The strict real-mode invocation with all five require flags passes and lists them in its PASS suffix.
- `git worktree list` row count is unchanged from the start of the plan.
- `230-ROLLBACK-POINT.json` and `229-REPOSITORY-INVENTORY.json` are byte-unchanged.
</verification>

<success_criteria>
The candidate SHA that the rest of Phase 231 binds to exists, is new, contains the Phase 230 verifiers, satisfies every D-04 shape and ancestry gate under an executable verifier, and carries a stated cause for superseding `bab50d92`.
</success_criteria>

<output>
Create `.planning/phases/231-exact-sha-release-gate-proof/231-01-SUMMARY.md` when done, recording the new candidate 40-hex, the milestone HEAD it was cut from, and the superseded object.
</output>

---
phase: 231-exact-sha-release-gate-proof
plan: 05
type: execute
wave: 3
depends_on: ["231-02", "231-03", "231-04"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json
  - .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.md
autonomous: true
requirements: [GATE-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/WINDOWS.md` ends the plan with `open_count: 0`, and every one of the ten rows is `fixed` or `waived` (GATE-03)."
    - "Every status flip is performed by `gsd-tools windows waive <id> \"<reason>\"` or `gsd-tools windows fixed <id>`, the ledger's only writer, which recomputes all four frontmatter counts; the file is never hand-edited (D-25)."
    - "`.planning/WINDOWS.md` still has exactly 10 columns and the identical pinned header line; no column is added (D-25)."
    - "Each of the six `unrun-verify` rows (1, 4, 5, 7, 8, 10) is `fixed` only because the gate it records now executes at the candidate SHA and reports a real outcome in the GATE-01 or GATE-02 evidence; otherwise it is `waived` with owner, rationale, and release impact (D-22)."
    - "Each of the four `deviation` rows (2, 3, 6, 9) is `fixed` only after confirming the intentional change is still present and intact at the candidate SHA, with a recorded rationale and release-impact statement (D-22)."
    - "Row 5 is investigated and either fixed on its merits or blocked; it is never auto-waived (D-24)."
    - "Every row's `current_evidence` is re-derived at the candidate SHA and is not byte-identical to the row's original ledger description — row 1 is the proof this matters, since its recorded blocker no longer describes reality (D-23)."
    - "`231-WINDOW-DISPOSITIONS.json` joins 1:1 by row id to every non-`open` WINDOWS.md row with exact set equality in both directions (D-26)."
    - "Every `waived` row carries a non-empty owner, rationale, and release impact (GATE-03)."
    - "Re-rendering the committed JSON byte-equals the committed Markdown (D-31)."
  artifacts:
    - .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json
    - .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.md
  key_links:
    - "GATE-01's cohort evidence is the execution proof the six `unrun-verify` rows depend on; a gate that ran and failed on its merits makes that row a GATE-01 blocker, not a silently-closed window (D-22)."
    - "WINDOWS.md's single `reason` cell holds the short summary, sourced from the rich sibling artifact — the terse ledger and the rich record stay joined, never duplicated (D-26)."
  prohibitions:
    - "MUST NOT hand-edit `.planning/WINDOWS.md`; all status, reason, and resolved_at changes go through the ledger writer CLI (D-25)."
    - "MUST NOT widen `.planning/WINDOWS.md` beyond its existing 10 columns (D-25)."
    - "MUST NOT copy a row's original ledger description forward as its current evidence (D-23)."
    - "MUST NOT auto-waive row 5 (D-24)."
    - "MUST NOT re-diff or modify the frozen 229 or 230 capsules when window statuses change; those are point-in-time artifacts and their strict re-verification failing is expected, not a regression to chase (D-27, D-28)."
    - "MUST NOT emit absolute paths, `$HOME`, actor names, adopter identifiers, or secrets into the committed evidence (D-31)."
---

<objective>
Drive all ten open ship windows to fixed-or-waived with current evidence, owner, rationale, and release impact re-derived at the candidate SHA.

Purpose: GATE-03 requires no unexplained open window at the release handoff. D-22 makes disposition a function of the row's `kind` decided by an evidence-backed test, not by judgment.
Output: `231-WINDOW-DISPOSITIONS.json` and `.md`, and `.planning/WINDOWS.md` at `open_count: 0`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md
@.planning/phases/231-exact-sha-release-gate-proof/231-RESEARCH.md
@.planning/WINDOWS.md
</context>

<artifacts_this_phase_produces>
Created by this plan:

- New evidence artifact paths: `.planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json` and `231-WINDOW-DISPOSITIONS.md`.
- No new script module or exported symbol; this plan consumes the triad built in Plan 02 and the evidence produced in Plans 03 and 04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive current evidence for all ten rows at the candidate SHA</name>
  <files>.planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json</files>
  <read_first>
    - .planning/WINDOWS.md (the ten rows with their id, phase, kind, file, line, and original description)
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json (Plan 03 — the per-job outcomes that decide the `unrun-verify` rows)
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson (Plan 04 — the dispatch run records; row 10 concerns workflow_dispatch observations)
    - .planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md (D-22, D-23, D-24)
    - examples/accrue_host/playwright.config.js (row 1 — the config that already defines a `chromium-mobile` project, contradicting the recorded description)
    - examples/accrue_host/test/accrue_host/billing_facade_test.exs (row 5 — the recorded failing test at line 160)
    - accrue_admin/mix.lock (row 9 — the locked `jose` dependency at line 41)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift (row 2)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (row 3)
    - accrue/lib/accrue/entitlements/snapshot.ex (row 6)
    - examples/accrue_host/lib/accrue_host_web/components/layouts.ex (row 8)
  </read_first>
  <action>
For every one of the ten rows, re-derive the row's condition at the candidate SHA and author a rich record row. Never copy the ledger description forward as evidence: row 1 records that the checked-in Playwright config has no `chromium-mobile` project, but the config already defines one, so a copied description would close that row on a false premise (D-23). Preserve that lesson explicitly in row 1's rationale — it is the quotable case for why re-derivation is the rule.

Apply the `kind`-driven test, not judgment (D-22).

For the six `unrun-verify` rows (1, 4, 5, 7, 8, 10): the window records that a gate could not run. Look up the corresponding lane in `231-GATE-01-EVIDENCE.json` — row 1 maps to the host Playwright lane, rows 4 and 7 and 8 to the formatting and `mix verify` lanes in `accrue` and `examples/accrue_host`, row 5 to the `examples/accrue_host` test lane, and row 10 to the `workflow_dispatch` observation set in `231-GATE-02-EVIDENCE.ndjson`. The row is `fixed` when that gate now executes at the candidate SHA and reports a real outcome, with the recorded exit code cited as its current evidence. If the gate ran and failed on its merits, that is a GATE-01 blocker recorded `failed` in the cohort evidence — do not mark the row `fixed`, and do not silently roll it forward: it is `waived` only with owner, rationale, and release impact.

Row 5 gets conservative handling and is never auto-waived (D-24). It records a real failing test, not merely a gate that could not start. Investigate: if the test passes at the candidate SHA inside the clean-checkout run, record `fixed` with the run's exit code as evidence; if it still fails, fix it or block the phase. Research observed it passing on a dirty milestone-HEAD tree, but that is a lead, not evidence — the evidence must be the clean-checkout run at the candidate SHA (D-12, D-23).

For the four `deviation` rows (2, 3, 6, 9): an intentional, already-merged, already-reviewed change is not a defect by default. Each closes as `fixed` by confirming at the candidate SHA that the change is still present and intact — read the file at the candidate SHA, not on the working tree — plus a recorded rationale and a release-impact statement. If inspection shows the change stale, absent, or newly risky, escalate it rather than closing it.

Write the rich rows to a scratch input file for the Plan 02 collector, with these fields per row: `id`, `phase`, `kind`, `disposition`, `state`, `exit_code`, `owner`, `rationale`, `release_impact`, `current_evidence`, `evidence_command`, `resolved_at`. Use the Phase 226 lexicon for `state` (`proved`, `failed`, `skipped`, `advisory`, `non_run`) — `deferred`, `n/a`, and `green` are forbidden, and `proved` requires a recorded exit code (D-29). Every `waived` row must carry a non-empty owner, rationale, and release impact; a waive without all three fails the verifier.

Keep every string free of absolute paths, home-directory references, actor names, adopter identifiers, and secrets (D-31); the collector's validator rejects them.

Do not touch `.planning/WINDOWS.md` in this task — the flips happen in Task 2 through the ledger writer.
  </action>
  <verify>
    <automated>node scripts/ci/collect_window_dispositions.mjs --repo . --expected-repository szTheory/accrue --candidate integration/v1.62-candidate --records-in "$RICH_ROWS_INPUT" --out /dev/stdout | node --input-type=module -e "let s=''; process.stdin.on('data',d=&gt;s+=d).on('end',()=&gt;{const r=JSON.parse(s); if(r.rows.length!==10){console.error('expected 10 disposition rows, got '+r.rows.length);process.exit(1);} const bad=r.rows.filter(x=&gt;x.disposition==='waived'&amp;&amp;(!x.owner||!x.rationale||!x.release_impact)); if(bad.length){console.error('waived rows missing owner/rationale/release_impact: '+bad.map(x=&gt;x.id).join(','));process.exit(1);}})"</automated>
    <fails_when>non-zero exit, a collector validation throw, or stderr reporting a row count other than 10 or naming waived rows with an empty owner, rationale, or release impact</fails_when>
    <automated>node --input-type=module -e "const fs=require('node:fs'); const rows=JSON.parse(fs.readFileSync(process.env.RICH_ROWS_INPUT,'utf8')); const led=fs.readFileSync('.planning/WINDOWS.md','utf8'); const dup=rows.filter(r=&gt;led.includes('| '+r.current_evidence+' |')); if(dup.length){console.error('current_evidence copied verbatim from the ledger for ids: '+dup.map(r=&gt;r.id).join(','));process.exit(1);} const five=rows.find(r=&gt;r.id===5); if(!five||(five.disposition==='waived'&amp;&amp;!five.rationale)){console.error('row 5 waived without a rationale');process.exit(1);}"</automated>
    <fails_when>non-zero exit, or stderr naming any row whose `current_evidence` was copied verbatim out of the ledger, or reporting row 5 waived without a rationale</fails_when>
  </verify>
  <acceptance_criteria>
    - The rich input carries exactly ten rows with ids 1 through 10, no duplicates.
    - Each of ids 1, 4, 5, 7, 8, 10 cites a specific lane and exit code from `231-GATE-01-EVIDENCE.json` or a specific run record from `231-GATE-02-EVIDENCE.ndjson` in its `current_evidence`.
    - Each of ids 2, 3, 6, 9 cites a file-content check performed at the candidate SHA in its `current_evidence`, and each carries a non-empty `release_impact`.
    - Row 1's `rationale` states that the originally recorded blocker no longer describes reality and that a copied-forward description would have closed it on a false premise.
    - Row 5's disposition is supported by the clean-checkout run's recorded outcome; if `waived`, the rationale names the specific reproduction observed in that run.
    - No row's `current_evidence` appears verbatim as a `description` cell in `.planning/WINDOWS.md`.
    - Every `state` value is one of `proved`, `failed`, `skipped`, `advisory`, `non_run`, and every `proved` row carries an integer `exit_code`.
  </acceptance_criteria>
  <done>All ten rows have evidence re-derived at the candidate SHA, dispositions decided by the row's kind against that evidence, and every waive carries owner, rationale, and release impact.</done>
</task>

<task type="auto">
  <name>Task 2: Flip every ledger status through the writer CLI and commit the joined disposition artifact</name>
  <files>.planning/WINDOWS.md, .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json, .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.md</files>
  <read_first>
    - .planning/WINDOWS.md (the pinned 10-column header, the four frontmatter counts, and the embedded JSON block the writer regenerates)
    - scripts/ci/collect_window_dispositions.mjs, render_window_dispositions.mjs, verify_window_dispositions.mjs (built in Plan 02)
    - scripts/ci/verify_repository_inventory.mjs (the `directShipWindows` parse the ledger must keep satisfying)
  </read_first>
  <action>
Emit the canonical record and its rendering: run `collect_window_dispositions.mjs` with the rich input from Task 1 to write `.planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json` with mode `0o600`, then `render_window_dispositions.mjs` to write `231-WINDOW-DISPOSITIONS.md`.

Flip every ledger row through the writer CLI and nothing else. For a row disposed `fixed`, run `gsd-tools windows fixed <id>`. For a row disposed `waived`, run `gsd-tools windows waive <id> "<short summary>"`, where the short summary is the one-line form of that row's rationale sourced from the rich artifact — WINDOWS.md's single `reason` cell holds the summary, the sibling artifact holds the full owner, rationale, and release impact (D-26). Never open `.planning/WINDOWS.md` in an editor and never write it with a shell redirect: the writer is the only supported mutator and is what deterministically recomputes `total_count`, `open_count`, `waived_count`, and `fixed_count` (D-25).

Do not add a column, do not change the header line, and do not extend the schema to carry owner or release impact — `directShipWindows` hardcodes a 10-column parse and widening it cascades into the Phase 229 and 230 fixture golden files (D-25).

Expect and do not chase one consequence: flipping statuses changes live WINDOWS.md, so strict re-verification of the frozen 229 and 230 capsules against the new ledger will legitimately report differences. Those capsules are point-in-time artifacts, routine CI only runs the `--fixtures` self-tests, and `collect_repository_inventory.mjs::readShipWindows` recomputes ship windows fresh at collection time, which is why a collect-plus-verify pair stays self-consistent (D-27). Do not re-diff or modify them (D-28); Plan 06 mints a fresh capsule after these edits land.
Before making the first edit in this task, capture the pre-edit commit into the environment: `export PHASE_BASE="$(git rev-parse HEAD)"`. The frozen-artifact verify command uses it as an explicit diff range, so a frozen file modified and then committed is still caught.


Finally, run the strict window-disposition verification with every require flag, so the 1:1 row-id join, the evidence-freshness check, the waiver-completeness check, and the render-determinism byte comparison all execute against the committed artifacts.
  </action>
  <verify>
    <automated>node scripts/ci/verify_window_dispositions.mjs --repo . --records .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json --rendered .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.md --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-row-join --require-evidence-freshness --require-waiver-completeness --require-determinism</automated>
    <fails_when>non-zero exit, a line beginning `window dispositions verify: FAIL:`, a missing/extra/changed triple naming any row id, or a PASS suffix containing `schema-only`</fails_when>
    <automated>grep -qE '^open_count: 0$' .planning/WINDOWS.md &amp;&amp; grep -qF '| id | phase | kind | file | line | description | status | reason | recorded_at | resolved_at |' .planning/WINDOWS.md &amp;&amp; test "$(grep -c '^| [0-9]' .planning/WINDOWS.md)" = "10"</automated>
    <fails_when>non-zero exit — the ledger frontmatter no longer reports zero open windows, the pinned 10-column header line changed, or the table no longer holds exactly ten numbered rows</fails_when>
    <automated>node --test scripts/ci/verify_repository_inventory.mjs</automated>
    <fails_when>non-zero exit, or any `node:test` case reported as `not ok` — the flipped ledger no longer satisfies the `directShipWindows` parse contract</fails_when>
    <automated>BASE="${PHASE_BASE:?capture PHASE_BASE=$(git rev-parse HEAD) before the first edit of this task}" &amp;&amp; git diff --quiet -- .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json &amp;&amp; git diff --quiet "$BASE"..HEAD -- .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json</automated>
    <fails_when>non-zero exit, meaning a frozen Phase 229 or Phase 230 capsule or record was modified in response to the ledger change instead of left alone</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` frontmatter reports `open_count: 0` and `total_count: 10`, with `waived_count` plus `fixed_count` summing to 10.
    - The ledger's header line is byte-identical to its pre-task value, and every row still has exactly 10 pipe-delimited columns.
    - Every non-`open` row's `reason` cell is non-empty for `waived` rows, and every row has a non-empty `resolved_at`.
    - Comparing `.planning/WINDOWS.md` against its pre-task committed revision with an explicit range (`git diff <pre-task-sha>..HEAD -- .planning/WINDOWS.md`) shows only status, reason, resolved_at, frontmatter count, and embedded JSON changes — no column added, no description rewritten.
    - Each WINDOWS.md `reason` summary corresponds to the same row id's `rationale` in `231-WINDOW-DISPOSITIONS.json`.
    - `stat -f '%Lp' .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json` prints `600`.
    - No absolute path or home-directory reference appears in either committed disposition artifact.
  </acceptance_criteria>
  <done>Every ship window is `fixed` or `waived` through the ledger writer, `open_count` is 0, the terse schema is intact, and the rich sibling artifact joins 1:1 by row id under strict verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored rationale to committed evidence | Free-text owner, rationale, and release impact cross into a file published on a public repository. |
| ledger writer to `.planning/WINDOWS.md` | The only sanctioned mutator; any other write path silently desynchronizes the four frontmatter counts and the embedded JSON block. |
| flipped ledger to frozen capsules | Changing live ship-window state invalidates strict re-verification of previously published capsules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-24 | Information Disclosure | `231-WINDOW-DISPOSITIONS.json` free-text fields | high | mitigate | The collector's validator rejects absolute paths and home-directory segments; an acceptance criterion re-checks both committed artifacts (D-31). |
| T-231-25 | Repudiation | A window closed on a stale premise | high | mitigate | `--require-evidence-freshness` fails on byte-identity between a row's current evidence and its original ledger description; row 1 is the canonical case (D-23). |
| T-231-26 | Tampering | Hand-edited ledger desynchronizing counts | high | mitigate | All flips go through `gsd-tools windows waive` / `fixed`, which recomputes the counts; `node --test scripts/ci/verify_repository_inventory.mjs` re-asserts the parse contract afterwards (D-25). |
| T-231-27 | Repudiation | An unexplained waive | high | mitigate | `--require-waiver-completeness` fails on any waived row with an empty owner, rationale, or release impact; row 5 additionally may not be auto-waived (D-22, D-24). |
| T-231-28 | Tampering | Frozen 229/230 capsules amended to absorb the ledger change | high | mitigate | An explicit-range `git diff --quiet "$PHASE_BASE"..HEAD` verify command, paired with a working-tree `git diff --quiet`, fails the task if any frozen capsule or record is modified; Plan 06 mints a fresh capsule instead (D-27, D-28). |
| T-231-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. It runs existing repository tooling and the GSD ledger CLI only; no manifest or lockfile is touched. |
</threat_model>

<verification>
- Strict window-disposition verification passes with all four require flags against the committed artifacts.
- `.planning/WINDOWS.md` reports `open_count: 0` with the pinned header intact and exactly ten rows.
- `node --test scripts/ci/verify_repository_inventory.mjs` still passes, proving the ledger parse contract holds.
- The frozen Phase 229 and Phase 230 artifacts are byte-unchanged.
</verification>

<success_criteria>
A maintainer can read one rendered document and see, for every former ship window, whether it is fixed or waived, who owns it, why, what the release impact is, and what evidence at the candidate SHA decided it — with nothing unexplained left open.
</success_criteria>

<output>
Create `.planning/phases/231-exact-sha-release-gate-proof/231-05-SUMMARY.md` when done, recording the fixed/waived split and the disposition of row 5 in particular.
</output>

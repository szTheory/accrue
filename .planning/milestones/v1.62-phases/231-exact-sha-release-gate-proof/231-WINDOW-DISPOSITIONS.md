# Ship-Window Dispositions

Sanitized schema-v1 evidence answering "can I ship this SHA, and what is still unproven?" for every non-open row in `.planning/WINDOWS.md`, joined 1:1 by row id. This is a deterministic projection: no raw payloads, actor identities, secret values, or absolute paths are present.

This document is a deterministic re-render (a projection) of the schema-v1 JSON record identified below; the JSON is the evidence of record, and this Markdown is generated from it, never edited directly.

The `--require-row-join`/`--require-evidence-freshness` strict checks apply only to the current phase's own record, because the join and freshness comparison are against the live `.planning/WINDOWS.md` ledger. A historical, frozen record from a prior phase verifies schema and determinism only -- if a historical record's row-join or freshness check ever fails, the ledger has moved, not the evidence; the fix is never to edit the frozen record.

<!-- phase231-window-dispositions:start -->

## Candidate identity

Candidate object: `f524f2a6b16d3576829632ab6fa77d24b718e6f7`. Observed at: 2026-09-15T22:53:02-04:00. Rows: **10**.

## Waived — the gate ran and failed

1 row(s).

| Id | Phase | Kind | Disposition | State | Exit code | Owner | Rationale | Release impact | Current evidence | Evidence command |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10 | 227 | unrun-verify | waived | failed | — | maintainer | The gate now executes and reports a real, honest outcome (GATE-02's dispatch), but that outcome is a failure, not a qualifying success, so Phase 227's three-success bounded critical-path comparison still cannot run to completion with genuinely qualifying data. This is the 'original blocker still reproduces' branch of D-22, not the 'runs and passes' branch, and per D-22 that closes as waived, never silently rolled forward and never auto-fixed by asserting a gate outcome that did not occur. | No release-blocking effect. The critical-path timing comparison is an observability/optimization improvement, not a correctness or security gate, and does not block shipping this candidate SHA. Qualifying successful workflow_dispatch observations will accumulate on future dispatches, including after Phase 232's fixes for the real GATE-02 regressions land. | GATE-02 (plan 231-04, 231-GATE-02-EVIDENCE.ndjson) dispatched \`ci.yml\` at this exact candidate SHA and recorded exactly one real workflow_dispatch-class run observation (run 35100620086), which concluded \`failure\`. The row's originally recorded blocker was that Phase 227's bounded three-success critical-path comparison had no qualifying successful workflow_dispatch observations to compare; the underlying data gap is unchanged in substance -- there is still zero qualifying successful workflow_dispatch observation for this candidate lineage, only one real, honestly-recorded failure. A gate that runs and produces a real outcome that is not a qualifying success does not satisfy the row's original need, per D-22's 'if the original blocker still reproduces, the row is waived' branch. | bash -c jq -r '. \| select(.kind=="run") \| .conclusion' .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson |

## Waived — the gate never proved anything

1 row(s).

| Id | Phase | Kind | Disposition | State | Exit code | Owner | Rationale | Release impact | Current evidence | Evidence command |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 3 | 215 | deviation | waived | non_run | — | maintainer | D-22 requires escalation rather than a 'fixed' close when inspection shows the recorded change is absent -- it is absent here: the specific Swift reducer this row named was deleted, not merely edited, as part of a later, separately reviewed and shipped phase (223-04). Waiving is the honest disposition: this row is not closed because the original change persists, but because a later authorized supersession replaced its enforcement mechanism, and that replacement was independently verified intact at the candidate SHA. The gate that could exercise the original Swift-level logic cannot run because the logic no longer exists (non_run), which is distinct from a gate that ran and failed on its merits. | No regression to this candidate. The equivalent safety property (a checked-in capability report can never be forced to report 'proven' feasibility without genuine device evidence) is enforced by scripts/ci/verify_ios_offline_client.sh's direct jq assertion, verified true at the candidate SHA. This is an architectural change from Phase 223, predating this candidate, and does not block shipping this SHA. No further action is required unless a future phase chooses to re-home an equivalent Swift-level reducer. | examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift at the candidate SHA is an 8-line re-export (\`@_exported import AccrueOfflineClientCore\` plus a doc comment) -- the CapabilityReport/Capability/CheckedInCapabilityReportValidator reducer logic this row originally recorded a fix in no longer exists anywhere in the tree (confirmed: no .swift file under packages/accrue-offline-client or examples/crosswake_tracer/Sources contains the string 'Capability' outside the now-orphaned test file in row 2). Commit a03634a2 (Phase 223-04) deleted 1080 lines from this exact file, replacing the tracer's own verifier/cache authority with the standalone \`accrue-offline-client\` package facade. The safety property this row protected -- that feasibility cannot be reported 'proven' from caller-supplied evidence -- is enforced today by a different mechanism: scripts/ci/verify_ios_offline_client.sh's \`jq\` assertion directly against the checked-in examples/crosswake_tracer/capability-report.json, confirmed at the candidate SHA to assert \`overall_status == "feasibility_blocked"\` and every capability row \`status == "feasibility_blocked"\`. | bash -c git show f524f2a6b16d3576829632ab6fa77d24b718e6f7:examples/crosswake_tracer/capability-report.json \| jq -e '.overall_status == "feasibility_blocked" and all(.capabilities[]; .status == "feasibility_blocked")' |

## Waived — the gate passed anyway

0 row(s).


## Fixed — proved at the candidate SHA

8 row(s).

| Id | Phase | Kind | Disposition | State | Exit code | Owner | Rationale | Release impact | Current evidence | Evidence command |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 214.2 | unrun-verify | fixed | proved | 0 | maintainer | The row's originally recorded blocker ('the checked-in Playwright config has no chromium-mobile project') no longer describes reality: the config already defines a chromium-mobile project matched exactly to this spec file. Copying that original description forward as 'current evidence' would have closed this row on a false premise -- this is the canonical case the phase's re-derivation discipline (D-23) exists to prevent, and it is preserved here explicitly as the quotable lesson. The gate itself (playwright-e2e) executed cleanly end-to-end at the candidate SHA per GATE-01, so the disposition rests on genuine re-derived evidence, not the stale description. | No release-blocking effect. The host mobile entitlement contract is no longer skipped by missing configuration and executes as part of the already-proved playwright-e2e merge-blocking job. | examples/accrue_host/playwright.config.js already defines a chromium-mobile project (testMatch matching verify01-admin-mobile.spec.js, Pixel 5 device profile) at the candidate SHA -- confirmed by reading the file directly at the candidate object. GATE-01's playwright-e2e lane (231-GATE-01-EVIDENCE.json) executed the full 3-shard suite in a fresh scratch clone at this exact candidate SHA and proved clean (exit 0): shard 1 17 passed, shard 3 4 passed / 3 skipped (the 3 skips are the beforeEach test.skip() firing for the non-mobile chromium-desktop project, by design, not an unrun gate). The gate this row recorded as unrunnable now runs end to end and reports a real, passing outcome. | git show f524f2a6b16d3576829632ab6fa77d24b718e6f7:examples/accrue_host/playwright.config.js |
| 2 | 215 | deviation | fixed | proved | 0 | maintainer | D-22 closes a deviation as fixed by confirming the intentional change is still present and intact at the candidate SHA -- it is: the specific corrected text has not regressed or been reverted. Re-derivation per D-23 additionally surfaced that this file has been orphaned from the SPM test-target graph by an unrelated later phase (223-04) and is never built or run by CI. That orphaning is a pre-existing Phase-223 housekeeping gap, not a candidate regression introduced by this phase, and carries zero runtime risk because the file exercises no compiled code path at all. Flagging it honestly here rather than silently closing the row on the original description alone. | No release-blocking effect. The corrected text is intact; the file's SPM-orphaned status predates this candidate by roughly six weeks (commit a03634a2) and does not change behavior of any shipped artifact. Recommended as a follow-up housekeeping item (delete or re-target the dead test directory) in a future phase, out of this plan's scope. | examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift still contains the corrected capability case names at the candidate SHA (9 occurrences of the \`Capability.\<case\>\` pattern, none of the stale pre-fix names), so the intentional Phase 215 correction remains present and byte-intact. Re-derivation also surfaced a material fact absent from the original description: since commit a03634a2 (Phase 223-04, 'consume offline client package from tracer'), examples/crosswake_tracer/Package.swift declares its test target as AccrueOfflineClientTracerTests, not AccrueOfflineClientTests -- confirmed by grepping the candidate SHA's Package.swift for the literal target name (zero matches). This whole test directory, including this file, is therefore orphaned from Swift Package Manager's build graph and is not compiled or executed by \`swift test\` at the candidate SHA (verified: running \`swift test --package-path examples/crosswake_tracer\` only builds/executes the AccrueOfflineClientTracerTests target's PackageConformanceTests.swift). | bash -c git show f524f2a6b16d3576829632ab6fa77d24b718e6f7:examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift \| grep -c 'Capability\\.' |
| 4 | 217 | unrun-verify | fixed | proved | 0 | maintainer | The gate this row recorded as unrunnable (mix test.all, blocked upstream by an unrelated dirty file's formatting) now executes and reports a real outcome: a clean pass, not merely an absence of the original blocker. Per D-22 this closes fixed, not waived. | No release-blocking effect. The package-docs verifier suite passes cleanly and formatting no longer blocks the wider \`mix test.all\` invocation. | Re-ran \`mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0\` in the \`accrue\` package inside a fresh scratch clone at the candidate SHA: 46 tests, 0 failures, exit 0. The original blocker was that \`mix test.all\` could not start because an unrelated shared dirty file was not formatted; re-ran \`mix format --check-formatted\` for the whole \`accrue\` package at the candidate SHA and it exits 0 with no diff, so the formatting blocker that prevented this test from starting no longer exists. This is independently corroborated by GATE-01's release-gate row (231-GATE-01-EVIDENCE.json), which ran \`mix format --check-formatted && ... && mix test --warnings-as-errors\` for \`accrue\` across 3 required matrix cells, all clean. | bash -c cd accrue && MIX_ENV=test mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0 |
| 5 | 220 | unrun-verify | fixed | proved | 0 | maintainer | D-24 requires this row be investigated and fixed-or-blocked on its merits at a genuinely clean checkout, never auto-waived, and never satisfied by the research note that it passed on a dirty milestone-HEAD working tree. The clean-checkout run above is exactly that required evidence, and it passes. | No release-blocking effect. The fake subscription uniqueness path is proven correct at the candidate SHA in a clean-checkout run, closing the row on its merits rather than by waiver. | Investigated per D-24 (never auto-waived): re-ran \`mix test test/accrue_host/billing_facade_test.exs --seed 0\` inside \`examples/accrue_host\` in a fresh scratch clone at the candidate SHA (deterministic Postgres migrate from a cold, freshly created test database, not a dirty working tree): 18 tests, 0 failures, exit 0, including the previously-failing 'billing_state_for/1 returns the fake-backed customer and latest subscription' case at line 160 that exercises the fake subscription uniqueness path this row recorded as failing. This is independently corroborated by GATE-01's host-integration row, which proved the full \`mix verify.full\` suite (219 ExUnit + 21 Playwright tests) clean in its own fresh scratch clone at this candidate SHA. | bash -c cd examples/accrue_host && MIX_ENV=test mix test test/accrue_host/billing_facade_test.exs --seed 0 |
| 6 | 220 | deviation | fixed | proved | 0 | maintainer | The intentional change (forwarding the snapshot \`:now\` option so frozen expiry-boundary proofs are deterministic) is confirmed present and intact by direct inspection of the file at the candidate SHA, plus a clean passing test run in a fresh clone -- satisfying D-22's fixed-disposition bar for a deviation row. | No release-blocking effect. Frozen expiry-boundary snapshot proofs remain deterministic and correct at the candidate SHA. | accrue/lib/accrue/entitlements/snapshot.ex at the candidate SHA still forwards the \`:now\` option from \`from_grants/2\`'s \`opts\` through to the live-window filtering fold (\`Keyword.get_lazy(opts, :now, &DateTime.utc_now/0)\` used both for \`Enum.filter(&live?(&1, now))\` and echoed back on the returned struct's \`revision\`/repository-folding path), matching the row's recorded intent. Re-ran \`mix test test/accrue/entitlements/snapshot_test.exs --seed 0\` in a fresh scratch clone at the candidate SHA: 4 tests, 0 failures, exit 0. | bash -c cd accrue && MIX_ENV=test mix test test/accrue/entitlements/snapshot_test.exs --seed 0 |
| 7 | 221 | unrun-verify | fixed | proved | 0 | maintainer | The gate this row recorded as unrunnable (mix verify, blocked upstream by tracked formatting violations) now executes and reports a real, clean outcome, satisfying D-22's fixed bar for an unrun-verify row. | No release-blocking effect. \`mix verify\` runs to completion at the candidate SHA with no formatting-related blocker. | Re-ran \`mix format --check-formatted\` for \`examples/accrue_host\` in a fresh scratch clone at the candidate SHA: exit 0, no unformatted files. The formatting violations that this row recorded as blocking \`mix verify\` before its test suite could start no longer exist. GATE-01's host-integration row (231-GATE-01-EVIDENCE.json) independently proved the full \`mix verify.full\` invocation (which itself gates on formatting before running tests) clean at exit 0 in its own fresh scratch clone at this candidate SHA. | bash -c cd examples/accrue_host && mix format --check-formatted |
| 8 | 221 | unrun-verify | fixed | proved | 0 | maintainer | The specific formatting violations this row named in layouts.ex and the existing migrations no longer reproduce at the candidate SHA -- the full-package format check now exits 0, satisfying D-22's fixed bar for an unrun-verify row. | No release-blocking effect. \`mix format --check-formatted\` passes cleanly for the whole \`examples/accrue_host\` package at the candidate SHA. | Re-ran \`mix format --check-formatted\` for \`examples/accrue_host\` in a fresh scratch clone at the candidate SHA: exit 0, no unformatted files, including examples/accrue_host/lib/accrue_host_web/components/layouts.ex and every tracked migration this row named as a specific blocker. This is the same evidence run recorded for row 7 (the two rows share the identical underlying \`mix format --check-formatted\` gate), and is independently corroborated by GATE-01's clean host-integration row. | bash -c cd examples/accrue_host && mix format --check-formatted |
| 9 | 225 | deviation | fixed | proved | 0 | maintainer | The intentional change (an explicit, already-declared \`jose\` lock entry that lets the Admin Playwright web server start in a clean checkout) is confirmed present and stable under \`mix deps.get\` at the candidate SHA, satisfying D-22's fixed bar for a deviation row. | No release-blocking effect. The Admin Playwright web server continues to start cleanly from a fresh checkout at the candidate SHA. | accrue_admin/mix.lock at the candidate SHA still carries the explicit \`jose\` lock entry (\`"jose": {:hex, :jose, "1.11.12", ...}\`) at the same location the row recorded. Re-ran \`mix deps.get\` for \`accrue_admin\` in a fresh scratch clone at the candidate SHA and diffed mix.lock before/after: byte-identical, confirming the lock resolves cleanly with no drift. This is independently corroborated by GATE-01's clean admin-group-contracts, admin-hardening-guardrails, admin-phase200-guardrails, and admin-drift-docs rows, all of which depend on the admin Playwright web server starting successfully from this exact lock. | bash -c cd accrue_admin && MIX_ENV=test mix deps.get |

<!-- phase231-window-dispositions:end -->

---
phase: 232-bounded-hygiene-release-handoff
plan: 03
type: execute
wave: 3
depends_on: ["232-02"]
files_modified:
  - scripts/ci/verify_ci_script_contract.mjs
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/README.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [HYG-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new git-tracked scripts/ci/*.mjs file that omits the shared guard, or that registers no prose-named test, fails a merge-blocking CI step."
    - "verify_ci_script_contract.mjs cannot pass on a zero-match glob: it compares a live recount of git-tracked scripts/ci/*.mjs against a committed non-zero expected count and fails when the live count is below it."
    - "The ci_baseline triad passes node --test on all three files and is wired into docs-contracts-shift-left in the same change, so nobody wires a red triad by pattern-matching a neighbour."
    - "verify_ci_script_contract.mjs prints which strict checks actually ran, so a reader can tell a real verification from a schema-only one."
  artifacts:
    - scripts/ci/verify_ci_script_contract.mjs
    - .github/workflows/ci.yml
    - scripts/ci/README.md
  key_links:
    - "scripts/ci/verify_ci_script_contract.mjs -> every git-tracked scripts/ci/*.mjs (closed-cohort enforcement of the guard + non-vacuity contract)"
    - "ci_baseline triad -> docs-contracts-shift-left (D-34: guards fixed before or with the wiring)"
  prohibitions:
    - "Do not mark a CLI flag required unless it is wired into a real comparison."
    - "Do not let a completeness loop declare pass after inspecting zero items."
    - "Do not wire the ci_baseline triad into CI in a commit that leaves any of its three files failing node --test."
    - "Do not hardcode a permanent expected file count that is never compared against a live recount."
    - "Do not rewrite published history, delete a remote branch or tag, merge a pull request, or publish a package in this plan."
---

<objective>
Make the guard-and-non-vacuity contract from plans 232-01/02 self-enforcing, and defuse the
`ci_baseline` triad landmine in the same change.

Purpose: D-32/D-34. Without a meta-verifier, the 21st `scripts/ci/*.mjs` file mints a new broken idiom
and nothing goes red; exit-code-only enforcement would rubber-stamp every vacuous file. Separately the
`ci_baseline` triad is not wired into `ci.yml` and two of its three files fail `node --test`, so anyone
wiring it by copying a neighbouring triad's step turns CI red.

Output: `scripts/ci/verify_ci_script_contract.mjs`, a green and wired `ci_baseline` triad, and two new
merge-blocking steps in `docs-contracts-shift-left`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-bounded-hygiene-release-handoff/232-CONTEXT.md
@.planning/phases/232-bounded-hygiene-release-handoff/232-PATTERNS.md
@.planning/phases/232-bounded-hygiene-release-handoff/232-RESEARCH.md
@.planning/phases/232-bounded-hygiene-release-handoff/232-02-SUMMARY.md
</context>

## Artifacts this phase produces (plan 232-03 slice)

| Symbol | Kind | Where |
|---|---|---|
| `scripts/ci/verify_ci_script_contract.mjs` | new file | `scripts/ci/` |
| `--fixtures` | new CLI flag | `verify_ci_script_contract.mjs` |
| `--require-guard-coverage` | new CLI flag | `verify_ci_script_contract.mjs` |
| `--require-non-vacuity` | new CLI flag | `verify_ci_script_contract.mjs` |
| `--require-cohort-floor` | new CLI flag | `verify_ci_script_contract.mjs` |
| `--expected-repository` | new CLI flag | `verify_ci_script_contract.mjs` |
| `CI script contract (D-32)` | new `ci.yml` step name | `docs-contracts-shift-left` job |
| `CI baseline triad units and fixture contract (D-34)` | new `ci.yml` step name | `docs-contracts-shift-left` job |

<sequencing_landmines_honored>
- **D-34:** Task 2 fixes the `ci_baseline` triad's guards and Task 3 wires it; the wiring commit must not precede the fix. Both land in this plan, fix first.
</sequencing_landmines_honored>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the CI script contract meta-verifier with a floor it cannot glob past</name>
  <files>scripts/ci/verify_ci_script_contract.mjs</files>
  <read_first>
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-PATTERNS.md` section `### scripts/ci/verify_ci_script_contract.mjs (D-32 meta-verifier, new)` — names both analogs and enumerates the four required assertions
    - `scripts/ci/verify_gate01_cohort.mjs` — the closed-cohort completeness gate whose committed-expected-count discipline this file copies
    - `scripts/ci/verify_ci_baseline.mjs` — the existing child-process spawn helper for `node --test`, the concrete precedent for the spawn loop
    - `scripts/ci/verify_window_dispositions.mjs` — the `PASS (verified: ...)` / `PASS (schema-only: ...)` suffix block to copy verbatim
    - `scripts/ci/main_module.mjs` — the guard this verifier enforces
  </read_first>
  <behavior>
    - Given a cohort where every file exits 0 under a child test run and registers a prose-named case, the verifier exits 0 and names every strict check that ran.
    - Given a synthetic file that exits 0 but whose only test name equals its own path, the verifier fails with a message naming that file and the word vacuity.
    - Given a synthetic file with a CLI entrypoint that does not import the shared guard and is not a `*.test.mjs` file, the verifier fails naming that file.
    - Given a cohort whose live file count is below the committed floor, the verifier fails rather than passing on a short or empty glob.
    - Given no strict flags at all, the verifier prints the schema-only suffix and does not claim any check ran.
  </behavior>
  <action>
    Create `scripts/ci/verify_ci_script_contract.mjs` following the triad verifier shape. Accept
    `--fixtures`, `--expected-repository`, `--require-guard-coverage`, `--require-non-vacuity`, and
    `--require-cohort-floor`. Every one of those strict flags must be wired into a real comparison; a
    flag that is parsed and never compared is the exact WR-01 defect and must not be introduced.
    Enumerate the cohort as the git-tracked `scripts/ci/*.mjs` set (use `git ls-files`, not a raw
    directory read, so untracked scratch files cannot join or leave the cohort silently).
    Assertions:
    (1) `--require-non-vacuity`: for each cohort file spawn `node --test --test-reporter=tap <file>`,
    require exit 0, parse the TAP result lines, and require at least one whose test name is not the
    file path itself. Pass the reporter explicitly (D-33).
    (2) `--require-guard-coverage`: for each cohort file, require the source text to import
    `isMainModule` from the shared helper, OR the filename to match the `*.test.mjs` suffix, OR the file
    to be a library module with no CLI entrypoint — and make that third exemption an explicit,
    committed allowlist inside this file with a one-line reason per entry, not an inferred property.
    (3) `--require-cohort-floor`: compare the live cohort size against a committed integer floor stored
    in this file. Re-measure the current cohort size at execution time and set the floor to that live
    value; do not copy any count from CONTEXT.md, RESEARCH.md or PATTERNS.md, all of which D-00 marks as
    bindings to re-measure. Fail when the live count is below the floor. State in a comment that the
    floor exists so a zero-match or short glob cannot be reported as a pass.
    (4) Before declaring any completeness pass, assert a non-zero count of items actually inspected.
    `--fixtures` runs hermetic positive and negative controls in a temp directory: one conforming
    synthetic file, one vacuous synthetic file, one guard-less synthetic file, and one short-cohort
    case. Emit failures as `ci script contract: FAIL: <reason>` and successes with the
    `PASS (verified: ...)` / `PASS (schema-only: ...)` suffix naming exactly which strict flags ran.
    Guard the CLI entrypoint with `isMainModule(import.meta.url)`.
  </action>
  <verify>
    <automated>node --check scripts/ci/verify_ci_script_contract.mjs && node --test --test-reporter=tap scripts/ci/verify_ci_script_contract.mjs</automated>
    <fails_when>non-zero exit, a `# fail` count greater than 0, or a TAP summary line reading `# pass 0`</fails_when>
    <automated>node scripts/ci/verify_ci_script_contract.mjs --fixtures --expected-repository szTheory/accrue --require-guard-coverage --require-non-vacuity --require-cohort-floor</automated>
    <fails_when>non-zero exit, empty stdout, or a stdout `PASS` line whose suffix does not name all three of guard-coverage, non-vacuity and cohort-floor</fails_when>
    <automated>node scripts/ci/verify_ci_script_contract.mjs --expected-repository szTheory/accrue</automated>
    <fails_when>non-zero exit, or a stdout `PASS` line that does not carry the schema-only suffix — claiming a verified suffix with no strict flags supplied is the vacuity signature this verifier exists to catch</fails_when>
    <automated>node scripts/ci/verify_ci_script_contract.mjs --repo . --expected-repository szTheory/accrue --require-guard-coverage --require-non-vacuity --require-cohort-floor</automated>
    <fails_when>non-zero exit against the real repository cohort, or a `FAIL:` line — a failure here means plan 232-02's sweep left a gap</fails_when>
  </verify>
  <acceptance_criteria>
    - Every accepted strict flag appears in at least one comparison in the file; `grep -n -- '--require-' scripts/ci/verify_ci_script_contract.mjs` lines each have a corresponding assertion body.
    - The committed cohort floor is a non-zero integer set from a live recount performed during this task; the SUMMARY quotes the command and the number.
    - `--fixtures` exercises four negative controls (vacuous file, guard-less file, short cohort, non-zero exit file) and each produces a `ci script contract: FAIL:` message naming the offending file or count.
    - The real-repository invocation exits 0.
    - The schema-only invocation prints a suffix that names no strict check.
    - `grep -c 'git ls-files' scripts/ci/verify_ci_script_contract.mjs` is at least 1 (cohort comes from tracked files, not a directory read).
  </acceptance_criteria>
  <done>A future file that omits the shared guard or registers no real test fails a real, non-vacuous check, and the check itself cannot pass on an empty glob.</done>
</task>

<task type="auto">
  <name>Task 2: Bring the ci_baseline triad green before anything wires it</name>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - `scripts/ci/collect_ci_baseline.mjs`, `scripts/ci/render_ci_baseline.mjs`, `scripts/ci/verify_ci_baseline.mjs` — all three, including their entrypoint guards and their `node:test` blocks
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-CONTEXT.md` D-34 — why the fix must precede or accompany the wiring
    - `scripts/ci/collect_window_dispositions.mjs` — the reference triad whose shape the ci_baseline triad is expected to match
  </read_first>
  <action>
    Run `node --test --test-reporter=tap` against each of the three `ci_baseline` files and record the
    live pass/fail state before touching anything — D-27's claim that two of the three fail is a binding
    to re-measure, not a fact to assume. For each failing file, diagnose whether the failure is a guard
    defect (already addressed by plan 232-02's sweep), a missing test-mode branch, or a real assertion
    failure, and fix it so the file exits 0 under `node --test` with at least one prose-named case.
    Do not weaken or delete an assertion to get green: if a file fails because the behavior it asserts is
    genuinely wrong, fix the behavior; if it fails because the assertion is wrong, state the corrected
    expectation explicitly in the SUMMARY with the before and after values.
    Confirm each of the three files imports the shared guard from the helper module and that
    `node scripts/ci/verify_ci_script_contract.mjs --repo . --require-guard-coverage --require-non-vacuity`
    now covers them without a special case.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/collect_ci_baseline.mjs scripts/ci/render_ci_baseline.mjs scripts/ci/verify_ci_baseline.mjs</automated>
    <fails_when>non-zero exit, a `# fail` count greater than 0, or a `# pass 0` summary line</fails_when>
    <automated>node scripts/ci/verify_ci_script_contract.mjs --repo . --expected-repository szTheory/accrue --require-guard-coverage --require-non-vacuity --require-cohort-floor</automated>
    <fails_when>non-zero exit, or a `FAIL:` line naming any of the three ci_baseline files</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the pre-fix per-file `node --test` exit codes, re-measured in this task.
    - All three ci_baseline files exit 0 under `node --test --test-reporter=tap` with at least one prose-named test each.
    - All three appear in the meta-verifier's cohort with no allowlist exemption.
    - The diff contains no deleted or skipped test case; any corrected expectation is documented in the SUMMARY with before and after values.
  </acceptance_criteria>
  <done>The ci_baseline triad is green on its own merits, so wiring it cannot turn CI red.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the meta-verifier and the ci_baseline triad into the shift-left job</name>
  <files>.github/workflows/ci.yml, scripts/ci/README.md</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 190-230 — the existing consecutive-triad `run:` block shape inside `docs-contracts-shift-left`
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-PATTERNS.md` section `### CI wiring block shape`
    - `scripts/ci/README.md` — the per-phase section structure and the guard-convention section added by plan 232-01
  </read_first>
  <action>
    Add two steps to the `docs-contracts-shift-left` job, both after the existing
    `Window dispositions triad units and fixture contract` step:
    (1) `CI baseline triad units and fixture contract (D-34)` — a multi-line `run:` chaining
    `node --test --test-reporter=tap` over the three ci_baseline files followed by the triad's own
    `--fixtures` self-test invocation with `--expected-repository szTheory/accrue` and whatever strict
    flags that verifier already declares; re-derive its real flag names from the file, do not invent them.
    (2) `CI script contract (D-32)` — runs `node --test --test-reporter=tap scripts/ci/verify_ci_script_contract.mjs`
    followed by both the `--fixtures` self-test and the real-repository invocation with
    `--repo .`, `--expected-repository szTheory/accrue`, `--require-guard-coverage`,
    `--require-non-vacuity`, `--require-cohort-floor`. The real invocation is the point: a fixtures-only
    wiring would repeat the D-16 defect where a committed artifact is verified by nothing.
    Neither step may carry `continue-on-error`.
    In `scripts/ci/README.md`, add a `## Phase 232` section documenting both new steps, the meta-verifier's
    flags, and the cohort-floor rationale in one sentence.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); s=d['jobs']['docs-contracts-shift-left']['steps']; names=[x.get('name','') for x in s]; need=['CI baseline triad units and fixture contract (D-34)','CI script contract (D-32)']; [print('missing',n) or exit(1) for n in need if n not in names]; [exit(1) for n in need if 'continue-on-error' in s[names.index(n)]]; r=s[names.index('CI script contract (D-32)')]['run']; assert '--require-guard-coverage' in r and '--repo .' in r, r; print('ok')"</automated>
    <fails_when>non-zero exit, a stdout line beginning `missing`, an `AssertionError` traceback, or stdout that does not contain the line `ok`</fails_when>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('parsed')"</automated>
    <fails_when>non-zero exit, a YAML error traceback, or stdout missing the line `parsed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs-contracts-shift-left` contains steps named exactly `CI baseline triad units and fixture contract (D-34)` and `CI script contract (D-32)`.
    - The `CI script contract (D-32)` step's `run:` contains both a `--fixtures` invocation and a real `--repo .` invocation with all three strict flags.
    - Neither new step carries `continue-on-error`, and the job itself still carries none.
    - `scripts/ci/README.md` contains a `Phase 232` heading naming both steps and the cohort floor.
    - The workflow file parses as YAML.
  </acceptance_criteria>
  <done>Both the contract and the previously-unwired triad run as merge-blocking checks, and the contract runs against the real cohort rather than fixtures alone.</done>
</task>

</tasks>

<assumption_delta_decision>
Carries plan 232-01's `promote` decision. This plan supplies its enforcement: the invariant test that
goes red if a future file reintroduces the per-file inline guard is
`scripts/ci/verify_ci_script_contract.mjs --require-guard-coverage`, run against the real cohort in
`docs-contracts-shift-left`. No second invariant test is introduced.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-glob result -> completeness claim | A short or empty glob would otherwise let a completeness loop pass having inspected nothing. |
| child test process exit code -> contract verdict | Exit code alone does not distinguish "asserted and passed" from "asserted nothing". |
| workflow step definition -> required check | A fixtures-only wiring verifies synthetic inputs and leaves the real cohort unverified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-03-01 | Spoofing (of a safety property) | `--require-*` flags on the new meta-verifier | high | mitigate | Each strict flag is wired to a real comparison and asserted present by an acceptance criterion; the schema-only invocation proves the suffix does not over-claim. |
| T-232-03-02 | Tampering (with evidence) | cohort enumeration | high | mitigate | `--require-cohort-floor` compares a live `git ls-files` recount against a committed non-zero floor; a zero-match glob fails instead of passing. |
| T-232-03-03 | Tampering (with evidence) | CI wiring | high | mitigate | The `CI script contract (D-32)` step runs the real-repository invocation, not only `--fixtures`, closing the D-16 class of "documented but never executed" verification. |
| T-232-03-04 | Information Disclosure | fixtures temp directories | low | mitigate | Fixtures are created under the process temp directory and removed; no record field carries a local path, so no new sanitization regex is minted. |
</threat_model>

<verification>
- `node scripts/ci/verify_ci_script_contract.mjs --repo . --expected-repository szTheory/accrue --require-guard-coverage --require-non-vacuity --require-cohort-floor` exits 0 against the real cohort.
- `node --test --test-reporter=tap` over the three ci_baseline files exits 0.
- The two new `docs-contracts-shift-left` steps exist, carry no `continue-on-error`, and the workflow parses.
</verification>

<success_criteria>
- The guard-and-non-vacuity contract is enforced by a merge-blocking check that runs against the real cohort.
- The cohort floor is a live-measured, committed, non-zero integer compared against a live recount.
- The ci_baseline triad is green and wired in the same plan, fix before wiring.
</success_criteria>

<output>
Create `.planning/phases/232-bounded-hygiene-release-handoff/232-03-SUMMARY.md` when done, with
`human_judgment: false`, the live cohort count and the command that produced it, and the pre-fix
ci_baseline exit codes.
</output>

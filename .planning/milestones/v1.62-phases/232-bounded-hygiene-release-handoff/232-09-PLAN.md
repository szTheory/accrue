---
phase: 232-bounded-hygiene-release-handoff
plan: 09
type: execute
wave: 8
depends_on: ["232-08"]
files_modified:
  - scripts/ci/verify_recut_candidate.mjs
  - .planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json
autonomous: false
requirements: [REL-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The re-cut candidate contains both lines: the published release state reachable from remote main is an ancestor of it, and the milestone line's work is present, with the manifest and all package version attributes reading the published release version rather than the older milestone value."
    - "Losslessness is proved by blob identity, not by a diffstat: for every path changed by exactly one side against the merge base, the blob in the re-cut equals that side's blob, and the count of drifted paths is zero."
    - "For a path changed by both sides, whole-blob identity to either parent is structurally impossible, so the check asserts instead that every hunk each side introduced is present in the result; the carve-out is derived from a live co-touched-path computation, not from a hardcoded filename."
    - "The commit count of the re-cut relative to both parents equals the declared number of new commits, so a duplicated-commit rebase is detected rather than assumed absent."
    - "Local rollback refs for both pre-re-cut tips exist before any ref write, and the rollback command is a single recorded invocation."
    - "The re-cut has exactly one merge point, so the existing shape contract is satisfied rather than tripped by a second merge."
  artifacts:
    - .planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json
    - scripts/ci/verify_recut_candidate.mjs
  key_links:
    - "local rollback refs -> every subsequent ref write in this plan (created first, never pushed)"
    - "verify_recut_candidate.mjs strict flags -> the new candidate SHA (re-run unchanged in shape)"
    - "232-ROLLBACK-POINT.json -> the integration pull request body (plan 232-11 quotes its restore invocation)"
  prohibitions:
    - "Do not rebase either line; the candidate branch is already published and rebasing rewrites public history."
    - "Do not advance the candidate by merging the milestone tip onto it; a second merge point fails the shape contract."
    - "Do not write any ref before both local rollback refs exist."
    - "Do not push a local rollback ref."
    - "Do not force-push, move a tag, delete a remote branch, merge a pull request, or publish a package."
    - "Do not hardcode a co-touched path by name in the verifier; derive the set live."
    - "Do not reuse any file count, SHA, or drift count from a planning document as an assertion constant."
    - "Do not re-cut from the milestone line alone: per D-02 the published release state is an ancestor of the candidate and not of the milestone tip, so that would regress two published releases."
---

<objective>
Re-cut `integration/v1.62-candidate` as a clean union of the two divergent lines, prove the union
lossless by blob identity with a generically-derived carve-out for co-touched paths, and record a
one-command rollback point before any ref moves.

Purpose: D-02 through D-08, and the drift finding that the recorded zero-co-touched-files premise is no
longer true. Re-cutting from the milestone line alone would regress two published releases; a diffstat
or a green test suite would miss a silent revert; and a hardcoded single-path exception would be wrong
the next time the branch moves.

Output: a new candidate SHA, a blob-identity proof printing zero drifted paths, and a committed
rollback point.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-bounded-hygiene-release-handoff/232-CONTEXT.md
@.planning/phases/232-bounded-hygiene-release-handoff/232-RESEARCH.md
@.planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json
@.planning/STATE.md
</context>

## Artifacts this phase produces (plan 232-09 slice)

| Symbol | Kind | Where |
|---|---|---|
| `232-ROLLBACK-POINT.json` | new artifact | phase directory |
| `--require-union-hunks` | new CLI flag | `verify_recut_candidate.mjs` |
| `rollback/232-pre-recut-milestone` | new local ref | local only, never pushed |
| `rollback/232-pre-recut-candidate` | new local ref | local only, never pushed |

<sequencing_landmines_honored>
- **D-06:** both local rollback refs exist BEFORE any ref write. Task 1 creates them and asserts their existence; no later task may write a ref until that assertion has passed.
- **D-05:** exactly one merge point. Advancing by merging the milestone tip onto the candidate is explicitly rejected by the existing shape contract.

</sequencing_landmines_honored>

<tasks>

<task type="auto">
  <name>Task 1: Rollback refs and a live re-measurement of the two lines, before any ref write</name>
  <files>.planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json</files>
  <read_first>
    - `.planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json` — the recorded shape this artifact mirrors, including its restore invocation field
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-CONTEXT.md` D-01 through D-08
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-RESEARCH.md` section `### DRIFT-1` and `### Pitfall 1` — why every number here must be re-derived
    - `.planning/STATE.md` — the phase 230 outcome describing the candidate's existing shape
  </read_first>
  <action>
    Create the two local rollback refs first, one at the current milestone tip and one at the current
    candidate tip, and assert both resolve before doing anything else. They are local only and must never
    be pushed.
    Then re-measure the union from scratch. Do not reuse any SHA, file count, or drift count from
    CONTEXT.md or RESEARCH.md — every one of them is explicitly a binding to re-measure, and the branch
    moved six commits between context gathering and research alone. Derive live: the merge base of the two
    lines; the set of paths each side changed against that base; their intersection, which is the
    co-touched set; whether the published release state reachable from remote main is an ancestor of the
    candidate and not of the milestone tip; and the manifest and package version attribute values on each
    side.
    Perform a write-tree merge probe and confirm it resolves cleanly before touching a ref. If it does
    not, stop and report rather than resolving conflicts by hand.
    Write `232-ROLLBACK-POINT.json` recording: both rollback ref names and the SHAs they point at, the
    merge base, both pre-re-cut tips, the live co-touched path set, and a single restore invocation that
    returns the repository to its pre-re-cut state. Run a leak sweep over the artifact before committing.
  </action>
  <verify>
    <automated>git rev-parse --verify rollback/232-pre-recut-milestone && git rev-parse --verify rollback/232-pre-recut-candidate</automated>
    <fails_when>non-zero exit from either resolution — a rollback ref is missing and no ref write may proceed</fails_when>
    <automated>git ls-remote --heads origin 'rollback/*' | wc -l</automated>
    <fails_when>a printed count greater than 0 — a rollback ref was pushed; these are local only</fails_when>
    <automated>python3 -c "import json; d=json.load(open('.planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json')); [d[k] for k in ('merge_base','restore_argv','co_touched_paths')]; assert isinstance(d['co_touched_paths'], list); print('rollback point ok: co_touched=%d' % len(d['co_touched_paths']))"</automated>
    <fails_when>non-zero exit, a `KeyError` naming a missing field, or stdout missing a line beginning `rollback point ok`</fails_when>
    <automated>grep -nE '/Users/|/home/|\$HOME' .planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json; test $? -eq 1</automated>
    <fails_when>exit 0 from the test, meaning grep matched — a local filesystem path leaked into a committed artifact</fails_when>
  </verify>
  <acceptance_criteria>
    - D-06: both local rollback refs are created BEFORE any ref write, resolve locally, and neither exists on the remote; no later task writes a ref until this assertion has passed.
    - Both rollback refs resolve locally and neither exists on the remote.
    - The rollback artifact records both tips, the merge base, the live co-touched path set, and a single restore invocation.
    - The SUMMARY quotes the live command and output for each re-measured fact: merge base, per-side changed-path counts, the co-touched set, the ancestry answers, and the version values on each side.
    - The write-tree merge probe exited zero and its resulting tree object is recorded.
    - The leak sweep over the artifact matches nothing.
  </acceptance_criteria>
  <done>Rollback exists before anything can need it, and every fact the re-cut depends on is freshly measured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the losslessness proof to handle co-touched paths by hunk union</name>
  <files>scripts/ci/verify_recut_candidate.mjs</files>
  <read_first>
    - `scripts/ci/verify_recut_candidate.mjs` — its existing strict flags, its shape and ancestry checks, and its fixtures battery
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-RESEARCH.md` section `### Pitfall 2` and `## Open Questions` — the recommendation to implement the carve-out generically
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-CONTEXT.md` D-07, D-08
    - `.planning/phases/231-exact-sha-release-gate-proof/231-REVIEW.md` WR-01 — the parsed-but-unused required flag defect this new flag must not reproduce
  </read_first>
  <behavior>
    - For a path changed by exactly one side, a result blob differing from that side's blob is reported as drifted, naming the path.
    - For a path changed by both sides, whole-blob equality to either parent is not required; instead every added line each side introduced against the merge base must be present in the result, and a missing one is reported as drifted, naming the path and the missing hunk.
    - The co-touched set is computed live from the two changed-path sets; no filename is hardcoded.
    - A run in which zero paths were inspected fails rather than passing.
    - A synthetic silent revert — same file count, different content — is detected.
  </behavior>
  <action>
    Add a strict flag to `scripts/ci/verify_recut_candidate.mjs` that performs the losslessness sweep, and
    wire it into a real comparison; a flag that is parsed and never compared is the exact recorded defect
    shape to avoid.
    Compute the two changed-path sets against the merge base, then partition: single-touched paths are
    checked by blob identity against the side that changed them; co-touched paths are checked by hunk
    union — every line each side added against the merge base must appear in the result. Derive the
    partition live; do not name any path as an exception in the source. Document the rule in a comment as
    a named, general exception with its reason, not as a special case for one file.
    Assert a non-zero count of inspected paths before declaring pass. Print the drifted count explicitly so
    the proof is a number a reviewer can read, and name every drifted path.
    Add fixtures covering all five behaviors, including a silent-revert negative control built as same
    file count with different content — that is precisely the class blob identity exists to catch, and it
    is the same shape as the two tamper-named local branches the hygiene record flags.
  </action>
  <verify>
    <automated>node --check scripts/ci/verify_recut_candidate.mjs && node --test --test-reporter=tap scripts/ci/verify_recut_candidate.mjs</automated>
    <fails_when>non-zero exit, a `# fail` count greater than 0, or no `ok` line whose name mentions the co-touched hunk-union rule</fails_when>
    <automated>node scripts/ci/verify_recut_candidate.mjs --fixtures --expected-repository szTheory/accrue</automated>
    <fails_when>non-zero exit, empty stdout, or a `PASS` line whose suffix does not name the new losslessness check among the checks that ran</fails_when>
    <automated>grep -c 'accrue_portal/mix.exs' scripts/ci/verify_recut_candidate.mjs</automated>
    <fails_when>a printed count greater than 0 — a co-touched path was hardcoded by name instead of derived live, which is wrong the next time the branch moves</fails_when>
  </verify>
  <acceptance_criteria>
    - The new strict flag is wired into a real comparison and named in the pass suffix when supplied.
    - The co-touched partition is computed from live changed-path sets; no source file path appears as a literal exception.
    - Five fixtures exist including a same-file-count-different-content silent-revert control; each produces a distinct failure message.
    - A sweep over zero inspected paths exits non-zero.
    - The verifier prints an explicit drifted count.
  </acceptance_criteria>
  <done>Losslessness is provable for a union that is no longer disjoint, without a filename baked into the verifier.</done>
</task>

<task type="auto">
  <name>Task 3: Perform the re-cut and prove it</name>
  <files>.planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json</files>
  <read_first>
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json` — the measured tips, merge base and co-touched set from Task 1
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-CONTEXT.md` D-03, D-04, D-05, D-08
    - `scripts/ci/verify_recut_candidate.mjs` — the strict flags to run unchanged in shape against the new SHA
    - `.planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md` — the sanctioned post-merge-commit pattern the shape contract enforces
  </read_first>
  <action>
    Re-cut by merging the published release line into the milestone tip with the milestone line as first
    parent, then re-applying the two declared post-merge commits. This reproduces exactly the shape the
    recorded rollback point describes and the existing shape contract already enforces.
    Do not rebase either line. Do not advance by merging the milestone tip onto the candidate branch — the
    shape contract explicitly rejects a second merge point, and doing so is a self-inflicted gate failure.
    Run three corroborating fail-closed assertions and record each command and output: the published
    release state is an ancestor of the new candidate; the manifest and every package version attribute
    read the published release version; and the commit count of the new candidate relative to both
    pre-re-cut tips equals the declared number of new commits, which catches duplicated commits from an
    accidental rebase.
    Then run the full strict verification against the new SHA, including the new losslessness flag, and
    record that the drifted count printed zero. Update the rollback artifact with the new candidate SHA and
    commit it.
    If any assertion fails, restore from the recorded rollback invocation and report rather than
    improvising a fix.
  </action>
  <verify>
    <automated>node scripts/ci/verify_recut_candidate.mjs --repo . --record .planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json --expected-repository szTheory/accrue --require-shape --require-ancestry --require-revert-proof --require-toolchain --require-supersession</automated>
    <fails_when>non-zero exit, any `FAIL:` line, or a printed drifted count other than 0</fails_when>
    <automated>git log --merges --format=%H "$(python3 -c "import json;print(json.load(open('.planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json'))['merge_base'])")"..HEAD | wc -l</automated>
    <fails_when>a printed count other than 1 — more than one merge point means the candidate was advanced by a second merge, which the shape contract rejects</fails_when>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh</automated>
    <fails_when>non-zero exit, or any stderr line beginning with the script-name prefix — the re-cut did not carry the published release version into the manifest and the package version attributes</fails_when>
    <automated>git rev-parse --verify rollback/232-pre-recut-milestone && git rev-parse --verify rollback/232-pre-recut-candidate</automated>
    <fails_when>non-zero exit from either — a rollback ref was destroyed by the re-cut and the recorded restore invocation is no longer executable</fails_when>
  </verify>
  <acceptance_criteria>
    - The new candidate has exactly one merge point.
    - All five existing strict checks pass at the new SHA and the losslessness sweep prints a drifted count of zero; the SUMMARY quotes that line verbatim.
    - The three corroborating assertions are each recorded with their command and output: ancestry of the published release line, version values, and the relative commit count matching the declared number.
    - Manifest alignment exits zero at the new SHA.
    - Both rollback refs still resolve, and neither exists on the remote.
    - The rollback artifact records the new candidate SHA.
  </acceptance_criteria>
  <done>A single re-cut SHA carries both lines with a zero-drift blob-identity proof and a one-command rollback.</done>
</task>

<task type="checkpoint:decision">
  <name>Checkpoint: authorize the ref write for the re-cut candidate</name>
  <read_first>
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-ROLLBACK-POINT.json`
    - `.planning/phases/232-bounded-hygiene-release-handoff/232-09-SUMMARY.md` draft, if present
  </read_first>
  <action>
    Moving the published candidate branch to the new SHA is a ref write on a branch that is already on the
    remote. Present, before writing: the new SHA, the drifted count, the one-merge-point proof, the three
    corroborating assertion outputs, both rollback refs and the single restore invocation. Ask the
    maintainer to authorize updating the published candidate branch to the new SHA.
    Do not force-push. Do not delete or move any other ref. If the update is not a fast-forward, stop and
    present that fact rather than forcing it.
  </action>
  <decision>
    Authorize updating the published `integration/v1.62-candidate` branch on the remote to the new re-cut SHA.
  </decision>
  <options>
    - **A (default):** Authorize the update. The branch moves to the new SHA; both local rollback refs and the single restore invocation remain the undo path.
    - **B:** Do not update the published branch. Keep the re-cut local only and open the integration pull request from a differently-named head branch instead; record the deviation.
    - **C:** Halt the phase here and re-examine the re-cut evidence before any ref moves.
  </options>
  <resume-signal>
    The maintainer replies with A, B, or C (or equivalent prose). Record the reply verbatim in the SUMMARY
    before performing any remote ref update, then continue.
  </resume-signal>
  <acceptance_criteria>
    - The maintainer's authorization is quoted verbatim in the SUMMARY before any remote ref update.
    - The remote candidate branch resolves to the new SHA after the update, and no other remote ref changed; both are quoted with their commands.
    - No force-push was used.
  </acceptance_criteria>
  <done>The one genuinely irreversible-adjacent action in this plan is taken with explicit authorization and a working rollback behind it.</done>
</task>

</tasks>

<assumption_delta_decision>
**Noun now primary:** the co-touched-path carve-out in the losslessness proof.

**Decision:** `promote`.

**Rationale:** the recorded premise was zero co-touched paths; research found one, and the branch is
still moving. A named single-path exception would be a singular assumption that the next branch
movement invalidates silently — the classic case. The general form (any path present in both changed-path
sets is checked by hunk union) becomes primary and the single-path case is simply an instance of it.
A grep asserting no source filename appears as a literal in the verifier is the invariant that goes red
if someone reintroduces the singular form.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two divergent commit lines -> one merged tree | A merge can silently drop one side's content while every count and test stays green. |
| local repository -> published remote ref | A ref write on an already-published branch is visible to everyone and not freely undoable. |
| verifier flag -> safety claim | A required flag that performs no comparison makes the proof decorative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-09-01 | Tampering (with content) | silent revert in the merged tree | high | mitigate | Blob identity for single-touched paths plus hunk union for co-touched paths, with a same-file-count-different-content negative control and an explicit drifted count printed. |
| T-232-09-02 | Spoofing (of a safety property) | the new losslessness flag | high | mitigate | The flag is wired into a real comparison, named in the pass suffix, and a zero-inspected-path run fails. |
| T-232-09-03 | Elevation of Privilege | ref write on a published branch | high | mitigate | Rollback refs created before any ref write and asserted present afterwards; the remote update is gated behind an explicit maintainer authorization checkpoint; force-push is prohibited. |
| T-232-09-04 | Tampering (with history) | accidental rebase duplicating commits | high | mitigate | The relative commit count assertion against both pre-re-cut tips detects duplication, and a one-merge-point check detects a second merge. |
| T-232-09-05 | Information Disclosure | committed rollback artifact | medium | mitigate | Leak sweep for local path prefixes runs over the artifact before commit. |
</threat_model>

<verification>
- Strict verification at the new SHA exits 0 with a drifted count of zero.
- Exactly one merge point between the merge base and the new tip.
- Manifest alignment exits 0 at the new SHA.
- Both rollback refs resolve locally and neither exists on the remote.
- Maintainer authorization is quoted before the remote ref update.
</verification>

<success_criteria>
- One re-cut SHA carries both lines without regressing the published release state.
- Losslessness is proved by blob identity with a generically-derived co-touched carve-out, printing zero drift.
- Rollback is one recorded invocation, created before any ref moved.
</success_criteria>

<output>
Create `.planning/phases/232-bounded-hygiene-release-handoff/232-09-SUMMARY.md` when done, with
`human_judgment: false` for every machine-verifiable claim, every re-measured fact quoted with its
command, the zero-drift line verbatim, and the maintainer authorization quote.
</output>


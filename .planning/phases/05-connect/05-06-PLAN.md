---
phase: 05-connect
plan: 06
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - accrue/lib/accrue/webhook/connect_handler.ex
  - accrue/lib/accrue/telemetry.ex
  - accrue/test/accrue/webhook/connect_handler_test.exs
autonomous: true
requirements: [CONN-03, CONN-10]
must_haves:
  truths:
    - "Accrue.Webhook.ConnectHandler has handle_event/3 clauses for account.updated, account.application.authorized, account.application.deauthorized, capability.updated, payout.created, payout.paid, payout.failed, person.created, person.updated"
    - "account.updated reducer uses load-or-fetch pattern: Repo.get_by || Connect.retrieve_account seed"
    - "account.updated uses force_status_changeset/2 (D3-17) and runs inside Repo.transact with Events.record_multi"
    - "account.application.deauthorized tombstones deauthorized_at without hard-deleting the row"
    - "account.application.deauthorized emits [:accrue, :ops, :connect_account_deauthorized] telemetry"
    - "capability.updated merges capability changes into the capabilities jsonb"
    - "payout.* reducers write accrue_events rows only (no dedicated schema in v1.0)"
    - "person.* reducers ack without local mutation (Custom accounts only; deferred)"
    - "Out-of-order account.updated (arriving before local create) seeds row via retrieve_account"
  artifacts:
    - path: "accrue/lib/accrue/webhook/connect_handler.ex"
      min_lines: 120
      contains: "account.updated"
  key_links:
    - from: "accrue/lib/accrue/webhook/connect_handler.ex"
      to: "accrue/lib/accrue/connect/account.ex"
      via: "force_status_changeset/2"
    - from: "accrue/lib/accrue/webhook/connect_handler.ex"
      to: "accrue/lib/accrue/connect.ex"
      via: "Accrue.Connect.retrieve_account/2 for out-of-order seed"
    - from: "accrue/lib/accrue/webhook/dispatch_worker.ex"
      to: "accrue/lib/accrue/webhook/connect_handler.ex"
      via: "case row.endpoint do :connect -> ConnectHandler (from Plan 01)"
---

<objective>
Wave 2: Replace the Plan 01 ConnectHandler stub with the full D5-05 reducer set. Wire account lifecycle webhook events to the local projection via `force_status_changeset/2`, handle out-of-order delivery (Pitfall 3), tombstone deauthorization, and thread Connect-specific events into the ledger.

Purpose: CONN-03 (account status sync from webhooks) + CONN-10 (per-endpoint routing — already done by Plan 01, now with a real handler). The dispatcher branching was landed in Plan 01; Plan 06 lands the actual reducer logic.

Output: Full ConnectHandler module + exhaustive test suite covering VALIDATION rows 8, 9 + all reducer clauses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-connect/05-CONTEXT.md
@.planning/phases/05-connect/05-RESEARCH.md
@.planning/phases/05-connect/05-PATTERNS.md

<interfaces>
From accrue/lib/accrue/webhook/default_handler.ex (Phase 3 reducer shape — authoritative analog):
```elixir
use Accrue.Webhook.Handler
require Logger
alias Accrue.{Events, Processor, Repo}

def handle_event("customer.subscription.updated", %Accrue.Webhook.Event{} = event, ctx) do
  # 1. Load or fetch
  # 2. force_status_changeset
  # 3. Repo.transact wrapping Repo.update + Events.record_multi
  # 4. Emit telemetry span
end

def handle_event(_type, _event, _ctx), do: :ok
```

From accrue/lib/accrue/webhook/handler.ex (Phase 2 behaviour):
- `@callback handle_event(type :: String.t(), event :: %Accrue.Webhook.Event{}, ctx :: map()) :: :ok | {:error, term}`
- `use Accrue.Webhook.Handler` imports defaults

From accrue/lib/accrue/connect/account.ex (from Plan 02):
- `Account.force_status_changeset(account, attrs)` — webhook path changeset
- `Account.changeset(account, attrs)` — user path
- `Accrue.Connect.retrieve_account/2` — for out-of-order seed

From PATTERNS.md Pattern 5 (D5-05 reducer shape):
```elixir
def handle_event("account.updated", event, ctx) do
  payload = event["data"]["object"]
  acct_id = payload["id"]

  Repo.transact(fn ->
    account =
      case Repo.get_by(Account, stripe_account_id: acct_id) do
        nil ->
          {:ok, fresh} = Connect.retrieve_account(acct_id)
          fresh
        existing -> existing
      end

    account
    |> Account.force_status_changeset(payload)
    |> Repo.update!()
    |> tap(fn updated ->
      Events.record_multi(:connect_account_updated, %{
        subject_type: "connect_account",
        subject_id: updated.id,
        data: payload
      })
    end)
  end)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full ConnectHandler reducers + out-of-order seeding + ops telemetry</name>
  <read_first>
    - accrue/lib/accrue/webhook/connect_handler.ex (stub from Plan 01)
    - accrue/lib/accrue/webhook/default_handler.ex (full reducer shapes — read especially subscription reducers for load-or-fetch pattern)
    - accrue/lib/accrue/webhook/handler.ex (behaviour definition)
    - accrue/lib/accrue/connect/account.ex (from Plan 02)
    - accrue/lib/accrue/connect.ex (retrieve_account/2 from Plan 02)
    - accrue/lib/accrue/events.ex (record_multi/2 signature)
    - accrue/lib/accrue/telemetry/ops.ex (Phase 4 ops telemetry emit helper — pattern for :connect_account_deauthorized)
    - accrue/test/support/stripe_fixtures.ex (Connect event fixtures added in Plan 02)
    - accrue/test/accrue/webhook/default_handler_test.exs (reducer test shape)
  </read_first>
  <behavior>
    - `handle_event("account.updated", event, ctx)`: load-or-fetch account by stripe_account_id → `force_status_changeset` → `Repo.update!` inside `Repo.transact` → `Events.record_multi(:connect_account_updated, ...)` → emit `[:accrue, :connect, :account_updated]` telemetry span
    - Out-of-order case: if `Repo.get_by` returns nil, call `Accrue.Connect.retrieve_account(acct_id)` to seed the local row via its persistence path, then apply force_status_changeset on top (idempotent — force_status applied to a just-fetched row is a no-op in terms of observable state)
    - `handle_event("account.application.authorized", event, ctx)`: upsert row if missing, stamp `authorized_at` (or synthesize from `:data` jsonb metadata — schema field is OPTIONAL since CONTEXT.md says "stamp authorized_at" but Plan 02 schema does NOT include an authorized_at column — decision: fold into `:data` jsonb since D5-02 schema is already locked; do not add a column mid-phase)
    - `handle_event("account.application.deauthorized", event, ctx)`:
      - Find row by stripe_account_id (may be nil if app was authorized before Accrue was installed — in that case, upsert via Connect.retrieve_account then proceed)
      - `force_status_changeset(%{deauthorized_at: DateTime.utc_now()})`
      - DO NOT `Repo.delete` — tombstone only (audit trail)
      - Events.record_multi `:connect_account_deauthorized`
      - Emit ops telemetry: `Accrue.Telemetry.Ops.emit(:connect_account_deauthorized, %{stripe_account_id: acct_id}, %{deauthorized_at: ts})` — follow Phase 4 OBS-03 pattern
    - `handle_event("capability.updated", event, ctx)`:
      - Extract capability name + status from payload
      - Load account row (load-or-fetch)
      - Merge into `capabilities` jsonb: `Map.put(account.capabilities, capability_name, capability_status)`
      - If capability key is NEW (not in existing map), refetch full account via `retrieve_account/2` to pick up related side effects
      - `force_status_changeset` + Repo.update + Events.record_multi `:connect_capability_updated`
      - If capability transitioned from "active" to "inactive", emit ops telemetry `:connect_capability_lost`
    - `handle_event("payout.created" | "payout.paid", event, ctx)`:
      - No local mutation; `Events.record/1` only with type `:connect_payout_<action>`, subject_type "connect_account", subject_id resolved via `Repo.get_by(Account, stripe_account_id: acct_id).id`
    - `handle_event("payout.failed", event, ctx)`:
      - Same as above + emit ops telemetry `:connect_payout_failed`
    - `handle_event("person.created" | "person.updated", _event, _ctx)`:
      - `Logger.debug("ConnectHandler: person.#{action} — passthrough (Custom-type scope, deferred to v1.x)")` + `:ok`
    - Catch-all: `handle_event(_type, _event, _ctx), do: :ok` (NOT a crash — Connect events should always ack)
    - Every reducer that fetches from Processor does so via `Accrue.Connect.*` facade (NOT directly via Processor) to ensure the local row side-effects fire
    - Idempotency: reducer body is safe to re-invoke (Phase 2 webhook_events unique index short-circuits at the ingest layer)
  </behavior>
  <action>
    1. Replace stub `accrue/lib/accrue/webhook/connect_handler.ex` with full module:
       - `@moduledoc` referencing D5-05 + CONN-03 + CONN-10 + Pitfall 3
       - `use Accrue.Webhook.Handler`
       - `require Logger`
       - Alias `Accrue.{Connect, Events, Repo}; alias Accrue.Connect.Account; alias Accrue.Telemetry.Ops`
       - Each clause wrapped in `:telemetry.span([:accrue, :connect, :<event_name>], %{}, fn -> ... end)`
       - Private helper `load_or_fetch_account(acct_id)` implementing the load-or-fetch pattern
       - Private helper `payload_object(event)` extracting `event.data["object"]` robustly (handle both raw map and `%Accrue.Webhook.Event{}` shapes — see default_handler.ex pattern)
    2. If `Accrue.Telemetry.Ops.emit/3` signature requires a specific shape (check against Phase 4 Plan 08 STATE.md note about `Accrue.Actor.current_operation_id/0`), use it verbatim. Otherwise add `:connect_account_deauthorized`, `:connect_capability_lost`, `:connect_payout_failed` to the canonical ops event enum.
    3. Create `accrue/test/accrue/webhook/connect_handler_test.exs`:
       - `use Accrue.ConnectCase, async: false` (telemetry handlers are process-global — async false per Phase 4 pattern)
       - Test: `account.updated` with existing row updates charges_enabled / details_submitted / payouts_enabled (VALIDATION row 8)
       - Test: `account.updated` with MISSING row calls `Connect.retrieve_account/2` and seeds (VALIDATION row 9, Pitfall 3)
       - Test: `account.application.deauthorized` stamps `deauthorized_at` and DOES NOT delete row
       - Test: `account.application.deauthorized` emits `[:accrue, :ops, :connect_account_deauthorized]` telemetry (attach handler, assert received)
       - Test: `capability.updated` merges new key into capabilities jsonb
       - Test: `capability.updated` capability lost → emits `[:accrue, :ops, :connect_capability_lost]`
       - Test: `payout.created` writes `accrue_events` row without mutating account
       - Test: `payout.failed` emits `[:accrue, :ops, :connect_payout_failed]`
       - Test: `person.created` no-ops (no DB mutation, no events row)
       - Test: every reducer wraps in Repo.transact (induce rollback, assert account NOT updated AND events row NOT created — EVT-04 invariant)
       - Test: unknown Connect event type (e.g. "account.external_account.created") returns `:ok` without crashing (catch-all)
       - Test: integration path — POST signed `account.updated` payload at `/webhooks/stripe/connect` → persisted row has `endpoint: :connect` → dispatched to ConnectHandler → local account row updated (proves Plan 01 + Plan 06 chain works end-to-end)
  </action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/webhook/connect_handler_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'defmodule Accrue.Webhook.ConnectHandler' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q '"account.updated"' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q '"account.application.deauthorized"' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q '"capability.updated"' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q '"payout.failed"' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q 'force_status_changeset' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q 'Connect.retrieve_account' accrue/lib/accrue/webhook/connect_handler.ex`
    - `grep -q ':connect_account_deauthorized' accrue/lib/accrue/webhook/connect_handler.ex`
    - `wc -l accrue/lib/accrue/webhook/connect_handler.ex | awk '{exit ($1 >= 120) ? 0 : 1}'` passes (min 120 lines)
    - `cd accrue && mix test test/accrue/webhook/connect_handler_test.exs` exits 0
    - VALIDATION.md rows 8, 9 pass
  </acceptance_criteria>
  <done>CONN-03 shipped end-to-end: Connect-endpoint events route through full reducer set with tombstoning, out-of-order seeding, and ops telemetry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe webhook payload → ConnectHandler reducer | Untrusted payload; must route through force_status_changeset (not user-path changeset) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | account.updated payload fields | mitigate | `force_status_changeset/2` casts only whitelisted status fields (`:charges_enabled`, `:details_submitted`, `:payouts_enabled`, `:capabilities`, `:requirements`, `:data`, `:deauthorized_at`). No cast on `:type`, `:owner_*`, `:stripe_account_id`. |
| T-05-06-02 | Repudiation | account.application.deauthorized with no local row | mitigate | Tombstone via `deauthorized_at` column never hard-deletes; event ledger records full deauthorization event for audit. |
| T-05-06-03 | Denial of Service | Out-of-order Connect.retrieve_account storm | accept | Rate limiting is lattice_stripe's concern; ConnectHandler does one retrieve per webhook. Unbounded retrieves would only happen with a broken webhook storm, which Phase 2 DLQ bounds. |
</threat_model>

<verification>
- VALIDATION rows 8, 9 pass
- End-to-end integration test proves Plan 01 dispatch branching → Plan 06 reducer chain
- Events ledger rollback test proves EVT-04 invariant
</verification>

<success_criteria>
- CONN-03 success criterion: "Every processor call taking a connected-account context threads Stripe-Account" — Plan 06 doesn't touch this directly but proves account state syncs from webhooks correctly
- Out-of-order delivery handled gracefully (Pitfall 3)
- Deauthorization tombstones, never deletes
</success_criteria>

<output>
After completion, create `.planning/phases/05-connect/05-06-SUMMARY.md`
</output>

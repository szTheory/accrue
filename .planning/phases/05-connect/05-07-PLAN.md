---
phase: 05-connect
plan: 07
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - accrue/test/accrue/connect/dual_scope_test.exs
  - accrue/test/live_stripe/connect_test.exs
  - accrue/guides/connect.md
  - accrue/lib/accrue/application.ex
autonomous: false
requirements: [PROC-05, CONN-01, CONN-02, CONN-03, CONN-04, CONN-05, CONN-06, CONN-07, CONN-08, CONN-09, CONN-10, CONN-11]
must_haves:
  truths:
    - "CONN-11 dual-scope integration test: same Accrue.Billing.subscribe/3 call succeeds platform-scoped AND inside with_account(acct, ...)"
    - "live_stripe/connect_test.exs exercises all 11 CONN-* requirements against Stripe test mode with @tag :live_stripe"
    - "guides/connect.md documents onboarding, charges, fee math, webhook config, platform fee per-account override recipe"
    - "Application boot warns if :platform and :connect webhook secrets are byte-identical (Pitfall 5)"
    - "nyquist_compliant: true set in 05-VALIDATION.md frontmatter after all 28 rows map to tasks"
  artifacts:
    - path: "accrue/test/accrue/connect/dual_scope_test.exs"
      provides: "CONN-11 cross-scope integration coverage"
    - path: "accrue/test/live_stripe/connect_test.exs"
      provides: "Live Stripe test-mode smoke for all CONN-* requirements"
    - path: "accrue/guides/connect.md"
      min_lines: 150
  key_links:
    - from: "accrue/lib/accrue/application.ex"
      to: "Accrue.Config webhook_endpoints"
      via: "boot-time byte-equality check (Pitfall 5)"
---

<objective>
Wave 3: Close Phase 5 with integration coverage across the full Connect surface — dual-scope tests proving CONN-11, a live_stripe smoke suite exercising all 11 CONN-* requirements against Stripe test mode, developer guide, and a boot-time warning for the Pitfall 5 secret-collision footgun. Finalize VALIDATION.md `nyquist_compliant: true`.

Purpose: Phase-wide verification that every locked decision shipped coherently and that an external developer following `guides/connect.md` can replicate Example 1-6 from RESEARCH.md end-to-end.

Output: dual-scope test + live_stripe suite + guide + boot-time warning + VALIDATION.md nyquist sign-off + human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-connect/05-CONTEXT.md
@.planning/phases/05-connect/05-RESEARCH.md
@.planning/phases/05-connect/05-PATTERNS.md
@.planning/phases/05-connect/05-VALIDATION.md
@.planning/phases/05-connect/05-01-PLAN.md
@.planning/phases/05-connect/05-02-PLAN.md
@.planning/phases/05-connect/05-03-PLAN.md
@.planning/phases/05-connect/05-04-PLAN.md
@.planning/phases/05-connect/05-05-PLAN.md
@.planning/phases/05-connect/05-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual-scope integration test + live_stripe Connect suite + boot-time secret-collision warning</name>
  <read_first>
    - accrue/test/support/connect_case.ex (from Plan 02)
    - accrue/test/support/billing_case.ex (Phase 3 case for cross-phase integration)
    - accrue/test/live_stripe/ (any existing live_stripe files from Phase 3/4 — mirror the @tag :live_stripe pattern)
    - accrue/lib/accrue/application.ex (boot-time checks — Phase 1 empty supervisor pattern; check how Phase 4 plan 06 added runtime warnings)
    - accrue/lib/accrue/config.ex (webhook_endpoints schema)
    - accrue/lib/accrue/connect.ex (full facade from Plans 02-05)
  </read_first>
  <behavior>
    - `accrue/test/accrue/connect/dual_scope_test.exs`:
      - `use Accrue.ConnectCase, async: true`
      - Seeds a Standard account (fully onboarded) via Fake factory
      - Test: `Accrue.Billing.create_customer(%{email: ...})` called WITHOUT scope creates customer in Fake `:platform` keyspace
      - Test: `Accrue.Connect.with_account(acct.stripe_account_id, fn -> Accrue.Billing.create_customer(%{email: ...}) end)` creates customer in Fake `{acct.stripe_account_id, :customer}` keyspace
      - Test: same `Accrue.Billing.subscribe/3` call works both scopes with correct keyspace isolation
      - Test: both scopes coexist in one test — platform customer visible via `Fake.customers_on(:platform)` but NOT via `Fake.customers_on(acct.stripe_account_id)`
      - Covers VALIDATION row 28 (CONN-11)
    - `accrue/test/live_stripe/connect_test.exs`:
      - `use Accrue.ConnectCase, async: false`
      - `@moduletag :live_stripe` — excluded from default `mix test`, included with `mix test --only live_stripe`
      - Uses real `Accrue.Processor.Stripe` adapter with a Stripe test-mode key from env
      - Test: create Standard account → verify `charges_enabled` syncs after onboarding (partial — automated test won't complete Stripe hosted onboarding, but asserts create + retrieve round-trip)
      - Test: create Express account → create login link → assert `%LoginLink{url}` has expected shape
      - Test: destination_charge against a pre-seeded test connected account (env var `STRIPE_TEST_CONNECTED_ACCOUNT`); skip test if env missing
      - Test: separate_charge_and_transfer same pre-seeded account
      - Test: platform_fee math matches live Stripe dashboard fee amounts for a $100 USD charge
      - Test: account.updated webhook verified against Connect-endpoint secret (requires Stripe CLI forwarding — document as manual-setup in guide)
      - Each test tagged individually so `mix test --only live_stripe:fee_math` runs a subset
    - `accrue/guides/connect.md`:
      - "Getting Started" — configure `:webhook_endpoints` with `:connect` key + secret, mount plug with `endpoint: :connect`
      - "Onboarding a Standard/Express/Custom account" — code for each type (RESEARCH.md Example 1)
      - "Destination charges" — Example 2 verbatim
      - "Separate charges + transfers" — Example 3
      - "Scoped operations" — Example 4 (`with_account/2`)
      - "Express dashboard" — Example 5
      - "Platform fee computation + per-account override recipe" — ~15 line code snippet showing host-owned fee_override jsonb
      - "Webhook config" — **EXPLICIT WARNING** that Stripe issues a SEPARATE signing secret per Connect endpoint in the Stripe Dashboard (Pitfall 5)
      - "Testing" — Fake keyspace scoping + `Accrue.Test.Factory.connect_account/1` usage
      - "Platform fee override per-account" — 5-line recipe reading host's fee_override jsonb and passing `rate_override:` to `platform_fee/2` (RESEARCH D5-04 explicit)
      - "Pitfalls" — top 6 pitfalls from RESEARCH.md summarized
    - `accrue/lib/accrue/application.ex`:
      - Add a private `warn_on_secret_collision/0` function called during `start/2` after `Config` load
      - Reads `Accrue.Config.get([:webhook_endpoints])`, checks if `platform[:secret]` and `connect[:secret]` are byte-identical
      - If equal and both non-nil, `Logger.warning("[Accrue] :platform and :connect webhook secrets are byte-identical. Stripe issues a SEPARATE signing secret per Connect endpoint in the Stripe Dashboard. Mixing them causes silent verification failures. (Pitfall 5; see guides/connect.md)")`
      - NOTE: Phase 1 empty supervisor pattern — `Application.start/2` returns the supervisor; the warning check runs BEFORE the supervisor tree starts, synchronously, non-blocking.
    - Update `.planning/phases/05-connect/05-VALIDATION.md` frontmatter: `nyquist_compliant: true` and `wave_0_complete: true`. Fill in the "Plan" column for each of the 28 rows with its assigned plan (01..07).
  </behavior>
  <action>
    1. Create `accrue/test/accrue/connect/dual_scope_test.exs` per behavior above.
    2. Create `accrue/test/live_stripe/connect_test.exs` per behavior above. Use existing Phase 3/4 `live_stripe` test files as a structural template if present; otherwise establish the pattern.
    3. Create `accrue/guides/connect.md` per behavior. Copy Examples 1-6 verbatim from RESEARCH.md Code Examples section.
    4. Edit `accrue/lib/accrue/application.ex`:
       - Add `warn_on_secret_collision/0` private fn
       - Call it at the start of `start/2` (after env guards)
    5. Update `.planning/phases/05-connect/05-VALIDATION.md`:
       - Frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`
       - Per-task verification map: fill in the Plan column for each of the 28 rows:
         - Rows 1, 2, 3, 25, 26, 27 → Plan 01
         - Rows 4, 5, 10, 23, 24, 28 → Plan 02
         - Rows 6, 7, 21, 22 → Plan 03
         - Rows 15, 16, 17, 18, 19, 20 → Plan 04
         - Rows 11, 12, 13, 14 → Plan 05
         - Rows 8, 9 → Plan 06
       - Append a final "sign-off" row: "Dual-scope + live_stripe coverage — Plan 07"
       - Update "Approval" line to "approved — planner"
    6. Run full Phase 5 test suite: `cd accrue && mix test test/accrue/connect/ test/accrue/webhook/connect_handler_test.exs test/property/connect_platform_fee_property_test.exs --warnings-as-errors`
    7. Run `mix credo --strict` + `mix dialyzer` — all clean
  </action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/connect/ test/accrue/webhook/connect_handler_test.exs test/property/connect_platform_fee_property_test.exs --warnings-as-errors &amp;&amp; mix credo --strict &amp;&amp; mix dialyzer</automated>
  </verify>
  <acceptance_criteria>
    - `ls accrue/test/accrue/connect/dual_scope_test.exs` exits 0
    - `ls accrue/test/live_stripe/connect_test.exs` exits 0
    - `grep -q '@moduletag :live_stripe' accrue/test/live_stripe/connect_test.exs`
    - `ls accrue/guides/connect.md` exits 0
    - `wc -l accrue/guides/connect.md | awk '{exit ($1 >= 150) ? 0 : 1}'` passes
    - `grep -q 'SEPARATE signing secret' accrue/guides/connect.md`
    - `grep -q 'warn_on_secret_collision' accrue/lib/accrue/application.ex`
    - `grep -q 'nyquist_compliant: true' .planning/phases/05-connect/05-VALIDATION.md`
    - `grep -q 'wave_0_complete: true' .planning/phases/05-connect/05-VALIDATION.md`
    - `cd accrue && mix test test/accrue/connect/ --warnings-as-errors` exits 0
    - `cd accrue && mix credo --strict` exits 0
    - `cd accrue && mix dialyzer` exits 0
    - VALIDATION.md row 28 passes
  </acceptance_criteria>
  <done>All 28 VALIDATION rows green; full Phase 5 test suite passes; guide published; boot-time warning active; nyquist sign-off complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Stripe test-mode round-trip</name>
  <action>Pause plan execution and wait for the human operator to perform the Stripe test-mode verification steps listed in how-to-verify. Do not resume until the operator types "approved".</action>
  <what-built>
    Full Accrue.Connect surface: account onboarding, charges, transfers, fee computation, webhook routing. Plan 07 ships a `live_stripe` test suite that hits Stripe test mode. Two items require human-in-the-loop verification that cannot be automated.
  </what-built>
  <how-to-verify>
    1. Configure a Stripe test-mode secret key in env: `export STRIPE_SECRET_KEY=sk_test_...`
    2. Run the live_stripe suite: `cd accrue && mix test --only live_stripe`
    3. Verify all tests pass (or expected-skipped tests are explicit about why — e.g. "STRIPE_TEST_CONNECTED_ACCOUNT not set")
    4. For CONN-07 Express LoginLink human verification (VALIDATION Manual-Only row 1):
       - Create a test Express connected account in Stripe Dashboard (one-time setup)
       - In iex: `{:ok, link} = Accrue.Connect.create_login_link(acct_id)`
       - Copy the `link.url` (you'll need to bypass Inspect masking by using `IO.inspect(link.url)` directly)
       - Paste into browser
       - Confirm: Stripe-hosted Express dashboard loads for that connected account
    5. For CONN-02 AccountLink onboarding flow (VALIDATION Manual-Only row 2):
       - `{:ok, account} = Accrue.Connect.create_account(%{type: "standard", country: "US", email: "test@example.com"})`
       - `{:ok, link} = Accrue.Connect.create_account_link(account, return_url: "https://example.com/return", refresh_url: "https://example.com/refresh")`
       - Paste link URL into browser, complete minimal Stripe-hosted onboarding
       - Confirm: browser redirects to `return_url` AND `Accrue.Connect.retrieve_account(account.stripe_account_id)` now shows updated onboarding status fields
    6. For Pitfall 5 secret collision warning:
       - Set `:webhook_endpoints` to have byte-identical `:platform` and `:connect` secrets in `config/test.exs`
       - Run `mix test test/accrue/connect/dual_scope_test.exs` and confirm a warning log appears at application start
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failed step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app → guides/connect.md | Developer follows guide verbatim; guide correctness is a security property |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Repudiation | Secret collision between :platform and :connect | mitigate | Boot-time Logger.warning (Pitfall 5). Non-fatal — hosts may intentionally use the same secret in dev/test. |
| T-05-07-02 | Information Disclosure | guides/connect.md code samples leaking API keys | accept | Samples use `sk_test_...` placeholder; README standard. |
| T-05-07-03 | Spoofing | live_stripe tests hitting production by mistake | mitigate | `@moduletag :live_stripe` default-excluded; env var checked for `sk_test_` prefix; guide explicit about test-mode only. |
</threat_model>

<verification>
- All 28 VALIDATION rows mapped to plans and green
- `mix credo --strict` + `mix dialyzer` clean
- Guide readable by external developer
- Human verification confirms Stripe hosted flows work
</verification>

<success_criteria>
- Phase 5 goal fully met: marketplace platforms can onboard, charge, transfer, and receive webhooks end-to-end
- VALIDATION.md marked `nyquist_compliant: true`
- Every Phase 5 requirement (PROC-05, CONN-01..CONN-11) has a passing test
</success_criteria>

<output>
After completion, create `.planning/phases/05-connect/05-07-SUMMARY.md`
</output>

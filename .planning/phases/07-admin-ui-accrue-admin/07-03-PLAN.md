---
phase: 07-admin-ui-accrue-admin
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - accrue/priv/repo/migrations/20260415090000_add_phase7_admin_indexes.exs
  - accrue_admin/lib/accrue_admin/queries/cursor.ex
  - accrue_admin/lib/accrue_admin/queries/behaviour.ex
  - accrue_admin/lib/accrue_admin/queries/customers.ex
  - accrue_admin/lib/accrue_admin/queries/subscriptions.ex
  - accrue_admin/lib/accrue_admin/queries/invoices.ex
  - accrue_admin/lib/accrue_admin/queries/charges.ex
  - accrue_admin/lib/accrue_admin/queries/coupons.ex
  - accrue_admin/lib/accrue_admin/queries/promotion_codes.ex
  - accrue_admin/lib/accrue_admin/queries/connect_accounts.ex
  - accrue_admin/test/accrue_admin/queries/cursor_test.exs
  - accrue_admin/test/accrue_admin/queries/query_modules_test.exs
autonomous: true
requirements: [ADMIN-07, ADMIN-09, ADMIN-11, ADMIN-13, ADMIN-15, ADMIN-19]
must_haves:
  truths:
    - "Operators can browse customer, subscription, invoice, charge, coupon, promotion-code, and Connect account lists with the same cursor-based paging and filter behavior over current billing data"
    - "Those list pages stay responsive under current data volume because the supporting indexes land before the page slices that consume them"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/queries/behaviour.ex"
      provides: "query contract for every list surface"
      contains: "@callback list"
    - path: "accrue/priv/repo/migrations/20260415090000_add_phase7_admin_indexes.exs"
      provides: "Phase 7 cursor and filter indexes"
      contains: "create index"
  key_links:
    - from: "Phase 7 admin index migration"
      to: "cursorized `AccrueAdmin.Queries.*` modules"
      via: "time/id composite ordering and filter coverage"
      pattern: "inserted_at"
---

<objective>
Create the data backbone for the billing and account list pages: schema-grounded query modules, cursor pagination, and the database indexes that make those Phase 7 filters executable.

Purpose: the previous revision combined query/index work with `DataTable`. This revision keeps 07-03 focused on the query contract and database support for billing/account pages, with `DataTable` split into its own follow-on plan and the ops-specific event/webhook queries landing with 07-07.

Output: admin query contract, billing/account query modules, and supporting indexes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-ui-accrue-admin/07-CONTEXT.md
@.planning/phases/07-admin-ui-accrue-admin/07-RESEARCH.md
@.planning/phases/07-admin-ui-accrue-admin/07-PATTERNS.md
@accrue/lib/accrue/billing/query.ex
@accrue/lib/accrue/billing/customer.ex
@accrue/lib/accrue/billing/subscription.ex
@accrue/lib/accrue/billing/invoice.ex
@accrue/lib/accrue/billing/charge.ex
@accrue/lib/accrue/billing/coupon.ex
@accrue/lib/accrue/billing/promotion_code.ex
@accrue/lib/accrue/connect/account.ex
@accrue/lib/accrue/events.ex
@accrue/lib/accrue/webhook/webhook_event.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin query contracts and supporting indexes grounded in current schemas</name>
  <files>accrue/priv/repo/migrations/20260415090000_add_phase7_admin_indexes.exs, accrue_admin/lib/accrue_admin/queries/cursor.ex, accrue_admin/lib/accrue_admin/queries/behaviour.ex, accrue_admin/lib/accrue_admin/queries/customers.ex, accrue_admin/lib/accrue_admin/queries/subscriptions.ex, accrue_admin/lib/accrue_admin/queries/invoices.ex, accrue_admin/lib/accrue_admin/queries/charges.ex, accrue_admin/lib/accrue_admin/queries/coupons.ex, accrue_admin/lib/accrue_admin/queries/promotion_codes.ex, accrue_admin/lib/accrue_admin/queries/connect_accounts.ex, accrue_admin/test/accrue_admin/queries/cursor_test.exs, accrue_admin/test/accrue_admin/queries/query_modules_test.exs</files>
  <action>Implement the D7-04 query contract under the single canonical module tree `accrue_admin/lib/accrue_admin/queries/` with `AccrueAdmin.Queries.*` modules for customers, subscriptions, invoices, charges, coupons, promotion codes, and Connect accounts. Use one module per resource and an opaque cursor module using time/id tuples. Translate context examples onto real repo fields: for invoices use `number`, `status`, `issued_at` equivalents from current schema (`inserted_at`, `finalized_at`, `due_date`, `number`) instead of fictional columns; for coupons and promotion codes use `valid`, `active`, `times_redeemed`, `expires_at`; for charges use `stripe_fee_amount_minor` and settlement fields already present. Add the explicit admin indexes called out in research, but ground them in existing table names and actual fields. Keep webhook/event query modules out of this plan so the ops slice in 07-07 owns that narrower concern end to end. Where the context names a drifted column, preserve user intent and map it to the closest executable schema field rather than inventing new columns.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/queries/cursor_test.exs test/accrue_admin/queries/query_modules_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Every list surface has a real `AccrueAdmin.Queries.*` module and the database has the indexes those queries depend on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query params -> admin query modules | Untrusted URL params become filter and cursor inputs for Ecto queries |
| admin list actions -> `DataTable` state | Client-side selection and paging state crosses into server-side LiveComponent handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | cursor decode path | mitigate | Make cursors opaque, signed/validated at decode, and fail closed to first-page semantics on invalid values |
| T-07-03-02 | Denial of Service | list queries and polling | mitigate | Use cursor pagination, composite indexes, and bounded stream limits; never use offset pagination |
| T-07-03-03 | Information Disclosure | JSON-backed metadata columns in list pages | mitigate | Query modules select explicit fields for rows instead of handing whole structs to the component by default |
</threat_model>

<verification>
- Query module tests prove schema-grounded filters and cursor encode/decode
</verification>

<success_criteria>
- The admin UI has a reusable, performant billing/account query foundation aligned with current schemas
- Later `DataTable` and page plans can stay thin and page-focused
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-accrue-admin/07-03-SUMMARY.md`
</output>

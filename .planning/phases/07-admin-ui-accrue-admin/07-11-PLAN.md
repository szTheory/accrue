---
phase: 07-admin-ui-accrue-admin
plan: 11
type: execute
wave: 4
depends_on: [07-03, 07-09, 07-10]
files_modified:
  - accrue_admin/lib/accrue_admin/components/data_table.ex
  - accrue_admin/test/accrue_admin/components/data_table_test.exs
autonomous: true
requirements: [ADMIN-02, ADMIN-07, ADMIN-09, ADMIN-11, ADMIN-13, ADMIN-15, ADMIN-16, ADMIN-17, ADMIN-18, ADMIN-19, ADMIN-27]
must_haves:
  truths:
    - "Operators can browse list-heavy admin pages in a shared desktop table and mobile card mode without each page re-implementing list behavior"
    - "Operators can keep filters in the URL, select rows in bulk, and explicitly load newer rows from the poll banner"
    - "List-heavy admin pages all consume the same `AccrueAdmin.Queries.*` contract instead of embedding resource-specific query code"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      provides: "shared LiveComponent for list pages"
      contains: "defmodule AccrueAdmin.Components.DataTable"
    - path: "accrue_admin/test/accrue_admin/components/data_table_test.exs"
      provides: "focused coverage for card mode, filters, bulk select, and poll banner behavior"
      contains: "new rows"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      to: "accrue_admin/lib/accrue_admin/queries/*.ex"
      via: "query module callbacks for list/filter/count_newer_than"
      pattern: "count_newer_than"
    - from: "DataTable mobile card mode"
      to: "AppShell breakpoints from 07-02"
      via: "responsive list rendering below `md:`"
      pattern: "card"
---

<objective>
Implement `AccrueAdmin.Components.DataTable` as the single shared list primitive.

Purpose: D7-09 makes `DataTable` the load-bearing component for most Phase 7 pages. The previous revision also needed the display-widget split, so this plan now depends on both component-foundation plans while keeping the list primitive isolated.

Output: shared `DataTable` LiveComponent and focused tests for its reusable behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-ui-accrue-admin/07-CONTEXT.md
@.planning/phases/07-admin-ui-accrue-admin/07-UI-SPEC.md
@.planning/phases/07-admin-ui-accrue-admin/07-03-PLAN.md
@.planning/phases/07-admin-ui-accrue-admin/07-09-PLAN.md
@.planning/phases/07-admin-ui-accrue-admin/07-10-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement core DataTable rendering, filter round-tripping, and cursor pagination</name>
  <files>accrue_admin/lib/accrue_admin/components/data_table.ex, accrue_admin/test/accrue_admin/components/data_table_test.exs</files>
  <action>Implement the core of D7-09 directly: one LiveComponent that handles streamed rows, URL-synced filters, and cursor pagination against the `AccrueAdmin.Queries.*` contract from 07-03. Keep query logic out of the component entirely; it must consume the shared query contract and the shared primitives from 07-09. Add focused tests proving the component stays resource-agnostic and does not embed resource-specific SQL or field assumptions.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs --warnings-as-errors</automated>
  </verify>
  <done>`DataTable` renders and pages against the shared query contract with passing focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DataTable with mobile card mode, bulk selection, and poll-banner updates</name>
  <files>accrue_admin/lib/accrue_admin/components/data_table.ex, accrue_admin/test/accrue_admin/components/data_table_test.exs</files>
  <action>Complete the remaining D7-09 behaviors: responsive card mode below `md:`, bulk selection, and the 5-second poll banner (`{N} new rows — click to load`). Keep the fixtures contract-level; do not bind the test to a single resource module. Preserve the DOM cap and explicit reload semantics from D7-04 rather than auto-inserting rows indefinitely.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The load-bearing list primitive has a focused, fast verify command instead of hiding inside a broader query or page suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params and row actions -> `DataTable` | Client-driven list state is interpreted by a shared component used across many admin pages |
| `AccrueAdmin.Queries.*` results -> rendered rows/cards | Server-selected billing data crosses into a reusable list primitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Tampering | filter and cursor state | mitigate | Accept only server-validated filter maps and opaque cursor values from `AccrueAdmin.Queries.*` |
| T-07-11-02 | Denial of Service | long-lived list sessions | mitigate | Enforce bounded DOM size and explicit poll-banner reload semantics instead of unbounded auto-insert |
| T-07-11-03 | Information Disclosure | generic row rendering | mitigate | Require explicit column/row shaping from the caller rather than rendering entire structs blindly |
</threat_model>

<verification>
- `DataTable` test suite passes with focused coverage for the shared behaviors page plans rely on
</verification>

<success_criteria>
- The load-bearing list primitive is isolated from query/index work and has its own narrow verify loop
- Later page plans can consume one stable table/card component instead of re-implementing list behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-accrue-admin/07-11-SUMMARY.md`
</output>

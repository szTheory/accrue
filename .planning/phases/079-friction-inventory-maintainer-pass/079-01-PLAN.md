---
phase: 79-friction-inventory-maintainer-pass
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INV-03]
autonomous: true
files_modified:
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - scripts/ci/verify_v1_17_friction_research_contract.sh
  - .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md
tags: [INV-03, friction-inventory, v1.25, maintainer-pass, FRG-01]
---

# Plan 079-01 — INV-03 friction inventory maintainer pass

<threat_model>
  <summary>ASVS L1; scope is planning artifacts + CI bash contracts. No runtime secrets or PII.</summary>
  <threat id="T-79-01">False attestation — certification without reproducible SHA + verifier bundle.</threat>
  <mitigation>Single merge SHA; name <code>verify_package_docs.sh</code>, <code>verify_adoption_proof_matrix.sh</code>, VERIFY-01 / host-integration; paste command transcripts or CI run links in <code>079-VERIFICATION.md</code>.</mitigation>
  <threat id="T-79-02">Silent merge-blocking break — inventory row added but <code>verify_v1_17_friction_research_contract.sh</code> counts stale.</threat>
  <mitigation>Same-PR update to count asserts; run script until stdout contains <code>verify_v1_17_friction_research_contract: OK</code>.</mitigation>
  <threat id="T-79-03">Dual SSOT drift — long certification in phase verification that contradicts inventory.</threat>
  <mitigation>Normative paragraph only under new <code>### v1.25</code> subsection in inventory; <code>079-VERIFICATION.md</code> links upward + methodology only per <code>079-CONTEXT.md</code> D-04/D-05.</mitigation>
</threat_model>

## Objective

Satisfy **INV-03** after **v1.24**: either **(a)** append **new sourced** **P1**/**P2** friction rows to **`.planning/research/v1.17-FRICTION-INVENTORY.md`** with stable **`v1.17-P*-NNN`** ids and co-update **`scripts/ci/verify_v1_17_friction_research_contract.sh`** row counts in the **same change set**, **or** **(b)** add a **dated maintainer certification** subsection (mirroring **`### v1.20 evidence refresh`**) stating no new sourced rows were warranted, with falsifiable verifier + SHA pointers. Publish evidence in **`079-VERIFICATION.md`**.

## Tasks

<task id="79-01-01">
  <read_first>
    <file>.planning/phases/079-friction-inventory-maintainer-pass/079-CONTEXT.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/research/v1.17-FRICTION-INVENTORY.md</file>
    <file>scripts/ci/verify_v1_17_friction_research_contract.sh</file>
  </read_first>
  <action>
    Create **`.planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md`** (or replace placeholder body if file exists) with YAML frontmatter:

    - **`phase: 79-friction-inventory-maintainer-pass`**
    - **`status: pending`** until all tasks complete, then set **`complete`**

    Body must contain these **Markdown headings** (exact text for grep contracts):

    1. **`## INV-03 path`** — one sentence declaring **`path (a) — new sourced rows`** **or** **`path (b) — dated no-new-rows certification`**, plus **one line** with literal substring **`Reviewed merge SHA:`** followed by the full **40-character lowercase hex** `git` SHA (no branch names).
    2. **`## Normative attestation`** — **single line** pointing to the inventory SSOT: must contain literal substrings **`v1.17-FRICTION-INVENTORY.md`** and **`INV-03`** (executor may add “### v1.25 …” anchor text once that subsection exists — optional in task **79-01-01**).
    3. **`## Verifier bundle`** — bullet list including these **exact substrings** as visible prose: **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`**, **`bash scripts/ci/verify_package_docs.sh`**, **`bash scripts/ci/verify_adoption_proof_matrix.sh`**, **`bash scripts/ci/verify_verify01_readme_contract.sh`**, and **`host-integration`** (job / CI reference).
    4. **`## Path evidence`** — placeholder line **`_(completed in task 79-01-02)_`** until task **79-01-02** runs.

    Do **not** paste a second full certification paragraph for path (b) here — only pointer + commands (D-05).
  </action>
  <acceptance_criteria>
    <criterion>Test `-f .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` is true.</criterion>
    <criterion>Command `rg -n '^## Normative attestation' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n '^## Normative attestation' -A5 .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md | rg -Fq 'INV-03'` exits **0**.</criterion>
    <criterion>Command `rg -n '^## INV-03 path' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'Reviewed merge SHA:' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'v1.17-FRICTION-INVENTORY.md' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_v1_17_friction_research_contract.sh' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'host-integration' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -n '^## Path evidence' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="79-01-02">
  <read_first>
    <file>.planning/research/v1.17-FRICTION-INVENTORY.md</file>
    <file>.planning/milestones/v1.20-phases/70-friction-evidence-refresh/70-VERIFICATION.md</file>
    <file>.planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md</file>
  </read_first>
  <action>
    Read **`## INV-03 path`** in **`079-VERIFICATION.md`**.

    **If it contains the substring `path (b)`:** In **`.planning/research/v1.17-FRICTION-INVENTORY.md`**, immediately **after** the existing **`### v1.20 evidence refresh`** subsection block (after its **revisit trigger** paragraph, before **`## FRG-03`**), insert a new subsection whose heading matches **`### v1.25 INV-03 maintainer pass (`** + ISO date **`YYYY-MM-DD`** + **`)`**.

    The subsection body must include: explicit **INV-03** reference, statement consistent with **no new sourced rows** (or justified deferral), the **same 40-char SHA** as in **`079-VERIFICATION.md`**, and a **`Revisit trigger:`** paragraph (≥1 bullet). Substring **`host-integration`** must appear ≥1× in the inventory file after this edit.

    **If it contains the substring `path (a)`:** **Append** one new **Markdown table data row** to the inventory table (same columns as existing rows). Use the **next unused** stable id (**`v1.17-P1-NNN`** or **`v1.17-P2-NNN`** only — no new **P0** without roadmap replan). Row must populate **`sources`**, **`ci_contract`**, **`integrator_impact`**, **`notes`**. Do **not** renumber existing rows. Append to **`079-VERIFICATION.md`** a line under **`## Path evidence`**: **`79-01-02: appended row id v1.17-P*-***`** with the literal id.

    **If path (b):** Append under **`## Path evidence`**: **`79-01-02: certification subsection ### v1.25`**.
  </action>
  <acceptance_criteria>
    <criterion>Shell `sh -c 'if grep -qF "path (b)" .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md; then rg -q "### v1\\.25 INV-03 maintainer pass" .planning/research/v1.17-FRICTION-INVENTORY.md; elif grep -qF "path (a)" .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md; then test "$(grep -cE "^\\| v1\\.17-P[012]-[0-9]{3} \\|" .planning/research/v1.17-FRICTION-INVENTORY.md)" -eq 6; else exit 1; fi'` exits **0** from repo root.</criterion>
    <criterion>Command `rg -n '^## Path evidence' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="79-01-03">
  <read_first>
    <file>scripts/ci/verify_v1_17_friction_research_contract.sh</file>
    <file>.planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md</file>
  </read_first>
  <action>
    **If path (b)** only: **skip** this task (mark in **`079-VERIFICATION.md`** under **`## Verifier bundle`** as **`79-01-03 skipped (path b)`**).

    **If path (a):** Update **`scripts/ci/verify_v1_17_friction_research_contract.sh`** numeric asserts so **`row_count`**, **`p0_count`**, **`p1_count`**, and **`p2_count`** match the post-append inventory (current baseline: **5**, **2**, **2**, **1**). Update only the **`[[ "$row_count" -eq`**, **`p0_count`**, **`p1_count`**, and **`p2_count`** test lines — preserve all **`grep -Fq`** backlog anchors and **`perl`** P0-id guard.
  </action>
  <acceptance_criteria>
    <criterion>For path (b): `rg -Fq '79-01-03 skipped (path b)' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>For path (a): `bash scripts/ci/verify_v1_17_friction_research_contract.sh` from repo root exits **0** and stdout is exactly **`verify_v1_17_friction_research_contract: OK`**.</criterion>
  </acceptance_criteria>
</task>

<task id="79-01-04">
  <read_first>
    <file>.planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md</file>
  </read_first>
  <action>
    From repository root, run and capture evidence (stdout tail or **`tee`** transcript path) in **`079-VERIFICATION.md`** under **`## Command transcripts`** — at minimum document that these were run **on the cited SHA’s tree** with exit code **0**:

    1. **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`**
    2. **`bash scripts/ci/verify_package_docs.sh`**
    3. **`bash scripts/ci/verify_adoption_proof_matrix.sh`**
    4. **`bash scripts/ci/verify_verify01_readme_contract.sh`**

    Each bullet line must start with the literal **`$`** character followed by the exact command string (markdown code span or fenced block both OK).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -Fq '## Command transcripts' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_v1_17_friction_research_contract.sh' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_package_docs.sh' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_adoption_proof_matrix.sh' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -F '$ bash scripts/ci/verify_verify01_readme_contract.sh' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="79-01-05">
  <read_first>
    <file>.planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md</file>
    <file>.github/workflows/ci.yml</file>
  </read_first>
  <action>
    Add subsection **`## VERIFY-01 / host-integration`** to **`079-VERIFICATION.md`** containing either:

    - A **GitHub Actions** URL to a **`host-integration`** (or equivalent) run on the **same SHA**, **or**
    - Instructions + outcome if run locally via documented host script (must name **`host-integration`** and **VERIFY-01** as substrings).

    If CI URL used, line must include literal **`github.com`** or **`actions`** so evidence is not hand-wavy.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '^## VERIFY-01 / host-integration' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'VERIFY-01' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'host-integration' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="79-01-06">
  <read_first>
    <file>.planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>
    Set **`079-VERIFICATION.md`** frontmatter **`status: complete`**.

    Add **`## INV-03 closure checklist`** mirroring **`.planning/REQUIREMENTS.md`** **INV-03** bullet: confirm **(a)** new rows + script co-update **or** **(b)** certification + verifier names + SHA; link inventory subsection anchor for path (b).

    Optionally (recommended): add **`INV-03`** row to **`.planning/REQUIREMENTS.md`** trace table **`| INV-03 | Phase 79 |`** status column **`Complete`** — only if project convention marks requirements in-file at phase close; if uncertain, **skip** table edit and note in verification.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n '^status: complete' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0** (YAML frontmatter line).</criterion>
    <criterion>Command `rg -n '^## INV-03 closure checklist' .planning/phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `bash scripts/ci/verify_v1_17_friction_research_contract.sh` from repo root exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

From repository root:

```bash
bash scripts/ci/verify_v1_17_friction_research_contract.sh
bash scripts/ci/verify_package_docs.sh
bash scripts/ci/verify_adoption_proof_matrix.sh
bash scripts/ci/verify_verify01_readme_contract.sh
```

Re-run all **`rg`** acceptance commands from tasks **79-01-01**–**79-01-06**.

## must_haves

- **INV-03 (a)** OR **(b)** fully evidenced per **`.planning/REQUIREMENTS.md`** with **`079-VERIFICATION.md`** + inventory updates.
- **`verify_v1_17_friction_research_contract.sh`** remains green on final tree; path **(a)** updates counts honestly.
- **No** duplicate normative certification outside inventory for path **(b)**.

## success_criteria

- `079-VERIFICATION.md` records SHA, path, verifier commands, and host-integration / VERIFY-01 evidence.
- Inventory reflects **v1.25** maintainer subsection **or** new sourced row(s) with script co-update.

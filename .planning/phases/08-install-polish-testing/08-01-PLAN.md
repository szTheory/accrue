---
phase: 08-install-polish-testing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - accrue/test/mix/tasks/accrue_install_test.exs
  - accrue/test/mix/tasks/accrue_gen_handler_test.exs
  - accrue/test/accrue/install/sigra_detection_test.exs
  - accrue/test/accrue/test/clock_test.exs
  - accrue/test/accrue/test/webhooks_test.exs
  - accrue/test/accrue/test/event_assertions_test.exs
  - accrue/test/accrue/test/facade_test.exs
  - accrue/test/accrue/telemetry/otel_test.exs
  - accrue/test/accrue/telemetry/billing_span_coverage_test.exs
  - accrue/test/accrue/docs/testing_guide_test.exs
  - accrue/test/accrue/docs/community_auth_test.exs
  - accrue/test/support/install_fixture.ex
autonomous: true
requirements: [INST-01, INST-02, INST-03, INST-04, INST-05, INST-06, INST-07, INST-08, INST-09, INST-10, AUTH-04, AUTH-05, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-10, OBS-02]
must_haves:
  truths:
    - "Wave 0 tests define the installer, generator, test-helper, OTel, and testing-guide behaviors before implementation."
    - "Installer tests cover no-clobber, dry-run, non-interactive flags, fingerprinted generated files, generated migrations, generated Billing facade, router/admin/auth wiring, and config validation."
    - "Testing helper tests prove Accrue.Test exposes clock, webhook, email, PDF, event, and setup helpers without hitting Stripe, Chrome, or SMTP."
  artifacts:
    - path: "accrue/test/mix/tasks/accrue_install_test.exs"
      provides: "Installer behavior contract"
    - path: "accrue/test/accrue/test/facade_test.exs"
      provides: "Public Accrue.Test facade contract"
    - path: "accrue/test/accrue/telemetry/otel_test.exs"
      provides: "Optional OTel privacy and with/without dependency compile contract"
    - path: "accrue/test/accrue/telemetry/billing_span_coverage_test.exs"
      provides: "Billing public function span coverage audit"
  key_links:
    - from: "accrue/test/mix/tasks/accrue_install_test.exs"
      to: "mix accrue.install"
      via: "Mix.Task.run(\"accrue.install\", argv)"
      pattern: "accrue.install"
    - from: "accrue/test/accrue/test/webhooks_test.exs"
      to: "Accrue.Webhook.Ingest / DefaultHandler"
      via: "Accrue.Test.trigger_event/2"
      pattern: "trigger_event"
---

<objective>
Create the Phase 08 Wave 0 validation surface from `08-VALIDATION.md`.

Purpose: downstream implementation plans must have concrete failing tests before adding installer, testing, OTel, and guide behavior.
Output: ExUnit test files and a small install-fixture helper that implementation plans make green.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/test/test_helper.exs
@accrue/test/support/data_case.ex
@accrue/test/support/conn_case.ex
@accrue/test/mix/tasks/accrue_webhooks_replay_test.exs
@accrue/test/accrue/test/mailer_assertions_test.exs
@accrue/test/accrue/test/pdf_assertions_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create installer and generator contract tests</name>
  <files>accrue/test/mix/tasks/accrue_install_test.exs, accrue/test/mix/tasks/accrue_gen_handler_test.exs, accrue/test/accrue/install/sigra_detection_test.exs, accrue/test/support/install_fixture.ex</files>
  <read_first>
    - .planning/phases/08-install-polish-testing/08-VALIDATION.md
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
    - accrue/test/mix/tasks/accrue_webhooks_replay_test.exs
    - accrue/lib/mix/tasks/accrue.webhooks.replay.ex
    - accrue/lib/mix/tasks/accrue.mail.preview.ex
    - accrue/lib/accrue/router.ex
    - accrue_admin/lib/accrue_admin/router.ex
    - accrue/lib/accrue/integrations/sigra.ex
  </read_first>
  <action>Create ExUnit tests that exercise `Mix.Task.run("accrue.install", argv)` in a temporary Phoenix-style fixture app. Add `Accrue.Test.InstallFixture` with helpers `tmp_app!/1`, `write_mix_project!/2`, `write_router!/2`, `write_config!/2`, `read!/2`, `assert_contains!/3`, and `refute_contains!/3`. The installer tests must assert these concrete values: dependency line `{:igniter, "~> 0.7.9", runtime: false}` in `mix.exs`; required switches `--dry-run`, `--yes`, `--non-interactive`, `--manual`, `--force`; generated module `defmodule MyApp.Billing`; generated handler `defmodule MyApp.BillingHandler`; webhook path `/webhooks/stripe`; route macro `accrue_webhook "/stripe", :stripe`; admin mount `accrue_admin "/billing"` only when `:accrue_admin` is present; Sigra config `config :accrue, :auth_adapter, Accrue.Integrations.Sigra` only when `:sigra` is present; fallback config `config :accrue, :auth_adapter, Accrue.Auth.Default`; no secret values such as `sk_test_`, `whsec_`, or `STRIPE_SECRET_KEY=` in reports; fingerprint marker `# accrue:generated` in generated host files. Per D8-01 through D8-05 and threat refs T-08-01/T-08-02, add a re-run test where a file with a matching fingerprint can update and a file with user edits is skipped unless it is pristine; `--force` must still skip user-edited files. For `mix accrue.gen.handler`, assert generated files contain `use Accrue.Webhook.Handler`, `def handle_event(type, event, ctx)`, and never overwrite a user-edited handler.</action>
  <acceptance_criteria>
    - `rg -n "igniter.*0\\.7\\.9|--dry-run|--non-interactive|--manual|--force|# accrue:generated|MyApp\\.Billing|MyApp\\.BillingHandler|accrue_webhook \"\\/stripe\", :stripe|accrue_admin \"\\/billing\"|Accrue\\.Integrations\\.Sigra|Accrue\\.Auth\\.Default|sk_test_|whsec_" accrue/test/mix/tasks/accrue_install_test.exs` returns matches.
    - `rg -n "use Accrue\\.Webhook\\.Handler|def handle_event\\(type, event, ctx\\)|never overwrite|user-edited" accrue/test/mix/tasks/accrue_gen_handler_test.exs` returns matches.
    - `rg -n "Accrue\\.Test\\.InstallFixture|tmp_app!|write_mix_project!|write_router!" accrue/test/support/install_fixture.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/mix/tasks/accrue_gen_handler_test.exs test/accrue/install/sigra_detection_test.exs</automated>
  </verify>
  <done>The files exist, assertions are specific enough to fail before implementation, and the failure points identify missing installer/generator behavior rather than syntax errors in the tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create public test helper contract tests</name>
  <files>accrue/test/accrue/test/clock_test.exs, accrue/test/accrue/test/webhooks_test.exs, accrue/test/accrue/test/event_assertions_test.exs, accrue/test/accrue/test/facade_test.exs</files>
  <read_first>
    - .planning/phases/08-install-polish-testing/08-VALIDATION.md
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
    - accrue/lib/accrue/processor/fake.ex
    - accrue/lib/accrue/test/mailer_assertions.ex
    - accrue/lib/accrue/test/pdf_assertions.ex
    - accrue/lib/accrue/webhook/ingest.ex
    - accrue/lib/accrue/webhook/default_handler.ex
    - accrue/lib/accrue/events.ex
  </read_first>
  <action>Create failing tests for `Accrue.Test`, `Accrue.Test.Clock`, `Accrue.Test.Webhooks`, and `Accrue.Test.EventAssertions`. Assert `use Accrue.Test` imports or delegates `advance_clock/2`, `trigger_event/2`, `assert_email_sent/1`, `assert_email_sent/2`, `assert_pdf_rendered/1`, `assert_event_recorded/1`, plus refute/no-op companions where implemented. Clock tests must cover `"1 month"`, `"30 days"`, `[months: 1]`, and `[seconds: 86_400]`, and must refute sleeps by asserting no `Process.sleep` is called in implementation. Webhook tests must assert `trigger_event(:invoice_payment_failed, invoice)` creates or routes through an `accrue_webhook_events` row and `Accrue.Webhook.DefaultHandler` behavior, not direct row mutation. Event assertion tests must cover keyword filters, struct/subject matching, partial maps, and one-arity predicate functions. Per D8-14 through D8-20 and T-08-03, the tests must prove synthetic events go through the normal webhook handler/reducer path.</action>
  <acceptance_criteria>
    - `rg -n "advance_clock\\(|trigger_event\\(|assert_email_sent\\(|assert_pdf_rendered\\(|assert_event_recorded\\(|refute_event_recorded|use Accrue\\.Test" accrue/test/accrue/test` returns matches.
    - `rg -n "\"1 month\"|\"30 days\"|months: 1|seconds: 86_400|Process\\.sleep" accrue/test/accrue/test/clock_test.exs` returns matches.
    - `rg -n "accrue_webhook_events|DefaultHandler|Accrue\\.Webhook\\.Ingest|not bypass|normal.*path" accrue/test/accrue/test/webhooks_test.exs` returns matches.
    - `rg -n "partial map|predicate|fn .* ->|subject|keyword" accrue/test/accrue/test/event_assertions_test.exs` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/test/clock_test.exs test/accrue/test/webhooks_test.exs test/accrue/test/event_assertions_test.exs test/accrue/test/facade_test.exs</automated>
  </verify>
  <done>Tests encode the public facade and helper semantics required by TEST-02, TEST-03, TEST-06, and TEST-07 before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create OTel and testing guide contract tests</name>
  <files>accrue/test/accrue/telemetry/otel_test.exs, accrue/test/accrue/telemetry/billing_span_coverage_test.exs, accrue/test/accrue/docs/testing_guide_test.exs, accrue/test/accrue/docs/community_auth_test.exs</files>
  <read_first>
    - .planning/phases/08-install-polish-testing/08-VALIDATION.md
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-RESEARCH.md
    - accrue/lib/accrue/telemetry.ex
    - accrue/guides/telemetry.md
    - accrue/guides/email.md
    - accrue/guides/pdf.md
  </read_first>
  <action>Create tests for `Accrue.Telemetry.OTel`, Billing span coverage, `accrue/guides/testing.md`, and `accrue/guides/auth_adapters.md`. OTel tests must assert span name conversion from `[:accrue, :billing, :subscription, :create]` to `accrue.billing.subscription.create`, allowlisted attributes `accrue.processor`, `accrue.customer_id`, `accrue.subscription_id`, `accrue.invoice_id`, `accrue.event_type`, `accrue.operation`, and `accrue.status`, and rejection/drop of prohibited keys `email`, `address`, `raw_body`, `payload`, `metadata`, `api_key`, `webhook_secret`, `stripe_secret_key`, and `card`. Add concrete compile-path verification for D8-26: test or shell out from ExUnit to run `MIX_ENV=test ACCRUE_OTEL_MATRIX=without_opentelemetry mix compile --warnings-as-errors --force` and `MIX_ENV=test ACCRUE_OTEL_MATRIX=with_opentelemetry mix compile --warnings-as-errors --force`, with the test restoring any mutated env/deps state. Add `accrue/test/accrue/telemetry/billing_span_coverage_test.exs` that enumerates public functions from `Accrue.Billing.__info__(:functions)`, excludes documentation/introspection helpers only by explicit allowlist, and asserts each Billing public context function is either wrapped by `Accrue.Telemetry.span/3` or listed in a test-owned audited exception map with a reason; the expected steady state is no unaudited public Billing functions. Testing guide tests must assert `guides/testing.md` starts with a Fake-first Phoenix scenario before helper-reference headings and contains copy-paste strings `use Accrue.Test`, `Accrue.Test.advance_clock`, `Accrue.Test.trigger_event`, `assert_email_sent`, `assert_pdf_rendered`, `assert_event_recorded`, `Oban.Testing`, and a footgun warning against `Process.sleep` and real Stripe sandbox calls by default. Add `accrue/test/accrue/docs/community_auth_test.exs` asserting AUTH-05 content for `PhxGenAuth`, `Pow`, `Assent`, `Sigra`, `Accrue.Auth.Default`, and required callbacks `current_user/1`, `require_admin_plug/0`, `user_schema/0`, `log_audit/2`, `actor_id/1`, `step_up_challenge/2`, and `verify_step_up/3`. Per D8-21 through D8-31, AUTH-05, OBS-02, and T-08-04, ensure privacy guardrails and docs requirements are executable.</action>
  <acceptance_criteria>
    - `rg -n "accrue\\.billing\\.subscription\\.create|accrue\\.processor|accrue\\.customer_id|accrue\\.subscription_id|accrue\\.invoice_id|accrue\\.event_type|raw_body|webhook_secret|stripe_secret_key|api_key|metadata" accrue/test/accrue/telemetry/otel_test.exs` returns matches.
    - `rg -n "ACCRUE_OTEL_MATRIX=without_opentelemetry mix compile --warnings-as-errors --force|ACCRUE_OTEL_MATRIX=with_opentelemetry mix compile --warnings-as-errors --force" accrue/test/accrue/telemetry/otel_test.exs` returns matches.
    - `rg -n "Accrue\\.Billing\\.__info__\\(:functions\\)|Accrue\\.Telemetry\\.span|unaudited|public.*Billing" accrue/test/accrue/telemetry/billing_span_coverage_test.exs` returns matches.
    - `rg -n "Fake-first|use Accrue\\.Test|Accrue\\.Test\\.advance_clock|Accrue\\.Test\\.trigger_event|assert_email_sent|assert_pdf_rendered|assert_event_recorded|Oban\\.Testing|Process\\.sleep|Stripe sandbox" accrue/test/accrue/docs/testing_guide_test.exs` returns matches.
    - `rg -n "PhxGenAuth|Pow|Assent|Sigra|Accrue\\.Auth\\.Default|current_user/1|require_admin_plug/0|user_schema/0|log_audit/2|actor_id/1|step_up_challenge/2|verify_step_up/3" accrue/test/accrue/docs/community_auth_test.exs` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/telemetry/otel_test.exs test/accrue/telemetry/billing_span_coverage_test.exs test/accrue/docs/testing_guide_test.exs test/accrue/docs/community_auth_test.exs</automated>
  </verify>
  <done>OTel and guide tests exist, are grep-verifiable, and fail only because production files have not yet implemented the specified behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture filesystem | Tests create and mutate temporary host Phoenix files; fixtures must not touch the real repo. |
| synthetic webhook helper | Test code asks Accrue to synthesize production-shaped webhook behavior. |
| OTel metadata | Test metadata can contain prohibited values that must never become span attributes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | installer tests | mitigate | Tests assert generated-file fingerprints, diff/no-clobber behavior, skipped edited files, and `--force` scoped to pristine generated files. |
| T-08-02 | Information Disclosure | installer report tests | mitigate | Tests assert reports redact `sk_test_`, `whsec_`, `STRIPE_SECRET_KEY`, and config/env secret values. |
| T-08-03 | Tampering | webhook helper tests | mitigate | Tests assert `trigger_event/2` exercises webhook ingest/handler path rather than directly mutating billing rows. |
| T-08-04 | Information Disclosure | OTel tests | mitigate | Tests assert only allowlisted attributes survive and prohibited keys are dropped. |
| T-08-05 | Elevation of Privilege | admin mount tests | mitigate | Tests assert admin route generation uses `AccrueAdmin.Router.accrue_admin/2` and includes host protection notes instead of raw unprotected LiveView routes. |
</threat_model>

<verification>
Run the Wave 0 test commands from each task. They may fail before implementation, but they must compile as tests and fail at missing production behavior assertions.
</verification>

<success_criteria>
All Wave 0 test files listed in `08-VALIDATION.md` exist and encode concrete acceptance for every Phase 08 requirement ID.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-01-SUMMARY.md`.
</output>

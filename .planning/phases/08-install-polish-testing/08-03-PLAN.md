---
phase: 08-install-polish-testing
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - accrue/lib/mix/tasks/accrue.install.ex
  - accrue/lib/accrue/install/patches.ex
  - accrue/lib/accrue/install/project.ex
  - accrue/lib/accrue/install/templates.ex
  - accrue/lib/mix/tasks/accrue.gen.handler.ex
  - accrue/priv/accrue/templates/install/billing_handler.ex.eex
autonomous: true
requirements: [INST-03, INST-04, INST-06, INST-08, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Installer can patch or print exact router, webhook, admin, Sigra, auth, test-support, and Oban snippets."
    - "`mix accrue.install` orchestrates option parsing, discovery, rendering, fingerprints, patch builders, config docs/validation, and final reporting."
    - "Webhook raw-body capture is route-scoped, never global."
    - "`mix accrue.gen.handler` creates a host handler without overwriting edited files."
  artifacts:
    - path: "accrue/lib/accrue/install/patches.ex"
      provides: "Router/config/application/test-support patch builders"
    - path: "accrue/lib/mix/tasks/accrue.install.ex"
      provides: "End-to-end installer orchestration"
    - path: "accrue/lib/mix/tasks/accrue.gen.handler.ex"
      provides: "Webhook handler generator"
    - path: "accrue/priv/accrue/templates/install/billing_handler.ex.eex"
      provides: "Host handler template"
  key_links:
    - from: "accrue/lib/accrue/install/patches.ex"
      to: "accrue/lib/accrue/router.ex"
      via: "imports and `accrue_webhook` macro snippet"
      pattern: "accrue_webhook"
    - from: "accrue/lib/accrue/install/patches.ex"
      to: "accrue_admin/lib/accrue_admin/router.ex"
      via: "`accrue_admin \"/billing\"` snippet"
      pattern: "accrue_admin"
    - from: "accrue/lib/mix/tasks/accrue.install.ex"
      to: "accrue/lib/accrue/install/patches.ex"
      via: "post-discovery patch orchestration"
      pattern: "Accrue.Install.Patches"
---

<objective>
Implement safe host wiring for router/webhook/admin/auth/test support and add the handler generator.

Purpose: complete the install surface that touches existing host files while preserving route-scoped raw-body capture, protected admin mounting, and no-clobber behavior.
Output: patch builders/manual snippets and `mix accrue.gen.handler`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/lib/accrue/router.ex
@accrue/lib/accrue/webhook/handler.ex
@accrue/lib/accrue/webhook/plug.ex
@accrue/lib/accrue/webhook/caching_body_reader.ex
@accrue/lib/accrue/auth/default.ex
@accrue/lib/accrue/integrations/sigra.ex
@accrue_admin/lib/accrue_admin/router.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add router, webhook, admin, auth, test-support, and Oban patch builders</name>
  <files>accrue/lib/accrue/install/patches.ex, accrue/lib/accrue/install/project.ex, accrue/lib/accrue/install/templates.ex</files>
  <read_first>
    - accrue/lib/accrue/install/project.ex
    - accrue/lib/accrue/install/templates.ex
    - accrue/lib/accrue/router.ex
    - accrue/lib/accrue/webhook/plug.ex
    - accrue/lib/accrue/webhook/caching_body_reader.ex
    - accrue/lib/accrue/auth/default.ex
    - accrue/lib/accrue/integrations/sigra.ex
    - accrue_admin/lib/accrue_admin/router.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
  </read_first>
  <action>Create `Accrue.Install.Patches` functions that return structured changes plus exact manual snippets. Router snippet must include `import Accrue.Router`, pipeline `:accrue_webhook_raw_body`, `plug Plug.Parsers`, `parsers: [:json]`, `pass: ["*/*"]`, `json_decoder: Jason`, `body_reader: {Accrue.Webhook.CachingBodyReader, :read_body, []}`, `length: 1_000_000`, `scope "/webhooks"`, `pipe_through :accrue_webhook_raw_body`, and `accrue_webhook "/stripe", :stripe`. Admin snippet must include `import AccrueAdmin.Router` and `accrue_admin "/billing"` only when `:accrue_admin` is detected or `--admin true` is passed; include host protection note `AccrueAdmin.AuthHook` / `Accrue.Auth.require_admin_plug`. Sigra config snippet must include `config :accrue, :auth_adapter, Accrue.Integrations.Sigra` when `:sigra` is detected or `--sigra true`; fallback snippet must include `config :accrue, :auth_adapter, Accrue.Auth.Default` and a prod-safety warning. Test support snippet must include `use Accrue.Test`, `config :accrue, :processor, Accrue.Processor.Fake`, `config :accrue, :mailer_adapter, Accrue.Mailer.Test`, and `config :accrue, :pdf_adapter, Accrue.PDF.Test`. Oban snippet must include queue names `accrue_webhooks`, `accrue_mailers`, and `accrue_pdf`. Per T-08-03 and T-08-05, never generate a global Plug.Parsers raw-body reader and never hand-write unprotected admin LiveView routes.</action>
  <acceptance_criteria>
    - `rg -n "pipeline :accrue_webhook_raw_body|body_reader: \\{Accrue\\.Webhook\\.CachingBodyReader, :read_body, \\[\\]\\}|accrue_webhook \"\\/stripe\", :stripe|scope \"\\/webhooks\"" accrue/lib/accrue/install/patches.ex` returns matches.
    - `rg -n "import AccrueAdmin\\.Router|accrue_admin \"\\/billing\"|AccrueAdmin\\.AuthHook|Accrue\\.Auth\\.require_admin_plug" accrue/lib/accrue/install/patches.ex` returns matches.
    - `rg -n "Accrue\\.Integrations\\.Sigra|Accrue\\.Auth\\.Default|prod-safety|accrue_webhooks|accrue_mailers|accrue_pdf|use Accrue\\.Test" accrue/lib/accrue/install/patches.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/accrue/install/sigra_detection_test.exs</automated>
  </verify>
  <done>Installer wiring tests pass for webhook route, admin mount, Sigra fallback, auth docs, test setup, and Oban snippets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `mix accrue.install` to all installer subsystems</name>
  <files>accrue/lib/mix/tasks/accrue.install.ex</files>
  <read_first>
    - accrue/lib/mix/tasks/accrue.install.ex
    - accrue/lib/accrue/install/options.ex
    - accrue/lib/accrue/install/project.ex
    - accrue/lib/accrue/install/templates.ex
    - accrue/lib/accrue/install/fingerprints.ex
    - accrue/lib/accrue/install/patches.ex
    - accrue/lib/accrue/config.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
  </read_first>
  <action>After `Accrue.Install.Patches` exists, modify `Mix.Tasks.Accrue.Install.run/1` so the task orchestrates the full install pipeline instead of only parsing options or rendering files. The pipeline must call `Accrue.Install.Options.parse!/1`, `Accrue.Install.Project.discover!/1`, `Accrue.Install.Templates` rendering for migrations/Billing/handler/test support, `Accrue.Install.Fingerprints` for generated-file pristine/user-edited decisions, `Accrue.Install.Patches` for router/webhook/admin/auth/test-support/Oban patch builders, and `Accrue.Config.validate!/1` plus config docs output through `NimbleOptions.docs/1` or the existing config docs helper. Apply `--dry-run` as review-only, `--manual` as snippet/report-only, `--yes`/`--non-interactive` as default acceptance, and `--force` only for pristine Accrue-owned generated files per D8-01 through D8-05. Final reporting must print separate changed, skipped, and manual follow-up sections; include patch/source names such as `Project`, `Templates`, `Fingerprints`, and `Patches`; redact secret-like values; and exit nonzero via `Mix.raise/1` on invalid config or unsafe ambiguous patching that cannot be represented as manual snippets.</action>
  <acceptance_criteria>
    - `rg -n "Accrue\\.Install\\.Options\\.parse!|Accrue\\.Install\\.Project\\.discover!|Accrue\\.Install\\.Templates|Accrue\\.Install\\.Fingerprints|Accrue\\.Install\\.Patches" accrue/lib/mix/tasks/accrue.install.ex` returns matches.
    - `rg -n "Accrue\\.Config\\.validate!|NimbleOptions\\.docs|config docs|changed|skipped|manual" accrue/lib/mix/tasks/accrue.install.ex` returns matches.
    - `rg -n "dry_run|manual|non_interactive|yes|force|redact|SECRET|whsec_|sk_" accrue/lib/mix/tasks/accrue.install.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/accrue/install/sigra_detection_test.exs</automated>
  </verify>
  <done>`mix accrue.install` applies or reports the generated artifacts and patch builders created in Plans 08-02 and 08-03, and its final report distinguishes changed/skipped/manual follow-up items.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement `mix accrue.gen.handler`</name>
  <files>accrue/lib/mix/tasks/accrue.gen.handler.ex, accrue/priv/accrue/templates/install/billing_handler.ex.eex</files>
  <read_first>
    - accrue/lib/mix/tasks/accrue.mail.preview.ex
    - accrue/lib/mix/tasks/accrue.webhooks.replay.ex
    - accrue/lib/accrue/webhook/handler.ex
    - accrue/lib/accrue/install/fingerprints.ex
    - accrue/priv/accrue/templates/install/billing_handler.ex.eex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-PATTERNS.md
  </read_first>
  <action>Create `Mix.Tasks.Accrue.Gen.Handler` with `use Mix.Task`, `Mix.Task.run("loadpaths")`, strict switches `module: :string`, `path: :string`, `dry_run: :boolean`, `yes: :boolean`, `non_interactive: :boolean`, `force: :boolean`, and `manual: :boolean`. Default module is `MyApp.BillingHandler` and default path is `lib/my_app/billing_handler.ex`. Render `billing_handler.ex.eex` containing `use Accrue.Webhook.Handler`, `@impl Accrue.Webhook.Handler`, `def handle_event(type, event, ctx)`, a commented custom side-effect branch, and a fallback `:ok`. Use `Accrue.Install.Fingerprints` so generated handlers contain `# accrue:generated` and edited handlers are skipped; `--force` may rewrite only pristine generated handlers. Per INST-08/T-08-01, never overwrite a user-edited handler without an explicit conflict-writing path.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Mix\\.Tasks\\.Accrue\\.Gen\\.Handler|Mix\\.Task\\.run\\(\"loadpaths\"\\)|module: :string|path: :string|non_interactive: :boolean|manual: :boolean" accrue/lib/mix/tasks/accrue.gen.handler.ex` returns matches.
    - `rg -n "use Accrue\\.Webhook\\.Handler|@impl Accrue\\.Webhook\\.Handler|def handle_event\\(type, event, ctx\\)|# accrue:generated" accrue/priv/accrue/templates/install/billing_handler.ex.eex accrue/lib/mix/tasks/accrue.gen.handler.ex` returns matches.
    - `rg -n "Fingerprints|pristine|user_edited|force" accrue/lib/mix/tasks/accrue.gen.handler.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_gen_handler_test.exs</automated>
  </verify>
  <done>`mix accrue.gen.handler` generates a handler scaffold and passes no-clobber tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host router | Installer mutates or prints Phoenix router snippets that affect request parsing and admin access. |
| host auth config | Installer chooses Sigra or default auth based on dependency detection. |
| host generated handler | Handler generator writes host-owned callback code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `mix accrue.gen.handler` | mitigate | Fingerprinted generated handlers; edited files skipped; `--force` limited to pristine generated files. |
| T-08-02 | Information Disclosure | config/auth snippets | mitigate | Snippets use env-var names and module names only; reports redact secret-looking values before output. |
| T-08-03 | Tampering | router patch | mitigate | Generate only route-scoped `:accrue_webhook_raw_body` pipeline with `Accrue.Webhook.CachingBodyReader`; no global parser edits. |
| T-08-05 | Elevation of Privilege | admin mount patch | mitigate | Mount via `AccrueAdmin.Router.accrue_admin/2` and include host auth protection notes; never copy raw admin LiveView routes. |
</threat_model>

<verification>
`cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/mix/tasks/accrue_gen_handler_test.exs test/accrue/install/sigra_detection_test.exs`
</verification>

<success_criteria>
Installer wiring and handler generation satisfy INST-03, INST-04, INST-06, INST-08, AUTH-04, and AUTH-05 without route/global parser or admin-auth regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-03-SUMMARY.md`.
</output>

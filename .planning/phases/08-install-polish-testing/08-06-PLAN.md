---
phase: 08-install-polish-testing
plan: 06
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - accrue/lib/accrue/billing.ex
  - accrue/lib/accrue/telemetry.ex
  - accrue/lib/accrue/telemetry/otel.ex
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "Accrue emits true OpenTelemetry spans when `:opentelemetry` is present."
    - "Accrue compiles without warnings when `:opentelemetry` is absent."
    - "OTel attributes are allowlisted and sanitized before attachment."
  artifacts:
    - path: "accrue/lib/accrue/telemetry/otel.ex"
      provides: "Optional OTel adapter"
    - path: "accrue/lib/accrue/telemetry.ex"
      provides: "Central telemetry span integration point"
    - path: "accrue/lib/accrue/billing.ex"
      provides: "Billing public function span coverage"
  key_links:
    - from: "accrue/lib/accrue/telemetry.ex"
      to: "accrue/lib/accrue/telemetry/otel.ex"
      via: "`Accrue.Telemetry.span/3` invokes OTel bridge"
      pattern: "Accrue.Telemetry.OTel"
---

<objective>
Add optional OpenTelemetry span bridging under the existing telemetry span path.

Purpose: satisfy OBS-02 without making OTel a required runtime dependency or leaking sensitive billing data into traces.
Output: `Accrue.Telemetry.OTel` plus a central call from `Accrue.Telemetry.span/3`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/lib/accrue/telemetry.ex
@accrue/lib/accrue/integrations/sigra.ex
@accrue/guides/telemetry.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement optional OTel adapter with allowlisted attributes</name>
  <files>accrue/lib/accrue/telemetry/otel.ex</files>
  <read_first>
    - accrue/lib/accrue/telemetry/otel.ex
    - accrue/lib/accrue/telemetry.ex
    - accrue/lib/accrue/integrations/sigra.ex
    - accrue/guides/telemetry.md
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-RESEARCH.md
  </read_first>
  <action>Create `Accrue.Telemetry.OTel` following the Sigra optional compile pattern: `@compile {:no_warn_undefined, [OpenTelemetry.Tracer]}` and guarded calls through `Code.ensure_loaded?(OpenTelemetry.Tracer)` or equivalent so without-OTel builds do not warn. Implement `span(event, metadata, fun)` where span name is `event |> Enum.map(&to_string/1) |> Enum.join(".")`, e.g. `[:accrue, :billing, :subscription, :create]` to `accrue.billing.subscription.create`. Sanitize attributes with an explicit allowlist mapping only these keys: `:processor` to `"accrue.processor"`, `:customer_id` to `"accrue.customer_id"`, `:subscription_id` to `"accrue.subscription_id"`, `:invoice_id` to `"accrue.invoice_id"`, `:event_type` to `"accrue.event_type"`, `:operation` to `"accrue.operation"`, `:status` to `"accrue.status"`, and already-prefixed string keys in that same set. Drop prohibited keys `:email`, `:address`, `:raw_body`, `:payload`, `:metadata`, `:api_key`, `:webhook_secret`, `:stripe_secret_key`, and `:card`. Use `OpenTelemetry.Tracer.with_span`, `OpenTelemetry.Tracer.set_attributes`, and status mapping `:ok` / `{:error, _}` where available. When OTel is absent, execute `fun.()` unchanged.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Accrue\\.Telemetry\\.OTel|no_warn_undefined|Code\\.ensure_loaded\\?|OpenTelemetry\\.Tracer|with_span|set_attributes" accrue/lib/accrue/telemetry/otel.ex` returns matches.
    - `rg -n "accrue\\.processor|accrue\\.customer_id|accrue\\.subscription_id|accrue\\.invoice_id|accrue\\.event_type|accrue\\.operation|accrue\\.status" accrue/lib/accrue/telemetry/otel.ex` returns matches.
    - `rg -n "raw_body|payload|metadata|api_key|webhook_secret|stripe_secret_key|email|address|card" accrue/lib/accrue/telemetry/otel.ex` returns matches in denylist code only.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/telemetry/otel_test.exs</automated>
  </verify>
  <done>OTel adapter passes allowlist, no-op, and span-name tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route `Accrue.Telemetry.span/3` through OTel without replacing telemetry</name>
  <files>accrue/lib/accrue/telemetry.ex</files>
  <read_first>
    - accrue/lib/accrue/telemetry.ex
    - accrue/lib/accrue/telemetry/otel.ex
    - accrue/guides/telemetry.md
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
  </read_first>
  <action>Modify `Accrue.Telemetry.span/3` so `:telemetry.span/3` remains the stable Elixir-native event surface, and the zero-arity work function is wrapped by `Accrue.Telemetry.OTel.span(event_without_suffix, base_metadata, fn -> fun.() end)` or equivalent without double-running `fun`. Preserve actor metadata behavior. OTel must mirror event names and never replace telemetry handlers. If current event includes suffix `:start`, `:stop`, or `:exception`, strip only the suffix before building OTel name; otherwise use the event as-is. Keep `current_trace_id/0` warning-free with and without OTel.</action>
  <acceptance_criteria>
    - `rg -n "Accrue\\.Telemetry\\.OTel\\.span|:telemetry\\.span|maybe_put_actor|current_trace_id" accrue/lib/accrue/telemetry.ex` returns matches.
    - `rg -n "start|stop|exception" accrue/lib/accrue/telemetry.ex` returns matches in suffix normalization or docs.
    - `cd accrue && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/telemetry/otel_test.exs test/accrue/telemetry_test.exs</automated>
  </verify>
  <done>OBS-02 tests pass and existing telemetry tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Audit and cover Billing public function spans</name>
  <files>accrue/lib/accrue/billing.ex</files>
  <read_first>
    - accrue/lib/accrue/billing.ex
    - accrue/lib/accrue/telemetry.ex
    - accrue/test/accrue/telemetry/billing_span_coverage_test.exs
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
  </read_first>
  <action>Audit every public function returned by `Accrue.Billing.__info__(:functions)` and ensure each Billing context entry point emits through `Accrue.Telemetry.span/3` with an explicit event name and allowlisted business metadata, or is listed in the test's explicit audited exception map with a reason such as pure predicate/helper and no side effect. Do not add macro-generated broad instrumentation; add small call-site wrappers with sanitized metadata per D8-24/D8-25. The implementation must satisfy `accrue/test/accrue/telemetry/billing_span_coverage_test.exs` so new public Billing functions fail the audit until span coverage or an explicit exception is added.</action>
  <acceptance_criteria>
    - `rg -n "Accrue\\.Telemetry\\.span|\\[:accrue, :billing" accrue/lib/accrue/billing.ex` returns matches.
    - `rg -n "email|raw_body|payload|metadata|api_key|webhook_secret|stripe_secret_key|card" accrue/lib/accrue/billing.ex` does not show values forwarded into telemetry metadata.
    - `cd accrue && mix test test/accrue/telemetry/billing_span_coverage_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/telemetry/billing_span_coverage_test.exs test/accrue/telemetry/otel_test.exs</automated>
  </verify>
  <done>Every public Billing context function is spanned or explicitly audited, and the coverage test prevents silent OBS-02 regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telemetry metadata to OTel exporter | Metadata leaves Accrue process into tracing backends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | `Accrue.Telemetry.OTel` | mitigate | Attribute allowlist plus denylist tests for raw payloads, emails, addresses, API keys, signing secrets, metadata blobs, and card data. |
</threat_model>

<verification>
`cd accrue && mix test test/accrue/telemetry/otel_test.exs test/accrue/telemetry/billing_span_coverage_test.exs test/accrue/telemetry_test.exs`

Compile matrix commands that must be run before closing OBS-02:
- `cd accrue && MIX_ENV=test ACCRUE_OTEL_MATRIX=without_opentelemetry mix compile --warnings-as-errors --force`
- `cd accrue && MIX_ENV=test ACCRUE_OTEL_MATRIX=with_opentelemetry mix compile --warnings-as-errors --force`
</verification>

<success_criteria>
OBS-02 is implemented through optional OTel spans, warning-free compile paths, and privacy tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-06-SUMMARY.md`.
</output>

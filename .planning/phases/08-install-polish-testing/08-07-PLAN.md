---
phase: 08-install-polish-testing
plan: 07
type: execute
wave: 4
depends_on: [08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - accrue/mix.exs
  - accrue/guides/testing.md
  - accrue/guides/auth_adapters.md
  - accrue/test/accrue/docs/testing_guide_test.exs
  - accrue/test/accrue/docs/community_auth_test.exs
autonomous: true
requirements: [AUTH-05, TEST-10, INST-10, OBS-02]
must_haves:
  truths:
    - "Testing guide opens with a copy-pasteable Fake-first Phoenix scenario."
    - "Guide proves billing behavior without Stripe, Chrome, SMTP, or sleeps."
    - "Community auth adapter guidance exists and is linked from docs extras."
  artifacts:
    - path: "accrue/guides/testing.md"
      provides: "Fake-first testing playbook"
    - path: "accrue/guides/auth_adapters.md"
      provides: "Community auth adapter patterns"
    - path: "accrue/mix.exs"
      provides: "ExDoc extras wiring for new guides"
  key_links:
    - from: "accrue/mix.exs"
      to: "accrue/guides/testing.md"
      via: "docs extras"
      pattern: "guides/testing.md"
    - from: "accrue/guides/testing.md"
      to: "Accrue.Test"
      via: "copy-paste scenario imports helpers"
      pattern: "use Accrue.Test"
---

<objective>
Write the Fake-first testing guide, auth adapter guide, and docs wiring.

Purpose: turn Phase 8 helper APIs into a developer-facing local testing story and document community auth integration patterns without entering Phase 9 release scope.
Output: targeted guides and ExDoc extras entries.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-install-polish-testing/08-CONTEXT.md
@.planning/phases/08-install-polish-testing/08-RESEARCH.md
@.planning/phases/08-install-polish-testing/08-VALIDATION.md
@.planning/phases/08-install-polish-testing/08-PATTERNS.md
@accrue/mix.exs
@accrue/guides/telemetry.md
@accrue/guides/email.md
@accrue/guides/pdf.md
@accrue/lib/accrue/test.ex
@accrue/lib/accrue/auth.ex
@accrue/lib/accrue/auth/default.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Fake-first testing guide and executable doc assertions</name>
  <files>accrue/guides/testing.md, accrue/test/accrue/docs/testing_guide_test.exs</files>
  <read_first>
    - accrue/guides/testing.md
    - accrue/test/accrue/docs/testing_guide_test.exs
    - accrue/lib/accrue/test.ex
    - accrue/lib/accrue/test/clock.ex
    - accrue/lib/accrue/test/webhooks.ex
    - accrue/lib/accrue/test/event_assertions.ex
    - accrue/lib/accrue/test/mailer_assertions.ex
    - accrue/lib/accrue/test/pdf_assertions.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
  </read_first>
  <action>Create `accrue/guides/testing.md` with this order: heading `# Testing Accrue Billing Flows`, opening section `## Fake-first Phoenix scenario`, then a copy-paste ExUnit module before any helper-reference section. The scenario must include `use MyApp.DataCase`, `use Accrue.Test`, `Oban.Testing`, `Accrue.Test.advance_clock(subscription, "1 month")`, `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)`, `assert_email_sent(:receipt, to: user.email)`, `assert_pdf_rendered(invoice)`, `assert_event_recorded(user, type: :subscription_created)`, and a persisted state assertion against `MyApp.Billing`. Add scenario sections for successful checkout, trial conversion, failed renewal and retry, cancellation/grace period, invoice email/PDF, webhook replay, background jobs, and provider-parity tests. Add a helper reference after scenario sections. Add external-provider appendix covering real Stripe test mode, Stripe test clocks, 3DS cards, and live webhook forwarding. Add footgun warnings against testing Accrue internals instead of host flows, `Process.sleep`, real Stripe sandbox calls by default, mixing live/test keys, hiding webhook setup, and failing to assert side effects. Keep this targeted guide only; do not add README quickstart or broad Phase 9 ExDoc guide set.</action>
  <acceptance_criteria>
    - `rg -n "# Testing Accrue Billing Flows|## Fake-first Phoenix scenario|use Accrue\\.Test|Oban\\.Testing|Accrue\\.Test\\.advance_clock\\(subscription, \"1 month\"\\)|Accrue\\.Test\\.trigger_event\\(:invoice_payment_failed, invoice\\)|assert_email_sent\\(:receipt, to: user\\.email\\)|assert_pdf_rendered\\(invoice\\)|assert_event_recorded\\(user, type: :subscription_created\\)" accrue/guides/testing.md` returns matches.
    - `rg -n "successful checkout|trial conversion|failed renewal|cancellation/grace period|invoice email/PDF|webhook replay|background jobs|provider-parity|Stripe test clocks|3DS cards|live webhook forwarding|Process\\.sleep|real Stripe sandbox calls by default|mixing live/test keys" accrue/guides/testing.md` returns matches.
    - `rg -n "README quickstart|Release Please|Hex publishing|GitHub Actions release" accrue/guides/testing.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/testing_guide_test.exs</automated>
  </verify>
  <done>TEST-10 is green and the guide is a Fake-first scenario playbook, not a helper catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth adapter guide and ExDoc extras wiring</name>
  <files>accrue/guides/auth_adapters.md, accrue/mix.exs, accrue/test/accrue/docs/community_auth_test.exs</files>
  <read_first>
    - accrue/guides/auth_adapters.md
    - accrue/mix.exs
    - accrue/lib/accrue/auth.ex
    - accrue/lib/accrue/auth/default.ex
    - accrue/lib/accrue/integrations/sigra.ex
    - .planning/phases/08-install-polish-testing/08-CONTEXT.md
    - .planning/phases/08-install-polish-testing/08-RESEARCH.md
  </read_first>
  <action>Create `accrue/guides/auth_adapters.md` documenting the `Accrue.Auth` callbacks `current_user/1`, `require_admin_plug/0`, `user_schema/0`, `log_audit/2`, `actor_id/1`, `step_up_challenge/2`, and `verify_step_up/3`. Include community adapter patterns for `MyApp.Auth.PhxGenAuth`, `MyApp.Auth.Pow`, and `MyApp.Auth.Assent`, plus Sigra install note `config :accrue, :auth_adapter, Accrue.Integrations.Sigra` when `:sigra` is present and fallback warning for `Accrue.Auth.Default` in prod. Add `accrue/test/accrue/docs/community_auth_test.exs` if Wave 0 did not already create it, and make it assert guide content for `PhxGenAuth`, `Pow`, `Assent`, `Sigra`, `Accrue.Auth.Default`, and all required callbacks; keep these assertions as executable AUTH-05 coverage instead of relying on `mix docs` alone. Update `docs/0` extras in `accrue/mix.exs` to include exactly `guides/telemetry.md`, `guides/testing.md`, and `guides/auth_adapters.md` at minimum. Per AUTH-05 and INST-10, keep config docs discoverable but do not add Phase 9 broad guide set.</action>
  <acceptance_criteria>
    - `rg -n "current_user/1|require_admin_plug/0|user_schema/0|log_audit/2|actor_id/1|step_up_challenge/2|verify_step_up/3|MyApp\\.Auth\\.PhxGenAuth|MyApp\\.Auth\\.Pow|MyApp\\.Auth\\.Assent|Accrue\\.Integrations\\.Sigra|Accrue\\.Auth\\.Default" accrue/guides/auth_adapters.md` returns matches.
    - `rg -n "PhxGenAuth|Pow|Assent|Sigra|Accrue\\.Auth\\.Default|current_user/1|require_admin_plug/0|user_schema/0|log_audit/2|actor_id/1|step_up_challenge/2|verify_step_up/3" accrue/test/accrue/docs/community_auth_test.exs` returns matches.
    - `rg -n "guides/testing\\.md|guides/auth_adapters\\.md|guides/telemetry\\.md" accrue/mix.exs` returns matches.
    - `rg -n "quickstart|upgrade guide|SECURITY\\.md|CONTRIBUTING" accrue/guides/auth_adapters.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/community_auth_test.exs && mix docs --warnings-as-errors</automated>
  </verify>
  <done>AUTH-05 docs exist and the new guides are wired into ExDoc extras without Phase 9 release-guide scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs examples to host apps | Developers may copy guide snippets directly into apps. |
| auth adapter guidance | Community adapters connect host auth state to Accrue admin and audit behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | guide examples | mitigate | Examples use env-var names and Fake helpers; no real Stripe keys, webhook secrets, raw payloads, or emails beyond `user.email` variable references. |
| T-08-04 | Information Disclosure | testing guide OTel appendix | mitigate | OTel guidance points to allowlisted `accrue.*` attributes only and warns against raw payload/metadata attributes. |
| T-08-05 | Elevation of Privilege | auth adapter guide | mitigate | Guide documents `require_admin_plug/0`, step-up callbacks, Sigra adapter, and `Accrue.Auth.Default` prod fail-closed behavior. |
</threat_model>

<verification>
`cd accrue && mix test test/accrue/docs/testing_guide_test.exs test/accrue/docs/community_auth_test.exs && mix docs --warnings-as-errors`
</verification>

<success_criteria>
TEST-10, AUTH-05, INST-10, and OBS-02 documentation surfaces are complete, targeted, and excluded from Phase 9 release work.
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-polish-testing/08-07-SUMMARY.md`.
</output>

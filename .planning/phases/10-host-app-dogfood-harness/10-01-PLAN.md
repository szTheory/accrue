---
phase: 10-host-app-dogfood-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/mix.exs
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/config/test.exs
  - examples/accrue_host/config/runtime.exs
  - examples/accrue_host/test/test_helper.exs
  - examples/accrue_host/test/support/data_case.ex
  - examples/accrue_host/test/support/conn_case.ex
  - examples/accrue_host/test/support/accrue_case.ex
  - examples/accrue_host/test/install_boundary_test.exs
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
  - examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs
  - examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs
  - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
  - examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs
autonomous: true
requirements: [HOST-01, HOST-08]
must_haves:
  truths:
    - "A normal Phoenix host app exists at examples/accrue_host and can run Mix tasks as its own app."
    - "The host app has its own Repo, endpoint, auth scaffold, and test harness instead of using private core-package fixtures."
    - "Wave 0 verification files exist for install boundary, billing facade, subscription flow, webhook ingest, admin mount, and admin replay."
  artifacts:
    - path: "examples/accrue_host/mix.exs"
      provides: "Host Phoenix app Mix project with local path dependencies to ../../accrue and ../../accrue_admin"
      contains: "app: :accrue_host"
    - path: "examples/accrue_host/test/support/accrue_case.ex"
      provides: "Host integration test helpers for Accrue-backed flows"
      contains: "defmodule AccrueHost.AccrueCase"
    - path: "examples/accrue_host/test/install_boundary_test.exs"
      provides: "Wave 0 install-boundary proof file"
      contains: "defmodule AccrueHost.InstallBoundaryTest"
  key_links:
    - from: "examples/accrue_host/mix.exs"
      to: "../../accrue"
      via: "path dependency"
      pattern: "path: \"../../accrue\""
    - from: "examples/accrue_host/test/test_helper.exs"
      to: "examples/accrue_host/test/support/data_case.ex"
      via: "test support boot"
      pattern: "ExUnit.start"
---

<objective>
Create the canonical host-app foundation and Wave 0 test surface for Phase 10.

Purpose: per D-01, D-02, and D-03, the dogfood proof has to be a real Phoenix app under `examples/accrue_host`, not a private fixture. Per the validation contract, the host app also needs its own Repo/test harness and every required test file present before feature work starts.

Output: a generated Phoenix app with local path deps, host-owned test support, and skeleton verification files for every Phase 10 proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
@.planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
@.planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
@.planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
@accrue/test/support/install_fixture.ex
@accrue/test/mix/tasks/accrue_install_uat_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the host app and host-owned test infrastructure</name>
  <files>examples/accrue_host/mix.exs, examples/accrue_host/config/config.exs, examples/accrue_host/config/test.exs, examples/accrue_host/config/runtime.exs, examples/accrue_host/test/test_helper.exs, examples/accrue_host/test/support/data_case.ex, examples/accrue_host/test/support/conn_case.ex, examples/accrue_host/test/support/accrue_case.ex</files>
  <read_first>
    - examples/accrue_host/mix.exs
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
    - .planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
    - accrue/test/support/install_fixture.ex
    - accrue_admin/config/test.exs
  </read_first>
  <action>Per D-01, D-02, D-03, and the generator-first pattern in 10-RESEARCH.md, create `examples/accrue_host` with the exact commands `mix phx.new examples/accrue_host --app accrue_host --module AccrueHost --database postgres --binary-id --no-dashboard --no-agents-md` and `cd examples/accrue_host && mix phx.gen.auth Accounts User users --live --binary-id`. Keep Bandit. Edit `examples/accrue_host/mix.exs` so deps include `{:accrue, path: "../../accrue"}` and `{:accrue_admin, path: "../../accrue_admin"}`. In `examples/accrue_host/config/test.exs`, configure `AccrueHost.Repo` with PostgreSQL defaults `hostname: System.get_env("PGHOST", "localhost")`, `username: System.get_env("PGUSER", "postgres")`, `password: System.get_env("PGPASSWORD", "postgres")`, `database: "accrue_host_test#{System.get_env("MIX_TEST_PARTITION")}"`, `pool: Ecto.Adapters.SQL.Sandbox`; also set `config :accrue, env: :test, repo: AccrueHost.Repo, processor: Accrue.Processor.Fake`. In `examples/accrue_host/config/runtime.exs`, keep host-owned runtime config with `config :accrue, :processor, Accrue.Processor.Fake` and `config :accrue, :webhook_signing_secrets, %{stripe: System.get_env("STRIPE_WEBHOOK_SECRET", "whsec_test_host")}` so the local Fake path needs no hidden secret. Add `test/support/data_case.ex`, `conn_case.ex`, and `accrue_case.ex` so host tests can boot the Repo, sandbox, ConnTest, and Accrue helpers without using private fixture projects.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix deps.get && mix compile && mix test --only does_not_exist</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/mix.exs` contains `path: "../../accrue"`
    - `examples/accrue_host/mix.exs` contains `path: "../../accrue_admin"`
    - `examples/accrue_host/config/test.exs` contains `database: "accrue_host_test#{System.get_env("MIX_TEST_PARTITION")}"`
    - `examples/accrue_host/config/test.exs` contains `processor: Accrue.Processor.Fake`
    - `examples/accrue_host/config/runtime.exs` contains `STRIPE_WEBHOOK_SECRET`
    - `examples/accrue_host/test/support/accrue_case.ex` contains `defmodule AccrueHost.AccrueCase`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix compile` exits 0
  </acceptance_criteria>
  <done>The host app exists as its own Phoenix project with local path deps, host-owned config, and reusable test support.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Wave 0 verification files required by 10-VALIDATION.md</name>
  <files>examples/accrue_host/test/install_boundary_test.exs, examples/accrue_host/test/accrue_host/billing_facade_test.exs, examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs, examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs, examples/accrue_host/test/accrue_host_web/admin_mount_test.exs, examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs</files>
  <read_first>
    - examples/accrue_host/test/install_boundary_test.exs
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - accrue/test/mix/tasks/accrue_install_uat_test.exs
    - accrue_admin/test/accrue_admin/live/webhook_replay_test.exs
  </read_first>
  <action>Per the Wave 0 checklist in `10-VALIDATION.md`, create all six host-app proof files now. `test/install_boundary_test.exs` must contain executable assertions that the host app has no private-shortcut contract drift: check the install boundary by asserting the generated app references `mix accrue.install`, `accrue_webhook "/stripe", :stripe`, `accrue_admin "/billing"`, and path deps. The other five files must exist as compile-safe ExUnit modules with the exact module names `AccrueHost.BillingFacadeTest`, `AccrueHostWeb.SubscriptionFlowTest`, `AccrueHostWeb.WebhookIngestTest`, `AccrueHostWeb.AdminMountTest`, and `AccrueHostWeb.AdminWebhookReplayTest`; include `@moduletag :phase10` plus one temporary `test "pending phase 10 implementation"` that fails with a clear message naming the later plan number that will finish it. This preserves Wave 0 file existence without silently claiming the feature already works.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/install_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/jon/projects/accrue/examples/accrue_host/test/install_boundary_test.exs`
    - `test -f /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host/billing_facade_test.exs`
    - `test -f /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs`
    - `test -f /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs`
    - `test -f /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host_web/admin_mount_test.exs`
    - `test -f /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs`
    - `examples/accrue_host/test/install_boundary_test.exs` contains `accrue_admin "/billing"`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/install_boundary_test.exs` exits 0
  </acceptance_criteria>
  <done>Every validation-map test file exists, and the install-boundary proof is executable immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root -> examples/accrue_host | Generated host app must remain a real user-visible Phoenix app, not a hidden fixture |
| host test harness -> Accrue public API | Test support may help setup, but proof files must target public boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | Wave 0 test scaffold | mitigate | Task 2 creates explicit install-boundary assertions so later work cannot silently bypass public installer, router macro, or host facade paths. |
| T-10-02 | I | runtime/test config | mitigate | Task 1 pins `STRIPE_WEBHOOK_SECRET` to env access with default `whsec_test_host`; no raw secret values beyond the Fake test default are logged or required. |
| T-10-03 | D | hidden local state | mitigate | Task 1 gives the host app its own Repo and sandbox wiring; Task 2 creates validation files that later plans must satisfy with documented commands. |
| T-10-04 | E | private fixture reuse | mitigate | Task 1 builds `examples/accrue_host` as a standalone Phoenix app per D-01 rather than reusing private install fixtures. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix deps.get && mix compile && mix test test/install_boundary_test.exs`.
</verification>

<success_criteria>
The repo contains `examples/accrue_host`, host-owned test support, and all six Wave 0 proof files named in `10-VALIDATION.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-host-app-dogfood-harness/10-01-SUMMARY.md`
</output>

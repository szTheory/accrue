---
phase: 10-host-app-dogfood-harness
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - examples/accrue_host/lib/accrue_host/accounts/user.ex
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/lib/accrue_host/billing_handler.ex
  - examples/accrue_host/lib/accrue_host_web/router.ex
  - examples/accrue_host/config/runtime.exs
  - examples/accrue_host/test/install_boundary_test.exs
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
autonomous: true
requirements: [HOST-02, HOST-03]
must_haves:
  truths:
    - "The host app is wired by the public installer and generated host-facing modules, not by hand-built private shortcuts."
    - "The host-owned user schema is the billable boundary and the generated Billing facade is the only host billing entrypoint."
    - "The router includes the installer-owned webhook and admin mount snippets exactly at public API boundaries."
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/billing.ex"
      provides: "Generated host billing facade"
      contains: "def subscribe"
    - path: "examples/accrue_host/lib/accrue_host/accounts/user.ex"
      provides: "Host-owned billable user schema"
      contains: "use Accrue.Billable"
    - path: "examples/accrue_host/lib/accrue_host_web/router.ex"
      provides: "Installer-owned webhook and admin mount wiring"
      contains: "accrue_webhook"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host/billing.ex"
      to: "Accrue.Billing"
      via: "generated public facade"
      pattern: "alias Accrue.Billing"
    - from: "examples/accrue_host/lib/accrue_host_web/router.ex"
      to: "Accrue.Router"
      via: "public router macro import"
      pattern: "import Accrue.Router"
---

<objective>
Wire the host app through the public Accrue install boundary and the host-owned billable schema.

Purpose: per D-04, D-05, D-07, D-13, and HOST-02/HOST-03, this plan must prove the normal user setup path: `mix accrue.install`, generated `AccrueHost.Billing`, generated `AccrueHost.BillingHandler`, host-owned `Accounts.User`, and installer-patched router/config snippets.

Output: installed host wiring, a billable user schema, and executable tests for install-boundary and billing-facade proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
@.planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
@.planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
@.planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
@accrue/lib/mix/tasks/accrue.install.ex
@accrue/priv/accrue/templates/install/billing.ex.eex
@accrue/priv/accrue/templates/install/billing_handler.ex.eex
@accrue/lib/accrue/install/patches.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the public installer and keep the generated host wiring intact</name>
  <files>examples/accrue_host/lib/accrue_host/billing.ex, examples/accrue_host/lib/accrue_host/billing_handler.ex, examples/accrue_host/lib/accrue_host_web/router.ex, examples/accrue_host/config/runtime.exs, examples/accrue_host/test/install_boundary_test.exs</files>
  <read_first>
    - examples/accrue_host/lib/accrue_host_web/router.ex
    - examples/accrue_host/config/runtime.exs
    - examples/accrue_host/test/install_boundary_test.exs
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - .planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
    - accrue/lib/mix/tasks/accrue.install.ex
    - accrue/lib/accrue/install/patches.ex
    - accrue/priv/accrue/templates/install/billing.ex.eex
    - accrue/priv/accrue/templates/install/billing_handler.ex.eex
  </read_first>
  <action>Per D-04, D-07, D-13, HOST-02, and the exact command in 10-RESEARCH.md, run `cd examples/accrue_host && mix accrue.install --yes --billable AccrueHost.Accounts.User --billing-context AccrueHost.Billing --admin-mount /billing --webhook-path /webhooks/stripe`. Keep the generated files package-owned: `lib/accrue_host/billing.ex` must remain a thin facade over `Accrue.Billing`, `lib/accrue_host/billing_handler.ex` must remain a `use Accrue.Webhook.Handler` host side-effect stub, and `lib/accrue_host_web/router.ex` must contain the route-scoped raw-body pipeline plus `accrue_webhook "/stripe", :stripe` and `accrue_admin "/billing", session_keys: [:user_token]`. In `config/runtime.exs`, keep the host-owned Fake defaults from Plan 10-01 and make sure the installer patch does not replace them with live-network-only config. Replace the temporary install-boundary placeholder assertions with executable checks that the generated files and router patch exist exactly at the public boundaries.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix accrue.install --yes --billable AccrueHost.Accounts.User --billing-context AccrueHost.Billing --admin-mount /billing --webhook-path /webhooks/stripe && mix test test/install_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/lib/accrue_host/billing.ex` contains `alias Accrue.Billing`
    - `examples/accrue_host/lib/accrue_host/billing_handler.ex` contains `use Accrue.Webhook.Handler`
    - `examples/accrue_host/lib/accrue_host_web/router.ex` contains `pipeline :accrue_webhook_raw_body`
    - `examples/accrue_host/lib/accrue_host_web/router.ex` contains `body_reader: {Accrue.Webhook.CachingBodyReader, :read_body, []}`
    - `examples/accrue_host/lib/accrue_host_web/router.ex` contains `accrue_webhook "/stripe", :stripe`
    - `examples/accrue_host/lib/accrue_host_web/router.ex` contains `accrue_admin "/billing"`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/install_boundary_test.exs` exits 0
  </acceptance_criteria>
  <done>The host app is installed through `mix accrue.install`, and the public-boundary router/config/generated files are proven by test.</done>
</task>

<task type="auto">
  <name>Task 2: Make the host user billable and finish the generated billing-facade proof</name>
  <files>examples/accrue_host/lib/accrue_host/accounts/user.ex, examples/accrue_host/lib/accrue_host/billing.ex, examples/accrue_host/test/accrue_host/billing_facade_test.exs</files>
  <read_first>
    - examples/accrue_host/lib/accrue_host/accounts/user.ex
    - examples/accrue_host/lib/accrue_host/billing.ex
    - examples/accrue_host/test/accrue_host/billing_facade_test.exs
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-PATTERNS.md
    - accrue/priv/accrue/templates/install/billing.ex.eex
  </read_first>
  <action>Per D-05 and HOST-03, edit `lib/accrue_host/accounts/user.ex` so the generated host schema includes `use Accrue.Billable`. Keep the schema host-owned; do not move user data into `accrue`. Keep `lib/accrue_host/billing.ex` thin and explicit: it must expose `subscribe/3`, `swap_plan/3`, `cancel/2`, and `customer_for/1` delegating to `Accrue.Billing`. Replace the placeholder `billing_facade_test.exs` with executable tests that assert the host schema is billable, the generated facade module exports those four public functions, and the facade round-trips through `Accrue.Billing.customer/1` / `subscribe/3` with Fake-backed fixtures instead of direct inserts used as the proof path.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/lib/accrue_host/accounts/user.ex` contains `use Accrue.Billable`
    - `examples/accrue_host/lib/accrue_host/billing.ex` contains `def subscribe(billable, price_id, opts \\ [])`
    - `examples/accrue_host/lib/accrue_host/billing.ex` contains `def swap_plan(subscription, price_id, opts)`
    - `examples/accrue_host/lib/accrue_host/billing.ex` contains `def cancel(subscription, opts \\ [])`
    - `examples/accrue_host/lib/accrue_host/billing.ex` contains `def customer_for(billable)`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs` exits 0
  </acceptance_criteria>
  <done>The host-owned user schema is the billable boundary and the generated Billing facade is executable and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host install command -> generated files | Public installer output must remain the integration contract |
| host schema -> core billing APIs | Host owns the billable schema; Accrue owns billing internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | E | install boundary | mitigate | Task 1 uses `mix accrue.install` with exact flags and tests for generated router/facade artifacts so private hand wiring cannot masquerade as public installation. |
| T-10-06 | T | router webhook patch | mitigate | Task 1 requires the route-scoped `body_reader: {Accrue.Webhook.CachingBodyReader, :read_body, []}` snippet so webhook verification remains non-bypassable. |
| T-10-07 | E | billable schema ownership | mitigate | Task 2 adds `use Accrue.Billable` on `AccrueHost.Accounts.User` rather than moving ownership into `accrue`, matching D-05. |
| T-10-08 | I | runtime config | mitigate | Task 1 keeps Fake-only defaults in host runtime config and avoids introducing live secrets or hidden service requirements. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix accrue.install --yes --billable AccrueHost.Accounts.User --billing-context AccrueHost.Billing --admin-mount /billing --webhook-path /webhooks/stripe && mix test test/install_boundary_test.exs test/accrue_host/billing_facade_test.exs`.
</verification>

<success_criteria>
The host app is installed via public Accrue surfaces, the host user schema is billable, and the generated facade/router wiring is proven by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-host-app-dogfood-harness/10-02-SUMMARY.md`
</output>

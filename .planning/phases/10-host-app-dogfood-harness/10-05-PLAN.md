---
phase: 10-host-app-dogfood-harness
plan: 05
type: execute
wave: 4
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - examples/accrue_host/lib/accrue_host/auth.ex
  - examples/accrue_host/lib/accrue_host/accounts/user.ex
  - examples/accrue_host/priv/repo/migrations/20260416000100_add_billing_admin_to_users.exs
  - examples/accrue_host/config/runtime.exs
  - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
  - examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs
  - examples/accrue_host/README.md
autonomous: true
requirements: [HOST-05, HOST-07, HOST-08]
must_haves:
  truths:
    - "Anonymous and non-admin users cannot access /billing."
    - "An admin user can inspect mounted billing/admin state and replay at least one webhook row."
    - "The replay path leaves persisted audit evidence, not just a UI flash."
    - "A clean checkout has exact documented commands for deps, DB setup, tests, and local boot."
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/auth.ex"
      provides: "Host Accrue auth adapter backed by host session/user data"
      contains: "defmodule AccrueHost.Auth"
    - path: "examples/accrue_host/test/accrue_host_web/admin_mount_test.exs"
      provides: "Mounted admin auth/session proof"
      contains: "/billing"
    - path: "examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs"
      provides: "Replay + audit proof"
      contains: "admin.webhook.replay.completed"
    - path: "examples/accrue_host/README.md"
      provides: "Clean-checkout command path"
      contains: "mix ecto.create"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host/auth.ex"
      to: "examples/accrue_host/lib/accrue_host/accounts/user.ex"
      via: "host admin/user lookup"
      pattern: "billing_admin"
    - from: "examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs"
      to: "admin.webhook.replay.completed"
      via: "event-ledger assertion"
      pattern: "admin.webhook.replay.completed"
---

<objective>
Protect the mounted admin UI with a realistic host session boundary, prove one audited admin replay action, and document the clean-checkout path.

Purpose: per D-06, D-07, D-08, D-16, D-17, D-18, D-19, D-21 and HOST-05/HOST-07/HOST-08, the host app must mount `accrue_admin` behind host-owned auth, deny anonymous access, allow a real admin to inspect/replay state, and leave persisted audit evidence. The same plan also documents the exact local commands needed to rebuild and boot the example without hidden state.

Output: a host auth adapter, admin authorization field/migration, executable admin mount and replay tests, and README commands for clean-checkout setup.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
@.planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
@.planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
@.planning/phases/10-host-app-dogfood-harness/10-UI-SPEC.md
@accrue/lib/accrue/auth.ex
@accrue_admin/lib/accrue_admin/router.ex
@accrue_admin/lib/accrue_admin/auth_hook.ex
@accrue_admin/test/accrue_admin/live/auth_hook_test.exs
@accrue_admin/test/accrue_admin/live/webhook_replay_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a host auth adapter and admin authorization field for /billing</name>
  <files>examples/accrue_host/lib/accrue_host/auth.ex, examples/accrue_host/lib/accrue_host/accounts/user.ex, examples/accrue_host/priv/repo/migrations/20260416000100_add_billing_admin_to_users.exs, examples/accrue_host/config/runtime.exs, examples/accrue_host/test/accrue_host_web/admin_mount_test.exs</files>
  <read_first>
    - examples/accrue_host/lib/accrue_host/accounts/user.ex
    - examples/accrue_host/config/runtime.exs
    - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-RESEARCH.md
    - accrue/lib/accrue/auth.ex
    - accrue_admin/lib/accrue_admin/auth_hook.ex
    - accrue_admin/test/accrue_admin/live/auth_hook_test.exs
  </read_first>
  <action>Per D-06, D-07, D-08, HOST-05, and the ASVS session-management requirement, create `lib/accrue_host/auth.ex` implementing the `Accrue.Auth` callbacks against the host `Accounts.User` model and the forwarded `:user_token` session key. Add a host-owned boolean field `billing_admin` to `Accounts.User` plus a migration file named exactly `20260416000100_add_billing_admin_to_users.exs` that adds `:billing_admin, :boolean, default: false, null: false` to the `users` table. In `config/runtime.exs`, set `config :accrue, :auth_adapter, AccrueHost.Auth`. Replace the placeholder admin-mount test with executable coverage that (1) redirects or denies anonymous `/billing` access, (2) denies a signed-in non-admin user, and (3) allows a signed-in `billing_admin: true` user to mount the admin surface with the forwarded `:user_token` session key. Keep the mount at `/billing`; do not invent a parallel host-only admin path.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/lib/accrue_host/auth.ex` contains `defmodule AccrueHost.Auth`
    - `examples/accrue_host/lib/accrue_host/auth.ex` contains `billing_admin`
    - `examples/accrue_host/lib/accrue_host/accounts/user.ex` contains `field :billing_admin, :boolean`
    - `examples/accrue_host/priv/repo/migrations/20260416000100_add_billing_admin_to_users.exs` contains `add :billing_admin, :boolean`
    - `examples/accrue_host/config/runtime.exs` contains `auth_adapter, AccrueHost.Auth`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs` exits 0
  </acceptance_criteria>
  <done>The mounted admin UI is protected by a realistic host session/admin boundary and tested accordingly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the admin replay placeholder with an audited replay proof</name>
  <files>examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs</files>
  <read_first>
    - examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs
    - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
    - accrue_admin/test/accrue_admin/live/webhook_replay_test.exs
    - accrue_admin/test/accrue_admin/live/events_live_test.exs
  </read_first>
  <action>Replace the placeholder in `test/accrue_host_web/admin_webhook_replay_test.exs` with executable replay coverage for D-17 and D-18. Seed or create a replayable webhook row using setup-only helpers, sign in a host `billing_admin: true` user, mount `/billing`, trigger the existing single-row replay action, and assert both the user-visible success path and persisted audit evidence. The test must assert an `accrue_events` row with the exact type `admin.webhook.replay.completed`; it must also assert causal linkage to the replayed webhook row or source event, following the existing admin tests. Do not replace the proof with a direct `Accrue.Webhooks.DLQ.requeue/1` call outside the mounted admin path.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/admin_webhook_replay_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs` contains `/billing`
    - `examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs` contains `admin.webhook.replay.completed`
    - `examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs` contains `replay-single`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/admin_webhook_replay_test.exs` exits 0
  </acceptance_criteria>
  <done>The host app proves an admin can replay one webhook through the mounted UI and leaves persisted audit evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Document the clean-checkout setup and local boot path</name>
  <files>examples/accrue_host/README.md</files>
  <read_first>
    - examples/accrue_host/README.md
    - .planning/phases/10-host-app-dogfood-harness/10-CONTEXT.md
    - .planning/phases/10-host-app-dogfood-harness/10-VALIDATION.md
  </read_first>
  <action>Per D-02, D-19, D-21, and HOST-08, write `examples/accrue_host/README.md` with the exact clean-checkout commands: `mix deps.get`, `mix ecto.create`, `mix ecto.migrate`, `mix test`, and `mix phx.server`. State the one manual prerequisite explicitly: PostgreSQL 14+ must be running on `localhost:5432` or the values supplied via `PGHOST`, `PGUSER`, and `PGPASSWORD`. Document that the local Fake-backed default requires no live Stripe network access and uses the default signing secret `whsec_test_host` unless `STRIPE_WEBHOOK_SECRET` is overridden. Include the exact install command used in Plan 10-02 so a reader can rebuild the host proof from scratch. After writing the README, run the documented clean-checkout setup command sequence from `examples/accrue_host` exactly as written: `mix ecto.create && mix ecto.migrate && mix test`. The task is not complete if the README text exists but the documented setup command has not been exercised.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix ecto.create && mix ecto.migrate && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/README.md` contains `mix deps.get`
    - `examples/accrue_host/README.md` contains `mix ecto.create`
    - `examples/accrue_host/README.md` contains `mix ecto.migrate`
    - `examples/accrue_host/README.md` contains `mix test`
    - `examples/accrue_host/README.md` contains `mix phx.server`
    - `examples/accrue_host/README.md` contains `whsec_test_host`
    - `examples/accrue_host/README.md` contains `mix accrue.install --yes --billable AccrueHost.Accounts.User --billing-context AccrueHost.Billing --admin-mount /billing --webhook-path /webhooks/stripe`
    - `cd /Users/jon/projects/accrue/examples/accrue_host && mix ecto.create && mix ecto.migrate && mix test` exits 0
  </acceptance_criteria>
  <done>The example app has an exact clean-checkout command path with one explicit PostgreSQL prerequisite and no hidden runtime state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser session -> mounted /billing UI | Only authenticated host admins may cross into admin functionality |
| mounted admin replay -> Accrue replay/audit APIs | Admin actions must leave durable audit evidence |
| clean checkout -> local boot | Docs must surface all prerequisites and avoid hidden state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | E | /billing mount | mitigate | Task 1 adds `billing_admin` plus `AccrueHost.Auth` and tests anonymous, non-admin, and admin outcomes at `/billing`. |
| T-10-18 | R | admin replay/audit | mitigate | Task 2 requires an `admin.webhook.replay.completed` ledger assertion, not only a flash message. |
| T-10-19 | I | secrets and local state docs | mitigate | Task 3 documents the exact commands and the Fake default `whsec_test_host` so no hidden secret or machine-local state is assumed. |
| T-10-20 | T | public API boundary bypass in admin proof | mitigate | Task 2 drives replay through the mounted admin UI and forbids replacing the proof with a direct DLQ call. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs test/accrue_host_web/admin_webhook_replay_test.exs` and `cd examples/accrue_host && mix ecto.create && mix ecto.migrate && mix test`.
</verification>

<success_criteria>
The mounted admin UI is realistically protected, one replay action is audited end to end, and the example app documents the exact clean-checkout path.
</success_criteria>

<output>
After completion, create `.planning/phases/10-host-app-dogfood-harness/10-05-SUMMARY.md`
</output>

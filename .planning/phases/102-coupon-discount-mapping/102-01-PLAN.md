---
phase: 102-coupon-discount-mapping
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/discount_mapping.ex
  - accrue/lib/accrue/billing/discount_mapping_actions.ex
  - accrue/lib/accrue/billing.ex
  - accrue/lib/accrue/errors.ex
  - accrue/priv/repo/migrations/*_create_accrue_discount_mappings.exs
  - accrue/test/accrue/billing/discount_mapping_actions_test.exs
autonomous: true
requirements:
  - BT-04
  - BT-05
must_haves:
  truths:
    - "Operators can persist local Braintree promotion-code mappings without pretending they are Stripe promotion-code projections."
    - "Core resolution returns explicit local-validation outcomes before any Braintree processor call."
    - "Redemption-cap fields are executable state, not write-only metadata."
    - "Phase 102 has an owned Wave 0 test lane for local mapping persistence and eligibility."
  artifacts:
    - path: "accrue/lib/accrue/billing/discount_mapping.ex"
      provides: "Canonical local mapping schema for Braintree promotion-code resolution"
      contains: "schema \"accrue_discount_mappings\""
    - path: "accrue/lib/accrue/billing/discount_mapping_actions.ex"
      provides: "Local mapping write/read/resolve helpers"
      contains: "def upsert_discount_mapping"
    - path: "accrue/lib/accrue/billing.ex"
      provides: "Facade-first local mapping API"
      contains: "def upsert_discount_mapping"
    - path: "accrue/test/accrue/billing/discount_mapping_actions_test.exs"
      provides: "Wave 0 BT-04 validation coverage"
      contains: "describe \"upsert_discount_mapping/2\""
  key_links:
    - from: "accrue/lib/accrue/billing.ex"
      to: "accrue/lib/accrue/billing/discount_mapping_actions.ex"
      via: "span_billing facade wrappers"
      pattern: "DiscountMappingActions\\."
    - from: "accrue/lib/accrue/billing/discount_mapping_actions.ex"
      to: "accrue/lib/accrue/billing/discount_mapping.ex"
      via: "changeset + Repo queries"
      pattern: "DiscountMapping"
    - from: "accrue/test/accrue/billing/discount_mapping_actions_test.exs"
      to: "accrue/lib/accrue/billing/discount_mapping_actions.ex"
      via: "targeted BT-04 tests"
      pattern: "Billing\\.(upsert|get|resolve)_discount_mapping"
---

<objective>
Establish the explicit local discount-mapping domain in `accrue`, including the honest facade write/read surface required by D-05/D-06/D-07 and the Wave 0 validation lane for BT-04.

Purpose: Give every downstream slice one canonical local mapping model instead of reusing Stripe projection semantics.
Output: Local mapping schema + migration, facade wrappers, typed drift error contract, and focused BT-04 tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.33-REQUIREMENTS.md
@.planning/phases/102-coupon-discount-mapping/102-CONTEXT.md
@.planning/phases/102-coupon-discount-mapping/102-RESEARCH.md
@.planning/phases/102-coupon-discount-mapping/102-VALIDATION.md
@.planning/phases/102-coupon-discount-mapping/102-PATTERNS.md
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/coupon_actions.ex
@accrue/lib/accrue/billing/promotion_code.ex
@accrue/lib/accrue/errors.ex

<interfaces>
From `accrue/lib/accrue/billing.ex`:
```elixir
def subscribe(user, price_id_or_opts \\ [], opts \\ []) do
  span_billing(:subscription, :create, user, opts, fn ->
    SubscriptionActions.subscribe(user, price_id_or_opts, opts)
  end)
end
```

From `accrue/lib/accrue/billing/coupon_actions.ex`:
```elixir
@type apply_error ::
        :not_found
        | :inactive
        | :expired
        | :max_redemptions_reached
        | :coupon_missing
        | term()
```

From `accrue/lib/accrue/billing/promotion_code.ex`:
```elixir
schema "accrue_promotion_codes" do
  field(:processor, :string, default: "stripe")
  field(:processor_id, :string)
  field(:code, :string)
  field(:max_redemptions, :integer)
  field(:times_redeemed, :integer, default: 0)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the explicit local discount-mapping contract and persistence layer</name>
  <files>accrue/lib/accrue/billing/discount_mapping.ex, accrue/lib/accrue/billing/discount_mapping_actions.ex, accrue/lib/accrue/billing.ex, accrue/lib/accrue/errors.ex, accrue/priv/repo/migrations/*_create_accrue_discount_mappings.exs</files>
  <read_first>.planning/phases/102-coupon-discount-mapping/102-CONTEXT.md, .planning/phases/102-coupon-discount-mapping/102-RESEARCH.md, accrue/lib/accrue/billing.ex, accrue/lib/accrue/billing/coupon_actions.ex, accrue/lib/accrue/billing/promotion_code.ex, accrue/lib/accrue/errors.ex</read_first>
  <behavior>
    - Test 1: `Accrue.Billing.upsert_discount_mapping/2` stores one canonical `code -> discount_id` row for processor `"braintree"` per D-01/D-02/D-06.
    - Test 2: `Accrue.Billing.get_discount_mapping/2` and `resolve_discount_mapping/3` never route through Stripe projection tables or `create_coupon/2`.
    - Test 3: Typed drift failures are represented by `%Accrue.Error.DiscountMappingInvalid{}` instead of generic invalid-request tuples per D-15/D-17.
  </behavior>
  <action>Create a new `Accrue.Billing.DiscountMapping` schema backed by `accrue_discount_mappings` with `@primary_key {:id, :binary_id, autogenerate: true}` and fields `processor` (default `"braintree"`), `code`, `discount_id`, `active`, `amount_off_minor`, `currency`, `duration_in_billing_cycles`, `expires_at`, `max_redemptions`, `times_redeemed`, `metadata`, `data`, and `lock_version`. Add a migration with unique indexes on `[:processor, :code]` and any lookup index needed for `discount_id`. Add `%Accrue.Error.DiscountMappingInvalid{mapping_id, code, discount_id, reason, message}` to `accrue/lib/accrue/errors.ex`. Extend `Accrue.Billing` with facade-first wrappers `upsert_discount_mapping/2`, `upsert_discount_mapping!/2`, `get_discount_mapping/2`, and `resolve_discount_mapping/3`, each wrapped in `span_billing/5`, per D-05/D-06/D-07. Keep the new actions local-canonical; do not call `Processor.__impl__().coupon_create/2` or reuse `Accrue.Billing.PromotionCode` as the canonical Braintree model.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Accrue\\.Billing\\.DiscountMapping' accrue/lib/accrue/billing/discount_mapping.ex` finds the new schema module.
    - `rg -n 'schema \"accrue_discount_mappings\"' accrue/lib/accrue/billing/discount_mapping.ex accrue/priv/repo/migrations/*_create_accrue_discount_mappings.exs` matches both schema and migration.
    - `rg -n 'def upsert_discount_mapping|def get_discount_mapping|def resolve_discount_mapping' accrue/lib/accrue/billing.ex accrue/lib/accrue/billing/discount_mapping_actions.ex` returns the new facade and action functions.
    - `rg -n 'defmodule Accrue\\.Error\\.DiscountMappingInvalid|defexception \\[:mapping_id, :code, :discount_id, :reason, :message\\]' accrue/lib/accrue/errors.ex` matches the typed operator-drift exception.
    - `rg -n 'create_coupon|create_promotion_code' accrue/lib/accrue/billing/discount_mapping_actions.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n 'defmodule Accrue\.Billing\.DiscountMapping|schema "accrue_discount_mappings"|def upsert_discount_mapping|def get_discount_mapping|def resolve_discount_mapping|defmodule Accrue\.Error\.DiscountMappingInvalid' accrue/lib/accrue/billing/discount_mapping.ex accrue/lib/accrue/billing/discount_mapping_actions.ex accrue/lib/accrue/billing.ex accrue/lib/accrue/errors.ex accrue/priv/repo/migrations/*_create_accrue_discount_mappings.exs</automated>
  </verify>
  <done>The repo has a dedicated local discount-mapping domain with an honest public facade and a typed drift-error contract, ready for validation and subscription-path wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement local eligibility resolution and Wave 0 BT-04 coverage</name>
  <files>accrue/lib/accrue/billing/discount_mapping_actions.ex, accrue/test/accrue/billing/discount_mapping_actions_test.exs</files>
  <read_first>.planning/phases/102-coupon-discount-mapping/102-VALIDATION.md, .planning/phases/102-coupon-discount-mapping/102-PATTERNS.md, accrue/lib/accrue/billing/discount_mapping_actions.ex, accrue/lib/accrue/billing/coupon_actions.ex, accrue/test/accrue/billing/coupon_actions_test.exs</read_first>
  <behavior>
    - Test 1: `resolve_discount_mapping/3` returns `{:error, :not_found | :inactive | :expired | :max_redemptions_reached}` before any processor call for D-09/D-14.
    - Test 2: Successful preview resolution returns enough local economics to compute savings and updated totals without calling Braintree, per D-10 and Pitfall 3.
    - Test 3: Redemption-cap semantics are honest: the action layer exposes a success-path helper that increments `times_redeemed` atomically instead of leaving `max_redemptions` write-only.
  </behavior>
  <action>Implement `Accrue.Billing.DiscountMappingActions` with `Repo.transact/1`-backed `upsert_discount_mapping/2`, `get_discount_mapping/2`, `resolve_discount_mapping/3`, and an explicit redemption-state helper such as `record_discount_mapping_redemption/2` or `consume_discount_mapping/2`. `resolve_discount_mapping/3` must normalize the submitted code, query only `processor: "braintree"`, enforce `active`, `expires_at`, and `max_redemptions` using the same explicit atom results as D-14, and return a preview payload that includes the `%DiscountMapping{}` row plus computed `amount_off_minor` and `estimated_total_minor` for the checkout amount passed from the caller. Keep `max_redemptions` honest by mutating `times_redeemed` in the helper used later by subscribe-time success. Add `accrue/test/accrue/billing/discount_mapping_actions_test.exs` as the Wave 0 owner for `102-W0-01` and `102-W0-05`, covering uniqueness, idempotent upsert, local validation branches, preview economics, and redemption mutation behavior. Use repo-local `TMPDIR` in every Mix verify command because `/var/folders/...` is currently exhausted in this environment.</action>
  <acceptance_criteria>
    - `rg -n 'def resolve_discount_mapping|def record_discount_mapping_redemption|def consume_discount_mapping' accrue/lib/accrue/billing/discount_mapping_actions.ex` finds the resolver and redemption helper.
    - `rg -n ':not_found|:inactive|:expired|:max_redemptions_reached' accrue/lib/accrue/billing/discount_mapping_actions.ex accrue/test/accrue/billing/discount_mapping_actions_test.exs` shows explicit user-domain failures.
    - `rg -n 'estimated_total_minor|amount_off_minor|times_redeemed' accrue/lib/accrue/billing/discount_mapping_actions.ex accrue/test/accrue/billing/discount_mapping_actions_test.exs` proves preview math and honest redemption accounting are implemented.
    - `rg -n 'describe \"upsert_discount_mapping/2\"|describe \"resolve_discount_mapping/3\"|describe \"record_discount_mapping_redemption' accrue/test/accrue/billing/discount_mapping_actions_test.exs` matches the Wave 0 test lanes.
    - The targeted BT-04 test command passes with the repo-local `TMPDIR` override.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mkdir -p /Users/jon/projects/accrue/.tmp/phase102 && TMPDIR=/Users/jon/projects/accrue/.tmp/phase102 mix test test/accrue/billing/discount_mapping_actions_test.exs</automated>
  </verify>
  <done>BT-04 has a real local mapping implementation, the validation lane `102-W0-01` is green, and redemption caps are executable state instead of dead fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator setup -> local discount mappings | Untrusted mapping attrs cross from setup callers into canonical billing state |
| checkout preview -> core resolver | User-submitted promo codes must be validated server-side, not trusted from LiveView assigns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | T | `DiscountMappingActions.upsert_discount_mapping/2` | mitigate | Validate all attrs with schema constraints and unique indexes; keep mappings in a new local table per D-01/D-05 instead of reusing Stripe projection rows. |
| T-102-04 | I | `resolve_discount_mapping/3` | mitigate | Return explicit server-side atoms for invalid codes while reserving `%Accrue.Error.DiscountMappingInvalid{}` for operator drift; no raw processor payloads or discount metadata in user-facing errors. |
| T-102-07 | R | `max_redemptions` state | mitigate | Add a dedicated redemption-state helper and test lane so successful applications mutate `times_redeemed` atomically instead of leaving caps advisory. |
</threat_model>

<verification>
Run the BT-04 lane with a repo-local temp directory:

```bash
cd /Users/jon/projects/accrue/accrue
mkdir -p /Users/jon/projects/accrue/.tmp/phase102
TMPDIR=/Users/jon/projects/accrue/.tmp/phase102 mix test test/accrue/billing/discount_mapping_actions_test.exs
```
</verification>

<success_criteria>
- `accrue_discount_mappings` exists as a dedicated schema/migration pair.
- `Accrue.Billing` exposes a facade-first local mapping API rather than `Accrue.Billing.Braintree.*`.
- `discount_mapping_actions_test.exs` exists and passes with the repo-local `TMPDIR` workaround.
- BT-04 and the redemption-cap part of BT-05 are covered without reusing Stripe projection semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/102-coupon-discount-mapping/102-01-SUMMARY.md`
</output>

---
phase: 12-first-user-dx-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/accrue_host_hex_smoke.sh
  - accrue/test/mix/tasks/accrue_install_test.exs
  - accrue/test/mix/tasks/accrue_install_uat_test.exs
autonomous: true
requirements: [DX-01, DX-07]
must_haves:
  truths:
    - "First-user installer reruns cannot silently clobber host changes because executable tests lock the expected summary categories, no-clobber behavior, and `.accrue/conflicts/` artifact contract."
    - "When installer conflicts happen, the user-visible conflict outputs are pinned behind executable tests so sidecar paths, headers, and artifact suffixes stay stable across reruns and remediation."
    - "Hex dependency-mode validation is locked to one named smoke script for the canonical `examples/accrue_host` app, so Phase 12 can prove host-app installation over Hex without introducing a second committed host project."
  artifacts:
    - path: "scripts/ci/accrue_host_hex_smoke.sh"
      provides: "Dedicated Hex-mode smoke command for the single canonical host app."
    - path: "accrue/test/mix/tasks/accrue_install_test.exs"
      provides: "Installer unit contract for no-clobber reruns and `--write-conflicts` output."
    - path: "accrue/test/mix/tasks/accrue_install_uat_test.exs"
      provides: "Installer UAT contract covering categorized summary output and host-visible conflict artifacts."
  key_links:
    - from: "scripts/ci/accrue_host_hex_smoke.sh"
      to: "examples/accrue_host/mix.exs"
      via: "future `ACCRUE_HOST_HEX_RELEASE=1` mode switch"
      pattern: "ACCRUE_HOST_HEX_RELEASE"
    - from: "accrue/test/mix/tasks/accrue_install_test.exs"
      to: "accrue/lib/mix/tasks/accrue.install.ex"
      via: "future summary and conflict-contract assertions"
      pattern: "write-conflicts|updated pristine|skipped user-edited"
---

<objective>
Create the missing Wave 1 validation scaffolds for installer rerun semantics and Hex-style host validation.

Purpose: Satisfy the remaining `12-VALIDATION.md` gaps before the implementation plans change installer behavior or host dependency mode.
Output: A named Hex smoke script scaffold plus installer tests that reserve the conflict-artifact and summary taxonomy.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
@.planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
@accrue/lib/mix/tasks/accrue.install.ex
@accrue/lib/accrue/install/options.ex
@accrue/lib/accrue/install/fingerprints.ex
@accrue/lib/accrue/install/patches.ex
@accrue/test/mix/tasks/accrue_install_test.exs
@accrue/test/mix/tasks/accrue_install_uat_test.exs
@examples/accrue_host/mix.exs
@scripts/ci/accrue_host_uat.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Hex-mode smoke script scaffold</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    scripts/ci/accrue_host_uat.sh
    examples/accrue_host/mix.exs
    scripts/ci/accrue_host_hex_smoke.sh
  </read_first>
  <files>scripts/ci/accrue_host_hex_smoke.sh</files>
  <acceptance_criteria>
    - `test -x scripts/ci/accrue_host_hex_smoke.sh` succeeds.
    - `rg -n 'ACCRUE_HOST_HEX_RELEASE=1' scripts/ci/accrue_host_hex_smoke.sh` finds the exact env flag from D-17.
    - `rg -n 'mix deps.get|mix accrue.install|mix compile --warnings-as-errors|mix ecto.migrate|test/install_boundary_test\\.exs|test/accrue_host/billing_facade_test\\.exs' scripts/ci/accrue_host_hex_smoke.sh` finds the exact smoke steps from D-18.
    - `rg -n 'Phase 12 plan 08 activates Hex smoke' scripts/ci/accrue_host_hex_smoke.sh` finds the scaffold activation message.
  </acceptance_criteria>
  <action>Create `scripts/ci/accrue_host_hex_smoke.sh` as an executable bash script with `set -euo pipefail`, `cd examples/accrue_host`, and the literal environment prefix `export ACCRUE_HOST_HEX_RELEASE=1`. Keep the script in scaffold mode for Wave 1 by printing the literal line `Phase 12 plan 08 activates Hex smoke` and exiting 0 before the command block runs, but include the real future command sequence as commented commands in this exact order: `mix deps.get`, `mix accrue.install`, `mix compile --warnings-as-errors`, `MIX_ENV=test mix ecto.create`, `MIX_ENV=test mix ecto.migrate`, and `MIX_ENV=test mix test --warnings-as-errors test/install_boundary_test.exs test/accrue_host/billing_facade_test.exs`. Do not add a second example app or any alternate project root; the script must target `examples/accrue_host` only per D-16.</action>
  <verify>
    <automated>bash scripts/ci/accrue_host_hex_smoke.sh</automated>
  </verify>
  <done>The repo has a named Hex smoke entrypoint with the exact future command order and env flag, ready for activation in the dependency-mode plan.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve the installer conflict-artifact and summary taxonomy in tests</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    accrue/lib/mix/tasks/accrue.install.ex
    accrue/lib/accrue/install/options.ex
    accrue/lib/accrue/install/fingerprints.ex
    accrue/lib/accrue/install/patches.ex
    accrue/test/mix/tasks/accrue_install_test.exs
    accrue/test/mix/tasks/accrue_install_uat_test.exs
  </read_first>
  <files>accrue/test/mix/tasks/accrue_install_test.exs, accrue/test/mix/tasks/accrue_install_uat_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'updated pristine|skipped user-edited|skipped exists|manual|conflict artifact' accrue/test/mix/tasks/accrue_install_test.exs accrue/test/mix/tasks/accrue_install_uat_test.exs` finds the exact summary labels from D-05.
    - `rg -n '\\.accrue/conflicts/' accrue/test/mix/tasks/accrue_install_test.exs accrue/test/mix/tasks/accrue_install_uat_test.exs` finds the exact conflict-artifact root from D-04.
    - `rg -n '--write-conflicts' accrue/test/mix/tasks/accrue_install_test.exs accrue/test/mix/tasks/accrue_install_uat_test.exs` finds the flag under test.
    - `rg -n 'Phase 12 plan 03 activates this installer contract' accrue/test/mix/tasks/accrue_install_test.exs accrue/test/mix/tasks/accrue_install_uat_test.exs` finds the scaffold activation reason.
  </acceptance_criteria>
  <action>Expand `accrue/test/mix/tasks/accrue_install_test.exs` and `accrue/test/mix/tasks/accrue_install_uat_test.exs` with compile-safe skipped tests that reserve the exact installer contract for D-01 through D-05. Use the literal skip reason `Phase 12 plan 03 activates this installer contract`. In the skipped assertions, require these exact per-result labels in task output: `created`, `updated pristine`, `skipped user-edited`, `skipped exists`, `manual`, and `conflict artifact`. Require `--force` to overwrite only unmarked files and never overwrite stamped user-edited files. Require `--write-conflicts` to create sidecar files under `.accrue/conflicts/` and to include `target:` plus `reason:` headers inside the artifact body. Reserve two artifact suffixes explicitly: `.new` for full rendered replacements and `.snippet` for patch/manual snippets. Keep all new tests in the existing installer test modules instead of creating a new test file, so plan 03 can activate them in place.</action>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/mix/tasks/accrue_install_uat_test.exs</automated>
  </verify>
  <done>The installer validation surface now names the exact conflict directory, exact summary labels, and exact flag behavior that the implementation plan must satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer tests -> generated host files | The no-clobber contract controls writes into host-owned files and sidecar artifacts |
| Hex smoke script -> host app dependency mode | The single canonical host app will later switch between path and Hex dependencies via an env flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | installer test contract | mitigate | Reserve exact assertions for `updated pristine`, `skipped user-edited`, and `.accrue/conflicts/` so later implementation cannot clobber host-edited files silently. |
| T-12-02-02 | Information Disclosure | future conflict artifacts | mitigate | Reserve artifact headers and locations only; do not place artifacts beside live `.ex` or `.exs` files and do not encode secret values in the scaffold. |
| T-12-02-03 | Security Misconfiguration | Hex smoke scaffold | mitigate | Keep the script pinned to `examples/accrue_host` with `ACCRUE_HOST_HEX_RELEASE=1`; do not introduce a second checked-in app or alternate install path. |
</threat_model>

<verification>
- `bash scripts/ci/accrue_host_hex_smoke.sh`
- `cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/mix/tasks/accrue_install_uat_test.exs`
</verification>

<success_criteria>
- Both remaining Wave 1 validation scaffold artifacts named by `12-VALIDATION.md` exist.
- The installer tests reserve exact conflict-artifact paths and exact summary labels.
- The Hex smoke scaffold reserves the exact env flag and command order that the implementation plan will activate.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-02-SUMMARY.md`
</output>

---
phase: 12-first-user-dx-stabilization
plan: 07
type: execute
wave: 5
depends_on: [12-01, 12-06]
files_modified:
  - accrue/mix.exs
  - accrue_admin/mix.exs
  - accrue_admin/README.md
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [DX-06]
must_haves:
  truths:
    - "Package version snippets, source refs, and HexDocs guide links stay on the real published package metadata."
    - "Package-doc drift is caught by one strict verifier command rather than manual review."
    - "The metadata verifier checks the docs set produced by plan 06 instead of relying on stale hardcoded expectations."
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Strict package-doc drift verifier activated against current package metadata."
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ExUnit wrapper that proves the strict verifier succeeds."
    - path: "accrue/mix.exs"
      provides: "Current Accrue version and ExDoc source-ref metadata."
    - path: "accrue_admin/mix.exs"
      provides: "Current Accrue Admin version and ExDoc source-ref metadata."
    - path: "accrue_admin/README.md"
      provides: "Admin package install snippet aligned with current versions and guide links."
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/mix.exs"
      via: "version and source_ref parsing"
      pattern: "@version|source_ref"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue_admin/mix.exs"
      via: "admin package version and source_ref parsing"
      pattern: "@version|source_ref"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "System.cmd/3 shell verification"
      pattern: "verify_package_docs\\.sh"
---

<objective>
Correct package metadata surfaces and turn the package-doc verifier fully strict.

Purpose: Deliver DX-06 by making package README snippets, ExDoc source refs, and HexDocs links version-correct and continuously verifiable.
Output: Updated package metadata/readme surfaces, a strict package-doc verifier script, and an activated ExUnit wrapper.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
@.planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
@accrue/mix.exs
@accrue_admin/mix.exs
@accrue_admin/README.md
@scripts/ci/verify_package_docs.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct package metadata surfaces and activate the strict docs verifier</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
    accrue/mix.exs
    accrue_admin/mix.exs
    accrue_admin/README.md
    scripts/ci/verify_package_docs.sh
    accrue/test/accrue/docs/package_docs_verifier_test.exs
    accrue/README.md
    accrue/guides/quickstart.md
    accrue/guides/first_hour.md
    accrue/guides/troubleshooting.md
    accrue/guides/webhooks.md
    accrue_admin/guides/admin_ui.md
  </read_first>
  <files>accrue/mix.exs, accrue_admin/mix.exs, accrue_admin/README.md, scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'source_ref: \"accrue-v#\\{@version\\}\"' accrue/mix.exs` succeeds.
    - `rg -n 'source_ref: \"accrue_admin-v#\\{@version\\}\"' accrue_admin/mix.exs` succeeds.
    - `rg -n '~> 0\\.1\\.2' accrue_admin/README.md` finds the current package version instead of stale `~> 1.0.0`.
    - `rg -n 'hexdocs\\.pm/accrue|hexdocs\\.pm/accrue_admin|guides/first_hour\\.md|guides/troubleshooting\\.md|guides/webhooks\\.md|guides/admin_ui\\.md' scripts/ci/verify_package_docs.sh` finds strict link checks.
    - `rg -n 'Phase 12 plan 07 activates package-doc drift checks' scripts/ci/verify_package_docs.sh` returns no matches.
    - `rg -n 'verify_package_docs\\.sh' accrue/test/accrue/docs/package_docs_verifier_test.exs` finds the strict script invocation with no scaffold-message assertion.
  </acceptance_criteria>
  <action>Keep `accrue/mix.exs` and `accrue_admin/mix.exs` on the current `@version "0.1.2"` metadata and verify their ExDoc config still uses `source_ref: "accrue-v#{@version}"` and `source_ref: "accrue_admin-v#{@version}"`. Update stale package install/link snippets in `accrue_admin/README.md` to `~> 0.1.2` and the real HexDocs destinations. Replace the Wave 1 scaffold in `scripts/ci/verify_package_docs.sh` with real checks that parse versions from `accrue/mix.exs` and `accrue_admin/mix.exs`, verify source-ref tag shapes, verify HexDocs links for both packages, and verify relative guide links to `guides/first_hour.md`, `guides/troubleshooting.md`, `guides/webhooks.md`, and `guides/admin_ui.md`. Update `accrue/test/accrue/docs/package_docs_verifier_test.exs` so it asserts the strict script succeeds without relying on the scaffold activation message.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs</automated>
  </verify>
  <done>The package docs and metadata surfaces are version-correct and self-verified by a strict repo script plus an ExUnit wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package metadata -> HexDocs/source links | Incorrect source refs or install snippets mislead users during evaluation and release validation |
| verifier script -> package docs | The script becomes the canonical gate for docs/version drift across package surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Repudiation | package-doc verification script | mitigate | Parse versions and source refs directly from `mix.exs` so README and HexDocs drift is caught automatically instead of by manual review. |
| T-12-07-02 | Tampering | package README and ExDoc metadata | mitigate | Keep source-ref and version expectations derived from package metadata rather than hardcoded strings spread across docs. |
| T-12-07-03 | Information Disclosure | verifier output | mitigate | Limit the script to versions, source refs, and links; do not emit secrets or environment-specific values. |
</threat_model>

<verification>
- `bash scripts/ci/verify_package_docs.sh`
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs`
</verification>

<success_criteria>
- Package snippets, source refs, and HexDocs links are correct and continuously verifiable.
- The verifier checks the docs generated by plan 06 instead of relying on scaffold placeholders.
- DX-06 is covered by a single strict command plus an ExUnit wrapper.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-07-SUMMARY.md`
</output>

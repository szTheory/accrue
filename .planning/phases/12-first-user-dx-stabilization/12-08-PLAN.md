---
phase: 12-first-user-dx-stabilization
plan: 08
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - examples/accrue_host/mix.exs
  - scripts/ci/accrue_host_hex_smoke.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [DX-07]
must_haves:
  truths:
    - "The single canonical host app can switch between path dependencies and Hex-style dependencies without duplicating the app."
    - "Hex-mode validation is a focused smoke that reruns installer, compile, migrations, and narrow host proofs."
    - "Path-mode UAT remains the primary release gate while Hex mode becomes an additional package-correctness check."
  artifacts:
    - path: "examples/accrue_host/mix.exs"
      provides: "Environment-switched dependency declarations for path vs Hex mode."
    - path: "scripts/ci/accrue_host_hex_smoke.sh"
      provides: "Activated Hex smoke command for the canonical host app."
    - path: ".github/workflows/ci.yml"
      provides: "CI wiring that keeps path UAT primary and runs Hex smoke as a focused package validation."
  key_links:
    - from: "examples/accrue_host/mix.exs"
      to: "scripts/ci/accrue_host_hex_smoke.sh"
      via: "ACCRUE_HOST_HEX_RELEASE=1"
      pattern: "ACCRUE_HOST_HEX_RELEASE"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/accrue_host_hex_smoke.sh"
      via: "dedicated workflow step"
      pattern: "accrue_host_hex_smoke\\.sh"
---

<objective>
Implement the host dependency-mode switch and activate the Hex-style smoke in CI without creating a second example app.

Purpose: Deliver DX-07 while keeping the path-mode host UAT as the primary gate and using Hex mode as a focused package correctness proof.
Output: Env-switched host deps, a live Hex smoke script, and CI wiring for the new validation path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
@.planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
@examples/accrue_host/mix.exs
@scripts/ci/accrue_host_uat.sh
@scripts/ci/accrue_host_hex_smoke.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the host dependency-mode switch in the canonical example app</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-PATTERNS.md
    examples/accrue_host/mix.exs
    accrue/mix.exs
    accrue_admin/mix.exs
  </read_first>
  <files>examples/accrue_host/mix.exs</files>
  <acceptance_criteria>
    - `rg -n 'ACCRUE_HOST_HEX_RELEASE' examples/accrue_host/mix.exs` finds the exact env flag from D-17.
    - `rg -n 'defp accrue_dep|defp accrue_admin_dep' examples/accrue_host/mix.exs` finds dedicated helpers for the two package deps.
    - `rg -n 'path: \"\\.\\./\\.\\./accrue\"|path: \"\\.\\./\\.\\./accrue_admin\"' examples/accrue_host/mix.exs` finds the default monorepo path mode.
    - `rg -n '~> #\\{accrue_version\\(\\)\\}|~> #\\{accrue_admin_version\\(\\)\\}' examples/accrue_host/mix.exs` finds the Hex-mode version sync.
  </acceptance_criteria>
  <action>Refactor `examples/accrue_host/mix.exs` so the `:accrue` and `:accrue_admin` dependency declarations come from helper functions `accrue_dep/0` and `accrue_admin_dep/0`. Use `System.get_env("ACCRUE_HOST_HEX_RELEASE") == "1"` as the exact switch. In path mode, keep the existing sibling `path: "../../accrue"` and `path: "../../accrue_admin"` declarations. In Hex mode, return `{:accrue, "~> #{accrue_version()}"}` and `{:accrue_admin, "~> #{accrue_admin_version()}"}`. Implement `accrue_version/0` and `accrue_admin_version/0` by parsing the sibling package `@version` from `../../accrue/mix.exs` and `../../accrue_admin/mix.exs` with a regex, so the example app does not hardcode stale version strings.</action>
  <verify>
    <automated>cd examples/accrue_host && mix deps.get && mix compile --warnings-as-errors</automated>
  </verify>
  <done>The canonical host app can now resolve either local sibling packages or the published Hex versions from one checked-in `mix.exs` file.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the Hex smoke script and wire it into CI</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VALIDATION.md
    scripts/ci/accrue_host_hex_smoke.sh
    scripts/ci/accrue_host_uat.sh
    .github/workflows/ci.yml
    examples/accrue_host/mix.exs
  </read_first>
  <files>scripts/ci/accrue_host_hex_smoke.sh, .github/workflows/ci.yml</files>
  <acceptance_criteria>
    - `rg -n 'Phase 12 plan 08 activates Hex smoke' scripts/ci/accrue_host_hex_smoke.sh` returns no matches.
    - `rg -n 'mix deps.get|mix accrue.install|mix compile --warnings-as-errors|MIX_ENV=test mix ecto.create|MIX_ENV=test mix ecto.migrate|test/install_boundary_test\\.exs test/accrue_host/billing_facade_test\\.exs' scripts/ci/accrue_host_hex_smoke.sh` finds the exact smoke command order from D-18.
    - `rg -n 'scripts/ci/accrue_host_uat\\.sh' .github/workflows/ci.yml` still finds the primary path-mode gate from D-19.
    - `rg -n 'scripts/ci/accrue_host_hex_smoke\\.sh' .github/workflows/ci.yml` finds the new Hex-mode validation step.
  </acceptance_criteria>
  <action>Replace the scaffold body in `scripts/ci/accrue_host_hex_smoke.sh` with the real command sequence reserved in plan 02. The script must: export `ACCRUE_HOST_HEX_RELEASE=1`, run `mix deps.get`, rerun `mix accrue.install`, run `mix compile --warnings-as-errors`, run `MIX_ENV=test mix ecto.create`, run `MIX_ENV=test mix ecto.migrate`, and then run `MIX_ENV=test mix test --warnings-as-errors test/install_boundary_test.exs test/accrue_host/billing_facade_test.exs`. Update `.github/workflows/ci.yml` so the existing path-mode host UAT step remains intact and the Hex smoke runs as an additional focused package-validation step, not a replacement for the full path-mode UAT. Do not add a second host app, a second workflow file, or a second broad integration matrix.</action>
  <verify>
    <automated>bash scripts/ci/accrue_host_hex_smoke.sh</automated>
  </verify>
  <done>The repo now proves both dependency modes on the same host app, and CI keeps path-mode UAT primary while running Hex mode as a focused smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env flag -> host dependency resolution | CI and local scripts choose whether the host app pulls sibling path deps or published Hex packages |
| host install/compile/migrate -> published package surface | Hex mode proves the published packages still satisfy the same host install path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08-01 | Security Misconfiguration | `examples/accrue_host/mix.exs` | mitigate | Keep one canonical host app and gate Hex mode only through `ACCRUE_HOST_HEX_RELEASE=1`; do not create a second committed app or alternate docs path. |
| T-12-08-02 | Denial of Service | `scripts/ci/accrue_host_hex_smoke.sh` | mitigate | Keep Hex validation narrow: `deps.get`, installer rerun, compile, migrate, and the two focused proof files only, per D-18. |
| T-12-08-03 | Repudiation | `.github/workflows/ci.yml` | mitigate | Preserve the existing path-mode UAT step as the primary gate and add Hex smoke as a separate package-correctness proof so failures are attributable to the right surface. |
</threat_model>

<verification>
- `cd examples/accrue_host && mix deps.get && mix compile --warnings-as-errors`
- `bash scripts/ci/accrue_host_hex_smoke.sh`
</verification>

<success_criteria>
- `examples/accrue_host` can switch between path and Hex dependency modes from one file.
- The Hex smoke script proves installer rerun, compile, migrations, and narrow host proofs in Hex mode.
- CI runs both the primary path-mode UAT and the focused Hex-mode smoke without splitting the host app.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-08-SUMMARY.md`
</output>

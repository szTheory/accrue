---
phase: 12-first-user-dx-stabilization
plan: 09
type: execute
wave: 6
depends_on: [12-05]
files_modified:
  - accrue/lib/mix/tasks/accrue.install.ex
  - accrue/test/mix/tasks/accrue_install_test.exs
  - accrue/test/mix/tasks/accrue_install_uat_test.exs
  - accrue/lib/accrue/config.ex
  - accrue/lib/accrue/setup_diagnostic.ex
  - accrue/test/accrue/config_test.exs
autonomous: true
requirements: [DX-02]
gap_closure: true
must_haves:
  truths:
    - "A valid Phoenix router that keeps the webhook route isolated from browser and auth pipelines passes `mix accrue.install --check`."
    - "Migration lookup failures no longer report success; expected setup failures raise `Accrue.ConfigError` with a stable setup diagnostic instead of returning `:ok`."
    - "Unexpected migration inspection failures still fail loud instead of being swallowed by a broad rescue."
  artifacts:
    - path: "accrue/lib/mix/tasks/accrue.install.ex"
      provides: "Scope-aware webhook pipeline preflight that inspects the actual webhook route context."
    - path: "accrue/lib/accrue/config.ex"
      provides: "Boot-time migration validation that maps expected lookup failures to a shared setup diagnostic."
    - path: "accrue/lib/accrue/setup_diagnostic.ex"
      provides: "Shared `ACCRUE-DX-MIGRATIONS-PENDING` diagnostic reused by boot validation."
    - path: "accrue/test/mix/tasks/accrue_install_test.exs"
      provides: "Installer regression coverage proving valid routers do not trip `ACCRUE-DX-WEBHOOK-PIPELINE`."
    - path: "accrue/test/accrue/config_test.exs"
      provides: "Config regression coverage proving migration lookup failures cannot silently return `:ok`."
  key_links:
    - from: "accrue/lib/mix/tasks/accrue.install.ex"
      to: "accrue/test/mix/tasks/accrue_install_test.exs"
      via: "router preflight regression cases"
      pattern: "ACCRUE-DX-WEBHOOK-PIPELINE"
    - from: "accrue/lib/accrue/config.ex"
      to: "accrue/lib/accrue/setup_diagnostic.ex"
      via: "shared migration diagnostic constructor"
      pattern: "ACCRUE-DX-MIGRATIONS-PENDING"
    - from: "accrue/lib/accrue/config.ex"
      to: "accrue/test/accrue/config_test.exs"
      via: "migration lookup failure assertions"
      pattern: "ensure_migrations_current!"
---

<objective>
Close the trustworthiness gaps in setup diagnostics so installer preflight and boot validation stop reporting false success or false failure.

Purpose: Finish DX-02 per D-06, D-07, D-09, and D-10 by making the router preflight scope-aware and turning migration lookup failures into explicit setup diagnostics.
Output: Tightened installer `--check` behavior, shared migration diagnostics, and focused regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-VERIFICATION.md
@.planning/phases/12-first-user-dx-stabilization/12-REVIEW.md
@.planning/phases/12-first-user-dx-stabilization/12-05-PLAN.md
@accrue/lib/mix/tasks/accrue.install.ex
@accrue/lib/accrue/config.ex
@accrue/lib/accrue/setup_diagnostic.ex
@accrue/test/mix/tasks/accrue_install_test.exs
@accrue/test/mix/tasks/accrue_install_uat_test.exs
@accrue/test/accrue/config_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make webhook pipeline preflight inspect the webhook scope, not unrelated router pipelines</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VERIFICATION.md
    .planning/phases/12-first-user-dx-stabilization/12-REVIEW.md
    .planning/phases/12-first-user-dx-stabilization/12-05-PLAN.md
    accrue/lib/mix/tasks/accrue.install.ex
    accrue/test/mix/tasks/accrue_install_test.exs
    accrue/test/mix/tasks/accrue_install_uat_test.exs
    examples/accrue_host/lib/accrue_host_web/router.ex
  </read_first>
  <files>accrue/lib/mix/tasks/accrue.install.ex, accrue/test/mix/tasks/accrue_install_test.exs, accrue/test/mix/tasks/accrue_install_uat_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'webhook_pipeline_misused\\?' accrue/lib/mix/tasks/accrue.install.ex` still finds the preflight entry point, but its implementation is keyed to the webhook scope or parsed route context instead of router-wide string matching, per D-09.
    - `rg -n 'ACCRUE-DX-WEBHOOK-PIPELINE' accrue/test/mix/tasks/accrue_install_test.exs accrue/test/mix/tasks/accrue_install_uat_test.exs` finds regression coverage for both bad and valid router shapes.
    - `rg -n 'protect_from_forgery|require_authenticated_user|:accrue_webhook_raw_body|accrue_webhook \"/webhooks/stripe\", :stripe' accrue/test/mix/tasks/accrue_install_test.exs accrue/test/mix/tasks/accrue_install_uat_test.exs` finds the exact route/pipeline fixtures under test.
    - A valid router with unrelated browser or auth pipelines does not fail `mix accrue.install --check` with `ACCRUE-DX-WEBHOOK-PIPELINE`.
  </acceptance_criteria>
  <action>Per D-09 and the blocker in `12-VERIFICATION.md`, replace the router-wide string heuristic in `accrue/lib/mix/tasks/accrue.install.ex` with a scope-aware inspection of the webhook route. Anchor the check to the actual `accrue_webhook "/webhooks/stripe", :stripe` placement and the enclosing `pipe_through(:accrue_webhook_raw_body)` scope, using a parsed or bounded-scope approach rather than scanning the entire router for browser/auth strings. Preserve the existing `ACCRUE-DX-WEBHOOK-PIPELINE` diagnostic code from plan 12-05. Update installer tests to cover both failure and pass cases explicitly: one router where the webhook route is actually behind a browser/auth pipeline should still fail, and one router shaped like `examples/accrue_host/lib/accrue_host_web/router.ex` with unrelated browser/auth pipelines elsewhere should pass `mix accrue.install --check`.</action>
  <verify>
    <automated>cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/mix/tasks/accrue_install_uat_test.exs</automated>
  </verify>
  <done>`mix accrue.install --check` only emits `ACCRUE-DX-WEBHOOK-PIPELINE` when the webhook route itself is mis-scoped, not when unrelated router pipelines exist elsewhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn migration lookup failures into explicit setup diagnostics and fail loud on unexpected exceptions</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VERIFICATION.md
    .planning/phases/12-first-user-dx-stabilization/12-REVIEW.md
    .planning/phases/12-first-user-dx-stabilization/12-05-PLAN.md
    accrue/lib/accrue/config.ex
    accrue/lib/accrue/setup_diagnostic.ex
    accrue/test/accrue/config_test.exs
  </read_first>
  <files>accrue/lib/accrue/config.ex, accrue/lib/accrue/setup_diagnostic.ex, accrue/test/accrue/config_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'ACCRUE-DX-MIGRATIONS-PENDING' accrue/lib/accrue/setup_diagnostic.ex accrue/lib/accrue/config.ex accrue/test/accrue/config_test.exs` finds the shared diagnostic wired through runtime validation, per D-06 and D-07.
    - `rg -n 'rescue' accrue/lib/accrue/config.ex` no longer shows a catch-all rescue path that converts every `Ecto.Migrator.migrations/0` failure into `:ok`.
    - `rg -n 'Accrue\\.ConfigError' accrue/test/accrue/config_test.exs` finds a regression asserting expected migration lookup failures raise the setup error contract.
    - A migration lookup failure does not return `:ok`, and an unexpected exception still surfaces instead of being swallowed.
  </acceptance_criteria>
  <action>Per D-06, D-07, and D-10, narrow `ensure_migrations_current!/1` in `accrue/lib/accrue/config.ex` so expected migration inspection failures are converted into `Accrue.ConfigError` carrying the shared `ACCRUE-DX-MIGRATIONS-PENDING` diagnostic, while unexpected failures are allowed to raise normally. Reuse the existing setup-diagnostic formatting path from plan 12-05 rather than inventing a new error shape. Keep details redacted and actionable: mention the migration inspection problem without leaking secrets or environment values. Update `accrue/test/accrue/config_test.exs` to prove three concrete cases: pending migrations still raise the shared diagnostic, migration lookup failures no longer return `:ok`, and an unexpected exception is not suppressed.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/config_test.exs</automated>
  </verify>
  <done>Boot validation now reports migration inspection failures through `Accrue.ConfigError` with `ACCRUE-DX-MIGRATIONS-PENDING`, and silent success from a broad rescue is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host router text -> installer preflight | Untrusted host router structure is inspected to decide whether webhook wiring is safe |
| repo/migration adapter errors -> boot diagnostics | Runtime failures from database migration inspection cross into user-facing setup errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09-01 | Security Misconfiguration | `accrue/lib/mix/tasks/accrue.install.ex` | mitigate | Scope the webhook pipeline check to the actual webhook route context so valid routers do not get blocked and truly mis-scoped routes still emit `ACCRUE-DX-WEBHOOK-PIPELINE`. |
| T-12-09-02 | Information Disclosure | `accrue/lib/accrue/config.ex` | mitigate | Keep migration failure diagnostics on `Accrue.ConfigError` with redacted details and a stable code instead of leaking raw adapter/env information. |
| T-12-09-03 | Tampering | installer/config tests | mitigate | Add regression tests that lock the valid-router pass case and the no-silent-`:ok` migration failure case so the diagnostic contract cannot drift back. |
| T-12-09-04 | Denial of Service | boot validation | accept | Failing loud on unexpected migration inspection exceptions can stop boot, but that is the correct outcome for a broken setup path and avoids false-green startup. |
</threat_model>

<verification>
- `cd accrue && mix test test/mix/tasks/accrue_install_test.exs test/mix/tasks/accrue_install_uat_test.exs`
- `cd accrue && mix test test/accrue/config_test.exs`
- `cd examples/accrue_host && mix accrue.install --check`
</verification>

<success_criteria>
- Valid routers with unrelated browser/auth pipelines no longer fail installer preflight.
- Migration lookup failures no longer return `:ok`.
- Expected migration inspection failures use the shared `ACCRUE-DX-MIGRATIONS-PENDING` setup diagnostic.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-09-SUMMARY.md`
</output>

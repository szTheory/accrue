---
phase: 12-first-user-dx-stabilization
plan: 10
type: execute
wave: 6
depends_on: [12-05, 12-06, 12-07]
files_modified:
  - accrue/guides/first_hour.md
  - accrue/guides/troubleshooting.md
  - accrue/test/accrue/docs/first_hour_guide_test.exs
  - accrue/test/accrue/docs/troubleshooting_guide_test.exs
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [DX-03, DX-04, DX-06]
gap_closure: true
must_haves:
  truths:
    - "The First Hour and troubleshooting guides both document the runtime key `config :accrue, :webhook_signing_secrets, %{stripe: ...}` instead of the dead singular key."
    - "Docs verification fails automatically if published guides drift back to singular `webhook_signing_secret`."
    - "Package/docs verification remains aligned with the host-first guides and runtime config contract."
  artifacts:
    - path: "accrue/guides/first_hour.md"
      provides: "Canonical first-hour runtime config snippet with `:webhook_signing_secrets`."
    - path: "accrue/guides/troubleshooting.md"
      provides: "Troubleshooting fix text that points users at the real runtime config key."
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Automated docs/package drift checks that reject singular `webhook_signing_secret` in published guides."
    - path: "accrue/test/accrue/docs/first_hour_guide_test.exs"
      provides: "Guide contract asserting the correct webhook signing secrets config shape."
    - path: "accrue/test/accrue/docs/troubleshooting_guide_test.exs"
      provides: "Troubleshooting contract asserting the correct webhook secret remediation text."
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ExUnit wrapper proving the strict docs verifier catches config-key drift."
  key_links:
    - from: "accrue/guides/first_hour.md"
      to: "accrue/test/accrue/docs/first_hour_guide_test.exs"
      via: "runtime config contract assertions"
      pattern: "webhook_signing_secrets"
    - from: "accrue/guides/troubleshooting.md"
      to: "accrue/test/accrue/docs/troubleshooting_guide_test.exs"
      via: "diagnostic fix assertions"
      pattern: "ACCRUE-DX-WEBHOOK-SECRET-MISSING"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      via: "strict shell verifier wrapper"
      pattern: "verify_package_docs\\.sh"
---

<objective>
Close the remaining docs/runtime drift so the published setup path teaches the real webhook signing config and the verifier blocks regressions.

Purpose: Finish DX-03, DX-04, and the docs-verification extension of DX-06 by aligning the published guides with the runtime contract from D-11 through D-15 and D-20.
Output: Corrected guide snippets plus stricter docs/package verification that rejects singular `webhook_signing_secret`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
@.planning/phases/12-first-user-dx-stabilization/12-RESEARCH.md
@.planning/phases/12-first-user-dx-stabilization/12-VERIFICATION.md
@.planning/phases/12-first-user-dx-stabilization/12-REVIEW.md
@.planning/phases/12-first-user-dx-stabilization/12-06-PLAN.md
@.planning/phases/12-first-user-dx-stabilization/12-07-PLAN.md
@accrue/guides/first_hour.md
@accrue/guides/troubleshooting.md
@accrue/test/accrue/docs/first_hour_guide_test.exs
@accrue/test/accrue/docs/troubleshooting_guide_test.exs
@scripts/ci/verify_package_docs.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the published guide snippets to use `:webhook_signing_secrets`</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VERIFICATION.md
    .planning/phases/12-first-user-dx-stabilization/12-REVIEW.md
    .planning/phases/12-first-user-dx-stabilization/12-06-PLAN.md
    accrue/guides/first_hour.md
    accrue/guides/troubleshooting.md
    accrue/test/accrue/docs/first_hour_guide_test.exs
    accrue/test/accrue/docs/troubleshooting_guide_test.exs
  </read_first>
  <files>accrue/guides/first_hour.md, accrue/guides/troubleshooting.md, accrue/test/accrue/docs/first_hour_guide_test.exs, accrue/test/accrue/docs/troubleshooting_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'config :accrue, :webhook_signing_secrets, %\\{' accrue/guides/first_hour.md accrue/guides/troubleshooting.md` finds the real runtime key and map shape in both guides.
    - `rg -n 'stripe: System.get_env\\(\"STRIPE_WEBHOOK_SECRET\", \"whsec_test_host\"\\)' accrue/guides/first_hour.md accrue/guides/troubleshooting.md` finds the exact Stripe example or an equivalent multiline Elixir config snippet from the gap scope.
    - `rg -n 'webhook_signing_secret' accrue/guides/first_hour.md accrue/guides/troubleshooting.md` returns no matches.
    - `rg -n 'webhook_signing_secrets|ACCRUE-DX-WEBHOOK-SECRET-MISSING' accrue/test/accrue/docs/first_hour_guide_test.exs accrue/test/accrue/docs/troubleshooting_guide_test.exs` finds explicit docs-contract assertions for the corrected key, per D-12 and D-13.
  </acceptance_criteria>
  <action>Per D-12, D-13, and the blocker in `12-VERIFICATION.md`, update `accrue/guides/first_hour.md` and `accrue/guides/troubleshooting.md` so both published paths teach the exact runtime config key `:webhook_signing_secrets`. Use the concrete config snippet `config :accrue, :webhook_signing_secrets, %{stripe: System.get_env("STRIPE_WEBHOOK_SECRET", "whsec_test_host")}` or the equivalent multi-line Elixir form, and remove every singular `webhook_signing_secret` mention from these guides. Extend the activated docs contract tests so `accrue/test/accrue/docs/first_hour_guide_test.exs` asserts the first-hour runtime config example contains `:webhook_signing_secrets`, and `accrue/test/accrue/docs/troubleshooting_guide_test.exs` asserts the `ACCRUE-DX-WEBHOOK-SECRET-MISSING` remediation text points to the same key.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs test/accrue/docs/troubleshooting_guide_test.exs</automated>
  </verify>
  <done>The published setup guides now teach the same webhook signing config shape the runtime actually reads, and the guide tests lock that contract in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the strict docs verifier to fail on singular webhook secret drift</name>
  <read_first>
    .planning/phases/12-first-user-dx-stabilization/12-CONTEXT.md
    .planning/phases/12-first-user-dx-stabilization/12-VERIFICATION.md
    .planning/phases/12-first-user-dx-stabilization/12-07-PLAN.md
    scripts/ci/verify_package_docs.sh
    accrue/test/accrue/docs/package_docs_verifier_test.exs
    accrue/test/accrue/docs/first_hour_guide_test.exs
    accrue/test/accrue/docs/troubleshooting_guide_test.exs
  </read_first>
  <files>scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'webhook_signing_secrets|webhook_signing_secret' scripts/ci/verify_package_docs.sh` finds an explicit pass/fail check for the plural key and rejection of the singular key, per D-20.
    - `rg -n 'first_hour\\.md|troubleshooting\\.md' scripts/ci/verify_package_docs.sh` finds both published guides in the verifier target set.
    - `rg -n 'webhook_signing_secrets|webhook_signing_secret' accrue/test/accrue/docs/package_docs_verifier_test.exs` finds ExUnit coverage for the drift check.
    - `bash scripts/ci/verify_package_docs.sh` fails if either published guide reintroduces singular `webhook_signing_secret`.
  </acceptance_criteria>
  <action>Extend `scripts/ci/verify_package_docs.sh` from plan 12-07 so it explicitly verifies both `accrue/guides/first_hour.md` and `accrue/guides/troubleshooting.md` contain `config :accrue, :webhook_signing_secrets, %{stripe:` and contain no singular `webhook_signing_secret`. Keep the script strict and grep-verifiable rather than relying on prose-only checks. Update `accrue/test/accrue/docs/package_docs_verifier_test.exs` to assert this drift check is part of the verifier contract; if the test already shells out only, add assertions around the script output or failure mode that make the new config-key guard visible. Do not weaken the existing package-doc checks from 12-07; add this guard on top of them so DX-06 continues to cover package/docs drift automatically.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs</automated>
  </verify>
  <done>The strict docs verifier now catches webhook config-key drift automatically, so the singular key cannot ship again without breaking CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published setup docs -> first-hour developer configuration | User-facing guides tell hosts which runtime key to set for webhook verification |
| docs verifier -> package and guide content | CI relies on the verifier to reject stale or unsafe published setup instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10-01 | Security Misconfiguration | `accrue/guides/first_hour.md`, `accrue/guides/troubleshooting.md` | mitigate | Replace singular `webhook_signing_secret` with the exact `:webhook_signing_secrets` runtime config shape and lock it with docs tests. |
| T-12-10-02 | Repudiation | `scripts/ci/verify_package_docs.sh` | mitigate | Add explicit verifier checks for plural-key presence and singular-key absence so future docs drift breaks automation instead of relying on manual review. |
| T-12-10-03 | Information Disclosure | troubleshooting remediation text | mitigate | Keep remediation examples limited to env-var names and placeholder `whsec_test_host`; do not publish real secret values or copied runtime dumps. |
| T-12-10-04 | Tampering | docs/package tests | mitigate | Extend the existing guide and package-doc tests rather than adding unverified prose-only expectations, so the runtime config contract stays executable. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs test/accrue/docs/troubleshooting_guide_test.exs`
- `bash scripts/ci/verify_package_docs.sh`
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs`
</verification>

<success_criteria>
- No published guide still mentions singular `webhook_signing_secret`.
- Both guides show `config :accrue, :webhook_signing_secrets, %{stripe: ...}`.
- The strict docs verifier rejects future regressions of this config-key drift.
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-dx-stabilization/12-10-SUMMARY.md`
</output>

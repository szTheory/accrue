---
phase: 120-release-contract-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REL-09, PPX-15]
must_haves:
  truths:
    - "The maintainer explicitly chooses either `{accrue, accrue_admin}` or `{accrue, accrue_admin, accrue_portal}` as the Phase 120 release contract."
    - "The scope choice is grounded in current automation, runbook, recovery-workflow, and registry evidence before any repo edits land."
  artifacts:
    - path: ".planning/phases/120-release-contract-audit/120-01-SUMMARY.md"
      provides: "Persisted release-scope decision token for downstream plans"
      contains: "narrow-two-package|promote-three-package"
  key_links:
    - from: "release-please-config.json"
      to: "RELEASING.md"
      via: "linked package-set wording"
      pattern: "accrue(_admin|_portal)?"
    - from: ".github/workflows/release-please.yml"
      to: ".github/workflows/publish-hex.yml"
      via: "publish scope and ordering"
      pattern: "publish-accrue(-admin|-portal)?"
    - from: ".planning/phases/120-release-contract-audit/120-01-SUMMARY.md"
      to: ".planning/phases/120-release-contract-audit/120-02-PLAN.md"
      via: "exact chosen scope token"
      pattern: "narrow-two-package|promote-three-package"
---

<objective>
Freeze the Phase 120 release-scope decision before implementation.

Purpose: Prevent silent scope drift by forcing an explicit maintainer choice between the current two-package public narrative and the current three-package automation.
Output: A blocking user decision that later plans implement without interpretation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-release-contract-audit/120-RESEARCH.md
@.planning/phases/120-release-contract-audit/120-PATTERNS.md
@RELEASING.md
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml

<interfaces>
From `release-please-config.json`:
```json
"components": ["accrue", "accrue_admin", "accrue_portal"]
```

From `.release-please-manifest.json`:
```json
{
  "accrue": "1.0.0",
  "accrue_admin": "1.0.0",
  "accrue_portal": "1.0.0"
}
```

From `.github/workflows/release-please.yml`:
```yaml
publish-accrue-admin:
  needs: [release, publish-accrue]

publish-accrue-portal:
  needs: [release, publish-accrue, publish-accrue-admin]
```

From `RELEASING.md`:
```md
This runbook is written for the **recurring** maintainer path: linked `accrue` +
`accrue_admin` releases via **Release Please**
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Choose the public linked release contract</name>
  <read_first>RELEASING.md; release-please-config.json; .release-please-manifest.json; .github/workflows/release-please.yml; .github/workflows/publish-hex.yml; .planning/phases/120-release-contract-audit/120-RESEARCH.md</read_first>
  <acceptance_criteria>`curl -fsSL https://hex.pm/api/packages/accrue_portal` returns `404` or a package payload before prompting, and the decision response contains exactly one of `narrow-two-package` or `promote-three-package`.</acceptance_criteria>
  <decision>Pick the release contract that all later file edits must implement.</decision>
  <context>The repo currently automates three packages (`accrue`, `accrue_admin`, `accrue_portal`), while `RELEASING.md` and the manual fallback workflow still mostly describe only `accrue` + `accrue_admin`. Phase 120 must resolve that contradiction explicitly per REL-09 and PPX-15.</context>
  <options>
    <option id="narrow-two-package">
      <name>Narrow to accrue + accrue_admin</name>
      <pros>Keeps the public release story aligned with the currently published Hex packages and existing maintainer recovery flow.</pros>
      <cons>Requires removing `accrue_portal` from linked-version automation, manifest scope, workflow outputs, and publish jobs.</cons>
    </option>
    <option id="promote-three-package">
      <name>Promote accrue + accrue_admin + accrue_portal</name>
      <pros>Preserves the current three-package automation and makes the public contract honest by teaching the runbook and recovery path about portal publication.</pros>
      <cons>Requires broadening maintainer docs and fallback workflow to a third package, plus making the ordered publish and proof language explicit for portal.</cons>
    </option>
  </options>
  <action>Before prompting, run the evidence bundle exactly as written: `rg -n "accrue_portal|accrue_release_created|accrue_admin_release_created|accrue_portal_release_created|publish-accrue-portal|workflow_dispatch|options:" RELEASING.md release-please-config.json .release-please-manifest.json .github/workflows/release-please.yml .github/workflows/publish-hex.yml` and `curl -fsSL https://hex.pm/api/packages/accrue_portal`. Present the evidence concisely, then block for one literal reply: `narrow-two-package` or `promote-three-package`. Do not start file edits in this plan.</action>
  <verify>
    <automated>rg -n "accrue_portal|accrue_release_created|accrue_admin_release_created|accrue_portal_release_created|publish-accrue-portal|workflow_dispatch|options:" RELEASING.md release-please-config.json .release-please-manifest.json .github/workflows/release-please.yml .github/workflows/publish-hex.yml && curl -fsSL https://hex.pm/api/packages/accrue_portal</automated>
  </verify>
  <done>The maintainer picks one scope token, and later plans are resumed against that exact token with no mixed-scope interpretation.</done>
  <resume-signal>Reply with exactly one token: `narrow-two-package` or `promote-three-package`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer→release contract | Human intent must cross into repo edits without ambiguity about package scope. |
| repo automation→public registry truth | Existing workflow scope may not match the packages actually published on Hex. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | T | Phase 120 scope choice | mitigate | Block implementation until the maintainer chooses one literal scope token after seeing automation and Hex evidence. |
| T-120-02 | I | Decision prompt evidence | mitigate | Require live `curl` evidence for `accrue_portal` registry state and explicit `rg` evidence from all release-contract files before presenting options. |
| T-120-03 | R | Release-scope audit trail | mitigate | The executor summary for this plan must record the chosen token verbatim so later implementation can be checked against it. |
| T-120-04 | E | Unintended scope expansion | mitigate | Later plans may modify only the surfaces required to implement the chosen token; mixed two-package and three-package wording is not allowed. |
</threat_model>

<verification>
Run the evidence bundle before prompting, then record the exact maintainer token in the plan summary.
</verification>

<success_criteria>
Phase 120 has one explicit, recorded scope decision and no implementation work starts before that decision exists.
</success_criteria>

<output>
After completion, create `.planning/phases/120-release-contract-audit/120-01-SUMMARY.md`
</output>

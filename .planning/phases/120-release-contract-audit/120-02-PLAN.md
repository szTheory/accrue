---
phase: 120-release-contract-audit
plan: 02
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - RELEASING.md
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
autonomous: true
requirements: [REL-09, PPX-15]
must_haves:
  truths:
    - "The runbook, Release Please config, manifest, and publish workflows all state the same linked package scope."
    - "Publish ordering is explicit and unchanged as a workflow `needs:` chain: core first, then each dependent UI package still in scope."
    - "The manual recovery path only offers package choices that belong to the chosen public release contract."
  artifacts:
    - path: "RELEASING.md"
      provides: "Maintainer-facing release contract and fallback instructions"
      min_lines: 120
    - path: "release-please-config.json"
      provides: "Linked-version scope definition"
      contains: "\"components\""
    - path: ".release-please-manifest.json"
      provides: "Release Please manifest scope"
      contains: "\"accrue\""
    - path: ".github/workflows/release-please.yml"
      provides: "Trusted same-workflow publish ordering"
      exports: ["publish-accrue", "publish-accrue-admin"]
    - path: ".github/workflows/publish-hex.yml"
      provides: "Manual recovery path for the chosen package set"
      contains: "workflow_dispatch"
  key_links:
    - from: "release-please-config.json"
      to: ".release-please-manifest.json"
      via: "same package key set"
      pattern: "accrue(_admin|_portal)?"
    - from: ".github/workflows/release-please.yml"
      to: ".github/workflows/publish-hex.yml"
      via: "identical package scope and recovery choices"
      pattern: "accrue(_admin|_portal)?"
    - from: "RELEASING.md"
      to: ".github/workflows/release-please.yml"
      via: "matching publish order language"
      pattern: "publish-accrue(-admin|-portal)?"
---

<objective>
Apply the chosen Phase 120 release scope across the runbook, Release Please config, manifest, and release workflows.

Purpose: Eliminate the current contradiction where automation, maintainer instructions, and fallback publication do not agree on which packages participate in a linked release.
Output: One coherent release contract implemented across docs and workflow-owned scope surfaces.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-release-contract-audit/120-RESEARCH.md
@.planning/phases/120-release-contract-audit/120-PATTERNS.md
@RELEASING.md
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@.planning/phases/120-release-contract-audit/120-01-SUMMARY.md

<interfaces>
From `.github/workflows/release-please.yml`:
```yaml
publish-accrue-admin:
  needs: [release, publish-accrue]

publish-accrue-portal:
  needs: [release, publish-accrue, publish-accrue-admin]
```

From `.github/workflows/publish-hex.yml`:
```yaml
on:
  workflow_dispatch:
    inputs:
      package:
        type: choice
```

From `accrue_admin/mix.exs` and `accrue_portal/mix.exs`:
```elixir
if System.get_env("ACCRUE_ADMIN_HEX_RELEASE") == "1" do
if System.get_env("ACCRUE_PORTAL_HEX_RELEASE") == "1" do
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile runbook and Release Please scope per the Phase 120 decision</name>
  <files>RELEASING.md; release-please-config.json; .release-please-manifest.json</files>
  <read_first>RELEASING.md; release-please-config.json; .release-please-manifest.json; .planning/phases/120-release-contract-audit/120-01-SUMMARY.md; .planning/phases/120-release-contract-audit/120-RESEARCH.md</read_first>
  <acceptance_criteria>`jq -r '.plugins[] | select(.type=="linked-versions") | .components | join(",")' release-please-config.json` and `jq -r 'keys | join(",")' .release-please-manifest.json` return the same chosen package set; `rg -n "linked \`accrue\` \+|\baccrue_portal\b|Last verified against" RELEASING.md` shows only the chosen-scope wording.</acceptance_criteria>
  <action>Read the Phase 120 Plan 01 summary first and extract the exact user token. If the token is `narrow-two-package`, remove `accrue_portal` from the linked-versions `components` array and from `.release-please-manifest.json`, then rewrite `RELEASING.md` routine, automation, checklist, fallback, and bootstrap prose so only `accrue` and `accrue_admin` are described. If the token is `promote-three-package`, keep or add `accrue_portal` in both config files and rewrite `RELEASING.md` so the recurring path, checklist, proof language, and manual fallback all explicitly name the three-package contract and preserve `accrue` before `accrue_admin` before `accrue_portal`. Update the `Last verified against` date line to the execution date when these semantics change. Do not leave mixed two-package and three-package wording anywhere in `RELEASING.md`.</action>
  <verify>
    <automated>scope_cfg="$(jq -r '.plugins[] | select(.type==\"linked-versions\") | .components | join(\",\")' release-please-config.json)" && scope_manifest="$(jq -r 'keys | join(\",\")' .release-please-manifest.json)" && test "$scope_cfg" = "$scope_manifest" && rg -n "Last verified against|accrue_portal|linked `accrue`" RELEASING.md</automated>
  </verify>
  <done>`RELEASING.md`, `release-please-config.json`, and `.release-please-manifest.json` all encode the same chosen scope with no leftover contradictory package-set wording.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile automated and manual publish workflows to the same contract</name>
  <files>.github/workflows/release-please.yml; .github/workflows/publish-hex.yml</files>
  <read_first>.github/workflows/release-please.yml; .github/workflows/publish-hex.yml; RELEASING.md; release-please-config.json; .release-please-manifest.json; .planning/phases/120-release-contract-audit/120-01-SUMMARY.md</read_first>
  <acceptance_criteria>`rg -n "publish-accrue-admin|publish-accrue-portal|ACCRUE_PORTAL_HEX_RELEASE|accrue_portal_release_created|options:" .github/workflows/release-please.yml .github/workflows/publish-hex.yml` matches only the chosen-scope paths, and `yamllint`-free grep checks show `publish-accrue-admin` needs `publish-accrue` while `publish-accrue-portal` exists only if the chosen scope includes portal.</acceptance_criteria>
  <action>Modify `.github/workflows/release-please.yml` and `.github/workflows/publish-hex.yml` to match the same token from Plan 01. Preserve the repo pattern that publish order lives in workflow `needs:` chains. If the token is `narrow-two-package`, remove portal-specific job outputs, manifest lookups, lockstep fallback, `publish-accrue-portal`, and the manual `package` choice for `accrue_portal`. If the token is `promote-three-package`, keep or add portal-specific outputs, fallback, `publish-accrue-portal`, and manual recovery support with an `accrue_portal` workflow-dispatch option, `ACCRUE_PORTAL_HEX_RELEASE=1`, explicit `grep -n "@version"` verification, `mix deps.get`, `mix hex.publish --dry-run`, and `mix hex.publish --yes` in `accrue_portal/`. In both branches, keep same-workflow trust boundaries intact: downstream publish jobs may read only `needs.release.outputs.*` and explicit workflow-dispatch inputs, never ad hoc branch state.</action>
  <verify>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh && rg -n "publish-accrue-admin:|needs: \\[release, publish-accrue\\]|publish-accrue-portal:|ACCRUE_PORTAL_HEX_RELEASE|accrue_portal_release_created|options:" .github/workflows/release-please.yml .github/workflows/publish-hex.yml</automated>
  </verify>
  <done>The automated publish workflow and the manual recovery workflow expose exactly the same chosen package set and ordered publish chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook→workflow | Maintainer instructions must match the real workflow scope and ordering. |
| release job outputs→publish jobs | Only trusted same-workflow outputs may control automated publication. |
| manual maintainer input→manual publish workflow | Explicit workflow-dispatch choices must not allow wrong-package or wrong-ref publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-05 | T | `RELEASING.md` vs automation scope | mitigate | Rewrite all runbook sections that mention package scope so every scope literal matches the Phase 120 decision and the workflow/package keys. |
| T-120-06 | E | `.github/workflows/release-please.yml` | mitigate | Preserve publish ordering in `needs:` and keep publish jobs bound to `needs.release.outputs.*` rather than branch assumptions or copied values. |
| T-120-07 | T | `.github/workflows/publish-hex.yml` | mitigate | Restrict manual recovery `package` choices to the chosen scope and keep explicit `tag` plus `release_version` verification before publish. |
| T-120-08 | I | Secret handling in release docs | mitigate | Keep `HEX_API_KEY` and `RELEASE_PLEASE_TOKEN` as secret names only; do not add secret values, secret examples, or verbose shell traces to the runbook. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_release_manifest_alignment.sh` plus the grep checks in each task after edits. The chosen scope token from Plan 01 must still be derivable from every modified surface.
</verification>

<success_criteria>
`RELEASING.md`, `release-please-config.json`, `.release-please-manifest.json`, `.github/workflows/release-please.yml`, and `.github/workflows/publish-hex.yml` all describe the same linked release contract and publish order.
</success_criteria>

<output>
After completion, create `.planning/phases/120-release-contract-audit/120-02-SUMMARY.md`
</output>

---
phase: 120-release-contract-audit
plan: 03
type: execute
wave: 3
depends_on: [120-02]
files_modified:
  - scripts/ci/verify_release_manifest_alignment.sh
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/verify_release_contract.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [REL-09, PPX-15]
must_haves:
  truths:
    - "CI fails if the runbook, Release Please config, manifest, release workflow, and manual publish workflow drift on package scope or publish order."
    - "The chosen Phase 120 scope is enforced by shell verifiers rather than trust in manual reading."
    - "Release-contract verification checks both automation truth and maintainer-facing recovery truth."
  artifacts:
    - path: "scripts/ci/verify_release_contract.sh"
      provides: "Cross-file release contract verifier"
      min_lines: 40
    - path: "scripts/ci/verify_release_manifest_alignment.sh"
      provides: "Manifest-to-source alignment plus chosen-scope awareness"
      contains: "set -euo pipefail"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking invocation for release contract verification"
      contains: "verify_release_contract.sh"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_release_contract.sh"
      via: "CI job step"
      pattern: "verify_release_contract\\.sh"
    - from: "scripts/ci/verify_release_contract.sh"
      to: "RELEASING.md"
      via: "fixed-string and regex assertions"
      pattern: "RELEASING\\.md"
    - from: "scripts/ci/verify_release_manifest_alignment.sh"
      to: ".release-please-manifest.json"
      via: "manifest key/value assertions"
      pattern: "jq -r"
---

<objective>
Turn the chosen release contract into an enforced CI invariant.

Purpose: Prevent Phase 120 from being a one-off doc cleanup by adding shell verifiers and CI wiring that reject future scope drift, wrong recovery options, or dishonest release-path prose.
Output: A merge-blocking release-contract verifier bundle that matches the chosen scope from Plan 02.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-release-contract-audit/120-PATTERNS.md
@.planning/phases/120-release-contract-audit/120-VALIDATION.md
@scripts/ci/verify_release_manifest_alignment.sh
@scripts/ci/verify_package_docs.sh
@.github/workflows/ci.yml
@.planning/phases/120-release-contract-audit/120-02-SUMMARY.md

<interfaces>
From `scripts/ci/verify_release_manifest_alignment.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR=${ROOT_DIR:-$(
  cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd
)}
fail() {
  echo "[verify_release_manifest_alignment] $*" >&2
  exit 1
}
```

From `scripts/ci/verify_package_docs.sh`:
```bash
require_fixed() {
  local file=$1
  local needle=$2
  grep -Fq "$needle" "$file" || fail "$file is missing: $needle"
}
```

From `.github/workflows/ci.yml`:
```yaml
release-manifest-ssot:
  steps:
    - name: Verify Release Please manifest ↔ mix.exs @version
      run: bash scripts/ci/verify_release_manifest_alignment.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a scope-aware release contract verifier bundle</name>
  <files>scripts/ci/verify_release_manifest_alignment.sh; scripts/ci/verify_package_docs.sh; scripts/ci/verify_release_contract.sh</files>
  <read_first>scripts/ci/verify_release_manifest_alignment.sh; scripts/ci/verify_package_docs.sh; RELEASING.md; release-please-config.json; .release-please-manifest.json; .github/workflows/release-please.yml; .github/workflows/publish-hex.yml; .planning/phases/120-release-contract-audit/120-02-SUMMARY.md</read_first>
  <acceptance_criteria>`bash scripts/ci/verify_release_contract.sh` exits `0`; `rg -n "set -euo pipefail|fail\\(\\)|require_fixed|require_regex|accrue_portal|publish-accrue-portal|workflow_dispatch" scripts/ci/verify_release_contract.sh scripts/ci/verify_release_manifest_alignment.sh` shows the new scope-aware assertions and the standard shell verifier structure.</acceptance_criteria>
  <action>Create `scripts/ci/verify_release_contract.sh` using the repo’s shell-verifier pattern: `#!/usr/bin/env bash`, `set -euo pipefail`, repo-root discovery, `fail()` helper, and small `require_fixed` / `require_regex` assertions. The script must read the chosen scope from the Phase 120 Plan 02 result by inspecting `release-please-config.json` and then assert that `RELEASING.md`, `.release-please-manifest.json`, `.github/workflows/release-please.yml`, and `.github/workflows/publish-hex.yml` all match that scope and publish order. Extend `scripts/ci/verify_release_manifest_alignment.sh` so it asserts either intentional two-package exclusion of `accrue_portal` or full three-package lockstep, depending on the chosen scope. Extend `scripts/ci/verify_package_docs.sh` only where needed to pin any new release-runbook wording that should never drift silently. Do not hand-roll broad parsers; keep the verifier as small grep/jq assertions.</action>
  <verify>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The release contract is enforced by executable shell checks, and those checks fail if package scope, publish order, or manual recovery wording drifts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the release contract verifier into merge-blocking CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml; scripts/ci/verify_release_contract.sh; scripts/ci/verify_release_manifest_alignment.sh; scripts/ci/verify_package_docs.sh; .planning/phases/120-release-contract-audit/120-VALIDATION.md</read_first>
  <acceptance_criteria>`rg -n "verify_release_contract\\.sh|release-manifest-ssot|docs-contracts-shift-left" .github/workflows/ci.yml` shows the new verifier invocation in the intended job path, and `bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_adoption_proof_matrix.sh` exits `0`.</acceptance_criteria>
  <action>Update `.github/workflows/ci.yml` so the chosen-scope release contract is merge-blocking. Prefer adding `bash scripts/ci/verify_release_contract.sh` to the existing release-contract lane if that preserves job-id stability; only add a new job if the current lane cannot express the check without making the YAML less clear. Preserve the stable job-id comment pattern and keep the docs-contracts bundle explicit. The final CI state must make future mixed-scope edits fail in automation before release day.</action>
  <verify>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && rg -n "verify_release_contract\\.sh" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI invokes the release-contract verifier from a stable merge-blocking path, and the full Phase 120 verification bundle passes on the updated contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files→CI verdict | Shell verifiers transform release-contract text and workflow structure into pass/fail automation. |
| maintainer docs→manual recovery | `RELEASING.md` and `publish-hex.yml` must agree or maintainers can publish the wrong package/ref. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-09 | T | `scripts/ci/verify_release_contract.sh` | mitigate | Assert cross-file scope and ordering invariants with explicit `grep`/`jq` checks against every owned release surface. |
| T-120-10 | D | `.github/workflows/ci.yml` | mitigate | Wire the new verifier into an existing merge-blocking CI path so release-contract drift cannot be ignored locally. |
| T-120-11 | R | Manual recovery docs | mitigate | Keep verifier assertions on `workflow_dispatch` package options, explicit tag/version checks, and the documented fallback order. |
| T-120-12 | I | Secret leakage through verifiers | mitigate | Verify secret names only; do not add assertions that require printing `HEX_API_KEY`, `RELEASE_PLEASE_TOKEN`, or any resolved values. |
</threat_model>

<verification>
Run the full Phase 120 bundle after CI wiring: `bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_adoption_proof_matrix.sh`.
</verification>

<success_criteria>
Future release-scope drift between docs, Release Please config, manifest, release workflow, and manual fallback workflow is caught by CI rather than by maintainer guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/120-release-contract-audit/120-03-SUMMARY.md`
</output>

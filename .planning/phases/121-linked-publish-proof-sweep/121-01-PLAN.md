---
phase: 121-linked-publish-proof-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_release_pr_scope.sh
  - scripts/ci/capture_linked_release_proof.sh
  - scripts/ci/README.md
  - .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md
autonomous: true
requirements: [REL-10]
must_haves:
  truths:
    - "The release PR is blocked from merge until it matches the locked three-package contract from Phase 120."
    - "Maintainers have deterministic commands to verify release-PR scope and capture publish proof before release-day decisions."
    - "Phase 121 has one canonical verification ledger that later plans append instead of scattering evidence across ad hoc notes."
  artifacts:
    - path: "scripts/ci/verify_release_pr_scope.sh"
      provides: "Deterministic pass/fail check for the live Release Please PR against the three-package contract"
      contains: "set -euo pipefail"
    - path: "scripts/ci/capture_linked_release_proof.sh"
      provides: "Deterministic proof capture for tags, GitHub releases, Hex API truth, and workflow ordering"
      contains: "set -euo pipefail"
    - path: ".planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md"
      provides: "Canonical dated release-proof ledger for Phase 121 with exact execution identifiers"
      contains: "PR_NUMBER:"
  key_links:
    - from: "scripts/ci/verify_release_pr_scope.sh"
      to: "https://github.com/szTheory/accrue/pull/18"
      via: "gh pr view JSON assertions against the live release PR"
      pattern: "accrue_portal|CHANGELOG.md|mix.exs|TARGET_VERSION"
    - from: "scripts/ci/capture_linked_release_proof.sh"
      to: ".planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md"
      via: "append-only proof capture bound to recorded identifiers"
      pattern: "PR_NUMBER|TARGET_VERSION|RUN_ID|Hex API|GitHub release|git tag|workflow run"
    - from: "scripts/ci/README.md"
      to: "scripts/ci/verify_release_pr_scope.sh"
      via: "contributor map entry"
      pattern: "REL-10|REL-11"
---

<objective>
Make the live release PR and proof tooling trustworthy before any merge happens.

Purpose: Phase 120 locked the contract to `promote-three-package`, but the current open Release Please PR still represents only `accrue` + `accrue_admin`; this plan must turn that mismatch into a deterministic merge blocker and stage the canonical proof artifact for the rest of the phase.
Output: Executable proof scripts, a documented operator path, and a verified three-package release PR (or a deliberate refreshed replacement) recorded in `121-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-linked-publish-proof-sweep/121-RESEARCH.md
@.planning/phases/121-linked-publish-proof-sweep/121-VALIDATION.md
@.planning/phases/120-release-contract-audit/120-03-SUMMARY.md
@RELEASING.md
@release-please-config.json
@.release-please-manifest.json
@.github/workflows/release-please.yml
@scripts/ci/README.md

<interfaces>
From `release-please-config.json`:
```json
"components": ["accrue", "accrue_admin", "accrue_portal"]
```

From `.release-please-manifest.json`:
```json
{
  "accrue": "1.0.0",
  "accrue_admin": "1.0.0",
  "accrue_portal": "1.0.0"
}
```

From live PR `#18` on `2026-05-07`:
```json
{
  "state": "OPEN",
  "mergeStateStatus": "UNSTABLE",
  "files": [
    ".release-please-manifest.json",
    "accrue/CHANGELOG.md",
    "accrue/mix.exs",
    "accrue_admin/CHANGELOG.md",
    "accrue_admin/mix.exs"
  ],
  "labels": ["autorelease: pending"]
}
```

From public registry state on `2026-05-07`:
```text
accrue latest_version=1.0.0
accrue_admin latest_version=1.0.0
accrue_portal => 404
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deterministic release-PR and publish-proof tooling per REL-10</name>
  <files>scripts/ci/verify_release_pr_scope.sh; scripts/ci/capture_linked_release_proof.sh; scripts/ci/README.md; .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</files>
  <action>Create `scripts/ci/verify_release_pr_scope.sh` using the repo’s shell-verifier pattern (`#!/usr/bin/env bash`, `set -euo pipefail`, repo-root discovery, `fail()` helper). The script must accept a PR number or URL plus an optional `--version`, then assert the Phase 120 `promote-three-package` contract: the PR updates `.release-please-manifest.json`, all three package `mix.exs` files, and all three package `CHANGELOG.md` files; if `--version` is provided it must also prove the three package bumps all target that exact version; it must fail on pair-only PRs like the current `#18`. Create `scripts/ci/capture_linked_release_proof.sh` as the Phase 121 evidence harness: given `--version`, `--run-id`, `--pr`, and `--output`, it must append a deterministic ledger block covering git tags, `gh release view`, Hex API responses, and release-workflow job ordering for those exact identifiers. Seed `.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md` with fixed marker lines `PR_NUMBER: pending`, `TARGET_VERSION: pending`, and `RUN_ID: pending`, followed by sections for release PR evidence, publish run evidence, public registry proof, post-publish smoke reruns, and post-publish verifier reruns. Update `scripts/ci/README.md` with REL-10/REL-11 triage entries naming both scripts and the marker-driven Phase 121 ledger flow. Keep the scripts small and grep/jq/gh-driven; do not hand-roll broad parsers or introduce new dependencies.</action>
  <verify>
    <automated>bash scripts/ci/verify_release_pr_scope.sh --help && bash scripts/ci/capture_linked_release_proof.sh --help && rg -n "verify_release_pr_scope\\.sh|capture_linked_release_proof\\.sh|REL-10|REL-11|^PR_NUMBER: pending$|^TARGET_VERSION: pending$|^RUN_ID: pending$" scripts/ci/README.md .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</automated>
  </verify>
  <done>The repo contains deterministic Phase 121 proof tooling, a documented contributor-map entry, and a canonical verification ledger skeleton ready for live evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh or replace the live Release Please PR until the three-package contract passes</name>
  <files>.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</files>
  <action>Use `bash scripts/ci/verify_release_pr_scope.sh --pr 18` against the live `chore: release main` PR first. If it fails because the PR is pair-only, do not merge it. Repair the live state using the least-destructive deterministic path: remove stale `autorelease: pending` state if needed, trigger `.github/workflows/release-please.yml` via `gh workflow run 'Release Please' --ref main`, wait for the run to finish, and inspect the resulting release PR. If GitHub still leaves only the stale pair-only PR, close that PR, rerun Release Please, and verify the replacement PR instead. Once a PR passes, derive the exact shipped target version from that PR’s three package `mix.exs` or changelog bumps, then record both `PR_NUMBER: <number>` and `TARGET_VERSION: <version>` at the top of `121-VERIFICATION.md` before appending the dated PR URL, file inventory, `mergeStateStatus`, verifier result, and an explicit block note that merge is forbidden until `bash scripts/ci/verify_release_pr_scope.sh --pr "$PR_NUMBER" --version "$TARGET_VERSION"` passes. Do not rely on `gh pr list`, “latest,” or first-match search results after the identifier pair is recorded; subsequent plans must consume the recorded markers only. If the workflow produces a PR that still omits `accrue_portal`, stop the plan with a blocking failure instead of papering it over. This task implements the pre-merge REL-10 gate per the Phase 120 `promote-three-package` decision.</action>
  <verify>
    <automated>LEDGER=.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md && PR_NUMBER="$(sed -n 's/^PR_NUMBER: //p' "$LEDGER")" && TARGET_VERSION="$(sed -n 's/^TARGET_VERSION: //p' "$LEDGER")" && test -n "$PR_NUMBER" && test -n "$TARGET_VERSION" && bash scripts/ci/verify_release_pr_scope.sh --pr "$PR_NUMBER" --version "$TARGET_VERSION" && rg -n "^PR_NUMBER: ${PR_NUMBER}$|^TARGET_VERSION: ${TARGET_VERSION}$|## Release PR evidence|Do not merge if scope verification fails|github.com/szTheory/accrue/pull/${PR_NUMBER}" "$LEDGER"</automated>
  </verify>
  <done>An open Release Please PR exists that passes the three-package scope verifier, and the canonical Phase 121 ledger records the exact pre-merge evidence and block condition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub PR state→maintainer merge action | The live release PR must cross from GitHub state into a merge decision without dropping the Phase 120 three-package contract. |
| repo truth→proof scripts | The new shell scripts must convert release assumptions into deterministic pass/fail checks instead of oral knowledge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | T | Release Please PR scope | mitigate | `verify_release_pr_scope.sh` fails unless the PR updates manifest + all three package `mix.exs` and changelog files per `promote-three-package`. |
| T-121-02 | R | Pre-merge release evidence | mitigate | `121-VERIFICATION.md` records the exact PR number, URL, file set, and verifier pass result before any merge proceeds. |
| T-121-03 | D | Stale `autorelease: pending` / pair-only PR state | mitigate | Task 2 explicitly refreshes or replaces the stale PR and blocks if the resulting PR still omits `accrue_portal`. |
| T-121-04 | I | Proof scripts and logs | mitigate | Keep all commands on public GitHub/Hex metadata only; never print secret values or add token-bearing commands to the ledger. |
</threat_model>

<verification>
Run the new release-PR verifier against the live open release PR and keep `121-VERIFICATION.md` as the single evidence ledger from this point forward.
</verification>

<success_criteria>
The repo has deterministic release-proof tooling, and the only mergeable release PR is one that matches the locked three-package contract.
</success_criteria>

<output>
After completion, create `.planning/phases/121-linked-publish-proof-sweep/121-01-SUMMARY.md`
</output>

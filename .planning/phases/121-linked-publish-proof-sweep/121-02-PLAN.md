---
phase: 121-linked-publish-proof-sweep
plan: 02
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md
autonomous: true
requirements: [REL-10, REL-11]
must_haves:
  truths:
    - "The linked release actually ships for `accrue`, `accrue_admin`, and `accrue_portal`, not just in branch-local docs."
    - "The ordered publish chain `accrue -> accrue_admin -> accrue_portal` is proven from the real release workflow run."
    - "One dated ledger ties one exact `PR_NUMBER`, one exact `RUN_ID`, and one exact `TARGET_VERSION` to tags, GitHub releases, Hex versions, and workflow-job ordering."
  artifacts:
    - path: ".planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md"
      provides: "Merged-release evidence ledger with ordered publish proof and public registry truth"
      contains: "## Publish run evidence"
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: ".planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md"
      via: "workflow run id and job ordering transcript"
      pattern: "publish-accrue|publish-accrue-admin|publish-accrue-portal"
    - from: "scripts/ci/capture_linked_release_proof.sh"
      to: "https://hex.pm/api/packages/accrue_portal"
      via: "public registry verification"
      pattern: "latest_version|updated_at"
---

<objective>
Merge the verified release PR, watch the ordered publishes complete, and capture the public release proof chain.

Purpose: REL-10 and REL-11 are only satisfied once the verified PR is merged, the linked packages publish in order, and the public surfaces all agree on the shipped version.
Output: A dated `121-VERIFICATION.md` ledger proving tags, GitHub releases, Hex registry versions, and workflow ordering for the shipped three-package line.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-linked-publish-proof-sweep/121-RESEARCH.md
@.planning/phases/121-linked-publish-proof-sweep/121-VALIDATION.md
@.planning/phases/121-linked-publish-proof-sweep/121-01-SUMMARY.md
@.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md
@RELEASING.md
@.github/workflows/release-please.yml
@scripts/ci/capture_linked_release_proof.sh
@scripts/ci/verify_release_pr_scope.sh

<interfaces>
From `.github/workflows/release-please.yml`:
```yaml
publish-accrue:
  needs: release

publish-accrue-admin:
  needs: [release, publish-accrue]

publish-accrue-portal:
  needs: [release, publish-accrue, publish-accrue-admin]
```

From Plan 01 output:
```text
An open release PR exists that passes verify_release_pr_scope.sh.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge only the verified release PR and capture ordered workflow evidence</name>
  <files>.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</files>
  <action>Read `PR_NUMBER` and `TARGET_VERSION` from `121-VERIFICATION.md` exactly as recorded by Plan 01, rerun `bash scripts/ci/verify_release_pr_scope.sh --pr "$PR_NUMBER" --version "$TARGET_VERSION"` immediately before merge, and merge only if it still passes. Prefer the maintainer-reviewed path already encoded in `RELEASING.md`: merge manually with `gh pr merge --merge` or queue the repo’s workflow-dispatch auto-merge path if branch protection requires it, but do not merge an unchecked or different PR. After merge, resolve the exact `Release Please` workflow run triggered by that merge by binding it to the merged PR outcome (for example via the merge commit SHA or the run URL produced during `gh pr merge` / `gh run watch`), then record `RUN_ID: <id>` in `121-VERIFICATION.md` before appending the run URL plus each relevant job’s conclusion/start/end ordering. From this point forward, use only the recorded `RUN_ID`; do not use `gh run list --limit 1`, “latest workflow run,” or similar first-match shortcuts. If `publish-accrue`, `publish-accrue-admin`, or `publish-accrue-portal` fails or appears out of order, stop here and record the blocking failure instead of continuing to public-proof capture. This task implements REL-11’s ordered-publish requirement against the real workflow run, not a branch assumption.</action>
  <verify>
    <automated>LEDGER=.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md && PR_NUMBER="$(sed -n 's/^PR_NUMBER: //p' "$LEDGER")" && TARGET_VERSION="$(sed -n 's/^TARGET_VERSION: //p' "$LEDGER")" && RUN_ID="$(sed -n 's/^RUN_ID: //p' "$LEDGER")" && test -n "$PR_NUMBER" && test -n "$TARGET_VERSION" && test -n "$RUN_ID" && bash scripts/ci/verify_release_pr_scope.sh --pr "$PR_NUMBER" --version "$TARGET_VERSION" && gh run view "$RUN_ID" --repo szTheory/accrue --json conclusion,jobs,url && rg -n "^PR_NUMBER: ${PR_NUMBER}$|^TARGET_VERSION: ${TARGET_VERSION}$|^RUN_ID: ${RUN_ID}$|## Publish run evidence|publish-accrue|publish-accrue-admin|publish-accrue-portal|github.com/szTheory/accrue/actions/runs/${RUN_ID}" "$LEDGER"</automated>
  </verify>
  <done>The verified release PR is merged, the real Release Please run has completed, and the Phase 121 ledger contains the ordered publish-run evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the shipped public release truth for all three packages</name>
  <files>.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</files>
  <action>Read `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` from `121-VERIFICATION.md`, then run `scripts/ci/capture_linked_release_proof.sh` to append a dated proof block to the same ledger. Before any local tag inspection, explicitly refresh remote tags with a read-only fetch such as `git fetch --tags --force origin` so subsequent `git tag` or `git rev-parse` checks observe the released tags instead of stale local state. The proof block must be explicitly keyed to those exact identifiers and must include all three package tags, `gh release view` results, and Hex API responses for `TARGET_VERSION`; it must fail if `accrue_portal` is still missing or any package reports a different version. Use the actual public surfaces as truth: refreshed `git tag`, `gh release view`, and `curl https://hex.pm/api/packages/...`. Record the exact shipped version, the tag names, the release URLs, and the Hex `updated_at` timestamps. Do not infer success from branch files alone, and do not treat “latest registry state” as sufficient unless it matches the already-recorded `TARGET_VERSION`. If the core package publishes but a downstream package does not, keep the ledger honest, stop the plan, and hand the failure to the recovery path rather than mutating docs as though the release succeeded.</action>
  <verify>
    <automated>LEDGER=.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md && PR_NUMBER="$(sed -n 's/^PR_NUMBER: //p' "$LEDGER")" && TARGET_VERSION="$(sed -n 's/^TARGET_VERSION: //p' "$LEDGER")" && RUN_ID="$(sed -n 's/^RUN_ID: //p' "$LEDGER")" && test -n "$PR_NUMBER" && test -n "$TARGET_VERSION" && test -n "$RUN_ID" && git fetch --tags --force origin && for pkg in accrue accrue_admin accrue_portal; do curl -fsSL "https://hex.pm/api/packages/${pkg}" | jq -e --arg v "$TARGET_VERSION" '.latest_version == $v' >/dev/null; done && gh release view "accrue-v${TARGET_VERSION}" --repo szTheory/accrue >/dev/null && gh release view "accrue_admin-v${TARGET_VERSION}" --repo szTheory/accrue >/dev/null && gh release view "accrue_portal-v${TARGET_VERSION}" --repo szTheory/accrue >/dev/null && git rev-parse "accrue-v${TARGET_VERSION}" >/dev/null && git rev-parse "accrue_admin-v${TARGET_VERSION}" >/dev/null && git rev-parse "accrue_portal-v${TARGET_VERSION}" >/dev/null && rg -n "^PR_NUMBER: ${PR_NUMBER}$|^TARGET_VERSION: ${TARGET_VERSION}$|^RUN_ID: ${RUN_ID}$|^## Public registry proof$|accrue-v${TARGET_VERSION}|accrue_admin-v${TARGET_VERSION}|accrue_portal-v${TARGET_VERSION}|hex.pm/api/packages/accrue|hex.pm/api/packages/accrue_admin|hex.pm/api/packages/accrue_portal|${RUN_ID}|${PR_NUMBER}" "$LEDGER"</automated>
  </verify>
  <done>`121-VERIFICATION.md` proves one shipped version across tags, GitHub releases, and Hex registry truth for `accrue`, `accrue_admin`, and `accrue_portal`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verified PR→merge action | Only a passing three-package release PR may cross into a merge. |
| GitHub Actions workflow→public registry | Publish jobs and registry results must align before the release is treated as shipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05 | T | Release merge target | mitigate | Re-run `verify_release_pr_scope.sh` immediately before merge and only merge the passing release PR. |
| T-121-06 | R | Ordered publish proof | mitigate | Record the exact Release Please run id, job ordering, and conclusions in `121-VERIFICATION.md`. |
| T-121-07 | T | Partial linked release | mitigate | `capture_linked_release_proof.sh` fails if any of the three packages lacks the shipped tag/release/Hex version, preventing silent partial success. |
| T-121-08 | D | Workflow or registry failure | mitigate | Stop the plan on failed publish jobs or missing registry evidence and force the recovery path instead of continuing into docs cleanup. |
</threat_model>

<verification>
This plan is complete only when the real release workflow run and the public registry both prove the same three-package shipped version.
</verification>

<success_criteria>
The linked release is publicly real, and the Phase 121 ledger proves the shipped version and ordered publish chain from GitHub and Hex evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/121-linked-publish-proof-sweep/121-02-SUMMARY.md`
</output>

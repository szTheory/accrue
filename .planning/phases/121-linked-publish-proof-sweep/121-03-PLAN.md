---
phase: 121-linked-publish-proof-sweep
plan: 03
type: execute
wave: 3
depends_on: [121-02]
files_modified:
  - README.md
  - accrue/guides/first_hour.md
  - examples/accrue_host/README.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/verify_adoption_proof_matrix.sh
  - .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md
autonomous: true
requirements: [PPX-13, PPX-14]
must_haves:
  truths:
    - "User-facing docs and proof mirrors no longer claim a linked pair after the three-package public release exists."
    - "The merge-blocking docs-contracts bundle passes against post-publish docs state."
    - "The Phase 121 ledger records the post-publish verifier reruns against the released line."
  artifacts:
    - path: "README.md"
      provides: "Root release-truth mirror"
      contains: "accrue_portal"
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "Host proof matrix aligned to the shipped three-package line"
      contains: "linked `"
    - path: ".planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md"
      provides: "Final post-publish verification ledger"
      contains: "## Post-publish verifier reruns"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "README.md"
      via: "version and package-set drift checks"
      pattern: "Hex vs `main`|accrue_portal"
    - from: "scripts/ci/verify_adoption_proof_matrix.sh"
      to: "examples/accrue_host/docs/adoption-proof-matrix.md"
      via: "proof-matrix taxonomy needles"
      pattern: "linked `|accrue_portal"
    - from: ".planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md"
      to: ".github/workflows/ci.yml"
      via: "post-publish shift-left bundle rerun results"
      pattern: "docs-contracts-shift-left|host-integration"
---

<objective>
Sweep the post-publish docs and verifier bundle so the released three-package line is mirrored honestly everywhere users are sent.

Purpose: PPX-13 and PPX-14 require the package docs, host proof docs, and shift-left bundle to agree with the actual published line after release, not the stale pre-publish pair story still present in several mirrors.
Output: Post-publish docs aligned to the shipped line, updated verifier needles where necessary, and a final `121-VERIFICATION.md` bundle showing green reruns.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-linked-publish-proof-sweep/121-RESEARCH.md
@.planning/phases/121-linked-publish-proof-sweep/121-VALIDATION.md
@.planning/phases/121-linked-publish-proof-sweep/121-02-SUMMARY.md
@.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md
@README.md
@accrue/guides/first_hour.md
@examples/accrue_host/README.md
@examples/accrue_host/docs/adoption-proof-matrix.md
@scripts/ci/verify_package_docs.sh
@scripts/ci/verify_adoption_proof_matrix.sh
@.github/workflows/ci.yml

<interfaces>
From `README.md`:
```md
the `accrue` / `accrue_admin` pair on the branch you are reading
```

From `examples/accrue_host/README.md`:
```md
This checked-in proof surface is the linked `accrue` / `accrue_admin` `1.0.0` release slice
```

From `examples/accrue_host/docs/adoption-proof-matrix.md`:
```md
This matrix is refreshed for the linked `1.0.0` pair
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align package and host mirrors to the shipped three-package release truth</name>
  <files>README.md; accrue/guides/first_hour.md; examples/accrue_host/README.md; examples/accrue_host/docs/adoption-proof-matrix.md; .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</files>
  <action>Use the shipped version and package availability recorded in `121-VERIFICATION.md` from Plan 02 as the truth source. Update the four known drift surfaces from research so they no longer describe a linked pair where the release contract is now a trio: root `README.md`, `accrue/guides/first_hour.md`, `examples/accrue_host/README.md`, and `examples/accrue_host/docs/adoption-proof-matrix.md`. Preserve the existing product story and default integration guidance; only broaden wording where those files make package-set or release-line claims. Make the edits verifier-exact, not approximate, so PPX-13 can prove stale pair text is gone and the shipped trio/version truth is present. Specifically require these file truths, all bound to `TARGET_VERSION`: `README.md` must state `Published trio on Hex: accrue, accrue_admin, and accrue_portal at TARGET_VERSION` and must not retain the phrase ``the `accrue` / `accrue_admin` pair on the branch you are reading``; `accrue/guides/first_hour.md` must state `Published release line: accrue, accrue_admin, and accrue_portal TARGET_VERSION` and must not contain `linked TARGET_VERSION pair`; `examples/accrue_host/README.md` must state `This checked-in proof surface is the linked accrue / accrue_admin / accrue_portal TARGET_VERSION release slice` and must not contain `accrue` / `accrue_admin` `TARGET_VERSION` release slice; `examples/accrue_host/docs/adoption-proof-matrix.md` must state `This matrix is refreshed for the linked TARGET_VERSION trio` and must not contain `This matrix is refreshed for the linked TARGET_VERSION pair`. Append the touched version/package-set mirror notes to `121-VERIFICATION.md` so later audits can see which public mirrors changed as part of PPX-13.</action>
  <verify>
    <automated>LEDGER=.planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md && TARGET_VERSION="$(sed -n 's/^TARGET_VERSION: //p' "$LEDGER")" && test -n "$TARGET_VERSION" && curl -fsSL https://hex.pm/api/packages/accrue_portal | jq -e --arg v "$TARGET_VERSION" '.latest_version == $v' >/dev/null && rg -Fx "Published trio on Hex: accrue, accrue_admin, and accrue_portal at ${TARGET_VERSION}" README.md && ! rg -F 'the `accrue` / `accrue_admin` pair on the branch you are reading' README.md && rg -Fx "Published release line: accrue, accrue_admin, and accrue_portal ${TARGET_VERSION}" accrue/guides/first_hour.md && ! rg -F "linked ${TARGET_VERSION} pair" accrue/guides/first_hour.md && rg -Fx "This checked-in proof surface is the linked accrue / accrue_admin / accrue_portal ${TARGET_VERSION} release slice" examples/accrue_host/README.md && ! rg -F "This checked-in proof surface is the linked \`accrue\` / \`accrue_admin\` \`${TARGET_VERSION}\` release slice" examples/accrue_host/README.md && rg -Fx "This matrix is refreshed for the linked ${TARGET_VERSION} trio" examples/accrue_host/docs/adoption-proof-matrix.md && ! rg -F "This matrix is refreshed for the linked ${TARGET_VERSION} pair" examples/accrue_host/docs/adoption-proof-matrix.md && rg -n "^## Post-publish mirror notes$|README.md|accrue/guides/first_hour.md|examples/accrue_host/README.md|examples/accrue_host/docs/adoption-proof-matrix.md" "$LEDGER"</automated>
  </verify>
  <done>The known pair-only public mirrors now reflect the shipped three-package line and the actual post-publish version without changing the underlying setup story unnecessarily.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run and, if needed, tighten the post-publish shift-left bundle</name>
  <files>scripts/ci/verify_package_docs.sh; scripts/ci/verify_adoption_proof_matrix.sh; .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</files>
  <action>Update `scripts/ci/verify_package_docs.sh` and `scripts/ci/verify_adoption_proof_matrix.sh` only where the current fixed needles still encode the stale pair-based release story or pre-publish version literals. Keep the verifier style consistent with Phase 120: fixed-string/regex guards, no parsers, and repo-root discovery. Before paying the full bundle cost, run a fast smoke loop using the two directly touched post-publish guards: `bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_adoption_proof_matrix.sh`. Append those dated smoke results to `121-VERIFICATION.md` under `## Post-publish smoke reruns`; this smoke loop is the task-level feedback gate and must stay fast enough for the Nyquist loop. After the smoke loop is green, run the full post-publish contract bundle required by `121-VALIDATION.md`: `bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_verify01_readme_contract.sh && bash scripts/ci/verify_production_readiness_discoverability.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/verify_core_admin_invoice_verify_ids.sh && bash scripts/ci/accrue_host_uat.sh`. Append the dated full-bundle results to `121-VERIFICATION.md` under `## Post-publish verifier reruns`. If the smoke loop fails, fix the source/verifier pair before running the slower full bundle; if any merge-blocking verifier still fails on trio truth or released-version drift, stop and fix the source/verifier pair in the same plan rather than downgrading the check.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && rg -n "## Post-publish smoke reruns|verify_package_docs\\.sh|verify_adoption_proof_matrix\\.sh" .planning/phases/121-linked-publish-proof-sweep/121-VERIFICATION.md</automated>
  </verify>
  <done>The smoke loop passes as the task-level gate, the full merge-blocking docs-contracts bundle and host proof gate then pass against the released docs state, and the Phase 121 ledger records both the smoke and full rerun results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs→integrator trust | User-facing docs must mirror the actual shipped line instead of branch-local pair assumptions. |
| docs state→merge-blocking verifiers | Shell gates must enforce the released truth so future drift fails before another release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-09 | I | Public release mirrors | mitigate | Update the four known pair-only mirrors to the shipped three-package truth and record the changes in `121-VERIFICATION.md`. |
| T-121-10 | T | `verify_package_docs.sh` | mitigate | Tighten only the needles that still encode stale pair/version language so PPX-13 becomes an executable invariant. |
| T-121-11 | T | `verify_adoption_proof_matrix.sh` | mitigate | Update matrix needles in the same PR as the matrix text so host proof taxonomy cannot drift silently after publish. |
| T-121-12 | D | Post-publish gate regressions | mitigate | Re-run the full bundle, including `accrue_host_uat.sh`, and stop on any failure instead of marking post-publish mirrors complete. |
</threat_model>

<verification>
Phase 121 closes only after the released-doc state passes the full post-publish bundle and that evidence is written into `121-VERIFICATION.md`.
</verification>

<success_criteria>
The released three-package line is mirrored honestly in package/host docs, and the full shift-left + host verification bundle passes against that post-publish truth.
</success_criteria>

<output>
After completion, create `.planning/phases/121-linked-publish-proof-sweep/121-03-SUMMARY.md`
</output>

---
phase: 123-config-core-gate-api-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/telemetry/otel.ex
  - accrue/test/accrue/telemetry/otel_test.exs
autonomous: true
requirements: [ENT-05]
must_haves:
  truths:
    - "OTel spans for entitlement checks retain the 6 entitlement attributes instead of silently dropping them"
  artifacts:
    - path: "accrue/lib/accrue/telemetry/otel.ex"
      provides: "@allowed_attributes extended with the 6 D-19 entitlement keys (atom + string forms)"
      contains: ":feature"
    - path: "accrue/test/accrue/telemetry/otel_test.exs"
      provides: "assertion that sanitize_attributes/1 retains the 6 new keys"
      contains: "sanitize_attributes"
  key_links:
    - from: "Accrue.Telemetry.OTel.sanitize_attributes/1"
      to: "@allowed_attributes"
      via: "strict allowlist forward (drops unlisted keys)"
      pattern: "allowed_attributes"
---

<objective>
Extend the strict OTel `@allowed_attributes` allowlist in `telemetry/otel.ex` with the six entitlement metadata keys (D-19) so entitlement OTel spans actually carry their attributes. Without this, every entitlement-specific OTel attribute is silently dropped by `sanitize_attributes/1`.

Purpose: ENT-05 (OTel observability half) — the D-18 entitlement metadata (`:feature`, `:result`, `:resolver`, `:reason`, `:subject_type`, `:subject_id`) must survive the OTel bridge. The raw `:telemetry` events are unaffected (only the OTel bridge filters), but operators relying on traces would see attribute-less spans.
Output: 6 keys (atom + `"accrue.<key>"` string form each) added to `@allowed_attributes`; an `otel_test.exs` assertion proving they are retained.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-config-core-gate-api-foundation/123-CONTEXT.md
@.planning/phases/123-config-core-gate-api-foundation/123-PATTERNS.md
@.planning/phases/123-config-core-gate-api-foundation/123-VALIDATION.md

<interfaces>
<!-- The six D-19 keys to add (atom + "accrue.<key>" string form for EACH). -->
<!-- Map each atom to its "accrue.<key>" namespaced OTel attribute string. -->
<!--   :feature      -> "accrue.feature"      -->
<!--   :result       -> "accrue.result"       (do NOT reuse :status / "accrue.status") -->
<!--   :resolver     -> "accrue.resolver"     -->
<!--   :reason       -> "accrue.reason"        -->
<!--   :subject_type -> "accrue.subject_type" -->
<!--   :subject_id   -> "accrue.subject_id"   -->
<!-- @prohibited_keys (otel.ex L29-51) already blocks :email/:address/etc (PII rule, D-18) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 6 entitlement keys to @allowed_attributes and assert retention</name>
  <files>accrue/lib/accrue/telemetry/otel.ex, accrue/test/accrue/telemetry/otel_test.exs</files>
  <read_first>
    - accrue/lib/accrue/telemetry/otel.ex L12-27 (@allowed_attributes — the strict allowlist map to extend; note both atom and "accrue.<key>" string entries exist for each key), L29-51 (@prohibited_keys — already blocks PII), L99-111 (sanitize_attributes/1 — drops any key not in @allowed_attributes)
    - accrue/test/accrue/telemetry/otel_test.exs L12-41 (existing sanitize_attributes/1 assertions — extend with a new test)
    - 123-PATTERNS.md § telemetry/otel.ex (MODIFY) (L243-257) and § Test: OTel allowlist (L341-343)
    - 123-CONTEXT.md D-18 (metadata map + PII rule) and D-19 (the 6 allowlist additions, atom + string)
    - 123-RESEARCH.md § Pitfall (123-specific): OTel attribute drop (L336-339) and the D-18/19 verification row (L160)
  </read_first>
  <action>
    Extend the `@allowed_attributes` map in `otel.ex` with the six D-19 keys, adding BOTH the atom form and the `"accrue.<key>"` string form for each, mirroring how every existing key (e.g. `:processor`/`"accrue.processor"`, `:status`/`"accrue.status"`) appears twice in the map: `:feature` -> `"accrue.feature"`, `:result` -> `"accrue.result"`, `:resolver` -> `"accrue.resolver"`, `:reason` -> `"accrue.reason"`, `:subject_type` -> `"accrue.subject_type"`, `:subject_id` -> `"accrue.subject_id"`. Do NOT reuse `:status`/`"accrue.status"` for `:result` — `:result` is a distinct key (D-19). Do not touch `@prohibited_keys` (it already blocks PII, satisfying D-18). Then add a test to `otel_test.exs` asserting `Accrue.Telemetry.OTel.sanitize_attributes(%{feature: :advanced_reports, result: true, resolver: :local_map, reason: :entitled, subject_type: "User", subject_id: "00000000-0000-0000-0000-000000000000"})` returns a map containing all six keys mapped to their `accrue.<key>` namespaced names (none dropped) — extend the existing `sanitize_attributes/1` assertion block style.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/telemetry/otel_test.exs 2>&1 | tail -15 && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accrue.subject_id\|accrue.feature\|accrue.result\|accrue.resolver\|accrue.reason\|accrue.subject_type" accrue/lib/accrue/telemetry/otel.ex` >= 6 (string forms present).
    - `grep -E ":feature|:resolver|:subject_type|:subject_id" accrue/lib/accrue/telemetry/otel.ex` shows the atom forms inside `@allowed_attributes`.
    - `otel_test.exs` contains a test calling `sanitize_attributes/1` with all six entitlement keys and asserting all six are retained (maps to VALIDATION.md ENT-05 OTel row).
    - `cd accrue && mix test test/accrue/telemetry/otel_test.exs` is GREEN.
    - `cd accrue && mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>The 6 entitlement keys (atom + string) are in `@allowed_attributes`; the `otel_test.exs` retention assertion is green; clean compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entitlement metadata -> OTel exporter | Span metadata crosses into the OTel telemetry pipeline (potentially exported off-host). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-04 | Information Disclosure | PII leakage via OTel span attributes | mitigate | Strict `@allowed_attributes` allowlist forwards ONLY the 6 bounded-cardinality keys; `@prohibited_keys` already blocks `:email`/`:address`/etc; `:subject_id` is an internal UUID only, never email/name/external id (D-18 PII rule). |
| T-123-05 | Repudiation | Attribute-less spans hide which decision/feature was checked | mitigate | Adding the 6 keys ensures `feature`/`result`/`reason`/`resolver`/`subject_type`/`subject_id` survive the bridge so operators can attribute decisions in traces (D-19/D-20). |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages installed in this phase. No install task in this plan. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/telemetry/otel_test.exs` — retention assertion green (VALIDATION.md ENT-05 OTel row).
- `cd accrue && mix compile --warnings-as-errors` — clean.
</verification>

<success_criteria>
- `sanitize_attributes/1` retains all six entitlement keys (atom + string) instead of dropping them (ENT-05 OTel half).
- `@prohibited_keys` untouched; PII rule (D-18) intact.
- No new external dependency added.
</success_criteria>

<output>
Create `.planning/phases/123-config-core-gate-api-foundation/123-02-SUMMARY.md` when done.
</output>

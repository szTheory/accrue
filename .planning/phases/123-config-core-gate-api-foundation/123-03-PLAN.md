---
phase: 123-config-core-gate-api-foundation
plan: 03
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - accrue/lib/accrue/entitlements/plan.ex
  - accrue/lib/accrue/entitlements/resolver.ex
  - accrue/lib/accrue/entitlements/resolver/local_map.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/test/accrue/entitlements/local_map_test.exs
  - accrue/test/accrue/entitlements_test.exs
autonomous: true
requirements: [ENT-02, ENT-03, ENT-04, ENT-05]
must_haves:
  truths:
    - "entitled?/2 resolves from local subscription state with zero processor API calls, true iff an affirmative resolved feature match"
    - "has_active_plan?/2 derives from Subscription.active?/1 lifecycle truth (never raw .status), accepts plan atom OR price_id string, and tests membership in the SET of ALL active plan atoms (true for BOTH plans when a billable holds two active subs on different mapped plans)"
    - "features_for/1 returns a sorted, deduped, plain [atom] UNION across active subs, never a MapSet"
    - "entitlement_quantity/2 returns min(cap, quantity) when a cap exists else quantity, fail-closed 0"
    - "nil/non-billable/no-customer/no-active-sub/unmapped/raising-resolver all collapse to false/[]/0"
    - "each check emits [:accrue, :entitlements, :check, :start|:stop|:exception] with D-18 metadata; zero accrue_events rows written"
  artifacts:
    - path: "accrue/lib/accrue/entitlements/plan.ex"
      provides: "%Accrue.Entitlements.Plan{plan_id, features: MapSet, quantities: map} value struct (no Ecto)"
      contains: "defstruct"
    - path: "accrue/lib/accrue/entitlements/resolver.ex"
      provides: "@behaviour resolve/2 callback (returns plan + active_plans SET + features + quantities) + __impl__/0 runtime dispatch"
      contains: "@callback resolve"
    - path: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      provides: "default resolver: read-only customer + active-subs fold to %{plan, active_plans: MapSet, features: MapSet, quantities}"
      contains: "defmodule Accrue.Entitlements.Resolver.LocalMap"
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "public context: entitled?/2, has_active_plan?/2 (set-membership), features_for/1, entitlement_quantity/2 (inline span, fail-closed)"
      contains: "Accrue.Telemetry.span([:accrue, :entitlements, :check]"
  key_links:
    - from: "Accrue.Entitlements.Resolver.LocalMap.resolve/2"
      to: "Accrue.Billing.Query.active/1 + Subscription + SubscriptionItem + Customer"
      via: "read-only Ecto query (Query.active scoped to customer, items joined)"
      pattern: "Query.active"
    - from: "Accrue.Entitlements (each public fn)"
      to: "[:accrue, :entitlements, :check]"
      via: "inline Accrue.Telemetry.span/3"
      pattern: "Telemetry.span\\(\\[:accrue, :entitlements, :check\\]"
    - from: "Accrue.Entitlements.has_active_plan?/2"
      to: "resolved active_plans MapSet"
      via: "MapSet.member? on the set of active plan atoms (reverse-index string arg first)"
      pattern: "MapSet.member\\?"
    - from: "Accrue.Entitlements"
      to: "Accrue.Entitlements.Resolver.__impl__/0"
      via: "runtime dispatch resolve(billable, opts)"
      pattern: "__impl__"
---

<objective>
Build the `Accrue.Entitlements` context tree: the `%Plan{}` value struct + `Resolver` behaviour (contracts), the `LocalMap` default resolver (read-only customer + active-subs fold), and the public context with the four fail-closed gate functions wrapped in inline telemetry spans. This is the core of ENT-02/03/04/05 and the read path the whole milestone inherits.

Purpose: ENT-02 (has_active_plan? from local state, reusing active?/1), ENT-03 (entitled?/features_for fail-closed), ENT-04 (entitlement_quantity read-only seat), ENT-05 (check telemetry, zero ledger writes). Resolution is local-only — zero processor calls (D-09), never the effectful Billing.customer/1.
Output: `plan.ex`, `resolver.ex`, `resolver/local_map.ex`, `entitlements.ex`, plus `local_map_test.exs` (read-path) and `entitlements_test.exs` (4-fn happy/edge + telemetry + ledger boundary). The property test lives in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-config-core-gate-api-foundation/123-CONTEXT.md
@.planning/phases/123-config-core-gate-api-foundation/123-PATTERNS.md
@.planning/phases/123-config-core-gate-api-foundation/123-VALIDATION.md
@.planning/phases/123-config-core-gate-api-foundation/123-01-SUMMARY.md

<interfaces>
<!-- Contracts created in Task 1, consumed by Tasks 2 & 3. Build to these exactly. -->

<!-- plan.ex (value struct, NO Ecto) -->
defstruct [:plan_id, features: MapSet.new(), quantities: %{}]
@type t :: %Accrue.Entitlements.Plan{plan_id: term(), features: MapSet.t(), quantities: map()}

<!-- resolver.ex (behaviour + runtime dispatch) -->
<!-- MULTI-ACTIVE-PLAN CONTRACT (load-bearing): the resolved map carries `active_plans`, the SET -->
<!-- of ALL active plan atoms. `:plan` is a single representative (last folded, or nil) for -->
<!-- display/back-compat ONLY. has_active_plan?/2 (Task 3) MUST test membership in `active_plans`, -->
<!-- NEVER in `:plan` — a single representative would make has_active_plan?(billable, other_plan) -->
<!-- return a WRONG false for a billable holding two active subs on two different mapped plans. -->
<!-- This keeps has_active_plan?/2 consistent with entitled?/2 + features_for/1, which already -->
<!-- UNION across all active subs. -->
@callback resolve(billable :: term(), opts :: keyword()) ::
            {:ok, %{
               plan: term(),                 # representative active plan atom (or nil) — display only, NOT membership
               active_plans: MapSet.t(),     # SET of ALL active plan atoms — the membership source of truth
               features: MapSet.t(),         # UNION of features across all active subs
               quantities: map()             # merged min(cap, qty) per quota_key
             }}
            | {:error, term()}
def __impl__ do
  :accrue
  |> Application.get_env(:entitlements, [])
  |> Keyword.get(:resolver, Accrue.Entitlements.Resolver.LocalMap)
end
<!-- NO capabilities/0 callback in 123 — that is ENT-08/Phase 125. -->

<!-- Already available from prior plans / billing core: -->
<!--   Accrue.Config.entitlements/0      (Plan 01) -> keyword: [plans:, resolver:, unmapped_action:] -->
<!--   Accrue.Billing.Query.active/1      (query.ex L30-32) -> where s.status in [:active, :trialing] -->
<!--   Accrue.Billing.Subscription.active?/1 (subscription.ex L146-149) -->
<!--   Accrue.Billing.SubscriptionItem    -> .price_id (L26), .quantity (L29 default 1) -->
<!--   Accrue.Billing.Customer            -> .owner_type, .owner_id (L48-49), has_many :subscriptions (L65) -->
<!--   billable.__struct__.__accrue__(:billable_type)  (billable.ex L66) -> owner_type -->
<!--   to_string(billable.id)             -> owner_id -->
<!--   Accrue.Telemetry.span/3            (telemetry.ex L74-83) -- reuses ONE base_metadata for :start AND :stop -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define contracts — Plan struct + Resolver behaviour with runtime dispatch</name>
  <files>accrue/lib/accrue/entitlements/plan.ex, accrue/lib/accrue/entitlements/resolver.ex</files>
  <read_first>
    - accrue/lib/accrue/plan_resolver.ex L27-39 (resolved_plan typespec shape + single result-tuple @callback shape)
    - accrue/lib/accrue/processor.ex L347-348 (__impl__/0 runtime dispatch idiom), and accrue/lib/accrue/auth.ex L41-47, L105 (behaviour @callback + impl/0)
    - accrue/lib/accrue/config.ex L570-576 (plan_resolver/0 — the same Application.get_env dispatch idiom)
    - 123-PATTERNS.md § resolver.ex (L86-114) and § plan.ex (L151-163)
    - 123-CONTEXT.md D-11, D-12, D-13 (resolver seam now, file set, dispatch idiom)
  </read_first>
  <behavior>
    - `%Accrue.Entitlements.Plan{}` has fields `plan_id` (default nil), `features` (default `MapSet.new()`), `quantities` (default `%{}`); is a plain struct (no `use Ecto.Schema`).
    - `Accrue.Entitlements.Resolver` declares `@callback resolve(billable, opts) :: {:ok, %{plan:, active_plans: MapSet.t, features: MapSet.t, quantities: map}} | {:error, term}` — the `active_plans` SET is the multi-active-plan membership contract (see the `<interfaces>` MULTI-ACTIVE-PLAN CONTRACT note).
    - `Accrue.Entitlements.Resolver.__impl__/0` returns `Accrue.Entitlements.Resolver.LocalMap` when `:entitlements` config has no `:resolver`, and returns the configured module when set.
  </behavior>
  <action>
    Create `entitlements/plan.ex`: `defmodule Accrue.Entitlements.Plan` with `defstruct [:plan_id, features: MapSet.new(), quantities: %{}]` and `@type t` per the `<interfaces>` block. Pure value type — NO `use Ecto.Schema`, no DB anything (the cache schema is a Phase 127 concern, D-12). Create `entitlements/resolver.ex`: `defmodule Accrue.Entitlements.Resolver` with the `@callback resolve/2` from `<interfaces>` — the result map MUST include `active_plans: MapSet.t()` (the SET of all active plan atoms) alongside `plan`, `features`, `quantities`; document in the `@callback`/moduledoc that `active_plans` is the membership source of truth and `:plan` is a representative only. Add a `@doc false` `__impl__/0` that reads `Application.get_env(:accrue, :entitlements, []) |> Keyword.get(:resolver, Accrue.Entitlements.Resolver.LocalMap)` (mirror `Processor.__impl__/0`). NO `capabilities/0` callback (that is ENT-08/Phase 125, D-12). Add a small assertion to a new or existing test that `__impl__/0` defaults to `LocalMap` and honors a configured override (you may colocate this in `local_map_test.exs` created in Task 2, or a brief inline test here — keep it runnable).
  </action>
  <verify>
    <automated>cd accrue && mix compile --warnings-as-errors 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements/plan.ex` exists with `defstruct` and no `use Ecto.Schema` (`grep -L "Ecto.Schema" accrue/lib/accrue/entitlements/plan.ex` returns the file path, i.e. Ecto.Schema absent).
    - `accrue/lib/accrue/entitlements/resolver.ex` contains `@callback resolve` and a `__impl__` function.
    - `grep -c "active_plans" accrue/lib/accrue/entitlements/resolver.ex` returns >= 1 (the multi-active-plan membership field is declared in the callback contract).
    - `grep -n "Application.get_env(:accrue, :entitlements" accrue/lib/accrue/entitlements/resolver.ex` shows the runtime dispatch.
    - `grep -c "capabilities" accrue/lib/accrue/entitlements/resolver.ex` returns 0 (no premature ENT-08 callback).
    - `cd accrue && mix compile --warnings-as-errors` succeeds (contracts compile; LocalMap may not exist yet but the default atom is a bare literal, not a compile reference).
  </acceptance_criteria>
  <done>Plan struct + Resolver behaviour (with the active_plans SET contract) + runtime dispatch compile cleanly; contracts ready for the impl and context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LocalMap resolver — read-only customer + active-subs fold (carries the active_plans SET)</name>
  <files>accrue/lib/accrue/entitlements/resolver/local_map.ex, accrue/test/accrue/entitlements/local_map_test.exs</files>
  <read_first>
    - accrue/lib/accrue/billing.ex L788-796 (PRIVATE fetch_customer/2 — clone its query body; do NOT call it), L754-786 (the FORBIDDEN effectful customer/1 — must NOT be called)
    - accrue/lib/accrue/billing/query.ex L30-32 (active/1), accrue/lib/accrue/billing/subscription.ex L146-149 (active?/1), accrue/lib/accrue/billing/subscription_item.ex L26,L29 (price_id, quantity default 1), accrue/lib/accrue/billing/customer.ex L48-49,L65 (owner_type/owner_id, has_many :subscriptions)
    - accrue/lib/accrue/billable.ex L48-68 (__accrue__(:billable_type) macro)
    - accrue/lib/accrue/repo.ex (read APIs all/2, one/2)
    - accrue/lib/accrue/test/factory.ex L92-203 (active/trialing/canceled subscription factories, :price_id override, returns %{customer:, subscription:, items:})
    - accrue/lib/accrue/billing.ex L55-59 (subscribe/2,3 — the fetch-or-create path) + accrue/lib/accrue/billing/subscription_actions.ex L94-102 (subscribe(%Customer{}) reuses the customer; subscribe(billable) routes through Billing.customer/1) — the two-active-plan fixture issues a SECOND subscribe on the SAME customer, NOT a second factory call
    - accrue/test/support/billing_case.ex (SQL Sandbox + Fake processor harness)
    - accrue/test/accrue/storage/null_test.exs L6-19 (app-env restore for the :entitlements config the resolver reads)
    - 123-PATTERNS.md § local_map.ex (L118-147) and § Lifecycle truth reuse (L284-287)
    - 123-RESEARCH.md § Read-only customer lookup (L353-368), § Active subs + items fold (L370-383), Open Question 2 RESOLVED (price_id-string semantics + multi-active-plan set)
    - 123-CONTEXT.md D-04, D-09, D-13 (read path, unmapped_action, never customer/1, reverse-index)
  </read_first>
  <behavior>
    - `resolve(billable, opts)` for a billable with an active sub on a mapped plan returns `{:ok, %{plan: plan_atom, active_plans: MapSet.new([plan_atom]), features: MapSet(union), quantities: %{quota_key => min(cap, qty)}}}`.
    - **Multi-active-plan (load-bearing):** for ONE billable holding TWO active subs on TWO different mapped plans P1, P2 → `active_plans` is `MapSet.new([:p1, :p2])` (BOTH atoms), `features` is the UNION of P1's and P2's features, and `quantities` is the merged map. `:plan` carries a single representative (e.g. the last folded plan) and is NOT used for membership. **Fixture construction (critical):** the two subs MUST share ONE customer/billable, so the resolver's `(owner_type, owner_id) limit:1` lookup returns that single customer and the fold sees both subs. Build it by creating ONE `active_subscription/1` (price `"price_p1"`) then issuing a SECOND `Accrue.Billing.subscribe(result.customer, "price_p2")` — NOT a second `active_subscription/1` factory call (each factory call mints a FRESH random owner_id via `customer/1`, producing two DISTINCT customers; forcing the same `:owner_id` would hit the `accrue_customers_owner_type_owner_id_processor` unique index and raise). `subscribe/2` on the existing `%Customer{}` reuses it (subscription_actions.ex L94-96), yielding two active subs on the one customer the resolver resolves.
    - `nil`/wrong-shape billable -> `{:ok, %{plan: nil, active_plans: MapSet.new(), features: MapSet.new(), quantities: %{}}}` (fail-closed empty, NOT `{:error,_}`).
    - billable with no `accrue_customers` row -> empty result (empty `active_plans`); billable with a customer but no active sub -> empty result.
    - active sub whose price_id is unmapped: under `:deny` (default) the item is dropped from the union AND its (absent) plan is NOT added to `active_plans`; under `:raise` the resolver raises (so the context's try/rescue collapses it to fail-closed).
    - trialing sub counts as active (reuses `active?/1`/`Query.active/1`); canceled does not.
    - read path makes ZERO processor calls — never `Accrue.Billing.customer/1`, never `Accrue.Processor.*`, never raw `.status`.
  </behavior>
  <action>
    Create `entitlements/resolver/local_map.ex`: `defmodule Accrue.Entitlements.Resolver.LocalMap`, `@behaviour Accrue.Entitlements.Resolver`. Implement `resolve/2`: (1) read-only customer lookup guarded by a `%{__struct__: mod, id: id}` head — `owner_type = mod.__accrue__(:billable_type)`, `owner_id = to_string(id)`, `Accrue.Repo.one(from c in Accrue.Billing.Customer, where: c.owner_type == ^owner_type and c.owner_id == ^owner_id, limit: 1)`; a catch-all `_ -> nil` clause so nil/wrong-shape billable yields nil customer; nil customer -> `{:ok, empty_result}` where `empty_result = %{plan: nil, active_plans: MapSet.new(), features: MapSet.new(), quantities: %{}}`. (2) Compose the active-subs read: `Accrue.Billing.Query.active(Subscription) |> where([s], s.customer_id == ^customer.id) |> join(:inner, [s], i in SubscriptionItem, on: i.subscription_id == s.id) |> select([_s, i], {i.price_id, i.quantity}) |> Accrue.Repo.all()`. (3) Build the `price_id => plan` reverse index from `Accrue.Config.entitlements/0` `:plans`. (4) Fold each active `{price_id, quantity}` into an accumulator carrying `active_plans` (a `MapSet` of plan atoms), `features` (a `MapSet`), `quantities` (a map), and `plan` (representative): mapped -> `MapSet.put(active_plans, plan_atom)`, union the plan's `features` into the features MapSet, merge `quantities` as `min(limits_cap, quantity)` per quota_key (cap from the plan's `:limits`; if no cap, use quantity), set `plan` to the mapped atom (representative — last write wins); unmapped -> under `:deny` drop it (do NOT add to `active_plans`), under `:raise` raise (read `unmapped_action` from config). Return `{:ok, %{plan: representative_or_nil, active_plans: active_plans_set, features: union_mapset, quantities: merged_map}}`. **`active_plans` is the membership source of truth that `has_active_plan?/2` (Task 3) tests against — `:plan` is representative/display only and MUST NOT be used for membership (a single representative would wrongly answer false for a real second active plan).** NEVER call the effectful `Billing.customer/1`. Then create `local_map_test.exs` with `use Accrue.BillingCase` (SQL Sandbox + factory): test active-sub maps to features/quantities and `active_plans` contains the plan; **TWO active subs on TWO different mapped plans (on ONE billable) -> `active_plans` contains BOTH plan atoms and `features` is the union — construct this fixture by creating ONE `active_subscription/1` with `:price_id` `"price_p1"`, then `{:ok, _} = Accrue.Billing.subscribe(result.customer, "price_p2")` so both subs share the one customer the resolver looks up; do NOT make a second `active_subscription/1` call (it mints a different customer)**; trialing -> active; canceled -> empty (`active_plans` empty); unmapped price_id under `:deny` -> dropped (not in `active_plans`); `min(cap, quantity)` and no-cap cases; nil/wrong-shape -> empty result. Mutate `:entitlements` via `Application.put_env` with `on_exit` restore (`async: false`). Include the Task 1 `__impl__/0` default+override assertion here if not done in Task 1.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/local_map_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements/resolver/local_map.ex` declares `@behaviour Accrue.Entitlements.Resolver` and implements `resolve/2`.
    - `grep -c "active_plans" accrue/lib/accrue/entitlements/resolver/local_map.ex` returns >= 1 (the resolved map carries the active-plans SET).
    - `grep -c "Billing.customer\b\|Accrue.Processor\|s.status" accrue/lib/accrue/entitlements/resolver/local_map.ex` returns 0 (no effectful customer/1, no processor calls, no raw .status — uses Query.active/1).
    - `grep -n "Query.active" accrue/lib/accrue/entitlements/resolver/local_map.ex` shows lifecycle-truth reuse.
    - `grep -n "owner_type\|owner_id" accrue/lib/accrue/entitlements/resolver/local_map.ex` shows the read-only customer lookup (cloned, not calling private fetch_customer/2).
    - `cd accrue && mix test test/accrue/entitlements/local_map_test.exs` is GREEN — covers active->features+active_plans, **two-different-active-plans on ONE billable (built via one `active_subscription/1` + a second `Accrue.Billing.subscribe(customer, "price_p2")`, NOT two factory calls) -> active_plans holds BOTH atoms + features union**, trialing->active, canceled->empty, unmapped->dropped, min(cap,qty)/no-cap, nil/wrong-shape->empty (maps to VALIDATION.md ENT-02/03/04 read-path rows incl. the multi-active-plan row).
  </acceptance_criteria>
  <done>LocalMap resolves entitlements from local subscription state with zero processor calls and carries the active_plans SET (multi-active-plan correct); read-path tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the public context — 4 fail-closed gate functions with inline telemetry (has_active_plan? = set membership)</name>
  <files>accrue/lib/accrue/entitlements.ex, accrue/test/accrue/entitlements_test.exs</files>
  <read_first>
    - accrue/lib/accrue/storage.ex L42-76 (inline Accrue.Telemetry.span/3 template — the cleanest 3-level-domain facade), L79 (impl/0)
    - accrue/lib/accrue/telemetry.ex L74-83 (span/3 — CRITICAL: reuses ONE base_metadata for :start AND :stop; compute result/reason BEFORE opening the span)
    - accrue/lib/accrue/billing/subscription.ex L146-149 (active?/1 catch-all-false idiom to mirror for fail-closed)
    - accrue/lib/accrue/events.ex L109 (record/1), L135 (record_multi/3) — the ledger APIs that must NOT be called here
    - accrue/lib/accrue/entitlements/resolver.ex (Task 1 — __impl__/0 + the active_plans contract), accrue/lib/accrue/entitlements/plan.ex (Task 1)
    - accrue/lib/accrue/billing.ex L55-59 (subscribe/2,3) + accrue/lib/accrue/billing/subscription_actions.ex L94-102 (subscribe(%Customer{}) reuses the customer) — the multi-active-plan fixture builds the SECOND active sub via `Accrue.Billing.subscribe(customer, "price_p2")` on the SAME customer, NOT a second factory call
    - accrue/lib/accrue/test/factory.ex L92-203 (active_subscription/1 builds the FIRST sub + its customer; each call mints a fresh owner_id, so the second active plan must reuse the first's customer via subscribe/2)
    - accrue/test/accrue/storage/null_test.exs L6-19 (app-env restore), L46-73 (telemetry attach_many -> send self pattern)
    - 123-PATTERNS.md § entitlements.ex (L33-83), § Shared Patterns (inline span, fail-closed predicate, ledger boundary)
    - 123-CONTEXT.md D-06, D-07, D-08, D-16, D-17, D-18, D-20, D-21 (the four fn signatures, boolean-only, fail-closed mechanics, plural events, inline span, metadata map, unmapped reason, ZERO ledger)
    - 123-RESEARCH.md Open Question 2 RESOLVED (has_active_plan? = membership in the active_plans SET; string arg reverse-indexed first; multi-active-plan true for both)
    - 123-VALIDATION.md § Per-Requirement Verification Map (ENT-02/03/04/05 example rows, incl. the multi-active-plan ENT-02 row)
  </read_first>
  <behavior>
    - `entitled?(billable, feature) :: boolean` — true iff the resolved active feature set contains `feature`; nil/non-billable/no-customer/no-active-sub/unmapped/raising-resolver -> false.
    - `has_active_plan?(billable, plan) :: boolean` — `plan` is atom (canonical) OR price_id string (reverse-indexed to its plan atom, then checked). Tests `MapSet.member?(resolved.active_plans, plan_atom)` — the SET of ALL active plan atoms — so a billable with TWO active subs on TWO different mapped plans returns `true` for BOTH plans. Unmapped/garbage string or unknown atom -> false (`reason: :unmapped_plan` for the unmapped-string case). Fail-closed false; reuses active?/1 truth via the resolver. NEVER tests the representative `:plan` field (that would yield a wrong false negative for the second active plan).
    - `features_for(billable) :: [atom]` — sorted, deduped UNION across active subs; returns a plain list, NEVER a MapSet; fail-closed `[]`.
    - `entitlement_quantity(billable, quota_key) :: non_neg_integer` — `min(cap, quantity)` when cap exists else quantity; fail-closed `0`.
    - Each call emits `[:accrue, :entitlements, :check, :start|:stop|:exception]` with D-18 metadata: `%{feature, result: boolean, resolver: :local_map, reason: :entitled|:not_entitled|:unmapped_plan|:no_active_subscription|:error, subject_type, subject_id}`.
    - Unmapped-plan deny puts `reason: :unmapped_plan` in `:stop` metadata.
    - ZERO `accrue_events` rows written during any check.
  </behavior>
  <action>
    Create `entitlements.ex`: `defmodule Accrue.Entitlements` with the four public functions per D-06 signatures. Each function: dispatch via `Accrue.Entitlements.Resolver.__impl__().resolve(billable, opts)` inside a `try/rescue/catch` so exceptions/throws/exits collapse to the fail-closed value (`false`/`[]`/`0` respectively — D-08). Compute the decision AND the D-18 `reason`/`result` BEFORE opening the span (telemetry.ex L74-83 reuses one base_metadata for :start and :stop — confirmed in PATTERNS.md L47-63), then wrap the projection in `Accrue.Telemetry.span([:accrue, :entitlements, :check], metadata, fn -> <return the resolved boolean/list/int> end)`. Build D-18 metadata: `feature` (the feature/quota_key/plan being checked, as applicable), `result` boolean, `resolver: :local_map` (a tag, not the module — derive via a small `resolver_tag/0` or hardcode `:local_map` for the default; if a non-default resolver is configured, derive a tag), `reason` from the D-18 atom set, `subject_type` (the billable struct name as string), `subject_id` (the internal UUID `to_string(billable.id)`; nil-safe for non-billable). `entitled?`: pattern-match the resolved features MapSet membership as the SOLE true path; `{:error, _}` / no match -> false (D-08); nil/wrong-type billable -> false with `reason: :no_active_subscription` or `:not_entitled` as appropriate. `has_active_plan?`: accept atom or string `plan`; if string, reverse-index to the plan atom via the `:entitlements` `price_id => plan` map (unmapped string -> false, `reason: :unmapped_plan`); then return `MapSet.member?(resolved.active_plans, plan_atom)` — test the SET of ALL active plans, NEVER the representative `:plan` field, so multi-active-plan customers answer true for each of their active plans (consistent with the UNION semantics of entitled?/features_for). `features_for`: take the resolved features MapSet, `MapSet.to_list |> Enum.sort` — NEVER leak a MapSet (D-06). `entitlement_quantity`: `Map.get(quantities, quota_key, 0)`. Boolean-only API — do NOT add a `fetch_entitled/2` tuple variant (D-07). NEVER call `Accrue.Events.record/1` or `record_multi/3` (D-21). Then create `entitlements_test.exs`: (a) happy/edge for all four fns (atom + price_id-string has_active_plan?, **two active subs on two different mapped plans on ONE billable -> has_active_plan? true for BOTH plan atoms (and both price_id strings); build this fixture by creating ONE `active_subscription/1` (price `"price_p1"`) then `{:ok, _} = Accrue.Billing.subscribe(result.customer, "price_p2")` (fetch-or-create reuses the same customer so the resolver sees both subs) — NOT a second `active_subscription/1` call (it mints a different customer and the resolver's limit:1 lookup would only see one sub)**, trialing->true/canceled->false, features_for sorted-deduped-union returning a plain list never a MapSet, entitlement_quantity min(cap,qty)/no-cap/0); (b) telemetry: attach to the 3 plural events, call each fn, assert :start+:stop received and :stop metadata contains feature/result(boolean)/resolver: :local_map/reason(in the D-18 atom set)/subject_type/subject_id; assert metadata does NOT contain :email/:name/external ids (PII); unmapped-plan check -> assert reason: :unmapped_plan in :stop; (c) ledger boundary: count `accrue_events` rows before/after a batch of checks and assert unchanged (D-21). Use `use Accrue.BillingCase`, mutate `:entitlements` via `Application.put_env` with on_exit restore, `async: false`.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements_test.exs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements.ex` defines `entitled?/2`, `has_active_plan?/2`, `features_for/1`, `entitlement_quantity/2` (`grep -E "def (entitled\?|has_active_plan\?|features_for|entitlement_quantity)" accrue/lib/accrue/entitlements.ex` shows all four).
    - `grep -c "MapSet.member?" accrue/lib/accrue/entitlements.ex` returns >= 1 (has_active_plan?/2 tests set membership against active_plans, not the representative :plan).
    - `grep -c "Telemetry.span(\[:accrue, :entitlements, :check\]" accrue/lib/accrue/entitlements.ex` >= 1 (PLURAL event literal, D-16).
    - `grep -c "try\|rescue\|catch" accrue/lib/accrue/entitlements.ex` shows the fail-closed wrapper present.
    - `grep -c "Events.record\|record_multi" accrue/lib/accrue/entitlements.ex` returns 0 (ledger boundary, D-21).
    - `grep -c "fetch_entitled" accrue/lib/accrue/entitlements.ex` returns 0 (boolean-only, D-07).
    - `grep -c "MapSet.to_list" accrue/lib/accrue/entitlements.ex` >= 1 (features_for never leaks a MapSet, D-06).
    - The two-different-active-plans assertion is concrete and runnable: a test sets `:entitlements` with two mapped plans (`:p1` price `"price_p1"`, `:p2` price `"price_p2"`), creates ONE billable/customer with the first active sub via `active_subscription/1` (`:price_id` `"price_p1"`), then adds the SECOND active sub to that SAME customer via `{:ok, _} = Accrue.Billing.subscribe(result.customer, "price_p2")` (NOT a second `active_subscription/1` call, which would mint a distinct customer and break the resolver's single-customer lookup), and asserts `assert Accrue.Entitlements.has_active_plan?(billable, :p1)` AND `assert Accrue.Entitlements.has_active_plan?(billable, :p2)` (and `assert Accrue.Entitlements.has_active_plan?(billable, "price_p1")` AND `... "price_p2"`), while `refute Accrue.Entitlements.has_active_plan?(billable, :enterprise)` for an unmapped plan.
    - `cd accrue && mix test test/accrue/entitlements_test.exs` is GREEN — covers all four fns happy/edge, the multi-active-plan has_active_plan? true-for-both case, telemetry start/stop with D-18 metadata + PII assertion + reason: :unmapped_plan, and the ledger-boundary row-count assertion (maps to VALIDATION.md ENT-02/03/04/05 rows).
  </acceptance_criteria>
  <done>The four fail-closed gate functions (has_active_plan? = active_plans set membership, multi-active-plan correct), inline plural-event telemetry with D-18 metadata, and the zero-ledger-write boundary all land; `entitlements_test.exs` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host code -> Accrue.Entitlements gate | Untrusted billable term (possibly nil/wrong-type/garbage) crosses into the authorization decision. |
| Accrue.Entitlements -> Accrue.Billing (read) | One-way read of local subscription state; resolver must not write or call processors. |
| check metadata -> telemetry/OTel | Decision metadata crosses into the telemetry pipeline (PII boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-06 | Elevation of Privilege | Fail-open on error/exception/nil (free user gets paid feature) | mitigate | Boolean-only API (D-07); `{:ok, true}` is the SOLE true path; `{:ok,false}`/`{:error,_}` -> false; `try/rescue/catch` collapses exceptions/throws/exits to false/[]/0 (D-08). Pinned by the property test in Plan 04 (D-10). |
| T-123-07 | Elevation of Privilege | Unmapped price_id silently allows | mitigate | Check-time unmapped -> false (`:deny` default) + `reason: :unmapped_plan` in :stop metadata (D-04/D-20); under `:raise`, the resolver raises and the context's try/rescue collapses to fail-closed. |
| T-123-07b | Elevation of Privilege | Multi-active-plan false negative — a single representative `:plan` makes `has_active_plan?(billable, other_plan)` return a WRONG false for a billable holding two active subs on different mapped plans (fail-closed-but-WRONG) | mitigate | Resolver carries `active_plans` (the SET of ALL active plan atoms); `has_active_plan?/2` tests `MapSet.member?` against that set, never the representative `:plan`. Pinned by the two-different-active-plans test in this plan (Task 2 read-path + Task 3 context) and the multi-active-plan property leg in Plan 04. Keeps `has_active_plan?/2` consistent with the UNION semantics of `entitled?/2`/`features_for/1`. |
| T-123-08 | Tampering | Recording per-check decisions floods/destroys the immutable audit ledger | mitigate | ZERO `Accrue.Events.record/1`/`record_multi/3` calls in the check path (D-21); ledger-boundary test counts `accrue_events` rows before/after and asserts unchanged. |
| T-123-09 | Denial of Service | Processor call on the gate path couples authz to Stripe availability | mitigate | Local-only resolution (D-09/D-13); never `Accrue.Processor.*` or the effectful `Billing.customer/1` — read-only Customer query + `Query.active/1` only (grep-asserted). |
| T-123-10 | Information Disclosure | PII in check telemetry metadata | mitigate | `subject_id` = internal UUID only (never email/name/external id); test asserts metadata excludes `:email`/`:name`; OTel `@prohibited_keys` + the strict allowlist (Plan 02) enforce the bound (D-18). |
| T-123-11 | Tampering | Re-deriving "active" from raw `.status` mishandles canceling/trialing/paused | mitigate | Reuse `Subscription.active?/1`/`Query.active/1` (never raw `.status`); grep-asserted absent of `s.status` in local_map.ex (Pitfall 2). |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages installed in this phase. No install task in this plan. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements_test.exs` — read-path + 4-fn + telemetry + ledger-boundary + multi-active-plan green (VALIDATION.md ENT-02/03/04/05 rows).
- `cd accrue && mix compile --warnings-as-errors` — clean.
- `! grep -rq "Accrue.Entitlements" accrue/lib/accrue/billing/ accrue/lib/accrue/billing.ex` — D-14 one-way dependency holds (final gate is in Plan 04, but this plan must not introduce a back-reference).
</verification>

<success_criteria>
- `entitled?/2`, `has_active_plan?/2`, `features_for/1`, `entitlement_quantity/2` resolve from local subscription state with zero processor calls and a fail-closed contract (ENT-02/03/04).
- `has_active_plan?/2` tests membership in the SET of ALL active plan atoms — a billable with two active subs on two different mapped plans answers true for BOTH (multi-active-plan correct, consistent with entitled?/features_for UNION semantics).
- Each check emits `[:accrue, :entitlements, :check, :start|:stop|:exception]` with D-18 metadata; unmapped-plan deny carries `reason: :unmapped_plan`; zero `accrue_events` rows written (ENT-05).
- `features_for/1` returns a plain sorted deduped `[atom]`, never a MapSet (D-06).
- No new external dependency added.
</success_criteria>

<output>
Create `.planning/phases/123-config-core-gate-api-foundation/123-03-SUMMARY.md` when done.
</output>

---
phase: 123-config-core-gate-api-foundation
plan: 04
type: execute
wave: 3
depends_on: ["123-03"]
files_modified:
  - accrue/lib/accrue.ex
  - accrue/test/property/entitlements_fail_closed_property_test.exs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ENT-02, ENT-03, ENT-04, ENT-05]
must_haves:
  truths:
    - "Accrue.has_active_plan?/2, entitled?/2, features_for/1, entitlement_quantity/2 are callable on the top-level Accrue module"
    - "The fail-closed contract holds under property test: only path to true is an affirmative resolved match; garbage/errors/nil/unmapped/raising-stub all -> false/[]/0"
    - "Accrue.has_active_plan?/2 returns true for BOTH plans when a billable holds two active subs on two different mapped plans (multi-active-plan, via the public delegate)"
    - "Nothing under lib/accrue/billing/** references Accrue.Entitlements.* (one-way dependency, D-14)"
    - "ROADMAP SC#5 and REQUIREMENTS ENT-05 use the plural [:accrue, :entitlements, :check] event name"
  artifacts:
    - path: "accrue/lib/accrue.ex"
      provides: "4 defdelegates to Accrue.Entitlements"
      contains: "defdelegate entitled?"
    - path: "accrue/test/property/entitlements_fail_closed_property_test.exs"
      provides: "D-10 dual property: never-true-on-garbage AND true-iff-affirmative-match + multi-active-plan affirmative leg"
      contains: "use ExUnitProperties"
  key_links:
    - from: "Accrue (top-level)"
      to: "Accrue.Entitlements"
      via: "defdelegate (4 fns)"
      pattern: "defdelegate"
    - from: "property test"
      to: "Accrue.entitled?/2 + entitlement_quantity/2 + features_for/1 + has_active_plan?/2"
      via: "StreamData garbage generators + raising-resolver stub + two-active-plan affirmative fixture"
      pattern: "check all"
---

<objective>
Wire the four gate functions onto the top-level `Accrue` module via `defdelegate`, write the load-bearing fail-closed property test (D-10), enforce the D-14 one-way-dependency gate, and reconcile the ROADMAP/REQUIREMENTS singular event name to the locked plural form (D-16). This wave makes the public API real and proves the fail-closed contract holds.

Purpose: ENT-02/03/04/05 public surface (the `Accrue.*` delegates the docs and downstream phases call) + the D-10 property test that is the phase's load-bearing correctness deliverable + the D-16 doc reconcile + the D-14 dependency invariant.
Output: 4 delegates on `accrue.ex`; `entitlements_fail_closed_property_test.exs`; ROADMAP SC#5 + REQUIREMENTS ENT-05 plural-event fix; the D-14 grep gate verified green; phase gate certified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-config-core-gate-api-foundation/123-CONTEXT.md
@.planning/phases/123-config-core-gate-api-foundation/123-PATTERNS.md
@.planning/phases/123-config-core-gate-api-foundation/123-VALIDATION.md
@.planning/phases/123-config-core-gate-api-foundation/123-03-SUMMARY.md

<interfaces>
<!-- The 4 delegates to add to accrue.ex (currently moduledoc-only, L1-16). -->
defdelegate has_active_plan?(billable, plan), to: Accrue.Entitlements
defdelegate entitled?(billable, feature), to: Accrue.Entitlements
defdelegate features_for(billable), to: Accrue.Entitlements
defdelegate entitlement_quantity(billable, quota_key), to: Accrue.Entitlements
<!-- Specs per house style: boolean(), boolean(), [atom()], non_neg_integer(). -->

<!-- has_active_plan?/2 tests membership in the resolver's active_plans SET (Plan 03) — multi-active-plan correct. -->

<!-- D-16 doc reconcile targets (singular -> plural): -->
<!--   ROADMAP.md Phase 123 SC#5: [:accrue, :entitlement, :check] -> [:accrue, :entitlements, :check] -->
<!--   REQUIREMENTS.md ENT-05:     [:accrue, :entitlement, :check] -> [:accrue, :entitlements, :check] -->

<!-- D-14 grep gate (must exit 0 / return empty):                                          -->
<!--   ! grep -rq "Accrue.Entitlements" accrue/lib/accrue/billing/ accrue/lib/accrue/billing.ex -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 4 defdelegates to Accrue and reconcile ROADMAP/REQUIREMENTS to plural event name</name>
  <files>accrue/lib/accrue.ex, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - accrue/lib/accrue.ex L1-16 (moduledoc-only today; delegates are net-new)
    - accrue/lib/accrue/auth.ex (delegate/spec/doc house style reference)
    - accrue/lib/accrue/entitlements.ex (Plan 03 — the delegate target; confirm the four signatures match)
    - 123-PATTERNS.md section "accrue.ex (MODIFY)" (L230-239)
    - 123-CONTEXT.md D-06 (the four signatures + specs), D-16 (plural event name + the ROADMAP/ENT-05 reconcile action)
    - .planning/ROADMAP.md Phase 123 Success Criteria #5 (the singular event name to fix), .planning/REQUIREMENTS.md ENT-05 (same singular to fix)
  </read_first>
  <action>
    Add the four defdelegates from the interfaces block to Accrue (accrue.ex), each with @doc and @spec per house style (has_active_plan?/2 returns boolean(), entitled?/2 returns boolean(), features_for/1 returns [atom()], entitlement_quantity/2 returns non_neg_integer()), targeting Accrue.Entitlements. Keep the existing moduledoc; optionally add a one-line mention of the gate API. Then reconcile D-16 with Edit (do NOT rewrite the files wholesale — change only the event token): in .planning/ROADMAP.md change Phase 123 Success Criterion #5 event name from the singular form to the plural [:accrue, :entitlements, :check]; in .planning/REQUIREMENTS.md change ENT-05's event name from the singular form to the plural [:accrue, :entitlements, :check]. Do not change any other content in those two doc files.
  </action>
  <verify>
    <automated>cd accrue && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for `defdelegate` of has_active_plan?/entitled?/features_for/entitlement_quantity in accrue/lib/accrue.ex shows all four delegating to Accrue.Entitlements.
    - grep -rn "accrue, :entitlements, :check" .planning/ROADMAP.md .planning/REQUIREMENTS.md shows the plural form present in BOTH files.
    - grep -rn "accrue, :entitlement, :check" .planning/ROADMAP.md .planning/REQUIREMENTS.md returns NO matches (singular fully replaced).
    - cd accrue and mix compile --warnings-as-errors succeeds.
  </acceptance_criteria>
  <done>The four delegates exist on Accrue; ROADMAP SC#5 and REQUIREMENTS ENT-05 carry the plural event name; clean compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the load-bearing fail-closed property test (D-10) + multi-active-plan affirmative leg, and verify the D-14 dependency gate</name>
  <files>accrue/test/property/entitlements_fail_closed_property_test.exs</files>
  <read_first>
    - accrue/test/property/connect_platform_fee_property_test.exs L1-44 (verified ExUnitProperties/StreamData idiom: use ExUnit.Case + use ExUnitProperties + property/check all)
    - accrue/test/accrue/storage/null_test.exs L6-19 (app-env restore — this test mutates :entitlements config so async: false)
    - accrue/lib/accrue/test/factory.ex L92-203 (active/trialing/canceled subscription factories with :price_id override, returns map of customer/subscription/items; NOTE: each call mints a fresh random owner_id, so two factory calls = two distinct customers)
    - accrue/lib/accrue/billing.ex L55-59 (subscribe/2,3 — fetch-or-create) + accrue/lib/accrue/billing/subscription_actions.ex L94-102 (subscribe(%Customer{}) reuses the customer) — the multi-active-plan leg adds the SECOND active sub via `Accrue.Billing.subscribe(customer, "price_p2")` on the SAME customer, NOT a second factory call
    - accrue/lib/accrue.ex (Task 1 — the public delegates the property test exercises)
    - accrue/lib/accrue/entitlements/resolver.ex (Plan 03 — __impl__/0; the raising-stub leg swaps :entitlements resolver to a module whose resolve/2 raises) + the active_plans SET contract (Plan 03 — what has_active_plan? tests)
    - 123-PATTERNS.md section "Test: property (StreamData)" (L325-339)
    - 123-RESEARCH.md section "Property Test Spec (D-10)" (incl. the multi-active-plan affirmative case); 123-VALIDATION.md section "Load-Bearing Property Test (D-10)" (L65-78)
    - 123-CONTEXT.md D-10 (the dual invariant), D-06/D-09 (multi-active-plan has_active_plan? semantics) and D-14 (the one-way dependency grep gate)
  </read_first>
  <behavior>
    - For ALL garbage/edge inputs (nil, arbitrary non-billable terms, billable with no customer, billable with a customer but no active sub, active sub whose price_id is unmapped, a raising-resolver stub): Accrue.entitled?(input, any_feature) == false, Accrue.entitlement_quantity(input, any_quota_key) == 0, Accrue.features_for(input) == [], Accrue.has_active_plan?(input, any_plan) == false. (never-true-on-garbage)
    - For a billable with an active subscription on a mapped plan whose feature set is F: Accrue.entitled?(billable, feat) == MapSet.member?(F, feat) for feat drawn from F plus one unmapped sentinel feature. (true-iff-affirmative-match)
    - **Multi-active-plan (load-bearing):** for ONE billable holding TWO active subscriptions on TWO different mapped plans :p1 (price "price_p1") and :p2 (price "price_p2") — both subs MUST share the one customer (built via one active_subscription/1 then a second Accrue.Billing.subscribe(customer, "price_p2"), NOT two factory calls): Accrue.has_active_plan?(billable, :p1) == true AND Accrue.has_active_plan?(billable, :p2) == true AND Accrue.has_active_plan?(billable, "price_p1") == true AND Accrue.has_active_plan?(billable, "price_p2") == true; Accrue.has_active_plan?(billable, :unmapped) == false. This proves the public delegate tests the active_plans SET, not a single representative plan (no fail-closed-but-wrong false negative).
    - No file under lib/accrue/billing/** or lib/accrue/billing.ex references Accrue.Entitlements.* (D-14).
  </behavior>
  <action>
    Create test/property/entitlements_fail_closed_property_test.exs with use ExUnit.Case, async: false plus use ExUnitProperties (async: false because it mutates :entitlements app env, unlike the pure-math connect property test). Restore :entitlements config via setup/on_exit (mirror null_test.exs). Garbage generator: StreamData.one_of([constant(nil), term(), integer(), string(:ascii), atom(:alphanumeric)]). Write the never-true-on-garbage property asserting Accrue.entitled?(input, :any) == false, Accrue.entitlement_quantity(input, :seats) == 0, Accrue.features_for(input) == [], and Accrue.has_active_plan?(input, :any) == false across the garbage generator (check all input, max_runs: 200). Add explicit cases for: billable with no customer; billable with a customer but only a canceled_subscription/1; billable with an active sub whose price_id is NOT in any plan's price_ids (unmapped); and the raising-resolver leg — configure :entitlements resolver to a tiny inline test module whose resolve/2 raises, then assert the four public fns still return false/0/[]/false (proves try/rescue/catch collapses, D-08). Write the affirmative-match property: set :entitlements with a known plan (feature set F) plus a factory-backed active_subscription with that price_id, and assert Accrue.entitled?(billable, feat) == MapSet.member?(F, feat) for feat drawn from F plus a sentinel unmapped feature. **Add the multi-active-plan affirmative leg (an explicit example test, not necessarily a generated property): set :entitlements with two mapped plans (:p1 -> price_ids ["price_p1"], :p2 -> price_ids ["price_p2"], each with its own feature set), create ONE billable/customer with TWO active subscriptions by first calling factory active_subscription/1 with :price_id "price_p1" (this creates the customer + first sub), then issuing a SECOND `{:ok, _} = Accrue.Billing.subscribe(result.customer, "price_p2")` on that SAME customer (subscribe/2 on the existing %Customer{} reuses it via Billing.customer/1 fetch-or-create — subscription_actions.ex L94-96). Do NOT make a second active_subscription/1 call: each factory call mints a fresh random owner_id, producing two DISTINCT customers, and forcing the same :owner_id would hit the accrue_customers_owner_type_owner_id_processor unique index and raise on the second insert — so a second factory call would NOT put two subs on one billable. Then assert Accrue.has_active_plan?(billable, :p1) and Accrue.has_active_plan?(billable, :p2) (both true), plus the price_id-string forms ("price_p1"/"price_p2"), plus refute Accrue.has_active_plan?(billable, :enterprise) for an unmapped plan; also assert Accrue.features_for(billable) is the sorted union of :p1's and :p2's features. This is the load-bearing proof that has_active_plan? tests the active_plans SET (Plan 03 contract), not a single representative.** Use Accrue.Test.Factory for fixtures.
  </action>
  <verify>
    <automated>cd accrue && mix test test/property/entitlements_fail_closed_property_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File accrue/test/property/entitlements_fail_closed_property_test.exs exists with use ExUnitProperties and at least one property block using check all.
    - Contains the never-true-on-garbage property using StreamData.one_of with nil/term/integer/string/atom asserting entitled? == false, entitlement_quantity == 0, features_for == [], has_active_plan? == false.
    - Contains the raising-resolver leg (an inline stub module whose resolve/2 raises, swapped in via :entitlements resolver config) proving the four fns stay fail-closed.
    - Contains the affirmative-match property asserting entitled?(billable, feat) == MapSet.member?(F, feat).
    - Contains the multi-active-plan affirmative leg: ONE billable with two active subs on two different mapped plans (:p1/"price_p1" and :p2/"price_p2"), built via one `active_subscription/1` ("price_p1") + a second `Accrue.Billing.subscribe(result.customer, "price_p2")` on the SAME customer (NOT two factory calls), asserting `assert Accrue.has_active_plan?(billable, :p1)` AND `assert Accrue.has_active_plan?(billable, :p2)` AND the price_id-string forms, AND `refute Accrue.has_active_plan?(billable, :enterprise)` (unmapped), AND `features_for/1` == the sorted union of both plans' features.
    - cd accrue and mix test test/property/entitlements_fail_closed_property_test.exs is GREEN (maps to VALIDATION.md ENT-03 property rows + the ENT-02 multi-active-plan row — the load-bearing test).
    - The D-14 gate command `! grep -rq "Accrue.Entitlements" accrue/lib/accrue/billing/ accrue/lib/accrue/billing.ex` exits 0 (returns no matches).
  </acceptance_criteria>
  <done>The dual fail-closed property (never-true-on-garbage AND true-iff-affirmative-match) plus the multi-active-plan true-for-both leg are green; the D-14 one-way-dependency grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite, dependency invariant, and quality checks</name>
  <files>accrue/test/property/entitlements_fail_closed_property_test.exs</files>
  <read_first>
    - 123-VALIDATION.md section "Sampling Rate" (L30-36, the phase-gate command set) and "Per-Requirement Verification Map" (L44-59, all rows)
    - 123-CONTEXT.md D-14 (grep gate), D-16/D-21 (telemetry/ledger contracts already implemented in Plan 03)
    - CLAUDE.md (Elixir conventions; mix credo --strict, mix dialyzer posture)
  </read_first>
  <action>
    Run the full phase verification gate and confirm every VALIDATION.md row is green. This is a verification/gate task — no new production code unless a gate surfaces a defect (if a check fails, fix the minimal cause in the owning file from Plans 01–03 and re-run; record the fix in the SUMMARY). Run, in order: (1) the full entitlement test subset (config_entitlements_test.exs, entitlements_test.exs, entitlements/local_map_test.exs, property/entitlements_fail_closed_property_test.exs, telemetry/otel_test.exs); (2) the D-14 grep gate; (3) mix credo --strict; (4) mix dialyzer (PLT may build on first run — allow time); (5) full mix test. Confirm: all four Accrue.* delegates callable; has_active_plan? answers true for both plans of a multi-active-plan billable; the plural [:accrue, :entitlements, :check] events emit; OTel allowlist retains the 6 keys; zero accrue_events rows during checks; ROADMAP/REQUIREMENTS plural reconcile present. If all green, make no edits — this task certifies the phase.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements_test.exs test/accrue/entitlements/ test/property/entitlements_fail_closed_property_test.exs test/accrue/telemetry/otel_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The phase test subset (config + entitlements + local_map + property + otel) is fully GREEN.
    - The D-14 gate command `! grep -rq "Accrue.Entitlements" accrue/lib/accrue/billing/ accrue/lib/accrue/billing.ex` exits 0.
    - mix credo --strict reports no new issues introduced by this phase.
    - Full `cd accrue && mix test` is green before sign-off.
    - All VALIDATION.md Per-Requirement-Verification-Map rows (incl. the multi-active-plan ENT-02 row) have a passing automated command.
  </acceptance_criteria>
  <done>Full phase gate green: all entitlement tests pass, D-14 holds, credo/dialyzer clean, every VALIDATION.md row certified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host code -> Accrue.* gate delegates | Untrusted billable/feature args cross into the public authorization API. |
| Accrue.Entitlements.* -> Accrue.Billing.* | One-way read dependency; the reverse must never exist (D-14). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-12 | Elevation of Privilege | Fail-open under any garbage/error/exception input via the public Accrue.* delegates | mitigate | The D-10 property test exhaustively asserts never-true-on-garbage (nil, non-billable, no-customer, no-active-sub, unmapped, raising-stub) AND true-iff-affirmative-match across the public API — the load-bearing proof of the fail-closed contract (D-08/D-10). |
| T-123-12b | Elevation of Privilege | Multi-active-plan false negative leaking through the public delegate (has_active_plan? wrongly false for a real second active plan) | mitigate | The multi-active-plan affirmative leg in the property test asserts Accrue.has_active_plan? is true for BOTH plans of a billable with two active subs on different mapped plans — proving the public delegate tests the active_plans SET (Plan 03 contract), not a single representative (D-06/D-09). |
| T-123-13 | Tampering | A future edit introduces a billing -> entitlements back-reference creating a cycle / coupling the billing core to the gate | mitigate | The D-14 grep gate runs in the phase gate and fails the build if any file under lib/accrue/billing/** references Accrue.Entitlements.* — keeps the dependency acyclic and one-way. |
| T-123-14 | Repudiation | Doc/spec drift (singular vs plural event name) misleads operators wiring telemetry | mitigate | D-16 reconcile fixes ROADMAP SC#5 + REQUIREMENTS ENT-05 to the plural [:accrue, :entitlements, :check] matching the emitted events (asserted by grep). |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages installed in this phase. No install task in this plan. |
</threat_model>

<verification>
- cd accrue && mix test test/property/entitlements_fail_closed_property_test.exs — the load-bearing D-10 property + multi-active-plan leg green (VALIDATION.md ENT-03 property rows + ENT-02 multi-active-plan row).
- The D-14 gate `! grep -rq "Accrue.Entitlements" accrue/lib/accrue/billing/ accrue/lib/accrue/billing.ex` exits 0.
- grep -rn "accrue, :entitlements, :check" .planning/ROADMAP.md .planning/REQUIREMENTS.md shows plural in both; singular form absent.
- Full cd accrue && mix test green; mix credo --strict clean.
</verification>

<success_criteria>
- The four gate functions are callable on the top-level Accrue module (ENT-02/03/04).
- The fail-closed contract holds under the D-10 property test: only path to true is an affirmative resolved match; all garbage/error/nil/unmapped/raising-stub inputs resolve to false/[]/0 (ENT-03).
- has_active_plan?/2 returns true for BOTH plans of a billable with two active subs on two different mapped plans, via the public delegate (multi-active-plan correct, ENT-02).
- Nothing under lib/accrue/billing/** references Accrue.Entitlements.* (D-14).
- ROADMAP SC#5 and REQUIREMENTS ENT-05 carry the plural [:accrue, :entitlements, :check] event name (D-16, reconciles ENT-05).
- No new external dependency added.
</success_criteria>

<output>
Create `.planning/phases/123-config-core-gate-api-foundation/123-04-SUMMARY.md` when done.
</output>

---
phase: 124-enforcement-surfaces-plug-liveview-guards
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/research/PITFALLS.md
  - accrue/lib/accrue/oban/middleware.ex
  - accrue/mix.exs
autonomous: true
requirements: [ENT-07]

must_haves:
  truths:
    - "Every project doc that claimed core accrue is 'LiveView-FREE' / 'no LiveView present' / 'phoenix_live_view absent-or-optional in core' is reconciled to the factually-correct 'LiveView-runtime-free' posture"
    - "ROADMAP SC#3 reflects the merge-blocking static check (no always-compiled core module references the LiveView socket runtime), not the infeasible 'compiles with no LiveView present'"
    - "The oban/middleware.ex moduledoc no longer claims LiveView is never a core dep"
  artifacts:
    - path: "CLAUDE.md"
      provides: "reconciled LiveView-runtime-free wording"
      contains: "runtime-free"
    - path: ".planning/ROADMAP.md"
      provides: "SC#3 + LiveView-free note reconciled to the static-gate framing"
      contains: "socket runtime"
    - path: ".planning/research/PITFALLS.md"
      provides: "Pitfall #8 corrected from guard-in-admin/LV-absent to cond-compile-in-core"
    - path: "accrue/lib/accrue/oban/middleware.ex"
      provides: "corrected moduledoc LiveView boundary comment"
    - path: "accrue/mix.exs"
      provides: "clarified phoenix_live_view non-optional comment (Phoenix.Component spine + cond-compiled guard, no socket runtime)"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 124 SC#3"
      via: "static merge-gate framing replaces 'no LiveView present'"
      pattern: "socket runtime"
---

<objective>
Execute the D-06 lockstep doc/wording reconciliation: correct every project artifact that asserts the (now factually-false) "core `accrue` is LiveView-FREE / compiles with no LiveView present / `phoenix_live_view` is absent-or-optional in core" claim to the accurate "core stays LiveView-runtime-free" posture. This is a doc-only change in the same PR as the code (mirrors Phase 123's plural-telemetry ROADMAP/ENT-05 reconcile, D-16).

Purpose: `phoenix_live_view` has been a hard, non-optional core dependency since the email/invoice render spine landed (16+ core modules `use Phoenix.Component`, which ships only in `phoenix_live_view`). The literal "LiveView-free" claim was always false; this phase makes the docs honest and re-frames the real invariant as "no always-compiled core module references the LiveView socket runtime," enforced by the Plan 06 static gate.
Output: reconciled CLAUDE.md, ROADMAP.md, PITFALLS.md, oban/middleware.ex moduledoc, and the mix.exs dep comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-CONTEXT.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-RESEARCH.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-PATTERNS.md

<notes>
<!-- Pre-verified facts the executor relies on (planner checked the live tree 2026-05-23): -->
- REQUIREMENTS.md ENT-07 (line 27) ALREADY reads "...stays runtime-LiveView-free..." so NO edit is needed there. Diff before touching; only fix what still says "no LiveView present" / "LiveView-free".
- accrue/mix.exs line 80 declares the dep NON-optional (correct, D-02 keep it). The comment lines 78-79 already mention Phoenix.Component but say nothing about the socket-runtime invariant or the cond-compiled guard.
- accrue/mix.exs application/0 extra_applications is [:logger] only (no :phoenix_live_view) so the runtime-free claim is true at the OTP level.
- The merge gate (Plan 06) allowlists doc comments, so editing the oban/middleware.ex comment is harmless to it.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile CLAUDE.md + ROADMAP.md + mix.exs comment to LiveView-runtime-free</name>
  <files>CLAUDE.md, .planning/ROADMAP.md, accrue/mix.exs</files>
  <read_first>
    - CLAUDE.md line 16 ("`phoenix_live_view` (hard dep in `accrue_admin`, not in core)") and line 53 ("Admin UI is the only place LiveView appears core `accrue` stays LiveView-free."). These are the two stale claims.
    - .planning/ROADMAP.md line 59 (SC#3: "compiles and loads with no LiveView present (no required LiveView in core)") and line 125 (the "LiveView-free constraint" note). Lines 13/23/52 already say "runtime-LiveView-free" so leave those.
    - accrue/mix.exs lines 77-80 (the {:phoenix, optional} + {:phoenix_live_view, "~> 1.1"} block and the two comment lines above it).
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-CONTEXT.md D-01, D-02, D-06 (the authoritative target wording).
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-PATTERNS.md §"Doc reconciliation (D-06 lockstep)" (lines 454-470).
  </read_first>
  <action>
    CLAUDE.md: change line 16 from "`phoenix_live_view` (hard dep in `accrue_admin`, not in core)" to state phoenix_live_view is a REQUIRED core dep (provides Phoenix.Component/~H for the email + invoice render spine and the cond-compiled entitlement guard) while core stays LiveView-runtime-free (no socket runtime; never in extra_applications). Change line 53's "core `accrue` stays LiveView-free" to "core `accrue` stays LiveView-runtime-free (uses Phoenix.Component for email/invoice rendering and a cond-compiled on_mount guard; no socket runtime; never in extra_applications)."
    ROADMAP.md: change SC#3 (line 59) from "...compiles and loads with no LiveView present (no required LiveView in core)." to "...and a merge-blocking CI check proves no always-compiled core module references the LiveView socket runtime (Phoenix.LiveView / on_mount / Socket)." Update the line-125 "LiveView-free constraint" note: re-frame "compiles absent LiveView" to "no always-compiled core module references the LiveView socket runtime; phoenix_live_view is a required core dep for Phoenix.Component," and drop the now-resolved "verify the live accrue/mix.exs posture at Phase 124 planning time" clause (it was verified non-optional, correct).
    mix.exs: extend the comment above {:phoenix_live_view, "~> 1.1"} (lines 78-79) to state it is a REQUIRED (non-optional) core dep providing Phoenix.Component/~H for the email + invoice render spine AND the cond-compiled Accrue.Live.Entitlements guard; core uses NO LiveView socket runtime (no Phoenix.LiveView/on_mount/Socket in always-compiled code) and phoenix_live_view never appears in extra_applications. Keep the dep line itself NON-optional and unchanged (D-02).
  </action>
  <verify>
    <automated>grep -c 'no LiveView present' .planning/ROADMAP.md; grep -c 'stays LiveView-free' CLAUDE.md; grep -c 'runtime-free' CLAUDE.md .planning/ROADMAP.md; grep -c 'socket runtime' .planning/ROADMAP.md accrue/mix.exs; cd accrue && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'no LiveView present' .planning/ROADMAP.md` returns 0.
    - `grep -c 'stays LiveView-free' CLAUDE.md` returns 0.
    - CLAUDE.md contains "runtime-free" (the reconciled wording on both line 16 and line 53 areas).
    - `.planning/ROADMAP.md` SC#3 and the line-125 note contain "socket runtime".
    - `accrue/mix.exs` comment above the phoenix_live_view dep contains "socket runtime" and "extra_applications"; the dep line is still `{:phoenix_live_view, "~> 1.1"}` (NON-optional, no `optional: true`).
    - `cd accrue && mix compile --warnings-as-errors` exits 0 (the mix.exs comment edit did not break the dep block).
  </acceptance_criteria>
  <done>CLAUDE.md, ROADMAP.md, and the mix.exs comment all state the LiveView-runtime-free posture; no stale "no LiveView present"/"stays LiveView-free" string remains in those files.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile PITFALLS.md Pitfall #8 + the oban/middleware.ex moduledoc comment</name>
  <files>.planning/research/PITFALLS.md, accrue/lib/accrue/oban/middleware.ex</files>
  <read_first>
    - .planning/research/PITFALLS.md Pitfall #8 (the "LiveView leaking into core" bullet ~line 208, the "Ship the on_mount guard from a LiveView-aware location, not accrue core" recommendation ~line 214, the warning-sign "phoenix_live_view shows up in accrue/mix.exs deps (non-optional)" ~line 222, and the summary-table rows ~268/282/333/349 that say "guard lives in admin / core stays LV-free / LV added to core = problem").
    - accrue/lib/accrue/oban/middleware.ex lines 19-24 — the "## LiveView integration" moduledoc comment ("LiveView is a hard dependency of `accrue_admin` only, never `accrue`.").
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-RESEARCH.md Pitfall 5 §"EXTRA FINDING" (lines 326-330) flagging the oban/middleware.ex hit as a D-06 reconciliation target NOT in CONTEXT's enumerated list.
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-CONTEXT.md D-03/D-04/D-06 (guard lives in CORE, cond-compiled).
  </read_first>
  <action>
    PITFALLS.md Pitfall #8: correct the stale stance to match the reconciled posture (D-03/D-04/D-06). Specifically: change the "LiveView leaking into core" framing and the "Ship the on_mount guard from a LiveView-aware location, not accrue core / belongs in accrue_admin or a thin opt-in module never a hard core dependency" recommendation to: the guard lives in CORE `accrue` at `lib/accrue/live/entitlements.ex`, conditionally compiled via the Sigra 4-pattern; `phoenix_live_view` is already a required core dep (for Phoenix.Component); the real invariant is "no always-compiled core module references the LiveView socket runtime," enforced by a merge-blocking static gate. Fix the warning-sign bullet ("phoenix_live_view shows up in accrue/mix.exs deps (non-optional)") and the summary-table rows (~268/282/333/349) so they no longer treat a non-optional core phoenix_live_view as a violation keep the genuine pitfalls (ordering-vs-auth, redirect loop, gating-after-expensive-mount, resolve-once, fail-closed-on-missing-current_user) intact, since those remain valid. Keep the "provide the controller-level Plug guard for non-LiveView routes" guidance (still correct).
    oban/middleware.ex: rewrite the "## LiveView integration" moduledoc text (lines ~19-24) from "LiveView is a hard dependency of `accrue_admin` only, never `accrue`." to "LiveView's socket runtime is never coupled in core; `phoenix_live_view` is a required core dep for `Phoenix.Component` (email/invoice render spine + the cond-compiled `Accrue.Live.Entitlements` on_mount guard). The operation-id on_mount equivalent for the admin socket lives in `accrue_admin`." Keep it a doc comment (the merge gate allowlists comments).
  </action>
  <verify>
    <automated>grep -c 'never `accrue`\|never accrue' accrue/lib/accrue/oban/middleware.ex; grep -c 'socket runtime\|required core dep' accrue/lib/accrue/oban/middleware.ex; cd accrue && mix compile --warnings-as-errors 2>&1 | tail -3 && cd .. && bash -c 'grep -nE "^[^#]*((import|alias)[[:space:]]+Phoenix\.LiveView|Phoenix\.LiveView\.Socket|Phoenix\.Socket|def[[:space:]]+on_mount)" accrue/lib/accrue/oban/middleware.ex || echo "GATE-CLEAN: oban/middleware.ex has no real LiveView ref"'</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/oban/middleware.ex` no longer contains "never `accrue`"/"never accrue" and DOES contain "socket runtime" and "required core dep".
    - The oban/middleware.ex edit stays a doc comment: the gate regex `grep -nE '^[^#]*((import|alias)[[:space:]]+Phoenix\.LiveView|Phoenix\.LiveView\.Socket|Phoenix\.Socket|def[[:space:]]+on_mount)' accrue/lib/accrue/oban/middleware.ex` returns nothing (still allowlisted as a comment, so it never trips the Plan 06 gate).
    - PITFALLS.md Pitfall #8 no longer recommends shipping the guard from `accrue_admin`/an opt-in module instead of core; it states the guard lives in cond-compiled core and frames the invariant as "no always-compiled core module references the LiveView socket runtime."
    - `cd accrue && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>PITFALLS.md Pitfall #8 and the oban/middleware.ex moduledoc reflect the cond-compile-in-core / LiveView-runtime-free posture; the oban comment remains gate-clean (a doc comment).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project docs → future contributors | Stale "LiveView-free" docs could pressure a future contributor to (re)move the hard dep or mis-place the guard, regressing the email/PDF spine or the gate; honest docs prevent that. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-15 | Repudiation / Tampering | doc/code drift letting a future change silently violate the real (socket-runtime-free) invariant under cover of the false "LiveView-free" claim | mitigate | Reconcile all stale docs to the accurate posture in the same PR as the code; the Plan 06 static merge gate is the enforced backstop, and these docs now point to it. Verified by source-assertion greps (no manual review). |
| T-124-16 | Information Disclosure | n/a (doc-only change; no runtime behavior, no data, no wire surface) | accept | Doc-only edits; no executable behavior changes; no secrets/PII touched (RESEARCH §"Runtime State Inventory" confirms no runtime state). |
</threat_model>

<verification>
- `grep -c 'no LiveView present' .planning/ROADMAP.md` returns 0; `grep -c 'stays LiveView-free' CLAUDE.md` returns 0.
- `accrue/lib/accrue/oban/middleware.ex` contains "socket runtime" and no "never accrue".
- `cd accrue && mix compile --warnings-as-errors` exits 0.
- The gate regex over `accrue/lib/accrue/oban/middleware.ex` returns nothing (still a doc comment).
</verification>

<success_criteria>
- All stale "LiveView-FREE / no LiveView present / phoenix_live_view absent-or-optional in core" claims across CLAUDE.md, ROADMAP.md, PITFALLS.md, oban/middleware.ex, and the mix.exs comment are reconciled to the accurate "LiveView-runtime-free / no socket-runtime coupling" posture (D-06). (Supports SC#3.)
</success_criteria>

<output>
Create `.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-05-SUMMARY.md` when done.
</output>

---
phase: 124-enforcement-surfaces-plug-liveview-guards
plan: 06
type: execute
wave: 4
depends_on: ["124-02", "124-03", "124-04"]
files_modified:
  - scripts/ci/verify_core_liveview_runtime_free.sh
  - .github/workflows/ci.yml
  - accrue/test/property/guard_fail_closed_property_test.exs
autonomous: true
requirements: [ENT-06, ENT-07]

must_haves:
  truths:
    - "A merge-blocking CI gate fails the build if any always-compiled core module references the LiveView socket runtime (Phoenix.LiveView / on_mount / Socket), with doc comments and lib/accrue/live/ allowlisted"
    - "Both enforcement surfaces fail closed under property test: nil billable, raising billable fn, and resolution exceptions all DENY, never ALLOW"
  artifacts:
    - path: "scripts/ci/verify_core_liveview_runtime_free.sh"
      provides: "static grep merge-gate (repo-root scripts/ci/, scans accrue/lib)"
      contains: "verify_core_liveview_runtime_free"
    - path: ".github/workflows/ci.yml"
      provides: "merge-blocking wiring of the gate in docs-contracts-shift-left job"
      contains: "verify_core_liveview_runtime_free.sh"
    - path: "accrue/test/property/guard_fail_closed_property_test.exs"
      provides: "cross-surface fail-closed property (SC#4)"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_core_liveview_runtime_free.sh"
      via: "merge-blocking run step in docs-contracts-shift-left"
      pattern: "verify_core_liveview_runtime_free"
    - from: "accrue/test/property/guard_fail_closed_property_test.exs"
      to: "Accrue.Entitlements.Guard.check/3"
      via: "fail-closed property over garbage/raising inputs"
      pattern: "Accrue\\.Entitlements\\.Guard"
---

<objective>
Lock the phase's two hardest invariants behind automated, merge-blocking enforcement: (1) the static gate (D-05) that fails the build if any always-compiled core module references the LiveView socket runtime, wired into CI as a merge-blocking step; and (2) the cross-surface fail-closed property test (SC#4) proving a nil billable, a raising billable fn, and resolution exceptions all DENY through the shared Guard.

Purpose: These are the load-bearing security guarantees of an authorization-enforcement phase. The static gate makes "LiveView-runtime-free" a verifiable, non-regressing invariant; the property test makes "fail-closed by construction" the proven, easy path for both surfaces. They run last so the gate validates against the real `live/entitlements.ex` (the `/accrue/live/` exclusion must be exercised against a module that now contains `def on_mount`).
Output: `scripts/ci/verify_core_liveview_runtime_free.sh`, the ci.yml wiring, and the fail-closed property test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-CONTEXT.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-RESEARCH.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-PATTERNS.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-02-SUMMARY.md
@.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-04-SUMMARY.md

<interfaces>
<!-- Path correction (load-bearing, PATTERNS.md): the CI gate goes at REPO-ROOT scripts/ci/, NOT accrue/scripts/ci/ (which does not exist). Scripts run from .github/workflows/ci.yml as `bash scripts/ci/<name>.sh` with cwd = repo root. The script's lib var must point at ${repo_root}/accrue/lib. -->

From scripts/ci/verify_processor_support_matrix.sh (the merge-gate model to clone, lines 1-6):
```
#!/usr/bin/env bash
set -euo pipefail
repo_root="${ROOT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"
# ... fail-to-stderr + exit 1; echo "...: OK" on pass ...
```

From .github/workflows/ci.yml (the docs-contracts-shift-left job, the step shape to mirror, lines 46-47):
```
- name: Processor support matrix contract
  run: bash scripts/ci/verify_processor_support_matrix.sh
```

From accrue/lib/accrue/entitlements/guard.ex (Plan 02) — the engine the property test drives:
```
Accrue.Entitlements.Guard.check(:plug | :live, container, opts) :: {:allow, container} | {:deny, deny_form, ctx}
```
</interfaces>

<gate_recipe>
<!-- Planner pre-validated this regex against the live tree 2026-05-23: the ONLY hit in accrue/lib is the
oban/middleware.ex doc comment (allowlisted by the ^[^#]* anchor); excluding /accrue/live/ keeps the new
def on_mount in live/entitlements.ex from tripping it. -->
Match REAL refs only (not doc comments / strings):
  (import|alias) Phoenix.LiveView | Phoenix.LiveView.Socket | Phoenix.Socket | def on_mount
Scan: ${repo_root}/accrue/lib --include='*.ex'
Exclude: lines beginning (after optional whitespace) with # (doc comments); paths containing /accrue/live/ (the cond-compiled guard)
Fail (exit 1) with the offending lines to stderr if any hit; else echo "verify_core_liveview_runtime_free: OK".
</gate_recipe>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify_core_liveview_runtime_free.sh static merge gate (repo-root scripts/ci/)</name>
  <files>scripts/ci/verify_core_liveview_runtime_free.sh</files>
  <read_first>
    - scripts/ci/verify_processor_support_matrix.sh — the header (`#!/usr/bin/env bash`, `set -euo pipefail`), the `repo_root="${ROOT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"` resolution, and the fail-to-stderr + `exit 1` + final `echo "...: OK"` shape. Clone this structure.
    - scripts/ci/compile_matrix.sh — the without-X compile-cell model (this is the REJECTED approach per D-05; do NOT use it — confirm the gate is a static grep, not a compile matrix cell).
    - accrue/lib/accrue/live/entitlements.ex (Plan 04) — the legitimately-LiveView module the gate must EXCLUDE via the `/accrue/live/` path filter. Confirm it now contains `def on_mount` so you validate the exclusion works.
    - accrue/lib/accrue/oban/middleware.ex — the doc-comment LiveView mention the gate must ALLOWLIST (the `^[^#]*` anchor handles it).
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-RESEARCH.md §"Static merge gate" (lines 393-428) for the exact script body and the verified-clean baseline note.
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-PATTERNS.md the verify-script assignment (lines 325-345) and the LOAD-BEARING path correction at the top (lines 7-15).
  </read_first>
  <action>
    Create `scripts/ci/verify_core_liveview_runtime_free.sh` (repo-root `scripts/ci/`, NOT under `accrue/`). Make it executable (`chmod +x`). Header: `#!/usr/bin/env bash` + `set -euo pipefail` + `repo_root="${ROOT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"` (resolves to repo root from `scripts/ci/`) + `lib="${repo_root}/accrue/lib"`. Run `grep -rnE '^[^#]*((import|alias)[[:space:]]+Phoenix\.LiveView|Phoenix\.LiveView\.Socket|Phoenix\.Socket|def[[:space:]]+on_mount)' "${lib}" --include='*.ex' | grep -v '/accrue/live/' || true`, capture into `hits`. If `hits` is non-empty: echo a FAIL message + the offending lines to stderr and `exit 1`; else `echo "verify_core_liveview_runtime_free: OK"`. Add a header comment documenting D-05, the doc-comment allowlist (`^[^#]*` anchor), and the `/accrue/live/` exclusion (the cond-compiled guard). The gate must pass on the current tree (the only baseline hit, oban/middleware.ex doc comment, is allowlisted; the new live/entitlements.ex is excluded by path).
  </action>
  <verify>
    <automated>chmod +x scripts/ci/verify_core_liveview_runtime_free.sh; bash scripts/ci/verify_core_liveview_runtime_free.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/verify_core_liveview_runtime_free.sh` exists at REPO ROOT (not under `accrue/`), is executable, and contains `verify_core_liveview_runtime_free` and `repo_root`.
    - `bash scripts/ci/verify_core_liveview_runtime_free.sh` exits 0 on the current tree and prints `verify_core_liveview_runtime_free: OK`.
    - Negative proof: temporarily adding `import Phoenix.LiveView` to a non-`/accrue/live/` core file (e.g. a scratch line in `accrue/lib/accrue.ex`) makes the gate `exit 1` with the offending line on stderr; revert the scratch line after confirming.
    - The gate does NOT flag `accrue/lib/accrue/oban/middleware.ex` (doc comment, allowlisted) nor `accrue/lib/accrue/live/entitlements.ex` (path-excluded) nor `accrue/lib/accrue/entitlements/guard.ex` (no LiveView refs).
  </acceptance_criteria>
  <done>The static gate passes clean on the current tree, fails on a real socket-runtime reference outside `/accrue/live/`, and correctly allowlists doc comments and the cond-compiled guard.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the gate into ci.yml as a merge-blocking step</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the `docs-contracts-shift-left` job (lines 30-66): its `runs-on: ubuntu-24.04`, the `actions/checkout@v6` step, and the existing `- name: Processor support matrix contract / run: bash scripts/ci/verify_processor_support_matrix.sh` step (lines 46-47) to mirror. Confirm this job is merge-blocking (line 6 lists it; the required-checks config gates merge).
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-RESEARCH.md §"Wire into ci.yml" (lines 421-425).
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-PATTERNS.md the ci.yml assignment (lines 349-359).
  </read_first>
  <action>
    EDIT `.github/workflows/ci.yml`: add a new step inside the `docs-contracts-shift-left` job (alongside the other `verify_*` steps, e.g. after the "Processor support matrix contract" step): `- name: Core stays LiveView-runtime-free (ENT-07 D-05)` / `run: bash scripts/ci/verify_core_liveview_runtime_free.sh`. cwd is repo root (bare `scripts/ci/...` path, matching the sibling steps). Do NOT create a new job and do NOT add a compile-matrix cell (D-05 rejects that). Confirm the step lands in the already-merge-blocking job.
  </action>
  <verify>
    <automated>grep -n 'verify_core_liveview_runtime_free.sh' .github/workflows/ci.yml; python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('ci.yml valid YAML')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a step `run: bash scripts/ci/verify_core_liveview_runtime_free.sh` inside the `docs-contracts-shift-left` job.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` succeeds (valid YAML).
    - The new step is in the same job as `verify_processor_support_matrix.sh` (merge-blocking), not a separate non-blocking job.
  </acceptance_criteria>
  <done>The static gate runs as a merge-blocking CI step in the docs-contracts-shift-left job; the workflow file is valid YAML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: guard_fail_closed_property_test.exs — cross-surface fail-closed property (SC#4)</name>
  <files>accrue/test/property/guard_fail_closed_property_test.exs</files>
  <read_first>
    - accrue/test/property/entitlements_fail_closed_property_test.exs — clone near-verbatim: the `garbage_gen/0` generator (lines 87-95), the `RaisingResolver` stub (47-51), the `setup` env-save/restore with `on_exit` (67-80, `async: false`), `use Accrue.BillingCase, async: false` + `use ExUnitProperties` (30-31), `alias Accrue.Test.Factory`, and the `assert_fail_closed/1` helper (98-103).
    - accrue/lib/accrue/entitlements/guard.ex (Plan 02) — `check/3` returns `{:allow, _} | {:deny, _, _}`; the property asserts `:deny` for all garbage/raising inputs.
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-RESEARCH.md §"SC→Test Map" SC#4 fail-closed row (line 488), §"Observable signal" SC#4 (line 495).
    - .planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-PATTERNS.md the property-test assignment (lines 365-373).
  </read_first>
  <behavior>
    - For ALL garbage inputs (nil, arbitrary terms, integers, strings, atoms) used as the resolved billable, `Guard.check(:plug, conn_with_that_billable, feature: :x)` returns `{:deny, _, _}` and never `{:allow, _}` (both `:plug` and `:live` surfaces).
    - A `billable:` fn that RAISES → `{:deny, _, _}` (fail-closed; the raise collapses to nil).
    - A billable with no active/mapped subscription → `{:deny, _, _}`.
    - The ONLY `{:allow, _}` path is a billable with an active subscription on a mapped plan whose feature set contains the gated feature (the affirmative-match leg).
  </behavior>
  <action>
    Create `Accrue.Property.GuardFailClosedPropertyTest` by cloning `entitlements_fail_closed_property_test.exs`. Adapt `assert_fail_closed/1` to drive through the GUARD on BOTH surfaces: for a garbage `input`, build a `:plug` container (a `%Plug.Conn{}` via `Plug.Test.conn/2` carrying the input as the resolved billable through a `billable:` fn returning `input`) and a `:live` container (a bare `%{assigns: %{...}}` map or stub socket), and assert `match?({:deny, _, _}, Accrue.Entitlements.Guard.check(:plug, plug_container, feature: :any))` and the `:live` equivalent. Add a `check all input <- garbage_gen()` property running both surfaces. Add the raising-`billable:`-fn leg (a fn that raises → `{:deny, _, _}`) and the affirmative-match leg (an active mapped subscription via Factory → `{:allow, _}`). `async: false` (mutates `:accrue, :entitlements`).
  </action>
  <verify>
    <automated>cd accrue && mix test test/property/guard_fail_closed_property_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd accrue && mix test test/property/guard_fail_closed_property_test.exs` exits 0.
    - The property asserts `{:deny, _, _}` for all garbage inputs across BOTH `:plug` and `:live` surfaces (file contains `Accrue.Entitlements.Guard.check(:plug` and `Accrue.Entitlements.Guard.check(:live`).
    - The file includes the raising-`billable:`-fn fail-closed leg and the affirmative-match `{:allow, _}` leg (proving `:allow` is reachable only via an affirmative resolved match).
    - `use ... async: false` is present (env mutation).
  </acceptance_criteria>
  <done>The cross-surface fail-closed property proves nil/garbage/raising inputs deny on both surfaces and that allow is reachable only via an affirmative resolved match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor PR → main | An accidental socket-runtime coupling in always-compiled core must be blocked at merge, not caught post-merge. |
| guard engine → both surfaces | Any input shape that resolves a billable must default to deny; allow is only the affirmative-match path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-17 | Tampering | merge-gate evasion / advisory-only gate | mitigate | The gate is a merge-BLOCKING step in the already-required `docs-contracts-shift-left` job (D-05); negative-proof test in Task 1 confirms it fails on a real reference. Not advisory. |
| T-124-18 | Elevation of Privilege | fail-OPEN slipping through on an untested input shape | mitigate | The property test exhausts garbage/nil/raising inputs across BOTH surfaces and asserts `{:deny, _, _}`; the affirmative-match leg pins `:allow` to a single real path (SC#4). |
| T-124-19 | Information Disclosure | gate output leaking source structure | accept | The gate prints only offending file:line:source to CI stderr (no secrets/PII; source is already public). |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (all deps pre-locked); no Package Legitimacy Gate applies (RESEARCH §"Package Legitimacy Audit"). |
</threat_model>

<verification>
- `bash scripts/ci/verify_core_liveview_runtime_free.sh` exits 0 on the current tree; exits 1 on a planted real reference outside `/accrue/live/`.
- `.github/workflows/ci.yml` is valid YAML and contains the merge-blocking gate step.
- `cd accrue && mix test test/property/guard_fail_closed_property_test.exs` green.
- Full phase regression: `cd accrue && mix test` green + `bash scripts/ci/verify_core_liveview_runtime_free.sh` OK.
</verification>

<success_criteria>
- A merge-blocking CI gate enforces "no always-compiled core module references the LiveView socket runtime" (SC#3); the cross-surface fail-closed property proves a guard whose check cannot resolve denies rather than allows (SC#4).
</success_criteria>

<output>
Create `.planning/phases/124-enforcement-surfaces-plug-liveview-guards/124-06-SUMMARY.md` when done.
</output>

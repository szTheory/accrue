---
phase: 125-provider-honesty-lifecycle-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor/capabilities.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/processor/braintree.ex
  - .planning/processor-support-matrix.md
  - scripts/ci/verify_processor_support_matrix.sh
  - accrue/test/accrue/entitlements/provider_honesty_test.exs
autonomous: true
requirements: [ENT-08]
must_haves:
  truths:
    - "Processor.Capabilities exposes an entitlements.local_mapping support label ('all first-party') and provider lanes ('local-identical' for fake/stripe/braintree)"
    - "Each adapter (Fake/Stripe/Braintree) capabilities/0 returns an identical entitlements: %{local_mapping: true} key"
    - "LocalMap.resolve/2 returns byte-identical resolved maps when :processor is swapped across Fake/Stripe/Braintree, with zero processor calls"
    - "The drift gate fails the build if the matrix doc and code labels diverge, OR if an entitlements row ever sprouts a per-provider native/unsupported/bounded divergence label"
  artifacts:
    - path: "accrue/lib/accrue/processor/capabilities.ex"
      provides: "entitlements: group in @support_labels and @provider_support_labels"
      contains: "entitlements:"
    - path: ".planning/processor-support-matrix.md"
      provides: "entitlements.local_mapping matrix row + identity prose"
      contains: "entitlements.local_mapping"
    - path: "scripts/ci/verify_processor_support_matrix.sh"
      provides: "positive require_substring assertions + negative divergence guard for entitlements rows"
      contains: "entitlements"
    - path: "accrue/test/accrue/entitlements/provider_honesty_test.exs"
      provides: "Fake-lane deterministic merge-blocking proof"
      contains: "provider"
  key_links:
    - from: "scripts/ci/verify_processor_support_matrix.sh"
      to: ".planning/processor-support-matrix.md"
      via: "grep -Fq exact-literal match on the entitlements row"
      pattern: "entitlements.local_mapping"
    - from: "accrue/test/accrue/entitlements/provider_honesty_test.exs"
      to: "accrue/lib/accrue/processor/capabilities.ex"
      via: "support_label/1 + provider_support_label/2 assertions equal the doc literals"
      pattern: "support_label"
---

<objective>
Establish the provider-honesty capability surface for entitlements (ENT-08, SC#1 + SC#2): add ONE additive `entitlements:` capability group to `Accrue.Processor.Capabilities` plus an identical `entitlements: %{local_mapping: true}` key to all three adapters, mirror it in the `.planning/processor-support-matrix.md` SSOT doc and the `verify_processor_support_matrix.sh` drift gate, and prove with a Fake-lane test that `LocalMap.resolve/2` is byte-identical across providers with zero processor calls.

Purpose: Make the local-first thesis machine-honest — entitlement resolution is provider-INDEPENDENT local derivation (structural, not coincidental), and the support contract says so in a place a drift gate protects. This mirrors the SCM-06 / PROC-24 support-contract pattern Phase 123 D-15 forecast.

Output: Extended capability code labels + matrix doc + bash drift gate (co-updated this PR per D-09) + a new merge-blocking provider-honesty proof test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-RESEARCH.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md

<interfaces>
<!-- Extracted from the live codebase — use these directly; no exploration needed. -->

From accrue/lib/accrue/processor/capabilities.ex (verified line numbers):
- `@support_labels %{...}` @11 — nested group map; existing groups include `webhook: %{verify: "all first-party", parse: "all first-party"}` @56. Convergence label literal `"all first-party"` is already in use.
- `@provider_support_labels %{...}` @62 — every existing provider lane encodes DIVERGENCE (e.g. `subscription.swap_plan: %{fake: "testing/local-only", stripe: "native", braintree: "bounded first-party"}`). No existing lane uses a convergence term — `"local-identical"` is NEW.
- `support_label/1` @124-131, `provider_support_label/2` @134-143, `for/1` @102, `supports?/2` @116 — read the attrs via `get_in/2`. REUSE VERBATIM — adding the group is the only change they need to resolve the new path.

From accrue/lib/accrue/processor/{fake,stripe,braintree}.ex:
- `def capabilities do %{...} end` — Fake @220, Stripe @79, Braintree @17. Returns a map of capability groups (`customer:`, `subscription:`, … `webhook: %{verify: true, parse: true}`). Braintree's map differs in places (e.g. `subscription` carries `cancel_at_period_end: false`) — leave all that untouched; only APPEND `entitlements: %{local_mapping: true}`.

From scripts/ci/verify_processor_support_matrix.sh (verified):
- `matrix="${repo_root}/.planning/processor-support-matrix.md"` @6
- `require_substring()` helper @13-20 (`grep -Fq "${needle}" "${matrix}"` → exit 1 with a labeled message on miss).
- Positive row assertions @22-48 (e.g. `require_substring "| subscription.swap_plan | testing/local-only | native | bounded first-party | official active-subscription-change |" "swap-plan capability row"` @26).
- Final `echo "verify_processor_support_matrix: OK"` @100 (place new assertions BEFORE it).

From .planning/processor-support-matrix.md:
- Table header `| Capability | Fake | Stripe | Braintree | Public label |` @31 (asserted by the gate @22).
- Existing divergence rows (swap_plan/preview) are the contrast template the entitlements convergence row deliberately differs from.

From accrue/lib/accrue/entitlements/resolver/local_map.ex:
- `def resolve(billable, _opts \\ [])` @39 — takes NO `:processor` arg; reads only `Accrue.Repo`. This is the structural proof that swapping `:processor` cannot change output (D-03).

From accrue/lib/accrue/processor/fake.ex:
- `reset/0` zeros all per-resource counters (Fake state is read via `:sys.get_state/1`; no public arbitrary-counter accessor confirmed — see A2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the entitlements: capability group (code labels + 3 adapters)</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, accrue/lib/accrue/processor/fake.ex, accrue/lib/accrue/processor/stripe.ex, accrue/lib/accrue/processor/braintree.ex</files>
  <read_first>
    - accrue/lib/accrue/processor/capabilities.ex (current @support_labels @11, @provider_support_labels @62, accessors @102-143 — the in-file analog to clone)
    - accrue/lib/accrue/processor/fake.ex (capabilities/0 @220 — the map to append to)
    - accrue/lib/accrue/processor/stripe.ex (capabilities/0 @79)
    - accrue/lib/accrue/processor/braintree.ex (capabilities/0 @17 — note its keys differ; append only)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (sections "capabilities.ex" and "{fake,stripe,braintree}.ex capabilities/0" — exact label literals)
    - accrue/test/accrue/processor/capabilities_test.exs (the support_label/provider_support_label assertion style)
  </read_first>
  <behavior>
    - `Capabilities.support_label([:entitlements, :local_mapping])` returns `"all first-party"`.
    - `Capabilities.provider_support_label(:fake, [:entitlements, :local_mapping])` returns `"local-identical"` (same for `:stripe`, `:braintree`).
    - `Accrue.Processor.Fake.capabilities()[:entitlements]` equals `%{local_mapping: true}` (same for Stripe and Braintree — byte-identical across all three).
    - Existing capability groups and accessors are unchanged (no regression in capabilities_test.exs).
  </behavior>
  <action>
    Add an `entitlements:` group to BOTH module attributes in `capabilities.ex` (D-02): in `@support_labels` add `entitlements: %{local_mapping: "all first-party"}`; in `@provider_support_labels` add `entitlements: %{local_mapping: %{fake: "local-identical", stripe: "local-identical", braintree: "local-identical"}}`. Use the EXACT string literals `"all first-party"` and `"local-identical"` — they must byte-match the matrix doc + bash gate in Tasks 2/3. Do NOT touch `support_label/1`, `provider_support_label/2`, `for/1`, `supports?/2` (they resolve the new path via `get_in/2` automatically). Add exactly ONE core `local_mapping` row — do NOT over-decompose per gate function, and do NOT add an `unmapped_plan_fail_closed` row (lean minimal per Open Question #1; resist matrix bloat). Do NOT add a `native` entitlements row (D-03 — that would over-promise the Phase 127 deferred path). Then APPEND `entitlements: %{local_mapping: true}` as a new key to `capabilities/0` in all three adapters — the literal must be byte-identical in all three (the honest claim is sameness per D-03); leave every other key in each adapter's map untouched.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/capabilities_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/capabilities.ex` contains `entitlements:` inside both `@support_labels` and `@provider_support_labels`.
    - `cd accrue && mix run -e 'IO.inspect(Accrue.Processor.Capabilities.support_label([:entitlements, :local_mapping]))'` prints `"all first-party"`.
    - `cd accrue && mix run -e 'for p <- [:fake, :stripe, :braintree], do: ^"local-identical" = Accrue.Processor.Capabilities.provider_support_label(p, [:entitlements, :local_mapping])'` exits 0.
    - `cd accrue && mix run -e 'true = (Accrue.Processor.Fake.capabilities()[:entitlements] == %{local_mapping: true}) and (Accrue.Processor.Stripe.capabilities()[:entitlements] == %{local_mapping: true}) and (Accrue.Processor.Braintree.capabilities()[:entitlements] == %{local_mapping: true})'` exits 0.
    - No `native`, `unsupported`, or `bounded` token appears on any entitlements label in `capabilities.ex` (`grep -v '^#' accrue/lib/accrue/processor/capabilities.ex | grep -A4 'entitlements:' | grep -Ec '(native|unsupported|bounded)'` returns 0).
    - `cd accrue && mix test test/accrue/processor/capabilities_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The entitlements capability group exists in code with the convergence labels and all three adapters return identical `entitlements: %{local_mapping: true}`; existing capability tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the entitlements row + identity prose into the matrix doc</name>
  <files>.planning/processor-support-matrix.md</files>
  <read_first>
    - .planning/processor-support-matrix.md (the `| Capability | Fake | Stripe | Braintree | Public label |` table @31 + the existing checkout/portal honesty paragraph for prose tone)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (section ".planning/processor-support-matrix.md" — exact row literal + the three gate-pinned prose strings)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md (D-04, D-06, D-08 — same-PR co-update + the contrast-legible framing)
  </read_first>
  <action>
    Add an Entitlements section to `.planning/processor-support-matrix.md` (D-06 — extend the existing artifact; do NOT create a separate entitlements matrix). Add the row to the capability table EXACTLY as: `| entitlements.local_mapping | local-identical | local-identical | local-identical | all first-party |` — it must byte-match the Task 1 code labels AND the Task 3 bash `require_substring` literal (D-09 same-PR mirror). Add a short Entitlements prose paragraph near the table that carries these three gate-pinned strings verbatim (Task 3 will `grep -Fq` them): "behaves identically across Stripe, Braintree, and Fake", "zero processor calls", and "local mapping remains the canonical default" (the ENT-10 deferral honesty). Model the wording on the existing checkout/portal honesty paragraph, but INVERT its framing — for entitlements both the public API shape AND the implementation are identical by construction (the row encodes convergence, contrasting the divergence rows above it per D-04). Do NOT touch the public `accrue/guides/entitlements.md` (D-07 — Phase 126 owns it). Do NOT add a `native` row.
  </action>
  <verify>
    <automated>grep -Fq "| entitlements.local_mapping | local-identical | local-identical | local-identical | all first-party |" .planning/processor-support-matrix.md && grep -Fq "behaves identically across Stripe, Braintree, and Fake" .planning/processor-support-matrix.md && grep -Fq "zero processor calls" .planning/processor-support-matrix.md && grep -Fq "local mapping remains the canonical default" .planning/processor-support-matrix.md && echo MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/processor-support-matrix.md` contains the exact row `| entitlements.local_mapping | local-identical | local-identical | local-identical | all first-party |`.
    - The doc contains all three prose strings: `behaves identically across Stripe, Braintree, and Fake`, `zero processor calls`, `local mapping remains the canonical default`.
    - The doc contains NO `entitlements` row carrying `native`, `unsupported`, or `bounded` (`grep -Eqc '\| entitlements\.[a-z_]+ \|[^|]*\b(native|unsupported|bounded)\b' .planning/processor-support-matrix.md` returns 0 / no match).
    - The public guide is untouched (`git diff --name-only accrue/guides/entitlements.md` is empty).
    - The verify `<automated>` command prints `MATRIX_OK`.
  </acceptance_criteria>
  <done>The matrix doc carries the convergence row + identity prose that byte-match the code labels and the bash gate; no public-guide or `native`-row changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the drift gate (positive asserts + negative divergence guard) and add the Fake-lane proof</name>
  <files>scripts/ci/verify_processor_support_matrix.sh, accrue/test/accrue/entitlements/provider_honesty_test.exs</files>
  <read_first>
    - scripts/ci/verify_processor_support_matrix.sh (the `require_substring` helper @13-20, positive asserts @22-48, the stale-row `if grep` guards, final `echo OK` @100)
    - accrue/test/accrue/entitlements/local_map_test.exs (the `:entitlements` env-mutation harness, `async: false`, `on_exit` restore, `Accrue.Test.Factory` seeding)
    - accrue/test/accrue/processor/capabilities_test.exs (the `support_label`/`provider_support_label` literal-assertion style)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (`resolve/2` @39 — takes no `:processor` arg; the structural zero-call proof)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (sections "verify_processor_support_matrix.sh" and "provider_honesty_test.exs" — exact bash literals + the provider-loop proof harness)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-VALIDATION.md (Wave 0: this test is a new merge-blocking artifact)
  </read_first>
  <behavior>
    - Running the gate against a matrix doc missing the entitlements row exits non-zero with a labeled message.
    - Running the gate against a matrix doc whose entitlements row carries `native`/`unsupported`/`bounded` exits non-zero (the negative divergence guard).
    - Running the gate against the correct (Task 2) doc exits 0.
    - In the proof test, `LocalMap.resolve/2` returns `==` resolved maps when `:processor` is set to `Fake`, then `Stripe`, then `Braintree` against identical seeded local state.
    - Zero processor calls occur during resolution (proven structurally and/or by a counter/telemetry regression guard).
    - The proof asserts `Capabilities.support_label([:entitlements, :local_mapping]) == "all first-party"` and `provider_support_label(p, [:entitlements, :local_mapping]) == "local-identical"` for each of `:fake/:stripe/:braintree` (the code-side mirror of the bash gate).
  </behavior>
  <action>
    In `verify_processor_support_matrix.sh`, REUSE the existing `require_substring` helper (do not redefine it). Add positive assertions (place them in the positive-assert block, before the final `echo OK` @100), each byte-matching the Task 2 doc strings (D-08): the entitlements row literal `| entitlements.local_mapping | local-identical | local-identical | local-identical | all first-party |`; `behaves identically across Stripe, Braintree, and Fake`; `zero processor calls`; `local mapping remains the canonical default`. Then add a NEGATIVE divergence guard (mirror the existing `if grep -Fq ...; then echo >&2; exit 1; fi` stale-row guards, but use `grep -Eq`): if `grep -Eq '\| entitlements\.[a-z_]+ \|[^|]*\b(native|unsupported|bounded)\b' "${matrix}"` matches, print a labeled error about an entitlements row sprouting a per-provider divergence label (drift toward Phase 127 ahead of schedule) and `exit 1`. This gate rides the EXISTING `docs-contracts-shift-left` CI job (it already invokes this script) — do NOT add a new CI step (D-09).

    Create `accrue/test/accrue/entitlements/provider_honesty_test.exs` (D-05). Use `Accrue.BillingCase, async: false` and the `local_map_test.exs` env-mutation/`on_exit`-restore harness for `:entitlements` AND for `:processor`. Seed identical local state ONCE (a customer with an active subscription on a mapped `price_id`). Loop `[Accrue.Processor.Fake, Accrue.Processor.Stripe, Accrue.Processor.Braintree]`, set each as `:processor` via `Application.put_env`, call `Accrue.Entitlements.Resolver.LocalMap.resolve(billable, [])`, collect the three `resolved` maps, and assert they are all `==`. Prove zero processor calls — the structural argument (resolve/2 takes no `:processor` arg, reads only `Accrue.Repo`) is the primary proof; add a regression guard as the mechanism (pick one per A2): attach a `:telemetry` handler to `[:accrue, :processor | _]` and assert it never fires, OR snapshot Fake counters via `:sys.get_state/1` after `reset/0` and assert unchanged. Add the capability-label mirror asserts: `Capabilities.support_label([:entitlements, :local_mapping]) == "all first-party"` and `provider_support_label(p, [:entitlements, :local_mapping]) == "local-identical"` for each provider. NOTE: swapping `:processor` is hermetic precisely because `resolve/2` ignores it — no Stripe/Braintree network call occurs. This test rides the merge-blocking `release-gate` `mix test`.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/provider_honesty_test.exs --warnings-as-errors && cd .. && bash scripts/ci/verify_processor_support_matrix.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_processor_support_matrix.sh` exits 0 against the Task 2 doc.
    - The script contains a `require_substring` line for the entitlements row literal and for each of the three prose strings.
    - The script contains a `grep -Eq` negative guard that exits 1 on an entitlements row with `native`/`unsupported`/`bounded` (verify by temporarily editing the doc row to `local-identical | native | local-identical` in a scratch copy → gate exits non-zero; revert).
    - `accrue/test/accrue/entitlements/provider_honesty_test.exs` exists and asserts the three `resolved` maps are `==` across the three processors.
    - The test asserts zero processor calls (telemetry-never-fired OR counter-unchanged).
    - The test asserts `support_label`/`provider_support_label` equal the doc literals.
    - `cd accrue && mix test test/accrue/entitlements/provider_honesty_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The drift gate has positive + negative entitlements assertions and exits 0; the Fake-lane proof passes, proving identical resolution and zero processor calls across all three providers, with code labels mirroring the doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support-contract claim → published doc | A code-declared capability label is a maintainer commitment; drift between code and doc is a repudiation/honesty risk (the contract lies). |
| resolver ← :processor config | The whole local-first thesis is that `:processor` cannot influence entitlement resolution; if it could, swapping providers could silently change access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Repudiation / honesty | entitlements capability row in matrix doc | mitigate | Negative `grep -Eq` divergence guard in `verify_processor_support_matrix.sh` rejects any `native`/`unsupported`/`bounded` label on an entitlements row (over-promising a Phase 127 path). Tied to Task 3 acceptance (scratch-edit → gate exits 1). |
| T-125-02 | Information disclosure / drift | code labels vs matrix doc vs bash gate | mitigate | Same-PR SSOT co-update (D-09): positive `require_substring` lines byte-match the doc row, and `provider_honesty_test.exs` asserts code labels == doc literals — code-side mirror of the bash gate. |
| T-125-03 | Elevation of Privilege | `LocalMap.resolve/2` provider-independence | mitigate | Fake-lane proof asserts `==` resolved maps across Fake/Stripe/Braintree + zero processor calls (structural + telemetry/counter regression guard). Proves `:processor` cannot change who is entitled. |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH "Package Legitimacy Audit: N/A — zero external packages"). No `mix deps.get` change. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/processor/capabilities_test.exs test/accrue/entitlements/provider_honesty_test.exs --warnings-as-errors` exits 0.
- `bash scripts/ci/verify_processor_support_matrix.sh` exits 0.
- `cd accrue && mix credo --strict` stays green (no raw `.status`, no new warnings).
- Full `release-gate`: `cd accrue && mix test --warnings-as-errors` stays green.
</verification>

<success_criteria>
- ENT-08 SC#1: A Resolver behaviour plus `entitlements:` capability-matrix rows make local plan→feature mapping behave identically across Stripe, Braintree, and Fake, with the Fake lane as a deterministic merge-blocking proof. ✔ via Task 1 + Task 3.
- ENT-08 SC#2: Drift between runtime capability labels and the published support-matrix doc fails the build before merge, mirroring SCM-06 / PROC-24. ✔ via Task 2 + Task 3.
- D-09 co-update: code labels + matrix doc + bash gate all updated in this one plan/PR.
</success_criteria>

<output>
Create `.planning/phases/125-provider-honesty-lifecycle-truth/125-01-SUMMARY.md` when done.
</output>

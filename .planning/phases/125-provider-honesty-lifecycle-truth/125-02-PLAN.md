---
phase: 125-provider-honesty-lifecycle-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/subscription.ex
  - accrue/lib/accrue/billing/query.ex
  - accrue/lib/accrue/entitlements/resolver/local_map.ex
  - accrue/guides/lifecycle_semantics.md
  - accrue/test/accrue/billing/subscription_predicates_test.exs
  - accrue/test/accrue/billing/query_test.exs
  - accrue/test/accrue/entitlements/local_map_test.exs
autonomous: true
requirements: [ENT-09]
must_haves:
  truths:
    - "Subscription.entitling?/1 returns true for trialing/active/paid-through-canceling and false for paused (incl. status:active + pause_collection)/ended/canceled/past_due/unpaid/incomplete/incomplete_expired"
    - "Query.entitling/1 returns the same rows per id as entitling?/1 (twin invariant), excluding status:active + non-nil pause_collection"
    - "LocalMap.fold_active/1 base fetch is Query.entitling/1, closing the paused fail-OPEN gap (a status:active + pause_collection subscription no longer grants entitlement)"
    - "lifecycle_semantics.md documents an entitling glossary entry + the full lifecycle→entitlement truth table"
  artifacts:
    - path: "accrue/lib/accrue/billing/subscription.ex"
      provides: "entitling?/1 pure-lifecycle predicate (composes active?/paused?/canceled?)"
      contains: "def entitling?("
    - path: "accrue/lib/accrue/billing/query.ex"
      provides: "entitling/1 Ecto fragment (twin of entitling?/1)"
      contains: "def entitling("
    - path: "accrue/guides/lifecycle_semantics.md"
      provides: "entitling glossary entry + truth table"
      contains: "entitling"
  key_links:
    - from: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      to: "accrue/lib/accrue/billing/query.ex"
      via: "fold_active/1 base fetch uses Query.entitling/1"
      pattern: "Query.entitling"
    - from: "accrue/lib/accrue/billing/subscription.ex"
      to: "accrue/lib/accrue/billing/query.ex"
      via: "predicate ↔ fragment twin invariant (entitling?/1 == Query.entitling/1 per row)"
      pattern: "entitling"
---

<objective>
Establish the lifecycle→entitlement truth SSOT (ENT-09, SC#3 + SC#4) and close the paused fail-OPEN gap (D-11, a V4 broken-access-control correction): add a pure-lifecycle `Accrue.Billing.Subscription.entitling?/1` predicate (composing `active?`/`paused?`/`canceled?`, never raw `.status`) and its mirrored `Accrue.Billing.Query.entitling/1` Ecto fragment, retarget `LocalMap.fold_active/1`'s base fetch to `Query.entitling/1`, document the canonical truth table in `lifecycle_semantics.md`, and pin both the in-memory predicate (all 8 statuses × modifiers) and the predicate↔fragment twin invariant with merge-blocking tests.

Purpose: One canonical answer to "which lifecycle states grant entitlement," reused by every downstream surface (Phase 126 admin + guide derive from `entitling?/1`, never re-deriving). Closes the real gap where a `status: :active` subscription with a non-nil `pause_collection` payload currently GRANTS entitlement despite being paused.

Output: `entitling?/1` + `Query.entitling/1` + retargeted resolver fetch + truth-table guide entry + pin/twin tests + a resolver integration case asserting the paused gap is closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-RESEARCH.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md

<interfaces>
<!-- Extracted from the live codebase — use these directly; no exploration needed. -->

From accrue/lib/accrue/billing/subscription.ex (verified line numbers):
- `def active?(%__MODULE__{status: s}) when s in [:active, :trialing], do: true` @147 (+ map head @148, catch-all @149). Includes trialing.
- `def paused?(...)` @201 — admits BOTH `status: :paused` AND a `status: :active` row with `is_map(pause_collection)`. THIS is the gap source.
- `def canceled?(%__MODULE__{status: s}) when s in [:canceled, :incomplete_expired], do: true` @163 (+ `ended_at: %DateTime{}` head @164, map heads @165-166, catch-all @167). Terminal override on ended_at.
- `def canceling?(...)` @175 — paid-through `cancel_at_period_end` rows are `status: :active` so already covered by `active?`/`not paused?`/`not canceled?`.
- `@statuses` @37 — the full enum (`:trialing, :active, :past_due, :unpaid, :canceled, :incomplete, :incomplete_expired, :paused`, etc.); `statuses/0` @116 returns it.
- Module is Credo-EXEMPT for raw `.status` (NoRawStatusAccess `@exempt_module_prefixes` = ["Accrue.Billing.Subscription", "Accrue.Billing.Query"]) — but compose predicates anyway (convention + avoids edge traps).

From accrue/lib/accrue/billing/query.ex (verified):
- `def active(query \\ Subscription), do: from(s in query, where: s.status in [:active, :trialing])` @30 — status-only. KEEP its semantics (other callers: dunning sweeper, projections — D-11).
- `def paused/1` @71, `def canceled/1` @57, `def past_due/1` @65 (includes :unpaid — do NOT reuse for entitling), `def dunning_sweep_candidates/2` @81.
- Module is Credo-EXEMPT for `s.status in [...]`.

From accrue/lib/accrue/entitlements/resolver/local_map.ex (verified):
- `@empty %{plan: nil, active_plans: MapSet.new(), features: MapSet.new(), quantities: %{}}` @36.
- `def resolve(billable, _opts \\ [])` @39.
- `defp fold_active(%Customer{id: customer_id})` @66 — current base fetch @67-80:
  `Subscription |> Query.active() |> where([s], is_nil(s.ended_at)) |> where([s], s.customer_id == ^customer_id) |> join(:inner, [s], i in SubscriptionItem, on: i.subscription_id == s.id) |> select([_s, i], {i.price_id, i.quantity}) |> Accrue.Repo.all()`. The WR-04 `where(is_nil(ended_at))` @77 folds INTO Query.entitling/1.

From accrue/guides/lifecycle_semantics.md:
- State glossary @133-160: `### active`, `### canceling`, `### paused`, `### past_due`, `### ended` — each a `### name` heading + short prose pointing at the predicate. Add `### entitling` in the same shape + the truth table.

Test analogs (verified to exist):
- accrue/test/accrue/billing/subscription_predicates_test.exs — pure struct-literal predicate tests, `async: true`, setup starts Fake clock + reset (for `canceling?`'s Clock.utc_now/0).
- accrue/test/accrue/billing/query_test.exs — Repo-backed fragment tests (extend for the twin cross-check).
- accrue/test/accrue/entitlements/local_map_test.exs — `:entitlements` env-mutation harness, `async: false`, Factory seeding (extend for the paused-gap case).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add entitling?/1 predicate + Query.entitling/1 fragment (the twin) + truth-table doc</name>
  <files>accrue/lib/accrue/billing/subscription.ex, accrue/lib/accrue/billing/query.ex, accrue/guides/lifecycle_semantics.md</files>
  <read_first>
    - accrue/lib/accrue/billing/subscription.ex (active?/1 @147, paused?/1 @201, canceled?/1 @163, canceling?/1 @175, the moduledoc predicate list @18-28, @statuses @37)
    - accrue/lib/accrue/billing/query.ex (active/1 @30, canceled/1 @57, paused/1 @71, past_due/1 @65, dunning_sweep_candidates/2 @81 — the fragment shapes; the module moduledoc declaring the twin contract @1-12)
    - accrue/guides/lifecycle_semantics.md (the State glossary entries @133-160 — the `### name` + prose shape to mirror)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (sections "subscription.ex entitling?/1", "query.ex entitling/1", "lifecycle_semantics.md" — exact signatures + the CRITICAL gotcha + the verbatim truth table)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md (D-10, D-11, D-12, D-14)
  </read_first>
  <behavior>
    - `Subscription.entitling?/1` returns true for: `%{status: :trialing}`, `%{status: :active}`, `%{status: :active, cancel_at_period_end: true, current_period_end: <future>}`.
    - `Subscription.entitling?/1` returns false for: `%{status: :active, pause_collection: %{"behavior" => "void"}}` (the D-11 gap), `%{status: :active, ended_at: <past>}` (WR-04), `:paused`, `:past_due`, `:unpaid`, `:canceled`, `:incomplete`, `:incomplete_expired`.
    - `Query.entitling/1 |> Repo.all()` returns exactly the rows for which `entitling?/1` is true (twin invariant), including EXCLUDING a `status: :active` + non-nil `pause_collection` row.
  </behavior>
  <action>
    In `subscription.ex`, add `entitling?/1` (D-10): `def entitling?(sub), do: active?(sub) and not paused?(sub) and not canceled?(sub)` with `@spec entitling?(%__MODULE__{} | map()) :: boolean()` and a `@doc` stating it is the single source of truth for which lifecycle states grant entitlement and pointing at `guides/lifecycle_semantics.md`. Compose the existing predicates — do NOT write raw `s.status == ...` (composition is the convention even though this module is Credo-exempt). `canceling?/1` (paid-through) is auto-covered (such rows are `status: :active`). Add an `entitling?/1` bullet to the moduledoc predicate list. Do NOT modify `@statuses`.

    In `query.ex`, add the twin fragment `entitling/1` (D-10): `def entitling(query \\ Subscription), do: from(s in query, where: s.status in [:active, :trialing] and is_nil(s.pause_collection) and is_nil(s.ended_at))` with `@spec` + `@doc`. CRITICAL gotcha (RESEARCH Pattern 2): add ONLY `is_nil(s.pause_collection)` beyond the `active/1` status set — do NOT add the full `paused/1` legacy-paused OR-clause (`active/1` already excludes `:paused` status; the ONLY thing missing is the `pause_collection` map on a `status: :active` row). Do NOT change `Query.active/1` — it keeps its semantics for other callers (D-11).

    In `lifecycle_semantics.md`, add an `### entitling` glossary entry (same `### name` + prose shape as the existing entries) + the D-12 truth table verbatim (the row set in PATTERNS.md "Truth table to render"). Render `:past_due` as `✗ default / ✅ in-grace` with a footnote noting the knob is configured via `past_due_grace` (Plan 03 adds the knob mechanics + the detailed footnote; this plan documents the pure-lifecycle table and the past_due row placeholder). Do NOT touch the PUBLIC `accrue/guides/entitlements.md` (D-07).
  </action>
  <verify>
    <automated>cd accrue && mix compile --warnings-as-errors && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription.ex` contains `def entitling?(`.
    - `accrue/lib/accrue/billing/query.ex` contains `def entitling(` and the body references `is_nil(s.pause_collection)` and `is_nil(s.ended_at)`.
    - `Query.entitling/1` does NOT contain a legacy `:paused` status OR-clause (`grep -A6 'def entitling(' accrue/lib/accrue/billing/query.ex | grep -c ':paused'` returns 0).
    - `Query.active/1` is unchanged (`git diff accrue/lib/accrue/billing/query.ex` shows no edit to the `def active(` clause).
    - `accrue/guides/lifecycle_semantics.md` contains an `entitling` glossary entry and a truth-table row for `pause_collection`.
    - `cd accrue && mix run -e 'true = Accrue.Billing.Subscription.entitling?(%Accrue.Billing.Subscription{status: :active}); false = Accrue.Billing.Subscription.entitling?(%Accrue.Billing.Subscription{status: :active, pause_collection: %{"behavior" => "void"}}); false = Accrue.Billing.Subscription.entitling?(%Accrue.Billing.Subscription{status: :past_due})'` exits 0.
    - `cd accrue && mix credo --strict` exits 0 (no NoRawStatusAccess violation).
  </acceptance_criteria>
  <done>`entitling?/1` and `Query.entitling/1` exist as a twin pair with the pause/ended guards, `Query.active/1` untouched, the truth table documented; compiles warning-free and Credo-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget LocalMap.fold_active/1 base fetch to Query.entitling/1 (close the paused gap)</name>
  <files>accrue/lib/accrue/entitlements/resolver/local_map.ex</files>
  <read_first>
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (fold_active/1 @66-80 — the current base fetch with the WR-04 `where(is_nil(ended_at))` @77; @empty @36; resolve/2 @39; merge_plan/handle_unmapped/catalog helpers)
    - accrue/lib/accrue/billing/query.ex (Query.entitling/1 added in Task 1)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (section "local_map.ex fold_active/1" — D-13 retarget; note the WR-04 local where folds into the fragment and is removable)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md (D-13, D-11)
  </read_first>
  <behavior>
    - `LocalMap.fold_active/1` fetches via `Query.entitling/1` (not `Query.active/1`), so a `status: :active` + non-nil `pause_collection` subscription on a mapped price is NO LONGER folded into `active_plans`/`features`/`quantities`.
    - A normal active subscription on a mapped price still resolves to the same `resolved` map as before (no regression).
    - The now-redundant local `where([s], is_nil(s.ended_at))` is removed (it lives in `Query.entitling/1`).
  </behavior>
  <action>
    Swap `fold_active/1`'s base fetch from `Subscription |> Query.active() |> where([s], is_nil(s.ended_at))` to `Subscription |> Query.entitling()` (D-13), keeping the `where([s], s.customer_id == ^customer_id)`, the inner join to `SubscriptionItem`, the `select([_s, i], {i.price_id, i.quantity})`, and `Accrue.Repo.all()` exactly as today. Remove the now-redundant WR-04 `where([s], is_nil(s.ended_at))` line (it is now inside `Query.entitling/1`) and update/trim the WR-04 comment to note the exclusion moved into the fragment. Do NOT add grace handling here — Plan 03 (Wave 2) adds the conditional widening; this plan only retargets the base fetch to close the paused gap. Leave `merge_plan/4`, `handle_unmapped/3`, `catalog/0`, and `@empty` unchanged.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/resolver_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements/resolver/local_map.ex` `fold_active/1` references `Query.entitling()` and NO LONGER calls `Query.active()` (`grep -c 'Query.active' accrue/lib/accrue/entitlements/resolver/local_map.ex` returns 0).
    - The standalone `where([s], is_nil(s.ended_at))` line is removed from `fold_active/1`.
    - `cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/resolver_test.exs --warnings-as-errors` exits 0.
    - `cd accrue && mix credo --strict` exits 0.
  </acceptance_criteria>
  <done>The resolver's base fetch is `Query.entitling/1`, closing the paused fail-OPEN gap in the read path; existing resolver tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin entitling?/1 (8-status table) + the predicate↔fragment twin invariant + the paused-gap closure</name>
  <files>accrue/test/accrue/billing/subscription_predicates_test.exs, accrue/test/accrue/billing/query_test.exs, accrue/test/accrue/entitlements/local_map_test.exs</files>
  <read_first>
    - accrue/test/accrue/billing/subscription_predicates_test.exs (the struct-literal pattern + setup starting the Fake clock @12-22; `async: true`)
    - accrue/test/accrue/billing/query_test.exs (the Repo-backed fragment-test harness + seeding pattern)
    - accrue/test/accrue/entitlements/local_map_test.exs (the `:entitlements` env-mutation harness, `async: false`, Factory active_subscription seeding)
    - accrue/lib/accrue/billing/subscription.ex (entitling?/1 from Task 1) and accrue/lib/accrue/billing/query.ex (Query.entitling/1 from Task 1)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (sections "lifecycle truth-table pin test" + "twin-invariant DB cross-check" — exact test bodies)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-VALIDATION.md (Wave 0: the 8-status pin + the twin invariant are new merge-blocking artifacts)
  </read_first>
  <behavior>
    - The 8-status pin enumerates all `@statuses` × the pause/ended/cancel_at_period_end modifiers and asserts the exact ✅/✗ from the D-12 truth table, INCLUDING `refute entitling?(%{status: :active, pause_collection: %{"behavior" => "void"}})` and `refute entitling?(%{status: :active, ended_at: <past>})`.
    - The twin cross-check seeds each status (incl. the `status: :active` + `pause_collection` row) in the DB and asserts `Subscription.entitling?(row) == (row.id in (Query.entitling() |> Repo.all() |> map ids))` for each.
    - The resolver integration case seeds a `status: :active` + `pause_collection` subscription on a mapped price and asserts the resolved map does NOT grant that plan/feature (the paused gap is closed end-to-end).
  </behavior>
  <action>
    Add the all-8-status `entitling?/1` pin to `subscription_predicates_test.exs` (D-12/D-14) as a single table-driven test using pure struct literals (mirror the existing struct-literal style; the setup already starts the Fake clock so `Accrue.Clock.utc_now/0` works for the `future`/`past` offsets). Use the verbatim assertion block from PATTERNS.md "8-status pin to add" — every status from `@statuses`, plus the `pause_collection`-on-active, `ended_at`-on-active, and `cancel_at_period_end`-future modifier rows.

    Add the twin-invariant cross-check to `query_test.exs` (Pitfall 2 — the strongest drift guard): seed each of the 8 statuses (and the `status: :active` + `pause_collection` row) via the Repo, then assert for each seeded row `Subscription.entitling?(row) == (row.id in (Query.entitling() |> Repo.all() |> Enum.map(& &1.id)))`.

    Add a resolver integration case to `local_map_test.exs`: seed a customer with a `status: :active` + `pause_collection: %{...}` subscription on a mapped `price_id` and assert `LocalMap.resolve/2`'s resolved map does NOT include that plan in `active_plans`/`features` (the paused gap is closed in the read path). All three test files ride the merge-blocking `release-gate` `mix test`.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_predicates_test.exs test/accrue/billing/query_test.exs test/accrue/entitlements/local_map_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `subscription_predicates_test.exs` contains a test asserting `entitling?/1` over all `@statuses` incl. `refute`s for `pause_collection`-on-active and `ended_at`-on-active.
    - `query_test.exs` contains a twin-invariant cross-check asserting `entitling?(row) == (row in Query.entitling() |> Repo.all())` per seeded status.
    - `local_map_test.exs` contains a case asserting a `status: :active` + `pause_collection` subscription does NOT grant entitlement through `resolve/2`.
    - `cd accrue && mix test test/accrue/billing/subscription_predicates_test.exs test/accrue/billing/query_test.exs test/accrue/entitlements/local_map_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The 8-status pin, the predicate↔fragment twin cross-check, and the end-to-end paused-gap closure all pass and are merge-blocking; lifecycle entitlement truth is enforced, not just documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscription lifecycle state → entitlement grant | The decision "does this lifecycle state grant a paid feature" is an access-control gate; granting on a paused/ended/past_due row is broken access control. |
| in-memory predicate ↔ SQL fragment | `entitling?/1` (single-record) and `Query.entitling/1` (multi-record) are two implementations of the same truth; drift between them silently grants or denies on one path only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-04 | Elevation of Privilege | paused fail-OPEN gap (`status: :active` + `pause_collection`) | mitigate | `Query.entitling/1` adds `is_nil(s.pause_collection)`; `fold_active/1` retargeted to it (Task 2). Pin test asserts ✗ (Task 3) and the resolver integration case proves end-to-end closure. (Closes the D-11 V4 broken-access-control gap.) |
| T-125-05 | Elevation of Privilege | predicate/fragment drift granting via the SQL path | mitigate | Twin-invariant cross-check (Task 3) asserts `entitling?(row) == (row in Query.entitling() |> Repo.all())` per status — the strongest drift guard (Pitfall 2). |
| T-125-06 | Elevation of Privilege | re-deriving lifecycle truth from raw `.status` (edge-case traps) | mitigate | `entitling?/1` composes `active?`/`paused?`/`canceled?` (no raw `.status`); `mix credo --strict` (NoRawStatusAccess) is the enforcement; the 8-status pin covers trialing/canceling/incomplete_expired edges. |
| T-125-07 | Tampering | `Query.active/1` semantics changed, breaking other callers | accept→mitigate | D-11 explicitly forbids touching `active/1`; acceptance criterion asserts `git diff` shows no edit to the `def active(` clause — only `entitling/1` carries the entitlement-grade guards. |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/billing/subscription_predicates_test.exs test/accrue/billing/query_test.exs test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/resolver_test.exs --warnings-as-errors` exits 0.
- `cd accrue && mix credo --strict` stays green (no raw `.status` outside the exempt modules).
- `cd accrue && mix compile --warnings-as-errors` succeeds.
- Full `release-gate`: `cd accrue && mix test --warnings-as-errors` stays green.
</verification>

<success_criteria>
- ENT-09 SC#3 (lifecycle half): Entitlement truth maps explicitly to existing lifecycle states (trialing ✅, canceling/paid-through ✅, paused ✗, canceled ✗) as a documented SSOT truth table. ✔ via Task 1 (predicate + table) + Task 3 (pin).
- ENT-09 SC#4: An operator/developer can read one canonical truth table in `lifecycle_semantics.md`. ✔ via Task 1.
- D-11: the paused fail-OPEN gap is closed in the resolver read path. ✔ via Task 2 + Task 3.
- (Past-due grace half of SC#3/#4 is delivered by Plan 03, Wave 2.)
</success_criteria>

<output>
Create `.planning/phases/125-provider-honesty-lifecycle-truth/125-02-SUMMARY.md` when done.
</output>

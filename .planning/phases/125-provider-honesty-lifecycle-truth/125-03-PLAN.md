---
phase: 125-provider-honesty-lifecycle-truth
plan: 03
type: execute
wave: 2
depends_on: ["125-02"]
files_modified:
  - accrue/lib/accrue/config.ex
  - accrue/lib/accrue/entitlements/past_due_grace.ex
  - accrue/lib/accrue/billing/query.ex
  - accrue/lib/accrue/entitlements/resolver.ex
  - accrue/lib/accrue/entitlements/resolver/local_map.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/guides/lifecycle_semantics.md
  - accrue/test/accrue/config_entitlements_test.exs
  - accrue/test/accrue/entitlements/past_due_grace_test.exs
autonomous: true
requirements: [ENT-09]
must_haves:
  truths:
    - "past_due_grace config key accepts :none (default), :dunning, or a positive integer; boot-validates the union and rejects garbage"
    - "With past_due_grace: :none, a :past_due subscription grants no entitlement and fold_active/1 issues zero extra query/compute (Query.entitling/1 unchanged)"
    - "With past_due_grace enabled, a :past_due subscription within the window grants entitlement; outside the window it is denied; :unpaid never receives grace"
    - "Telemetry :reason is :past_due_grace when granted via the window and :past_due_expired when denied because the window lapsed"
    - "lifecycle_semantics.md footnotes the past_due knob: affirmative configured grant, measured from past_due_since via Accrue.Clock, surfaced as :past_due_grace / :past_due_expired"
  artifacts:
    - path: "accrue/lib/accrue/config.ex"
      provides: "past_due_grace schema key + past_due_grace/0 accessor"
      contains: "past_due_grace"
    - path: "accrue/lib/accrue/entitlements/past_due_grace.ex"
      provides: "pure clock-driven within_grace?/2 (fail-closed on nil past_due_since)"
      contains: "def within_grace?("
    - path: "accrue/lib/accrue/entitlements/resolver.ex"
      provides: "additive :grace_plans field on the resolved type"
      contains: "grace_plans"
  key_links:
    - from: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      to: "accrue/lib/accrue/entitlements/past_due_grace.ex"
      via: "per-row within_grace?/2 evaluation on the grace lane"
      pattern: "PastDueGrace.within_grace?"
    - from: "accrue/lib/accrue/entitlements.ex"
      to: "accrue/lib/accrue/entitlements/resolver.ex"
      via: ":grace_plans on the resolved map selects :past_due_grace / :past_due_expired reasons"
      pattern: "grace_plans"
    - from: "accrue/lib/accrue/entitlements/past_due_grace.ex"
      to: "accrue/lib/accrue/clock.ex"
      via: "clock-driven cutoff math via Accrue.Clock.utc_now/0"
      pattern: "Accrue.Clock.utc_now"
---

<objective>
Deliver the past-due grace knob (ENT-09, SC#3 + SC#4): add a `past_due_grace` config key under `:entitlements` (default fail-closed `:none`), a pure clock-driven `Accrue.Entitlements.PastDueGrace.within_grace?/2` helper, a cost-aware conditional fold-widening in `LocalMap.fold_active/1` (zero query change when `:none`; widen + per-row clock filter when enabled), an additive `:grace_plans` field on the resolved map, two additive telemetry reason atoms (`:past_due_grace`, `:past_due_expired`), and the knob footnote in the truth table.

Purpose: Operators get a fail-safe configurable grace policy for `:past_due` subscriptions that reuses the dunning grace overlay. The default `:none` preserves the shipped fail-closed behavior; a grace grant is always an affirmative, resolved, configured decision — never a fail-open (D-15, preserving the headline Phase 123 D-10 contract).

Output: config key + accessor + `PastDueGrace` module + grace-widen query fragment + conditional resolver widening + `:grace_plans` resolved field + grace reason selection + truth-table footnote + boot-validation and behavioral grace tests.

Depends on Plan 02 (Wave 1): requires `Query.entitling/1` (the `:none` base fetch), `Subscription.entitling?/1`, the retargeted `fold_active/1`, and the documented truth table to footnote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-RESEARCH.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md
@.planning/phases/125-provider-honesty-lifecycle-truth/125-02-SUMMARY.md

<interfaces>
<!-- Extracted from the live codebase — use these directly; no exploration needed. -->

From accrue/lib/accrue/config.ex (verified line numbers):
- `:entitlements` schema @356 — a nested `:keyword_list` with `keys:` including `plans:`, `resolver:`, `unmapped_action: [type: {:in, [:deny, :raise]}]`, `billable: [type: {:or, [nil, {:fun, 1}]}]`, `on_deny:`, `deny_path:`. ADD `past_due_grace` here.
- `:dunning` schema @228 — has `grace_days: 14` default @232 (the window source for the `:dunning` mode).
- `dunning/0` @747 — `def dunning, do: get!(:dunning)` (returns the keyword incl. `grace_days: 14`). The accessor shape to mirror.
- `entitlements/0` @874-886 — RAW `get!()` read + `Keyword.put_new/3` for the three guard keys; does NOT normalize nested defaults — so the new accessor adds its own `:none` default via `Keyword.get/3`.
- `validate_at_boot!/0` @500 — runs `NimbleOptions.validate!/2` over the whole `@schema`; the union validates automatically — NO edit here, NO custom validator (D-16; precedents: `{:or, [...]}` @ reply_to_email/billable, `{:in, [...]}` @ unmapped_action).

From accrue/lib/accrue/billing/query.ex:
- `Query.entitling/1` (added Plan 02) — `from(s in query, where: s.status in [:active, :trialing] and is_nil(s.pause_collection) and is_nil(s.ended_at))`. The grace-widen fragment ADDS `:past_due` to the status set, keeps both nil guards.
- `dunning_sweep_candidates/2` @81 — strict `:past_due` precedent + the cutoff math `cutoff = DateTime.add(Accrue.Clock.utc_now(), -grace_days * 86_400, :second)` (inverted for grace).
- Query module is Credo-EXEMPT for `s.status in [...]` (so `:past_due` in the widen fragment is sanctioned).
- Do NOT reuse `Query.past_due/1` @65 — it includes `:unpaid` (D-17 forbids grace for `:unpaid`).

From accrue/lib/accrue/entitlements/resolver.ex:
- `@type resolved :: %{plan: term(), active_plans: MapSet.t(), features: MapSet.t(), quantities: map()}` @38-43. ADD `grace_plans: MapSet.t()` (Open Question #2 → MapSet, consistent with `:active_plans`). Do NOT add a `capabilities/0` callback (D-01). Update the typedoc bullets @28-37.

From accrue/lib/accrue/entitlements/resolver/local_map.ex:
- `@empty %{plan: nil, active_plans: MapSet.new(), features: MapSet.new(), quantities: %{}}` @36 — ADD `grace_plans: MapSet.new()`.
- `fold_active/1` @66 (retargeted to `Query.entitling/1` by Plan 02) — the `:none` lane keeps the lean `select([_s, i], {i.price_id, i.quantity})`; the grace lane needs a richer select carrying `s.status` + `s.past_due_since` to evaluate `within_grace?/2` per row.
- `merge_plan/4`, `handle_unmapped/3`, `catalog/0` reused unchanged.

From accrue/lib/accrue/entitlements.ex:
- `entitled?/3` reason `cond` @62-77 (`{true, :entitled}` / `{false, :no_active_subscription}` / `{false, :not_entitled}` / `:error -> {false, :error}`); parallel conds in `has_active_plan?/3` @91, `features_for/1` @119, `entitlement_quantity/2` @138. The reason flows into `span/6` @232 as `:reason` metadata (already OTel-allowlisted — only atom VALUES change; NO new event, NO Telemetry.Ops.emit per D-19/D-21). `subject_id` @222 is PII-safe.

From accrue/lib/accrue/billing/subscription.ex:
- `dunning_sweepable?/1` @218 — strictly `status == :past_due` (Credo-clean). USE THIS for the per-row "is this a grace candidate?" check in the resolver — NOT a raw `sub.status == :past_due` (which WOULD trip NoRawStatusAccess in `local_map.ex`, NOT an exempt module).

From accrue/lib/accrue/clock.ex:
- `utc_now/0` @26 — delegates to the Fake test clock in `:test`. The MANDATED clock for grace math; NEVER `DateTime.utc_now/0` directly.

Credo exemption (verified): `@exempt_module_prefixes = ["Accrue.Billing.Subscription", "Accrue.Billing.Query"]` + all `test/` files. `Accrue.Entitlements.*` (incl. `PastDueGrace`, `LocalMap`) is NOT exempt — route raw status through `Subscription.dunning_sweepable?/1`.

Test analogs (verified):
- accrue/test/accrue/config_entitlements_test.exs — boot/validation tests for the `:entitlements` schema (extend for `past_due_grace` union validation).
- accrue/test/accrue/entitlements/local_map_test.exs + guard_telemetry_test.exs — env-mutation + telemetry-capture harnesses for the grace behavior + reason atoms.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add past_due_grace config key + accessor + the pure PastDueGrace.within_grace?/2 helper</name>
  <files>accrue/lib/accrue/config.ex, accrue/lib/accrue/entitlements/past_due_grace.ex, accrue/test/accrue/config_entitlements_test.exs, accrue/test/accrue/entitlements/past_due_grace_test.exs</files>
  <read_first>
    - accrue/lib/accrue/config.ex (:entitlements schema keys @356-419, :dunning schema + grace_days @228-242, dunning/0 @747, entitlements/0 @874-886, validate_at_boot!/0 @500; the {:or,..}/{:in,..} precedents)
    - accrue/lib/accrue/billing/subscription.ex (canceling?/1 @175 for the DateTime.compare + Accrue.Clock pattern; dunning_sweepable?/1 @218)
    - accrue/lib/accrue/billing/query.ex (dunning_sweep_candidates/2 @81 cutoff math — the template to invert)
    - accrue/lib/accrue/clock.ex (utc_now/0 @26 — the mandated clock)
    - accrue/test/accrue/config_entitlements_test.exs (the validation-test harness to extend)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (sections "config.ex past_due_grace" + "past_due_grace.ex" — verbatim schema literal, accessor, module body, inverted cutoff math)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md (D-15, D-16, D-17)
  </read_first>
  <behavior>
    - `Accrue.Config.past_due_grace()` returns `:none` by default, the configured `:dunning`, or a configured positive integer.
    - Boot validation accepts `:none`, `:dunning`, and any positive integer for `past_due_grace`; it rejects an invalid value (e.g. `:bogus`, `0`, `-3`) by raising at boot.
    - `PastDueGrace.within_grace?(%{past_due_since: nil}, n)` returns false (fail-closed).
    - `PastDueGrace.within_grace?(%{past_due_since: <now - 1 day>}, 14)` returns true; with `<now - 30 days>` returns false (clock-driven via `Accrue.Clock`).
    - `within_grace?/2` with non-positive grace_days or a non-DateTime since returns false (catch-all).
  </behavior>
  <action>
    In `config.ex`, add the `past_due_grace` key to the nested `:entitlements` schema `keys:` list (D-16) using the EXACT literal from PATTERNS.md: `type: {:or, [{:in, [:dunning, :none]}, :pos_integer]}`, `default: :none`, with the verbatim `doc:` string. No custom validator — NimbleOptions validates the union natively at boot via the existing `validate_at_boot!/0` (do NOT edit `validate_at_boot!/0`). Add a `past_due_grace/0` accessor mirroring `dunning/0`'s shape: `def past_due_grace, do: entitlements() |> Keyword.get(:past_due_grace, :none)` with a `@spec past_due_grace() :: :none | :dunning | pos_integer()` and `@doc`. (The `:none` default is applied in the accessor because `entitlements/0` does a raw read without nested defaults.)

    Create `accrue/lib/accrue/entitlements/past_due_grace.ex` (D-17) — a pure, multi-head, fail-closed module under the entitlements layer (NEVER on `Subscription`, D-14). Mirror PATTERNS.md "New module": `within_grace?(%{past_due_since: nil}, _grace_days), do: false`; `within_grace?(%{past_due_since: %DateTime{} = since}, grace_days) when is_integer(grace_days) and grace_days > 0` computes `cutoff = DateTime.add(Accrue.Clock.utc_now(), -grace_days * 86_400, :second)` and returns `DateTime.compare(since, cutoff) != :lt` (since >= cutoff → still in window); catch-all `within_grace?(_sub, _grace_days), do: false`. Use `Accrue.Clock.utc_now/0` — NEVER `DateTime.utc_now/0` directly. `@spec within_grace?(map(), pos_integer()) :: boolean()` + moduledoc.

    Extend `config_entitlements_test.exs` with `past_due_grace` boot-validation cases (accepts `:none`/`:dunning`/`N`; rejects garbage). Create `accrue/test/accrue/entitlements/past_due_grace_test.exs` exercising `within_grace?/2` via the Fake/`Accrue.Clock` (advance the test clock past/within the window) — assert nil-`past_due_since` → false, within-window → true, beyond-window → false, non-positive grace_days → false.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements/past_due_grace_test.exs --warnings-as-errors && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/config.ex` contains `past_due_grace:` in the `:entitlements` schema with `type: {:or, [{:in, [:dunning, :none]}, :pos_integer]}` and `default: :none`, and a `def past_due_grace`.
    - `cd accrue && mix run -e ':none = Accrue.Config.past_due_grace()'` exits 0 (default).
    - `accrue/lib/accrue/entitlements/past_due_grace.ex` contains `def within_grace?(` and references `Accrue.Clock.utc_now` and does NOT reference `DateTime.utc_now` (`grep -c 'DateTime.utc_now' accrue/lib/accrue/entitlements/past_due_grace.ex` returns 0).
    - `config_entitlements_test.exs` includes a case asserting boot rejects an invalid `past_due_grace` value.
    - `past_due_grace_test.exs` includes within-window true / beyond-window false / nil-since false cases driven by `Accrue.Clock`.
    - `cd accrue && mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements/past_due_grace_test.exs --warnings-as-errors` exits 0.
    - `cd accrue && mix credo --strict` exits 0 (no raw `.status` in `PastDueGrace`).
  </acceptance_criteria>
  <done>The `past_due_grace` knob is configurable + boot-validated, and the pure clock-driven `within_grace?/2` helper is correct and fail-closed; both are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grace-widen fragment + conditional fold widening + :grace_plans resolved field</name>
  <files>accrue/lib/accrue/billing/query.ex, accrue/lib/accrue/entitlements/resolver.ex, accrue/lib/accrue/entitlements/resolver/local_map.ex</files>
  <read_first>
    - accrue/lib/accrue/billing/query.ex (Query.entitling/1 from Plan 02, dunning_sweep_candidates/2 @81, past_due/1 @65 — do NOT reuse it)
    - accrue/lib/accrue/entitlements/resolver.ex (@type resolved @38-43 + typedoc @28-37)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (@empty @36, fold_active/1 @66 as retargeted by Plan 02, merge_plan/4, handle_unmapped/3, catalog/0)
    - accrue/lib/accrue/entitlements/past_due_grace.ex (within_grace?/2 from Task 1)
    - accrue/lib/accrue/config.ex (past_due_grace/0 + dunning/0 from Task 1)
    - accrue/lib/accrue/billing/subscription.ex (dunning_sweepable?/1 @218 — the Credo-clean strict-:past_due grace-candidate check)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (sections "query.ex entitling_with_grace_candidates", "resolver.ex resolved type", "local_map.ex fold_active/1 conditional widening")
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md (D-18, D-19)
  </read_first>
  <behavior>
    - With `past_due_grace: :none`, `fold_active/1` uses `Query.entitling/1` with the lean `{price_id, quantity}` select — ZERO query/compute change vs Plan 02.
    - With grace enabled, `fold_active/1` uses the grace-widen fragment (status set `[:active, :trialing, :past_due]`, both nil guards), evaluates `within_grace?/2` per `:past_due` row in Elixir, and DROPS past_due rows outside the window before folding their `price_id`.
    - Plans admitted via the grace window are recorded in the resolved map's `:grace_plans` MapSet; the `:none` lane leaves `:grace_plans` as `MapSet.new()`.
    - `:unpaid` rows are never folded (the widen status set excludes `:unpaid`).
  </behavior>
  <action>
    In `query.ex`, add `entitling_with_grace_candidates/1` (D-18) mirroring `Query.entitling/1` but with `s.status in [:active, :trialing, :past_due]` (ADD `:past_due` ONLY — never `:unpaid`; do NOT reuse `Query.past_due/1`), keeping `is_nil(s.pause_collection)` and `is_nil(s.ended_at)`. The clock check stays in Elixir, so this fragment does NOT do the cutoff math. `@spec` + `@doc`.

    In `resolver.ex`, extend `@type resolved` additively with `grace_plans: MapSet.t()` (Open Question #2 — MapSet, consistent with `:active_plans`; do NOT add a `capabilities/0` callback, D-01). Update the typedoc bullets to document `:grace_plans` (plans admitted via the past-due grace window, used to select the `:past_due_grace` reason).

    In `local_map.ex`: add `grace_plans: MapSet.new()` to `@empty`. Make `fold_active/1`'s base fetch conditional on `Accrue.Config.past_due_grace()` (RESEARCH Pattern 3): `:none` → `Query.entitling()` with the existing lean select + fold-as-today (zero change); enabled → `Query.entitling_with_grace_candidates()` with a richer select that ALSO carries `s.status` + `s.past_due_since` so `PastDueGrace.within_grace?/2` runs per row. For the grace lane: resolve the configured policy to a concrete day count (`:dunning` → `Accrue.Config.dunning() |> Keyword.fetch!(:grace_days)`; integer N → N), use `Subscription.dunning_sweepable?/1` (NOT raw `sub.status == :past_due`) as the per-row grace-candidate check, DROP candidate rows where `within_grace?/2` is false BEFORE folding, and tag the kept-via-grace plans into the resolved map's `:grace_plans`. Leave `merge_plan/4`, `handle_unmapped/3`, `catalog/0` unchanged.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/resolver_test.exs --warnings-as-errors && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/query.ex` contains `def entitling_with_grace_candidates(` with status set `[:active, :trialing, :past_due]` and NO `:unpaid` (`grep -A6 'def entitling_with_grace_candidates(' accrue/lib/accrue/billing/query.ex | grep -c ':unpaid'` returns 0).
    - `accrue/lib/accrue/entitlements/resolver.ex` `@type resolved` contains `grace_plans:`.
    - `accrue/lib/accrue/entitlements/resolver/local_map.ex` `@empty` contains `grace_plans: MapSet.new()`; `fold_active/1` branches on `Accrue.Config.past_due_grace()` and references `PastDueGrace.within_grace?` and `Subscription.dunning_sweepable?`.
    - `local_map.ex` contains NO raw `sub.status == :past_due` / `s.status == :past_due` outside an exempt context (`grep -v '^#' accrue/lib/accrue/entitlements/resolver/local_map.ex | grep -Ec 'status == :past_due'` returns 0).
    - `cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/resolver_test.exs --warnings-as-errors` exits 0.
    - `cd accrue && mix credo --strict` exits 0.
  </acceptance_criteria>
  <done>The resolver conditionally widens for grace (zero cost when `:none`), drops out-of-window past_due rows, tags `:grace_plans`, and never grants `:unpaid` — Credo-clean via `dunning_sweepable?/1`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grace telemetry reasons + truth-table footnote + end-to-end grace behavioral coverage</name>
  <files>accrue/lib/accrue/entitlements.ex, accrue/guides/lifecycle_semantics.md, accrue/test/accrue/entitlements/local_map_test.exs</files>
  <read_first>
    - accrue/lib/accrue/entitlements.ex (the reason cond in entitled?/3 @62-77 + has_active_plan?/3 @91 + features_for/1 @119 + entitlement_quantity/2 @138; span/6 @232; subject_id @222)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (the :grace_plans field set in Task 2)
    - accrue/guides/lifecycle_semantics.md (the truth table + past_due row added in Plan 02 — add the detailed footnote here)
    - accrue/test/accrue/entitlements/guard_telemetry_test.exs (the telemetry-capture harness to mirror)
    - accrue/test/accrue/entitlements/local_map_test.exs (the env-mutation + Factory harness for the end-to-end grace cases)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-PATTERNS.md (section "entitlements.ex reason computation" + "lifecycle_semantics.md" footnote)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-CONTEXT.md (D-19, D-20)
    - .planning/phases/125-provider-honesty-lifecycle-truth/125-VALIDATION.md (the grace behavioral coverage Wave 0 item)
  </read_first>
  <behavior>
    - When a `:past_due` subscription within the window grants a feature, the `[:accrue, :entitlements, :check]` span carries `reason: :past_due_grace`.
    - When a `:past_due` subscription is denied specifically because the grace window lapsed, the span carries `reason: :past_due_expired` (distinct from `:no_active_subscription`).
    - Existing reasons (`:entitled`, `:not_entitled`, `:no_active_subscription`, `:error`) are unchanged for non-grace paths; the fail-closed `:error` head is unchanged.
    - The truth table footnote in `lifecycle_semantics.md` explains the past_due knob behavior.
    - End-to-end: `Accrue.entitled?/2` returns true for an in-window past_due sub (grace enabled), false for an out-of-window one and for `:unpaid`, and false for any past_due sub when `past_due_grace: :none`.
  </behavior>
  <action>
    In `entitlements.ex`, extend the reason selection (D-19) to read the resolved map's `:grace_plans` field (set in Task 2 — no re-query): when the affirmative match came via a grace plan, select `:past_due_grace`; when denied specifically because a past-due grace window lapsed, select `:past_due_expired` (distinct from `:no_active_subscription`). Keep the existing `:entitled`/`:not_entitled`/`:no_active_subscription` reasons for non-grace paths and the fail-closed `:error` sentinel head unchanged. The `:reason` is already OTel-allowlisted (only atom VALUES change) — do NOT add a new event and do NOT call `Telemetry.Ops.emit` (D-19/D-21; per-check decisions stay telemetry-only). Apply the reason extension consistently across the gate fns whose conds surface reasons (`entitled?/3` is the primary; mirror in `has_active_plan?/3` where a granted/denied plan can be a grace plan).

    In `lifecycle_semantics.md`, add the D-20 footnote to the past_due truth-table row: grace is an affirmative configured grant (the fail-closed contract is preserved), measured from `past_due_since` via `Accrue.Clock`, surfaced as `reason: :past_due_grace` / `:past_due_expired`; `:none` (default) fails closed, `:dunning`/N grants within the window. Do NOT touch the PUBLIC `accrue/guides/entitlements.md` (D-07).

    Add end-to-end grace behavioral cases to `local_map_test.exs` (or a sibling integration test): with grace enabled (`:dunning` and an integer N), seed a `:past_due` sub on a mapped price with `past_due_since` within/beyond the window and assert grant/deny + the `:grace_plans` membership; seed an `:unpaid` sub and assert it is never granted; with `past_due_grace: :none` assert a `:past_due` sub is denied and the fetch issues no widening. Capture telemetry (mirror `guard_telemetry_test.exs`) to assert `reason: :past_due_grace` on the grant and `:past_due_expired` on the lapsed-window deny. Drive the clock via `Accrue.Clock` (advance the Fake test clock).
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/guard_telemetry_test.exs --warnings-as-errors && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements.ex` references `:past_due_grace` and `:past_due_expired` and reads `grace_plans` from the resolved map.
    - No new telemetry event and no `Telemetry.Ops.emit` added (`grep -c 'Telemetry.Ops.emit' accrue/lib/accrue/entitlements.ex` is unchanged from baseline / introduces no new call).
    - `accrue/guides/lifecycle_semantics.md` past_due footnote mentions `past_due_since`, `Accrue.Clock`, and both `:past_due_grace` / `:past_due_expired`.
    - The public guide `accrue/guides/entitlements.md` is untouched (`git diff --name-only accrue/guides/entitlements.md` is empty).
    - `local_map_test.exs` includes: in-window past_due grant (with `:grace_plans`), out-of-window past_due deny (`:past_due_expired`), `:unpaid`-never-granted, and `:none`-default-denies cases; a telemetry assertion for `:past_due_grace` and `:past_due_expired`.
    - `cd accrue && mix test test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/guard_telemetry_test.exs --warnings-as-errors` exits 0.
    - `cd accrue && mix credo --strict` exits 0.
  </acceptance_criteria>
  <done>Grace grants/denials surface the additive `:past_due_grace` / `:past_due_expired` telemetry reasons, the truth-table footnote documents the knob, and end-to-end grace behavior (incl. `:unpaid` exclusion and `:none` default) is merge-blocking-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config (`past_due_grace`) → entitlement grant | A misconfigured/unsafely-parsed grace knob could grant access it shouldn't; the union must boot-validate fail-loud. |
| clock → grace window decision | `within_grace?/2` decides access based on `now - past_due_since`; a wall-clock-dependent or clock-skewed check is untestable and could grant/deny incorrectly. |
| `:past_due` vs `:unpaid` lifecycle | Grace must extend only to `:past_due` (recoverable); leaking it to `:unpaid` (dunning-terminal) is elevation of privilege. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-08 | Tampering / Input Validation | `past_due_grace` config union | mitigate | `type: {:or, [{:in, [:dunning, :none]}, :pos_integer]}` boot-validated by NimbleOptions via `validate_at_boot!/0` (fail-loud at boot); Task 1 test asserts garbage is rejected. |
| T-125-09 | Tampering | clock-skew / wall-clock dependence in `within_grace?/2` | mitigate | `Accrue.Clock.utc_now/0` mandate (never `DateTime.utc_now/0`); Task 1 acceptance asserts no `DateTime.utc_now` reference; tests drive the Fake clock deterministically. |
| T-125-10 | Information Disclosure (nil) / fail-open | `past_due_since == nil` in `within_grace?/2` | mitigate | Explicit fail-closed head `within_grace?(%{past_due_since: nil}, _) -> false` + catch-all `false`; Task 1 test pins nil → false. |
| T-125-11 | Elevation of Privilege | grace mis-applied to `:unpaid` (dunning-terminal) | mitigate | Grace-widen fragment status set is `[:active, :trialing, :past_due]` (no `:unpaid`); per-row check uses `dunning_sweepable?/1` (strict `:past_due`); Task 3 test asserts `:unpaid` is never granted. |
| T-125-12 | Elevation of Privilege | grace grant treated as fail-open (violating D-10) | accept | A grace grant is an affirmative, RESOLVED, configured decision — never a fail-open; default `:none` preserves the shipped fail-closed contract. Documented in the truth-table footnote (Task 3) and the config `doc:` string. |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: N/A — zero external packages). |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements/past_due_grace_test.exs test/accrue/entitlements/local_map_test.exs test/accrue/entitlements/resolver_test.exs test/accrue/entitlements/guard_telemetry_test.exs --warnings-as-errors` exits 0.
- `cd accrue && mix credo --strict` stays green (no raw `.status` in entitlements layer).
- `cd accrue && mix compile --warnings-as-errors` succeeds.
- Full `release-gate`: `cd accrue && mix test --warnings-as-errors` stays green.
</verification>

<success_criteria>
- ENT-09 SC#3 (grace half): past-due grace is a fail-safe configurable knob reusing the dunning grace overlay, default fail-closed. ✔ via Task 1 (config + helper) + Task 2 (resolver) + Task 3 (reasons).
- ENT-09 SC#4 (grace half): an operator can read one canonical truth table and know exactly how the past-due grace knob behaves. ✔ via Task 3 (footnote).
- D-15: default `:none` preserves shipped fail-closed behavior + the headline Phase 123 D-10 contract.
- D-18: zero query/compute cost when `:none`.
- D-19: additive `:past_due_grace`/`:past_due_expired` reasons on the existing `:check` span; no new event, no Ops.emit.
</success_criteria>

<output>
Create `.planning/phases/125-provider-honesty-lifecycle-truth/125-03-SUMMARY.md` when done.
</output>

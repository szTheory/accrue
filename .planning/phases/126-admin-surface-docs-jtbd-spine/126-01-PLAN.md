---
phase: 126-admin-surface-docs-jtbd-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/entitlements/admin.ex
  - accrue/lib/accrue/entitlements/resolver/local_map.ex
  - accrue/test/accrue/entitlements/admin_test.exs
autonomous: true
requirements: [ENT-11]
must_haves:
  truths:
    - "Admin code can resolve a customer's entitlements from a %Customer{} without re-implementing the resolver fold (D-04)"
    - "The read seam surfaces the unmapped entitling price_ids the resolver structurally discards under :deny (D-03/D-04)"
    - "No new public Accrue.* gate/diagnostic API is added — fetch_entitled/2 (Phase 123 D-07) stays deferred (D-04 hard constraint)"
    - "The one-way dependency holds: admin/entitlements -> billing, never reverse (D-04 hard constraint)"
  artifacts:
    - path: "accrue/lib/accrue/entitlements/admin.ex"
      provides: "Accrue.Entitlements.Admin.resolve_for_customer/1 returning {resolved_map, unmapped_price_ids}"
      contains: "def resolve_for_customer"
      min_lines: 15
    - path: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      provides: "Two @doc false delegations: fold_for_customer/1 and unmapped_entitling_price_ids/1 (reuse fold_active/1 + catalog/0 + active_items/1, no copy)"
      contains: "def fold_for_customer"
    - path: "accrue/test/accrue/entitlements/admin_test.exs"
      provides: "Wave 0 unit test for resolve_for_customer/1 (mapped, unmapped, empty, grace)"
      contains: "resolve_for_customer"
  key_links:
    - from: "accrue/lib/accrue/entitlements/admin.ex"
      to: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      via: "LocalMap.fold_for_customer/1 + LocalMap.unmapped_entitling_price_ids/1"
      pattern: "LocalMap\\.(fold_for_customer|unmapped_entitling_price_ids)"
    - from: "accrue/lib/accrue/entitlements/resolver/local_map.ex (fold_for_customer/1)"
      to: "accrue/lib/accrue/entitlements/resolver/local_map.ex (fold_active/1)"
      via: "thin delegation — zero fold copy"
      pattern: "fold_active\\("
---

<objective>
Build the ONE load-bearing technical artifact for ENT-11: the additive read seam `Accrue.Entitlements.Admin.resolve_for_customer/1` that the admin entitlements tab (Plan 02) calls. It reuses the resolver's private fold (no drift) and independently surfaces the unmapped entitling `price_id`s the resolver structurally discards.

Purpose: The resolver's `fold_active/1` / `catalog/0` / `active_items/1` are all private to `LocalMap`, `resolve/2` takes a *billable* (not a `%Customer{}`), and unmapped price_ids are silently dropped (`handle_unmapped/3 :deny`). Admin holds a `%Customer{}` and needs both the resolved map AND the dropped drift list. This is D-04, candidate (iii): a new internal module + two `@doc false` delegations — never a public gate API (D-07 stays deferred), never a fold copy (SSOT discipline, PITFALLS #2).
Output: `accrue/lib/accrue/entitlements/admin.ex` (new), two `@doc false` functions added to `local_map.ex`, and the Wave 0 unit test `admin_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-CONTEXT.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-VALIDATION.md

<interfaces>
<!-- Resolver privates the seam reuses. Extracted from 126-PATTERNS.md / 126-RESEARCH.md (verified at file:line). Use these directly — no codebase exploration needed beyond confirming current line numbers. -->

From accrue/lib/accrue/entitlements/resolver/local_map.ex:
- fold_active/1 (private, :96) — takes a %Customer{}, returns the resolved map shape below
- catalog/0 (private, :253) — returns {reverse_index, plans, unmapped_action}; reverse_index is %{price_id => plan_atom}
- active_items/1 (private, :150) — takes customer_id, returns [{price_id, quantity, via_grace?}] 3-tuples (entitling subs via Query.entitling/1)
- handle_unmapped(acc, _price_id, :deny), do: acc (:244) — the silent-drop the seam must compensate for

Resolved-map shape returned by fold_active/1 (from @empty, local_map.ex:53-61):
  %{
    plan: atom() | nil,                          # last representative active plan (display only)
    active_plans: MapSet.t(atom()),              # membership SSOT
    features: MapSet.t(atom()),                  # UNION of all active plans' features
    quantities: %{atom() => non_neg_integer()},  # quota_key => min(cap, quantity)
    grace_plans: MapSet.t(atom()),
    grace_features: MapSet.t(atom()),
    expired_grace_plans: MapSet.t(atom())
  }
  # :non_grace_features is an internal accumulator stripped before return (local_map.ex:111-112).

From accrue/lib/accrue/billing/customer.ex:
- %Accrue.Billing.Customer{} with fields id, owner_type, owner_id (the struct admin holds)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing unit test for Accrue.Entitlements.Admin.resolve_for_customer/1</name>
  <read_first>
    - accrue/test/accrue/entitlements/local_map_test.exs (clone target — config-mutation setup at :28-49, customer-seeding idiom at :67-68)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (the resolved-map shape at :53-61; active_items/1 :150; handle_unmapped/3 :deny :244)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ NEW admin_test.exs — exact @entitlements config + setup block)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (Pitfall 1 — factory default "price_basic" is UNMAPPED, the free unmapped fixture)
  </read_first>
  <behavior>
    - Test 1 (mapped): a customer with an entitling sub on a mapped price_id (e.g. "price_p1") → resolve_for_customer/1 returns {resolved, unmapped} where resolved.active_plans contains the plan atom, resolved.features contains the mapped features, and unmapped == [].
    - Test 2 (unmapped drift): a customer with an entitling sub on the factory default "price_basic" (NOT in the test :entitlements config) → resolved.active_plans is empty (silently dropped) AND unmapped contains "price_basic" (Pitfall 1 gift).
    - Test 3 (empty): a customer with no entitling subscription → returns {@empty-equivalent map, []} (active_plans empty, features empty, unmapped []).
    - Test 4 (grace, optional but recommended): with past_due_grace configured and a past_due sub in-window → resolved.grace_plans / grace_features are populated; out-of-window → expired_grace_plans populated. Mirror local_map_test.exs grace coverage if present; otherwise assert the grace_* keys exist and default to empty MapSets.
  </behavior>
  <action>
    Create `accrue/test/accrue/entitlements/admin_test.exs` cloning `local_map_test.exs`'s structure. Use `use Accrue.BillingCase, async: false` (async:false is REQUIRED — the test mutates `Application.put_env(:accrue, :entitlements, ...)` with an `on_exit` restore, exactly per local_map_test.exs:28-49). Define a module-level `@entitlements` keyword with at least two plans (e.g. `p1: [features: [:reports, :export], limits: [seats: 5], price_ids: ["price_p1"]]`, `p2: [...]`) and `unmapped_action: :deny`. Seed customers with the Factory (`Accrue.Test.Factory.active_subscription/1` or `Factory.customer/1` + `Billing.subscribe/2-3`); the test calls `Accrue.Entitlements.Admin.resolve_for_customer(customer)` (it HOLDS a `%Customer{}`, unlike LocalMap.resolve/2 which takes a billable). Assert the `{resolved, unmapped}` tuple shape for the four cases in <behavior>. This test MUST FAIL initially (module `Accrue.Entitlements.Admin` does not exist yet) — that is the RED state. Do NOT stub the module; the failure proves the contract before Task 2 implements it.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/admin_test.exs 2>&1 | grep -qE '(undefined function|module Accrue.Entitlements.Admin is not available|UndefinedFunctionError|CompileError)' && echo "RED-as-expected" || (echo "EXPECTED RED but test did not fail on missing module" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue/test/accrue/entitlements/admin_test.exs` exists and uses `async: false`.
    - It defines an `:entitlements` app-env mutation with `on_exit` restore (clone of local_map_test.exs:28-49).
    - It contains at least the mapped / unmapped / empty test cases asserting the `{resolved, unmapped}` tuple from `Accrue.Entitlements.Admin.resolve_for_customer/1`.
    - Running it fails because `Accrue.Entitlements.Admin` is not yet defined (RED state confirmed by the verify command).
  </acceptance_criteria>
  <done>The unit test exists, mutates :entitlements config with restore, asserts the four resolve_for_customer/1 cases, and fails RED on the missing module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the two @doc false delegations on LocalMap (reuse fold_active/1 + catalog/0 + active_items/1 — no copy)</name>
  <files>accrue/lib/accrue/entitlements/resolver/local_map.ex</files>
  <read_first>
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (fold_active/1 :96; active_items/1 :150 returns {price_id, quantity, via_grace?} 3-tuples; catalog/0 :253 returns {reverse_index, plans, unmapped_action}; the Customer struct match used elsewhere in the module)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ MOD local_map.ex — the exact two @doc false fns to add, verbatim)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-CONTEXT.md (D-04 hard constraints: reuse don't copy; no public gate API; one-way dependency)
  </read_first>
  <action>
    In `accrue/lib/accrue/entitlements/resolver/local_map.ex`, add exactly two `@doc false` public functions that REUSE the existing privates (zero fold copy — drift is the #1 hazard per PITFALLS #2):
    1. `fold_for_customer/1` — matches a `%Customer{}` and delegates straight to the existing private `fold_active/1`. One line of body; it must literally call `fold_active(customer)` so there is one fold, not two.
    2. `unmapped_entitling_price_ids/1` — matches `%Customer{id: customer_id}`, destructures `{reverse_index, _plans, _action} = catalog()` (reusing the existing reverse-index build), pipes `customer_id |> active_items()` (the existing private entitling-sub fetch returning {price_id, quantity, via_grace?} 3-tuples), maps to the `price_id`, rejects any `price_id` already present in `reverse_index` via `Map.has_key?/2`, and `Enum.uniq`s. This surfaces ONLY the price_ids the resolver discards under `:deny`.
    Both functions MUST carry `@doc false` (keeps them out of published API docs — preserves the "no public gate API" posture, D-04). Place them near the other private helpers but as public defs. Do NOT add anything to `accrue/lib/accrue/entitlements.ex` (that is the public gate API and the deferred fetch_entitled/2 trap — D-07). Confirm current line numbers before editing (the file may have shifted since RESEARCH read it).
  </action>
  <verify>
    <automated>cd accrue && grep -q 'def fold_for_customer' lib/accrue/entitlements/resolver/local_map.ex && grep -q 'def unmapped_entitling_price_ids' lib/accrue/entitlements/resolver/local_map.ex && grep -A1 'def fold_for_customer' lib/accrue/entitlements/resolver/local_map.ex | grep -q 'fold_active' && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `local_map.ex` defines `def fold_for_customer(%Customer{} = customer)` whose body calls `fold_active(customer)` (proven by the grep -A1 fold_active check).
    - `local_map.ex` defines `def unmapped_entitling_price_ids(%Customer{...})` that uses `catalog()` + `active_items()` + `Enum.reject`/`Map.has_key?`.
    - Both functions carry `@doc false`.
    - `mix compile --warnings-as-errors` succeeds (no unused-private warnings introduced).
    - `accrue/lib/accrue/entitlements.ex` is UNCHANGED (no new public function added there).
  </acceptance_criteria>
  <done>Two @doc false delegations exist on LocalMap, fold_for_customer/1 calls fold_active/1 (single fold, no copy), unmapped_entitling_price_ids/1 reuses catalog()/active_items(), and core compiles warning-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Accrue.Entitlements.Admin.resolve_for_customer/1 — make Task 1 GREEN</name>
  <files>accrue/lib/accrue/entitlements/admin.ex</files>
  <read_first>
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (the two @doc false delegations added in Task 2)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ NEW admin.ex — exact module + moduledoc + @spec to clone)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (Pattern 1 — why candidate iii; A2 hard-code LocalMap, document the limitation in moduledoc)
    - accrue/lib/accrue/entitlements.ex (mirror its moduledoc-then-alias header SHAPE only — do NOT extend that module)
  </read_first>
  <action>
    Create `accrue/lib/accrue/entitlements/admin.ex` defining `Accrue.Entitlements.Admin`. Moduledoc states: internal read-only diagnostic seam for the accrue_admin entitlements tab (ENT-11); NOT a public gate API (no boolean entitled?-style surface; Phase 123 D-07 fetch_entitled/2 stays deferred); one-way dependency admin -> billing; hard-codes the default `LocalMap` resolver (custom resolvers are out of scope for this read-only diagnostic — A2 limitation, documented here). `alias Accrue.Entitlements.Resolver.LocalMap`. Define one public function `resolve_for_customer/1` with `@spec resolve_for_customer(Accrue.Billing.Customer.t()) :: {resolved :: map(), unmapped_price_ids :: [String.t()]}`, matching `%Accrue.Billing.Customer{} = customer`, returning `{LocalMap.fold_for_customer(customer), LocalMap.unmapped_entitling_price_ids(customer)}`. This makes the Task 1 test GREEN. Do NOT add a boolean/gate function, do NOT touch `Accrue.Entitlements`, and keep the module dependency one-way (it calls into billing/entitlements core; nothing in billing calls it).
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/admin_test.exs 2>&1 | tail -3 | grep -qE '0 failures' && grep -q 'NOT a public gate API\|no boolean' lib/accrue/entitlements/admin.ex && echo "GREEN + posture-documented"</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements/admin.ex` defines `Accrue.Entitlements.Admin.resolve_for_customer/1` returning `{resolved, unmapped}`.
    - The function body calls `LocalMap.fold_for_customer/1` and `LocalMap.unmapped_entitling_price_ids/1` (no re-implemented fold; verifiable by grep for `LocalMap.fold_for_customer`).
    - `Accrue.Entitlements` (the public gate API) gained NO new function (one-way / no-public-gate-API constraint).
    - `cd accrue && mix test test/accrue/entitlements/admin_test.exs` → 0 failures (Task 1 now GREEN).
    - The moduledoc explicitly states it is not a public gate API and hard-codes LocalMap (A2).
  </acceptance_criteria>
  <done>Accrue.Entitlements.Admin.resolve_for_customer/1 exists, delegates to the two LocalMap @doc false seam fns, returns {resolved, unmapped}, the unit test is GREEN, and the no-public-gate-API + one-way-dependency + LocalMap-hardcode posture is documented in the moduledoc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accrue_admin LiveView → accrue core (Plan 02 consumer of this seam) | Admin holds an already-owner-scoped %Customer{}; calls resolve_for_customer/1. No untrusted input crosses here in THIS plan (the seam is pure functions over a Customer struct + app config). |
| Public package API surface | The seam must NOT leak into the published `Accrue.*` gate API — @doc false + a separate module keep it off the docs surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Elevation of Privilege | A new public `Accrue.*` function could be misused as a fail-open gate (the deferred fetch_entitled/2 trap, D-07) | mitigate | Seam lives in a separate internal `Accrue.Entitlements.Admin` module; returns a `{map, list}` (never a boolean); the two LocalMap helpers are `@doc false`. Acceptance criteria assert `Accrue.Entitlements` gained no function and the moduledoc states "NOT a public gate API". |
| T-126-02 | Tampering | Two divergent copies of the resolver fold (admin copy drifting from the SSOT) silently granting/denying differently | mitigate | `fold_for_customer/1` literally calls `fold_active/1` (single fold). Acceptance criterion greps `def fold_for_customer` + next line for `fold_active`. PITFALLS #2 SSOT discipline. |
| T-126-03 | Information Disclosure | Resolved map containing PII | accept | The resolved map contains only plan atoms, feature atoms, and integer quantities — no customer email/name/Stripe secret. The seam returns `resolved` (entitlement state), not the `%Customer{}`. No PII to disclose. |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (RESEARCH § Package Legitimacy Audit: N/A — no mix.exs dependency edits). No supply-chain surface. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/entitlements/admin_test.exs` → 0 failures (the seam contract).
- `cd accrue && mix compile --warnings-as-errors` → clean (no unused-private from the new delegations).
- `grep 'def ' accrue/lib/accrue/entitlements.ex` shows the SAME public function set as before this plan (no new gate API).
- One-way dependency intact: nothing under `accrue/lib/accrue/billing/` or the resolver references `Accrue.Entitlements.Admin`.
</verification>

<success_criteria>
- `Accrue.Entitlements.Admin.resolve_for_customer/1` returns `{resolved_map, unmapped_price_ids}` for mapped, unmapped, empty, and grace customers (ENT-11 read path).
- Zero fold drift: `fold_for_customer/1` delegates to `fold_active/1`; the admin seam never re-implements resolution.
- No new public `Accrue.*` gate/diagnostic API; `fetch_entitled/2` (D-07) stays deferred.
- Wave 0 test `admin_test.exs` is GREEN and is the contract Plan 02's LiveView relies on.
</success_criteria>

<output>
Create `.planning/phases/126-admin-surface-docs-jtbd-spine/126-01-SUMMARY.md` when done.
</output>

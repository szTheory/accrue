---
phase: 126-admin-surface-docs-jtbd-spine
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - accrue_admin/lib/accrue_admin/copy/entitlements.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
  - accrue_admin/lib/accrue_admin/live/customer_live.ex
  - accrue_admin/test/accrue_admin/live/entitlements_live_test.exs
autonomous: true
requirements: [ENT-11]
must_haves:
  truths:
    - "An operator can open /customers/:id?tab=entitlements and see the customer's resolved active plans, granted features, quantities, and grace state (D-01/D-02)"
    - "An active subscription whose price_id has no plan mapping is badged '⚠ Unmapped plan' so drift is visible by eye (D-03)"
    - "When the customer has no entitling subscription, a Copy-backed empty state renders (no crash) (D-05)"
    - "Every operator-facing string flows through AccrueAdmin.Copy.Entitlements -> defdelegate -> export allowlist; nothing is hardcoded in the template (D-05/VERIFY-01)"
    - "The tab inherits CustomerLive's AuthHook/owner_scope mount — no new route, no new auth surface (security)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy/entitlements.ex"
      provides: "AccrueAdmin.Copy.Entitlements — one @doc false 0-arity fn per operator string (UI-SPEC Copywriting Contract)"
      contains: "entitlements_unmapped_badge"
    - path: "accrue_admin/lib/accrue_admin/live/customer_live.ex"
      provides: "entitlements added to @tabs + a case @tab \"entitlements\" render clause + StatusBadge alias"
      contains: "\"entitlements\""
    - path: "accrue_admin/test/accrue_admin/live/entitlements_live_test.exs"
      provides: "Wave 0 LiveView test — resolved features render, unmapped badge, empty state"
      contains: "tab=entitlements"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/customer_live.ex (entitlements clause)"
      to: "Accrue.Entitlements.Admin.resolve_for_customer/1"
      via: "private helper entitlements_view/1 calling the seam once"
      pattern: "Accrue\\.Entitlements\\.Admin\\.resolve_for_customer"
    - from: "accrue_admin/lib/accrue_admin/copy.ex"
      to: "AccrueAdmin.Copy.Entitlements"
      via: "defdelegate entitlements_*(), to: Entitlements, as: :<shortname>"
      pattern: "defdelegate entitlements_.*to: Entitlements"
    - from: "accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex (@allowlist)"
      to: "AccrueAdmin.Copy.entitlements_* 0-arity fns"
      via: "allowlist entries (export emits only allowlisted 0-arity Copy fns)"
      pattern: "entitlements_"
---

<objective>
Add the read-only entitlements TAB to `AccrueAdmin.Live.CustomerLive` (`/customers/:id?tab=entitlements`) — a mechanical clone of the existing `payment_methods` tab clause. It renders resolved state first (active plans / features / quantities / grace), then the drift signal (a "⚠ Unmapped plan" badge on entitling subs not in the reverse-index), using ONLY existing components. All operator strings go through a new `AccrueAdmin.Copy.Entitlements` submodule (VERIFY-01).

Purpose: Satisfy ENT-11 SC#1 — an operator views a customer's currently-active entitlements in `accrue_admin` and spots unmapped-plan drift by eye. The tab reuses CustomerLive's mount / `owner_scope` / `AuthHook` / breadcrumb spine for free (D-01) and calls the Plan 01 read seam (no re-derivation; one-way dependency).
Output: new `copy/entitlements.ex`, `copy.ex` defdelegates, export-task allowlist entries, the CustomerLive tab clause + `@tabs` entry + `StatusBadge` alias, and the Wave 0 LiveView test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-CONTEXT.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-UI-SPEC.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-VALIDATION.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-01-SUMMARY.md

<interfaces>
<!-- Read seam (from Plan 01) + component signatures the tab consumes. Use directly — no exploration. -->

From accrue/lib/accrue/entitlements/admin.ex (Plan 01 output):
- Accrue.Entitlements.Admin.resolve_for_customer(%Customer{}) :: {resolved :: map(), unmapped_price_ids :: [String.t()]}
  resolved keys: :plan, :active_plans (MapSet of atoms), :features (MapSet of atoms), :quantities (%{atom=>int}), :grace_plans, :grace_features, :expired_grace_plans (all MapSets)

Component signatures (verified, 126-PATTERNS.md / 126-UI-SPEC.md):
- StatusBadge.status_badge: attr :status (required, :any), :label (:string, default nil), :tone (:string, default nil). Tones: "moss" active plans, "amber" grace + unmapped badge, "slate" expired/neutral. :past_due/:grace_period auto-map to "amber". Unknown-atom fallback "ink".
- KpiCard.kpi_card: attr :label (required), :value (required, :string — MUST be string via Integer.to_string/1), :delta, :delta_tone (default "slate"), slot :meta.
- JsonViewer.json_viewer: attr :id (required), :payload (required, :any), :label. PITFALL: MapSet renders as %{"__struct__" => "MapSet"} — build a plain display map (MapSets -> sorted lists) BEFORE passing.
- Tabs.tabs: count badge renders only :if={tab[:count]} (tabs.ex:21) — a nil/absent count = no badge (safe to omit the entitlements count).

CustomerLive edit anchors (accrue_admin/lib/accrue_admin/live/customer_live.ex — verify current line numbers before editing):
- @tabs ~w(subscriptions invoices charges payment_methods events metadata) at :31 — add "entitlements" (drives normalize_tab/1 :670 + tabs/4 :410)
- alias block :17-27 — StatusBadge NOT yet aliased; add it (KpiCard/JsonViewer/Tabs already aliased)
- case @tab do at :223 — add a "entitlements" -> clause (clone the "payment_methods" clause :264-354 shape; "metadata" clause :366 shows bare JsonViewer)
- tab_counts/1 at :387 — OMIT an :entitlements key (D-01; missing key -> Map.get returns nil -> no badge; :entitlements atom already exists so String.to_existing_atom won't raise)
- private helper idiom: subscriptions(@customer) at :424 — clone as entitlements_view(@customer) returning {resolved, unmapped}
- empty-state copy idiom: Copy.customer_detail_no_subscriptions() (copy.ex:430) used at customer_live.ex:236
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccrueAdmin.Copy.Entitlements submodule + defdelegates + export allowlist (VERIFY-01 three-part contract)</name>
  <files>accrue_admin/lib/accrue_admin/copy/entitlements.ex, accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex (exact clone target — @moduledoc false + one @doc false 0-arity fn per string)
    - accrue_admin/lib/accrue_admin/copy.ex (the CustomerPaymentMethods defdelegate block :434-499 + the alias block :9-15)
    - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex (@allowlist :18-60; export loop :80-84 emits only 0-arity fns in BOTH allowlist and Copy.__info__(:functions))
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-UI-SPEC.md (Copywriting Contract :113-134 — the exact copy strings + proposed fn names; fail-closed error-state honesty requirement)
  </read_first>
  <action>
    Create `accrue_admin/lib/accrue_admin/copy/entitlements.ex` defining `AccrueAdmin.Copy.Entitlements` with `@moduledoc false`, cloning `customer_payment_methods.ex`'s pattern: one `@doc false` 0-arity function per operator string from the UI-SPEC Copywriting Contract. Author exactly these functions returning the UI-SPEC verbatim copy: `entitlements_section_title/0` ("Active entitlements"), `entitlements_drift_section_title/0` ("Plan mapping"), `entitlements_active_plans_label/0` ("Active plans"), `entitlements_features_label/0` ("Granted features"), `entitlements_quantities_label/0` ("Seats & limits"), `entitlements_grace_label/0` ("In grace period"), `entitlements_unmapped_badge/0` ("⚠ Unmapped plan"), `entitlements_unmapped_hint/0` ("This subscription's price isn't in your :plans config, so the resolver drops it."), `entitlements_empty_title/0` ("No active entitlements"), `entitlements_empty_copy/0` ("This customer has no entitling subscriptions, so no plans or features are currently granted."), `entitlements_no_drift_copy/0` ("All active subscriptions map to a configured plan."), `entitlements_raw_map_label/0` ("Show resolved map"), `entitlements_error_copy/0` (the fail-closed honesty string from UI-SPEC :129 — MUST state "no access is granted on error"). In `copy.ex`: add `alias AccrueAdmin.Copy.Entitlements` to the alias block, and add one `defdelegate entitlements_<name>(), to: Entitlements, as: :entitlements_<name>` per fn (clone the CustomerPaymentMethods block exactly; note the submodule fn names already carry the `entitlements_` prefix here, so `as:` is the same name — keep it explicit and 0-arity so the export loop picks them up). In the export task: add every new `entitlements_*` fn name to the `~w( ... )a` `@allowlist` alongside the `customer_payment_methods_*` entries.
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -2 && grep -q 'def entitlements_unmapped_badge' lib/accrue_admin/copy/entitlements.ex && grep -q 'defdelegate entitlements_unmapped_badge' lib/accrue_admin/copy.ex && grep -q 'entitlements_unmapped_badge' lib/mix/tasks/accrue_admin.export_copy_strings.ex && echo "three-part-contract-present"</automated>
  </verify>
  <acceptance_criteria>
    - `copy/entitlements.ex` defines `AccrueAdmin.Copy.Entitlements` with one `@doc false` 0-arity fn per UI-SPEC Copywriting Contract row, returning the verbatim strings (including "⚠ Unmapped plan" and the fail-closed error copy stating no access is granted).
    - `copy.ex` aliases `Entitlements` and has a `defdelegate entitlements_*()` for each fn.
    - The export task `@allowlist` contains every new `entitlements_*` fn name.
    - `cd accrue_admin && mix compile --warnings-as-errors` succeeds.
    - The three-part-contract grep (submodule fn + defdelegate + allowlist for `entitlements_unmapped_badge`) passes.
  </acceptance_criteria>
  <done>Every operator string for the tab exists once in Copy.Entitlements, is reachable via Copy.entitlements_*/0 defdelegates, and is in the export allowlist — zero hardcoded strings will be needed in Task 2's template.</done>
</task>

<task type="auto">
  <name>Task 2: CustomerLive entitlements tab — @tabs entry + StatusBadge alias + case @tab clause calling the read seam</name>
  <files>accrue_admin/lib/accrue_admin/live/customer_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/customer_live.ex (the "payment_methods" clause :264-354 — richest clone target; "metadata" clause :366 — bare JsonViewer; @tabs :31; alias block :17-27; subscriptions/1 helper :424; empty-state use :236)
    - accrue/lib/accrue/entitlements/admin.ex (Plan 01 — resolve_for_customer/1 returning {resolved, unmapped})
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ MOD customer_live.ex — clause skeleton, component call idioms, the MapSet->display-map conversion, tab_counts OMIT guidance)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-UI-SPEC.md (component tones, structural classes ax-card/ax-heading/ax-list-row/ax-body, drift-signal amber rule, drift self-explaining hint requirement)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (Pitfall 2 JsonViewer mangles MapSets; Pitfall 5 @tabs-without-count is safe)
  </read_first>
  <action>
    Edit `accrue_admin/lib/accrue_admin/live/customer_live.ex` (confirm current line numbers first — file may have shifted):
    1. Add `entitlements` to the `@tabs` sigil-w list (place after `payment_methods`): `~w(subscriptions invoices charges payment_methods entitlements events metadata)`.
    2. Add `alias AccrueAdmin.Components.StatusBadge` to the alias block (KpiCard/JsonViewer/Tabs already aliased; StatusBadge is NOT).
    3. Add a private helper `entitlements_view(customer)` that calls `Accrue.Entitlements.Admin.resolve_for_customer(customer)` ONCE and returns `{resolved, unmapped}` (clone the `subscriptions/1` helper idiom). Add a private `entitlements_display_map(resolved)` that converts each MapSet to a sorted list (`MapSet.to_list/1 |> Enum.sort/1`) for `:active_plans`, `:features`, `:grace_plans`, `:grace_features`, `:expired_grace_plans` and passes `:quantities` through — to dodge the JsonViewer MapSet mangling (Pitfall 2).
    4. Add a `<% "entitlements" -> %>` clause to the `case @tab do` block, cloning the `payment_methods` clause structure (`<section class="ax-card">`, `ax-heading`, `:for` list rows, `:if` empty state). Render in this order (D-02 resolved-first, then drift):
       - Active plans: `:for` over `resolved.active_plans` (sorted) as `StatusBadge.status_badge status={plan} tone="moss"`, under a `Copy.entitlements_active_plans_label()` heading.
       - Granted features: `:for` over `resolved.features` (sorted) as list rows/badges, under `Copy.entitlements_features_label()`.
       - Quantities/limits: `:for` over `resolved.quantities`, each a `KpiCard.kpi_card label={to_string(key)} value={Integer.to_string(count)}`, under `Copy.entitlements_quantities_label()`.
       - Grace section: render ONLY `:if` any of grace_plans/grace_features/expired_grace_plans is non-empty (avoid noise when past_due_grace: :none default), amber StatusBadges, under `Copy.entitlements_grace_label()`.
       - Drift section (`Copy.entitlements_drift_section_title()`): `:for` each `price_id <- unmapped` render a list row with `StatusBadge.status_badge status={:unmapped} label={Copy.entitlements_unmapped_badge()} tone="amber"` plus an `ax-muted` line `Copy.entitlements_unmapped_hint()` (drift must be self-explaining per UI-SPEC :134). When `unmapped == []`, optionally show `Copy.entitlements_no_drift_copy()`.
       - Empty state: `:if` `MapSet.size(resolved.active_plans) == 0 and unmapped == []` render `Copy.entitlements_empty_title()` + `Copy.entitlements_empty_copy()` (mirror the no-subscriptions empty-state idiom at :236).
       - Raw map: `JsonViewer.json_viewer id="customer-entitlements" label={Copy.entitlements_raw_map_label()} payload={entitlements_display_map(resolved)}` (the converted display map, never the raw MapSet map).
    Use ONLY existing components (KpiCard, StatusBadge, JsonViewer) and existing structural classes (ax-card, ax-heading, ax-list-row, ax-body, ax-muted, ax-eyebrow) per UI-SPEC — zero new components, zero new CSS, zero raw hex. Do NOT add an `:entitlements` key to `tab_counts/1` (D-01 — omit the count). NEVER hardcode an operator string — every label/body/badge text comes from `Copy.entitlements_*` (Task 1).
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -3 && grep -q '~w(subscriptions invoices charges payment_methods entitlements' lib/accrue_admin/live/customer_live.ex && grep -q 'alias AccrueAdmin.Components.StatusBadge' lib/accrue_admin/live/customer_live.ex && grep -q 'Accrue.Entitlements.Admin.resolve_for_customer' lib/accrue_admin/live/customer_live.ex && echo "tab-wired"</automated>
  </verify>
  <acceptance_criteria>
    - `@tabs` includes `entitlements` (between payment_methods and events).
    - `alias AccrueAdmin.Components.StatusBadge` is present.
    - A `case @tab do` clause for `"entitlements"` exists and calls `Accrue.Entitlements.Admin.resolve_for_customer/1` exactly once (via the private helper).
    - The render passes a MapSet-free display map to JsonViewer (grep shows `entitlements_display_map` or an inline MapSet.to_list conversion in the JsonViewer payload).
    - No `:entitlements` key was added to `tab_counts/1`.
    - No hardcoded operator string in the new clause (every label/badge/empty/hint references `Copy.entitlements_*`).
    - `cd accrue_admin && mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>The entitlements tab renders resolved plans/features/quantities/grace then the unmapped-drift badge, reuses only existing components/classes, calls the seam once, dodges the JsonViewer MapSet trap, and uses Copy-backed strings throughout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 LiveView test — three render states (resolved features, unmapped badge, empty)</name>
  <files>accrue_admin/test/accrue_admin/live/entitlements_live_test.exs</files>
  <read_first>
    - accrue_admin/test/accrue_admin/live/customer_live_test.exs (clone target — AuthAdapter + setup :1-47, LiveView assertion idiom :162-184, owner-scope tests :197-230)
    - accrue/test/accrue/entitlements/local_map_test.exs (the :entitlements config-mutation setup :28-49 to compose into setup)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ NEW entitlements_live_test.exs — AuthAdapter clone, setup, assertion idiom, three render states)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (Pitfall 1 — "price_basic" factory default is the free unmapped fixture; Code Examples test scaffolding :364-394)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-UI-SPEC.md (the exact Copy strings to assert on: "⚠ Unmapped plan", empty-state copy)
  </read_first>
  <behavior>
    - Test 1 (resolved features render): a customer with an entitling sub on a MAPPED price (e.g. "price_pro" mapped to features [:reports]) → loading `?tab=entitlements` returns html containing the granted feature ("reports") and the active plan.
    - Test 2 (unmapped badge): a customer with an entitling sub on the factory default "price_basic" (NOT in the test :entitlements config) → html contains the "⚠ Unmapped plan" badge text (Copy.entitlements_unmapped_badge()).
    - Test 3 (empty state): a bare Factory.customer with NO subscription → html contains the empty-state copy (Copy.entitlements_empty_copy()), and the LiveView mounts without crashing.
  </behavior>
  <action>
    Create `accrue_admin/test/accrue_admin/live/entitlements_live_test.exs` cloning `customer_live_test.exs`. `use AccrueAdmin.LiveCase, async: false` (async:false REQUIRED — mutates app env). Define the `AuthAdapter` (clone :1-34) and a `setup` that: saves/sets `Application.put_env(:accrue, :auth_adapter, AuthAdapter)` with on_exit restore; saves/sets `Application.put_env(:accrue, :entitlements, ...)` with at least `pro: [features: [:reports], limits: [seats: 5], price_ids: ["price_pro"]]` + `unmapped_action: :deny` and on_exit restore (compose from local_map_test.exs:36-49). Drive subscriptions with `Billing.subscribe/2-3` + `Fake.transition(sub.processor_id, :active, synthesize_webhooks: false)`. Assertions use `Phoenix.ConnTest.init_test_session(conn, admin_token: "admin")` then `live(conn, "/billing/customers/#{customer.id}?tab=entitlements")` and `assert html =~ ...` (confirm the exact route prefix from customer_live_test.exs — it uses `/billing/customers/`). Assert against the Copy strings (import or reference `AccrueAdmin.Copy` rather than hardcoding, OR assert the verbatim UI-SPEC text) for the unmapped badge and empty state. Cover the three <behavior> states. The test depends on Plan 01's seam and Task 2's tab being wired — it should be GREEN once both land.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/entitlements_live_test.exs 2>&1 | tail -3 | grep -qE '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue_admin/test/accrue_admin/live/entitlements_live_test.exs` exists, `use AccrueAdmin.LiveCase, async: false`.
    - Setup mutates both `:auth_adapter` and `:entitlements` app env with on_exit restore.
    - Three tests cover: resolved feature renders, "⚠ Unmapped plan" badge appears for a "price_basic" sub, empty state renders for a no-sub customer.
    - `cd accrue_admin && mix test test/accrue_admin/live/entitlements_live_test.exs` → 0 failures.
  </acceptance_criteria>
  <done>The LiveView test exercises the three ENT-11 render states against the wired tab + seam and is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → accrue_admin LiveView | The only untrusted input is the `tab` query param; `normalize_tab/1` allowlists against `@tabs` (adding "entitlements" is the sole validation change). |
| accrue_admin → accrue core | The tab calls `Accrue.Entitlements.Admin.resolve_for_customer/1` with an already-owner-scoped %Customer{} from mount; no bypass of `Customers.detail/2`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-04 | Information Disclosure / Elevation | Cross-tenant entitlement view (seeing another owner's customer) | mitigate | The tab inherits CustomerLive's `AuthHook` on_mount + `owner_scope` via `Customers.detail/2` (out-of-scope customers already redirect before render, tested customer_live_test.exs:197-230). No new route, no new query bypassing the owner-validated mount. The read seam queries by the already-validated `customer.id`. |
| T-126-05 | Information Disclosure | PII leak in the resolved-map JsonViewer render | mitigate | The JsonViewer payload is the resolved entitlement map (plan/feature atoms + integer quantities), NOT the %Customer{} — no email/name/Stripe secret. Acceptance asserts the payload is `entitlements_display_map(resolved)`, not the customer. The telemetry subject_id PII concern is not rendered here (telemetry-only, core). |
| T-126-06 | Tampering (operational misread) | The "⚠ Unmapped plan" drift badge mistaken for an enforcement action | mitigate | Copy is read-only and self-explaining: `entitlements_unmapped_hint/0` states the resolver drops the price; no grant/revoke/edit control exists in this phase. UI-SPEC Destructive = none. |
| T-126-07 | Input Validation | A crafted `tab` value | accept | `normalize_tab/1` defaults non-allowlisted tab values to "subscriptions"; `String.to_existing_atom("entitlements")` is safe (atom pre-exists as a config key). No new attack surface. |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface. |
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/entitlements_live_test.exs` → 0 failures (the three render states).
- `cd accrue_admin && mix compile --warnings-as-errors` → clean.
- `mix accrue_admin.export_copy_strings` (or its test) succeeds and includes the new `entitlements_*` strings (VERIFY-01).
- Visual coherence (fail-closed error copy reads correctly, drift hint legible) is the one manual check — covered by the LiveView test asserting the Copy strings render.
</verification>

<success_criteria>
- An operator viewing `/customers/:id?tab=entitlements` sees the customer's resolved active plans, granted features, quantities, and (when present) grace state (ENT-11 SC#1).
- Active subscriptions whose `price_id` is not in the plan reverse-index are badged "⚠ Unmapped plan" with a self-explaining hint (D-03 drift-by-eye).
- The empty/no-entitlements state renders Copy-backed text without crashing.
- All operator strings flow through `AccrueAdmin.Copy.Entitlements` → defdelegate → export allowlist (VERIFY-01); zero hardcoded strings.
- Only existing components/classes used; no new component, CSS, route, or auth surface.
</success_criteria>

<output>
Create `.planning/phases/126-admin-surface-docs-jtbd-spine/126-02-SUMMARY.md` when done.
</output>

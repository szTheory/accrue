---
phase: 126-admin-surface-docs-jtbd-spine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/entitlements.md
  - accrue/guides/jobs_to_be_done.md
  - accrue/README.md
  - accrue/guides/quickstart.md
  - .planning/research/JTBD-FRONTIER.md
  - .planning/PROJECT.md
autonomous: true
requirements: [ENT-12]
must_haves:
  truths:
    - "A reader of guides/entitlements.md learns the full story fail-closed-first: gate API, config, Plug guard, LiveView guard, lifecycle truth (linked SSOT), provider matrix (linked SSOT), telemetry (D-06/D-07/D-08)"
    - "The public JTBD doc flips entitlements ⛔→✅ with honest phrasing (core shipped / optional Stripe-native sync deferred off-by-default Phase 127) (D-09/D-10)"
    - "The internal JTBD-FRONTIER.md mirror flips Gap->Shipped, TL;DR, delta-table, and '5 of 6'->'6 of 6'; both files get a dated Update-log entry (D-11)"
    - "README 'Start here' + quickstart focused-guides each gain ONE entitlements pointer; First Hour's pinned spine is untouched (D-12)"
    - "PROJECT.md gains the literal 'gateway subscription core' phrase, restoring SSOT parity and clearing the pre-existing verifier RED holdout (D-13)"
    - "guides/jobs_to_be_done.md is committed this phase so a clean-checkout CI can see the needle/seed target (Pitfall 3)"
  artifacts:
    - path: "accrue/guides/entitlements.md"
      provides: "The authoritative entitlements guide; contains needles entitled?, Accrue.Plug.RequireEntitlement, [:accrue, :entitlements, :check]"
      contains: "entitled?"
      min_lines: 80
    - path: "accrue/guides/jobs_to_be_done.md"
      provides: "New '## Gate access on what they paid for' body section + flipped scope prose/row + reworded Update log + dated entry"
      contains: "Gate access on what they paid for"
    - path: "accrue/README.md"
      provides: "A '[Entitlements](guides/entitlements.md)' bullet in 'Start here'"
      contains: "[Entitlements](guides/entitlements.md)"
    - path: "accrue/guides/quickstart.md"
      provides: "A '[Entitlements](entitlements.md)' bullet in the focused-guides list"
      contains: "[Entitlements](entitlements.md)"
    - path: ".planning/PROJECT.md"
      provides: "The literal 'gateway subscription core' phrase in the PROC-08 posture prose (D-13)"
      contains: "gateway subscription core"
    - path: ".planning/research/JTBD-FRONTIER.md"
      provides: "Internal flip: entitlements Gap->Shipped, '6 of 6 shipped', dated Update log"
      contains: "6 of 6"
  key_links:
    - from: "accrue/guides/entitlements.md (## Lifecycle truth)"
      to: "accrue/guides/lifecycle_semantics.md#lifecycle--entitlement-truth-table"
      via: "summarize-and-link (inline ~5 rows + canonical-source link)"
      pattern: "lifecycle_semantics.md#lifecycle--entitlement-truth-table"
    - from: "accrue/README.md (Start here)"
      to: "accrue/guides/entitlements.md"
      via: "[Entitlements](guides/entitlements.md) markdown link"
      pattern: "\\[Entitlements\\]\\(guides/entitlements.md\\)"
    - from: "accrue/guides/jobs_to_be_done.md (Gate section)"
      to: "accrue/guides/entitlements.md"
      via: "Deep-dive link [Entitlements](entitlements.md)"
      pattern: "\\[Entitlements\\]\\(entitlements.md\\)"
---

<objective>
Author the entitlements documentation content for ENT-12 SC#2/#3: the new `guides/entitlements.md` (fail-closed-first, summarize-and-link to SSOTs), the JTBD ⛔→✅ flip in BOTH the public `jobs_to_be_done.md` and the internal `JTBD-FRONTIER.md`, the minimal README/quickstart "Start here" spine pointers, and the pre-existing `gateway subscription core` needle fix in PROJECT.md (D-13) that the verifier requires.

Purpose: Make the shipped entitlements feature discoverable and documented end-to-end without re-deriving any SSOT truth (lifecycle truth table, provider matrix). This plan produces the doc FILES; Plan 04 adds the verifier needles + seed fixtures that pin them. Committing `jobs_to_be_done.md` (currently untracked) is part of this plan so CI can see it.
Output: one new guide, four edited docs, the internal frontier mirror, and the PROJECT.md parity fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-CONTEXT.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-VALIDATION.md

# SSOTs to summarize-and-link (NEVER re-derive truth):
@accrue/guides/lifecycle_semantics.md
@accrue/guides/connect.md
@accrue/guides/webhooks.md

<interfaces>
<!-- Verbatim anchors, needle strings, and flip targets. Use directly. Re-read on-disk files before editing — README/quickstart have uncommitted edits, jobs_to_be_done.md is untracked, and line numbers may shift. -->

Guide module/event names that MUST appear verbatim in entitlements.md (needles, Plan 04 pins them):
- entitled?                              (gate-fn anchor; in the fail-closed opener)
- Accrue.Plug.RequireEntitlement         (Plug enforcement anchor; in "Gate a controller route")
- [:accrue, :entitlements, :check]       (telemetry event; pinned ONCE here only — mirrors first_hour.md pinning [:accrue, :billing, :checkout_session, :create])

Guide section order (D-06 Option A — mirror connect.md's shape):
1. # Entitlements — gate features on what they paid for
2. Fail-closed easy path FIRST: if Accrue.entitled?(user, :pro), do: render_pro(), else: upsell()  (PITFALLS #1)
3. ## Configure the catalog — plans / unmapped_action: :deny / past_due_grace
4. ## Gate a controller route — Accrue.Plug.RequireEntitlement + require_feature/1 / require_plan/1 router macros (router.ex:79,93), opaque-403 default + on_deny/deny_path
5. ## Gate a LiveView — Accrue.Live.Entitlements on_mount({:require_feature,..}) / ({:require_plan,..}) (live/entitlements.ex:104,108), conditionally compiled
6. ## Lifecycle truth — inline ~5 rows (trialing ✅ / active ✅ / active+cancel_at_period_end ✅ / paused ✗ / past_due ✗-default-✅-in-grace / canceled ✗) then link lifecycle_semantics.md#lifecycle--entitlement-truth-table (anchor VERIFIED at lifecycle_semantics.md:173)
7. ## Provider honesty — prose only ("local-identical across Stripe/Braintree/Fake — reads local subscription state, never the processor"), link Accrue.Processor.Capabilities (NOT the .planning/ matrix — it is internal-only, research A1)
8. ## Telemetry — [:accrue, :entitlements, :check] start/stop/exception metadata; subject_id internal-only never PII; per-check telemetry-only never ledgered
9. ## Related guides — links OUT to lifecycle_semantics.md, telemetry.md, auth_adapters.md, the admin tab
Length ≤ connect.md (~414 lines). No mix.exs edit (guides auto-glob).

jobs_to_be_done.md flip targets (on-disk; re-read first):
- New body section "## Gate access on what they paid for" BETWEEN "## The customer changes their mind" (:116) and "## When payments fail" (:156). Clone body-section shape: bold The-job line -> prose -> elixir snippet -> "In admin:" pointer (the new tab) -> "Deep dive:" [Entitlements](entitlements.md).
- Scope-and-maturity prose flip (:339): the exact phrase "on the table** is **entitlements" MUST be DROPPED (it is the Plan 04 require_absent_regex flip-guard target). Replace with honest "core entitlements ✅ shipped — gate API, Plug + LiveView guards, provider-honest, lifecycle-truthful; optional Stripe-native sync is deferred, off-by-default (Phase 127)".
- Scope row ⛔→✅.
- Update-log line 354: currently "...entitlements flagged as the headline gap." -> REWORD to drop "headline gap" (Pitfall 4 belt-and-suspenders) AND append a new dated entry containing a distinctive post-flip shipped marker (e.g. "entitlements ✅") — Plan 04 pins that exact marker (needle 5a).

JTBD-FRONTIER.md flip targets (D-11):
- TL;DR "exactly one item" (:21) / "spend it on entitlements" (:23) -> "6 of 6 shipped"
- Coverage-map gap row (:85): entitlements ⛔ row -> move to ✅ Shipped with deferred-sync note
- Delta-table row (:110): flip Accrue cell ⛔→✅
- Diminishing-returns prose (:118/:125/:142/:154/:158): rewrite "the one capability Accrue lacks" / "the last high-value point" / "highest-leverage thing left"
- Update log (:160-162): append a dated entry; re-verify cells against current code

PROJECT.md D-13 (verifier RED holdout — phrase confirmed MISSING on disk):
- Line ~37, PROC-08 posture prose: "...a bounded dual-provider core centered on **Stripe-first** defaults plus one Stripe-like gateway." -> reword to include the literal "gateway subscription core", e.g. "...a bounded dual-provider **gateway subscription core** centered on **Stripe-first** defaults plus one Stripe-like gateway." (grep -F match for verify_package_docs.sh:220).

README "Start here" bullet shape (README.md:18): "- [Webhooks](guides/webhooks.md) — signing, retries, and operational notes." Add ONE peer bullet with the EXACT link text "[Entitlements](guides/entitlements.md)" (Plan 04 needle 1 pins this byte-for-byte).

quickstart focused-guides bullet: add "[Entitlements](entitlements.md)" (relative, no guides/ prefix). Plan 04 needle 6 pins this exact text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/entitlements.md (fail-closed-first, summarize-and-link, all 3 needles present)</name>
  <files>accrue/guides/entitlements.md</files>
  <read_first>
    - accrue/guides/connect.md (the peer skeleton: # Title -> opening prose + blockquote -> ## Getting Started -> topic ## sections w/ fenced snippets -> ## Pitfalls -> ## Related guides -> ## References)
    - accrue/guides/webhooks.md (the "defer truth, own the how" SSOT-defer voice :1-6)
    - accrue/guides/lifecycle_semantics.md (the truth-table SSOT to link; confirm the anchor "## Lifecycle → entitlement truth table" :173 and the ~5 rows to inline verbatim :182-191; ### entitling glossary :162)
    - accrue/lib/accrue/entitlements.ex (the fail-closed contract moduledoc :1-22; the [:accrue, :entitlements, :check] span + metadata :32-47 — for the Telemetry section)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ NEW entitlements.md — section order + load-bearing needle anchors)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (Pattern 3 — full skeleton; A1 link Processor.Capabilities not the .planning matrix)
  </read_first>
  <action>
    Create `accrue/guides/entitlements.md` mirroring `connect.md`'s skeleton and `webhooks.md`'s defer-truth voice. Follow the 9-section order in the interfaces block. Section 2 leads with the fail-closed easy path snippet `if Accrue.entitled?(user, :pro), do: render_pro(), else: upsell()` and states the only path to `true` is an affirmative resolved match (nil/error/unmapped/exception all resolve to false) — this satisfies needle #2 (`entitled?`). Section 4 ("Gate a controller route") MUST contain the literal `Accrue.Plug.RequireEntitlement` (needle #3) plus the `require_feature`/`require_plan` router macros and the opaque-403 default + `on_deny`/`deny_path`. Section 8 ("Telemetry") MUST contain the literal `[:accrue, :entitlements, :check]` (needle #4, pinned ONCE here only) with its metadata keys and the "subject_id internal-only never PII; per-check telemetry-only never ledgered" note. Section 6 ("Lifecycle truth") inlines ONLY the ~5 reader-critical rows verbatim from `lifecycle_semantics.md:182-191` then defers: "Canonical source: [`lifecycle_semantics.md#lifecycle--entitlement-truth-table`](lifecycle_semantics.md#lifecycle--entitlement-truth-table) — `entitling?/1` is the SSOT." Keep the grace footnote nuance (past_due_since, Accrue.Clock, :past_due_grace/:past_due_expired reasons, :unpaid exclusion) OUT of this guide — link the SSOT only (D-07). Section 7 ("Provider honesty") is PROSE ONLY (no table): "Entitlement resolution is local-identical across Stripe/Braintree/Fake — it reads local subscription state, never the processor," linking `Accrue.Processor.Capabilities` (A1 — do NOT link the internal `.planning/processor-support-matrix.md`). Section 9 links OUT to lifecycle_semantics.md, telemetry.md, auth_adapters.md, and the admin tab (D-08 hub-and-spoke; SSOTs do not link back for truth). One runnable snippet per topic; length ≤ connect.md (~414 lines). No mix.exs edit.
  </action>
  <verify>
    <automated>cd accrue && grep -Fq 'entitled?' guides/entitlements.md && grep -Fq 'Accrue.Plug.RequireEntitlement' guides/entitlements.md && grep -Fq '[:accrue, :entitlements, :check]' guides/entitlements.md && grep -Fq 'lifecycle_semantics.md#lifecycle--entitlement-truth-table' guides/entitlements.md && [ $(grep -vc '^#' guides/entitlements.md) -gt 60 ] && echo "guide-needles+link+length OK"</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/guides/entitlements.md` exists with the 9 sections in D-06 order, fail-closed snippet at the top.
    - Contains the literals `entitled?`, `Accrue.Plug.RequireEntitlement`, and `[:accrue, :entitlements, :check]` (the three needles Plan 04 pins).
    - Lifecycle section inlines ~5 rows and links `lifecycle_semantics.md#lifecycle--entitlement-truth-table` (no full-table re-derivation; grace nuance deferred to SSOT).
    - Provider section is prose-only and links `Accrue.Processor.Capabilities` (not the internal .planning matrix).
    - Length ≤ connect.md; one runnable snippet per topic.
  </acceptance_criteria>
  <done>The guide documents the full entitlements story fail-closed-first, contains all three needle anchors, summarizes-and-links both SSOTs (truth flows one direction), and is connect.md-shaped.</done>
</task>

<task type="auto">
  <name>Task 2: JTBD ⛔→✅ flip in BOTH artifacts + commit the untracked public JTBD file</name>
  <files>accrue/guides/jobs_to_be_done.md, .planning/research/JTBD-FRONTIER.md</files>
  <read_first>
    - accrue/guides/jobs_to_be_done.md (re-read ON DISK — untracked; flip targets at body sections :116/:156, scope prose :339, scope row, Update log :350/:354; the body-section shape :116-154 to clone)
    - .planning/research/JTBD-FRONTIER.md (re-read on disk — flip targets TL;DR :21/:23, gap rows :85/:110, diminishing-returns :118/:125/:142/:154/:158, Update log :160-162)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ MOD jobs_to_be_done.md + § MOD JTBD-FRONTIER.md — exact flip targets and Update-log append shape)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (D-09/D-10/D-11; Pitfall 4 — the "headline gap" / "on the table** is **entitlements" flip-guard collision and the (a)/(b) options)
  </read_first>
  <action>
    PUBLIC `accrue/guides/jobs_to_be_done.md` (re-read on disk first — line numbers may have shifted, and it is untracked):
    (1) Insert a new body section `## Gate access on what they paid for` BETWEEN `## The customer changes their mind` and `## When payments fail`. Clone the existing body-section shape (bold The-job line, then prose, then a fenced elixir snippet, then an "In admin:" pointer referencing the new entitlements tab, then a "Deep dive:" link `[Entitlements](entitlements.md)`).
    (2) Flip the Scope-and-maturity prose: the exact phrase `on the table** is **entitlements` MUST be REMOVED (Plan 04's require_absent_regex flip-guard targets it). Replace with the honest D-10 phrasing: "core entitlements ✅ shipped — gate API (`has_active_plan?`/`entitled?`), Plug + LiveView `on_mount` guards, provider-honest and lifecycle-truthful; the optional Stripe-native sync (`entitlements.active_entitlement_summary.updated`) is a deferred, off-by-default add-on (Phase 127)." Never claim the optional sync ships now.
    (3) Flip the scope-table row ⛔→✅.
    (4) Update-log line 354: reword to drop "headline gap" (Pitfall 4 belt-and-suspenders), AND append a new dated `2026-05-23` entry that includes a distinctive post-flip shipped marker (e.g. the phrase `entitlements ✅`) — pick the EXACT marker Plan 04 will pin as needle 5a and record it in the SUMMARY so Plan 04 uses the byte-identical string.
    INTERNAL `.planning/research/JTBD-FRONTIER.md` (re-read on disk; D-11 re-run convention — re-verify each status cell against current code, trust code not memory):
    (5) Move the entitlements row from the Gap table to **Shipped** (✅, with the deferred-sync note).
    (6) Rewrite the TL;DR "exactly one item" / "spend it on entitlements" to "6 of 6 shipped".
    (7) Flip the delta-table "Entitlements ⛔" Accrue cell ⛔→✅.
    (8) Rewrite the diminishing-returns "5 of 6 / the sixth / highest-leverage thing left" prose to "6 of 6 shipped".
    (9) Keep all deferred/future items internal-only (no public roadmap leakage).
    (10) Append a dated `2026-05-23` Update-log entry to JTBD-FRONTIER.md (clone the :162 shape).
    Then ensure `accrue/guides/jobs_to_be_done.md` is COMMITTED this phase (it is currently untracked) so a clean-checkout CI can see the needle/seed target (Pitfall 3) — this is essential or Plan 04's needle + seed fixture reference a file CI cannot see. Use `git add accrue/guides/jobs_to_be_done.md` so the docs-commit step tracks it.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -Fq 'Gate access on what they paid for' accrue/guides/jobs_to_be_done.md && ! grep -Eq 'on the table\*\* is \*\*entitlements' accrue/guides/jobs_to_be_done.md && grep -Fq '6 of 6' .planning/research/JTBD-FRONTIER.md && echo "flip-content OK"</automated>
  </verify>
  <acceptance_criteria>
    - `jobs_to_be_done.md` has a new `## Gate access on what they paid for` body section between the change-of-mind and payments-fail sections, with a `[Entitlements](entitlements.md)` deep-dive link.
    - The phrase `on the table** is **entitlements` is GONE (flip-guard satisfied); the scope prose + row read ⛔→✅ with honest "core shipped / sync deferred (Phase 127)" phrasing.
    - The Update log no longer contains "headline gap" and gains a dated `2026-05-23` entry with a distinctive shipped marker (the exact string recorded in the SUMMARY for Plan 04).
    - `JTBD-FRONTIER.md` moved entitlements Gap->Shipped, reads "6 of 6 shipped" in TL;DR + diminishing-returns + delta table, and has a dated Update-log entry.
    - `accrue/guides/jobs_to_be_done.md` is staged/tracked (no longer untracked) so CI sees it.
  </acceptance_criteria>
  <done>Both JTBD artifacts flip entitlements ⛔→✅ honestly (core shipped / sync deferred), the flip-guard phrase is removed, both gain dated Update logs, and the previously-untracked public JTBD file is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Spine pointers (README + quickstart) + PROJECT.md D-13 gateway-subscription-core parity fix</name>
  <files>accrue/README.md, accrue/guides/quickstart.md, .planning/PROJECT.md</files>
  <read_first>
    - accrue/README.md (re-read on disk — uncommitted edits; the "## Start here" bullet list :9-22, peer bullet shape :18)
    - accrue/guides/quickstart.md (re-read on disk — uncommitted edits; the focused-guides list using [First Hour](first_hour.md) + auth_adapters.md)
    - .planning/PROJECT.md (the Current posture / PROC-08 prose ~:37 — the lone holdout missing "gateway subscription core")
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ MOD README.md + § MOD quickstart.md + § MOD PROJECT.md — exact bullet shapes + the PROJECT.md reword)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (D-12 minimal spine; D-13 PROJECT.md edit exact line; Verifier State section — confirmed RED)
  </read_first>
  <action>
    README (`accrue/README.md`, re-read on disk first): add ONE peer bullet to the "## Start here" list with the EXACT link text `[Entitlements](guides/entitlements.md)` (Plan 04 pins this byte-for-byte — including brackets/parens, grep -F is literal), framed as a "next, when you need to gate" pointer, e.g. "- [Entitlements](guides/entitlements.md) — gate features on what they paid for; the next read when you need to lock paid surfaces." Do NOT restructure the Start-here list; add one bullet alongside the existing peers (Webhooks etc.).
    quickstart (`accrue/guides/quickstart.md`, re-read on disk first): add ONE bullet `[Entitlements](entitlements.md)` (relative, NO `guides/` prefix — it lives in the guides dir) to the focused-guides list, peer to `[First Hour](first_hour.md)`. Plan 04 pins `[Entitlements](entitlements.md)` as needle 6.
    DO NOT touch `first_hour.md`'s verifier-pinned numbered spine (D-12) — leave it intact (an optional plain-prose cross-link is allowed but skip it to minimize churn; if added it must be plain prose with no new structural needle).
    PROJECT.md (`.planning/PROJECT.md`, D-13): reword the PROC-08 posture line (~:37) to include the literal phrase `gateway subscription core`, e.g. change "a bounded dual-provider core centered on **Stripe-first** defaults..." to "a bounded dual-provider **gateway subscription core** centered on **Stripe-first** defaults...". This is a `grep -F` match for the EXISTING assertion at `verify_package_docs.sh:220` (the verifier is RED on main because PROJECT.md is the lone holdout among 5 files that already pin this phrase). Keep the prose true to the bounded dual-provider scope — this restores SSOT parity, it does not invent new scope. No script change is needed for this needle (the assertion already exists).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -Fq '[Entitlements](guides/entitlements.md)' accrue/README.md && grep -Fq '[Entitlements](entitlements.md)' accrue/guides/quickstart.md && grep -Fq 'gateway subscription core' .planning/PROJECT.md && echo "spine+D13 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/README.md` "Start here" contains the EXACT link text `[Entitlements](guides/entitlements.md)` (one new bullet, list not restructured).
    - `accrue/guides/quickstart.md` focused-guides list contains `[Entitlements](entitlements.md)` (relative, no guides/ prefix).
    - `first_hour.md` numbered spine is UNCHANGED (no structural edit).
    - `.planning/PROJECT.md` contains the literal `gateway subscription core` in the PROC-08 posture prose.
    - All three greps in the verify command pass.
  </acceptance_criteria>
  <done>README and quickstart each gain exactly one entitlements pointer (First Hour spine untouched), and PROJECT.md gains the gateway-subscription-core phrase that clears the pre-existing verifier holdout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published docs → reader | Guide prose instructs developers how to gate access; fail-open guidance would be a security anti-pattern propagated to every host. |
| Public vs internal docs | jobs_to_be_done.md + README + quickstart + entitlements.md are PUBLIC (HexDocs); JTBD-FRONTIER.md + PROJECT.md are INTERNAL. Deferred/future roadmap items must stay internal-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-08 | Repudiation / Misleading guidance | entitlements.md teaching a fail-open pattern | mitigate | The guide LEADS with the fail-closed easy path (`entitled?` returning false on any ambiguity) per PITFALLS #1; the acceptance criterion requires the fail-closed snippet at the top. No fail-open snippet appears. |
| T-126-09 | Information Disclosure | Over-claiming the deferred Stripe-native sync as shipped, OR leaking internal deferred roadmap into public docs | mitigate | D-10 honest phrasing: "core shipped / optional Stripe-native sync deferred off-by-default (Phase 127)". Deferred/future items stay in JTBD-FRONTIER.md (internal) only. Acceptance asserts the honest phrasing and the public flip. |
| T-126-10 | Tampering | Documenting a truth table / provider matrix that drifts from the SSOT | mitigate | Summarize-and-link: lifecycle truth links `lifecycle_semantics.md` (entitling?/1 SSOT); provider honesty is prose + `Processor.Capabilities` link — no re-derived table. D-07/D-08 one-direction truth flow. |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (docs only; RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface. |
</threat_model>

<verification>
- `cd accrue && mix docs` builds and produces `doc/entitlements.html` (the guide auto-globs) — full smoke is run in Plan 04's phase gate, but this plan's content must be `mix docs`-clean (no malformed markdown link breaking the build).
- `grep -F 'gateway subscription core' .planning/PROJECT.md` returns a hit (D-13 holdout cleared).
- `! grep -E 'on the table\*\* is \*\*entitlements' accrue/guides/jobs_to_be_done.md` (flip-guard phrase removed).
- `git ls-files accrue/guides/jobs_to_be_done.md` will show it tracked after the docs-commit step (Pitfall 3).
- Manual: open `doc/entitlements.html` after Plan 04's `mix docs` and confirm the fail-closed opener leads and the SSOT links resolve (the one manual check in 126-VALIDATION.md).
</verification>

<success_criteria>
- `guides/entitlements.md` documents gate API, config, Plug guard, LiveView guard, lifecycle truth (linked), provider matrix (linked), and telemetry — fail-closed-first (ENT-12 SC#2).
- Both JTBD artifacts flip entitlements ⛔→✅ with honest "core shipped / sync deferred" phrasing and dated Update logs (ENT-12 SC#3).
- README + quickstart each reference the entitlements guide; First Hour's spine is untouched (ENT-12 SC#3).
- PROJECT.md gains the `gateway subscription core` phrase, restoring parity and clearing the verifier RED holdout (prereq for ENT-12 SC#4, finished in Plan 04).
- The previously-untracked public JTBD file is committed so CI can see the needle/seed target.
</success_criteria>

<output>
Create `.planning/phases/126-admin-surface-docs-jtbd-spine/126-03-SUMMARY.md` when done. Record the EXACT post-flip shipped marker string chosen in jobs_to_be_done.md (needle 5a) so Plan 04 pins the byte-identical string.
</output>

---
phase: 126-admin-surface-docs-jtbd-spine
plan: 04
type: execute
wave: 2
depends_on: ["126-03"]
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [ENT-12]
must_haves:
  truths:
    - "The package-doc verifier pins the new entitlements spine: README link, entitlements.md gate-fn/Plug/telemetry needles, and the JTBD shipped marker + flip-guard (D-14)"
    - "The flip-guard require_absent_regex does not collide with the historical Update log (scoped to the unique scope-prose phrase; Plan 03 reworded line 354) (D-14 #5b / Pitfall 4)"
    - "Every file a new needle references is in the Elixir verifier-test seed_tmp_dir! copy list (entitlements.md + jobs_to_be_done.md) (D-15)"
    - "The phase gate passes: verify_package_docs.sh exit 0 + package_docs_verifier_test 8/0 + mix docs builds entitlements.html (D-15)"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "5 new entitlements-spine require_* needles (D-14) in a labeled block"
      contains: "guides/entitlements.md"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Two new copy_fixture! seed entries: entitlements.md + jobs_to_be_done.md (D-15)"
      contains: "entitlements.md"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh (new needles)"
      to: "accrue/guides/entitlements.md + accrue/README.md + accrue/guides/jobs_to_be_done.md"
      via: "require_fixed / require_absent_regex against the doc files Plan 03 authored"
      pattern: "require_(fixed|absent_regex).*entitlements"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs (seed_tmp_dir!)"
      to: "the new needle target files"
      via: "copy_fixture! into the seeded tmp dir (else negative fixtures fail 'No such file')"
      pattern: "copy_fixture!\\(\"accrue/guides/(entitlements|jobs_to_be_done).md\""
---

<objective>
Close ENT-12 SC#4: extend `scripts/ci/verify_package_docs.sh` with the tight set of new entitlements-spine needles (D-14), co-update the Elixir verifier-test seed list so every needle-referenced file is seeded (D-15), and prove the whole doc contract GREEN via the 3-command phase gate. This is the LAST plan — it depends on Plan 03's doc files existing (the needles and seed fixtures reference them), and the D-13 PROJECT.md fix Plan 03 already landed clears the pre-existing RED holdout.

Purpose: The verifier is RED on main (PROJECT.md missing `gateway subscription core` short-circuits the script and fails 6 of 8 Elixir tests). Plan 03 fixed that holdout; this plan adds the new spine needles, ensures they reference seeded files (or negative fixtures fail "No such file"), and verifies the gate. SSOT-mirror discipline: doc labels (Plan 03) + their verifier needles (this plan) ship in the same phase/PR.
Output: 5 new needles in the bash verifier, 2 new seed fixtures in the Elixir wrapper, and a GREEN 3-command phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-CONTEXT.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-VALIDATION.md
@.planning/phases/126-admin-surface-docs-jtbd-spine/126-03-SUMMARY.md

<interfaces>
<!-- Exact verifier helpers, the needles to add, and the seed-list co-update. Use directly. -->

verify_package_docs.sh helpers (verified :23-44):
- require_fixed(file, str)         { grep -Fq "$str" "$file" || fail ... }      # literal (brackets/parens are literal)
- require_regex(file, ere)         { grep -Eq "$ere" "$file" || fail ... }      # ERE present
- require_absent_regex(file, ere)  { if grep -Eq "$ere" "$file"; then fail; fi } # ERE absent
- The existing `gateway subscription core` assertion is at :220 (already satisfied by Plan 03's PROJECT.md edit — no script change for THAT needle).
- New needles go near the README/guide require_fixed block (:97-109) in a labeled "# Entitlements spine (Phase 126, ENT-12)" block.

The 5 new needles to ADD (D-14):
1. require_fixed "$ROOT_DIR/accrue/README.md" '[Entitlements](guides/entitlements.md)'        # link text MUST byte-match Plan 03's README bullet
2. require_fixed "$ROOT_DIR/accrue/guides/entitlements.md" 'entitled?'
3. require_fixed "$ROOT_DIR/accrue/guides/entitlements.md" 'Accrue.Plug.RequireEntitlement'
4. require_fixed "$ROOT_DIR/accrue/guides/entitlements.md" '[:accrue, :entitlements, :check]'
5a. require_fixed "$ROOT_DIR/accrue/guides/jobs_to_be_done.md" '<exact post-flip shipped marker from 126-03-SUMMARY.md>'   # e.g. 'entitlements ✅'
5b. require_absent_regex "$ROOT_DIR/accrue/guides/jobs_to_be_done.md" 'on the table\*\* is \*\*entitlements'   # flip-guard (Pitfall 4 — unique to scope prose, NOT the Update log)
6 (only if Plan 03 added the quickstart pointer — it did): require_fixed "$ROOT_DIR/accrue/guides/quickstart.md" '[Entitlements](entitlements.md)'
Do NOT pin: deny-redirect prose, quota numbers, per-provider matrix wording, replicated gate-fn names across README+first_hour+host.

Elixir seed-list co-update (package_docs_verifier_test.exs, D-15):
- seed_tmp_dir!/1 copy_fixture! list at :254-274 (PROJECT.md already seeded :258; .md guides seeded around :260-266).
- ADD: copy_fixture!("accrue/guides/entitlements.md", tmp_dir) and copy_fixture!("accrue/guides/jobs_to_be_done.md", tmp_dir).
- copy_fixture!/2 (:241-245) uses File.cp! from the working tree, so the (now-committed by Plan 03) jobs_to_be_done.md seeds fine.
- The 6 currently-failing negative-fixture tests auto-green from Plan 03's PROJECT.md fix (each short-circuits today on the missing phrase before reaching its mutation).

Phase gate (D-15 — all three MUST pass at phase end):
1. bash scripts/ci/verify_package_docs.sh          -> exit 0
2. cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs  -> 8 tests, 0 failures
3. cd accrue && mix docs                            -> builds; doc/entitlements.html present

Baseline caveat: the 6 PRE-EXISTING PackageDocsVerifier failures are EXPECTED to green via the D-13 fix — that is the validation signal, not a regression. Do not re-triage. (A flaky PdfTest in the full suite is unrelated.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 5 (+1) entitlements-spine needles to verify_package_docs.sh (D-14, flip-guard scoped per Pitfall 4)</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh (the helpers :23-44; the README/guide require_fixed block :97-109; the quickstart block :73-76; the existing gateway-subscription-core assertion :220)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-03-SUMMARY.md (the EXACT post-flip shipped marker string Plan 03 authored in jobs_to_be_done.md — pin THIS byte-for-byte as needle 5a)
    - accrue/README.md + accrue/guides/quickstart.md + accrue/guides/entitlements.md + accrue/guides/jobs_to_be_done.md (re-read on disk — confirm the EXACT link/marker strings Plan 03 wrote so the grep -F literals byte-match)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (Verifier State & Edit Plan — D-14 verbatim needle strings + Pitfall 4 flip-guard collision)
  </read_first>
  <action>
    Edit `scripts/ci/verify_package_docs.sh`. Add a labeled `# Entitlements spine (Phase 126, ENT-12)` block near the README/guide `require_fixed` block (after ~:109, before the PROJECT.md posture block at :219) with these needles, each string byte-matching what Plan 03 actually wrote (re-read the doc files first; `grep -F` is literal including brackets/parens/the ✅ glyph):
    - `require_fixed "$ROOT_DIR/accrue/README.md" '[Entitlements](guides/entitlements.md)'`
    - `require_fixed "$ROOT_DIR/accrue/guides/entitlements.md" 'entitled?'`
    - `require_fixed "$ROOT_DIR/accrue/guides/entitlements.md" 'Accrue.Plug.RequireEntitlement'`
    - `require_fixed "$ROOT_DIR/accrue/guides/entitlements.md" '[:accrue, :entitlements, :check]'`
    - `require_fixed "$ROOT_DIR/accrue/guides/jobs_to_be_done.md" '<exact marker from 126-03-SUMMARY.md, e.g. entitlements ✅>'`
    - `require_absent_regex "$ROOT_DIR/accrue/guides/jobs_to_be_done.md" 'on the table\*\* is \*\*entitlements'` (the flip-guard — this ERE is unique to the scope prose Plan 03 removed; it does NOT collide with the historical Update log, which Plan 03 reworded to drop "headline gap" — Pitfall 4)
    - `require_fixed "$ROOT_DIR/accrue/guides/quickstart.md" '[Entitlements](entitlements.md)'` (Plan 03 added this pointer, so pin it)
    Do NOT add the `gateway subscription core` assertion — it ALREADY exists at :220 and Plan 03's PROJECT.md edit satisfies it. Do NOT pin deny-redirect prose, quota numbers, matrix wording, or gate-fn names replicated across README+first_hour+host (D-14 explicit exclusions). Verify the script still exits 0 against the current (Plan 03-edited) on-disk docs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -q 'Entitlements spine (Phase 126' scripts/ci/verify_package_docs.sh && grep -q "require_fixed.*accrue/guides/entitlements.md.*Accrue.Plug.RequireEntitlement" scripts/ci/verify_package_docs.sh && grep -q "require_absent_regex.*jobs_to_be_done.md.*on the table" scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_package_docs.sh && echo "verifier-exit-0"</automated>
  </verify>
  <acceptance_criteria>
    - `verify_package_docs.sh` has a labeled `# Entitlements spine (Phase 126, ENT-12)` block with the README link needle, the three entitlements.md needles (entitled? / Accrue.Plug.RequireEntitlement / [:accrue, :entitlements, :check]), the jobs_to_be_done.md shipped-marker needle, the scoped flip-guard, and the quickstart pointer needle.
    - The flip-guard `require_absent_regex` targets the unique scope-prose phrase (not "headline gap"), and does not fire (Plan 03 removed the phrase and reworded the Update log).
    - No `gateway subscription core` line was added (it already exists at :220).
    - `bash scripts/ci/verify_package_docs.sh` exits 0 against the Plan 03-edited docs.
  </acceptance_criteria>
  <done>The verifier pins the entitlements spine with byte-matching needles and a non-colliding flip-guard, excludes the forbidden over-pins, and exits 0 against the current docs.</done>
</task>

<task type="auto">
  <name>Task 2: Co-update the Elixir verifier-test seed list (D-15) + run the 3-command phase gate</name>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs (seed_tmp_dir!/1 copy_fixture! list :254-274; copy_fixture!/2 :241-245; PROJECT.md already seeded :258)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-PATTERNS.md (§ MOD package_docs_verifier_test.exs — the exact two copy_fixture! lines to add)
    - .planning/phases/126-admin-surface-docs-jtbd-spine/126-RESEARCH.md (D-15 seed co-update; the green-proof 3-command gate; baseline caveat — 6 verifier failures expected to green)
  </read_first>
  <action>
    In `accrue/test/accrue/docs/package_docs_verifier_test.exs`, inside `seed_tmp_dir!/1`'s `copy_fixture!` block (alongside the other `accrue/guides/*` copies around :260-266), add exactly two new lines: `copy_fixture!("accrue/guides/entitlements.md", tmp_dir)` and `copy_fixture!("accrue/guides/jobs_to_be_done.md", tmp_dir)`. These are mandatory because any file a new needle references must exist in the seeded tmp dir — otherwise the NEGATIVE-drift fixture tests (which seed then mutate one file) fail with "No such file" when the verifier hits the new `require_fixed`/`require_absent_regex` against an absent file. `jobs_to_be_done.md` was NOT previously seeded (and was untracked until Plan 03 committed it); `entitlements.md` is brand new. `.planning/PROJECT.md` is already seeded, so Plan 03's D-13 fix auto-greens the 6 previously-failing negative fixtures (each short-circuited on the missing phrase). Then run the full D-15 phase gate (all three must pass). If any negative-fixture test fails on "No such file", a needle references a file not in the seed list — add it. If a positive needle fails, the doc string Plan 03 wrote does not byte-match the needle Task 1 pinned — reconcile by re-reading the doc and aligning Task 1's literal (the doc string is the SSOT; the needle follows it).
  </action>
  <verify>
    <automated>cd accrue && grep -q 'copy_fixture!("accrue/guides/entitlements.md"' test/accrue/docs/package_docs_verifier_test.exs && grep -q 'copy_fixture!("accrue/guides/jobs_to_be_done.md"' test/accrue/docs/package_docs_verifier_test.exs && bash ../scripts/ci/verify_package_docs.sh && mix test test/accrue/docs/package_docs_verifier_test.exs 2>&1 | tail -3 | grep -qE '8 tests, 0 failures' && mix docs >/dev/null 2>&1 && test -f doc/entitlements.html && echo "PHASE-GATE-GREEN (verifier exit0 + 8/0 + entitlements.html)"</automated>
  </verify>
  <acceptance_criteria>
    - `package_docs_verifier_test.exs` `seed_tmp_dir!` copies BOTH `accrue/guides/entitlements.md` and `accrue/guides/jobs_to_be_done.md`.
    - Phase gate command 1: `bash scripts/ci/verify_package_docs.sh` exits 0.
    - Phase gate command 2: `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` reports 8 tests, 0 failures (the 6 previously-RED tests greened via Plan 03's D-13 fix; the 2 new needle targets are seeded).
    - Phase gate command 3: `cd accrue && mix docs` builds and `doc/entitlements.html` exists.
    - No negative-fixture test fails with "No such file".
  </acceptance_criteria>
  <done>Both new guide files are in the seed list, all 8 verifier tests pass, the bash verifier exits 0, and mix docs builds entitlements.html — the D-15 phase gate is GREEN end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI verifier → merge gate | The package-doc verifier is merge-blocking; a needle that references an unseeded or absent file would either false-fail (blocking valid merges) or false-pass (letting drift through). |
| Doc label (SSOT) ↔ verifier needle | SSOT-mirror discipline requires the needle to byte-match the doc string authored in Plan 03 — divergence is itself the drift the gate exists to catch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-11 | Tampering | A needle references a file not in the seed list, making negative fixtures fail "No such file" (false-fail) or a missing seed hiding real drift | mitigate | Task 2 adds both needle-referenced files (entitlements.md + jobs_to_be_done.md) to seed_tmp_dir! and the phase gate asserts no "No such file" failure (8/0 required). |
| T-126-12 | Spoofing / drift slip-through | The flip-guard regex defeated by the historical Update log line, letting the old gap wording silently remain | mitigate | The flip-guard is scoped to the unique scope-prose phrase `on the table** is **entitlements` (not "headline gap"), and Plan 03 reworded the Update log line 354 (belt-and-suspenders). Acceptance asserts the guard targets the scoped phrase. |
| T-126-13 | Tampering | A positive needle that does not byte-match the authored doc string (false-fail) | mitigate | Task 1 re-reads the on-disk docs (and 126-03-SUMMARY.md's recorded marker) before pinning; Task 2 reconciles any mismatch by aligning the needle to the doc string (doc is SSOT). |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (CI/test edits only; RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface. |
</threat_model>

<verification>
THE PHASE GATE (D-15 — all three, run at the end of this plan):
1. `bash scripts/ci/verify_package_docs.sh` → exit 0
2. `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` → 8 tests, 0 failures
3. `cd accrue && mix docs` → builds; `doc/entitlements.html` present

Cross-plan sanity (run before declaring the phase done):
- `cd accrue && mix test` (expect: only the known-flaky PdfTest may flake; the 6 verifier failures are now GREEN — not a regression)
- `cd accrue_admin && mix test` (Plan 02's tab + the rest of the admin suite green)
</verification>

<success_criteria>
- The package-doc verifier pins the entitlements spine (README link, 3 entitlements.md needles, JTBD shipped marker + scoped flip-guard, quickstart pointer) without forbidden over-pins (ENT-12 SC#4, D-14).
- Every needle-referenced file is in the Elixir verifier-test seed list (D-15).
- The 3-command phase gate is GREEN: verifier exit 0, 8/0 tests, mix docs builds entitlements.html.
- The 6 pre-existing verifier failures are resolved (greened via Plan 03's D-13 fix), confirming SC#4 "verifiers stay green".
</success_criteria>

<output>
Create `.planning/phases/126-admin-surface-docs-jtbd-spine/126-04-SUMMARY.md` when done. Record the final phase-gate command outputs (exit codes / test counts) as the closing evidence for ENT-12 SC#4.
</output>

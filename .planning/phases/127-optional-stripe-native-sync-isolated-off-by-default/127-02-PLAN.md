---
phase: 127-optional-stripe-native-sync-isolated-off-by-default
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/lib/accrue/entitlements/stripe_sync.ex
autonomous: true
requirements: [ENT-10]
user_setup: []

must_haves:
  truths:
    - "With sync :advisory, an entitlements.active_entitlement_summary.updated webhook writes/upserts one accrue_entitlement_summaries row keyed on customer, persisting customer, ≤10 entitlements, has_more→truncated, and the event watermark."
    - "Out-of-order or replayed summaries cannot regress the cache: a strictly-older event (:lt) is skipped with [:accrue, :webhooks, :stale_event] (object_type :entitlement_summary); :eq and :gt proceed."
    - "With sync :disabled (default), the dispatch clause early-returns {:ok, :ignored} BEFORE any Repo call — the off lane is provably DB-free."
    - "A customer-not-found summary returns {:ok, :deferred} + [:accrue, :webhooks, :orphan_entitlement_summary]; a malformed payload returns {:ok, :ignored}; neither raises nor writes garbage."
    - "An accrue_events ledger row is written ONLY when the cached set materially changes (sorted {feature_id, lookup_key} pairs OR truncated differs; first write = material); type entitlements.summary.synced; never the raw payload."
    - "The sync write is wrapped in a [:accrue, :entitlements, :sync] span; a [:accrue, :entitlements, :summary_synced] event fires with result :written | :unchanged; [:accrue, :ops, :entitlement_summary_truncated] fires only when has_more is true."
    - "A read-only seam exposes the cached row for a customer; the gate path (entitled?/has_active_plan?/Resolver/LocalMap) never references it (one-way)."
  artifacts:
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "config-gated dispatch clause + reduce_entitlement_summary reducer (monotonic upsert + orphan/malformed tolerance + on-change ledger + telemetry/span/ops)"
      contains: "entitlements.active_entitlement_summary.updated"
    - path: "accrue/lib/accrue/entitlements/stripe_sync.ex"
      provides: "read-only observational seam summary_for_customer/1 over accrue_entitlement_summaries"
      contains: "EntitlementSummary"
  key_links:
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "Accrue.Config.stripe_native_sync?/0"
      via: "dispatch clause runtime gate checked first"
      pattern: "stripe_native_sync\\?"
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "Accrue.Billing.EntitlementSummary"
      via: "Repo.get_by + upsert in reduce_entitlement_summary"
      pattern: "EntitlementSummary"
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "accrue_events"
      via: "on-change Events.record(type: entitlements.summary.synced)"
      pattern: "entitlements.summary.synced"
---

<objective>
Implement the heart of ENT-10: the config-gated webhook reducer that writes the
advisory cache with monotonic ordering, on-change-only ledgering, full telemetry, and
orphan/malformed tolerance — plus the read-only observational seam that exposes the
cached row WITHOUT ever touching the gate path. Turns the Plan 01 RED tests GREEN.

Purpose: This is where the locked overlay semantics (D-01 observational-only),
monotonic write (D-06), truncation honesty (D-07), on-change ledger (D-08), telemetry
(D-09), and the read seam (D-11) become real. Local-first stays canonical; the cache
is recorded/surfaced but never gate-consulted.

Output: A new dispatch clause + reducer in DefaultHandler (reusing check_stale/2 +
stamp_watermark/3 verbatim) and a read-only StripeSync seam module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-RESEARCH.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-VALIDATION.md
@accrue/lib/accrue/billing/entitlement_summary.ex
@accrue/lib/accrue/entitlements/admin.ex

<interfaces>
<!-- Helpers to REUSE verbatim from default_handler.ex (do NOT reinvent): -->
<!-- check_stale/2 (:1078-1087): nil/nil-ts/nil-evt → :ok; :lt → :stale; :eq/:gt → :ok -->
<!-- stamp_watermark/3 (:1102-1104): Map.merge(attrs, %{last_stripe_event_ts: evt_ts, last_stripe_event_id: evt_id}) -->
<!-- get/2 dual atom/string (:1136-1140): Map.get(map, key) || Map.get(map, Atom.to_string(key)) -->
<!-- record_event/5 (:1106-1115): Events.record with type/subject_type/subject_id/data/idempotency_key -->
<!-- dispatch/4 checkout.session placement (:250-253); catch-all at :268 -->
<!-- orphan_charge tolerance (:840-848): Repo.get_by(Customer, processor_id:) miss → telemetry + {:ok, :deferred} -->

Read seam clone target (admin.ex full file — one-way admin→billing, @doc false, never gate-referenced):
  def resolve_for_customer(%Customer{} = customer) do
    {LocalMap.fold_for_customer(customer), LocalMap.unmapped_entitling_price_ids(customer)}
  end

Events idempotency dedup (events.ex:146-154): on_conflict :nothing, conflict_target
  {:unsafe_fragment, "(idempotency_key) WHERE idempotency_key IS NOT NULL"}.

Sync span mirror (entitlements.ex:290): Accrue.Telemetry.span([:accrue, :entitlements, :check], meta, fun).
Ops emit (ops.ex:60-75): Accrue.Telemetry.Ops.emit(suffix_list, measurements, metadata) → prefixes [:accrue, :ops].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config-gated dispatch clause + monotonic reducer (D-04/D-06/D-07/D-09)</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex</files>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex (full: dispatch/4 clauses 205-268 incl. checkout.session 250-253 + catch-all 268; reduce_charge/orphan 840-848; check_stale/2 1078-1087; reduce_row/5 1058-1076; stamp_watermark/3 1102-1104; record_event/5 1106-1115; dual get/2 1136-1140)
    - accrue/lib/accrue/webhook/connect_handler.ex (1-66 — the moduledoc model for documenting WHY this path uses monotonic-snapshot not refetch-canonical: lattice_stripe 1.1 has no Entitlements API)
    - accrue/lib/accrue/billing/entitlement_summary.ex (the schema + force changeset from Plan 01)
    - accrue/lib/accrue/telemetry/ops.ex (emit/3 at 57-75 — connect_account_deauthorized model)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-RESEARCH.md ("Monotonic upsert" 342-369 — the assembled clone skeleton; "Webhook payload shape" 296-326)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md (D-04, D-06, D-07, D-09)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md (default_handler.ex section a/b/c/d + the [:accrue,:entitlements,:sync] span + ops sections)
  </read_first>
  <behavior>
    - dispatch("entitlements.active_entitlement_summary.updated", ...): when `Accrue.Config.stripe_native_sync?()` is false → `{:ok, :ignored}` BEFORE any Repo call; when true → reduce_entitlement_summary.
    - reduce_entitlement_summary reads `customer` (binary) + `entitlements`→`data` (list) from the raw obj via the dual get/2; malformed (missing customer / non-list entitlements) → telemetry + `{:ok, :ignored}`, no write.
    - Customer resolved via Repo.get_by(Customer, processor_id: cus_id); miss → `[:accrue, :webhooks, :orphan_entitlement_summary]` + `{:ok, :deferred}`, never raise, never create a customer.
    - Inside Repo.transact: load row by customer_id; check_stale(row, evt_ts) — `:lt` → `[:accrue, :webhooks, :stale_event]` (object_type :entitlement_summary, stripe_id cus_id, event_id evt_id) + `{:ok, :stale}` (no ledger); `:eq`/`:gt` proceed.
    - On proceed: build attrs (entitlement_count = length(data ≤10), truncated = has_more, livemode, stripe_customer_id, synced_at from event created, data = raw object) |> stamp_watermark(evt_ts, evt_id); upsert via the force changeset.
    - Wrap the write in `Accrue.Telemetry.span([:accrue, :entitlements, :sync], metadata, fun)`; emit `[:accrue, :entitlements, :summary_synced]` with measurements `%{count: 1, entitlement_count: n}`, metadata `%{customer_id, has_more, result: :written | :unchanged}`; fire `Accrue.Telemetry.Ops.emit(:entitlement_summary_truncated, ...)` ONLY when has_more is true.
  </behavior>
  <action>Add a `dispatch("entitlements.active_entitlement_summary.updated", evt_id, evt_ts, obj)` clause before the catch-all (mirror the checkout.session placement at default_handler.ex:250); the clause checks `Accrue.Config.stripe_native_sync?()` FIRST and early-returns `{:ok, :ignored}` when false (D-04 layer 1 — zero Repo call on the off lane). Implement `reduce_entitlement_summary/3` cloning the RESEARCH "Monotonic upsert" skeleton: REUSE `check_stale/2` and `stamp_watermark/3` verbatim (do not reinvent — D-06); REUSE the dual `get/2` to read `customer`/`entitlements.data` defensively (malformed → telemetry + `{:ok, :ignored}`, D-06/Pitfall 6); clone the `orphan_charge` tolerance for customer-not-found → `[:accrue, :webhooks, :orphan_entitlement_summary]` + `{:ok, :deferred}` (D-06). Build attrs with `entitlement_count`, `truncated` (← `entitlements.has_more`, D-07), `livemode`, `stripe_customer_id`, `synced_at` (← event `created`), `data` (raw object), then `stamp_watermark`. Upsert keyed on `customer_id` via the force changeset from Plan 01. Wrap in `[:accrue, :entitlements, :sync]` span (mirror entitlements.ex:290 `:check`); emit `[:accrue, :entitlements, :summary_synced]` with `result: :written | :unchanged`; fire `Accrue.Telemetry.Ops.emit(:entitlement_summary_truncated, %{count: 1}, %{customer_id: ...})` only when `has_more`. Keep `entitlement_count`/`has_more` telemetry-only — do NOT widen the OTel `@allowed_attributes` allowlist (D-09). Never log the raw payload (V7). Add a moduledoc/comment (ConnectHandler-style) explaining the monotonic-snapshot choice. Whether the reducer is a private clause here or a small delegated helper module is your discretion (keep gate path cache-free). Copy-from details are in the read_first analogs — no full implementations in this action.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs -x 2>&1 | tail -3 | grep -qE "0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs` exits 0 (enabled-write / stale-skip / tie-proceeds / orphan-deferred / malformed-ignored / truncated all GREEN).
    - `cd accrue && mix test test/property/entitlement_summary_monotonic_property_test.exs` exits 0 (out-of-order/replay cannot regress the cache).
    - Source grep: `grep 'stripe_native_sync?' accrue/lib/accrue/webhook/default_handler.ex` matches and appears in the dispatch clause guard (off lane returns `{:ok, :ignored}`).
    - Source grep: `grep 'object_type: :entitlement_summary' accrue/lib/accrue/webhook/default_handler.ex` and `grep 'orphan_entitlement_summary' accrue/lib/accrue/webhook/default_handler.ex` both match.
    - Source grep: `grep '\[:accrue, :entitlements, :sync\]' accrue/lib/accrue/webhook/default_handler.ex` and `grep 'entitlement_summary_truncated' accrue/lib/accrue/webhook/default_handler.ex` both match.
    - OTel allowlist UNCHANGED: `git diff --stat accrue/lib/accrue/telemetry/otel.ex` shows no change (D-09).
  </acceptance_criteria>
  <done>Config-gated reducer writes the cache monotonically with full telemetry/span/ops, tolerates orphan/malformed, off lane is DB-free; integration + monotonic property tests GREEN; OTel allowlist untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: On-change-only ledger (D-08) + read-only observational seam (D-11)</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/lib/accrue/entitlements/stripe_sync.ex</files>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex (record_event/5 at 1106-1115 — the ledger clone target; the reducer added in Task 1)
    - accrue/lib/accrue/events.ex (record/1 at 109-117; insert_opts/1 idempotency dedup at 146-154)
    - accrue/lib/accrue/entitlements/admin.ex (full file — the read-seam clone target: one-way admin→billing, @doc false, never gate-referenced)
    - accrue/lib/accrue/billing/entitlement_summary.ex (the schema to read back)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md (D-08, D-11)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md (default_handler.ex section e + the stripe_sync.ex seam section)
  </read_first>
  <behavior>
    - Ledger row written ONLY on material change: sorted `{feature_id, lookup_key}` pairs of the new set differ from the existing row's set, OR `truncated` differs; a first-ever write (no prior row) is material. Byte-identical re-delivery → emit `result: :unchanged` telemetry, NO ledger row. Stale-skip and orphan/deferred → NO ledger row.
    - Ledger event type `"entitlements.summary.synced"`; idempotency_key `"entitlements.summary.synced:" <> evt_id` (Oban retries collapse via the partial unique index); ledger `data` = IDs/counts only, never the raw payload.
    - `Accrue.Entitlements.StripeSync.summary_for_customer/1` returns the cached row (or nil) for a customer via Repo.get_by(EntitlementSummary, customer_id: customer.id); it is observational-only — nothing under the gate path (entitled?/has_active_plan?/Resolver/LocalMap) references it.
  </behavior>
  <action>In the reducer, compute the material-change comparison: derive the sorted `{feature_id, lookup_key}` pair list from the incoming `entitlements.data` and compare against the existing row's stored set plus the `truncated` flag; treat first-ever write as material (D-08). Only when material, append an `accrue_events` row inside the same `Repo.transact` via the `record_event/5` shape with type `"entitlements.summary.synced"`, subject = the EntitlementSummary id, and `idempotency_key: "entitlements.summary.synced:" <> evt_id`; ledger `data` carries IDs/counts only (never the raw payload, V7). On a non-material re-delivery emit `[:accrue, :entitlements, :summary_synced]` with `result: :unchanged` and write no ledger row. Create `Accrue.Entitlements.StripeSync` (your discretion: dedicated module per D-Discretion) cloning the `Accrue.Entitlements.Admin` one-way read-seam stance (moduledoc: "one-way seam → billing read, never gate → seam; observational-only, the gate path MUST NOT reference this", @doc false on the fn) with `summary_for_customer/1` that `Repo.get_by(EntitlementSummary, customer_id: customer.id)`. Do NOT add any reference to StripeSync/EntitlementSummary from entitlements.ex, resolver.ex, or local_map.ex (Plan 03's static gate enforces this). Copy-from details are in the read_first analogs.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs -x 2>&1 | tail -3 | grep -qE "0 failures" && grep -q 'def summary_for_customer' lib/accrue/entitlements/stripe_sync.ex</automated>
  </verify>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs` exits 0 including the on-change-ledger assertions (material → one accrue_events row; identical re-delivery → zero new rows + `result: :unchanged`; stale/orphan → zero rows).
    - Source grep: `grep 'entitlements.summary.synced:' accrue/lib/accrue/webhook/default_handler.ex` matches (idempotency_key prefix).
    - Source grep: `grep 'def summary_for_customer' accrue/lib/accrue/entitlements/stripe_sync.ex` matches; `grep -c 'EntitlementSummary' accrue/lib/accrue/entitlements/stripe_sync.ex` ≥ 1.
    - Gate-path purity (pre-check for Plan 03's gate): `grep -REc 'EntitlementSummary|StripeSync' accrue/lib/accrue/entitlements.ex accrue/lib/accrue/entitlements/resolver/local_map.ex accrue/lib/accrue/entitlements/resolver.ex` returns 0 across all three (excluding comments — use `grep -E '^[^#]*' | grep -c`).
  </acceptance_criteria>
  <done>Ledger row appends on material change only (idempotency-keyed, IDs/counts only); read-only StripeSync seam exposes the cached row one-way; the gate path has zero cache references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe → webhook reducer | Untrusted full-snapshot summary payload (untyped raw map) parsed here; signature already verified upstream and non-bypassable. |
| Webhook reducer → cache table | Monotonic write boundary; out-of-order/replay must not regress. |
| Read seam → gate path | The seam must NOT be reachable from entitled?/has_active_plan? (observational-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-04 | Tampering | out-of-order / replayed summary clobbering newer cache state | mitigate | REUSE `check_stale/2` keyed on event `created` ts; strict `:lt` → skip + `[:accrue, :webhooks, :stale_event]`; monotonic property test asserts final state == highest-ts snapshot (ASVS V4, Task 1). |
| T-127-05 | Tampering | cache poisoning via malformed payload | mitigate | Defensive dual `get/2` extraction; `customer` must be binary, `entitlements.data` must be a list, else `{:ok, :ignored}` with no write (ASVS V5, Task 1). Handler is rescue-wrapped upstream (safe_handle/2) as defense-in-depth. |
| T-127-06 | Elevation of Privilege | stale/partial cache influencing an authorization decision | mitigate | Observational-only (D-01): the reducer/seam never touch the gate path; truncated (`has_more`) is recorded but never used to grant/deny. Task 2 keeps the gate-path grep clean; Plan 03 statically enforces it. |
| T-127-07 | Information Disclosure | raw summary payload leaking into logs/telemetry/ledger | mitigate | Telemetry dims are IDs + counts only; ledger `data` carries IDs/counts only; raw payload stored only in the cache `data` JSONB column, never logged (ASVS V7, D-08/D-09). OTel allowlist not widened. |
| T-127-08 | Repudiation | sync state change leaving no audit trail | mitigate | On-change `accrue_events` row (`entitlements.summary.synced`, idempotency-keyed) records material changes (ENT-05 split, D-08, Task 2). |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | Zero new deps this phase (RESEARCH §Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs` exits 0.
- `cd accrue && mix test test/property/entitlement_summary_monotonic_property_test.exs` exits 0.
- Gate-path files (entitlements.ex, resolver.ex, local_map.ex) contain zero EntitlementSummary/StripeSync references.
- `git diff --stat accrue/lib/accrue/telemetry/otel.ex` shows no change.
</verification>

<success_criteria>
- Monotonic config-gated reducer writes the advisory cache; off lane is DB-free; out-of-order/replay cannot regress.
- On-change-only ledger + full telemetry/span/ops; raw payload never logged.
- Read-only observational seam exposes the cache one-way; gate path stays cache-free.
</success_criteria>

<output>
Create `.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-02-SUMMARY.md` when done.
</output>

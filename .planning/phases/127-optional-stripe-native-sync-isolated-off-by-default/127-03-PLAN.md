---
phase: 127-optional-stripe-native-sync-isolated-off-by-default
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor/capabilities.ex
  - .planning/processor-support-matrix.md
  - scripts/ci/verify_processor_support_matrix.sh
  - scripts/ci/verify_entitlement_sync_isolation.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [ENT-10]
user_setup: []

must_haves:
  truths:
    - "A NEW entitlements.stripe_native_sync capability row exists (stripe native/advisory, fake/braintree unsupported|out-of-slice) in code labels, the support-matrix markdown, and the drift gate — same-PR SSOT co-update."
    - "The existing entitlements.local_mapping convergence row is byte-for-byte UNCHANGED (all three local-identical)."
    - "verify_processor_support_matrix.sh passes: its negative divergence guard is tightened to exempt the new stripe_native_sync row while still failing if local_mapping ever sprouts a native/unsupported/bounded label."
    - "A new merge-blocking static gate verify_entitlement_sync_isolation.sh proves no EntitlementSummary/StripeSync/cache reference is reachable from the always-on gate-path files; it is wired into the docs-contracts-shift-left CI job."
  artifacts:
    - path: "accrue/lib/accrue/processor/capabilities.ex"
      provides: "new entitlements.stripe_native_sync row in @core_capability_labels + @provider_support_labels; convergence row untouched"
      contains: "stripe_native_sync"
    - path: ".planning/processor-support-matrix.md"
      provides: "new entitlements.stripe_native_sync markdown row"
      contains: "entitlements.stripe_native_sync"
    - path: "scripts/ci/verify_processor_support_matrix.sh"
      provides: "require_substring for the new row + tightened negative guard scoped to local_mapping"
      contains: "stripe_native_sync"
    - path: "scripts/ci/verify_entitlement_sync_isolation.sh"
      provides: "static grep gate: no cache reference reachable from gate-path files"
      contains: "EntitlementSummary"
  key_links:
    - from: "scripts/ci/verify_entitlement_sync_isolation.sh"
      to: "accrue/lib/accrue/entitlements.ex + resolver/local_map.ex + resolver.ex"
      via: "grep -rnE over the gate-path files, ^[^#]* comment-anchor, exit 1 on hit"
      pattern: "EntitlementSummary|StripeSync"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_entitlement_sync_isolation.sh"
      via: "docs-contracts-shift-left job step"
      pattern: "verify_entitlement_sync_isolation"
---

<objective>
Land the provider-honesty surface (a NEW capability-matrix row for Stripe-native sync,
never mutating the convergence row) AND the static isolation merge-gate that proves the
advisory cache can never be reached from the gate path. Both are independent of the
reducer's source code — they gate against gate-path files Phase 127 does NOT modify —
so this plan runs in parallel with Plan 01.

Purpose: D-10's new row WILL trip the existing negative drift guard (it matches
`\b(native|unsupported|bounded)\b` on any `entitlements.*` row) — the load-bearing risk
flagged in the Pattern Map. This plan resolves that same-PR. D-04 layer 2's static gate
makes "the cache is observational-only" a machine-enforced, non-regressing invariant.

Output: capabilities.ex + matrix markdown + tightened drift gate (3-way SSOT co-update),
plus a new isolation CI script wired merge-blocking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-VALIDATION.md

<interfaces>
<!-- Capabilities row shapes (capabilities.ex). Convergence row is NEVER edited. -->
<!-- @core_capability_labels entitlements (60-62): %{local_mapping: "all first-party"} -->
<!-- @provider_support_labels entitlements (106-112): CONVERGENCE — all three "local-identical" -->
<!-- Divergence row template (swap_plan, 67-71): %{fake:, stripe:, braintree:} -->

Drift gate negative guard to TIGHTEN (verify_processor_support_matrix.sh:104-112):
  if grep -Eq '^\| entitlements\.[a-z_]+ \|.*\b(native|unsupported|bounded)\b' "${matrix}"; then exit 1; fi
  # must be scoped to entitlements.local_mapping only, OR exempt the exact stripe_native_sync row line.

Convergence-row require_substring already present (line 60):
  require_substring "| entitlements.local_mapping | local-identical | local-identical | local-identical | all first-party |" "..."

Isolation gate clone target (verify_core_liveview_runtime_free.sh:34-47):
  hits=$(grep -rnE '^[^#]*(<PATTERN>)' "${lib}" --include='*.ex' | grep -v '<ALLOWLIST>' || true)
  [[ -n "${hits}" ]] && { echo FAIL; echo "${hits}"; exit 1; } ; echo OK

CI wiring location (ci.yml docs-contracts-shift-left job, after line 50 — the
verify_core_liveview_runtime_free.sh step):
  - name: Core stays LiveView-runtime-free (ENT-07 D-05)
    run: bash scripts/ci/verify_core_liveview_runtime_free.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New entitlements.stripe_native_sync capability row + tightened drift gate (D-10)</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, .planning/processor-support-matrix.md, scripts/ci/verify_processor_support_matrix.sh</files>
  <read_first>
    - accrue/lib/accrue/processor/capabilities.ex (the @core_capability_labels entitlements row at 60-62; the @provider_support_labels CONVERGENCE row at 106-112 — DO NOT EDIT; the swap_plan divergence template at 67-71)
    - scripts/ci/verify_processor_support_matrix.sh (full file — the require_substring positive idiom at 22-63, the negative-guard idiom at 64-102, and the entitlements negative guard at 104-112 that MUST be tightened)
    - .planning/processor-support-matrix.md (the entitlements section — find the local_mapping convergence row to add a SIBLING row beneath, never edit the convergence row)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md (D-10 + Specifics: label wording "native (advisory)"/"native (observational)")
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md (the "verify_processor_support_matrix.sh + ... + capabilities.ex" section, esp. the PLANNER-CRITICAL CONFLICT box)
  </read_first>
  <action>Add a NEW `stripe_native_sync` key under the `entitlements:` group in BOTH `@core_capability_labels` (a public label, e.g. "Stripe-native advisory (observational)") AND `@provider_support_labels` (`%{fake: "out of slice", stripe: "native (advisory)", braintree: "unsupported"}`, cloning the swap_plan divergence shape). NEVER touch the existing `entitlements.local_mapping` convergence row in either block. Add the matching `entitlements.stripe_native_sync` row to `.planning/processor-support-matrix.md` as a SIBLING beneath the local_mapping convergence row (do not edit the convergence row). In `verify_processor_support_matrix.sh`: add a `require_substring` for the exact new `| entitlements.stripe_native_sync | ... |` markdown line, and TIGHTEN the negative divergence guard at 104-112 so it fires only for the `entitlements.local_mapping` convergence row (e.g. anchor the regex to `entitlements\.local_mapping` instead of `entitlements\.[a-z_]+`, OR keep the broad scan but `grep -v` the exact stripe_native_sync line before the test) — the guard MUST still `exit 1` if `local_mapping` ever sprouts a native/unsupported/bounded label, and MUST allow the new stripe_native_sync row's `native (advisory)`/`unsupported` labels. This is a same-PR 3-way SSOT co-update (code labels + matrix markdown + drift gate). Copy-from idioms are in the read_first files.</action>
  <verify>
    <automated>bash scripts/ci/verify_processor_support_matrix.sh && cd accrue && mix compile --warnings-as-errors 2>&1 | grep -qv warning</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_processor_support_matrix.sh` exits 0 (prints "verify_processor_support_matrix: OK").
    - `cd accrue && mix compile --warnings-as-errors` exits 0 (capabilities.ex compiles).
    - Convergence row unchanged: `grep -c "| entitlements.local_mapping | local-identical | local-identical | local-identical | all first-party |" .planning/processor-support-matrix.md` returns 1 and `git diff .planning/processor-support-matrix.md` shows NO change on that line.
    - New row present: `grep -c "entitlements.stripe_native_sync" .planning/processor-support-matrix.md` ≥ 1; `grep -c "stripe_native_sync" accrue/lib/accrue/processor/capabilities.ex` ≥ 1.
    - Regression proof of the tightened guard: temporarily flipping a `local_mapping` cell to `native` still makes the script `exit 1` (executor confirms in the SUMMARY by manual one-off, then reverts) — the guard must not be neutered.
  </acceptance_criteria>
  <done>New stripe_native_sync row lands in all three SSOT locations; convergence row byte-identical; drift gate passes and still protects local_mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Static isolation gate verify_entitlement_sync_isolation.sh + CI wiring (D-04 layer 2)</name>
  <files>scripts/ci/verify_entitlement_sync_isolation.sh, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/ci/verify_core_liveview_runtime_free.sh (full file 1-48 — the exact clone target: comment-anchored grep, allowlist-by-construction, exit 1 on hit, trailing `|| true`)
    - .github/workflows/ci.yml (the docs-contracts-shift-left job 30-68, esp. the verify_core_liveview_runtime_free.sh step at 49-50)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md (D-04 layer 2)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md (the "verify_entitlement_sync_isolation.sh" section — scope to the gate-path files specifically)
  </read_first>
  <action>Create `scripts/ci/verify_entitlement_sync_isolation.sh` cloning `verify_core_liveview_runtime_free.sh`: keep the `set -euo pipefail`, the `^[^#]*` comment-anchor (so doc-comments/strings don't trip it), and the trailing `|| true` (so a clean grep doesn't fail `set -e`) — both load-bearing, copy verbatim. Scope the grep to the always-on gate-path files specifically (`accrue/lib/accrue/entitlements.ex`, `accrue/lib/accrue/entitlements/resolver/local_map.ex`, `accrue/lib/accrue/entitlements/resolver.ex`) and FAIL (`exit 1`) if any of them references `EntitlementSummary`, `StripeSync`, or `accrue_entitlement_summaries`. Print a clear FAIL message listing the offending lines and "verify_entitlement_sync_isolation: OK" on success. Make it executable. Wire it merge-blocking in `.github/workflows/ci.yml` as a new step in the `docs-contracts-shift-left` job, immediately after the `verify_core_liveview_runtime_free.sh` step (same idiom: `- name: ... (ENT-10 D-04)` / `run: bash scripts/ci/verify_entitlement_sync_isolation.sh`). Copy-from details are in the read_first files.</action>
  <verify>
    <automated>chmod +x scripts/ci/verify_entitlement_sync_isolation.sh && bash scripts/ci/verify_entitlement_sync_isolation.sh && grep -q 'verify_entitlement_sync_isolation' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_entitlement_sync_isolation.sh` exits 0 against the current (Plan 02-complete OR not-yet) tree and prints "verify_entitlement_sync_isolation: OK" — the gate-path files have no cache reference.
    - The script contains the `^[^#]*` anchor and a trailing `|| true` (grep both present: `grep -c '\^\[\^#\]\*' scripts/ci/verify_entitlement_sync_isolation.sh` ≥ 1; `grep -c '|| true' scripts/ci/verify_entitlement_sync_isolation.sh` ≥ 1).
    - `grep -q 'verify_entitlement_sync_isolation' .github/workflows/ci.yml` matches and the step is inside the `docs-contracts-shift-left` job.
    - Negative proof: temporarily adding `alias Accrue.Billing.EntitlementSummary` to entitlements.ex makes the script `exit 1` (executor confirms one-off in SUMMARY, then reverts).
  </acceptance_criteria>
  <done>New merge-blocking static isolation gate exists, passes clean, anchors/allowlist copied verbatim, wired into docs-contracts-shift-left.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capability labels → published support matrix | Drift between code labels and the public matrix would mislead operators about provider support. |
| Always-on gate path → advisory cache | Must remain uncoupled; a future refactor accidentally wiring the cache into the gate is the threat the static gate blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-09 | Elevation of Privilege | future code coupling the advisory cache into the gate path (fail-open via stale/partial cache) | mitigate | Merge-blocking static grep gate (`verify_entitlement_sync_isolation.sh`) fails the build if any gate-path file references the cache (ASVS V4, D-04 layer 2, Task 2). |
| T-127-10 | Tampering / Repudiation | support-matrix drift (over-promising Stripe-native as gate-influencing, or silently mutating the convergence contract) | mitigate | 3-way SSOT co-update + tightened drift gate keeps labels honest ("native (advisory)"/observational) and protects the local_mapping convergence row (Task 1, D-10). |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | Zero new deps this phase (RESEARCH §Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `bash scripts/ci/verify_processor_support_matrix.sh` exits 0.
- `bash scripts/ci/verify_entitlement_sync_isolation.sh` exits 0.
- `cd accrue && mix compile --warnings-as-errors` exits 0.
- `git diff .planning/processor-support-matrix.md` shows the convergence row unchanged.
- ci.yml docs-contracts-shift-left job includes the new isolation step.
</verification>

<success_criteria>
- New stripe_native_sync capability row across code + matrix + drift gate; convergence row untouched; matrix gate passes.
- New merge-blocking static isolation gate proves the cache is unreachable from the gate path and is wired into CI.
</success_criteria>

<output>
Create `.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-03-SUMMARY.md` when done.
</output>

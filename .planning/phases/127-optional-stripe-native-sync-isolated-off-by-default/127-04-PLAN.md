---
phase: 127-optional-stripe-native-sync-isolated-off-by-default
plan: 04
type: execute
wave: 3
depends_on: ["127-02", "127-03"]
files_modified:
  - accrue/guides/entitlements.md
  - accrue/guides/telemetry.md
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements: [ENT-10]
user_setup: []

must_haves:
  truths:
    - "guides/entitlements.md documents the optional Stripe-native sync: the eventual-consistency window, the 10-entitlement inline cap, the deferred lattice_stripe 1.2 paginated read, the enable steps (config :advisory + host Stripe Dashboard event-enable), and the plain 'advisory = observational, does not change entitled?' statement."
    - "guides/telemetry.md catalogs the new events: [:accrue, :entitlements, :sync] span, [:accrue, :entitlements, :summary_synced], [:accrue, :ops, :entitlement_summary_truncated], and the reused [:accrue, :webhooks, :stale_event]/orphan_entitlement_summary."
    - "verify_package_docs.sh has new needles asserting the entitlements.md Stripe-native section content; the doc verifier stays green."
  artifacts:
    - path: "accrue/guides/entitlements.md"
      provides: "Stripe-native advisory sync section (window + 10-cap + deferred 1.2 + enable steps + observational disclaimer)"
      contains: "entitlements.active_entitlement_summary.updated"
    - path: "accrue/guides/telemetry.md"
      provides: "catalog entries for the new sync telemetry/ops events"
      contains: "[:accrue, :entitlements, :sync]"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "new doc needles for the Stripe-native section"
      contains: "stripe_native_sync"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/guides/entitlements.md"
      via: "require_fixed needle for the Stripe-native section"
      pattern: "entitlements.active_entitlement_summary.updated"
---

<objective>
Document the optional Stripe-native sync end-to-end and pin the documentation with
doc-verifier needles. This is the human-facing half of D-12: the eventual-consistency
window, the 10-cap, the deferred 1.2 read, the enable steps, the observational
disclaimer, and the new telemetry/ops catalog — all referencing the finalized event
names from Plan 02 and the matrix row from Plan 03.

Purpose: SC#4 requires the eventual-consistency window and the 10-entitlement cap to be
documented with the full paginated read recorded as a deferred follow-up. The plain
"advisory = observational" statement neutralizes the only DX risk (an operator
expecting `:advisory` to alter gating).

Output: extended entitlements.md + telemetry.md guides + new verify_package_docs.sh needles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-PATTERNS.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-02-SUMMARY.md
@.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-03-SUMMARY.md

<interfaces>
<!-- Finalized event/config names to document (confirm against the Plan 02/03 SUMMARYs): -->
<!-- config: :entitlements -> stripe_native_sync: :disabled | :advisory -->
<!-- webhook: entitlements.active_entitlement_summary.updated (10 inline cap, has_more, url) -->
<!-- ledger event type: entitlements.summary.synced (on material change only) -->
<!-- telemetry span: [:accrue, :entitlements, :sync] -->
<!-- telemetry event: [:accrue, :entitlements, :summary_synced] (result: :written | :unchanged) -->
<!-- ops event: [:accrue, :ops, :entitlement_summary_truncated] (only when has_more) -->
<!-- reused: [:accrue, :webhooks, :stale_event] (object_type :entitlement_summary), [:accrue, :webhooks, :orphan_entitlement_summary] -->
<!-- deferred: full paginated read via GET /v1/entitlements/active_entitlements (lattice_stripe >= 1.2) -->

Existing doc needles (verify_package_docs.sh:118-124) — these MUST stay green:
  require_fixed entitlements.md 'entitled?'
  require_fixed entitlements.md 'Accrue.Plug.RequireEntitlement'
  require_fixed entitlements.md '[:accrue, :entitlements, :check]'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend entitlements.md + telemetry.md with the Stripe-native sync story (D-12)</name>
  <files>accrue/guides/entitlements.md, accrue/guides/telemetry.md</files>
  <read_first>
    - accrue/guides/entitlements.md (full — find the existing structure; append a new "Optional Stripe-native sync (advisory)" section, do not disturb the existing entitled?/Plug/[:accrue,:entitlements,:check] needles)
    - accrue/guides/telemetry.md (full — find the existing event catalog format to match)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-02-SUMMARY.md (confirm the FINAL event/config/ledger names as implemented)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-03-SUMMARY.md (confirm the matrix row label wording to reference)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md (D-12 — the required content list)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-RESEARCH.md (Pitfall 2 the 10-cap, Pitfall 4 eventual consistency, the deferred 1.2 read)
  </read_first>
  <action>Append an "Optional Stripe-native sync (advisory)" section to `guides/entitlements.md` covering, per D-12: (1) how to enable — set `:entitlements` `stripe_native_sync: :advisory` (off/`:disabled` by default) AND the host-owned step of enabling the `entitlements.active_entitlement_summary.updated` event on their Stripe Dashboard webhook endpoint; (2) the plain observational disclaimer — `:advisory` records summaries to an advisory cache for audit/telemetry/the admin read-seam and does NOT change `entitled?`/`has_active_plan?` decisions in v1.x (local mapping stays canonical); (3) the eventual-consistency window — webhooks have no order/lag SLA, local-first canonical means a stale cache never produces a wrong gate decision; (4) the 10-entitlement inline cap — `has_more`/`truncated`, partial cache surfaced via the typed column and `[:accrue, :ops, :entitlement_summary_truncated]`; (5) the deferred full paginated read (`GET /v1/entitlements/active_entitlements`) as a follow-up depending on `lattice_stripe ≥ 1.2`. Reference the new `entitlements.stripe_native_sync` capability-matrix row label from Plan 03. In `guides/telemetry.md`, catalog the new events in the existing format: `[:accrue, :entitlements, :sync]` span (state-change mirror of `:check`), `[:accrue, :entitlements, :summary_synced]` (`result: :written | :unchanged`), `[:accrue, :ops, :entitlement_summary_truncated]` (only when `has_more`), and the reused `[:accrue, :webhooks, :stale_event]` (`object_type: :entitlement_summary`) + `[:accrue, :webhooks, :orphan_entitlement_summary]`. Use the EXACT names confirmed in the Plan 02 SUMMARY. Do not disturb existing needles.</action>
  <verify>
    <automated>cd accrue && mix docs 2>&1 | grep -qiv "error" && grep -q 'entitlements.active_entitlement_summary.updated' guides/entitlements.md && grep -q '\[:accrue, :entitlements, :sync\]' guides/telemetry.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'stripe_native_sync' accrue/guides/entitlements.md` and `grep -q 'entitlements.active_entitlement_summary.updated' accrue/guides/entitlements.md` both match.
    - The observational disclaimer is present: `grep -i 'does not change\|does NOT change' accrue/guides/entitlements.md` matches near the new section.
    - The 10-cap + deferred read are documented: `grep -i 'has_more\|10 entitlement\|truncat' accrue/guides/entitlements.md` matches AND `grep -i '1.2\|paginated\|active_entitlements' accrue/guides/entitlements.md` matches.
    - `grep -q '\[:accrue, :entitlements, :sync\]' accrue/guides/telemetry.md` and `grep -q 'entitlement_summary_truncated' accrue/guides/telemetry.md` both match.
    - `cd accrue && mix docs` builds entitlements.html and telemetry.html without error.
    - Existing needles intact: `grep -c 'entitled?' accrue/guides/entitlements.md` ≥ 1, `grep -c 'Accrue.Plug.RequireEntitlement' accrue/guides/entitlements.md` ≥ 1, `grep -c '\[:accrue, :entitlements, :check\]' accrue/guides/entitlements.md` ≥ 1.
  </acceptance_criteria>
  <done>Both guides document the Stripe-native advisory sync (enable steps, observational disclaimer, eventual-consistency window, 10-cap, deferred 1.2 read, new telemetry/ops events); docs build clean; existing needles intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add verify_package_docs.sh needles for the Stripe-native section (D-12)</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh (the entitlements-spine needle block at 118-124 — the require_fixed idiom; add new needles alongside)
    - accrue/guides/entitlements.md (the section written in Task 1 — pick stable, byte-exact substrings to pin)
    - accrue/guides/telemetry.md (the new event catalog — pin one stable substring)
    - .planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-CONTEXT.md (D-12 — "Extend scripts/ci/verify_package_docs.sh needles")
  </read_first>
  <action>Add new `require_fixed` needles to `verify_package_docs.sh` (using the existing 118-124 idiom) pinning the Stripe-native section content so it can't silently regress: at minimum (1) `entitlements.md` contains `stripe_native_sync` (or the byte-exact enable substring), (2) `entitlements.md` contains `entitlements.active_entitlement_summary.updated`, (3) `entitlements.md` contains the byte-exact observational-disclaimer substring, and (4) `telemetry.md` contains `[:accrue, :entitlements, :sync]`. Choose substrings that are byte-exact present in the Task 1 output (read the written guides to confirm exact text before pinning). Do not remove or alter the existing needles.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh && grep -q 'stripe_native_sync\|entitlements.active_entitlement_summary.updated' scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_package_docs.sh` exits 0 (all needles, old + new, satisfied).
    - `grep -c 'entitlements.active_entitlement_summary.updated\|stripe_native_sync' scripts/ci/verify_package_docs.sh` ≥ 1 (new needle added).
    - `grep -c '\[:accrue, :entitlements, :sync\]' scripts/ci/verify_package_docs.sh` ≥ 1 (telemetry needle added).
    - The existing entitlements-spine needles (lines 118-124) are byte-for-byte intact (`git diff` shows only additions, no deletions in that block).
  </acceptance_criteria>
  <done>New doc needles pin the Stripe-native section + telemetry catalog; verify_package_docs.sh stays green; no existing needle altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator mental model | Misleading docs (e.g. implying `:advisory` gates) are a DX/authorization-expectation risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-11 | Information Disclosure / Repudiation | operator misunderstands `:advisory` as gate-influencing (expectation gap that could mask a real access-control assumption) | mitigate | The plain "advisory = observational, does not change entitled?" disclaimer is documented AND pinned by a doc-verifier needle so it cannot silently regress (D-03/D-12, Task 1 + Task 2). |
| T-127-12 | Repudiation | telemetry/ops events undocumented → operators can't observe sync health/staleness | mitigate | telemetry.md catalogs all new + reused events with their result/has_more semantics (D-09/D-12, Task 1). |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | Zero new deps this phase (docs-only plan). |
</threat_model>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits 0.
- `cd accrue && mix docs` builds entitlements.html + telemetry.html without error.
- entitlements.md documents the window + 10-cap + deferred 1.2 + enable steps + observational disclaimer.
- telemetry.md catalogs the new sync/ops events.
</verification>

<success_criteria>
- The optional Stripe-native sync is documented end-to-end with the observational disclaimer, eventual-consistency window, 10-cap, deferred 1.2 read, and enable steps.
- New telemetry/ops events are cataloged.
- verify_package_docs.sh stays green with new needles pinning the section.
</success_criteria>

<output>
Create `.planning/phases/127-optional-stripe-native-sync-isolated-off-by-default/127-04-SUMMARY.md` when done.
</output>

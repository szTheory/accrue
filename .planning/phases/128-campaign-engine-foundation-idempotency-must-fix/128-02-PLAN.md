---
phase: 128-campaign-engine-foundation-idempotency-must-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/priv/repo/migrations/20260525120000_add_dunning_campaign_started_at_to_subscriptions.exs
  - accrue/lib/accrue/billing/subscription.ex
autonomous: true
requirements: [DUN-05]
must_haves:
  truths:
    - "A single nullable anchor column `dunning_campaign_started_at :utc_datetime_usec` exists on accrue_subscriptions (column add, NOT a new table) (D-08)."
    - "The anchor is castable through force_status_changeset/2 for the recovery-CLEAR path (D-08, D-12) and present in @cast_fields."
    - "Subscription.dunning_campaign_active?/1 returns true iff the anchor is a non-nil DateTime (D-08)."
    - "Migration is forward-only and nullable — existing rows survive with nil anchor, no backfill (RESEARCH Runtime State Inventory)."
  artifacts:
    - path: "accrue/priv/repo/migrations/20260525120000_add_dunning_campaign_started_at_to_subscriptions.exs"
      provides: "Nullable forward-only column add for the campaign anchor"
      contains: "add :dunning_campaign_started_at"
    - path: "accrue/lib/accrue/billing/subscription.ex"
      provides: "dunning_campaign_started_at field + @cast_fields entry + dunning_campaign_active?/1 predicate"
      contains: "dunning_campaign_active?"
  key_links:
    - from: "accrue/lib/accrue/billing/subscription.ex schema"
      to: "accrue_subscriptions.dunning_campaign_started_at"
      via: "field(:dunning_campaign_started_at, :utc_datetime_usec)"
      pattern: "field\\(:dunning_campaign_started_at"
    - from: "force_status_changeset/2"
      to: "dunning_campaign_started_at"
      via: "@cast_fields word-list entry (recovery-clear cast path)"
      pattern: "dunning_campaign_started_at"
---

<objective>
Add the single nullable anchor column `dunning_campaign_started_at` to `accrue_subscriptions` (forward-only migration), wire it into the Subscription schema + `@cast_fields` + the lifecycle predicates, and add `Subscription.dunning_campaign_active?/1`. This anchor is the campaign identity and the first-transition edge signal that everything downstream keys on (DUN-05 foundation, D-08).

Purpose: The D-09 atomic `update_all WHERE is_nil(dunning_campaign_started_at)` elector (Plan 06), the D-11 worker cancel-guard (Plan 05), and the D-12 cancel-on-recovery clear (Plan 06) all depend on this column existing, being castable for the clear path, and the predicate. The migration MUST land in Wave 1 because `test_helper.exs` runs all migrations at boot — every integration test in later plans needs the column.
Output: A new migration file and a modified `accrue/lib/accrue/billing/subscription.ex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-campaign-engine-foundation-idempotency-must-fix/128-CONTEXT.md
@.planning/phases/128-campaign-engine-foundation-idempotency-must-fix/128-PATTERNS.md

<interfaces>
<!-- Anchor contract (D-08) consumed by Plans 05/06. -->
Column:      accrue_subscriptions.dunning_campaign_started_at :utc_datetime_usec, null: true
Field:       Subscription.dunning_campaign_started_at
Predicate:   Subscription.dunning_campaign_active?(sub) :: boolean  (true iff anchor is a non-nil DateTime)
Clear path:  force_status_changeset(sub, %{dunning_campaign_started_at: nil})  (D-12 recovery clear — cast, in @cast_fields)
Start path:  Repo.update_all ... set: [dunning_campaign_started_at: now]  (D-09 — sibling update_all, NOT a cast; Plan 06)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forward-only nullable migration for the campaign anchor (D-08)</name>
  <files>accrue/priv/repo/migrations/20260525120000_add_dunning_campaign_started_at_to_subscriptions.exs</files>
  <read_first>
    - accrue/priv/repo/migrations/20260414130300_add_dunning_and_pause_columns_to_subscriptions.exs (clone target — the migration that added the sibling `dunning_sweep_attempted_at`; column-add pattern at :20-27)
    - .planning/phases/128-campaign-engine-foundation-idempotency-must-fix/128-PATTERNS.md (§ migration — note: do NOT add the partial index the analog adds)
  </read_first>
  <action>
    Create `accrue/priv/repo/migrations/20260525120000_add_dunning_campaign_started_at_to_subscriptions.exs` (timestamp prefix MUST be > the latest existing migration `20260524120000`; `20260525120000` satisfies this — confirm with `ls priv/repo/migrations/ | tail -1` before writing and bump if needed). Clone the column-add `change/0` from the analog: `alter table(:accrue_subscriptions) do add :dunning_campaign_started_at, :utc_datetime_usec, null: true end`. Forward-only, nullable. Do NOT add any index — D-08 says no index is required for correctness (the optional partial `WHERE dunning_campaign_started_at IS NOT NULL` is explicitly deferred to Phase 129). Add a moduledoc noting "nullable, forward-only, mirrors `dunning_sweep_attempted_at`; no backfill needed."
  </action>
  <verify>
    <automated>cd accrue && MIX_ENV=test mix ecto.migrate 2>&1 | tail -8 && MIX_ENV=test mix ecto.rollback --step 0 >/dev/null 2>&1; echo "migrate exit gate"</automated>
  </verify>
  <acceptance_criteria>
    - `ls accrue/priv/repo/migrations/ | grep dunning_campaign_started_at` matches the new file, and its timestamp prefix is numerically greater than `20260524120000`.
    - The migration body contains `add :dunning_campaign_started_at, :utc_datetime_usec, null: true`.
    - The migration body does NOT contain `create index` / `create unique_index` (no index per D-08).
    - `cd accrue && MIX_ENV=test mix ecto.migrate` applies cleanly (no error).
  </acceptance_criteria>
  <done>The nullable forward-only anchor column migration applies cleanly against the test repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add anchor field, cast entry, and dunning_campaign_active?/1 to Subscription (D-08)</name>
  <files>accrue/lib/accrue/billing/subscription.ex</files>
  <read_first>
    - accrue/lib/accrue/billing/subscription.ex (the file being modified — field block :57-86, `dunning_sweep_attempted_at` field at :65, `@cast_fields` word-list at :88 with the field listed at :91, `force_status_changeset/2` at :106-113, lifecycle predicates :138-256 incl. `dunning_sweepable?/1` at :239-241 and `past_due?/1` at :154-156)
    - .planning/phases/128-campaign-engine-foundation-idempotency-must-fix/128-PATTERNS.md (§ "lib/accrue/billing/subscription.ex — D-08" — the dual-clause predicate convention)
  </read_first>
  <behavior>
    - `Subscription.dunning_campaign_active?/1` returns `true` for a `%Subscription{dunning_campaign_started_at: %DateTime{}}` and for a bare map with a `%DateTime{}` anchor.
    - Returns `false` for `nil` anchor and for any other shape (catch-all).
    - `force_status_changeset(sub, %{dunning_campaign_started_at: nil})` casts the anchor (clear path) — proven by changeset inspection.
  </behavior>
  <action>
    Modify `accrue/lib/accrue/billing/subscription.ex` per D-08, cloning the documented self-precedents:
    1. Add `field(:dunning_campaign_started_at, :utc_datetime_usec)` immediately after the `dunning_sweep_attempted_at` field (:65).
    2. Add `dunning_campaign_started_at` to the `@cast_fields` word-list (:88, beside `dunning_sweep_attempted_at`) so BOTH `force_status_changeset/2` and `changeset/2` can cast it (the recovery-CLEAR path D-12 casts it; the START path D-09 is a sibling `update_all`, NOT a cast — do NOT add the start to a changeset).
    3. Add the predicate `dunning_campaign_active?/1` beside the existing lifecycle predicates (after :256), using the established dual-clause convention: `%__MODULE__{dunning_campaign_started_at: %DateTime{}}` -> true, bare-map `%{dunning_campaign_started_at: %DateTime{}}` -> true, catch-all `_` -> false.
    Add a unit test (either inline in the existing subscription test or a focused new one) proving the three predicate behaviors and that the anchor casts through `force_status_changeset/2`. Do NOT add any start-path logic, telemetry, or anchor-on-status-flip changeset (RESEARCH anti-pattern — the start is a sibling `update_all` in Plan 06).
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_test.exs --seed 0 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "field(:dunning_campaign_started_at, :utc_datetime_usec)" accrue/lib/accrue/billing/subscription.ex` matches.
    - `grep -c "dunning_campaign_started_at" accrue/lib/accrue/billing/subscription.ex` is >= 3 (field + @cast_fields + predicate).
    - `grep -n "def dunning_campaign_active?" accrue/lib/accrue/billing/subscription.ex` shows the dual-clause + catch-all predicate.
    - `cd accrue && mix test test/accrue/billing/subscription_test.exs --seed 0` passes (subscription suite green with the new predicate covered).
    - `cd accrue && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Subscription carries the anchor field (castable for the clear path) and `dunning_campaign_active?/1`; subscription tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook reducer → DB | The anchor column is written/cleared by the webhook reducer (Plans 05/06). This plan only defines the column + cast + predicate; the race-safe write is Plan 06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-03 | Tampering / data integrity | `dunning_campaign_started_at` cast surface | mitigate | The anchor is added to `@cast_fields` ONLY for the recovery-CLEAR path; the first-transition START is a sibling `update_all` (Plan 06, D-09) so it never contends with `optimistic_lock(:lock_version)` (RESEARCH anti-pattern). This plan deliberately ships no start-on-changeset path. |
| T-128-04 | Information Disclosure | anchor column contents | accept | Column stores a UTC timestamp only — no PII, no Stripe secrets (CLAUDE.md constraint). Mirrors `dunning_sweep_attempted_at`. Low risk. |
| T-128-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (no new deps). |
</threat_model>

<verification>
- `cd accrue && MIX_ENV=test mix ecto.migrate` applies the column.
- `cd accrue && mix test test/accrue/billing/subscription_test.exs --seed 0` (this plan's gate).
- Wave merge (after all Wave 1 plans): `cd accrue && mix test --seed 0`.
</verification>

<success_criteria>
- The nullable anchor column exists and migrates cleanly; `Subscription.dunning_campaign_active?/1` and the clear-path cast are available for Plans 05/06.
- No start-path / telemetry / index code (scope + boundary honored).
</success_criteria>

<output>
Create `.planning/phases/128-campaign-engine-foundation-idempotency-must-fix/128-02-SUMMARY.md` when done.
</output>

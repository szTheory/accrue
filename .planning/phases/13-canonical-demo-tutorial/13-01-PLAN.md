---
phase: 13-canonical-demo-tutorial
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - examples/accrue_host/demo/command_manifest.exs
  - examples/accrue_host/mix.exs
  - scripts/ci/accrue_host_uat.sh
autonomous: true
requirements:
  - DEMO-04
  - DEMO-05
  - DEMO-06
must_haves:
  truths:
    - "A maintainer can run `cd examples/accrue_host && mix verify` for the focused tutorial proof suite."
    - "A maintainer can run `cd examples/accrue_host && mix verify.full` as the CI-equivalent local gate."
    - "The repo-root `bash scripts/ci/accrue_host_uat.sh` wrapper delegates to the same full contract instead of maintaining a second command graph."
  artifacts:
    - path: "examples/accrue_host/demo/command_manifest.exs"
      provides: "Shared ordered command and mode metadata for First run, Seeded history, verify, and verify.full."
      min_lines: 25
    - path: "examples/accrue_host/mix.exs"
      provides: "Host-local Mix aliases for setup, verify, and verify.full."
      contains: "\"verify\""
    - path: "scripts/ci/accrue_host_uat.sh"
      provides: "Thin repo-root wrapper around the host-local full verification contract."
      contains: "mix verify.full"
  key_links:
    - from: "scripts/ci/accrue_host_uat.sh"
      to: "examples/accrue_host/mix.exs"
      via: "repo-root wrapper calls host-local verify.full"
      pattern: "mix verify.full"
---

<objective>
Repair and lock the canonical full-gate contract before later manifest-driven docs and tutorial work begins.

Purpose: Resolve the Wave 0 blocker from research and validation by making `mix verify.full` truthful before the phase teaches it.
Output: A repaired host-local `mix setup` / `mix verify` / `mix verify.full` contract, a thin repo-root wrapper, and the manifest metadata later docs tests will consume.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
@.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
@.planning/phases/13-canonical-demo-tutorial/13-VALIDATION.md
@.planning/phases/13-canonical-demo-tutorial/13-PATTERNS.md
@examples/accrue_host/mix.exs
@scripts/ci/accrue_host_uat.sh

<interfaces>
From `examples/accrue_host/mix.exs`:
```elixir
defp aliases do
  [
    setup: ["deps.get", "ecto.setup", "assets.setup", "assets.build"],
    "ecto.setup": ["ecto.create", "ecto.migrate", "run priv/repo/seeds.exs"],
    test: ["ecto.create --quiet", "ecto.migrate --quiet", "test"]
  ]
end
```

From `scripts/ci/accrue_host_uat.sh`:
```bash
port="${ACCRUE_HOST_PORT:-4100}"
browser_port="${ACCRUE_HOST_BROWSER_PORT:-4101}"
...
bash scripts/ci/accrue_host_uat.sh
```
</interfaces>

<source_audit>
## Plan-Set Coverage Audit

GOAL:
- Canonical local evaluation path and tutorial proof are covered by 13-01, 13-02, and 13-03.

REQ:
- `DEMO-01` -> 13-03
- `DEMO-02` -> 13-03
- `DEMO-03` -> 13-03
- `DEMO-04` -> 13-01, 13-03
- `DEMO-05` -> 13-01, 13-03
- `DEMO-06` -> 13-01, 13-02
- `ADOPT-02` -> 13-03

RESEARCH:
- Host-local `mix setup` / `mix verify` / `mix verify.full` contract -> 13-01
- Manifest-backed parity checks -> 13-02
- Human-written README + First Hour mirror with `First run` and `Seeded history` -> 13-03
- Thin repo-root wrapper around host-local full gate -> 13-01

CONTEXT:
- D-01, D-02, D-03 -> 13-03
- D-04, D-05, D-06, D-07, D-08 -> 13-03
- D-09, D-10, D-11, D-12, D-13 -> 13-01
- D-14, D-15, D-16, D-17 -> 13-02
- D-18 -> 13-01 and 13-03
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared demo command manifest</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
.planning/phases/13-canonical-demo-tutorial/13-PATTERNS.md
accrue/test/accrue/docs/first_hour_guide_test.exs
examples/accrue_host/README.md
accrue/guides/first_hour.md
  </read_first>
  <files>examples/accrue_host/demo/command_manifest.exs</files>
  <behavior>
    - Test 1: The manifest exports `First run` and `Seeded history` mode labels plus their ordered command lists.
    - Test 2: The manifest exports the exact command labels `mix setup`, `mix verify`, `mix verify.full`, and `bash scripts/ci/accrue_host_uat.sh`.
    - Test 3: The manifest distinguishes public-boundary commands from seeded-history-only evaluation commands.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'First run|Seeded history' examples/accrue_host/demo/command_manifest.exs` returns both labels.
    - `rg -n 'mix setup|mix verify|mix verify\\.full|bash scripts/ci/accrue_host_uat\\.sh' examples/accrue_host/demo/command_manifest.exs` returns all four command labels.
    - `rg -n 'customer.subscription.created|/billing|/webhooks/stripe' examples/accrue_host/demo/command_manifest.exs` returns the canonical story artifacts.
  </acceptance_criteria>
  <action>Create `examples/accrue_host/demo/command_manifest.exs` as the single structured source of truth per D-14. Follow the pattern-map analog in `accrue/test/accrue/docs/first_hour_guide_test.exs`: externalize ordered command strings, labels, and public-versus-seeded boundaries into repo-readable data so the docs-contract layer can consume one source instead of maintaining parallel literals. Store concrete ordered data for the two named modes from D-04: `First run` with `cd examples/accrue_host`, `mix setup`, `mix phx.server`, first Fake-backed subscription through `AccrueHost.Billing`, one signed `/webhooks/stripe` proof, `/billing` inspection, and `mix verify`; and `Seeded history` with the deterministic replay/history path used for browser smoke. Include explicit mode labels for Fake-backed local evaluation, CI-equivalent validation, Hex smoke, and production setup so downstream docs/tests can read exact values without scraping prose. Do not generate Markdown from this file and do not include private setup steps inside the `First run` path per D-05, D-07, and D-15.</action>
  <verify>
    <automated>test -f examples/accrue_host/demo/command_manifest.exs && rg -n 'First run|Seeded history|mix verify.full' examples/accrue_host/demo/command_manifest.exs</automated>
  </verify>
  <done>The manifest exists, exposes the locked labels and commands, and cleanly separates public `First run` from seeded evaluation-only steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the host-local `mix verify` and `mix verify.full` contract</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
examples/accrue_host/mix.exs
scripts/ci/accrue_host_uat.sh
examples/accrue_host/test/accrue_host/billing_facade_test.exs
examples/accrue_host/test/accrue_host_web/subscription_flow_test.exs
examples/accrue_host/test/accrue_host_web/webhook_ingest_test.exs
examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs
examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
examples/accrue_host/test/install_boundary_test.exs
  </read_first>
  <files>examples/accrue_host/mix.exs</files>
  <behavior>
    - Test 1: `cd examples/accrue_host && mix verify` runs the six focused proof files named in Phase 13 validation.
    - Test 2: `cd examples/accrue_host && mix verify.full` composes `mix verify`, compile warnings, asset build, bounded dev boot, and browser smoke.
    - Test 3: The alias graph is package-local and memorable; it does not introduce `make`, `just`, or many public split-by-concern commands.
  </behavior>
  <acceptance_criteria>
    - `rg -n '"verify"' examples/accrue_host/mix.exs` finds a `verify` alias.
    - `rg -n '"verify\\.full"' examples/accrue_host/mix.exs` finds a `verify.full` alias.
    - `rg -n 'install_boundary_test\\.exs|billing_facade_test\\.exs|subscription_flow_test\\.exs|webhook_ingest_test\\.exs|admin_webhook_replay_test\\.exs|admin_mount_test\\.exs' examples/accrue_host/mix.exs` finds all focused proof files inside the alias implementation.
  </acceptance_criteria>
  <action>Update `examples/accrue_host/mix.exs` per D-09 through D-13 so `mix setup` remains the clone-to-ready entrypoint from D-10, `mix verify` runs the focused tutorial proof suite from D-11, and `mix verify.full` becomes the CI-equivalent local gate from D-12. Keep the implementation primarily in package-local Mix aliases. The focused suite must include `test/install_boundary_test.exs`, `test/accrue_host/billing_facade_test.exs`, `test/accrue_host_web/subscription_flow_test.exs`, `test/accrue_host_web/webhook_ingest_test.exs`, `test/accrue_host_web/admin_webhook_replay_test.exs`, and `test/accrue_host_web/admin_mount_test.exs`. The full alias must compose the focused suite with `mix compile --warnings-as-errors`, `mix assets.build`, bounded `mix phx.server` readiness, and browser smoke. Preserve the current env switches `ACCRUE_HOST_SKIP_DEV_BOOT`, `ACCRUE_HOST_SKIP_BROWSER`, `ACCRUE_HOST_PORT`, and `ACCRUE_HOST_BROWSER_PORT` so CI and local runs stay aligned.</action>
  <verify>
    <automated>cd examples/accrue_host && mix verify</automated>
  </verify>
  <done>`examples/accrue_host` exposes the exact host-local commands the tutorial will teach, and `mix verify` is the short focused proof while `mix verify.full` is the full local gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collapse the repo-root wrapper into a thin delegate</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
scripts/ci/accrue_host_uat.sh
examples/accrue_host/mix.exs
.github/workflows/accrue_host_uat.yml
.github/workflows/ci.yml
  </read_first>
  <files>scripts/ci/accrue_host_uat.sh</files>
  <behavior>
    - Test 1: The wrapper still works from repo root with `bash scripts/ci/accrue_host_uat.sh`.
    - Test 2: The wrapper delegates to `cd examples/accrue_host && mix verify.full` instead of embedding a second public contract.
    - Test 3: The wrapper preserves the same env passthrough needed by GitHub Actions and local runs.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'mix verify\\.full' scripts/ci/accrue_host_uat.sh` shows host-local delegation.
    - `rg -n 'ACCRUE_HOST_PORT|ACCRUE_HOST_BROWSER_PORT|ACCRUE_HOST_SKIP_DEV_BOOT|ACCRUE_HOST_SKIP_BROWSER' scripts/ci/accrue_host_uat.sh` shows env passthrough remains documented or forwarded.
    - `bash scripts/ci/accrue_host_uat.sh` exits 0.
  </acceptance_criteria>
  <action>Refactor `scripts/ci/accrue_host_uat.sh` into the thin repo-root wrapper required by D-13. Keep repo-root conveniences such as `pg_isready`, `host_dir`, and GitHub Actions compatibility, but remove the duplicated public command graph so the script ends by delegating to `cd "$host_dir" && mix verify.full`. Repair or reconcile the current bounded dev-boot smoke failure with `ACCRUE-DX-MIGRATIONS-PENDING` inside this Wave 0 work before finalizing the task, because the research decision is now explicit: `mix verify.full` must be repaired before it becomes canonical. Do not introduce `make`, `just`, or a new public top-level command.</action>
  <verify>
    <automated>bash scripts/ci/accrue_host_uat.sh</automated>
  </verify>
  <done>The repo-root wrapper is a thin delegate to the host-local full contract and no longer risks drifting from the package-local verification path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer shell -> host verification contract | Untrusted local environment variables and stale local state can make the documented verification path lie. |
| repo-root wrapper -> host-local Mix aliases | Two public command graphs can diverge and silently stop validating the same security-critical webhook/admin proof path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | `examples/accrue_host/mix.exs` aliases | mitigate | Make `mix verify` and `mix verify.full` explicit and grep-verifiable so command drift is caught in code review and by docs tests. |
| T-13-02 | R | `examples/accrue_host/demo/command_manifest.exs` | mitigate | Record exact ordered commands and labels in one manifest so README/guide/wrapper parity can be proven from tests instead of memory. |
| T-13-03 | D | `scripts/ci/accrue_host_uat.sh` | mitigate | Keep the wrapper thin and fail closed on any nonzero `mix verify.full` result so CI cannot bypass the full gate. |
| T-13-04 | E | full verification contract | mitigate | Keep signed webhook ingest, admin mount, and admin replay inside the full contract so the command cannot pass without the authenticated admin and webhook proof surfaces. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix verify` after Task 2. Run `cd examples/accrue_host && mix verify.full` and `bash scripts/ci/accrue_host_uat.sh` after Task 3. Capture the exact command names, repaired full-gate behavior, and any env toggles in the phase summary.
</verification>

<success_criteria>
- `examples/accrue_host/demo/command_manifest.exs` exists with `First run` and `Seeded history`.
- `examples/accrue_host/mix.exs` exposes `mix verify` and `mix verify.full`.
- `bash scripts/ci/accrue_host_uat.sh` delegates to the same full contract and passes locally.
</success_criteria>

<output>
After completion, create `.planning/phases/13-canonical-demo-tutorial/13-01-SUMMARY.md`
</output>

---
phase: 13-canonical-demo-tutorial
plan: 02
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - accrue/test/accrue/docs/first_hour_guide_test.exs
  - accrue/test/accrue/docs/canonical_demo_contract_test.exs
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - DEMO-05
  - DEMO-06
must_haves:
  truths:
    - "Maintainers can detect drift between the canonical demo contract and the tutorial docs before release."
    - "The docs contract forbids private setup surfaces in the public `First run` story."
    - "Mode labels and command order stay in parity across tests and shell verification."
  artifacts:
    - path: "accrue/test/accrue/docs/canonical_demo_contract_test.exs"
      provides: "Manifest-backed parity coverage for README, First Hour guide, and wrapper labels/order."
      min_lines: 40
    - path: "accrue/test/accrue/docs/first_hour_guide_test.exs"
      provides: "Public-boundary and forbidden-surface contract checks against the guide."
      contains: "First run"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Fixed-invariant package-doc verification for anchors, labels, links, and versions."
      contains: "first_hour.md"
  key_links:
    - from: "accrue/test/accrue/docs/canonical_demo_contract_test.exs"
      to: "examples/accrue_host/demo/command_manifest.exs"
      via: "reads manifest to assert labels and command order"
      pattern: "command_manifest"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      via: "shell verifier is exercised by ExUnit"
      pattern: "verify_package_docs"
---

<objective>
Add the drift guard layer that enforces parity between the manifest, the human-written docs, and the shell verifier.

Purpose: Implement D-14 through D-17 so Phase 13 does not rely on memory or grep-only checks to keep the demo path coherent.
Output: Manifest-backed ExUnit contracts plus narrow shell verification for version, links, and anchor invariants.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
@.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
@examples/accrue_host/demo/command_manifest.exs
@accrue/test/accrue/docs/first_hour_guide_test.exs
@accrue/test/accrue/docs/package_docs_verifier_test.exs
@scripts/ci/verify_package_docs.sh

<interfaces>
From `accrue/test/accrue/docs/first_hour_guide_test.exs`:
```elixir
@ordered_steps [
  "mix deps.get",
  "mix accrue.install",
  "config/runtime.exs",
  "mix ecto.migrate",
  "Oban",
  "/webhooks/stripe",
  ~s|accrue_admin "/billing"|,
  "MyApp.Billing.subscribe",
  "customer.subscription.created",
  "/billing",
  "mix test"
]
```

From `scripts/ci/verify_package_docs.sh`:
```bash
bash ../scripts/ci/verify_package_docs.sh
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded ordered-step assertions with manifest-backed docs contracts</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
.planning/phases/13-canonical-demo-tutorial/13-PATTERNS.md
examples/accrue_host/demo/command_manifest.exs
accrue/test/accrue/docs/first_hour_guide_test.exs
accrue/test/accrue/docs/package_docs_verifier_test.exs
examples/accrue_host/README.md
accrue/guides/first_hour.md
  </read_first>
  <files>accrue/test/accrue/docs/first_hour_guide_test.exs, accrue/test/accrue/docs/canonical_demo_contract_test.exs</files>
  <behavior>
    - Test 1: The guide test reads canonical ordered steps from the manifest instead of embedding a stale command list.
    - Test 2: A new canonical demo contract test asserts parity between `examples/accrue_host/README.md`, `accrue/guides/first_hour.md`, and the wrapper-facing command labels.
    - Test 3: The public `First run` contract rejects private schema/module surfaces and keeps `Seeded history` explicitly separate.
  </behavior>
  <acceptance_criteria>
    - `test -f accrue/test/accrue/docs/canonical_demo_contract_test.exs` succeeds.
    - `rg -n 'command_manifest|First run|Seeded history|mix verify\\.full' accrue/test/accrue/docs/canonical_demo_contract_test.exs accrue/test/accrue/docs/first_hour_guide_test.exs` returns matches in both files.
    - `rg -n 'Accrue\\.Billing\\.Customer|Accrue\\.Billing\\.Subscription|Accrue\\.Webhook\\.WebhookEvent' accrue/test/accrue/docs/first_hour_guide_test.exs` returns forbidden-surface assertions, not doc content.
  </acceptance_criteria>
  <action>Update `accrue/test/accrue/docs/first_hour_guide_test.exs` so ordered-step, public-surface, and forbidden-surface checks are driven from `examples/accrue_host/demo/command_manifest.exs` per D-14 and D-16. Add `accrue/test/accrue/docs/canonical_demo_contract_test.exs` using `accrue/test/accrue/docs/package_docs_verifier_test.exs` as the PATTERNS analog called out in `13-PATTERNS.md`: copy the verifier test shape of one green-path `System.cmd("bash", [...], stderr_to_stdout: true)` assertion plus one targeted tmp-tree failure-path test that mutates copied fixture docs and asserts on the failing output, then adapt that pattern from shell-invariant verification to manifest-backed docs parity. The new contract test must assert exact parity for the locked labels `First run`, `Seeded history`, `mix verify`, `mix verify.full`, and `bash scripts/ci/accrue_host_uat.sh` across the host README, the First Hour guide, and the repo-root wrapper reference. Keep the docs human-written; the tests should validate order and boundary semantics, not generate prose, per D-15 through D-17.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs test/accrue/docs/canonical_demo_contract_test.exs</automated>
  </verify>
  <done>The canonical tutorial parity and forbidden-surface rules are enforced from the shared manifest rather than repeated as hand-maintained lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the shell verifier for fixed invariants only</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
scripts/ci/verify_package_docs.sh
accrue/test/accrue/docs/package_docs_verifier_test.exs
examples/accrue_host/README.md
accrue/guides/first_hour.md
accrue/README.md
  </read_first>
  <files>scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <behavior>
    - Test 1: The shell verifier checks version/link/anchor invariants and the required command labels, but not full tutorial semantics.
    - Test 2: The ExUnit wrapper proves both success and at least one targeted failure mode under a temporary copied docs tree.
    - Test 3: The shell verifier remains narrow enough that ExUnit owns the ordered-step and boundary semantics.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'First run|Seeded history|mix verify|mix verify\\.full|accrue_host_uat\\.sh' scripts/ci/verify_package_docs.sh` returns the required labels or anchors being checked.
    - `rg -n 'ROOT_DIR|first_hour\\.md|README\\.md' scripts/ci/verify_package_docs.sh accrue/test/accrue/docs/package_docs_verifier_test.exs` shows temp-tree verifier coverage remains supported.
    - `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits 0.
  </acceptance_criteria>
  <action>Extend `scripts/ci/verify_package_docs.sh` and `accrue/test/accrue/docs/package_docs_verifier_test.exs` to enforce only the narrow fixed invariants allowed by D-17: current package versions, required links between `accrue/README.md`, `guides/first_hour.md`, and the host demo, required anchors or section labels for `First run` and `Seeded history`, and presence of the exact verification command labels `mix verify`, `mix verify.full`, and `bash scripts/ci/accrue_host_uat.sh`. Keep ordered-command and public/private-boundary semantics in ExUnit; do not turn the shell verifier into the primary drift guard.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs</automated>
  </verify>
  <done>The shell verifier catches narrow docs drift, while ExUnit remains the authoritative parity and semantics layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/tests -> manifest | If the tests stop reading the manifest, docs can drift while still appearing reviewed. |
| shell verifier -> published docs tree | Over-broad shell checks can create false trust or brittle release gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | T | docs contract tests | mitigate | Read canonical labels and ordered steps from `examples/accrue_host/demo/command_manifest.exs` so changes must flow through one source of truth. |
| T-13-06 | R | README / First Hour parity | mitigate | Add one dedicated `canonical_demo_contract_test.exs` that checks both surfaces and records exact mismatches in failing assertions. |
| T-13-07 | D | `scripts/ci/verify_package_docs.sh` | mitigate | Keep shell checks narrow and deterministic so the release gate cannot be destabilized by prose-level variability. |
| T-13-08 | I | public tutorial boundaries | mitigate | Assert forbidden private modules and internal setup phrases never appear in the `First run` story. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs test/accrue/docs/canonical_demo_contract_test.exs test/accrue/docs/package_docs_verifier_test.exs`.
</verification>

<success_criteria>
- New manifest-backed docs tests exist and pass.
- Shell verification checks fixed invariants only.
- Drift between manifest, README, guide, and wrapper labels is automatically caught before release.
</success_criteria>

<output>
After completion, create `.planning/phases/13-canonical-demo-tutorial/13-02-SUMMARY.md`
</output>

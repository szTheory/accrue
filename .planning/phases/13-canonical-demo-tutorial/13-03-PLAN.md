---
phase: 13-canonical-demo-tutorial
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - examples/accrue_host/README.md
  - accrue/guides/first_hour.md
  - accrue/README.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - ADOPT-02
must_haves:
  truths:
    - "A new evaluator can follow the host README from clone to a first Fake-backed subscription without live Stripe credentials."
    - "The tutorial explicitly covers signed webhook ingest, mounted admin inspection, replay visibility, and the focused proof command."
    - "The docs clearly separate `First run`, `Seeded history`, `mix verify`, `mix verify.full`, Hex smoke, and production setup."
  artifacts:
    - path: "examples/accrue_host/README.md"
      provides: "Canonical clone-to-running tutorial for the checked-in host app."
      contains: "## First run"
    - path: "accrue/guides/first_hour.md"
      provides: "Package-facing tutorial mirror of the canonical host demo path."
      contains: "Seeded history"
    - path: "accrue/README.md"
      provides: "Package README orientation link to the canonical First Hour and host demo path without becoming the full adoption front door."
      contains: "First Hour"
  key_links:
    - from: "examples/accrue_host/README.md"
      to: "accrue/guides/first_hour.md"
      via: "same ordered story and command labels"
      pattern: "First run|Seeded history|mix verify"
    - from: "accrue/README.md"
      to: "accrue/guides/first_hour.md"
      via: "orientation link into the package-facing tutorial"
      pattern: "First Hour"
---

<objective>
Rewrite the host README and package tutorial as one coherent, Fake-first evaluation story that ends in admin inspection and focused proofs.

Purpose: Deliver the user-facing adoption path required by D-01 through D-08 and ADOPT-02 without turning the root README into Phase 14 scope.
Output: Human-written docs that mirror the manifest-backed contract and teach the supported host-owned boundaries only.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
@.planning/phases/13-canonical-demo-tutorial/13-RESEARCH.md
@.planning/phases/13-canonical-demo-tutorial/13-UI-SPEC.md
@examples/accrue_host/demo/command_manifest.exs
@examples/accrue_host/README.md
@accrue/guides/first_hour.md
@accrue/README.md

<interfaces>
Locked labels from `13-UI-SPEC.md`:
```text
First run
Seeded history
mix verify
mix verify.full
bash scripts/ci/accrue_host_uat.sh
```

Locked story from `13-CONTEXT.md`:
```text
create a Fake-backed subscription through the host UI/generated facade,
post one signed webhook through the real endpoint,
then inspect billing state in the mounted admin UI
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the host README as the canonical `First run` path</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-UI-SPEC.md
examples/accrue_host/demo/command_manifest.exs
examples/accrue_host/README.md
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <acceptance_criteria>
    - `rg -n '^## First run$|^## Seeded history$|^## Verification modes$' examples/accrue_host/README.md` returns all three sections.
    - `rg -n 'cd examples/accrue_host|mix setup|mix phx.server|/webhooks/stripe|/billing|mix verify|mix verify\\.full|bash scripts/ci/accrue_host_uat\\.sh' examples/accrue_host/README.md` returns the exact canonical commands and surfaces.
    - `rg -n 'live Stripe credentials|sk_live|whsec_live' examples/accrue_host/README.md` returns no matches.
  </acceptance_criteria>
  <action>Rewrite `examples/accrue_host/README.md` so it becomes the canonical executable path from D-01 and D-05. The visible order must be: prerequisites, `First run`, `Seeded history`, then verification modes. In `First run`, teach `cd examples/accrue_host`, `mix setup`, `mix phx.server`, starting one Fake-backed subscription through the host-owned `AccrueHost.Billing` flow or UI, posting one signed webhook through `/webhooks/stripe`, inspecting `/billing`, and then running `mix verify`. In `Seeded history`, clearly label the seeded replay/history path as deterministic evaluation setup, not the public teaching path, and keep cancellation out of the main story per D-08. In `Verification modes`, explain the difference between `mix verify`, `mix verify.full`, `bash scripts/ci/accrue_host_uat.sh`, Hex smoke, and production setup. Keep Fake/test/live Stripe positioning visible but make the canonical path Fake-backed and credential-free per D-03.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/canonical_demo_contract_test.exs</automated>
  </verify>
  <done>The host README teaches the exact locked story in Phoenix order and no longer leads with the repo-root UAT wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the canonical story in `accrue/guides/first_hour.md`</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
.planning/phases/13-canonical-demo-tutorial/13-UI-SPEC.md
examples/accrue_host/demo/command_manifest.exs
accrue/guides/first_hour.md
examples/accrue_host/README.md
  </read_first>
  <files>accrue/guides/first_hour.md</files>
  <acceptance_criteria>
    - `rg -n '^## 1\\. First run$|^## 2\\. Seeded history$|^## 3\\. Focused verification$' accrue/guides/first_hour.md` returns all three numbered sections.
    - `rg -n 'MyApp\\.Billing|use Accrue\\.Webhook\\.Handler|accrue_admin \"/billing\"|customer\\.subscription\\.created|mix verify' accrue/guides/first_hour.md` returns the required public-boundary surfaces.
    - `rg -n 'Accrue\\.Billing\\.Customer|Accrue\\.Webhook\\.WebhookEvent|GenServer\\.call|worker internals' accrue/guides/first_hour.md` returns no matches.
  </acceptance_criteria>
  <action>Rewrite `accrue/guides/first_hour.md` as the package-facing mirror required by D-01. Keep it human-written and public-boundary-only: generated `MyApp.Billing`, `use Accrue.Webhook.Handler`, `accrue_admin "/billing"`, and focused host proofs. The guide must mirror the README story in package-facing language: `First run` first, then `Seeded history`, then focused verification. Include the explicit tutorial arc from ADOPT-02: install, runtime config, first subscription, signed webhook ingest, admin inspection/replay, and focused host tests. Make the distinction between `mix verify` and `mix verify.full` explicit, and keep Hex smoke and production setup as adjacent but separate modes rather than part of the primary tutorial.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs test/accrue/docs/canonical_demo_contract_test.exs</automated>
  </verify>
  <done>The First Hour guide mirrors the canonical host story with the same labels, order, and public-boundary surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the package README as orientation, not the full front door</name>
  <read_first>
.planning/phases/13-canonical-demo-tutorial/13-CONTEXT.md
accrue/README.md
accrue/guides/first_hour.md
examples/accrue_host/README.md
  </read_first>
  <files>accrue/README.md</files>
  <acceptance_criteria>
    - `rg -n 'First Hour|examples/accrue_host|mix verify|mix verify\\.full' accrue/README.md` returns orientation links or references.
    - `rg -n '^## Start Here$|^## Install$' accrue/README.md` returns the compact orientation sections.
    - `rg -n '^## First run$|^## Seeded history$' accrue/README.md` returns no matches.
  </acceptance_criteria>
  <action>Update `accrue/README.md` only enough to stay aligned with D-01: it should orient users toward `guides/first_hour.md` and the checked-in `examples/accrue_host` demo path, and it may mention `mix verify` / `mix verify.full` as the tutorial’s focused and full verification commands. Do not turn `accrue/README.md` into the detailed tutorial itself and do not duplicate the full `First run` or `Seeded history` prose that belongs in the host README and First Hour guide.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The package README stays compact, accurate, and clearly routes readers into the canonical Phase 13 tutorial surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing docs -> supported public APIs | If docs teach private internals, users can integrate against unstable surfaces and bypass security-critical routes or auth boundaries. |
| tutorial prose -> demo behavior | If the prose drifts from the real host demo, adopters lose trust in the product before reaching the first successful flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | S | tutorial webhook/admin story | mitigate | Keep the first-run path on real `/webhooks/stripe` and mounted `/billing` surfaces, not fake internal calls or screenshots. |
| T-13-10 | I | README / First Hour guide | mitigate | Forbid private package modules and internal setup helpers from the public walkthrough, and keep live credentials out of docs text. |
| T-13-11 | R | command explanations | mitigate | Explain exact differences among `mix verify`, `mix verify.full`, root wrapper, Hex smoke, and production setup so users can reproduce the same path maintainers validate. |
| T-13-12 | E | admin inspection story | mitigate | Preserve host-auth-controlled `/billing` inspection and replay language so the tutorial never implies an unauthenticated admin surface. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs test/accrue/docs/canonical_demo_contract_test.exs test/accrue/docs/package_docs_verifier_test.exs` and `bash scripts/ci/verify_package_docs.sh`.
</verification>

<success_criteria>
- `examples/accrue_host/README.md` is the canonical clone-to-running path with `First run`, `Seeded history`, and verification-mode sections.
- `accrue/guides/first_hour.md` mirrors the same story and covers first subscription, signed webhook ingest, admin inspection/replay, and focused host tests.
- `accrue/README.md` remains a compact orientation surface that points to the canonical tutorial instead of duplicating it.
</success_criteria>

<output>
After completion, create `.planning/phases/13-canonical-demo-tutorial/13-03-SUMMARY.md`
</output>

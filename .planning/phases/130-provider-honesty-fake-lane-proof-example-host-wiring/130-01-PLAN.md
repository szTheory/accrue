---
phase: 130-provider-honesty-fake-lane-proof-example-host-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor/capabilities.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/processor/braintree.ex
  - .planning/processor-support-matrix.md
  - scripts/ci/verify_processor_support_matrix.sh
autonomous: true
requirements: [DUN-09]
must_haves:
  truths:
    - "Accrue.Processor.Capabilities exposes a dunning group with a campaign convergence row (local-identical x3) and a smart_retry_alignment divergence row"
    - "Each adapter (Fake, Stripe, Braintree) declares dunning support in capabilities/0"
    - ".planning/processor-support-matrix.md carries the two dunning rows + a Dunning prose section"
    - "verify_processor_support_matrix.sh fails the build if the dunning matrix rows or honest prose drift from code labels"
    - "verify_processor_support_matrix.sh fails the build if the dunning.campaign convergence row sprouts a native/unsupported/bounded label"
  artifacts:
    - path: "accrue/lib/accrue/processor/capabilities.ex"
      provides: "dunning: group in @support_labels and @provider_support_labels"
      contains: "smart_retry_alignment"
    - path: ".planning/processor-support-matrix.md"
      provides: "dunning.campaign + dunning.smart_retry_alignment matrix rows + Dunning prose section"
      contains: "dunning.smart_retry_alignment"
    - path: "scripts/ci/verify_processor_support_matrix.sh"
      provides: "dunning require_substring pins + negative convergence guard"
      contains: "dunning.campaign convergence row sprouted"
  key_links:
    - from: ".planning/processor-support-matrix.md"
      to: "accrue/lib/accrue/processor/capabilities.ex"
      via: "verify_processor_support_matrix.sh require_substring pins"
      pattern: "dunning\\.smart_retry_alignment"
---

<objective>
Add the `dunning:` capability group to the processor support-contract stack (the exact Phase 125 entitlements drift-gate move) so per-provider dunning behavior is provider-honest and merge-gated. Two rows: `dunning.campaign` (convergent — `local-identical` across Fake/Stripe/Braintree, because the campaign is Accrue-clock-driven with zero processor calls) and `dunning.smart_retry_alignment` (divergent — Stripe native Smart Retries, Braintree clock-driven-only, Fake testing/local-only).

Purpose: Implements DUN-09 SC#2 — the runtime capability labels become the SSOT, the published matrix mirrors them, and the existing `docs-contracts-shift-left` CI job fails the build on drift. No new CI step (D-05).
Output: Extended `capabilities.ex` + three adapters + `processor-support-matrix.md` rows/prose + `verify_processor_support_matrix.sh` pins and negative guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-CONTEXT.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-PATTERNS.md

<interfaces>
<!-- Locked label vocabulary (RESEARCH.md + CONTEXT.md D-06). Use VERBATIM. Do NOT invent terms. -->
Convergence row labels (dunning.campaign):
  fake / stripe / braintree = "local-identical"   (all three identical)
  public label              = "all first-party"

Divergence row labels (dunning.smart_retry_alignment):
  fake      = "testing/local-only"
  stripe    = "native (Smart Retries)"
  braintree = "unsupported (clock-driven only)"
  public label = "provider-divergent (see dunning guide)"

@support_labels dunning group:
  campaign: "all first-party"
  smart_retry_alignment: "provider-divergent (see dunning guide)"

provider_support_label/2 (capabilities.ex) is path-generic via get_in/2 — the new
[:dunning, :campaign] and [:dunning, :smart_retry_alignment] paths work with no function change.

Existing entitlements template to clone (capabilities.ex):
  @support_labels        — entitlements block at lines ~60-63
  @provider_support_labels — entitlements convergence/divergence block at lines ~103-125
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dunning capability group to Capabilities + three adapters</name>
  <files>accrue/lib/accrue/processor/capabilities.ex, accrue/lib/accrue/processor/fake.ex, accrue/lib/accrue/processor/stripe.ex, accrue/lib/accrue/processor/braintree.ex</files>
  <read_first>
    - accrue/lib/accrue/processor/capabilities.ex (read the entitlements block in @support_labels ~lines 60-63 and the entitlements convergence/divergence block in @provider_support_labels ~lines 103-125 — this is the exact template; also confirm provider_support_label/2 uses get_in so no signature change is needed)
    - accrue/lib/accrue/processor/fake.ex (capabilities/0 ~lines 220-240, the `entitlements: %{local_mapping: true}` sibling at line ~239)
    - accrue/lib/accrue/processor/stripe.ex (capabilities/0 ~lines 79-99, `entitlements: %{local_mapping: true}` at line ~98)
    - accrue/lib/accrue/processor/braintree.ex (capabilities/0 ~lines 17-44, `entitlements: %{local_mapping: true}` at line ~43)
  </read_first>
  <action>
In `capabilities.ex` `@support_labels`, add a `dunning:` key as a sibling AFTER the `entitlements:` block with two pairs: `campaign: "all first-party"` and `smart_retry_alignment: "provider-divergent (see dunning guide)"`.

In `capabilities.ex` `@provider_support_labels`, add a `dunning:` key as a sibling AFTER the `entitlements:` block. It has TWO nested rows mirroring the entitlements convergence/divergence comment structure:
  - `campaign:` — CONVERGENCE row. All three providers `"local-identical"` (fake/stripe/braintree). Add a comment block (mirroring the entitlements convergence comment at ~lines 103-107) explaining the campaign is provider-INDEPENDENT local derivation driven off `dunning_campaign_started_at` / `past_due_since` + `Accrue.Clock`, zero processor calls, and that it must NEVER carry a native/unsupported/bounded label (the drift gate enforces this).
  - `smart_retry_alignment:` — DIVERGENCE row. `fake: "testing/local-only"`, `stripe: "native (Smart Retries)"`, `braintree: "unsupported (clock-driven only)"`. Add a comment (mirroring the entitlements divergence comment) explaining processor-native payment retry behavior differs: Stripe Smart Retries run beneath Accrue's cadence; Braintree is clock-driven only with no smart-retry overlay; Fake is the deterministic proof lane.

In each adapter's `capabilities/0`, add a `dunning:` key as a sibling AFTER `entitlements: %{local_mapping: true}`:
  - fake.ex: `dunning: %{campaign: true, smart_retry_alignment: true}`
  - stripe.ex: `dunning: %{campaign: true, smart_retry_alignment: true}`
  - braintree.ex: `dunning: %{campaign: true, smart_retry_alignment: false}` (Braintree has NO processor-native smart retries; `campaign: true` reflects the Accrue cadence works identically, `smart_retry_alignment: false` reflects genuine absence)

Do NOT change `provider_support_label/2` or any other function — `get_in/2` is path-generic.
  </action>
  <verify>
    <automated>cd accrue && mix compile --warnings-as-errors 2>&1 | tail -5; grep -c "smart_retry_alignment" lib/accrue/processor/capabilities.ex</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/capabilities.ex` `@support_labels` contains `dunning:` with `campaign: "all first-party"` and `smart_retry_alignment: "provider-divergent (see dunning guide)"`
    - `accrue/lib/accrue/processor/capabilities.ex` `@provider_support_labels` contains a `dunning:` group where `campaign` has fake/stripe/braintree all `"local-identical"` and `smart_retry_alignment` has stripe `"native (Smart Retries)"`, braintree `"unsupported (clock-driven only)"`, fake `"testing/local-only"`
    - `grep -c "smart_retry_alignment" accrue/lib/accrue/processor/capabilities.ex` returns >= 2 (one in @support_labels, one in @provider_support_labels)
    - `accrue/lib/accrue/processor/fake.ex` and `stripe.ex` `capabilities/0` contain `dunning: %{campaign: true, smart_retry_alignment: true}`
    - `accrue/lib/accrue/processor/braintree.ex` `capabilities/0` contains `dunning: %{campaign: true, smart_retry_alignment: false}`
    - `cd accrue && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The dunning capability group exists in code as the SSOT; all three adapters declare dunning support; compile is clean with no warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Add dunning rows + prose to the support matrix and extend the drift gate</name>
  <files>.planning/processor-support-matrix.md, scripts/ci/verify_processor_support_matrix.sh</files>
  <read_first>
    - .planning/processor-support-matrix.md (the `| Capability | Fake | Stripe | Braintree | Public label |` table ending at the entitlements rows ~lines 59-60; the `## Entitlements` prose section ~line 64 — mirror its convergence-before-divergence prose for the new `## Dunning` section)
    - scripts/ci/verify_processor_support_matrix.sh (the `require_substring` helper ~lines 13-20; the existing matrix pins ~lines 22-64; the entitlements NEGATIVE convergence guard ~lines 105-119; the final `echo "...: OK"` ~line 121)
    - accrue/lib/accrue/processor/capabilities.ex (the dunning labels just added in Task 1 — the matrix rows MUST match these strings verbatim)
  </read_first>
  <action>
In `.planning/processor-support-matrix.md`, immediately AFTER the two `entitlements.*` rows (~line 60), add two dunning table rows (cells must match the code labels verbatim):
  `| dunning.campaign | local-identical | local-identical | local-identical | all first-party |`
  `| dunning.smart_retry_alignment | testing/local-only | native (Smart Retries) | unsupported (clock-driven only) | provider-divergent (see dunning guide) |`

Then add a `## Dunning` prose section (place it after the `## Entitlements` section, mirroring its convergence-before-divergence shape). The prose MUST contain these three load-bearing literal substrings VERBATIM (they are pinned by the gate and by the public guide in Plan 02):
  - "the campaign cadence behaves identically across Stripe, Braintree, and Fake"
  - "Braintree is not retry-aligned"
  - "Stripe has adaptive Smart Retries"
Frame `dunning.campaign` as the second convergence row (local-identical because the cadence is Accrue-clock-driven, zero processor calls) and `dunning.smart_retry_alignment` as a divergence row (Stripe native, Braintree clock-driven only, Fake deterministic proof lane).

In `scripts/ci/verify_processor_support_matrix.sh`, add these `require_substring` calls in the matrix-pins block (after the entitlements pins, before the stale-row guards):
  - the full `| dunning.campaign | local-identical | local-identical | local-identical | all first-party |` row
  - the divergence row prefix `| dunning.smart_retry_alignment | testing/local-only | native (Smart Retries) | unsupported (clock-driven only) |`
  - "the campaign cadence behaves identically across Stripe, Braintree, and Fake"
  - "Braintree is not retry-aligned"
  - "Stripe has adaptive Smart Retries"

Then add a NEGATIVE convergence guard mirroring the entitlements guard at ~lines 105-119 (place it directly after that guard, before the final OK echo). It greps the matrix for `^\| dunning\.campaign \|.*\b(native|unsupported|bounded)\b` and exits 1 with a message like "verify_processor_support_matrix: dunning.campaign convergence row sprouted a per-provider divergence label (broke the local-identical contract)". The `smart_retry_alignment` divergence row is exempt by name (the guard only targets the `dunning.campaign` row).

Do NOT add the guide-side (`accrue/guides/dunning.md`) pins here — those belong to Plan 02 which creates the guide.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_processor_support_matrix.sh && grep -c "dunning\." .planning/processor-support-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/processor-support-matrix.md` contains the exact row `| dunning.campaign | local-identical | local-identical | local-identical | all first-party |`
    - `.planning/processor-support-matrix.md` contains the exact row `| dunning.smart_retry_alignment | testing/local-only | native (Smart Retries) | unsupported (clock-driven only) | provider-divergent (see dunning guide) |`
    - `.planning/processor-support-matrix.md` contains a `## Dunning` heading and the three pinned substrings "the campaign cadence behaves identically across Stripe, Braintree, and Fake", "Braintree is not retry-aligned", "Stripe has adaptive Smart Retries"
    - `scripts/ci/verify_processor_support_matrix.sh` contains a negative guard matching `dunning\.campaign` against `(native|unsupported|bounded)`
    - `bash scripts/ci/verify_processor_support_matrix.sh` exits 0 and prints `verify_processor_support_matrix: OK`
    - Mutation check: editing the matrix `dunning.campaign` row to `| dunning.campaign | local-identical | native | local-identical | all first-party |` causes `bash scripts/ci/verify_processor_support_matrix.sh` to exit 1 (then revert)
  </acceptance_criteria>
  <done>The published matrix carries the dunning rows + honest prose; the drift gate pins them to the code labels and fails on convergence-row divergence drift. The matrix-side gate is green; the guide-side pins are deferred to Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs ↔ runtime capability labels | drift between matrix/code labels could mislead adopters about provider behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Information disclosure (label drift) | processor-support-matrix.md ↔ capabilities.ex | mitigate | verify_processor_support_matrix.sh require_substring pins + negative convergence guard fail the build on divergence (this plan) |

No high-severity threats. Documentation + capability-label + test phase: no auth, no PII, no network-facing changes. No new packages installed — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `cd accrue && mix compile --warnings-as-errors` exits 0
- `bash scripts/ci/verify_processor_support_matrix.sh` exits 0
- The dunning capability labels in code, the matrix rows, and the gate pins are mutually consistent (same literal strings)
</verification>

<success_criteria>
- DUN-09 SC#2 (code+matrix half): `dunning.campaign` convergence row + `dunning.smart_retry_alignment` divergence row exist in `Capabilities`, the three adapters, and the published matrix, all pinned by the merge-blocking `verify_processor_support_matrix.sh` with a negative convergence guard.
</success_criteria>

<output>
Create `.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-01-SUMMARY.md` when done. Record the exact dunning label strings used (for Plan 02's guide pins and Plan 03's D-09 label-mirror test).
</output>

---
phase: 130-provider-honesty-fake-lane-proof-example-host-wiring
plan: 02
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - accrue/guides/dunning.md
  - scripts/ci/verify_processor_support_matrix.sh
autonomous: true
requirements: [DUN-09]
must_haves:
  truths:
    - "A new guides/dunning.md documents the campaign as provider-independent (local-identical) and smart-retry alignment as provider-divergent (Stripe native, Braintree not retry-aligned, Fake deterministic proof lane)"
    - "The guide carries the over-email warning (Stripe Dashboard dunning + Accrue cadence can double-email; disable one side)"
    - "The guide cross-references lifecycle_semantics.md for past_due/unpaid/grace truth instead of re-deriving it"
    - "The guide documents Stripe Test Clocks as the advisory real-Stripe E2E tool (not the merge gate)"
    - "verify_processor_support_matrix.sh fails the build if guides/dunning.md drops the per-provider labels it claims"
  artifacts:
    - path: "accrue/guides/dunning.md"
      provides: "Provider-honest dunning guide (auto-discovered by ExDoc guides/*.md wildcard)"
      min_lines: 60
      contains: "unsupported (clock-driven only)"
    - path: "scripts/ci/verify_processor_support_matrix.sh"
      provides: "guide-side require_substring pins against accrue/guides/dunning.md"
      contains: "guides/dunning.md"
  key_links:
    - from: "accrue/guides/dunning.md"
      to: "accrue/guides/lifecycle_semantics.md"
      via: "markdown cross-reference link to the past_due section"
      pattern: "lifecycle_semantics.md"
    - from: "scripts/ci/verify_processor_support_matrix.sh"
      to: "accrue/guides/dunning.md"
      via: "require_substring_in_guide pins"
      pattern: "native \\(Smart Retries\\)"
---

<objective>
Create the provider-honest `accrue/guides/dunning.md` (DUN-09 SC#1) and extend the drift gate with light guide-side pins (D-08) so the published guide's per-provider claims stay aligned with the code labels. The guide tells the honest story: the campaign cadence is provider-INDEPENDENT (identical across Stripe/Braintree/Fake, zero processor calls), while smart-retry alignment is where providers DIVERGE (Stripe native Smart Retries beneath Accrue's cadence; Braintree clock-driven-only, NOT retry-aligned; Fake the deterministic proof lane). It carries the over-email warning and Stripe Test Clocks note, and cross-references `lifecycle_semantics.md` for lifecycle truth.

Purpose: Implements DUN-09 SC#1 — a `guides/` home documenting per-provider dunning behavior truthfully, drift-gated where labels are claimed (D-08).
Output: New `accrue/guides/dunning.md` (auto-registered by the ExDoc `Path.wildcard("guides/*.md")` in `accrue/mix.exs` — no mix.exs edit needed) + guide-side `require_substring` pins added to `verify_processor_support_matrix.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-CONTEXT.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-PATTERNS.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-01-SUMMARY.md

<interfaces>
<!-- Locked label vocabulary (must appear VERBATIM in the guide; pinned by the gate). -->
Convergence claim (campaign):  "local-identical" / "zero processor calls" / "all first-party"
Divergence labels:
  Stripe    = "native (Smart Retries)"
  Braintree = "unsupported (clock-driven only)"
  Fake      = "testing/local-only"

Shipped contract to DOCUMENT (do NOT re-spec — D-03):
  - default journey: [0, 5, 12] (after_days, absolute from anchor)
  - campaign config: under `:dunning`, `campaign:` DSL; opt-out via
    `dunning: [campaign: [enabled: false]]` or `campaign: false`
  - accessors: Accrue.Config.dunning_campaign_steps/0
  - four ledger events: dunning.campaign_started / step_sent / recovered / exhausted
  - telemetry family: [:accrue, :ops, :dunning_*]  (see guides/telemetry.md)
  - anchor column: dunning_campaign_started_at on accrue_subscriptions

Lifecycle SSOT cross-ref target (do NOT re-derive past_due/unpaid/grace here):
  accrue/guides/lifecycle_semantics.md  → its `### past_due` section (~lines 150-211)
  also note the entitlements.past_due_grace interaction (orthogonal knobs: a past-due
  sub may still be entitled within grace while the campaign emails)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write accrue/guides/dunning.md (provider-honest, cross-referenced)</name>
  <files>accrue/guides/dunning.md</files>
  <read_first>
    - accrue/guides/entitlements.md (the opening cross-reference pattern at lines 1-7 — clone its "see lifecycle_semantics.md for truth; use this guide for how-to" framing; also its overall section flow)
    - accrue/guides/lifecycle_semantics.md (the `### past_due` section ~lines 150-211 — the cross-reference target; do NOT re-derive this truth in dunning.md)
    - .planning/processor-support-matrix.md (the `## Dunning` prose section + dunning rows from Plan 01 — the guide's per-provider claims must restate the same labels)
    - accrue/guides/telemetry.md (confirm how the telemetry family is documented so dunning.md can link to it rather than re-spec)
    - accrue/mix.exs (the ExDoc `extras:` block ~lines 134-135 — confirm the `Path.wildcard("guides/*.md")` so no mix.exs edit is needed)
  </read_first>
  <action>
Create `accrue/guides/dunning.md`. Open with a cross-reference paragraph (clone entitlements.md lines 1-7 framing): for `past_due`/`unpaid`/grace truth, point to `lifecycle_semantics.md` (link to its past_due section); this guide is for how dunning behaves per provider and how to configure it.

Section ordering (recommended; ordering is Claude's discretion but ALL content below is required):
  1. Cross-reference to lifecycle_semantics.md (lifecycle SSOT).
  2. Overview — the campaign is a provider-INDEPENDENT multi-step email cadence driven off local `dunning_campaign_started_at` / `past_due_since` + `Accrue.Clock`, making zero processor calls; default journey `[0, 5, 12]`. State the convergence claim verbatim: it includes the substring "zero processor calls" and frames the cadence as `local-identical` / `all first-party`.
  3. Per-provider breakdown:
     - Stripe — has native Smart Retries (1-4 week adaptive payment-retry schedule); Accrue's email cadence runs on top of it. Use the label "native (Smart Retries)". Document Stripe Test Clocks as the tool for the advisory real-Stripe E2E lane ONLY (network-gated, NOT the merge gate) with a link to https://docs.stripe.com/billing/testing/test-clocks.
     - Braintree — NO smart-retry overlay; the Accrue clock-driven cadence is the only cadence. Explicitly NOT retry-aligned. Use the label "unsupported (clock-driven only)" and state plainly that a Braintree host gets Accrue's emails but no processor-native payment retries beyond Braintree's own dunning settings.
     - Fake — the deterministic proof lane (clock-advanceable; substrate for the merge gate). Use the label "testing/local-only".
  4. Configuration reference — the `campaign:` DSL under `:dunning`, the `[0, 5, 12]` default, `Accrue.Config.dunning_campaign_steps/0`. Inline the load-bearing config shape and link to the Accrue.Config moduledoc for the full reference (inline-vs-link depth is Claude's discretion).
  5. Observability — the four ledger events (`dunning.campaign_started` / `step_sent` / `recovered` / `exhausted`) + the `[:accrue, :ops, :dunning_*]` telemetry family; link to guides/telemetry.md (do NOT re-spec the event payloads).
  6. Over-email warning (D-04) — a prominent `> **Warning:**` blockquote: if a host has Stripe Dashboard dunning emails enabled AND Accrue's campaign enabled, customers may receive duplicate emails; recommend disabling one side (Accrue via `dunning: [campaign: [enabled: false]]` / `campaign: false`, or the Stripe Dashboard emails).
  7. Lifecycle/capability truth note — link lifecycle_semantics.md (past_due/unpaid/grace) AND note the entitlements.past_due_grace interaction (a past-due sub may still be entitled within grace while the campaign emails — orthogonal knobs).

The guide MUST contain these exact substrings (pinned by the gate in Task 2): "local-identical", "native (Smart Retries)", "unsupported (clock-driven only)", "zero processor calls". Do NOT invent new label vocabulary. Do NOT re-derive lifecycle truth (cross-reference instead). Do NOT edit accrue/mix.exs (the guide is auto-discovered).
  </action>
  <verify>
    <automated>test -f accrue/guides/dunning.md && grep -Fq "local-identical" accrue/guides/dunning.md && grep -Fq "native (Smart Retries)" accrue/guides/dunning.md && grep -Fq "unsupported (clock-driven only)" accrue/guides/dunning.md && grep -Fq "zero processor calls" accrue/guides/dunning.md && grep -Fq "lifecycle_semantics.md" accrue/guides/dunning.md && echo GUIDE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/guides/dunning.md` exists and is >= 60 lines
    - Contains all four pinned substrings verbatim: "local-identical", "native (Smart Retries)", "unsupported (clock-driven only)", "zero processor calls"
    - Contains a markdown link/reference to `lifecycle_semantics.md` (cross-reference, not a re-derivation of past_due truth)
    - Contains a `> **Warning:**` blockquote covering the over-email risk and naming `dunning: [campaign: [enabled: false]]` (or `campaign: false`) as the Accrue-side opt-out
    - Contains a reference to Stripe Test Clocks framed as advisory/real-Stripe-only (not the merge gate)
    - Contains the phrase "not retry-aligned" (or equivalent) for Braintree and the `[0, 5, 12]` default journey
    - Does NOT add any new `extras:` entry to `accrue/mix.exs` (auto-discovered via the wildcard)
  </acceptance_criteria>
  <done>The provider-honest dunning guide exists, restates the locked capability labels verbatim, carries the over-email warning + Test Clocks note, and cross-references the lifecycle SSOT.</done>
</task>

<task type="auto">
  <name>Task 2: Add guide-side drift pins to verify_processor_support_matrix.sh</name>
  <files>scripts/ci/verify_processor_support_matrix.sh</files>
  <read_first>
    - scripts/ci/verify_processor_support_matrix.sh (the current state AFTER Plan 01 — the `require_substring` helper, the matrix pins, the dunning matrix pins + negative guard added in Plan 01, the final OK echo; note it uses a single `$matrix` variable pointing at .planning/processor-support-matrix.md)
    - accrue/guides/dunning.md (the guide created in Task 1 — the pins must match its literal strings)
  </read_first>
  <action>
Extend `scripts/ci/verify_processor_support_matrix.sh` with guide-side pins (D-08). Add a `guide` variable resolving to `${repo_root}/accrue/guides/dunning.md` (next to the existing `matrix` variable near the top), and a guard that exits 1 with a clear message if the guide file is missing (mirror the existing `if [[ ! -f "${matrix}" ]]` block — do NOT silently pass).

Add a `require_substring_in_guide()` helper (clone the `require_substring` helper but grep against `$guide`). Then add these guide-side pins (keep it minimal — load-bearing per-provider claims only, D-08):
  - "local-identical"            (convergence label)
  - "native (Smart Retries)"     (Stripe label)
  - "unsupported (clock-driven only)"  (Braintree not-retry-aligned label)
  - "zero processor calls"       (convergence claim)

Place the new variable + missing-file guard with the other top-of-file setup, and the helper + pins in the pins region (after the existing dunning matrix pins from Plan 01). Keep the final `echo "verify_processor_support_matrix: OK"` as the last line. Do NOT duplicate the existing `require_substring` helper definition.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_processor_support_matrix.sh && grep -c "require_substring_in_guide" scripts/ci/verify_processor_support_matrix.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/verify_processor_support_matrix.sh` defines a `guide` variable pointing at `accrue/guides/dunning.md` and a missing-file guard for it
    - The script defines a `require_substring_in_guide` helper and calls it for "local-identical", "native (Smart Retries)", "unsupported (clock-driven only)", "zero processor calls"
    - `bash scripts/ci/verify_processor_support_matrix.sh` exits 0 and prints `verify_processor_support_matrix: OK`
    - Mutation check: removing "unsupported (clock-driven only)" from `accrue/guides/dunning.md` causes `bash scripts/ci/verify_processor_support_matrix.sh` to exit 1 with a guide-missing-label message (then revert)
    - Mutation check: temporarily renaming `accrue/guides/dunning.md` causes the script to exit 1 with the missing-guide message (then restore)
  </acceptance_criteria>
  <done>The published guide is drift-gated against the code labels it claims; the gate fails loudly if the guide drops a per-provider label or goes missing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published guide ↔ runtime capability labels | the public dunning guide could drift from code labels, misleading adopters about per-provider behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-02 | Information disclosure (doc drift) | accrue/guides/dunning.md ↔ capabilities.ex | mitigate | guide-side require_substring_in_guide pins + missing-file guard in verify_processor_support_matrix.sh (this plan) |

No high-severity threats. Documentation phase: no auth, no PII, no network-facing changes. No new packages installed — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `bash scripts/ci/verify_processor_support_matrix.sh` exits 0 (both matrix-side and guide-side pins satisfied)
- `accrue/guides/dunning.md` exists, restates the locked labels, cross-references lifecycle_semantics.md, carries the over-email warning + Test Clocks note
- (Manual, optional) `cd accrue && mix docs` renders the guide via the ExDoc wildcard
</verification>

<success_criteria>
- DUN-09 SC#1: provider-honest dunning guide exists in `guides/` with the per-provider story, over-email warning, Test Clocks note, and lifecycle/capability truth cross-reference.
- DUN-09 SC#2 (guide half): the published guide is drift-gated against the code labels via `verify_processor_support_matrix.sh`.
</success_criteria>

<output>
Create `.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-02-SUMMARY.md` when done.
</output>

---
phase: 130-provider-honesty-fake-lane-proof-example-host-wiring
plan: 03
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - accrue/test/accrue/dunning/dunning_full_journey_test.exs
autonomous: true
requirements: [DUN-10]
must_haves:
  truths:
    - "A deterministic untagged ExUnit test proves the full dunning journey driven through the real Accrue.Webhook.DefaultHandler entry point"
    - "Stage 1: invoice.payment_failed through DefaultHandler sets dunning_campaign_started_at and enqueues the day-0 DunningStep"
    - "Stage 2: clock-advance + Oban.drain_queue progresses through the configured steps"
    - "Stage 3: invoice.paid through DefaultHandler nils the anchor, cancels scheduled steps, and emits dunning.recovered"
    - "Stage 4: exhaustion via DunningSweeper drives the terminal transition + dunning.exhausted"
    - "Each stage asserts the Phase-129 observable contract (telemetry + ledger events) on the real path"
    - "The test asserts the Capabilities dunning.* labels equal the doc literals (D-09 code-side mirror)"
    - "The test stays deterministic (clock-advance + Oban drain, never Process.sleep, never network) so it is untagged and merge-blocking"
  artifacts:
    - path: "accrue/test/accrue/dunning/dunning_full_journey_test.exs"
      provides: "Full-journey + label-mirror proof through DefaultHandler"
      min_lines: 120
      contains: "DefaultHandler.handle"
  key_links:
    - from: "accrue/test/accrue/dunning/dunning_full_journey_test.exs"
      to: "Accrue.Webhook.DefaultHandler"
      via: "DefaultHandler.handle(StripeFixtures.webhook_event(...))"
      pattern: "DefaultHandler\\.handle"
    - from: "accrue/test/accrue/dunning/dunning_full_journey_test.exs"
      to: "Accrue.Processor.Capabilities"
      via: "provider_support_label/2 label mirror assertion"
      pattern: "provider_support_label"
---

<objective>
Create the deterministic, clock-advanceable Fake-lane full-journey test (DUN-10 SC#3) that proves the entire dunning journey — start → step progression → cancel-on-recovery → exhaustion — driven THROUGH the real `Accrue.Webhook.DefaultHandler` entry point (D-10, the cross-phase graduation lesson: "a fully green suite can hide a feature dead on the production path"). At each stage it asserts the Phase-129 observable contract (telemetry + ledger events). It additionally asserts the new `Capabilities` `dunning.*` labels equal the doc literals (D-09 code-side mirror of the bash gate).

Purpose: Implements DUN-10 SC#3 — a merge-blocking proof (an ordinary untagged default-suite test) that the dunning feature works on the production webhook path, deterministically.
Output: New `accrue/test/accrue/dunning/dunning_full_journey_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-CONTEXT.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-PATTERNS.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-RESEARCH.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-01-SUMMARY.md

<interfaces>
<!-- Mechanics confirmed from codebase (RESEARCH.md + PATTERNS.md). -->
Real entry point: Accrue.Webhook.DefaultHandler.handle(event)
Fixture builder:  StripeFixtures.webhook_event("invoice.payment_failed", canonical)
                  StripeFixtures.webhook_event("invoice.paid", canonical)   # recovery
Base case:        use Accrue.BillingCase, async: false   (Fake-backed DB, :processor = Accrue.Processor.Fake)
Oban testing:     use Oban.Testing, repo: Accrue.TestRepo   (testing: :manual in test_helper.exs:51)
Clock advance:    Accrue.Test.Clock.advance([days: N])   (NO Process.sleep; absolute days from anchor)
Drain:            Oban.drain_queue(queue: :accrue_dunning)
Worker:           Accrue.Workers.DunningStep  (queue :accrue_dunning)
Sweeper:          Accrue.Jobs.DunningSweeper.sweep/0   (queue :accrue_dunning; call directly for exhaustion)
Resolver:         Accrue.Dunning.Campaign.next_step/3  (default journey [0, 5, 12] after_days ABSOLUTE; >= boundary)
Anchor:           Subscription.dunning_campaign_started_at; Subscription.dunning_campaign_active?/1
Capabilities API: Accrue.Processor.Capabilities.provider_support_label(provider, path)

Locked labels (D-09 mirror; from Plan 01 SUMMARY):
  provider_support_label(:fake,      [:dunning, :campaign]) == "local-identical"
  provider_support_label(:stripe,    [:dunning, :campaign]) == "local-identical"
  provider_support_label(:braintree, [:dunning, :campaign]) == "local-identical"
  provider_support_label(:stripe,    [:dunning, :smart_retry_alignment]) == "native (Smart Retries)"
  provider_support_label(:braintree, [:dunning, :smart_retry_alignment]) == "unsupported (clock-driven only)"
  provider_support_label(:fake,      [:dunning, :smart_retry_alignment]) == "testing/local-only"

Observable contract (Phase 129):
  ledger events: dunning.campaign_started / step_sent / recovered / exhausted (subject_type "Subscription")
  telemetry:     [:accrue, :ops, :dunning_campaign_started | :dunning_step_sent | :dunning_recovered | :dunning_exhausted]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the full-journey test (header, setup, helpers, label mirror)</name>
  <files>accrue/test/accrue/dunning/dunning_full_journey_test.exs</files>
  <read_first>
    - accrue/test/accrue/webhook/dunning_campaign_start_test.exs (the entire file — the real-entry-point precedent: module header + aliases lines 1-31, the dunning-config save/restore setup lines 39-71, stub_invoice_fetch/3 lines 75-93, fire_payment_failed/2 lines 95-102, attach_telemetry/2 lines 104-116, ledger_events/2 lines 118-126 — copy these helpers)
    - accrue/lib/accrue/processor/capabilities.ex (the dunning labels from Plan 01 — the mirror assertion targets these exact strings)
    - .planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-01-SUMMARY.md (the exact label strings recorded by Plan 01)
    - accrue/test/test_helper.exs (confirm Oban testing: :manual at line ~51 and the default exclusion tags [:live_stripe, :slow, :compile_matrix] at line ~70 — this test MUST stay untagged so it is in the merge gate)
  </read_first>
  <action>
Create `accrue/test/accrue/dunning/dunning_full_journey_test.exs`. Module `Accrue.Dunning.DunningFullJourneyTest`. Header: `use Accrue.BillingCase, async: false` and `use Oban.Testing, repo: Accrue.TestRepo`; `import Ecto.Query, only: [from: 2]`; alias `Accrue.Billing.Subscription`, `Accrue.Events.Event` (as LedgerEvent), `Accrue.Webhook.DefaultHandler`, `Accrue.Workers.DunningStep`, `Accrue.Processor.Capabilities`, `Accrue.Test.Clock`, `Accrue.Jobs.DunningSweeper`.

Copy these helpers VERBATIM from dunning_campaign_start_test.exs:
  - the `setup` block with the `prev_dunning = Application.get_env(:accrue, :dunning, :__unset__)` save + `on_exit` restore (mandatory — Pitfall 2: prevents config pollution that short-circuits `dunning_campaign_enabled?()`), plus the customer + subscription seed. Ensure the setup installs a known-good campaign config so the journey actually starts.
  - `stub_invoice_fetch/3`
  - `fire_payment_failed/2`
  - `attach_telemetry/2`
  - `ledger_events/2`
Add a sibling `fire_payment_succeeded/2` helper that fires `"invoice.paid"` through `DefaultHandler.handle/1` (build the canonical via the same stub pattern, for the recovery stage).

Add the D-09 label-mirror test: `test "dunning.* capability labels equal the documented literals (code-side mirror)"` asserting all six `Capabilities.provider_support_label/2` values from the interfaces block above.

Do NOT add any `@tag :slow`, `@tag :live_stripe`, or `@tag :compile_matrix` — the test MUST stay untagged to be merge-blocking (D-11).
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/dunning/dunning_full_journey_test.exs --seed 0 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `accrue/test/accrue/dunning/dunning_full_journey_test.exs` exists with `use Accrue.BillingCase, async: false` and `use Oban.Testing, repo: Accrue.TestRepo`
    - The file contains `fire_payment_failed`, `fire_payment_succeeded`, `attach_telemetry`, `ledger_events`, and `stub_invoice_fetch` helpers
    - The file contains a label-mirror test asserting `Capabilities.provider_support_label(:braintree, [:dunning, :smart_retry_alignment]) == "unsupported (clock-driven only)"` and the five other label assertions
    - The file contains NO `@tag :slow`, `@tag :live_stripe`, or `@tag :compile_matrix`
    - `cd accrue && grep -c "Process.sleep" test/accrue/dunning/dunning_full_journey_test.exs` returns 0
    - `cd accrue && mix test test/accrue/dunning/dunning_full_journey_test.exs --seed 0` runs the label-mirror test green (full-journey tests added in Task 2)
  </acceptance_criteria>
  <done>The test scaffold compiles and runs; the D-09 label-mirror test passes against the Plan 01 labels; helpers and setup are in place for Task 2's journey stages; the file is untagged (merge-blocking).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the four-stage journey through DefaultHandler with observable-contract assertions</name>
  <files>accrue/test/accrue/dunning/dunning_full_journey_test.exs</files>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex (the first-transition elector maybe_bump_past_due_since + maybe_start_dunning_campaign ~lines 1146-1212; the recovery clear + cancel ~lines 806-895 / 925-946; maybe_emit_dunning_exhaustion ~lines 758-781 — to know what each stage produces)
    - accrue/lib/accrue/dunning/campaign.ex (next_step/3 ~lines 79-94 — default journey [0, 5, 12] after_days ABSOLUTE from anchor, >= boundary; informs the clock-advance amounts)
    - accrue/lib/accrue/workers/dunning_step.ex (queue :accrue_dunning, cancel-guard-first perform ~lines 82-109, unique keys ~lines 143-150 — confirms which step keys fire and the campaign_active? guard)
    - accrue/lib/accrue/jobs/dunning_sweeper.ex (sweep/0 at line ~64, queue :accrue_dunning — call directly in the exhaustion stage)
    - accrue/test/accrue/jobs/dunning_sweeper_test.exs (the exhaustion/grace-terminal assertion pattern ~lines 109-135)
    - .planning/phases/129-customer-operator-surfaces-observability/129-CONTEXT.md (the four dunning.* ledger event names + the [:accrue, :ops, :dunning_*] telemetry family the stages assert)
  </read_first>
  <behavior>
    - Stage 1 (start): after fire_payment_failed, the subscription row has dunning_campaign_started_at set (non-nil), exactly one DunningStep is enqueued for :accrue_dunning, a dunning.campaign_started ledger event exists, and the [:accrue, :ops, :dunning_campaign_started] telemetry fired.
    - Stage 2 (progression): advancing the clock to >= day 5 (absolute from anchor) then draining :accrue_dunning delivers the next step; advancing to >= day 12 then draining delivers the final step. Each delivered step writes a dunning.step_sent ledger event and fires [:accrue, :ops, :dunning_step_sent].
    - Stage 3 (cancel-on-recovery, sibling scenario): firing fire_payment_succeeded mid-journey nils dunning_campaign_started_at, leaves no further enqueued DunningStep jobs, and writes a dunning.recovered ledger event + [:accrue, :ops, :dunning_recovered] telemetry. No further step emails after recovery.
    - Stage 4 (exhaustion, sibling scenario): after the final step with no recovery, DunningSweeper.sweep/0 drives the terminal transition and writes a dunning.exhausted ledger event + [:accrue, :ops, :dunning_exhausted] telemetry.
  </behavior>
  <action>
Add the journey tests to the file from Task 1. Drive EVERY webhook-triggered path through `DefaultHandler.handle/1` — NEVER call `maybe_start_dunning_campaign/2`, `DunningStep.enqueue_step/4`, or any internal helper directly (D-10; this is the load-bearing constraint). Advance time only via `Accrue.Test.Clock.advance([days: N])` and fire jobs only via `Oban.drain_queue(queue: :accrue_dunning)` — NEVER `Process.sleep`, NEVER a network call.

Structure (a happy-path progression test, a recovery test, and an exhaustion test — splitting into sibling test cases is fine and recommended):
  - Stage 1: attach telemetry for [:accrue, :ops, :dunning_campaign_started]; `fire_payment_failed(invoice_id, sub_id)`; reload the subscription and assert dunning_campaign_started_at non-nil; assert exactly one DunningStep enqueued (use all_enqueued / Oban.Testing); assert ledger_events("dunning.campaign_started", sub_id) non-empty; assert the telemetry message was received.
  - Stage 2: advance the clock to total elapsed >= 5 days from anchor (e.g. advance days: 5), drain :accrue_dunning, assert the next step delivered (ledger_events("dunning.step_sent", ...) grows, telemetry [:accrue, :ops, :dunning_step_sent] fired). Then advance to total >= 12 days (advance another days: 7), drain, assert the final step delivered. Respect the ABSOLUTE-from-anchor boundary semantics (Pitfall 3) and ensure the campaign anchor stays set across drains so the per-step campaign_active? guard does not suppress delivery (Pitfall 4).
  - Stage 3 (separate test): start the campaign as in Stage 1, then `fire_payment_succeeded(invoice_id, sub_id)`; reload and assert dunning_campaign_started_at is nil; assert no remaining enqueued DunningStep jobs; assert ledger_events("dunning.recovered", sub_id) non-empty + [:accrue, :ops, :dunning_recovered] telemetry; advancing the clock + draining after recovery delivers no further steps.
  - Stage 4 (separate test): run the campaign to the final step without recovery, then call `assert {:ok, _} = DunningSweeper.sweep()`; assert the subscription transitioned to the terminal status (per the sweeper's terminal_action) and ledger_events("dunning.exhausted", sub_id) non-empty + [:accrue, :ops, :dunning_exhausted] telemetry.

If the real-path proof surfaces a genuine wiring bug (e.g. an event that does not fire on the DefaultHandler path), STOP and record it in the SUMMARY as a finding — bug-fixes uncovered by the real-entry-point proof are in scope as corrections per CONTEXT.md, but the test must assert the correct contract, not be weakened to pass.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/dunning/dunning_full_journey_test.exs --seed 0 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The file drives all webhook-triggered stages through `DefaultHandler.handle` (grep: `grep -c "DefaultHandler.handle" test/accrue/dunning/dunning_full_journey_test.exs` >= 2)
    - The file contains NO `Process.sleep` and NO `maybe_start_dunning_campaign`/`enqueue_step` direct calls (grep both return 0)
    - The file uses `Accrue.Test.Clock.advance` and `Oban.drain_queue(queue: :accrue_dunning)` for progression
    - Stage assertions reference all four ledger event types ("dunning.campaign_started", "dunning.step_sent", "dunning.recovered", "dunning.exhausted") and all four telemetry events ([:accrue, :ops, :dunning_campaign_started | :dunning_step_sent | :dunning_recovered | :dunning_exhausted])
    - The exhaustion stage calls `DunningSweeper.sweep()` and asserts the terminal status transition
    - `cd accrue && mix test test/accrue/dunning/dunning_full_journey_test.exs --seed 0` exits 0
    - `cd accrue && mix test test/accrue/webhook/dunning_campaign_start_test.exs --seed 0` still exits 0 (no regression to the precedent test)
  </acceptance_criteria>
  <done>The full journey (start → progression → recovery → exhaustion) is proven deterministically through the real webhook entry point, with the Phase-129 observable contract asserted at every stage. The test is untagged and runs in the default merge-blocking suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ webhook entry point | a test that bypasses DefaultHandler could pass while the production path is dead (the Phase 126/127 failure mode) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-03 | Tampering (false-green test) | dunning_full_journey_test.exs | mitigate | drive every stage through Accrue.Webhook.DefaultHandler.handle (D-10); grep gate forbids direct internal-helper calls and Process.sleep (this plan) |

No high-severity threats. Test-only phase: no auth, no PII, no network-facing changes (the test is deterministic and never calls the network). No new packages installed — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/dunning/dunning_full_journey_test.exs --seed 0` exits 0
- `cd accrue && mix test --seed 0` exits 0 (full suite; the new untagged test is in the merge gate)
- The test contains no `Process.sleep`, no network calls, and no `@tag :slow`/`:live_stripe`
</verification>

<success_criteria>
- DUN-10 SC#3: a deterministic, clock-advanceable Fake-lane test proves the full journey (start → step progression → cancel-on-recovery → exhaustion) through the real entry point and runs as an untagged (merge-blocking) default-suite test, asserting the Phase-129 observable contract and the D-09 capability-label mirror at each relevant stage.
</success_criteria>

<output>
Create `.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-03-SUMMARY.md` when done. Record any real-path wiring bug found + correction, and confirm whether Oban drain executes scheduled-future jobs after clock advance (RESEARCH.md A1).
</output>

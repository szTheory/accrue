---
phase: 130-provider-honesty-fake-lane-proof-example-host-wiring
plan: 04
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/config/test.exs
  - examples/accrue_host/test/accrue_host/dunning_wiring_test.exs
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_adoption_proof_matrix.sh
autonomous: true
requirements: [DUN-10]
must_haves:
  truths:
    - "examples/accrue_host has an accrue_dunning queue and an Oban.Plugins.Cron entry (DunningSweeper) so the dunning campaign is no longer dead on the host path"
    - "A Fake-backed host-level test proves failed-payment → campaign-step → recovery wiring works through the host's webhook handler + wired Oban"
    - "The host test config enables Oban testing :manual so the proof can drain the accrue_dunning queue"
    - "The adoption-proof matrix carries a dunning wiring row in the Fake-backed blocking lane"
    - "verify_adoption_proof_matrix.sh fails the build if the dunning matrix row drifts"
  artifacts:
    - path: "examples/accrue_host/config/config.exs"
      provides: "accrue_dunning queue + Oban.Plugins.Cron (DunningSweeper)"
      contains: "accrue_dunning"
    - path: "examples/accrue_host/test/accrue_host/dunning_wiring_test.exs"
      provides: "Fake-backed host wiring/journey smoke proof"
      min_lines: 50
      contains: "drain_queue"
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "dunning wiring row in the blocking table"
      contains: "dunning_wiring_test.exs"
    - path: "scripts/ci/verify_adoption_proof_matrix.sh"
      provides: "dunning needle pins"
      contains: "accrue_dunning"
  key_links:
    - from: "examples/accrue_host/config/config.exs"
      to: "Accrue.Jobs.DunningSweeper"
      via: "Oban.Plugins.Cron crontab entry"
      pattern: "Accrue.Jobs.DunningSweeper"
    - from: "examples/accrue_host/test/accrue_host/dunning_wiring_test.exs"
      to: ":accrue_dunning queue"
      via: "Oban.drain_queue(queue: :accrue_dunning)"
      pattern: "accrue_dunning"
---

<objective>
Wire the default dunning campaign into the canonical example host (DUN-10 SC#4) so failed-payment recovery is demonstrated end-to-end instead of shipping as a dormant cron. Today `examples/accrue_host/config/config.exs` has NO `accrue_dunning` queue and NO Cron plugin — so a day-0 `DunningStep` cannot even enqueue (the campaign is silently dead on the host path). Add the `accrue_dunning: 2` queue + an `Oban.Plugins.Cron` entry for `Accrue.Jobs.DunningSweeper`, enable Oban `testing: :manual` in the host test config, add a Fake-backed host-level wiring/journey smoke proof, and a matching adoption-proof-matrix row + verifier needle.

Purpose: Implements DUN-10 SC#4 — closes the dormant-cron gap; recovery is no longer invisible until an adopter reads a guide and adds a crontab.
Output: Extended host `config.exs` + `test.exs` + new `dunning_wiring_test.exs` + `adoption-proof-matrix.md` row + `verify_adoption_proof_matrix.sh` needle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-CONTEXT.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-PATTERNS.md
@.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-RESEARCH.md

<interfaces>
<!-- Host wiring facts confirmed from codebase. -->
Current host Oban (config/config.exs ~lines 37-44):
  queues: [accrue_webhooks: 10, accrue_mailers: 20, accrue_pdf: 5]
  plugins: [{Oban.Plugins.Pruner, max_age: 60*60*24}]
Host test Oban (config/test.exs ~line 32): queues: false, plugins: false  (NO testing: :manual today)

Worker queues:
  Accrue.Workers.DunningStep      → queue :accrue_dunning
  Accrue.Jobs.DunningSweeper      → queue :accrue_dunning   (sweep/0)
  Accrue.Jobs.DetectExpiringCards → queue :accrue_scheduled  (DISCRETION — if wired, ALSO add accrue_scheduled: 5)

Host test base:  AccrueHost.AccrueCase  (Fake processor + sandbox; sets :accrue, :env, :test so Accrue.Clock → Fake clock; `use Accrue.Test`)
Host webhook handler: AccrueHost.BillingHandler (use Accrue.Webhook.Handler) — but for a deterministic unit proof, drive Accrue.Webhook.DefaultHandler.handle/1 with a StripeFixtures.webhook_event, mirroring the accrue-layer pattern.
Clock advance:  Accrue.Test.Clock.advance([days: N])
Drain:          Oban.drain_queue(queue: :accrue_dunning)
Sweeper:        Accrue.Jobs.DunningSweeper.sweep/0

DISCRETION (D-14): DetectExpiringCards is the most safely droppable item. RECOMMENDED: scope tightly to the
failed-payment dunning loop — wire ONLY DunningSweeper. If the executor wires DetectExpiringCards too, it MUST
add accrue_scheduled: 5 to the host queues (Pitfall 6) — otherwise the cron silently fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the host Oban (queue + cron) and enable manual Oban testing</name>
  <files>examples/accrue_host/config/config.exs, examples/accrue_host/config/test.exs</files>
  <read_first>
    - examples/accrue_host/config/config.exs (the Oban block ~lines 37-44 — add the queue + cron here)
    - examples/accrue_host/config/test.exs (the Oban override at ~line 32: `queues: false, plugins: false` — must add `testing: :manual` so the proof can drain/inspect the queue)
    - examples/accrue_host/lib/accrue_host/application.ex (the `{Oban, ...}` supervision entry ~line 14 — confirm Oban reads the config; no change expected)
    - accrue/lib/accrue/jobs/dunning_sweeper.ex (confirm it is `use Oban.Worker, queue: :accrue_dunning` so the cron-dispatched sweep lands in the wired queue)
  </read_first>
  <action>
In `examples/accrue_host/config/config.exs`, extend the `config :accrue_host, Oban` block:
  - add `accrue_dunning: 2` to the `queues:` list (matching the documented recommendation)
  - change `plugins:` from the single Pruner to a list with both the existing `{Oban.Plugins.Pruner, max_age: 60 * 60 * 24}` AND an `{Oban.Plugins.Cron, crontab: [{"*/15 * * * *", Accrue.Jobs.DunningSweeper}]}` entry. (Cron schedule expression is Claude's discretion — match operator-runbook guidance; */15 is a reasonable sweep cadence.)

RECOMMENDED scope (D-14 discretion): wire ONLY `Accrue.Jobs.DunningSweeper`. If you choose to also wire `Accrue.Jobs.DetectExpiringCards` into the crontab, you MUST also add `accrue_scheduled: 5` to the `queues:` list (it uses queue `:accrue_scheduled`, NOT `:accrue_dunning` — Pitfall 6). Default to the tight scope unless there is a clear reason to include it.

In `examples/accrue_host/config/test.exs`, change the Oban override (currently `queues: false, plugins: false`) to add `testing: :manual` so the host proof test can use `Oban.drain_queue/2` and `Oban.Testing` helpers (the campaign self-chains via Oban; without manual testing mode the queue cannot be drained deterministically). Keep `queues: false, plugins: false` (manual testing mode does not run real queues/cron).
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix compile 2>&1 | tail -5; grep -q "accrue_dunning" config/config.exs && grep -q "Oban.Plugins.Cron" config/config.exs && grep -q "Accrue.Jobs.DunningSweeper" config/config.exs && grep -q "testing: :manual" config/test.exs && echo WIRING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/config/config.exs` `queues:` list contains `accrue_dunning: 2`
    - `examples/accrue_host/config/config.exs` `plugins:` is a list containing both `Oban.Plugins.Pruner` and `{Oban.Plugins.Cron, crontab: [{..., Accrue.Jobs.DunningSweeper}]}`
    - If `Accrue.Jobs.DetectExpiringCards` is wired, `queues:` also contains `accrue_scheduled: 5` (else neither appears)
    - `examples/accrue_host/config/test.exs` Oban override contains `testing: :manual`
    - `cd examples/accrue_host && mix compile` exits 0
  </acceptance_criteria>
  <done>The host Oban is wired so the dunning campaign can enqueue and the sweeper runs on cron; the host test config enables deterministic queue draining.</done>
</task>

<task type="auto">
  <name>Task 2: Host-level Fake-backed dunning wiring/journey smoke proof</name>
  <files>examples/accrue_host/test/accrue_host/dunning_wiring_test.exs</files>
  <read_first>
    - examples/accrue_host/test/support/accrue_case.ex (AccrueHost.AccrueCase — Fake processor, sandbox, sets :accrue, :env, :test, `use Accrue.Test`; the customer/sub cleanup helpers)
    - accrue/test/accrue/jobs/dunning_sweeper_test.exs (the Fake-backed seed_sub/2 helper ~lines 55-77 + the BillingCase setup pattern — clone the seed helper)
    - accrue/test/accrue/webhook/dunning_campaign_start_test.exs (stub_invoice_fetch/3 + fire_payment_failed/2 + the dunning-config save/restore setup — the same real-entry-point firing pattern used host-side)
    - examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs (how host tests build invoice.payment_failed events + use the host Repo + Oban — reference for host-test conventions)
    - .planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-03-PLAN.md (the accrue-layer rich journey proof — the host proof is a THIN wiring smoke, NOT a duplicate)
  </read_first>
  <action>
Create `examples/accrue_host/test/accrue_host/dunning_wiring_test.exs`. Module `AccrueHost.DunningWiringTest`. `use AccrueHost.AccrueCase, async: false` and `use Oban.Testing, repo: AccrueHost.Repo`. Alias `Accrue.Billing.Subscription`, `Accrue.Webhook.DefaultHandler`, `Accrue.Workers.DunningStep`, `Accrue.Jobs.DunningSweeper`, `Accrue.Test.Clock`.

This is a THIN wiring smoke (the rich four-stage journey lives in the accrue package — Plan 03 — to avoid duplication, per Claude's Discretion in CONTEXT.md). Prove the HOST's wiring works:
  - setup: save/restore `Application.get_env(:accrue, :dunning)` (same pattern as the accrue tests) with a known-good campaign config; seed ONE Fake-backed customer + subscription (clone the seed_sub/2 helper).
  - Stage A (enqueue works on the wired queue): fire `invoice.payment_failed` through `DefaultHandler.handle/1` (via stub_invoice_fetch + StripeFixtures.webhook_event). Assert the subscription's `dunning_campaign_started_at` is set and exactly one `DunningStep` is enqueued on `:accrue_dunning` (this is the load-bearing proof that the host's `accrue_dunning` queue makes the campaign live — it could not enqueue before this phase).
  - Stage B (drain delivers): `Oban.drain_queue(queue: :accrue_dunning)` and assert it runs without error (success count >= 1 / step delivered). Advance the clock + drain for at least one progression step to show the wired queue chains.
  - Stage C (recovery wiring): fire `invoice.paid` through `DefaultHandler.handle/1`; assert the anchor is nilled (recovery cancels the campaign on the host path).
  - Stage D (sweeper wiring): assert `{:ok, _} = DunningSweeper.sweep()` runs without error against the host Repo (the cron-dispatched job's entry point works in the host).

Drive all webhook paths through `DefaultHandler.handle/1`; NEVER `Process.sleep`; advance via `Accrue.Test.Clock.advance/2`; drain via `Oban.drain_queue(queue: :accrue_dunning)`. Keep it untagged (merge-blocking in the host suite).
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/dunning_wiring_test.exs --seed 0 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host/dunning_wiring_test.exs` exists, `use AccrueHost.AccrueCase, async: false` and `use Oban.Testing, repo: AccrueHost.Repo`
    - The test fires `invoice.payment_failed` and `invoice.paid` through `Accrue.Webhook.DefaultHandler.handle` (grep `DefaultHandler.handle` >= 2)
    - The test asserts a `DunningStep` is enqueued on `:accrue_dunning` and calls `Oban.drain_queue(queue: :accrue_dunning)`
    - The test asserts `dunning_campaign_started_at` is set after failure and nilled after recovery
    - The test calls `DunningSweeper.sweep()` and asserts `{:ok, _}`
    - `grep -c "Process.sleep" examples/accrue_host/test/accrue_host/dunning_wiring_test.exs` returns 0
    - `cd examples/accrue_host && mix test test/accrue_host/dunning_wiring_test.exs --seed 0` exits 0
  </acceptance_criteria>
  <done>The host-level proof demonstrates the wired dunning loop (enqueue → drain → recovery → sweep) works end-to-end on the host path, deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Add the dunning row to the adoption-proof matrix and extend its verifier</name>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md, scripts/ci/verify_adoption_proof_matrix.sh</files>
  <read_first>
    - examples/accrue_host/docs/adoption-proof-matrix.md (the `## Blocking: Fake-backed host + browser` table ~lines 14-26 — add a dunning row in this Fake-backed blocking lane, matching the existing `| Concern | Proof | Where |` column shape)
    - scripts/ci/verify_adoption_proof_matrix.sh (the `require_substring` helper + the existing pins block ~lines 22-62 + the negative guards at the end + the final OK echo — add needles in the pins block)
    - examples/accrue_host/test/accrue_host/dunning_wiring_test.exs (the host test from Task 2 — the matrix row references its filename)
  </read_first>
  <action>
In `examples/accrue_host/docs/adoption-proof-matrix.md`, add a row to the `## Blocking: Fake-backed host + browser` table (the `| Concern | Proof | Where |` table). The row documents the dunning campaign wiring proof and MUST contain these literal tokens (pinned by the verifier): `dunning_wiring_test.exs`, `accrue_dunning`, `Oban.Plugins.Cron`, and `dunning_full_journey_test.exs`. Phrase the Concern as the dunning campaign wiring (accrue_dunning queue + Oban.Plugins.Cron in host config; Fake-backed failed-payment → campaign-step → recovery loop through the real webhook entry point); the Proof column names `dunning_wiring_test.exs` (host smoke) + `dunning_full_journey_test.exs` (accrue package full journey); the Where column names the test locations.

In `scripts/ci/verify_adoption_proof_matrix.sh`, add `require_substring` calls in the pins block (after the existing pins, before the negative guards) for:
  - "dunning_wiring_test.exs"
  - "accrue_dunning"
  - "Oban.Plugins.Cron"
  - "dunning_full_journey_test.exs"
Keep the final `echo "verify_adoption_proof_matrix: OK"` last. Reuse the existing `require_substring` helper (do NOT redefine it).
  </action>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh && grep -c "dunning" examples/accrue_host/docs/adoption-proof-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/docs/adoption-proof-matrix.md` Blocking table contains a row with all four tokens: `dunning_wiring_test.exs`, `accrue_dunning`, `Oban.Plugins.Cron`, `dunning_full_journey_test.exs`
    - `scripts/ci/verify_adoption_proof_matrix.sh` contains `require_substring` calls for those four tokens
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits 0 and prints `verify_adoption_proof_matrix: OK`
    - Mutation check: removing the `accrue_dunning` token from the matrix causes `bash scripts/ci/verify_adoption_proof_matrix.sh` to exit 1 (then revert)
  </acceptance_criteria>
  <done>The adoption-proof matrix records the dunning wiring proof in the Fake-backed blocking lane, drift-gated by the existing merge-blocking verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host Oban config ↔ campaign runtime | a misconfigured/absent queue silently kills the campaign on the host path (the current dormant-cron gap) |
| adoption-proof matrix ↔ host wiring | matrix could claim dunning is proven while the wiring is absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-04 | Denial of service (silent queue failure) | examples/accrue_host Oban config | mitigate | explicit accrue_dunning queue + (if DetectExpiringCards) accrue_scheduled queue; the host wiring test asserts enqueue+drain actually fire (this plan) |
| T-130-05 | Repudiation (false proof claim) | adoption-proof-matrix.md ↔ host test | mitigate | verify_adoption_proof_matrix.sh pins the dunning row to the real test filenames + queue/cron tokens (this plan) |

No high-severity threats. Host-config + test + docs phase: no auth, no PII, no network-facing changes. No new packages installed — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `cd examples/accrue_host && mix compile` exits 0
- `cd examples/accrue_host && mix test test/accrue_host/dunning_wiring_test.exs --seed 0` exits 0
- `bash scripts/ci/verify_adoption_proof_matrix.sh` exits 0
- The host campaign can enqueue (it could not before this phase) and the sweeper runs on the wired cron
</verification>

<success_criteria>
- DUN-10 SC#4: the default campaign is wired into `examples/accrue_host` (accrue_dunning queue + Oban.Plugins.Cron sweeper), recovery is demonstrated end-to-end by a Fake-backed merge-blocking host proof, and an adoption-proof-matrix row + verifier needle attest the wiring — closing the dormant-cron gap.
</success_criteria>

<output>
Create `.planning/phases/130-provider-honesty-fake-lane-proof-example-host-wiring/130-04-SUMMARY.md` when done. Record the chosen cron schedule and whether DetectExpiringCards was wired (and if so, that accrue_scheduled: 5 was added).
</output>

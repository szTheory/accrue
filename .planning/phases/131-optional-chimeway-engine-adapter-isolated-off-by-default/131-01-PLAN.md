---
phase: 131-optional-chimeway-engine-adapter-isolated-off-by-default
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - accrue/test/accrue/dunning/engine_test.exs
  - accrue/test/accrue/dunning/engine/oban_test.exs
  - accrue/test/accrue/integrations/chimeway_test.exs
autonomous: true
requirements: [DUN-03]

must_haves:
  truths:
    - "Three Wave-0 test files exist and run (red until implementation lands)"
    - "engine_test.exs asserts the Accrue.Dunning.Engine behaviour declares start_campaign/3 + cancel_campaign/3"
    - "chimeway_test.exs asserts the conditional-compile contract (loaded-or-:nofile, never a crash)"
  artifacts:
    - path: "accrue/test/accrue/dunning/engine_test.exs"
      provides: "DUN-03 SC#1 behaviour-contract test"
      contains: "behaviour_info(:callbacks)"
    - path: "accrue/test/accrue/dunning/engine/oban_test.exs"
      provides: "Engine.Oban start/cancel unit test scaffold"
      contains: "Accrue.Dunning.Engine.Oban"
    - path: "accrue/test/accrue/integrations/chimeway_test.exs"
      provides: "DUN-03 SC#2 conditional-compile test (sigra_test.exs clone)"
      contains: "Code.ensure_loaded"
  key_links:
    - from: "accrue/test/accrue/dunning/engine_test.exs"
      to: "Accrue.Dunning.Engine"
      via: "behaviour_info/1 reflection"
      pattern: "Accrue\\.Dunning\\.Engine"
---

<objective>
Create the Wave 0 test scaffolds for DUN-03 BEFORE any implementation exists, so every
downstream implementation task (engine behaviour, Engine.Oban wrapper, Chimeway adapter)
has a concrete red→green target. These tests encode the contract: the `Accrue.Dunning.Engine`
behaviour shape, the built-in `Engine.Oban` callback surface, and the conditional-compile
isolation contract for `Accrue.Integrations.Chimeway`.

Purpose: Nyquist compliance — no `<verify>` may point at a non-existent test. This plan
creates the test files the later plans turn green.
Output: Three new ExUnit test files under `accrue/test/accrue/dunning/` and
`accrue/test/accrue/integrations/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-CONTEXT.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-RESEARCH.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md

<interfaces>
<!-- Contracts these tests assert against. The modules do NOT exist yet (later plans create them).
     Tests MUST be written so they FAIL cleanly (UndefinedFunctionError / :nofile) until then. -->

Behaviour to be created in Plan 02 (accrue/lib/accrue/dunning/engine.ex):
  @callback start_campaign(Subscription.t(), DateTime.t(), keyword()) :: :ok | {:error, term()}
  @callback cancel_campaign(Subscription.t(), iso_anchor :: String.t(), keyword()) :: :ok | {:error, term()}

Built-in engine to be created in Plan 03 (accrue/lib/accrue/dunning/engine/oban.ex):
  Accrue.Dunning.Engine.Oban — @behaviour Accrue.Dunning.Engine; start_campaign/3, cancel_campaign/3

Conditional adapter to be created in Plan 04 (accrue/lib/accrue/integrations/chimeway.ex):
  Accrue.Integrations.Chimeway — conditionally compiled via Code.ensure_loaded?(Chimeway);
  @behaviour Accrue.Dunning.Engine; start_campaign/3, cancel_campaign/3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine behaviour + Engine.Oban contract tests</name>
  <files>accrue/test/accrue/dunning/engine_test.exs, accrue/test/accrue/dunning/engine/oban_test.exs</files>
  <read_first>
    - accrue/test/accrue/integrations/sigra_test.exs (structure + reflection patterns to clone)
    - accrue/lib/accrue/integrations/sigra.ex (the behaviour-implementation shape Engine.Oban mirrors)
    - accrue/test/accrue/dunning/dunning_full_journey_test.exs (existing dunning test conventions: aliases, RepoCase vs ExUnit.Case)
    - .planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md (engine_test.exs + oban_test.exs pattern blocks)
  </read_first>
  <action>
    Create `accrue/test/accrue/dunning/engine_test.exs` defining `Accrue.Dunning.EngineTest`
    using `ExUnit.Case, async: true`. Add a `describe "behaviour contract"` block with tests
    asserting: `{:start_campaign, 3} in Accrue.Dunning.Engine.behaviour_info(:callbacks)`;
    `{:cancel_campaign, 3} in Accrue.Dunning.Engine.behaviour_info(:callbacks)`;
    `Accrue.Dunning.Engine in (Accrue.Dunning.Engine.Oban.module_info(:attributes) |> Keyword.get_values(:behaviour) |> List.flatten())`;
    `function_exported?(Accrue.Dunning.Engine.Oban, :start_campaign, 3)`;
    `function_exported?(Accrue.Dunning.Engine.Oban, :cancel_campaign, 3)`.

    Create `accrue/test/accrue/dunning/engine/oban_test.exs` defining
    `Accrue.Dunning.Engine.Oban.ObanTest`. This file requires DB/Oban — use the same test-case
    module the existing `dunning_full_journey_test.exs` uses (read it to confirm whether it is a
    RepoCase or a custom case with Oban sandbox). Add `describe "start_campaign/3"` and
    `describe "cancel_campaign/3"` blocks. Write at least four tests covering the four documented
    behaviours: (a) start_campaign enqueues exactly one DunningStep job for the configured
    `after_days: 0` step key (assert via `Oban.Job` query or `assert_enqueued` with worker
    `Accrue.Workers.DunningStep`); (b) start_campaign returns `:ok` and enqueues nothing when no
    `after_days: 0` step is configured (override config in the test); (c) cancel_campaign cancels
    Oban jobs matching `subscription_id` + `campaign_started_at` (the ISO anchor); (d)
    cancel_campaign returns `:ok` even when the underlying cancel raises (the rescue contract).
    Tests MAY be tagged with the existing dunning-campaign test tag if one is used by the journey
    test; do NOT require a running Chimeway instance.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/dunning/engine_test.exs test/accrue/dunning/engine/oban_test.exs --seed 0 2>&1 | grep -qE "Undefined|UndefinedFunctionError|\(CompileError\)|module Accrue.Dunning.Engine is not available|failures?" && echo "RED-AS-EXPECTED" || echo "CHECK"</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue/test/accrue/dunning/engine_test.exs` exists and defines module `Accrue.Dunning.EngineTest`
    - engine_test.exs contains the literal `behaviour_info(:callbacks)`
    - engine_test.exs references `{:start_campaign, 3}` and `{:cancel_campaign, 3}` literally
    - File `accrue/test/accrue/dunning/engine/oban_test.exs` exists and references `Accrue.Dunning.Engine.Oban`
    - oban_test.exs has at least 4 test cases across `start_campaign/3` and `cancel_campaign/3` describe blocks
    - Both files FAIL to compile or run RED (modules under test do not exist yet) — this is the expected Wave 0 state
  </acceptance_criteria>
  <done>Two test files exist; running them produces a clean red (undefined module/function), not a syntax error in the test code itself.</done>
</task>

<task type="auto">
  <name>Task 2: Chimeway conditional-compile contract test (sigra_test.exs clone)</name>
  <files>accrue/test/accrue/integrations/chimeway_test.exs</files>
  <read_first>
    - accrue/test/accrue/integrations/sigra_test.exs (EXACT clone target — copy structure verbatim, rename)
    - accrue/lib/accrue/integrations/sigra.ex (the 4-pattern the test verifies via source-read assertions)
    - .planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md (chimeway_test.exs full pattern block, lines 472-518)
  </read_first>
  <action>
    Create `accrue/test/accrue/integrations/chimeway_test.exs` defining
    `Accrue.Integrations.ChimewayTest` using `ExUnit.Case, async: true`. Clone the structure of
    `sigra_test.exs`. Add a `describe "conditional compile"` block with two tests:

    Test 1 ("loaded OR :nofile — never a crash"): `case Code.ensure_loaded(Accrue.Integrations.Chimeway) do`.
    In the `{:module, _}` branch assert `function_exported?(Accrue.Integrations.Chimeway, :start_campaign, 3)`,
    `function_exported?(Accrue.Integrations.Chimeway, :cancel_campaign, 3)`, and that
    `Accrue.Dunning.Engine` is in the module's `:behaviour` attributes (same reflection as the
    engine test). In the `{:error, :nofile}` branch assert `refute Code.ensure_loaded?(Chimeway)`
    (the dep is genuinely absent so the adapter is correctly elided).

    Test 2 ("source uses the 4-pattern conditional compile"): `File.read!("lib/accrue/integrations/chimeway.ex")`
    and assert the source contains `"Code.ensure_loaded?(Chimeway)"`, `"@compile {:no_warn_undefined"`,
    and `"@behaviour Accrue.Dunning.Engine"`. (This test reads the source file relative to the
    accrue package cwd, matching how sigra_test.exs reads its source if it does so; if sigra_test.exs
    uses a different relative base, mirror that base exactly.)

    Do NOT reference `stop_conditions` anywhere in this test (RESEARCH override: that DSL does not
    exist in Chimeway 1.0.0).
  </action>
  <verify>
    <automated>cd accrue && test -f test/accrue/integrations/chimeway_test.exs && grep -q 'Code.ensure_loaded' test/accrue/integrations/chimeway_test.exs && ! grep -q 'stop_conditions' test/accrue/integrations/chimeway_test.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue/test/accrue/integrations/chimeway_test.exs` exists and defines `Accrue.Integrations.ChimewayTest`
    - chimeway_test.exs contains `Code.ensure_loaded(Accrue.Integrations.Chimeway)` and a `{:error, :nofile}` branch
    - chimeway_test.exs Test 2 asserts the source contains `Code.ensure_loaded?(Chimeway)`, `@compile {:no_warn_undefined`, and `@behaviour Accrue.Dunning.Engine`
    - chimeway_test.exs contains ZERO occurrences of `stop_conditions`
    - `cd accrue && mix test test/accrue/integrations/chimeway_test.exs --seed 0` runs; Test 1's `{:error, :nofile}` branch is exercised (adapter absent) — Test 2 is RED until `lib/accrue/integrations/chimeway.ex` exists (Plan 04)
  </acceptance_criteria>
  <done>chimeway_test.exs exists, mirrors sigra_test.exs, asserts the conditional-compile contract, and contains no stop_conditions reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → modules-under-test | Tests use reflection (`behaviour_info`, `module_info`, `Code.ensure_loaded`) — no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering | Wave-0 tests assert stale DSL | mitigate | Test 2 explicitly forbids `stop_conditions`; CI grep gate (Plan 05) backstops. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (test-only files). The `{:chimeway, "~> 1.0", optional: true}` install is in Plan 04 and gated there. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/dunning/ test/accrue/integrations/chimeway_test.exs --seed 0` runs (red as expected — implementation lands in Plans 02–04).
- No test file references `stop_conditions`.
</verification>

<success_criteria>
- Three new test files exist with the contract assertions above.
- Tests are RED (undefined modules) — the expected Wave 0 state.
- `grep -rc stop_conditions accrue/test/accrue/dunning accrue/test/accrue/integrations/chimeway_test.exs` returns 0.
</success_criteria>

<output>
Create `.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-01-SUMMARY.md` when done
</output>

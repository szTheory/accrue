---
phase: 131-optional-chimeway-engine-adapter-isolated-off-by-default
plan: 05
type: execute
wave: 3
depends_on: ["131-03", "131-04"]
files_modified:
  - scripts/ci/verify_dunning_chimeway_isolation.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [DUN-03]

must_haves:
  truths:
    - "A merge-blocking CI gate fails the build if any always-on dunning file references Chimeway"
    - "The gate passes against the current tree (the always-on dunning path is Chimeway-free)"
    - "The gate is wired into .github/workflows/ci.yml as a named merge-blocking step"
  artifacts:
    - path: "scripts/ci/verify_dunning_chimeway_isolation.sh"
      provides: "Static isolation gate for the always-on dunning path (DUN-03 SC#3, D-04)"
      contains: "exit 1"
    - path: ".github/workflows/ci.yml"
      provides: "CI wiring for the isolation gate"
      contains: "verify_dunning_chimeway_isolation.sh"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_dunning_chimeway_isolation.sh"
      via: "run: bash step"
      pattern: "verify_dunning_chimeway_isolation\\.sh"
---

<objective>
Add the static isolation merge gate `scripts/ci/verify_dunning_chimeway_isolation.sh` (D-04) that
fails the build if any ALWAYS-ON dunning module references Chimeway symbols, and wire it into CI as
a named merge-blocking step. This makes "core dunning stays Chimeway-free" a verifiable,
non-regressing invariant (DUN-03 SC#3) — mirroring the `verify_core_liveview_runtime_free.sh` and
`verify_entitlement_sync_isolation.sh` precedents.

Purpose: Prove the default recovery path has zero Chimeway dependency (DUN-03 SC#3).
Output: One new shell gate + one CI step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-CONTEXT.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-RESEARCH.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md

<interfaces>
<!-- Clone target + existing CI wiring pattern. -->

Clone target: scripts/ci/verify_core_liveview_runtime_free.sh
  set -euo pipefail
  repo_root="${ROOT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"
  lib="${repo_root}/accrue/lib"
  hits=$(grep -rnE '<pattern>' "${lib}" --include='*.ex' | grep -v '<allowlist>' || true)
  if [[ -n "${hits}" ]]; then echo "...: FAIL ..." >&2; echo "${hits}" >&2; exit 1; fi
  echo "...: OK"

Always-on dunning files to assert Chimeway-free (RESEARCH Pattern 7 + PATTERNS gate scope):
  accrue/lib/accrue/billing/dunning.ex
  accrue/lib/accrue/workers/dunning_step.ex
  accrue/lib/accrue/dunning/campaign.ex
  # NOT default_handler.ex (it dispatches through Config.dunning_engine(), no direct Chimeway ref)
  # NOT engine/oban.ex (always-on, references only Oban + DunningStep)

Existing CI wiring (.github/workflows/ci.yml lines 49-53):
  - name: Core stays LiveView-runtime-free (ENT-07 D-05)
    run: bash scripts/ci/verify_core_liveview_runtime_free.sh
  - name: Entitlement gate path stays advisory-cache-free (ENT-10 D-04)
    run: bash scripts/ci/verify_entitlement_sync_isolation.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify_dunning_chimeway_isolation.sh + wire into CI</name>
  <files>scripts/ci/verify_dunning_chimeway_isolation.sh, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/ci/verify_core_liveview_runtime_free.sh (EXACT clone target — header comment, set -euo pipefail, repo_root, grep + comment-anchor, exit 1 structure)
    - scripts/ci/verify_entitlement_sync_isolation.sh (the closest semantic precedent: an integration-isolation gate)
    - .github/workflows/ci.yml (lines 40-68: the isolation-gate step block where the new step goes)
    - accrue/lib/accrue/billing/dunning.ex + accrue/lib/accrue/workers/dunning_step.ex + accrue/lib/accrue/dunning/campaign.ex (confirm these are currently Chimeway-free so the gate passes against HEAD)
    - .planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-RESEARCH.md (Pattern 7, lines 422-460)
    - .planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md (verify_dunning_chimeway_isolation.sh block, lines 375-427)
  </read_first>
  <action>
    Create `scripts/ci/verify_dunning_chimeway_isolation.sh` (make it executable, `chmod +x`).
    Clone `verify_core_liveview_runtime_free.sh` structure verbatim, adapting:
    - Header comment: "Shift-left merge gate (DUN-03): always-on dunning path stays Chimeway-free."
      Document the allowlist-by-construction: comment lines stripped via the comment anchor; the
      legitimately cond-compiled `lib/accrue/integrations/chimeway.ex` adapter lives OUTSIDE the
      checked always-on files so it is never scanned.
    - Keep `set -euo pipefail` and
      `repo_root="${ROOT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"`,
      `lib="${repo_root}/accrue/lib"`, and the missing-dir `exit 1` guard.
    - grep the THREE always-on dunning files ONLY — `${lib}/accrue/billing/dunning.ex`,
      `${lib}/accrue/workers/dunning_step.ex`, `${lib}/accrue/dunning/campaign.ex` — for the pattern
      `'Accrue\.Integrations\.Chimeway\|Chimeway\.'` with `--include='*.ex'`, piped through
      `grep -v '^[[:space:]]*#'` to strip comment lines, with `|| true` so zero matches do not trip
      `set -e`. (Do NOT scan the whole lib tree — the conditionally-compiled adapter must not be
      flagged.) On non-empty `hits`: echo a FAIL message + the hits to stderr and `exit 1`. On empty:
      echo `verify_dunning_chimeway_isolation: OK`.

    Wire the gate into `.github/workflows/ci.yml`: add a new step immediately AFTER the
    "Entitlement gate path stays advisory-cache-free" step (line ~53), named
    "Dunning core stays Chimeway-free (DUN-03 D-04)" with
    `run: bash scripts/ci/verify_dunning_chimeway_isolation.sh`. Match the exact indentation/format
    of the surrounding isolation-gate steps.
  </action>
  <verify>
    <automated>chmod +x scripts/ci/verify_dunning_chimeway_isolation.sh; bash scripts/ci/verify_dunning_chimeway_isolation.sh && grep -q 'verify_dunning_chimeway_isolation.sh' .github/workflows/ci.yml && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/ci/verify_dunning_chimeway_isolation.sh` exists and is executable
    - The script greps ONLY `billing/dunning.ex`, `workers/dunning_step.ex`, `dunning/campaign.ex` (not the whole lib, not chimeway.ex, not default_handler.ex)
    - The script contains `set -euo pipefail`, `grep -v '^[[:space:]]*#'`, `|| true`, and `exit 1` on non-empty hits
    - `bash scripts/ci/verify_dunning_chimeway_isolation.sh` exits 0 and prints `verify_dunning_chimeway_isolation: OK` against the current tree
    - Negative check: temporarily adding a line `# Chimeway.trigger` to one target file does NOT trip the gate (comment-anchor strips it), but a real `Chimeway.Signal.track(...)` line DOES trip it (exit 1) — verify both by hand-running the grep, then revert any test edit
    - `.github/workflows/ci.yml` contains a named step running `bash scripts/ci/verify_dunning_chimeway_isolation.sh`
  </acceptance_criteria>
  <done>The isolation gate exists, passes against HEAD, fails on a real Chimeway ref in an always-on file, and is wired as a merge-blocking CI step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> repo source | The gate reads source files only; no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-11 | Tampering | Accidental Chimeway coupling in always-on dunning path | mitigate | The merge-blocking gate fails the build on any real Chimeway ref in the three always-on dunning files (DUN-03 SC#3). |
| T-131-12 | Repudiation | Gate silently passes due to comment-only match | mitigate | The `grep -v '^[[:space:]]*#'` comment anchor strips comment lines so a documentation mention does not mask a real coupling; the negative-check acceptance criterion verifies a real ref trips it. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (shell + YAML only). |
</threat_model>

<verification>
- `bash scripts/ci/verify_dunning_chimeway_isolation.sh` exits 0 against HEAD.
- `grep -c verify_dunning_chimeway_isolation.sh .github/workflows/ci.yml` returns >= 1.
- A real `Chimeway.Signal.track` line in `dunning/campaign.ex` makes the gate exit 1 (then revert).
</verification>

<success_criteria>
- The isolation gate exists, is executable, passes against the current Chimeway-free always-on dunning path, and fails on a real coupling.
- The gate runs as a named merge-blocking step in ci.yml.
</success_criteria>

<output>
Create `.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-05-SUMMARY.md` when done
</output>

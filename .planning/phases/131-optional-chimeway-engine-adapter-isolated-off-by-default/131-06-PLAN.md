---
phase: 131-optional-chimeway-engine-adapter-isolated-off-by-default
plan: 06
type: execute
wave: 3
depends_on: ["131-02", "131-04"]
files_modified:
  - accrue/guides/dunning.md
  - .planning/processor-support-matrix.md
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [DUN-03]

must_haves:
  truths:
    - "guides/dunning.md has an opt-in 'Upgrading to Chimeway orchestration' section documenting install, config, and what changes/stays the same"
    - "processor-support-matrix.md has a dunning.engine row reading built-in (Oban) for all providers with the Chimeway optional adapter noted"
    - "verify_package_docs.sh pins the new dunning.md Chimeway needles and the PackageDocsVerifierTest fixture seeds dunning.md so its 6 negative tests stay green"
  artifacts:
    - path: "accrue/guides/dunning.md"
      provides: "Opt-in Chimeway upgrade section (DUN-03 SC#4, D-07)"
      contains: "Accrue.Integrations.Chimeway"
    - path: ".planning/processor-support-matrix.md"
      provides: "dunning.engine capability row (D-07)"
      contains: "dunning.engine"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Chimeway dunning needles (D-07)"
      contains: "Accrue.Dunning.Engine"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/guides/dunning.md"
      via: "require_fixed needle"
      pattern: "require_fixed.*dunning\\.md"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "accrue/guides/dunning.md"
      via: "copy_fixture! in seed_tmp_dir!"
      pattern: "copy_fixture!\\(\"accrue/guides/dunning\\.md\""
---

<objective>
Document the optional Chimeway engine as an opt-in upgrade (DUN-03 SC#4, D-07): extend
`guides/dunning.md` with an "Upgrading to Chimeway orchestration" section, add a `dunning.engine`
row to the processor support matrix, and pin the new doc content with `verify_package_docs.sh`
needles — including the mandatory test-coupling step (the PackageDocsVerifierTest fixture must seed
`dunning.md`, else all 6 negative tests fail).

Purpose: Close DUN-03 SC#4 (documented opt-in targeting the published 1.0.0 API) and lock the docs
against silent regression.
Output: Doc section + matrix row + verifier needles + test fixture coupling.

## MANDATORY TEST COUPLING (project invariant)

`verify_package_docs.sh` is mirrored by `accrue/test/accrue/docs/package_docs_verifier_test.exs`.
Per the project memory: a new `require_fixed` needle on a doc file that is NOT already seeded in
`seed_tmp_dir!` makes ALL 6 negative tests fail (the standalone script stays green, but the ExUnit
mirror breaks). `dunning.md` is NOT currently seeded (it is not in the `copy_fixture!` list,
lines 254-277). This plan MUST add `copy_fixture!("accrue/guides/dunning.md", tmp_dir)` to
`seed_tmp_dir!` in the SAME plan that adds the needles.

## RESEARCH OVERRIDE

The docs MUST NOT reference `stop_conditions` or a `Chimeway.Workflow` behaviour — those are from
the stale Chimeway guide and do not exist in 1.0.0. Document the actual surface:
`Chimeway.trigger/3` + the `Chimeway.Notifier` behaviour, and the v1.40 email-only :immediate path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-CONTEXT.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-RESEARCH.md
@.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md

<interfaces>
<!-- Existing doc anchors + needle patterns (verified line numbers). -->

guides/dunning.md headings (current): "## Configuration" (line 90), "## Observability" (line 128),
  "## Over-email warning" (line 150), "## Lifecycle and entitlements interaction" (line 170).
  The new "## Upgrading to Chimeway orchestration" section goes after "## Configuration" (or near
  the end), executor places it logically; it MUST be discoverable.

processor-support-matrix.md (lines 31-62): capability table with columns
  | Capability | Fake | Stripe | Braintree | Public label |
  Existing dunning rows: `dunning.campaign` (line 61, convergence: local-identical x3) and
  `dunning.smart_retry_alignment` (line 62, divergence). The new `dunning.engine` row goes adjacent.
  D-07 label mapping: Fake/Stripe/Braintree = `built-in (Oban)`; Chimeway is noted via the Public
  label as an optional adapter. The drift gate scripts/ci/verify_processor_support_matrix.sh may pin
  row labels — keep that gate green after adding the row.

verify_package_docs.sh needle pattern (lines 126-135, Phase 127 block):
  # comment block
  require_fixed "$ROOT_DIR/accrue/guides/<file>.md" '<literal needle>'

PackageDocsVerifierTest seed_tmp_dir! (lines 247-278): list of copy_fixture! calls; dunning.md is
  NOT present and must be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dunning.md Chimeway upgrade section + processor-support-matrix dunning.engine row</name>
  <files>accrue/guides/dunning.md, .planning/processor-support-matrix.md</files>
  <read_first>
    - accrue/guides/dunning.md (full current content — to place the new section coherently and match the existing tone/anchors)
    - accrue/guides/entitlements.md (the Phase 127 "stripe_native_sync" opt-in upgrade section — the structural analog: Prerequisites/Installation/Configuration/What changes/What stays the same)
    - .planning/processor-support-matrix.md (lines 29-78: the capability table + the dunning.campaign / dunning.smart_retry_alignment rows and their convergence/divergence prose)
    - scripts/ci/verify_processor_support_matrix.sh (the drift gate that may pin matrix row labels — keep it green)
    - .planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-PATTERNS.md (dunning.md MODIFY block lines 457-466; matrix + needle blocks)
  </read_first>
  <action>
    (1) Extend `accrue/guides/dunning.md` with a new `## Upgrading to Chimeway orchestration` section
    (place it after the `## Configuration` section so the engine config key flows naturally, or near
    the lifecycle section — executor's call, must be discoverable). Subsections: `### Prerequisites`,
    `### Installation`, `### Configuration`, `### What changes`, `### What stays the same`. Content MUST:
    - State Chimeway is OPTIONAL and OFF BY DEFAULT; the built-in `Accrue.Dunning.Engine.Oban` is the
      always-on default and core accrue never requires Chimeway.
    - Installation: a code block adding `{:chimeway, "~> 1.0"}` to the host's deps, plus a note that
      the host runs Chimeway's own migrations per Chimeway's install guide (Accrue does not start
      Chimeway).
    - Configuration: a code block showing `dunning: [engine: Accrue.Integrations.Chimeway]` and the
      literal config fragment `dunning: [engine:` (needle target).
    - Reference the `Accrue.Dunning.Engine` behaviour by name (needle target) and
      `Accrue.Integrations.Chimeway` by name (needle target).
    - "What changes": orchestration of dunning notifications delegates to Chimeway via
      `Chimeway.trigger/3` + the bundled `Chimeway.Notifier`; cancel-on-recovery emits a
      `payment_recovered` signal via `Chimeway.Signal.track/4`.
    - "What stays the same": Accrue still owns campaign DB state (`dunning_campaign_started_at`),
      the same email templates, the ledger/telemetry events, and the customer/admin surfaces.
    - Note v1.40 is email-only (`channels: [:email]`, `:immediate` orchestration); multi-channel is
      future scope.
    - Target the PUBLISHED Chimeway 1.0.0 API. Do NOT mention `stop_conditions` or `Chimeway.Workflow`.

    (2) Add a `dunning.engine` row to the `.planning/processor-support-matrix.md` capability table
    (adjacent to the existing `dunning.campaign` / `dunning.smart_retry_alignment` rows): columns read
    `| dunning.engine | built-in (Oban) | built-in (Oban) | built-in (Oban) | optional adapter (Chimeway v1.0.0) |`
    (per D-07: built-in for all three provider lanes, Chimeway noted in the Public label). Add a short
    prose paragraph beneath the table (matching the existing convergence/divergence prose style)
    explaining the engine is provider-independent (the engine choice is orthogonal to the processor)
    and Chimeway is an off-by-default opt-in adapter. If the drift gate pins exact label strings,
    align the row with the gate's expectations.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -q 'Upgrading to Chimeway' accrue/guides/dunning.md && grep -q 'dunning: \[engine:' accrue/guides/dunning.md && grep -q 'Accrue.Dunning.Engine' accrue/guides/dunning.md && grep -q 'Accrue.Integrations.Chimeway' accrue/guides/dunning.md && ! grep -q 'stop_conditions' accrue/guides/dunning.md && grep -q 'dunning.engine' .planning/processor-support-matrix.md && bash scripts/ci/verify_processor_support_matrix.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - accrue/guides/dunning.md contains a `## Upgrading to Chimeway orchestration` heading
    - dunning.md contains the literal `Accrue.Dunning.Engine`, `Accrue.Integrations.Chimeway`, and `dunning: [engine:`
    - dunning.md contains `{:chimeway, "~> 1.0"}` and references `Chimeway.trigger/3` + `Chimeway.Signal.track/4`
    - dunning.md contains ZERO occurrences of `stop_conditions` and ZERO occurrences of `Chimeway.Workflow`
    - .planning/processor-support-matrix.md contains a `dunning.engine` row with `built-in (Oban)` for Fake, Stripe, and Braintree and a Chimeway optional-adapter note in the Public label
    - `bash scripts/ci/verify_processor_support_matrix.sh` exits 0 (no drift)
  </acceptance_criteria>
  <done>The opt-in Chimeway upgrade section and the matrix row exist, target the 1.0.0 API, avoid the stale DSL, and keep the matrix drift gate green.</done>
</task>

<task type="auto">
  <name>Task 2: verify_package_docs.sh needles + PackageDocsVerifierTest fixture coupling</name>
  <files>scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh (lines 126-145: Phase 127 needle block + the require_fixed helper; the insertion point before the accrue_admin/mix.exs needle block)
    - accrue/test/accrue/docs/package_docs_verifier_test.exs (lines 247-278: seed_tmp_dir! copy_fixture! list — confirm dunning.md is absent; lines 1-10 header comment about needle/seed coupling)
    - accrue/guides/dunning.md (the file the new needles target — Task 1 wrote the content; pick literal substrings that exist verbatim)
  </read_first>
  <action>
    (1) Add a new needle block to `scripts/ci/verify_package_docs.sh` AFTER the Phase 127
    Stripe-native block (after line 135) and BEFORE the `require_fixed "$ROOT_DIR/accrue_admin/mix.exs"`
    block (line 137). Lead with a one-line comment: "Optional Chimeway dunning engine adapter
    (Phase 131, DUN-03)". Add three `require_fixed` calls against `"$ROOT_DIR/accrue/guides/dunning.md"`
    pinning the literal needles: `Accrue.Dunning.Engine`, `Accrue.Integrations.Chimeway`, and
    `dunning: [engine:`. Use the same `require_fixed "$ROOT_DIR/..." 'needle'` shape as the existing
    Phase 127 needles (grep -F literal matching — the needle strings must match dunning.md byte-for-byte).

    (2) Add `copy_fixture!("accrue/guides/dunning.md", tmp_dir)` to the `seed_tmp_dir!` function in
    `accrue/test/accrue/docs/package_docs_verifier_test.exs` (in the `copy_fixture!` list, near the
    other `accrue/guides/*.md` lines around 261-269). This is MANDATORY: without it, the new
    require_fixed needles make all 6 negative tests in PackageDocsVerifierTest fail because the seeded
    tmp fixture would be missing the dunning.md the verifier now requires.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -q "guides/dunning.md' 'Accrue.Dunning.Engine\|guides/dunning.md\" 'Accrue.Dunning.Engine" scripts/ci/verify_package_docs.sh; grep -c "require_fixed.*dunning.md" scripts/ci/verify_package_docs.sh; grep -q 'copy_fixture!("accrue/guides/dunning.md"' accrue/test/accrue/docs/package_docs_verifier_test.exs && bash scripts/ci/verify_package_docs.sh && cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - scripts/ci/verify_package_docs.sh contains exactly 3 new `require_fixed` lines targeting `accrue/guides/dunning.md` with needles `Accrue.Dunning.Engine`, `Accrue.Integrations.Chimeway`, `dunning: [engine:`
    - `grep -c "require_fixed.*dunning.md" scripts/ci/verify_package_docs.sh` returns 3
    - accrue/test/accrue/docs/package_docs_verifier_test.exs `seed_tmp_dir!` contains `copy_fixture!("accrue/guides/dunning.md", tmp_dir)`
    - `bash scripts/ci/verify_package_docs.sh` exits 0 (all needles, including the new dunning.md ones, are present)
    - `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0` passes ALL tests (including the 6 negative tests — the dunning.md fixture seed keeps them green)
  </acceptance_criteria>
  <done>The three Chimeway doc needles are pinned, the test fixture seeds dunning.md, the standalone script exits 0, and the PackageDocsVerifier ExUnit mirror (incl. all 6 negative tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> doc files | The verifier reads doc files only; literal `grep -F` matching, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-13 | Repudiation | Chimeway opt-in docs silently regress | mitigate | Three `require_fixed` needles pin the behaviour name, adapter name, and config key in dunning.md; the merge gate fails if any is removed. |
| T-131-14 | Tampering | Docs reference the stale stop_conditions DSL | mitigate | Task 1 acceptance forbids `stop_conditions`/`Chimeway.Workflow` in dunning.md; the isolation/needle gates do not re-introduce it. |
| T-131-15 | Denial of Service | New needle breaks the 6 PackageDocsVerifier negative tests | mitigate | Task 2 mandatorily seeds dunning.md in `seed_tmp_dir!` in the same plan (documented project invariant). |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (docs + shell + test only). |
</threat_model>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits 0.
- `bash scripts/ci/verify_processor_support_matrix.sh` exits 0.
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs --seed 0` passes (all tests incl. 6 negative).
- `grep -c stop_conditions accrue/guides/dunning.md` returns 0.
</verification>

<success_criteria>
- guides/dunning.md documents the opt-in Chimeway upgrade targeting the 1.0.0 API, no stale DSL.
- processor-support-matrix.md has the dunning.engine row; the matrix drift gate stays green.
- verify_package_docs.sh pins the three Chimeway needles; the PackageDocsVerifierTest fixture seeds dunning.md so the ExUnit mirror (incl. 6 negative tests) stays green.
</success_criteria>

<output>
Create `.planning/phases/131-optional-chimeway-engine-adapter-isolated-off-by-default/131-06-SUMMARY.md` when done
</output>

---
phase: "132"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/lib/accrue_host_web/router.ex
  - examples/accrue_host/lib/accrue_host_web/live/advanced_reports_live.ex
autonomous: true
requirements:
  - PROOF-03
must_haves:
  truths:
    - Host resolves the correct `:billable` organization from the `current_scope` for entitlements gating.
    - An entitlement-gated LiveView route exists in the host router.
    - A dummy LiveView page exists to serve the gated route.
  artifacts:
    - path: examples/accrue_host/config/config.exs
      provides: Entitlements configuration with `:billable` and `:plans` mappings
      contains: "config :accrue, :entitlements"
    - path: examples/accrue_host/lib/accrue_host_web/router.ex
      provides: Entitlement-gated route for advanced reports
      contains: "{Accrue.Live.Entitlements, {:require_feature, :advanced_reports}}"
    - path: examples/accrue_host/lib/accrue_host_web/live/advanced_reports_live.ex
      provides: Gated dummy LiveView
      min_lines: 10
  key_links:
    - from: examples/accrue_host/lib/accrue_host_web/router.ex
      to: examples/accrue_host/lib/accrue_host_web/live/advanced_reports_live.ex
      via: live_session route definition
      pattern: "live.*/app/reports/advanced.*AdvancedReportsLive"
---

<objective>
Configure the entitlement gate configuration and create the gated LiveView route to demonstrate the v1.39 feature gating API.

Purpose: To provide an end-to-end demo of gating a feature based on an organization's plan, proving the `Accrue.Live.Entitlements` gate natively fail-closes properly.
Output: Configuration mapping, a `live_session` guarded by the entitlement plug, and the corresponding LiveView page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-CONTEXT.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-RESEARCH.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure entitlements map and billable resolver</name>
  <files>examples/accrue_host/config/config.exs</files>
  <action>
    Add a new `config :accrue, :entitlements` block in `examples/accrue_host/config/config.exs`. 
    Define the `billable` callback to extract the organization from `container.assigns.current_scope` (fallback to `current_user` if org is missing).
    Define the `plans` list mapping a `:premium` plan to `features: [:advanced_reports]` and `price_ids: ["price_premium"]`.
    Use standard `LocalMap` resolver implicitly (default).
    DO NOT overwrite existing `config :accrue` settings; append this block safely.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix compile</automated>
  </verify>
  <done>Configuration accurately extracts organization as `:billable` and defines `:advanced_reports` feature mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Create Advanced Reports LiveView component</name>
  <files>examples/accrue_host/lib/accrue_host_web/live/advanced_reports_live.ex</files>
  <action>
    Create a minimal LiveView module `AccrueHostWeb.AdvancedReportsLive` using `use AccrueHostWeb, :live_view`.
    Add a basic `mount` function assigning the page title.
    Add a `render` function that displays an H1 (e.g., "Advanced Reports") and a simple placeholder message (e.g., "You have access to this premium feature.").
    Reference the `examples/accrue_host/lib/accrue_host_web/controllers/premium_feature_html.ex` analog from `132-PATTERNS.md` for role-match context, adjusting for LiveView.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix format lib/accrue_host_web/live/advanced_reports_live.ex</automated>
  </verify>
  <done>LiveView component is created and formatted correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Add gated route to the router</name>
  <files>examples/accrue_host/lib/accrue_host_web/router.ex</files>
  <action>
    Add a new `live_session` block for `:entitled_reports` inside the authenticated scope.
    Per `132-PATTERNS.md`, this builds upon the Route Gating Pattern using `pipe_through([:browser, :require_authenticated_user])` and the `browser` pipeline which includes `fetch_current_scope_for_user`.
    The `on_mount` MUST include `{AccrueHostWeb.UserAuth, :require_authenticated}` first, followed by `{Accrue.Live.Entitlements, {:require_feature, :advanced_reports}}`. This mirrors the Shared Patterns for Entitlement Plug Usage mentioned in `132-PATTERNS.md`.
    Inside the session block, add a `live "/app/reports/advanced", AdvancedReportsLive, :index` route.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix phx.routes | grep "/app/reports/advanced"</automated>
  </verify>
  <done>Route is registered and properly wrapped in the entitlement `on_mount` lifecycle hooks based on patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → App Route | Authenticated user requests a gated premium resource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Elevation of Privilege | Router | mitigate | `Accrue.Live.Entitlements` fail-closes gracefully if user is not authenticated or lacks the required feature entitlement. Auth runs first to provide the scope. |
</threat_model>

<verification>
Check that the `config.exs` compiles cleanly and the `mix phx.routes` exposes `/app/reports/advanced`.
</verification>

<success_criteria>
The configuration maps the organization correctly and the route is gated behind the standard Accrue live entitlements component based on patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/132-entitlements-adopter-proof-demo/132-01-SUMMARY.md`
</output>

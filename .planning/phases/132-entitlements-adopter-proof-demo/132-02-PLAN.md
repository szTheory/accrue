---
phase: "132"
plan: "02"
type: execute
wave: 2
depends_on:
  - "132-01"
files_modified:
  - scripts/ci/accrue_host_seed_e2e.exs
  - examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs
autonomous: true
requirements:
  - PROOF-03
must_haves:
  truths:
    - Entitled billable can access the page.
    - Non-entitled billable is denied access.
    - Seeds ensure `price_premium` is provisioned to at least one test organization.
  artifacts:
    - path: examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs
      provides: Verification of gate behavior for different plan levels
      min_lines: 30
    - path: scripts/ci/accrue_host_seed_e2e.exs
      provides: Correct seeding of a premium plan for E2E
      contains: "price_premium"
  key_links:
    - from: examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs
      to: examples/accrue_host/lib/accrue_host_web/router.ex
      via: integration testing
      pattern: "live.*reports/advanced"
---

<objective>
Implement test verification and adjust e2e seeds to prove the `Accrue.Live.Entitlements` gate operates exactly as expected across different billable plan states.

Purpose: Automated proof for the adopter-proof contract that a premium organization can access the gated surface, while a basic or free organization cannot.
Output: A new ExUnit LiveView test file (`entitlements_guard_test.exs`) and updated seeds for CI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-CONTEXT.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update e2e seeds to provision a premium plan</name>
  <files>scripts/ci/accrue_host_seed_e2e.exs</files>
  <action>
    Update the `accrue_host_seed_e2e.exs` script. 
    Ensure that one of the provisioned mock subscriptions (e.g., the primary or a specific test org) subscribes to the `price_premium` price ID so the `Accrue.Live.Entitlements` logic correctly resolves it as entitled during e2e tests.
    If no subscriptions are currently created, ensure a dummy subscription for `price_premium` is seeded for a test organization.
  </action>
  <verify>
    <automated>cat scripts/ci/accrue_host_seed_e2e.exs | grep "price_premium"</automated>
  </verify>
  <done>Seeds properly provision `price_premium` for at least one org.</done>
</task>

<task type="auto">
  <name>Task 2: Write Entitlements Guard test</name>
  <files>examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs</files>
  <action>
    Create a new LiveView integration test file `entitlements_guard_test.exs`.
    Set up two organizations: one entitled (subscribed to `price_premium`) and one not entitled (either no subscription or `price_basic`).
    Write a test proving an authenticated user in the scope of the entitled organization can mount the `/app/reports/advanced` LiveView and see the "Advanced Reports" header.
    Write a test proving a user in the scope of the non-entitled organization is denied (e.g., redirected away with a flash message or receives an error).
    Use `AccrueHost.HostFlowProofCase` or the relevant standard Host case for integration tests.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host_web/live/entitlements_guard_test.exs</automated>
  </verify>
  <done>Tests pass locally proving the gate behaves differently for entitled vs non-entitled organizations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Context | Mocking database state to simulate real-world plan separation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-02 | Spoofing | Test Fixtures | mitigate | Tests strictly rely on canonical Fake adapter subscriptions instead of raw database insertion to ensure valid billable data structures. |
</threat_model>

<verification>
Ensure that running `cd examples/accrue_host && mix test test/accrue_host_web/live/entitlements_guard_test.exs` results in 0 failures.
</verification>

<success_criteria>
LiveView integration tests successfully run, asserting correct allow and deny states for the `/app/reports/advanced` route based on seeded data.
</success_criteria>

<output>
After completion, create `.planning/phases/132-entitlements-adopter-proof-demo/132-02-SUMMARY.md`
</output>

---
phase: "132"
plan: "03"
type: execute
wave: 2
depends_on:
  - "132-01"
files_modified:
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - scripts/ci/verify_adoption_proof_matrix.sh
autonomous: true
requirements:
  - PROOF-03
must_haves:
  truths:
    - The adoption-proof matrix guarantees the entitlement-gating demo continues to exist.
    - CI enforces the matrix documentation update.
  artifacts:
    - path: examples/accrue_host/docs/adoption-proof-matrix.md
      provides: Documentation contract for Entitlements demo
      contains: "Accrue.Live.Entitlements"
    - path: scripts/ci/verify_adoption_proof_matrix.sh
      provides: Verification rules for the new matrix row
      contains: "require_substring \"Entitlement gating\""
  key_links:
    - from: scripts/ci/verify_adoption_proof_matrix.sh
      to: examples/accrue_host/docs/adoption-proof-matrix.md
      via: grep validation
      pattern: "require_substring"
---

<objective>
Update the Adopter-Proof Matrix to establish a binding documentation contract that the Entitlement gating demo works and exists.

Purpose: To close the "headline JTBD not proven in the demo" gap explicitly required by PROOF-03.
Output: Updated `adoption-proof-matrix.md` with a new row, and a corresponding rule in the `verify_adoption_proof_matrix.sh` CI script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-CONTEXT.md
@.planning/phases/132-entitlements-adopter-proof-demo/132-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add row to Adoption-Proof Matrix</name>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <action>
    Add a new row under the "Blocking: Fake-backed host + browser" section (or whichever logical section holds integration proofs).
    The row MUST detail:
    - Concern: Entitlement gating (`Accrue.Live.Entitlements`)
    - Proof: Gated `/app/reports/advanced` with `{:require_feature, :advanced_reports}` ; `entitlements_guard_test.exs`
    - Where: `examples/accrue_host` router + `Accrue.Config.entitlements()` configuration
  </action>
  <verify>
    <automated>cat examples/accrue_host/docs/adoption-proof-matrix.md | grep "Accrue.Live.Entitlements"</automated>
  </verify>
  <done>The matrix explicitly lists the entitlement feature gate.</done>
</task>

<task type="auto">
  <name>Task 2: Update Matrix Validation Script</name>
  <files>scripts/ci/verify_adoption_proof_matrix.sh</files>
  <action>
    Update the script to enforce the presence of the new row you just added.
    Add matching `require_substring` assertions for the new matrix content.
    Example assertions:
    - `require_substring "Entitlement gating" "Entitlement gating row"`
    - `require_substring "Accrue.Live.Entitlements" "Accrue.Live.Entitlements API reference"`
  </action>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh</automated>
  </verify>
  <done>The CI verification script runs successfully and asserts the matrix content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo Documentation | Matrix serves as the human-readable source of truth for repository health. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-03 | Repudiation | CI Scripts | mitigate | Script ensures the markdown proof is never removed accidentally by triggering a CI failure. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_adoption_proof_matrix.sh` and ensure it passes with 0 exit code.
</verification>

<success_criteria>
Matrix documentation is updated and the bash script checks assert the string successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/132-entitlements-adopter-proof-demo/132-03-SUMMARY.md`
</output>

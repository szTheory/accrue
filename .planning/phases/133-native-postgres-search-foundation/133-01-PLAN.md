---
phase: 133
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "accrue/priv/repo/migrations/*_add_pg_trgm_and_search_indices.exs"
  - "accrue/lib/accrue/billing/search.ex"
  - "accrue/lib/accrue/billing.ex"
  - "accrue/test/accrue/billing/search_test.exs"
autonomous: true
requirements: [SRCH-01, SRCH-02]
must_haves:
  truths:
    - "Developer can run migrations without table locks blocking production (using concurrent indices)."
    - "Database utilizes GIN trigram indices rather than doing full table scans for searches."
    - "Application can query core billing domain objects by partial and exact matches."
  artifacts:
    - path: "accrue/priv/repo/migrations/*_add_pg_trgm_and_search_indices.exs"
      provides: "pg_trgm extension and GIN indices"
    - path: "accrue/lib/accrue/billing/search.ex"
      provides: "Ecto search query composition using pg_trgm fragments"
    - path: "accrue/lib/accrue/billing.ex"
      provides: "Facade context functions for unified search access"
  key_links:
    - from: "accrue/lib/accrue/billing.ex"
      to: "accrue/lib/accrue/billing/search.ex"
      via: "Delegation and telemetry spans"
    - from: "accrue/lib/accrue/billing/search.ex"
      to: "Database Tier"
      via: "Ecto fragments invoking '%' and 'similarity()'"
---

<objective>
Equip the database for fast text search using PostgreSQL's native `pg_trgm` extension and expose a unified search API via the Billing context.

Purpose: Provide robust, highly performant similarity search for core billing objects (Customers, Subscriptions, Invoices) without requiring an external search service (like Elasticsearch).
Output: A new database migration for trigram indices, a `Search` service module, and updated `Accrue.Billing` facade endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-native-postgres-search-foundation/133-PATTERNS.md
@.planning/phases/133-native-postgres-search-foundation/133-RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pg_trgm Extension and Search Indices Migration</name>
  <files>accrue/priv/repo/migrations/*_add_pg_trgm_and_search_indices.exs</files>
  <action>
    Generate an Ecto migration using `mix ecto.gen.migration add_pg_trgm_and_search_indices` in the `accrue` directory.
    - Add `@disable_ddl_transaction true` to the module.
    - In `up`, execute `"CREATE EXTENSION IF NOT EXISTS pg_trgm"`.
    - Create GIN indices with `concurrently: true` and raw string column definitions (`"column_name gin_trgm_ops"`) for:
      - `accrue_customers` (`email` and `name`)
      - `accrue_subscriptions` (`processor_id`)
      - `accrue_invoices` (`processor_id` and `number`)
    - In `down`, explicitly `drop index(..., name: ...)` for each, and `execute "DROP EXTENSION IF EXISTS pg_trgm"`.
    (Reference `133-RESEARCH.md` Code Examples for the exact syntax to avoid pitfall 1 and 2.)
  </action>
  <verify>
    <automated>cd accrue && MIX_ENV=test mix ecto.migrate</automated>
  </verify>
  <done>Migration runs successfully in the test environment without syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Billing Search Service</name>
  <files>accrue/lib/accrue/billing/search.ex, accrue/test/accrue/billing/search_test.exs</files>
  <behavior>
    - Test 1: `search_customers(Customer, term)` returns customers matching by email or name, sorted by similarity.
    - Test 2: `search_subscriptions(Subscription, term)` returns subscriptions matching by `processor_id`.
    - Test 3: `search_invoices(Invoice, term)` returns invoices matching by `processor_id` or `number`.
  </behavior>
  <action>
    Create `Accrue.Billing.Search`. Provide `search_customers/2`, `search_subscriptions/2`, and `search_invoices/2`.
    - Reference analog `accrue/lib/accrue/billing/payment_method_actions.ex` for service query and options validation patterns.
    - Use `Ecto.Query.where` with `fragment("? % ?", column, ^term)` to utilize the GIN index for filtering.
    - Use `Ecto.Query.order_by` with descending `fragment("similarity(?, ?)", column, ^term)` for ranking. Use `GREATEST(similarity(...))` when searching across multiple columns (like email and name).
    - Limit results to 50.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/search_test.exs</automated>
  </verify>
  <done>Search queries compile into valid SQL and tests verify the expected ranking and filtering behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Unified Search API in Billing Facade</name>
  <files>accrue/lib/accrue/billing.ex</files>
  <action>
    Update `Accrue.Billing` to expose `search_customers/1`, `search_subscriptions/1`, and `search_invoices/1` APIs.
    - Delegate these calls to `Accrue.Billing.Search` (passing the base schema struct/alias and the search term).
    - Wrap the execution in the standard `span_billing/5` telemetry function, e.g., `span_billing(:search, :customers, term, [], fn -> ... end)` to match the established patterns in `133-PATTERNS.md`.
  </action>
  <verify>
    <automated>cd accrue && mix compile</automated>
  </verify>
  <done>Billing context facade exposes the search functions and properly wraps them in telemetry spans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Database | Search terms from user input are passed into database query fragments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | Ecto Queries | mitigate | Use Ecto parameterized fragments (`^term`) rather than string interpolation in `where` and `order_by` to prevent SQL injection. |
| T-133-02 | Denial of Service | Postgres Database | mitigate | Apply `limit(50)` to all queries and rely on GIN index filtering (`%` operator) prior to sorting to prevent table scans and catastrophic performance degradation on wildcard searches. |
</threat_model>

<verification>
Ensure tests pass, telemetry spans are correctly structured, and the migration is fully reversible (`up`/`down`).
</verification>

<success_criteria>
- The `pg_trgm` extension is enabled and trigram GIN indices exist on the targeted columns.
- `Accrue.Billing` endpoints exist for searching Customers, Subscriptions, and Invoices.
- Search execution utilizes Ecto fragments to leverage index filtering and ranking properly.
</success_criteria>

<output>
After completion, create `.planning/phases/133-native-postgres-search-foundation/133-01-SUMMARY.md`
</output>
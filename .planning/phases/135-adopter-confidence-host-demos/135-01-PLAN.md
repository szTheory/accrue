---
phase: 135-adopter-confidence-host-demos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/lib/accrue_host/billing/plans.ex
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
  - examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs
autonomous: true
requirements: [PROOF-04, PROOF-05]

must_haves:
  truths:
    - "Host app has a metered plan option"
    - "Host can report usage via a button in the UI"
    - "Host can create a checkout session via a button in the UI"
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/billing.ex"
      provides: "report_usage and create_checkout_session wrappers"
    - path: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      provides: "Demo UI for metered usage and checkout"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      to: "AccrueHost.Billing.report_usage/3"
      via: "phx-click event"
    - from: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      to: "AccrueHost.Billing.create_checkout_session/2"
      via: "phx-click event"
---

<objective>
Implement metered usage and checkout facade demos in `examples/accrue_host` to increase adopter confidence.

Purpose: Show practical integration examples of Accrue core features in a host application.
Output: Updated host billing logic, plans, and UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/accrue_host/lib/accrue_host/billing.ex
@examples/accrue_host/lib/accrue_host/billing/plans.ex
@examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex

<interfaces>
From accrue/lib/accrue/billing.ex:
```elixir
def report_usage(customer, event_name, opts \\ [])
def create_checkout_session(%Customer{} = customer, attrs)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metered plan and update billing facade</name>
  <files>
    examples/accrue_host/lib/accrue_host/billing/plans.ex,
    examples/accrue_host/lib/accrue_host/billing.ex
  </files>
  <action>
    - Add a `:metered` plan to `AccrueHost.Billing.Plans` with ID `price_metered`.
    - In `AccrueHost.Billing`, add `report_usage/3` and `report_usage_for_scope/3` wrappers.
    - In `AccrueHost.Billing`, add `create_checkout_session/2` and `create_checkout_session_for_scope/2` wrappers.
    - Ensure these wrappers delegate to `Accrue.Billing` while handling host scoping (Organizations/Scope).
  </action>
  <verify>
    <automated>mix test examples/accrue_host/test/accrue_host/billing_test.exs (if it exists) or verify compilation</automated>
  </verify>
  <done>Billing facade and plans updated to support metered usage and checkout sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement demo UI in SubscriptionLive</name>
  <files>
    examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
  </files>
  <action>
    - Add "Metered Usage Demo" section to the UI, visible when a subscription is active.
    - Add a "Simulate API Call" button that calls `Billing.report_usage_for_scope/3` with `event_name: "api_calls"`.
    - Add "Checkout Facade Demo" section to the UI.
    - Add a "Create Checkout Session" button that calls `Billing.create_checkout_session_for_scope/2` for the Pro plan.
    - Display the resulting checkout URL when created.
  </action>
  <verify>
    <automated>mix test examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs</automated>
  </verify>
  <done>SubscriptionLive view contains functional demos for metered usage and checkout sessions.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Metered usage and Checkout Session demos in the example host.</what-built>
  <how-to-verify>
    1. Start the example host: `cd examples/accrue_host && iex -S mix phx.server`
    2. Visit http://localhost:4000/app/subscription
    3. Ensure an organization is active.
    4. Start a subscription if not already active.
    5. Click "Simulate API Call" and verify it succeeds (check logs for MeterEvent insertion).
    6. Click "Create Checkout Session" and verify it returns a URL.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Host API | UI events trigger billing operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Tampering | report_usage | mitigate | Uses existing host auth scoping (Scope) to ensure only authorized admins can trigger demo actions. |
</threat_model>

<verification>
- Run `mix test examples/accrue_host`
- Manual walkthrough of the new UI sections.
</verification>

<success_criteria>
1. `AccrueHost.Billing` has `report_usage` and `create_checkout_session` methods.
2. `SubscriptionLive` displays the new demo sections.
3. Metered usage reporting works (inserts `MeterEvent`).
4. Checkout session creation works (returns URL).
</success_criteria>

<output>
After completion, create `.planning/phases/135-adopter-confidence-host-demos/135-01-SUMMARY.md`
</output>

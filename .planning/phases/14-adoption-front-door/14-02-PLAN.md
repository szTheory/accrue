---
phase: 14-adoption-front-door
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/ISSUE_TEMPLATE/bug.yml
  - .github/ISSUE_TEMPLATE/integration-problem.yml
  - .github/ISSUE_TEMPLATE/documentation-gap.yml
  - .github/ISSUE_TEMPLATE/feature-request.yml
  - accrue/test/accrue/docs/issue_templates_test.exs
autonomous: true
requirements:
  - ADOPT-04
  - ADOPT-05
must_haves:
  truths:
    - "A user can choose a bug, integration problem, documentation gap, or feature request form from the repository support chooser."
    - "Public issue intake warns users not to paste secrets, production payloads, customer data, or PII."
    - "Issue forms steer users toward supported public surfaces and private security reporting."
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Chooser config with blank issues disabled and private/security contact links"
    - path: ".github/ISSUE_TEMPLATE/bug.yml"
      provides: "Structured bug report form"
    - path: ".github/ISSUE_TEMPLATE/integration-problem.yml"
      provides: "Structured integration-blocker form"
    - path: ".github/ISSUE_TEMPLATE/documentation-gap.yml"
      provides: "Structured docs-gap form"
    - path: ".github/ISSUE_TEMPLATE/feature-request.yml"
      provides: "Structured feature request form"
    - path: "accrue/test/accrue/docs/issue_templates_test.exs"
      provides: "Executable no-secrets and taxonomy contract for support intake"
  key_links:
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "SECURITY.md"
      via: "contact_links"
      pattern: "SECURITY.md"
    - from: ".github/ISSUE_TEMPLATE/integration-problem.yml"
      to: "accrue/guides/first_hour.md"
      via: "issue guidance"
      pattern: "first_hour"
    - from: ".github/ISSUE_TEMPLATE/bug.yml"
      to: "Accrue public surfaces"
      via: "sanitized reproduction fields"
      pattern: "MyApp.Billing|Accrue.Webhook.Handler|Accrue.Auth"
---

<objective>
Add structured public support intake that covers the four approved issue types, routes security reports privately, and blocks secret-sharing footguns.

Purpose: Implement D-15 through D-19 with executable no-secrets coverage.
Output: Four GitHub issue forms, a chooser config, and an ExUnit contract for taxonomy, warnings, and support routing.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-adoption-front-door/14-CONTEXT.md
@.planning/phases/14-adoption-front-door/14-RESEARCH.md
@.planning/phases/14-adoption-front-door/14-VALIDATION.md
@.planning/phases/14-adoption-front-door/14-PATTERNS.md
@CONTRIBUTING.md
@SECURITY.md
@accrue/guides/first_hour.md
@accrue/guides/troubleshooting.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the issue-template contract test and public issue forms</name>
  <read_first>
    .github/ISSUE_TEMPLATE/config.yml (if present)
    .github/ISSUE_TEMPLATE/bug.yml (if present)
    .github/ISSUE_TEMPLATE/integration-problem.yml (if present)
    .github/ISSUE_TEMPLATE/documentation-gap.yml (if present)
    .github/ISSUE_TEMPLATE/feature-request.yml (if present)
    SECURITY.md
    CONTRIBUTING.md
    accrue/guides/first_hour.md
    accrue/guides/troubleshooting.md
    .planning/phases/14-adoption-front-door/14-CONTEXT.md
    .planning/phases/14-adoption-front-door/14-RESEARCH.md
  </read_first>
  <files>.github/ISSUE_TEMPLATE/config.yml, .github/ISSUE_TEMPLATE/bug.yml, .github/ISSUE_TEMPLATE/integration-problem.yml, .github/ISSUE_TEMPLATE/documentation-gap.yml, .github/ISSUE_TEMPLATE/feature-request.yml, accrue/test/accrue/docs/issue_templates_test.exs</files>
  <acceptance_criteria>
    - `rg -n "bug.yml|integration-problem.yml|documentation-gap.yml|feature-request.yml|blank_issues_enabled|Do not paste API keys|webhook secrets|customer data|PII" accrue/test/accrue/docs/issue_templates_test.exs` returns matches.
    - `rg -n "SECURITY.md|CONTRIBUTING.md|first_hour|troubleshooting" accrue/test/accrue/docs/issue_templates_test.exs` returns matches.
    - `rg -n "blank_issues_enabled: false|contact_links" .github/ISSUE_TEMPLATE/config.yml` returns matches.
    - `rg -n "Do not paste API keys|webhook secrets|production payloads|customer data|PII" .github/ISSUE_TEMPLATE/*.yml` returns matches in all four forms.
    - `rg -n "\\[Bug\\]: |\\[Integration\\]: |\\[Docs\\]: |\\[Feature\\]: " .github/ISSUE_TEMPLATE/*.yml` returns one match per form.
    - `rg -n "MyApp.Billing|Accrue.Webhook.Handler|Accrue.Auth|First Hour|Troubleshooting" .github/ISSUE_TEMPLATE/*.yml` returns matches.
    - `cd accrue && mix test test/accrue/docs/issue_templates_test.exs` exits 0.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/docs/issue_templates_test.exs` to enforce D-15, D-16, D-17, D-18, and D-19. Assert `.github/ISSUE_TEMPLATE/config.yml` sets `blank_issues_enabled: false`, exposes contact links to `SECURITY.md` and `CONTRIBUTING.md`, and that exactly four YAML forms exist: `bug.yml`, `integration-problem.yml`, `documentation-gap.yml`, and `feature-request.yml`. Assert every form contains a top-of-form warning with the exact phrases `Do not paste API keys`, `webhook secrets`, `production payloads`, `customer data`, and `PII`, and that forms reference public surfaces such as `MyApp.Billing`, `Accrue.Webhook.Handler`, `Accrue.Auth`, `accrue/guides/first_hour.md`, or `accrue/guides/troubleshooting.md` rather than private schemas or reducers. In the same task, create `.github/ISSUE_TEMPLATE/config.yml` with `blank_issues_enabled: false` and contact links that route security/vulnerability reports to `SECURITY.md` and contributor/process questions to `CONTRIBUTING.md`. Create four YAML issue forms per D-16: `bug.yml`, `integration-problem.yml`, `documentation-gap.yml`, and `feature-request.yml`. Use titles `[Bug]: `, `[Integration]: `, `[Docs]: `, and `[Feature]: `; add stable labels such as `bug`, `integration`, `documentation`, and `feature-request` plus `needs-triage`; and put the no-secrets warning at the top of every form with the exact language from the test. The bug and integration forms must ask for supported surfaces like `MyApp.Billing`, `/webhooks/stripe`, `/billing`, `Accrue.Webhook.Handler`, and `Accrue.Auth`; the docs-gap form must ask which doc path is wrong or missing; the feature request form must ask for the user problem, current workaround, affected public API surface, and why the change belongs in Accrue rather than host-app code. Do not create a generic support-question form.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/issue_templates_test.exs</automated>
  </verify>
  <done>The new docs test fails if the chooser taxonomy changes, blank issues are re-enabled, security routing disappears, or any form stops warning against secrets and PII. The repository exposes exactly the four approved public issue forms, blank issues are disabled, security reports are routed privately, and every form warns against secret or PII disclosure while anchoring requests to supported public surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public GitHub user → issue forms | Untrusted input crosses from public users into a permanent public repo surface |
| issue chooser → support/security routing | Public support must not replace private vulnerability reporting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I | .github/ISSUE_TEMPLATE/*.yml | mitigate | Put the no-secrets warning at the top of every form, forbid generic support intake, and ask only for sanitized reproduction context instead of raw keys, secrets, production payloads, customer data, or PII. |
| T-14-02 | T | integration-problem.yml | accept | This plan routes webhook questions to public docs; webhook-verification wording itself is hardened in Plans 14-01 and 14-03. |
| T-14-03 | T | bug.yml, integration-problem.yml, feature-request.yml | mitigate | Ask reporters to reference public surfaces such as `MyApp.Billing`, `Accrue.Webhook.Handler`, `/webhooks/stripe`, `/billing`, and `Accrue.Auth`, not private modules or tables. |
| T-14-04 | D | config.yml | accept | Release-lane wording is not owned here; this plan keeps support routing separate so public issue intake does not imply advisory checks are blockers. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/issue_templates_test.exs`.
</verification>

<success_criteria>
The repository exposes a structured GitHub issue chooser with four focused forms, disables blank issues, routes vulnerabilities privately, and enforces no-secrets intake language.
</success_criteria>

<output>
After completion, create `.planning/phases/14-adoption-front-door/14-adoption-front-door-02-SUMMARY.md`
</output>

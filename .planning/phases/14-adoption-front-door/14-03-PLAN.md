---
phase: 14-adoption-front-door
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - RELEASING.md
  - CONTRIBUTING.md
  - guides/testing-live-stripe.md
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/release_guidance_test.exs
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-03
  - ADOPT-05
  - ADOPT-06
must_haves:
  truths:
    - "A maintainer can tell which checks are required for the canonical local demo and release lane versus which checks are provider-parity or advisory."
    - "Public docs keep Fake, Stripe test mode, and live Stripe in distinct lanes."
    - "Drift guards fail if the front-door route map or mode labels regress."
  artifacts:
    - path: "RELEASING.md"
      provides: "Release guidance with required versus advisory lane labeling"
    - path: "guides/testing-live-stripe.md"
      provides: "Explicit provider-parity and advisory language for Stripe-backed checks"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Fixed-invariant shell guard for root README and release-guidance labels"
    - path: "accrue/test/accrue/docs/release_guidance_test.exs"
      provides: "Executable contract for Fake/test/live wording and required/advisory lanes"
  key_links:
    - from: "RELEASING.md"
      to: "guides/testing-live-stripe.md"
      via: "provider-parity detail link"
      pattern: "guides/testing-live-stripe.md"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "README.md"
      via: "front-door fixed invariants"
      pattern: "README.md"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "RELEASING.md"
      via: "required versus advisory wording checks"
      pattern: "RELEASING.md"
---

<objective>
Clarify release and provider-parity guidance, then extend the fixed-invariant drift guard so the new adoption language cannot silently regress.

Purpose: Implement D-09 through D-14 and keep the front-door docs truthful after Plan 14-01 lands.
Output: Updated release/support docs, a release-guidance contract test, and shell-verifier coverage for the new root README and mode labels.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-adoption-front-door/14-CONTEXT.md
@.planning/phases/14-adoption-front-door/14-RESEARCH.md
@.planning/phases/14-adoption-front-door/14-VALIDATION.md
@.planning/phases/13-canonical-demo-tutorial/13-02-SUMMARY.md
@README.md
@RELEASING.md
@CONTRIBUTING.md
@guides/testing-live-stripe.md
@scripts/ci/verify_package_docs.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release-guidance contracts and update release/support docs with drift guards</name>
  <read_first>
    RELEASING.md
    CONTRIBUTING.md
    guides/testing-live-stripe.md
    scripts/ci/verify_package_docs.sh
    accrue/test/accrue/docs/package_docs_verifier_test.exs
    README.md
    .planning/phases/14-adoption-front-door/14-CONTEXT.md
    .planning/phases/14-adoption-front-door/14-VALIDATION.md
  </read_first>
  <files>RELEASING.md, CONTRIBUTING.md, guides/testing-live-stripe.md, scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/release_guidance_test.exs, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <acceptance_criteria>
    - `rg -n "canonical local demo|provider-parity checks|advisory/manual before shipping your app|required|advisory" accrue/test/accrue/docs/release_guidance_test.exs` returns matches.
    - `rg -n "README.md|RELEASING.md|provider-parity checks|advisory/manual before shipping your app" accrue/test/accrue/docs/package_docs_verifier_test.exs` returns matches.
    - `rg -n "Canonical local demo: Fake|provider-parity checks|advisory/manual before shipping your app|required deterministic gate" RELEASING.md` returns matches.
    - `rg -n "provider-parity checks|advisory/manual|continue-on-error: true|STRIPE_TEST_SECRET_KEY" guides/testing-live-stripe.md` returns matches.
    - `rg -n "guides/testing-live-stripe.md|keep real credentials out of shell history and logs|provider-parity" CONTRIBUTING.md` returns matches.
    - `rg -n "README.md|RELEASING.md|Canonical local demo: Fake|Provider parity: Stripe test mode|Advisory/manual: live Stripe|provider-parity checks|advisory/manual before shipping your app" scripts/ci/verify_package_docs.sh` returns matches.
    - `cd accrue && mix test test/accrue/docs/release_guidance_test.exs test/accrue/docs/package_docs_verifier_test.exs && bash ../scripts/ci/verify_package_docs.sh` exits 0.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/docs/release_guidance_test.exs` to enforce D-10, D-11, D-12, D-13, and D-14. Assert `RELEASING.md` names Fake-backed local verification as the required deterministic lane, Stripe test mode as `provider-parity checks`, and live Stripe as `advisory/manual before shipping your app`; assert it does not imply provider checks block clone-to-eval or standard releases. Assert `guides/testing-live-stripe.md` remains explicit that live Stripe uses test-mode credentials, is excluded from the main CI lane, and exists to catch provider drift rather than replace Fake. Extend `accrue/test/accrue/docs/package_docs_verifier_test.exs` so the shell verifier's success output and failure coverage include the new root README and release-guidance invariants. In the same task, update `RELEASING.md` so the release checklist and verification sections explicitly separate three lanes: `Canonical local demo: Fake` as the required deterministic gate for docs and release readiness, `Provider parity: Stripe test mode` for optional/manual Stripe-backed checks that prove SCA/3DS, hosted Checkout, and signature fidelity, and `Advisory/manual: live Stripe` for final app-level confidence before shipping a real host app. Update `guides/testing-live-stripe.md` so it clearly says the suite runs against Stripe test mode, is a provider-parity lane, and remains advisory because the scheduled/manual CI job uses `continue-on-error: true`. Update `CONTRIBUTING.md` so contributor guidance points readers to the provider-parity guide and repeats that real credentials stay out of shell history and logs. Extend `scripts/ci/verify_package_docs.sh` with fixed-string checks for the new root README route-map labels and the release-guidance labels; keep the script narrow and grep-based rather than moving prose-semantics out of ExUnit.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/release_guidance_test.exs test/accrue/docs/package_docs_verifier_test.exs && bash ../scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The docs test suite now fails if release/support copy collapses Fake, Stripe test mode, and live Stripe into one lane or if the shell verifier stops checking the new fixed invariants. Release and support docs clearly distinguish required deterministic checks from provider-parity and advisory lanes, and the shell verifier now guards the front-door and release-copy fixed invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs → release decisions | Release wording changes which checks people believe are required blockers |
| provider-parity guide → contributor behavior | Readers may over-trust or under-trust Stripe-backed checks based on wording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I | CONTRIBUTING.md, guides/testing-live-stripe.md | mitigate | Keep explicit warnings that real credentials stay out of shell history and logs; do not request keys or payloads in public docs. |
| T-14-02 | T | RELEASING.md, guides/testing-live-stripe.md | mitigate | State that signed webhook verification and runtime secrets remain required on real integrations, and route webhook setup details back to the public docs rather than implying raw-body verification is optional. |
| T-14-03 | T | RELEASING.md | mitigate | Keep release/support wording on public surfaces and generated host boundaries; do not teach private modules as stable APIs. |
| T-14-04 | D | RELEASING.md, scripts/ci/verify_package_docs.sh | mitigate | Label Fake as the required deterministic gate, Stripe test mode as provider-parity, and live Stripe as advisory/manual, then lock those strings in tests and the shell verifier so advisory checks cannot silently become perceived blockers. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/release_guidance_test.exs test/accrue/docs/package_docs_verifier_test.exs` and `bash scripts/ci/verify_package_docs.sh`.
</verification>

<success_criteria>
Maintainers and adopters can distinguish canonical Fake-backed verification from Stripe test-mode parity checks and live-Stripe advisory checks, and automated drift guards enforce those labels.
</success_criteria>

<output>
After completion, create `.planning/phases/14-adoption-front-door/14-adoption-front-door-03-SUMMARY.md`
</output>

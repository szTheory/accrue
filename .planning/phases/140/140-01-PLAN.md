---
phase: 140
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REL-12]

must_haves:
  truths:
    - "Release automation explicitly configures accrue_portal alongside accrue and accrue_admin."
    - "Runbooks accurately reflect the three-package publish workflow."
  artifacts:
    - path: "release-please-config.json"
      provides: "Linked versions config"
    - path: ".github/workflows/release-please.yml"
      provides: "Publish automation"
    - path: "RELEASING.md"
      provides: "Maintainer runbook"
  key_links: []
---

<objective>
Verify and lock the three-package release contract for accrue, accrue_admin, and accrue_portal.

Purpose: To ensure release automation and runbooks accurately reflect the three-package architecture before triggering the publish workflow.
Output: Verified release configuration and runbooks confirming the 3-package contract is locked.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@release-please-config.json
@.github/workflows/release-please.yml
@RELEASING.md
@scripts/ci/verify_release_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Release Contract</name>
  <files></files>
  <action>Run the automated verifier script `scripts/ci/verify_release_contract.sh` to ensure the 3-package contract is fully satisfied across the release-please config, workflows, and runbooks.</action>
  <verify>
    <automated>bash scripts/ci/verify_release_contract.sh</automated>
  </verify>
  <done>The verify_release_contract.sh script returns success ("OK: linked release contract aligned").</done>
</task>

<task type="auto">
  <name>Task 2: Verify Package Docs Invariants</name>
  <files></files>
  <action>Run the automated verifier script `scripts/ci/verify_package_docs.sh` to ensure the README, runbook, and contributing documentation refer correctly to accrue_portal.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The verify_package_docs.sh script completes without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer→GitHub Actions | Release-please workflows execute with HEX_API_KEY secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | Elevation of Privilege | `.github/workflows/publish-hex.yml` | mitigate | Ensure publish jobs enforce strict dependency sequence to avoid unintended publishing order. Already mitigated in workflow config. |
</threat_model>

<verification>
All three packages (accrue, accrue_admin, accrue_portal) are explicitly declared in release automation config and referenced correctly in Releasing/Contributing docs.
</verification>

<success_criteria>
1. Release automation config explicitly includes all three packages as a linked release.
2. The runbook instructions accurately reflect the three-package publish workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/140/140-01-SUMMARY.md`
</output>

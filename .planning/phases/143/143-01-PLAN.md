---
phase: 143
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["accrue/lib/accrue/webhook/default_handler.ex", "accrue/lib/accrue/analytics/dunning.ex"]
autonomous: true
requirements: [ANA-01]
must_haves:
  truths:
    - MRR is snapshotted inside event payloads for recovery and exhaustion
    - Ecto context exists to aggregate MRR data without new database tables
  artifacts:
    - path: accrue/lib/accrue/webhook/default_handler.ex
      provides: Event payload snapshotting
    - path: accrue/lib/accrue/analytics/dunning.ex
      provides: MRR Ecto aggregations
  key_links:
    - from: accrue/lib/accrue/webhook/default_handler.ex
      to: accrue_events (Postgres)
      via: Events.record with mrr_value_cents in data map
    - from: accrue/lib/accrue/analytics/dunning.ex
      to: accrue_events (Postgres)
      via: JSONB fragment queries
---

<objective>
Snapshot MRR at emission time for dunning events and create the Ecto analytics context to aggregate them.
Purpose: Accurately track recovered vs lost revenue without adding temporal data leakage or new database tables.
Output: Modified DefaultHandler and a new Dunning analytics module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/143/143-RESEARCH.md
@.planning/phases/143/143-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MRR calculation to DefaultHandler</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex</files>
  <action>
    Implement `calculate_mrr_cents/1` in `Accrue.Webhook.DefaultHandler` to calculate MRR from Stripe's `data["items"]["data"]`. 
    Update the `Events.record/1` calls for `dunning.recovered` and `dunning.exhausted` events to include `mrr_value_cents` and `currency` in the `data` map payload.
    Refer to the Code Examples section in `143-RESEARCH.md` for the calculation logic. This ensures MRR is snapshotted at emission time to avoid temporal data leakage.
  </action>
  <verify>
    <automated>mix test test/accrue/webhook/default_handler_test.exs</automated>
  </verify>
  <done>
    `calculate_mrr_cents` private function exists, and `mrr_value_cents` is passed into the `data` payload for both recovery and exhaustion emissions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Dunning Analytics Context</name>
  <files>accrue/lib/accrue/analytics/dunning.ex</files>
  <action>
    Create `Accrue.Analytics.Dunning` providing a public API `recovered_vs_lost_mrr/1`.
    The module must perform Ecto `group_by` aggregations directly against the `accrue_events` table using PostgreSQL JSONB fragments: `sum(fragment("(?->>'mrr_value_cents')::integer", e.data))`.
    It must group by `e.type` filtering for `dunning.recovered` and `dunning.exhausted`.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>
    Module created, returns a map like `%{recovered_cents: int, lost_cents: int}` using JSONB aggregations, without requiring new tables.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analytics Query | Reading from events table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | `Accrue.Analytics.Dunning` | mitigate | Ensure query parameters (since/until) are properly parameterized via Ecto `^` binding to prevent SQL injection in fragment interpolation. |
</threat_model>

<verification>
Automated tests should pass. Ecto queries must compile successfully.
</verification>

<success_criteria>
MRR data is captured on events, and can be queried via Ecto JSONB aggregations.
</success_criteria>

<output>
After completion, create `.planning/phases/143/143-01-SUMMARY.md`
</output>

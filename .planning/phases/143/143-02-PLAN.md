---
phase: 143
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["accrue_admin/lib/accrue_admin/router.ex", "accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex"]
autonomous: true
requirements: [ANA-02]
must_haves:
  truths:
    - User can access the Recovered Revenue dashboard
    - Dashboard displays accurate "Money Saved" metrics
  artifacts:
    - path: accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
      provides: LiveView for the recovery dashboard
    - path: accrue_admin/lib/accrue_admin/router.ex
      provides: Route definition under /analytics scope
  key_links:
    - from: accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
      to: accrue/lib/accrue/analytics/dunning.ex
      via: Calling Dunning.recovered_vs_lost_mrr/1 in mount
    - from: accrue_admin/lib/accrue_admin/router.ex
      to: accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
      via: live "/recovery", RecoveryLive, :index
---

<objective>
Build the `/analytics/recovery` LiveView dashboard in `accrue_admin` to visualize the success of the Dunning Engine.
Purpose: Prove the ROI of Accrue by displaying "Money Saved" without cluttering the main dashboard.
Output: A new standalone LiveView dashboard and route for Dunning Analytics.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/143/143-RESEARCH.md
@.planning/phases/143/143-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RecoveryLive Component</name>
  <files>accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex</files>
  <action>
    Create the `AccrueAdmin.Live.Analytics.RecoveryLive` component. 
    In `mount/3`, call `Accrue.Analytics.Dunning.recovered_vs_lost_mrr/1` (from Wave 1) to load the initial statistics payload.
    Implement the `render/1` callback using the `AppShell.app_shell` component layout pattern (refer to `143-PATTERNS.md`). Render KPI Cards indicating the "Money Saved" (MRR recovered).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>
    Component compiles and fetches data correctly using `Accrue.Analytics.Dunning` in `mount/3`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Recovery Route</name>
  <files>accrue_admin/lib/accrue_admin/router.ex</files>
  <action>
    Define a new `scope "/analytics", AccrueAdmin.Live.Analytics` section in the router.
    Map `live "/recovery", RecoveryLive, :index` inside this scope. 
    Ensure it is placed appropriately alongside the existing administrative LiveView routes.
  </action>
  <verify>
    <automated>mix phx.routes | grep "/analytics/recovery"</automated>
  </verify>
  <done>
    The `/analytics/recovery` route is publicly exposed in the application router.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI Route | Exposing sensitive analytics data to authenticated administrators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-02 | Elevation of Privilege | Router | mitigate | Ensure the new `/analytics` scope is nested within the appropriate `pipe_through` for admin authentication blocks (e.g. `[:browser, :require_authenticated_admin]`). |
</threat_model>

<verification>
Route resolves in the application, and LiveView mounts without crashing, rendering the retrieved analytics metrics.
</verification>

<success_criteria>
Dashboard is fully accessible and displays the data returned by the backend analytics context.
</success_criteria>

<output>
After completion, create `.planning/phases/143/143-02-SUMMARY.md`
</output>

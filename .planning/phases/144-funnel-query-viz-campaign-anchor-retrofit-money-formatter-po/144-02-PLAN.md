---
phase: 144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/test/accrue/webhook/dunning_exhaustion_test.exs
  - accrue/test/accrue/webhook/dunning_campaign_keying_test.exs
autonomous: true
requirements: [DAN-02]
tags: [webhook, dunning, ledger, retrofit, event-payload]

must_haves:
  truths:
    - "A dunning.exhausted ledger event carries data.campaign_anchor as an ISO-8601 string when the subscription had an active anchor at exhaustion"
    - "A dunning.exhausted ledger event carries data.campaign_anchor: nil when the subscription transitioned past_due → canceled/unpaid without an Accrue-set anchor (Stripe-native non-Accrue dunning path)"
    - "A dunning.recovered ledger event carries data.campaign_anchor as the ISO-8601 string of the anchor that was just cleared"
    - "The recovered-edge retrofit preserves the atomic Ecto.Multi write — clear_anchor + Events.record_multi remain in one transaction"
    - "The exhausted-edge retrofit preserves the existing Repo.transact invariant — no nil.year KeyError when anchor is nil"
  artifacts:
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "campaign_anchor: iso_anchor in dunning.exhausted (around :804–814) AND dunning.recovered (around :885–894) Events.record payloads"
      contains: "campaign_anchor:"
    - path: "accrue/test/accrue/webhook/dunning_exhaustion_test.exs"
      provides: "Emission-boundary assertion: dunning.exhausted carries campaign_anchor (binary OR nil)"
      contains: "campaign_anchor"
    - path: "accrue/test/accrue/webhook/dunning_campaign_keying_test.exs"
      provides: "Emission-boundary assertion: dunning.recovered carries campaign_anchor matching anchor set"
      contains: "campaign_anchor"
  key_links:
    - from: "default_handler.ex maybe_emit_dunning_exhaustion/3 (around :777)"
      to: "Events.record/1 data jsonb payload"
      via: "campaign_anchor: iso_anchor map key"
      pattern: "campaign_anchor:"
    - from: "default_handler.ex maybe_finalize_dunning_campaign/3 (around :868)"
      to: "Events.record_multi/3 inside Ecto.Multi"
      via: "iso_anchor already in scope at line 868; one-line append to data map"
      pattern: "campaign_anchor: iso_anchor"
---

<objective>
Snapshot the dunning campaign anchor onto the `dunning.recovered` and `dunning.exhausted` ledger event payloads at their emission sites in `accrue/lib/accrue/webhook/default_handler.ex`. This is the Phase 143 forward-fix that DAN-01's DISTINCT-`(subject_id, campaign_anchor)` funnel relies on for cycled-dunning de-duplication.

Purpose: Closes DAN-02 (campaign-anchor retrofit) + closes Phase 143 emission-boundary test coverage gap noted in `143-VERIFICATION.md §"Notes / Minor Observations" #1`. Independent from Plan 01 (different file, no overlap) — both can run in Wave 1 in parallel.
Output: `default_handler.ex` augmented at two emission sites; emission-boundary assertions added at both retrofit test surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md
@.planning/phases/143/143-VERIFICATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from 144-PATTERNS.md + source reads. -->

From `accrue/lib/accrue/webhook/default_handler.ex` (existing — modify in place):

**Exhausted-edge emission site (around lines 777–820)**:
  - Function: `defp maybe_emit_dunning_exhaustion(%Subscription{} = row, %Subscription{} = updated, canonical)`
  - Pre-conditions: `Subscription.dunning_sweepable?(row)` returns true AND `Subscription.dunning_exhausted_status(updated)` returns non-nil
  - Pre-existing local bindings near the `Events.record/1` call: `source`, `mrr_value_cents`, `currency`, `to_status`
  - Pre-existing payload (around :808–814):
    `Events.record(%{type: "dunning.exhausted", subject_type: "Subscription", subject_id: updated.id, data: %{to_status: to_status, source: source, mrr_value_cents: mrr_value_cents, currency: currency}})`
  - Inside enclosing `Repo.transact/2` — write + signal atomic per doc comment at :777
  - **CRITICAL:** `row.dunning_campaign_started_at` may be `nil` (Stripe-native non-Accrue dunning; `Subscription.dunning_sweepable?/1` at `accrue/lib/accrue/billing/subscription.ex:241` ONLY checks `status: :past_due`)

**Recovered-edge emission site (around lines 865–897)**:
  - Function: `defp maybe_finalize_dunning_campaign/3` (inside `with` chain)
  - Pre-existing local bindings: `anchor` (`%DateTime{}` — guaranteed non-nil via `with` clause), `iso_anchor = DateTime.to_iso8601(anchor)` at line 868, `recovery?`, `mrr_value_cents`, `currency`
  - Pre-existing payload (around :885–894):
    `Events.record_multi(multi, :dunning_recovered_event, %{type: "dunning.recovered", subject_type: "Subscription", subject_id: updated.id, data: %{source: dunning_source(row.dunning_sweep_attempted_at), mrr_value_cents: mrr_value_cents, currency: currency}})`
  - Folded into `Ecto.Multi` alongside `:clear_anchor` (`Subscription.force_status_changeset/2` with `dunning_campaign_started_at: nil`) — atomic with anchor clear

From `accrue/test/accrue/webhook/dunning_exhaustion_test.exs`:
  - Test setup: `%{sub: sub, sub_id: sub_id}` context with a `past_due` subscription
  - Helper: `ledger_events("dunning.exhausted", sub.id)` returns the list of recorded events
  - Existing assertion shape at :308–311: `assert ledger.data["to_status"] == "canceled"`; `assert ledger.data["source"] == "stripe_native"`
  - To set an anchor: `sub |> Accrue.Billing.Subscription.force_status_changeset(%{dunning_campaign_started_at: anchor}) |> Accrue.Repo.update!()`
  - Helper for non-microsecond DateTime: `anchor = %{Accrue.Clock.utc_now() | microsecond: {0, 6}}`

From `accrue/test/accrue/webhook/dunning_campaign_keying_test.exs`:
  - Existing `describe "dunning.recovered observability (DUN-08)"` block at :358–388
  - Helper: `set_anchor/2` (file-local; used at :362)
  - Existing assertion shape at :369–370: `assert ledger.data["source"] == "stripe_native"`
  - Anchor is set in the test before firing the recovery; the snapshotted ISO string must equal `DateTime.to_iso8601(anchor)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retrofit dunning.exhausted emission with defensive iso_anchor (DAN-02)</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/webhook/dunning_exhaustion_test.exs</files>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex (lines 770–830 — `maybe_emit_dunning_exhaustion/3` body including the `Events.record/1` call site at :804–814)
    - accrue/lib/accrue/billing/subscription.ex (lines 240–275 — `dunning_sweepable?/1` only checks `status: :past_due`; anchor may be nil)
    - accrue/test/accrue/webhook/dunning_exhaustion_test.exs (lines 280–340 — existing `describe "dunning.exhausted observability (DUN-08)"` block + `ledger_events` helper usage at :308–311)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md (§"`accrue/lib/accrue/webhook/default_handler.ex`" — exhausted-edge retrofit excerpt)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md (§"Code Examples §3" — Example 3; §"Pitfall 2" — anchor-unavailable rationale)
    - .planning/phases/143/143-VERIFICATION.md (§"Notes / Minor Observations" #1 — the emission-boundary gap this closes)
  </read_first>
  <behavior>
    - Test: A `past_due` subscription with an active anchor (set via `force_status_changeset(%{dunning_campaign_started_at: anchor})`) transitioning to `canceled` records a `dunning.exhausted` ledger event whose `data["campaign_anchor"]` is a non-nil binary parseable by `DateTime.from_iso8601/1`.
    - Test: A `past_due` subscription with NO anchor (Stripe-native immediate-cancel path; `row.dunning_campaign_started_at == nil`) transitioning to `canceled` records a `dunning.exhausted` ledger event whose `data["campaign_anchor"]` is `nil`. The handler does NOT raise (defensive `case` branch).
    - Existing assertions (`to_status`, `source`, `mrr_value_cents`, `currency`) still hold — no regression on the existing emission shape.
  </behavior>
  <action>
    Per D-08 + D-09 + RESEARCH.md Pitfall #2, in `accrue/lib/accrue/webhook/default_handler.ex` inside `maybe_emit_dunning_exhaustion/3` (around lines 777–820), BEFORE the existing `Events.record/1` call at :808–814, add a defensive ISO encoding of `row.dunning_campaign_started_at`:
    `iso_anchor = case row.dunning_campaign_started_at do %DateTime{} = dt -> DateTime.to_iso8601(dt); _ -> nil end`
    Then append `campaign_anchor: iso_anchor` as a new key in the `data:` map of the `Events.record/1` call. Do NOT change the `type`, `subject_type`, `subject_id`, or any of the existing `data:` keys (`to_status`, `source`, `mrr_value_cents`, `currency`). Do NOT use `DateTime.to_iso8601(row.dunning_campaign_started_at)` directly — that raises `KeyError` on `nil`.

    In `accrue/test/accrue/webhook/dunning_exhaustion_test.exs`, inside the existing `describe "dunning.exhausted observability (DUN-08)"` block at :294–318, add two new tests (or extend with two new assertions — planner discretion, prefer two named tests for clarity):

    Test A — "records campaign_anchor when an anchor was set" (per D-10): Set `anchor = %{Accrue.Clock.utc_now() | microsecond: {0, 6}}`; update `sub` via `sub |> Accrue.Billing.Subscription.force_status_changeset(%{dunning_campaign_started_at: anchor}) |> Accrue.Repo.update!()`; fire the same `customer.subscription.updated → :canceled` webhook as the existing test; assert `[ledger] = ledger_events("dunning.exhausted", sub.id)` AND `is_binary(ledger.data["campaign_anchor"])` AND `{:ok, %DateTime{}, _} = DateTime.from_iso8601(ledger.data["campaign_anchor"])` AND `ledger.data["campaign_anchor"] == DateTime.to_iso8601(anchor)`.

    Test B — "records campaign_anchor: nil when no anchor was set (Stripe-native path)" (defensive branch): Do NOT pre-set the anchor on `sub`; fire the same webhook; assert `[ledger] = ledger_events("dunning.exhausted", sub.id)` AND `is_nil(ledger.data["campaign_anchor"])`.

    Do NOT add `:telemetry` event emissions or new telemetry metadata — this retrofit is payload-only (CONTEXT.md does not specify telemetry changes).
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/dunning_exhaustion_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue/lib/accrue/webhook/default_handler.ex` contains `iso_anchor = case row.dunning_campaign_started_at do` near the exhausted-edge emission site
    - File `accrue/lib/accrue/webhook/default_handler.ex` contains `campaign_anchor: iso_anchor` inside the `dunning.exhausted` `Events.record/1` data map (grep `'campaign_anchor:'` returns at least one match in the file)
    - File `accrue/test/accrue/webhook/dunning_exhaustion_test.exs` contains an assertion on `ledger.data["campaign_anchor"]` with `is_binary` AND a separate test asserting `is_nil(ledger.data["campaign_anchor"])`
    - `cd accrue && mix test test/accrue/webhook/dunning_exhaustion_test.exs` exits 0 (all pre-existing tests in the file pass + two new tests pass)
    - No raise/error in the nil-anchor test case (defensive branch works)
  </acceptance_criteria>
  <done>Exhausted-edge payload carries `campaign_anchor` ISO string OR nil; both code paths covered by emission-boundary tests; no nil.year KeyError regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrofit dunning.recovered emission inside Ecto.Multi (DAN-02)</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/webhook/dunning_campaign_keying_test.exs</files>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex (lines 855–905 — `maybe_finalize_dunning_campaign/3` body; `iso_anchor` already in scope at :868 via the `with` clause)
    - accrue/test/accrue/webhook/dunning_campaign_keying_test.exs (lines 1–60 + 355–389 — `describe "dunning.recovered observability (DUN-08)"` block; `set_anchor/2` helper)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md (§"`accrue/lib/accrue/webhook/default_handler.ex`" — recovered-edge retrofit excerpt; "the only new line")
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md (§"Code Examples §4" — Example 4)
  </read_first>
  <behavior>
    - Test: A `past_due` subscription with an active anchor (set via `set_anchor/2`) transitioning to `active` records a `dunning.recovered` ledger event whose `data["campaign_anchor"] == DateTime.to_iso8601(anchor)` (exact string match — `iso_anchor` is computed from the same DateTime that's about to be cleared).
    - Existing assertions (`source`) still hold — no regression on existing payload shape.
    - The `Ecto.Multi` atomicity is preserved: `clear_anchor` + `Events.record_multi` still commit in one transaction (i.e., `sub.dunning_campaign_started_at` is `nil` AFTER the recovery webhook fires).
  </behavior>
  <action>
    Per D-08 + D-09, in `accrue/lib/accrue/webhook/default_handler.ex` inside `maybe_finalize_dunning_campaign/3`'s recovery branch (around lines 885–894), append a single new key `campaign_anchor: iso_anchor` to the `data:` map of the `Events.record_multi/3` call. `iso_anchor` is ALREADY in lexical scope at line 868 (`iso_anchor = DateTime.to_iso8601(anchor)`) — do NOT recompute it; do NOT compute defensively (the `with %DateTime{} = anchor <- row.dunning_campaign_started_at` clause guarantees non-nil).

    Do NOT change the `Ecto.Multi` shape — `clear_anchor` and `Events.record_multi` MUST remain in the same `Multi`. Do NOT change the `type`, `subject_type`, `subject_id`, `source`, `mrr_value_cents`, or `currency` keys.

    In `accrue/test/accrue/webhook/dunning_campaign_keying_test.exs`, inside the existing `describe "dunning.recovered observability (DUN-08)"` block at :358–388, extend the existing test (after `assert ledger.data["source"] == "stripe_native"` at around :369) with three new assertions:
    `assert is_binary(ledger.data["campaign_anchor"])`
    `assert {:ok, %DateTime{}, _} = DateTime.from_iso8601(ledger.data["campaign_anchor"])`
    `assert ledger.data["campaign_anchor"] == DateTime.to_iso8601(anchor)` (where `anchor` is the `%{Accrue.Clock.utc_now() | microsecond: {0, 6}}` value already in test scope from `set_anchor(sub, anchor)` at :362).
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/dunning_campaign_keying_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue/lib/accrue/webhook/default_handler.ex` contains a SECOND `campaign_anchor:` occurrence (one for exhausted, one for recovered — `grep -c 'campaign_anchor:' accrue/lib/accrue/webhook/default_handler.ex` returns at least 2)
    - The recovered-edge `Events.record_multi` payload uses the existing `iso_anchor` binding (NOT a fresh `DateTime.to_iso8601` call) — visible by reading the diff
    - File `accrue/test/accrue/webhook/dunning_campaign_keying_test.exs` contains `ledger.data["campaign_anchor"] == DateTime.to_iso8601(anchor)`
    - `cd accrue && mix test test/accrue/webhook/dunning_campaign_keying_test.exs` exits 0 (all pre-existing tests pass + extended observability assertions pass)
    - Post-recovery, `sub.dunning_campaign_started_at` is `nil` in DB (Ecto.Multi atomicity preserved — `clear_anchor` still runs)
  </acceptance_criteria>
  <done>Recovered-edge payload carries `campaign_anchor` ISO string matching the cleared anchor; Ecto.Multi atomicity preserved; emission-boundary test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe webhook → DefaultHandler → accrue_events | Webhook signature is verified upstream (T-128-01); this plan adds a payload field but does not change the trust boundary |
| Subscription row → event payload | `row.dunning_campaign_started_at` originates from prior Accrue writes; ISO encoding is server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-04 | Tampering (reporting integrity — write path) | `default_handler.ex` exhausted + recovered emission sites | mitigate | Inject `campaign_anchor` as ISO-8601 string from `row.dunning_campaign_started_at` BEFORE `clear_anchor` runs in the recovered case. Atomic via existing `Ecto.Multi` / `Repo.transact`. Emission-boundary tests (Tasks 1+2) enforce. |
| T-144-05 | Denial of Service (nil.year crash) | `maybe_emit_dunning_exhaustion/3` defensive branch | mitigate | `case row.dunning_campaign_started_at do %DateTime{} -> ... ; _ -> nil end` prevents the `KeyError` that bare `DateTime.to_iso8601/1` would raise on `nil`. Test B in Task 1 covers the nil-anchor path. |

**Threats considered, none applicable:** Information Disclosure (`campaign_anchor` is an ISO-8601 DateTime — server clock value derived from `Accrue.Clock`, not PII; per T-129-01 the existing posture is reaffirmed), Spoofing (webhook signature gate is upstream and unchanged), Repudiation (append-only ledger immutability trigger SQLSTATE 45A01 is intact — D-07), Elevation of Privilege (no auth surface in webhook handler).
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/webhook/dunning_exhaustion_test.exs` exits 0.
- `cd accrue && mix test test/accrue/webhook/dunning_campaign_keying_test.exs` exits 0.
- Cross-check: `grep -c 'campaign_anchor:' accrue/lib/accrue/webhook/default_handler.ex` returns at least 2 (one per emission site).
- Full retrofit-adjacent suite: `cd accrue && mix test test/accrue/webhook/dunning_exhaustion_test.exs test/accrue/webhook/dunning_campaign_keying_test.exs test/accrue/webhook/dunning_campaign_start_test.exs test/accrue/jobs/dunning_sweeper_test.exs` exits 0 (no broader regression).
</verification>

<success_criteria>
- [ ] `dunning.exhausted` event payload includes `campaign_anchor` (ISO-8601 string OR nil) — emission-boundary test asserts both branches (DAN-02 + ROADMAP SC#2 prerequisite)
- [ ] `dunning.recovered` event payload includes `campaign_anchor` as the exact ISO-8601 string of the cleared anchor — emission-boundary test asserts string equality (DAN-02 + ROADMAP SC#2 prerequisite)
- [ ] Exhausted-edge defensive `case` handles `nil` anchor without raising (Pitfall #2 from RESEARCH.md)
- [ ] Recovered-edge `Ecto.Multi` atomicity preserved — `clear_anchor` + `dunning_recovered_event` still in one transaction
- [ ] Phase 143 emission-boundary test coverage gap (143-VERIFICATION.md §Notes #1) is now closed
- [ ] All pre-existing dunning webhook tests pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-02-SUMMARY.md` when done. Summary must include: confirmation of both retrofit sites edited (with `grep -c 'campaign_anchor:'` count), the recovered-edge `iso_anchor`-reuse vs exhausted-edge defensive-case distinction, three new emission-boundary test outcomes (anchor-present exhausted + nil-anchor exhausted + anchor-equals-cleared recovered), and confirmation that the broader retrofit-adjacent suite stays green.
</output>

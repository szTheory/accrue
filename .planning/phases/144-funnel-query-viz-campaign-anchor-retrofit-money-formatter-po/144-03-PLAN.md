---
phase: 144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po
plan: 03
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - accrue_admin/lib/accrue_admin/components/funnel_chart.ex
  - accrue_admin/test/accrue_admin/components/funnel_chart_test.exs
  - accrue_admin/assets/css/app.css
autonomous: true
requirements: [DAN-09]
tags: [phoenix-component, svg, accessibility, css, funnel-chart, design-system]

must_haves:
  truths:
    - "AccrueAdmin.Components.FunnelChart.funnel_chart/1 is a functional Phoenix.Component (use Phoenix.Component, NOT use Phoenix.LiveView) — no socket runtime"
    - "Rendering funnel_chart with entered=10, recovered=4, exhausted=3, active=3 produces inline SVG with three <rect> bars at proportional widths and an external <dl> legend listing all four counts"
    - "When entered=0, the component renders without dividing by zero (recovered_pct and exhausted_pct each return 0)"
    - "The component renders 'Recovered MRR' tooltip with the yearly-plan worked example copy on the Exhausted stage"
    - "CSS classes .ax-funnel-chart, .ax-funnel-row, .ax-funnel-row--{slate,moss,amber}, .ax-funnel-bar, .ax-funnel-legend exist in app.css adjacent to .ax-kpi-* block"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/funnel_chart.ex"
      provides: "FunnelChart functional component (entered/recovered/exhausted/active attrs)"
      contains: "use Phoenix.Component"
    - path: "accrue_admin/test/accrue_admin/components/funnel_chart_test.exs"
      provides: "Component-unit tests via Phoenix.LiveViewTest.render_component/2"
      contains: "render_component"
    - path: "accrue_admin/assets/css/app.css"
      provides: ".ax-funnel-* class block appended after .ax-kpi-* block (after line 560)"
      contains: ".ax-funnel-chart"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/funnel_chart.ex"
      to: "accrue_admin/assets/css/app.css"
      via: ".ax-funnel-* class names referenced in component"
      pattern: "ax-funnel-(chart|row|bar|legend|active-chip)"
    - from: "accrue_admin/lib/accrue_admin/components/funnel_chart.ex"
      to: "accrue_admin/lib/accrue_admin/components/kpi_card.ex"
      via: "Phoenix.Component shell pattern (use Phoenix.Component + attr macros + ax-card outer)"
      pattern: "use Phoenix.Component"
---

<objective>
Build `AccrueAdmin.Components.FunnelChart` — a pure functional Phoenix.Component rendering an inline-SVG horizontal proportional-bar funnel with three stages (Entered, Recovered, Exhausted), an "active currently in dunning" chip, and an external `<dl>` legend. Add the matching `.ax-funnel-*` CSS block to `app.css`. Add a component-unit test using `Phoenix.LiveViewTest.render_component/2`.

Purpose: Closes the visualization half of DAN-09. Depends on Plan 01 only for the `funnel/1` return-shape contract (`%{entered, recovered, exhausted, active}` — 4 keys); does NOT depend on Plan 02. Plan 04 will wire this component into RecoveryLive.
Output: New `accrue_admin/lib/accrue_admin/components/funnel_chart.ex`, new `accrue_admin/test/accrue_admin/components/funnel_chart_test.exs`, augmented `accrue_admin/assets/css/app.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from 144-PATTERNS.md + source reads. -->

From `accrue_admin/lib/accrue_admin/components/kpi_card.ex` (analog — closest existing functional component):
  - module shell: `defmodule AccrueAdmin.Components.KpiCard do use Phoenix.Component end`
  - attr macros: `attr(:label, :string, required: true)`, `attr(:value, :string, required: true)`, etc.
  - outer wrapper: `<article class={["ax-card ax-kpi-card", @class]}>`
  - helper-defp pattern for derived assigns: `defp normalize_tone/1`
  - lines: 1–67 (full file)

From `accrue_admin/assets/css/app.css` (existing tone palette + SVG idiom — analog block at lines 519–560):
  - `.ax-kpi-delta-moss` uses `var(--ax-success)` + `color-mix(in srgb, var(--ax-success) 16%, transparent)`
  - `.ax-kpi-delta-amber` uses `var(--ax-warning)` + `color-mix(... var(--ax-warning) 16% ...)`
  - `.ax-kpi-delta-cobalt` uses `var(--ax-accent)` + `color-mix(... var(--ax-accent) 16% ...)`
  - `.ax-kpi-delta-slate` uses `var(--ax-muted) 12%`
  - `.ax-kpi-sparkline svg` uses `width: 100%; height: 3rem; color: var(--ax-accent)` + `fill: none; stroke: currentColor`

Component contract (`AccrueAdmin.Components.FunnelChart.funnel_chart/1`):
  - `attr(:entered, :integer, required: true)`
  - `attr(:recovered, :integer, required: true)`
  - `attr(:exhausted, :integer, required: true)`
  - `attr(:active, :integer, required: true)`
  - `attr(:class, :string, default: nil)`
  - private `defp pct(_n, 0), do: 0` and `defp pct(n, total), do: round(n * 100 / total)`
  - external rendering: ALWAYS use `Phoenix.LiveViewTest.render_component/2` for unit testing (no LiveView mount needed)

SVG structure (from D-13, D-14, D-15, D-16):
  - `<svg viewBox="0 0 100 36" role="img" aria-labelledby="funnel-title funnel-desc" preserveAspectRatio="none" class="ax-funnel-svg">`
  - `<title id="funnel-title">Dunning recovery funnel</title>`
  - `<desc id="funnel-desc">… counts narrative …</desc>`
  - Three `<g transform="translate(0, idx*12)" class="ax-funnel-row ax-funnel-row--{slate|moss|amber}">` rows
  - Each row: `<rect width={pct_value} height="10" rx="1.5" class="ax-funnel-bar">` with inline `<title>` for per-bar hover tooltip
  - Entered row always width="100" (it's the 100% denominator); recovered/exhausted use computed `recovered_pct`/`exhausted_pct`

Legend structure:
  - `<dl class="ax-funnel-legend">` with one `<div class="ax-funnel-legend-row"><dt>Entered</dt><dd>{count}</dd></div>` per stage
  - Recovered/Exhausted legend rows include the percentage in a `<span class="ax-muted">({pct}%)</span>`

Active chip:
  - `<span class="ax-funnel-active-chip">{active} currently in dunning</span>` inside the `<header>` (component owns it per RESEARCH.md OQ#2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AccrueAdmin.Components.FunnelChart functional component</name>
  <files>accrue_admin/lib/accrue_admin/components/funnel_chart.ex, accrue_admin/test/accrue_admin/components/funnel_chart_test.exs</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/components/kpi_card.ex (lines 1–67 — full file; analog shell + attr macros + helper-defp pattern)
    - accrue_admin/lib/accrue_admin/components/money_formatter.ex (lines 1–77 — secondary `use Phoenix.Component` example)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md (D-13 through D-17, plus §Specifics "Active count exposure")
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md (§"`accrue_admin/lib/accrue_admin/components/funnel_chart.ex`" — full skeleton; §"No Analog Found" — test file uses stdlib `Phoenix.LiveViewTest.render_component/2`)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md (§"Pattern 3" — full FunnelChart code block in Example 3, lines 336–410)
  </read_first>
  <behavior>
    - Test: `render_component(&FunnelChart.funnel_chart/1, entered: 10, recovered: 4, exhausted: 3, active: 3)` returns HTML containing `"Recovery Funnel"`, `"10"`, `"4"`, `"3"` (entered/recovered/exhausted counts), `"3 currently in dunning"` (active chip), `"40%"` and `"30%"` (computed percentages), and at least three `<rect` elements.
    - Test: `render_component(&FunnelChart.funnel_chart/1, entered: 0, recovered: 0, exhausted: 0, active: 0)` returns HTML containing `"Entered"` (legend label present) without `"NaN"` (pct guard works) and without crashing.
    - Test: HTML contains `role="img"` AND `aria-labelledby="funnel-title funnel-desc"` AND `<title id="funnel-title">` AND `<desc id="funnel-desc">` (accessibility contract from D-15).
    - Test: HTML contains the yearly-plan worked-example copy substring — `"$120/yr"` AND `"$10/mo"` AND `"Exhausted MRR"` (Exhausted tooltip content from D-16).
    - Test: HTML contains class names `ax-funnel-row--slate`, `ax-funnel-row--moss`, `ax-funnel-row--amber` (one per stage; D-14 tone palette).
  </behavior>
  <action>
    Create `accrue_admin/lib/accrue_admin/components/funnel_chart.ex` per D-17 + RESEARCH.md Pattern 3. Module: `defmodule AccrueAdmin.Components.FunnelChart do`. `@moduledoc` describing the 3-stage funnel and `@since "1.4.0"`. `use Phoenix.Component` (per D-17 and CLAUDE.md C9 — NOT `use Phoenix.LiveView`). Declare attrs exactly: `attr(:entered, :integer, required: true)`, `attr(:recovered, :integer, required: true)`, `attr(:exhausted, :integer, required: true)`, `attr(:active, :integer, required: true)`, `attr(:class, :string, default: nil)`.

    Public function `def funnel_chart(assigns)` body: first compute derived assigns via `assigns = assigns |> assign(:recovered_pct, pct(assigns.recovered, assigns.entered)) |> assign(:exhausted_pct, pct(assigns.exhausted, assigns.entered))`. Then render the `~H` template per the RESEARCH.md Pattern 3 skeleton — outer `<article class={["ax-card", "ax-funnel-chart", @class]}>` (mirrors KpiCard's `ax-card` shell per D-17). Inside: a `<header class="ax-funnel-header">` containing `<p class="ax-label">Recovery Funnel</p>` and the active chip `<span class="ax-funnel-active-chip"><%= @active %> currently in dunning</span>`.

    The SVG (per D-13, D-14, D-15): `<svg viewBox="0 0 100 36" role="img" aria-labelledby="funnel-title funnel-desc" preserveAspectRatio="none" class="ax-funnel-svg">` with `<title id="funnel-title">Dunning recovery funnel</title>` and `<desc id="funnel-desc">` carrying a counts narrative (e.g., "10 campaigns entered, 4 recovered, 3 exhausted."). Three `<g transform="translate(0, idx*12)">` rows: idx=0 `class="ax-funnel-row ax-funnel-row--slate"` with `<rect width="100" height="10" rx="1.5" class="ax-funnel-bar">` + inline `<title>Entered: <%= @entered %> campaigns</title>`; idx=12 `--moss` with `<rect width={@recovered_pct}…>` + inline `<title>Recovered: <%= @recovered %> campaigns (<%= @recovered_pct %>% of entered)</title>`; idx=24 `--amber` with `<rect width={@exhausted_pct}…>` + inline `<title>` carrying the worked yearly-plan example per D-16: "Exhausted: <count> campaigns (<pct>% of entered). A $120/yr plan that exhausts dunning contributes $10/mo to Exhausted MRR — annualized MRR snapshot at the exhaustion event."

    External `<dl class="ax-funnel-legend">` with one `<div class="ax-funnel-legend-row"><dt>{Stage}</dt><dd>{count} <span class="ax-muted">({pct}%)</span></dd></div>` per stage (Entered omits the `(pct%)` span since it's the denominator).

    Private helpers: `defp pct(_n, 0), do: 0` and `defp pct(n, total), do: round(n * 100 / total)`.

    Then create `accrue_admin/test/accrue_admin/components/funnel_chart_test.exs` per 144-PATTERNS.md §"No Analog Found": `defmodule AccrueAdmin.Components.FunnelChartTest do use ExUnit.Case, async: true; import Phoenix.LiveViewTest; alias AccrueAdmin.Components.FunnelChart end`. Five tests inside `describe "funnel_chart/1"`:
    1. `"renders all 4 counts in legend"` — call with `entered: 10, recovered: 4, exhausted: 3, active: 3`; assert html contains `"Recovery Funnel"`, `"10"`, `"4"`, `"3"`, `"3 currently in dunning"`.
    2. `"renders proportional percentages"` — same assigns; assert html contains `"40"` (recovered_pct) and `"30"` (exhausted_pct).
    3. `"guards against division-by-zero when entered: 0"` — call with all zeros; assert html contains `"Entered"`; refute html contains `"NaN"`.
    4. `"declares accessibility contract"` — assert html contains `role="img"`, `aria-labelledby="funnel-title funnel-desc"`, `<title id="funnel-title">`, `<desc id="funnel-desc">`.
    5. `"renders Exhausted tooltip with yearly-plan worked example"` — call with non-zero counts; assert html contains `"$120/yr"`, `"$10/mo"`, `"Exhausted MRR"`.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/funnel_chart_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue_admin/lib/accrue_admin/components/funnel_chart.ex` exists
    - File contains `use Phoenix.Component` (grep) AND does NOT contain `use Phoenix.LiveView` (grep)
    - File contains five `attr(:` declarations: `:entered`, `:recovered`, `:exhausted`, `:active`, `:class`
    - File contains `defp pct(_n, 0), do: 0`
    - File contains the literal substrings `role="img"`, `aria-labelledby="funnel-title funnel-desc"`, `ax-funnel-row--slate`, `ax-funnel-row--moss`, `ax-funnel-row--amber`
    - File contains the worked-example tooltip copy substring `$120/yr` AND `$10/mo`
    - File `accrue_admin/test/accrue_admin/components/funnel_chart_test.exs` exists with 5 tests inside `describe "funnel_chart/1"`
    - `cd accrue_admin && mix test test/accrue_admin/components/funnel_chart_test.exs` exits 0
  </acceptance_criteria>
  <done>FunnelChart component compiles, renders correctly for both populated and empty inputs, accessibility contract present, component-unit test green.</done>
</task>

<task type="auto">
  <name>Task 2: Append .ax-funnel-* CSS block to app.css</name>
  <files>accrue_admin/assets/css/app.css</files>
  <read_first>
    - accrue_admin/assets/css/app.css (lines 515–560 — existing `.ax-kpi-delta-*` tone palette + `.ax-kpi-sparkline svg` SVG idiom; new block APPENDS after :560)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md (D-14, D-17)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md (§"Code Examples §9" — Example 9 full CSS block)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md (§"`accrue_admin/assets/css/app.css`" — quotes tone palette + SVG idiom)
  </read_first>
  <action>
    Per D-14 + D-17 + RESEARCH.md Example 9, append the following CSS block to `accrue_admin/assets/css/app.css` immediately after the existing `.ax-kpi-sparkline path` rule at line 560 (no new tokens, only reuse existing custom properties):

    `.ax-funnel-chart` (display: grid; gap: var(--ax-space-md))
    `.ax-funnel-header` (display: flex; align-items: center; justify-content: space-between; gap: var(--ax-space-md))
    `.ax-funnel-active-chip` (display: inline-flex; align-items: center; padding: var(--ax-space-xs) var(--ax-space-sm); border-radius: 999px; font-size: 0.875rem; font-weight: 600; color: var(--ax-accent-readable); background: color-mix(in srgb, var(--ax-accent) 16%, transparent) — mirrors `.ax-kpi-delta-cobalt`)
    `.ax-funnel-svg` (width: 100%; height: 3.5rem)
    `.ax-funnel-row--slate .ax-funnel-bar` (fill: color-mix(in srgb, var(--ax-muted) 28%, transparent))
    `.ax-funnel-row--moss .ax-funnel-bar` (fill: var(--ax-success))
    `.ax-funnel-row--amber .ax-funnel-bar` (fill: var(--ax-warning))
    `.ax-funnel-legend` (display: grid; gap: var(--ax-space-xs); margin: 0)
    `.ax-funnel-legend-row` (display: flex; justify-content: space-between; font-size: 0.875rem)
    `.ax-funnel-legend-row dt` (font-weight: 600)
    `.ax-funnel-legend-row dd` (margin: 0; color: var(--ax-primary))

    Do NOT define new CSS custom properties (no new `--ax-*` tokens). Do NOT add `@media` queries or theme-override blocks — `currentColor` + existing `var(--ax-*)` custom properties already handle light/dark theming.
  </action>
  <verify>
    <automated>grep -c '\.ax-funnel' accrue_admin/assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue_admin/assets/css/app.css` contains all 11 selectors listed in `<action>`
    - `grep -c '\.ax-funnel' accrue_admin/assets/css/app.css` returns >= 11 (one per selector line, possibly more)
    - File does NOT introduce any new `--ax-*` custom property declarations (grep `^\s*--ax-[a-z]*:` returns the same count as before)
    - The new block is positioned AFTER the existing `.ax-kpi-sparkline path` rule (visible by reading the file after the edit)
    - `cd accrue_admin && mix compile --warnings-as-errors` exits 0 (asset compile is part of mix compile in this project, OR `cd accrue_admin && mix assets.deploy` exits 0 if separate)
  </acceptance_criteria>
  <done>`.ax-funnel-*` CSS block appended after `.ax-kpi-*` block; no new design-system tokens; existing token palette reused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (FunnelChart render) → Browser (DOM) | SSR HEEx escapes interpolations by default; integer attrs are typed; no user input reaches inline SVG/HTML attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-06 | Cross-Site Scripting (XSS via SVG/HTML interpolation) | `funnel_chart/1` HEEx template | accept | All attrs are typed `:integer` (Phoenix.Component runtime validation rejects strings). All interpolations use `<%= @assign %>` which HEEx escapes by default. No `raw/1` or `Phoenix.HTML.raw/1` calls in the component. Static literals only in SVG attributes. |
| T-144-07 | DoS via malformed component args | `funnel_chart/1` `pct/2` helper | mitigate | `defp pct(_n, 0), do: 0` guard prevents division-by-zero; test 3 in Task 1 enforces. Integer-only attrs prevent type coercion DoS. |

**Threats considered, none applicable:** Authentication/Session/Access Control (FunnelChart is a pure rendering component — no auth surface; admin auth is enforced at the LiveView level inherited from `live_session :accrue_admin` in router.ex), Tampering (no state mutations), Information Disclosure (counts are aggregate integers — no PII), Communications (server-side render, no outbound requests).
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/components/funnel_chart_test.exs` exits 0.
- `cd accrue_admin && mix compile --warnings-as-errors` exits 0.
- `grep -c '\.ax-funnel' accrue_admin/assets/css/app.css` returns >= 11.
- `grep -c 'use Phoenix.LiveView' accrue_admin/lib/accrue_admin/components/funnel_chart.ex` returns 0 (LiveView-runtime-free posture preserved per CLAUDE.md C9).
</verification>

<success_criteria>
- [ ] `AccrueAdmin.Components.FunnelChart.funnel_chart/1` is a functional `Phoenix.Component` accepting four required integer attrs + one optional class attr (DAN-09)
- [ ] Component renders inline SVG with three proportional bars (Entered/Recovered/Exhausted), an "active currently in dunning" chip, and an external `<dl>` legend (DAN-09 + ROADMAP SC#1)
- [ ] Accessibility contract present: `role="img"`, linked `<title>`/`<desc>` via `aria-labelledby`, per-bar inline `<title>` for screen readers (DAN-09 + D-15)
- [ ] Exhausted-stage tooltip carries the yearly-plan worked-example copy (DAN-09 + ROADMAP SC#5 + D-16)
- [ ] `.ax-funnel-*` CSS block appended to `app.css` reusing existing `var(--ax-*)` tokens (D-14 + D-17 — no new design-system tokens)
- [ ] Component-unit test covers populated input, empty input (division-by-zero guard), a11y contract, worked-example copy
- [ ] `use Phoenix.Component` only — no `use Phoenix.LiveView` (LiveView-runtime-free posture, CLAUDE.md C9)
</success_criteria>

<output>
Create `.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-03-SUMMARY.md` when done. Summary must include: confirmation of `use Phoenix.Component` (with grep result for `Phoenix.LiveView` returning 0), the FunnelChart attrs list, five component-test outcomes, and the CSS selector count from `grep -c '\.ax-funnel' accrue_admin/assets/css/app.css`.
</output>

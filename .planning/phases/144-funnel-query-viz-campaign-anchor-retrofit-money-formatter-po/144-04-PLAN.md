---
phase: 144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po
plan: 04
type: execute
wave: 3
depends_on: ["144-01", "144-03"]
files_modified:
  - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
  - accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs
autonomous: true
requirements: [DAN-09, DAN-13]
tags: [liveview, recovery-dashboard, money-formatter, cldr, jpy-regression, copy-rename]

must_haves:
  truths:
    - "Operator visiting /billing/analytics/recovery sees a FunnelChart rendered directly below the existing ax-kpi-grid section"
    - "The KPI card previously labeled 'Lost MRR' now reads 'Exhausted MRR' with a delta-tooltip carrying the yearly-plan worked example"
    - "With Application.put_env(:accrue, :default_currency, :jpy), a seeded dunning.recovered event with mrr_value_cents: 5000 + currency: \"jpy\" renders with ¥ (or CLDR locale-correct symbol) in the rendered HTML, NOT with $"
    - "RecoveryLive.format_minor/1 is deleted from recovery_live.ex (no more :erlang.float_to_binary)"
    - "RecoveryLive.mount/3 calls Accrue.Analytics.Dunning.funnel() (no opts in P144 — Phase 145 threads :since/:until)"
    - "RecoveryLive uses Accrue.Config.get!(:default_currency) — never Application.compile_env"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex"
      provides: "Funnel + MoneyFormatter swap + label rename + FunnelChart slot"
      contains: "FunnelChart.funnel_chart"
    - path: "accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs"
      provides: "JPY regression test + funnel-render assertion + Exhausted-MRR copy assertion"
      contains: "default_currency"
  key_links:
    - from: "recovery_live.ex mount/3"
      to: "Accrue.Analytics.Dunning.funnel/1"
      via: "Dunning.funnel() call returns 4-key map"
      pattern: "Dunning.funnel"
    - from: "recovery_live.ex render/1"
      to: "AccrueAdmin.Components.FunnelChart"
      via: "<FunnelChart.funnel_chart entered={@funnel.entered} ... />"
      pattern: "FunnelChart.funnel_chart"
    - from: "recovery_live.ex mount/3"
      to: "Accrue.Invoices.Render.format_money/3"
      via: "MoneyFormatter via Render.format_money/3 driven by Accrue.Config.get!(:default_currency) + Accrue.Config.default_locale()"
      pattern: "Accrue.Config.get!\\(:default_currency\\)"
---

<objective>
Wire the dashboard end-to-end: call `Accrue.Analytics.Dunning.funnel/1` from `RecoveryLive.mount/3`, render `<FunnelChart.funnel_chart>` directly below the existing `<section class="ax-kpi-grid">` block, replace the USD-only `format_minor/1` helper with `Accrue.Invoices.Render.format_money/3` driven by `Accrue.Config.get!(:default_currency)` + `Accrue.Config.default_locale()`, rename the "Lost MRR" KPI card to "Exhausted MRR" with the yearly-plan worked-example tooltip, and add the JPY regression test.

Purpose: Closes the UI half of DAN-09 (funnel rendered below KPI cards) and all of DAN-13 (MoneyFormatter polish for currency-correct rendering across JPY/EUR/GBP/USD). Implements ROADMAP success criteria #1, #4, #5. Depends on Plan 01 (funnel/1 contract) and Plan 03 (FunnelChart component) — wave 3.
Output: Augmented `accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex`, augmented `accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-01-PLAN.md
@.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-03-PLAN.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from 144-PATTERNS.md + source reads. -->

From `accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex` (existing — modify in place):
  - module: `AccrueAdmin.Live.Analytics.RecoveryLive` (LiveView)
  - existing alias: `alias AccrueAdmin.Components.{AppShell, Breadcrumbs, KpiCard}` (line 7)
  - existing `mount/3` (lines 9–18): reads `admin = Map.get(session, "accrue_admin", %{})`, calls `stats = Dunning.recovered_vs_lost_mrr()`, assigns `:stats`
  - existing `render/1` includes a `<section class="ax-kpi-grid">` block (lines 38–56) with two `<KpiCard.kpi_card>` elements
  - existing private helper `format_minor/1` (lines 76–81): `"$" <> :erlang.float_to_binary(amount_minor / 100, decimals: 2)` — USD-ONLY BUG, DELETE
  - existing import: `alias Accrue.Analytics.Dunning` (verify line; should already be present from Phase 143)

From Plan 01's deliverable (`Accrue.Analytics.Dunning.funnel/1`):
  - return shape: `%{entered: integer, recovered: integer, exhausted: integer, active: integer}` (4 keys, all integers)
  - call without opts: `Dunning.funnel()` returns the full-ledger funnel (windowing arrives in Phase 145 via DAN-10)

From Plan 03's deliverable (`AccrueAdmin.Components.FunnelChart.funnel_chart/1`):
  - alias: `AccrueAdmin.Components.FunnelChart`
  - call shape: `<FunnelChart.funnel_chart entered={@funnel.entered} recovered={@funnel.recovered} exhausted={@funnel.exhausted} active={@funnel.active} />`
  - attrs are required + integer-typed

From `accrue_admin/lib/accrue_admin/components/kpi_card.ex`:
  - `attr(:value, :string, required: true)` — **value is a string, NOT a slot** (per RESEARCH.md A6). MUST compute the formatted-money string in `mount/3` and pass it through; cannot nest `<MoneyFormatter>` inside `<KpiCard>`.

From `accrue/lib/accrue/invoices/render.ex` (lines 100–150):
  - `format_money(amount_minor :: integer, currency :: atom | String.t(), locale :: atom | String.t()) :: String.t()` — never raises; double-fallback to `"N currency"` raw string

From `accrue/lib/accrue/config.ex`:
  - `Accrue.Config.get!(:default_currency)` — returns atom (`:usd`, `:jpy`, `:eur`, …); RUNTIME accessor (NOT `Application.compile_env`)
  - `Accrue.Config.default_locale()` — accessor (returns string or atom; pass through to `format_money/3`)

From `accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs`:
  - existing setup at :22–32 uses `Application.put_env(:accrue, :auth_adapter, AuthAdapter)` + `on_exit(fn -> Application.put_env(...) end)` for restoration — mirror this pattern for `:default_currency`
  - existing test at :48–66 asserts `html =~ "Lost MRR"` and `html =~ "$50.00"` — must update to "Exhausted MRR" + remove the literal `$50.00` (still present under USD default, but rename + funnel additions need new assertions)
  - existing `Events.record(%{type: "dunning.recovered", ... mrr_value_cents: 5000, currency: "usd"})` seeding shape — extend with cycled-dunning fixture for funnel render assertion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire funnel + MoneyFormatter + label rename in RecoveryLive</name>
  <files>accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex (full file — current mount/3, render/1, and format_minor/1 helper)
    - accrue_admin/lib/accrue_admin/components/kpi_card.ex (lines 1–67 — confirm `value` attr is `:string, required: true`)
    - accrue/lib/accrue/invoices/render.ex (lines 100–150 — `format_money/3` contract; never raises, double-fallback)
    - accrue/lib/accrue/config.ex (lines 160–170 and 370–390 and 1010–1030 — `get!/1` and `default_locale/0` accessors)
    - accrue/lib/accrue/billing/metered_renewal_invoice.ex (line 258 — `Accrue.Config.get!(:default_currency)` runtime-read precedent)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md (D-13, D-18, D-19, D-20, D-22, D-23)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md (§"`accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex`" — quotes current mount/3 + render/1 + format_minor/1, and the target retrofit)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md (§"Code Examples §5" — Example 5 MoneyFormatter swap shape; §"Pitfall 4" — compile_env vs runtime accessor)
  </read_first>
  <behavior>
    - Compile-time: `recovery_live.ex` compiles without warnings (no unused `format_minor/1`).
    - Run-time: `mount/3` produces socket assigns `:stats`, `:funnel`, `:recovered_str`, `:exhausted_str` (plus the existing `:shell` from `assign_shell/2`).
    - Render: HTML contains the existing `<section class="ax-kpi-grid">` followed IMMEDIATELY by a FunnelChart element.
    - Render: KPI card formerly labeled "Lost MRR" now reads "Exhausted MRR" with a delta string carrying the yearly-plan worked example.
    - Render: With default `:usd`, both KPI card values are CLDR-formatted (e.g., `"$50.00"` for 5000 cents).
  </behavior>
  <action>
    Per D-13 + D-18 + D-19 + D-20 + D-22 + D-23, modify `accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex` as follows:

    Alias retrofit (line ~7): change `alias AccrueAdmin.Components.{AppShell, Breadcrumbs, KpiCard}` to `alias AccrueAdmin.Components.{AppShell, Breadcrumbs, FunnelChart, KpiCard}`.

    `mount/3` (lines ~9–18): keep the existing `admin = Map.get(session, "accrue_admin", %{})` and `stats = Dunning.recovered_vs_lost_mrr()`. Add: `funnel = Dunning.funnel()` (no opts in P144 per Deferred Ideas — Phase 145 owns `:since`/`:until` threading). Add: `currency = Accrue.Config.get!(:default_currency)` (per D-19 and RESEARCH.md Pitfall #4 — use `Config.get!/1`, NEVER `Application.compile_env/2`). Add: `locale = Accrue.Config.default_locale()`. Add: `recovered_str = Accrue.Invoices.Render.format_money(stats.recovered_cents, currency, locale)`. Add: `exhausted_str = Accrue.Invoices.Render.format_money(stats.lost_cents, currency, locale)` (NOTE: the source-of-truth shape `%{recovered_cents, lost_cents}` is unchanged in P144 per D-20; `lost_cents` stays as the key — only the UI label changes). Pipe all four new assigns through the socket: `|> assign(:funnel, funnel) |> assign(:recovered_str, recovered_str) |> assign(:exhausted_str, exhausted_str)`.

    `render/1` ax-kpi-grid block (lines ~38–56): replace the existing two `<KpiCard.kpi_card>` calls. First card: `<KpiCard.kpi_card label="Recovered MRR" value={@recovered_str} delta="Amount saved by successful Dunning" delta_tone="moss"><:meta>Money Saved</:meta></KpiCard.kpi_card>`. Second card (DAN-23 rename): `<KpiCard.kpi_card label="Exhausted MRR" value={@exhausted_str} delta="Annualized MRR snapshot at the exhaustion event — e.g., a $120/yr plan contributes $10/mo to Exhausted MRR." delta_tone="amber"><:meta>Churned Revenue</:meta></KpiCard.kpi_card>`. The `delta` copy aligns with FunnelChart's Exhausted-stage tooltip per D-23. Do NOT nest `<MoneyFormatter>` inside `<KpiCard>` — KpiCard's `:value` attr is `:string, required: true` (per RESEARCH.md A6).

    Per D-18, IMMEDIATELY after the closing `</section>` of `ax-kpi-grid`, append the FunnelChart element: `<FunnelChart.funnel_chart entered={@funnel.entered} recovered={@funnel.recovered} exhausted={@funnel.exhausted} active={@funnel.active} />`.

    Delete the entire `format_minor/1` private helper (current lines 76–81 — both clauses: integer and fallback). Do NOT keep it as a "for backward compat" stub — it has no other callers and the bug is the rename target.

    Do NOT add `?window=…` URL handling or `handle_params/3` here — Phase 145 owns DAN-10. The funnel call uses no opts.
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex` contains `FunnelChart` in its alias line
    - File contains `funnel = Dunning.funnel()` inside `mount/3`
    - File contains `Accrue.Config.get!(:default_currency)` AND `Accrue.Config.default_locale()`
    - File contains `Accrue.Invoices.Render.format_money(` (at least twice — once per KPI card value)
    - File contains the literal string `"Exhausted MRR"` AND does NOT contain `"Lost MRR"` (grep)
    - File contains `<FunnelChart.funnel_chart` element with all four attrs (entered/recovered/exhausted/active)
    - File does NOT contain `:erlang.float_to_binary` (the USD-only bug helper is deleted) AND does NOT contain `defp format_minor`
    - File does NOT contain `Application.compile_env` (RESEARCH.md Pitfall #4)
    - `cd accrue_admin && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>RecoveryLive wires funnel + MoneyFormatter swap + label rename + FunnelChart slot; format_minor helper deleted; no compile warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JPY regression, funnel render, and Exhausted-MRR copy tests</name>
  <files>accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs</files>
  <read_first>
    - accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs (lines 1–66 — full file; existing setup + auth-adapter put_env/on_exit pattern at :22–32; existing render test at :48–66)
    - accrue/test/accrue/config_test.exs (lines 125–140 — the sole `Application.put_env(:accrue, :default_currency, :…)` precedent in the repo at :132–138)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-CONTEXT.md (D-21, D-23)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-PATTERNS.md (§"`accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs`" — quotes existing test + JPY describe block skeleton)
    - .planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-RESEARCH.md (§"Code Examples §8" — Example 8 JPY regression test full skeleton)
  </read_first>
  <behavior>
    - Test (existing rename): the original `"renders recovery dashboard with MRR totals"` test asserts `html =~ "Exhausted MRR"` (was "Lost MRR"); the literal `html =~ "$50.00"` assertion is preserved (default currency is still `:usd` for this test).
    - Test (funnel render): after seeding cycled-dunning events with distinct `campaign_anchor` values, the rendered HTML contains funnel structural strings — `"Recovery Funnel"`, `"currently in dunning"`, and the bar tooltip content (e.g., `"Entered:"`).
    - Test (JPY regression — D-21): with `Application.put_env(:accrue, :default_currency, :jpy)` and on_exit cleanup, after seeding a `dunning.recovered` event with `currency: "jpy"` and `mrr_value_cents: 5000`, the rendered HTML does NOT contain `"$50.00"` AND contains `"¥"` OR `"￥"` OR `"JPY"` (CLDR may render any locale-appropriate symbol — accept all three per D-21 latitude).
    - Test (Exhausted-MRR copy — DAN-23 / ROADMAP SC#5): rendered HTML contains the yearly-plan worked example substring (e.g., `"$120/yr"` AND `"$10/mo"`) somewhere in the response (delta string OR FunnelChart tooltip).
  </behavior>
  <action>
    Per D-21 + D-23 + ROADMAP SC#4 + ROADMAP SC#5, modify `accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs`:

    Update the existing test at :48–66 (`"renders recovery dashboard with MRR totals"`): change `assert html =~ "Lost MRR"` to `assert html =~ "Exhausted MRR"` AND add `refute html =~ "Lost MRR"` (DAN-23 rename enforcement; ROADMAP SC#5). Keep the existing `$50.00` / `$20.00` assertions (default currency is still `:usd` for this test).

    Add a new test `"renders funnel chart below KPI grid (DAN-09)"` (or a sibling `describe "funnel rendering (DAN-09)"`): seed (in addition to the existing recovered/exhausted events from the file-level setup) a cycled-dunning fixture for the same `subject_id` via `Events.record/1` with distinct `campaign_anchor` ISO strings (e.g., `DateTime.to_iso8601(DateTime.utc_now())` and `DateTime.to_iso8601(DateTime.add(DateTime.utc_now(), -1, :day))`); mount `/billing/analytics/recovery`; assert `html =~ "Recovery Funnel"` AND `html =~ "currently in dunning"`. ROADMAP SC#1 ("Operator sees 3-stage funnel below KPI cards"). The cycled-fixture also exercises ROADMAP SC#2 ("subscription cycling 3 times shows entered:3, recovered:1, exhausted:1, active:1") at the rendering boundary; ground-truth math is enforced by Plan 01's unit tests.

    Add a new `describe "JPY rendering (DAN-13)"` block per D-21 + RESEARCH.md Example 8. Setup: capture `prior = Application.get_env(:accrue, :default_currency)`; `Application.put_env(:accrue, :default_currency, :jpy)`; `on_exit(fn -> if is_nil(prior), do: Application.delete_env(:accrue, :default_currency), else: Application.put_env(:accrue, :default_currency, prior) end)`; seed `Events.record(%{type: "dunning.recovered", subject_type: "Subscription", subject_id: "sub_jpy", data: %{mrr_value_cents: 5000, currency: "jpy"}})`. Test: init test session with admin_token, live `/billing/analytics/recovery`, `refute html =~ "$50.00"`, `assert html =~ "¥" or html =~ "￥" or html =~ "JPY"` (encoded as Elixir: `assert html =~ "¥" or html =~ "￥" or html =~ "JPY"`).

    Add a new test `"Exhausted MRR card carries yearly-plan worked example (ROADMAP SC#5)"`: mount `/billing/analytics/recovery`; assert `html =~ "$120/yr"` AND `html =~ "$10/mo"` somewhere (the delta string on the Exhausted KPI card AND/OR the FunnelChart Exhausted-bar tooltip).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/analytics/recovery_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `accrue_admin/test/accrue_admin/live/analytics/recovery_live_test.exs` contains `describe "JPY rendering (DAN-13)"`
    - File contains `Application.put_env(:accrue, :default_currency, :jpy)` AND an `on_exit` block restoring prior value (delete-if-nil branch)
    - File contains `refute html =~ "Lost MRR"`
    - File contains `assert html =~ "Recovery Funnel"`
    - File contains `assert html =~ "$120/yr"` (ROADMAP SC#5 worked-example assertion)
    - `cd accrue_admin && mix test test/accrue_admin/live/analytics/recovery_live_test.exs` exits 0
    - All pre-existing tests in the file still pass (the rename update is the only intentional behavior change)
  </acceptance_criteria>
  <done>JPY regression + funnel-render + Exhausted-MRR copy tests all green; existing rename-updated test green; full file suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser session → /billing/analytics/recovery | Admin auth gate inherited from `live_session :accrue_admin` in router.ex:52–86 — no new auth surface |
| Accrue.Config runtime read → render | `:default_currency` and `:default_locale` are HOST-OWNED config values read at runtime; no compile-time leakage |
| `format_money/3` rendering → DOM | CLDR via ex_money/ex_cldr produces escaped strings; HEEx escapes interpolations by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-08 | Information Disclosure (non-admin access to recovery dashboard) | `/billing/analytics/recovery` route | accept | Inherited from Phase 143's `live_session :accrue_admin` + `on_mount {AccrueAdmin.AuthHook, :ensure_admin}` (router.ex). Plan 04 adds no new routes and no new auth surface. |
| T-144-09 | Configuration leak (currency baked into release artifact) | `Accrue.Config.get!(:default_currency)` vs `Application.compile_env/2` | mitigate | Per RESEARCH.md Pitfall #4 + CLAUDE.md "Config Boundaries", use `Accrue.Config.get!/1` (runtime). Acceptance criteria assert `Application.compile_env` does NOT appear in the file. |
| T-144-10 | XSS via currency/locale interpolation | `format_money/3` output rendered in KPI card value | accept | CLDR output is a known-shape string (digits + currency symbol/code); HEEx escapes the `<%= @recovered_str %>` interpolation; `format_money/3` never produces raw HTML (double-fallback to `"N currency"` raw string is also a literal string). No `raw/1` call introduced. |

**Threats considered, none applicable:** Tampering (no state mutations from this LiveView — read-only dashboard), Repudiation (out-of-scope for read path), Spoofing (admin auth + signed session inherited), Denial of Service (funnel/1 single-query cost is bounded by ledger size; Phase 148 docs the ~100k events expression-index threshold — A8 in RESEARCH.md).
</threat_model>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors` exits 0.
- `cd accrue_admin && mix test test/accrue_admin/live/analytics/recovery_live_test.exs` exits 0.
- Full admin suite: `cd accrue_admin && mix test` exits 0 (no broader regression).
- Manual smoke (optional): `cd accrue_admin && mix phx.server` → visit `/billing/analytics/recovery` → see funnel below KPI grid.
- `grep -c 'Lost MRR' accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex` returns 0 (rename complete).
- `grep -c ':erlang.float_to_binary' accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex` returns 0 (USD-only bug deleted).
</verification>

<success_criteria>
- [ ] `RecoveryLive.mount/3` calls `Dunning.funnel()` and assigns `:funnel` (DAN-09)
- [ ] `<FunnelChart.funnel_chart>` rendered directly below `<section class="ax-kpi-grid">` (DAN-09 + ROADMAP SC#1)
- [ ] `RecoveryLive.format_minor/1` deleted; KPI card values formatted via `Accrue.Invoices.Render.format_money/3` driven by `Accrue.Config.get!(:default_currency)` + `Accrue.Config.default_locale()` (DAN-13 + ROADMAP SC#4)
- [ ] "Lost MRR" → "Exhausted MRR" rename complete with yearly-plan worked-example delta copy (ROADMAP SC#5 + D-23)
- [ ] JPY regression test asserts no `$` and presence of `¥` / `￥` / `JPY` (DAN-13 + D-21)
- [ ] No `Application.compile_env(:accrue, :default_currency)` anywhere in `recovery_live.ex` (RESEARCH.md Pitfall #4)
- [ ] All tests in `recovery_live_test.exs` pass (no regressions on Phase 143 tests)
</success_criteria>

<output>
Create `.planning/phases/144-funnel-query-viz-campaign-anchor-retrofit-money-formatter-po/144-04-SUMMARY.md` when done. Summary must include: confirmation of all five edits (alias add, mount/3 wiring, FunnelChart slot, KpiCard rename, format_minor deletion), four new test outcomes (rename update + funnel render + JPY regression + worked-example copy), and the grep counts demonstrating the rename + deletion are complete.
</output>

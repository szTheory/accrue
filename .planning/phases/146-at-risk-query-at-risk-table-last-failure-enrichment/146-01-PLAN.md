---
phase: 146
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/lib/accrue/billing/query.ex
  - accrue/test/accrue/webhook/dunning_campaign_start_test.exs
  - accrue/test/accrue/billing/query_test.exs
autonomous: true
requirements:
  - DAN-03
  - DAN-04
must_haves:
  truths:
    - "emit_campaign_started/2 accepts canonical as a second argument and stores invoice_id in event data"
    - "Pre-existing tests for emit_campaign_started assert event.data[invoice_id] is present"
    - "in_active_dunning_campaign/1 is a composable query predicate returning subscriptions with non-nil dunning_campaign_started_at"
    - "in_active_dunning_campaign/1 composes correctly when piped after an existing query"
  artifacts:
    - path: "accrue/lib/accrue/billing/query.ex"
      provides: "in_active_dunning_campaign/1 query composer"
      contains: "def in_active_dunning_campaign"
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "emit_campaign_started/2 with invoice_id in data map"
      contains: "invoice_id: get(canonical, :id)"
  key_links:
    - from: "default_handler.ex:maybe_start_dunning_campaign/2"
      to: "default_handler.ex:emit_campaign_started/2"
      via: "emit_campaign_started(sub, canonical)"
      pattern: "emit_campaign_started\\(sub, canonical\\)"
    - from: "dunning.campaign_started event data"
      to: "accrue_invoices.processor_id"
      via: "invoice_id key holds Stripe Invoice ID string"
      pattern: "invoice_id: get\\(canonical, :id\\)"
---

<objective>
Lay the foundation for Phase 146's failure-reason enrichment: refactor `emit_campaign_started/1` to `/2`, injecting the Stripe Invoice ID into the `dunning.campaign_started` event data map (D-04), and add the `in_active_dunning_campaign/1` query composer to `Accrue.Billing.Query` (D-11).

Purpose: Plan 02 joins through `accrue_invoices.processor_id` to resolve failure reasons — that bridge only works if `dunning.campaign_started` events carry the Stripe Invoice ID. The query composer is the schema-side building block consumed inline by `at_risk_subscriptions/1` in Plan 02.

Output: Modified `default_handler.ex` (emit_campaign_started/2), modified `query.ex` (in_active_dunning_campaign/1), extended test coverage in both `dunning_campaign_start_test.exs` and `query_test.exs`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase. -->

From accrue/lib/accrue/billing/query.ex (structural template — paused/1 pattern):
```elixir
@doc "Subscriptions that are paused..."
@spec paused(Ecto.Queryable.t()) :: Ecto.Query.t()
def paused(query \\ Subscription) do
  from(s in query, where: not is_nil(s.pause_collection) or s.status == :paused)
end
```
Module-level `import Ecto.Query` already covers `from/2`, `where/3`, `is_nil/1`.
Alias `Accrue.Billing.Subscription` already present.
New function goes after `dunning_sweep_candidates/2`, before the module `end`.

From accrue/lib/accrue/webhook/default_handler.ex (current call site, lines 1234–1238):
```elixir
1 ->
  emit_campaign_started(sub)           # line 1236 — becomes emit_campaign_started(sub, canonical)
  opts = [invoice_id: get(canonical, :id)]
  Accrue.Config.dunning_engine().start_campaign(sub, now_usec, opts)
```

Current emit_campaign_started/1 body (lines 1256–1272):
```elixir
defp emit_campaign_started(%Subscription{} = sub) do
  step_count = length(Accrue.Config.dunning_campaign_steps())
  Events.record(%{
    type: "dunning.campaign_started",
    subject_type: "Subscription",
    subject_id: sub.id,
    data: %{step_count: step_count}
  })
  Accrue.Telemetry.Ops.emit(:dunning_campaign_started, %{count: 1}, %{
    subscription_id: sub.id,
    step_count: step_count
  })
  :ok
end
```
`get/2` macro (from `import` already active in `reduce_invoice/4` scope) resolves `get(canonical, :id)` to the Stripe Invoice ID string e.g. `"in_xxxx"`.

From accrue/test/accrue/webhook/dunning_campaign_start_test.exs (assertion to extend, lines 228–231):
```elixir
events = ledger_events("dunning.campaign_started", sub.id)
assert [event] = events
assert event.data["step_count"] == step_count
# NEW assertion to add on the next line:
assert event.data["invoice_id"] == "in_fake_obs_start1"
```
`"in_fake_obs_start1"` is the invoice ID passed to `fire_payment_failed/2` on line 224 of the same test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: emit_campaign_started/1 → /2 with invoice_id enrichment (D-04)</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/webhook/dunning_campaign_start_test.exs</files>
  <read_first>
    - accrue/lib/accrue/webhook/default_handler.ex lines 1224–1272 (maybe_start_dunning_campaign/2 + emit_campaign_started/1 full body)
    - accrue/test/accrue/webhook/dunning_campaign_start_test.exs lines 216–250 (campaign_started observability describe block — the test to extend)
    - .planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-CONTEXT.md D-04 decision
  </read_first>
  <behavior>
    - Test: `event.data["invoice_id"]` equals the Stripe Invoice ID `"in_fake_obs_start1"` passed to `fire_payment_failed/2`
    - Test: `event.data["step_count"]` is still present (not accidentally dropped)
    - Test: telemetry metadata does NOT include `invoice_id` (no PII leak to telemetry — only the ledger event carries it)
  </behavior>
  <action>
    In accrue/lib/accrue/webhook/default_handler.ex:

    1. Change the call site on line 1236 from `emit_campaign_started(sub)` to `emit_campaign_started(sub, canonical)`. The variable `canonical` is already in scope at this point — it is the parameter of `maybe_start_dunning_campaign/2`.

    2. Change the function signature on line 1256 from `defp emit_campaign_started(%Subscription{} = sub)` to `defp emit_campaign_started(%Subscription{} = sub, canonical)`.

    3. In the `Events.record/1` call inside `emit_campaign_started`, change the `data` map from `%{step_count: step_count}` to `%{step_count: step_count, invoice_id: get(canonical, :id)}`. The `get/2` macro is already imported in the surrounding function scope — do NOT add a new import.

    4. Do NOT add `invoice_id` to the `Accrue.Telemetry.Ops.emit` metadata map — telemetry carries only `subscription_id` and `step_count` per D-04 / T-129-01 (no PII or Stripe IDs in telemetry).

    In accrue/test/accrue/webhook/dunning_campaign_start_test.exs, in the `"campaign_started observability (DUN-08)"` describe block, in the test `"the first nil→past_due edge records a ledger event AND fires telemetry"`:

    5. After the existing assertion `assert event.data["step_count"] == step_count`, add `assert event.data["invoice_id"] == "in_fake_obs_start1"`. The invoice ID `"in_fake_obs_start1"` is the value passed to `fire_payment_failed("in_fake_obs_start1", sub_id)` on line 224 — it is the Stripe Invoice ID that `get(canonical, :id)` returns for that test fixture.

    6. Also add a refute: `refute Map.has_key?(meta, :invoice_id)` alongside the existing `refute Map.has_key?(meta, :source)` in the telemetry assertion block (line 238).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/dunning_campaign_start_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `event.data["invoice_id"] == "in_fake_obs_start1"` passes in the campaign_started observability test
    - `event.data["step_count"]` assertion still passes (not dropped)
    - `refute Map.has_key?(meta, :invoice_id)` passes (telemetry metadata unchanged)
    - No compile error from `emit_campaign_started/1` — only `/2` variant exists now
    - `mix test test/accrue/webhook/dunning_campaign_start_test.exs --seed 0` exits 0
  </acceptance_criteria>
  <done>emit_campaign_started/2 stores invoice_id in dunning.campaign_started event data; telemetry unchanged; all dunning_campaign_start_test.exs tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: in_active_dunning_campaign/1 query composer (D-11)</name>
  <files>accrue/lib/accrue/billing/query.ex, accrue/test/accrue/billing/query_test.exs</files>
  <read_first>
    - accrue/lib/accrue/billing/query.ex lines 122–145 (paused/1 structural template + dunning_sweep_candidates/2 for placement reference)
    - accrue/test/accrue/billing/query_test.exs lines 1–15 (module header) and lines 66–130 (existing predicate tests for structural reference)
    - .planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-CONTEXT.md D-11 decision
  </read_first>
  <behavior>
    - Test: `in_active_dunning_campaign/1` returns subscriptions whose `dunning_campaign_started_at` is non-nil
    - Test: `in_active_dunning_campaign/1` does NOT return subscriptions whose `dunning_campaign_started_at` is nil
    - Test: composability — when piped after `from(s in Subscription, where: s.status == :past_due)`, only subscriptions that are both `:past_due` AND have non-nil `dunning_campaign_started_at` are returned
  </behavior>
  <action>
    In accrue/lib/accrue/billing/query.ex:

    1. Add the following function immediately after `dunning_sweep_candidates/2` (the function ending around line 145), before the module `end`:

    Function name: `in_active_dunning_campaign/1`
    Default arg: `query \\ Subscription`
    Spec: `@spec in_active_dunning_campaign(Ecto.Queryable.t()) :: Ecto.Query.t()`
    Doc string: `"Subscriptions currently in an active dunning campaign (anchor column is non-nil). Composable — pipe after any Subscription query to add the campaign-active predicate."`
    Body: `from(s in query, where: not is_nil(s.dunning_campaign_started_at))`

    No new imports or aliases needed — module-level `import Ecto.Query` already covers `from/2`, `where/3`, `not/1`, and `is_nil/1`.

    In accrue/test/accrue/billing/query_test.exs:

    2. Add a new test in the existing test module. Read the setup block at the top of the file to understand what fixtures are already available. Add a fixture subscription with `dunning_campaign_started_at` set to `Accrue.Clock.utc_now()` and status `:past_due` (to satisfy not-null constraints visible in the existing setup) in the setup block, OR insert directly in the test if the existing setup fixture can be extended without breaking existing tests. Use the same pattern as the other fixture insertions in the setup block.

    3. Write two test cases under a `describe "in_active_dunning_campaign/1"` block:
       - First test: assert that `Query.in_active_dunning_campaign() |> Repo.all()` returns a list that includes the fixture subscription (assert `Enum.any?(result, &(&1.id == dunning_sub.id))`), AND that every element in the result has a non-nil `dunning_campaign_started_at`.
       - Second test (composition): `from(s in Subscription, where: s.status == :past_due) |> Query.in_active_dunning_campaign() |> Repo.all()` returns only subscriptions that are both `:past_due` AND have non-nil `dunning_campaign_started_at`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/billing/query_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `in_active_dunning_campaign/1` is defined in `accrue/lib/accrue/billing/query.ex` with the exact spec `@spec in_active_dunning_campaign(Ecto.Queryable.t()) :: Ecto.Query.t()`
    - `mix test test/accrue/billing/query_test.exs --seed 0` exits 0 with no failures
    - The two `describe "in_active_dunning_campaign/1"` tests both pass
    - Existing predicate tests are not broken (all tests in the file pass)
  </acceptance_criteria>
  <done>in_active_dunning_campaign/1 added to query.ex with @spec and @doc; two tests pass confirming predicate and composability</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook handler → ledger | `emit_campaign_started/2` writes `invoice_id` (Stripe reference) into the jsonb `data` map of `accrue_events`; canonical is a fetched Stripe struct (validated upstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Tampering | `emit_campaign_started/2` data map | accept | `invoice_id` is `get(canonical, :id)` — a Stripe-fetched ID, not user input; no injection vector at write time |
| T-146-02 | Information Disclosure | Telemetry metadata | mitigate | Do NOT emit `invoice_id` in `Accrue.Telemetry.Ops.emit` metadata — Stripe IDs are reference data but maintaining the existing narrow-metadata posture is correct; assertion added to test |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; not applicable |
</threat_model>

<verification>
Full quick-run after both tasks:

```
cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/dunning_campaign_start_test.exs test/accrue/billing/query_test.exs --seed 0
```

Both test files must exit 0 with no failures.
</verification>

<success_criteria>
1. `default_handler.ex` calls `emit_campaign_started(sub, canonical)` (not `emit_campaign_started(sub)`)
2. `default_handler.ex` `emit_campaign_started/2` data map contains `invoice_id: get(canonical, :id)` alongside `step_count`
3. `query.ex` exports `in_active_dunning_campaign/1` with correct `@spec`
4. `dunning_campaign_start_test.exs` asserts `event.data["invoice_id"] == "in_fake_obs_start1"` and the test passes
5. `query_test.exs` has `describe "in_active_dunning_campaign/1"` with two passing tests
6. `mix test test/accrue/webhook/dunning_campaign_start_test.exs test/accrue/billing/query_test.exs --seed 0` exits 0
</success_criteria>

<output>
Create `/Users/jon/projects/accrue/.planning/phases/146-at-risk-query-at-risk-table-last-failure-enrichment/146-01-SUMMARY.md` when done.
</output>

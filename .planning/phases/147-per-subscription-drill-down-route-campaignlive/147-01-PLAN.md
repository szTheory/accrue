---
phase: "147"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/analytics/dunning.ex
  - accrue/test/accrue/analytics/dunning_test.exs
autonomous: true
requirements:
  - DAN-05
  - DAN-12

must_haves:
  truths:
    - "Dunning.campaign_timeline/2 returns only dunning.* typed events for a subscription, in chronological order (asc inserted_at, asc id)"
    - "Dunning.campaign_timeline_grouped/2 groups a flat event list into [{anchor, events}] arcs — each arc boundary at a dunning.campaign_started event"
    - "Dunning.campaign_timeline_grouped/2 returns [] for an unknown subscription_id"
    - "Two-campaign subscription produces two arc tuples; the first arc contains a dunning.recovered event; the second arc has no terminal event"
    - "Legacy events before the first campaign_started form a {nil, events} prefix arc"
    - "Dunning.invoices_for_campaign/2 returns a map keyed by Stripe processor_id, with status, amount_due_cents, card_last4, card_brand per invoice"
    - "invoices_for_campaign/2 returns %{} for a subscription with no invoices"
    - "invoice with no default payment method returns nil card_last4 and nil card_brand (left_join path)"
  artifacts:
    - path: "accrue/lib/accrue/analytics/dunning.ex"
      provides: "campaign_timeline/2, campaign_timeline_grouped/2, invoices_for_campaign/2 public functions"
      exports: ["campaign_timeline", "campaign_timeline_grouped", "invoices_for_campaign"]
    - path: "accrue/test/accrue/analytics/dunning_test.exs"
      provides: "describe blocks for all three new functions"
      contains: "campaign_timeline"
  key_links:
    - from: "Accrue.Analytics.Dunning.campaign_timeline/2"
      to: "Accrue.Events.timeline_for/3"
      via: "direct delegation (in-memory Enum.filter on dunning.* types)"
      pattern: "Events\\.timeline_for"
    - from: "Accrue.Analytics.Dunning.invoices_for_campaign/2"
      to: "accrue_invoices.processor_id"
      via: "Ecto join on processor_id (NOT accrue_invoices.id)"
      pattern: "processor_id"
---

<objective>
Implements the three new public analytics functions in `Accrue.Analytics.Dunning` that power the Phase 147 drill-down, with TDD: write failing tests first, then implement to green.

Purpose: `campaign_timeline/2` and `campaign_timeline_grouped/2` expose the per-subscription dunning arc list (DAN-05). `invoices_for_campaign/2` provides the invoice context map (DAN-12 data layer). Both are public API that Phase 148 will freeze in docs.

Output: Three new public functions in dunning.ex, new describe blocks in dunning_test.exs — all tests green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Test stubs + campaign_timeline/2 and campaign_timeline_grouped/2</name>
  <files>accrue/test/accrue/analytics/dunning_test.exs, accrue/lib/accrue/analytics/dunning.ex</files>

  <read_first>
    - accrue/lib/accrue/analytics/dunning.ex — full file: existing module-level aliases (Billing.{Customer, Subscription}, Events.Event, Repo, import Ecto.Query), @dunning_lifecycle_types constant at line 20, existing describe/test patterns to match
    - accrue/lib/accrue/events.ex lines 261-275 — timeline_for/3 signature and upcast_to_current/1 private scope confirmation
    - accrue/test/accrue/analytics/dunning_test.exs — existing test module header, RepoCase setup, Repo.insert! fixture pattern for Event structs
    - .planning/phases/147-per-subscription-drill-down-route-campaignlive/147-PATTERNS.md Pattern 1 and Pattern 2 — exact delegate-and-filter and group_into_arcs implementations
  </read_first>

  <behavior>
    RED phase (write these tests first, confirm they fail before implementing):

    describe "campaign_timeline/2":
    - Test 1: returns [] for subscription with no events — assert [] = Dunning.campaign_timeline(Ecto.UUID.generate())
    - Test 2: returns only dunning.* events for subject — insert one "subscription.updated" event and one "dunning.campaign_started" event for same subscription_id; assert length == 1 and the returned event has type "dunning.campaign_started"
    - Test 3: returns events in chronological order (asc inserted_at) — insert "dunning.step_sent" with an earlier inserted_at, then "dunning.campaign_started" with a later inserted_at; assert first event in result has type "dunning.step_sent"

    describe "campaign_timeline_grouped/2":
    - Test 1: returns [] for subscription with no events — assert [] = Dunning.campaign_timeline_grouped(Ecto.UUID.generate())
    - Test 2: groups two campaigns into two arcs — insert: campaign_started (anchor "A"), step_sent, recovered, then campaign_started (anchor "B"), step_sent; assert length(arcs) == 2; first arc has dunning.recovered; second arc has no terminal event
    - Test 3: legacy events before first campaign_started form {nil, events} arc — insert a step_sent with no preceding campaign_started; assert result == [{nil, [event]}]

    GREEN phase: implement functions after tests are red.

    campaign_timeline/2 implementation:
    - Delegate to Accrue.Events.timeline_for("Subscription", subscription_id, opts) then Enum.filter(&String.starts_with?(&1.type, "dunning."))
    - DO NOT write a raw Ecto query in dunning.ex — upcast_to_current/1 is private to Accrue.Events; bypassing timeline_for/3 breaks legacy event shape contracts
    - Function head: def campaign_timeline(subscription_id, opts \\ []) when is_binary(subscription_id) and is_list(opts)
    - @spec campaign_timeline(String.t(), keyword()) :: [Event.t()]
    - @since "1.4.0" in moduledoc

    campaign_timeline_grouped/2 implementation:
    - Calls campaign_timeline/2 then group_into_arcs/1 (private helper)
    - @spec campaign_timeline_grouped(String.t(), keyword()) :: [{String.t() | nil, [Event.t()]}]
    - @since "1.4.0" in moduledoc
    - group_into_arcs([]) returns []
    - group_into_arcs(events): plain Enum.reduce over events — when event.type == "dunning.campaign_started", append {event.data["campaign_anchor"], [event]} to acc; otherwise append event to last arc's events list (or create {nil, [event]} if acc is empty)
    - Use List.replace_at(acc, -1, {anchor, arc_events ++ [event]}) to append to last arc — avoids Enum.chunk_by alternating-chunks pitfall
    - Return type is [{anchor :: String.t() | nil, events :: [Event.t()]}]
  </behavior>

  <action>
  In accrue/test/accrue/analytics/dunning_test.exs: add two new describe blocks after the existing "funnel/1" describe block. Use the same module header (Accrue.RepoCase, async: false, alias Accrue.Analytics.Dunning). Fixture pattern for events: Accrue.Repo.insert!(%Accrue.Events.Event{type: ..., subject_type: "Subscription", subject_id: subscription_id, actor_type: "system", schema_version: 1, data: %{}}). To control inserted_at ordering in tests, use %Accrue.Events.Event{..., inserted_at: ~U[2026-01-01 10:00:00Z]} etc.

  In accrue/lib/accrue/analytics/dunning.ex: append campaign_timeline/2 and campaign_timeline_grouped/2 after the at_risk_subscriptions/1 function. No new aliases needed — Accrue.Events.Event is already aliased; Accrue.Events is NOT aliased (call as full module name Accrue.Events.timeline_for). group_into_arcs/1 is a private defp. Do NOT redefine @dunning_lifecycle_types (already at line 20).
  </action>

  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/analytics/dunning_test.exs 2>&1 | tail -20</automated>
  </verify>

  <done>
    - dunning_test.exs contains describe "campaign_timeline/2" with 3 tests and describe "campaign_timeline_grouped/2" with 3 tests
    - dunning.ex contains def campaign_timeline(subscription_id, opts \\ []) and def campaign_timeline_grouped(subscription_id, opts \\ []) and defp group_into_arcs/1
    - dunning.ex campaign_timeline/2 calls Accrue.Events.timeline_for("Subscription", subscription_id, opts) (not a raw Ecto query)
    - All dunning_test.exs tests pass (mix test exits 0 for that file)
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: invoices_for_campaign/2 — tests and implementation</name>
  <files>accrue/test/accrue/analytics/dunning_test.exs, accrue/lib/accrue/analytics/dunning.ex</files>

  <read_first>
    - accrue/lib/accrue/analytics/dunning.ex — module-level aliases (line 12: Billing.{Customer, Subscription}); import Ecto.Query only: [from: 2, subquery: 1, where: 3] — need to add Invoice and PaymentMethod to the Billing alias and add left_join to the import list
    - accrue/lib/accrue/billing/invoice.ex — schema fields: amount_due_minor (:integer), processor_id (:string), status (Ecto.Enum), subscription_id (:binary_id), customer_id (:binary_id)
    - accrue/lib/accrue/billing/payment_method.ex — confirm card_brand and card_last4 field names
    - accrue/lib/accrue/billing/customer.ex — confirm default_payment_method_id FK field name
    - accrue/lib/accrue/analytics/dunning.ex lines 183-248 — at_risk_subscriptions/1 join shape as structural analog for compound join pattern with fragment
    - .planning/phases/147-per-subscription-drill-down-route-campaignlive/147-PATTERNS.md Pattern 3 — exact invoices_for_campaign/2 Ecto query with type(^subscription_id, :binary_id) cast
  </read_first>

  <behavior>
    RED phase (write these tests first, confirm they fail before implementing):

    describe "invoices_for_campaign/2":
    - Test 1: returns %{} for subscription with no invoices — assert %{} = Dunning.invoices_for_campaign(Ecto.UUID.generate())
    - Test 2: returns map keyed by Stripe processor_id — create Customer, Subscription, Invoice (with processor_id: "in_test_abc", amount_due_minor: 4999, status: :open), PaymentMethod (card_last4: "4242", card_brand: "visa"); link Customer.default_payment_method_id = pm.id; assert result = Dunning.invoices_for_campaign(subscription.id); assert Map.has_key?(result, "in_test_abc"); entry = result["in_test_abc"]; assert entry.status == :open; assert entry.amount_due_cents == 4999; assert entry.card_last4 == "4242"; assert entry.card_brand == "visa"
    - Test 3: invoice with no default payment method returns nil card fields — Customer with default_payment_method_id: nil; left_join produces nil pm; assert result["in_test_xyz"].card_last4 == nil; assert result["in_test_xyz"].card_brand == nil
    - Test 4: excludes invoices with nil processor_id — insert Invoice with processor_id: nil for same subscription; assert map has only the keyed entry (the one with processor_id set), NOT a nil key

    GREEN phase: implement invoices_for_campaign/2 after tests are red.

    invoices_for_campaign/2 implementation:
    - Function head: def invoices_for_campaign(subscription_id, opts \\ []) when is_binary(subscription_id) and is_list(opts)
    - @spec invoices_for_campaign(String.t(), keyword()) :: %{String.t() => map()}
    - @since "1.4.0" in moduledoc
    - Ecto query: from(i in Invoice, join: c in Customer, on: c.id == i.customer_id, left_join: pm in PaymentMethod, on: pm.id == c.default_payment_method_id, where: i.subscription_id == type(^subscription_id, :binary_id), where: not is_nil(i.processor_id), select: %{processor_id: i.processor_id, status: i.status, amount_due_cents: i.amount_due_minor, card_last4: pm.card_last4, card_brand: pm.card_brand})
    - |> Repo.all() |> Map.new(fn row -> {row.processor_id, Map.delete(row, :processor_id)} end)
    - Key by processor_id (Stripe invoice ID, e.g. "in_xxxx") — NOT by accrue_invoices.id (UUID). This is verified by dunning.ex line 234 where cs.data->>'invoice_id' = i.processor_id.
    - amount_due_minor field renamed to amount_due_cents in select map to match return contract spec
    - Add Invoice and PaymentMethod to the existing Billing alias tuple at line 12: alias Accrue.Billing.{Customer, Invoice, PaymentMethod, Subscription}
    - Add left_join: 3 to the import Ecto.Query only: list if not already present (check line 10)
  </behavior>

  <action>
  In accrue/test/accrue/analytics/dunning_test.exs: add describe "invoices_for_campaign/2" block. Fixtures need Customer, Subscription, Invoice, and PaymentMethod records. Use the project's existing factory or direct Repo.insert! with struct literals. Invoice must have processor_id set (a Stripe-format string like "in_test_abc") — the join is on i.processor_id, not i.id. Test 4 verifies the where: not is_nil(i.processor_id) guard excludes nil-processor_id invoices.

  In accrue/lib/accrue/analytics/dunning.ex: update the alias Accrue.Billing.{} tuple at line 12 to add Invoice and PaymentMethod. Add invoices_for_campaign/2 after campaign_timeline_grouped/2. Check if left_join is already in the import only: list — if not, add it. Use type(^subscription_id, :binary_id) for the subscription_id cast (not Ecto.UUID.cast! which would raise on invalid input).
  </action>

  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/analytics/dunning_test.exs 2>&1 | tail -20</automated>
  </verify>

  <done>
    - dunning_test.exs contains describe "invoices_for_campaign/2" with 4 tests
    - dunning.ex contains def invoices_for_campaign(subscription_id, opts \\ []) joining Invoice, Customer, PaymentMethod via Ecto
    - dunning.ex invoices_for_campaign/2 joins on i.processor_id (not i.id) and keys result map by processor_id
    - dunning.ex line-12 alias includes Invoice and PaymentMethod
    - All dunning_test.exs tests pass (mix test exits 0 for that file)
    - Full accrue suite green: cd /Users/jon/projects/accrue/accrue && mix test exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Dunning API | subscription_id is a binary string; Ecto parameterized queries prevent SQL injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-03 | Tampering | invoices_for_campaign/2, campaign_timeline/2 | mitigate | Both functions use Ecto parameterized queries (^subscription_id bound via type/2 or pipeline) — no raw SQL interpolation; malformed UUID produces empty result, not crash |
| T-147-04 | Information Disclosure | dunning.ex event data logging | accept | dunning.* event data contains only invoice_id (Stripe ID) and campaign_anchor (ISO timestamp) — no PII per CLAUDE.md; dunning.ex never logs event data fields |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — zero mix dep changes |
</threat_model>

<verification>
After both tasks:
- cd /Users/jon/projects/accrue/accrue && mix test test/accrue/analytics/dunning_test.exs — all new describes green
- cd /Users/jon/projects/accrue/accrue && mix test — full accrue suite green
- grep -n "def campaign_timeline\|def campaign_timeline_grouped\|def invoices_for_campaign" /Users/jon/projects/accrue/accrue/lib/accrue/analytics/dunning.ex — all three functions present
- grep "Events\.timeline_for" /Users/jon/projects/accrue/accrue/lib/accrue/analytics/dunning.ex — campaign_timeline/2 delegates to Events.timeline_for (not raw Ecto)
- grep "processor_id" /Users/jon/projects/accrue/accrue/lib/accrue/analytics/dunning.ex — invoices_for_campaign/2 joins and keys on processor_id
</verification>

<success_criteria>
- Three public functions defined in Accrue.Analytics.Dunning: campaign_timeline/2, campaign_timeline_grouped/2, invoices_for_campaign/2
- All functions have @spec and @since "1.4.0" tags
- campaign_timeline/2 delegates to Accrue.Events.timeline_for/3 (upcast_to_current/1 chain preserved)
- invoices_for_campaign/2 keys result by accrue_invoices.processor_id (Stripe ID), NOT accrue_invoices.id
- group_into_arcs/1 uses Enum.reduce (not raw Enum.chunk_by) to avoid alternating-chunks pitfall
- 10 new test cases in dunning_test.exs covering all three functions
- cd /Users/jon/projects/accrue/accrue && mix test exits 0
</success_criteria>

<output>
Create `.planning/phases/147-per-subscription-drill-down-route-campaignlive/147-01-SUMMARY.md` when done
</output>

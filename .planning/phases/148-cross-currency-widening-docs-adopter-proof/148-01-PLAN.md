---
phase: 148-cross-currency-widening-docs-adopter-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/analytics/dunning.ex
  - accrue/test/accrue/analytics/dunning_test.exs
autonomous: true
requirements:
  - DAN-06
  - DAN-07
must_haves:
  truths:
    - "recovered_vs_lost_mrr/1 returns lists of currency amounts instead of flattened totals"
    - "recovery_rate/1 returns the arithmetic rate of recovered/(recovered+exhausted)"
  artifacts:
    - path: accrue/lib/accrue/analytics/dunning.ex
      provides: "Widened API shapes and recovery rate calculation"
      contains: "def recovery_rate"
  key_links:
    - from: "Accrue.Analytics.Dunning.recovery_rate/1"
      to: "Accrue.Analytics.Dunning.funnel/1"
      via: "delegation"
      pattern: "funnel("
---

<objective>
Widen the cross-currency return shape of `recovered_vs_lost_mrr/1` (DAN-07) and implement the new `recovery_rate/1` API (DAN-06) before the 1.4.0 API freeze.

Purpose: Break the shape now so we don't violate semver later. Ensure single and multi-currency operators have a consistent query. Add recovery rate helper so adopters don't manually parse the funnel for simple UI elements.
Output: Modified `Accrue.Analytics.Dunning` with new APIs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-CONTEXT.md
@.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-DISCUSSION-LOG.md
@accrue/lib/accrue/analytics/dunning.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen recovered_vs_lost_mrr/1</name>
  <files>accrue/lib/accrue/analytics/dunning.ex, accrue/test/accrue/analytics/dunning_test.exs</files>
  <behavior>
    - Returns `%{recovered: [%{currency: "usd", cents: N}, ...], lost: [%{currency: "usd", cents: N}, ...]}`
    - Groups by `data->>'currency'`
    - Handles single-currency users correctly (returning 1 item per list)
  </behavior>
  <action>Modify the Ecto query in `recovered_vs_lost_mrr/1` to `group_by: [e.type, fragment("?->>'currency'", e.data)]` and select both type and currency alongside the safe-cast `mrr_value_cents` sum. Enum.reduce the result into the new `%{recovered: [...], lost: [...]}` map structure.</action>
  <verify>
    <automated>mix test accrue/test/accrue/analytics/dunning_test.exs</automated>
  </verify>
  <done>Query returns the widened multi-currency list format and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recovery_rate/1</name>
  <files>accrue/lib/accrue/analytics/dunning.ex, accrue/test/accrue/analytics/dunning_test.exs</files>
  <behavior>
    - Returns `%{rate: 0.0..1.0 | nil, recovered: N, total_concluded: N}`
    - Returns `rate: nil` if total_concluded is 0 (prevent divide by zero)
  </behavior>
  <action>Implement `recovery_rate(opts \\ [])` that delegates internally to `funnel(opts)`. Extract `recovered` and `exhausted` from the funnel result, calculate `total = rec + exh`, and compute the arithmetic rate.</action>
  <verify>
    <automated>mix test accrue/test/accrue/analytics/dunning_test.exs</automated>
  </verify>
  <done>Returns the correct mathematical rate and handles 0 totals properly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal logic | No external input processed; analytics uses internal DB state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Tampering | `recovered_vs_lost_mrr/1` | mitigate | Use `CASE WHEN jsonb_typeof` safe-casts for MRR cents to prevent malformed string insertions from crashing aggregation. |
</threat_model>

<verification>
Ensure both `recovered_vs_lost_mrr` and `recovery_rate` tests are passing and return shapes strictly follow the new contract.
</verification>

<success_criteria>
Public core API functions operate using the exact documented shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-01-SUMMARY.md`
</output>
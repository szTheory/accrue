---
phase: 148-cross-currency-widening-docs-adopter-proof
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - accrue/guides/analytics.md
  - accrue/lib/accrue/analytics/dunning.ex
  - accrue/mix.exs
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - DAN-14
  - DAN-15
must_haves:
  truths:
    - "Analytics guide is available in ExDoc."
    - "Module docs link to the guide and show version markers."
    - "verify_package_docs.sh asserts the guide exists."
  artifacts:
    - path: accrue/guides/analytics.md
      provides: "Public documentation of Analytics APIs"
      contains: "Showing data since"
    - path: accrue/lib/accrue/analytics/dunning.ex
      provides: "Expanded moduledoc"
      contains: "@since \"1.4.0\""
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue/guides/analytics.md"
      via: "grep verification"
      pattern: "require_fixed"
---

<objective>
Draft the new `guides/analytics.md` and expand `@moduledoc` on `Accrue.Analytics.Dunning` to freeze the public v1.44 surface (DAN-14, DAN-15). 

Purpose: Educate adopters on cutoff-date semantics, performance thresholds (100k events expression index rule-of-thumb), open-shape map contracts, and provide clear `@spec` / `@since` guarantees.
Output: Documentation files and updated CI verifier script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft guides/analytics.md</name>
  <files>accrue/guides/analytics.md, accrue/mix.exs</files>
  <action>Create `accrue/guides/analytics.md`. Document: public-API surface (with specs, examples, shapes), cutoff-date semantics + "Showing data since YYYY-MM-DD" UI badge context, window semantics (outcome-timestamp attribution, UTC), per-currency contract (no FX conversion), performance guide (when to add `(data->>'mrr_value_cents')` expression index at ~100k events), admin-auth limitation + escape-hatch, and open-shape map contracts. Finally, add the guide to `accrue/mix.exs` `extras` so it builds into HexDocs.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix docs</automated>
  </verify>
  <done>Guide exists and builds cleanly via `mix docs`.</done>
</task>

<task type="auto">
  <name>Task 2: Expand @moduledoc and API @since markers</name>
  <files>accrue/lib/accrue/analytics/dunning.ex</files>
  <action>Expand the `@moduledoc` on `Accrue.Analytics.Dunning` to contain a one-paragraph summary and link to `guides/analytics.md`. Add `@since "1.4.0"` to every public API function (`recovered_vs_lost_mrr`, `funnel`, `at_risk_subscriptions`, `campaign_timeline`, `recovery_rate`).</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix compile --warnings-as-errors</automated>
  </verify>
  <done>All public functions tagged and cross-linked correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Update CI Verifier Script</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <action>Add a `require_fixed` assertion line to `scripts/ci/verify_package_docs.sh` to ensure `guides/analytics.md` is checked, e.g., searching for a specific needle like "100k events" or a link to it from the README. Validate the addition.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The script runs successfully and tests the presence of the new guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs | Static documentation only. No execution risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-03 | Information Disclosure | `analytics.md` | accept | Public open-source repository; information is public. |
</threat_model>

<verification>
Ensure `mix docs` works properly and the package docs verification bash script passes.
</verification>

<success_criteria>
HexDocs will correctly serve the new guide and API specs.
</success_criteria>

<output>
After completion, create `.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-03-SUMMARY.md`
</output>
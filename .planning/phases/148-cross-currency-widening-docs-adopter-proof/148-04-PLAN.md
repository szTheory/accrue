---
phase: 148-cross-currency-widening-docs-adopter-proof
plan: 04
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - examples/accrue_host/priv/repo/seeds.exs
  - examples/accrue_host/test/accrue_host_web/live/recovery_analytics_test.exs
autonomous: true
requirements:
  - DAN-16
must_haves:
  truths:
    - "examples/accrue_host mounts a fully populated Recovery dashboard using seeded data."
    - "Seeds are deterministic using Accrue.Clock."
  artifacts:
    - path: examples/accrue_host/priv/repo/seeds.exs
      provides: "Deterministic dunning event seeds"
      contains: "Accrue.Clock"
    - path: examples/accrue_host/test/accrue_host_web/live/recovery_analytics_test.exs
      provides: "UI rendering proof"
      contains: "live(conn"
  key_links:
    - from: "examples/accrue_host/docs/adoption-proof-matrix.md"
      to: "examples/accrue_host/priv/repo/seeds.exs"
      via: "doc link"
      pattern: "Recovered Revenue Dashboard"
---

<objective>
Wire up deterministic-clock dunning events in `examples/accrue_host/priv/repo/seeds.exs` and prove out the dashboard in the Adopter Proof matrix (DAN-16).

Purpose: Allow adopters to clone the repository, run `mix setup`, and immediately see a populated, beautiful recovery dashboard demonstrating the value of Accrue's analytics. Test assertions lock this down.
Output: Updated seeds, updated test suite, and matrix markdown row.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Adopter Proof Matrix Row</name>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <action>Add a row for "Recovered Revenue Dashboard". Link it to the seed script `priv/repo/seeds.exs`, a placeholder screenshot path, and the LiveView path (`/billing/analytics/recovery`).</action>
  <verify>
    <automated>grep -q "Recovered Revenue Dashboard" examples/accrue_host/docs/adoption-proof-matrix.md</automated>
  </verify>
  <done>Row exists and links correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic Clock Seed Wiring</name>
  <files>examples/accrue_host/priv/repo/seeds.exs</files>
  <action>Update the seed script to insert dunning events spanning 7d / 30d / 90d windows. Use `Accrue.Clock` (not wall-clock `DateTime.utc_now()`) for relative timestamps so that tests remain deterministic. Include varied currencies if practical to showcase the multi-currency KPI cards.</action>
  <verify>
    <automated>cd examples/accrue_host &amp;&amp; mix run priv/repo/seeds.exs</automated>
  </verify>
  <done>Seeds execute without crashing and populate `accrue_events`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UI Rendering Proof Test</name>
  <files>examples/accrue_host/test/accrue_host_web/live/recovery_analytics_test.exs</files>
  <behavior>
    - Mocks admin auth.
    - Mounts `/billing/analytics/recovery`.
    - Asserts that KPI cards and funnel render with the deterministic seed values.
  </behavior>
  <action>Create or update the LiveView integration test to assert the dashboard mounts and renders a non-empty funnel, at-risk table, and drill-down against the deterministic seed data.</action>
  <verify>
    <automated>cd examples/accrue_host &amp;&amp; mix test test/accrue_host_web/live/recovery_analytics_test.exs</automated>
  </verify>
  <done>LiveView tests pass proving the dashboard renders end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Env | Runs completely isolated on developer hardware or CI runner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-04 | Spoofing | Seeds | accept | Test seeds are purely for development; clock manipulation is intentional here. |
</threat_model>

<verification>
`cd examples/accrue_host && mix test` should show perfectly passing integration tests.
</verification>

<success_criteria>
The Adopter Proof matrix is formally expanded with passing UI tests locking in the dashboard layout.
</success_criteria>

<output>
After completion, create `.planning/phases/148-cross-currency-widening-docs-adopter-proof/148-04-SUMMARY.md`
</output>
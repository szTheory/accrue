---
phase: 149
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["accrue/lib/accrue/dunning.ex", "accrue/test/accrue/dunning_test.exs"]
autonomous: true
requirements: ["BAN-01"]
must_haves:
  truths:
    - "`Accrue.Dunning.requires_attention?/1` correctly identifies active dunning campaigns without false positives from projection lag."
  artifacts:
    - path: "accrue/lib/accrue/dunning.ex"
      provides: "Public API for dunning state"
  key_links:
    - from: "accrue/lib/accrue/dunning.ex"
      to: "accrue/lib/accrue/billing/query.ex"
      via: "Accrue.Billing.Query.in_active_dunning_campaign/1"
---

<objective>
Provide the core state-checking helper `Accrue.Dunning.requires_attention?/1` for host apps to query if a customer is in an active dunning campaign.

Purpose: To enable host applications to know if they need to display a dunning intervention (e.g., a banner to update payment info) without false positives caused by projection lag.
Output: `Accrue.Dunning` module with the `requires_attention?/1` function.
</objective>

<context>
@.planning/milestones/v1.45-ROADMAP.md
@.planning/milestones/v1.45-REQUIREMENTS.md
@accrue/lib/accrue.ex
@accrue/lib/accrue/billing.ex
@accrue/lib/accrue/billing/query.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `Accrue.Dunning.requires_attention?/1`</name>
  <files>accrue/lib/accrue/dunning.ex</files>
  <action>Create the `Accrue.Dunning` module (or add to it if it exists as an empty shell). Implement `requires_attention?(billable_or_customer)` that returns a boolean. It should fetch the customer's active subscriptions using `Accrue.Billing.Query.in_active_dunning_campaign/1` to ensure the ledger is the source of truth, avoiding projection lag false positives.</action>
  <verify>
    <automated>MISSING — Wave 0 must create accrue/test/accrue/dunning_test.exs first</automated>
  </verify>
  <done>Function correctly identifies if a customer is currently in an active dunning campaign based on ledger state.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for `requires_attention?/1`</name>
  <files>accrue/test/accrue/dunning_test.exs</files>
  <action>Create a test module `Accrue.DunningTest` that verifies `requires_attention?/1` returns `true` when a customer has a subscription with `dunning_campaign_started_at` set (using factory setups to mimic an active dunning campaign), and `false` otherwise.</action>
  <verify>
    <automated>mix test accrue/test/accrue/dunning_test.exs</automated>
  </verify>
  <done>All tests pass, verifying both the true and false conditions.</done>
</task>

</tasks>

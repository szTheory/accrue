---
phase: 149
plan: 02
type: execute
wave: 2
depends_on: ["149-01"]
files_modified: ["accrue_admin/lib/accrue_admin/components/dunning_banner.ex"]
autonomous: true
requirements: ["BAN-02"]
must_haves:
  truths:
    - "The headless component correctly renders its inner block or default message when dunning is active, and renders nothing when not."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/dunning_banner.ex"
      provides: "Headless HEEx component for dunning banner"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/dunning_banner.ex"
      to: "accrue/lib/accrue/dunning.ex"
      via: "Accrue.Dunning.requires_attention?/1"
---

<objective>
Provide a headless HEEx component `<AccrueAdmin.Components.DunningBanner />` so host apps can easily display a dunning intervention in their UI.

Purpose: To encapsulate the logic of checking dunning state and optionally rendering content, giving host apps a drop-in component that works out of the box without enforcing CSS.
Output: `AccrueAdmin.Components.DunningBanner` module.
</objective>

<context>
@.planning/milestones/v1.45-ROADMAP.md
@.planning/milestones/v1.45-REQUIREMENTS.md
@accrue/lib/accrue/dunning.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `<DunningBanner />` headless component</name>
  <files>accrue_admin/lib/accrue_admin/components/dunning_banner.ex</files>
  <action>Create `AccrueAdmin.Components.DunningBanner` that uses `Phoenix.Component`. Implement a function component (e.g. `dunning_banner/1`) that accepts a `customer` (or `billable`) assign. It should use `Accrue.Dunning.requires_attention?(@customer)` to conditionally render its `inner_block` (or a fallback default slot/message) only when attention is required. Ensure the component is "headless" (minimally styled) so host apps can fully control appearance.</action>
  <verify>
    <automated>mix format accrue_admin/lib/accrue_admin/components/dunning_banner.ex</automated>
  </verify>
  <done>Component correctly delegates to `Accrue.Dunning.requires_attention?/1` and renders conditionally.</done>
</task>

</tasks>

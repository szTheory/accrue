---
phase: 150-documentation-adopter-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/dunning.md
autonomous: true
requirements: [BAN-03]
tags: [dunning, docs, banner, guides]

must_haves:
  truths:
    - "guides/dunning.md contains a top-level `## In-App Banners` section"
    - "The new section appears immediately after `## Over-email warning` and before `## Lifecycle and entitlements interaction`"
    - "The over-email warning section cross-links into the new In-App Banners section"
    - "The section documents the accrue_admin component path (zero-config default AND inner_block CTA customization)"
    - "The section documents the core-only DIY path using Accrue.Dunning.requires_attention?/1"
    - "The section is explicit about the dependency boundary: the component needs accrue_admin; the helper is core"
  artifacts:
    - path: "accrue/guides/dunning.md"
      provides: "In-App Banners integration documentation (BAN-03)"
      contains: "## In-App Banners"
  key_links:
    - from: "accrue/guides/dunning.md ## Over-email warning"
      to: "accrue/guides/dunning.md ## In-App Banners"
      via: "in-document cross-link"
      pattern: "In-App Banners"
---

<objective>
Add a `## In-App Banners` section to `accrue/guides/dunning.md` documenting both integration paths for the dunning banner capability shipped in Phase 149 (BAN-03).

Purpose: Adopters need clear copy/paste instructions to surface dunning status in their Phoenix UI — both the ready-made `accrue_admin` component and a core-only DIY path for hosts that do not pull `accrue_admin`.
Output: An updated guide section (placed after "Over-email warning") plus a cross-link from the over-email warning into it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-documentation-adopter-proof/150-CONTEXT.md
@.planning/phases/150-documentation-adopter-proof/150-RESEARCH.md
@.planning/phases/150-documentation-adopter-proof/150-PATTERNS.md

<interfaces>
<!-- Shipped Phase 149 artifacts being documented. DO NOT MODIFY. Use these signatures verbatim in snippets. -->

From accrue_admin/lib/accrue_admin/components/dunning_banner.ex (lines 15-37):
- Module: AccrueAdmin.Components.DunningBanner
- Function component: dunning_banner/1
- attr :customer, :any, required: true  (a %Accrue.Billing.Customer{} or billable)
- slot :inner_block, required: false
- Wrapper class: accrue-dunning-banner-wrapper
- Default-message class: accrue-default-dunning-banner
- Default copy (verbatim): "Action Required: We were unable to process your recent payment. Please update your payment method to avoid service interruption."
- Renders ~H"" (nothing) when the customer is NOT in an active dunning campaign.
- Lives in accrue_admin (LiveView-runtime hard-dep package), NOT in core accrue.

From accrue/lib/accrue/dunning.ex (lines 20-33):
- Function: Accrue.Dunning.requires_attention?/1 -> boolean
- Accepts a %Accrue.Billing.Customer{} or a billable.
- Core module (no accrue_admin dependency). Safe boolean for DIY markup.
- Delegates to Accrue.Billing.Query.in_active_dunning_campaign/1, whose predicate is
  `where: not is_nil(s.dunning_campaign_started_at)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the In-App Banners guide section with both integration paths</name>
  <files>accrue/guides/dunning.md</files>
  <read_first>
    - accrue/guides/dunning.md (the FULL file — to match heading style, fenced-block conventions, blockquote-warning style, and to find the exact `## Over-email warning` block at lines 237-253 and the `## Lifecycle and entitlements interaction` heading at line 256 it must be inserted between)
    - accrue_admin/lib/accrue_admin/components/dunning_banner.ex (verify the verbatim default copy string and wrapper/message class names before quoting them)
    - accrue/lib/accrue/dunning.ex (verify the requires_attention?/1 signature for the DIY snippet)
  </read_first>
  <action>
    Insert a new top-level `## In-App Banners` section into `accrue/guides/dunning.md` immediately AFTER the `## Over-email warning` block (after its closing `---` separator following line 253) and BEFORE `## Lifecycle and entitlements interaction` (line 256), per D-06.

    The section MUST contain two subsections (`### ...`) covering BOTH integration paths (D-07):

    1. Component path (requires accrue_admin): Reference `AccrueAdmin.Components.DunningBanner.dunning_banner/1` with its `:customer` attr and `inner_block` slot. Show TWO usages (D-08):
       (a) zero-config default — `<AccrueAdmin.Components.DunningBanner.dunning_banner customer={@customer} />` — and state the default copy verbatim: "Action Required: We were unable to process your recent payment. Please update your payment method to avoid service interruption."
       (b) inner_block customization — an actionable "Update your card" CTA linking to the host's payment/subscription route (use `~p"/app/billing"` as the example route, matching the example host). Use the `:let`/`inner_block` slot form.
       Be explicit that the customer must be a resolved `%Accrue.Billing.Customer{}` (or nil), NOT a raw billable — passing a billable triggers a get-or-create side effect on every render (Pitfall 1). Show the recommended resolution via the host's scope→customer helper pattern (analogous to `AccrueHost.Billing.customer_for_scope/1`).

    2. Core-only DIY path (core accrue, no accrue_admin): Show roll-your-own HEEx markup gated on `Accrue.Dunning.requires_attention?/@customer` (the verbatim source pattern from `accrue/lib/accrue/dunning.ex:20-33`), e.g. an `<%= if Accrue.Dunning.requires_attention?(@customer) do %>` block wrapping custom markup with an "Update your card" link.

    Be explicit about the DEPENDENCY BOUNDARY (D-07): the ready-made component requires the `accrue_admin` package; the `Accrue.Dunning.requires_attention?/1` helper is core `accrue`. Core stays LiveView-runtime-free — the component lives in accrue_admin.

    Add a cross-link FROM the existing `## Over-email warning` section INTO the new `## In-App Banners` section (D-06) — a sentence pointing readers to in-app banners as the non-email alternative (a markdown anchor link `[In-App Banners](#in-app-banners)`).

    Match the guide's existing conventions exactly: top-level `## Title`, sub-sections `### Title`, fenced ```elixir/```heex blocks, `>` blockquote for warnings, `# config/runtime.exs` comments. Do NOT re-derive `past_due`/lifecycle truth — keep the existing discipline of cross-referencing `lifecycle_semantics.md#past_due`. Do NOT re-specify campaign config/engine — this section is about DISPLAY only. Do NOT modify the shipped Phase 149 component or core helper.
  </action>
  <verify>
    <automated>grep -q "## In-App Banners" accrue/guides/dunning.md && grep -q "AccrueAdmin.Components.DunningBanner" accrue/guides/dunning.md && grep -q "Accrue.Dunning.requires_attention?" accrue/guides/dunning.md && awk '/## Over-email warning/{o=NR} /## In-App Banners/{b=NR} /## Lifecycle and entitlements interaction/{l=NR} END{exit !(o<b && b<l)}' accrue/guides/dunning.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## In-App Banners" accrue/guides/dunning.md` succeeds.
    - The `## In-App Banners` heading appears after `## Over-email warning` and before `## Lifecycle and entitlements interaction` (verified by the awk line-order check in <verify>).
    - The section contains the literal `AccrueAdmin.Components.DunningBanner` (component path) AND `Accrue.Dunning.requires_attention?` (core DIY path).
    - The section contains the verbatim default copy substring `Action Required: We were unable to process your recent payment`.
    - The section contains an inner_block CTA example with an "Update your card" CTA and a `~p"/app/billing"` link.
    - The section states the dependency boundary: component requires `accrue_admin`; helper is core.
    - The `## Over-email warning` section contains a cross-link mentioning In-App Banners (anchor `[In-App Banners](#in-app-banners)` or equivalent).
    - The 4 existing `verify_package_docs.sh` dunning.md needles (`Accrue.Dunning.Engine`, `Accrue.Integrations.Chimeway`, `dunning: [engine:`) remain present and untouched.
  </acceptance_criteria>
  <done>The guide has a complete `## In-App Banners` section documenting both the accrue_admin component path (default + CTA) and the core DIY path, placed correctly with a cross-link from over-email warning, and the verify command exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → adopter | Guide snippets are copied verbatim by adopters into their own apps; inaccurate or unsafe snippets propagate the risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Tampering | Component-path snippet passing a raw billable | mitigate | Snippet MUST resolve to a `%Accrue.Billing.Customer{}` (scope-bound) and never pass a raw `Organization` billable — documents the get-or-create Pitfall 1 to prevent adopters introducing a side-effecting customer-creation-on-render bug. |
| T-150-02 | Information Disclosure | Cross-tenant dunning-state leak in adopter snippet | mitigate | Snippet resolves the customer strictly from the current scope's active organization (`customer_for_scope/1`-style), never from a params-supplied customer id. |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this docs-only plan; no supply-chain surface. |
</threat_model>

<verification>
- `grep -q "## In-App Banners" accrue/guides/dunning.md` exits 0.
- Section ordering (over-email → in-app banners → lifecycle) holds.
- Both integration paths and the dependency boundary are documented.
- No existing `verify_package_docs.sh` dunning.md needle is removed.
</verification>

<success_criteria>
ROADMAP SC#1 satisfied: `guides/dunning.md` contains clear copy/paste instructions for adding the banner to a Phoenix layout (both the component path and the core-only DIY path), placed after "Over-email warning" with a cross-link.
</success_criteria>

<output>
Create `.planning/phases/150-documentation-adopter-proof/150-01-SUMMARY.md` when done.
</output>

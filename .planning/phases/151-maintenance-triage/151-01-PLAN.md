---
phase: "151"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
autonomous: true
requirements:
  - MNT-01
must_haves:
  truths:
    - "Entitlement summary webhook reducer scopes updates by processor"
  artifacts:
    - path: accrue/lib/accrue/webhook/default_handler.ex
      provides: "Webhook handler with strict processor filtering"
  key_links:
    - from: accrue/lib/accrue/webhook/default_handler.ex
      to: "Customer model"
      via: "Repo.get_by with processor_id and processor"
      pattern: "processor: to_string"
---

<objective>
Resolve webhook caching code-review feedback (ENT-10) by enforcing strict processor scoping for customer lookup in the default handler, per D-01 triage priority.

Purpose: Prevents customer ID collisions across different payment processors (e.g. "fake" vs "stripe").
Output: Updated webhook handler and passing tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@accrue/lib/accrue/webhook/default_handler.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve ENT-10 scoping collisions in webhook handler</name>
  <files>accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs</files>
  <behavior>
    - Test 1: Webhook processor matches customer processor -> success
    - Test 2: Webhook processor differs from customer processor -> no match
  </behavior>
  <action>Per D-01 (Clean Room Triage & Money-Safety First), resolve the ENT-10 advisory-cache scoping collision. In `accrue/lib/accrue/webhook/default_handler.ex`, modify `handle_event("entitlements.active_entitlement_summary.updated", ...)` to pass `event.processor` into the `dispatch` call. Then update the `reduce_entitlement_summary_for_customer` function signature to accept the processor. Update the `Repo.get_by(Customer, processor_id: cus_id)` query inside the reducer to strictly filter by both `processor: to_string(event.processor)` and `processor_id: cus_id`. Finally, in `accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs`, update the setup block to use `processor: "stripe"` (instead of `"fake"`) since the test webhook payload uses Stripe, ensuring the test passes with the new strict scoping.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs</automated>
  </verify>
  <done>Handler uses `processor` for customer lookup and the test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook Event | External processor identifiers used to query local database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Information Disclosure | Webhook Handler | mitigate | Strict scoping of `processor_id` by `processor` to avoid collisions across providers |
</threat_model>

<verification>
`cd accrue && mix test`
</verification>

<success_criteria>
ENT-10 webhook scoping bug is resolved and tested.
</success_criteria>

<output>
Create `.planning/phases/151-maintenance-triage/151-01-SUMMARY.md` when done
</output>
---
phase: 152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/analytics/dunning.ex
  - accrue_admin/lib/accrue_admin/components/funnel_chart.ex
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-02: All @since annotations are in canonical @doc since: \"1.3.0\" form — no stray @since text inside heredocs"
    - "D-02: The compiler warning for dunning.ex:343 (module attribute @since was set but never used) is eliminated"
    - "D-02: ExDoc renders a '(since 1.3.0)' badge for all 7 fixed locations — no literal @since junk in rendered output"
    - "D-03: mix compile --warnings-as-errors exits 0 in both accrue and accrue_admin"
    - "D-03: bash scripts/ci/verify_package_docs.sh exits 0 after the fix"
  artifacts:
    - path: "accrue/lib/accrue/analytics/dunning.ex"
      provides: "Fixed @since annotations — 6 heredoc/free-floating instances removed, 1 canonical @doc since: version corrected"
      contains: "@doc since: \"1.3.0\""
    - path: "accrue_admin/lib/accrue_admin/components/funnel_chart.ex"
      provides: "Fixed @since annotation — 1 heredoc instance replaced with canonical @doc since:"
      contains: "@doc since: \"1.3.0\""
  key_links:
    - from: "accrue/lib/accrue/analytics/dunning.ex"
      to: "ExDoc @doc since: badge"
      via: "@doc since: \"1.3.0\" placed as separate module attribute after the closing \"\"\""
      pattern: "@doc since: \"1.3.0\""
    - from: "mix compile --warnings-as-errors"
      to: "zero compiler warnings"
      via: "elimination of free-floating @since \"1.4.0\" at lines 333 and 343"
      pattern: "no @since module attribute warnings"
---

<objective>
Fix all 7 malformed `@since` annotations across the two-package monorepo.

The malformed annotations fall into three categories:
1. Lines 50, 98, 159, 224 in dunning.ex — stray `@since "1.4.0"` text INSIDE `@doc """ ... """` heredocs (renders as literal junk in ExDoc output)
2. Lines 333, 343 in dunning.ex — free-floating `@since "1.4.0"` module attributes OUTSIDE the heredoc but between `@doc` and `@spec` (line 343 generates a compiler warning)
3. Line 371 in dunning.ex — already-canonical `@doc since: "1.4.0"` form but wrong version
4. Line 27 in funnel_chart.ex — stray `@since "1.4.0"` text INSIDE `@doc """ ... """` heredoc

Purpose: Eliminate the compiler warning, remove ExDoc junk text, and establish canonical ExDoc version badges at the correct version (1.3.0, per D-01). This is a prerequisite for the Three Zeros gate (Plan 02) and the release PR (Plan 03).

Output: Both source files with all @since annotations in canonical @doc since: "1.3.0" form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-CONTEXT.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-RESEARCH.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dunning.ex — remove 4 heredoc-embedded @since lines and replace 2 free-floating @since attributes and 1 wrong-version @doc since:</name>
  <files>accrue/lib/accrue/analytics/dunning.ex</files>
  <read_first>
    Read accrue/lib/accrue/analytics/dunning.ex in full before touching anything. Key areas:
    - Line 50: `@since "1.4.0"` is the second-to-last line inside the `@doc """ ... """` heredoc for recovered_vs_lost_mrr/1. The heredoc closes at line 51 (`"""`). Remove line 50 from inside the heredoc; add `@doc since: "1.3.0"` on a new line immediately after line 51 (the `"""`), before the `@spec` at line 52.
    - Line 98: Same pattern inside the heredoc for recovery_rate/1. Heredoc closes at line 99. Remove line 98; add `@doc since: "1.3.0"` after the `"""` at line 99.
    - Line 159: Same pattern inside the heredoc for funnel/1. Heredoc closes at line 160. Remove line 159; add `@doc since: "1.3.0"` after the `"""` at line 160.
    - Line 224: Same pattern inside the heredoc for at_risk_subscriptions/1. Heredoc closes at line 225. Remove line 224; add `@doc since: "1.3.0"` after the `"""` at line 225.
    - Lines 330-334: `@doc """ ... """` heredoc for campaign_timeline/2 closes at line 332; line 333 is a FREE-FLOATING `@since "1.4.0"` outside the heredoc. Replace line 333 with `@doc since: "1.3.0"`.
    - Lines 340-344: `@doc """ ... """` heredoc for campaign_timeline_grouped/2 closes at line 342; line 343 is a FREE-FLOATING `@since "1.4.0"` outside the heredoc. Replace line 343 with `@doc since: "1.3.0"`. THIS IS THE LINE THAT GENERATES THE COMPILER WARNING.
    - Line 371: `@doc since: "1.4.0"` — already canonical form, only needs version changed from "1.4.0" to "1.3.0".

    The canonical form (reference: dunning.ex line 371 before editing) is:
      @doc """
      Description text.
      """
      @doc since: "1.3.0"
      @spec function_name(arg) :: return_type
      def function_name(arg) do

    Do NOT put `@doc since:` inside the heredoc content. It must be on its own line AFTER the closing `"""`.
  </read_first>
  <action>
    Edit accrue/lib/accrue/analytics/dunning.ex to apply all 7 fixes in a single pass per D-02. Apply these changes:

    FIX 1 (line 50 — inside heredoc): Remove the line `  @since "1.4.0"` from inside the heredoc body. After the closing `  """` on the next line, insert `  @doc since: "1.3.0"` on its own line.

    FIX 2 (line 98 — inside heredoc): Same operation for the recovery_rate/1 heredoc.

    FIX 3 (line 159 — inside heredoc): Same operation for the funnel/1 heredoc.

    FIX 4 (line 224 — inside heredoc): Same operation for the at_risk_subscriptions/1 heredoc. Note: the `:since` option key in the documentation prose (e.g., "* `:since` — `%DateTime{}`") must NOT be altered — only the stray `@since "1.4.0"` line at line 224 is removed.

    FIX 5 (line 333 — free-floating outside heredoc): Replace `  @since "1.4.0"` with `  @doc since: "1.3.0"`. This line sits between the closing `"""` of campaign_timeline's @doc heredoc and the `@spec` line.

    FIX 6 (line 343 — free-floating outside heredoc, compiler warning source): Replace `  @since "1.4.0"` with `  @doc since: "1.3.0"`. This line sits between the closing `"""` of campaign_timeline_grouped's @doc heredoc and the `@spec` line.

    FIX 7 (line 371 — already canonical, wrong version): Change `@doc since: "1.4.0"` to `@doc since: "1.3.0"` for invoices_for_campaign/2.

    After all edits, verify with grep that no bare `@since` remains: `grep -n '@since' accrue/lib/accrue/analytics/dunning.ex` should return only lines with `@doc since:` form (the `:since` prose in option docs is fine — those are string literals, not module attributes).
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue && mix compile --warnings-as-errors 2>&1 | grep -i 'since\|warning\|error' || echo "CLEAN"
      grep -n '@since' /Users/jon/projects/accrue/accrue/lib/accrue/analytics/dunning.ex
      grep -c '@doc since: "1.3.0"' /Users/jon/projects/accrue/accrue/lib/accrue/analytics/dunning.ex
    </automated>
  </verify>
  <done>
    - `mix compile --warnings-as-errors` exits 0 in accrue (no @since warning, no other new warnings)
    - `grep -n '@since' dunning.ex` returns only lines containing `@doc since:` form (no bare `@since "1.4.0"`)
    - `grep -c '@doc since: "1.3.0"' dunning.ex` returns 7
    - No `:since` option docs in prose were altered (confirm lines like "* `:since` — `%DateTime{}`" are intact)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix funnel_chart.ex — remove heredoc-embedded @since and add canonical @doc since:</name>
  <files>accrue_admin/lib/accrue_admin/components/funnel_chart.ex</files>
  <read_first>
    Read accrue_admin/lib/accrue_admin/components/funnel_chart.ex in full before editing. Key context:
    - Line 27: `  @since "1.4.0"` sits inside the `@doc """ ... """` heredoc for the FunnelChart component module. The heredoc closes at line 28 (`  """`).
    - Line 30: `use Phoenix.Component` follows after the closing `"""`.
    - The canonical placement is `@doc since: "1.3.0"` as a separate line AFTER the closing `"""` (line 28), BEFORE the `use Phoenix.Component` (line 30). Since this is a module-level @doc (not a function), the `@doc since:` form still applies as a module doc option.
    - Confirm: does `grep -n '@since' accrue_admin/lib/accrue_admin/components/funnel_chart.ex` return exactly 1 hit at line 27? (RESEARCH.md confirms: yes, exactly 1.)
  </read_first>
  <action>
    Edit accrue_admin/lib/accrue_admin/components/funnel_chart.ex to apply 1 fix per D-02:

    FIX 1 (line 27 — inside heredoc): Remove the line `  @since "1.4.0"` from inside the @moduledoc or @doc heredoc body. After the closing `  """` on line 28, insert `  @doc since: "1.3.0"` on its own line.

    After editing, confirm with grep: `grep -n '@since' accrue_admin/lib/accrue_admin/components/funnel_chart.ex` must return only the `@doc since:` form — no bare `@since`.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue_admin && mix compile --warnings-as-errors 2>&1 | grep -i 'since\|warning\|error' || echo "CLEAN"
      grep -n '@since\|@doc since' /Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/funnel_chart.ex
    </automated>
  </verify>
  <done>
    - `mix compile --warnings-as-errors` exits 0 in accrue_admin (no new warnings)
    - `grep -n '@since' funnel_chart.ex` returns exactly 1 line containing `@doc since: "1.3.0"` (no bare `@since "1.4.0"`)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify docs gate and run accrue mix docs spot-check</name>
  <files></files>
  <read_first>
    Review scripts/ci/verify_package_docs.sh to understand which needles it checks. Per RESEARCH.md pitfall 7, this phase does NOT add any new needles to the script, so no PackageDocsVerifierTest fixture update is needed.
  </read_first>
  <action>
    From the repo root (not a package subdirectory), run the docs-honesty gate script:

      bash scripts/ci/verify_package_docs.sh

    This must exit 0. If it fails on a version string check (e.g., looking for `{:accrue, "~> 1.2.0"}` in README), the @since fix did not introduce the failure — investigate and confirm the failure predated this plan before escalating.

    Optionally, run `cd accrue && mix docs` and spot-check that the functions fixed in Task 1 display a "(since 1.3.0)" badge in the generated HTML. This is a visual manual-only check (listed in VALIDATION.md Manual-Only Verifications) — record the outcome but do not block on it.
  </action>
  <verify>
    <automated>
      bash /Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
    </automated>
  </verify>
  <done>
    - `bash scripts/ci/verify_package_docs.sh` exits 0
    - No regressions introduced by the @since edits
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source edits | Developer edits Elixir source files; no external input crosses any trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01-plan01 | — | dunning.ex, funnel_chart.ex | accept | No new attack surface. This plan edits docstring metadata and version strings only. No authentication, authorization, webhook handling, or secret management is modified. Read-only verification scripts. |

No new attack surface (docstring/metadata + read-only verification).
</threat_model>

<verification>
After both tasks complete:
- `grep -rn '@since "' accrue/lib/accrue/analytics/dunning.ex accrue_admin/lib/accrue_admin/components/funnel_chart.ex` returns zero results (all stray bare @since forms eliminated)
- `grep -c '@doc since: "1.3.0"' accrue/lib/accrue/analytics/dunning.ex` returns 7
- `grep -c '@doc since: "1.3.0"' accrue_admin/lib/accrue_admin/components/funnel_chart.ex` returns 1
- `cd accrue && mix compile --warnings-as-errors` exits 0
- `cd accrue_admin && mix compile --warnings-as-errors` exits 0
- `bash scripts/ci/verify_package_docs.sh` exits 0
</verification>

<success_criteria>
- All 8 @since items across both files fixed to canonical `@doc since: "1.3.0"` (7 in dunning.ex + 1 in funnel_chart.ex)
- Zero compiler warnings in accrue and accrue_admin (--warnings-as-errors clean)
- verify_package_docs.sh exits 0
- D-02 fully satisfied: malformed annotations converted to canonical ExDoc form at version 1.3.0
</success_criteria>

<output>
Create `.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-01-SUMMARY.md` when done.
</output>

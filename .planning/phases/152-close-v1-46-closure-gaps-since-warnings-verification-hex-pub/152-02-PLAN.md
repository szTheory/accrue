---
phase: 152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03: mix test --seed 0 exits 0 in all three packages (accrue, accrue_admin, accrue_portal)"
    - "D-03: mix dialyzer exits 0 in accrue"
    - "D-03: mix credo --strict exits 0 in all three packages"
    - "D-03: mix coveralls exits 0 in accrue; accrue_admin and accrue_portal meet their configured thresholds (80 and 75 respectively)"
    - "D-03: All verify_*.sh scripts in scripts/ci/ exit 0 (including adoption matrix, release contract, manifest alignment, and release notes contract)"
    - "D-03: bash scripts/ci/verify_release_notes_contract.sh passes against the current version (1.2.0 at time of this gate)"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Docs-honesty gate (run by Plan 01 Task 3; re-confirmed here)"
    - path: "scripts/ci/verify_adoption_proof_matrix.sh"
      provides: "Adoption-proof matrix gate"
    - path: "scripts/ci/verify_release_notes_contract.sh"
      provides: "Release-notes contract gate (checks ### 1.2.0 currently)"
    - path: "scripts/ci/verify_release_manifest_alignment.sh"
      provides: "Manifest-alignment gate (all three packages at 1.2.0)"
    - path: "scripts/ci/verify_release_contract.sh"
      provides: "Release contract gate"
  key_links:
    - from: "accrue mix test --seed 0"
      to: "PdfTest flake dodge"
      via: "--seed 0 ensures stable test order that avoids the known PdfTest flake"
      pattern: "mix test --seed 0"
    - from: "mix coveralls (accrue)"
      to: "ExCoveralls report"
      via: "preferred_cli_env set in accrue/mix.exs from Phase 151-03"
      pattern: "mix coveralls"
---

<objective>
Run the "Three Zeros" closure gate across all three packages and report green status.

The Three Zeros gate (D-03, carried from Phase 151) defines readiness for publish:
1. Zero open P0/P1 triage issues (confirmed by STATE.md — no open blockers)
2. Zero audit gaps — all verify_*.sh scripts green
3. Zero Nyquist/coverage gaps — mix test / dialyzer / credo / coveralls all passing

This plan is Wave 1 (parallel with Plan 01) because it runs the gate against the current codebase state BEFORE the @since fixes from Plan 01 land. The gate must be green on everything EXCEPT the @since compiler warning (which Plan 01 eliminates). If the gate reveals other failures, document and fix them — they are pre-existing issues, not regressions from this phase.

After Plan 01 lands, the full gate (including --warnings-as-errors) will be re-run in Plan 03's pre-publish checklist.

Purpose: Confirm the codebase is otherwise clean and publishable before the release PR is cut.

Output: A passing gate run documented in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-CONTEXT.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-RESEARCH.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mix test --seed 0, mix credo --strict, and mix coveralls across all three packages</name>
  <files><!-- verification only — no files created or modified --></files>
  <read_first>
    Read .planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-RESEARCH.md §"Gate Commands — Full Mix Test + Static Analysis" and §"Gate Scripts — Zero Nyquist/Coverage Component" for the exact commands to run. Key points:
    - Run mix test with --seed 0 in every package to dodge the known PdfTest flake (accrue only — but use --seed 0 everywhere for consistency).
    - accrue_admin threshold: 80 (mix.exs line 19 test_coverage summary threshold).
    - accrue_portal threshold: 75 (mix.exs line 20 test_coverage summary threshold).
    - accrue uses ExCoveralls: run `mix coveralls` (not `mix test --cover`) in the accrue directory.
    - dialyzer only needed in accrue (admin/portal have no separate PLTs per RESEARCH.md).
    - Confirm `command -v jq` before running release contract scripts that require jq.
  </read_first>
  <action>
    Run the following commands sequentially (each must exit 0 before proceeding):

    1. In accrue/: `mix test --seed 0`
    2. In accrue_admin/: `mix test --seed 0`
    3. In accrue_portal/: `mix test --seed 0`
    4. In accrue/: `mix coveralls` (ExCoveralls wired in Phase 151-03)
    5. In accrue_admin/: `mix test --cover` (threshold 80)
    6. In accrue_portal/: `mix test --cover` (threshold 75)
    7. In accrue/: `mix credo --strict`
    8. In accrue_admin/: `mix credo --strict`
    9. In accrue_portal/: `mix credo --strict`

    Document results. If any step fails, investigate whether it is a pre-existing issue or a regression. Do not lower ExCoveralls thresholds — they are ratified baselines from Phase 151-03 per D-03.

    Known acceptable outcome: accrue `mix test --seed 0` may still show the @since "1.4.0" compiler warning (not --warnings-as-errors here) until Plan 01 fixes land. That warning is expected and is not a gate failure at this step.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue && mix test --seed 0 2>&1 | tail -5
      cd /Users/jon/projects/accrue/accrue_admin && mix test --seed 0 2>&1 | tail -5
      cd /Users/jon/projects/accrue/accrue_portal && mix test --seed 0 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - mix test --seed 0 exits 0 in all three packages (PdfTest flake dodged by --seed 0)
    - mix coveralls exits 0 in accrue
    - mix test --cover exits 0 in accrue_admin (coverage >= 80%) and accrue_portal (coverage >= 75%)
    - mix credo --strict exits 0 in all three packages
  </done>
</task>

<task type="auto">
  <name>Task 2: Run mix dialyzer in accrue and execute the full verify_*.sh gate suite</name>
  <files><!-- verification only — no files created or modified --></files>
  <read_first>
    Read .planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-RESEARCH.md §"Gate Scripts — Zero Audit Gaps Component" for the full list of scripts. The gate suite includes:
    - scripts/ci/verify_package_docs.sh (already run by Plan 01 Task 3 — re-confirm here)
    - scripts/ci/verify_adoption_proof_matrix.sh
    - scripts/ci/verify_release_notes_contract.sh (checks ### 1.2.0 currently — must pass at 1.2.0 before release PR)
    - scripts/ci/verify_release_manifest_alignment.sh (requires jq)
    - scripts/ci/verify_release_contract.sh (requires jq)
    - scripts/ci/verify_processor_support_matrix.sh
    - scripts/ci/verify_core_liveview_runtime_free.sh
    - scripts/ci/verify_entitlement_sync_isolation.sh
    - scripts/ci/verify_dunning_chimeway_isolation.sh
    - scripts/ci/verify_v1_17_friction_research_contract.sh
    - scripts/ci/verify_verify01_readme_contract.sh
    - scripts/ci/verify_production_readiness_discoverability.sh
    - scripts/ci/verify_core_admin_invoice_verify_ids.sh

    Confirm `command -v jq` and `command -v gh` before running scripts that require them. If jq is missing, note it — the manifest/contract scripts need jq.
  </read_first>
  <action>
    Run from the repo root (/Users/jon/projects/accrue):

    1. `cd accrue && mix dialyzer` — dialyzer for the core package only. This may take several minutes on first run (PLT build); subsequent runs are faster. Exit 0 is required.

    2. Run each verify_*.sh script from the repo root. Run them in the order listed in read_first. All must exit 0.

    If `verify_release_notes_contract.sh` fails because it now expects `### 1.3.0` (after Plan 01's @since fix commit triggers something unexpected), re-read the script to confirm it checks the current @version (1.2.0) — at this stage the release PR has not yet been cut, so the script should check for `### 1.2.0`.

    Document any failures with the exact command and output. If a script fails on something unrelated to this phase (pre-existing issue), note it in the SUMMARY and do not block the phase — but the failure must be understood and categorized as pre-existing before proceeding to Plan 03.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue && mix dialyzer 2>&1 | tail -10
      bash /Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
      bash /Users/jon/projects/accrue/scripts/ci/verify_adoption_proof_matrix.sh
      bash /Users/jon/projects/accrue/scripts/ci/verify_release_notes_contract.sh
      bash /Users/jon/projects/accrue/scripts/ci/verify_release_manifest_alignment.sh
      bash /Users/jon/projects/accrue/scripts/ci/verify_release_contract.sh
    </automated>
  </verify>
  <done>
    - mix dialyzer exits 0 in accrue
    - All verify_*.sh scripts exit 0 from repo root
    - Any failures are documented with a pre-existing/regression classification
    - Gate is green: Three Zeros achieved (D-03)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell execution | Gate scripts run bash commands locally; no external input |
| CI scripts | Read-only verification against local file state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-02-plan02 | — | scripts/ci/verify_*.sh | accept | No new attack surface. This plan runs read-only verification scripts and mix commands against the local codebase. No credentials, secrets, or external services are involved. Results are local output only. |

No new attack surface (read-only verification gate).
</threat_model>

<verification>
Three Zeros gate is complete and green when:
- mix test --seed 0 exits 0 in all three packages
- mix dialyzer exits 0 in accrue
- mix credo --strict exits 0 in all three packages
- mix coveralls exits 0 in accrue; mix test --cover threshold met in accrue_admin (80%) and accrue_portal (75%)
- All scripts/ci/verify_*.sh scripts exit 0 from repo root
</verification>

<success_criteria>
- All Three Zeros criteria satisfied (D-03)
- No unclassified failures — every non-zero exit is either resolved or documented as pre-existing with category (pre-existing / phase-regression)
- Gate result documented in SUMMARY for Plan 03 to reference as proof of pre-publish readiness
</success_criteria>

<output>
Create `.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-02-SUMMARY.md` when done.
</output>

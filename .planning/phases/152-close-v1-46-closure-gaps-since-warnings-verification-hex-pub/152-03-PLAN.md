---
phase: 152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub
plan: 03
type: execute
wave: 2
depends_on:
  - "152-01"
  - "152-02"
files_modified:
  - accrue/guides/release-notes.md
autonomous: false
requirements: []
user_setup:
  - service: hex
    why: "Hex.pm API key required for mix hex.publish — must exist as GitHub Actions secret HEX_API_KEY"
    env_vars:
      - name: HEX_API_KEY
        source: "Hex.pm Dashboard -> Your Profile -> API Keys; stored as GitHub Actions secret (Settings -> Secrets -> Actions -> HEX_API_KEY)"
    dashboard_config:
      - task: "Confirm HEX_API_KEY secret exists in GitHub repository secrets"
        location: "GitHub repository -> Settings -> Secrets and variables -> Actions"
      - task: "Confirm RELEASE_PLEASE_TOKEN secret exists in GitHub repository secrets"
        location: "GitHub repository -> Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "D-01: Release Please computes version 1.3.0 (not 1.4.0) for all three packages from conventional commits since 1.2.0"
    - "D-01: Tags accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0 exist on the remote"
    - "D-04: A single combined Release Please PR updates @version in all three mix.exs files plus .release-please-manifest.json and all three CHANGELOG.md files"
    - "D-04: accrue_admin and accrue_portal version pins to accrue resolve correctly at 1.3.0 (~> 1.3.0 and == 1.3.0 respectively via @version interpolation)"
    - "D-04: mix hex.publish succeeds for all three packages in order: accrue → accrue_admin → accrue_portal (enforced by release-please.yml needs: chain)"
    - "D-04: CHANGELOG Unreleased sections in accrue and accrue_admin are frozen or drained — no overlapping Unreleased + [1.3.0] prose ships"
    - "D-02/D-01: accrue/guides/release-notes.md has ### 1.3.0 blocks in both ## accrue and ## accrue_admin sections (gates verify_release_notes_contract.sh)"
  artifacts:
    - path: "accrue/guides/release-notes.md"
      provides: "Human-readable 1.3.0 release notes in both package sections"
      contains: "### 1.3.0"
    - path: ".release-please-manifest.json"
      provides: "Version manifest bumped to 1.3.0 by Release Please (NOT manually edited)"
      contains: "\"1.3.0\""
    - path: "accrue/mix.exs"
      provides: "@version bumped to 1.3.0 by Release Please"
      contains: "@version \"1.3.0\""
    - path: "accrue_admin/mix.exs"
      provides: "@version bumped to 1.3.0 by Release Please"
      contains: "@version \"1.3.0\""
    - path: "accrue_portal/mix.exs"
      provides: "@version bumped to 1.3.0 by Release Please"
      contains: "@version \"1.3.0\""
  key_links:
    - from: "release-please.yml"
      to: "accrue Hex package"
      via: "publish-accrue job; mix hex.publish using HEX_API_KEY secret"
      pattern: "accrue-v1.3.0 tag created on merge"
    - from: "accrue/guides/release-notes.md (### 1.3.0)"
      to: "verify_release_notes_contract.sh"
      via: "Script checks that ### ${accrue_version} appears twice in release-notes.md"
      pattern: "### 1.3.0 in both ## accrue and ## accrue_admin sections"
---

<objective>
Prepare and merge the linked 1.3.0 Release Please PR, then confirm the triggered publish pipeline creates all three Hex packages and tags.

This plan has two preparatory autonomous tasks (adding release-notes.md entries + CHANGELOG reconciliation) and two manual human-gate tasks (release PR review + publish confirmation). The irreversible steps — merge, hex.publish, tag creation — are behind manual checkpoints per project convention and D-04.

Pre-conditions (must be true before executing this plan):
- Plan 01 SUMMARY confirms: @since fixes merged to main, mix compile --warnings-as-errors clean in both packages
- Plan 02 SUMMARY confirms: Three Zeros gate green (all verify_*.sh + mix test/dialyzer/credo/coveralls passing)

Purpose: Cut the 1.3.0 linked release across all three packages, completing the v1.46 milestone.

Output: accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0 live on Hex.pm with matching git tags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@RELEASING.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-CONTEXT.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-RESEARCH.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-VALIDATION.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-01-SUMMARY.md
@.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ### 1.3.0 entries to release-notes.md and reconcile CHANGELOG Unreleased sections</name>
  <files>accrue/guides/release-notes.md, accrue/CHANGELOG.md, accrue_admin/CHANGELOG.md</files>
  <read_first>
    Read these files in full before editing:
    - accrue/guides/release-notes.md — Current state has ### 1.2.0 as the latest in both ## accrue and ## accrue_admin sections. Add ### 1.3.0 ABOVE the ### 1.2.0 entry in both sections. The ### 1.3.0 block for ## accrue should summarize the v1.46 feature batch (dunning analytics, recovered-revenue dashboard, in-app dunning banners, ENT-10 webhook fix, dependency bumps). The ### 1.3.0 block for ## accrue_admin should summarize the admin-side features (funnel chart component, recovery dashboard LiveView, banner component).
    - accrue/CHANGELOG.md — Read the ## Unreleased section fully. Per RELEASING.md §"Changelog ship boundary", the Unreleased prose must be frozen or drained into the Release-Please-generated ## [1.3.0] section. The strategy: add a short comment above ## Unreleased noting "Content merged into ## [1.3.0] by Release Please — see below" and remove or retitle the ## Unreleased header to ## [Unreleased — see 1.3.0 below] before the release PR opens, so Release Please does not create a duplicate conflict. Alternatively, leave ## Unreleased as-is and drain it manually DURING the release PR review (on the PR branch). Choose the cleaner approach: leave CHANGELOG.md untouched here and drain on the PR branch.
    - accrue_admin/CHANGELOG.md — Same policy as accrue. Leave untouched here; drain on the PR branch.
    - scripts/ci/verify_release_notes_contract.sh — Read to understand exactly what ### ${version} pattern it checks and in which sections. Confirm it will require ### 1.3.0 (not ### 1.2.0) once @version becomes 1.3.0 after the release PR lands. The ### 1.3.0 entries must be present on the release PR branch BEFORE merge so CI passes on the release PR diff.

    IMPORTANT: The ### 1.3.0 entries are added to release-notes.md now (on main or the release branch), NOT post-merge. See RESEARCH.md §"release-notes.md Requires Manual Update" — Pitfall 3.
  </read_first>
  <action>
    Edit accrue/guides/release-notes.md to add ### 1.3.0 blocks:

    In the ## accrue section: add a ### 1.3.0 heading with a concise paragraph summarizing the 1.3.0 feature batch. Content should cover: recovered-revenue analytics (recovered_vs_lost_mrr/1, recovery_rate/1, funnel/1, at_risk_subscriptions/1, campaign_timeline/2, campaign_timeline_grouped/2, invoices_for_campaign/2), recovery funnel dashboard at /billing/analytics/recovery, in-app dunning banners (Accrue.Analytics.Dunning.current_dunning_context/2), ENT-10 webhook scoping fix (per-entity not global cache), dependency maintenance (accrue, accrue_admin, accrue_portal). Reference version 1.3.0 explicitly.

    In the ## accrue_admin section: add a ### 1.3.0 heading covering: FunnelChart LiveComponent, recovery dashboard LiveView at /admin/analytics/recovery, in-app banner component (AccrueAdmin.Components.DunningBanner), and the @since annotation cleanup.

    The existing ### 1.2.0 entries remain in place below the new ### 1.3.0 entries.

    Do NOT edit accrue/CHANGELOG.md or accrue_admin/CHANGELOG.md in this task — per RELEASING.md, CHANGELOG reconciliation happens on the release PR branch, not on main before the PR is cut.

    After editing, run verify_release_notes_contract.sh with the version overridden to 1.3.0 to pre-validate (if the script supports a VERSION env var or argument). If not, the contract will be validated by CI after the release PR bumps @version.
  </action>
  <verify>
    <automated>
      grep -c '### 1.3.0' /Users/jon/projects/accrue/accrue/guides/release-notes.md
      bash /Users/jon/projects/accrue/scripts/ci/verify_release_notes_contract.sh
    </automated>
  </verify>
  <done>
    - accrue/guides/release-notes.md has at least 2 occurrences of "### 1.3.0" (one in ## accrue section, one in ## accrue_admin section)
    - verify_release_notes_contract.sh exits 0 (note: at this stage the script may still check for 1.2.0 if @version has not been bumped yet — that is expected and fine; confirm the ### 1.2.0 entries are still present)
    - accrue/CHANGELOG.md and accrue_admin/CHANGELOG.md are NOT modified by this task (deferred to release PR branch)
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-publish gate — confirm Release Please stale branch and run final Three Zeros with --warnings-as-errors</name>
  <files><!-- verification only — no files created or modified --></files>
  <read_first>
    Read RELEASING.md §"Routine linked releases" steps 1-2 and §"Release Please + Hex (linked automation)" for the trigger mechanism. Key checks before triggering:
    - Check for stale Release Please branch: `git branch -r | grep release-please` — if a stale `release-please--branches--main` branch exists from prior RP runs, see RELEASING.md step 3 (reset to main or run repair_linked_release_pr.sh).
    - The @since fix commits from Plan 01 must be on main (merged) before this gate — confirm with `git log --oneline -5`.
    - Re-run mix compile --warnings-as-errors in both accrue and accrue_admin to confirm Plan 01's fixes are on main and the compiler warning is gone.
    - Confirm `command -v jq` and `command -v gh` for the scripts that require them.
    - Run verify_release_manifest_alignment.sh and verify_release_contract.sh one final time to confirm current-state green.
    - If Release Please might compute a version other than 1.3.0 (e.g., due to fewer feat: commits), prepare a `Release-As: 1.3.0` commit trailer per RESEARCH.md Assumption A2 fallback and RELEASING.md appendix.
  </read_first>
  <action>
    Execute pre-publish checks in this order:

    1. Confirm Plan 01 and Plan 02 SUMMARYs both report green (read them if not in context).

    2. Run final --warnings-as-errors compile in both packages:
       - `cd accrue && mix compile --warnings-as-errors` — must exit 0 (no @since warning)
       - `cd accrue_admin && mix compile --warnings-as-errors` — must exit 0

    3. Check for stale Release Please branch: `git branch -r | grep release-please`
       - If found: read RELEASING.md step 3 for reset instructions. Run `bash scripts/ci/repair_linked_release_pr.sh --version 1.3.0` if a stale PR exists.
       - If not found: proceed.

    4. Run the release-specific verification scripts one final time:
       - `bash scripts/ci/verify_release_contract.sh`
       - `bash scripts/ci/verify_release_manifest_alignment.sh`
       - Both must exit 0.

    5. Confirm Release Please is triggered by push to main (it runs automatically on push per ci.yml). The @since fix commit(s) from Plan 01 should already have triggered a RP run. Check GitHub Actions for a running/pending RP job: `gh run list --workflow=release-please.yml --limit 3`.

    6. If RP has not yet opened a combined PR (e.g., no conventional feat: commits found since last tag), add a `Release-As: 1.3.0` trailer to an empty commit:
       `git commit --allow-empty -m "chore: trigger Release Please for 1.3.0\n\nRelease-As: 1.3.0"` and push to main.

    7. Wait for Release Please to open the combined PR. Confirm it exists: `gh pr list --label "autorelease: pending"`.

    Document all outcomes. If the gate is green and the RP PR exists, proceed to Task 3.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue && mix compile --warnings-as-errors 2>&1 | grep -E 'warning|error' | grep -v '^$' || echo "CLEAN"
      cd /Users/jon/projects/accrue/accrue_admin && mix compile --warnings-as-errors 2>&1 | grep -E 'warning|error' | grep -v '^$' || echo "CLEAN"
      bash /Users/jon/projects/accrue/scripts/ci/verify_release_contract.sh
      bash /Users/jon/projects/accrue/scripts/ci/verify_release_manifest_alignment.sh
      gh run list --workflow=release-please.yml --limit 5 2>/dev/null || echo "gh CLI not available or workflow not found"
    </automated>
  </verify>
  <done>
    - mix compile --warnings-as-errors exits 0 in both accrue and accrue_admin
    - verify_release_contract.sh and verify_release_manifest_alignment.sh exit 0
    - A Release Please PR exists with label "autorelease: pending" (or is in progress)
    - No stale RP branch blocking a fresh PR
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Claude has:
    1. Fixed all 8 @since annotations across dunning.ex and funnel_chart.ex (Plan 01)
    2. Run the Three Zeros gate green — mix test/dialyzer/credo/coveralls + all verify_*.sh scripts (Plan 02)
    3. Added ### 1.3.0 entries to accrue/guides/release-notes.md in both package sections (Task 1 above)
    4. Run the pre-publish gate: --warnings-as-errors clean, stale branch check, final contract scripts (Task 2 above)
    5. Confirmed a Release Please combined PR is open or triggered
  </what-built>
  <how-to-verify>
    Review the Release Please PR on GitHub:

    1. Open the PR listed by `gh pr list --label "autorelease: pending"` (or check GitHub Actions → release-please.yml run).

    2. Confirm the PR diff includes ALL of the following:
       - accrue/mix.exs: `@version "1.2.0"` → `@version "1.3.0"`
       - accrue_admin/mix.exs: `@version "1.2.0"` → `@version "1.3.0"`
       - accrue_portal/mix.exs: `@version "1.2.0"` → `@version "1.3.0"`
       - .release-please-manifest.json: all three values `"1.2.0"` → `"1.3.0"`
       - accrue/CHANGELOG.md: new `## [1.3.0]` section generated by Release Please
       - accrue_admin/CHANGELOG.md: new `## [1.3.0]` section generated by Release Please
       - accrue_portal/CHANGELOG.md: new `## [1.3.0]` section generated by Release Please

    3. Run the PR-scope check: `bash scripts/ci/verify_release_pr_scope.sh --pr <PR_NUMBER> --version 1.3.0`
       (Replace <PR_NUMBER> with the actual PR number from step 1.)

    4. On the Release Please PR branch:
       - Reconcile accrue/CHANGELOG.md: the ## Unreleased prose must be merged into the ## [1.3.0] section or the ## Unreleased header must be removed/retitled so no overlapping prose ships. Per RELEASING.md §"Changelog ship boundary".
       - Same for accrue_admin/CHANGELOG.md.
       - accrue_portal/CHANGELOG.md has no ## Unreleased section — no action needed.

    5. Verify CI is green on the PR (all required checks passing). Per RELEASING.md, the required deterministic gate includes: package verification, host integration, generated drift/docs drift, security/trust artifact, seeded performance smoke, compatibility floor/target checks, browser accessibility/responsive checks.

    6. Confirm HEX_API_KEY and RELEASE_PLEASE_TOKEN exist as GitHub Actions secrets (Settings → Secrets → Actions).

    7. Check that accrue/guides/release-notes.md on the PR branch has ### 1.3.0 in both sections (added in Task 1).

    8. Run verify_release_notes_contract.sh after the PR is checked out locally to confirm it passes against @version 1.3.0:
       `bash scripts/ci/verify_release_notes_contract.sh`

    When all checks above are satisfactory, merge the PR manually on GitHub (or run `bash scripts/ci/gh_merge_release_pr.sh` to queue auto-merge via workflow_dispatch).
  </how-to-verify>
  <resume-signal>
    After merging the Release Please PR, type "merged" here. If CI failed on the PR, describe the failure. If the version computed was not 1.3.0, describe what RP computed.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Release Please PR has been merged. The release-please.yml workflow should now be running to:
    1. Publish accrue to Hex.pm (accrue-v1.3.0)
    2. Publish accrue_admin to Hex.pm (accrue_admin-v1.3.0)
    3. Publish accrue_portal to Hex.pm (accrue_portal-v1.3.0)
    4. Create GitHub releases for all three packages
    5. Push git tags: accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0
  </what-built>
  <how-to-verify>
    1. Monitor the release-please.yml workflow run: `gh run watch --repo $(gh repo view --json nameWithOwner -q .nameWithOwner)` or check GitHub Actions tab.

    2. After the workflow completes, confirm all three packages are live on Hex.pm:
       - https://hex.pm/packages/accrue (version 1.3.0 listed)
       - https://hex.pm/packages/accrue_admin (version 1.3.0 listed)
       - https://hex.pm/packages/accrue_portal (version 1.3.0 listed)

    3. Confirm all three git tags exist: `git fetch --tags && git tag | grep 1.3.0`
       Expected output includes: accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0

    4. Confirm HexDocs are building (allow ~5-10 min for Hex.pm to generate docs). Check:
       - https://hexdocs.pm/accrue/1.3.0
       - https://hexdocs.pm/accrue_admin/1.3.0
       - https://hexdocs.pm/accrue_portal/1.3.0

    5. On the HexDocs page for accrue 1.3.0, check one of the fixed functions (e.g., Accrue.Analytics.Dunning.recovered_vs_lost_mrr/1) to confirm it shows "(since 1.3.0)" badge and no literal "@since" junk text.

    6. If any package's publish step failed (partial publish), consult RELEASING.md §"Partial Hex publish recovery" for the corrective path (retry, retire, or forward-fix).

    7. Run final post-publish verification from RELEASING.md checklist:
       - `bash scripts/ci/verify_release_manifest_alignment.sh` (should now pass with all versions at 1.3.0)
       - `bash scripts/ci/verify_release_notes_contract.sh` (should pass with ### 1.3.0 present)
  </how-to-verify>
  <resume-signal>
    After all three packages are confirmed live on Hex.pm and all three tags exist, type "published: 1.3.0" here. If any step failed, describe which package failed and what error the release-please.yml workflow reported.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Hex.pm | mix hex.publish transmits package tarball; credentials come from GitHub Actions secrets only |
| GitHub Actions → GitHub API | Release Please creates tags, PRs, GitHub Releases; RELEASE_PLEASE_TOKEN scoped to repo only |
| Developer → GitHub | PR review and merge is human-gate; no automated merge without human approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Information Disclosure | Hex API key / RELEASE_PLEASE_TOKEN | mitigate | HEX_API_KEY and RELEASE_PLEASE_TOKEN exist ONLY as GitHub Actions secrets (Settings → Secrets → Actions). Never echoed in workflow logs (use `--yes` flag on mix hex.publish, not interactive). Never committed to any file. Never appear in PR descriptions, commit messages, or CHANGELOG content. Validate with RELEASING.md §"Minimum secret setup". |
| T-152-02 | Tampering | release-please-config.json, .release-please-manifest.json | mitigate | Never manually edit @version in any mix.exs or the manifest. All version bumps go through Release Please PR review (human gate: Task 3 checkpoint). The PR diff is reviewed before merge. |
| T-152-03 | Tampering | accrue/guides/release-notes.md | accept | Release notes are documentation; no security-sensitive content. Worst case: incorrect version summary. Reviewer catches in PR review. |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. Package legitimacy gate not applicable. |
</threat_model>

<verification>
Release is complete and v1.46 milestone is closed when:
- accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0 are live on Hex.pm
- Git tags accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0 exist on the remote
- HexDocs for all three packages show version 1.3.0
- Accrue.Analytics.Dunning functions show "(since 1.3.0)" badge in HexDocs (D-02 visual confirmation)
- .release-please-manifest.json shows all three packages at "1.3.0" (post-merge)
- verify_release_manifest_alignment.sh exits 0 post-publish
- verify_release_notes_contract.sh exits 0 post-publish
- No CHANGELOG.md ships with both ## Unreleased and ## [1.3.0] containing overlapping content
</verification>

<success_criteria>
- D-01: Linked 1.3.0 release published on Hex.pm for all three packages
- D-04: Release Please pipeline used end-to-end (no manual @version edits, no manual tag creation)
- D-02: Visual confirmation: HexDocs shows "(since 1.3.0)" badge, no literal @since junk text
- D-03: Three Zeros gate confirmed green in Plan 02 SUMMARY before this plan executed
- All three git tags exist: accrue-v1.3.0, accrue_admin-v1.3.0, accrue_portal-v1.3.0
- v1.46 milestone marked complete in STATE.md
</success_criteria>

<output>
Create `.planning/phases/152-close-v1-46-closure-gaps-since-warnings-verification-hex-pub/152-03-SUMMARY.md` when done.
Update `.planning/STATE.md` to record milestone v1.46 as shipped.
</output>

---
phase: 154-advisory-cache-core-correctness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
  - accrue/test/accrue/webhook/wr05_concurrency_test.exs
  - accrue/lib/accrue/billing/entitlement_summary.ex
  - accrue/lib/accrue/webhook/default_handler.ex
autonomous: true
requirements: [ADV-01, ADV-02, ADV-03, ADV-04, POL-01, POL-02]

must_haves:
  truths:
    - "D-01: EntitlementSummary.force_changeset/2 does not call optimistic_lock(:lock_version) and lock_version is absent from @cast_fields, so the DB-level on_conflict_where is the sole concurrency guard"
    - "D-01, D-06: Two concurrent webhook workers delivering entitlement events for the same customer complete without error (no Ecto.StaleEntryError), and the row reflects the newer event's last_stripe_event_ts — proven by the upgraded wr05_concurrency_test with Sandbox.allow/3"
    - "D-03: A webhook worker delivering an entitlement event with no prior last_stripe_event_ts in the DB updates the row (does not silently no-op) — the NULL-safe on_conflict_where condition allows the write when EXCLUDED.last_stripe_event_ts IS NULL"
    - "D-04, D-05: A DB-level stale write returns {:ok, :stale}, emits result: :unchanged to [:accrue, :entitlements, :summary_synced], and does not write a ledger event — upsert_entitlement_summary/2 rescues Ecto.StaleEntryError; write_entitlement_summary/9 handles the stale branch before maybe_record_summary_event/3"
    - "D-07, D-08: A non-Stripe processor's entitlement summary row shows the correct :processor value from the event arg (to_string(processor)), not the global config default from processor_name() — write_entitlement_summary/9 takes processor as 9th arg and all call sites in reduce_entitlement_summary_for_customer pass it through"
    - "D-09: A follow-up event that omits the livemode key carries forward the prior row's livemode rather than overwriting with nil — the nil-guard mirrors the stamp_summary_watermark/4 pattern"
  artifacts:
    - path: "accrue/lib/accrue/billing/entitlement_summary.ex"
      provides: "EntitlementSummary schema without optimistic_lock or lock_version in @cast_fields"
      contains: "force_changeset"
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "write_entitlement_summary/9 with processor arg, livemode nil-guard, stale branch + upsert with NULL-safe on_conflict_where"
      exports: ["write_entitlement_summary", "upsert_entitlement_summary"]
    - path: "accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs"
      provides: "Tests for ADV-02 nil-ts, ADV-03 DB-level stale, POL-01 processor, POL-02 livemode carry-forward"
      contains: "nil last_stripe_event_ts"
    - path: "accrue/test/accrue/webhook/wr05_concurrency_test.exs"
      provides: "Concurrent delivery test with explicit Sandbox.allow/3"
      contains: "Sandbox.allow"
  key_links:
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "accrue/lib/accrue/billing/entitlement_summary.ex"
      via: "EntitlementSummary.force_changeset/2 called in upsert_entitlement_summary/2"
      pattern: "EntitlementSummary.force_changeset"
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "PostgreSQL ON CONFLICT DO UPDATE WHERE"
      via: "Repo.insert on_conflict_where with EXCLUDED pseudo-table fragment"
      pattern: "on_conflict_where"
    - from: "reduce_entitlement_summary_for_customer/7"
      to: "write_entitlement_summary/9"
      via: "processor arg threaded through call site"
      pattern: "write_entitlement_summary.*processor"
---

<objective>
Fix three latent correctness bugs in the advisory entitlement cache write path and two field-accuracy gaps, validated by failing tests written first (TDD).

Purpose: The advisory cache is silently incorrect under concurrent delivery and first-ever writes. This phase makes the write path provably correct — no Ecto.StaleEntryError, no silent upsert suppression, accurate processor and livemode fields on every row.

Output:
- Four new failing tests (RED) covering ADV-02, ADV-03, POL-01, POL-02 in default_handler_entitlement_summary_test.exs
- ADV-04 concurrent test upgraded in wr05_concurrency_test.exs with explicit Sandbox.allow/3 (currently RED because it passes by accident, not by design)
- Production fixes in entitlement_summary.ex (ADV-01) and default_handler.ex (ADV-02, ADV-03, POL-01, POL-02) that turn all tests GREEN
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/154-advisory-cache-core-correctness/154-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/154-advisory-cache-core-correctness/154-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/154-advisory-cache-core-correctness/154-PATTERNS.md

<interfaces>
From accrue/lib/accrue/billing/entitlement_summary.ex (lines 55–89 — current buggy state):

  @cast_fields ~w[
    processor customer_id stripe_customer_id livemode entitlement_count
    truncated data synced_at lock_version          # <-- lock_version IS the bug (ADV-01)
    last_stripe_event_ts last_stripe_event_id
  ]a

  def force_changeset(summary_or_changeset, attrs \\ %{}) do
    summary_or_changeset
    |> cast(attrs, @cast_fields)
    |> optimistic_lock(:lock_version)              # <-- REMOVE this line (ADV-01)
    |> unique_constraint(:customer_id)
    |> foreign_key_constraint(:customer_id)
  end

  # field(:lock_version, :integer, default: 1)   # <-- stays in schema (D-02, no migration)

From accrue/lib/accrue/webhook/default_handler.ex:

  # Current function head (line 579) — 8 args (pre-fix):
  defp write_entitlement_summary(evt_id, evt_ts, obj, cus_id, customer, row, entitlements, data)

  # Current attrs construction bugs (lines 595–596):
  processor: processor_name(),           # bug: always global config (POL-01)
  livemode: get(obj, :livemode),         # bug: nil when key absent (POL-02)

  # Current span structure (lines 610–629 — pre-fix):
  Accrue.Telemetry.span([:accrue, :entitlements, :sync], metadata, fn ->
    with {:ok, saved} <- upsert_entitlement_summary(row, attrs),
         {:ok, _} <- maybe_record_summary_event(material?, saved, evt_id) do
      :telemetry.execute([:accrue, :entitlements, :summary_synced], ...)
      {:ok, saved}
    end
  end)

  # Current upsert (lines 669–687 — pre-fix):
  defp upsert_entitlement_summary(_row, attrs) do
    import Ecto.Query
    %EntitlementSummary{}
    |> EntitlementSummary.force_changeset(attrs)
    |> Repo.insert(
      returning: true,
      conflict_target: :customer_id,
      on_conflict: {:replace_all_except, [:id, :inserted_at, :customer_id]},
      on_conflict_where:
        from(e in EntitlementSummary,
          where: e.last_stripe_event_ts < fragment("EXCLUDED.last_stripe_event_ts")
          # bug: NULL EXCLUDED.last_stripe_event_ts → comparison returns NULL → no-op (ADV-02)
        )
    )
    # bug: no rescue — Ecto.StaleEntryError propagates (ADV-03)
  end

  # Call site (lines 551–562) — currently passes 8 args (needs 9 after POL-01):
  write_entitlement_summary(evt_id, evt_ts, obj, cus_id, customer, row, entitlements, data)

  # Watermark carry-forward pattern to mirror for POL-02 (lines 700–710):
  defp stamp_summary_watermark(attrs, %DateTime{} = evt_ts, evt_id, _row),
    do: stamp_watermark(attrs, evt_ts, evt_id)
  defp stamp_summary_watermark(attrs, _evt_ts, _evt_id, nil), do: attrs
  defp stamp_summary_watermark(attrs, _evt_ts, _evt_id, %EntitlementSummary{} = row) do
    Map.merge(attrs, %{last_stripe_event_ts: row.last_stripe_event_ts,
                       last_stripe_event_id: row.last_stripe_event_id})
  end

  # Existing stale return convention (line 549 — pre-DB gate):
  {:ok, :stale}

  # processor_name/0 (lines 1716–1722 — the global config to stop using in write path):
  defp processor_name do
    case Processor.__impl__() do
      Accrue.Processor.Fake   -> "fake"
      Accrue.Processor.Stripe -> "stripe"
      other -> other |> Module.split() |> List.last() |> String.downcase()
    end
  end

From accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs (line 1–40):
  defmodule Accrue.Webhook.DefaultHandlerEntitlementSummaryTest do
    use Accrue.BillingCase, async: false    # shared: true sandbox; prerequisite for Sandbox.allow/3
    alias Accrue.Billing.EntitlementSummary

From accrue/test/accrue/webhook/wr05_concurrency_test.exs (lines 1–30):
  defmodule Accrue.Webhook.WR05ConcurrencyTest do
    use Accrue.RepoCase, async: false       # shared sandbox mode
    alias Accrue.Billing.{Customer, EntitlementSummary}
    alias Accrue.Webhook.DefaultHandler
    @cus_processor_id "cus_wr05_race"
    # Current concurrent tasks (lines 71–76) — lacks Sandbox.allow/3:
    tasks = [
      Task.async(fn -> DefaultHandler.handle(event1) end),
      Task.async(fn -> DefaultHandler.handle(event2) end)
    ]
    results = Enum.map(tasks, &Task.await/1)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for ADV-02, ADV-03, POL-01, POL-02 and upgrade ADV-04</name>
  <files>
    accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs,
    accrue/test/accrue/webhook/wr05_concurrency_test.exs
  </files>
  <read_first>
    - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs — read the entire file to see existing test setup, BillingCase imports, existing stale-skip telemetry test (lines 145–185) for the telemetry attach/assert_received pattern, and customer setup in the setup block
    - accrue/test/accrue/webhook/wr05_concurrency_test.exs — read the entire file to see the existing concurrent test (lines 60–90) and the make_summary_event/3 helper, specifically what fields it sets and where it puts the timestamp
    - accrue/test/support/billing_case.ex — confirm sandbox: not tags[:async] behavior and which aliases/imports BillingCase provides
    - accrue/test/support/stripe_fixtures.ex — locate entitlement_summary_event/2 to understand what options it accepts for building fixture events (particularly how to set the created timestamp and processor_id)
  </read_first>
  <behavior>
    ADV-02 test — "nil last_stripe_event_ts event updates the row (not a silent no-op)":
      - Seed an EntitlementSummary row with no last_stripe_event_ts (nil watermark, first-ever state)
      - Deliver a fixture event where the Stripe event has no `created` timestamp (nil evt_ts path)
      - Assert result is {:ok, %EntitlementSummary{}} — not {:ok, :stale}, not {:ok, :ignored}
      - This test MUST FAIL before the ADV-02 fix (current on_conflict_where returns nil on NULL EXCLUDED, silently no-oping)

    ADV-03 test — "DB-level stale skip emits result: :unchanged, no ledger event":
      - Attach telemetry handler for [:accrue, :entitlements, :summary_synced]
      - Seed a row with last_stripe_event_ts = ts_newer
      - Deliver a fixture event with ts_older (older than the row — this would pass check_stale if both arrive concurrently, but after the fix the DB-level guard catches it)
      - To simulate DB-level stale (bypassing check_stale pre-gate): seed the row AFTER the pre-DB check would have passed, OR use equal timestamps (check_stale passes :eq, DB guard catches stale)
      - Alternatively, directly call upsert_entitlement_summary in a unit test context to verify the rescue
      - Assert the handler returns {:ok, :stale}
      - Assert telemetry message received: result: :unchanged in the metadata
      - Assert no accrue_events ledger row was written for this evt_id
      - This test MUST FAIL before the ADV-03 fix (current path propagates Ecto.StaleEntryError or never returns {:ok, :stale})

    POL-01 test — "processor field reflects event processor, not global config":
      - BillingCase wires Fake processor — processor_name() returns "fake" in test env
      - Deliver a StripeFixtures.entitlement_summary_event with customer processor_id "cus_fake_ent_summary" (already set up in existing test setup)
      - Assert {:ok, %EntitlementSummary{processor: "stripe"}} — the Stripe event's processor should be "stripe", not "fake"
      - This test MUST FAIL before the POL-01 fix (current code uses processor_name() which returns "fake" in BillingCase)

    POL-02 test — "follow-up event with absent livemode key carries forward prior row livemode":
      - Seed a row with livemode: true and a recent last_stripe_event_ts
      - Deliver a second fixture event with a newer timestamp but with the livemode key absent from the event object (not nil — key not present at all)
      - Assert {:ok, %EntitlementSummary{livemode: true}} — prior livemode preserved
      - This test MUST FAIL before the POL-02 fix (current code: livemode: get(obj, :livemode) returns nil when key absent, overwrites prior value)

    ADV-04 upgrade in wr05_concurrency_test.exs — add explicit Sandbox.allow/3:
      - Inside each Task.async lambda, call Ecto.Adapters.SQL.Sandbox.allow(Accrue.TestRepo, parent, self()) where parent = self() is captured before Task.async
      - Both tasks must succeed (no StaleEntryError) and the newer timestamp must win
      - The test already passes (shared sandbox mode works) but must be upgraded to use explicit allow per D-06
      - This test stays GREEN before and after (the upgrade is about explicitness, not fixing a failure)
  </behavior>
  <action>
    In default_handler_entitlement_summary_test.exs, append four new test blocks after the existing tests. Do NOT modify existing tests.

    ADV-02 test: Seed an EntitlementSummary with customer_id, stripe_customer_id, entitlement_count set but last_stripe_event_ts deliberately absent (not in the attrs map). Then build a fixture event where the `created` field in the Stripe event data is absent or nil (use StripeFixtures and strip/override the created field). Call DefaultHandler.handle/1. Assert result matches {:ok, %EntitlementSummary{}}.

    ADV-03 test: Attach a telemetry listener for [:accrue, :entitlements, :summary_synced] using :telemetry.attach/4 with a unique ref string (use System.unique_integer). Seed a row with last_stripe_event_ts ts_newer. Deliver an event with ts_newer (same timestamp — check_stale returns :ok for equal timestamps, so it reaches the DB gate). Use two sequential deliveries of the same event_id to force the DB-level conflict branch to fire — the second delivery hits on_conflict_where, which rejects because the existing row's ts is NOT strictly less than EXCLUDED (they are equal). Assert the second delivery returns {:ok, :stale}. Assert a telemetry message was received with result: :unchanged. Assert Repo.aggregate(Accrue.Events.Event, :count) does not increase for the second evt_id.

    POL-01 test: Use the existing customer setup (processor_id: "cus_fake_ent_summary", processor: "stripe"). Build a StripeFixtures.entitlement_summary_event with that customer. Call DefaultHandler.handle/1. Assert the returned EntitlementSummary has processor: "stripe" (not "fake").

    POL-02 test: Insert an EntitlementSummary row with livemode: true and last_stripe_event_ts set to a known timestamp ts_first. Build a second fixture event with a newer timestamp ts_second. Modify the event's object payload so the top-level :livemode key is absent (use Map.delete or build the fixture without it). Call DefaultHandler.handle/1 with the second event. Assert the returned EntitlementSummary has livemode: true (carried forward, not nil).

    In wr05_concurrency_test.exs, capture parent = self() before the Task.async calls. Inside each Task.async lambda, add as the first expression: Ecto.Adapters.SQL.Sandbox.allow(Accrue.TestRepo, parent, self()). Change Enum.map(tasks, &Task.await/1) to Task.await_many(tasks) per D-06 pattern.

    Run the test file after writing: `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs --seed 0 2>&1 | tail -20`

    Confirm ADV-02, ADV-03, POL-01, POL-02 tests are RED (failing). Confirm ADV-04 concurrent test remains GREEN or turns GREEN after upgrade.

    Commit: `test(154-01): add failing tests for ADV-02 ADV-03 POL-01 POL-02 + upgrade ADV-04 Sandbox.allow`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs --seed 0 2>&1 | grep -E "(failures|errors|Finished)"</automated>
  </verify>
  <done>
    - 4 new tests exist in default_handler_entitlement_summary_test.exs for ADV-02, ADV-03, POL-01, POL-02
    - All 4 new tests fail (RED) with meaningful assertion errors, not compile errors
    - wr05_concurrency_test.exs contains Sandbox.allow(Accrue.TestRepo, parent, self()) inside each Task.async lambda
    - Existing tests in default_handler_entitlement_summary_test.exs still pass
    - mix compile exits 0 for the accrue package
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Apply all production fixes — ADV-01 OCC removal, ADV-02 NULL watermark, ADV-03 stale path, POL-01 processor arg, POL-02 livemode carry-forward</name>
  <files>
    accrue/lib/accrue/billing/entitlement_summary.ex,
    accrue/lib/accrue/webhook/default_handler.ex
  </files>
  <read_first>
    - accrue/lib/accrue/billing/entitlement_summary.ex — read the entire file; focus on @cast_fields (lines 68–72) and force_changeset/2 (lines 81–88); confirm lock_version is in both @cast_fields and the optimistic_lock call before making changes
    - accrue/lib/accrue/webhook/default_handler.ex lines 500–700 — read write_entitlement_summary/8 (line 579), attrs construction (lines 591–602), Telemetry.span block (lines 610–629), and upsert_entitlement_summary/2 (lines 669–687); confirm exact current state before editing
    - accrue/lib/accrue/webhook/default_handler.ex lines 700–730 — read stamp_summary_watermark/4 to confirm the nil-guard carry-forward pattern to mirror for POL-02
    - accrue/lib/accrue/webhook/default_handler.ex lines 1677–1686 — read check_stale/2 to confirm its interface (not being changed)
    - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs — run test file BEFORE editing production code to confirm RED state
  </read_first>
  <behavior>
    ADV-01: After removing lock_version from @cast_fields and removing |> optimistic_lock(:lock_version) from force_changeset/2, the existing serial write tests must still pass. The lock_version field declaration in the schema block stays.

    ADV-02 + ADV-03 (upsert_entitlement_summary/2): After the fix, upsert with nil last_stripe_event_ts in attrs allows the update (NULL-safe condition true). When on_conflict_where rejects (condition false → 0 rows updated → Ecto.StaleEntryError), the function returns {:ok, :stale}.

    ADV-03 + stale branch in write_entitlement_summary/9: When upsert returns {:ok, :stale}, the span block emits [:accrue, :entitlements, :summary_synced] with result: :unchanged in metadata and returns {:ok, :stale} without calling maybe_record_summary_event/3.

    POL-01: After adding processor as 9th arg to write_entitlement_summary/9 and changing attrs to processor: to_string(processor), the row's processor field reflects the event's processor atom, not the global config.

    POL-02: After adding the livemode nil-guard in attrs construction, a nil result from get(obj, :livemode) carries forward row.livemode (when row is non-nil), not nil.

    All six requirements must be GREEN after this task. Full suite (mix test) must exit 0.
  </behavior>
  <action>
    Apply changes in this order (each is a targeted edit, not a full-file rewrite):

    1. entitlement_summary.ex — ADV-01:
       Remove lock_version from the @cast_fields atom list (line 70). Remove the |> optimistic_lock(:lock_version) pipe (line 85). Leave the field(:lock_version, :integer, default: 1) declaration untouched (per D-02).

    2. default_handler.ex — POL-01: Add processor as 9th argument to write_entitlement_summary. Change the function head at line 579 from:
         defp write_entitlement_summary(evt_id, evt_ts, obj, cus_id, customer, row, entitlements, data)
       to:
         defp write_entitlement_summary(evt_id, evt_ts, obj, cus_id, customer, row, entitlements, data, processor)

       Change the attrs construction from processor: processor_name() to processor: to_string(processor).

       Update the call site in reduce_entitlement_summary_for_customer/7 (lines 551–562) to pass processor as the 9th argument at the end of the write_entitlement_summary call.

    3. default_handler.ex — POL-02: In the attrs construction map inside write_entitlement_summary/9, replace:
         livemode: get(obj, :livemode)
       with a case expression that returns row.livemode when get(obj, :livemode) returns nil and row is a non-nil EntitlementSummary struct, and returns the incoming value otherwise. Mirror the stamp_summary_watermark/4 pattern: nil incoming + non-nil row → carry forward; nil row → nil; non-nil incoming → use incoming.

    4. default_handler.ex — ADV-02 + ADV-03 in upsert_entitlement_summary/2: Change the on_conflict_where from:
         where: e.last_stripe_event_ts < fragment("EXCLUDED.last_stripe_event_ts")
       to:
         where: fragment("EXCLUDED.last_stripe_event_ts IS NULL") or
                  e.last_stripe_event_ts < fragment("EXCLUDED.last_stripe_event_ts")

       Add a rescue block after the Repo.insert call:
         rescue
           Ecto.StaleEntryError -> {:ok, :stale}

    5. default_handler.ex — ADV-03 in write_entitlement_summary/9: Change the Telemetry.span block from using with to using case. The {:ok, :stale} branch: emit [:accrue, :entitlements, :summary_synced] with result: :unchanged in metadata, return {:ok, :stale} without calling maybe_record_summary_event/3. The {:ok, saved} branch: the existing with chain calling maybe_record_summary_event. The error branch: propagate.

    After all edits, run: `cd /Users/jon/projects/accrue/accrue && mix compile --warnings-as-errors 2>&1 | tail -20`

    Then run: `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/webhook/wr05_concurrency_test.exs --seed 0 2>&1 | tail -30`

    All tests must be GREEN. Then run the full suite: `cd /Users/jon/projects/accrue/accrue && mix test --seed 0 2>&1 | tail -30`

    Commit: `fix(154-01): correct advisory cache write path — remove OCC, fix NULL watermark, add stale branch, thread processor arg, carry livemode forward`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/webhook/wr05_concurrency_test.exs --seed 0 2>&1 | grep -E "(0 failures|failures|errors)"</automated>
  </verify>
  <done>
    - mix compile --warnings-as-errors exits 0
    - mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs exits 0 with 0 failures
    - mix test test/accrue/webhook/wr05_concurrency_test.exs exits 0 with 0 failures
    - mix test (full accrue/ suite) exits 0 with 0 failures (ignoring pre-existing PdfTest flakiness with --seed 0)
    - entitlement_summary.ex does not contain lock_version in @cast_fields
    - entitlement_summary.ex does not contain optimistic_lock call in force_changeset/2
    - default_handler.ex write_entitlement_summary/9 has processor as 9th arg
    - default_handler.ex upsert_entitlement_summary/2 contains fragment("EXCLUDED.last_stripe_event_ts IS NULL")
    - default_handler.ex upsert_entitlement_summary/2 contains rescue Ecto.StaleEntryError
    - default_handler.ex write_entitlement_summary/9 handles {:ok, :stale} with result: :unchanged telemetry before any maybe_record_summary_event call
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oban Worker → Repo | Webhook workers call write path with Stripe-sourced event data; the write path is the trust boundary for DB writes |
| Stripe event payload → entitlement_summary attrs | get(obj, :livemode) and get(obj, :customer) are the entry points for untrusted payload data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01 | Tampering | EntitlementSummary row via concurrent upsert | mitigate | DB-level ON CONFLICT DO UPDATE WHERE provides atomic row-level serialization; removes OCC which was incompatible with this path (ADV-01 + ADV-02) |
| T-154-02 | Tampering | processor field via global config pollution | mitigate | POL-01: write_entitlement_summary/9 uses to_string(processor) from the event arg, not processor_name() which is global |
| T-154-03 | Information Disclosure | livemode nil overwrite exposes incorrect billing mode | mitigate | POL-02: carry-forward nil-guard prevents nil from overwriting a known livemode value |
| T-154-04 | Denial of Service | Ecto.StaleEntryError propagating from upsert and crashing Oban worker | mitigate | ADV-03: rescue Ecto.StaleEntryError in upsert_entitlement_summary/2 and return {:ok, :stale}; worker completes normally |
| T-154-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this phase — all changes are code-only in the existing Elixir/Ecto/PostgreSQL stack |

No new public API surface is introduced in this phase. write_entitlement_summary/9 is a private defp. No new input surface, no new authentication surface, no new data exposure.
</threat_model>

<verification>
Phase-level verification sequence (executor runs these in order):

1. `cd /Users/jon/projects/accrue/accrue && mix compile --warnings-as-errors` — must exit 0
2. `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs --seed 0` — must exit 0, 0 failures
3. `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/wr05_concurrency_test.exs --seed 0` — must exit 0, 0 failures
4. `cd /Users/jon/projects/accrue/accrue && mix test --seed 0` — full suite must exit 0 (PdfTest flakiness is pre-existing; use --seed 0 to suppress)

Source assertions (grep-verifiable after Task 2):
- `grep -v '^#' accrue/lib/accrue/billing/entitlement_summary.ex | grep -c lock_version` == 1 (schema field only, not in @cast_fields)
- `grep -c 'optimistic_lock' accrue/lib/accrue/billing/entitlement_summary.ex` == 0
- `grep -c 'EXCLUDED.last_stripe_event_ts IS NULL' accrue/lib/accrue/webhook/default_handler.ex` >= 1
- `grep -c 'StaleEntryError' accrue/lib/accrue/webhook/default_handler.ex` >= 1
- `grep -c 'Sandbox.allow' accrue/test/accrue/webhook/wr05_concurrency_test.exs` >= 2
- `grep -c 'to_string(processor)' accrue/lib/accrue/webhook/default_handler.ex` >= 1
</verification>

<success_criteria>
Phase 154 is complete when:
1. Two concurrent Task.async workers delivering entitlement events for the same customer both return {:ok, _} (no Ecto.StaleEntryError), and the DB row's last_stripe_event_ts equals the newer timestamp — proven by the upgraded wr05_concurrency_test with Sandbox.allow/3
2. A fixture event with nil last_stripe_event_ts (no created field) updates the row and returns {:ok, %EntitlementSummary{}} — proven by ADV-02 test in default_handler_entitlement_summary_test.exs
3. A DB-level stale write returns {:ok, :stale} and emits result: :unchanged to [:accrue, :entitlements, :summary_synced] with no ledger event — proven by ADV-03 test
4. The processor field in an EntitlementSummary row reflects the event's processor atom (e.g., "stripe"), not the global config default ("fake" in BillingCase) — proven by POL-01 test
5. A follow-up event with the livemode key absent carries forward the prior row's livemode — proven by POL-02 test
6. mix compile --warnings-as-errors exits 0
7. Full mix test suite exits 0
</success_criteria>

<output>
Create /Users/jon/projects/accrue/.planning/phases/154-advisory-cache-core-correctness/154-01-SUMMARY.md when done.
</output>

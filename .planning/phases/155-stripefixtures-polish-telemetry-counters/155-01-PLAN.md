---
phase: 155-stripefixtures-polish-telemetry-counters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/test/support/stripe_fixtures.ex
  - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs
  - accrue/lib/accrue/telemetry/metrics.ex
  - accrue/test/accrue/telemetry/metrics_test.exs
autonomous: true
requirements: [POL-03, POL-04]

must_haves:
  truths:
    - "D-01, D-02, D-11: A test author can call `StripeFixtures.entitlement_summary_event(omit_livemode: true, ...)` and receive a fixture whose summary object omits the `livemode` key, and the existing POL-02 reducer regression proves that path directly."
    - "D-05, D-06: `Accrue.Test.StripeFixtures` clearly states it is test support under `test/support`, not part of the published Hex package, and not part of Accrue's runtime/public support contract."
    - "D-07, D-08, D-12: `Accrue.Telemetry.Metrics.defaults/0` exposes counters for `[:accrue, :webhooks, :malformed_entitlement_summary]` and `[:accrue, :webhooks, :orphan_entitlement_summary]`, so host apps can wire them without custom metric definitions."
    - "D-09, D-10: The malformed-summary default metric keeps tags bounded to `[:reason]` only, the orphan-summary default metric adds no new tags, and the ops-only parity inventory remains unchanged."
  artifacts:
    - path: "accrue/test/support/stripe_fixtures.ex"
      provides: "Test-only Stripe entitlement summary fixture with `omit_livemode: true` option and clarified support boundary"
      contains: "entitlement_summary_event"
    - path: "accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs"
      provides: "Carry-forward regression that uses `omit_livemode: true` and asserts the key is absent before handling"
      contains: "preserves existing livemode: true"
    - path: "accrue/lib/accrue/telemetry/metrics.ex"
      provides: "Default metrics for malformed and orphan entitlement summary webhook events"
      contains: "accrue.webhooks.malformed_entitlement_summary.count"
    - path: "accrue/test/accrue/telemetry/metrics_test.exs"
      provides: "Tuple-based defaults assertions for malformed/orphan entitlement summary metrics"
      contains: "malformed_entitlement_summary"
  key_links:
    - from: "accrue/test/support/stripe_fixtures.ex"
      to: "accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs"
      via: "POL-02 regression consumes `omit_livemode: true` and asserts `livemode` is absent in the raw fixture"
      pattern: "omit_livemode"
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "accrue/lib/accrue/telemetry/metrics.ex"
      via: "already-emitted webhook tuples map to new defaults/0 counters"
      pattern: "malformed_entitlement_summary|orphan_entitlement_summary"
    - from: "accrue/lib/accrue/telemetry/metrics.ex"
      to: "accrue/test/accrue/telemetry/metrics_test.exs"
      via: "tests assert `event_name` tuple presence and bounded tags"
      pattern: "event_name"
---

<objective>
Ship the additive Phase 155 polish locked in CONTEXT.md: make the livemode-absent entitlement-summary fixture path first-class for tests, clarify that `StripeFixtures` is test-only support, and expose the two missing entitlement-summary webhook counters through `Accrue.Telemetry.Metrics.defaults/0`.

Purpose: Remove brittle per-test payload surgery from the Phase 154 carry-forward regression and let operators wire the missing malformed/orphan signals into any `telemetry_metrics` reporter without custom metric definitions.
Output: Updated Stripe fixture docs/helper, updated reducer regression, updated default metrics recipe, and focused assertions for both default metric tuples.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/155-stripefixtures-polish-telemetry-counters/155-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/155-stripefixtures-polish-telemetry-counters/155-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/155-stripefixtures-polish-telemetry-counters/155-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/155-stripefixtures-polish-telemetry-counters/155-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/154-advisory-cache-core-correctness/154-01-SUMMARY.md

<interfaces>
From accrue/test/support/stripe_fixtures.ex:
```elixir
@spec entitlement_summary_event(keyword() | map(), map()) :: map()
def entitlement_summary_event(opts \\ [], overrides \\ %{}) do
  opts = Enum.into(opts, %{})
  livemode = Map.get(opts, :livemode, false)
  summary_object = %{
    "object" => "entitlements.active_entitlement_summary",
    "customer" => customer,
    "livemode" => livemode,
    "entitlements" => %{"object" => "list", "data" => ..., "has_more" => has_more, "url" => url}
  }
end
```

From accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs:
```elixir
raw =
  StripeFixtures.entitlement_summary_event(
    [customer: customer.processor_id],
    %{"id" => "evt_pol02_second", "created" => DateTime.to_unix(ts_second)}
  )

event_without_livemode = update_in(raw, ["data", "object"], &Map.delete(&1, "livemode"))
assert {:ok, %EntitlementSummary{} = saved} = Accrue.Webhook.DefaultHandler.handle(event_without_livemode)
```

From accrue/lib/accrue/webhook/default_handler.ex:
```elixir
:telemetry.execute(
  [:accrue, :webhooks, :orphan_entitlement_summary],
  %{},
  %{customer_stripe_id: cus_id}
)

defp emit_summary_malformed(evt_id, reason) do
  :telemetry.execute(
    [:accrue, :webhooks, :malformed_entitlement_summary],
    %{},
    %{event_id: evt_id, reason: reason}
  )
end
```

From accrue/lib/accrue/telemetry/metrics.ex:
```elixir
counter("accrue.webhooks.received.count", tags: [:type]),
counter("accrue.webhooks.dispatched.count", tags: [:status]),
last_value("accrue.webhooks.queue_depth"),
summary("accrue.webhooks.dispatch.duration", unit: {:native, :millisecond}),
```

From accrue/test/accrue/telemetry/metrics_ops_parity_test.exs:
```elixir
for tuple <- TelemetryOpsInventory.expected_ops_events() do
  assert Enum.any?(defs, &(&1.event_name == tuple))
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `omit_livemode: true` fixture support, clarify test-only docs, and swap the carry-forward regression to the new option</name>
  <files>accrue/test/support/stripe_fixtures.ex, accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs</files>
  <read_first>
    - .planning/phases/155-stripefixtures-polish-telemetry-counters/155-CONTEXT.md — locked decisions D-01 through D-06, D-11, and D-13 define the fixture option, precedence, docs wording, and test placement.
    - accrue/test/support/stripe_fixtures.ex — read the moduledoc, the `entitlement_summary_event/2` option list, and the current `summary_object` construction before editing.
    - accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs — read the existing POL-02 test and keep it as the home for the carry-forward proof.
    - accrue/lib/accrue/webhook/default_handler.ex — read `livemode_for_upsert/2` so the regression still proves the Phase 154 carry-forward path rather than changing reducer semantics.
  </read_first>
  <behavior>
    - Calling `StripeFixtures.entitlement_summary_event(omit_livemode: true, customer: "...")` returns a webhook map whose `["data"]["object"]` map does not contain `"livemode"`.
    - Default `livemode: false` and explicit `livemode: true | false` remain supported; `omit_livemode: true` takes precedence if both are passed, per D-01 and D-02.
    - The `StripeFixtures` moduledoc and option docs state the module is test support under `test/support`, is not shipped as a published Hex/runtime contract, and tells adopters to copy needed shapes into their own test support per D-05 and D-06.
    - The existing POL-02 regression directly asserts the raw fixture omits `"livemode"` and still proves `saved.livemode == true` after `DefaultHandler.handle/1`.
  </behavior>
  <action>Follow the `155-PATTERNS.md` mapping for `accrue/test/support/stripe_fixtures.ex` to `accrue/test/support/webhook_fixtures.ex`: keep the existing module-doc plus option-list style, and follow the local `summary_object` plus `maybe_put/3` optional-field assembly pattern in `stripe_fixtures.ex`. In `accrue/test/support/stripe_fixtures.ex`, update the module doc per D-05 and D-06 so it explicitly says this module is test support that lives under `test/support`, is not part of the published Hex package, and is not part of Accrue's runtime/public support contract. In the `entitlement_summary_event/2` option docs, add `:omit_livemode` with explicit precedence over `:livemode` per D-01 and D-02. Implement the option by reading `omit_livemode = Map.get(opts, :omit_livemode, false)`, building the `summary_object`, and deleting only the `"livemode"` key when `omit_livemode` is true; do not introduce a `livemode: :omit` sentinel and do not leave nested `Map.delete` payload surgery in consumer tests per D-03 and D-04. Follow the exact-pattern mapping for `accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs`: replace the current absence modeling `update_in(raw, ["data", "object"], &Map.delete(&1, "livemode"))` with the new fixture option while preserving the fixture-driven setup shape in the existing POL-02 test. In that test, call `StripeFixtures.entitlement_summary_event([customer: customer.processor_id, omit_livemode: true], %{"id" => "evt_pol02_second", "created" => DateTime.to_unix(ts_second)})`, add `refute Map.has_key?(raw["data"]["object"], "livemode")`, and remove the manual `update_in(... Map.delete(&1, "livemode"))` branch so the regression proves the fixture option directly per D-11. Keep verification proportional by staying inside the existing test module rather than creating a new fixture-specific test module per D-13.</action>
  <acceptance_criteria>
    - `accrue/test/support/stripe_fixtures.ex` documents `:omit_livemode` and states the module is test-only support, not part of the published Hex/runtime contract.
    - `entitlement_summary_event/2` reads `:omit_livemode` and removes the `"livemode"` key from the summary object only when the option is true.
    - `accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs` contains `omit_livemode: true` and `refute Map.has_key?(raw["data"]["object"], "livemode")`.
    - `accrue/test/accrue/webhook/default_handler_entitlement_summary_test.exs` no longer contains the exact manual livemode deletion expression `Map.delete(&1, "livemode")`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs --seed 0</automated>
  </verify>
  <done>The livemode-absent path is first-class in `StripeFixtures`, the docs warn clearly that the module is test-only/non-Hex-contract, and the existing POL-02 regression proves the omission path without hand-editing nested payload maps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the missing entitlement-summary webhook counters to `defaults/0` with bounded tags and tuple-based assertions</name>
  <files>accrue/lib/accrue/telemetry/metrics.ex, accrue/test/accrue/telemetry/metrics_test.exs</files>
  <read_first>
    - .planning/phases/155-stripefixtures-polish-telemetry-counters/155-CONTEXT.md — locked decisions D-07 through D-10 and D-12 define the exact metric names, namespace, tags, and files that must stay unchanged.
    - accrue/lib/accrue/telemetry/metrics.ex — read the moduledoc cardinality guidance and the existing `defaults/0` webhook section before adding counters.
    - accrue/lib/accrue/webhook/default_handler.ex — read the `:orphan_entitlement_summary` and `emit_summary_malformed/2` emits so the new counters match the already-emitted tuples exactly.
    - accrue/test/accrue/telemetry/metrics_test.exs — read the existing helper style and extend it with tuple-based checks instead of count/order assertions.
    - accrue/test/support/telemetry_ops_inventory.ex and accrue/test/accrue/telemetry/metrics_ops_parity_test.exs — confirm the ops parity inventory is intentionally ops-only and must remain untouched per D-10.
  </read_first>
  <behavior>
    - `Accrue.Telemetry.Metrics.defaults/0` includes `counter("accrue.webhooks.malformed_entitlement_summary.count", tags: [:reason])` mapped to `[:accrue, :webhooks, :malformed_entitlement_summary]`.
    - `Accrue.Telemetry.Metrics.defaults/0` includes `counter("accrue.webhooks.orphan_entitlement_summary.count")` mapped to `[:accrue, :webhooks, :orphan_entitlement_summary]`.
    - The malformed metric keeps tags bounded to `[:reason]` only, and the orphan metric does not introduce customer/event/payload tags, satisfying the Phase 155 telemetry threat model.
    - `metrics_test.exs` proves presence by `event_name` tuple and targeted metric assertions rather than brittle list length/order checks, while `TelemetryOpsInventory` and `metrics_ops_parity_test.exs` remain unchanged.
  </behavior>
  <action>Follow the `155-PATTERNS.md` mapping for `accrue/lib/accrue/telemetry/metrics.ex` to the emit/metric tuple alignment in `accrue/lib/accrue/webhook/default_handler.ex`: the default metric names must map exactly to the already-emitted `[:accrue, :webhooks, :malformed_entitlement_summary]` and `[:accrue, :webhooks, :orphan_entitlement_summary]` tuples. In `accrue/lib/accrue/telemetry/metrics.ex`, add the two exact Phase 155 counters in the existing webhook section of `defaults/0`: `counter("accrue.webhooks.malformed_entitlement_summary.count", tags: [:reason])` and `counter("accrue.webhooks.orphan_entitlement_summary.count")`, preserving the `accrue.webhooks.*` namespace per D-07 and D-08. Apply the `155-PATTERNS.md` low-cardinality tag mapping from `metrics.ex`: keep malformed-summary tags bounded to `[:reason]` only and do not add `customer_stripe_id`, `event_id`, raw payload fields, or any other high-cardinality tags per D-09. Follow the `metrics_test.exs` to `metrics_ops_parity_test.exs` role-match mapping by asserting tuple presence with `event_name` rather than list count/order. In `accrue/test/accrue/telemetry/metrics_test.exs`, add focused assertions that `defaults/0` contains metrics whose `event_name` equals `[:accrue, :webhooks, :malformed_entitlement_summary]` and `[:accrue, :webhooks, :orphan_entitlement_summary]`, and add a direct assertion that the malformed-summary metric exposes only the `[:reason]` tag. Reuse the existing helper style or add a small local helper such as `find_metric_by_event/1`; do not switch to list-length/order coupling. Do not modify `accrue/test/support/telemetry_ops_inventory.ex` or `accrue/test/accrue/telemetry/metrics_ops_parity_test.exs` because D-10 keeps those files ops-only.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/telemetry/metrics.ex` contains `counter("accrue.webhooks.malformed_entitlement_summary.count", tags: [:reason])`.
    - `accrue/lib/accrue/telemetry/metrics.ex` contains `counter("accrue.webhooks.orphan_entitlement_summary.count")`.
    - `accrue/test/accrue/telemetry/metrics_test.exs` asserts both `event_name` tuples are present in `M.defaults()`.
    - `accrue/test/accrue/telemetry/metrics_test.exs` asserts the malformed-summary metric tags are exactly `[:reason]`.
    - `accrue/test/support/telemetry_ops_inventory.ex` and `accrue/test/accrue/telemetry/metrics_ops_parity_test.exs` are not modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/telemetry/metrics_test.exs --seed 0</automated>
  </verify>
  <done>`defaults/0` exposes the two previously-missing entitlement-summary webhook counters with bounded tags, and the focused metrics test locks the tuple mapping without broadening the ops-only parity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test support docs -> external adopters | The `StripeFixtures` moduledoc can be misread as a published support contract if its wording is vague. |
| emitted webhook metadata -> host metrics reporter | Telemetry metadata crosses from webhook handling into host-observed metric tags, so tag cardinality must stay bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-01 | Tampering | `accrue/test/support/stripe_fixtures.ex` support boundary | mitigate | Task 1 updates the moduledoc and option docs per D-05 and D-06 to state the module is test-only support under `test/support`, not part of the published Hex/runtime contract, and introduces no runtime API changes. |
| T-155-02 | Tampering | `accrue/test/support/stripe_fixtures.ex` fixture payload shaping | mitigate | Task 1 makes `omit_livemode: true` delete only the `"livemode"` key and documents that the option wins over `livemode:` so tests cannot accidentally exercise the wrong branch. |
| T-155-03 | Denial of service | `accrue/lib/accrue/telemetry/metrics.ex` default metric tags | mitigate | Task 2 adds only `tags: [:reason]` to the malformed-summary counter, leaves the orphan counter untagged, and explicitly forbids customer/event/payload identifiers per D-09. |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs or dependency changes are part of Phase 155. |
</threat_model>

<verification>
Run the focused phase checks in order:

1. `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs --seed 0`
2. `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/telemetry/metrics_test.exs --seed 0`
3. `cd /Users/jon/projects/accrue/accrue && mix test test/accrue/webhook/default_handler_entitlement_summary_test.exs test/accrue/telemetry/metrics_test.exs --seed 0`

Source assertions after implementation:

- `grep -F 'omit_livemode' /Users/jon/projects/accrue/accrue/test/support/stripe_fixtures.ex`
- `grep -F 'not part of the published Hex package' /Users/jon/projects/accrue/accrue/test/support/stripe_fixtures.ex`
- `grep -F 'counter(\"accrue.webhooks.malformed_entitlement_summary.count\", tags: [:reason])' /Users/jon/projects/accrue/accrue/lib/accrue/telemetry/metrics.ex`
- `grep -F 'counter(\"accrue.webhooks.orphan_entitlement_summary.count\")' /Users/jon/projects/accrue/accrue/lib/accrue/telemetry/metrics.ex`
- `grep -F 'event_name == [:accrue, :webhooks, :malformed_entitlement_summary]' /Users/jon/projects/accrue/accrue/test/accrue/telemetry/metrics_test.exs`
- `grep -F 'event_name == [:accrue, :webhooks, :orphan_entitlement_summary]' /Users/jon/projects/accrue/accrue/test/accrue/telemetry/metrics_test.exs`
</verification>

<success_criteria>
1. `StripeFixtures.entitlement_summary_event/2` accepts `omit_livemode: true`, omits the `livemode` key from the summary object, and this behavior is asserted in the existing POL-02 regression.
2. `StripeFixtures` documentation clearly marks the module as test-only support under `test/support`, not part of the published Hex package or runtime/public support contract.
3. `Accrue.Telemetry.Metrics.defaults/0` includes `[:accrue, :webhooks, :malformed_entitlement_summary]` and `[:accrue, :webhooks, :orphan_entitlement_summary]` defaults with bounded tags.
4. Focused phase tests for the reducer regression and metrics defaults both pass.
</success_criteria>

<output>
Create `.planning/phases/155-stripefixtures-polish-telemetry-counters/155-01-SUMMARY.md` when done
</output>

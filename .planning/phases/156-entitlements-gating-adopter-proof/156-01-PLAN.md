---
phase: 156-entitlements-gating-adopter-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/entitlements/guard.ex
  - examples/accrue_host/config/config.exs
  - examples/accrue_host/lib/accrue_host_web/router.ex
  - examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs
  - accrue/guides/entitlements.md
autonomous: true
requirements: [PRF-01]

must_haves:
  truths:
    - "PRF-01 / D-01 / D-03: Mounting `/app/reports/advanced` with a `%Ecto.Association.NotLoaded{}` billable denies safely with the existing generic flash and does not raise."
    - "D-02 / D-06 / D-11: The checked-in example host shows the required `on_mount` order: `AccrueHostWeb.UserAuth` first, `Accrue.Live.Entitlements` second, and the deny target outside the gated session."
    - "D-07 / D-08 / D-09: `examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs` keeps the existing allow/deny proofs and adds one focused `NotLoaded` regression in the same module; no broad duplicate core-guard proof is added."
    - "D-10 / D-12: The router comment stays concise and points adopters to a canonical guide note that explains the fail-closed unloaded-billable behavior."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/guard.ex"
      provides: "Shared guard normalization that converts `%Ecto.Association.NotLoaded{}` into a denyable nil billable before entitlement predicates run"
      contains: "Ecto.Association.NotLoaded"
    - path: "examples/accrue_host/config/config.exs"
      provides: "Explicit host resolver branch for loaded organization vs unloaded organization vs nil fallback"
      contains: "config :accrue, :entitlements"
    - path: "examples/accrue_host/lib/accrue_host_web/router.ex"
      provides: "Inline ordering contract comment above `live_session :entitled_reports`"
      contains: "Ordering contract"
    - path: "examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs"
      provides: "Existing allow/deny proofs plus one route-level `NotLoaded` regression for `/app/reports/advanced`"
      contains: "NotLoaded"
    - path: "accrue/guides/entitlements.md"
      provides: "Canonical LiveView gating guidance that mentions unloaded billables failing closed"
      contains: "Ecto.Association.NotLoaded"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host_web/router.ex"
      to: "examples/accrue_host/lib/accrue_host_web/user_auth.ex"
      via: "live_session :entitled_reports on_mount ordering"
      pattern: "require_authenticated"
    - from: "accrue/lib/accrue/entitlements/guard.ex"
      to: "examples/accrue_host/config/config.exs"
      via: "Accrue.Config.entitlements() billable resolver feeds Guard.resolve_billable/3"
      pattern: "billable"
    - from: "examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs"
      to: "/app/reports/advanced"
      via: "real route-level LiveView mount proof"
      pattern: "live\\(conn, \"/app/reports/advanced\"\\)"
---

<objective>
Close PRF-01 by hardening the shared entitlement guard against `%Ecto.Association.NotLoaded{}` and making the checked-in adopter example explicit about ordering and fail-closed behavior.

Purpose: An adopter should be able to copy the `examples/accrue_host` `live_session` pattern without crashing on an unloaded billable association and without guessing where `Accrue.Live.Entitlements` belongs in the `on_mount` list.

Output: One focused host route regression, a shared guard normalization in `Accrue.Entitlements.Guard`, an explicit example-host resolver branch, and a short router-plus-guide documentation contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/156-entitlements-gating-adopter-proof/156-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/156-entitlements-gating-adopter-proof/156-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/156-entitlements-gating-adopter-proof/156-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/156-entitlements-gating-adopter-proof/156-VALIDATION.md

<interfaces>
From accrue/lib/accrue/entitlements/guard.ex:
```elixir
@spec check(:plug | :live, container(), keyword()) ::
        {:allow, container()} | {:deny, deny_form(), ctx()}
def check(surface, container, opts) when surface in [:plug, :live]

@spec resolve_billable(:plug | :live, container(), keyword()) :: term() | nil
def resolve_billable(_surface, container, opts)

defp safe_apply(fun, container) do
  fun.(container)
rescue
  _ -> nil
end

defp default_probe(%{assigns: %{} = assigns}) do
  case Map.get(assigns, :current_scope) do
    %{user: user} when not is_nil(user) -> user
    _ -> Map.get(assigns, :current_user)
  end
end
```

From accrue/lib/accrue/live/entitlements.ex:
```elixir
def on_mount({:require_feature, feature}, _params, _session, socket) do
  decide(:feature, feature, socket)
end

case Accrue.Entitlements.Guard.check(:live, socket, [{kind, required}]) do
  {:allow, socket} -> {:cont, socket}
  {:deny, deny_form, _ctx} -> {:halt, deny(socket, deny_form)}
end
```

From examples/accrue_host/config/config.exs:
```elixir
config :accrue, :entitlements,
  billable: fn container ->
    scope = Map.get(container.assigns, :current_scope)

    if scope do
      Map.get(scope, :active_organization) || Map.get(scope, :user)
    else
      Map.get(container.assigns, :current_user)
    end
  end
```

From examples/accrue_host/lib/accrue_host_web/router.ex:
```elixir
live_session :entitled_reports,
  on_mount: [
    {AccrueHostWeb.UserAuth, :require_authenticated},
    {Accrue.Live.Entitlements, {:require_feature, :advanced_reports}}
  ] do
  live("/app/reports/advanced", AdvancedReportsLive, :index)
end
```

From examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs:
```elixir
{:ok, _view, html} = live(conn, "/app/reports/advanced")
assert html =~ "Advanced Reports"

result = live(conn, "/app/reports/advanced")
assert {:error, {:redirect, %{to: "/", flash: %{"error" => "You don't have access to this page."}}}} = result
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden `Accrue.Entitlements.Guard` and prove the real host route fails closed on `%Ecto.Association.NotLoaded{}`</name>
  <files>accrue/lib/accrue/entitlements/guard.ex, examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs</files>
  <read_first>
    - .planning/phases/156-entitlements-gating-adopter-proof/156-CONTEXT.md — D-01 through D-09 lock the guard seam, fail-closed semantics, and proof shape.
    - .planning/phases/156-entitlements-gating-adopter-proof/156-RESEARCH.md — Pattern 1, Pattern 2, and the security domain define the `NotLoaded` normalization target and the route-level proof expectation.
    - .planning/phases/156-entitlements-gating-adopter-proof/156-VALIDATION.md — Task rows `156-01-01` and `156-01-03` define the required focused test command.
    - accrue/lib/accrue/entitlements/guard.ex — read `check/3`, `resolve_billable/3`, `safe_apply/2`, `default_probe/1`, and `deny_reason/1` before editing.
    - accrue/lib/accrue/live/entitlements.ex — confirm this module stays a thin transport adapter and receives no new decision logic.
    - examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs — keep the existing two route proofs unchanged and add the new regression in this file only.
    - accrue/lib/accrue/config.ex — read `entitlements/0` to confirm the host test can override `:billable` via runtime `Application.put_env`.
  </read_first>
  <behavior>
    - `Accrue.Entitlements.Guard.resolve_billable/3` treats `%Ecto.Association.NotLoaded{}` as `nil` before `Accrue.Entitlements.entitled?/3` or `has_active_plan?/3` is called.
    - Mounting `/app/reports/advanced` while the runtime `:billable` resolver returns `%Ecto.Association.NotLoaded{}` produces the existing redirect to `/` with flash `%{"error" => "You don't have access to this page."}`.
    - The existing allow and deny route proofs in `entitlements_guard_test.exs` remain in place and still pass; no duplicate broad core-guard unit proof is added in `accrue/test/accrue/entitlements/guard_test.exs`.
  </behavior>
  <action>In `accrue/lib/accrue/entitlements/guard.ex`, add a private normalization seam in shared guard code per D-01 and D-03. The exact target state is: any `%Ecto.Association.NotLoaded{}` produced by the configured `billable` function or the default probe becomes `nil` before the guard predicates run; every other billable value stays unchanged. Implement this in `Accrue.Entitlements.Guard`, not `Accrue.Live.Entitlements`, and route the final result of `safe_apply/2` and `default_probe/1` through the same normalization helper so both code paths behave identically. Keep the deny UX generic by preserving the existing `deny_reason(nil) -> :no_active_subscription` and `Accrue.Live.Entitlements` flash/redirect behavior per D-04.

In `examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs`, add one third regression in the same module per D-07 through D-09. Use the real `/app/reports/advanced` route and `live/2`. Before the mount, capture `prev = Application.get_env(:accrue, :entitlements)` and restore it in `on_exit`. Override only the runtime `:billable` key with `Application.put_env(:accrue, :entitlements, Keyword.put(prev, :billable, fn _container -> %Ecto.Association.NotLoaded{__field__: :active_organization, __owner__: AccrueHost.Accounts.Scope, __cardinality__: :one} end))`. Log in a user with an active organization as the existing tests do, call `live(conn, "/app/reports/advanced")`, and assert the exact redirect tuple `{:error, {:redirect, %{to: "/", flash: %{"error" => "You don't have access to this page."}}}}`. Do not rename or relax the two existing test cases, do not add a new `on_mount`, and do not add a broad duplicate unit test outside this host proof module.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/entitlements/guard.ex` contains a `%Ecto.Association.NotLoaded{}` branch in shared billable normalization logic.
    - `accrue/lib/accrue/entitlements/guard.ex` does not add new authorization logic to `accrue/lib/accrue/live/entitlements.ex`.
    - `examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs` still contains the existing two test names and adds one new test that uses `Application.put_env(:accrue, :entitlements, ...)`.
    - The new host regression asserts the exact redirect target `/` and the exact flash string `You don't have access to this page.`.
    - `accrue/test/accrue/entitlements/guard_test.exs` is not modified by this phase plan.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/live/entitlements_guard_test.exs --seed 0</automated>
  </verify>
  <done>The shared guard denies `%Ecto.Association.NotLoaded{}` safely, the new host route regression passes, and the existing allow/deny route proofs remain merge-blocking without extra duplicate coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Make the example host pattern explicit in config, router, and the canonical entitlements guide</name>
  <files>examples/accrue_host/config/config.exs, examples/accrue_host/lib/accrue_host_web/router.ex, accrue/guides/entitlements.md</files>
  <read_first>
    - .planning/phases/156-entitlements-gating-adopter-proof/156-CONTEXT.md — D-02, D-04, D-05, D-06, D-10, D-11, and D-12 lock the example-host and documentation contract.
    - .planning/phases/156-entitlements-gating-adopter-proof/156-PATTERNS.md — follow the mapped host resolver, router, and guide locations; keep the router comment concise and the guide as the detailed explanation.
    - .planning/phases/156-entitlements-gating-adopter-proof/156-VALIDATION.md — task row `156-01-02` defines the exact static source check.
    - examples/accrue_host/config/config.exs — read the current `config :accrue, :entitlements` resolver and keep the rest of the host config unchanged.
    - examples/accrue_host/lib/accrue_host_web/router.ex — read the `live_session :entitled_reports` block and preserve the existing `on_mount` order.
    - examples/accrue_host/lib/accrue_host_web/user_auth.ex — confirm `:require_authenticated` is the auth/scope loader that must stay first.
    - accrue/guides/entitlements.md — read the `Gate a LiveView` section and extend that canonical recipe instead of turning the router comment into a tutorial.
  </read_first>
  <action>In `examples/accrue_host/config/config.exs`, rewrite the `config :accrue, :entitlements, billable:` function per D-02 through D-04 so the host example is explicit instead of relying on `Map.get(scope, :active_organization) || Map.get(scope, :user)`. The exact target state is: if `scope` is absent, fall back to `Map.get(container.assigns, :current_user)`; if `scope` exists and `Map.get(scope, :active_organization)` is a loaded non-nil organization, return it; if it is `%Ecto.Association.NotLoaded{}`, return `nil` so the guard fails closed; if it is `nil`, fall back to `Map.get(scope, :user)`. Do not pass the sentinel through to entitlement predicates, do not add a separate pre-entitlement organization `on_mount`, and do not add a new "select organization first" UX path in this phase per D-04 and D-05.

In `examples/accrue_host/lib/accrue_host_web/router.ex`, add a concise comment immediately above `live_session :entitled_reports` starting with `# Ordering contract:`. The comment must state four concrete points per D-10 and D-11: `AccrueHostWeb.UserAuth` scope/auth mount runs first, `Accrue.Live.Entitlements` runs second, the deny target must stay outside this gated session, and missing/unloaded billables fail closed instead of raising. Keep the existing `on_mount` order exactly as it is now.

In `accrue/guides/entitlements.md`, extend the `Gate a LiveView` section so the guide is the detailed explanation per D-12. Add one focused note that a host auth/scope loader must populate any organization billable before `Accrue.Live.Entitlements` runs, and that `%Ecto.Association.NotLoaded{}` or other unloaded billables are normalized to a fail-closed deny rather than a crash. Keep the router comment short by pointing adopters to this guide section for the fuller recipe.</action>
  <acceptance_criteria>
    - `examples/accrue_host/config/config.exs` no longer contains the bare expression `Map.get(scope, :active_organization) || Map.get(scope, :user)`.
    - `examples/accrue_host/config/config.exs` contains an explicit `%Ecto.Association.NotLoaded{}` branch that returns `nil`.
    - `examples/accrue_host/lib/accrue_host_web/router.ex` contains `# Ordering contract:` immediately above `live_session :entitled_reports`.
    - The router comment includes the strings `Accrue.Live.Entitlements`, `fails closed`, and a guide reference to `accrue/guides/entitlements.md` or `guides/entitlements.md`.
    - `accrue/guides/entitlements.md` mentions `%Ecto.Association.NotLoaded{}` or `unloaded billable` in the LiveView gating section and states that the guard denies instead of raising.
    - `examples/accrue_host/lib/accrue_host_web/router.ex` still lists `{AccrueHostWeb.UserAuth, :require_authenticated}` before `{Accrue.Live.Entitlements, {:require_feature, :advanced_reports}}`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/live/entitlements_guard_test.exs --seed 0 && rg -n "Ordering contract|Accrue.Live.Entitlements|NotLoaded|unloaded|fails closed" /Users/jon/projects/accrue/examples/accrue_host/lib/accrue_host_web/router.ex /Users/jon/projects/accrue/accrue/guides/entitlements.md</automated>
  </verify>
  <done>The checked-in example host shows the explicit fail-closed resolver and the required `on_mount` order, and the canonical entitlements guide carries the fuller unloaded-billable explanation so adopters do not have to infer the contract from router code alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host `billable` resolver -> shared guard | Untrusted or partially-loaded host assigns cross into authorization logic here; `%Ecto.Association.NotLoaded{}` must not be treated as a real billable. |
| router `on_mount` ordering -> LiveView authorization | The host router decides whether auth/scope loading happens before entitlement checks; mis-ordering can produce incorrect deny behavior or ambiguous state. |
| deny result -> end-user response | The deny path must not leak feature/plan names or internal billable-state details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Elevation of privilege | `accrue/lib/accrue/entitlements/guard.ex` billable resolution | mitigate | Task 1 normalizes `%Ecto.Association.NotLoaded{}` to `nil` in shared guard code before predicate calls and adds the host route regression that asserts redirect/deny instead of grant or crash. |
| T-156-02 | Elevation of privilege | `examples/accrue_host/lib/accrue_host_web/router.ex` `on_mount` ordering | mitigate | Task 2 keeps `{AccrueHostWeb.UserAuth, :require_authenticated}` before `{Accrue.Live.Entitlements, ...}` and documents the ordering contract inline plus in `accrue/guides/entitlements.md`. |
| T-156-03 | Information disclosure | LiveView deny path and adopter docs | mitigate | Task 1 preserves the existing generic deny flash, and Task 2 documents fail-closed behavior without exposing required feature/plan names or adding a more revealing UX branch. |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | No dependency or package-manager installs are part of Phase 156. |
</threat_model>

<verification>
Run the focused phase checks in order:

1. `cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/live/entitlements_guard_test.exs --seed 0`
2. `cd /Users/jon/projects/accrue/examples/accrue_host && mix test --seed 0`
3. `cd /Users/jon/projects/accrue/examples/accrue_host && mix verify.full`

Source assertions after implementation:

- `rg -n "Ecto.Association.NotLoaded" /Users/jon/projects/accrue/accrue/lib/accrue/entitlements/guard.ex /Users/jon/projects/accrue/examples/accrue_host/config/config.exs /Users/jon/projects/accrue/accrue/guides/entitlements.md`
- `rg -n "Ordering contract|fails closed|Accrue.Live.Entitlements" /Users/jon/projects/accrue/examples/accrue_host/lib/accrue_host_web/router.ex`
- `rg -n "Application.put_env\\(:accrue, :entitlements|/app/reports/advanced|You don't have access to this page\\." /Users/jon/projects/accrue/examples/accrue_host/test/accrue_host_web/live/entitlements_guard_test.exs`
</verification>

<success_criteria>
1. `Accrue.Entitlements.Guard` converts `%Ecto.Association.NotLoaded{}` into a fail-closed denyable value before any entitlement predicate runs.
2. `examples/accrue_host` contains a single focused route-level `NotLoaded` regression in `entitlements_guard_test.exs`, and the existing allow/deny proofs still pass unchanged.
3. The example host resolver and router comment explicitly teach the required `on_mount` order and fail-closed unloaded-billable behavior.
4. `accrue/guides/entitlements.md` carries the fuller LiveView gating recipe so the router comment can stay concise.
</success_criteria>

<output>
Create `.planning/phases/156-entitlements-gating-adopter-proof/156-01-SUMMARY.md` when done
</output>

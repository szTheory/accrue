---
phase: 157-metered-usage-adopter-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs
  - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
autonomous: true
requirements: [PRF-02]

must_haves:
  truths:
    - "PRF-02 / D-01 / D-04: The adopter proof stays in `examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs` and exercises `/app/billing`, not a new parallel or core-only surface."
    - "PRF-02 / D-02 / D-03 / D-05 / D-06 / D-07: The test establishes the subscription precondition through `AccrueHost.Billing.subscribe/3` using `AccrueHost.Billing.Plans.ids().metered`, then clicks the visible `Simulate API Call` LiveView button."
    - "PRF-02 / D-08 / D-09 / D-10: The test asserts the flash `Usage reported: 1 API call recorded.`, exactly one `Accrue.Billing.MeterEvent` row, `event.event_name == \"api_calls\"`, and `event.value == 1`."
    - "PRF-02 / D-11 / D-12 / D-13: `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` has an inline comment at the `Billing.report_usage_for_scope(..., value: 1)` callsite explaining that metered usage uses `value:` and subscription/invoice line items use `quantity:`."
  artifacts:
    - path: "examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs"
      provides: "Executable host-level metered usage adopter proof"
      contains: "Plans.ids().metered"
    - path: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      provides: "Adopter-copyable `value:` usage reporting callsite with footgun comment"
      contains: "quantity:"
  key_links:
    - from: "examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs"
      to: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      via: "LiveView click on `Simulate API Call` invokes `handle_event(\"simulate_api_call\", ...)`"
      pattern: "render_click"
    - from: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      to: "examples/accrue_host/lib/accrue_host/billing.ex"
      via: "`Billing.report_usage_for_scope/3` keeps authorization and active-organization resolution in the host facade"
      pattern: "report_usage_for_scope"
---

<objective>
Close PRF-02 by making the checked-in `examples/accrue_host` metered usage proof match the adopter story: subscribe to the metered plan, trigger the visible `Simulate API Call` action, assert the success flash, assert exactly one durable meter-event row with `event_name == "api_calls"` and `value == 1`, and document the `value:` vs `quantity:` footgun at the copyable callsite.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/157-metered-usage-adopter-proof/157-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/157-metered-usage-adopter-proof/157-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/157-metered-usage-adopter-proof/157-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/157-metered-usage-adopter-proof/157-VALIDATION.md

<interfaces>
From `examples/accrue_host/lib/accrue_host/billing/plans.ex`:
```elixir
@ids %{basic: "price_basic", pro: "price_pro", metered: "price_metered"}
def ids, do: @ids
```

From `examples/accrue_host/lib/accrue_host/billing.ex`:
```elixir
def report_usage_for_scope(%Scope{} = scope, event_name, opts \\ []) do
  with {:ok, customer} <- customer_for_scope(scope),
       :ok <- authorize_billing_mutation(scope) do
    Billing.report_usage(customer, event_name, opts)
  end
end
```

From `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex`:
```elixir
Billing.report_usage_for_scope(socket.assigns.current_scope, "api_calls", value: 1)
```

From `accrue/lib/accrue/billing/meter_event_actions.ex`:
```elixir
@report_usage_schema [
  value: [type: :non_neg_integer, default: 1],
  timestamp: [type: :any, default: nil],
  identifier: [type: {:or, [:string, nil]}, default: nil],
  operation_id: [type: {:or, [:string, nil]}, default: nil],
  payload: [type: {:or, [{:map, :any, :any}, nil]}, default: nil]
]
```
</interfaces>
</context>

<threat_model>
## Security Threat Model

| ID | Threat | Severity | Mitigation in Plan |
|----|--------|----------|--------------------|
| T-157-01 | Bypassing organization billing authorization by replacing the LiveView path with a direct core metering call | medium | Keep the source call through `Billing.report_usage_for_scope(socket.assigns.current_scope, ...)` and keep the test mounted through `/app/billing`. |
| T-157-02 | Teaching adopters the wrong write option, causing silently ignored usage submissions in real integrations | medium | Add an inline comment at the copyable host callsite explaining `value:` for meter events vs `quantity:` for subscription/invoice line items. |
| T-157-03 | False-positive proof by seeding impossible billing rows directly | low | Subscribe through `AccrueHost.Billing.subscribe/3` with `Plans.ids().metered`; do not direct-insert customer/subscription/subscription-item setup. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade the host metered usage proof to the metered plan and durable row-shape assertion</name>
  <files>examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs</files>
  <read_first>
    - .planning/phases/157-metered-usage-adopter-proof/157-CONTEXT.md — D-01 through D-10 lock proof shape, setup boundary, and row assertions.
    - .planning/phases/157-metered-usage-adopter-proof/157-RESEARCH.md — Findings identify the existing proof, metered price helper, and validation command.
    - .planning/phases/157-metered-usage-adopter-proof/157-PATTERNS.md — Pattern map names the exact test edits.
    - examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs — read the current `"demonstrates metered usage reporting (PROOF-04)"` test and `cleanup_fake_billing_rows!/0`.
    - examples/accrue_host/lib/accrue_host/billing/plans.ex — confirm `Plans.ids().metered` maps to `"price_metered"`.
    - examples/accrue_host/lib/accrue_host/billing.ex — confirm `subscribe/3` is the host facade setup path.
    - accrue/lib/accrue/billing/meter_event.ex — confirm persisted fields `event_name` and `value`.
  </read_first>
  <behavior>
    - The existing metered usage proof subscribes the organization to the configured metered plan before mounting `/app/billing`.
    - The proof still clicks the visible `"Simulate API Call"` button through Phoenix LiveView.
    - The proof still asserts `"Usage reported: 1 API call recorded."`.
    - The proof asserts exactly one `Accrue.Billing.MeterEvent` row, `event.event_name == "api_calls"`, and `event.value == 1`.
  </behavior>
  <action>Add `alias AccrueHost.Billing.Plans` near the existing aliases in `examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs`. In the `"demonstrates metered usage reporting (PROOF-04)"` test, replace `Billing.subscribe(organization, "price_basic")` with `Billing.subscribe(organization, Plans.ids().metered)`. Keep the setup through `Billing.subscribe/3`; do not direct-insert customer, subscription, or subscription-item rows. Keep the existing LiveView mount, `"Metered Usage Demo"` assertion, `"Simulate API Call"` click, flash assertion, and global one-row `MeterEvent` count. Add `assert event.value == 1` immediately after `assert event.event_name == "api_calls"`.</action>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs` contains `alias AccrueHost.Billing.Plans`.
    - The metered usage proof contains `Billing.subscribe(organization, Plans.ids().metered)`.
    - The metered usage proof does not contain `Billing.subscribe(organization, "price_basic")`.
    - The metered usage proof still contains `element("button", "Simulate API Call") |> render_click()`.
    - The metered usage proof contains `assert Repo.aggregate(MeterEvent, :count, :id) == 1`, `assert event.event_name == "api_calls"`, and `assert event.value == 1`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/live/subscription_live_test.exs --seed 0</automated>
  </verify>
  <done>The host proof is an executable end-to-end metered usage adopter proof for the configured metered price.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the inline `value:` vs `quantity:` comment at the adopter-copyable usage callsite</name>
  <files>examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex</files>
  <read_first>
    - .planning/phases/157-metered-usage-adopter-proof/157-CONTEXT.md — D-11 through D-13 lock comment placement and scope.
    - .planning/phases/157-metered-usage-adopter-proof/157-RESEARCH.md — `value:` schema finding and source footgun validation.
    - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex — read `handle_event("simulate_api_call", ...)` before editing.
    - accrue/lib/accrue/billing/meter_event_actions.ex — confirm `value:` is the metered usage option and `quantity:` is not part of the report-usage schema.
  </read_first>
  <behavior>
    - The `simulate_api_call` handler still calls `Billing.report_usage_for_scope(socket.assigns.current_scope, "api_calls", value: 1)`.
    - A short inline comment immediately adjacent to that call explains that metered usage uses `value:`, while `quantity:` belongs to subscription/invoice line items and is not the meter-event option.
    - No broad LiveView tutorial, route redesign, or billing API behavior change is introduced.
  </behavior>
  <action>In `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex`, add a concise comment immediately above `Billing.report_usage_for_scope(socket.assigns.current_scope, "api_calls", value: 1)`. The comment must include both exact tokens `value:` and `quantity:` and state the target meaning: use `value:` for meter events; use `quantity:` for subscription/invoice line items, not this usage-reporting call. Leave the call through `Billing.report_usage_for_scope/3`, the event name `"api_calls"`, and `value: 1` unchanged.</action>
  <acceptance_criteria>
    - `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains a comment adjacent to the usage call with the exact tokens `value:` and `quantity:`.
    - The `simulate_api_call` handler still contains `Billing.report_usage_for_scope(socket.assigns.current_scope, "api_calls", value: 1)`.
    - The file does not introduce a new route, new LiveView event, new billing facade function, or new core metering API.
  </acceptance_criteria>
  <verify>
    <automated>rg "value:.*quantity|quantity:.*value" /Users/jon/projects/accrue/examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex</automated>
    <automated>cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/live/subscription_live_test.exs --seed 0</automated>
  </verify>
  <done>The copyable host usage example explains the `value:` footgun without changing behavior.</done>
</task>

</tasks>

<verification>
Run:

`cd /Users/jon/projects/accrue/examples/accrue_host && mix test test/accrue_host_web/live/subscription_live_test.exs --seed 0`

Then verify source comment presence:

`rg "value:.*quantity|quantity:.*value" /Users/jon/projects/accrue/examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex`
</verification>

<success_criteria>
- `examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs` proves subscribe to metered price -> click `Simulate API Call` -> flash confirmation -> exactly one `MeterEvent`.
- The asserted `MeterEvent` has `event_name == "api_calls"` and `value == 1`.
- `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` contains the inline `value:` vs `quantity:` comment at the usage-reporting callsite.
- No schema changes, new billing primitives, new routes, or broad documentation rewrites are introduced.
</success_criteria>

## PLANNING COMPLETE

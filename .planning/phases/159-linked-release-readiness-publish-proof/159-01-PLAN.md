---
phase: 159-linked-release-readiness-publish-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
  - scripts/ci/verify_release_manifest_alignment.sh
  - scripts/ci/capture_linked_release_proof.sh
  - scripts/ci/README.md
  - RELEASING.md
  - accrue/guides/release-notes.md
  - .release-please-manifest.json
  - accrue_portal/mix.exs
  - accrue_portal/CHANGELOG.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
user_setup:
  - service: github
    why: "The primary release path needs repository secrets and maintainer merge/workflow visibility."
    env_vars:
      - name: RELEASE_PLEASE_TOKEN
        source: "GitHub repository Settings -> Secrets and variables -> Actions"
    dashboard_config:
      - task: "Confirm the maintainer account used by the executor can review/merge the combined Release Please PR and inspect the release-please workflow run."
        location: "GitHub repository Pull Requests and Actions"
  - service: hex
    why: "GitHub Actions publish jobs need package publish authority for all three linked packages."
    env_vars:
      - name: HEX_API_KEY
        source: "Hex.pm account -> API Keys"
    dashboard_config:
      - task: "Ensure the API key can publish accrue, accrue_admin, and accrue_portal."
        location: "Hex.pm account settings"
must_haves:
  truths:
    - "One canonical Phase 159 verification ledger reconciles Release Please intent, deterministic gate results, and post-publish public truth for accrue, accrue_admin, and accrue_portal."
    - "No release is treated as ready unless manifest versions, mix.exs versions, changelog headings, release notes, and runbook instructions agree on the same linked target version."
    - "The deterministic release gate records pass/fail evidence for release manifest alignment, release contract, release notes freshness, package docs, support-matrix drift, adoption-proof drift, and host integration."
    - "The linked publish executes in strict order: accrue first, then accrue_admin, then accrue_portal."
    - "Phase completion requires all three package tags, GitHub releases, Hex package versions, HexDocs pages, and the host Hex smoke to agree with the same target version."
  artifacts:
    - path: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      provides: "Canonical append-only release-truth artifact for REL-01..03"
      contains: "PR_NUMBER:"
    - path: "scripts/ci/verify_release_manifest_alignment.sh"
      provides: "Three-package manifest versus mix.exs lockstep verifier"
      contains: "accrue_portal"
    - path: "scripts/ci/capture_linked_release_proof.sh"
      provides: "Post-publish proof capture for workflow ordering, tags, releases, Hex truth, and HexDocs checks"
      contains: "HexDocs availability"
    - path: "scripts/ci/README.md"
      provides: "Current release gate map and triage guidance for Phase 159"
      contains: "159-VERIFICATION.md"
    - path: "RELEASING.md"
      provides: "Primary linked-release runbook with current proof capture steps"
      contains: "verify_release_pr_scope.sh"
    - path: "accrue/guides/release-notes.md"
      provides: "Target-version public release-notes mirror for core/admin with linked portal context"
      contains: "## accrue_admin"
  key_links:
    - from: "scripts/ci/verify_release_manifest_alignment.sh"
      to: ".release-please-manifest.json"
      via: "jq package-version extraction for accrue/accrue_admin/accrue_portal"
      pattern: "accrue_portal"
    - from: "scripts/ci/capture_linked_release_proof.sh"
      to: ".github/workflows/release-please.yml"
      via: "gh run view on one exact RUN_ID"
      pattern: "Publish accrue_portal"
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/verify_release_pr_scope.sh"
      via: "PR_NUMBER and TARGET_VERSION audit rows"
      pattern: "PR_NUMBER:"
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/accrue_host_hex_smoke.sh"
      via: "Host Hex smoke evidence block"
      pattern: "Host Hex smoke"
    - from: "RELEASING.md"
      to: ".github/workflows/publish-hex.yml"
      via: "fallback-only recovery instructions"
      pattern: "manual fallback"
---

<objective>
Verify, publish, and prove the next linked release line after `1.3.0` without split-brain release truth across `accrue`, `accrue_admin`, and `accrue_portal`.

Purpose: REL-01, REL-02, and REL-03 only close when one artifact proves release intent, deterministic readiness, and published public fact for the same target version.
Output:
- `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` as the canonical release-truth ledger
- three-package release verifiers and runbook guidance aligned to the Phase 159 proof contract
- one published linked Hex line with reconciled tags, GitHub releases, Hex registry state, HexDocs, release notes, and host Hex smoke evidence
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-RESEARCH.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-VALIDATION.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-PATTERNS.md
@.planning/milestones/v1.23-phases/75-post-publish-contract-alignment/75-VERIFICATION.md
@RELEASING.md
@scripts/ci/README.md
@scripts/ci/verify_release_manifest_alignment.sh
@scripts/ci/verify_release_pr_scope.sh
@scripts/ci/verify_release_contract.sh
@scripts/ci/verify_release_notes_contract.sh
@scripts/ci/capture_linked_release_proof.sh
@scripts/ci/accrue_host_uat.sh
@scripts/ci/accrue_host_hex_smoke.sh
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@release-please-config.json
@.release-please-manifest.json
@accrue/mix.exs
@accrue_admin/mix.exs
@accrue_portal/mix.exs
@accrue/guides/release-notes.md

<interfaces>
From scripts/ci/verify_release_manifest_alignment.sh (current gap to close):

```bash
m_accrue=$(jq -r '.accrue // empty' "$MANIFEST")
m_admin=$(jq -r '.accrue_admin // empty' "$MANIFEST")
mix_accrue=$(sed -n 's/^  @version "\([^"]*\)"/\1/p' "$ROOT_DIR/accrue/mix.exs" | head -n 1)
mix_admin=$(sed -n 's/^  @version "\([^"]*\)"/\1/p' "$ROOT_DIR/accrue_admin/mix.exs" | head -n 1)
```

This task must add first-class `accrue_portal` coverage and fail if any one of the three package versions diverges.

From scripts/ci/capture_linked_release_proof.sh (existing proof spine):

```bash
Usage: bash scripts/ci/capture_linked_release_proof.sh --version <x.y.z> --run-id <id> --pr <number-or-url> --output <path>
...
for package in accrue accrue_admin accrue_portal; do
  tag="${package}-v${VERSION}"
  release_json=$(gh release view "$tag" --repo "$REPO" --json tagName,url,publishedAt)
  hex_json=$(curl -fsSL "https://hex.pm/api/packages/${package}")
done
```

Keep the append-only proof write pattern and extend it rather than inventing a second capture format.

From .github/workflows/release-please.yml (primary publish DAG):

```yaml
publish-accrue-admin:
  needs: [release, publish-accrue]
  env:
    ACCRUE_ADMIN_HEX_RELEASE: "1"

publish-accrue-portal:
  needs: [release, publish-accrue, publish-accrue-admin]
  env:
    ACCRUE_PORTAL_HEX_RELEASE: "1"
```

This ordering is locked by D-08 and must remain the public proof order.

From accrue_admin/mix.exs and accrue_portal/mix.exs (publish-mode dependency switches):

```elixir
if System.get_env("ACCRUE_ADMIN_HEX_RELEASE") == "1" do
  {:accrue, "~> #{@version}"}
else
  {:accrue, path: "../accrue"}
end

if System.get_env("ACCRUE_PORTAL_HEX_RELEASE") == "1" do
  {:accrue, "== #{@version}"}
else
  {:accrue, path: "../accrue"}
end
```

The Phase 159 proof must keep both switches intact and use them only in ordered publish/Hex-smoke lanes.

From scripts/ci/verify_release_notes_contract.sh (release-notes contract already enforced):

```bash
version_heading_count=$(grep -Ec "^### ${accrue_version}$" "$notes" || true)
[[ "$version_heading_count" -ge 2 ]] ||
  fail "release-notes.md must describe ${accrue_version} for both accrue and accrue_admin"
grep -Fq "accrue_portal" "$notes" || fail "release-notes.md must mention accrue_portal version-family context"
```

Do not weaken this contract; update `accrue/guides/release-notes.md` to the exact target version instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the three-package proof surfaces and seed the Phase 159 ledger</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md, scripts/ci/verify_release_manifest_alignment.sh, scripts/ci/capture_linked_release_proof.sh, scripts/ci/README.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md — locked decisions D-01 through D-14
    - .planning/phases/159-linked-release-readiness-publish-proof/159-RESEARCH.md — exact fixed-schema proof expectations and current gap notes
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VALIDATION.md — Wave 0 requirements for `159-VERIFICATION.md` and `verify_release_manifest_alignment.sh`
    - .planning/phases/159-linked-release-readiness-publish-proof/159-PATTERNS.md — append-only proof and three-package iteration patterns
    - .planning/milestones/v1.23-phases/75-post-publish-contract-alignment/75-VERIFICATION.md — closest verification-ledger analog
    - scripts/ci/verify_release_manifest_alignment.sh — file being modified; current two-package-only implementation
    - scripts/ci/capture_linked_release_proof.sh — file being modified; current post-publish proof spine
    - scripts/ci/README.md — file being modified; current REL section still points at the old 121 ledger
  </read_first>
  <action>Implement D-01 through D-07 before any publish attempt. Expand `scripts/ci/verify_release_manifest_alignment.sh` in place so it parses `.accrue_portal` from `.release-please-manifest.json`, parses `@version` from `accrue_portal/mix.exs`, enforces one lockstep version across `accrue`, `accrue_admin`, and `accrue_portal`, and emits an `OK:` line that names all three packages. Create `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` as the active append-only ledger for this release line, using exact section headings `## Release identifiers`, `## Pre-merge truth audit`, `## Deterministic gate bundle`, `## Publish execution`, `## Post-publish public truth`, `## Notes`, and `## Sign-off`, with exact machine fields `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:`. Extend `scripts/ci/capture_linked_release_proof.sh` so its appended block includes not only workflow ordering, tags, GitHub releases, and Hex API truth, but also a release-file snapshot for manifest plus all three `mix.exs` and `CHANGELOG.md` checks, and a `#### HexDocs availability` table keyed by `accrue`, `accrue_admin`, and `accrue_portal`. Update the REL section in `scripts/ci/README.md` from the old Phase 121 artifact to the current Phase 159 ledger, add REL-01/REL-02/REL-03 wording, and name `verify_release_manifest_alignment.sh`, `capture_linked_release_proof.sh`, and `accrue_host_hex_smoke.sh` as the current proof chain per D-04 and D-06.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_release_manifest_alignment.sh</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'PR_NUMBER:|TARGET_VERSION:|RUN_ID:|## Release identifiers|## Deterministic gate bundle|## Post-publish public truth|HexDocs availability' .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n '159-VERIFICATION\\.md|REL-01|REL-02|REL-03|verify_release_manifest_alignment\\.sh|capture_linked_release_proof\\.sh|accrue_host_hex_smoke\\.sh' scripts/ci/README.md scripts/ci/capture_linked_release_proof.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/verify_release_manifest_alignment.sh` contains `.accrue_portal` and `accrue_portal/mix.exs`.
    - `scripts/ci/verify_release_manifest_alignment.sh` fails if any one of `accrue`, `accrue_admin`, or `accrue_portal` diverges from the shared manifest version.
    - `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` exists and contains the exact headings `## Release identifiers`, `## Pre-merge truth audit`, `## Deterministic gate bundle`, `## Publish execution`, and `## Post-publish public truth`.
    - `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` contains exact machine fields `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:`.
    - `scripts/ci/capture_linked_release_proof.sh` contains a `HexDocs availability` section and still iterates `for package in accrue accrue_admin accrue_portal`.
    - `scripts/ci/README.md` no longer points REL proof ownership at `121-VERIFICATION.md` and instead references the current Phase 159 ledger and the three-script proof chain.
    - `bash scripts/ci/verify_release_manifest_alignment.sh` exits 0 on the current `1.3.0` workspace after the portal checks are added.
  </acceptance_criteria>
  <done>The release-proof scaffold, three-package manifest verifier, post-publish capture script, and CI gate map all treat `accrue_portal` and `159-VERIFICATION.md` as first-class release-truth surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the live Release Please PR, run the deterministic gate bundle, and align the public mirrors</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md, RELEASING.md, accrue/guides/release-notes.md, .release-please-manifest.json, accrue_portal/mix.exs, accrue_portal/CHANGELOG.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — file being modified; append evidence instead of replacing sections
    - RELEASING.md — file being modified; current routine linked release steps and fallback wording
    - accrue/guides/release-notes.md — file being modified; current release-note headings and portal version-family wording
    - scripts/ci/verify_release_pr_scope.sh — exact PR audit command and required file set
    - scripts/ci/verify_release_contract.sh — exact linked-release contract expectations
    - scripts/ci/verify_release_notes_contract.sh — exact release-notes contract; keep it strict
    - scripts/ci/verify_release_manifest_alignment.sh — updated three-package verifier from Task 1
    - scripts/ci/verify_package_docs.sh — docs drift gate named in REL-02
    - scripts/ci/verify_processor_support_matrix.sh — support-matrix drift gate named in REL-02
    - scripts/ci/verify_adoption_proof_matrix.sh — adoption-proof drift gate named in REL-02
    - scripts/ci/accrue_host_uat.sh — host integration gate named in REL-02
    - .release-please-manifest.json — source of truth for the exact target version
    - .github/workflows/release-please.yml — primary publish path and stale-branch repair flow
  </read_first>
  <action>Implement D-02, D-03, D-04, D-06, D-07, and D-14 before merge. Resolve the exact `TARGET_VERSION` from `.release-please-manifest.json` and the live combined Release Please PR number from GitHub, then run `bash scripts/ci/verify_release_pr_scope.sh --pr <release-pr-number> --version <target-version>`. If the only drift is the known stale-portal case while `accrue` and `accrue_admin` already match the target version, check out `release-please--branches--main`, run `bash scripts/ci/repair_linked_release_pr.sh --version <target-version>`, commit and push only `.release-please-manifest.json`, `accrue_portal/mix.exs`, and `accrue_portal/CHANGELOG.md`, then rerun `verify_release_pr_scope.sh`; if any other package drifts, stop instead of hand-editing unrelated release files. Run the REL-02 deterministic gate bundle with the exact commands `bash scripts/ci/verify_release_manifest_alignment.sh`, `bash scripts/ci/verify_release_contract.sh`, `bash scripts/ci/verify_release_notes_contract.sh`, `bash scripts/ci/verify_package_docs.sh`, `bash scripts/ci/verify_processor_support_matrix.sh`, `bash scripts/ci/verify_adoption_proof_matrix.sh`, and `bash scripts/ci/accrue_host_uat.sh`. Append one row per command into `159-VERIFICATION.md` under `## Pre-merge truth audit` or `## Deterministic gate bundle`, including the exact command string, UTC timestamp, and pass/fail result. Update `RELEASING.md` so the routine linked-release steps explicitly tell maintainers to record `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` in the current release-phase verification ledger, keep `.github/workflows/publish-hex.yml` fallback-only per D-11, and avoid naming Phase 159 as a permanent future path. Update `accrue/guides/release-notes.md` so the exact `<target-version>` appears as `### <target-version>` under both `## accrue` and `## accrue_admin`, and the same release-family prose mentions `accrue_portal` rather than implying a two-package line.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_release_pr_scope.sh --pr &lt;release-pr-number&gt; --version &lt;target-version&gt;</automated>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_release_notes_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/accrue_host_uat.sh</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'PR_NUMBER:|TARGET_VERSION:|verify_release_pr_scope\\.sh|verify_release_manifest_alignment\\.sh|verify_release_contract\\.sh|verify_release_notes_contract\\.sh|verify_package_docs\\.sh|verify_processor_support_matrix\\.sh|verify_adoption_proof_matrix\\.sh|accrue_host_uat\\.sh' .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md && bash scripts/ci/verify_release_notes_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `159-VERIFICATION.md` contains one exact `PR_NUMBER:` value and one exact `TARGET_VERSION:` value before publish.
    - `159-VERIFICATION.md` contains explicit pass/fail evidence rows for `verify_release_manifest_alignment.sh`, `verify_release_contract.sh`, `verify_release_notes_contract.sh`, `verify_package_docs.sh`, `verify_processor_support_matrix.sh`, `verify_adoption_proof_matrix.sh`, and `accrue_host_uat.sh`.
    - If the stale-portal repair path runs, only `.release-please-manifest.json`, `accrue_portal/mix.exs`, and `accrue_portal/CHANGELOG.md` are edited on the release branch before the rerun of `verify_release_pr_scope.sh`.
    - `RELEASING.md` instructs maintainers to capture `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` in the current release-phase verification ledger, and does not describe `.github/workflows/publish-hex.yml` as the primary path.
    - `accrue/guides/release-notes.md` contains `### <target-version>` under both `## accrue` and `## accrue_admin`.
    - `accrue/guides/release-notes.md` mentions `accrue_portal` in the target-version family story rather than leaving the line implicitly two-package.
    - The deterministic gate bundle exits 0 without adding a live Stripe/provider-backed blocker to the required release lane.
  </acceptance_criteria>
  <done>The live release PR is audited against one exact target version, the deterministic gate bundle is captured in the canonical ledger, and the runbook plus release-note mirrors are aligned before any publish starts.</done>
</task>

<task type="auto">
  <name>Task 3: Drive the ordered linked publish and append canonical post-publish truth</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md, accrue/guides/release-notes.md, RELEASING.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — file being modified; append post-publish evidence only
    - RELEASING.md — primary path, fallback path, and D-10 recovery policy wording
    - .github/workflows/release-please.yml — exact release workflow and publish job names
    - .github/workflows/publish-hex.yml — fallback-only recovery workflow
    - scripts/ci/capture_linked_release_proof.sh — exact post-publish capture command from Task 1
    - scripts/ci/accrue_host_hex_smoke.sh — exact host Hex smoke command
    - scripts/ci/verify_release_notes_contract.sh — post-publish mirror gate to rerun after capture
    - accrue/guides/release-notes.md — public mirror that must still match the published line
    - accrue/CHANGELOG.md — public changelog mirror for the core package
    - accrue_admin/CHANGELOG.md — public changelog mirror for the admin package
    - accrue_portal/CHANGELOG.md — public changelog mirror for the portal package
  </read_first>
  <action>Implement D-08 through D-13 exactly. Merge the reviewed combined Release Please PR through the primary path in `RELEASING.md` and let `.github/workflows/release-please.yml` perform the ordered publish `accrue` first, then `accrue_admin`, then `accrue_portal`; do not use `.github/workflows/publish-hex.yml` unless the primary path has already failed and the fallback use is documented in `159-VERIFICATION.md`. Record the exact successful workflow `RUN_ID`, then run `bash scripts/ci/capture_linked_release_proof.sh --version <target-version> --run-id <release-please-run-id> --pr <release-pr-number> --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md`. After that, run `bash scripts/ci/accrue_host_hex_smoke.sh` and append a `Host Hex smoke` block to `159-VERIFICATION.md` with the exact command string, UTC timestamp, and exit result. Append or confirm `HexDocs availability` rows for `https://hexdocs.pm/accrue/readme.html`, `https://hexdocs.pm/accrue_admin/readme.html`, and `https://hexdocs.pm/accrue_portal/readme.html`. If the publish partially succeeds after `accrue` is already correct on Hex, retry the downstream package at the same version first; use `mix hex.publish --revert <target-version>` only for a clear immediate Hex mistake inside the documented revert window; otherwise retire the bad line and cut a new linked patch release forward with the recovery path documented in the ledger. Rerun `bash scripts/ci/verify_release_notes_contract.sh` after capture, and do not mark REL-03 complete while any one of tags, GitHub releases, Hex versions, HexDocs, package changelog headings, or release notes still disagrees with the target version.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/capture_linked_release_proof.sh --version &lt;target-version&gt; --run-id &lt;release-please-run-id&gt; --pr &lt;release-pr-number&gt; --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</automated>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/accrue_host_hex_smoke.sh && bash scripts/ci/verify_release_notes_contract.sh</automated>
    <automated>cd /Users/jon/projects/accrue && sh -c 'for pkg in accrue accrue_admin accrue_portal; do curl -fsSI "https://hexdocs.pm/${pkg}/readme.html" | grep -E "^HTTP/"; done' && rg -n 'RUN_ID:|Workflow job ordering|Git tags|GitHub releases|Hex API truth|HexDocs availability|Host Hex smoke' .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `159-VERIFICATION.md` contains one exact `RUN_ID:` tied to the successful `.github/workflows/release-please.yml` run for the target version.
    - `159-VERIFICATION.md` contains `Workflow job ordering`, `Git tags`, `GitHub releases`, `Hex API truth`, `HexDocs availability`, and `Host Hex smoke` sections after publish.
    - The `Workflow job ordering` table shows `release`, `publish-accrue`, `publish-accrue-admin`, and `publish-accrue-portal` succeeding in the documented order for the same run id.
    - The `Hex API truth` table records `latest_version = <target-version>` for `accrue`, `accrue_admin`, and `accrue_portal`.
    - The `Host Hex smoke` block records the exact `bash scripts/ci/accrue_host_hex_smoke.sh` command and a successful exit result; this block exists in addition to, not instead of, the three-package release truth rows.
    - If fallback or recovery is used, `159-VERIFICATION.md` explicitly records whether the executor retried downstream publish, used `.github/workflows/publish-hex.yml`, reverted, or retired-forwarded the line.
    - `bash scripts/ci/verify_release_notes_contract.sh` still exits 0 after the published proof is captured.
    - No completion claim is made if any one of git tags, GitHub releases, Hex versions, HexDocs, changelog headings, or release notes still disagrees with the target version.
  </acceptance_criteria>
  <done>The linked line is either published and publicly proven across all three packages, or a fully documented recovery path is recorded instead of a false success claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo and planning ledger -> GitHub Actions release workflow | Untrusted assumptions about which PR, ref, or run produced the publish can create split-brain release truth. |
| GitHub Actions secrets -> repo docs and verification artifacts | Release credentials exist outside the repo and must never be copied into scripts, ledgers, or logs. |
| published Hex / GitHub / HexDocs state -> planning mirrors | Public registry and release surfaces can diverge from changelogs, release notes, or runbook text if not explicitly reconciled. |
| upstream core publish -> downstream admin/portal publishes | Partial publish success can leave the linked line in an inconsistent state if downstream recovery is undocumented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Information Disclosure | `RELEASING.md`, GitHub workflow usage, `159-VERIFICATION.md` | mitigate | Keep `RELEASE_PLEASE_TOKEN` and `HEX_API_KEY` only in GitHub/Hex secret stores, record only PR numbers, run ids, URLs, and timestamps in the ledger, and never paste raw secrets or secret-bearing logs into proof artifacts. |
| T-159-02 | Tampering | `.release-please-manifest.json`, `mix.exs`, package changelogs, release notes | mitigate | Enforce one target version with `verify_release_pr_scope.sh`, the expanded `verify_release_manifest_alignment.sh`, and the release-notes contract before merge; stop instead of manually rationalizing multi-package drift. |
| T-159-03 | Repudiation | `159-VERIFICATION.md` | mitigate | Require the canonical append-only ledger to carry exact `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` values plus deterministic gate rows and post-publish capture tables so no publish claim rests on memory or prose alone. |
| T-159-04 | Tampering | `.github/workflows/release-please.yml`, `.github/workflows/publish-hex.yml` | mitigate | Preserve the strict `publish-accrue` -> `publish-accrue-admin` -> `publish-accrue-portal` DAG, keep `publish-hex.yml` fallback-only, and document any deviation or recovery step in the ledger before sign-off. |
| T-159-05 | Denial of Service | downstream admin/portal publish after core success | mitigate | Follow D-10 exactly: retry the downstream package at the same version first, use revert only for a clear immediate Hex mistake, otherwise retire and ship a new linked patch line forward with explicit proof notes. |
| T-159-06 | Spoofing | proof surface based on one mirror only | mitigate | Use the composite truth model from D-01 and D-03: no single manifest, changelog, GitHub release, or Hex page counts as success without reconciliation across the full artifact. |
</threat_model>

<verification>
Run the phase in three passes:
1. `bash scripts/ci/verify_release_manifest_alignment.sh` plus source assertions on `159-VERIFICATION.md`, `scripts/ci/capture_linked_release_proof.sh`, and `scripts/ci/README.md`.
2. `bash scripts/ci/verify_release_pr_scope.sh --pr <release-pr-number> --version <target-version>` plus the full deterministic gate bundle and release-notes verification.
3. `bash scripts/ci/capture_linked_release_proof.sh --version <target-version> --run-id <release-please-run-id> --pr <release-pr-number> --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md`, then `bash scripts/ci/accrue_host_hex_smoke.sh`, then `bash scripts/ci/verify_release_notes_contract.sh`.
</verification>

<success_criteria>
Phase 159 is complete only when the next linked release line after `1.3.0` is either fully published and proven through one canonical ledger across all three packages, or blocked with an explicitly documented recovery state instead of a false release-complete claim.
</success_criteria>

<output>
Create `.planning/phases/159-linked-release-readiness-publish-proof/159-01-SUMMARY.md` when done.
</output>

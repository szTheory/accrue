---
phase: 159-linked-release-readiness-publish-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
  - scripts/ci/verify_release_manifest_alignment.sh
  - scripts/ci/capture_linked_release_proof.sh
  - scripts/ci/README.md
  - RELEASING.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "Phase 159 has one canonical append-only release-truth ledger for accrue, accrue_admin, and accrue_portal."
    - "The deterministic release gate is recorded as real evidence for the current public line without inventing a next linked release candidate."
    - "REL-01 and REL-03 remain open until a real post-1.3.0 Release Please PR and successful release-please run exist."
  artifacts:
    - path: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      provides: "Canonical release ledger with deterministic gate rows and blocker state"
      contains: "PR_NUMBER:"
    - path: "scripts/ci/verify_release_manifest_alignment.sh"
      provides: "Three-package manifest and mix.exs lockstep verification"
      contains: "accrue_portal"
    - path: "scripts/ci/capture_linked_release_proof.sh"
      provides: "Post-publish proof capture spine with HexDocs checks"
      contains: "HexDocs availability"
    - path: "scripts/ci/README.md"
      provides: "REL gate map pointing at the Phase 159 ledger"
      contains: "159-VERIFICATION.md"
    - path: "RELEASING.md"
      provides: "Primary-path runbook that records identifiers in the current ledger and keeps manual publish fallback-only"
      contains: "publish-hex.yml"
  key_links:
    - from: "scripts/ci/verify_release_manifest_alignment.sh"
      to: ".release-please-manifest.json"
      via: "jq extraction for accrue, accrue_admin, and accrue_portal"
      pattern: "accrue_portal"
    - from: "scripts/ci/capture_linked_release_proof.sh"
      to: ".github/workflows/release-please.yml"
      via: "gh run view on one exact RUN_ID"
      pattern: "publish-accrue-portal"
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/verify_release_pr_scope.sh"
      via: "PR_NUMBER and TARGET_VERSION ledger fields"
      pattern: "PR_NUMBER:"
---

<objective>
Preserve the real Phase 159 result from 2026-05-31: the three-package readiness scaffolding and deterministic gate proof are complete, while live publish proof remains blocked until a post-`1.3.0` linked release line exists.

Purpose: Keep REL-02 as completed work, keep REL-01 and REL-03 honest, and prevent Phase 159 from claiming a release line that GitHub/Hex do not yet show.
Output:
- `159-VERIFICATION.md` as the canonical readiness-plus-blocker ledger
- three-package release verifiers and proof capture script aligned with `accrue_portal`
- release docs and CI gate map updated to reference the current Phase 159 ledger
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-RESEARCH.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-PATTERNS.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-01-SUMMARY.md
@RELEASING.md
@scripts/ci/README.md
@scripts/ci/verify_release_manifest_alignment.sh
@scripts/ci/verify_release_pr_scope.sh
@scripts/ci/verify_release_contract.sh
@scripts/ci/verify_release_notes_contract.sh
@scripts/ci/verify_package_docs.sh
@scripts/ci/verify_processor_support_matrix.sh
@scripts/ci/verify_adoption_proof_matrix.sh
@scripts/ci/accrue_host_uat.sh
@scripts/ci/accrue_host_hex_smoke.sh
@scripts/ci/capture_linked_release_proof.sh
@.github/workflows/release-please.yml
@.release-please-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the three-package readiness ledger and proof scripts</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md, scripts/ci/verify_release_manifest_alignment.sh, scripts/ci/capture_linked_release_proof.sh, scripts/ci/README.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md — locked decisions D-01 through D-07 and D-09
    - .planning/phases/159-linked-release-readiness-publish-proof/159-RESEARCH.md — fixed-schema proof expectations and `accrue_portal` coverage requirement
    - .planning/phases/159-linked-release-readiness-publish-proof/159-PATTERNS.md — append-only proof and three-package iteration patterns
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — file being modified; preserve append-only structure
    - scripts/ci/verify_release_manifest_alignment.sh — file being modified; expand to first-class `accrue_portal` coverage
    - scripts/ci/capture_linked_release_proof.sh — file being modified; keep the append-only capture spine
    - scripts/ci/README.md — file being modified; point REL ownership at Phase 159
  </read_first>
  <action>Implement the readiness scaffolding exactly as executed, per D-01, D-04, D-05, D-06, D-07, and D-09. Follow the `159-PATTERNS.md` analogs explicitly: use the `scripts/ci/verify_release_contract.sh` guard/fail-helper style when hardening `scripts/ci/verify_release_manifest_alignment.sh`, use the existing `scripts/ci/capture_linked_release_proof.sh` append-only proof write pattern when extending proof capture, and use the Phase 75 verification-ledger structure as the closest ledger analog for `159-VERIFICATION.md`. Make `scripts/ci/verify_release_manifest_alignment.sh` enforce one shared version across `accrue`, `accrue_admin`, and `accrue_portal`. Keep `.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md` as the single canonical ledger with the exact machine fields `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:` and the exact sections `## Release identifiers`, `## Pre-merge truth audit`, `## Deterministic gate bundle`, `## Publish execution`, `## Post-publish public truth`, `## Notes`, and `## Sign-off`. Extend `scripts/ci/capture_linked_release_proof.sh` so the post-publish capture format includes release-file snapshots and `HexDocs availability` for all three packages without inventing publish data when no run exists. Update `scripts/ci/README.md` so REL-01, REL-02, and REL-03 point to `159-VERIFICATION.md` and the three-script proof chain.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_release_manifest_alignment.sh</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'PR_NUMBER:|TARGET_VERSION:|RUN_ID:|## Release identifiers|## Deterministic gate bundle|## Post-publish public truth|## Sign-off' .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'accrue_portal|HexDocs availability|159-VERIFICATION\\.md|REL-01|REL-02|REL-03' scripts/ci/verify_release_manifest_alignment.sh scripts/ci/capture_linked_release_proof.sh scripts/ci/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/verify_release_manifest_alignment.sh` reads `.accrue_portal` from `.release-please-manifest.json` and the portal `@version` from `accrue_portal/mix.exs`.
    - `scripts/ci/verify_release_manifest_alignment.sh` exits non-zero if any one of the three package versions diverges from the shared manifest version.
    - `159-VERIFICATION.md` exists with the exact machine fields `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:` and the exact section headings named in the task action.
    - `scripts/ci/capture_linked_release_proof.sh` still iterates `accrue`, `accrue_admin`, and `accrue_portal` and includes a `HexDocs availability` section in its appended proof block.
    - `scripts/ci/README.md` references the Phase 159 ledger instead of an older linked-release artifact and names REL-01, REL-02, and REL-03 explicitly.
  </acceptance_criteria>
  <done>The release-proof scaffold, verifier coverage, and CI gate map all treat the three-package ledger as the source of release truth for Phase 159.</done>
</task>

<task type="auto">
  <name>Task 2: Capture deterministic gate evidence and the real blocker state without fabricating publish proof</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md, RELEASING.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — file being modified; append evidence and blocker rows instead of rewriting history
    - .planning/phases/159-linked-release-readiness-publish-proof/159-01-SUMMARY.md — executed result on 2026-05-31
    - .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md — locked decisions D-02, D-03, D-08, D-11, D-13, D-14
    - RELEASING.md — file being modified; keep the primary path and fallback distinction accurate
    - scripts/ci/verify_release_pr_scope.sh — exact pre-merge audit command
    - scripts/ci/verify_release_manifest_alignment.sh — three-package lockstep gate from Task 1
    - scripts/ci/verify_release_contract.sh — linked release contract gate
    - scripts/ci/verify_release_notes_contract.sh — release-notes gate
    - scripts/ci/verify_package_docs.sh — package docs gate
    - scripts/ci/verify_processor_support_matrix.sh — support-matrix gate
    - scripts/ci/verify_adoption_proof_matrix.sh — adoption-proof gate
    - scripts/ci/accrue_host_uat.sh — host integration gate
    - scripts/ci/accrue_host_hex_smoke.sh — host Hex smoke evidence source
    - .github/workflows/release-please.yml — serialized publish order that remains future-proof only
  </read_first>
  <action>Preserve the executed reality from 2026-05-31, per D-02, D-03, D-08, D-11, D-13, and D-14. Follow the `159-PATTERNS.md` analogs explicitly: append evidence rows in the same append-only style as `scripts/ci/capture_linked_release_proof.sh`, preserve the Phase 75 checklist/sign-off ledger shape for status rows, and keep `RELEASING.md` changes within the existing primary-path versus manual-fallback wording pattern. Run the deterministic REL-02 gate bundle and record the exact pass/fail rows in `159-VERIFICATION.md` under the canonical CI job ids `release-manifest-ssot`, `docs-contracts-shift-left`, `release-gate`, and `host-integration`. Query GitHub for a combined Release Please PR after `1.3.0`; if none exists, keep `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:` unpopulated and append blocker notes instead of placeholders. Record the real host Hex smoke failure and recovery notes instead of treating it as a publish success. Update `RELEASING.md` so maintainers record `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` in the current phase ledger, preserve the strict future publish order `accrue -> accrue_admin -> accrue_portal`, and keep `.github/workflows/publish-hex.yml` fallback-only rather than the default path.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_release_manifest_alignment.sh && bash scripts/ci/verify_release_contract.sh && bash scripts/ci/verify_release_notes_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/accrue_host_uat.sh</automated>
    <automated>cd /Users/jon/projects/accrue && gh pr list --repo szTheory/accrue --state open --json number,title,url</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'release-manifest-ssot|docs-contracts-shift-left|release-gate|host-integration|No open combined Release Please PR|RUN_ID:|Host Hex smoke|publish-hex\\.yml|accrue_admin|accrue_portal' .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md RELEASING.md</automated>
  </verify>
  <acceptance_criteria>
    - `159-VERIFICATION.md` contains explicit pass rows for the deterministic gate bundle and ties them to the named CI job ids instead of a generic summary.
    - `159-VERIFICATION.md` leaves `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:` blank when no post-`1.3.0` release line exists, and it explains that absence in dated blocker rows.
    - `159-VERIFICATION.md` records the host Hex smoke result as a failure/blocker note rather than a success claim.
    - `RELEASING.md` instructs maintainers to record exact identifiers in the current phase ledger and still treats `.github/workflows/publish-hex.yml` as fallback-only.
    - No task step claims REL-01 or REL-03 complete on the basis of the current `1.3.0` public line or a placeholder next-line identifier.
  </acceptance_criteria>
  <done>REL-02 is evidenced as completed work, and REL-01 plus REL-03 are left explicitly blocked on real external release state rather than fabricated proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo ledger -> GitHub release state | A local planning artifact can drift from the actual Release Please, tag, or Hex state if identifiers are invented or copied from the wrong line. |
| release verifiers -> three-package version files | Legacy two-package assumptions can omit `accrue_portal` and create split-brain release truth. |
| runbook wording -> maintainer publish behavior | If `RELEASING.md` implies the fallback path is primary, maintainers can bypass the ordered linked-release workflow and lose proof coherence. |
| host Hex smoke result -> completion claim | A failing consumer smoke run can be misreported as success if it is treated as optional narrative instead of a tracked proof surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Tampering | `159-VERIFICATION.md` release identifiers | mitigate | Treat missing post-`1.3.0` PR/run state as a blocking integrity failure: leave `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` blank and append dated blocker rows instead of placeholders. |
| T-159-02 | Tampering | `scripts/ci/verify_release_manifest_alignment.sh` | mitigate | Require `accrue`, `accrue_admin`, and `accrue_portal` to share the same manifest and `mix.exs` version before any later plan can claim release readiness. |
| T-159-03 | Repudiation | deterministic gate evidence | mitigate | Record exact command strings and CI job ids for REL-02 so later plans can distinguish real readiness proof from hand-written prose. |
| T-159-04 | Information Disclosure | `RELEASING.md`, `159-VERIFICATION.md` | mitigate | Record only PR numbers, run ids, timestamps, and URLs; never paste `RELEASE_PLEASE_TOKEN`, `HEX_API_KEY`, or secret-bearing logs into planning artifacts. |
| T-159-05 | Denial of Service | host Hex smoke result | mitigate | Keep host Hex smoke as a required evidence row and treat failure as blocking until a clean publish-proof context exists, per D-13. |
</threat_model>

<verification>
1. `bash scripts/ci/verify_release_manifest_alignment.sh`
2. `bash scripts/ci/verify_release_contract.sh`
3. `bash scripts/ci/verify_release_notes_contract.sh`
4. `bash scripts/ci/verify_package_docs.sh`
5. `bash scripts/ci/verify_processor_support_matrix.sh`
6. `bash scripts/ci/verify_adoption_proof_matrix.sh`
7. `bash scripts/ci/accrue_host_uat.sh`
8. `gh pr list --repo szTheory/accrue --state open --json number,title,url`
</verification>

<success_criteria>
The plan matches what actually happened on 2026-05-31: three-package readiness proof is preserved, REL-02 is evidenced, and Phase 159 does not pretend that a post-`1.3.0` release candidate or publish run already exists.
</success_criteria>

<output>
Create `.planning/phases/159-linked-release-readiness-publish-proof/159-01-SUMMARY.md` when done.
</output>

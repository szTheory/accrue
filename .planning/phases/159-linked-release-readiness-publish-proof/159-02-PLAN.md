---
phase: 159-linked-release-readiness-publish-proof
plan: "02"
type: execute
wave: 2
depends_on:
  - 159-01
files_modified:
  - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
autonomous: false
gap_closure: true
requirements:
  - REL-01
  - REL-03
must_haves:
  truths:
    - "A real linked release candidate after `1.3.0` is identified by exact `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` values."
    - "Release Please intent, workflow ordering, tags, GitHub releases, Hex versions, HexDocs, and host Hex smoke reconcile for one target version across all three packages."
    - "If any publish or proof surface disagrees, Phase 159 records the blocker and recovery path instead of a false completion claim."
  artifacts:
    - path: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      provides: "Canonical live publish-proof ledger for REL-01 and REL-03"
      contains: "Workflow job ordering"
  key_links:
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/verify_release_pr_scope.sh"
      via: "exact PR_NUMBER and TARGET_VERSION audit rows"
      pattern: "TARGET_VERSION:"
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/capture_linked_release_proof.sh"
      via: "exact RUN_ID-bound proof capture"
      pattern: "RUN_ID:"
    - from: ".planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md"
      to: "scripts/ci/accrue_host_hex_smoke.sh"
      via: "Host Hex smoke evidence block"
      pattern: "Host Hex smoke"
---

<objective>
Close the remaining Phase 159 work only when a real post-`1.3.0` linked release exists in GitHub and Hex.

Purpose: REL-01 and REL-03 require public release truth, not readiness scaffolding. This plan is intentionally blocked until a combined Release Please PR and successful `release-please.yml` publish run exist for the same target version.
Output:
- `159-VERIFICATION.md` updated with exact PR, version, run, workflow-ordering, tag, release, Hex, HexDocs, and host-smoke evidence
- truthful REL-01/REL-03 completion or an explicit recovery/blocker record for the live linked line
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-RESEARCH.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-01-SUMMARY.md
@RELEASING.md
@scripts/ci/verify_release_pr_scope.sh
@scripts/ci/capture_linked_release_proof.sh
@scripts/ci/accrue_host_hex_smoke.sh
@scripts/ci/verify_release_notes_contract.sh
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Resume only when real live release identifiers exist</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — current blocker ledger; do not override the 2026-05-31 evidence
    - .planning/phases/159-linked-release-readiness-publish-proof/159-01-SUMMARY.md — completed readiness scaffolding and blocker state
    - .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md — locked decisions D-01, D-03, D-04, D-08, D-10, D-12, D-13
    - RELEASING.md — primary linked-release flow and fallback-only recovery path
    - .github/workflows/release-please.yml — required successful workflow source for `RUN_ID`
  </read_first>
  <what-built>Phase 159 already has REL-02 readiness proof. This gate prevents REL-01 and REL-03 from running against imagined release state.</what-built>
  <how-to-verify>
    1. Confirm GitHub has a real combined Release Please PR for a target version greater than `1.3.0`.
    2. Confirm the same linked line has a successful `.github/workflows/release-please.yml` run that performed the ordered publish.
    3. Resume with the exact `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID` from that real release line.
  </how-to-verify>
  <action>Block this plan until GitHub exposes a real combined Release Please PR and a successful publish run for the same target version, per D-01, D-03, D-04, and D-12. Follow the `159-PATTERNS.md` analogs explicitly: use the existing `scripts/ci/capture_linked_release_proof.sh` PR/version/run argument contract as the identifier shape, and preserve the Phase 75 verification-ledger pattern by appending only dated status/proof rows after identifiers are validated. Do not supply placeholders, inferred values, or values from the already-published `1.3.0` line. If that external state still does not exist, leave the plan blocked and keep `159-VERIFICATION.md` unchanged.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && gh pr list --repo szTheory/accrue --state open --json number,title,headRefName,url | jq -e '[.[] | select((.title | test(\"release|Release\")) and (.title | test(\"1\\\\.3\\\\.0\") | not))] | length &gt; 0'</automated>
    <automated>cd /Users/jon/projects/accrue && test -n \"${PR_NUMBER:-}\" && test -n \"${TARGET_VERSION:-}\" && test -n \"${RUN_ID:-}\" && test \"$(printf '%s\n%s\n' \"1.3.0\" \"$TARGET_VERSION\" | sort -V | tail -n 1)\" = \"$TARGET_VERSION\" && test \"$TARGET_VERSION\" != \"1.3.0\"</automated>
  </verify>
  <acceptance_criteria>
    - The resume signal includes one exact `PR_NUMBER`, one exact `TARGET_VERSION` greater than `1.3.0`, and one exact successful `RUN_ID`.
    - The identifiers refer to the same linked release line across `accrue`, `accrue_admin`, and `accrue_portal`.
    - No placeholder values or copied identifiers from the `1.3.0` line are accepted.
  </acceptance_criteria>
  <done>The plan is resumed only with concrete live release identifiers, or it remains blocked without any false progress claim.</done>
  <resume-signal>Type `approved PR_NUMBER=&lt;n&gt; TARGET_VERSION=&lt;x.y.z&gt; RUN_ID=&lt;id&gt;` or describe why the next linked release line still does not exist.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validate the live PR and publish run against the exact identifiers</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — file being modified; append the new live-release block after the existing 2026-05-31 blocker evidence
    - .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md — locked decisions D-01, D-04, D-08, D-12
    - RELEASING.md — expected ordered publish flow and recovery language
    - scripts/ci/verify_release_pr_scope.sh — exact PR scope contract
    - .github/workflows/release-please.yml — expected workflow name and publish job ordering
  </read_first>
  <action>Using the checkpoint-provided `PR_NUMBER`, `TARGET_VERSION`, and `RUN_ID`, validate the live release candidate per D-01, D-04, D-08, and D-12. Follow the `159-PATTERNS.md` analogs explicitly: use `scripts/ci/verify_release_pr_scope.sh` as the pre-merge contract verifier, use the existing `scripts/ci/capture_linked_release_proof.sh` exact-run lookup pattern for `gh run view`, and append ledger rows in the existing proof-capture style rather than creating a second proof format. Run `bash scripts/ci/verify_release_pr_scope.sh --pr <PR_NUMBER> --version <TARGET_VERSION>`. Use `gh pr view` to confirm the PR is the combined linked release line for all three packages and `gh run view <RUN_ID>` to confirm the successful workflow is `.github/workflows/release-please.yml`. Append a new dated proof block to `159-VERIFICATION.md` that records the exact identifiers, PR URL, workflow URL, job conclusions, and the fact that the line is later than `1.3.0`. Do not backfill or rewrite the prior blocker rows from 2026-05-31.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_release_pr_scope.sh --pr &lt;PR_NUMBER&gt; --version &lt;TARGET_VERSION&gt;</automated>
    <automated>cd /Users/jon/projects/accrue && gh pr view &lt;PR_NUMBER&gt; --repo szTheory/accrue --json number,title,url,files,mergeCommit</automated>
    <automated>cd /Users/jon/projects/accrue && gh run view &lt;RUN_ID&gt; --repo szTheory/accrue --json databaseId,url,workflowName,conclusion,jobs</automated>
  </verify>
  <acceptance_criteria>
    - `verify_release_pr_scope.sh` exits 0 for the exact `PR_NUMBER` and `TARGET_VERSION`.
    - `gh pr view` shows one combined linked release PR for `accrue`, `accrue_admin`, and `accrue_portal`.
    - `gh run view` shows `.github/workflows/release-please.yml` with `conclusion=success` for the exact `RUN_ID`.
    - `159-VERIFICATION.md` contains the exact `PR_NUMBER:`, `TARGET_VERSION:`, and `RUN_ID:` values for the live linked line in a new dated block.
    - `159-VERIFICATION.md` preserves the prior blocker evidence and does not overwrite it.
  </acceptance_criteria>
  <done>The ledger contains a verified live release candidate and successful publish run identity for one exact target version after `1.3.0`.</done>
</task>

<task type="auto">
  <name>Task 3: Capture canonical post-publish truth and close or re-block REL-01 and REL-03</name>
  <files>.planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md — file being modified; append post-publish proof and sign-off state
    - .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md — locked decisions D-08 through D-13
    - RELEASING.md — primary path and recovery policy
    - scripts/ci/capture_linked_release_proof.sh — exact proof capture command
    - scripts/ci/accrue_host_hex_smoke.sh — required consumer smoke proof
    - scripts/ci/verify_release_notes_contract.sh — post-publish mirror contract
    - .github/workflows/release-please.yml — expected `release`, `publish-accrue`, `publish-accrue-admin`, `publish-accrue-portal` order
    - .github/workflows/publish-hex.yml — fallback-only recovery path if the primary run already failed
  </read_first>
  <action>Capture the live publish truth per D-08, D-10, D-11, D-12, and D-13. Follow the `159-PATTERNS.md` analogs explicitly: reuse `scripts/ci/capture_linked_release_proof.sh` as the append-only proof spine, preserve `.github/workflows/release-please.yml` serialized publish DAG naming, and keep `.github/workflows/publish-hex.yml` fallback-only using the existing `RELEASING.md` recovery wording. Run `bash scripts/ci/capture_linked_release_proof.sh --version <TARGET_VERSION> --run-id <RUN_ID> --pr <PR_NUMBER> --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md`, then run `bash scripts/ci/accrue_host_hex_smoke.sh` in a clean publish-proof context and rerun `bash scripts/ci/verify_release_notes_contract.sh`. Ensure the appended proof block includes workflow job ordering, git tags, GitHub releases, Hex API truth, release file snapshot, HexDocs availability, and Host Hex smoke. If any proof surface fails or a partial publish needs recovery, record the exact failure and recovery step in the ledger per D-10 and D-11 and leave REL-01/REL-03 incomplete; only mark the sign-off complete when all three packages and all proof surfaces agree on the same target version.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/capture_linked_release_proof.sh --version &lt;TARGET_VERSION&gt; --run-id &lt;RUN_ID&gt; --pr &lt;PR_NUMBER&gt; --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</automated>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/accrue_host_hex_smoke.sh && bash scripts/ci/verify_release_notes_contract.sh</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'Workflow job ordering|Git tags|GitHub releases|Hex API truth|Release file snapshot|HexDocs availability|Host Hex smoke|REL-01|REL-03' .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `159-VERIFICATION.md` contains `Workflow job ordering`, `Git tags`, `GitHub releases`, `Hex API truth`, `Release file snapshot`, `HexDocs availability`, and `Host Hex smoke` sections for the exact `TARGET_VERSION`.
    - The workflow-ordering proof shows `release`, `publish-accrue`, `publish-accrue-admin`, and `publish-accrue-portal` succeeding in serialized order for the exact `RUN_ID`.
    - The proof block shows the same `TARGET_VERSION` across `accrue`, `accrue_admin`, and `accrue_portal` in Hex, tags, and GitHub releases.
    - `bash scripts/ci/accrue_host_hex_smoke.sh` and `bash scripts/ci/verify_release_notes_contract.sh` both exit 0 before REL-01 and REL-03 are marked complete.
    - If any proof surface fails, `159-VERIFICATION.md` records the exact failed surface and recovery step, and REL-01/REL-03 remain incomplete.
  </acceptance_criteria>
  <done>The ledger either proves the real linked release end-to-end or preserves a concrete recovery state without any false publish-success claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied identifiers -> GitHub and Hex state | A wrong PR number, target version, or run id can point the proof ledger at the wrong release line. |
| GitHub workflow outputs -> planning ledger | A successful workflow can still be the wrong workflow or wrong run if it is not tied back to the exact PR/version pair. |
| post-publish public surfaces -> completion claim | Tags, GitHub releases, Hex, HexDocs, and release notes can drift or update at different times. |
| core publish success -> downstream package publish success | A partial publish can leave the linked line inconsistent if recovery is not documented precisely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-06 | Spoofing | `PR_NUMBER`, `TARGET_VERSION`, `RUN_ID` inputs | mitigate | Block the plan until exact live identifiers exist, then validate them with `gh pr view`, `gh run view`, and `verify_release_pr_scope.sh` before writing any success rows. |
| T-159-07 | Tampering | `159-VERIFICATION.md` live proof block | mitigate | Append a new dated block only for the exact validated identifiers; preserve the 2026-05-31 blocker rows and never rewrite them to look successful. |
| T-159-08 | Repudiation | release completion claim | mitigate | Require workflow ordering, tags, GitHub releases, Hex API truth, HexDocs availability, and host Hex smoke in the same ledger before checking REL-01 or REL-03 complete. |
| T-159-09 | Denial of Service | partial linked publish recovery | mitigate | Follow D-10 exactly: retry downstream first, use `publish-hex.yml` only as documented fallback, and record retire-forward recovery if a linked line must be abandoned. |
| T-159-10 | Information Disclosure | release logs and proof capture | mitigate | Keep only URLs, timestamps, package versions, and job conclusions in the ledger; do not copy secret-bearing workflow logs or credentials into planning artifacts. |
</threat_model>

<verification>
1. `bash scripts/ci/verify_release_pr_scope.sh --pr <PR_NUMBER> --version <TARGET_VERSION>`
2. `gh pr view <PR_NUMBER> --repo szTheory/accrue --json number,title,url,files,mergeCommit`
3. `gh run view <RUN_ID> --repo szTheory/accrue --json databaseId,url,workflowName,conclusion,jobs`
4. `bash scripts/ci/capture_linked_release_proof.sh --version <TARGET_VERSION> --run-id <RUN_ID> --pr <PR_NUMBER> --output .planning/phases/159-linked-release-readiness-publish-proof/159-VERIFICATION.md`
5. `bash scripts/ci/accrue_host_hex_smoke.sh`
6. `bash scripts/ci/verify_release_notes_contract.sh`
</verification>

<success_criteria>
Phase 159 can only close when one real post-`1.3.0` linked release line is proven end-to-end across GitHub, Hex, HexDocs, release notes, and host smoke. Until then, this plan stays blocked rather than inventing release truth.
</success_criteria>

<output>
Create `.planning/phases/159-linked-release-readiness-publish-proof/159-02-SUMMARY.md` when done.
</output>

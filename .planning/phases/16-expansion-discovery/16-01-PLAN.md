---
phase: 16-expansion-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md
  - accrue/test/accrue/docs/expansion_discovery_test.exs
  - .planning/phases/16-expansion-discovery/16-VALIDATION.md
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
must_haves:
  truths:
    - "Maintainers can open one checked-in expansion recommendation artifact and see decision-quality recommendations for tax, revenue/export, additional processors, and org or multi-tenant billing."
    - "Each recommendation states user value, architecture impact, risk, prerequisites, and placement as Next milestone, Backlog, or Planted seed."
    - "The recommendation preserves Accrue's Stripe-first, host-owned architecture and documents migration-path notes instead of proposing core billing rewrites."
  artifacts:
    - path: ".planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md"
      provides: "Canonical ranked recommendation artifact for all four expansion candidates"
    - path: "accrue/test/accrue/docs/expansion_discovery_test.exs"
      provides: "Executable docs contract for the Phase 16 recommendation artifact"
    - path: ".planning/phases/16-expansion-discovery/16-VALIDATION.md"
      provides: "Per-task verification map aligned to the recommendation artifact and docs contract"
  key_links:
    - from: ".planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md"
      to: "accrue/test/accrue/docs/expansion_discovery_test.exs"
      via: "direct file-read docs contract"
      pattern: "16-EXPANSION-RECOMMENDATION.md"
    - from: ".planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md"
      to: ".planning/phases/16-expansion-discovery/16-VALIDATION.md"
      via: "task verification map and grep vocabulary"
      pattern: "Stripe Tax|Revenue Recognition|Planted seed|Sigra"
---

<objective>
Create the canonical Phase 16 recommendation artifact and lock it with a narrow docs contract.

Purpose: Satisfy DISC-01 through DISC-05 with one checked-in decision source that recommends the next mature-library expansion without weakening the current Stripe-first, host-owned architecture.
Output: A final recommendation document, an ExUnit docs contract for its required sections and strings, and a validation file updated to point at the new artifact and verification commands.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-expansion-discovery/16-RESEARCH.md
@.planning/phases/16-expansion-discovery/16-VALIDATION.md
@.planning/phases/16-expansion-discovery/16-PATTERNS.md
@.planning/phases/15-trust-hardening/15-TRUST-REVIEW.md
@.planning/phases/15-trust-hardening/15-VERIFICATION.md
@accrue/test/accrue/docs/trust_review_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical expansion recommendation artifact and its docs contract</name>
  <read_first>
    .planning/phases/16-expansion-discovery/16-RESEARCH.md
    .planning/phases/16-expansion-discovery/16-PATTERNS.md
    .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md (if present)
    accrue/test/accrue/docs/expansion_discovery_test.exs (if present)
    .planning/phases/15-trust-hardening/15-TRUST-REVIEW.md
    accrue/test/accrue/docs/trust_review_test.exs
    accrue/guides/custom_processors.md
    accrue/lib/accrue/processor.ex
    accrue/lib/accrue/billing/customer.ex
    accrue/lib/accrue/billable.ex
  </read_first>
  <files>.planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md, accrue/test/accrue/docs/expansion_discovery_test.exs</files>
  <acceptance_criteria>
    - `test -f .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md` succeeds.
    - `rg -n "^## Ranked Recommendation$|^## Assumptions Log$|^## Open Questions$|^## Recommendation Rationale$|^## Migration Path Notes$" .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md` returns matches.
    - `rg -n "Stripe Tax|Revenue Recognition|Sigma|Data Pipeline|Official second processor adapter|Organization / multi-tenant billing|Sigra|owner_type|owner_id|custom processor|Next milestone|Backlog|Planted seed" .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md` returns matches.
    - `rg -n "cross-tenant billing leakage|wrong-audience finance exports|tax rollout correctness|processor-boundary downgrade" .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md` returns matches.
    - `rg -n "user value|architecture impact|risk|prerequisites|Next milestone|Backlog|Planted seed|Stripe-first|host-owned" accrue/test/accrue/docs/expansion_discovery_test.exs` returns matches.
    - `cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace` exits 0.
  </acceptance_criteria>
  <action>Create `.planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md` as the single canonical Phase 16 output, following the checked-in artifact style from `15-TRUST-REVIEW.md` but with decision-ranking content from `16-RESEARCH.md`. Give it frontmatter with `phase: 16`, `slug: expansion-discovery`, `status: recommended`, `created: 2026-04-17`, and `requirements` listing `DISC-01` through `DISC-05`. Add these exact top-level sections: `## Recommendation Rationale`, `## Ranked Recommendation`, `## Migration Path Notes`, `## Assumptions Log`, `## Open Questions`, `## Security And Boundary Checks`, `### Verification Runs`, and `## Sign-Off`. In `## Ranked Recommendation`, keep a four-row table with these exact candidate names and outcome labels: `Stripe Tax support` -> `Next milestone`, `Organization / multi-tenant billing` -> `Backlog`, `Revenue recognition / exports` -> `Backlog`, and `Official second processor adapter` -> `Planted seed`. Every row must include concrete text for `User Value`, `Architecture Impact`, `Risk`, and `Prerequisites`. In `## Migration Path Notes`, spell out that Phase 16 does not change the core billing API, schema, or processor abstraction; it may only document future migration-path notes such as customer location capture for Stripe Tax, recurring-item migration for existing subscriptions, host-authorized export delivery, row-scoped org tenancy with `owner_type` and `owner_id`, and separate-package thinking for any future processor beyond Stripe. In `## Security And Boundary Checks`, add four bullets using these exact phrases: `cross-tenant billing leakage`, `wrong-audience finance exports`, `tax rollout correctness`, and `processor-boundary downgrade`, each paired with the recommended constraint or prerequisite. Create `accrue/test/accrue/docs/expansion_discovery_test.exs` as a narrow file-read docs contract that asserts the recommendation artifact exists, contains all four candidate rows, contains the ranking vocabulary `Next milestone`, `Backlog`, and `Planted seed`, includes the strings `user value`, `architecture impact`, `risk`, and `prerequisites`, preserves `Stripe-first`, `host-owned`, `custom processor`, `Sigra`, `owner_type`, and `owner_id`, and mentions the four security phrases named above. Do not modify production Elixir billing code, schemas, or processor contracts in this plan.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace</automated>
  </verify>
  <done>The repo contains one canonical Phase 16 recommendation artifact with ranked outcomes for all four expansion candidates, explicit migration-path notes that preserve the Stripe-first host-owned architecture, and an executable docs contract that fails if any required recommendation dimension or boundary phrase disappears.</done>
</task>

<task type="auto">
  <name>Task 2: Align the Phase 16 validation contract with the recommendation artifact</name>
  <read_first>
    .planning/phases/16-expansion-discovery/16-VALIDATION.md
    .planning/phases/16-expansion-discovery/16-RESEARCH.md
    .planning/phases/16-expansion-discovery/16-PATTERNS.md
    .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md
    accrue/test/accrue/docs/expansion_discovery_test.exs
  </read_first>
  <files>.planning/phases/16-expansion-discovery/16-VALIDATION.md</files>
  <acceptance_criteria>
    - `rg -n "16-EXPANSION-RECOMMENDATION\\.md|expansion_discovery_test\\.exs|mix test test/accrue/docs/expansion_discovery_test\\.exs --trace" .planning/phases/16-expansion-discovery/16-VALIDATION.md` returns matches.
    - `rg -n "16-01-01|16-01-02|DISC-01|DISC-02|DISC-03|DISC-04|DISC-05" .planning/phases/16-expansion-discovery/16-VALIDATION.md` returns matches.
    - `rg -n "cross-tenant billing leakage|wrong-audience finance exports|tax rollout correctness|processor-boundary downgrade risks" .planning/phases/16-expansion-discovery/16-VALIDATION.md` returns matches.
    - `rg -n "Wave 0" .planning/phases/16-expansion-discovery/16-VALIDATION.md` finds only the checklist heading and no `MISSING` or unresolved `no W0` language.
    - `rg -n "Next milestone|Backlog|Planted seed|Stripe Tax|Revenue Recognition|Sigra|custom processor" .planning/phases/16-expansion-discovery/16-VALIDATION.md` returns matches.
  </acceptance_criteria>
  <action>Update `.planning/phases/16-expansion-discovery/16-VALIDATION.md` so its quick command, per-task verification map, and wave checklist point at the new canonical artifact and ExUnit docs contract instead of treating Phase 16 as unresolved Wave 0 work. Keep the existing frontmatter and table structure, but change the quick command to `rg -n "DISC-0[1-5]|Next milestone|Backlog|Planted seed" .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md .planning/phases/16-expansion-discovery/16-VALIDATION.md`, and change the automated command for the recommendation artifact to `cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace`. Rewrite the `Wave 0 Requirements` checklist into completed setup items that name the actual contract file `accrue/test/accrue/docs/expansion_discovery_test.exs` and the canonical artifact `16-EXPANSION-RECOMMENDATION.md`. In the per-task verification map, keep the five requirement rows `DISC-01` through `DISC-05`, but make their automated commands point at the canonical artifact or the docs contract with exact grep strings for `Stripe Tax`, `Revenue Recognition`, `Sigma`, `Data Pipeline`, `Sigra`, `owner_type`, `owner_id`, `custom processor`, `Next milestone`, `Backlog`, and `Planted seed`. Add threat references or secure-behavior text that explicitly names `cross-tenant billing leakage`, `wrong-audience finance exports`, `tax rollout correctness`, and `processor-boundary downgrade risks`. Do not add host runtime tests or any command that depends on implementing the future features themselves.</action>
  <verify>
    <automated>rg -n "16-EXPANSION-RECOMMENDATION.md|expansion_discovery_test.exs|DISC-0[1-5]|cross-tenant billing leakage|wrong-audience finance exports|tax rollout correctness|processor-boundary downgrade risks" .planning/phases/16-expansion-discovery/16-VALIDATION.md</automated>
  </verify>
  <done>The validation contract now points at the final Phase 16 artifact and docs test, the verification map covers all five DISC requirements with exact commands, and the unresolved Wave 0 framing has been replaced with concrete checked-in verification steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recommendation artifact -> future phase planners | Phase 16 conclusions will steer milestone scope and must not misstate architecture or risk. |
| finance recommendation -> export audiences | Planning language can normalize unsafe export surfaces if audience boundaries are vague. |
| org billing recommendation -> tenant boundaries | Planning language can under-specify tenant isolation and create future cross-tenant leakage risk. |
| processor recommendation -> core billing contract | Planning language can pressure Accrue into weakest-common-denominator abstractions that degrade the Stripe-first boundary. |

Policy: High-severity threats are blocking for this phase's recommendation artifact and cannot be accepted without a concrete mitigation recorded in the checked-in docs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | T | tax recommendation and migration notes | mitigate | Require explicit `tax rollout correctness` language covering customer location capture, disabled automatic tax states, and migration handling for existing recurring items. |
| T-16-02 | I | revenue/export recommendation | mitigate | Require explicit `wrong-audience finance exports` language that keeps exports host-authorized and prefers Stripe-owned reporting paths before app-level downloads. |
| T-16-03 | I/E | org or multi-tenant recommendation | mitigate | Require explicit `cross-tenant billing leakage` language plus row-scoped tenancy, `owner_type` or `owner_id`, and Sigra gating in the canonical recommendation artifact. |
| T-16-04 | T | processor recommendation | mitigate | Require explicit `processor-boundary downgrade` language that keeps second-processor work as a `Planted seed`, preserves `custom processor` escape-hatch wording, and forbids core parity promises. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace` and `rg -n "16-EXPANSION-RECOMMENDATION.md|DISC-0[1-5]|Next milestone|Backlog|Planted seed" .planning/phases/16-expansion-discovery/16-VALIDATION.md`.
</verification>

<success_criteria>
Phase 16 has one checked-in recommendation artifact with explicit risk and prerequisite language for all four expansion candidates, backed by a narrow ExUnit docs contract and a validation map that references the real artifact and commands.
</success_criteria>

<output>
After completion, create `.planning/phases/16-expansion-discovery/16-01-SUMMARY.md`
</output>

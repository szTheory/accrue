---
phase: 16-expansion-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - .planning/phases/16-expansion-discovery/16-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
must_haves:
  truths:
    - "Maintainers can verify that the Phase 16 recommendation artifact satisfies the phase goal, must-haves, and all five DISC requirements."
    - "The roadmap and project records preserve the recommendation outcome as next milestone, backlog, and planted seed guidance for later planning."
    - "No planning record implies that tax, revenue/export, processor, or org billing was implemented in v1.2."
  artifacts:
    - path: ".planning/phases/16-expansion-discovery/16-VERIFICATION.md"
      provides: "Artifact-centric verification report for the recommendation and docs contract"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16 plan list and durable phase-detail summary"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement traceability updated to reflect Phase 16 recommendation outcomes"
    - path: ".planning/PROJECT.md"
      provides: "Project-level next milestone, backlog, and planted-seed guidance"
  key_links:
    - from: ".planning/phases/16-expansion-discovery/16-VERIFICATION.md"
      to: ".planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md"
      via: "artifact verification evidence"
      pattern: "Ranked Recommendation"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/PROJECT.md"
      via: "future milestone and product expansion recording"
      pattern: "TAX-01|REV-01|PROC-08|ORG-01"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md"
      via: "phase detail and plan list reference"
      pattern: "Expansion Discovery"
---

<objective>
Verify the Phase 16 recommendation artifact and record its ranking outcome in the durable planning sources.

Purpose: Close DISC-01 through DISC-05 by proving the recommendation artifact meets the phase goal and by persisting the next milestone, backlog, and planted-seed guidance in roadmap, requirements, and project records.
Output: A verification report plus roadmap, requirements, and project updates that preserve the ranking outcome without implying implementation work happened in v1.2.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md
@.planning/phases/16-expansion-discovery/16-VALIDATION.md
@.planning/phases/16-expansion-discovery/16-RESEARCH.md
@.planning/phases/16-expansion-discovery/16-PATTERNS.md
@.planning/phases/15-trust-hardening/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 16 verification report from the recommendation artifact</name>
  <read_first>
    .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md
    .planning/phases/16-expansion-discovery/16-VALIDATION.md
    .planning/phases/16-expansion-discovery/16-VERIFICATION.md (if present)
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/15-trust-hardening/15-VERIFICATION.md
    accrue/test/accrue/docs/expansion_discovery_test.exs
  </read_first>
  <files>.planning/phases/16-expansion-discovery/16-VERIFICATION.md</files>
  <acceptance_criteria>
    - `test -f .planning/phases/16-expansion-discovery/16-VERIFICATION.md` succeeds.
    - `rg -n "^# Phase 16: Expansion Discovery Verification Report$|^## Goal Achievement$|^### Observable Truths$|^### Required Artifacts$|^### Key Link Verification$|^### Requirements Coverage$" .planning/phases/16-expansion-discovery/16-VERIFICATION.md` returns matches.
    - `rg -n "16-EXPANSION-RECOMMENDATION.md|expansion_discovery_test.exs|DISC-01|DISC-02|DISC-03|DISC-04|DISC-05|Next milestone|Backlog|Planted seed" .planning/phases/16-expansion-discovery/16-VERIFICATION.md` returns matches.
    - `rg -n "cross-tenant billing leakage|wrong-audience finance exports|tax rollout correctness|processor-boundary downgrade" .planning/phases/16-expansion-discovery/16-VERIFICATION.md` returns matches.
    - `rg -n "tax rollout correctness" .planning/phases/16-expansion-discovery/16-VERIFICATION.md .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md` returns matches.
    - `cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace` exits 0.
  </acceptance_criteria>
  <action>Create `.planning/phases/16-expansion-discovery/16-VERIFICATION.md` using the artifact-centric verification structure from `15-VERIFICATION.md`: frontmatter, `## Goal Achievement`, `### Observable Truths`, `### Required Artifacts`, `### Key Link Verification`, `### Requirements Coverage`, and a short concluding summary. Verify only artifact-level truths for this discovery phase. The observable truths must explicitly prove these four outcomes: the canonical recommendation artifact covers tax, revenue/export, second processor, and org or multi-tenant options; each option includes `User Value`, `Architecture Impact`, `Risk`, and `Prerequisites`; the ranking places exactly one candidate in `Next milestone`, two in `Backlog`, and one in `Planted seed`; and no artifact claims that the core billing API, schema, or processor abstraction changed in v1.2. In the requirements table, map `DISC-01` through `DISC-05` to the recommendation artifact and docs contract. In the key-link table, verify the link from `16-EXPANSION-RECOMMENDATION.md` to `accrue/test/accrue/docs/expansion_discovery_test.exs` and from `16-VALIDATION.md` to the same docs contract. Include one brief `Anti-Patterns Found` row only if an artifact still implies implementation instead of recommendation; otherwise state that no phase-blocking gaps were found. Mention the four security phrases `cross-tenant billing leakage`, `wrong-audience finance exports`, `tax rollout correctness`, and `processor-boundary downgrade` in either the observable-truth or key-link evidence text so the verification report proves those concerns survived from research into the checked-in output, and call out `tax rollout correctness` specifically as the durable evidence that legacy recurring-item migration and customer-location prerequisites were preserved.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace</automated>
  </verify>
  <done>The phase directory contains a verification report that proves the recommendation artifact and docs contract satisfy all must-haves and DISC requirements without claiming any feature implementation landed in v1.2.</done>
</task>

<task type="auto">
  <name>Task 2: Persist the ranking outcome in roadmap, requirements, and project records</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/phases/16-expansion-discovery/16-EXPANSION-RECOMMENDATION.md
    .planning/phases/16-expansion-discovery/16-VALIDATION.md
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <acceptance_criteria>
    - `rg -n "\\*\\*Plans:\\*\\* 2 plans|16-01-PLAN.md|16-02-PLAN.md" .planning/ROADMAP.md` returns matches.
    - `rg -n "Stripe Tax support|Organization / multi-tenant billing|Revenue recognition / exports|Official second processor adapter|Next milestone|Backlog|Planted seed" .planning/ROADMAP.md .planning/PROJECT.md` returns matches.
    - `rg -n "\\[x\\] \\*\\*DISC-01\\*\\*|\\[x\\] \\*\\*DISC-02\\*\\*|\\[x\\] \\*\\*DISC-03\\*\\*|\\[x\\] \\*\\*DISC-04\\*\\*|\\[x\\] \\*\\*DISC-05\\*\\*" .planning/REQUIREMENTS.md` returns matches.
    - `rg -n "TAX-01|REV-01|PROC-08|ORG-01" .planning/REQUIREMENTS.md .planning/PROJECT.md` returns matches.
    - `rg -n "tax rollout correctness|legacy recurring-item migration|customer location" .planning/PROJECT.md .planning/REQUIREMENTS.md` returns matches.
    - `rg -n "recommendation|ranked|no implementation|decision-quality" .planning/ROADMAP.md .planning/PROJECT.md` returns matches.
  </acceptance_criteria>
  <action>Update `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/PROJECT.md` so the Phase 16 ranking survives outside the phase directory. In `.planning/ROADMAP.md`, set `**Plans:** 2 plans` in the Phase 16 detail block and add these exact plan list bullets under Phase 16: `16-01-PLAN.md — Create the canonical expansion recommendation artifact and its docs contract.` and `16-02-PLAN.md — Verify the recommendation artifact and persist the ranking outcome in planning records.` Do not mark the phase complete in this planning step. In `.planning/REQUIREMENTS.md`, keep the discovery requirements in the v1.2 section but update the traceability table so `DISC-01` through `DISC-05` point to `Phase 16` once execution is done; in the future or product expansion section, preserve `TAX-01`, `REV-01`, `PROC-08`, and `ORG-01` and add brief wording that matches the recommendation outcome: Stripe Tax is the next milestone candidate, organization or multi-tenant billing and revenue/export remain backlog candidates with prerequisites, and official second-processor work remains a planted seed. Preserve `tax rollout correctness` in the durable planning text by explicitly carrying forward legacy recurring-item migration and customer-location prerequisites as recommendation-only conditions for any future tax milestone. In `.planning/PROJECT.md`, add one project-level key decision or current-milestone note that states the ranking outcome in plain language: `Stripe Tax support` is the recommended next implementation milestone, `Organization / multi-tenant billing` and `Revenue recognition / exports` stay in backlog pending prerequisites, and `Official second processor adapter` stays a planted seed because Accrue remains Stripe-first and host-owned. Make every new sentence explicit that these are recommendations and planning outputs, not implemented features. Do not add new schema, processor, or API promises to any of these planning records.</action>
  <verify>
    <automated>rg -n "\\*\\*Plans:\\*\\* 2 plans|16-01-PLAN.md|16-02-PLAN.md|Stripe Tax support|Organization / multi-tenant billing|Revenue recognition / exports|Official second processor adapter|TAX-01|REV-01|PROC-08|ORG-01" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md</automated>
  </verify>
  <done>The roadmap, requirements, and project records preserve the Phase 16 ranking as recommendation-only planning guidance, with Stripe Tax positioned for the next milestone, org and revenue/export held in backlog, and second-processor work preserved as a planted seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase verification -> roadmap and requirements records | Verification evidence must carry into durable planning sources without mutating the scope into implementation promises. |
| project-level milestone guidance -> future execution plans | Project records influence later milestone planning and must preserve the Stripe-first boundary. |
| tax recommendation -> future rollout guidance | Durable records can under-specify customer-location or legacy-recurring migration prerequisites and create tax rollout mistakes later. |
| backlog and seed wording -> future finance or tenancy work | Wording can accidentally authorize unsafe exports or weak tenancy assumptions if it is vague. |

Policy: High-severity threats are blocking for this phase's planning outputs and cannot be accepted without concrete wording that preserves the documented boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | T | roadmap and project ranking records | mitigate | Record the outcome as recommendation-only language and explicitly state that no core billing API, schema, or processor abstraction changes landed in v1.2. |
| T-16-06 | I | backlog export guidance | mitigate | Preserve `wrong-audience finance exports` language in verification and project records so backlog export work remains host-authorized and audience-scoped. |
| T-16-07 | I/E | backlog org or multi-tenant guidance | mitigate | Preserve `cross-tenant billing leakage` language and Sigra or row-scoped tenancy prerequisites in durable planning records. |
| T-16-08 | T | planted second-processor seed | mitigate | Preserve `processor-boundary downgrade` language and keep official second-processor work as a planted seed instead of a current milestone promise. |
| T-16-09 | T | tax milestone recommendation and durable records | mitigate | Preserve `tax rollout correctness` language in the verification report and durable planning records, including customer-location and legacy recurring-item migration prerequisites, so later planners cannot treat Stripe Tax as a flip-the-switch rollout. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/docs/expansion_discovery_test.exs --trace` and `rg -n "\\*\\*Plans:\\*\\* 2 plans|DISC-0[1-5]|TAX-01|REV-01|PROC-08|ORG-01|Planted seed|tax rollout correctness|legacy recurring-item migration|customer location" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/phases/16-expansion-discovery/16-VERIFICATION.md`.
</verification>

<success_criteria>
Phase 16 finishes with a verification report and durable planning records that preserve the ranking outcome as decision-quality guidance only, with no ambiguity about what is implemented versus merely recommended next.
</success_criteria>

<output>
After completion, create `.planning/phases/16-expansion-discovery/16-02-SUMMARY.md`
</output>

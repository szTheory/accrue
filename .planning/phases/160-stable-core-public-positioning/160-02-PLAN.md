---
phase: 160-stable-core-public-positioning
plan: "02"
type: execute
wave: 2
depends_on:
  - 160-01
files_modified:
  - scripts/ci/verify_stable_core_posture.sh
  - scripts/ci/verify_release_notes_contract.sh
  - scripts/ci/README.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - POS-03
must_haves:
  truths:
    - "A maintainer can run one dedicated stable-core posture verifier instead of relying on reviewer memory to spot docs drift."
    - "Release-note posture continuity is checked lightly and points back to canonical public guides instead of becoming a second static contract."
    - "GitHub Actions blocks merges when the stable-core posture, release-note pointer, or gate ownership docs drift away from the docs surfaces created in Plan 160-01."
  artifacts:
    - path: "scripts/ci/verify_stable_core_posture.sh"
      provides: "Dedicated stable-core posture contract for public and planning mirrors"
      contains: "verify_stable_core_posture:"
    - path: "scripts/ci/verify_release_notes_contract.sh"
      provides: "Release-note posture token and pointer check"
      contains: "stable-core posture"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking CI step for the stable-core posture contract"
      contains: "Stable-core posture contract"
    - path: "scripts/ci/README.md"
      provides: "REQ mapping and triage ownership for POS-01, POS-02, and POS-03"
      contains: "verify_stable_core_posture"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_stable_core_posture.sh"
      via: "docs-contracts-shift-left step"
      pattern: "run: bash scripts/ci/verify_stable_core_posture.sh"
    - from: "scripts/ci/verify_stable_core_posture.sh"
      to: "README.md"
      via: "require_fixed and require_absent_regex checks"
      pattern: "stable-core / demand-driven expansion"
    - from: "scripts/ci/verify_release_notes_contract.sh"
      to: "accrue/guides/release-notes.md"
      via: "posture token and guide pointer check"
      pattern: "stable-core posture"
    - from: "scripts/ci/README.md"
      to: "POS-01"
      via: "gate registry row"
      pattern: "POS-01"
---

<objective>
Add the dedicated verifier and CI wiring that keep the stable-core docs surfaces from Plan 160-01 aligned over time.

Purpose: Close the executable-verification portion of POS-03 by turning the stable-core posture into a merge-blocking docs contract.
Output: one dedicated posture verifier, a light release-notes contract extension, CI integration, and documented gate ownership/triage
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/160-stable-core-public-positioning/160-CONTEXT.md
@.planning/phases/160-stable-core-public-positioning/160-RESEARCH.md
@.planning/phases/160-stable-core-public-positioning/160-VALIDATION.md
@.planning/phases/160-stable-core-public-positioning/160-PATTERNS.md
@README.md
@accrue/README.md
@accrue/guides/first_hour.md
@accrue/guides/jobs_to_be_done.md
@accrue/guides/maturity-and-maintenance.md
@accrue/guides/release-notes.md
@accrue_admin/README.md
@accrue_portal/README.md
@examples/accrue_host/README.md
@examples/accrue_host/docs/adoption-proof-matrix.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/processor-support-matrix.md
@scripts/ci/README.md
@scripts/ci/verify_package_docs.sh
@scripts/ci/verify_processor_support_matrix.sh
@scripts/ci/verify_adoption_proof_matrix.sh
@scripts/ci/verify_release_notes_contract.sh
@.github/workflows/ci.yml

<interfaces>
From `scripts/ci/verify_package_docs.sh`:

```bash
fail() {
  echo "[verify_package_docs] package docs verification failed: $*" >&2
  exit 1
}

require_fixed() { grep -Fq "$2" "$1" || fail "$1 is missing: $2"; }
require_regex() { grep -Eq "$2" "$1" || fail "$1 does not match: $2"; }
require_absent_regex() {
  if grep -Eq "$2" "$1"; then
    fail "$1 must not match: $2"
  fi
}
```

From `.github/workflows/ci.yml`:

```yaml
jobs:
  docs-contracts-shift-left:
    steps:
      - uses: actions/checkout@v6
      - name: verify_package_docs.sh
        run: bash scripts/ci/verify_package_docs.sh
      - name: Release notes freshness contract
        run: bash scripts/ci/verify_release_notes_contract.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated stable-core verifier and lightly extend the release-notes contract per D-20 through D-26</name>
  <files>scripts/ci/verify_stable_core_posture.sh, scripts/ci/verify_release_notes_contract.sh</files>
  <action>Implement D-20 through D-26 exactly. Create `scripts/ci/verify_stable_core_posture.sh` using the repository's `require_fixed`, `require_regex`, `require_absent_regex`, `set -euo pipefail`, computed `ROOT_DIR`, and an explicit stderr prefix `verify_stable_core_posture:`. The verifier must assert the stable-core anchors added by Plan 160-01 across the D-23 minimum set: `README.md`, `accrue/README.md`, `accrue/guides/maturity-and-maintenance.md`, `accrue/guides/jobs_to_be_done.md`, `accrue/guides/release-notes.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/processor-support-matrix.md`, and `examples/accrue_host/docs/adoption-proof-matrix.md`. Also assert the thin boundary mirrors required by D-08, D-12, and D-17 in `accrue/guides/first_hour.md`, `accrue_admin/README.md`, `accrue_portal/README.md`, and `examples/accrue_host/README.md` so POS-01 and POS-02 are executable instead of prose-only. Add negative guards for the exact retired public terms `feature freeze`, `no new features ever`, and `maintenance only`, per D-26. Extend `scripts/ci/verify_release_notes_contract.sh` only lightly so it requires the exact token `stable-core posture` and at least one public-guide pointer back to `maturity-and-maintenance.md`, `first_hour.md`, or `jobs_to_be_done.md`, without tying posture checks to a version heading, per D-24. Do not move posture checks into `verify_package_docs.sh` and do not add ExUnit that re-encodes the same needles.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash -n scripts/ci/verify_stable_core_posture.sh && bash -n scripts/ci/verify_release_notes_contract.sh && bash scripts/ci/verify_stable_core_posture.sh && bash scripts/ci/verify_release_notes_contract.sh</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'verify_stable_core_posture:|require_fixed|require_regex|require_absent_regex|feature freeze|no new features ever|maintenance only|stable-core posture' scripts/ci/verify_stable_core_posture.sh scripts/ci/verify_release_notes_contract.sh</automated>
  </verify>
  <done>The stable-core posture becomes a dedicated executable contract, and the release-notes verifier only adds the narrow posture token and guide pointer checks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the verifier into CI and document gate ownership per D-21 through D-25</name>
  <files>scripts/ci/README.md, .github/workflows/ci.yml</files>
  <action>Wire the new verifier into the existing docs-contract lane per D-21. In `.github/workflows/ci.yml`, add one standalone `Stable-core posture contract` step under `docs-contracts-shift-left` that runs `bash scripts/ci/verify_stable_core_posture.sh`, keeping the existing package-doc, support-matrix, adoption-proof, and release-notes steps intact. In `scripts/ci/README.md`, add POS-01, POS-02, and POS-03 registry rows mapping to `verify_stable_core_posture.sh` and `verify_release_notes_contract.sh`, plus a triage section for `verify_stable_core_posture:` failures that explains the expected anchor categories and how to update docs without creating a second support matrix. Keep the verifier documented as a dedicated gate rather than overloading `verify_package_docs.sh`, and do not add a second hand-maintained capability table or planning-only posture source.</action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && actionlint .github/workflows/ci.yml</automated>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_stable_core_posture.sh && bash scripts/ci/verify_release_notes_contract.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh && bash scripts/ci/verify_adoption_proof_matrix.sh</automated>
    <automated>cd /Users/jon/projects/accrue && rg -n 'POS-01|POS-02|POS-03|verify_stable_core_posture|Stable-core posture contract' scripts/ci/README.md .github/workflows/ci.yml</automated>
  </verify>
  <done>The stable-core posture verifier is merge-blocking under `docs-contracts-shift-left`, and the CI gate map documents POS-01 through POS-03 ownership and triage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs surfaces from Plan 160-01 -> verifier contract | The new verifier must reflect the canonical public wording instead of inventing new semantics. |
| release notes -> canonical public guides | Release notes summarize posture but must not become the static support contract. |
| verifier scripts -> CI gate | Drift detection only matters if GitHub Actions actually executes the dedicated verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | Tampering | `scripts/ci/verify_stable_core_posture.sh` | mitigate | Use narrow `require_fixed`, `require_regex`, and `require_absent_regex` checks against the exact stable-core anchors established in Plan 160-01. |
| T-160-05 | Repudiation | `scripts/ci/verify_release_notes_contract.sh` | mitigate | Keep release-note posture enforcement light and pointer-based so release notes cannot silently diverge into a second policy source. |
| T-160-06 | Elevation of Privilege | `.github/workflows/ci.yml` | mitigate | Add a standalone `Stable-core posture contract` step under `docs-contracts-shift-left` so the gate is visible and merge-blocking. |
| T-160-SC | Tampering | gate ownership docs | mitigate | Record POS-01, POS-02, and POS-03 mapping plus `verify_stable_core_posture:` triage in `scripts/ci/README.md` so contributors update the right surfaces when the contract fails. |
</threat_model>

<verification>
1. `bash -n scripts/ci/verify_stable_core_posture.sh`
2. `bash scripts/ci/verify_stable_core_posture.sh`
3. `bash scripts/ci/verify_release_notes_contract.sh`
4. `bash scripts/ci/verify_package_docs.sh`
5. `bash scripts/ci/verify_processor_support_matrix.sh`
6. `bash scripts/ci/verify_adoption_proof_matrix.sh`
7. `actionlint .github/workflows/ci.yml`
</verification>

<success_criteria>
Stable-core posture alignment is enforced by one dedicated docs contract, release notes stay a thin posture mirror, and CI clearly owns the POS-03 drift gate.
</success_criteria>

<output>
Create `.planning/phases/160-stable-core-public-positioning/160-02-SUMMARY.md` when done
</output>

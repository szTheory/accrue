---
phase: 161-backlog-anchor-closure-pause-rule
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_roadmap_hygiene.sh
  - scripts/ci/README.md
  - .github/workflows/ci.yml
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements:
  - BAK-01
  - BAK-02
  - PAU-01
must_haves:
  truths:
    - "Maintainers can distinguish historical backlog anchors from dormant seeds and deferred ideas because the active roadmap surfaces label them explicitly instead of implying they are current scope."
    - "Per D-07 through D-11, one fast `bash scripts/ci/verify_roadmap_hygiene.sh` contract proves the roadmap hygiene ledger, deferred-item trigger schema, and PROJECT/ROADMAP/STATE pause-rule mirrors."
    - "Per D-12 through D-15, PROJECT owns the canonical post-v1.48 pause doctrine and ROADMAP plus STATE repeat the same reopen trigger set without using `feature freeze` or `maintenance only` language."
    - "No active planning pointer suggests broad feature work is currently open; broad feature milestones remain closed by default until concrete evidence or strategy explicitly reopens them."
  artifacts:
    - path: "scripts/ci/verify_roadmap_hygiene.sh"
      provides: "Dedicated roadmap hygiene contract with exact historical/dormant/pause-rule checks"
      contains: "verify_roadmap_hygiene:"
    - path: ".planning/ROADMAP.md"
      provides: "Human-readable planning hygiene ledger separating historical anchors from deferred seeds and ideas"
      contains: "Deferred Seeds and Ideas"
    - path: ".planning/PROJECT.md"
      provides: "Canonical post-v1.48 pause doctrine"
      contains: "broad feature milestones remain closed by default"
    - path: ".planning/STATE.md"
      provides: "Deferred registry rows with status, reason, future owner/category, and revisit_trigger"
      contains: "revisit_trigger"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking docs-contract step for roadmap hygiene"
      contains: "Roadmap hygiene contract"
    - path: "scripts/ci/README.md"
      provides: "REQ mapping and triage ownership for BAK-01, BAK-02, and PAU-01"
      contains: "verify_roadmap_hygiene"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_roadmap_hygiene.sh"
      via: "docs-contracts-shift-left step"
      pattern: "run: bash scripts/ci/verify_roadmap_hygiene.sh"
    - from: "scripts/ci/verify_roadmap_hygiene.sh"
      to: ".planning/ROADMAP.md"
      via: "historical-anchor and deferred-seed section checks"
      pattern: "Historical Backlog Anchors \\(not active scope\\)|Deferred Seeds and Ideas"
    - from: "scripts/ci/verify_roadmap_hygiene.sh"
      to: ".planning/PROJECT.md"
      via: "canonical pause-rule fixed-string check"
      pattern: "broad feature milestones remain closed by default"
    - from: ".planning/ROADMAP.md"
      to: ".planning/research/v1.17-FRICTION-INVENTORY.md"
      via: "traceability-only historical anchor links"
      pattern: "research/v1.17-FRICTION-INVENTORY.md#backlog--int-10-phase-63"
---

<objective>
Close the backlog-anchor and pause-rule phase as one auditable planning-hygiene slice.

Purpose: Implement D-01 through D-17 without reopening feature scope, keep historical context intact, and make roadmap drift merge-blocking.
Output: one roadmap hygiene verifier, CI and README wiring for that verifier, a human-readable roadmap hygiene ledger, and canonical pause-rule mirrors in PROJECT and STATE
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-backlog-anchor-closure-pause-rule/161-CONTEXT.md
@.planning/phases/161-backlog-anchor-closure-pause-rule/161-RESEARCH.md
@.planning/phases/161-backlog-anchor-closure-pause-rule/161-VALIDATION.md
@.planning/phases/161-backlog-anchor-closure-pause-rule/161-PATTERNS.md
@.planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md
@.planning/phases/160-stable-core-public-positioning/160-CONTEXT.md
@.planning/research/v1.17-FRICTION-INVENTORY.md
@.planning/research/v1.17-north-star.md
@.planning/seeds/SEED-001-post-v1-36-linked-release-window.md
@.planning/seeds/SEED-002-ecosystem-integrations.md
@prompts/MILESTONE-NEXT-STEP-ASSESSMENT.md
@scripts/ci/verify_stable_core_posture.sh
@scripts/ci/verify_v1_17_friction_research_contract.sh
@scripts/ci/README.md
@.github/workflows/ci.yml

<interfaces>
From `scripts/ci/verify_stable_core_posture.sh`:

```bash
fail() {
  echo "verify_stable_core_posture: $*" >&2
  exit 1
}

require_fixed() { grep -Fq "$2" "$1" || fail "$1 is missing: $2"; }
require_regex() { grep -Eq "$2" "$1" || fail "$1 does not match: $2"; }
require_absent_regex() {
  if grep -Eq "$2" "$1"; then
    fail "$1 must not match: $2"
  fi
}
```

From `scripts/ci/verify_v1_17_friction_research_contract.sh`:

```bash
grep -Fq 'research/v1.17-FRICTION-INVENTORY.md#backlog--int-10-phase-63' "$roadmap" ||
  die "ROADMAP missing FRG-03 INT-10 inventory anchor"
```

From `.planning/ROADMAP.md`:

```markdown
## Planning Doctrine
Future feature milestones require at least one of:
- a concrete adopter failure mode,
- a correctness/security/data-loss risk,
- a repeated support issue,
- an operational release/support failure,
- or an explicit strategy change recorded in `.planning/PROJECT.md` / `.planning/STRATEGY.md`.

## Historical Backlog Anchors (not active scope)
These v1.17 FRG anchors are retained for traceability only.
```

From `.planning/STATE.md`:

```markdown
## Deferred Items
| Category | Item | Status | Deferred At |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create the roadmap hygiene contract, triage entry, and CI step per D-07 through D-11</name>
  <files>scripts/ci/verify_roadmap_hygiene.sh, scripts/ci/README.md, .github/workflows/ci.yml</files>
  <read_first>.planning/phases/161-backlog-anchor-closure-pause-rule/161-CONTEXT.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-RESEARCH.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-VALIDATION.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-PATTERNS.md, scripts/ci/verify_stable_core_posture.sh, scripts/ci/verify_v1_17_friction_research_contract.sh, scripts/ci/README.md, .github/workflows/ci.yml, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <behavior>
    - Test 1: The new script uses `set -euo pipefail`, a `verify_roadmap_hygiene:` stderr prefix, and the same `require_fixed` / `require_regex` / `require_absent_regex` helper shape as the existing docs-contract scripts.
    - Test 2: Before the planning docs are edited in Tasks 2 and 3, `bash scripts/ci/verify_roadmap_hygiene.sh` should fail because the new historical/dormant/pause-rule needles are not present yet.
    - Test 3: `scripts/ci/README.md` and `.github/workflows/ci.yml` reference the script by the exact name `verify_roadmap_hygiene.sh`.
  </behavior>
  <action>Implement the dedicated roadmap hygiene verifier as a new script, not an extension of `verify_v1_17_friction_research_contract.sh`, per D-07, D-08, D-09, D-10, and the default discretion choice. Create `scripts/ci/verify_roadmap_hygiene.sh` with the same bash contract skeleton used by `verify_stable_core_posture.sh`: `#!/usr/bin/env bash`, `set -euo pipefail`, computed `ROOT_DIR`, `fail`, `require_fixed`, `require_regex`, and `require_absent_regex`. Give it the exact failure prefix `verify_roadmap_hygiene:`. Encode the D-09 checks up front even though Tasks 2 and 3 have not landed yet: require a human-readable historical-anchor section in `.planning/ROADMAP.md`, a dormant/deferred section in `.planning/ROADMAP.md`, explicit `revisit_trigger` data in `.planning/STATE.md`, and the exact pause-rule mirror sentence in `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`. Add negative guards so the active planning surfaces reject `feature freeze`, `maintenance only`, and any wording that implies a dormant seed alone creates scope. In `scripts/ci/README.md`, add BAK-01 / BAK-02 / PAU-01 registry rows plus a `### Triage: verify_roadmap_hygiene.sh` section that names the failure prefix, the three planning files it governs, and the rule that fixes belong in the canonical surface first. In `.github/workflows/ci.yml`, add one standalone `Roadmap hygiene contract` step under `docs-contracts-shift-left` that runs `bash scripts/ci/verify_roadmap_hygiene.sh`, keeping it adjacent to the other docs-contract gates and not creating a new job. Do not introduce a machine-readable manifest or a second CI lane, per D-08 and D-10.</action>
  <acceptance_criteria>
    - `scripts/ci/verify_roadmap_hygiene.sh` exists and contains `verify_roadmap_hygiene:`, `require_fixed`, `require_regex`, and `require_absent_regex`.
    - `.github/workflows/ci.yml` contains a step named `Roadmap hygiene contract` with `run: bash scripts/ci/verify_roadmap_hygiene.sh`.
    - `scripts/ci/README.md` contains `BAK-01`, `BAK-02`, `PAU-01`, and `### Triage: verify_roadmap_hygiene.sh`.
    - Running `bash scripts/ci/verify_roadmap_hygiene.sh` immediately after this task returns non-zero because the ROADMAP / PROJECT / STATE edits are not in place yet.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash -n scripts/ci/verify_roadmap_hygiene.sh && rg -n 'verify_roadmap_hygiene:|require_fixed|require_regex|require_absent_regex' scripts/ci/verify_roadmap_hygiene.sh && rg -n 'BAK-01|BAK-02|PAU-01|verify_roadmap_hygiene|Roadmap hygiene contract' scripts/ci/README.md .github/workflows/ci.yml && ! bash scripts/ci/verify_roadmap_hygiene.sh</automated>
  </verify>
  <done>The roadmap hygiene contract exists, is wired into the existing docs-contract lane, is documented for contributors, and is ready to turn green once the planning docs are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Reclassify historical anchors and deferred seeds in ROADMAP per D-01 through D-06</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-CONTEXT.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-RESEARCH.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-VALIDATION.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-PATTERNS.md, .planning/research/v1.17-FRICTION-INVENTORY.md, .planning/research/v1.17-north-star.md, .planning/seeds/SEED-001-post-v1-36-linked-release-window.md, .planning/seeds/SEED-002-ecosystem-integrations.md, .planning/phases/159-linked-release-readiness-publish-proof/159-CONTEXT.md, .planning/phases/160-stable-core-public-positioning/160-CONTEXT.md, scripts/ci/verify_roadmap_hygiene.sh</read_first>
  <action>Update `.planning/ROADMAP.md` as the human-readable hygiene ledger, keeping D-06 traceability intact and not deleting any historical links. Preserve the existing `## Historical Backlog Anchors (not active scope)` section, but rewrite its intro and each of the three bullets so they explicitly read as non-active historical anchors per D-01 and D-02, and each bullet ends with a concrete reopen trigger that matches the stable-core evidence bar rather than vague backlog language. Add one exact fixed-string mirror sentence in the Planning Doctrine block: `After v1.48, broad feature milestones remain closed by default unless reopened by concrete adopter failure, correctness/security/data-loss risk, repeated support issue, operational failure, or explicit strategy change.` Add a second explicit line `No broad feature milestone is currently open.` so BAK-02 has one unambiguous active-scope marker. Then add a new `## Deferred Seeds and Ideas (dormant / trigger-bound)` section with a markdown table whose columns are exactly `Item | Status | Reason | Future owner/category | Revisit trigger`. Populate at least four rows: `SEED-001` as `resolved historical context` per D-03, `SEED-002` as `dormant future roadmap` per D-04, `ENT-EXT-01` as deferred until a concrete adopter contract requires richer entitlement math, and `FIN-03` as a standing non-goal unless strategy changes. Each row must include a concrete reason, a future owner/category, and a trigger phrased in the D-05 evidence vocabulary. Do not add new future-milestone promises, do not re-open broad feature work by implication, and do not use `feature freeze` or `maintenance only` phrasing, per D-13 and D-15.</action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains the exact sentence `After v1.48, broad feature milestones remain closed by default unless reopened by concrete adopter failure, correctness/security/data-loss risk, repeated support issue, operational failure, or explicit strategy change.`
    - `.planning/ROADMAP.md` contains the exact sentence `No broad feature milestone is currently open.`
    - `.planning/ROADMAP.md` contains `## Deferred Seeds and Ideas (dormant / trigger-bound)` and a table header with `Revisit trigger`.
    - The historical anchor section still links to all three `v1.17-FRICTION-INVENTORY.md#backlog--...` anchors and each bullet labels the item as non-active or historical.
    - The deferred table includes rows named `SEED-001`, `SEED-002`, `ENT-EXT-01`, and `FIN-03`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_roadmap_hygiene.sh && rg -n 'After v1\\.48, broad feature milestones remain closed by default|No broad feature milestone is currently open\\.|Historical Backlog Anchors \\(not active scope\\)|Deferred Seeds and Ideas \\(dormant / trigger-bound\\)|SEED-001|SEED-002|ENT-EXT-01|FIN-03|Revisit trigger' .planning/ROADMAP.md && rg -n 'research/v1\\.17-FRICTION-INVENTORY\\.md#backlog--(int-10-phase-63|bil-03-phase-64|adm-12-phase-65)' .planning/ROADMAP.md</automated>
  </verify>
  <done>The roadmap now separates traceability-only historical anchors from dormant trigger-bound future material and states plainly that no broad feature milestone is currently open.</done>
</task>

<task type="auto">
  <name>Task 3: Canonicalize the pause rule in PROJECT and STATE and enrich the deferred registry per D-05 and D-12 through D-17</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>.planning/PROJECT.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-CONTEXT.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-RESEARCH.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-VALIDATION.md, .planning/phases/161-backlog-anchor-closure-pause-rule/161-PATTERNS.md, .planning/seeds/SEED-001-post-v1-36-linked-release-window.md, .planning/seeds/SEED-002-ecosystem-integrations.md, .planning/research/v1.17-FRICTION-INVENTORY.md, scripts/ci/verify_roadmap_hygiene.sh, scripts/ci/verify_stable_core_posture.sh, scripts/ci/verify_v1_17_friction_research_contract.sh</read_first>
  <action>Make `.planning/PROJECT.md` the canonical doctrine owner per D-12 through D-15, and make `.planning/STATE.md` the session-continuity mirror plus full deferred registry. In `.planning/PROJECT.md`, keep the existing `## Development Posture` section and stable-core language from Phase 160, but add a dedicated `### Post-v1.48 pause rule` subsection that uses the exact same sentence added to ROADMAP in Task 2. Immediately below it, add one explicit second sentence that says a historical anchor, dormant seed, or deferred idea never opens milestone scope by itself and that any reopen decision must be recorded in `.planning/PROJECT.md` or a future strategy artifact before ROADMAP/STATE change, per D-14. Replace the now-expired `Recommended next default milestone: Release Readiness + Stable Core Posture ...` line with a closeout-default line that says no broad feature milestone is currently open after v1.48 and future work defaults to release-readiness, maintenance, verifier hardening, or explicitly justified strategy change. In `.planning/STATE.md`, add the same exact pause-rule sentence in a concise mirror location used for session continuity, then expand `## Deferred Items` from the current four-column table to the exact schema `Category | Item | Status | Reason | Future owner/category | revisit_trigger | Deferred At`. Preserve every currently listed row and add concrete `Reason`, `Future owner/category`, and `revisit_trigger` values for every surviving item per D-05, using the stable-core trigger vocabulary rather than generic `future` wording. Keep `SEED-002` in this registry as dormant future-roadmap material, keep the existing out-of-scope and non-goal rows, and do not silently drop any deferred item. Do not add public-abandonment phrasing, and do not widen the work into package README or guide edits reserved for Phase 160.</action>
  <acceptance_criteria>
    - `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` all contain the exact same pause-rule sentence beginning `After v1.48, broad feature milestones remain closed by default`.
    - `.planning/PROJECT.md` contains `### Post-v1.48 pause rule`.
    - `.planning/PROJECT.md` no longer recommends `Release Readiness + Stable Core Posture` as the next default milestone.
    - `.planning/STATE.md` contains the exact table header `| Category | Item | Status | Reason | Future owner/category | revisit_trigger | Deferred At |`.
    - Every row that existed in the pre-phase `## Deferred Items` table remains present in `.planning/STATE.md` with populated `Reason`, `Future owner/category`, and `revisit_trigger` cells.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_roadmap_hygiene.sh && bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_processor_support_matrix.sh && bash scripts/ci/verify_verify01_readme_contract.sh && bash scripts/ci/verify_adoption_proof_matrix.sh && bash scripts/ci/verify_stable_core_posture.sh && bash scripts/ci/verify_v1_17_friction_research_contract.sh && rg -n 'After v1\\.48, broad feature milestones remain closed by default|### Post-v1\\.48 pause rule|revisit_trigger|No broad feature milestone is currently open' .planning/PROJECT.md .planning/STATE.md</automated>
  </verify>
  <done>PROJECT becomes the canonical pause-rule doctrine, STATE becomes the explicit deferred-item registry with revisit triggers, and the full docs-contract bundle stays green with the new roadmap hygiene gate included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> roadmap hygiene verifier | The script must enforce the intended historical/dormant/pause-rule semantics instead of inventing a second source of truth. |
| historical anchors and deferred registry -> future milestone planning | Ambiguous wording here can accidentally reopen broad feature scope or erase traceability. |
| verifier scripts -> GitHub Actions | Drift protection matters only if `docs-contracts-shift-left` actually runs the new contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-01 | Tampering | `scripts/ci/verify_roadmap_hygiene.sh` | mitigate | Use exact `require_fixed`, `require_regex`, and `require_absent_regex` checks for the historical-anchor section, deferred-seed table, `revisit_trigger` schema, and the shared pause-rule sentence per D-07 through D-11. |
| T-161-02 | Repudiation | `.planning/ROADMAP.md`, `.planning/STATE.md` | mitigate | Label FRG anchors as historical/non-active, add a dedicated dormant-seed ledger, and require populated `Reason`, `Future owner/category`, and `revisit_trigger` cells so future planners cannot plausibly claim the scope was still open. |
| T-161-03 | Elevation of Privilege | `.planning/PROJECT.md` | mitigate | Replace the stale next-milestone pointer with the exact post-v1.48 pause doctrine so a dormant seed or old anchor cannot escalate into default milestone scope without explicit reopen evidence. |
| T-161-04 | Tampering | `.github/workflows/ci.yml`, `scripts/ci/README.md` | mitigate | Add a visible `Roadmap hygiene contract` step to `docs-contracts-shift-left` and document BAK-01 / BAK-02 / PAU-01 ownership plus `verify_roadmap_hygiene:` triage in `scripts/ci/README.md`. |
| T-161-SC | Tampering | broad-scope posture language | mitigate | Add negative guards for `feature freeze`, `maintenance only`, and scope-opening wording so the stable-core posture from Phase 160 is preserved rather than replaced with abandonment language. |
</threat_model>

<verification>
1. `bash -n scripts/ci/verify_roadmap_hygiene.sh`
2. `bash scripts/ci/verify_roadmap_hygiene.sh`
3. `bash scripts/ci/verify_package_docs.sh`
4. `bash scripts/ci/verify_processor_support_matrix.sh`
5. `bash scripts/ci/verify_verify01_readme_contract.sh`
6. `bash scripts/ci/verify_adoption_proof_matrix.sh`
7. `bash scripts/ci/verify_stable_core_posture.sh`
8. `bash scripts/ci/verify_v1_17_friction_research_contract.sh`
</verification>

<success_criteria>
Historical anchors are preserved as traceability-only context, dormant seeds and deferred ideas carry explicit reopen triggers, the post-v1.48 pause rule is canonical in PROJECT and mirrored in ROADMAP plus STATE, and CI blocks future wording drift through the new roadmap hygiene contract.
</success_criteria>

<output>
Create `.planning/phases/161-backlog-anchor-closure-pause-rule/161-01-SUMMARY.md` when done
</output>

---
phase: "163"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/mix.exs
  - examples/accrue_host/priv/repo/seeds.exs
  - examples/accrue_host/priv/repo/seeds/hero_accounts.exs
  - examples/accrue_host/priv/repo/seeds/background_data.exs
autonomous: true
requirements:
  - EVD-01
  - EVD-02
must_haves:
  truths:
    - "User can login to demo app as a Hero persona and see realistic data."
    - "Background accounts populate the dashboard lists."
    - "Time-series charts show up to 90 days of historical trend data."
    - "Seeds run idempotently without crashing."
  artifacts:
    - path: "examples/accrue_host/mix.exs"
      provides: "faker dependency"
    - path: "examples/accrue_host/priv/repo/seeds.exs"
      provides: "Main seeding entrypoint"
    - path: "examples/accrue_host/priv/repo/seeds/hero_accounts.exs"
      provides: "Deterministic Hero seeds (PingPal persona)"
    - path: "examples/accrue_host/priv/repo/seeds/background_data.exs"
      provides: "Bulk random background seeds and time-series history"
  key_links:
    - from: "examples/accrue_host/priv/repo/seeds.exs"
      to: "examples/accrue_host/priv/repo/seeds/hero_accounts.exs"
      via: "Code.require_file"
      pattern: "Code.require_file.*hero_accounts.exs"
    - from: "examples/accrue_host/priv/repo/seeds.exs"
      to: "examples/accrue_host/priv/repo/seeds/background_data.exs"
      via: "Code.require_file"
      pattern: "Code.require_file.*background_data.exs"
---

<objective>
Transform the Accrue Demo App (examples/accrue_host) from a blank slate into a realistic, lived-in SaaS product featuring a "PingPal" Uptime Monitoring persona.

Purpose: To immediately demonstrate the Admin UI value, MRR analytics, and pagination features via realistic fixtures and historical event trends, supporting both manual QA and stable Playwright E2E tests.
Output: Extract seeded logic into `hero_accounts.exs` and `background_data.exs`, add `faker` dependency, and seed 100+ bulk accounts with backdated time-series data.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-realistic-domain-rich-seeds/163-CONTEXT.md
@.planning/phases/163-realistic-domain-rich-seeds/163-RESEARCH.md
@examples/accrue_host/mix.exs
@examples/accrue_host/priv/repo/seeds.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add faker dependency</name>
  <files>examples/accrue_host/mix.exs</files>
  <action>
    Add `{:faker, "~> 0.18", only: [:dev, :test]}` to the `deps` function in `examples/accrue_host/mix.exs`.
    Run `cd examples/accrue_host && mix deps.get`.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix deps | grep faker</automated>
  </verify>
  <done>faker is present in mix.lock and dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor seeds.exs and implement Hero Accounts</name>
  <files>examples/accrue_host/priv/repo/seeds.exs, examples/accrue_host/priv/repo/seeds/hero_accounts.exs</files>
  <action>
    1. Create the `examples/accrue_host/priv/repo/seeds/` directory.
    2. Move the existing demo account creation logic from `seeds.exs` into a new file: `examples/accrue_host/priv/repo/seeds/hero_accounts.exs`.
    3. Enhance the `hero_accounts.exs` with additional B2B "PingPal" personas (e.g. `canceled@example.com`, `enterprise@example.com`, `trialing@example.com`).
    4. Ensure the hero accounts are created via `AccrueHost.Billing.subscribe` to accurately simulate production paths and guarantee Fake processor parity.
    5. In `examples/accrue_host/priv/repo/seeds.exs`, replace the old logic with `Code.require_file("seeds/hero_accounts.exs", __DIR__)` (and prepare for background_data.exs). 
    Avoid overwriting `record_at` helpers just yet if they can be reused or moved sensibly.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix run priv/repo/seeds.exs</automated>
  </verify>
  <done>Seeds run idempotently without unique constraint crashes and PingPal hero accounts exist.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Bulk Background Data</name>
  <files>examples/accrue_host/priv/repo/seeds.exs, examples/accrue_host/priv/repo/seeds/background_data.exs</files>
  <action>
    1. Create `examples/accrue_host/priv/repo/seeds/background_data.exs`.
    2. Use `faker` to generate ~100 realistic background accounts (using `Repo.insert_all` to avoid N+1 slow downs).
    3. Memory-map associations via `Ecto.UUID.generate()` (User -> Organization -> Customer -> Subscription) as documented in the RESEARCH.md pattern.
    4. Use `on_conflict: :nothing` to guarantee the background data is idempotent on DB reset or re-runs.
    5. Wire this file into `seeds.exs` by adding `Code.require_file("seeds/background_data.exs", __DIR__)`.
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix run priv/repo/seeds.exs</automated>
  </verify>
  <done>Background accounts populate the database in less than a few seconds.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Time-series backdating for analytics</name>
  <files>examples/accrue_host/priv/repo/seeds/background_data.exs</files>
  <action>
    Integrate the `record_at` backdating technique to inject `Accrue.Events.Event` rows for the last 30/90 days into the database (MRR growth, invoices, dunning).
    Backdate subscriptions, customers, and events using `Faker.DateTime.backward(90)`.
    Ensure the `inserted_at` logic respects Accrue's strict immutable ledger SQL triggers (do not attempt to UPDATE an event's timestamp, always INSERT with `inserted_at` predefined).
  </action>
  <verify>
    <automated>cd examples/accrue_host && mix run priv/repo/seeds.exs</automated>
  </verify>
  <done>Dashboard analytics (7d/30d/90d) would reflect the seeded time-series data without raising SQLSTATE 45A01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo Data -> Database | Seeds script interacting with DB during local testing/QA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-SC | Tampering | npm/pip/cargo/hex installs | mitigate | slopcheck via standard GSD tooling; `faker` is an approved dev-only package |
</threat_model>

<verification>
- `cd examples/accrue_host && mix ecto.reset` runs successfully.
- `cd examples/accrue_host && mix test e2e/` (if applicable) succeeds against the new hero fixtures.
- Validated that EVD-01 and EVD-02 are satisfied by generating a robust PingPal B2B schema and rich seed history.
</verification>

<success_criteria>
`seeds.exs` cleanly modularizes into `hero_accounts` and `background_data`.
A developer can run `mix ecto.reset` and see ~100 realistic DevTool users with populated 90-day charts immediately upon login.
</success_criteria>

<output>
Create `.planning/phases/163-realistic-domain-rich-seeds/163-01-SUMMARY.md` when done
</output>

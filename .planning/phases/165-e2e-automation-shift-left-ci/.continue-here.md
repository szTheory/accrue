---
context: phase
phase: 165-e2e-automation-shift-left-ci
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-06-02T00:54:33.466Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: DB Connection Exhaustion — Postgres FATAL 53300 — Structural mitigation: set `fullyParallel: false` in `playwright.config.js` and use `--workers=1` for sensitive tests.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fake Processor Race | The `Accrue.Processor.Fake` is a singleton GenServer. Parallel tests mutating the same user/org will conflict. | blocking | Use serial execution (`test.describe.configure({ mode: 'serial' })`) or consolidate flows. |

<current_state>
We are at the end of Plan 165-03. The core functional E2E tests for onboarding and billing have been implemented and verified in `examples/accrue_host/e2e/onboarding_and_billing.spec.js`. They pass reliably when run with a single worker.
</current_state>

<completed_work>

Completed Tasks:
- Plan 165-01: Setup Ecto Sandbox infrastructure - Done
- Plan 165-02: Configure Playwright for Sandbox - Done
- Plan 165-03: Task 1 & 2: Write core functional E2E tests - Implemented and verified, but SUMMARY and STATE update are pending.
</completed_work>

<remaining_work>

- Plan 165-03: Finalize documentation (SUMMARY.md and STATE.md).
- Plan 165-04: Integrate E2E testing into GitHub Actions CI.
</remaining_work>

<decisions_made>

- **Consolidated Tests**: Chose to merge onboarding and billing tests into a single serial spec `onboarding_and_billing.spec.js` to ensure deterministic network/GenServer behavior on the shared Fake Processor.
- **Disabled Parallelism**: Set `fullyParallel: false` in `playwright.config.js` and limited workers to mitigate Postgres connection exhaustion.
</decisions_made>

<blockers>
- **Postgres Connections**: `FATAL 53300 (too_many_connections)` is a recurring issue when running many Playwright workers. Mitigated by serializing tests.
</blockers>

## Required Reading (in order)
1. `.planning/phases/165-e2e-automation-shift-left-ci/165-03-PLAN.md` — The objective for the current plan.
2. `examples/accrue_host/e2e/onboarding_and_billing.spec.js` — The current working test suite.
3. `examples/accrue_host/playwright.config.js` — Updated configuration for stability.

## Infrastructure State
- **Postgres**: Running locally. Max connections is a bottleneck.
- **Phoenix**: Tests run against a local test server started by Playwright.

<context>
The goal is to establish a solid E2E foundation before moving to CI integration. The tests are now high-signal and stable. Resuming agent should wrap up the docs for Plan 03 and move to CI setup.
</context>

<next_action>
Start with: Create `.planning/phases/165-e2e-automation-shift-left-ci/165-03-SUMMARY.md` and update `.planning/STATE.md` to mark Plan 03 complete.
</next_action>

---
phase: 165-e2e-automation-shift-left-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/lib/accrue_host_web/endpoint.ex
  - examples/accrue_host/config/test.exs
  - examples/accrue_host/lib/accrue_host_web/router.ex
  - examples/accrue_host/lib/accrue_host_web/controllers/sandbox_controller.ex
autonomous: true
requirements: [E2E-04]
must_haves:
  truths:
    - Phoenix allows sandbox sessions via an API endpoint in test mode
  artifacts:
    - path: examples/accrue_host/lib/accrue_host_web/endpoint.ex
      provides: Sandbox plug initialization
    - path: examples/accrue_host/lib/accrue_host_web/controllers/sandbox_controller.ex
      provides: Sandbox metadata distribution
  key_links:
    - from: examples/accrue_host/lib/accrue_host_web/router.ex
      to: examples/accrue_host/lib/accrue_host_web/controllers/sandbox_controller.ex
      via: POST and DELETE endpoints
---

<objective>
Setup the Ecto Sandbox infrastructure to allow deterministic, fully parallel E2E testing in Playwright.

Purpose: Automate the happy paths for primary user journeys by first establishing a flake-free sandboxed environment (D-02).
Output: A new sandbox API and Phoenix endpoint configuration to support transaction isolation per test.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@examples/accrue_host/lib/accrue_host_web/endpoint.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ecto Sandbox API</name>
  <files>examples/accrue_host/lib/accrue_host_web/endpoint.ex, examples/accrue_host/config/test.exs, examples/accrue_host/lib/accrue_host_web/router.ex, examples/accrue_host/lib/accrue_host_web/controllers/sandbox_controller.ex</files>
  <action>
    Configure `Phoenix.Ecto.SQL.Sandbox` in `endpoint.ex`, conditional on `Application.compile_env(:accrue_host, :sql_sandbox)`. Set `sql_sandbox: true` in `config/test.exs`. 
    Create a `SandboxController` in test mode exposing `POST /api/sandbox` (calls `Ecto.Adapters.SQL.Sandbox.start_owner!(AccrueHost.Repo, shared: false)` and returns `Phoenix.Ecto.SQL.Sandbox.metadata_for(AccrueHost.Repo, owner)`) and `DELETE /api/sandbox/:metadata` (stops the owner). Wire this up in `router.ex` under a `scope "/api/sandbox"` conditional on `Mix.env() == :test`.
  </action>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix compile</automated>
  </verify>
  <done>Phoenix is configured to allow Sandbox sessions via API and endpoint plug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> Phoenix | Test environment API is restricted to `:test` mix env only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Information Disclosure | Sandbox API | accept | Only exposed in `:test` environment |
</threat_model>

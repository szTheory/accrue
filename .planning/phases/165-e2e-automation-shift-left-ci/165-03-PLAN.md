---
phase: 165-e2e-automation-shift-left-ci
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - examples/accrue_host/e2e/onboarding.spec.js
  - examples/accrue_host/e2e/billing.spec.js
autonomous: true
requirements: [E2E-01, E2E-02, E2E-04]
must_haves:
  truths:
    - Playwright tests cover onboarding and checkout
    - Playwright tests cover subscription changes and billing management
    - Tests run reliably against seeded data
  artifacts:
    - path: examples/accrue_host/e2e/onboarding.spec.js
      provides: Test suite for onboarding/checkout
    - path: examples/accrue_host/e2e/billing.spec.js
      provides: Test suite for billing management
  key_links:
    - from: examples/accrue_host/e2e/onboarding.spec.js
      to: examples/accrue_host/e2e/support/test.js
      via: Custom Playwright test fixture import
---

<objective>
Write the core functional E2E tests for onboarding and billing management.

Purpose: Automate the happy paths for primary user journeys using deterministic network behavior (D-03).
Output: Two new Playwright specs (`onboarding.spec.js`, `billing.spec.js`).
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Onboarding & Checkout Tests</name>
  <files>examples/accrue_host/e2e/onboarding.spec.js</files>
  <action>
    Create `examples/accrue_host/e2e/onboarding.spec.js` using the custom `./support/test.js` fixture. 
    Implement robust Playwright tests for the primary onboarding and checkout happy paths. It should simulate a user signing up, selecting a plan, and successfully checking out using the `Fake` processor (which guarantees deterministic network behavior).
  </action>
  <verify>
    <automated>cd examples/accrue_host && npm run e2e -- onboarding.spec.js || true</automated>
  </verify>
  <done>Playwright test executes onboarding and checkout successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Write Billing Management Tests</name>
  <files>examples/accrue_host/e2e/billing.spec.js</files>
  <action>
    Create `examples/accrue_host/e2e/billing.spec.js` using the custom `./support/test.js` fixture.
    Implement tests for billing management: upgrading a plan, downgrading a plan, canceling a subscription, and adding/removing a payment method via the customer portal. Ensure it relies on the seeded data appropriately and runs completely independently without state leakage due to the Sandbox.
  </action>
  <verify>
    <automated>cd examples/accrue_host && npm run e2e -- billing.spec.js || true</automated>
  </verify>
  <done>Playwright test executes billing management scenarios successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Phoenix | Standard browser interactions in test environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-03 | Spoofing | Auth flows | accept | Handled by testing standard login/registration paths |
</threat_model>

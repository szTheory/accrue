---
phase: 166-adoption-dx-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/config/dev.exs
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "D-08: Docker browser reachability must be fixed or verified before README promises `http://localhost:4000`."
    - "D-10: Docker implementation must preserve the Phase 164 Debian slim and named-volume decisions; do not recommend Alpine or casual deletion of `deps`, `_build`, or `assets/node_modules`."
    - Docker Compose publishes the host app at localhost:4000.
    - The Phoenix endpoint listens on an address reachable through the Compose-published port.
    - Bare-metal native Phoenix development still works without setting Docker-only environment variables.
  artifacts:
    - path: examples/accrue_host/config/dev.exs
      provides: Dual-mode endpoint bind configuration for native and Docker development
  key_links:
    - from: examples/accrue_host/config/dev.exs
      to: examples/accrue_host/docker-compose.yml
      via: PGHOST=db selects container-safe endpoint bind behavior
---

<objective>
Make the Docker browser contract true before Phase 166 documentation makes Docker the primary evaluator path.

Purpose: `examples/accrue_host/docker-compose.yml` publishes `4000:4000`, and Phase 165 CI polls `http://localhost:4000/`, but the current Phoenix dev endpoint binds to `127.0.0.1`. The executor must reconcile this so the README can honestly tell evaluators to boot with Docker and open localhost.
Output: `examples/accrue_host/config/dev.exs` keeps native loopback behavior while allowing the Docker web container to serve through the published port.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/166-adoption-dx-docs/166-CONTEXT.md
@.planning/phases/166-adoption-dx-docs/166-RESEARCH.md
@.planning/phases/166-adoption-dx-docs/166-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the dev endpoint bind Docker-aware</name>
  <files>examples/accrue_host/config/dev.exs</files>
  <read_first>
    - examples/accrue_host/config/dev.exs
    - examples/accrue_host/docker-compose.yml
    - examples/accrue_host/Dockerfile.dev
    - .planning/phases/164-docker-dx-optimized-caching/164-02-SUMMARY.md
    - .planning/phases/165-e2e-automation-shift-left-ci/165-04-SUMMARY.md
  </read_first>
  <action>
    Update `examples/accrue_host/config/dev.exs` so the Phoenix endpoint `http:` bind IP is `{0, 0, 0, 0}` when running inside the Compose web service and remains `{127, 0, 0, 1}` for native bare-metal development.
    Use the existing Compose signal `PGHOST=db` as the selector, because `examples/accrue_host/docker-compose.yml` already sets `PGHOST: db` for the web service.
    A concrete acceptable shape is:
    `endpoint_ip = if System.get_env("PGHOST") == "db", do: {0, 0, 0, 0}, else: {127, 0, 0, 1}`
    and then `http: [ip: endpoint_ip]`.
    Keep `check_origin: false`, watchers, live reload, and all database settings unchanged.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/config/dev.exs` contains `endpoint_ip = if System.get_env("PGHOST") == "db"`.
    - `examples/accrue_host/config/dev.exs` contains `do: {0, 0, 0, 0}`.
    - `examples/accrue_host/config/dev.exs` contains `else: {127, 0, 0, 1}`.
    - `examples/accrue_host/config/dev.exs` contains `http: [ip: endpoint_ip]`.
    - `examples/accrue_host/config/dev.exs` still contains `hostname: System.get_env("PGHOST") || "localhost"`.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq 'endpoint_ip = if System.get_env("PGHOST") == "db"' examples/accrue_host/config/dev.exs && grep -Fq 'http: [ip: endpoint_ip]' examples/accrue_host/config/dev.exs</automated>
    <automated>cd examples/accrue_host && mix format config/dev.exs</automated>
    <automated>cd examples/accrue_host && docker compose config >/dev/null</automated>
  </verify>
  <done>Docker selects a container-reachable endpoint bind while native development remains loopback-bound.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Docker browser smoke contract</name>
  <files>examples/accrue_host/config/dev.exs</files>
  <read_first>
    - examples/accrue_host/config/dev.exs
    - examples/accrue_host/docker-compose.yml
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Verify the same Docker browser contract that Phase 165 CI protects: Compose builds and boots the host app, and `http://localhost:4000/` responds through the published port.
    If local Docker or host port `5432` is unavailable, do not modify files to work around the local environment. Record the exact blocker in the execution summary and still run `cd examples/accrue_host && docker compose config >/dev/null`.
    When the smoke runs, clean up with `docker compose down --volumes --remove-orphans`; treat `--volumes` as reset/cleanup only, not as the normal stop command for future README prose.
  </action>
  <acceptance_criteria>
    - `cd examples/accrue_host && docker compose config >/dev/null` exits 0.
    - If Docker boot smoke is possible, `curl -fsS http://localhost:4000/` exits 0 while the Compose stack is running.
    - If Docker boot smoke is not possible, the summary names the blocker, such as Docker unavailable or host port `5432` collision.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && docker compose config >/dev/null</automated>
    <automated>cd examples/accrue_host && docker compose up --build -d && ok=false; for i in $(seq 1 30); do if curl -fsS http://localhost:4000/ >/dev/null; then ok=true; break; fi; sleep 1; done; docker compose down --volumes --remove-orphans; test "$ok" = true</automated>
  </verify>
  <done>The Docker localhost browser claim has either passing local evidence or a recorded environment blocker plus valid Compose configuration.</done>
</task>

</tasks>

<verification>
- `grep -Fq 'endpoint_ip = if System.get_env("PGHOST") == "db"' examples/accrue_host/config/dev.exs`
- `grep -Fq 'http: [ip: endpoint_ip]' examples/accrue_host/config/dev.exs`
- `cd examples/accrue_host && mix format config/dev.exs`
- `cd examples/accrue_host && docker compose config >/dev/null`
- Attempt the Docker boot smoke and record pass/blocker.
</verification>

<success_criteria>
- Docker remains the primary Phase 166 evaluator path because the application can bind correctly inside Compose.
- Native Phoenix still defaults to loopback and local Postgres unless `PGHOST=db` is set.
- No README claims are changed in this plan.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host browser -> Docker container | Localhost traffic reaches the Phoenix dev server through a published development port. |
| Docker web -> Docker db | The web service uses Compose service discovery through `PGHOST=db`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-01 | Information Disclosure | Phoenix dev endpoint bind | mitigate | Bind all interfaces only when `PGHOST=db` indicates the Docker dev service; keep native development on `127.0.0.1`. |
| T-166-02 | Denial of Service | Local Docker smoke | accept | Local port collisions are environment-specific; record blockers and rely on CI Docker smoke for clean-run evidence. |
</threat_model>

---
phase: 166-adoption-dx-docs
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - examples/accrue_host/README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "D-01: Replace the caveat-first opening with a compact `## Start Here` near the top that answers whether an adopter can boot and inspect a realistic Phoenix SaaS billing loop locally."
    - "D-02: Frame the demo as a realistic B2B devtool SaaS/PingPal-style host with seeded accounts, Fake-backed billing, signed webhook ingest, and mounted admin inspection."
    - "D-03: Move the Sigra truth boundary after the run path while keeping `Accrue.Auth`, First Hour, and Organization Billing handoffs visible."
    - "D-04: Do not front-load support matrix minutiae, Braintree limitations, `swap_plan/3`, `preview_upcoming_invoice/2`, VERIFY-01 internals, Playwright shards, or CI job details before first successful run."
    - "D-05: Make Docker the primary evaluator path with `cd examples/accrue_host` and `docker compose up --build`."
    - "D-06: Preserve the native Phoenix lane immediately after Docker with `mix setup` and `mix phx.server`."
    - "D-07: Avoid equal choose-your-adventure framing; Docker is primary and native Phoenix is secondary."
    - "D-09: Mention Docker footguns tersely: `PGHOST=db`, local Postgres defaults, `5432:5432` collisions, named volume isolation, and `docker compose down --volumes` as reset."
    - "D-14: State that Start Here uses `Accrue.Processor.Fake` and requires no live Stripe keys."
    - "D-16: Implement one top-of-README adoption flow: realistic frame, Docker command, native fallback, `http://localhost:4000`, `/app/billing`, `/billing`, then `mix verify`."
    - The top of the host README has a clear Start Here section.
    - Docker is the primary evaluator path and native Phoenix is the secondary contributor path.
    - The Start Here narrative is framed around a Phoenix SaaS adopter evaluating a realistic billing loop.
    - Local evaluation is explicitly Fake-backed and does not require live Stripe keys.
  artifacts:
    - path: examples/accrue_host/README.md
      provides: Persona-framed Start Here flow and Docker-first run path
  key_links:
    - from: examples/accrue_host/README.md
      to: examples/accrue_host/docker-compose.yml
      via: Docker command and localhost browser destination
    - from: examples/accrue_host/README.md
      to: accrue/guides/first_hour.md
      via: Non-Sigra and build-your-own-app handoff
---

<objective>
Rewrite the top of `examples/accrue_host/README.md` so an adopter can immediately understand, boot, and inspect the realistic local demo.

Purpose: Phase 166 requires a clear Start Here DX summary, prominent Docker commands, and persona-framed documentation. The existing README starts with caveats and support details before the evaluator can run the app.
Output: A compact top-of-README `## Start Here` section with Docker-first commands, native Phoenix fallback, browser destination, walkthrough path, and honest Fake/Sigra boundaries.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/166-adoption-dx-docs/166-CONTEXT.md
@.planning/phases/166-adoption-dx-docs/166-RESEARCH.md
@.planning/phases/166-adoption-dx-docs/166-PATTERNS.md
@.planning/phases/166-adoption-dx-docs/166-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Docker-first Start Here section</name>
  <files>examples/accrue_host/README.md</files>
  <read_first>
    - examples/accrue_host/README.md
    - examples/accrue_host/docker-compose.yml
    - examples/accrue_host/config/dev.exs
    - examples/accrue_host/demo/command_manifest.exs
    - accrue/guides/first_hour.md
    - accrue/guides/organization_billing.md
  </read_first>
  <action>
    Edit the top of `examples/accrue_host/README.md` so `## Start Here` appears near the top, before caveat-heavy support or Sigra details.
    The section must include one short persona frame: this is the canonical local demo for a realistic B2B devtool Phoenix SaaS with seeded accounts, Fake-backed billing, signed webhook ingest, and mounted admin inspection.
    Make Docker the primary evaluator command block:
    `cd examples/accrue_host`
    `docker compose up --build`
    Tell the reader to open `http://localhost:4000`.
    Direct the first walkthrough to `/app/billing`, starting a Fake-backed subscription through `AccrueHost.Billing`, then inspecting `/billing`.
    Keep `mix verify` in or immediately after Start Here as the focused proof command.
    Do not put `mix verify.full`, `bash scripts/ci/accrue_host_uat.sh`, Playwright shards, or CI job details before the first successful run path.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/README.md` contains `## Start Here`.
    - In the README, `## Start Here` appears before `## Prerequisites`.
    - The README contains `docker compose up --build`.
    - The README contains `http://localhost:4000`.
    - The README contains `/app/billing`.
    - The README contains `Accrue.Processor.Fake`.
    - The README contains `mix verify`.
    - The text before the first `mix verify.full` occurrence contains `docker compose up --build` and does not contain `bash scripts/ci/accrue_host_uat.sh`.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq '## Start Here' examples/accrue_host/README.md && grep -Fq 'docker compose up --build' examples/accrue_host/README.md && grep -Fq 'http://localhost:4000' examples/accrue_host/README.md</automated>
    <automated>elixir -e 's = File.read!("examples/accrue_host/README.md"); unless :binary.match(s, "## Start Here") < :binary.match(s, "## Prerequisites"), do: System.halt(1); {idx, _} = :binary.match(s, "mix verify.full"); if String.contains?(binary_part(s, 0, idx), "bash scripts/ci/accrue_host_uat.sh"), do: System.halt(1)'</automated>
  </verify>
  <done>The README first screen gives adopters a Docker-first path to boot and inspect the demo.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve native Phoenix and trust-boundary handoffs below Start Here</name>
  <files>examples/accrue_host/README.md</files>
  <read_first>
    - examples/accrue_host/README.md
    - examples/accrue_host/demo/command_manifest.exs
    - examples/accrue_host/test/demo/command_manifest_test.exs
    - accrue/test/accrue/docs/canonical_demo_contract_test.exs
    - accrue/guides/first_hour.md
    - accrue/guides/organization_billing.md
  </read_first>
  <action>
    Keep the existing `## First run` section and manifest-backed native command order intact: `cd examples/accrue_host`, `mix setup`, `mix phx.server`, subscription through `AccrueHost.Billing`, `/webhooks/stripe`, `customer.subscription.created`, `/billing`, and `mix verify`.
    Move the Sigra caveat below the Start Here run path as a concise trust boundary: production Accrue apps integrate through host-owned `Accrue.Auth`; this demo uses Sigra for reproducible signed-in organization billing; non-Sigra teams should follow First Hour and Organization Billing.
    Keep the `### Capsule H`, `### Capsule M`, and `### Capsule R` headings because `verify_package_docs.sh` pins them.
    Keep the PGHOST/PGPORT/PGUSER/PGPASSWORD footgun text somewhere visible below Start Here. Add the Docker-specific footguns from context: Compose uses `PGHOST=db`, publishes `5432:5432`, named volumes isolate container deps/builds/node modules, and `docker compose down --volumes` is a reset command rather than a normal stop command.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/README.md` still contains `### Capsule H`, `### Capsule M`, and `### Capsule R`.
    - `examples/accrue_host/README.md` still contains `## First run`.
    - `examples/accrue_host/README.md` still contains `mix setup` before `mix phx.server`.
    - `examples/accrue_host/README.md` still contains `customer.subscription.created`.
    - `examples/accrue_host/README.md` still contains `PGHOST`, `PGPORT`, `PGUSER`, and `PGPASSWORD`.
    - `examples/accrue_host/README.md` contains `docker compose down --volumes`.
    - `examples/accrue_host/README.md` contains `Accrue.Auth`.
    - `examples/accrue_host/README.md` contains `Organization billing`.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test test/demo/command_manifest_test.exs</automated>
    <automated>cd accrue && MIX_ENV=test mix test test/accrue/docs/canonical_demo_contract_test.exs test/accrue/docs/first_hour_guide_test.exs</automated>
  </verify>
  <done>The new Start Here flow coexists with the manifest-backed First run spine and trust-boundary handoffs.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh`
- `bash scripts/ci/verify_verify01_readme_contract.sh`
- `cd examples/accrue_host && MIX_ENV=test mix test test/demo/command_manifest_test.exs`
- `cd accrue && MIX_ENV=test mix test test/accrue/docs/canonical_demo_contract_test.exs test/accrue/docs/first_hour_guide_test.exs`
- `git diff --check -- examples/accrue_host/README.md`
</verification>

<success_criteria>
- README has a clear `## Start Here` section.
- Docker setup commands are prominent and truthful.
- Documentation is framed around the adopter's goal: boot and inspect a realistic Phoenix SaaS billing loop locally.
- Existing proof-depth and manifest-backed doc contracts remain intact.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> evaluator action | README commands cause users to run local services and inspect billing state. |
| Demo truth -> production truth | The checked-in Sigra/Fake demo must not imply production apps require Sigra or live Stripe keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-03 | Spoofing | Demo identity/auth framing | mitigate | Move Sigra below Start Here but keep explicit `Accrue.Auth`, First Hour, and Organization Billing handoffs. |
| T-166-04 | Information Disclosure | Local setup instructions | mitigate | State no live Stripe keys are required for Start Here and keep local Fake-backed proof framing. |
| T-166-05 | Tampering | Docker volume cleanup guidance | mitigate | Describe `docker compose down --volumes` as reset/cleanup only, not the normal stop path. |
</threat_model>

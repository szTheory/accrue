---
phase: 17-milestone-closure-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - scripts/ci/accrue_host_seed_e2e.exs
  - examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs
  - RELEASING.md
  - guides/testing-live-stripe.md
  - CONTRIBUTING.md
  - accrue/test/accrue/docs/release_guidance_test.exs
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "Planning records show both Phase 13 and the canonical local demo outcome as complete."
    - "Rerunning the browser fixture seed removes only fixture-owned webhook, replay, and payment-failed rows."
    - "Release, provider-parity, and contributor docs reference the current trust lanes and job names only."
    - "Focused docs contracts and the host trust lane still pass after the cleanup."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Canonical-demo milestone checklist marked complete"
      contains: "- [x] Phoenix developers can clone the repository, run the canonical local demo, create a Fake-backed subscription, inspect/replay billing state in admin, and run the focused proof suite without hidden state."
    - path: "scripts/ci/accrue_host_seed_e2e.exs"
      provides: "Fixture-owned browser seed cleanup"
      contains: "caused_by_webhook_event_id"
    - path: "examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs"
      provides: "Regression proof that unrelated shared DB history is preserved"
      contains: "preserves unrelated invoice.payment_failed and admin.webhook.replay.completed rows"
    - path: "RELEASING.md"
      provides: "Current required/provider-parity/advisory release wording"
      contains: "required deterministic gate"
    - path: "guides/testing-live-stripe.md"
      provides: "Provider-parity guide without stale CI job references"
      contains: "live-stripe"
    - path: "CONTRIBUTING.md"
      provides: "Contributor setup that points browser UAT to examples/accrue_host"
      contains: "Node.js for browser UAT in `examples/accrue_host`"
  key_links:
    - from: "scripts/ci/accrue_host_seed_e2e.exs"
      to: "examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs"
      via: "fixture-owned delete predicates proven against unrelated event rows"
      pattern: "caused_by_webhook_event_id|processor_event_id|sub_host_browser_replay"
    - from: "RELEASING.md"
      to: "accrue/test/accrue/docs/release_guidance_test.exs"
      via: "positive and negative docs assertions"
      pattern: "Phase 9 release gate|required deterministic gate"
    - from: "guides/testing-live-stripe.md"
      to: ".github/workflows/ci.yml"
      via: "current workflow job names and advisory status"
      pattern: "release-gate|host-integration|live-stripe"
---

<objective>
Close the non-critical v1.2 milestone-audit tech debt without reopening product scope.

Purpose: Remove stale bookkeeping and trust-lane drift while preserving the verified Phase 13-16 outcomes.
Output: One focused cleanup that updates the remaining planning/doc records, narrows browser seed deletion to fixture-owned rows, and extends regression coverage for docs and host trust checks.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/17-milestone-closure-cleanup/17-RESEARCH.md
@.planning/phases/17-milestone-closure-cleanup/17-VALIDATION.md
@.planning/phases/17-milestone-closure-cleanup/17-PATTERNS.md
@.planning/phases/13-canonical-demo-tutorial/13-03-SUMMARY.md
@.planning/phases/14-adoption-front-door/14-03-SUMMARY.md
@.planning/phases/15-trust-hardening/15-02-SUMMARY.md
@.planning/phases/15-trust-hardening/15-03-SUMMARY.md
@.planning/phases/16-expansion-discovery/16-02-SUMMARY.md
@.github/workflows/ci.yml
@examples/accrue_host/test/support/host_flow_proof_case.ex
@scripts/ci/accrue_host_uat.sh
@scripts/ci/verify_package_docs.sh

<interfaces>
From `scripts/ci/accrue_host_seed_e2e.exs`:
```elixir
Events.record(%{
  type: "invoice.payment_failed",
  subject_type: "Subscription",
  subject_id: subscription.id,
  actor_type: "webhook",
  actor_id: webhook.processor_event_id,
  caused_by_webhook_event_id: webhook.id
})
```

From `.github/workflows/ci.yml`:
```yaml
jobs:
  release-gate:
  admin-drift-docs:
  host-integration:
  annotation-sweep:
  live-stripe:
```

From `accrue/test/accrue/docs/release_guidance_test.exs`:
```elixir
assert releasing =~ "Canonical local demo: Fake"
assert releasing =~ "Provider parity: Stripe test mode"
assert releasing =~ "Advisory/manual: live Stripe"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the Phase 13 and canonical-demo bookkeeping drift</name>
  <files>.planning/PROJECT.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/v1.2-MILESTONE-AUDIT.md, .planning/phases/13-canonical-demo-tutorial/13-VERIFICATION.md</read_first>
  <action>Update only the stale v1.2 Active checklist bullet in `.planning/PROJECT.md` so it is checked and reads exactly `- [x] Phoenix developers can clone the repository, run the canonical local demo, create a Fake-backed subscription, inspect/replay billing state in admin, and run the focused proof suite without hidden state.` Do not change the roadmap wording for Phase 13 unless you find a fresh mismatch while reading; `ROADMAP.md` already records `- [x] Phase 13: Canonical Demo + Tutorial` and must remain the canonical milestone status. Keep the Phase 16 recommendation-only notes intact.</action>
  <acceptance_criteria>
    - `rg -n --fixed-strings -- "- [x] Phoenix developers can clone the repository, run the canonical local demo, create a Fake-backed subscription, inspect/replay billing state in admin, and run the focused proof suite without hidden state." .planning/PROJECT.md` returns exactly one match.
    - `rg -n --fixed-strings -- "- [x] Phase 13: Canonical Demo + Tutorial — make \`examples/accrue_host\` the polished local demo and tutorial proof path. (completed 2026-04-17)" .planning/ROADMAP.md` returns one match.
    - `rg -n "PROJECT.md milestone checklist still shows the canonical demo outcome unchecked" .planning/v1.2-MILESTONE-AUDIT.md` still points to the resolved source item without introducing any new unchecked canonical-demo bullet in `.planning/PROJECT.md`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n --fixed-strings -- "- [x] Phoenix developers can clone the repository, run the canonical local demo, create a Fake-backed subscription, inspect/replay billing state in admin, and run the focused proof suite without hidden state." .planning/PROJECT.md && rg -n --fixed-strings -- "- [x] Phase 13: Canonical Demo + Tutorial — make \`examples/accrue_host\` the polished local demo and tutorial proof path. (completed 2026-04-17)" .planning/ROADMAP.md</automated>
  </verify>
  <done>PROJECT and ROADMAP agree that the canonical demo and Phase 13 are complete, with no new planning-state drift introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow browser seed cleanup to fixture-owned rows and add a preservation regression test</name>
  <files>scripts/ci/accrue_host_seed_e2e.exs, examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs</files>
  <read_first>scripts/ci/accrue_host_seed_e2e.exs, examples/accrue_host/test/support/host_flow_proof_case.ex, examples/accrue_host/test/accrue_host_web/admin_webhook_replay_test.exs, examples/accrue_host/test/accrue_host_web/trust_smoke_test.exs, scripts/ci/accrue_host_uat.sh</read_first>
  <behavior>
    - Test 1: Seeding twice deletes the prior fixture-owned `WebhookEvent` rows for `evt_host_browser_replay` and `evt_host_browser_first_run` plus their linked `invoice.payment_failed` or `admin.webhook.replay.completed` `Accrue.Events.Event` rows, then recreates the fixture successfully.
    - Test 2: Unrelated `invoice.payment_failed` and `admin.webhook.replay.completed` rows with different `processor_event_id`, different `caused_by_webhook_event_id`, and different subscription/webhook IDs remain in the database after the cleanup runs.
  </behavior>
  <action>Rewrite the destructive `Event` delete in `scripts/ci/accrue_host_seed_e2e.exs` so it no longer deletes by type alone. Use fixture-owned identifiers already created in the script: `evt_host_browser_replay`, `evt_host_browser_first_run`, `sub_host_browser_replay`, `cus_host_browser_replay`, the `subscription.id` selected from the seeded customer, and the linked `webhook.id`. The final delete predicate must key `accrue_events` to fixture ownership via `event.actor_id in ["evt_host_browser_replay", "evt_host_browser_first_run"]`, `event.caused_by_webhook_event_id in ^fixture_webhook_ids`, or `event.subject_type == "Subscription" and event.subject_id in ^fixture_subscription_ids`; do not leave any `event.type in [...] and event.subject_type in [...]` delete without fixture identity. Add `examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs` as the regression contract that inserts both fixture-owned rows and unrelated rows, runs the cleanup path, and asserts the unrelated rows survive. Keep the trigger disable/enable around the event delete, but only for the narrowed delete. Preserve the existing delete ordering for webhook, subscription item, subscription, customer, and user cleanup.</action>
  <acceptance_criteria>
    - `rg -n "event\\.actor_id|caused_by_webhook_event_id|subject_id in" scripts/ci/accrue_host_seed_e2e.exs` shows fixture-owned predicates in the event delete block.
    - `rg -n 'event\\.type in \\["invoice\\.payment_failed", "admin\\.webhook\\.replay\\.completed"\\].*subject_type in \\["Subscription", "WebhookEvent"\\]' scripts/ci/accrue_host_seed_e2e.exs` returns no matches.
    - `examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs` contains assertions for both `preserves unrelated` and `deletes fixture-owned` behavior.
    - The focused regression test passes; full host UAT remains green in wave-end/final verification.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test test/accrue_host/seed_e2e_cleanup_test.exs --trace</automated>
  </verify>
  <done>The browser seed can rerun without erasing unrelated shared DB history, and a dedicated regression test proves the fixture-owned boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale release and contributor references, then lock them with docs contracts</name>
  <files>RELEASING.md, guides/testing-live-stripe.md, CONTRIBUTING.md, accrue/test/accrue/docs/release_guidance_test.exs, accrue/test/accrue/docs/package_docs_verifier_test.exs, scripts/ci/verify_package_docs.sh</files>
  <read_first>RELEASING.md, guides/testing-live-stripe.md, CONTRIBUTING.md, accrue/test/accrue/docs/release_guidance_test.exs, accrue/test/accrue/docs/package_docs_verifier_test.exs, scripts/ci/verify_package_docs.sh, .github/workflows/ci.yml, .planning/phases/14-adoption-front-door/14-03-SUMMARY.md, .planning/phases/15-trust-hardening/15-03-SUMMARY.md</read_first>
  <action>Update `RELEASING.md` to remove the exact stale phrase `Phase 9 release gate` and replace it with current wording anchored to the existing CI lane, for example `release-gate workflow` or `required deterministic gate`, while preserving the existing lane labels `Canonical local demo: Fake`, `Provider parity: Stripe test mode`, and `Advisory/manual: live Stripe`. Update `guides/testing-live-stripe.md` so the scheduled-run section no longer mentions the non-existent `primary \`test\` job`; instead reference current workflow surfaces such as `release-gate`, `host-integration`, or the workflow summary without inventing a new job name. Update `CONTRIBUTING.md` so the toolchain bullet reads exactly `- Node.js for browser UAT in \`examples/accrue_host\`` and keep the rest of the contributor routing intact. Extend `accrue/test/accrue/docs/release_guidance_test.exs` with negative assertions for `Phase 9 release gate`, `primary \`test\` job`, and `Node.js for browser UAT in \`accrue_admin\``, plus positive assertions for `release-gate`, `host-integration`, and `examples/accrue_host` where appropriate. Extend `scripts/ci/verify_package_docs.sh` with fixed-string or absent-regex checks for the corrected contributor/browser-lane wording and the stale strings being absent. Update `accrue/test/accrue/docs/package_docs_verifier_test.exs` so the shell verifier contract fails when those new fixed invariants drift.</action>
  <acceptance_criteria>
    - `rg -n "Phase 9 release gate|primary \`test\` job|Node.js for browser UAT in \`accrue_admin\`" RELEASING.md guides/testing-live-stripe.md CONTRIBUTING.md accrue/test/accrue/docs/release_guidance_test.exs scripts/ci/verify_package_docs.sh` returns no matches.
    - `rg -n --fixed-strings "release-gate" RELEASING.md guides/testing-live-stripe.md accrue/test/accrue/docs/release_guidance_test.exs` returns at least one match.
    - `rg -n --fixed-strings "host-integration" guides/testing-live-stripe.md accrue/test/accrue/docs/release_guidance_test.exs` returns at least one match.
    - `rg -n --fixed-strings "Node.js for browser UAT in \`examples/accrue_host\`" CONTRIBUTING.md scripts/ci/verify_package_docs.sh accrue/test/accrue/docs/release_guidance_test.exs` returns at least one match.
    - `cd accrue && mix test test/accrue/docs/release_guidance_test.exs test/accrue/docs/package_docs_verifier_test.exs --trace` passes and `bash scripts/ci/verify_package_docs.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/release_guidance_test.exs test/accrue/docs/package_docs_verifier_test.exs --trace && cd .. && bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>Release/provider-parity/contributor docs match the current trust lanes, stale CI references are removed, and both ExUnit and shell docs contracts lock the cleanup against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts -> milestone close decision | Stale planning status can falsely imply incomplete or complete milestone work |
| seed script -> shared test database | Destructive deletes cross from fixture cleanup into durable shared test history |
| docs -> maintainer/operator actions | Stale release or contributor wording can misroute trust checks or encourage unsafe handling of secrets/PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T | `scripts/ci/accrue_host_seed_e2e.exs` | mitigate | Replace type-only event deletion with fixture-owned predicates keyed by `actor_id`, `caused_by_webhook_event_id`, and seeded subject IDs; add a regression test proving unrelated rows survive. |
| T-17-02 | R | `.planning/PROJECT.md` | mitigate | Update the exact canonical-demo checklist line and verify it against `ROADMAP.md`, `REQUIREMENTS.md`, and the Phase 13 verification record. |
| T-17-03 | I | `RELEASING.md`, `guides/testing-live-stripe.md`, `CONTRIBUTING.md` | mitigate | Preserve the existing no-secrets/no-PII wording, remove stale trust-lane references, and lock required phrases plus forbidden phrases in ExUnit and shell checks. |
| T-17-04 | E | docs and workflow guidance | mitigate | Reference only current workflow lanes (`release-gate`, `host-integration`, `live-stripe`) so contributors do not elevate advisory checks into the required lane or bypass the trust gate. |
| T-17-05 | D | docs verification coverage | accept | Focused docs contracts and `verify_package_docs.sh` are sufficient for this cleanup; no broader new framework is needed because Phase 14 and 15 already own the surrounding coverage. |
</threat_model>

<verification>
- `rg -n --fixed-strings -- "- [x] Phoenix developers can clone the repository, run the canonical local demo, create a Fake-backed subscription, inspect/replay billing state in admin, and run the focused proof suite without hidden state." .planning/PROJECT.md`
- `cd examples/accrue_host && MIX_ENV=test mix test test/accrue_host/seed_e2e_cleanup_test.exs --trace`
- `cd accrue && mix test test/accrue/docs/release_guidance_test.exs test/accrue/docs/package_docs_verifier_test.exs --trace`
- `bash scripts/ci/verify_package_docs.sh`
- `bash scripts/ci/accrue_host_uat.sh`
</verification>

<success_criteria>
1. `.planning/PROJECT.md` and `.planning/ROADMAP.md` both present Phase 13 and the canonical demo outcome as complete.
2. `scripts/ci/accrue_host_seed_e2e.exs` deletes only fixture-owned webhook/payment-failed/replay rows, and `examples/accrue_host/test/accrue_host/seed_e2e_cleanup_test.exs` proves unrelated rows survive.
3. `RELEASING.md`, `guides/testing-live-stripe.md`, and `CONTRIBUTING.md` contain no stale `Phase 9`, `primary test job`, or wrong browser-lane wording.
4. Focused docs contracts, the shell verifier, and the host trust lane all pass after the cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/17-milestone-closure-cleanup/17-01-SUMMARY.md`
</output>

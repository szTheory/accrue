---
phase: 174
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/assets/css/theme.css
autonomous: true
requirements:
  - DSY-01

must_haves:
  truths:
    - "theme.css exports --ax-leading-tight (1.2), --ax-leading-normal (1.4), --ax-leading-relaxed (1.5) inside html.accrue-admin {}"
    - "theme.css exports --ax-tracking-tight (-0.02em), --ax-tracking-normal (0), --ax-tracking-wide (0.04em), --ax-tracking-caps (0.08em)"
    - "theme.css exports --ax-measure (68ch)"
    - "theme.css exports all 4 --ax-transition-* bundles composed from existing --ax-dur-*/--ax-ease-* atoms, with background-color (not background shorthand)"
    - "The existing @media (prefers-reduced-motion: reduce) block in theme.css contains overrides for all 4 --ax-transition-* bundles using --ax-dur-instant"
  artifacts:
    - path: "accrue_admin/assets/css/theme.css"
      provides: "All new type micro-tokens and transition-bundle tokens"
      contains: "--ax-leading-tight"
  key_links:
    - from: "theme.css :root html.accrue-admin block"
      to: "app.css literal→token migration (Plan 02)"
      via: "CSS custom property cascade"
      pattern: "--ax-leading-tight"
---

<objective>
Add all missing design-system token definitions to theme.css: line-height tokens (--ax-leading-*), letter-spacing tokens (--ax-tracking-*), the reading-measure token (--ax-measure), four transition-bundle tokens (--ax-transition-*), and their reduced-motion overrides inside the existing prefers-reduced-motion block.

Purpose: Establish the token substrate that Plan 02's literal→token migration will consume. Plans 02 and 03 cannot proceed until these tokens exist in theme.css.

Output: theme.css with 9 new custom properties + 4 reduced-motion bundle overrides, all inside the existing html.accrue-admin {} block. Zero functional CSS behavior change — these are new custom properties only; no class definitions, no value changes to existing tokens.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md

<interfaces>
<!-- From theme.css: existing motion token block pattern (the house style to follow) -->
<!-- Existing atoms that transition bundles MUST compose from (do not hardcode ms/curves): -->
<!--   --ax-dur-instant: 0ms -->
<!--   --ax-dur-1: 120ms -->
<!--   --ax-dur-2: 180ms  ← the one to use for transition bundles (default state change) -->
<!--   --ax-dur-3: 240ms -->
<!--   --ax-dur-exit: 140ms -->
<!--   --ax-ease-out: cubic-bezier(0.2, 0, 0, 1)  ← enter-neutral, use for all 4 bundles -->
<!--   --ax-ease-in: cubic-bezier(0.4, 0, 1, 1) -->
<!--   --ax-ease-inout: cubic-bezier(0.4, 0, 0.2, 1) -->
<!--   --ax-ease-emphasis: cubic-bezier(0.2, 0.9, 0.3, 1.2) -->
<!-- Back-compat aliases (DO NOT extend): --ax-motion-fast, --ax-motion-standard, --ax-theme-transition -->

<!-- Existing @media (prefers-reduced-motion: reduce) block is at theme.css ~line 157. -->
<!-- The new bundle overrides must be appended INSIDE that existing block — not a new block. -->

<!-- House comment style (copy exactly): -->
<!-- /* Category — unit clarification */          -->
<!-- --ax-token-name: value;  /* purpose comment */ -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add line-height, letter-spacing, measure, and transition-bundle tokens to theme.css</name>
  <files>accrue_admin/assets/css/theme.css</files>
  <read_first>
    - accrue_admin/assets/css/theme.css — read the full file. Identify: (a) the html.accrue-admin {} block and the existing motion token block (lines 52–68) as the style template; (b) the --ax-z-* block as the insertion landmark (new tokens go before it); (c) the @media (prefers-reduced-motion: reduce) block (~line 157) as the hook for reduced-motion overrides. Read all to avoid placement errors.
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-CONTEXT.md — D-07 (exact token block verbatim), D-08 (units: unitless/em/ch), D-09 (body stays 1.4), D-10 (keep both wide+caps), D-13 (4 bundles, background-color not background, no --ax-transition-all), D-14 (new --ax-transition-* family; freeze --ax-motion-* back-compat), D-15 (reduced-motion override at token level using --ax-dur-instant).
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md — Pattern 1 (token definitions in theme.css, house style, insertion target, full verbatim block for leading/tracking/measure) and Pattern 2 (transition bundles, exact bundle definitions, reduced-motion override block).
  </read_first>
  <action>
    Inside the html.accrue-admin {} block, insert two new token groups before the --ax-z-* block, following the exact house comment style (/* Category — unit clarification */ header, inline /* purpose */ on each value):

    Group 1 — type micro-tokens (per D-07, verbatim):
    - /* Line-height — unitless (inherits as a ratio) */
    - --ax-leading-tight: 1.2; /* display, headings */
    - --ax-leading-normal: 1.4; /* body, labels — the default */
    - --ax-leading-relaxed: 1.5; /* prose / long-form copy */
    - /* Letter-spacing — em (scales with font-size) */
    - --ax-tracking-tight: -0.02em; /* large display tightening */
    - --ax-tracking-normal: 0;
    - --ax-tracking-wide: 0.04em; /* smaller uppercase labels */
    - --ax-tracking-caps: 0.08em; /* uppercase eyebrows / section labels */
    - /* Reading measure — ch ≈ one "0" advance; 68ch ≈ 66 chars (60–75 sweet spot) */
    - --ax-measure: 68ch;

    Group 2 — transition bundles (per D-13; enter-neutral = --ax-ease-out; 4 bundles only, no --ax-transition-all):
    - /* Transition bundles — property-bundles composed from dur/ease atoms. Use background-color (not background shorthand) to never conflict with skeleton shimmer's background-position animation. */
    - --ax-transition-colors: color var(--ax-dur-2) var(--ax-ease-out), background-color var(--ax-dur-2) var(--ax-ease-out), border-color var(--ax-dur-2) var(--ax-ease-out);
    - --ax-transition-transform: transform var(--ax-dur-2) var(--ax-ease-out);
    - --ax-transition-shadow: box-shadow var(--ax-dur-2) var(--ax-ease-out);
    - --ax-transition-base: color var(--ax-dur-2) var(--ax-ease-out), background-color var(--ax-dur-2) var(--ax-ease-out), border-color var(--ax-dur-2) var(--ax-ease-out), transform var(--ax-dur-2) var(--ax-ease-out), box-shadow var(--ax-dur-2) var(--ax-ease-out);

    In the existing @media (prefers-reduced-motion: reduce) block (~line 157), append the 4 bundle overrides INSIDE the existing html.accrue-admin { } nested block, after the current last override (--ax-theme-transition: 0ms linear). Use --ax-dur-instant for all properties, linear easing (per D-15):
    - --ax-transition-colors: color var(--ax-dur-instant) linear, background-color var(--ax-dur-instant) linear, border-color var(--ax-dur-instant) linear;
    - --ax-transition-transform: transform var(--ax-dur-instant) linear;
    - --ax-transition-shadow: box-shadow var(--ax-dur-instant) linear;
    - --ax-transition-base: color var(--ax-dur-instant) linear, background-color var(--ax-dur-instant) linear, border-color var(--ax-dur-instant) linear, transform var(--ax-dur-instant) linear, box-shadow var(--ax-dur-instant) linear;

    CRITICAL constraints (per D-13/D-14/RESEARCH pitfall 2):
    - Use background-color, NEVER the background shorthand (background shorthand captures background-position and will suppress the skeleton shimmer animation).
    - Do NOT add a --ax-transition-all token — it is a perf footgun.
    - Do NOT extend the existing --ax-motion-* / --ax-theme-transition aliases — they are frozen back-compat.
    - All bundle values use var(--ax-dur-2) and var(--ax-ease-out) — never hardcoded ms or cubic-bezier values.
  </action>
  <verify>
    <automated>
      grep -c '\-\-ax-leading-tight' /Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
      grep -c '\-\-ax-tracking-caps' /Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
      grep -c '\-\-ax-measure' /Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
      grep -c '\-\-ax-transition-base' /Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
      grep -c 'background-color' /Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
      grep -c 'background ' /Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
      (the last grep should return 0 — no background shorthand in transition bundles)
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c '--ax-leading-tight' accrue_admin/assets/css/theme.css returns >= 2 (definition + reduced-motion? no — reduced-motion doesn't redefine leading; must appear at least 1 time in token definition block).
    - grep -c '--ax-leading-normal' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-leading-relaxed' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-tracking-tight' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-tracking-normal' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-tracking-wide' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-tracking-caps' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-measure' accrue_admin/assets/css/theme.css returns >= 1.
    - grep -c '--ax-transition-colors' accrue_admin/assets/css/theme.css returns >= 2 (definition + reduced-motion override).
    - grep -c '--ax-transition-transform' accrue_admin/assets/css/theme.css returns >= 2.
    - grep -c '--ax-transition-shadow' accrue_admin/assets/css/theme.css returns >= 2.
    - grep -c '--ax-transition-base' accrue_admin/assets/css/theme.css returns >= 2.
    - grep 'background ' accrue_admin/assets/css/theme.css | grep 'ax-transition' returns 0 lines (no background shorthand in bundle definitions).
    - grep '--ax-transition-all' accrue_admin/assets/css/theme.css returns 0 lines (forbidden token never defined).
    - Reduced-motion block contains the 4 bundle overrides using --ax-dur-instant.
  </acceptance_criteria>
  <done>theme.css has all 9 new custom properties (3 leading + 4 tracking + measure) and all 4 transition bundles, plus 4 reduced-motion bundle overrides — all inside the existing html.accrue-admin {} and prefers-reduced-motion blocks, following house comment style. No background shorthand used. No hardcoded ms or curve values. Back-compat aliases untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme.css → compiled bundle | Source CSS is developer-authored; output is committed to priv/static — no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Tampering | theme.css token definitions | accept | CSS custom property definitions; no executable code, no user input, no auth surface. Only risk is accidental value regression — mitigated by the grep acceptance criteria verifying exact token names. |
| T-174-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan. Zero install surface. |
</threat_model>

<verification>
After this plan completes:
- cd accrue_admin && grep -c '\-\-ax-leading-tight' assets/css/theme.css — must be >= 1
- cd accrue_admin && grep -c '\-\-ax-transition-base' assets/css/theme.css — must be >= 2 (definition + reduced-motion override)
- cd accrue_admin && grep '\-\-ax-transition-all' assets/css/theme.css — must return nothing (0 lines)
- cd accrue_admin && grep 'background ' assets/css/theme.css | grep 'transition' — must return nothing (no background shorthand in any transition rule)

NOTE: theme.css changes are NOT yet in the committed bundle — Plan 02 runs the asset rebuild after all CSS edits.
</verification>

<success_criteria>
theme.css contains all 9 new design-token custom properties (--ax-leading-tight/normal/relaxed, --ax-tracking-tight/normal/wide/caps, --ax-measure) and all 4 --ax-transition-* property-bundle tokens, plus their 4 reduced-motion bundle overrides using --ax-dur-instant, all nested inside the existing html.accrue-admin {} element following the project's house comment style.
</success_criteria>

<output>
Create .planning/phases/174-a-design-system-gap-closure-token-completeness/174-01-SUMMARY.md when done.
</output>

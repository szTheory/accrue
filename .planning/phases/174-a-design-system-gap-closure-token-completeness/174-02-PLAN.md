---
phase: 174
plan: 02
type: execute
wave: 2
depends_on:
  - 174-01
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/lib/accrue_admin/components/dunning_banner.ex
  - accrue_admin/test/accrue_admin/components/dunning_banner_test.exs
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - accrue_admin/priv/static/accrue_admin.css
autonomous: true
requirements:
  - DSY-01
  - DSY-02

must_haves:
  truths:
    - "app.css has no bare line-height numeric literals (1.2, 1.4, 1.5) — all 13 sites migrated to var(--ax-leading-*)"
    - "app.css has no bare letter-spacing em literals — all 5 sites migrated to var(--ax-tracking-*)"
    - "Every breakpoint @media in app.css carries an --ax-bp-* inline comment — 10 sites annotated"
    - "A breakpoint registry comment block appears at the top of app.css documenting the 5 bp values"
    - "The .ax-measure utility class exists in app.css"
    - "All 5 multi-line transition blocks are collapsed to a single var(--ax-transition-*) reference"
    - "dunning_banner.ex has no style= attribute — line 27 inline style removed"
    - "verify_package_docs.sh fails if an @media breakpoint in app.css lacks an --ax-bp-* comment"
    - "PackageDocsVerifierTest seed_tmp_dir! includes app.css and has a negative test for unguarded breakpoints"
    - "priv/static/accrue_admin.css is rebuilt and committed after all CSS edits"
  artifacts:
    - path: "accrue_admin/assets/css/app.css"
      provides: "Migrated CSS with token references + breakpoint registry + .ax-measure"
      contains: "--ax-bp-content"
    - path: "accrue_admin/lib/accrue_admin/components/dunning_banner.ex"
      provides: "Bypass-free banner component"
      contains: "ax-banner-danger"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Breakpoint drift guard needle"
      contains: "--ax-bp-"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Negative test for the breakpoint guard"
      contains: "900px"
    - path: "accrue_admin/priv/static/accrue_admin.css"
      provides: "Committed minified bundle with token references"
      contains: "accrue_admin.css rebuilt"
  key_links:
    - from: "app.css line-height/letter-spacing class definitions"
      to: "theme.css --ax-leading-*/--ax-tracking-* tokens (Plan 01)"
      via: "CSS custom property var() references"
      pattern: "var\\(--ax-leading-"
    - from: "verify_package_docs.sh guard"
      to: "PackageDocsVerifierTest seed_tmp_dir!"
      via: "D-04 coupling — must change in lockstep"
      pattern: "app\\.css"
---

<objective>
Three parallel deliverables, all in Wave 2, committed together:

1. app.css literal→token migration: Add the breakpoint registry comment block at the top, annotate all 10 @media breakpoint sites with --ax-bp-* inline comments, migrate all 13 line-height literals and 5 letter-spacing literals to var(--ax-leading-*/--ax-tracking-*) references, collapse all 5 multi-line transition blocks to var(--ax-transition-*) calls, and add the .ax-measure utility class.

2. DSY-02 bypass kill: Remove the inline style= attribute from dunning_banner.ex line 27 (the sole remaining token bypass in accrue_admin/lib/); add a refute assertion to the existing dunning_banner_test.exs.

3. Breakpoint guard + test coupling: Add a require_absent_regex needle to verify_package_docs.sh detecting bare breakpoint @media without --ax-bp-* comments; add app.css to seed_tmp_dir! in PackageDocsVerifierTest and add the negative test case.

4. Asset rebuild: Run mix accrue_admin.assets.build and commit priv/static/accrue_admin.css after all CSS edits (pitfall 3 — host apps consume the committed bundle).

Purpose: This plan is the bulk mechanical migration. All token bypasses and literal sites are killed here. The grep guards enforce the new invariant going forward.

Output: Migrated app.css, bypass-free dunning_banner.ex, a passing dunning_banner_test, a guard needle in verify_package_docs.sh, a negative test + seed fixture in PackageDocsVerifierTest, and a rebuilt committed CSS bundle.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md

<interfaces>
<!-- app.css migration map (from RESEARCH.md §Ground-Truth) -->
<!-- Line-height literal sites (all in app.css): -->
<!--   line 228: .ax-sidebar-name, .ax-heading → line-height: 1.2 → var(--ax-leading-tight) -->
<!--   line 234: .ax-display, .ax-kpi-value → line-height: 1.2 → var(--ax-leading-tight) -->
<!--   line 373: .ax-label, .ax-eyebrow, .ax-sidebar-link-label, ... → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 396: .ax-body → line-height: 1.5 → var(--ax-leading-relaxed) (prose, correct per D-09) -->
<!--   line 583: .ax-filter-chip → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 847: .ax-timeline-details pre, .ax-json-raw → line-height: 1.5 → var(--ax-leading-relaxed) -->
<!--   line 933: .ax-breadcrumbs-* → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 986: .ax-button, .ax-status-badge → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 1190: .ax-badge → line-height: 1.2 → var(--ax-leading-tight) -->
<!--   line 1266: .ax-field-label (first occurrence) → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 1295: .ax-field-help, .ax-field-error, .ax-dropdown-item-description → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 1363: .ax-dropdown-item-label → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   line 1394: .ax-tab → line-height: 1.4 → var(--ax-leading-normal) -->
<!--   NOTE: RESEARCH also flagged a second .ax-field-label block at line 2214 — executor must check -->
<!-- Letter-spacing literal sites: -->
<!--   line 114: .ax-dev-toolbar-label → letter-spacing: 0.08em → var(--ax-tracking-caps) -->
<!--   line 260: .ax-sidebar-group-label → letter-spacing: 0.08em → var(--ax-tracking-caps) -->
<!--   line 389: .ax-eyebrow → letter-spacing: 0.04em → var(--ax-tracking-wide) -->
<!--   line 2159: .ax-summary-title → letter-spacing: -0.02em → var(--ax-tracking-tight) -->
<!--   line 2221: .ax-field-label (detail) → letter-spacing: 0.04em → var(--ax-tracking-wide) -->
<!-- Breakpoint @media sites to annotate (from RESEARCH.md §Breakpoint @media Sites): -->
<!--   line 896: @media (min-width: 768px) → /* --ax-bp-md ↑ */ -->
<!--   line 1246: @media (min-width: 1024px) → /* --ax-bp-lg ↑ */ -->
<!--   line 1620: @media (min-width: 768px) → /* --ax-bp-md ↑ */ -->
<!--   line 1637: @media (min-width: 1024px) → /* --ax-bp-lg ↑ */ -->
<!--   line 1655: @media (max-width: 1023.98px) → /* --ax-bp-lg ↓ */ -->
<!--   line 1785: @media (max-width: 599.98px) → /* --ax-bp-sm ↓ */ -->
<!--   line 1921: @media (max-width: 599.98px) → /* --ax-bp-sm ↓ */ -->
<!--   line 2026: @media (min-width: 640px) → /* --ax-bp-content ↑ */ -->
<!--   line 2036: @media (min-width: 1024px) → /* --ax-bp-lg ↑ */ -->
<!--   line 2231: @media (min-width: 640px) → /* --ax-bp-content ↑ */ -->
<!-- Multi-line transition block collapse targets: -->
<!--   line 270: .ax-sidebar-link, .ax-card, etc. → collapse to var(--ax-transition-base) -->
<!--   line 992: .ax-button → collapse to var(--ax-transition-base) -->
<!--   line 1446: .ax-search-trigger → OPEN QUESTION: mixed speeds; collapse to var(--ax-transition-base) or flag for Phase D (executor judgment) -->
<!--   line 1967: .ax-launcher → collapse to var(--ax-transition-base) -->
<!--   line 2077: .ax-related-item → collapse to var(--ax-transition-colors) (colors only) -->
<!-- Breakpoint registry block (place at top of app.css after @font-face, before html/body rule): -->
<!--   /* === AX BREAKPOINT REGISTRY === -->
<!--      CSS @media cannot read var(), so values live here as documented constants. -->
<!--      Every @media below carries an inline token comment so it is grep-able. -->
<!--      --ax-bp-content : 640px  (intrinsic content step, not a layout tier) -->
<!--      --ax-bp-md      : 768px -->
<!--      --ax-bp-lg      : 1024px -->
<!--      --ax-bp-sm ↓    : 599.98px  (max-width guard = bp-sm - 0.02px) -->
<!--      --ax-bp-lg ↓    : 1023.98px (max-width guard = bp-lg - 0.02px) -->
<!--      =========================== */ -->
<!-- .ax-measure utility (add near typography utilities): -->
<!--   .ax-measure { max-width: var(--ax-measure); } -->
<!-- dunning_banner.ex current state (line 25-28 before fix): -->
<!--   <div class="accrue-default-dunning-banner ax-banner ax-banner-danger" style="background-color: var(--ax-danger-surface, #fef2f2); ..."> -->
<!-- dunning_banner.ex after fix: -->
<!--   <div class="accrue-default-dunning-banner ax-banner ax-banner-danger"> -->
<!-- verify_package_docs.sh guard shape (require_absent_regex pattern at line 37-44 already defined): -->
<!--   app_css="$ROOT_DIR/accrue_admin/assets/css/app.css" -->
<!--   if grep -E '@media \((min|max)-width: [0-9.]+px\)' "$app_css" | grep -qv '\-\-ax-bp-'; then -->
<!--     fail "$app_css must not have bare breakpoint @media without an --ax-bp-* annotation comment" -->
<!--   fi -->
<!-- PackageDocsVerifierTest seed_tmp_dir! additions needed: -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "accrue_admin/assets/css")) — in mkdir_p! block (~line 249) -->
<!--   copy_fixture!("accrue_admin/assets/css/app.css", tmp_dir) — after accrue_admin/mix.exs (~line 272) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate app.css — breakpoint registry, literal→token migration, transition collapse, .ax-measure utility</name>
  <files>accrue_admin/assets/css/app.css</files>
  <read_first>
    - accrue_admin/assets/css/app.css — read the full file. The line-number map in RESEARCH.md is your authoritative search target but lines may shift during editing; use both line numbers AND selector names to locate each site. Pay special attention to: the second .ax-field-label block (RESEARCH notes it may be at ~line 2214 in addition to line 1266); the .ax-search-trigger block at line 1446 (open question about mixed transition speeds — see guidance below). Also identify the correct placement for the registry comment block (after @font-face declarations, before the html, body rule) and for .ax-measure (near typography utilities).
    - accrue_admin/assets/css/theme.css — confirm the 9 new tokens from Plan 01 are present before proceeding.
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md — §Line-Height Literal Sites, §Letter-Spacing Literal Sites, §Breakpoint @media Sites, §Multi-Line Transition Block Sites, §Common Pitfalls (all 6 pitfalls).
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md — Pattern 3 (breakpoint registry block format + inline comment annotation pattern) and the transition collapse example.
  </read_first>
  <action>
    Make the following edits to app.css — all are mechanical 1:1 replacements with zero value changes (per D-11):

    A. Breakpoint registry comment block: Insert the documented constants block immediately after the last @font-face rule and before the html, body {} rule. The block is a comment only — no CSS declarations. Format per PATTERNS.md Pattern 3 (five-value registry with --ax-bp-content/md/lg and the two max-width guards).

    B. Breakpoint @media annotations: For all 10 @media breakpoint sites (lines 896, 1246, 1620, 1637, 1655, 1785, 1921, 2026, 2036, 2231 — check against actual content), add the inline token comment to the opening brace line:
       - @media (min-width: 640px) gets /* --ax-bp-content ↑ */
       - @media (min-width: 768px) gets /* --ax-bp-md ↑ */
       - @media (min-width: 1024px) gets /* --ax-bp-lg ↑ */
       - @media (max-width: 599.98px) gets /* --ax-bp-sm ↓ */
       - @media (max-width: 1023.98px) gets /* --ax-bp-lg ↓ */
       IMPORTANT: do NOT annotate prefers-color-scheme or prefers-reduced-motion @media blocks — only px breakpoint queries. Do NOT annotate min-width: 0, max-width: 16rem, or any intrinsic box constraint — only layout breakpoint @media conditions.

    C. Line-height migration (13 sites — verify against both line numbers and selector names; D-11 zero value changes):
       - All line-height: 1.2 → line-height: var(--ax-leading-tight)
       - All line-height: 1.4 → line-height: var(--ax-leading-normal)
       - All line-height: 1.5 → line-height: var(--ax-leading-relaxed)
       Check .ax-field-label at both occurrences (~lines 1266 and 2214).

    D. Letter-spacing migration (5 sites — D-11 zero value changes):
       - All letter-spacing: 0.08em → letter-spacing: var(--ax-tracking-caps)
       - All letter-spacing: 0.04em → letter-spacing: var(--ax-tracking-wide)
       - All letter-spacing: -0.02em → letter-spacing: var(--ax-tracking-tight)

    E. Transition block collapse (5 sites):
       - Line 270 block (.ax-sidebar-link, .ax-card, etc.): collapse the multi-property transition: to transition: var(--ax-transition-base);
       - Line 992 block (.ax-button): collapse to transition: var(--ax-transition-base);
       - Line 1446 block (.ax-search-trigger): inspect the actual block. If it mixes --ax-theme-transition (colors) with --ax-motion-fast (transform) at intentionally different speeds, flag it with a comment /* Phase D: asymmetric speed — collapse pending */ and leave as-is. If the mixed speeds are effectively the same (both resolve to similar durations), collapse to var(--ax-transition-base). See D-16: do not encode a 5th bundle; flag for Phase D instead.
       - Line 1967 block (.ax-launcher): collapse to transition: var(--ax-transition-base);
       - Line 2077 block (.ax-related-item): collapse to transition: var(--ax-transition-colors); (colors/border only, per RESEARCH)

    F. .ax-measure utility class: Add near the typography utility classes (near .ax-body, .ax-heading etc.):
       .ax-measure { max-width: var(--ax-measure); }
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue && grep -E 'line-height: [0-9]\.[0-9]' accrue_admin/assets/css/app.css | wc -l
      cd /Users/jon/projects/accrue && grep -E 'letter-spacing: -?[0-9.]+em' accrue_admin/assets/css/app.css | wc -l
      cd /Users/jon/projects/accrue && grep -E '@media \((min|max)-width: [0-9.]+px\)' accrue_admin/assets/css/app.css | grep -v '\-\-ax-bp-' | wc -l
      cd /Users/jon/projects/accrue && grep -c '\.ax-measure' accrue_admin/assets/css/app.css
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -E 'line-height: [0-9]\.[0-9]' accrue_admin/assets/css/app.css returns ZERO lines (all 13 literal sites migrated).
    - grep -E 'letter-spacing: -?[0-9.]+em' accrue_admin/assets/css/app.css returns ZERO lines (all 5 literal sites migrated).
    - grep -E '@media \((min|max)-width: [0-9.]+px\)' accrue_admin/assets/css/app.css | grep -v '\-\-ax-bp-' returns ZERO lines (all 10 breakpoint @media sites annotated; any unannotated = drift).
    - grep -c '.ax-measure' accrue_admin/assets/css/app.css returns >= 1.
    - grep -c '--ax-bp-content' accrue_admin/assets/css/app.css returns >= 1 (in registry block).
    - grep -c 'var(--ax-leading-tight)' accrue_admin/assets/css/app.css returns >= 3 (at least lines 228, 234, 1190).
    - grep -c 'var(--ax-transition-base)' accrue_admin/assets/css/app.css returns >= 3 (collapsed blocks at 270, 992, 1967).
  </acceptance_criteria>
  <done>app.css has zero bare line-height, letter-spacing, and breakpoint literals; all 10 @media breakpoint sites carry --ax-bp-* comments; the breakpoint registry block is at the top; .ax-measure utility class exists; multi-line transition blocks are collapsed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove dunning_banner.ex inline style bypass and add regression assertion</name>
  <files>
    accrue_admin/lib/accrue_admin/components/dunning_banner.ex
    accrue_admin/test/accrue_admin/components/dunning_banner_test.exs
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/components/dunning_banner.ex — read lines 20–35. Confirm line 27 contains the style= attribute with hex fallbacks (#fef2f2, #991b1b, #fecaca). Confirm lines 26–27 have class="accrue-default-dunning-banner ax-banner ax-banner-danger". The fix is to remove the style= attribute only; class= and all other content unchanged.
    - accrue_admin/test/accrue_admin/components/dunning_banner_test.exs — read the full test file. Find the test "renders the default message when dunning is active and no inner_block is given" (or equivalent). After the html assertion, add the refute. Confirm the test currently passes before the fix (run cd accrue_admin && mix test test/accrue_admin/components/dunning_banner_test.exs).
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md — §DSY-02 Token Bypasses (confirmed) and §Code Examples (dunning banner before/after).
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md — Pattern for dunning_banner.ex (lines 188–208).
  </read_first>
  <action>
    In dunning_banner.ex: Remove the style= attribute from the outer div on line 27 (or wherever the inline style with #fef2f2/#991b1b/#fecaca is located after reading the file). The div keeps its existing class= attribute (class="accrue-default-dunning-banner ax-banner ax-banner-danger"). All other content — inner HEEx, assigns, slot — is unchanged.

    In dunning_banner_test.exs: In the existing test that renders the banner (the one asserting the rendered HTML matches expected output), add this assertion after the existing html assertions:
    refute html =~ ~s(style=), "inline style= attribute must not appear in dunning banner (DSY-02)"

    Do NOT touch brand-config hex defaults in *_live.ex, layouts.ex, or brand_plug.ex — those are explicitly out of scope per D (Claude's Discretion) in CONTEXT.md.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue && grep -rn 'style=' accrue_admin/lib/accrue_admin/components/dunning_banner.ex
      cd /Users/jon/projects/accrue/accrue_admin && mix test test/accrue_admin/components/dunning_banner_test.exs
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -rn 'style=' accrue_admin/lib/accrue_admin/components/dunning_banner.ex returns zero lines (no style= attribute remains).
    - grep -rn '#fef2f2\|#991b1b\|#fecaca' accrue_admin/lib/ returns zero lines (hex fallbacks gone from the lib tree).
    - cd accrue_admin && mix test test/accrue_admin/components/dunning_banner_test.exs exits 0 with all tests green.
    - The dunning_banner_test.exs contains a refute that prevents style= from reappearing (the new assertion is present in the file).
  </acceptance_criteria>
  <done>dunning_banner.ex line 27 has no style= attribute; the banner renders purely via .ax-banner.ax-banner-danger CSS classes; the test refutes the presence of any style= attribute, guarding against future regression.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add breakpoint guard needle to verify_package_docs.sh and couple it to PackageDocsVerifierTest</name>
  <files>
    scripts/ci/verify_package_docs.sh
    accrue/test/accrue/docs/package_docs_verifier_test.exs
  </files>
  <read_first>
    - scripts/ci/verify_package_docs.sh — read the full file. The require_absent_regex function is already defined at lines 37–44. Find the section structure and the last require_* call before any for-loop to identify the correct insertion point for the new DSY-01 token-bypass guard section. Note the existing pattern of using require_absent_regex with a file path and a regex string.
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — read the full file (especially lines 241–280 for seed_tmp_dir! and copy_fixture! patterns). Identify: (a) the @script_path variable definition, (b) the exact structure of an existing negative test case (lines 32–60 pattern), (c) the seed_tmp_dir! function and its mkdir_p! calls and copy_fixture! list. Note lines 249–252 for the mkdir_p! block and line 272 for where to insert the new app.css copy.
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-RESEARCH.md — §The Token-Bypass Grep Guard and Its Negative-Test Coupling (D-04), including the exact grep pattern for the guard and the exact negative test structure.
    - .planning/phases/174-a-design-system-gap-closure-token-completeness/174-PATTERNS.md — Pattern for verify_package_docs.sh (the new needle) and Pattern for package_docs_verifier_test.exs (seed_tmp_dir! modification and new negative test).
  </read_first>
  <action>
    In verify_package_docs.sh: After the last existing require_* call (and before any for-loop), add a new section with the comment # Token bypass guards (Phase 174, DSY-01) and the following guard logic:

    app_css="$ROOT_DIR/accrue_admin/assets/css/app.css"
    if grep -E '@media \((min|max)-width: [0-9.]+px\)' "$app_css" | grep -qv '\-\-ax-bp-'; then
      fail "$app_css must not have bare breakpoint @media without an --ax-bp-* annotation comment (DSY-01 — add a /* --ax-bp-NAME ↑/↓ */ comment to every breakpoint @media)"
    fi

    Note: the fail message must mention "app.css" and "--ax-bp-" (the negative test asserts output =~ "app.css" and output =~ "--ax-bp-").

    In package_docs_verifier_test.exs — TWO changes:

    Change 1 — seed_tmp_dir! additions (BOTH required together — D-04 coupling):
    a) In the File.mkdir_p! block (~line 249), add: File.mkdir_p!(Path.join(tmp_dir, "accrue_admin/assets/css"))
    b) After the copy_fixture!("accrue_admin/mix.exs", tmp_dir) call (~line 272), add: copy_fixture!("accrue_admin/assets/css/app.css", tmp_dir)

    Change 2 — New negative test (add after the last existing negative test, before the defp helpers):
    Add a test named "package docs verifier rejects unguarded breakpoint @media in app.css" that:
    a) Creates a tmp_dir, calls on_exit(fn -> File.rm_rf(tmp_dir) end), and calls seed_tmp_dir!(tmp_dir)
    b) Reads the seeded app.css and appends a bare unregistered breakpoint: "\n@media (min-width: 900px) { .ax-drift { display: block; } }\n"
    c) Writes the drifted content back to the tmp app.css path
    d) Runs System.cmd("bash", [@script_path], stderr_to_stdout: true, env: [{"ROOT_DIR", tmp_dir}])
    e) Asserts status != 0, output =~ "[verify_package_docs]", output =~ "app.css", output =~ "--ax-bp-"

    The @script_path attribute is already defined in the test module — reuse it.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue && bash scripts/ci/verify_package_docs.sh
      cd /Users/jon/projects/accrue/accrue && mix test test/accrue/docs/package_docs_verifier_test.exs
    </automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/ci/verify_package_docs.sh exits 0 (the guard passes on the now-annotated app.css from Task 1).
    - cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs exits 0 with all tests green including the new negative test for unguarded breakpoints.
    - The new negative test in package_docs_verifier_test.exs: asserts status != 0 when an unregistered 900px breakpoint without a comment is added; asserts output contains "app.css" and "--ax-bp-".
    - seed_tmp_dir! now includes accrue_admin/assets/css/app.css in the copy_fixture! list (grep copy_fixture package_docs_verifier_test.exs | grep app.css returns a result).
  </acceptance_criteria>
  <done>verify_package_docs.sh detects bare breakpoint @media lacking --ax-bp-* comments and exits non-zero with a descriptive message. The PackageDocsVerifierTest seed includes app.css and has a green negative test proving the guard works. The script passes on the annotated app.css from Task 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Rebuild asset bundle and commit priv/static</name>
  <files>accrue_admin/priv/static/accrue_admin.css</files>
  <read_first>
    - accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex — confirm the build command (Tailwind v3 CLI → esbuild, no PostCSS). The run command is: cd accrue_admin && mix accrue_admin.assets.build
    - Confirm Tasks 1–3 in this plan are complete (all CSS edits in app.css are done, dunning_banner.ex is fixed) before running the build.
  </read_first>
  <action>
    Run the asset build from the accrue_admin directory:
      cd accrue_admin && mix accrue_admin.assets.build

    This will regenerate priv/static/accrue_admin.css (minified bundle) and priv/static/accrue_admin.js.

    After the build succeeds, stage and git-add priv/static/accrue_admin.css (and .js if changed).

    The committed bundle is what host apps consume — do NOT skip this step (per RESEARCH pitfall 3).
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue_admin && mix accrue_admin.assets.build
      grep -c 'ax-leading-tight\|ax-tracking-caps\|ax-transition-base' /Users/jon/projects/accrue/accrue_admin/priv/static/accrue_admin.css
    </automated>
  </verify>
  <acceptance_criteria>
    - mix accrue_admin.assets.build exits 0 (no compilation errors).
    - priv/static/accrue_admin.css is modified (mtime updated).
    - grep -E 'line-height: ?1\.[245]' accrue_admin/priv/static/accrue_admin.css returns ZERO lines (token references are minified forms like line-height:var(--ax-leading-tight)).
    - The file is staged for commit (git status shows priv/static/accrue_admin.css modified).
  </acceptance_criteria>
  <done>priv/static/accrue_admin.css is rebuilt and staged. The minified bundle reflects all token migrations and the dunning banner fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.css → committed bundle | Developer-authored CSS; no runtime user input; output committed to priv/static |
| verify_package_docs.sh → CI gate | Bash script reads files in the repo; no external input; only risk is false-positive/negative in regex |
| dunning_banner.ex → rendered HTML | HEEx template rendered server-side; removing style= reduces XSS surface (no inline attribute to inject into) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-02 | Tampering | dunning_banner.ex style= removal | mitigate | Removing inline style= reduces the XSS/CSP bypass surface — style= with hex fallbacks was weaker than class-based CSS token resolution. The refute assertion in dunning_banner_test.exs ensures the bypass cannot reappear. No new injection surface introduced. |
| T-174-03 | Tampering | verify_package_docs.sh breakpoint needle | accept | The grep guard is a read-only check on developer-authored CSS. The risk of the grep regex false-matching non-breakpoint queries (e.g., intrinsic min-width: 0 or max-width: 16rem) is mitigated by the regex targeting the `@media (` prefix — intrinsic box constraints do not appear inside @media conditions. |
| T-174-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. mix accrue_admin.assets.build uses pinned npx tailwindcss@3.4.17 and esbuild@0.25.3 already in the project. |
</threat_model>

<verification>
After all 4 tasks in this plan complete, run in sequence:

1. grep -E 'line-height: [0-9]\.[0-9]' accrue_admin/assets/css/app.css — must return ZERO lines
2. grep -E 'letter-spacing: -?[0-9.]+em' accrue_admin/assets/css/app.css — must return ZERO lines
3. grep -E '@media \((min|max)-width: [0-9.]+px\)' accrue_admin/assets/css/app.css | grep -v '\-\-ax-bp-' — must return ZERO lines
4. grep -rn 'style=' accrue_admin/lib/accrue_admin/components/dunning_banner.ex — must return ZERO lines
5. bash scripts/ci/verify_package_docs.sh — must exit 0
6. cd accrue_admin && mix test test/accrue_admin/components/dunning_banner_test.exs — must exit 0
7. cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs — must exit 0
8. cd accrue_admin && mix test — full admin suite green (flaky PdfTest dodge: use --seed 0 if needed)
</verification>

<success_criteria>
All CSS literals tokenized (zero bare line-height/letter-spacing/breakpoint values remain in app.css), dunning banner renders without any inline style attribute, the breakpoint guard in verify_package_docs.sh detects unannotated @media breakpoints and exits non-zero, the PackageDocsVerifierTest negative test confirms the guard works and seed includes app.css, and the committed bundle at priv/static/accrue_admin.css reflects all changes.
</success_criteria>

<output>
Create .planning/phases/174-a-design-system-gap-closure-token-completeness/174-02-SUMMARY.md when done.
</output>

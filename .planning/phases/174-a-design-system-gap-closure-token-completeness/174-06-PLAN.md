---
phase: 174
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
gap_closure: true
requirements:
  - DSY-01

must_haves:
  truths:
    - "seed_tmp_dir! creates examples/accrue_host/docs/ directory and copies adoption-proof-matrix.md into the seeded tmp directory"
    - "A negative test exists that injects 'Stripe-only' into the seeded adoption-proof-matrix.md and asserts verify_package_docs.sh exits non-zero"
    - "All 6 existing negative tests in PackageDocsVerifierTest still pass (no regression)"
    - "cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs exits 0"
  artifacts:
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Corrected seed_tmp_dir! + new Stripe-only negative test"
      contains: "adoption-proof-matrix.md"
  key_links:
    - from: "seed_tmp_dir! copy_fixture! call"
      to: "examples/accrue_host/docs/adoption-proof-matrix.md"
      via: "File.cp! with Path.expand relative to __DIR__"
      pattern: "adoption-proof-matrix"
    - from: "negative test"
      to: "verify_package_docs.sh line 304 require_absent_regex guard"
      via: "System.cmd bash @script_path with ROOT_DIR env var"
      pattern: "Stripe-only"
---

<objective>
Fix the broken coupling between verify_package_docs.sh line 304 and PackageDocsVerifierTest's seed_tmp_dir! (DSY-01, Gap 2).

verify_package_docs.sh runs `require_absent_regex` against `examples/accrue_host/docs/adoption-proof-matrix.md`. seed_tmp_dir! in the test never copies that file. When the guard fires during negative tests, grep exits code 2 (file not found); inside bash's `if` condition, `set -e` is suppressed and the if evaluates to false — the guard silently passes. A PR reintroducing "Stripe-only" language in that file would not be caught by CI tests.

Purpose: Close the silent-pass by seeding the file in every negative test's tmp directory, then adding a dedicated negative test that verifies the guard fires.

Output: corrected seed_tmp_dir! (2 additions: mkdir_p! + copy_fixture!). One new negative test. All existing tests pass.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/174-a-design-system-gap-closure-token-completeness/174-VERIFICATION.md

<interfaces>
<!-- Existing seed_tmp_dir! structure (package_docs_verifier_test.exs lines 276-311): -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "accrue/guides"))          line 277 -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "accrue_admin"))            line 278 -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "accrue_admin/assets/css")) line 279 -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "accrue_portal"))           line 280 -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "examples/accrue_host"))    line 281  ← already present -->
<!--   File.mkdir_p!(Path.join(tmp_dir, "scripts/ci"))              line 282 -->
<!--   copy_fixture!("README.md", tmp_dir)                          line 284 -->
<!--   ... 22 copy_fixture! calls total ...                         lines 284-310 -->
<!--   copy_fixture!("scripts/ci/accrue_host_uat.sh", tmp_dir)      line 310 -->

<!-- copy_fixture!/2 signature (lines 270-274): -->
<!--   defp copy_fixture!(relative_path, tmp_dir) do -->
<!--     destination = Path.join(tmp_dir, relative_path) -->
<!--     File.mkdir_p!(Path.dirname(destination)) -->
<!--     File.cp!(Path.expand("../../../../" <> relative_path, __DIR__), destination) -->
<!--   end -->
<!-- NOTE: copy_fixture! already calls File.mkdir_p!(Path.dirname(destination)) internally, -->
<!-- so a separate mkdir_p! for "examples/accrue_host/docs" is redundant but harmless. -->
<!-- HOWEVER: the VERIFICATION.md gap.missing explicitly says to add the mkdir_p! call -->
<!-- (matching project memory note: couple the gap exactly). Add BOTH as specified. -->

<!-- Real file that must be copy-able: -->
<!--   examples/accrue_host/docs/adoption-proof-matrix.md — CONFIRMED EXISTS in repo -->
<!--   (ls showed it present in /Users/jon/projects/accrue/examples/accrue_host/docs/) -->

<!-- verify_package_docs.sh guard (line 304): -->
<!--   require_absent_regex "$ROOT_DIR/examples/accrue_host/docs/adoption-proof-matrix.md" 'Stripe-only|remain Stripe-only' -->
<!-- The guard fails (exits non-zero) if the file contains "Stripe-only" or "remain Stripe-only". -->

<!-- Negative test pattern (from existing tests in the file): -->
<!--   1. Generate tmp_dir with System.unique_integer -->
<!--   2. File.rm_rf!(tmp_dir) + on_exit cleanup -->
<!--   3. seed_tmp_dir!(tmp_dir) -->
<!--   4. Read the seeded file, inject the forbidden string, write it back -->
<!--   5. System.cmd("bash", [@script_path], stderr_to_stdout: true, env: [{"ROOT_DIR", tmp_dir}]) -->
<!--   6. assert status != 0 -->
<!--   7. assert output =~ "[verify_package_docs]" -->
<!--   8. assert output =~ "adoption-proof-matrix.md" or "Stripe-only" -->

<!-- The @script_path is defined at module level as "../scripts/ci/verify_package_docs.sh" -->
<!-- (relative to the accrue package root at accrue/) -->

<!-- Project memory coupling invariant: -->
<!-- "a new doc-file needle in verify_package_docs.sh must also be added to seed_tmp_dir!" -->
<!-- This gap IS exactly that coupling being incomplete. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adoption-proof-matrix.md to seed_tmp_dir! and add Stripe-only negative test</name>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — read the full file (325 lines) before editing. Pay specific attention to: (a) the copy_fixture!/2 helper at lines 270-274 — note it already calls File.mkdir_p! internally, so the explicit mkdir_p! for the docs subdir is redundant but must be added per the gap spec; (b) the seed_tmp_dir! body at lines 276-311 — identify where to insert the two new lines (after line 281 for mkdir_p!, after line 310 for copy_fixture!); (c) the pattern of all 8 negative tests (lines 32-268) — the new test follows the same pattern: generate tmp_dir, seed, mutate the target file, run script with ROOT_DIR, assert status != 0 and output assertions.
    - examples/accrue_host/docs/adoption-proof-matrix.md — read the first 20 lines to confirm the file exists and understand its content. The negative test injects "Stripe-only" (a string not present in the original) and verifies the guard fires.
    - scripts/ci/verify_package_docs.sh — read lines 298-310 to see the require_absent_regex call at line 304 and understand the guard's exact regex pattern ('Stripe-only|remain Stripe-only').
  </read_first>
  <action>
    Two separate edits to the test file:

    EDIT 1 — seed_tmp_dir! additions (two lines, per DSY-01 Gap 2 missing items):

    In seed_tmp_dir!, after the existing `File.mkdir_p!(Path.join(tmp_dir, "examples/accrue_host"))` line, add:
      File.mkdir_p!(Path.join(tmp_dir, "examples/accrue_host/docs"))

    At the end of seed_tmp_dir! (after the last copy_fixture! call for "scripts/ci/accrue_host_uat.sh"), add:
      copy_fixture!("examples/accrue_host/docs/adoption-proof-matrix.md", tmp_dir)

    EDIT 2 — new negative test (append after the last existing test "package docs verifier rejects unguarded breakpoint @media in app.css", before the defp copy_fixture!/2 definition):

    Test name: "package docs verifier rejects Stripe-only language in adoption-proof-matrix.md"

    Test body follows the exact pattern of the 6 existing negative tests:
    - Generate tmp_dir using System.unique_integer([:positive]) with a unique suffix (e.g. "accrue-docs-verifier-apm-#{...}")
    - File.rm_rf!(tmp_dir); on_exit cleanup
    - seed_tmp_dir!(tmp_dir)
    - Read the seeded adoption-proof-matrix.md from tmp_dir via File.read!, append "This is Stripe-only content.\n" to its content
    - File.write! the mutated content back to Path.join(tmp_dir, "examples/accrue_host/docs/adoption-proof-matrix.md")
    - System.cmd("bash", [@script_path], stderr_to_stdout: true, env: [{"ROOT_DIR", tmp_dir}])
    - assert status != 0
    - assert output =~ "[verify_package_docs]"
    - assert output =~ "adoption-proof-matrix.md" (the script's output must reference the file that triggered the guard)

    Do NOT inject "remain Stripe-only" — "Stripe-only" alone matches the regex and is simpler.
    Do NOT modify any existing test, any existing copy_fixture! call, or the copy_fixture!/2 or seed_tmp_dir! signatures.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/accrue/accrue && mix test test/accrue/docs/package_docs_verifier_test.exs
    </automated>
  </verify>
  <acceptance_criteria>
    - cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs exits 0, all tests pass (10 tests total: the original 9 — 1 positive + 8 negative — plus the new 10th test — count the total after editing).
    - grep -c "adoption-proof-matrix" accrue/test/accrue/docs/package_docs_verifier_test.exs returns >= 3 (the mkdir_p! line, the copy_fixture! line, and the test body at minimum).
    - grep -c "Stripe-only" accrue/test/accrue/docs/package_docs_verifier_test.exs returns >= 1 (the injection string in the new negative test).
    - All 8 pre-existing negative tests still pass (their test names are unchanged and appear in the test output as passing).
    - The new negative test appears in the test output as passing with its name "package docs verifier rejects Stripe-only language in adoption-proof-matrix.md".
    - bash scripts/ci/verify_package_docs.sh (run from repo root) still exits 0 against the real repo (no regression).
  </acceptance_criteria>
  <done>seed_tmp_dir! seeds examples/accrue_host/docs/adoption-proof-matrix.md in every negative test's tmp directory. The new negative test confirms that injecting "Stripe-only" into that file causes the verifier to fail. The Stripe-only regression guard is now enforced by CI tests, not just by manual script execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PackageDocsVerifierTest → tmp filesystem | Tests write to OS temp dir; cleaned up via on_exit. No user input. No network. |
| System.cmd → bash @script_path | Runs the CI script in an isolated tmp directory via ROOT_DIR env override. Script has read-only access to tmp_dir contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-08 | Information Disclosure | verify_package_docs.sh require_absent_regex guard | mitigate | Exactly the gap being closed: the guard silently passed when the file was absent from the seeded tmp dir. Fixed by seeding the file in seed_tmp_dir! and adding a negative test that confirms the guard fires on injection. |
| T-174-SC | Tampering | npm/pip/cargo installs | accept | No new packages. ExUnit, System.cmd, File.read!/write! are stdlib/OTP. |
</threat_model>

<verification>
Phase-gate verification (run before /gsd:verify-work):
1. cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs — exits 0, all tests green
2. grep -c "adoption-proof-matrix" accrue/test/accrue/docs/package_docs_verifier_test.exs — returns >= 3
3. bash scripts/ci/verify_package_docs.sh — exits 0 (against real repo, no regression)
</verification>

<success_criteria>
seed_tmp_dir! copies adoption-proof-matrix.md into every seeded tmp directory. The new negative test injects "Stripe-only" and asserts the verifier exits non-zero. All 8 tests in PackageDocsVerifierTest are green. The Stripe-only regression guard is now enforced by the test suite.
</success_criteria>

<output>
Create .planning/phases/174-a-design-system-gap-closure-token-completeness/174-06-SUMMARY.md when done.
</output>

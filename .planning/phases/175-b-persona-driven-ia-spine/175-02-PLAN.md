---
phase: 175
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/attention_counts.ex
  - accrue_admin/lib/accrue_admin/nav_badge_hook.ex
  - accrue_admin/lib/accrue_admin/nav.ex
  - accrue_admin/lib/accrue_admin/components/app_shell.ex
  - accrue_admin/lib/accrue_admin/router.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/assets/js/hooks/sidebar_collapse.js
  - accrue_admin/assets/js/app.js
autonomous: true
requirements:
  - IA-01
  - IA-02

must_haves:
  truths:
    - "AttentionCounts.compute/1 returns %{recovery: integer, developer: integer} using existing dashboard_live query shapes"
    - "NavBadgeHook assigns :nav_attention_counts to socket on_mount once per navigation"
    - "Nav.items/3 accepts optional attention_counts, adds :badge and :collapsible fields, uses /payments href"
    - "AppShell threads nav_attention_counts to Nav.items/3"
    - "Router @default_on_mount includes NavBadgeHook after AuthHook"
    - "copy.ex has 4 updated verb strings (exact strings from UI-SPEC §7)"
    - "sidebar_collapse.js hook registered in app.js hooks map"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/attention_counts.ex"
      provides: "Shared context fn compute/1 returning recovery + developer counts"
      exports: ["AccrueAdmin.AttentionCounts.compute/1"]
    - path: "accrue_admin/lib/accrue_admin/nav_badge_hook.ex"
      provides: "on_mount/4 hook that assigns nav_attention_counts from AttentionCounts"
    - path: "accrue_admin/assets/js/hooks/sidebar_collapse.js"
      provides: "SidebarCollapse JS hook with localStorage persistence + mount_path prefix"
  key_links:
    - from: "router.ex @default_on_mount"
      to: "AccrueAdmin.NavBadgeHook.on_mount/4"
      via: "live_session on_mount list"
      pattern: "NavBadgeHook"
    - from: "app_shell.ex"
      to: "Nav.items/3"
      via: "nav_attention_counts attr threaded to items/3 call"
      pattern: "Nav.items.*nav_attention_counts"
---

<objective>
Wave 1 (parallel with Plan 01): Build the attention-count data pipeline and update the nav/shell contracts so the sidebar can display badges and collapse state in Wave 2.

Creates:
- AccrueAdmin.AttentionCounts module (shared query fn, replaces inline dashboard_live counts)
- AccrueAdmin.NavBadgeHook (on_mount hook, registered in router after AuthHook)
- Nav.items/3 extended with :badge + :collapsible + /payments href
- AppShell threads nav_attention_counts attr
- sidebar_collapse.js JS hook file + registration in app.js
- copy.ex verb relabels (4 strings — exact per UI-SPEC §7)

Anti-churn justification for verb relabels: persona-job — launchers must name the job by verb (IA-01). Anti-churn for badges/collapse: persona-job — specialist rooms should light up only when work exists (IA-02).

Purpose: Data + JS hook contracts established before sidebar visuals in Wave 2.
Output: New module + hook + extended nav API + updated copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-CONTEXT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md
@.planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md
</context>

<interfaces>
<!-- dashboard_live.ex lines 234-270: dashboard_stats/0 — the query shapes to extract:
     past_due_subscription_count: Subscription |> Query.past_due() |> Repo.aggregate(:count, :id)
     blocked_webhook_count: WebhookEvent |> where([e], e.status in [:failed, :dead]) |> Repo.aggregate(:count, :id) -->
<!-- auth_hook.ex on_mount/4 contract (lines 1-34):
     def on_mount(:ensure_admin, params, session, socket) → {:cont, socket} | {:halt, redirect} -->
<!-- nav.ex items/2 current signature (lines 1-90):
     def items(mount_path, current_path) — returns list of %{label, href, icon, group}
     Payments leaf today: href: nav_href(mount_path, "/charges", org) (update to /payments) -->
<!-- app_shell.ex line 21:
     assigns = assign(assigns, :nav_items, Nav.items(assigns.mount_path, assigns.current_path))
     Add: attr(:nav_attention_counts, :map, default: %{}) and thread to Nav.items/3 -->
<!-- router.ex line 9:
     @default_on_mount [{AccrueAdmin.AuthHook, :ensure_admin}]
     Add NavBadgeHook: [{AccrueAdmin.AuthHook, :ensure_admin}, {AccrueAdmin.NavBadgeHook, :default}] -->
<!-- copy.ex launcher lines 698-712 (verified in RESEARCH.md §9):
     home_launcher_customers_title: "Find a customer" → "Look up a customer"
     home_launcher_invoices_title: "Work open invoices" → "Clear the invoice queue"
     home_launcher_recovery_title: "Recover failed payments" → "Recover at-risk revenue"
     home_launcher_developer_title: "Debug webhooks & events" → "Investigate an incident" -->
<!-- global_search.ex also has inline quick-link strings (not through Copy module) — update these too.
     Grep for: "Find a customer", "Work open invoices", "Recover failed payments", "Debug dead-letter webhooks" -->
<!-- SidebarCollapse JS hook — localStorage key prefixed with mount_path from data-mount-path attr.
     app_shell.ex line 24 has data-mount-path={@mount_path}. -->
<!-- command_palette.js hook lifecycle (mounted/destroyed pattern) is the JS analog. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttentionCounts module + NavBadgeHook + Nav/AppShell extension</name>
  <files>
    accrue_admin/lib/accrue_admin/attention_counts.ex,
    accrue_admin/lib/accrue_admin/nav_badge_hook.ex,
    accrue_admin/lib/accrue_admin/nav.ex,
    accrue_admin/lib/accrue_admin/components/app_shell.ex,
    accrue_admin/lib/accrue_admin/router.ex,
    accrue_admin/lib/accrue_admin/live/dashboard_live.ex
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/dashboard_live.ex (lines 234-270 — dashboard_stats/0 query shapes)
    accrue_admin/lib/accrue_admin/auth_hook.ex (full — on_mount/4 contract to replicate)
    accrue_admin/lib/accrue_admin/nav.ex (full — items/2 to extend to items/3)
    accrue_admin/lib/accrue_admin/components/app_shell.ex (full — line 21 Nav.items call)
    accrue_admin/lib/accrue_admin/router.ex (lines 1-20 — @default_on_mount)
    accrue_admin/test/accrue_admin/nav_test.exs (full — existing assertions to preserve + extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (attention_counts.ex + nav_badge_hook + nav.ex + app_shell.ex sections)
  </read_first>
  <behavior>
    - Test (nav_test.exs): Nav.items/3 with %{recovery: 2, developer: 0} returns a "Recovery" group item with badge: 2 and collapsible: true
    - Test (nav_test.exs): Nav.items/3 with %{recovery: 0, developer: 0} returns a "Recovery" group item with badge: nil
    - Test (nav_test.exs): Nav.items/2 (backward compat call) still returns all items without crash
    - Test (nav_test.exs): "Payments" leaf href contains "/payments" not "/charges"
    - Test (nav_test.exs): "Billing" group items have collapsible: false
  </behavior>
  <action>
    Create accrue_admin/lib/accrue_admin/attention_counts.ex as the AccrueAdmin.AttentionCounts module. Use @moduledoc false. Import Ecto.Query, alias Accrue.Billing.{Query, Subscription}, alias Accrue.Repo, alias Accrue.Webhook.WebhookEvent. Implement compute/1 accepting owner_scope (ignored for now, scoped queries are a future enhancement; use global counts matching dashboard_live.ex current behavior). Returns %{recovery: integer, developer: integer}.

    Update dashboard_live.ex dashboard_stats/0 to call AttentionCounts.compute(nil) for the past_due_subscription_count and blocked_webhook_count fields rather than inlining the queries. Keep the other fields in dashboard_stats/0 unchanged.

    Create accrue_admin/lib/accrue_admin/nav_badge_hook.ex as AccrueAdmin.NavBadgeHook. @moduledoc false. Import Phoenix.Component, only: [assign: 3]. Implement on_mount(:default, _params, _session, socket) — call AttentionCounts.compute(socket.assigns[:current_owner_scope]), assign :nav_attention_counts. Return {:cont, socket}. Wrap in rescue so DB errors return %{recovery: 0, developer: 0} (per Pitfall 3 in RESEARCH.md).

    In router.ex, update @default_on_mount to append {AccrueAdmin.NavBadgeHook, :default} after {AccrueAdmin.AuthHook, :ensure_admin}. The hook runs post-auth so socket.assigns.current_owner_scope is available.

    In nav.ex, change items/2 to items/3 with attention_counts defaulting to %{} (backward compat). Add :badge and :collapsible fields per PATTERNS.md §nav.ex shape. Update "Payments" href from /charges to /payments. Billing items: collapsible: false, badge: nil. Recovery: collapsible: true, badge computed from attention_counts.recovery. Developer (Webhooks leaf): collapsible: true, badge computed from attention_counts.developer. Catalog: collapsible: true, badge: nil. Connect/Home: collapsible: false, badge: nil.

    In app_shell.ex, add attr(:nav_attention_counts, :map, default: %{}) and thread it into the Nav.items/3 call on the assigns line. All callers without this attr still compile due to the default.

    Extend nav_test.exs with new assertions per the behavior block above.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/nav_test.exs --seed 0 -x</automated>
  </verify>
  <done>AttentionCounts module compiles. NavBadgeHook compiles. Nav.items/3 returns :badge and :collapsible on every item. Nav.items/2 still works (backward compat). /payments href correct. Router includes NavBadgeHook. AppShell threads the counts. Nav tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Copy verb relabels + sidebar_collapse JS hook</name>
  <files>
    accrue_admin/lib/accrue_admin/copy.ex,
    accrue_admin/lib/accrue_admin/components/global_search.ex,
    accrue_admin/assets/js/hooks/sidebar_collapse.js,
    accrue_admin/assets/js/app.js
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex (lines 695-715 — 4 launcher title functions)
    accrue_admin/lib/accrue_admin/components/global_search.ex (grep for "Find a customer", "Work open invoices", "Recover failed payments", "Debug dead-letter webhooks" — inline quick-link strings)
    accrue_admin/assets/js/hooks/command_palette.js (full — hook lifecycle to replicate)
    accrue_admin/assets/js/hooks/accrue_shell_nav.js (full — document-click + localStorage pattern)
    accrue_admin/assets/js/app.js (full — hooks registration)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (sidebar_collapse.js + app.js sections)
    .planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md (§7 Copywriting Contract — exact strings)
  </read_first>
  <action>
    In copy.ex, apply the 4 exact verb-relabel string changes per UI-SPEC §7 Copywriting Contract:
    home_launcher_customers_title: "Look up a customer"
    home_launcher_invoices_title: "Clear the invoice queue"
    home_launcher_recovery_title: "Recover at-risk revenue"
    home_launcher_developer_title: "Investigate an incident"
    No template changes required — dashboard_live.ex already calls these functions.

    In global_search.ex, find and update the 4 inline quick-link label strings (not routed through Copy module — they are inline in the quick-links list). Apply the same new verb strings to match. Also update the search placeholder text to "Search customers, invoices… ⌘K" if it differs.

    Create accrue_admin/assets/js/hooks/sidebar_collapse.js following the shape in PATTERNS.md §sidebar_collapse.js section. Key requirements:
    - mounted(): read localStorage using storageKey(); override server-rendered aria-expanded if stored value exists; attach click listener.
    - destroyed(): remove click listener.
    - handleClick(e): toggle only when e.target.closest("[data-collapse-toggle]") matches; write to localStorage.
    - setExpanded(expanded): update aria-expanded on this.el; toggle hidden attr on the element identified by this.el.dataset.controls.
    - storageKey(): prefix key with mount_path from closest [data-mount-path] ancestor (data-mount-path attr exists on ax-shell div per app_shell.ex line 24); key format: "ax-sidebar-{mountPath}-{group}" where group comes from this.el.dataset.group.

    In app.js, import SidebarCollapse from "./hooks/sidebar_collapse" and add it to the hooks map: { CommandPalette, SidebarCollapse }.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs --seed 0 -x 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <done>copy.ex has 4 new verb strings. global_search.ex quick-link labels updated. sidebar_collapse.js created with correct hook lifecycle. app.js registers SidebarCollapse. Dashboard test still passes (or correctly exercises new verb strings).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NavBadgeHook on_mount | Runs after AuthHook; socket.assigns.current_owner_scope is admin-scoped |
| AttentionCounts.compute/1 | DB query result only; no user input flows in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-02-01 | Denial of Service | AttentionCounts.compute/1 runs on every LiveView mount | accept | Two aggregate-count queries; sub-millisecond on indexed columns; no per-row scan; same pattern as existing dashboard_stats/0 |
| T-175-02-02 | Information Disclosure | nav_attention_counts exposed in socket assigns | accept | Values are aggregate counts (integers), no PII; admin session required to reach any LiveView |
| T-175-02-03 | Tampering | SidebarCollapse hook reads/writes localStorage | accept | Client-side UI state only; localStorage cannot affect server-side auth or data access; no server event fired from the hook |
| T-175-02-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- cd accrue_admin && mix test test/accrue_admin/nav_test.exs test/accrue_admin/live/dashboard_live_test.exs --seed 0
- Nav.items/3 returns :badge and :collapsible on all items (nav_test assertions)
- copy.ex verb strings match exactly: "Look up a customer", "Clear the invoice queue", "Recover at-risk revenue", "Investigate an incident"
- sidebar_collapse.js exists and app.js registers it
</verification>

<success_criteria>
Data pipeline for attention badges is established. Nav API extended with badge/collapsible fields. Verb relabels applied. JS hook file and registration ready for sidebar to consume in Wave 2.
</success_criteria>

<output>
Create `.planning/phases/175-b-persona-driven-ia-spine/175-02-SUMMARY.md` when done.
</output>

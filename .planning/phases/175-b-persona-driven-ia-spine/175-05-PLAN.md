---
phase: 175
plan: 05
type: execute
wave: 3
depends_on:
  - "175-03"
  - "175-04"
files_modified:
  - accrue_admin/lib/accrue_admin/live/event_live.ex
  - accrue_admin/lib/accrue_admin/live/webhook_live.ex
  - accrue_admin/lib/accrue_admin/router.ex
  - accrue_admin/test/accrue_admin/live/event_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
autonomous: true
requirements:
  - IA-04
  - IA-06

must_haves:
  truths:
    - "EventLive exists at /events/:id and renders the event type, actor, subject, and a Related billing card"
    - "EventLive Related card includes source webhook link (if caused_by_webhook_event_id present) and affected entity link"
    - "WebhookLive Related card renders event links pointing to /events/:id for each derived event"
    - "Webhook→Event→entity thread is fully navigable (Webhook detail → event detail → entity detail)"
    - "event_live_test.exs tests pass (remove :pending tags)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/event_live.ex"
      provides: "AccrueAdmin.Live.EventLive — detail LiveView for /events/:id"
      exports: ["mount/3", "render/1 or handle_params/3"]
    - path: "accrue_admin/lib/accrue_admin/live/webhook_live.ex"
      provides: "webhook_live Related card linking to /events/:id via related_items/4"
  key_links:
    - from: "webhook_live.ex derived_events timeline"
      to: "event_live.ex /events/:id route"
      via: "ScopedPath.build mount_path /events/:id"
      pattern: "events/#{event.id}"
    - from: "event_live.ex related_items/3"
      to: "webhooks/:id and subject entity path"
      via: "caused_by_webhook_event_id + subject_href/3"
      pattern: "webhooks/#{webhook_id}"
---

<objective>
Wave 3a (parallel with Plan 06): Close the Webhook→Event→entity threading gap.

Creates EventLive — a lightweight detail LiveView at /events/:id that:
- Loads the event from Accrue.Events (or Accrue.Webhook.WebhookEvent based on existing Event query shape)
- Renders event type, actor_type, subject_type, subject_id, inserted_at
- Renders a Related billing card with: source webhook link (if caused_by_webhook_event_id) and affected entity link (via subject_href logic from events_live.ex)

Extends WebhookLive to add a Related card linking to derived events via the new /events/:id route.

Anti-churn justification: persona-job — Developer incident path (Webhook→Event→entity) is the weakest thread (IA-04). Dead ends are eliminated.

Purpose: Complete the bidirectional Webhook→Event→entity thread. The /events/:id route was added to router.ex in Plan 03.
Output: New EventLive + extended WebhookLive Related card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-CONTEXT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md
@.planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md
</context>

<interfaces>
<!-- webhook_live.ex derived_events/1 (lines 310-315):
     from(event in Event, where: event.caused_by_webhook_event_id == ^webhook_id, order_by: ...) |> Repo.all()
     Event schema has: id, type, actor_type, subject_type, subject_id, caused_by_webhook_event_id, inserted_at

     webhook_live.ex assign_shell/2 (lines 551-561) — use same pattern in EventLive.
     webhook_live.ex mount/3 lines 28-60 — exact analog for EventLive mount (not_found redirect + assign pattern).

     events_live.ex subject_href/3 (lines 243-258) — copy to EventLive or call via a shared helper.
     NOTE: existing subject_href for Charge uses /charges/:id — update to /payments/:id in EventLive. -->

<!-- Queries.Events or Accrue.Events access: check existing events_live.ex to see how it loads events.
     events_live.ex uses DataTable which calls Queries.Events.list/1.
     For the detail, use Ecto directly or the Queries.Events.detail/2 function if it exists.
     If no detail/2 exists in Queries.Events, load via Repo.get(Event, id) with appropriate preloads. -->

<!-- RelatedResources component API (PATTERNS.md):
     attr :items, :list — list of %{icon: atom, label: string, href: string, value: string}
     Renders nothing when items == []. -->

<!-- Webhook detail related_items/4 pattern (PATTERNS.md §webhook_live.ex):
     Takes (webhook, derived_events, mount_path, scope).
     Maps up to 3 derived events to %{icon: :events, label: "Event", value: event.type, href: /events/:id}.
     The "view all events from this webhook" plain link already exists — Related card supplements it. -->

<!-- ScopedPath.build/3,4 for all hrefs. Never string concat. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EventLive (/events/:id detail LiveView)</name>
  <files>
    accrue_admin/lib/accrue_admin/live/event_live.ex,
    accrue_admin/test/accrue_admin/live/event_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/webhook_live.ex (full — mount/3 pattern lines 28-60, assign_shell/2 lines 551-561, not_found redirect pattern)
    accrue_admin/lib/accrue_admin/live/events_live.ex (full — subject_href/3 lines 243-258, Event struct fields, how events are queried)
    accrue_admin/lib/accrue_admin/queries/events.ex (check for detail/2 function; if missing, use Repo.get with preloads)
    accrue_admin/lib/accrue_admin/components/related_resources.ex (full — component API)
    accrue_admin/test/accrue_admin/live/webhook_live_test.exs (test setup pattern — how to build Event fixture)
    accrue_admin/test/accrue_admin/live/event_live_test.exs (existing scaffold from Plan 01 — remove :pending and implement)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (event_live.ex section — full module shape)
  </read_first>
  <behavior>
    - Test: GET /billing/events/:id with valid event ID renders event type and actor_type
    - Test: EventLive Related card contains a link to the source webhook when caused_by_webhook_event_id is present
    - Test: EventLive Related card contains a link to the affected entity (customer/subscription/invoice) via subject_href
    - Test: GET /billing/events/:id with unknown ID redirects back to /events with flash error
  </behavior>
  <action>
    Create accrue_admin/lib/accrue_admin/live/event_live.ex as AccrueAdmin.Live.EventLive.
    Use Phoenix.LiveView. @moduledoc false.

    mount/3 signature: %{"id" => event_id}, session, socket.
    - Load the event: try Queries.Events.detail/2 if it exists; otherwise use Repo.get + preload (check events.ex for available functions first).
    - On :not_found → redirect to /events with flash error.
    - On success → assign_shell/2 (copy pattern from webhook_live.ex lines 551-561, set page_title to "Event", current_path to /events/#{id}). Assign :event, :subject_href (compute via subject_href/3 from events_live.ex logic), :related_items.

    subject_href/3 function: copy the pattern from events_live.ex lines 243-258 but update the Charge/WebhookEvent case to use /payments/:id instead of /charges/:id.

    related_items/3: returns list with webhook item (if event.caused_by_webhook_event_id) + entity item (if subject_href not nil). Use :webhooks and :users/:subscriptions/:invoices/:payments icons respectively. Cap at 3 items.

    render/1: minimal detail layout following the webhook_live.ex shape. Show page_title, event.type, event.actor_type, event.subject_type, event.subject_id, event.inserted_at. Render RelatedResources.related_resources with items={@related_items}. Do not build a full activity timeline — Phase C/rubric uplift handles that. Keep it lean: event facts + Related card.

    In event_live_test.exs: remove @tag :pending from both stubs. Implement tests using the Event fixture that webhook_live_test.exs uses. Assert status 200, presence of event.type in HTML, presence of webhook link in Related card (when caused_by_webhook_event_id set).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/event_live_test.exs --seed 0 -x</automated>
    <automated>grep "/events/:id" accrue_admin/lib/accrue_admin/router.ex || (echo "/events/:id route missing — adding"; grep -n "live_session\|live(\"" accrue_admin/lib/accrue_admin/router.ex | tail -20)</automated>
  </verify>
  <done>EventLive compiles and mounts at /events/:id. Detail page renders event facts and Related card. Not-found case redirects. /events/:id route confirmed in router.ex (add  inside live_session block if absent — Plan 03 should have added it; if not, add it here). Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebhookLive Related card linking to /events/:id</name>
  <files>
    accrue_admin/lib/accrue_admin/live/webhook_live.ex,
    accrue_admin/test/accrue_admin/live/webhook_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/webhook_live.ex (full — derived_events/1 lines 310-315, existing render structure, what imports are already present)
    accrue_admin/lib/accrue_admin/components/related_resources.ex (full — component API)
    accrue_admin/test/accrue_admin/live/webhook_live_test.exs (full — existing test setup to extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (webhook_live.ex section — related_items/4 shape)
  </read_first>
  <behavior>
    - Test: WebhookLive render includes a Related billing card section
    - Test: Related card contains a link to /events/:id for each derived event (up to 3)
    - Test: Existing webhook_live tests still pass (no regression on existing assigns)
  </behavior>
  <action>
    In webhook_live.ex:

    1. Add RelatedResources to imports/aliases if not already present (alias AccrueAdmin.Components.RelatedResources).

    2. Add ScopedPath alias if not present.

    3. Add a private related_items/4 function: takes (webhook, derived_events, mount_path, scope). Maps Enum.take(derived_events, 3) to %{icon: :events, label: "Event", value: event.type, href: ScopedPath.build(mount_path, "/events/#{event.id}", scope)}. Returns the list.

    4. In the mount/3 success branch (or handle_params if used), add an assign for :related_items calling related_items(@webhook, @derived_events, @admin_mount_path, @current_owner_scope). If derived_events is fetched separately, wire the 4-arg call where both webhook and derived_events are available.

    5. In the render, add <RelatedResources.related_resources items={@related_items} /> alongside the existing derived events Timeline section.

    In webhook_live_test.exs, add assertions:
    - related_resources renders when derived events exist
    - Each event link points to the /events/:id path
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs --seed 0 -x</automated>
  </verify>
  <done>WebhookLive renders a Related card with event links. /events/:id links are correct. Existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /events/:id route | :id path param comes from user browser or bookmark — must load event within owner scope |
| EventLive related links | hrefs constructed from server-side data, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-05-01 | Elevation of Privilege | EventLive /events/:id bypasses auth | mitigate | Route is inside live_session :accrue_admin block which runs AuthHook.ensure_admin on_mount; no auth bypass possible |
| T-175-05-02 | Information Disclosure | IDOR — loading another org's event by ID | mitigate | EventLive mount loads event using owner_scope from socket assigns (set by AuthHook); detail query must scope by owner. If Queries.Events.detail/2 does not scope, add scope_query/2 by analogy with other query modules. Verify in implementation. |
| T-175-05-03 | Tampering | EventLive Related card href injection | accept | All hrefs constructed via ScopedPath.build from server-side event fields (no user-controlled strings in the path beyond the :id used to load the event) |
| T-175-05-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- cd accrue_admin && mix test test/accrue_admin/live/event_live_test.exs test/accrue_admin/live/webhook_live_test.exs --seed 0
- EventLive mounts at /events/:id (route added in Plan 03)
- WebhookLive Related card has event links pointing to /events/:id
- EventLive Related card has source webhook link + entity link
- No dead ends in the Webhook→Event→entity chain
</verification>

<success_criteria>
A Developer persona can navigate: Webhook detail → click an event in the Related card → EventLive at /events/:id → click the affected entity link. Thread is fully bidirectional with no dead ends.
</success_criteria>

<output>
Create `.planning/phases/175-b-persona-driven-ia-spine/175-05-SUMMARY.md` when done.
</output>

---
phase: 175
plan: 06
type: execute
wave: 3
depends_on:
  - "175-03"
  - "175-04"
files_modified:
  - accrue_admin/lib/accrue_admin/live/customer_live.ex
  - accrue_admin/lib/accrue_admin/live/events_live.ex
  - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  - accrue_admin/test/accrue_admin/live/events_live_test.exs
autonomous: true
requirements:
  - IA-05
  - IA-07

must_haves:
  truths:
    - "Customer-360 primary tabs are Subscriptions, Invoices, Payments (not 7 flat tabs)"
    - "Customer-360 'More' button renders with aria-haspopup=menu and aria-expanded"
    - "Clicking 'More' reveals Payment methods, Entitlements, Events, Metadata tab links"
    - "Charges tab internal ID and href updated to charges/payments tab, label reads 'Payments'"
    - "handle_params resets more_tabs_open: false on tab navigation"
    - "EventsLive renders a FilterChipBar with a 'By actor' chip that is always visible"
    - "'By actor' chip activates actor filter (URL-param synced) and clears it via remove_href"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/customer_live.ex"
      provides: "@primary_tabs/@more_tabs split, more_tabs_open assign, toggle_more_tabs event handler, tab strip with More ▾ dropdown"
    - path: "accrue_admin/lib/accrue_admin/live/events_live.ex"
      provides: "Compliance 'By actor' FilterChipBar chip always rendered"
  key_links:
    - from: "customer_live.ex handle_event toggle_more_tabs"
      to: "socket.assigns.more_tabs_open"
      via: "assign(:more_tabs_open, !socket.assigns.more_tabs_open)"
      pattern: "more_tabs_open"
    - from: "events_live.ex render FilterChipBar"
      to: "URL ?actor_type= param"
      via: "compliance_chips/2 remove_href pointing to table_path (clear) or table_path?actor_type=admin (activate)"
      pattern: "actor_type"
---

<objective>
Wave 3b (parallel with Plan 05): Customer-360 tab tiering + compliance actor-lens.

Customer-360 gets:
- @primary_tabs = ["subscriptions", "invoices", "charges"] (displayed as Subscriptions/Invoices/Payments)
- @more_tabs = ["payment_methods", "entitlements", "events", "metadata"]
- more_tabs_open boolean assign toggled by handle_event("toggle_more_tabs")
- handle_params resets more_tabs_open: false
- "More ▾" button with aria-haspopup="menu" + aria-expanded
- ax-tab-more-menu dropdown (CSS class defined in Plan 03) with recessed tab links

Events list gets:
- Always-visible "By actor" chip in FilterChipBar as a saved compliance lens
- Activates actor_type filter (URL-param synced via existing DataTable mechanism)
- Chip shows cobalt tone when active, slate when inactive

Anti-churn justifications: Customer-360 (rubric ② visual hierarchy + persona-job for Support, IA-05); Compliance lens (persona-job — Compliance persona "who did what when" without nav group, IA-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-CONTEXT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md
@.planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md
@.planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md
</context>

<interfaces>
<!-- customer_live.ex current @tabs (line 34):
     @tabs ~w(subscriptions invoices charges payment_methods entitlements events metadata)
     
     New constants:
     @primary_tabs ~w(subscriptions invoices charges)
     @more_tabs ~w(payment_methods entitlements events metadata)
     Keep @tabs for normalize_tab guard (it guards tab in @tabs in handle_params).
     
     normalize_tab/1 backward compat: add defp normalize_tab("payments"), do: "charges" if helpful.
     
     handle_params/3 (lines 68-79): reset more_tabs_open: false + assign :tab, :params, :more_tabs_open.
     
     Tab display label for "charges": render as "Payments" (label only; internal id stays "charges" to avoid
     breaking deep-link URLs with ?tab=charges — add normalize_tab("payments") → "charges" as a compat clause
     per RESEARCH.md Pitfall 5).
     
     charges tab href: update from /charges/:id to /payments (tabs navigate to the customer's sub-list,
     not a specific charge). In customer_live.ex related_items/3 (line 486), update /charges href to /payments. -->

<!-- events_live.ex filter_fields (lines 113-119):
     actor_type filter_field already exists.
     
     compliance_chips/2: when actor_type param is active (not nil/empty), chip is cobalt + remove_href = table_path.
     When actor_type param is nil/empty, chip is slate.
     
     Footgun from RESEARCH.md §10: when inactive, the chip has no remove_href and therefore renders no Clear link.
     The chip must also provide an activation href to apply the actor filter.
     Options: (a) extend filter_chip_bar to support an href attr for inactive chips, or
              (b) render the inactive chip as a link-styled chip (not using remove_href mechanism).
     
     Recommended: extend filter_chip_bar to accept optional :href attr on chips; when :href is set and the
     chip has no remove_href, render the chip label as an <a href={chip.href}> instead of a plain <span>.
     This is a minimal, backward-compatible extension to filter_chip_bar.ex. -->

<!-- tabs.ex (29 lines, simple functional component): extend OR inline the More ▾ variant in customer_live.
     PATTERNS.md recommends inlining the tab strip in customer_live (Tabs component is simple enough).
     Do NOT modify Tabs component's existing public API. -->

<!-- ax-tab-more-menu CSS: defined in Plan 03 (Wave 2). By Wave 3, this class exists in the committed bundle. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Customer-360 primary tabs + "More ▾" overflow</name>
  <files>
    accrue_admin/lib/accrue_admin/live/customer_live.ex,
    accrue_admin/test/accrue_admin/live/customer_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/customer_live.ex (full — @tabs, mount, handle_params, tabs/4 fn, render, related_items/3)
    accrue_admin/lib/accrue_admin/components/tabs.ex (full — existing component to understand if inline or extend)
    accrue_admin/test/accrue_admin/live/customer_live_test.exs (full — existing tests to preserve + extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (customer_live.ex section — @primary_tabs/@more_tabs, handle_event, tab tiering HEEx sketch)
    .planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md (§4 Customer-360 primary tabs + "More ▾", Copywriting)
  </read_first>
  <behavior>
    - Test: customer_live render contains 3 primary tab links (Subscriptions, Invoices, Payments)
    - Test: customer_live render contains a "More" button with aria-haspopup="menu"
    - Test: phx-click toggle_more_tabs on the More button renders the 4 recessed tab links
    - Test: handle_params resets more_tabs_open to false
    - Test: when ?tab=charges is in params, customer is on the "Payments" tab (normalize_tab compat)
    - Test: related_items uses /payments href not /charges
  </behavior>
  <action>
    In customer_live.ex:

    1. Add module attributes:
       @primary_tabs ~w(subscriptions invoices charges)
       @more_tabs ~w(payment_methods entitlements events metadata)

    2. In mount/3 (or via handle_params), add assign(:more_tabs_open, false).

    3. Add handle_event("toggle_more_tabs", _params, socket): assign(:more_tabs_open, !socket.assigns.more_tabs_open).

    4. Add handle_event("close_more_tabs", _params, socket): assign(:more_tabs_open, false).

    5. Optionally add phx-window-keydown="close_more_tabs" phx-key="Escape" on the More ▾ wrapper div for Esc key close.

    6. In handle_params/3, add |> assign(:more_tabs_open, false) alongside the existing :tab and :params assigns.

    7. Add normalize_tab("payments"), do: "charges" as a compat clause in normalize_tab/1.

    8. In the render, replace the existing <Tabs.tabs ...> call with an inline tab strip:
       - Primary tabs via a :for loop over primary_tab_list/4 (private fn building tab structs from @primary_tabs).
       - "More ▾" button: type="button", class={["ax-tab ax-tab-more-trigger", @tab in @more_tabs && "ax-tab-active"]}, aria-haspopup="menu", aria-expanded={to_string(@more_tabs_open)}, phx-click="toggle_more_tabs".
       - Dropdown :if={@more_tabs_open}: <ul class="ax-tab-more-menu" role="menu">, each tab as <li role="none"><a role="menuitem"...>.

    9. Tab label for "charges" in primary_tab_list/4: render as "Payments" (string display label).

    10. In related_items/3, update the charges/payments link to use ScopedPath.build(mount_path, "/payments", scope, %{"customer_id" => customer.id}).

    11. In customer_live_test.exs, add/update assertions per behavior block. Preserve existing tab navigation tests.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs --seed 0 -x</automated>
  </verify>
  <done>Customer-360 shows 3 primary tabs + More button. More dropdown opens on click and closes on Esc/nav. Payments tab label correct. related_items uses /payments. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compliance actor-lens chip on EventsLive</name>
  <files>
    accrue_admin/lib/accrue_admin/live/events_live.ex,
    accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex,
    accrue_admin/test/accrue_admin/live/events_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/events_live.ex (full — handle_params, filter_fields, render structure, table_path)
    accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex (full — chip API; inspect how chips render when active: true but no remove_href)
    accrue_admin/test/accrue_admin/live/events_live_test.exs (full — existing tests to preserve + extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (events_live.ex section — compliance_chips/2 + footgun note)
    .planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md (§10 Compliance Actor-Lens Chip — footgun on chip API)
    .planning/phases/175-b-persona-driven-ia-spine/175-UI-SPEC.md (§8 Compliance actor-lens chip)
  </read_first>
  <behavior>
    - Test: events_live render always contains a "By actor" chip element (visible even when no filter active)
    - Test: when ?actor_type=admin in params, "By actor" chip has cobalt tone and a remove_href
    - Test: when ?actor_type= is absent, "By actor" chip has slate tone and an activation href
    - Test: existing events_live tests still pass
  </behavior>
  <action>
    In filter_chip_bar.ex, inspect the current rendering logic. If chips only render a label/value and have no :href attr for activation, add optional :href support: when a chip has href: set and active: true but no remove_href, render the chip label as an <a href={chip.href}> link (the "activate this lens" behavior). This is a minimal, backward-compatible extension (new optional key, existing chips without :href are unaffected).

    In events_live.ex, add a private compliance_chips/2 function taking (params, table_path):
    - actor_active = Map.get(params, "actor_type") not in [nil, ""]
    - Returns [%{id: :by_actor, label: "By actor", tone: (if actor_active, do: :cobalt, else: :slate), active: true, remove_href: (if actor_active, do: table_path, else: nil), href: (if not actor_active, do: table_path <> "?actor_type=admin", else: nil)}]
    The chip always renders (active: true). When active it has remove_href to clear. When inactive it has href to activate with a default actor_type of "admin" (the primary compliance use case).

    In the events_live.ex render, add <FilterChipBar.filter_chip_bar items={compliance_chips(@params, @table_path)} label="Quick filters" /> above the DataTable component.

    In events_live_test.exs, add assertions per the behavior block.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/events_live_test.exs --seed 0 -x</automated>
  </verify>
  <done>Events list always shows "By actor" chip. Chip activates/deactivates actor filter via URL param. filter_chip_bar extended with :href support. Tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Customer-360 ?tab= param | User-controlled tab ID flows into normalize_tab → guard check |
| Events ?actor_type= param | User-controlled actor_type flows into DataTable filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-06-01 | Tampering | ?tab= param in customer_live → normalize_tab | mitigate | normalize_tab/1 pattern-matches on known tab strings only; unknown values default to "subscriptions" (the fallback clause) — no arbitrary atom creation |
| T-175-06-02 | Information Disclosure | IDOR via ?actor_type= filter exposing cross-org events | accept | scope_query in Queries.Events applies owner_scope before actor_type filter; multi-org isolation unchanged |
| T-175-06-03 | Tampering | More ▾ menu open state stored in server assign | accept | more_tabs_open is a boolean UI state; no data is exposed by opening the menu; no auth or data scope affected |
| T-175-06-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- cd accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs test/accrue_admin/live/events_live_test.exs --seed 0
- Customer-360 has 3 primary tabs + More ▾ button (aria-haspopup="menu" present)
- Events list has "By actor" chip always rendered
- Filter chip activation/deactivation URL-param behavior verified
</verification>

<success_criteria>
Support persona sees primary tabs on Customer-360 without distraction. Advanced tabs are one click away. Compliance persona can apply actor filter via a visible chip without nav changes. Tests green.
</success_criteria>

<output>
Create `.planning/phases/175-b-persona-driven-ia-spine/175-06-SUMMARY.md` when done.
</output>

---
phase: 175
plan: 07
type: execute
wave: 4
depends_on:
  - "175-05"
  - "175-06"
files_modified:
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/lib/accrue_admin/live/coupon_live.ex
  - accrue_admin/lib/accrue_admin/live/promotion_code_live.ex
  - accrue_admin/lib/accrue_admin/live/connect_account_live.ex
  - accrue_admin/lib/accrue_admin/live/invoice_live.ex
  - accrue_admin/lib/accrue_admin/live/charge_live.ex
  - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
  - accrue_admin/test/accrue_admin/live/coupon_live_test.exs
  - accrue_admin/test/accrue_admin/live/promotion_code_live_test.exs
  - accrue_admin/test/accrue_admin/live/connect_account_live_test.exs
autonomous: true
requirements:
  - IA-04

must_haves:
  truths:
    - "All 8 detail screens render a non-empty RelatedResources card where relationships exist"
    - "subscription_live Related card has at minimum a customer link and an events link"
    - "coupon_live Related card has at minimum a promotions/events link"
    - "promotion_code_live Related card has at minimum a coupon link and an events link"
    - "connect_account_live Related card has at minimum a charges/events link"
    - "invoice_live and charge_live /charges hrefs updated to /payments"
    - "No detail screen is a dead end for a persona navigating from a list"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      provides: "Related billing card via RelatedResources with customer + invoice + events links"
    - path: "accrue_admin/lib/accrue_admin/live/coupon_live.ex"
      provides: "Related billing card with promotion codes + events links"
    - path: "accrue_admin/lib/accrue_admin/live/promotion_code_live.ex"
      provides: "Related billing card with source coupon + events links"
    - path: "accrue_admin/lib/accrue_admin/live/connect_account_live.ex"
      provides: "Related billing card with events link"
  key_links:
    - from: "All 8 detail screens"
      to: "related_resources.ex component"
      via: "RelatedResources.related_resources items={related_items(...)}"
      pattern: "related_items"
---

<objective>
Wave 4: Close the bidirectional Related card gap on the 4 detail screens that currently have no RelatedResources.

Audit (from codebase inspection):
- subscription_live.ex — NO Related card (has inline breadcrumb links but no RelatedResources component)
- coupon_live.ex — NO Related card
- promotion_code_live.ex — NO Related card
- connect_account_live.ex — NO Related card
- invoice_live.ex — HAS Related card, but /charges hrefs need updating to /payments
- charge_live.ex — HAS Related card, already present

This plan adds RelatedResources to the 4 missing screens, and fixes the /charges → /payments href in invoice_live.ex and charge_live.ex.

Anti-churn justification: persona-job — no detail page dead ends; Developer incident path and Finance audit path both need entity-to-entity navigation (IA-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-CONTEXT.md
@.planning/phases/175-b-persona-driven-ia-spine/175-RESEARCH.md
@.planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md
</context>

<interfaces>
<!-- RelatedResources component API (PATTERNS.md):
     alias AccrueAdmin.Components.RelatedResources (add to each file's alias/import block)
     <RelatedResources.related_resources items={related_items(...)} />
     Each item: %{icon: atom, label: string, href: string, value: string (optional)}
     Renders nothing when items == []. Icon vocabulary: :users, :invoices, :subscriptions,
     :payments, :webhooks, :events, :coupons, :promotions, :connect. -->

<!-- ScopedPath.build/3,4 — use for all hrefs. Never string concat. -->

<!-- Related card items by screen:
     subscription_live:
       - Customer: icon: :users, label: "Customer", value: customer.name, href: /customers/:id
       - Invoices for sub: icon: :invoices, label: "Invoices", href: /invoices?subscription_id=:id
       - Events for sub: icon: :events, label: "Events", href: /events?subject_type=Subscription&subject_id=:id
     
     coupon_live:
       - Promotion codes: icon: :promotions, label: "Promotion codes", href: /promotion-codes?coupon_id=:id (if such a filter exists — check; fallback: /promotion-codes)
       - Events for coupon: icon: :events, label: "Events", href: /events?subject_type=Coupon&subject_id=:id
     
     promotion_code_live:
       - Source coupon: icon: :coupons, label: "Coupon", value: promotion_code.coupon.id, href: /coupons/:coupon_id
       - Events: icon: :events, label: "Events", href: /events?subject_type=PromotionCode&subject_id=:id
     
     connect_account_live:
       - Events: icon: :events, label: "Events", href: /events?subject_type=ConnectAccount&subject_id=:id
       (ConnectAccount may use a different Accrue schema name — check connect_account_live.ex for subject_type string)
     
     invoice_live (fix only):
       - Change /charges hrefs to /payments in related_items/3 (line 655 — "Charges for this customer")
     
     charge_live (fix only):
       - Review related_items/3 and update any /charges hrefs to /payments -->

<!-- subscription_live.ex currently renders inline breadcrumb-style links (e.g. lines 132-140 show
     href: .../customers/:id etc.). These are breadcrumbs/overview links, NOT RelatedResources.
     Add RelatedResources as an additional card in the detail section alongside the existing content. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Related cards on subscription_live and coupon_live + invoice/charge href fixes</name>
  <files>
    accrue_admin/lib/accrue_admin/live/subscription_live.ex,
    accrue_admin/lib/accrue_admin/live/coupon_live.ex,
    accrue_admin/lib/accrue_admin/live/invoice_live.ex,
    accrue_admin/lib/accrue_admin/live/charge_live.ex,
    accrue_admin/test/accrue_admin/live/subscription_live_test.exs,
    accrue_admin/test/accrue_admin/live/coupon_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/subscription_live.ex (full — render structure, mount, existing assigns)
    accrue_admin/lib/accrue_admin/live/coupon_live.ex (full — render structure, what entity fields are available)
    accrue_admin/lib/accrue_admin/live/invoice_live.ex (lines 640-674 — existing related_items/3 to fix /charges hrefs)
    accrue_admin/lib/accrue_admin/live/charge_live.ex (lines 550-590 — existing related_items/4 to fix /charges hrefs)
    accrue_admin/lib/accrue_admin/components/related_resources.ex (full — component API)
    accrue_admin/test/accrue_admin/live/subscription_live_test.exs (existing tests to extend)
    accrue_admin/test/accrue_admin/live/coupon_live_test.exs (existing tests to extend)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (RelatedResources API section, ScopedPath usage)
  </read_first>
  <behavior>
    - Test (subscription): subscription detail render includes a related-resources element with customer link
    - Test (subscription): related items include /invoices?subscription_id and /events?subject_type=Subscription
    - Test (coupon): coupon detail render includes a related-resources element with events link
    - Test (invoice): related_items no longer contains /charges href (uses /payments instead)
    - Test (charge): charge related_items use /payments paths for customer charges link
  </behavior>
  <action>
    In subscription_live.ex:
    1. Add RelatedResources alias.
    2. Add ScopedPath alias if not present (check existing imports — likely already there from existing inline links).
    3. Add private related_items/3 function. Arguments: (subscription, mount_path, scope). Items:
       - Customer: icon: :users, label: "Customer", value: customer.name or customer.email, href: ScopedPath.build(mount_path, "/customers/#{subscription.customer_id}", scope). Only include if customer is available.
       - Invoices: icon: :invoices, label: "Invoices", href: ScopedPath.build(mount_path, "/invoices", scope, %{"subscription_id" => subscription.id}).
       - Events: icon: :events, label: "Events", href: ScopedPath.build(mount_path, "/events", scope, %{"subject_type" => "Subscription", "subject_id" => subscription.id}).
    4. In mount/3 success branch, add assign(:related_items, related_items(subscription, mount_path, scope)).
    5. In render, add <RelatedResources.related_resources items={@related_items} /> in the detail content section.

    In coupon_live.ex:
    1. Read the full file to understand entity shape. Add RelatedResources + ScopedPath aliases.
    2. Add private related_items/3. Items:
       - Promotion codes: icon: :promotions, label: "Promotion codes", href: ScopedPath.build(mount_path, "/promotion-codes", scope) — filter by coupon_id if the query supports it; otherwise bare list.
       - Events: icon: :events, label: "Events", href: ScopedPath.build(mount_path, "/events", scope, %{"subject_type" => "Coupon", "subject_id" => coupon.id}).
    3. Wire assign + render.

    In invoice_live.ex, update related_items/3 (line ~655): change the "Charges for this customer" href from ScopedPath.build(mount_path, "/charges", scope, ...) to ScopedPath.build(mount_path, "/payments", scope, ...). Update the label if appropriate ("Payments for this customer").

    In charge_live.ex, review related_items/4 and update any /charges references to /payments (customer charges links, etc.).

    Add test assertions per behavior block using existing test fixture setup patterns.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/coupon_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs --seed 0 -x</automated>
  </verify>
  <done>Subscription and coupon detail screens render RelatedResources cards. invoice_live and charge_live use /payments hrefs. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Related cards on promotion_code_live and connect_account_live + final suite gate</name>
  <files>
    accrue_admin/lib/accrue_admin/live/promotion_code_live.ex,
    accrue_admin/lib/accrue_admin/live/connect_account_live.ex,
    accrue_admin/test/accrue_admin/live/promotion_code_live_test.exs,
    accrue_admin/test/accrue_admin/live/connect_account_live_test.exs
  </files>
  <read_first>
    accrue_admin/lib/accrue_admin/live/promotion_code_live.ex (full — entity shape, available assigns)
    accrue_admin/lib/accrue_admin/live/connect_account_live.ex (full — entity shape, available assigns)
    accrue_admin/lib/accrue_admin/components/related_resources.ex (full — API)
    accrue_admin/test/accrue_admin/live/promotion_code_live_test.exs (full — existing tests)
    accrue_admin/test/accrue_admin/live/connect_account_live_test.exs (full — existing tests)
    .planning/phases/175-b-persona-driven-ia-spine/175-PATTERNS.md (RelatedResources API + ScopedPath)
  </read_first>
  <behavior>
    - Test (promo_code): promotion_code detail render includes a related-resources element with coupon link
    - Test (promo_code): related items include source coupon href /coupons/:coupon_id
    - Test (connect_account): connect_account detail render includes a related-resources element with events link
    - Test: Full accrue_admin suite passes (final gate)
  </behavior>
  <action>
    In promotion_code_live.ex:
    1. Read full file to understand entity shape. PromotionCode likely has a coupon_id or preloaded coupon.
    2. Add RelatedResources + ScopedPath aliases.
    3. Add private related_items/3. Items:
       - Source coupon: icon: :coupons, label: "Coupon", value: promotion_code.coupon_id (or coupon.name if preloaded), href: ScopedPath.build(mount_path, "/coupons/#{promotion_code.coupon_id}", scope). Only include if coupon_id present.
       - Events: icon: :events, label: "Events", href: ScopedPath.build(mount_path, "/events", scope, %{"subject_type" => "PromotionCode", "subject_id" => promotion_code.id}).
    4. Wire assign + render.

    In connect_account_live.ex:
    1. Read full file. Connect accounts may have limited relationships — at minimum events.
    2. Add RelatedResources + ScopedPath aliases.
    3. Add private related_items/3. Items:
       - Events: icon: :events, label: "Events", href: ScopedPath.build(mount_path, "/events", scope, %{"subject_type" => "ConnectAccount", "subject_id" => account.id}). NOTE: check what Accrue.Webhook.Event.subject_type field uses for ConnectAccount — may be "Connect" or "ConnectAccount". Use the actual string value from the Event schema or events_live.ex subject_href pattern.
       - Charges (if the connect account has a processor_id that maps to charges): icon: :payments, label: "Payments", href: ScopedPath.build(mount_path, "/payments", scope). Only include if meaningful.
    4. Wire assign + render.

    Add test assertions per behavior block.

    After both tasks complete, run the full suite as the final phase gate:
    `cd accrue_admin && mix test --seed 0`
    Confirm all tests pass before marking the phase complete.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test --seed 0 2>&1 | tail -10</automated>
  </verify>
  <done>All 8 detail screens have non-empty Related cards where relationships exist. Full accrue_admin suite passes. No detail screen is a dead end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Related card hrefs | Constructed from server-side entity IDs loaded within owner scope |
| Events filter params in hrefs | subject_type and subject_id are server-derived constants and entity IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-07-01 | Information Disclosure | Related card links to events filtered by entity ID | accept | Event list is already scoped by owner_scope via DataTable + Queries.Events; entity IDs in hrefs are loaded from the server-side entity (already authorized) |
| T-175-07-02 | Tampering | subject_type in events filter param | accept | subject_type values are hardcoded strings in related_items functions — not user-controlled input; no dynamic string construction from URL params |
| T-175-07-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- cd accrue_admin && mix test --seed 0 (full suite gate — all plans)
- grep -rn "related_resources" accrue_admin/lib/accrue_admin/live/ | wc -l → should be 8+ (one per detail screen)
- grep -rn "/charges" accrue_admin/lib/accrue_admin/live/ | grep -v redirect | grep -v "router.ex" → should return 0 (all updated to /payments)
</verification>

<success_criteria>
All 8 detail screens render a Related billing card. No dead ends in entity navigation. Full accrue_admin test suite passes. All 7 IA requirement IDs delivered across Phase 175.
</success_criteria>

<output>
Create `.planning/phases/175-b-persona-driven-ia-spine/175-07-SUMMARY.md` when done.
</output>

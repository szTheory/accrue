---
phase: 176-c-systematic-per-screen-rubric-uplift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/assets/app.css
  - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04

must_haves:
  truths:
    - "176-SCORECARD.md exists in the phase dir with before-scores for every admin screen × 10 rubric dimensions"
    - "app.css data-table collapse breakpoint fires at --ax-bp-md (768px) not --ax-bp-lg (1024px)"
    - "The grep-able token comment on the changed @media reads '/* --ax-bp-md ↑ */'"
    - "priv/static asset bundle is rebuilt and committed alongside the CSS change"
    - "600/640 reconciliation rationale is documented in SCORECARD.md header"
    - "mix test --seed 0 stays at 227 green after the CSS change"
  artifacts:
    - path: ".planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md"
      provides: "Baseline before-scores per screen × 10 dimensions — the worst-first input for all subsequent waves"
      contains: "| screen | ① | ② | ③ | ④ | ⑤ | ⑥ | ⑦ | ⑧ | ⑨ | ⑩ |"
    - path: "accrue_admin/assets/css/app.css"
      provides: "data-table breakpoint at --ax-bp-md"
      contains: "min-width: 768px) { /* --ax-bp-md ↑ */"
  key_links:
    - from: "app.css line ~1361"
      to: ".ax-data-table-shell / .ax-data-table-cards visibility rules"
      via: "@media (min-width: 768px) { /* --ax-bp-md ↑ */"
      pattern: "min-width: 768px.*--ax-bp-md"
---

<objective>
Wave 0 — the dependency root for all uplift waves. This plan does two things: (1) captures the per-screen × 10-dimension baseline SCORECARD.md by code-level audit of every admin LiveView, and (2) applies the single most-leveraged CSS fix in the phase — moving the data-table table↔card breakpoint from --ax-bp-lg (1024px) to --ax-bp-md (768px), so tablets see tables and phones see cards.

Purpose: Every subsequent wave task uses the SCORECARD before-score as its anti-churn justification token (per anti-churn rule). Without the baseline, no wave task has permission to change anything. The CSS fix simultaneously lifts rubric dimension ⑤ for all 9 list screens.

Output: 176-SCORECARD.md with before-scores, updated app.css, rebuilt priv/static bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-CONTEXT.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-RESEARCH.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-UI-SPEC.md
@.planning/ADMIN-UX-BASELINE-AUDIT.md

<interfaces>
<!-- 10-dimension rubric (score 0–3; pass ≥ 2): -->
<!-- ① token compliance   ② visual hierarchy  ③ spacing rhythm -->
<!-- ④ state coverage     ⑤ responsive/mobile ⑥ contrast       -->
<!-- ⑦ focus & semantics  ⑧ brand expression  ⑨ motion(record only) ⑩ reuse/DRY -->

<!-- Scoring signals per dimension (from RESEARCH.md §Baseline Scoring Recipe): -->
<!--   ① grep style= / literal hex in live file (excl. accent_hex lines) -->
<!--   ② look for ax-eyebrow→ax-heading→ax-body hierarchy; ax-display on hero numbers -->
<!--   ③ no literal px/rem in spacing; uses --ax-space-* tokens -->
<!--   ④ empty/loading/error/populated branches present in markup -->
<!--   ⑤ ax-grid-2/3 used (stacks); card_fields+card_title wired; breakpoint correct -->
<!--   ⑥ -readable variants on tinted surfaces; status has text label not color-only -->
<!--   ⑦ aria-*/scope=/role= present; dl/dt/dd for field lists; th scope="col" in tables -->
<!--   ⑧ ax-display on hero numbers; Geist tabular figures (global); no finance clichés -->
<!--   ⑨ record existing state only — do NOT change (Phase 177 scope) -->
<!--   ⑩ Detail.summary_card/detail_section/detail_field_list used; no hand-rolled cards -->

<!-- Screens to score (from router.ex): -->
<!-- DashboardLive, CustomersLive, CustomerLive, SubscriptionsLive, SubscriptionLive, -->
<!-- InvoicesLive, InvoiceLive, ChargesLive, ChargeLive, CouponsLive, CouponLive, -->
<!-- PromotionCodesLive, PromotionCodeLive, ConnectAccountsLive, ConnectAccountLive, -->
<!-- EventsLive, EventLive, WebhooksLive, WebhookLive, RecoveryLive, CampaignLive -->

<!-- app.css line ~1361 (CHANGE THIS): -->
<!-- @media (min-width: 1024px) { /* --ax-bp-lg ↑ */ -->
<!--   .ax-data-table-shell { display: block; } -->
<!--   .ax-data-table-cards { display: none; } -->
<!-- } -->
<!-- MUST BECOME: -->
<!-- @media (min-width: 768px) { /* --ax-bp-md ↑ */ -->

<!-- Token-bypass exclusion: accent_hex / accent_contrast_hex in *_live.ex are -->
<!-- branding-config seed data, NOT violations. Do not flag them. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 176-SCORECARD.md baseline (code-level audit, all 21 screens)</name>
  <files>
    .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/*.ex — read each live file to audit (one pass per file)
    - accrue_admin/lib/accrue_admin/live/analytics/ — RecoveryLive and CampaignLive
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-RESEARCH.md §Baseline Scoring Recipe — the per-dimension code-level audit signal
    - .planning/ADMIN-UX-BASELINE-AUDIT.md — prior baseline for reference
  </read_first>
  <action>
    Read every LiveView file in accrue_admin/lib/accrue_admin/live/ (including analytics/) and score each screen × 10 rubric dimensions using the code-level audit signals from RESEARCH.md. Produce 176-SCORECARD.md with:

    1. Header section documenting the 600/640 breakpoint reconciliation rationale (per CONTEXT.md locked decision): `--ax-bp-sm ↓` (599.98px) hides non-essential chrome (search-trigger text @1903, attention pill @2076); `--ax-bp-content ↑` (640px) promotes content layout (field-list/launcher/KPI 1→2col). These serve distinct intents (max-width chrome-hide guard vs min-width content-promotion step) and are NOT duplicates. The 40px gap (600–640px) is observable only at unusual viewport widths and is not visible @360px. Decision: document, do not collapse.

    2. A before-scores table with columns: Screen | File | Group | ① | ② | ③ | ④ | ⑤ | ⑥ | ⑦ | ⑧ | ⑨ | ⑩ | Min | Pass?

    Score using the RESEARCH.md signals:
    - ① (token compliance): grep style= and literal hex in each live file (exclude accent_hex/accent_contrast_hex lines — branding config, not violations). If clean: 3. If inline style= present: 1.
    - ② (visual hierarchy): ax-eyebrow→ax-heading→ax-body present AND ax-display used on hero numbers for screens that have them.
    - ③ (spacing rhythm): no literal px/rem in markup spacing; uses ax-space-* classes.
    - ④ (state coverage): empty, error, and populated branches present in markup.
    - ⑤ (responsive): ax-grid-2/3 used on detail screens; card_fields+card_title wired on list screens. Score 1 for list screens until the CSS fix lands (breakpoint fires at wrong rung currently). After Wave 0 CSS fix, list screens with card_fields wired move to 2.
    - ⑥ (contrast): status uses text label (not color-only); -readable variants used on tinted surfaces. Check status_badge usage.
    - ⑦ (focus & semantics): aria-*/ scope=/role= present; dl/dt/dd for field-list; th scope="col" in data_table (already correct per RESEARCH — don't double-count).
    - ⑧ (brand): ax-display on KPI/hero numbers where applicable.
    - ⑨ (motion): record existing state; do NOT change anything. All screens inherit reduced-motion block.
    - ⑩ (reuse/DRY): Detail.summary_card/detail_section/detail_field_list used vs hand-rolled.

    Known baseline findings from RESEARCH.md to confirm/record:
    - event_live: ② score 1 (bare span facts, no body sections), ④ score 1 (no not-found state copy), ⑦ score 1 (span Actor/Subject/Recorded, not dl/dt/dd), ⑩ score 1-2 (uses summary_card but body facts are bare spans, no detail_section body)
    - coupon_live: ⑩ score 1 (no Detail alias, hand-rolled ax-page key/value), ⑦ score 1 (p Key: value, not dl), ② score 1-2 (missing summary_card hero)
    - promotion_code_live: ⑩ score 1 (no Detail alias, minimal structure), same issues as coupon
    - webhook_live forensic payload section: ⑩ score 2 (uses detail_section elsewhere; forensic section is hand-rolled ax-card)
    - connect_account_live: ⑤ score 1 (nested ax-grid-2 inside detail_section at 360px needs check); ① check line 145 for prose without ax-measure
    - List screens (all 9): ⑤ score 1 until CSS fix (breakpoint at 1024px, should be 768px)

    3. After the before-scores table, add a "Worst-First Ordering" section listing screens ranked by minimum dimension score (ascending), which drives the uplift sequence.

    4. Add an "After Scores" section (empty table — to be filled in by subsequent wave executors).

    Do NOT make any changes to any LiveView files. This task is read-only except for creating the SCORECARD.md artifact.
  </action>
  <verify>
    <automated>ls .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md &amp;&amp; grep -c "| screen\|Screen" .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md</automated>
  </verify>
  <done>176-SCORECARD.md exists with: (a) 600/640 reconciliation header rationale, (b) before-scores table with all 21 screens scored on all 10 dimensions, (c) worst-first ordering list, (d) empty after-scores table stub.</done>
</task>

<task type="auto">
  <name>Task 2: Fix data-table collapse breakpoint --ax-bp-lg→--ax-bp-md in app.css + asset rebuild</name>
  <files>
    accrue_admin/assets/css/app.css
    accrue_admin/priv/static/assets/app.css
  </files>
  <read_first>
    - accrue_admin/assets/css/app.css lines 1351–1370 — the block to change (PATTERNS.md §2 has the exact before/after)
    - 176-SCORECARD.md (just created in Task 1) — confirm Task 1 completed before touching CSS
    - 176-PATTERNS.md §2 (mobile card-collapse breakpoint fix) — exact required change
  </read_first>
  <action>
    Edit accrue_admin/assets/css/app.css at approximately line 1361. Change the data-table card↔table swap @media rule from --ax-bp-lg (1024px) to --ax-bp-md (768px):

    The block currently reads:
      @media (min-width: 1024px) { /* --ax-bp-lg ↑ */
        .ax-data-table-shell { display: block; }
        .ax-data-table-cards { display: none; }
      }

    Change BOTH the value (1024 → 768) AND the token comment (--ax-bp-lg ↑ → --ax-bp-md ↑):
      @media (min-width: 768px) { /* --ax-bp-md ↑ */
        .ax-data-table-shell { display: block; }
        .ax-data-table-cards { display: none; }
      }

    Also add `overflow-x: auto` to the `.ax-data-table-shell` CSS rule (defensive guard for widest tables at 768px — RESEARCH.md §Pitfall 3 / §Open Question 3). Find the .ax-data-table-shell rule in the block before the @media and add overflow-x: auto to it.

    After the edit, run: cd accrue_admin && mix accrue_admin.assets.build

    Commit the changed app.css AND all modified files in priv/static/ together. The grep-guard for this change: after editing, `grep -n 'ax-data-table-shell' accrue_admin/assets/css/app.css` must show the display:none default (mobile-first) and the display:block @media rule that now fires at 768px.

    Anti-churn token: rubric ⑤ (responsive/mobile-first) — before score 1 for all 9 list screens (breakpoint fires at 1024px); after score 2+ (breakpoint fires at 768px per SCR-02 + CONTEXT locked decision D per 176-CONTEXT.md).
  </action>
  <verify>
    <automated>grep -n 'min-width: 768px.*--ax-bp-md' /Users/jon/projects/accrue/accrue_admin/assets/css/app.css | grep -q 'ax-data-table' || grep -c 'min-width: 768px.*--ax-bp-md' /Users/jon/projects/accrue/accrue_admin/assets/css/app.css &amp;&amp; cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>app.css @media rule for .ax-data-table-shell/.ax-data-table-cards fires at 768px with grep-able comment "/* --ax-bp-md ↑ */"; .ax-data-table-shell has overflow-x:auto; priv/static rebuilt and committed; mix test --seed 0 = 227 tests, 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → CSS file | CSS edits are static; no user input, no data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01-SC | Tampering | no new package installs this phase | accept | No npm/pip/cargo installs; no slopcheck required |

This plan is presentation-only (CSS edit + planning artifact creation). No new data access, no new routes, no new user inputs, no new HTTP endpoints. No authentication or authorization surface is touched. The tokenization sweep (SCORECARD audit) must not misclassify branding-config hex literals (`accent_hex`/`accent_contrast_hex` in `*_live.ex`) as token violations — they are domain seed data, not CSS bypasses.
</threat_model>

<verification>
- 176-SCORECARD.md exists with 21-screen × 10-dimension before-score table and 600/640 reconciliation rationale
- grep confirms: `grep -c 'min-width: 768px.*--ax-bp-md' accrue_admin/assets/css/app.css` ≥ 2 (this rule + at least one pre-existing ax-grid-2 rule)
- grep confirms no bare-literal @media for data-table: `grep 'ax-data-table-shell' app.css` shows the 768px rule
- `cd accrue_admin && mix test --seed 0` = 227 tests, 0 failures
- `git status accrue_admin/priv/static/` is clean (bundle committed)
</verification>

<success_criteria>
176-SCORECARD.md captures all 21 screen × 10 rubric-dimension before-scores at code level, with worst-first ordering and 600/640 reconciliation rationale. The data-table CSS fires at 768px with the correct grep-able token comment. Asset bundle rebuilt and committed. Suite green at 227. All subsequent waves can use the SCORECARD before-scores as their anti-churn tokens.
</success_criteria>

<output>
Create `.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-01-SUMMARY.md` when done.
</output>

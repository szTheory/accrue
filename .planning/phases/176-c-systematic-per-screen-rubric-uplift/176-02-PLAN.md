---
phase: 176-c-systematic-per-screen-rubric-uplift
plan: 02
type: execute
wave: 2
depends_on:
  - 176-01
files_modified:
  - accrue_admin/lib/accrue_admin/live/charges_live.ex
  - accrue_admin/lib/accrue_admin/live/coupons_live.ex
  - accrue_admin/lib/accrue_admin/live/promotion_codes_live.ex
  - accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
  - accrue_admin/lib/accrue_admin/live/events_live.ex
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/lib/accrue_admin/live/invoices_live.ex
  - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
  - accrue_admin/lib/accrue_admin/live/customers_live.ex
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03

must_haves:
  truths:
    - "Every list screen's card_title surfaces the human-meaningful identifier for the operator job"
    - "Every list screen's card_fields contains only decision-critical columns for its persona (not all columns)"
    - "The CSS breakpoint fix from Plan 01 is in place so these audits verify against the correct 768px tier"
    - "Any copy/empty_title domain-specific labels are wired where they serve the operator persona better"
    - "mix test --seed 0 stays at 227 green"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/charges_live.ex"
      provides: "card_fields contains only decision-critical columns for payment persona"
    - path: "accrue_admin/lib/accrue_admin/live/events_live.ex"
      provides: "card_title using event type is readable; aria-label on KPI section"
  key_links:
    - from: "list LiveView card_fields attr"
      to: "data_table component card rendering"
      via: "DataTable.data_table card_title/card_fields attrs"
      pattern: "card_fields"
---

<objective>
Wave 1 — list screen card_fields quality audit. All 9 list screens already wire card_fields/card_title (confirmed in RESEARCH.md). The Wave 0 CSS fix activated the correct 768px breakpoint. This plan audits each list screen's card_title and card_fields selections against the operator persona and fixes quality misses.

Purpose: Rubric ⑤ for list screens is now structurally correct (CSS fires at 768px). This plan lifts rubric ② (visual hierarchy — is the card_title the right identifier?) and ⑩ (reuse/DRY — are the card_fields the right columns for the persona's job, not just all columns?) for the 9 tail list screens.

Output: List screen LiveViews with confirmed or improved card_title/card_fields selections. SCORECARD.md after-scores updated for Wave 1 screens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-CONTEXT.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-RESEARCH.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md

<interfaces>
<!-- From PATTERNS.md §5 — card_fields/card_title wiring: -->
<!-- DataTable.data_table attrs: -->
<!--   card_title={fn row -> row.processor_id || row.id end} -->
<!--   card_fields={[@col_name, @col_status, @col_amount]}  ← explicit list, not all cols -->

<!-- Persona-job criteria per list screen: -->
<!--   charges/payments: Support/Finance Ops — needs customer name, amount, status, date -->
<!--   coupons: Catalog — needs discount %, redemption count, status, validity -->
<!--   promotion_codes: Catalog — needs code, coupon parent, redemptions, status -->
<!--   connect_accounts: Developer — needs account ID, status, fees -->
<!--   events: Compliance/Developer — needs event type (already card_title), subject, actor, when -->
<!--   webhooks: Developer — needs type, status (delivered/failed), endpoint, received_at -->
<!--   invoices: Finance Ops — needs number/id, customer, status, amount, due_at -->
<!--   customers: Support — needs name/email, subscription status -->
<!--   subscriptions: Finance Ops — needs customer, plan, status, billing interval -->

<!-- RESEARCH.md confirmed current card_fields per screen: -->
<!--   customers: 3 fields (name/email/processor_id/id, + 3 more) -->
<!--   charges: 5 fields (customer, signals, status, amount, fees) — may be excessive -->
<!--   subscriptions: 4 fields -->
<!--   invoices: 4+ fields -->
<!--   coupons: 4 fields (discount, redemptions, status, redeem_by) -->
<!--   promotion_codes: 4 fields (code, coupon link, redemptions, status) -->
<!--   connect_accounts: 4 fields -->
<!--   events: 4 fields (subject, actor, webhook source, when) -->
<!--   webhooks: 4 fields (type, status, endpoint, received) -->

<!-- Anti-churn: only edit a list screen if its SCORECARD before-score shows a -->
<!-- dimension < 2 OR if card_fields audit reveals a confirmed persona-job miss. -->
<!-- Frozen screens (customers_live, dashboard_live) touched only on flagged miss. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix tail list screens card_title/card_fields quality (charges, coupons, promo-codes, connect, events, webhooks)</name>
  <files>
    accrue_admin/lib/accrue_admin/live/charges_live.ex
    accrue_admin/lib/accrue_admin/live/coupons_live.ex
    accrue_admin/lib/accrue_admin/live/promotion_codes_live.ex
    accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
    accrue_admin/lib/accrue_admin/live/events_live.ex
    accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  </files>
  <read_first>
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for each screen; only edit where score < 2 on a dimension or confirmed persona miss
    - accrue_admin/lib/accrue_admin/live/charges_live.ex — verify card_fields 5 cols (customer, signals, status, amount, fees); assess against payment-persona job
    - accrue_admin/lib/accrue_admin/live/events_live.ex — verify card_title = row.type is sufficiently meaningful; check aria-label on KPI section
    - 176-PATTERNS.md §5 — card_fields/card_title pattern from charges_live (the gold standard for this work)
  </read_first>
  <action>
    For each of the 6 tail list screens, read the current card_title and card_fields, then compare against the persona-job test below. Only make changes where a dimension scores < 2 in the SCORECARD or a confirmed persona-job miss exists (anti-churn rule — the SCORECARD before-score IS the justification token).

    Per-screen audit criteria:

    charges_live.ex — card_title: `row.processor_id || row.id` (adequate). card_fields: currently 5 fields (customer, signals, status, amount, fees). Assess: does the payment-support persona need the "signals" column (likely a Braintree-specific field) on mobile? If signals is a minor/rarely-needed column, trim to 4 decision-critical fields (customer, status, amount, date). Anti-churn justification: rubric ② (visual hierarchy — information density on mobile card) or ⑩ (reuse — card mirrors the desktop table's most important columns). If all 5 are genuinely decision-critical, leave as-is and document.

    coupons_live.ex — card_title: `coupon_label(row)` (correct). card_fields: 4 fields (discount, redemptions, status, redeem_by). Verify these are the right fields for catalog persona (valid/invalid status and redemption count are the two most important). If correct, confirm and move on. Also check: does the empty_title use `data_table_default_empty_title/0` or a domain-specific label? If it's the generic default, and a more domain-specific label exists in Copy, update it (persona clarity for catalog operators).

    promotion_codes_live.ex — card_title: `row.code || row.processor_id || row.id`. card_fields: 4 fields. Verify code is the meaningful identifier. Check empty_title for domain specificity.

    connect_accounts_live.ex — card_title: `row.stripe_account_id || row.id`. card_fields: 4 fields. Verify account ID is the right identifier for the developer persona. Check empty_title.

    events_live.ex — card_title: `row.type`. Event type names can be long (e.g., "customer.subscription.updated") — verify this renders acceptably in a mobile card heading. Check that the KPI section (if present — events_live has a KPI showing event counts) has an aria-label on its container section element. If missing, add `aria-label="Event log metrics"` (or equivalent) to the KPI section's parent element. This satisfies rubric ⑦ (focus & semantics).

    webhooks_live.ex — card_title: `row.processor_event_id || row.id`. card_fields: 4 fields (type, status, endpoint, received). Verify: for developer debugging persona, status (delivered/failed/dead-letter) and type are the two key fields; endpoint and received_at are secondary. Order should be status first, type second for triage. If current order is different, reorder card_fields to put status first.

    After any change to a file, record the justification: (a) rubric dimension with before-score, (b) persona-job miss fixed. Do not add new columns or fields; adjust existing selections only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/live/charges_live_test.exs test/accrue_admin/live/events_live_test.exs test/accrue_admin/live/webhooks_live_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>Each of the 6 tail list screens has been audited; changes made only where SCORECARD before-score < 2 or confirmed persona-job miss; card_title and card_fields selections verified or corrected against persona criteria; any changed file compiles cleanly; relevant tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Audit polished list screens (invoices, subscriptions, customers) + update SCORECARD Wave 1 after-scores</name>
  <files>
    accrue_admin/lib/accrue_admin/live/invoices_live.ex
    accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
    accrue_admin/lib/accrue_admin/live/customers_live.ex
    .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
  </files>
  <read_first>
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for invoices (moderately iterated), subscriptions (moderately iterated), customers (heavily iterated / polished); only touch on flagged miss
    - accrue_admin/lib/accrue_admin/live/invoices_live.ex — verify card_fields surfaces the right finance-ops columns (number, customer, status, amount, due_at)
    - accrue_admin/lib/accrue_admin/live/customers_live.ex — polished (Phase 21 focus) — touch only if SCORECARD shows a dimension < 2
  </read_first>
  <action>
    Audit the three polished/moderately-iterated list screens. Apply the same criteria as Task 1.

    customers_live.ex — HEAVILY ITERATED. Touch only if the SCORECARD before-score shows a dimension < 2. The anti-churn rule is strict here: if all dimensions are ≥ 2, do NOT touch this file at all. Document "confirmed ≥ 2 on all dimensions, no changes made" in the SCORECARD.

    subscriptions_live.ex — MODERATELY ITERATED. Check SCORECARD before-score. Specifically verify: card_fields surfaces the customer identifier, plan/interval, status, and next-billing-date for the finance-ops persona (the work-queue default filters from Phase 175 must not be regressed). If all dimensions are ≥ 2, no changes.

    invoices_live.ex — MODERATELY ITERATED. Check SCORECARD before-score. Specifically verify: card_fields includes invoice number (or processor_id), customer, status, amount, and due_at — these are the columns the Finance Ops persona needs at a glance to work the queue. Also verify the aria-label on any KPI section above the table (finance ops expects totals like "total overdue"). If missing, add it.

    After auditing all 9 list screens, update the SCORECARD.md "After Scores" section for the 9 list screens with the Wave 1 after-scores. For screens with no changes made, copy the before-score as the after-score and annotate "no changes — already ≥ 2". For changed screens, record the new score per dimension.

    Finally, run the full suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>All 9 list screens audited; changes made only on rubric-flagged misses; SCORECARD Wave 1 after-scores filled in for all 9 list screens; mix test --seed 0 = 227+ tests, 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → LiveView templates | Read-only audit + targeted attribute changes; no new user inputs or routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-02-SC | Tampering | no new packages installed | accept | No package installs; no slopcheck required |

This plan modifies only list LiveView templates (card_title/card_fields attributes and optional aria-label additions). No new routes, no new data ingestion, no new user inputs, no new authentication surface. The work-queue default filters wired in Phase 175 must not be regressed — the executor must verify by running the relevant test files.
</threat_model>

<verification>
- All 9 list screens audited; changes justified by SCORECARD before-score < 2 or confirmed persona miss
- `cd accrue_admin && mix test --seed 0` = 227+ tests, 0 failures
- SCORECARD Wave 1 after-scores section populated for all 9 list screens
- No file touches made on frozen/polished screens unless SCORECARD confirmed a dimension < 2
</verification>

<success_criteria>
All 9 list screens confirmed or corrected for card_title/card_fields quality against persona criteria. SCORECARD Wave 1 after-scores documented. Suite stays green at 227+. Only changes backed by SCORECARD before-score < 2 or persona-job miss.
</success_criteria>

<output>
Create `.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-02-SUMMARY.md` when done.
</output>

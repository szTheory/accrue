---
phase: 176-c-systematic-per-screen-rubric-uplift
plan: 03
type: execute
wave: 3
depends_on:
  - 176-01
  - 176-02
files_modified:
  - accrue_admin/lib/accrue_admin/live/event_live.ex
  - accrue_admin/lib/accrue_admin/live/coupon_live.ex
  - accrue_admin/test/accrue_admin/live/event_live_test.exs
  - accrue_admin/test/accrue_admin/live/coupon_live_test.exs
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04

must_haves:
  truths:
    - "event_live renders a Detail.detail_section body section (actor/subject/type fields) not just bare span facts"
    - "event_live facts slot uses dl/dt/dd semantic structure inside the summary_card"
    - "event_live has a not-found state copy path (state coverage, dim ④)"
    - "coupon_live imports Detail and uses Detail.summary_card for the hero section"
    - "coupon_live projection section uses Detail.detail_section + Detail.detail_field_list not hand-rolled ax-page key/value"
    - "coupon_live promotion-codes list section uses Detail.detail_section wrapper"
    - "mix test --seed 0 stays at 227+ green"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/event_live.ex"
      provides: "Semantic event detail with dl/dt/dd facts, Detail.detail_section body, not-found state coverage"
      contains: "detail_section"
    - path: "accrue_admin/lib/accrue_admin/live/coupon_live.ex"
      provides: "Detail-component-based hero and projection sections"
      contains: "Detail.detail_section"
  key_links:
    - from: "event_live.ex Detail.summary_card :facts slot"
      to: "dl/dt/dd semantic structure"
      via: "dt class='ax-label' / dd class='ax-body'"
      pattern: "ax-label.*ax-body"
    - from: "coupon_live.ex projection section"
      to: "Detail.detail_field_list"
      via: "fields={[%{label: ..., value: ...}]}"
      pattern: "detail_field_list"
---

<objective>
Wave 2a — catalog/specialist detail DRY uplift, part 1. Lifts the two thinnest catalog tail screens: event_live (thinnest in the codebase — bare span facts, no body sections, no not-found state) and coupon_live (missing Detail alias, hand-rolled ax-page key/value, no summary_card hero).

Purpose: event_live scores 1 on dimensions ②③④⑦⑩ — the most misses of any screen. coupon_live scores 1 on ②⑦⑩ from the hand-rolled structure. Fixing these two screens produces the largest rubric score improvements in Wave 2.

Output: event_live with semantic detail sections and dl/dt/dd facts; coupon_live with proper Detail component structure throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-CONTEXT.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-RESEARCH.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md

<interfaces>
<!-- Detail component API (from PATTERNS.md §1, verified from detail.ex): -->
<!--   detail_section: attr :title required, slot :inner_block required, slot :actions optional -->
<!--     renders: <section class="ax-card ax-detail-section"> + <header class="ax-detail-section-head"> -->

<!--   detail_field_list: attr :fields, :list, required — list of %{label: ..., value: ...} maps -->
<!--     renders: <dl class="ax-field-list"> with <dt class="ax-field-label"> + <dd class="ax-field-value"> -->
<!--     CSS: .ax-field-list goes 1-col → 2-col at --ax-bp-content (640px) — already responsive -->

<!--   summary_card: attr :eyebrow optional, :title required -->
<!--     slot :status, :facts, :actions (all optional) -->
<!--     IMPORTANT: :facts slot renders children inside <div class="ax-summary-facts"> (NOT a <dl>) -->
<!--     Use <dl class="ax-summary-facts-dl"> / <dt class="ax-label"> / <dd class="ax-body"> INSIDE the slot -->

<!-- event_live CURRENT state (from PATTERNS.md §Pattern Assignments / event_live.ex): -->
<!--   line 65–73: Detail.summary_card with bare <span> facts (Actor, Subject, Recorded) -->
<!--   Detail alias IS already present (line 12) — only RelatedResources + Detail aliased -->
<!--   No detail_section body. No not-found state copy. -->
<!--   Needs: FlashGroup + Copy aliases added; detail_section body; dl/dt/dd inside :facts; -->
<!--          not-found state path (copy webhook_live ":ambiguous" pattern) -->

<!-- coupon_live CURRENT state (from PATTERNS.md §coupon_live): -->
<!--   line 10: alias WITHOUT Detail (AppShell, Breadcrumbs, JsonViewer, KpiCard, RelatedResources only) -->
<!--   lines 46–61: hand-rolled <header class="ax-page-header"> (no summary_card) -->
<!--   lines 78–98: promotion codes list as <section class="ax-card"> (no detail_section wrapper) -->
<!--   lines 100–111: <div class="ax-page"> + <p class="ax-body">Key: value</p> (hand-rolled key/value) -->
<!--   Needs: Detail alias added; hand-rolled sections replaced with detail_section/detail_field_list -->

<!-- Gold standard for coupon uplift: charge_live.ex lines 123–286 (PATTERNS.md §charge_live section) -->
<!-- charge_live.ex already uses Detail.summary_card, detail_section, detail_field_list -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Uplift event_live.ex — semantic facts + detail_section body + not-found state</name>
  <files>
    accrue_admin/lib/accrue_admin/live/event_live.ex
    accrue_admin/test/accrue_admin/live/event_live_test.exs
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/event_live.ex — full file read (it is thin; ~100 lines)
    - accrue_admin/lib/accrue_admin/components/detail.ex — verify :facts slot contract (does it wrap in a dl? RESEARCH A2 assumption)
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex lines 120–240 — the analog for a thin forensic detail screen with not-found state + detail_section body
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for event_live (dim ②③④⑦⑩)
  </read_first>
  <behavior>
    - event_live_test.exs: "renders semantic dl/dt/dd facts inside summary_card" — html contains ax-label and ax-body within the facts area
    - event_live_test.exs: "renders a detail_section body section with event type, actor, subject, recorded fields" — html contains "detail_section" marker and field list output
    - event_live_test.exs: "applies ax-measure to any prose description in body sections" — html =~ ~s(class="ax-body ax-measure") when prose content exists
    - event_live_test.exs: "redirects or shows flash on nil event" — nil-event path produces a meaningful flash or redirect (state coverage dim ④)
  </behavior>
  <action>
    Read event_live.ex in full (it is thin). Also read detail.ex to confirm the :facts slot wrapper element (critical for deciding whether to put dl directly inside or use a dl inside div). Then:

    1. Resolve the :facts slot contract: if detail.ex renders :facts inside a bare div (not dl), the correct pattern is to place `<dl class="ax-summary-facts-dl"><dt class="ax-label">…</dt><dd class="ax-body">…</dd></dl>` INSIDE the slot. If it already wraps in a dl, the dt/dd approach is still correct. Do NOT use bare <span>Actor: </span> patterns.

    2. Upgrade the :facts slot (lines 65–73): replace bare `<span>Actor: …</span>` lines with proper dl/dt/dd pairs:
       - dt class="ax-label" for each label (Actor, Subject, Recorded)
       - dd class="ax-body" for each value
       Check if the Event struct has `actor_id` and `subject_id` fields by reading the Accrue.Events.Event schema before writing. Use only fields that exist.

    3. Add a Detail.detail_section body section (after the summary_card block): a "Event details" section with detail_field_list using %{label:, value:} maps for type, actor_type, actor_id (if exists), subject_type, subject_id (if exists), inserted_at (format_datetime). This is the body content that event_live currently lacks (dim ② miss).

    4. Add not-found state coverage (dim ④): copy the not-found pattern from webhook_live (which redirects or shows a flash when @webhook is nil). For event_live, the current handle_params redirects on nil but shows no user-facing message. Add either a flash message or a rendered "Event not found" path following the same pattern used in webhook_live. Do not add a new error component — reuse existing flash/banner patterns.

    5. Apply .ax-measure to any prose description paragraphs added in the detail_section body (any `<p class="ax-body">` that contains a sentence, not just a value). Field-list items themselves do NOT get ax-measure (they use detail_field_list).

    6. Verify imports: event_live already aliases Detail. Confirm FlashGroup and Copy are aliased — add them if missing (follow webhook_live import structure).

    7. Anti-churn tokens (all justified by SCORECARD before-scores):
       - dim ② before: 1 → after: 2 (add detail_section body with heading hierarchy)
       - dim ④ before: 1 → after: 2 (add not-found state path)
       - dim ⑦ before: 1 → after: 2 (dl/dt/dd facts in summary_card)
       - dim ⑩ before: 1–2 → after: 2–3 (detail_section body added)
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/live/event_live_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>event_live.ex has dl/dt/dd facts in :facts slot; a Detail.detail_section body section with detail_field_list; a not-found state path (flash or rendered copy); .ax-measure on any added prose sentences; event_live_test.exs assertions added and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Uplift coupon_live.ex — add Detail alias, summary_card hero, DRY projection section + update SCORECARD Wave 2a</name>
  <files>
    accrue_admin/lib/accrue_admin/live/coupon_live.ex
    accrue_admin/test/accrue_admin/live/coupon_live_test.exs
    .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/coupon_live.ex — full file read
    - accrue_admin/lib/accrue_admin/live/charge_live.ex lines 123–160 — gold standard: summary_card hero + detail_section + detail_field_list pattern
    - accrue_admin/lib/accrue_admin/components/detail.ex — confirm detail_section and detail_field_list slot API
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-PATTERNS.md §coupon_live — exact before/after code shapes
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — before-scores for coupon_live (dim ②⑦⑩)
  </read_first>
  <behavior>
    - coupon_live_test.exs: "renders Detail.summary_card hero section not a hand-rolled page header" — html =~ ~s(ax-detail-summary) or similar output from summary_card (inspect actual output by reading charge_live_test.exs for the assertion pattern)
    - coupon_live_test.exs: "renders projection section as Detail.detail_section with semantic dl field list" — html contains "ax-field-list" inside a "ax-detail-section"
    - coupon_live_test.exs: "applies ax-measure to projection prose" — html =~ ~s(class="ax-body ax-measure") when projection section has prose
    - coupon_live_test.exs: "promotion codes section rendered in Detail.detail_section wrapper" — html contains ax-detail-section around the promotion codes list
  </behavior>
  <action>
    Read coupon_live.ex in full. Then apply PATTERNS.md §coupon_live exactly:

    1. Add Detail to the alias list at line 10:
       Change: alias AccrueAdmin.Components.{AppShell, Breadcrumbs, JsonViewer, KpiCard, RelatedResources}
       To: alias AccrueAdmin.Components.{AppShell, Breadcrumbs, Detail, JsonViewer, KpiCard, RelatedResources}

    2. Replace the hand-rolled `<header class="ax-page-header">` hero section (lines ~46–61) with Detail.summary_card:
       - eyebrow from AccrueAdmin.Copy.coupon_detail_eyebrow() — if no such Copy key exists, use a string "Coupon" as a fallback (do not add new Copy keys)
       - title from coupon_label(@coupon) (already available as a private function)
       - :status slot: wrap the existing valid/invalid status display in the status badge pattern (check what status_summary/1 currently renders — if it already uses StatusBadge, preserve it; otherwise use the existing status_summary output as-is)
       - :facts slot: move the existing processor_id/discount/status summary span content into the slot

    3. Wrap the promotion codes list section (lines ~78–98) in Detail.detail_section:
       - title from AccrueAdmin.Copy.coupon_detail_section_codes_heading() (already used as an h3)
       - inner content unchanged (the link list and empty-state paragraph stay as-is inside the section)
       - Remove the now-redundant hand-rolled `<section class="ax-card"><header>` wrapper

    4. Replace the projection key/value section (lines ~100–111) with Detail.detail_section + Detail.detail_field_list:
       - title from AccrueAdmin.Copy.coupon_detail_section_projection_heading() (already used as an h3)
       - fields list: %{label: AccrueAdmin.Copy.coupon_detail_label_duration(), value: duration_summary(@coupon)}, %{label: AccrueAdmin.Copy.coupon_detail_label_currency(), value: @coupon.currency || "--"}, %{label: AccrueAdmin.Copy.coupon_detail_label_processor(), value: @coupon.processor || "--"}
       - Check: does Copy module have all these keys? If a key is missing, use the string value from the existing <p> tag directly

    5. Add .ax-measure to any prose description `<p class="ax-body">` elements added in or around the projection section (not to the detail_field_list itself).

    6. Anti-churn tokens (justified by SCORECARD):
       - dim ② before: 1–2 → after: 2–3 (summary_card hero replaces ad-hoc page-header)
       - dim ⑦ before: 1 → after: 2 (detail_field_list provides dl/dt/dd semantics)
       - dim ⑩ before: 1 → after: 2–3 (Detail primitives replace hand-rolled sections)

    7. After updating both coupon_live.ex and event_live.ex: update SCORECARD.md "After Scores" section for these two screens with Wave 2a after-scores per dimension.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/live/coupon_live_test.exs test/accrue_admin/live/event_live_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>coupon_live.ex has Detail alias, summary_card hero, Detail.detail_section wrapper on codes list, Detail.detail_field_list on projection section, .ax-measure on prose; coupon_live_test.exs assertions added and green; SCORECARD Wave 2a after-scores filled for event_live and coupon_live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → LiveView templates | Markup/component structure changes; no new data inputs, no new routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-03-SC | Tampering | no new packages installed | accept | No package installs; no slopcheck required |
| T-176-03-01 | Tampering | coupon_live form/action handlers | mitigate | When replacing hand-rolled sections with Detail.detail_section, do not alter or remove any phx-submit/phx-change handlers or data-role attributes. The Detail.detail_section wrapper is a pure layout change — inner content is passed via :inner_block slot unchanged. Verify by checking that all existing phx-* event names in coupon_live are present after the refactor. |

This plan modifies only presentation markup — no new routes, no new user inputs, no data access changes. coupon_live.ex has no form submission (coupons are read-only in the admin). event_live.ex similarly has no form. No new security surface introduced.
</threat_model>

<verification>
- `cd accrue_admin && mix test --seed 0 test/accrue_admin/live/event_live_test.exs test/accrue_admin/live/coupon_live_test.exs` — all new assertions pass
- `cd accrue_admin && mix test --seed 0` — 227+ tests, 0 failures (no regressions)
- `grep "detail_section" accrue_admin/lib/accrue_admin/live/event_live.ex` returns at least 1 match
- `grep "detail_section" accrue_admin/lib/accrue_admin/live/coupon_live.ex` returns at least 2 matches
- `grep "Detail," accrue_admin/lib/accrue_admin/live/coupon_live.ex` confirms Detail is now aliased
- `grep "ax-measure" accrue_admin/lib/accrue_admin/live/coupon_live.ex` returns ≥ 1 match (prose regions)
- SCORECARD Wave 2a after-scores updated for event_live and coupon_live
</verification>

<success_criteria>
event_live.ex: semantic dl/dt/dd facts in :facts slot; Detail.detail_section body with event fields; not-found state coverage. coupon_live.ex: Detail alias added; hand-rolled header replaced with summary_card; hand-rolled projection section replaced with detail_section/detail_field_list; codes list wrapped in detail_section. All tests green. SCORECARD after-scores updated.
</success_criteria>

<output>
Create `.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-03-SUMMARY.md` when done.
</output>

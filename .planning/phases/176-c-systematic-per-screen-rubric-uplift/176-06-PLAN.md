---
phase: 176-c-systematic-per-screen-rubric-uplift
plan: 06
type: execute
wave: 5
depends_on:
  - 176-05
files_modified:
  - accrue_admin/test/accrue_admin/components/data_table_test.exs
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04

must_haves:
  truths:
    - "data_table_test.exs has a Nyquist assertion confirming the breakpoint CSS comment reads '--ax-bp-md ↑'"
    - "A grep guard confirms no bare-literal @media for data-table-shell (the registered token comment is present)"
    - "The full suite stays at 227+ green"
    - "No ax-measure applied to wrong targets (grep guard passes)"
    - "176-SCORECARD.md is the completed before/after evidence artifact for SCR-03"
  artifacts:
    - path: "accrue_admin/test/accrue_admin/components/data_table_test.exs"
      provides: "Nyquist breakpoint guard asserting CSS comment token and ax-measure structural assertions"
      contains: "--ax-bp-md"
  key_links:
    - from: "data_table_test.exs Nyquist assertion"
      to: "app.css data-table @media block"
      via: "grep-based CSS comment verification"
      pattern: "--ax-bp-md"
---

<objective>
Wave 4 (final) — Nyquist validation assertions gate. Adds the structural test assertions required by the VALIDATION.md Wave 0 gap checklist, confirms the full suite is green with all uplift committed, and verifies no ax-measure misapplication.

Purpose: VALIDATION.md lists three Wave 0 gaps: (1) grep guard for data-table breakpoint, (2) ax-measure presence assertions per touched screen, (3) token-compliance grep guard. This plan closes those gaps with cheap, durable assertions in the existing test infrastructure so future regressions are caught automatically.

Output: data_table_test.exs with breakpoint guard test; grep-based verifications for ax-measure and token compliance; full suite green; phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-CONTEXT.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-VALIDATION.md
@.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md

<interfaces>
<!-- VALIDATION.md Wave 0 gaps to close: -->
<!--   1. grep guard: grep -c 'ax-bp-md.*↑' accrue_admin/assets/css/app.css ≥ 2 after breakpoint edit -->
<!--      (data-table block + ax-grid-2 block both use --ax-bp-md ↑ comment) -->
<!--   2. ExUnit assertion for .ax-measure presence per touched screen (invoice, charge, coupon, event, webhook, connect_account) -->
<!--   3. grep guard: no bare-literal @media for data-table (the token comment must be present) -->

<!-- Existing data_table_test.exs: -->
<!--   line 157: card_title={& &1.label} — card rendering already tested -->
<!--   line 282: test "renders card mode markup..." — data-role="card-list" assertion exists -->
<!--   line 286: assert html =~ ~s(data-role="card-list") — exists -->
<!--   NEW assertion to add: verify the CSS comment in app.css has the correct --ax-bp-md token -->

<!-- The ExUnit/LiveView approach for CSS grep assertion: -->
<!--   Use ExUnit + File.read! in a test module to verify the CSS file content. -->
<!--   Alternatively: a lightweight @moduletag :slow test that reads app.css and asserts -->
<!--   the token comment pattern. This avoids a custom Mix task. -->
<!--   Pattern from Phase 174 tests (check existing token guard tests for the approach used). -->

<!-- ax-measure misapplication guard: -->
<!--   grep "ax-empty-copy ax-measure\|ax-field-list ax-measure\|json_viewer.*ax-measure" -->
<!--   across all live files should return ZERO matches. -->

<!-- Live test ax-measure assertions already added in Plans 03/04/05. -->
<!-- This plan ONLY adds: the CSS breakpoint guard test in data_table_test.exs. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Nyquist breakpoint guard assertion + ax-measure misapplication guard to data_table_test.exs</name>
  <files>
    accrue_admin/test/accrue_admin/components/data_table_test.exs
  </files>
  <read_first>
    - accrue_admin/test/accrue_admin/components/data_table_test.exs — full file read; locate the existing card-mode test (line 282); find the right place to add the new breakpoint guard assertion
    - accrue_admin/test/accrue_admin/ — grep for any existing CSS-file grep tests in other test files (look for File.read! or System.cmd patterns used in Phase 174 guard tests) to match the established convention
    - .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-VALIDATION.md §Nyquist Structural Assertions — the exact assertion patterns required
  </read_first>
  <behavior>
    - data_table_test.exs: "data-table card/table CSS swap uses --ax-bp-md token" — reads app.css and asserts the @media block for .ax-data-table-shell contains '/* --ax-bp-md ↑ */'
    - data_table_test.exs: "no ax-measure misapplied to ax-empty-copy, ax-field-list, or json_viewer" — grep across all live files returns zero matches for the misapplication patterns
  </behavior>
  <action>
    Read data_table_test.exs in full. Check for any existing CSS file-read assertion pattern (if Phase 174's token tests use File.read!, follow the same pattern; if they use a grep System.cmd, follow that instead).

    Then add a new test block (or add to the existing breakpoint-related describe block if one exists):

    Assertion 1 — Breakpoint token guard:
    Add a test that reads accrue_admin/assets/css/app.css and asserts the data-table @media block uses the --ax-bp-md token comment. The assertion should be:
    - Read app.css with File.read! (use the path relative to the mix project root, or use Path.expand with __DIR__)
    - Assert css_content contains "min-width: 768px) { /* --ax-bp-md ↑ */"
    - Also assert css_content does NOT contain "ax-data-table-shell" adjacent to "min-width: 1024px" (regression guard: breakpoint must not have reverted to lg)
    - If the Phase 174 token tests use a different mechanism, match that mechanism instead

    Assertion 2 — ax-measure misapplication guard:
    Add a test that verifies ax-measure is not applied to forbidden targets across all live files:
    - Read all files matching accrue_admin/lib/accrue_admin/live/**/*_live.ex
    - Assert NONE contain "ax-empty-copy ax-measure" (double-cap pitfall)
    - Assert NONE contain "ax-field-list ax-measure" (columnar, not prose)
    - Note: the json_viewer guard is harder to do via string grep without false positives (the class is not added inline). Skip the json_viewer guard or do a simple check that no ax-measure appears in the JSON-viewer slot context. Prioritize the ax-empty-copy and ax-field-list guards.

    After writing the tests, run them to confirm they pass with the current codebase state (all uplift from Plans 01–05 should be committed by now).

    Then run the full suite:
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin &amp;&amp; mix test --seed 0 test/accrue_admin/components/data_table_test.exs 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>data_table_test.exs has: (a) a test asserting the CSS @media for data-table-shell uses 768px with --ax-bp-md comment; (b) a test asserting no ax-measure misapplication on ax-empty-copy/ax-field-list; all new tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 176 uplift: Wave 0 SCORECARD baseline + CSS breakpoint fix, Wave 1 list screen audit, Wave 2a/b catalog/specialist detail DRY uplift (event_live, coupon_live, promotion_code_live, connect_account_live, webhook_live), Wave 3 dense financial reading-measure (invoice_live, charge_live, subscription_live), and Wave 4 Nyquist guard assertions. 176-SCORECARD.md fully populated with before/after scores. VALIDATION.md gaps closed.
  </what-built>
  <how-to-verify>
    1. Run full suite: `cd accrue_admin && mix test --seed 0` — must show 227+ tests, 0 failures
    2. Spot-check structural assertions pass:
       - `grep -c "min-width: 768px.*--ax-bp-md" accrue_admin/assets/css/app.css` — should be ≥ 2
       - `grep -c "ax-body ax-measure" accrue_admin/lib/accrue_admin/live/invoice_live.ex` — should be ≥ 4
       - `grep -c "detail_section" accrue_admin/lib/accrue_admin/live/coupon_live.ex` — should be ≥ 2
       - `grep "Detail," accrue_admin/lib/accrue_admin/live/coupon_live.ex` — should show Detail in alias
       - `grep "Detail," accrue_admin/lib/accrue_admin/live/promotion_code_live.ex` — should show Detail in alias
       - `grep -c "detail_section" accrue_admin/lib/accrue_admin/live/webhook_live.ex` — should be ≥ 4
       - `grep "ax-measure" accrue_admin/lib/accrue_admin/live/charge_live.ex` — should match ≥ 3 lines
       - `grep "ax-measure" accrue_admin/lib/accrue_admin/live/event_live.ex` — should match ≥ 1 line (prose regions in body)
    3. Review 176-SCORECARD.md: confirm before/after scores populated for all 21 screens and a "Phase 176 Final Summary" section is present
    4. Confirm git status: priv/static/ is clean (bundle was committed after CSS edit in Plan 01)
    5. Optional spot visual check: `cd accrue_admin && npm run e2e:visuals:png-only` — no errors; screenshots generate (full visual scoring is Phase 179)
  </how-to-verify>
  <resume-signal>Type "approved" when suite is green and structural spot-checks pass. Or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → test files | Test file additions; read-only access to CSS and live files for guard assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-06-SC | Tampering | no new packages installed | accept | No package installs; no slopcheck required |

This plan adds only test assertions. No production code changes. No new routes, no new user inputs, no data access. The CSS guard test reads app.css as a plain file (no HTTP, no user input). The ax-measure guard reads live files as plain text. No security surface affected.
</threat_model>

<verification>
- `cd accrue_admin && mix test --seed 0` = 227+ tests, 0 failures
- `grep -c "ax-bp-md" accrue_admin/test/accrue_admin/components/data_table_test.exs` ≥ 1 (new guard test)
- `grep "ax-empty-copy ax-measure" accrue_admin/lib/accrue_admin/live/*.ex` returns no matches (misapplication guard)
- 176-SCORECARD.md fully populated
- `git status accrue_admin/priv/static/` clean
</verification>

<success_criteria>
Nyquist guard assertions in data_table_test.exs confirm the CSS breakpoint and ax-measure are correctly applied. Full suite green at 227+. 176-SCORECARD.md is the complete before/after evidence artifact for SCR-03. Phase 176 structural uplift complete; visual proof deferred to Phase 179.
</success_criteria>

<output>
Create `.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-06-SUMMARY.md` when done.
</output>

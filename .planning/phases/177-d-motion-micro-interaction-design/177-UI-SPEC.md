---
phase: 177
slug: d-motion-micro-interaction-design
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-04
reviewed_at: 2026-06-04
---

# Phase 177 — UI Design Contract (Motion Contract)

> Visual and interaction contract for Phase D — Motion & Micro-interaction Design.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **This is a MOTION CONTRACT, not a greenfield visual spec.** Phase 177 is a depth pass
> on shipped `accrue_admin` (Elixir/Phoenix/LiveView 1.1). The design system is LOCKED:
> custom `ax-*` tokens, Tailwind inert, NO migration. Spacing / typography / color are
> frozen from Phase 167/174 and reproduced here only for completeness — this phase invents
> **no new motion tokens and no new components**. It applies restrained, functional motion
> to existing surfaces via the Phase-174 `--ax-transition-*` bundles. Satisfies MOT-01/02/03.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom `ax-*` CSS + tokens; Tailwind configured but inert — **do NOT migrate**) |
| Preset | not applicable (Elixir/Phoenix/LiveView, not React/Next/Vite — shadcn gate N/A) |
| Component library | none (in-repo `Phoenix.Component` / `Phoenix.LiveComponent` in `accrue_admin/lib/accrue_admin/components/`) |
| Icon library | heroicons rendered inline via `Components.Icon`, sized by `--ax-icon-*` |
| Font | Geist (sans) / Geist Mono — self-hosted variable, tabular numerals |
| Motion substrate | Phase-174 `--ax-transition-*` bundles in `theme.css`; reduced-motion override at `theme.css:187` |

**Registry safety:** not applicable — no shadcn, no third-party registries. All component
source is in-repo and already vetted.

---

## Motion Vocabulary (LOCKED — Phase 174, `theme.css`)

This phase composes **only** from these existing atoms and bundles. No raw `ms`,
no raw `cubic-bezier()`, no new motion tokens. If a need can't be expressed from these,
**flag it** (see Token Gaps) — do **not** invent a literal.

### Duration atoms

| Token | Value | Intended use |
|-------|-------|--------------|
| `--ax-dur-instant` | `0ms` | reduced-motion collapse target |
| `--ax-dur-1` | `120ms` | press, hover, micro feedback |
| `--ax-dur-2` | `180ms` | default state change / **enter** |
| `--ax-dur-3` | `240ms` | drawer / modal **enter** (largest travel) |
| `--ax-dur-exit` | `140ms` | **exit** (snappier than enter — asymmetry) |

### Easing atoms

| Token | Curve | Intended use |
|-------|-------|--------------|
| `--ax-ease-out` | `cubic-bezier(0.2,0,0,1)` | **enter** / default (decelerate in) |
| `--ax-ease-in` | `cubic-bezier(0.4,0,1,1)` | **exit** (accelerate away) |
| `--ax-ease-inout` | `cubic-bezier(0.4,0,0.2,1)` | continuous loops (skeleton shimmer only) |
| `--ax-ease-emphasis` | `cubic-bezier(0.2,0.9,0.3,1.2)` | the single earned overshoot (scale-in) |

### Travel / scale atoms

| Token | Value | Use |
|-------|-------|-----|
| `--ax-rise-sm` | `4px` | small enter translate (dropdowns, toasts) → `0px` under reduced-motion |
| `--ax-rise-md` | `8px` | larger enter translate (drawer, palette) → `0px` under reduced-motion |
| `--ax-press-scale` | `0.97` | active-press feedback → `1` under reduced-motion |

### Property bundles (the primary mechanism — route everything through these)

| Bundle | Composition | Animates |
|--------|-------------|----------|
| `--ax-transition-colors` | `--ax-dur-2 / --ax-ease-out` on color, background-color, border-color | color/state changes |
| `--ax-transition-transform` | `--ax-dur-2 / --ax-ease-out` on transform | slide/scale |
| `--ax-transition-shadow` | `--ax-dur-2 / --ax-ease-out` on box-shadow | elevation |
| `--ax-transition-base` | all of the above (5-property) | general controls |

> **Why bundles are non-negotiable:** the reduced-motion `@media` block at `theme.css:187`
> overrides each bundle to `--ax-dur-instant` (0ms). Any motion routed through a bundle
> honors `prefers-reduced-motion` **for free** (MOT-03). Motion written with inline durations
> bypasses that override and is a contract violation.

---

## The Motion Contract (MOT-01 / MOT-02)

Per surface: trigger · animated property · token · enter vs exit · reduced-motion fallback ·
**functional justification** (continuity / feedback / affordance — never decorative).
Discretion (per CONTEXT §Claude's Discretion) is exercised in the token picks below; all
compose from the locked atoms.

| # | Surface (file) | Trigger | Animated property | Enter token | Exit token | Reduced-motion fallback | Justification |
|---|----------------|---------|-------------------|-------------|------------|-------------------------|---------------|
| 1 | **detail_drawer** (`detail_drawer.ex`, `.ax-detail-drawer`) | open/close (`@open` toggle / mount-remove) | `transform` (translateX `--ax-rise-md`→0) + `opacity` 0→1 | enter: `transform --ax-dur-3 --ax-ease-out, opacity --ax-dur-3 --ax-ease-out` | exit: `opacity --ax-dur-exit --ax-ease-in` (fade only — no travel out) | travel→0 (`--ax-rise-md`=0px), opacity crossfade retained, dur→instant via bundle | **Continuity** — the drawer slides from the edge it's anchored to, so the operator keeps spatial context with the list behind it. |
| 1b | **detail_drawer-backdrop** (`.ax-detail-drawer-backdrop`) | same as drawer | `opacity` 0→1 | `opacity --ax-dur-3 --ax-ease-out` | `opacity --ax-dur-exit --ax-ease-in` | opacity retained, dur→instant | **Affordance** — dimming signals a modal layer; click-to-dismiss target. |
| 2 | **dropdown_menu** (`dropdown_menu.ex`, `.ax-dropdown-panel`) | `<details>` open (`[open]`) | `opacity` 0→1 + `transform` (translateY `--ax-rise-sm`→0) | `--ax-transition-transform` + opacity `--ax-dur-2 --ax-ease-out` | `opacity --ax-dur-exit --ax-ease-in` | translate→0, opacity retained, dur→instant via bundle | **Affordance** — the small rise reads as "this panel belongs to the trigger above it." |
| 3 | **More ▾ overflow** (Phase-175 `.ax-tab-more-menu` in `customer_live.ex`) | toggle open | identical to #2 | same as #2 | same as #2 | same as #2 | **Affordance** — recessed Customer-360 tabs reveal with the same grammar as any dropdown (consistency). |
| 4 | **collapsible nav group** (`sidebar.ex` + `sidebar_collapse.js`, `.ax-sidebar-nav-group` controls list) | toggle (`aria-expanded`; JS sets `list.hidden`) | `--ax-sidebar-group-chevron` rotate (already wired at `app.css:1218`) + group reveal | chevron rotate via `--ax-transition-transform`; reveal: opacity `--ax-dur-2 --ax-ease-out` on the controlled list | symmetric (collapse: opacity `--ax-dur-exit --ax-ease-in`) | chevron rotate→instant, opacity retained, dur→instant | **Feedback** — chevron confirms the toggle registered; reveal shows where the items went. *Animate `opacity`, NOT `height`/`auto` (layout thrash — antipattern A2). The `hidden` attr stays the structural toggle.* |
| 5 | **global_search palette** (`global_search.ex`, `.ax-command-palette`) | Cmd-K open / Esc/backdrop close (`is_open`) | panel `opacity` 0→1 + `transform` scale `0.98`→1; backdrop `opacity` | enter: `transform --ax-dur-2 --ax-ease-emphasis, opacity --ax-dur-2 --ax-ease-out` | exit: `opacity --ax-dur-exit --ax-ease-in` (no scale-out) | scale→none (`--ax-ease-emphasis`→`--ax-ease-out`, overshoot removed by override), opacity retained, dur→instant | **Feedback** — the subtle scale-in is the one earned `--ax-ease-emphasis` use; it announces the palette took focus. Scale is composited (perf-safe). |
| 5b | **palette backdrop** (`.ax-command-palette-backdrop`) | same | `opacity` 0→1 | `opacity --ax-dur-2 --ax-ease-out` | `opacity --ax-dur-exit --ax-ease-in` | opacity retained, dur→instant | **Affordance** — modal dimming. |
| 6 | **tabs active indicator** (`tabs.ex`, `.ax-tab` / `.ax-tab-active`) | active tab change (link nav) | `color` + `border-color` (underline indicator) via colors bundle | `--ax-transition-colors` | symmetric (colors bundle) | color change retained (no travel), dur→instant via bundle | **Feedback** — confirms selection. **Discretion resolved: crossfade the indicator color, do NOT slide it.** Tabs are link-based (full page nav between LiveViews) so a slide has no continuous element to animate — a color crossfade is the honest, functional choice. |
| 7 | **flash / toasts** (`flash_group.ex`, `.ax-flash`) | mount (server push) / dismiss | `opacity` 0→1 + `transform` (translateY `--ax-rise-sm`→0) | enter: `transform --ax-dur-2 --ax-ease-out, opacity --ax-dur-2 --ax-ease-out` | exit: `opacity --ax-dur-exit --ax-ease-in` | travel→0, opacity retained, dur→instant | **Feedback** — a new notice slides in so it's noticed; snappy fade-out so dismissal feels immediate. |
| 8 | **skeleton → content** (`data_table.ex`, `.ax-skeleton`) | data arrives (skeleton removed, rows mount) | content `opacity` 0→1 (crossfade in); skeleton shimmer already exists | `opacity --ax-dur-2 --ax-ease-out` on mounted content | n/a (skeleton removed by LiveView patch) | shimmer already `animation:none` + static (`app.css:2409`); content crossfade → opacity retained, dur→instant | **Continuity** — content fades in where the skeleton was, so the table doesn't "pop." Shimmer is the **only** sanctioned infinite loop (genuine loading; antipattern A4 exception). |
| 9 | **badge / state change** (`.ax-status-badge`, `.ax-badge`, nav attention badges from Phase 175) | status change / first-appear | `color` + `background` + `border` via colors bundle; first-appear adds `transform` scale `--ax-press-scale`→1 | `--ax-transition-colors`; first-appear: `--ax-transition-transform` (`--ax-ease-emphasis` optional, single pop) | symmetric (colors bundle) | scale→none (`--ax-press-scale`=1), color change retained, dur→instant | **Feedback** — a status that just changed (e.g. invoice → paid, dead-letter count appears) draws a glance without a layout shift. |

**Enter/exit asymmetry (Phase-174 D-16, deferred to here — now encoded):**
- **Enter** = gentle: `--ax-ease-out`, longer duration (`--ax-dur-2` / `--ax-dur-3`), may include travel/scale.
- **Exit** = snappy: `--ax-ease-in` + `--ax-dur-exit` (140ms), **fade-only — no travel/scale out**. Dismissal should feel immediate; an element leaving doesn't need to narrate where it goes.

**Mechanism (per CONTEXT):**
- **Prefer CSS transitions** keyed on `[open]` / `data-state` / `hidden` / `.ax-active` toggles — cheapest, declarative, matches how dropdown (`<details>`) and collapse already work.
- Use **`Phoenix.LiveView.JS` show/hide-with-transition** only for genuine mount/remove where no hook exists, **mirroring `step_up_auth_modal.ex`** (`JS.push_focus`/`focus_first`/`pop_focus` pattern). Pass transition class names that resolve to the tokens above — never inline durations in the `JS` call.

---

## Antipattern List (MOT-01) — grounded in Emil Kowalski

Source: **Emil Kowalski, "Great Animations"** — <https://emilkowal.ski/ui/great-animations>
(fetched 2026-06-04). This list is the spec's normative "do not" and the basis of the
enforcement guard below.

| ID | Antipattern | Rule for `accrue_admin` |
|----|-------------|-------------------------|
| A1 | `transition: all` | **Banned.** Name the exact properties (or use an `--ax-transition-*` bundle). Animating "all" animates layout props by accident and kills perf. |
| A2 | Animating layout-triggering properties (`height:auto`, `width`, `margin`, `padding`, `top/left`) | **Banned for motion.** Animate only `transform` and `opacity` (composited, 60fps). Reveal/collapse uses `opacity` + the structural `hidden`/`[open]` toggle, never animated `height`. |
| A3 | Raw duration / curve literals (`200ms`, `0.3s`, `cubic-bezier(...)`) in `app.css` | **Banned.** Compose from `--ax-dur-*` / `--ax-ease-*` atoms only. Genuine token gaps get flagged, not literal-ized. |
| A4 | Decorative / infinite animation (spinners, auto-loops, parallax) | **Banned** except genuine loading. The skeleton shimmer (`ax-skeleton-shimmer`) and `.ax-spinner` (active fetch only) are the **sole** sanctioned loops. |
| A5 | Durations outside 150–300ms | **Banned.** Enter ∈ {`--ax-dur-2` 180ms, `--ax-dur-3` 240ms}; exit `--ax-dur-exit` 140ms (sub-150 is *intentional* for snappy dismissal — the only allowed exception, and only for exits). Micro press/hover `--ax-dur-1` 120ms is feedback, not entrance. |
| A6 | Ignoring `prefers-reduced-motion` | **Banned.** All motion routes through the bundles so the `theme.css:187` override collapses it. No motion may be authored that bypasses the bundle override. |
| A7 | Animating frequently-repeated / keyboard-initiated actions | **Banned.** Cmd-K → palette uses one subtle scale-in, not a heavy entrance; nav between tabs is a color crossfade, not a slide. Power-user paths stay fast. |
| A8 | Decorative motion ("looks nicer") | **Inadmissible.** Every animation in the table above cites continuity / feedback / affordance. The `motion.md` entry **is** the justification record; no entry → no animation. |

---

## Enforcement Guard (MOT-01)

Extend the existing token-bypass guard pattern into a motion guard over `app.css`
(and `theme.css` consumer rules — atom *definitions* in `theme.css` are exempt):

**Banned patterns (grep, fail the build):**
1. `transition: all` / `transition:all` (A1)
2. Raw `cubic-bezier(` outside `theme.css` token definitions (A3)
3. Raw `ms`/`s` duration literals in `transition:` / `animation:` declarations outside `theme.css` definitions (A3) — e.g. `transition: ... 200ms`, except the sanctioned `ax-skeleton-shimmer 1.4s` keyframe driver (allowlisted by name).
4. Animated layout properties in `transition:` lists (`height`, `width`, `margin`, `padding`, `top`, `left`, `right`, `bottom`) (A2).

> ⚠️ **Known coupling (MEMORY: verify_package_docs ↔ test):** a new guard needle must be
> added to **BOTH** the guard script **AND** its negative-test seed fixture, or the negative
> tests fail. Add the motion needles to both sides in the same change.

**Allowlist:** `ax-skeleton-shimmer` (A4 loading exception); the asymmetric two-token
transition already documented at `app.css:1562` (Phase-174 registered exception).

---

## Reduced-Motion & Verification (MOT-03)

| Aspect | Contract |
|--------|----------|
| Mechanism | Honor the existing token-level override (`theme.css:187`): bundles collapse to `--ax-dur-instant`; `--ax-rise-*`→0; `--ax-press-scale`→1; `--ax-ease-emphasis`→`--ax-ease-out`. **Travel + overshoot + scale removed; opacity crossfades retained** (vestibular issue is movement, not opacity). |
| Routing rule | ALL new motion rides the `--ax-transition-*` bundles → reduced-motion correctness is free. No surface may animate via inline durations. |
| Automated check 1 | **Extend the Phase-174 D-15 test** (`e2e/reduced-motion.spec.js`) — currently asserts `.ax-button` bundle collapse — to assert the newly-animated surfaces (drawer, dropdown, command palette) also collapse to `0s` on every transition segment under `prefers-reduced-motion: reduce`, with a matching non-reduced "proves the override is the cause" assertion (mirror the existing two-test structure). |
| Automated check 2 | **Playwright structural check** emulating `prefers-reduced-motion: reduce`: open a drawer/dropdown and assert **no transform travel** (computed `transform` shows no translate, or transition-duration is `0s`). Structural, not pixel-diff. |
| Phase-179 handoff | Motion needs trace/video review (static PNGs can't see it). Phase 179 (F) runs the Playwright trace/video pass. **This phase's proof = the automated reduced-motion checks + the documented `motion.md` spec.** |

---

## Deliverables (where the spec lives)

| Deliverable | Location |
|-------------|----------|
| Authoritative motion catalog | `accrue_admin/guides/motion.md` (the per-surface table above + antipatterns + justifications) |
| Live motion reference | a motion section in `/dev/components` (`component_kitchen_live.ex`) |
| Guard | motion needles added to the token-bypass guard **and** its negative-test seed fixture |
| Reduced-motion proof | extended `e2e/reduced-motion.spec.js` + new structural Playwright assertion |

**Build/verify (per CONTEXT §Specific Ideas):**
`cd accrue_admin && mix accrue_admin.assets.build` (commit `priv/static`) →
`cd accrue_admin && mix test --seed 0` → reduced-motion check. 252 tests currently green — do not regress.

---

## Token Gaps (flagged, NOT invented)

Per CONTEXT discretion ("whether to introduce an `--ax-dur-exit` consumption pattern or
reuse the existing exit atom"): **reuse the existing atoms — no new tokens needed.**

- `--ax-dur-exit` (140ms) + `--ax-ease-in` already exist → exit asymmetry is fully expressible. ✅
- `--ax-rise-sm/md`, `--ax-press-scale`, `--ax-ease-emphasis` cover all travel/scale/overshoot. ✅
- **No genuine gap found.** If implementation surfaces one (e.g. a distinct "exit transform" bundle is wanted rather than per-rule fade-only exits), **flag it to the planner** and add the atom to `theme.css` — do not write a literal in `app.css`.

---

## Frozen Visual Tokens (reproduced for completeness — NOT this phase's work)

These satisfy the template's required sections; they are inherited and frozen from Phase 167/174.

### Spacing (`theme.css`, 4px base)
`--ax-space-2xs` 2px · `xs` 4 · `sm` 8 · `md` 16 · `lg` 24 · `xl` 32 · `2xl` 48 · `3xl` 64.
Exceptions: 2px rung (`2xs`) for dense tables — registered Phase 167.

### Typography
| Role | Token | Weight | Line height |
|------|-------|--------|-------------|
| Body | `--ax-type-md` 16px | 400 | `--ax-leading-normal` 1.4 |
| Label/eyebrow | `--ax-type-sm` 14px | 600 | 1.4 |
| Heading | `--ax-type-xl` 24px | 600 | `--ax-leading-tight` 1.2 |
| Display | `--ax-type-3xl` 36px | 600 | 1.2 |

### Color (theme-aware via `data-theme`)
| Role | Token | Usage |
|------|-------|-------|
| Dominant (60%) | `--ax-base` | page background, surfaces |
| Secondary (30%) | `--ax-elevated` / `--ax-sunken` | cards, sidebar, nav, panels |
| Accent (10%) | `--ax-accent` / `--ax-accent-strong` | primary buttons, active tab/nav, command-palette active item, focus ring — **interactive emphasis only** |
| Destructive | `--ax-danger` / `--ax-danger-readable` | destructive actions, dunning/error flash, dead-letter badges |

Accent reserved for: primary action buttons, the active nav/tab indicator, the active
command-palette item, and the focus ring. **Not** for decorative fills or every hover.

### Copywriting (this phase ships NO new copy)
Motion is non-textual. No new CTA / empty-state / error / destructive copy is introduced
in Phase 177. Existing copy (e.g. command-palette verb prompts "Look up a customer", flash
titles "Action required" / "Notice", "Nothing needs attention" empty state) is frozen.
N/A items are intentional, not omissions.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no shadcn / third-party registries; all component source in-repo |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (N/A — no new copy; existing frozen copy noted)
- [ ] Dimension 2 Visuals (Motion): PASS — every surface maps trigger → property → token → enter/exit → reduced-motion → justification
- [ ] Dimension 3 Color: PASS (frozen tokens reproduced; accent reserved-for listed)
- [ ] Dimension 4 Typography: PASS (frozen tokens reproduced)
- [ ] Dimension 5 Spacing: PASS (frozen 4px scale reproduced)
- [ ] Dimension 6 Registry Safety: PASS (N/A — no registries)

**Motion-specific gates (for the checker / ui-auditor):**
- [ ] All 9 surfaces route through `--ax-transition-*` bundles (no inline ms/curve)
- [ ] Enter/exit asymmetry encoded (`--ax-ease-out` enter / `--ax-dur-exit`+`--ax-ease-in` exit)
- [ ] Antipattern guard wired into BOTH guard script AND negative-test seed fixture
- [ ] Reduced-motion check extended to drawer/dropdown/palette
- [ ] Every animation cites a functional justification (no decorative motion)
- [ ] No new motion tokens invented; gaps flagged not literal-ized

**Approval:** pending

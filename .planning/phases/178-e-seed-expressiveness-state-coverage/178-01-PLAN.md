---
phase: 178-e-seed-expressiveness-state-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md
  - accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
autonomous: true
requirements:
  - SEED-01
  - SEED-02

must_haves:
  truths:
    - "STATE-MATRIX.md enumerates all 21 screens × 8 state dimensions with fixture + click-path per non-N/A cell"
    - "Every gap cell (marked NEEDED in RESEARCH.md audit) maps to a named fixture or a documented no-fixture mechanism"
    - "e2e_fixtures_test.exs compiles and all tests are RED (fixtures not yet implemented)"
  artifacts:
    - path: ".planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md"
      provides: "Screen×state contract consumed by Phase 179 Playwright sweep"
      contains: "21 rows (one per screen), 8 state dimension columns, fixture+click-path in every cell"
    - path: "accrue_admin/test/accrue_admin/e2e_fixtures_test.exs"
      provides: "Nyquist Wave-0 test scaffold for SEED-01 and SEED-02"
      exports: []
  key_links:
    - from: "STATE-MATRIX.md"
      to: "Phase 179 Playwright sweep"
      via: "fixture names in 'fixture' column drive POST /__e2e__/seed/<name> calls"
      pattern: "seed_edge_states|seed_overflow|operator-flows|dashboard"
---

<objective>
Produce the two Wave-0 deliverables that all subsequent plans depend on: (1) STATE-MATRIX.md — the screen×state QA contract enumerating every admin screen's reachable states with fixture names and click-paths; and (2) the e2e_fixtures_test.exs scaffold whose tests must be RED because the fixtures they exercise do not yet exist.

Purpose: The matrix is the anti-entropy contract — every subsequent plan closes a cell. The test scaffold enforces Nyquist compliance; RED tests now become GREEN in Plan 02.
Output: STATE-MATRIX.md (phase dir), accrue_admin/test/accrue_admin/e2e_fixtures_test.exs (failing).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-CONTEXT.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-RESEARCH.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- From accrue_admin/test/support/e2e_fixtures.ex — existing fixture functions -->
def seed_dashboard!() :: %{customer_id: id, subscription_id: id, event_id: id}
def seed_operator_flows!() :: %{charge_id: id, source_event_id: id, single_webhook_id: id, bulk_webhook_id: id}

<!-- From accrue_admin/test/support/e2e_plug.ex — existing routes -->
POST /__e2e__/seed/dashboard       -> 200, JSON map
POST /__e2e__/seed/operator-flows  -> 200, JSON map

<!-- Existing test file patterns (from customers_live_test.exs): -->
use AccrueAdmin.LiveCase, async: false
# Tests use Factory + TestRepo.insert! directly; e2e_fixtures tests must use AccrueAdmin.E2E.Fixtures
# module directly (not via HTTP) to remain unit-style
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write STATE-MATRIX.md — 21 screens × 8 state dimensions</name>
  <files>.planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md</files>
  <behavior>
    - Matrix has 21 screen rows matching the Phase 176 SCORECARD inventory
    - Columns: Screen | Empty | Populated | Overflow/Pagination | Error (filtered-empty) | Loading (poll-banner) | Dunning/At-risk | Multi-currency | Long-strings | Dark-contrast
    - Each cell contains one of: fixture name + click-path, "N/A", or "no-fixture: [mechanism]"
    - "no-fixture" cells must document the reach mechanism (e.g., "no-fixture: navigate /billing/invoices?status=nonexistent_status" for error; "no-fixture: double-seed + 5s wait" for loading)
    - Cells covered by existing fixtures (operator-flows, dashboard) are already FILLED
    - Cells that will be filled by seed_edge_states! (Wave 2 Plan 02) are marked "seed_edge_states"
    - Cells that will be filled by seed_overflow! are marked "seed_overflow"
    - Cells for host-seed edge_states.exs (Plan 04) are marked "host:edge_states"
    - Every cell is filled — no blank cells, no "TBD"
  </behavior>
  <action>
    Create .planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md with a Markdown table.

    Header row: | Screen | Empty | Populated | Overflow | Error | Loading | Dunning/At-risk | Multi-currency | Long-strings | Dark-contrast |

    21 screen rows (from Phase 176 SCORECARD — list and detail screens):

    List screens (9): CustomersLive, SubscriptionsLive, InvoicesLive, ChargesLive, CouponsLive, PromotionCodesLive, ConnectAccountsLive, EventsLive, WebhooksLive

    Detail screens (10): CustomerLive, SubscriptionLive, InvoiceLive, ChargeLive, CouponLive, PromotionCodeLive, ConnectAccountLive, EventLive, WebhookLive, CampaignLive

    Specialist screens (2): RecoveryLive, DashboardLive

    Cell rules (follow RESEARCH.md audit exactly):

    EMPTY column:
    - List screens: "no-fixture: apply impossible filter e.g. ?status=nonexistent" OR "no-fixture: empty DB (pre-seed state)" as appropriate; for work-queue lists (SubscriptionsLive, InvoicesLive) "no-fixture: work-queue defaults show 0 if no past_due/open rows" 
    - Detail screens: "no-fixture: navigate /billing/customers/nonexistent-id → redirect-nil"
    - DashboardLive, RecoveryLive: "N/A (always renders)"

    POPULATED column:
    - CustomersLive, SubscriptionLive (customer detail from), ChargeLive, EventLive, WebhookLive, WebhooksLive, ChargesLive, EventsLive: "operator-flows — navigate /billing/charges (for ChargesLive)"
    - InvoicesLive, InvoiceLive: "dashboard — navigate /billing/invoices"
    - CouponsLive, CouponLive, PromotionCodesLive, PromotionCodeLive: "host:showcase.exs — navigate /billing/coupons"
    - ConnectAccountsLive, ConnectAccountLive: "seed_edge_states — navigate /billing/connect-accounts"
    - RecoveryLive, CampaignLive: "seed_edge_states — navigate /billing/analytics/recovery"
    - DashboardLive: "dashboard — navigate /"

    OVERFLOW column:
    - All 9 list screens: "seed_overflow — navigate /billing/<resource> → Load more button visible"
    - Detail screens: "N/A"
    - Specialist screens: "N/A"

    ERROR column:
    - All list screens: "no-fixture: navigate /billing/<resource>?status=nonexistent_status → empty-state renders"
    - Detail screens: "no-fixture: navigate /billing/<resource>/nonexistent-uuid → redirect"
    - RecoveryLive, DashboardLive: "N/A"

    LOADING column:
    - All 9 list screens: "no-fixture: POST /seed/operator-flows twice (no reset between) + wait 5s for poll_interval → newer_count banner appears"
    - Detail screens: "N/A"
    - Specialist screens: "N/A"

    DUNNING/AT-RISK column:
    - SubscriptionsLive: "seed_edge_states — navigate /billing/subscriptions (work-queue default shows past_due + canceling)"
    - SubscriptionLive: "seed_edge_states — navigate to at_risk_sub_id detail"
    - RecoveryLive: "seed_edge_states — navigate /billing/analytics/recovery → at-risk table non-empty"
    - CampaignLive: "seed_edge_states — navigate /billing/analytics/recovery/campaigns/:at_risk_sub_id"
    - DashboardLive: "seed_edge_states — sidebar badge shows Recovery count > 0"
    - All others: "N/A"

    MULTI-CURRENCY column:
    - InvoiceLive: "seed_edge_states — navigate to jpy_invoice_id → amount renders ¥55,000"
    - ChargeLive: "seed_edge_states — navigate to jpy_charge_id → amount renders ¥"
    - RecoveryLive: "seed_edge_states — JPY exhausted row in 30d window (host:hero_accounts.exs after dunning fix)"
    - All others: "N/A"

    LONG-STRINGS column:
    - CustomerLive: "seed_edge_states — navigate to long_name_customer_id → 120-char name truncates in header"
    - CustomersLive: "seed_edge_states — long_name_customer appears in list"
    - All others: "seed_edge_states where applicable (coupon/promo long name via host:edge_states.exs); N/A for analytics"

    DARK-CONTRAST column:
    - Screens with tinted status badges (SubscriptionLive, InvoiceLive, WebhookLive, DashboardLive, RecoveryLive): "seed_edge_states — :past_due status triggers ax-badge-danger / tinted chip"
    - All others: "N/A (no tinted status chips on this screen)"

    After the table, add a ## Legend section explaining each mechanism abbreviation.
    Add a ## Gap Closure Status section showing which plans close which cells.
  </action>
  <verify>
    <automated>
      MATRIX=.planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md
      test -f "$MATRIX" || { echo "MISSING: STATE-MATRIX.md"; exit 1; }
      ROW_COUNT=$(grep -c "^|" "$MATRIX" || true)
      [ "$ROW_COUNT" -ge 21 ] || { echo "FAIL: only $ROW_COUNT rows (need >=21)"; exit 1; }
      OVERFLOW_COUNT=$(grep -v '^#' "$MATRIX" | grep -o 'seed_overflow' | wc -l | tr -d ' ')
      [ "$OVERFLOW_COUNT" -eq 9 ] || { echo "FAIL: $OVERFLOW_COUNT seed_overflow cells (need exactly 9)"; exit 1; }
      BLANK_COUNT=$(grep -v '^#' "$MATRIX" | grep -cE '\|[[:space:]]*(TBD|)[[:space:]]*\|' || true)
      [ "$BLANK_COUNT" -eq 0 ] || { echo "FAIL: $BLANK_COUNT blank/TBD cells"; exit 1; }
      echo "PASS: $ROW_COUNT rows, 9 seed_overflow cells, 0 blank/TBD"
    </automated>
  </verify>
  <done>STATE-MATRIX.md exists, has 21 screen rows, every cell is filled (no blank cells), "seed_edge_states" appears in at least 8 cells, "seed_overflow" appears in 9 cells (one per list screen).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write e2e_fixtures_test.exs scaffold — RED tests for new fixtures</name>
  <files>accrue_admin/test/accrue_admin/e2e_fixtures_test.exs</files>
  <behavior>
    - Test "seed_edge_states!/0 inserts at-risk subscription with :past_due status and dunning_campaign_started_at set" — calls Fixtures.seed_edge_states!(), queries TestRepo for the at_risk_sub_id, asserts status == :past_due and dunning_campaign_started_at != nil
    - Test "seed_edge_states!/0 inserts canceling subscription (active + cancel_at_period_end: true)" — queries for canceling_sub_id, asserts status == :active and cancel_at_period_end == true and current_period_end > DateTime.utc_now()
    - Test "seed_edge_states!/0 inserts JPY invoice with currency jpy" — queries for jpy_invoice_id, asserts currency == "jpy" and total_minor == 55_000
    - Test "seed_edge_states!/0 inserts long-name customer" — queries for long_name_customer_id, asserts String.length(customer.name) > 100
    - Test "seed_overflow!/0 inserts at least 26 customers" — calls Fixtures.seed_overflow!(), counts Customer rows in TestRepo, asserts count >= 26
    - Test "seed_overflow!/0 inserts at least 26 subscriptions" — counts Subscription rows, asserts >= 26
    - Test "POST /__e2e__/seed/edge-states returns 200" — uses ConnCase or direct plug test, posts to /__e2e__/seed/edge-states, asserts status 200
    - Test "POST /__e2e__/seed/overflow returns 200" — same for /overflow
    - All tests use AccrueAdmin.LiveCase or AccrueAdmin.ConnCase (async: false) and call Fixtures.reset!() in setup
    - All tests FAIL because seed_edge_states!/0 and seed_overflow!/0 do not yet exist — this is correct (Wave 0 RED)
  </behavior>
  <action>
    Create accrue_admin/test/accrue_admin/e2e_fixtures_test.exs.

    Module: AccrueAdmin.E2EFixturesTest
    Use: AccrueAdmin.ConnCase, async: false (ConnCase provides the DB sandbox and conn)

    Aliases needed: Accrue.Billing.{Customer, Invoice, Subscription}, AccrueAdmin.E2E.Fixtures, AccrueAdmin.TestRepo

    setup block: call Fixtures.reset!() to truncate all fixture tables before each test.

    For the HTTP POST tests (POST /__e2e__/seed/edge-states and /overflow):
    - Use the test Router that mounts E2E.Plug — check how existing e2e_plug tests work in the test suite; if no existing plug integration test exists, use Plug.Test.conn(:post, "/__e2e__/seed/edge-states") |> AccrueAdmin.E2E.Plug.call([]) and assert conn.status == 200.

    For the unit tests (seed_edge_states!, seed_overflow!):
    - Call Fixtures.seed_edge_states!() directly, bind the returned map, then query TestRepo to assert field values.
    - Example for at-risk: result = Fixtures.seed_edge_states!(); sub = TestRepo.get!(Subscription, result.at_risk_sub_id); assert sub.status == :past_due; assert sub.dunning_campaign_started_at != nil

    Include @moduletag :phase178 on the describe/module for easy scoped running.

    The file MUST compile but ALL 8 tests must fail with UndefinedFunctionError (seed_edge_states!/0 is not yet defined) — this proves the RED phase.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs --seed 0 2>&1 | grep -E "8 tests|UndefinedFunctionError|seed_edge_states"</automated>
  </verify>
  <done>e2e_fixtures_test.exs compiles without syntax errors. Running the test file produces exactly 8 test failures (all UndefinedFunctionError for seed_edge_states!/0 and seed_overflow!/0). Zero compilation errors in the admin suite (cd accrue_admin && mix compile --warnings-as-errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifact → executor | STATE-MATRIX.md is a planning doc, not served over HTTP; no attack surface |
| Test scaffold → DB | e2e_fixtures_test.exs runs in ExUnit sandbox; Ecto sandbox isolates transactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-01-SC | Tampering | No new packages installed in this plan | accept | No package installs; no slopcheck needed |
| T-178-01-I | Information Disclosure | STATE-MATRIX.md committed to git | accept | Planning artifact; no PII or secrets; documents fixture names and URL paths only |
| T-178-01-A | Spoofing | e2e_fixtures_test.exs calls AccrueAdmin.E2E.Plug directly | accept | Test-only code in test/; ExUnit sandbox; plug only mounted in test/dev environments (no prod exposure) |

The /__e2e__/seed/* endpoint is served exclusively by AccrueAdmin.E2E.Plug, which is wired in test/support/ and mounted only under Mix.env() == :test. This plan adds no new routes (routes are added in Plan 02). Reaffirming: the seed endpoint is NOT mounted in production.
</threat_model>

<verification>
- STATE-MATRIX.md present in phase dir with 21 rows and all cells filled
- e2e_fixtures_test.exs compiles, produces 8 RED failures (UndefinedFunctionError)
- cd accrue_admin && mix compile --warnings-as-errors passes
- Existing 254 admin tests still green: cd accrue_admin && mix test --seed 0
</verification>

<success_criteria>
STATE-MATRIX.md documents every screen×state cell with fixture name or no-fixture mechanism. e2e_fixtures_test.exs is a valid Elixir file with 8 failing tests ready to go GREEN in Plan 02. No regressions in the 254-test suite.
</success_criteria>

<output>
Create .planning/phases/178-e-seed-expressiveness-state-coverage/178-01-SUMMARY.md when done.
</output>

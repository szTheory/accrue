---
phase: 178-e-seed-expressiveness-state-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 178-01
files_modified:
  - accrue_admin/test/support/e2e_fixtures.ex
  - accrue_admin/test/support/e2e_plug.ex
autonomous: true
requirements:
  - SEED-01
  - SEED-02

must_haves:
  truths:
    - "POST /__e2e__/seed/edge-states returns 200 with JSON map containing at_risk_sub_id, canceling_sub_id, jpy_invoice_id, jpy_charge_id, long_name_customer_id, coupon_id, promo_code_id, connect_account_id"
    - "POST /__e2e__/seed/overflow returns 200 with JSON map containing first_customer_id"
    - "seed_edge_states! inserts a :past_due subscription with dunning_campaign_started_at set — at_risk table non-empty"
    - "seed_edge_states! inserts an :active + cancel_at_period_end: true subscription — canceling work queue non-empty"
    - "seed_edge_states! inserts an invoice with currency: jpy and total_minor: 55_000"
    - "seed_overflow! inserts ≥26 customers and ≥26 subscriptions — DataTable Load-more button reachable"
    - "All 8 tests in e2e_fixtures_test.exs pass GREEN"
    - "Existing operator-flows and dashboard fixtures still work (no regression)"
  artifacts:
    - path: "accrue_admin/test/support/e2e_fixtures.ex"
      provides: "seed_edge_states!/0 and seed_overflow!/0 fixture functions + insert_connect_account/2 helper"
      exports: ["seed_edge_states!/0", "seed_overflow!/0"]
    - path: "accrue_admin/test/support/e2e_plug.ex"
      provides: "POST /seed/edge-states and POST /seed/overflow routes (before catch-all)"
      contains: "post \"/seed/edge-states\""
  key_links:
    - from: "accrue_admin/test/support/e2e_plug.ex"
      to: "AccrueAdmin.E2E.Fixtures.seed_edge_states!/0"
      via: "json(conn, 200, Fixtures.seed_edge_states!())"
      pattern: "seed_edge_states"
    - from: "seed_edge_states!"
      to: "Subscription.force_status_changeset/2"
      via: "at-risk sub must bypass transition guards to reach :past_due"
      pattern: "force_status_changeset"
---

<objective>
Implement seed_edge_states!/0 and seed_overflow!/0 in e2e_fixtures.ex, wire both as named routes in e2e_plug.ex, and turn the 8 RED tests from Plan 01 GREEN. This makes the following STATE-MATRIX cells reachable: dunning/at-risk (SubscriptionsLive, SubscriptionLive, RecoveryLive, CampaignLive, DashboardLive badge), multi-currency JPY (InvoiceLive, ChargeLive), long-strings (CustomerLive, CustomersLive), and overflow/pagination on all 9 list screens.

Purpose: Provides the concrete E2E seed layer the automated QA loop (Phase 179 Playwright sweep) iterates over for all non-host edge states.
Output: Extended e2e_fixtures.ex with two new public functions + one new private helper; extended e2e_plug.ex with two new routes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-CONTEXT.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-RESEARCH.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/178-PATTERNS.md
@.planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md
@accrue_admin/test/support/e2e_fixtures.ex
@accrue_admin/test/support/e2e_plug.ex
@accrue_admin/test/accrue_admin/e2e_fixtures_test.exs

<interfaces>
<!-- From accrue_admin/test/support/e2e_fixtures.ex (current file — read before editing) -->
defmodule AccrueAdmin.E2E.Fixtures do
  alias Accrue.Billing.{Charge, Customer, Invoice, Refund, Subscription}
  alias Accrue.Events
  alias Accrue.Webhook.WebhookEvent
  alias AccrueAdmin.TestRepo

  # Existing public functions:
  def reset!()           -- TRUNCATE all tables + Fake.reset() + put_operation_id
  def seed_dashboard!()  -- returns %{customer_id, subscription_id, event_id}
  def seed_operator_flows!() -- returns %{charge_id, source_event_id, single_webhook_id, bulk_webhook_id}
  def current_counts()   -- returns %{webhook_replayed, admin_events}

  # Private insert helpers (patterns to follow exactly):
  defp insert_customer(attrs)        -- Map.merge(defaults, attrs) |> Customer.changeset |> TestRepo.insert!
  defp insert_subscription(customer, attrs) -- defaults include cancel_at_period_end: false, lock_version: 1
  defp insert_invoice(customer, subscription, attrs) -- defaults include currency: "usd"
  defp insert_charge(customer, subscription, attrs)  -- defaults include lock_version: 1
  defp insert_refund(charge, attrs)
  defp insert_webhook(attrs)         -- must delete :status then put_change(:status, ...) after ingest_changeset

<!-- From accrue_admin/test/support/e2e_plug.ex (current file) -->
post "/seed/dashboard" do
  json(conn, 200, Fixtures.seed_dashboard!())
end

post "/seed/operator-flows" do
  json(conn, 200, Fixtures.seed_operator_flows!())
end
# catch-all is BELOW -- new routes must go ABOVE match _

<!-- Subscription.force_status_changeset/2 — used to bypass transition guards for :past_due -->
# Pattern from hero_accounts.exs and PATTERNS.md:
%Subscription{} |> Subscription.force_status_changeset(attrs) |> TestRepo.insert!()

<!-- Accrue.Connect.Account — new alias needed in e2e_fixtures.ex -->
alias Accrue.Connect.Account
# changeset function: Account.changeset(%Account{}, attrs) |> TestRepo.insert!()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seed_edge_states!/0 to e2e_fixtures.ex + insert_connect_account/2 helper</name>
  <files>accrue_admin/test/support/e2e_fixtures.ex</files>
  <behavior>
    - seed_edge_states!/0 returns a map with keys: at_risk_sub_id, canceling_sub_id, jpy_invoice_id, jpy_charge_id, dunning_customer_id, long_name_customer_id, coupon_id, promo_code_id, connect_account_id
    - at_risk_sub: status :past_due + dunning_campaign_started_at set 5 days ago — inserted via Subscription.force_status_changeset/2 (not changeset/2, per D-03 PATTERNS.md; bypasses transition guards)
    - canceling_sub: status :active + cancel_at_period_end: true + current_period_end 7 days in future — inserted via standard insert_subscription/2 (no force needed for :active)
    - jpy_invoice: currency "jpy", total_minor 55_000, amount_due_minor 55_000, amount_remaining_minor 55_000, status :open — inserted via insert_invoice/3 overriding currency default
    - jpy_charge: currency "jpy", amount_cents 55_000, status "succeeded" — inserted via insert_charge/3
    - long_name_customer: name is String.duplicate("A", 100) &lt;&gt; " LongNameCo" (111 chars), email "long-name-e2e@example.com"
    - coupon: inserted via new insert_coupon/1 helper (or direct Accrue.Billing.Coupon.changeset); processor_id "coupon_e2e_edge"
    - promo_code: inserted via Accrue.Billing.PromotionCode.changeset; processor_id "promo_e2e_edge"; references coupon
    - connect_account: inserted via new insert_connect_account/2 helper; stripe_account_id "acct_e2e_edge", type "standard"
    - All entities under the dunning_customer owner_id; long_name_customer gets its own owner_id
    - Add alias Accrue.Connect.Account + Accrue.Billing.{Coupon, PromotionCode} to module header
    - insert_connect_account/2 private helper follows same Map.merge(defaults, attrs) pattern; defaults: stripe_account_id generated via unique_integer, type: "standard", owner_type: "User", email: "connect-e2e@example.com", country: "us", charges_enabled: true, payouts_enabled: true, details_submitted: true, capabilities: %{}, requirements: %{}, data: %{}
  </behavior>
  <action>
    Read accrue_admin/test/support/e2e_fixtures.ex first (already in context above — do NOT re-read). Append seed_edge_states!/0 as a new public function after seed_operator_flows!/0. Append insert_connect_account/2 as a new private helper after insert_webhook/1.

    For the at-risk subscription:
    - Build the attrs map: %{customer_id: customer.id, processor: "fake", processor_id: "sub_e2e_dunning_at_risk", status: :past_due, past_due_since: DateTime.add(DateTime.utc_now(), -5 * 86_400, :second), dunning_campaign_started_at: DateTime.add(DateTime.utc_now(), -5 * 86_400, :second), cancel_at_period_end: false, lock_version: 1, metadata: %{}, data: %{}}
    - Use Subscription.force_status_changeset(attrs) (not changeset/2) — this is MANDATORY per PATTERNS.md to bypass transition guards when setting :past_due directly

    For the canceling subscription:
    - Use insert_subscription(customer, %{processor_id: "sub_e2e_canceling", status: :active, cancel_at_period_end: true, current_period_end: DateTime.add(DateTime.utc_now(), 7 * 86_400, :second)})
    - standard changeset/2 is fine for :active status

    For JPY invoice — use insert_invoice(customer, at_risk_sub, %{processor_id: "in_e2e_jpy", currency: "jpy", total_minor: 55_000, amount_due_minor: 55_000, amount_remaining_minor: 55_000, status: :open, number: "E2E-JPY-001"})

    For JPY charge — use insert_charge(customer, at_risk_sub, %{processor_id: "ch_e2e_jpy", currency: "jpy", amount_cents: 55_000, status: "succeeded"})

    For coupon + promo_code: if Accrue.Billing.Coupon/PromotionCode changeset functions are available, use them; otherwise use Ecto.Changeset.change directly. Coupon fields: name "E2E Edge Coupon", duration :once, percent_off: Decimal.new("10.0") (or integer 10 depending on schema), currency: "usd". PromotionCode fields: code "E2E-EDGE", coupon_id: coupon.id, active: true, processor_id: "promo_e2e_edge".

    If Coupon/PromotionCode helpers do not exist in the private section, add insert_coupon/1 and insert_promo_code/2 following the same Map.merge(defaults, attrs) |> Changeset |> TestRepo.insert! pattern.

    Return map: %{at_risk_sub_id: at_risk_sub.id, canceling_sub_id: canceling_sub.id, jpy_invoice_id: jpy_invoice.id, jpy_charge_id: jpy_charge.id, dunning_customer_id: customer.id, long_name_customer_id: long_name_customer.id, coupon_id: coupon.id, promo_code_id: promo_code.id, connect_account_id: connect_account.id}
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs --seed 0 -t "seed_edge_states" 2>&1 | grep -E "passed|failed|error"</automated>
  </verify>
  <done>seed_edge_states!/0 tests in e2e_fixtures_test.exs all pass. mix compile --warnings-as-errors clean. cd accrue_admin && mix test --seed 0 still green (no regressions in existing 254 tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add seed_overflow!/0 to e2e_fixtures.ex + wire both routes in e2e_plug.ex</name>
  <files>accrue_admin/test/support/e2e_fixtures.ex, accrue_admin/test/support/e2e_plug.ex</files>
  <behavior>
    - seed_overflow!/0 inserts 26 customers (processor_ids "cus_e2e_overflow_1" through "cus_e2e_overflow_26"), 26 subscriptions (processor_ids "sub_e2e_overflow_1" through "sub_e2e_overflow_26"), 26 invoices, 26 charges, 26 webhook events (processor_event_ids "evt_e2e_overflow_1" through "evt_e2e_overflow_26"), 26 events
    - Returns %{first_customer_id: ..., first_invoice_id: ..., first_charge_id: ...}
    - 26 rows exceeds DataTable @default_limit = 25 — triggers the Load-more button on all 9 list screens
    - e2e_plug.ex adds POST /seed/edge-states and POST /seed/overflow routes BEFORE the match _ catch-all (line 45 in the current file)
    - Existing routes POST /seed/dashboard and POST /seed/operator-flows are UNCHANGED
  </behavior>
  <action>
    In e2e_fixtures.ex, append seed_overflow!/0 after seed_edge_states!/0:

    Use Enum.map(1..26, fn i -> insert_customer(%{name: "E2E Overflow Customer #{i}", email: "overflow-e2e-#{i}@example.com", processor_id: "cus_e2e_overflow_#{i}"}) end) to build customers list.

    Then Enum.each(customers, fn customer -> insert_subscription(customer, %{processor_id: "sub_e2e_overflow_#{customer.processor_id}", status: :active}) end).

    Create 26 invoices: one per customer, processor_ids "in_e2e_overflow_#{i}", each attached to the matching subscription. Create 26 charges: processor_ids "ch_e2e_overflow_#{i}". Create 26 webhooks: processor_event_ids "evt_e2e_overflow_#{i}", type "invoice.payment_created", status :received. Create 26 events via Events.record/1 with type "customer.created", subject_type "Customer", subject_id customer.id for each customer.

    first_customer = List.first(customers)
    first_invoice = first inserted invoice
    first_charge = first inserted charge
    Return: %{first_customer_id: first_customer.id, first_invoice_id: first_invoice.id, first_charge_id: first_charge.id}

    In e2e_plug.ex, insert the two new routes between post "/seed/operator-flows" and match _:

    post "/seed/edge-states" do
      json(conn, 200, Fixtures.seed_edge_states!())
    end

    post "/seed/overflow" do
      json(conn, 200, Fixtures.seed_overflow!())
    end

    The routes use kebab-case names matching the Plug.Router convention (operator-flows uses hyphens, not underscores). The function names use snake_case (seed_edge_states!, seed_overflow!).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs --seed 0 2>&1 | grep -E "8 tests|passed|0 failures"</automated>
  </verify>
  <done>All 8 tests in e2e_fixtures_test.exs pass (0 failures). POST /seed/edge-states and POST /seed/overflow return 200. cd accrue_admin && mix test --seed 0 shows 254+ tests green (8 new tests added, none regressed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /__e2e__/seed/edge-states | Test-only endpoint; AccrueAdmin.E2E.Plug is only mounted in Mix.env() == :test |
| Fixture DB writes | All writes go through TestRepo (Ecto sandbox); isolated per test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-02-SC | Tampering | No new packages installed in this plan | accept | No package installs; no slopcheck needed |
| T-178-02-E | Elevation | POST /seed/edge-states inserts arbitrary DB rows | accept | E2E.Plug is in test/support/ — never compiled into prod release; Mix.env() gating confirmed (file location is accrue_admin/test/support/) |
| T-178-02-I | Information Disclosure | seed_edge_states! uses hardcoded processor_ids | accept | processor_ids are fixture strings like "sub_e2e_dunning_at_risk"; no PII, no secrets |

Reaffirming: the /__e2e__/seed/* endpoint is NOT mounted in production. It lives in accrue_admin/test/support/e2e_plug.ex which is only compiled when Mix.env() == :test. No new gating required beyond what already exists.
</threat_model>

<verification>
- cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs --seed 0 → 8 tests, 0 failures
- cd accrue_admin && mix test --seed 0 → 254+ tests, 0 failures
- grep "seed/edge-states" accrue_admin/test/support/e2e_plug.ex → route present
- grep "seed/overflow" accrue_admin/test/support/e2e_plug.ex → route present
- grep "force_status_changeset" accrue_admin/test/support/e2e_fixtures.ex → at-risk sub uses correct changeset function
</verification>

<success_criteria>
seed_edge_states!/0 and seed_overflow!/0 are implemented and tested. POST /seed/edge-states and POST /seed/overflow return 200. The 8 Wave-0 tests are GREEN. The existing operator-flows and dashboard fixtures are unaffected. No regressions.
</success_criteria>

<output>
Create .planning/phases/178-e-seed-expressiveness-state-coverage/178-02-SUMMARY.md when done.
</output>

---
phase: 179-f-screenshot-driven-visual-qa-loop-sign-off
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/e2e/score-visuals.mjs
  - accrue_admin/e2e/admin-motion-trace.spec.js
autonomous: true
requirements: [QA-02, QA-03]
must_haves:
  truths:
    - "node e2e/score-visuals.mjs exits 0 with a clear skip message when ANTHROPIC_API_KEY is not set"
    - "score-visuals.mjs emits one findings object per PNG per dimension scored — schema: {screen, viewport, theme, dimension, dimension_name, score, defect, suggested_fix}"
    - "score-visuals.mjs skips PNGs whose base64 encoding exceeds 5 MB with a warning (not an error)"
    - "score-visuals.mjs uses claude-sonnet-4-5 by default, overrideable via SCORE_MODEL env var"
    - "admin-motion-trace.spec.js has test.use({ trace: 'on' }) scoped to this file only"
    - "Motion trace spec covers 4 surfaces: webhook-detail replay (#search-trigger command palette), dropdown (details.ax-dropdown summary), nav-collapse ([data-collapse-toggle='true'])"
    - "npx playwright test e2e/admin-motion-trace.spec.js --list shows 4 tests"
  artifacts:
    - path: "accrue_admin/e2e/score-visuals.mjs"
      provides: "LLM vision-scoring Node CLI"
      contains: "ANTHROPIC_API_KEY"
    - path: "accrue_admin/e2e/admin-motion-trace.spec.js"
      provides: "Playwright motion trace capture"
      contains: "trace: \"on\""
  key_links:
    - from: "score-visuals.mjs"
      to: "Anthropic messages API"
      via: "@anthropic-ai/sdk client.messages.create with image blocks"
      pattern: "client.messages.create"
    - from: "admin-motion-trace.spec.js"
      to: "webhook-detail replay drawer"
      via: "data-role=\"replay-single\" phx-click trigger"
      pattern: "replay-single"
---

<objective>
Write score-visuals.mjs — the LLM vision-scoring Node CLI that reads captured PNGs, sends each to the Anthropic messages API with the 10-dimension rubric, and emits structured NDJSON findings. Write admin-motion-trace.spec.js — the Playwright trace spec that captures animated motion surfaces for review. Both are build/tooling artifacts; neither requires a live server or API key to pass their autonomous verification gates.

Purpose: QA-02 — LLM-analysis step with structured findings schema {screen, viewport, theme, dimension, score, defect, suggested_fix}. QA-03 (motion) — Playwright trace capture for Phase 177 deferred motion UAT items (drawer/command palette/dropdown/nav-collapse).

Output: score-visuals.mjs (exits 0 without key, full scoring logic with key); admin-motion-trace.spec.js (4 motion surface tests with trace: "on").
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-RESEARCH.md

<interfaces>
<!-- score-visuals.mjs: ESM script (import, not require). No .mjs files exist in e2e/ yet — this is the first. -->
<!-- Use: import Anthropic from "@anthropic-ai/sdk"; import fs from "fs"; import path from "path"; import { fileURLToPath } from "url"; -->
<!-- __dirname shim: const __dirname = path.dirname(fileURLToPath(import.meta.url)); -->
<!-- RESULTS_DIR: path.join(__dirname, "../test-results/admin-visuals") — two project subdirs: chromium-desktop, chromium-mobile -->
<!-- PNG naming: <name>.png (light) and <name>-dark.png (dark) — derive theme from filename suffix -->
<!-- Viewport derives from subdirectory name (chromium-desktop vs chromium-mobile) -->
<!-- Screen name derives from PNG filename without -dark suffix and without .png extension -->

<!-- Findings schema (locked per CONTEXT.md D-01/QA-02): -->
<!-- { "screen": string, "viewport": string, "theme": "light"|"dark", -->
<!--   "dimension": 1..10, "dimension_name": string, "score": 0..3, -->
<!--   "defect": string|null, "suggested_fix": string|null } -->

<!-- Motion trace trigger selectors (verified from component templates in PATTERNS.md lines 203-212): -->
<!-- #search-trigger → phx-click="open" on command palette (topbar.ex) -->
<!-- details.ax-dropdown > summary → opens any dropdown disclosure -->
<!-- [data-collapse-toggle="true"] → sidebar group toggle (Recovery, Developer, or Catalog group) -->
<!-- data-role="replay-single" → webhook replay drawer trigger on webhook-detail page -->

<!-- admin-motion-trace.spec.js should copy reset/seed/login helpers verbatim from admin-visuals.spec.js -->
<!-- test.use({ trace: "on" }) must be FILE-scoped only — do NOT put it in playwright.config.js -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write score-visuals.mjs LLM scoring script</name>
  <files>accrue_admin/e2e/score-visuals.mjs</files>
  <read_first>
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md — lines 222-298 (full score-visuals.mjs pattern: ESM header, API key guard, model selection, image content block shape, findings schema, PNG discovery, large-image guard, npm script).
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-RESEARCH.md — Pattern 3 (LLM Scoring Script) and Pitfall 5 (non-zero exit without key) and Pitfall 3 (PNG base64 size limits).
  </read_first>
  <action>
    Create accrue_admin/e2e/score-visuals.mjs as an ESM module (Node 22, .mjs extension). The file must follow this structure:

    ESM imports: Anthropic from "@anthropic-ai/sdk"; fs, path from node builtins; fileURLToPath from "url". Declare __dirname via fileURLToPath shim.

    API key guard — the FIRST executable statement after imports: check process.env.ANTHROPIC_API_KEY. If absent, print "[score-visuals] ANTHROPIC_API_KEY not set — skipping (human/CI gate only)" and call process.exit(0). Only construct the Anthropic client after this guard passes.

    Model selection: const model = process.env.SCORE_MODEL || "claude-sonnet-4-5";

    RESULTS_DIR: path.join(__dirname, "../test-results/admin-visuals").

    RUBRIC_PROMPT constant: a string containing the scoring instruction and all 10 rubric dimensions with score criteria (0–3, pass ≥2). Include all 10 dimension names: token-compliance, visual-hierarchy, spacing-rhythm, state-coverage, responsive-mobile-first, contrast, focus-semantics, brand-expression, motion, reuse-dry. The prompt must ask the model to return a JSON array of findings objects matching the locked schema: {screen, viewport, theme, dimension (1-10), dimension_name, score (0-3), defect (string or null), suggested_fix (string or null)}.

    PNG discovery: use fs.readdirSync on each project subdirectory (chromium-desktop, chromium-mobile) to list .png files. Skip if RESULTS_DIR does not exist (no captured screenshots yet). Derive theme from filename: ends with "-dark.png" → theme "dark"; otherwise → theme "light". Derive screen name by stripping "-dark.png" or ".png". Derive viewport from subdirectory name.

    Large-image guard: const MAX_B64_BYTES = 5 * 1024 * 1024 (5 MB). Read each PNG as base64. If b64.length > MAX_B64_BYTES, emit a console.warn and continue (skip that image; do not error).

    Batch strategy: score one PNG at a time (one API call per PNG) for simplicity. Each API call sends one image block + the RUBRIC_PROMPT text block. Parse the model response JSON. For each finding object in the parsed array, append to a NDJSON findings output (one JSON line per finding). Emit to findings.ndjson in RESULTS_DIR (or stdout with --stdout flag if present in process.argv).

    Error handling: wrap the main loop in try/catch; print any API error to stderr and exit 1 (only when key IS present — the no-key path already exits 0 above).

    Print a final summary line: "[score-visuals] Scored N PNGs → M findings (K below bar)" where K = findings with score < 2.

    Remediation loop documentation: add a comment block near the top of the file (after the imports) documenting the intended usage loop: "Run npm run e2e:visuals:png-only (with live server) to capture screenshots, then ANTHROPIC_API_KEY=... npm run score-visuals to score. Review findings.ndjson. For any dimension with score < 2: fix the CSS/component, run mix accrue_admin.assets.build + commit priv/static, reshoot, rescore. Cap at 3 rounds. Phase 176 already confirmed all 21 screens >= 2 at code level so minimal remediation is expected."

    The script must NOT hardcode the API key or any Stripe/test credentials. The ANTHROPIC_API_KEY comes from environment only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && node e2e/score-visuals.mjs 2>&1 | grep "ANTHROPIC_API_KEY not set"</automated>
  </verify>
  <done>
    node e2e/score-visuals.mjs (without ANTHROPIC_API_KEY set) prints "[score-visuals] ANTHROPIC_API_KEY not set — skipping (human/CI gate only)" and exits 0. File is a valid ESM module (node --input-type=module parses it). The locked findings schema fields (screen, viewport, theme, dimension, dimension_name, score, defect, suggested_fix) are all referenced in the code. The 5 MB large-image guard is present. SCORE_MODEL env var override is implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write admin-motion-trace.spec.js Playwright motion trace spec</name>
  <files>accrue_admin/e2e/admin-motion-trace.spec.js</files>
  <read_first>
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md — lines 167-218 (motion trace spec pattern: helpers, test.use trace override, 4 motion surfaces with verified trigger selectors: #search-trigger, details.ax-dropdown > summary, [data-collapse-toggle="true"], data-role="replay-single").
    accrue_admin/e2e/admin-visuals.spec.js — lines 1-17 (reset, seed, login helpers to copy verbatim).
    accrue_admin/e2e/reduced-motion.spec.js — read for the pattern of navigating to a surface and checking computed state; reference, do not copy the reduced-motion assertions.
  </read_first>
  <action>
    Create accrue_admin/e2e/admin-motion-trace.spec.js using require("@playwright/test") (CommonJS, matching all other specs in e2e/).

    Copy the three helpers verbatim from admin-visuals.spec.js: reset(request), seed(request, fixture), login(page, target).

    Add test.use({ trace: "on" }) at the suite level — this MUST be file-scoped so trace recording applies only to this spec. Do NOT modify playwright.config.js. The global config already has trace: "retain-on-failure"; this override is "on" (always) for motion review purposes only.

    Use a test.describe("Motion trace — animated surface capture") wrapper with test.beforeEach calling reset(request).

    Write 4 tests (one per motion surface). Each test must:
    1. Seed the appropriate fixture(s).
    2. Login to the target path.
    3. Await expect(page.locator("#main-content")).toBeVisible() as the page-settled gate.
    4. Trigger the motion surface using the verified selector.
    5. Assert the animated element becomes visible (expect(...).toBeVisible()).
    6. For open/close surfaces: trigger close and assert the element is hidden or the original state is restored.
    7. Add a comment "// Trace artifact: test-results/<test-name>/trace.zip" and "// Review with: npx playwright show-trace test-results/.../trace.zip".

    The 4 tests:

    Test "motion trace — command palette open/close": seed operator-flows (for a populated page). Login to /billing. Click #search-trigger. Assert page.locator(".ax-command-palette-wrapper") is visible. Click #search-trigger again or press Escape to close. Assert .ax-command-palette-wrapper is hidden or data-open is "false".

    Test "motion trace — dropdown open/close": seed operator-flows. Login to /billing/customers (any page with a dropdown). Click 'details.ax-dropdown > summary'. Assert page.locator('.ax-dropdown-panel').first() is visible. Click the summary again to close.

    Test "motion trace — nav group collapse/expand": seed operator-flows. Login to /billing. Click '[data-collapse-toggle="true"]' first occurrence. Assert the controlled sidebar group links are hidden (the aria-controls target). Click the toggle again to re-expand.

    Test "motion trace — webhook replay drawer open": seed operator-flows. Login to /billing/webhooks/${data.single_webhook_id}. Click '[data-role="replay-single"]'. Assert page.locator(".ax-detail-drawer-shell") is visible. Note in a comment: "detail_drawer.ex uses JS.show/JS.hide on .ax-detail-drawer-shell; the trace captures the enter transition frames."

    For the nav-collapse test: the aria-controls attribute on the toggle button gives the id of the collapsible list element. Use page.getAttribute to read it, then assert the list element is hidden via toHaveAttribute("hidden") or toBeHidden().

    The spec must NOT use test.use({ trace: "on" }) globally in playwright.config.js — confirm the override is at the describe/suite level within this file only.

    Security note: this spec uses the same /__e2e__/reset and /__e2e__/seed endpoints as all other e2e specs — they are MIX_ENV=test-only and this plan makes no changes to e2e_plug.ex or e2e_fixtures.ex.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && npx playwright test e2e/admin-motion-trace.spec.js --list 2>&1 | grep -c "motion trace"</automated>
  </verify>
  <done>
    npx playwright test e2e/admin-motion-trace.spec.js --list reports 4 tests (in both chromium-desktop and chromium-mobile projects = 8 lines, or 4 if listed by title). The file contains test.use({ trace: "on" }). The four motion surfaces are covered: command palette (#search-trigger), dropdown (details.ax-dropdown), nav-collapse ([data-collapse-toggle]), webhook replay (data-role="replay-single"). playwright.config.js is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| score-visuals.mjs → Anthropic API | Outbound HTTPS to api.anthropic.com; sends base64-encoded PNG content of seeded TEST DATA (no real PII) |
| ANTHROPIC_API_KEY env var | Secret read from environment only; never committed; never logged |
| PNGs → findings.ndjson | Local file I/O only; PNGs are gitignored; findings.ndjson is in test-results/ (also gitignored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-03 | Information Disclosure | score-visuals.mjs → Anthropic API | accept | PNGs contain seeded test data (synthetic names, fake Stripe IDs) — no real PII. API key is env-only, never committed (CLAUDE.md constraint honored). |
| T-179-04 | Information Disclosure | ANTHROPIC_API_KEY | mitigate | API key check is the first executable statement; key is never logged, never embedded in output artifacts, never committed. |
| T-179-05 | Tampering | admin-motion-trace.spec.js trace: "on" scope | mitigate | trace override is file-scoped via test.use() — does not affect playwright.config.js global config, preventing trace bloat on all other specs. |
| T-179-SC | Tampering | npm/pip installs | accept | @anthropic-ai/sdk already approved in Plan 01 (Package Legitimacy Audit); this plan creates no new installs. |
</threat_model>

<verification>
1. `cd accrue_admin && node e2e/score-visuals.mjs` — exits 0, prints skip message (no ANTHROPIC_API_KEY in env).
2. `cd accrue_admin && npx playwright test e2e/admin-motion-trace.spec.js --list` — shows 4 test titles (motion trace — command palette / dropdown / nav group / webhook replay).
3. `grep "trace.*on" accrue_admin/e2e/admin-motion-trace.spec.js` — shows test.use({ trace: "on" }).
4. `grep "trace.*on" accrue_admin/playwright.config.js` — must NOT show "on" (only "retain-on-failure" remains).
5. `grep "ANTHROPIC_API_KEY" accrue_admin/e2e/score-visuals.mjs` — confirms the guard is present.
6. `grep "suggested_fix" accrue_admin/e2e/score-visuals.mjs` — confirms findings schema field is referenced.
7. `cd accrue_admin && mix test --seed 0` — 262 tests, 0 failures (regression check).
</verification>

<success_criteria>
- score-visuals.mjs exits 0 cleanly without ANTHROPIC_API_KEY; script logic is complete for when a key is present.
- Findings schema fields (screen, viewport, theme, dimension, dimension_name, score, defect, suggested_fix) all appear in the script's output construction.
- SCORE_MODEL env var override is implemented.
- 5 MB base64 large-image guard is implemented.
- admin-motion-trace.spec.js covers all 4 motion surfaces.
- trace: "on" is file-scoped only (playwright.config.js unchanged).
- Elixir suite stays at 262 green.
</success_criteria>

<output>
Create `.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-02-SUMMARY.md` when done.
</output>

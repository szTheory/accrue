---
phase: 179-f-screenshot-driven-visual-qa-loop-sign-off
plan: 03
type: execute
wave: 2
depends_on: [179-01]
files_modified:
  - accrue_admin/e2e/admin-a11y.spec.js
  - .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md
autonomous: true
requirements: [QA-01, QA-02, QA-03]
must_haves:
  truths:
    - "admin-a11y.spec.js surfaces[] array contains exactly 21 entries with corrected route slugs"
    - "admin-a11y.spec.js seeds operator-flows + dashboard + edge-states without intermediate reset()"
    - "admin-a11y.spec.js retains emulateMedia({ reducedMotion: 'reduce' }) and the scan() helper unchanged"
    - "SIGN-OFF.md scaffold exists at .planning/phases/179-.../SIGN-OFF.md with all 9 required sections"
    - "SIGN-OFF.md before-column is seeded from 176-SCORECARD after-scores (all 21 screens noted as >= 2)"
    - "SIGN-OFF.md human/CI gate section uses literal [PENDING: ...] placeholder — does NOT claim the photographic run completed"
    - "SIGN-OFF.md consolidates deferred UAT items from phases 175/176/177/178"
    - "mix test --seed 0 stays green (262 tests)"
  artifacts:
    - path: "accrue_admin/e2e/admin-a11y.spec.js"
      provides: "Full 21-screen axe accessibility sweep"
      contains: "edge-states"
    - path: ".planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md"
      provides: "v1.51 milestone done-proof scaffold"
      contains: "PENDING"
  key_links:
    - from: "admin-a11y.spec.js surfaces[]"
      to: "/__e2e__/seed/edge-states"
      via: "seed(request, 'edge-states') call — same corrected routes as Plan 01"
      pattern: "edge-states"
    - from: "SIGN-OFF.md before-scores"
      to: ".planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md"
      via: "after-scores table (all 21 screens >= 2 after Wave 4 + Wave 5 gap-closure)"
      pattern: "176-SCORECARD"
---

<objective>
Extend admin-a11y.spec.js from 12 to 21 surfaces using the same 3-fixture multi-seed pattern from Plan 01, covering the full screen inventory including seeded edge-state screens in both light and dark. Produce SIGN-OFF.md — the milestone "done" proof scaffold aggregating evidence from all five prior phases (174–178) plus axe results and screenshot inventory reference. SIGN-OFF.md uses PENDING placeholders for the photographic gate run that requires a live server + API key.

Purpose: QA-03 — final scorecard 21/21 ≥2 with before/after evidence; axe passes both light and dark across the full inventory; SIGN-OFF consolidates deferred 175–178 human UAT items. Also closes QA-01 (SIGN-OFF references the 21-screen sweep inventory) and QA-02 (SIGN-OFF documents the scoring + remediation loop procedure).

Output: Updated admin-a11y.spec.js (21 surfaces, 3 fixtures, corrected routes); SIGN-OFF.md scaffold (9 sections, all with PENDING where photographic gate hasn't run).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md

<interfaces>
<!-- admin-a11y.spec.js current structure (lines 1-75): -->
<!-- Helpers: reset, seed, login (lines 1-16) identical to visuals spec -->
<!-- scan(page, theme) helper (lines 23-28): emulateMedia({ reducedMotion: "reduce" }) is called at line 39 -->
<!--   scan helper reads: evaluate data-theme, waitForTimeout(50), AxeBuilder withTags wcag2a/wcag2aa, filter critical/serious -->
<!--   DO NOT MODIFY the scan() helper — it is correct and race-condition-safe -->
<!-- Test body (lines 35-74): emulateMedia(reduce), seed operator-flows, surfaces[] array, failures accumulator, loop, expect([]).toEqual([]) -->
<!-- Extension: add multi-fixture seed (operator-flows + dashboard + edge-states), expand surfaces[] to 21 -->

<!-- 176-SCORECARD after-scores (for SIGN-OFF before-column): all 21 screens >= 2 after Wave 4+5 uplift -->
<!-- DashboardLive: 3-3-3-2-2-2-2-3-2-3 min=2 -->
<!-- CustomerLive: 3-3-3-3-3-2-3-3-2-3 min=2 (all dims >=2) -->
<!-- (All 21 screens moved to passing after Phase 176 waves) -->

<!-- Deferred UAT consolidation sources: -->
<!-- 175-HUMAN-UAT.md: persona-path click-throughs (4+ items deferred to photographic gate) -->
<!-- 176-HUMAN-UAT.md: rubric spot-check (3 items deferred to Phase 179 photographic pass) -->
<!-- 177-HUMAN-UAT.md: motion quality review + reduced-motion pass (2 items deferred) -->
<!-- 178-HUMAN-UAT.md: dark-contrast edge states + state completeness (3 items deferred) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend admin-a11y.spec.js to 21 surfaces with multi-fixture seed</name>
  <files>accrue_admin/e2e/admin-a11y.spec.js</files>
  <read_first>
    accrue_admin/e2e/admin-a11y.spec.js — read full file (lines 1-75); identify the surfaces[] array and the multi-fixture extension points.
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md — lines 125-163 (axe extension: scan() helper retention, multi-fixture seed pattern, failures accumulator pattern, corrected surfaces[]).
    accrue_admin/e2e/admin-visuals.spec.js — (as updated by Plan 01) for the 21-entry shots[] to mirror as surfaces[]; use identical corrected route slugs.
  </read_first>
  <action>
    Extend admin-a11y.spec.js. The helpers (reset, seed, login, scan) and the test.describe/beforeEach structure must be preserved exactly. Only the test body changes.

    In the test body, replace the single seed("operator-flows") call with three sequential seed calls — seed(request, "operator-flows") stored as opFlows, seed(request, "dashboard") stored as dash, seed(request, "edge-states") stored as edge. Do NOT call reset() between them — same accumulation safety as Plan 01 (System.unique_integer processor IDs).

    The emulateMedia({ reducedMotion: "reduce" }) call on line 39 must remain, and it must remain BEFORE the seed calls (it controls media state for the entire test; ordering vs. seed doesn't matter functionally but preserve the existing line position to minimize diff noise).

    Replace the 12-entry surfaces[] array with the same 21-entry array as Plan 01's shots[] — using identical corrected route slugs. The array variable is named surfaces (not shots) in this file; keep that name.

    Surfaces array (same 21 entries as Plan 01, using opFlows/dash/edge variables):
    1. ["dashboard",           "/billing"]
    2. ["customers",           "/billing/customers"]
    3. ["customer-detail",     "/billing/customers/${dash.customer_id}"]
    4. ["subscriptions",       "/billing/subscriptions"]
    5. ["subscription-detail", "/billing/subscriptions/${dash.subscription_id}"]
    6. ["invoices",            "/billing/invoices"]
    7. ["invoice-detail",      "/billing/invoices/${edge.jpy_invoice_id}"]
    8. ["payments",            "/billing/payments"]
    9. ["charge-detail",       "/billing/payments/${opFlows.charge_id}"]
    10. ["coupons",            "/billing/coupons"]
    11. ["coupon-detail",      "/billing/coupons/${edge.coupon_id}"]
    12. ["promotion-codes",    "/billing/promotion-codes"]
    13. ["promo-code-detail",  "/billing/promotion-codes/${edge.promo_code_id}"]
    14. ["connect",            "/billing/connect"]
    15. ["connect-detail",     "/billing/connect/${edge.connect_account_id}"]
    16. ["events",             "/billing/events"]
    17. ["event-detail",       "/billing/events/${opFlows.source_event_id}"]
    18. ["webhooks",           "/billing/webhooks"]
    19. ["webhook-detail",     "/billing/webhooks/${opFlows.single_webhook_id}"]
    20. ["recovery",           "/billing/analytics/recovery"]
    21. ["campaign-detail",    "/billing/analytics/recovery/subscriptions/${edge.at_risk_sub_id}"]

    The failures accumulator, the for loop over surfaces (login + visible + scan light/dark + push to failures), and the final expect(failures).toEqual([]) assertion all remain unchanged.

    Update the test description comment to note this now covers the full 21-screen inventory including seeded edge states.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && node -e "const s = require('fs').readFileSync('e2e/admin-a11y.spec.js','utf8'); const m = s.match(/\[\"[a-z]/g); console.log(m ? m.length : 0)"</automated>
  </verify>
  <done>
    The node -e count of ["<name> entries in the file returns 21. The file retains scan() helper unchanged, emulateMedia({ reducedMotion: "reduce" }), and the failures accumulator pattern. Corrected routes (/billing/payments, /billing/connect, /billing/analytics/recovery/subscriptions/) appear; /billing/charges, /billing/connect-accounts, /campaigns/ do not.
  </done>
</task>

<task type="auto">
  <name>Task 2: Produce SIGN-OFF.md milestone done-proof scaffold</name>
  <files>.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md</files>
  <read_first>
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-PATTERNS.md — lines 308-328 (SIGN-OFF.md scaffold: 9 required sections, PENDING placeholder wording, before-scores from 176-SCORECARD, deferred UAT consolidation).
    .planning/phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md — lines 47-70 (before-scores table for all 21 screens; use the after-scores as the SIGN-OFF "before" column since that table shows scores after all Phase 176 waves).
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-CONTEXT.md — QA-03 decision: SIGN-OFF aggregates 176 rubric (21/21 ≥2) + 177 motion + 178 state coverage + axe both themes + screenshot evidence.
    .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-VALIDATION.md — Manual-Only Verifications section (items that the photographic gate closes).
  </read_first>
  <action>
    Create SIGN-OFF.md at .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md.

    The document must contain the following 9 sections in order:

    SECTION 1 — Milestone header: v1.51 Admin UI: Depth Pass — SIGN-OFF. Sub-header with phase range (174–179) and date placeholder. A "Gate status" line: "Vision-scoring photographic run: [PENDING — run `npm run score-visuals` with ANTHROPIC_API_KEY + live server]". State explicitly that this scaffold marks build complete; the photographic run closes the final gate.

    SECTION 2 — Rubric scorecard: A markdown table with columns: Screen | Before (176-SCORECARD ①–⑩ min) | After (photographic gate) | Pass. The "Before" column is populated from the 176-SCORECARD after-scores (all 21 screens show their min score from the SCORECARD; all 21 are ≥2 after Phase 176). The "After" column uses "[PENDING]" for every row. Include all 21 screens in this order: DashboardLive, CustomersLive, CustomerLive, SubscriptionsLive, SubscriptionLive, InvoicesLive, InvoiceLive, ChargesLive, ChargeLive, CouponsLive, CouponLive, PromotionCodesLive, PromotionCodeLive, ConnectAccountsLive, ConnectAccountLive, EventsLive, EventLive, WebhooksLive, WebhookLive, RecoveryLive, CampaignLive.

    SECTION 3 — Axe status: A table with columns: Surface | Light violations | Dark violations | Mobile (Pixel5) | Status. Populate from the autonomous axe run in the verification section. For all 21 screens, status = "[PENDING — run npm run e2e:a11y]" until the axe spec runs against a live server. Note that the spec asserts 0 critical/serious violations.

    SECTION 4 — Motion confirmation: A table with columns: Surface | Trace captured | Quality reviewed | Reduced-motion spec | Status. The 4 motion surfaces: command palette, dropdown, nav-collapse, webhook replay drawer. Trace captured = "[PENDING — run npx playwright test e2e/admin-motion-trace.spec.js]". Quality reviewed = "[PENDING — human review: npx playwright show-trace ...]". Reference the Phase 177 reduced-motion spec (e2e/reduced-motion.spec.js) — note it passes in CI (Phase 177 shipped). List the deferred 177 HUMAN-UAT items (reduced-motion live quality pass + motion review) as the items this section closes.

    SECTION 5 — State coverage: Reference Phase 178 STATE-MATRIX.md. State "All 21 screens have reachable states via E2E fixtures (operator-flows + dashboard + edge-states + overflow). Evidence: .planning/phases/178-e-seed-expressiveness-state-coverage/STATE-MATRIX.md. SEED-01 and SEED-02 requirements verified by Phase 178 execution." List the deferred 178 HUMAN-UAT items that the photographic sweep closes.

    SECTION 6 — Design-system completeness: Reference Phase 174 outcomes. State "DSY-01/02/03 requirements verified by Phase 174 execution. Token compliance guard: grep -v '^#' accrue_admin/assets/css/app.css | grep -c 'var(--ax-' returns non-zero; no literal px/hex in live templates. Evidence: .planning/phases/174-a-design-system-gap-closure-token-completeness/174-07-SUMMARY.md".

    SECTION 7 — IA persona paths: Reference Phase 175 outcomes. State "IA-01 through IA-07 requirements verified by Phase 175 execution. Six personas reach their primary job in ≤2 clicks from Home. Related-billing cards on all detail screens. Route redirects from old paths confirmed. Evidence: .planning/phases/175-b-persona-driven-ia-spine/175-07-SUMMARY.md". List the deferred 175 HUMAN-UAT items that the photographic sweep closes.

    SECTION 8 — Screenshot evidence directory: State "Screenshots are gitignored artifacts produced by: cd accrue_admin && npm run e2e:visuals:png-only (requires live server). Output location: accrue_admin/test-results/admin-visuals/chromium-desktop/ and accrue_admin/test-results/admin-visuals/chromium-mobile/. 21 screens × 2 themes × 2 projects = 84 PNGs expected. Status: [PENDING — run capture command against live server]". Do NOT embed any PNG paths as actual image references.

    SECTION 9 — Human/CI gate checklist: A markdown checklist of the items the photographic run must complete before this SIGN-OFF is finalized:
    - [ ] Full 4-cell screenshot capture (84 PNGs) — npm run e2e:visuals:png-only
    - [ ] Vision-LLM scoring all dimensions >= 2 — ANTHROPIC_API_KEY=... npm run score-visuals → review findings.ndjson
    - [ ] Remediation loop complete (≤3 rounds; Phase 176 confirmed >= 2 at code level, minimal expected)
    - [ ] Axe 0 critical/serious in light + dark across all 21 screens — npm run e2e:a11y
    - [ ] Motion traces reviewed in Playwright Trace Viewer for 4 surfaces
    - [ ] SIGN-OFF scorecard After-column filled from findings.ndjson
    - [ ] Deferred 175 HUMAN-UAT items confirmed via photographic evidence
    - [ ] Deferred 176 HUMAN-UAT items confirmed via photographic evidence
    - [ ] Deferred 177 HUMAN-UAT items confirmed via motion trace review
    - [ ] Deferred 178 HUMAN-UAT items confirmed via axe dark-contrast pass + photographic evidence
    - [ ] v1.51 milestone CLOSED: all QA-01, QA-02, QA-03 requirements satisfied

    The document must NOT claim any photographic run has occurred. It must NOT assert "all screens pass" in the After column. PENDING placeholders are the correct and honest state.
  </action>
  <verify>
    <automated>grep -c "PENDING" /Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md</automated>
  </verify>
  <done>
    SIGN-OFF.md exists at .planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md. grep -c "PENDING" returns ≥5 (multiple PENDING placeholders throughout). The file contains all 9 sections. All 21 screen names appear in the rubric scorecard table. The before-scores are sourced from 176-SCORECARD (all ≥2). The human/CI gate checklist has all 11 items. The file references deferred UAT items from phases 175, 176, 177, and 178.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e spec → /__e2e__/seed, /__e2e__/reset | Test-only endpoints; MIX_ENV=test-only; unchanged by this plan |
| SIGN-OFF.md → planning artifacts | Internal planning document; no external trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-06 | Information Disclosure | /__e2e__/seed routes consumed by axe spec | accept | Routes remain MIX_ENV=test-only; this plan makes no changes to e2e_plug.ex; existing compile guard unchanged |
| T-179-07 | Repudiation | SIGN-OFF.md false completion claim | mitigate | All photographic gate sections use explicit [PENDING: ...] placeholders; milestone audit reader can unambiguously distinguish scaffold-present from run-completed |
| T-179-SC | Tampering | npm/pip installs | accept | No new npm installs in this plan; @anthropic-ai/sdk already added in Plan 01 |
</threat_model>

<verification>
1. `cd /Users/jon/projects/accrue/accrue_admin && node -e "const s = require('fs').readFileSync('e2e/admin-a11y.spec.js','utf8'); const m = s.match(/\[\"[a-z]/g); console.log(m ? m.length : 0)"` — returns 21.
2. `grep "edge-states" /Users/jon/projects/accrue/accrue_admin/e2e/admin-a11y.spec.js` — shows the seed call.
3. `grep -c "billing/payments" /Users/jon/projects/accrue/accrue_admin/e2e/admin-a11y.spec.js` — returns ≥2.
4. `grep -c "billing/charges" /Users/jon/projects/accrue/accrue_admin/e2e/admin-a11y.spec.js` — returns 0.
5. `grep "scan" /Users/jon/projects/accrue/accrue_admin/e2e/admin-a11y.spec.js` — confirms scan() helper and emulateMedia present.
6. `test -f /Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md && echo "exists"` — returns "exists".
7. `grep -c "PENDING" /Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md` — returns ≥5.
8. `grep "DashboardLive" /Users/jon/projects/accrue/.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md` — confirms scorecard table present.
9. `cd /Users/jon/projects/accrue/accrue_admin && mix test --seed 0` — 262 tests, 0 failures.
</verification>

<success_criteria>
- admin-a11y.spec.js has exactly 21 entries in surfaces[].
- Three fixtures seeded without intermediate reset(); same corrected routes as Plan 01.
- scan() helper (emulateMedia + waitForTimeout + AxeBuilder) is unchanged.
- SIGN-OFF.md exists with all 9 sections.
- Before-scores table covers all 21 screens, sourced from 176-SCORECARD.
- Every photographic gate item uses [PENDING: ...] — no false completion claims.
- Deferred UAT items from phases 175/176/177/178 are referenced in the appropriate sections.
- Elixir suite stays at 262 green.
</success_criteria>

<output>
Create `.planning/phases/179-f-screenshot-driven-visual-qa-loop-sign-off/179-03-SUMMARY.md` when done.
</output>

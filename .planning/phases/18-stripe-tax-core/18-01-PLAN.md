---
phase: 18-stripe-tax-core
plan: 01
type: execute
wave: 2
depends_on:
  - 18-02
files_modified:
  - accrue/lib/accrue/billing/subscription_actions.ex
  - accrue/test/accrue/billing/subscription_test.exs
autonomous: true
requirements:
  - TAX-01
must_haves:
  truths:
    - "Developers can enable or disable automatic tax when calling `Accrue.Billing.subscribe/3`."
    - "Existing `Accrue.Billing.subscribe/3` callers without a tax option still behave the same way."
  artifacts:
    - path: "accrue/lib/accrue/billing/subscription_actions.ex"
      provides: "Flat public `:automatic_tax` option normalized into processor params"
    - path: "accrue/test/accrue/billing/subscription_test.exs"
      provides: "Backward-compatible enabled and disabled subscription tax coverage"
  key_links:
    - from: "accrue/lib/accrue/billing/subscription_actions.ex"
      to: "Accrue.Processor.__impl__().create_subscription/2"
      via: "stripe_params automatic_tax map"
      pattern: "create_subscription\\("
---

<objective>
Add the public subscription tax contract on `Accrue.Billing.subscribe/3` and prove that tax-enabled and tax-disabled calls stay backward-compatible.

Purpose: Make Stripe Tax opt-in explicit at the public billing boundary without leaking Stripe request shape into host code.
Output: A flat `:automatic_tax` option on `subscribe/3` plus focused subscription tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-stripe-tax-core/18-RESEARCH.md
@.planning/phases/18-stripe-tax-core/18-VALIDATION.md
@.planning/phases/18-stripe-tax-core/18-PATTERNS.md
@.planning/milestones/v1.0-phases/03-core-subscription-lifecycle/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `:automatic_tax` to the subscription entry point</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription_actions.ex
    - .planning/phases/18-stripe-tax-core/18-RESEARCH.md
    - .planning/phases/18-stripe-tax-core/18-VALIDATION.md
    - .planning/phases/18-stripe-tax-core/18-PATTERNS.md
  </read_first>
  <files>accrue/lib/accrue/billing/subscription_actions.ex</files>
  <action>Update `Accrue.Billing.SubscriptionActions.do_subscribe/3` so Phase 18 standardizes on a flat `automatic_tax: true | false` public option for TAX-01. Add a helper named `maybe_put_automatic_tax/2` that reads `Keyword.get(opts, :automatic_tax, false)` and writes `automatic_tax: %{enabled: enabled}` into the `stripe_params` map before `Processor.__impl__().create_subscription/2` is called. Keep the public option boolean-only; do not accept caller-supplied nested Stripe maps and do not introduce any caller-controlled tax totals. Update `sanitize_opts/1` so `:automatic_tax` is stripped from request opts and only the normalized processor param reaches the adapter.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription_actions.ex` contains `maybe_put_automatic_tax(opts)`
    - `accrue/lib/accrue/billing/subscription_actions.ex` contains `Keyword.get(opts, :automatic_tax, false)`
    - `accrue/lib/accrue/billing/subscription_actions.ex` contains `Map.put(params, :automatic_tax, %{enabled: enabled})`
    - `accrue/lib/accrue/billing/subscription_actions.ex` strips `:automatic_tax` inside `sanitize_opts/1`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_test.exs</automated>
  </verify>
  <done>`subscribe/3` accepts a flat `:automatic_tax` boolean and only the normalized processor param is passed downstream.</done>
</task>

<task type="auto">
  <name>Task 2: Add subscription tax regression coverage</name>
  <read_first>
    - accrue/test/accrue/billing/subscription_test.exs
    - accrue/lib/accrue/billing/subscription_actions.ex
    - .planning/phases/18-stripe-tax-core/18-VALIDATION.md
  </read_first>
  <files>accrue/test/accrue/billing/subscription_test.exs</files>
  <action>Add focused Phase 18 tests for TAX-01 in `subscription_test.exs`. Cover one `Billing.subscribe/3` call with `automatic_tax: true` and assert the stored subscription keeps the raw processor payload in `sub.data["automatic_tax"]["enabled"] == true` or the atom-key equivalent before persistence normalization. Cover one explicit `automatic_tax: false` call and one legacy call with no tax option so backward compatibility is proven. Keep the tests Fake-backed and deterministic; do not use scripted Stripe errors for ordinary success paths.</action>
  <acceptance_criteria>
    - `accrue/test/accrue/billing/subscription_test.exs` contains the string `automatic_tax: true`
    - `accrue/test/accrue/billing/subscription_test.exs` contains the string `automatic_tax: false`
    - `accrue/test/accrue/billing/subscription_test.exs` asserts against `automatic_tax`
    - `cd accrue && mix test test/accrue/billing/subscription_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/subscription_test.exs</automated>
  </verify>
  <done>Subscription tests prove enabled, disabled, and legacy no-tax calls all pass against the Fake processor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host billing call -> `Accrue.Billing.subscribe/3` | Untrusted caller input crosses into Accrue's public API |
| `SubscriptionActions` -> processor adapter | Normalized tax intent crosses into a provider-specific adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | `accrue/lib/accrue/billing/subscription_actions.ex` | mitigate | Accept only flat boolean `:automatic_tax`; reject implicit nested Stripe maps by never reading anything except `Keyword.get(opts, :automatic_tax, false)`. |
| T-18-02 | I | `accrue/test/accrue/billing/subscription_test.exs` | mitigate | Assert on local projection/raw payload fields only; do not log full processor payloads or sensitive Stripe objects in test helpers. |
| T-18-03 | S | `accrue/lib/accrue/billing/subscription_actions.ex` | mitigate | Do not accept caller-supplied tax totals or statuses; only forward enablement intent to the processor. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/subscription_test.exs`.
</verification>

<success_criteria>
`Accrue.Billing.subscribe/3` accepts `automatic_tax: true | false`, existing callers remain compatible, and Fake-backed tests cover enabled, disabled, and omitted tax options.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stripe-tax-core/18-01-SUMMARY.md`
</output>

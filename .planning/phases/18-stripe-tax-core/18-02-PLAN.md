---
phase: 18-stripe-tax-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/test/accrue/processor/stripe_test.exs
  - accrue/test/accrue/processor/fake_test.exs
autonomous: true
requirements:
  - TAX-01
must_haves:
  truths:
    - "Stripe-backed adapter calls receive automatic-tax intent when Accrue enables tax."
    - "Fake-backed tests can deterministically observe enabled and disabled automatic-tax states."
  artifacts:
    - path: "accrue/lib/accrue/processor/stripe.ex"
      provides: "Stripe passthrough for automatic_tax params"
    - path: "accrue/lib/accrue/processor/fake.ex"
      provides: "Deterministic fake automatic-tax payloads"
    - path: "accrue/test/accrue/processor/fake_test.exs"
      provides: "Parity tests for tax-enabled and tax-disabled fake responses"
  key_links:
    - from: "accrue/lib/accrue/processor/stripe.ex"
      to: "LatticeStripe.Subscription.create"
      via: "stringify_keys(params)"
      pattern: "LatticeStripe\\.Subscription\\.create"
    - from: "accrue/lib/accrue/processor/fake.ex"
      to: "subscription / invoice / checkout payload builders"
      via: "automatic_tax field population"
      pattern: "automatic_tax"
---

<objective>
Make Stripe and Fake adapters represent automatic-tax intent the same way so TAX-01 is provider-consistent and testable.

Purpose: Keep Accrue's Stripe-first strategy intact while making the Fake processor authoritative for deterministic tax tests.
Output: Processor parity for automatic tax plus adapter-focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-stripe-tax-core/18-RESEARCH.md
@.planning/phases/18-stripe-tax-core/18-VALIDATION.md
@.planning/phases/18-stripe-tax-core/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve automatic-tax intent inside the Stripe adapter</name>
  <read_first>
    - accrue/lib/accrue/processor/stripe.ex
    - accrue/test/accrue/processor/stripe_test.exs
    - .planning/phases/18-stripe-tax-core/18-RESEARCH.md
    - .planning/phases/18-stripe-tax-core/18-PATTERNS.md
  </read_first>
  <files>accrue/lib/accrue/processor/stripe.ex, accrue/test/accrue/processor/stripe_test.exs</files>
  <action>Keep TAX-01 Stripe-specific behavior isolated to `Accrue.Processor.Stripe`. Do not add any tax branching outside `params`. Ensure `create_subscription/2` and `checkout_session_create/2` continue to forward the already-normalized `automatic_tax` map through `stringify_keys(params)` unchanged, and add tests in `stripe_test.exs` that assert the adapter preserves `%{automatic_tax: %{enabled: true}}` or `%{"automatic_tax" => %{"enabled" => true}}` in the outgoing request-shape helper path. Use existing adapter-level test style; do not introduce new HTTP clients or bypass the LatticeStripe facade rule.</action>
  <acceptance_criteria>
    - `accrue/test/accrue/processor/stripe_test.exs` contains the string `automatic_tax`
    - `accrue/lib/accrue/processor/stripe.ex` still calls `stringify_keys(params)` in both `create_subscription/2` and `checkout_session_create/2`
    - `cd accrue && mix test test/accrue/processor/stripe_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/stripe_test.exs</automated>
  </verify>
  <done>The Stripe adapter still owns provider-specific request shape and tests prove automatic-tax intent is preserved to the LatticeStripe boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic Fake tax payloads for subscription, invoice, and checkout flows</name>
  <read_first>
    - accrue/lib/accrue/processor/fake.ex
    - accrue/test/accrue/processor/fake_test.exs
    - .planning/phases/18-stripe-tax-core/18-RESEARCH.md
    - .planning/phases/18-stripe-tax-core/18-VALIDATION.md
  </read_first>
  <files>accrue/lib/accrue/processor/fake.ex, accrue/test/accrue/processor/fake_test.exs</files>
  <action>Extend `Accrue.Processor.Fake` so normal success-path resources include deterministic automatic-tax state for TAX-01. Subscription payloads should carry `automatic_tax: %{enabled: true | false, status: "complete" | nil}`. Invoice payloads should carry the same automatic-tax map and a deterministic tax amount source such as `tax` and `total_details: %{amount_tax: integer}` when enabled, or zero/nil when disabled. Checkout session payloads should carry `automatic_tax` and `total_details.amount_tax`. Keep the enabled/disabled choice driven solely by the normalized `params[:automatic_tax][:enabled]` or string-key equivalent. Add Fake tests for enabled and disabled subscription or checkout creation that assert the returned maps include the exact `automatic_tax` and tax-amount fields.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/fake.ex` contains the string `automatic_tax`
    - `accrue/lib/accrue/processor/fake.ex` contains the string `amount_tax`
    - `accrue/test/accrue/processor/fake_test.exs` contains the string `automatic_tax`
    - `cd accrue && mix test test/accrue/processor/fake_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/fake_test.exs</automated>
  </verify>
  <done>Fake processor responses expose enabled and disabled automatic-tax states without scripted-response hacks for ordinary tax coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized Accrue params -> Stripe adapter | Provider-specific tax payload leaves Accrue's public boundary |
| normalized Accrue params -> Fake adapter | Test double materializes tax state for local verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | T | `accrue/lib/accrue/processor/stripe.ex` | mitigate | Preserve only normalized `automatic_tax.enabled`; never derive or trust caller-supplied tax totals in adapter params. |
| T-18-05 | I | `accrue/lib/accrue/processor/stripe.ex` | mitigate | Keep tax payload handling inside existing adapter flow and avoid new logging of Stripe payload maps or tax details. |
| T-18-06 | R | `accrue/lib/accrue/processor/fake.ex` | mitigate | Fake emits the same observable automatic-tax keys as Stripe-facing flows so parity drift is caught by tests instead of hidden behind custom fixtures. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/processor/stripe_test.exs test/accrue/processor/fake_test.exs`.
</verification>

<success_criteria>
Stripe adapter tests prove automatic-tax intent survives to the provider boundary, and Fake tests prove enabled and disabled tax payloads are deterministic for subscription, invoice, and checkout paths.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stripe-tax-core/18-02-SUMMARY.md`
</output>

---
phase: 18-stripe-tax-core
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs
  - accrue/lib/accrue/billing/subscription.ex
  - accrue/lib/accrue/billing/invoice.ex
  - accrue/lib/accrue/billing/subscription_projection.ex
  - accrue/lib/accrue/billing/invoice_projection.ex
  - accrue/test/accrue/billing/invoice_projection_test.exs
  - accrue/test/accrue/billing/subscription_projection_tax_test.exs
autonomous: true
requirements:
  - TAX-01
must_haves:
  truths:
    - "Local subscription rows expose whether automatic tax was enabled and what status Stripe/Fake reported."
    - "Local invoice rows preserve automatic-tax state and tax amount without requiring 1:1 Stripe column parity."
  artifacts:
    - path: "accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs"
      provides: "Schema support for automatic_tax observability"
    - path: "accrue/lib/accrue/billing/subscription_projection.ex"
      provides: "Subscription automatic-tax projection"
    - path: "accrue/lib/accrue/billing/invoice_projection.ex"
      provides: "Invoice automatic-tax and tax-amount projection"
  key_links:
    - from: "accrue/lib/accrue/billing/subscription_projection.ex"
      to: "accrue/lib/accrue/billing/subscription.ex"
      via: "decompose/1 attrs map"
      pattern: "automatic_tax"
    - from: "accrue/lib/accrue/billing/invoice_projection.ex"
      to: "accrue/lib/accrue/billing/invoice.ex"
      via: "invoice_attrs"
      pattern: "tax_minor|automatic_tax"
---

<objective>
Persist the minimum automatic-tax observability Accrue needs for subscription and invoice rows, while keeping full upstream payloads in `data`.

Purpose: Make tax state queryable and testable locally without turning Accrue into a Stripe-schema mirror.
Output: Billing-table migration, schema fields, projection updates, and focused projection tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-stripe-tax-core/18-RESEARCH.md
@.planning/phases/18-stripe-tax-core/18-VALIDATION.md
@.planning/phases/18-stripe-tax-core/18-PATTERNS.md
@.planning/milestones/v1.0-phases/04-advanced-billing-webhook-hardening/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add narrow automatic-tax storage for subscriptions and invoices</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription.ex
    - accrue/lib/accrue/billing/invoice.ex
    - accrue/priv/repo/migrations/20260414120000_phase3_schema_upgrades.exs
    - .planning/phases/18-stripe-tax-core/18-RESEARCH.md
    - .planning/phases/18-stripe-tax-core/18-PATTERNS.md
  </read_first>
  <files>accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs, accrue/lib/accrue/billing/subscription.ex, accrue/lib/accrue/billing/invoice.ex</files>
  <action>Create migration `accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs` that adds `:automatic_tax` as `:boolean, default: false, null: false` and `:automatic_tax_status` as `:string` to both `:accrue_subscriptions` and `:accrue_invoices`. Update `Accrue.Billing.Subscription` and `Accrue.Billing.Invoice` schemas plus `@cast_fields` so those exact fields are persisted. Do not add 1:1 Stripe tax payload columns beyond these two fields; keep rich provider payloads in `data` and keep invoice `tax_minor` as the numeric amount field for TAX-01.</action>
  <acceptance_criteria>
    - `accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs` contains `add :automatic_tax, :boolean, default: false, null: false`
    - `accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs` contains `add :automatic_tax_status, :string`
    - `accrue/lib/accrue/billing/subscription.ex` contains `field(:automatic_tax, :boolean, default: false)`
    - `accrue/lib/accrue/billing/invoice.ex` contains `field(:automatic_tax_status, :string)`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/invoice_projection_test.exs</automated>
  </verify>
  <done>Subscriptions and invoices have explicit automatic-tax storage without expanding into a full Stripe tax schema clone.</done>
</task>

<task type="auto">
  <name>Task 2: Project automatic-tax state and tax amount from processor payloads</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription_projection.ex
    - accrue/lib/accrue/billing/invoice_projection.ex
    - accrue/test/accrue/billing/invoice_projection_test.exs
    - .planning/phases/18-stripe-tax-core/18-VALIDATION.md
    - .planning/phases/18-stripe-tax-core/18-PATTERNS.md
  </read_first>
  <files>accrue/lib/accrue/billing/subscription_projection.ex, accrue/lib/accrue/billing/invoice_projection.ex, accrue/test/accrue/billing/invoice_projection_test.exs, accrue/test/accrue/billing/subscription_projection_tax_test.exs</files>
  <action>Update `SubscriptionProjection.decompose/1` to set `automatic_tax` from `get(stripe_sub, :automatic_tax)` -> `get(..., :enabled) || false` and `automatic_tax_status` from `get(..., :status)`. Update `InvoiceProjection.decompose/1` so `invoice_attrs.tax_minor` prefers `automatic_tax`-aware sources in this order: `SubscriptionProjection.get(stripe_inv, :tax)`, then `total_details.amount_tax`, then `total_details["amount_tax"]`, then `0` when automatic tax is enabled and no amount is present. Also populate `invoice_attrs.automatic_tax` and `invoice_attrs.automatic_tax_status` from the same `automatic_tax` map. Extend `invoice_projection_test.exs` and add `subscription_projection_tax_test.exs` to prove enabled/disabled cases, forward-compatible `total_details.amount_tax` fallback, and string-key / atom-key parity. Keep full upstream payloads in `data` unchanged.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription_projection.ex` contains `automatic_tax:`
    - `accrue/lib/accrue/billing/subscription_projection.ex` contains `automatic_tax_status:`
    - `accrue/lib/accrue/billing/invoice_projection.ex` contains `total_details`
    - `accrue/test/accrue/billing/subscription_projection_tax_test.exs` contains `automatic_tax_status`
    - `cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs</automated>
  </verify>
  <done>Subscription and invoice projections expose enabled/status state plus invoice tax amount while retaining raw payloads for forward compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor payload -> projection modules | Canonical Stripe/Fake billing payloads are converted into local DB attrs |
| projection attrs -> database rows | Narrow derived observability fields are persisted for application queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | T | `accrue/lib/accrue/billing/invoice_projection.ex` | mitigate | Derive `tax_minor` only from processor payload fields (`tax`, `total_details.amount_tax`); never trust caller-supplied totals. |
| T-18-08 | I | `accrue/lib/accrue/billing/subscription_projection.ex` | mitigate | Persist only `automatic_tax`, `automatic_tax_status`, and raw `data`; avoid logging or denormalizing sensitive Stripe tax payload details beyond needed observability. |
| T-18-09 | D | migration/schema pair | mitigate | Keep schema addition narrow and backwards-compatible with defaults so existing non-tax rows remain readable and test setup stays stable. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs`.
</verification>

<success_criteria>
Local subscription and invoice rows expose automatic-tax enabled/status state, invoice tax amounts still project correctly, and existing non-tax data remains compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stripe-tax-core/18-03-SUMMARY.md`
</output>

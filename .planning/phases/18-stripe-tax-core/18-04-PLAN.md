---
phase: 18-stripe-tax-core
plan: 04
type: execute
wave: 2
depends_on:
  - 18-02
files_modified:
  - accrue/lib/accrue/checkout/session.ex
  - accrue/test/accrue/checkout_test.exs
autonomous: true
requirements:
  - TAX-01
must_haves:
  truths:
    - "Developers can enable or disable automatic tax when creating checkout sessions."
    - "Returned checkout session structs expose automatic-tax state and tax amount for local observation."
  artifacts:
    - path: "accrue/lib/accrue/checkout/session.ex"
      provides: "Checkout public tax option and projection fields"
    - path: "accrue/test/accrue/checkout_test.exs"
      provides: "Checkout tax-enabled and tax-disabled coverage"
  key_links:
    - from: "accrue/lib/accrue/checkout/session.ex"
      to: "Processor.__impl__().checkout_session_create/2"
      via: "build_stripe_params/1"
      pattern: "checkout_session_create"
---

<objective>
Add the checkout-side public tax contract and make returned checkout sessions expose automatic-tax state without adding checkout persistence.

Purpose: Cover the second TAX-01 public entry point with the same clear option shape as subscriptions.
Output: `Accrue.Checkout.Session.create/1` tax support plus checkout tax tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-stripe-tax-core/18-RESEARCH.md
@.planning/phases/18-stripe-tax-core/18-VALIDATION.md
@.planning/phases/18-stripe-tax-core/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `automatic_tax` to checkout session creation and projection</name>
  <read_first>
    - accrue/lib/accrue/checkout/session.ex
    - .planning/phases/18-stripe-tax-core/18-RESEARCH.md
    - .planning/phases/18-stripe-tax-core/18-PATTERNS.md
  </read_first>
  <files>accrue/lib/accrue/checkout/session.ex</files>
  <action>Extend `Accrue.Checkout.Session` for TAX-01 by adding `:automatic_tax` to `@create_schema` as `type: :boolean, default: false`. Extend the struct with `:automatic_tax` and `:amount_tax`. In `build_stripe_params/1`, always include `"automatic_tax" => %{"enabled" => opts[:automatic_tax]}`. In `from_stripe/1`, project `automatic_tax` to a boolean field using `get(stripe, :automatic_tax)` -> `get(..., :enabled) || false`, and project `amount_tax` from `get(stripe, :total_details)` -> `get(..., :amount_tax)` with string-key fallback. Keep `data: stripe` unchanged and do not add a checkout table in this phase.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/checkout/session.ex` contains `automatic_tax: [type: :boolean, default: false]`
    - `accrue/lib/accrue/checkout/session.ex` contains `:automatic_tax`
    - `accrue/lib/accrue/checkout/session.ex` contains `:amount_tax`
    - `accrue/lib/accrue/checkout/session.ex` contains `"automatic_tax" => %{"enabled" => opts[:automatic_tax]}`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/checkout_test.exs</automated>
  </verify>
  <done>Checkout session creation accepts a flat automatic-tax boolean and returned session structs expose tax state and amount.</done>
</task>

<task type="auto">
  <name>Task 2: Cover checkout tax-enabled and tax-disabled flows</name>
  <read_first>
    - accrue/test/accrue/checkout_test.exs
    - accrue/lib/accrue/checkout/session.ex
    - .planning/phases/18-stripe-tax-core/18-VALIDATION.md
  </read_first>
  <files>accrue/test/accrue/checkout_test.exs</files>
  <action>Add TAX-01 tests that call `Session.create/1` with `automatic_tax: true` and `automatic_tax: false`. Assert the returned `%Session{}` exposes `automatic_tax == true | false`, and for enabled flows assert `amount_tax` is populated from the Fake payload. Preserve existing hosted and embedded checkout coverage; add focused tax cases instead of replacing current tests.</action>
  <acceptance_criteria>
    - `accrue/test/accrue/checkout_test.exs` contains the string `automatic_tax: true`
    - `accrue/test/accrue/checkout_test.exs` contains the string `automatic_tax: false`
    - `accrue/test/accrue/checkout_test.exs` asserts against `amount_tax`
    - `cd accrue && mix test test/accrue/checkout_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/checkout_test.exs</automated>
  </verify>
  <done>Checkout tests prove enabled and disabled automatic-tax flows are observable and remain compatible with existing session behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host checkout call -> `Accrue.Checkout.Session.create/1` | Untrusted caller input enters checkout creation |
| checkout projection -> returned `%Accrue.Checkout.Session{}` | Processor payload becomes host-visible observable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | T | `accrue/lib/accrue/checkout/session.ex` | mitigate | Accept only a flat boolean `automatic_tax` option and construct the Stripe-shaped nested map inside `build_stripe_params/1`. |
| T-18-11 | I | `accrue/lib/accrue/checkout/session.ex` | mitigate | Project only boolean state and numeric tax amount into the struct; keep sensitive provider payload details inside `data` without new logging. |
| T-18-12 | R | `accrue/test/accrue/checkout_test.exs` | mitigate | Add explicit enabled/disabled assertions so invalid or missing automatic-tax state is observable in tests even though recovery UX is deferred to Phase 19. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/checkout_test.exs`.
</verification>

<success_criteria>
`Accrue.Checkout.Session.create/1` accepts `automatic_tax: true | false`, returned checkout sessions expose `automatic_tax` and `amount_tax`, and checkout tests cover enabled and disabled paths.
</success_criteria>

<output>
After completion, create `.planning/phases/18-stripe-tax-core/18-04-SUMMARY.md`
</output>

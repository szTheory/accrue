---
phase: "180"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js
  - .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt
autonomous: true
requirements:
  - AUD-03

must_haves:
  truths:
    - "A zero-dependency Node script exists at artifacts/contrast.js and exits 0 when run"
    - "artifacts/contrast-table.txt contains computed WCAG 2.x ratios for all 21 Accrue palette pairs"
    - "Every row in contrast-table.txt matches the pre-verified values in RESEARCH.md"
    - "The script uses the WCAG 2.0 normative threshold 0.03928 (not 0.04045)"
  artifacts:
    - path: ".planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js"
      provides: "WCAG 2.x contrast ratio computation for the 7-color Accrue palette"
      min_lines: 28
    - path: ".planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt"
      provides: "Computed palette contrast ratios — every audit claim must cite a row here"
      contains: "Accrue palette WCAG 2.x contrast ratios"
  key_links:
    - from: "BRAND-AUDIT.md palette verdict entries"
      to: "artifacts/contrast-table.txt"
      via: "row-name citation in each TIGHTEN verdict"
      pattern: "contrast-table.txt"
---

<objective>
Create the WCAG 2.x contrast script and run it to produce the committed evidence table that all subsequent audit palette verdicts will cite.

Purpose: AUD-03 requires that every palette or font verdict cites a concrete, computable failure. The contrast script + its output table are the single source of truth for all palette claims in BRAND-AUDIT.md. They must exist before the audit authoring plans begin so that Plans 2 and 3 can cite row names by reference.

Output: `artifacts/contrast.js` (commit-ready, zero dependencies) and `artifacts/contrast-table.txt` (generated output, committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/180-brand-audit-dna-lock/180-CONTEXT.md
@.planning/phases/180-brand-audit-dna-lock/180-RESEARCH.md
@.planning/phases/180-brand-audit-dna-lock/180-PATTERNS.md
</context>

<interfaces>
<!-- Key contract for downstream plans to cite. These row names are canonical. -->
<!-- Every TIGHTEN verdict in BRAND-AUDIT.md must cite: see `artifacts/contrast-table.txt` row "X vs Y" -->

Expected contrast-table.txt row format (per RESEARCH.md lines 409–431):
  Ink vs Slate: 1.37:1  [FAIL]
  Ink vs Fog: 15.81:1  [AAA]
  Ink vs Paper: 17.83:1  [AAA]
  Ink vs Moss: 5.89:1  [AA-body]
  Ink vs Cobalt: 4.86:1  [AA-body]
  Ink vs Amber: 6.71:1  [AA-body]
  Slate vs Fog: 11.52:1  [AAA]
  Slate vs Paper: 12.99:1  [AAA]
  Slate vs Moss: 4.29:1  [AA-large]
  Slate vs Cobalt: 3.54:1  [AA-large]
  Slate vs Amber: 4.89:1  [AA-body]
  Fog vs Paper: 1.13:1  [FAIL]
  Fog vs Moss: 2.68:1  [FAIL]
  Fog vs Cobalt: 3.25:1  [AA-large]
  Fog vs Amber: 2.36:1  [FAIL]
  Paper vs Moss: 3.03:1  [AA-large]
  Paper vs Cobalt: 3.66:1  [AA-large]
  Paper vs Amber: 2.66:1  [FAIL]
  Moss vs Cobalt: 1.21:1  [FAIL]
  Moss vs Amber: 1.14:1  [FAIL]
  Cobalt vs Amber: 1.38:1  [FAIL]

WCAG level thresholds used by the script:
  r >= 7.0  → 'AAA'
  r >= 4.5  → 'AA-body'
  r >= 3.0  → 'AA-large'
  else      → 'FAIL'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write and commit artifacts/contrast.js</name>
  <files>.planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js</files>
  <read_first>
    - .planning/phases/180-brand-audit-dna-lock/180-RESEARCH.md — lines 582–629 contain the exact commit-ready CJS script; copy verbatim
    - .planning/phases/180-brand-audit-dna-lock/180-PATTERNS.md — lines 24–103 confirm the zero-dependency CJS form (no ESM imports needed) and the exact PALETTE entries
    - .planning/phases/180-brand-audit-dna-lock/180-CONTEXT.md — D-04 specifies "~30-line zero-dependency WCAG 2.x contrast script" and the exact threshold 0.03928
  </read_first>
  <action>
    Create the directory `.planning/phases/180-brand-audit-dna-lock/artifacts/` and write `contrast.js` there. Use the exact script from RESEARCH.md lines 582–629 — this is the normative form verified against W3C WCAG 2.0 Techniques G17.

    Critical invariants to preserve exactly:
    - The linearize threshold is `0.03928` (not 0.04045) — per WCAG 2.0 normative formula
    - The PALETTE object contains exactly 7 entries: Ink `#111418`, Slate `#24303B`, Fog `#E9EEF2`, Paper `#FAFBFC`, Moss `#5E9E84`, Cobalt `#5D79F6`, Amber `#C8923B`
    - The level thresholds are: `>= 7.0` → `'AAA'`, `>= 4.5` → `'AA-body'`, `>= 3.0` → `'AA-large'`, else `'FAIL'`
    - The header line is `'Accrue palette WCAG 2.x contrast ratios (threshold: 0.03928)'`
    - Output format per row: `${a} vs ${b}: ${r.toFixed(2)}:1  [${level}]`
    - The script is CJS (no `import` statements, no `__dirname` setup) — it takes no file inputs and writes only to stdout
    - The comment header cites: `Source: W3C WCAG 2.0 Techniques G17 + WCAG 2.1 Understanding SC 1.4.3`
  </action>
  <verify>
    <automated>node .planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js | grep -c "vs " | grep -q "21" && echo "21 pairs present" || echo "FAIL: wrong pair count"</automated>
  </verify>
  <done>artifacts/contrast.js exists, runs without error, emits exactly 21 "X vs Y" rows</done>
</task>

<task type="auto">
  <name>Task 2: Run script and commit contrast-table.txt</name>
  <files>.planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt</files>
  <read_first>
    - .planning/phases/180-brand-audit-dna-lock/180-PATTERNS.md — lines 121–149 show the expected full output of contrast-table.txt verbatim; the committed file must match these values exactly
    - .planning/phases/180-brand-audit-dna-lock/180-RESEARCH.md — lines 409–431 show the pre-verified contrast matrix; cross-check output against this table
  </read_first>
  <action>
    Run `node .planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js` and redirect the stdout to `.planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt`.

    After writing the file, verify correctness by spot-checking four key rows that future audit TIGHTEN verdicts will cite:
    - "Paper vs Moss" row must read `3.03:1  [AA-large]`
    - "Paper vs Amber" row must read `2.66:1  [FAIL]`
    - "Fog vs Moss" row must read `2.68:1  [FAIL]`
    - "Ink vs Paper" row must read `17.83:1  [AAA]`

    If any row does not match, the script has a bug — fix contrast.js before committing.

    The file header line must be: `Accrue palette WCAG 2.x contrast ratios (threshold: 0.03928)`
    Total rows (including header + divider): 23 lines (1 header + 1 divider + 21 pair rows).
  </action>
  <verify>
    <automated>grep -q "Paper vs Moss: 3.03:1" .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt && grep -q "Paper vs Amber: 2.66:1" .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt && grep -q "Fog vs Moss: 2.68:1" .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt && grep -q "Ink vs Paper: 17.83:1" .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt && echo "All key rows match" || echo "FAIL: row mismatch"</automated>
  </verify>
  <done>contrast-table.txt exists with correct values for all 21 pairs; the 4 key audit-referenced rows match pre-verified values exactly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script inputs | contrast.js takes no external input; PALETTE is hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-01 | Tampering | contrast-table.txt (committed output) | accept | File is generated from the committed script; any tampering is detectable by re-running `node contrast.js` and diffing; no sensitive data involved |
| T-180-02 | Information Disclosure | prompts/accrue-brand-book.md | accept | Gitignored seed never committed; contrast.js hardcodes only palette hex values (not the brand book content) — no disclosure path |
| T-180-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; script uses zero external dependencies |
</threat_model>

<verification>
After both tasks complete:
- `node .planning/phases/180-brand-audit-dna-lock/artifacts/contrast.js` exits 0 with no errors
- `wc -l .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt` reports 23 lines
- `grep -c "vs " .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt` reports 21
- "Paper vs Moss: 3.03:1  [AA-large]" row present (D-06 key finding)
- "Amber vs Paper: 2.66:1  [FAIL]" row present — NOTE: script outputs "Paper vs Amber" not "Amber vs Paper" (iteration order is alphabetical/insertion order); accept either form as long as the ratio is 2.66:1
</verification>

<success_criteria>
- artifacts/contrast.js committed; zero external npm dependencies
- artifacts/contrast-table.txt committed; 21 pair rows, values match pre-verified RESEARCH.md matrix
- Plans 2 and 3 can reference `artifacts/contrast-table.txt` row names as the canonical evidence source for all palette verdict citations
- AUD-03 evidence infrastructure is in place before any audit section is authored
</success_criteria>

<output>
Create `.planning/phases/180-brand-audit-dna-lock/180-01-SUMMARY.md` when done
</output>

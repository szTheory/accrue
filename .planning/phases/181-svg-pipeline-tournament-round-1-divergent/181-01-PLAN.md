---
phase: "181-svg-pipeline-tournament-round-1-divergent"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/package.json
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/geist-spine.mjs
autonomous: false

requirements:
  - LOGO-01

user_setup: []

must_haves:
  truths:
    - "Running `node harness/geist-spine.mjs --test` exits 0 and prints `[geist-spine] smoke: OK`"
    - "Geist font loads from the `geist` npm TTF path OR falls back to wawoff2 with a logged warning"
    - "extractGlyphs(font, 'accrue') returns exactly 6 elements (one per character, verified by assertion)"
    - "All 5 new npm packages have been verified against their GitHub repos and download counts by the user before install"
  artifacts:
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/package.json"
      provides: "Harness Node project declaration — opentype.js, pngjs, svgo, geist, wawoff2, @xmldom/xmldom, playwright"
      contains: '"name": "accrue-logo-harness"'
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/geist-spine.mjs"
      provides: "Geist font loading + per-glyph path extraction"
      exports: ["loadGeistFont", "extractGlyphs"]
  key_links:
    - from: "harness/geist-spine.mjs"
      to: "geist npm package dist/fonts/geist-sans/GeistSans-Regular.ttf"
      via: "require.resolve() + opentype.parse()"
      pattern: "GeistSans-Regular.ttf"
    - from: "harness/geist-spine.mjs"
      to: "accrue_admin/priv/static/fonts/geist-sans-vf.woff2"
      via: "wawoff2.decompress() fallback"
      pattern: "loadFromWoff2"
---

<objective>
Bootstrap the harness Node project: verify package legitimacy, install dependencies, and build the Geist font loading + glyph path extraction spine that every downstream generator and lint depends on.

Purpose: Phase 182's parametric variation rounds and Phase 183's production pipeline both depend on reproducible Geist outline extraction. The font spine is the single point of truth for letterform geometry — get it right here so no downstream plan re-litigates coordinate math.

Output: `harness/package.json` (locked deps), `harness/geist-spine.mjs` (exports `loadGeistFont()` + `extractGlyphs()`, with smoke test), and a committed `node_modules` lockfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-CONTEXT.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-RESEARCH.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-PATTERNS.md
@.planning/phases/180-brand-audit-dna-lock/logo-brief.md
@.planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md
</context>

<interfaces>
<!-- Key patterns extracted from codebase — executor must not re-read these -->

From accrue_admin/package.json (shape to copy):
```json
{
  "name": "accrue-admin-e2e",
  "private": true,
  "scripts": { "score-visuals": "node e2e/score-visuals.mjs" },
  "devDependencies": { "@playwright/test": "^1.57.0" }
}
```

From accrue_admin/e2e/score-visuals.mjs (ESM header pattern all scripts must copy):
```javascript
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";
const __dirname = path.dirname(fileURLToPath(import.meta.url));
```

From RESEARCH.md Pattern 1 (opentype.js font loading — critical flipY note):
```javascript
// PRIMARY: require.resolve('geist/dist/fonts/geist-sans/GeistSans-Regular.ttf')
// FALLBACK: wawoff2.decompress(readFileSync(woff2Path))
// CRITICAL: path.toPathData({ decimalPlaces: 3, flipY: false })
// font.getPath() already flips y — passing flipY:true (default) double-flips
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify package legitimacy before npm install</name>
  <read_first>
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-RESEARCH.md (Package Legitimacy Audit section — all 5 packages are tagged [ASSUMED]; verify before any install)
  </read_first>
  <what-built>
    The research file lists 5 npm packages for manual verification: `opentype.js`, `pngjs`, `svgo`, `geist`, `wawoff2`. All are tagged [ASSUMED] because slopcheck was unavailable at research time. No install has run yet.
  </what-built>
  <how-to-verify>
    For each of the 5 packages, open the listed URL and confirm: (1) the package exists at that exact name, (2) it has significant weekly downloads and/or a credible maintainer org, (3) the GitHub repo matches what the research says. Quick checks:
    - `opentype.js` → https://www.npmjs.com/package/opentype.js (expect: ~757k+ weekly downloads, opentypejs org)
    - `pngjs` → https://www.npmjs.com/package/pngjs (expect: pngjs/pngjs, wide use in Playwright ecosystem)
    - `svgo` → https://www.npmjs.com/package/svgo (expect: svg/svgo, industry-standard, 2M+ weekly downloads)
    - `geist` → https://www.npmjs.com/package/geist (expect: vercel org, contains dist/fonts/geist-sans/ TTF files)
    - `wawoff2` → https://www.npmjs.com/package/wawoff2 (expect: fontello org, WebAssembly WOFF2 decompressor)

    Also confirm `@xmldom/xmldom` (already widely used in SVG tooling — less scrutiny needed, but check exists at https://www.npmjs.com/package/@xmldom/xmldom).
  </how-to-verify>
  <resume-signal>Type "packages verified" after completing the checks, or describe any concern found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bootstrap harness package.json and geist-spine.mjs</name>
  <read_first>
    - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-RESEARCH.md (Standard Stack section, Pattern 1 opentype.js font loading, Pattern 2 wawoff2 fallback, Pitfall 1 flipY double-flip, Pitfall 2 variable font unreliability, Environment Availability section)
    - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-PATTERNS.md (harness/package.json section, harness/geist-spine.mjs section, Node CLI Script Structure shared pattern, ESM Module Pattern for pure helpers)
    - accrue_admin/package.json (shape reference — private:true, type:module, scripts shape)
    - accrue_admin/e2e/score-visuals.mjs lines 1-50 (ESM header, __dirname shim, bracketed log prefix conventions)
    - accrue_admin/priv/static/fonts/ (confirm geist-sans-vf.woff2 exists for fallback path)
  </read_first>
  <action>
    Create `.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/` directory.

    Create `harness/package.json` with:
    - `"name": "accrue-logo-harness"`, `"private": true`, `"type": "module"` (all scripts are .mjs ESM)
    - Scripts: `"generate": "node generate.mjs"`, `"smoke": "node generate.mjs --smoke"`, `"lint-test": "node lint.mjs --test"`, `"spine-test": "node geist-spine.mjs --test"`
    - `"dependencies"` (not devDependencies — these are runtime for the harness): `"opentype.js": "^2.0.0"`, `"pngjs": "^7.0.0"`, `"svgo": "^4.0.1"`, `"geist": "^1.7.2"`, `"wawoff2": "^2.0.1"`, `"@xmldom/xmldom": "^0.9.0"`, `"playwright": "^1.59.1"` (pinned to match accrue_admin installed version; do NOT run `playwright install chromium` — Chromium is already cached at ~/Library/Caches/ms-playwright/chromium-1223 from accrue_admin's install; if it fails to locate, add a note to run `cd accrue_admin && npx playwright install chromium` first)

    Run `npm install` from inside the `harness/` directory to generate `package-lock.json`.

    Create `harness/geist-spine.mjs` as a pure ESM module (no `main()`, no `await main()` at bottom — exports only). Follow the ESM header pattern from score-visuals.mjs: JSDoc header, ESM imports, `__dirname` shim via `fileURLToPath`. Use `createRequire(import.meta.url)` for `require.resolve()`.

    The module exports two named functions:
    - `export async function loadGeistFont()` — primary: `require.resolve('geist/dist/fonts/geist-sans/GeistSans-Regular.ttf')` + `opentype.parse(fs.readFileSync(ttfPath).buffer)`; catch block falls through to wawoff2 fallback loading the in-repo variable font at `../../../../accrue_admin/priv/static/fonts/geist-sans-vf.woff2` relative to `__dirname` (resolve path with `path.resolve(__dirname, '../../../../accrue_admin/priv/static/fonts/geist-sans-vf.woff2')`); log `[geist-spine] geist npm TTF not found — falling back to wawoff2` via `console.warn` on fallback; throw if both paths fail.
    - `export function extractGlyphs(font, text, fontSize = 1000)` — calls `font.getPaths(text, 0, 0, fontSize)`, maps to `{ char, d, advanceWidth, xMin, yMin, xMax, yMax }`. CRITICAL: `toPathData({ decimalPlaces: 3, flipY: false })` — `flipY: false` is mandatory because `font.getPaths()` already performs the Cartesian→SVG y-axis flip internally (opentype.js Issue #724). Also export `export function getCapHeight(font)` returning `font.tables.os2?.sCapHeight ?? 700` (the `os2.sCapHeight` value at `unitsPerEm=1000` gives gap-ratio lint its reference).

    Add a smoke-test early-exit at the bottom of the file (before any other execution):
    ```
    if (process.argv.includes('--test')) { ... }
    ```
    The smoke test: calls `loadGeistFont()`, then `extractGlyphs(font, 'accrue')`, asserts `glyphs.length === 6` (per Assumption A4 — if ligatures fire, this catches it), logs `[geist-spine] smoke: OK`, exits 0. If assertion fails, log the actual length and a note about ligatures and exit 1.

    After creating both files, run from the harness directory:
    `node geist-spine.mjs --test`

    If it fails because `geist` TTF path resolution fails under `require.resolve` in ESM context, use `import.meta.resolve()` or explicitly construct the path via `path.join(path.dirname(fileURLToPath(import.meta.url)), 'node_modules/geist/dist/fonts/geist-sans/GeistSans-Regular.ttf')` — the `node_modules/geist` is in `harness/node_modules/` after install.
  </action>
  <verify>
    <automated>cd .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness && node geist-spine.mjs --test</automated>
  </verify>
  <acceptance_criteria>
    - `harness/package.json` exists with `"type": "module"`, `"name": "accrue-logo-harness"`, and all 7 listed dependencies under `"dependencies"`
    - `harness/package-lock.json` exists (npm install ran)
    - `harness/node_modules/opentype.js/` exists
    - `harness/node_modules/geist/dist/fonts/geist-sans/GeistSans-Regular.ttf` exists (verify with `ls`)
    - `node geist-spine.mjs --test` exits 0 and prints exactly `[geist-spine] smoke: OK`
    - `harness/geist-spine.mjs` exports `loadGeistFont`, `extractGlyphs`, `getCapHeight` (grep: `export async function loadGeistFont`, `export function extractGlyphs`, `export function getCapHeight`)
    - `flipY: false` appears in `geist-spine.mjs` (grep: `flipY: false`)
  </acceptance_criteria>
  <done>Harness Node project bootstrapped, all 7 deps installed, geist-spine.mjs smoke test passes (6 glyphs extracted from "accrue" with correct orientation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → harness/node_modules | Untrusted package code enters the developer's machine; supply chain attack surface |
| harness script → filesystem | All output paths constructed from __dirname-anchored constants, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-01 | Tampering | npm install of 5 new packages | mitigate | Blocking human checkpoint (Task 1) verifies each package against GitHub repo and download count before first install |
| T-181-02 | Tampering | geist-spine.mjs path construction | accept | All paths are __dirname-anchored constants; no user input reaches path construction |
| T-181-SC | Tampering | npm/geist/opentype.js install | mitigate | Human-verify gate (Task 1) is blocking for all [ASSUMED] packages; cannot auto-advance |
</threat_model>

<verification>
- `node geist-spine.mjs --test` exits 0 with `[geist-spine] smoke: OK`
- `harness/node_modules/geist/dist/fonts/geist-sans/GeistSans-Regular.ttf` exists
- `grep -c "flipY: false" harness/geist-spine.mjs` returns >= 1
</verification>

<success_criteria>
Harness package.json committed with all 7 dependencies. geist-spine.mjs smoke test passes: 6 glyphs extracted from "accrue", correct SVG orientation (flipY:false enforced), capHeight readable from os2 table. Package legitimacy verified by user for all 5 [ASSUMED] packages.
</success_criteria>

<output>
Create `.planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-01-SUMMARY.md` when done.
</output>

---
phase: 182-tournament-convergent-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/build-gallery.mjs
  - .planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs
autonomous: true
requirements: [LOGO-03]

must_haves:
  truths:
    - "The four harness scripts (generate.mjs, lint.mjs, render-matrix.mjs, build-gallery.mjs) accept --output-dir and write all artifacts to the specified directory instead of the 181 phase dir"
    - "assembleLockup() accepts accentPathD + palette.accentFill and overlays the accent path in Moss on top of the base Ink mark without violating the coordinate-space contract"
    - "b-step-r2.mjs declares the 7-candidate R2_CONFIGS array and generate() emits accentPathD when accentStep:true"
    - "lintCandidate() uses monoSvgString override so Moss-colored SVGs are linted against their declared mono mapping instead of the color SVG"
    - "All four REVIEW findings in scope (WR-01, WR-05, WR-07, IN-04d) are fixed"
  artifacts:
    - path: ".planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs"
      provides: "Round 2 candidate configs (R2-1..R2-7) + extended generate() with accentPathD"
      exports: ["R2_CONFIGS", "generate"]
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs"
      provides: "Two-tone mark support via accentPathD + palette.accentFill"
      contains: "accentPathD"
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs"
      provides: "Parameterized --output-dir; buildMonoSvg(); IN-01 dead-assignment fix"
      contains: "--output-dir"
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs"
      provides: "monoSvgString override for color-variant lint; WR-05 jsdoc corrected"
      contains: "monoSvgString"
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs"
      provides: "--output-dir; Moss INK_DARK_COLOR_MAP entry; WR-07 avatar-circle clip; WR-01 smoke no-clobber"
      contains: "#5E9E84"
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/build-gallery.mjs"
      provides: "--output-dir; --gallery-name; Round 2 section headers; color-treatment grouping"
      contains: "--gallery-name"
  key_links:
    - from: ".planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs"
      to: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs"
      via: "accentPathD field consumed by assembleLockup"
      pattern: "accentPathD"
    - from: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs"
      to: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs"
      via: "lintCandidate(candidate) where candidate.monoSvgString is populated for color variants"
      pattern: "monoSvgString"
---

<objective>
Adapt the Phase 181 harness for Round 2: add --output-dir parameterization to the four path-dependent scripts, extend assembleLockup for two-tone marks, create the b-step-r2.mjs config module with the 7 Round 2 candidates, fix the four in-scope REVIEW warnings, and wire the mono-derivation proof pipeline for Moss-colored variants.

Purpose: This is the prerequisite infrastructure plan. The Round 2 pipeline run (plan 182-02) cannot execute without these changes. Every modification is surgical and evidence-gated from RESEARCH.md and PATTERNS.md.

Output: Five modified harness files and one new generator config file, all committed. A smoke test validates the --output-dir wiring before full pipeline execution is attempted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-06-SUMMARY.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/181-07-SUMMARY.md
@.planning/phases/182-tournament-convergent-refinement/182-CONTEXT.md
@.planning/phases/182-tournament-convergent-refinement/182-RESEARCH.md
@.planning/phases/182-tournament-convergent-refinement/182-PATTERNS.md

<interfaces>
<!-- Read these files before any edits. Extract the exact line numbers of the sections you will modify. -->
<!-- Do NOT re-read a file after editing — edit results are tracked. -->

From .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/b-step.mjs (analog for b-step-r2.mjs):
  - R2_CONFIGS must extend B1/B4 config shape with: id, steps, stepHeight, stepWidth, curvature, colorTreatment ("ink"|"moss"|"two-tone"), monoMap ({} for ink; {"#5E9E84":"#818181"} for Moss variants), accentStep (boolean), rationale
  - generate(config) return shape: { markPathD, accentPathD?, markWidth, markHeight }
  - accentPathD = pathParts[steps - 1] when config.accentStep === true (top/rightmost step)
  - BASE_UNIT = 40; NaN guard: if (markPathD.includes('NaN')) throw

From .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs (CRITICAL coordinate contract):
  - Default palette: { ink: "#111418", paper: "#FAFBFC" } — WR-05 fix: change default to { ink: "#181818", paper: "#FAFBFC" }
  - Mark <g> block: translate(markTx,markTy) scale(s) — accent path goes INSIDE this <g>, inheriting the same transform
  - DO NOT add per-element translate loops (Fix 3 lesson)
  - accentPathD + palette.accentFill conditional overlay pattern from PATTERNS.md lines 157-164

From .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs (PHASE_DIR pattern):
  - PHASE_DIR hardcoded via path.resolve(__dirname, "..") — replace with argOutputDir ?? path.resolve(__dirname, "..")
  - argOutputDir pattern: process.argv.indexOf("--output-dir") → next arg → path.resolve()
  - buildStandardCandidate() must thread accentPathD + monoSvgString through
  - buildMonoSvg(svgString, monoMap): replaceAll loop; returns undefined for empty monoMap
  - IN-01 fix: replace dead `failed = rawCandidates.length - (rawCandidates.length - culled)` with `const failed = culled`

From .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs:
  - lintMonochromeDeriv() at lines ~129-146: correct jsdoc; logic unchanged
  - lintCandidate(): replace mono lint invocation with monoSvgString override pattern
  - WR-05 jsdoc fix text from PATTERNS.md lines 318-323
  - Also apply --output-dir parameterization to this file's PHASE_DIR

From .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs:
  - INK_DARK_COLOR_MAP: add "#5E9E84": "#5E9E84" identity entry
  - WR-07 fix: circleStyle border-radius:50% in buildTileHtml(); screenshot target = page.locator("body") for avatar-circle tile
  - WR-01 fix: in smoke-mode surviving filter, read fullIndex from disk before filtering
  - IN-04a fix: update blank-render guard comment from "paper-light or 32px-favicon" to "paper-light"
  - Also apply --output-dir parameterization

From .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/build-gallery.mjs:
  - Add --gallery-name CLI arg parallel to --output-dir
  - GALLERY_PATH = path.join(PHASE_DIR, argGalleryName ?? "round-1-gallery.html")
  - Group candidates by colorTreatment (ink / moss / two-tone) instead of direction letter for Round 2
  - Update <title>, h1, paste-instruction copy to say "Round 2"
  - VERDICT_JS: change "## Round 1" to "## Round 2", update constraints placeholder label
  - Also apply --output-dir parameterization
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create b-step-r2.mjs + extend assemble-lockup.mjs for two-tone marks</name>
  <files>
    .planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs
  </files>

  <read_first>
    Read these files FIRST before writing anything:
    1. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/dirs/b-step.mjs — full file; extract: CONFIGS array shape, generate() implementation (pathParts loop, NaN guard, return shape), IN-04d stale comment location
    2. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs — full file; identify: default palette line, mark <g> block (the three lines with translate/path/close-g), coordinate-space contract header
  </read_first>

  <action>
    Step 1 — Create .planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs.

    This file exports R2_CONFIGS (array, 7 entries) and generate(config) (the identical pathParts loop from b-step.mjs, extended with accentStep support). Do NOT import from b-step.mjs — self-contained.

    R2_CONFIGS array (exact values, in this order):
    1. { id:"R2-1", steps:6, stepHeight:0.18, stepWidth:0.18, curvature:0, colorTreatment:"ink", monoMap:{}, accentStep:false, rationale:"B4 exact — ink baseline; user primary pick from Round 1" }
    2. { id:"R2-2", steps:5, stepHeight:0.22, stepWidth:0.22, curvature:0, colorTreatment:"ink", monoMap:{}, accentStep:false, rationale:"B4 chunkier 5-step — 16px weakness fix; wider bars reduce column-merge at favicon scale" }
    3. { id:"R2-3", steps:4, stepHeight:0.22, stepWidth:0.26, curvature:0, colorTreatment:"ink", monoMap:{}, accentStep:false, rationale:"4-wide steps — B1 step-count with B4 crispness; no rounding" }
    4. { id:"R2-4", steps:4, stepHeight:0.25, stepWidth:0.25, curvature:0.05, colorTreatment:"ink", monoMap:{}, accentStep:false, rationale:"B1 exact — runner-up baseline; strongest 16px favicon in Round 1 self-review" }
    5. { id:"R2-5", steps:5, stepHeight:0.22, stepWidth:0.22, curvature:0, colorTreatment:"moss", monoMap:{"#5E9E84":"#818181"}, accentStep:false, rationale:"B4 chunkier 5-step with full Moss mark — primary color test; may fail 16px on paper-light (BRAND-DNA: Moss large-text only on light surfaces)" }
    6. { id:"R2-6", steps:5, stepHeight:0.22, stepWidth:0.22, curvature:0, colorTreatment:"two-tone", monoMap:{"#5E9E84":"#818181"}, accentStep:true, rationale:"B4 chunkier 5-step with Ink base + Moss accent on top step — subtler color; passes 16px on paper-light (base fill is #181818)" }
    7. { id:"R2-7", steps:4, stepHeight:0.25, stepWidth:0.25, curvature:0.05, colorTreatment:"two-tone", monoMap:{"#5E9E84":"#818181"}, accentStep:true, rationale:"B1 exact with Ink base + Moss accent on top step — B1 robustness at all sizes, with brand-color accent" }

    generate(config) implementation:
    - Copy the pathParts loop verbatim from b-step.mjs (steps, stepHeight, stepWidth, curvature → sw, sh, markWidth, markHeight, rr, pathParts)
    - IN-04d fix: in the curvature branch, replace the stale comment "top corners only" (if present) with "full rounded rect (all 4 corners)" — apply while editing
    - After the loop, NaN guard: if (markPathD.includes('NaN')) throw new Error(...)
    - accentStep extension: if (config.accentStep) { const accentPathD = pathParts[steps - 1]; return { markPathD, accentPathD, markWidth, markHeight }; }
    - Default return: { markPathD, markWidth, markHeight }
    - Export: export { R2_CONFIGS, generate };

    Step 2 — Edit .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs.

    Two surgical edits only:
    A. WR-05 default palette fix: find the default palette declaration (currently `{ ink: "#111418", paper: "#FAFBFC" }`) and change `"#111418"` to `"#181818"`. This is a default only; explicit callers in generate.mjs who already pass `"#181818"` are unaffected.
    B. Two-tone mark overlay: find the mark <g> block (three lines: opening translate/scale g, the single path element with fill="${palette.ink}", closing </g>). After the base path line, add a conditional accent path line:
       (config.accentPathD && palette.accentFill) ? `    <path d="${config.accentPathD}" fill="${palette.accentFill}"/>` : ""
       The accent path is inside the same <g> (inherits the same translate+scale) — this is the coordinate-space contract; DO NOT add a new transform.
    C. Update the @param jsdoc for assembleLockup config to add: accentPathD?: string, and palette.accentFill?: string. Keep all other jsdoc unchanged.

    Mkdir .planning/phases/182-tournament-convergent-refinement/harness/dirs/ before writing.
  </action>

  <verify>
    <automated>
      # b-step-r2.mjs exists with 7 configs and exports
      node -e "
        import('.planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs').then(m => {
          console.assert(m.R2_CONFIGS.length === 7, 'expected 7 configs');
          const r26 = m.R2_CONFIGS.find(c => c.id === 'R2-6');
          const { markPathD, accentPathD } = m.generate(r26);
          console.assert(accentPathD !== undefined, 'R2-6 accentStep:true must emit accentPathD');
          console.assert(!markPathD.includes('NaN'), 'no NaN in markPathD');
          const r21 = m.R2_CONFIGS.find(c => c.id === 'R2-1');
          const r21gen = m.generate(r21);
          console.assert(r21gen.accentPathD === undefined, 'R2-1 accentStep:false must not emit accentPathD');
          console.log('b-step-r2.mjs OK');
        }).catch(e => { console.error(e); process.exit(1); });
      "
      # assemble-lockup.mjs default palette fix
      grep -c '"#181818"' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs
      # (count >= 1 — verifies the default was changed; exact count is not critical)
      grep -v '#111418' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs | grep -c 'ink.*"#' || true
      # accentPathD conditional present
      grep -c 'accentPathD' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs
    </automated>
  </verify>

  <acceptance_criteria>
    - .planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs exists
    - `node -e "import('.../b-step-r2.mjs').then(m => { console.assert(m.R2_CONFIGS.length === 7); console.assert(m.generate(m.R2_CONFIGS[5]).accentPathD !== undefined); console.log('ok'); })"` prints "ok"
    - assemble-lockup.mjs default palette line contains "#181818" (not "#111418")
    - assemble-lockup.mjs contains "accentPathD" at least twice (jsdoc param + conditional render line)
    - No new transform attributes added to the mark <g> (grep for "transform" in the mark <g> block — only the original translate+scale remains)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Parameterize generate.mjs + lint.mjs (--output-dir, mono-lint override, REVIEW fixes)</name>
  <files>
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs
  </files>

  <read_first>
    Read these files FIRST before any edits:
    1. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs — full file; locate: PHASE_DIR line, buildStandardCandidate() function, buildLockupSvg() function, the summary "failed = ..." line (IN-01), the SMOKE constant, the direction-floor logic
    2. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs — full file; locate: PHASE_DIR line, lintMonochromeDeriv() jsdoc + implementation, lintCandidate() mono-lint invocation
  </read_first>

  <action>
    Edit generate.mjs — four changes:

    A. --output-dir parameterization (RESEARCH Finding 1 pattern):
    After the `const __dirname = path.dirname(fileURLToPath(import.meta.url));` line, insert:
    ```
    const argOutputDir = (() => {
      const i = process.argv.indexOf("--output-dir");
      return i !== -1 ? path.resolve(process.argv[i + 1]) : null;
    })();
    const PHASE_DIR = argOutputDir ?? path.resolve(__dirname, "..");
    ```
    Remove (or comment out) the original `const PHASE_DIR = path.resolve(__dirname, "..");` line. All downstream path joins (CANDIDATES_DIR, REJECTED_DIR, SCREENSHOTS_DIR, LINT_LOG_PATH) derive from PHASE_DIR and need no further change.

    B. buildLockupSvg() extension — thread accentPathD and color treatment:
    Extend the function to derive ink color from config.colorTreatment:
    - colorTreatment "moss" → ink = "#5E9E84"
    - colorTreatment "two-tone" or "ink" (or undefined) → ink = "#181818"
    Derive accentFill = (config.colorTreatment === "two-tone") ? "#5E9E84" : undefined.
    Pass accentPathD (from generator output) and palette: { ink, paper: "#FAFBFC", accentFill } to assembleLockup.
    The function signature becomes: buildLockupSvg(markPathD, accentPathD, glyphs, markWidth, markHeight, capHeight, config).

    C. buildMonoSvg(svgString, monoMap) helper — new function after buildLockupSvg:
    ```
    function buildMonoSvg(svgString, monoMap) {
      if (!monoMap || Object.keys(monoMap).length === 0) return undefined;
      let mono = svgString;
      for (const [from, to] of Object.entries(monoMap)) {
        mono = mono.replaceAll(from, to);
      }
      return mono;
    }
    ```

    D. buildStandardCandidate() — destructure accentPathD from generator output; call buildMonoSvg(lockupSvg, config.monoMap); include both monoSvgString and accentPathD in the returned candidate object.

    E. IN-01 fix: find the dead assignment in the final summary section (`failed = rawCandidates.length - (rawCandidates.length - culled)` or equivalent) and replace with `const failed = culled;` (or just use `culled` directly in the log line).

    Edit lint.mjs — three changes:

    A. --output-dir parameterization: same two-line argOutputDir pattern after __dirname line; replace the existing PHASE_DIR line.

    B. lintMonochromeDeriv() jsdoc fix (WR-05): Change the second sentence of the jsdoc that claims "#111418, #24303B, #FAFBFC, #E9EEF2 all have saturation < 0.15" to:
    "NOTE: #111418 (sat≈0.29) and #24303B (sat≈0.39) exceed this threshold. Generated SVGs use #181818 (sat=0) for Ink. Color variants (e.g. Moss #5E9E84, sat≈0.40) must pass this lint via their mono-derived SVG (monoSvgString override), not the color SVG itself."
    The implementation logic (sat > 0.15 check) is CORRECT — do not change it.

    C. lintCandidate() mono-lint override (RESEARCH Finding 3 / PATTERNS.md lines 326-333):
    Find the line that calls lintMonochromeDeriv(svgString) for the "monochrome-derivable" check. Replace it with:
    ```
    const svgForMonoLint = candidate.monoSvgString ?? svgString;
    const monoPassed = lintMonochromeDeriv(svgForMonoLint);
    ```
    Update the record() call's failure message to: "saturated fill — if color variant, check monoMap config produces a low-saturation SVG".
    Add monoSvgString?: string to the @param jsdoc of lintCandidate.
  </action>

  <verify>
    <automated>
      # --output-dir accepted by generate.mjs (dry-run: no output-dir supplied → uses 181 default; with bogus path it should at least parse args)
      node -e "
        // Check argOutputDir pattern is present
        const fs = await import('fs');
        const src = fs.default.readFileSync('.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/generate.mjs', 'utf8');
        console.assert(src.includes('--output-dir'), 'generate.mjs missing --output-dir');
        console.assert(src.includes('buildMonoSvg'), 'generate.mjs missing buildMonoSvg helper');
        console.assert(src.includes('monoSvgString'), 'generate.mjs missing monoSvgString threading');
        console.log('generate.mjs checks OK');
      " --input-type=module
      # lint.mjs checks
      node -e "
        const fs = await import('fs');
        const src = fs.default.readFileSync('.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs', 'utf8');
        console.assert(src.includes('--output-dir'), 'lint.mjs missing --output-dir');
        console.assert(src.includes('monoSvgString'), 'lint.mjs missing monoSvgString override');
        console.assert(!src.includes('#111418, #24303B, #FAFBFC'), 'WR-05 jsdoc not fixed');
        console.log('lint.mjs checks OK');
      " --input-type=module
    </automated>
  </verify>

  <acceptance_criteria>
    - generate.mjs contains "argOutputDir" and "--output-dir" (grep confirms)
    - generate.mjs contains "buildMonoSvg" function definition
    - generate.mjs contains "monoSvgString" in the returned candidate object of buildStandardCandidate
    - generate.mjs does NOT contain the dead "failed = rawCandidates.length - (rawCandidates.length - culled)" assignment
    - lint.mjs contains "argOutputDir" and "--output-dir"
    - lint.mjs contains "monoSvgString ?? svgString" (the override pattern)
    - lint.mjs lintMonochromeDeriv jsdoc no longer claims "#111418" has sat < 0.15
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Parameterize render-matrix.mjs + build-gallery.mjs (--output-dir, WR-01, WR-07, Moss color map, Round 2 gallery format)</name>
  <files>
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/build-gallery.mjs
  </files>

  <read_first>
    Read these files FIRST before any edits:
    1. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs — full file; locate: PHASE_DIR line, INK_DARK_COLOR_MAP, TILES array, buildTileHtml() function, renderTile() or the page.locator("svg") screenshot call, the smoke-mode surviving filter, blank-render guard comment
    2. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/build-gallery.mjs — full file; locate: PHASE_DIR line, GALLERY_PATH line, renderCandidate() function, buildGallery() direction grouping loop, VERDICT_JS text with "Round 1", <title> and h1 text, CANDIDATES_DIR / SCREENSHOTS_DIR derivations
  </read_first>

  <action>
    Edit render-matrix.mjs — five changes:

    A. --output-dir parameterization: same argOutputDir two-line pattern after __dirname; replace PHASE_DIR line.

    B. INK_DARK_COLOR_MAP (RESEARCH Finding 5 / PATTERNS.md lines 363-379): Add the Moss identity entry:
    ```
    "#5E9E84": "#5E9E84",   // Moss → Moss (identity; 5.89:1 on Ink-dark per contrast-table.txt; no swap needed)
    ```

    C. WR-07 avatar-circle clip (PATTERNS.md lines 410-427):
    In buildTileHtml() (or equivalent tile HTML builder), add a circleStyle variable:
    ```
    const circleStyle = tile.id === "avatar-circle"
      ? `<style>body { border-radius: 50%; overflow: hidden; }</style>`
      : "";
    ```
    Include circleStyle in the returned HTML <head>. Then in the screenshot call, change the locator for avatar-circle from `page.locator("svg")` to `page.locator("body")` so the circular clip is captured in the screenshot:
    ```
    const screenshotTarget = tile.id === "avatar-circle"
      ? page.locator("body")
      : page.locator("svg");
    await screenshotTarget.screenshot({ path: outputPath });
    ```

    D. WR-01 smoke no-clobber (PATTERNS.md lines 430-437): In the smoke-mode surviving filter, before filtering by culledIds, read the full candidate list from the on-disk index.json:
    ```
    const fullIndex = JSON.parse(fs.readFileSync(indexPath, "utf8"));
    let surviving = fullIndex.filter(c => !culledIds.has(c.id));
    ```
    (Rather than filtering the in-memory `candidates` array which may be the smoke subset.)

    E. IN-04a fix: Update the blank-render guard comment from "paper-light or 32px-favicon tile" (or equivalent) to "paper-light tile" — the guard only checks the paper-light tile.

    Edit build-gallery.mjs — five changes:

    A. --output-dir parameterization + --gallery-name: same argOutputDir pattern; add argGalleryName:
    ```
    const argGalleryName = (() => {
      const i = process.argv.indexOf("--gallery-name");
      return i !== -1 ? process.argv[i + 1] : null;
    })();
    const GALLERY_PATH = path.join(PHASE_DIR, argGalleryName ?? "round-1-gallery.html");
    ```

    B. Color-treatment grouping in buildGallery() (PATTERNS.md lines 505-521): Replace (or supplement) the direction-letter section loop with a colorTreatment grouping. For Round 2 where all candidates are direction B, group by candidate.colorTreatment into three sections: "ink" (section header: "Ink — Monochrome Baseline"), "moss" (section header: "Full Moss (#5E9E84)"), "two-tone" (section header: "Two-tone: Ink + Moss Accent"). Render sections in order: ink → moss → two-tone. If a candidate has no colorTreatment field, fall back to the existing direction-letter grouping (backward compatibility for Round 1 galleries).

    C. Gallery <title>, h1, instruction text: Change occurrences of "Round 1" to use a dynamic round variable (derive round number from argGalleryName if present: "round-2-gallery.html" → "Round 2"; default to "Round 1" for backward compatibility).

    D. VERDICT_JS round heading: Change the hardcoded "## Round 1" string to use the same dynamic round label ("## Round 2" when invoked with --gallery-name round-2-gallery.html).

    E. data-direction badge (renderCandidate function): When a candidate has a colorTreatment field, display "Color: {colorTreatment}" instead of "Direction {direction}" in the badge element.
  </action>

  <verify>
    <automated>
      # render-matrix.mjs checks
      node -e "
        const fs = await import('fs');
        const src = fs.default.readFileSync('.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs', 'utf8');
        console.assert(src.includes('--output-dir'), 'render-matrix missing --output-dir');
        console.assert(src.includes('#5E9E84'), 'render-matrix missing Moss INK_DARK_COLOR_MAP entry');
        console.assert(src.includes('border-radius: 50%'), 'WR-07 avatar-circle not fixed');
        console.assert(src.includes('fullIndex'), 'WR-01 smoke no-clobber not fixed');
        console.log('render-matrix.mjs checks OK');
      " --input-type=module
      # build-gallery.mjs checks
      node -e "
        const fs = await import('fs');
        const src = fs.default.readFileSync('.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/build-gallery.mjs', 'utf8');
        console.assert(src.includes('--output-dir'), 'build-gallery missing --output-dir');
        console.assert(src.includes('--gallery-name'), 'build-gallery missing --gallery-name');
        console.assert(src.includes('colorTreatment'), 'build-gallery missing colorTreatment grouping');
        console.log('build-gallery.mjs checks OK');
      " --input-type=module
    </automated>
  </verify>

  <acceptance_criteria>
    - render-matrix.mjs contains "argOutputDir" and "--output-dir"
    - render-matrix.mjs INK_DARK_COLOR_MAP contains "#5E9E84": "#5E9E84" (Moss identity entry)
    - render-matrix.mjs contains "border-radius: 50%" (WR-07 circle clip)
    - render-matrix.mjs contains "fullIndex" (WR-01 smoke no-clobber)
    - build-gallery.mjs contains "argOutputDir" and "--gallery-name"
    - build-gallery.mjs contains "colorTreatment" grouping logic
    - A smoke-mode invocation of render-matrix.mjs with a non-existent output dir exits with a clear error rather than silently clobbering the 181 phase dir: `node .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs --output-dir /tmp/nonexistent-182 2>&1 | grep -i "error\|ENOENT\|no such"` (any error message confirms the path is honoured)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-system → harness scripts | Harness reads from .planning/ which is repo-local; no network, no user-supplied paths at runtime except --output-dir CLI arg |
| SVG source → inlined gallery | Generated SVGs are from our own parametric generator; no external SVG ingestion |
| --output-dir CLI arg | Path is resolved via path.resolve(); no traversal risk beyond filesystem permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Tampering | TOURNAMENT.md monotonic ledger | mitigate | Plan 182-02 appends ONLY at <!-- ROUND-2-APPEND-BELOW --> marker; plan instructions explicitly prohibit modifying Round 1 block; executor grep-verifies marker is intact after append |
| T-182-02 | Tampering | assemble-lockup.mjs coordinate contract | mitigate | accentPathD overlay is inside the existing mark <g> (inherits transform); no new translate added; acceptance criteria grep for "transform" in mark <g> block |
| T-182-03 | Information Disclosure | SVG inlined in gallery | accept | SVGs contain only path geometry data (no PII, no secrets); file:// gallery has no network access; no script tags in generated SVGs (generator emits path elements only) |
| T-182-04 | Denial of Service | Playwright tmp HTML cleanup | mitigate | render-matrix.mjs already uses try/finally to delete _tmp_{tileId}.html; WR-07 edit preserves this pattern |
| T-182-05 | Tampering | --output-dir path traversal | accept | path.resolve() normalizes the path; harness only writes to subdirs of PHASE_DIR (candidates/, screenshots/, rejected/); no symlink-following logic added |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | No new npm installs this plan; 181 node_modules is reused in-place; no new packages introduced |
</threat_model>

<verification>
After all three tasks complete:

1. Smoke test the full harness wiring end-to-end (no Playwright needed at this stage):
   ```bash
   HARNESS=.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness
   OUTPUT=.planning/phases/182-tournament-convergent-refinement
   # generate.mjs smoke (1 candidate, no render):
   node $HARNESS/generate.mjs --output-dir $OUTPUT --smoke 2>&1 | tail -5
   # Expect: "smoke: 1 candidate" or similar; no NaN errors; lint-results.ndjson created in 182 dir
   ls $OUTPUT/candidates/index.json && echo "index.json OK"
   ls $OUTPUT/lint-results.ndjson && echo "lint-results.ndjson OK"
   ```

2. Verify no files written to the 181 dir from the 182 smoke run:
   ```bash
   # candidates/index.json in 181 dir should still list 16 Round 1 candidates (unchanged):
   node -e "const f = JSON.parse(require('fs').readFileSync('.planning/phases/181-svg-pipeline-tournament-round-1-divergent/candidates/index.json','utf8')); console.assert(f.length === 16, 'Round 1 index corrupted'); console.log('Round 1 index intact:', f.length, 'candidates');"
   ```
   Note: use `require` or ESM import as appropriate for the Node version.

3. REVIEW fix coverage:
   ```bash
   grep -c 'fullIndex' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs  # WR-01 ≥ 1
   grep -c '#5E9E84' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs    # WR-07 via Moss map ≥ 1
   grep -c 'border-radius' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/render-matrix.mjs  # WR-07 circle ≥ 1
   grep -c 'monoSvgString' .planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/lint.mjs       # WR-05 ≥ 1
   grep -c 'accentPathD' .planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs        # IN-04d context ≥ 1
   ```
</verification>

<success_criteria>
- b-step-r2.mjs exports R2_CONFIGS (7 entries) and generate(); R2-6 and R2-7 emit accentPathD; R2-1..R2-4 do not
- assemble-lockup.mjs default palette is #181818; accentPathD conditional overlay present inside mark <g>
- All four harness scripts accept --output-dir and write to the specified directory
- build-gallery.mjs accepts --gallery-name
- lintCandidate() uses monoSvgString ?? svgString for the mono-derivability lint
- WR-01 (smoke no-clobber), WR-05 (jsdoc fixed), WR-07 (avatar-circle clip), IN-04d (b-step comment), IN-01 (dead assignment), IN-04a (blank-render comment) all resolved
- Smoke run of generate.mjs --output-dir .../182 produces index.json in the 182 dir without modifying any file in the 181 dir
</success_criteria>

<output>
Create `.planning/phases/182-tournament-convergent-refinement/182-01-SUMMARY.md` when done.
</output>

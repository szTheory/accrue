---
phase: 182-tournament-convergent-refinement
plan: 03
type: execute
wave: 3
depends_on: ["182-02"]
files_modified:
  - .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md
autonomous: true
requirements: [LOGO-03]

must_haves:
  truths:
    - "The final TOURNAMENT.md entry records the locked winner's candidate ID and the full generator config object (all fields Phase 183 needs to regenerate the mark geometry deterministically)"
    - "The WINNER LOCKED section is present and correctly positioned relative to the Round 2 verdict block"
    - "The Round 1 block (R1-C1..R1-C4) remains byte-identical"
    - "Phase 183 can read this file and immediately know the production mark geometry without opening any other file"
  artifacts:
    - path: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md"
      provides: "WINNER LOCKED entry with candidate ID + full generator config + frozen geometry"
      contains: "WINNER LOCKED"
    - path: ".planning/phases/182-tournament-convergent-refinement/182-FREEZE.md"
      provides: "Phase 183 consumption artifact — winner config + geometry spec in a single standalone file"
      min_lines: 20
  key_links:
    - from: ".planning/phases/182-tournament-convergent-refinement/182-FREEZE.md"
      to: ".planning/phases/183-logo-system-production"
      via: "Phase 183 planner reads this file to know the locked winner's generator config and geometry"
      pattern: "stepHeight"
---

<objective>
Verify the winner freeze is complete and create the Phase 183 consumption artifact: a standalone 182-FREEZE.md that captures the locked winner's generator config, assembled geometry dimensions, color treatment, and monoMap — everything Phase 183 needs to derive the production logo system without opening the full TOURNAMENT.md.

Purpose: This plan executes only on the LOCK/SETTLE path from plan 182-02. If plan 182-02 ended with EXTEND, this plan is skipped and the planner is re-invoked for Round 3 instead. This plan closes LOGO-03 by ensuring the monotonic ledger is complete and the Phase 183 handoff is clean.

Output: Verified TOURNAMENT.md WINNER LOCKED entry + 182-FREEZE.md Phase 183 consumption artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md
@.planning/phases/182-tournament-convergent-refinement/182-CONTEXT.md

<interfaces>
<!-- Read TOURNAMENT.md FIRST to extract the exact winner ID and geometry config before writing 182-FREEZE.md -->

Phase 183 needs from 182-FREEZE.md:
  - Winner candidate ID (e.g. "R2-6")
  - Full generator config object: { id, steps, stepHeight, stepWidth, curvature, colorTreatment, monoMap, accentStep }
  - Computed geometry at BASE_UNIT=40: markWidth (steps × stepWidth × 40), markHeight (steps × stepHeight × 40)
  - Color fills: ink hex, accentFill hex (for two-tone), monoMap mapping
  - Logotype: "accrue" in Geist (locked, R1-C3), gapRatio: 0.15, viewboxH: capHeight × 1.4
  - Lint status: all lints passed (or which lints were accepted/culled with reason)
  - Self-review scores: the 4-dimension scores for the winning candidate from self-review-r2.ndjson
  - Locked date and round (Round 2, 2026-06-{date})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify winner freeze in TOURNAMENT.md + create 182-FREEZE.md</name>
  <files>
    .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md
    .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
  </files>

  <read_first>
    Read the following before writing anything:
    1. .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md — full file; verify:
       - Round 1 block intact (R1-C1..R1-C4, "Winners: B4", "ROUND-2-APPEND-BELOW" marker)
       - Round 2 block present with Winners, Killed, verbatim quote, per-winner sections, R2-C constraints
       - WINNER LOCKED section present with full geometry config object
       - No modification to lines before the ROUND-2-APPEND-BELOW marker
    2. .planning/phases/182-tournament-convergent-refinement/self-review-r2.ndjson — extract scores for the winning candidate ID
    3. .planning/phases/182-tournament-convergent-refinement/candidates/index.json — extract the winning candidate's full config object (to cross-check with TOURNAMENT.md entry)
  </read_first>

  <action>
    Step 1 — Verify TOURNAMENT.md integrity:

    Grep-check all invariants:
    - `grep -c "R1-C1" TOURNAMENT.md` ≥ 1
    - `grep -c "R1-C2" TOURNAMENT.md` ≥ 1
    - `grep -c "R1-C3" TOURNAMENT.md` ≥ 1
    - `grep -c "R1-C4" TOURNAMENT.md` ≥ 1
    - `grep -c "Winners.*B4" TOURNAMENT.md` ≥ 1 (Round 1 line)
    - `grep -c "WINNER LOCKED" TOURNAMENT.md` ≥ 1
    - `grep -c "ROUND-2-APPEND-BELOW" TOURNAMENT.md` = 1 (marker intact, not duplicated)

    If any check fails: STOP. Do not write 182-FREEZE.md. Report which invariant failed and the current TOURNAMENT.md content around the failure point.

    Step 2 — Extract winner config from TOURNAMENT.md WINNER LOCKED section.

    Read the geometry config object from the ## WINNER LOCKED section. Cross-reference with candidates/index.json entry for the same ID. Both must agree on: steps, stepHeight, stepWidth, curvature, colorTreatment, accentStep.

    Step 3 — Compute derived geometry (for Phase 183 reference):

    BASE_UNIT = 40
    sw = stepWidth × 40
    sh = stepHeight × 40
    markWidth = steps × sw
    markHeight = steps × sh
    rr = Math.min(sw, sh) × curvature  (0 for non-curved variants)

    Step 4 — Extract self-review scores for the winner from self-review-r2.ndjson.

    Step 5 — Write .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md:

    ```markdown
    # Phase 182 Winner Freeze

    **Locked:** {date}
    **Winner:** {R2-N}
    **Round:** Round 2

    ## Generator Config (Phase 183 authoritative source)

    ```js
    // b-step-r2.mjs config for the locked winner
    {
      id: "{R2-N}",
      steps: {N},
      stepHeight: {N},
      stepWidth: {N},
      curvature: {N},
      colorTreatment: "{ink|moss|two-tone}",
      monoMap: {{"#5E9E84": "#818181"} or {}},
      accentStep: {true|false},
      rationale: "{config rationale string}"
    }
    ```

    ## Computed Geometry (at BASE_UNIT=40)

    | Property | Value |
    |----------|-------|
    | Step width (sw) | {stepWidth × 40} units |
    | Step height (sh) | {stepHeight × 40} units |
    | Mark width | {steps × sw} units |
    | Mark height | {steps × sh} units |
    | Corner radius (rr) | {rr} units |
    | Scale factor (s) | capHeight / markHeight (computed at render time from Geist) |

    ## Color Fills

    | Element | Color |
    |---------|-------|
    | Mark base (ink) | {ink hex} |
    | Mark accent (accentFill) | {accentFill hex or "N/A"} |
    | Logotype | #181818 (greyscale; sat=0; passes lintMonochromeDeriv) |
    | Paper background | #FAFBFC |
    | monoMap | {hex} → {grey hex} |

    ## Lockup Parameters

    - gapRatio: 0.15 (mark-to-logotype gap as fraction of cap height)
    - viewboxH: capHeight × 1.4
    - markIsTypemark: false
    - Logotype: "accrue" in Geist (R1-C3 — locked)

    ## Self-Review Scores (from self-review-r2.ndjson)

    | Dimension | Score | Note |
    |-----------|-------|------|
    | legibility-16px | {0-3} | {note} |
    | monochrome-survival | {0-3} | {note} |
    | avatar-crop-integrity | {0-3} | {note} |
    | brand-fit | {0-3} | {note} |

    ## Lint Status

    All pre-gate lints passed:
    - valid-svg: PASS
    - no-rect-background: PASS
    - gap-ratio: PASS
    - 16px-legibility: {PASS or note if R2-5 was culled and a different candidate was chosen}
    - monochrome-derivable: PASS (linted on mono-derived SVG via monoMap)
    - no-subtitle: PASS

    ## Phase 183 Instructions

    Phase 183 reads this file to:
    1. Import `generate` from `harness/dirs/b-step-r2.mjs` and call `generate({steps, stepHeight, stepWidth, curvature, accentStep})` to obtain `{ markPathD, accentPathD, markWidth, markHeight }`
    2. Load Geist via `geist-spine.mjs` and extract "accrue" glyphs
    3. Call `assembleLockup(markPathD, glyphs, { markWidth, markHeight, capHeight, gapRatio: 0.15, viewboxH: capHeight*1.4, markIsTypemark: false, palette: { ink: "{ink hex}", paper: "#FAFBFC", accentFill: "{accentFill or undefined}" }, accentPathD })` to produce the master lockup SVG
    4. Apply `svgo` optimization and outline all paths (no text elements)
    5. Derive the full logo system (see Phase 183 plan for the complete file set)

    The `b-step-r2.mjs` generator and the 181 harness assemble-lockup.mjs are the SSOT for mark geometry. Phase 183 MUST NOT hand-edit the path data — regenerate from the config.

    ---
    *Phase 182 closed. Phase 183: Logo System Production.*
    ```

    Step 6 — Commit both files:
    ```bash
    git add .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md \
            .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
    git commit -m "feat(182-03): freeze winner — ${WINNER_ID}; 182-FREEZE.md for Phase 183"
    ```
  </action>

  <verify>
    <automated>
      # TOURNAMENT.md integrity
      grep -c "R1-C1" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md
      grep -c "WINNER LOCKED" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md
      grep -c "ROUND-2-APPEND-BELOW" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md

      # 182-FREEZE.md exists and has key fields
      grep -c "Generator Config" .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
      grep -c "stepHeight" .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
      grep -c "Phase 183 Instructions" .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
      grep -c "assembleLockup" .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
    </automated>
  </verify>

  <acceptance_criteria>
    - All TOURNAMENT.md grep checks pass (R1-C1 through R1-C4: ≥ 1 each; WINNER LOCKED: ≥ 1; ROUND-2-APPEND-BELOW: exactly 1)
    - TOURNAMENT.md "Winners: B4 (primary)" still present in Round 1 block
    - 182-FREEZE.md exists with: Generator Config section, Computed Geometry table, Color Fills table, Self-Review Scores table, Phase 183 Instructions section
    - 182-FREEZE.md contains "stepHeight" (confirming the geometry config is present)
    - 182-FREEZE.md contains "assembleLockup" (confirming Phase 183 instructions are complete)
    - 182-FREEZE.md "Phase 183 Instructions" section provides a 5-step numbered list for how to regenerate the mark from the config
    - Commit exists with "182-FREEZE.md" in the changed files list
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOURNAMENT.md read → 182-FREEZE.md write | Agent reads the user-ratified verdict and transcribes geometry; no external input |
| 182-FREEZE.md → Phase 183 | Phase 183 will import harness code and regenerate SVGs; freeze file is a spec document only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-11 | Tampering | TOURNAMENT.md Round 1 block post-write | mitigate | Verification step re-greps for R1-C1..R1-C4 after all writes complete; any failure blocks commit |
| T-182-12 | Repudiation | 182-FREEZE.md geometry config correctness | mitigate | Config values cross-checked against candidates/index.json for the winning candidate ID before writing; discrepancy blocks commit |
| T-182-13 | Information Disclosure | 182-FREEZE.md in public repo | accept | Contains only SVG geometry parameters and brand color hex values — no PII, no secrets, no credentials |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | No installs this plan; read-only operations |
</threat_model>

<verification>
Final phase verification — all four ROADMAP success criteria for Phase 182:

SC-1 (6–9 variants in context matrix):
```bash
grep -c 'class="candidate"' .planning/phases/182-tournament-convergent-refinement/round-2-gallery.html  # 6-9
ls .planning/phases/182-tournament-convergent-refinement/screenshots/ | wc -l  # one dir per candidate
```

SC-2 (monotonic ledger — no R1 constraint re-litigated):
```bash
grep -c "R1-C1" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md  # ≥ 1
grep -c "R1-C4" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md  # ≥ 1
grep -c "## Round 2" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md  # ≥ 1
grep -c "Winners.*B4.*runner-up\|Winners.*B4.*B1" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md  # ≥ 1
```

SC-3 (loop concluded with winner locked, or settle/extend question posed if not):
```bash
grep -c "WINNER LOCKED" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md  # ≥ 1 on LOCK path
```

SC-4 (final entry records winner ID + frozen geometry):
```bash
grep -c "stepHeight" .planning/phases/181-svg-pipeline-tournament-round-1-divergent/TOURNAMENT.md  # ≥ 1
grep -c "stepHeight" .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md  # ≥ 1
grep -c "Phase 183" .planning/phases/182-tournament-convergent-refinement/182-FREEZE.md  # ≥ 1
```
</verification>

<success_criteria>
- TOURNAMENT.md has a complete Round 2 verdict block + WINNER LOCKED entry with full geometry config
- All four R1-C constraints (R1-C1..R1-C4) are intact in TOURNAMENT.md Round 1 block
- 182-FREEZE.md is a standalone Phase 183 consumption document containing: winner ID, generator config, computed geometry, color fills, self-review scores, lint status, and Phase 183 regeneration instructions
- Phase 182 LOGO-03 requirement is satisfied: refinement rounds iterated on winners via a monotonic TOURNAMENT.md feedback ledger; user locked one winner; default 3-round cap with explicit settle-or-extend question is encoded in the checkpoint protocol
</success_criteria>

<output>
Create `.planning/phases/182-tournament-convergent-refinement/182-03-SUMMARY.md` when done.

Update ROADMAP.md Phase 182 Plans section to reflect the final plan count and mark each plan complete.
</output>

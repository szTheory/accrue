---
phase: 183-logo-system-production
plan: 02
type: execute
wave: 2
depends_on:
  - 183-01
files_modified:
  - brandbook/logo/harness/generate-logo-suite.mjs
  - brandbook/logo/accrue-logo.svg
  - brandbook/logo/accrue-logo-on-dark.svg
  - brandbook/logo/accrue-logo-subtitle.svg
  - brandbook/logo/accrue-wordmark.svg
  - brandbook/logo/accrue-mark.svg
  - brandbook/logo/accrue-mark-on-dark.svg
  - brandbook/logo/accrue-logo-mono.svg
  - brandbook/logo/accrue-logo-mono-inverse.svg
  - brandbook/logo/accrue-mark-mono.svg
  - brandbook/logo/accrue-mark-mono-inverse.svg
  - brandbook/logo/accrue-clearspace.svg
  - brandbook/logo/accrue-social-card.svg
  - brandbook/logo/favicon.svg
autonomous: false
requirements:
  - LOGO-04
must_haves:
  truths:
    - "brandbook/logo/ contains all 13 named SVG files including favicon.svg"
    - "D-02/D-03: every committed SVG uses the locked naming grammar — project-prefixed accrue-<role>[-<modifier>].svg, kebab-case, role in {logo, wordmark, mark}, modifiers ordered tone→background (-mono, -mono-inverse, -on-dark); light-bg full-color is the default (no -on-light file is emitted)"
    - "No committed SVG contains <text>, @font-face, or style/@import with a font reference"
    - "Every SVG contains a <title> and <desc> element"
    - "accrue-logo-subtitle.svg contains the exact string 'Billing for Elixir apps' (rendered as paths, not text nodes)"
    - "accrue-logo-mono.svg and accrue-mark-mono.svg use #818181 for the accent step (not #5E9E84)"
    - "The executor Read the rendered PNG previews (via Playwright) and confirmed the mark renders with visible ink — not blank"
  artifacts:
    - path: "brandbook/logo/harness/generate-logo-suite.mjs"
      provides: "Main orchestrator — generates all 13 SVG brand artifacts"
      exports: []
    - path: "brandbook/logo/accrue-logo.svg"
      provides: "PRIMARY lockup (mark + wordmark, light bg, full color)"
      contains: "<title>Accrue logo</title>"
    - path: "brandbook/logo/accrue-logo-subtitle.svg"
      provides: "With-subtitle lockup — subtitle = 'Billing for Elixir apps'"
      contains: "<title>Accrue logo with descriptor</title>"
    - path: "brandbook/logo/accrue-social-card.svg"
      provides: "1200×630 social card SVG"
      contains: "<title>Accrue social card</title>"
    - path: "brandbook/logo/favicon.svg"
      provides: "Outlined mark-only SVG (bare filename per D-04)"
      contains: "<title>Accrue favicon</title>"
  key_links:
    - from: "brandbook/logo/harness/generate-logo-suite.mjs"
      to: ".planning/phases/182-tournament-convergent-refinement/harness/dirs/b-step-r2.mjs"
      via: "dynamic import path.join(HARNESS_182_DIR, 'dirs/b-step-r2.mjs')"
      pattern: "generate\\(\\{steps: 4"
    - from: "brandbook/logo/harness/generate-logo-suite.mjs"
      to: ".planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs"
      via: "dynamic import HARNESS_181_DIR"
      pattern: "assembleLockup"
    - from: "brandbook/logo/harness/generate-logo-suite.mjs"
      to: "brandbook/logo/harness/geist-spine-mono.mjs"
      via: "dynamic import for subtitle glyph extraction"
      pattern: "loadGeistMonoFont"
---

<objective>
Write `generate-logo-suite.mjs` — the main SVG production orchestrator — and run it to
commit all 13 SVG brand artifacts to `brandbook/logo/`. Includes a BLOCKING visual-fidelity
checkpoint where the executor reads rendered PNG previews to confirm no blank-render.

Purpose: This is the core of LOGO-04 — the complete outlined-path SVG suite with accessible
metadata, svgo-optimized, deterministically generated from the frozen R2-7 config.
Output: 13 committed SVGs at brandbook/logo/ plus generate-logo-suite.mjs.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-01-SUMMARY.md

<interfaces>
<!-- Coordinate contract — executor must honor exactly. -->

From assemble-lockup.mjs header (the 181 coordinate-space contract):
  1. extractGlyphs() bakes ABSOLUTE X positions into each glyph path, baseline at y=0
  2. ALL glyph paths go inside ONE <g transform="translate(markScaledWidth+gap, BASELINE)">
  3. Mark scaled: s = capHeight / markHeight; placed at (0, BASELINE - capHeight)
  4. NEVER add per-glyph translate loops — paths already carry x layout
  5. BASELINE = capHeight * 1.1 inside viewboxH = capHeight * 1.4

Frozen R2-7 generator call (from 182-FREEZE.md):
  const { markPathD, accentPathD, markWidth, markHeight } = generate({
    steps: 4, stepHeight: 0.25, stepWidth: 0.25, curvature: 0.05, accentStep: true
  });
  // markWidth: 40, markHeight: 40, viewBox: "0 0 40 40"

assembleLockup call signature for R2-7 (from 183-PATTERNS.md):
  assembleLockup(markPathD, glyphs, {
    markWidth, markHeight, capHeight, gapRatio: 0.15, viewboxH: capHeight * 1.4,
    markIsTypemark: false, accentPathD,
    palette: { ink: "#181818", paper: "#FAFBFC", accentFill: "#5E9E84" }
  })
  // Returns string SVG or { svg: string } — check typeof before using

Mark-only SVG pattern (from 183-PATTERNS.md):
  `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 40 40">
     <path d="${markPathD}" fill="#181818"/>
     <path d="${accentPathD}" fill="#5E9E84"/>
   </svg>`

On-dark color swap (from 183-PATTERNS.md):
  INK_DARK_COLOR_MAP = {
    "#181818": "#FAFBFC",  // ink → paper-visible on dark bg
    "#FAFBFC": "#111418",  // paper → dark surface bg
    "#5E9E84": "#5E9E84",  // Moss identity (5.89:1 on #111418)
  }

Mono derivation (buildMonoSvg from generate.mjs):
  monoMap = { "#5E9E84": "#818181" }
  import buildMonoSvg from 181 harness generate.mjs

Mono-inverse: additionally replace "#181818" → "#FAFBFC" on the mono result
  (light ink on dark bg; accent already collapsed to grey)

Dynamic import pattern (from 183-PATTERNS.md §generate-logo-suite.mjs):
  const HARNESS_181 = path.resolve(__dirname,
    "../../.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness");
  const HARNESS_182 = path.resolve(__dirname,
    "../../.planning/phases/182-tournament-convergent-refinement/harness");

SVG accessibility title values (from 183-PATTERNS.md §SVG Accessibility):
  accrue-logo.svg:              "Accrue logo"
  accrue-logo-on-dark.svg:      "Accrue logo, dark background"
  accrue-logo-subtitle.svg:     "Accrue logo with descriptor"
  accrue-wordmark.svg:          "Accrue wordmark"
  accrue-mark.svg:              "Accrue logomark"
  accrue-mark-on-dark.svg:      "Accrue logomark, dark background"
  accrue-logo-mono.svg:         "Accrue logo, monochrome"
  accrue-logo-mono-inverse.svg: "Accrue logo, monochrome inverse"
  accrue-mark-mono.svg:         "Accrue logomark, monochrome"
  accrue-mark-mono-inverse.svg: "Accrue logomark, monochrome inverse"
  accrue-clearspace.svg:        "Accrue logo clearspace diagram"
  accrue-social-card.svg:       "Accrue social card"
  favicon.svg:                  "Accrue favicon"

Frozen palette (from 182-FREEZE.md + 183-PATTERNS.md):
  ink: "#181818", paper: "#FAFBFC", accentFill: "#5E9E84",
  slate: "#3A4754" (subtitle only), inkDark: "#111418"
  monoMap: { "#5E9E84": "#818181" }

svgo invocation (programmatic, not CLI):
  import { optimize } from "svgo";
  import svgoConfig from "./svgo.config.mjs";
  const result = optimize(svgString, svgoConfig);
  const optimized = result.data;

Blank-render guard (from 183-PATTERNS.md — MANDATORY):
  const BLANK_RENDER_MIN_DARK_COVERAGE = 0.005; // 0.5%
  // Reads pngBuffer via pngjs, counts pixels with color-distance from Paper bg > 30
  // Throws if coverage < threshold — prevents blank-render SVGs from being committed

isMain guard (from 183-PATTERNS.md):
  if (process.argv[1] === fileURLToPath(import.meta.url)) { main().catch(...); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write generate-logo-suite.mjs — SVG production orchestrator</name>
  <files>brandbook/logo/harness/generate-logo-suite.mjs</files>
  <read_first>
    - /Users/jon/projects/accrue/.planning/phases/182-tournament-convergent-refinement/harness/generate-r2.mjs (full file — exact import pattern, isMain guard, error handling, mono derivation, on-dark swap)
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-PATTERNS.md (full file — every pattern excerpt is load-bearing)
    - /Users/jon/projects/accrue/.planning/phases/182-tournament-convergent-refinement/182-FREEZE.md (frozen generator config + lockup parameters)
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-CONTEXT.md §D-05/D-07 (subtitle string + typographic spec)
    - /Users/jon/projects/accrue/.planning/phases/181-svg-pipeline-tournament-round-1-divergent/harness/assemble-lockup.mjs (module header — coordinate contract)
  </read_first>
  <behavior>
    - Imports: path, fileURLToPath, fs (ESM header); dynamic imports for geist-spine.mjs,
      assemble-lockup.mjs, generate.mjs (buildMonoSvg) from HARNESS_181; dynamic import
      b-step-r2.mjs from HARNESS_182/dirs/; dynamic import geist-spine-mono.mjs from __dirname;
      static import of svgo optimize + svgoConfig
    - Calls generate({steps:4, stepHeight:0.25, stepWidth:0.25, curvature:0.05, accentStep:true})
      per the FROZEN config — NEVER modify these params
    - Loads Geist Regular, extracts "accrue" glyphs; loads Geist Mono, extracts
      "Billing for Elixir apps" glyphs (for subtitle)
    - Calls assembleLockup per the coordinate contract (see interfaces above)
    - Derives all 13 SVG variants in this order:
        1. accrue-logo.svg — master lockup, svgo optimized, inject <title>/<desc>
        2. accrue-logo-on-dark.svg — on-dark swap applied, dark-surface viewBox bg optional
        3. accrue-logo-subtitle.svg — master lockup + subtitle glyph group below wordmark
           (Geist Mono paths, cap-height 0.42× logotype, Slate #3A4754, +0.02em tracking
           expressed as spacing between glyphs at extraction time, left-aligned to wordmark
           left edge, baseline 0.55× capHeight below wordmark baseline)
        4. accrue-wordmark.svg — logotype only (mark stripped, just the "accrue" glyph paths)
        5. accrue-mark.svg — mark-only SVG (viewBox "0 0 40 40", two-tone)
        6. accrue-mark-on-dark.svg — mark-only with on-dark inversion
        7. accrue-logo-mono.svg — buildMonoSvg(lockupSvg, monoMap)
        8. accrue-logo-mono-inverse.svg — mono result + "#181818"→"#FAFBFC" swap for inverse
        9. accrue-mark-mono.svg — buildMonoSvg(markSvg, monoMap)
       10. accrue-mark-mono-inverse.svg — mark mono result + ink→paper swap
       11. accrue-clearspace.svg — mark or lockup with clearspace annotation guides
           (geometry-only: sh-unit margin rect outlines, <title> + <desc> stating the ratio
           rule in words: "Clearspace: one step-height on all four sides.")
       12. accrue-social-card.svg — 1200×630 viewBox, dark bg (#111418), wordmark centered,
           "Billing for Elixir apps" subtitle below, subtle grid/line motif consistent with
           brand book social-card direction
       13. favicon.svg — mark-only SVG passthrough (same as accrue-mark.svg content,
           bare filename per D-04)
    - Each SVG: inject <title> and <desc> BEFORE svgo optimization (svgo must not strip them
      — verify svgo.config.mjs excludes removeTitle/removeDesc), then apply svgo
    - Write all SVGs to brandbook/logo/ (not harness/)
    - isMain guard; error handling exits process.exit(1) on any FATAL
  </behavior>
  <action>
    Write `brandbook/logo/harness/generate-logo-suite.mjs`. Structure: (1) ESM imports header
    with __dirname setup; (2) HARNESS_181 and HARNESS_182 path constants; (3) dynamic imports
    of all primitives; (4) frozen R2-7 config call; (5) font/glyph loads for both Geist Regular
    and Geist Mono; (6) assembleLockup master call; (7) derive all 13 variants with explicit
    title/desc injection using the exact strings from the interfaces block; (8) write all to
    OUTPUT_DIR = path.resolve(__dirname, "../") (i.e., brandbook/logo/); (9) isMain guard.

    For the subtitle placement in accrue-logo-subtitle.svg: extract Geist Mono glyphs for
    "Billing for Elixir apps" at fontSize = capHeight * 0.42; place them in a <g> with
    translate(wordmarkLeftEdge, wordmarkBaseline + capHeight*0.55) — wordmarkLeftEdge is the
    mark-scaled-width + gap (same X as the wordmark group start). The tracking +0.02em at
    fontSize = capHeight*0.42 means adding approximately 0.02 * capHeight * 0.42 extra advance
    between each glyph when computing absolute X positions.

    For accrue-social-card.svg: viewBox "0 0 1200 630", rect fill="#111418" as background
    (note: this is the bg rect, NOT behind the mark — the mark floats on the dark surface,
    which is permitted; the hard constraint is no rect BEHIND the mark specifically as a
    container shape, but a full-bleed bg is standard social-card practice); wordmark and
    subtitle centered.

    For accrue-clearspace.svg: render the lockup SVG with guide rectangles (stroke only, no fill)
    showing the 1× step-height (sh) clearspace zone on all four sides. sh = markHeight * scale *
    (0.25) in lockup units. Desc: "Clearspace: one step-height on all four sides. Minimum sizes
    are documented in brandbook/README.md."

    Do NOT paste full SVG path data into the <action> — the generator produces it programmatically.
  </action>
  <verify>
    <automated>node /Users/jon/projects/accrue/brandbook/logo/harness/generate-logo-suite.mjs && ls /Users/jon/projects/accrue/brandbook/logo/*.svg | wc -l</automated>
  </verify>
  <done>Script exits 0 and produces 13 .svg files in brandbook/logo/. Zero errors in console output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING visual fidelity checkpoint — executor reads generated PNGs</name>
  <read_first>
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-PATTERNS.md §Blank-render guard (the 181 coordinate-space lesson — PNGs must be read, not just existence-checked)
    - /Users/jon/projects/accrue/.planning/phases/182-tournament-convergent-refinement/182-FREEZE.md §Self-Review Scores (what the winner looks like: 4 rounded steps, Ink base, Moss on topmost step)
  </read_first>
  <what-built>
    generate-logo-suite.mjs ran and produced 13 SVG files in brandbook/logo/. Before committing,
    the executor must render and READ the primary SVGs to confirm visual fidelity — not just
    that the files exist. The 181 tournament shipped a broken gallery because nobody looked at
    the PNGs. This checkpoint prevents a repeat.
  </what-built>
  <how-to-verify>
    The executor performs this verification autonomously using Playwright before presenting
    to the user:

    1. Run `cd /Users/jon/projects/accrue/brandbook/logo/harness && node -e "
       import('./size-matrix-qa.mjs').catch(() => {})
    "` — if size-matrix-qa.mjs doesn't exist yet (it's Plan 04), use a minimal Playwright
    inline script instead to screenshot accrue-logo.svg and accrue-mark.svg.

    2. Alternatively (simpler): use Playwright to screenshot just the two critical files
       inline. Open accrue-logo.svg in a browser page (file://) and take a screenshot;
       open accrue-mark.svg and take a screenshot. Read both PNG screenshots with the
       Read tool.

    3. Confirm by READING the PNG bytes (not just existence):
       - accrue-logo.svg screenshot: shows the 4-step mark (Ink base, Moss top step) + "accrue" wordmark side by side — NOT blank, NOT all-white, NOT a rotated/flipped glyph
       - accrue-mark.svg screenshot: shows the 4-step rounded mark with visible Moss accent on the topmost/rightmost step — NOT blank
       - accrue-logo-subtitle.svg screenshot: shows "accrue" + "Billing for Elixir apps" subtitle below in a noticeably smaller, lighter font — NOT a duplicate of the primary lockup

    4. Also grep-verify: `grep -c "<text\|@font-face\|font-family" /Users/jon/projects/accrue/brandbook/logo/*.svg` must return 0 for all files (no live text or font references).

    5. Verify <title> and <desc> present: `grep -c "<title>" /Users/jon/projects/accrue/brandbook/logo/accrue-logo.svg` must return 1.

    6. Verify mono color: `grep "#5E9E84" /Users/jon/projects/accrue/brandbook/logo/accrue-logo-mono.svg` must return empty (Moss collapsed to #818181 in mono variants).

    If any check fails: fix the generator and re-run before presenting to the user.
  </how-to-verify>
  <resume-signal>
    After executor self-review passes all checks, the user eyeballs the generator output.
    Open brandbook/logo/ in Finder or browser. Confirm accrue-logo.svg looks correct.
    Type "approved" to continue to raster generation, or describe any fidelity issues.
  </resume-signal>
  <verify>
    <human-check>User confirms the SVG suite renders correctly: mark is visible (not blank), wordmark is readable, subtitle variant shows "Billing for Elixir apps" below the lockup, mono variants show grey accent step not green.</human-check>
  </verify>
  <done>All grep checks pass, executor has read the PNG screenshots and confirmed fidelity, user types "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dynamic imports from 181/182 harness | Resolved via absolute path — wrong path = runtime error not silent failure |
| SVG path data from generator | Regenerated from frozen config — never hand-edited |
| svgo optimization | Could strip required attributes if config is wrong |
| Subtitle glyph placement | Incorrect font-unit math could misplace the subtitle (blank or overlapping) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-05 | Tampering | svgo stripping <title>/<desc> | mitigate | svgoConfig from Plan 01 explicitly excludes removeTitle/removeDesc; grep check in checkpoint verifies title present post-optimization |
| T-183-06 | Tampering | SVG path data hand-edit | mitigate | generate-logo-suite.mjs regenerates from b-step-r2.mjs FROZEN config; any path modification is detected by CI re-run `git diff --exit-code` (Plan 04) |
| T-183-07 | Information Disclosure | Geist letterform outlines | accept | OFL 1.1 permits; documented in LICENSE-FONTS.txt (Plan 04) |
| T-183-08 | Elevation of Privilege | Blank-render SVG committed without detection | mitigate | Mandatory BLOCKING checkpoint: executor reads rendered PNG screenshots before any commit; blank-render guard (0.5% dark-pixel threshold) added to generate-logo-suite.mjs; the 181 lesson is the explicit precedent |
| T-183-SC | Tampering | Dynamic imports from planning/ harness dirs | accept | Paths are absolute and resolved at runtime; wrong path = explicit Node resolution error, not silent data corruption |

**Severity assessment:** No high-severity threats. The most impactful risk (blank/incorrect render committed) is fully mitigated by the BLOCKING checkpoint.
</threat_model>

<verification>
After Task 1 and the checkpoint pass:
1. `ls /Users/jon/projects/accrue/brandbook/logo/*.svg | wc -l` returns 13
2. `grep -rl "<text\|@font-face" /Users/jon/projects/accrue/brandbook/logo/` returns empty
3. `grep -c "<title>" /Users/jon/projects/accrue/brandbook/logo/accrue-logo.svg` returns 1
4. `grep "#5E9E84" /Users/jon/projects/accrue/brandbook/logo/accrue-logo-mono.svg` returns empty
5. User has typed "approved" after eyeballing the SVG outputs
</verification>

<success_criteria>
- 13 SVG files committed at brandbook/logo/
- Zero <text> or @font-face in any committed SVG
- Every SVG has <title> and <desc>
- Mono variants contain #818181 not #5E9E84 for the accent step
- Executor READ rendered PNGs and confirmed non-blank renders
- User checkpoint passed
</success_criteria>

<output>
Create `.planning/phases/183-logo-system-production/183-02-SUMMARY.md` when done.
</output>

---
phase: 183-logo-system-production
plan: 04
type: execute
wave: 4
depends_on:
  - 183-03
files_modified:
  - brandbook/logo/harness/size-matrix-qa.mjs
  - brandbook/README.md
  - brandbook/LICENSE-FONTS.txt
autonomous: false
requirements:
  - LOGO-04
must_haves:
  truths:
    - "size-matrix-qa.mjs runs Playwright and produces a screenshot gallery of all SVG variants at representative sizes in both light and dark themes"
    - "The executor Read the Playwright screenshot output and confirmed all variants render with visible ink — not blank"
    - "brandbook/README.md contains: manifest table (filename/role/when-to-use/format/provenance), Logo usage section with min-size numbers (icon 16px, lockup 120px, subtitle-lockup 168px), clearspace ratio rule in prose, tagline-exclusion usage rule"
    - "brandbook/LICENSE-FONTS.txt documents OFL 1.1 provenance for Geist Sans and Geist Mono letterform outlines"
    - "git diff --exit-code over brandbook/logo/ exits 0 after re-running generate-logo-suite.mjs and generate-rasters.mjs (determinism gate)"
    - "du -sh brandbook/ reports under 2MB total"
  artifacts:
    - path: "brandbook/logo/harness/size-matrix-qa.mjs"
      provides: "Playwright size-matrix QA screenshot script — visual fidelity evidence (SC-4)"
    - path: "brandbook/README.md"
      provides: "Manifest table + logo usage rules + min-size SSOT + tagline exclusion rule"
      contains: "Logo usage"
    - path: "brandbook/LICENSE-FONTS.txt"
      provides: "OFL 1.1 + Geist provenance for outlined letterforms"
      contains: "OFL 1.1"
  key_links:
    - from: "brandbook/README.md"
      to: "brandbook/logo/"
      via: "manifest table links each filename to its role"
      pattern: "accrue-logo.svg"
    - from: "brandbook/README.md"
      to: "brandbook/logo/harness/"
      via: "Regenerating section instructs developers how to re-run the pipeline"
      pattern: "generate-logo-suite.mjs"
---

<objective>
Complete the Phase 183 deliverables:
1. `size-matrix-qa.mjs` — Playwright size-matrix QA screenshot gallery (SC-4 fidelity evidence)
2. `brandbook/README.md` — manifest, usage rules, min-size SSOT, tagline exclusion rule
3. `brandbook/LICENSE-FONTS.txt` — OFL 1.1 + Geist provenance for outlined letterforms
4. Determinism gate: re-run pipeline, assert `git diff --exit-code` over artifact dir

Purpose: These are the documentation and quality-gate deliverables that close LOGO-04.
The QA screenshot provides the SC-4 "eyeball checkpoint" evidence. README is the SSOT for
all min-size numbers (D-12/D-14). LICENSE-FONTS.txt closes SC-3 provenance requirement.
Output: 3 files + determinism assertion + human checkpoint for the derivative-sheet eyeball.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/182-tournament-convergent-refinement/182-FREEZE.md
@/Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-03-SUMMARY.md

<interfaces>
<!-- Key specs for docs and QA. -->

README "Logo usage" section SSOT numbers (from 183-CONTEXT.md D-14 — single source of truth):
  icon-only mark minimum: 16px (favicon floor; do not move)
  primary lockup minimum: 120px wide
  with-subtitle lockup minimum: 168px wide (below this, drop subtitle → use primary)
  Clearspace: one step-height (sh = ¼ of mark height) on all four sides

README manifest table columns (per D-01):
  | Filename | Role | When to use | Format | Provenance |
  Rows: all 21 files in brandbook/logo/ (13 SVGs + 8 rasters)

Tagline exclusion rule wording (per D-06):
  The tagline "Billing state, modeled clearly." is editorial typography only —
  it is NEVER welded to a logo lockup file. For use in headings and homepage
  context, pair with Geist sans, Ink (#181818), at heading scale.

Clearspace diagram description in README (per D-12/D-13):
  "Clearspace equals one step-height of the mark on all four sides — for both the
   icon-only mark and the full lockup. See accrue-clearspace.svg for a visual diagram.
   The clearspace ratio is approximately 1:4 of the mark's height/width."

LICENSE-FONTS.txt content (per 183-CONTEXT.md SC-3 + UI-SPEC typography section):
  - Geist Sans Regular (400): letterform outlines in accrue-logo.svg, accrue-logo-on-dark.svg,
    accrue-logo-subtitle.svg, accrue-wordmark.svg, accrue-logo-mono.svg,
    accrue-logo-mono-inverse.svg, accrue-social-card.svg, favicon.svg
  - Geist Mono Regular (400): letterform outlines in accrue-logo-subtitle.svg (subtitle text)
  - License: SIL Open Font License 1.1
  - Copyright holder: Vercel, Inc.
  - Font source: https://github.com/vercel/geist-font
  - OFL-1.1 license: https://openfontlicense.org

Playwright browser pattern (from 183-PATTERNS.md §size-matrix-qa.mjs):
  import { chromium } from "playwright";
  // Tiles: representative sizes at light and dark theme
  // For each SVG: render at 320px wide (light bg), render at 320px wide (dark bg)
  // Plus: mark-only at 32px (favicon scale), mark-only at 16px (minimum)
  // Save screenshots to a temp dir or .planning/phases/183-*/qa-screenshots/
  // (QA screenshots go in .planning/, not brandbook/)
  isMain guard required

Determinism gate (from D-11):
  cd /Users/jon/projects/accrue
  node brandbook/logo/harness/generate-logo-suite.mjs
  node brandbook/logo/harness/generate-rasters.mjs
  git diff --exit-code brandbook/logo/
  # Must exit 0 — any diff means the pipeline is non-deterministic
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: size-matrix-qa.mjs, brandbook/README.md, brandbook/LICENSE-FONTS.txt, and determinism gate</name>
  <files>
    brandbook/logo/harness/size-matrix-qa.mjs,
    brandbook/README.md,
    brandbook/LICENSE-FONTS.txt
  </files>
  <read_first>
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-PATTERNS.md §size-matrix-qa.mjs (Playwright browser pattern, HTML wrapper, tile definitions)
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-CONTEXT.md §D-01/D-05/D-06/D-12/D-13/D-14 (README content spec, filename manifest, clearspace SSOT rules, tagline exclusion)
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-UI-SPEC.md §Copywriting Contract (exact tagline wording, exact subtitle string, usage rules)
    - /Users/jon/projects/accrue/brandbook/logo/ (list all files present — manifest must match actual contents)
  </read_first>
  <action>
    Write `brandbook/logo/harness/size-matrix-qa.mjs`:
    - Playwright chromium launch (headless); read all SVG files from brandbook/logo/
    - Tile set: each SVG at 320px wide on #FAFBFC bg (light) and #111418 bg (dark);
      additionally render accrue-mark.svg at 32px and 16px wide to show favicon scale
    - Wrap each tile in an inline HTML page (same pattern as render-matrix.mjs: body sized
      to tile, display:flex center, SVG inlined); screenshot via page.locator("svg").screenshot()
    - Output screenshots to `.planning/phases/183-logo-system-production/qa-screenshots/`
      (NOT in brandbook/ — exploration artifacts stay in .planning/ per ROADMAP guardrails)
    - Blank-render guard: after each screenshot, use pngjs to check darkPixelCoverage > 0.5%;
      print WARN if below threshold (do not throw — this is a QA script, not a production gate)
    - Print completion summary: "QA screenshots saved to .../qa-screenshots/ — review before commit"
    - isMain guard

    Write `brandbook/README.md`:
    - H1: "Accrue Brand Assets"
    - Brief intro: "Committed brand artifacts for the Accrue billing library. Regenerate
      using the harness in `brandbook/logo/harness/`."
    - ## Logo System section: manifest table with all 21 files
      Columns: Filename · Role · When to use · Format · Provenance
      Rows covering all 13 SVGs + 8 rasters from brandbook/logo/
    - ## Logo usage section (SSOT for min-size numbers per D-12/D-14):
      * Icon-only mark minimum: 16px
      * Primary lockup minimum: 120px wide
      * With-subtitle lockup minimum: 168px wide (below this, drop subtitle → use primary)
      * Clearspace: one step-height on all four sides (~¼ of mark height)
        "For both the icon-only mark and the full lockup. See accrue-clearspace.svg for
        a visual diagram."
    - ## Tagline usage section (documents tagline exclusion per D-06):
      "The tagline 'Billing state, modeled clearly.' is editorial typography only — it
      is never included in a logo lockup file. For homepage headings, pair with Geist sans,
      Ink (#181818), at heading scale."
    - ## Regenerating section:
      Instructions to run `node brandbook/logo/harness/generate-logo-suite.mjs` then
      `node brandbook/logo/harness/generate-rasters.mjs`, then assert
      `git diff --exit-code brandbook/logo/` exits 0.
    - ## License section: reference brandbook/LICENSE-FONTS.txt

    Write `brandbook/LICENSE-FONTS.txt`:
    - OFL 1.1 header with Vercel copyright, Geist font name
    - Explain that Geist Sans Regular and Geist Mono Regular letterform outlines are
      embedded as SVG path data in the committed brand SVG files (accrue-logo.svg etc.)
    - State this usage is permitted under OFL 1.1 (embedded outlines in graphics are not
      "bundling" in the sense restricted by OFL's Reserved Font Name provision, and are
      explicitly an allowed use per the OFL FAQ)
    - Font source: https://github.com/vercel/geist-font
    - OFL 1.1 full text reference: https://openfontlicense.org/open-font-license-official-text/

    After writing all three files, run the determinism gate:
    `node /Users/jon/projects/accrue/brandbook/logo/harness/generate-logo-suite.mjs`
    `node /Users/jon/projects/accrue/brandbook/logo/harness/generate-rasters.mjs`
    `cd /Users/jon/projects/accrue && git diff --exit-code brandbook/logo/`
    If the diff exits non-zero, the pipeline has a non-determinism bug — investigate and fix
    before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && node brandbook/logo/harness/generate-logo-suite.mjs && node brandbook/logo/harness/generate-rasters.mjs && git diff --exit-code brandbook/logo/ && echo "Determinism gate: PASS"</automated>
  </verify>
  <done>
    size-matrix-qa.mjs, README.md, and LICENSE-FONTS.txt all exist.
    Determinism gate exits 0 (git diff clean after re-run).
    README contains "Logo usage" heading with the three min-size numbers.
    LICENSE-FONTS.txt contains "OFL 1.1".
  </done>
</task>

<task type="auto">
  <name>Task 2: Run size-matrix-qa.mjs and executor reads the QA screenshots</name>
  <files>
    .planning/phases/183-logo-system-production/qa-screenshots/ (generated, not committed)
  </files>
  <read_first>
    - /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/183-PATTERNS.md §Blank-render guard (0.5% threshold, the 181 lesson)
    - /Users/jon/projects/accrue/.planning/phases/182-tournament-convergent-refinement/182-FREEZE.md §Self-Review Scores (what to look for: 4 rounded steps, Ink base, Moss top step, wordmark "accrue")
  </read_first>
  <action>
    Run `node /Users/jon/projects/accrue/brandbook/logo/harness/size-matrix-qa.mjs` to
    produce QA screenshots in `.planning/phases/183-logo-system-production/qa-screenshots/`.

    After the script completes, the executor MUST Read the following screenshots using the
    Read tool (not just confirm they exist):
    - The light-theme screenshot of accrue-logo.svg (320px wide): confirm 4-step mark
      with Moss accent on top step is visible, "accrue" wordmark is right of the mark,
      no blank/white render, no upside-down glyphs
    - The dark-theme screenshot of accrue-logo.svg: confirm marks invert correctly
      (light ink on dark bg)
    - The accrue-mark.svg at 32px: confirm 4-step mark is recognizable at favicon scale
    - The accrue-mark.svg at 16px: confirm mark is legible (not a grey blob) — this is
      the minimum-size floor from D-14 and was PASS in 182-FREEZE lint
    - The accrue-logo-subtitle.svg: confirm "Billing for Elixir apps" appears below
      the wordmark in visibly smaller, lighter (Slate) text

    If ANY screenshot shows a blank render, glitch, or incorrect layout:
    1. Fix the source generator (generate-logo-suite.mjs)
    2. Re-run generate-logo-suite.mjs to regenerate SVGs
    3. Re-run size-matrix-qa.mjs for fresh screenshots
    4. Re-read the screenshots to confirm fix

    Only proceed to the checkpoint after all five screenshots above look correct.
  </action>
  <verify>
    <automated>ls /Users/jon/projects/accrue/.planning/phases/183-logo-system-production/qa-screenshots/ | wc -l</automated>
  </verify>
  <done>QA screenshots directory contains screenshot files. Executor has Read each critical screenshot and confirmed visual fidelity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Light eyeball checkpoint — user reviews the derivative sheet</name>
  <what-built>
    All Phase 183 deliverables are complete:
    - 13 SVG files in brandbook/logo/ (all outlined paths, accessible title/desc, svgo-optimized)
    - 8 raster artifacts (PNG + .ico) in brandbook/logo/
    - brandbook/README.md with manifest, usage rules, and min-size SSOT
    - brandbook/LICENSE-FONTS.txt with OFL 1.1 + Geist provenance
    - size-matrix-qa.mjs QA gallery at .planning/phases/183-logo-system-production/qa-screenshots/
    - Determinism gate passed (git diff --exit-code clean after re-run)
    - Executor self-review confirmed all critical QA screenshots show correct renders
  </what-built>
  <how-to-verify>
    1. Open brandbook/logo/ in Finder and spot-check the SVG files in a browser (drag
       accrue-logo.svg onto a browser tab). You should see:
       - 4-step mark with Ink base and a green (Moss) top step
       - "accrue" wordmark to the right of the mark
       - Correct proportions (mark visually close to type, per hard constraint #2)

    2. Open accrue-logo-subtitle.svg — confirm "Billing for Elixir apps" appears
       below the wordmark in a noticeably smaller, lighter monospace font

    3. Open accrue-logo-mono.svg — confirm all green is gone (accent step is grey)

    4. Open accrue-mark-on-dark.svg — confirm the mark inverts correctly on a dark bg

    5. Optionally: view the QA screenshots at
       .planning/phases/183-logo-system-production/qa-screenshots/
       to see all variants at multiple sizes side by side

    6. Check brandbook/README.md in a markdown viewer — confirm the manifest table
       lists all 21 files and the "Logo usage" section has the three minimum sizes

    This is a light checkpoint — it is a fidelity check (does it look like what was
    designed in Round 2?), not a taste round. The winner is already locked.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 183, or describe any fidelity issues to fix.</resume-signal>
  <verify>
    <human-check>User confirms the logo suite renders correctly across variants. Fidelity matches the locked R2-7 winner: 4-step rounded mark, Ink base, Moss top step, "accrue" wordmark.</human-check>
  </verify>
  <done>User types "approved". Phase 183 artifacts ready to commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA screenshots | Playwright renders in a headless Chromium — screenshots for human review only, not committed artifacts |
| brandbook/README.md min-size numbers | These are the SSOT per D-12/D-14 — must not contain values that contradict the frozen design |
| LICENSE-FONTS.txt OFL claim | Must accurately represent OFL 1.1 permissions for outlined letterforms in graphics |
| Determinism gate | git diff --exit-code must be clean — any drift means pipeline produces different outputs on re-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-13 | Tampering | Pipeline non-determinism caught post-commit | mitigate | Determinism gate (git diff --exit-code) runs in this plan before the human checkpoint; any drift is caught and fixed before the checkpoint fires |
| T-183-14 | Information Disclosure | brandbook/logo/ size budget | accept | SVG + small PNGs for a single mark will be well under 500KB; total brandbook/ budget ≤2MB enforced at Phase 186, not this phase |
| T-183-15 | Repudiation | OFL 1.1 claim in LICENSE-FONTS.txt | mitigate | OFL 1.1 §5 explicitly permits embedding font outlines in graphics; wording in LICENSE-FONTS.txt cites the FAQ rationale; no legal risk for open-source library usage |
| T-183-16 | Tampering | Incorrect min-size numbers in README | mitigate | Numbers sourced directly from D-14 (CONTEXT.md); planner verified these are defensible anchors consistent with 182-FREEZE lint results (16px PASS confirmed); if SC-4 QA reveals a number needs adjustment, only the README changes (SSOT property preserved) |
| T-183-SC | Tampering | QA screenshot blank-render | mitigate | Executor MUST Read each critical screenshot with the Read tool before the checkpoint fires; blank-render guard in size-matrix-qa.mjs prints WARN if darkPixelCoverage < 0.5% |

**Severity assessment:** No high-severity threats. This plan is documentation + QA + human review. The most meaningful risk (non-deterministic artifacts) is caught by the determinism gate before the checkpoint.
</threat_model>

<verification>
After all three tasks complete:
1. `ls /Users/jon/projects/accrue/brandbook/` shows: README.md, LICENSE-FONTS.txt, logo/
2. `ls /Users/jon/projects/accrue/brandbook/logo/*.svg | wc -l` returns 13
3. `ls /Users/jon/projects/accrue/brandbook/logo/*.png /Users/jon/projects/accrue/brandbook/logo/*.ico | wc -l` returns 8
4. `grep -c "Logo usage" /Users/jon/projects/accrue/brandbook/README.md` returns >= 1
5. `grep -c "OFL 1.1" /Users/jon/projects/accrue/brandbook/LICENSE-FONTS.txt` returns >= 1
6. `cd /Users/jon/projects/accrue && git diff --exit-code brandbook/logo/` exits 0 (determinism gate)
7. `du -sh /Users/jon/projects/accrue/brandbook/` reports under 2MB
8. Executor has Read QA screenshots and confirmed fidelity
9. User has typed "approved"
</verification>

<success_criteria>
LOGO-04 complete when all of the following are TRUE:
- brandbook/logo/ contains all 21 required files (13 SVG + 8 rasters per SC-1)
- No SVG contains <text>, @font-face, or live text — all outlined paths (SC-2)
- Every SVG has accessible <title> and <desc> (SC-2)
- brandbook/LICENSE-FONTS.txt documents OFL 1.1 provenance for Geist outlines (SC-3)
- size-matrix-qa.mjs produced screenshots reviewed by executor; user checkpoint passed (SC-4)
- brandbook/README.md is the SSOT for all min-size numbers (D-12/D-14)
- Determinism gate passed: git diff --exit-code brandbook/logo/ exits 0 after re-run
- du -sh brandbook/ is under 2MB
</success_criteria>

<output>
Create `.planning/phases/183-logo-system-production/183-04-SUMMARY.md` when done.
</output>

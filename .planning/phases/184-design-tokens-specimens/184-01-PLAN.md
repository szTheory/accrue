---
phase: 184-design-tokens-specimens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens/harness/package.json
  - brandbook/tokens/harness/package-lock.json
  - brandbook/tokens/harness/lib.mjs
  - brandbook/tokens/tokens.json
autonomous: false
requirements: [TOK-01]
user_setup: []

must_haves:
  truths:
    - "The tokens harness installs postcss + postcss-value-parser + culori from a committed lockfile with a human-approved supply-chain gate"
    - "tokens.json is a DTCG v2025.10-conformant SSOT defining the 7 raw --accrue-* palette tokens, palette-bearing semantic roles, and reference-only scales"
    - "D-02: tokens.json is W3C DTCG-formatted at the v2025.10 stable version, using $value / $type / $description with nested groups"
    - "D-04: the brand to admin --ax-* mapping is encoded in DTCG $extensions (org.accrue.ax axMap) per token, so the JSON holds both the value and its parity relationship"
    - "lib.mjs exports the shared flatten / name-derivation / color-resolution helpers imported by the generator, parity check, and specimens"
  artifacts:
    - path: "brandbook/tokens/harness/package.json"
      provides: "harness manifest with generate/specimens/parity/parity-test scripts and the three pure-JS deps"
      contains: "accrue-tokens-harness"
    - path: "brandbook/tokens/harness/package-lock.json"
      provides: "committed lockfile for reproducible CI installs"
    - path: "brandbook/tokens/harness/lib.mjs"
      provides: "flattenTokens, deriveCssVar, resolveColor (var+color-mix+normalize), iterAxMappedTokens"
      min_lines: 60
    - path: "brandbook/tokens/tokens.json"
      provides: "DTCG SSOT — 7 raw palette + semantic roles + reference scales + ax-map extensions"
      contains: "org.accrue.ax"
  key_links:
    - from: "brandbook/tokens/harness/lib.mjs"
      to: "culori formatHex / interpolate"
      via: "import"
      pattern: "from \"culori\""
    - from: "brandbook/tokens/tokens.json"
      to: "--accrue-* raw palette + axMap"
      via: "$value.hex + $extensions org.accrue.ax"
      pattern: "\"hex\""
---

<objective>
Stand up the lean tokens harness (mirroring `brandbook/logo/harness/`) and author the two foundational artifacts every downstream plan consumes: the DTCG v2025.10 `tokens.json` SSOT and the shared `lib.mjs` helper module. Install the three pure-JS dependencies behind a supply-chain human-verify gate (slopcheck was unavailable in research — D-16 / RESEARCH Package Legitimacy Audit).

Purpose: tokens.json is the single source of truth (D-01); lib.mjs centralizes the load-bearing color/AST logic (RESEARCH "Component Responsibilities"). Both must exist and the deps must be installed before the generator (Plan 02), parity check (Plan 03), and specimens (Plan 04) can run.
Output: `brandbook/tokens/harness/{package.json,package-lock.json,lib.mjs}` + `brandbook/tokens/tokens.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-design-tokens-specimens/184-CONTEXT.md
@.planning/phases/184-design-tokens-specimens/184-RESEARCH.md
@.planning/phases/184-design-tokens-specimens/184-PATTERNS.md
@.planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md
@.planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt
@brandbook/logo/harness/package.json
@brandbook/logo/harness/geist-spine-mono.mjs
@accrue_admin/assets/css/theme.css

<interfaces>
<!-- Authoritative skeletons live in 184-RESEARCH.md. Key contracts the executor MUST honor: -->

DTCG v2025.10 color $value is a STRUCTURED OBJECT, not a bare hex string:
  { "colorSpace": "srgb", "components": [r,g,b], "hex": "#rrggbb" }   // hex is authoritative; generator reads $value.hex
DTCG dimension $value: { "value": <number>, "unit": "px"|"rem" }
Per-token parity metadata namespace (single, consistent — D-10 / RESEARCH Pattern 2):
  "$extensions": { "org.accrue.ax": { "axMap": "--ax-success" | null, "divergesFrom"?: "--ax-…", "reason"?: "…" } }

lib.mjs MUST export (RESEARCH Component Responsibilities / PATTERNS lib.mjs section):
  flattenTokens(json)        -> [{ cssVar, hex, axMap, divergesFrom?, reason?, name, scope:"light"|"dark" }]
  deriveCssVar(pathSegs)     -> "--accrue-<…>"  (raw under color.brand.* -> --accrue-<leaf>; roles -> --accrue-<group>-<leaf>)
  resolveColor(raw, vars, brandRaw) -> lowercase "#rrggbb"  (resolves var() indirection, evaluates color-mix via culori interpolate, normalizes via formatHex; THROWS on unresolved — never returns undefined)
  iterAxMappedTokens(json)   -> iterable of { axMap, brandHex, divergesFrom, reason, name, scope }

Throw-don't-return-undefined discipline (PATTERNS / RESEARCH Pitfall 1): culori returns undefined for color-mix(); a silent undefined makes the parity check falsely pass. resolveColor MUST throw a clear Error on any unresolved color.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Scaffold harness manifest and install pure-JS deps (supply-chain gate)</name>
  <files>brandbook/tokens/harness/package.json, brandbook/tokens/harness/package-lock.json</files>
  <read_first>
    - brandbook/logo/harness/package.json (exact shape to mirror: private, type:module, scripts block, dependencies block — STRIP the native-build deps)
    - .planning/phases/184-design-tokens-specimens/184-PATTERNS.md (package.json target shape, lines ~72-91)
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md ("## Package Legitimacy Audit" — the three [ASSUMED] packages)
    - .gitignore (line 23 `node_modules/` already covers the new harness dir — do NOT add a local .gitignore)
  </read_first>
  <action>
    Create `brandbook/tokens/harness/package.json` with name `accrue-tokens-harness`, `"private": true`, `"type": "module"`, and a `scripts` block: `generate` -> `node generate-tokens-css.mjs`, `specimens` -> `node generate-specimens.mjs`, `parity` -> `node parity-check.mjs`, `parity-test` -> `node parity-check.mjs --test`. Dependencies: `postcss` `^8.5.15`, `postcss-value-parser` `^4.2.0`, `culori` `^4.0.2`. Do NOT add native-build deps (no @resvg/resvg-js, playwright, wawoff2) and do NOT add geist/opentype.js (type specimen uses font-family text labels per RESEARCH Discretion). Add `svgo` `^4.0.1` ONLY — the specimen plan needs it; include it now so the lockfile is complete. Then run `npm install` inside `brandbook/tokens/harness/` to generate `package-lock.json`. Rely on repo-root `.gitignore` for `node_modules/`.

    This is a Tampering / supply-chain control (T-184-SC): slopcheck could not be run in research, so all four packages are [ASSUMED]. Before treating the install as trusted, the human must verify legitimacy.
  </action>
  <what-built>A new `brandbook/tokens/harness/` with `package.json` (postcss, postcss-value-parser, culori, svgo) and a freshly generated `package-lock.json` from `npm install`.</what-built>
  <how-to-verify>
    1. Confirm each package resolves to its real source repo:
       - https://www.npmjs.com/package/postcss (repo github.com/postcss/postcss)
       - https://www.npmjs.com/package/postcss-value-parser (repo github.com/TrySound/postcss-value-parser)
       - https://www.npmjs.com/package/culori (repo github.com/Evercoder/culori)
       - https://www.npmjs.com/package/svgo (repo github.com/svg/svgo)
    2. Confirm none introduced an unexpected `postinstall` script (the three core deps are pure-JS; svgo is the logo-harness's existing dep).
    3. Confirm `package-lock.json` exists and `node_modules/` is NOT staged in git (`git status` shows only package.json + package-lock.json under the new harness).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the installed dependency tree, or describe issues.</resume-signal>
  <acceptance_criteria>
    - `brandbook/tokens/harness/package.json` contains the string `accrue-tokens-harness` and lists `postcss`, `postcss-value-parser`, `culori`, `svgo` under `dependencies`.
    - `brandbook/tokens/harness/package-lock.json` exists.
    - `git status --porcelain brandbook/tokens/harness/node_modules` produces NO output (node_modules is gitignored).
    - Human typed "approved".
  </acceptance_criteria>
  <done>Harness manifest + lockfile committed; node_modules gitignored; deps human-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Author tokens.json (DTCG v2025.10 SSOT)</name>
  <files>brandbook/tokens/tokens.json</files>
  <read_first>
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md (tokens.json skeleton, lines ~296-354 — adopt this DTCG 2025.10 object-shaped $value form; Pitfall 4 on $value object shape)
    - .planning/phases/180-brand-audit-dna-lock/BRAND-AUDIT.md (§7, lines ~307-367 — the 7 raw hexes + admin bindings + semantic role categories)
    - accrue_admin/assets/css/theme.css (lines 105-183 — the --ax-* role values + dark overrides each role token must map to / mirror)
    - .planning/phases/184-design-tokens-specimens/184-CONTEXT.md (D-09, D-09b, D-11 token vocabulary scope)
  </read_first>
  <action>
    Author `brandbook/tokens/tokens.json` as a DTCG v2025.10-conformant document with `"$schema": "https://tr.designtokens.org/format/tokens.schema.json"`. Every color `$value` is the object form `{ "colorSpace": "srgb", "components": [r,g,b], "hex": "#rrggbb" }` with `hex` lowercase and authoritative (NOT a bare hex string — Pitfall 4). Author:

    (1) `color.brand.*` — the 7 raw tokens (D-09): ink `#111418` (axMap `--ax-primary`), slate `#24303b` (axMap `--ax-subtle`), fog `#e9eef2` (axMap `null`, brand-only per D-09b), paper `#fafbfc` (axMap `--ax-base`), moss `#5e9e84` (axMap `--ax-success`), cobalt `#5d79f6` (axMap `null`, brand-only per D-09b), amber `#c8923b` (axMap `--ax-warning`).

    (2) Palette-bearing semantic roles (D-09), each carrying an explicit `axMap` and value-checked by Plan 03: surface (base->`--ax-base`, elevated->`--ax-elevated` `#ffffff`, sunken->`--ax-sunken` `#f1f5f8`); content (primary->`--ax-primary`, muted->`--ax-muted` `#5d6a73`, subtle->`--ax-subtle`); interactive (accent + focus-ring — see note below); feedback (success->`--ax-success`, warning->`--ax-warning`, danger->`--ax-danger` `#d64b4b`, info->`--ax-info` `#3878a6`). Use DTCG alias syntax `"{color.brand.paper}"` where a role equals a raw token. The role values MUST equal the LIGHT (`html.accrue-admin`) values in theme.css after var() resolution.

    (3) Dark-mode role counterparts (BRAND-AUDIT §7 requires them; RESEARCH Open Question 2 — use a parallel `color.dark.*` group so the dark parity pass has a brand side): base `#0f1318`, elevated `#171d24`, sunken `#0b1015`, primary `#f4f7fa`, muted `#a8b2bc`, subtle `#d7dde3`, info `#4a90b8`. Each carries the same `axMap` as its light counterpart with a `scope:"dark"` marker (your lib.mjs flatten reads it). Match the `html.accrue-admin[data-theme="dark"]` values in theme.css EXACTLY.

    (4) Brand-only NEW tokens this phase authors (D-09b, no upstream): `color.code-block` and `color.callout`, both `axMap: null`. Derive code-block from the neutral Fog/Slate/Ink family (e.g. surface near Fog `#e9eef2`, text near Ink) and callout as a tinted neutral. RESEARCH Open Question 1 flags these for one-line PR ratification — pick defensible values and add a `$description` noting they are new brand-layer definitions.

    (5) Reference-only dimension entries (D-11, axMap:null, NOT value-enforced): a `dimension.space` group mirroring the `--ax-space-*` rungs as `{ "value": …, "unit": "rem" }` with `$extensions.org.accrue.ax = { "axMap": null, "referencesAx": "--ax-space-md" }`. These document the scale without minting `--accrue-space-*` (D-11 forbids minting). Type/radius reference entries are documented in Plan 02's README prose table; do not mint them as --accrue-* either.

    Use the single namespace `org.accrue.ax` for ALL `$extensions` (RESEARCH Pattern 2). For `interactive.focus-ring`: admin `--ax-focus-ring` is a `color-mix(in oklch, …)` (NOT a flat palette color) — set `axMap: null` and add a `$description` documenting "2px solid Cobalt focus ring, 2px offset (BRAND-AUDIT §7 item 6)"; do NOT value-check it. For `interactive.accent`: if no flat `--ax-accent` value exists in theme.css, treat as brand-only cobalt with `axMap: null`.
  </action>
  <verify>
    <automated>node -e "const t=require('./brandbook/tokens/tokens.json'); const b=t.color.brand; ['ink','slate','fog','paper','moss','cobalt','amber'].forEach(k=>{if(!b[k] || typeof b[k].\$value!=='object' || !/^#[0-9a-f]{6}$/.test(b[k].\$value.hex)) throw new Error('bad raw token '+k)}); if(b.moss.\$value.hex!=='#5e9e84') throw new Error('moss hex'); if(b.fog.\$extensions['org.accrue.ax'].axMap!==null) throw new Error('fog must be brand-only'); console.log('tokens.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/tokens/tokens.json` parses as JSON and the verify command above prints `tokens.json OK` (exit 0).
    - All 7 raw `color.brand.*` tokens use object-form `$value` with a lowercase 6-digit `hex`.
    - `color.brand.moss.$value.hex` === `#5e9e84`; `color.brand.fog` and `color.brand.cobalt` have `axMap: null`.
    - Every `$extensions` block uses the key `org.accrue.ax` (grep `grep -c '"org.accrue.ax"' brandbook/tokens/tokens.json` returns ≥ 11).
    - A `color.dark` group exists with `base.$value.hex` === `#0f1318` and `primary.$value.hex` === `#f4f7fa` (matching theme.css dark block).
    - `color.code-block` and `color.callout` exist with `axMap: null` and a `$description`.
  </acceptance_criteria>
  <done>tokens.json is a complete, DTCG-conformant SSOT covering all D-09/09b/11 tokens in light + dark, ready for the generator, parity check, and specimens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author lib.mjs shared helpers (flatten / name-derive / resolveColor / iter)</name>
  <files>brandbook/tokens/harness/lib.mjs</files>
  <read_first>
    - brandbook/tokens/harness/lib.mjs (the file being created)
    - brandbook/tokens/tokens.json (the structure flatten must walk — created in Task 2)
    - brandbook/tokens/harness/../../logo/harness/geist-spine-mono.mjs (PURE-ESM module header, __dirname pattern, throw-on-failure discipline, isMain guard + --test smoke main — lines 16-21, 38-56, 127-169)
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md (parity-check.mjs resolveColor / resolveColorMix verified pipeline, lines ~400-474; generator flatten/resolveHex/deriveName, lines ~365-396; Pitfalls 1 & 2)
    - .planning/phases/184-design-tokens-specimens/184-PATTERNS.md (lib.mjs section — exact exports list + throw-don't-return-undefined pattern)
  </read_first>
  <behavior>
    - flattenTokens(json) returns one row per concrete color token with { cssVar, hex, axMap, divergesFrom?, reason?, name, scope }; light tokens scope "light", color.dark.* tokens scope "dark"; resolves DTCG `{alias}` references to the target's hex.
    - deriveCssVar(["color","brand","moss"]) === "--accrue-moss"; deriveCssVar(["color","surface","base"]) === "--accrue-surface-base".
    - resolveColor("#FFF", {}, {}) === "#ffffff" (culori formatHex normalizes case + 3-digit expand).
    - resolveColor("var(--accrue-paper)", {}, {"--accrue-paper":"#fafbfc"}) === "#fafbfc" (var indirection against brandRaw fallback).
    - resolveColor evaluating a color-mix node returns a lowercase #rrggbb via interpolate (Pitfall 2 direction: interpolate([second,first],space)(firstPercent/100)).
    - resolveColor THROWS (does not return undefined/null) when a var or color is unresolved — assert the throw in --test.
    - iterAxMappedTokens(json) yields { axMap, brandHex, divergesFrom, reason, name, scope } and SKIPS no entries (caller filters axMap==null).
  </behavior>
  <action>
    Create `brandbook/tokens/harness/lib.mjs` as a pure-ESM module (mirror geist-spine-mono.mjs header + `__dirname` from `fileURLToPath`). Import `parse`, `formatHex`, `interpolate` from `culori` and `valueParser` from `postcss-value-parser`. Implement and `export` the four functions per the verified RESEARCH pipeline. The CSS var-name derivation rule (RESEARCH Pattern 1): drop the leading `color`/`dimension` segment; if `rest[0] === "brand"` emit `--accrue-<rest.slice(1).join("-")>`; else emit `--accrue-<rest.join("-")>`; lowercase. resolveColor must (a) detect a single bare `var(--x)` node and recurse against `vars` then `brandRaw`, throwing if neither resolves; (b) detect a `color-mix` function node and evaluate via `interpolate` (document Pitfall 2 percentage direction even though no brand-mapped token uses it today); (c) otherwise `formatHex(parse(raw))`, throwing if culori returns undefined. Add an `isMain` guard with a `--test` smoke main that exercises each behavior above (including asserting resolveColor throws on an unresolved var) and exits 0 on success / non-zero on failure, mirroring the geist-spine-mono `--test` shape.
  </action>
  <verify>
    <automated>node brandbook/tokens/harness/lib.mjs --test</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tokens/harness/lib.mjs --test` exits 0 and prints a smoke OK line.
    - `lib.mjs` exports `flattenTokens`, `deriveCssVar`, `resolveColor`, `iterAxMappedTokens` (grep `grep -c '^export ' brandbook/tokens/harness/lib.mjs` returns ≥ 4).
    - The --test main asserts resolveColor THROWS on an unresolved var (non-undefined return) — the smoke fails if resolveColor ever returns undefined.
    - `node -e "import('./brandbook/tokens/harness/lib.mjs').then(m=>{if(m.deriveCssVar(['color','brand','moss'])!=='--accrue-moss')throw 1; if(m.deriveCssVar(['color','surface','base'])!=='--accrue-surface-base')throw 2; console.log('derive OK')})"` prints `derive OK`.
  </acceptance_criteria>
  <done>lib.mjs is a tested shared helper module exporting the four functions, with a passing --test smoke gate and throw-on-unresolved discipline; ready for import by the generator, parity check, and specimens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → harness node_modules | Third-party package code enters the build at `npm install` time (untrusted until verified) |
| repo files → Node scripts | tokens.json / theme.css are read by harness `.mjs`; trusted in-repo inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-SC | Tampering | npm install of postcss / postcss-value-parser / culori / svgo | mitigate | Task 1 is a `checkpoint:human-verify` (blocking-human): human confirms each package's real source repo on npmjs.com and absence of suspicious postinstall before the install is trusted; lockfile committed and reinstalled in CI (D-17). slopcheck unavailable in research → [ASSUMED] packages require the gate. |
| T-184-01 | Tampering | lib.mjs resolveColor silently returning undefined | mitigate | Throw-don't-return-undefined discipline (RESEARCH Pitfall 1); --test asserts the throw so a silent undefined cannot make a downstream parity check falsely pass. |
| T-184-02 | Spoofing | tokens.json claiming a hex that does not match the locked palette | accept | Plan 03's parity check + Plan 02's verify-tokens assert values against theme.css / BRAND-AUDIT; this plan's verify asserts the moss hex and brand-only flags. Low risk (internal authored file, reviewed at PR). |
| T-184-03 | Information Disclosure | scripts reading outside intended repo paths | accept | Scripts read only tokens.json and (downstream) theme.css via fixed relative paths; no network, no secrets, no user input. Buildless artifact generator — minimal surface. |
</threat_model>

<verification>
- Task 1: dependency tree human-approved; package.json + lockfile committed; node_modules gitignored.
- Task 2: `node` verify asserts tokens.json structure, raw hexes, brand-only flags, dark group, code-block/callout.
- Task 3: `node lib.mjs --test` exits 0; exports present; throw-on-unresolved proven.
</verification>

<success_criteria>
- `brandbook/tokens/harness/{package.json,package-lock.json,lib.mjs}` and `brandbook/tokens/tokens.json` all exist and are committed.
- The four lib.mjs exports are importable and `--test` passes.
- tokens.json is DTCG v2025.10-conformant (object-form `$value`), single `org.accrue.ax` namespace, light + dark roles, all D-09/09b/11 tokens present.
- node_modules is gitignored (no local .gitignore added).
</success_criteria>

<output>
Create `.planning/phases/184-design-tokens-specimens/184-01-SUMMARY.md` when done
</output>
</content>
</invoke>

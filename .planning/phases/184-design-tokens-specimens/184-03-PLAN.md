---
phase: 184-design-tokens-specimens
plan: 03
type: execute
wave: 2
depends_on: ["184-01"]
files_modified:
  - brandbook/tokens/harness/parity-check.mjs
autonomous: true
requirements: [TOK-02]
user_setup: []

must_haves:
  truths:
    - "The parity check live-derives expected --ax-* values from accrue_admin theme.css (read-only) and compares each ax-mapped brand token in both light and dark"
    - "The check exits 0 when every ax-mapped token matches (or carries a documented $extensions divergence) and non-zero on any undocumented drift"
    - "A --test mode proves drift detection both directions via an injected-drift fixture (exit non-zero) and a documented-divergence fixture (exit zero)"
  artifacts:
    - path: "brandbook/tokens/harness/parity-check.mjs"
      provides: "live brand↔admin token parity gate with --test fixture mode"
      min_lines: 60
  key_links:
    - from: "brandbook/tokens/harness/parity-check.mjs"
      to: "accrue_admin/assets/css/theme.css"
      via: "postcss.parse read-only"
      pattern: "theme.css"
    - from: "brandbook/tokens/harness/parity-check.mjs"
      to: "brandbook/tokens/harness/lib.mjs"
      via: "import resolveColor / iterAxMappedTokens"
      pattern: "from \"./lib.mjs\""
---

<objective>
Build the brand↔admin parity check (D-05/06/07/08): a Node `.mjs` that reads the READ-ONLY `accrue_admin/assets/css/theme.css`, resolves each ax-mapped brand token to a canonical lowercase `#rrggbb` in BOTH light and dark scopes, compares against tokens.json, and exits non-zero on undocumented drift. Ship a `--test` injected-drift fixture mode that proves SC#2 in both directions — this is the single most important test in the phase.

Purpose: TOK-02 / SC#2. The check is a LIVE derivation (no golden file, D-08) so it cannot rot. It NEVER edits theme.css; divergence is declared only in tokens.json `$extensions` (D-10).
Output: `brandbook/tokens/harness/parity-check.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-design-tokens-specimens/184-CONTEXT.md
@.planning/phases/184-design-tokens-specimens/184-RESEARCH.md
@.planning/phases/184-design-tokens-specimens/184-PATTERNS.md
@.planning/phases/184-design-tokens-specimens/184-VALIDATION.md
@.planning/phases/184-design-tokens-specimens/184-01-SUMMARY.md
@brandbook/tokens/tokens.json
@brandbook/tokens/harness/lib.mjs
@accrue_admin/assets/css/theme.css
@brandbook/logo/harness/geist-spine-mono.mjs

<interfaces>
<!-- From Plan 01 lib.mjs (already built): -->
  resolveColor(raw, vars, brandRaw) -> lowercase "#rrggbb"  (resolves var(), evaluates color-mix via interpolate, normalizes via formatHex; THROWS on unresolved — never undefined)
  iterAxMappedTokens(json) -> iterable of { axMap, brandHex, divergesFrom, reason, name, scope }
  flattenTokens(json) -> rows incl. raw --accrue-* (used to build the brandRaw map)

theme.css scopes (PATTERNS / RESEARCH Pitfall 3 — tokens are scoped to html.accrue-admin, NOT :root):
  light scope selector:  "html.accrue-admin"                     (theme.css:12)  — roles bind via var(--accrue-*) AND standalone hex
  dark  scope selector:  'html.accrue-admin[data-theme="dark"]'  (theme.css:145) — standalone hexes
  raw --accrue-* are NOT defined in theme.css (only referenced) → resolve var(--accrue-*) against the brandRaw map from tokens.json (GAP-C2).

Exit contract (D-08): process.exit(failures===0 ? 0 : 1).
Skip axMap==null (brand-only D-09b). color-mix appears ONLY on non-brand-mapped tokens (--ax-focus-ring etc.) so the hot path is pure hex/var; keep the interpolate evaluator for robustness but it should never fire on a brand-mapped token.
Divergence tolerance (D-10): tolerate a mismatch ONLY when the token's $extensions.org.accrue.ax has divergesFrom===axMap AND a non-empty reason.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write parity-check.mjs live derivation + exit-code contract</name>
  <files>brandbook/tokens/harness/parity-check.mjs</files>
  <read_first>
    - brandbook/tokens/harness/parity-check.mjs (file being created)
    - brandbook/tokens/harness/lib.mjs (resolveColor / iterAxMappedTokens / flattenTokens)
    - accrue_admin/assets/css/theme.css (lines 12, 105-141 light scope; 145-162 dark scope — the exact --ax-* values and var() bindings)
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md (parity-check.mjs verified pipeline lines ~400-474; Pitfalls 1, 2, 3)
    - .planning/phases/184-design-tokens-specimens/184-PATTERNS.md (parity-check.mjs section — theme.css scopes + key resolution facts)
    - brandbook/logo/harness/geist-spine-mono.mjs (isMain guard + exit-code main, lines 127-169)
  </read_first>
  <behavior>
    - buildScopes(css): postcss.parse → walkRules → walkDecls(/^--/) → { selector: { prop: rawValue } }.
    - For each iterAxMappedTokens row with axMap != null, in scopes light ("html.accrue-admin") and dark ('html.accrue-admin[data-theme="dark"]'): read admin raw value at axMap; if absent in a scope, skip that scope; else resolveColor(adminRaw, scope, brandRaw) and compare to the brand token's hex for that scope.
    - On mismatch: if $extensions divergesFrom===axMap and reason present → log "OK (documented divergence)"; else console.error a named DRIFT line and increment failures.
    - Resolves var(--accrue-paper) etc. against brandRaw (from tokens.json raw palette) since theme.css does not define raw tokens.
    - Exits 0 when failures===0, non-zero otherwise.
  </behavior>
  <action>
    Create parity-check.mjs. Import postcss, postcss-value-parser (only if not fully delegated to lib.mjs), and from ./lib.mjs import resolveColor, iterAxMappedTokens, flattenTokens. Resolve THEME = path.resolve(__dirname, "../../../accrue_admin/assets/css/theme.css") and read tokens.json from path.resolve(__dirname, "../tokens.json"). Build brandRaw = map of "--accrue-<leaf>" -> hex from the raw palette rows of flattenTokens. Build light + dark scope maps via buildScopes. Iterate ax-mapped tokens (skip axMap==null); for light compare against the light brand value (raw/role light hex) and for dark compare against the brand color.dark.* value; resolve each admin side via resolveColor against its own scope + brandRaw. Apply the D-10 documented-divergence tolerance. Tally failures and process.exit(failures===0?0:1). Add an isMain guard so importing the module does not run the check. NEVER write to theme.css. Keep the file lean and importable so --test (Task 2) can drive its core.
  </action>
  <verify>
    <automated>node brandbook/tokens/harness/parity-check.mjs && echo PARITY_CLEAN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tokens/harness/parity-check.mjs` exits 0 against the real (matched) theme.css + tokens.json and prints `PARITY_CLEAN_OK`.
    - The script reads `accrue_admin/assets/css/theme.css` and writes to NO accrue_admin file (`git diff --exit-code -- accrue_admin/` stays clean after running it).
    - It resolves `var(--accrue-*)` against the tokens.json raw palette (brandRaw), not against theme.css (which lacks raw defs).
    - Tokens with `axMap: null` are skipped (no false drift on fog/cobalt/code-block/callout).
  </acceptance_criteria>
  <done>parity-check.mjs live-derives and compares every ax-mapped token in light + dark, exits 0 on the matched repo, honors documented divergences, and never mutates theme.css.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --test injected-drift fixture mode (SC#2 both directions)</name>
  <files>brandbook/tokens/harness/parity-check.mjs</files>
  <read_first>
    - brandbook/tokens/harness/parity-check.mjs (the file from Task 1 being extended)
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md ("How the parity check ITSELF is tested" lines ~569-580 — the four fixture cases)
    - .planning/phases/184-design-tokens-specimens/184-VALIDATION.md (SC#2 drift row — the load-bearing negative test)
    - brandbook/logo/harness/geist-spine-mono.mjs (--test smoke main shape)
  </read_first>
  <behavior>
    - `--test` runs four in-memory fixture cases and exits 0 only if ALL behave as expected:
      (a) Positive: real theme.css + real tokens.json → parity core returns 0 failures.
      (b) Injected drift: a copy of theme.css with ONE ax-mapped value mutated (e.g. --ax-success changed to a different hex) → parity core returns >0 failures (drift detected and named).
      (c) Documented divergence: the same mutation BUT with a matching $extensions { divergesFrom, reason } injected into a tokens.json copy → parity core returns 0 failures (tolerated).
      (d) Sanity: an unrelated mutation outside the ax-mapped set does NOT cause failures.
    - Uses temp/in-memory copies; does NOT modify the committed theme.css or tokens.json (asserts git diff stays clean).
  </behavior>
  <action>
    Refactor the Task 1 core into a callable function (e.g. runParity({ themeCss, tokens }) -> failureCount) so --test can drive it with fixture inputs without touching disk-committed files. Add a main() guarded by isMain + `process.argv.includes("--test")` that constructs the four fixtures above (mutate strings in memory: copy the theme.css text and replace one ax-mapped declaration's value; copy the tokens.json object and inject a divergesFrom+reason for case c). Assert each case's expected failure count; console.error which case failed; process.exit(0) only if all four pass, else non-zero. This is the SC#2 proof in both directions per 184-VALIDATION.md. Wire `parity-test` -> `node parity-check.mjs --test` (already in package.json from Plan 01).
  </action>
  <verify>
    <automated>node brandbook/tokens/harness/parity-check.mjs --test && git diff --exit-code -- accrue_admin/assets/css/theme.css brandbook/tokens/tokens.json && echo PARITY_TEST_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tokens/harness/parity-check.mjs --test` exits 0 and reports all four fixture cases passing.
    - The injected-drift case proves a NON-ZERO failure count on mutated input (drift detected) — verifiable by inspecting the --test output naming the drifted token.
    - The documented-divergence case proves ZERO failures when divergesFrom+reason is present.
    - After `--test`, `git diff --exit-code` over theme.css and tokens.json is clean (fixtures are in-memory only).
    - The verify command prints `PARITY_TEST_OK`.
  </acceptance_criteria>
  <done>--test proves SC#2 both directions (non-zero on undocumented drift, zero on documented divergence) using in-memory fixtures that never mutate committed files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accrue_admin theme.css → parity check | READ-ONLY admin SSOT crosses into the brandbook tool; must never be written back |
| tokens.json → parity check | Brand SSOT compared against admin; divergence declared only here ($extensions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-07 | Tampering | parity check writing to / editing the read-only theme.css | mitigate | Check only `fs.readFileSync(theme.css)`; never writes; acceptance asserts `git diff --exit-code -- accrue_admin/` stays clean. Zero-admin-changes constraint (CONTEXT). |
| T-184-08 | Repudiation | silent pass on real drift (false negative) | mitigate | resolveColor throws on unresolved (never undefined, Pitfall 1); --test injected-drift fixture proves NON-ZERO exit on mutation (SC#2 negative direction) — the load-bearing test. |
| T-184-09 | Tampering | undocumented divergence sneaking through | mitigate | Divergence tolerated ONLY when $extensions.divergesFrom===axMap AND reason present (D-10); --test case (c) proves the tolerance is gated on the declaration. |
| T-184-10 | Information Disclosure | reading files outside the repo | accept | Fixed relative paths to theme.css + tokens.json only; no network, no secrets, no user input. |
| T-184-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan — deps gated + locked in Plan 01 (T-184-SC). |
</threat_model>

<verification>
- Task 1: parity-check.mjs exits 0 on matched repo; never mutates theme.css; skips axMap:null.
- Task 2: --test exits 0 with all four fixtures passing; proves non-zero on injected drift and zero on documented divergence; committed files untouched.
</verification>

<success_criteria>
- parity-check.mjs proves brand↔admin value parity in light + dark with the D-08 exit-code contract.
- --test proves SC#2 in BOTH directions via in-memory injected-drift fixtures (no golden file, D-08).
- Zero admin code changes (theme.css read-only); divergence only via tokens.json $extensions (D-10).
</success_criteria>

<output>
Create `.planning/phases/184-design-tokens-specimens/184-03-SUMMARY.md` when done
</output>

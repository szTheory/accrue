---
phase: 184-design-tokens-specimens
plan: 04
type: execute
wave: 2
depends_on: ["184-01"]
files_modified:
  - brandbook/tokens/harness/generate-specimens.mjs
  - brandbook/tokens/harness/verify-specimens.mjs
  - brandbook/tokens/harness/svgo.config.mjs
  - brandbook/examples/palette.svg
  - brandbook/examples/typography.svg
  - brandbook/examples/spacing.svg
autonomous: true
requirements: [TOK-03]
user_setup: []

must_haves:
  truths:
    - "Three deterministic specimen SVGs are generated from tokens.json: palette, typography, spacing"
    - "D-14: three separate specimen files are emitted — examples/palette.svg, examples/typography.svg, examples/spacing.svg — so Phase 186 can inline/section each independently"
    - "palette.svg renders every raw swatch + semantic role with hex + token + role + AA status on both light and dark surfaces; Moss/Cobalt/Amber are flagged AA-FAIL on light body per contrast-table.txt"
    - "typography.svg shows the full Geist sans + Geist Mono scale (px+rem); spacing.svg shows every spacing step as a labeled ruler"
    - "Specimens regenerate byte-identical so the git diff --exit-code determinism gate passes"
  artifacts:
    - path: "brandbook/tokens/harness/generate-specimens.mjs"
      provides: "deterministic palette/typography/spacing SVG generator reading tokens.json"
    - path: "brandbook/examples/palette.svg"
      provides: "swatch grid with hex/token/role/AA on light + dark surfaces"
      contains: "<svg"
    - path: "brandbook/examples/typography.svg"
      provides: "Geist sans + Geist Mono scale with px+rem labels"
    - path: "brandbook/examples/spacing.svg"
      provides: "labeled spacing ruler for every step"
    - path: "brandbook/tokens/harness/verify-specimens.mjs"
      provides: "content-coverage assertion for SC#3"
  key_links:
    - from: "brandbook/tokens/harness/generate-specimens.mjs"
      to: "brandbook/tokens/tokens.json"
      via: "read + flattenTokens"
      pattern: "tokens.json"
    - from: "brandbook/tokens/harness/generate-specimens.mjs"
      to: "brandbook/examples/*.svg"
      via: "fs.writeFileSync"
      pattern: "examples"
---

<objective>
Generate the three deterministic specimen SVGs (D-13/14/15) from the `tokens.json` SSOT: `palette.svg`, `typography.svg`, `spacing.svg`. Each renders the locked content checklist; AA annotations are sourced from `contrast-table.txt` (never invented). Add `verify-specimens.mjs` for SC#3 content coverage. Specimens are `git diff --exit-code`-gated like the logo suite (D-17).

Purpose: TOK-03 / SC#3. These are the visual brand documentation Phase 186 inlines into `index.html`; SVG keeps them clean self-contained nodes and drift-free (generated from the same SSOT).
Output: `brandbook/examples/{palette,typography,spacing}.svg` + `brandbook/tokens/harness/{generate-specimens.mjs,verify-specimens.mjs,svgo.config.mjs}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-design-tokens-specimens/184-CONTEXT.md
@.planning/phases/184-design-tokens-specimens/184-RESEARCH.md
@.planning/phases/184-design-tokens-specimens/184-PATTERNS.md
@.planning/phases/184-design-tokens-specimens/184-VALIDATION.md
@.planning/phases/184-design-tokens-specimens/184-01-SUMMARY.md
@brandbook/tokens/tokens.json
@brandbook/tokens/harness/lib.mjs
@brandbook/logo/harness/generate-logo-suite.mjs
@brandbook/logo/harness/svgo.config.mjs
@.planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt
@accrue_admin/assets/css/theme.css

<interfaces>
<!-- From Plan 01 lib.mjs (already built): -->
  flattenTokens(json) -> rows incl. raw + role tokens with hex, name, scope.

OUTPUT_DIR for specimens (PATTERNS specimen section): path.resolve(__dirname, "../../examples")  // brandbook/examples/
Determinism (RESEARCH Pitfall 5 / shared svgo): sorted token order, fixed coordinate math, fixed decimals (.toFixed(3)), trailing "\n"; optionally pipe through svgo.config.mjs (multipass, preserves viewBox/title/desc).
Label rendering (RESEARCH Discretion): use <text font-family="Geist, system-ui"> / <text font-family="Geist Mono, monospace"> labels — NOT opentype.js glyph outlines (keeps SVGs kilobyte-scale, dependency-light).

AA annotations — SOURCE FROM contrast-table.txt, do not recompute-and-claim (RESEARCH Pitfall 6):
  Paper vs Moss   = 3.03:1 [AA-large]  → FAIL AA-body on light
  Paper vs Cobalt = 3.66:1 [AA-large]  → FAIL AA-body on light
  Paper vs Amber  = 2.66:1 [FAIL]      → FAIL AA on light
  Ink vs Moss 5.89 / Ink vs Cobalt 4.86 / Ink vs Amber 6.71 = [AA-body] on dark.

Reference scale values for typography.svg + spacing.svg come from theme.css --ax-type-* / --ax-space-* (px+rem each).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy svgo.config.mjs and scaffold generate-specimens.mjs + palette.svg</name>
  <files>brandbook/tokens/harness/svgo.config.mjs, brandbook/tokens/harness/generate-specimens.mjs, brandbook/examples/palette.svg</files>
  <read_first>
    - brandbook/logo/harness/svgo.config.mjs (copy verbatim — multipass, preserves viewBox/title/desc; lines 1-34)
    - brandbook/logo/harness/generate-logo-suite.mjs (injectMeta/svgoOptimize/addBgRect/writeSvg patterns; lines 135-179)
    - brandbook/tokens/tokens.json + brandbook/tokens/harness/lib.mjs (flattenTokens to enumerate swatches)
    - .planning/phases/180-brand-audit-dna-lock/artifacts/contrast-table.txt (the AA rows to annotate)
    - .planning/phases/184-design-tokens-specimens/184-CONTEXT.md (D-15 palette checklist)
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md (palette.svg specimen skeleton lines ~476-495)
  </read_first>
  <action>
    Copy `brandbook/logo/harness/svgo.config.mjs` verbatim to `brandbook/tokens/harness/svgo.config.mjs`. Create `generate-specimens.mjs` importing flattenTokens from ./lib.mjs, `optimize` from svgo, and the local svgo.config.mjs. Resolve OUTPUT_DIR = path.resolve(__dirname, "../../examples") and create `brandbook/examples/` if missing. Implement helpers mirroring generate-logo-suite (injectMeta for <title>/<desc>, addBgRect for dark surface bands, writeSvg). Build palette.svg (D-15): a deterministic standalone <svg> with viewBox + <title>Accrue palette specimen</title><desc>… AA status …</desc>. Render TWO surface bands — a light (Paper) band and a dark (Ink/#0f1318) band. In each band, one <g> per swatch covering all 7 raw --accrue-* tokens AND the semantic roles (surface/content/interactive/feedback): rect(fill=hex) + <text> token name + <text font-family="Geist Mono">hex</text> + <text> role + <text> AA status. AA status text MUST come from contrast-table.txt rows — explicitly flag Moss/Cobalt/Amber as "AA-FAIL body (light)" on the light band (Paper vs Moss 3.03 AA-large, Cobalt 3.66 AA-large, Amber 2.66 FAIL) and "[AA-body]" on the dark band (Ink vs Moss/Cobalt/Amber). Sort swatches deterministically; fixed coordinate math; round numbers with .toFixed(3); run through svgo; write ending in "\n". (typography + spacing SVGs are Task 2.)
  </action>
  <verify>
    <automated>cd brandbook/tokens/harness && node generate-specimens.mjs && cd ../../.. && test -f brandbook/examples/palette.svg && grep -q '<title>' brandbook/examples/palette.svg && grep -qi 'moss' brandbook/examples/palette.svg && node brandbook/tokens/harness/generate-specimens.mjs && git diff --exit-code -- brandbook/examples/palette.svg && echo PALETTE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/examples/palette.svg` exists, is valid SVG with a `<title>` and `<desc>`, and contains the moss token + hex `#5e9e84`.
    - palette.svg includes BOTH a light surface band and a dark surface band (two background rects / surfaces).
    - palette.svg annotates Moss/Cobalt/Amber as AA-FAIL on light body (text sourced from contrast-table.txt values, not invented).
    - Re-running the generator leaves `git diff --exit-code -- brandbook/examples/palette.svg` clean (determinism, D-17).
    - svgo.config.mjs in the tokens harness matches the logo harness config (multipass, preserves viewBox/title/desc).
    - The verify command prints `PALETTE_OK`.
  </acceptance_criteria>
  <done>svgo config copied; generate-specimens.mjs emits a deterministic palette.svg with every swatch + role, hex/token/role/AA on light + dark surfaces, AA flags sourced from contrast-table.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Emit typography.svg and spacing.svg</name>
  <files>brandbook/tokens/harness/generate-specimens.mjs, brandbook/examples/typography.svg, brandbook/examples/spacing.svg</files>
  <read_first>
    - brandbook/tokens/harness/generate-specimens.mjs (the file from Task 1 being extended)
    - accrue_admin/assets/css/theme.css (lines 16-33 — --ax-type-* and --ax-space-* values with px comments)
    - .planning/phases/184-design-tokens-specimens/184-CONTEXT.md (D-15 typography + spacing checklists)
    - .planning/phases/184-design-tokens-specimens/184-RESEARCH.md (type-specimen glyph-metrics decision lines ~497-498 — text labels, not outlines)
  </read_first>
  <action>
    Extend generate-specimens.mjs to also emit typography.svg and spacing.svg. typography.svg (D-15): the full Geist sans + Geist Mono size scale (--ax-type-xs 0.75rem/12px through --ax-type-3xl 2.25rem/36px), each step a <text font-family="Geist, system-ui"> sample line rendered AT that size with a px+rem label, plus a Geist Mono sample row. spacing.svg (D-15): every spacing step (--ax-space-2xs 0.125rem/2px through --ax-space-3xl 4rem/64px) as a labeled visual ruler/bar — a <rect> whose width encodes the step plus a <text> token name + px/rem label. Source the scale values from theme.css (these are reference-only, D-11 — read them as literals, do not mint --accrue-* tokens). Both SVGs: <title>/<desc>, deterministic coordinate math, fixed decimals, svgo pass, trailing "\n". Re-run the generator so all three examples/*.svg are written.
  </action>
  <verify>
    <automated>cd brandbook/tokens/harness && node generate-specimens.mjs && cd ../../.. && test -f brandbook/examples/typography.svg && test -f brandbook/examples/spacing.svg && grep -qi 'geist' brandbook/examples/typography.svg && node brandbook/tokens/harness/generate-specimens.mjs && git diff --exit-code -- brandbook/examples/typography.svg brandbook/examples/spacing.svg && echo TYPE_SPACE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/examples/typography.svg` exists, references the Geist font-family, and shows multiple type steps with px+rem labels.
    - `brandbook/examples/spacing.svg` exists and renders every spacing step as a labeled ruler/bar.
    - Re-running the generator leaves `git diff --exit-code -- brandbook/examples/typography.svg brandbook/examples/spacing.svg` clean (determinism).
    - Type/spacing values are read as literals from the reference scale; NO `--accrue-type*`/`--accrue-space*` tokens are minted.
    - The verify command prints `TYPE_SPACE_OK`.
  </acceptance_criteria>
  <done>All three specimen SVGs are generated deterministically from the SSOT; typography and spacing cover their full D-15 checklists with text labels (no glyph outlines).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write verify-specimens.mjs content-coverage check (SC#3)</name>
  <files>brandbook/tokens/harness/verify-specimens.mjs</files>
  <read_first>
    - brandbook/tokens/harness/verify-specimens.mjs (file being created)
    - brandbook/tokens/harness/lib.mjs (flattenTokens to enumerate expected tokens)
    - brandbook/examples/palette.svg, typography.svg, spacing.svg (artifacts being asserted)
    - brandbook/logo/harness/geist-spine-mono.mjs (isMain + exit-code pattern)
    - .planning/phases/184-design-tokens-specimens/184-VALIDATION.md (SC#3 row — content assert + determinism)
  </read_first>
  <behavior>
    - Asserts each raw --accrue-* token name appears in palette.svg.
    - Asserts palette.svg contains both a light and a dark surface band and the Moss/Cobalt/Amber AA-FAIL flag on light.
    - Asserts each type step label appears in typography.svg and each spacing step appears in spacing.svg.
    - Exits 0 when all content present, non-zero naming the first missing item otherwise.
  </behavior>
  <action>
    Create verify-specimens.mjs reading the three SVG files and asserting the SC#3 content checklist above (string-presence assertions; enumerate expected tokens via flattenTokens + the reference scale literals). console.error any missing item; process.exit(failures===0?0:1). Mirror the geist-spine-mono isMain/exit-code shape. This is the SC#3 content assertion referenced by 184-VALIDATION.md.
  </action>
  <verify>
    <automated>node brandbook/tokens/harness/verify-specimens.mjs && echo VERIFY_SPECIMENS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tokens/harness/verify-specimens.mjs` exits 0 against the three generated SVGs and prints a coverage OK line.
    - Negative check: removing a swatch token name from a copy of palette.svg makes the script exit non-zero naming it (executor demonstrates once, does not commit the mutation).
    - The verify command prints `VERIFY_SPECIMENS_OK`.
  </acceptance_criteria>
  <done>verify-specimens.mjs proves SC#3 content coverage (every swatch/type-step/spacing-step + light+dark bands + AA flags) with a 0/non-zero exit contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.json + contrast-table.txt → specimen generator | In-repo trusted inputs transformed to committed SVGs |
| committed *.svg → CI diff gate | Determinism boundary — regeneration must reproduce byte-identical SVGs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-11 | Repudiation | Non-deterministic SVG output hiding a real change | mitigate | Sorted token order, fixed coordinate math, fixed decimals, committed svgo.config.mjs (multipass), trailing newline (RESEARCH Pitfall 5); each task verify re-runs the generator and asserts `git diff --exit-code`. |
| T-184-12 | Spoofing | Inventing AA/contrast claims not in contrast-table.txt | mitigate | AA annotations sourced from contrast-table.txt rows (RESEARCH Pitfall 6); verify-specimens asserts the Moss/Cobalt/Amber AA-FAIL flag is present; PR review confirms wording matches the table. |
| T-184-13 | Tampering | Minting --accrue-* scale tokens via the spacing/type specimens | mitigate | Type/spacing values read as literals from the reference scale; acceptance asserts no --accrue-type*/--accrue-space* minted (D-11). |
| T-184-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs — svgo + the three deps gated + locked in Plan 01 (T-184-SC). |
</threat_model>

<verification>
- Task 1: palette.svg has both surfaces, every swatch, AA flags from contrast-table; deterministic.
- Task 2: typography.svg + spacing.svg cover their full checklists; deterministic.
- Task 3: verify-specimens.mjs exits 0 on complete SVGs, non-zero on a removed item.
</verification>

<success_criteria>
- Three deterministic specimen SVGs exist in brandbook/examples/ covering their locked D-15 checklists.
- AA annotations match contrast-table.txt (Moss/Cobalt/Amber AA-FAIL on light body).
- Specimens regenerate byte-identical (git diff --exit-code clean).
- verify-specimens.mjs passes (SC#3); no --accrue-* scale tokens minted.
</success_criteria>

<output>
Create `.planning/phases/184-design-tokens-specimens/184-04-SUMMARY.md` when done
</output>
</content>
</invoke>

---
phase: 184-design-tokens-specimens
plan: 05
type: execute
wave: 3
depends_on: ["184-02", "184-03", "184-04"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [TOK-01, TOK-02, TOK-03]
user_setup: []

must_haves:
  truths:
    - "CI installs the tokens harness from the committed lockfile, regenerates tokens.css + specimens, and fails on any diff (determinism gate, D-17)"
    - "CI runs the parity check AND its --test fixture as a separate correctness gate (SC#2 both directions)"
    - "The brandbook gate runs in the docs-contracts-shift-left job with no native deps and zero admin code changes"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "brandbook tokens determinism + parity CI steps"
      contains: "parity-check.mjs"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "brandbook/tokens/harness/package-lock.json"
      via: "npm ci"
      pattern: "npm ci"
    - from: ".github/workflows/ci.yml"
      to: "brandbook/tokens/tokens.css + brandbook/examples/*.svg"
      via: "git diff --exit-code"
      pattern: "git diff --exit-code"
---

<objective>
Wire the brandbook token toolchain into CI as two distinct gates (D-17): a determinism gate (`npm ci` → regenerate tokens.css + specimens → `git diff --exit-code`) and a correctness gate (`parity-check.mjs` plus its `--test` injected-drift fixture). There is no existing brandbook CI wiring — this is new work whose shape mirrors the admin-asset determinism gate and the node-setup pattern already in `ci.yml`.

Purpose: closes the Wave 0 CI requirement from 184-VALIDATION.md and makes SC#1/SC#2/SC#3 enforceable on every push. Zero admin code changes.
Output: new steps appended to the `docs-contracts-shift-left` job in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-design-tokens-specimens/184-CONTEXT.md
@.planning/phases/184-design-tokens-specimens/184-PATTERNS.md
@.planning/phases/184-design-tokens-specimens/184-VALIDATION.md
@.planning/phases/184-design-tokens-specimens/184-02-SUMMARY.md
@.planning/phases/184-design-tokens-specimens/184-03-SUMMARY.md
@.planning/phases/184-design-tokens-specimens/184-04-SUMMARY.md
@.github/workflows/ci.yml
@brandbook/tokens/harness/package.json

<interfaces>
<!-- CI shape-analogs already in ci.yml (PATTERNS CI section): -->
  docs-contracts-shift-left job: ci.yml:31  (runs-on ubuntu-24.04, no native deps — recommended home)
  setup-node@v6 + node 22 pattern: ci.yml:457-478
  git diff --exit-code asset determinism gate: ci.yml:392-396

npm scripts available in brandbook/tokens/harness/package.json (from Plan 01):
  generate     -> node generate-tokens-css.mjs
  specimens    -> node generate-specimens.mjs
  parity       -> node parity-check.mjs
  parity-test  -> node parity-check.mjs --test

Determinism gate output set (D-17): brandbook/tokens/tokens.css + brandbook/examples/{palette,typography,spacing}.svg
Two DISTINCT gates: git diff --exit-code (reproducibility) vs parity-check (correctness).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brandbook tokens determinism + parity steps to CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the docs-contracts-shift-left job at line 31; the node-setup pattern at ~457-478; the diff-gate pattern at ~392-396)
    - brandbook/tokens/harness/package.json (the npm script names to invoke)
    - .planning/phases/184-design-tokens-specimens/184-PATTERNS.md (CI workflow step section — exact recommended steps lines ~313-326)
    - .planning/phases/184-design-tokens-specimens/184-VALIDATION.md (Wave 0 CI requirement; full suite command)
  </read_first>
  <action>
    Add a Node setup step and brandbook token gate steps to the `docs-contracts-shift-left` job in `.github/workflows/ci.yml` (it already runs on ubuntu-24.04 with no native deps — the recommended home per PATTERNS). Steps, in order: (1) `actions/setup-node@v6` with `node-version: '22'`, `cache: npm`, `cache-dependency-path: brandbook/tokens/harness/package-lock.json`. (2) "Install tokens harness" — `cd brandbook/tokens/harness && npm ci` (consumes the committed lockfile, D-17). (3) "Regenerate tokens + specimens" — `cd brandbook/tokens/harness && npm run generate && npm run specimens`. (4) "Tokens/specimens are reproducible (determinism gate, D-17)" — `git diff --exit-code -- brandbook/tokens/tokens.css brandbook/examples/palette.svg brandbook/examples/typography.svg brandbook/examples/spacing.svg`. (5) "Brand↔admin token parity (SC#2)" — `cd brandbook/tokens/harness && npm run parity`. (6) "Parity drift detection proof (SC#2 both directions)" — `cd brandbook/tokens/harness && npm run parity-test`. Keep these as TWO distinct gates (diff = reproducibility; parity + parity-test = correctness). Do NOT touch any accrue_admin file or any other job. Preserve existing YAML indentation/structure exactly.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); steps=d['jobs']['docs-contracts-shift-left']['steps']; runs=' '.join(s.get('run','') for s in steps)+' '+' '.join(str(s.get('with','')) for s in steps); assert 'npm run parity' in runs, 'parity step missing'; assert 'parity-test' in runs, 'parity-test step missing'; assert 'git diff --exit-code' in runs and 'brandbook/tokens/tokens.css' in runs, 'determinism gate missing'; assert 'brandbook/tokens/harness/package-lock.json' in runs, 'node cache path missing'; print('CI_YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML (the python3 verify above succeeds and prints `CI_YAML_OK`).
    - The `docs-contracts-shift-left` job contains: a setup-node@v6 step caching `brandbook/tokens/harness/package-lock.json`; an `npm ci` install in the tokens harness; a `npm run generate && npm run specimens` regenerate step; a `git diff --exit-code` step over tokens.css + the three examples/*.svg; an `npm run parity` step; an `npm run parity-test` step.
    - `git diff --exit-code -- accrue_admin/` stays clean (no admin file touched).
    - No other CI job is modified.
  </acceptance_criteria>
  <done>CI enforces both the determinism gate (regenerate + diff) and the correctness gate (parity + parity-test) for the brandbook tokens, in the native-dep-free docs-contracts-shift-left job, with zero admin changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed lockfile → CI npm ci | CI installs third-party packages; pinned by the lockfile committed + human-verified in Plan 01 |
| committed brandbook artifacts → CI diff/parity gates | CI is the enforcement boundary for determinism (D-17) and parity (SC#2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-SC | Tampering | npm ci in CI pulling a mutated dependency | mitigate | `npm ci` consumes the committed `package-lock.json` (integrity-hashed), which was human-verified in Plan 01 (T-184-SC); no `npm install` in CI. |
| T-184-14 | Repudiation | a committed-artifact drift slipping past review | mitigate | `git diff --exit-code` determinism gate fails CI if tokens.css/specimens are stale; `parity-check` + `parity-test` fail CI on undocumented brand↔admin drift. |
| T-184-15 | Tampering | CI step inadvertently editing read-only admin theme.css | mitigate | Steps only read theme.css (via parity-check) and write only brandbook outputs; acceptance asserts `git diff --exit-code -- accrue_admin/` stays clean. |
</threat_model>

<verification>
- Task 1: ci.yml parses; docs-contracts-shift-left job has node setup + npm ci + regenerate + diff gate + parity + parity-test; no admin file touched; no other job changed.
</verification>

<success_criteria>
- CI runs the two distinct brandbook gates (determinism + parity/parity-test) on every non-schedule push.
- The brandbook gate lives in the native-dep-free docs-contracts-shift-left job and caches the harness lockfile.
- Zero admin code changes; ci.yml remains valid YAML.
</success_criteria>

<output>
Create `.planning/phases/184-design-tokens-specimens/184-05-SUMMARY.md` when done
</output>

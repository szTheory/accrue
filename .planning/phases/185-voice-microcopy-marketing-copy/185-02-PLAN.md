---
phase: 185-voice-microcopy-marketing-copy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/copy.md
autonomous: true
requirements:
  - COPY-02

must_haves:
  truths:
    - "Every adopter-facing copy surface has a ready-to-paste block — no surface left blank"
    - "GitHub repo description block contains the exact locked string from D-05"
    - "Hex.pm description block leads with 'Elixir' and lists concrete feature nouns, per D-05"
    - "The comparison table contains stripity_stripe and raw Stripe API rows, no Bling row"
    - "Pay (Rails) and Laravel Cashier are credited in prose, not as comparison table rows (D-01)"
    - "No copy block contains any of the banned adjectives from D-10"
    - "No CTA block contains any banned CTA from D-11"
    - "Error/empty/success-state microcopy examples exist and use Precise=5 register (D-09)"
    - "Release-note and changelog voice templates exist"
  artifacts:
    - path: "brandbook/copy.md"
      provides: "Complete set of ready-to-paste copy blocks for every adopter-facing surface"
      contains: "## GitHub"
  key_links:
    - from: "brandbook/copy.md"
      to: "brandbook/voice.md"
      via: "Every copy block in copy.md must satisfy the constraints in voice.md"
      pattern: "voice\\.md"
    - from: "brandbook/copy.md"
      to: "brandbook/index.html (Phase 186)"
      via: "Phase 186 inlines copy.md sections into the HTML brand book"
      pattern: "copy\\.md"
---

<objective>
Write all ready-to-paste copy blocks at `brandbook/copy.md` covering every adopter-facing surface.

Purpose: Deliver the complete set of copy that Phase 185 success criterion #2 requires — so no surface is left with a placeholder when someone ships Accrue to Hex, GitHub, or a landing page. All blocks are written per the locked decisions (D-01..D-11) and the voice constraints in `brandbook/voice.md` (Wave 1 sibling).

Output: `brandbook/copy.md` — organized by surface, each block clearly labeled, ready to paste verbatim with no further editing required (editing happens at the review gate in Plan 03, not after).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/phases/185-voice-microcopy-marketing-copy/185-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md
@/Users/jon/projects/accrue/README.md
@/Users/jon/projects/accrue/brandbook/README.md
@/Users/jon/projects/accrue/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write brandbook/copy.md — all copy blocks</name>
  <files>brandbook/copy.md</files>

  <read_first>
  1. `.planning/phases/185-voice-microcopy-marketing-copy/185-CONTEXT.md` — read D-01 through D-11 in full. Every locked string, every banned word, every CTA rule comes from here. This is the primary constraint source; do not deviate.
  2. `.planning/phases/180-brand-audit-dna-lock/BRAND-DNA.md` — ratified voice adjectives, positioning statement, Do/Don't register.
  3. `README.md` (root) — mine for on-brand prose: the hero block (already correct per D-06, use it verbatim as the README hero block), the VERIFY-01 proof-path section (source of proof-led language for benefit copy), the "Stable first-time setup surface" section (feature nouns for Hex description).
  4. `CLAUDE.md` — Core Value statement, security posture, deps — the substantiated facts behind D-10 proof-led claims. Use this to derive verifiable mechanism statements.
  5. `brandbook/README.md` — file register/format conventions to match.

  NOTE: `brandbook/voice.md` is being written in parallel (Wave 1). Do not depend on it being present — derive copy constraints from 185-CONTEXT.md D-01..D-11 directly.
  </read_first>

  <action>
  Create `brandbook/copy.md` as a standalone, browser-readable markdown file organized by surface. Phase 186 will inline this file into the HTML brand book — keep it clean, no internal nav anchors required (Phase 186 will add those).

  Required sections in this exact order, using these exact H2 headings:

  ---

  ## GitHub

  **H3: Repo description**
  The exact locked string from D-05 (copy verbatim, no paraphrasing):
  `Billing state, modeled clearly — the Elixir billing library for Phoenix apps`

  Note: This 69-character string fits GitHub's 350-char limit with room for topics.

  **H3: Topics / tags**
  A comma-separated list of 10-15 GitHub topics for discoverability. Must include: `elixir`, `phoenix`, `billing`, `subscriptions`, `stripe`, `ecto`, `payments`, `saas`, `webhooks`, `hex`. Add 2-3 more that are accurate (e.g. `invoices`, `open-source`). Do not pad with irrelevant tags.

  ---

  ## Hex.pm

  **H3: Package description**
  A 1-2 sentence Hex `description:` field replacement per D-05. Must: lead with "The Elixir-native billing library" or "The Elixir billing library for Phoenix apps" (search-indexed category noun first); list concrete feature nouns in a scannable series. Derive from the "Stable first-time setup surface" section of README.md and CLAUDE.md Core Value. Example shape (executor must draft the actual text): "The Elixir-native billing library for Phoenix apps. Subscriptions, checkout, invoices, coupons, emails, PDFs, webhook handling, admin UI." Keep under 300 characters (Hex display truncation).

  Note below the block: "Replaces current mix.exs description: 'Billing state, modeled clearly.' (too abstract for search-indexed context per D-05)."

  ---

  ## HexDocs

  **H3: Intro paragraph**
  The opening paragraph that appears above the module index on HexDocs (typically set in the `:docs` `intro_md:` option or the leading paragraph of `accrue/lib/accrue.ex`'s moduledoc). Tone: Precise=5 / Formal=4 (API reference surface, D-09). Must use the positioning statement ("Accrue is the Elixir-native billing library for Phoenix apps."), state the core integration model (host app owns facade/routes/auth, Accrue owns the billing engine), and reference the proof path (VERIFY-01 / Fake-backed evaluation). 3-4 sentences. No adjective-led claims.

  ---

  ## README Hero

  **H3: H1 + tagline + descriptor**
  The current root README.md hero is already correct (per D-06 and CONTEXT.md canonical_refs). Copy it verbatim as the approved block:

  ```
  # Accrue

  Billing state, modeled clearly.

  Accrue is an open-source billing library for Elixir, Ecto, and Phoenix. Your app owns the billing facade, routes, auth boundary, and runtime config; Accrue owns the billing engine behind them.
  ```

  Add a note: "This block is already live in root README.md and matches D-06. Do not restructure — tagline leads because this is an engaged surface."

  ---

  ## Landing Page

  All landing page sections are "ready-to-paste" into an HTML/template file. Tone: Casual=2 / Precise=3 (landing/marketing surface, D-09). Still proof-led, no adjective-led claims.

  **H3: Hero section**
  Headline: the tagline "Billing state, modeled clearly." (D-06: tagline leads on landing because it's an engaged surface)
  Subhead: the descriptor "The Elixir billing library for Phoenix apps." (concrete, category-locked)
  Body (1-2 sentences): state what Accrue delivers — derive from CLAUDE.md Core Value and README prose. Must be mechanism-led, not adjective-led.
  CTA block:
  - Primary: "Get started" (links to docs/install)
  - Secondary: "Browse the source" (links to GitHub)
  - Sub-CTA: "MIT · Elixir 1.17+ · Phoenix 1.8+"
  No exclamation marks. No banned CTAs.

  **H3: Problem section**
  The gap Accrue fills: Elixir devs building SaaS need billing that is idiomatic to Ecto, modeled explicitly, and ships complete — not a thin API wrapper requiring months of custom domain work. State this as a factual situation, not an attack on other libraries. 2-3 sentences. Reference the "migration pain / design regrets" framing as a private motivation only (D-04) — public copy states what Accrue does.

  **H3: Solution section**
  What Accrue delivers: list 4-6 concrete capability statements (mechanism-led, no adjectives). Each statement should be verifiable. Derive from CLAUDE.md Core Value + VERIFY-01 proof path + README stable setup surface. Examples of the shape:
  - "Every webhook is signature-verified before it touches your database, and the verification step can't be bypassed."
  - "A tamper-evident, append-only event ledger records every billing state change."
  - "Ships complete at v1.0: subscriptions, checkout, invoices, coupons, emails, PDFs, and a mounted admin UI."
  Do not use: production-grade, batteries-included, bank-grade, modern alternative.

  **H3: Install section**
  A one-block install snippet (Markdown, not a fenced code block — write it as a verbatim preformatted block the executor will format appropriately):
  ```elixir
  # mix.exs deps
  {:accrue, "~> 1.4"},
  {:accrue_admin, "~> 1.4"}
  ```
  Follow with: "Then run `mix deps.get && mix accrue.install`." (or the accurate install command from the README / guides). Plus a link text: "See the install guide →" (no exclamation mark).

  **H3: Benefits section**
  3-4 benefit statements. Each benefit is a mechanism described from the adopter's perspective — what it means for them, not what Accrue is. Shape: "[What the adopter gains] because [mechanism]." Example: "Your PR is merge-blocked if billing correctness regresses, because the Fake-backed proof suite runs on every CI push." Derive from VERIFY-01, CLAUDE.md Core Value, and the stable setup surface. No adjective-led claims.

  **H3: Comparison section**
  Brief prose acknowledging ecosystem context (D-01 gracious credits):
  "Accrue is inspired by Pay for Rails and Laravel Cashier — both excellent in their ecosystems. Accrue brings the same table-stakes completeness to Elixir, built idiomatically for Ecto and Phoenix."

  Then the comparison table per D-02 and D-03. Three columns: Feature | stripity_stripe | Raw Stripe API | Accrue. Rows must be verifiable capability facts, not value judgments. Required rows:
  1. Stripe API version — "Pinned to 2019 API (no active updates)" | "Current (caller manages)" | "Current via lattice_stripe (tracks Stripe 2026-03-25.dahlia)"
  2. Ecto-native domain modeling — "None (raw HTTP responses)" | "None (raw HTTP responses)" | "Explicit Ecto schemas: Subscription, Invoice, Coupon, etc."
  3. Webhook handling — "None (caller implements)" | "None (caller implements)" | "Signature-verified, async via Oban, non-bypassable"
  4. Admin UI — "None" | "None" | "Mounted LiveView dashboard (accrue_admin)"
  5. Test support — "None (HTTP stubs required)" | "None (HTTP stubs required)" | "Fake processor — no live keys, no HTTP in tests"

  Add NO Bling row. Do not name Bling anywhere in copy.md.
  Add a factual footnote: "stripity_stripe: last significant Stripe API update was 2019 (source: package hex.pm/packages/stripity_stripe changelog). Raw Stripe API: all domain modeling and webhook verification is caller-responsibility."

  **H3: CTAs (secondary)**
  A secondary CTA block for mid-page or footer use:
  - "Read the guide" (links to first_hour.md or docs)
  - "View on Hex" (links to hex.pm/packages/accrue)
  - "Browse the source" (links to GitHub)
  No exclamation marks. No "demo", "free trial", "Get billing in minutes".

  ---

  ## Release Notes

  **H3: Voice template**
  A fill-in-the-blank template for release note entries. Tone: Casual=2-3 / Precise=4 (release notes surface, D-09). Demonstrate the register with a concrete example.

  Template structure:
  ```
  ## v{VERSION} — {DATE}

  ### Added
  - {Mechanism-led description of what was added and why it matters}

  ### Fixed
  - {What was wrong; what the correct behavior is now}

  ### Changed
  - {What changed; migration note if any}
  ```

  Example release note block (not real, but register-correct):
  ```
  ## v1.5.0 — 2026-07-01

  ### Added
  - `Accrue.Billing.meter_event/2` records usage events for metered subscriptions via Stripe's Meter Events API, with two-layer idempotency via Oban uniqueness.

  ### Fixed
  - `Accrue.Webhook.Handler` now correctly handles `customer.subscription.paused` events that arrive before the corresponding `invoice.finalized` — no state machine regression.
  ```

  **H3: Changelog entry pattern**
  A note on the voice rule for changelog entries: "Lead with the affected module or behavior, not with 'We'. State the corrected behavior, not the symptom. Include a migration note if any public-facing API changed."

  ---

  ## Microcopy

  **H3: Error states**
  Tone: Precise=5 / Formal=3 (error/empty-state, D-09). Examples must be specific to Accrue's domain (billing errors, not generic 404s).

  5-7 error message examples. Format: `[Context]` — `[Message text]` — `[Guidance text]`.

  Derive error domains from CLAUDE.md (webhook sig verification, Stripe API, config). Examples of the shape:
  - Context: Webhook signature verification failure
    Message: "Signature mismatch — this request was not accepted."
    Guidance: "Check that ACCRUE_WEBHOOK_SECRET matches the value in your Stripe dashboard."
  - Context: Stripe API key missing at startup
    Message: "ACCRUE_STRIPE_SECRET_KEY is not set."
    Guidance: "Set this in config/runtime.exs from an environment variable. See the configuration guide."
  - Context: Subscription not found (admin UI)
    Message: "No subscription found for this customer."
    Guidance: "The customer may not have completed checkout, or the event has not been processed yet."

  No apologetic softening ("Sorry, something went wrong"). No adjective-led reassurance ("Our secure system..."). State the fact; give the next action.

  **H3: Empty states**
  3-4 empty state messages for the admin UI (accrue_admin) — surfaces with no data yet. Same tone register as error states.

  Examples of the shape:
  - Context: Subscriptions list, no subscriptions yet
    Message: "No active subscriptions."
    Guidance: "Subscriptions appear here after a customer completes checkout."
  - Context: Webhook events log, empty
    Message: "No webhook events received."
    Guidance: "Send a test event from the Stripe dashboard or run `mix verify` to generate Fake events."

  **H3: Success states**
  3-4 success confirmations. Brief, factual. No exclamation marks.

  Examples:
  - "Subscription activated." (in-admin confirmation after manual activation)
  - "Webhook event processed and recorded." (post-processing status)
  - "Invoice finalized." (after manual finalization action)

  ---

  File conventions: H1 `# Accrue Copy Blocks` at top, ratification line. H2 per surface. H3 per block within surface. Verbatim copy blocks in plain text (not fenced) or in a clearly labeled blockquote where needed for visual distinction — Phase 186 will handle the HTML rendering. Keep ≤ 350 lines. No fenced code blocks in the action instructions above means: in the copy.md file itself, the copy blocks are plain text (not in ``` fences), so Phase 186 can render them directly. Exception: the install snippet block is legitimately code and SHOULD be in a fenced elixir block in copy.md itself.
  </action>

  <verify>
  <automated>
  # File exists
  test -f /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: brandbook/copy.md" && exit 1)

  # Required H2 sections present
  grep -q "^## GitHub" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## GitHub" && exit 1)
  grep -q "^## Hex" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## Hex.pm" && exit 1)
  grep -q "^## HexDocs" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## HexDocs" && exit 1)
  grep -q "^## README" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## README Hero" && exit 1)
  grep -q "^## Landing" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## Landing Page" && exit 1)
  grep -q "^## Release" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## Release Notes" && exit 1)
  grep -q "^## Microcopy" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: ## Microcopy" && exit 1)

  # Exact locked GitHub repo description string present (D-05)
  grep -q "Billing state, modeled clearly — the Elixir billing library for Phoenix apps" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: exact GitHub description from D-05" && exit 1)

  # Comparison table contains stripity_stripe row (D-02)
  grep -q "stripity_stripe" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: stripity_stripe in comparison table (D-02)" && exit 1)

  # Comparison table contains Raw Stripe API row (D-02)
  grep -qi "raw stripe\|raw stripe api\|Raw Stripe" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: raw Stripe API row in comparison table (D-02)" && exit 1)

  # No Bling row (D-03)
  grep -qi "^.*[|].*[Bb]ling.*[|]" /Users/jon/projects/accrue/brandbook/copy.md && (echo "VIOLATION: Bling appears as a comparison table row (D-03 forbids this)" && exit 1) || true

  # Pay and Cashier credited in prose, not as table rows
  grep -qi "Pay\|Cashier" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: gracious credit to Pay/Cashier in prose (D-01)" && exit 1)

  # Banned adjectives absent from copy blocks (D-10)
  # Use grep -v to skip comment/note lines (lines with "Replaces" or "Note:")
  if grep -v "^>" /Users/jon/projects/accrue/brandbook/copy.md | grep -v "^Note\|^note\|Replaces\|current mix\|banned\|Avoid\|Don't\|don't\|## Vocabulary\|production-grade\|batteries-included\|bank-grade" | grep -qi "production-grade\|batteries-included\|bank-grade\|modern alternative"; then
    echo "VIOLATION: banned adjective(s) from D-10 found in copy blocks"
    exit 1
  fi

  # Banned CTAs absent (D-11)
  if grep -qi "Start free\|Try it now\|Get billing in minutes" /Users/jon/projects/accrue/brandbook/copy.md; then
    echo "VIOLATION: banned CTA from D-11 found in copy blocks"
    exit 1
  fi

  # Approved primary CTAs present
  grep -q "Get started\|Install" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: approved primary CTA (Get started / Install)" && exit 1)

  # Sub-CTA spec string present (D-11)
  grep -q "MIT.*Elixir.*Phoenix\|Elixir.*Phoenix.*MIT" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: sub-CTA spec string (MIT · Elixir 1.17+ · Phoenix 1.8+)" && exit 1)

  # Microcopy sections present
  grep -qi "Error state\|error message\|error state\|signature\|mismatch" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: error state microcopy (D-09 Precise=5 surface)" && exit 1)
  grep -qi "empty state\|no active\|no subscription" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: empty state microcopy" && exit 1)
  grep -qi "success\|activated\|processed\|finalized" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: success state microcopy" && exit 1)

  # Release note template present
  grep -qi "release\|changelog\|v{VERSION}\|Added\|Fixed\|Changed" /Users/jon/projects/accrue/brandbook/copy.md || (echo "MISSING: release note template" && exit 1)

  # File is under 350 lines
  LINES=$(wc -l < /Users/jon/projects/accrue/brandbook/copy.md)
  [ "$LINES" -le 350 ] || (echo "OVERSIZED: copy.md is $LINES lines (limit 350)" && exit 1)

  echo "copy.md: all assertions passed"
  </automated>
  </verify>

  <acceptance_criteria>
  - `brandbook/copy.md` exists and is committed to git
  - File contains all 7 required H2 sections: GitHub, Hex.pm, HexDocs, README Hero, Landing Page, Release Notes, Microcopy
  - GitHub repo description block contains EXACTLY: `Billing state, modeled clearly — the Elixir billing library for Phoenix apps` (per D-05)
  - Hex.pm description block leads with "Elixir" / "Elixir-native" and lists concrete feature nouns (per D-05)
  - Comparison table contains a `stripity_stripe` row and a `Raw Stripe API` row (per D-02)
  - Comparison table contains NO Bling row (per D-03) — verified by grep scan
  - Pay (Rails) and Laravel Cashier are credited in prose, not as table rows (per D-01)
  - No copy block contains: "production-grade", "batteries-included", "bank-grade", "modern alternative" (per D-10)
  - No CTA block contains: "Start free", "Try it now", "Get billing in minutes", exclamation marks in CTAs (per D-11)
  - Approved primary CTAs "Get started" and "Install" are present
  - Sub-CTA spec `MIT · Elixir 1.17+ · Phoenix 1.8+` (or equivalent) is present
  - Microcopy section contains error state, empty state, and success state examples
  - Release notes section contains a voice template with Added/Fixed/Changed structure
  - File is ≤ 350 lines
  </acceptance_criteria>

  <done>
  `brandbook/copy.md` exists, all 7 surface sections present, exact D-05 GitHub string present, comparison table correct (stripity_stripe + Raw Stripe, no Bling), banned adjectives/CTAs absent, microcopy and release note templates present, ≤ 350 lines, committed to git.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan produces static markdown prose in brandbook/ only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure markdown authoring |

This phase produces only static markdown copy in `brandbook/`. There is no executable code, no network surface, no user input, and no data handling. There are no in-scope STRIDE threats.
</threat_model>

<verification>
Run the automated assertions in Task 1's `<verify>` block. All must pass before the plan is marked complete.

Cross-check manually: open the comparison table in copy.md and confirm no Bling row exists; confirm stripity_stripe's 2019 API note is factual (not a value judgment).
</verification>

<success_criteria>
`brandbook/copy.md` committed, all 7 surface sections present, exact D-05 GitHub string verbatim, comparison table correct (stripity_stripe + Raw Stripe API only, no Bling), no banned adjectives in copy blocks, no banned CTAs, microcopy examples present for error/empty/success states, release note template present, ≤ 350 lines.
</success_criteria>

<output>
Create `.planning/phases/185-voice-microcopy-marketing-copy/185-02-SUMMARY.md` when done.
</output>

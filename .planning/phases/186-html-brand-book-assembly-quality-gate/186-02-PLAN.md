---
phase: 186-html-brand-book-assembly-quality-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 186-01
files_modified:
  - brandbook/index.html
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02

must_haves:
  truths:
    - "brandbook/index.html exists and is committed — opening it via file:// shows the Accrue brand book with no build step"
    - "All automated quality-gate assertions pass (no external URLs, no JS frameworks, data-theme present, ≥10 sections, SVGs inlined)"
    - "Committed weight is ≤ 2 MB (git ls-files brandbook/ | xargs du -c)"
    - "4 screenshot files exist in qa-screenshots/ proving light/dark × mobile/desktop renders correctly"
    - "No changes to accrue_admin/assets/css/theme.css or any .ex/.exs files"
  artifacts:
    - path: "brandbook/index.html"
      provides: "Self-contained brand book HTML — inline style, inline SVGs, inline JS toggle, all copy present"
      contains: "VERIFY_BRANDBOOK"
    - path: ".planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/light-desktop.png"
      provides: "Light desktop screenshot evidence"
    - path: ".planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/dark-desktop.png"
      provides: "Dark desktop screenshot evidence"
    - path: ".planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/light-mobile.png"
      provides: "Light 360px screenshot evidence"
    - path: ".planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/dark-mobile.png"
      provides: "Dark 360px screenshot evidence"
  key_links:
    - from: "brandbook/index.html"
      to: "brandbook/tokens/tokens.css"
      via: "inline <style> block contains tokens.css content verbatim"
      pattern: "--accrue-surface-base"
    - from: "brandbook/index.html"
      to: "brandbook/logo/*.svg"
      via: "SVG files inlined as <svg> elements"
      pattern: "<svg.*viewBox"
    - from: "brandbook/harness/verify-brandbook.mjs"
      to: "brandbook/index.html"
      via: "node brandbook/harness/verify-brandbook.mjs"
      pattern: "VERIFY_BRANDBOOK_OK"
---

<objective>
Run `assemble.mjs` to produce the committed `brandbook/index.html`, then run `verify-brandbook.mjs` to confirm all automated quality-gate criteria pass. Commit both the assembled HTML and the qa-screenshots.

Purpose: This plan converts the Phase 186 harness (Plan 01) into the actual deliverable — the committed `brandbook/index.html`. It also produces the screenshot evidence that will be reviewed in the human UAT checkpoint (Plan 03). If `verify-brandbook.mjs` fails any gate, fix the issue in `assemble.mjs` and re-run before committing.

Output:
- `brandbook/index.html` — committed, self-contained brand book
- `.planning/phases/186-.../qa-screenshots/*.png` — 4 screenshot files (light/dark × mobile/desktop)
</objective>

<execution_context>
@/Users/jon/projects/accrue/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/projects/accrue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/186-html-brand-book-assembly-quality-gate/186-01-SUMMARY.md

@/Users/jon/projects/accrue/.planning/phases/186-html-brand-book-assembly-quality-gate/186-RESEARCH.md

<interfaces>
<!-- What Plan 01 created — the executor reads these to understand what is available. -->

From Plan 01 outputs:
- brandbook/harness/assemble.mjs — run with: `node brandbook/harness/assemble.mjs`
  - reads: tokens/tokens.css, logo/*.svg (sorted), examples/*.svg (sorted), voice.md, copy.md, README.md, LICENSE-FONTS.txt
  - writes: brandbook/index.html
  - log prefix: [assemble-brandbook]; prints "[assemble-brandbook] Wrote: brandbook/index.html"

- brandbook/harness/verify-brandbook.mjs — run with: `node brandbook/harness/verify-brandbook.mjs`
  - imports Playwright from: ../../logo/harness/node_modules/playwright/index.js
  - gates: index.html exists, no external src/href URLs, no JS frameworks, data-theme present, ≥10 section IDs, no SVG img refs, committed weight ≤ 2MB
  - screenshots to: .planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/
  - terminal tag on success: "[verify-brandbook] VERIFY_BRANDBOOK_OK"
  - env override: HTML_PATH_OVERRIDE for testing against fixture

Known pitfalls to verify against (from RESEARCH.md):
- Dark-mode CSS must use --accrue-dark-base / --accrue-dark-primary (NOT --accrue-surface-base for dark surfaces)
- Raster PNGs (accrue-social-card.png etc.) must NOT be base64-inlined — use relative <img src="logo/filename"> 
- No external https?:// refs of any kind in committed HTML (except SVG xmlns which is not a src/href)
- Working-dir `du -sh brandbook/` includes node_modules (hundreds of MB) — verifier uses git ls-files instead

No-thrash assertions (run AFTER verify passes):
- `git diff --name-only accrue_admin/` must be empty
- `git diff --name-only -- '*.ex' '*.exs'` must be empty
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run assemble.mjs and resolve any failures</name>
  <files>brandbook/index.html</files>
  <read_first>
    - brandbook/harness/assemble.mjs — read the full script before running; understand what it reads and writes
    - brandbook/tokens/tokens.css — confirm --accrue-dark-* token names (verify assemble.mjs references them correctly in dark CSS)
    - 186-RESEARCH.md §"Common Pitfalls" — all 6 pitfalls; check each applies/doesn't apply before first run
    - brandbook/voice.md — scan for any Markdown constructs beyond the 6 covered by mdToHtml (headings, tables, code fences, bold, italic, bullets); if any are found, extend mdToHtml before running
    - brandbook/copy.md — same scan
  </read_first>
  <action>
    Run `node brandbook/harness/assemble.mjs` from the repo root. If it exits non-zero, read the FATAL error message and fix `assemble.mjs` before re-running. Do not proceed to Task 2 until exit code is 0 and `brandbook/index.html` exists.

    After successful assembly, inspect `brandbook/index.html` manually for the following correctness checks:

    a) Dark-mode CSS: search for `--accrue-dark-base` and `--accrue-dark-primary` — these MUST appear in a `[data-theme="dark"]` scoped CSS block inside the `<style>` tag. If absent, fix `assemble.mjs` (the body background and text color rules for dark theme reference the wrong tokens — see RESEARCH.md Pitfall 2).

    b) Raster PNGs: search for `base64` — must NOT appear. Social card and favicon PNGs must be referenced as `<img src="logo/accrue-social-card.png">` etc.

    c) External URLs: `grep -oE '(src|href)="[^"]*https?://[^"]*"' brandbook/index.html` must return empty. If any external refs exist, fix the assemble.mjs section that introduced them.

    d) JS toggle: `grep "theme-toggle" brandbook/index.html` must return at least one line (the toggle button and the event listener reference it).

    e) Section IDs: `grep -c 'id="section-' brandbook/index.html` must return ≥ 10.

    f) Idempotency: run `node brandbook/harness/assemble.mjs` a second time, then `git diff brandbook/index.html`. Must show no diff (empty = idempotent). If diff is non-empty, fix the non-determinism in `assemble.mjs` (check for unsorted file arrays, injected timestamps, random values).

    Fix-iterate loop: if any check fails, fix `assemble.mjs`, re-run, re-check. Fix `assemble.mjs` not `index.html` directly — the HTML is a generated artifact and must be reproducible.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && node brandbook/harness/assemble.mjs && test -f brandbook/index.html && echo "EXISTS" && grep -c 'id="section-' brandbook/index.html && grep -oE '(src|href)="[^"]*https?://[^"]*"' brandbook/index.html | wc -l | xargs test 0 -eq && echo "NO_EXTERNAL_REFS" && grep "accrue-dark-base" brandbook/index.html | wc -l | xargs test 0 -lt && echo "DARK_TOKENS_OK" && node brandbook/harness/assemble.mjs && git diff --exit-code brandbook/index.html && echo "IDEMPOTENT"</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/harness/assemble.mjs` exits 0; prints `[assemble-brandbook] Wrote: brandbook/index.html`
    - `brandbook/index.html` exists and is > 50,000 bytes (assembled from ~100KB of sources)
    - `grep -c 'id="section-' brandbook/index.html` returns ≥ 10
    - `grep -oE '(src|href)="[^"]*https?://[^"]*"' brandbook/index.html | wc -l` = 0
    - `grep "accrue-dark-base" brandbook/index.html` returns at least one match
    - `grep "theme-toggle" brandbook/index.html` returns at least two matches (button + listener)
    - `grep "base64" brandbook/index.html | wc -l` = 0 (no inlined rasters)
    - Second run of assemble.mjs → `git diff --exit-code brandbook/index.html` exits 0 (idempotent)
  </acceptance_criteria>
  <done>brandbook/index.html exists, passes manual inspection, and is idempotent on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Run verify-brandbook.mjs — all automated quality gates</name>
  <files>brandbook/index.html</files>
  <read_first>
    - brandbook/harness/verify-brandbook.mjs — read the full script to understand what it asserts and what failures look like
    - .planning/phases/186-html-brand-book-assembly-quality-gate/186-RESEARCH.md §"Playwright Verification Script" — screenshot matrix, FILE_URL, waitForLoadState, dark-mode page.evaluate
    - .planning/phases/180-brand-audit-dna-lock/quality-gate-checklist.md — 8 checklist items; identify which are automated (items 6,7,8) vs. manual (items 1,2,3,4,5 — deferred to Plan 03)
  </read_first>
  <action>
    Run `node brandbook/harness/verify-brandbook.mjs` from the repo root. The verifier runs in two phases: structural assertions (synchronous) then Playwright screenshots (async).

    If any structural assertion fails, the verifier prints `[verify-brandbook] MISSING: <label>` and `[verify-brandbook] FAIL — N check(s) failed` then exits 1. Fix the issue in `assemble.mjs`, re-run assembly, then re-run verify.

    Structural assertion failures and fixes:
    - "no external src/href URLs" — find the source of the URL in assemble.mjs and remove or make relative
    - "no JS frameworks" — ensure assemble.mjs does not mention react/vue/etc. in any context (even in copy.md content that mentions competitor frameworks would trip this; make the check more precise if needed — scope grep to `<script>` blocks only)
    - "data-theme attribute" — assemble.mjs must set `data-theme` on `<html>` or reference it in the toggle script
    - "≥10 section IDs" — count the sections; add missing ones to assemble.mjs
    - "committed weight ≤ 2MB" — run `git ls-files brandbook/ | xargs du -c | tail -1`; if over budget, diagnose which file is large (not expected — projected total is ~1.06 MB)

    If Playwright screenshot phase fails:
    - "Chromium not available" — run `node brandbook/logo/harness/node_modules/.bin/playwright install chromium` to install the Playwright-managed Chromium
    - "Cannot find module playwright/index.js" — verify the relative import path in verify-brandbook.mjs resolves correctly from brandbook/harness/; path should be `../../logo/harness/node_modules/playwright/index.js`
    - Blank-render (screenshot is all white/black) — check that FILE_URL uses absolute path; add a brief wait after theme injection

    After `node brandbook/harness/verify-brandbook.mjs` exits 0 and prints `VERIFY_BRANDBOOK_OK`:

    Run no-thrash gates explicitly:
    ```
    git diff --name-only accrue_admin/
    git diff --name-only -- '*.ex' '*.exs'
    ```
    Both must be empty (zero lines). If either is non-empty, the plan has drifted into disallowed scope — investigate and revert.

    Run committed-weight gate explicitly for the commit record:
    ```
    git ls-files brandbook/ | xargs du -c | tail -1
    ```
    Record the byte count in the SUMMARY.

    Stage and commit: `brandbook/index.html` (the assembled artifact) and `.planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/` (screenshot evidence).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && node brandbook/harness/verify-brandbook.mjs && echo "VERIFY_EXIT_0" && test -f .planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/light-desktop.png && test -f .planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/dark-desktop.png && test -f .planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/light-mobile.png && test -f .planning/phases/186-html-brand-book-assembly-quality-gate/qa-screenshots/dark-mobile.png && echo "SCREENSHOTS_OK" && git diff --name-only accrue_admin/ | wc -l | xargs test 0 -eq && echo "NO_THRASH_ADMIN" && git diff --name-only -- '*.ex' '*.exs' | wc -l | xargs test 0 -eq && echo "NO_THRASH_ELIXIR"</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/harness/verify-brandbook.mjs` exits 0
    - Output includes `[verify-brandbook] VERIFY_BRANDBOOK_OK`
    - 4 screenshot files exist: light-desktop.png, dark-desktop.png, light-mobile.png, dark-mobile.png in qa-screenshots/
    - `git diff --name-only accrue_admin/` returns empty (no-thrash: admin CSS unchanged)
    - `git diff --name-only -- '*.ex' '*.exs'` returns empty (no-thrash: no Elixir changes)
    - `git ls-files brandbook/ | xargs du -c | tail -1` reports < 2,097,152 bytes
    - No Playwright blank-render warnings (screenshots show actual content, not blank white/black)
  </acceptance_criteria>
  <done>verify-brandbook.mjs exits 0 with VERIFY_BRANDBOOK_OK; all 4 screenshots produced; no-thrash gates pass; committed weight confirmed ≤ 2MB; brandbook/index.html and screenshots committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembler output → committed HTML | The assembled index.html is the artifact under scrutiny; verify-brandbook.mjs is the automated guard |
| git ls-files → byte count | Child process execSync for size gate; input is the repo's tracked file list — fully developer-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-02 | Information Disclosure | External src/href URLs in index.html | mitigate | verify-brandbook.mjs structural assertion: `(src|href)=["']…https?://` match === 0 (blocking gate, plan fails if non-zero) |
| T-186-03 | Information Disclosure | SVG xlink:href to remote URL | mitigate | No-external-URL assertion in verify-brandbook.mjs covers href attribute in any element; logo SVGs from Phase 183 are path-only (no remote references) |
| T-186-NT | Tampering | Accidental changes to accrue_admin/ or .ex/.exs | mitigate | Explicit no-thrash gates: `git diff --name-only accrue_admin/` and `git diff --name-only -- '*.ex' '*.exs'` both checked in Task 2 acceptance criteria |
| T-186-SC | Tampering | npm/pip/cargo installs | accept | Zero new installs in this plan; Playwright already installed in logo harness node_modules |

This plan runs existing scripts against committed inputs. No server, no auth, no user input, no network. ASVS V2–V6 do not apply.
</threat_model>

<verification>
Full automated gate suite:

```bash
cd /Users/jon/projects/accrue

# Assembly
node brandbook/harness/assemble.mjs

# Idempotency
node brandbook/harness/assemble.mjs && git diff --exit-code brandbook/index.html && echo "IDEMPOTENT"

# All quality gates
node brandbook/harness/verify-brandbook.mjs

# No-thrash
git diff --name-only accrue_admin/ | wc -l
git diff --name-only -- '*.ex' '*.exs' | wc -l

# Committed weight
git ls-files brandbook/ | xargs du -c | tail -1
```

All commands must exit 0 / return expected values before this plan is complete.
</verification>

<success_criteria>
- `brandbook/index.html` committed — self-contained, ≤2 MB total committed weight, all source materials assembled
- `node brandbook/harness/verify-brandbook.mjs` exits 0 and prints VERIFY_BRANDBOOK_OK
- 4 screenshots exist in qa-screenshots/ (light/dark × mobile/desktop)
- No changes to accrue_admin/ or any .ex/.exs file
- `git diff --exit-code brandbook/index.html` exits 0 after second run of assemble.mjs (idempotent)
</success_criteria>

<output>
Create `.planning/phases/186-html-brand-book-assembly-quality-gate/186-02-SUMMARY.md` when done.
Record the committed-weight byte count in the SUMMARY.
</output>

---
phase: 187-audit-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/187-audit-baseline/187-RUBRIC.md
  - .planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json
  - .planning/phases/187-audit-baseline/schemas/defect.schema.json
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "Maintainer can read the refreshed 12-dimension rubric and see why interaction-integrity and microcopy were added."
    - "Layer/z-index is documented as an overlay tag, not as a thirteenth rubric dimension."
    - "Structured baseline and defect rows have parseable schemas before harness code writes canonical artifacts."
  artifacts:
    - path: ".planning/phases/187-audit-baseline/187-RUBRIC.md"
      provides: "12-dimension rubric, scoring anchors, overlay tags, severity scale, and examples"
      contains: "11 interaction-integrity"
    - path: ".planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json"
      provides: "JSON Schema for canonical baseline.cells.json rows"
      contains: "coverage_status"
    - path: ".planning/phases/187-audit-baseline/schemas/defect.schema.json"
      provides: "JSON Schema for canonical defects.ndjson rows"
      contains: "owner_phase"
  key_links:
    - from: ".planning/phases/187-audit-baseline/187-RUBRIC.md"
      to: ".planning/phases/187-audit-baseline/187-CONTEXT.md"
      via: "D-01 through D-05 and D-24 through D-27"
      pattern: "interaction-integrity"
    - from: ".planning/phases/187-audit-baseline/schemas/defect.schema.json"
      to: ".planning/phases/187-audit-baseline/defects.ndjson"
      via: "required fields id,severity,surface,rubric_dimension,overlay_tags,owner_phase"
      pattern: "AX187"
---

<objective>
Create the Phase 187 audit contract: the refreshed rubric, the overlay-tag vocabulary, the severity/owner routing model, and machine-readable schemas for the two canonical structured artifacts.

Purpose: Later plans need fixed contracts before writing a manifest, Playwright probes, scorer output, and final baseline artifacts. This plan locks the only-forward shape for VER-01 without running the audit yet.

Output:
- `.planning/phases/187-audit-baseline/187-RUBRIC.md`
- `.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json`
- `.planning/phases/187-audit-baseline/schemas/defect.schema.json`
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/research/v1.51-admin-ui-depth-design.md
@/Users/jon/projects/accrue/.planning/milestones/v1.51-phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
@/Users/jon/projects/accrue/brandbook/voice.md
@/Users/jon/projects/accrue/brandbook/copy.md
@/Users/jon/projects/accrue/brandbook/tokens/README.md

<interfaces>
Rubric dimensions required by D-01 and D-02:
1 token-compliance, 2 visual-hierarchy, 3 spacing-rhythm, 4 state-coverage, 5 responsive-mobile-first, 6 contrast, 7 focus-semantics, 8 brand-expression, 9 motion, 10 reuse-dry, 11 interaction-integrity, 12 microcopy.

Overlay tags required by D-04:
layer-z-index, live-focus, focus-restore, focus-trap, scroll-reachability, overlay-position, actionability, disabled-affordance, hover-affordance, copy-recovery, copy-vocabulary, copy-specificity, dark-mode-role, reduced-motion.

State taxonomy required by D-08:
default-populated, empty, loading, error, permission-denied, disconnected-reconnecting, overflow, long-content, disabled-readonly, interactive-open.

Defect minimum fields required by D-19:
id, severity, surface, surface_type, persona_job, reproduction, expected, actual, rubric_dimension, overlay_tags, cell_id, evidence_refs, owner_phase, status, notes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the refreshed rubric contract</name>
  <files>.planning/phases/187-audit-baseline/187-RUBRIC.md</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RUBRIC.md (new file; if absent, inspect the phase directory listing before creation)
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-PATTERNS.md
@/Users/jon/projects/accrue/.planning/milestones/v1.51-phases/176-c-systematic-per-screen-rubric-uplift/176-SCORECARD.md
@/Users/jon/projects/accrue/brandbook/voice.md
@/Users/jon/projects/accrue/brandbook/copy.md
@/Users/jon/projects/accrue/brandbook/tokens/README.md
  </read_first>
  <action>Create `187-RUBRIC.md` with sections named `Rubric Dimensions`, `Overlay Tags`, `State Taxonomy`, `Severity Scale`, `Defect Ownership`, `Scoring Rules`, and `Examples`. Per D-01, preserve the first 10 dimensions exactly as continuity dimensions: token-compliance, visual-hierarchy, spacing-rhythm, state-coverage, responsive-mobile-first, contrast, focus-semantics, brand-expression, motion, reuse-dry. Per D-02, add exactly `11 interaction-integrity` and `12 microcopy`. Per D-03, document `layer/z-index` only under `Overlay Tags`, never as dimension 13. Per D-04, include the required overlay tags exactly: layer-z-index, live-focus, focus-restore, focus-trap, scroll-reachability, overlay-position, actionability, disabled-affordance, hover-affordance, copy-recovery, copy-vocabulary, copy-specificity, dark-mode-role, reduced-motion. Per D-08, include the exact state taxonomy. Per D-20, document owner_phase values `188`, `189`, `190`, and `191` and what each receives. Per D-24, state that `brandbook/` supersedes `prompts/accrue-brand-book.md` on conflicts. Per D-25, define microcopy scoring against measured, exact, native, durable voice. Per D-26, evaluate empty/error/permission states as operator UX with what happened, affected object/process, and next useful action. Per D-27, score visual/design findings against quiet, well-made developer tooling and `ax-*` tokens, not generic SaaS dashboard taste.</action>
  <verify>
    <automated>bash -lc 'test -f .planning/phases/187-audit-baseline/187-RUBRIC.md && grep -q "11 interaction-integrity" .planning/phases/187-audit-baseline/187-RUBRIC.md && grep -q "12 microcopy" .planning/phases/187-audit-baseline/187-RUBRIC.md && grep -q "layer-z-index" .planning/phases/187-audit-baseline/187-RUBRIC.md && ! grep -v "^#" .planning/phases/187-audit-baseline/187-RUBRIC.md | grep -q "13 .*layer"'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `187-RUBRIC.md` contains the exact strings `11 interaction-integrity`, `12 microcopy`, `layer-z-index`, `copy-recovery`, `dark-mode-role`, `default-populated`, `permission-denied`, and `disconnected-reconnecting`.
- Source assertion: non-heading content does not contain a `13` rubric dimension for layer/z-index.
- Source assertion: the file explicitly states structured data wins when markdown and `baseline.cells.json` / `defects.ndjson` disagree.
- Source assertion: the severity scale is rankable and includes `critical`, `high`, `medium`, and `low`.
  </acceptance_criteria>
  <done>The refreshed Phase 187 rubric is readable, carries D-01 through D-05 and D-24 through D-27, and can be used by later plans without reinterpreting the v1.51 dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: Create structured artifact schemas</name>
  <files>.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json, .planning/phases/187-audit-baseline/schemas/defect.schema.json</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json (new file; if absent, inspect `.planning/phases/187-audit-baseline/`)
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/schemas/defect.schema.json (new file; if absent, inspect `.planning/phases/187-audit-baseline/`)
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RUBRIC.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RESEARCH.md
  </read_first>
  <action>Create the `schemas/` directory and two JSON Schema draft-07 files. `baseline-cell.schema.json` must require `cell_id`, `surface`, `surface_type`, `mode`, `theme`, `state`, `dimension`, `dimension_name`, `score`, `coverage_status`, `evidence_refs`, and `notes`; constrain `mode` to `chromium-desktop` or `chromium-mobile`, `theme` to `light` or `dark`, `coverage_status` to `covered`, `gap`, or `n/a`, and `score` to integer `0`, `1`, `2`, `3`, or `null`. `defect.schema.json` must require the D-19 fields exactly and constrain `id` to pattern `^AX187-[0-9]{3}$`, `severity` to `critical`, `high`, `medium`, or `low`, `owner_phase` to `188`, `189`, `190`, or `191`, and `status` to `open`, `confirmed`, `gap`, or `n/a`. Use standard JSON syntax only; do not introduce a schema validation package.</action>
  <verify>
    <automated>node -e 'const fs=require("fs"); const base=JSON.parse(fs.readFileSync(".planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json","utf8")); const defect=JSON.parse(fs.readFileSync(".planning/phases/187-audit-baseline/schemas/defect.schema.json","utf8")); for (const key of ["cell_id","surface","coverage_status","dimension"]) if (!base.required.includes(key)) throw new Error("missing baseline required "+key); for (const key of ["id","severity","surface","rubric_dimension","overlay_tags","owner_phase"]) if (!defect.required.includes(key)) throw new Error("missing defect required "+key); if (!String(defect.properties.id.pattern).includes("AX187")) throw new Error("missing AX187 id pattern");'</automated>
  </verify>
  <acceptance_criteria>
- CLI assertion: both schema files parse with `JSON.parse`.
- Source assertion: `baseline-cell.schema.json` required fields include `cell_id`, `coverage_status`, `dimension`, `score`, and `evidence_refs`.
- Source assertion: `defect.schema.json` required fields include `id`, `severity`, `surface`, `rubric_dimension`, `overlay_tags`, `cell_id`, `evidence_refs`, `owner_phase`, and `status`.
- Source assertion: no `package.json` dependency is added for schema validation in this plan.
  </acceptance_criteria>
  <done>Both canonical structured artifact shapes are explicit, parseable, and aligned with D-17 through D-21.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning source -> committed rubric | Locked decisions become phase contract files that downstream executors trust. |
| Schema files -> generated artifacts | Later scripts use these field names to write baseline and defect data. |
| Human-readable markdown -> canonical JSON/NDJSON | Markdown summaries must not override structured data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-01-01 | Tampering | `187-RUBRIC.md` | mitigate | Encode D-01 through D-05 directly, include grep gates for dimensions 11 and 12, and block a layer/z-index dimension 13. |
| T-187-01-02 | Tampering | `schemas/*.schema.json` | mitigate | Parse schemas with Node JSON APIs and require D-19 fields plus enum constraints before downstream scripts consume them. |
| T-187-01-03 | Repudiation | Rubric/defect ownership | mitigate | Document owner_phase values `188`, `189`, `190`, `191` so every later defect can be routed without relitigating ownership. |
| T-187-01-04 | Information disclosure | Rubric examples | accept | Examples must use generic surface names and generated evidence paths only; no screenshots, secrets, or trace payloads are stored in this plan. |
| T-187-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists in this plan; if execution proposes one, stop for package legitimacy review. |
</threat_model>

<verification>
Run the task-level commands plus:
- `node -e 'JSON.parse(require("fs").readFileSync(".planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json","utf8")); JSON.parse(require("fs").readFileSync(".planning/phases/187-audit-baseline/schemas/defect.schema.json","utf8")); console.log("schemas parse")'`
- `grep -n "interaction-integrity\\|microcopy\\|layer-z-index" .planning/phases/187-audit-baseline/187-RUBRIC.md`
</verification>

<success_criteria>
Plan 01 is complete when the rubric and schemas exist, are parseable, carry VER-01's scoring contract, and do not implement any deferred tooling such as PhoenixStorybook, SARIF, or pixel-diff services.
</success_criteria>

<output>
Create `.planning/phases/187-audit-baseline/187-01-SUMMARY.md` when done.
</output>

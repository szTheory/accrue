---
phase: 187-audit-baseline
plan: "04"
type: execute
wave: 3
depends_on:
  - 187-02
files_modified:
  - accrue_admin/e2e/admin-interactions.spec.js
  - accrue_admin/test/support/e2e_auth_adapter.ex
  - accrue_admin/test/support/e2e_plug.ex
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "Live interaction probes open every required modal, drawer, dropdown, overlay, scroll, focus, disabled, permission, disconnected, error, loading, and empty-state class that is reachable now."
    - "Interaction evidence uses Playwright traces as primary evidence and does not encode broken behavior as permanent corrected-behavior tests."
    - "Permission-denied forcing remains test-only and cannot alter production admin auth."
  artifacts:
    - path: "accrue_admin/e2e/admin-interactions.spec.js"
      provides: "Trace-backed live interaction baseline probes"
      contains: "trace: \"on\""
    - path: "accrue_admin/test/support/e2e_auth_adapter.ex"
      provides: "Test-only non-admin user branch for permission-denied reachability"
      contains: "e2e_member"
    - path: "accrue_admin/test/support/e2e_plug.ex"
      provides: "Test-only login-member helper route"
      contains: "login-member"
  key_links:
    - from: "accrue_admin/e2e/admin-interactions.spec.js"
      to: "accrue_admin/test-results/admin-interactions"
      via: "trace and observation output"
      pattern: "admin-interactions"
    - from: "accrue_admin/e2e/admin-interactions.spec.js"
      to: "accrue_admin/e2e/baseline-manifest.js"
      via: "cell_id/overlay tag mapping"
      pattern: "OVERLAY_TAGS"
---

<objective>
Add trace-backed live interaction probes and minimal test-only state forcing for permission-denied coverage.

Purpose: VER-01 must include the defects screenshots miss: modal/scrim layering, focus trap/restore, overlay position, scroll reachability, actionability, disabled affordances, keyboard-only flows, LiveView patch focus, permission-denied, disconnected/reconnecting, loading, error, and empty states.

Output:
- `accrue_admin/e2e/admin-interactions.spec.js`
- updated `accrue_admin/test/support/e2e_auth_adapter.ex`
- updated `accrue_admin/test/support/e2e_plug.ex`
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-UI-SPEC.md
@/Users/jon/projects/accrue/accrue_admin/e2e/baseline-manifest.js
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-motion-trace.spec.js
@/Users/jon/projects/accrue/accrue_admin/test/support/e2e_auth_adapter.ex
@/Users/jon/projects/accrue/accrue_admin/test/support/e2e_plug.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/detail_drawer.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/global_search.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/dropdown_menu.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/flash_group.ex

<interfaces>
Existing motion trace pattern:
`test.use({ trace: "on" })` is file-scoped and must not modify `playwright.config.js`.

Required live probes from D-13:
modal/drawer/scrim layering, dropdown/popover/toast position, scroll reachability, focus trap, focus restore, Escape/click-outside dismiss, keyboard-only completion, focus after LiveView patches, disabled/read-only affordances, hover/focus weirdness on non-interactive surfaces, disconnected/reconnecting behavior, permission-denied, error, loading, and empty states.

Stable contracts from D-14:
dialog/drawer above scrim and clickable, Escape closes, focus returns to trigger, Tab/Shift+Tab stay inside modal, dropdown/menu trigger exposes open state, keyboard activation works for menu items, scroll-bottom content is reachable, disabled controls are not actionable, and Playwright actionability does not report intercepted clicks for visible controls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test-only permission-denied login support</name>
  <files>accrue_admin/test/support/e2e_auth_adapter.ex, accrue_admin/test/support/e2e_plug.ex</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/test/support/e2e_auth_adapter.ex
@/Users/jon/projects/accrue/accrue_admin/test/support/e2e_plug.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/auth_hook.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/owner_scope.ex
@/Users/jon/projects/accrue/accrue/lib/accrue/auth.ex
  </read_first>
  <action>Update only test support. In `e2e_auth_adapter.ex`, add a branch before the fallback: `current_user(%{"admin_token" => "member"})` returns `%{id: "e2e_member", role: :member}`. In `e2e_plug.ex`, add `get "/login-member"` and `get "/__e2e__/login-member"` routes that mirror existing `/login` behavior but put session key `:admin_token` to `"member"` and redirect to the `to` query param or `/billing`. Do not change `AccrueAdmin.AuthHook`, `AccrueAdmin.OwnerScope`, production router code, or `Accrue.Auth.admin?/1`. This creates a permission-denied reachability probe because `role: :member` is not an admin under existing auth heuristics.</action>
  <verify>
    <automated>bash -lc 'grep -q "e2e_member" accrue_admin/test/support/e2e_auth_adapter.ex && grep -q "login-member" accrue_admin/test/support/e2e_plug.ex && cd accrue_admin && mix test --warnings-as-errors test/accrue_admin'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `e2e_auth_adapter.ex` contains `e2e_member` and `role: :member`.
- Source assertion: `e2e_plug.ex` contains `login-member` and `put_session(:admin_token, "member")`.
- Source assertion: no production files under `accrue_admin/lib/accrue_admin/` are modified by this task.
- CLI assertion: `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin` exits 0.
  </acceptance_criteria>
  <done>Permission-denied state can be reached from Playwright through E2E-only session setup without changing production access control.</done>
</task>

<task type="auto">
  <name>Task 2: Implement live interaction probes</name>
  <files>accrue_admin/e2e/admin-interactions.spec.js</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-interactions.spec.js (new file; if absent, inspect `accrue_admin/e2e/`)
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-motion-trace.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/baseline-manifest.js
@/Users/jon/projects/accrue/accrue_admin/test/support/e2e_plug.ex
@/Users/jon/projects/accrue/accrue_admin/test/support/e2e_auth_adapter.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/detail_drawer.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/global_search.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/dropdown_menu.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/flash_group.ex
  </read_first>
  <action>Create `admin-interactions.spec.js` with `test.use({ trace: "on" })`. Reuse reset/seed/login helpers and add `loginMember(page, target)` that navigates to `/__e2e__/login-member?to=${encodeURIComponent(target)}`. Per D-12, use a ledger-first probe design: record exploratory live-interaction findings now and automate stable baseline contracts only. Write raw interaction observations under `test-results/admin-interactions/{project}/observations.ndjson`, one JSON object per probe with fields `probe_id`, `interaction_class`, `cell_id`, `surface`, `surface_type`, `state`, `rubric_dimension`, `overlay_tags`, `coverage_status`, `target_selector`, `expected`, `actual`, `assertions`, `evidence_refs`, `failure_kind`, and `notes`. For modal, drawer, and scrim coverage, open `.ax-detail-drawer-shell`, `.ax-detail-drawer-backdrop`, `.ax-detail-drawer`, `[data-role="replay-single"]`, `[data-role="replay-confirm"]`, `[data-role="confirm-panel"]`, and `.ax-command-palette-wrapper`; assert overlay order by checking the top element at the target point, verify the visible overlay receives clicks without `{ force: true }`, record `layer-z-index` / `overlay-position` when a scrim or panel intercepts actionability, press Escape, click outside the active surface, and assert dismissed state or record a defect observation. Explicitly probe `StepUpAuthModal` through `#accrue-admin-step-up-dialog` with interaction_class `step-up-auth-modal`: drive the existing charge refund destructive-action path using `[data-role="refund-form"]`, `[data-role="confirm-panel"]`, and `[data-role="confirm-refund"]`, then drive representative invoice and subscription destructive-action paths when seeded controls are reachable; if an invoice or subscription destructive path is not reachable from current fixtures, write a `gap` observation naming that missing path instead of omitting the modal surface. For every step-up probe, record layering, top-element/actionability, focus trap, focus restore, Escape dismissal, click-outside dismissal, keyboard submit with code `123456`, keyboard cancel through the `step_up_dismiss` control, invalid-code error copy from `[data-role="step-up-error"]`, and trace-backed `evidence_refs`. For full dropdown/popover/toast coverage, probe `#search-trigger`, `.ax-tab-more-trigger`, `.ax-tab-more-menu`, `details.ax-dropdown > summary`, `.ax-dropdown-panel`, and flash/toast containers; assert open-state exposure through `aria-expanded`, `[open]`, or equivalent DOM state; verify keyboard activation of menu items; record position, clipping, click interception, and toast overlap evidence. For scroll reachability, probe page scroll, `.ax-detail-drawer` body scroll, modal/panel scroll, dropdown panel scroll, table overflow regions, long-content rows, and the active main content container; scroll each container to bottom, assert the last actionable or sentinel element is reachable and visible, and mark `gap` or `n/a` with a reason when the surface has no scroll container. For focus behavior, assert Tab and Shift+Tab remain inside open modal/drawer/command-palette/step-up surfaces, assert focus restores to the trigger after Escape and click-outside dismissal, and trigger LiveView patches through search/filter/menu selection/replay confirmation/step-up submit/cancel so focus after patch is recorded with `live-focus` and `focus-restore` overlay tags. For keyboard-only primary flows, complete command-palette search/result activation, dropdown menu item activation, replay-confirm open/confirm/cancel, drawer open/close, step-up submit/cancel, and primary empty/error recovery action paths using keyboard events only; record `actionability` and `focus-semantics` defects when a primary flow cannot complete. For hover/focus edge cases, hover and focus representative non-interactive cards, rows, status chips, disabled controls, and read-only controls; assert non-interactive surfaces are not tabbable/clickable, disabled controls are not actionable, and affordance mismatches carry `hover-affordance` or `disabled-affordance`. For state coverage, record loading, error, empty, permission-denied, disabled-readonly, disconnected-reconnecting, overflow, long-content, and interactive-open observations; reach permission-denied through `loginMember`, disconnected/reconnecting through Playwright offline/network interruption after an admin page is loaded, and loading/error/empty through existing manifest seeds or explicit `gap` observations. Per D-15, treat Playwright traces as primary evidence for focus, scroll, animation, overlay, and LiveView patch behavior. Per D-16, do not encode current broken behavior as permanent corrected-behavior regression tests; permanent regression tests belong to Phase 191 when defects are fixed. Do not use `{ force: true }`; intercepted clicks, timeouts, missing selectors, and unstable state setup must become observations unless the observation writer, Playwright runtime, or JSON/NDJSON parser itself fails.</action>
  <verify>
    <automated>bash -lc 'node -e '"'"'const fs=require("fs"); const src=fs.readFileSync("accrue_admin/e2e/admin-interactions.spec.js","utf8"); for (const token of ["trace: \\"on\\"","scroll-reachability","focus-trap","focus-restore","Escape","click-outside","keyboard-only","live-focus","hover-affordance","loading","error","empty","permission-denied","disconnected-reconnecting",".ax-detail-drawer",".ax-dropdown-panel",".ax-command-palette-wrapper","#accrue-admin-step-up-dialog","step-up-auth-modal","step-up-error","step_up_submit","step_up_dismiss","confirm-refund"]) if (!src.includes(token)) throw new Error("missing interaction probe token "+token); if (src.includes("{ force: true }")) throw new Error("force clicks are forbidden");'"'"' && cd accrue_admin && npm run e2e -- e2e/admin-interactions.spec.js --project=chromium-desktop -x && cd .. && node -e '"'"'const fs=require("fs"); const p="accrue_admin/test-results/admin-interactions/chromium-desktop/observations.ndjson"; const lines=fs.readFileSync(p,"utf8").trim().split("\\n").filter(Boolean).map(JSON.parse); const classes=new Set(lines.map((l)=>l.interaction_class)); for (const c of ["modal-drawer-scrim","step-up-auth-modal","dropdown-popover-toast","scroll-reachability","focus-trap-restore","keyboard-only-primary-flow","liveview-patch-focus","hover-focus-affordance","loading-error-empty-state"]) if (!classes.has(c)) throw new Error("missing observation class "+c);'"'"''</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `admin-interactions.spec.js` contains `test.use({ trace: "on" })`.
- Source assertion: `admin-interactions.spec.js` contains probes for scroll reachability across page, main content, drawer body, modal/panel, dropdown panel, table overflow, and long-content containers.
- Source assertion: `admin-interactions.spec.js` contains focus trap, focus restore, Escape dismissal, click-outside dismissal, keyboard-only primary-flow completion, and focus-after-LiveView-patch probes.
- Source assertion: `admin-interactions.spec.js` explicitly probes `StepUpAuthModal` through `#accrue-admin-step-up-dialog` from a charge refund destructive-action flow and either invoice/subscription destructive-action flows or gap observations that name why those flows are unreachable from existing fixtures.
- Source assertion: step-up observations record layering, focus trap/restore, Escape dismissal, click-outside dismissal, keyboard submit, keyboard cancel, invalid-code error copy from `[data-role="step-up-error"]`, and trace-backed evidence refs.
- Source assertion: `admin-interactions.spec.js` contains `#search-trigger`, `.ax-command-palette-wrapper`, `.ax-tab-more-trigger`, `.ax-tab-more-menu`, `details.ax-dropdown > summary`, `.ax-dropdown-panel`, `.ax-detail-drawer-shell`, `.ax-detail-drawer-backdrop`, `.ax-detail-drawer`, `[data-role="replay-single"]`, `[data-role="replay-confirm"]`, `[data-role="confirm-panel"]`, flash/toast coverage, `[data-role="empty-state"]`, `login-member`, and `setOffline` or equivalent browser network interruption.
- Source assertion: `admin-interactions.spec.js` records loading, error, empty, permission-denied, disconnected-reconnecting, disabled-readonly, overflow, long-content, and interactive-open state observations.
- Source assertion: `admin-interactions.spec.js` writes observation fields `probe_id`, `interaction_class`, `cell_id`, `surface`, `surface_type`, `state`, `rubric_dimension`, `overlay_tags`, `coverage_status`, `target_selector`, `expected`, `actual`, `assertions`, `evidence_refs`, `failure_kind`, and `notes`.
- Source assertion: `admin-interactions.spec.js` does not contain `{ force: true }`.
- Behavior assertion: the chromium-desktop run writes `test-results/admin-interactions/chromium-desktop/observations.ndjson` with valid JSON lines.
 - Behavior assertion: observations include classes for modal/drawer/scrim, step-up-auth-modal, dropdown/popover/toast, scroll reachability, focus trap/restore, Escape/click-outside dismissal, keyboard-only primary flows, LiveView patch focus, hover/focus affordance, loading/error/empty states, permission-denied, and disconnected/reconnecting coverage.
  </acceptance_criteria>
  <done>The live interaction baseline is trace-backed, records stable observations, and captures required interaction classes without asserting future fixed behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E login routes -> admin session | Test-only session helpers can simulate admin and non-admin users. |
| Browser actions -> LiveView state | Playwright probes trigger DOM patches, focus movement, overlays, and network state. |
| Trace/screenshots -> defect ledger | Generated evidence can contain sensitive values if run against non-test data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-04-01 | Elevation of Privilege | `e2e_plug.ex` login-member route | mitigate | Keep routes inside test support only; do not touch production router/auth; verify production files are unchanged. |
| T-187-04-02 | Spoofing | `e2e_auth_adapter.ex` member branch | mitigate | Use explicit token `"member"` and user id `e2e_member`; this branch exists only in E2E auth adapter. |
| T-187-04-03 | Information Disclosure | Playwright traces | mitigate | Use deterministic test fixtures and write traces under `accrue_admin/test-results`; commit only manifest references/checksums later. |
| T-187-04-04 | Denial of Service | live interaction probes | mitigate | Use stable baseline contracts, serial Playwright config, and observation rows for gaps instead of retry loops that hide defects. |
| T-187-04-05 | Tampering | interaction observations | mitigate | Each NDJSON line carries manifest `cell_id`, `overlay_tags`, expected, actual, and evidence_refs so Phase 187 artifacts can route defects. |
| T-187-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned; rely on existing Playwright and Phoenix test support. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin`
- `cd accrue_admin && npm run e2e -- e2e/admin-interactions.spec.js --project=chromium-desktop -x`
- `node -e 'const fs=require("fs"); const p="accrue_admin/test-results/admin-interactions/chromium-desktop/observations.ndjson"; if (fs.existsSync(p)) for (const l of fs.readFileSync(p,"utf8").trim().split("\\n").filter(Boolean)) JSON.parse(l);'`
</verification>

<success_criteria>
Plan 04 is complete when test-only permission-denied forcing exists, live interaction probes produce trace-backed observation rows, and no production auth or route file is changed.
</success_criteria>

<output>
Create `.planning/phases/187-audit-baseline/187-04-SUMMARY.md` when done.
</output>

---
phase: 187-audit-baseline
plan: "05"
type: execute
wave: 4
depends_on:
  - 187-03
  - 187-04
files_modified:
  - .planning/phases/187-audit-baseline/187-BASELINE.md
  - .planning/phases/187-audit-baseline/baseline.cells.json
  - .planning/phases/187-audit-baseline/defects.ndjson
  - .planning/phases/187-audit-baseline/artifacts.manifest.json
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "A severity-ranked defect ledger exists with surface, reproduction, severity, dimension, overlay tags, evidence, owner phase, and status."
    - "A scored baseline captures every manifest cell as covered, gap, or n/a across audited surface, mode, theme, state, and dimension."
    - "Structured artifacts are canonical for Phase 192 comparison and markdown is a generated/readable summary."
    - "No generated PNG or trace zip is committed by default."
  artifacts:
    - path: ".planning/phases/187-audit-baseline/187-BASELINE.md"
      provides: "Maintainer-readable baseline summary, severity-ranked ledger, score summaries, and downstream handoff"
      contains: "Only-forward baseline"
    - path: ".planning/phases/187-audit-baseline/baseline.cells.json"
      provides: "Canonical scored cell data for Phase 192 comparison"
      contains: "coverage_status"
    - path: ".planning/phases/187-audit-baseline/defects.ndjson"
      provides: "Canonical defect ledger with AX187 stable IDs"
      contains: "AX187-"
    - path: ".planning/phases/187-audit-baseline/artifacts.manifest.json"
      provides: "Generated evidence references and checksums"
      contains: "sha256"
  key_links:
    - from: ".planning/phases/187-audit-baseline/defects.ndjson"
      to: ".planning/phases/187-audit-baseline/baseline.cells.json"
      via: "cell_id"
      pattern: "cell_id"
    - from: ".planning/phases/187-audit-baseline/artifacts.manifest.json"
      to: "accrue_admin/test-results"
      via: "evidence_refs paths and checksums"
      pattern: "test-results"
---

<objective>
Run the Phase 187 audit pipeline and commit the canonical only-forward baseline artifacts.

Purpose: VER-01 is only complete when the rubric, static matrix evidence, live interaction evidence, severity-ranked defect ledger, and scored baseline exist together and can be parsed for Phase 188-192 consumption.

Output:
- `.planning/phases/187-audit-baseline/187-BASELINE.md`
- `.planning/phases/187-audit-baseline/baseline.cells.json`
- `.planning/phases/187-audit-baseline/defects.ndjson`
- `.planning/phases/187-audit-baseline/artifacts.manifest.json`
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RUBRIC.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/schemas/defect.schema.json
@/Users/jon/projects/accrue/accrue_admin/e2e/baseline-manifest.js
@/Users/jon/projects/accrue/accrue_admin/e2e/baseline-artifacts.mjs
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-baseline.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-interactions.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/score-visuals.mjs
@/Users/jon/projects/accrue/accrue_admin/package.json

<interfaces>
Canonical committed artifacts from D-18:
`187-RUBRIC.md`, `187-BASELINE.md`, `baseline.cells.json`, `defects.ndjson`, `artifacts.manifest.json`, `schemas/baseline-cell.schema.json`, `schemas/defect.schema.json`.

Canonical structured-data rule from D-21:
`baseline.cells.json` and `defects.ndjson` win over `187-BASELINE.md` if they disagree.

Generated evidence rule from D-22:
Do not commit PNGs or trace zips by default; reference them through `artifacts.manifest.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the static and interaction audit evidence</name>
  <files>.planning/phases/187-audit-baseline/artifacts.manifest.json</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/artifacts.manifest.json (new file; if absent, inspect `.planning/phases/187-audit-baseline/`)
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-baseline.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-interactions.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/baseline-artifacts.mjs
@/Users/jon/projects/accrue/accrue_admin/e2e/score-visuals.mjs
@/Users/jon/projects/accrue/accrue_admin/package.json
  </read_first>
  <action>Run the Phase 187 evidence commands in `accrue_admin`: `npm run e2e -- e2e/admin-baseline.spec.js`, `npm run e2e -- e2e/admin-interactions.spec.js`, and `npm run e2e:a11y`. If `ANTHROPIC_API_KEY` is present, run `npm run score-visuals`; if it is absent, run `npm run score-visuals` once to record the clean skip behavior and let `baseline-artifacts.mjs` record vision scoring as unavailable/gap rather than failing the phase. Then run `npm run baseline:artifacts`. Per D-22, keep screenshots, traces, axe raw output, and vision findings under `accrue_admin/test-results`; commit only `artifacts.manifest.json` with paths, sha256 checksums, producer command, timestamp, project, theme, surface, and cell_id metadata. Per D-23, do not create SARIF or GitHub code-scanning export for Phase 187.</action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && npm run e2e -- e2e/admin-baseline.spec.js && npm run e2e -- e2e/admin-interactions.spec.js && npm run e2e:a11y && npm run score-visuals && npm run baseline:artifacts && cd .. && test -f .planning/phases/187-audit-baseline/artifacts.manifest.json && node -e "JSON.parse(require(\"fs\").readFileSync(\".planning/phases/187-audit-baseline/artifacts.manifest.json\",\"utf8\"));"'</automated>
  </verify>
  <acceptance_criteria>
- CLI assertion: `npm run e2e -- e2e/admin-baseline.spec.js` completes and writes `accrue_admin/test-results/admin-baseline`.
- CLI assertion: `npm run e2e -- e2e/admin-interactions.spec.js` completes and writes `accrue_admin/test-results/admin-interactions`.
- CLI assertion: `npm run e2e:a11y` completes or any failure is represented in generated defect rows with severity and evidence references.
- Source assertion: `artifacts.manifest.json` parses as JSON and contains evidence path entries under `accrue_admin/test-results`.
- Git assertion: `git status --short` does not show staged `.png` or `.zip` files from generated evidence.
  </acceptance_criteria>
  <done>All generated evidence exists under test-results and is referenced by a committed artifact manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize canonical baseline and defect ledger</name>
  <files>.planning/phases/187-audit-baseline/187-BASELINE.md, .planning/phases/187-audit-baseline/baseline.cells.json, .planning/phases/187-audit-baseline/defects.ndjson</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-BASELINE.md (new file; if absent, inspect `.planning/phases/187-audit-baseline/`)
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/baseline.cells.json (new file; if absent, inspect `.planning/phases/187-audit-baseline/`)
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/defects.ndjson (new file; if absent, inspect `.planning/phases/187-audit-baseline/`)
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/artifacts.manifest.json
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/187-RUBRIC.md
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json
@/Users/jon/projects/accrue/.planning/phases/187-audit-baseline/schemas/defect.schema.json
@/Users/jon/projects/accrue/accrue_admin/e2e/baseline-artifacts.mjs
  </read_first>
  <action>Use `baseline-artifacts.mjs` outputs and direct trace/screenshot review where needed to finalize `baseline.cells.json`, `defects.ndjson`, and `187-BASELINE.md`. Every manifest cell must appear in `baseline.cells.json` with `coverage_status` `covered`, `gap`, or `n/a`. Every defect line must use a stable id `AX187-001`, `AX187-002`, and so on; include D-19 fields; assign `rubric_dimension` from the 12-dimension rubric; attach overlay tags from D-04 when applicable; assign `owner_phase` to `188`, `189`, `190`, or `191`; and set `status` to `open`, `confirmed`, `gap`, or `n/a`. Sort the markdown ledger by severity order `critical`, `high`, `medium`, `low`, then by owner phase and surface. `187-BASELINE.md` must state that `baseline.cells.json` and `defects.ndjson` are canonical, summarize coverage by `surface_type`, `mode`, `theme`, `state`, and owner phase, and list exact rerun commands for Phase 192 comparison.</action>
  <verify>
    <automated>bash -lc 'node -e '"'"'const fs=require("fs"); const cells=JSON.parse(fs.readFileSync(".planning/phases/187-audit-baseline/baseline.cells.json","utf8")); if (!Array.isArray(cells) || cells.length===0) throw new Error("baseline cells empty"); for (const c of cells) if (!["covered","gap","n/a"].includes(c.coverage_status)) throw new Error("bad coverage "+c.coverage_status); const defects=fs.readFileSync(".planning/phases/187-audit-baseline/defects.ndjson","utf8").trim().split("\n").filter(Boolean).map(JSON.parse); for (const d of defects) { if (!/^AX187-[0-9]{3}$/.test(d.id)) throw new Error("bad id "+d.id); if (!["188","189","190","191"].includes(String(d.owner_phase))) throw new Error("bad owner "+d.owner_phase); } console.log(`cells=${cells.length} defects=${defects.length}`);'"'"' && grep -q "Only-forward baseline" .planning/phases/187-audit-baseline/187-BASELINE.md && grep -q "baseline.cells.json and defects.ndjson are canonical" .planning/phases/187-audit-baseline/187-BASELINE.md'</automated>
  </verify>
  <acceptance_criteria>
- CLI assertion: `baseline.cells.json` parses as a non-empty JSON array.
- CLI assertion: every non-empty line of `defects.ndjson` parses as JSON and every id matches `AX187-[0-9]{3}`.
- Source assertion: `187-BASELINE.md` contains `Only-forward baseline` and `baseline.cells.json and defects.ndjson are canonical`.
- Source assertion: every defect row has `surface`, `reproduction`, `severity`, `rubric_dimension`, `overlay_tags`, `evidence_refs`, and `owner_phase`.
- Source assertion: every `gap` and `n/a` cell has a non-empty `notes` or reason field.
  </acceptance_criteria>
  <done>The Phase 187 only-forward baseline and severity-ranked defect ledger are complete, parseable, and ready for Phases 188-192.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated test-results -> committed artifacts | Evidence files are generated and should be referenced, not committed. |
| Optional vision scorer -> canonical baseline | Vision output may be absent or malformed and cannot become authoritative metadata. |
| Defect ledger -> downstream remediation plans | Owner phase routing drives later implementation scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-05-01 | Information Disclosure | screenshots/traces | mitigate | Run against deterministic E2E fixtures only; commit paths/checksums in `artifacts.manifest.json`, not binary evidence. |
| T-187-05-02 | Tampering | `baseline.cells.json` | mitigate | Parse JSON, require non-empty array, enforce coverage_status enum, and treat structured data as canonical. |
| T-187-05-03 | Tampering | `defects.ndjson` | mitigate | Parse every line, enforce AX187 ids and owner_phase enum, and sort severity in markdown from structured data. |
| T-187-05-04 | Repudiation | defect ownership | mitigate | Every defect includes reproduction, expected, actual, evidence_refs, owner_phase, and status. |
| T-187-05-05 | Denial of Service | optional vision scoring | mitigate | Absence of `ANTHROPIC_API_KEY` records a gap/unavailable evidence note; it does not block baseline artifact creation. |
| T-187-SC | Tampering | npm/pip/cargo installs | mitigate | No new package install is planned; use locked existing dependencies and generated-output conventions. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && npm run e2e -- e2e/admin-baseline.spec.js`
- `cd accrue_admin && npm run e2e -- e2e/admin-interactions.spec.js`
- `cd accrue_admin && npm run e2e:a11y`
- `cd accrue_admin && npm run score-visuals`
- `cd accrue_admin && npm run baseline:artifacts && npm run baseline:parse`
- `git status --short | grep -E "\\.(png|zip)$" && exit 1 || true`
</verification>

<success_criteria>
Plan 05 is complete when the severity-ranked defect ledger, scored baseline, and artifact manifest are committed as the single only-forward reference point for Phases 188-192, with no bulky generated evidence committed.
</success_criteria>

<output>
Create `.planning/phases/187-audit-baseline/187-05-SUMMARY.md` when done.
</output>

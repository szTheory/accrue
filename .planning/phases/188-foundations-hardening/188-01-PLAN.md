---
phase: 188-foundations-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/assets/tailwind.config.js
  - accrue_admin/assets/tailwind_preset.js
  - accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
  - accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs
  - accrue_admin/guides/admin_ui.md
autonomous: true
requirements:
  - FND-04
must_haves:
  truths:
    - "D-12: `accrue_admin/assets/tailwind.config.js` and `accrue_admin/assets/tailwind_preset.js` are deleted."
    - "D-13: `mix accrue_admin.assets.build` still uses `tailwindcss@3.4.17` but no longer passes `--config`."
    - "D-14: AccrueAdmin docs state that package styles are authored only in `assets/css/theme.css` and `assets/css/app.css` using `--ax-*` tokens and `ax-*` classes."
    - "D-16: Phase 188 does not replace the Tailwind CLI with a different CSS bundler."
  artifacts:
    - path: "accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex"
      provides: "Package-local asset build task without Tailwind config input"
      contains: "tailwindcss@3.4.17"
    - path: "accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs"
      provides: "Regression coverage for asset build arguments"
      contains: "refute Enum.member?(tailwind_args, \"--config\")"
    - path: "accrue_admin/guides/admin_ui.md"
      provides: "Documented styling source of truth"
      contains: "assets/css/theme.css"
  key_links:
    - from: "accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex"
      to: "accrue_admin/assets/css/app.css"
      via: "Tailwind CLI --input argument"
      pattern: "--input"
    - from: "accrue_admin/guides/admin_ui.md"
      to: "accrue_admin/assets/css/theme.css"
      via: "Authoring contract"
      pattern: "theme.css"
---

<objective>
Resolve the inert Tailwind source-of-truth ambiguity while preserving the current package-local asset build.

Purpose: FND-04 requires one styling contract. This plan removes the stale Tailwind config/preset authoring surface, keeps Tailwind 3 only as the CSS compiler/minifier, and updates maintainer docs before enforcement guards are added in Plan 06.

Output:
- Deleted `accrue_admin/assets/tailwind.config.js`
- Deleted `accrue_admin/assets/tailwind_preset.js`
- Updated asset build task and test
- Updated `accrue_admin/guides/admin_ui.md`
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
@/Users/jon/projects/accrue/accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs
@/Users/jon/projects/accrue/accrue_admin/guides/admin_ui.md

<interfaces>
Asset build task contract:
- Module: `Mix.Tasks.AccrueAdmin.Assets.Build`
- Runner callback: `run(String.t(), [String.t()], keyword()) :: {:ok, integer()} | {:error, term()}`
- Tailwind tool version remains `tailwindcss@3.4.17`
- Esbuild tool version remains `esbuild@0.25.3`
- CSS input remains `assets/css/app.css`
- CSS output remains `priv/static/accrue_admin.css`

Locked decisions implemented here:
- D-12 delete `assets/tailwind.config.js` and `assets/tailwind_preset.js`
- D-13 remove `--config` while keeping Tailwind 3 as compiler/minifier
- D-14 document `theme.css` and `app.css` as the authoring contract
- D-16 do not replace the CSS compiler/minifier in Phase 188
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove Tailwind config from the build contract</name>
  <files>accrue_admin/assets/tailwind.config.js, accrue_admin/assets/tailwind_preset.js, accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex, accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/assets/tailwind.config.js
@/Users/jon/projects/accrue/accrue_admin/assets/tailwind_preset.js
@/Users/jon/projects/accrue/accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
@/Users/jon/projects/accrue/accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: The fake runner receives Tailwind args containing `--input`, `assets/css/app.css`, `--output`, `priv/static/accrue_admin.css`, and `--minify`.
    - Test 2: The fake runner Tailwind args do not contain `--config` and do not contain `assets/tailwind.config.js`.
    - Test 3: `accrue_admin/assets/tailwind.config.js` and `accrue_admin/assets/tailwind_preset.js` do not exist after the task.
  </behavior>
  <action>Update `accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs` first so the build test asserts `refute Enum.member?(tailwind_args, "--config")` and `refute Enum.any?(tailwind_args, &String.contains?(&1, "tailwind.config.js"))`. Then update `tailwind_args/1` in `accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex` to keep exactly these Tailwind argument roles: `"--yes"`, `@tailwind_version`, `"--input"`, `Path.join(root, "assets/css/app.css")`, `"--output"`, `Path.join(root, "priv/static/accrue_admin.css")`, and `"--minify"`. Remove the `"--config"` argument and the `Path.join(root, "assets/tailwind.config.js")` argument. Delete `accrue_admin/assets/tailwind.config.js` and `accrue_admin/assets/tailwind_preset.js` per D-12. Do not change `@tailwind_version`, `@esbuild_version`, JS bundling, output paths, or committed bundle restoration behavior.</action>
  <verify>
    <automated>bash -lc 'test ! -e accrue_admin/assets/tailwind.config.js && test ! -e accrue_admin/assets/tailwind_preset.js && cd accrue_admin && mix test --warnings-as-errors test/mix/tasks/accrue_admin_assets_build_test.exs'</automated>
  </verify>
  <acceptance_criteria>
- CLI assertion: `test ! -e accrue_admin/assets/tailwind.config.js` exits 0.
- CLI assertion: `test ! -e accrue_admin/assets/tailwind_preset.js` exits 0.
- Source assertion: `accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex` contains `@tailwind_version "tailwindcss@3.4.17"`.
- Source assertion: `accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex` does not contain `"--config"` or `assets/tailwind.config.js`.
- Test assertion: `cd accrue_admin && mix test --warnings-as-errors test/mix/tasks/accrue_admin_assets_build_test.exs` exits 0.
  </acceptance_criteria>
  <done>The package asset build no longer has a Tailwind config dependency and the deleted config/preset files are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the admin UI styling source-of-truth docs</name>
  <files>accrue_admin/guides/admin_ui.md</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/guides/admin_ui.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
@/Users/jon/projects/accrue/brandbook/voice.md
  </read_first>
  <action>Update `accrue_admin/guides/admin_ui.md` so the `UI stack and polish direction` section says Tailwind CSS v3 compiles `assets/css/app.css` into `priv/static/accrue_admin.css` via `mix accrue_admin.assets.build` with no package Tailwind config or preset. Replace the authoring guidance with the exact contract: AccrueAdmin styles are authored in `assets/css/theme.css` and `assets/css/app.css` using `--ax-*` tokens and `ax-*` classes; Tailwind utilities are not an authoring path; host apps never configure Tailwind for `accrue_admin`. Keep the private asset bundle section and preserve the statement that no host Tailwind config edits or host JavaScript bootstrap changes are required. Use measured, exact, native, durable wording from the brand voice; do not add marketing copy or a new build tool recommendation.</action>
  <verify>
    <automated>bash -lc 'grep -q "assets/css/theme.css" accrue_admin/guides/admin_ui.md && grep -q "assets/css/app.css" accrue_admin/guides/admin_ui.md && grep -q "Tailwind utilities are not an authoring path" accrue_admin/guides/admin_ui.md && grep -q "host apps never configure Tailwind for .accrue_admin" accrue_admin/guides/admin_ui.md && ! grep -Eq "tailwind_preset|tailwind.config.js|Add Tailwind utilities|Prefer .*Tailwind utilities" accrue_admin/guides/admin_ui.md'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `accrue_admin/guides/admin_ui.md` contains `assets/css/theme.css`, `assets/css/app.css`, `--ax-*`, and `ax-*`.
- Source assertion: `accrue_admin/guides/admin_ui.md` contains `Tailwind utilities are not an authoring path`.
- Source assertion: `accrue_admin/guides/admin_ui.md` contains `host apps never configure Tailwind for ` followed by `accrue_admin`.
- Source assertion: `accrue_admin/guides/admin_ui.md` does not contain `tailwind_preset`, `tailwind.config.js`, or guidance to add Tailwind utilities in HEEx.
  </acceptance_criteria>
  <done>The docs expose one unambiguous AccrueAdmin styling source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package asset task -> generated static bundle | Build arguments control what CSS compiler reads and writes. |
| Package docs -> host app maintainers | Documentation can incorrectly tell host apps to configure package styling. |
| Deleted config files -> future contributors | Removed files must not silently reappear as a second styling source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-01-01 | Tampering | `tailwind.config.js` / `tailwind_preset.js` | mitigate | Delete both files and add Plan 06 verifier guards that fail if either file exists. |
| T-188-01-02 | Denial of Service | `accrue_admin.assets.build` | mitigate | Preserve Tailwind 3.4.17, input/output paths, esbuild args, and fake-runner tests while removing only `--config`. |
| T-188-01-03 | Repudiation | `admin_ui.md` | mitigate | Document the exact authoring contract and remove references to config/preset utility authoring. |
| T-188-01-04 | Information Disclosure | Asset build logs | accept | This plan changes local file paths only and does not introduce new secrets, user data, or external calls beyond the existing `npx --yes` build tool invocation. |
| T-188-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager install is planned; Tailwind remains the existing `npx --yes tailwindcss@3.4.17` invocation. |
</threat_model>

<verification>
Run:
- `test ! -e accrue_admin/assets/tailwind.config.js`
- `test ! -e accrue_admin/assets/tailwind_preset.js`
- `cd accrue_admin && mix test --warnings-as-errors test/mix/tasks/accrue_admin_assets_build_test.exs`
- `bash -lc 'grep -q "Tailwind utilities are not an authoring path" accrue_admin/guides/admin_ui.md'`
</verification>

<success_criteria>
Plan 01 is complete when FND-04 is implemented without changing the CSS compiler/minifier: Tailwind config/preset files are gone, `--config` is absent from the build task, the build test passes, and docs name `theme.css` plus `app.css` as the only package styling authoring path.
</success_criteria>

<output>
Create `.planning/phases/188-foundations-hardening/188-01-SUMMARY.md` when done.
</output>

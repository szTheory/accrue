---
phase: 188-foundations-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/assets/css/theme.css
  - accrue_admin/assets/css/app.css
autonomous: true
requirements:
  - FND-01
  - FND-03
must_haves:
  truths:
    - "D-01 through D-04: composed typography bundles exist and semantic admin classes consume role tokens instead of rebuilding type properties."
    - "D-02: Typography roles use the two-token shape `--ax-type-{role}-font` plus `--ax-type-{role}-tracking`."
    - "D-05: every raw typography declaration in admin CSS is scanned before Plan 06, and any non-allowlisted `font-size`, `font-weight`, `line-height`, `letter-spacing`, or `font-family` declaration is migrated to composed `ax-*` type roles or documented with an `ax-type-exception:` comment."
    - "D-06: `--ax-measure` applies to prose/help/error/empty/description regions and narrative cells without globally capping raw data grids."
  artifacts:
    - path: "accrue_admin/assets/css/theme.css"
      provides: "Composed typography role tokens"
      contains: "--ax-type-body-font"
    - path: "accrue_admin/assets/css/app.css"
      provides: "Typography role utilities and measure consumers"
      contains: ".ax-type-body"
  key_links:
    - from: "accrue_admin/assets/css/app.css"
      to: "accrue_admin/assets/css/theme.css"
      via: "font: var(--ax-type-{role}-font)"
      pattern: "font: var\\(--ax-type-.*-font\\)"
    - from: "accrue_admin/assets/css/app.css"
      to: "--ax-measure"
      via: "max-width for prose/help/empty/description selectors"
      pattern: "max-width: var\\(--ax-measure\\)"
---

<objective>
Introduce the composed typography and reading-measure foundation that downstream primitives and pages inherit.

Purpose: FND-01 and FND-03 need root tokens and semantic consumers, not per-page font and width patches. This plan keeps the atomic type tokens, adds role bundles, migrates shared classes, and applies readable measure to actual prose-like surfaces.

Output:
- `theme.css` composed type role tokens
- `app.css` `.ax-type-*` utilities
- `app.css` global non-allowlisted raw type migration and measure application
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css

<interfaces>
Required type roles from D-03:
`body`, `body-sm`, `body-relaxed`, `label`, `label-sm`, `eyebrow`, `title`, `title-lg`, `heading`, `display`, `metric`, `code`, `code-xs`.

Two-token role shape from D-02:
- `--ax-type-{role}-font`
- `--ax-type-{role}-tracking`

Concrete role values for this plan:
- body: `400 var(--ax-type-md)/var(--ax-leading-normal) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- body-sm: `400 var(--ax-type-sm)/var(--ax-leading-normal) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- body-relaxed: `400 var(--ax-type-md)/var(--ax-leading-relaxed) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- label: `600 var(--ax-type-sm)/var(--ax-leading-normal) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- label-sm: `600 var(--ax-type-xs)/var(--ax-leading-normal) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- eyebrow: `600 var(--ax-type-xs)/var(--ax-leading-normal) var(--ax-font-sans)` + `var(--ax-tracking-caps)`
- title: `600 var(--ax-type-lg)/var(--ax-leading-tight) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- title-lg: `600 var(--ax-type-xl)/var(--ax-leading-tight) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- heading: `600 var(--ax-type-2xl)/var(--ax-leading-tight) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- display: `600 var(--ax-type-3xl)/var(--ax-leading-tight) var(--ax-font-sans)` + `var(--ax-tracking-normal)`
- metric: `600 var(--ax-type-2xl)/var(--ax-leading-tight) var(--ax-font-sans)` + `var(--ax-tracking-normal)` plus tabular numerals in the utility
- code: `400 var(--ax-type-sm)/var(--ax-leading-normal) var(--ax-font-mono)` + `var(--ax-tracking-normal)`
- code-xs: `400 var(--ax-type-xs)/var(--ax-leading-normal) var(--ax-font-mono)` + `var(--ax-tracking-normal)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type role tokens and migrate all non-allowlisted raw type declarations</name>
  <files>accrue_admin/assets/css/theme.css, accrue_admin/assets/css/app.css</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
  </read_first>
  <action>In `theme.css`, keep the existing atomic `--ax-font-*`, `--ax-type-*`, `--ax-leading-*`, `--ax-tracking-*`, and `--ax-measure` tokens. Add a composed type role block under `html.accrue-admin` with the exact role token names and values listed in this plan's `<interfaces>` block. Use only weight `400` or `600` in these role bundles. In `app.css`, add `.ax-type-{role}` utilities for every D-03 role. Each utility must set `font: var(--ax-type-{role}-font)` and `letter-spacing: var(--ax-type-{role}-tracking)`; `.ax-type-metric` must also set `font-variant-numeric: tabular-nums`; code roles must use the mono role tokens through the shorthand. Per D-05, do not stop at the named selector examples: scan every admin CSS file under `accrue_admin/assets/css/`, including `app.css`, for `font-size`, `font-weight`, `line-height`, `letter-spacing`, and `font-family`. Classify each raw declaration as one of the documented allowlist zones (`@font-face`, root body inheritance, icon/SVG mechanics) or as a migration target. Migrate every non-allowlisted declaration to a composed type role by applying an `.ax-type-{role}` utility in markup when practical, grouping selectors with an existing role utility, or replacing property-by-property declarations with `font: var(--ax-type-{role}-font)` plus `letter-spacing: var(--ax-type-{role}-tracking)`. Cover existing shared semantic selectors such as `.ax-label`, `.ax-eyebrow`, `.ax-sidebar-link-label`, `.ax-theme-button`, `.ax-icon-label`, `.ax-body`, `.ax-heading`, `.ax-display`, `.ax-kpi-value`, `.ax-field-help`, `.ax-field-error`, and existing code/token selectors, plus any other non-allowlisted selector found by the scan. If a raw declaration must remain outside the allowlist, add a local comment beginning exactly `/* ax-type-exception:` next to the declaration with the reason it cannot use a role; do not use broad file-header exceptions. The final source state must be ready for Plan 06 to turn the raw-type scan into enforced CI without requiring another typography migration. Do not introduce numeric role names like `.ax-type-md` as the maintainer-facing contract.</action>
  <verify>
    <automated>bash -lc 'for role in body body-sm body-relaxed label label-sm eyebrow title title-lg heading display metric code code-xs; do grep -Eq -- "^[[:space:]]*--ax-type-${role}-font:" accrue_admin/assets/css/theme.css && grep -Eq -- "^[[:space:]]*--ax-type-${role}-tracking:" accrue_admin/assets/css/theme.css && grep -q -- ".ax-type-${role}" accrue_admin/assets/css/app.css || exit 1; done; ! grep -Eq -- "^[[:space:]]*--ax-type-(4|8|12|16|xs|sm|md|lg)-font:" accrue_admin/assets/css/theme.css && perl -0777 -ne '"'"'while (/([^{}]+)\{([^{}]*)\}/g) { my ($selector, $body) = ($1, $2); while ($body =~ /(^|\n)([^\n]*(font-size|font-weight|line-height|letter-spacing|font-family)\s*:[^;\n]+;?[^\n]*)/g) { my ($line, $prop) = ($2, $3); next if $selector =~ /\@font-face/; next if $selector =~ /\b(html\.accrue-admin\s+body|html\.accrue-admin|body)\b/ && $prop =~ /font-family|line-height/; next if $selector =~ /(icon|svg|lucide|glyph)/i; next if $line =~ /ax-type-exception:/ || $body =~ /ax-type-exception:/; print "$ARGV:$prop in $selector\n"; $bad = 1; } } END { exit($bad ? 1 : 0) }'"'"' accrue_admin/assets/css/*.css'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `theme.css` defines `--ax-type-{role}-font:` and `--ax-type-{role}-tracking:` for all 13 roles: body, body-sm, body-relaxed, label, label-sm, eyebrow, title, title-lg, heading, display, metric, code, code-xs.
- Source assertion: `app.css` defines `.ax-type-{role}` for all 13 roles.
- Source assertion: every `.ax-type-{role}` utility sets `font: var(--ax-type-{role}-font)` and `letter-spacing: var(--ax-type-{role}-tracking)`.
- Source assertion: `.ax-type-metric` contains `font-variant-numeric: tabular-nums`.
- Source assertion: no public composed token named `--ax-type-md-font`, `--ax-type-sm-font`, or another numeric/t-shirt role is added.
- Source assertion: every admin CSS file under `accrue_admin/assets/css/` was scanned for `font-size`, `font-weight`, `line-height`, `letter-spacing`, and `font-family`.
- Source assertion: every non-allowlisted raw typography declaration in admin component/page CSS has been migrated to composed `ax-*` type roles, or has a local `ax-type-exception:` comment with a concrete reason.
- Source assertion: remaining raw type declarations are limited to `@font-face`, root body inheritance, icon/SVG mechanics, or documented `ax-type-exception:` lines.
- Pre-Plan-06 guard assertion: the automated raw-type scan in this task exits 0 before Plan 06 implements the permanent verifier guard.
  </acceptance_criteria>
  <done>Typography bundles exist as semantic `ax-*` roles and all non-allowlisted raw type declarations in admin CSS have been migrated or explicitly documented before enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Apply readable measure to prose and dense narrative surfaces</name>
  <files>accrue_admin/assets/css/app.css</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
  </read_first>
  <action>Keep `.ax-measure { max-width: var(--ax-measure); }` and apply the reading measure to real prose-like selectors. Ensure these selectors, where present or added as reusable utilities, set `max-width: var(--ax-measure)` and preserve wrapping with `overflow-wrap: anywhere` when long copy is possible: `.ax-body`, `.ax-page-description`, `.ax-card-description`, `.ax-empty-state-body`, `.ax-field-help`, `.ax-field-error`, `.ax-description`, `.ax-banner-body`, `.ax-callout-body`, `.ax-prose`, and `.ax-cell-narrative`. For dense data surfaces, only cap explicit narrative/help/copy cells such as `.ax-cell-narrative`; do not set `max-width: var(--ax-measure)` on `.ax-data-table`, `.ax-data-table-shell`, `.ax-data-table table`, generic `table`, generic `td`, or generic `th`. Preserve horizontal overflow guards for raw tables and machine data grids.</action>
  <verify>
    <automated>bash -lc 'grep -q ".ax-measure" accrue_admin/assets/css/app.css && grep -q "max-width: var(--ax-measure)" accrue_admin/assets/css/app.css && ! grep -Eq "(^|[,{[:space:]])(table|td|th|\\.ax-data-table|\\.ax-data-table-shell)[^{]*\\{[^}]*max-width:[[:space:]]*var\\(--ax-measure\\)" accrue_admin/assets/css/app.css'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `app.css` contains `.ax-measure` with `max-width: var(--ax-measure)`.
- Source assertion: at least six prose/help/error/empty/description selectors in `app.css` consume `max-width: var(--ax-measure)`.
- Source assertion: `app.css` does not apply `max-width: var(--ax-measure)` to generic `table`, `td`, `th`, `.ax-data-table`, `.ax-data-table-shell`, or `.ax-data-table table` selectors.
- Source assertion: narrative dense-surface support exists through `.ax-cell-narrative` or an equivalent explicit narrative-cell class, not through a blanket table cap.
  </acceptance_criteria>
  <done>The reading-measure token is applied to prose-like surfaces and avoids hiding raw data-grid information.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token layer -> authored CSS | `theme.css` role tokens are consumed by `app.css` shared classes. |
| Shared classes -> downstream components | Component/page markup inherits typography and measure from root selectors. |
| Dense data -> readable measure | Applying measure must not hide raw machine data or table columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-02-01 | Tampering | `theme.css` type roles | mitigate | Define all D-03 role names with the D-02 two-token shape and assert them with grep. |
| T-188-02-02 | Denial of Service | dense tables | mitigate | Forbid blanket `--ax-measure` caps on generic table/data-grid selectors. |
| T-188-02-03 | Repudiation | type exceptions | mitigate | Require inline comments beginning `ax-type-exception:` for any remaining raw type property outside allowlist zones before Plan 06 enforces the guard. |
| T-188-02-04 | Spoofing | typography hierarchy | mitigate | Use semantic role names and 400/600 weights only so downstream selectors do not imply unsupported hierarchy levels. |
| T-188-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; all work is CSS source editing. |
</threat_model>

<verification>
Run:
- `bash -lc 'for role in body body-sm body-relaxed label label-sm eyebrow title title-lg heading display metric code code-xs; do grep -Eq -- "^[[:space:]]*--ax-type-${role}-font:" accrue_admin/assets/css/theme.css && grep -q -- ".ax-type-${role}" accrue_admin/assets/css/app.css || exit 1; done'`
- `bash -lc 'perl -0777 -ne '"'"'while (/([^{}]+)\{([^{}]*)\}/g) { my ($selector, $body) = ($1, $2); while ($body =~ /(^|\n)([^\n]*(font-size|font-weight|line-height|letter-spacing|font-family)\s*:[^;\n]+;?[^\n]*)/g) { my ($line, $prop) = ($2, $3); next if $selector =~ /\@font-face/; next if $selector =~ /\b(html\.accrue-admin\s+body|html\.accrue-admin|body)\b/ && $prop =~ /font-family|line-height/; next if $selector =~ /(icon|svg|lucide|glyph)/i; next if $line =~ /ax-type-exception:/ || $body =~ /ax-type-exception:/; print "$ARGV:$prop in $selector\n"; $bad = 1; } } END { exit($bad ? 1 : 0) }'"'"' accrue_admin/assets/css/*.css'`
- `bash -lc '! grep -Eq "(table|td|th|\\.ax-data-table)[^{]*\\{[^}]*max-width:[[:space:]]*var\\(--ax-measure\\)" accrue_admin/assets/css/app.css'`
</verification>

<success_criteria>
Plan 02 is complete when FND-01 and FND-03 have source-level implementation: composed type roles exist, every non-allowlisted raw typography declaration in admin CSS is migrated to role-based typography or documented with `ax-type-exception:`, the pre-Plan-06 raw-type scan exits 0, and readable measure applies to prose/dense narrative surfaces without blanket table caps.
</success_criteria>

<output>
Create `.planning/phases/188-foundations-hardening/188-02-SUMMARY.md` when done.
</output>

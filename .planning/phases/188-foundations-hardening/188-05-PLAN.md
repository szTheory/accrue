---
phase: 188-foundations-hardening
plan: "05"
type: execute
wave: 4
depends_on:
  - 188-01
  - 188-04
files_modified:
  - accrue_admin/lib/accrue_admin/dev/component_registry.ex
  - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
  - accrue_admin/test/accrue_admin/dev/component_registry_test.exs
  - accrue_admin/e2e/foundation-tokens.spec.js
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05
  - FND-06
must_haves:
  truths:
    - "The component kitchen exposes maintainer-facing foundation specimens for typography, measure, layers, focus, disabled/readonly, interactive hover/active/selected states, scrollbars, and status roles."
    - "Playwright computed-style checks prove foundation tokens resolve and are consumed in browser-rendered light and dark surfaces."
    - "Browser-rendered FND-05 specimens calculate contrast ratios for focus, disabled, readonly, scrollbar, status, and interactive role pairs in both light and dark themes."
    - "Existing component registry tests continue to prove token definitions and rendered specimen availability."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      provides: "Registry entries for foundation specimens and token metadata"
      contains: "foundation"
    - path: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      provides: "Rendered foundation specimen surface at `/billing/dev/components`"
      contains: "data-ax-foundation-specimen"
    - path: "accrue_admin/e2e/foundation-tokens.spec.js"
      provides: "Computed-style browser checks for Phase 188 foundations"
      contains: "foundation tokens"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      to: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      via: "ComponentRegistry.variants_for/1"
      pattern: "variants_for"
    - from: "accrue_admin/e2e/foundation-tokens.spec.js"
      to: "/billing/dev/components"
      via: "Playwright login and computed styles"
      pattern: "getComputedStyle"
---

<objective>
Expose and verify the foundation contract in the component kitchen.

Purpose: The root fixes need a maintainer-facing proof surface before later component phases consume them. This plan extends the existing `/billing/dev/components` kitchen and adds browser computed-style checks instead of relying on screenshot-only judgment.

Output:
- Foundation registry entries
- Foundation kitchen specimens
- Registry tests for rendered/token coverage
- Playwright computed-style spec
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/scripts/ci/verify_foundation_contrast.mjs
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/dev/component_registry.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
@/Users/jon/projects/accrue/accrue_admin/test/accrue_admin/dev/component_registry_test.exs
@/Users/jon/projects/accrue/accrue_admin/e2e/kitchen-banner.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/reduced-motion.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-a11y.spec.js

<interfaces>
Component registry entry shape:
`%{family: String.t(), variant: String.t(), ax_class: String.t(), tokens: [String.t()]}`

Required foundation specimen families:
- `foundation-type`
- `foundation-measure`
- `foundation-layer`
- `foundation-focus`
- `foundation-disabled-readonly`
- `foundation-interactive`
- `foundation-scrollbar`
- `foundation-status`

Required kitchen data attributes:
- `data-ax-foundation-specimen="type-body"`
- `data-ax-foundation-specimen="type-display"`
- `data-ax-foundation-specimen="measure-prose"`
- `data-ax-foundation-layer="sticky|dropdown|popover|drawer|modal|toast"`
- `data-ax-foundation-specimen="focus-control"`
- `data-ax-foundation-specimen="disabled-control"`
- `data-ax-foundation-specimen="readonly-control"`
- `data-ax-foundation-specimen="interactive-hover"`
- `data-ax-foundation-specimen="interactive-active"`
- `data-ax-foundation-specimen="interactive-selected"`
- `data-ax-foundation-specimen="scrollbar"`
- `data-ax-foundation-status="success|warning|danger|info|neutral"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add foundation specimens to registry and kitchen</name>
  <files>accrue_admin/lib/accrue_admin/dev/component_registry.ex, accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex, accrue_admin/test/accrue_admin/dev/component_registry_test.exs</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/dev/component_registry.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
@/Users/jon/projects/accrue/accrue_admin/test/accrue_admin/dev/component_registry_test.exs
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Every new foundation registry `ax_class` variant appears in rendered `/billing/dev/components` HTML.
    - Test 2: Every token listed by new foundation registry entries has a `--token:` definition in `theme.css` or `app.css`, except existing layout-injected tokens already allowlisted by the test.
  </behavior>
  <action>Add registry entries for the eight foundation families named in this plan's `<interfaces>` block. Each entry must keep the existing `%{family, variant, ax_class, tokens}` shape and list concrete tokens, including representative type role tokens, layer tokens, focus tokens, disabled/readonly tokens, interactive hover/active/selected tokens, scrollbar tokens, status tokens, and `--ax-measure`. Extend `component_kitchen_live.ex` with a `Foundation Tokens` section that renders after the existing component variant sections and before or near the motion reference. Use the UI-SPEC copy: heading text `Review foundations`, empty/body copy only where a specimen picker is present, and otherwise measured labels such as `Typography roles`, `Reading measure`, `Layer stack`, `Focus`, `Disabled and readonly`, `Interactive states`, `Scrollbars`, and `Status roles`. Add the required `data-ax-foundation-*` attributes exactly as listed in `<interfaces>`. Render light and dark samples for status, focus, disabled/readonly, interactive hover/active/selected, and type specimens. For interactive specimens, use real CSS-consuming elements or state classes that exercise the same selectors migrated in Plan 04, not inline styles that merely display token names. Keep the surface maintainer-facing and dense; do not create a landing page, marketing hero, PhoenixStorybook integration, or new route.</action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/dev/component_registry_test.exs && cd .. && grep -q "data-ax-foundation-specimen=\\\"type-body\\\"" accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex && grep -q "data-ax-foundation-specimen=\\\"interactive-hover\\\"" accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex && grep -q "data-ax-foundation-specimen=\\\"interactive-active\\\"" accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex && grep -q "data-ax-foundation-specimen=\\\"interactive-selected\\\"" accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex && grep -q "foundation-interactive" accrue_admin/lib/accrue_admin/dev/component_registry.ex && grep -q "foundation-status" accrue_admin/lib/accrue_admin/dev/component_registry.ex'</automated>
  </verify>
  <acceptance_criteria>
- Test assertion: `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/dev/component_registry_test.exs` exits 0.
- Source assertion: `component_registry.ex` contains entries for `foundation-type`, `foundation-measure`, `foundation-layer`, `foundation-focus`, `foundation-disabled-readonly`, `foundation-interactive`, `foundation-scrollbar`, and `foundation-status`.
- Source assertion: `component_kitchen_live.ex` contains all required `data-ax-foundation-*` attributes from this plan's `<interfaces>` block.
- Source assertion: the kitchen section references `--ax-type-body-font`, `--ax-measure`, `--ax-z-modal`, `--ax-focus-ring`, `--ax-disabled-bg`, `--ax-interactive-hover`, `--ax-interactive-active`, `--ax-interactive-selected`, `--ax-scrollbar-thumb`, and `--ax-status-success-bg` through registry token metadata or rendered token evidence.
  </acceptance_criteria>
  <done>The component kitchen displays the Phase 188 foundation contract and the registry treats those specimens as testable truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright computed-style checks for foundations</name>
  <files>accrue_admin/e2e/foundation-tokens.spec.js</files>
  <read_first>
@/Users/jon/projects/accrue/accrue_admin/e2e/kitchen-banner.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/reduced-motion.spec.js
@/Users/jon/projects/accrue/accrue_admin/e2e/admin-a11y.spec.js
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
@/Users/jon/projects/accrue/accrue_admin/playwright.config.js
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/scripts/ci/verify_foundation_contrast.mjs
  </read_first>
  <action>Create `accrue_admin/e2e/foundation-tokens.spec.js` using the existing login/reset helper style from `kitchen-banner.spec.js` and `admin-a11y.spec.js`. The spec must visit `/billing/dev/components`, emulate reduced motion with `page.emulateMedia({ reducedMotion: "reduce" })`, wait for `#main-content`, and run computed-style checks in both `light` and `dark` themes by setting `document.documentElement.dataset.theme` or `data-theme`. Define local no-dependency helpers such as `parseColor`, `relativeLuminance`, `contrastRatio`, and `expectContrastAtLeast`; they must parse computed `rgb()`/`rgba()` colors and calculate WCAG contrast ratios in the browser spec rather than relying on screenshot judgment. Check these concrete expectations: body and display specimens have different computed `fontSize` values and non-empty `letterSpacing`; the measure specimen's computed `maxWidth` is not `none` and the root `--ax-measure` token is non-empty; layer specimens compute z-index values `100`, `200`, `300`, `400`, `500`, and `600`; the focus specimen receives focus and has computed `outlineWidth` equal to `2px` with outline style not `none`, and its computed `outlineColor` contrasts at least 3:1 against the adjacent background/offset surface in both themes; disabled specimens have computed text/background contrast at least 3:1 and resolve disabled cursor/opacity semantics; readonly specimens have computed text/background contrast at least 4.5:1; interactive hover, active, and selected specimens have computed foreground/background contrast at least 4.5:1 and compare their state backgrounds to the resolved `--ax-interactive-hover`, `--ax-interactive-active`, and `--ax-interactive-selected` root tokens in both themes; selected/current state-only indicators, if represented without text, must contrast at least 3:1 against their adjacent surface; status specimens must check every `success`, `warning`, `danger`, `info`, and `neutral` bg/text pair at 4.5:1 and every solid/on-solid pair at 4.5:1 in both themes; root `--ax-scrollbar-thumb`, `--ax-scrollbar-track`, and `--ax-scrollbar-thumb-hover` tokens are non-empty, and where the browser exposes `scrollbarColor` or an opaque track/thumb pair, thumb/track contrast is at least 3:1. Also run the fast source contrast verifier from Plan 04 before Playwright so low-contrast token values fail without browser startup. Do not add screenshot assertions, a new browser harness, or the full reduced-motion/a11y/kitchen suite here; that full browser gate remains Plan 07 only.</action>
  <verify>
    <automated>bash -lc 'test -f accrue_admin/e2e/foundation-tokens.spec.js && node --check accrue_admin/e2e/foundation-tokens.spec.js && grep -q "contrastRatio" accrue_admin/e2e/foundation-tokens.spec.js && grep -q "expectContrastAtLeast" accrue_admin/e2e/foundation-tokens.spec.js && grep -q -- "--ax-status-success-on-solid" accrue_admin/e2e/foundation-tokens.spec.js && node scripts/ci/verify_foundation_contrast.mjs && cd accrue_admin && npm run e2e -- e2e/foundation-tokens.spec.js'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `foundation-tokens.spec.js` imports or defines the same `login` helper pattern used by existing admin e2e specs.
- Source assertion: `foundation-tokens.spec.js` visits `/billing/dev/components`.
- Source assertion: `foundation-tokens.spec.js` calls `window.getComputedStyle`.
- Fast-source assertion: `node --check accrue_admin/e2e/foundation-tokens.spec.js` exits 0 before Playwright runs.
- Fast-source assertion: `node scripts/ci/verify_foundation_contrast.mjs` exits 0 before Playwright runs.
- Source assertion: `foundation-tokens.spec.js` contains contrast helpers named `contrastRatio` and `expectContrastAtLeast`.
- Source assertion: the spec checks z-index values `100`, `200`, `300`, `400`, `500`, and `600`.
- Source assertion: the spec reads `--ax-interactive-hover`, `--ax-interactive-active`, and `--ax-interactive-selected` with `getPropertyValue` and compares them to computed styles from rendered kitchen specimens rather than only checking that the tokens are declared.
- Source assertion: the spec checks focus ring/background contrast, disabled text/background contrast, readonly text/background contrast, scrollbar thumb/track contrast where exposed, all status bg/text and solid/on-solid pairs, and interactive hover/active/selected foreground/background or state/background pairs in both light and dark.
- CLI assertion: `cd accrue_admin && npm run e2e -- e2e/foundation-tokens.spec.js` exits 0.
  </acceptance_criteria>
  <done>Browser-rendered foundation specimens have computed-style regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev-only kitchen -> maintainer verification | The kitchen is a non-production proof surface but drives human and browser validation. |
| Registry metadata -> token definitions | Registry token lists can drift from actual CSS definitions. |
| Browser computed styles -> phase acceptance | Playwright checks prove runtime CSS resolution across themes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-05-01 | Repudiation | `ComponentRegistry` | mitigate | Existing token-definition and render-coverage tests include new foundation entries. |
| T-188-05-02 | Information Disclosure | `/billing/dev/components` | accept | The route already exists behind the Fake-processor dev gate; specimens contain token names and sample copy only. |
| T-188-05-03 | Tampering | computed style checks | mitigate | Tests inspect actual browser `getComputedStyle` values after theme toggles. |
| T-188-05-04 | Denial of Service | e2e runtime | mitigate | Reuse existing Playwright config and `/__e2e__/login`; do not add a new harness or dependency. |
| T-188-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; use existing Playwright dependencies. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/dev/component_registry_test.exs`
- `node scripts/ci/verify_foundation_contrast.mjs`
- `node --check accrue_admin/e2e/foundation-tokens.spec.js`
- `cd accrue_admin && npm run e2e -- e2e/foundation-tokens.spec.js`
</verification>

<success_criteria>
Plan 05 is complete when FND-01 through FND-06 have a rendered proof surface and targeted computed-style browser coverage, including contrast-ratio proof for FND-05 semantic roles, without adding a new component documentation dependency or moving the full browser suite out of Plan 07.
</success_criteria>

<output>
Create `.planning/phases/188-foundations-hardening/188-05-SUMMARY.md` when done.
</output>

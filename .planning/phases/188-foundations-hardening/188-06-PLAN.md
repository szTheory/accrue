---
phase: 188-foundations-hardening
plan: "06"
type: execute
wave: 5
depends_on:
  - 188-01
  - 188-02
  - 188-03
  - 188-04
  - 188-05
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05
  - FND-06
must_haves:
  truths:
    - "D-15: Static guards enforce Tailwind SSOT, typography role usage, layer token usage, semantic role presence, and motion policy."
    - "Every new verifier rule has a negative-fixture test that proves the guard fails on drift."
    - "Existing package-doc verifier success output remains intact."
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Static guardrails for Phase 188 foundations"
      contains: "FND-"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Negative fixture coverage for new guardrails"
      contains: "ROOT_DIR"
  key_links:
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "System.cmd with ROOT_DIR temp fixtures"
      pattern: "ROOT_DIR"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue_admin/assets/css/theme.css"
      via: "required semantic token checks"
      pattern: "--ax-status-"
---

<objective>
Make Phase 188 foundations enforceable with static verifier guards and negative fixtures.

Purpose: The root fixes should not depend on maintainer memory. This plan extends the existing verifier pattern so Tailwind SSOT, type roles, layers, semantic roles, and motion policy fail fast when drift is introduced.

Output:
- Extended `scripts/ci/verify_package_docs.sh`
- Negative fixture tests in `package_docs_verifier_test.exs`
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
@/Users/jon/projects/accrue/accrue/test/accrue/docs/package_docs_verifier_test.exs
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/accrue_admin/guides/admin_ui.md
@/Users/jon/projects/accrue/accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex

<interfaces>
Verifier helper pattern:
- `fail()`
- `require_fixed(file, needle)`
- `require_absent_regex(file, pattern)`
- `ROOT_DIR` temp fixture override

Required new guard categories from `188-VALIDATION.md`:
- Tailwind config/preset files stay absent
- Tailwind CLI invocation omits `--config`
- Package CSS contains no `@tailwind` or `@apply`
- Package docs do not recommend Tailwind utilities or host Tailwind config
- HEEx does not grow obvious Tailwind utility authoring outside intentional `ax-*` package classes
- Overlay/sticky z-index values consume layer tokens instead of literals
- Raw type properties are absent outside documented allowlist zones
- Required semantic role tokens exist in root, dark, and system-dark scopes
- Existing motion literal and easing guards continue to run
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add negative fixture coverage for new verifier rules</name>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
@/Users/jon/projects/accrue/accrue/test/accrue/docs/package_docs_verifier_test.exs
@/Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: A temp fixture with `accrue_admin/assets/tailwind.config.js` fails verifier output mentioning `tailwind.config.js`.
    - Test 2: A temp fixture with `--config` in the asset build task fails verifier output mentioning `--config`.
    - Test 3: A temp fixture with `@tailwind` or `@apply` in `app.css` fails verifier output mentioning `@tailwind` or `@apply`.
    - Test 4: A temp fixture with Tailwind utility authoring guidance in `admin_ui.md` fails verifier output mentioning Tailwind authoring.
    - Test 5: A temp fixture with `.ax-drift { z-index: 999; }` fails verifier output mentioning z-index literals.
    - Test 6: A temp fixture with `.ax-drift { font-size: 13px; }` fails verifier output mentioning raw type declarations.
    - Test 7: A temp fixture missing `--ax-focus-ring-offset:` from `theme.css` fails verifier output mentioning semantic role tokens.
  </behavior>
  <action>Update `seed_tmp_dir!/1` so it copies `accrue_admin/assets/css/app.css`, `accrue_admin/assets/css/theme.css`, `accrue_admin/guides/admin_ui.md`, `accrue_admin/guides/motion.md`, `accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex`, and at least one representative HEEx/LiveView source file used by the utility-authoring guard. Add negative fixture tests matching the behavior block. Each test must create a temp `ROOT_DIR`, call `seed_tmp_dir!`, mutate exactly the file needed for that guard, run `System.cmd("bash", [@script_path], stderr_to_stdout: true, env: [{"ROOT_DIR", tmp_dir}])`, assert `status != 0`, assert output contains `[verify_package_docs]`, and assert output contains the expected guard-specific text. Preserve the existing verifier success test and existing negative tests.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs'</automated>
  </verify>
  <acceptance_criteria>
- Test assertion: `package_docs_verifier_test.exs` includes negative tests for Tailwind file presence, `--config`, `@tailwind`/`@apply`, Tailwind authoring docs, z-index literals, raw type declarations, and missing semantic roles.
- Source assertion: every new negative test passes `env: [{"ROOT_DIR", tmp_dir}]`.
- Source assertion: `seed_tmp_dir!/1` copies `theme.css`, `app.css`, `admin_ui.md`, `motion.md`, and `accrue_admin.assets.build.ex`.
- CLI assertion: `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs` exits 0 after Task 2 implements the guards.
  </acceptance_criteria>
  <done>Verifier drift rules have behavior-level negative tests before they are trusted.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase 188 static verifier guards</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <read_first>
@/Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
@/Users/jon/projects/accrue/accrue/test/accrue/docs/package_docs_verifier_test.exs
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/accrue_admin/guides/admin_ui.md
@/Users/jon/projects/accrue/accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
  </read_first>
  <action>Per D-15, extend `scripts/ci/verify_package_docs.sh` near the existing CSS and motion guards with a clearly labeled `Phase 188 foundation guards (FND-01..FND-06)` block. Add deterministic checks for the categories in this plan's `<interfaces>` block. For Tailwind SSOT, fail if `$ROOT_DIR/accrue_admin/assets/tailwind.config.js` or `$ROOT_DIR/accrue_admin/assets/tailwind_preset.js` exists, if `accrue_admin.assets.build.ex` contains `--config`, if package CSS contains `@tailwind` or `@apply`, or if `admin_ui.md` contains `Tailwind utilities`, `tailwind_preset`, `tailwind.config.js`, or host Tailwind configuration guidance. For HEEx utility authoring, scan `accrue_admin/lib` for obvious utility class prefixes such as `mt-`, `mb-`, `mx-`, `my-`, `p-`, `px-`, `py-`, `flex`, `grid`, `hidden`, `block`, `rounded`, `shadow`, `text-`, and `bg-` inside class attributes, then filter out strings containing `ax-`, Phoenix-generated assigns, and documented examples; if the guard would flag current intentional non-utility classes, narrow the regex rather than weakening the rule. For z-index, fail on `z-index:` numeric literals other than `-1`, `0`, or `1`; require any allowed micro-stacking literal to appear near `isolation: isolate` and `ax-z-micro-stack`. For raw type declarations, fail on `font-size`, `font-weight`, `line-height`, `letter-spacing`, and `font-family` outside `@font-face`, root body inheritance, icon/SVG mechanics, and lines/comments marked `ax-type-exception:`. For semantic roles, require every D-18 token and each status pair token to appear in root, explicit dark, and system-dark scopes. Keep existing motion guards for `transition: all`, raw `cubic-bezier()`, raw `ms`/`s`, and layout-property transitions, and add no new motion token family exception.</action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_package_docs.sh && cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs'</automated>
  </verify>
  <acceptance_criteria>
- CLI assertion: `bash scripts/ci/verify_package_docs.sh` exits 0 on the real repo.
- CLI assertion: `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs` exits 0.
- Source assertion: `verify_package_docs.sh` contains `Phase 188 foundation guards (FND-01..FND-06)`.
- Source assertion: `verify_package_docs.sh` references `tailwind.config.js`, `tailwind_preset.js`, `--config`, `@tailwind`, `@apply`, `z-index`, `font-size`, `--ax-focus-ring-offset`, `--ax-scrollbar-thumb`, `--ax-disabled-bg`, `--ax-readonly-bg`, and `--ax-status-success-bg`.
- Source assertion: the z-index guard does not use an unfiltered bare `grep -c` equality gate; comments cannot satisfy or invalidate the count.
  </acceptance_criteria>
  <done>Static verification catches regressions for every Phase 188 foundation requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree -> shell verifier | Static grep/awk rules decide whether styling drift is accepted. |
| Temp fixtures -> verifier behavior | Negative tests mutate copied files to prove failures are meaningful. |
| Comments -> grep gates | Guards must not be satisfied or invalidated by header prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-06-01 | Tampering | Tailwind SSOT | mitigate | Fail on config/preset files, `--config`, `@tailwind`, `@apply`, and utility-authoring docs. |
| T-188-06-02 | Tampering | layer/type/semantic CSS | mitigate | Fail literal z-index, raw type declarations, and missing semantic roles with negative fixtures. |
| T-188-06-03 | Repudiation | verifier failures | mitigate | Failure messages include offending file/rule category and tests assert the error text. |
| T-188-06-04 | Denial of Service | overbroad grep guards | mitigate | Use filtered matches and fixture tests so comments and allowlisted zones do not make the verifier unusable. |
| T-188-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; shell and ExUnit use existing repo tools. |
</threat_model>

<verification>
Run:
- `bash scripts/ci/verify_package_docs.sh`
- `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs`
</verification>

<success_criteria>
Plan 06 is complete when the package docs verifier and its negative fixture tests enforce every FND-01 through FND-06 root invariant without new dependencies or comment-sensitive grep gates.
</success_criteria>

<output>
Create `.planning/phases/188-foundations-hardening/188-06-SUMMARY.md` when done.
</output>

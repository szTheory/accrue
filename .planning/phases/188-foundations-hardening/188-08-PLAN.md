---
phase: 188-foundations-hardening
plan: "08"
type: execute
wave: 7
depends_on:
  - 188-07
files_modified:
  - accrue_admin/assets/css/app.css
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/verify_foundation_contrast.mjs
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - accrue_admin/lib/accrue_admin/components/global_search.ex
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/lib/accrue_admin/copy/subscription.ex
  - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
autonomous: true
gap_closure: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05
  - FND-06
must_haves:
  truths:
    - "A maintainer can run the Phase 188 full automated gate successfully."
    - "Phase 188 package-doc verifier negative fixtures pass and prove the foundation guards."
    - "Every overlay and sticky element references the formal layer system, with no undocumented ad-hoc z-index literals."
    - "Static guards enforce Tailwind SSOT and reject obvious Tailwind utility authoring in `accrue_admin/lib`."
    - "The full `accrue_admin` test suite passes as part of the Phase 188 final gate."
    - "`scripts/ci/verify_foundation_contrast.mjs` checks explicit dark/system scopes and the subtree dark-token scope."
  artifacts:
    - path: "accrue_admin/assets/css/app.css"
      provides: "Annotated breakpoint and documented isolated overlay micro-stack CSS"
      contains: "--ax-bp-sm"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Static guards for annotated breakpoints, micro-stacks, literal and dynamic HEEx classes, and FND-05 contrast helper invocation"
      contains: "Phase 188 foundation guards"
    - path: "scripts/ci/verify_foundation_contrast.mjs"
      provides: "Source contrast verification across light, explicit dark, subtree dark, and system dark scopes"
      contains: "subtreeDark"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Negative fixtures for micro-stack drift, dynamic HEEx utility classes, and subtree dark contrast drift"
      contains: "class={"
    - path: "accrue_admin/lib/accrue_admin/components/global_search.ex"
      provides: "Root-approved loading/hidden class pattern for command palette spinner"
      contains: "ax-"
    - path: "accrue_admin/test/accrue_admin/live/subscription_live_test.exs"
      provides: "Admin suite coverage for provider-honest subscription confirmation copy"
      contains: "provider-honest"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue_admin/assets/css/app.css"
      via: "breakpoint, z-index micro-stack, raw type, motion, and utility-authoring guards"
      pattern: "app_css"
    - from: "scripts/ci/verify_package_docs.sh"
      to: "scripts/ci/verify_foundation_contrast.mjs"
      via: "ROOT_DIR-preserving Node invocation"
      pattern: "verify_foundation_contrast"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "temp ROOT_DIR negative fixtures"
      pattern: "run_verifier"
    - from: "accrue_admin/test/accrue_admin/live/subscription_live_test.exs"
      to: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      via: "LiveView rendered confirmation copy assertions"
      pattern: "render_submit"
---

<objective>
Close the Phase 188 verification gaps found after executed Plans 01 through 07 without rewriting executed history.

Purpose: Phase 188's foundation artifacts mostly exist, but the final automated gate is not currently reproducible. This gap-closure plan restores the static verifier baseline, tightens the planned layer and Tailwind SSOT guardrails, expands the FND-05 contrast helper to the subtree dark-token scope, and restores the full `accrue_admin` suite that Plan 07 made part of Phase 188 acceptance.

Output:
- Passing package-doc verifier and verifier negative fixtures
- Enforced isolated overlay micro-stack convention
- Dynamic HEEx class-expression coverage in the Tailwind SSOT guard
- Subtree dark semantic-role contrast coverage
- Passing full `accrue_admin` ExUnit suite for the Phase 188 gate
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/REQUIREMENTS.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-UI-SPEC.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VERIFICATION.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-REVIEW.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-01-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-02-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-03-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-04-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-05-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-06-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-07-SUMMARY.md

<interfaces>
Phase 188 gap inputs:
- `188-VERIFICATION.md` gap 1: `bash scripts/ci/verify_package_docs.sh` fails because `accrue_admin/assets/css/app.css` has an unannotated `@media (max-width: 599.98px)` near the dropdown rules.
- `188-VERIFICATION.md` gap 2: package-doc verifier negative fixtures fail because the unannotated breakpoint preempts intended failure messages.
- `188-VERIFICATION.md` gap 3: overlay internals use local numeric zero/one z-index values without `ax-z-micro-stack` documentation or isolation pairing, and the verifier allows those numbers without checking the planned constraint from D-10.
- `188-VERIFICATION.md` gap 4 and `188-REVIEW.md` WR-02: HEEx utility guard scans only literal quoted class attributes and misses dynamic `class={...}` expressions; `global_search.ex` line 167 demonstrates the blind spot.
- `188-VERIFICATION.md` gap 5: `cd accrue_admin && mix test --warnings-as-errors` fails six tests; visible failures are in `subscription_live_test.exs` provider-honest confirmation copy.
- `188-VERIFICATION.md` warning and `188-REVIEW.md` WR-01: `verify_foundation_contrast.mjs` checks light, explicit dark, and system-dark scopes but not the subtree dark-token scope in `theme.css`.

Locked decisions that still govern this gap closure:
- D-10: no literal overlay z-index values except isolated, documented local micro-stacks with numeric minus-one/zero/one.
- D-15: guards must enforce Tailwind config absence, no package CSS Tailwind directives, no docs that invite Tailwind utilities, and no obvious Tailwind utility authoring in live HEEx.
- D-18 and D-19: semantic roles must be defined and verified in light, dark, and system-dark; the current Phase 189 subtree-dark scope is also an active local consumer and must be verified.
- D-23 through D-25: do not introduce a new motion token family while editing dropdown or overlay CSS.
</interfaces>
</context>

## Artifacts this phase produces

- `188-08-SUMMARY.md` proving all six verification gaps were closed or explicitly routed with command output.
- `accrue_admin/assets/css/app.css` with the dropdown breakpoint annotated with the correct `--ax-bp-*` marker and overlay internals documented as isolated micro-stacks or routed through semantic layer tokens.
- `scripts/ci/verify_package_docs.sh` with stricter layer micro-stack checks and HEEx class scanning that covers literal and dynamic class attributes.
- `scripts/ci/verify_foundation_contrast.mjs` with `subtreeDark` semantic-role contrast coverage.
- `accrue/test/accrue/docs/package_docs_verifier_test.exs` with negative fixtures for intended failure messages, micro-stack drift, dynamic HEEx utility authoring, and subtree-dark contrast drift.
- `accrue_admin/lib/accrue_admin/components/global_search.ex` with the command-palette loading/hidden class pattern converted to an approved `ax-*` class or attribute-driven pattern.
- `accrue_admin/lib/accrue_admin/live/subscription_live.ex`, `accrue_admin/lib/accrue_admin/copy/subscription.ex`, and/or `accrue_admin/test/accrue_admin/live/subscription_live_test.exs` with provider-honest confirmation copy and assertions reconciled so the full admin suite passes.

<tasks>

<task type="auto">
  <name>Task 1: Restore the CSS baseline and enforce isolated layer micro-stacks</name>
  <files>accrue_admin/assets/css/app.css, scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VERIFICATION.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-03-SUMMARY.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-06-SUMMARY.md
@/Users/jon/projects/accrue/accrue_admin/assets/css/app.css
@/Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
@/Users/jon/projects/accrue/accrue/test/accrue/docs/package_docs_verifier_test.exs
  </read_first>
  <action>Per D-10 and the first three verification gaps, update `accrue_admin/assets/css/app.css` so the dropdown breakpoint near the `details.ax-dropdown` rules carries the appropriate inline breakpoint token annotation for the existing `max-width: 599.98px` guard, matching the registry comment convention already present near the top of the file. Then resolve the drawer and step-up modal internal stacking values at `.ax-detail-drawer-backdrop`, `.ax-detail-drawer`, `.ax-step-up-modal-backdrop`, and `.ax-step-up-modal`: either replace local numeric internal values with semantic `--ax-z-*` tokens where that preserves layering, or document each intentional local internal stack with an `ax-z-micro-stack` comment and pair the containing shell with `isolation: isolate`. Do not change the semantic outer shell roles: detail drawer shell remains `--ax-z-drawer`; step-up modal shell remains `--ax-z-modal`. In `scripts/ci/verify_package_docs.sh`, tighten the z-index guard so local numeric minus-one/zero/one is accepted only when the containing CSS rule or immediately enclosing component block has both `ax-z-micro-stack` documentation and `isolation: isolate`; larger numeric literals still fail. In `accrue/test/accrue/docs/package_docs_verifier_test.exs`, add or update negative fixtures so a deliberately unannotated breakpoint still fails with the breakpoint message, a numeric internal z-index without the micro-stack/isolation pairing fails with a layer message, and a numeric internal z-index with both markers remains accepted. Keep the fixture baseline free of the current breakpoint preemption so every other Phase 188 negative fixture can reach its intended guard.</action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_package_docs.sh && cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs --trace'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `accrue_admin/assets/css/app.css` has no unannotated breakpoint media query; the existing dropdown max-width guard includes the correct `--ax-bp-*` annotation.
- Source assertion: `.ax-detail-drawer-shell` and `.ax-step-up-modal-shell` either isolate their local internal stacking context or the internal backdrop/panel declarations consume semantic layer tokens.
- Source assertion: any remaining local numeric overlay z-index in `app.css` is accompanied by `ax-z-micro-stack` and an isolation declaration in the relevant component context.
- CLI assertion: `bash scripts/ci/verify_package_docs.sh` exits 0 from the repo root.
- CLI assertion: `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs --trace` exits 0 and includes the intended breakpoint and z-index fixture tests.
  </acceptance_criteria>
  <done>The Phase 188 static gate reaches foundation checks again, and local overlay micro-stacks cannot bypass D-10 silently.</done>
</task>

<task type="auto">
  <name>Task 2: Cover dynamic HEEx classes and subtree dark contrast</name>
  <files>scripts/ci/verify_package_docs.sh, scripts/ci/verify_foundation_contrast.mjs, accrue/test/accrue/docs/package_docs_verifier_test.exs, accrue_admin/lib/accrue_admin/components/global_search.ex</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-REVIEW.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VERIFICATION.md
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/components/global_search.ex
@/Users/jon/projects/accrue/accrue_admin/assets/css/theme.css
@/Users/jon/projects/accrue/scripts/ci/verify_package_docs.sh
@/Users/jon/projects/accrue/scripts/ci/verify_foundation_contrast.mjs
@/Users/jon/projects/accrue/accrue/test/accrue/docs/package_docs_verifier_test.exs
  </read_first>
  <action>Per D-15, D-18, D-19, `188-REVIEW.md` WR-01/WR-02, and the fourth verification gap, update the HEEx utility guard in `scripts/ci/verify_package_docs.sh` so it inspects both literal quoted class attributes and dynamic `class={...}` attributes inside `~H` templates. It must reject obvious Tailwind utility authoring tokens such as spacing, layout, visibility, rounded, shadow, text, and background utilities when they appear in string literals, lists, conditionals, or interpolation-like class expressions. Keep legitimate `ax-*` classes accepted. Convert the current `global_search.ex` spinner branch away from the non-`ax-*` dynamic hidden class by using a root-approved `ax-*` class or an attribute-driven hidden/loading pattern that the guard can reason about. In `scripts/ci/verify_foundation_contrast.mjs`, add a `subtreeDark` scope that extracts the dark descendant selector block in `theme.css` and runs the same semantic-role contrast pairs as light, explicit dark, and system-dark. In `package_docs_verifier_test.exs`, add negative fixtures that mutate only a dynamic `class={...}` expression to include an obvious utility and expect the verifier to fail with the HEEx utility-authoring message, and mutate only a semantic token inside the subtree-dark scope so the contrast helper fails while light, explicit dark, and system-dark remain unchanged. Ensure `seed_tmp_dir!/1` copies any representative LiveView or component source file needed for the dynamic class fixture.</action>
  <verify>
    <automated>bash -lc 'node scripts/ci/verify_foundation_contrast.mjs && bash scripts/ci/verify_package_docs.sh && cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs --trace'</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `scripts/ci/verify_package_docs.sh` scans literal quoted `class` attributes and dynamic `class={...}` expressions in `~H` templates.
- Source assertion: `accrue_admin/lib/accrue_admin/components/global_search.ex` no longer uses a dynamic non-`ax-*` visibility utility for the command-palette spinner.
- Source assertion: `scripts/ci/verify_foundation_contrast.mjs` defines and evaluates a `subtreeDark` scope for the descendant dark-token block in `theme.css`.
- Fixture assertion: `package_docs_verifier_test.exs` has a negative fixture proving a dynamic HEEx utility class expression fails the package-doc verifier.
- Fixture assertion: `package_docs_verifier_test.exs` has a negative fixture proving subtree-dark semantic contrast drift fails the package-doc verifier.
- CLI assertion: `node scripts/ci/verify_foundation_contrast.mjs`, `bash scripts/ci/verify_package_docs.sh`, and `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs --trace` all exit 0.
  </acceptance_criteria>
  <done>The verifier covers the known Tailwind SSOT blind spot and the subtree dark-token contrast scope.</done>
</task>

<task type="auto">
  <name>Task 3: Restore the full admin suite gate without widening Phase 188 scope</name>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex, accrue_admin/lib/accrue_admin/copy/subscription.ex, accrue_admin/test/accrue_admin/live/subscription_live_test.exs</files>
  <read_first>
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VALIDATION.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-VERIFICATION.md
@/Users/jon/projects/accrue/.planning/phases/188-foundations-hardening/188-07-SUMMARY.md
@/Users/jon/projects/accrue/accrue_admin/test/accrue_admin/live/subscription_live_test.exs
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/live/subscription_live.ex
@/Users/jon/projects/accrue/accrue_admin/lib/accrue_admin/copy/subscription.ex
  </read_first>
  <action>Because Plan 07 made `cd accrue_admin && mix test --warnings-as-errors` a Phase 188 acceptance gate, run the full admin suite and fix the observed six failures without expanding foundation scope into new billing behavior. Start with the visible `subscription_live_test.exs` failures around provider-honest confirmation copy for cancel-now and cancel-at-period-end forms. Preserve the existing operator-copy contract: cancel-now confirmation must explain that the action executes against the local billing projection, and cancel-at-period-end confirmation must explain that renewal is turned off while access continues through the current billing period. Prefer production copy fixes in `AccrueAdmin.Copy.Subscription.subscription_confirm_workflow_message/2` or `SubscriptionLive` confirmation rendering when the UI lost required copy; update tests only when assertions are stale relative to existing source-of-truth copy functions. If the remaining full-suite failures are unrelated to foundation files, still resolve them only to the extent required to restore the Plan 07 gate, and document each repaired failure in `188-08-SUMMARY.md` with file, assertion, and why it was gate repair rather than new feature scope. Do not add new admin actions, routes, billing primitives, or provider capabilities.</action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/live/subscription_live_test.exs --trace && mix test --warnings-as-errors'</automated>
  </verify>
  <acceptance_criteria>
- CLI assertion: `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/live/subscription_live_test.exs --trace` exits 0.
- CLI assertion: `cd accrue_admin && mix test --warnings-as-errors` exits 0.
- Behavior assertion: rendered cancel-now confirmation includes provider-honest local billing projection copy.
- Behavior assertion: rendered cancel-at-period-end confirmation includes renewal-off/access-through-current-period copy.
- Scope assertion: no new billing primitive, provider capability, public route, or admin action is introduced to satisfy the suite.
- Summary assertion: `188-08-SUMMARY.md` lists each full-suite failure repaired, including any failure outside foundation CSS and why it was repaired as a Phase 188 gate issue.
  </acceptance_criteria>
  <done>The full `accrue_admin` suite passes again as required by the Phase 188 final gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer source edits to CI verifier | Shell, Perl, and Node scripts inspect repo files and must fail deterministically without executing untrusted project content beyond local verifier logic. |
| HEEx source to static guard | LiveView template text is parsed by the verifier to reject Tailwind utility authoring drift in package-owned UI code. |
| CSS tokens to rendered UI | Foundation tokens control focus, disabled, status, overlay, and dark-theme visibility across admin surfaces. |
| Admin LiveView action confirmation | Operator-triggered subscription actions cross from UI confirmation text into privileged billing mutations after step-up. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-08-01 | Spoofing | `accrue_admin/lib/accrue_admin/live/subscription_live.ex` confirmation copy | mitigate | Preserve provider-honest confirmation text so destructive and renewal-changing actions cannot appear safer or different than their actual billing effect. |
| T-188-08-02 | Tampering | `scripts/ci/verify_package_docs.sh` HEEx utility guard | mitigate | Scan dynamic and literal class attributes so package-owned styling cannot bypass the `ax-*` source-of-truth contract through dynamic expressions. |
| T-188-08-03 | Tampering | `accrue_admin/assets/css/app.css` overlay internals | mitigate | Require documented isolated micro-stacks or semantic layer tokens for local overlay internals, preserving D-10 and preventing accidental overlay-order drift. |
| T-188-08-04 | Information Disclosure | Phase 188 source and test changes | accept | This gap closure changes CSS, static verifiers, and tests; it does not add data exposure paths or new user-data rendering. |
| T-188-08-05 | Denial of Service | Package-doc verifier and admin test gate | mitigate | Keep guards deterministic and covered by temp-root negative fixtures; require full commands to pass so CI remains actionable. |
| T-188-08-06 | Elevation of Privilege | Subscription action UI | mitigate | Do not change step-up or authorization behavior while restoring copy assertions; run `subscription_live_test.exs` and the full admin suite. |
| T-188-08-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; schema push detection is irrelevant because no schema files are in `files_modified`. |
</threat_model>

<verification>
Run the closure proof in this order:

1. `node scripts/ci/verify_foundation_contrast.mjs`
2. `bash scripts/ci/verify_package_docs.sh`
3. `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs --trace`
4. `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/live/subscription_live_test.exs --trace`
5. `cd accrue_admin && mix test --warnings-as-errors`

The executor should include command results in `.planning/phases/188-foundations-hardening/188-08-SUMMARY.md`.
</verification>

<success_criteria>
- All current `188-VERIFICATION.md` gaps are either closed by passing automated commands or, if an unexpected full-suite failure remains outside Phase 188 ownership, explicitly routed in `188-08-SUMMARY.md` with the failing command and exact non-foundation owner file.
- `bash scripts/ci/verify_package_docs.sh` exits 0.
- `cd accrue && mix test --warnings-as-errors test/accrue/docs/package_docs_verifier_test.exs` exits 0.
- `node scripts/ci/verify_foundation_contrast.mjs` exits 0 and includes subtree dark-token scope in its checked scopes.
- `cd accrue_admin && mix test --warnings-as-errors` exits 0.
- No executed plan history from 188-01 through 188-07 is rewritten.
</success_criteria>

## Source Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 188 | Root foundation hardening remains reproducibly verified after executed history | 188-08 | COVERED | Restores the failed final gate rather than rewriting prior plans. |
| REQ | FND-01 | Typography bundles and raw type guard remain enforceable | 188-08 | COVERED | Package-doc verifier tests must pass again after breakpoint preemption is removed. |
| REQ | FND-02 | Layer system has no undocumented ad-hoc literals | 188-08 | COVERED | Task 1 closes the micro-stack documentation/isolation gap. |
| REQ | FND-03 | Reading-measure foundation remains guarded by package-doc verifier baseline | 188-08 | COVERED | Baseline verifier success is required so existing measure checks are reachable. |
| REQ | FND-04 | Tailwind SSOT guard covers dynamic HEEx class expressions | 188-08 | COVERED | Task 2 closes the literal-only scan blind spot. |
| REQ | FND-05 | Semantic role contrast includes subtree dark-token scope | 188-08 | COVERED | Task 2 adds subtree-dark contrast verification. |
| REQ | FND-06 | Motion-token guard remains reachable in package-doc verifier | 188-08 | COVERED | Restored verifier baseline ensures existing motion negative fixtures reach their intended messages. |
| RESEARCH | Layer micro-stacks require isolation and documentation | 188-08 | COVERED | Directly implements the D-10 enforcement expected by research/validation. |
| RESEARCH | Tailwind remains compiler/minifier only and not an HEEx authoring path | 188-08 | COVERED | Dynamic class-expression guard covers the known bypass. |
| RESEARCH | FND-05 source contrast helper is the fast guard before browser checks | 188-08 | COVERED | Contrast helper remains no-dependency Node and expands scope coverage. |
| CONTEXT | D-01..D-06 | Typography role contract and strict type guard | 188-08 | COVERED | Existing guard is made reachable again through package-doc verifier success. |
| CONTEXT | D-07..D-11 | Formal layer scale and local micro-stack exception rule | 188-08 | COVERED | Task 1 cites and enforces D-10. |
| CONTEXT | D-12..D-16 | Tailwind source-of-truth resolution and guards | 188-08 | COVERED | Task 2 cites and enforces D-15. |
| CONTEXT | D-17..D-22 | Semantic role completion in dark/system scopes | 188-08 | COVERED | Task 2 expands contrast coverage to local subtree dark consumers while preserving D-18/D-19 checks. |
| CONTEXT | D-23..D-25 | Existing motion-token family only | 188-08 | COVERED | Tasks explicitly avoid adding motion token families while restoring verifier reachability. |

<output>
Create `.planning/phases/188-foundations-hardening/188-08-SUMMARY.md` when done.
</output>

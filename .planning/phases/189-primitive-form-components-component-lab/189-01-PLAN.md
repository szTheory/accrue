---
phase: 189-primitive-form-components-component-lab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/assets/css/theme.css
  - accrue_admin/assets/css/app.css
  - accrue_admin/lib/accrue_admin/dev/component_registry.ex
  - accrue_admin/test/accrue_admin/dev/component_registry_test.exs
autonomous: true
requirements: [CMP-01, CMP-02]

must_haves:
  truths:
    - "`.ax-dev-state-grid` CSS exists in app.css with 2-column grid, gap:1px border, and `.ax-dev-state-cell` rules"
    - "theme.css contains a `.accrue-admin [data-theme=\"dark\"]` descendant selector with the FULL dark token block"
    - "ComponentRegistry entries for `button` and `status` families have `applicable_states`, `na_states`, and `specimens` fields"
    - "component_registry_test.exs includes test (e) asserting `data-ax-state` cells and test (f) asserting `data-theme` column wrappers"
    - "mix test test/accrue_admin/dev/component_registry_test.exs passes green"
  artifacts:
    - path: "accrue_admin/assets/css/app.css"
      provides: "`.ax-dev-state-grid` + `.ax-dev-state-grid-col` + `.ax-dev-state-cell` + `.ax-dev-state-cell-na` CSS rules"
      contains: ".ax-dev-state-grid"
    - path: "accrue_admin/assets/css/theme.css"
      provides: "sub-tree dark selector for lab column scoping"
      contains: ".accrue-admin [data-theme=\"dark\"]"
    - path: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      provides: "registry schema with `applicable_states`/`na_states`/`specimens` on button and status families"
      contains: "applicable_states"
    - path: "accrue_admin/test/accrue_admin/dev/component_registry_test.exs"
      provides: "tests (e) and (f) for state-matrix markup and theme-column data-attributes"
      contains: "data-ax-state"
  key_links:
    - from: "accrue_admin/assets/css/theme.css"
      to: ".ax-dev-state-grid-col[data-theme=\"dark\"]"
      via: ".accrue-admin [data-theme=\"dark\"] descendant selector"
      pattern: "\\.accrue-admin \\[data-theme=\"dark\"\\]"
    - from: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      to: "accrue_admin/test/accrue_admin/dev/component_registry_test.exs"
      via: "entries/0 applicable_states consumed by test (e)"
      pattern: "applicable_states"
---

<objective>
Lay the Wave 0 CSS and registry foundations that all later plans build on:
(1) add the `.ax-dev-state-grid` CSS rules to app.css so the two-column light/dark lab renderer has its layout,
(2) fix the D-07 gotcha in theme.css by adding a `.accrue-admin [data-theme="dark"]` sub-tree selector with the FULL dark token block so column-level `data-theme` attributes genuinely re-scope `--ax-*` tokens,
(3) extend ComponentRegistry entries for the existing `button` and `status` families with the three new schema fields (`applicable_states`, `na_states`, `specimens`), and
(4) add lockstep registry tests (e) and (f) per D-08.

Purpose: Without the CSS grid + sub-tree theme selector, the lab renderer (Plan 03) has no layout or genuine dark column; without at least two registry entries carrying the new schema, tests (e)/(f) cannot even mount and assert. This plan is the critical-path gate.

Output: Updated theme.css, app.css, component_registry.ex, component_registry_test.exs — all passing `mix test test/accrue_admin/dev/component_registry_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-primitive-form-components-component-lab/189-CONTEXT.md
@.planning/phases/189-primitive-form-components-component-lab/189-RESEARCH.md
@.planning/phases/189-primitive-form-components-component-lab/189-PATTERNS.md
@.planning/phases/189-primitive-form-components-component-lab/189-UI-SPEC.md
@.planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md

<interfaces>
<!-- Key code patterns the executor must know before editing. Extracted from PATTERNS.md. -->

From accrue_admin/assets/css/app.css — existing .ax-dev-* block (lines 160-229):
```css
.ax-dev-toolbar, .ax-dev-toolbar-links, .ax-dev-grid, .ax-dev-stack { display: flex; }
.ax-dev-toolbar, .ax-dev-stack { gap: var(--ax-space-md); }
.ax-dev-toolbar-links, .ax-dev-grid { gap: var(--ax-space-sm); flex-wrap: wrap; }
.ax-dev-variant-row { display: flex; flex-direction: column; gap: var(--ax-space-sm); }
```

From accrue_admin/assets/css/theme.css — dark selector (lines 218-276):
```css
html.accrue-admin[data-theme="dark"] {
  --ax-base: #0f1318;
  --ax-elevated: #171d24;
  --ax-sunken: #0b1015;
  --ax-primary: #f4f7fa;
  --ax-muted: #a8b2bc;
  /* ... FULL dark token block through line 276 ... */
  color-scheme: dark;
}
```

From accrue_admin/lib/accrue_admin/dev/component_registry.ex — current entry shape (lines 30-35):
```elixir
%{
  family: "button",
  variant: "primary",
  ax_class: "ax-button ax-button-primary",
  tokens: ["--ax-accent-strong", "--ax-accent-contrast", "--ax-transition-colors"]
}
```

From accrue_admin/test/accrue_admin/dev/component_registry_test.exs — test (a) mount pattern (lines 19-33):
```elixir
test "every registry variant appears in the /dev/components page render", %{conn: conn} do
  conn = Phoenix.ConnTest.init_test_session(conn, admin_token: "admin")
  assert {:ok, _view, html} = live(conn, "/billing/dev/components")
  for %{ax_class: ax_class} <- ComponentRegistry.entries() do
    [_base, variant_class] = String.split(ax_class, " ", parts: 2)
    assert html =~ variant_class, "..."
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add .ax-dev-state-grid CSS to app.css and sub-tree dark selector to theme.css</name>
  <files>accrue_admin/assets/css/app.css, accrue_admin/assets/css/theme.css</files>
  <read_first>
    - accrue_admin/assets/css/app.css — read the full `.ax-dev-*` block (~lines 160-229) to find the insertion point after `.ax-dev-variant-row`
    - accrue_admin/assets/css/theme.css — read the full `html.accrue-admin[data-theme="dark"]` block (lines 218-276) AND the `@media (prefers-color-scheme: dark)` block (lines 278+) to find the insertion point and copy the FULL token set verbatim
    - 189-PATTERNS.md `.ax-dev-state-grid` new block section — the exact CSS to add
    - 189-RESEARCH.md "D-07 Theme Column Implementation" section — the Option B sub-tree selector rationale and exact selector form
  </read_first>
  <behavior>
    - app.css gets `.ax-dev-state-grid` block (grid, 2 columns, `--ax-border` gap/bg), `.ax-dev-state-grid-col` (flex-col, gap 1px, bg `--ax-border`), `.ax-dev-state-grid-col-header` (padding + `--ax-sunken` bg), `.ax-dev-state-cell` (flex-col, gap `--ax-space-sm`, padding `--ax-space-md`, bg `--ax-base`, min-height 4rem), `.ax-dev-state-cell-na` (bg `--ax-sunken`, opacity 0.6), and a `@media (max-width: 599.98px)` rule collapsing the grid to 1 column
    - theme.css gets a new rule block with selector `html.accrue-admin [data-theme="dark"], .accrue-admin [data-theme="dark"]` containing the VERBATIM full dark token list copied from `html.accrue-admin[data-theme="dark"]` (every `--ax-*` variable, including `color-scheme: dark`) — MUST be 100% complete, no subset; inserted AFTER the existing `html.accrue-admin[data-theme="dark"]` block and BEFORE the `@media (prefers-color-scheme: dark)` block
    - Add a comment in theme.css: "Phase 189 — sub-tree theme scoping for the component lab state-matrix columns. Keeps html.accrue-admin[data-theme='dark'] for full-page production use; adds .accrue-admin [data-theme='dark'] so any descendant element with data-theme='dark' inside .accrue-admin genuinely re-scopes --ax-* tokens (standard Radix/Primer pattern). Must contain the FULL dark token set — no subset."
  </behavior>
  <verify>
    <automated>grep -c "ax-dev-state-grid" accrue_admin/assets/css/app.css && grep -c "\.accrue-admin \[data-theme=\"dark\"\]" accrue_admin/assets/css/theme.css</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `.ax-dev-state-grid { display: grid; grid-template-columns: 1fr 1fr;` (exact)
    - app.css contains `.ax-dev-state-cell-na { background: var(--ax-sunken); opacity: 0.6; }`
    - app.css contains a `@media (max-width: 599.98px)` rule setting `.ax-dev-state-grid { grid-template-columns: 1fr; }`
    - theme.css contains `.accrue-admin [data-theme="dark"]` (the sub-tree selector)
    - theme.css dark sub-tree selector block contains `--ax-base: #0f1318` (confirms full token copy, not a subset)
    - theme.css dark sub-tree selector block contains `color-scheme: dark`
    - The sub-tree selector appears BEFORE `@media (prefers-color-scheme: dark)` in the file
  </acceptance_criteria>
  <done>app.css has .ax-dev-state-grid layout rules; theme.css has sub-tree dark selector with full token set; grep counts both non-zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ComponentRegistry schema for button + status families; add lockstep tests (e) and (f)</name>
  <files>accrue_admin/lib/accrue_admin/dev/component_registry.ex, accrue_admin/test/accrue_admin/dev/component_registry_test.exs</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/dev/component_registry.ex — read in full to understand all existing entries (button×4, status×5, card×6, foundation×8 families); note the `variants_for/1` helper and the module `Mix.env() != :prod` guard
    - accrue_admin/test/accrue_admin/dev/component_registry_test.exs — read in full to understand all four existing tests (a)–(d), the `AccrueAdmin.LiveCase` usage, and the CSS path helpers; confirm which fields the tests currently read (only `ax_class` and `tokens`)
    - 189-PATTERNS.md "component_registry.ex — Schema Extension" section — the new 3-field format, the `ax_class` two-class guard, and the `variants_for/1` note
    - 189-PATTERNS.md "component_registry_test.exs — New Tests (e) and (f)" section — exact test (e) and test (f) code to add
    - 189-UI-SPEC.md "Per-component specimen contracts — Button" and "StatusBadge / tag / pill" sections — exact `applicable_states`, `na_states`, and specimen content
  </read_first>
  <behavior>
    - Add three new fields to each of the 4 button entries and each of the 5 status/badge entries in ComponentRegistry: `applicable_states` (list of strings from the 11-state taxonomy), `na_states` (list of `%{state: string, reason: string}` maps), `specimens` (list of `%{label: string, props: map, content: string | nil}` maps)
    - Button families: `applicable_states: ["default", "hover", "focus", "active", "pressed", "disabled", "loading", "overflow"]`; `na_states: [%{state: "selected", reason: "button has no selection state — use aria-pressed for toggle buttons separately"}, %{state: "empty", reason: "button always has a label"}, %{state: "error", reason: "button conveys intent via variant, not validation state"}]`; specimens include default "Save changes", long-label "Export all subscription events to CSV" (overflow), disabled "Archived", loading "Saving…" (D-06 overflow baked in)
    - Status families: `applicable_states: ["default", "overflow"]`; `na_states` for all 9 remaining states each with reason "non-interactive display element — no interactive state applies"; specimens for each tone with the copy from UI-SPEC (e.g. "Active" for moss, "Processing" for cobalt, etc.) and a long-label specimen "Requires additional customer authentication step" (CMP-02 overflow)
    - The four existing tests (a)–(d) MUST continue to pass — do NOT remove any existing fields, only add new ones
    - Add test (e): mount `/billing/dev/components`; for each `ComponentRegistry.entries()` entry that `Map.has_key?(entry, :applicable_states)`, assert `html =~ ~s(data-ax-state="#{state}")` for every `applicable_states` item AND every `na_states` item (n/a rows must still render with the state label); match the exact pattern from PATTERNS.md test (e)
    - Add test (f): mount `/billing/dev/components`; assert `html =~ ~s(data-theme="light")` and `html =~ ~s(data-theme="dark")`; match the exact pattern from PATTERNS.md test (f)
    - Note: tests (e) and (f) will FAIL until Plan 03 adds the renderer — that is intentional TDD (RED phase); the registry structural assertions succeed as soon as the kitchen renderer emits the `data-ax-state` and `data-theme` attributes
    - IMPORTANT: The executor must handle a chicken-and-egg here. Since the lab renderer (Plan 03) doesn't exist yet, tests (e) and (f) that mount the page will fail. Solution: mark these tests with `@tag :pending` or use `@tag skip: true` temporarily, OR scope them so they only check the registry data structure rather than the mounted page HTML. Preferred: add tests (e) and (f) as STRUCTURAL tests that assert `ComponentRegistry.entries()` returns maps with the new fields — NOT mounted-page HTML assertions. The mounted-page HTML assertions (that `data-ax-state` cells render) belong in Plan 03 after the renderer exists. Document this split clearly with a comment in the test file: "HTML mount assertions for state-matrix cells are in Plan 03's wave; these tests assert the registry data contract."
  </behavior>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/accrue_admin/dev/component_registry_test.exs` exits 0 with all tests passing (including new structural tests (e) and (f))
    - `ComponentRegistry.entries()` returns maps where the first button entry contains `applicable_states: ["default", "hover", "focus", "active", "pressed", "disabled", "loading", "overflow"]`
    - `ComponentRegistry.entries()` returns maps where the first status entry contains `applicable_states: ["default", "overflow"]`
    - Every `na_states` entry has both `state` and `reason` keys (non-nil strings)
    - Every `specimens` entry has `label`, `props`, and `content` keys
    - All four pre-existing tests (a)–(d) still pass
    - The new structural test (e) asserts `Map.has_key?(entry, :applicable_states)` for each entry that has the field
    - The new test (f) asserts `Map.has_key?(entry, :na_states)` and `Map.has_key?(entry, :specimens)`
  </acceptance_criteria>
  <done>ComponentRegistry entries for button and status families carry 7 fields; all registry tests green; new fields structurally validated; no existing tests broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only guard | Both ComponentRegistry and ComponentKitchenLive are guarded by `Mix.env() != :prod`; this surface never runs in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
| T-189-01 | Information Disclosure | theme.css dark token set | accept | Dev-only page; tokens are non-sensitive CSS values, not secrets |

Security domain: **not applicable** — dev-only surface, prod-guarded by `Mix.env() != :prod`.
</threat_model>

<verification>
- `grep -c "ax-dev-state-grid" accrue_admin/assets/css/app.css` — non-zero
- `grep -c "\.accrue-admin \[data-theme=\"dark\"\]" accrue_admin/assets/css/theme.css` — non-zero
- theme.css dark sub-tree block contains `--ax-base: #0f1318` — confirms full token copy
- `cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs` — exits 0
- No existing tests broken
</verification>

<success_criteria>
- app.css contains the full `.ax-dev-state-grid` block with 2-column grid, cell padding, n/a styling, and mobile media query
- theme.css contains the `.accrue-admin [data-theme="dark"]` sub-tree selector with the FULL dark token block (D-07 critical path)
- ComponentRegistry button and status entries each carry `applicable_states`, `na_states`, and `specimens`
- All 6 registry tests (4 existing + 2 new structural) pass
- No regressions to any pre-existing test
</success_criteria>

<output>
Create `.planning/phases/189-primitive-form-components-component-lab/189-01-SUMMARY.md` when done
</output>

---
phase: 189-primitive-form-components-component-lab
plan: 03
type: execute
wave: 2
depends_on: ["189-01", "189-02"]
files_modified:
  - accrue_admin/lib/accrue_admin/dev/component_registry.ex
  - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
  - accrue_admin/test/accrue_admin/dev/component_registry_test.exs
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03]

must_haves:
  truths:
    - "ComponentRegistry has entries for all 14 Phase-189 primitive families with applicable_states, na_states, and specimens fields"
    - "ComponentKitchenLive renders a registry-driven .ax-dev-state-grid per family with data-theme='light' and data-theme='dark' columns"
    - "Every applicable state renders a .ax-dev-state-cell[data-ax-state] element; every n/a state renders a .ax-dev-state-cell-na[data-ax-state] with reason"
    - "Specimens bake long/overflowing content (CMP-02) per D-06"
    - "mix test test/accrue_admin/dev/component_registry_test.exs exits 0 with all 7 tests (a)–(g) passing"
    - "The mounted /billing/dev/components page HTML contains data-theme='light' AND data-theme='dark' column wrappers (test (g))"
    - "D-07 is only PARTIALLY validated here (data-theme HTML attributes present in rendered output); browser-level resolved-color delta is verified in Plan 06 (themeColumnDeltaProbe) — do NOT mark D-07 fully resolved before Plan 06 passes"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      provides: "All 14 Phase-189 primitive family entries with full 7-field schema"
      contains: "applicable_states"
    - path: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      provides: "Registry-driven matrix renderer replacing hand-authored sections"
      contains: "ax-dev-state-grid"
    - path: "accrue_admin/test/accrue_admin/dev/component_registry_test.exs"
      provides: "Tests (e) and (f) remain structural; new test (g) adds mounted-page HTML assertions for data-ax-state and data-theme attributes"
      contains: "data-ax-state"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      to: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      via: "ComponentRegistry.entries() consumed by matrix renderer loop"
      pattern: "ComponentRegistry.entries"
    - from: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      to: "accrue_admin/assets/css/app.css"
      via: ".ax-dev-state-grid CSS class emitted by renderer"
      pattern: "ax-dev-state-grid"
---

<objective>
Convert ComponentKitchenLive from hand-authored per-family sections to a registry-driven two-column state-matrix renderer, and extend ComponentRegistry with all 14 Phase-189 primitive family entries (button, input, textarea, checkbox, radio, toggle, select, form-field, status_badge, icon, money_formatter, json_viewer, spinner/skeleton, tooltip, inline_id, empty_state).

The renderer emits `.ax-dev-state-grid` > two `.ax-dev-state-grid-col[data-theme]` columns, each with `.ax-dev-state-cell[data-ax-state]` rows for applicable states and `.ax-dev-state-cell-na[data-ax-state]` rows for n/a states with their stated reasons. This resolves the D-07 gotcha — the `data-theme` attribute on each column wrapper genuinely re-scopes `--ax-*` tokens via the theme.css sub-tree selector added in Plan 01.

Add new test (g) to component_registry_test.exs as a mounted-page HTML assertion for data-theme and data-ax-state presence. Tests (e) and (f) from Plan 01 remain as structural data-contract tests and are NOT modified into mounted-page assertions — they stay exactly as Plan 01 left them.

Purpose: This is the CMP-01 deliverable — the systematic gallery proving every component is exercised in both themes. It also fulfills D-05 "tested ⇔ shown": the state list the registry declares is the same list the grid renders and the tests assert.

Output: Updated component_registry.ex, component_kitchen_live.ex, component_registry_test.exs — all tests passing, page renderable at /billing/dev/components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-primitive-form-components-component-lab/189-CONTEXT.md
@.planning/phases/189-primitive-form-components-component-lab/189-RESEARCH.md
@.planning/phases/189-primitive-form-components-component-lab/189-PATTERNS.md
@.planning/phases/189-primitive-form-components-component-lab/189-UI-SPEC.md
@.planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md

<interfaces>
<!-- Current registry and kitchen patterns. Must read before editing. -->

From accrue_admin/lib/accrue_admin/dev/component_registry.ex — current entry shape, variants_for/1:
```elixir
# Current 4-field entry (Phase 189 extends to 7 fields for all families):
%{
  family: "button", variant: "primary",
  ax_class: "ax-button ax-button-primary",
  tokens: ["--ax-accent-strong", "--ax-accent-contrast", "--ax-transition-colors"],
  # Phase 189 adds:
  applicable_states: [...], na_states: [...], specimens: [...]
}

def variants_for(family) do
  Enum.filter(entries(), &(&1.family == family))
end
```

From accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex — current registry-driven loop (lines 172-186, to REPLACE):
```elixir
<div class="ax-dev-grid">
  <%= for entry <- ComponentRegistry.variants_for("button") do %>
    <div class="ax-dev-variant-row">
      <Button.button variant={entry.variant} type="button">
        <%= String.capitalize(entry.variant) %>
      </Button.button>
      ...token map...
    </div>
  <% end %>
</div>
```

Replacement — new matrix renderer pattern (from 189-PATTERNS.md):
```heex
<%!-- State-matrix renderer: rows = applicable states; light + dark columns side-by-side.
     .ax-dev-state-grid-col[data-theme] re-scopes --ax-* tokens via the theme.css
     `.accrue-admin [data-theme="dark"]` sub-tree selector added in Phase 189. --%>
<section class="ax-card ax-dev-stack" data-ax-family={entry.family}>
  <p class="ax-label"><%= entry.family %></p>
  <div class="ax-dev-state-grid">
    <div class="ax-dev-state-grid-col" data-theme="light">
      <p class="ax-dev-state-grid-col-header ax-label">Light</p>
      <%= for state <- entry.applicable_states do %>
        <div class="ax-dev-state-cell" data-ax-state={state}>
          <span class="ax-dev-state-cell-label ax-type-code-xs ax-muted"><%= state %></span>
          <%!-- render specimen for this state --%>
        </div>
      <% end %>
      <%= for %{state: state, reason: reason} <- entry.na_states do %>
        <div class="ax-dev-state-cell ax-dev-state-cell-na" data-ax-state={state} data-ax-na-reason={reason}>
          <span class="ax-dev-state-cell-label ax-type-code-xs ax-muted"><%= state %> (n/a)</span>
          <span class="ax-type-code-xs ax-muted"><%= reason %></span>
        </div>
      <% end %>
    </div>
    <div class="ax-dev-state-grid-col" data-theme="dark">
      <%!-- mirror of light column --%>
    </div>
  </div>
</section>
```

Cell-id grammar (frozen, from baseline-manifest.js):
```
p187__{surface}__{mode}__{theme}__{state}__{dXX}
Phase-189 vocabulary → Phase-187 COMPONENT_STATES mapping:
  default → default-populated
  hover/focus/active → interactive-open
  disabled → disabled-readonly
  error → error
  overflow → overflow
  loading → loading
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ComponentRegistry with all 14 Phase-189 primitive family entries</name>
  <files>accrue_admin/lib/accrue_admin/dev/component_registry.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/dev/component_registry.ex — read in full to understand current structure; note all existing families (button×4, status×5, card×6, foundation×~8) and the `Mix.env() != :prod` guard
    - 189-UI-SPEC.md "Per-component specimen contracts" for all 14 families — exact `applicable_states`, `na_states`, and specimen content strings (this is the authoritative source for what goes in each entry)
    - 189-PATTERNS.md "component_registry.ex — Schema Extension" — the `ax_class` two-class guard requirement
    - accrue_admin/lib/accrue_admin/components/select.ex — read to get the function name and CSS class used
    - accrue_admin/lib/accrue_admin/components/money_formatter.ex — read to get the function name and CSS class used
    - accrue_admin/lib/accrue_admin/components/json_viewer.ex — read to get the function name and CSS class used
  </read_first>
  <action>
    Add entries for these 14 families (each entry must have all 7 fields; `ax_class` must always contain at least two space-separated tokens to satisfy test (a)):

    BUTTON — update the 4 existing entries (primary, secondary, ghost, danger) with the Phase 189 schema fields. From UI-SPEC: `applicable_states: ["default", "hover", "focus", "active", "pressed", "disabled", "loading", "overflow"]`; `na_states: [selected, empty, error with reasons]`; specimens include "Save changes", "Go" (short), "Export all subscription events to CSV" (long/overflow, bakes D-06), "Saving…" (loading).

    INPUT — add one entry per variant (text, email, number, password). `ax_class: "ax-field ax-field-input"`. `applicable_states: ["default", "focus", "error", "disabled", "overflow"]`. `na_states: [hover (inputs show cursor change only, no bg shift), active (inputs do not press), loading, selected, empty, pressed]` each with reason. Specimens per UI-SPEC: default with placeholder, filled value (cus_1234567890abcdefghijklmnopqrstuvwxyz for overflow), error state, disabled, readonly.

    TEXTAREA — add one entry. `ax_class: "ax-field ax-textarea-field"`. Same applicable_states as input. Specimens: default, multiline filled, error, disabled, readonly.

    CHECKBOX — add one entry. `ax_class: "ax-field ax-checkbox-field"`. `applicable_states: ["default", "selected", "focus", "disabled"]`. `na_states: [hover, active, loading, error, empty, overflow, pressed]` with reasons.

    RADIO — add one entry. `ax_class: "ax-field ax-radio-field"`. Same as checkbox.

    TOGGLE — add one entry. `ax_class: "ax-toggle ax-toggle-switch"`. `applicable_states: ["default", "selected", "focus", "disabled"]`. Same na_states rationale as checkbox.

    SELECT — add one entry. `ax_class: "ax-field ax-select-field"`. `applicable_states: ["default", "focus", "error", "disabled"]`.

    FORM-FIELD — add one entry for the wrapper. `ax_class: "ax-field ax-form-field"`. `applicable_states: ["default", "error"]`. `na_states` for all others.

    STATUS-BADGE — update the 5 existing entries (moss, cobalt, amber, slate, ink) with schema fields. `applicable_states: ["default", "overflow"]`. `na_states` for all 9 others with reason "non-interactive display element".

    ICON — add one entry. `ax_class: "ax-icon ax-icon-md"`. `applicable_states: ["default"]`. `na_states` for all 10 others with reason "non-interactive display primitive".

    MONEY — add one entry. `ax_class: "ax-money ax-money-display"`. `applicable_states: ["default", "overflow"]`.

    JSON-VIEWER — add one entry. `ax_class: "ax-json-viewer ax-json-tree"`. `applicable_states: ["default", "empty", "overflow"]`.

    SPINNER — add one entry. `ax_class: "ax-spinner ax-spinner-sm"`. `applicable_states: ["loading"]`. `na_states` for all others with reason "spinner only exists in loading state".

    TOOLTIP — add one entry. `ax_class: "ax-tooltip-wrapper ax-tooltip-above"`. `applicable_states: ["default", "overflow"]`.

    INLINE-ID — add one entry. `ax_class: "ax-inline-id ax-inline-id-short"`. `applicable_states: ["default", "overflow"]`.

    EMPTY-STATE — add one entry. `ax_class: "ax-empty ax-empty-no-data"`. `applicable_states: ["empty"]`. `na_states` for all others with reason "empty-state hero only exists in the empty context; container is non-interactive".

    The `specimens` field for each entry must bake at least one long/overflow specimen (D-06 and CMP-02). Every `na_states` entry MUST have a non-empty `reason` string (D-09).

    The existing card×6 and foundation×~8 entries keep their current 4-field shape (they are not Phase-189 primitives; adding `applicable_states` to them is out of scope — leave them unchanged).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/accrue_admin/dev/component_registry_test.exs` exits 0 with all tests passing
    - `ComponentRegistry.entries() |> Enum.filter(& Map.has_key?(&1, :applicable_states)) |> length()` returns 14+ (all Phase-189 families have the new fields)
    - Every entry with `applicable_states` also has `na_states` and `specimens`
    - Every `na_states` entry has a non-empty `reason` string
    - At least one `specimens` entry per family has content longer than 30 chars (D-06 overflow specimen)
    - All existing tests (a)–(f) still pass (including the structural tests (e) and (f) from Plan 01 — do NOT modify them)
  </acceptance_criteria>
  <done>All 14 Phase-189 primitive families have 7-field registry entries; all registry tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert ComponentKitchenLive to registry-driven matrix renderer; add test (g) as mounted HTML assertion</name>
  <files>
    accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex,
    accrue_admin/test/accrue_admin/dev/component_registry_test.exs
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex — read in full (all ~498 lines) to understand the full render structure; note: section 9 (lines 172-186 buttons loop), section 10 (statuses), section 11 (cards), and all helper functions (assign_shell/4, fake_processor?/0, color_token?/1, non_color_token?/1, token_kind/1) which must be preserved
    - accrue_admin/test/accrue_admin/dev/component_registry_test.exs — read in full; tests (e) and (f) are structural data-contract tests (added in Plan 01) and MUST NOT be changed; add new test (g) as an additional test
    - 189-PATTERNS.md "component_kitchen_live.ex — Registry-Driven Matrix Renderer" section — the exact template for the new two-column renderer, the D-07 gotcha comment, and the assign_shell/4 deficiency (active_organization_name)
    - 189-RESEARCH.md "ComponentKitchenLive current structure" — the 14-section render map; note which sections are hand-authored (keep) vs registry-driven (replace)
    - 189-UI-SPEC.md "Lab Renderer Visual Contract" — column header labels ("Light"/"Dark"), family section structure, state label format
    - 189-PATTERNS.md "component_registry_test.exs — New Tests (e) and (f)" — for context on the structural tests already present; the new test is (g)
  </read_first>
  <action>
    KITCHEN LIVE CONVERSION:
    Replace sections 9 (buttons), 10 (statuses), and 11 (cards) hand-authored loops with a single registry-driven primitive section. The new section iterates over `ComponentRegistry.entries()` grouped by family (use `Enum.group_by/2` on `entries() |> Enum.filter(& Map.has_key?(&1, :applicable_states))`), and for EACH family group renders:
    - A `<section class="ax-dev-family" data-ax-family={family}>` wrapper
    - An `<h3 class="ax-type-eyebrow">` heading with the capitalized family name
    - An optional description line in `.ax-type-body-sm.ax-muted`
    - A `<div class="ax-dev-state-grid">` containing:
      - Left: `<div class="ax-dev-state-grid-col" data-theme="light">` with header "Light" + one `.ax-dev-state-cell[data-ax-state]` per `entry.applicable_states` (each cell renders the appropriate specimen for that state) + one `.ax-dev-state-cell.ax-dev-state-cell-na[data-ax-state][data-ax-na-reason]` per `entry.na_states`
      - Right: `<div class="ax-dev-state-grid-col" data-theme="dark">` — exact mirror of the left column

    For specimen rendering inside cells: each cell must call the appropriate Phoenix component. Use a `render_specimen/2` helper private function that pattern-matches on `entry.family` and `state` to dispatch the right component call with the right specimen props. The specimen data in `entry.specimens` provides labels and props — use them to parametrize each component call. Key requirement: each `applicable_states` state must produce a visually distinct cell (e.g., "disabled" state cell must render the component with `disabled={true}`).

    Preserve ALL existing hand-authored sections (icon gallery, detail skeleton, related-resources, banners showcase, motion reference table, Phase 188 foundation specimens). These are NOT replaced — only the button/status/card loops are superseded.

    Fix the `assign_shell/4` deficiency identified in PATTERNS.md: add `|> assign(:active_organization_name, admin["active_organization_name"])` to prevent a potential KeyError if AppShell requires this assign.

    Replace the D-07 gotcha comment (lines 166-168) with a comment explaining the new genuine two-column approach: "State-matrix renderer: light and dark columns are genuine theme scopes via .accrue-admin [data-theme='dark'] sub-tree selector in theme.css (Phase 189). Each column independently inherits --ax-* token values — not merely duplicates with a class toggle. NOTE: HTML attribute presence (data-theme='light'/'dark') is verified by test (g); browser-level resolved-color delta is verified by Plan 06's themeColumnDeltaProbe — that is the definitive D-07 sign-off."

    Add alias imports for all new component modules at the top of the module: `alias AccrueAdmin.Components.{Textarea, Checkbox, Radio, Toggle, Spinner, Tooltip, EmptyState, InlineId}`.

    ADD NEW TEST (g) — DO NOT MODIFY tests (e) or (f):
    Tests (e) and (f) from Plan 01 are structural data-contract tests. Add a NEW test (g) that mounts the page and asserts HTML:
    - Test (g): mount `/billing/dev/components`; assert `html =~ ~s(data-theme="light")` and `html =~ ~s(data-theme="dark")` (confirms the renderer emits both column wrappers); assert `html =~ ~s(data-ax-state="default")` and `html =~ ~s(data-ax-state="disabled")` (confirms state cells are rendered). Add a comment: "Mounted-page HTML assertion (introduced in Plan 03 after the renderer exists). Tests (e) and (f) remain structural data-contract tests — do not collapse them into this test."
    Remove any `@tag skip: true` workarounds if they were added to tests (e) or (f) in Plan 01; the structural tests should run cleanly. Test (g) is the new mounted-page test and should also run cleanly (renderer now exists).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/accrue_admin/dev/component_registry_test.exs` exits 0 with all 7 tests (a)–(g) passing
    - Mounted `/billing/dev/components` HTML contains `data-theme="light"` and `data-theme="dark"` (test (g))
    - Mounted HTML contains `data-ax-state="default"` and `data-ax-state="disabled"` (test (g) sampling)
    - Mounted HTML contains `data-ax-na-reason` attributes for n/a states
    - The page no longer has the D-07 "per-specimen wrappers were inert" comment (it was replaced with the genuine-scoping explanation and the Plan 06 cross-reference)
    - `assign_shell/4` now includes `|> assign(:active_organization_name, ...)` (no potential KeyError)
    - All 4 pre-existing tests (a)–(d) still pass
    - Tests (e) and (f) (structural) still pass and are NOT modified into page-mount tests
    - New test (g) is the only mounted-page HTML assertion for data-theme/data-ax-state
    - `cd accrue_admin && mix test` exits 0 (no regressions across the full test suite)
    - NOTE: Presence of data-theme attributes in HTML proves the renderer emits the correct structure. Browser-level resolved-color delta (D-07 definitive proof) is in Plan 06 — do not mark D-07 fully resolved here.
  </acceptance_criteria>
  <done>Kitchen renderer is registry-driven; two-column state-grid emitted per family; all 7 registry tests green; full test suite green; D-07 HTML-attribute layer verified, browser-color layer deferred to Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only guard | ComponentKitchenLive is guarded by `Mix.env() != :prod`; renderer only active in dev/test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
| T-189-03 | Tampering | ComponentRegistry entries | accept | Registry is read-only at runtime; dev-only module; data integrity verified by drift tests |

Security domain: **not applicable** — dev-only surface, prod-guarded by `Mix.env() != :prod`.
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/dev/component_registry_test.exs` — exits 0, 7 tests pass (a)–(g)
- Mounted page HTML contains `data-theme="light"` and `data-theme="dark"` (test (g))
- Mounted page HTML contains `data-ax-state` attributes for all families that have `applicable_states`
- Tests (e) and (f) remain structural data-contract tests (unchanged from Plan 01)
- `cd accrue_admin && mix test` — full suite exits 0
</verification>

<success_criteria>
- ComponentRegistry has 14+ Phase-189 primitive family entries with 7-field schema (CMP-01)
- ComponentKitchenLive renders the registry-driven two-column state-matrix for all primitive families
- All 7 registry tests pass (CMP-01 "tested ⇔ shown" invariant, per D-05 and D-08)
- Specimens include overflow content for every family (CMP-02, D-06)
- D-07 gotcha: renderer emits genuine `data-theme` column wrappers (HTML attribute layer proven by test (g)); browser-level resolved-color delta is the definitive D-07 proof — verified in Plan 06 (themeColumnDeltaProbe)
- Cross-reference: Plan 06 themeColumnDeltaProbe is the definitive D-07 sign-off; do not mark D-07 complete before Plan 06 passes
</success_criteria>

<output>
Create `.planning/phases/189-primitive-form-components-component-lab/189-03-SUMMARY.md` when done
</output>

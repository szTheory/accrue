---
phase: 189-primitive-form-components-component-lab
plan: 04
type: execute
wave: 2
depends_on: ["189-01", "189-02"]
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/lib/accrue_admin/components/button.ex
  - accrue_admin/lib/accrue_admin/components/input.ex
  - accrue_admin/lib/accrue_admin/components/select.ex
autonomous: true
requirements: [CMP-03, CMP-04, CMP-05]

must_haves:
  truths:
    - "app.css .ax-button has no raw font-size/font-weight/line-height literals — uses composed type role token font: var(--ax-type-label-font)"
    - "app.css .ax-field-control:focus-visible has outline: 2px solid var(--ax-focus-ring); outline-offset: 2px; box-shadow: var(--ax-focus-shadow) — no outline: none"
    - "app.css .ax-field-control-error uses var(--ax-status-danger-border) not var(--ax-warning)"
    - "app.css .ax-field-error uses var(--ax-status-danger-text) not var(--ax-warning)"
    - "app.css .ax-button:disabled / [aria-disabled] uses opacity: var(--ax-disabled-opacity) and cursor: var(--ax-disabled-cursor) — no pointer-events: none without cursor"
    - "app.css .ax-field-control:disabled uses --ax-disabled-bg/border/text tokens"
    - "app.css .ax-field-control[readonly] uses --ax-readonly-bg/border/text tokens"
    - "input.ex duplicate error-ID defect fixed: errors use Enum.with_index for indexed IDs"
    - "select.ex duplicate error-ID defect fixed"
    - "button.ex has loading attr and emits aria-busy on the button element when loading is true"
  artifacts:
    - path: "accrue_admin/assets/css/app.css"
      provides: "Button/input/field focus, disabled, readonly, error token root fixes"
      contains: "--ax-status-danger-border"
    - path: "accrue_admin/lib/accrue_admin/components/button.ex"
      provides: "loading attr + aria-busy; min touch target at 390px"
      contains: "aria-busy"
    - path: "accrue_admin/lib/accrue_admin/components/input.ex"
      provides: "Indexed error IDs (no duplicate-id axe violation)"
      contains: "Enum.with_index"
    - path: "accrue_admin/lib/accrue_admin/components/select.ex"
      provides: "Indexed error IDs"
      contains: "Enum.with_index"
  key_links:
    - from: "accrue_admin/assets/css/app.css"
      to: "accrue_admin/assets/css/theme.css"
      via: "var(--ax-status-danger-border) resolves to #b42e2e (danger border token)"
      pattern: "--ax-status-danger-border"
    - from: "accrue_admin/lib/accrue_admin/components/input.ex"
      to: "accrue_admin/assets/css/app.css"
      via: "ax-field-control-error class triggers danger token border"
      pattern: "ax-field-control-error"
---

<objective>
Apply Phase-188 token root fixes to the button, input, select, and form-field CSS selectors in app.css, and fix HEEx defects in button.ex, input.ex, and select.ex.

The CSS fixes resolve the confirmed defects from the Phase-187 defect ledger:
1. Remove all `ax-type-exception` raw font literal blocks from `.ax-button`, `.ax-button-sm`, `.ax-field-label`, `.ax-field-help`, `.ax-field-error`, `.ax-field-control` — replace with composed role tokens
2. Fix `.ax-field-control:focus-visible` — remove `outline: none`, apply the full focus ring contract
3. Fix `.ax-field-control-error` — change `var(--ax-warning)` to `var(--ax-status-danger-border)`
4. Fix `.ax-field-error` color — change `var(--ax-warning)` to `var(--ax-status-danger-text)`
5. Fix `.ax-button:disabled` / `.ax-button[aria-disabled]` — replace `opacity: 0.5` with `var(--ax-disabled-opacity)`, remove `pointer-events: none` and add `cursor: var(--ax-disabled-cursor)` explicitly
6. Add `.ax-field-control:disabled` and `.ax-field-control[readonly]` rules consuming Phase-188 tokens
7. Add `.ax-button` and `.ax-field-control` to the consolidated `:focus-visible` block (if not already there per Phase-188 Plan 04)
8. Add mobile min-height for `.ax-button` (44px at 390px) for CMP-03 WCAG 2.5.5

HEEx fixes in button.ex, input.ex, select.ex address the axe-core `duplicate-id` defect (multiple `<p id=@id <> "-error">` elements when there are multiple errors).

Purpose: CMP-03 (accessible focus, role, keyboard), CMP-04 (disabled unmistakable, button text contrast), CMP-05 (root-fix only, not per-page patch).

Output: Corrected app.css, button.ex, input.ex, select.ex — all fixes at component root, zero per-page patches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-primitive-form-components-component-lab/189-CONTEXT.md
@.planning/phases/189-primitive-form-components-component-lab/189-RESEARCH.md
@.planning/phases/189-primitive-form-components-component-lab/189-PATTERNS.md
@.planning/phases/189-primitive-form-components-component-lab/189-UI-SPEC.md

<interfaces>
<!-- Current defective CSS selectors the executor must find and fix. -->

From 189-PATTERNS.md "app.css Button root defects" (lines 1268-1338):
Current defective rules to replace:
- `.ax-button` raw font-size: 0.875rem → font: var(--ax-type-label-font); letter-spacing: var(--ax-type-label-tracking)
- `.ax-button[aria-disabled], .ax-button:disabled` opacity: 0.5 → var(--ax-disabled-opacity); pointer-events: none → remove, add cursor: var(--ax-disabled-cursor)
- `.ax-button-sm` raw font-size: ... → migrate to label-sm token

Focus-visible block (lines 2898-2917 — AUTHORITATIVE, check if .ax-button and .ax-field-control already in it):
```css
.ax-button:focus-visible,
/* ... others ... */
.ax-field-control:focus-visible,
.ax-select-control:focus-visible,
/* ... */ {
  outline: 2px solid var(--ax-focus-ring);
  outline-offset: 2px;
  box-shadow: var(--ax-focus-shadow);
}
```

From 189-PATTERNS.md "app.css field error token defect":
```css
/* CURRENT (defective): */
.ax-field-control-error { border-color: var(--ax-warning); }    /* → var(--ax-status-danger-border) */
.ax-field-error { color: var(--ax-warning); }                   /* → var(--ax-status-danger-text) */
```

Lines to REMOVE (shadow the correct Phase-188 block):
- Lines 1687-1692: `.ax-input:focus-visible, .ax-select:focus-visible, .ax-checkbox:focus-visible { border-color:...; outline: none; }`
- Lines 1758-1762: `.ax-field-control:focus-visible { border-color:...; outline: none; }`

From 189-PATTERNS.md "Shared Patterns — Disabled state token consumption":
```css
.ax-field-control:disabled {
  color: var(--ax-disabled-text);
  background: var(--ax-disabled-bg);
  border-color: var(--ax-disabled-border);
  opacity: var(--ax-disabled-opacity);
  cursor: var(--ax-disabled-cursor);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix button, field-control, and field CSS root defects in app.css</name>
  <files>accrue_admin/assets/css/app.css</files>
  <read_first>
    - accrue_admin/assets/css/app.css — read the following specific sections (use grep -n to find exact line numbers first, then read each section):
      - Search: `grep -n "ax-type-exception\|ax-button\|ax-field-control\|ax-field-error\|ax-field-label\|ax-field-help\|outline: none\|ax-warning\|focus-visible" accrue_admin/assets/css/app.css | head -60`
      - The button block (~lines 1268-1338): `.ax-button`, `.ax-button-primary`, `.ax-button-secondary`, `.ax-button-ghost`, `.ax-button-danger`, `.ax-button-sm`, `.ax-button[aria-disabled]`, `.ax-button:disabled`
      - The focus-visible block (search for `focus-visible` to find the authoritative Phase-188 consolidated block at ~lines 2898-2917)
      - The field-control block (~lines 1740-1790): `.ax-field-label`, `.ax-field-control`, `.ax-field-control:focus-visible`, `.ax-field-control-error`, `.ax-field-help`, `.ax-field-error`
      - Any stray `outline: none` rules on `.ax-input:focus-visible`, `.ax-select:focus-visible`, `.ax-checkbox:focus-visible` (search: `grep -n "outline: none" accrue_admin/assets/css/app.css`)
    - accrue_admin/assets/css/theme.css — search for the disabled and readonly token definitions: `grep -n "ax-disabled\|ax-readonly\|ax-type-label-font\|ax-type-body-sm-font" accrue_admin/assets/css/theme.css | head -20`
    - 189-RESEARCH.md "Per-Family Root-Fix Landmines — Button family" and "Input / Textarea / Checkbox / Select / Form-field families" — the complete list of defects and their remediation
    - 189-UI-SPEC.md "Focus ring contract" and "Component-state semantic colors" — authoritative token names
  </read_first>
  <action>
    Apply these 8 surgical fixes to app.css (edit in place — do not rewrite the file, make targeted changes):

    FIX 1 — Button type tokens: In `.ax-button` selector, replace the `ax-type-exception` comment and raw `font-size: 0.875rem; font-weight: 600; line-height: var(--ax-leading-normal);` with `font: var(--ax-type-label-font); letter-spacing: var(--ax-type-label-tracking);`. Same for `.ax-button-sm` (use `font: var(--ax-type-label-sm-font); letter-spacing: var(--ax-type-label-sm-tracking);`).

    FIX 2 — Button disabled tokens: In `.ax-button[aria-disabled="true"], .ax-button:disabled`, replace `opacity: 0.5` with `opacity: var(--ax-disabled-opacity)`. Change or remove `pointer-events: none` — if it stays for click-blocking, add `cursor: var(--ax-disabled-cursor)` BEFORE the `pointer-events: none` rule so the cursor is applied before it is blocked. Add `background: var(--ax-disabled-bg); color: var(--ax-disabled-text); border-color: var(--ax-disabled-border);` to fully consume the Phase-188 disabled tokens.

    FIX 3 — Field label type tokens: In `.ax-field-label`, replace `ax-type-exception` comment + raw font properties with `font: var(--ax-type-label-font); letter-spacing: var(--ax-type-label-tracking);`.

    FIX 4 — Field help/error type tokens: In `.ax-field-help`, replace raw font properties with `font: var(--ax-type-body-sm-font);`. In `.ax-field-error`, replace raw font properties with `font: var(--ax-type-body-sm-font);`.

    FIX 5 — Field error token: In `.ax-field-control-error`, change `border-color: var(--ax-warning)` to `border-color: var(--ax-status-danger-border)`. In `.ax-field-error`, change `color: var(--ax-warning)` to `color: var(--ax-status-danger-text)`.

    FIX 6 — Remove stray outline:none rules: Remove or replace these stale rules that shadow the Phase-188 consolidated focus-visible block:
      - Any `.ax-input:focus-visible, .ax-select:focus-visible, .ax-checkbox:focus-visible { ... outline: none; }` block
      - `.ax-field-control:focus-visible { border-color:...; outline: none; }` — REMOVE (or change to NOT set outline: none; verify the Phase-188 consolidated block at ~lines 2898-2917 already covers `.ax-field-control:focus-visible` with the correct ring)
    If the Phase-188 consolidated block at ~line 2898 does NOT yet include `.ax-field-control:focus-visible` or `.ax-button:focus-visible`, ADD them there now.

    FIX 7 — Field control disabled/readonly: Add new rules after the `.ax-field-control` block:
      `.ax-field-control:disabled { background: var(--ax-disabled-bg); border-color: var(--ax-disabled-border); color: var(--ax-disabled-text); cursor: var(--ax-disabled-cursor); opacity: var(--ax-disabled-opacity); }`
      `.ax-field-control[readonly] { background: var(--ax-readonly-bg); border-color: var(--ax-readonly-border); color: var(--ax-readonly-text); cursor: default; user-select: text; }`

    FIX 8 — Button mobile touch target: Add inside a `@media (max-width: 599.98px)` block (or extend the existing mobile breakpoint if one exists): `.ax-button { min-height: 2.75rem; }` (44px, meeting WCAG 2.5.5 minimum touch target).

    IMPORTANT: After each fix, ensure the shell verifier (scripts/ci/verify_package_docs.sh) would still pass:
    - No new raw font-size/font-weight/line-height literals outside ax-type-exception allowlist (FND-01)
    - No new literal z-index values (FND-02)
    - No new `outline: none` on `:focus-visible` without a replacement (the fix REMOVES these)
  </action>
  <verify>
    <automated>grep -v "^//" accrue_admin/assets/css/app.css | grep -c "outline: none" ; grep -c "ax-status-danger-border" accrue_admin/assets/css/app.css ; grep -c "ax-disabled-opacity" accrue_admin/assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "outline: none" accrue_admin/assets/css/app.css` — any remaining matches are NOT on interactive-primitive `:focus-visible` selectors (verify manually: they must be inside comment blocks or on non-focus contexts)
    - `grep -c "ax-status-danger-border" accrue_admin/assets/css/app.css` — at least 1 (error border token applied)
    - `grep -c "ax-status-danger-text" accrue_admin/assets/css/app.css` — at least 1 (error text token applied)
    - `grep -c "ax-disabled-opacity" accrue_admin/assets/css/app.css` — at least 1 (disabled opacity token applied)
    - `grep -c "ax-type-label-font" accrue_admin/assets/css/app.css` — at least 1 (button type role applied)
    - `grep -c "ax-disabled-bg" accrue_admin/assets/css/app.css` — at least 1 (disabled bg token applied on field-control:disabled)
    - `grep -c "ax-readonly-bg" accrue_admin/assets/css/app.css` — at least 1 (readonly bg token applied)
    - `bash scripts/ci/verify_package_docs.sh` exits 0 (no regressions to existing guards)
    - `cd accrue_admin && mix test` exits 0
  </acceptance_criteria>
  <done>8 CSS root defects fixed; tokens replace raw literals; outline:none removed; disabled/readonly rules added; verifier still green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix button.ex loading attr + aria-busy; fix input.ex and select.ex duplicate-error-ID defect</name>
  <files>
    accrue_admin/lib/accrue_admin/components/button.ex,
    accrue_admin/lib/accrue_admin/components/input.ex,
    accrue_admin/lib/accrue_admin/components/select.ex
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/components/button.ex — read in full; current attrs, HEEx template, all three render branches (link disabled, link, button)
    - accrue_admin/lib/accrue_admin/components/input.ex — read in full; note the defective line: `<p :for={error <- @errors} id={@id <> "-error"}` (multiple elements with same id)
    - accrue_admin/lib/accrue_admin/components/select.ex — read in full; same duplicate-id defect
    - 189-RESEARCH.md "Common Pitfalls" — Pitfall 5 (duplicate error ID) and the Pitfall 2 (pointer-events cursor)
    - 189-PATTERNS.md "button.ex — Root Fixes" section and "input.ex — Root Fixes" section — exact changes needed
  </read_first>
  <action>
    BUTTON.EX:
    Add `attr(:loading, :boolean, default: false)` declaration. In the `<button>` render branch (the non-link branch), add `aria-busy={if @loading, do: "true"}` to the `<button>` element (emit `aria-busy="true"` only when `@loading` is true; omit when false). Add `disabled={@disabled || @loading}` so a loading button is also functionally disabled. The loading visual (spinner) is provided by the lab specimen via inline content — the component only needs to set `aria-busy` correctly. No other HEEx change needed.

    INPUT.EX:
    Fix the duplicate-error-ID defect (axe-core `duplicate-id` violation): change `<p :for={error <- @errors} id={@id <> "-error"}` to use a wrapping div with a single ID:
    - Wrap all error paragraphs in `<div id={@id <> "-errors"}>` (note plural)
    - Change inner paragraphs to `<p :for={error <- @errors} class="ax-field-error"><%= error %></p>` (no per-error ID)
    - Update `described_by/3` to reference `@id <> "-errors"` (plural, matching the div wrapper): `maybe_add(errors != [] && id <> "-errors")`
    This ensures `aria-describedby` points to one element that contains all errors, which is semantically correct and avoids duplicate IDs.

    SELECT.EX:
    Apply the same duplicate-error-ID fix as input.ex: wrap errors in `<div id={@id <> "-errors"}>`, update `described_by/3` to reference `-errors` (plural).
  </action>
  <verify>
    <automated>cd accrue_admin && mix test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "aria-busy" accrue_admin/lib/accrue_admin/components/button.ex` — returns at least one match
    - `grep "loading" accrue_admin/lib/accrue_admin/components/button.ex` — returns `attr(:loading, :boolean` declaration
    - `grep "Enum.with_index\|id <> \"-errors\"" accrue_admin/lib/accrue_admin/components/input.ex` — confirms the duplicate-ID fix
    - `grep "Enum.with_index\|id <> \"-errors\"" accrue_admin/lib/accrue_admin/components/select.ex` — confirms same fix
    - `grep -c "id={@id <> \"-error\"}" accrue_admin/lib/accrue_admin/components/input.ex` returns 0 (old pattern gone)
    - `cd accrue_admin && mix test` exits 0 with no regressions
  </acceptance_criteria>
  <done>button.ex has loading/aria-busy; input.ex and select.ex have single error-wrapper ID; all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form inputs | aria-invalid and aria-describedby correctly link inputs to error messages — no XSS risk from error content (already rendered as text, not HTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
| T-189-04 | Spoofing | aria-busy on button | accept | aria-busy is a truthful state reflection; no spoofing risk in a dev surface |

Security domain: **not applicable** — dev-only component lab surface; fixes are CSS/HEEx component root changes only.
</threat_model>

<verification>
- `grep -n "outline: none" accrue_admin/assets/css/app.css` — no matches on interactive `:focus-visible` selectors
- `grep -c "ax-status-danger-border" accrue_admin/assets/css/app.css` — non-zero
- `grep -c "ax-disabled-opacity" accrue_admin/assets/css/app.css` — non-zero
- `grep "aria-busy" accrue_admin/lib/accrue_admin/components/button.ex` — match found
- `grep "id <> \"-errors\"" accrue_admin/lib/accrue_admin/components/input.ex` — match found
- `bash scripts/ci/verify_package_docs.sh` — exits 0
- `cd accrue_admin && mix test` — exits 0
</verification>

<success_criteria>
- All `ax-type-exception` raw font-property blocks in button and field selectors replaced with composed role tokens (CMP-01, FND-01)
- `outline: none` on `:focus-visible` eliminated; full focus-ring contract active (CMP-03)
- Error state uses `--ax-status-danger-border` / `--ax-status-danger-text` (not `--ax-warning`) (CMP-04)
- Disabled and readonly field states consume Phase-188 semantic tokens (CMP-04)
- button.ex emits `aria-busy` for loading state (CMP-03)
- input.ex and select.ex have unique error IDs (CMP-03, axe-core duplicate-id)
- All fixes at component root only, no per-page patches (CMP-05)
</success_criteria>

<output>
Create `.planning/phases/189-primitive-form-components-component-lab/189-04-SUMMARY.md` when done
</output>

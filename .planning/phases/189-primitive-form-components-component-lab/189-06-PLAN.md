---
phase: 189-primitive-form-components-component-lab
plan: 06
type: execute
wave: 3
depends_on: ["189-03", "189-04", "189-05"]
files_modified:
  - accrue_admin/e2e/admin-a11y.spec.js
  - accrue_admin/e2e/admin-interactions.spec.js
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04]

must_haves:
  truths:
    - "admin-a11y.spec.js includes /billing/dev/components in its surfaces list so both themes are axe-swept"
    - "admin-interactions.spec.js has a component-kitchen probe block with focusRingProbe, themeColumnDeltaProbe, overflowProbe, cursorProbe, and disabledAffordanceProbe helpers"
    - "themeColumnDeltaProbe asserts lightBase !== darkBase (D-07 resolved-color delta, not just class presence)"
    - "All probe results are recorded to NDJSON using the frozen p187__{surface}__{mode}__{theme}__{state}__{dXX} cell-id grammar"
    - "Phase-187 vocabulary is used for NDJSON state field (disabled-readonly, interactive-open, default-populated) not Phase-189 matrix vocabulary"
    - "npm run e2e -- e2e/admin-a11y.spec.js runs without error on the kitchen route"
    - "npm run e2e -- e2e/admin-interactions.spec.js runs without error and produces NDJSON observations"
  artifacts:
    - path: "accrue_admin/e2e/admin-a11y.spec.js"
      provides: "component-kitchen added to surfaces list; both themes swept"
      contains: "component-kitchen"
    - path: "accrue_admin/e2e/admin-interactions.spec.js"
      provides: "focusRingProbe, themeColumnDeltaProbe, overflowProbe, cursorProbe, disabledAffordanceProbe helpers + kitchen probe block"
      contains: "themeColumnDeltaProbe"
  key_links:
    - from: "accrue_admin/e2e/admin-interactions.spec.js"
      to: "accrue_admin/e2e/baseline-manifest.js"
      via: "frozen p187__ cell-id grammar; COMPONENT_STATES taxonomy"
      pattern: "p187__"
    - from: "accrue_admin/e2e/admin-a11y.spec.js"
      to: "/billing/dev/components"
      via: "axe sweep added for kitchen route in surfaces list"
      pattern: "component-kitchen"
---

<objective>
Extend the verification harness with the three deterministic layers required by D-10:

Layer 1 (admin-a11y.spec.js): Add `/billing/dev/components` to the axe-core sweep surfaces so both light and dark themes are scanned with `wcag2a`/`wcag2aa` (including `color-contrast`) against settled colors.

Layer 2 (admin-interactions.spec.js): Add a component-kitchen probe block with five new helper functions:
- `focusRingProbe` — `getComputedStyle` on focused interactive primitives; asserts `outlineWidth >= 2px` and `outlineOffset >= 2px`
- `themeColumnDeltaProbe` — asserts `--ax-base` differs between `.ax-dev-state-grid-col[data-theme="light"]` and `[data-theme="dark"]` (the D-07 resolved-color delta)
- `overflowProbe` — asserts `scrollWidth <= clientWidth` on overflow specimens (CMP-02)
- `cursorProbe` — asserts `getComputedStyle(el).cursor !== "pointer"` on non-interactive primitives (StatusBadge, EmptyState hero container)
- `disabledAffordanceProbe` — asserts disabled form controls have background resolving to `--ax-disabled-bg` value

Layer 3 (score-visuals.mjs): The kitchen route must be included in the PNG capture. This plan verifies that `npm run e2e:visuals:png-only` (or equivalent) covers `/billing/dev/components` — if it does not, add it. Score-visuals.mjs is not modified structurally; only ensuring the kitchen route is in scope.

All probe results are written to the NDJSON ledger using the frozen `p187__{surface}__{mode}__{theme}__{state}__{dXX}` cell-id grammar. Phase-189 matrix vocabulary must be mapped to Phase-187 COMPONENT_STATES (e.g., `disabled` → `disabled-readonly`, `hover`/`focus`/`active` → `interactive-open`).

Purpose: CMP-01 (lab proven by axe sweep + computed-style probes), CMP-02 (overflow probe), CMP-03 (focus ring, cursor, accessible name via axe), CMP-04 (disabled affordance probe, color-contrast via axe).

Output: Extended admin-a11y.spec.js and admin-interactions.spec.js — both runnable against the kitchen route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-primitive-form-components-component-lab/189-CONTEXT.md
@.planning/phases/189-primitive-form-components-component-lab/189-RESEARCH.md
@.planning/phases/189-primitive-form-components-component-lab/189-PATTERNS.md
@.planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md

<interfaces>
<!-- Existing e2e patterns the executor must follow exactly. -->

From accrue_admin/e2e/admin-a11y.spec.js — scan() helper and surfaces loop:
```js
async function scan(page, theme) {
  await page.evaluate((t) => document.documentElement.setAttribute("data-theme", t), theme);
  await page.waitForTimeout(50);
  const results = await new AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa"]).analyze();
  return results.violations.filter((v) => v.impact === "critical" || v.impact === "serious");
}
// surfaces array has tuples [name, path] — ADD:
// ["component-kitchen", "/billing/dev/components"]
```

From accrue_admin/e2e/admin-interactions.spec.js — makeRecorder + observe pattern:
```js
function makeRecorder(projectName) {
  const rows = [];
  let sequence = 0;
  function observe(row) {
    sequence += 1;
    const complete = {
      probe_id: row.probe_id || `ixn-${String(sequence).padStart(3, "0")}`,
      cell_id: row.cell_id || `p187__${slug(row.surface||row.interaction_class)}__${projectName}__...`,
      // ... all OBSERVATION_FIELDS
    };
    rows.push(complete);
    return complete;
  }
  function write() { /* writes NDJSON */ }
  return { observe, write, rows };
}
```

Cell-id grammar (frozen — do not change):
```
p187__{surface}__{mode}__{theme}__{state}__{dXX}
Phase-189 vocabulary → Phase-187 COMPONENT_STATES mapping:
  default → default-populated
  disabled → disabled-readonly
  hover/focus/active/pressed → interactive-open
  error → error
  overflow → overflow
  loading → loading
  empty → default-populated (empty-state renders in default context)
```

OBSERVATION_FIELDS (from admin-interactions.spec.js lines 65-92):
```js
const OBSERVATION_FIELDS = [
  "probe_id", "interaction_class", "cell_id", "surface", "surface_type",
  "state", "rubric_dimension", "overlay_tags", "coverage_status",
  "target_selector", "expected", "actual", "assertions",
  "evidence_refs", "failure_kind", "notes"
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend admin-a11y.spec.js with component-kitchen route and score-visuals coverage check</name>
  <files>accrue_admin/e2e/admin-a11y.spec.js</files>
  <read_first>
    - accrue_admin/e2e/admin-a11y.spec.js — read in full (all ~93 lines); note the exact surfaces array format, the `scan()` helper, the login call, and the `expect(page.locator("#main-content")).toBeVisible()` wait pattern
    - accrue_admin/e2e/score-visuals.mjs — read the first 50 lines to understand how PNG targets are defined; check if `/billing/dev/components` is already in the target list
    - 189-RESEARCH.md "Verification Harness Mechanics — Layer 1: admin-a11y.spec.js extension" — the exact surface name and path to add
    - 189-PATTERNS.md "admin-a11y.spec.js — Kitchen Route Extension" — the exact code pattern for the surfaces array
  </read_first>
  <action>
    ADMIN-A11Y.SPEC.JS:
    Add one entry to the `surfaces` array: `["component-kitchen", "/billing/dev/components"]`. This matches the existing tuple format `[name, path]` used throughout the file.

    The existing `scan()` helper already handles both themes (it runs twice — once for `"light"` and once for `"dark"` — in the calling loop). No change to `scan()` is needed. The kitchen route will be swept in both themes automatically by the existing loop.

    The existing `login(page, path)` call and `#main-content` wait already work for the kitchen route (it renders inside the standard AccrueAdmin AppShell layout which includes `id="main-content"`). No structural change to the test framework needed.

    Cell-id grammar for axe results from the kitchen route:
    - `p187__component-kitchen__chromium-desktop__light__default-populated__d06` (contrast dimension)
    - `p187__component-kitchen__chromium-desktop__dark__default-populated__d06`
    These are observation records, not hardcoded in the a11y spec — the a11y spec reports violations; Phase 192 uses the report. No cell-id change needed in admin-a11y.spec.js itself.

    SCORE-VISUALS.MJS check:
    Read the PNG target list. If `/billing/dev/components` is NOT in it, add it to the target array using the same format as other routes. If it IS already there, no change needed. Log this finding in the task output.
  </action>
  <verify>
    <automated>grep -c "component-kitchen" accrue_admin/e2e/admin-a11y.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "component-kitchen" accrue_admin/e2e/admin-a11y.spec.js` — returns 1
    - `grep "dev/components" accrue_admin/e2e/admin-a11y.spec.js` — finds the entry
    - The spec file is syntactically valid JavaScript: `node --check accrue_admin/e2e/admin-a11y.spec.js` exits 0
    - `grep "dev/components\|component-kitchen" accrue_admin/e2e/score-visuals.mjs` — confirms kitchen route is covered (or is already present)
  </acceptance_criteria>
  <done>admin-a11y.spec.js covers the component-kitchen route in both themes; score-visuals kitchen coverage confirmed or added.</done>
</task>

<task type="auto">
  <name>Task 2: Add component-kitchen probe block to admin-interactions.spec.js</name>
  <files>accrue_admin/e2e/admin-interactions.spec.js</files>
  <read_first>
    - accrue_admin/e2e/admin-interactions.spec.js — read the FULL file to understand: the makeRecorder factory, observe helper, all OBSERVATION_FIELDS, the existing probe helpers (scrollProbe, focusCycleProbe, topElementAt, clickOrObserve), and the test.describe block structure; find where new helpers should be added (AFTER existing helpers, BEFORE test.describe)
    - accrue_admin/e2e/baseline-manifest.js — read to get the exact COMPONENT_STATES taxonomy values, PROJECTS format (1440/390), and any existing component surface slugs
    - 189-RESEARCH.md "Verification Harness Mechanics — Layer 2" — the 5 new probe functions with exact field names
    - 189-PATTERNS.md "admin-interactions.spec.js — Component State Probes" — the exact focusRingProbe and themeColumnDeltaProbe implementations
    - 189-VALIDATION.md "Per-Requirement Verification Map" — all 7 probes mapped to CMP requirements
  </read_first>
  <action>
    Add a new section to admin-interactions.spec.js after all existing probe helpers and before the existing `test.describe` block. The section adds:

    1. FIVE NEW PROBE HELPERS following the exact `makeRecorder`/`observe` pattern. Copy all field names from OBSERVATION_FIELDS exactly. Each helper writes one or more observation rows per call:

    `focusRingProbe(page, selector, recorder, surface, state)` — focuses the element at `selector`, reads `getComputedStyle`: `outlineWidth`, `outlineOffset`, `boxShadow`, `cursor`; records one observation row with `interaction_class: "focus-ring"`, `rubric_dimension: "focus-semantics"`, `coverage_status: parseFloat(outlineWidth) >= 2 && parseFloat(outlineOffset) >= 2 ? "covered" : "gap"`, `failure_kind: parseFloat(outlineWidth) < 2 ? "focus-ring-missing" : null`, cell_id using `interactive-open` Phase-187 state, `d07` dimension.

    `themeColumnDeltaProbe(page, recorder)` — evaluates `getComputedStyle` on both `.ax-dev-state-grid-col[data-theme="light"]` and `[data-theme="dark"]`, reads `--ax-base` computed value from each; records observation with `interaction_class: "theme-column-delta"`, `rubric_dimension: "color-theme"`, `coverage_status: lightBase && darkBase && lightBase !== darkBase ? "covered" : "gap"`, `failure_kind: lightBase === darkBase ? "theme-column-inert" : null`, cell_id `p187__component-kitchen__${projectName}__light__default-populated__d01`. This is the D-07 resolution assertion.

    `overflowProbe(page, selector, recorder, surface)` — evaluates element at `selector` for `scrollWidth`, `clientWidth`; records with `interaction_class: "overflow-clip"`, `rubric_dimension: "responsive-mobile-first"`, `coverage_status: scrollWidth <= clientWidth ? "covered" : "gap"`, `failure_kind: scrollWidth > clientWidth ? "content-overflow-escape" : null`, cell_id using `overflow` Phase-187 state, `d05` dimension.

    `cursorProbe(page, selector, recorder, surface)` — reads `getComputedStyle(el).cursor`; records with `interaction_class: "cursor-affordance"`, `rubric_dimension: "interaction-integrity"`, `expected: "cursor !== pointer (non-interactive element)"`, `coverage_status: cursor !== "pointer" ? "covered" : "gap"`, `failure_kind: cursor === "pointer" ? "misleading-cursor" : null`, cell_id using `default-populated` state, `d08` dimension.

    `disabledAffordanceProbe(page, selector, recorder, surface)` — reads `getComputedStyle(el)` for `backgroundColor`, `cursor`, `opacity`; records with `interaction_class: "disabled-affordance"`, `rubric_dimension: "color-theme"`, `expected: "background resolves to --ax-disabled-bg; cursor is not-allowed or default"`, `coverage_status`, cell_id using `disabled-readonly` state, `d04` dimension.

    2. ONE NEW TEST BLOCK: `test.describe("Phase 189: component-kitchen probes", () => { ... })` containing:
    - `beforeEach`: navigate to `/billing/dev/components`; wait for `#main-content` visible
    - `test("theme column delta: light and dark columns resolve different --ax-base")`: create recorder; call `themeColumnDeltaProbe(page, recorder)`; call `recorder.write()`; the observation `coverage_status` must be `"covered"` — assert this (if `"gap"`, the test fails, proving D-07 is broken)
    - `test("focus ring: interactive primitives have outline >= 2px on :focus-visible")`: create recorder; call `focusRingProbe` for each of these selectors on the light column: `.ax-dev-state-grid-col[data-theme="light"] .ax-button`, `.ax-dev-state-grid-col[data-theme="light"] .ax-field-control`; call `recorder.write()`; assert observations with `"focus-ring-missing"` failure_kind do not exist in the rows
    - `test("overflow probe: overflow specimens do not escape their bounding box")`: create recorder; call `overflowProbe` for `.ax-dev-state-cell[data-ax-state="overflow"] .ax-field-control`; call `recorder.write()`; no `"content-overflow-escape"` failures
    - `test("cursor probe: non-interactive primitives have no cursor:pointer")`: create recorder; call `cursorProbe` for `.ax-dev-state-cell .ax-status-badge` and `.ax-empty` (empty state hero container); call `recorder.write()`; no `"misleading-cursor"` failures
    - `test("disabled affordance: disabled controls have correct token-resolved backgrounds")`: create recorder; call `disabledAffordanceProbe` for `.ax-dev-state-cell[data-ax-state="disabled"] .ax-button`; call `recorder.write()`

    3. CELL-ID VOCABULARY COMMENT: Add a comment block above the new test describe block explaining the Phase-187 state vocabulary mapping (disabled→disabled-readonly, hover/focus/active→interactive-open) so future maintainers do not introduce vocabulary drift.

    For selector specificity: the probes target elements INSIDE the `.ax-dev-state-grid-col[data-theme="light"]` column first (then repeat for dark where appropriate). Since the kitchen page renders both columns simultaneously, a single page navigation covers both themes in the DOM.

    NDJSON write target: `test-results/admin-interactions/${projectName}/observations.ndjson` — follow the exact `makeRecorder.write()` implementation already in the file.
  </action>
  <verify>
    <automated>node --check accrue_admin/e2e/admin-interactions.spec.js && grep -c "themeColumnDeltaProbe\|focusRingProbe\|overflowProbe\|cursorProbe\|disabledAffordanceProbe" accrue_admin/e2e/admin-interactions.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check accrue_admin/e2e/admin-interactions.spec.js` exits 0 (valid JavaScript)
    - `grep -c "themeColumnDeltaProbe" accrue_admin/e2e/admin-interactions.spec.js` — at least 2 (definition + call)
    - `grep -c "focusRingProbe" accrue_admin/e2e/admin-interactions.spec.js` — at least 2
    - `grep -c "overflowProbe" accrue_admin/e2e/admin-interactions.spec.js` — at least 2
    - `grep -c "cursorProbe" accrue_admin/e2e/admin-interactions.spec.js` — at least 2
    - `grep -c "p187__component-kitchen" accrue_admin/e2e/admin-interactions.spec.js` — at least 1 (frozen cell-id grammar used)
    - `grep "theme-column-inert\|focus-ring-missing\|content-overflow-escape\|misleading-cursor" accrue_admin/e2e/admin-interactions.spec.js` — all four failure_kind values are defined (they appear in failure_kind assignments)
    - `grep "disabled-readonly\|interactive-open\|default-populated" accrue_admin/e2e/admin-interactions.spec.js` — Phase-187 vocabulary used in cell_id grammar (not Phase-189 matrix vocabulary)
    - No `toHaveScreenshot` calls (D-13 prohibition on visual-regression snapshots)
    - `cd accrue_admin && mix test` exits 0 (no Elixir regressions)
  </acceptance_criteria>
  <done>admin-interactions.spec.js has 5 new probe helpers and a component-kitchen test block; all probes write to frozen p187__ NDJSON grammar; JavaScript syntax valid; no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e test access | Playwright tests authenticate as `admin` token against the dev server; no production access |
| NDJSON output | Test results written to test-results/ directory (not committed as planning artifacts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
| T-189-06 | Information Disclosure | NDJSON observation output | accept | test-results/ is gitignored; observations contain no user PII or secrets |

Security domain: **not applicable** — dev-only surface; e2e tests run in test environment only.
</threat_model>

<verification>
- `grep -c "component-kitchen" accrue_admin/e2e/admin-a11y.spec.js` — 1
- `grep -c "themeColumnDeltaProbe" accrue_admin/e2e/admin-interactions.spec.js` — 2+
- `node --check accrue_admin/e2e/admin-a11y.spec.js` — exits 0
- `node --check accrue_admin/e2e/admin-interactions.spec.js` — exits 0
- `grep "p187__component-kitchen" accrue_admin/e2e/admin-interactions.spec.js` — match found
- `grep "toHaveScreenshot" accrue_admin/e2e/admin-interactions.spec.js` — 0 matches (D-13)
</verification>

<success_criteria>
- Layer 1 verified: admin-a11y.spec.js sweeps the kitchen route in both themes with wcag2a/wcag2aa (CMP-01, CMP-03, CMP-04)
- Layer 2 verified: admin-interactions.spec.js has focusRingProbe asserting outline >= 2px (CMP-03), themeColumnDeltaProbe asserting resolved-color delta (D-07, CMP-01), overflowProbe asserting scrollWidth <= clientWidth (CMP-02), cursorProbe asserting no pointer on static elements (CMP-03), disabledAffordanceProbe (CMP-04)
- All results write to frozen p187__ NDJSON cell-id grammar (D-12)
- No visual-regression snapshot tooling introduced (D-13)
</success_criteria>

<output>
Create `.planning/phases/189-primitive-form-components-component-lab/189-06-SUMMARY.md` when done
</output>

---
phase: 189-primitive-form-components-component-lab
plan: 07
type: execute
wave: 5
depends_on: ["189-06"]
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - .planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md
autonomous: false
requirements: [CMP-05]

must_haves:
  truths:
    - "verify_package_docs.sh contains a CMP-05 guard block checking for per-page CSS overrides of primitive ax-* classes"
    - "verify_package_docs.sh contains a CMP-05 guard block checking for raw inline style= on primitive component wrappers"
    - "bash scripts/ci/verify_package_docs.sh exits 0 on the clean codebase"
    - "package_docs_verifier_test.exs contains a negative fixture that injects a page-override CSS file and asserts the verifier exits 1 with CMP-05 in output"
    - "package_docs_verifier_test.exs contains a negative fixture that injects an inline style= on a primitive and asserts the verifier exits 1 with CMP-05 in output"
    - "mix test test/accrue/docs/package_docs_verifier_test.exs exits 0 with all tests passing"
    - "189-VALIDATION.md is closed out: nyquist_compliant: true, wave_0_complete: true, all sign-off items checked, Approval date set"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "CMP-05 shell guard: per-page CSS override detection + inline style detection"
      contains: "CMP-05"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Two new negative-fixture tests for CMP-05 violations"
      contains: "per-page CSS overrides"
    - path: ".planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md"
      provides: "Phase 189 Nyquist contract closed out"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "accrue_admin/assets/css/"
      via: "find *.css ! -name app.css ! -name theme.css for per-page override detection"
      pattern: "primitive_override_hit"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "run_verifier/1 calling the shell script against tmp_dir"
      pattern: "run_verifier"
---

<objective>
Implement the CMP-05 root-fix enforcement guard (D-14/D-15): add a verifier block to `verify_package_docs.sh` that detects per-page CSS overrides of primitive `ax-*` classes and raw inline `style=` attributes on primitive component wrappers, plus two corresponding negative-fixture ExUnit tests in `package_docs_verifier_test.exs`.

Then close out the Phase 189 Nyquist contract: update 189-VALIDATION.md to reflect completion of all wave items.

This completes the established verifier ↔ negative-fixture coupling pattern from Phase 188 (where FND-01 raw-type and FND-02 z-index guards were added with the same structure). The CMP-05 guard ensures future contributors cannot accidentally introduce per-page overrides of primitive component styles — it is the mechanical enforcement of D-14.

After this plan, the phase is complete: every component is shown in the lab (Plan 03), every defect is fixed at the root (Plans 04–05), the verification harness proves it (Plan 06), and the enforcement guard prevents future regressions (Plan 07).

Purpose: CMP-05 ("component-level visual/brand fixes made at the component root so they propagate to every consuming page — no per-page patching").

Output: Updated verify_package_docs.sh, package_docs_verifier_test.exs, and 189-VALIDATION.md — guard passes on clean tree, fails with clear CMP-05 message on violation; maintainer screenshot checkpoint at phase boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-primitive-form-components-component-lab/189-CONTEXT.md
@.planning/phases/189-primitive-form-components-component-lab/189-RESEARCH.md
@.planning/phases/189-primitive-form-components-component-lab/189-PATTERNS.md
@.planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md

<interfaces>
<!-- Existing guard and test patterns to follow exactly. -->

From scripts/ci/verify_package_docs.sh — FND-02 z-index guard (existing analog to copy):
```bash
z_index_hit=$(
  perl -0ne '
    s{/\*.*?\*/}{}gs;
    while (/z-index\s*:\s*(-?[0-9]+)/gi) {
      my $value = $1;
      next if $value eq "-1" || $value eq "0" || $value eq "1";
      print "$value\n";
      last;
    }
  ' "$app_css"
)
[[ -z "$z_index_hit" ]] || fail "$app_css must not contain z-index literals outside micro-stacking exceptions (FND-02 z-index literals)"
```

From accrue/test/accrue/docs/package_docs_verifier_test.exs — z-index negative fixture (exact analog):
```elixir
test "package docs verifier rejects z-index literals in package CSS" do
  tmp_dir = tmp_dir!()
  seed_tmp_dir!(tmp_dir)

  app_css_path = Path.join(tmp_dir, "accrue_admin/assets/css/app.css")
  File.write!(app_css_path, File.read!(app_css_path) <> "\n.ax-drift { z-index: 999; }\n")

  {output, status} = run_verifier(tmp_dir)

  assert status != 0
  assert output =~ "[verify_package_docs]"
  assert output =~ "z-index literals"
end
```

CMP-05 guard additions (from 189-RESEARCH.md "CMP-05 Guard" section):
```bash
# Phase 189 CMP-05: no per-page CSS overrides of primitive ax-* classes.
primitive_override_hit=$(
  find "$ROOT_DIR/accrue_admin/assets/css" -name "*.css" \
    ! -name "app.css" ! -name "theme.css" -print0 |
    xargs -0 grep -E '\.ax-(button|field|input|select|status-badge|icon|money|json|empty)[^{]*\{' 2>/dev/null |
    head -n 1
)
[[ -z "$primitive_override_hit" ]] || fail "per-page CSS overrides of primitive ax-* classes are not allowed (CMP-05): $primitive_override_hit"

# Phase 189 CMP-05: no raw inline style= on primitive component wrappers.
inline_style_hit=$(
  find "$ROOT_DIR/accrue_admin/lib" -type f \( -name '*.ex' -o -name '*.heex' \) -print0 |
    xargs -0 perl -0ne '
      while (/~H"""(.*?)"""/sg) {
        my $tmpl = $1;
        while ($tmpl =~ /<[a-z][^>]*class="[^"]*\b(ax-button|ax-field|ax-input|ax-select|ax-status-badge|ax-money|ax-json)\b[^"]*"[^>]*style=/g) {
          print "$ARGV: $1\n";
          last;
        }
      }
    ' 2>/dev/null |
    head -n 1
)
[[ -z "$inline_style_hit" ]] || fail "raw inline style= on primitive ax-* elements is not allowed (CMP-05): $inline_style_hit"
```

NOTE on inline_id.ex: InlineId component uses `style={"max-width: #{@max_width};"}` — this is structural (a computed prop constraint) not a design override. The CMP-05 inline-style guard targets elements that have BOTH an ax-primitive class AND a `style=` attribute in the same element tag. Since `inline_id.ex` uses `class="ax-inline-id"` (not one of the guarded classes: ax-button, ax-field, ax-input, ax-select, ax-status-badge, ax-money, ax-json), it will NOT be flagged. The whitelist is specific.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CMP-05 guard blocks to verify_package_docs.sh</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh — read the FULL file to understand: the `fail()` function, the `ROOT_DIR` variable setup, the `app_css` and `theme_css` variables, and the location of the final `echo "[verify_package_docs] All checks passed."` line (the new guard must be inserted BEFORE this final echo, after the last existing guard block)
    - Find the last guard block in the file: `grep -n "fail\|echo.*passed" scripts/ci/verify_package_docs.sh | tail -10` — insert new guards just before the final echo
    - 189-RESEARCH.md "CMP-05 Guard — Shell verifier approach" — the exact bash code to add
    - 189-PATTERNS.md "verify_package_docs.sh — CMP-05 Guard" section — the verbatim guard blocks (copy exactly, including the guard comment header "Phase 189 CMP-05: ...")
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — read lines 623-670 to understand what `seed_tmp_dir!` already copies; confirm `accrue_admin/assets/css/` and `accrue_admin/lib/` are already seeded (the guard scans these paths — they must be in the tmp_dir fixture)
  </read_first>
  <action>
    Add TWO new guard blocks to verify_package_docs.sh, inserted just before the final `echo "[verify_package_docs] All checks passed."` line:

    GUARD BLOCK 1 — Per-page CSS overrides:
    A comment header "# Phase 189 CMP-05: no per-page CSS overrides of primitive ax-* classes." followed by a `primitive_override_hit=$(...)` variable that uses `find "$ROOT_DIR/accrue_admin/assets/css" -name "*.css" ! -name "app.css" ! -name "theme.css" -print0 | xargs -0 grep -E '\.ax-(button|field|input|select|status-badge|icon|money|json|empty)[^{]*\{' 2>/dev/null | head -n 1`, followed by `[[ -z "$primitive_override_hit" ]] || fail "per-page CSS overrides of primitive ax-* classes are not allowed (CMP-05): $primitive_override_hit"`.

    GUARD BLOCK 2 — Inline style on primitives:
    A comment header "# Phase 189 CMP-05: no raw inline style= on primitive component wrappers." followed by an `inline_style_hit=$(...)` variable using `find "$ROOT_DIR/accrue_admin/lib" -type f \( -name '*.ex' -o -name '*.heex' \) -print0 | xargs -0 perl -0ne '...'` (the perl one-liner from PATTERNS.md that scans `~H"""` heredoc templates for elements with both an ax-primitive class AND a `style=` attribute — copy the perl pattern verbatim from PATTERNS.md), followed by `[[ -z "$inline_style_hit" ]] || fail "raw inline style= on primitive ax-* elements is not allowed (CMP-05): $inline_style_hit"`.

    The `find ... -print0 | xargs -0` pattern handles filenames with spaces. The `2>/dev/null` suppresses errors if no CSS files other than app.css/theme.css exist (which is normal for a clean tree).

    After adding, run `bash scripts/ci/verify_package_docs.sh` against the current codebase and confirm it exits 0 — the clean codebase must not have any violations.

    NOTE: If the `xargs -0` pattern fails on macOS (Darwin 25.5.0), use `find ... -exec grep ... {} +` instead. The executor should verify the shell syntax works on the current platform.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_package_docs.sh` exits 0 on the clean codebase (no false positives)
    - `grep -c "CMP-05" scripts/ci/verify_package_docs.sh` — at least 2 (two CMP-05 guard blocks)
    - `grep -c "primitive_override_hit" scripts/ci/verify_package_docs.sh` — at least 1
    - `grep -c "inline_style_hit" scripts/ci/verify_package_docs.sh` — at least 1
    - `grep "ax-button\|ax-field\|ax-input\|ax-status-badge" scripts/ci/verify_package_docs.sh` — the primitive class list is in the grep pattern
    - The final `echo "[verify_package_docs] All checks passed."` still exists and is the last output line
  </acceptance_criteria>
  <done>CMP-05 guard blocks added to verify_package_docs.sh; script exits 0 on clean codebase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add negative-fixture tests for CMP-05 in package_docs_verifier_test.exs; close out 189-VALIDATION.md</name>
  <files>
    accrue/test/accrue/docs/package_docs_verifier_test.exs,
    .planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md
  </files>
  <read_first>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — read the FULL file to understand: the `tmp_dir!()`, `seed_tmp_dir!()`, and `run_verifier()` helpers; the `copy_fixture!()` calls in `seed_tmp_dir!`; the exact paths seeded into tmp_dir (must include accrue_admin/assets/css/app.css and the directory structure the CMP-05 guard scans); the z-index and font-literal negative-fixture tests (lines ~457-483) as the structural analog
    - 189-PATTERNS.md "package_docs_verifier_test.exs — CMP-05 Negative Fixture" — the exact two test blocks to add
    - 189-RESEARCH.md "CMP-05 Guard — Negative fixture" section — the coupling pattern and seed_tmp_dir! requirements
    - .planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md — read in full to understand all sign-off items and the current state
  </read_first>
  <behavior>
    - Test 1 (RED): a temporary CSS file with `.ax-button { font-size: 1rem; }` injected into `accrue_admin/assets/css/page-overrides.css` → verifier must exit non-zero with "CMP-05" in output
    - Test 2 (RED): `component_kitchen_live.ex` in tmp_dir modified to include a `<button class="ax-button ax-button-primary" style="color: red;">` element → verifier must exit non-zero with "CMP-05" in output
  </behavior>
  <action>
    NEGATIVE FIXTURE TESTS:
    Add TWO new ExUnit tests at the end of the test module (before `end`) following the z-index negative fixture pattern exactly:

    TEST 1 — Per-page CSS override negative fixture:
    Copy the `tmp_dir!() / seed_tmp_dir!() / run_verifier()` pattern. After seeding, create `accrue_admin/assets/css/page-overrides.css` in the tmp_dir with content `.ax-button { font-size: 1rem; }\n`. Run verifier; assert `status != 0`, `output =~ "[verify_package_docs]"`, `output =~ "CMP-05"`.
    `seed_tmp_dir!` already copies `accrue_admin/assets/css/` — the new guard uses `find ... ! -name "app.css" ! -name "theme.css"` so the injected `page-overrides.css` will be found. No new `copy_fixture!` calls needed.

    TEST 2 — Inline style on primitive negative fixture:
    Copy the same pattern. After seeding, read `component_kitchen_live.ex` from tmp_dir, replace one occurrence of a `<Button.button ...>` call with a raw `<button class="ax-button ax-button-primary" style="color: red;" type="button">Primary action</button>` element (an inline `~H"""` heredoc substitution). Write the modified file back. Run verifier; assert `status != 0`, `output =~ "CMP-05"`.
    The key: the injected element has BOTH `class="ax-button ax-button-primary"` (matching the guard's `ax-button` pattern) AND `style=` in the same opening tag. The perl one-liner in the guard scans `~H"""` heredocs, so the element must be inside a HEEx heredoc block in the file.

    NOTE about seed_tmp_dir! coupling: if `component_kitchen_live.ex` is NOT currently seeded by `seed_tmp_dir!`, add a `copy_fixture!()` call for it. But per 189-PATTERNS.md, `component_kitchen_live.ex` is already seeded at line 657 of the test file — verify this before adding a duplicate call.

    Name the tests descriptively:
    - "package docs verifier rejects per-page CSS overrides of primitive ax-* classes (CMP-05)"
    - "package docs verifier rejects raw inline style= on primitive ax-* elements (CMP-05)"

    VALIDATION.MD CLOSE-OUT:
    After all tests pass, update `.planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md`:
    - Set `nyquist_compliant: true` (or equivalent field name in the file)
    - Set `wave_0_complete: true` (or equivalent)
    - Check off all sign-off items (mark each checklist item as complete)
    - Set the Approval date to today's date (2026-06-17)
    - Add a "Phase 189 complete" note referencing the Plans 01–07 SUMMARY files as evidence
    The VALIDATION.md close-out is part of this task's definition of done — both the tests AND the VALIDATION.md update must be committed together.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits 0 with ALL tests passing (including the 2 new CMP-05 negative fixtures)
    - The two new test names contain "CMP-05" (grep confirmation)
    - `grep -c "CMP-05" accrue/test/accrue/docs/package_docs_verifier_test.exs` — at least 2
    - All pre-existing tests in the file still pass (no regressions — the new tests only ADD; they do not modify existing tests)
    - `cd accrue && mix test` exits 0 (full accrue package suite green)
    - `grep "nyquist_compliant.*true\|wave_0_complete.*true" .planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md` — confirms both fields are set to true
    - 189-VALIDATION.md Approval date is set (grep for today's date or a non-empty approval line)
    - All sign-off checklist items in 189-VALIDATION.md are marked complete
  </acceptance_criteria>
  <done>Two CMP-05 negative-fixture tests pass; pre-existing tests unchanged; full accrue test suite green; 189-VALIDATION.md closed out (nyquist_compliant: true, wave_0_complete: true, Approval date set, all sign-off items checked).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 189 is now complete:
    - Plan 01: CSS foundations (ax-dev-state-grid, D-07 sub-tree dark selector) + registry schema + structural tests (e)/(f)
    - Plan 02: 8 new primitive component modules (textarea, checkbox, radio, toggle, spinner, tooltip, empty_state, inline_id)
    - Plan 03: Registry-driven two-column state-matrix renderer in ComponentKitchenLive; new test (g) as mounted HTML assertion
    - Plan 04: Button/input/select/field CSS root fixes (focus ring, error tokens, disabled tokens, type exceptions)
    - Plan 05: StatusBadge status-token migration + remaining primitive CSS (inline-id, tooltip, toggle, spinner, empty-state) — serialized after Plan 04
    - Plan 06: Verification harness extensions (axe sweep on kitchen, getComputedStyle probes, NDJSON observations, D-07 definitive themeColumnDeltaProbe)
    - Plan 07: CMP-05 verifier guard + negative fixtures + VALIDATION.md close-out
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd examples/accrue_host && docker compose up -d` (or `iex -S mix phx.server` in accrue_admin)
    2. Navigate to `https://accrue.localhost/billing/dev/components` (or local dev URL)
    3. Verify the page shows the registry-driven state-matrix gallery:
       - Each of the 14 component families has a two-column grid (Light | Dark)
       - Each applicable state renders a labeled cell with the actual component specimen
       - Each n/a state renders a muted cell with a stated reason
       - The dark column visually looks dark (different background from the light column) — this proves D-07 is resolved
    4. Run: `bash scripts/ci/verify_package_docs.sh` — must exit 0
    5. Run: `cd accrue_admin && mix test` — must exit 0
    6. Run: `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` — must exit 0
    7. Visual check: Toggle the page theme (light/dark toggle in the top bar) — the ENTIRE page should switch, but each grid column maintains its own fixed theme scope
    8. Focus check: Tab into a button or input in the grid — confirm a visible focus ring appears (outline, not just a color change)
    9. Screenshot: Take a screenshot of the component kitchen page and note it for the phase boundary sign-off
    10. Confirm: `cat .planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md | grep -i "nyquist_compliant\|wave_0_complete\|approval"` — all set to true/complete with today's date
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any issues for remediation</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI guard | verify_package_docs.sh runs in CI against the committed codebase; it scans source files, not runtime data |
| tmp_dir isolation | negative-fixture tests write to system temp directories, isolated per test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
| T-189-07 | Tampering | verify_package_docs.sh | mitigate | Shell verifier runs in CI on every push; CMP-05 guard prevents per-page design drift; negative fixtures prove the guard actually works |

Security domain: **not applicable** — dev-only surface; verifier guards code quality, not security boundaries.
</threat_model>

<verification>
- `bash scripts/ci/verify_package_docs.sh` — exits 0 on clean codebase
- Inject `.ax-button { font-size: 1rem; }` in any non-app.css file → `bash scripts/ci/verify_package_docs.sh` exits non-zero with "CMP-05"
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` — exits 0 with all tests passing
- `cd accrue_admin && mix test` — exits 0
- `grep "nyquist_compliant.*true" .planning/phases/189-primitive-form-components-component-lab/189-VALIDATION.md` — match found
- Human screenshot checkpoint: /billing/dev/components page shows two-column state-matrix gallery with genuine dark column
</verification>

<success_criteria>
- CMP-05 guard in CI catches per-page overrides of primitive ax-* classes (D-14, D-15)
- CMP-05 guard catches raw inline style= on primitive wrappers (D-15)
- Two negative-fixture tests prove the guard works (established coupling pattern)
- Full test suites (accrue + accrue_admin) pass
- 189-VALIDATION.md closed out: nyquist_compliant: true, wave_0_complete: true, all sign-off items checked, Approval date set
- Human checkpoint confirms the component lab gallery is visually correct at the phase boundary
</success_criteria>

<output>
Create `.planning/phases/189-primitive-form-components-component-lab/189-07-SUMMARY.md` when done
</output>

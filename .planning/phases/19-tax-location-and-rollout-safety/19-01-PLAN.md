---
phase: 19-tax-location-and-rollout-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/processor/stripe/error_mapper.ex
  - accrue/lib/accrue/processor/fake.ex
  - accrue/test/accrue/processor/stripe_test.exs
  - accrue/test/accrue/processor/fake_test.exs
autonomous: true
requirements:
  - TAX-02
must_haves:
  truths:
    - "Stripe and Fake customer tax-location validation fail with a stable Accrue error instead of a raw provider payload."
    - "Processor-backed customer updates can request immediate tax-location validation without logging address PII."
  artifacts:
    - path: "accrue/lib/accrue/processor/stripe/error_mapper.ex"
      provides: "Stable `customer_tax_location_invalid` mapping with sanitized metadata"
    - path: "accrue/lib/accrue/processor/fake.ex"
      provides: "Deterministic invalid-location behavior for request-time and recurring-tax tests"
  key_links:
    - from: "accrue/lib/accrue/processor/stripe.ex"
      to: "accrue/lib/accrue/processor/stripe/error_mapper.ex"
      via: "customer update / subscription error translation"
      pattern: "customer_tax_location_invalid|translate_"
    - from: "accrue/lib/accrue/processor/fake.ex"
      to: "accrue/test/accrue/processor/fake_test.exs"
      via: "deterministic invalid-location helpers"
      pattern: "requires_location_inputs|customer_tax_location_invalid"
---

<objective>
Lock the processor contract for Phase 19 before the billing facade starts using it.

Purpose: TAX-02 depends on Stripe and Fake agreeing on how immediate tax-location validation fails and how that failure is exposed safely.
Output: Processor-backed customer tax-location validation, sanitized error mapping, and deterministic adapter tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-tax-location-and-rollout-safety/19-RESEARCH.md
@.planning/phases/19-tax-location-and-rollout-safety/19-PATTERNS.md
@.planning/phases/19-tax-location-and-rollout-safety/19-VALIDATION.md
@.planning/phases/18-stripe-tax-core/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stripe-side tax-location validation and safe error mapping</name>
  <read_first>
    - accrue/lib/accrue/processor/stripe.ex
    - accrue/lib/accrue/processor/stripe/error_mapper.ex
    - accrue/test/accrue/processor/stripe_test.exs
    - .planning/phases/19-tax-location-and-rollout-safety/19-RESEARCH.md
    - .planning/phases/19-tax-location-and-rollout-safety/19-PATTERNS.md
  </read_first>
  <files>accrue/lib/accrue/processor/stripe.ex, accrue/lib/accrue/processor/stripe/error_mapper.ex, accrue/test/accrue/processor/stripe_test.exs</files>
  <action>Extend the existing Stripe customer-update path so Phase 19 can send nested tax-location validation params without changing the adapter boundary: keep `Accrue.Processor.Stripe.update_customer/3` on the existing `stringify_keys(params)` path, but add focused adapter tests that lock support for `address`, `shipping`, and `tax: %{validate_location: "immediately", ip_address: ...}` fields. In `Accrue.Processor.Stripe.ErrorMapper`, add a dedicated `customer_tax_location_invalid` branch that returns a stable `%Accrue.APIError{code: "customer_tax_location_invalid"}` with actionable copy that tells callers to update customer address or shipping before enabling automatic tax. Sanitize the stored `processor_error` payload for this branch to a narrow map with request id, status, type, and code only; do not keep raw address fields, raw body text, or request params in the mapped error.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/stripe/error_mapper.ex` contains `customer_tax_location_invalid`
    - `accrue/lib/accrue/processor/stripe/error_mapper.ex` contains `update customer address or shipping`
    - `accrue/test/accrue/processor/stripe_test.exs` asserts `validate_location`
    - `accrue/test/accrue/processor/stripe_test.exs` asserts the mapped error code `customer_tax_location_invalid`
    - `cd accrue && mix test test/accrue/processor/stripe_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/stripe_test.exs</automated>
  </verify>
  <done>Stripe customer updates can request immediate tax-location validation, and invalid-location failures surface as a stable Accrue API error without raw provider payload leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Make Fake deterministic for invalid tax-location cases</name>
  <read_first>
    - accrue/lib/accrue/processor/fake.ex
    - accrue/test/accrue/processor/fake_test.exs
    - .planning/phases/19-tax-location-and-rollout-safety/19-RESEARCH.md
    - .planning/phases/19-tax-location-and-rollout-safety/19-VALIDATION.md
  </read_first>
  <files>accrue/lib/accrue/processor/fake.ex, accrue/test/accrue/processor/fake_test.exs</files>
  <action>Add deterministic Fake helpers for both invalid-location families called out in the research: request-time validation failures and recurring automatic-tax disable state. For request-time failures, make Fake return `%Accrue.APIError{code: "customer_tax_location_invalid"}` when `tax.validate_location` is `"immediately"` and the supplied tax location is insufficient by the Fake rule set. For recurring rollback, make Fake emit `automatic_tax.status`, `automatic_tax.disabled_reason`, and `last_finalization_error.code` fields needed by Phase 19 projection and webhook tests. Keep the rule set small and explicit for test determinism; do not implement a custom jurisdiction calculator or real-world tax logic.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/processor/fake.ex` contains `customer_tax_location_invalid`
    - `accrue/lib/accrue/processor/fake.ex` contains `disabled_reason`
    - `accrue/lib/accrue/processor/fake.ex` contains `finalization_requires_location_inputs`
    - `accrue/test/accrue/processor/fake_test.exs` covers invalid customer tax location and recurring disabled-reason payloads
    - `cd accrue && mix test test/accrue/processor/fake_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/fake_test.exs</automated>
  </verify>
  <done>Fake can reproduce both request-time and async invalid-location behaviors without scripted one-off Stripe payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tax-location attrs -> `Accrue.Processor.Stripe.update_customer/3` | Customer address and IP data cross into the Stripe adapter |
| raw processor error -> `Accrue.Processor.Stripe.ErrorMapper` | Provider payload crosses into Accrue's public error surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | I | `accrue/lib/accrue/processor/stripe/error_mapper.ex` | mitigate | Map `customer_tax_location_invalid` to a stable `%Accrue.APIError{}` with sanitized `processor_error`; do not retain raw address values, raw body text, or Stripe request params. |
| T-19-02 | T | `accrue/lib/accrue/processor/fake.ex` | mitigate | Keep Fake deterministic and limited to validation-state simulation only; do not invent custom tax rates or jurisdiction logic. |
| T-19-03 | R | `accrue/test/accrue/processor/stripe_test.exs` | mitigate | Assert on stable error codes and narrow metadata fields so tests fail if raw provider payloads leak into the public contract. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/processor/stripe_test.exs test/accrue/processor/fake_test.exs`.
</verification>

<success_criteria>
Processor-backed tax-location validation exists, invalid-location failures are stable and sanitized, and Fake can reproduce both failure families deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tax-location-and-rollout-safety/19-01-SUMMARY.md`
</output>

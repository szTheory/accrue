---
phase: 19-tax-location-and-rollout-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/priv/repo/migrations/20260417193000_add_tax_rollout_safety_columns.exs
  - accrue/lib/accrue/billing/subscription.ex
  - accrue/lib/accrue/billing/invoice.ex
  - accrue/lib/accrue/billing/subscription_projection.ex
  - accrue/lib/accrue/billing/invoice_projection.ex
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/test/accrue/billing/subscription_projection_tax_test.exs
  - accrue/test/accrue/billing/invoice_projection_test.exs
  - accrue/test/accrue/webhook/default_handler_test.exs
autonomous: true
requirements:
  - TAX-03
must_haves:
  truths:
    - "Recurring invalid-location rollback is visible on local subscription and invoice rows."
    - "Invoice finalization failures caused by missing tax-location inputs are reconciled locally and do not stay hidden inside Stripe."
  artifacts:
    - path: "accrue/priv/repo/migrations/20260417193000_add_tax_rollout_safety_columns.exs"
      provides: "Schema support for disabled-reason and finalization-error observability"
    - path: "accrue/lib/accrue/billing/invoice_projection.ex"
      provides: "Invoice projection of disabled-reason and finalization-error code"
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "Webhook reconciliation for `invoice.updated` and `invoice.finalization_failed`"
  key_links:
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "accrue/lib/accrue/billing/invoice_projection.ex"
      via: "canonical invoice fetch + projection"
      pattern: "invoice\\.finalization_failed|invoice\\.updated"
    - from: "accrue/lib/accrue/billing/subscription_projection.ex"
      to: "accrue/lib/accrue/billing/subscription.ex"
      via: "disabled_reason attrs"
      pattern: "automatic_tax_disabled_reason"
---

<objective>
Project and reconcile the invalid-location states that Stripe can emit after rollout.

Purpose: TAX-03 fails if invalid tax location only exists in Stripe logs or dashboard state; Accrue needs local projection and webhook reconciliation for the async failure family.
Output: Migration, additive schema fields, projection updates, webhook dispatch updates, and focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-tax-location-and-rollout-safety/19-RESEARCH.md
@.planning/phases/19-tax-location-and-rollout-safety/19-PATTERNS.md
@.planning/phases/19-tax-location-and-rollout-safety/19-VALIDATION.md
@.planning/phases/18-stripe-tax-core/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add narrow tax-rollout observability columns</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription.ex
    - accrue/lib/accrue/billing/invoice.ex
    - accrue/priv/repo/migrations/20260417180000_add_automatic_tax_columns_to_billing_tables.exs
    - .planning/phases/19-tax-location-and-rollout-safety/19-RESEARCH.md
    - .planning/phases/19-tax-location-and-rollout-safety/19-PATTERNS.md
  </read_first>
  <files>accrue/priv/repo/migrations/20260417193000_add_tax_rollout_safety_columns.exs, accrue/lib/accrue/billing/subscription.ex, accrue/lib/accrue/billing/invoice.ex</files>
  <action>Create migration `20260417193000_add_tax_rollout_safety_columns.exs` that adds `:automatic_tax_disabled_reason` to both `:accrue_subscriptions` and `:accrue_invoices`, plus `:last_finalization_error_code` to `:accrue_invoices`. Update the `Accrue.Billing.Subscription` and `Accrue.Billing.Invoice` schemas and cast fields to persist exactly those columns. Do not add raw finalization messages, raw payload blobs, or free-form provider JSON columns beyond the existing `data` field; Phase 19 only needs stable, queryable observability fields.</action>
  <acceptance_criteria>
    - `accrue/priv/repo/migrations/20260417193000_add_tax_rollout_safety_columns.exs` contains `add :automatic_tax_disabled_reason, :string`
    - `accrue/priv/repo/migrations/20260417193000_add_tax_rollout_safety_columns.exs` contains `add :last_finalization_error_code, :string`
    - `accrue/lib/accrue/billing/subscription.ex` contains `field(:automatic_tax_disabled_reason, :string)`
    - `accrue/lib/accrue/billing/invoice.ex` contains `field(:last_finalization_error_code, :string)`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs</automated>
  </verify>
  <done>Subscriptions and invoices have additive local columns for recurring invalid-location observability without persisting raw provider error payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile disabled-reason and finalization-failure state</name>
  <read_first>
    - accrue/lib/accrue/billing/subscription_projection.ex
    - accrue/lib/accrue/billing/invoice_projection.ex
    - accrue/lib/accrue/webhook/default_handler.ex
    - accrue/test/accrue/billing/subscription_projection_tax_test.exs
    - accrue/test/accrue/billing/invoice_projection_test.exs
    - accrue/test/accrue/webhook/default_handler_test.exs
    - .planning/phases/19-tax-location-and-rollout-safety/19-VALIDATION.md
  </read_first>
  <files>accrue/lib/accrue/billing/subscription_projection.ex, accrue/lib/accrue/billing/invoice_projection.ex, accrue/lib/accrue/webhook/default_handler.ex, accrue/test/accrue/billing/subscription_projection_tax_test.exs, accrue/test/accrue/billing/invoice_projection_test.exs, accrue/test/accrue/webhook/default_handler_test.exs</files>
  <action>Extend `SubscriptionProjection.automatic_tax_fields/1` to return `disabled_reason` and persist it into `automatic_tax_disabled_reason`. Extend `InvoiceProjection.decompose/1` to persist both `automatic_tax_disabled_reason` and `last_finalization_error_code` from canonical processor fields while continuing to keep the full payload in `data`. Update `Accrue.Webhook.DefaultHandler` so the invoice reducer listens to `invoice.updated` and `invoice.finalization_failed` in addition to the existing invoice events, then reconciles the canonical invoice and records the event without exposing raw provider payloads in flashes or logs. Add tests that cover string-keyed Stripe payloads, atom-keyed Fake payloads, `requires_location_inputs`, `finalization_requires_location_inputs`, and the finalization-failed webhook family.</action>
  <acceptance_criteria>
    - `accrue/lib/accrue/billing/subscription_projection.ex` contains `disabled_reason`
    - `accrue/lib/accrue/billing/invoice_projection.ex` contains `last_finalization_error_code`
    - `accrue/lib/accrue/webhook/default_handler.ex` contains `invoice.finalization_failed`
    - `accrue/lib/accrue/webhook/default_handler.ex` contains `invoice.updated`
    - `accrue/test/accrue/webhook/default_handler_test.exs` covers `finalization_requires_location_inputs`
    - `cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs test/accrue/webhook/default_handler_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs test/accrue/webhook/default_handler_test.exs</automated>
  </verify>
  <done>Recurring invalid-location rollback and invoice finalization failure states are projected locally and reconciled through the default webhook path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical Stripe/Fake payload -> projection modules | Provider billing state is reduced into local queryable columns |
| webhook event family -> default handler reducer | Stripe event types trigger local reconciliation and admin-visible state updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | I | `accrue/lib/accrue/billing/invoice_projection.ex` | mitigate | Persist only `automatic_tax_disabled_reason` and `last_finalization_error_code`; do not store raw finalization messages or raw address payloads outside `data`. |
| T-19-08 | D | `accrue/lib/accrue/webhook/default_handler.ex` | mitigate | Add `invoice.updated` and `invoice.finalization_failed` to the explicit reducer list so recurring invalid-location failures are reconciled instead of silently ignored. |
| T-19-09 | T | migration/schema pair | mitigate | Keep new columns additive and string-only so existing rows remain readable and rollback-safe for non-tax hosts. |
</threat_model>

<verification>
Run `cd accrue && mix test test/accrue/billing/invoice_projection_test.exs test/accrue/billing/subscription_projection_tax_test.exs test/accrue/webhook/default_handler_test.exs`.
</verification>

<success_criteria>
Subscriptions and invoices expose disabled-reason / finalization-error state locally, and the default webhook path reconciles the async invalid-location failure family.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tax-location-and-rollout-safety/19-03-SUMMARY.md`
</output>

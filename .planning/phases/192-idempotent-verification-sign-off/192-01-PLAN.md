---
phase: 192-idempotent-verification-sign-off
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_phase192_scorecard.mjs
  - scripts/ci/verify_phase192_signoff.mjs
autonomous: true
requirements:
  - VER-02
  - VER-04
must_haves:
  truths:
    - "Phase 192 has fail-closed verifiers for the canonical structured scorecard artifacts required by D-04 through D-19."
    - "A missing, malformed, downgraded, evidence-free, or markdown-only scorecard package exits non-zero before sign-off can proceed."
    - "The maintainer sign-off surface is verified as an explicit evidence-linked accept/block artifact, not a loose screenshot note."
  artifacts:
    - path: "scripts/ci/verify_phase192_scorecard.mjs"
      provides: "Strict verifier for final.cells.json, scorecard.delta.json, regressions.ndjson, artifacts.manifest.json, and Phase 187 baseline comparison"
      exports: ["main", "verifyPhase192Scorecard"]
    - path: "scripts/ci/verify_phase192_signoff.mjs"
      provides: "Strict verifier for 192-SIGN-OFF.md sign-off completeness and evidence-linked checklist rows"
      exports: ["main", "verifyPhase192Signoff"]
  key_links:
    - from: "scripts/ci/verify_phase192_scorecard.mjs"
      to: ".planning/phases/187-audit-baseline/baseline.cells.json"
      via: "loads Phase 187 canonical baseline and compares every comparable p187__ cell"
      pattern: "baseline\\.cells\\.json"
    - from: "scripts/ci/verify_phase192_scorecard.mjs"
      to: ".planning/phases/192-idempotent-verification-sign-off/final.cells.json"
      via: "validates final cells preserve Phase 187 cell grammar and evidence refs"
      pattern: "final\\.cells\\.json"
    - from: "scripts/ci/verify_phase192_signoff.mjs"
      to: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      via: "requires executive status, curated gallery, CI guardrail status, artifact links, and explicit checklist outcome"
      pattern: "192-SIGN-OFF\\.md"
---

<objective>
Create the Phase 192 artifact contract and fail-closed verifiers for VER-02 and VER-04.

Purpose: Later Phase 192 plans need deterministic gates before they generate final scorecard and sign-off artifacts. This plan locks the contract so structured data remains canonical per D-04 and D-05, the Phase 187 cell grammar is preserved per D-09 through D-12, and maintainer sign-off cannot pass without evidence-linked accept/block rows per D-19 and D-29 through D-37.

Output:
- `scripts/ci/verify_phase192_scorecard.mjs`
- `scripts/ci/verify_phase192_signoff.mjs`
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
@.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
@.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
@.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
@.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
@.planning/phases/187-audit-baseline/baseline.cells.json
@.planning/phases/187-audit-baseline/defects.ndjson
@.planning/phases/187-audit-baseline/artifacts.manifest.json
@.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json
@.planning/phases/187-audit-baseline/schemas/defect.schema.json
@scripts/ci/verify_phase191_ax187_coverage.mjs
@accrue_admin/e2e/baseline-manifest.js
@accrue_admin/e2e/baseline-artifacts.mjs
</context>

## Artifacts this phase produces

- New file: `scripts/ci/verify_phase192_scorecard.mjs`.
- New exported function: `verifyPhase192Scorecard(options)`.
- New exported function: `main(argv)`.
- New CLI command: `node scripts/ci/verify_phase192_scorecard.mjs`.
- New CLI flag: `--manifest`, requiring manifest-only validation to be runnable independently.
- New CLI flag: `--self-test`, creating temporary positive and negative fixtures so the verifier proves fail-closed behavior before final artifacts exist.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/final.cells.json`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson`.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json`.
- New baseline input path: `.planning/phases/187-audit-baseline/baseline.cells.json`.
- New file: `scripts/ci/verify_phase192_signoff.mjs`.
- New exported function: `verifyPhase192Signoff(options)`.
- New CLI command: `node scripts/ci/verify_phase192_signoff.mjs`.
- New CLI flag: `--self-test`, creating temporary positive and negative sign-off markdown fixtures.
- New default input path: `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strict Phase 192 scorecard artifact verifier</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
    - @.planning/phases/187-audit-baseline/baseline.cells.json
    - @.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json
    - @.planning/phases/187-audit-baseline/schemas/defect.schema.json
    - @scripts/ci/verify_phase191_ax187_coverage.mjs
  </read_first>
  <files>scripts/ci/verify_phase192_scorecard.mjs</files>
  <behavior>
    - Positive case: a temp fixture with one Phase 187 baseline cell, matching final cell, one passing delta row, empty regressions NDJSON, and manifest evidence exits 0.
    - Negative case: final score below baseline exits non-zero and reports a score-downgrade section.
    - Negative case: covered baseline cell changed to gap, missing, newly unreachable, or lacking evidence exits non-zero and reports a coverage or evidence section.
    - Negative case: non-empty `regressions.ndjson` exits non-zero even when aggregate summary counts look passing.
    - Negative case: cell IDs that do not begin with `p187__`, do not match a comparable Phase 187 row, or change the `__dXX` dimension suffix fail unless recorded as an explicit baseline correction row.
    - Manifest mode: `--manifest` validates `artifacts.manifest.json` evidence refs and checksums without requiring all scorecard artifacts.
  </behavior>
  <action>Create `scripts/ci/verify_phase192_scorecard.mjs` using the Node ESM pattern from `verify_phase191_ax187_coverage.mjs`: `fs`, `path`, `fileURLToPath`, repo-root resolution, grouped failure reporting, and `process.exit(1)` on any failure. Per D-04 through D-08, treat `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json` as canonical and reject packages that rely on `192-SCORECARD.md` or aggregates to prove pass/fail. Per D-09 through D-12, load `.planning/phases/187-audit-baseline/baseline.cells.json`, build `baselineById`, require final comparable cell IDs to preserve the `p187__...__dXX` grammar and 12-dimension range, compare every comparable row with `final_score >= baseline_score`, reject coverage downgrades from `covered`, reject newly unreachable or missing evidence rows, and allow baseline corrections only when a structured correction note names the invalid Phase 187 row and includes evidence refs. Per D-13 through D-18, require evidence refs for every final score, downgrade, correction, and regression; keep raw lens names separate in validation fields such as `correctness`, `axe`, `reduced-motion`, `component-lab`, `interaction-trace`, `visual-brand-microcopy`, or `maintainer-review`; reject rows where visual/model evidence is the sole support for accessibility, interaction integrity, focus, scroll, overlay actionability, or CI pass/fail. Implement `--manifest` to validate repo-relative artifact refs under allowed generated roots such as `accrue_admin/test-results/`, `accrue_admin/playwright-report/`, and `.planning/phases/192-idempotent-verification-sign-off/`, with SHA-256-looking checksums where checksums are present. Implement `--self-test` with temporary fixtures that prove the positive and negative behaviors above. Do not add dependencies, do not mutate Phase 187 artifacts, and do not generate final scorecard artifacts in this plan.</action>
  <acceptance_criteria>
    - `scripts/ci/verify_phase192_scorecard.mjs` exists, is ESM, and exports `verifyPhase192Scorecard` plus `main`.
    - Default execution reads `.planning/phases/192-idempotent-verification-sign-off/final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json`.
    - Default execution compares against `.planning/phases/187-audit-baseline/baseline.cells.json`.
    - The verifier fails on score downgrade, coverage downgrade, missing comparable cell, missing evidence refs, malformed JSON/NDJSON, non-empty regressions, and invalid artifact refs.
    - The verifier's pass/fail logic cites D-04 through D-19 concepts in code identifiers or error messages without making markdown the source of truth.
    - `--self-test` exits 0 only after exercising at least one passing fixture and multiple intentional failing fixtures.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_phase192_scorecard.mjs --self-test</automated>
    <automated>node --check scripts/ci/verify_phase192_scorecard.mjs</automated>
    <automated>grep -q "verifyPhase192Scorecard" scripts/ci/verify_phase192_scorecard.mjs</automated>
    <automated>grep -q "baseline.cells.json" scripts/ci/verify_phase192_scorecard.mjs</automated>
    <automated>grep -q "final.cells.json" scripts/ci/verify_phase192_scorecard.mjs</automated>
  </verify>
  <done>The scorecard verifier is executable, self-tested, and fails closed for the canonical structured artifact contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create strict Phase 192 sign-off verifier</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
    - @.planning/v1.51-MILESTONE-AUDIT.md
    - @.planning/milestones/v1.51-phases/179-f-screenshot-driven-visual-qa-loop-sign-off/SIGN-OFF.md
  </read_first>
  <files>scripts/ci/verify_phase192_signoff.mjs</files>
  <behavior>
    - Positive case: a temp `192-SIGN-OFF.md` containing executive status, baseline comparison summary, CI guardrail status, curated gallery rows, artifact manifest links, trace refs, and explicit checklist outcome exits 0.
    - Negative case: sign-off markdown without `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json` links exits non-zero.
    - Negative case: curated gallery rows missing `who`, `job`, route or surface, state, theme, viewport, evidence ref, why it matters, or accept/block status exit non-zero.
    - Negative case: an approved checklist with no explicit maintainer outcome, no evidence refs, or unchecked required review categories exits non-zero.
    - Negative case: sign-off claims screenshot approval without trace refs for focus trap, focus restore, Escape, outside click, scroll reachability, LiveView patch focus, and actionability exits non-zero.
  </behavior>
  <action>Create `scripts/ci/verify_phase192_signoff.mjs` as a Node ESM static verifier for `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`. Per D-29 through D-31, verify that sign-off is over `192-SIGN-OFF.md` and includes scorecard, curated gallery, and human checklist sections instead of raw `test-results` inspection. Per D-30, require executive pass/fail, baseline comparison summary, CI guardrail status, artifact manifest links, and maintainer checklist text. Per D-32 through D-36, require gallery rows or equivalent structured markdown entries naming `who`, `job`, route or surface, state, theme, viewport, evidence ref, why the screenshot matters, accept/block status, and trace refs for focus trap, focus restore, Escape, outside click, scroll reachability, LiveView patch focus, and actionability. Per D-37 through D-41, require checklist categories for JTBD clarity, domain vocabulary, microcopy recovery, brand fit, accessible focus/contrast, mobile usability, dark-mode role clarity, absence of backend-guts presentation, responsive layout, light/dark or system-theme behavior where supported, interaction integrity, focus/hover/disabled affordance, information hierarchy, and developer/operator DX. Implement `--self-test` with temporary markdown fixtures that prove both passing and fail-closed behavior. Do not generate `192-SIGN-OFF.md` in this plan; only create the verifier that later sign-off generation must satisfy.</action>
  <acceptance_criteria>
    - `scripts/ci/verify_phase192_signoff.mjs` exists, is ESM, and exports `verifyPhase192Signoff` plus `main`.
    - Default execution reads `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`.
    - The verifier requires links or literal references to `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json`.
    - The verifier requires CI guardrail rows for `baseline:parse`, `verify_phase191_ax187_coverage`, `e2e:group-contracts`, `e2e:phase191`, `e2e:a11y`, `reduced-motion`, and component-lab coverage.
    - The verifier requires curated gallery and trace categories from the Phase 192 UI-SPEC.
    - `--self-test` exits 0 only after exercising at least one passing fixture and multiple intentional failing fixtures.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_phase192_signoff.mjs --self-test</automated>
    <automated>node --check scripts/ci/verify_phase192_signoff.mjs</automated>
    <automated>grep -q "verifyPhase192Signoff" scripts/ci/verify_phase192_signoff.mjs</automated>
    <automated>grep -q "192-SIGN-OFF.md" scripts/ci/verify_phase192_signoff.mjs</automated>
    <automated>grep -q "artifacts.manifest.json" scripts/ci/verify_phase192_signoff.mjs</automated>
  </verify>
  <done>The sign-off verifier is executable, self-tested, and fails closed when the maintainer package lacks required evidence, gallery, CI, or checklist structure.</done>
</task>

</tasks>

<must_haves>
## Goal-Backward Truths

- Maintainer-readable markdown cannot pass Phase 192 unless canonical structured artifacts also satisfy the strict verifier.
- Final scorecard comparison preserves Phase 187 cell identity and fails on score or coverage regressions.
- Sign-off verification proves curated screenshots are paired with deterministic evidence and explicit accept/block outcomes.

## Required Artifacts

- `scripts/ci/verify_phase192_scorecard.mjs` exists and owns structured scorecard validation.
- `scripts/ci/verify_phase192_signoff.mjs` exists and owns sign-off markdown validation.

## Key Links

- `verify_phase192_scorecard.mjs` reads `.planning/phases/187-audit-baseline/baseline.cells.json` and Phase 192 structured artifacts.
- `verify_phase192_signoff.mjs` reads `192-SIGN-OFF.md` and requires links to the structured artifacts plus evidence refs.
</must_haves>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 192 | Prove final scorecard is only-forward and maintainable through structured verification and sign-off | 192-01 | COVERED | This plan creates the strict verifier contract; later outline plans generate artifacts, CI wiring, and final sign-off. |
| REQ | VER-02 | Multi-lens scorecard must compare >= baseline with zero regressions | 192-01 | COVERED | Scorecard verifier covers artifact strictness and baseline comparison gate. |
| REQ | VER-04 | Maintainer sign-off closes v1.51 photographic-sign-off debt | 192-01 | COVERED | Sign-off verifier enforces curated gallery, evidence refs, checklist, and accept/block outcome. |
| RESEARCH | Pure reducer over evidence | Scorecard pass/fail belongs to deterministic structured data, not markdown or model output | 192-01 | COVERED | Verifier contract requires structured artifacts and rejects markdown-only proof. |
| RESEARCH | Bounded deterministic guardrails | CI/final evidence statuses must remain distinct and reproducible | 192-01 | COVERED | Sign-off verifier requires named guardrail status rows without adding CI wiring in this plan. |
| RESEARCH | Artifact manifest refs | Evidence refs must be repo-relative generated references with checksums where present | 192-01 | COVERED | Scorecard verifier validates `artifacts.manifest.json` and evidence paths. |
| CONTEXT | D-04-D-08 | Hybrid artifact package, structured-data precedence, scorecard summary-only, aggregates cannot hide failures | 192-01 | COVERED | Task 1 creates canonical artifact verifier. |
| CONTEXT | D-09-D-12 | Preserve Phase 187 cell grammar and strict per-cell comparison | 192-01 | COVERED | Task 1 compares final cells against baseline cells. |
| CONTEXT | D-13-D-19 | Layered evidence system, pure synthesis, concrete evidence refs, visual scoring advisory limits | 192-01 | COVERED | Task 1 rejects unsupported or wrong-lens evidence. |
| CONTEXT | D-29-D-41 | Maintainer sign-off package, JTBD gallery, trace refs, checklist categories, voice/domain contract | 192-01 | COVERED | Task 2 verifies sign-off structure and required categories. |
| CONTEXT | Deferred portal design-system todo | Future portal/UI design-system scope | 192-01 | EXCLUDED | Explicitly deferred and not present in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated artifacts -> verifier | JSON, NDJSON, and markdown are untrusted generated files that must be parsed defensively. |
| Phase 187 baseline -> Phase 192 comparison | Baseline cells are canonical historical input; verifier must not mutate or reinterpret them. |
| Evidence refs -> repository filesystem | Artifact refs can point at generated outputs and must not escape allowed repo-relative roots. |
| Maintainer markdown -> milestone sign-off | Human-readable sign-off must be checked against required evidence and explicit accept/block states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-01 | Tampering | `verify_phase192_scorecard.mjs` artifact parsing | mitigate | Parse JSON/NDJSON with line-numbered errors, reject malformed rows, reject score/coverage downgrades, and fail non-empty regression files. |
| T-192-02 | Repudiation | Scorecard comparison | mitigate | Print grouped failure sections for missing comparable cells, score downgrades, coverage downgrades, missing evidence refs, baseline corrections, and manifest issues. |
| T-192-03 | Information Disclosure | Artifact refs | mitigate | Accept only repo-relative refs under generated/planning evidence roots and never print raw screenshot or trace payloads. |
| T-192-04 | Tampering | `verify_phase192_signoff.mjs` markdown validation | mitigate | Require artifact links, curated gallery fields, trace refs, guardrail statuses, and explicit checklist outcome before accepting sign-off. |
| T-192-05 | Repudiation | Maintainer sign-off | mitigate | Require explicit accept/block status and named review categories so approval can be audited later. |
| T-192-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; no new dependency is allowed in this plan. |
</threat_model>

<verification>
Run:
- `node scripts/ci/verify_phase192_scorecard.mjs --self-test`
- `node scripts/ci/verify_phase192_signoff.mjs --self-test`
- `node --check scripts/ci/verify_phase192_scorecard.mjs`
- `node --check scripts/ci/verify_phase192_signoff.mjs`
</verification>

<success_criteria>
Plan 192-01 is complete when both verifier scripts exist, pass their self-tests, fail closed on malformed or incomplete fixtures, and encode the D-04 through D-19 structured scorecard contract plus the D-29 through D-41 sign-off contract without generating final artifacts, changing CI, adding dependencies, or implementing deferred portal/design-system scope.
</success_criteria>

<output>
Create `.planning/phases/192-idempotent-verification-sign-off/192-01-SUMMARY.md` when done.
</output>

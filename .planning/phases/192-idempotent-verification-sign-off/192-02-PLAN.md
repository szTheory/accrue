---
phase: 192-idempotent-verification-sign-off
plan: "02"
type: execute
wave: 2
depends_on:
  - "192-01"
  - "192-03"
files_modified:
  - accrue_admin/e2e/phase192-scorecard.mjs
  - accrue_admin/package.json
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - "VER-02: Final scorecard generation is a pure reducer over separated evidence lenses, not a markdown-only or model-only judgment, per D-04, D-05, D-13, D-14, and D-15."
    - "VER-02: Every comparable final cell preserves the frozen Phase 187 p187__ cell identity and is compared against baseline with final_score >= baseline_score, no coverage downgrade, no missing evidence, and no regression rows, per D-09 through D-18."
    - "VER-02: The scorecard reducer can produce the structured artifact package plus readable 192-SCORECARD.md when Plan 192-06 runs the final evidence workflow, with structured data taking precedence over markdown per D-04 through D-08."
    - "D-01: The reducer contract is grounded in the five advisor-researched gray areas: final scorecard shape, adversarial judge loop, CI guardrail boundary, screenshot sign-off package, and regression repair policy."
    - "D-02: The scorecard package is treated as a locked coherent implementation package; executors must not re-ask or redesign the artifact, judge, CI, sign-off, or repair-policy decisions."
    - "D-06: The final package includes or aliases 192-SCORECARD.md, final.cells.json, scorecard.delta.json, regressions.ndjson, artifacts.manifest.json, and 192-SIGN-OFF.md."
    - "D-07: 192-SCORECARD.md is a readable summary only and must summarize pass/fail, coverage status, regression count, CI guardrail status, and maintainer sign-off state from structured data."
  artifacts:
    - path: "accrue_admin/e2e/phase192-scorecard.mjs"
      provides: "Pure final-scorecard reducer and package generator"
      exports: ["generatePhase192Scorecard", "main"]
    - path: "accrue_admin/package.json"
      provides: "phase192:scorecard npm command"
      contains: "phase192:scorecard"
  key_links:
    - from: "accrue_admin/e2e/phase192-scorecard.mjs"
      to: ".planning/phases/187-audit-baseline/baseline.cells.json"
      via: "loads canonical Phase 187 baseline cells for strict comparison"
      pattern: "baseline\\.cells\\.json"
    - from: "accrue_admin/e2e/phase192-scorecard.mjs"
      to: "accrue_admin/e2e/baseline-manifest.js"
      via: "imports dimensions/projects/surface vocabulary and preserves frozen p187__ grammar"
      pattern: "baseline-manifest\\.js"
    - from: "accrue_admin/package.json"
      to: "accrue_admin/e2e/phase192-scorecard.mjs"
      via: "npm script invokes the generator"
      pattern: "phase192:scorecard"
---

<objective>
Build the pure final-scorecard reducer and npm command for VER-02.

Purpose: Phase 192 needs a deterministic final comparison command that synthesizes separated evidence lenses, preserves the Phase 187 baseline identity, defines the machine-readable artifact contract, and fails closed on regressions when Plan 192-06 runs the final evidence workflow.

Package script ownership note: this plan runs after Plan 192-03 so `accrue_admin/package.json` edits are serialized and the `phase192:scorecard` script is added without disturbing the guardrail scripts created by Plan 192-03.

Output:
- `accrue_admin/e2e/phase192-scorecard.mjs`
- `accrue_admin/package.json` script `phase192:scorecard`
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
@.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
@.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
@.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
@.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
@.planning/phases/192-idempotent-verification-sign-off/192-01-PLAN.md
@.planning/phases/187-audit-baseline/187-RUBRIC.md
@.planning/phases/187-audit-baseline/baseline.cells.json
@.planning/phases/187-audit-baseline/defects.ndjson
@.planning/phases/187-audit-baseline/artifacts.manifest.json
@.planning/phases/187-audit-baseline/schemas/baseline-cell.schema.json
@accrue_admin/package.json
@accrue_admin/e2e/baseline-manifest.js
@accrue_admin/e2e/baseline-artifacts.mjs
@accrue_admin/e2e/admin-baseline.spec.js
@accrue_admin/e2e/admin-interactions.spec.js
@accrue_admin/e2e/admin-a11y.spec.js
@accrue_admin/e2e/admin-group-contracts.spec.js
@accrue_admin/e2e/admin-page-flow-phase191.spec.js
@accrue_admin/e2e/reduced-motion.spec.js
@accrue_admin/e2e/score-visuals.mjs
@scripts/ci/verify_phase192_scorecard.mjs
</context>

## Artifacts this phase produces

- New file: `accrue_admin/e2e/phase192-scorecard.mjs`.
- New exported function: `generatePhase192Scorecard(options)`.
- New exported function: `main(argv)`.
- New npm script: `phase192:scorecard`.
- New final CLI command for Plan 192-06: `cd accrue_admin && npm run phase192:scorecard`.
- New non-final validation command for this plan: `cd accrue_admin && npm run phase192:scorecard -- --self-test`.
- New CLI flag: `--self-test`, using temporary fixtures to prove pass and fail-closed reducer behavior without touching real Phase 192 outputs.
- New CLI flag: `--dry-run`, computing the package and verifier result without writing the Phase 192 artifact files.
- New default input path: `.planning/phases/187-audit-baseline/baseline.cells.json`.
- New default evidence root: `accrue_admin/test-results`.
- Default final output path used by Plan 192-06: `.planning/phases/192-idempotent-verification-sign-off/final.cells.json`.
- Default final output path used by Plan 192-06: `.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json`.
- Default final output path used by Plan 192-06: `.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson`.
- Default final output path used by Plan 192-06: `.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json`.
- Default final output path used by Plan 192-06: `.planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the pure final-scorecard reducer</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-01-PLAN.md
    - @.planning/phases/187-audit-baseline/baseline.cells.json
    - @.planning/phases/187-audit-baseline/artifacts.manifest.json
    - @accrue_admin/e2e/baseline-manifest.js
    - @accrue_admin/e2e/baseline-artifacts.mjs
    - @scripts/ci/verify_phase192_scorecard.mjs
  </read_first>
  <files>accrue_admin/e2e/phase192-scorecard.mjs</files>
  <behavior>
    - Positive case: a self-test fixture with separated lens inputs for correctness, axe, reduced motion, component-lab coverage, interaction trace, visual/brand/microcopy, and maintainer-review rows normalizes to the matching frozen Phase 187 `p187__...__dXX` cell ID, produces one final cell, one passing delta row, zero regression rows, and manifest evidence refs.
    - Negative case: final score below baseline produces a `score-downgrade` regression row and causes the generator to exit non-zero after writing or calculating the package.
    - Negative case: baseline `covered` becoming `gap`, `missing`, or `unreachable` produces a `coverage-downgrade` regression row and exits non-zero.
    - Negative case: a comparable cell with no evidence refs produces a `missing-evidence` regression row and exits non-zero.
    - Negative case: a final cell with a non-Phase-187 cell ID, changed dimension suffix, or reinterpreted baseline semantics exits non-zero unless represented as an explicit structured baseline correction with evidence refs and readable notes.
    - Negative case: accessibility, focus, scroll, overlay actionability, interaction integrity, or CI pass/fail claims supported only by visual/model evidence are rejected per D-17 and D-18.
  </behavior>
  <action>Create `accrue_admin/e2e/phase192-scorecard.mjs` as an ESM Node script modeled on `baseline-artifacts.mjs`: resolve `adminRoot`, `repoRoot`, `testResultsRoot`, and `phaseDir`; import `baseline-manifest.js`; reuse repo-relative evidence ref and SHA-256 inventory patterns; and export `generatePhase192Scorecard(options)` plus `main(argv)`. Per D-13 through D-15, implement the generator as a pure reducer: read canonical Phase 187 baseline cells, read separated raw evidence lenses from generated JSON/NDJSON files under `accrue_admin/test-results` and any explicit lens input paths, normalize each lens row to the frozen Phase 187 `p187__...__dXX` cell identity, synthesize final cell rows without mutating Phase 187 artifacts, compare every comparable final cell to its baseline row, and return structured results before any markdown rendering. Per D-09 through D-12, preserve the 12-dimension rubric, state taxonomy, overlay tags, canonical desktop/mobile Playwright project vocabulary, targeted-width vocabulary, and `__dXX` dimension suffixes from Phase 187; reject baseline reinterpretation unless a structured correction note identifies the invalid baseline row, why it is invalid, and the evidence refs proving the correction. Per D-10, D-11, D-16, D-42, D-43, D-44, and D-45, classify blocking rows into `score-downgrade`, `coverage-downgrade`, `missing-evidence`, `new-regression`, and `baseline-correction-required`; emit non-empty `regressions.ndjson` for any blocker; exit non-zero when blockers exist; and allow inline repair only for harness/parser/artifact-reference/evidence-normalization defects. Per D-14, keep raw lens identity in fields such as `evidence_lenses` or `lens_results` so correctness/browser behavior, axe/WCAG, reduced motion, component-lab/group/page coverage, interaction traces, visual/brand/microcopy scoring, and maintainer review remain separable before final synthesis. Per D-17 and D-18, treat `score-visuals.mjs` output as advisory visual/brand/microcopy evidence and never as the sole basis for accessibility, interaction integrity, focus, scroll, overlay actionability, or CI pass/fail. Implement `--self-test` with temporary fixture files that exercise the positive and negative behaviors above. Implement `--dry-run` so executors can inspect summary counts without writing canonical outputs. Do not add dependencies, do not alter browser specs, do not alter `scripts/ci/verify_phase192_scorecard.mjs`, do not create `192-SIGN-OFF.md`, and do not include deferred portal or pixel-diff scope.</action>
  <acceptance_criteria>
    - `accrue_admin/e2e/phase192-scorecard.mjs` exists, is ESM, and exports `generatePhase192Scorecard` plus `main`.
    - The reducer loads `.planning/phases/187-audit-baseline/baseline.cells.json` and never writes to any Phase 187 artifact.
    - The reducer normalizes separated evidence lenses to frozen `p187__...__dXX` cell IDs and preserves dimension, state, theme, viewport, surface type, and evidence refs.
    - The reducer emits final cells, delta rows, regression rows, manifest content, and markdown content from structured data in one deterministic pass.
    - The reducer exits non-zero on score downgrades, coverage downgrades, missing evidence, malformed lens input, unsupported visual-only evidence for deterministic claims, or non-empty regressions.
    - `--self-test` proves at least one passing fixture and multiple intentional failing fixtures without writing real Phase 192 outputs.
  </acceptance_criteria>
  <verify>
    <automated>node accrue_admin/e2e/phase192-scorecard.mjs --self-test</automated>
    <automated>node --check accrue_admin/e2e/phase192-scorecard.mjs</automated>
    <automated>grep -q "generatePhase192Scorecard" accrue_admin/e2e/phase192-scorecard.mjs</automated>
    <automated>grep -q "baseline.cells.json" accrue_admin/e2e/phase192-scorecard.mjs</automated>
    <automated>grep -q "regressions.ndjson" accrue_admin/e2e/phase192-scorecard.mjs</automated>
  </verify>
  <done>The pure reducer can synthesize the VER-02 scorecard package from separated evidence and fails closed before any markdown or aggregate can mask a blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the scorecard command and validate non-final dry-run behavior</name>
  <read_first>
    - @accrue_admin/package.json
    - @accrue_admin/e2e/phase192-scorecard.mjs
    - @scripts/ci/verify_phase192_scorecard.mjs
    - @.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
  </read_first>
  <files>accrue_admin/package.json</files>
  <action>Add `phase192:scorecard` to `accrue_admin/package.json` with the command `node e2e/phase192-scorecard.mjs`. Validate the package-script wiring by running `cd accrue_admin && npm run phase192:scorecard -- --self-test`, not by generating final Phase 192 artifacts. Per D-04 through D-08, verify the reducer's fixture-based output shape for `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, and `192-SCORECARD.md`: JSON/NDJSON data is canonical, markdown is derived from structured rows, aggregate scores are summary-only, and blocking sections precede aggregate summaries. Per D-16, the fixture output shape must require concrete evidence refs for every final score, downgrade, regression, advisory finding, or correction row and must keep bulky screenshot, trace, report, or ZIP files out of committed artifacts. Run `node accrue_admin/e2e/phase192-scorecard.mjs --dry-run` and a path-boundary check proving dry-run does not mutate `.planning/phases/187-audit-baseline/*` and does not write final artifacts outside `.planning/phases/192-idempotent-verification-sign-off/`. If the script supports a temporary fixture output directory, use that only under a temp directory or the Phase 192 directory and clean it up during the check. Do not run the final no-argument `phase192:scorecard` command, do not run `node scripts/ci/verify_phase192_scorecard.mjs` as the regression-free gate, and do not claim canonical scorecard success in this plan. Plan `192-06` is the sole owner of final evidence rerun, canonical scorecard generation, zero-regression verification, and sign-off generation. Do not generate `192-SIGN-OFF.md`; Plan `192-05` owns sign-off generator construction and Plan `192-06` owns final sign-off generation.</action>
  <acceptance_criteria>
    - `accrue_admin/package.json` contains a `phase192:scorecard` script that invokes `node e2e/phase192-scorecard.mjs`.
    - `cd accrue_admin && npm run phase192:scorecard -- --self-test` exercises the reducer through the npm script without writing real Phase 192 outputs.
    - Fixture-based checks prove `final.cells.json` shape uses the Phase 187 cell field contract and contains only normalized final cells with evidence refs.
    - Fixture-based checks prove `scorecard.delta.json` shape records comparable baseline-to-final comparison fields for score, coverage, evidence, and blocker classification.
    - Fixture-based checks prove `regressions.ndjson` shape is valid NDJSON and that intentional blocker fixtures produce rows.
    - Fixture-based checks prove `artifacts.manifest.json` shape includes generated output paths, evidence inventory, command statuses where available, lens inputs, and checksums where available.
    - Fixture-based checks prove `192-SCORECARD.md` is readable markdown regenerated from structured data and does not introduce independent pass/fail facts.
    - Dry-run validation proves Phase 187 artifacts are not mutated and no final artifacts are written outside `.planning/phases/192-idempotent-verification-sign-off/`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && npm run phase192:scorecard -- --self-test</automated>
    <automated>node accrue_admin/e2e/phase192-scorecard.mjs --dry-run</automated>
    <automated>node -e "const fs=require('fs'); const cp=require('child_process'); const phase187=['baseline.cells.json','defects.ndjson','artifacts.manifest.json'].map(f=>'.planning/phases/187-audit-baseline/'+f); const phase192=['final.cells.json','scorecard.delta.json','regressions.ndjson','artifacts.manifest.json','192-SCORECARD.md'].map(f=>'.planning/phases/192-idempotent-verification-sign-off/'+f); const before=new Map([...phase187,...phase192].map(p=>[p, fs.existsSync(p) ? fs.statSync(p).mtimeMs : null])); cp.execFileSync('node',['accrue_admin/e2e/phase192-scorecard.mjs','--dry-run'],{stdio:'inherit'}); for (const p of phase187) { const after=fs.existsSync(p) ? fs.statSync(p).mtimeMs : null; if (after !== before.get(p)) throw new Error('Phase 187 artifact mutated during dry-run: '+p); } for (const p of phase192) { const after=fs.existsSync(p) ? fs.statSync(p).mtimeMs : null; if (after !== before.get(p)) throw new Error('Final Phase 192 artifact written during non-final dry-run: '+p); } console.log('phase192 scorecard dry-run is non-final and path-bounded')"</automated>
    <automated>node -e "const pkg=require('./accrue_admin/package.json'); if (!pkg.scripts || pkg.scripts['phase192:scorecard'] !== 'node e2e/phase192-scorecard.mjs') throw new Error('phase192:scorecard script missing or changed'); console.log('phase192:scorecard script wired')"</automated>
  </verify>
  <done>The npm command is wired, self-tests and dry-run behavior prove reducer output shapes and path boundaries, and final scorecard generation remains reserved for Plan 192-06.</done>
</task>

</tasks>

<must_haves>
## Goal-Backward Truths

- VER-02 is satisfied only when component, component-group, and page-flow cells are scored through a layered evidence system and reduced to strict Phase 187 baseline comparisons.
- Final scorecard artifacts preserve the Phase 187 `p187__...__dXX` identity and fail on every score downgrade, coverage downgrade, missing evidence row, or regression.
- `192-SCORECARD.md` is useful to a maintainer but cannot override `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, or `artifacts.manifest.json`.
- D-01: The reducer contract is grounded in the five advisor-researched gray areas: final scorecard shape, adversarial judge loop, CI guardrail boundary, screenshot sign-off package, and regression repair policy.
- D-02: The scorecard package is treated as a locked coherent implementation package; executors must not re-ask or redesign the artifact, judge, CI, sign-off, or repair-policy decisions.
- D-06: The final package includes or aliases `192-SCORECARD.md`, `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, and `192-SIGN-OFF.md`.
- D-07: `192-SCORECARD.md` is a readable summary only and must summarize pass/fail, coverage status, regression count, CI guardrail status, and maintainer sign-off state from structured data.

## Required Artifacts

- `accrue_admin/e2e/phase192-scorecard.mjs` creates the reducer and markdown generator.
- `accrue_admin/package.json` exposes `phase192:scorecard`.
- The reducer defines the output contract for `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, and `192-SCORECARD.md`; Plan 192-06 creates or refreshes those final files after the final evidence workflow.

## Key Links

- `phase192-scorecard.mjs` reads Phase 187 baseline cells and baseline manifest vocabulary.
- `phase192-scorecard.mjs` reads raw lens evidence from generated evidence roots while preserving lens separation.
- `phase192:scorecard` invokes the reducer.
- `verify_phase192_scorecard.mjs` validates the generated package during Plan 192-06 after the final evidence workflow reruns.
</must_haves>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 192 | Re-run and score every level with an adversarial multi-lens judge, compare against Phase 187 baseline, and prove zero regressions | 192-02 | COVERED | This plan creates the reducer and command that Plan 192-06 uses after final evidence rerun for canonical comparison and zero-regression proof. |
| REQ | VER-02 | Each level is scored by adversarial multi-lens judge and final scorecard is >= baseline with zero regressions | 192-02 | COVERED | Tasks normalize lens evidence, define final/delta/regression artifact shapes, self-test pass/fail behavior, and leave final strict verification to Plan 192-06. |
| RESEARCH | Pure reducer over evidence | Deterministic synthesis normalizes evidence to p187 cells and compares baseline strictly | 192-02 | COVERED | Task 1 builds `phase192-scorecard.mjs` as pure reducer. |
| RESEARCH | Artifact manifest references | Evidence refs and checksums are stored in manifest, not bulky committed binaries | 192-02 | COVERED | Task 2 validates the manifest output shape and forbids committing bulky evidence; Plan 192-06 emits the canonical manifest after final evidence rerun. |
| RESEARCH | Advisory visual lens | `score-visuals.mjs` can contribute advisory brand/microcopy evidence but cannot gate deterministic claims | 192-02 | COVERED | Task 1 rejects visual/model-only support for deterministic claims. |
| CONTEXT | D-01 | All five advisor-researched gray areas feed this plan's scorecard reducer: final scorecard shape, adversarial judge loop, CI boundary, screenshot sign-off package, and regression repair policy | 192-02 | COVERED | The reducer contract is shaped by the researched scorecard and adversarial judge decisions; CI/sign-off/repair decisions are consumed by Plans 192-03 through 192-06. |
| CONTEXT | D-02 | Locked coherent package; downstream agents should not re-ask decisions | 192-02 | COVERED | Tasks implement the locked reducer/package contract directly and do not introduce new design questions or optional alternatives. |
| CONTEXT | D-06 | Final package includes `192-SCORECARD.md`, `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, and `192-SIGN-OFF.md` | 192-02 | COVERED | This plan defines the scorecard artifact contract and leaves final writes/sign-off completion to Plans 192-05 and 192-06. |
| CONTEXT | D-07 | `192-SCORECARD.md` is readable summary only and reports pass/fail, coverage, regressions, CI status, and sign-off state | 192-02 | COVERED | Task 2 validates fixture output shape for derived markdown while preserving structured JSON/NDJSON as source of truth. |
| CONTEXT | D-04-D-08 | Hybrid package, structured-data precedence, readable scorecard summary, aggregate scores summary-only | 192-02 | COVERED | Task 2 validates fixture output shapes for canonical structured artifacts and generated `192-SCORECARD.md`; Plan 192-06 owns final writes. |
| CONTEXT | D-09-D-12 | Preserve Phase 187 grammar and strict per-cell comparison | 192-02 | COVERED | Task 1 preserves `p187__...__dXX` identity and compares against `baseline.cells.json`. |
| CONTEXT | D-13-D-18 | Layered evidence system, separated raw lens outputs, pure reducer, concrete evidence refs, advisory visual scoring, axe not sufficient alone | 192-02 | COVERED | Task 1 and Task 2 enforce lens separation and evidence citation. |
| CONTEXT | D-42-D-45 | Inline fixes limited to harness/parser/artifact-reference/evidence-normalization; true regressions block and corrected Phase 191 behavior is target | 192-02 | COVERED | Task 1 classifies blocking rows and rejects encoding broken Phase 187 observations as expected final behavior. |
| CONTEXT | Deferred TOOL-01/TOOL-02 and portal scope | PhoenixStorybook, pixel-diff service, portal design-system work are out of scope | 192-02 | EXCLUDED | No task adds dependencies, pixel-diff service, or portal work. |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw evidence lens files -> reducer | JSON/NDJSON files under generated evidence roots are untrusted inputs that can be malformed, stale, incomplete, or lens-misclassified. |
| Phase 187 baseline -> Phase 192 scorecard | Historical baseline is canonical and must be read-only; final synthesis must not mutate or reinterpret it to pass. |
| Reducer -> planning artifacts | Generated JSON/NDJSON/markdown artifacts become sign-off inputs and must reflect structured truth exactly. |
| Artifact refs -> filesystem | Evidence refs must remain repo-relative generated references and must not expose arbitrary local paths or bulky binary payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-06 | Tampering | `accrue_admin/e2e/phase192-scorecard.mjs` input parsing | mitigate | Parse JSON/NDJSON defensively, reject malformed lens rows, preserve raw lens identity, and fail closed before writing pass summaries. |
| T-192-07 | Repudiation | Baseline comparison rows | mitigate | Emit `scorecard.delta.json` with baseline/final scores, coverage transitions, evidence refs, and blocker classification for every comparable cell. |
| T-192-08 | Information Disclosure | `artifacts.manifest.json` evidence refs | mitigate | Store repo-relative refs and checksums only; do not inline screenshots, traces, reports, or arbitrary local filesystem paths. |
| T-192-09 | Tampering | `192-SCORECARD.md` readable summary | mitigate | Build reducer logic so markdown is generated from structured artifacts only; Plan 192-06 runs `verify_phase192_scorecard.mjs` after final generation so markdown cannot override JSON/NDJSON. |
| T-192-10 | Denial of Service | Broad evidence corpus reduction | mitigate | Keep reducer bounded to generated JSON/NDJSON inventories and summary metadata, not full binary payload reads except checksum calculation on discovered files. |
| T-192-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; no new dependency is allowed in this plan. |
</threat_model>

<verification>
Run:
- `node accrue_admin/e2e/phase192-scorecard.mjs --self-test`
- `node --check accrue_admin/e2e/phase192-scorecard.mjs`
- `cd accrue_admin && npm run phase192:scorecard -- --self-test`
- `node accrue_admin/e2e/phase192-scorecard.mjs --dry-run`
- Path-boundary check proving dry-run does not mutate Phase 187 artifacts or write final Phase 192 scorecard artifacts
- Package-script check proving `phase192:scorecard` invokes `node e2e/phase192-scorecard.mjs`
</verification>

<success_criteria>
Plan 192-02 is complete when `phase192:scorecard` is wired, the reducer self-tests pass through both direct Node and npm-script entry points, fixture output-shape checks cover the VER-02 package contract, dry-run behavior is non-final and path-bounded, and no canonical final scorecard, zero-regression verification, deferred portal, pixel-diff, CI wiring, or maintainer sign-off scope is implemented in this plan. Plan 192-06 remains the sole owner of final evidence rerun, final scorecard generation, `verify_phase192_scorecard.mjs` zero-regression verification, and sign-off generation.
</success_criteria>

<output>
Create `.planning/phases/192-idempotent-verification-sign-off/192-02-SUMMARY.md` when done.
</output>

---
phase: 192-idempotent-verification-sign-off
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/package.json
  - scripts/ci/verify_phase192_admin_guardrails.sh
  - scripts/ci/verify_phase192_guardrail_contract.sh
autonomous: true
requirements:
  - VER-03
must_haves:
  truths:
    - "Contributors can run the Phase 192 deterministic admin guardrail boundary locally with one command before CI."
    - "The local guardrail boundary includes baseline parse, Phase 191 AX187 coverage, group contracts, Phase 191 interactions, axe a11y, reduced-motion, and component-lab structural coverage per D-23."
    - "Broad or subjective evidence producers stay out of the merge-blocking guardrail command per D-24 through D-27."
  artifacts:
    - path: "scripts/ci/verify_phase192_admin_guardrails.sh"
      provides: "Serial root-level local guardrail runner for the deterministic Phase 192 CI boundary"
    - path: "scripts/ci/verify_phase192_guardrail_contract.sh"
      provides: "Static contract verifier for required and forbidden Phase 192 guardrail commands"
    - path: "accrue_admin/package.json"
      provides: "Named npm scripts for phase192:component-lab and phase192:guardrails"
  key_links:
    - from: "accrue_admin/package.json"
      to: "scripts/ci/verify_phase192_admin_guardrails.sh"
      via: "phase192:guardrails npm alias shells to the root guardrail runner"
      pattern: "phase192:guardrails"
    - from: "scripts/ci/verify_phase192_admin_guardrails.sh"
      to: "scripts/ci/verify_phase191_ax187_coverage.mjs"
      via: "runner executes the existing Phase 191 AX187 verifier from the repo root"
      pattern: "verify_phase191_ax187_coverage"
    - from: "scripts/ci/verify_phase192_admin_guardrails.sh"
      to: "accrue_admin/test/accrue_admin/dev/component_registry_test.exs"
      via: "runner executes the selected component-lab structural coverage command"
      pattern: "phase192:component-lab"
---

<objective>
Add deterministic local guardrail commands for the required Phase 192 CI boundary for VER-03.

Purpose: Phase 192 needs a bounded, reproducible PR gate that contributors can run locally and that Plan 192-04 can wire into CI without re-deciding the command boundary. This plan implements D-20 through D-28 by naming every deterministic guardrail command and keeping broad capture, full-suite, model-scored, and maintainer-review evidence out of the merge-blocking path.

Output:
- `scripts/ci/verify_phase192_admin_guardrails.sh`
- `scripts/ci/verify_phase192_guardrail_contract.sh`
- `accrue_admin/package.json` script aliases
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
@.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
@.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
@.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
@.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
@accrue_admin/package.json
@scripts/ci/verify_phase191_ax187_coverage.mjs
@scripts/ci/verify_phase190_automation_contract.sh
@accrue_admin/test/accrue_admin/dev/component_registry_test.exs
@accrue_admin/test/accrue_admin/dev/component_group_registry_test.exs
@accrue_admin/e2e/admin-group-contracts.spec.js
@accrue_admin/e2e/admin-page-flow-phase191.spec.js
@accrue_admin/e2e/admin-a11y.spec.js
@accrue_admin/e2e/reduced-motion.spec.js
</context>

## Artifacts this phase produces

- New file: `scripts/ci/verify_phase192_admin_guardrails.sh`.
- New CLI command: `bash scripts/ci/verify_phase192_admin_guardrails.sh`.
- New file: `scripts/ci/verify_phase192_guardrail_contract.sh`.
- New CLI command: `bash scripts/ci/verify_phase192_guardrail_contract.sh`.
- New npm script: `phase192:component-lab`.
- New npm script: `phase192:guardrails`.
- Existing required command preserved: `cd accrue_admin && npm run baseline:parse`.
- Existing required command preserved: `node scripts/ci/verify_phase191_ax187_coverage.mjs`.
- Existing required command preserved: `cd accrue_admin && npm run e2e:group-contracts`.
- Existing required command preserved: `cd accrue_admin && npm run e2e:phase191`.
- Existing required command preserved: `cd accrue_admin && npm run e2e:a11y`.
- Existing required command preserved: `cd accrue_admin && npx playwright test e2e/reduced-motion.spec.js --workers=1`.
- New component-lab structural coverage command: `cd accrue_admin && npm run phase192:component-lab`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add serial Phase 192 local guardrail runner and npm aliases</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
    - @accrue_admin/package.json
    - @scripts/ci/verify_phase191_ax187_coverage.mjs
    - @scripts/ci/verify_phase190_automation_contract.sh
    - @accrue_admin/test/accrue_admin/dev/component_registry_test.exs
    - @accrue_admin/test/accrue_admin/dev/component_group_registry_test.exs
  </read_first>
  <files>scripts/ci/verify_phase192_admin_guardrails.sh, accrue_admin/package.json</files>
  <behavior>
    - Running `bash scripts/ci/verify_phase192_admin_guardrails.sh` executes the D-23 deterministic guardrails serially from a clean repo root.
    - Running `cd accrue_admin && npm run phase192:guardrails` executes the same root-level guardrail runner.
    - Running `cd accrue_admin && npm run phase192:component-lab` executes component-lab structural coverage through existing ExUnit registry tests.
    - The runner fails immediately on the first failed command and prints a clear step label before each command.
    - The runner does not call the broad full E2E script, visual scoring script, baseline artifact generator, screenshot capture, trace capture, or maintainer sign-off commands.
  </behavior>
  <action>Create `scripts/ci/verify_phase192_admin_guardrails.sh` using the bash pattern from `verify_phase190_automation_contract.sh`: shebang, `set -euo pipefail`, repo-root resolution from the script path, and explicit step labels. Per D-20 and D-23, run these commands in this exact serial boundary: `cd accrue_admin && npm run baseline:parse`; `node scripts/ci/verify_phase191_ax187_coverage.mjs`; `cd accrue_admin && npm run e2e:group-contracts`; `cd accrue_admin && npm run e2e:phase191`; `cd accrue_admin && npm run e2e:a11y`; `cd accrue_admin && npx playwright test e2e/reduced-motion.spec.js --workers=1`; `cd accrue_admin && npm run phase192:component-lab`. Keep all commands serial in one process because Phase 191 closeout established that shared Playwright/build/server state is safer when not launched in parallel. Per D-24 through D-27, do not call the broad all-spec E2E script, visual scoring, baseline artifact generation, screenshot capture, trace capture, or maintainer sign-off from this runner. Modify `accrue_admin/package.json` using the existing scripts style to add `phase192:component-lab` as `mix test test/accrue_admin/dev/component_registry_test.exs test/accrue_admin/dev/component_group_registry_test.exs` and `phase192:guardrails` as `bash ../scripts/ci/verify_phase192_admin_guardrails.sh`. Preserve existing scripts and do not upgrade dependencies or change `package-lock.json` unless npm formatting tooling requires it.</action>
  <acceptance_criteria>
    - `scripts/ci/verify_phase192_admin_guardrails.sh` exists, is executable by `bash`, and resolves the repo root without depending on the current working directory.
    - The runner contains the seven required D-23 deterministic commands and runs them in serial order.
    - `accrue_admin/package.json` contains `phase192:component-lab` and `phase192:guardrails` scripts without removing existing `baseline:parse`, `e2e:group-contracts`, `e2e:phase191`, or `e2e:a11y`.
    - `phase192:component-lab` uses the existing registry ExUnit tests as the component-lab structural coverage path.
    - The runner does not include the broad all-spec E2E command, visual scoring, or baseline artifact generation as merge-blocking steps.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/verify_phase192_admin_guardrails.sh</automated>
    <automated>node -e "const pkg=require('./accrue_admin/package.json'); for (const k of ['phase192:component-lab','phase192:guardrails','baseline:parse','e2e:group-contracts','e2e:phase191','e2e:a11y']) { if (!pkg.scripts[k]) throw new Error('missing '+k); } console.log('phase192 package scripts ok')"</automated>
    <automated>grep -Fq "npm run baseline:parse" scripts/ci/verify_phase192_admin_guardrails.sh</automated>
    <automated>grep -Fq "verify_phase191_ax187_coverage.mjs" scripts/ci/verify_phase192_admin_guardrails.sh</automated>
    <automated>grep -Fq "npm run phase192:component-lab" scripts/ci/verify_phase192_admin_guardrails.sh</automated>
  </verify>
  <done>Phase 192 has one local serial guardrail command and npm aliases that cover every deterministic PR-boundary check required by VER-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static guardrail boundary contract verifier</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
    - @accrue_admin/package.json
    - @scripts/ci/verify_phase192_admin_guardrails.sh
    - @scripts/ci/verify_phase190_automation_contract.sh
  </read_first>
  <files>scripts/ci/verify_phase192_guardrail_contract.sh</files>
  <behavior>
    - Running `bash scripts/ci/verify_phase192_guardrail_contract.sh` exits 0 when the package scripts and root guardrail runner include every required deterministic command.
    - The verifier exits non-zero if the runner omits baseline parse, the Phase 191 AX187 verifier, group contracts, Phase 191 interactions, axe a11y, reduced-motion, or component-lab structural coverage.
    - The verifier exits non-zero if the runner contains broad all-spec E2E execution, visual scoring, baseline artifact generation, screenshot capture, trace capture, or maintainer sign-off language as a required PR gate.
    - The verifier confirms `phase192:guardrails` points to the root runner and `phase192:component-lab` points at existing component registry test files.
  </behavior>
  <action>Create `scripts/ci/verify_phase192_guardrail_contract.sh` with the same bash contract style as `verify_phase190_automation_contract.sh`: `fail`, `require_file`, `require_fixed`, `require_regex`, and a scoped forbidden-pattern helper. Per D-20 through D-23, require `accrue_admin/package.json`, `scripts/ci/verify_phase192_admin_guardrails.sh`, `scripts/ci/verify_phase191_ax187_coverage.mjs`, `accrue_admin/e2e/admin-group-contracts.spec.js`, `accrue_admin/e2e/admin-page-flow-phase191.spec.js`, `accrue_admin/e2e/admin-a11y.spec.js`, `accrue_admin/e2e/reduced-motion.spec.js`, `accrue_admin/test/accrue_admin/dev/component_registry_test.exs`, and `accrue_admin/test/accrue_admin/dev/component_group_registry_test.exs`. Require fixed strings in the runner for each deterministic command named in D-23 and require fixed strings in package.json for `phase192:guardrails` and `phase192:component-lab`. Per D-24 through D-27, reject forbidden merge-blocking constructs only inside `verify_phase192_admin_guardrails.sh` and the `phase192:*` package script lines so existing advisory scripts such as `score-visuals` and `baseline:artifacts` can remain in package.json without failing this verifier. Use region-scoped extraction or targeted regex instead of file-wide negative greps on package.json because sibling advisory scripts legitimately exist there. End with `verify_phase192_guardrail_contract: ok` on success.</action>
  <acceptance_criteria>
    - `scripts/ci/verify_phase192_guardrail_contract.sh` exists and is syntax-valid bash.
    - The contract verifier requires every D-23 deterministic command by fixed string or precise regex.
    - The contract verifier rejects forbidden broad/advisory/final-evidence commands only in the Phase 192 runner and Phase 192 package script values, not in unrelated existing package scripts.
    - The contract verifier checks component-lab coverage is backed by existing registry test paths.
    - The contract verifier can be run locally without starting Playwright, Postgres, or the E2E server.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/verify_phase192_guardrail_contract.sh</automated>
    <automated>bash scripts/ci/verify_phase192_guardrail_contract.sh</automated>
    <automated>grep -Fq "verify_phase192_admin_guardrails.sh" scripts/ci/verify_phase192_guardrail_contract.sh</automated>
    <automated>grep -Fq "phase192:component-lab" scripts/ci/verify_phase192_guardrail_contract.sh</automated>
    <automated>grep -Fq "verify_phase192_guardrail_contract: ok" scripts/ci/verify_phase192_guardrail_contract.sh</automated>
  </verify>
  <done>The local guardrail boundary has a fast static verifier that later CI wiring can run before expensive browser checks.</done>
</task>

</tasks>

<must_haves>
## Goal-Backward Truths

- Contributors can run one local Phase 192 admin hardening guardrail command that mirrors the deterministic CI boundary.
- The guardrail boundary includes baseline parse, AX187 coverage, group contracts, Phase 191 interaction coverage, axe a11y, reduced-motion, and component-lab structural coverage.
- Broad all-spec, generated-evidence, model-scored, screenshot, trace, and maintainer sign-off work remains final/advisory evidence, not a merge-blocking PR gate.

## Required Artifacts

- `scripts/ci/verify_phase192_admin_guardrails.sh` exists and runs the guardrails serially.
- `scripts/ci/verify_phase192_guardrail_contract.sh` exists and verifies the command boundary without starting browser infrastructure.
- `accrue_admin/package.json` contains `phase192:component-lab` and `phase192:guardrails`.

## Key Links

- `phase192:guardrails` in `accrue_admin/package.json` invokes `../scripts/ci/verify_phase192_admin_guardrails.sh`.
- `verify_phase192_admin_guardrails.sh` invokes the existing `verify_phase191_ax187_coverage.mjs` source verifier.
- `phase192:component-lab` invokes the existing component registry ExUnit coverage files.
</must_haves>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 192 | Regression guardrails run in CI so re-running the milestone only ever finds new gaps | 192-03 | COVERED | This plan creates the local deterministic command boundary that Plan 192-04 wires into CI. |
| REQ | VER-03 | Interaction e2e, axe a11y, reduced-motion, and component-lab coverage run in CI | 192-03 | COVERED | The runner includes Phase 191 interactions, group contracts, axe, reduced-motion, and component-lab structural coverage. |
| RESEARCH | Bounded serial CI guardrail | Run deterministic commands serially to avoid shared browser/build lock collisions | 192-03 | COVERED | Task 1 creates a serial bash runner rather than parallel Playwright jobs. |
| RESEARCH | Component-lab coverage source | Use existing `AccrueAdmin.Dev.ComponentRegistry` and registry tests rather than a new registry | 192-03 | COVERED | `phase192:component-lab` runs existing registry ExUnit coverage. |
| RESEARCH | Advisory evidence separation | Keep generated screenshots, traces, full e2e, and model scoring out of PR blockers | 192-03 | COVERED | Task 2 statically rejects these constructs in the Phase 192 guardrail boundary. |
| CONTEXT | D-20 | CI blocks only on deterministic, bounded, locally reproducible checks | 192-03 | COVERED | Task 1 creates a local root runner and npm alias. |
| CONTEXT | D-21 | Existing BEAM/package CI remains required | 192-03 | COVERED | This plan does not remove or weaken existing CI; Plan 192-04 preserves jobs when wiring guardrails. |
| CONTEXT | D-22 | Keep `admin-group-contracts` merge-blocking | 192-03 | COVERED | Runner preserves `npm run e2e:group-contracts`; Plan 192-04 handles CI topology. |
| CONTEXT | D-23 | Add baseline parse, AX187 verifier, group contracts, Phase 191 interactions, axe, reduced-motion, and component-lab check | 192-03 | COVERED | Task 1 runner includes all listed commands. |
| CONTEXT | D-24-D-27 | Keep broad e2e, score-visuals, baseline artifacts, screenshots, traces, baseline regeneration, and sign-off out of PR gates | 192-03 | COVERED | Task 2 contract verifier prevents those commands from entering the Phase 192 guardrail boundary. |
| CONTEXT | D-28 | Upload artifacts where useful, do not commit bulky outputs | 192-03 | COVERED | This plan creates local commands only and commits no generated reports, PNGs, traces, or ZIPs; artifact upload is Plan 192-04. |
| CONTEXT | Deferred portal design-system todo | Future portal/design-system scope | 192-03 | EXCLUDED | Explicitly deferred and not present in tasks. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell -> local guardrail runner | Local commands execute package scripts and browser tests from the repository; command scope must be explicit and bounded. |
| Package scripts -> root CI scripts | `accrue_admin/package.json` aliases cross from package working directory to repo-root scripts. |
| Static contract verifier -> source files | The verifier reads scripts and package JSON as untrusted text and must avoid overbroad negative checks that break existing advisory scripts. |
| Browser guardrails -> E2E server/database | Playwright commands share server, build output, and test database state; serial execution limits collision risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-03-01 | Tampering | `scripts/ci/verify_phase192_admin_guardrails.sh` | mitigate | Task 2 adds a static contract verifier that requires every D-23 command and rejects forbidden PR-gate commands in the Phase 192 boundary. |
| T-192-03-02 | Denial of Service | Playwright guardrail runner | mitigate | Task 1 runs guardrails serially with existing bounded scripts and `--workers=1` reduced-motion execution, avoiding parallel shared-state collisions. |
| T-192-03-03 | Repudiation | Local guardrail output | mitigate | Task 1 prints explicit step labels before each command so failed local and CI logs identify the failing guardrail. |
| T-192-03-04 | Information Disclosure | CI/local logs | accept | Guardrails run local test commands and do not require secrets; no screenshots, traces, model output, or bulky evidence are produced by this plan. |
| T-192-03-05 | Elevation of Privilege | Package script alias | mitigate | Aliases only call repo-local scripts and existing test commands; no curl, eval, dependency install, or external service command is introduced. |
| T-192-03-SC | Tampering | npm installs | accept | No new npm/pip/cargo package installs are planned; existing dependencies are not upgraded. |
</threat_model>

<verification>
Run the fast structural checks after implementation:

- `bash -n scripts/ci/verify_phase192_admin_guardrails.sh`
- `bash -n scripts/ci/verify_phase192_guardrail_contract.sh`
- `bash scripts/ci/verify_phase192_guardrail_contract.sh`
- `node -e "const pkg=require('./accrue_admin/package.json'); for (const k of ['phase192:component-lab','phase192:guardrails']) { if (!pkg.scripts[k]) throw new Error('missing '+k); } console.log('phase192 package scripts ok')"`

Run the full local guardrail command when the environment has BEAM deps, npm deps, Chromium, and Postgres ready:

- `bash scripts/ci/verify_phase192_admin_guardrails.sh`
</verification>

<success_criteria>
VER-03 planning for this slice is complete when the repo has deterministic local guardrail commands for the required Phase 192 CI boundary, a fast static verifier for that boundary, and package aliases that contributors can run without remembering the full command list. The implementation must preserve existing CI/package scripts and must not promote broad all-spec E2E, visual/model scoring, baseline artifact generation, screenshots, traces, or maintainer sign-off into merge-blocking PR gates.
</success_criteria>

<output>
Create `.planning/phases/192-idempotent-verification-sign-off/192-03-SUMMARY.md` when done.
</output>

---
phase: 192-idempotent-verification-sign-off
plan: "06"
type: execute
wave: 3
depends_on:
  - "192-02"
  - "192-04"
  - "192-05"
files_modified:
  - .planning/phases/192-idempotent-verification-sign-off/final.cells.json
  - .planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json
  - .planning/phases/192-idempotent-verification-sign-off/regressions.ndjson
  - .planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json
  - .planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md
  - .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md
autonomous: false
requirements:
  - VER-02
  - VER-03
  - VER-04
must_haves:
  truths:
    - "VER-02: The final Phase 192 scorecard is regenerated from canonical structured evidence and verifies final_score >= Phase 187 baseline for every comparable cell with zero coverage downgrades and zero regression rows, per D-04 through D-18 and D-42 through D-45."
    - "VER-03: The complete deterministic admin hardening guardrail boundary runs locally before sign-off, including baseline parse, AX187 coverage, group contracts, Phase 191 interactions, axe a11y, reduced motion, component-lab coverage, scorecard verifier, CI contract verifier, and sign-off verifier, per D-20 through D-28."
    - "VER-04: The maintainer approves 192-SIGN-OFF.md after reviewing the scorecard, curated gallery, manifest evidence refs, trace refs, CI guardrail status, and checklist, per D-29 through D-41."
  artifacts:
    - path: ".planning/phases/192-idempotent-verification-sign-off/final.cells.json"
      provides: "Regenerated canonical final matrix cells"
    - path: ".planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json"
      provides: "Regenerated canonical Phase 187 baseline-to-final comparison"
    - path: ".planning/phases/192-idempotent-verification-sign-off/regressions.ndjson"
      provides: "Regenerated canonical blocking regression ledger; empty means zero blockers"
    - path: ".planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json"
      provides: "Regenerated evidence manifest with command statuses, output refs, and evidence refs"
    - path: ".planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md"
      provides: "Regenerated maintainer-readable scorecard summary derived from structured artifacts"
    - path: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      provides: "Maintainer sign-off package and approval record"
  key_links:
    - from: "bash scripts/ci/verify_phase192_admin_guardrails.sh"
      to: ".planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json"
      via: "deterministic browser/a11y/motion/component guardrails produce or validate evidence refs before final scorecard generation"
      pattern: "verify_phase192_admin_guardrails\\.sh"
    - from: "cd accrue_admin && npm run phase192:scorecard"
      to: ".planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json"
      via: "pure reducer regenerates final comparison from Phase 187 baseline and current evidence"
      pattern: "phase192:scorecard"
    - from: "cd accrue_admin && npm run phase192:signoff"
      to: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      via: "sign-off generator renders the maintainer decision surface from structured scorecard and manifest refs"
      pattern: "phase192:signoff"
    - from: ".planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md"
      to: "maintainer checkpoint"
      via: "human approval records final VER-04 accept/block decision"
      pattern: "Maintainer Checklist"
---

<objective>
Run the full final verification loop and maintainer sign-off checkpoint for Phase 192.

Purpose: This is the final v1.53 closeout slice. It proves the milestone is done by executing deterministic guardrails, regenerating the canonical final scorecard package, verifying zero regressions against the Phase 187 baseline, verifying the sign-off package, and obtaining maintainer approval of `192-SIGN-OFF.md` to close v1.51 photographic-sign-off debt.

Output:
- Refreshed Phase 192 scorecard artifacts.
- Refreshed `192-SIGN-OFF.md`.
- Maintainer approval or a precise blocking regression/sign-off issue list.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
@.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
@.planning/phases/192-idempotent-verification-sign-off/192-RESEARCH.md
@.planning/phases/192-idempotent-verification-sign-off/192-PATTERNS.md
@.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
@.planning/phases/192-idempotent-verification-sign-off/192-PLAN-OUTLINE.md
@.planning/phases/192-idempotent-verification-sign-off/192-02-PLAN.md
@.planning/phases/192-idempotent-verification-sign-off/192-04-PLAN.md
@.planning/phases/192-idempotent-verification-sign-off/192-05-PLAN.md
@.planning/phases/187-audit-baseline/baseline.cells.json
@.planning/phases/187-audit-baseline/defects.ndjson
@.planning/phases/187-audit-baseline/artifacts.manifest.json
@scripts/ci/verify_phase191_ax187_coverage.mjs
@scripts/ci/verify_phase192_admin_guardrails.sh
@scripts/ci/verify_phase192_guardrail_contract.sh
@scripts/ci/verify_phase192_ci_contract.sh
@scripts/ci/verify_phase192_scorecard.mjs
@scripts/ci/verify_phase192_signoff.mjs
@accrue_admin/package.json
@accrue_admin/e2e/phase192-scorecard.mjs
@accrue_admin/e2e/phase192-gallery.mjs
</context>

## Artifacts this phase produces

- Refreshed path: `.planning/phases/192-idempotent-verification-sign-off/final.cells.json`.
- Refreshed path: `.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json`.
- Refreshed path: `.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson`.
- Refreshed path: `.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json`.
- Refreshed path: `.planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md`.
- Refreshed path: `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`.
- Required closeout summary path: `.planning/phases/192-idempotent-verification-sign-off/192-06-SUMMARY.md`.
- No new symbols are created by this plan.
- No new CLI flags are created by this plan.
- Required CLI command: `bash scripts/ci/verify_phase192_guardrail_contract.sh`.
- Required CLI command: `bash scripts/ci/verify_phase192_ci_contract.sh`.
- Required CLI command: `bash scripts/ci/verify_phase192_admin_guardrails.sh`.
- Required CLI command: `cd accrue_admin && npm run phase192:scorecard`.
- Required CLI command: `node scripts/ci/verify_phase192_scorecard.mjs`.
- Required CLI command: `cd accrue_admin && npm run phase192:signoff`.
- Required CLI command: `node scripts/ci/verify_phase192_signoff.mjs`.

<tasks>

<task type="auto">
  <name>Task 1: Execute the deterministic final guardrail boundary</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-VALIDATION.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-04-PLAN.md
    - @scripts/ci/verify_phase192_guardrail_contract.sh
    - @scripts/ci/verify_phase192_ci_contract.sh
    - @scripts/ci/verify_phase192_admin_guardrails.sh
  </read_first>
  <files>.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json</files>
  <action>Run the exact deterministic guardrail boundary from Plans 192-03 and 192-04 before any final sign-off claim. Execute `bash scripts/ci/verify_phase192_guardrail_contract.sh`, `bash scripts/ci/verify_phase192_ci_contract.sh`, and `bash scripts/ci/verify_phase192_admin_guardrails.sh`. The guardrail runner must preserve the D-23 command set: `cd accrue_admin && npm run baseline:parse`; `node scripts/ci/verify_phase191_ax187_coverage.mjs`; `cd accrue_admin && npm run e2e:group-contracts`; `cd accrue_admin && npm run e2e:phase191`; `cd accrue_admin && npm run e2e:a11y`; `cd accrue_admin && npx playwright test e2e/reduced-motion.spec.js --workers=1`; and the component-lab structural coverage check selected by Plan 192-03. If a guardrail fails, stop this plan and classify the failure under D-42 or D-43: harness/parser/artifact-reference/evidence-normalization issues may be fixed in a narrow Phase 192 repair plan, while true UI, accessibility, interaction, copy, component, group, or page-flow regressions block sign-off and require rerunning the scorecard after repair. Do not replace the runner with broad `npm run e2e`, do not promote `score-visuals`, `baseline:artifacts`, screenshot capture, trace capture, or maintainer sign-off into merge-blocking PR gates, and do not commit bulky Playwright reports, screenshots, traces, or ZIP outputs.</action>
  <acceptance_criteria>
    - `bash scripts/ci/verify_phase192_guardrail_contract.sh` exits 0.
    - `bash scripts/ci/verify_phase192_ci_contract.sh` exits 0.
    - `bash scripts/ci/verify_phase192_admin_guardrails.sh` exits 0.
    - The executed guardrail boundary includes baseline parse, Phase 191 AX187 coverage, group contracts, Phase 191 interactions, axe a11y, reduced motion, and component-lab coverage.
    - Any failure is recorded as a blocker with the exact failing command and is not hidden by advisory visual/model evidence or aggregate score prose.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_phase192_guardrail_contract.sh</automated>
    <automated>bash scripts/ci/verify_phase192_ci_contract.sh</automated>
    <automated>bash scripts/ci/verify_phase192_admin_guardrails.sh</automated>
  </verify>
  <done>The deterministic VER-03 guardrail boundary is green locally and ready for final scorecard/sign-off regeneration.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and verify final scorecard and sign-off artifacts</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-CONTEXT.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-UI-SPEC.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-02-PLAN.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-05-PLAN.md
    - @accrue_admin/e2e/phase192-scorecard.mjs
    - @accrue_admin/e2e/phase192-gallery.mjs
    - @scripts/ci/verify_phase192_scorecard.mjs
    - @scripts/ci/verify_phase192_signoff.mjs
  </read_first>
  <files>.planning/phases/192-idempotent-verification-sign-off/final.cells.json, .planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json, .planning/phases/192-idempotent-verification-sign-off/regressions.ndjson, .planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json, .planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md, .planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md</files>
  <action>Regenerate the canonical final scorecard package after Task 1 succeeds. Run `cd accrue_admin && npm run phase192:scorecard`, then `node scripts/ci/verify_phase192_scorecard.mjs`. Per D-04 through D-18, treat `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json` as canonical; `192-SCORECARD.md` is derived readable summary only; aggregate scores cannot hide a failing cell or dimension. Verify strict D-10 semantics: every comparable final cell has `final_score >= baseline_score`, no coverage downgrade, no missing evidence, and no new regression row in `regressions.ndjson`. Then run `cd accrue_admin && npm run phase192:signoff`, followed by `node scripts/ci/verify_phase192_signoff.mjs`, so `192-SIGN-OFF.md` references the refreshed scorecard artifacts, curated gallery rows, manifest links, interaction trace refs, CI guardrail status, and explicit accept/block checklist. If `regressions.ndjson` is non-empty, if the scorecard verifier fails, or if the sign-off verifier fails, stop before the human checkpoint and report the exact blocker. Do not edit Phase 187 baseline artifacts, do not reinterpret Phase 187 semantics to pass, do not add portal/PhoenixStorybook/pixel-diff scope, and do not commit bulky evidence outputs.</action>
  <acceptance_criteria>
    - `cd accrue_admin && npm run phase192:scorecard` exits 0.
    - `node scripts/ci/verify_phase192_scorecard.mjs` exits 0.
    - `regressions.ndjson` has zero rows after final scorecard generation.
    - `final.cells.json`, `scorecard.delta.json`, and `artifacts.manifest.json` parse as valid JSON.
    - `192-SCORECARD.md` is regenerated from structured artifacts and summarizes pass/fail, coverage, regression count, CI guardrail status, and sign-off state.
    - `cd accrue_admin && npm run phase192:signoff` exits 0.
    - `node scripts/ci/verify_phase192_signoff.mjs` exits 0.
    - `192-SIGN-OFF.md` links the refreshed scorecard artifacts, manifest evidence refs, curated gallery, trace refs, CI guardrail status, and maintainer checklist.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && npm run phase192:scorecard</automated>
    <automated>node scripts/ci/verify_phase192_scorecard.mjs</automated>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson'; const rows=fs.readFileSync(p,'utf8').split(/\\r?\\n/).filter(Boolean); if (rows.length) throw new Error(`${p} has ${rows.length} regression row(s)`); console.log('phase192 regressions: zero')"</automated>
    <automated>node -e "for (const p of ['.planning/phases/192-idempotent-verification-sign-off/final.cells.json','.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json','.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json']) JSON.parse(require('fs').readFileSync(p,'utf8')); console.log('phase192 final json artifacts parse ok')"</automated>
    <automated>cd accrue_admin && npm run phase192:signoff</automated>
    <automated>node scripts/ci/verify_phase192_signoff.mjs</automated>
  </verify>
  <done>The final VER-02 and VER-04 artifacts are regenerated, parseable, verifier-clean, evidence-linked, and zero-regression before maintainer review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer screenshot and sign-off approval checkpoint</name>
  <read_first>
    - @.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md
    - @.planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md
    - @.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json
    - @.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson
  </read_first>
  <files>.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md</files>
  <action>Present the refreshed `192-SIGN-OFF.md` to the maintainer after Tasks 1 and 2 are green. The maintainer reviews executive status, baseline comparison summary, CI guardrail status, curated JTBD gallery, artifact manifest links, interaction trace refs, and checklist rows. Per D-29 through D-37, approval is on `192-SIGN-OFF.md`, not raw `test-results` or the full final-cell corpus. Per D-38 through D-41, the maintainer must explicitly consider JTBD clarity, domain vocabulary, microcopy recovery, brand fit, accessible focus/contrast, mobile usability, dark-mode role clarity, absence of backend-guts presentation, performance, responsive layout, theme behavior, interaction integrity, focus/hover/disabled affordance, information hierarchy, brand expression, and developer/operator DX. If the maintainer blocks sign-off, record the blocking regression ID, affected cell or gallery row, evidence ref, and required repair before rerunning Tasks 1 and 2 after the repair plan executes. If approved, record the approval state in the checklist or plan summary exactly as the maintainer provides it.</action>
  <acceptance_criteria>
    - Maintainer has reviewed `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`.
    - Maintainer has confirmed `regressions.ndjson` has zero rows or has blocked sign-off with named regression evidence.
    - Maintainer has reviewed the curated gallery rows and trace refs rather than relying on still screenshots alone.
    - Maintainer has given an explicit approval or block decision.
    - Approval closes VER-04 and v1.51 photographic-sign-off debt; a block creates a concrete repair target and prevents closeout.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_phase192_signoff.mjs</automated>
    <human-check>Open `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md`, inspect the linked scorecard/manifest/gallery/trace evidence, and reply `approved` to close sign-off or provide the exact block reason, evidence ref, and required repair.</human-check>
  </verify>
  <done>Maintainer gives explicit approval of the Phase 192 sign-off package, or the plan stops with a concrete blocker that must be repaired and rerun.</done>
</task>

</tasks>

<must_haves>
## Goal-Backward Truths

- A maintainer can see that the deterministic CI-equivalent guardrail boundary ran green before sign-off.
- A maintainer can see that the final scorecard is refreshed from structured artifacts and every comparable cell is greater than or equal to Phase 187 baseline with no coverage downgrade.
- A maintainer can see that `regressions.ndjson` has zero rows before approval.
- A maintainer can approve or block `192-SIGN-OFF.md` using curated screenshots, trace refs, manifest evidence refs, CI status, and explicit checklist rows.
- A block cannot be hidden by aggregate score, advisory model output, or prose summary.

## Required Artifacts

- `.planning/phases/192-idempotent-verification-sign-off/final.cells.json` exists and is refreshed.
- `.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json` exists and is refreshed.
- `.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson` exists and has zero rows for approval.
- `.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json` exists and references evidence without committing bulky binaries.
- `.planning/phases/192-idempotent-verification-sign-off/192-SCORECARD.md` exists and is regenerated from structured artifacts.
- `.planning/phases/192-idempotent-verification-sign-off/192-SIGN-OFF.md` exists, passes its verifier, and receives maintainer approval or a concrete block decision.

## Key Links

- `verify_phase192_admin_guardrails.sh` proves the deterministic VER-03 guardrail boundary.
- `phase192:scorecard` regenerates structured VER-02 artifacts.
- `verify_phase192_scorecard.mjs` proves zero regressions and strict Phase 187 comparison.
- `phase192:signoff` regenerates `192-SIGN-OFF.md`.
- `verify_phase192_signoff.mjs` proves the sign-off package is complete before the maintainer checkpoint.
</must_haves>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 192 | Full re-run, adversarial multi-lens judge, only-forward scorecard, CI guardrails, maintainer screenshot UAT | 192-06 | COVERED | This plan executes the final guardrail loop, regenerates artifacts, and gates closeout on maintainer approval. |
| REQ | VER-02 | Each level is scored by adversarial multi-lens judge and final scorecard is >= baseline with zero regressions | 192-06 | COVERED | Task 2 runs `phase192:scorecard`, `verify_phase192_scorecard.mjs`, and zero-row regression checks. |
| REQ | VER-03 | Regression guardrails run in CI: interaction e2e, axe a11y, reduced-motion, component-lab coverage | 192-06 | COVERED | Task 1 runs the Plan 192-03/192-04 guardrail contracts and serial guardrail runner. |
| REQ | VER-04 | Maintainer signs off on screenshots and final package, closing v1.51 photographic-sign-off debt | 192-06 | COVERED | Task 3 is a blocking maintainer approval checkpoint over `192-SIGN-OFF.md`. |
| RESEARCH | Pure reducer over evidence | Deterministic scorecard synthesis normalizes evidence to p187 cells and compares baseline strictly | 192-06 | COVERED | Task 2 regenerates and verifies the scorecard package from Plan 192-02. |
| RESEARCH | Bounded serial CI guardrail | Deterministic guardrails run serially and locally reproducibly | 192-06 | COVERED | Task 1 runs `verify_phase192_admin_guardrails.sh` and CI contracts. |
| RESEARCH | Maintainer sign-off | Sign-off belongs in planning artifacts as human review over curated evidence | 192-06 | COVERED | Task 3 obtains approval on `192-SIGN-OFF.md`. |
| CONTEXT | D-04-D-08 | Hybrid package, structured-data precedence, readable summary, aggregate scores summary-only | 192-06 | COVERED | Task 2 verifies canonical artifacts and derived markdown. |
| CONTEXT | D-09-D-12 | Preserve Phase 187 grammar and strict per-cell comparison | 192-06 | COVERED | Task 2 runs scorecard verifier and zero-regression checks. |
| CONTEXT | D-13-D-19 | Layered evidence, separated raw lens outputs, pure reducer, evidence refs, advisory visual scoring, axe not sufficient, explicit subjective accept/block | 192-06 | COVERED | Task 1 supplies deterministic lenses, Task 2 verifies evidence refs, Task 3 handles subjective approval. |
| CONTEXT | D-20-D-28 | Deterministic CI boundary and artifact upload separation; broad/advisory/final evidence not PR blockers | 192-06 | COVERED | Task 1 runs guardrail and CI contract verifiers without broad or advisory command substitution. |
| CONTEXT | D-29-D-37 | Sign-off on `192-SIGN-OFF.md`, curated gallery, trace refs, checklist | 192-06 | COVERED | Task 2 verifies package completeness; Task 3 obtains approval. |
| CONTEXT | D-38-D-41 | Operator-friendly domain language, brandbook voice, microcopy and UI sign-off categories | 192-06 | COVERED | Task 3 explicitly requires maintainer review of every category. |
| CONTEXT | D-42-D-45 | Inline fixes limited to harness/parser/artifact refs; true regressions block; no broken Phase 187 behavior as target | 192-06 | COVERED | Task 1 and Task 2 stop on blockers and route true regressions to repair reruns. |
| CONTEXT | Deferred portal/PhoenixStorybook/pixel-diff/host chrome scope | Out-of-scope ideas must not enter Phase 192 closeout | 192-06 | EXCLUDED | No task adds portal work, PhoenixStorybook, pixel-diff tooling, or host chrome redesign. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated evidence -> final guardrails | Browser, a11y, motion, component, and coverage outputs may be stale, malformed, incomplete, or failing. |
| Guardrail runner -> final scorecard | Final scorecard must only be trusted after deterministic guardrails and contracts pass. |
| Structured artifacts -> markdown sign-off | Markdown summaries and sign-off surfaces must remain derived from canonical JSON/NDJSON artifacts. |
| Human checkpoint -> milestone closeout | Maintainer approval is a human trust boundary and must be explicit, evidence-linked, and blockable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-06-01 | Tampering | Guardrail command boundary | mitigate | Run `verify_phase192_guardrail_contract.sh`, `verify_phase192_ci_contract.sh`, and `verify_phase192_admin_guardrails.sh`; do not substitute broad or advisory commands. |
| T-192-06-02 | Repudiation | Final scorecard comparison | mitigate | Run `verify_phase192_scorecard.mjs`, parse artifacts, and require zero rows in `regressions.ndjson` before human approval. |
| T-192-06-03 | Information Disclosure | Playwright screenshots, traces, and reports | mitigate | Keep bulky evidence in generated output/artifact roots and manifest refs; do not commit PNG, trace ZIP, report, or ZIP payloads. |
| T-192-06-04 | Tampering | `192-SCORECARD.md` and `192-SIGN-OFF.md` | mitigate | Regenerate markdown through npm scripts and verify with scorecard/sign-off verifiers so prose cannot override canonical data. |
| T-192-06-05 | Spoofing | Maintainer approval | mitigate | Use a blocking human checkpoint requiring an explicit `approved` response or exact block reason tied to evidence refs. |
| T-192-06-06 | Denial of Service | Full final verification loop | mitigate | Use the bounded serial runner from Plan 192-03/192-04 and stop on the first concrete blocker rather than launching broad all-spec E2E. |
| T-192-06-07 | Elevation of Privilege | CI/local scripts | mitigate | Execute only existing repo-local scripts and package commands created by prior Phase 192 plans; no new install or external service is introduced. |
| T-192-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install task exists; no new dependency or upgrade is allowed in this plan. |
</threat_model>

<verification>
Run, in order:

1. `bash scripts/ci/verify_phase192_guardrail_contract.sh`
2. `bash scripts/ci/verify_phase192_ci_contract.sh`
3. `bash scripts/ci/verify_phase192_admin_guardrails.sh`
4. `cd accrue_admin && npm run phase192:scorecard`
5. `node scripts/ci/verify_phase192_scorecard.mjs`
6. `node -e "const fs=require('fs'); const p='.planning/phases/192-idempotent-verification-sign-off/regressions.ndjson'; const rows=fs.readFileSync(p,'utf8').split(/\r?\n/).filter(Boolean); if (rows.length) throw new Error(`${p} has ${rows.length} regression row(s)`); console.log('phase192 regressions: zero')"`
7. `node -e "for (const p of ['.planning/phases/192-idempotent-verification-sign-off/final.cells.json','.planning/phases/192-idempotent-verification-sign-off/scorecard.delta.json','.planning/phases/192-idempotent-verification-sign-off/artifacts.manifest.json']) JSON.parse(require('fs').readFileSync(p,'utf8')); console.log('phase192 final json artifacts parse ok')"`
8. `cd accrue_admin && npm run phase192:signoff`
9. `node scripts/ci/verify_phase192_signoff.mjs`
10. Human check: maintainer reviews `192-SIGN-OFF.md` and replies `approved` or a concrete block reason with evidence ref.
</verification>

<success_criteria>
Plan 192-06 is complete when all deterministic guardrail, CI-contract, scorecard, zero-regression, JSON/NDJSON, and sign-off verifiers pass; the final artifacts are regenerated; `regressions.ndjson` has zero rows; `192-SIGN-OFF.md` contains the scorecard, curated gallery, manifest links, trace refs, CI status, and checklist; and the maintainer explicitly approves the sign-off package. Any guardrail failure, scorecard regression, missing evidence, sign-off verifier failure, or maintainer block prevents closeout until repaired and rerun.
</success_criteria>

<output>
Create `.planning/phases/192-idempotent-verification-sign-off/192-06-SUMMARY.md` when done.
</output>

---
phase: 193
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/guides/spec-overview.md
  - accrue_admin/guides/spec-list.md
  - accrue_admin/guides/spec-detail.md
  - accrue_admin/mix.exs
autonomous: true
requirements:
  - RES-01
  - STY-01
must_haves:
  truths:
    - "D-06: Three guide files exist at accrue_admin/guides/spec-overview.md, spec-list.md, spec-detail.md each with a kebab-case filename matching the motion.md precedent."
    - "D-07: All three guides appear in accrue_admin/mix.exs docs/0 extras AND in the Guides: group inside groups_for_extras."
    - "D-10: Each guide draws the line explicitly — items are listed as machine assertions ONLY where the page-flow driver / source-guards / axe-core can decide them deterministically (the invariant table); residual taste (visual hierarchy, brand voice, info-dump density, persona ordering, card-in-card) stays in the prose judge-graded section as 12-dim rubric material, never faked as assertions."
    - "D-11: Each guide contains its prescribed stable anchor heading (## SPEC-OVERVIEW — , ## SPEC-LIST — , ## SPEC-DETAIL — summary-then-drill) and a GOV.UK-style machine-checkable invariant table."
    - "D-08 prerequisite: The guide files exist on disk so Plan 05 can wire the verify_package_docs.sh needles and seed_tmp_dir! copies against real content."
    - "STY-01 partial: accrue_admin/mix.exs deps gains {:phoenix_storybook, ~> 1.2, only: [:dev, :test]} and elixirc_paths(:dev) is extended to ['lib', 'storybook/_support'] — prerequisite for Plan 04 Storybook scaffold."
  artifacts:
    - path: "accrue_admin/guides/spec-overview.md"
      provides: "SPEC-OVERVIEW design contract — attention-rail / task-launcher / KPI demotion archetype"
      contains: "## SPEC-OVERVIEW — "
    - path: "accrue_admin/guides/spec-list.md"
      provides: "SPEC-LIST design contract — table-first / four-state / chips+count+clear-all archetype"
      contains: "## SPEC-LIST — "
    - path: "accrue_admin/guides/spec-detail.md"
      provides: "SPEC-DETAIL design contract — summary-then-drill / action-menu / overlay contract archetype"
      contains: "## SPEC-DETAIL — summary-then-drill"
    - path: "accrue_admin/mix.exs"
      provides: "Extended docs/0 + deps + elixirc_paths"
      contains: "guides/spec-overview.md"
  key_links:
    - from: "accrue_admin/mix.exs extras"
      to: "accrue_admin/guides/spec-overview.md"
      via: "ExDoc extras list"
      pattern: "spec-overview.md"
    - from: "accrue_admin/mix.exs groups_for_extras Guides:"
      to: "accrue_admin/guides/spec-overview.md"
      via: "ExDoc groups_for_extras Guides: list"
      pattern: "spec-overview.md"
---

<objective>
Author the three archetype pattern specs as durable ExDoc guides and wire them into mix.exs exactly like the shipped motion.md precedent. Also add the phoenix_storybook dep and extend elixirc_paths(:dev) as prerequisites for Plan 04.

Purpose: RES-01 — The three locked archetype pattern specs are the design contracts that every Phase 194–200 page is built or conformed against. Downstream build agents must be able to read a single ExDoc-published guide to understand the full spec for their archetype. The mix.exs dep addition unblocks Plan 04's Storybook scaffold.

Output:
- accrue_admin/guides/spec-overview.md (SPEC-OVERVIEW guide with anchor heading + machine invariant table + prose judge section)
- accrue_admin/guides/spec-list.md (SPEC-LIST guide, same structure)
- accrue_admin/guides/spec-detail.md (SPEC-DETAIL guide, same structure)
- accrue_admin/mix.exs (3 spec paths added to extras + Guides:; {:phoenix_storybook} dep added; elixirc_paths(:dev) extended)
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-PATTERNS.md
@/Users/jon/projects/accrue/.planning/research/FEATURES.md
@/Users/jon/projects/accrue/.planning/research/ARCHITECTURE.md
@/Users/jon/projects/accrue/.planning/research/PITFALLS.md

<interfaces>
Spec guide anchor headings (exact strings — these are the require_fixed needles):
  spec-overview.md:  ## SPEC-OVERVIEW —
  spec-list.md:      ## SPEC-LIST —
  spec-detail.md:    ## SPEC-DETAIL — summary-then-drill

mix.exs docs/0 pattern to replicate (PATTERNS.md § accrue_admin/mix.exs):
  - Add "guides/spec-overview.md", "guides/spec-list.md", "guides/spec-detail.md" to extras AFTER "guides/motion.md"
  - Add the same three paths to Guides: in groups_for_extras AFTER "guides/motion.md"

mix.exs dep addition (PATTERNS.md § mix.exs dep):
  {:phoenix_storybook, "~> 1.2", only: [:dev, :test]},

mix.exs elixirc_paths extension (RESEARCH.md Open Question #3 — concrete fix):
  defp elixirc_paths(:dev), do: ["lib", "storybook/_support"]
  defp elixirc_paths(:test), do: ["lib", "storybook/_support", "test/support"]
  defp elixirc_paths(_), do: ["lib"]

Source for guide content (use the D-11 invariant split from CONTEXT.md as the seed; prose directions from FEATURES.md archetypes A/B/C):
  SPEC-OVERVIEW machine invariants: exactly one h1; ⌘K trigger present + focusable; KPI cluster is DOM-sibling after attention-rail; healthy/empty attention-rail renders non-interactive hero
  SPEC-LIST machine invariants: renders 4 distinct states; filter chips + result count + clear-all present when filter active; truncating cell pairs ellipsis with min-width:0; no pager when pages≤1
  SPEC-DETAIL machine invariants: ≤2 primary action buttons + overflow menu; action forms NOT pre-expanded; exactly one related-resources strip; overlay/drawer hit-testable above scrim
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author three archetype spec guides</name>
  <files>accrue_admin/guides/spec-overview.md, accrue_admin/guides/spec-list.md, accrue_admin/guides/spec-detail.md</files>
  <read_first>
    - accrue_admin/guides/motion.md — the exact structural analog: intro prose → section headings → enforcement/invariant table; this is the style to replicate
    - .planning/phases/193-research-re-baseline-pattern-lock/193-CONTEXT.md — D-09 through D-12 for the layered spec format; D-11 for the concrete machine/prose invariant split per archetype
    - .planning/research/FEATURES.md — sections A.1–A.3, B.1–B.3, C.1–C.3 for prose seed content per archetype
    - .planning/research/ARCHITECTURE.md — IXN acceptance criteria R-1..R-6 for the SPEC-DETAIL overlay invariant
    - .planning/research/PITFALLS.md — the 9 bug classes relevant to SPEC-DETAIL and SPEC-LIST anti-patterns
  </read_first>
  <action>
    Create three new files. Each follows the motion.md structural pattern: H1 title, short intro paragraph, then the stable anchor heading, then the machine-checkable invariant table, then the prose judge-graded items section.

    For spec-overview.md:
    - H1: "Overview Page Spec (SPEC-OVERVIEW)"
    - Intro: describe the exceptions-first / task-launcher / demoted-KPI archetype; audience is build agents 194–200, the maintainer, and adopters extending the admin UI
    - Stable anchor heading: "## SPEC-OVERVIEW — exceptions-first, tasks-as-doors" (the require_fixed needle searches for "## SPEC-OVERVIEW — " so any suffix after the em-dash satisfies it; pick a descriptive suffix)
    - Machine invariant table: four rows per D-11: (1) exactly one h1, (2) ⌘K trigger present and focusable, (3) KPI cluster is a DOM sibling after the attention-rail zone not a first child, (4) healthy/empty attention-rail renders a non-interactive hero (no cursor:pointer or role=button on the empty state container)
    - Prose section: "Judge-graded criteria (12-dim rubric)": exceptions read as higher-signal than KPIs; KPIs demoted not deleted; Recovery reads as a work queue; no chart wall

    For spec-list.md:
    - H1: "List Page Spec (SPEC-LIST)"
    - Intro: table-first dense-list archetype; audience same
    - Stable anchor heading: "## SPEC-LIST — table-first, four-state, chips-count-clear" (needle prefix "## SPEC-LIST — " is what matters)
    - Machine invariant table per D-11: (1) renders 4 distinct states (populated/first-run-empty/filtered-empty/loading) with distinct copy strings, (2) filter chips + result count + clear-all all present when a filter is active, (3) every truncating cell pairs text-overflow:ellipsis with min-width:0 in the same CSS block (new source-guard enforces this), (4) no pager rendered when pages ≤ 1
    - Prose section: column priority serves identity·state·money·time; "deliberate dense" padding rhythm; work-queue default

    For spec-detail.md:
    - H1: "Detail Page Spec (SPEC-DETAIL)"
    - Intro: summary-then-drill / GOV.UK-summary-list archetype; audience same
    - Stable anchor heading: "## SPEC-DETAIL — summary-then-drill" (this is the EXACT string the require_fixed needle requires — do not alter it)
    - Machine invariant table per D-11: (1) ≤2 primary action buttons + an overflow menu, (2) action forms NOT pre-expanded on load (visible action-band form count == 0 until menu invoked), (3) exactly one related-resources strip (no duplicate), (4) overlay/drawer is hit-testable above its scrim (assertTopPointerTarget) with body scroll locked
    - Prose section: summary-list answers "what state, what's wrong" at a glance; no card-in-card double-border; tabs only for peer record-sets

    All three files MUST use GOV.UK-style published acceptance criteria language in the machine invariant tables (per D-12). Format each machine invariant as a two-column markdown table with columns "Invariant" and "How verified". Reference the page-flow Playwright helpers or source guards in the "How verified" column where applicable.

    Do NOT include fenced code blocks containing implementation code in the guide prose — narrative examples only.
  </action>
  <verify>
    <automated>grep -c "## SPEC-OVERVIEW — " accrue_admin/guides/spec-overview.md && grep -c "## SPEC-LIST — " accrue_admin/guides/spec-list.md && grep -c "## SPEC-DETAIL — summary-then-drill" accrue_admin/guides/spec-detail.md</automated>
  </verify>
  <done>All three files exist; each contains exactly one stable anchor heading matching the require_fixed needle pattern; each has a machine invariant table and a prose judge-graded section per D-09/D-10/D-11.</done>
</task>

<task type="auto">
  <name>Task 2: Wire spec guides into mix.exs (extras, groups, dep, elixirc_paths)</name>
  <files>accrue_admin/mix.exs</files>
  <read_first>
    - accrue_admin/mix.exs — read the full docs/0 block (extras + groups_for_extras) and the existing deps block and elixirc_paths clauses; PATTERNS.md § mix.exs shows current structure at lines 65–80 and the dep/elixirc_paths changes
  </read_first>
  <action>
    Make four distinct changes to accrue_admin/mix.exs via targeted Edit calls:

    1. Add the three spec guide paths to docs/0 extras — append after "guides/motion.md" inside the extras list:
       "guides/spec-overview.md",
       "guides/spec-list.md",
       "guides/spec-detail.md",

    2. Add the same three paths to the Guides: group in groups_for_extras — append after "guides/motion.md" in the Guides: list:
       "guides/spec-overview.md",
       "guides/spec-list.md",
       "guides/spec-detail.md",

    3. Add phoenix_storybook dep to deps/0 alongside other only: [:dev, :test] deps (per D-13 / STY-01):
       {:phoenix_storybook, "~> 1.2", only: [:dev, :test]},

    4. Extend elixirc_paths/1 (per RESEARCH.md Open Question #3 — required for storybook/_support/registry_story.ex to compile):
       - Change the :dev clause from ["lib"] to ["lib", "storybook/_support"]
       - Change the :test clause from ["lib", "test/support"] to ["lib", "storybook/_support", "test/support"]
       - The catch-all clause ["lib"] stays unchanged

    Use Edit for each change (not Write). Preserve all other content exactly.

    CRITICAL: Both extras AND groups_for_extras must get the three new paths. Adding to only one causes ExDoc to display guides under "Pages" not "Guides" (Pitfall 6 from RESEARCH.md).
  </action>
  <verify>
    <automated>grep -c '"guides/spec-overview.md"' accrue_admin/mix.exs && grep -c '"guides/spec-list.md"' accrue_admin/mix.exs && grep -c '"guides/spec-detail.md"' accrue_admin/mix.exs && grep -c ':phoenix_storybook' accrue_admin/mix.exs && grep -c 'storybook/_support' accrue_admin/mix.exs</automated>
  </verify>
  <done>mix.exs has each spec guide path appearing exactly twice (once in extras, once in Guides: group_for_extras); :phoenix_storybook dep present with only: [:dev, :test]; elixirc_paths(:dev) includes "storybook/_support".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → ExDoc guides | Static markdown files authored by Claude executor; no untrusted input crosses |
| mix.exs edit → compiler | Config changes are compile-time; evaluated by Mix, not a web request boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-01-SC | Tampering | phoenix_storybook hex dep install | high | mitigate | Package Legitimacy Audit in RESEARCH.md confirmed OK (hex.pm verified, 2+ year history, 1.2.0 released 2026-06-11); no [ASSUMED]/[SUS] packages in this plan |
| T-193-02 | Information Disclosure | spec guide content in Hex tarball | low | accept | guides/ is already in package.files; content is design documentation intended for adopters — this is the desired behavior per D-06/D-12 |
</threat_model>

<verification>
After both tasks:
1. `grep -c "## SPEC-OVERVIEW — " accrue_admin/guides/spec-overview.md` → 1
2. `grep -c "## SPEC-LIST — " accrue_admin/guides/spec-list.md` → 1
3. `grep -c "## SPEC-DETAIL — summary-then-drill" accrue_admin/guides/spec-detail.md` → 1
4. `grep -c '"guides/spec-overview.md"' accrue_admin/mix.exs` → 2 (extras + groups)
5. `grep -c '"guides/spec-list.md"' accrue_admin/mix.exs` → 2
6. `grep -c '"guides/spec-detail.md"' accrue_admin/mix.exs` → 2
7. `grep -c ':phoenix_storybook' accrue_admin/mix.exs` → 1
8. `grep -c 'storybook/_support' accrue_admin/mix.exs` → at least 2 (dev + test clauses)
9. `bash scripts/ci/verify_package_docs.sh` → passes (guards added in Plan 05 will catch regressions then; today's existing guards must still pass)
</verification>

<success_criteria>
- Three archetype spec guides exist as ExDoc guides under accrue_admin/guides/ with correct anchor headings
- Each guide has a machine-checkable invariant table (GOV.UK-style) and a prose judge-graded section
- mix.exs extras and groups_for_extras both list all three spec guides
- mix.exs deps includes {:phoenix_storybook, "~> 1.2", only: [:dev, :test]}
- elixirc_paths(:dev) and :test include "storybook/_support"
- Existing package docs CI gate still passes
</success_criteria>

<output>
Create .planning/phases/193-research-re-baseline-pattern-lock/193-01-SUMMARY.md when done
</output>

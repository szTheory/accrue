---
phase: 193
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json
autonomous: true
requirements:
  - RES-02
must_haves:
  truths:
    - "D-16: An additive sibling file baseline.page-flow.cells.json exists next to the immutable baseline.cells.json with surface_type:page-flow cells covering the ~20 admin routes from baseline-manifest.js PAGE_FLOWS."
    - "The existing baseline.cells.json (21,276 cells) is NOT modified — additive sibling, clean provenance."
    - "Each cell uses the p193 prefix in cell_id, surface_type page-flow, coverage_status pending, and the 12-dimension + 9-state (FLOW_STATES) cross product per PROJECTS (chromium-desktop + chromium-mobile) × THEMES (light + dark)."
    - "The cell schema exactly matches the confirmed baseline.cells.json schema from RESEARCH.md: cell_id, surface, surface_type, mode, viewport_width, theme, state, dimension, dimension_name, score, coverage_status, evidence_refs, notes, targeted_label, breakpoint."
  artifacts:
    - path: ".planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json"
      provides: "Page-flow scored cell baseline for ~20 admin routes; additive sibling to baseline.cells.json"
      contains: "page-flow"
  key_links:
    - from: "baseline.page-flow.cells.json"
      to: "accrue_admin/e2e/phase192-scorecard.mjs baselinePath"
      via: "Union-load for regressions.ndjson gate in Phase 200"
      pattern: "baseline.page-flow.cells.json"
---

<objective>
Generate the additive sibling baseline.page-flow.cells.json covering the ~20 admin routes from baseline-manifest.js PAGE_FLOWS, extending the forward-only zero-regression gate to include composed page surfaces.

Purpose: RES-02 — The Phase-187 baseline only scored component and component-group surfaces. Phase 193 adds page-flow cells so Phase 200 can confirm zero regressions across entire composed pages, not just isolated components. The cell file is created with coverage_status: "pending" (not scored yet); Phase 200 will score them and fold into the regressions.ndjson gate.

Output:
- .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json
  (valid JSON array of cell objects; one cell per surface × project × theme × state × dimension combination)
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-RESEARCH.md

<interfaces>
Source of truth for page surfaces:
  accrue_admin/e2e/baseline-manifest.js — PAGE_FLOWS array (lines 83–155): 22 surfaces (11 list + 11 detail routes)
  Each surface defined as [surface_slug, route, persona_job, optional_routeBuilder]
  PROJECTS: [{name: "chromium-desktop", mode: "chromium-desktop", viewport_width: 1440}, {name: "chromium-mobile", mode: "chromium-mobile", viewport_width: 390}]
  THEMES: ["light", "dark"]
  FLOW_STATES: ["default-populated", "empty", "loading", "error", "permission-denied", "disconnected-reconnecting", "overflow", "long-content", "interactive-open"]  (9 states — NOT "disabled-readonly")
  DIMENSIONS: 12 dimensions, id 1–12

Cell schema (RESEARCH.md § Forward-only baseline machinery):
  cell_id: "p193__{surface}__{mode}__{theme}__{state}__d{NN}"  (slug separators __)
  surface: the surface slug from PAGE_FLOWS (e.g. "dashboard", "subscriptions-list")
  surface_type: "page-flow"
  mode: project.mode (chromium-desktop or chromium-mobile)
  viewport_width: project.viewport_width (1440 or 390)
  theme: "light" or "dark"
  state: one of FLOW_STATES
  dimension: dimension id integer (1–12)
  dimension_name: dimension name string (from DIMENSIONS)
  score: null  (not scored yet)
  coverage_status: "pending"
  evidence_refs: []
  notes: "Awaiting Phase 193 page-flow baseline capture."
  targeted_label: "{surface} page-flow d{NN}"
  breakpoint: "desktop" when viewport_width >= 1024 else "mobile"

cell_id slug function: lowercase, replace non-alphanumeric with hyphens, trim leading/trailing hyphens
  (match the slug() function in baseline-manifest.js)

NOTE on "page-flow" prefix: the existing baseline.cells.json uses "p187" as the prefix.
Use "p193" as the prefix for all new page-flow cells to distinguish provenance.

Approximate cell count: 22 surfaces × 2 projects × 2 themes × 9 states × 12 dimensions = 9,504 cells
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate baseline.page-flow.cells.json</name>
  <files>.planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json</files>
  <read_first>
    - accrue_admin/e2e/baseline-manifest.js — read the full PAGE_FLOWS array (lines 83–155), PROJECTS, THEMES, FLOW_STATES, DIMENSIONS; this is the canonical source for all surfaces and their metadata
    - .planning/milestones/v1.53-phases/187-audit-baseline/baseline.cells.json — read the first 2–3 cell objects to confirm the exact JSON field names and ordering used in the existing baseline; the new file must use the same schema
  </read_first>
  <action>
    Write a Node.js script (inline Bash execution, not a committed file) that:
    1. Reads PAGE_FLOWS, PROJECTS, THEMES, FLOW_STATES, DIMENSIONS from baseline-manifest.js
    2. Generates all cells using the cross-product: PAGE_FLOWS × PROJECTS × THEMES × FLOW_STATES × DIMENSIONS
    3. For each cell, applies the slug() function to produce the cell_id components (lowercase, replace [^a-z0-9]+ with hyphens, trim)
    4. Sets cell_id prefix to "p193" (not "p187")
    5. Sets score: null, coverage_status: "pending", evidence_refs: [], notes: "Awaiting Phase 193 page-flow baseline capture."
    6. Sets breakpoint: "desktop" for viewport_width >= 1024, else "mobile"
    7. Writes the result as a JSON array to .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json

    Then run the script via Bash and verify the output file exists with the expected cell count (~9,504 cells for 22 surfaces × 2 projects × 2 themes × 9 states × 12 dims).

    CRITICAL: Do NOT modify baseline.cells.json (the existing p187 file). Write ONLY to baseline.page-flow.cells.json.

    Verify the first cell object in the output file has all required fields: cell_id, surface, surface_type, mode, viewport_width, theme, state, dimension, dimension_name, score, coverage_status, evidence_refs, notes, targeted_label, breakpoint.
  </action>
  <verify>
    <automated>node -e "const c = require('./.planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json'); console.log('cells:', c.length); const sample = c[0]; const required = ['cell_id','surface','surface_type','mode','viewport_width','theme','state','dimension','dimension_name','score','coverage_status','evidence_refs','notes','targeted_label','breakpoint']; required.forEach(k => { if (!(k in sample)) throw new Error('missing field: ' + k); }); console.log('schema OK'); const pf = c.filter(x => x.surface_type === 'page-flow'); console.log('page-flow cells:', pf.length); const prefix = c.filter(x => x.cell_id.startsWith('p193')); console.log('p193 prefix:', prefix.length);"</automated>
  </verify>
  <done>
    - File exists at .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json
    - All cells have surface_type: "page-flow" and cell_id prefix "p193"
    - All 16 required schema fields present in every cell
    - Cell count is ~9,504 (22 surfaces × 2 projects × 2 themes × 9 states × 12 dimensions)
    - Original baseline.cells.json is unmodified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline.page-flow.cells.json → phase192-scorecard.mjs | Baseline file is read by the scorecard script; cells with coverage_status: "pending" are treated as not-yet-scored and do not produce regression rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-03 | Tampering | baseline.cells.json mutation | medium | mitigate | executor must write ONLY to baseline.page-flow.cells.json; verify baseline.cells.json line count is unchanged after task completion |
| T-193-04 | Information Disclosure | planning artifacts in committed repo | low | accept | .planning/ is an internal planning directory, not part of published Hex tarball; no sensitive information in cell schema |
</threat_model>

<verification>
After task:
1. File exists: `ls .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json`
2. Cell count check: node inline evaluation → ~9,504 cells, all surface_type: "page-flow", all cell_id prefixed "p193"
3. Schema check: all 16 required fields present in first cell
4. Immutability check: `wc -l .planning/milestones/v1.53-phases/187-audit-baseline/baseline.cells.json` output unchanged from pre-task value
</verification>

<success_criteria>
- baseline.page-flow.cells.json exists as an additive sibling to baseline.cells.json
- ~9,504 page-flow cells covering the 22 PAGE_FLOWS surfaces from baseline-manifest.js
- All cells use cell_id prefix "p193", surface_type "page-flow", coverage_status "pending"
- All 16 required schema fields present
- Existing baseline.cells.json is NOT modified
</success_criteria>

<output>
Create .planning/phases/193-research-re-baseline-pattern-lock/193-02-SUMMARY.md when done
</output>

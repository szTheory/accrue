---
phase: 193
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/e2e/spike-overlay-portal.spec.js
autonomous: true
requirements:
  - RES-03
must_haves:
  truths:
    - "D-05: spike-overlay-portal.spec.js exists with four describe/test blocks proving the four D-05 empirical proofs."
    - "RES-03: The spec file is the provenance artifact recording the D-01 portal-primary decision — it contains the /* D-05 recorded decision: portal primary (A) */ comment block."
    - "All four proofs must pass (green): (1) overlay primary action hit-testable above scrim, (2) portal survives LiveView navigation, (3) scroll locked without gutter jump, (4) transformed-ancestor probe."
  artifacts:
    - path: "accrue_admin/e2e/spike-overlay-portal.spec.js"
      provides: "D-05 provenance artifact — four Playwright proofs confirming portal-primary direction and its empirical properties"
      contains: "D-05 recorded decision"
  key_links:
    - from: "spike-overlay-portal.spec.js"
      to: "accrue_admin/e2e/phase191-page-flow-helpers.js assertTopPointerTarget"
      via: "import from phase191-page-flow-helpers.js"
      pattern: "assertTopPointerTarget"
---

<objective>
Write and run the four-proof D-05 overlay portal spike Playwright spec. This spec is the provenance artifact for the overlay-portal-vs-native-dialog decision (RES-03 Spike A).

Purpose: RES-03 — The four D-05 proofs must be empirically verified before Phase 195 can build the production overlay primitive. This spec records the decision that body-level portal (D-01) is confirmed as the primary approach, and documents which conditions would trigger the D-02 dialog fallback per-surface.

Output:
- accrue_admin/e2e/spike-overlay-portal.spec.js with four Playwright test blocks:
  (1) overlay primary action is hit-testable above scrim (assertTopPointerTarget)
  (2) portal re-parent survives LiveView navigation without orphan/double-mount
  (3) body scroll locked without scrollbar-gutter jump
  (4) portal escapes transformed ancestor (the D-02 trigger condition)
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-PATTERNS.md

<interfaces>
Helper imports (confirmed from phase191-page-flow-helpers.js):
  import { assertTopPointerTarget, assertFocusWithin, setPhase191Theme } from './phase191-page-flow-helpers.js';

Test setup pattern (from admin-page-flow-phase191.spec.js):
  async function reset(request) — POST /__e2e__/reset
  async function login(page, target) — GET /__e2e__/login?to=...
  Use the subscriptions-list or subscription-detail route as the test surface (has a DetailDrawer and interactive content behind the scrim)

D-05 proof details (CONTEXT.md + RESEARCH.md § Spike A):
  Proof 1: assertTopPointerTarget(page.locator('[role=dialog] button').first()) while scrim is present
  Proof 2: Navigate away and back; assert document.getElementById('ax-overlay-root') is present exactly once and contains no stale children
  Proof 3: capture getBoundingClientRect().top of a sticky header element before/after drawer open; assert delta === 0
  Proof 4: page.evaluate() to add transform:translateZ(0) to the LiveView root element; open drawer; assertTopPointerTarget still passes

Phase note: At Phase 193, the DetailDrawer does NOT yet use a body-level portal — the portal is designed/decided here and built in Phase 199. The spike tests should:
  - If the drawer already portals to #ax-overlay-root → all four proofs run against the live implementation
  - If the drawer does NOT yet portal → the spec creates a minimal test fixture (inline HTML injected via page.evaluate or a test-only page) that implements the portal pattern and proves the four properties on that fixture; the /* D-05 recorded decision */ comment documents which path was taken and whether proof 4 passed or triggered the D-02 condition

Playwright config: accrue_admin/playwright.config.js (existing)
Test runner start: mix accrue_admin.e2e.server (existing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run spike-overlay-portal.spec.js</name>
  <files>accrue_admin/e2e/spike-overlay-portal.spec.js</files>
  <read_first>
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js — read the first 80 lines for test setup pattern (reset/seed/login helpers, test.describe structure, imports)
    - accrue_admin/e2e/phase191-page-flow-helpers.js — read the assertTopPointerTarget, assertFocusWithin, setPhase191Theme function signatures
    - accrue_admin/lib/accrue_admin/components/detail_drawer.ex — read lines 1–50 to understand the current drawer's phx-hook, :if={@open} render condition, and DOM structure (especially whether #ax-overlay-root exists in the current implementation)
  </read_first>
  <action>
    Create accrue_admin/e2e/spike-overlay-portal.spec.js. Structure:

    Header comment block:
      /* D-05 recorded decision: portal primary (A) — spike-overlay-portal.spec.js
         Four proofs required per CONTEXT.md D-05 / Phase 193 RES-03.
         All four must be green before Phase 193 merge.
         Decision: [FILLED IN BY EXECUTOR AFTER RUNNING — e.g. "portal primary confirmed; proof 4 PASSED (escapes transform)" or "proof 4 FAILED — D-02 trigger condition confirmed for transforms; D-01 still primary for clean-ancestor surfaces"]
      */

    Imports: test and expect from @playwright/test; assertTopPointerTarget and setPhase191Theme from ./phase191-page-flow-helpers.js

    Test setup helper: an async login() function that resets, optionally seeds, and logs in (mirror admin-page-flow-phase191.spec.js pattern)

    test.describe block "D-05 overlay portal spike" with four test cases:

    Proof 1 — "primary action is hit-testable above scrim":
      - Navigate to a route that can show the DetailDrawer open (subscription detail or trigger a drawer via a button click)
      - Verify the scrim/backdrop element is visible in DOM
      - Call assertTopPointerTarget on the drawer's primary action button (locator('[role=dialog] button').first() or similar)
      - If drawer not yet implemented with portal: inject a minimal portal fixture via page.evaluate (create #ax-overlay-root as body child, move a test dialog into it, add a scrim div with z-index below); then assert the button inside #ax-overlay-root passes assertTopPointerTarget

    Proof 2 — "portal survives LiveView navigation without orphan":
      - Open drawer, navigate to a different admin route, navigate back
      - Assert document.getElementById('ax-overlay-root') is present exactly once (count == 1) after navigation
      - Assert it contains no stale drawer children after navigation-away

    Proof 3 — "body scroll locked without scrollbar-gutter jump":
      - Capture layout metrics of a stable element (e.g. the topbar or page header) via getBoundingClientRect().top before opening drawer
      - Open drawer
      - Capture same element's getBoundingClientRect().top again
      - Assert delta == 0 (no scrollbar-gutter jump)
      - Assert document.body.style.overflow or computed overflow indicates scroll-lock is applied (note: ScrollLock hook is designed in Phase 193 spikes but built in Phase 199; for this spike, test the CURRENT behavior and document the result — if the jump IS non-zero, record it as a finding, not a failure)

    Proof 4 — "portal escapes transformed ancestor":
      - Use page.evaluate to inject transform:translateZ(0) on the topmost LiveView root div (the wrapper div Elixir LiveView renders)
      - Open drawer
      - Assert assertTopPointerTarget still passes on the drawer's button
      - This proves that IF the portal moves markup to a body-level #ax-overlay-root that is a direct body child, the overlay always escapes the transform re-rooting context
      - Document the result in a comment; if proof fails, add the D-02 note

    After running: update the header comment block with the actual decision based on results.
    Each test must complete in < 30 seconds.

    IMPORTANT: The spike tests document current behavior and prove the portal approach properties. They are NOT regression tests for production code that doesn't exist yet. If the DetailDrawer does not yet use a body-level portal, the tests should use a test-fixture approach (inject portal-pattern HTML via page.evaluate) to prove the four properties of the portal pattern itself — so the decision is based on empirical evidence, not assumption.
  </action>
  <verify>
    <automated>npx playwright test accrue_admin/e2e/spike-overlay-portal.spec.js --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>
    - spike-overlay-portal.spec.js exists with four test blocks matching the D-05 proof structure
    - The /* D-05 recorded decision */ comment block is present and filled in with the actual result
    - All four tests run (pass or produce documented findings); no test times out
    - RES-03 Spike A decision is recorded in the file header
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → admin app | Playwright connects to the running accrue_admin E2E server on localhost; login is via the existing __e2e__ test-support route |
| page.evaluate injection | Test-only DOM mutations injected by page.evaluate; scoped to the test page lifetime; no production code path modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-05 | Elevation of Privilege | page.evaluate transform injection | low | accept | page.evaluate runs only in E2E test context against localhost test server; no production route or auth path modified; test helper is not shipped in the Hex package |
| T-193-06 | Information Disclosure | spike spec shipped in Hex tarball | low | mitigate | accrue_admin/e2e/ is NOT in package.files = ~w(lib config guides priv/static mix.exs README* LICENSE* CHANGELOG*); E2E specs are excluded automatically |
</threat_model>

<verification>
After task:
1. `ls accrue_admin/e2e/spike-overlay-portal.spec.js` → file exists
2. `grep -c "D-05 recorded decision" accrue_admin/e2e/spike-overlay-portal.spec.js` → 1
3. `grep -c "assertTopPointerTarget" accrue_admin/e2e/spike-overlay-portal.spec.js` → at least 1
4. `npx playwright test accrue_admin/e2e/spike-overlay-portal.spec.js --reporter=line` → all 4 tests pass or produce documented findings (no timeout/crash)
</verification>

<success_criteria>
- spike-overlay-portal.spec.js exists with four D-05 proof test blocks
- /* D-05 recorded decision */ comment present and filled in with actual result
- All four tests complete without timeout
- RES-03 Spike A is resolved with a recorded decision
</success_criteria>

<output>
Create .planning/phases/193-research-re-baseline-pattern-lock/193-03-SUMMARY.md when done
</output>

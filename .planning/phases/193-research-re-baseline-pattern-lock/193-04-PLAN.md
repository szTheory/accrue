---
phase: 193
plan: "04"
type: execute
wave: 2
depends_on:
  - "193-01"
files_modified:
  - accrue_admin/lib/accrue_admin/dev/storybook.ex
  - accrue_admin/lib/accrue_admin/router.ex
  - accrue_admin/lib/accrue_admin/assets.ex
  - storybook/_support/registry_story.ex
  - storybook/components/button.story.exs
  - accrue_admin/priv/static/storybook.css
  - accrue_admin/priv/static/storybook.js
autonomous: true
requirements:
  - STY-01
  - RES-03
must_haves:
  truths:
    - "D-13: AccrueAdmin.Dev.Storybook backend module exists at accrue_admin/lib/accrue_admin/dev/storybook.ex, wrapped in if Mix.env() != :prod do."
    - "D-13: wrap_with_storybook_dev_routes/3 is added to router.ex chaining after wrap_with_mailglass_dev_routes; the Code.ensure_loaded?(PhoenixStorybook.Router) guard is present INSIDE the true-branch quote block."
    - "D-13: AccrueAdmin.Assets gains storybook_css and storybook_js kind atoms, module attributes (@storybook_css_file/@storybook_js_file/@external_resource/@storybook_css_hash/@storybook_js_hash), and storybook_css_hash/0 + storybook_js_hash/0 public functions."
    - "D-13: storybook/_support/registry_story.ex compiles (it is in elixirc_paths(:dev) from Plan 01) and AccrueAdmin.Storybook.RegistryStory.variations_for/1 works."
    - "D-13: One PoC button.story.exs exists at storybook/components/; it delegates to RegistryStory.variations_for('button')."
    - "STY-01: host-absence compile test passes — examples/accrue_host compiles in both MIX_ENV=dev and MIX_ENV=prod with phoenix_storybook absent, no /dev/storybook route exposed."
    - "D-03 (rationale honored): portal is primary because dialog.showModal() fights morphdom; the rationale is documented in storybook.ex or a source comment so Phase 195/199 implementors understand the decision."
    - "D-04 (constraint honored): the swap-seam is kept clean — shell markup is isolated behind the primitive component boundary in detail_drawer.ex (not touched in Phase 193) so Phase 199 can flip individual surfaces to dialog fallback without touching call sites."
    - "D-14 (deferral honored): Phase 193 ships ONLY the one button.story.exs PoC — the remaining ~13 families + 8 group contracts + both-color-mode theming verification are explicitly deferred to Phase 200 per D-14."
    - "D-15 (constraint honored): AccrueAdmin.Dev.ComponentRegistry stays SSOT; the /dev/components kitchen and its data-ax-* drift-test locators are NOT touched in this plan — Storybook is added as a second renderer only."
    - "D-17 Spikes B/C/D: The three spike decisions (dark-mode shim, inert browser-floor, asset-serving without Tailwind) are each recorded as a comment in the relevant source file (storybook.css, storybook.ex, or router.ex)."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/dev/storybook.ex"
      provides: "PhoenixStorybook backend module (dev/test-only)"
      contains: "AccrueAdmin.Dev.Storybook"
    - path: "accrue_admin/lib/accrue_admin/router.ex"
      provides: "wrap_with_storybook_dev_routes/3 with Code.ensure_loaded? guard"
      contains: "Code.ensure_loaded?(PhoenixStorybook.Router)"
    - path: "accrue_admin/lib/accrue_admin/assets.ex"
      provides: "Storybook asset kinds (storybook_css/storybook_js) with committed-bundle serving"
      contains: "storybook_css_hash"
    - path: "storybook/_support/registry_story.ex"
      provides: "AccrueAdmin.Storybook.RegistryStory.variations_for/1 — registry→Variation pipeline"
      contains: "RegistryStory"
    - path: "storybook/components/button.story.exs"
      provides: "PoC proof story delegating to RegistryStory"
      contains: "AccrueAdmin.Storybook.RegistryStory.variations_for"
    - path: "accrue_admin/priv/static/storybook.css"
      provides: "Committed storybook CSS bundle (PSB sandbox CSS + accrue_admin bundle + dark-mode shim)"
      contains: "ax-theme-dark-shim"
    - path: "accrue_admin/priv/static/storybook.js"
      provides: "Committed storybook JS bundle (PhoenixStorybook client JS)"
      contains: ""
  key_links:
    - from: "accrue_admin/lib/accrue_admin/dev/storybook.ex css_path"
      to: "accrue_admin/lib/accrue_admin/assets.ex storybook_css_hash"
      via: "AccrueAdmin.Assets.storybook_css_hash() call in backend config"
      pattern: "storybook_css_hash"
    - from: "accrue_admin/lib/accrue_admin/router.ex wrap_with_storybook_dev_routes"
      to: "accrue_admin/lib/accrue_admin/dev/storybook.ex"
      via: "live_storybook backend_module: AccrueAdmin.Dev.Storybook"
      pattern: "AccrueAdmin.Dev.Storybook"
    - from: "storybook/components/button.story.exs"
      to: "storybook/_support/registry_story.ex"
      via: "AccrueAdmin.Storybook.RegistryStory.variations_for/1 delegation"
      pattern: "RegistryStory.variations_for"
---

<objective>
Stand up the PhoenixStorybook walking skeleton: Storybook backend module, leak-proof router wrap, committed-bundle asset serving, RegistryStory generator, and one PoC button story. Also record the three D-17 spike decisions.

Purpose: STY-01 + RES-03 Spikes B/C/D — The scaffold proves the registry→Variation pipeline end-to-end, resolves the three remaining spike questions (dark-mode shim, inert floor, asset-serving), and establishes the dev/test-only isolation contract so adopters never carry PhoenixStorybook at runtime.

Output:
- accrue_admin/lib/accrue_admin/dev/storybook.ex (env-guarded backend module)
- accrue_admin/lib/accrue_admin/router.ex (wrap_with_storybook_dev_routes added)
- accrue_admin/lib/accrue_admin/assets.ex (storybook CSS/JS kinds added)
- storybook/_support/registry_story.ex (compiled generator, dev/test only)
- storybook/components/button.story.exs (PoC proof story)
- accrue_admin/priv/static/storybook.css (committed bundle)
- accrue_admin/priv/static/storybook.js (committed bundle)
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/accrue/.planning/PROJECT.md
@/Users/jon/projects/accrue/.planning/ROADMAP.md
@/Users/jon/projects/accrue/.planning/STATE.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-CONTEXT.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-RESEARCH.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-PATTERNS.md
@/Users/jon/projects/accrue/.planning/phases/193-research-re-baseline-pattern-lock/193-01-SUMMARY.md

<interfaces>
router.ex wrap-chain site (RESEARCH.md § AccrueAdmin.Router, confirmed lines 107–108):
  The Mailglass chain is at: |> wrap_with_mailglass_dev_routes(dev_routes?, mount_path)
  Add: |> wrap_with_storybook_dev_routes(dev_routes?, mount_path)  immediately after

wrap_with_storybook_dev_routes/3 pattern (PATTERNS.md § router.ex):
  - Same shape as wrap_with_mailglass_dev_routes/3 (true-branch + no-op fallback)
  - MANDATORY: if Code.ensure_loaded?(PhoenixStorybook.Router) do ... end wraps the true-branch quote
  - import PhoenixStorybook.Router INSIDE the quote bind_quoted: block (NOT at module top-level)
  - Pipeline: :accrue_admin_browser (confirmed pipeline name from RESEARCH.md)
  - live_storybook("/dev/storybook", backend_module: AccrueAdmin.Dev.Storybook)

AccrueAdmin.Assets module attributes pattern (RESEARCH.md § AccrueAdmin.Assets + PATTERNS.md):
  - @storybook_css_file Application.app_dir(:accrue_admin, "priv/static/storybook.css")
  - @storybook_js_file Application.app_dir(:accrue_admin, "priv/static/storybook.js")
  - @external_resource for each (triggers recompile when committed file changes)
  - @storybook_css_body File.read!(@storybook_css_file)
  - @storybook_js_body File.read!(@storybook_js_file)
  - @storybook_css_hash :md5 |> :crypto.hash(@storybook_css_body) |> Base.encode16(case: :lower)
  - @storybook_js_hash :md5 |> :crypto.hash(@storybook_js_body) |> Base.encode16(case: :lower)
  - Extend @type kind to include :storybook_css | :storybook_js
  - Add storybook_css_hash/0 and storybook_js_hash/0 public functions
  - Add asset(:storybook_css) and asset(:storybook_js) clauses

storybook.css content composition (RESEARCH.md § Spike D + PATTERNS.md):
  1. PhoenixStorybook sandbox CSS: cat deps/phoenix_storybook/priv/static/*.css (or the main PSB CSS file)
  2. The committed accrue_admin.css bundle: cat accrue_admin/priv/static/accrue_admin.css
  3. Dark-mode shim (D-17 Spike B):
     /* D-17 spike B: CSS class→attribute bridge for Storybook dark-mode sandbox */
     /* Uses color_mode_sandbox_dark_class: "ax-theme-dark-shim" in storybook.ex */
     /* Bridges Storybook's class-based toggle to accrue_admin's [data-theme="dark"] token scoping */
     .psb-sandbox.accrue-admin.ax-theme-dark-shim { [mirror dark token block from theme.css] }
  Write to accrue_admin/priv/static/storybook.css (committed bundle)

storybook.js content (RESEARCH.md § Spike D):
  PhoenixStorybook's required client JS from deps/phoenix_storybook/priv/static/
  Write to accrue_admin/priv/static/storybook.js (committed bundle)

AccrueAdmin.Dev.Storybook config (PATTERNS.md § storybook.ex):
  otp_app: :accrue_admin
  content_path: Path.expand("../../../../storybook", __DIR__)  (relative from lib/accrue_admin/dev/)
  css_path: computed via AccrueAdmin.Assets.hashed_path(:storybook_css, "/dev/storybook")
  js_path: computed via AccrueAdmin.Assets.hashed_path(:storybook_js, "/dev/storybook")
  sandbox_class: "accrue-admin"
  color_mode_sandbox_dark_class: "ax-theme-dark-shim"

RegistryStory pattern (PATTERNS.md § registry_story.ex):
  Module: AccrueAdmin.Storybook.RegistryStory
  Guard: if Mix.env() != :prod do ... end (same as ComponentRegistry)
  specs_for/1: AccrueAdmin.Dev.ComponentRegistry.variants_for(family) → [%PhoenixStorybook.Story.Variation{}]
    Variation fields: id (atom), attributes (map), slots (list), description (string)

button.story.exs pattern (PATTERNS.md § button.story.exs):
  defmodule AccrueAdmin.Storybook.Components.Button do
    use PhoenixStorybook.Story, :component
    def function, do: &AccrueAdmin.Components.Core.button/1  [check exact module name]
    def variations, do: AccrueAdmin.Storybook.RegistryStory.variations_for("button")
  end

D-17 spike decisions to record as comments:
  Spike B (dark-mode shim): in storybook.css dark-mode shim block
  Spike C (inert browser-floor): in storybook.ex or registry_story.ex as a comment
  Spike D (asset-serving without Tailwind): in storybook.css header or the Mix task
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build committed asset bundles (storybook.css + storybook.js)</name>
  <files>accrue_admin/priv/static/storybook.css, accrue_admin/priv/static/storybook.js</files>
  <read_first>
    - deps/phoenix_storybook/priv/static/ — ls this directory to find the PSB CSS and JS files that must be bundled; identify the storybook.css and storybook.js (or phoenix_storybook.js) files provided by the dep
    - accrue_admin/priv/static/accrue_admin.css — read first ~30 lines to confirm the file exists and check its approximate size; this is the CSS to concatenate into storybook.css after the PSB sandbox CSS
    - accrue_admin/assets/css/theme.css — read only the dark-mode token block (search for data-theme="dark") to extract the custom-property values to include in the ax-theme-dark-shim block in storybook.css
  </read_first>
  <action>
    Build two committed bundle files using Bash concatenation:

    1. storybook.css: concatenate in this order:
       a. PhoenixStorybook sandbox CSS (from deps/phoenix_storybook/priv/static/ — find the appropriate .css file; typically phoenix_storybook.css or storybook.css)
       b. The committed accrue_admin.css bundle (accrue_admin/priv/static/accrue_admin.css)
       c. The D-17 Spike B dark-mode shim block — add a CSS comment header:
          "/* D-17 spike B recorded decision: CSS class-to-attribute bridge for Storybook dark-mode sandbox */"
          "/* Option A: .psb-sandbox selector shim chosen over Option B JS hook because zero JS overhead */"
          "/* Bridges Storybook color_mode_sandbox_dark_class:'ax-theme-dark-shim' to accrue_admin token scoping */"
          Followed by the selector block: .psb-sandbox.accrue-admin.ax-theme-dark-shim { [all --ax-* dark token overrides from theme.css [data-theme="dark"] block] }
       Write to accrue_admin/priv/static/storybook.css

    2. storybook.js: copy the PhoenixStorybook client JS from deps/phoenix_storybook/priv/static/
       Add a header comment: /* D-17 spike D recorded decision: Storybook JS served via AccrueAdmin.Assets committed-bundle route; no Tailwind rebuild required */
       Write to accrue_admin/priv/static/storybook.js

    These are COMMITTED bundles — they must be committed to git as real files. The AccrueAdmin.Assets module will read them at compile time via File.read!.

    Verify both files exist and are non-empty.
  </action>
  <verify>
    <automated>test -s accrue_admin/priv/static/storybook.css && test -s accrue_admin/priv/static/storybook.js && grep -c "ax-theme-dark-shim" accrue_admin/priv/static/storybook.css && grep -c "D-17 spike" accrue_admin/priv/static/storybook.css</automated>
  </verify>
  <done>Both storybook.css and storybook.js exist as non-empty files; storybook.css contains the dark-mode shim selector block and the D-17 spike B decision comment.</done>
</task>

<task type="auto">
  <name>Task 2: Storybook backend module, asset extension, router wrap, and story files</name>
  <files>accrue_admin/lib/accrue_admin/dev/storybook.ex, accrue_admin/lib/accrue_admin/assets.ex, accrue_admin/lib/accrue_admin/router.ex, storybook/_support/registry_story.ex, storybook/components/button.story.exs</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/dev/component_registry.ex — read lines 1–5 to see the if Mix.env() != :prod do wrapper pattern
    - accrue_admin/lib/accrue_admin/assets.ex — read the full file to understand the current @type kind, init/1, call/2, hashed_path/2, asset/1 clause shapes and where to add the new storybook_css/storybook_js extensions
    - accrue_admin/lib/accrue_admin/router.ex — read lines 100–145 to see the exact wrap_with_mailglass_dev_routes pattern and the wrap-chain site at line 107–108 where the Storybook chain is appended
    - accrue_admin/lib/accrue_admin/components/core.ex or equivalent — grep for "def button" to identify the exact module and function arity for button.story.exs function/0 definition
  </read_first>
  <action>
    Create and modify five files in sequence:

    A. CREATE accrue_admin/lib/accrue_admin/dev/storybook.ex:
       Wrap in if Mix.env() != :prod do ... end.
       Inside: defmodule AccrueAdmin.Dev.Storybook do
         @moduledoc false
         Add comment: /* D-17 spike C recorded decision: inert attribute chosen as background-suppression mechanism over aria-hidden+focusguard; browser floor (Chrome 102+, Firefox 112+, Safari 15.5+) is satisfied by accrue_admin's target audience */
         use PhoenixStorybook,
           otp_app: :accrue_admin,
           content_path: Path.expand("../../../../storybook", __DIR__),
           css_path: AccrueAdmin.Assets.hashed_path(:storybook_css, "/dev/storybook"),
           js_path: AccrueAdmin.Assets.hashed_path(:storybook_js, "/dev/storybook"),
           sandbox_class: "accrue-admin",
           color_mode_sandbox_dark_class: "ax-theme-dark-shim"
       end

    B. MODIFY accrue_admin/lib/accrue_admin/assets.ex via Edit:
       1. After the existing @external_resource declarations, add:
          @storybook_css_file Application.app_dir(:accrue_admin, "priv/static/storybook.css")
          @storybook_js_file Application.app_dir(:accrue_admin, "priv/static/storybook.js")
          @external_resource @storybook_css_file
          @external_resource @storybook_js_file
          @storybook_css_body File.read!(@storybook_css_file)
          @storybook_js_body File.read!(@storybook_js_file)
          @storybook_css_hash :md5 |> :crypto.hash(@storybook_css_body) |> Base.encode16(case: :lower)
          @storybook_js_hash :md5 |> :crypto.hash(@storybook_js_body) |> Base.encode16(case: :lower)
       2. Extend @type kind to include :storybook_css | :storybook_js
       3. Add after the existing css_hash/0 and js_hash/0 functions:
          @spec storybook_css_hash() :: String.t()
          def storybook_css_hash, do: @storybook_css_hash
          @spec storybook_js_hash() :: String.t()
          def storybook_js_hash, do: @storybook_js_hash
       4. Add hashed_path clauses for :storybook_css and :storybook_js (after the existing :brand/:css/:js clause):
          def hashed_path(kind, mount_path) when kind in [:storybook_css, :storybook_js] and is_binary(mount_path) do
            normalized = normalize_mount_path(mount_path)
            suffix = case kind do
              :storybook_css -> "storybook-css-#{@storybook_css_hash}"
              :storybook_js  -> "storybook-js-#{@storybook_js_hash}"
            end
            normalized <> "/assets/" <> suffix
          end
       5. Add asset/1 clauses:
          def asset(:storybook_css), do: {@storybook_css_body, "text/css", @storybook_css_hash}
          def asset(:storybook_js),  do: {@storybook_js_body, "application/javascript", @storybook_js_hash}
       6. Extend init/1 and call/2 guards to include :storybook_css and :storybook_js in the kind in [...] guard

    C. MODIFY accrue_admin/lib/accrue_admin/router.ex via Edit:
       1. Add |> wrap_with_storybook_dev_routes(dev_routes?, mount_path) immediately after the Mailglass chain line
       2. Add the defp wrap_with_storybook_dev_routes/3 function AFTER wrap_with_mailglass_dev_routes/3 (keep related functions grouped):
          Add the comment about why Code.ensure_loaded? is mandatory (unlike Mailglass)
          true-branch: if Code.ensure_loaded?(PhoenixStorybook.Router) do quote bind_quoted: [mount_path: mount_path] do scope mount_path do pipe_through(:accrue_admin_browser); import PhoenixStorybook.Router; live_storybook("/dev/storybook", backend_module: AccrueAdmin.Dev.Storybook) end end
          wrap in: quote do: unquote(base_ast); unquote(dev_ast) end
          no-op fallback: defp wrap_with_storybook_dev_routes(base_ast, _dev_routes?, _mount_path), do: base_ast

    D. CREATE storybook/_support/registry_story.ex:
       Ensure storybook/ and storybook/_support/ directories exist (mkdir -p via Bash if needed).
       Wrap in if Mix.env() != :prod do ... end.
       Module: AccrueAdmin.Storybook.RegistryStory
       @moduledoc false
       alias PhoenixStorybook.Story.Variation
       def variations_for(family) — call AccrueAdmin.Dev.ComponentRegistry.variants_for(family), map each entry's specimens to %Variation{id: atom, attributes: map, slots: list, description: string}
       Pattern from PATTERNS.md § registry_story.ex

    E. CREATE storybook/components/button.story.exs:
       Ensure storybook/components/ directory exists.
       defmodule AccrueAdmin.Storybook.Components.Button do
         use PhoenixStorybook.Story, :component
         def function, do: &AccrueAdmin.Components.Core.button/1  (or the correct module found when reading core.ex)
         def variations, do: AccrueAdmin.Storybook.RegistryStory.variations_for("button")
       end

    VERIFICATION STEP after all edits: run the host-absence compile test per D-13/STY-01:
      cd examples/accrue_host && MIX_ENV=dev mix compile 2>&1 | tail -20
      cd examples/accrue_host && MIX_ENV=prod mix compile 2>&1 | tail -20
    Both must compile without error. If MIX_ENV=dev shows a storybook route exposed or compilation error related to PhoenixStorybook, the Code.ensure_loaded? guard is not working — fix before proceeding.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -c "Code.ensure_loaded?(PhoenixStorybook.Router)" accrue_admin/lib/accrue_admin/router.ex && grep -c "AccrueAdmin.Dev.Storybook" accrue_admin/lib/accrue_admin/dev/storybook.ex && grep -c "storybook_css_hash" accrue_admin/lib/accrue_admin/assets.ex && (cd examples/accrue_host && MIX_ENV=prod mix compile 2>&1 | grep -c "error") || true</automated>
  </verify>
  <done>
    - All five files created/modified
    - storybook.ex exists with if Mix.env() != :prod guard and correct use PhoenixStorybook config
    - router.ex has wrap_with_storybook_dev_routes with Code.ensure_loaded? guard
    - assets.ex has storybook_css/storybook_js kinds, hash functions, hashed_path clauses, asset clauses
    - registry_story.ex compiles and exports variations_for/1
    - button.story.exs delegates to RegistryStory.variations_for("button")
    - examples/accrue_host compiles in both MIX_ENV=dev and MIX_ENV=prod with 0 errors
    - D-17 spikes B/C/D each have a recorded decision comment in the appropriate source file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccrueAdmin.Assets → committed static bundles | Module reads storybook.css/storybook.js at compile time via File.read!; the files are committed to the repo and verified by @external_resource; no runtime file-system reads |
| dev_routes? check → router compilation | dev_routes? is computed at compile time (Mix.env() != :prod); Code.ensure_loaded? provides a second compile-time guard so host apps never get a runtime dependency on PhoenixStorybook |
| storybook/ directory → Hex tarball | storybook/ is NOT in package.files = ~w(lib config guides priv/static mix.exs README* LICENSE* CHANGELOG*); story files are auto-excluded from published tarballs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-07 | Information Disclosure | Storybook routes exposed in host prod build | high | mitigate | Code.ensure_loaded?(PhoenixStorybook.Router) guard in wrap_with_storybook_dev_routes true-branch; Mix.env() != :prod backend guard in storybook.ex; host-absence compile test in Task 2 verifies both |
| T-193-08 | Information Disclosure | Story files in Hex tarball | medium | mitigate | storybook/ excluded from package.files allowlist (auto-excluded per existing ~w() pattern); verify with mix hex.build --dry-run |
| T-193-09 | Elevation of Privilege | Dev Storybook tools exposed in adopter production | high | mitigate | Dual guard: (1) only: [:dev, :test] in mix.exs dep prevents runtime dependency; (2) if Mix.env() != :prod in storybook.ex prevents module from existing; (3) Code.ensure_loaded? in router prevents even the import at compile time |
</threat_model>

<verification>
After all tasks:
1. `grep -c "Code.ensure_loaded?(PhoenixStorybook.Router)" accrue_admin/lib/accrue_admin/router.ex` → 1
2. `test -s accrue_admin/lib/accrue_admin/dev/storybook.ex` → exits 0
3. `grep -c "storybook_css_hash" accrue_admin/lib/accrue_admin/assets.ex` → ≥ 2
4. `test -s storybook/_support/registry_story.ex` → exits 0
5. `test -s storybook/components/button.story.exs` → exits 0
6. `test -s accrue_admin/priv/static/storybook.css` → exits 0
7. `test -s accrue_admin/priv/static/storybook.js` → exits 0
8. `cd examples/accrue_host && MIX_ENV=prod mix compile` → exits 0 with 0 errors
9. `cd examples/accrue_host && MIX_ENV=dev mix compile` → exits 0 with 0 errors
10. No route /dev/storybook appears in: `cd examples/accrue_host && MIX_ENV=dev mix phx.routes 2>&1 | grep storybook | wc -l` → 0
</verification>

<success_criteria>
- Storybook walking skeleton stands up: backend module + router wrap + asset serving + RegistryStory + PoC button story
- Code.ensure_loaded? guard present in router; if Mix.env() != :prod guard in storybook.ex
- examples/accrue_host compiles clean in dev AND prod with phoenix_storybook absent
- No /dev/storybook route exposed in host
- D-17 spikes B/C/D each have recorded decision comments in source files
- Committed storybook.css contains dark-mode shim block with correct selector
</success_criteria>

<output>
Create .planning/phases/193-research-re-baseline-pattern-lock/193-04-SUMMARY.md when done
</output>

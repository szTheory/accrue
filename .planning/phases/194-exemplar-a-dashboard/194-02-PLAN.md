---
phase: 194-exemplar-a-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
autonomous: true
requirements: [EXE-01]

must_haves:
  truths:
    - "On the Recovery analytics page, the at-risk work-queue table renders ABOVE the funnel chart (no chart precedes the work-queue)."
    - "The hero metric pair (Recovered MRR / Exhausted MRR) remains the leading zone, above the promoted at-risk table."
    - "Recovery exposes data-ax-zone markers honest to its actual structure (leading hero zone + work-queue table), without forcing an attention-rail marker onto a page with no exception rail."
  artifacts:
    - "accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex with AtRiskTable rendered above FunnelChart and additive zone markers"
  key_links:
    - "The at-risk-table-before-funnel DOM order (D-01) is the load-bearing machine check the Wave-2 e2e spec asserts for Recovery."
---

<objective>
Re-grammar the Recovery analytics page (`recovery_live.ex`) so at-risk work reads as a work-queue, not a chart wall: move `AtRiskTable.at_risk_table` ABOVE `FunnelChart.funnel_chart` (the single structural change — D-01), keep the hero metric pair leading (D-02), and add additive `data-ax-zone` markers honest to Recovery's actual structure (Q-A). No new chart component, no new data plumbing.

Purpose: Operators act on at-risk records before seeing the conversion funnel; the funnel stays as the supporting viz below. Makes Recovery conform to the SPEC-OVERVIEW "no chart before the work-queue" invariant.
Output: An updated Recovery LiveView with the table/funnel order swapped and additive zone markers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/194-exemplar-a-dashboard/194-CONTEXT.md
@.planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md
@.planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md
@.planning/phases/194-exemplar-a-dashboard/194-UI-SPEC.md
@accrue_admin/guides/spec-overview.md
@accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap AtRiskTable above FunnelChart (the single structural change)</name>
  <files>accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex L90-156 (render/1: hero pair L121-143, FunnelChart L145-150, AtRiskTable L152)
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L58-74 (recovery render-order swap excerpt)
  </read_first>
  <action>
    In recovery_live.ex render/1, swap the order of the two render blocks so `AtRiskTable.at_risk_table rows={@at_risk} base_path={@admin_mount_path}` (currently L152) renders ABOVE `FunnelChart.funnel_chart entered=... recovered=... exhausted=... active=...` (currently L145-150), per D-01. Preserve both components' attributes verbatim — this is purely a re-order, no new chart, no new data plumbing (replacing the funnel with a time-series chart is an explicitly deferred idea). The `@kpi_pairs` hero loop (L121-143, Recovered MRR / Exhausted MRR) stays leading and unchanged (D-02), above the now-promoted table.
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -5; awk '/AtRiskTable.at_risk_table/{t=NR} /FunnelChart.funnel_chart/{f=NR} END{print "table@"t" funnel@"f; exit !(t<f)}' lib/accrue_admin/live/analytics/recovery_live.ex</automated>
  </verify>
  <acceptance_criteria>
    Module compiles clean; the `AtRiskTable.at_risk_table` line number is LESS than the `FunnelChart.funnel_chart` line number (table renders first); the hero `@kpi_pairs` loop is unchanged and still precedes both.
  </acceptance_criteria>
  <done>At-risk table renders above the funnel; hero pair still leads; no component attributes changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive data-ax-zone markers honest to Recovery's structure</name>
  <files>accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/analytics/recovery_live.ex L100-153 (page section, hero `<section class="ax-kpi-grid ax-section-gap">` L122, the AtRiskTable/FunnelChart blocks)
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L58-74 (Recovery zone markers — planner discretion Q-A)
    - .planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md L416-419 (Q-A recommendation)
  </read_first>
  <action>
    Add additive `data-ax-zone` markers to Recovery honest to its actual structure (Q-A is explicit CONTEXT discretion). Recovery has NO exception/attention rail — do NOT force an `attention-rail` marker onto a page without one. Mark the leading hero pair `<section class="ax-kpi-grid ax-section-gap">` (L122) as the leading zone with `data-ax-zone="kpi-cluster"` (the hero metric pair IS Recovery's leading KPI zone), and wrap the now-promoted `AtRiskTable` in a `<section data-ax-zone="task-launcher">` (the work-queue) so the load-bearing assertion — work-queue marker DOM index < funnel — has a stable hook. Markers are additive static enum values only (no PII/IDs); structure beyond the Task-1 swap is not moved. Do NOT add a `data-ax-command-palette-trigger` here (the ⌘K trigger lives in the shared topbar/dashboard, not on the Recovery page body). The single `<h1 class="ax-display">` at L104 is already present (one-h1 invariant satisfied).
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -5; grep -c 'data-ax-zone' lib/accrue_admin/live/analytics/recovery_live.ex</automated>
  </verify>
  <acceptance_criteria>
    Compiles clean; `data-ax-zone` markers are present (at least the work-queue/table zone and the hero zone) with honest enum values; no `attention-rail` marker is forced onto Recovery; the work-queue zone precedes the funnel in DOM order.
  </acceptance_criteria>
  <done>Recovery carries additive zone markers honest to its structure; the work-queue zone DOM index precedes the funnel.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (from this plan)
- Recovery render-order change: `AtRiskTable.at_risk_table` rendered above `FunnelChart.funnel_chart` (D-01).
- `data-ax-zone` markers on Recovery honest to its structure: `kpi-cluster` (hero metric pair, leading) and `task-launcher` (the at-risk work-queue section) — deliberately NO `attention-rail` marker (Recovery has no exception rail; Q-A resolution).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → rendered DOM | Static marker attributes + a render re-order; no user input crosses here this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-03 | Information Disclosure | Recovery `data-ax-zone` markers | low | mitigate | Markers carry static enum literals only (kpi-cluster/task-launcher) — no PII, no record IDs, no scope. Verified in Task 2 action. |
| T-194-04 | Tampering | AtRiskTable/FunnelChart re-order | low | accept | Pure render-block re-order with attributes preserved verbatim; no data source or query changes. No realistic tampering surface. |

No new auth, routes, data flows, or destructive actions (read/route-only analytics page). No high-severity threats. **SCHEMA_PUSH not applicable:** LiveView markup only — no ORM/schema/migration files in scope.
</threat_model>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors` clean.
- `AtRiskTable.at_risk_table` line < `FunnelChart.funnel_chart` line (table above funnel).
- Hero `@kpi_pairs` loop unchanged and leading.
- `data-ax-zone` markers present and honest (no forced attention-rail).
</verification>

<success_criteria>
Recovery reads as a work-queue, not a chart wall: hero pair → at-risk work-queue table → supporting funnel. Additive markers provide the DOM-order hook the Wave-2 e2e spec asserts.
</success_criteria>

<output>
Create `.planning/phases/194-exemplar-a-dashboard/194-02-SUMMARY.md` when done.
</output>

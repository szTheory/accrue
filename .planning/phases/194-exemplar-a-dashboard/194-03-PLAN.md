---
phase: 194-exemplar-a-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [EXE-01]

must_haves:
  truths:
    - "verify_package_docs.sh fails when .ax-attention-rail--empty carries cursor:pointer in app.css."
    - "The standalone script stays green on the real (compliant) app.css."
    - "A new ExUnit negative test in PackageDocsVerifierTest proves the guard fails on a planted violation (D-08 coupling honored)."
  artifacts:
    - "scripts/ci/verify_package_docs.sh with the new empty-rail non-interactivity guard (Guard D)"
    - "accrue/test/accrue/docs/package_docs_verifier_test.exs with the new mirrored negative test"
  key_links:
    - "The D-08 coupling invariant: any new needle in verify_package_docs.sh MUST be mirrored by a negative test against the already-seeded app.css fixture, or all negative tests can drift out of sync."
    - "The guard fail-message substring 'empty-rail' is matched by the ExUnit mirror — keep it stable."
---

<objective>
Add the SPEC-OVERVIEW empty-rail non-interactivity source guard: a new `require_regex`-style block-scan in `verify_package_docs.sh` that fails when `.ax-attention-rail--empty` carries `cursor:pointer`, and its MANDATORY mirror as a new negative test in `PackageDocsVerifierTest` (the D-08 coupling invariant). This is the source-lint half of the SPEC-OVERVIEW non-interactive-empty-rail invariant (the Playwright half lands in plan 194-04). Per D-06 and resolved discretion item: BOTH mechanisms, both NEW.

Purpose: Catch a `cursor:pointer` regression on the healthy empty-state at compile-cost-free CI lint, before a browser boots.
Output: A new guard in the CI script + its coupled ExUnit negative test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/194-exemplar-a-dashboard/194-CONTEXT.md
@.planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md
@.planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md
@scripts/ci/verify_package_docs.sh
@accrue/test/accrue/docs/package_docs_verifier_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the empty-rail non-interactivity source guard (Guard D)</name>
  <files>scripts/ci/verify_package_docs.sh</files>
  <read_first>
    - scripts/ci/verify_package_docs.sh L9-12 (fail helper), L23-35 (require_fixed/require_regex), L323 (app_css def), L400-433 (z-index/raw-type block-scan guards), L497-515 (interactive/motion guards), L552+ (RES-04 Phase 193 CSS source guards region)
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L157-172 (Guard D excerpt — block-scan mirrors existing Guard C style)
    - .planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md L287-298 (Q2 — both mechanisms, both new)
  </read_first>
  <action>
    In verify_package_docs.sh, add a new empty-rail guard placed with the other RES-04 CSS source guards (the Phase 193 region ~L552+, after the existing block-scan guards). Use `$app_css` (already defined at L323). Scan each `.ax-attention-rail--empty { ... }` rule block and `fail` if it contains `cursor: pointer` (tolerate optional whitespace around the colon), mirroring the existing Guard C perl `-0ne` block-scan convention. The fail message MUST be stable and contain the substring `empty-rail` (the ExUnit mirror matches on it) and name the SPEC-OVERVIEW non-interactive-empty-rail guard. Do NOT alter any existing guard or the `fail`/helper definitions. The guard reads app.css via fixed grep/perl patterns only — no eval of file content (same posture as the existing CSS guards).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_package_docs.sh 2>&1 | tail -3; echo "--- planted-violation check ---"; cp accrue_admin/assets/css/app.css /tmp/appcss.bak && printf '\n.ax-attention-rail--empty { cursor: pointer; }\n' >> accrue_admin/assets/css/app.css && (bash scripts/ci/verify_package_docs.sh 2>&1 | grep -i 'empty-rail' && echo GUARD_FIRED || echo GUARD_MISSED); mv /tmp/appcss.bak accrue_admin/assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    On the real app.css the script passes (exit 0). After appending a violating `.ax-attention-rail--empty { cursor: pointer; }` line, the script fails with a message containing `empty-rail` (GUARD_FIRED). The original app.css is restored afterward. No existing guard altered.
  </acceptance_criteria>
  <done>Guard D fails on a cursor:pointer violation and passes on the compliant app.css; fail message contains the stable `empty-rail` substring.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the guard as an ExUnit negative test (D-08 coupling)</name>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - accrue/test/accrue/docs/package_docs_verifier_test.exs L241-310 (existing app.css negative tests: append-with-`\n` pattern, run_verifier, status/output assertions) and the seed_tmp_dir! / copy_fixture! helpers
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L176-198 (the mirror negative test excerpt)
    - .planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md L176-180 (Pitfall 3 — D-08 coupling)
  </read_first>
  <action>
    Add a new negative test to PackageDocsVerifierTest mirroring Guard D (D-08 coupling — MEMORY: verify_package_docs ↔ test coupling; without the mirror the standalone script can be green while the negative-test suite drifts). Follow the existing app.css negative-test pattern exactly: `tmp_dir!()` → `seed_tmp_dir!(tmp_dir)` (app.css is already in the seed set via copy_fixture!) → read the seeded app.css and APPEND (with leading+trailing `\n`) a violating `.ax-attention-rail--empty { cursor: pointer; }` line (append, NOT full-replace — full-replace breaks earlier token-consumption guards, per 193-05 deviations #4/#5) → `run_verifier(tmp_dir)` → assert `status != 0` and `output =~ "empty-rail"` (the stable substring from Guard D's fail message). Do NOT modify seed_tmp_dir! (app.css is already seeded). Do NOT alter existing tests.
  </action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    `mix test test/accrue/docs/package_docs_verifier_test.exs` passes with ALL tests green (the 6 prior negative tests + the new one — proving the D-08 mirror is in sync, not drifted). The new test plants the violation by append-with-`\n` and asserts a non-zero status with `empty-rail` in the output.
  </acceptance_criteria>
  <done>The new negative test passes alongside all existing ones; the guard and its mirror are coupled and in sync.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (from this plan)
- New `verify_package_docs.sh` empty-rail non-interactivity guard (Guard D) with a stable fail message containing the `empty-rail` substring, banning `cursor:pointer` on `.ax-attention-rail--empty`.
- New `PackageDocsVerifierTest` negative test mirroring Guard D (D-08 coupling), planting a violating line into the seeded app.css fixture via the append-with-`\n` pattern.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script → app.css | The guard reads app.css as untrusted source text via fixed grep/perl patterns (no code execution). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-05 | Tampering | Guard D parsing app.css | low | mitigate | Guard uses fixed grep/perl patterns with no eval of file content — same posture as the existing Phase-193 RES-04 CSS guards. |
| T-194-06 | Repudiation | guard ↔ test coupling drift | medium | mitigate | D-08 coupling: the ExUnit mirror (Task 2) asserts the guard fires, so a silently-removed/weakened guard fails the test suite. Both run in CI. |

No high-severity threats. **SCHEMA_PUSH not applicable:** a CI shell guard + an ExUnit test only — no ORM/schema/migration files in scope.
</threat_model>

<verification>
- `bash scripts/ci/verify_package_docs.sh` passes on the real tree.
- The guard fails on a planted `.ax-attention-rail--empty { cursor: pointer; }` line (fail message contains `empty-rail`).
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` all green (existing + new).
</verification>

<success_criteria>
The empty-rail non-interactivity invariant is enforced at CI source-lint and its D-08 mirror is in sync — the standalone script and the ExUnit suite agree.
</success_criteria>

<output>
Create `.planning/phases/194-exemplar-a-dashboard/194-03-SUMMARY.md` when done.
</output>

---
phase: 194-exemplar-a-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["194-01", "194-02", "194-03"]
files_modified:
  - accrue_admin/e2e/admin-spec-overview-phase194.spec.js
  - accrue_admin/package.json
files_referenced:
  - accrue_admin/e2e/phase192-scorecard.mjs
  - .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json
autonomous: true
requirements: [EXE-01]

must_haves:
  truths:
    - "A new Playwright spec asserts the SPEC-OVERVIEW machine invariants on both Dashboard and Recovery (one h1; ⌘K visible+focusable; zone DOM order; non-interactive empty hero; recovery table-before-funnel)."
    - "The spec reuses the Phase-191 page-flow helper library (no new helper library)."
    - "The p193-to-p187 baseline-lookup keying in phase192-scorecard.mjs is confirmed before the SC3 gate wording is finalized (Open Q-B)."
    - "Zero score-downgrade rows exist for the dashboard + recovery page-flow surfaces vs the v1.53 p187 baseline (SC3)."
  artifacts:
    - "accrue_admin/e2e/admin-spec-overview-phase194.spec.js (new SPEC-OVERVIEW assertion spec)"
    - "accrue_admin/package.json with an e2e:phase194 script"
  key_links:
    - "The spec binds to the additive data-ax-* markers + .ax-attention-rail--empty class created in plans 194-01/194-02 — those must exist first (wave 2)."
    - "SC3 zero-regression depends on how the scorecard pairs p193__ page-flow cells with their p187__ baseline counterparts (Open Q-B)."
---

<objective>
Wire the SPEC-OVERVIEW invariant assertions — the first consumer of the contract. Create a new Playwright spec (`admin-spec-overview-phase194.spec.js`) that asserts the machine-checkable invariants on both Dashboard and Recovery using the existing Phase-191 helper library, and confirm the SC3 zero-regression scoring path (resolving Open Q-B before writing the gate wording). Per the resolved discretion: the empty-rail invariant is enforced as BOTH a source guard (194-03) AND this Playwright `assertTopPointerTarget` assertion.

Purpose: Phase 193 locked the SPEC-OVERVIEW contract and built the substrate but wired ZERO invariant assertions — this plan makes the contract machine-enforced. Phase 198 reuses this machinery.
Output: A new e2e spec covering SC1/SC2 machine parts, an `e2e:phase194` script, and a confirmed SC3 scoring path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/194-exemplar-a-dashboard/194-CONTEXT.md
@.planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md
@.planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md
@.planning/phases/194-exemplar-a-dashboard/194-UI-SPEC.md
@accrue_admin/guides/spec-overview.md
@accrue_admin/e2e/admin-page-flow-phase191.spec.js
@accrue_admin/e2e/phase191-page-flow-helpers.js
@.planning/phases/194-exemplar-a-dashboard/194-01-SUMMARY.md
@.planning/phases/194-exemplar-a-dashboard/194-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the p193→p187 baseline-lookup keying for SC3 (Open Q-B)</name>
  <files>accrue_admin/e2e/phase192-scorecard.mjs (read-only)</files>
  <read_first>
    - accrue_admin/e2e/phase192-scorecard.mjs L305-360 (contractedCell / validP187Id — REQUIRES a p187__...__dXX cell_id), L412-465 (compareCells keys baseline & final by exact cell_id; score-downgrade at L457-465)
    - .planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json (the 432 p193__dashboard__* + 432 p193__recovery__* pending cells)
    - .planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md L300-307, L421-424 (Q3 + Open Q-B)
  </read_first>
  <action>
    Surface and resolve Open Q-B as an explicit early step (do NOT silently drop it). The scorecard's `contractedCell` (L324) throws unless `cell_id` matches `p187__...__dXX`, and `compareCells` (L413-419) pairs baseline↔final by EXACT `cell_id`. The page-flow cells are `p193__`-prefixed, so a naive comparison would have no `p187__` baseline counterpart. Read phase192-scorecard.mjs around L401/L457 and the page-flow lens union-load to determine the ACTUAL pairing mechanism (prefix-stripped surface/mode/theme/state/dimension tuple, a normalization step, or a separate page-flow scoring path). Document the confirmed keying in the SUMMARY. Then state the SC3 gate command precisely: which scorecard invocation produces `regressions.ndjson`, and how "zero score-downgrade rows for dashboard + recovery" is read from it. If the scorecard genuinely cannot pair p193 page-flow cells to a p187 baseline (making the score-downgrade check a no-op for these surfaces), record that finding explicitly and define SC3 as "the two surfaces' cells are scored (coverage flipped to covered, score set) with no downgrade against whatever baseline the scorecard pairs them to" — do not fabricate a passing gate. This is a planning/verification-wording task; it does not require editing the scorecard.
  </action>
  <verify>
    <automated>cd accrue_admin && node -e "const s=require('fs').readFileSync('e2e/phase192-scorecard.mjs','utf8'); console.log('validP187Id:', /validP187Id/.test(s)); console.log('compareCells keys by cell_id:', /finalById = new Map\(finalRows\.map/.test(s));" && node -e "const c=require('fs').readFileSync('../.planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json','utf8'); const j=JSON.parse(c); const cells=Array.isArray(j)?j:(j.cells||[]); const dash=cells.filter(x=>String(x.cell_id||'').includes('__dashboard__')).length; const rec=cells.filter(x=>String(x.cell_id||'').includes('__recovery__')).length; console.log('dashboard cells:',dash,'recovery cells:',rec);"</automated>
  </verify>
  <acceptance_criteria>
    The SUMMARY records the confirmed p193↔p187 baseline-lookup keying (or an explicit finding that the page-flow cells use a distinct scoring path), and states the exact SC3 gate command + how zero-downgrade is read. The dashboard/recovery cell counts are confirmed present (≈432 each).
  </acceptance_criteria>
  <done>Open Q-B is resolved in writing; the SC3 gate command and pass condition are unambiguous before the verification step relies on them.</done>
</task>

<task type="auto">
  <name>Task 2: Create the SPEC-OVERVIEW invariant assertion spec (reuses Phase-191 helpers)</name>
  <files>accrue_admin/e2e/admin-spec-overview-phase194.spec.js</files>
  <read_first>
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js L1-22, L82-133, L139, L205-208 (helper import, reset/seed/login scaffolding, trace+describe, viewport+theme loop)
    - accrue_admin/e2e/phase191-page-flow-helpers.js L315-331 (exports: PHASE191_VIEWPORTS, setPhase191Theme, assertFocusWithin, assertTopPointerTarget, assertScrollReachable, assertNoHorizontalClip)
    - .planning/phases/194-exemplar-a-dashboard/194-PATTERNS.md L106-153 (spec scaffold + the invariant assertion excerpts)
    - .planning/phases/194-exemplar-a-dashboard/194-UI-SPEC.md L110-120 (the invariant table)
    - accrue_admin/lib/accrue_admin/live/dashboard_live.ex + analytics/recovery_live.ex (the markers created in 194-01/194-02)
  </read_first>
  <action>
    Create `accrue_admin/e2e/admin-spec-overview-phase194.spec.js`. Scaffold from admin-page-flow-phase191.spec.js: same `require("./phase191-page-flow-helpers.js")` import (no new helper library — Don't Hand-Roll), same reset/seedScenario/login helpers, `test.use({ trace: "retain-on-failure" })`, and `test.describe`. Assert the SPEC-OVERVIEW invariants (none exist yet — 194 is the first enforcer):
    - DASHBOARD: `page.locator('h1').count() === 1`; the ⌘K trigger `page.locator('[data-ax-command-palette-trigger]')` is visible, then focus it and `assertFocusWithin(page, kbd, ...)`; zone DOM order via `querySelectorAll('[data-ax-zone]')` asserting `indexOf('attention-rail') < indexOf('task-launcher') < indexOf('kpi-cluster')` (D-05).
    - DASHBOARD empty-rail (D-06): seed the EMPTY attention state first (use the existing seed scenario that yields zero attention rows — confirm the scenario name from the phase191 spec's seed usage), then assert `.ax-attention-rail--empty` is NOT the top pointer target via `assertTopPointerTarget` (it should NOT be a pointer target / the assertion confirms non-interactivity). Also assert the empty hero element has no `role="button"`.
    - RECOVERY (D-01): assert the at-risk work-queue table's DOM index is LESS than the funnel chart's DOM index (no chart before the work-queue) — locate the table and funnel by their stable selectors/markers and compare positions.
    Run across the existing `PHASE191_VIEWPORTS` + light/dark via `setPhase191Theme` only where it adds signal; keep the spec deterministic and `--workers=1`-safe. Do not duplicate the phase191 page-flow harness assertions — this spec is SPEC-OVERVIEW-specific.
  </action>
  <verify>
    <automated>cd accrue_admin && node --check e2e/admin-spec-overview-phase194.spec.js && npx playwright test e2e/admin-spec-overview-phase194.spec.js --workers=1 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    The new spec parses and runs; all SPEC-OVERVIEW assertions pass against the markers/class/swap from 194-01/194-02: one h1 per page, ⌘K visible+focusable, dashboard zone order attention<task<kpi, empty-rail non-interactive (assertTopPointerTarget) with no role="button", recovery table index < funnel index. It imports the existing phase191 helpers (no new helper library).
  </acceptance_criteria>
  <done>The SPEC-OVERVIEW invariants are machine-asserted on both pages and pass.</done>
</task>

<task type="auto">
  <name>Task 3: Register the e2e:phase194 script and run the full gate suite</name>
  <files>accrue_admin/package.json</files>
  <read_first>
    - accrue_admin/package.json L5-16 (the e2e script block, e.g. e2e:phase191 at L8)
    - .planning/phases/194-exemplar-a-dashboard/194-RESEARCH.md L356-377 (Validation Architecture: full suite + sampling rate)
  </read_first>
  <action>
    Add an `e2e:phase194` script to accrue_admin/package.json mirroring the `e2e:phase191` shape (`env -u NO_COLOR playwright test e2e/admin-spec-overview-phase194.spec.js --timeout=60000 --workers=1`). Then run the full phase gate: the new spec, the existing `e2e:phase191` page-flow suite (must stay green — zero regression to the phase191 harness), `bash scripts/ci/verify_package_docs.sh` (Guard D + RES-04), and `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` (the D-08 mirror). Finally execute the SC3 scoring path exactly as confirmed in Task 1 and confirm zero `score-downgrade` rows for the dashboard + recovery surfaces.
  </action>
  <verify>
    <automated>cd accrue_admin && grep -c 'e2e:phase194' package.json && npm run e2e:phase194 2>&1 | tail -8 && npm run e2e:phase191 2>&1 | tail -6 && bash ../scripts/ci/verify_package_docs.sh 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    `e2e:phase194` script exists and passes; `e2e:phase191` still green; `verify_package_docs.sh` passes; the SC3 scoring path (per Task 1) shows zero `score-downgrade` rows for dashboard + recovery. (The ExUnit mirror is exercised in plan 194-03's verify and re-run here.)
  </acceptance_criteria>
  <done>The full phase gate passes: SPEC-OVERVIEW spec + phase191 harness + source guards + zero page-flow regression.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (from this plan)
- `accrue_admin/e2e/admin-spec-overview-phase194.spec.js` — the new SPEC-OVERVIEW invariant assertion spec (first enforcer of the contract; reuses phase191-page-flow-helpers.js).
- `e2e:phase194` npm script in `accrue_admin/package.json`.
- A written resolution of Open Q-B (the p193↔p187 baseline-lookup keying) and the precise SC3 gate command in the SUMMARY.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → rendered DOM | The spec drives a real browser against seeded fixtures; no production data flow; reset/seed endpoints are e2e-only (`/__e2e__/*`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-07 | Tampering | empty-rail false affordance | low | mitigate | `assertTopPointerTarget` confirms the healthy empty hero is NOT a pointer target — pairs with the 194-03 source guard so the non-interactive invariant is enforced at both render-detect and source-lint layers. |
| T-194-08 | Information Disclosure | data-ax-* markers asserted by the spec | low | accept | The spec only reads static enum marker values already vetted in 194-01/194-02 — no sensitive data is introduced or surfaced. |

No high-severity threats; this plan adds test infra only. **SCHEMA_PUSH not applicable:** a Playwright spec + a package.json script + scoring confirmation — no ORM/schema/migration files in scope.
</threat_model>

<verification>
- `node --check` + `npx playwright test e2e/admin-spec-overview-phase194.spec.js --workers=1` all green.
- `npm run e2e:phase191` still green (no regression to the phase191 harness).
- `bash scripts/ci/verify_package_docs.sh` passes; `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` green.
- SC3: zero `score-downgrade` rows for dashboard + recovery per the Task-1-confirmed scoring path.
</verification>

<success_criteria>
The SPEC-OVERVIEW contract is machine-enforced for the first time: all invariants asserted on both pages, zero page-flow regression, and the SC3 gate command is unambiguous (Open Q-B resolved).
</success_criteria>

<output>
Create `.planning/phases/194-exemplar-a-dashboard/194-04-SUMMARY.md` when done.
</output>

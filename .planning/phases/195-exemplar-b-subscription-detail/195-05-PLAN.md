---
phase: 195-exemplar-b-subscription-detail
plan: 05
type: execute
wave: 3
depends_on: [195-04]
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/accrue_admin.css
  - accrue_admin/test/accrue_admin/components/overlay_components_test.exs
autonomous: true
requirements: [IXN-01]
requirements_addressed: [IXN-01]
must_haves:
  truths:
    - "IXN-01: Overlay layers use the existing z-token scale with scrim below panel."
    - "IXN-01: Drawer geometry is right-docked on desktop and bottom-sheet on mobile."
    - "IXN-01: Overlay CSS uses the existing 4px scale and motion tokens."
  artifacts:
    - path: "accrue_admin/assets/css/app.css"
      provides: "Overlay, drawer, modal, and popover CSS geometry"
    - path: "accrue_admin/priv/static/accrue_admin.css"
      provides: "Committed generated CSS bundle"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/overlay.ex"
      to: "accrue_admin/assets/css/app.css"
      via: "shared ax-overlay class names"
      pattern: "ax-overlay"
    - from: "accrue_admin/assets/css/app.css"
      to: "accrue_admin/assets/css/theme.css"
      via: "uses existing --ax-z-* and --ax-space-* tokens"
      pattern: "var\\(--ax-z-(drawer|modal|popover)"
  prohibitions:
    - "Do not add new spacing, motion, color, or z-index tokens."
    - "Do not use raw px for padding, margin, or gap; Phase 193 spacing guard must remain green."
---

<objective>
Add the overlay CSS geometry and layer contract, then rebuild the committed CSS bundle.

Purpose: make the canonical overlay visually and interactively correct across desktop and mobile before it hosts Subscription actions.
Output: token-compliant overlay CSS, CSS contract assertions, and rebuilt served CSS.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-exemplar-b-subscription-detail/195-CONTEXT.md
@.planning/phases/195-exemplar-b-subscription-detail/195-PATTERNS.md
@.planning/phases/195-exemplar-b-subscription-detail/195-UI-SPEC.md
@.planning/phases/193-research-re-baseline-pattern-lock/193-05-SUMMARY.md
@accrue_admin/assets/css/theme.css
@accrue_admin/assets/css/app.css
@accrue_admin/lib/accrue_admin/components/overlay.ex
@accrue_admin/test/accrue_admin/components/overlay_components_test.exs
@scripts/ci/verify_package_docs.sh
</context>

<artifacts_this_phase_produces>
- New component module: `AccrueAdmin.Components.Overlay` in `accrue_admin/lib/accrue_admin/components/overlay.ex`.
- New component function: `AccrueAdmin.Components.Overlay.overlay/1`.
- New component functions: `AccrueAdmin.Components.Detail.summary_list/1` and `AccrueAdmin.Components.DropdownMenu.action_menu/1`.
- New JS modules/hooks: `ScrollLock` in `accrue_admin/assets/js/hooks/scroll_lock.js` and `Overlay` in `accrue_admin/assets/js/hooks/overlay.js`.
- New body-level root: `#ax-overlay-root`.
- New npm script: `e2e:phase195`.
- New Storybook modules/files: `storybook/components/overlay.story.exs`, `storybook/components/action_menu.story.exs`, `storybook/components/detail.story.exs`, `storybook/components/subscription_detail.story.exs`.
- New tests: `accrue_admin/test/js/scroll_lock_test.mjs`, `accrue_admin/test/js/dropdown_test.mjs`, and `accrue_admin/e2e/admin-spec-detail-phase195.spec.js`.
- New planning handoff: `.planning/phases/195-exemplar-b-subscription-detail/195-PHASE199-HANDOFF.md`.
- New data attributes: `data-ax-overlay-shell`, `data-ax-overlay-panel`, `data-ax-overlay-backdrop`, `data-presentation`, `data-ax-action-band`, `data-ax-primary-action`, `data-ax-action-overflow-menu`, `data-ax-related-resources`, `data-ax-summary-list`, `data-ax-drill-section`, `data-ax-lazy-activity`, `data-ax-lazy-json`.
- Generated artifacts updated by later plans: `accrue_admin/priv/static/accrue_admin.css`, `accrue_admin/priv/static/accrue_admin.js`, `examples/accrue_host/e2e/generated/copy_strings.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add token-compliant overlay CSS</name>
  <files>accrue_admin/assets/css/app.css</files>
  <read_first>
    - accrue_admin/assets/css/theme.css
    - accrue_admin/assets/css/app.css
    - accrue_admin/lib/accrue_admin/components/overlay.ex
    - .planning/phases/195-exemplar-b-subscription-detail/195-UI-SPEC.md
    - .planning/phases/193-research-re-baseline-pattern-lock/193-05-SUMMARY.md
  </read_first>
  <behavior>
    - `.ax-overlay-shell` is fixed, inset to viewport, isolated, and uses presentation-specific z tokens.
    - `.ax-overlay-backdrop` is local z=0 and `.ax-overlay-panel` is local z=1.
    - Drawer desktop width is `min(34rem, 92vw)` and docks to the right; drawer mobile presentation is a bottom sheet below the existing md breakpoint.
    - Modal and popover presentations use existing layer tokens and motion durations.
    - Inner overlay scroll uses `overscroll-behavior: contain`.
  </behavior>
  <action>Add generic overlay CSS near the existing drawer/modal layer section. Reuse existing `--ax-space-*`, `--ax-dur-*`, `--ax-ease-*`, and `--ax-z-*` tokens per D-03 and the UI-SPEC. Keep any compatibility aliases needed by current dev specimens, but make the new `ax-overlay-*` classes the canonical contract.</action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/verify_package_docs.sh` passes after CSS edits.
    - CSS contains `var(--ax-z-drawer)`, `var(--ax-z-modal)`, and `var(--ax-z-popover)` consumers for overlay presentations.
    - CSS contains no new raw-px padding, margin, or gap violations.
  </acceptance_criteria>
  <done>All acceptance criteria for this task are satisfied, the automated verification command has been run, and the result is recorded in the plan summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green CSS layer and geometry assertions</name>
  <files>accrue_admin/test/accrue_admin/components/overlay_components_test.exs</files>
  <read_first>
    - accrue_admin/test/accrue_admin/components/overlay_components_test.exs
    - accrue_admin/assets/css/app.css
    - accrue_admin/assets/css/theme.css
  </read_first>
  <action>Update the CSS-focused overlay component tests to assert the new canonical class names, layer tokens, local scrim/panel ordering, drawer width, and mobile bottom-sheet media rule. Keep the StepUp modal ordering assertion.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/overlay_components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Component tests pass and assert `.ax-overlay-shell`, `.ax-overlay-backdrop`, and `.ax-overlay-panel`.
    - Tests assert drawer, modal, and popover z-token usage.
    - Tests assert desktop and mobile drawer geometry rules exist.
  </acceptance_criteria>
  <done>All acceptance criteria for this task are satisfied, the automated verification command has been run, and the result is recorded in the plan summary.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild committed CSS bundle</name>
  <files>accrue_admin/priv/static/accrue_admin.css</files>
  <read_first>
    - accrue_admin/assets/css/app.css
    - accrue_admin/priv/static/accrue_admin.css
  </read_first>
  <action>Run the existing asset build after CSS source changes so the served admin bundle includes the overlay geometry. Do not hand-edit generated bundle output.</action>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.assets.build && git diff --check priv/static/accrue_admin.css</automated>
  </verify>
  <acceptance_criteria>
    - `accrue_admin/priv/static/accrue_admin.css` contains the minified overlay CSS.
    - `bash scripts/ci/verify_package_docs.sh` passes from the repo root.
    - `cd accrue_admin && mix test test/accrue_admin/components/overlay_components_test.exs` passes after the build.
  </acceptance_criteria>
  <done>All acceptance criteria for this task are satisfied, the automated verification command has been run, and the result is recorded in the plan summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS layer model -> pointer hit-testing | z-index and local stacking determine whether a user clicks the panel or scrim. |
| Viewport geometry -> action reachability | Drawer/bottom-sheet sizing determines whether controls are reachable on narrow screens. |
| Source CSS -> served bundle | Runtime behavior depends on committed generated CSS, not source CSS alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-17 | Spoofing | Overlay scrim/panel | mitigate | Tests assert scrim local z=0, panel local z=1, and presentation z tokens. |
| T-195-18 | Denial of Service | Mobile drawer | mitigate | Mobile bottom-sheet geometry and scrollable body prevent unreachable actions. |
| T-195-19 | Tampering | Served CSS bundle | mitigate | Plan requires `mix accrue_admin.assets.build` and generated CSS diff check. |
| T-195-20 | Information Disclosure | CSS data attributes | accept | Static data attributes expose no billing data or identifiers. |
</threat_model>

<verification>
- `bash scripts/ci/verify_package_docs.sh`
- `cd accrue_admin && mix test test/accrue_admin/components/overlay_components_test.exs`
- `cd accrue_admin && mix accrue_admin.assets.build`
</verification>

<success_criteria>
Overlay CSS is token-compliant, geometry-correct, covered by component/source checks, and present in the committed served CSS bundle.
</success_criteria>

<output>
Create `.planning/phases/195-exemplar-b-subscription-detail/195-05-SUMMARY.md` when done.
</output>

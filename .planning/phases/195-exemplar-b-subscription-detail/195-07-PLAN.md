---
phase: 195-exemplar-b-subscription-detail
plan: 07
type: execute
wave: 5
depends_on: [195-02, 195-06]
files_modified:
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/lib/accrue_admin/copy/subscription.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - examples/accrue_host/e2e/generated/copy_strings.json
autonomous: true
requirements: [EXE-02, IXN-01]
requirements_addressed: [EXE-02, IXN-01]
must_haves:
  truths:
    - "EXE-02: Subscription detail is converted from flat zones to the six-band DETAIL spine."
    - "EXE-02: Duplicate related resources are merged into the canonical strip and card-in-card nesting is flattened."
    - "EXE-02: Action forms render only in the side drawer after an action is selected."
    - "IXN-01: Destructive actions continue through StepUp and never execute directly from the menu."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      provides: "Gold-standard Subscription detail exemplar"
    - path: "accrue_admin/lib/accrue_admin/copy/subscription.ex"
      provides: "Phase 195 subscription action labels and summary/drill copy"
    - path: "examples/accrue_host/e2e/generated/copy_strings.json"
      provides: "Regenerated copy fixture"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      to: "accrue_admin/lib/accrue_admin/components/overlay.ex"
      via: "DetailDrawer wrapper around canonical overlay hosts action forms"
      pattern: "DetailDrawer\\.detail_drawer"
    - from: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      to: "AccrueAdmin.StepUp"
      via: "existing confirm_action destructive branch"
      pattern: "StepUp\\.require_fresh"
  prohibitions:
    - "Do not alter billing domain functions or introduce new billing primitives."
    - "Do not render unavailable provider actions as disabled controls."
    - "Do not keep the old duplicate related-resources card or nested action card layout."
---

<objective>
Convert `SubscriptionLive` into the Phase 195 gold-standard object-detail exemplar.

Purpose: satisfy EXE-02 while preserving the existing billing action execution path and instantiating IXN-01 through the canonical drawer.
Output: six-band Subscription detail page, drawer-hosted action forms, copy relabels, one related strip, lazy activity/raw JSON, and regenerated copy fixture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-exemplar-b-subscription-detail/195-CONTEXT.md
@.planning/phases/195-exemplar-b-subscription-detail/195-RESEARCH.md
@.planning/phases/195-exemplar-b-subscription-detail/195-PATTERNS.md
@.planning/phases/195-exemplar-b-subscription-detail/195-UI-SPEC.md
@.planning/phases/195-exemplar-b-subscription-detail/195-VALIDATION.md
@accrue_admin/guides/spec-detail.md
@accrue_admin/lib/accrue_admin/live/subscription_live.ex
@accrue_admin/lib/accrue_admin/components/detail.ex
@accrue_admin/lib/accrue_admin/components/dropdown_menu.ex
@accrue_admin/lib/accrue_admin/components/detail_drawer.ex
@accrue_admin/lib/accrue_admin/copy/subscription.ex
@accrue_admin/lib/accrue_admin/copy.ex
@examples/accrue_host/e2e/generated/copy_strings.json
@accrue_admin/test/accrue_admin/live/subscription_live_test.exs
</context>

<artifacts_this_phase_produces>
- New component module: `AccrueAdmin.Components.Overlay` in `accrue_admin/lib/accrue_admin/components/overlay.ex`.
- New component function: `AccrueAdmin.Components.Overlay.overlay/1`.
- New component functions: `AccrueAdmin.Components.Detail.summary_list/1` and `AccrueAdmin.Components.DropdownMenu.action_menu/1`.
- New JS modules/hooks: `ScrollLock` in `accrue_admin/assets/js/hooks/scroll_lock.js` and `Overlay` in `accrue_admin/assets/js/hooks/overlay.js`.
- New body-level root: `#ax-overlay-root`.
- New npm script: `e2e:phase195`.
- New Storybook modules/files: `storybook/components/overlay.story.exs`, `storybook/components/action_menu.story.exs`, `storybook/components/detail.story.exs`, `storybook/components/subscription_detail.story.exs`.
- New tests: `accrue_admin/test/js/scroll_lock_test.mjs`, `accrue_admin/test/js/dropdown_test.mjs`, and `accrue_admin/e2e/admin-spec-detail-phase195.spec.js`.
- New planning handoff: `.planning/phases/195-exemplar-b-subscription-detail/195-PHASE199-HANDOFF.md`.
- New data attributes: `data-ax-overlay-shell`, `data-ax-overlay-panel`, `data-ax-overlay-backdrop`, `data-presentation`, `data-ax-action-band`, `data-ax-primary-action`, `data-ax-action-overflow-menu`, `data-ax-related-resources`, `data-ax-summary-list`, `data-ax-drill-section`, `data-ax-lazy-activity`, `data-ax-lazy-json`.
- Generated artifacts updated by later plans: `accrue_admin/priv/static/accrue_admin.css`, `accrue_admin/priv/static/accrue_admin.js`, `examples/accrue_host/e2e/generated/copy_strings.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the six-band DETAIL spine</name>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/lib/accrue_admin/components/detail.ex
    - accrue_admin/lib/accrue_admin/components/related_resources.ex
    - accrue_admin/guides/spec-detail.md
    - .planning/phases/195-exemplar-b-subscription-detail/195-CONTEXT.md
    - .planning/phases/195-exemplar-b-subscription-detail/195-UI-SPEC.md
  </read_first>
  <behavior>
    - Band 1: `Detail.summary_card/1` wraps `Detail.summary_list/1` rows for status, customer, plan/price, current period, renews/ends, amount/MRR, optional quantity, and optional dunning.
    - Band 2: `data-ax-action-band` contains no inline forms on first render and shows the D-01 primary actions plus the D-04 overflow action menu.
    - Band 3: drill sections are `Billing & items`, `Dunning & recovery`, and `Tax & compliance`, with exactly one default-open section based on dunning state.
    - Band 4: exactly one `RelatedResources.related_resources` strip remains.
    - Bands 5 and 6: activity timeline and raw JSON are collapsed/lazy sections after core state; `timeline_events` is not eagerly loaded in `mount/3`.
  </behavior>
  <action>Refactor the `render/1` template into the D-02 six-band spine. Delete the redundant KPI grid, standalone dunning card, duplicate related card, nested tax/action cards, and above-the-fold raw JSON. Move duplicate-card unique links into `related_items/3`. Use existing money/date helpers for the amount row; if a complete MRR cannot be derived from current subscription items, render a truthful dash/unknown state through existing formatting rather than creating billing logic. Move eager `timeline_events` loading out of `mount/3`; load activity data only when the Activity section is first expanded or when an action drawer needs `source_event_select/1`, and keep raw JSON collapsed until explicitly expanded.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/accrue_admin/live/subscription_live_test.exs` passes the six-band assertions from Plan 02.
    - Rendered HTML has exactly one `data-ax-related-resources` strip.
    - Rendered HTML has zero visible forms in `data-ax-action-band` on initial load.
    - A source or LiveView test assertion proves `mount/3` no longer assigns eager `timeline_events`; activity data is assigned only after first expand or action-drawer need.
    - Tests assert `data-ax-lazy-activity` and `data-ax-lazy-json` sections are collapsed on initial render and become populated only through the explicit lazy-load path.
  </acceptance_criteria>
  <done>All acceptance criteria for this task are satisfied, the automated verification command has been run, and the result is recorded in the plan summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Host all subscription actions in the drawer</name>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/lib/accrue_admin/components/detail_drawer.ex
    - accrue_admin/lib/accrue_admin/components/dropdown_menu.ex
    - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
    - .planning/phases/195-exemplar-b-subscription-detail/195-CONTEXT.md
  </read_first>
  <behavior>
    - Primary buttons and action-menu items select an action into drawer render state; they do not execute billing mutations directly.
    - Drawer forms submit through the existing `prepare_action` path and final confirmation still uses `confirm_action`.
    - `cancel_pending_action` clears pending/drawer state and closes the drawer.
    - The existing destructive branch in `confirm_action` still checks `@destructive_actions` and uses `StepUp.require_fresh/4`.
    - Provider predicates drive absence of unsupported menu items.
    - If a drawer form needs source event options, it uses the same lazy activity loader instead of reintroducing mount-time eager timeline loading.
  </behavior>
  <action>Add render-only drawer selection state such as `@drawer_action_type` and an `open_action_drawer` event if needed; keep the existing `prepare_action`, `confirm_action`, `cancel_pending_action`, `StepUp.require_fresh/4`, provider predicates, `maybe_attach_preview/2`, and refresh behavior as the execution path. Move the existing action form bodies into drawer-render helpers and keep `source_event_select/1` available by loading timeline events when an action drawer or lazy activity section needs them.</action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Destructive action regression still records `admin.subscription.action.completed` only after StepUp verification.
    - `prepare_action`, `confirm_action`, and `cancel_pending_action` handlers remain present.
    - Tests cover the action-drawer source-event path without restoring eager `timeline_events` loading in `mount/3`.
    - No new billing context function or route is introduced.
  </acceptance_criteria>
  <done>All acceptance criteria for this task are satisfied, the automated verification command has been run, and the result is recorded in the plan summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update copy labels and regenerate fixture</name>
  <files>accrue_admin/lib/accrue_admin/copy/subscription.ex, accrue_admin/lib/accrue_admin/copy.ex, examples/accrue_host/e2e/generated/copy_strings.json</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy/subscription.ex
    - accrue_admin/lib/accrue_admin/copy.ex
    - examples/accrue_host/e2e/generated/copy_strings.json
    - .planning/phases/195-exemplar-b-subscription-detail/195-UI-SPEC.md
  </read_first>
  <behavior>
    - `swap_plan` label is `Change plan`.
    - reversible cancellation label is `Cancel renewal`.
    - immediate destructive cancellation label is `Cancel immediately`.
    - comp action label is `Comp this subscription`.
    - New summary/drill/action copy used outside the module is delegated through `AccrueAdmin.Copy`.
  </behavior>
  <action>Apply the D-01b copy relabels in `AccrueAdmin.Copy.Subscription`, add delegates in `AccrueAdmin.Copy` only for new functions used by `SubscriptionLive`, and run the committed copy export task. Keep copy precise, calm, and literal; do not render raw inspect output in recoverable errors.</action>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.export_copy_strings && git diff --check ../examples/accrue_host/e2e/generated/copy_strings.json && mix test test/accrue_admin/live/subscription_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/accrue_host/e2e/generated/copy_strings.json` contains the four D-01b labels.
    - `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs` passes.
    - No copy string for the new page structure is hardcoded in `subscription_live.ex` when an `AccrueAdmin.Copy` function is appropriate.
  </acceptance_criteria>
  <done>All acceptance criteria for this task are satisfied, the automated verification command has been run, and the result is recorded in the plan summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Action selection -> billing mutation | Render-only drawer selection must not bypass existing billing execution handlers. |
| Provider predicates -> UI controls | Available actions differ by processor and PlanResolver configuration. |
| Lazy raw JSON -> admin browser | Raw payload remains escaped/collapsed and should not become primary UI. |
| Copy fixture -> browser tests | E2E tests consume exported copy strings and can drift from source copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-25 | Elevation of Privilege | `confirm_action` | mitigate | Preserve `@destructive_actions` and StepUp branch exactly in execution path. |
| T-195-26 | Tampering | Drawer forms | mitigate | Forms submit to existing `prepare_action` and server-side `execute_action/4` allowlist. |
| T-195-27 | Spoofing | Action labels | mitigate | Copy relabels and hidden context labels reduce action confusion. |
| T-195-28 | Information Disclosure | Raw JSON/activity | mitigate | Raw JSON and activity render in collapsed/lazy bands after core state. |
| T-195-29 | Tampering | Provider-specific controls | mitigate | Existing predicates omit unavailable actions from action menu and primary band. |
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs`
- `cd accrue_admin && mix accrue_admin.export_copy_strings`
- `cd accrue_admin && git diff --check ../examples/accrue_host/e2e/generated/copy_strings.json`
</verification>

<success_criteria>
Subscription detail now matches the six-band DETAIL contract, all action forms are drawer-hosted, destructive actions still require StepUp, and copy fixture drift is closed.
</success_criteria>

<output>
Create `.planning/phases/195-exemplar-b-subscription-detail/195-07-SUMMARY.md` when done.
</output>

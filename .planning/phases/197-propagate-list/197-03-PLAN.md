---
phase: 197-propagate-list
plan: "03"
type: execute
wave: 1
depends_on:
  - "197-01"
  - "197-02"
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/copy/invoice.ex
  - accrue_admin/lib/accrue_admin/copy/coupon.ex
  - accrue_admin/lib/accrue_admin/copy/promotion_code.ex
  - accrue_admin/lib/accrue_admin/copy/connect.ex
  - accrue_admin/lib/accrue_admin/copy/billing_event.ex
  - accrue_admin/lib/accrue_admin/queries/webhooks.ex
  - accrue_admin/lib/accrue_admin/queries/connect_accounts.ex
  - accrue_admin/lib/accrue_admin/queries/charges.ex
  - accrue_admin/test/accrue_admin/copy_test.exs
  - accrue_admin/test/accrue_admin/queries/query_modules_test.exs
autonomous: true
requirements:
  - PRP-01
must_haves:
  truths:
    - "PRP-01: Per-page JTBD and four-state copy is available through AccrueAdmin.Copy before LiveView migrations."
    - "Webhooks can represent Needs replay as a real failed/dead filter, not UI-only copy."
    - "Connect can represent Needs attention as a named OR lens, not AND-ed readiness booleans."
    - "Payments/Charges owner scope is explicit before Payments adopts owner-scope-preserving filter links."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy.ex"
      provides: "Phase 197 copy helper surface"
    - path: "accrue_admin/lib/accrue_admin/queries/webhooks.ex"
      provides: "Allowlisted multi-status replay queue decode"
    - path: "accrue_admin/lib/accrue_admin/queries/connect_accounts.ex"
      provides: "needs_attention OR lens"
    - path: "accrue_admin/lib/accrue_admin/queries/charges.ex"
      provides: "Payments owner-scope filtering"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/*_live.ex"
      to: "accrue_admin/lib/accrue_admin/copy.ex"
      via: "pages call AccrueAdmin.Copy helpers for JTBD and state copy"
      pattern: "Copy\\."
    - from: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      to: "accrue_admin/lib/accrue_admin/queries/webhooks.ex"
      via: "default status=failed,dead decodes to a status collection"
      pattern: "failed,dead"
    - from: "accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex"
      to: "accrue_admin/lib/accrue_admin/queries/connect_accounts.ex"
      via: "default needs_attention=true decodes to an OR predicate"
      pattern: "needs_attention"
---

<objective>
Implement the narrow shared copy and query seams needed by all Phase 197 page migrations.

Purpose: Page migrations should reuse page-specific copy helpers and real query semantics for Webhooks, Connect, and Payments instead of faking defaults in UI text.
Output: Copy helpers/delegates, Webhooks multi-status decode, Connect `needs_attention=true`, and Charges owner-scope filtering.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/197-propagate-list/197-CONTEXT.md
@.planning/phases/197-propagate-list/197-RESEARCH.md
@.planning/phases/197-propagate-list/197-PATTERNS.md
@.planning/phases/197-propagate-list/197-UI-SPEC.md
@.planning/phases/197-propagate-list/197-VALIDATION.md
@brandbook/voice.md
@brandbook/copy.md
@accrue_admin/lib/accrue_admin/copy/subscription.ex
@accrue_admin/lib/accrue_admin/queries/invoices.ex
@accrue_admin/test/accrue_admin/queries/query_modules_test.exs
</context>

## Artifacts this phase produces

- Test-only module/file: `accrue_admin/test/support/list_contracts.ex`.
- New npm script: `e2e:phase197`.
- New Playwright spec: `accrue_admin/e2e/admin-spec-list-phase197.spec.js`.
- New test fixture param: `phase197_state=loading-skeleton`.
- New or expanded copy helper names: `customers_list_*`, `invoices_list_*`, `payments_list_*`, `coupons_list_*`, `promotion_codes_list_*`, `webhooks_list_*`, `events_list_*`, `connect_accounts_list_*`.
- Query behavior symbols: Webhooks multi-status decode for `status=failed,dead`, Connect `needs_attention=true`, and Charges owner-scope support.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 197 copy helpers and delegates</name>
  <files>accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/lib/accrue_admin/copy/invoice.ex, accrue_admin/lib/accrue_admin/copy/coupon.ex, accrue_admin/lib/accrue_admin/copy/promotion_code.ex, accrue_admin/lib/accrue_admin/copy/connect.ex, accrue_admin/lib/accrue_admin/copy/billing_event.ex, accrue_admin/test/accrue_admin/copy_test.exs</files>
  <read_first>
    - `accrue_admin/lib/accrue_admin/copy.ex`
    - `accrue_admin/lib/accrue_admin/copy/subscription.ex`
    - `accrue_admin/lib/accrue_admin/copy/invoice.ex`
    - `accrue_admin/lib/accrue_admin/copy/coupon.ex`
    - `accrue_admin/lib/accrue_admin/copy/promotion_code.ex`
    - `accrue_admin/lib/accrue_admin/copy/connect.ex`
    - `accrue_admin/lib/accrue_admin/copy/billing_event.ex`
    - `accrue_admin/test/accrue_admin/copy_test.exs`
    - `.planning/phases/197-propagate-list/197-UI-SPEC.md`
    - `brandbook/voice.md`
  </read_first>
  <behavior>
    - Copy helpers return the exact Phase 197 page titles and descriptions from D-14.
    - Copy helpers return distinct first-run, queue/default empty, filtered-empty, and loading labels from D-18.
    - Pages can call copy through `AccrueAdmin.Copy`; touched templates do not need inline state strings per D-15.
  </behavior>
  <action>
    Add Phase 197 copy helpers in `AccrueAdmin.Copy` and existing resource modules per D-14, D-15, and D-18. For resources with existing modules, add module functions and central delegates: `invoices_list_*` in `Copy.Invoice`, `coupons_list_*` in `Copy.Coupon`, `promotion_codes_list_*` in `Copy.PromotionCode`, `connect_accounts_list_*` in `Copy.Connect`, and `events_list_*` in `Copy.BillingEvent`. For resources without an existing page copy module, add direct `customers_list_*`, `payments_list_*`, and `webhooks_list_*` helpers in `Copy`.

    Provide these helper groups for each page: heading, subtitle, first-run-empty title/body, queue-empty title/body where the default lens can be clear, filtered-empty title/body, loading label, default lens label, all lens label, result-label pair where existing tests need it, and any quick-chip label such as `Missing payment method` or `Admin changes`. Use the exact operator-job titles from D-14 and the state-copy seeds from the UI spec. Update `copy_test.exs` with focused assertions that first-run, queue/default empty, filtered-empty, and loading strings differ where D-18 requires distinction.
  </action>
  <acceptance_criteria>
    `AccrueAdmin.Copy` exposes deterministic helper names for all eight target pages; copy tests prove the D-14 headings/subtitles and D-18 state strings exist and remain distinct. No touched LiveView needs new inline state copy to satisfy Phase 197.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/copy_test.exs --max-failures 3</automated>
    <automated>rg -n "customers_list_|invoices_list_|payments_list_|coupons_list_|promotion_codes_list_|webhooks_list_|events_list_|connect_accounts_list_" accrue_admin/lib/accrue_admin/copy.ex accrue_admin/lib/accrue_admin/copy</automated>
  </verify>
  <done>All Phase 197 JTBD and four-state copy helpers are available through the copy layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Webhooks multi-status and Connect attention query semantics</name>
  <files>accrue_admin/lib/accrue_admin/queries/webhooks.ex, accrue_admin/lib/accrue_admin/queries/connect_accounts.ex, accrue_admin/test/accrue_admin/queries/query_modules_test.exs</files>
  <read_first>
    - `accrue_admin/lib/accrue_admin/queries/webhooks.ex`
    - `accrue_admin/lib/accrue_admin/queries/connect_accounts.ex`
    - `accrue_admin/lib/accrue_admin/queries/invoices.ex`
    - `accrue_admin/test/accrue_admin/queries/query_modules_test.exs`
    - `.planning/phases/197-propagate-list/197-RESEARCH.md`
  </read_first>
  <behavior>
    - `Webhooks.decode_filter/1` handles `status=failed,dead` with an allowlist and returns a value `filter_query/2` can apply as status IN failed/dead.
    - Unknown webhook statuses are ignored or compacted without creating atoms.
    - `ConnectAccounts.decode_filter/1` handles `needs_attention=true`.
    - `ConnectAccounts.list/1` with the attention lens returns accounts matching any attention condition.
  </behavior>
  <action>
    In `AccrueAdmin.Queries.Webhooks`, add an allowlisted comma-split decoder for status text per D-10. Use the existing accepted webhook status vocabulary, return a single atom for one valid status and a list of atoms for multiple valid statuses, preserve nil/blank handling, and keep existing `filter_query/2` list-status behavior. Do not use unbounded atom creation.

    In `AccrueAdmin.Queries.ConnectAccounts`, add `needs_attention` parsing per D-12. Implement one named OR predicate covering deauthorized accounts, details not submitted, charges disabled, or payouts disabled. Keep the existing individual boolean filters available and AND-reduced for explicit user filters; the attention lens is a separate filter key. Green the RED query tests from Plan 01.
  </action>
  <acceptance_criteria>
    Query tests prove Webhooks `failed,dead` returns failed or dead rows and ignores unknown statuses safely. Query tests prove Connect `needs_attention=true` is OR semantics and does not require every readiness field to match.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --max-failures 3</automated>
  </verify>
  <done>Webhooks and Connect defaults are backed by real query semantics instead of UI-only labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Payments owner-scope query support</name>
  <files>accrue_admin/lib/accrue_admin/queries/charges.ex, accrue_admin/test/accrue_admin/queries/query_modules_test.exs</files>
  <read_first>
    - `accrue_admin/lib/accrue_admin/queries/charges.ex`
    - `accrue_admin/lib/accrue_admin/queries/invoices.ex`
    - `accrue_admin/lib/accrue_admin/owner_scope.ex`
    - `accrue_admin/test/accrue_admin/queries/query_modules_test.exs`
    - `.planning/phases/197-propagate-list/197-RESEARCH.md`
  </read_first>
  <behavior>
    - `Charges.list/1` accepts `owner_scope`.
    - Organization owner scope limits Payments/Charges results to rows owned through the joined customer or existing ownership relation.
    - Global owner scope remains unchanged.
    - Multi-status Payments filters keep working after owner-scope support lands.
  </behavior>
  <action>
    Update `AccrueAdmin.Queries.Charges` to accept `owner_scope` in `list/1` and apply scoping with the closest existing Invoices pattern per D-07 and D-17. Preserve current status, cursor, and limit behavior; do not change the `/payments` route or introduce `/charges` UI labels. Green the owner-scope RED tests from Plan 01 and keep existing multi-status Charges tests passing.
  </action>
  <acceptance_criteria>
    Query tests prove organization-scoped Payments/Charges results cannot include another organization's payments, while global scope and existing status filters still work.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --max-failures 3</automated>
  </verify>
  <done>Payments has query-level owner-scope support before the Payments list migration relies on owner-scope-preserving URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL status params to atoms | Webhooks status text enters query decode and must be allowlisted. |
| URL readiness params to Connect query | Connect attention values decide which accounts appear in the default queue. |
| Owner scope to Payments query | Charges rows must not cross tenant boundaries. |
| Copy helpers to rendered UI | Operator-facing copy must not imply exact totals or hidden records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-11 | Denial of Service | Webhooks.decode_filter/1 | mitigate | Use an allowlist before atom conversion for multi-status defaults per D-10. |
| T-197-12 | Information Disclosure | Charges.list/1 | mitigate | Apply owner-scope filtering and prove with query tests per D-07 and D-17. |
| T-197-13 | Tampering | Connect needs_attention | mitigate | Implement a named OR predicate and test its semantics per D-12. |
| T-197-14 | Repudiation | Count/copy helpers | mitigate | Copy avoids exact-total claims and supports visible-count language per D-04. |
| T-197-SC | Tampering | package installs | accept | No package install is planned; existing dependencies are used. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && mix test test/accrue_admin/copy_test.exs --max-failures 3`
- `cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --max-failures 3`
- `cd accrue_admin && mix compile --warnings-as-errors`
</verification>

<success_criteria>
Shared copy and query seams are green before the LiveView page migrations begin. No runtime LIST DSL, package install, schema push, or route addition is introduced.
</success_criteria>

<output>
Create `.planning/phases/197-propagate-list/197-03-SUMMARY.md` when done.
</output>

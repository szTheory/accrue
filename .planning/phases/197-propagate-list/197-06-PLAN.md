---
phase: 197-propagate-list
plan: "06"
type: execute
wave: 3
depends_on:
  - "197-03"
files_modified:
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/lib/accrue_admin/live/events_live.ex
  - accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
  - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  - accrue_admin/test/accrue_admin/live/events_live_test.exs
  - accrue_admin/test/accrue_admin/live/connect_accounts_live_test.exs
autonomous: true
requirements:
  - PRP-01
must_haves:
  truths:
    - "Webhooks defaults to Needs replay backed by status=failed,dead and preserves bulk retry selection/confirmation/scope behavior."
    - "Events defaults to All ledger and keeps Admin changes as a quick lens."
    - "Connect defaults to Needs attention backed by the named OR query lens."
    - "All three pages adopt PageHeader, one h1, FilterChipBar, four states, owner-safe clear-all, and row-to-card degradation."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      provides: "Webhooks LIST propagation with bulk replay preserved"
    - path: "accrue_admin/lib/accrue_admin/live/events_live.ex"
      provides: "Events LIST propagation"
    - path: "accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex"
      provides: "Connect LIST propagation"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      to: "accrue_admin/lib/accrue_admin/queries/webhooks.ex"
      via: "Needs replay default passes status=failed,dead"
      pattern: "failed,dead"
    - from: "accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex"
      to: "accrue_admin/lib/accrue_admin/queries/connect_accounts.ex"
      via: "Needs attention default passes needs_attention=true"
      pattern: "needs_attention"
---

<objective>
Propagate the locked LIST contract to Webhooks, Events, and Connect accounts.

Purpose: These pages carry the risky semantics in Phase 197: Webhooks replay must stay scoped, Events must stay a full ledger by default, and Connect attention must be a real OR lens.
Output: Three migrated LiveViews with green focused tests and no loss of existing action semantics.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/197-propagate-list/197-CONTEXT.md
@.planning/phases/197-propagate-list/197-PATTERNS.md
@.planning/phases/197-propagate-list/197-UI-SPEC.md
@.planning/phases/197-propagate-list/197-VALIDATION.md
@.planning/phases/196-exemplar-c-subscriptions-list-pageheader/196-04-SUMMARY.md
@accrue_admin/guides/spec-list.md
@accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
@accrue_admin/lib/accrue_admin/queries/webhooks.ex
@accrue_admin/lib/accrue_admin/queries/connect_accounts.ex
@accrue_admin/lib/accrue_admin/data_table_nav.ex
</context>

## Artifacts this phase produces

- Test-only module/file: `accrue_admin/test/support/list_contracts.ex`.
- New npm script: `e2e:phase197`.
- New Playwright spec: `accrue_admin/e2e/admin-spec-list-phase197.spec.js`.
- New test fixture param: `phase197_state=loading-skeleton`.
- New or expanded copy helper names: `customers_list_*`, `invoices_list_*`, `payments_list_*`, `coupons_list_*`, `promotion_codes_list_*`, `webhooks_list_*`, `events_list_*`, `connect_accounts_list_*`.
- Query behavior symbols: Webhooks multi-status decode for `status=failed,dead`, Connect `needs_attention=true`, and Charges owner-scope support.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate Webhooks to the LIST exemplar while preserving replay</name>
  <files>accrue_admin/lib/accrue_admin/live/webhooks_live.ex, accrue_admin/test/accrue_admin/live/webhooks_live_test.exs</files>
  <read_first>
    - `accrue_admin/lib/accrue_admin/live/webhooks_live.ex`
    - `accrue_admin/test/accrue_admin/live/webhooks_live_test.exs`
    - `accrue_admin/lib/accrue_admin/live/subscriptions_live.ex`
    - `accrue_admin/lib/accrue_admin/queries/webhooks.ex`
    - `accrue_admin/lib/accrue_admin/components/page_header.ex`
    - `accrue_admin/lib/accrue_admin/components/data_table.ex`
    - `accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex`
    - `.planning/phases/197-propagate-list/197-PATTERNS.md`
  </read_first>
  <behavior>
    - Bare `/webhooks` represents `status=failed,dead`.
    - The default queue is labeled `Needs replay`, with `All deliveries` one chip away.
    - Existing bulk retry selection, confirmation, scope guard, and flash behavior remain covered and passing.
    - Webhooks exposes page-specific first-run, queue-empty, filtered-empty, and loading copy.
  </behavior>
  <action>
    In `WebhooksLive`, adopt PageHeader with title `Replay failed deliveries`, D-14 description, existing actions if any, and `DataTable.filter_toolbar/1` in the PageHeader filter-toolbar slot per D-01 and D-13. Pass `render_filter_toolbar={false}` to DataTable and render FilterChipBar in `:list_status` per D-16. Implement D-10 by using the Plan 03 query seam for `status=failed,dead`, labeling the default lens `Needs replay`, and exposing `All deliveries` through `view=all`. Clear-all removes `status`, `type`, `livemode`, `cursor`, `phase197_state`, and fixture params while preserving owner scope per D-17.

    Preserve the existing Webhooks bulk retry flow exactly: selected-row message handling, pending confirmation, no-selection warning, scoped `Webhooks.detail/2` guard, `DLQ.requeue/1`, audit/event behavior, and success/warning flashes. Do not generalize bulk behavior to other list pages per D-22. Use Plan 03 copy for no deliveries, nothing needs replay, filtered empty, and loading states per D-18. Keep type/status/subject/time as primary debugging nouns per D-19.
  </action>
  <acceptance_criteria>
    Webhooks tests pass for PageHeader, exactly one h1, `status=failed,dead` default, Needs replay/All deliveries chips, owner-safe clear-all, four states, visible count, row/card priority, and unchanged bulk replay behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhooks_live_test.exs</automated>
    <automated>cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --max-failures 3</automated>
  </verify>
  <done>Webhooks conforms to SPEC-LIST without flattening or weakening replay semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Events to the LIST exemplar as an all-ledger page</name>
  <files>accrue_admin/lib/accrue_admin/live/events_live.ex, accrue_admin/test/accrue_admin/live/events_live_test.exs</files>
  <read_first>
    - `accrue_admin/lib/accrue_admin/live/events_live.ex`
    - `accrue_admin/test/accrue_admin/live/events_live_test.exs`
    - `accrue_admin/lib/accrue_admin/live/subscriptions_live.ex`
    - `accrue_admin/lib/accrue_admin/copy/billing_event.ex`
    - `accrue_admin/lib/accrue_admin/components/page_header.ex`
    - `accrue_admin/lib/accrue_admin/components/data_table.ex`
    - `accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex`
    - `accrue_admin/lib/accrue_admin/data_table_nav.ex`
    - `.planning/phases/197-propagate-list/197-CONTEXT.md`
  </read_first>
  <behavior>
    - Bare `/events` shows all ledger rows and does not hide the ledger.
    - `All ledger` renders as the active default lens.
    - `Admin changes` quick chip applies `actor_type=admin`.
    - Clear-all removes user filters while preserving owner scope.
  </behavior>
  <action>
    In `EventsLive`, adopt PageHeader with title `Trace billing activity`, D-14 description, existing stat strip if present, and `DataTable.filter_toolbar/1` in PageHeader per D-01 and D-13. Keep Events as an all-ledger default per D-11: bare `/events` must not hide rows, must render an active `All ledger` chip, and must keep an `Admin changes` quick chip one action away using `actor_type=admin`. Replace any path append helper with `DataTableNav.merge_query/2` for chip and clear-all links per D-17. Render FilterChipBar in DataTable `:list_status` per D-16 and use visible count copy per D-04.

    Add explicit state helpers for populated, first-run-empty, filtered-empty, and loading-skeleton per D-18. There is no queue-empty state for the all-ledger default; tests should assert no hidden ledger queue is manufactured per D-02. Keep type, subject/actor, source, and time ahead of raw event IDs per D-19.
  </action>
  <acceptance_criteria>
    Events tests pass for PageHeader, exactly one h1, All ledger default, Admin changes quick chip, owner-safe clear-all, four applicable states, visible count, and DataTable row-to-card behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/events_live_test.exs</automated>
  </verify>
  <done>Events conforms to SPEC-LIST as a full append-only ledger page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate Connect accounts to the LIST exemplar with needs_attention</name>
  <files>accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex, accrue_admin/test/accrue_admin/live/connect_accounts_live_test.exs</files>
  <read_first>
    - `accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex`
    - `accrue_admin/test/accrue_admin/live/connect_accounts_live_test.exs`
    - `accrue_admin/lib/accrue_admin/live/subscriptions_live.ex`
    - `accrue_admin/lib/accrue_admin/queries/connect_accounts.ex`
    - `accrue_admin/lib/accrue_admin/copy/connect.ex`
    - `accrue_admin/lib/accrue_admin/components/page_header.ex`
    - `accrue_admin/lib/accrue_admin/components/data_table.ex`
    - `accrue_admin/lib/accrue_admin/components/filter_chip_bar.ex`
    - `.planning/phases/197-propagate-list/197-RESEARCH.md`
  </read_first>
  <behavior>
    - Bare `/connect` represents `needs_attention=true`.
    - `Needs attention` renders as the default chip with `All accounts` one chip away.
    - The page uses the named OR query lens from Plan 03 and does not combine readiness booleans as AND filters.
    - Connect exposes first-run, no-attention, filtered-empty, and loading copy.
  </behavior>
  <action>
    In `ConnectAccountsLive`, adopt PageHeader with title `Finish account readiness`, D-14 description, stat strip, and DataTable filter toolbar in the PageHeader slot per D-01 and D-13. Implement D-12 by canonicalizing bare `/connect` to `needs_attention=true`, labeling the default chip `Needs attention`, and exposing `All accounts` through `view=all`. Use the Plan 03 query seam; do not approximate attention by combining `deauthorized`, `charges_enabled`, `payouts_enabled`, and `details_submitted` filters. Clear-all removes readiness filters, search/type params, cursor, `phase197_state`, and fixture params while preserving owner scope per D-17.

    Render FilterChipBar in `:list_status` per D-16, use visible count copy per D-04, and add explicit DataTable states for first-run-empty, queue/default empty, filtered-empty, and loading-skeleton per D-18. Keep account identity, readiness/capability state, type, and time before raw account IDs per D-19.
  </action>
  <acceptance_criteria>
    Connect tests pass for PageHeader, exactly one h1, `needs_attention=true` default, Needs attention/All accounts chips, owner-safe clear-all, OR-lens behavior, four states, and row/card priority.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/connect_accounts_live_test.exs</automated>
    <automated>cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --max-failures 3</automated>
  </verify>
  <done>Connect conforms to SPEC-LIST with a real attention queue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook row selection to retry side effect | Selected IDs cross from browser UI into replay side effects. |
| Events filters to audit ledger | Ledger filters must not hide default rows or drop scope. |
| Connect readiness params to query | Attention queue logic determines actionable account visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-23 | Elevation of Privilege | Webhooks replay | mitigate | Preserve scoped selected-id guard before `DLQ.requeue/1` and keep existing tests green per D-10. |
| T-197-24 | Denial of Service | Webhooks status params | mitigate | Use Plan 03 allowlisted multi-status decode per D-10. |
| T-197-25 | Spoofing | Events default lens | mitigate | Tests prove all-ledger default and no manufactured exception queue per D-11. |
| T-197-26 | Tampering | Connect needs_attention | mitigate | Tests prove named OR lens and reject AND-filter approximation per D-12. |
| T-197-27 | Information Disclosure | Clear-all links | mitigate | DataTableNav/ScopedPath patterns preserve owner scope per D-17. |
| T-197-SC | Tampering | package installs | accept | No package install is planned; existing dependencies are used. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && mix test test/accrue_admin/live/webhooks_live_test.exs`
- `cd accrue_admin && mix test test/accrue_admin/live/events_live_test.exs`
- `cd accrue_admin && mix test test/accrue_admin/live/connect_accounts_live_test.exs`
- `cd accrue_admin && mix test test/accrue_admin/queries/query_modules_test.exs --max-failures 3`
- `cd accrue_admin && mix test test/accrue_admin/components/page_header_test.exs test/accrue_admin/components/data_table_test.exs test/accrue_admin/components/filter_chip_bar_test.exs`
</verification>

<success_criteria>
Webhooks, Events, and Connect satisfy PRP-01 with PageHeader, one h1, correct page-owned default lenses, URL-backed filters, FilterChipBar chips/count/clear-all, honest visible counts, owner-scope-safe clear-all, row-to-card degradation, four states, and preserved replay/ledger/attention semantics.
</success_criteria>

<output>
Create `.planning/phases/197-propagate-list/197-06-SUMMARY.md` when done.
</output>

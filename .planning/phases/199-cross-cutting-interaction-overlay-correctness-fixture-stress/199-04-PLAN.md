---
phase: 199-cross-cutting-interaction-overlay-correctness-fixture-stress
plan: "04"
type: execute
wave: 3
depends_on: ["199-03"]
files_modified:
  - accrue_admin/test/support/e2e_fixtures.ex
  - accrue_admin/test/support/e2e_plug.ex
  - accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
  - accrue_admin/e2e/phase191-page-flow-helpers.js
  - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
autonomous: true
requirements: [FIX-01, FIX-02, IXN-01, IXN-02, IXN-03]
must_haves:
  truths:
    - "FIX-01: Deterministic composed route flows cover Customer list to peer record set to related detail to back, Invoice drawer/step-up to back, Webhook to Event drill to replay, Recovery to Campaign to Subscription, and Connect platform-fee drawer/step-up."
    - "FIX-01: Every route transition and back navigation preserves focus and scroll integrity, and no overlay, step-up, scroll lock, or stale filter state survives when it should close."
    - "FIX-02: Long names, long emails, long processor IDs, non-ASCII names, zero-decimal JPY values, past-due dunning, failed/dead webhooks, connect attention, raw payload overflow, and row-count boundaries are seeded idempotently."
    - "FIX-02: Browser assertions catch horizontal overflow, clipping, squish, unreachable scroll sentinels, and focus falling to `document.body`."
  artifacts:
    - path: "accrue_admin/test/support/e2e_fixtures.ex"
      provides: "Phase 199 deterministic fixture extension for real route flows and edge layout data"
    - path: "accrue_admin/test/support/e2e_plug.ex"
      provides: "Test-only `/__e2e__/seed/phase199-interactions` endpoint if existing seeds lack required IDs"
    - path: "accrue_admin/test/accrue_admin/e2e_fixtures_test.exs"
      provides: "Fixture idempotency and route-ID proof"
    - path: "accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js"
      provides: "Route-level FIX-01/FIX-02 browser checks"
  key_links:
    - from: "accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js"
      to: "accrue_admin/test/support/e2e_plug.ex"
      via: "POST `/__e2e__/seed/*` calls before composed route assertions"
      pattern: "__e2e__/seed"
    - from: "accrue_admin/test/support/e2e_fixtures.ex"
      to: "accrue_admin/test/accrue_admin/e2e_fixtures_test.exs"
      via: "fixture return map is asserted for deterministic IDs and repeated-run counts"
      pattern: "seed_phase199"
    - from: "accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js"
      to: "accrue_admin/e2e/phase191-page-flow-helpers.js"
      via: "shared no-horizontal-clip, scroll reachability, focus, and body-focus helpers"
      pattern: "assertNoHorizontalClip"
  prohibitions:
    - "No synthetic-only component state as acceptance evidence."
    - "No broad fixture DSL or generated route manifest."
    - "No production route, schema, or billing primitive changes."
---

<objective>
Add deterministic route-level fixture stress for Phase 199.

Purpose: prove FIX-01 and FIX-02 against real seeded admin pages, including multi-step operator paths and edge layout data that reveal clipping, scroll, focus, and overflow defects.
Output: minimal test-only fixture extension, fixture idempotency tests, and passing @fixture Phase 199 browser checks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-CONTEXT.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-RESEARCH.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-PATTERNS.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-UI-SPEC.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-03-SUMMARY.md
@.planning/phases/198-propagate-detail-analytics/198-09-SUMMARY.md
@accrue_admin/test/support/e2e_fixtures.ex
@accrue_admin/test/support/e2e_plug.ex
@accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
@accrue_admin/e2e/phase191-page-flow-helpers.js
@accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
</context>

## Artifacts this phase produces

- New or extended fixture function: `seed_phase199_interactions!/0` in `AccrueAdmin.E2E.Fixtures`, only if existing seed responses do not cover all required route IDs.
- New or extended endpoint: `/__e2e__/seed/phase199-interactions`, only if the fixture function is added.
- New fixture response fields when needed: `long_customer_id`, `long_email_customer_id`, `long_processor_invoice_id`, `jpy_invoice_id`, `jpy_charge_id`, `dead_webhook_id`, `event_with_payload_id`, `connect_attention_id`, `recovery_campaign_subscription_id`, `overflow_payload_event_id`.
- New or extended browser helper: `assertRouteFocusAndScroll` or `assertNoBodyFocusAfterNavigation` if repeated across flows.
- Passing browser subset: `npm run e2e:phase199 -- --grep @fixture`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend deterministic edge fixtures only where required</name>
  <files>accrue_admin/test/support/e2e_fixtures.ex, accrue_admin/test/support/e2e_plug.ex, accrue_admin/test/accrue_admin/e2e_fixtures_test.exs</files>
  <read_first>
    - accrue_admin/test/support/e2e_fixtures.ex
    - accrue_admin/test/support/e2e_plug.ex
    - accrue_admin/test/accrue_admin/e2e_fixtures_test.exs
    - .planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-CONTEXT.md
    - .planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-PATTERNS.md
  </read_first>
  <behavior>
    - Existing `/__e2e__/seed/phase191-matrix`, `/operator-flows`, `/edge-states`, and `/overflow` are reused whenever they already provide IDs per D-12.
    - Any new Phase 199 fixture function is deterministic, idempotent, and returns route IDs for every missing FIX-01/FIX-02 flow per D-12 and D-14.
    - Fixture tests assert repeated runs return stable route IDs/counts and persisted rows contain long names/emails/processor IDs, zero-decimal JPY values, dunning, failed/dead webhooks, connect attention, and overflow payload data per D-14.
  </behavior>
  <action>Audit existing seed return maps first. If they already expose all required IDs, add tests documenting that coverage and do not add a new endpoint. If missing IDs remain, add the narrow `seed_phase199_interactions!/0` fixture and a matching test-only endpoint under `E2EPlug`. Keep reset/idempotency behavior consistent with `seed_phase191_matrix!/0`, and keep fixtures under test support only.</action>
  <acceptance_criteria>
    - `e2e_fixtures_test.exs` proves every required Phase 199 route ID is available from existing seeds or the new narrow fixture.
    - Re-running the fixture path is idempotent and starts from a deterministic state.
    - No migration, schema file, production route, or package file changes are introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs --max-failures 5</automated>
  </verify>
  <done>FIX-01/FIX-02 route IDs and edge data are deterministic and test-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focus, scroll, and clipping helpers for repeated route-flow checks</name>
  <files>accrue_admin/e2e/phase191-page-flow-helpers.js, accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js</files>
  <read_first>
    - accrue_admin/e2e/phase191-page-flow-helpers.js
    - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js
    - accrue_admin/e2e/admin-spec-detail-phase198.spec.js
    - .planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-RESEARCH.md
  </read_first>
  <behavior>
    - Repeated flows can assert no horizontal clipping, reachable scroll sentinels, focus within expected region, no body focus, and no stale overlay/scroll lock after navigation per D-13 and D-14.
    - Helpers remain small browser-test utilities, not a runtime or generic route DSL, per D-21 and D-22.
  </behavior>
  <action>Add helper functions only for repeated assertions that would otherwise duplicate code in three or more Phase 199 route-flow tests. Candidate names are `assertRouteFocusAndScroll`, `assertNoBodyFocusAfterNavigation`, and `assertNoStaleOverlayState`; keep exports explicit. Use existing helpers directly where one assertion is enough. Update the Phase 199 spec imports accordingly.</action>
  <acceptance_criteria>
    - Helper exports are used by the Phase 199 spec and do not alter existing Phase 191/197/198 helper behavior.
    - `node --check` passes for both helper and spec files.
    - Helpers contain no route manifests or page DSLs.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && node --check e2e/phase191-page-flow-helpers.js && node --check e2e/admin-interaction-overlay-phase199.spec.js</automated>
  </verify>
  <done>Route-flow assertions are reusable without adding a broad test framework.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Green the composed route-flow and edge-layout browser checks</name>
  <files>accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js</files>
  <read_first>
    - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
    - accrue_admin/e2e/admin-spec-detail-phase198.spec.js
    - accrue_admin/e2e/admin-spec-list-phase197.spec.js
    - accrue_admin/e2e/admin-spec-detail-phase195.spec.js
    - accrue_admin/test/support/e2e_fixtures.ex
    - .planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-UI-SPEC.md
  </read_first>
  <behavior>
    - Customer list -> Customer detail -> peer record set -> related detail -> back preserves focus, scroll, owner scope, and no stale drawer/step-up per D-13.
    - Invoice list -> Invoice detail -> drawer/step-up action -> back preserves focus, scroll, overlay cleanup, and JPY zero-decimal rendering per D-13 and D-14.
    - Webhooks list -> Webhook detail -> Event/detail drill -> replay drawer/step-up checks failed/dead replay states, raw payload overflow, and cleanup per D-13 and D-14.
    - Recovery -> Campaign -> Subscription detail preserves route state and scroll while exercising dunning/at-risk data per D-13 and D-14.
    - Connect list -> Connect account detail -> platform-fee drawer/step-up checks attention states and cleanup per D-13 and D-14.
    - Long content and overflow checks assert no document-level horizontal overflow, no clipped visible elements, reachable scroll end, and no body focus after transitions per FIX-02.
  </behavior>
  <action>Fill in the @fixture tests in `admin-interaction-overlay-phase199.spec.js` using the deterministic seed responses from Task 1 and existing login/reset patterns. Keep route targets explicit and readable. Use real composed pages as acceptance evidence; component kitchen or Storybook specimens may only supplement comments/test setup and are not the pass condition.</action>
  <acceptance_criteria>
    - `npm run e2e:phase199 -- --grep @fixture` passes.
    - Browser tests cover all five recommended multi-step paths from D-13.
    - Browser tests include long content, zero-decimal JPY, dunning, webhooks, connect attention, and overflow payload assertions from D-14.
    - No body focus, stale overlay, stale inert shell, stale scroll lock, or horizontal clipping remains in the route-flow assertions.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && npm run e2e:phase199 -- --grep @fixture</automated>
  </verify>
  <done>FIX-01 and FIX-02 are green on focused composed-page browser evidence.</done>
</task>

</tasks>

<threat_model>
## ASVS Context

ASVS level: 1. Security block threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only seed endpoint to database | E2E plug writes deterministic records used by browser routes. |
| Browser navigation to LiveView state | Multi-step route transitions patch URL, drawer state, filters, and scroll/focus state. |
| Edge data to rendered layout | Long strings, raw payloads, and boundary values stress overflow and clipping behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-17 | Information Disclosure | Seeded owner data | mitigate | Fixture tests and route flows use existing owner-scoped admin routes and deterministic test-only data. |
| T-199-18 | Tampering | E2E seed endpoint | mitigate | Endpoints stay under test support and mirror existing `/__e2e__/seed/*` patterns. |
| T-199-19 | Denial of Service | Overflow content | mitigate | Browser checks require scroll reachability and no horizontal overflow under long payloads. |
| T-199-20 | Spoofing | Back navigation state | mitigate | Route-flow tests require no stale drawer, step-up, filter, focus, or scroll-lock state after back. |
| T-199-SC | Tampering | package installs | accept | This plan adds no package-manager installs. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && mix test test/accrue_admin/e2e_fixtures_test.exs --max-failures 5`
- `cd accrue_admin && node --check e2e/phase191-page-flow-helpers.js && node --check e2e/admin-interaction-overlay-phase199.spec.js`
- `cd accrue_admin && npm run e2e:phase199 -- --grep @fixture`
</verification>

<success_criteria>
FIX-01 and FIX-02 are fully covered by deterministic seed tests and composed-route browser checks, with no schema, package, production route, or broad DSL changes.
</success_criteria>

<output>
Create `.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-04-SUMMARY.md` when done.
</output>

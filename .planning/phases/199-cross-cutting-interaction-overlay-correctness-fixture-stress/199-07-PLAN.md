---
phase: 199-cross-cutting-interaction-overlay-correctness-fixture-stress
plan: "07"
type: execute
wave: 3
depends_on: ["199-06"]
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/assets/css/theme.css
  - accrue_admin/priv/static/accrue_admin.css
  - accrue_admin/e2e/reduced-motion.spec.js
  - accrue_admin/test/accrue_admin/components/overlay_components_test.exs
  - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
autonomous: true
requirements: [IXN-02, IXN-04]
must_haves:
  truths:
    - "IXN-02: Desktop drawers right-dock with horizontal travel and mobile drawers bottom-sheet with vertical travel."
    - "IXN-02: Focus rings appear immediately and reduced motion removes travel while preserving state change."
    - "IXN-04: CSS checks cover overlay shell geometry without introducing trapped fixed descendants."
  artifacts:
    - path: "accrue_admin/assets/css/app.css"
      provides: "Drawer geometry and overlay shell source rules"
    - path: "accrue_admin/assets/css/theme.css"
      provides: "Motion token and reduced-motion behavior"
    - path: "accrue_admin/e2e/reduced-motion.spec.js"
      provides: "Reduced-motion coverage for final overlay/floating surfaces"
    - path: "accrue_admin/priv/static/accrue_admin.css"
      provides: "Rebuilt served admin CSS bundle"
  key_links:
    - from: "accrue_admin/assets/css/app.css"
      to: "accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js"
      via: "rendered drawer geometry is asserted in browser"
      pattern: "ax-detail-drawer"
    - from: "accrue_admin/e2e/reduced-motion.spec.js"
      to: "accrue_admin/assets/css/theme.css"
      via: "computed duration/transform assertions use motion tokens"
      pattern: "prefers-reduced-motion"
  prohibitions:
    - "No transition-all, layout-property motion, per-row stagger, or focus-ring animation."
    - "No new color hues or one-off theme-specific hex values."
---

<objective>
Green drawer geometry, motion tokens, and reduced-motion coverage.

Purpose: close the geometry/motion portion of IXN-02 while keeping the custom `ax-*` design system and generated CSS in sync.
Output: updated CSS/tests, rebuilt CSS bundle, and passing Phase 199 @motion checks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-CONTEXT.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-PATTERNS.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-UI-SPEC.md
@.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-06-SUMMARY.md
@accrue_admin/guides/motion.md
@accrue_admin/assets/css/app.css
@accrue_admin/assets/css/theme.css
@accrue_admin/e2e/reduced-motion.spec.js
</context>

## Artifacts this phase produces

- Extended CSS geometry assertions in `overlay_components_test.exs`.
- Extended reduced-motion browser checks in `reduced-motion.spec.js`.
- Rebuilt generated asset: `accrue_admin/priv/static/accrue_admin.css`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct drawer geometry, motion tokens, and reduced-motion coverage</name>
  <files>accrue_admin/assets/css/app.css, accrue_admin/assets/css/theme.css, accrue_admin/priv/static/accrue_admin.css, accrue_admin/e2e/reduced-motion.spec.js, accrue_admin/test/accrue_admin/components/overlay_components_test.exs, accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js</files>
  <read_first>
    - accrue_admin/assets/css/app.css
    - accrue_admin/assets/css/theme.css
    - accrue_admin/e2e/reduced-motion.spec.js
    - accrue_admin/test/accrue_admin/components/overlay_components_test.exs
    - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
    - accrue_admin/guides/motion.md
  </read_first>
  <behavior>
    - Desktop drawer CSS uses right-edge docking and `translateX(100%)` entry; mobile CSS uses bottom docking and `translateY(100%)` entry per D-07.
    - Overlay/floating transitions compose from `--ax-dur-*`, `--ax-ease-*`, and `--ax-transition-*` tokens, with max drawer/modal travel duration 240ms per D-07.
    - Reduced motion collapses travel/scale via token overrides and preserves visible state changes per D-09.
    - Focus rings are instant and not animated per D-09.
  </behavior>
  <action>Patch source CSS and tests to satisfy the locked geometry/motion contract. Extend `reduced-motion.spec.js` for drawer/mobile-sheet, command palette, dropdown/popover classes, and focus-ring immediacy. Rebuild CSS with `mix accrue_admin.assets.build` after source CSS changes. Keep existing `--ax-*` tokens as the source of truth.</action>
  <acceptance_criteria>
    - Component CSS tests assert desktop `translateX(100%)` and mobile `translateY(100%)`.
    - `reduced-motion.spec.js` includes overlay/floating surfaces beyond the earlier button/dropdown/palette baseline.
    - `npm run e2e:phase199 -- --grep @motion` passes.
    - `priv/static/accrue_admin.css` is rebuilt when source CSS changes.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/overlay_components_test.exs --max-failures 5</automated>
    <automated>cd accrue_admin && env -u NO_COLOR playwright test e2e/reduced-motion.spec.js --timeout=60000 --workers=1</automated>
    <automated>cd accrue_admin && npm run e2e:phase199 -- --grep @motion</automated>
    <automated>cd accrue_admin && mix accrue_admin.assets.build && git diff --check priv/static/accrue_admin.css</automated>
  </verify>
  <done>IXN-02 drawer geometry, focus ring, and reduced-motion expectations are green.</done>
</task>

</tasks>

<threat_model>
## ASVS Context

ASVS level: 1. Security block threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS geometry to browser hit testing | CSS determines whether panels are reachable, clipped, or trapped under page wrappers. |
| Source CSS to committed bundle | Runtime CSS ships through `priv/static/accrue_admin.css`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-199-15 | Denial of Service | Motion/reduced motion | mitigate | Reduced-motion tests ensure travel collapses and focus rings remain immediate. |
| T-199-16 | Tampering | Generated CSS bundle | mitigate | Asset build plus `git diff --check` verifies committed CSS output. |
| T-199-SC | Tampering | package installs | accept | This plan adds no package-manager installs. |
</threat_model>

<verification>
Run:
- `cd accrue_admin && env -u NO_COLOR playwright test e2e/reduced-motion.spec.js --timeout=60000 --workers=1`
- `cd accrue_admin && npm run e2e:phase199 -- --grep @motion`
- `cd accrue_admin && mix test test/accrue_admin/components/overlay_components_test.exs --max-failures 5`
- `cd accrue_admin && mix accrue_admin.assets.build && git diff --check priv/static/accrue_admin.css`
</verification>

<success_criteria>
IXN-02 drawer geometry and reduced-motion expectations are green across source, browser, and generated CSS checks.
</success_criteria>

<output>
Create `.planning/phases/199-cross-cutting-interaction-overlay-correctness-fixture-stress/199-07-SUMMARY.md` when done.
</output>

---
phase: 20-organization-billing-with-sigra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/mix.exs
  - examples/accrue_host/lib/accrue_host/accounts/organization.ex
  - examples/accrue_host/lib/accrue_host/accounts/organization_membership.ex
  - examples/accrue_host/lib/accrue_host/organizations.ex
  - examples/accrue_host/test/support/fixtures/accounts_fixtures.ex
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
  - accrue/test/accrue/billable_test.exs
  - examples/accrue_host/priv/repo/migrations/20260417210000_create_organizations.exs
  - examples/accrue_host/priv/repo/migrations/20260417210100_create_organization_memberships.exs
autonomous: true
requirements:
  - ORG-01
  - ORG-02
must_haves:
  truths:
    - "A Sigra-backed host organization can round-trip through `Accrue.Billing.customer/1` with `owner_type` and `owner_id` preserved."
    - "The example host compiles against a real local Sigra dependency and exposes the concrete `AccrueHost.Organizations` wrapper that later scope and admin work will call."
    - "The host test database has the organization and membership tables required for Phase 20 proof."
  artifacts:
    - path: "examples/accrue_host/mix.exs"
      provides: "Local Sigra path dependency with explicit Hex-release fallback behavior"
    - path: "examples/accrue_host/lib/accrue_host/organizations.ex"
      provides: "Concrete `use Sigra.Organizations` host wrapper"
    - path: "examples/accrue_host/lib/accrue_host/accounts/organization.ex"
      provides: "Billable Sigra-compatible organization schema"
    - path: "examples/accrue_host/lib/accrue_host/accounts/organization_membership.ex"
      provides: "Membership schema using Sigra role semantics"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host/accounts/organization.ex"
      to: "accrue/test/accrue/billable_test.exs"
      via: "shared `Accrue.Billable` ownership contract"
      pattern: "billable_type: \"Organization\"|owner_type == \"Organization\""
    - from: "examples/accrue_host/lib/accrue_host/organizations.ex"
      to: "examples/accrue_host/mix.exs"
      via: "host wrapper compiles against the real local Sigra dependency"
      pattern: "use Sigra\\.Organizations|{:sigra, path:"
---

<objective>
Create the concrete host-side Sigra foundation for Phase 20.

Purpose: the checker blocked the earlier plan because it did not target the actual Sigra API. This plan locks the real host dependency, wrapper module, organization schemas, and ORG-01 proof before any scope or admin work begins.
Output: local Sigra dependency wiring, billable organization and membership schemas, `AccrueHost.Organizations`, fixtures, migrations, and passing ORG-01 regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-organization-billing-with-sigra/20-RESEARCH.md
@.planning/phases/20-organization-billing-with-sigra/20-PATTERNS.md
@.planning/phases/20-organization-billing-with-sigra/20-VALIDATION.md
@.planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md
@/Users/jon/projects/sigra/lib/sigra/organizations.ex
@/Users/jon/projects/sigra/test/example/lib/example/organizations.ex
@/Users/jon/projects/sigra/test/example/lib/example/accounts/organization.ex
@/Users/jon/projects/sigra/test/example/lib/example/accounts/organization_membership.ex

<interfaces>
From `/Users/jon/projects/sigra/test/example/lib/example/organizations.ex`:
```elixir
use Sigra.Organizations,
  repo: Example.Repo,
  schemas: [
    organization: Example.Accounts.Organization,
    membership: Example.Accounts.OrganizationMembership,
    invitation: Example.Accounts.OrganizationInvitation,
    user_session: Example.Accounts.UserSession,
    organization_slug_alias: Example.Accounts.OrganizationSlugAlias,
    user: Example.Accounts.User,
    scope: Example.Accounts.Scope
  ]
```

From `accrue/lib/accrue/billable.ex`:
```elixir
use Accrue.Billable, billable_type: "User"
def __accrue__(:billable_type), do: @__accrue_billable_type__
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ORG-01 regression coverage for a real Sigra-backed organization billable</name>
  <read_first>
    - accrue/test/accrue/billable_test.exs
    - examples/accrue_host/test/accrue_host/billing_facade_test.exs
    - examples/accrue_host/test/support/fixtures/accounts_fixtures.ex
    - .planning/phases/20-organization-billing-with-sigra/20-VALIDATION.md
  </read_first>
  <files>accrue/test/accrue/billable_test.exs, examples/accrue_host/test/accrue_host/billing_facade_test.exs, examples/accrue_host/test/support/fixtures/accounts_fixtures.ex</files>
  <behavior>
    - Test 1: An organization using `Accrue.Billable` creates or fetches a customer whose `owner_type` is `Organization` and `owner_id` matches the organization id.
    - Test 2: `AccrueHost.Billing.customer_for/1` and `billing_state_for/1` accept an organization fixture backed by the host Sigra wrapper shape.
    - Test 3: The fixture layer can create Sigra-role memberships (`:owner`, `:admin`, `:member`) for downstream Phase 20 tests.
  </behavior>
  <action>Extend `accrue/test/accrue/billable_test.exs` and `examples/accrue_host/test/accrue_host/billing_facade_test.exs` so they prove organization billing through the unchanged public ownership contract. Update `examples/accrue_host/test/support/fixtures/accounts_fixtures.ex` to build organization, membership, and active-scope fixtures using the concrete Phase 20 Sigra host shape rather than a vague seam. Do not add org-specific ownership columns under `accrue/`; ORG-01 is satisfied only by the existing `owner_type` / `owner_id` contract.</action>
  <acceptance_criteria>
    - `accrue/test/accrue/billable_test.exs` contains `owner_type == "Organization"`
    - `examples/accrue_host/test/accrue_host/billing_facade_test.exs` contains `owner_type == "Organization"`
    - `examples/accrue_host/test/support/fixtures/accounts_fixtures.ex` contains organization and membership fixtures with roles `:owner`, `:admin`, and `:member`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/billable_test.exs --warnings-as-errors && cd ../examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <done>Executable ORG-01 coverage proves organizations use the existing public owner contract unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the local Sigra dependency, host wrapper, and organization schemas</name>
  <read_first>
    - examples/accrue_host/mix.exs
    - /Users/jon/projects/sigra/lib/sigra/organizations.ex
    - /Users/jon/projects/sigra/test/example/lib/example/organizations.ex
    - /Users/jon/projects/sigra/test/example/lib/example/accounts/organization.ex
    - /Users/jon/projects/sigra/test/example/lib/example/accounts/organization_membership.ex
  </read_first>
  <files>examples/accrue_host/mix.exs, examples/accrue_host/lib/accrue_host/accounts/organization.ex, examples/accrue_host/lib/accrue_host/accounts/organization_membership.ex, examples/accrue_host/lib/accrue_host/organizations.ex, examples/accrue_host/priv/repo/migrations/20260417210000_create_organizations.exs, examples/accrue_host/priv/repo/migrations/20260417210100_create_organization_memberships.exs</files>
  <action>Update `examples/accrue_host/mix.exs` to depend on local Sigra via `{:sigra, path: "../../../sigra"}` when not in Hex-release mode, and document or encode the Hex-release branch explicitly so host verification remains deterministic. Add `AccrueHost.Organizations` using `Sigra.Organizations` with the concrete wrapper shape from the Sigra example, substituting `AccrueHost` modules. Create `AccrueHost.Accounts.Organization` with Sigra-required fields `name`, `slug`, `deleted_at`, `personal`, and `owner_user_id`, plus `use Accrue.Billable, billable_type: "Organization"`. Create `AccrueHost.Accounts.OrganizationMembership` with `Ecto.Enum` roles `[:owner, :admin, :member]`. Add matching host migrations with uniqueness and foreign-key indexes. Do not create any client-driven active-org selector or trust request params for organization identity in this plan.</action>
  <acceptance_criteria>
    - `examples/accrue_host/mix.exs` contains a local `:sigra` path dependency and explicit Hex-release fallback behavior
    - `examples/accrue_host/lib/accrue_host/organizations.ex` contains `use Sigra.Organizations`
    - `examples/accrue_host/lib/accrue_host/accounts/organization.ex` contains `use Accrue.Billable, billable_type: "Organization"`
    - `examples/accrue_host/lib/accrue_host/accounts/organization.ex` contains `field :personal`
    - `examples/accrue_host/lib/accrue_host/accounts/organization_membership.ex` contains roles `[:owner, :admin, :member]`
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && mix deps.get && MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>The example host compiles against the real Sigra library and owns the concrete organization primitives later plans depend on.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the host organization schema before downstream verification</name>
  <read_first>
    - examples/accrue_host/priv/repo/migrations/20260417210000_create_organizations.exs
    - examples/accrue_host/priv/repo/migrations/20260417210100_create_organization_memberships.exs
    - .planning/phases/20-organization-billing-with-sigra/20-VALIDATION.md
  </read_first>
  <files>examples/accrue_host/priv/repo/migrations/20260417210000_create_organizations.exs, examples/accrue_host/priv/repo/migrations/20260417210100_create_organization_memberships.exs, examples/accrue_host/test/accrue_host/billing_facade_test.exs, accrue/test/accrue/billable_test.exs</files>
  <action>After the migration files are complete, run `cd examples/accrue_host && MIX_ENV=test mix ecto.migrate` before any later Phase 20 host or admin proof. Then rerun the focused `accrue` and host tests from this plan. If the database is unavailable, record the exact blocked command/output in the summary rather than claiming the foundation is green.</action>
  <acceptance_criteria>
    - The plan summary records the exact migration command `cd examples/accrue_host && MIX_ENV=test mix ecto.migrate`
    - Either the migration exits 0 or the summary records the blocking output verbatim
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix ecto.migrate && mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs && cd ../../accrue && mix test test/accrue/billable_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The host schema foundation is migrated and ready for Sigra hydration, LiveView, and admin execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host organization schemas -> `Accrue.Billing.customer/1` | Host-owned org rows cross into Accrue's public ownership contract |
| local Sigra dependency -> example host | The host compiles against a real local library surface instead of an invented shim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | `examples/accrue_host/lib/accrue_host/accounts/organization.ex`, `accrue/test/accrue/billable_test.exs` | mitigate | Keep ownership on the existing `Accrue.Billable` contract and prove it with ORG-01 tests; reject any new org-specific billing storage. |
| T-20-02 | E | `examples/accrue_host/lib/accrue_host/accounts/organization_membership.ex` | mitigate | Persist explicit roles `:owner`, `:admin`, `:member` for server-side authorization; no client-selected org identity enters the contract here. |
| T-20-04 | T | `examples/accrue_host/mix.exs`, `examples/accrue_host/lib/accrue_host/organizations.ex` | mitigate | Target the actual local Sigra API surface so later plans do not drift onto an unverifiable fallback seam. |
</threat_model>

<verification>
Run `cd examples/accrue_host && mix deps.get && MIX_ENV=test mix compile --warnings-as-errors && MIX_ENV=test mix ecto.migrate && mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs`, then `cd ../../accrue && mix test test/accrue/billable_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
The example host is wired to the local Sigra source, organizations are concrete billables, and ORG-01 proof passes on the migrated host schema.
</success_criteria>

<output>
After completion, create `.planning/phases/20-organization-billing-with-sigra/20-01-SUMMARY.md`
</output>

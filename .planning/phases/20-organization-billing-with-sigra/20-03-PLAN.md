---
phase: 20-organization-billing-with-sigra
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - accrue_admin/lib/accrue_admin/router.ex
  - accrue_admin/lib/accrue_admin/auth_hook.ex
  - accrue_admin/lib/accrue_admin/owner_scope.ex
  - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
autonomous: true
requirements:
  - ORG-03
must_haves:
  truths:
    - "The host-mounted admin session carries enough organization context for `accrue_admin` to resolve owner scope once per mount."
    - "Admin scope resolution distinguishes global admin, organization admin, and out-of-scope org routes through Sigra-aware owner scope."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/owner_scope.ex"
      provides: "Shared owner-scope normalization for admin routes and LiveViews"
    - path: "accrue_admin/lib/accrue_admin/router.ex"
      provides: "Threaded session keys for owner scope"
    - path: "examples/accrue_host/test/accrue_host_web/admin_mount_test.exs"
      provides: "Host mount proof for Phase 20 admin scope threading"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/router.ex"
      to: "accrue_admin/lib/accrue_admin/auth_hook.ex"
      via: "session threading and assign setup"
      pattern: "session_keys|current_owner_scope"
    - from: "accrue_admin/lib/accrue_admin/auth_hook.ex"
      to: "accrue_admin/lib/accrue_admin/owner_scope.ex"
      via: "mount-time owner scope resolution"
      pattern: "current_owner_scope|resolve"
---

<objective>
Thread owner scope through the admin session before touching query modules.

Purpose: the previous 20-03 was too large and missed the required host admin mount verification. This plan isolates the session and owner-scope contract, with the exact host verification command the checker requested.
Output: router/auth threading, shared owner scope helper, and passing host admin mount proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-organization-billing-with-sigra/20-RESEARCH.md
@.planning/phases/20-organization-billing-with-sigra/20-PATTERNS.md
@.planning/phases/20-organization-billing-with-sigra/20-VALIDATION.md
@.planning/phases/20-organization-billing-with-sigra/20-01-SUMMARY.md
@/Users/jon/projects/sigra/lib/sigra/live_view/admin_scope.ex
@/Users/jon/projects/sigra/lib/sigra/admin/scope.ex

<interfaces>
From `/Users/jon/projects/sigra/lib/sigra/admin/scope.ex`:
```elixir
Scope.resolve(scope, requested_org, policy_module)
#=> {:ok, t()} | {:error, :unauthenticated | :forbidden | :not_found}
```

From `accrue_admin/lib/accrue_admin/router.ex`:
```elixir
live_session :accrue_admin,
  on_mount: on_mount,
  session: {AccrueAdmin.Router, :__session__, [session_keys, mount_path]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ORG-03 host proof that admin mount threads organization scope into `accrue_admin`</name>
  <read_first>
    - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
    - .planning/phases/20-organization-billing-with-sigra/20-VALIDATION.md
  </read_first>
  <files>examples/accrue_host/test/accrue_host_web/admin_mount_test.exs</files>
  <behavior>
    - Test 1: The host admin mount passes the active organization id, slug, and membership-derived admin scope needed by `accrue_admin`.
    - Test 2: An out-of-scope organization route is not mounted as a valid organization owner scope.
  </behavior>
  <action>Extend `examples/accrue_host/test/accrue_host_web/admin_mount_test.exs` so it proves the mounted admin session carries the active-organization keys consumed by the new owner-scope helper. Keep the assertions focused on session and assign shape, not downstream row loading. This is the explicit host proof the checker required before query work proceeds.</action>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host_web/admin_mount_test.exs` contains active-organization session assertions
    - the test file covers an out-of-scope org route case
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host_web/admin_mount_test.exs</automated>
  </verify>
  <done>ORG-03 admin session contract is covered before implementation lands.</done>
</task>

<task type="auto">
  <name>Task 2: Add ORG-03 shared admin owner scope and wire router/auth threading</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/router.ex
    - accrue_admin/lib/accrue_admin/auth_hook.ex
    - /Users/jon/projects/sigra/lib/sigra/live_view/admin_scope.ex
    - /Users/jon/projects/sigra/lib/sigra/admin/scope.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/router.ex, accrue_admin/lib/accrue_admin/auth_hook.ex, accrue_admin/lib/accrue_admin/owner_scope.ex</files>
  <action>Create `accrue_admin/lib/accrue_admin/owner_scope.ex` as the shared Phase 20 contract that normalizes the host session into one owner-scope shape and bridges to Sigra-style global versus organization admin scope semantics. Update `AccrueAdmin.Router` to thread the needed active-organization keys once through the existing session transport. Update `AccrueAdmin.AuthHook` to assign `:current_owner_scope` during mount without inventing a second session path. Keep this plan focused on session and assign plumbing only; query modules and LiveViews move in later plans.</action>
  <acceptance_criteria>
    - `accrue_admin/lib/accrue_admin/auth_hook.ex` contains `current_owner_scope`
    - `accrue_admin/lib/accrue_admin/router.ex` threads owner scope session keys once
    - `accrue_admin/lib/accrue_admin/owner_scope.ex` exposes a resolve function for global versus organization scope
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host_web/admin_mount_test.exs</automated>
  </verify>
  <done>ORG-03 `accrue_admin` mounts with a single shared owner-scope contract ready for query gating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host admin session -> `accrue_admin` | Host-provided organization context enters the admin package |
| route intent -> owner scope | Admin route params determine whether the request is global or org-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | I | `accrue_admin/lib/accrue_admin/router.ex`, `auth_hook.ex` | mitigate | Thread active-organization context once and resolve owner scope at mount so later loaders cannot silently fall back to global lookups. |
| T-20-02 | E | `accrue_admin/lib/accrue_admin/owner_scope.ex` | mitigate | Normalize only server-derived session and route state; do not trust client-selected organization ids for billing authorization. |
</threat_model>

<verification>
Run `cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host_web/admin_mount_test.exs`.

Task 1 automated verification must run this exact command:
`cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host_web/admin_mount_test.exs`
</verification>

<success_criteria>
The admin package receives one shared owner scope from the host mount path, and the required admin mount verification is present and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/20-organization-billing-with-sigra/20-03-SUMMARY.md`
</output>

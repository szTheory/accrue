---
phase: 200-idempotent-verification-sign-off
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/dev/storybook.ex
  - accrue_admin/storybook/_support/registry_story.ex
  - storybook/components/component_registry.story.exs
  - storybook/groups/component_groups.story.exs
  - accrue_admin/test/accrue_admin/dev/storybook_coverage_test.exs
  - accrue_admin/test/accrue_admin/dev/storybook_asset_test.exs
  - accrue_admin/test/accrue_admin/theme_test.exs
autonomous: true
requirements:
  - STY-02
  - STY-03
must_haves:
  truths:
    - "STY-02: Every family returned by ComponentRegistry.entries/0 has Storybook coverage derived from registry data, not copied metadata."
    - "STY-02: All eight group_contracts/0 slugs have Storybook coverage with proof selectors and representative composition markers."
    - "STY-03: Storybook color mode uses the committed storybook.css/storybook.js bundle and the ax-theme-dark-shim bridge."
    - "STY-03: /dev/components remains green as the second renderer after Storybook coverage is added."
  artifacts:
    - path: "accrue_admin/storybook/_support/registry_story.ex"
      provides: "Registry-derived component and group Storybook support"
      exports: ["variations_for/1", "component_variations/0", "group_variations/0"]
    - path: "storybook/components/component_registry.story.exs"
      provides: "Generated registry component story coverage"
    - path: "storybook/groups/component_groups.story.exs"
      provides: "Generated group-contract story coverage"
    - path: "accrue_admin/test/accrue_admin/dev/storybook_coverage_test.exs"
      provides: "Dynamic STY-02 coverage gate"
  key_links:
    - from: "storybook/components/component_registry.story.exs"
      to: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      via: "RegistryStory helpers call ComponentRegistry.entries/0 and variants_for/1"
      pattern: "ComponentRegistry\\.(entries|variants_for)"
    - from: "storybook/groups/component_groups.story.exs"
      to: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      via: "RegistryStory helpers call ComponentRegistry.group_contracts/0"
      pattern: "group_contracts"
  prohibitions:
    - statement: "No Tailwind rebuild path or separate Storybook styling surface."
      status: "resolved by plan action"
      verification: "storybook asset tests compare mounted routes to committed priv/static/storybook.css and storybook.js"
    - statement: "Storybook must not replace /billing/dev/components."
      status: "resolved by plan action"
      verification: "component registry and group registry drift tests remain in the verify command"
---

<objective>
Complete registry-driven PhoenixStorybook coverage and theme parity for STY-02 and STY-03.

Purpose: Storybook becomes the human-facing design lab without replacing the deterministic /dev/components renderer or duplicating ComponentRegistry truth.
Output: Enhanced RegistryStory support, generated component/group story modules, dynamic coverage tests, asset/theming tests, and Storybook color-mode configuration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md
@.planning/phases/200-idempotent-verification-sign-off/200-RESEARCH.md
@.planning/phases/200-idempotent-verification-sign-off/200-PATTERNS.md
@.planning/phases/200-idempotent-verification-sign-off/200-VALIDATION.md
@.planning/phases/200-idempotent-verification-sign-off/200-UI-SPEC.md
@.planning/phases/193-research-re-baseline-pattern-lock/193-04-SUMMARY.md
@accrue_admin/lib/accrue_admin/dev/component_registry.ex
@accrue_admin/lib/accrue_admin/dev/storybook.ex
@accrue_admin/storybook/_support/registry_story.ex
@accrue_admin/test/accrue_admin/dev/component_registry_test.exs
@accrue_admin/test/accrue_admin/dev/component_group_registry_test.exs
@accrue_admin/test/accrue_admin/assets_test.exs
@accrue_admin/test/accrue_admin/theme_test.exs
@accrue_admin/priv/static/storybook.css
@accrue_admin/priv/static/storybook.js
</context>

## Artifacts this phase produces

- Module extension: `AccrueAdmin.Dev.Storybook` config field `color_mode: true`.
- Module extension: `AccrueAdmin.Storybook.RegistryStory.variations_for/1`.
- New support functions: `AccrueAdmin.Storybook.RegistryStory.component_variations/0`, `group_variations/0`, `slug_id/1`, and `dom_id/2`.
- Story module: `AccrueAdmin.Storybook.Components.ComponentRegistry` in `storybook/components/component_registry.story.exs`.
- Story module: `AccrueAdmin.Storybook.Groups.ComponentGroups` in `storybook/groups/component_groups.story.exs`.
- Test module: `AccrueAdmin.Dev.StorybookCoverageTest` in `accrue_admin/test/accrue_admin/dev/storybook_coverage_test.exs`.
- Test module: `AccrueAdmin.Dev.StorybookAssetTest` in `accrue_admin/test/accrue_admin/dev/storybook_asset_test.exs`.
- Extended test coverage in `accrue_admin/test/accrue_admin/theme_test.exs` for Storybook dark-shim parity.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Storybook backend and registry support</name>
  <files>accrue_admin/lib/accrue_admin/dev/storybook.ex, accrue_admin/storybook/_support/registry_story.ex, accrue_admin/test/accrue_admin/theme_test.exs</files>
  <read_first>
    <file>accrue_admin/lib/accrue_admin/dev/storybook.ex</file>
    <file>accrue_admin/storybook/_support/registry_story.ex</file>
    <file>accrue_admin/test/accrue_admin/theme_test.exs</file>
    <file>accrue_admin/lib/accrue_admin/dev/component_registry.ex</file>
    <file>accrue_admin/assets/css/theme.css</file>
    <file>accrue_admin/priv/static/storybook.css</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Assert `AccrueAdmin.Dev.Storybook` enables PhoenixStorybook color mode while preserving `color_mode_sandbox_dark_class: "ax-theme-dark-shim"`.
    - Assert Storybook dark shim parity fails if a dark `--ax-*` token from `theme.css` is absent from `priv/static/storybook.css`.
    - Assert registry variation IDs are slugified, stable, unique, and derived from registry family, variant, state, and specimen data.
  </behavior>
  <action>Per D-06, D-07, D-08, D-09, D-10, and D-11, extend the existing Storybook backend and `RegistryStory` support instead of creating a parallel catalog. Add `color_mode: true` to `AccrueAdmin.Dev.Storybook` beside the existing committed `css_path`, `js_path`, `sandbox_class: "accrue-admin"`, and `color_mode_sandbox_dark_class: "ax-theme-dark-shim"`. Extend `RegistryStory` so it derives state groups, `na_states` notes, stable slugified variation IDs, unique DOM IDs, and template or named-slot escape hatches from `ComponentRegistry.entries/0`, `variants_for/1`, and `group_contracts/0`. Keep the `if Mix.env() != :prod do` guards. Do not add packages, a Tailwind build path, hardcoded registry counts, copied variant/state lists, or new runtime admin routes.</action>
  <acceptance_criteria>
    - Source assertion: `accrue_admin/lib/accrue_admin/dev/storybook.ex` contains `color_mode: true` and still contains `color_mode_sandbox_dark_class: "ax-theme-dark-shim"`.
    - Source assertion: `RegistryStory` calls `ComponentRegistry.entries/0`, `variants_for/1`, or `group_contracts/0` for Storybook metadata.
    - Behavior assertion: `theme_test.exs` includes a Storybook dark-shim parity test comparing `theme.css` dark tokens to `priv/static/storybook.css`.
    - Prohibition assertion: no new package dependency or Tailwind-specific Storybook build command is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/theme_test.exs test/accrue_admin/dev/component_registry_test.exs --max-failures 5</automated>
  </verify>
  <done>Storybook backend color mode is enabled, registry support is dynamic and SSOT-backed, and dark-token shim parity is covered by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dynamic component and group story coverage</name>
  <files>storybook/components/component_registry.story.exs, storybook/groups/component_groups.story.exs, accrue_admin/test/accrue_admin/dev/storybook_coverage_test.exs</files>
  <read_first>
    <file>storybook/components/component_registry.story.exs (new)</file>
    <file>storybook/groups/component_groups.story.exs (new)</file>
    <file>accrue_admin/test/accrue_admin/dev/storybook_coverage_test.exs (new)</file>
    <file>storybook/components/button.story.exs</file>
    <file>storybook/components/page_header.story.exs</file>
    <file>accrue_admin/storybook/_support/registry_story.ex</file>
    <file>accrue_admin/lib/accrue_admin/dev/component_registry.ex</file>
    <file>accrue_admin/test/accrue_admin/dev/component_registry_test.exs</file>
    <file>accrue_admin/test/accrue_admin/dev/component_group_registry_test.exs</file>
  </read_first>
  <behavior>
    - Assert expected families are computed from `ComponentRegistry.entries/0 |> Enum.map(&amp; &amp;1.family) |> Enum.uniq()`.
    - Assert expected groups are computed from `ComponentRegistry.group_contracts/0`.
    - Assert every group story renders a stable `data-component-group` marker matching the contract slug.
    - Assert all registry applicable states appear in Storybook or are represented by registry-derived `na_states` notes.
  </behavior>
  <action>Per D-06, D-07, D-08, D-09, and D-10, add registry-driven Storybook coverage for all current and future registry families and all group contracts. Create `AccrueAdmin.Storybook.Components.ComponentRegistry` and `AccrueAdmin.Storybook.Groups.ComponentGroups` story modules that delegate metadata to `RegistryStory`; use curated template escape hatches only for composites, named slots, overlays, drawers, and group compositions that cannot render from flat props. Create `AccrueAdmin.Dev.StorybookCoverageTest` to derive expected family, entry, applicable-state, N/A-state, and group coverage dynamically. Keep existing curated stories as additional examples; do not weaken or replace `/billing/dev/components` drift coverage.</action>
  <acceptance_criteria>
    - Behavior assertion: every family from `ComponentRegistry.entries/0` is covered by a Storybook story or variation.
    - Behavior assertion: every slug from `ComponentRegistry.group_contracts/0` is covered by a Storybook story with one representative `data-component-group` marker.
    - Source assertion: tests derive expected coverage from registry functions, not from the current counts `30`, `42`, or `8`.
    - Behavior assertion: `component_registry_test.exs` and `component_group_registry_test.exs` still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/dev/storybook_coverage_test.exs test/accrue_admin/dev/component_registry_test.exs test/accrue_admin/dev/component_group_registry_test.exs --max-failures 5</automated>
  </verify>
  <done>Storybook coverage dynamically spans every registry family and group while `/dev/components` remains the second renderer and drift target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Storybook assets and adopter leak boundaries</name>
  <files>accrue_admin/test/accrue_admin/dev/storybook_asset_test.exs</files>
  <read_first>
    <file>accrue_admin/test/accrue_admin/dev/storybook_asset_test.exs (new)</file>
    <file>accrue_admin/test/accrue_admin/assets_test.exs</file>
    <file>accrue_admin/lib/accrue_admin/assets.ex</file>
    <file>accrue_admin/lib/accrue_admin/router.ex</file>
    <file>accrue_admin/mix.exs</file>
    <file>examples/accrue_host/mix.exs</file>
    <file>accrue_admin/priv/static/storybook.css</file>
    <file>accrue_admin/priv/static/storybook.js</file>
  </read_first>
  <behavior>
    - Assert mounted Storybook CSS and JS asset paths return 200 and match committed `priv/static/storybook.css` and `priv/static/storybook.js` bodies.
    - Assert `phoenix_storybook` remains dev/test-only and host/adopter production compile exposes no `/dev/storybook` route.
  </behavior>
  <action>Per D-11 and D-12, add request/response tests for `AccrueAdmin.Assets.hashed_path(:storybook_css, "/dev/storybook")` and `AccrueAdmin.Assets.hashed_path(:storybook_js, "/dev/storybook")` using the same ConnCase pattern as `assets_test.exs`. Verify the response bodies equal the committed bundle files and that content types are correct. Add host/adopter leak proof using the existing `Code.ensure_loaded?(PhoenixStorybook.Router)` router boundary and `phoenix_storybook` dev/test-only dependency declaration. Do not expose Storybook from production host routes.</action>
  <acceptance_criteria>
    - CLI assertion: `storybook_asset_test.exs` passes with CSS and JS route status 200 and body equality checks.
    - Source assertion: `accrue_admin/mix.exs` keeps `:phoenix_storybook` scoped to `only: [:dev, :test]`.
    - Behavior assertion: host/adopter production compile or route inspection shows no `/dev/storybook` route.
    - Prohibition assertion: Storybook asset serving continues through committed `AccrueAdmin.Assets` routes.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/dev/storybook_asset_test.exs --max-failures 5</automated>
    <automated>cd examples/accrue_host && MIX_ENV=prod mix compile --warnings-as-errors</automated>
  </verify>
  <done>Storybook assets are served from committed bundles, and adopter runtime surfaces do not expose Storybook.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Complete Storybook coverage and theming as part of final forward-only closeout | 200-01 | COVERED | Implements the Storybook portion of the phase goal.
REQ | STY-02 | Registry families and all eight groups have generated Storybook coverage | 200-01 | COVERED | Dynamic registry tests prevent hardcoded inventory drift.
REQ | STY-03 | Stories render in both color modes against committed ax bundle | 200-01 | COVERED | Backend color mode, shim parity, and asset tests.
RESEARCH | Storybook coverage/theming | Hybrid generated plus curated strategy; registry SSOT; asset and leak checks | 200-01 | COVERED | Follows research recommendation and pattern map.
CONTEXT | D-06,D-07,D-08,D-09,D-10,D-11,D-12 | Storybook strategy, SSOT, dynamic inventory, support extensions, kitchen retention, theme parity, asset/leak proof | 200-01 | COVERED | Task actions cite each decision.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Storybook dev route | Rendered story pages and assets are served only in dev/test surfaces. |
| registry -> Storybook templates | Registry data drives rendered examples and must not become duplicated or unsafe raw content. |
| host/adopter runtime -> dev tooling | Host runtime must not receive Storybook routes or dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-01 | Information Disclosure | `/dev/storybook` routing | mitigate | Keep `phoenix_storybook` dev/test-only, keep `Code.ensure_loaded?(PhoenixStorybook.Router)`, and add host/adopter leak tests per D-12. |
| T-200-02 | Tampering | Storybook metadata | mitigate | Derive coverage from `ComponentRegistry.entries/0` and `group_contracts/0`; dynamic tests fail copied or stale metadata per D-07/D-08. |
| T-200-03 | Spoofing | Storybook dark mode | mitigate | Enable PhoenixStorybook color mode and verify dark-shim token parity with committed CSS per D-11. |
| T-200-04 | Elevation of Privilege | Story templates | accept | Stories are dev/test static examples backed by local registry data; no user-submitted HTML is introduced in this plan. |
| T-200-SC | Tampering | package installs | mitigate | No package-manager install is planned; if execution adds npm, pip, or cargo installs, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
Run focused ExUnit Storybook, asset, registry, group, and theme tests listed in each task. Do not run broad browser or final scorecard gates in this plan.
</verification>

<success_criteria>
STY-02 and STY-03 have deterministic source-level and request-level coverage foundations: dynamic Storybook inventory, group story proof markers, committed asset serving, color-mode config, dark-shim parity, and adopter leak protection.
</success_criteria>

<output>
Create `.planning/phases/200-idempotent-verification-sign-off/200-01-SUMMARY.md` when done.
</output>

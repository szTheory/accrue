---
phase: 200-idempotent-verification-sign-off
plan: "03"
type: execute
wave: 3
depends_on: ["200-02"]
files_modified:
  - accrue_admin/e2e/phase200-scorecard.mjs
  - scripts/ci/verify_phase200_scorecard.mjs
  - .planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - "VER-01: baseline.union.cells.json is the immutable union of archived component/group and page-flow baselines."
    - "VER-01: duplicate union cell IDs fail generation unless a structured duplicate proof is emitted."
    - "VER-01: final comparison fails on score downgrade, coverage downgrade, missing evidence, or any non-empty regressions.ndjson row."
    - "VER-01: p193 page-flow pending cells must finish as covered with score >= 2 and deterministic evidence refs."
  artifacts:
    - path: "accrue_admin/e2e/phase200-scorecard.mjs"
      provides: "Phase 200 scorecard package generator"
      exports: ["generatePhase200Scorecard", "main"]
    - path: "scripts/ci/verify_phase200_scorecard.mjs"
      provides: "Phase 200 structured artifact verifier"
      exports: ["verifyPhase200Scorecard", "main"]
    - path: ".planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json"
      provides: "Union baseline artifact"
  key_links:
    - from: "accrue_admin/e2e/phase200-scorecard.mjs"
      to: ".planning/milestones/v1.53-phases/187-audit-baseline/baseline.cells.json"
      via: "read-only archived component/group baseline"
      pattern: "baseline\\.cells\\.json"
    - from: "accrue_admin/e2e/phase200-scorecard.mjs"
      to: ".planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json"
      via: "read-only archived page-flow baseline"
      pattern: "baseline\\.page-flow\\.cells\\.json"
  prohibitions:
    - statement: "Archived baseline files must not be mutated."
      status: "resolved by plan action"
      verification: "generator writes derived outputs only under the Phase 200 directory"
---

<objective>
Build the Phase 200 union scorecard generator and verifier for VER-01.

Purpose: The final closeout needs structured, repeatable comparison against the archived union baseline before any markdown or maintainer decision can be trusted.
Output: `phase200-scorecard.mjs`, `verify_phase200_scorecard.mjs`, and `baseline.union.cells.json`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md
@.planning/phases/200-idempotent-verification-sign-off/200-RESEARCH.md
@.planning/phases/200-idempotent-verification-sign-off/200-PATTERNS.md
@.planning/phases/200-idempotent-verification-sign-off/200-VALIDATION.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-01-SUMMARY.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-02-SUMMARY.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-06-SUMMARY.md
@accrue_admin/e2e/phase192-scorecard.mjs
@scripts/ci/verify_phase192_scorecard.mjs
@accrue_admin/e2e/baseline-manifest.js
@.planning/milestones/v1.53-phases/187-audit-baseline/baseline.cells.json
@.planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json
</context>

## Artifacts this phase produces

- Script: `accrue_admin/e2e/phase200-scorecard.mjs` with `generatePhase200Scorecard(options)` and `main(argv)`.
- Script: `scripts/ci/verify_phase200_scorecard.mjs` with `verifyPhase200Scorecard(options)` and `main(argv)`.
- Generated artifact: `.planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json`.
- Later generated artifacts owned by this script: `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, and `200-SCORECARD.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create union baseline and scorecard generator</name>
  <files>accrue_admin/e2e/phase200-scorecard.mjs, .planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json</files>
  <read_first>
    <file>accrue_admin/e2e/phase200-scorecard.mjs (new)</file>
    <file>accrue_admin/e2e/phase192-scorecard.mjs</file>
    <file>.planning/milestones/v1.53-phases/187-audit-baseline/baseline.cells.json</file>
    <file>.planning/milestones/v1.53-phases/187-audit-baseline/baseline.page-flow.cells.json</file>
    <file>accrue_admin/test-results/phase200/storybook-a11y.json</file>
    <file>accrue_admin/test-results/phase200/page-flow-evidence.json</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Self-test fails on duplicate union IDs.
    - Self-test fails when a comparable final cell has a lower score than its baseline.
    - Self-test fails when a `p193` page-flow row stays pending, lacks evidence, or has score below 2.
    - Baseline-only mode writes `baseline.union.cells.json` under the Phase 200 directory without modifying archived inputs.
  </behavior>
  <action>Per D-13, D-14, D-15, D-16, D-17, and D-25, create a Phase 200 scorecard generator by parameterizing or narrowly copying the Phase 192 reducer. Read the archived component/group baseline and archived page-flow baseline as immutable inputs, write `baseline.union.cells.json` under `.planning/phases/200-idempotent-verification-sign-off/`, discover Phase 200 evidence from `accrue_admin/test-results/phase200/`, and generate final artifacts only under the Phase 200 directory. The reducer must preserve structured JSON/NDJSON as canonical and render `200-SCORECARD.md` only from structured output. Include `--self-test`, `--baseline-only`, `--dry-run`, and normal write modes.</action>
  <acceptance_criteria>
    - CLI assertion: `node accrue_admin/e2e/phase200-scorecard.mjs --self-test` passes.
    - Artifact assertion: `baseline.union.cells.json` exists under the Phase 200 directory after baseline-only generation.
    - Source assertion: archived baseline paths are read inputs only; no output path points into Phase 187, Phase 192, or milestone archive directories.
    - Behavior assertion: self-tests include duplicate union ID, score downgrade, missing evidence, coverage downgrade, and p193 pending closure failures.
  </acceptance_criteria>
  <verify>
    <automated>node accrue_admin/e2e/phase200-scorecard.mjs --self-test</automated>
    <automated>node accrue_admin/e2e/phase200-scorecard.mjs --baseline-only</automated>
  </verify>
  <done>The union baseline generator exists, self-tests fail closed, and `baseline.union.cells.json` is created without mutating archived baselines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create strict Phase 200 scorecard verifier</name>
  <files>scripts/ci/verify_phase200_scorecard.mjs</files>
  <read_first>
    <file>scripts/ci/verify_phase200_scorecard.mjs (new)</file>
    <file>scripts/ci/verify_phase192_scorecard.mjs</file>
    <file>accrue_admin/e2e/phase200-scorecard.mjs</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/baseline.union.cells.json</file>
    <file>.planning/phases/200-idempotent-verification-sign-off/200-CONTEXT.md</file>
  </read_first>
  <behavior>
    - `--self-test` covers valid passing artifacts and invalid regressions, missing evidence, bad manifest refs, stale pending page-flow rows, malformed JSON, and duplicate cell IDs.
    - `--baseline-only` validates union baseline shape and count without requiring final artifacts.
    - Full mode fails on any row in `regressions.ndjson`.
    - Full mode rejects absolute paths, `..` segments, and unmanifested evidence refs.
  </behavior>
  <action>Per D-13, D-14, D-15, D-16, D-17, and D-25, create `verifyPhase200Scorecard(options)` and CLI entrypoint using the Phase 192 verifier style. Validate `baseline.union.cells.json`, `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, and `artifacts.manifest.json`; require no coverage downgrade, no score downgrade, evidence refs for deterministic claims, and empty regression rows for ACCEPT. Allow repo-relative refs only under `accrue_admin/test-results/`, `accrue_admin/playwright-report/`, `.planning/phases/200-idempotent-verification-sign-off/`, or explicit `playwright-trace:` refs. Add the Phase 200 rule that `p193` page-flow baseline rows must close as `coverage_status: "covered"` with score `>= 2` and evidence refs in full mode.</action>
  <acceptance_criteria>
    - CLI assertion: `node scripts/ci/verify_phase200_scorecard.mjs --self-test` passes.
    - CLI assertion: `node scripts/ci/verify_phase200_scorecard.mjs --baseline-only` passes after Task 1.
    - Source assertion: full verifier reads Phase 200 paths and not Phase 192 output paths.
    - Behavior assertion: non-empty `regressions.ndjson` always fails full verification.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_phase200_scorecard.mjs --self-test</automated>
    <automated>node scripts/ci/verify_phase200_scorecard.mjs --baseline-only</automated>
  </verify>
  <done>Phase 200 scorecard artifacts have a strict structural verifier ready for CI and final closeout.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Whole surface proven forward-only >= baseline with zero regressions | 200-03 | COVERED | Builds the structured scorecard machinery.
REQ | VER-01 | Empty regressions versus union baseline and every inherited cell >= baseline | 200-03 | COVERED | Generator and verifier enforce the rule; final generation occurs in Plan 200-06.
REQ | VER-02 | Guardrail evidence feeds final scorecard | 200-03 | COVERED | Consumes evidence generated by Plan 200-02.
RESEARCH | Union baseline and artifact verifier | 30,348 expected rows unless duplicate proof differs | 200-03 | COVERED | Enforces read-only archived baselines and Phase 200 output paths.
CONTEXT | D-13,D-14,D-15,D-16,D-17,D-25 | Union baseline, archive immutability, pending closure, artifact paths, hard regression signal, structured artifacts canonical | 200-03 | COVERED | Task actions cite each decision.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archived baselines -> Phase 200 artifacts | Frozen historical JSON is read and transformed into derived current artifacts. |
| Playwright/test output -> scorecard | Generated evidence is normalized into canonical scorecard rows. |
| artifact refs -> filesystem | Manifest and evidence refs point to repo files and generated test outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-09 | Tampering | archived baselines | mitigate | Treat archived baselines as read-only and write only Phase 200 derived outputs per D-14/D-16. |
| T-200-10 | Repudiation | regressions.ndjson | mitigate | Verifier fails full mode on any regression row; empty file is required for ACCEPT per D-17. |
| T-200-11 | Information Disclosure | artifact manifest refs | mitigate | Reject absolute paths, backslashes, `..`, and refs outside allowed generated roots. |
| T-200-12 | Tampering | page-flow pending closure | mitigate | Require `p193` rows to close as covered with score >= 2 and deterministic evidence per D-15. |
| T-200-SC | Tampering | package installs | mitigate | No package-manager install is planned; if execution adds npm, pip, or cargo installs, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
Run self-tests and baseline-only generation/verification. Full final scorecard generation is intentionally reserved for Plan 200-06 after guardrails and sign-off tooling exist.
</verification>

<success_criteria>
The Phase 200 union baseline exists, scorecard generation is fail-closed, and deterministic verification can prove or reject VER-01 using structured artifacts.
</success_criteria>

<output>
Create `.planning/phases/200-idempotent-verification-sign-off/200-03-SUMMARY.md` when done.
</output>

---
phase: 203-database-schema-contract-adr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
requirements_addressed:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
user_setup: []
must_haves:
  truths:
    - "ADR explicitly keeps `billing` as the default dedicated Postgres schema for this milestone."
    - "ADR preserves explicit `public` handling where required by current behavior."
    - "ADR documents why switching the default to `accrue` is out of scope for v1.55."
    - "ADR describes future schema-prefix hardening checks as Phase 204 follow-up implementation work."
    - "Phase 203 changes only the ADR/planning artifact and does not change defaults, migrations, installer behavior, source code, runtime schemas, public docs, CI, or package metadata."
  artifacts:
    - path: ".planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md"
      provides: "Accepted database schema contract ADR"
      contains: "Status: Accepted for v1.55"
    - path: ".planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md"
      provides: "Phase 204 DB schema-prefix hardening handoff"
      contains: "Phase 204 Handoff"
  key_links:
    - from: ".planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md"
      to: "accrue/lib/accrue/config.ex"
      via: "ADR cites `:billing_schema` default and validation evidence"
      pattern: "billing_schema.*billing"
    - from: ".planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md"
      to: "accrue/lib/accrue/schema.ex"
      via: "ADR cites compile-time Ecto `@schema_prefix` behavior"
      pattern: "@schema_prefix|Application\\.compile_env"
    - from: ".planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md"
      to: "accrue/lib/accrue/migration.ex"
      via: "ADR cites migration prefix helpers and raw SQL qualification"
      pattern: "qualified_table|prefix"
---

<objective>
Produce the database schema contract ADR.

Purpose: Lock Accrue's current Postgres schema-prefix support contract for v1.55 without changing runtime behavior or defaults.
Output: Updated `.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md` with a hybrid accepted ADR, explicit `billing`/`public`/`accrue` compatibility story, and structured Phase 204 hardening handoff.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/201-software-quality-evaluation/201-01-SUMMARY.md
@.planning/phases/202-ci-cd-performance-and-determinism-audit/202-01-SUMMARY.md
@.planning/phases/203-database-schema-contract-adr/203-CONTEXT.md
@.planning/phases/203-database-schema-contract-adr/203-RESEARCH.md
@.planning/phases/203-database-schema-contract-adr/203-PATTERNS.md
@.planning/phases/203-database-schema-contract-adr/203-VALIDATION.md
@.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md

# Evidence files to read, not modify in Phase 203
@accrue/lib/accrue/config.ex
@accrue/lib/accrue/schema.ex
@accrue/lib/accrue/migration.ex
@accrue/lib/accrue/install/options.ex
@accrue/lib/accrue/install/patches.ex
@accrue/lib/accrue/install/templates.ex
@accrue/lib/mix/tasks/accrue.install.ex
@accrue/test/mix/tasks/accrue_install_test.exs
@accrue/guides/configuration.md
@accrue/guides/first_hour.md
@accrue/guides/upgrade.md
@examples/accrue_host/config/config.exs
@brandbook/voice.md
</context>

## Artifacts This Phase Produces

- `.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md` updated as the canonical accepted ADR.
- ADR sections created or updated: Context, Current Contract, Authoritative Surfaces, Compatibility and Upgrade Warning, Why Not Rename Default to `accrue`, Consequences, Phase 204 Handoff, Non-Goals, Verification, Requirement Coverage, Phase Handoff and Boundary.
- No source, migration, installer, docs mirror, CI, package metadata, runtime schema, or database state artifact is produced by this phase.

## Source Coverage Audit

| Source | ID | Requirement / decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Produce `203-DB-SCHEMA-CONTRACT-ADR.md`; lock `billing`, preserve explicit `public`, reject default `accrue`, and define Phase 204 hardening checks without changing defaults | 203-01 | COVERED | Tasks 1-3 update and verify only the ADR. |
| REQ | DB-01 | Explain current Accrue-owned Postgres schema contract: default `billing`, explicit `public`, Ecto compile-time schema prefix, migration prefix helpers, host-owned data migration | 203-01 | COVERED | Task 1 and Task 3. |
| REQ | DB-02 | Explain why v1.55 keeps `billing` instead of switching to `accrue`, including pros/cons and upgrade risk | 203-01 | COVERED | Task 1 and Task 3. |
| REQ | DB-03 | List concrete future hardening checks for prefix agreement, raw SQL qualification, installer/docs/test coverage, and old-default compatibility | 203-01 | COVERED | Task 2. |
| REQ | DB-04 | Identify future implementation work and work not worth doing now | 203-01 | COVERED | Task 2 and Task 3. |
| RESEARCH | 203-RESEARCH | Hybrid accepted ADR; authoritative surfaces; compile-time config; no `search_path`; structured Phase 204 handoff; markdown smoke validation | 203-01 | COVERED | Tasks 1-3 follow the research recommendation and validation map. |
| CONTEXT | D-01, D-02, D-03, D-04 | Coherent decision set, one-shot recommendations, repo-local proof-checkable voice, DX lens only | 203-01 | COVERED | Task 1 action. |
| CONTEXT | D-05, D-06, D-07, D-08 | Hybrid accepted ADR; current `MUST` contract; advisory Phase 204 checks; sparing BCP-14 wording | 203-01 | COVERED | Tasks 1-2 actions. |
| CONTEXT | D-09, D-10, D-11, D-12 | Layered authoritative surfaces, named binding surfaces, compile-time config warning, no implementation hardening in Phase 203 | 203-01 | COVERED | Tasks 1-2 actions. |
| CONTEXT | D-13, D-14, D-15, D-16, D-17 | Detailed upgrade warning; pin existing installs; host-owned data movement; `public` supported; reject `accrue` default | 203-01 | COVERED | Task 3 action. |
| CONTEXT | D-18, D-19, D-20, D-21 | Structured Phase 204 handoff table with required rows and local-priority boundary | 203-01 | COVERED | Task 2 action. |
| CONTEXT | D-22, D-23, D-24, D-25 | Ecto/Phoenix/Oban ecosystem lessons and clear DX contract | 203-01 | COVERED | Task 1 action. |
| CONTEXT | Deferred ideas | No schema hardening implementation, no default rename, no data movement, no public relocation recipe, no product/source/CI/runtime changes | 203-01 | EXCLUDED | Deferred by user; Task 3 verifies boundary. |

## Dependency Graph

| Task | Needs | Creates | Wave |
|---|---|---|---|
| Task 1 | Current ADR, context, research, pattern map, evidence files | Normative current-contract ADR sections | 1 |
| Task 2 | Task 1 ADR structure, Phase 204 handoff decisions | Structured future hardening handoff table and non-goal framing | 1 |
| Task 3 | Tasks 1-2 ADR content and validation strategy | Requirement coverage, compatibility warning, boundary proof | 1 |

All tasks modify the same ADR file, so they execute in one plan and one wave. Schema push detection: schema-relevant files are evidence only; this phase modifies only `.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md`, so no schema push task belongs in this plan.

<tasks>

<task type="auto">
  <name>Task 1: Lock Current Schema Contract In The ADR</name>
  <files>.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md</files>
  <read_first>
    .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
    .planning/phases/203-database-schema-contract-adr/203-CONTEXT.md
    .planning/phases/203-database-schema-contract-adr/203-RESEARCH.md
    .planning/phases/203-database-schema-contract-adr/203-PATTERNS.md
    .planning/REQUIREMENTS.md
    accrue/lib/accrue/config.ex
    accrue/lib/accrue/schema.ex
    accrue/lib/accrue/migration.ex
    accrue/lib/accrue/install/options.ex
    accrue/lib/accrue/install/patches.ex
    accrue/lib/accrue/install/templates.ex
    accrue/lib/mix/tasks/accrue.install.ex
    accrue/test/mix/tasks/accrue_install_test.exs
    accrue/guides/configuration.md
    accrue/guides/first_hour.md
    accrue/guides/upgrade.md
    examples/accrue_host/config/config.exs
    brandbook/voice.md
  </read_first>
  <action>Update the ADR header, Context, Current Contract, Authoritative Surfaces, Why Not Rename Default to `accrue`, and Consequences sections to implement D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-09, D-10, D-11, D-22, D-23, D-24, and D-25 for DB-01 and DB-02. Target state: the ADR says Accrue MUST keep `billing` as the default Accrue-owned Postgres schema for v1.55 / v1.x, MAY support explicit `public` only when the host opts in, MUST NOT rename the default to `accrue`, and MUST NOT rely on Postgres `search_path` as the primary contract. Name the authoritative executable surfaces as `Accrue.Config`, `Accrue.Schema`, `Accrue.Migration`, and installer generation; name docs, example host config, and tests as mirror surfaces. State that `:billing_schema` is compile-time configuration for Ecto schemas and belongs in `config/config.exs`, not `config/runtime.exs`. Use measured Phoenix/Ecto-native wording and keep code/source evidence as citations, not edits.</action>
  <verify>
    <automated>bash -lc 'set -e
rg -n "billing|public|compile-time|Accrue\\.Schema|Accrue\\.Migration|host-owned" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "Accrue\\.Config|accrue/lib/accrue/config\\.ex|accrue/lib/accrue/schema\\.ex|accrue/lib/accrue/migration\\.ex|config/config\\.exs|config/runtime\\.exs" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "MUST.*billing|MAY.*public|MUST NOT.*accrue|MUST NOT.*search_path" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md'</automated>
  </verify>
  <acceptance_criteria>
    - ADR names `billing`, `public`, `accrue`, `search_path`, `Accrue.Config`, `Accrue.Schema`, `Accrue.Migration`, `config/config.exs`, and `config/runtime.exs`.
    - ADR distinguishes authoritative executable surfaces from mirror surfaces.
    - ADR satisfies DB-01 and DB-02 without editing any evidence file.
  </acceptance_criteria>
  <done>Current contract is normative, evidence-backed, and explicit about `billing`, `public`, `accrue`, compile-time schema config, migration helpers, and host-owned data migration.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Future Work Prose With Phase 204 Handoff</name>
  <files>.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md</files>
  <read_first>
    .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
    .planning/phases/203-database-schema-contract-adr/203-CONTEXT.md
    .planning/phases/203-database-schema-contract-adr/203-RESEARCH.md
    .planning/phases/203-database-schema-contract-adr/203-PATTERNS.md
    .planning/phases/203-database-schema-contract-adr/203-VALIDATION.md
    .planning/phases/201-software-quality-evaluation/201-01-SUMMARY.md
    .planning/phases/202-ci-cd-performance-and-determinism-audit/202-01-SUMMARY.md
  </read_first>
  <action>Replace the existing plain Future Hardening Work list with a structured Phase 204 handoff table/checklist implementing D-07, D-12, D-18, D-19, D-20, and D-21 for DB-03 and DB-04. The handoff must be advisory follow-up implementation work for Phase 204 to rank, not shipped behavior in Phase 203. Include columns for Area, Evidence path, Current risk, Expected impact, Tradeoff, Implementation approach, Verification, Rollback, Metric/evidence-needed status, and Non-goals. Include rows for centralizing the default `billing` schema constant, representative schema-prefix assertions, explicit `billing` / `public` compatibility lanes, a raw SQL qualification guard for Accrue-owned `accrue_*` tables outside approved helpers, continued installer `--billing-schema public` coverage, configuration / First Hour / Upgrade / example-host docs alignment, and preservation of the host-owned data migration boundary. Label any priorities as local DB-schema-contract inputs only; Phase 204 owns final cross-audit ordering after Phases 201, 202, and 203.</action>
  <verify>
    <automated>bash -lc 'set -e
rg -n "Phase 204 Handoff|Evidence path|Current risk|Expected impact|Tradeoff|Implementation approach|Verification|Rollback|Metric/evidence-needed|Non-goals" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "centraliz|prefix-agreement|schema-prefix|raw SQL|--billing-schema public|configuration|First Hour|Upgrade|example-host|host-owned data migration|qualified" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "Phase 204.*rank|local DB-schema-contract|not final cross-audit ordering" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md'</automated>
  </verify>
  <acceptance_criteria>
    - ADR contains a structured Phase 204 handoff, not only a narrative future-work list.
    - Handoff rows cover prefix agreement, raw SQL qualification, installer/docs/test coverage, explicit `billing`, explicit `public`, and old-default compatibility.
    - ADR clearly states Phase 203 does not implement those checks and Phase 204 ranks them.
  </acceptance_criteria>
  <done>Future schema-prefix hardening work is concrete, rankable, and safely handed to Phase 204 without implying implementation happened.</done>
</task>

<task type="auto">
  <name>Task 3: Close Compatibility, Non-Goals, And Verification</name>
  <files>.planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md</files>
  <read_first>
    .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
    .planning/phases/203-database-schema-contract-adr/203-CONTEXT.md
    .planning/phases/203-database-schema-contract-adr/203-RESEARCH.md
    .planning/phases/203-database-schema-contract-adr/203-PATTERNS.md
    .planning/phases/203-database-schema-contract-adr/203-VALIDATION.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    accrue/guides/configuration.md
    accrue/guides/upgrade.md
    accrue/test/mix/tasks/accrue_install_test.exs
    examples/accrue_host/config/config.exs
  </read_first>
  <action>Finalize the Compatibility and Upgrade Warning, Non-Goals, Verification, Requirement Coverage, and Phase Handoff and Boundary sections to implement D-13, D-14, D-15, D-16, and D-17 while closing DB-01, DB-02, DB-03, and DB-04. The compatibility warning must say existing installs pin their intended placement before recompiling with `config :accrue, :billing_schema, "public"` for current public-schema installs or `config :accrue, :billing_schema, "billing"` for billing-schema installs, while fresh installs keep `billing`. State that moving production data between `public`, `billing`, or any future schema is host-owned data migration work and is not automated by Accrue. Preserve explicit `public` as a supported opt-out, not a deprecated or inferior path. Add a Requirement Coverage table naming DB-01, DB-02, DB-03, and DB-04. Add phase boundary proof language: Phase 203 updates only `203-DB-SCHEMA-CONTRACT-ADR.md` and does not change current defaults, database schemas, migrations, installer behavior, source code, runtime behavior, public docs defaults, CI topology, package metadata, or product surface. State that no schema push task is required because schema-relevant files are evidence only.</action>
  <verify>
    <automated>bash -lc 'set -e
rg -n "DB-01|DB-02|DB-03|DB-04" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "config :accrue, :billing_schema, \"public\"|config :accrue, :billing_schema, \"billing\"|before recompiling|Fresh installs keep|host-owned" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "Future Hardening|Phase 204 Handoff|Non-Goals|not part|out of scope|implementation milestone|schema push" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
test -z "$(git diff --name-only -- accrue accrue_admin accrue_portal examples .github scripts)"'</automated>
  </verify>
  <acceptance_criteria>
    - ADR includes explicit DB-01, DB-02, DB-03, and DB-04 coverage.
    - ADR preserves `billing` as default, explicit `public` as supported opt-out, and rejects default `accrue` for v1.55.
    - ADR verifies the Phase 203 boundary and schema-push exclusion.
    - No source, migration, installer, docs mirror, CI, package metadata, runtime schema, or product file is modified.
  </acceptance_criteria>
  <done>ADR is phase-complete, requirement-traceable, and bounded to documentation/planning-only changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Maintainer/executor -> ADR artifact | Human and agent interpretation becomes a durable support-contract document. |
| ADR artifact -> future implementation planning | Phase 204 will rank DB hardening based on this document, so overstatement or missing evidence can misdirect future work. |
| ADR artifact -> adopters/support readers | Schema-placement wording can influence production upgrade behavior even though this phase does not change code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-203-01 | Tampering | `203-DB-SCHEMA-CONTRACT-ADR.md` current contract | mitigate | Require ADR evidence for `billing`, explicit `public`, compile-time `Accrue.Schema`, `Accrue.Migration`, and `search_path` non-reliance; verify with markdown smoke checks. |
| T-203-02 | Repudiation | Compatibility and upgrade warning | mitigate | Require explicit pin-before-recompile language for `public` and `billing`, plus host-owned data migration boundary. |
| T-203-03 | Information Disclosure | Evidence citations | accept | ADR cites local file paths and mechanisms only; it does not add secrets, database data, credentials, or runtime logs. |
| T-203-04 | Denial of Service | Future implementation handoff | mitigate | Label Phase 204 hardening rows as advisory follow-up work so the ADR does not imply unimplemented guards or a default rename. |
| T-203-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this documentation-only plan. |
</threat_model>

<verification>
Overall phase checks:

<automated>bash -lc 'set -e
test -f .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "billing|public|compile-time|Accrue\\.Schema|Accrue\\.Migration|host-owned" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "accrue\\.accrue_|Why Not|out of scope|upgrade risk|rename" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "Phase 204|prefix-agreement|raw SQL|installer|docs|test|compatibility|qualified" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
rg -n "Future Hardening|Non-Goals|not part|out of scope|implementation milestone" .planning/phases/203-database-schema-contract-adr/203-DB-SCHEMA-CONTRACT-ADR.md
test -z "$(git diff --name-only -- accrue accrue_admin accrue_portal examples .github scripts)"'</automated>

If any code or public docs under `accrue/`, `examples/`, `.github/`, or `scripts/` are changed despite this plan boundary, stop and either revert the plan-owned out-of-scope edits or run the relevant code/doc tests before proceeding. The expected Phase 203 path is markdown-only.
</verification>

<success_criteria>
- `203-DB-SCHEMA-CONTRACT-ADR.md` is accepted and requirement-traceable for DB-01, DB-02, DB-03, and DB-04.
- ADR explicitly keeps `billing` as the default dedicated Postgres schema for this milestone.
- ADR preserves explicit `public` handling as a supported opt-out.
- ADR explains why default `accrue` is out of scope for v1.55.
- ADR includes structured Phase 204 follow-up hardening checks.
- No defaults, database schemas, runtime migrations, installer behavior, source code, public docs defaults, CI topology, package metadata, or product behavior changed.
</success_criteria>

<output>
Create `.planning/phases/203-database-schema-contract-adr/203-01-SUMMARY.md` when done.
</output>

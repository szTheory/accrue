---
phase: 205-persona-design-lens-evaluator-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/e2e/ratchet/DESIGN-LENS-RUBRIC.md
  - accrue_admin/e2e/ratchet/exemplars/PROVENANCE.json
  - accrue_admin/e2e/ratchet/exemplars/good/dashboard.png
  - accrue_admin/e2e/ratchet/exemplars/good/dev-components.png
  - accrue_admin/e2e/ratchet/exemplars/bad/cramped.png
  - accrue_admin/e2e/ratchet/exemplars/bad/wasteful.png
  - accrue_admin/e2e/ratchet/exemplars/bad/off-register.png
autonomous: true
requirements: [EVAL-02, EVAL-04]
must_haves:
  truths:
    - "A committed DESIGN-LENS-RUBRIC.md anchored to brandbook/ sharpens dims 2/3/5/8 with 1/6 support, comparative (not an absolute award score), penalizing BOTH cramped and wasteful density"
    - "A curated 5-image own-render exemplar set (2 good, 3 bad covering both density poles) is committed, each ≤~1600px wide and encoding under the 5 MB base64 guard"
    - "PROVENANCE.json records one auditable entry per PNG with source_commit_sha, capture route, viewport+theme, capture command, max-dimensions, and curator note"
  artifacts:
    - accrue_admin/e2e/ratchet/DESIGN-LENS-RUBRIC.md
    - accrue_admin/e2e/ratchet/exemplars/PROVENANCE.json
    - accrue_admin/e2e/ratchet/exemplars/good/*.png (2)
    - accrue_admin/e2e/ratchet/exemplars/bad/*.png (3)
  key_links:
    - "The design lens (plan 04) attaches exactly one archetype-matched good + one bad exemplar per call; this set is the calibration anchor"
    - "brandbook/ supersedes prompts/accrue-brand-book.md wherever they conflict (D-22)"
---

<objective>
Produce the design-lens calibration assets: a committed `DESIGN-LENS-RUBRIC.md` sub-rubric anchored to the current `brandbook/`, and a curated, license-clean good/bad exemplar set (5 own-render PNGs) with a version-pinned `PROVENANCE.json`. These anchor the comparative graphic-design lens to Accrue's locked brand DNA ("quiet polish, well-made dev tooling, not fintech") and directly suppress the density footgun.

Purpose: Without a correctly-dense GOOD Accrue table and BOTH density poles represented in the bad set, the design lens biases toward over-whitespacing a dense operator console (the milestone's biggest brand risk).
Output: A committed rubric + 5 curated exemplars + provenance manifest, consumed by plan 04's design-lens call.

**Artifacts THIS plan produces:** `DESIGN-LENS-RUBRIC.md`, `exemplars/good/{dashboard,dev-components}.png`, `exemplars/bad/{cramped,wasteful,off-register}.png`, `exemplars/PROVENANCE.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-persona-design-lens-evaluator-harness/205-CONTEXT.md
@.planning/phases/205-persona-design-lens-evaluator-harness/205-RESEARCH.md
@.planning/phases/205-persona-design-lens-evaluator-harness/205-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture + curate the 5 own-render exemplar PNGs</name>
  <files>accrue_admin/e2e/ratchet/exemplars/good/dashboard.png, accrue_admin/e2e/ratchet/exemplars/good/dev-components.png, accrue_admin/e2e/ratchet/exemplars/bad/cramped.png, accrue_admin/e2e/ratchet/exemplars/bad/wasteful.png, accrue_admin/e2e/ratchet/exemplars/bad/off-register.png</files>
  <read_first>
    - accrue_admin/e2e/admin-visuals.spec.js:1-40 (reset/seed/login helpers + captureThemes shape — the capture route to reuse)
    - accrue_admin/e2e/admin-visuals.spec.js:40-80 (surface sweep — how dashboard + /dev/components are captured)
  </read_first>
  <action>
Capture and curate 5 license-clean own-render admin screenshots (own screenshots of an MIT repo are license-clean by construction, D-19). CRITICAL runbook step (RESEARCH Pitfall 5): run `mix accrue_admin.assets.build` and ensure `priv/static/accrue_admin.css` is fresh/committed BEFORE any capture, so PNGs reflect committed CSS, not a stale bundle.

GOOD set (2) — capture from the current admin using the existing playwright capture route (`npm --prefix accrue_admin run e2e:visuals:png-only` or a targeted capture):
- `good/dashboard.png` — the dashboard (data-dense-operator archetype). This correctly-dense Accrue table/KPI surface is the most direct suppressor of the density footgun.
- `good/dev-components.png` — the `/billing/dev/components` kitchen (foundation archetype).

BAD set (3) — cover BOTH density poles (a single bad teaches only one pole and biases the model):
- `bad/cramped.png` — a rough, too-tight render. Preferred: re-capture a v1.50-era render by checking out an old SHA (`baf593f3`) and re-booting the old admin (D-18: the pixels must be regenerated; the capture *code* has history but no PNG was ever committed). If an old-SHA boot is impractical in this working tree, synthesize the cramped pole on the current admin (tighten spacing/padding tokens → shoot → revert) and record that method in PROVENANCE; either way the result must read as under-spaced/cramped.
- `bad/wasteful.png` — synthetically over-whitespaced dashboard: inflate padding/spacing → shoot → REVERT. Store the generating diff reference in PROVENANCE (D-19).
- `bad/off-register.png` — an off-register / fintech-glossy negative. Per D-19/D-21 this MAY be textual-only if no clean own-render exists; if you cannot produce a license-clean own render, OMIT the PNG and document the off-register pole as a TEXTUAL anchor in the rubric + a PROVENANCE note (do NOT commit a third-party screenshot).

Downscale every committed PNG to ≤~1600px wide as a ONE-TIME curation step using an existing tool (macOS `sips -Z 1600 <file>`, or ImageMagick, or a fixed-width playwright capture) — do NOT add `sharp` or any npm dependency (the harness only reads committed bytes; resizing is not a runtime concern). Verify each downscaled PNG encodes to under the 5 MB base64 guard.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='accrue_admin/e2e/ratchet/exemplars';const req=['good/dashboard.png','good/dev-components.png','bad/cramped.png','bad/wasteful.png'];for(const f of req){const b=fs.readFileSync(p+'/'+f);const b64=b.toString('base64').length;if(b64>5*1024*1024)throw new Error('over 5MB base64: '+f);}console.log('exemplar bytes ok')"</automated>
  </verify>
  <acceptance_criteria>
    - At least the 4 mandatory PNGs exist (`good/dashboard`, `good/dev-components`, `bad/cramped`, `bad/wasteful`); `bad/off-register.png` exists OR is documented as a textual-only anchor
    - Each committed PNG is ≤~1600px wide (`sips -g pixelWidth <file>`) and under the 5 MB base64 guard
    - `priv/static/accrue_admin.css` was rebuilt before capture (recorded in PROVENANCE)
    - No new npm dependency was added to `accrue_admin/package.json`
  </acceptance_criteria>
  <done>A curated 5-image (or 4-image + textual off-register) own-render exemplar set covering both density poles is committed, each under the size guard.</done>
</task>

<task type="auto">
  <name>Task 2: DESIGN-LENS-RUBRIC.md + exemplars/PROVENANCE.json</name>
  <files>accrue_admin/e2e/ratchet/DESIGN-LENS-RUBRIC.md, accrue_admin/e2e/ratchet/exemplars/PROVENANCE.json</files>
  <read_first>
    - .planning/milestones/v1.53-phases/187-audit-baseline/187-RUBRIC.md:1-40 (structured-data-wins + brandbook precedence note + dimension table — reuse tone)
    - brandbook/voice.md (brand DNA; bans wallet/money/funds — the anti-fintech anchor)
    - brandbook/README.md (brand DNA anchor)
    - .planning/milestones/v1.53-phases/187-audit-baseline/schemas/baseline-cell.schema.json (closed-enum / additionalProperties discipline for the PROVENANCE shape)
  </read_first>
  <action>
Write `DESIGN-LENS-RUBRIC.md` following the D-22 section outline: Purpose & scope · Brand DNA anchor (brandbook/) · Comparative tier anchors (textual) · Sharpened sub-criteria · Exemplar set reference · Output contract · Justification-token vocab · Anti-patterns/footgun table.

Constraints (binding):
- Anchor to CURRENT `brandbook/`, and include the verbatim-tone precedence note: "brandbook/ supersedes prompts/accrue-brand-book.md wherever they conflict; the older prompt remains historical context only" (D-22, from 187-RUBRIC).
- Comparative, NOT an absolute "award" score. Sharpen dims 2 (hierarchy), 3 (spacing-rhythm), 5 (responsive), 8 (brand-expression), with 1 (token) and 6 (contrast) as support. NO 13th rubric dimension (would fork the DIMENSIONS map in 4 files).
- d3 (spacing-rhythm) must penalize BOTH cramped AND wasteful density.
- Textual tier anchors (license-safe, more stable than one overfit screenshot): primary brand-positive = Linear, Vercel, Prisma, Tailscale, Oban. DROP Stripe as a brand-positive exemplar; cite Stripe ONLY as a density/IA reference under an explicit anti-fintech caveat ("borrow its operator density, never its brand, color, or voice") — D-21.
- Output contract section: defect-only; dims {2,3,5,8,(1,6)}; each design candidate carries a `direction: air|cramped` self-flag; justification token vocab = `rubric-dim-below-bar | persona-job-miss:<job> | token-bypass`.
- Document how "archetype-matched" exemplar selection is keyed (RESEARCH Open Q3 recommendation: key off `surface_type` — list/detail/component — for a small static good/bad selection).

Write `exemplars/PROVENANCE.json` (D-23) — one entry per committed PNG with: `source_commit_sha`, capture route/spec, `viewport`, `theme`, exact capture command, max-dimensions (pixel width/height post-downscale), and a curator note (`role: good|bad`, density pole, why it was chosen). Include a top-level note that the set is version-pinned and refreshed ONLY via a deliberate "re-baseline exemplars" maintainer commit (PNGs + PROVENANCE together) — never auto-drift from latest renders. Use closed enums / no extra keys (mirror the schema discipline of `baseline-cell.schema.json`). If `off-register` shipped as textual-only, record that disposition instead of a PNG entry.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('accrue_admin/e2e/ratchet/exemplars/PROVENANCE.json','utf8'));const r=fs.readFileSync('accrue_admin/e2e/ratchet/DESIGN-LENS-RUBRIC.md','utf8');if(!/brandbook\//.test(r))throw new Error('rubric missing brandbook anchor');if(/award/i.test(r)&&!/not an? absolute .?award|rather than.*award|no absolute/i.test(r))console.warn('check award wording');if(!/Linear/.test(r)||!/Oban/.test(r))throw new Error('missing tier anchors');console.log('rubric+provenance ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `DESIGN-LENS-RUBRIC.md` anchors to `brandbook/`, includes the precedence note, sharpens ONLY dims 2/3/5/8 (+1/6 support), adds no 13th dimension, and is comparative not absolute
    - d3 explicitly penalizes both cramped and wasteful; Stripe appears only under an anti-fintech caveat; Linear/Vercel/Prisma/Tailscale/Oban are the brand-positive anchors
    - `PROVENANCE.json` parses and has one auditable entry per committed PNG with all D-23 fields
    - The `direction: air|cramped` self-flag and justification-token vocab are documented in the output contract
  </acceptance_criteria>
  <done>The committed rubric + provenance manifest anchor the design lens to brand DNA and document the exemplar contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exemplar bytes → model context | Committed exemplar PNGs are attached to the design-lens call; must be own-render (license-clean) and size-bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-03 | Denial of Service | exemplar image size | medium | mitigate | Every committed exemplar is downscaled ≤~1600px and verified under the 5 MB base64 guard at curation time. |
| T-205-04 | Info Disclosure / licensing | exemplar provenance | low | mitigate | Own-render-only (MIT repo) exemplars; third-party dashboards stay TEXTUAL in the rubric; provenance recorded per PNG. |
</threat_model>

<verification>
- Exemplar PNGs exist, are ≤~1600px, and encode under 5 MB base64.
- `DESIGN-LENS-RUBRIC.md` and `PROVENANCE.json` pass the content/parse checks.
- No new npm dependency added.
</verification>

<success_criteria>
- EVAL-02: comparative design lens is anchored to named quiet-dev-tooling tier exemplars (textual) rather than an absolute award score.
- EVAL-04: committed, license-clean good/bad exemplar set + committed sub-rubric anchor the lens to brand DNA.
</success_criteria>

<output>
Create `.planning/phases/205-persona-design-lens-evaluator-harness/205-02-SUMMARY.md` when done.
</output>

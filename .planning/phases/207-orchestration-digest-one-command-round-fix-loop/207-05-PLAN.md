---
phase: 207-orchestration-digest-one-command-round-fix-loop
plan: 05
type: execute
wave: 3
depends_on: ["207-01", "207-02", "207-04"]
files_modified:
  - accrue_admin/lib/mix/tasks/accrue_admin.ui.round.ex
  - accrue_admin/test/mix/tasks/accrue_admin_ui_round_test.exs
  - accrue_admin/package.json
autonomous: true
requirements: [ORCH-01]

must_haves:
  truths:
    - "mix accrue_admin.ui.round with no flags sequences: next-round -> assets.build -> capture -> propose -> verify -> seal-round -> digest, in that exact order, via the swappable Runner behaviour"
    - "mix accrue_admin.ui.round --slice foundation resolves to RATCHET_SURFACES=component-kitchen,dashboard,subscription-detail,subscriptions and threads it to the capture/propose/seal-round steps only"
    - "when the round-status marker reads cap-reached, the task raises (non-zero exit) only AFTER the digest step has already run — a maintainer always gets a rendered digest even on cap-reached"
  artifacts:
    - accrue_admin/lib/mix/tasks/accrue_admin.ui.round.ex
    - accrue_admin/test/mix/tasks/accrue_admin_ui_round_test.exs
  key_links:
    - "reads the .round-next / .round-status marker files 207-01's phase-ratchet-ledger.mjs writes — never re-derives the round number or convergence status itself"
    - "the --slice name -> surface CSV mapping is READ DIRECTLY from baseline-manifest.js's SLICES (207-02) via a captured node -e call — Elixir never hardcodes the slice contents, so there is no duplicated closed-constant to drift"
---

<objective>
Implement `mix accrue_admin.ui.round` — the ORCH-01 one-command measurement pipeline — as a thin Elixir orchestrator twinning `accrue_admin.assets.build.ex`'s exact `Runner`/`ShellRunner`/`Application.get_env` idiom (D-51: Elixir sequences `System.cmd` calls and reimplements zero logic). It resolves `--slice`/`--surface` into a shared `RATCHET_SURFACES` env var (D-52), reads the round number and final convergence status from the two marker files 207-01's `phase-ratchet-ledger.mjs` writes, always renders the digest before deciding whether to raise, and raises only on the 6-round cap-reached escalation (D-49c).

Purpose: this is the maintainer's entire "run a round" interaction — one command, no manual step-ordering, no hand-pruned PNGs.
Output: a working `mix accrue_admin.ui.round [--slice NAME | --surface a,b,c]` command plus a FakeRunner ExUnit test proving the exact step sequence and both marker-driven branches (continue/cap-reached).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-CONTEXT.md
@.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-RESEARCH.md
@accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
@accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `accrue_admin.ui.round.ex` mix task</name>
  <files>accrue_admin/lib/mix/tasks/accrue_admin.ui.round.ex, accrue_admin/package.json</files>
  <read_first>
    accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex (the EXACT `Runner`/`ShellRunner`/`run_step!`/`Application.get_env` idiom to twin verbatim)
  </read_first>
  <behavior>
    - No flags: env passed to `capture`/`propose`/`seal-round` carries no `RATCHET_SURFACES` key at all (unscoped = full configured set).
    - `--slice foundation` (or bare `--slice`, defaulting to `"foundation"`): env carries `RATCHET_SURFACES=component-kitchen,dashboard,subscription-detail,subscriptions`.
    - `--surface dashboard,subscriptions`: env carries `RATCHET_SURFACES=dashboard,subscriptions` verbatim (no slice-map lookup).
    - `--slice` and `--surface` together: `Mix.raise` with a clear mutual-exclusivity message, before any subprocess runs.
    - An unknown `--slice NAME`: `Mix.raise` naming the known slice keys, before any subprocess runs.
  </behavior>
  <action>
    Create `Mix.Tasks.AccrueAdmin.Ui.Round` twinning `accrue_admin.assets.build.ex`'s `Runner` (`@callback run/3`) + `ShellRunner` (`System.cmd/3` with `stderr_to_stdout: true, into: IO.stream(:stdio, :line)`) + `Application.get_env(:accrue_admin, :accrue_admin_ui_round_runner, ShellRunner)` idiom exactly, with a private `run_step!/5` identical to the reused pattern (`Mix.raise` on non-zero/error).

    Parse `argv` via `OptionParser.parse(argv, strict: [slice: :string, surface: :string], aliases: [])`. Resolve the slice-name → surface-CSV mapping by reading the JS `SLICES` map DIRECTLY (no hand-mirrored Elixir copy — avoids the duplicated-closed-constant drift the checker flagged): a private `load_slices!(root, runner)` calls `run_step!`-style but CAPTURING stdout (use a dedicated `Runner.run` variant or `System.cmd/3` with a captured `into:` accumulator — the FakeRunner in Task 2 returns a canned JSON string for this call) of `node -e "console.log(JSON.stringify(require('./e2e/baseline-manifest.js').SLICES))"` (cd: root), then `Jason.decode!/1` the result into a `%{slice_name => [surface, ...]}` map. `baseline-manifest.js`'s `SLICES` (207-02) is the SINGLE source of truth — Elixir never hardcodes the slice contents. Resolve `--slice`/`--surface` into `surfaces_env` (`[]` when unscoped, or `[{"RATCHET_SURFACES", csv}]`) per the exact rules in `<behavior>`, where `--slice NAME` (bare `--slice` → `"foundation"`) looks up `NAME` in the JS-loaded map and joins its surface list with commas — `Mix.raise` on mutual exclusivity or an unknown slice name (naming the JS-loaded known keys) BEFORE any pipeline `run_step!` call.

    Sequence (all via `run_step!`, `cd: root` where `root = File.cwd!()`):
    1. `"next-round"`: `run_step!(runner, "next-round", "node", ["e2e/ratchet/phase-ratchet-ledger.mjs", "--next-round"], cd: root)`. Then read `round = root |> Path.join("test-results/ui-ratchet/.round-next") |> File.read!() |> String.trim() |> String.to_integer()`.
    2. `"assets.build"`: `run_step!(runner, "assets.build", "mix", ["accrue_admin.assets.build"], cd: root)`.
    3. `"capture"`: `run_step!(runner, "capture", "npx", ["playwright", "test", "e2e/admin-visuals.spec.js"], cd: root, env: surfaces_env)`.
    4. `"propose"`: `run_step!(runner, "propose", "node", ["e2e/ratchet/ratchet-propose.mjs"], cd: root, env: [{"RATCHET_ROUND", Integer.to_string(round)} | surfaces_env])`.
    5. `"verify"`: `run_step!(runner, "verify", "node", ["e2e/ratchet/ratchet-verify.mjs"], cd: root, env: [{"RATCHET_ROUND", Integer.to_string(round)}])`.
    6. `"seal-round"`: `run_step!(runner, "seal-round", "node", ["e2e/ratchet/phase-ratchet-ledger.mjs", "--seal-round"], cd: root, env: [{"RATCHET_ROUND", Integer.to_string(round)} | surfaces_env])`.
    7. `"digest"`: `run_step!(runner, "digest", "node", ["e2e/ratchet/ratchet-digest.mjs"], cd: root)` — ALWAYS runs, unconditionally, regardless of the status marker's eventual value.

    After step 7, read `status = root |> Path.join("test-results/ui-ratchet/.round-status") |> File.read!() |> String.trim()`, print `Mix.shell().info("Round #{round} complete — status=#{status}. Open test-results/ui-ratchet/round-#{String.pad_leading(Integer.to_string(round), 2, "0")}/digest.html")`, then `case status do "cap-reached" -> Mix.raise("UI ratchet round #{round}: 6-round cap reached without convergence — see the digest for the next action."); _ -> :ok end`.

    Add `"ui:round": "cd .. && mix accrue_admin.ui.round"` to `accrue_admin/package.json`'s `scripts` block (a convenience alias; the mix task itself is the canonical entry point).
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The slice map is loaded from `baseline-manifest.js`'s `SLICES` via the captured `node -e` call — there is NO hardcoded slice-contents literal in the Elixir source (grep the `.ex` file for `"component-kitchen"` returns nothing; the surface names live only in the JS SSOT).
    - `mix accrue_admin.ui.round --slice foundation --surface x` raises immediately (mutual exclusivity) with zero PIPELINE subprocess calls.
    - `mix accrue_admin.ui.round --slice not-a-real-slice` raises immediately naming the JS-loaded known slice keys, with zero pipeline subprocess calls.
    - The task compiles cleanly with `--warnings-as-errors`.
  </acceptance_criteria>
  <done>The mix task exists, resolves `--slice`/`--surface` exactly per D-52 by reading the JS `SLICES` SSOT (no drift-prone Elixir copy), sequences all 7 pipeline steps through the swappable Runner, and raises on cap-reached only after the digest has rendered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FakeRunner ExUnit test — sequence + marker-driven branches</name>
  <files>accrue_admin/test/mix/tasks/accrue_admin_ui_round_test.exs</files>
  <read_first>
    accrue_admin/test/mix/tasks/accrue_admin_assets_build_test.exs (the EXACT `FakeRunner`/`setup`/`on_exit`/`assert_received` pattern to twin)
  </read_first>
  <behavior>
    - The 7-step sequence fires in the documented order with the documented command/args, asserted via `assert_received {:runner_call, cmd, args, cd}` in strict order.
    - `--slice foundation` threads `RATCHET_SURFACES=component-kitchen,dashboard,subscription-detail,subscriptions` into the `capture`/`propose`/`seal-round` steps' env, and NOT into `assets.build`/`verify`/`digest`.
    - A pre-seeded `.round-next` marker of `3` makes the task use `RATCHET_ROUND=3` for `propose`/`verify`/`seal-round`.
    - A pre-seeded `.round-status` marker of `cap-reached` makes the task run the digest step THEN raise `Mix.Error`; a marker of `continue` or `converged` makes it complete normally (no raise).
  </behavior>
  <action>
    Create `Mix.Tasks.AccrueAdmin.Ui.RoundTest` twinning `accrue_admin_assets_build_test.exs`'s exact shape: a `FakeRunner` implementing `@behaviour Round.Runner` that `send(self(), {:runner_call, command, args, opts[:env]})` and returns `{:ok, 0}` for every call WITHOUT actually invoking `node`/`npx`/`mix` (so the test never needs a live server, PNGs, or an API key). The `load_slices!` `node -e` capture is the ONE call whose stdout the task consumes: the FakeRunner must recognize the `node -e "...SLICES..."` invocation (match on the args containing `"baseline-manifest.js"` + `"SLICES"`) and return canned JSON `{"foundation":["component-kitchen","dashboard","subscription-detail","subscriptions"]}` as its captured stdout, so the test exercises the real JS-read resolution path without spawning node. `setup`/`on_exit` swaps `Application.put_env(:accrue_admin, :accrue_admin_ui_round_runner, FakeRunner)` and restores the prior value; before each test, pre-seed the marker files the FakeRunner would NOT actually produce (`test-results/ui-ratchet/.round-next` and `.round-status`) with fixture content via `File.write!`, and clean them up in `on_exit`.

    Write tests for: (a) the full 7-step order and exact command/args per step (the captured `load_slices!` node -e call may precede them); (b) `--slice foundation` resolves via the canned `SLICES` JSON and threads `RATCHET_SURFACES=component-kitchen,dashboard,subscription-detail,subscriptions` into the `capture`/`propose`/`seal-round` steps only; (c) the pre-seeded round-3 marker driving `RATCHET_ROUND=3`; (d) `cap-reached` → digest step still fires (assert its `:runner_call` was received) AND `assert_raise Mix.Error, fn -> Round.run([]) end`; (e) `continue`/`converged` → no raise.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/mix/tasks/accrue_admin_ui_round_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All 5 scenarios in `<behavior>` pass.
    - No test in this file invokes a real subprocess, a live Phoenix server, or the network.
  </acceptance_criteria>
  <done>The exact 7-step sequence, the slice/surface env threading, and both marker-driven branches (cap-reached raises after digest; continue/converged does not) are proven without any live subprocess.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--slice`/`--surface` CLI input → subprocess env | Maintainer-supplied CLI flags become an env var passed to `System.cmd` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-05 | Tampering | `--surface` CSV → `RATCHET_SURFACES` env | low | mitigate | Passed via `System.cmd`'s `env:` keyword list (never interpolated into a shell string); an unrecognized surface name simply matches nothing downstream (207-02's filter), never expands scope or executes anything |
| T-207-06 | Repudiation | Marker-file trust | low | accept | `.round-next`/`.round-status` are ephemeral, gitignored, single-scalar files written by the SAME pipeline run that reads them seconds later — not a durable trust boundary; the durable record is the committed `rounds.ndjson` (207-01) |
| T-207-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan |

</threat_model>

<verification>
`mix compile --warnings-as-errors` is clean; `mix test test/mix/tasks/accrue_admin_ui_round_test.exs` is green covering the full step sequence, slice/surface env threading, and both marker-driven exit branches.
</verification>

<success_criteria>
`mix accrue_admin.ui.round [--slice NAME | --surface a,b,c]` runs the entire ORCH-01 pipeline in one command, thin and Runner-swappable, always rendering the digest before deciding whether to escalate.
</success_criteria>

<output>
Create `.planning/phases/207-orchestration-digest-one-command-round-fix-loop/207-05-SUMMARY.md` when done
</output>

---
phase: 208-prove-convergence-on-the-representative-slice-wire-ci-accept
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_ui_ratchet_signoff.mjs
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-05
  - CONV-06
  - CONV-07
must_haves:
  truths:
    - "CONV-06/D-67/D-68: UI-RATCHET-SIGN-OFF.md is enforced by a deterministic Phase-200-style verifier."
    - "CONV-07/D-69/D-70: the follow-on runbook is executable, bounded, and uses exact status/copy vocabulary."
    - "CONV-01/CONV-02/D-58/D-61: ACCEPT fails unless convergence, empty regressions, and frozen material baseline evidence are present."
  artifacts:
    - "scripts/ci/verify_ui_ratchet_signoff.mjs: sign-off verifier with fixture self-tests."
  key_links:
    - "verify_ui_ratchet_signoff.mjs -> UI-RATCHET-SIGN-OFF.md via --signoff/DEFAULT_SIGNOFF_PATH."
    - "verify_ui_ratchet_signoff.mjs -> verify_ratchet_ledger.mjs output by requiring frozen evidence refs and statuses."
---

<objective>
Create the maintainer sign-off verifier foundation before the sign-off artifact exists.

Purpose: enforce the UI-SPEC, D-67 structured evidence discipline, D-68 Phase-200-style verification, D-69 bounded runbook procedure, and D-70 exact operational copy without relying on reviewer memory.

Output: `scripts/ci/verify_ui_ratchet_signoff.mjs` with `--self-test`, `--require-accept`, strict section/status/final-line validation, evidence-reference validation, and acceptance red paths.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-CONTEXT.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-UI-SPEC.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-RESEARCH.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-PATTERNS.md
@scripts/ci/verify_phase200_signoff.mjs
@scripts/ci/verify_ratchet_ledger.mjs
</context>

## Plan-Specific Artifacts

- New path: `scripts/ci/verify_ui_ratchet_signoff.mjs`.
- New verifier exports in `scripts/ci/verify_ui_ratchet_signoff.mjs`: `verifyUiRatchetSignoff`, structure/status/runbook validators, evidence-reference validation, and ACCEPT evidence checks.
- New CLI modes: `--self-test`, `--signoff`, `--phase-dir`, and `--require-accept`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ratchet sign-off verifier skeleton</name>
  <files>scripts/ci/verify_ui_ratchet_signoff.mjs</files>
  <read_first>
    - scripts/ci/verify_ui_ratchet_signoff.mjs (new/optional before this task; read only if a prior partial file exists)
    - scripts/ci/verify_phase200_signoff.mjs
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-UI-SPEC.md
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-PATTERNS.md
  </read_first>
  <behavior>
    - A valid fixture with all required sections, allowed statuses, required runbook headings, and exact final ACCEPT line passes.
    - Missing or duplicate final decision lines fail.
    - Any status outside `PASS`, `BLOCKED`, `PENDING`, or `N/A` fails.
  </behavior>
  <action>Create `scripts/ci/verify_ui_ratchet_signoff.mjs` by mirroring the structure of `verify_phase200_signoff.mjs`, but make the Phase 208 UI-SPEC the authority. Define `REQUIRED_SECTIONS`, `REQUIRED_RUNBOOK_HEADINGS`, `VALID_STATUS_VALUES`, `DEFAULT_SIGNOFF_PATH`, `parseDecisionLine`, `validateStructure`, `validateStatusValues`, `validateRunbook`, `verifyUiRatchetSignoff(options = {})`, `runSelfTest`, and CLI args `--self-test`, `--signoff`, `--phase-dir`, and `--require-accept`. The exact ACCEPT prefix must be `Final maintainer decision: ACCEPT (maintainer approved {YYYY-MM-DD}). Evidence source:` and it must require exactly one final decision line.</action>
  <verify>
    <automated>node scripts/ci/verify_ui_ratchet_signoff.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `--self-test` proves valid ACCEPT, missing final line, duplicate final line, invalid status token, missing required section, and missing runbook heading.
    - The verifier exports `verifyUiRatchetSignoff` for direct fixture use and exits non-zero for failed CLI verification.
    - Artifact references reject absolute paths, backslashes, `..`, and refs outside `accrue_admin/e2e/ratchet/`, `accrue_admin/test-results/`, `.github/workflows/ci.yml`, `scripts/ci/`, and the Phase 208 directory.
  </acceptance_criteria>
  <done>The sign-off verifier exists and enforces the UI-SPEC structure and final-line contract per D-67, D-68, and D-70.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce ACCEPT evidence and runbook red paths</name>
  <files>scripts/ci/verify_ui_ratchet_signoff.mjs</files>
  <read_first>
    - scripts/ci/verify_ui_ratchet_signoff.mjs
    - scripts/ci/verify_ratchet_ledger.mjs
    - accrue_admin/e2e/ratchet/ledger.baseline.json
    - accrue_admin/e2e/ratchet/finding-regressions.ndjson
    - .planning/milestones/v1.54-phases/200-idempotent-verification-sign-off/regressions.ndjson
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-VALIDATION.md
  </read_first>
  <behavior>
    - `--require-accept` fails if final ACCEPT is absent.
    - ACCEPT fails if regression files are non-empty, baseline is unfrozen/materiality-invalid, synthetic proofs are missing, existing-gate evidence is missing, or the runbook implies an immediate full-admin sweep.
    - Error messages name the artifact or command to inspect.
  </behavior>
  <action>Extend `verifyUiRatchetSignoff` so ACCEPT validates D-58/D-61/D-66/D-68/D-69 evidence rows, not only Markdown shape. Require table rows or equivalent text for the UI-SPEC CI checks: no-key proof, 0-byte finding regressions, independent recompute match, synthetic count-increase proof, persona regression proof, existing UI gates, and bundle freshness. For final ACCEPT, reject existing UI gate or bundle freshness rows whose Status is `PENDING`, `N/A`, or `BLOCKED`; those statuses are allowed only before the final ACCEPT line is present. Reject broad sweep wording that makes Phase 208 perform SWEEP-01 instead of teeing up bounded surface/slice graduation. Keep the verifier deterministic and SDK-free.</action>
  <verify>
    <automated>node scripts/ci/verify_ui_ratchet_signoff.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `--self-test` includes failing fixtures for non-empty regression file evidence, unfrozen/all-zero empty-file baseline evidence, missing synthetic failure proof rows, missing existing UI gate evidence, existing UI gate rows marked `PENDING`/`N/A`/`BLOCKED` under final ACCEPT, invalid artifact refs, and broad sweep mandate language.
    - `--require-accept` with no sign-off file exits non-zero and reports the missing path.
    - No fixture writes outside its `fs.mkdtempSync` root.
  </acceptance_criteria>
  <done>The sign-off verifier enforces the complete D-67/D-68/D-69 acceptance contract before Plan 05 creates the real sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown sign-off -> verifier | The sign-off document is untrusted user-editable text. |
| Evidence refs -> filesystem | Markdown refs can attempt path traversal or point at stale/nonexistent files. |
| Human ACCEPT -> CI | A malformed decision line could claim approval without evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-208-02-01 | Repudiation | `UI-RATCHET-SIGN-OFF.md` | mitigate | Require exactly one final ACCEPT line and reject missing/duplicate decision lines. |
| T-208-02-02 | Tampering | Evidence references | mitigate | Reject absolute paths, backslashes, `..`, and refs outside allowed evidence roots. |
| T-208-02-03 | Spoofing | Status tables | mitigate | Closed status vocabulary: `PASS`, `BLOCKED`, `PENDING`, `N/A`. |
| T-208-02-04 | Tampering | Runbook scope | mitigate | Reject language that converts bounded follow-on graduation into Phase 208 full-surface execution. |
| T-208-02-05 | Information Disclosure | Verifier runtime | mitigate | Verifier reads local files only and has no SDK/API-key code path. |
| T-208-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned; package legitimacy audit says no new packages. |
</threat_model>

<verification>
<automated>node scripts/ci/verify_ui_ratchet_signoff.mjs --self-test</automated>
</verification>

<success_criteria>
- Phase 208 sign-off acceptance is verifier-enforced before the actual sign-off exists.
- The verifier encodes UI-SPEC sections, status tokens, final ACCEPT format, runbook headings, bounded-language rules, and evidence refs.
</success_criteria>

<output>
Create `.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-02-SUMMARY.md` when done.
</output>

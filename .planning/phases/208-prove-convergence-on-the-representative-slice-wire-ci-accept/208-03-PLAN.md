---
phase: 208-prove-convergence-on-the-representative-slice-wire-ci-accept
plan: 03
type: execute
wave: 2
depends_on:
  - 208-01
  - 208-02
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/verify_admin_ui_ratchet_ci_contract.sh
  - accrue_admin/package.json
autonomous: true
requirements:
  - CONV-03
  - CONV-05
  - CONV-06
must_haves:
  truths:
    - "CONV-03/D-62/D-63/D-64: CI has a dedicated deterministic non-mutating job with no secret or LLM dependency."
    - "CONV-03/CONV-04/D-65: CI runs deterministic self-tests that prove required red paths."
    - "CONV-05/D-66: status copy and existing UI gate references are explicit and accessible."
  artifacts:
    - ".github/workflows/ci.yml: admin-ui-ratchet-guardrails job."
    - "scripts/ci/verify_admin_ui_ratchet_ci_contract.sh: workflow contract checker."
    - "accrue_admin/package.json: deterministic Phase 208 package scripts."
  key_links:
    - "ci.yml admin-ui-ratchet-guardrails -> scripts/ci/verify_ratchet_ledger.mjs --self-test and --verify-frozen."
    - "ci.yml admin-ui-ratchet-guardrails -> scripts/ci/verify_ui_ratchet_signoff.mjs --require-accept."
    - "annotation-sweep -> admin-ui-ratchet-guardrails."
---

<objective>
Wire the deterministic-only Phase 208 CI gate beside existing admin UI gates.

Purpose: satisfy D-62 through D-66 by adding a fast Node-only `admin-ui-ratchet-guardrails` job that never runs LLM/browser capture, never mutates or freezes the baseline, proves red paths in scratch fixtures, and reports exact status copy.

Output: CI job, package scripts, and contract verifier.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-CONTEXT.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-UI-SPEC.md
@.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-PATTERNS.md
@.github/workflows/ci.yml
@scripts/ci/verify_phase200_ci_contract.sh
@scripts/ci/verify_phase200_guardrail_contract.sh
@scripts/ci/verify_phase200_admin_guardrails.sh
@accrue_admin/package.json
@scripts/ci/verify_ratchet_ledger.mjs
@scripts/ci/verify_ui_ratchet_signoff.mjs
</context>

## Plan-Specific Artifacts

- New path: `scripts/ci/verify_admin_ui_ratchet_ci_contract.sh`.
- Updated workflow job: `.github/workflows/ci.yml` job id `admin-ui-ratchet-guardrails`, display name `Admin UI ratchet guardrails`, and annotation-sweep wiring.
- New package scripts in `accrue_admin/package.json`: `ratchet:ledger:verify-frozen`, `ratchet:signoff`, `ratchet:signoff:self-test`, and `ratchet:ci-contract`.

<tasks>

<task type="auto">
  <name>Task 1: Create CI contract verifier</name>
  <files>scripts/ci/verify_admin_ui_ratchet_ci_contract.sh</files>
  <read_first>
    - scripts/ci/verify_admin_ui_ratchet_ci_contract.sh (new/optional before this task; read only if a prior partial file exists)
    - scripts/ci/verify_phase200_ci_contract.sh
    - scripts/ci/verify_phase200_guardrail_contract.sh
    - .github/workflows/ci.yml
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-CONTEXT.md
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-PATTERNS.md
  </read_first>
  <action>Create `scripts/ci/verify_admin_ui_ratchet_ci_contract.sh` with the Phase 192/200 shell pattern: `set -euo pipefail`, `fail`, `require_file`, `require_fixed`, `require_source_regex`, `require_source_absent_regex`, and `job_body`. It must require job id `admin-ui-ratchet-guardrails`, display name `Admin UI ratchet guardrails`, Node setup, npm install from `accrue_admin/package-lock.json`, ledger self-tests, frozen read-only verification, sign-off verification, contract verification, and annotation-sweep inclusion. It must reject these exact target-job tokens/commands: `secrets.`, `ANTHROPIC_API_KEY`, `ratchet-propose`, `ratchet-verify`, `ui.round`, `ui.fix`, `playwright test`, browser capture commands, and `--freeze`.</action>
  <verify>
    <automated>bash -n scripts/ci/verify_admin_ui_ratchet_ci_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/ci/verify_admin_ui_ratchet_ci_contract.sh` passes before workflow wiring.
    - Running the script before Task 2 workflow wiring fails with a missing-job error; after Task 2 it passes.
    - The script extracts only the `admin-ui-ratchet-guardrails` job body before applying forbidden-token checks.
    - The script checks annotation-sweep `needs` and `annotation_sweep.sh` arguments include `admin-ui-ratchet-guardrails`.
  </acceptance_criteria>
  <done>The workflow contract checker exists and encodes D-64's job-boundary guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Node-only ratchet guardrail job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/ci/verify_admin_ui_ratchet_ci_contract.sh
    - scripts/ci/verify_ratchet_ledger.mjs
    - scripts/ci/verify_ui_ratchet_signoff.mjs
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-UI-SPEC.md
  </read_first>
  <action>Add a new `.github/workflows/ci.yml` job with stable key `admin-ui-ratchet-guardrails` and display name `Admin UI ratchet guardrails`, adjacent to the existing `admin-hardening-guardrails` and `admin-phase200-guardrails` jobs. The job must use checkout, `actions/setup-node@v6` with Node 22 and `cache-dependency-path: accrue_admin/package-lock.json`, `cd accrue_admin && npm ci`, then deterministic commands only: ledger reducer/verifier self-tests, read-only frozen verifier, sign-off verifier self-test and real `--require-accept`, and the CI contract script. Add a `GITHUB_STEP_SUMMARY` block or equivalent readback with the UI-SPEC PASS/BLOCKED/PENDING copy from D-66. Add artifact upload for ratchet evidence paths if present. Add `admin-ui-ratchet-guardrails` to the stable job-id contract comment, annotation-sweep `needs`, and annotation-sweep argument list. Do not add BEAM/Postgres/browser setup, LLM proposer/verifier commands, live `ui.round`/`ui.fix`, or baseline freeze.</action>
  <verify>
    <automated>bash scripts/ci/verify_admin_ui_ratchet_ci_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_admin_ui_ratchet_ci_contract.sh` passes.
    - The job body contains `continue-on-error: true` nowhere.
    - The job body contains no secret reference, no LLM command, no browser capture command, and no freeze flag.
    - Existing job ids `admin-hardening-guardrails` and `admin-phase200-guardrails` remain independent jobs.
  </acceptance_criteria>
  <done>The deterministic-only CI job is wired and contract-proven per D-62, D-63, D-64, and D-66.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic package scripts</name>
  <files>accrue_admin/package.json</files>
  <read_first>
    - accrue_admin/package.json
    - scripts/ci/verify_admin_ui_ratchet_ci_contract.sh
    - scripts/ci/verify_ratchet_ledger.mjs
    - scripts/ci/verify_ui_ratchet_signoff.mjs
    - .planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-PATTERNS.md
  </read_first>
  <action>Add package scripts for Phase 208 deterministic commands without changing existing live ratchet aliases: `ratchet:ledger:verify-frozen` runs `node ../scripts/ci/verify_ratchet_ledger.mjs --verify-frozen`; `ratchet:signoff:self-test` runs `node ../scripts/ci/verify_ui_ratchet_signoff.mjs --self-test`; `ratchet:signoff` runs `node ../scripts/ci/verify_ui_ratchet_signoff.mjs --require-accept`; `ratchet:ci-contract` runs `bash ../scripts/ci/verify_admin_ui_ratchet_ci_contract.sh`. Do not put `--freeze`, `ui.round`, `ui.fix`, `ratchet-propose`, or `ratchet-verify` into any CI-oriented package script.</action>
  <verify>
    <automated>cd accrue_admin && npm run ratchet:signoff:self-test && npm run ratchet:ci-contract</automated>
  </verify>
  <acceptance_criteria>
    - New package scripts resolve and run from `accrue_admin`.
    - Existing `ratchet:ledger`, `ratchet:ledger:self-test`, `phase200:signoff`, and `phase200:guardrails` scripts remain present.
    - CI contract script passes after package wiring.
  </acceptance_criteria>
  <done>Local and CI command names are stable for Plans 04 and 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions YAML -> PR gate | Workflow text defines what can block merges. |
| CI environment -> deterministic scripts | CI must not expose secrets or invoke live LLM/browser paths. |
| Package scripts -> CI job | Script indirection must not hide mutation or freeze commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-208-03-01 | Information Disclosure | CI job | mitigate | Contract rejects secret references and LLM command paths in the ratchet job. |
| T-208-03-02 | Tampering | `ledger.baseline.json` | mitigate | CI uses `--verify-frozen` only and contract rejects `--freeze`. |
| T-208-03-03 | Elevation of Privilege | GitHub job semantics | mitigate | Job must not use advisory failure semantics and is included in annotation sweep. |
| T-208-03-04 | Repudiation | CI status copy | mitigate | Step summary/status copy names exact artifacts and status constants. |
| T-208-03-05 | Denial of Service | Overbroad CI setup | mitigate | Node-only job avoids BEAM/Postgres/browser setup and live capture. |
| T-208-SC | Tampering | npm/pip/cargo installs | accept | Uses existing npm lockfile; no new package installation request is introduced. |
</threat_model>

<verification>
<automated>bash scripts/ci/verify_admin_ui_ratchet_ci_contract.sh</automated>
<automated>cd accrue_admin && npm run ratchet:signoff:self-test && npm run ratchet:ci-contract</automated>
</verification>

<success_criteria>
- Dedicated deterministic CI job exists with exact id/name.
- CI job has no secret, LLM, browser capture, round/fix, or freeze path.
- Annotation sweep and package script contracts include the new job and commands.
</success_criteria>

<output>
Create `.planning/phases/208-prove-convergence-on-the-representative-slice-wire-ci-accept/208-03-SUMMARY.md` when done.
</output>

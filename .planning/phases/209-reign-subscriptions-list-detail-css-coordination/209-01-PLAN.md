---
phase: 209-reign-subscriptions-list-detail-css-coordination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REIGN-01]

must_haves:
  truths:
    - "A pre-reign PNG snapshot of Subscriptions (light + dark) and Subscription-detail (light + dark) exists before any markup edit lands, so Plan 03 can prove density-no-regression and detail-page-unbroken."
    - "The Elixir test baseline (subscriptions_live_test.exs) is confirmed green (12/12) before any code change, establishing the pre-reign starting point."
  artifacts:
    - "accrue_admin/test-results/admin-visuals-baseline-209/{project}/subscriptions.png (+ -dark, + .bbox.json)"
    - "accrue_admin/test-results/admin-visuals-baseline-209/{project}/subscription-detail.png (+ -dark, + .bbox.json)"
  key_links:
    - "Baseline PNG directory path is consumed by Plan 03's density-no-regression and detail-page-parity checks."
---

<objective>
Capture the pre-reign evidence baseline (PNG screenshots + a green test run) for the Subscriptions list and Subscription-detail pages BEFORE any template edit lands, so later plans in this phase can prove "no regression" against a real snapshot rather than a description.

Purpose: REIGN-01's density-no-regression and D-04's detail-page-unbroken proof both require a true "before" artifact. Capturing it after edits begin would make the comparison meaningless.
Output: A gitignored baseline PNG set under `accrue_admin/test-results/admin-visuals-baseline-209/`, plus a confirmed-green starting test run recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-CONTEXT.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm pre-reign Elixir test baseline is green</name>
  <files></files>
  <read_first>
    - accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs (the file that will be migrated in Plan 03 — confirm its current green state as the documented starting point)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md (§ "Test/Selector Migration Surface" — already confirms 12/12 passing at commit f6278ff9; this task re-confirms against the current working tree before edits begin)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (the load-bearing design contract this phase's later plans implement against — read now so the pre-reign baseline you're confirming is understood in light of what it will become)
  </read_first>
  <action>
    Run the existing subscriptions test file exactly as-is, with zero edits, to record the pre-reign baseline pass count. This is a read-only confirmation — do not modify subscriptions_live_test.exs or any source file in this task. If the count differs from 12/12 (e.g. because of unrelated drift since RESEARCH was written), record the actual current count in the plan SUMMARY instead of assuming 12 — Plan 03's migration must diff against whatever is actually true at execution time, not the RESEARCH doc's snapshot.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/subscriptions_live_test.exs` exits 0.
    - The SUMMARY for this plan records the exact "N tests, 0 failures" line from the test run output.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscriptions_live_test.exs</automated>
  </verify>
  <done>Test run completes with 0 failures; the exact pass count is recorded for Plan 03 to diff against.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-reign PNG baseline for Subscriptions + Subscription-detail</name>
  <files></files>
  <read_first>
    - accrue_admin/e2e/admin-visuals.spec.js (lines 1-175 — capture harness, `RATCHET_SURFACES` filter, `captureThemes/3` output path `test-results/admin-visuals/{project}/{name}.png` / `{name}-dark.png` + `.bbox.json` sidecar)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md (§ "Density + PNG-Parity Verification Approach")
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (§ "Spacing Scale" — density-no-regression gate: row height, rows-per-viewport, header-band height held at-or-tighter)
  </read_first>
  <action>
    From `accrue_admin/`, run the scoped Playwright capture for exactly the two surfaces this phase touches (`subscriptions` + `subscription-detail`), both themes. This writes into the gitignored `test-results/admin-visuals/{project}/` directory (confirmed gitignored at `.gitignore:28`). Copy that entire output directory tree to a sibling gitignored location, `accrue_admin/test-results/admin-visuals-baseline-209/`, so it survives Plan 02's edits and later re-captures without being overwritten (do not `git add` any of this — it must never enter version control, matching the project's established "zero PNGs in git" convention for capture-and-curate exemplars). After copying, use the Read tool to open `subscriptions.png` and `subscriptions-dark.png` directly (not just diff pixel counts) and note in the plan SUMMARY: current row height / approximate rows-per-viewport / header-band height, and the current triplicated-CTA + triplicated-verdict visual shape — this is the literal "before" picture Plan 03 compares against.
  </action>
  <acceptance_criteria>
    - `RATCHET_SURFACES=subscriptions,subscription-detail npx playwright test e2e/admin-visuals.spec.js` (run from `accrue_admin/`) exits 0.
    - `accrue_admin/test-results/admin-visuals-baseline-209/{project}/subscriptions.png`, `subscriptions-dark.png`, `subscription-detail.png`, `subscription-detail-dark.png` all exist (plus their `.bbox.json` sidecars).
    - None of the baseline files are staged in git (`git status --porcelain accrue_admin/test-results/` shows nothing, confirming the gitignore covers them).
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && RATCHET_SURFACES=subscriptions,subscription-detail npx playwright test e2e/admin-visuals.spec.js && ls test-results/admin-visuals-baseline-209/*/subscriptions.png test-results/admin-visuals-baseline-209/*/subscription-detail.png</automated>
  </verify>
  <done>Baseline PNG set (light+dark, both surfaces) captured and copied to the gitignored `admin-visuals-baseline-209` holding directory; Read directly and geometry/shape notes recorded in the plan SUMMARY for Plan 03 to diff against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan performs zero writes to application source, tests, or CSS — it only runs the existing test suite and an existing, already-wired Playwright capture, and copies output files within the already-gitignored `test-results/` tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-00 | N/A | N/A | low | accept | No trust boundary is crossed by this plan (read-only verification/capture); no mitigation needed. |
</threat_model>

<verification>
Both tasks are read-only against source; the only "output" is the baseline evidence set. Verify by confirming both automated commands exit 0 and the four baseline PNGs (plus bbox sidecars) exist on disk under the gitignored holding directory.
</verification>

<success_criteria>
- Pre-reign test pass count recorded (expected 12/12, but record whatever is actually true).
- Pre-reign PNG baseline (Subscriptions + Subscription-detail, light + dark) captured and preserved outside git for Plan 03's before/after comparison.
</success_criteria>

## Artifacts This Phase Produces (this plan's contribution)

- `accrue_admin/test-results/admin-visuals-baseline-209/{project}/subscriptions{,-dark}.png` (+ `.bbox.json`) — gitignored, not committed.
- `accrue_admin/test-results/admin-visuals-baseline-209/{project}/subscription-detail{,-dark}.png` (+ `.bbox.json`) — gitignored, not committed.
- No source, test, or CSS files are created or modified by this plan.

<output>
Create `.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-01-SUMMARY.md` when done
</output>

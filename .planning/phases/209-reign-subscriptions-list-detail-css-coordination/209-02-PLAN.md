---
phase: 209-reign-subscriptions-list-detail-css-coordination
plan: 02
type: execute
wave: 2
depends_on: ["209-01"]
files_modified:
  - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
  - accrue_admin/lib/accrue_admin/copy/subscription.ex
  - accrue_admin/lib/accrue_admin/copy.ex
autonomous: true
requirements: [REIGN-01, REIGN-02, COMP-01]

must_haves:
  truths:
    - "Visiting /billing/subscriptions shows exactly one health verdict (a real StatusBadge, not a hand-rolled span) and exactly one primary CTA ('Open invoice queue') in PageHeader :actions — per D-01/D-03 and ROADMAP Phase 209 success criteria #2."
    - "Nothing renders between FlashGroup.flash_group and the DataTable live_component — the five bespoke band <section>s are gone, per UI-SPEC Interaction Contract point 1."
    - "The rebuilt identity_cell/3 and billing_signals_cell/3 contain zero <button> or in-cell action <a class=\"ax-button...\"> elements — per D-01/REIGN-02."
    - "No new component file exists at accrue_admin/lib/accrue_admin/components/work_queue_callout.ex — COMP-01 resolves inline per D-02."
    - "Every distinct operator datum from the removed bands (open-invoice count, exposure $, at-risk count, failed-webhook count) is relocated into the 4-stat StatStrip or the single verdict — none are silently dropped, per D-03 content-preservation."
  artifacts:
    - "accrue_admin/lib/accrue_admin/live/subscriptions_live.ex (rebuilt render/1 spine + cells; dead code removed)"
    - "accrue_admin/lib/accrue_admin/copy/subscription.ex (6 new Copy fns: subscriptions_index_breadcrumb/0, subscriptions_invoice_queue_cta/0, subscriptions_health_verdict_healthy/0, subscriptions_health_verdict_action_required/0, subscriptions_route_line/0, subscriptions_kpi_section_aria_label/0)"
    - "accrue_admin/lib/accrue_admin/copy.ex (6 new defdelegate lines for the above)"
  key_links:
    - "identity_cell/3's primary <a class=\"ax-link\"> href must resolve to /subscriptions/:id (D-01's literal href mandate) even though its link TEXT is the customer label."
    - "PageHeader :stat_strip slot renders StatStrip.stat_strip with NO wrapping <div> (matches Invoices/Customers; removes the exact ax-kpi-row class subscriptions_live_test.exs:111 currently asserts on)."
    - "The single CTA's href still resolves through the unchanged invoice_queue_path/2 helper — do not rename or duplicate this function."
---

<objective>
Recompose `subscriptions_live.ex`'s `render/1` onto the canonical spine (drop page-override classes, collapse the verdict to one StatusBadge, collapse the CTA to one primary button, rebuild the StatStrip to D-03's 4-stat money-at-risk order), remove the five bespoke band `<section>`s and their now-dead helper chain, and rebuild `identity_cell/3` + `billing_signals_cell/3` to the compact `ax-stack-xs` / `ax-chip.ax-label` idiom with zero in-cell actions.

Purpose: This is the load-bearing REIGN-01/REIGN-02/COMP-01 implementation work — everything Phase 209 is named for. COMP-01 resolves as "inline" per D-02 (no new component file is created).
Output: A `subscriptions_live.ex` that structurally matches `invoices_live.ex`/`customers_live.ex`'s spine, with zero dead code left behind (`mix compile --warnings-as-errors` clean).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-CONTEXT.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md
@accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
@accrue_admin/lib/accrue_admin/live/invoices_live.ex
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Header/spine rebuild — breadcrumbs, title, single verdict badge, single CTA, 4-stat StatStrip, Copy additions</name>
  <files>
    accrue_admin/lib/accrue_admin/live/subscriptions_live.ex,
    accrue_admin/lib/accrue_admin/copy/subscription.ex,
    accrue_admin/lib/accrue_admin/copy.ex
  </files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex:87-192 (current PageHeader block — the exact region this task rewrites) and :344-355 (assign_shell/2) and :496-545 (billing_priority_title/1, subscriptions_health_verdict/1 — the two dead functions this task retires)
    - accrue_admin/lib/accrue_admin/live/invoices_live.ex:83-129 (canonical spine: breadcrumbs via Copy fns, plain `ax-page`, no PageHeader `class=`, `:stat_strip` with no wrapping div)
    - accrue_admin/lib/accrue_admin/components/stat_strip.ex (full file — `tone_class/1` only supports "moss"/"cobalt"/"amber"; do not pass any other tone string)
    - accrue_admin/lib/accrue_admin/components/status_badge.ex (full file — `<StatusBadge.status_badge status={} label={} tone={} />` function-component signature, used directly in a `~H` template here, unlike the raw-HTML workaround in cell render-fns)
    - accrue_admin/lib/accrue_admin/copy/subscription.ex (full file — existing function/comment-header style to match) and accrue_admin/lib/accrue_admin/copy.ex:210-251 (existing `defdelegate subscription_*`/`subscriptions_list_*` block — add the 6 new delegates adjacent to this block)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (§ Copywriting Contract — locked exact strings; § Color — StatStrip tone rules; § Interaction & Structural Contract points 1, 2, 6, 8)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-CONTEXT.md (D-01, D-03, and the Open Question 2 resolution: keep both secondary `:actions` buttons unchanged)
  </read_first>
  <action>
    In `copy/subscription.ex`, add a new comment-headed section ("# Subscriptions list reign (SubscriptionsLive) — Phase 209, REIGN-01/REIGN-02") with 6 functions: `subscriptions_index_breadcrumb/0` returning the literal short noun `"Subscriptions"` (used for BOTH the breadcrumb leaf and the PageHeader `title=` attr, per the UI-SPEC Copywriting Contract locking title == breadcrumb leaf for this page — do not treat this the way `invoices_list_heading/0`/`customers_list_heading/0` differ from their breadcrumb-leaf fns; Subscriptions is explicitly locked to reuse the same short noun for both); `subscriptions_invoice_queue_cta/0` returning `"Open invoice queue"` (drops "dedicated" per the Copywriting Contract); `subscriptions_health_verdict_healthy/0` returning `"Healthy"`; `subscriptions_health_verdict_action_required/0` returning `"Action required"`; `subscriptions_route_line/0` returning a short plain-language one-liner describing the operator's next actions (no "dedicated"/"workspace" jargon, replacing the current `ax-subscriptions-route-line` paragraph's text); `subscriptions_kpi_section_aria_label/0` returning an accurate aria-label describing the 4-stat strip's actual contents (the current `"Dunning funnel and open-invoice queue summary"` becomes inaccurate once the Dunning-funnel stat is dropped in this same task — replace it). Add matching `defdelegate` lines in `copy.ex` next to the existing `subscription_*`/`subscriptions_list_*` block (~line 210-251), delegating to `Subscription`.

    In `subscriptions_live.ex`'s `render/1`: change the outer `<section class="ax-page ax-page-compact ax-subscriptions-page">` to plain `<section class="ax-page">` (drop both override classes). Remove the `class="ax-page-header-compact ax-subscriptions-header"` attribute from `PageHeader.page_header` entirely (no `class=` attr at all, matching Invoices/Customers). Replace the `breadcrumbs` list: first item becomes `%{label: Copy.dashboard_breadcrumb_home(), href: scoped_path(@admin_mount_path, "", @current_owner_scope)}` (do NOT change `Copy.dashboard_breadcrumb_home/0`'s return value itself — it is shared infrastructure used by 7+ other admin pages; only use it here as-is), second item becomes `%{label: Copy.subscriptions_index_breadcrumb()}` (no href — current-page leaf crumb). Change `title={subscriptions_health_verdict(@summary)}` to `title={Copy.subscriptions_index_breadcrumb()}`.

    Rebuild the `:description` slot to two short paragraphs: first, a `<p>` wrapping a `<StatusBadge.status_badge status={verdict_status(@summary)} label={verdict_label(verdict_status(@summary))} tone={verdict_tone(verdict_status(@summary))} />` — this is the ONE single health verdict for the whole page; second, a `<p class="ax-body">` rendering `Copy.subscriptions_route_line()`. Add three new private helpers: `verdict_status/1` returning `:action_required` when `summary.open_invoice_count > 0` else `:healthy`; `verdict_label/1` mapping `:action_required` → `Copy.subscriptions_health_verdict_action_required()` and `:healthy` → `Copy.subscriptions_health_verdict_healthy()`; `verdict_tone/1` mapping `:action_required` → `"amber"` and `:healthy` → `"moss"` (both valid `StatStrip`/`StatusBadge`-supported tones). Remove `subscriptions_health_verdict/1` (both clauses, its only caller was the old `title=` attr you just replaced) — this function is now dead. Do NOT remove `billing_priority_title/1` in this task — it is still called by band 1, which Task 2 removes.

    Collapse `:actions` to exactly one primary `<a class="ax-button ax-button-primary ax-button-sm">` (drop the `ax-subscriptions-primary-workspace` decorative class) with text `Copy.subscriptions_invoice_queue_cta()` and unchanged `href={invoice_queue_path(@admin_mount_path, @current_owner_scope)}`. Keep the two existing secondary buttons exactly as they are today (text "Webhooks: debug failed deliveries" and "Events audit log", unchanged hrefs) per the phase's resolved Open Question 2 — only drop the `ax-subscriptions-webhook-workspace` decorative class from the first secondary.

    Rebuild `:stat_strip` to pass `<StatStrip.stat_strip label={Copy.subscriptions_kpi_section_aria_label()}>` directly into the slot with NO wrapping `<div class="ax-kpi-row ax-subscriptions-kpi-row">` (matches Invoices/Customers exactly — this is the change that removes the exact class string `subscriptions_live_test.exs:111` asserts on). Reduce the `:stat` entries from 5 to exactly 4, in this order (D-03's money-at-risk-first order): (1) label `"Open invoices"`, value `count(@summary.open_invoice_count, "invoice")`, tone `"cobalt"`, href `invoice_queue_path(@admin_mount_path, @current_owner_scope)`; (2) label `"Exposure"`, value `format_minor(@summary.open_invoice_exposure_minor, "usd")`, tone from a new helper `exposure_tone/1` (`"amber"` when `open_invoice_exposure_minor > 0`, else `"moss"`), same href as (1); (3) label `"At-risk subscriptions"`, value `count(@summary.past_due_count, "subscription")`, tone `"amber"` (unconditional, matches today's existing behavior — do not make this dynamic, out of scope), href unchanged (`/analytics/recovery`); (4) label `"Failed webhooks"`, value `count(@summary.failed_webhook_count, "failed webhook")`, tone from a new helper `webhook_tone/1` (`"amber"` when `failed_webhook_count > 0`, else `"moss"`), href unchanged (webhooks filtered to `status=failed,dead`). Remove the "MRR signal" stat entirely (it was `tone="slate"`, which `StatStrip.tone_class/1` does not support — falls through to untoned today; it was also non-computed placeholder text `"Not projected"`, not a real datum) and the "Dunning funnel" stat entirely (it duplicated `past_due_count`, the same underlying count the surviving "At-risk subscriptions" stat already shows).

    In `assign_shell/2`, change `|> assign(:page_title, "Subscriptions")` to `|> assign(:page_title, Copy.subscriptions_index_breadcrumb())` for consistency (this is the browser-tab title, already correct text-wise, just now Copy-sourced).
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix compile --warnings-as-errors` succeeds with zero warnings (proves `subscriptions_health_verdict/1` was fully removed with no dangling caller, and no new unused code was introduced).
    - `grep -c "ax-kpi-row ax-subscriptions-kpi-row" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - `grep -c "Billing health overview" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - `grep -c "ax-page-compact ax-subscriptions-page" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - `grep -c "def subscriptions_index_breadcrumb\|def subscriptions_invoice_queue_cta\|def subscriptions_health_verdict_healthy\|def subscriptions_health_verdict_action_required\|def subscriptions_route_line\|def subscriptions_kpi_section_aria_label" accrue_admin/lib/accrue_admin/copy/subscription.ex` returns `6`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <done>PageHeader renders with plain `ax-page`/no override class, a single StatusBadge verdict + single primary CTA, and a 4-stat unwrapped StatStrip in D-03's order; 6 new Copy fns exist and are wired; `mix compile --warnings-as-errors` is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the five bespoke bands + dead-code chain; rebuild identity_cell/3 and billing_signals_cell/3 to the compact idiom</name>
  <files>accrue_admin/lib/accrue_admin/live/subscriptions_live.ex</files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex:29-54 (mount/3 — the `:open_invoice_queue` assign to remove), :196-279 (the five bands to delete), :407-449 (open_invoice_queue/1 + open_invoice_queue_base/2 — dead), :451-538 (billing_signals_cell/3 + billing_priority_title/1 + identity_cell/3 — rebuild targets), :835-874 (invoice_queue_path/2 and invoice_queue_path_from_table/1 — KEEP both; :841-865 invoice_queue_record_href/3, invoice_queue_due/1, invoice_queue_customer_label/1 — dead, remove)
    - accrue_admin/lib/accrue_admin/live/invoices_live.ex:223-242 (`billing_signals_cell/1` — the exact 2-chip idiom to extract to; `invoice_identity_cell/3` — the exact two-line `ax-stack-xs` idiom, primary link to the row's own detail page, secondary muted link to the related customer)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md (§ "Target File Anatomy" — cell rebuild table and dead-code table; § "Common Pitfalls" Pitfall 4 — ordering-assertion trap this rebuild must not fall into, relevant for Plan 03's test rewrite, not this task, but shapes the design below)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-CONTEXT.md (D-01 — identity cell's primary `ax-link` MUST resolve to `/subscriptions/:id`)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (§ Interaction & Structural Contract points 1, 3, 5 — band removal, compact-cell idiom, detail-page shared-CSS coordination)
  </read_first>
  <action>
    Delete the five bespoke `<section>` blocks that currently sit between `FlashGroup.flash_group` and the `.live_component module={DataTable}` call: the `ax-inline-worklist ax-subscriptions-invoice-strip` section (band 1, with its `aria-label="Open-invoice queue worklist"`), the `ax-subscriptions-queue-shortcut` section (band 2, `aria-label="Open-invoice queue records"`), the `ax-inline-worklist ax-subscriptions-invoice-records` section (band 3, `aria-label="Open-invoice queue preview"`), and the `ax-subscriptions-secondary-strips` wrapper `<div>` containing both the `ax-inline-worklist ax-subscriptions-at-risk-strip` section (band 4a) and the `ax-inline-worklist ax-subscriptions-audit-strip` section (band 4b). After deletion, `FlashGroup.flash_group` must be immediately followed by `<.live_component module={DataTable} ...>` with nothing in between.

    Remove the `|> assign(:open_invoice_queue, open_invoice_queue(socket.assigns.current_owner_scope))` line from `mount/3` (its only consumer, band 3, is gone). Remove the now-fully-dead helper chain: `open_invoice_queue/1`, `open_invoice_queue_base/2` (both clauses), `invoice_queue_record_href/3`, `invoice_queue_due/1` (all clauses), `invoice_queue_customer_label/1` (all clauses), and `billing_priority_title/1` (both clauses — band 1 was its only caller and is now gone). Do NOT remove `invoice_queue_path/2` (still used by the single primary CTA from Task 1) or `invoice_queue_path_from_table/1` (still used by `work_queue_chips/3`'s "Open invoice queue workspace" chip — unrelated to the bands, filters stay untouched).

    Rebuild `identity_cell/3` to a two-line `ax-stack-xs` block matching the Invoices idiom structurally, but with content roles chosen deliberately for Subscriptions (document this choice with a one-line code comment above the function): the PRIMARY `<a class="ax-link">` has `href` = `scoped_path(mount_path, "/subscriptions/#{row.id}", owner_scope)` (satisfies D-01's literal mandate that the identity cell's link resolves to the subscription's own detail page) and TEXT = `customer_label(row)` (preserves the current customer-first scannability — an operator scanning this column wants to see WHO, not a raw subscription ID, as the prominent text); the SECONDARY `<a class="ax-label ax-muted">` has `href` = `scoped_path(mount_path, "/customers/#{row.customer_id}", owner_scope)` (cross-navigation to the customer, mirroring Invoices' bidirectional-link pattern) and TEXT = `row.processor_id || row.id` (the subscription's own compact identifier, still visible but no longer wrapped in a "Subscription:" labeled fact). Remove entirely: the hand-rolled inline `ax-status-badge`/state-dot markup (the real `StatusBadge` already renders correctly in the separate "State" column via `state_cell/1` — no duplicate needed here), the "Customer ID: …" / "Subscription: …" labeled meta-grid facts, the customer-scope caption line, and all 3 in-cell action links/buttons ("Work open invoices", "Send reminder", "Filter invoice queue to this subscription" — D-01 forbids in-cell actions; no actions column or DropdownMenu is added, per D-01's "drop the per-row action entirely" decision).

    Rebuild `billing_signals_cell/3` to render ONLY the two existing `<span class="ax-chip ax-label">Owner: …</span> <span class="ax-chip ax-label">Tax: …</span>` spans (currently the last line of the function, already the correct idiom — extract verbatim, drop the wrapping `ax-subscription-row-admin-chips` span since it is bespoke/Subscriptions-only with no CSS-coordination risk). Remove entirely: the `ax-audit-summary-row`-classed audit-fact block (this stops referencing a base CSS rule shared with the detail page and dev component-kitchen — per D-04 you are only removing the REFERENCE here, not editing the CSS rule itself, which stays in `app.css` untouched for those other two consumers), the fake "Invoice queue status" line, and the in-cell "Webhook status" line + its "Open failed-delivery debugger" link, and the "Open audit context for this subscription" link (all either dead/misleading copy or forbidden in-cell action links per D-01/REIGN-02).

    In both the `columns` and `card_fields` lists passed to the `DataTable` `live_component`, rename the `"Signals / audit"` column label to `"Signals"` (the audit content is gone from the cell, so the label should no longer promise it).
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix compile --warnings-as-errors` succeeds with zero warnings (proves the entire dead-code chain — `open_invoice_queue/1`, `open_invoice_queue_base/2`, `invoice_queue_record_href/3`, `invoice_queue_due/1`, `invoice_queue_customer_label/1`, `billing_priority_title/1` — was fully removed with no orphaned callers, and `invoice_queue_path/2`/`invoice_queue_path_from_table/1` are still referenced so they do NOT warn).
    - `grep -c "ax-inline-worklist\|ax-audit-summary-row\|ax-subscriptions-invoice-strip\|ax-subscriptions-queue-shortcut\|ax-subscriptions-invoice-records\|ax-subscriptions-secondary-strips\|ax-subscriptions-at-risk-strip\|ax-subscriptions-audit-strip" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - `grep -c "Work open invoices\|Send reminder\|Filter invoice queue to this subscription\|Open failed-delivery debugger\|Open audit context for this subscription" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - A visual read of the file confirms `FlashGroup.flash_group` is immediately followed by `<.live_component module={DataTable}`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Zero bespoke bands remain; `identity_cell/3` and `billing_signals_cell/3` are two-line/two-chip compact cells with zero in-cell actions; the full dead-code chain is gone; `mix compile --warnings-as-errors` is clean. `mix test` on the (not-yet-migrated) test file is EXPECTED to fail here — that is Plan 03's job, not this task's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin operator → rendered HTML | `identity_cell/3`/`billing_signals_cell/3` interpolate DB-sourced strings (customer name/email, processor IDs, ownership/tax labels) into raw HTML via `Phoenix.HTML.raw/1`. |
| Admin operator → cross-navigation links | Rebuilt cell hrefs expose `customer_id`/`subscription.processor_id` in link text and `href` attributes to any authenticated admin viewing this page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-01 | Tampering (stored-XSS via unescaped interpolation) | `identity_cell/3`, `billing_signals_cell/3` (`Phoenix.HTML.raw/1` blocks) | medium | mitigate | Every interpolated dynamic value (`customer_label(row)`, `row.processor_id`, ownership/tax labels) MUST pass through the existing `escape/1` helper (`Phoenix.HTML.html_escape/1` + `safe_to_string/1`) exactly as the current code already does before this rebuild — no new raw-interpolation surface is introduced; verify by grep for `escape(` wrapping every dynamic value inside the two rebuilt functions. |
| T-209-02 | Information Disclosure | Cross-navigation link text/hrefs exposing `customer_id`/`processor_id` | low | accept | Pre-existing behavior — these are internal operator-facing identifiers already gated behind `require_admin_plug` (unchanged by this phase); the rebuild only relocates where the IDs render, it does not add new exposure. |
| T-209-03 | Availability (unnecessary DB load) | Removal of `open_invoice_queue/1` Ecto query | low | accept | Query removal reduces load (favorable); no availability risk introduced. |
| T-209-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this phase — confirmed via `git diff --stat accrue_admin/mix.exs accrue_admin/mix.lock` empty; package legitimacy gate not triggered. |
</threat_model>

<verification>
Both tasks gate on `mix compile --warnings-as-errors`, which is the authoritative proxy for "every named dead function was actually removed and nothing new went dead." `mix test` on the not-yet-migrated test file is intentionally left red at the end of this plan — Plan 03 owns the test rewrite. Do not "fix" the test file in this plan.
</verification>

<success_criteria>
- `subscriptions_live.ex`'s spine matches `invoices_live.ex`'s structurally: plain `ax-page`, no PageHeader `class=`, unwrapped `:stat_strip`, single verdict, single primary CTA.
- Zero bespoke band `<section>`s remain between FlashGroup and DataTable.
- `identity_cell/3`/`billing_signals_cell/3` are compact, zero-in-cell-action cells.
- No new component file exists — COMP-01 resolves inline.
- `mix compile --warnings-as-errors` clean at the end of both tasks.
</success_criteria>

## Artifacts This Phase Produces (this plan's contribution)

- New functions in `accrue_admin/lib/accrue_admin/copy/subscription.ex`: `subscriptions_index_breadcrumb/0`, `subscriptions_invoice_queue_cta/0`, `subscriptions_health_verdict_healthy/0`, `subscriptions_health_verdict_action_required/0`, `subscriptions_route_line/0`, `subscriptions_kpi_section_aria_label/0`.
- New `defdelegate` entries for the above in `accrue_admin/lib/accrue_admin/copy.ex`.
- New private helpers in `subscriptions_live.ex`: `verdict_status/1`, `verdict_label/1`, `verdict_tone/1`, `exposure_tone/1`, `webhook_tone/1`.
- Rebuilt `identity_cell/3` and `billing_signals_cell/3` in `subscriptions_live.ex`.
- Removed (dead code, no longer present): `subscriptions_health_verdict/1`, `billing_priority_title/1`, `open_invoice_queue/1`, `open_invoice_queue_base/2`, `invoice_queue_record_href/3`, `invoice_queue_due/1`, `invoice_queue_customer_label/1`, and the `:open_invoice_queue` mount assign.

<output>
Create `.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-02-SUMMARY.md` when done
</output>

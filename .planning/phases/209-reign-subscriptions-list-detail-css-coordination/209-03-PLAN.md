---
phase: 209-reign-subscriptions-list-detail-css-coordination
plan: 03
type: execute
wave: 3
depends_on: ["209-02"]
files_modified:
  - accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs
  - accrue_admin/priv/static/accrue_admin.css
  - examples/accrue_host/e2e/generated/copy_strings.json
autonomous: true
requirements: [REIGN-01, REIGN-02, COMP-01]

must_haves:
  truths:
    - "The subscription DETAIL page (subscription_live.ex) and the dev component-kitchen gallery still reference .ax-inline-worklist*/.ax-audit-summary-row after this phase — subscriptions_live.ex no longer does — proving D-04's shared-CSS coordination held."
    - "accrue_admin/assets/css/app.css has zero diff for this entire phase (git diff --stat empty) — Phase 209 made zero edits to shared CSS rules, as D-04 requires."
    - "subscriptions_live_test.exs is fully green (mix test, 0 failures) against the Plan 02 rebuild — every retired-class/copy assertion is migrated, not deleted-without-replacement where a positive replacement is warranted."
    - "Both generated artifacts (priv/static/accrue_admin.css, examples/accrue_host/e2e/generated/copy_strings.json) are rebuilt via their mix tasks and committed, so the served bundle and the e2e-consumed copy snapshot never go stale."
    - "The post-reign Subscriptions PNG shows row height / rows-per-viewport / header-band height at-or-tighter than the Plan 01 baseline, and the post-reign Subscription-detail PNG is visually identical to the Plan 01 baseline."
  artifacts:
    - "accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs (rewritten assertions)"
    - "accrue_admin/priv/static/accrue_admin.css (rebuilt via mix accrue_admin.assets.build)"
    - "examples/accrue_host/e2e/generated/copy_strings.json (rebuilt via mix accrue_admin.export_copy_strings)"
  key_links:
    - "Plan 01's baseline PNGs under accrue_admin/test-results/admin-visuals-baseline-209/ are the comparison target for this plan's density/parity checks."
    - "copy_strings.json must contain the 6 new Copy.Subscription function values added in Plan 02, or e2e specs reading it will go stale (per project's 'CI green-up' lesson)."
---

<objective>
Prove the phase's namesake shared-CSS-coordination risk held (D-04 grep-gate + detail-page PNG parity), migrate every retired-class/copy assertion in `subscriptions_live_test.exs` to the post-reign shape, and rebuild + commit both generated artifacts so nothing ships stale.

Purpose: This is the phase's closing verification — it turns Plan 02's code change into a provably-safe, fully-green, fully-committed unit. Without this plan, Plan 02's work is unverified and the detail page's safety is merely asserted, not demonstrated.
Output: Green `mix test`, a grep-gate transcript proving zero shared-CSS edits and correct residual reference ownership, PNG evidence of density-no-regression + detail-page parity, and both generated artifacts committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-CONTEXT.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-01-SUMMARY.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-02-SUMMARY.md
@accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04 shared-CSS grep-gate + detail-page/list-page PNG verification</name>
  <files></files>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex (post-Plan-02 state — confirm the rebuilt shape before grepping it)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md (§ "Shared-CSS Coordination (the namesake risk)" — the exact base-rule vs scoped-rule tables this task's greps re-derive against the post-edit tree)
    - accrue_admin/e2e/admin-visuals.spec.js (capture harness, same as Plan 01)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-01-SUMMARY.md (baseline PNG location + recorded geometry notes)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (§ Interaction & Structural Contract points 5, 6, 8 — the D-04 shared-CSS coordination gate and density/parity gate this task proves)
  </read_first>
  <action>
    Run six grep/diff checks in sequence and record each result verbatim in the SUMMARY (do not summarize away a non-matching result — if any check fails, stop and fix Plan 02's code before proceeding to Task 2):
    1. Confirm zero remaining band/override-class references in the target file (all `.ax-subscriptions-*` band, kpi-row, and page/header override classes).
    2. Confirm zero remaining shared-base-rule class references (`ax-inline-worklist`, `ax-audit-summary-row`) in the target file — these rules must now be referenced by OTHER files only.
    3. Confirm the only `.ex` files under `lib/accrue_admin/live/` and `lib/accrue_admin/dev/` still referencing `ax-inline-worklist` are `subscription_live.ex` and `component_kitchen_live.ex` (NOT `subscriptions_live.ex`).
    4. Confirm the only `.ex` files still referencing `ax-audit-summary-row` are `dashboard_live.ex`, `subscription_live.ex`, and `component_kitchen_live.ex` (NOT `subscriptions_live.ex`) — `dashboard_live.ex` is out-of-scope Phase 210 territory, expected to still reference it here.
    5. Confirm `accrue_admin/assets/css/app.css` has zero diff for the phase so far (`git diff --stat` against the commit this phase started from) — proving D-04's "zero edits to shared CSS rules" held.
    6. Confirm no new component file was created: `accrue_admin/lib/accrue_admin/components/work_queue_callout.ex` must not exist (COMP-01 resolves inline per D-02).

    Then, from `accrue_admin/`, re-run the same scoped Playwright capture Plan 01 used (`RATCHET_SURFACES=subscriptions,subscription-detail npx playwright test e2e/admin-visuals.spec.js`). Use the Read tool to open the freshly-captured `subscription-detail.png`/`subscription-detail-dark.png` side-by-side (mentally) against the Plan 01 baseline copies at `test-results/admin-visuals-baseline-209/{project}/subscription-detail{,-dark}.png` — confirm the detail page is visually unchanged (this is the D-04 namesake proof: since app.css has zero diff and `subscription_live.ex` was never touched, this should be a true no-op; any visible difference is a signal something leaked outside this phase's intended blast radius). Then Read the new `subscriptions.png`/`subscriptions-dark.png` against the Plan 01 baseline's `subscriptions{,-dark}.png` and confirm: exactly one verdict badge, exactly one primary CTA, the 4-stat StatStrip, no bands between header and table, and row height / rows-per-viewport / header-band height at-or-tighter than baseline (density-no-regression).
  </action>
  <acceptance_criteria>
    - `grep -c "ax-subscriptions-invoice-strip\|ax-subscriptions-queue-shortcut\|ax-subscriptions-invoice-records\|ax-subscriptions-secondary-strips\|ax-subscriptions-at-risk-strip\|ax-subscriptions-audit-strip\|ax-page-compact\|ax-subscriptions-page\|ax-page-header-compact\|ax-subscriptions-header\|ax-kpi-row\|ax-subscriptions-kpi-row\|ax-subscriptions-primary-workspace\|ax-subscriptions-webhook-workspace\|ax-subscriptions-route-line" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - `grep -c "ax-inline-worklist\|ax-audit-summary-row" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex` returns `0`.
    - `grep -rl "ax-inline-worklist" accrue_admin/lib/accrue_admin/live/ accrue_admin/lib/accrue_admin/dev/` lists exactly `subscription_live.ex` and `component_kitchen_live.ex` (not `subscriptions_live.ex`).
    - `grep -rl "ax-audit-summary-row" accrue_admin/lib/accrue_admin/live/ accrue_admin/lib/accrue_admin/dev/` lists exactly `dashboard_live.ex`, `subscription_live.ex`, and `component_kitchen_live.ex` (not `subscriptions_live.ex`).
    - `git diff --stat accrue_admin/assets/css/app.css` prints nothing.
    - `ls accrue_admin/lib/accrue_admin/components/work_queue_callout.ex` fails (file does not exist).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/accrue && grep -c "ax-inline-worklist\|ax-audit-summary-row" accrue_admin/lib/accrue_admin/live/subscriptions_live.ex; git diff --stat accrue_admin/assets/css/app.css; [ ! -f accrue_admin/lib/accrue_admin/components/work_queue_callout.ex ] && echo "COMP-01 inline confirmed"</automated>
  </verify>
  <done>Grep-gate confirms zero remaining band/override/shared-CSS-class references in `subscriptions_live.ex`, correct residual ownership by `subscription_live.ex`/`dashboard_live.ex`/`component_kitchen_live.ex`, zero `app.css` diff, and no new component file; PNG re-capture confirms the detail page is visually unbroken and the list page shows the single-verdict/single-CTA/4-stat/no-bands shape with density at-or-tighter than baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate subscriptions_live_test.exs to the post-reign shape</name>
  <files>accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs</files>
  <read_first>
    - accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs (full file, 387 lines — every line-numbered edit below refers to THIS file's current state, before this task's edits)
    - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex (post-Plan-02 state — confirm exact new copy/classes before writing assertions against them)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-RESEARCH.md (§ "Test/Selector Migration Surface" — the base migration table; § "Common Pitfalls" Pitfall 4 — scope Floki-based assertions to the specific cell, not bare `html =~` substring checks, for the identity-cell test)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-02-SUMMARY.md (exact final Copy fn names/values and cell shapes Plan 02 implemented)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (§ Copywriting Contract — the exact locked strings the rewritten assertions must check for)
  </read_first>
  <action>
    Rewrite `subscriptions_live_test.exs` test-by-test. Test "filters subscription rows and renders lifecycle-safe links" (currently L45-59): change the verdict assertion (L50-51) from the old sentence-substring check to `assert html =~ "Action required" or html =~ "Healthy"` (StatusBadge label text, no more full-sentence title); change L53's `assert html =~ "exposure"` to `assert html =~ "Exposure"` (the lowercase match today is coincidental — it hits the CSS class `ax-subscriptions-exposure` inside band 1, which is now deleted; the new StatStrip "Exposure" stat label is capitalized); leave L54 (`"target"`), L56-58 unchanged (they hit `phx-target` attrs from `DataTable` and the unchanged `lifecycle_status/1`/chip markup, none of which this phase touches).

    Test "renders Subscriptions through PageHeader with exactly one h1" (currently L82-132): keep L87-92, L101-102, L125-126, L128-131 unchanged (PageHeader internals, topbar strings, and filter-form Floki check are untouched by this phase). Apply the same verdict fix as above to L94-95 and the same "exposure"→"Exposure" fix to L97. Change L103's `"Open dedicated invoice queue"` to `"Open invoice queue"` (dropped "dedicated"). Delete L104 (`"Open recovery analytics"` — band 4a removed, and this text is NOT one of the two surviving `:actions` secondaries), L106 (`"At-risk subscription queue"` aria-label — band 4a removed), L107 (`"Open failed-delivery debugger"` — in-cell link removed). Change L108 from the hardcoded string to `assert html =~ Copy.subscriptions_kpi_section_aria_label()` (dynamic reference to whatever Plan 02 actually implemented, matching this test file's existing style of referencing `Copy.*` functions rather than hardcoding their return values, e.g. L67-68). Keep L109 (`"At-risk subscriptions"`, unchanged stat label). Change L110 from `"Open-invoice queue"` to `"Open invoices"` (renamed stat label). Delete L111 entirely (the exact `ax-kpi-row ax-subscriptions-kpi-row` class this phase drops). Keep L112-114 (already-refuted strings, still true). Delete L115 (`"Open-invoice queue records"`), L116 (already-refuted, harmless to keep or delete — keep), L117 (`"Open dedicated invoice queue records"`), L118 (`"Who did what, when?"`), L119 (`"subscription.created by Accrue system"`), L120 (`"Events: open full actor audit log"`), L121 (`"Filter admin actions"`), L122 (duplicate `"Open failed-delivery debugger"`), L123-124 (duplicate at-risk-strip strings). Keep L125-126. Add two new positive assertions after L109: `assert html =~ "Webhooks: debug failed deliveries"` and `assert html =~ "Events audit log"` (confirm the two surviving `:actions` secondaries render unchanged) and `assert html =~ "ax-status-badge"` (confirm the verdict renders via the real StatusBadge component, not hand-rolled markup) and `assert html =~ "Failed webhooks"` (new stat label).

    Test "bare subscriptions route represents the default At risk queue..." (currently L134-151): keep L141-145, L149-150 unchanged. Delete L146 (`"ax-subscription-row-state"` — the hand-rolled inline badge class this phase removes from `identity_cell/3`; the real state semantics are already covered by other assertions in this file via `lifecycle_status/1` text like "Canceling at period end") and L147-148 (`"Open failed-delivery debugger"`, `"Who did what, when?"` — both removed).

    Tests "renders filtered-empty copy…", "bare navigation push_patches…", "default queue chip clears…", "renders chip row result count…", "distinguishes populated, first-run-empty…", "loading skeleton fixture is ignored…", and "submitting the shared filter form…" (currently L61-69, 71-80, 153-184, 186-258, 260-272, 334-348): leave completely unchanged — none of their assertions touch the header, bands, or cells this phase rebuilds.

    Test "prioritizes identity, state, plan amount, time, and signals columns" (currently L274-311): apply "exposure"→"Exposure" to L279. Keep L280-281. Delete L282 (`"Open dedicated invoice queue records"`), L283 (`"Open failed-delivery debugger"`), L284 (`"Webhook status"`), L285 (`"Filter invoice queue to this subscription"`). Apply the verdict fix to L287-288. Delete L290 (`"Open audit context for this subscription"`). Keep L291-292 (chips, unchanged idiom). Delete L293 (`"Audit"`) and L294 (`"subscription.created by Accrue system"`, duplicate of the earlier deletion). Keep L295-298, L300, L302-308 (the heading-order list stays exactly as `["Customer details", "State", "Plan / amount", "Renews / ends", "Signals"]` — the substring `"Signals"` still matches the renamed `"Signals"` column label from Plan 02, so no change needed here), L310.

    Test "uses customer identity before raw subscription or processor IDs" (currently L313-330): RENAME to `"renders the identity cell as a customer-label link to the subscription and a muted subscription-id link to the customer"` (the test's original premise — raw IDs shown as labeled facts — no longer applies after the cell rebuild). Keep L321 (`subscription.customer.email` / `"phase196-primary@example.com"` still appears, now as the primary link's visible TEXT). Delete L322 (the old `"Open unified customer view: …"` prefix text is gone — the primary link's text is now JUST the customer label, no prefix). Delete L323-324 (band references, gone) and L325 (`"Customer ID"` labeled fact, gone) and L327 (`"Subscription"` labeled fact, gone). Replace L326's bare `assert html =~ subscription.customer_id` and L328's `assert html =~ subscription.processor_id` with scoped Floki assertions per RESEARCH Pitfall 4 (a bare `html =~` substring check on an ID can pass for a coincidental reason, e.g. matching inside an unrelated attribute): parse the html with `Floki.parse_document!/1`, find `a.ax-link` elements, assert at least one has text equal to the customer's label (e.g. the seeded email) AND its `href` attribute contains `"/subscriptions/#{subscription.id}"`; separately find `a.ax-label.ax-muted` elements, assert at least one has text equal to `subscription.processor_id` AND its `href` attribute contains `"/customers/#{subscription.customer_id}"`. Keep L329's `assert_before(html, "phase196-primary@example.com", subscription.processor_id)` (the primary link's text must still render before the secondary link's text in document order — this remains true and is now a meaningful check on two genuinely-present, non-coincidental strings rather than one of them being an artifact of a longer sentence).

    After all edits, run the full file and confirm 0 failures.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/subscriptions_live_test.exs` exits 0 with the same test COUNT as Plan 01 recorded (no tests silently deleted — renamed is fine, deleted-without-replacement is not, except where explicitly called for above).
    - `grep -c "ax-kpi-row ax-subscriptions-kpi-row\|Open dedicated invoice queue\|ax-subscription-row-state" accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs` returns `0`.
    - `grep -c "Open invoice queue" accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs` returns at least `1` (the renamed CTA copy is positively asserted somewhere).
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscriptions_live_test.exs</automated>
  </verify>
  <done>`subscriptions_live_test.exs` is fully green against the post-reign template; every retired-class/copy assertion is migrated (removed where the underlying content is gone, rewritten where a positive replacement exists); the identity-cell test uses scoped Floki assertions instead of bare substring checks on IDs.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild + commit both generated artifacts; full-suite green gate</name>
  <files>
    accrue_admin/priv/static/accrue_admin.css,
    examples/accrue_host/e2e/generated/copy_strings.json
  </files>
  <read_first>
    - accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex (confirms the real mix task that regenerates `priv/static/accrue_admin.css` from `assets/css/app.css` + `theme.css`)
    - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex (confirms the real mix task that regenerates `examples/accrue_host/e2e/generated/copy_strings.json` from every `AccrueAdmin.Copy.*` function)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-CONTEXT.md (§ "Established Patterns" — build contract: editing `app.css` ships nothing until rebuilt; copy changes need the export task; both artifacts committed every change)
    - .planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md (§ Design System — "Build contract" row: every CSS and copy change this phase must rebuild + commit both generated artifacts)
  </read_first>
  <action>
    From `accrue_admin/`, run `mix accrue_admin.assets.build`. Because Phase 209 made zero edits to `app.css` (confirmed by Task 1's grep-gate), expect this to produce either no diff or a purely mechanical rebuild-hash diff in `priv/static/accrue_admin.css` — check `git diff --stat accrue_admin/priv/static/accrue_admin.css`; if non-empty, stage and include it in the phase's commit (proves no drift was silently introduced); if empty, note that explicitly in the SUMMARY rather than skipping the step (the mandate is "rebuilt", not "rebuilt only if you expect a diff"). Then run `mix accrue_admin.export_copy_strings`, which WILL produce a real diff in `examples/accrue_host/e2e/generated/copy_strings.json` because Plan 02 added 6 new `Copy.Subscription` functions and changed 2 existing strings ("Open dedicated invoice queue" → "Open invoice queue", the old aria-label wording, etc.) — confirm the regenerated file contains the new CTA copy and stage it. Finally, from `accrue_admin/`, run the full `mix test` suite (not just the scoped file) to confirm no other test anywhere in the app was inadvertently broken by the Copy/CSS changes, and confirm it is green.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix accrue_admin.assets.build` exits 0.
    - `cd accrue_admin && mix accrue_admin.export_copy_strings` exits 0.
    - `grep -c "Open invoice queue" examples/accrue_host/e2e/generated/copy_strings.json` returns at least `1`.
    - `cd accrue_admin && mix test` exits 0 (full suite, zero failures).
    - Both `accrue_admin/priv/static/accrue_admin.css` (if it changed) and `examples/accrue_host/e2e/generated/copy_strings.json` are staged for commit.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.assets.build && mix accrue_admin.export_copy_strings && mix test</automated>
  </verify>
  <done>Both generated artifacts are rebuilt and (where changed) committed; the full `mix test` suite is green at the phase boundary — no red left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Generated-artifact staleness | `copy_strings.json` is consumed by e2e specs as an SSOT snapshot of admin copy; a stale snapshot silently masks copy regressions in CI (a documented past project failure mode, "CI green-up 260622"). |
| Shared CSS rule blocks | `.ax-inline-worklist*`/`.ax-audit-summary-row` are consumed by three different `.ex` files; an accidental edit here would be invisible to `subscriptions_live_test.exs` but would break `subscription_live.ex` or `component_kitchen_live.ex` silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-04 | Tampering | `.ax-inline-worklist*`/`.ax-audit-summary-row` CSS rule blocks shared with `subscription_live.ex`/`component_kitchen_live.ex` | high | mitigate | Task 1's grep-gate + `git diff --stat app.css` empty-check + detail-page PNG re-capture is the mitigation — proves zero edits and zero visual drift before this plan closes. |
| T-209-05 | Repudiation / Integrity | Generated artifacts (`accrue_admin.css`, `copy_strings.json`) drifting from source | medium | mitigate | Task 3 explicitly rebuilds and commits both artifacts every time source/copy changes, per the project's established "CI green-up" lesson (a stale `copy_strings.json` silently breaks e2e string-parity checks). |
| T-209-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this phase — no install task exists in this plan. |
</threat_model>

<verification>
Task 1's six grep/diff checks + PNG re-capture are the D-04 proof. Task 2's `mix test` on the scoped file is the REIGN-02 proof. Task 3's `mix test` (full suite) + both artifact rebuilds are the phase-closing gate. If any of Task 1's checks fail, this plan must NOT proceed to Task 2/3 until Plan 02's code is corrected.
</verification>

<success_criteria>
- D-04 grep-gate passes: zero shared-CSS edits, correct residual ownership (`subscription_live.ex` + `component_kitchen_live.ex` for `ax-inline-worklist*`; those two plus `dashboard_live.ex` for `ax-audit-summary-row`).
- Detail-page PNG visually unbroken; list-page PNG shows the single-verdict/single-CTA/4-stat/no-bands shape with density at-or-tighter than the Plan 01 baseline.
- `subscriptions_live_test.exs` fully green with intentionally-migrated assertions (not coincidentally-passing ones).
- Both generated artifacts rebuilt and committed; full `mix test` green at the phase boundary.
- COMP-01 confirmed resolved inline (no new component file).
</success_criteria>

## Artifacts This Phase Produces (this plan's contribution)

- Rewritten `accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs` (retired-class/copy assertions migrated; one test renamed to reflect the new identity-cell shape).
- Rebuilt `accrue_admin/priv/static/accrue_admin.css` (committed if it changed; explicitly confirmed either way).
- Rebuilt `examples/accrue_host/e2e/generated/copy_strings.json` (committed — contains the 6 new/changed Copy.Subscription strings from Plan 02).
- A grep-gate transcript (recorded in the SUMMARY) proving REIGN-04's Phase-211 grep-gate will find exactly the expected residual ownership when it eventually runs.

<output>
Create `.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-03-SUMMARY.md` when done
</output>

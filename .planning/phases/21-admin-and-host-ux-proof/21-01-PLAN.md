---
phase: 21-admin-and-host-ux-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/accrue_host_seed_e2e.exs
  - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "E2E fixture JSON includes backward-compatible keys plus named org and tax-invalid hints for new specs."
    - "phase13 canonical spec still passes with the extended fixture."
  artifacts:
    - path: "scripts/ci/accrue_host_seed_e2e.exs"
      provides: "Named fixture keys for two-org demo and tax-invalid path"
    - path: "examples/accrue_host/e2e/phase13-canonical-demo.spec.js"
      provides: "Regression guard for fixture shape"
  key_links:
    - from: "scripts/ci/accrue_host_seed_e2e.exs"
      to: "examples/accrue_host/e2e/global-setup.js"
      via: "ACCRUE_HOST_E2E_FIXTURE"
      pattern: "write_fixture!"
---

<objective>
Extend the deterministic E2E seed so Playwright and host proofs can exercise **two named organizations**, **org switching**, and **tax-invalid / invalid-location** hints without breaking the existing phase13 fixture contract.

Output: updated `accrue_host_seed_e2e.exs` fixture map + phase13 assertions updated only where keys are required.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
@.planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
@.planning/phases/21-admin-and-host-ux-proof/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend seed data for two-org + tax-invalid fixture keys</name>
  <read_first>
    - scripts/ci/accrue_host_seed_e2e.exs
    - examples/accrue_host/e2e/global-setup.js
    - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
    - .planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
  </read_first>
  <files>scripts/ci/accrue_host_seed_e2e.exs</files>
  <action>
    In `AccrueHostSeedE2E.run!/1`, after existing user/customer/subscription setup, add Sigra-backed **second organization** and membership for the normal user if the host app’s existing seed helpers allow; otherwise document the minimal host changes needed in the same commit. Emit these **additional** fixture keys (exact names): `org_alpha_slug`, `org_beta_slug`, `org_alpha_name`, `org_beta_name`, `tax_invalid_customer_hint` (string that downstream UI or tests can grep for a controlled invalid-tax-location scenario — use a stable test-only value like `tax_invalid_e2e_hint`), and preserve **all** existing keys (`password`, `normal_email`, `admin_email`, `webhook_id`, `subscription_id`, `first_run_webhook`, etc.). If a key cannot be populated without larger host schema work, set it to `null` in JSON **only** with a `// TODO Phase 21` comment in a companion README note — prefer populating real rows.
  </action>
  <acceptance_criteria>
    - `scripts/ci/accrue_host_seed_e2e.exs` writes JSON containing the literal substrings `org_alpha_slug` and `org_beta_slug` as map keys in `write_fixture!/2` input
    - Existing keys `password` and `normal_email` remain in the fixture map
    - `grep -n tax_invalid_customer_hint scripts/ci/accrue_host_seed_e2e.exs` returns at least one match
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix run ../../scripts/ci/accrue_host_seed_e2e.exs</automated>
  </verify>
  <done>Fixture contract extended for Phase 21 browser specs.</done>
</task>

<task type="auto">
  <name>Task 2: Keep phase13 spec compatible with extended fixture</name>
  <read_first>
    - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
    - scripts/ci/accrue_host_seed_e2e.exs
  </read_first>
  <files>examples/accrue_host/e2e/phase13-canonical-demo.spec.js</files>
  <action>Update phase13 spec only if required keys moved or new required env vars were introduced; otherwise add a single assertion that `readFixture().org_alpha_slug` is a non-empty string when present. Do not remove a11y or LiveView wait helpers.</action>
  <acceptance_criteria>
    - `examples/accrue_host/e2e/phase13-canonical-demo.spec.js` calls `readFixture()` and references `org_alpha_slug` with optional chaining or guard
    - `cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js` exits 0 (or document skip if CI lacks browsers — prefer run locally in verify block comment)
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js</automated>
  </verify>
  <done>Regression guard for canonical demo passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script -> fixture JSON | Fixture must not embed live Stripe secrets or production URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | I | `scripts/ci/accrue_host_seed_e2e.exs` | mitigate | Keep test-only emails/passwords; no `sk_live` literals; fixture path from env only |
</threat_model>

<verification>
`MIX_ENV=test mix run ../../scripts/ci/accrue_host_seed_e2e.exs` from `examples/accrue_host`; `npx playwright test e2e/phase13-canonical-demo.spec.js` from same directory.
</verification>

<success_criteria>
Named org + tax-invalid fixture keys exist; phase13 still green.
</success_criteria>

<output>
After completion, create `.planning/phases/21-admin-and-host-ux-proof/21-01-SUMMARY.md`
</output>

---
phase: 21-admin-and-host-ux-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - examples/accrue_host/e2e/support/fixture.js
  - examples/accrue_host/e2e/verify01-org-switching.spec.js
  - examples/accrue_host/e2e/verify01-tax-invalid.spec.js
  - examples/accrue_host/e2e/verify01-admin-denial.spec.js
  - examples/accrue_host/playwright.config.js
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "Multiple feature-scoped Playwright specs share helpers under e2e/support/."
    - "Chromium desktop runs full set; mobile uses grep tag @mobile when configured."
  artifacts:
    - path: "examples/accrue_host/e2e/support/fixture.js"
      provides: "Shared readFixture + reseedFixture export"
  key_links:
    - from: "examples/accrue_host/e2e/verify01-admin-denial.spec.js"
      to: "accrue_admin/lib/accrue_admin/live/customer_live.ex"
      via: "exact flash copy"
      pattern: "You don't have access to billing for this organization."
---

<objective>
Split Playwright coverage into **feature-scoped specs** with shared `e2e/support/` helpers, exercising org switching, tax-invalid messaging, and admin denial / webhook ambiguity per `21-CONTEXT.md` D-01.

Output: new specs + optional `@mobile` project in `playwright.config.js`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
@.planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
@.planning/phases/21-admin-and-host-ux-proof/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared Playwright helpers</name>
  <read_first>
    - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
    - examples/accrue_host/playwright.config.js
  </read_first>
  <files>examples/accrue_host/e2e/support/fixture.js, examples/accrue_host/e2e/phase13-canonical-demo.spec.js</files>
  <action>
    Create `examples/accrue_host/e2e/support/fixture.js` exporting `readFixture`, `reseedFixture`, `login`, `waitForLiveView` copied from phase13 with **no behavior change**.
    Update `phase13-canonical-demo.spec.js` to import from `./support/fixture.js` (adjust relative path). Keep default export or named exports consistent with Node resolution used by Playwright.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/e2e/support/fixture.js` exists and contains the string `readFixture`
    - `examples/accrue_host/e2e/phase13-canonical-demo.spec.js` contains `support/fixture`
    - `cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js</automated>
  </verify>
  <done>Shared support module extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Add verify01-org-switching spec</name>
  <read_first>
    - examples/accrue_host/e2e/support/fixture.js
    - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
    - .planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/e2e/verify01-org-switching.spec.js</files>
  <action>
    New spec: login as `normal_email`, assert host UI exposes org switcher or organization selector, switch `org_alpha_slug` -> `org_beta_slug` (use fixture keys from Plan 21-01), assert billing chrome updates (e.g. text includes `org_beta_name`). Use `waitForLiveView` after each navigation. Call `reseedFixture` only if the flow mutates subscriptions.
  </action>
  <acceptance_criteria>
    - File `examples/accrue_host/e2e/verify01-org-switching.spec.js` exists
    - File references `org_alpha_slug` and `org_beta_slug` as fixture properties
    - `grep -l verify01-org-switching examples/accrue_host/e2e/verify01-org-switching.spec.js` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test e2e/verify01-org-switching.spec.js</automated>
  </verify>
  <done>Org switching covered in browser.</done>
</task>

<task type="auto">
  <name>Task 3: Add verify01-tax-invalid + verify01-admin-denial specs</name>
  <read_first>
    - .planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
    - examples/accrue_host/e2e/support/fixture.js
  </read_first>
  <files>examples/accrue_host/e2e/verify01-tax-invalid.spec.js, examples/accrue_host/e2e/verify01-admin-denial.spec.js</files>
  <action>
    **tax-invalid:** Navigate host billing path that surfaces invalid tax location; assert visible text includes `Tax location needs attention` (exact from UI-SPEC) OR the stable `tax_invalid_customer_hint` fixture string if UI not yet wired — prefer UI-SPEC string when host copy exists.
    **admin-denial:** Log in as `admin_email`, open an admin URL deliberately targeting out-of-scope org (construct from fixture `org_alpha_slug` vs `org_beta_slug` per test data), expect flash or banner text exactly `You don't have access to billing for this organization.`
  </action>
  <acceptance_criteria>
    - `verify01-tax-invalid.spec.js` contains `Tax location needs attention` OR `tax_invalid_customer_hint`
    - `verify01-admin-denial.spec.js` contains the exact substring `You don't have access to billing for this organization.`
    - `cd examples/accrue_host && npx playwright test e2e/verify01-tax-invalid.spec.js e2e/verify01-admin-denial.spec.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test e2e/verify01-tax-invalid.spec.js e2e/verify01-admin-denial.spec.js</automated>
  </verify>
  <done>Tax-invalid and admin denial browser proofs exist.</done>
</task>

<task type="auto">
  <name>Task 4: Configure optional @mobile project</name>
  <read_first>
    - examples/accrue_host/playwright.config.js
    - .planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/playwright.config.js</files>
  <action>Add a second project (e.g. `Mobile Chrome`) or `grep: /@mobile/` on a duplicate Chromium project so CI can run `npx playwright test --grep-invert @mobile` for default PR and scheduled full matrix documented in `examples/accrue_host/README.md` in Plan 21-06 if not here. Minimum: document `npx playwright test --grep @mobile` in a comment in playwright.config.js and add `@mobile` to one non-critical test.</action>
  <acceptance_criteria>
    - `examples/accrue_host/playwright.config.js` contains the substring `@mobile`
    - Default `npx playwright test` without grep still runs verify01 specs added in this plan
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test</automated>
  </verify>
  <done>Mobile tagging hook exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright -> host app | Tests use seeded credentials only; no live Stripe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | I | `verify01-admin-denial.spec.js` | mitigate | Assert denial copy; do not assert partial customer ids on denied routes |
</threat_model>

<verification>
`cd examples/accrue_host && npx playwright test`
</verification>

<success_criteria>
Feature-scoped Playwright specs green; shared support in place.
</success_criteria>

<output>
After completion, create `.planning/phases/21-admin-and-host-ux-proof/21-02-SUMMARY.md`
</output>

---
phase: 21-admin-and-host-ux-proof
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/billing_presentation.ex
  - accrue_admin/test/accrue_admin/billing_presentation_test.exs
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "One module maps owner + tax fields to list/detail signals without duplicating logic in templates."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/billing_presentation.ex"
      provides: "Ownership class + tax_health tri-state"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/billing_presentation.ex"
      to: "accrue_admin/lib/accrue_admin/live/customers_live.ex"
      via: "assign_row_metadata"
      pattern: "tax_health"
---

<objective>
Introduce a **single** classification module for admin money surfaces: `ownership_class` (`:user | :org`) and `tax_health` (`:off | :active | :invalid_or_blocked`) derived from existing projections and tax-risk fields.

Output: `AccrueAdmin.BillingPresentation` (name may be adjusted to match codebase naming) with pure functions and ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
@.planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
@.planning/phases/20-organization-billing-with-sigra/20-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BillingPresentation pure functions + tests</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/queries/customers.ex
    - accrue_admin/lib/accrue_admin/queries/subscriptions.ex
    - .planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
    - .planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/billing_presentation.ex, accrue_admin/test/accrue_admin/billing_presentation_test.exs</files>
  <action>
    Create `accrue_admin/lib/accrue_admin/billing_presentation.ex` with at least:
    - `ownership_label/1` or `ownership_class/1` accepting a map or struct with `owner_type` string
    - `tax_health/1` accepting a map with tax-risk / Stripe tax fields already loaded on admin rows (reuse field names from Phase 19 tax-risk assigns — inspect `invoice_live.ex` / query modules for exact keys)
    Return atoms `:off`, `:active`, `:invalid_or_blocked` only (no boolean “tax on”).
    Add `accrue_admin/test/accrue_admin/billing_presentation_test.exs` with **table-driven** cases proving `:invalid_or_blocked` wins when both active subscription tax and invalid risk flags appear.
  </action>
  <acceptance_criteria>
    - File `accrue_admin/lib/accrue_admin/billing_presentation.ex` exists and defines `def tax_health`
    - File `accrue_admin/test/accrue_admin/billing_presentation_test.exs` exists and contains at least three test blocks or describe cases for the three tax_health states
    - `cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/billing_presentation_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/billing_presentation_test.exs</automated>
  </verify>
  <done>Classifier implemented and unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row maps -> presentation | Pure functions must not fetch Repo; callers supply already-scoped data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | I | `billing_presentation.ex` | mitigate | No side effects; tests lock tri-state precedence to prevent misleading operators |
</threat_model>

<verification>
`cd accrue_admin && mix test --warnings-as-errors test/accrue_admin/billing_presentation_test.exs`
</verification>

<success_criteria>
Single source of truth for list/detail badges exists with tests.
</success_criteria>

<output>
After completion, create `.planning/phases/21-admin-and-host-ux-proof/21-03-SUMMARY.md`
</output>

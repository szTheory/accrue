---
phase: 21-admin-and-host-ux-proof
plan: 06
type: execute
wave: 4
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - examples/accrue_host/README.md
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
  - examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "README documents one-command VERIFY-01 path and Fake-only default."
    - "Host integration tests prove billing flows only through AccrueHost.Billing facade."
  artifacts:
    - path: "examples/accrue_host/README.md"
      provides: "VERIFY checklist"
  key_links:
    - from: "examples/accrue_host/test/accrue_host/billing_facade_test.exs"
      to: "examples/accrue_host/lib/accrue_host/billing.ex"
      via: "public API"
      pattern: "AccrueHost.Billing"
---

<objective>
Document the **canonical local VERIFY-01 path** for host evaluators and add **integration tests** that prove org billing and tax-boundary behavior through `AccrueHost.Billing` (no direct Accrue `Repo` queries from tests for behaviors this phase owns).

Output: README section + focused `mix test` modules.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README VERIFY-01 checklist + commands</name>
  <read_first>
    - examples/accrue_host/README.md
    - .planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <action>
    Add section `## VERIFY-01 (Phase 21)` with numbered checklist: (1) `MIX_ENV=test mix run ../../scripts/ci/accrue_host_seed_e2e.exs`, (2) `MIX_ENV=test mix test --warnings-as-errors`, (3) `npx playwright test`, (4) note Fake-backed default and advisory live Stripe. Include exact commands from repo root vs `examples/accrue_host` cwd — be explicit: `cd examples/accrue_host` prefix for each.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/README.md` contains the heading `VERIFY-01`
    - Same file contains the substring `npx playwright test`
    - Same file contains the substring `accrue_host_seed_e2e.exs`
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'VERIFY-01' examples/accrue_host/README.md && grep -n 'npx playwright test' examples/accrue_host/README.md</automated>
  </verify>
  <done>Evaluator path documented.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AccrueHost.Billing facade tests for org scope</name>
  <read_first>
    - examples/accrue_host/lib/accrue_host/billing.ex
    - examples/accrue_host/test/accrue_host/billing_facade_test.exs
    - examples/accrue_host/test/support/conn_case.ex
    - .planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/test/accrue_host/billing_facade_test.exs</files>
  <action>
    Extend `test/accrue_host/billing_facade_test.exs` to assert public `AccrueHost.Billing` functions return `{:error, _}` or safe empty results when active organization does not match row owner (choose concrete function names after reading `billing.ex`). Reuse `AccrueHost.AccrueCase` and **Fake** processor — no network.
  </action>
  <acceptance_criteria>
    - `examples/accrue_host/test/accrue_host/billing_facade_test.exs` references module `AccrueHost.Billing`
    - File adds at least one new `test` block asserting `{:error` or error atom for wrong-org scenario
    - `cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <done>Facade-level proofs extended.</done>
</task>

<task type="auto">
  <name>Task 3: Host subscription LiveView copy assertions</name>
  <read_first>
    - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
    - examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs
    - .planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
  </read_first>
  <files>examples/accrue_host/test/accrue_host_web/live/subscription_live_test.exs</files>
  <action>
    Add LiveViewTest cases proving the billing page renders the substring `AccrueHost.Billing` in educational copy (already present per grep) and asserts org-switching helper copy per CONTEXT D-03 (`Organization billing state` or equivalent) remains visible — grep-fixture-level assertion only if full flow is too heavy.
  </action>
  <acceptance_criteria>
    - `subscription_live_test.exs` contains `AccrueHost.Billing`
    - `cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host_web/live/subscription_live_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host_web/live/subscription_live_test.exs</automated>
  </verify>
  <done>Host LiveView documents facade in tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> operator | README must not instruct storing `sk_live` in `.env` for VERIFY-01 default path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | I | README | mitigate | Document Fake-first; live Stripe labeled advisory only |
</threat_model>

<verification>
`cd examples/accrue_host && MIX_ENV=test mix test --warnings-as-errors test/accrue_host/billing_facade_test.exs test/accrue_host_web/live/subscription_live_test.exs`
</verification>

<success_criteria>
README VERIFY path + host integration tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/21-admin-and-host-ux-proof/21-06-SUMMARY.md`
</output>

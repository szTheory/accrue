---
phase: 210-reign-home-certify-answer-first-ia-copy-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - examples/accrue_host/e2e/generated/copy_strings.json
autonomous: true
requirements: [COPY-01, COPY-02]
must_haves:
  truths:
    - "Every operator-facing string the reigned Home page will render is a def in AccrueAdmin.Copy (no inline template literals remain to be authored in dashboard_live.ex)"
    - "Home's verdict badge language is 'Healthy' / 'Action required' (same literals as Subscriptions), not the bespoke 'Unhealthy'"
    - "No surviving operator string on Home contains the word 'workspace'"
    - "copy_strings.json is regenerated from the Copy module and reflects every add/rename"
  artifacts:
    - "accrue_admin/lib/accrue_admin/copy.ex (dashboard_health_verdict_*, home_attention_priority_heading, home_customer_search_cta added; workspace jargon removed)"
    - "examples/accrue_host/e2e/generated/copy_strings.json (rebuilt via mix accrue_admin.export_copy_strings)"
  key_links:
    - "dashboard_live.ex (Plan 02) references the new Copy fns — the JSON regen must precede host-e2e reading copy_strings.json as committed"
---

<objective>
Land the plain-language copy pass for the Home reign in AccrueAdmin.Copy so that Plan 02's dashboard_live.ex recomposition can source every operator string from Copy (COPY-01) and so breadcrumb/verdict/customer-lookup wording is correct and discoverable (COPY-02). This is the dependency root: dashboard_live.ex (Plan 02) and the migrated unit test (Plan 03) both reference the Copy fns added here.

Purpose: COPY-01 (plain-language, sourced from Copy, no inline literals) + COPY-02 (real-parent breadcrumbs, discoverable customer lookup) become physically possible only once these strings exist. Verdict-language parity with Subscriptions (Phase 209) is the cross-page through-line.
Output: Updated copy.ex + regenerated, committed copy_strings.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-CONTEXT.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-PATTERNS.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-UI-SPEC.md
@accrue_admin/lib/accrue_admin/copy.ex
@accrue_admin/lib/accrue_admin/copy/subscription.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Home reign Copy strings + de-jargon existing strings</name>
  <read_first>
    - 210-PATTERNS.md § "copy.ex" (the "Inline literals … that MUST migrate" list + "Jargon audit" list — authoritative string inventory)
    - 210-UI-SPEC.md § "Copywriting Contract" (the per-element copy table — authoritative final wording)
    - accrue_admin/lib/accrue_admin/copy.ex L1229–1367 (existing dashboard_* / home_* fns to add near / rename in place)
    - accrue_admin/lib/accrue_admin/copy/subscription.ex L101–112 (verdict literals to reuse verbatim: "Healthy", "Action required", "Open invoice queue")
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex</files>
  <action>
    Add these NEW Copy defs (reusing the exact literals from copy/subscription.ex so the two pages read identically — per COPY-01/D-04 and 210-UI-SPEC Copywriting Contract):
    - dashboard_health_verdict_healthy/0 → "Healthy"
    - dashboard_health_verdict_action_required/0 → "Action required"
    - home_attention_priority_heading/0 → "Priority exceptions" (migrates the inline literal at dashboard_live.ex L471 per PATTERNS §8; the dynamic attention_rail_heading/1 keeps calling dashboard_display_headline/0 for the empty branch, unchanged)
    - home_customer_search_cta/0 → "Find a customer" (sentence case; this is the single header customer-lookup control label per D-02. Keep the existing home_search_customers_title/0 = "Find one customer" untouched — PATTERNS L182 notes they are deliberately distinct)
    Rename / de-jargon these EXISTING defs (COPY-01 "no workspace jargon", D-04 rule 1) — change only the returned string literal, keep the fn name/arity so no caller breaks:
    - home_launcher_invoices_title/0: "Invoices queue: $0.00 target" → "Invoice queue" (short noun, per UI-SPEC Tile 1)
    - home_launcher_recovery_meta/0: "Recovery workspace" → drop "workspace" (UI-SPEC Tile 2 frames this as at-risk-account count; if the recomposed tile renders the count dynamically instead, still de-jargon the literal so the SSOT holds no "workspace")
    - dashboard_kpi_invoices_aria_label/0 (L1279): "Open invoice queue workspace" → "Open invoice queue"
    - dashboard_kpi_open_invoice_balance_meta/0 (L1257): remove the "…Invoices queue workspace…" jargon phrasing, plain-language rewrite that keeps the same meaning
    Do NOT create Copy defs for the strings inside blocks being DELETED in Plan 02 (the ax-home-header-health block, the ax-attention-summary "Billing status: Unhealthy." + P1/P2/P3 sentence, the customer-search strip "Open customer search", the customer launcher tile "Open global customer search") — those are retired, not migrated (PATTERNS §"Inline literals").
    Sentence-case rule: audit any newly added string for SHOUTING and use sentence case ("Find ONE customer" is retired, replaced by "Find a customer"; no all-caps words). No double-negative healthy-state phrasing.
    Keep every def public (module `def`s — no unused-warning risk even though callers land in Plan 02).
  </action>
  <verify>
    <automated>cd accrue_admin && grep -nE 'workspace' lib/accrue_admin/copy.ex | grep -viE '^#|storybook' ; test -z "$(grep -nE '"[^"]*workspace[^"]*"' lib/accrue_admin/copy.ex | grep -iE 'invoice|recovery|home_|dashboard_')" && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dashboard_health_verdict_healthy/0, dashboard_health_verdict_action_required/0, home_attention_priority_heading/0, home_customer_search_cta/0 exist and return the specified literals.
    - home_launcher_invoices_title, home_launcher_recovery_meta, dashboard_kpi_invoices_aria_label, dashboard_kpi_open_invoice_balance_meta no longer contain "workspace".
    - copy.ex compiles with --warnings-as-errors.
    - No SHOUTING (all-caps) word in any added/renamed Home string.
  </acceptance_criteria>
  <done>The four new verdict/heading/customer-search Copy defs exist with the specified literals, and the four de-jargoned defs no longer contain "workspace"; copy.ex compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and commit copy_strings.json</name>
  <read_first>
    - 210-PATTERNS.md § "Build contract (generated artifacts)" (mix accrue_admin.export_copy_strings is mandatory on every Copy change; copy_strings.json is read AS-COMMITTED by host playwright shards)
    - MEMORY note "CI green-up 260622" context: committed copy_strings.json is regenerated by host-integration but read as-committed by playwright-e2e shards — stale = silent e2e failure
  </read_first>
  <files>examples/accrue_host/e2e/generated/copy_strings.json</files>
  <action>
    Run `mix accrue_admin.export_copy_strings` (from the accrue_admin project or per the task's documented invocation) to regenerate examples/accrue_host/e2e/generated/copy_strings.json from the updated Copy module. Do NOT hand-edit the JSON. Confirm the regenerated JSON reflects the renamed strings (no "workspace" in the invoice/recovery/aria entries) and the new verdict/customer-search strings are present. Stage both copy.ex and the regenerated copy_strings.json for commit in this plan's atomic commit.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && git diff HEAD --name-only | grep -E 'copy_strings.json' && ! grep -iE 'invoice queue workspace|recovery workspace' examples/accrue_host/e2e/generated/copy_strings.json</automated>
  </verify>
  <acceptance_criteria>
    - copy_strings.json shows as changed in git and was produced by the mix task (not hand-edited).
    - No "invoice queue workspace" / "recovery workspace" strings remain in copy_strings.json.
    - "Find a customer", "Healthy", "Action required", "Priority exceptions" resolve through Copy and appear in the regenerated JSON.
  </acceptance_criteria>
  <done>copy_strings.json is regenerated by the mix task (shows as changed in git), contains the new strings, and contains no "workspace" jargon.</done>
</task>

</tasks>

## Artifacts this plan produces

**New `AccrueAdmin.Copy` string keys:**
- `dashboard_health_verdict_healthy/0` → "Healthy"
- `dashboard_health_verdict_action_required/0` → "Action required"
- `home_attention_priority_heading/0` → "Priority exceptions"
- `home_customer_search_cta/0` → "Find a customer"

**Renamed string values (fn names unchanged):** `home_launcher_invoices_title/0`, `home_launcher_recovery_meta/0`, `dashboard_kpi_invoices_aria_label/0`, `dashboard_kpi_open_invoice_balance_meta/0` (all de-jargoned — "workspace" removed).

**Regenerated artifact:** `examples/accrue_host/e2e/generated/copy_strings.json`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan edits a compile-time string module + a generated test fixture. No runtime trust boundary is crossed, no user input is handled, no data leaves the process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-210-01 | Information Disclosure | Copy strings | low | accept | Strings are static operator-facing labels; no PII, secrets, or Stripe fields are introduced. Confirmed by review of added literals. |
| T-210-SC | Tampering | dependency installs | low | accept | No package installs in this phase (no npm/pip/cargo); no supply-chain surface. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean in accrue_admin.
- `grep` confirms zero surviving "workspace" jargon in the four de-jargoned Home strings.
- copy_strings.json regenerated by the mix task (not hand-edited) and staged.
</verification>

<success_criteria>
All Home reign strings live in AccrueAdmin.Copy with correct plain-language / verdict-parity wording, and the committed copy_strings.json reflects them. Plan 02 can now source every Home operator string from Copy.
</success_criteria>

<output>
Create `.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-01-SUMMARY.md` when done.
</output>

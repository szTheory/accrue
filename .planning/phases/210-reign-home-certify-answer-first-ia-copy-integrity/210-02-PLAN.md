---
phase: 210-reign-home-certify-answer-first-ia-copy-integrity
plan: 02
type: execute
wave: 2
depends_on: ["210-01"]
files_modified:
  - accrue_admin/lib/accrue_admin/live/dashboard_live.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/accrue_admin.css
autonomous: true
requirements: [REIGN-03, IA-01, IA-02, IA-04, COPY-02]
must_haves:
  truths:
    - "Home renders its header via PageHeader.page_header (breadcrumbs + title + :description + :actions + :stat_strip), not a hand-rolled <header class='ax-page-header'>"
    - "Home shows exactly ONE billing-health verdict — a single StatusBadge in :description plus a 4-stat exposure-first StatStrip — with the h1 verdict sentence, ax-home-header-health block, and ax-attention-summary block all removed"
    - "Home offers exactly ONE customer-lookup control (a command-palette trigger in :actions); the ax-home-customer-search-strip section and the ax-launcher-customer tile are removed"
    - "The launcher grid renders THREE tiles (Invoice queue / Dunning funnel / Developer-webhooks), each composed from .ax-card + Icon + StatusBadge/chip + Button — the customer tile is gone and this is CORRECT per D-02a (not a regression)"
    - "The attention rail rows and empty branch are rebuilt from StatusBadge + .ax-stack-xs + .ax-chip + EmptyState; the failed_meter_event_count P4 row survives (content-preservation)"
    - "Answer-first order holds: verdict+StatStrip → primary action → attention rail → launchers → KpiCard band (kept) → Timeline (kept)"
    - "Breadcrumb is a single self-referential-free crumb [Dashboard] with no href and no fake parent"
    - "No datum from the three removed verdict renderings is lost — each lands in the StatStrip or the retained rail rows / KpiCard band"
  artifacts:
    - "accrue_admin/lib/accrue_admin/live/dashboard_live.ex (recomposed onto the canonical spine)"
    - "accrue_admin/priv/static/accrue_admin.css (rebuilt ONLY if app.css was touched)"
  key_links:
    - "PageHeader/StatStrip/StatusBadge/EmptyState/FlashGroup component APIs (unchanged) consumed from dashboard_live.ex"
    - "New verdict/tone helpers read Home's @stats keys (open_invoice_count, open_invoice_balance_minor, past_due_subscription_count, blocked_webhook_count)"
    - "Stable markers data-ax-health-verdict / data-ax-launcher-primary / data-ax-command-palette-trigger consumed by Plan 03 gate migration"
---

<objective>
Recompose the Home page (dashboard_live.ex) onto the canonical shared-component spine — the core REIGN deliverable. Replace the hand-rolled header with PageHeader; collapse the triplicated verdict to one StatusBadge + exposure-first StatStrip (IA-01); collapse the triplicated customer-search to one command-palette trigger in :actions (IA-02); rebuild the attention rail + launcher tiles from shared primitives (REIGN-03); keep the KpiCard "At a glance" band and Timeline activity cards untouched; preserve answer-first order (IA-04) and real-parent breadcrumbs (COPY-02).

Purpose: Home is the last outlier page; reigning it makes the whole admin read as one operator-first system with cross-page parity to Phase 209's reigned Subscriptions.
Output: Recomposed dashboard_live.ex; rebuilt accrue_admin.css bundle only if app.css needed an additive edit.

**This is a REIGN, not a redesign.** Every construct copies from the sibling `subscriptions_live.ex`. Zero new shared components. No new tokens/palette/deps/Tailwind. CSS is stop-referencing-not-delete — do NOT delete any `.ax-home-*`/`.ax-launcher*`/`.ax-attention*`/`.ax-health-summary*` rule (that is Phase 211). Density is the point — do not over-air.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-CONTEXT.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-PATTERNS.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-UI-SPEC.md
@.planning/phases/209-reign-subscriptions-list-detail-css-coordination/209-UI-SPEC.md
@accrue_admin/lib/accrue_admin/live/dashboard_live.ex
@accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
@accrue_admin/lib/accrue_admin/components/page_header.ex
@accrue_admin/lib/accrue_admin/components/stat_strip.ex
@accrue_admin/lib/accrue_admin/components/status_badge.ex
@accrue_admin/lib/accrue_admin/components/empty_state.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt PageHeader — verdict badge, exposure-first StatStrip, single-action header, FlashGroup</name>
  <read_first>
    - subscriptions_live.ex L13–23 (alias block shape), L98–190 (PageHeader wiring — MIRROR this), L345–358 (verdict/tone helpers), L494–501 (flash_messages/1)
    - 210-PATTERNS.md §1 (alias deltas), §2 (PageHeader Home-specific deltas), §3 (verdict helpers + the webhook_tone/1 ARITY COLLISION caveat)
    - 210-UI-SPEC.md § "Copywriting Contract" (title/breadcrumbs/verdict/StatStrip/primary-CTA/customer-lookup rows) + Interaction Contract §1
    - dashboard_live.ex L1–104 (current header being replaced), L461–471 (helpers to retire), L595–606 (format_minor — reuse), L473–474 (count/2 — reuse)
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex</files>
  <action>
    Update the alias block: add PageHeader, StatStrip, StatusBadge, EmptyState, FlashGroup; drop Breadcrumbs (PageHeader renders breadcrumbs internally); keep AppShell, Icon, KpiCard, Timeline, Copy, ScopedPath (per PATTERNS §1).
    Replace the hand-rolled `<header class="ax-page-header ax-page-header-compact">` (L52–104) with `PageHeader.page_header` mirroring subscriptions_live.ex L98–187, with these Home deltas:
    - breadcrumbs = single crumb `[%{label: Copy.dashboard_breadcrumb_home()}]` — NO href (Home is root; D-04a/COPY-02). Do not add a parent crumb.
    - title = Copy.dashboard_breadcrumb_home() ("Dashboard") — plain noun, NOT a verdict sentence.
    - :description = a `<p>` wrapping a single `StatusBadge.status_badge` (status/label/tone from the new verdict helpers) + one `<p class="ax-body">` sub-line (reuse Copy.home_intro_copy/0 as the route line, mirroring Subscriptions' badge+route-line shape). This is the ONE verdict (D-03: StatusBadge + exposure-first StatStrip, mirroring Subscriptions D-03 for cross-page parity) — the h1 sentence, ax-home-header-health block, and ax-attention-summary block are all removed. Add a stable marker `data-ax-health-verdict="true"` on the StatusBadge's wrapper element so Plan 03 can assert the single verdict.
    - :actions = (1) primary `.ax-button ax-button-primary ax-button-sm` "Open invoice queue" → /invoices?status=open (drop the "collect $X" suffix — the amount now lives in the Exposure stat); (2) a `<button type="button" class="ax-button ax-button-secondary ax-button-sm">` labelled Copy.home_customer_search_cta() ("Find a customer") carrying BOTH data-command-palette-trigger="true" and data-ax-command-palette-trigger="true" (carry the stable marker from current L84); (3) secondary `.ax-button-secondary` "Audit ledger" → /events. DROP "Debug dead-lettered webhooks" and "Dunning after invoices" from :actions (they duplicate launcher tiles — UI-SPEC "Header secondary links (dropped)").
    - :stat_strip (D-03a — the header StatStrip is the one-line exposure-first answer; the retained KpiCard "At a glance" band is the metric drill-down; they must not restate the same number as the same sentence, and every datum from the three removed verdict renderings must land here or in the retained rail rows — content-preservation) = a `StatStrip.stat_strip` (label Copy.dashboard_kpi_section_aria_label()) with 4 :stat slots in Subscriptions order, sourced from Home's @stats: (1) "Open invoices" value count(@stats.open_invoice_count,"invoice") tone "cobalt" → /invoices?status=open; (2) "Exposure" value format_minor(@stats.open_invoice_balance_minor,"usd") tone exposure_tone(@stats) → /invoices?status=open; (3) "At-risk subscriptions" value count(@stats.past_due_subscription_count,"subscription") tone "amber" → /analytics/recovery; (4) "Failed webhooks" value count(@stats.blocked_webhook_count,"failed webhook") tone stat_webhook_tone(@stats) → /webhooks?status=failed,dead. No :filter_toolbar slot (Home has no table). Use existing ScopedPath.build/4 for every href.
    Add `<FlashGroup.flash_group flashes={flash_messages(@flash)} />` immediately after the PageHeader (mirror subscriptions_live.ex L189) and copy the flash_messages/1 helper from subscriptions_live.ex L494–501.
    Add the verdict/tone helpers adapted to Home's @stats keys: verdict_status/1 matching `%{open_invoice_count: c} when c > 0 -> :action_required` else `:healthy`; verdict_label/1 → Copy.dashboard_health_verdict_action_required()/healthy(); verdict_tone/1 → "amber"/"moss"; exposure_tone/1 matching `%{open_invoice_balance_minor: m} when m > 0 -> "amber"` else "moss". Name the StatStrip webhook helper `stat_webhook_tone/1` (matching `%{blocked_webhook_count: c} when c > 0 -> "amber"` else "moss") — do NOT reuse the name webhook_tone, which already exists at L567–570 for timeline tones (PATTERNS §3 arity-collision caveat).
    Delete the now-dead helpers: dashboard_health_headline/1 (L467–468), attention_health_summary/1 (L461), attention_health_issue_summary/1 (L463–465). Retire the `<h1 class="ax-display">` verdict sentence and the ax-dashboard-title-row / ax-home-header-health markup (removed with the header). Do not delete any CSS rule.
    Verdict badge language MUST be "Healthy"/"Action required" — the bespoke "Unhealthy" wording is gone.
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -8 && grep -c 'PageHeader.page_header' lib/accrue_admin/live/dashboard_live.ex && test -z "$(grep -nE 'dashboard_health_headline|attention_health_summary|attention_health_issue_summary|ax-home-header-health|Billing health: Unhealthy' lib/accrue_admin/live/dashboard_live.ex)"</automated>
  </verify>
  <acceptance_criteria>
    - dashboard_live.ex compiles with --warnings-as-errors.
    - Exactly one PageHeader.page_header; zero references to dashboard_health_headline / attention_health_summary / attention_health_issue_summary / ax-home-header-health / "Billing health: Unhealthy".
    - :description carries one StatusBadge with data-ax-health-verdict marker; :actions has exactly one primary CTA + one customer-search trigger (data-ax-command-palette-trigger) + Audit ledger; StatStrip has 4 stats in the specified order/tone.
    - stat_webhook_tone/1 exists and the pre-existing webhook_tone/1 (timeline) is untouched.
  </acceptance_criteria>
  <done>Home's header is a single PageHeader with one StatusBadge verdict + 4-stat StatStrip + single primary CTA + one customer-search trigger + Audit ledger; the three dead verdict helpers and the h1 verdict sentence are gone; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild attention rail from primitives + EmptyState; drop the verdict-summary block and the customer-search strip</name>
  <read_first>
    - dashboard_live.ex L106–159 (attention rail section, ax-attention-summary block, customer-search strip), L414–459 (attention_items/3 — KEPT data pipeline)
    - 210-PATTERNS.md §4 (rail recomposition + tone mapping), §5 (EmptyState + selector-migration caution for .ax-attention-rail--empty)
    - 210-UI-SPEC.md Interaction Contract §2 and §3; Copywriting Contract rows "Attention-rail zone heading / empty state"
    - status_badge.ex L28–39 (tone scale moss/cobalt/amber/slate/ink) and empty_state.ex L19–24 (icon/title/body/class + :actions slot)
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex</files>
  <action>
    Keep the attention-rail `<section … data-ax-zone="attention-rail">` and its `@attention` pipeline (attention_items/3 unchanged — it still yields P1→P4 rows incl. the failed_meter_event_count P4 row, which MUST survive for content-preservation). Source the zone heading literal from Copy: attention_rail_heading/1 non-empty branch returns Copy.home_attention_priority_heading() (added in Plan 01) instead of the inline "Priority exceptions"; the empty branch keeps Copy.dashboard_display_headline().
    DELETE the `ax-attention-summary` verdict block (L119–122) — its content is now the header StatusBadge/StatStrip.
    Rebuild each rail row (currently L124–136 with bespoke ax-attention-priority-*/ax-attention-dot-*/ax-attention-pill-*/ax-attention-action spans) from shared primitives: a StatusBadge for the priority chip (its built-in .ax-status-dot replaces the bespoke dot) with tone mapped from row.tone via a small helper attention_status_badge_tone/1 ("warning"→"amber", "danger"→"amber", "info"→"cobalt"); a `<span class="ax-stack-xs">` for the metric+label; an optional `<span class="ax-chip ax-label">` for row.pill; and a quiet trailing link/small Button for row.action. Keep the whole-row `<a href={row.href}>` navigation (parity with 209 D-01 whole-row-nav; no per-row action button). Preserve the section's aria-label/landmark semantics.
    Replace the bespoke empty branch (L138–142, `.ax-card.ax-empty.ax-attention-rail--empty`) with `EmptyState.empty_state icon={:check_circle} title={Copy.home_attention_empty_title()} body={Copy.home_attention_empty_copy()} class="ax-attention-rail--empty"` — pass class="ax-attention-rail--empty" to preserve the intentional stable hook the phase194 e2e locates (PATTERNS §5); this keeps the empty hero non-interactive (no role=button — EmptyState renders a static div, satisfying the D-06 guard).
    DELETE the entire customer-search strip `<section class="ax-home-section ax-home-customer-search-strip">` (L145–159) — the customer-lookup control now lives only in the header :actions (D-02/IA-02). Do not migrate its "Open customer search" literal.
    Do not delete any CSS rule.
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -6 && test -z "$(grep -nE 'ax-attention-summary|ax-home-customer-search-strip|Billing status: Unhealthy|Find ONE customer|Open customer search' lib/accrue_admin/live/dashboard_live.ex)" && grep -c 'EmptyState.empty_state' lib/accrue_admin/live/dashboard_live.ex && grep -c 'ax-attention-rail--empty' lib/accrue_admin/live/dashboard_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - Zero references to ax-attention-summary, ax-home-customer-search-strip, "Billing status: Unhealthy", "Find ONE customer", "Open customer search" in dashboard_live.ex.
    - Rail rows render via StatusBadge + .ax-stack-xs + optional .ax-chip; the P4 failed_meter_event_count row still renders (attention_items/3 unchanged).
    - Empty branch renders via EmptyState with class="ax-attention-rail--empty" preserved and no role=button.
    - Compiles with --warnings-as-errors.
  </acceptance_criteria>
  <done>The attention rail rows + empty branch are rebuilt from StatusBadge/.ax-stack-xs/.ax-chip/EmptyState with the P4 meter row preserved; the ax-attention-summary block and the customer-search strip are removed; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the three-tile launcher grid from .ax-card primitives; remove the customer tile; rebuild bundle if app.css changed</name>
  <read_first>
    - dashboard_live.ex L162–221 (launcher grid — 4 tiles), L476–481 (invoice_launcher_title/1 to retire)
    - 210-PATTERNS.md §6 (three-tile rebuild + stable data-ax-launcher-primary marker + "do NOT widen tile padding")
    - 210-UI-SPEC.md Interaction Contract §4 + Copywriting Contract tiles 1/2/3 + Spacing "Exceptions" (reuse existing .ax-launchers gap; no widened padding to fill the freed 4th column)
    - accrue_admin/assets/css/app.css L6078–6100 (.ax-launchers / .ax-launcher grid) and L6300–6325 (breakpoints: repeat(2,minmax(0,1fr)) with .ax-launcher-primary span 2) — decide whether the card-based grid needs an additive rule
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex, accrue_admin/assets/css/app.css, accrue_admin/priv/static/accrue_admin.css</files>
  <action>
    Rebuild the launcher zone to render exactly THREE tiles (Invoice queue / Dunning funnel / Developer-webhooks). REMOVE the `ax-launcher-customer` button tile (L179–191) entirely — customer lookup is header-only now (D-02). Three tiles is CORRECT and intended per D-02a — do NOT keep a fourth tile and do NOT treat the count drop as a regression.
    Rebuild each surviving tile from `.ax-card` + Icon (size lg) + a title + a StatusBadge/`.ax-chip` meta (rendered only when the underlying count > 0, matching today's `:if` conditionals) + a Button-styled action link — replacing the bespoke ax-launcher-*/ax-launcher-health-*/ax-launcher-meta-* ad-hoc spans. Copy per UI-SPEC: Tile 1 title Copy.home_launcher_invoices_title() ("Invoice queue"), action "Open invoice queue" (drop "workspace"); Tile 2 title Copy.home_launcher_recovery_title() ("Dunning funnel"), meta a de-jargoned at-risk-account count, action "Open dunning analytics"; Tile 3 title Copy.home_launcher_developer_title(), meta N dead-letter events when blocked_webhook_count > 0, action "Debug webhook failures". Retire invoice_launcher_title/1 (its dynamic "Open-invoice queue: $X open" title is replaced by the static "Invoice queue" title + the count in meta).
    Put a stable `data-ax-launcher-primary="true"` marker on the invoice-queue tile (highest-priority door) so Plan 03 asserts a real marker rather than the retired `.ax-launcher-primary` class.
    Grid layout: keep the container using the existing `.ax-launchers` grid + gap token. Verify the 3-tile layout has NO dead 4th column and the primary tile keeps appropriate emphasis. The prior span-2 emphasis was driven by the `.ax-launcher-primary` CSS class; since tiles move to `.ax-card`, if primary emphasis or a clean 3-up layout requires a CSS change, make it ADDITIVE ONLY (add a new rule/marker selector — e.g. keyed off data-ax-launcher-primary or a new container class — do NOT delete or edit any existing `.ax-launcher*` rule; deletion is Phase 211). Do not introduce a new spacing token and do not widen tile padding to fill the freed column.
    IF and ONLY IF you edited app.css: run `mix accrue_admin.assets.build` and commit the regenerated priv/static/accrue_admin.css in this plan. IF app.css was NOT edited: do not rebuild — confirm `git diff --stat` shows no app.css/accrue_admin.css change.
    Confirm the answer-first order end-to-end: PageHeader verdict+StatStrip → :actions primary → attention rail → three launchers → KpiCard "At a glance" band (L224–276, KEEP untouched) → Timeline activity cards (L279–360, KEEP untouched).
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | tail -6 && test -z "$(grep -nE 'ax-launcher-customer|Open global customer search|invoice_launcher_title' lib/accrue_admin/live/dashboard_live.ex)" && grep -c 'data-ax-launcher-primary' lib/accrue_admin/live/dashboard_live.ex && (git -C /Users/jon/projects/accrue diff --quiet -- accrue_admin/assets/css/app.css || test -n "$(git -C /Users/jon/projects/accrue diff --name-only -- accrue_admin/priv/static/accrue_admin.css)")</automated>
  </verify>
  <acceptance_criteria>
    - Launcher zone renders exactly three tiles; no ax-launcher-customer tile; no "Open global customer search"; invoice_launcher_title/1 retired.
    - Invoice tile carries data-ax-launcher-primary="true".
    - If app.css was edited, accrue_admin.css was rebuilt via the mix task and is staged; if not, neither file changed.
    - KpiCard band and Timeline cards are byte-for-byte unchanged.
    - Compiles with --warnings-as-errors.
  </acceptance_criteria>
  <done>The launcher zone renders exactly three .ax-card-built tiles with a stable data-ax-launcher-primary marker, no customer tile, KpiCard/Timeline untouched, and the CSS bundle rebuilt iff app.css was edited.</done>
</task>

</tasks>

## Artifacts this plan produces

**New/renamed helper functions in `dashboard_live.ex`:**
- `verdict_status/1`, `verdict_label/1`, `verdict_tone/1`, `exposure_tone/1` — verdict/tone helpers (copied from subscriptions_live.ex, adapted to Home's `@stats` keys).
- `stat_webhook_tone/1` — NEW name to avoid the arity collision with the pre-existing `webhook_tone/1` (timeline-tone mapper at L567–570, which stays).
- `attention_status_badge_tone/1` — maps rail `row.tone` ("warning"/"danger"/"info") → StatusBadge tone.
- `flash_messages/1` — copied from subscriptions_live.ex.
- **Removed:** `dashboard_health_headline/1`, `attention_health_summary/1`, `attention_health_issue_summary/1`, `invoice_launcher_title/1`.

**New DOM markers (stable selectors for Plan 03 gate migration):**
- `data-ax-health-verdict="true"` (single header verdict) — replaces the retired `ax-home-health-answer`.
- `data-ax-launcher-primary="true"` (invoice tile) — replaces the retired `.ax-launcher-primary` class assertion.
- `data-ax-command-palette-trigger="true"` (customer-search control, carried forward) — replaces `ax-home-customer-search-cta`.
- Attention-rail empty hero keeps `class="ax-attention-rail--empty"` (intentional hook preserved for phase194 e2e).

**Retired-from-reference (NOT deleted from CSS — Phase 211):** `.ax-home-header-health`, `.ax-attention-summary`, `.ax-home-customer-search-strip`, `.ax-launcher-customer`, `.ax-dashboard-title-row`, and the bespoke `ax-attention-*`/`ax-launcher-*` row/tile spans are no longer referenced by dashboard_live.ex.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → admin LiveView (existing) | Home is an authenticated read/navigate surface behind the existing admin auth + owner-scope guard. This reign recomposes existing markup onto shared components; it adds no new endpoint, no new user-input sink, and no new data query. The AppShell auth/entitlement guard and ScopedPath owner-scoping are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-210-02 | Information Disclosure | StatStrip / rail hrefs | low | accept | All hrefs are built via the existing ScopedPath.build/4 owner-scoping helper (unchanged), so no cross-owner data is exposed; the reign relocates existing @stats/@attention data, adding no new data source. |
| T-210-03 | Elevation of Privilege | PageHeader adoption | low | accept | No auth/entitlement code is touched; Home stays behind the existing admin plug + AppShell guard. The h1/landmark change is cosmetic and preserves the single-h1 a11y contract (verified in Plan 03 axe run). |
| T-210-SC | Tampering | dependency installs | low | accept | No npm/pip/cargo installs; zero new deps (scope fence). No supply-chain surface. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- Grep gates confirm the three removed verdict renderings and the customer-search strip/tile are gone, and the stable markers are present.
- KpiCard band + Timeline cards unchanged (`git diff` scoped to those line ranges shows no edit).
- If app.css changed, the bundle was rebuilt via `mix accrue_admin.assets.build`; otherwise neither CSS file changed.
- NOTE: the full dashboard_live_test.exs is intentionally NOT this plan's gate — its assertions are migrated in Plan 03 (Wave 3). This plan's gate is compile + grep markers + render sanity.
</verification>

<success_criteria>
Home renders from PageHeader with a single verdict, a single customer-lookup control, three primitive-built launcher tiles, a primitive-built attention rail with EmptyState, and answer-first order — with cross-page parity to the reigned Subscriptions header grammar, no CSS rule deleted, and no density over-airing.
</success_criteria>

<output>
Create `.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-02-SUMMARY.md` when done.
</output>

---
phase: 210-reign-home-certify-answer-first-ia-copy-integrity
plan: 03
type: execute
wave: 3
depends_on: ["210-02"]
files_modified:
  - accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
  - accrue_admin/e2e/admin-spec-overview-phase194.spec.js
  - accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
autonomous: false
requirements: [REIGN-03, IA-03]
must_haves:
  truths:
    - "dashboard_live_test.exs asserts the new shared-component DOM/copy and passes (mix test green) with no assertion left targeting a removed rendering"
    - "The phase194 empty-rail e2e locator and the phase199 .ax-attention-rail ratchet guards resolve against the reigned DOM (no selector left pointing at removed markup)"
    - "admin-a11y.spec.js stays green: exactly one top-level h1 (PageHeader's), landmark/aria-label/visually-hidden semantics preserved"
    - "Home shows no operator-density regression vs the pre-reign screenshot, and reads as one system with the canonical reference + reigned Subscriptions, in both light and dark themes"
    - "No verification gate is left red across the phase boundary"
  artifacts:
    - "accrue_admin/test/accrue_admin/live/dashboard_live_test.exs (migrated assertions)"
    - "accrue_admin/e2e/admin-spec-overview-phase194.spec.js (migrated empty-rail locator)"
    - "accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js (migrated .ax-attention-rail guards)"
  key_links:
    - "Stable markers from Plan 02 (data-ax-health-verdict / data-ax-launcher-primary / data-ax-command-palette-trigger / ax-attention-rail--empty) are the migration targets"
---

<objective>
Certify the reign: migrate every named verification gate to the shared-component DOM in-phase (D-05) so nothing is left red across the phase boundary (part of REIGN-03 / ROADMAP SC5), and prove operator-density is preserved with no regression (IA-03 / ROADMAP SC3) via PNG comparison against both the pre-reign screenshot and the canonical reference, in both themes.

Purpose: The DOM/copy changed in Plan 02, so the L107/L130/L184 unit assertions and the `.ax-attention-rail*` e2e selectors must move to the new markers; axe must stay green; and density (the point of a console) must be verified visually because no automated gate can assert "no density regression" or "reads as one system."
Output: Migrated unit test + two e2e specs (green), and a human-confirmed density/parity PNG review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-CONTEXT.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-PATTERNS.md
@.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-UI-SPEC.md
@accrue_admin/lib/accrue_admin/live/dashboard_live.ex
@accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
@accrue_admin/e2e/admin-spec-overview-phase194.spec.js
@accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dashboard_live_test.exs assertions to the reigned DOM/copy</name>
  <read_first>
    - 210-PATTERNS.md section "dashboard_live_test.exs (unit test) — migrate in-phase (D-05)" (the exact assertion-by-assertion migration list)
    - dashboard_live_test.exs L91-185 (current assertions — many break)
    - Plan 02 "Artifacts this plan produces" (the stable markers to assert)
    - 210-UI-SPEC.md Copywriting Contract (new copy to assert)
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/dashboard_live_test.exs</files>
  <action>
    Rewrite the breaking assertions in the single render test to target the reigned DOM/copy (do not leave any red):
    - Replace the verdict-sentence assertions ("Billing health: Unhealthy", "Billing status: Unhealthy", the ax-home-health-answer string, "Collect ", "before webhook or dunning work", and the P1/P2/P3 sentence) with single-verdict assertions: assert Copy.dashboard_health_verdict_action_required() (seed data has an open invoice, so verdict is :action_required) and the data-ax-health-verdict marker.
    - Replace "ax-launcher-primary" with data-ax-launcher-primary.
    - Replace "ax-home-customer-search-cta" with data-ax-command-palette-trigger; replace "Find ONE customer" with Copy.home_customer_search_cta() ("Find a customer"); drop "Open customer search" (strip removed).
    - Drop the customer-launcher-tile assertions: Copy.home_launcher_customers_title(), Copy.home_launcher_customers_meta(), "Open global customer search", "Search customers globally".
    - Update de-jargoned strings: "Open invoice queue workspace" / "Open invoice queue: collect" / "Opens the Invoices queue workspace..." are gone; assert the new de-jargoned copy instead. "Priority exceptions" now comes from Copy.home_attention_priority_heading(). Drop the "Dunning check:" / "Next stage: reminder pending" / "Funnel preview:" launcher-internal literals the recomposed tiles no longer render, asserting the new tile copy in their place.
    - KEEP the surviving assertions: the P4 meter row (Copy.home_attention_meter_label(), Copy.home_attention_action_investigate()), the webhook row (Copy.home_attention_webhooks_label(), "Debug failed webhook queue"), the KpiCard band labels/aria labels, the Timeline/activity assertions, and the exact nav hrefs (/billing/customers etc.). Keep "ax-display" (PageHeader still emits the h1 with ax-display).
    - Where an assertion targets a StatStrip stat, assert the stat label + a representative value ("Open invoices", "Exposure", "At-risk subscriptions", "Failed webhooks").
    Do not weaken coverage — every surviving zone keeps at least one assertion.
  </action>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/accrue_admin/live/dashboard_live_test.exs passes.
    - No assertion references ax-home-health-answer, ax-launcher-primary, ax-home-customer-search-cta, "Billing health: Unhealthy", "Find ONE customer", or the removed customer-tile / launcher-internal literals.
    - Surviving zones (P4 meter row, webhook row, KpiCard band, Timeline, nav hrefs) still have assertions.
  </acceptance_criteria>
  <done>dashboard_live_test.exs passes on the reigned DOM with all L107/L130/L184 + verdict-sentence + customer-tile assertions migrated or dropped, and surviving zones still covered.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the two e2e specs' retired-class selectors and confirm axe stays green</name>
  <read_first>
    - 210-PATTERNS.md section "admin-spec-overview-phase194.spec.js + admin-interaction-overlay-phase199.spec.js (e2e) — migrate in-phase (D-05)"
    - admin-spec-overview-phase194.spec.js L85-115 (empty-rail D-06 non-interactive test using .ax-attention-rail--empty)
    - admin-interaction-overlay-phase199.spec.js L964-1035 (RATCHET_AUTO_GUARDS array + the focus-ring guard loop; the two .ax-attention-rail rows default to route /billing/dev/components and require the selector to resolve to a visible element that contains a focusable control)
  </read_first>
  <files>accrue_admin/e2e/admin-spec-overview-phase194.spec.js, accrue_admin/e2e/admin-interaction-overlay-phase199.spec.js</files>
  <action>
    phase194: the empty-rail D-06 test locates .ax-attention-rail--empty. Plan 02 preserved that class on the EmptyState (class="ax-attention-rail--empty"), so this locator should still resolve — confirm by reading the reigned markup, and confirm the EmptyState renders as a non-interactive static div (no role=button, not the top pointer target) so the D-06 assertions still hold. Change the locator only if the class was not preserved (prefer preserving the marker over editing the test).
    phase199: the two RATCHET_AUTO_GUARDS rows with "selector":".ax-attention-rail" run a focus-ring guard (default route /billing/dev/components) that requires .ax-attention-rail to be visible AND to contain (or be) a focusable element painting a visible outline. Determine what .ax-attention-rail currently resolves to for these guards, then migrate each row's selector to a stable selector on the reigned attention-rail DOM that (a) exists and is visible on the guard route and (b) contains a focusable control — e.g. [data-ax-zone="attention-rail"] or a rail-row anchor / the Plan-02 stable marker. Keep every @ratchet:<finding_id> trailing comment and the finding_id values intact (ledger keys must not change); update only the selector field, and set an explicit route on those rows if the default route no longer surfaces a rail (e.g. route "/billing"). Do NOT delete any guard row; preserve the array shape and the >>> @ratchet:auto-guards >>> / <<< @ratchet:auto-guards <<< fences.
    Confirm admin-a11y.spec.js needs no change: PageHeader supplies the single top-level h1 (the bespoke h1 was retired in Plan 02) and the rail/launcher sections keep their aria-labels, so axe should be green without editing that spec. If a landmark/heading regression is found, fix it in dashboard_live.ex markup (never by weakening the axe spec) and note it in the summary.
  </action>
  <verify>
    <automated>cd accrue_admin && grep -c '@ratchet:' e2e/admin-interaction-overlay-phase199.spec.js && grep -c 'ax-attention-rail--empty' e2e/admin-spec-overview-phase194.spec.js && test -z "$(grep -nE '\"selector\":\"\.ax-attention-rail\"' e2e/admin-interaction-overlay-phase199.spec.js)"</automated>
  </verify>
  <acceptance_criteria>
    - The phase199 guard count (@ratchet: comment count) is unchanged from before this task and no row still uses the bare "selector":".ax-attention-rail" pointing at removed markup.
    - phase194 still locates .ax-attention-rail--empty (preserved marker) and its D-06 non-interactive assertions still hold.
    - admin-a11y.spec.js unchanged and expected green (single h1, preserved landmarks); any fix applied to markup, not the spec.
  </acceptance_criteria>
  <done>Both e2e specs' retired-class selectors resolve against the reigned DOM (empty-rail marker preserved, ratchet guard selectors retargeted with finding_ids intact), and axe stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Density-no-regression + one-system parity PNG review (both themes)</name>
  <action>Human visually verifies the reigned Home against the pre-reign screenshot and the canonical reference pages, in light and dark themes, per the steps below. No code is written in this task; it gates on operator-density preservation (SC3), which no automated check can assert.</action>
  <what-built>
    Home (dashboard_live.ex) reigned onto the canonical spine: PageHeader (single StatusBadge verdict + 4-stat exposure-first StatStrip), one customer-lookup control in :actions, a primitive-built attention rail with EmptyState, three primitive-built launcher tiles (down from four — customer tile folded into the header), KpiCard band + Timeline kept. Unit test + e2e selectors migrated to the new DOM.
  </what-built>
  <how-to-verify>
    Density-no-regression + one-system parity is a visual judgment SC3 requires PNG comparison for. Steps:
    1. Start the demo host (native arch) and log into /billing (Home). See MEMORY "accrue_host Docker DX" for the curl-login / restart pattern; the served bundle is priv/static/accrue_admin.css.
    2. Capture Home in BOTH light and dark theme (use the theme picker / system toggle).
    3. Compare against (a) the PRE-REIGN Home screenshot (header-band height + attention-rail + launcher-row above-the-fold extent must be at-or-tighter — NO over-airing, NO added whitespace) and (b) the canonical reference pages (Payments/Customers/Invoices) + the reigned Subscriptions header — Home must read as the same grammar (StatusBadge verdict + exposure-first StatStrip + single primary action).
    4. Confirm the answer-first order reads top-to-bottom: verdict+StatStrip -> primary action -> attention rail -> three launchers -> "At a glance" KpiCard band -> Timeline.
    5. Confirm exactly ONE verdict, ONE customer-search control, THREE launcher tiles (three is CORRECT per D-02a — do not flag the drop), and no fake breadcrumb parent (single [Dashboard] crumb).
    6. Spot-check the empty attention-rail state (reset seed data) renders the EmptyState hero, non-interactive.
    7. RUN the three named e2e gates against the live host (ROADMAP SC5 requires them green on the reigned DOM in-phase — a grep of selector strings is not sufficient; a runtime selector mismatch only surfaces on a real run): execute `admin-spec-overview-phase194.spec.js`, `admin-interaction-overlay-phase199.spec.js`, and `admin-a11y.spec.js` (e.g. `npx playwright test e2e/admin-spec-overview-phase194.spec.js e2e/admin-interaction-overlay-phase199.spec.js e2e/admin-a11y.spec.js` from accrue_admin against the booted host). Confirm all three pass green — in particular the phase194 empty-rail D-06 test, the phase199 `.ax-attention-rail` focus-ring guards (retargeted in Task 2), and the axe landmark/heading/visually-hidden checks. If any fails, fix the selector/markup in-phase (Task 2 / dashboard_live.ex) and re-run before approving.
  </how-to-verify>
  <resume-signal>Type "approved" only if BOTH (a) density holds and Home reads as one system in both themes AND (b) all three e2e gates (phase194, phase199, admin-a11y) ran and passed green against the live host; otherwise describe the specific density/parity issue or the failing e2e gate to fix.</resume-signal>
  <acceptance_criteria>
    - Density-no-regression + one-system parity confirmed by PNG comparison vs pre-reign + canonical reference in both light and dark themes.
    - admin-spec-overview-phase194.spec.js, admin-interaction-overlay-phase199.spec.js, and admin-a11y.spec.js were RUN against the live host and all passed green (not merely grep-checked) — SC5 satisfied in-phase.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this plan produces

**Migrated gates (no new symbols):**
- `dashboard_live_test.exs` — assertions retargeted to `data-ax-health-verdict`, `data-ax-launcher-primary`, `data-ax-command-palette-trigger`, and the new verdict/tile copy; customer-tile assertions removed.
- `admin-spec-overview-phase194.spec.js` — empty-rail locator confirmed against the preserved `.ax-attention-rail--empty` marker.
- `admin-interaction-overlay-phase199.spec.js` — the two `.ax-attention-rail` RATCHET_AUTO_GUARDS `selector` fields retargeted to the reigned rail DOM; `finding_id` ledger keys and fences unchanged.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan edits test + e2e specs only. No production code path, no endpoint, no user input, no data handling is changed. The a11y check confirms the reign did not regress landmark/heading semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-210-04 | Tampering | ratchet guard ledger keys | low | mitigate | Migrate only the `selector` field of the two `.ax-attention-rail` guard rows; keep `finding_id` values and the `@ratchet:` fences intact so the parked ratchet ledger stays consistent for a future re-freeze. |
| T-210-05 | Repudiation | a11y regression | low | mitigate | Keep admin-a11y.spec.js unmodified and green; any landmark/heading fix goes in markup, not by weakening the axe assertion — preserving the audit value of the gate. |
| T-210-SC | Tampering | dependency installs | low | accept | No npm/pip/cargo installs in this phase; no supply-chain surface. |
</threat_model>

<verification>
- `mix test test/accrue_admin/live/dashboard_live_test.exs` green.
- Grep confirms no e2e selector still targets removed markup; ratchet guard count + finding_ids unchanged.
- Human-verify checkpoint RUNS all three named e2e gates against the live host and confirms green: admin-spec-overview-phase194.spec.js (empty-rail D-06), admin-interaction-overlay-phase199.spec.js (retargeted .ax-attention-rail focus-ring guards), and admin-a11y.spec.js (single h1, preserved landmarks) — SC5 satisfied in-phase, catching any runtime selector mismatch before the boundary.
- Human-verify checkpoint: density-no-regression + one-system parity confirmed by PNG comparison in both themes.
- Confirm the whole phase diff touches no accrue/lib, adds no nav room, and introduces no diagnosis/causality synthesis.
</verification>

<success_criteria>
Every named gate (unit L107/L130/L184, phase194 empty-rail, phase199 `.ax-attention-rail` guards, axe) passes on the reigned DOM with nothing left red across the phase boundary, and Home's operator density + one-system parity is human-confirmed in light and dark themes.
</success_criteria>

<output>
Create `.planning/phases/210-reign-home-certify-answer-first-ia-copy-integrity/210-03-SUMMARY.md` when done.
</output>

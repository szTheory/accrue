---
phase: 211-grep-gated-css-retirement-cross-surface-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["211-02"]
files_modified:
  - accrue_admin/priv/static/storybook.css
autonomous: true
requirements: [REIGN-04]
must_haves:
  truths:
    - "priv/static/storybook.css no longer embeds any of the 97 retired selectors - it is recomposed from PhoenixStorybook CSS + the freshly rebuilt accrue_admin.css + the byte-preserved D-17 dark-mode shim tail"
    - "The orphan-guard script (built in Plan 01) reports zero of the 97 target selectors as orphans anywhere in the final app.css, confirming the deletion is complete and correctly scoped"
    - "The three phase200 storybook unit tests plus mix compile pass as an early smoke gate before the full Wave 4 verification"
  artifacts:
    - "accrue_admin/priv/static/storybook.css (recomposed)"
  key_links:
    - "storybook.css has no dedicated mix task - it must be recomposed via the documented shell recipe (PSB CSS + accrue_admin.css + D-17 shim tail), not hand-edited"
---

<objective>
Recompose the committed `priv/static/storybook.css` bundle (D-04) so it no longer embeds the pre-retirement `accrue_admin.css`, then run the Plan 01 orphan-guard script against the fully-deleted `app.css` to confirm the retirement is complete and correctly scoped, plus an early smoke-test pass before the full Wave 4 verification.

Purpose: `storybook.css` has no automated rebuild task (a one-time hand-composed file since Phase 193) and will silently keep serving dead-class remnants unless recomposed by hand per the documented recipe. Running the guard now, immediately after deletion, is the first automated proof (beyond the manual per-task greps in Plan 02) that nothing was missed and nothing live was over-deleted.
Output: A freshly recomposed `storybook.css`, a clean (zero-orphan) guard run, and a green early smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/211-grep-gated-css-retirement-cross-surface-cleanup/211-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose priv/static/storybook.css per the D-04 shell recipe</name>
  <read_first>
    - 211-RESEARCH.md "## Bundle Rebuild" section (the full shell recipe: awk extraction of the D-17 shim tail, then printf headers + phoenix_storybook.css + accrue_admin.css + shim tail)
    - accrue_admin/priv/static/storybook.css lines 1-15 (the existing header comments and the "=== D-17 Spike B: dark-mode class-to-attribute shim ===" marker line - confirm the exact marker text before writing the awk extraction)
  </read_first>
  <files>accrue_admin/priv/static/storybook.css</files>
  <action>
    From accrue_admin/, run the documented recomposition recipe exactly: first extract the existing dark-mode shim tail (the byte range starting at the "/* === D-17 Spike B: dark-mode class-to-attribute shim ===" marker line through end-of-file) from the CURRENT priv/static/storybook.css into a scratch file using awk, matching on that marker. Then rebuild priv/static/storybook.css by concatenating, in order: (1) the same three header comment lines already present at the top of the current file (the D-17 spike D decision line, the "served via AccrueAdmin.Assets" line, and the "Composition:" line - copy them verbatim, do not reword), (2) a blank line, (3) the full contents of deps/phoenix_storybook/priv/static/css/phoenix_storybook.css verbatim, (4) a blank line and the literal marker comment "/* === accrue_admin.css bundle === */", (5) the full contents of the freshly-rebuilt priv/static/accrue_admin.css from Plan 02 verbatim, (6) a blank line, (7) the extracted shim-tail scratch file from step one (which itself begins with the "=== D-17 Spike B" marker) - preserved byte-for-byte, no re-wording, no re-ordering of its `--ax-*` custom-property declarations. Overwrite priv/static/storybook.css with this concatenation. Do not touch the shim tail's content in any way - only its position in the new file must match the original (last section).
  </action>
  <verify>
    <automated>cd accrue_admin && grep -c "D-17 Spike B" priv/static/storybook.css</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms exactly one "D-17 Spike B" marker line is present (the shim tail was appended, not dropped).
    - A separate check (run after this, before closing the task) confirms zero exact-token matches for ax-attention, ax-home-search, and ax-launcher anywhere in the recomposed storybook.css, proving the stale pre-retirement accrue_admin.css embed was replaced.
    - storybook.css file size is close to phoenix_storybook.css size plus the freshly rebuilt (smaller) accrue_admin.css size plus the unchanged shim tail size (a large shrink versus the pre-Plan-03 committed file is expected).
  </acceptance_criteria>
  <done>storybook.css is recomposed from PSB CSS + the fresh accrue_admin.css + the byte-preserved D-17 shim tail, with no retired-class remnants.</done>
</task>

<task type="auto">
  <name>Task 2: Run the orphan-guard against the retired app.css and an early smoke test</name>
  <read_first>
    - accrue_admin/e2e/verify-css-census.mjs (the Plan 01 guard script - full read to confirm its CLI/exit-code contract before relying on it)
    - 211-RESEARCH.md "## Test/E2E Migration Surface" table (the "How to run the relevant suites" table, specifically the phase200:storybook row)
  </read_first>
  <files>none - verification only, no files modified</files>
  <action>
    Run the orphan-guard from accrue_admin/ against the now-fully-retired app.css: `npm run css:census`. Confirm the orphan report contains ZERO of the 97 target selectors from RESEARCH.md (the 92 named DELETE classes plus the 5 D-01 adjacent rules) - if any of them still appear as orphans, STOP and return to Plan 02 to finish the deletion before proceeding (the guard finding a target class as an orphan means it is genuinely dead in source but was never removed from CSS - re-run the corresponding Plan 02 task). If the guard reports OTHER orphans outside the 97 target selectors, note them in the SUMMARY as informational/out-of-scope for REIGN-04 - do not delete them in this phase. Then run `mix compile` from accrue_admin/ to confirm the CSS/asset changes compile cleanly (the compile-time module-attribute caching in AccrueAdmin.Assets means a full compile is required to pick up the new bundle bytes), followed by the three phase200 storybook unit tests as an early smoke gate: `mix test test/accrue_admin/dev/storybook_coverage_test.exs test/accrue_admin/dev/storybook_asset_test.exs test/accrue_admin/theme_test.exs`.
  </action>
  <verify>
    <automated>cd accrue_admin && npm run css:census; mix compile</automated>
  </verify>
  <acceptance_criteria>
    - The guard's orphan report contains none of the 97 target selectors (confirms Plan 02's deletion is complete and correctly scoped).
    - `mix compile` exits 0.
    - `mix test test/accrue_admin/dev/storybook_coverage_test.exs test/accrue_admin/dev/storybook_asset_test.exs test/accrue_admin/theme_test.exs` passes (byte-equality of the served storybook assets against the just-recomposed committed file, coverage-row parity, and dark-mode-token mirroring all confirmed).
  </acceptance_criteria>
  <done>The guard confirms a clean, complete retirement and the earliest possible smoke tests are green before the full Wave 4 verification.</done>
</task>

</tasks>

## Artifacts this phase produces

No new artifacts in this plan - it recomposes an existing generated file (`priv/static/storybook.css`) and runs the Plan 01 guard script plus pre-existing tests. No new symbols or files.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan recomposes a static dev-tooling asset bundle and runs read-only verification. No production code path, endpoint, or user input is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-04 | Tampering | D-17 dark-mode shim tail corruption during recomposition | medium | mitigate | The shim tail is extracted via awk on the existing marker and appended byte-for-byte, never re-typed or re-ordered; theme_test.exs independently verifies the dark-mode token mirroring stays correct. |
| T-211-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan. |
</threat_model>

<verification>
- storybook.css recomposed, contains the D-17 marker exactly once, and no retired-class remnants.
- Orphan guard reports zero of the 97 target selectors anywhere in app.css.
- mix compile and the three phase200 storybook unit tests pass.
</verification>

<success_criteria>
storybook.css is fresh and clean, the orphan guard independently confirms the CSS retirement is complete and correctly scoped, and an early smoke test is green before Wave 4's full verification.
</success_criteria>

<output>
Create `.planning/phases/211-grep-gated-css-retirement-cross-surface-cleanup/211-03-SUMMARY.md` when done.
</output>

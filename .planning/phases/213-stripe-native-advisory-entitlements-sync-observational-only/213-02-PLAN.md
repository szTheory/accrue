---
phase: 213-stripe-native-advisory-entitlements-sync-observational-only
plan: "02"
type: execute
wave: 2
depends_on:
  - 213-01
files_modified:
  - accrue/lib/accrue/processor/stripe.ex
  - accrue/lib/accrue/entitlements/stripe_sync/refresh_worker.ex
  - accrue/test/accrue/processor/stripe_entitlements_contract_test.exs
  - accrue/test/accrue/entitlements/stripe_sync_refresh_worker_test.exs
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-05
autonomous: true
must_haves:
  truths:
    - "Per D-03, the Stripe adapter drains ActiveEntitlement.stream!/3 completely and returns only webhook-compatible plain string-keyed maps."
    - "Per D-07, hosts can enqueue a scalar customer_id job on the existing accrue_webhooks queue and the worker delegates to StripeSync.refresh/1."
    - "Per D-04/SYNC-05, automated tests use local dependency contracts and Fake/Test seams only; they make no live Stripe or Chrome calls."
  artifacts:
    - path: accrue/lib/accrue/processor/stripe.ex
      provides: "real list_active_entitlements/2 adapter"
      contains: "ActiveEntitlement.stream!"
    - path: accrue/lib/accrue/entitlements/stripe_sync/refresh_worker.ex
      provides: "thin Oban refresh worker"
      contains: "use Oban.Worker"
    - path: accrue/test/accrue/entitlements/stripe_sync_refresh_worker_test.exs
      provides: "worker success, disabled, missing-customer, and error behavior"
  key_links:
    - from: accrue/lib/accrue/processor/stripe.ex
      to: accrue/deps/lattice_stripe/lib/lattice_stripe/entitlements/active_entitlement.ex
      via: "build_client! then fully materialize customer-filtered stream"
      pattern: "ActiveEntitlement\\.stream!"
    - from: accrue/lib/accrue/entitlements/stripe_sync/refresh_worker.ex
      to: accrue/lib/accrue/entitlements/stripe_sync.ex
      via: "perform/1 loads customer by scalar id and calls refresh/1"
      pattern: "StripeSync\\.refresh"
  prohibitions:
    - requirement_id: SYNC-02
      statement: "The worker must not create an always-on poller, new queue requirement, or request-path network grant check."
      status: unverified
      flagged: true
  flagged_assumptions:
    - "SYNC-01 unresolved edge: SDK struct projection includes only id, object, feature, lookup_key, and livemode and materialization errors write nothing."
    - "SYNC-02 unresolved edge: enqueueing is host-owned; the worker is inert unless the existing queue is run."
    - "SYNC-05 unresolved edge: dependency contract tests may inspect local LatticeStripe code/structs but never perform HTTP."
---

<objective>
Connect the proven refresh primitive to the real LatticeStripe Entitlements client and the host-owned Oban execution surface.

Purpose: Turn the Fake-proven tracer into a deployable client-backed refresh without adding host wiring or grant semantics.
Output: Stripe adapter implementation, thin refresh worker, and deterministic tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-CONTEXT.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-RESEARCH.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/COVERAGE.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-01-SUMMARY.md
@accrue/lib/accrue/processor/stripe.ex
@accrue/lib/accrue/webhook/dispatch_worker.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exhaustive ActiveEntitlement client projection inside the Stripe facade</name>
  <files>accrue/lib/accrue/processor/stripe.ex, accrue/test/accrue/processor/stripe_entitlements_contract_test.exs</files>
  <read_first>accrue/lib/accrue/processor/stripe.ex; accrue/lib/accrue/processor/stripe/error_mapper.ex; accrue/deps/lattice_stripe/lib/lattice_stripe/entitlements/active_entitlement.ex; accrue/deps/lattice_stripe/lib/lattice_stripe/entitlements/active_entitlement_summary.ex; .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/COVERAGE.md</read_first>
  <behavior>
    - "The adapter invokes ActiveEntitlement.stream!/3 with customer id and limit 100 and drains the enumerable."
    - "Each SDK struct becomes a plain string-keyed map containing the D-01 webhook-compatible fields."
    - "A LatticeStripe.Error is converted through ErrorMapper and a stream/page failure cannot return a partial success."
  </behavior>
  <action>Per D-03 implement `Accrue.Processor.Stripe.list_active_entitlements/2` as the only raw SDK call: build the configured client, pass non-idempotent Stripe options, call `LatticeStripe.Entitlements.ActiveEntitlement.stream!/3` with `%{"customer" => id, "limit" => "100"}`, fully materialize it, and project id/object/feature/lookup_key/livemode to string-keyed maps. Rescue only the SDK error shape through `ErrorMapper.to_accrue_error/1`; do not swallow arbitrary failures or return accumulated partial pages. Add a deterministic contract test using local structs/injected enumerable seams already supported by the adapter test pattern; do not call live Stripe and do not move an SDK reference outside `stripe.ex`.</action>
  <reversibility rating="reversible">The adapter implementation is internal behind the optional Processor contract.</reversibility>
  <acceptance_criteria>The real adapter compiles against LatticeStripe 2.x, exhaustively projects the customer stream, and converts SDK failures without partial writes.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/processor/stripe_entitlements_contract_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <done>The processor facade has a production Stripe implementation whose success always represents a complete list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the thin existing-queue RefreshWorker</name>
  <files>accrue/lib/accrue/entitlements/stripe_sync/refresh_worker.ex, accrue/test/accrue/entitlements/stripe_sync_refresh_worker_test.exs</files>
  <read_first>accrue/lib/accrue/webhook/dispatch_worker.ex; accrue/lib/accrue/workers/dunning_step.ex; accrue/lib/accrue/entitlements/stripe_sync.ex; accrue/test/accrue/workers/dunning_step_test.exs</read_first>
  <behavior>
    - "A string-keyed customer_id job loads the customer and delegates once to StripeSync.refresh/1."
    - "Disabled refresh completes successfully without calling the processor."
    - "Missing customer cancels deterministically; refresh errors are returned for Oban retry semantics."
  </behavior>
  <action>Per D-07 create `Accrue.Entitlements.StripeSync.RefreshWorker` with `use Oban.Worker`, queue `:accrue_webhooks`, scalar `%{"customer_id" => id}` args, and the existing worker max-attempt convention. Load the local Customer, cancel a missing row, delegate all sync semantics to `StripeSync.refresh/1`, and map its success/error tuples to documented Oban results. Do not start Oban, add a queue, schedule poll-all work, accept structs in job args, or add the D-08 top-level facade. Test with `Oban.Testing` plus Fake only.</action>
  <reversibility rating="reversible">The additive worker is inert until a host enqueues it and uses an existing queue.</reversibility>
  <acceptance_criteria>The worker is host-runnable through the existing queue, uses JSON-safe args, inherits config-off behavior, and has no scheduler or supervision side effect.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/stripe_sync_refresh_worker_test.exs test/accrue/entitlements/stripe_sync_refresh_test.exs</automated>
  </verify>
  <done>Hosts have an out-of-request-path refresh wrapper without new operational wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Stripe/LatticeStripe → processor adapter | External paginated data and failures enter Accrue through one facade. |
| persisted Oban args → worker | JSON job arguments are host-controlled and must stay scalar/string-keyed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-213-04 | Tampering / Denial of Service | `Processor.Stripe.list_active_entitlements/2` | high | mitigate | Drain `stream!/3` fully; any page error returns an error and prevents reconciliation. |
| T-213-05 | Information Disclosure | Stripe adapter telemetry/errors | medium | mitigate | Project bounded entitlement fields and map errors; no payload logging. |
| T-213-06 | Denial of Service | `RefreshWorker.perform/1` | medium | mitigate | JSON-safe scalar ids, existing queue, bounded retry convention, no internal scheduler. |
| T-213-SC | Tampering | package installs | high | mitigate | No install; only Phase-212-audited LatticeStripe and existing Oban are used. |
</threat_model>

## Artifacts this phase produces

- `Accrue.Processor.Stripe.list_active_entitlements/2`.
- Internal active-entitlement-to-wire-map projection helper.
- `Accrue.Entitlements.StripeSync.RefreshWorker`.
- `accrue/test/accrue/processor/stripe_entitlements_contract_test.exs`.
- `accrue/test/accrue/entitlements/stripe_sync_refresh_worker_test.exs`.

<verification>
Run both task commands, then `cd accrue && mix test test/accrue/processor/stripe_entitlements_contract_test.exs test/accrue/entitlements/stripe_sync_refresh_worker_test.exs test/accrue/entitlements/stripe_sync_refresh_test.exs`.
</verification>

<success_criteria>
The public refresh primitive can fetch a complete real Stripe active-entitlement list through the Processor facade, and hosts can run it asynchronously through the existing queue with Fake-only automated proof.
</success_criteria>

<output>
Create `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-02-SUMMARY.md` when done.
</output>

---
phase: 213-stripe-native-advisory-entitlements-sync-observational-only
plan: "03"
type: execute
wave: 2
depends_on:
  - 213-01
files_modified:
  - scripts/ci/verify_entitlement_sync_isolation.sh
  - accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs
  - accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
  - accrue/lib/accrue/entitlements/admin.ex
  - accrue/guides/entitlements.md
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
requirements:
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
autonomous: true
must_haves:
  truths:
    - "Per D-15, the isolation guard fails when any gate-path file contains a real list_active_entitlements or Reconcile reference and remains green for comments/docs."
    - "Per D-01/D-11, local grant results are identical with empty, stale, and directly contradictory advisory cache rows."
    - "Per D-14, admin moduledoc and the entitlements guide unambiguously close fetch_entitled/2 as will-not-build; no function is added."
  artifacts:
    - path: scripts/ci/verify_entitlement_sync_isolation.sh
      provides: "gate-to-new-fetch/shared-writer static isolation coverage"
      contains: "list_active_entitlements"
    - path: accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs
      provides: "negative fixture proving new guard tokens fail"
    - path: accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
      provides: "empty/stale/contradictory cache grant invariance"
    - path: accrue/lib/accrue/entitlements/admin.ex
      provides: "closed fetch_entitled/2 rationale"
  key_links:
    - from: scripts/ci/verify_entitlement_sync_isolation.sh
      to: accrue/lib/accrue/entitlements/resolver/local_map.ex
      via: "static scan rejects advisory fetch/reconciler symbols before comments"
      pattern: "list_active_entitlements|Reconcile"
    - from: accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
      to: accrue/lib/accrue/entitlements/resolver/local_map.ex
      via: "grant APIs resolve only from local plan/feature state despite contradictory cache"
      pattern: "entitled\\?"
  prohibitions:
    - requirement_id: SYNC-02
      statement: "No empty, stale, contradictory, or fresh advisory row may change a grant result."
      status: unverified
      flagged: true
    - requirement_id: SYNC-04
      statement: "No function named fetch_entitled/2 or equivalent network-backed authorization predicate may be introduced."
      status: unverified
      flagged: true
  flagged_assumptions:
    - "SYNC-03 unresolved concurrency edge is covered by Plan 01's atomic race tests; this plan independently proves the static gate catches the new symbols."
    - "SYNC-04 unresolved edge: closure is stronger than deferral and satisfies the requirement's no-ambiguity clause without adding behavior."
    - "SYNC-05 unresolved edge: contradictory-cache tests insert diagnostic rows directly and compare the complete local grant surface before/after."
---

<objective>
Make the observational-only boundary independently enforceable and close the ambiguous Stripe-backed predicate question.

Purpose: Prove the new refresh surface cannot be wired into grant decisions and document the permanent local-gate choice.
Output: Live negative isolation guard, runtime grant-invariance tests, and D-14 closure text.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-CONTEXT.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VALIDATION.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-01-SUMMARY.md
@scripts/ci/verify_entitlement_sync_isolation.sh
@accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend and red-path prove isolation for the client fetch and shared writer</name>
  <files>scripts/ci/verify_entitlement_sync_isolation.sh, accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs</files>
  <read_first>scripts/ci/verify_entitlement_sync_isolation.sh; accrue/lib/accrue/entitlements.ex; accrue/lib/accrue/entitlements/resolver.ex; accrue/lib/accrue/entitlements/resolver/local_map.ex; accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs</read_first>
  <behavior>
    - "The clean repository passes the isolation script."
    - "A temporary gate-path fixture with executable list_active_entitlements fails the script."
    - "A temporary gate-path fixture with executable Accrue.Entitlements.Reconcile fails the script."
    - "The same tokens in comments/moduledoc text do not fail."
  </behavior>
  <action>Write a hermetic negative-path test that copies the three gate files into a temporary ROOT_DIR fixture, injects executable references before any comment delimiter, and asserts non-zero status plus the guard failure message for each new token; also prove comments remain allowed. Per D-15 extend the script's forbidden alternatives and explanatory output with `list_active_entitlements` and the exact shared module symbol `Reconcile`. Preserve missing-file checks and comment-text filtering. Do not weaken the gate with broad allowlists or scan diagnostic modules as if they were grant files.</action>
  <reversibility rating="reversible">The additive static guard and fixtures can be adjusted without runtime or schema migration.</reversibility>
  <acceptance_criteria>Both new executable gate→seam edges demonstrably fail, clean/comment-only fixtures pass, and the repository guard is green.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs && cd .. && bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>The isolation guard covers the new fetch and reconciler symbols with a proven red path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove contradictory-cache grant invariance and close fetch_entitled/2</name>
  <files>accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs, accrue/lib/accrue/entitlements/admin.ex, accrue/guides/entitlements.md, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs; accrue/lib/accrue/entitlements/admin.ex; accrue/guides/entitlements.md; accrue/test/accrue/docs/package_docs_verifier_test.exs; accrue/lib/accrue/entitlements/resolver/local_map.ex</read_first>
  <behavior>
    - "The full local grant surface is identical with no cache row, a stale row, and a fresh row whose entitlements directly contradict the local mapping."
    - "Admin moduledoc and guide say fetch_entitled/2 is closed/will-not-build because network authorization fails open and diagnostic observation already exists."
    - "No def fetch_entitled or top-level Stripe-backed grant predicate exists."
  </behavior>
  <action>Extend the existing isolation test per SYNC-02/SYNC-05 to insert empty, stale, and contradictory `EntitlementSummary` rows and compare `entitled?/2`, `features_for/1`, `has_active_plan?/2`, and `entitlement_quantity/2` against the same local fixture; include advisory-enabled configuration to prove the invariant is structural, not only config-off. Per D-14 replace the deferred sentence in `Admin` moduledoc and the matching guide section with a concise decided closure: a Stripe-backed predicate is rejected because network partition can fail open, while `StripeSync.summary_for_customer/1` and `Admin.resolve_for_customer/1` already serve diagnostics. Add a docs verifier assertion that rejects renewed deferred/ambiguous wording or any `def fetch_entitled`; keep Phase 214's broader doc reconciliation out of scope.</action>
  <reversibility rating="reversible">The code change is tests plus documentation, though the architectural closure is intentional and permanent.</reversibility>
  <acceptance_criteria>All advisory cache states leave grants byte-for-byte local, and both code documentation surfaces record an unambiguous will-not-build decision with no new predicate.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs test/accrue/docs/package_docs_verifier_test.exs && ! rg -n 'def(p)? fetch_entitled' lib test</automated>
  </verify>
  <done>The grant boundary is runtime-proven and the prior fetch_entitled/2 ambiguity is permanently closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| advisory cache → public grant APIs | Untrusted/stale diagnostic state must have no dependency path into authorization. |
| future source edits → CI isolation script | A future executable gate reference must be detected rather than hidden by comments or regex drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-213-07 | Elevation of Privilege | resolver/guard grant path | high | mitigate | Empty/stale/contradictory cache invariance tests and D-15 static red-path guard. |
| T-213-08 | Tampering | isolation regex | high | mitigate | Hermetic violation fixtures prove each new token has enforcement teeth. |
| T-213-09 | Elevation of Privilege | proposed `fetch_entitled/2` | high | mitigate | D-14 closes the predicate and docs verifier rejects reintroduction. |
| T-213-SC | Tampering | package installs | high | mitigate | No package-manager operation occurs in this plan. |
</threat_model>

## Artifacts this phase produces

- Extended forbidden-symbol set in `scripts/ci/verify_entitlement_sync_isolation.sh`.
- `accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs`.
- Empty/stale/contradictory advisory-cache cases in `StripeSyncDisabledIsolationTest`.
- D-14 closure text in `Accrue.Entitlements.Admin` moduledoc.
- D-14 closure text in `accrue/guides/entitlements.md`.
- Package-doc verifier assertions for the closure and missing predicate.

<verification>
Run both task commands, then `bash scripts/ci/verify_entitlement_sync_isolation.sh && cd accrue && mix test test/accrue/entitlements test/accrue/docs/package_docs_verifier_test.exs`.
</verification>

<success_criteria>
The merge gate demonstrably catches new client-fetch/shared-writer references from grant files, every tested advisory state leaves grant results unchanged, and the network-backed predicate is explicitly rejected.
</success_criteria>

<output>
Create `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-03-SUMMARY.md` when done.
</output>

---
phase: 213-stripe-native-advisory-entitlements-sync-observational-only
plan: "05"
type: execute
wave: 4
depends_on:
  - 213-04
files_modified:
  - scripts/ci/verify_entitlement_sync_isolation.sh
  - accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs
autonomous: true
gap_closure: true
requirements:
  - SYNC-02
  - SYNC-03
must_haves:
  truths:
    - "Per D-01/D-11/D-15, every always-on grant module, including Accrue.Entitlements.Guard, is scanned for executable references to the observational Stripe-native sync surface."
    - "Executable list_active_entitlements, Reconcile, StripeSync, and EntitlementSummary references injected into guard.ex each fail the isolation gate with a non-zero status."
    - "Comments and Elixir documentation in guard.ex may name all four forbidden symbols without failing the isolation gate."
    - "The clean repository passes the isolation script, preserving the observational-only authority boundary."
  artifacts:
    - path: scripts/ci/verify_entitlement_sync_isolation.sh
      provides: "complete static scan inventory for the resolver/guard grant path"
      contains: "entitlements/guard.ex"
    - path: accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs
      provides: "hermetic guard.ex executable-reference red paths and prose allowance proof"
      contains: "guard_surface_red_path"
  key_links:
    - from: scripts/ci/verify_entitlement_sync_isolation.sh
      to: accrue/lib/accrue/entitlements/guard.ex
      via: "guard.ex is an explicit gate_path_files entry consumed by the existing awk scan"
      pattern: "entitlements/guard\\.ex"
    - from: accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs
      to: scripts/ci/verify_entitlement_sync_isolation.sh
      via: "ROOT_DIR points the script at a unique temporary repository containing a synthetic Guard module"
      pattern: "ROOT_DIR"
    - from: accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs
      to: accrue/lib/accrue/entitlements/guard.ex
      via: "the fixture inventory mirrors Guard as an always-on grant module without modifying production guard code"
      pattern: "Accrue\\.Entitlements\\.Guard"
  prohibitions:
    - requirement_id: SYNC-02
      category: safety
      status: resolved
      verification: test
      statement: "No advisory-cache, client-fetch, or shared-writer dependency may be executable from Accrue.Entitlements.Guard."
    - requirement_id: SYNC-03
      category: completeness
      status: resolved
      verification: test
      statement: "The isolation proof must cover the shared Plug/LiveView Guard path as well as the previously scanned resolver files."
  flagged_assumptions:
    - "SYNC-02 unclassified probe: guard.ex remains production-code unchanged; this plan strengthens the merge gate around it rather than adding a runtime dependency."
    - "SYNC-03 test-isolation probe: every fixture uses System.unique_integer/1 plus on_exit cleanup, so the existing async: true ExUnit case remains collision-free."
---

<objective>
Close the final Phase 213 verification gap by extending the observational-only isolation proof across the actual shared guard decision module.

Purpose: Ensure a future Plug or LiveView guard edit cannot call the advisory client fetch, shared writer, read seam, or cache model without failing CI.
Output: A complete grant-path file inventory in the isolation script and a hermetic guard.ex red-path/prose-allowance regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-CONTEXT.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-03-SUMMARY.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-04-SUMMARY.md
@scripts/ci/verify_entitlement_sync_isolation.sh
@accrue/lib/accrue/entitlements/guard.ex
@accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs

<interfaces>
From scripts/ci/verify_entitlement_sync_isolation.sh:
- `ROOT_DIR` overrides the repository root for hermetic fixtures.
- `gate_path_files` is the complete inventory passed to both missing-file validation and the awk executable-reference scan.
- The forbidden alternation already contains `EntitlementSummary`, `StripeSync`, `accrue_entitlement_summaries`, `stripe_native_sync`, `list_active_entitlements`, and `Reconcile`.
- Success prints `verify_entitlement_sync_isolation: OK`; a forbidden executable reference prints `verify_entitlement_sync_isolation: FAIL` and exits non-zero.

From accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs:
- `fixture_root!/0` creates a unique temporary ROOT_DIR and writes every path in `@gate_files`.
- `inject_before_comment!/3` inserts executable fixture code before `def clean`.
- `write_gate_file!/3` replaces one fixture module with controlled source text.
- `run_guard/1` invokes the repository script with stderr merged into stdout.
- The ExUnit module is `async: true`; fixtures must remain process-isolated and self-cleaning.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make Guard isolation executable-reference complete</name>
  <files>scripts/ci/verify_entitlement_sync_isolation.sh, accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs</files>
  <read_first>scripts/ci/verify_entitlement_sync_isolation.sh; accrue/lib/accrue/entitlements/guard.ex; accrue/test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs; .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md; .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-03-SUMMARY.md</read_first>
  <behavior>
    - "RED: with guard.ex present in the temporary fixture but absent from gate_path_files, executable Accrue.Processor.list_active_entitlements/2, Accrue.Entitlements.Reconcile, Accrue.Entitlements.StripeSync, and Accrue.Billing.EntitlementSummary references injected into Guard incorrectly return exit status 0."
    - "GREEN: after guard.ex joins gate_path_files, each executable reference independently returns non-zero output containing the isolation FAIL marker and the injected forbidden token."
    - "A Guard fixture whose moduledoc and comment lines name all four forbidden symbols returns status 0 and the isolation OK marker."
    - "The unchanged production repository returns status 0 and the isolation OK marker."
  </behavior>
  <action>Start with the hermetic failing proof. Add `accrue/lib/accrue/entitlements/guard.ex` to the test module's `@gate_files`, add the matching `module_for/1` clause for `Accrue.Entitlements.Guard`, and add a test tagged `@tag :guard_surface_red_path` that iterates four isolated executable injections into guard.ex: `Accrue.Processor.list_active_entitlements/2`, `Accrue.Entitlements.Reconcile`, `Accrue.Entitlements.StripeSync`, and `Accrue.Billing.EntitlementSummary`. Use a fresh `fixture_root!/0` per injection; assert non-zero status, the existing FAIL marker, and the specific token so no forbidden alternative can be ceremonial. Extend the prose-allowance coverage on guard.ex with one multiline `@moduledoc` and comment containing all four names, then assert status 0 and the OK marker. Confirm this test fails before changing the script. Per D-01/D-11/D-15, add `${lib}/accrue/entitlements/guard.ex` to the script's `gate_path_files` array and its documented gate-path list; keep the existing awk code/comment/doc filtering, forbidden-token set, missing-file failure, and diagnostics unchanged. Do not edit production `guard.ex`, broaden an allowlist, call live Stripe, invoke Chrome, or serialize the async-safe test case.</action>
  <reversibility rating="reversible">The change adds one existing source file to a static CI inventory and expands hermetic tests; undo requires no runtime, data, or public-contract migration.</reversibility>
  <acceptance_criteria>The tagged guard-surface fixture proves all four executable seam references fail, guard comments/docs naming the same symbols pass, the complete test file passes, and the real repository isolation command stays green.</acceptance_criteria>
  <verify>
    <automated>cd accrue && mix test test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs --only guard_surface_red_path --trace && mix test test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs && cd .. && bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>D-15 covers Guard as part of the real resolver/guard grant path, closing the SYNC-02/SYNC-03 proof gap without changing observational runtime behavior.</done>
</task>

</tasks>

<threat_model>
## ASVS Policy

OWASP ASVS Level 1 applies. Critical and high-severity threats are completion-blocking and must be mitigated before this plan can pass.

## Trust Boundaries

| Boundary | Description |
|---|---|
| future grant-path source edit → merge gate | Executable references added to Guard are untrusted changes that must be rejected when they cross into the advisory seam. |
| Elixir prose → executable-reference scanner | Documentation and comments must remain expressive without creating a bypass for executable source. |
| hermetic fixture → repository script | The test-controlled ROOT_DIR must exercise the exact production scanner without touching the working tree or external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-213-13 | Elevation of Privilege | `Accrue.Entitlements.Guard` | high | mitigate | Add Guard to `gate_path_files` and block completion until each advisory symbol has a proven executable red path. |
| T-213-14 | Tampering | isolation scanner and test fixture | high | mitigate | Exercise the production script through ROOT_DIR fixtures, assert the exact token for four independent injections, and retain the clean-repository command. |
| T-213-15 | Denial of Service | comment/moduledoc filtering | low | accept | The existing bounded awk scan skips prose; a Guard-specific regression proves legitimate architecture documentation remains accepted. |
| T-213-SC | Tampering | package installs | high | mitigate | No package-manager operation, dependency, or external artifact enters this plan. |
</threat_model>

## Artifacts this phase produces

- Modified `gate_path_files` in `scripts/ci/verify_entitlement_sync_isolation.sh`, adding the existing `accrue/lib/accrue/entitlements/guard.ex` path.
- Modified `@gate_files` and one `module_for/1` clause in `Accrue.Entitlements.EntitlementSyncIsolationGuardTest` for the Guard fixture.
- New tagged ExUnit guard-surface red-path case covering executable `list_active_entitlements`, `Reconcile`, `StripeSync`, and `EntitlementSummary` references.
- Guard-specific comment/moduledoc allowance coverage for the same four symbols.
- No new files, production modules, public functions, schemas, migrations, dependencies, queues, UI surfaces, live Stripe calls, or Chrome tests.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Client-backed opt-in advisory refresh with proof it cannot become a grant gate | 213-01..05 | COVERED | Plan 05 closes the final incomplete grant-module scan edge. |
| REQ | SYNC-01 | Client-backed refresh writes the advisory cache | 213-01, 213-02, 213-04 | COVERED | Completed implementation remains unchanged. |
| REQ | SYNC-02 | Off-by-default observational sync; resolver/guard never consumes advisory output | 213-01..05 | COVERED | Plan 05 claims the requirement only for the missing Guard isolation proof. |
| REQ | SYNC-03 | Isolation assertion covers the client fetch/shared writer and fails future gate use | 213-03, 213-05 | COVERED | Plan 05 adds the omitted Guard path and four live red paths. |
| REQ | SYNC-04 | Close the fetch_entitled/2 ambiguity | 213-03 | COVERED | Completed will-not-build decision remains unchanged. |
| REQ | SYNC-05 | Fake/Test-only cache, grant-invariance, and default-off proof | 213-01..04 | COVERED | Already satisfied; Plan 05 does not claim it. |
| RESEARCH | isolation guard | Forbidden symbols require a hermetic executable failure-path proof | 213-03, 213-05 | COVERED | Plan 05 applies the established ROOT_DIR pattern to Guard. |
| RESEARCH | no external lanes | Tests use local Fake/Test or hermetic fixtures, never live Stripe or Chrome | 213-01..05 | COVERED | Plan 05 uses only shell and ExUnit fixtures and remains async-safe. |
| RESEARCH | remaining architecture | Facade, exhaustive stream, shared writer, worker, provenance, and ordering constraints | 213-01..04 | COVERED | Completed work is preserved; no reopening is required. |
| CONTEXT | D-01, D-11 | Local plan-to-feature state remains the sole grant authority | 213-01..05 | COVERED | Plan 05 protects the shared Guard enforcement path. |
| CONTEXT | D-15 | Guard rejects executable client-fetch/shared-writer references | 213-03, 213-05 | COVERED | The final verification failure is directly closed. |
| CONTEXT | D-02..D-14 | Optional facade, Stripe/Fake adapters, refresh, worker, reconciliation, and predicate closure | 213-01..04 | COVERED | Existing completed plans implement these locked decisions. |
| CONTEXT | deferred | Admin refresh button, scheduler, top-level facade, truncated-webhook pagination | NONE | EXCLUDED | Explicitly deferred/out of Phase 213 scope. |

<verification>
Run the guard-surface red-path proof exactly:

`cd accrue && mix test test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs --only guard_surface_red_path --trace`

Run the complete hermetic guard regression exactly:

`cd accrue && mix test test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs`

Prove the unchanged repository is green exactly:

`bash scripts/ci/verify_entitlement_sync_isolation.sh`

The plan is complete only when all three commands exit 0, the tagged test internally observes non-zero script status for all four executable Guard injections, and the prose-only Guard fixture plus the real repository both print the OK marker. These checks are local, hermetic, async-safe, and require neither live Stripe nor Chrome.
</verification>

<success_criteria>
The final verification truth is satisfied: `Accrue.Entitlements.Guard` is part of the static isolation boundary, all four executable advisory references are proven to fail there, comments/docs remain allowed, and the clean repository gate remains green.
</success_criteria>

<output>
Create `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-05-SUMMARY.md` when done.
</output>

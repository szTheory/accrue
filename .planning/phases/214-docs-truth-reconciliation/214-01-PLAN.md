---
phase: 214-docs-truth-reconciliation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - accrue/guides/jobs_to_be_done.md
  - .planning/research/JTBD-FRONTIER.md
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
  - accrue/guides/entitlements.md
  - .planning/processor-support-matrix.md
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "Current stack surfaces state the declared lattice_stripe constraint as ~> 2.0 and describe the shipped 2.x entitlements support (DOCS-01, D-13)."
    - "Current JTBD and entitlement surfaces state that Stripe-native sync shipped as optional, default-off, observational diagnostics while the local plan-to-feature map remains the only Accrue grant authority (DOCS-02, D-01, D-02, D-03)."
    - "Support, adoption-proof, and active planning mirrors describe the same shipped advisory capability and Phase 213's final passed status without rewriting dated evidence (DOCS-03, D-13, D-14, D-15)."
    - "The existing package-doc verifier rejects scoped current-surface regressions in the dependency pin, sync status, and authorization boundary, and focused fixtures prove those guards fail red (D-16, D-17, D-18)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Current lattice_stripe stack and compatibility-matrix truth"
    - path: "accrue/guides/jobs_to_be_done.md"
      provides: "Public shipped/observational JTBD status"
    - path: ".planning/research/JTBD-FRONTIER.md"
      provides: "Planning-side shipped/observational JTBD mirror"
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Executable current-truth contract"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "Non-vacuous red-path proof for documentation drift guards"
    - path: ".planning/processor-support-matrix.md"
      provides: "Maintainer-facing local-gate/advisory-sync capability split"
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "Merge-blocking proof location for the advisory lane"
  key_links:
    - from: "scripts/ci/verify_package_docs.sh"
      to: "CLAUDE.md and current JTBD/support/adoption docs"
      via: "scoped require_fixed/require_regex/require_absent_regex assertions"
    - from: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      to: "scripts/ci/verify_package_docs.sh"
      via: "live-repo success run and ROOT_DIR-backed mutated fixtures"
    - from: ".planning/processor-support-matrix.md"
      to: "scripts/ci/verify_entitlement_sync_isolation.sh"
      via: "documented observational boundary backed by the existing isolation gate"
  assumptions:
    - requirement_id: DOCS-01
      status: unresolved
      flag: "Spec-less edge probe classified this requirement as unclassified; the plan assumes scoped stack-row and compatibility-cell assertions cover its meaningful version-drift boundary."
    - requirement_id: DOCS-02
      status: unresolved
      flag: "Spec-less edge probe classified this requirement as unclassified; the plan assumes positive authority/default/advisory facts plus focused red fixtures cover its meaningful semantic boundary."
    - requirement_id: DOCS-03
      status: unresolved
      flag: "Spec-less edge probe classified this requirement as unclassified; the plan assumes support/adoption/planning mirror checks plus Plan 02 release/metadata checks cover its cross-surface boundary."
  prohibitions:
    - requirement_id: DOCS-02
      category: transparency
      status: unresolved
      statement: "MUST NOT present Stripe-native advisory data as Accrue grant authority or imply that it changes entitlement, plug, or LiveView guard decisions."
    - requirement_id: DOCS-01
      category: transparency
      status: unresolved
      statement: "MUST NOT rewrite dated phase, seed, archive, review, or verification evidence merely because it records an older dependency pin or deferred status."
    - requirement_id: DOCS-03
      category: transparency
      status: unresolved
      statement: "MUST NOT claim an admin- or portal-owned workflow, API, grant authority, numbered release, or package version that has not shipped."
---

<objective>
Establish the production-quality cross-surface truth contract for the shipped lattice_stripe 2.x bump and observational entitlements sync, then expand it through current support, adoption, and planning mirrors.

Purpose: Close DOCS-01 and DOCS-02 and the mirror portion of DOCS-03 with an executable contract that prevents current public truth from drifting while preserving historical evidence.
Output: Correct current stack/JTBD/support/adoption/planning prose plus focused package-doc verifier coverage and red-path fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md
@.planning/phases/214-docs-truth-reconciliation/214-RESEARCH.md
@.planning/phases/214-docs-truth-reconciliation/214-PATTERNS.md
@.planning/phases/214-docs-truth-reconciliation/214-VALIDATION.md
@.planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md
@.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md
</context>

<assumption_delta_decision>
Primary noun: local plan→feature grant authority.
Decision: no-change.
Rationale: The detector fired on the quoted word "optional", but this phase changes documentation status only; Stripe-native sync remains advisory and the local plan→feature map remains the sole grant authority.
</assumption_delta_decision>

<schema_gate>
No schema-relevant Payload, Prisma, Drizzle, Supabase, or TypeORM paths occur in the phase scope; no schema push task applies.
</schema_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one current-truth path from stack pin through JTBD status to CI rejection</name>
  <reversibility rating="one-way">D-01 records the already-approved v1.x public authorization contract; this task publishes that locked decision and therefore records, but does not re-ask, the one-way choice.</reversibility>
  <files>CLAUDE.md, accrue/guides/jobs_to_be_done.md, .planning/research/JTBD-FRONTIER.md, scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-01..D-04, D-13..D-18 and the exact current-versus-history scope
    - .planning/phases/212-lattice-stripe-2-x-bump-green-reconciliation/212-UPGRADE-EVIDENCE.md — authoritative declared `~> 2.0` and resolved 2.1.0 evidence
    - .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md — final 13/13 shipped sync evidence
    - CLAUDE.md — modify the `:lattice_stripe` Technology Stack row and Version Compatibility Matrix anchor cell
    - accrue/guides/jobs_to_be_done.md — modify the current entitlements JTBD row/status
    - .planning/research/JTBD-FRONTIER.md — modify the current planning JTBD mirror
    - scripts/ci/verify_package_docs.sh — extend the existing helper-based docs contract; retain its stderr prefix
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — extend the existing live success and ROOT_DIR fixture pattern
  </read_first>
  <behavior>
    - Test 1: a clean repository with the `~> 2.0` current stack pin and shipped observational JTBD statements passes `verify_package_docs.sh`.
    - Test 2: a fixture that regresses the current compatibility cell to an older lattice_stripe line fails with the stable `[verify_package_docs]` prefix.
    - Test 3: a fixture that reintroduces deferred sync status or grant-authority wording into a current JTBD surface fails with the stable verifier prefix.
  </behavior>
  <action>
    Start RED by adding focused ROOT_DIR-backed fixture mutations for the current compatibility cell and JTBD authority/status semantics, then run the focused ExUnit file and confirm the new cases fail. In `CLAUDE.md`, set both the Technology Stack dependency row and Version Compatibility Matrix anchor to the declared `~> 2.0` constraint; distinguish that declared constraint from the current 2.1.0 lock resolution and mention the 2.x active-entitlements support per DOCS-01 and D-13. In both JTBD surfaces, replace the current Phase 127 deferral status with shipped/observational language per DOCS-02 and D-01..D-04: the local plan→feature map is the Accrue grant gate; sync is optional and default-off; its cache supports diagnostics/admin reads; it never changes the named grant APIs, plugs, or guards. Extend `verify_package_docs.sh` with narrowly scoped positive and negative helpers over these current files per D-15..D-18; do not scan dated phase/archive/seed evidence and do not freeze full paragraphs. Finish GREEN by running the focused tests and direct verifier.
  </action>
  <acceptance_criteria>
    - Source assertion: both current `CLAUDE.md` lattice_stripe version surfaces declare `~> 2.0`, with 2.x entitlements context and no conflation with the resolved lock version.
    - Behavior assertion: both JTBD files state shipped/observational and preserve all four D-01 facts in audience-appropriate wording.
    - Test assertion: at least one version-regression fixture and at least one sync-status/authority-regression fixture fail before the implementation and pass after it.
    - CLI assertion: `bash scripts/ci/verify_package_docs.sh` exits 0 and retains its existing success/failure prefix contract.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/package_docs_verifier_test.exs &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>The smallest complete current-truth path—declared version, shipped advisory status, authorization boundary, verifier, and non-vacuous red fixtures—is green end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the authority contract through adopter and maintainer proof mirrors</name>
  <reversibility rating="costly">Changing the cross-document authorization wording later requires a coordinated public/support/proof update, though all edits remain source-controlled.</reversibility>
  <files>accrue/guides/entitlements.md, .planning/processor-support-matrix.md, examples/accrue_host/docs/adoption-proof-matrix.md, scripts/ci/verify_package_docs.sh, accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-01..D-04, D-13..D-18
    - accrue/lib/accrue/entitlements/stripe_sync.ex — shipped module contract and default-off/advisory semantics
    - scripts/ci/verify_entitlement_sync_isolation.sh — executable proof that the cache cannot enter grant paths
    - accrue/guides/entitlements.md — modify the primary adopter explanation
    - .planning/processor-support-matrix.md — modify the canonical local-mapping and Stripe-native-sync capability rows/prose
    - examples/accrue_host/docs/adoption-proof-matrix.md — modify the proof-location mirror
    - scripts/ci/verify_package_docs.sh — extend Task 1's scoped semantic assertions
    - accrue/test/accrue/docs/package_docs_verifier_test.exs — add focused mirror regressions using the established fixture seed
  </read_first>
  <behavior>
    - Test 1: current entitlement/support/adoption docs all preserve local mapping as the sole grant path and Stripe sync as a separate advisory lane.
    - Test 2: a fixture that makes the advisory lane authoritative or says it changes a grant API fails the package-doc verifier.
    - Test 3: the adoption proof identifies the existing merge-blocking docs/isolation verification location rather than claiming live Stripe proof.
  </behavior>
  <action>
    Add failing fixture mutations first, then update the three current surfaces. In `entitlements.md`, define the advisory cache once, explain refresh and reconciliation after the authorization fact, and preserve the closed `fetch_entitled/2` decision per D-01..D-04. In the processor matrix, retain `entitlements.local_mapping` as the convergence/grant row and `entitlements.stripe_native_sync` as the Stripe-only native advisory divergence row. In the adoption proof, identify what is proven, where `verify_package_docs.sh` and `verify_entitlement_sync_isolation.sh` prove it, and which existing CI layer blocks regressions. Extend the existing verifier/test family with semantic tokens and focused negative fixtures per D-16..D-18; do not add a new script or CI lane.
  </action>
  <acceptance_criteria>
    - Source assertion: all three documents explicitly distinguish canonical local grants from advisory Stripe diagnostics.
    - Source assertion: the proof matrix names the deterministic existing verifier/isolation proof and does not imply a live-Stripe merge gate.
    - Test assertion: an authority inversion and a grant-invariance contradiction each produce non-zero verifier status in ROOT_DIR-backed fixtures.
    - CLI assertion: the package-doc verifier, focused ExUnit file, processor-support verifier, and entitlement isolation verifier all exit 0 on the repository.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/package_docs_verifier_test.exs &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_package_docs.sh &amp;&amp; bash scripts/ci/verify_processor_support_matrix.sh &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>Adopter guidance, support capability truth, and proof-location truth all express and enforce the same observational-only architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile active planning status without altering dated evidence</name>
  <reversibility rating="reversible">The active planning mirrors are scoped text/status edits that can be locally reverted without migration or public API breakage.</reversibility>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-13..D-15 current-truth boundary
    - .planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md — final passed 13/13 status
    - .planning/ROADMAP.md — modify only active v1.58 and Phase 213/214 current status/plan inventory
    - .planning/REQUIREMENTS.md — modify Phase 213 traceability statuses from intermediate gaps to final complete
    - .planning/STATE.md — preserve concise active milestone status and guardrail prose
    - .planning/seeds/SEED-005-lattice-stripe-entitlements-bump.md — historical source to leave unchanged
  </read_first>
  <action>
    Correct only active/current v1.58 mirrors per D-13: the checked Phase 213 roadmap summary and the active v1.58 STATE section must explicitly say its final re-verification passed 13/13 truths, and all five Phase 213 traceability rows in REQUIREMENTS must be `Complete`; Phase 214 remains the active executing phase until the normal GSD completion transition. Preserve requirement IDs, table shapes, the Phase 214 goal/criteria, and the observational-only guardrail. Per D-14 and D-15, do not edit completed phase plans/contexts/research/summaries/reviews/verifications, archived milestones, retrospectives, upgrade evidence, or SEED-005; classify their older wording as dated evidence during the final cold-read.
  </action>
  <acceptance_criteria>
    - Source assertion: active ROADMAP, REQUIREMENTS, and STATE status mirrors agree that Phase 213 and SYNC-01..05 are complete/passed.
    - Source assertion: Phase 214 remains current/in progress until executor completion and retains DOCS-01..03 mapping.
    - Scope assertion: `git diff --name-only` contains no dated phase/archive/seed evidence file as a result of this task.
    - CLI assertion: scoped grep output over the three active planning files contains no intermediate Phase 213 gap status.
  </acceptance_criteria>
  <verify>
    <automated>node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 214 &amp;&amp; bash -ceu 'roadmap=$(awk "/^### .*v1\\.58 lattice_stripe/{active=1} /^## Historical Backlog Anchors/{active=0} active" .planning/ROADMAP.md); requirements=$(cat .planning/REQUIREMENTS.md); state=$(awk "/### v1\\.58 Phase Summary/{active=1} /### v1\\.57 Phase Summary/{active=0} active" .planning/STATE.md); grep -Eq "^- \\[x\\] \\*\\*Phase 213:.*final re-verification passed 13/13 truths" &lt;&lt;&lt;"$roadmap"; ! grep -Eq "Phase 213.*([Gg]aps [Ff]ound|intermediate)" &lt;&lt;&lt;"$roadmap"; for id in 01 02 03 04 05; do grep -Eq "^\\| SYNC-${id} \\| Phase 213 .* \\| Complete \\|$" &lt;&lt;&lt;"$requirements"; done; test "$(grep -Ec "^\\| SYNC-0[1-5] \\| Phase 213 .* \\| Complete \\|$" &lt;&lt;&lt;"$requirements")" -eq 5; ! grep -Eq "^\\| SYNC-0[1-5] .* \\| (Gaps Found|Pending|In Progress) \\|$" &lt;&lt;&lt;"$requirements"; grep -Eq "^\\| 213 \\| .* \\| SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05 \\| Complete \\|$" &lt;&lt;&lt;"$state"; grep -Fq "Phase 213 final re-verification passed 13/13 truths" &lt;&lt;&lt;"$state"; grep -Fq "**Current focus:** Phase 214 — Docs &amp; truth reconciliation" .planning/STATE.md; ! grep -Eq "Phase 213.*([Gg]aps [Ff]ound|intermediate)" &lt;&lt;&lt;"$state"'</automated>
  </verify>
  <done>Active planning mirrors report the final shipped state while all dated evidence remains intact and visibly historical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public/current docs → adopter authorization design | Readers may implement access control from wording; documentation must not elevate advisory Stripe state into a grant decision. |
| Historical evidence → current-truth sweep | Broad replacements could corrupt audit provenance by rewriting what earlier phases actually knew. |
| Verifier fixtures → CI claim | A vacuous positive-only check could claim drift protection without proving regressions fail. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-01 | Tampering | Current entitlement/JTBD/support prose | high | mitigate | Encode D-01's local-only grant authority, default-off advisory cache, and grant invariance in scoped verifier assertions plus red-path fixtures. |
| T-214-02 | Repudiation | Dated planning/seed/archive evidence | medium | mitigate | Restrict edits and negative checks to enumerated current surfaces; preserve historical files and inspect the scoped diff. |
| T-214-03 | Spoofing | Adoption proof claims | medium | mitigate | Name the actual existing verifier/isolation scripts and CI ownership; do not substitute live-service or prose-only proof. |
| T-214-04 | Information Disclosure | Documentation-only changes | low | accept | No secrets or runtime payloads are introduced; current docs continue to avoid credentials and sensitive Stripe fields. |
</threat_model>

## Artifacts this phase produces

This plan creates no new runtime symbols, functions, CLI flags, schema, or external API surface.

- Modified public/current documentation paths: `CLAUDE.md`, `accrue/guides/jobs_to_be_done.md`, `.planning/research/JTBD-FRONTIER.md`, `accrue/guides/entitlements.md`, `.planning/processor-support-matrix.md`, `examples/accrue_host/docs/adoption-proof-matrix.md`.
- Modified active planning mirrors: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`.
- Extended verifier contract: scoped lattice_stripe version, shipped/observational status, local grant authority, default-off advisory cache, and grant-invariance assertions in `scripts/ci/verify_package_docs.sh`.
- Extended test/verifier contract: ROOT_DIR-backed red fixtures in `accrue/test/accrue/docs/package_docs_verifier_test.exs`.
- Planning gate file already produced for execution/seal: `.planning/phases/214-docs-truth-reconciliation/COVERAGE.md` declaring that no new external API integration exists.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | One current version and sync-status story across public/planning surfaces | 01, 02 | COVERED | Tracer establishes the contract; Plan 02 expands release/API metadata. |
| REQ | DOCS-01 | CLAUDE stack row and compatibility cell use `~> 2.0` | 01 | COVERED | Task 1 plus verifier red path. |
| REQ | DOCS-02 | JTBD deferral flips to shipped/observational without authority overstatement | 01 | COVERED | Tasks 1-2 plus semantic verifier fixtures. |
| REQ | DOCS-03 | Release, ExDoc, support, adoption, and planning mirrors agree | 01, 02 | COVERED | Plan 01 covers mirrors; Plan 02 covers release and metadata. |
| RESEARCH | — | Extend existing verifier/test families, not a new CI lane | 01, 02 | COVERED | Tasks explicitly use package-doc and release-note owners. |
| RESEARCH | — | Preserve historical evidence under a scoped sweep | 01 | COVERED | Task 3 and scoped assertions. |
| CONTEXT | D-01..D-04 | Canonical authorization wording and voice | 01 | COVERED | Tasks 1-2. |
| CONTEXT | D-05..D-12 | Release ownership and exact public `since` metadata | 02 | COVERED | Plan 02 Tasks 1-3. |
| CONTEXT | D-13..D-19 | Current/history boundary and drift prevention | 01, 02 | COVERED | Both plans use scoped verifiers and cold-read. |

<verification>
After each task, run its focused command. At wave completion run:
`bash scripts/ci/verify_package_docs.sh &amp;&amp; bash scripts/ci/verify_release_notes_contract.sh &amp;&amp; bash scripts/ci/verify_processor_support_matrix.sh &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh`.
</verification>

<success_criteria>
- DOCS-01 and DOCS-02 are mechanically enforced on current surfaces.
- The DOCS-03 support/adoption/planning mirrors agree with shipped Phase 212/213 evidence.
- Every scoped negative guard has a focused fixture that proves it can fail.
- Historical phase/archive/seed evidence is untouched.
- All three unresolved probe edges and all three descriptor-less recalled prohibitions remain explicitly flagged for verification review.
</success_criteria>

<output>
Create `.planning/phases/214-docs-truth-reconciliation/214-01-SUMMARY.md` when done.
</output>

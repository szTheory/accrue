---
phase: 214-docs-truth-reconciliation
plan: "03"
type: execute
wave: 3
depends_on: ["214-02"]
files_modified:
  - scripts/ci/verify_release_notes_contract.sh
  - accrue/test/accrue/docs/release_notes_contract_test.exs
autonomous: true
gap_closure: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "DOCS-03 / Truth 6: the release-note contract accepts both the checked-in linked 1.4.0 pre-release state and a fully aligned Release Please-generated 1.5.0 candidate state."
    - "All three package versions remain valid SemVer and lockstep; a mismatch or malformed version fails before it can influence shell matching or command construction."
    - "The pre-release state keeps D-05..D-08 Unreleased ownership, while the candidate state proves each package has a matching generated numbered section and the linked 1.5.0 release story remains discoverable."
    - "Release Please remains the only writer of package versions and numbered changelog content; no generated candidate artifact is a manual setup prerequisite."
  artifacts:
    - path: "scripts/ci/verify_release_notes_contract.sh"
      provides: "Release-state-aware linked-version, changelog, and release-note contract"
      contains: "verify_release_notes_contract:"
    - path: "accrue/test/accrue/docs/release_notes_contract_test.exs"
      provides: "Current-state, generated-candidate, mismatch, malformed-version, and incomplete-candidate regression fixtures"
      contains: "1.5.0"
  key_links:
    - from: "accrue/test/accrue/docs/release_notes_contract_test.exs"
      to: "scripts/ci/verify_release_notes_contract.sh"
      via: "ROOT_DIR temporary repository fixtures exercising the same script used by CI and Release Please"
      pattern: "ROOT_DIR"
    - from: "scripts/ci/verify_release_notes_contract.sh"
      to: "accrue/mix.exs, accrue_admin/mix.exs, accrue_portal/mix.exs"
      via: "strict parsed SemVer equality before state selection"
      pattern: "extract_version"
    - from: "scripts/ci/verify_release_notes_contract.sh"
      to: "accrue/CHANGELOG.md, accrue_admin/CHANGELOG.md, accrue_portal/CHANGELOG.md"
      via: "pre-release Unreleased checks or candidate matching-numbered-section checks selected from aligned package state"
      pattern: "changelog"
  assumptions:
    - requirement_id: DOCS-01
      status: unresolved
      flag: "The spec-less edge probe classified DOCS-01 as unclassified; Plans 01-02 already satisfy its current `lattice_stripe ~> 2.0` surface, and this gap plan assumes that truth remains unchanged."
    - requirement_id: DOCS-02
      status: unresolved
      flag: "The spec-less edge probe classified DOCS-02 as unclassified; Plans 01-02 already satisfy its shipped/observational authority boundary, and this gap plan assumes candidate release prose preserves it."
    - requirement_id: DOCS-03
      status: unresolved
      flag: "The spec-less edge probe classified DOCS-03 as unclassified; this plan explicitly treats current-versus-candidate transition behavior, invalid SemVer, package mismatch, missing numbered candidate sections, and release-story linkage as contract edges."
  prohibitions:
    - requirement_id: DOCS-03
      category: transparency
      status: unresolved
      statement: "MUST NOT hard-pin the verifier to the currently published package version or reject a fully aligned Release Please candidate merely because generated numbered sections now exist."
    - requirement_id: DOCS-03
      category: transparency
      status: unresolved
      statement: "MUST NOT weaken lockstep package alignment, matching changelog coverage, three-package release-note discoverability, or Release Please ownership of version and numbered-release writes."
    - requirement_id: DOCS-02
      category: authorization
      status: unresolved
      statement: "MUST NOT alter the documented local plan-to-feature grant authority or imply that Stripe-native advisory data changes entitlement, plug, or LiveView guard decisions."
---

<objective>
Close the verified DOCS-03 release-pipeline gap by making the existing release-note contract accept the repository's current pre-release state and the aligned Release Please 1.5.0 candidate it is intended to validate.

Purpose: Restore a usable merge/release gate without weakening linked-package alignment, changelog/release-note linkage, or the Release Please single-writer boundary established by D-05..D-08 and enforced through D-16..D-18.
Output: A release-state-aware shell contract and non-vacuous current/candidate/invalid-state ExUnit fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md
@.planning/phases/214-docs-truth-reconciliation/214-RESEARCH.md
@.planning/phases/214-docs-truth-reconciliation/214-PATTERNS.md
@.planning/phases/214-docs-truth-reconciliation/214-VERIFICATION.md
@.planning/phases/214-docs-truth-reconciliation/214-02-SUMMARY.md
@RELEASING.md
@release-please-config.json
@.release-please-manifest.json
@scripts/ci/verify_release_notes_contract.sh
@accrue/test/accrue/docs/release_notes_contract_test.exs
</context>

<assumption_delta_decision>
Primary noun: local plan→feature grant authority.
Decision: no-change.
Rationale: This correction changes only how a release-state verifier recognizes pre-release and generated candidate repository states; it does not generalize an identity model or change authorization semantics.
</assumption_delta_decision>

<schema_gate>
No schema-relevant files occur in this shell-contract and fixture correction; no schema task applies.
</schema_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one aligned Release Please candidate end to end</name>
  <reversibility rating="costly">The verifier is merge-blocking and release-workflow invoked, so changing its state model affects every linked package release even though the two-file implementation is straightforward to revert.</reversibility>
  <files>scripts/ci/verify_release_notes_contract.sh, accrue/test/accrue/docs/release_notes_contract_test.exs</files>
  <read_first>
    - .planning/phases/214-docs-truth-reconciliation/214-VERIFICATION.md — authoritative Truth 6 failure and candidate-state evidence
    - .planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md — D-05..D-08 release allocation/ownership and D-16..D-18 verifier rules
    - .planning/phases/214-docs-truth-reconciliation/214-02-SUMMARY.md — existing release truth and fixture patterns to preserve
    - RELEASING.md — linked Release Please lifecycle, Unreleased drain/freeze rule, and single-writer boundary
    - release-please-config.json — authoritative linked three-package configuration
    - .release-please-manifest.json — checked-in published-line evidence, not a constant to hard-pin in the verifier
    - scripts/ci/verify_release_notes_contract.sh — remove pre-release-only assumptions across version, Unreleased, numbered-section, and release-note section checks
    - accrue/test/accrue/docs/release_notes_contract_test.exs — extend the existing ROOT_DIR fixture rather than adding a new verifier family
  </read_first>
  <behavior>
    - Test 1 (RED baseline): the untouched checked-in 1.4.0 repository fixture exits 0 and retains all D-05..D-08 pre-release Unreleased/ownership checks.
    - Test 2 (RED candidate): a temporary fixture programmatically changes all three package versions to 1.5.0 and promotes each package's Unreleased content into a matching generated 1.5.0 changelog section; the existing verifier fails before implementation and must exit 0 after GREEN.
    - Test 3 (candidate linkage): the aligned candidate succeeds using the existing hand-authored 1.5.0 story and three changelog links; it does not require hand-authored admin/portal release-note headings or any manually prepared Release Please output outside the fixture.
    - Test 4 (ownership): the live checkout is not mutated; only the temporary fixture simulates Release Please's generated version and numbered-changelog writes.
  </behavior>
  <action>
    Start RED by adding a named candidate-state test and a deterministic fixture helper that operates only under the test's temporary ROOT_DIR: update all three copied `mix.exs` version declarations to `1.5.0`, convert/copy each copied changelog's D-05/D-06 Unreleased payload into a matching `## [1.5.0]` generated release section, and leave the real repository untouched. Prove this exact fixture fails against the current script for the verified reason before changing production code.

    Then implement GREEN in `verify_release_notes_contract.sh`. Keep strict equality across all three parsed package versions, but replace the three fixed `1.4.0` comparisons with a state-aware contract. Validate the parsed aligned value as strict stable SemVer before using it in awk/grep comparisons. In the checked-in pre-release state, continue requiring top-level Unreleased ownership, forbid a premature numbered 1.5.0 section, and require the existing current-version release-note coverage. In the aligned 1.5.0 candidate state, accept Release Please's version updates, require a matching numbered 1.5.0 section in every package changelog, validate the D-05 substantive-core versus D-06 compatibility-only ownership tokens in the matching generated sections (not only in Unreleased), and keep the existing three-link plus 1.5.0 story checks. Do not require Release Please-generated numbered content to exist before the release PR, and do not require a human to create candidate files. Preserve the stable failure prefix and ROOT_DIR override per D-16..D-18.
  </action>
  <acceptance_criteria>
    - Red evidence: the new aligned-candidate test fails against the pre-change script because the gate assumes the pre-release state.
    - Green evidence: both the untouched current 1.4.0 fixture and the programmatically generated aligned 1.5.0 candidate fixture exit 0.
    - State evidence: pre-release assertions inspect Unreleased content; candidate assertions inspect each matching numbered section and tolerate a drained/absent Unreleased section.
    - Ownership evidence: core candidate content remains substantive and admin/portal content remains compatibility-only per D-05/D-06.
    - Scope evidence: only the verifier and its existing focused ExUnit file are modified; no package version, changelog, release note, Release Please config, manifest, or workflow file changes.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/release_notes_contract_test.exs</automated>
  </verify>
  <done>The exact generated 1.5.0 candidate that previously failed now passes end to end through the same script used by merge CI and Release Please, while the checked-in 1.4.0 state remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject malformed, mismatched, and incomplete release states</name>
  <reversibility rating="reversible">These focused negative fixtures and validation branches are local verifier details behind the established release contract.</reversibility>
  <files>scripts/ci/verify_release_notes_contract.sh, accrue/test/accrue/docs/release_notes_contract_test.exs</files>
  <read_first>
    - scripts/ci/verify_release_notes_contract.sh — harden the state-aware helpers from Task 1 without duplicating release-state parsing
    - accrue/test/accrue/docs/release_notes_contract_test.exs — reuse the current/candidate fixture helpers and stable failure assertions from Task 1
    - RELEASING.md — preserve lockstep and matching package-local changelog requirements
    - release-please-config.json — preserve the linked three-package ownership model
  </read_first>
  <behavior>
    - Test 1: changing only one package to 1.5.0 fails with the existing linked-version divergence label.
    - Test 2: a malformed or injection-shaped package version fails the strict SemVer check before the value is interpolated into a matcher or executed as shell syntax.
    - Test 3: an otherwise aligned 1.5.0 candidate missing one package's matching numbered section fails with that package path and version in the diagnostic.
    - Test 4: an aligned candidate whose core 1.5.0 section loses substantive advisory-sync truth, or whose admin/portal 1.5.0 section claims capability ownership, fails with the stable verifier prefix.
    - Test 5: a 1.4.0 pre-release fixture with a premature numbered 1.5.0 section continues to fail, proving Release Please retains numbered-section ownership.
    - Test 6: ROOT_DIR containing spaces succeeds, demonstrating fixed path joins and quoting across the environment-to-filesystem boundary.
  </behavior>
  <action>
    Add the negative tests first, using isolated temporary trees and one mutation per assertion so each failure label identifies the violated invariant. Harden the Task 1 state model without using `eval`, `source`, dynamic command construction, or version-derived file paths: ROOT_DIR may select only the fixed repository-relative inputs already owned by this verifier, every path remains quoted, and version text must pass strict SemVer validation before fixed-string/awk comparison. Keep the existing cross-package equality diagnostics. Add explicit candidate checks for a matching numbered section in each changelog and apply the existing D-05/D-06 content validators to the active release section selected for that state. Retain the pre-release premature-numbered-section rejection, but scope it to the pre-release branch so Release Please's aligned candidate is accepted. Do not loosen the portal changelog link, next-release story, stable-core posture, or canonical advisory-authority assertions already established by Plans 01-02.
  </action>
  <acceptance_criteria>
    - Test assertion: aligned current and candidate happy paths pass; mismatch, malformed version, missing numbered section, ownership inversion, and premature pre-release numbering each fail non-zero.
    - Diagnostic assertion: every failure uses `verify_release_notes_contract:` and identifies the package/version or invariant involved without echoing executable attacker-controlled content.
    - Security assertion: the script never evaluates version or ROOT_DIR-derived text as shell code and accesses only quoted fixed relative paths beneath ROOT_DIR.
    - Contract assertion: all existing release-note contract tests remain green, including portal discovery, 1.5.0 story, stable-core posture, and companion compatibility-only semantics.
    - Integration assertion: direct live verifier, focused ExUnit suite, package-doc verifier, support-matrix verifier, and entitlement-isolation verifier all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/docs/release_notes_contract_test.exs &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_release_notes_contract.sh &amp;&amp; bash scripts/ci/verify_package_docs.sh &amp;&amp; bash scripts/ci/verify_processor_support_matrix.sh &amp;&amp; bash scripts/ci/verify_entitlement_sync_isolation.sh</automated>
  </verify>
  <done>The release gate accepts exactly the intended linked current/candidate states and rejects invalid or incomplete states without weakening docs, ownership, or authorization boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ROOT_DIR` environment → fixed repository paths | Tests override the root; the script must treat it only as a quoted directory prefix and never as shell code. |
| Parsed `@version` text → shell/awk/grep state selection | Repository text becomes control data; malformed or injection-shaped values must fail strict SemVer validation before use. |
| Temporary candidate fixture → release contract | Generated fixture content models Release Please output and must not mutate the checkout or become a manual prerequisite. |
| Release Please candidate → merge/release CI | The gate must admit an aligned generated release while rejecting partial or spoofed package transitions. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-09 | Tampering | Parsed package versions and state selection | high | mitigate | Strictly validate stable SemVer, compare all three values for equality, and red-test malformed/injection-shaped values before they reach matchers. |
| T-214-10 | Elevation of Privilege | `ROOT_DIR` environment override | high | mitigate | Use only quoted fixed relative paths beneath ROOT_DIR; prohibit eval/source/dynamic commands and verify a root path containing spaces. |
| T-214-11 | Spoofing | Incomplete linked 1.5.0 candidate | high | mitigate | Require matching numbered sections and correct core-versus-companion ownership in all three changelogs before candidate acceptance. |
| T-214-12 | Repudiation | Numbered changelog ownership | medium | mitigate | Retain premature numbering rejection in pre-release state and accept numbered 1.5.0 only when all package versions are aligned to that candidate. |
| T-214-13 | Denial of Service | Valid Release Please candidate blocked by pre-release-only checks | high | mitigate | Exercise an exact generated candidate fixture through the production verifier and keep the test in the merge-blocking focused suite. |
| T-214-14 | Information Disclosure | Verifier diagnostics | low | accept | Inputs are repository-local version/path metadata; diagnostics remain bounded to invariant/package labels and do not print file contents or secrets. |
</threat_model>

## Artifacts this phase produces

- New paths: none.
- New public/runtime symbols: none.
- Expected internal shell helpers in `scripts/ci/verify_release_notes_contract.sh`: a strict stable-SemVer validator and a state-selected changelog-section reader; exact private helper names may follow the file's existing shell naming pattern.
- Expected internal ExUnit helper in `accrue/test/accrue/docs/release_notes_contract_test.exs`: a deterministic temporary-fixture promotion helper for the aligned 1.5.0 Release Please candidate; exact private helper name may follow the file's existing `setup_release_fixture!/1` pattern.
- New regression cases: current 1.4.0 pass, generated aligned 1.5.0 candidate pass, package mismatch fail, malformed/injection-shaped version fail, incomplete candidate fail, ownership inversion fail, premature pre-release numbering fail, and spaced ROOT_DIR pass.
- Planning/output path: `.planning/phases/214-docs-truth-reconciliation/214-03-SUMMARY.md` on completion.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | All current public/planning version and advisory-sync surfaces tell one story | 214-01..03 | COVERED | Plans 01-02 corrected content; Plan 03 makes the linked release candidate traversable. |
| REQ | DOCS-01 | `lattice_stripe ~> 2.0` current stack truth | 214-01 | COVERED | Verified satisfied; preserved as an explicit assumption. |
| REQ | DOCS-02 | Shipped/observational JTBD truth with local grant authority | 214-01 | COVERED | Verified satisfied; preserved as a prohibition/invariant. |
| REQ | DOCS-03 | Changelog, release-note, metadata, support/adoption/planning consistency | 214-01..03 | COVERED | Plan 03 closes authoritative Truth 6 only. |
| RESEARCH | — | Extend the existing release verifier and ROOT_DIR ExUnit fixture family | 214-03 | COVERED | No new verifier, lane, dependency, or manual release artifact. |
| RESEARCH | — | Preserve Release Please ownership and current/historical boundary | 214-02..03 | COVERED | State-aware branch distinguishes pre-release from generated candidate. |
| CONTEXT | D-01 | Local grant authority; advisory sync cannot change grants | 214-01..03 | COVERED | Existing prose/gates remain mandatory and unchanged. |
| CONTEXT | D-02 | Reconciliation is secondary, never dual authority | 214-01..02 | COVERED | No new prose model introduced. |
| CONTEXT | D-03 | Stripe object authority is not Accrue grant authority | 214-01..02 | COVERED | Preserved by existing verifier assertions. |
| CONTEXT | D-04 | Measured, exact documentation voice | 214-01..02 | COVERED | No public prose edits in Plan 03. |
| CONTEXT | D-05 | Core owns substantive release truth | 214-02..03 | COVERED | Candidate numbered core section retains substantive checks. |
| CONTEXT | D-06 | Admin/portal are compatibility-only | 214-02..03 | COVERED | Candidate numbered companion sections retain compatibility-only checks. |
| CONTEXT | D-07 | Hand-authored linked 1.5.0 story and three changelog links | 214-02..03 | COVERED | Candidate consumes existing story; no manual generated content prerequisite. |
| CONTEXT | D-08 | Release Please owns versions and numbered changelog sections | 214-02..03 | COVERED | Pre-release rejects premature numbering; aligned generated candidate is accepted. |
| CONTEXT | D-09 | Phase 213 public metadata uses 1.5.0 | 214-02 | COVERED | Verified satisfied and unaffected. |
| CONTEXT | D-10 | Exact supported metadata allowlist | 214-02 | COVERED | Verified satisfied and unaffected. |
| CONTEXT | D-11 | Internal surfaces remain unbadged | 214-02 | COVERED | Verified satisfied and unaffected. |
| CONTEXT | D-12 | No mixed 1.4.0/1.5.0 feature-family claims | 214-02..03 | COVERED | Package mismatch and incomplete candidate regressions fail. |
| CONTEXT | D-13 | Scoped current-truth sweep | 214-01..02 | COVERED | Verified satisfied; Plan 03 changes no truth prose. |
| CONTEXT | D-14 | Preserve dated evidence | 214-01..03 | COVERED | Only two current verifier/test files are modified. |
| CONTEXT | D-15 | Classify current versus historical paths | 214-01..02 | COVERED | No repo-wide absence rule added. |
| CONTEXT | D-16 | Extend existing verifier ownership | 214-03 | COVERED | Existing script and focused test only. |
| CONTEXT | D-17 | High-signal positive/negative checks | 214-03 | COVERED | Both valid states plus mismatched/malformed/incomplete negatives. |
| CONTEXT | D-18 | Focused fixtures prove guards fail | 214-03 | COVERED | One-mutation negative ROOT_DIR fixtures. |
| CONTEXT | D-19 | Scoped manual acceptance grep | 214-01..02 | COVERED | Already executed and verified; no new prose surface. |

<verification>
Run the focused release-note ExUnit suite, then the live release-note, package-doc, support-matrix, and entitlement-isolation scripts. Confirm the tests exercise both valid states and each negative state through ROOT_DIR temporary fixtures without editing repository package versions or changelogs.
</verification>

<success_criteria>
- Authoritative Truth 6 passes: the merge/release gate accepts current 1.4.0 and aligned generated 1.5.0 candidate states.
- DOCS-03 is closed without re-claiming already satisfied DOCS-01 or DOCS-02 work.
- Cross-package alignment, strict version parsing, matching candidate changelogs, linked release-note discovery, and D-05/D-06 ownership remain enforced.
- D-08 Release Please ownership remains executable: premature numbering fails before the candidate transition; aligned generated numbering passes during it.
- Truths 1-5 and 7-9 remain green through the existing verifier families.
</success_criteria>

<output>
Create `.planning/phases/214-docs-truth-reconciliation/214-03-SUMMARY.md` when done.
</output>

# Phase 214.1: Close gap: DOCS-03 — reconcile StripeSync writer documentation - Context

**Gathered:** 2026-07-31
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the remaining v1.58 DOCS-03 truth gap: make the public `Accrue.Entitlements.StripeSync` documentation accurately describe both webhook and client-backed pull writes to the observational entitlement-summary cache, protect that truth with the existing package-documentation verifier family, and reconcile active planning status after the correction verifies.

This phase changes documentation, documentation regression tests, and current planning truth only. It does not change entitlement runtime behavior, grant semantics, cache ordering, processor capabilities, package versions, release ownership, admin/portal UI, or queued v1.59 work.

</domain>

<decisions>
## Implementation Decisions

### Public writer wording

- **D-01:** Repair the complete stale `## One-way dependency` paragraph in `Accrue.Entitlements.StripeSync`, not only the phrase "written exclusively." Correct both false implications: the module is not read-only internally, and webhooks are not the cache's only writer.
- **D-02:** Name both supported writer paths explicitly: webhook handling and the client-backed pull refresh. State that both write the same advisory `Accrue.Billing.EntitlementSummary` row through `Accrue.Entitlements.Reconcile`.
- **D-03:** Apply the `stripe_native_sync: :advisory` opt-in condition to both webhook and pull paths. Preserve that the cache is off by default, diagnostic only, and never consulted by grant decisions.
- **D-04:** Keep the mechanism explanation concise. Name the shared reconciler and same-row convergence, but do not expose timestamp/event ordering, conflict clauses, or other internal persistence detail in the public moduledoc.
- **D-05:** Preserve the architectural direction exactly: `seam → billing`, never `gate → seam`; local plan→feature mapping remains the sole Accrue grant authority. — **Reversibility: one-way** — reversing this statement would change the published v1.x authorization contract and contradict the merge-blocking isolation guarantee.

### Regression protection

- **D-06:** Extend `scripts/ci/verify_package_docs.sh`; do not create a new verifier or CI lane. This remains the existing owner of current package/source documentation truth.
- **D-07:** Assert semantic fragments rather than one exact paragraph. The contract must recognize webhook writing, pull refresh writing, shared `Accrue.Entitlements.Reconcile`, the advisory-only boundary, and the absence of webhook-exclusive/read-only claims.
- **D-08:** Add a focused ROOT_DIR-backed negative fixture in `accrue/test/accrue/docs/package_docs_verifier_test.exs` that mutates the correct source back to stale writer wording and proves the production verifier fails with a stable, actionable diagnostic.
- **D-09:** Close the change with the focused package-doc ExUnit suite, the live package-doc shell verifier, `mix compile --warnings-as-errors`, and `scripts/ci/verify_entitlement_sync_isolation.sh`. Do not require a full repository-wide suite for this documentation-only correction.

### Planning truth reconciliation

- **D-10:** After executable verification passes, reconcile current active truth: Phase 214/214.1 status in `.planning/ROADMAP.md`, DOCS-01..03 traceability status in `.planning/REQUIREMENTS.md`, v1.58 current-state prose in `.planning/PROJECT.md`, and active/session pointers in `.planning/STATE.md`.
- **D-11:** Use registered GSD handlers for `ROADMAP.md` and `STATE.md` mutations where handlers exist; keep any necessary current-prose edits narrowly scoped.
- **D-12:** Preserve dated evidence unchanged: Phase 212–214 plans, contexts, research, summaries, reviews, validation and verification reports, seeds, archives, and historical audit evidence remain accurate records of what was known at their timestamps.
- **D-13:** Phase 214.1 must not self-declare the milestone audit passed. `.planning/v1.58-v1.58-MILESTONE-AUDIT.md` remains `gaps_found` until this phase verifies and `$gsd-audit-milestone` independently reruns.

### the agent's Discretion

- Exact audience-appropriate prose for the repaired paragraph, provided D-01..05 remain recognizable.
- Exact semantic tokens and failure label used by the verifier, provided the contract does not freeze a full paragraph.
- Whether the negative fixture exercises the two stale claims in one mutation or separate focused cases.
- Exact registered handler sequence for current roadmap/state reconciliation.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Gap definition and active scope

- `.planning/v1.58-v1.58-MILESTONE-AUDIT.md` — authoritative remaining DOCS-03 gap, required closeout work, current scores, and re-audit gate.
- `.planning/ROADMAP.md` — inserted Phase 214.1 location, v1.58 scope fence, Phase 214 history, and queued Phase 215 dependency.
- `.planning/REQUIREMENTS.md` — DOCS-03 stable-core truth requirement and the active traceability rows to reconcile after verification.
- `.planning/PROJECT.md` — stable-core documentation-truth posture and current v1.58 status.
- `.planning/STATE.md` — protected active phase/session state; mutate through registered handlers.

### Prior locked documentation decisions

- `.planning/phases/214-docs-truth-reconciliation/214-CONTEXT.md` — canonical sync wording, current-vs-historical truth rules, verifier ownership, Release Please boundary, and drift-prevention decisions carried forward unchanged.
- `.planning/phases/214-docs-truth-reconciliation/214-VERIFICATION.md` — passed 9/9 phase verification that the milestone integration cold-read subsequently refined with this writer-wording gap.
- `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-CONTEXT.md` — locked observational-only architecture, shared-writer decision, and grant-isolation boundary.
- `.planning/phases/213-stripe-native-advisory-entitlements-sync-observational-only/213-VERIFICATION.md` — passed runtime evidence for pull/webhook convergence and grant invariance.

### Shipped source truth

- `accrue/lib/accrue/entitlements/stripe_sync.ex` — public moduledoc containing both stale writer claims and the implemented pull refresh path.
- `accrue/lib/accrue/entitlements/reconcile.ex` — shared webhook/pull writer and same-row convergence implementation.
- `accrue/lib/accrue/webhook/default_handler.ex` — webhook entry path into the shared reconciler.
- `scripts/ci/verify_entitlement_sync_isolation.sh` — executable one-way dependency contract that must remain green.

### Existing drift-contract assets

- `scripts/ci/verify_package_docs.sh` — production documentation contract to extend.
- `accrue/test/accrue/docs/package_docs_verifier_test.exs` — existing ROOT_DIR-backed mutation fixture pattern to reuse.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `scripts/ci/verify_package_docs.sh` already provides fixed/regex positive, absence, count, and actionable failure helpers and is wired into CI.
- `package_docs_verifier_test.exs` already copies `stripe_sync.ex` into isolated ROOT_DIR fixtures, mutates one invariant at a time, invokes the production script, and asserts bounded diagnostics.
- `Accrue.Entitlements.Reconcile.write_pull/4` and `write_webhook/4` are the concrete source of truth for the corrected public sentence.

### Established Patterns

- Current public docs follow semantic positive and negative assertions rather than exact paragraph snapshots.
- Documentation fixtures exercise the production shell verifier rather than duplicating its logic in ExUnit.
- Active planning mirrors are corrected forward; dated execution and verification artifacts are preserved.
- Runtime grant isolation is independently protected by `verify_entitlement_sync_isolation.sh` and must not be weakened by wording changes.

### Integration Points

- Repaired `StripeSync` moduledoc → package-doc verifier semantic checks → ROOT_DIR negative fixture → existing CI package-doc lane.
- Phase verification → active ROADMAP/REQUIREMENTS/PROJECT/STATE reconciliation → independent milestone re-audit.

</code_context>

<specifics>
## Specific Ideas

- The public paragraph should make this sentence-level contract obvious: when advisory sync is enabled, webhook handling and pull refresh both write the same diagnostic cache row through the shared reconciler; neither path changes a grant decision.
- Keep ordering and conflict-resolution mechanics in internal code/tests rather than the public module overview.

</specifics>

<deferred>
## Deferred Ideas

- `Processor.Stripe.list_charges/2` still names `lattice_stripe ~> 1.0` in an unsupported-operation message; this is adjacent cleanup, not the DOCS-03 writer-truth gap.
- Processor capability discovery still does not advertise `stripe_native_sync` consistently; runtime direct optional-callback dispatch works, and the existing Phase 213 review finding remains outside this documentation closure.
- Phases 212 and 213 remain Nyquist `draft`; run `$gsd-validate-phase 212` and `$gsd-validate-phase 213` separately after the closure phase.

</deferred>

---

*Phase: 214.1-close-gap-docs-03-reconcile-stripesync-writer-documentation*
*Context gathered: 2026-07-31*

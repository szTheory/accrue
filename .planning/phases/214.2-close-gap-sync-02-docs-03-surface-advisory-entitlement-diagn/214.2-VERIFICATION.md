---
phase: 214.2-close-gap-sync-02-docs-03-surface-advisory-entitlement-diagn
verified: 2026-07-31T19:28:57Z
status: gaps_found
score: 17/19 must-haves verified
behavior_unverified: 1
overrides_applied: 0
gaps:
  - truth: "D-03 / D-19 / D-20: focused LiveView tests cover every visible state, preview/raw evidence, accessibility labels, local-only headline, and contradictory writer evidence without a cross-model verdict."
    status: partial
    reason: "The LiveView suite exercises the recorded contradictory snapshot only. Its other advisory-state test calls Copy functions directly, so disabled, not_observed, age_unknown, incomplete, unavailable, bounded-preview, and lazy Raw-data rendering have no LiveView coverage."
    artifacts:
      - path: "accrue_admin/test/accrue_admin/live/entitlements_live_test.exs"
        issue: "No LiveView test mounts the five remaining advisory states or opens Raw data to assert complete normalized evidence."
    missing:
      - "Add focused LiveView assertions for all remaining advisory states, the eight-key +N preview boundary, and lazy Raw-data full-key evidence."
behavior_unverified_items:
  - truth: "D-22: existing customer-detail axe light/dark and real mobile horizontal-overflow contracts pass."
    test: "Run the existing customer-detail browser accessibility contract in light and dark themes and the Pixel-5/approximately-360px mobile customer-detail overflow flow."
    expected: "The disclosure remains keyboard-readable, exposes text labels independent of badge color, has no axe violations, and does not overflow horizontally."
    why_human: "The browser command starts application/browser infrastructure and was not run during this non-mutating verification; source inspection cannot prove browser layout or axe output."
---

# Phase 214.2: Close gap: SYNC-02/DOCS-03 — surface advisory entitlement diagnostics Verification Report

**Phase Goal:** Close the verified v1.58 observation gap by surfacing already-persisted Stripe advisory entitlement diagnostics in the existing admin customer detail flow while local resolution remains the sole authorization source, and correct plus regression-protect both stale advisory pagination statements.

**Verified:** 2026-07-31T19:28:57Z  
**Status:** gaps_found  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | CustomerLive obtains the composed diagnostic once per mount/refresh while local resolution and grants remain local-only. | ✓ VERIFIED | `entitlements_view/1` is the sole CustomerLive call site and is used on mount and `refresh_customer_detail/1`; `access_headline/1` pattern-matches only `local`. The focused isolation suite passed. |
| 2 | The disclosure is canonical-first, advisory-second, with the exact access boundary. | ✓ VERIFIED | CustomerLive renders `accrue-access-canonical` before `stripe-observation-advisory`; Copy exports `Stripe advisory snapshot — does not change access.` The contradictory LiveView test passed. |
| 3 | Diagnostic branches are independent; disabled mode performs no advisory I/O; pull provenance uses only the trusted stamp. | ✓ VERIFIED | `admin.ex` isolates `safe_local_diagnostic/1` and config-gated `safe_stripe_advisory_diagnostic/1`; passing core tests cover malformed data/local errors, pull provenance, disabled no-query, and one enabled lookup. |
| 4 | A Fake/Test contradictory writer-to-row-to-render flow proves local access does not change. | ✓ VERIFIED | Passing LiveView test calls `Reconcile.write_pull/4`, renders `Reports` and `priority-support`, asserts `2 active access grants`, and rejects comparison verdict language. |
| 5 | The diagnostic distinguishes every locked state, including observed-empty, with independent failure containment. | ✓ VERIFIED | Passing `admin_test.exs` covers disabled, not_observed, recorded empty, recorded, age_unknown, incomplete, unavailable, pull/webhook/unavailable provenance, and both asymmetric failures. |
| 6 | Disabled is no-I/O, enabled has one owner-scoped lookup, and diagnostics cannot alter any grant surface. | ✓ VERIFIED | Passing `stripe_sync_disabled_isolation_test.exs` telemetry-checks no disabled cache query/one enabled query and compares `entitled?`, features, plan state, and quantity across no/empty/stale/contradictory rows. |
| 7 | The renderer shows bounded sorted preview and retains complete normalized evidence only in lazy Raw data. | ✓ VERIFIED | `advisory_lookup_preview/1` sorts, takes eight, and appends exact `+N more`; `raw_advisory_payload/1` forwards every normalized key only through the existing lazy JsonViewer path. |
| 8 | Every advisory state has Copy-backed text and non-color badge semantics, without stale or recovery claims. | ✓ VERIFIED | CustomerLive selects a text label and tone through Copy-backed state functions; no stale state, live-region, or recovery action is present. |
| 9 | Focused LiveView tests cover every visible state, preview/raw evidence, accessibility labels, local-only headline, and contradictory writer evidence. | ✗ FAILED | Only the recorded contradictory state mounts a LiveView. The sole all-state test checks Copy return values; it neither renders the other states nor opens Raw data. |
| 10 | Admin tests, copy export, warnings-as-errors compilation, and browser accessibility/mobile contracts pass. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Focused admin tests, warnings-as-errors compilation, and regenerated-copy parity passed; browser contracts were not executed because they require starting browser/application infrastructure. |
| 11 | EntitlementSummary and telemetry accurately distinguish exhaustive pull from possibly incomplete webhook snapshots and retain advisory-only wording. | ✓ VERIFIED | Current moduledoc and telemetry row contain the shipped completeness statements; the package-doc verifier passed. |
| 12 | The existing verifier pins both statements and rejects both stale pagination claims through real ROOT_DIR mutations. | ✓ VERIFIED | Passing `package_docs_verifier_test.exs` runs the real script against separate EntitlementSummary and telemetry stale fixtures and asserts nonzero targeted failures. |
| 13 | Documentation, isolation, focused tests, and warnings-as-errors compilation are green without a new lane. | ✓ VERIFIED | Focused core/docs tests, isolation and package-doc scripts, and core/admin warnings-as-errors compilation passed; no new verifier script or CI lane was added. |
| 14 | The dated v1.58 `gaps_found` audit is not rewritten or treated as a passing re-audit. | ✓ VERIFIED | The audit frontmatter still states `status: gaps_found`; Phase 214.2 code/docs do not claim milestone completion. |

**Score:** 17/19 truths verified (1 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `accrue/lib/accrue/entitlements/admin.ex` | Two-branch core diagnostic | ✓ VERIFIED | Substantive typed diagnostic, config guard before reader, normalized scalar/map result. |
| `accrue_admin/lib/accrue_admin/live/customer_live.ex` | Existing disclosure consumer and Raw projection | ✓ VERIFIED | Mounted/refreshed diagnostic consumer; canonical-first groups and existing lazy JsonViewer are wired. |
| `accrue/test/accrue/entitlements/admin_test.exs` | Branch/provenance/failure tests | ✓ VERIFIED | Focused file passed. |
| `accrue/test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs` | No-I/O and grant invariance proof | ✓ VERIFIED | Focused file passed. |
| `accrue_admin/test/accrue_admin/live/entitlements_live_test.exs` | Writer-to-render integration proof | ⚠️ PARTIAL | Contradictory tracer passes, but it lacks rendering/Raw tests for the remaining states. |
| `accrue_admin/lib/accrue_admin/copy/entitlements.ex` | Advisory operator copy | ✓ VERIFIED | Complete state/copy helpers are substantive and facade-exported. |
| `examples/accrue_host/e2e/generated/copy_strings.json` | Generated Copy contract | ✓ VERIFIED | Valid JSON matches a fresh export to a temporary file byte-for-byte; plan's literal `stripe_advisory_snapshot` pattern is stale, while all actual `entitlements_advisory_*` exports are present. |
| `accrue/lib/accrue/billing/entitlement_summary.ex` | Current completeness documentation | ✓ VERIFIED | Moduledoc states exhaustive pull, possibly incomplete webhook, and advisory-only boundary. |
| `accrue/guides/telemetry.md` | Current operational completeness guidance | ✓ VERIFIED | Existing telemetry row carries the corresponding completeness wording. |
| `scripts/ci/verify_package_docs.sh` | Positive and negative docs contract | ✓ VERIFIED | Both target files have fixed and stale-absence checks. |
| `accrue/test/accrue/docs/package_docs_verifier_test.exs` | Real stale-fixture proof | ✓ VERIFIED | Two target-specific ROOT_DIR mutation tests passed. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| CustomerLive | `Admin.diagnostic_for_customer/1` | mount and refresh through `entitlements_view/1` | ✓ WIRED | One production call expression is shared by the two lifecycle paths. |
| Admin diagnostic | `StripeSync.summary_for_customer/1` | `stripe_native_sync?/0` guard then reader | ✓ WIRED | The false branch returns disabled before the reader; test instrumentation proves no disabled table query. |
| LiveView test | `Reconcile.write_pull/4` | contradictory pull before render | ✓ WIRED | Test invokes the real writer and mounts the resulting customer detail. |
| CustomerLive | Copy facade / JsonViewer | state labels and lazy raw payload | ✓ WIRED | Visible labels use `Copy.entitlements_advisory_*`; Raw path calls `raw_advisory_payload/1`. |
| Docs tests | package-doc verifier | `run_verifier/1` against temporary roots | ✓ WIRED | Tests execute the actual shell verifier and assert nonzero stale-target failures. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| CustomerLive disclosure | `@entitlements_view` | `Admin.diagnostic_for_customer(customer)` | Local resolver result plus persisted `StripeSync.summary_for_customer/1` normalized core result | ✓ FLOWING |
| Lazy Raw data | `raw_entitlements_payload/1` | `@entitlements_view.stripe_advisory.raw` | Complete normalized scalar/map payload; not a schema/provider object | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Core states, containment, and no-I/O/grant invariance | `cd accrue && mix test test/accrue/entitlements/admin_test.exs test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs` | Exit 0 | ✓ PASS |
| Writer-to-render tracer and Copy state contract | `cd accrue_admin && mix test test/accrue_admin/live/entitlements_live_test.exs test/accrue_admin/copy_test.exs` | Exit 0 | ✓ PASS |
| Docs stale-fixture guard | `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` | Exit 0 | ✓ PASS |
| Core/admin compiler and generated copy parity | warnings-as-errors compile in both packages; export to temporary file then `cmp` | Exit 0; valid JSON and byte parity | ✓ PASS |
| Isolation/docs executable gates | `bash scripts/ci/verify_entitlement_sync_isolation.sh && bash scripts/ci/verify_package_docs.sh` | Exit 0 | ✓ PASS |
| Browser accessibility and 360px overflow | Existing Playwright contracts | Not run; server/browser startup is outside this non-mutating verification | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- |
| SYNC-02 | 01, 02, 03 | Opt-in advisory output is diagnostic/admin-only and never becomes a grant source. | ✓ SATISFIED | Config guard, branch separation, local-only headline, contradictory tracer, and grant-surface isolation checks. |
| DOCS-03 | 04 | Package/support/planning truth remains mutually consistent. | ✓ SATISFIED | Corrected completeness docs plus real verifier negative fixtures; historical audit remains explicitly unpassed. |

No phase-mapped requirement is orphaned: both declared IDs are present in all relevant plans and accounted for above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- |
| — | — | No unreferenced `TBD`, `FIXME`, or `XXX`, placeholder render, or hardcoded empty output found in phase artifacts. | ℹ️ Info | No blocker debt marker. |

### Human Verification Required

### 1. Browser accessibility and mobile density

**Test:** Run the existing customer-detail axe contract in light and dark themes, then the existing Pixel-5/approximately-360px customer-detail flow.  
**Expected:** Advisory labels are readable without color, keyboard interaction works, axe reports no violations, and the customer detail has no horizontal overflow.  
**Why human:** The browser suite requires application/browser infrastructure and was intentionally not started during this verification.

### Gaps Summary

The delivered runtime implementation and its core/documentation gates are substantive and wired, but the claimed UI test coverage is not true. The phase needs focused LiveView coverage for each non-recorded state plus bounded preview and lazy Raw evidence before the test contract can certify the full operator flow. This is not scheduled by a later milestone phase, so it remains an actionable blocker.

---

_Verified: 2026-07-31T19:28:57Z_  
_Verifier: the agent (gsd-verifier)_

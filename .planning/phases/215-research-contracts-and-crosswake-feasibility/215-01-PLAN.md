---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/crosswake_tracer/Package.swift
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
  - examples/crosswake_tracer/capability-report.json
  - examples/crosswake_tracer/README.md
  - examples/crosswake_tracer/physical-device-evidence.md
  - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
autonomous: true
requirements:
  - RAIL-05
must_haves:
  truths:
    - "A checked-in Swift target compiles without adding Crosswake to Accrue runtime dependencies and reports every required native/client bridge separately per D-09 and D-10."
    - "Absent pinned Crosswake source or any missing Crosswake/client/device bridge evidence yields an overall feasibility_blocked result; partial, simulator-only, or native-only evidence cannot become runtime-feasible per D-11 and D-12."
    - "Server/vector/JWS parity is a separate merge-blocking contract in Plan 215-05: its absence or failure never becomes a feasibility_blocked reason and never participates in this report reducer."
    - "The tracer contract reserves atomic replacement for a verified newer server allow or signed denial; scene/network changes only coalesce authenticated reconciliation per D-13."
    - statement: "RAIL-05 edge classification remains unresolved: the authoritative Crosswake API surface is unknown, so the capability report must carry the flagged uncertainty without inventing a bridge."
      verification: backstop
  artifacts:
    - path: "examples/crosswake_tracer/Package.swift"
      provides: "Independently buildable Swift feasibility target"
    - path: "examples/crosswake_tracer/capability-report.json"
      provides: "Machine-readable RAIL-05 proof/block disposition"
    - path: ".planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md"
      provides: "Resolved, dated Crosswake-source/build and physical-device evidence conclusions"
  key_links:
    - from: "examples/crosswake_tracer/capability-report.json"
      to: "examples/crosswake_tracer/physical-device-evidence.md"
      via: "Every proven physical-device row cites dated redacted evidence; otherwise status is feasibility_blocked"
      pattern: "feasibility_blocked|proven"
    - from: "examples/crosswake_tracer/capability-report.json"
      to: ".planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md"
      via: "The dated research resolution records whether Crosswake/client/device evidence was acquired or remained unavailable and states the resulting coupling consequence"
      pattern: "Open Questions \(RESOLVED\)|feasibility_blocked"
  assumptions:
    - requirement_id: RAIL-05
      statement: "The exact Crosswake shell/core repository, pinned version, build command, and documented bridge API are unavailable."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RAIL-05
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT describe simulator-only, Apple-native-only, partial, or undocumented-bridge evidence as Crosswake runtime feasibility."
    - requirement_id: RAIL-05
      category: privacy
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT commit device identifiers, private keys, raw receipts, raw JWS bodies, provider payloads, adopter identity, or PII as feasibility evidence."
---

<objective>
Prove the Phase-215 client architecture skeleton end to end with the thinnest truthful Crosswake tracer: one checked-in Swift boundary and one machine-readable result that is either fully proven or explicitly `feasibility_blocked` per D-09 through D-13.

Decision coverage: D-09: checked-in host-owned tracer without runtime dependency or invented bridge; D-10: complete capability-row contract; D-11: compile/unit plus physical evidence lanes; D-12: deterministic `feasibility_blocked`; D-13: reconciliation events are not entitlement authority.

Purpose: Resolve the phase's hardest dependency before later plans expand contracts, while preserving the rule that missing Crosswake source or device evidence blocks runtime coupling but not server/vector work.
Output: A compiling native feasibility harness, redacted evidence runbook, resolved dated research answers, and an explicit client/device feasibility status that remains independent from Plan 215-05's canonical contract-test gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
@.planning/research/v1.59-STACK.md
@.planning/research/v1.59-ARCHITECTURE.md
@.planning/research/v1.59-PITFALLS.md
</context>

## Artifacts this phase produces

- `examples/crosswake_tracer/Package.swift` — independent Swift package definition with no inferred Crosswake package.
- `AccrueOfflineClient`, `Capability`, `CapabilityEvidence`, `CapabilityReport`, and `FeasibilityStatus` in `Sources/AccrueOfflineClient/AccrueOfflineClient.swift`.
- `examples/crosswake_tracer/capability-report.json` — deterministic per-capability result and overall `proven|feasibility_blocked` status.
- `examples/crosswake_tracer/physical-device-evidence.md` — redacted, dated physical-device evidence template and runbook.
- `215-RESEARCH.md` — resolved Crosswake source/build and physical-device evidence questions with dated answers and runtime-coupling consequence.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one honest native-to-Crosswake capability result</name>
  <files>examples/crosswake_tracer/Package.swift, examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift, examples/crosswake_tracer/capability-report.json</files>
  <read_first>
    - examples/crosswake_tracer/Package.swift (new target path; confirm absence before creating)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-09 through D-13)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md (Crosswake assumptions, environment, and Pattern 3)
    - .planning/research/v1.59-STACK.md
  </read_first>
  <behavior>
    - Test 1: a report with all required capability rows proven has overall status proven.
    - Test 2: a missing pinned Crosswake source, undocumented bridge, missing physical-device evidence, or any non-proven required row has overall status feasibility_blocked.
    - Test 3: capability rows retain deterministic order and distinguish native, Crosswake-bridge, simulator-advisory, and physical-device evidence.
  </behavior>
  <action>Create the production-quality feasibility skeleton per D-09/D-10 without inventing Crosswake APIs. Define a narrow host-owned `AccrueOfflineClient` contract and closed capability/report value types covering authenticated transport, StoreKit purchase with account UUID `appAccountToken`, updates, current entitlements, explicit restore, Secure Enclave P-256 registration/nonce proof, `ThisDeviceOnly` Keychain state, durable state, `iat`/revision/freshness high-water checks, atomic verified allow/deny replacement, lifecycle, network coalescing, and reconnect. The package must use only installed Apple/Swift facilities. If the pinned Crosswake source/version/build surface is absent, encode that fact in the checked-in report and mark every dependent row plus the overall result `feasibility_blocked`; native-only rows may carry evidence but cannot lift the overall result. Start with failing Swift tests, implement the report reduction, and keep device evidence non-authoritative for local grants per D-10.</action>
  <acceptance_criteria>
    - `swift test --package-path examples/crosswake_tracer --filter CapabilityReportTests` exits 0.
    - `capability-report.json` contains every D-10 capability exactly once, a schema/contract version, evidence kind/location, and an overall status.
    - With no authoritative Crosswake checkout recorded, `jq -e '.overall_status == "feasibility_blocked"' examples/crosswake_tracer/capability-report.json` succeeds.
    - `! rg -qi 'crosswake' accrue/mix.exs accrue/mix.lock` succeeds, proving no Accrue runtime dependency is declared.
  </acceptance_criteria>
  <verify>
    <automated>swift test --package-path examples/crosswake_tracer --filter CapabilityReportTests &amp;&amp; jq -e '.overall_status == "proven" or .overall_status == "feasibility_blocked"' examples/crosswake_tracer/capability-report.json &amp;&amp; ! rg -qi 'crosswake' accrue/mix.exs accrue/mix.lock</automated>
  </verify>
  <done>The repository has one runnable client-boundary path and truthfully exposes the unresolved Crosswake boundary before any expansion work begins.</done>
</task>

<task type="auto">
  <name>Task 2: Record the device proof lane, coupling disposition, and resolved research answers</name>
  <files>examples/crosswake_tracer/README.md, examples/crosswake_tracer/physical-device-evidence.md, examples/crosswake_tracer/capability-report.json, .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md</files>
  <read_first>
    - examples/crosswake_tracer/capability-report.json
    - examples/crosswake_tracer/README.md (new runbook path)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-11/D-12 physical evidence and block rule)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md (Open Questions and Environment Availability)
    - brandbook/voice.md
  </read_first>
  <action>Document exact build/test/report commands and a dated redacted physical-device procedure for Secure Enclave non-exportability, Keychain migration exclusion, foreground/background recovery, termination during atomic replacement, authenticated Crosswake shell transport, and reconnect. The feasibility reducer may mark a capability row proven only when that row's required Crosswake/client compile or unit evidence and, where D-11 requires it, dated physical-device evidence are present. Otherwise keep the affected row and overall result `feasibility_blocked` per D-11/D-12. Do not read Plan 215-05 output, contract vectors, server JWS results, or their test status when computing this report: those tests are an independent merge-blocking gate whose failure must remain a test failure. After attempting evidence acquisition, update `215-RESEARCH.md`: rename the section to `## Open Questions (RESOLVED)` and record explicit dated answers for the authoritative Crosswake repository/version/build invocation and the physical-device evidence location/approval process. If evidence remains unavailable, say so explicitly and record the consequence that later mobile runtime coupling remains `feasibility_blocked` while Plan 215-05 server/vector work remains mandatory. State that scene/network reachability merely coalesces authenticated reconciliation and cannot change entitlement state per D-13. Never include device identifiers, secrets, raw provider material, or adopter identity.</action>
  <acceptance_criteria>
    - README gives copy-paste Swift and ExUnit commands and lists all required evidence lanes.
    - `physical-device-evidence.md` contains date, pinned source/version/build, device-class-without-identifier, command/result, redaction attestation, and reviewer fields.
    - A scriptable JSON assertion proves every required capability is `proven` or the overall status is `feasibility_blocked`; no mixed partial state reports overall proven.
    - `215-RESEARCH.md` contains exactly one `## Open Questions (RESOLVED)` heading, dated explicit answers to both former questions, and the `feasibility_blocked` coupling consequence when source/device evidence is unavailable.
    - The report schema and reducer contain no Plan-215-05 evidence reference or contract-test-status input; Plan 215-05 failure remains independently merge-blocking.
  </acceptance_criteria>
  <verify>
    <automated>swift test --package-path examples/crosswake_tracer &amp;&amp; jq -e '([.capabilities[].status] | all(. == "proven")) or .overall_status == "feasibility_blocked"' examples/crosswake_tracer/capability-report.json &amp;&amp; test "$(rg -c '^## Open Questions \(RESOLVED\)$' .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md)" -eq 1 &amp;&amp; ! rg -q '^## Open Questions$' .planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md &amp;&amp; ! rg -q '215-05|contract[_ -]test|golden[_ -]vector|server[_ -]vector' examples/crosswake_tracer/capability-report.json examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift</automated>
  </verify>
  <done>Maintainers have reproducible proof instructions, resolved dated research answers, and an explicit machine-readable client/device go/block answer; absent current Crosswake/device inputs, later mobile coupling remains `feasibility_blocked`, while Plan 215-05 remains an independent mandatory gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| device → authenticated host transport | Device/account claims, nonce proof, and evidence cross an untrusted client boundary. |
| server proof → device cache | Signed allow/deny material crosses into durable client state and must replace atomically. |
| Crosswake shell → Apple native APIs | An unresolved bridge could misrepresent unavailable native operations as proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-01 | Spoofing | device registration/reconnect | high | mitigate | `CapabilityReportTests` fails closed to `feasibility_blocked` unless authenticated Crosswake transport and physical-device account/device/key-thumbprint/nonce evidence is present; server/vector results are not reducer inputs. |
| T-215-02 | Tampering | cached allow/deny replacement | high | mitigate | `CapabilityReportTests` requires Crosswake/client compile/unit and dated device atomic-replacement evidence for feasibility; Plan 215-05 separately merge-blocks monotonic denial/JWS/vector failures without translating them into feasibility status. |
| T-215-03 | Information Disclosure | physical-device evidence | medium | mitigate | Commit only redacted device-class and result metadata; assert no device IDs, secrets, raw receipts/JWS, payloads, or PII. |
| T-215-04 | Elevation of Privilege | feasibility report | high | mitigate | Overall proven requires all capability lanes; missing Crosswake or device evidence deterministically yields feasibility_blocked. |
| T-215-SC | Tampering | package admission | high | mitigate | Install no package; authoritative Crosswake source remains an explicit unresolved input rather than an inferred dependency. |

## ASVS L1 Control Mapping

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable | `CapabilityReportTests` requires authenticated account/device transport and nonce-proof rows; any missing row forces `feasibility_blocked` (T-215-01). |
| V3 Session Management | Applicable | Lifecycle/reconnect rows require authenticated reconciliation evidence; scene/network events alone cannot produce `proven` (T-215-04). |
| V4 Access Control | Applicable | The report reducer denies overall feasibility unless every source-qualified capability lane is proven (T-215-04). |
| V5 Input Validation | Applicable in independent Plan 215-05 gate | This feasibility report does not consume signed-input test status; Plan 215-05 independently executes and merge-blocks claim/schema rejection after canonical cases exist. |
| V6 Cryptography | Applicable | Secure Enclave/key-thumbprint capability rows and physical evidence are mandatory, while `! rg -qi 'crosswake' accrue/mix.exs accrue/mix.lock` automatically fails any undeclared runtime-coupling attempt (T-215-SC). |
</threat_model>

<source_audit>
GOAL is covered by the machine-readable prove-or-block result. RAIL-05 Crosswake/client/device feasibility and physical-evidence lanes are covered by both tasks; canonical D-05/D-11 signed-vector proof is deliberately and independently merge-blocking in Plan 215-05 after Plan 215-03 creates DecisionCases and is not an input to this reducer. CONTEXT D-09 through D-13 is cited and implemented without weakening `feasibility_blocked`. RESEARCH recommendations for native Apple primitives, physical-device evidence, explicit blocking, and resolution of the two evidence questions are covered, including the dated `Open Questions (RESOLVED)` update. The RAIL-05 unclassified edge is retained as a flagged backstop assumption. No item is silently dropped.
</source_audit>

<verification>
Run `swift test --package-path examples/crosswake_tracer`, the `jq` all-proven-or-blocked assertion, the resolved-research heading/date checks, and `! rg -qi 'crosswake' accrue/mix.exs accrue/mix.lock`. The canonical signed-vector/server proof runs as the separate mandatory Plan 215-05 gate and never changes the feasibility report status.
</verification>

<success_criteria>
The tracer compiles, every required Crosswake/client/device bridge has explicit evidence/status, the two research questions have dated resolutions, and the overall result cannot be green unless every applicable D-10/D-11 client/device lane is proven. With current missing inputs it ends in `feasibility_blocked` without waiving or absorbing Plan 215-05's independent merge-blocking contract tests.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-01-SUMMARY.md` when done.
</output>

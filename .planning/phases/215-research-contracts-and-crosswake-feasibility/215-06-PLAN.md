---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 06
type: execute
wave: 4
depends_on:
  - 215-05
files_modified:
  - accrue/lib/accrue/entitlements/decision_cases.ex
  - accrue/test/accrue/entitlements/decision_cases_test.exs
  - accrue/test/support/entitlements/decision_case_contract_consumer.ex
  - accrue/test/property/entitlement_decision_cases_property_test.exs
autonomous: true
gap_closure: true
requirements:
  - RSCH-02
must_haves:
  truths:
    - "DecisionCases.valid?/1 closes every D-07 field over explicit vocabularies and shapes, including account/device bindings, evidence kind, continuity, repair, prior sources/snapshot, ordering values, and a non-negative revision_delta."
    - "Generated evidence and prior state flow through a real test-support contract consumer that applies the canonical case transition; properties observe computed snapshot/revision/continuity/repair outcomes rather than re-reading immutable fixture fields per D-06/D-08."
    - "The contract consumer remains test-only conformance machinery: it interprets canonical case data and cannot become a production projector, public runtime API, or second policy source per D-06."
    - "The RSCH-02 fallback prohibition intentionally remains descriptor-less and flagged-unverified under the projectProhibitions contract; the targeted test suite plus the production-import static gate are its executable evidence without inventing a wired-check descriptor."
    - statement: "RSCH-02's production-reducer-equivalence edge remains unresolved until Phase 217 supplies a production reducer; Phase 215 closes the schema and non-vacuous contract-consumer proof without claiming runtime equivalence."
      verification: backstop
  artifacts:
    - path: "accrue/lib/accrue/entitlements/decision_cases.ex"
      provides: "Closed D-07 case schema and canonical corpus"
      exports: ["version/0", "all/0", "valid?/1"]
    - path: "accrue/test/support/entitlements/decision_case_contract_consumer.ex"
      provides: "Test-only canonical transition consumer for generated evidence/prior state"
    - path: "accrue/test/property/entitlement_decision_cases_property_test.exs"
      provides: "Non-vacuous permutation, duplicate, ordering, survivor, and atomic-boundary properties"
  key_links:
    - from: "accrue/test/property/entitlement_decision_cases_property_test.exs"
      to: "accrue/test/support/entitlements/decision_case_contract_consumer.ex"
      via: "each generated evidence/prior-state sequence is consumed before its computed result is asserted"
      pattern: "DecisionCaseContractConsumer"
    - from: "accrue/test/support/entitlements/decision_case_contract_consumer.ex"
      to: "accrue/lib/accrue/entitlements/decision_cases.ex"
      via: "consumer validates and interprets the canonical DecisionCase instead of selecting policy independently"
      pattern: "DecisionCases.valid"
  assumptions:
    - requirement_id: RSCH-02
      statement: "The deterministic probe classified the downstream production-consumer behavior as unclassified; production reducer equivalence is explicitly unresolved and remains a Phase-217 backstop, while this plan proves the Phase-215 contract consumer."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RSCH-02
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT present the test-support consumer as production entitlement logic or allow it to choose policy independently of the canonical DecisionCase."
---

<objective>
Close the D-07 case schema and replace vacuous properties with generated evidence/prior-state execution through a real conformance consumer.

Decision coverage: D-05 preserves one versioned canonical corpus; D-06 keeps the consumer test-only and data-driven; D-07 closes every case field and binding; D-08 requires generated ordering, duplicate, survivor, concurrency, and transaction evidence.

Purpose: Make RSCH-02 safe for implementers to consume without falsely claiming that immutable fixture assertions prove decision behavior.
Output: Closed validation, exhaustive malformed-field tests, a test-only transition consumer, and non-vacuous StreamData properties.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-03-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-05-SUMMARY.md
@accrue/lib/accrue/entitlements/decision_cases.ex
@accrue/test/property/entitlement_decision_cases_property_test.exs
</context>

## Artifacts this phase produces

- A fully closed `Accrue.Entitlements.DecisionCases.valid?/1` contract for every D-07 field.
- `Accrue.Entitlements.DecisionCaseContractConsumer`, compiled only in tests, which applies canonical case transitions to supplied prior state.
- Mutation-sensitive unit tests and generated properties that prove data flows through the consumer operation.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Close one D-07 case path from malformed binding to rejected contract</name>
  <reversibility rating="costly">The D-07 field vocabulary is consumed by generated JSON, Swift vectors, support reasons, and later reducers under the already-locked D-05 contract.</reversibility>
  <files>accrue/lib/accrue/entitlements/decision_cases.ex, accrue/test/accrue/entitlements/decision_cases_test.exs</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-05 through D-08)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (first failed truth)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-PATTERNS.md (`decision_cases.ex` closed data declaration/total fail-closed lookup assignment and Shared Patterns: Closed contracts and typed failure)
    - accrue/lib/accrue/processor/capabilities.ex (`@support_labels` closed vocabulary and `support_label/1` total fail-closed lookup analog)
    - accrue/lib/accrue/entitlements/decision_cases.ex
    - accrue/test/accrue/entitlements/decision_cases_test.exs
    - accrue/priv/entitlements/v1.59-decision-cases.json
  </read_first>
  <behavior>
    - Test 1: a current canonical case validates, while changing its account or device binding to an unknown value rejects the complete case.
    - Test 2: changing evidence kind, continuity, or repair to an atom outside the declared closed vocabulary rejects the case.
    - Test 3: a negative revision_delta rejects the case; zero and the corpus's positive deltas remain valid.
    - Test 4: malformed prior sources/snapshot, ordering cursor/timestamp/relation, missing atomic flag, or unbounded reason rejects the case.
  </behavior>
  <action>Write mutation-sensitive tests first, using a known canonical case as the end-to-end tracer through `valid?/1`. Follow the closed-vocabulary/contract-validation pattern mapped in `215-PATTERNS.md` from `Accrue.Processor.Capabilities`: declare named module-level accepted values and make `DecisionCases.valid?/1` a total, fail-closed lookup/shape boundary that returns false for anything outside the contract. Replace open atom and shape checks with those closed vocabularies derived from the accepted D-07 corpus: evidence kinds; account/device binding states; source rails; continuity and repair outcomes; and all existing closed rail/environment/relation/disposition/lease/eligibility sets. Validate bindings rather than ignoring them, require unique known prior-source rails and the documented snapshot shape, retain non-negative prior revision/order constraints, and require `revision_delta >= 0`. Preserve all currently valid canonical cases and the internal/data-only boundary per D-05/D-06; do not add Repo, provider, gateway, runtime facade, or schema work.</action>
  <acceptance_criteria>
    - Every `DecisionCases.all/0` item passes the closed validator.
    - A one-field mutation for every D-07 field has a focused failing assertion, including both bindings and negative revision delta.
    - Validation returns false rather than raising for wrong structs, nils, unknown atoms, malformed maps/lists, and invalid scalar types.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs</automated>
  </verify>
  <done>A complete canonical case validates and every malformed D-07 field or binding fails closed through the same public internal validator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive generated evidence and prior state through the contract consumer</name>
  <files>accrue/test/support/entitlements/decision_case_contract_consumer.ex, accrue/test/property/entitlement_decision_cases_property_test.exs</files>
  <read_first>
    - accrue/lib/accrue/entitlements/decision_cases.ex
    - accrue/test/property/entitlement_decision_cases_property_test.exs
    - accrue/test/property/entitlement_summary_monotonic_property_test.exs
    - accrue/test/test_helper.exs
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-PATTERNS.md (permutation/ordering property pattern)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (vacuous-property finding)
  </read_first>
  <behavior>
    - Property 1: permuted duplicate deliveries are consumed and converge to the same computed snapshot/revision as one delivery.
    - Property 2: generated older evidence is consumed after a denial/revocation prior state and cannot restore an allow snapshot.
    - Property 3: generated source sets retain a surviving live rail when the other rail retracts.
    - Property 4: generated prior revisions plus a case revision_delta compute a non-decreasing revision and expose only the complete declared atomic result.
    - Property 5: generated invalid evidence/bindings/prior state are rejected by the consumer before any expected transition is emitted.
  </behavior>
  <action>Create a test-support conformance consumer that accepts a canonical `DecisionCase`, supplied evidence, supplied prior state, and delivery sequence; calls the closed validator; matches the supplied inputs to the case contract; and applies the case's declared disposition/revision/snapshot/continuity/repair as a state transition. Keep case selection and policy out of this helper: it interprets the passed canonical data and is not compiled into `lib`. Rewrite the existing properties so StreamData generates evidence, bindings, source sets, ordering/delivery sequences, and prior revisions, passes them into this operation, and asserts the computed result or rejection. Remove assertions that merely read fixed fields from a fixture. Exercise equal/adjacent ordering explicitly as equality or monotonic comparison, not list adjacency semantics, per D-08.</action>
  <acceptance_criteria>
    - Every property invokes the consumer operation and asserts its computed or rejected result.
    - A deliberate mutation of the consumer's revision, duplicate, survivor, or older-evidence behavior makes the corresponding property fail.
    - At least 50 generated cases run per property, credential-free and async-safe.
    - The test-support module contains no independent case-ID table and no production module imports it.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs test/property/entitlement_decision_cases_property_test.exs &amp;&amp; ! rg -n 'DecisionCaseContractConsumer' lib</automated>
  </verify>
  <done>Generated evidence and prior state now traverse a real contract-consumer transition, proving D-08 invariants without creating production Phase-217 logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical case data → validator | Later consumers rely on invalid cases being rejected before policy data is trusted. |
| generated evidence/prior state → test consumer | Adversarial shapes must fail before they can produce a granting transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-22 | Tampering | D-07 validator | high | mitigate | Closed vocabularies, binding checks, non-negative deltas, and one-field mutation tests reject contract drift. |
| T-215-23 | Elevation of Privilege | contract consumer | high | mitigate | The consumer calls `valid?/1`, rejects mismatched evidence/prior state, and proves older/invalid evidence cannot emit a grant transition. |
| T-215-24 | Repudiation | case/reason outputs | medium | mitigate | Stable case/reason IDs and computed transition assertions preserve an auditable result vocabulary. |
| T-215-25 | Information Disclosure | generated evidence | low | accept | Tests use bounded synthetic account/device states and contain no PII or provider payloads. |

## ASVS L1 Control Mapping

**security_block_on: high.** Any failed high-severity ASVS control or its named automated evidence is merge-blocking and prevents execution completion and Phase-215 closure.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable as contract data | Unknown account/device binding states fail focused validator and consumer tests. |
| V3 Session Management | Applicable as ordering data | Generated revisions and ordering tuples pass through the consumer; rollback cannot advance state. |
| V4 Access Control | Applicable | Invalid, older, duplicate, revoke, and survivor cases compute bounded non-widening transitions. |
| V5 Input Validation | Applicable | Every D-07 field has wrong-shape/value mutation coverage and `valid?/1` returns false without raising. |
| V6 Cryptography | Not modified here | Signed proof verification remains owned by 215-07; this plan closes its canonical input contract. |
</threat_model>

<source_audit>
GOAL truth 2 and REQ RSCH-02 are covered by both tasks. CONTEXT D-05/D-06/D-07/D-08 are cited and implemented without entering Phase 217 runtime projection. RESEARCH's canonical-data, closed-contract, generated-property, and no-second-reducer constraints are covered. VERIFICATION gap 1 is covered item-for-item. The RSCH-02 unclassified production-consumer edge is visibly retained as a backstop; no deferred Phase-216–220 runtime feature is added. No ORM/schema path is touched, so schema-push detection is non-applicable. External API surface is unchanged, so COVERAGE.md remains valid.
</source_audit>

<verification>
Run the targeted schema and property suites. Confirm mutation tests cover every D-07 field, inspect the property module to verify every generated value flows through `DecisionCaseContractConsumer` before assertion, and require the explicit `rg` production-import gate to prove no module under `accrue/lib` references the test-support consumer. The fallback prohibition stays descriptor-less (`verification: null`, flagged) by contract; this task-level gate is the executable evidence and is not a `check_*` descriptor.
</verification>

<success_criteria>
The canonical corpus rejects every out-of-contract D-07 value and generated inputs produce observable decisions through a test-only consumer instead of vacuous fixture assertions.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-06-SUMMARY.md` when done.
</output>

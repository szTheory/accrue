---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 08
type: execute
wave: 6
depends_on:
  - 215-07
files_modified:
  - examples/crosswake_tracer/Package.swift
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
autonomous: true
gap_closure: true
requirements:
  - RSCH-01
  - RSCH-02
  - RSCH-03
  - RAIL-04
  - RAIL-05
must_haves:
  truths:
    - "All replacements targeting the same standardized cache path serialize through one per-cache coordinator even when invoked concurrently from distinct cache handles."
    - "Each replacement uses a unique same-directory candidate, cleans candidates on success/failure, fsyncs candidate bytes before rename, and performs a platform-appropriate parent-directory durability barrier after rename per D-11/D-13."
    - "Concurrent allow/deny replacement and child-process crash/reopen tests observe only a complete old or complete committed new state, never torn bytes, missing cache, candidate resurrection, or an older allow winning after a same/newer denial."
    - "The cache durability proof remains an independent merge-blocking native lane and cannot change `feasibility_blocked` while Crosswake/device evidence is absent per D-10/D-12."
    - "All previously satisfied RSCH-01, RSCH-03, and RAIL-04 authority/watchlist/source-matrix gates remain green after RSCH-02/RAIL-05 closure."
    - "The RAIL-05 fallback prohibition intentionally remains descriptor-less and flagged-unverified under the projectProhibitions contract; a capability-report invariant test plus a report-diff static gate are its executable evidence without an invented wired-check descriptor."
  artifacts:
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      provides: "Per-cache serialized, unique-candidate, durably renamed offline cache"
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift"
      provides: "Child-process crash/reopen durability harness"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      provides: "Concurrent replacement and crash/reopen regression proof"
  key_links:
    - from: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      to: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      via: "task groups race distinct handles against one path and reopen the cache after injected process exit"
      pattern: "AtomicOfflineCache"
    - from: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      to: "cache parent directory"
      via: "same-directory unique candidate, candidate fsync, atomic rename, then parent-directory fsync before success"
      pattern: "synchronize|fsync|rename|replaceItemAt"
    - from: "examples/crosswake_tracer/Package.swift"
      to: "examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift"
      via: "test-only executable target used by crash/reopen tests"
      pattern: "AccrueOfflineCacheCrashHarness"
  assumptions:
    - requirement_id: RSCH-01
      statement: "RSCH-01's unclassified edge is human completeness of the authority bundle; automated first-entry, precedence, provenance, and supersession gates remain the executable backstop."
      status: unresolved
      flagged: true
    - requirement_id: RSCH-03
      statement: "RSCH-03 empty/single/null behavior is resolved by the existing authority fixture tests: empty, null-like/malformed, singleton-incomplete, and duplicate watchlist tuples fail closed; a complete singleton is accepted."
      status: resolved
      flagged: true
    - requirement_id: RSCH-03
      statement: "RSCH-03 encoding is non-applicable to semantic normalization: artifacts are UTF-8 text while stable IDs/date/owner keys are ASCII literals, and no grapheme-equivalence or Unicode-normalization comparison is part of the watchlist contract."
      status: dismissed
      reason: "The watchlist contract compares stable ASCII identifiers and dates, not user-visible strings or Unicode-normalized semantic values."
      flagged: true
    - requirement_id: RAIL-04
      statement: "RAIL-04 equal/touching adjacency is resolved as exact tuple identity: duplicate equal rows fail, distinct ordered rows remain separate, and no interval merge semantics exist."
      status: resolved
      flagged: true
    - requirement_id: RAIL-04
      statement: "RAIL-04 empty/single/null behavior is resolved by registry validation: the built-in registry is non-empty, a valid singleton declaration is inspectable, and empty/nil/malformed declarations fail closed."
      status: resolved
      flagged: true
    - requirement_id: RAIL-04
      statement: "RAIL-04 equal-element ordering is resolved by unique source/capability tuples plus the registry's fixed source and capability order; equal duplicate elements are invalid rather than unstably sorted."
      status: resolved
      flagged: true
    - requirement_id: RAIL-05
      statement: "RAIL-05 remains unclassified for the unavailable authoritative Crosswake bridge and dated physical-device evidence; this plan strengthens native cache evidence but runtime coupling stays explicitly blocked."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT upgrade Crosswake runtime feasibility from native cache tests, simulator results, reachability, or lifecycle callbacks without the pinned bridge and dated physical-device evidence."
    - requirement_id: RSCH-03
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT let a watchlist trigger silently alter entitlement policy without a dated owned reassessment."
    - requirement_id: RAIL-04
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT make the entitlement-source matrix imply gateway ownership or dispatch Apple outcomes into Stripe mutation paths."
---

<objective>
Make Swift offline cache replacement concurrency-safe and durably prove crash/reopen behavior, then re-run the complete Phase-215 contract gates.

Decision coverage: D-10 requires durable local state and atomic verified replacement; D-11 requires concurrency plus crash/fault evidence; D-12 keeps missing Crosswake/device evidence blocking; D-13 allows only verified newer allow or signed denial to replace cached state.

Purpose: Close the final RAIL-05 tracer defect without expanding into Phase-219 production offline runtime.
Output: Per-cache serialization, unique candidates/cleanup, parent-directory durability, subprocess crash/reopen proof, and full phase regression evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-01-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-04-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-07-SUMMARY.md
@examples/crosswake_tracer/Package.swift
@examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
</context>

## Artifacts this phase produces

- A per-standardized-path cache replacement coordinator shared by all cache handles.
- Unique same-directory candidate creation, cleanup, candidate fsync, atomic rename, and parent-directory durability barrier.
- Concurrent-replacement and subprocess crash/reopen regression tests plus a bounded test harness executable.
- Full Phase-215 regression evidence for all five requirements.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Serialize one real concurrent allow/deny race per cache path</name>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (third failed truth)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-10 through D-13)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
    - examples/crosswake_tracer/Package.swift
  </read_first>
  <behavior>
    - Test 1: two distinct cache handles targeting the same standardized URL serialize concurrent replacements through the same coordinator.
    - Test 2: every concurrent operation gets a unique candidate in the target directory and all candidates are removed after success or injected failure.
    - Test 3: a same/newer denial racing an allow finishes as complete denial; an older allow cannot resurrect access.
    - Test 4: repeated task-group races expose only complete known payloads and never ENOENT, torn bytes, cross-renamed candidates, or leaked candidate files.
  </behavior>
  <action>Write a failing Swift concurrency regression first, using multiple independently constructed cache handles for the same standardized file path so instance-local locking cannot pass accidentally. Refactor `AtomicOfflineCache` behind a Sendable per-cache coordinator keyed by standardized path; all replace/read operations for that path must serialize. Create each candidate with an unguessable unique name in the same parent directory, write and synchronize it, rename/replace while holding the path coordinator, and clean it with a defer-style path on every exit. Carry revision/disposition metadata into the test seam so a same/newer denial wins and an older allow is rejected before filesystem replacement per D-13. Avoid one global lock across unrelated cache paths and do not introduce a new package or Crosswake API.</action>
  <acceptance_criteria>
    - A stress test races at least two handles and multiple iterations against one cache path and exits 0 under Swift strict concurrency checks.
    - The final cache is a complete allowed outcome permitted by monotonic ordering or the complete winning denial, never partial/missing data.
    - Directory inspection after successful and faulted races finds no candidate artifacts.
    - Concurrent operations on different cache paths are not forced through one process-wide critical section.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter GoldenVectorTests</automated>
  </verify>
  <done>All handles for one cache path share serialization, unique candidates cannot collide, cleanup is complete, and denial/ordering semantics survive concurrent replacement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove parent-directory durability and crash/reopen recovery</name>
  <files>examples/crosswake_tracer/Package.swift, examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift</files>
  <read_first>
    - examples/crosswake_tracer/Package.swift
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
    - examples/crosswake_tracer/physical-device-evidence.md
    - scripts/ci/verify_v159_authority.sh
    - scripts/ci/verify_entitlement_source_matrix.sh
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
  </read_first>
  <behavior>
    - Test 1: replacement synchronizes candidate contents before rename and the parent directory after rename before returning success.
    - Test 2: a child process terminated before rename reopens the old complete cache and leaves no candidate after recovery cleanup.
    - Test 3: a child process terminated after rename and directory durability barrier reopens the complete new cache.
    - Test 4: repeated reopen through a fresh cache instance never resurrects an older allow after a committed denial.
    - Test 5: the complete Phase-215 authority, decision, source-matrix, Elixir vector, and Swift suites remain green while capability-report status stays feasibility_blocked.
    - Test 6: executing vector verification and cache replacement paths cannot mutate or upgrade the checked-in capability report; reloading it still yields overall feasibility_blocked and all evidence-dependent rows remain blocked.
  </behavior>
  <action>Add a bounded executable target used only by tests to perform one cache replacement and terminate at deterministic before-rename or after-directory-sync fault points. In the cache implementation, synchronize the candidate file, perform the atomic same-directory rename, then open and fsync the parent directory using Darwin/POSIX APIs before reporting committed success; handle only documented platform unsupported-directory-sync errors explicitly and fail every other durability error. On open/recovery, remove abandoned unique candidates while serialized and read only the canonical cache. Drive the harness with `Process`, wait for its exit, instantiate a fresh cache object, and assert old-or-new complete state plus denial non-resurrection. Keep physical-device atomicity evidence pending and `capability-report.json` unchanged unless authoritative evidence exists. Finish by running all Phase-215 gates; this regression task verifies RSCH-01/03 and RAIL-04 without changing their surfaces.</action>
  <acceptance_criteria>
    - Tests prove the exact write → candidate sync → atomic rename → parent-directory sync order through injectable events.
    - Child-process before/after crash cases reopen old/new complete bytes respectively and leave no abandoned candidates after recovery.
    - Unsupported parent-directory synchronization is surfaced by a named platform disposition; arbitrary I/O errors never pass as durable success.
    - `capability-report.json` remains `feasibility_blocked` absent pinned Crosswake and dated physical-device evidence.
    - Authority/watchlist, decision export/schema/property, source matrix, Elixir vector, and complete Swift package gates all exit 0.
    - The capability-report invariant test runs after vector/cache operations, and `git diff --exit-code -- examples/crosswake_tracer/capability-report.json` proves this plan did not rewrite the feasibility disposition.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_v159_authority.sh &amp;&amp; bash scripts/ci/verify_entitlement_source_matrix.sh &amp;&amp; cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs test/property/entitlement_decision_cases_property_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs &amp;&amp; cd ../examples/crosswake_tracer &amp;&amp; swift test &amp;&amp; ruby -rjson -e 'r=JSON.parse(File.read("capability-report.json")); abort unless r.fetch("overall_status")=="feasibility_blocked" &amp;&amp; r.fetch("capabilities").all? { |c| c.fetch("status")=="feasibility_blocked" }' &amp;&amp; cd ../.. &amp;&amp; git diff --exit-code -- examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>A fresh process can recover only the complete durable old or committed new cache, concurrent denial cannot be undone by an older allow, and all five Phase-215 requirement gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| concurrent reconciliation tasks → one cache path | Distinct callers can race writes and candidate renames. |
| process/filesystem crash → reopened cache | Durability ordering determines whether old/new state survives without resurrection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-31 | Tampering | candidate/rename sequence | high | mitigate | Per-path serialization, unique same-directory candidates, cleanup, and concurrent stress tests prevent cross-replacement. |
| T-215-32 | Elevation of Privilege | allow/deny race | high | mitigate | Monotonic ordering plus same/newer denial precedence is asserted under concurrent handles and reopen. |
| T-215-33 | Denial of Service | abandoned candidates/cache absence | medium | mitigate | Serialized recovery cleanup and old-or-new canonical reads reject missing/torn state. |
| T-215-34 | Repudiation | durability claim | medium | mitigate | Subprocess fault points and ordered sync-event assertions provide repeatable crash/reopen evidence. |
| T-215-35 | Information Disclosure | candidate filenames/data | low | accept | Candidates remain in the protected cache directory, use random names, and are removed after every path; test payloads contain no PII. |

## ASVS L1 Control Mapping

**security_block_on: high.** Any failed high-severity ASVS control or its named automated evidence is merge-blocking and prevents execution completion and Phase-215 closure.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Not changed | Replacement still consumes only previously verified server evidence; authenticated transport remains blocked pending Crosswake proof. |
| V3 Session Management | Applicable | Per-cache serialization and monotonic revision ordering prevent replay/race state regression. |
| V4 Access Control | Applicable | Concurrent and reopened same/newer denial wins; older allow cannot regain access. |
| V5 Input Validation | Applicable | Cache metadata and fault/harness arguments are bounded; malformed/older replacements fail before write. |
| V6 Cryptography | Inherited | 215-07 verifies proofs before this replacement boundary; full regression reruns both cryptographic suites. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One evidence-backed contract and honest Crosswake feasibility decision | 215-06..08 | COVERED | Contract proof, vector oracle, cache durability, and explicit block are closed together. |
| REQ | RSCH-01 | Current research authority | 215-08 | COVERED | Existing 215-02 artifacts are regression-gated; human completeness remains flagged. |
| REQ | RSCH-02 | Decision table drives consumers | 215-06, 215-07 | COVERED | Closed D-07 consumer proof and self-validating vectors. |
| REQ | RSCH-03 | Dated watchlist | 215-08 | COVERED | Existing malformed/empty/duplicate gate reruns; encoding non-applicability is explicit. |
| REQ | RAIL-04 | Separate source matrix | 215-08 | COVERED | Existing order/duplicate/empty/leakage gate reruns; probe rows are explicit. |
| REQ | RAIL-05 | Prove-or-block Crosswake tracer | 215-07, 215-08 | COVERED | Complete vectors plus concurrent durable cache proof; unavailable bridge/device evidence remains an honest block. |
| RESEARCH | — | Canonical data, strict JWS, no new package, fail-closed feasibility | 215-06..08 | COVERED | No dependency install, runtime issuer, or invented Crosswake API. |
| CONTEXT | D-01, D-02, D-03, D-04 | Authority and supersession decisions | 215-08 | COVERED | Existing 215-02 artifacts retained and regression-gated. |
| CONTEXT | D-05, D-06, D-07, D-08 | Closed canonical case data and properties | 215-06, 215-07 | COVERED | Direct gap closure. |
| CONTEXT | D-09, D-10, D-11, D-12, D-13 | Host-owned tracer, complete proof lanes, hard block, verified replacement | 215-07, 215-08 | COVERED | Direct gap closure without runtime coupling. |
| CONTEXT | D-14, D-15, D-16, D-17 | Separate typed source capability contract | 215-08 | COVERED | Existing 215-04 artifacts retained and regression-gated. |

All eight spec-less probe rows are authored under `must_haves.assumptions` across 215-06 and 215-08: RSCH-01 unclassified; RSCH-02 unclassified; RSCH-03 empty and encoding; RAIL-04 adjacency, empty, and ordering; RAIL-05 unclassified. Prohibition recall keeps the bespoke no-second-authority, no-label-as-proof, no-feasibility-upgrade, no-silent-watchlist-policy, and no-gateway-ownership constraints; generic OWASP concerns remain in the threat models and `$gsd-secure-phase` canon. Deferred Phase-216–220 runtime work is excluded. No ORM/schema path is modified, so no schema push task applies. External API coverage does not change, so COVERAGE.md is preserved without an opt-out change.
</source_audit>

<verification>
Run the full commands in Task 2, confirm no candidate files remain after tests, require the capability-report invariant test plus JSON/static-diff gates to keep every row and the overall result `feasibility_blocked`, and verify `git diff --name-only` contains no Phase-219 production or schema files. The fallback prohibitions stay descriptor-less (`verification: null`, flagged) by contract; the test/static gates are executable evidence and are not `check_*` descriptors.
</verification>

<success_criteria>
Cache replacement is serialized per path, uses collision-free durable rename semantics, survives real child-process crash/reopen tests, preserves denial ordering, and leaves all Phase-215 research/decision/source/feasibility gates green without claiming unavailable Crosswake proof.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-08-SUMMARY.md` when done.
</output>

---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 09
type: execute
wave: 7
depends_on:
  - 215-08
files_modified:
  - accrue/lib/accrue/entitlements/decision_cases/markdown.ex
  - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
  - .planning/research/v1.59-DECISION-TABLE.md
  - accrue/test/accrue/entitlements/decision_cases_test.exs
  - accrue/test/support/entitlements/offline_golden_vector_verifier.ex
  - accrue/test/accrue/entitlements/offline_golden_vectors_test.exs
autonomous: true
gap_closure: true
requirements:
  - RSCH-02
  - RAIL-05
must_haves:
  truths:
    - "The generated decision table renders each canonical `expected.continuity` value under Continuity and preserves lease as a separately labeled concept per D-05/D-07."
    - "The offline export check rejects drift in every top-level and per-vector schema field, duplicate vector IDs, missing vectors, extra vectors, and canonical case/version/disposition mismatches per D-05/D-11."
    - "The Elixir corpus consumer validates each vector's case ID, contract version, and expected disposition against `DecisionCases.all/0` before accepting its declared verification/cache oracle."
    - "RSCH-02's production-reducer-equivalence edge remains unresolved until Phase 217; this plan corrects the Phase-215 contract and does not claim runtime reducer equivalence."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/decision_cases/markdown.ex"
      provides: "Semantically correct decision-table renderer and complete offline-corpus drift oracle"
    - path: ".planning/research/v1.59-DECISION-TABLE.md"
      provides: "Regenerated maintainer table with distinct lease and continuity semantics"
    - path: "accrue/test/accrue/entitlements/offline_golden_vectors_test.exs"
      provides: "Elixir canonical metadata and uniqueness consumer regression"
  key_links:
    - from: "accrue/lib/accrue/entitlements/decision_cases/markdown.ex"
      to: ".planning/research/v1.59-DECISION-TABLE.md"
      via: "Markdown.render/1 emits expected.continuity under the Continuity heading and the Mix task regenerates the checked-in view"
      pattern: "expected\\.continuity|Continuity"
    - from: "accrue/lib/accrue/entitlements/decision_cases/markdown.ex"
      to: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      via: "Export.check/1 compares the complete decoded generated corpus, schema version, exact key sets, vector identity, and canonical bindings"
      pattern: "case_id|contract_version|expected_disposition|compact_jws"
    - from: "accrue/test/accrue/entitlements/offline_golden_vectors_test.exs"
      to: "accrue/lib/accrue/entitlements/decision_cases.ex"
      via: "the Elixir reader resolves every decoded vector binding against DecisionCases.all/0"
      pattern: "DecisionCases"
  assumptions:
    - requirement_id: RSCH-02
      statement: "The deterministic probe classified downstream production-consumer behavior as unclassified; production equivalence remains a Phase-217 backstop while Phase 215 validates its canonical generated and test-support consumers."
      status: unresolved
      flagged: true
    - requirement_id: RAIL-05
      statement: "The unavailable authoritative Crosswake bridge and dated physical-device evidence remain outside this corpus correction; runtime coupling stays feasibility_blocked."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RSCH-02
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT present lease/freshness values as offline-continuity outcomes or treat a partial metadata comparison as canonical binding."
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT let a fixture label, duplicated vector ID, or unbound case/version/disposition field stand in for an executed canonical contract check."
---

<objective>
Correct the human decision-table semantics and make the generated offline corpus a complete canonical assertion boundary in Elixir.

Decision coverage: D-05 keeps Markdown/JSON/offline views derived from one versioned corpus; D-06 keeps renderers and readers non-runtime; D-07 distinguishes lease from continuity; D-11 makes every signed-vector binding and outcome field merge-blocking.

Purpose: Close RSCH-02's false Continuity presentation and the generator/Elixir half of the canonical metadata drift gap before Swift consumes the corrected contract.
Output: Corrected generated Markdown, exhaustive offline drift validation, regenerated corpus bytes, and an Elixir reader that validates canonical metadata before outcome assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-07-SUMMARY.md
@accrue/lib/accrue/entitlements/decision_cases.ex
@accrue/lib/accrue/entitlements/decision_cases/markdown.ex
</context>

## Artifacts this phase produces

- A generated v1.59 decision table whose lease and continuity columns are semantically distinct.
- A complete decoded-corpus drift oracle covering schema metadata, exact vector fields, uniqueness, and canonical binding.
- Elixir consumer assertions that bind every offline vector to the canonical DecisionCase/version/disposition tuple.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one canonical case through correct Continuity rendering and full corpus drift rejection</name>
  <reversibility rating="costly">The generated table and offline corpus are shared contracts consumed by maintainers, Elixir, Swift, and later Phase-219 work under D-05.</reversibility>
  <files>accrue/lib/accrue/entitlements/decision_cases/markdown.ex, accrue/priv/entitlements/v1.59-offline-golden-vectors.json, .planning/research/v1.59-DECISION-TABLE.md, accrue/test/accrue/entitlements/decision_cases_test.exs</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (failed decision-table truth and offline binding truth)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-05 through D-08 and D-11)
    - accrue/lib/accrue/entitlements/decision_cases.ex (`Expected` lease/continuity fields and canonical values)
    - accrue/lib/accrue/entitlements/decision_cases/markdown.ex (`render/1`, `Export.check/1`, `offline_drift/3`, and generated vector shape)
    - accrue/lib/accrue/processor/capabilities.ex (closed-data declarations and total fail-closed renderer/lookup analog)
    - accrue/test/accrue/entitlements/decision_cases_test.exs (existing generated-view and three-field mutation tests)
    - accrue/test/accrue/entitlements/local_map_test.exs (isolated deterministic regression-test analog)
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json (current generated schema)
    - .planning/research/v1.59-DECISION-TABLE.md (current false Continuity output)
  </read_first>
  <behavior>
    - Test 1: a case whose lease and continuity differ renders the continuity value under Continuity and the lease value only under a distinct Lease heading.
    - Test 2: mutating case_id, contract_version, expected_disposition, compact_jws, any expected result, fault_point, vector id, or top-level schema metadata makes Export.check/1 fail with the vector/field diagnostic.
    - Test 3: duplicate IDs, missing vectors, extra vectors, missing keys, and unexpected keys fail even when all remaining expected-result fields match.
    - Test 4: repeated generation remains byte-identical and every generated case/version/disposition comes from DecisionCases.all/0.
  </behavior>
  <action>Write the semantic regression first using a canonical case where `expected.lease` and `expected.continuity` are observably different. Apply the closed-data and total fail-closed lookup/rendering conventions from `Accrue.Processor.Capabilities`: keep the renderer a pure consumer of the explicitly ordered canonical corpus and return a bounded failure for every value outside that closed contract. Change `Markdown.render/1` so the Continuity cell reads `expected.continuity`; retain lease only as its own clearly labeled column so D-07 data is not dropped. Regenerate `.planning/research/v1.59-DECISION-TABLE.md`. Replace `offline_drift/3`'s three-field oracle with a complete decoded-contract comparison: require the expected top-level schema/purpose and exact vector list; reject duplicate IDs before building maps; require equal ID sets and exact per-vector key sets; compare `id`, `case_id`, `contract_version`, `expected_disposition`, `compact_jws`, `expected_verification`, `expected_reason`, `expected_cache_disposition`, and optional `fault_point`; reject missing and additional vectors. Keep generated expected bindings sourced from `DecisionCases.all/0` per D-05 and return stable path/vector/field diagnostics. Following the isolated deterministic regression style in `local_map_test.exs`, expand temp-root mutation tests field-by-field and for duplicate/missing/extra vectors without shared runtime state or provider calls. Do not broaden this task into runtime reducers, source adapters, or a public facade, and do not hand-edit checked-in generated outputs as authority.</action>
  <acceptance_criteria>
    - The generated table has separately labeled Lease and Continuity columns and every row's Continuity cell equals its canonical `expected.continuity` value.
    - `mix accrue.entitlements.decision_cases --check` exits 0 on checked-in files.
    - A mutation test covers every top-level/per-vector field, duplicate IDs, missing/extra vectors, and missing/extra keys; each mutation makes `Export.check/1` return an error naming the affected contract location.
    - Generated offline vector IDs are unique and their case ID/version/disposition values exactly match `DecisionCases.all/0`.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs</automated>
  </verify>
  <done>One canonical case renders the correct continuity outcome and any semantic or byte-bearing offline metadata drift is rejected by the generator gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind every Elixir vector observation to canonical case metadata</name>
  <files>accrue/test/support/entitlements/offline_golden_vector_verifier.ex, accrue/test/accrue/entitlements/offline_golden_vectors_test.exs</files>
  <read_first>
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
    - accrue/priv/entitlements/v1.59-decision-cases.json
    - accrue/lib/accrue/entitlements/decision_cases.ex
    - accrue/test/support/entitlements/offline_golden_vector_verifier.ex
    - accrue/test/accrue/entitlements/offline_golden_vectors_test.exs
    - accrue/test/accrue/entitlements/decision_cases_test.exs (complete generator mutation expectations from Task 1)
  </read_first>
  <behavior>
    - Test 1: the reader rejects a vector whose case_id is absent from DecisionCases.all/0.
    - Test 2: the reader rejects contract_version or expected_disposition that differs from its canonical DecisionCase.
    - Test 3: duplicate vector IDs are rejected before observation sorting or map construction.
    - Test 4: valid vectors still execute cryptographic/claim/cache observation and match their declared outcomes after canonical binding succeeds.
  </behavior>
  <action>Decode and retain `case_id`, `contract_version`, and `expected_disposition` for every vector instead of treating them as unused fixture metadata. Before running JWS observations, require the top-level schema version, unique vector IDs, and exact lookup against `DecisionCases.all/0`; return a bounded contract error naming vector ID and mismatched field for unknown cases or version/disposition drift. Keep expected verification/reason/cache values as the outcome oracle only after binding succeeds. Add mutation-sensitive ExUnit coverage through a temporary fixture seam or a pure validation helper so each canonical field and duplicate ID fails independently. Preserve the test-only boundary and do not introduce a second reducer or Phase-219 issuer.</action>
  <acceptance_criteria>
    - Every vector is validated against exactly one canonical DecisionCase before signature/claim/cache observation.
    - Unknown case, version drift, disposition drift, schema-version drift, and duplicate IDs fail with stable bounded diagnostics.
    - The unchanged corpus still yields one sorted observed tuple per unique vector and the targeted Elixir suite exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs</automated>
  </verify>
  <done>The Elixir reader can no longer report a green vector corpus whose canonical case, version, disposition, or identity metadata has drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical DecisionCases → generated Markdown/offline corpus | Derived views must preserve the intended field semantics and complete canonical identity. |
| checked-in offline JSON → Elixir test consumer | Modified fixture metadata must fail before cryptographic outcomes are treated as canonical evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-36 | Tampering | Markdown semantic projection | high | mitigate | A case with differing lease/continuity values proves `Markdown.render/1` and the checked-in table expose the D-07 fields under distinct headings. |
| T-215-37 | Tampering | offline drift oracle | high | mitigate | Complete schema/key/value comparison plus duplicate/missing/extra-vector mutation tests make every corpus field merge-blocking. |
| T-215-38 | Elevation of Privilege | Elixir vector binding | high | mitigate | The reader rejects unknown case IDs and version/disposition drift before observing or accepting a signed allow. |
| T-215-39 | Repudiation | vector identity | medium | mitigate | Unique vector IDs and stable field-specific diagnostics preserve one auditable result per fixture. |
| T-215-40 | Information Disclosure | generated fixtures | low | accept | Existing test-only, privacy-safe corpus remains unchanged in sensitivity and contains no adopter identity or production keys. |

## ASVS L1 Control Mapping

**security_block_on: high.** All high-severity controls above are merge-blocking through the named targeted tests.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable as binding data | Account/device checks remain in the reader and now run only after canonical vector identity succeeds. |
| V3 Session Management | Applicable as version/order data | Contract version and signed-vector high-water fields cannot drift outside the generated canonical corpus. |
| V4 Access Control | Applicable | Unknown/misbound case metadata cannot reach an accepted allow outcome. |
| V5 Input Validation | Applicable | Exact schema/key sets, unique IDs, and complete field mutation coverage reject malformed or ambiguous fixture data. |
| V6 Cryptography | Inherited | Existing OTP ES256 tests rerun after canonical binding validation; no cryptographic package or issuance path changes. |
</threat_model>

<source_audit>
GOAL truth 2 and REQ RSCH-02 are directly closed by correct Continuity rendering and complete canonical generator/Elixir binding. RAIL-05 is covered for the Elixir half of the shared D-11 corpus; Swift consumes the same corrected metadata in 215-10. CONTEXT D-05/D-06/D-07/D-11 is cited without adding runtime reducer/issuer behavior. RESEARCH canonical-data, derived-view, strict validation, deterministic fixture, and no-new-package constraints are preserved. VERIFICATION blockers 1 and the generator/Elixir portion of blocker 2 are covered item-for-item. The RSCH-02 and RAIL-05 unclassified fallback assumptions remain flagged, and descriptor-less prohibitions remain descriptor-less. No deferred Phase-216–220 runtime work or ORM schema path is included. `COVERAGE.md` remains valid because no external API surface is added.
</source_audit>

<verification>
Run the deterministic export check plus the decision-case and offline-vector ExUnit suites. Require the semantic lease-versus-continuity regression, exhaustive corpus mutation table, unique-ID rejection, and canonical Elixir binding tests. Validate the existing API coverage artifact with `test -s .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md` and `rg -q 'No external API integration'` rather than generating a new matrix.
</verification>

<success_criteria>
Maintainers see the true continuity outcome, the generator rejects drift in every offline field and identity constraint, and Elixir validates canonical case/version/disposition bindings before trusting vector outcomes.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-09-SUMMARY.md` when done.
</output>

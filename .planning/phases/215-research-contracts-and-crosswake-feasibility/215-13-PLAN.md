---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 13
type: execute
wave: 11
depends_on:
  - 215-12
files_modified:
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
  - .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
autonomous: true
gap_closure: true
requirements:
  - RAIL-05
must_haves:
  truths:
    - "Every production AtomicOfflineCache write requires an authentication key plus an explicit verified disposition and revision; no public no-key/raw-byte path can replace authenticated state, per D-10/D-11/D-13."
    - "An attempted legacy raw/no-key overwrite at the canonical path cannot alter a persisted authenticated signed-denial envelope, its revision, its disposition, or its authentication tag."
    - "CheckedInCapabilityReportValidator rejects an all-proven report when any required evidence location is unavailable, pending, non-resolving, outside the evidence root, or inconsistent with its evidence kind, per D-11/D-12."
    - "A checked-in report has a non-empty reason consistent with its uniform terminal status; missing, blocked-while-proven, and proof-claim-while-blocked mutations reject."
    - "The checked-in report remains byte-unchanged and feasibility_blocked until real pinned Crosswake bridge and dated physical-device evidence satisfy every required lane."
    - "Previously passing RSCH-01, RSCH-02, RSCH-03, and RAIL-04 gates remain green while RAIL-05 closes both current verification regressions."
  artifacts:
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift"
      provides: "Authenticated-only production cache writes and evidence-root-aware checked-in capability validation"
    - path: "examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift"
      provides: "Authenticated crash/replacement operations with the obsolete raw invocation rejected before touching the canonical file"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift"
      provides: "Authenticated atomicity, concurrency, recovery, and public-surface regressions after raw production API removal"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift"
      provides: "Separate-process attempted no-key overwrite regression preserving the exact denial envelope"
    - path: "examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift"
      provides: "All-proven-unavailable, artifact-resolution, and terminal-reason mutation regressions"
    - path: ".planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md"
      provides: "A detector-valid no-external-API declaration retaining the existing detailed deferred-capability explanation"
  key_links:
    - from: "AtomicOfflineCache public initializer and replacement surface"
      to: "authenticated envelope encoding and ProofReplacementOrder"
      via: "the only production construction path requires SymmetricKey, and every replacement carries disposition and revision before the coordinated write"
      pattern: "authenticationKey|Disposition|revision|ProofReplacementOrder"
    - from: "AtomicOfflineCacheProcessTests"
      to: "AccrueOfflineCacheCrashHarness"
      via: "a signed denial is committed through the authenticated operation, then the obsolete no-key invocation is attempted in a fresh process and must fail without changing canonical bytes"
      pattern: "replace|deny|raw|terminationStatus"
    - from: "CheckedInCapabilityReportValidator"
      to: "capability-report.json evidence artifacts"
      via: "validation resolves each relative location from the report directory, enforces containment and kind-specific artifact requirements, and checks physical-device completion"
      pattern: "reason|location|EvidenceKind|physicalDevice"
  assumptions:
    - requirement_id: RAIL-05
      statement: "The spec-less probe classified RAIL-05 as unclassified; the authoritative verification gaps and D-10 through D-13 define the cache and evidence behavior, while no fabricated edge descriptor or Crosswake API is inferred."
      status: unresolved
      flagged: true
    - requirement_id: RSCH-01
      statement: "The RSCH-01 unclassified probe result is non-applicable to these Swift-only repairs; the existing authority gate is rerun and no research-authority criterion is invented."
      status: unresolved
      flagged: true
    - requirement_id: RSCH-02
      statement: "The RSCH-02 unclassified probe result is non-applicable to these Swift-only repairs; canonical decision/vector gates rerun without inferring a new case shape."
      status: unresolved
      flagged: true
    - requirement_id: RSCH-03
      statement: "The RSCH-03 empty and encoding probe results are non-applicable because this plan changes neither watchlist cardinality nor watchlist serialization; the authority/watchlist gate remains the backstop."
      status: unresolved
      flagged: true
    - requirement_id: RAIL-04
      statement: "The RAIL-04 adjacency, empty, and ordering probe results are non-applicable because no source-capability row, registry order, or matrix output changes; the source-isolation gate reruns."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT expose an unauthenticated production cache writer that can replace a signed denial or bypass verified disposition/revision ordering."
    - requirement_id: RAIL-05
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT classify unavailable, pending, non-resolving, path-escaping, or semantically incomplete evidence as proven Crosswake feasibility."
    - requirement_id: RAIL-05
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT lift feasibility_blocked from native-only success or without the pinned Crosswake bridge and dated physical-device evidence required by D-11/D-12."
---

<objective>
Close both current Phase-215 verification regressions by making production offline-cache writes authenticated-only and making the checked-in Crosswake report prove real, terminal-state-consistent evidence rather than accept arbitrary location text.

Decision coverage: D-09 keeps the repair inside the checked-in host-owned tracer without adding Crosswake runtime coupling; D-10 requires authenticated durable state and verified atomic allow/deny replacement; D-11 requires rollback/deny-precedence, crash/restart, compile-unit, and dated device evidence; D-12 keeps any missing bridge/device lane feasibility_blocked; D-13 permits only verified server-issued replacement and denies lifecycle/reachability any grant authority.

Locked-decision traceability: D-01: preserve the existing authority entry point; D-02: preserve its precedence; D-03: preserve its amendment/watchlist governance; D-04: preserve source history and rejected alternatives; D-05: preserve the canonical versioned case source and derived views; D-06: keep that source out of production decision logic; D-07: preserve the closed case schema; D-08: rerun the named property/transaction-boundary contract; D-09: remain in the host-owned reference target; D-10: require authenticated durable replacement; D-11: retain merge-blocking fault/restart/evidence proof; D-12: keep missing bridge/device proof feasibility_blocked; D-13: admit no unauthenticated replacement authority; D-14: leave source and processor boundaries separate; D-15: preserve the closed source-state vocabulary; D-16: preserve provider-honest management and mutation isolation; D-17: rerun source conformance, documentation-drift, and gateway-leakage gates.

Purpose: Remove the two bypasses that still make RAIL-05 unsafe to consume: an unauthenticated writer can currently destroy denial state, and a fabricated all-proven report can currently unlock the feasibility boundary without real evidence.
Output: An authenticated-only AtomicOfflineCache production surface, an obsolete-raw-invocation denial-preservation regression, a closed evidence-location/reason validator, mutation tests for false proof, and a complete green Phase-215 regression pass.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-PATTERNS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VALIDATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-REVIEW.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-12-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md
@examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
@examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
@examples/crosswake_tracer/capability-report.json
@examples/crosswake_tracer/physical-device-evidence.md
</context>

## Artifacts this phase produces

- Modified production symbols in `AccrueOfflineClient.swift`: `AtomicOfflineCache` loses its public no-key initializer and raw payload replacement overload; its production initializer requires `SymmetricKey`, and its public replacement path requires explicit `Disposition` and `revision`. `CacheCoordinator.accepts` loses the implicit max-revision compatibility branch. `CheckedInCapabilityReportValidator` gains report-root-aware evidence resolution, required `reason` decoding, terminal-reason validation, path containment, kind-specific artifact validation, and physical-device completion checks.
- Modified executable symbols in `Sources/AccrueOfflineCacheCrashHarness/main.swift`: authenticated `replace` and fault operations remain available to tests; the legacy no-key argument form is rejected before constructing a cache or writing the canonical path.
- Modified test symbols in `GoldenVectorTests.swift`: raw-byte atomicity/concurrency/recovery cases become authenticated disposition/revision cases while preserving before-rename, after-rename, serialized-write, candidate-cleanup, and reopened-state coverage.
- New test symbols in `AtomicOfflineCacheProcessTests.swift`: a separate-process regression snapshots an authenticated signed-denial envelope, attempts the obsolete no-key overwrite form, and proves the exact envelope and candidate set are unchanged.
- New test symbols in `CapabilityReportTests.swift`: mutations for all-proven rows retaining unavailable locations, non-resolving or path-escaping artifacts, missing reason, a blocked reason on proven status, and a proof reason on blocked status; existing coherent blocked and schema/row mutations remain.
- Modified file paths: the six paths listed in `files_modified`; new files: none.
- Unchanged artifacts: `examples/crosswake_tracer/capability-report.json`, `examples/crosswake_tracer/physical-device-evidence.md`, both canonical entitlement JSON corpora, research authority documents, source registry/matrix, Elixir modules, and database/ORM schemas.
- New dependencies, package installs, environment variables, external API matrices, schema pushes, migrations, UI symbols, Crosswake APIs, or runtime coupling: none.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace authenticated denial persistence through an attempted legacy no-key overwrite</name>
  <reversibility rating="reversible">The change narrows an unsafe reference-client API before downstream runtime coupling; undo is a local Swift surface change with no persisted-format migration or published package release.</reversibility>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (first failed truth, unsafe public link, exact missing regression)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-REVIEW.md (CR-01 exploit path)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-09 through D-13)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-12-SUMMARY.md (shared ProofReplacementOrder and authenticated process sequence already shipped)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`AtomicOfflineCache` initializers and replacement overloads, `encodedReplacement`, `loadVerifiedEnvelope`, `CacheCoordinator.accepts`)
    - examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift (legacy four-argument raw mode and authenticated six-argument operations)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift (atomic fault, concurrency, recovery, and child-process cases that still use the raw surface)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift (authenticated denial restart harness)
  </read_first>
  <behavior>
    - Test 1: public construction of AtomicOfflineCache requires a SymmetricKey, and public replacement requires payload, disposition, and revision; the production module exposes no raw/no-key write overload.
    - Test 2: authenticated before-rename failure retains the old authenticated envelope; authenticated after-rename failure exposes the complete new authenticated envelope; both leave no candidate residue after recovery.
    - Test 3: authenticated concurrent handles still serialize on one canonical path and retain same-revision denial precedence under ProofReplacementOrder.
    - Test 4: a fresh process commits signed denial revision n, a second process invokes the obsolete raw/no-key argument form against the same path, that invocation fails, and reopen observes byte-identical denial payload, revision, disposition, authentication tag, and no candidate files.
    - Test 5: authenticated allow n to signed deny n to stale/equal allow process ordering from Plan 215-12 remains green.
  </behavior>
  <action>Write the focused process regression first: persist a denial through the authenticated harness, snapshot the canonical envelope bytes, invoke the former four-argument no-key/raw operation in a fresh process, require nonzero termination, then reopen with the key and compare the exact bytes plus recovered payload, revision, disposition, and empty candidate set. Remove the public `AtomicOfflineCache(url:)` initializer and public raw `replace(with:fault:)` overload, make key presence non-optional inside the production cache, and remove the `.allow`/`Int64.max` acceptance exception from `CacheCoordinator`. Keep the existing authenticated initializer and explicit disposition/revision replacement as the sole production writer per D-10/D-13. Delete the crash harness's legacy raw branch so invalid legacy arguments exit before cache construction or filesystem mutation. Convert the existing fault, concurrency, recovery, and crash tests to use deterministic test keys plus explicit dispositions/revisions; preserve every atomicity and cleanup assertion rather than reducing the fault suite. Retain `ProofReplacementOrder`, authenticated envelope format, path-scoped lock, durability synchronization, and Plan-215-12 denial precedence unchanged. Do not introduce a second production cache, a public testing flag, an optional key fallback, an implicit revision, or any Crosswake/runtime dependency.</action>
  <acceptance_criteria>
    - Source/API inspection finds no public no-key AtomicOfflineCache initializer, raw payload replacement overload, optional authentication key, or max-revision allow exception.
    - Every production replacement creates an authenticated envelope from explicit payload, disposition, and revision before atomic rename.
    - The obsolete raw harness invocation exits nonzero before a write, and the persisted authenticated denial envelope remains byte-identical with the same recovered metadata and no candidate residue.
    - Before-rename, after-rename, concurrent-handle, restart, stale/equal allow, and recovery tests all remain substantive and green using authenticated inputs.
    - No checked-in fixture, capability report, evidence record, or generated contract changes.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter AtomicOfflineCacheProcessTests &amp;&amp; swift test --filter GoldenVectorTests &amp;&amp; ! rg -n 'public init\(url: URL\)[[:space:]]*\{' Sources/AccrueOfflineClient/AccrueOfflineClient.swift &amp;&amp; ! rg -n 'candidateRevision == \.max|authenticationKey: SymmetricKey\?' Sources/AccrueOfflineClient/AccrueOfflineClient.swift</automated>
  </verify>
  <done>The production cache has one authenticated disposition/revision write boundary, and a separate-process attempt to use the removed raw/no-key route cannot alter an authoritative signed denial.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject fabricated proven reports and rerun every Phase-215 contract gate</name>
  <reversibility rating="reversible">The validator is an internal checked-in-artifact gate; evidence policy can be adjusted locally without changing the report schema, persisted data, or external runtime API.</reversibility>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift, .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (second failed truth, hollow provenance flow, exact missing mutations)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-REVIEW.md (CR-02 false-proof path)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-09 through D-12 proof-or-block requirements)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/COVERAGE.md (API coverage detector result)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`Capability.requiredEvidenceKinds`, `EvidenceKind`, and `CheckedInCapabilityReportValidator`)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift (checked-in report loading and current duplicate mutation)
    - examples/crosswake_tracer/capability-report.json (current reason, blocked rows, unavailable/native/device/advisory location forms)
    - examples/crosswake_tracer/physical-device-evidence.md (blocked/pending evidence record that cannot satisfy a proven row)
    - scripts/ci/verify_v159_authority.sh
    - scripts/ci/verify_entitlement_source_matrix.sh
  </read_first>
  <behavior>
    - Test 1: the current checked-in report, current blocked reason, unavailable bridge rows, and pending physical-device record validate only as feasibility_blocked.
    - Test 2: changing every row and overall status to proven while retaining current unavailable, pending, or advisory-placeholder locations throws ValidationError.invalid.
    - Test 3: a proven report rejects a missing file, a path escaping the report evidence root, a directory, the wrong artifact kind, or an incomplete physical-device evidence record.
    - Test 4: omitted/empty reason, blocked-language reason on a proven report, and proof-complete language on a blocked report throw ValidationError.invalid.
    - Test 5: only complete uniform proven rows whose required locations resolve to permitted non-empty kind-specific artifacts and whose reason states completed proof can return proven; no native-only subset can do so.
    - Test 6: full Swift, authority/watchlist, decision-case/vector, and source-matrix suites stay green, while the checked-in report and generated contracts remain byte-clean.
  </behavior>
  <action>Add mutation helpers that start from the checked-in JSON rather than constructing a smaller synthetic report, then make the all-proven-unavailable and reason mutations fail before implementation. Change the checked-in validator boundary so it receives or derives the report file URL and can resolve evidence relative to the report directory; do not retain a public data-only validation path that cannot establish provenance. Decode the required top-level `reason` and enforce uniform terminal-state semantics: blocked reports require a non-empty unavailable/blocked explanation and at least one explicit unavailable lane, while proven reports require completed-proof wording and reject unavailable, blocked, pending, advisory-only, or template language. For every proven row, require the evidence-kind set exactly as today, resolve each relative location to a non-empty regular file contained beneath the report evidence root, and apply a closed kind-specific policy: native compile evidence resolves to the client source tree, Crosswake bridge compile evidence resolves to a dedicated checked-in pinned-bridge compile artifact, simulator evidence resolves to a substantive simulator result artifact, and physical-device evidence resolves to the dated device record only when every required lane has a non-pending result plus completed redaction attestation/reviewer approval. Reject absolute/path-escaping locations and mismatched kinds. Keep the current report feasibility_blocked and byte-unchanged because its Crosswake and physical-device evidence is genuinely unavailable. Normalize the first `COVERAGE.md` no-external-API declaration to a concise reason within the deterministic detector's 200-character limit while retaining the detailed capability/deferred-lane explanation below it; do not create an API matrix because this remains a local feasibility validator, not an external API integration. Do not fabricate bridge/device files, weaken required evidence kinds, infer Crosswake APIs, add dependencies, or turn native vector/cache success into runtime-feasibility proof. After focused tests pass, run the full Phase-215 gates for RSCH-01/02/03, RAIL-04, and RAIL-05 regression safety.</action>
  <acceptance_criteria>
    - All-proven mutation of the current report fails while any current unavailable, pending, advisory-placeholder, missing, escaping, directory, or kind-mismatched location remains.
    - Proven status is reachable only from the complete exact capability set with every required kind backed by a permitted resolving artifact and a completed-proof reason.
    - Missing, empty, blocked-while-proven, and proof-while-blocked reasons fail closed; the current blocked reason remains accepted for the current uniformly blocked report.
    - Physical-device evidence cannot satisfy proven status while its metadata, lanes, attestation, or reviewer approval remain pending/template values.
    - `capability-report.json` remains feasibility_blocked and byte-unchanged; no unavailable Crosswake/device capability is claimed and no runtime-coupling block is lifted.
    - The deterministic API-coverage detector accepts the existing no-external-API declaration, and no external capability matrix is introduced.
    - Full Swift, authority, decision-case/property/vector, and source-matrix gates exit 0 with no generated artifact drift.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter CapabilityReportTests &amp;&amp; swift test &amp;&amp; cd ../.. &amp;&amp; bash scripts/ci/verify_v159_authority.sh &amp;&amp; bash scripts/ci/verify_entitlement_source_matrix.sh &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query check.api-coverage-verify-pre 215 | rg -q '"passed": true' &amp;&amp; cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; mix test test/accrue/entitlements/decision_cases_test.exs test/property/entitlement_decision_cases_property_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs &amp;&amp; cd .. &amp;&amp; git diff --exit-code -- accrue/priv/entitlements/v1.59-decision-cases.json accrue/priv/entitlements/v1.59-offline-golden-vectors.json examples/crosswake_tracer/capability-report.json examples/crosswake_tracer/physical-device-evidence.md</automated>
  </verify>
  <done>The checked-in validator cannot turn placeholder text into proof, the honest feasibility block remains in force, and every Phase-215 requirement gate passes after both verification regressions are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller → AtomicOfflineCache production writer | Untrusted payload and ordering claims must enter only through an authenticated key plus explicit verified disposition/revision. |
| fresh process → canonical cache path | A process lacking the cache key must not replace or reinterpret the authenticated denial envelope. |
| capability-report JSON → feasibility terminal state | Checked-in status, reason, required kinds, and locations are untrusted until closed-schema and provenance validation succeeds. |
| evidence location → local artifact | Relative paths must stay inside the evidence root and resolve to the required kind-specific, non-placeholder artifact. |
| physical-device record → runtime-coupling decision | Template/pending/redaction-incomplete device evidence must never unlock Crosswake feasibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-57 | Tampering | public AtomicOfflineCache raw seam | critical | mitigate | Remove no-key/raw production entry points and prove an obsolete invocation cannot change exact authenticated denial bytes. |
| T-215-58 | Elevation of Privilege | signed denial replacement | high | mitigate | Require key, disposition, and revision on every production write; preserve same-revision denial precedence and stale/equal allow rejection. |
| T-215-59 | Spoofing | all-proven capability report | critical | mitigate | Validate complete rows, terminal reason, resolving contained locations, evidence-kind policy, and completed physical-device record before proven. |
| T-215-60 | Tampering | evidence path resolution | high | mitigate | Reject absolute/path-escaping, missing, directory, empty, placeholder, and wrong-kind evidence locations. |
| T-215-61 | Repudiation | physical-device evidence | high | mitigate | Require dated non-pending lanes, redaction attestation, and reviewer approval before the physical-device kind can support proven status. |
| T-215-62 | Denial of Service | malformed report or obsolete harness invocation | low | accept | Both inputs fail with bounded validation/usage errors before changing entitlement state; no grant behavior occurs. |

## ASVS L1 Control Mapping

**security_block_on: high.** Any failed critical/high cache-authentication, denial-preservation, evidence-containment, or false-proof regression is merge-blocking.

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V2 Authentication | Applicable | Public cache construction requires the host key; exact HMAC-authenticated denial bytes survive a keyless overwrite attempt. |
| V3 Session Management | Applicable | Restart/process tests restore revision/disposition before later candidates and reject stale/equal allow evidence. |
| V4 Access Control | Applicable | No no-key writer can widen access after signed denial; only verified ordering reaches atomic replacement. |
| V5 Input Validation | Applicable | Closed report schema, reason semantics, path containment, artifact-kind checks, and pending/template rejection fail false proof closed. |
| V6 Cryptography | Applicable, unchanged | Existing ES256 vector and HMAC envelope suites rerun; algorithms and key formats do not change. |
| V8 Data Protection | Applicable | Cache key remains host-supplied and unpersisted; evidence validation rejects path escape and requires redacted physical-device records. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One current evidence-backed contract and honest Crosswake feasibility before runtime coupling | 215-13 | COVERED | Authenticated denial cannot be bypassed and proven status requires real evidence. |
| REQ | RSCH-01 | Versioned research authority/provenance | 215-02, 215-08, 215-12; 215-13 regression | COVERED | Already verified; authority gate reruns. `RSCH-01 unclassified` remains a flagged non-applicable assumption. |
| REQ | RSCH-02 | Canonical decision contract drives consumers | 215-03, 215-05..10, 215-12; 215-13 regression | COVERED | Already verified; decision/property/vector gates rerun. `RSCH-02 unclassified` remains flagged; no case criterion is invented. |
| REQ | RSCH-03 | Dated watchlist and owner responses | 215-02, 215-08, 215-12; 215-13 regression | COVERED | Already verified; authority/watchlist gate reruns. `RSCH-03 empty` and `encoding` are explained probe noise for untouched watchlist data. |
| REQ | RAIL-04 | Dedicated entitlement-source capability matrix | 215-04, 215-08, 215-12; 215-13 regression | COVERED | Already verified; source/leakage gate reruns. `RAIL-04 adjacency`, `empty`, and `ordering` are explained noise for untouched source rows. |
| REQ | RAIL-05 | Crosswake tracer proves or explicitly blocks every required client/device capability | 215-13 Tasks 1-2 | COVERED | Both authoritative current verifier regressions are repaired; `RAIL-05 unclassified` remains flagged without a fabricated descriptor. |
| RESEARCH | — | Authenticated durable cache, rollback/deny precedence, crash/restart proof | 215-13 Task 1 | COVERED | Removes the unauthenticated bypass while retaining the existing signed ordering and fault suite. |
| RESEARCH | — | Capability-by-capability report with evidence location/environment/status, never one works boolean | 215-13 Task 2 | COVERED | Proven status requires complete resolving kind-specific artifacts and coherent reason. |
| RESEARCH | — | No invented Crosswake API/package/runtime dependency; missing source/device proof stays blocked | 215-13 Task 2 | COVERED | Report and physical-device record remain byte-unchanged and blocked. |
| API COVERAGE | — | Detector-valid declaration for a phase with no external API integration | 215-13 Task 2 | COVERED | Existing `COVERAGE.md` is normalized, not replaced by an invented matrix. |
| CONTEXT | D-01..D-08 | Authority and decision-table governance | prior plans; 215-13 regression | COVERED | No reimplementation; all deterministic gates rerun. |
| CONTEXT | D-09 | Host-owned tracer, no runtime Crosswake dependency or invented API | 215-13 Tasks 1-2 | COVERED | Swift reference target only; no package or coupling. |
| CONTEXT | D-10 | Authenticated durable state and verified atomic replacement | 215-13 Task 1 | COVERED | Every production write requires key/disposition/revision. |
| CONTEXT | D-11 | Denial, rollback, fault/restart, compile-unit, and physical-device evidence | 215-13 Tasks 1-2 | COVERED | Denial survives keyless attempt; proven validator requires all evidence lanes. |
| CONTEXT | D-12 | Missing bridge/device proof blocks coupling as feasibility_blocked | 215-13 Task 2 | COVERED | Placeholder/pending/non-resolving artifacts cannot prove; current report stays blocked. |
| CONTEXT | D-13 | Only verified server-issued replacement; lifecycle/reachability non-authoritative | 215-13 Task 1 | COVERED | The raw bypass is removed; shared ordering remains. |
| CONTEXT | D-14..D-17 | Typed processor-free source boundary | prior plans; 215-13 regression | COVERED | Source/leakage gate reruns without contract edits. |
| REVIEW | CR-01 | Public unauthenticated cache overwrites verified denial | 215-13 Task 1 | COVERED | Remove production seam and add exact-envelope separate-process regression. |
| REVIEW | CR-02 | Placeholder evidence can assert false proven feasibility | 215-13 Task 2 | COVERED | Closed path/artifact/reason validation plus mutations. |
| VERIFICATION | Gap 1 | Raw/no-key overwrite of authenticated signed denial | 215-13 Task 1 | COVERED | Directly implements every `missing` item. |
| VERIFICATION | Gap 2 | All-proven report with arbitrary/unavailable evidence | 215-13 Task 2 | COVERED | Directly implements every `missing` item. |

All current `215-VERIFICATION.md` gaps are covered with no deferral. All eight spec-less edge-probe outputs are surfaced: `RAIL-05 unclassified` is applicable and flagged; `RSCH-01 unclassified` and `RSCH-02 unclassified` remain explicit flagged assumptions; `RSCH-03 empty`/`encoding` and `RAIL-04 adjacency`/`empty`/`ordering` are explicitly explained as non-applicable noise because their contracts are untouched and their existing deterministic gates rerun. Prohibition recall keeps three bespoke RAIL-05 safety/transparency constraints descriptor-less (`verification: null`), unresolved, and flagged; none is auto-dismissed. There are no deferred CONTEXT ideas, assumption-delta changes, ORM/schema matches, UI work, external API integration changes, or package installs.
</source_audit>

<verification>
Run the denial-preservation tracer first, then authenticated atomicity/concurrency/recovery tests, focused false-proven mutations, the complete Swift package, authority/watchlist and source-matrix CI gates, deterministic decision export, scoped Elixir decision/property/vector tests, and byte-drift checks. Inspect the public Swift surface to confirm there is no no-key/raw writer or max-revision compatibility exception. Keep `capability-report.json` and `physical-device-evidence.md` unchanged at feasibility_blocked/pending; closure means the validator is honest, not that Crosswake feasibility has been proved.
</verification>

<success_criteria>
Both verifier regressions are closed: unauthenticated callers cannot overwrite a signed denial at the canonical path, and fabricated all-proven reports cannot pass with unavailable, pending, non-resolving, path-escaping, wrong-kind, or reason-inconsistent evidence. All already-green Phase-215 requirement gates remain green, and mobile runtime coupling remains explicitly blocked until real Crosswake and dated device evidence exists.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-13-SUMMARY.md` when done.
</output>

---
phase: 215-research-contracts-and-crosswake-feasibility
plan: 15
type: execute
wave: 13
depends_on:
  - 215-14
files_modified:
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
autonomous: true
gap_closure: true
requirements:
  - RAIL-05

must_haves:
  truths:
    - "RAIL-05 / D-12: a caller-selected report directory cannot lift the Crosswake runtime-coupling block, even when it contains a complete proven report and nonempty evidence files."
    - "RAIL-05 / D-09 through D-11: CheckedInCapabilityReportValidator can return proven only while validating the canonical checked-in capability-report.json and its evidence root."
    - "The canonical checked-in report remains feasibility_blocked until every required bridge and physical-device lane has genuine checked-in evidence."
  artifacts:
    - path: examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
      provides: Canonical report-identity guard at the sole proven-producing validator boundary
    - path: examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
      provides: Complete attacker-controlled temporary report/evidence-tree regression
  key_links:
    - from: CheckedInCapabilityReportValidator.validate(reportURL:)
      to: examples/crosswake_tracer/capability-report.json
      via: standardized equality with a validator-owned canonical report URL before report bytes or evidence are trusted
    - from: proven capability rows
      to: validProvenEvidence(_:reportURL:)
      via: the already-authenticated canonical report root, followed by existing containment, kind, file, and physical-device checks
---

<objective>
Close the remaining RAIL-05 provenance/root-substitution gap by binding the sole public proven-producing validator to the canonical checked-in Crosswake report before it evaluates report content or evidence.

Purpose: A fully shaped attacker-controlled temporary directory must not be able to impersonate the checked-in feasibility artifact and authorize unsupported mobile runtime coupling.
Output: A canonical-root guard in `CheckedInCapabilityReportValidator` and a focused regression that constructs a complete synthetic proven report/evidence tree under a temporary root and proves rejection.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-RESEARCH.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-14-SUMMARY.md
@examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
@examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift
@examples/crosswake_tracer/capability-report.json

<interfaces>
- `CheckedInCapabilityReportValidator.validate(reportURL:) throws -> FeasibilityStatus` is the sole public operation that can currently return `.proven`.
- `CheckedInCapabilityReportValidator.validate(_:reportURL:)` is the internal mutation-test seam and must inherit the same canonical report identity requirement.
- `validProvenEvidence(_:reportURL:)` resolves evidence relative to the accepted report URL and already enforces containment, regular nonempty files, evidence-kind path conventions, and completed physical-device evidence.
- `Capability.allRequired` and each capability's `requiredEvidenceKinds` are the authoritative inputs for constructing the complete hostile fixture.
</interfaces>
</context>

## Artifacts this phase produces

- Modified production symbol: `CheckedInCapabilityReportValidator`, with one private canonical report URL/identity helper and a fail-closed guard shared by both validator entry points.
- Modified test symbol: `CapabilityReportTests`, with one regression and private fixture helpers that create a complete synthetic proven report plus native, Crosswake-bridge, simulator, and physical-device evidence under a temporary root.
- New production files, public symbols, dependencies, package installs, external integrations, manifests, environment variables, migrations, UI artifacts, and runtime coupling: None.
- Unchanged checked-in evidence artifacts: `examples/crosswake_tracer/capability-report.json` and `examples/crosswake_tracer/physical-device-evidence.md`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject a complete proven report rooted outside the checked-in tracer</name>
  <reversibility rating="reversible">The identity guard and regression are confined to the reference Swift validator and its tests; changing the canonical-location mechanism later requires no persisted-data migration or published wire-format change.</reversibility>
  <files>examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift</files>
  <read_first>
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-VERIFICATION.md (authoritative remaining gap: arbitrary public report root can manufacture proven)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-CONTEXT.md (D-09 through D-12 host-owned tracer, merge-blocking proof, and fail-closed runtime-coupling contract)
    - .planning/phases/215-research-contracts-and-crosswake-feasibility/215-14-SUMMARY.md (data-only false-proof closure and sole proven-producing boundary)
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift (`CheckedInCapabilityReportValidator.validate(reportURL:)`, internal `validate(_:reportURL:)`, `validProvenEvidence`, and `physicalDeviceEvidenceIsComplete`)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/CapabilityReportTests.swift (checked-in URL helper, false-proof mutations, and current missing arbitrary-root case)
    - examples/crosswake_tracer/capability-report.json (canonical report location and exact report schema)
  </read_first>
  <behavior>
    - Test 1: a temporary package-shaped tree containing all required capability rows marked proven, exact required evidence kinds, valid relative-path classes, nonempty files, and syntactically complete physical-device evidence throws `CheckedInCapabilityReportValidator.ValidationError.invalid` when passed to the public validator.
    - Test 2: the hostile fixture is complete enough to reach proven under the old caller-root behavior, so the regression fails before the production guard is added rather than passing because an unrelated shape check rejects it.
    - Test 3: the canonical checked-in report still validates as `.feasibilityBlocked` while unavailable bridge and physical-device evidence remain unresolved.
    - Test 4: existing duplicate, mixed-state, terminal-reason, data-only, unsupported-schema, missing-lane, and ordering regressions remain green.
  </behavior>
  <action>Begin RED by adding a focused test that creates a unique temporary directory, writes a `capability-report.json` with schema `1.0`, every `Capability.allRequired` row, uniform proven statuses, a completed-proof terminal reason, and one evidence entry for every `requiredEvidenceKind`. Map evidence kinds to nonempty files at paths that satisfy the existing validator conventions: Swift source beneath `Sources/AccrueOfflineClient/`, Swift bridge evidence beneath `Evidence/CrosswakeBridge/`, Markdown simulator evidence beneath `Evidence/Simulator/`, and `physical-device-evidence.md` containing a concrete date plus completed redaction-attestation and reviewer-approval text without pending markers. Assert the public `validate(reportURL:)` call throws `ValidationError.invalid`, and use automatic temporary-directory cleanup so the regression leaves no artifacts. Run the focused test before implementation and confirm this new assertion fails by returning `.proven`; if it fails on fixture shape instead, correct the fixture until it demonstrates the verified exploit path.

Then implement the smallest canonical identity guard in `CheckedInCapabilityReportValidator` per D-09/D-11/D-12. Derive a private canonical URL for the package-root `capability-report.json` from the validator source file's compile-time `#filePath` (three parent-directory steps from `Sources/AccrueOfflineClient/AccrueOfflineClient.swift`), standardize both URLs, and reject any supplied report URL that is not exactly that canonical URL before reading or decoding caller-selected report bytes. Apply the guard in the shared internal validation seam as well as the public file-loading entry so test-only mutation calls cannot accidentally define a second authority. After the identity check, retain the existing schema, capability-set, evidence-kind, containment, nonempty-file, terminal-reason, and physical-device checks unchanged; the canonical location is an additional provenance condition, not a replacement for evidence validation. Keep `CapabilityReport` data-only construction fail-closed per Plan 215-14. Do not change the checked-in report/evidence files, introduce Crosswake APIs or dependencies, accept a configurable root, or weaken blocked-report validation.</action>
  <verify>
    <automated>cd examples/crosswake_tracer &amp;&amp; swift test --filter CapabilityReportTests &amp;&amp; swift test &amp;&amp; cd ../.. &amp;&amp; bash scripts/ci/verify_v159_authority.sh &amp;&amp; bash scripts/ci/verify_entitlement_source_matrix.sh &amp;&amp; cd accrue &amp;&amp; mix accrue.entitlements.decision_cases --check &amp;&amp; cd .. &amp;&amp; git diff --exit-code -- examples/crosswake_tracer/capability-report.json examples/crosswake_tracer/physical-device-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    - The complete attacker-controlled temporary report/evidence tree is rejected by the public validator with `ValidationError.invalid`.
    - `validate(reportURL:)` cannot return `.proven` for any standardized URL other than the canonical checked-in package-root report URL.
    - The internal data mutation seam cannot bypass the canonical report identity guard.
    - Canonical proven evaluation still requires all existing contained, kind-specific, nonempty, terminal-reason, and physical-device evidence checks.
    - The checked-in report remains byte-unchanged and validates as `.feasibilityBlocked`.
    - The focused and full Swift suites plus authority, source-matrix, and deterministic decision-export gates pass.
  </acceptance_criteria>
  <done>Caller-selected roots can no longer manufacture Crosswake feasibility; the validator-owned canonical checked-in report is the only artifact eligible for proven-producing evaluation, and the repository's unresolved evidence keeps it honestly blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller URL → checked-in validator | The URL and every file under its parent are caller-controlled until the report URL is bound to the validator-owned canonical checked-in artifact. |
| canonical report → evidence files | Even under the accepted root, report fields and evidence bytes remain untrusted until the existing schema, lane, containment, kind, file, and device-evidence checks pass. |
| feasibility decision → later runtime coupling | A false `.proven` result could authorize mobile assumptions explicitly prohibited by D-12. |

## STRIDE Threat Register — ASVS L1

**security_block_on: high.** Every critical/high provenance bypass or alternate-root proof is merge-blocking.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-215-68 | Spoofing | `validate(reportURL:)` report identity | critical | mitigate | Compare the standardized caller URL with a validator-owned canonical checked-in report URL before reading or decoding it; reject every alternate root. |
| T-215-69 | Tampering | caller-populated temporary report and evidence tree | high | mitigate | Add a complete exploit-shaped regression whose paths, file kinds, and physical-device text satisfy all legacy content checks but whose noncanonical root is rejected. |
| T-215-70 | Elevation of Privilege | `.proven` runtime-coupling decision | high | mitigate | Require canonical report identity and all existing evidence checks before `.proven` can cross into later-phase feasibility assumptions. |
| T-215-71 | Repudiation | checked-in evidence provenance | medium | mitigate | Preserve the single canonical report path plus deterministic focused/full-suite evidence in the plan summary. |
| T-215-72 | Denial of Service | absent canonical source-tree artifact in a redistributed binary | low | accept | This is a checked-in feasibility tracer, not an Accrue runtime dependency; inability to locate the canonical artifact fails closed with `ValidationError.invalid`. |

## ASVS L1 Control Mapping

| ASVS control | Applicability | Executable evidence / fail-closed gate |
|---|---|---|
| V4 Access Control | Applicable | Only the canonical checked-in artifact may reach proven-producing evaluation. |
| V5 Input Validation | Applicable | The complete alternate-root fixture is rejected before its valid-looking content can establish authority. |
| V8 Data Protection | Applicable | Existing root containment remains in force after canonical root authentication. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Evidence-backed Crosswake feasibility before runtime coupling | 215-15 | COVERED | Canonical report identity closes the last hollow provenance link. |
| REQ | RAIL-05 | Checked-in tracer proves or explicitly blocks every required client capability | 215-15 Task 1 | COVERED | A complete synthetic alternate root cannot manufacture proven. |
| RESEARCH | — | Capability-by-capability checked-in proof or explicit feasibility_blocked, with no invented Crosswake boundary | 215-15 Task 1 | COVERED | The change authenticates the existing tracer artifact and adds no runtime dependency or bridge API. |
| CONTEXT | D-09 | Checked-in host-owned tracer, no Accrue runtime dependency | 215-15 Task 1 | COVERED | Validation is bound to the checked-in package artifact. |
| CONTEXT | D-10 | Required lanes and authenticated evidence do not self-grant | 215-15 Task 1 | COVERED | Existing complete lane/kind checks remain after the identity guard. |
| CONTEXT | D-11 | Merge-blocking tests and dated physical-device proof | 215-15 Task 1 | COVERED | Exploit-shaped regression is merge-blocking; physical-device completeness remains required. |
| CONTEXT | D-12 | Missing proof reports feasibility_blocked rather than partial proof | 215-15 Task 1 | COVERED | Alternate roots cannot lift the block; canonical unresolved report stays blocked. |
| VERIFICATION | remaining gap | Caller-selected report parent is trusted as evidence root | 215-15 Task 1 | COVERED | Canonical URL guard plus a complete temporary-root regression targets the root cause. |

RSCH-01, RSCH-02, RSCH-03, RAIL-04, CONTEXT D-01 through D-08, and CONTEXT D-13 through D-17 are already satisfied and are outside this gap-closure plan; their fast gates rerun only as regression protection. No deferred idea is included. No source-audit item relevant to the authoritative remaining gap is missing.
</source_audit>

<verification>
Run the focused attacker-root regression first, then the complete Swift package. Re-run the authority/watchlist, entitlement-source matrix/leakage, and deterministic decision-export gates to confirm the narrow provenance repair does not regress the other four already-satisfied Phase-215 requirements. Confirm both checked-in evidence artifacts are byte-unchanged.
</verification>

<success_criteria>
RAIL-05 is no longer falsified by root substitution: a fully populated temporary proof tree cannot return `.proven`, only the canonical checked-in report may enter proven-producing validation, every existing evidence check remains active, the checked-in report remains honestly feasibility-blocked, and all Phase-215 regression gates pass.
</success_criteria>

<output>
Create `.planning/phases/215-research-contracts-and-crosswake-feasibility/215-15-SUMMARY.md` when done.
</output>

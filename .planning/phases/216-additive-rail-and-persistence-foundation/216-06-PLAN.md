---
phase: 216-additive-rail-and-persistence-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 216-05
files_modified:
  - accrue/lib/accrue/entitlements/observation.ex
  - accrue/lib/accrue/entitlements/grant.ex
  - accrue/priv/repo/migrations/20260802200000_bound_accrue_entitlement_provider_identity.exs
  - accrue/test/accrue/entitlements/persistence_test.exs
  - accrue/test/mix/tasks/accrue_install_test.exs
autonomous: true
gap_closure: true
requirements:
  - RAIL-03
must_haves:
  truths:
    - "Provider event identity remains globally unique within `{rail, environment}` and transaction fallback identity remains globally unique within `{rail, environment, provider_transaction_id, kind}` per D-10; a collision from another account returns an opaque ownership error and never that account's row."
    - "Repeated or concurrent ingestion for the same account and provider identity returns one durable observation, while competing accounts cannot both claim or receive the same provider identity."
    - "Observation provider event, transaction, lineage, and product identifiers accept at most 255 bytes and observation kind accepts at most 64 bytes; grant provider lineage and product identifiers use the same 255-byte provenance ceiling per D-07/D-09/D-12."
    - "Ecto byte-count validation and named PostgreSQL `octet_length` constraints reject identical over-limit values, including direct SQL writes that bypass changesets."
    - "Rollback-isolated direct-write tests prove PostgreSQL rejects representative invalid account, observation, grant, and device rows while valid zero-valued ordering/revision metadata and retained grant/device history persist."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/observation.ex"
      provides: "Account-safe global idempotency resolution and documented byte-bounded observation provenance"
      exports: ["ingest_changeset/1", "insert_idempotently/2"]
    - path: "accrue/lib/accrue/entitlements/grant.ex"
      provides: "Documented byte-bounded grant lineage and product provenance"
      exports: ["changeset/2"]
    - path: "accrue/priv/repo/migrations/20260802200000_bound_accrue_entitlement_provider_identity.exs"
      provides: "Named PostgreSQL byte constraints for observation and grant provider fields"
    - path: "accrue/test/accrue/entitlements/persistence_test.exs"
      provides: "Cross-account collision, byte-bound, catalog, direct-write, and valid-boundary PostgreSQL proof"
    - path: "accrue/test/mix/tasks/accrue_install_test.exs"
      provides: "Generated-host propagation proof for the provider-identity hardening migration"
  key_links:
    - from: "accrue/lib/accrue/entitlements/observation.ex"
      to: "accrue_entitlement_observations_provider_event_identity_index and accrue_entitlement_observations_transaction_kind_identity_index"
      via: "Conflict targets remain global per D-10; the fetched durable row is compared to the requested account before success is returned"
      pattern: "conflict_target|fetch_by_identity|account_id"
    - from: "Observation.ingest_changeset/1 and Grant.changeset/2"
      to: "20260802200000_bound_accrue_entitlement_provider_identity.exs"
      via: "The same 255/64 byte constants are represented by `validate_length(..., count: :bytes)` and named `octet_length` checks"
      pattern: "validate_length|octet_length"
    - from: "accrue/test/accrue/entitlements/persistence_test.exs"
      to: "PostgreSQL constraint catalog and direct-write boundary"
      via: "Sandbox-owned savepoints isolate each expected PostgreSQL error and allow valid boundary/history assertions to continue"
      pattern: "Postgrex.Error|pg_constraint|transaction"
  assumptions:
    - requirement_id: RAIL-03
      statement: "Adjacency backstop: values exactly 255 bytes for provider event/transaction/lineage/product identifiers and exactly 64 bytes for kind are accepted; one additional byte is rejected by both application and database boundaries."
      status: backstop
      flagged: true
    - requirement_id: RAIL-03
      statement: "Empty/null backstop: either optional observation identity may be null after blank normalization, but at least one must remain; kind, lineage, and product identities remain required and blank input remains invalid."
      status: backstop
      flagged: true
    - requirement_id: RAIL-03
      statement: "Equal-order backstop: provider_order and account revision are monotonic metadata, not identity keys; equal zero-valued metadata on distinct qualified records remains separate and no output ordering API is introduced."
      status: backstop
      flagged: true
    - requirement_id: RAIL-03
      statement: "Idempotency backstop: repeating the same qualified provider identity for the same account returns the original durable observation; changing only account ownership returns an opaque error and creates no second row."
      status: backstop
      flagged: true
    - requirement_id: RAIL-03
      statement: "Concurrency backstop: PostgreSQL global unique indexes select one provider-identity winner; same-account contenders converge on it and cross-account contenders receive an ownership collision without learning the winning account or row."
      status: backstop
      flagged: true
  prohibitions: []
---

<objective>
Close the remaining Phase 216 RAIL-03 verification gaps with an account-safe global observation identity boundary, explicit provider-provenance byte limits, and executable PostgreSQL bypass proofs per D-07, D-09, D-10, D-11, and D-12.

Purpose: Prevent a provider identity collision from crossing entitlement-account ownership, bound every retained provider identifier at both write boundaries, and prove the already-declared database constraints govern direct writes before Phase 217 consumes the persistence substrate.
Output: One forward-only hardening migration, aligned observation/grant changesets, generated-host propagation coverage, and real PostgreSQL regressions for collisions, byte limits, invalid bypass writes, zero metadata, and retained history.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-RESEARCH.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-VERIFICATION.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-03-SUMMARY.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-05-SUMMARY.md
@accrue/lib/accrue/entitlements/observation.ex
@accrue/lib/accrue/entitlements/grant.ex
@accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs
@accrue/priv/repo/migrations/20260802180000_harden_accrue_entitlement_persistence.exs
@accrue/test/accrue/entitlements/persistence_test.exs
</context>

## Artifacts this phase produces

- Identity behavior: global provider-event identity `{rail, environment, provider_event_id}` and global fallback identity `{rail, environment, provider_transaction_id, kind}` remain the D-10 concurrency authority; `Observation.insert_idempotently/2` returns success only when the durable row's `account_id` equals the requested account.
- Observation byte-limit symbols: `@provider_event_id_max_bytes 255`, `@provider_transaction_id_max_bytes 255`, `@kind_max_bytes 64`, `@provider_lineage_id_max_bytes 255`, and `@provider_product_id_max_bytes 255`; private shared byte-validation wiring and opaque cross-account ownership-error construction.
- Grant byte-limit symbols: `@provider_lineage_id_max_bytes 255` and `@provider_product_id_max_bytes 255`, consumed by `Grant.changeset/2` with byte counting.
- New migration file: `accrue/priv/repo/migrations/20260802200000_bound_accrue_entitlement_provider_identity.exs`, using `Accrue.Migration.qualified_table/1` and reversible `up/0`/`down/0` SQL without rewriting persisted rows.
- New observation constraints: `accrue_ent_obs_provider_event_id_bytes_check`, `accrue_ent_obs_provider_transaction_id_bytes_check`, `accrue_ent_obs_kind_bytes_check`, `accrue_ent_obs_provider_lineage_id_bytes_check`, and `accrue_ent_obs_provider_product_id_bytes_check`.
- New grant constraints: `accrue_ent_grants_provider_lineage_id_bytes_check` and `accrue_ent_grants_provider_product_id_bytes_check`.
- New persistence regressions: event and transaction-plus-kind cross-account collision cases; same-account repeat/concurrency convergence; exact-limit/one-byte-over application and direct-write cases; PostgreSQL catalog inventory; rollback-isolated invalid account/observation/grant/device writes; valid zero metadata and history cases.
- Extended installer regression requiring `20260802200000_bound_accrue_entitlement_provider_identity.exs` and one stable byte-constraint name in the generated host.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one globally idempotent event without crossing account ownership</name>
  <reversibility rating="costly">The byte ceilings and ownership-collision behavior become the durable provider-ingest contract used by Phases 217-218, but the migration has an explicit down path and changes no stored identity values.</reversibility>
  <files>accrue/lib/accrue/entitlements/observation.ex, accrue/priv/repo/migrations/20260802200000_bound_accrue_entitlement_provider_identity.exs, accrue/test/accrue/entitlements/persistence_test.exs</files>
  <read_first>
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-VERIFICATION.md (authoritative cross-account collision and byte-bound gaps)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md (D-07, D-10, D-11, D-12)
    - accrue/lib/accrue/entitlements/observation.ex (`insert_idempotently/2`, `conflict_target/1`, `fetch_by_identity!/2`, current constraint mappings)
    - accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs (global partial observation indexes and provider columns)
    - accrue/priv/repo/migrations/20260802180000_harden_accrue_entitlement_persistence.exs (forward hardening and reversible prefix-qualified SQL pattern)
    - accrue/lib/accrue/migration.ex (`qualified_table/1` prefix contract)
    - accrue/test/accrue/entitlements/persistence_test.exs (RepoCase fixtures and observation idempotency tests)
    - accrue/test/support/repo_case.ex (sandbox owner and rollback isolation)
    - accrue/test/test_helper.exs (`Ecto.Migrator.run/4` schema path)
  </read_first>
  <behavior>
    - Test 1: ingesting the same event identity twice for account A returns the same row and leaves exactly one observation.
    - Test 2: ingesting account A's event identity for account B returns `{:error, %Ecto.Changeset{}}` with an opaque `account_id` ownership error, exposes neither account A's ID nor its observation row, and leaves exactly one observation.
    - Test 3: two concurrent same-account event ingests converge on one row; concurrent different-account contenders produce one success and one opaque error regardless of winner.
    - Test 4: observation event, transaction, lineage, and product identifiers at 255 bytes plus kind at 64 bytes validate; one-byte-over values fail with byte-oriented errors, including multibyte input whose grapheme count is below the byte ceiling.
    - Test 5: the new migration creates all five named observation `octet_length` constraints under the configured billing prefix and rolls them back in reverse order.
  </behavior>
  <action>Start with failing RepoCase tests for same-account idempotency, cross-account event collision, and byte adjacency. Preserve the global partial unique index and conflict target from D-10: provider event identity is scoped only by rail/environment, and transaction fallback identity only by rail/environment/transaction/kind. After PostgreSQL resolves the insert, fetch the one global durable row by that same identity and compare its `account_id` to the normalized requested `account_id`; return the row only on equality, otherwise return an invalid changeset with a generic ownership-collision error on `account_id` that contains no durable row or owner identifier. Keep the operation race-safe when either contender wins. Document the byte ceilings in the observation module and apply `validate_length/3` with `count: :bytes`: 255 for provider event, transaction, lineage, and product IDs; 64 for kind. Create the new forward migration per D-11, using `Accrue.Migration.qualified_table/1`, named `octet_length` checks, and an exact reverse `down/0`; do not edit executed migrations, account-scope the provider unique indexes, disclose the winning account, rewrite data, or add projection/provider runtime behavior.</action>
  <acceptance_criteria>
    - Same-account event repeats and contenders return one durable row; a second account receives only an opaque error and never the first account's row.
    - The existing D-10 global index keys and conflict fragments remain globally rail/environment-qualified rather than account-qualified.
    - Exact byte limits pass and one-byte-over values fail in the changeset for every observation provider field named by the gap.
    - The new prefix-qualified migration contains five stable observation constraint names, runs through the test Repo migration path, and has a complete reverse-order down path.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/persistence_test.exs --only observation</automated>
  </verify>
  <done>Global event idempotency is account-safe under repeats and races, and observation provider provenance is explicitly byte-bounded at both Ecto and PostgreSQL boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete fallback, grant, direct-write, and blocking schema proofs</name>
  <reversibility rating="costly">The remaining named checks tighten the downstream persistence contract, while the dedicated migration can remove them without transforming valid rows.</reversibility>
  <files>accrue/lib/accrue/entitlements/observation.ex, accrue/lib/accrue/entitlements/grant.ex, accrue/priv/repo/migrations/20260802200000_bound_accrue_entitlement_provider_identity.exs, accrue/test/accrue/entitlements/persistence_test.exs, accrue/test/mix/tasks/accrue_install_test.exs</files>
  <read_first>
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-VERIFICATION.md (transaction-plus-kind collision, over-limit direct writes, and behavior-unverified direct constraint matrix)
    - .planning/phases/216-additive-rail-and-persistence-foundation/216-CONTEXT.md (D-07, D-09, D-10, D-11, D-12, D-17)
    - accrue/lib/accrue/entitlements/observation.ex (Task 1 ownership and byte-bound implementation)
    - accrue/lib/accrue/entitlements/grant.ex (provider lineage/product changeset and named constraint pipeline)
    - accrue/priv/repo/migrations/20260802200000_bound_accrue_entitlement_provider_identity.exs (Task 1 observation constraints)
    - accrue/priv/repo/migrations/20260802180000_harden_accrue_entitlement_persistence.exs (existing account/observation/grant/device CHECK names)
    - accrue/test/accrue/entitlements/persistence_test.exs (Task 1 tests plus account/grant/device fixture helpers)
    - accrue/test/accrue/entitlements/fake_fixture_test.exs (valid retained history scenarios)
    - accrue/test/mix/tasks/accrue_install_test.exs (wildcard migration-copy assertions)
    - accrue/test/support/install_fixture.ex (generated-host file assertion helpers)
    - accrue/test/test_helper.exs (`Ecto.Migrator.run/4` real migration bootstrap)
  </read_first>
  <behavior>
    - Test 1: transaction-plus-kind repeats for account A return one row, while account B receives the same opaque ownership error and no second row; same-account and different-account concurrent fallback contenders preserve the same guarantee.
    - Test 2: grant lineage and product IDs accept 255 bytes and reject 256 bytes by changeset and named PostgreSQL constraints; observation direct writes reject each over-limit event, transaction, kind, lineage, and product value by its exact named constraint.
    - Test 3: PostgreSQL catalog inspection finds all seven new byte checks plus the representative existing account, observation, grant, and device constraints.
    - Test 4: separate savepoint-isolated SQL writes reject negative account revision, an invalid observation domain/locator, an invalid grant quantity or provenance bound, and an invalid device lifecycle without aborting later assertions.
    - Test 5: direct valid rows preserve account revision zero, observation provider_order/retry_count zero, grant provider_order/account_revision zero, a superseded grant alongside its replacement, and revoked/superseded device history.
    - Test 6: the installer copies the new migration exactly once and the generated file contains `accrue_ent_obs_provider_event_id_bytes_check`.
  </behavior>
  <action>Extend the Task 1 account-ownership result across the transaction-plus-kind branch without changing its D-10 global identity key. Apply the 255-byte lineage/product limit to `Grant.changeset/2` with byte counting, add matching grant checks to the new migration, and map all seven new named constraints to their owning changeset fields. In `persistence_test.exs`, add exact-limit and one-byte-over cases, including multibyte values, plus PostgreSQL catalog assertions. Implement a helper that runs each expected invalid raw SQL insert inside its own sandbox-compatible nested transaction/savepoint, asserts `%Postgrex.Error{postgres: %{code: :check_violation, constraint: expected_name}}`, and rolls back that savepoint so later cases remain usable. Cover at least one representative existing named constraint for each account, observation, grant, and device table, every new provider byte constraint, and valid zero/history rows. Extend the real wildcard installer test per D-17 to require the new migration and stable constraint content exactly once. After schema edits, run the blocking migration/schema gate below before the broader final verification; do not replace direct SQL with changesets or `insert_all` paths that cast away invalid enum values before PostgreSQL sees them.</action>
  <acceptance_criteria>
    - Event and fallback identity collisions have identical account-safe behavior under sequential and concurrent calls.
    - All seven named byte checks exist in PostgreSQL, have matching changeset mappings, accept their exact ceiling, and reject one additional byte through direct writes.
    - Rollback-isolated SQL tests identify the exact rejecting constraint for representative invalid account, observation, grant, and device rows and continue to valid boundary/history assertions afterward.
    - Valid zero-valued ordering/revision metadata, current uniqueness, superseded grants, and revoked/superseded device history still persist.
    - `MIX_ENV=test mix ecto.migrate --quiet` and the focused persistence/installer suites pass after the schema change; the generated host contains the new migration exactly once.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix test test/accrue/entitlements/persistence_test.exs test/accrue/entitlements/fake_fixture_test.exs test/mix/tasks/accrue_install_test.exs</automated>
  </verify>
  <done>The blocking migration/schema gate proves byte bounds and all representative durable constraints at PostgreSQL, while valid zero metadata and retained history remain usable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| provider-normalized input → `Observation.insert_idempotently/2` | Provider identities and account binding cross into the durable idempotency API. |
| Ecto changeset → PostgreSQL | Provider-controlled strings and ordinary schema validation cross into database-enforced byte/domain constraints. |
| concurrent account claims → global provider identity index | Competing accounts may submit the same provider identity; PostgreSQL selects the identity winner before application ownership confirmation. |
| direct SQL/test fixture → durable tables | Writes that bypass Ecto must still satisfy the complete database contract. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-216-06-01 | Spoofing | observation account ownership | high | mitigate | Retain D-10 global provider identity, compare the durable winner's account to the request, and return an opaque error on mismatch; sequential and concurrent event/fallback regressions prove no foreign row is returned. |
| T-216-06-02 | Information Disclosure | cross-account collision response | high | mitigate | The error exposes neither the durable observation nor either account identifier; tests assert the foreign row is not returned and error text is generic. |
| T-216-06-03 | Tampering | provider provenance fields | high | mitigate | Ecto byte validation and named PostgreSQL `octet_length` checks enforce the same 255/64 ceilings, including raw SQL bypass tests and constraint-name assertions. |
| T-216-06-04 | Denial of Service | indexed provider identities | medium | mitigate | Reject oversized provider-controlled event/transaction IDs before index insertion and again at PostgreSQL; multibyte and direct-write tests cover byte rather than grapheme semantics. |
| T-216-06-05 | Tampering | direct durable writes | high | mitigate | Rollback-isolated SQL tests exercise named account, observation, grant, and device checks while catalog inspection proves the constraints exist in the configured prefix. |
| T-216-06-SC | Tampering | package-manager installs | low | accept | No npm, pip, cargo, or new Mix package installation occurs in this plan. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Concurrent Stripe/Apple evidence on durable rail-qualified records with legacy compatibility | 216-01..216-06 | COVERED | Plans 01-04 provide the rail/catalog/host path; Plans 03, 05, and 06 complete durable safety. |
| REQ | RAIL-01 | Concurrent Stripe/Apple registration with legacy processor alias | 216-01, 216-02, 216-04 | COVERED | Verified before this gap plan; no reimplementation needed. |
| REQ | RAIL-02 | Qualified product mapping without rail/environment collisions | 216-01, 216-02, 216-04 | COVERED | Verified before this gap plan; the prior unclassified fallback remains explicitly flagged in 216-02. |
| REQ | RAIL-03 | Stable accounts, qualified observations/grants/devices, bounded provenance, quarantine, ordering, and transactional uniqueness | 216-01, 216-03, 216-04, 216-05, 216-06 | COVERED | Plan 06 closes the remaining account-collision, byte-bound, and direct-write proof gaps. |
| RESEARCH | — | Additive host-owned Ecto/PostgreSQL persistence with named database constraints as concurrency authority | 216-01, 216-03, 216-05, 216-06 | COVERED | Plan 06 retains global PostgreSQL identity authority and adds executable bypass proof. |
| RESEARCH | — | Observation history is privacy-bounded and separate from gateway webhooks | 216-03, 216-05, 216-06 | COVERED | Plan 06 bounds provider identifiers without adding raw evidence storage. |
| RESEARCH | — | Deterministic Fake-first fixtures and installer migration propagation | 216-04, 216-05, 216-06 | COVERED | Plan 06 extends focused fixtures and the wildcard installer path; no live provider is added. |
| CONTEXT | D-01 | Preserve processor alias | 216-01, 216-02, 216-04 | COVERED | Existing completed plans. |
| CONTEXT | D-02 | Explicit controllable default agreement | 216-01, 216-02 | COVERED | Existing completed plans. |
| CONTEXT | D-03 | Stripe/Apple production vocabulary; Apple processor-free | 216-01, 216-02, 216-04 | COVERED | Existing completed plans. |
| CONTEXT | D-04 | Nested qualified product catalog | 216-01, 216-02, 216-04 | COVERED | Existing completed plans. |
| CONTEXT | D-05 | `price_ids` default alias only | 216-01, 216-02 | COVERED | Existing completed plans. |
| CONTEXT | D-06 | Qualified tuple uniqueness | 216-02, 216-04 | COVERED | Existing completed plans. |
| CONTEXT | D-07 | UUID durable records and database concurrency authority | 216-01, 216-03, 216-05, 216-06 | COVERED | Plan 06 adds direct PostgreSQL proof. |
| CONTEXT | D-08 | Stable owner account and revision zero | 216-01, 216-03, 216-06 | COVERED | Plan 06 preserves and directly proves zero revision. |
| CONTEXT | D-09 | Qualified grants and retained history | 216-03, 216-05, 216-06 | COVERED | Plan 06 bounds grant provenance and re-proves history. |
| CONTEXT | D-10 | Global rail/environment-qualified observation idempotency | 216-03, 216-05, 216-06 | COVERED | Plan 06 keeps the global keys and makes ownership resolution safe. |
| CONTEXT | D-11 | Additive forward-only foundation | 216-01, 216-03, 216-05, 216-06 | COVERED | Plan 06 adds a forward migration and no data rewrite/read cutover. |
| CONTEXT | D-12 | Normalized/redacted bounded provenance; no raw evidence | 216-03, 216-04, 216-05, 216-06 | COVERED | Plan 06 supplies explicit byte bounds. |
| CONTEXT | D-13 | Nullable opaque expiring evidence reference | 216-03, 216-05 | COVERED | Existing completed hardening remains unchanged. |
| CONTEXT | D-14 | Account-scoped device identity | 216-03, 216-04, 216-06 | COVERED | Plan 06 direct-write proof includes device scope/lifecycle. |
| CONTEXT | D-15 | Device revocation/history | 216-03, 216-04, 216-06 | COVERED | Plan 06 re-proves retained valid history. |
| CONTEXT | D-16 | Deterministic multi-rail persistence fixtures | 216-04, 216-05, 216-06 | COVERED | All collision/boundary tests stay local and deterministic. |
| CONTEXT | D-17 | Installer/example/compatibility propagation | 216-02, 216-04, 216-05, 216-06 | COVERED | Plan 06 asserts the new migration in generated hosts. |

<verification>
Blocking schema gate after migration changes: `cd accrue && MIX_ENV=test mix ecto.migrate --quiet && mix test test/accrue/entitlements/persistence_test.exs test/accrue/entitlements/fake_fixture_test.exs test/mix/tasks/accrue_install_test.exs`.

Then run `cd accrue && mix test test/accrue/config_entitlements_test.exs test/accrue/entitlements/persistence_test.exs test/accrue/entitlements/fake_fixture_test.exs test/mix/tasks/accrue_install_test.exs test/accrue/docs/entitlements_guide_test.exs` to prove the gap closure does not regress the previously verified Phase 216 rail, catalog, fixture, installer, or guide contracts.
</verification>

<success_criteria>
- Both event and transaction-plus-kind identities remain globally rail/environment-qualified, same-account duplicates converge, and cross-account collisions never return foreign rows.
- All named provider identifiers have documented application byte ceilings and matching PostgreSQL checks, with exact-limit, over-limit, multibyte, and direct-write coverage.
- PostgreSQL behavior—not source presence—rejects representative invalid account, observation, grant, and device writes by exact constraint name.
- Valid zero ordering/revision metadata and retained grant/device history persist after every expected-error savepoint.
- The migration/schema blocking gate and complete focused Phase 216 regression suite pass.
</success_criteria>

<output>
Create `.planning/phases/216-additive-rail-and-persistence-foundation/216-06-SUMMARY.md` when done.
</output>

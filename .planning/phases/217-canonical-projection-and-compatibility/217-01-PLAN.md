---
phase: 217-canonical-projection-and-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/entitlements/snapshot.ex
  - accrue/lib/accrue/entitlements/projector.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/test/accrue/entitlements/snapshot_test.exs
  - accrue/test/accrue/entitlements/projector_test.exs
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
must_haves:
  truths:
    - "Per D-01/D-02, an authenticated host can read one deterministic, rail-neutral snapshot without provisioning an account, calling a provider, reconciling evidence, or writing state."
    - "Per D-01, live grants deduplicate logical plans, union features, and take the maximum integer quantity for each quota across Stripe and Apple."
    - "Per D-03/D-04, one row-locked transaction is the sole grant/revision writer and advances revision at most once only when the effective authorization signature changes."
    - "Per D-04, retracting one qualified source preserves equivalent live access supplied by another source."
    - "Boundary contract for ACCT-01: effective and expiry/revocation timestamps are tested immediately before, exactly at, and immediately after the cutoff."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/snapshot.ex"
      provides: "Typed deterministic `Accrue.Entitlements.Snapshot` and authorization signature"
      exports: ["from_grants/2", "fetch/2", "authorization_signature/1"]
    - path: "accrue/lib/accrue/entitlements/projector.ex"
      provides: "Serialized qualified-observation projection and material revision result"
      exports: ["project/2"]
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "Pure public snapshot read and explicit account provisioning boundary"
      exports: ["snapshot/2", "provision_account/3"]
    - path: "accrue/test/accrue/entitlements/snapshot_test.exs"
      provides: "ACCT-01 deterministic union/max/boundary proof"
    - path: "accrue/test/accrue/entitlements/projector_test.exs"
      provides: "ACCT-02 row-lock, survivor, no-op revision, and audit proof"
  key_links:
    - from: "accrue/lib/accrue/entitlements/projector.ex"
      to: "accrue/lib/accrue/entitlements/snapshot.ex"
      via: "before/after authorization-signature comparison inside one `Accrue.Repo.transact/1` account lock"
      pattern: "FOR UPDATE|authorization_signature|transact"
    - from: "accrue/lib/accrue/entitlements/snapshot.ex"
      to: "accrue/lib/accrue/entitlements/grant.ex"
      via: "pure fold over current, effective, mapped grants from every rail"
      pattern: "Grant|maximum|max|Enum.sort"
    - from: "accrue/lib/accrue/entitlements.ex"
      to: "accrue/lib/accrue/entitlements/snapshot.ex"
      via: "host-authenticated billable/account lookup with no provider call"
      pattern: "snapshot|Account"
  assumptions: []
  prohibitions:
    - requirement_id: ACCT-01
      statement: "MUST NOT expose email, adopter identity, raw receipts/JWS, provider payloads, Apple account tokens, or unredacted provider correlation in snapshots, audit events, or telemetry."
      status: unverified
      flagged: true
---

<objective>
Prove the production canonical-decision architecture end to end: one qualified observation changes only its source lineage, the projector serializes the write, the snapshot folds every surviving grant, and the public read returns the new revision per D-01 through D-05.

Purpose: Retire the phase's hardest risk first—grant loss or revision churn caused by a non-serialized or provider-shaped projection—and establish the contract every later slice consumes.
Output: Typed snapshot, sole projector, pure public read/provision seams, and real PostgreSQL tracer tests for ACCT-01/ACCT-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md
@.planning/phases/217-canonical-projection-and-compatibility/217-RESEARCH.md
@.planning/phases/217-canonical-projection-and-compatibility/217-PATTERNS.md
@.planning/phases/217-canonical-projection-and-compatibility/217-VALIDATION.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-01-SUMMARY.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-03-SUMMARY.md
@accrue/lib/accrue/entitlements/account.ex
@accrue/lib/accrue/entitlements/grant.ex
@accrue/lib/accrue/entitlements/observation.ex
@accrue/lib/accrue/entitlements/resolver/local_map.ex
@accrue/lib/accrue/events.ex
@accrue/lib/accrue/repo.ex

<interfaces>
- `Account.fetch_or_create(repo, owner_type, owner_id)` returns the stable UUID account at revision zero; snapshot reads must use lookup rather than this writer per D-02.
- `Grant` persists `rail`, `environment`, `provider_lineage_id`, `provider_product_id`, `logical_plan`, `source_item_id`, `quantity`, `provider_order`, `account_revision`, effective/expiry/supersession bounds.
- `Observation.insert_idempotently(repo, attrs)` returns one durable, account-safe qualified observation.
- `Accrue.Repo.transact/1` is the host-Repo transaction boundary; `Accrue.Events.record_multi/3` is the immutable audit seam.
- `LocalMap` demonstrates feature union and quota maximum semantics but remains the disabled-mode authority, not canonical grant storage.
</interfaces>
</context>

## Artifacts this phase produces

- `Accrue.Entitlements.Snapshot` with opaque `account_id`, `revision`, sorted plans/features/sources, maximum quota quantities, normalized effective bounds, and redacted correlation.
- `Snapshot.from_grants/2`, `Snapshot.fetch/2`, and `Snapshot.authorization_signature/1` pure functions.
- `Accrue.Entitlements.Projector.project/2` as the only current-grant and account-revision mutation seam.
- `Accrue.Entitlements.snapshot/2` and `provision_account/3` as separate read/write public boundaries.
- Snapshot and projector ExUnit suites required by Wave 0 validation.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one qualified observation through projection to a survivor-safe account snapshot</name>
  <reversibility rating="costly">The snapshot and material-revision value contracts are consumed by later purchase, compatibility, Apple, and offline phases, but remain additive internal/public modules with no data migration.</reversibility>
  <files>accrue/lib/accrue/entitlements/snapshot.ex, accrue/lib/accrue/entitlements/projector.ex, accrue/lib/accrue/entitlements.ex, accrue/test/accrue/entitlements/snapshot_test.exs, accrue/test/accrue/entitlements/projector_test.exs</files>
  <read_first>
    - .planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md (D-01 through D-05, especially pure reads, sole writer, and material revision)
    - .planning/phases/217-canonical-projection-and-compatibility/217-RESEARCH.md (semantic snapshot signature and serialized projector patterns)
    - .planning/phases/217-canonical-projection-and-compatibility/217-PATTERNS.md (`LocalMap` fold, `Repo.transact`, and `Events.record_multi` analogs)
    - accrue/lib/accrue/entitlements/account.ex (`revision`, `fetch_or_create/3`)
    - accrue/lib/accrue/entitlements/grant.ex (qualified current-row fields and constraints)
    - accrue/lib/accrue/entitlements/observation.ex (`insert_idempotently/2` and provider ordering fields)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (`fold_for_customer/1`, feature union, quota max)
    - accrue/lib/accrue/entitlements.ex (existing fail-closed boolean/scalar facade shapes)
    - accrue/lib/accrue/events.ex (`record_multi/3`)
    - accrue/lib/accrue/repo.ex (`transact/1` and host Repo delegation)
  </read_first>
  <behavior>
    - Test 1: live Stripe Pro plus live Apple Pro produces one `:pro` plan, the union of Pro features, the maximum integer quota, two sorted redacted source summaries, and revision 1.
    - Test 2: expiring/revoking the Stripe lineage supersedes only that lineage and leaves the Apple-supplied plan/features/quantity present.
    - Test 3: a duplicate, stale, out-of-order, quarantined, unmapped, or metadata-only observation leaves grants, revision, and material audit count unchanged.
    - Test 4: one applicable authorization change increments revision exactly once and records one bounded audit event in the same transaction.
    - Test 5: snapshot reads by account or authenticated billable do not insert an account, change revision, invoke a provider, or emit a projection audit event.
    - Test 6: effective/expiry bounds behave deterministically immediately before, exactly at, and immediately after their cutoff.
  </behavior>
  <action>Write the two Wave 0 test files first and prove RED. Implement `Snapshot` per D-01/D-02/D-05 as an enforced rail-neutral value whose public collections are sorted and whose fold reads only current, effective, catalog-mapped grants. Deduplicate logical plans, union configured features, and merge quota quantities with integer `max/2`; include only privacy-safe source summaries and keep incidental row IDs/metadata out of `authorization_signature/1`. Implement `Projector.project/2` per D-03/D-04: enter `Accrue.Repo.transact/1`, lock the account `FOR UPDATE`, classify the qualified observation against only its rail/environment/lineage, supersede/write that lineage, refold all current grants, compare before/after signatures, then bump the account once and append the audit event only for a material difference. Return tagged no-op results for expected duplicate/stale/unmapped/quarantined cases. Add separate `Entitlements.snapshot/2` and `provision_account/3` boundaries; preserve all existing boolean/scalar gate signatures and keep `Billing.EntitlementSummary` outside every query. Do not add a snapshot table, replay provider delivery order, write from the read path, or call any provider.</action>
  <acceptance_criteria>
    - The single tracer runs against the real test Repo from observation through qualified grant mutation, account lock/revision, audit, and public snapshot read.
    - Duplicate logical grants collapse, features union, quantities select the exact integer maximum, and source summaries are deterministic and privacy-bounded per D-01.
    - Retraction is lineage-local and survivor access remains; no-op evidence changes neither revision nor material audit history per D-03/D-04.
    - Snapshot reads are side-effect free and existing `entitled?/2`, `has_active_plan?/2`, `features_for/1`, and `entitlement_quantity/2` return shapes remain unchanged per D-02.
  </acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/snapshot_test.exs test/accrue/entitlements/projector_test.exs &amp;&amp; mix format --check-formatted lib/accrue/entitlements/snapshot.ex lib/accrue/entitlements/projector.ex lib/accrue/entitlements.ex test/accrue/entitlements/snapshot_test.exs test/accrue/entitlements/projector_test.exs</automated>
  </verify>
  <done>A production-quality observation-to-snapshot path proves canonical union/max semantics, source-local retraction, pure reads, and material-only revision under one PostgreSQL account lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated host reference → account lookup | Host identity selects which canonical account may be read or provisioned. |
| qualified observation → projector transaction | Provider-normalized state may alter one lineage's durable authorization. |
| current grants → public snapshot/audit/telemetry | Durable provider facts are reduced into a privacy-safe decision value. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-217-01 | Tampering | `Projector.project/2` | high | mitigate | Task 1 locks the account, scopes mutation to one qualified lineage, uses PostgreSQL constraints, and tests duplicate/stale/concurrent-safe material revision. |
| T-217-02 | Elevation of Privilege | snapshot account lookup | high | mitigate | Task 1 accepts only an account or host-authenticated billable reference, separates provisioning, and tests that reads cannot create or cross accounts. |
| T-217-03 | Information Disclosure | snapshot/audit/telemetry | high | mitigate | Task 1 exposes only opaque IDs and redacted bounded summaries; tests reject raw evidence, identity, token, and payload fields. |
| T-217-04 | Repudiation | material revision | medium | mitigate | Each material change records one bounded immutable audit event in the same transaction; no-op evidence records no false authorization revision. |
| T-217-SC | Tampering | package-manager installs | low | accept | No package installation occurs; the plan uses only locked project dependencies. |
</threat_model>

<verification>
Run the focused tracer command after the task, then `cd accrue && mix test test/accrue/entitlements --exclude live_stripe` at the wave boundary. Record focused and wave runtimes in `217-VALIDATION.md`.
</verification>

<success_criteria>
- A host can read one deterministic rail-neutral snapshot whose live grant fold satisfies ACCT-01.
- Source-local mutation, material-only monotonic revision, and survivor access satisfy ACCT-02.
- The tracer proves every canonical projection layer before any expansion plan executes.
- No external API, new schema, raw evidence, advisory summary, or legacy gate-shape change enters the path.
</success_criteria>

<output>
Create `.planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md` when done.
</output>

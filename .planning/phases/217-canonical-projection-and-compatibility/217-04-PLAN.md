---
phase: 217-canonical-projection-and-compatibility
plan: 04
type: execute
wave: 2
depends_on: [217-01]
files_modified:
  - accrue/lib/accrue/config.ex
  - accrue/lib/accrue/entitlements/compatibility.ex
  - accrue/lib/accrue/entitlements/resolver/canonical.ex
  - accrue/lib/accrue/entitlements/resolver.ex
  - accrue/test/accrue/entitlements/compatibility_test.exs
autonomous: true
requirements: [ACCT-04]
must_haves:
  truths:
    - "Per D-11, omitted configuration and `disabled` preserve LocalMap authority; `shadow` compares but returns LocalMap; `enabled` uses canonical only for approved accounts/cohorts."
    - "Per D-12, backfill is deterministic, chunked, resumable, and idempotent and derives only mapped entitling Stripe subscriptions without mutating gateway or advisory records."
    - "Per D-13, parity compares normalized entitlement meaning and unresolved unmapped/ambiguous mismatches remain stable privacy-safe blockers."
    - "Per D-14, rollback changes only resolver authority and preserves all canonical accounts, observations, grants, revisions, audits, and repair work."
    - "Boundary contract for ACCT-04: disabled/shadow/enabled and included/excluded cohort edges each select exactly one deterministic authority."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/compatibility.ex"
      provides: "Idempotent backfill, normalized parity, blocker, cohort, cutover, and rollback service"
      exports: ["backfill/2", "compare/2", "authority/2", "enable/2", "rollback/1"]
    - path: "accrue/lib/accrue/entitlements/resolver/canonical.ex"
      provides: "Resolver-compatible canonical snapshot projection"
      exports: ["resolve/2"]
    - path: "accrue/lib/accrue/config.ex"
      provides: "Validated disabled/shadow/enabled compatibility configuration"
    - path: "accrue/test/accrue/entitlements/compatibility_test.exs"
      provides: "ACCT-04 legacy, backfill, parity, cutover, rollback, advisory-isolation proof"
  key_links:
    - from: "accrue/lib/accrue/entitlements/compatibility.ex"
      to: "accrue/lib/accrue/entitlements/resolver/local_map.ex"
      via: "normalized parity and disabled/shadow authority"
      pattern: "LocalMap|compare|shadow"
    - from: "accrue/lib/accrue/entitlements/resolver/canonical.ex"
      to: "accrue/lib/accrue/entitlements/snapshot.ex"
      via: "translation into existing `Resolver.resolved()` map shape"
      pattern: "Snapshot|MapSet|quantities"
    - from: "accrue/lib/accrue/config.ex"
      to: "accrue/lib/accrue/entitlements/compatibility.ex"
      via: "validated mode/cohort selection with inert legacy default"
      pattern: "disabled|shadow|enabled|cohort"
  assumptions:
    - requirement_id: ACCT-04
      statement: "Precision probe unresolved: D-13 requires a defined clean shadow window, but no source artifact specifies its duration or sample threshold. Implement a validated explicit host value with no permissive default; verification must flag `insufficient_spec` until the maintainer supplies the deployment-specific value."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: ACCT-04
      statement: "MUST NOT silently fall back between LocalMap and canonical authority within a request, auto-enable an account with parity blockers, or delete canonical evidence during rollback."
      status: unverified
      flagged: true
---

<objective>
Deliver the explicit disabled/shadow/enabled compatibility lane, deterministic backfill/parity blockers, cohort cutover, and evidence-preserving rollback required by D-11 through D-14 and ACCT-04.

Purpose: Let legacy single-processor hosts adopt canonical projection without changing behavior until evidence says a chosen cohort is safe.
Output: Compatibility service, canonical resolver, validated mode/cohort configuration, and complete regression suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md
@.planning/phases/217-canonical-projection-and-compatibility/217-RESEARCH.md
@.planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-02-SUMMARY.md
@.planning/phases/216-additive-rail-and-persistence-foundation/216-04-SUMMARY.md
@accrue/lib/accrue/entitlements/resolver.ex
@accrue/lib/accrue/entitlements/resolver/local_map.ex
@accrue/lib/accrue/config.ex
@accrue/test/accrue/entitlements/resolver_test.exs
</context>

## Artifacts this phase produces

- Validated `:disabled | :shadow | :enabled` compatibility mode with explicit approved-account/host-cohort selector.
- `Accrue.Entitlements.Compatibility` backfill/parity/authority/cutover/rollback APIs and stable blocker reasons.
- `Accrue.Entitlements.Resolver.Canonical` implementing the existing Resolver output contract.
- ACCT-04 integration suite covering legacy configuration, customers, mappings, webhooks, Stripe subscriptions, gates, advisory isolation, resumable backfill, cutover, and rollback.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock deterministic resolver authority and normalized shadow parity</name>
  <reversibility rating="costly">Hosts will rely on three public deployment modes and cohort semantics, but switching mode remains an additive configuration change with preserved evidence.</reversibility>
  <files>accrue/lib/accrue/config.ex, accrue/lib/accrue/entitlements/compatibility.ex, accrue/lib/accrue/entitlements/resolver/canonical.ex, accrue/lib/accrue/entitlements/resolver.ex, accrue/test/accrue/entitlements/compatibility_test.exs</files>
  <read_first>
    - .planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md (D-11, D-13, D-14)
    - .planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md (Snapshot interface)
    - accrue/lib/accrue/config.ex (`entitlements/0`, `plan_resolver/0`, boot validation)
    - accrue/lib/accrue/entitlements/resolver.ex (`resolved` type and `resolve/2` callback)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (`resolve/2`, `fold_for_customer/1`, unmapped handling)
    - accrue/test/accrue/entitlements/resolver_test.exs (application-env restoration and gate parity)
    - accrue/lib/accrue/billing/entitlement_summary.ex (advisory-only boundary, if present)
  </read_first>
  <behavior>
    - Test 1: omitted config and `:disabled` select LocalMap and preserve all existing gate/customer/mapping/webhook/subscription behavior.
    - Test 2: `:shadow` computes canonical normalized meaning and records/returns stable mismatch details while the gate result stays exactly LocalMap's.
    - Test 3: `:enabled` selects canonical only for an approved account/cohort; the immediately excluded account remains LocalMap-authoritative.
    - Test 4: unknown mode, ambiguous cohort result, unresolved unmapped product, projection ambiguity, or missing explicit clean-window value fails validation/enablement rather than falling back.
    - Test 5: advisory Stripe entitlement summaries cannot alter either authority's canonical grant inputs or parity result.
  </behavior>
  <action>Start the Wave 0 compatibility suite. Add inert-default validated config for D-11 modes and a host-supplied cohort predicate/approved account set; omission must retain current Resolver/LocalMap behavior. Implement Canonical as the existing Resolver behaviour by translating Snapshot into the exact `resolved()` map expected by legacy gates. Implement `Compatibility.authority/2` to choose exactly once per request, and normalized parity over plan/features/quantities rather than internal representation. Shadow records/emits bounded blockers but returns LocalMap. Because the D-13 shadow-window duration is unresolved, require an explicit validated value for enablement and surface `insufficient_spec`; do not invent a duration. Keep advisory cache excluded and restore app env after tests.</action>
  <acceptance_criteria>
    - Omitted/disabled/shadow/enabled and cohort boundary cases select exactly the locked authority with no per-request fallback.
    - Legacy public gate shapes and single-processor behavior are byte/term equivalent in omitted/disabled/shadow modes.
    - Enablement rejects every blocker and a missing explicit clean-window value; mismatch reasons contain no raw provider or owner data.
    - Canonical Resolver consumes only Snapshot/current grants and never the advisory summary.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/compatibility_test.exs --exclude backfill</automated></verify>
  <done>Each request has one explicit authority, shadow parity is observable without behavior change, and unsafe/underspecified enablement fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill mapped Stripe truth idempotently and prove evidence-preserving cutover rollback</name>
  <files>accrue/lib/accrue/entitlements/compatibility.ex, accrue/test/accrue/entitlements/compatibility_test.exs</files>
  <read_first>
    - .planning/phases/217-canonical-projection-and-compatibility/217-CONTEXT.md (D-12 through D-14 and D-19)
    - accrue/lib/accrue/entitlements/compatibility.ex (Task 1 authority/parity API)
    - accrue/lib/accrue/entitlements/account.ex (`fetch_or_create/3` idempotent account identity)
    - accrue/lib/accrue/entitlements/grant.ex (qualified current identity)
    - accrue/lib/accrue/billing/subscription.ex (`entitling?/1`, processor provenance)
    - accrue/lib/accrue/entitlements/resolver/local_map.ex (mapped entitling Stripe meaning)
    - accrue/test/support/entitlements/fixtures.ex (deterministic Stripe/Apple fixtures)
  </read_first>
  <behavior>
    - Test 1: backfill creates one account per known billable and only mapped entitling Stripe grants; repeated chunks and resume from the same/next cursor do not duplicate rows or revisions.
    - Test 2: backfill never modifies customer/subscription/provider/advisory rows and never calls a provider.
    - Test 3: a clean explicit shadow window plus zero blockers enables only the approved cohort; any blocker leaves authority unchanged.
    - Test 4: rollback returns authority to LocalMap while canonical accounts/grants/observations/revisions/audits remain byte-equivalent and repair continues.
    - Test 5: concurrent duplicate chunk execution converges on database account/current-grant identities.
  </behavior>
  <action>Implement D-12 deterministic chunked/resumable backfill over stable billable ordering. For each row, provision the durable account and derive only mapped entitling Stripe subscription state into the same qualified current-grant/projector boundary; use database identities for repeats/concurrency and return a cursor/result with bounded counts/reasons. Implement D-13 enablement only after explicit clean-window evidence and zero blockers, and D-14 rollback as authority configuration/state change only. Preserve ongoing canonical repair and all evidence. Do not rewrite subscriptions/customers, call providers, ingest advisory summaries, delete canonical rows, or silently change authority on a failed cutover.</action>
  <acceptance_criteria>
    - Backfill is chunked, resumable, repeat/concurrency-idempotent, and limited to mapped entitling Stripe state.
    - Parity blockers remain visible and prevent enablement; successful enablement affects only an approved cohort.
    - Rollback changes only authority and preserves canonical evidence/revision/audit state exactly.
    - Existing compatibility surfaces and advisory-isolation gates remain green.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/compatibility_test.exs test/accrue/entitlements/resolver_test.exs test/accrue/entitlements/entitlement_sync_isolation_guard_test.exs test/accrue/entitlements/stripe_sync_disabled_isolation_test.exs</automated></verify>
  <done>Hosts have an idempotent parity-checked adoption path and a non-destructive rollback to unchanged legacy authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host configuration/cohort → resolver authority | Configuration determines the authorization system for an account. |
| legacy subscriptions → canonical backfill | Gateway records are translated into grants without provider mutation. |
| parity blockers → enablement | Diagnostic evidence decides whether cutover is allowed. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-217-12 | Tampering | compatibility authority | high | mitigate | Task 1 validates closed modes/cohorts, selects authority once, and tests all boundary/fail-closed cases. |
| T-217-13 | Elevation of Privilege | cutover blockers | high | mitigate | Tasks 1-2 require explicit clean-window evidence and zero unmapped/ambiguous/resource-isolation blockers before cohort enablement. |
| T-217-14 | Information Disclosure | parity/backfill telemetry | medium | mitigate | Stable reason IDs and bounded counts replace raw owner/provider/advisory data. |
| T-217-15 | Tampering | backfill/rollback | high | mitigate | Task 2 uses idempotent database identities, no provider mutations, and exact evidence-preservation assertions on rollback. |
| T-217-SC | Tampering | package-manager installs | low | accept | No package installation occurs. |
</threat_model>

<verification>
Run both task commands, then `cd accrue && mix test test/accrue/entitlements --exclude live_stripe`. The unresolved clean-window precision assumption remains visible until a maintainer supplies the deployment-specific value; implementation must not manufacture one.
</verification>

<success_criteria>
- ACCT-04 legacy parity, idempotent backfill, blocker-gated cohort cutover, and non-destructive rollback are executable.
- Omitted configuration remains unchanged legacy behavior.
- The unresolved shadow-window precision item is explicit and fail-closed, satisfying no-silent-drop equality.
</success_criteria>

<output>
Create `.planning/phases/217-canonical-projection-and-compatibility/217-04-SUMMARY.md` when done.
</output>

---
phase: 218-apple-observation-and-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/priv/repo/migrations/20260803030000_create_accrue_apple_lineages_and_intakes.exs
  - accrue/lib/accrue/entitlements/apple/lineage.ex
  - accrue/lib/accrue/entitlements/apple/intake.ex
  - accrue/lib/accrue/entitlements/projector.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/test/accrue/entitlements/apple_observation_tracer_test.exs
  - accrue/test/accrue/entitlements/projector_test.exs
autonomous: true
requirements: [AAPL-01, AAPL-03]
must_haves:
  truths:
    - "Per D-01/D-02, initial bind, qualified Observation, Projector grant/revision, audit, Projector follow-up, and reconciliation wakeup commit or roll back as one PostgreSQL transaction."
    - "Per D-03/D-07, missing verified token returns `verified_unbound`, a different bound account returns `ownership_conflict`, and neither outcome creates an Observation, Grant, or account revision."
    - "Per D-08/D-15, the tracer stores normalized bounded facts/digests only and the existing Projector remains the sole grant/revision writer."
    - "Flagged assumption AAPL-01 (unclassified): the authenticated host account passed to purchase/restore is already authorized by host policy; Accrue verifies token equality but does not authenticate the end user."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/lineage.ex"
      provides: "Environment-qualified bind-once Apple lineage schema and locked claim"
      exports: ["lock_or_insert/4", "claim/5"]
    - path: "accrue/lib/accrue/entitlements/apple/intake.ex"
      provides: "Closed Apple evidence/outcome values and verified-to-projector admission"
      exports: ["observe/3"]
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "Host-facing Apple purchase context and observation facade"
      exports: ["apple_purchase_context/2", "observe_apple_evidence/3"]
    - path: "accrue/lib/accrue/entitlements/projector.ex"
      provides: "Sole-writer projection callable inside an existing repository transaction"
      exports: ["project/2", "project_in_transaction/3"]
    - path: "accrue/test/accrue/entitlements/apple_observation_tracer_test.exs"
      provides: "Real Repo/Projector/snapshot end-to-end tracer"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/intake.ex"
      to: "accrue/lib/accrue/entitlements/projector.ex"
      via: "fully verified and account-bound Observation only"
      pattern: "Observation.insert_idempotently|Projector.project_in_transaction"
    - from: "accrue/lib/accrue/entitlements/apple/lineage.ex"
      to: "accrue_entitlement_apple_lineages"
      via: "FOR UPDATE plus named unique identity"
      pattern: "FOR UPDATE|original_transaction_id"
  assumptions:
    - requirement_id: AAPL-01
      category: unclassified
      status: unresolved
      statement: "Host authentication and account-membership authorization are supplied before `apple_purchase_context/2` or authenticated observation; Accrue does not authenticate users."
  prohibitions:
    - requirement_id: AAPL-01
      category: privacy
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT reveal the account that already owns an Apple lineage in any conflict result, audit record, telemetry event, exception, or inspected value."
---

<objective>
Prove one production-quality Apple purchase/restore path from a verified account token through serialized lineage ownership, qualified observation, the existing Projector, and a revisioned account snapshot per D-01 through D-03, D-07/D-08, D-15, and D-17.

Purpose: Validate the hardest ownership and authority boundary before expanding verification, notification, and repair behavior.
Output: Apple lineage/intake persistence, closed values, public purchase/observe facade, and a real PostgreSQL tracer.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@.planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md
@accrue/lib/accrue/entitlements/account.ex
@accrue/lib/accrue/entitlements/observation.ex
@accrue/lib/accrue/entitlements/projector.ex
</context>

## Artifacts this phase produces

- Table `accrue_entitlement_apple_lineages` with fields `id`, `environment`, `original_transaction_id`, `account_id`, `binding_state`, `verified_token_digest`, `provider_order_high_water`, `last_reason`, `attempts`, `next_retry_at`, and timestamps.
- Table `accrue_entitlement_apple_intakes` with bounded fields `id`, `environment`, `provider_event_id`, `lineage_id`, `evidence_digest`, `correlation_hash`, `verifier_version`, `config_version`, `disposition`, `reason`, `next_action`, `attempts`, `next_retry_at`, `evidence_ref`, `evidence_expires_at`, and timestamps.
- Named unique indexes `accrue_apple_lineages_environment_original_transaction_index` and `accrue_apple_intakes_environment_provider_event_index` plus named domain/bounds constraints mirrored by changesets.
- `%Accrue.Entitlements.Apple.Intake.VerifiedEvidence{environment, original_transaction_id, app_account_token, provider_event_id, provider_transaction_id, product_id, logical_plan, lifecycle, effective_at, expires_at, signed_at, evidence_digest, verifier_version, config_version}`.
- `%Accrue.Entitlements.Apple.Intake.Outcome{disposition, reason, next_action, snapshot, revision}` with closed dispositions `verified|noop|retryable|quarantined`.
- `Accrue.Entitlements.Projector.project_in_transaction(repo, observation, opts \\ [])`, an `@doc false` composition seam that performs the existing account lock, grant/revision mutation, projection audit, and Projector follow-up insert without opening `Repo.transact`; `project/2` remains the public telemetry wrapper and opens one transaction around this seam.
- `Accrue.Entitlements.apple_purchase_context/2` and `observe_apple_evidence/3`; no CLI flags are added.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace verified Apple purchase evidence to one canonical snapshot</name>
  <precondition>Phase 217 projector and snapshot modules exist and `cd accrue &amp;&amp; mix test test/accrue/entitlements/projector_test.exs` passes.</precondition>
  <reversibility rating="costly">Per D-01, later repair, reconciliation, diagnostics, and ownership policy depend on the lineage identity and bind-once schema.</reversibility>
  <files>accrue/priv/repo/migrations/20260803030000_create_accrue_apple_lineages_and_intakes.exs, accrue/lib/accrue/entitlements/apple/lineage.ex, accrue/lib/accrue/entitlements/apple/intake.ex, accrue/lib/accrue/entitlements/projector.ex, accrue/lib/accrue/entitlements.ex, accrue/test/accrue/entitlements/apple_observation_tracer_test.exs, accrue/test/accrue/entitlements/projector_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-01 through D-03, D-07/D-08, D-15, D-17)
    - .planning/phases/217-canonical-projection-and-compatibility/217-01-SUMMARY.md (actual projector transaction/result contract)
    - accrue/priv/repo/migrations/20260802150000_create_accrue_entitlement_persistence.exs (Accrue.Migration and named-index conventions)
    - accrue/lib/accrue/entitlements/observation.ex (`insert_idempotently/2`, account-required boundary)
    - accrue/lib/accrue/entitlements/projector.ex (`project/2`, sole-writer and lock contract)
    - accrue/lib/accrue/entitlements.ex (public span/private identifier conventions)
  </read_first>
  <behavior>
    - Test 1: `apple_purchase_context(account)` returns the exact opaque account UUID as `app_account_token` and bounded environment/bundle context.
    - Test 2: a Fake-verified purchase whose token equals the authenticated account UUID creates/binds one lineage, inserts one qualified observation, projects one Apple grant, and returns snapshot revision 1.
    - Test 3: repeating the identical evidence returns `noop/duplicate`, leaves one lineage/observation/current grant, and does not advance revision.
    - Test 4: verified evidence without a usable token persists `verified_unbound` and creates no account observation/grant/revision.
    - Test 5: an already-bound lineage observed for another authenticated account persists `ownership_conflict`, never reveals owner identity, and creates no second account observation/grant/revision.
    - Test 6: an injected failure after projection/audit/job insertion returns an error and leaves zero persisted binding, qualified observation, grant/revision, audit event, Projector follow-up job, or reconciliation wakeup; retry then commits exactly one of each intended effect.
  </behavior>
  <action>Write the failing RepoCase tracer first. Add the two bounded Apple tables and exact named constraints. Refactor Projector so `project/2` retains telemetry and owns `Repo.transact`, while new `@doc false project_in_transaction(repo, %Observation{}, opts)` contains the existing account lock, grant/revision mutation, `Events.record`, and Oban follow-up insert and never starts or commits a transaction. Preserve all existing `project/2` results and tests. Implement `Lineage.lock_or_insert/4` and `claim/5` so one outer `Repo.transact` inserts-or-selects and locks `{apple, environment, original_transaction_id}`, rechecks and binds only when the independently verified token equals the authenticated Account UUID per D-01/D-02, persists the intake and qualified Observation, calls `Projector.project_in_transaction/3`, records the bounded ownership/projection audit, and inserts the coalesced reconciliation wakeup before returning. Add a test-only callback option invoked after every write and before return so the tracer can raise and prove rollback; it must not be reachable from the public facade. Expose the thin D-17 facade with `span_private`; keep raw signed input and provider identifiers out of results and metadata. Return `verified_unbound` or `ownership_conflict` per D-03 without heuristic ownership, owner disclosure, or direct Grant writes.</action>
  <acceptance_criteria>
    - The happy path crosses public facade, lineage lock, Observation, Projector, Grant, revision, and Snapshot in one real database test.
    - Duplicate execution is idempotent and conflict/unbound outcomes are durable but non-granting.
    - All queryable fields are bounded normalized facts or digests; raw JWS, receipt, token, notification body, PII, and adopter identity are absent.
    - PostgreSQL uniqueness and row locking, not an Elixir pre-check, decide ownership.
    - Failure after the final in-transaction write proves the lineage binding, intake/Observation, grant/revision, all audit rows, Projector follow-up, and reconciliation wakeup are absent together; no nested `Repo.transact` occurs.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_observation_tracer_test.exs</automated></verify>
  <done>An authenticated host can supply one Fake-verified Apple purchase and receive a canonical snapshot, while duplicate, unbound, and conflicting evidence cannot grant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated host account + verified token -> lineage | Account ownership is claimed exactly once. |
| verified facts -> Observation/Projector | Only bounded, bound facts may become grant authority. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-01 | Elevation of Privilege | lineage claim | high | mitigate | Task 1 requires token/account UUID equality under a database lock and proves races/conflicts non-granting. |
| T-218-02 | Information Disclosure | conflict outcome | high | mitigate | Task 1 returns fixed copy/reason and recursively rejects owner identifiers from results, audit, telemetry, and inspect. |
| T-218-03 | Tampering | projector admission | high | mitigate | Intake accepts only VerifiedEvidence and delegates grant writes exclusively to Projector. |
| T-218-SC | Tampering | package-manager installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
Run the tracer, then `cd accrue &amp;&amp; mix test test/accrue/entitlements/projector_test.exs test/accrue/entitlements/apple_observation_tracer_test.exs` and focused format checks.
</verification>

<success_criteria>
- One verified Apple path reaches a revisioned canonical snapshot end to end.
- Bind-once, duplicate, unbound, conflict, privacy, and sole-writer invariants are executable.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-01-SUMMARY.md` when done.
</output>

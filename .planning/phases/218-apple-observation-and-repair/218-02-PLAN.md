---
phase: 218-apple-observation-and-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [AAPL-02]
must_haves:
  truths:
    - "Per D-05, `app_store_server_library` is not installed until a human verifies the SUS package identity and accepts evaluation behind the private seam."
    - "Approval authorizes evaluation only; Plan 218-03 admits the package only if every hostile-chain, API, supervision, privacy, and dependency gate passes."
  artifacts: []
  key_links:
    - from: "Hex package identity"
      to: "218-03 adapter admission"
      via: "blocking package-legitimacy approval"
      pattern: "app_store_server_library.*2.2"
  assumptions: []
  prohibitions: []
---

<objective>
Close the mandatory human legitimacy gate for the low-adoption `app_store_server_library` candidate before any package installation per D-05.

Purpose: Prevent dependency-confusion or unreviewed package admission while preserving the locked candidate-first evaluation.
Output: An explicit approve/reject signal consumed by Plan 218-03; no repository files change.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
</context>

## Artifacts this phase produces

- Human approval/rejection of the exact Hex package `app_store_server_library` version `2.2.0`, registry page, and source repository for evaluation only.
- No symbol, CLI flag, field, or file path is added by this checkpoint.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the candidate Apple server package identity</name>
  <files>none</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-RESEARCH.md (Package Legitimacy Audit and D-05)
    - https://hex.pm/packages/app_store_server_library (publisher, version, release, repository metadata)
    - https://github.com/yaglo/app-store-server-library-elixir (source ownership and tagged release)
  </read_first>
  <action>Run the automated registry query, then present the exact package name `app_store_server_library`, version `2.2.0`, Hex publisher/owners, declared source URL, tagged source commit, license, and download signal. Ask the human to approve only a private-seam evaluation. A rejection is valid and routes Plan 218-03 directly to the Accrue-owned Finch plus JOSE/`:public_key` fallback; approval does not waive any D-05 adapter-admission test.</action>
  <acceptance_criteria>
    - The human sees registry and source-repository identity together and responds `approved` or `rejected`.
    - No dependency is added before the response.
    - Either response leaves the public Apple verifier interface and all AAPL-02 checks unchanged.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix hex.info app_store_server_library</automated></verify>
  <what-built>Read-only package legitimacy evidence for the exact candidate dependency; no code or dependency change.</what-built>
  <how-to-verify>Compare Hex owner/source metadata with the GitHub repository and `v2.2.0` tag. Type `approved` to permit gated evaluation, or `rejected` to force the existing-dependency fallback.</how-to-verify>
  <resume-signal>Type "approved" or "rejected".</resume-signal>
  <done>The executor has an explicit package-evaluation authorization outcome and has not installed anything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package registry/source metadata -> dependency evaluation | An attacker or typo-squatted package could enter the build. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-SC | Tampering | Hex dependency | high | mitigate | Blocking human verification compares the exact registry package, owners, repository, tag, and license before Plan 218-03 may install it. |
| T-218-04 | Repudiation | package approval | medium | mitigate | The checkpoint response is retained in the plan summary and the adapter admission report records the chosen path. |
</threat_model>

<verification>
Confirm no `mix.exs` or `mix.lock` diff exists at checkpoint completion and record the explicit response.
</verification>

<success_criteria>
- No SUS package is installed without a non-auto-approvable legitimacy decision.
- Rejection cleanly selects the locked fallback without reducing verification scope.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-02-SUMMARY.md` when done.
</output>

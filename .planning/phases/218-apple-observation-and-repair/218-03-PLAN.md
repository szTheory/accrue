---
phase: 218-apple-observation-and-repair
plan: 03
type: execute
wave: 2
depends_on: [218-01, 218-02]
files_modified:
  - accrue/mix.exs
  - accrue/mix.lock
  - accrue/lib/accrue/entitlements/apple/verifier.ex
  - accrue/lib/accrue/entitlements/apple/verifier/production.ex
  - accrue/test/fixtures/apple/server_evidence.exs
  - accrue/test/accrue/entitlements/apple_verifier_test.exs
  - .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md
autonomous: true
requirements: [AAPL-02]
user_setup:
  - service: apple_app_store_server
    why: "Production JWS verification and status/history requests"
    env_vars:
      - name: APPLE_ISSUER_ID
        source: "App Store Connect -> Users and Access -> Integrations -> In-App Purchase"
      - name: APPLE_KEY_ID
        source: "App Store Connect in-app purchase key metadata"
      - name: APPLE_PRIVATE_KEY
        source: "Downloaded App Store Server API private key; runtime secret only"
      - name: APPLE_BUNDLE_ID
        source: "App Store Connect app identifier"
      - name: APPLE_APP_ID
        source: "Numeric production Apple app ID"
      - name: APPLE_ROOT_CERT_PATH
        source: "Pinned Apple root certificate bundle maintained by the host"
must_haves:
  truths:
    - "Per D-05/D-06, outer notification, nested transaction, and nested renewal JWS are each independently verified with ES256, ordered x5c trust, certificate time/purpose, signature, bundle, environment, and production app identity checks."
    - "AAPL-02 idempotency edge is covered: verifying the same signed input twice returns byte-equivalent normalized facts and performs no persistence or provider mutation."
    - "AAPL-02 concurrency edge is covered: parallel verification is stateless, uses immutable config, and returns identical facts/errors without shared mutable verifier state."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/verifier.ex"
      provides: "Private verifier behaviour, immutable config, and closed verification errors"
      exports: ["verify_notification/2", "verify_transaction/2", "verify_renewal/2"]
    - path: "accrue/lib/accrue/entitlements/apple/verifier/production.ex"
      provides: "Strict production adapter selected by deterministic admission evidence"
    - path: ".planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md"
      provides: "Reproducible package/fallback gate evidence"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/verifier/production.ex"
      to: "OTP :public_key and Jason"
      via: "dependency-free fallback selected when candidate admission rejects"
      pattern: "Jason.decode|public_key.pkix_path_validation|public_key.verify"
  assumptions: []
  prohibitions:
    - requirement_id: AAPL-02
      category: privacy
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT retain or emit raw JWS, receipts, app-account tokens, notification bodies, adopter identity, or PII through rows, metadata, telemetry, logs, exceptions, Oban arguments, or inspected values."
---

<objective>
Admit one replaceable strict production verifier only after deterministic evidence per D-05/D-06 and D-19/D-20.

Purpose: Make cryptographic verification non-bypassable while containing provider-library risk behind one reviewable boundary.
Output: Private verifier/adapter, hostile corpus, admission report, and deterministic verification tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@.planning/phases/218-apple-observation-and-repair/218-01-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-02-SUMMARY.md
@accrue/lib/accrue/webhook/caching_body_reader.ex
@accrue/lib/accrue/entitlements/apple/intake.ex
</context>

## Artifacts this phase produces

- Behaviour `Accrue.Entitlements.Apple.Verifier` and struct `%Verifier.Config{roots, bundle_id, environment, app_apple_id, verifier_version, config_version}`.
- Closed verification reasons `invalid_payload|invalid_algorithm|invalid_header|invalid_chain|invalid_certificate_purpose|invalid_certificate_time|invalid_signature|wrong_bundle|wrong_environment|wrong_app|unsupported_family`.
- `Accrue.Entitlements.Apple.Verifier.Production` backed either by admitted `app_store_server_library ~> 2.2` or the dependency-free `Jason` plus OTP `:public_key` fallback, hidden behind the same behaviour.
- Fixture file `accrue/test/fixtures/apple/server_evidence.exs` and admission report `218-ADAPTER-ADMISSION.md`.

<tasks>

<task type="auto">
  <name>Task 1: Decide adapter admission against the hostile verifier contract</name>
  <precondition>Plan 218-02 summary records `approved` or `rejected` for package evaluation.</precondition>
  <reversibility rating="reversible">Provider-library choice is hidden behind the verifier behaviour and can be swapped without changing public outcomes or persisted facts.</reversibility>
  <files>accrue/lib/accrue/entitlements/apple/verifier.ex, accrue/test/fixtures/apple/server_evidence.exs, accrue/test/accrue/entitlements/apple_verifier_test.exs, .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-05 through D-08, D-20)
    - .planning/phases/218-apple-observation-and-repair/218-02-SUMMARY.md (legitimacy decision)
    - .planning/phases/218-apple-observation-and-repair/218-RESEARCH.md (candidate gates, A1/A2, Apple claim differences)
    - accrue/lib/accrue/entitlements/apple/intake.ex (VerifiedEvidence output contract)
    - accrue/mix.exs and accrue/mix.lock (current dependency boundary; read-only in this task)
  </read_first>
  <behavior>
    - Test 1: independent valid outer, transaction, and renewal fixtures normalize to the same bounded facts on repeat and in parallel.
    - Test 2: wrong algorithm, missing/unexpected critical header, wrong root/order, certificate purpose/time failure, bad signature, wrong bundle/environment, and wrong production app ID each return its closed reason.
    - Test 3: a valid outer JWS with either invalid nested JWS remains non-granting.
    - Test 4: production requires `appAppleId`; sandbox accepts its documented absence but still requires configured bundle/environment.
    - Test 5: raw fixture bytes and seeded secrets/PII are absent from returned structs, exceptions, Logger capture, telemetry, and inspect output.
  </behavior>
  <action>Create the verifier behaviour, immutable Config, hostile/captured corpus, and failing production-contract tests. If Plan 218-02 rejected the package, select the fallback in the report immediately. If approved, evaluate `app_store_server_library` in a temporary isolated Mix project against every D-05 gate: API shape, independent outer/nested verification, hostile x5c corpus, certificate purpose/time, critical-header rejection, supervision/side effects, recursive privacy, license/dependency tree, and replacement insulation. Do not modify the repository dependency files in this task. Record each gate, exact command, evidence, and deterministic `admit|reject` decision in `218-ADAPTER-ADMISSION.md`; any failed or unproven gate selects fallback. Define the stable D-06 output/error contract without exposing dependency or JOSE terms.</action>
  <acceptance_criteria>
    - Admission report has an explicit pass/fail row for every D-05 gate and identifies exactly one selected adapter.
    - Repository dependency files remain unchanged in this task and the report selects exactly one implementation path.
    - Every outer/nested, algorithm, chain, certificate, signature, bundle, environment, and app-identity case exists as an executable production-contract test.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix compile --warnings-as-errors &amp;&amp; test -s ../.planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md</automated></verify>
  <done>The verifier contract and hostile corpus are fixed, and reproducible evidence selects candidate or fallback without installing into the repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the selected strict production adapter</name>
  <files>accrue/mix.exs, accrue/mix.lock, accrue/lib/accrue/entitlements/apple/verifier/production.ex</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md (deterministic selected path)
    - accrue/lib/accrue/entitlements/apple/verifier.ex (Task 1 behaviour/config/error contract)
    - accrue/test/fixtures/apple/server_evidence.exs (golden and hostile corpus)
    - accrue/test/accrue/entitlements/apple_verifier_test.exs (failing production contract)
    - accrue/mix.exs and accrue/mix.lock (dependency boundary modified by this task only when admitted)
  </read_first>
  <behavior>
    - Test 1: independent valid outer, transaction, and renewal fixtures normalize to the same bounded facts on repeat and in parallel.
    - Test 2: wrong algorithm/header/root/order/purpose/time/signature/bundle/environment/app ID each returns its closed reason.
    - Test 3: a valid outer JWS with either invalid nested JWS remains non-granting; production/sandbox identity rules differ exactly as documented.
    - Test 4: raw fixture bytes and seeded secrets/PII are absent from returned structs, exceptions, Logger capture, telemetry, and inspect output.
  </behavior>
  <action>Implement `Verifier.Production` using the exact `admit|reject` decision from Task 1. On `admit`, add only `{:app_store_server_library, "~> 2.2"}` and its resolved lock entries and wrap all dependency values privately. On `reject`, leave `mix.exs`/`mix.lock` byte-unchanged and implement the complete fallback with the already direct `Jason` dependency plus OTP `:public_key`: split compact JWS into exactly three non-empty base64url segments; decode protected header and payload with `Jason.decode/1`; reject duplicate/unknown critical semantics and require `alg == "ES256"` plus a non-empty ordered `x5c`; decode every certificate from standard base64 DER; parse with `:public_key.pkix_decode_cert/2`; pin path validation to one configured Apple root with `:public_key.pkix_path_validation/3`; enforce child/issuer order, validity at the configured verification clock, CA/basicConstraints and keyCertSign on intermediates, digitalSignature and the configured Apple leaf purpose/EKU/OID policy on the leaf; reject extra/unconsumed certificates; and verify the exact ASCII `protected64.payload64` bytes. Require the JWS signature to decode to exactly 64-byte raw `R || S`, reject zero or out-of-range P-256 integers, DER-encode each unsigned integer with required sign padding into an ECDSA-Sig-Value SEQUENCE, and call `:public_key.verify/4` with SHA-256 and the leaf EC key. Only after signature and chain success, allowlist payload fields and enforce bundle/environment/production `appAppleId`. Re-run this complete function independently for outer notification, nested transaction, and nested renewal compact JWS; outer validity never blesses nested evidence. In both paths return only Task 1 values/errors. Verification is pure and concurrency-safe and performs no persistence, provider mutation, process registration, or secret logging.</action>
  <acceptance_criteria>
    - On candidate admission, dependency state contains only the admitted package and resolved transitive lock entries; on rejection, `mix.exs` and `mix.lock` are byte-identical and the fallback uses only direct Jason plus OTP `:public_key`; `mix deps.audit` is green in either path.
    - Fallback tests directly cover compact-segment/base64url/JSON failures, protected-header/alg/crit policy, x5c parse/order/root/time/purpose/constraints, raw-to-DER ES256 normalization including sign padding and invalid integer bounds, signature failure, application claims, and independent nested re-verification.
    - Every hostile and golden test passes against the selected production adapter.
    - Repeat/parallel tests cover AAPL-02 idempotency and concurrency without shared mutable state.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_verifier_test.exs &amp;&amp; mix deps.audit</automated></verify>
  <done>The selected private production verifier passes the entire strict corpus without public implementation leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| internet signedPayload -> verifier | Opaque attacker-controlled bytes claim Apple authority. |
| x5c/certificate claims -> normalized facts | Algorithm, chain, app, and environment confusion can forge access. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-05 | Spoofing/Tampering | verifier | critical | mitigate | Task 1 fixed ES256, validates ordered Apple trust/certificates/signatures/app claims independently for every JWS, and merge-blocks hostile fixtures. |
| T-218-06 | Information Disclosure | verifier/intake telemetry | high | mitigate | Tasks 1-2 recursively reject raw evidence, secrets, tokens, PII, and adopter values across every output channel. |
| T-218-SC | Tampering | candidate Hex package | high | mitigate | Plan 218-02 human legitimacy approval plus Task 1 all-gates admission; any failure removes the package and selects fallback. |
</threat_model>

<verification>
Run both focused suites, `mix deps.audit`, compile with warnings-as-errors, and confirm the admission report matches the actual dependency lock.
</verification>

<success_criteria>
- AAPL-02 hostile verification is complete and non-bypassable.
- No provider/dependency type or raw evidence escapes the private boundary.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-03-SUMMARY.md` when done.
</output>

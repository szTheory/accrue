---
phase: 218-apple-observation-and-repair
plan: 06
type: execute
wave: 5
depends_on: [218-05]
files_modified:
  - accrue/priv/repo/migrations/20260803032000_add_apple_ordering_to_entitlement_records.exs
  - accrue/lib/accrue/entitlements/observation.ex
  - accrue/lib/accrue/entitlements/grant.ex
  - accrue/lib/accrue/entitlements/projector.ex
  - accrue/lib/accrue/entitlements/apple/reconciliation.ex
  - accrue/lib/accrue/entitlements.ex
  - accrue/lib/accrue/entitlements/decision_cases.ex
  - accrue/test/accrue/entitlements/apple_reconciliation_test.exs
  - accrue/test/accrue/entitlements/apple_source_isolation_test.exs
  - accrue/test/support/entitlements/fixtures.ex
autonomous: true
requirements: [AAPL-04, AAPL-05]
must_haves:
  truths:
    - "Per D-14/D-15, active/grace/retry/expiry/refund/revocation/renewal-disabled facts normalize to rail-neutral bounds and a complete monotonic Apple order; delayed positive evidence cannot overwrite a later terminal fact."
    - "Per D-16, every Apple management result is `externally_managed` with exact text `Manage this subscription in Apple.` and action label `Manage subscription`; no Apple path reaches Stripe lifecycle code."
    - "Per D-17/D-19, hosts receive typed purchase/observe/repair/reconcile outcomes and bounded diagnostics, never Ecto rows, Apple transaction IDs, cursors, JWS, or provider-library values."
    - "Flagged assumption AAPL-05 (unclassified): hosts render the returned external-management guidance as actionable text/link and do not relabel it as an Accrue-controlled mutation."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/projector.ex"
      provides: "Apple complete-order comparison and source-local lifecycle projection"
    - path: "accrue/lib/accrue/entitlements.ex"
      provides: "Final typed Apple context surface and diagnostics"
      exports: ["apple_purchase_context/2", "observe_apple_evidence/3", "repair_apple_lineage/3", "reconcile_apple_lineage/2"]
    - path: "accrue/test/accrue/entitlements/apple_source_isolation_test.exs"
      provides: "Exact management guidance, explicit deferral, privacy, and zero-Stripe-call proof"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      to: "accrue/lib/accrue/entitlements/projector.ex"
      via: "verified normalized Observation carrying complete `provider_order_key`"
      pattern: "provider_order_key|Projector.project"
    - from: "accrue/lib/accrue/entitlements.ex"
      to: "accrue/lib/accrue/entitlements/source/registry.ex"
      via: "existing Apple management outcome/guidance"
      pattern: "Source.Registry.outcome.*apple.*management"
  assumptions:
    - requirement_id: AAPL-05
      category: unclassified
      status: unresolved
      statement: "Host presentation preserves the returned externally-managed semantics and literal action rather than implying an Accrue mutation."
  prohibitions:
    - requirement_id: AAPL-05
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT describe Apple cancellation, retry, swap, proration, refund, invoice, payment-method, dunning, Family Sharing, or offer authoring as controlled or completed by Accrue."
---

<objective>
Finish Apple lifecycle ordering, typed host outcomes, exact external-management guidance, and merge-blocking Stripe isolation per D-14 through D-20.

Purpose: Ensure reconciled Apple evidence changes only its own bounded source and never creates provider-lifecycle confusion.
Output: Complete ordering fields/projector logic, final Entitlements facade, decision fixtures, and lifecycle/isolation gates.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-05-SUMMARY.md
@.planning/phases/217-canonical-projection-and-compatibility/217-05-SUMMARY.md
@accrue/lib/accrue/entitlements/source/registry.ex
@accrue/lib/accrue/rails/gateway_registry.ex
@accrue/test/accrue/billing/resource_dispatch_test.exs
</context>

## Artifacts this phase produces

- Observation/Grant field `provider_order_key` (bounded canonical string) and named byte/order constraints.
- Canonical Apple order key `signed_epoch_us:effective_epoch_us:lifecycle_precedence:evidence_digest_prefix`, with precedence `active=10`, `renewal_disabled=20`, `grace=30`, `billing_retry=40`, `expired=50`, `refunded=60`, `revoked=70`.
- Rail-neutral normalized lifecycle kinds `active|renewal_disabled|grace|billing_retry|expired|refunded|revoked` and effective `expires_at` bounds.
- Public functions `apple_purchase_context/2`, `observe_apple_evidence/3`, `repair_apple_lineage/3`, `reconcile_apple_lineage/2`; typed fields `disposition`, `reason`, `next_action`, optional `snapshot`, optional `revision`.
- Exact customer conflict string and operator labels/actions from D-18; explicit deferred outcomes for Family Sharing and offer authoring. No CLI flags.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and compare the complete Apple ordering key</name>
  <files>accrue/priv/repo/migrations/20260803032000_add_apple_ordering_to_entitlement_records.exs, accrue/lib/accrue/entitlements/observation.ex, accrue/lib/accrue/entitlements/grant.ex, accrue/lib/accrue/entitlements/projector.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-13 through D-15, D-20)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (verified status/history facts)
    - accrue/lib/accrue/entitlements/observation.ex and grant.ex (durable ordering fields)
    - accrue/lib/accrue/entitlements/projector.ex (`apply_observation`, retraction, survivor semantics)
    - .planning/research/v1.59-DECISION-TABLE.md (lifecycle/order cases)
  </read_first>
  <behavior>
    - Test 1: later refund/revocation order beats delayed positive evidence and equal timestamps resolve by lifecycle precedence/digest tie-break.
    - Test 2: permutations yield the same snapshot/revision and an authoritative later key may restore Apple.
    - Test 3: sandbox/production and lineage/product order keys never compare across scopes; Stripe integer ordering remains compatible.
  </behavior>
  <action>Add bounded `provider_order_key` to Observation and Grant through a new additive migration and mirror named constraints in both changesets. Build it only from verified normalized timestamps, the exact precedence table above, and a digest prefix; never raw IDs. Extend Projector's scoped comparison so Apple uses the complete key within rail/environment/lineage/product while existing Stripe integer ordering remains compatible.</action>
  <acceptance_criteria>
    - Delayed positive evidence cannot overwrite later terminal evidence; later authoritative reversal may restore.
    - Scope isolation and Phase 217 Stripe ordering/revision semantics remain green.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs test/accrue/entitlements/projector_test.exs test/property/entitlement_projection_property_test.exs</automated></verify>
  <done>Apple observations/grants persist and compare one complete deterministic order key without changing Stripe ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize lifecycle bounds through the existing Projector</name>
  <files>accrue/lib/accrue/entitlements/apple/reconciliation.ex, accrue/lib/accrue/entitlements/projector.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs</files>
  <read_first>
    - accrue/lib/accrue/entitlements/projector.ex (Task 1 complete ordering)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (verified status/history normalization)
    - .planning/research/v1.59-DECISION-TABLE.md (lifecycle bounds and survivor cases)
  </read_first>
  <behavior>
    - Test 1: active grants through verified expiry; renewal-disabled preserves that bound; grace grants only through grace expiry; retry never invents a later bound.
    - Test 2: expiry/refund/revocation retract only the Apple source; equivalent Stripe survivor access remains and does not spuriously change authorization revision.
    - Test 3: Billing.Subscription enums and gateway reducers remain byte-unchanged.
  </behavior>
  <action>Normalize D-14 bounds before Observation: active/renewal-disabled use verified expiry, grace uses verified grace expiry, retry cannot exceed the last verified bound, and terminal facts retract only their Apple source. Feed the Task 1 order key through the existing Projector. Do not widen Billing.Subscription or route Apple through gateway reducers.</action>
  <acceptance_criteria>
    - Every lifecycle boundary and adjacency is deterministic and fixture-tested.
    - Survivor Stripe grants and account revision semantics from Phase 217 remain green.
    - No Stripe enum/schema/gateway lifecycle behavior changes.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs test/accrue/entitlements/projector_test.exs</automated></verify>
  <done>Verified Apple lifecycle facts affect only their bounded source through the existing Projector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize typed host outcomes and prove Apple-to-Stripe isolation</name>
  <reversibility rating="costly">Per D-17, host integrations and Phase 220 support tooling will pattern-match these additive outcome fields/reasons/actions.</reversibility>
  <files>accrue/lib/accrue/entitlements.ex, accrue/lib/accrue/entitlements/decision_cases.ex, accrue/test/accrue/entitlements/apple_source_isolation_test.exs, accrue/test/support/entitlements/fixtures.ex</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-16 through D-20 and deferred ideas)
    - accrue/lib/accrue/entitlements.ex (facades created by Plans 01/03/04)
    - accrue/lib/accrue/entitlements/source/registry.ex (exact Apple guidance)
    - accrue/lib/accrue/rails/gateway_registry.ex and accrue/test/accrue/billing/resource_dispatch_test.exs (Phase 217 isolation/call capture)
    - accrue/lib/accrue/entitlements/decision_cases.ex and test/support/entitlements/fixtures.ex (stable fixture vocabulary)
  </read_first>
  <behavior>
    - Test 1: all four public functions return only typed bounded outcomes and named start/stop/exception telemetry with allowlisted/hashed fields.
    - Test 2: conflict customer text is exactly “We couldn’t link this Apple purchase to this account. Contact support to review the purchase.” and contains no owner hint.
    - Test 3: management text/action exactly match D-16 and URL is the existing Registry URL; Family Sharing and offer authoring return explicit deferred capability/policy outcomes.
    - Test 4: observation, notification, repair, reconciliation, repeat, typed failure, and concurrent calls produce zero Fake callbacks for cancellation, retry, swap, proration, refund, invoice, payment method, item, and dunning families.
    - Test 5: public results/telemetry/audit/jobs/inspect contain no raw transaction ID, cursor/revision token, JWS, provider struct, account token, PII, or adopter identity.
  </behavior>
  <action>Finalize the D-17 context surface and typed values without bang APIs. `reconcile_apple_lineage/2` accepts an opaque local lineage reference and enqueues/returns bounded repair status; it never exposes the history cursor or provider ID. Reuse `Source.Registry.outcome(:apple, :management)` unchanged for exact D-16 guidance. Add explicit stable deferred outcomes for Family Sharing and offer authoring without implementing either. Extend DecisionCases/fixtures and the Phase 217 Fake callback inventory so every Apple path proves zero reachability to Stripe cancellation, retry, swap, proration, refund, invoice, payment-method, item, and dunning code. Capture D-19 telemetry/diagnostics and recursively reject forbidden keys and seeded values.</action>
  <acceptance_criteria>
    - Host surface is small, tagged, bounded, telemetry-instrumented, and hides all schemas/provider transport.
    - Exact conflict and management copy/action pass literal assertions without owner disclosure.
    - Family Sharing and offer authoring are visible deferrals only.
    - The full negative callback matrix proves no Apple-to-Stripe lifecycle reachability.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_source_isolation_test.exs test/accrue/billing/resource_dispatch_test.exs</automated></verify>
  <done>Hosts receive honest actionable Apple outcomes, and all Apple observation/repair/management paths are structurally isolated from Stripe lifecycle mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified lifecycle facts -> Projector | Ordering errors can resurrect revoked/refunded access. |
| Apple host outcome -> provider lifecycle UI/code | Mislabeling can imply Accrue controls Apple or dispatch Stripe mutations. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-17 | Tampering | Apple order/projector | critical | mitigate | Task 1 complete scoped order key and permutation/terminal-precedence tests prevent delayed resurrection. |
| T-218-18 | Elevation of Privilege | lifecycle normalization | high | mitigate | Task 1 never invents bounds and retracts only verified source-local facts. |
| T-218-19 | Spoofing | management/lifecycle facade | high | mitigate | Task 2 reuses exact externally-managed Registry outcome and exhaustively proves zero gateway callbacks. |
| T-218-20 | Information Disclosure | public outcomes/diagnostics | high | mitigate | Task 2 typed values and recursive privacy tests exclude provider IDs/cursors/JWS/token/PII. |
| T-218-SC | Tampering | package-manager installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
Run both task suites, all Phase 218 focused suites, existing projector/resource-dispatch tests, then the full `cd accrue &amp;&amp; mix test` phase gate.
</verification>

<success_criteria>
- AAPL-04 lifecycle bounds/order remain safe under delay, reversal, survivor grants, and environment isolation.
- AAPL-05 exact externally-managed guidance and explicit deferrals are truthful.
- Every Apple path remains unreachable from Stripe lifecycle mutations.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-06-SUMMARY.md` when done.
</output>

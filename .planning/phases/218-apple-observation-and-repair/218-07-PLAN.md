---
phase: 218-apple-observation-and-repair
plan: 07
type: execute
wave: 3
depends_on: [218-01, 218-03]
files_modified:
  - accrue/lib/accrue/entitlements/apple/notification_plug.ex
  - accrue/test/accrue/entitlements/apple_notification_test.exs
autonomous: true
requirements: [AAPL-03]
must_haves:
  truths:
    - "Per D-09, Notifications V2 acknowledges only durable `verified|noop|quarantined`; retryable persistence/provider results remain unsuccessful."
    - "Per D-08/D-19, request admission and acknowledgement never retain raw JWS/body/token/PII and emit only bounded allowlisted telemetry."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/notification_plug.ex"
      provides: "Bounded Notifications V2 HTTP acknowledgement boundary"
      exports: ["init/1", "call/2"]
    - path: "accrue/test/accrue/entitlements/apple_notification_test.exs"
      provides: "Durability, duplicate, failure, admission-limit, and redaction proof"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/notification_plug.ex"
      to: "accrue/lib/accrue/entitlements/apple/verifier.ex"
      via: "opaque signedPayload enters strict verification"
      pattern: "verify_notification"
    - from: "accrue/lib/accrue/entitlements/apple/notification_plug.ex"
      to: "accrue/lib/accrue/entitlements/apple/intake.ex"
      via: "HTTP status follows committed intake disposition"
      pattern: "Intake.observe"
  assumptions: []
  prohibitions:
    - requirement_id: AAPL-03
      category: privacy
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT retain notification bodies, raw JWS, app-account tokens, adopter identity, or PII in rows, telemetry, logs, exceptions, jobs, or inspected values."
---

<objective>
Expose bounded Notifications V2 ingress whose acknowledgement follows durable disposition per D-08/D-09 and D-19/D-20.

Purpose: Isolate the internet-input and HTTP durability boundary from cryptographic adapter admission for focused security review.
Output: Route-scoped notification Plug and deterministic admission/acknowledgement tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-01-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-03-SUMMARY.md
@accrue/lib/accrue/webhook/caching_body_reader.ex
@accrue/lib/accrue/webhook/plug.ex
</context>

## Artifacts this phase produces

- Plug `Accrue.Entitlements.Apple.NotificationPlug` with options `max_body_bytes` (default 262144), `rate_limiter`, `verifier`, and `verifier_config`; no CLI flags.
- Focused tests for durable acknowledgement, duplicate delivery, persistence/provider failure, size/rate admission, environment isolation, and recursive redaction.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Durably acknowledge bounded Notifications V2 intake</name>
  <files>accrue/lib/accrue/entitlements/apple/notification_plug.ex, accrue/test/accrue/entitlements/apple_notification_test.exs</files>
  <read_first>
    - accrue/lib/accrue/entitlements/apple/verifier.ex (Plan 218-03 verifier results)
    - accrue/lib/accrue/entitlements/apple/intake.ex (Plan 218-01 durable dispositions and transaction contract)
    - accrue/lib/accrue/webhook/caching_body_reader.ex (raw-body scoping pattern)
    - accrue/lib/accrue/webhook/plug.ex (Plug response/error pattern)
    - .planning/phases/218-apple-observation-and-repair/COVERAGE.md (Notifications V2 integrated and V1 opt-out)
  </read_first>
  <behavior>
    - Test 1: verified notification returns 200 only after durable intake/reconciliation wakeup; duplicate returns 200 with one row/job identity.
    - Test 2: terminal verification quarantine returns 200 only after its bounded row commits; database failure returns 503 and commits no partial state.
    - Test 3: provider/network/online-check retryable failure returns 503; malformed, oversized, and rate-rejected requests create no intake row.
    - Test 4: sandbox and production identities cannot collide; raw body/token/JWS is absent from rows, telemetry, Logger, exceptions, and inspected results.
  </behavior>
  <action>Implement `NotificationPlug` as a route-scoped Plug using the raw body assigned by the existing reader. Enforce the 262144-byte default before verification and call a host-supplied deterministic `rate_limiter.(conn)` returning `:allow | {:deny, retry_after_seconds}`; add no global mutable rate state. Verify outer and nested evidence through Plan 218-03, pass only normalized facts or a bounded closed quarantine to Plan 218-01 intake, and send 200 only for durably committed `verified|noop|quarantined`; send 503 for retryable provider/persistence results per D-09. Emit only allowlisted D-19 fields and response class; never route the body/JWS into generic webhook storage.</action>
  <acceptance_criteria>
    - HTTP status derives from committed disposition, not verification alone.
    - Duplicate delivery is a durable successful no-op; transient failure remains retryable by Apple.
    - Request-size/rate rejection is bounded and storage-free.
    - Notification V1 and unverified client-decoding paths do not exist.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_notification_test.exs test/accrue/entitlements/apple_verifier_test.exs</automated></verify>
  <done>Notifications V2 are strictly verified and acknowledged only after a bounded durable result, without raw-evidence retention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| internet request -> verifier | Attacker-controlled bytes can consume crypto/storage resources. |
| durable result -> HTTP acknowledgement | Premature success can lose evidence or create retry storms. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-07 | Denial of Service | notification plug | high | mitigate | Task 1 enforces size and host rate decisions before crypto/storage and bounds terminal quarantine. |
| T-218-08 | Repudiation | acknowledgement | high | mitigate | Task 1 returns success only after disposition commit and tests persistence failure as 503. |
| T-218-21 | Information Disclosure | request/telemetry | high | mitigate | Task 1 recursively excludes raw evidence and identity from every durable/emitted channel. |
| T-218-SC | Tampering | package-manager installs | low | accept | This plan installs no package and consumes the selected verifier seam. |
</threat_model>

<verification>
Run the focused notification and verifier suites, then `cd accrue &amp;&amp; mix format --check-formatted`.
</verification>

<success_criteria>
- AAPL-03 notification duplicates converge and terminal/retryable outcomes receive the correct HTTP class.
- Internet input cannot create unbounded work/storage or leak raw evidence.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-07-SUMMARY.md` when done.
</output>

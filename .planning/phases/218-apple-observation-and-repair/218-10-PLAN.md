---
phase: 218-apple-observation-and-repair
plan: 10
type: execute
wave: 7
depends_on: [218-06]
files_modified:
  - accrue/lib/accrue/entitlements/apple/reconciliation.ex
  - accrue/lib/accrue/entitlements/apple/reconcile_worker.ex
  - accrue/lib/accrue/entitlements/apple/reconciliation_sweeper.ex
  - accrue/test/accrue/entitlements/apple_reconciliation_test.exs
  - accrue/guides/entitlements.md
autonomous: true
gap_closure: true
requirements: [AAPL-04]
must_haves:
  truths:
    - "Per D-10 through D-13, a host-scheduled production sweeper selects due idle Apple checkpoints by `next_due_at`, locks them with `FOR UPDATE SKIP LOCKED`, and durably inserts privacy-safe `ReconcileWorker` jobs."
    - "Concurrent or repeated sweep ticks enqueue at most one due reconciliation for a lineage/environment and cannot rely on Oban uniqueness as the execution lock."
    - "A checkpoint that becomes due without any notification is selected, its worker executes configured status/history reconciliation, and newly verified provider state repairs the canonical Apple grant/snapshot."
    - "Accrue remains childless: the host explicitly wires `ReconciliationSweeper` into its owned Oban Cron plugin and `:accrue_entitlements` queue."
    - "Provider outage and job-insert failure remain durable/retryable; missing or invalid reconciliation configuration atomically moves a claimed checkpoint from `running` to `needs_repair` before its Oban job is cancelled, and scheduling never extends a grant beyond its last verified bound."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/reconciliation_sweeper.ex"
      provides: "Host-owned Oban cron entry point for durable due-checkpoint dispatch"
      exports: ["perform/1", "sweep/2"]
    - path: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      provides: "Locked due-checkpoint claim, atomic ReconcileWorker insertion, and durable configuration-failure transition"
      exports: ["enqueue_due/2", "mark_configuration_failure/3"]
    - path: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      provides: "Configured reconciliation execution that cannot strand a claimed checkpoint before Reconciliation.run/2"
    - path: "accrue/guides/entitlements.md"
      provides: "Exact host Oban queue, Cron plugin, and Apple admission/reconciliation configuration wiring"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/reconciliation_sweeper.ex"
      to: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      via: "host Cron tick calls `enqueue_due/2` with host-owned Repo"
      pattern: "ReconciliationSweeper|enqueue_due"
    - from: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      to: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      via: "locked due checkpoint inserts scalar lineage/environment/reason job and reserves the due row in one transaction"
      pattern: "FOR UPDATE SKIP LOCKED|ReconcileWorker.new|scheduled_due"
    - from: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      to: "accrue/lib/accrue/entitlements/apple/intake.ex"
      via: "configured status/history client and strict admission repair the canonical snapshot after a missed notification"
      pattern: "Reconciliation.run|Admission.admit"
    - from: "accrue/lib/accrue/entitlements/apple/reconcile_worker.ex"
      to: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      via: "missing or invalid host configuration is persisted as `needs_repair` under the checkpoint lock before cancellation"
      pattern: "mark_configuration_failure|missing_reconciliation_configuration|invalid_reconciliation_configuration"
  assumptions: []
  prohibitions:
    - requirement_id: AAPL-04
      category: authority
      status: unverified
      verification: null
      flagged: true
      statement: "MUST NOT treat notification delivery order, notification history, process-local timers, or Oban uniqueness as current entitlement or concurrency authority."
    - requirement_id: AAPL-04
      category: lifecycle
      status: unverified
      verification: null
      flagged: true
      statement: "MUST NOT start an Accrue-owned Oban process, widen a known Apple access bound during outage, or route scheduled Apple repair into Stripe lifecycle mutation."
---

<objective>
Close the AAPL-04 missed-notification repair gap by turning persisted `next_due_at` checkpoints into durable, locked, host-scheduled reconciliation work per D-10 through D-13 and D-19/D-20.

Purpose: A stored due time is inert until production code consumes it; the host-owned sweeper makes periodic status/history repair reachable after notifications are absent.
Output: Due-checkpoint dispatcher, Oban cron worker, host wiring guide, and an end-to-end missed-notification repair test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@.planning/phases/218-apple-observation-and-repair/218-05-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-06-SUMMARY.md
@accrue/lib/accrue/entitlements/apple/reconciliation.ex
@accrue/lib/accrue/entitlements/apple/reconcile_worker.ex
@accrue/lib/accrue/jobs/dunning_sweeper.ex
</context>

## Artifacts this phase produces

- `Accrue.Entitlements.Apple.Reconciliation.enqueue_due/2`, which transactionally claims due checkpoint rows and inserts `ReconcileWorker` jobs, plus `mark_configuration_failure/3`, which locks the claimed checkpoint and durably records pre-run configuration failure.
- Revised `Accrue.Entitlements.Apple.ReconcileWorker`, which validates host client/admission configuration and cancels for configuration repair only after the checkpoint reaches `needs_repair`; persistence failure returns an Oban error so the job retries instead of abandoning `running` state.
- `Accrue.Entitlements.Apple.ReconciliationSweeper`, an Oban worker on `:accrue_entitlements` with `perform/1` and testable `sweep/2` entry points.
- Due-dispatch defaults: batch limit 100; eligible checkpoints are `run_state: :idle`, `next_due_at <= now`, and not postponed by a later `retry_after_at`; job reason is the bounded scalar `scheduled_due`.
- Host wiring documentation for the existing host-owned Oban instance: `:accrue_entitlements` queue, `{Oban.Plugins.Cron, crontab: [{"*/15 * * * *", Accrue.Entitlements.Apple.ReconciliationSweeper}]}`, and existing `:apple_reconciliation` client/admission configuration.
- No new table, field, migration, dependency, CLI flag, or Accrue supervision child.

## Gap-closure source audit

| SOURCE | ID | Gap item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Apple evidence repairs itself after missed delivery | 218-10 | COVERED | Due checkpoint becomes durable worker execution without a notification wakeup. |
| REQ | AAPL-04 | Scheduled status/history repair | 218-10 | COVERED | Host Cron, locked dispatch, and worker execution are tested end to end. |
| RESEARCH | — | Schedule due checkpoint work separately | 218-10 | COVERED | New sweeper is the production caller of due selection. |
| CONTEXT | D-10 | Status/history authority, notification diagnostic only | 218-10 | COVERED | Scheduler invokes the existing authoritative reconciliation lane. |
| CONTEXT | D-11 | Durable checkpoint/cursor state | 218-10 | COVERED | Existing next_due_at and cursor fields are consumed without schema change. |
| CONTEXT | D-12 | Host-owned Oban, locking, bounded concurrency | 218-10 | COVERED | Host Cron plus row locks dispatch bounded jobs. |
| CONTEXT | D-13 | Outage cannot widen access | 218-10 | COVERED | Existing retry/needs-repair state remains authoritative. |
| CONTEXT | D-19 | Bounded queue diagnostics | 218-10 | COVERED | Jobs contain only lineage UUID, environment, and bounded reason. |
| CONTEXT | D-20 | Missed-delivery/outage/concurrency proof | 218-10 | COVERED | Focused integration test covers each case. |

## Planning gates

- Schema-push detection: not applicable; `next_due_at`, `retry_after_at`, `run_state`, and the checkpoint unique identity already exist and no migration is planned.
- API coverage: applicable only as previously recorded status/history repair; the sweeper adds no Apple API capability and invokes the existing client/admission lane.
- Assumption delta: none; D-12 already fixes host-owned Oban as the scheduler and the live code establishes the queue/worker conventions.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Sweep one missed-notification checkpoint through status/history repair</name>
  <reversibility rating="costly">Per D-12/D-17, host applications will depend on the queue, Cron worker, and `:apple_reconciliation` configuration contract; rollback removes the host Cron entry only after draining queued jobs or atomically returning any scheduler-reserved `running` checkpoints to `idle`, while the schema and verified grant state require no migration.</reversibility>
  <files>accrue/lib/accrue/entitlements/apple/reconciliation.ex, accrue/lib/accrue/entitlements/apple/reconcile_worker.ex, accrue/lib/accrue/entitlements/apple/reconciliation_sweeper.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs, accrue/guides/entitlements.md</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (second failed truth and unwired `next_due_at` key link)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-10 through D-13 and D-19/D-20)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (`Checkpoint`, `due/2`, final-page `next_due_at`, row locks, and worker insertion patterns)
    - accrue/lib/accrue/entitlements/apple/reconcile_worker.ex (configured production execution contract)
    - accrue/lib/accrue/entitlements/apple/reconciliation_wakeup_worker.ex (thin host-owned worker result mapping)
    - accrue/lib/accrue/jobs/dunning_sweeper.ex (host-owned Oban Cron documentation and testable sweep convention)
    - accrue/test/accrue/entitlements/apple_reconciliation_test.exs (configured client/admission and `perform_job` conventions)
  </read_first>
  <behavior>
    - Test 1: a previously completed checkpoint with `next_due_at` in the past and no reconciliation wakeup is claimed by the sweeper, inserts one `ReconcileWorker` job with scalar args, and is no longer immediately claimable.
    - Test 2: performing that job against configured Fake status/history evidence retracts or restores the affected Apple grant and updates the canonical snapshot/checkpoint even though no notification arrived.
    - Test 3: two concurrent sweeps use `FOR UPDATE SKIP LOCKED` and enqueue exactly one scheduled-due job per lineage/environment; repeating the tick before worker completion remains a no-op.
    - Test 4: future, retry-postponed, running, and `needs_repair` checkpoints are not selected; the batch limit is enforced deterministically.
    - Test 5: job-insert failure rolls back the checkpoint reservation so a later tick can reclaim it; provider failure follows the existing durable retry path without extending the last verified grant bound.
    - Test 6: when the sweeper has reserved a due checkpoint as `running`, an absent `:apple_reconciliation` setting, a non-keyword setting, a missing/nil client, or a non-keyword admission value makes `ReconcileWorker` lock that checkpoint, set `run_state: :needs_repair`, clear retry/due deadlines, record the bounded configuration reason, and only then return its cancel result; an injected persistence failure returns an Oban error and leaves the job retryable rather than silently stranded.
    - Test 7: `Accrue.Application` still starts with no children, and the guide contains runnable host Oban queue/Cron plus Apple reconciliation configuration and a safe disable/rollback sequence.
  </behavior>
  <action>Write the missed-notification integration test first: complete a reconciliation to persist `next_due_at`, advance the supplied clock past that deadline without creating a wakeup, run the sweeper, perform the persisted `ReconcileWorker`, and assert the changed verified status/history facts repair the Apple grant and snapshot. Add `Reconciliation.enqueue_due(repo, opts \\ [])` with default `now: DateTime.utc_now()` and `limit: 100`; in one `repo.transact`, query checkpoint rows whose `run_state` is `:idle`, whose `next_due_at` is due, and whose retry deadline is absent or due, order deterministically by due time/id, limit the batch, and lock with `FOR UPDATE SKIP LOCKED`. For each row, insert `ReconcileWorker.new/2` with only string lineage UUID, environment, and reason `scheduled_due`, then reserve it in the same transaction by setting `run_state: :running` and clearing `next_due_at`; if insertion/update fails, roll back so the due row remains claimable. Correctness comes from checkpoint locking and state transition, not Oban uniqueness. Add `Reconciliation.mark_configuration_failure(args, reason, opts \\ [])`: resolve the repository from `opts`, transact, reuse `lock_checkpoint/3` for the lineage/environment scalar args, and set `run_state: :needs_repair`, `retry_after_at: nil`, `next_due_at: nil`, and bounded `last_provider_class` (`missing_reconciliation_configuration` or `invalid_reconciliation_configuration`). Update `ReconcileWorker.perform/1` to read the full `:apple_reconciliation` setting once, guard it with `Keyword.keyword?/1` before any `Keyword.get/3`, then require a non-nil client and keyword admission value before `Reconciliation.run/2`. Treat an absent setting or nil client as missing configuration and a non-keyword outer/admission value as invalid configuration. On either pre-run failure, call `mark_configuration_failure/3` and return `{:cancel, :needs_repair}` only after the transaction succeeds; return `{:error, persistence_reason}` when the transition cannot commit so Oban retries the job. This closes the exact claimed-then-cancelled failure path without incrementing provider attempts per D-13. Add `ReconciliationSweeper` as a thin Oban worker on `:accrue_entitlements`; `perform/1` installs Accrue Oban middleware and calls `sweep/2`, while `sweep/2` delegates to `enqueue_due/2` for testability and returns `{:ok, count}`. Preserve existing `Reconciliation.run/2` completion, continuation, retry, final-page cursor, and `needs_repair` transitions. Document exact host-owned Oban queue/Cron configuration, the existing Apple client/admission application configuration, and a safe disable path that removes Cron dispatch only after queued reconciliation jobs drain or reserved checkpoints are atomically released; do not add a child to `Accrue.Application` or create a process-local timer.</action>
  <acceptance_criteria>
    - Production code has a real caller that selects persisted due checkpoints and inserts `ReconcileWorker` jobs.
    - A no-notification end-to-end test proves the scheduled job executes existing strict status/history admission and changes only the affected Apple source in the canonical snapshot.
    - Locks plus an atomic checkpoint reservation make concurrent/repeated sweeps exactly-once at dispatch for each due cycle; a failed transaction remains retryable.
    - Every scheduled-due job that encounters missing or invalid host configuration durably transitions its claimed checkpoint from `running` to `needs_repair` with a bounded reason before cancellation; if that write fails, Oban receives an error and retries the job.
    - Non-due, postponed, running, and repair-required checkpoints are not scheduled, and no raw provider evidence or credentials enter job args.
    - Host documentation supplies runnable Cron/queue/config wiring while `Accrue.Application` remains childless.
    - Provider outages retain retry scheduling, while configuration failures consume no provider attempt and cannot leave `running` without a due time, retry job, or repair state.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs test/accrue/entitlements/apple_source_isolation_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix format --check-formatted</automated></verify>
  <done>A host-wired periodic tick repairs Apple state after a missed notification through durable due selection, locked job dispatch, strict reconciliation admission, and canonical projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted checkpoint time -> Oban job | Concurrent cron ticks must not duplicate or lose due repair work. |
| scheduled worker -> Apple status/history | Remote state may be unavailable, repeated, delayed, or hostile and must traverse strict admission. |
| Accrue library -> host runtime | The host owns Repo, Oban, queue concurrency, Cron, credentials, and client supervision. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-G10-01 | Denial of Service | due-checkpoint sweeper | high | mitigate | Task 1 limits each batch to 100, filters eligible states, and uses SKIP LOCKED so parallel ticks do not contend or multiply jobs. |
| T-218-G10-02 | Tampering | checkpoint/job transition | high | mitigate | Task 1 inserts the scalar worker job and reserves the checkpoint in one transaction with rollback-on-failure proof. |
| T-218-G10-06 | Denial of Service | pre-run worker configuration | high | mitigate | Task 1 makes missing or malformed host configuration a locked `needs_repair` transition before cancellation and makes transition-write failure retry the Oban job. |
| T-218-G10-03 | Elevation of Privilege | scheduled provider evidence | high | mitigate | The scheduled worker reuses configured strict Admission and existing source-local Projector rules; due time itself never grants. |
| T-218-G10-04 | Information Disclosure | Oban args/telemetry | medium | mitigate | Jobs contain only local lineage UUID, environment, and bounded reason; provider payloads, tokens, credentials, and account identities remain excluded. |
| T-218-G10-05 | Spoofing | host scheduler wiring | low | accept | Host-owned Cron configuration is operationally trusted; exact runnable wiring and queue ownership are documented and tested structurally. |
| T-218-SC | Tampering | package-manager installs | low | accept | This plan installs no package and uses existing Oban/Ecto facilities. |
</threat_model>

<verification>
Run the focused reconciliation suite twice to prove stable due-cycle dispatch, then `cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_*_test.exs test/property/apple_convergence_property_test.exs` and confirm `rg 'Reconciliation\.enqueue_due|ReconciliationSweeper' accrue/lib accrue/guides/entitlements.md` finds production and host-wiring callers.
</verification>

<success_criteria>
- AAPL-04 periodic repair is reachable without a notification or manual wakeup.
- Due selection is durable, locked, bounded, replay-safe, and host-owned.
- Scheduled execution reuses strict status/history admission and cannot widen access during outage or cross into Stripe lifecycle control.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-10-SUMMARY.md` when done.
</output>

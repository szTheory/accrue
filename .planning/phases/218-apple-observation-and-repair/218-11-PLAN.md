---
phase: 218-apple-observation-and-repair
plan: 11
type: execute
wave: 8
depends_on: [218-09, 218-10]
files_modified:
  - accrue/lib/accrue/entitlements/apple/admission.ex
  - accrue/lib/accrue/entitlements/apple/intake.ex
  - accrue/lib/accrue/entitlements/apple/reconciliation.ex
  - accrue/lib/accrue/entitlements/apple/verifier/production.ex
  - accrue/test/accrue/entitlements/apple_observation_tracer_test.exs
  - accrue/test/accrue/entitlements/apple_lineage_test.exs
  - accrue/test/accrue/entitlements/apple_reconciliation_test.exs
  - accrue/test/accrue/entitlements/apple_verifier_test.exs
  - accrue/test/fixtures/apple/server_evidence.exs
  - .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md
autonomous: true
gap_closure: true
requirements: [AAPL-01, AAPL-02, AAPL-03, AAPL-04]
must_haves:
  truths:
    - "D-13: invalid or ambiguous evidence never widens access; D-14: active or renewal-disabled Apple evidence without a valid verified expiry is non-granting and cannot create an Observation, Grant, or account revision."
    - "D-02: only a verified matching account token may claim; D-03: unbound/conflicting evidence remains non-granting; D-04: repair is authorized and unbound-only; D-17: the regression seeds through configured opaque signed evidence and preserves separate public forged-struct rejection proof."
    - "D-06: the production verifier accepts a real valid three-certificate ES256 Apple-purpose chain and rejects each purpose, CA, and key-usage mutation before any durable entitlement effect; D-20: this corpus is merge-blocking."
    - "The documented Phase 218 wildcard Apple suite is green with the expiry, lineage-repair, and certificate-purpose regressions included."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/admission.ex"
      provides: "Fail-closed conversion from verified Apple claims to bounded lifecycle evidence"
    - path: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      provides: "Lifecycle normalization that refuses missing active/renewal access bounds"
      exports: ["normalize_lifecycle/1"]
    - path: "accrue/test/fixtures/apple/server_evidence.exs"
      provides: "Test-only valid and hostile three-certificate ES256 corpus"
    - path: "accrue/test/accrue/entitlements/apple_lineage_test.exs"
      provides: "Runnable opaque-evidence authorized-repair regression"
  key_links:
    - from: "accrue/lib/accrue/entitlements/apple/admission.ex"
      to: "accrue/lib/accrue/entitlements/apple/intake.ex"
      via: "only lifecycle evidence with a verified required bound reaches claim/projection"
      pattern: "expiresDate|normalize_lifecycle|Intake.observe"
    - from: "accrue/lib/accrue/entitlements/apple/verifier/production.ex"
      to: "accrue/test/fixtures/apple/server_evidence.exs"
      via: "real ES256 JWS signatures over leaf-first x5c chains rooted in the configured test root"
      pattern: "invalid_certificate_purpose|x5c|ES256"
    - from: "accrue/test/accrue/entitlements/apple_lineage_test.exs"
      to: "accrue/lib/accrue/entitlements.ex"
      via: "opaque signed observation followed by authorized repair_apple_lineage/3"
      pattern: "observe_apple_evidence|repair_apple_lineage"
  assumptions:
    - requirement_id: AAPL-04
      category: required_to_optional
      status: resolved
      statement: "The implementation drifted from D-14's required verified expiry to an optional active bound; this plan rejects that delta and keeps active/renewal expiry required and provider-derived."
  prohibitions:
    - requirement_id: AAPL-01
      category: authority
      status: resolved
      verification: "Opaque signed-evidence repair setup plus separate forged-struct rejection regression"
      flagged: false
      statement: "A caller-created verified value cannot seed ownership or bypass configured admission."
    - requirement_id: AAPL-02
      category: authority
      status: resolved
      verification: "Valid and hostile real-chain Production verifier tests"
      flagged: false
      statement: "A valid EC chain for the wrong certificate purpose cannot become grant authority."
---

<objective>
Close the remaining Phase 218 safety and executable-evidence gaps: reject unbounded active Apple evidence, restore the opaque authorized-repair regression, and prove the production certificate-purpose predicate with real valid and hostile ES256 chains per D-02 through D-04, D-06, D-13/D-14, D-17, and D-20.

Purpose: Phase 218 cannot be considered safe while incomplete subscription facts can grant indefinitely or its production verifier policy and repair path remain unexercised.
Output: Fail-closed lifecycle admission/normalization and a green end-to-end Apple regression corpus covering ownership repair and certificate purpose.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md
@.planning/phases/218-apple-observation-and-repair/218-09-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-10-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@accrue/lib/accrue/entitlements/apple/admission.ex
@accrue/lib/accrue/entitlements/apple/intake.ex
@accrue/lib/accrue/entitlements/apple/reconciliation.ex
@accrue/lib/accrue/entitlements/apple/verifier/production.ex
</context>

## Artifacts this phase produces

- No schema, migration, public API, CLI flag, dependency, Apple endpoint, or runtime process is added.
- Existing `Accrue.Entitlements.Apple.Admission` and `Intake` gain fail-closed required-bound handling; existing `Reconciliation.normalize_lifecycle/1` gains an explicit invalid-bound result consumed before projection.
- Existing `Accrue.Test.AppleServerEvidence` gains test-only real ES256 transaction builders and valid/hostile leaf-first three-certificate chains; no production key or Apple credential is included.
- Existing Apple tracer, lineage, reconciliation, and verifier suites gain the regression cases required by `218-VERIFICATION.md`.

## Gap-closure source audit

| SOURCE | ID | Gap item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Verified Apple evidence contributes safely and repair remains reachable | 218-11 | COVERED | Required bounds fail closed, opaque repair runs, and verifier purpose is behavior-proven. |
| REQ | AAPL-01 | Opaque token bind-once and authorized repair regression | 218-11 | COVERED | The obsolete public struct setup is replaced with configured opaque signed evidence per D-02/D-04. |
| REQ | AAPL-02 | Production Apple certificate-purpose behavior | 218-11 | COVERED | Valid and hostile real three-certificate ES256 chains execute Production verification per D-06. |
| REQ | AAPL-03 | Repairable non-granting invalid/unbound evidence | 218-11 | COVERED | Missing bounds and unbound setup cannot grant; repair remains authorized and idempotent per D-03/D-07. |
| REQ | AAPL-04 | Active/renewal access is bounded by verified expiry | 218-11 | COVERED | Admission and normalization reject the observed required-to-optional drift per D-13/D-14. |
| REQ | AAPL-05 | External Apple management and explicit policy deferrals | 218-08 | COVERED | Verification already passes; no new work is manufactured here. |
| RESEARCH | verifier pipeline | Private Jason/OTP verifier requires positive and hostile chain evidence | 218-11 | COVERED | Existing package rejection and API coverage stay unchanged; fixtures prove the selected fallback. |
| RESEARCH | lifecycle bounds | Ambiguous evidence must not widen access | 218-11 | COVERED | Missing/malformed required bounds stop before projection. |
| CONTEXT | D-01–D-04 | Bind-once ownership and explicit unbound repair | 218-01/04/11 | COVERED | Existing implementation remains; this plan repairs its runnable regression path. |
| CONTEXT | D-05–D-09 | Private strict verification, closed outcomes, bounded retention | 218-03/07/09/11 | COVERED | This plan supplies the missing production behavior proof without broadening ingress. |
| CONTEXT | D-10–D-13 | Authoritative repair, durable checkpointing, no outage widening | 218-05/10/11 | COVERED | Existing reconciliation remains; missing provider bounds now fail closed. |
| CONTEXT | D-14–D-16 | Bounded lifecycle, Projector sole writer, Apple isolation | 218-06/08/11 | COVERED | Required expiry is enforced before sole-writer projection. |
| CONTEXT | D-17–D-20 | Small host API, bounded diagnostics, merge-blocking corpus | 218-08/09/11 | COVERED | Opaque repair and real-chain evidence make the documented suite executable. |

## Planning gates

- Schema-push detection: not applicable; no schema-relevant file is touched.
- API coverage: preserve `COVERAGE.md` exactly; this plan adds no Apple API capability and exercises only already-integrated purchase/restore, verification, and repair paths.
- Assumption delta: a required-to-optional drift is present. D-14 requires active/renewal access to end at a verified expiry, so missing or malformed expiry is classified non-granting rather than chosen or defaulted.
- Reversibility: all changes are local test and validation behavior behind existing contracts; no migration, published-shape change, or one-way decision warrants a checkpoint.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace bounded opaque evidence through grant and authorized repair</name>
  <files>accrue/lib/accrue/entitlements/apple/admission.ex, accrue/test/accrue/entitlements/apple_observation_tracer_test.exs, accrue/test/accrue/entitlements/apple_lineage_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (failed missing-expiry truth and stale lineage-test gap)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-02 through D-04, D-13/D-14, D-17/D-20)
    - accrue/lib/accrue/entitlements/apple/admission.ex (`evidence/6`, `lifecycle/1`, and Apple timestamp conversion)
    - accrue/test/accrue/entitlements/apple_observation_tracer_test.exs (configured opaque Fake verifier and durable-effect assertions)
    - accrue/test/accrue/entitlements/apple_lineage_test.exs (obsolete caller-created initial evidence and authorized repair flow)
  </read_first>
  <behavior>
    - Test 1: an opaque verified active transaction with a valid future `expiresDate` binds once, creates one Observation and one bounded Grant, and returns snapshot revision 1.
    - Test 2: an otherwise valid opaque transaction with absent, nil, malformed, or out-of-range `expiresDate` returns the existing closed invalid-payload outcome and creates no lineage, intake, Observation, Grant, wakeup, or account revision.
    - Test 3: an opaque verified transaction with no usable account token durably seeds `verified_unbound`; authorized `repair_apple_lineage/3` re-verifies a bounded internal fact, binds it once, projects revision 1, and repeats as a duplicate no-op.
    - Test 4: the separate public forged `%Intake.VerifiedEvidence{}` assertion remains closed with zero durable effects.
    - Test 5: denied authorization invokes neither re-verification nor database mutation, and ownership conflicts remain non-granting and non-disclosing.
  </behavior>
  <action>Start by updating the public tracer's valid signed fixture to carry a deterministic future millisecond `expiresDate`, assert the persisted Observation and current Grant share that exact bound, and add a table-driven missing/nil/malformed/out-of-range expiry regression that snapshots all durable row counts and account revision before and after the call. In `Admission.evidence/6`, derive lifecycle from independently verified facts and require every active or renewal-bearing lifecycle to carry a successfully parsed `%DateTime{}` provider expiry before constructing internal `VerifiedEvidence`; return the existing closed invalid-payload result when the required bound is unusable, without inventing a time or consulting receipt/session order per D-13/D-14. Preserve terminal retraction handling and the public opaque-only boundary from Plan 218-09. In `apple_lineage_test.exs`, configure the existing Fake verifier/product map, seed the unbound lineage through `observe_apple_evidence/3` using opaque signed bytes with nil token and a valid expiry, obtain the local opaque lineage UUID from persistence, then execute the existing host-authorized repair callback with a bounded re-verified fact. Retain the explicit forged-struct rejection in the observation tracer and the authorization-before-provider assertion per D-02 through D-04/D-17.</action>
  <acceptance_criteria>
    - The successful public purchase tracer creates a Grant whose expiry exactly equals the verified Apple expiry.
    - Every missing or malformed active expiry case is non-granting and leaves zero new lineage, intake, Observation, Grant, wakeup, or revision effects.
    - The repair test reaches authorized repair only after an opaque configured signed-evidence call creates the unbound lineage; it no longer treats a caller-created verified struct as public authority.
    - Public forged-struct rejection, denial-before-reverification, idempotent repair, bind-once ownership, and privacy-safe conflict behavior remain independently asserted.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_observation_tracer_test.exs test/accrue/entitlements/apple_lineage_test.exs</automated></verify>
  <done>Opaque verified evidence grants only to its provider bound, and the documented authorized unbound-lineage repair path runs without reopening public trust-by-struct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce required bounds at the final normalization seam</name>
  <files>accrue/lib/accrue/entitlements/apple/intake.ex, accrue/lib/accrue/entitlements/apple/reconciliation.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (hollow `expires_at: nil` data-flow finding)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-07, D-13 through D-15)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (`normalize_lifecycle/1` and `lifecycle_bound/2`)
    - accrue/lib/accrue/entitlements/apple/intake.ex (`do_observe/4`, `observation_attrs/2`, terminal quarantine, and Projector handoff)
    - accrue/test/accrue/entitlements/apple_reconciliation_test.exs (`lifecycle normalization preserves only verified provider bounds`)
  </read_first>
  <behavior>
    - Test 1: active and renewal-disabled normalization succeeds only with a `%DateTime{}` expiry and retains that exact bound.
    - Test 2: grace succeeds only with verified `grace_expires_at`; billing retry succeeds only with verified `last_verified_expires_at`; missing or malformed required bounds return a closed invalid-bound result rather than a normalized map with nil expiry.
    - Test 3: expired, refunded, and revoked facts retain source-local retraction behavior without an invented future access bound.
    - Test 4: direct internal Intake or reconciliation admission of a verified positive fact lacking its required bound persists at most the existing bounded `invalid_payload` quarantine and creates no Observation, Grant, or revision.
    - Test 5: delayed-positive ordering, survivor Stripe access, final-page history, and scheduled reconciliation tests remain green.
  </behavior>
  <action>Make `Reconciliation.normalize_lifecycle/1` return an explicit tagged success or closed invalid-bound error so callers cannot confuse a nil access bound with a valid active/renewal/grace/retry fact. Require `%DateTime{}` for active and renewal-disabled `expires_at`, grace `grace_expires_at`, and billing-retry `last_verified_expires_at`; keep terminal lifecycle output source-local and non-granting. Update Intake to validate/normalize before constructing an Observation or invoking Projector, map invalid required bounds to the existing terminal `invalid_payload` disposition/action rather than adding an open-ended public reason vocabulary, and ensure the transaction cannot leave an Observation, Grant, or revision if normalization fails. Adapt all normalization call sites and focused assertions to the tagged result while preserving the complete Apple order key and Stripe integer-order compatibility from D-15.</action>
  <acceptance_criteria>
    - No positive lifecycle can produce a normalized result containing a nil access bound.
    - Invalid positive bounds are closed before Observation/Projector and create no Grant or account revision; any retained intake is bounded quarantine using the existing reason vocabulary.
    - Terminal retractions, complete Apple ordering, survivor grants, status/history pagination, and scheduled repair semantics remain unchanged.
    - No billing subscription enum, gateway reducer, database field, or migration changes.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs test/accrue/entitlements/apple_observation_tracer_test.exs test/accrue/entitlements/projector_test.exs</automated></verify>
  <done>The final normalized Apple-to-Projector seam cannot represent an active, renewal, grace, or retry grant without its required verified provider bound.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Apple certificate purpose with real ES256 chains</name>
  <files>accrue/lib/accrue/entitlements/apple/verifier/production.ex, accrue/test/accrue/entitlements/apple_verifier_test.exs, accrue/test/fixtures/apple/server_evidence.exs, accrue/test/accrue/entitlements/apple_observation_tracer_test.exs, .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md</files>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (behavior-unverified three-certificate production predicate)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-05/D-06, D-08, D-15, D-20)
    - accrue/lib/accrue/entitlements/apple/verifier/production.ex (`validate_chain/2`, `validate_leaf_purpose/1`, extension decoding, and ES256 signature verification)
    - accrue/test/accrue/entitlements/apple_verifier_test.exs (current malformed/invalid-chain-only corpus)
    - accrue/test/fixtures/apple/server_evidence.exs (current compact JWS helper and bounded claims)
    - .planning/phases/218-apple-observation-and-repair/218-ADAPTER-ADMISSION.md (certificate-purpose evidence row that currently overstates the executed corpus)
  </read_first>
  <behavior>
    - Test 1: a leaf-first three-certificate chain rooted in the configured test root, with the exact leaf purpose OID `1.2.840.113635.100.6.11.1`, intermediate purpose OID `1.2.840.113635.100.6.2.1`, non-CA leaf, and digital-signature key usage verifies a real ES256 transaction JWS and returns only allowlisted facts.
    - Test 2: missing or wrong leaf purpose returns `{:error, :invalid_certificate_purpose}`.
    - Test 3: missing or wrong intermediate purpose returns `{:error, :invalid_certificate_purpose}`.
    - Test 4: a CA leaf, missing digitalSignature, or CA-signing-only leaf usage returns `{:error, :invalid_certificate_purpose}` even though chain signatures and the configured root remain valid.
    - Test 5: wrong root/order/length/time, bad JWS signature, wrong bundle/environment/app ID, and invalid nested evidence retain their distinct closed failures.
    - Test 6: the valid Production result crosses configured Admission to one bounded Grant; every hostile purpose/usage variant leaves lineage, intake, Observation, Grant, wakeup, and revision counts unchanged.
  </behavior>
  <action>Expand `Accrue.Test.AppleServerEvidence` with checked-in test-only P-256 key/certificate material and compact-JWS builders for a genuinely valid, leaf-first, three-certificate chain plus one-variable hostile variants. Each purpose/CA/key-usage variant must remain a cryptographically valid path to the same configured test root so it reaches `validate_leaf_purpose/1` and proves the specific closed error rather than failing earlier as an invalid chain. Use real ES256 signatures encoded in the JWS raw `R || S` form expected by Production; fixture claims include valid bundle, environment, production app ID, signed date, and future expiry, and fixture helpers never expose their key/certificate bytes in verifier results. Tighten Production tuple/extension decoding only where the real corpus demonstrates a bug; preserve the exact Apple OIDs, configured-root trust, certificate-time, algorithm/header, signature, application-claim, and independent nested-JWS gates from D-06. Add a Repo tracer using Production as host configuration: valid evidence produces one bound Grant with the verified expiry, while each hostile purpose/usage input produces no durable effects. Correct `218-ADAPTER-ADMISSION.md` so the ordered x5c/root/time/purpose row cites the new named tests and does not claim evidence beyond what executes. Do not add a dependency or modify dependency files.</action>
  <acceptance_criteria>
    - A real valid Apple-purpose three-certificate ES256 transaction succeeds in Production and through configured Admission to a bounded canonical grant.
    - Every missing/wrong leaf or intermediate purpose, CA leaf, and missing/wrong signing-usage chain fails specifically as `invalid_certificate_purpose` with zero durable entitlement effects.
    - Root/path/time/signature/claim/nested-JWS failures remain independently classified and tested.
    - Verifier outputs, errors, persistence, jobs, telemetry, and inspected values contain no private key, certificate bytes, raw JWS, token, or provider transaction identifier.
    - `accrue/mix.exs` and `accrue/mix.lock` remain unchanged and the adapter admission evidence row matches the executed corpus.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_verifier_test.exs test/accrue/entitlements/apple_observation_tracer_test.exs &amp;&amp; git diff --quiet -- mix.exs mix.lock</automated></verify>
  <done>The selected production verifier has executable positive and hostile proof that only correctly purposed Apple ES256 signing chains can reach bounded grant admission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified Apple claims -> lifecycle evidence | Cryptographically valid but incomplete positive facts must not create unbounded access. |
| host repair caller -> lineage repair | Authorization and configured opaque evidence must precede any bind or re-verification effect. |
| JWS x5c chain -> normalized facts | A valid EC chain for another purpose must not impersonate Apple subscription evidence. |
| normalized evidence -> Intake/Projector | Only bounded, account-linked facts may create Observation, Grant, or revision state. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-G11-01 | Elevation of Privilege | Admission/lifecycle normalization | critical | mitigate | Tasks 1-2 require a verified provider bound at both conversion and final normalization seams and assert zero grant/revision effects on failure. |
| T-218-G11-02 | Spoofing | Production x5c purpose | high | mitigate | Task 3 proves exact leaf/intermediate purpose, non-CA leaf, and signing usage using valid and hostile real chains. |
| T-218-G11-03 | Elevation of Privilege | lineage repair setup | high | mitigate | Task 1 seeds through configured opaque evidence, authorizes before provider/database work, and keeps forged public values non-authoritative. |
| T-218-G11-04 | Information Disclosure | fixture and verifier outputs | high | mitigate | Task 3 confines test-only key/certificate material to fixtures and recursively asserts raw evidence is absent from results and durable/emitted channels. |
| T-218-G11-05 | Tampering | lifecycle ordering | high | mitigate | Task 2 preserves complete Apple ordering and runs delayed-positive, terminal, and survivor regression suites after the result-shape change. |
| T-218-SC | Tampering | package-manager installs | low | accept | No install occurs; the rejected package remains excluded and dependency files are verified unchanged. |
</threat_model>

<verification>
Run the three task gates, then execute the authoritative phase regression command twice: `cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_*_test.exs test/property/apple_convergence_property_test.exs`. Finish with `cd accrue &amp;&amp; mix compile --warnings-as-errors` and targeted formatting checks for the ten files listed in `files_modified`.
</verification>

<success_criteria>
- Active/renewal/grace/retry Apple evidence cannot reach the Projector without its lifecycle-specific verified access bound.
- The authorized unbound-lineage repair regression uses opaque configured evidence and the public forged-struct rejection remains green.
- Production certificate-purpose enforcement has real positive and hostile three-certificate ES256 behavior proof through durable Admission.
- The Phase 218 wildcard Apple suite is green and all AAPL-01 through AAPL-04 implicated regression predicates are executable.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-11-SUMMARY.md` when done.
</output>

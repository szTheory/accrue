---
phase: 218-apple-observation-and-repair
plan: 15
type: execute
wave: 12
depends_on: [218-14]
files_modified:
  - accrue/lib/accrue/entitlements/apple/notification_plug.ex
  - accrue/lib/accrue/router.ex
  - accrue/guides/webhooks.md
  - accrue/test/fixtures/apple/server_evidence.exs
  - accrue/test/accrue/entitlements/apple_notification_test.exs
autonomous: true
gap_closure: true
requirements: [AAPL-02, AAPL-03]
must_haves:
  truths:
    - "Per D-09, a non-empty Apple notification whose exact raw bytes were not captured returns retryable HTTP 503 before verifier or quarantine work and persists no Intake or reconciliation wakeup."
    - "Per D-06/D-20, deterministic decoded-byte corruption of each outer, transaction, and renewal ES256 signature independently returns `:invalid_signature` before wakeup or grant admission."
    - "The documented Apple route uses `Accrue.Webhook.CachingBodyReader.read_body/2`; missing, malformed, or empty capture is never silently substituted with another byte sequence."
    - "Flagged assumption AAPL-02 idempotency/concurrency: Plan 218-14 already proves byte-identical replay and parallel durable coalescing; this repair preserves those database identities and adds repeatable side-effect-free rejection for each byte-corrupted JWS."
    - "Flagged assumption AAPL-03 (unclassified): the host mounts the documented Apple endpoint in the dedicated raw-body pipeline and supplies verifier configuration and rate policy; Accrue fails retryably when that host contract is absent."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/notification_plug.ex"
      provides: "Fail-closed raw-body capture validation before verification or quarantine"
    - path: "accrue/lib/accrue/router.ex"
      provides: "Phoenix router macro and route contract for the Apple Notifications V2 plug"
    - path: "accrue/guides/webhooks.md"
      provides: "Host-facing Apple route and caching-body-reader wiring"
    - path: "accrue/test/fixtures/apple/server_evidence.exs"
      provides: "Deterministic decoded-signature-byte corruption helper"
    - path: "accrue/test/accrue/entitlements/apple_notification_test.exs"
      provides: "Route-capture, retry acknowledgement, and independent outer/nested rejection regressions"
  key_links:
    - from: "Phoenix Apple notification route"
      to: "conn.assigns[:raw_body]"
      via: "route-scoped Plug.Parsers body_reader using Accrue.Webhook.CachingBodyReader.read_body/2"
      pattern: "accrue_apple_notifications|CachingBodyReader"
    - from: "NotificationPlug.call/2"
      to: "Verifier.Production and Intake.quarantine_notification/6"
      via: "validated non-empty captured bytes only"
      pattern: "raw_body.*{:ok|:missing_raw_body}"
    - from: "decoded ES256 signature mutation"
      to: "Production.verify_notification/2"
      via: "Base.url_decode64!/2, deterministic byte flip, and Base.url_encode64/2"
      pattern: "tamper_signature|invalid_signature"
  assumptions:
    - requirement_id: AAPL-02
      category: idempotency
      status: resolved
      statement: "Repeated verification of the same byte-corrupted outer or nested JWS deterministically returns the same closed `:invalid_signature` result and has no durable side effect."
    - requirement_id: AAPL-02
      category: concurrency
      status: resolved
      statement: "Plan 218-14's parallel-delivery test remains the authority for concurrent valid delivery; this plan preserves its provider-event and wakeup database identities."
    - requirement_id: AAPL-03
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The host is responsible for mounting the Apple endpoint through the documented raw-body pipeline and for supplying verifier configuration and rate policy."
  prohibitions:
    - requirement_id: AAPL-03
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT acknowledge a delivered Apple notification as terminal when its exact request bytes were not captured for authentication."
    - requirement_id: AAPL-02
      category: privacy
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT persist or emit raw notification bodies, compact JWS values, account tokens, receipts, certificate material, or PII while diagnosing capture or signature failure."
---

<objective>
Close the notification-boundary and cryptographic-proof gaps so only exact captured request bytes reach Apple verification, and independently corrupted outer or nested signatures are deterministically rejected (AAPL-02/AAPL-03; D-06/D-08/D-09/D-20).

Purpose: Prevent a genuine delivery from being lost behind terminal acknowledgement and replace a Base64url textual mutation with proof that the signed bytes actually changed.
Output: Retry-safe capture handling, an explicit Apple router/docs contract, and deterministic independent signature rejection coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-PATTERNS.md
@.planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md
@.planning/phases/218-apple-observation-and-repair/218-VALIDATION.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@.planning/phases/218-apple-observation-and-repair/218-07-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-14-SUMMARY.md

<interfaces>
From `accrue/lib/accrue/webhook/caching_body_reader.ex`:
- `read_body(conn, opts)` mirrors `Plug.Conn.read_body/2` and prepends each captured binary chunk to `conn.assigns[:raw_body]`; consumers reverse and flatten the list.

From `accrue/lib/accrue/entitlements/apple/notification_plug.ex`:
- `call/2` currently reads `conn.assigns[:raw_body]`, enforces size/rate limits, invokes `verifier.verify_notification/2`, and acknowledges only a durable Intake result.
- `raw_body_or_empty/1` is the defective seam: any missing or non-binary/list assignment becomes empty bytes, and the generic `else` branch may terminally quarantine it.
- Existing response classes are bounded; this repair must use HTTP 503 with `:retryable` and must not add a queryable raw-body field.

From `accrue/lib/accrue/router.ex`:
- `accrue_webhook/2` demonstrates the macro style: a route macro forwards to a Plug and documents the required route-scoped caching-body-reader pipeline.

From `accrue/test/fixtures/apple/server_evidence.exs`:
- `tamper_signature/1` currently changes only the compact JWS final character, which may alter unused Base64url bits while decoding to identical signature bytes.
- `production_notification/1` accepts independently supplied `:transaction` and `:renewal` compact JWS values.
</interfaces>
</context>

## Planning gates

- Gap root causes: missing critical route contract plus an invalid negative-test technique; production verifier callbacks, identity model, and candidate/private fallback selection remain unchanged.
- Assumption delta: `no-change`. The primary abstraction remains `Apple.Verifier` with candidate/private fallback implementations behind it; no identity-model promotion is introduced.
- API coverage: preserve `COVERAGE.md`; this plan implements its Notifications V2, nested verification, acknowledgement/retry, request admission, deduplication, and privacy decisions without recreating the matrix.
- Schema-push detection: not applicable. No migration, Ecto schema, or persisted-field change is planned.
- Reversibility: all changes are internal Plug/router/documentation/test-fixture corrections behind existing persistence and verifier contracts; they are reversible and require no decision checkpoint.
- Specless exclusions: AAPL-01 and AAPL-05 manual-review probes are already verified by Plans 218-01/04/08/11 and are untouched. AAPL-04 idempotency/concurrency belongs to the independent reconciliation gap in Plan 218-16.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an uncaptured non-empty delivery to retry without persistence</name>
  <reversibility rating="reversible">The correction changes private request admission plus additive router/docs wiring while retaining the existing HTTP, verifier, Intake, and database contracts.</reversibility>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (AAPL-03 blocker and exact zero-persistence proof)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-08/D-09/D-10/D-12/D-19)
    - .planning/phases/218-apple-observation-and-repair/COVERAGE.md (notification acknowledgement, request admission, and raw-evidence opt-out)
    - .planning/phases/218-apple-observation-and-repair/218-07-SUMMARY.md (durable Intake/quarantine and response-class contract)
    - accrue/lib/accrue/entitlements/apple/notification_plug.ex (`call/2`, `raw_body/2`, `raw_body_or_empty/1`, quarantine branch)
    - accrue/lib/accrue/webhook/caching_body_reader.ex (`read_body/2` chunk order and assignment shape)
    - accrue/lib/accrue/router.ex (`accrue_webhook/2` macro and raw-body pipeline documentation idiom)
    - accrue/guides/webhooks.md (current Stripe-only route example and troubleshooting link)
    - accrue/test/accrue/entitlements/apple_notification_test.exs (`request/1`, Repo counts, response and privacy assertions)
  </read_first>
  <files>accrue/lib/accrue/entitlements/apple/notification_plug.ex, accrue/lib/accrue/router.ex, accrue/guides/webhooks.md, accrue/test/accrue/entitlements/apple_notification_test.exs</files>
  <behavior>
    - Test 1: a Plug.Test connection with a non-empty request body and no `:raw_body` assignment returns 503 and calls neither verifier, Intake, nor terminal quarantine; Intake, wakeup, Observation, and Grant counts remain zero.
    - Test 2: malformed capture shapes and an empty captured value follow the same retryable, storage-free path; valid binary and multi-chunk list captures preserve byte-exact order and continue through existing verification.
    - Test 3: the Apple route macro forwards to `Apple.NotificationPlug` with host options, and the documented route is inside the dedicated parser pipeline using `CachingBodyReader.read_body/2`.
    - Test 4: valid captured production delivery, duplicate/concurrent coalescing, terminal cryptographic quarantine, size/rate rejection, and injected persistence rollback retain Plan 218-14 behavior.
  </behavior>
  <action>Write failing Plug/Repo tests first using a non-empty `Plug.Test.conn/3` whose request body exists but whose assigns map has no raw capture. Refactor the private body extraction into a tagged result that accepts only a binary or a proper list of binary chunks, reverses chunk capture order, and returns a dedicated internal capture error for missing, invalid, or empty captured bytes. Handle that error in `call/2` before the verifier/rate/quarantine path by returning the existing generic 503 retryable response. Remove the empty-byte fallback from all error branches: only a successfully extracted exact body may be hashed, verified, or terminally quarantined. Add an `Accrue.Router` Apple Notifications V2 forwarding macro consistent with `accrue_webhook/2`, accepting the host-supplied NotificationPlug options without changing verifier callbacks or global configuration. Document a concrete Phoenix route mounted through `Plug.Parsers` with `Accrue.Webhook.CachingBodyReader.read_body/2`, including the 503 failure mode and existing troubleshooting diagnostic. Keep raw bytes route-scoped and absent from storage, telemetry, exceptions, logs, and returned values per D-08. Preserve the existing size-before-crypto, host rate-limiter, durable acknowledgement, duplicate identity, wakeup-only, and closed terminal-quarantine behavior per D-09/D-10/D-12.</action>
  <acceptance_criteria>
    - A non-empty request without the capture assignment returns 503 before verifier, quarantine, Intake, or wakeup work.
    - Missing, invalid, and empty capture never become a digest or a terminal 200; no raw evidence is persisted or emitted.
    - Binary and chunk-list capture preserve exact bytes and existing valid, invalid, duplicate, parallel, size/rate, and rollback behavior.
    - Router docs provide an executable Apple notification route using the existing caching body reader and host-supplied verifier configuration/rate policy.
    - No schema, dependency, verifier callback, ownership identity, provider-event identity, or Stripe lifecycle path changes.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_notification_test.exs test/accrue/webhook/plug_test.exs &amp;&amp; mix format --check-formatted lib/accrue/entitlements/apple/notification_plug.ex lib/accrue/router.ex test/accrue/entitlements/apple_notification_test.exs &amp;&amp; mix compile --warnings-as-errors</automated></verify>
  <done>An Apple delivery is authenticated only from exact route-captured bytes; absent or unusable capture produces retryable 503 with no durable side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove decoded-byte corruption closes each JWS boundary independently</name>
  <reversibility rating="reversible">This replaces a test-only corruption helper and strengthens assertions without changing Production verification behavior or public interfaces.</reversibility>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (AAPL-02 behavior-unverified item and reproduced false success)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-05/D-06/D-20)
    - .planning/phases/218-apple-observation-and-repair/218-14-SUMMARY.md (outer/data/nested verification structure to preserve)
    - accrue/test/fixtures/apple/server_evidence.exs (`tamper_signature/1`, compact JWS constructors, deterministic ES256 signature encoding)
    - accrue/test/accrue/entitlements/apple_notification_test.exs (`production V2 independently closes outer and nested signature tampering`)
    - accrue/lib/accrue/entitlements/apple/verifier/production.ex (`verify_notification/2`, nested verification, `:invalid_signature` result)
  </read_first>
  <files>accrue/test/fixtures/apple/server_evidence.exs, accrue/test/accrue/entitlements/apple_notification_test.exs</files>
  <behavior>
    - Test 1: the helper splits the compact JWS into exactly three segments, decodes the signature segment, flips a deterministic bit in a real signature byte, re-encodes without padding, and proves decoded original and corrupted signatures differ.
    - Test 2: corrupting only the outer signature returns `{:error, :invalid_signature}`, HTTP 200 only after bounded terminal quarantine, and zero wakeups, Observations, or Grants.
    - Test 3: separately corrupting only `signedTransactionInfo` or only `signedRenewalInfo` yields the same verifier result and no wakeup/grant while the other two JWS values remain valid.
    - Test 4: repeated runs of all three corruptions produce the same rejection and stable durable counts, while an unmodified production notification still reaches exactly one wakeup.
  </behavior>
  <action>Replace the final-character compact-text mutation with a deterministic signature-byte mutation: split the JWS into three segments, Base64url-decode the signature segment with no padding, require a non-empty decoded signature, flip one fixed bit in one decoded byte, re-encode without padding, and reconstruct the original protected-header and payload segments unchanged. Add a direct fixture assertion that the decoded signature bytes differ, so the regression cannot pass on a textual-only change. Exercise outer, transaction, and renewal corruption as three named cases through `Production.verify_notification/2` and the now capture-safe NotificationPlug. For each case assert the closed `:invalid_signature` result before wakeup/grant authority, bounded terminal acknowledgement only after quarantine persistence, and zero ReconciliationWakeup, Observation, and Grant rows. Repeat each corruption to establish deterministic rejection and stable idempotent counts. Retain Plan 218-14's valid production envelope, independent nested checks, current-clock certificate policy, closed error vocabulary, privacy rules, verifier behaviour, and candidate/private fallback abstraction. Do not modify Production code unless a newly deterministic failing test exposes a real verifier defect; if that occurs, keep the repair behind the unchanged behaviour and rerun the complete verifier corpus.</action>
  <acceptance_criteria>
    - The corruption helper mechanically proves that decoded signature bytes changed while protected header and payload segments remain byte-identical.
    - Outer, transaction, and renewal signature corruption are three independent assertions, each returning `:invalid_signature` before any wakeup, Observation, Grant, or revision.
    - Repeated corruptions remain deterministic and do not inflate durable terminal state; valid production delivery remains green.
    - No public verifier/config, dependency, schema, identity, lifecycle, or raw-evidence contract changes.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_notification_test.exs test/accrue/entitlements/apple_verifier_test.exs --seed 458442 &amp;&amp; mix test test/accrue/entitlements/apple_*_test.exs test/property/apple_convergence_property_test.exs &amp;&amp; mix format --check-formatted test/fixtures/apple/server_evidence.exs test/accrue/entitlements/apple_notification_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; git diff --quiet -- mix.exs mix.lock</automated></verify>
  <done>Independent decoded-byte changes to the outer, transaction, and renewal signatures are deterministically rejected before repair wakeup or grant authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| internet request stream -> route-captured bytes | Untrusted delivery must be captured byte-exactly before any signature or quarantine decision. |
| captured bytes -> verifier/quarantine | Missing or malformed capture must not be reinterpreted as a different authenticated payload. |
| authenticated outer JWS -> nested JWS | Transaction and renewal signatures remain independent cryptographic boundaries. |
| terminal disposition -> HTTP acknowledgement | Success must follow durable bounded quarantine; capture failure remains retryable. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-G15-01 | Spoofing | raw notification capture | critical | mitigate | Task 1 rejects absent, invalid, or empty capture before verifier/quarantine and proves a non-empty uncaptured delivery returns 503 with zero rows. |
| T-218-G15-02 | Tampering | outer/nested JWS negative proof | high | mitigate | Task 2 mutates decoded signature bytes and independently asserts outer, transaction, and renewal rejection. |
| T-218-G15-03 | Repudiation | Apple retry acknowledgement | high | mitigate | Task 1 preserves durable 200 semantics but maps capture failure to retryable 503 before persistence. |
| T-218-G15-04 | Information Disclosure | capture failure diagnostics | high | mitigate | Both tasks keep raw body/JWS/token/certificate/PII out of storage, telemetry, logs, exceptions, jobs, and results. |
| T-218-G15-05 | Denial of Service | notification ingress | medium | mitigate | Task 1 retains size admission before crypto and the host-provided rate policy; capture errors are storage-free. |
| T-218-SC | Tampering | package-manager installs | low | accept | No package install or dependency edit is planned; `git diff --quiet -- mix.exs mix.lock` guards the task. |
</threat_model>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Gap-closure item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Verified Apple evidence repairs safely without ownership or lifecycle confusion | 218-15 | COVERED | Exact capture gates verification; invalid evidence remains wakeup/grant-free. |
| REQ | AAPL-02 | Strict outer, transaction, and renewal verification | 218-15 | COVERED | Decoded-byte-changing corruption independently proves all three signature boundaries. |
| REQ | AAPL-03 | Invalid evidence is non-granting and retry/quarantine semantics do not lose deliveries | 218-15 | COVERED | Missing capture is retryable/storage-free; terminal crypto failures remain bounded and durable. |
| REQ | AAPL-01 | Opaque bind-once ownership | prior 218-01/04/11 | EXCLUDED | Verification report marks it satisfied; Task 1 asserts no ownership/Observation/Grant effect. |
| REQ | AAPL-04 | Scheduled status/history reconciliation | 218-16 | COVERED | Provider identifier regression is owned by the independent reconciliation plan. |
| REQ | AAPL-05 | Honest Apple management and deferrals | prior 218-08 | EXCLUDED | Verification report marks it satisfied and no management surface changes. |
| RESEARCH | — | Exact raw-body verification, independent nested JWS, retry-safe acknowledgement, bounded privacy | 218-15 | COVERED | Uses the established CachingBodyReader, Production, Plug, and Intake seams. |
| CONTEXT | D-05/D-06 | Verifier seam and independent strict JWS verification | 218-15 | COVERED | Abstraction unchanged; deterministic hostile proof closes the warning. |
| CONTEXT | D-08/D-09 | Raw-data exclusion and durable/retry-safe acknowledgement | 218-15 | COVERED | Uncaptured input never reaches digest/quarantine and returns 503. |
| CONTEXT | D-10/D-12/D-19/D-20 | Wakeup-only notifications, bounded diagnostics, deterministic fixtures | 218-15 | COVERED | Valid delivery behavior stays intact; hostile cases remain non-granting. |
| CONTEXT | D-01–D-04/D-07/D-11/D-13–D-18 | Ownership, disposition, cursor, lifecycle, projection, management, host outcomes | prior plans / 218-16 | EXCLUDED | Already verified or owned by the provider-identifier repair; no silent scope addition. |
| CONTEXT | Deferred Family Sharing, offer authoring, transfer, offline/mobile/UI work | — | NONE | EXCLUDED | Explicitly outside Phase 218 and not introduced here. |
</source_coverage_audit>

<verification>
Run both task gates, then the complete Apple-focused suite. Confirm the no-assignment test begins with a non-empty Plug request body yet returns 503 with zero Intake/wakeup/Observation/Grant rows and zero verifier invocation. Inspect the route/docs test for the exact caching-body-reader pipeline. Confirm each hostile compact JWS has different decoded signature bytes and independently returns `:invalid_signature`; ensure valid delivery and Plan 218-14 duplicate/concurrent behavior remain green. Verify the diff is limited to the five declared files and contains no dependency or schema changes.
</verification>

<success_criteria>
- Missing or invalid raw-body capture cannot be authenticated, quarantined, or acknowledged terminally.
- The Apple route contract makes byte-exact capture explicit and executable for Phoenix hosts.
- Outer, transaction, and renewal cryptographic rejection is deterministic and independently proven before wakeup or grant.
- Existing ownership, provider-event identity, durable acknowledgement, privacy, reconciliation, and provider-lifecycle contracts remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-15-SUMMARY.md` when done.
</output>

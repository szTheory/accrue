---
phase: 218-apple-observation-and-repair
plan: 16
type: execute
wave: 11
depends_on: [218-13]
files_modified:
  - accrue/lib/accrue/entitlements/apple/reconciliation.ex
  - accrue/test/accrue/entitlements/apple_reconciliation_test.exs
autonomous: true
gap_closure: true
requirements: [AAPL-04]
must_haves:
  truths:
    - "Per D-10/D-11, reconciliation locks the environment-qualified local lineage and uses `lineage.original_transaction_id` for both Apple status and Transaction History V2 requests."
    - "The local `lineage.id` remains the sole checkpoint, wakeup, Oban job, retry, and account-bound admission identity; the Apple identifier crosses only the provider client boundary."
    - "Production status/history URLs contain the URI-encoded Apple original transaction identifier and never contain the local lineage UUID."
    - "Flagged assumption AAPL-04 idempotency: rerunning the same lineage job retains one checkpoint keyed by local lineage id and reuses the same Apple original transaction identifier without duplicating observations or grants."
    - "Flagged assumption AAPL-04 concurrency/interruption: the existing lineage/checkpoint row locks, page cursor, and transaction rollback remain correctness authority; parallel or interrupted runs cannot advance a cursor or switch provider identity."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/apple/reconciliation.ex"
      provides: "Locked local-lineage lookup and explicit local/provider identity separation"
    - path: "accrue/test/accrue/entitlements/apple_reconciliation_test.exs"
      provides: "End-to-end Production URL, replay, concurrency, and checkpoint identity regression"
  key_links:
    - from: "Reconciliation.run/2 job args lineage_id"
      to: "Apple.Lineage"
      via: "repository row lock by local UUID and matching environment"
      pattern: "Lineage.*FOR UPDATE|original_transaction_id"
    - from: "locked lineage.original_transaction_id"
      to: "Client.subscription_statuses/3 and Client.transaction_history/5"
      via: "provider-boundary argument only"
      pattern: "subscription_statuses.*original_transaction_id|transaction_history.*original_transaction_id"
    - from: "locked lineage.id"
      to: "Checkpoint, Admission, retry and continuation"
      via: "unchanged local identity"
      pattern: "lock_checkpoint|admit_statuses|admit_transactions"
  assumptions:
    - requirement_id: AAPL-04
      category: idempotency
      status: resolved
      statement: "The same local lineage job always targets the stored immutable original transaction identifier and retains the existing checkpoint/observation uniqueness semantics on replay."
    - requirement_id: AAPL-04
      category: concurrency
      status: resolved
      statement: "The locked lineage and checkpoint rows serialize provider-identifier selection and cursor advancement; Oban uniqueness remains insertion deduplication only."
  prohibitions:
    - requirement_id: AAPL-04
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT send an Accrue lineage UUID, checkpoint UUID, account UUID, or job identifier as Apple's original transaction identifier."
    - requirement_id: AAPL-04
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT reinterpret reconciliation as ownership transfer, Apple lifecycle control, or authority to mutate Stripe subscriptions."
---

<objective>
Repair the reconciliation provider boundary so local lineage identity continues to own checkpoints and jobs while Apple's immutable original transaction identifier is used for every status/history request (AAPL-04; D-10/D-11/D-12/D-15).

Purpose: Make scheduled repair capable of reaching authoritative Apple status and history without weakening local serialization, idempotency, ownership, or cursor semantics.
Output: Explicit locked-lineage identity separation and Production URL regressions for both Apple endpoints.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-apple-observation-and-repair/218-CONTEXT.md
@.planning/phases/218-apple-observation-and-repair/218-RESEARCH.md
@.planning/phases/218-apple-observation-and-repair/218-PATTERNS.md
@.planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md
@.planning/phases/218-apple-observation-and-repair/218-VALIDATION.md
@.planning/phases/218-apple-observation-and-repair/COVERAGE.md
@.planning/phases/218-apple-observation-and-repair/218-05-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-12-SUMMARY.md
@.planning/phases/218-apple-observation-and-repair/218-13-SUMMARY.md

<interfaces>
From `accrue/lib/accrue/entitlements/apple/lineage.ex`:
- `%Apple.Lineage{id, environment, original_transaction_id, account_id, ...}` stores separate local UUID and immutable environment-qualified Apple identity.
- `lock_or_insert/4` demonstrates the required `FOR UPDATE` locking pattern by environment and original transaction identifier.

From `accrue/lib/accrue/entitlements/apple/reconciliation.ex`:
- `run/2` receives `%{lineage_id: local_uuid, environment: atom}`, opens one repository transaction, locks the Checkpoint by local identity, and currently passes that UUID into `reconcile_page/9`.
- `reconcile_page/9` currently uses the same argument for local admission and both Client calls; the repair must split these roles without changing checkpoint/cursor semantics.
- `admit_statuses/5` and `admit_transactions/5` consume local lineage identity for account binding and persistence.

From `accrue/lib/accrue/entitlements/apple/client.ex`:
- `Client.subscription_statuses(client, identifier, environment)` and `transaction_history(client, identifier, filters, revision, environment)` treat their identifier as Apple's endpoint path parameter.
- `Client.Production` URI-encodes that value into `/inApps/v1/subscriptions/{id}` and `/inApps/v2/history/{id}`.
</interfaces>
</context>

## Planning gates

- Gap root cause: local and provider identities were represented by one `lineage_id` variable across a provider boundary; Fake clients ignored the argument and hid the production defect.
- Assumption delta: `no-change`. `Apple.Verifier` remains the primary verification abstraction with the selected private implementation; this repair adds no identity-model promotion or public API.
- API coverage: preserve `COVERAGE.md`; the Get All Subscription Statuses and Transaction History V2 decisions remain visible and are exercised through Production URL capture.
- Schema-push detection: not applicable. `original_transaction_id` already exists and is immutable in the Lineage schema; no migration or schema file is planned.
- Reversibility: private argument separation and tests are reversible behind unchanged Client callbacks, job args, database keys, and public reconciliation facade.
- Specless exclusions: AAPL-01/AAPL-03/AAPL-05 unclassified probes and AAPL-02 idempotency/concurrency are already represented by executed plans or Plan 218-15; this plan changes only the AAPL-04 provider identifier path.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one local reconciliation job to Apple URLs with the stored provider identifier</name>
  <reversibility rating="reversible">The change separates two existing private identities without altering their persisted values, callbacks, job shapes, or public result contract.</reversibility>
  <read_first>
    - .planning/phases/218-apple-observation-and-repair/218-VERIFICATION.md (AAPL-04 blocker and exact production URL requirement)
    - .planning/phases/218-apple-observation-and-repair/218-CONTEXT.md (D-01, D-10/D-11/D-12, D-15/D-16, D-20)
    - .planning/phases/218-apple-observation-and-repair/COVERAGE.md (status authority, history V2, pagination, environment separation)
    - .planning/phases/218-apple-observation-and-repair/218-05-SUMMARY.md (checkpoint/job/wakeup identity and cursor contract)
    - .planning/phases/218-apple-observation-and-repair/218-12-SUMMARY.md (signed-date delayed reconciliation verification)
    - accrue/lib/accrue/entitlements/apple/lineage.ex (local UUID, original_transaction_id, environment, locking pattern)
    - accrue/lib/accrue/entitlements/apple/reconciliation.ex (`run/2`, `lock_checkpoint/3`, `reconcile_page/9`, admissions and cursor continuation)
    - accrue/lib/accrue/entitlements/apple/client.ex (Production URL interpolation and Client callback shapes)
    - accrue/test/accrue/entitlements/apple_reconciliation_test.exs (Production transport capture, checkpoint replay, worker and lifecycle tests)
  </read_first>
  <files>accrue/lib/accrue/entitlements/apple/reconciliation.ex, accrue/test/accrue/entitlements/apple_reconciliation_test.exs</files>
  <behavior>
    - Test 1: a persisted lineage whose UUID and `original_transaction_id` are deliberately distinct runs through `Reconciliation.run/2`; captured Production status and history URLs contain the URI-encoded original transaction identifier and contain no local UUID.
    - Test 2: Checkpoint lookup, admission, continuation scheduling, and returned Checkpoint retain `lineage.id`; no checkpoint keyed by the Apple identifier is created.
    - Test 3: rerunning the same job reuses the same local Checkpoint and Apple URL identity, retains completed/pending revision semantics, and does not duplicate Observation or Grant rows.
    - Test 4: parallel starts or an injected provider/interruption result serialize through the existing row locks, preserve one local Checkpoint, and never switch to the local UUID at the provider boundary.
    - Test 5: a missing lineage or environment mismatch fails closed before any Apple transport call and cannot synthesize an alternate provider identifier.
  </behavior>
  <action>Write an end-to-end Repo/Production-client tracer first. Seed a Lineage with a conspicuously different local UUID and original transaction identifier, use a transport callback to capture every URL, and return minimal valid status/history responses. In `Reconciliation.run/2`, load and lock the Lineage inside the existing repository transaction by the job's local `lineage_id` and asserted environment before locking/using the Checkpoint. Carry two explicitly named values thereafter: `lineage.id` for `lock_checkpoint`, status/history admission, continuation, retries, jobs, telemetry correlations, and all database relationships; `lineage.original_transaction_id` only for `Client.subscription_statuses/3` and `Client.transaction_history/5`. Keep the original identifier opaque and URI encoding inside `Client.Production`; do not expose it in public results or telemetry. Fail closed without provider I/O if the local lineage does not exist, the environment disagrees, or the stored provider identifier is unusable. Add replay, parallel/interruption, and URL-negative assertions that prove D-11's local checkpoint/cursor idempotency remains intact and both Production endpoints use Apple's identifier on every page. Preserve current-state/status authority, ascending history filters and revision reuse, final-page cursor commit, signed-date verification, bound-lineage admission, Projector sole-writer behavior, retry/backoff, outage bounds, and zero Apple-to-Stripe lifecycle paths.</action>
  <acceptance_criteria>
    - Both Production endpoint paths contain the encoded `lineage.original_transaction_id` and exclude `lineage.id` for a real persisted lineage run.
    - Checkpoint, job, wakeup, retry, continuation, admission, and database identities continue using local `lineage.id`.
    - Replay and concurrent/interrupted execution retain one Checkpoint and existing cursor/Observation/Grant idempotency.
    - Missing or mismatched lineage fails before transport and cannot fall back to another identifier.
    - Status authority, ascending history pagination, final cursor commit, lifecycle bounds, privacy, ownership, and provider isolation remain green.
    - No schema, migration, Client callback, job-argument, dependency, public API, or Stripe lifecycle change.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/apple_reconciliation_test.exs &amp;&amp; mix test test/accrue/entitlements/apple_*_test.exs test/property/apple_convergence_property_test.exs &amp;&amp; mix format --check-formatted lib/accrue/entitlements/apple/reconciliation.ex test/accrue/entitlements/apple_reconciliation_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; git diff --quiet -- mix.exs mix.lock</automated></verify>
  <done>Every scheduled Apple status/history repair keeps local identity for durable orchestration and uses the stored original transaction identifier exclusively across the provider boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Oban job args -> local Lineage row | A local UUID from durable work must resolve to exactly one environment-qualified locked lineage. |
| local Lineage -> Apple Client | Only the immutable original transaction identifier may cross into provider endpoint paths. |
| Apple responses -> reconciliation admission | Verified status/history facts may affect only the bound local lineage through existing admission and Projector rules. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-218-G16-01 | Tampering | reconciliation provider identifier | critical | mitigate | Task 1 locks Lineage and passes only stored `original_transaction_id` to both Production calls, with URL assertions excluding local UUID. |
| T-218-G16-02 | Spoofing | job lineage/environment | high | mitigate | Task 1 requires a matching environment-qualified local row before any provider transport call. |
| T-218-G16-03 | Repudiation | checkpoint replay/concurrency | high | mitigate | Task 1 retains local checkpoint identity and row locks and proves replay/parallel execution converges without identifier drift. |
| T-218-G16-04 | Elevation of Privilege | provider evidence admission | high | mitigate | Existing bound-lineage verification and Projector sole-writer path remain unchanged and are covered by the full Apple suite. |
| T-218-G16-05 | Information Disclosure | Apple/local identifiers | medium | mitigate | Provider identity is used only in the private request path and is excluded from public result/telemetry changes. |
| T-218-SC | Tampering | package-manager installs | low | accept | No package install or dependency change is planned; the verification gate checks dependency files remain unchanged. |
</threat_model>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Gap-closure item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Verified Apple evidence repairs itself without ownership/provider confusion | 218-16 | COVERED | Restores authoritative provider repair while preserving local ownership and lifecycle isolation. |
| REQ | AAPL-04 | Scheduled status/history repair uses authoritative Apple identity and preserves lifecycle bounds | 218-16 | COVERED | Production URL tracer plus replay/concurrency/cursor assertions. |
| REQ | AAPL-02 | Strict notification/nested verification | 218-15 / prior 218-12/14 | EXCLUDED | Independent verification gap is owned elsewhere; signed-date reconciliation verification remains unchanged. |
| REQ | AAPL-03 | Convergent quarantine/retry | 218-15 / prior plans | EXCLUDED | Notification capture gap is owned elsewhere; reconciliation retry/cursor semantics are regression-checked here. |
| REQ | AAPL-01 | Opaque bind-once ownership | prior 218-01/04/11 | EXCLUDED | Verification report marks it satisfied; locked lineage is read, never rebound. |
| REQ | AAPL-05 | Honest external management and deferrals | prior 218-08 | EXCLUDED | Verification report marks it satisfied; no lifecycle-management surface changes. |
| RESEARCH | — | Status as current authority, ascending history V2, local checkpoint identity, provider/environment separation | 218-16 | COVERED | Uses existing Lineage, Checkpoint, Client.Production, admission, and cursor seams. |
| CONTEXT | D-01 | Durable environment-qualified Apple lineage identity | 218-16 | COVERED | Reads/locks existing Lineage; no identity-model change. |
| CONTEXT | D-10/D-11/D-12 | Hybrid repair, cursor idempotency, and coalesced jobs | 218-16 | COVERED | Provider ID is separated without changing local orchestration/cursor identity. |
| CONTEXT | D-13–D-16/D-19/D-20 | Outage bounds, lifecycle normalization, Projector authority, provider isolation, diagnostics/tests | 218-16 | COVERED | Full suite and URL tracer preserve these contracts. |
| CONTEXT | D-02–D-09/D-17/D-18 | Claiming, verifier/quarantine, host results and copy | prior plans / 218-15 | EXCLUDED | Already verified or owned by notification repair; this plan has no admission/result vocabulary change. |
| CONTEXT | Deferred Family Sharing, offer authoring, transfer, offline/mobile/UI work | — | NONE | EXCLUDED | Explicitly outside Phase 218 and not introduced here. |
</source_coverage_audit>

<verification>
Run the focused reconciliation gate and full Apple suite. Inspect captured transport URLs from an end-to-end `Reconciliation.run/2` using a persisted lineage whose local and provider identifiers are intentionally distinct. Confirm both endpoint paths include only the encoded Apple identifier, while the returned/reloaded Checkpoint, jobs, continuation, admissions, and rows use local lineage id. Exercise replay plus concurrent/interrupted paths, missing/mismatched lineage fail-closed behavior, pagination/filter reuse, and final cursor commit. Confirm the diff is limited to the two declared files with no schema, dependency, callback, public API, ownership, or Stripe lifecycle change.
</verification>

<success_criteria>
- Scheduled reconciliation can call authoritative Apple status and history endpoints with the correct original transaction identifier.
- Local lineage UUID remains the durable checkpoint/job/wakeup/admission identity across replay, concurrency, and interruption.
- Invalid local identity or environment fails before provider I/O.
- Existing verification, lifecycle, cursor, privacy, ownership, Projector, and externally-managed provider contracts remain intact.
</success_criteria>

<output>
Create `.planning/phases/218-apple-observation-and-repair/218-16-SUMMARY.md` when done.
</output>

---
phase: 219-offline-study-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/mix.exs
  - accrue/mix.lock
  - accrue/lib/accrue/entitlements/offline.ex
  - accrue/lib/accrue/entitlements/offline/proof.ex
  - accrue/lib/accrue/entitlements/offline/key_provider.ex
  - accrue/test/accrue/entitlements/offline_protocol_test.exs
autonomous: true
requirements: [OFF-01, OFF-06]
must_haves:
  truths:
    - "Per D-07/D-09/D-12, `Accrue.Entitlements.Offline.verify/3` accepts one compact proof only after strict ES256, fixed type/version/issuer/audience, local `kid`, duplicate-member, binding, time, disposition, and high-water validation."
    - "Per D-10/D-11, public verification-key rendering contains only active/retained P-256 public keys with stable `kid`; signing remains behind a host provider, no private key crosses the facade, and the renderer accepts explicit retention requirements that Plan 219-04 derives from durable issuance history."
    - "OFF-01 verification succeeds with the published JWKS alone and fails when the signing capability is absent, proving the registered device does not possess a minting secret."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/offline.ex"
      provides: "Additive public offline facade"
      exports: ["verify/3", "verification_keys/1"]
    - path: "accrue/lib/accrue/entitlements/offline/proof.ex"
      provides: "Versioned proof values, strict parser, verifier, and bounded decisions"
    - path: "accrue/lib/accrue/entitlements/offline/key_provider.ex"
      provides: "Host-owned signing and public-key behaviour"
    - path: "accrue/test/accrue/entitlements/offline_protocol_test.exs"
      provides: "Public-key-only production protocol tracer and negative security tests"
  key_links:
    - from: "accrue/lib/accrue/entitlements/offline.ex"
      to: "accrue/lib/accrue/entitlements/offline/proof.ex"
      via: "typed verification context and bounded decision"
      pattern: "Proof.verify"
    - from: "accrue/lib/accrue/entitlements/offline/proof.ex"
      to: "accrue/lib/accrue/entitlements/offline/key_provider.ex"
      via: "local `kid` lookup; never token-directed retrieval"
      pattern: "public_keys|verification_keys"
  assumptions:
    - requirement_id: OFF-01
      category: unclassified
      status: unresolved
      statement: "Public-key-only verification in the Elixir facade and checked-in Swift consumer is the independently executable registered-device contract; this phase does not claim Crosswake runtime or physical-device feasibility."
  prohibitions: []
---

<objective>
Trace one production compact ES256 allow proof through the additive public facade to a four-state-capable verified decision using public material only, per D-07 and D-09 through D-12.

Purpose: Prove the cross-language cryptographic and authority boundary before adding policy, persistence, issuance, and reconnect.
Output: JOSE dependency lock, public Offline facade, proof model/verifier, key-provider behaviour, JWKS renderer, and protocol tracer tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-offline-study-contract/219-CONTEXT.md
@.planning/phases/219-offline-study-contract/219-RESEARCH.md
@.planning/phases/219-offline-study-contract/219-PATTERNS.md
@.planning/phases/219-offline-study-contract/COVERAGE.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-05-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-12-SUMMARY.md
@accrue/test/support/entitlements/offline_golden_vector_verifier.ex

<interfaces>
- Existing seed: `OfflineGoldenVectorVerifier.verify(compact, key, context \\ %{})` stages compact parsing, ES256 signature, claims, disposition, and high-water checks.
- New public boundary: `Offline.verify(compact, %Proof.VerificationContext{}, opts \\ []) :: {:ok, Proof.Decision.t()}`; invalid input is a successful bounded decision, not leaked JOSE/provider detail.
- New host boundary: `KeyProvider.sign(signing_input, opts)` and `KeyProvider.public_keys(opts)` return tagged values; provider implementations remain host-owned.
</interfaces>
</context>

## Artifacts this phase produces

- Required dependency `{:jose, "~> 1.11"}` and resolved `mix.lock` entries; no CLI flags or environment variables.
- `Accrue.Entitlements.Offline.verify/3` and `verification_keys/1`.
- `%Accrue.Entitlements.Offline.Proof.VerificationContext{issuer, audience, account_subject, installation_id, device_thumbprint, now, clock_high_water, accepted_revision, accepted_disposition, accepted_iat, accepted_fresh_until}`.
- `%Accrue.Entitlements.Offline.Proof.Claims{version, issuer, audience, token_id, subject, confirmation, revision, issued_at, not_before, fresh_until, expires_at, disposition, plans, features, quantities, denial_reason}`.
- `%Accrue.Entitlements.Offline.Proof.Decision{state, reason, next_action, claims}` with fields present even though Plan 219-02 completes policy derivation.
- `Accrue.Entitlements.Offline.KeyProvider` callbacks `sign/2` and `public_keys/1`; pure JWKS output contains only `kty`, `crv`, `x`, `y`, `kid`, `use`, and `alg`.
- No new database field, route, external API capability, or CLI flag.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verify one production-profile allow proof through the public facade</name>
  <precondition>Phase 215's offline golden vector and test-only public P-256 key exist and `cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_golden_vectors_test.exs` passes.</precondition>
  <files>accrue/mix.exs, accrue/mix.lock, accrue/lib/accrue/entitlements/offline.ex, accrue/lib/accrue/entitlements/offline/proof.ex, accrue/test/accrue/entitlements/offline_protocol_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-01/D-06/D-07/D-09/D-12/D-14)
    - .planning/phases/219-offline-study-contract/219-RESEARCH.md (strict JOSE stages and package legitimacy audit)
    - accrue/test/support/entitlements/offline_golden_vector_verifier.ex (staged seed; do not copy its legacy claim names or handwritten ECDSA)
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json (one current signed tracer input)
    - accrue/lib/accrue/entitlements.ex (tagged facade and private telemetry conventions)
    - accrue/mix.exs (required dependency section)
    - accrue/mix.lock (existing dependency lock graph to update deterministically)
  </read_first>
  <behavior>
    - Test 1: a compact JWS with protected `alg=ES256`, `typ=accrue-entitlement-proof+jwt`, stable `kid`, version `v1.59`, exact issuer/audience/account/installation/thumbprint, valid times, revision 1, and allow entitlements returns `fresh/ok` through `Offline.verify/3` using only a public JWK.
    - Test 2: the same test has no private `d` member or signing callback in the verification context.
    - Test 3: wrong algorithm/type/issuer/audience, unknown key, invalid signature, malformed compact input, duplicate sensitive JSON members, remote-key headers, or unknown critical behavior returns `invalid` with the exact bounded D-06 reason.
  </behavior>
  <action>Add the approved required `:jose ~> 1.11` dependency and lock it. D-09 is already a locked, human-approved one-way wire decision in `219-CONTEXT.md`, so this task implements that exact contract and does not reopen it. Write the failing tracer before implementation. Define the versioned D-09 protected header and claims with integer NumericDate seconds, bounded strings/collections, normalized sorted plans/features and deterministic quantity keys. Parse exactly three compact segments, reject duplicate security-sensitive members in raw protected/payload JSON, reject `jku`, `x5u`, embedded `jwk`, nonempty/unknown `crit`, and any nonlocal key selection, then call `JOSE.JWS.verify_strict/3` with only `ES256`. Validate fixed type/version/issuer/audience, P-256 key shape/use, account/installation/confirmation thumbprint binding, time and monotonic context before classification. Map every failure into D-06; never return JOSE structs, decoded unverified claims, proof bytes, or library messages. Expose only the thin D-07 facade with allowlisted private telemetry metadata.</action>
  <acceptance_criteria>
    - `mix.lock` resolves the approved `jose` package and no unreviewed package is added.
    - The happy path crosses public facade, strict JOSE verification, semantic validation, and bounded decision with no private material.
    - Every D-12 confusion/remote-key/duplicate-member case fails closed with a D-06 reason.
    - Existing `Accrue.Entitlements` boolean/scalar gates are untouched per D-08.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix deps.get &amp;&amp; mix test test/accrue/entitlements/offline_protocol_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs</automated></verify>
  <done>A registered-device consumer can verify one production-profile compact proof using public material only, with strict local policy and bounded failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the host key-provider and public-only JWKS boundary</name>
  <reversibility rating="costly">D-10/D-11 make provider callbacks and JWKS shape host integration contracts, but implementations remain replaceable behind the behaviour.</reversibility>
  <files>accrue/lib/accrue/entitlements/offline/key_provider.ex, accrue/lib/accrue/entitlements/offline.ex, accrue/test/accrue/entitlements/offline_protocol_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-10/D-11/D-14)
    - accrue/lib/accrue/repo.ex (host-resolved dependency convention)
    - accrue/lib/accrue/entitlements/apple/verifier.ex (host cryptographic boundary analog)
    - accrue/lib/accrue/entitlements/offline.ex (Task 1 facade)
    - accrue/test/accrue/entitlements/offline_protocol_test.exs (Task 1 test contract)
  </read_first>
  <behavior>
    - Test 1: `verification_keys/1` renders deterministic `{"keys": [...]}` ordered by `kid` and strips private/unknown members recursively.
    - Test 2: two overlapping public keys verify proofs by their own `kid`; an unknown or retired key is `invalid/unknown_key` without remote fetch.
    - Test 3: provider output containing `d`, symmetric `k`, a non-P-256 curve, duplicate `kid`, wrong use/alg, or unbounded metadata returns `{:error, :config_invalid}` and no JWKS.
    - Test 4: the pure renderer rejects omission of any `kid` present in supplied retention requirements and accepts its removal only when that requirement is explicitly eligible; Plan 219-04 supplies requirements derived from real issuance rows.
  </behavior>
  <action>Define `KeyProvider` callback types and resolve the configured module at call time without starting a process. Keep `sign/2` internal-to-issuance capability and expose only deterministic public-key rendering through `Offline.verification_keys/1`. Validate each public JWK as EC/P-256 with stable bounded `kid`, `use: "sig"`, `alg: "ES256"`, 32-byte decoded coordinates, unique key IDs, and no private/symmetric members. Sort keys by `kid`; make the pure renderer accept a bounded per-`kid` retention-requirement map and return `{:error, :config_invalid}` when a required old key is absent. This task proves the renderer seam with supplied requirements; Plan 219-04 derives them from actual Issuance rows and wires the facade, so D-11 cannot be satisfied by arbitrary provider overlap alone. Do not add a route or Plug in this task: D-10 leaves host mounting optional and the pure renderer is the owned protocol surface.</action>
  <acceptance_criteria>
    - JWKS is deterministic, cacheable data and contains only the seven allowlisted public members.
    - Private signing capability is never reachable through `verification_keys/1`, inspect, logs, telemetry, fixtures, or errors.
    - Rotation overlap and unknown-key failure are executable without a remote-key URL.
    - A supplied not-yet-eligible retention requirement makes early key omission configuration-invalid rather than publishing an unsafe JWKS.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_protocol_test.exs</automated></verify>
  <done>Hosts can supply signing custody while devices receive only strict, deterministic public verification material.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| compact JWS + cached JWKS -> verifier | Attacker-controlled bytes cross into authorization parsing and cryptography. |
| host key provider -> public facade | Private signing capability must be separated from public verification output. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-219-01 | Tampering | proof verifier | high | mitigate | Task 1 fixes ES256/type/profile, rejects duplicate/critical/remote-key input, and verifies signature before claims/classification. |
| T-219-02 | Spoofing | key selection/binding | high | mitigate | Tasks 1-2 use only local `kid` lookup and exact account/installation/RFC-7638 thumbprint binding. |
| T-219-03 | Information Disclosure | JWKS/provider boundary | high | mitigate | Task 2 recursively rejects private/symmetric members and tests public output/log/inspect surfaces. |
| T-219-SC | Tampering | Hex package install | high | mitigate | Research's Package Legitimacy Audit approves `jose`; Task 1 pins `~> 1.11`, commits the lockfile, and runs focused tests. |
</threat_model>

<verification>
Run `cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_protocol_test.exs test/accrue/entitlements/offline_golden_vectors_test.exs` and `mix format --check-formatted` on plan-owned files.
</verification>

<success_criteria>
- Public-key-only strict ES256 verification succeeds through the new facade.
- Confusion, remote-key, malformed, duplicate-member, wrong-binding, and private-key leakage paths fail closed.
- Existing server entitlement gates remain byte-compatible in signature and return shape.
</success_criteria>

<output>
Create `.planning/phases/219-offline-study-contract/219-01-SUMMARY.md` when done.
</output>

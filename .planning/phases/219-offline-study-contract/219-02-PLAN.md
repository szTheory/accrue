---
phase: 219-offline-study-contract
plan: 02
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - accrue/lib/accrue/entitlements/offline.ex
  - accrue/lib/accrue/entitlements/offline/proof.ex
  - accrue/test/accrue/entitlements/offline_test.exs
autonomous: true
requirements: [OFF-02, OFF-03, OFF-04]
must_haves:
  truths:
    - "Per D-01/D-02, only the four states `fresh|stale_offline|denied|invalid` exist; `reconnect_required` is an action and fresh exposes only normalized signed entitlements."
    - "Per D-03/D-04, equality at `fresh_until` becomes stale, equality at explicit `exp` becomes `invalid/hard_expired`, and no independent 72-hour post-freshness cutoff exists."
    - "Per D-03/D-05, stale permits only downloaded lesson reads and local-progress reads/writes; denied/invalid preserve app shell/local data but permit no entitlement-gated study or value expansion."
    - "Per D-06/D-08, reasons are closed/bounded and existing boolean/scalar server gates keep their signatures, return types, and connectivity-independent semantics."
    - "OFF-02 edge contract: null/malformed temporal fields are invalid; NumericDate values are integers; stable sorting and exact equality/one-second-before/one-second-after tests define boundary, adjacency, empty, ordering, and precision behavior."
  artifacts:
    - path: "accrue/lib/accrue/entitlements/offline/proof.ex"
      provides: "Four-state classification, action policy, guidance seeds, and monotonic ordering"
    - path: "accrue/lib/accrue/entitlements/offline.ex"
      provides: "Public action-policy and guidance delegates"
      exports: ["action_policy/2", "guidance/1"]
    - path: "accrue/test/accrue/entitlements/offline_test.exs"
      provides: "Boundary/property/action-policy and legacy compatibility tests"
  key_links:
    - from: "accrue/lib/accrue/entitlements/offline/proof.ex"
      to: "verified Claims"
      via: "classification executes only after Plan 219-01 verification succeeds"
      pattern: "classify|action_policy"
    - from: "accrue/lib/accrue/entitlements/offline.ex"
      to: "accrue/lib/accrue/entitlements.ex"
      via: "additive boundary; no gate calls or return-shape changes"
      pattern: "Offline"
  assumptions:
    - requirement_id: OFF-03
      category: unclassified
      status: unresolved
      statement: "The closed action vocabulary in this plan is the complete core policy seam; host-specific actions default to value-expanding/reconnect-required unless explicitly mapped to downloaded study or local progress."
    - requirement_id: OFF-04
      category: unclassified
      status: unresolved
      statement: "Typed state, reason, action, and guidance values are sufficient for Phase 220/hosts to render policy without altering existing entitlement gates or requiring Phase 219 UI."
  prohibitions:
    - requirement_id: OFF-03
      category: safety
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT delete, erase, invalidate, or hide host-owned downloaded lessons or unsynced local learner progress solely because a proof becomes stale, denied, invalid, revoked, or unavailable."
    - requirement_id: OFF-04
      category: transparency
      status: unresolved
      verification: null
      flagged: true
      statement: "MUST NOT use shaming, punitive, or guaranteed-restoration language for stale, denied, invalid, or reconnect guidance."
---

<objective>
Complete the exact four-state continuity and host action-policy contract, including the 30-day revalidation boundary, explicit hard expiry, closed reasons, guidance seeds, and legacy gate compatibility per D-01 through D-08 and D-23/D-24.

Purpose: Turn verified proof claims into safe learner continuity without creating a connectivity-sensitive server authorization path.
Output: State classifier, action-policy values, guidance seeds, boundary/property tests, and compatibility regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-offline-study-contract/219-CONTEXT.md
@.planning/phases/219-offline-study-contract/219-VALIDATION.md
@.planning/phases/219-offline-study-contract/219-01-SUMMARY.md
@.planning/phases/215-research-contracts-and-crosswake-feasibility/215-12-SUMMARY.md
@accrue/lib/accrue/entitlements.ex
@accrue/lib/accrue/entitlements/offline/proof.ex

<interfaces>
- `Offline.verify/3` from Plan 219-01 returns `%Proof.Decision{state, reason, next_action, claims}` only after strict verification.
- Stable server gates are `Accrue.Entitlements.entitled?/3`, `has_active_plan?/3`, `features_for/1`, and `entitlement_quantity/2`; tests consume them but production signatures remain unchanged.
</interfaces>
</context>

## Artifacts this phase produces

- Closed atoms `fresh`, `stale_offline`, `denied`, `invalid`; closed D-06 reasons; closed next actions `none`, `reconnect_required`, `access_unavailable`, `check_access`.
- Closed action atoms `read_downloaded_lesson`, `read_local_progress`, `write_local_progress`, `download_premium`, `enroll`, `export`, `purchase`, `mutate_account`, `mutate_rail`, and `other_value_expansion`; unknown actions default to reconnect-required.
- `%Proof.ActionPolicy{action, allowed, next_action, guidance_key}` and `%Proof.Guidance{key, text, action_label}`.
- `Offline.action_policy/2` and `Offline.guidance/1`; no database fields, routes, environment variables, or CLI flags.
- `offline_test.exs` with StreamData coverage for time/order boundaries and exhaustive state/action matrices.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock four-state time, order, and empty-snapshot semantics</name>
  <reversibility rating="one-way">D-01's state vocabulary is consumed by fixtures, mobile pattern matches, support copy, and Phase 220; changing it breaks the public contract.</reversibility>
  <files>accrue/lib/accrue/entitlements/offline/proof.ex, accrue/test/accrue/entitlements/offline_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-01 through D-06)
    - .planning/phases/219-offline-study-contract/219-VALIDATION.md (OFF-02/OFF-04 Wave-0 gaps)
    - accrue/lib/accrue/entitlements/offline/proof.ex (Plan 219-01 verified values)
    - accrue/lib/accrue/entitlements/snapshot.ex (deterministic entitlements and authorization bounds)
    - accrue/test/property/entitlement_projection_property_test.exs (StreamData conventions)
  </read_first>
  <behavior>
    - Test 1: allow at `fresh_until - 1` is fresh, exactly at/after `fresh_until` is stale, and remains stale beyond 72 hours until explicit `exp`.
    - Test 2: exactly at/after `exp` is invalid/hard_expired; `exp` earlier than `fresh_until`, null/malformed required times, fractional NumericDate, future `nbf`, or clock rollback fail closed.
    - Test 3: a verified deny is denied/signed_denial and equal-revision deny outranks allow; lower revision, older `iat`, or regressed freshness is invalid/superseded or clock_rollback without replacing high-water.
    - Test 4: plans/features are stably sorted, quantities have deterministic keys and positive integers, a server snapshot with no effective entitlement is issued as denial rather than an empty allow, and malformed/null entitlement members are invalid.
  </behavior>
  <action>Write the complete boundary/property matrix first. Implement classification only on verified `Proof.Claims`: deny precedence first, then explicit hard expiry, then freshness, with exact integer comparisons and no derived post-freshness expiry. Treat `fresh_until` as the lesser of issue time plus 30 days and an earlier known authorization bound when issuing later, but make verification consume the signed value rather than re-derive provider state. Centralize candidate ordering as `{revision, deny_precedence, iat, fresh_until}` with higher revision winning, deny winning at equal revision, and nondecreasing issuance/freshness required. Reject malformed/null fields and empty allow authority; keep denial metadata bounded. Ensure every returned state/reason belongs to D-01/D-06.</action>
  <acceptance_criteria>
    - Boundary tests cover one second before, exact equality, and one second after both freshness and expiry thresholds.
    - A time beyond freshness plus 72 hours remains stale when no earlier explicit expiry exists.
    - Empty, singleton, duplicate, permuted, equal-order, rollback, and precision cases have stable outcomes.
    - There is no fifth proof state or unbounded public reason.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_test.exs</automated></verify>
  <done>Every verified proof maps deterministically to exactly one closed state with exact time/order behavior and no independent 72-hour cutoff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce continuity-only stale actions and legacy gate compatibility</name>
  <reversibility rating="costly">D-03/D-23 action and guidance vocabularies feed host UI and support behavior, but remain additive typed values.</reversibility>
  <files>accrue/lib/accrue/entitlements/offline.ex, accrue/lib/accrue/entitlements/offline/proof.ex, accrue/test/accrue/entitlements/offline_test.exs</files>
  <read_first>
    - .planning/phases/219-offline-study-contract/219-CONTEXT.md (D-03/D-05/D-08/D-23/D-24)
    - accrue/lib/accrue/entitlements.ex (`entitled?/3`, `has_active_plan?/3`, `features_for/1`, `entitlement_quantity/2`)
    - accrue/test/accrue/entitlements_test.exs (existing public gate assertions)
    - accrue/lib/accrue/entitlements/offline.ex (public delegates modified by this task)
    - accrue/lib/accrue/entitlements/offline/proof.ex (Task 1 states/order)
    - brandbook/voice.md and brandbook/copy.md (measured next-action language)
  </read_first>
  <behavior>
    - Test 1: fresh permits actions only when their required plan/feature/quantity is in signed normalized claims.
    - Test 2: stale permits exactly downloaded-lesson read plus local-progress read/write and rejects every enumerated expansion and unknown action with reconnect-required.
    - Test 3: denied/invalid permit app shell/local data handling only, reject entitlement-gated study/expansion, and never emit a delete-local-data action.
    - Test 4: stale, invalid, and denied guidance matches D-23; no copy guarantees reconnect restores access.
    - Test 5: existing server gates still return boolean/list/integer values independent of an Offline decision or connectivity option.
  </behavior>
  <action>Define exhaustive closed action-policy values and public delegates. Unknown actions take the restrictive value-expanding path. For fresh, require the corresponding signed plan/feature/quantity rather than treating fresh as universal access. For stale, allow only the three D-03 continuity actions. For denied/invalid, keep host-owned local data outside entitlement deletion and reject all gated actions. Return D-23 guidance keys/text/action labels as typed seeds only; do not render UI per D-24. Add regression tests against the existing facade using the Fake resolver and prove no Offline state/network option influences the boolean/scalar gates per D-08.</action>
  <acceptance_criteria>
    - The state-by-action table is exhaustive and unknown actions fail closed.
    - Stale restrictions exactly match OFF-03 and preserve downloaded study/local progress.
    - Guidance states learner job and next action without proof/provider internals, shame, or restoration promises.
    - Existing gate signatures and scalar/boolean/list return types remain unchanged.
  </acceptance_criteria>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_test.exs test/accrue/entitlements_test.exs</automated></verify>
  <done>Hosts can distinguish and render four offline states while enforcing continuity-only stale study and preserving all existing server gate contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified claims -> client action policy | Signed authority must not silently expand after freshness. |
| offline policy -> legacy server gates | Client/network state must not become server authorization input. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-219-04 | Elevation of Privilege | stale action policy | high | mitigate | Task 2 exhaustively permits only downloaded study/local progress and makes unknown actions reconnect-required. |
| T-219-05 | Tampering | proof high-water/time | high | mitigate | Task 1 tests exact boundaries, denial precedence, rollback, future time, and monotonic ordering. |
| T-219-06 | Elevation of Privilege | legacy gates | high | mitigate | Task 2 proves Offline/connectivity never enters stable boolean/scalar gate decisions. |
| T-219-07 | Repudiation | guidance contract | low | accept | Typed reason/guidance values are deterministic; Phase 220 owns rendered/audited operator surfaces. |
</threat_model>

<verification>
Run `cd accrue &amp;&amp; mix test test/accrue/entitlements/offline_test.exs test/accrue/entitlements_test.exs test/accrue/entitlements/offline_protocol_test.exs` and focused format checks.
</verification>

<success_criteria>
- Exact freshness/expiry/order boundaries and no-72-hour behavior are executable.
- Stale continuity is restricted to downloaded lessons and local progress.
- Four states/reasons/actions are typed and existing gates remain compatible.
</success_criteria>

<output>
Create `.planning/phases/219-offline-study-contract/219-02-SUMMARY.md` when done.
</output>

---
phase: 220-first-adopter-proof-and-release-gates
plan: 04
type: execute
wave: 2
depends_on: [220-01]
files_modified:
  - accrue/priv/entitlements/v1.59-public-contract.json
  - accrue/lib/accrue/entitlements/reference_scenarios/markdown.ex
  - accrue/lib/mix/tasks/accrue.entitlements.reference_scenarios.ex
  - examples/accrue_host/docs/capability-limits-matrix.md
  - accrue/test/accrue/entitlements/reference_scenarios_test.exs
  - scripts/ci/verify_reference_scenario_contract.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [PROOF-05]
must_haves:
  truths:
    - "Per D-09, one versioned public-contract fixture generates exact supported/unsupported capability and compatibility assertions while prose remains hand-authored."
    - "Per D-10, generated facts state additive legacy compatibility, externally managed Apple, no cross-rail finance/lifecycle mutation, stale-study-only continuity, and strict privacy limits."
    - "Per D-11, the generated matrix exposes scenario IDs and evidence lanes and points to one deterministic verification command."
    - "Per D-03, the generator and CI gate preserve `feasibility_blocked` until the tracer contains the required bridge plus physical-device evidence."
    - "A deterministic check mode rejects schema drift, stale generated output, contradictory claims, unsupported lane promotion, missing scenarios, and private-data fields."
  artifacts:
    - path: "accrue/priv/entitlements/v1.59-public-contract.json"
      provides: "Versioned exact public support/limit/lane facts"
    - path: "accrue/lib/accrue/entitlements/reference_scenarios/markdown.ex"
      provides: "Deterministic fixture-to-matrix generator and check mode"
    - path: "examples/accrue_host/docs/capability-limits-matrix.md"
      provides: "Generated first-adopter capability/compatibility reference"
    - path: "scripts/ci/verify_reference_scenario_contract.sh"
      provides: "Fail-closed fixture/scenario/generated-output/public-claim gate"
  key_links:
    - from: "accrue/priv/entitlements/v1.59-public-contract.json"
      to: "examples/accrue_host/docs/capability-limits-matrix.md"
      via: "deterministic Markdown write/check output"
      pattern: "ReferenceScenarios.Markdown|--check"
    - from: "scripts/ci/verify_reference_scenario_contract.sh"
      to: "examples/crosswake_tracer/capability-report.json"
      via: "runtime-capability status and required-evidence gate"
      pattern: "feasibility_blocked|physical-device|bridge"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_reference_scenario_contract.sh"
      via: "merge-blocking docs-contracts-shift-left execution"
      pattern: "verify_reference_scenario_contract"
  assumptions: []
  prohibitions:
    - requirement_id: PROOF-05
      statement: "MUST NOT publish unsupported capability, runtime-feasibility, provider-control, offline-expansion, or privacy claims that exceed the versioned facts and evidence lane."
      status: unverified
      flagged: true
---

<objective>
Make the exact v1.59 capability/compatibility contract generated and merge-blocking per D-03 and D-09 through D-11.

Purpose: Prevent release material from drifting beyond deterministic evidence or understating explicit limits.
Output: Public-contract fixture, deterministic matrix generator/task, generated matrix, strict tests, and blocking CI gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-RESEARCH.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-PATTERNS.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-01-SUMMARY.md
@accrue/lib/accrue/entitlements/decision_cases/markdown.ex
@scripts/ci/verify_entitlement_source_matrix.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate the exact capability and limits matrix from versioned facts</name>
  <reversibility rating="costly">Per D-09, the fixture schema, generated cells, scenario IDs, and command become coordinated release artifacts.</reversibility>
  <files>accrue/priv/entitlements/v1.59-public-contract.json, accrue/lib/accrue/entitlements/reference_scenarios/markdown.ex, accrue/lib/mix/tasks/accrue.entitlements.reference_scenarios.ex, examples/accrue_host/docs/capability-limits-matrix.md, accrue/test/accrue/entitlements/reference_scenarios_test.exs</files>
  <read_first>
    - .planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md (D-03, D-09 through D-11)
    - accrue/priv/entitlements/v1.59-reference-scenarios.json (Plan 01 scenario/lane/artifact IDs)
    - accrue/priv/entitlements/v1.59-source-capabilities.json (closed capability-state precedent)
    - accrue/lib/accrue/entitlements/decision_cases/markdown.ex (deterministic output/check pattern)
    - accrue/lib/mix/tasks/accrue.entitlements.decision_cases.ex (Mix task argument/check pattern)
    - examples/crosswake_tracer/capability-report.json (runtime status and required evidence)
    - examples/accrue_host/docs/adoption-proof-matrix.md (visible proof-lane taxonomy)
  </read_first>
  <behavior>
    - Test 1: strict fixture validation accepts only versioned closed support/limit/lane states and references existing unique scenario IDs/artifacts.
    - Test 2: write mode deterministically generates the same bytes; check mode passes only when the committed matrix equals generated output.
    - Test 3: matrix states legacy compatibility, Apple external management, no cross-rail mutation, stale downloaded-study/local-progress continuity only, privacy exclusions, scenario IDs, evidence lanes, and one verification command.
    - Test 4: runtime-capability cells are derived from the current capability report and remain feasibility_blocked without both bridge and physical-device evidence.
    - Test 5: mutation cases reject unknown/duplicate fields, stale scenario references, unsupported assertions, private fields, contradictory limits, and generated prose/procedure content.
  </behavior>
  <action>Create the sibling v1.59 public-contract fixture with exact supported/unsupported assertions and references to the Plan-01 corpus. Implement deterministic Markdown generation and `mix accrue.entitlements.reference_scenarios [--check]` following DecisionCases export/check conventions. Per D-09, generate only exact capability/compatibility facts, lane labels, scenario IDs, status, and verification command; do not generate walkthrough, App Review, security/privacy explanation, runbook procedure, threat analysis, watchlist rationale, or release narrative. Per D-03, read the capability report and require its evidence inventory before any runtime-capability promotion. Add mutation-sensitive tests for schema, cross-reference, determinism, privacy, and forbidden claim boundaries.</action>
  <verify><automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenarios_test.exs &amp;&amp; mix accrue.entitlements.reference_scenarios --check</automated></verify>
  <done>The versioned facts deterministically own every exact capability/limit/lane cell, and the committed matrix is reproducible with no hand-edited support assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Block release-contract drift and evidence-lane inflation in CI</name>
  <files>scripts/ci/verify_reference_scenario_contract.sh, .github/workflows/ci.yml, accrue/test/accrue/entitlements/reference_scenarios_test.exs</files>
  <read_first>
    - scripts/ci/verify_entitlement_source_matrix.sh (required-file, exact-state, negative-boundary style)
    - scripts/ci/verify_adoption_proof_matrix.sh (public matrix drift pattern)
    - .github/workflows/ci.yml (`docs-contracts-shift-left` job and existing release gates)
    - scripts/ci/README.md (current gate ownership/triage map; Plan 05 will update it)
    - examples/crosswake_tracer/physical-device-evidence.md (required evidence contract)
    - examples/crosswake_tracer/capability-report.json (status to preserve)
  </read_first>
  <behavior>
    - Test 1: gate fails if fixture/parser/scenario/matrix/capability-report inputs are missing or malformed and cannot pass by comparing absent/default values.
    - Test 2: gate runs generator check, validates all cross-references, and confirms deterministic_conformance is the only merge-blocking lane.
    - Test 3: known-bad temporary copies prove failure for runtime-claim inflation, Apple lifecycle control, cross-rail mutation, stale premium expansion, raw transaction/proof/token/PII claims, and stale generated output.
    - Test 4: the gate is executed in `docs-contracts-shift-left` on pull requests and a missing/skipped invocation fails the workflow contract test.
  </behavior>
  <action>Add a strict Bash gate with `set -euo pipefail`, explicit file/tool guards, `jq` schema/cross-reference assertions, deterministic Mix check, and region-scoped negative claim checks. Avoid vacuous fallback comparisons and comment-text self-invalidation. Prove the gate has teeth by running it against known-bad temporary copies for each D-03/D-10 contradiction, while leaving repository sources unchanged after the test. Wire the script as a merge-blocking step in the existing `docs-contracts-shift-left` job; do not create a live-store, credential, simulator, or LLM gate.</action>
  <verify><automated>bash scripts/ci/verify_reference_scenario_contract.sh &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenarios_test.exs</automated></verify>
  <done>CI fails closed on corpus/public-contract/generated-output drift or unsupported claims, and only deterministic conformance can block merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| versioned fixtures/report → generated public matrix | Repository facts become public support claims. |
| generated output/prose → merge gate | Human edits and stale outputs attempt to cross release policy. |
| CI configuration → required status | Gate invocation must remain present and blocking. |

## STRIDE Threat Register (ASVS L1; block threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-14 | Tampering | public contract/matrix | high | mitigate | Strict schema, deterministic byte check, cross-reference validation, and known-bad mutation tests. |
| T-220-15 | Spoofing | evidence lane/runtime status | high | mitigate | Gate permits merge authority only for deterministic_conformance and derives runtime status from required evidence. |
| T-220-16 | Information Disclosure | generated matrix/fixture | high | mitigate | Closed public allowlist and negative sensitive-field/value tests reject raw evidence, proof material, tokens, and PII. |
| T-220-17 | Tampering | CI workflow | high | mitigate | Workflow contract asserts the gate remains in the pull-request blocking job. |
| T-220-SC | Tampering | package-manager installs | low | accept | No package installation occurs. |
</threat_model>

## Plan-Local Source Coverage Audit

| Source | ID | Feature / requirement | Task(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Honest public release contract | 1-2 | COVERED | Generated facts and blocking drift gate. |
| REQ | PROOF-05 | Public matrices/compatibility/conformance truth | 1-2 | COVERED | Hand-authored procedure material follows in Plan 05. |
| RESEARCH | — | Fixture-derived assertions plus literal/scripted gates | 1-2 | COVERED | Reuses existing no-package stack. |
| CONTEXT | D-03 | No runtime claim before bridge/device evidence | 1-2 | COVERED | One-way locked commitment recorded; no checkpoint repeated. |
| CONTEXT | D-09 | Versioned fixture generates exact cells only | 1 | COVERED | Costly rating retained. |
| CONTEXT | D-10 | One additive contract and privacy limits | 1-2 | COVERED | Positive and negative claim tests. |
| CONTEXT | D-11 | Compact command/matrix/scenario/lane path | 1 | COVERED | Matrix exposes exact references. |

### Spec-less probes and prohibition recall

| Kind | Requirement | Item | Disposition |
|---|---|---|---|
| Edge | PROOF-05 | Manually review the public-contract boundary | COVERED by exact generation, known-bad contradiction cases, and evidence-lane gate. |
| Prohibition | PROOF-05 | No unsupported/inflated public claim | FLAGGED-UNVERIFIED, descriptor-less in frontmatter; mutation tests support later judgment. |
| Canon referral | PROOF-05 | Generic injection/privacy controls | Covered by threat model and `$gsd-secure-phase`; not minted separately. |

<verification>
Run both task commands, then `bash scripts/ci/verify_entitlement_source_matrix.sh &amp;&amp; bash scripts/ci/verify_reference_scenario_contract.sh`; inspect the CI diff to confirm one blocking invocation and no credential/live-provider dependency.
</verification>

<success_criteria>
- The generated matrix is byte-deterministic and references every exact v1.59 fact/scenario/lane.
- D-03 and D-09 through D-11 are implemented exactly and cited.
- Known-bad contradictory claims make the gate red before clean sources make it green.
- The PROOF-05 prohibition remains descriptor-less and flagged-unverified.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-04-SUMMARY.md` when done.
</output>

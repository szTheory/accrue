---
phase: 220-first-adopter-proof-and-release-gates
plan: 14
type: execute
wave: 2
depends_on: [220-13]
files_modified:
  - accrue/test/support/entitlements/reference_scenario_executor/read.ex
  - accrue/test/accrue/entitlements/reference_scenario_read_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "Web/iOS reads call Accrue.Entitlements.snapshot/2 and collect revision/plans/sources without creating observations, grants, or audit events."
    - "Purchase preflight calls purchase_decision/4 and observes its exact status, reason, target rail/product, and snapshot revision."
    - "All three expiry-adjacency rows seed the declared finite grant, call the production snapshot path at their frozen clock, and distinguish immediately-before from at/after boundary."
    - "Actual generic-grant and no-effect adapters fail read/preflight/expiry expectations; snapshot-only cannot satisfy preflight and an in-memory snapshot cannot satisfy expiry durable facts."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor/read.ex"
      provides: "Production read, preflight, and expiry execution/collection"
    - path: "accrue/test/accrue/entitlements/reference_scenario_read_test.exs"
      provides: "Four-kind, seven-instance focused and adversarial proof"
  key_links:
    - from: "accrue/test/support/entitlements/reference_scenario_executor/read.ex"
      to: "Accrue.Entitlements.snapshot/2 + purchase_decision/4"
      via: "fixture-declared account/rail/product/clock inputs and fresh persistence collection"
      pattern: "snapshot|purchase_decision"
---

<objective>
Execute every read, duplicate-purchase preflight, and expiry-boundary action through its real read seam and bounded collector.

Purpose: Prevent read-like actions from passing as generic grants or hand-built in-memory snapshots.
Output: A dedicated read family executor and focused seven-instance conformance suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-13-SUMMARY.md
@accrue/lib/accrue/entitlements.ex
@accrue/lib/accrue/entitlements/snapshot.ex
@accrue/lib/accrue/entitlements/purchase_decision.ex
@accrue/test/accrue/entitlements/snapshot_test.exs
@accrue/test/accrue/entitlements/purchase_decision_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute login reads and duplicate-purchase preflight</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/read.ex, accrue/test/accrue/entitlements/reference_scenario_read_test.exs</files>
  <behavior>
    - "web_login and ios_login return the freshly fetched canonical snapshot and zero write deltas."
    - "purchase_preflight returns the production PurchaseDecision tuple and its bound revision."
    - "Real generic-grant, no-effect, and snapshot-only substitutions fail the preflight expected result."
  </behavior>
  <action>Implement the read-family module selected by Plan 13's exact dispatcher. For login actions, call snapshot/2 and independently count observations, grants, and audit rows before and after. For purchase_preflight, call purchase_decision/4 with the command's exact rail, product, environment, and account reference after the declared setup has produced the equivalent other-rail grant. Collect only bounded public decision fields and revision. Execute generic-grant, no-effect replay, and snapshot-only adversarial adapters through their real seams and prove their collected tuples cannot satisfy preflight/read expectations. Honor D-01/D-04: setup may resolve references but may not calculate the expected decision.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_read_test.exs test/accrue/entitlements/purchase_decision_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>Login and preflight actions execute the declared production reads, collect exact bounded outcomes, and reject snapshot-only substitution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove all three persisted expiry-boundary outcomes</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/read.ex, accrue/test/accrue/entitlements/reference_scenario_read_test.exs</files>
  <behavior>
    - "One microsecond before expiry retains the declared plan/source."
    - "At expiry and one microsecond after expiry omit the expired authorization."
    - "Each result comes from the repository-backed snapshot path and reports the unchanged durable grant row plus observed authorization state."
  </behavior>
  <action>For each expiry_boundary command, create the declared observation/grant through Observation and Projector, then call the repository-backed snapshot seam with the frozen clock mechanism already supported by the snapshot test configuration. Collect the stored grant expiry, account revision, visible plan/source set, and zero unexpected write delta. Remove the hand-built Snapshot.from_grants example from aggregate conformance in Plan 20, not here. Reuse the real generic-grant/no-effect adapters and add an in-memory snapshot result through the same matcher; prove each fails the intended boundary expectation because it lacks the exact clock/read/persisted facts.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_read_test.exs test/accrue/entitlements/snapshot_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The before/at/after expiry rows are distinguished by production snapshot behavior and persisted facts, not fixture arithmetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fixture clock/account → read APIs | Synthetic inputs select a read; production evaluation owns the decision. |
| Snapshot/decision → collector | Only bounded state crosses into assertions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-14-01 | Tampering | frozen clock/preflight command | high | mitigate | Exact parsed clock/rail/product fields and persisted setup assertions. |
| T-220-14-02 | Spoofing | snapshot-only adapter | high | mitigate | Execute the adapter and require decision-specific result plus zero/write deltas. |
| T-220-14-03 | Information Disclosure | read collector | low | accept | Collector exposes only revision, plans, sources, status, reason, and counts. |
| T-220-14-SC | Tampering | package installs | high | mitigate | No dependency installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Deterministic safe access proof | 220-14 | COVERED | Read/preflight/expiry family.
REQ | PROOF-02 | Duplicate prevention and boundary expiry | 220-14 | COVERED | Real purchase/read seams.
RESEARCH | — | Production contexts are semantic authority | 220-14 | COVERED | Snapshot and PurchaseDecision own results.
CONTEXT | D-01/D-02/D-04 | Data-only, closed, focused proof | 220-14 | COVERED | Exact commands plus Ecto-backed assertions.
CONTEXT | D-03/D-05..D-12 | Existing evidence/public/operator boundaries | 220-20 | COVERED | Final aggregate certification.
</source_audit>

<verification>
The focused command enumerates web_login, ios_login, purchase_preflight, and all three expiry scenarios, and it proves the write/no-write and persisted-state distinctions for each.
</verification>

<success_criteria>
- Seven read/preflight/expiry instances execute through production seams.
- Snapshot-only and in-memory substitutes fail their intended expectations.
- No fixture-derived decision logic or new public behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-14-SUMMARY.md` when done.
</output>

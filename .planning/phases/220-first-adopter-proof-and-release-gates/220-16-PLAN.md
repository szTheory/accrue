---
phase: 220-first-adopter-proof-and-release-gates
plan: 16
type: execute
wave: 2
depends_on: [220-13]
files_modified:
  - accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex
  - accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "reconnect_request creates a real challenge, signs the declared request, consumes PoP through Offline.reconnect/3, and collects the durable attempt/issuance outcome."
    - "verified_cache_replace consumes the proof returned by that same reconnect flow, verifies it, and adopts a complete new cache tuple only after successful verification."
    - "The collector observes challenge consumption, reconnect-attempt terminal state, issuance count/revision/disposition, and cache replacement from durable/runtime outputs rather than fixture values."
    - "Generic-grant, no-effect, snapshot-only, and registration-only adapters really execute and fail reconnect/cache expectations."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex"
      provides: "Signed reconnect plus verified cache replacement execution and collection"
    - path: "accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs"
      provides: "Two-action chained reconnect/cache conformance and adversarial proof"
  key_links:
    - from: "accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex"
      to: "Accrue.Entitlements.Offline.reconnect_challenge/3 + reconnect/3"
      via: "declared installation, challenge, signed PoP, idempotency, client high-water, and source coordinator"
      pattern: "reconnect_challenge|Offline.reconnect"
    - from: "reconnect outcome proof"
      to: "verified cache state"
      via: "Offline.verify before atomic cache adoption"
      pattern: "Offline.verify|cache"
---

<objective>
Execute reconnect and verified cache replacement as one chained production flow with independently observed durable and cache outcomes.

Purpose: Replace the current snapshot substitutes with a real signed reconnect, issuance, proof verification, and complete cache adoption.
Output: A reconnect/cache family executor and focused chained conformance suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-13-SUMMARY.md
@accrue/lib/accrue/entitlements/offline.ex
@accrue/lib/accrue/entitlements/offline/reconnect.ex
@accrue/lib/accrue/entitlements/offline/issuer.ex
@accrue/lib/accrue/entitlements/offline/reconnect_attempt.ex
@accrue/test/accrue/entitlements/offline_reconnect_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute a signed reconnect and collect its durable attempt/issuance</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex, accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs</files>
  <behavior>
    - "The first action creates and consumes a reconnect challenge and returns an issued production outcome from Offline.reconnect/3."
    - "Fresh queries find one terminal reconnect attempt, one issuance, consumed challenge state, and the exact issued revision/disposition."
    - "No proof, nonce, signature, private key, raw idempotency key, or provider payload appears in the bounded observation."
  </behavior>
  <action>Build the registered device and resolved source state from bounded command references, call reconnect_challenge/3, construct/sign the exact Reconnect.Request with the test-owned key, and call Offline.reconnect/3 using the existing source-coordinator/issuer options. Capture the returned proof only inside the executor state for the next action. Separately query Challenge, ReconnectAttempt, Issuance, Device, and Snapshot to produce the bounded durable map. Ensure expected_transition is unavailable to setup, signing, and collection code.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_reconnect_test.exs test/accrue/entitlements/offline_reconnect_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The reconnect action proves signed production admission, durable attempt settlement, issuance, and bounded collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify and atomically adopt the replacement cache, then defeat wrong adapters</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/reconnect_cache.ex, accrue/test/accrue/entitlements/reference_scenario_reconnect_test.exs</files>
  <behavior>
    - "verified_cache_replace uses the prior reconnect proof, verifies it with the declared context, and replaces revision/disposition/proof as one complete tuple."
    - "Verification failure preserves the old complete cache tuple."
    - "Actual generic-grant, no-effect, snapshot-only, and registration-only adapter outcomes fail the reconnect and cache expectations."
  </behavior>
  <action>Carry the opaque proof from action 1 in executor runtime state keyed by scenario/action order, not in the JSON or assertion output. Invoke Offline.verify/3 before cache adoption and use the established Phase-215 atomic cache writer/consumer boundary already exercised by the Crosswake contract tests; collect old/new revision, disposition, proof state, and replace/preserve disposition only. Add a failed-verification case proving old-cache preservation. Execute generic-grant, no-effect replay, snapshot-only, and register_device/3 adapters, collect their actual durable results, and prove they cannot satisfy either reconnect action. Do not claim Crosswake execution; this is deterministic server/contract evidence per D-03/D-04.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_reconnect_test.exs test/accrue/entitlements/offline_reconnect_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The two fixture actions form one real reconnect-to-cache flow, failures preserve the old tuple, and snapshot/registration substitutes fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Device request → reconnect admission | Signed PoP and host authorization cross into a durable attempt. |
| Issued proof → verifier/cache | Untrusted proof must verify before complete-tuple replacement. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-16-01 | Spoofing | reconnect PoP | high | mitigate | Real challenge, signed request, one-time consumption, and authorization. |
| T-220-16-02 | Tampering | cache replacement | high | mitigate | Verify first; adopt complete tuple atomically; preserve prior tuple on failure. |
| T-220-16-03 | Information Disclosure | proof/collector | high | mitigate | Proof remains executor-local; bounded state only crosses assertions. |
| T-220-16-04 | Repudiation | reconnect attempt | medium | mitigate | Query durable attempt, challenge, and issuance correlations after execution. |
| T-220-16-SC | Tampering | package installs | high | mitigate | No dependency installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Safe reconnect and cache recovery | 220-16 | COVERED | Signed reconnect through verified adoption.
REQ | PROOF-02 | Reconnect/cache replacement | 220-16 | COVERED | Both declared actions.
RESEARCH | — | Durable reconnect authority and atomic cache replacement | 220-16 | COVERED | Production attempts/issuance plus verify-before-adopt.
CONTEXT | D-01..D-04 | Data-only, closed, truthful focused proof | 220-16 | COVERED | Runtime proof stays local; no Crosswake promotion.
CONTEXT | D-10 | Stale continuity and restricted expansion | 220-16 | COVERED | Old cache preserved until verified replacement.
CONTEXT | D-05..D-09/D-11/D-12 | Existing boundaries | 220-20 | COVERED | Final aggregate certification.
</source_audit>

<verification>
The focused suite consumes both ordered fixture actions, queries durable attempt/challenge/issuance state, verifies the returned proof, observes cache replacement/preservation, and executes both wrong adapters.
</verification>

<success_criteria>
- reconnect_request and verified_cache_replace are one real chained flow.
- Durable and cache facts are independently collected.
- Snapshot/registration substitutes fail and Crosswake remains feasibility_blocked.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-16-SUMMARY.md` when done.
</output>

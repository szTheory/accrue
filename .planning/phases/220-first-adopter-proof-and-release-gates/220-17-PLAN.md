---
phase: 220-first-adopter-proof-and-release-gates
plan: 17
type: execute
wave: 2
depends_on: [220-13]
files_modified:
  - accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex
  - accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "device_replace reuses the shipped 220-12 Offline.replace_device/3 API and collects prior/replacement/challenge/audit/snapshot facts from fresh queries."
    - "rotated_key_proof creates finite issued-proof retention requirements, calls verification_keys_with_issued_retention/1, and observes the exact retained/retired public key IDs."
    - "The command contract contains only synthetic key/proof references; private JWK members, nonce, signature, compact proof, and raw idempotency data never enter the corpus or observed tuple."
    - "Generic-grant, no-effect, registration-only, and snapshot-only adapters actually execute and fail device/key expectations."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex"
      provides: "Production device replacement and issued-key-retention execution/collection"
    - path: "accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs"
      provides: "Device/key conformance, privacy, and adversarial proof"
  key_links:
    - from: "accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex"
      to: "Accrue.Entitlements.Offline.replace_device/3"
      via: "existing authorized challenge and replacement PoP boundary from Plan 220-12"
      pattern: "Offline.replace_device"
    - from: "accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex"
      to: "Offline.verification_keys_with_issued_retention/1"
      via: "persisted issuance retirement requirements and candidate public key set"
      pattern: "verification_keys_with_issued_retention"
---

<objective>
Integrate the real device-replacement API and key-retention publisher into the shared action executor with exact persisted outcomes.

Purpose: Preserve Plan 220-12's production device seam while closing the remaining rotated-key bypass and adversarial gaps.
Output: A device/key family executor and two-action production conformance suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-12-SUMMARY.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-13-SUMMARY.md
@accrue/lib/accrue/entitlements/offline.ex
@accrue/lib/accrue/entitlements/offline/registration.ex
@accrue/lib/accrue/entitlements/offline/issuance.ex
@accrue/lib/accrue/entitlements/offline/key_provider.ex
@accrue/test/accrue/entitlements/offline_registration_test.exs
@accrue/test/accrue/entitlements/offline_reconnect_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route device replacement through the shipped API and fresh collector</name>
  <precondition>`Accrue.Entitlements.Offline.replace_device/3` and its existing replacement request/result types from completed Plan 220-12 compile and the named registration test passes.</precondition>
  <files>accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex, accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs</files>
  <behavior>
    - "The action creates the declared prior device and challenge, signs replacement PoP, calls Offline.replace_device/3, and reports its bounded result."
    - "Fresh queries independently confirm prior state, active replacement, challenge consumption, one bounded audit delta, and snapshot revision."
    - "Exact replay is collected as already_completed and divergent replay fails without new rows."
  </behavior>
  <action>Move the existing real device_replace setup/dispatch/collection out of the shared executor into DeviceKeys without changing the 220-12 public API or production implementation. Resolve references into test-owned keys only inside the executor, invoke the public facade with host authorization/actor options, and query Device, Challenge, Event, and Snapshot after the call. Add exact replay and divergent replay cases to the family test. Ensure the common observed tuple is constructed exclusively from the returned result and fresh queries, not expected_transition.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_device_keys_test.exs test/accrue/entitlements/offline_registration_test.exs:238 --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The shared device action retains the 220-12 production seam, exact replay behavior, and independently collected persisted tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute issued-proof key retention and defeat registration/snapshot substitutes</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/device_keys.ex, accrue/test/accrue/entitlements/reference_scenario_device_keys_test.exs</files>
  <behavior>
    - "An old key with a not-yet-retired finite issuance remains in published JWKS alongside the new key."
    - "After actual proof expiry plus the required retention margin, the old key can retire while the current key remains."
    - "Actual generic-grant, no-effect, register_device, and snapshot-only results fail both device replacement and rotated-key expectations."
  </behavior>
  <action>Seed the declared issuance through the existing Issuer/Issuance production path, supply the candidate public key set and frozen clock, then call Offline.verification_keys_with_issued_retention/1. Collect sorted public kid values and persisted retirement requirement bounds only; never expose coordinates beyond the public JWK output or any private member. Advance only the fixture clock to the declared post-retention case and re-run the public key publisher. Execute generic-grant, no-effect replay, register_device/3, and snapshot/2 wrong adapters, collect their actual results, and prove the exact device/key matchers reject them.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_device_keys_test.exs test/accrue/entitlements/offline_reconnect_test.exs:780 --seed 458442 --max-failures 1</automated>
  </verify>
  <done>Key rotation is proven by actual issuance-derived retention, and device/key expectations reject real registration/snapshot substitutes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Host/device → replacement API | Authorization and replacement PoP cross into an atomic device lifecycle transition. |
| Issuance rows → public JWKS | Durable proof horizons constrain key retirement without exposing secrets. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-17-01 | Spoofing | device replacement | high | mitigate | Reuse 220-12 authorization, challenge, signed PoP, and account-scoped query. |
| T-220-17-02 | Tampering | key retirement | high | mitigate | Derive requirements from persisted issuances and production retention publisher. |
| T-220-17-03 | Information Disclosure | keys/proofs | high | mitigate | Synthetic refs only; reject private JWK/proof/signature/raw idempotency fields. |
| T-220-17-04 | Denial of Service | replay | medium | mitigate | Exact idempotent replay and bounded row-count assertions. |
| T-220-17-SC | Tampering | package installs | high | mitigate | No dependency installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Safe device/key operations proof | 220-17 | COVERED | Uses shipped production boundaries.
REQ | PROOF-02 | Device replacement and key rotation | 220-17 | COVERED | Both declared actions.
RESEARCH | — | Host-owned keys and issuance-backed retention | 220-17 | COVERED | Production publisher and bounded fixtures.
CONTEXT | D-01/D-02/D-04 | Data-only closed focused proof | 220-17 | COVERED | Real API plus fresh queries.
CONTEXT | D-03/D-06/D-08/D-10 | Runtime honesty, privacy, replacement/rotation, no raw proof | 220-17 | COVERED | Explicit privacy and operation bounds.
CONTEXT | D-05/D-07/D-09/D-11/D-12 | Existing contracts | 220-20 | COVERED | Final aggregate certification.
</source_audit>

<verification>
The focused suite exercises replacement, replay, retention-before-expiry, and retirement-after-margin; structural key checks reject private material and both actual wrong adapters fail.
</verification>

<success_criteria>
- Device replacement remains on Offline.replace_device/3 with fresh collection.
- Key rotation follows persisted issuance retention, not a fixture kid list.
- Registration/snapshot substitutes fail and no secret material crosses the corpus boundary.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-17-SUMMARY.md` when done.
</output>

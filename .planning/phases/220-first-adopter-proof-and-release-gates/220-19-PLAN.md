---
phase: 220-first-adopter-proof-and-release-gates
plan: 19
type: execute
wave: 3
depends_on: [220-16]
files_modified:
  - accrue/test/support/entitlements/reference_scenario_executor/resume.ex
  - accrue/test/accrue/entitlements/reference_scenario_resume_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "durable_interruption runs a complete signed reconnect command through the production after_admission or after_issuance_commit hook and returns the declared interruption while leaving the expected durable attempt/issuance boundary."
    - "resume_delivery replays the same complete request from the fixture, resumes the same ReconnectAttempt/Challenge authority, and reaches one issuance/cache replacement without a torn tuple."
    - "Interruption and resume are two ordered fixture actions sharing explicit request/attempt references; no projector-only local example stands in for reconnect resume."
    - "Generic-grant, no-effect, and snapshot-only adapters actually execute and fail the attempt/issuance/cache expectation."
  artifacts:
    - path: "accrue/test/support/entitlements/reference_scenario_executor/resume.ex"
      provides: "Production reconnect fault injection, durable state collection, and exact replay/resume"
    - path: "accrue/test/accrue/entitlements/reference_scenario_resume_test.exs"
      provides: "Two-action interruption/resume conformance and torn-state checks"
  key_links:
    - from: "durable_interruption command"
      to: "Offline.reconnect/3 production hook"
      via: "complete signed request plus declared after_admission/after_issuance_commit boundary"
      pattern: "after_admission|after_issuance_commit"
    - from: "resume_delivery command"
      to: "ReconnectAttempt + Challenge + Issuance"
      via: "exact replay of the same request and fresh durable/cache collection"
      pattern: "ReconnectAttempt|Issuance|resume"
---

<objective>
Execute interruption and resume as two ordered fixture actions against the durable reconnect state machine.

Purpose: Replace the projector-only stand-in with the production fault-injection and exact replay boundary already shipped by Offline.reconnect/3.
Output: A resume family executor and two-action durable recovery suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-first-adopter-proof-and-release-gates/220-CONTEXT.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-VERIFICATION.md
@.planning/phases/220-first-adopter-proof-and-release-gates/220-16-SUMMARY.md
@accrue/lib/accrue/entitlements/offline/reconnect.ex
@accrue/lib/accrue/entitlements/offline/reconnect_attempt.ex
@accrue/lib/accrue/entitlements/offline/issuance.ex
@accrue/test/accrue/entitlements/offline_reconnect_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interrupt a complete fixture reconnect at its declared durable boundary</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/resume.ex, accrue/test/accrue/entitlements/reference_scenario_resume_test.exs</files>
  <behavior>
    - "The command chooses one supported production hook boundary and supplies the same full reconnect request later consumed by resume_delivery."
    - "The interrupted result and fresh queries agree on pending/resumable attempt, challenge state, issuance count, and cache preservation."
  </behavior>
  <action>Reuse the signed-request setup/collector pattern from Plan 16, but consume the interruption command's explicit hook value and request reference. Call Offline.reconnect/3 with the existing after_admission or after_issuance_commit callback returning the declared interruption. Query ReconnectAttempt, Challenge, Issuance, Device, and Snapshot immediately afterward and collect the exact bounded partial state plus preserve-cache disposition. Do not construct a standalone projector observation or treat a lost function response as equivalent to reconnect interruption.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_resume_test.exs test/accrue/entitlements/offline_reconnect_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The first action produces the declared error at a real reconnect hook and independently proves the intended durable resumable boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume the same attempt to one issuance and complete cache tuple</name>
  <files>accrue/test/support/entitlements/reference_scenario_executor/resume.ex, accrue/test/accrue/entitlements/reference_scenario_resume_test.exs</files>
  <behavior>
    - "Exact replay resumes the same attempt and challenge instead of creating new authority."
    - "Terminal state has one issuance, one consumed challenge, one terminal attempt, and one verified replacement cache tuple."
    - "A second exact replay is stable; generic-grant, no-effect, and snapshot-only adapters fail."
  </behavior>
  <action>Execute resume_delivery with the same complete request identity and no interruption callback, then verify the returned proof and cache through Plan 16's established pattern. Collect attempt identity/state/token progression, challenge consumption, issuance count/revision/disposition, returned result, and cache replacement as one bounded observation. Replay once more to prove stability. Execute real generic-grant, no-effect, and snapshot-only adapters and show they lack the required resumed-attempt/issuance/cache evidence. Preserve D-03: this proves server durability and the contract cache boundary, not physical-device Crosswake runtime.</action>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/entitlements/reference_scenario_resume_test.exs test/accrue/entitlements/offline_reconnect_test.exs --seed 458442 --max-failures 1</automated>
  </verify>
  <done>The fixture pair proves durable interruption, exact resume, single issuance, complete cache adoption, stable replay, and wrong-adapter failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fault hook → reconnect transaction | Test-only interruption may stop delivery but must not replace durable authority. |
| Persisted attempt → exact replay | Replay must bind the same account/device/challenge/idempotency tuple. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-220-19-01 | Tampering | interruption hook | high | mitigate | Closed hook enum and production hook invocation only. |
| T-220-19-02 | Spoofing | resume request | high | mitigate | Exact signed request replay and same durable attempt/challenge checks. |
| T-220-19-03 | Repudiation | torn outcome | high | mitigate | Fresh attempt/challenge/issuance/cache tuple after both actions. |
| T-220-19-04 | Denial of Service | repeated resume | medium | mitigate | Stable replay and exact single-issuance counts. |
| T-220-19-SC | Tampering | package installs | high | mitigate | No dependency installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Operable reconnect recovery proof | 220-19 | COVERED | Durable interruption/resume.
REQ | PROOF-02 | Interruption and resume | 220-19 | COVERED | Both fixture actions use reconnect state machine.
RESEARCH | — | Durable attempt/outbox and exact replay authority | 220-19 | COVERED | Existing production hooks and rows.
CONTEXT | D-01..D-04 | Data-only closed truthful focused proof | 220-19 | COVERED | No projector stand-in or runtime promotion.
CONTEXT | D-08/D-10 | Durable recovery and complete cache | 220-19 | COVERED | Single issuance and preserve/replace behavior.
CONTEXT | D-05..D-07/D-09/D-11/D-12 | Existing contracts | 220-20 | COVERED | Final aggregate certification.
</source_audit>

<verification>
The suite consumes both ordered actions, proves the selected real hook fired, compares fresh state before/after resume, and certifies one issuance plus complete cache adoption and stable replay.
</verification>

<success_criteria>
- Interruption/resume is a production reconnect flow, not a projector example.
- The same durable attempt resumes to exactly one issuance/cache tuple.
- Wrong adapters fail and no Crosswake capability claim changes.
</success_criteria>

<output>
Create `.planning/phases/220-first-adopter-proof-and-release-gates/220-19-SUMMARY.md` when done.
</output>

---
phase: 221-close-gap-reference-host-apple-notification-ingress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [D-01, D-04, D-05, D-06]

must_haves:
  truths:
    - "D-01/D-06 have one explicit, user-approved route composition that does not silently change Accrue's public API."
    - "D-04/D-05 have one approved production verifier identity and environment-loading contract before it becomes published host guidance."
  artifacts:
    - path: ".planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-01-SUMMARY.md"
      provides: "Recorded selections for the two one-way implementation doors"
  key_links:
    - from: "D-01/D-06 selection"
      to: "221-02-PLAN.md"
      via: "approved router/wrapper composition"
      pattern: "wrapper-forward"
    - from: "D-04/D-05 selection"
      to: "examples/accrue_host/config/runtime.exs"
      via: "approved production environment contract"
      pattern: "APPLE_TRUST_ROOTS_PEM_PATH"
---

<objective>
Resolve the two one-way host contracts that must be explicit before implementation: the incompatible literal macro/wrapper wording and the production verifier/client identity.

Purpose: Prevent a silent D-01/D-06 compromise or an accidental published trust contract while keeping all package APIs unchanged.
Output: Two recorded decisions that unblock the production tracer.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-CONTEXT.md
@.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-PATTERNS.md
@accrue/lib/accrue/router.ex
@accrue/lib/accrue/entitlements/apple/notification_plug.ex
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Resolve the D-01 macro and D-06 host-wrapper composition</name>
  <read_first>
    - `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-CONTEXT.md` — D-01, D-06, and the no-public-API-expansion boundary.
    - `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-PATTERNS.md` — verified macro hardcoding conflict.
    - `accrue/lib/accrue/router.ex` — `accrue_apple_notifications/2` expands directly to `NotificationPlug`.
  </read_first>
  <action>Present the verified conflict exactly: `Accrue.Router.accrue_apple_notifications/2` hardcodes `Accrue.Entitlements.Apple.NotificationPlug`, while D-06 requires the mounted Plug to be host-owned and resolve runtime options. Ask the user to approve option A or stop for replanning; do not modify files and do not infer approval.</action>
  <decision>Which route composition is authoritative for D-01/D-06?</decision>
  <context>Changing this after publication changes the documented endpoint installation contract. The package macro cannot mount the wrapper without a package API change, which this phase forbids.</context>
  <options>
    <option id="option-a">
      <name>Wrapper-forward resolution (Recommended)</name>
      <pros>Mount `AccrueHost.AppleNotificationIngress` with Phoenix `forward/3`; the wrapper delegates unchanged to the same package `NotificationPlug`, preserves the macro-defined parser/Plug contract, keeps runtime ownership host-side, and adds no package API.</pros>
      <cons>Clarifies D-01's phrase “invokes the existing macro” as “implements the existing macro contract”; the host route does not literally call the macro.</cons>
    </option>
    <option id="option-b">
      <name>Stop for scope revision</name>
      <pros>Allows the user to rewrite D-01 or D-06 before code lands.</pros>
      <cons>Blocks Plans 221-02 through 221-05; a literal macro call cannot satisfy D-06 under the current package API.</cons>
    </option>
  </options>
  <verify>
    <automated>rg -n "defmacro accrue_apple_notifications|Accrue\.Entitlements\.Apple\.NotificationPlug" accrue/lib/accrue/router.ex</automated>
  </verify>
  <acceptance_criteria>Option A is explicitly selected and recorded in `221-01-SUMMARY.md`, or execution stops for replanning with no implementation commit.</acceptance_criteria>
  <resume-signal>Select `option-a`; select `option-b` to stop and replan.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the one-way production verifier and client identity</name>
  <read_first>
    - `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-CONTEXT.md` — D-04 and D-05 one-way trust boundary.
    - `accrue/lib/accrue/entitlements/apple/verifier.ex` — enforced `Verifier.Config` fields.
    - `accrue/lib/accrue/entitlements/apple/client.ex` — production client authorization contract.
    - `examples/accrue_host/config/runtime.exs` — existing production fail-fast convention.
  </read_first>
  <action>Ask the user to approve the exact option A contract used by downstream plans: production-only `Verifier.Config`, constant verifier identity `apple-production-v1`, and required environment names `APPLE_TRUST_ROOTS_PEM_PATH`, `APPLE_BUNDLE_ID`, `APPLE_APP_ID`, `APPLE_VERIFIER_CONFIG_VERSION`, `APPLE_SERVER_API_BEARER_TOKEN`, and `APPLE_PRODUCT_MAP_JSON`. The PEM path is read at boot and decoded into pinned DER trust roots; the same immutable config term is reused for ingress and reconciliation admission. If not approved, stop for replanning rather than substituting names or loading semantics.</action>
  <decision>Publish the proposed runtime verifier/client environment contract?</decision>
  <context>D-04 marks verifier identity/configuration one-way because hosts and deployment documentation will depend on these names and semantics.</context>
  <options>
    <option id="option-a">
      <name>Approve runtime contract (Recommended)</name>
      <pros>Explicit fail-fast production inputs, file-based pinned roots, one immutable verifier config, production-only environment, and no committed trust material or credentials.</pros>
      <cons>Renaming an input later requires coordinated deployment and documentation changes.</cons>
    </option>
    <option id="option-b">
      <name>Stop for contract revision</name>
      <pros>Allows a different deployment secret/root-loading contract to be specified before publication.</pros>
      <cons>Blocks all production runtime implementation until a replacement contract is supplied.</cons>
    </option>
  </options>
  <verify>
    <automated>rg -n "@enforce_keys|authorization" accrue/lib/accrue/entitlements/apple/verifier.ex accrue/lib/accrue/entitlements/apple/client.ex</automated>
  </verify>
  <acceptance_criteria>Option A is explicitly selected and recorded in `221-01-SUMMARY.md`, or execution stops for replanning with no implementation commit.</acceptance_criteria>
  <resume-signal>Select `option-a`; select `option-b` to stop and replan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Planning decision → public host contract | A clarification controls the published route and verifier identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-221-01 | Tampering | Route composition | high | mitigate | Block implementation until the wrapper-forward interpretation is explicitly selected; no package API change is permitted. |
| T-221-02 | Spoofing | Runtime verifier identity | high | mitigate | Block implementation until production-only pinned roots, bundle, environment, app ID, and version identity are confirmed. |
| T-221-03 | Information Disclosure | Runtime input contract | high | mitigate | Approve names and loading semantics only; never record values, roots, tokens, payloads, or certificates. |
</threat_model>

<verification>
Both checkpoints have explicit selections recorded in `221-01-SUMMARY.md`; no source file changes occur in this plan.
</verification>

<success_criteria>
- The D-01/D-06 conflict is resolved visibly without a package API expansion.
- The D-04/D-05 runtime identity is confirmed before the published contract is implemented.
</success_criteria>

## Artifacts this phase produces

- `examples/accrue_host/lib/accrue_host/apple_notification_ingress.ex` — `AccrueHost.AppleNotificationIngress` Plug and safe runtime parsers.
- `examples/accrue_host/lib/accrue_host/apple_rate_policy.ex` — `AccrueHost.AppleRatePolicy` supervised single-node callback.
- `examples/accrue_host/test/accrue_host_web/apple_notification_ingest_test.exs` — Fake-first router/persistence proof.
- `examples/accrue_host/test/accrue_host/apple_rate_policy_test.exs` — deterministic limiter proof.
- `:accrue_apple_notifications_raw_body` and `/webhooks/apple` — dedicated host router pipeline and endpoint.
- `:apple_notification_ingress` and `:apple_reconciliation` — shared runtime ingress/admission configuration.
- `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/COVERAGE.md` — decided Apple API surface matrix.

<output>
Create `.planning/phases/221-close-gap-reference-host-apple-notification-ingress/221-01-SUMMARY.md` when done.
</output>

---
phase: 223-ios-compatible-accrue-offline-client
plan: 02
type: execute
wave: 2
depends_on: [223-01]
files_modified:
  - packages/accrue-offline-client/Package.swift
  - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift
  - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift
  - packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift
autonomous: true
requirements: [IOS-01, IOS-02, IOS-03]
must_haves:
  truths:
    - "D-10: Every canonical vector and decision case is exercised from the language-neutral corpus; test keys, mutation signing, fixture paths, and crash commands exist only in test/harness targets."
    - "Specless fallback (IOS-01 adjacency): at an equal revision, signed deny is stronger than allow; equal or older allow cannot replace deny."
    - "Specless fallback (IOS-01 ordering): iteration order of vectors, JWKS keys, or concurrent candidates cannot change the canonical result or denial precedence."
    - "Specless fallback (IOS-01 idempotency): applying the same verified proof twice yields the same public state and cannot weaken or corrupt the authenticated cache."
    - "Specless fallback (IOS-01 concurrency): parallel or interrupted replacements serialize through one ordering/durability boundary and leave one complete authenticated canonical envelope."
    - "Specless fallback (IOS-02 idempotency): repeated verification of identical bytes against identical context returns the same state without creating new authority."
    - "Specless fallback (IOS-02 concurrency): concurrent verification shares no mutable caller authority and cannot bypass account/device/time/profile checks."
    - "D-08: candidate write, candidate sync, same-volume atomic replacement, supported parent-directory sync, and canonical-envelope recovery preserve the previous complete cache on every injected failure."
  artifacts:
    - path: "packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift"
      provides: "Canonical corpus, mutation, rotation, state, ordering, and idempotency coverage"
    - path: "packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift"
      provides: "Test-only fresh-process fault and replacement harness"
    - path: "packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift"
      provides: "Restart, interruption, concurrency, and recovery proof"
  key_links:
    - from: "GoldenVectorFixtureSupport"
      to: "accrue/priv/entitlements/v1.59-offline-golden-vectors.json"
      via: "test-only repository fixture resolution"
      pattern: "v1.59-offline-golden-vectors"
    - from: "AtomicOfflineCacheProcessTests"
      to: "AccrueOfflineCacheCrashHarness"
      via: "fresh child processes against one cache URL and authentication key"
      pattern: "runHarness"
  prohibitions:
    - statement: "A malformed, forged, stale, superseded, cross-account, cross-device, wrong-profile, unknown-key, or expired proof must never mutate cache authority."
      status: unverified
      flagged: true
---

<objective>
Expand the tracer into complete canonical, adversarial, rotation, ordering, concurrency, and crash/recovery coverage while preserving the single coupled verifier→opaque-proof→high-water→authenticated-atomic-cache invariant per D-03, D-06, D-08, D-10, and D-11.

Purpose: Turn the extracted core from one proven path into a merge-blocking behavioral match for IOS-01 through IOS-03.
Output: Full Swift mutation/unit suite, test-only signing/fixture support, a non-iOS crash harness, and process/fault tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-CONTEXT.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-RESEARCH.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-PATTERNS.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-01-SUMMARY.md

Specless fallback status: this plan authors seven probe-derived adjacency, ordering, idempotency, and concurrency predicates for IOS-01/IOS-02. The IOS-03 repetition/concurrency predicates are authored in 223-03. No fallback predicate is treated as a prohibition.
</context>

## Artifacts this phase produces

- Complete `OfflineEntitlementClientTests` covering corpus parity, strict profile/binding/time mutation rejection, four states, key rotation, high-water, denial precedence, and idempotency.
- Test-only `GoldenVectorFixtureSupport` with corpus/key loading and mutation signing isolated from runtime products.
- `AccrueOfflineCacheCrashHarness` executable target excluded from the public iOS build surface.
- `AtomicOfflineCacheProcessTests` proving fresh-process ordering, fault preservation, concurrent serialization, candidate cleanup, and authenticated recovery.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock canonical corpus parity, strict mutations, rotation, four states, and deterministic high-water order</name>
  <read_first>
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/GoldenVectorTests.swift
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json
    - accrue/priv/entitlements/v1.59-decision-cases.json
    - accrue/priv/entitlements/v1.59-offline-test-key.jwk.json
  </read_first>
  <behavior>
    - "All canonical vectors map byte-for-byte semantics to fresh/ok/none, stale_offline/revalidation_due/reconnect_required, denied/signed_denial/reconnect_required, or the exact invalid reason/reconnect_required."
    - "Mutations of alg, typ, header membership, kid/key validity, signature, issuer, audience, subject/account, cnf.jkt, version, nbf, iat, fresh_until, exp, revision, and disposition fail before cache mutation."
    - "Rotation accepts every currently valid matching kid, rejects unknown/not-yet-valid/retired keys according to corpus context, and an unbounded cached proof retains its verification key as required by the v1.59 contract."
    - "Same-revision signed deny survives older, equal, or reordered allow candidates; repeat application is deterministic and non-weakening."
  </behavior>
  <files>packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift</files>
  <action>Write the failing corpus-table and mutation tests first, using only `GoldenVectorFixtureSupport` to load the two canonical JSON files and test-only JWK. Drive any missing production behavior through the existing private verifier and `ProofHighWater`; do not create a second test verifier or duplicate reducer/oracle logic. Iterate every corpus row and assert exact state/reason/next-action/cache-disposition fields, including stale exactly at `fresh_until`, long stale continuity, hard expiry, clock rollback, malformed compact input, wrong algorithm/type/issuer/audience/device, unknown key, superseded proof, valid allow, and valid signed denial. Assert the resolved D-02/D-10 vocabulary exactly: canonical reasons `ok`, `signed_denial`, `revalidation_due`, `clock_rollback`, `device_mismatch`, `hard_expired`, `malformed`, `superseded`, `unknown_key`, `wrong_algorithm`, `wrong_audience`, `wrong_issuer`, and `wrong_type`; bounded operational reasons `reconnect_failed`, `cache_write_failed`, and `cache_recovery_failed`; and only actions `none` and `reconnect_required`. Add deterministic mutations for account subject, header membership, signature, key rotation validity, and temporal boundaries. Exercise shuffled input/JWKS order and repeated identical proof to prove result stability. Preserve D-02 stale study continuity as non-grant and D-10 test-only key/fixture isolation; production code may change only to satisfy canonical behavior through the single private admission path.</action>
  <acceptance_criteria>Every canonical row and named mutation has an explicit expectation, all four public states appear, exact binding/profile/time failures are distinguishable only through bounded reasons, and same-revision denial cannot be displaced by allow under repeat or reordered execution.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/accrue-offline-client --filter OfflineEntitlementClientTests</automated>
  </verify>
  <done>The public package behavior is a complete canonical-corpus consumer rather than an independently invented Swift oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove atomic durability, serialized high-water, and authenticated recovery across faults and fresh processes</name>
  <read_first>
    - packages/accrue-offline-client/Package.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift (create)
    - packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift (create)
    - examples/crosswake_tracer/Tests/AccrueOfflineClientTests/AtomicOfflineCacheProcessTests.swift
    - examples/crosswake_tracer/Sources/AccrueOfflineCacheCrashHarness/main.swift
  </read_first>
  <behavior>
    - "A crash/fault before candidate sync, before replace, or before supported directory sync returns a bounded failure and leaves the previous complete authenticated canonical envelope recoverable."
    - "Tampered or malformed canonical envelopes are rejected; orphan candidates are never recovered as authority and are cleaned safely."
    - "Fresh processes and concurrent writers preserve monotonic issuance/freshness/revision order and same-revision deny precedence."
    - "The host-provided HMAC key is used in memory and never persisted by the library."
  </behavior>
  <files>packages/accrue-offline-client/Package.swift, packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift, packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift</files>
  <action>Add the crash harness as a non-library executable target used only by macOS/Linux tests; do not make it a dependency of either public library product or the generic iOS compile lane. Port and strengthen the existing process suite so each command supplies compact proof bytes to the facade rather than constructing replacement metadata. Keep candidate creation in the destination directory, synchronize the candidate, use same-volume `replaceItemAt`/move semantics, synchronize the parent directory where supported, authenticate the canonical envelope before reading high-water, and update in-memory coordination only after durable replacement per D-08. Inject faults at every durability boundary, kill/restart child processes, race allow and signed-deny writers, corrupt envelope body/tag/version, and leave orphan candidates. Assert the previous canonical file remains byte-complete/authenticated after failures, deny wins at equal revision after restart, stale/older candidates do not replace, orphan candidates never become recovered state, and the cache authentication key never appears in envelope bytes or files.</action>
  <acceptance_criteria>Process tests cover candidate/sync/replace/directory-sync faults, restart, concurrent writes, tampering, malformed envelopes, candidate cleanup, key non-persistence, and denial/high-water order, with each failed replacement preserving the previous authenticated cache.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/accrue-offline-client --filter AtomicOfflineCacheProcessTests</automated>
  </verify>
  <done>The file-backed store has executable crash/recovery evidence and cannot recover unauthenticated or merely candidate state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| corpus/test mutations → verifier | Adversarial test inputs prove untrusted bytes cannot cross admission. |
| concurrent processes → cache path | Multiple writers and crashes contend on one authoritative file. |
| persisted envelope → process memory | Restarted processes must authenticate before trusting high-water or proof bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-223-05 | Tampering | JWS/JWKS/profile | high | mitigate | Canonical mutation matrix rejects algorithm/header/signature/key/claim changes before admission. |
| T-223-06 | Tampering | replacement order | high | mitigate | Persisted issuance/freshness/revision high-water plus equal-revision signed-deny precedence tested across reordered and fresh-process execution. |
| T-223-07 | Denial of service | atomic file replacement | medium | mitigate | Fault-injection returns bounded retry/reconnect results while preserving the previous complete canonical envelope. |
| T-223-08 | Information disclosure | cache authentication key | high | mitigate | Host-provided key remains memory-only; tests scan envelope/files and runtime source for persistence paths. |
| T-223-09 | Repudiation | test evidence | low | accept | Deterministic canonical IDs and named fault cases provide reproducible evidence; runtime audit logging remains host-owned. |
| T-223-SC | Tampering | package dependencies | low | accept | No third-party package installation is introduced; Package Legitimacy Audit is not applicable. |
</threat_model>

<verification>
Run the two focused suites followed by `swift test --package-path packages/accrue-offline-client`; verify the package graph keeps the harness out of both library products and scan `Sources/AccrueOfflineClientCore` for fixture/key-loading symbols.
</verification>

<success_criteria>
- Canonical corpus and all required mutation/rotation/state/order cases pass.
- Repeat, reorder, concurrent, interrupted, and restarted execution cannot weaken signed deny or high-water.
- Every durability failure preserves the previous authenticated canonical cache.
- Test keys, corpus loaders, mutation signing, and crash controls remain outside runtime products.
</success_criteria>

<output>
Create `.planning/phases/223-ios-compatible-accrue-offline-client/223-02-SUMMARY.md` when done.
</output>

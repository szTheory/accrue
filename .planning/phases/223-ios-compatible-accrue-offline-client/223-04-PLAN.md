---
phase: 223-ios-compatible-accrue-offline-client
plan: 04
type: execute
wave: 4
depends_on: [223-03]
files_modified:
  - packages/accrue-offline-client/README.md
  - examples/crosswake_tracer/Package.swift
  - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
  - examples/crosswake_tracer/README.md
  - scripts/ci/verify_ios_offline_client.sh
  - scripts/ci/verify_reference_scenario_contract.sh
  - .github/workflows/ci.yml
autonomous: false
requirements: [IOS-01, IOS-02, IOS-03]
must_haves:
  truths:
    - "D-05: `examples/crosswake_tracer` consumes the standalone package through a local path and remains a feasibility/conformance consumer, not the distributable package."
    - "D-11: merge-blocking package tests run separately from a generic iPhoneOS SDK core-library build at the arm64-apple-ios16.0 floor; the compile lane excludes process harnesses and proves compilation only."
    - "D-12: package test/build success cannot mutate, infer, or promote `examples/crosswake_tracer/capability-report.json`; absent authorized physical-device evidence, overall and capability statuses remain feasibility_blocked."
    - "D-04/D-07/D-14: public documentation names host ownership of authentication, key custody, transport, lifecycle, StoreKit, content policy, and UI, and describes stale as study continuity rather than grant."
    - "The installed runtime package contains no test fixture/key, repository-relative loader, host UI, StoreKit, Crosswake bridge, or physical-device/simulator proof claim."
  artifacts:
    - path: "packages/accrue-offline-client/README.md"
      provides: "Public import, safe-path, ownership, state/copy, and evidence-boundary documentation"
    - path: "examples/crosswake_tracer/Package.swift"
      provides: "Local-path conformance consumer dependency"
      contains: "../../packages/accrue-offline-client"
    - path: "scripts/ci/verify_ios_offline_client.sh"
      provides: "Package tests, generic iOS 16 core compilation, runtime-surface, and feasibility-independence gate"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking macOS iOS-SDK lane and package contract invocation"
  key_links:
    - from: "examples/crosswake_tracer/Package.swift"
      to: "packages/accrue-offline-client/Package.swift"
      via: "SwiftPM local path dependency"
      pattern: "package.*path.*packages/accrue-offline-client"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_ios_offline_client.sh"
      via: "named merge-gate step on a macOS runner"
      pattern: "verify_ios_offline_client"
    - from: "scripts/ci/verify_ios_offline_client.sh"
      to: "examples/crosswake_tracer/capability-report.json"
      via: "read-only assertion that tests/build do not promote feasibility status"
      pattern: "feasibility_blocked"
  prohibitions:
    - statement: "SwiftPM test, macOS/Linux process-test, simulator, generic iOS compilation, tracer-consumer, or canonical-vector success must never be represented as proven Crosswake bridge or physical-device runtime capability."
      status: unverified
      flagged: true
---

<objective>
Make the extracted package consumable and merge-gated while preserving an honest, one-way separation between deterministic client evidence and Crosswake/device feasibility per D-05, D-10, D-11, D-12, D-13, and D-14.

Purpose: Close IOS-01..IOS-03 with a real consumer, comprehensive CI, and public ownership/readiness truth that cannot overclaim runtime proof.
Output: Package README, path-dependent tracer consumer, generic iOS 16 compile gate, full package/test contract gate, and explicit feasibility non-promotion assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-CONTEXT.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-RESEARCH.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-PATTERNS.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-UI-SPEC.md
@.planning/phases/223-ios-compatible-accrue-offline-client/COVERAGE.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-03-SUMMARY.md

API coverage decision: the deterministic detector returned `detected:false`. `COVERAGE.md` records that this is a local SwiftPM package with a narrow host-supplied reconnect protocol, not an external API/SDK/service integration.
</context>

## Artifacts this phase produces

- `packages/accrue-offline-client/README.md` documenting import/configuration, four states, operational reasons, reconnect composition, host ownership, accessible host-copy examples, and proof limits.
- Crosswake tracer `Package.swift` path dependency and source import of `AccrueOfflineClientCore`.
- Revised tracer README classifying the example/capability report as feasibility-only.
- `scripts/ci/verify_ios_offline_client.sh` as the one-command package/core-iOS/evidence-boundary verifier.
- `.github/workflows/ci.yml` merge-blocking macOS lane and `verify_reference_scenario_contract.sh` package-consumer wiring.
- Existing `examples/crosswake_tracer/capability-report.json` retained byte-for-byte as feasibility evidence, never rewritten by package verification.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Confirm the one-way deterministic-evidence versus device-feasibility boundary (D-12)</name>
  <read_first>
    - .planning/phases/223-ios-compatible-accrue-offline-client/223-CONTEXT.md
    - examples/crosswake_tracer/capability-report.json
    - examples/crosswake_tracer/README.md
    - .planning/ROADMAP.md
  </read_first>
  <decision>Confirm that package vectors/tests and generic iOS compilation remain deterministic client evidence only and can never promote Crosswake/device capability without the separately authorized physical-device artifact.</decision>
  <context>Reversing this after publication would invalidate public readiness claims and downstream Phases 224-226; D-12 rates the evidence boundary one-way.</context>
  <options>
    <option id="confirm-evidence-boundary">
      <name>Confirm evidence boundary</name>
      <pros>Preserves honest feasibility_blocked status and the approved downstream external gate.</pros>
      <cons>Package success alone cannot produce a runtime-ready claim.</cons>
    </option>
    <option id="halt-phase">
      <name>Halt before claims</name>
      <pros>Avoids publishing evidence semantics the maintainer no longer accepts.</pros>
      <cons>Blocks Phase 223 completion and requires replacing D-12 through discuss-phase.</cons>
    </option>
  </options>
  <action>Pause before changing consumer documentation or CI evidence gates. Proceed only with `confirm-evidence-boundary`; `halt-phase` leaves all evidence files and claims unchanged.</action>
  <acceptance_criteria>The executor records explicit confirmation that `feasibility_blocked` cannot become `proven` from package test/build/consumer success.</acceptance_criteria>
  <verify>
    <automated>jq -e '.overall_status == "feasibility_blocked" and all(.capabilities[]; .status == "feasibility_blocked")' examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>The one-way evidence gate is resolved without broadening what deterministic compilation proves.</done>
  <resume-signal>Select: confirm-evidence-boundary or halt-phase</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Convert Crosswake tracer into a path-dependent conformance consumer and publish honest host integration guidance</name>
  <reversibility rating="costly">D-05 package import paths and D-12 public evidence language become externally consumed integration/readiness contracts.</reversibility>
  <read_first>
    - packages/accrue-offline-client/README.md (create)
    - examples/crosswake_tracer/Package.swift
    - examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift
    - examples/crosswake_tracer/README.md
    - examples/crosswake_tracer/capability-report.json
    - accrue/guides/first_adopter_ios_bridge.md
    - brandbook/voice.md
    - brandbook/copy.md
    - .planning/phases/223-ios-compatible-accrue-offline-client/223-UI-SPEC.md
  </read_first>
  <files>packages/accrue-offline-client/README.md, examples/crosswake_tracer/Package.swift, examples/crosswake_tracer/Sources/AccrueOfflineClient/AccrueOfflineClient.swift, examples/crosswake_tracer/README.md</files>
  <action>Change the tracer manifest to depend on `../../packages/accrue-offline-client` and import/re-export or adapt to `AccrueOfflineClientCore` instead of compiling a second verifier/cache implementation. Remove duplicated runtime verifier/cache authority from the tracer source while retaining only tracer-specific conformance/feasibility code; all proof application must call the package facade. Write the package README around one safe host job: configure issuer/audience/account/device/JWKS/cache URL/in-memory host key, load cached state, apply compact server proof, or reconnect using a host-authenticated transport. List the exact four states and bounded reasons/actions, state that stale continuity permits downloaded study/progress only and never new value expansion, and use the UI-SPEC literal examples (`Reconnect`, `No offline proof is cached.`, and the verification error text) as documentation-only host rendering guidance. Explicitly assign host ownership for authentication, endpoint/request construction, cache-key custody, Keychain service/access group and Secure Enclave policy, lifecycle, StoreKit, purchase/restore, content policy, localization, accessibility, and UI. Revise the tracer README to call it a local-path conformance/feasibility consumer and state that package tests, generic iOS compilation, and simulator observations do not establish bridge/device proof. Do not edit `capability-report.json`; preserve its existing feasibility_blocked statuses and external physical evidence requirements per D-12.</action>
  <acceptance_criteria>The tracer imports the package rather than duplicating authority; both READMEs name the narrow facade and host ownership, stale non-grant semantics, accessible text-backed seam, and the deterministic-versus-device evidence boundary without claiming StoreKit/Crosswake/device support.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path examples/crosswake_tracer &amp;&amp; swift package --package-path examples/crosswake_tracer describe | grep -F 'accrue-offline-client' &amp;&amp; jq -e '.overall_status == "feasibility_blocked"' examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>A real consumer proves package integration while the capability report and documentation remain feasibility-only.</done>
</task>

<task type="auto">
  <name>Task 2: Add merge-blocking package, generic iOS 16 core compilation, runtime-surface, and feasibility-independence gates</name>
  <read_first>
    - scripts/ci/verify_ios_offline_client.sh (create)
    - scripts/ci/verify_reference_scenario_contract.sh
    - .github/workflows/ci.yml
    - packages/accrue-offline-client/Package.swift
    - examples/crosswake_tracer/capability-report.json
    - examples/crosswake_tracer/physical-device-evidence.md
    - scripts/ci/verify_adoption_proof_matrix.sh
  </read_first>
  <files>scripts/ci/verify_ios_offline_client.sh, scripts/ci/verify_reference_scenario_contract.sh, .github/workflows/ci.yml</files>
  <action>Create `scripts/ci/verify_ios_offline_client.sh` with `set -euo pipefail` and explicit file/tool guards. Run the full package Swift tests (including process/fault targets on the macOS host), then resolve `SDKROOT` using `xcrun --sdk iphoneos --show-sdk-path` and compile only `AccrueOfflineClientCore` with `swift build --package-path packages/accrue-offline-client --triple arm64-apple-ios16.0 --product AccrueOfflineClientCore -Xswiftc -sdk -Xswiftc "$SDKROOT"`; if SwiftPM product selection proves insufficient, use the validated target-selection equivalent while retaining core-only iOS compilation. Add structural checks that runtime `Sources/` contains no repository-relative fixture lookup, private test key, StoreKit/UI/Crosswake host API, or crash harness target dependency, and that test support does contain canonical corpus/key references. Snapshot/hash the capability report before package tests/build and compare afterward, require overall and every capability status remain `feasibility_blocked` unless the existing external validator sees authorized physical-device evidence, and never write evidence/report files. Wire this script as a named merge-blocking step/job on a macOS runner in `.github/workflows/ci.yml`; keep it distinct from Linux/macOS package/process tests so the iOS lane means compile compatibility only. Update `verify_reference_scenario_contract.sh` to run the standalone package verifier and then the path-dependent tracer consumer without letting either invocation feed capability promotion. Preserve unrelated workflow edits.</action>
  <acceptance_criteria>One command runs package tests, core-only generic iOS 16 compilation, runtime/test boundary checks, tracer consumption, and read-only feasibility assertions; CI invokes it on macOS and no successful lane can alter or promote capability evidence.</acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ios_offline_client.sh &amp;&amp; bash scripts/ci/verify_reference_scenario_contract.sh</automated>
  </verify>
  <done>IOS-01..IOS-03 are merge-gated at the honest package-compilation boundary and Crosswake/device feasibility remains external.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package → Crosswake consumer | The consumer may call public domain APIs but cannot regain private verification/cache authority. |
| deterministic CI → readiness evidence | Test/build results must not be treated as external runtime proof. |
| repository test support → installed runtime | Fixtures, keys, mutation tools, and process harnesses must not enter library products. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-223-15 | Elevation of privilege | tracer consumer | high | mitigate | Remove duplicated authority and consume the package facade through a path dependency. |
| T-223-16 | Repudiation | capability claims | high | mitigate | Hash/read-only assert the capability report around package commands and require feasibility_blocked absent external evidence. |
| T-223-17 | Tampering | CI target selection | medium | mitigate | Build the named core product against arm64-apple-ios16.0 and assert the crash harness is outside the selected graph. |
| T-223-18 | Information disclosure | published package surface | high | mitigate | Structural gate separates runtime products from test keys, fixture loaders, raw envelopes, and diagnostics. |
| T-223-19 | Spoofing | physical-device evidence | high | mitigate | Existing external evidence validator remains the only promotion authority; package script never writes report/evidence files. |
| T-223-SC | Tampering | package dependencies | low | accept | The phase adds no third-party dependency; the Package Legitimacy Audit records system frameworks only. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reusable iOS-compatible SwiftPM offline client retaining verification, ordering, durability, and honest proof boundary | 01-04 | COVERED | Tracer, expansions, consumer, and CI collectively deliver the outcome. |
| REQ | IOS-01 | Importable package with canonical proof/high-water/allow-deny parity | 01, 02, 04 | COVERED | Public tracer, full corpus/order suite, consumer and iOS build. |
| REQ | IOS-02 | ES256 server proof, device binding, stale-study-only continuity | 01, 02, 03 | COVERED | Strict admission and mutation/reconnect coverage. |
| REQ | IOS-03 | Authenticated reconnect and verified newer allow/signed-deny replacement | 02, 03, 04 | COVERED | Host protocol, shared admission, process recovery and merge gate. |
| CONTEXT | D-01 | One narrow facade and immutable Sendable values | 01 | COVERED | Public contract tracer. |
| CONTEXT | D-02 | Exactly four states and bounded vocabulary | 01 | COVERED | One-way checkpoint plus canonical cases. |
| CONTEXT | D-03 | Compact bytes only; private verified admission | 01, 03 | COVERED | Direct and reconnect share admission. |
| CONTEXT | D-04 | Host owns commerce/auth/transport/lifecycle/UI | 01, 03, 04 | COVERED | API boundary, prohibition, and docs. |
| CONTEXT | D-05 | Standalone core + optional Apple product; tracer consumer | 01, 03, 04 | COVERED | Products and path dependency. |
| CONTEXT | D-06 | Verification/order/envelope/atomic recovery stay coupled | 01, 02 | COVERED | Core and fault suite. |
| CONTEXT | D-07 | Host supplies URLs/keys/policy/auth/lifecycle; no library key persistence | 01, 03 | COVERED | Construction and Apple helper boundary. |
| CONTEXT | D-08 | Synced same-volume atomic replacement and canonical recovery | 02 | COVERED | Process/fault tests. |
| CONTEXT | D-09 | Explicit ThisDeviceOnly; bounded first-unlock outcome | 03 | COVERED | Apple policy helper/tests. |
| CONTEXT | D-10 | Canonical oracle; merge-blocking mutation/unit tests; test-only fixtures/keys | 02, 04 | COVERED | Corpus suite and CI surface gate. |
| CONTEXT | D-11 | macOS/Linux fault tests plus generic iOS 16 library compile | 02, 04 | COVERED | Separate process and core-only compile lanes. |
| CONTEXT | D-12 | Capability/device evidence remains external | 04 | COVERED | One-way checkpoint, read-only report assertions. |
| CONTEXT | D-13 | Phoenix-style safe facade and domain nouns | 01, 03, 04 | COVERED | API and integration docs. |
| CONTEXT | D-14 | No UI; accessible literal host-rendering seam | 01, 04 | COVERED | Closed values and UI-SPEC copy/docs. |
| RESEARCH | — | CryptoKit/Foundation/Security system stack; no third-party package | 01-04 | COVERED | Package manifest and threat registers add no install. |
| RESEARCH | — | Opaque verified admission and coupled atomic store | 01, 02 | COVERED | Tracer and expansion tests. |
| RESEARCH | — | Test-only canonical fixture/key support, mutation and rotation coverage | 02 | COVERED | Core tests. |
| RESEARCH | — | Host byte transport and policy-only Apple helper | 03 | COVERED | Narrow protocols/products. |
| RESEARCH | — | Core-only iOS compile; process harness excluded | 02, 04 | COVERED | Target graph and CI. |
| RESEARCH | — | Crosswake report cannot become proven from package success | 04 | COVERED | Read-only evidence gate. |

<verification>
Run `bash scripts/ci/verify_ios_offline_client.sh` and `bash scripts/ci/verify_reference_scenario_contract.sh`; confirm `git diff -- examples/crosswake_tracer/capability-report.json examples/crosswake_tracer/physical-device-evidence.md` is empty and the API coverage declaration validates as a no-integration decision.
</verification>

<success_criteria>
- A real path-dependent Crosswake consumer imports the package and passes without duplicated authority.
- Full Swift tests and the separate generic iOS 16 core-only compilation lane are merge-blocking.
- Public docs name the safe facade, host ownership, stale non-grant behavior, and accessible text seam.
- Capability/evidence files remain unchanged and feasibility_blocked without separately authorized physical-device proof.
- The source audit has no missing GOAL, REQ, RESEARCH, or CONTEXT item.
</success_criteria>

<output>
Create `.planning/phases/223-ios-compatible-accrue-offline-client/223-04-SUMMARY.md` when done.
</output>

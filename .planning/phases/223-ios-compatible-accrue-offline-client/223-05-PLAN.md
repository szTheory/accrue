---
phase: 223-ios-compatible-accrue-offline-client
plan: 05
type: execute
wave: 5
depends_on: [223-04]
files_modified:
  - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift
  - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/CanonicalJSONAdmission.swift
  - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift
  - packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineReconnectTests.swift
  - packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift
autonomous: true
gap_closure: true
requirements: [IOS-01, IOS-02, IOS-03]
must_haves:
  truths:
    - "IOS-01/IOS-02, D-03/D-10: compact JWS admission rejects duplicate object members at every nesting level in protected headers and payloads, then validates the complete canonical v1.59 claim profile before any cache mutation."
    - "IOS-01/IOS-02: the required profile includes a non-empty bounded jti, exact single-member cnf.jkt binding, normalized bounded plans/features, positive integral quantities, a non-empty effective allow authority, and only signed_denial/access_unavailable/superseded/device_revoked denial reasons."
    - "IOS-03, D-06/D-08: under the per-path lock, an absent or invalid existing cache carries no high-water authority; malformed or HMAC-invalid bytes can be quarantined and replaced by a newly verified direct or reconnect proof."
    - "IOS-03, D-08: any candidate-write, candidate-file-sync, atomic-replace, parent-directory-sync, or authenticated rollback/recovery failure returns a bounded failure while leaving the previous complete authenticated cache recoverable."
    - "IOS-01/IOS-03: simultaneous equal-revision allow and signed deny admissions always converge to denied, and the process harness exits unsuccessfully whenever public admission returns invalid."
    - "Behavior-unverified closure: load recovery is read-only for absent/tampered/unrecoverable state, malformed proof variants preserve prior bytes, and concurrent profile/account/device/time-invalid proofs never gain authority."
    - "D-01/D-02/D-04/D-12: the public facade and four Sendable states remain unchanged; no StoreKit, host-auth, host UI, Crosswake bridge, simulator/device evidence, or feasibility promotion surface is added."
  artifacts:
    - path: "packages/accrue-offline-client/Sources/AccrueOfflineClientCore/CanonicalJSONAdmission.swift"
      provides: "Internal duplicate-preserving JSON object-member admission used before Foundation's lossy map decoding"
    - path: "packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift"
      provides: "Complete canonical ES256/JWS claim-profile verification feeding only opaque VerifiedOfflineProof values to persistence"
      exports: ["OfflineEntitlementClient", "OfflineEntitlementClient.applyServerProof(_:now:)", "OfflineEntitlementClient.loadCachedState(now:)"]
    - path: "packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift"
      provides: "Recoverable invalid-cache replacement and deterministic transactional fault seams behind the existing internal cache boundary"
    - path: "packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift"
      provides: "Signed adversarial profile, immutable-cache, load-recovery, malformed-input, and concurrent-invalid authority tests"
    - path: "packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineReconnectTests.swift"
      provides: "Direct/reconnect recovery proof for malformed and HMAC-invalid canonical cache bytes"
    - path: "packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift"
      provides: "Strict equal-revision denial convergence and deterministic atomic-stage preservation evidence"
  key_links:
    - from: "OfflineEntitlementClient.verify(_:now:)"
      to: "CanonicalJSONAdmission"
      via: "both decoded JWS JSON segments pass duplicate-member rejection before JSONSerialization is allowed to collapse objects"
      pattern: "CanonicalJSONAdmission"
    - from: "OfflineEntitlementClient.applyServerProof(_:now:)"
      to: "AtomicOfflineCache.replace(_:)"
      via: "only a fully profile-validated internal VerifiedOfflineProof reaches locked ordering and replacement"
      pattern: "cache\.replace\(verified\)"
    - from: "AtomicOfflineCache.replace(_:)"
      to: "authenticated prior envelope or invalid quarantine"
      via: "locked tri-state inspection separates absent, authenticated, and invalid prior bytes before high-water comparison"
      pattern: "authenticated|invalid|absent"
    - from: "OfflineEntitlementClient.reconnect(using:now:)"
      to: "recoverable replacement"
      via: "host-returned Data continues to delegate to the exact direct apply path"
      pattern: "applyServerProof"
    - from: "AccrueOfflineCacheCrashHarness"
      to: "AtomicOfflineCacheProcessTests"
      via: "nonzero exit on invalid admission makes concurrent process assertions authoritative"
      pattern: "terminationStatus"
---

<objective>
Close every failed and behavior-unverified Phase 223 verifier item by making canonical proof admission parser-differential-safe, making verified replacement recover from unauthenticated prior bytes without weakening authenticated-state durability, and proving denial ordering plus every declared atomic-write boundary.

Purpose: Restore IOS-01 through IOS-03 as executable security and durability outcomes while retaining D-01 through D-14's public API, host-ownership, stale-study, and non-promotion fences.
Output: Strict internal JSON/claim admission, recoverable transactional cache replacement, and exhaustive Swift unit/process evidence for all verification gaps.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-CONTEXT.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-RESEARCH.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-PATTERNS.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-VERIFICATION.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-01-SUMMARY.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-02-SUMMARY.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-03-SUMMARY.md
@.planning/phases/223-ios-compatible-accrue-offline-client/223-04-SUMMARY.md
@accrue/lib/accrue/entitlements/offline/proof.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace strict signed-proof rejection and verified recovery through the public facade</name>
  <reversibility rating="costly">This repairs the already-published D-03/D-06 admission and persistence invariants without changing their public shape; undoing it would reintroduce cross-language acceptance and availability defects.</reversibility>
  <read_first>
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/CanonicalJSONAdmission.swift (create)
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift
    - accrue/lib/accrue/entitlements/offline/proof.ex (canonical duplicate/schema/normalization contract)
    - accrue/priv/entitlements/v1.59-offline-golden-vectors.json (language-neutral oracle)
    - .planning/phases/223-ios-compatible-accrue-offline-client/223-VERIFICATION.md (failed truths 1-3, 8, 18)
  </read_first>
  <files>packages/accrue-offline-client/Sources/AccrueOfflineClientCore/CanonicalJSONAdmission.swift, packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift, packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift</files>
  <behavior>
    - Test 1: a correctly signed payload containing a duplicate top-level claim and one containing a duplicate nested cnf member each return invalid(malformed), and byte-for-byte comparison proves an already authenticated allow cache is unchanged.
    - Test 2: a correctly signed protected header with a duplicate alg, typ, or kid member returns invalid(malformed) before ordinary object decoding and leaves the authenticated cache unchanged.
    - Test 3: after malformed existing cache bytes, applying a valid allow returns fresh and a new client loads fresh from the authenticated replacement.
    - Test 4: after an envelope with an invalid HMAC, applying a valid signed deny returns denied and a new client loads denied from the authenticated replacement.
  </behavior>
  <action>Start RED with the four end-to-end behaviors above. Add internal `CanonicalJSONAdmission` that parses JSON string escapes and nesting while retaining object-member occurrence order, rejects duplicates recursively in both protected-header and payload bytes, and returns no public value; call it before `JSONSerialization` in `verify(_:now:)` so map decoding cannot resolve ambiguous authority per D-03/D-10. Keep exact `alg`, `typ`, and `kid` header membership and ES256 signature verification unchanged. Refactor `AtomicOfflineCache`'s locked prior-state inspection into explicit absent, authenticated, and invalid outcomes. Only authenticated state supplies `ProofHighWater`; invalid bytes have no ordering authority. Move invalid canonical bytes to a same-directory uniquely named quarantine under the lock, persist the already verified candidate by the D-08 candidate-write/sync/replace/directory-sync path, remove quarantine only after success, and restore/quarantine safely on an unsuccessful replacement so invalid bytes never become accepted state. Do not expose the JSON scanner, quarantine format, raw cache bytes, `VerifiedOfflineProof`, replacement metadata, or fault control through the D-01 public facade. Extend test-only signing support to sign caller-provided raw header/payload JSON so duplicate-member cases are genuinely signed rather than mutated after signature creation.</action>
  <acceptance_criteria>Signed duplicate members at any object nesting level are rejected before lossy decoding and cannot alter authenticated bytes; a newly verified direct allow or deny replaces malformed and HMAC-invalid prior bytes and remains loadable by a fresh client; only authenticated prior state participates in ordering; the existing public API and four-state Sendable boundary are byte-for-byte source-compatible.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/accrue-offline-client --filter OfflineEntitlementClientTests</automated>
  </verify>
  <done>The narrow public apply path proves both strict signed duplicate rejection and recovery from unauthenticated canonical-cache bytes end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust the canonical claim profile and previously unverified state transitions</name>
  <read_first>
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/CanonicalJSONAdmission.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineReconnect.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineReconnectTests.swift
    - accrue/lib/accrue/entitlements/offline/proof.ex (claims_from/1, normalized_strings/1, normalized_quantities/1, validate_denial/1)
    - .planning/phases/223-ios-compatible-accrue-offline-client/223-VERIFICATION.md (behavior-unverified items 6, 10, 17)
  </read_first>
  <files>packages/accrue-offline-client/Sources/AccrueOfflineClientCore/OfflineEntitlementClient.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineReconnectTests.swift</files>
  <behavior>
    - Test 1: missing, empty, non-string, or over-bounded jti; extra/missing/non-string cnf members; and incorrect jkt are rejected with bounded malformed/device-mismatch results as the canonical contract requires.
    - Test 2: plans/features must be bounded strings in sorted unique order; quantities must be bounded-key maps of positive integers; allow requires at least one effective plan, feature, or quantity; allow forbids denial_reason; deny requires exactly one of signed_denial, access_unavailable, superseded, or device_revoked.
    - Test 3: all required claim keys and only the disposition-specific exact key set are accepted; wrong JSON types, booleans masquerading as numbers, nulls, missing keys, extra keys, and invalid temporal/revision integers return invalid without mutation.
    - Test 4: absent load returns invalid(malformed); malformed/HMAC-invalid load returns invalid(cache_recovery_failed); every load is read-only and byte comparisons show no canonical or quarantined file mutation.
    - Test 5: empty, truncated compact segments, UTF-8 null-equivalent, JSON null, malformed JSON, and bad base64 input return invalid(malformed) while a seeded authenticated proof remains recoverable byte-for-byte.
    - Test 6: concurrent valid admission mixed with separately signed wrong profile, account, device, future-not-before, expired, and clock-rollback inputs retains only the verified ordered candidate; no invalid-context proof becomes cached authority.
    - Test 7: direct apply and `reconnect(using:now:)` each restore service after both malformed and HMAC-invalid existing bytes, while reconnect continues to accept only host-returned compact Data per D-04.
  </behavior>
  <action>Build signed table-driven adversarial cases with `GoldenVectorFixtureSupport`, using raw JSON signing where duplicate/type/null fidelity matters. Complete `verify(_:now:)` to mirror the canonical server profile: exact disposition-specific keys; bounded non-empty jti/sub and exact issuer/audience; exact single-member cnf with bounded jkt; nonnegative integral revision/iat/nbf/fresh_until/exp with the established temporal order; normalized plans/features capped at 100, with non-empty bounded strings in sorted unique order; quantities capped at 100 with non-empty bounded keys and strictly positive integral values; at least one effective benefit for allow; no allow denial_reason; and the four allowed bounded deny reasons. Preserve canonical error mapping where the corpus distinguishes issuer, audience, account/device, time, key, algorithm, and type failures; schema-shape failures remain malformed. Add byte snapshots around every rejected application and every `loadCachedState` call. Exercise absent, malformed, and HMAC-invalid load behavior without cleanup or quarantine mutation, then prove recovery occurs only after a separately verified direct/reconnect candidate. Add task-group or equivalent concurrent tests mixing the valid proof with every invalid context class, then load through a fresh client and assert only valid ordered authority survived. Keep D-02 stale behavior and the four public cases unchanged, and add no host credentials, lifecycle, commerce, UI, bridge, or evidence handling.</action>
  <acceptance_criteria>Every required canonical header/claim/schema/normalization rule has a signed positive or negative test; malformed and null-equivalent inputs plus all read-only recovery outcomes have byte-preservation proof; direct and reconnect restore malformed/HMAC-invalid caches; concurrent invalid-context verification never changes cached authority.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/accrue-offline-client --filter OfflineEntitlementClientTests &amp;&amp; swift test --package-path packages/accrue-offline-client --filter OfflineReconnectTests</automated>
  </verify>
  <done>The verifier's full canonical claim profile and all three behavior-unverified transition groups are executable, merge-blocking evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove denial convergence and transactional durability at every filesystem stage</name>
  <read_first>
    - packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift
    - packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift
    - packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift
    - .planning/phases/223-ios-compatible-accrue-offline-client/223-PATTERNS.md (verified candidate/sync/replace/directory-sync and fresh-process patterns)
    - .planning/phases/223-ios-compatible-accrue-offline-client/223-VERIFICATION.md (failed truths 13, 15, 18 and harness defect)
  </read_first>
  <files>packages/accrue-offline-client/Sources/AccrueOfflineClientCore/AtomicOfflineCache.swift, packages/accrue-offline-client/Sources/AccrueOfflineCacheCrashHarness/main.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/GoldenVectorFixtureSupport.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientCoreTests/OfflineEntitlementClientTests.swift, packages/accrue-offline-client/Tests/AccrueOfflineClientProcessTests/AtomicOfflineCacheProcessTests.swift</files>
  <behavior>
    - Test 1: repeated fresh-process races between the canonical equal-revision allow and signed denial terminate successfully and always load denied; fresh is never an accepted terminal assertion.
    - Test 2: the crash harness exits nonzero when `applyServerProof` returns any invalid state, including superseded and cache-write failures, so the parent cannot mistake rejected admission for a successful writer.
    - Test 3: deterministic failures at candidate write, candidate file synchronize, atomic destination replace, and parent-directory synchronize each return cache_write_failed and leave the seeded authenticated cache bytes and loaded state unchanged.
    - Test 4: deterministic authenticated rollback/recovery failure never exposes partial/unauthenticated bytes; the prior canonical authenticated state remains recoverable and all temporary candidate, backup, and quarantine artifacts are handled by the documented recovery invariant.
  </behavior>
  <action>Add an internal, test-injectable file-operation seam to `AtomicOfflineCache` with named single-shot stages `candidateWrite`, `candidateFileSync`, `atomicReplace`, `parentDirectorySync`, and `authenticatedRecovery`; production initialization continues to select real Foundation/POSIX operations, and no fault selector enters a public API or installed host configuration. Retain prior authenticated bytes or a same-volume authenticated backup under the per-path lock until candidate replacement and parent-directory sync both succeed. On any stage failure, restore the old canonical authenticated envelope atomically before returning; recovery must authenticate the rollback source and never install a partial, invalid, or unverified file. Make cleanup deterministic and ensure coordinator high-water advances only after the complete transaction commits. In the process tests, race equal-revision canonical allow and deny writers across multiple fresh-process iterations, require both processes to report genuine successful admission, and assert final denied every time. Change `AccrueOfflineCacheCrashHarness` to inspect the returned four-state value: exit zero only for fresh/staleOffline/denied success appropriate to the invoked operation and exit a stable nonzero code for invalid, printing no proof, key, envelope, or authentication material. Add deterministic stage-fault tests using the internal seam; for every stage snapshot the seeded authenticated bytes, assert the bounded failure, construct a fresh client, and assert the exact prior state remains recoverable. Preserve D-08's same-directory candidate, file sync, atomic replacement, and supported directory sync ordering rather than swallowing a durability error.</action>
  <acceptance_criteria>Concurrent equal-revision allow/deny writers converge only to denied; the harness cannot report invalid admission as success; each named filesystem or authenticated-recovery fault returns a bounded failure and preserves the exact prior complete authenticated cache with no partial authority.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/accrue-offline-client --filter AtomicOfflineCacheProcessTests &amp;&amp; swift test --package-path packages/accrue-offline-client</automated>
  </verify>
  <done>Ordering, admission-status propagation, and every declared transactional durability boundary are enforced by deterministic tests.</done>
</task>

</tasks>

## Artifacts this phase produces

- `CanonicalJSONAdmission.swift`: private duplicate-aware JSON admission for JWS protected headers and payloads.
- Hardened `OfflineEntitlementClient.swift`: exact canonical v1.59 schema/profile validation before opaque admission.
- Hardened `AtomicOfflineCache.swift`: invalid-cache recovery and authenticated rollback across all declared atomic stages.
- Expanded core, reconnect, harness, and process tests closing every failed and behavior-unverified verification item.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host/reconnect transport → compact JWS verifier | Untrusted bytes must pass duplicate-safe JSON admission, ES256, exact profile, account/device, and time checks before acquiring authority. |
| verified proof → authenticated cache | Only internal opaque proof values cross ordering and persistence; invalid prior bytes carry no high-water authority. |
| canonical cache → filesystem candidates/backups/quarantine | Partial writes and unauthenticated files must never become canonical authority or erase a prior authenticated state. |
| child process → process test | Exit status must communicate admission success/failure without leaking proof or key material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-223-20 | Tampering | `CanonicalJSONAdmission` / `verify` | high | mitigate | Reject duplicate members recursively before map decoding and validate exact signed header/claim schema before signature-derived admission. |
| T-223-21 | Spoofing | account/device/profile bindings | high | mitigate | Signed adversarial and concurrent-invalid tests cover issuer, audience, account, exact cnf.jkt, key, and time contexts. |
| T-223-22 | Denial of service | invalid existing cache | high | mitigate | Locked tri-state inspection quarantines unauthenticated bytes and permits only a newly verified proof to restore service. |
| T-223-23 | Tampering | high-water concurrency | high | mitigate | Serialize per path and require equal-revision signed denial to dominate regardless of process scheduling. |
| T-223-24 | Tampering | atomic write/rollback | high | mitigate | Keep authenticated prior bytes until candidate write, file sync, replace, and directory sync commit; deterministically test rollback at every stage. |
| T-223-25 | Repudiation | crash harness exit status | medium | mitigate | Nonzero exit for every invalid admission prevents tests from recording rejected writers as successful. |
| T-223-26 | Information disclosure | harness/test diagnostics | medium | mitigate | Keep keys/fixtures/fault control in test targets and environment; emit no proof, envelope, HMAC, or key material. |
| T-223-SC | Tampering | package dependencies | low | accept | No npm, pip, cargo, or new Swift package dependency is introduced; CryptoKit/Foundation remain system frameworks. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan / task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reusable iOS client retains strict ES256/device verification, ordering, recoverable atomic cache, and honest compile boundary | 05 / 1-3 | COVERED | Repairs the three verifier blockers; previously verified package/iOS/evidence artifacts remain untouched. |
| REQ | IOS-01 | Canonical verification, high-water, and allow/deny replacement | 05 / 1-3 | COVERED | Exact signed schema, denial race, and persistence faults. |
| REQ | IOS-02 | Server ES256 proof, device binding, stale-study-only continuity | 05 / 1-2 | COVERED | Strict parser/profile and concurrent invalid-context proof; four-state/stale boundary preserved. |
| REQ | IOS-03 | Reconnect admits only verified newer allow/signed deny | 05 / 1-3 | COVERED | Direct/reconnect corrupt-cache recovery and transactional ordering. |
| CONTEXT | D-01 | Narrow facade and immutable Sendable values | 05 / 1-3 | COVERED | No public symbol expansion. |
| CONTEXT | D-02 | Exactly four states and bounded vocabulary | 05 / 1-3 | COVERED | Tests consume existing states/reasons/actions; no fifth state. |
| CONTEXT | D-03 | Compact bytes only; opaque verified replacement | 05 / 1-2 | COVERED | Strict signed admission remains private. |
| CONTEXT | D-04 | StoreKit/auth/transport/lifecycle/UI host-owned | 05 / 1-3 | COVERED | Reconnect remains byte-only; prohibited host surfaces excluded. |
| CONTEXT | D-05 | Standalone package and optional Apple boundary | 05 / 1-3 | COVERED | Repairs stay inside existing core/test targets. |
| CONTEXT | D-06 | Verification/order/envelope/recovery coupled | 05 / 1-3 | COVERED | One core transaction owns all authority transitions. |
| CONTEXT | D-07 | Host supplies URL/key; library never persists key | 05 / 1-3 | COVERED | Existing configuration retained; key non-disclosure asserted. |
| CONTEXT | D-08 | Candidate write, sync, atomic replace, directory sync, recover prior complete cache | 05 / 1, 3 | COVERED | Invalid-cache recovery plus every stage fault. |
| CONTEXT | D-09 | Explicit ThisDeviceOnly policy | 05 | COVERED | Existing verified Apple helper untouched. |
| CONTEXT | D-10 | Canonical corpus, adversarial/order/recovery tests, test-only keys | 05 / 1-3 | COVERED | Raw signed adversarial cases and fault suite live under tests. |
| CONTEXT | D-11 | macOS/Linux fault tests and iOS 16 compilation | 05 / 3 | COVERED | Process tests expanded; existing compile gate rerun overall. |
| CONTEXT | D-12 | No feasibility promotion | 05 | COVERED | Evidence/report files untouched and existing gate rerun. |
| CONTEXT | D-13 | One obvious safe domain path | 05 / 1-2 | COVERED | All direct/reconnect bytes use the same facade admission. |
| CONTEXT | D-14 | No UI; bounded host-rendering semantics | 05 | COVERED | Public state vocabulary remains unchanged. |
| RESEARCH | — | Strict compact JWS/profile/JWKS input validation | 05 / 1-2 | COVERED | Duplicate-aware parsing and exact canonical claims. |
| RESEARCH | — | Authenticated high-water and signed-deny precedence | 05 / 3 | COVERED | Equal-revision process race requires deny. |
| RESEARCH | — | Same-volume synced atomic replacement and recovery | 05 / 1, 3 | COVERED | Invalid cache recovery and transaction rollback. |
| RESEARCH | — | Fixtures/private keys/fault harness excluded from runtime API | 05 / 1-3 | COVERED | All signing and injection controls are internal/test-only. |
| VERIFICATION | failed-1 | Duplicate-safe full canonical JWS/claim admission | 05 / 1-2 | COVERED | Signed header/payload/nested duplicates and full schema table. |
| VERIFICATION | failed-2 | Direct/reconnect replacement after malformed or HMAC-invalid cache | 05 / 1-2 | COVERED | Both entry paths and both invalid-cache classes. |
| VERIFICATION | failed-3 | Concurrent deny ordering and every durability stage | 05 / 3 | COVERED | Strict harness status plus five named failure seams. |
| VERIFICATION | unverified-1 | Read-only absent/tampered/unrecoverable load | 05 / 2 | COVERED | Bounded outcomes and byte snapshots. |
| VERIFICATION | unverified-2 | Malformed proof variants preserve cache | 05 / 2 | COVERED | Empty/truncated/null-equivalent/malformed table. |
| VERIFICATION | unverified-3 | Concurrent invalid contexts gain no authority | 05 / 2 | COVERED | Profile/account/device/time concurrency matrix. |
| CONTEXT | deferred fence | StoreKit, host UI/auth, bridge APIs, simulator/device proof, feasibility promotion | — | EXCLUDED | Explicitly belongs to later or separately authorized work. |

<verification>
Run `swift test --package-path packages/accrue-offline-client`, then `bash scripts/ci/verify_ios_offline_client.sh` and `bash scripts/ci/verify_reference_scenario_contract.sh`. Confirm `git diff -- examples/crosswake_tracer/capability-report.json examples/crosswake_tracer/physical-device-evidence.md` is empty. The focused suites must demonstrate exact prior-cache byte preservation, denied-only race convergence, and nonzero harness status for invalid admission.
</verification>

<success_criteria>
- All signed duplicate-key and full canonical-profile adversarial proofs return bounded invalid states without changing authenticated cache bytes.
- Valid direct and reconnect proofs restore service after malformed and HMAC-invalid canonical-cache bytes.
- Absent/tampered loads are read-only; malformed inputs and concurrent invalid-context proofs never gain authority.
- Equal-revision cross-process allow/deny races always finish denied, and the harness fails rejected admission.
- Candidate write, file sync, atomic replace, parent directory sync, and authenticated recovery faults each preserve the prior complete authenticated cache.
- The standalone package still compiles for iOS 16, the public API/four-state seam remains unchanged, and feasibility evidence remains unmodified and blocked.
</success_criteria>

<output>
Create `.planning/phases/223-ios-compatible-accrue-offline-client/223-05-SUMMARY.md` when done.
</output>

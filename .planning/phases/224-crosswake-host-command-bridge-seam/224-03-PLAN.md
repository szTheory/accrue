---
phase: 224-crosswake-host-command-bridge-seam
plan: 03
type: execute
wave: 3
depends_on: [224-02]
files_modified:
  - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
  - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
  - scripts/ci/verify_crosswake_host_commands.sh
  - "Crosswake test paths recorded by 224-01"
autonomous: true
requirements: [BRDG-01, BRDG-02]
must_haves:
  truths:
    - "D-13/BRDG-01: deterministic tests prove every mandatory validation layer precedes delegate dispatch and only exact active manifest capability ∩ registry/version reaches the delegate."
    - "D-13/BRDG-02: malformed/oversized, undeclared/unregistered, version-drifted, cross-origin/subframe, inactive-route, throwing/cancelled, duplicate-complete, and navigation-race cases invoke no unauthorized delegate/reply and produce at most one bounded terminal outcome."
    - "D-08/D-09/D-12: API and privacy tests prove the host surface lacks WebKit/reply transport and all typed schemas, denials, telemetry, guides, and evidence exclude sensitive payload material."
    - "D-14: this phase adds no end-user visual system; any later host-facing state is outside this seam."
    - "FLAGGED-UNVERIFIED specless assumptions BRDG-01/BRDG-02 and retained bespoke prohibitions remain visibly unresolved despite complete deterministic phase evidence."
  artifacts:
    - path: "Crosswake tests listed in 224-CROSSWAKE-SOURCE-AUDIT.md"
      provides: "Admission, descriptor/API/privacy, denial, one-shot, cancellation, and navigation-race proof at the exact pin"
    - path: "scripts/ci/verify_crosswake_host_commands.sh"
      provides: "Exact-pin admission and lifecycle/race test modes"
  key_links:
    - from: "scripts/ci/verify_crosswake_host_commands.sh full"
      to: "audit-recorded Crosswake native tests"
      via: "remote/revision/diff/audit checks followed by exact admission and lifecycle filters"
      pattern: "full"
  assumptions:
    - id: "SPEC-FALLBACK-BRDG-01"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BRDG-01 was unclassified by the specless probe and has no explicit edge-verification tier."
    - id: "SPEC-FALLBACK-BRDG-02"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BRDG-02 was unclassified by the specless probe and has no explicit prohibition-verification tier."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: The seam must not silently become a generic commerce/plugin API or acquire Accrue/StoreKit authority."
      category: product
      status: unresolved
      verification: null
    - statement: "FLAGGED-UNVERIFIED: Evidence and telemetry must not expose adopter identity or sensitive billing/account/device material."
      category: privacy
      status: unresolved
      verification: null
    - statement: "FLAGGED-UNVERIFIED: Deterministic bridge success must not be presented as entitlement grant or physical-device runtime proof."
      category: transparency
      status: unresolved
      verification: null
---

<objective>
Close BRDG-01 and BRDG-02 with exhaustive deterministic negative/race proof at the final reviewed Crosswake revision per D-13.

Purpose: Demonstrate not merely that the happy path works, but that every bypass, stale-route, handler-failure, and reply-injection class fails closed at the authoritative Crosswake seam.
Output: Complete pinned-source test suites, static/API privacy proof, and the final immutable reviewed patch/audit consumed by Plan 224-04's evidence handoff.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-RESEARCH.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-PATTERNS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-VALIDATION.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
@.planning/phases/224-crosswake-host-command-bridge-seam/224-02-SUMMARY.md
@accrue/guides/first_adopter_ios_bridge.md
</context>

## Artifacts this phase produces

- In the exact source-audit-recorded Crosswake test paths: admission/descriptor/API/privacy tests and denial/epoch/one-shot race tests. Their exact suite, type, helper, fake, and test symbol names must be appended exhaustively to `224-CROSSWAKE-SOURCE-AUDIT.md`; this plan does not guess them before source access.
- Final reviewed Crosswake patch revision and stable diff identity in `crosswake-source-lock.json`, with base/diff/upstream-convergence and exhaustive files/symbols in `224-CROSSWAKE-SOURCE-AUDIT.md`.
- `scripts/ci/verify_crosswake_host_commands.sh full` as the combined exact-pin native admission/lifecycle/API/privacy suite; Plan 224-04 adds the Accrue evidence-boundary checks.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove ordered admission, fixed schemas, delegate isolation, and privacy with deterministic negatives</name>
  <precondition>`CROSSWAKE_SOURCE_ROOT` matches the exact reviewed Plan 02 lock and both pinned `admission` and `lifecycle` modes pass.</precondition>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - Every audit-recorded Crosswake envelope validator, protocol/runtime version validator, frame/content-world/origin validator, pack/manifest/route/epoch validator, capability/registry/schema implementation, delegate API, denial encoder, telemetry event, and admission/API test path
  </read_first>
  <files>Only the exact Crosswake admission/descriptor/API/privacy test and necessary implementation paths recorded in 224-CROSSWAKE-SOURCE-AUDIT.md, plus the audit, lock, and verification runner</files>
  <behavior>
    - Test 1: malformed, duplicate-member, unknown-field, wrong-type, and oversized envelope/schema inputs invoke the delegate zero times and yield one bounded malformed denial.
    - Test 2: bad protocol/runtime version, subframe/content-world, cross-origin, missing pack/manifest, inactive route, stale epoch, undeclared/unregistered command, and version mismatch each invoke the delegate zero times.
    - Test 3: manifest-only and registration-only cases fail independently; all four exact matching command/version pairs can dispatch through the same single path.
    - Test 4: success response schema rejects unknown/duplicate/oversized output before reply and cannot carry sensitive payload categories.
    - Test 5: public/API inspection proves delegate request/context/outcome has no WebKit, raw data/envelope, JavaScript evaluation, callback, frame-selection, or reply-transport member.
    - Test 6: telemetry for attempts and every denial/result is limited to the declared D-12 field/type vocabulary and contains no payload, stack, allowlist, credential, receipt/proof, account/device, or adopter identity.
  </behavior>
  <action>Expand the audit-recorded native tests with table-driven/fake-driven cases for every D-13 pre-dispatch failure, asserting both delegate invocation count zero and the exact bounded bridge outcome. Exercise boundaries immediately below/at/above each source-recorded byte/field/container limit; duplicate and unknown members; all fixed request/response descriptors; declaration-only, registration-only, version drift, cross-origin, subframe/content-world, missing or inactive pack/manifest/route, and stale epoch. Prove the same audit-recorded ordered dispatcher is used for all four command IDs and there is no alternate admission or reply route.

Add an API/static or compile-time contract test, appropriate to the pinned build system, that enumerates the host-visible request, cancellation, and outcome surface and fails if WebKit types, raw message/data/envelope access, JavaScript evaluation, callbacks, reply handlers, or target-frame control become reachable. Add privacy fixtures populated with synthetic sensitive-shaped values and assert these values cannot enter a typed request/response and never appear in stable denial text, telemetry, audit output, or test evidence. Do not encode real adopter values. Keep setup diagnostics actionable only at install time and runtime denials opaque per D-06/D-07/D-08/D-09/D-12/D-13. Review/commit any test-discovered production fixes in the same authorized source lane, then update the exact revision/diff/audit inventory and runner modes.</action>
  <acceptance_criteria>Every validation/schema failure is proven to invoke the delegate zero times; every four-command success uses one ordered dispatcher; malformed host output cannot reply; delegate/API surface is compile/static-proven transport-incapable; and synthetic privacy fixtures prove payload/telemetry/evidence data minimization at the updated immutable revision.</acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_crosswake_host_commands.sh admission</automated>
  </verify>
  <done>BRDG-01's admission boundary and the non-race half of BRDG-02 have exhaustive deterministic fail-closed proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove handler failure, cancellation, double completion, and navigation races cannot inject stale replies</name>
  <precondition>The source checkout matches Task 1's updated lock and its admission/API/privacy suite passes.</precondition>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - Every audit-recorded Crosswake navigation hook, route-state/epoch store, pending invocation/task store, delegate adapter, outcome encoder, terminal reply owner, suppression telemetry, deterministic scheduler/fake, and lifecycle/race test path
  </read_first>
  <files>Only the exact Crosswake denial/epoch/terminalization/race test and necessary implementation paths recorded in 224-CROSSWAKE-SOURCE-AUDIT.md, plus the audit, lock, and verification runner</files>
  <behavior>
    - Test 1: throwing and explicitly failing handlers produce one bounded denial and cannot directly or indirectly invoke transport.
    - Test 2: cooperative cancellation and a handler that ignores cancellation both fail to reply after main-frame navigation invalidates the captured epoch.
    - Test 3: barriers cover navigation before handler start, during work, after outcome/before terminal claim, and after claim; no old-route result targets the new route.
    - Test 4: duplicate/concurrent completion, throw-after-success, and cancellation-after-success create at most one terminal action.
    - Test 5: origin, manifest, route, or epoch mutation immediately before reply suppresses or safely denies according to the recorded protocol and never yields success.
  </behavior>
  <action>Use deterministic barriers, continuations, or the pinned source's scheduler fake—never sleeps—to place navigation/cancellation and duplicate completion at each meaningful boundary: admitted before delegate start, while suspended, after host outcome but before binding recheck, during terminal claim, and after terminalization. Cover a throwing delegate, closed failure outcome, cooperative cancellation, non-cooperative work, concurrent duplicate outcomes, success followed by throw, and cancellation after success. Mutate exact origin, installed manifest, active route, and epoch independently immediately before reply. Assert at most one terminal action, no old-route success after any binding change, no host access to transport, bounded `handler_failed`/`reply_expired` behavior, pending-work cleanup, and bounded suppression telemetry. Assert command success remains a transport result only and cannot mutate or expose an entitlement-grant state.

Run the race suite repeatedly using its deterministic iteration/seed facility and record that command in the audit. Review and commit fixes, update the final source revision/diff/symbol inventory/convergence status, and make the full runner execute baseline, admission, lifecycle/race, and API/privacy filters against this exact final commit per D-08/D-10/D-11/D-12/D-13.</action>
  <acceptance_criteria>All handler failure/cancellation/double-completion and four binding-mutation races are deterministic and green; a stale or duplicate result can never target a new route or exceed one terminal action; pending work is cleaned up; no outcome changes entitlement authority; and the final reviewed source pin and audit cover every production/test symbol.</acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_crosswake_host_commands.sh lifecycle &amp;&amp; bash scripts/ci/verify_crosswake_host_commands.sh full</automated>
  </verify>
  <done>BRDG-02 has deterministic failure and concurrency proof at the final immutable Crosswake revision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hostile test input → dispatcher | Each malformed, unauthorized, and cross-route class must fail before host authority or transport is reachable. |
| asynchronous host work → terminal reply | Races and duplicate completions are adversarial even when the handler is trusted. |
| private source/test output → checked-in evidence | Paths, remotes, payloads, adopter data, and logs require sanitization before entering Accrue artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-224-10 | Spoofing | origin/frame/route admission | high | mitigate | Table-driven negatives assert every invalid binding invokes the delegate zero times through the actual dispatcher. |
| T-224-11 | Tampering | navigation/double-completion races | high | mitigate | Deterministic barriers exercise every pre-reply boundary; exact binding recheck plus one-shot claim prevents stale/duplicate delivery. |
| T-224-12 | Information disclosure | telemetry/evidence | high | mitigate | Synthetic privacy fixtures and sanitized evidence allowlists reject payloads, secrets, account/device data, adopter identity, stack traces, and private remote credentials. |
| T-224-13 | Elevation of privilege | delegate API | high | mitigate | Compile/static API test proves host-visible types expose neither WebKit nor reply/evaluator/frame control. |
| T-224-SC | Tampering | final source pin | high | mitigate | Full runner rejects remote/revision/diff/audit mismatch before running native or consumer evidence. |
</threat_model>

<verification>
With the final authorized checkout selected, run `bash scripts/ci/verify_crosswake_host_commands.sh full`. Confirm the final source diff matches the audit inventory and contains no Accrue dependency or host/StoreKit/UI implementation. Plan 224-04 consumes this exact revision for evidence publication.
</verification>

<success_criteria>
- Every BRDG-01 pre-dispatch validator has a zero-delegate negative proof through the real dispatcher.
- Every BRDG-02 malformed/failure/cancellation/double/navigation case is deterministic, bounded, and stale-reply-safe.
- Host types cannot access reply transport; schema/telemetry/evidence privacy is mechanically tested.
- Final source revision, base/diff, review, commands, symbols, and convergence state are immutable and auditable.
- The final native proof remains input to Plan 224-04; no readiness/evidence file is promoted in this plan.
</success_criteria>

<output>
Create `.planning/phases/224-crosswake-host-command-bridge-seam/224-03-SUMMARY.md` when done.
</output>

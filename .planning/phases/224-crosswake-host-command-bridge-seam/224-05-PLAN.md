---
phase: 224-crosswake-host-command-bridge-seam
plan: 05
type: execute
wave: 5
depends_on: [224-04]
files_modified:
  - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
  - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift
  - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
  - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
  - .planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md
  - scripts/ci/verify_crosswake_host_commands.sh
autonomous: true
gap_closure: true
requirements: [BRDG-01, BRDG-02]
must_haves:
  truths:
    - "BRDG-01 / D-07: A WebKit message reaches host-command evaluation only when trusted WKScriptMessage metadata proves page content world, main frame, and an exact same origin as the active session; the envelope's origin remains a secondary check and never substitutes for frame metadata."
    - "BRDG-02 / D-11: D-13: A subframe, cross-origin main frame, non-page-world sender, or absent trusted context invokes the host delegate zero times and causes zero reply delivery even when its JSON envelope forges the active route's allowlisted origin."
    - "D-04: D-05: D-06: D-10: The trusted-frame gate precedes and preserves manifest/registry intersection, literal commands, bounded denials, route epoch, cancellation, and one-terminal-reply controls on the single Crosswake-owned path."
    - "D-01: D-02: D-03: The fix exists only in the exact pinned Alpha-fork Crosswake lane, never in an Accrue-local substitute, with refreshed base-to-patch diff, audit, runner, deterministic conformance evidence, and every runtime capability still feasibility_blocked."
    - "D-08: D-09: D-12: D-14: Trusted frame metadata remains private to Crosswake admission and never enters the delegate, payload, telemetry, UI, or entitlement authority."
  artifacts:
    - path: "/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift"
      provides: "Production WKScriptMessage frame/content-world/exact-origin admission before envelope evaluation"
    - path: "/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift"
      provides: "Injectable trusted-frame regression coverage proving forged envelope origin cannot dispatch or reply"
    - path: ".planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json"
      provides: "New immutable reviewed patch revision and base-to-patch diff identity"
    - path: ".planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md"
      provides: "Updated exact source inventory and trusted WebKit boundary audit"
    - path: ".planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md"
      provides: "Revision-bound deterministic trusted-frame conformance evidence with runtime limitations retained"
    - path: "scripts/ci/verify_crosswake_host_commands.sh"
      provides: "Exact-pin trusted-frame and complete verification modes"
  key_links:
    - from: "BridgeChannel.userContentController(_:didReceive:)"
      to: "BridgeChannel.evaluate(_:completion:)"
      via: "an internal value-only sender context derived exclusively from WKScriptMessage.frameInfo and WKScriptMessage.world, admitted before JSON decoding/evaluation"
      pattern: "frameInfo|securityOrigin|world"
    - from: "trusted sender context"
      to: "LiveViewSession.allowedOrigin"
      via: "scheme, case-normalized host, and effective-port equality; opaque or malformed origins fail closed"
      pattern: "allowedOrigin"
    - from: "HostCommandAdmissionTests"
      to: "production receive/admission path"
      via: "@testable access to an internal value context, not a public production bypass"
      pattern: "isMainFrame|securityOrigin|isPageWorld"
    - from: "scripts/ci/verify_crosswake_host_commands.sh"
      to: "crosswake-source-lock.json and 224-BRIDGE-CONFORMANCE-EVIDENCE.md"
      via: "clean exact HEAD, ancestry, binary diff, audit digest, evidence digest, full native suite, tracer consumer, and blocked-status assertions"
      pattern: "source-gate|trusted-frame|full"
---

<objective>
Close the failed trusted-WebKit-frame admission boundary at the exact reviewed Crosswake lane, so a caller-controlled envelope origin can no longer authorize a host command per D-01, D-02, D-07, D-11, and D-13.

Purpose: Restore BRDG-01 and BRDG-02 at the real `WKScriptMessageHandler` boundary while retaining every completed Phase 224 manifest, route, protocol, schema, epoch, reply-ownership, host-locality, and runtime-honesty control.
Output: A new immutable Crosswake patch with frame/content-world/origin admission and deterministic forged-origin tests, plus refreshed source lock, audit, runner, and conformance evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-RESEARCH.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-PATTERNS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-03-SUMMARY.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-04-SUMMARY.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
@.planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md
@scripts/ci/verify_crosswake_host_commands.sh
@/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
@/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift

Pinned API fact: `CrosswakeShellCore` targets iOS 15. The installed iPhoneOS SDK declares `WKScriptMessage.frameInfo`, `WKFrameInfo.isMainFrame`, `WKFrameInfo.securityOrigin`, and `WKScriptMessage.world`; `world` is available from iOS 14. The existing `WKUserContentController.add(_:name:)` registration is WebKit's page-world equivalent, so this lane supports a mandatory `message.world == WKContentWorld.page` check without changing host ownership.
</context>

## Artifacts this phase produces

- A reviewed Crosswake patch after `57e03b61082b1f865bc31c5e8b6dcee444f56dad` on `chore/accrue-host-command-bridge`, changing only `BridgeChannel.swift` and `HostCommandAdmissionTests.swift` for the gap closure.
- A private/internal value-only trusted message context populated in production solely from `WKScriptMessage.frameInfo.isMainFrame`, `frameInfo.securityOrigin`, and `WKScriptMessage.world`, with deterministic `@testable` injection for unit tests.
- Forged-origin/subframe/content-world negative tests that assert zero delegate calls and zero reply-sink calls, plus a trusted main-frame/page-world/exact-origin positive tracer.
- Refreshed `224-CROSSWAKE-SOURCE-AUDIT.md`, `crosswake-source-lock.json`, `224-BRIDGE-CONFORMANCE-EVIDENCE.md`, and `verify_crosswake_host_commands.sh` exact-revision evidence, with `feasibility_blocked` unchanged.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Manifest- and route-scoped host delegate behind every existing safe bridge check | 224-01..224-04 retained; 224-05 repairs the trusted sender-frame precondition | COVERED |
| REQ | BRDG-01 | No bypass of protocol, version, route, origin, pack, or manifest validation | 224-05 Task 1 | COVERED |
| REQ | BRDG-02 | Cross-origin/subframe input denies safely and cannot inject replies | 224-05 Task 1 | COVERED |
| RESEARCH | — | Main-frame/content-world and exact-origin checks before dispatch | 224-05 Task 1 | COVERED |
| RESEARCH | — | Exact-source deterministic evidence remains blocked runtime evidence | 224-05 Task 2 | COVERED |
| CONTEXT | D-01/D-02/D-03 | Crosswake-owned exact pinned lane; no local substitute; deterministic evidence only | 224-05 Tasks 1-2 | COVERED |
| CONTEXT | D-04/D-05/D-06 | Manifest/registry intersection, literal commands, stable bounded denials | Preserved from 224-01..224-04 and re-run by 224-05 | COVERED |
| CONTEXT | D-07 | Trusted main-frame/content-world/exact-origin validation in the ordered admission path | 224-05 Task 1 | COVERED |
| CONTEXT | D-08/D-09 | Delegate remains transport-free and payload schemas remain fixed/bounded | Preserved and re-run by 224-05 Task 1 | COVERED |
| CONTEXT | D-10/D-11 | Epoch recheck and safe denial/reply suppression | Preserved and re-run by 224-05 Tasks 1-2 | COVERED |
| CONTEXT | D-12/D-13 | Privacy-bounded telemetry and deterministic negative proof | 224-05 Task 1 | COVERED |
| CONTEXT | D-14 | No end-user visual system | No UI artifact is introduced | COVERED |

Deferred ideas remain excluded: no generic plugin/commerce API, StoreKit or host UI/auth transport, Accrue integration, additional command, Android work, or device-runtime promotion appears in this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind host-command admission to trusted WebKit sender metadata before evaluation</name>
  <precondition>`/Users/jon/projects/crosswake-accrue-bridge` is clean on `chore/accrue-host-command-bridge` at the lock's exact patch revision `57e03b61082b1f865bc31c5e8b6dcee444f56dad`, and `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh source-gate` passes before edits.</precondition>
  <reversibility rating="costly">D-01/D-07/D-10 admission and source-pin changes affect Crosswake hosts and the downstream Phase 225 integration, so the production boundary, tests, and revision evidence must move together.</reversibility>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Package.swift
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift
    - /Users/jon/projects/crosswake-accrue-bridge/examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - The active iPhoneOS SDK declarations for `WKScriptMessage`, `WKFrameInfo`, `WKSecurityOrigin`, `WKContentWorld`, and `WKUserContentController.add(_:contentWorld:name:)`
  </read_first>
  <files>/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift, /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift</files>
  <behavior>
    - Trusted page-world, main-frame metadata whose `WKSecurityOrigin` exactly matches the active session origin plus a matching envelope invokes the typed delegate once and emits one terminal reply.
    - A subframe with an envelope that claims `https://app.example.com` invokes the delegate zero times and emits zero replies.
    - A cross-origin main frame with an envelope that claims `https://app.example.com` invokes the delegate zero times and emits zero replies.
    - A non-page content world with otherwise matching metadata invokes the delegate zero times and emits zero replies.
    - Missing/opaque/unparseable sender-origin context fails closed; a trusted frame with a mismatched envelope origin remains rejected by the existing envelope-origin guard.
    - Existing protocol/version, inactive-route, pack, manifest descriptor, exact registration/version, no-field schema, throwing-handler, epoch, and at-most-once tests remain green.
  </behavior>
  <action>Start red by extending `HostCommandAdmissionTests` with a value-only sender-context test seam and the positive/negative behaviors above. Then change only Crosswake's `BridgeChannel` boundary per D-01/D-02/D-07: in `userContentController(_:didReceive:)`, derive an internal immutable context exclusively from `message.frameInfo.isMainFrame`, `message.frameInfo.securityOrigin` (`protocol`, case-normalized `host`, and effective `port`), and `message.world == WKContentWorld.page`. Route the production callback and `@testable` tests through one internal receive/admission function. That function must reject the sender before decoding the body or calling `evaluate`; a rejected sender must neither call the delegate nor call `replySink`, because evaluating a denial into another frame would itself be reply injection. Compare the trusted security origin to `session.allowedOrigin` by origin components, normalizing only standard default ports, and fail closed for absent/opaque/malformed components. Preserve `request.origin == session.allowedOrigin.absoluteString` as defense in depth after trusted-frame admission; never authorize from the envelope field alone.

Keep the synthetic context type and receive helper internal so production callers cannot inject trusted status. After a repository-wide call-site check confirms `evaluate` is used only by the message boundary and `@testable` tests, remove public bypass authority by making raw evaluation internal, or otherwise require the same non-forgeable internal admission token before any host-command delegate branch can execute. Do not expose `WKScriptMessage`, `WKFrameInfo`, security-origin data, content-world data, WebKit objects, reply closures, or evaluator access to `HostCommandDelegate`. Do not modify the literal four-command contract or any protocol/version, route, pack, manifest, descriptor/version, schema, telemetry, epoch, terminalization, StoreKit, Accrue, or capability-report behavior per D-04 through D-14. Run the focused tests red, implement green, then run the complete Crosswake SwiftPM suite. Commit the two Crosswake files on the existing authorized branch so Task 2 can pin the new immutable revision.</action>
  <acceptance_criteria>At the real `WKScriptMessageHandler` entry point, trusted WebKit metadata is checked before JSON decode/evaluation. Only page-world, main-frame, exact-session-origin input can reach the unchanged ordered bridge guards. Deterministic tests inject value metadata through an internal test seam and prove that a forged allowlisted envelope origin from a subframe, cross-origin main frame, or non-page world produces exactly zero delegate calls and zero reply deliveries, while the trusted tracer dispatches once. No public API can mark a context trusted or invoke the host-command delegate without passing this admission. All pre-existing host-command and full package tests remain green, including protocol/version, route, pack, manifest, descriptor/version, schema, failure, epoch, and one-terminal-reply assertions.</acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake-accrue-bridge &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter HostCommandAdmissionTests &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios</automated>
  </verify>
  <done>Caller-controlled envelope origin can no longer cross the trusted WebKit-frame boundary or cause host dispatch/reply injection, and the existing Crosswake-owned validation/terminalization path remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin the reviewed patch and refresh deterministic blocked-status evidence</name>
  <reversibility rating="costly">D-01/D-03 bind the downstream integration and security evidence to one immutable Crosswake patch, diff, audit, and conformance record.</reversibility>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md
    - scripts/ci/verify_crosswake_host_commands.sh
    - examples/crosswake_tracer/capability-report.json
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift
  </read_first>
  <files>.planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md, .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json, .planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md, scripts/ci/verify_crosswake_host_commands.sh</files>
  <action>After Task 1's Crosswake commit, update the source audit with the exact new patch revision, immutable base `932b4f32bf087b8e4c0c36c3e54b1031839e867d`, full base-to-patch range, binary diff identity, clean-tree review result, exhaustive two-file gap-closure symbol inventory, verified SDK availability (`WKScriptMessage.world` is available for the package's iOS 15 lane), frame/origin/content-world admission order, test names, and unchanged `alpha_fork_pending_upstream_review` convergence status. Replace the old security-boundary finding with the observed fix and retain the two specless assumptions rather than claiming they were resolved.

Extend `verify_crosswake_host_commands.sh` with a `trusted-frame` mode that performs the existing source gate first and runs the exact `HostCommandAdmissionTests` filter containing the forged-origin assertions; retain all existing modes. Update `crosswake-source-lock.json` to the new clean reviewed patch HEAD, recomputed base-to-patch binary diff identity, reviewed range, audit digest, unchanged remote/base/lane/test target/convergence fields, and recomputed conformance-evidence digest. Update `224-BRIDGE-CONFORMANCE-EVIDENCE.md` with the new revision/diff/audit binding and explicit observed assertions: trusted main-frame/page-world/exact-origin admission passes; forged-envelope subframe, cross-origin, and non-page-world cases yield no delegate call and no reply delivery. Preserve the D-03 limitation language: deterministic compile/unit conformance is not StoreKit, Accrue integration, simulator, physical-device, host-authentication, UI, entitlement, or runtime-readiness proof. Do not change the capability report's statuses or add a new evidence class. Run `trusted-frame` and `full` from the Accrue checkout with the exact source root; both must fail on a dirty/wrong revision, diff, audit, or evidence digest and must leave every capability `feasibility_blocked` per D-01/D-03/D-13.</action>
  <acceptance_criteria>The authorized Crosswake checkout is clean at one new immutable descendant of the recorded base. Lock, audit, evidence, and runner agree on the exact remote, lane, base, patch, diff, file/symbol inventory, test target, and digests. `trusted-frame` proves the forged-envelope regression at that exact revision; `full` proves the native suite, tracer consumer, evidence integrity, and blocked-status assertions together. `examples/crosswake_tracer/capability-report.json` still reports `overall_status == feasibility_blocked` and every capability remains `feasibility_blocked`; no physical-device, StoreKit, entitlement, or generic-commerce claim is introduced.</acceptance_criteria>
  <verify>
    <automated>CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh trusted-frame &amp;&amp; CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh full &amp;&amp; jq -e '.overall_status == "feasibility_blocked" and all(.capabilities[]; .status == "feasibility_blocked")' examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>The trusted-frame fix is immutably pinned and independently reproducible as deterministic conformance evidence, with exact-source integrity and runtime-honesty boundaries unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| WebKit frame/content world → Crosswake receiver | `WKScriptMessage` body and its claimed JSON origin are untrusted; only WebKit-supplied frame, security-origin, and content-world metadata may establish sender context. |
| Crosswake admission → host delegate | All protocol/version/route/origin/pack/manifest/descriptor/version/schema checks must pass before normalized intent crosses; WebKit and reply authority remain private. |
| External Crosswake checkout → Accrue evidence | A result is trusted only when clean remote/HEAD/ancestry/diff/audit/evidence bindings match the exact source lock. |

## STRIDE Threat Register (ASVS L1; high severity blocks completion)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-224-17 | Spoofing | `userContentController(_:didReceive:)` | high | mitigate | Derive main-frame, page-world, and exact origin from `WKScriptMessage.frameInfo`, `securityOrigin`, and `world` before decode/evaluation; forged envelope origin has no admission authority. |
| T-224-18 | Tampering | rejected-frame reply path | high | mitigate | Metadata rejection invokes neither delegate nor reply sink; Crosswake's existing private epoch-bound one-shot terminalizer remains the sole post-admission reply owner. |
| T-224-19 | Elevation of privilege | direct/contextless evaluation seam | high | mitigate | Keep the injectable context internal and remove or gate any public/contextless route to host-command dispatch; tests use `@testable`, not a production trust override. |
| T-224-20 | Information disclosure | trusted sender metadata | medium | mitigate | Keep frame origin/world details inside admission; do not add them to delegate input, payloads, telemetry, evidence logs, or UI. |
| T-224-21 | Tampering | exact-source conformance evidence | high | mitigate | Source gate binds clean remote, new immutable patch, base ancestry, binary diff, audit digest, evidence digest, native tests, and blocked capability status. |
| T-224-SC | Tampering | package/source acquisition | high | mitigate | No package install occurs; the already authorized Alpha-fork lane remains pinned and independently checked before any conformance result is accepted. |
</threat_model>

<verification>
1. Run `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh trusted-frame`; expect exact-source gating followed by passing trusted positive and forged-origin/subframe/non-page-world negatives.
2. Run `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh full`; expect the complete pinned Crosswake suite, Accrue tracer consumer, audit/evidence digest checks, and blocked-status assertions to pass.
3. Inspect the base-to-patch inventory: only the audited Crosswake bridge/test files implement this closure, no Accrue-local handler/wrapper or generic commerce API exists, and no high-severity threat remains unmitigated.
</verification>

<success_criteria>
- WebKit-supplied main-frame, page-world, and exact security-origin metadata is mandatory before a host command can reach evaluation or the delegate.
- Forging the envelope origin from a subframe, cross-origin frame, or wrong content world produces zero delegate calls and zero reply injection.
- Protocol, version, route, envelope origin, installed pack, manifest capability, exact descriptor/registration version, fixed schema, epoch, and at-most-one terminal reply controls remain green.
- The new Crosswake revision, binary diff, audit, runner, and evidence digests agree and reproduce from the authorized clean checkout.
- The seam remains Crosswake-owned and host-local; every runtime capability remains `feasibility_blocked` and no StoreKit, entitlement, UI, host-authentication, simulator, or physical-device claim is made.
</success_criteria>

<output>
Create `.planning/phases/224-crosswake-host-command-bridge-seam/224-05-SUMMARY.md` when done.
</output>

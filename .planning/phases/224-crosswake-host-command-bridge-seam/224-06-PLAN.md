---
phase: 224-crosswake-host-command-bridge-seam
plan: 06
type: execute
wave: 6
depends_on: [224-05]
files_modified:
  - scripts/ci/verify_crosswake_host_commands.sh
  - scripts/ci/test_verify_crosswake_host_commands.sh
autonomous: true
gap_closure: true
requirements: [BRDG-01, BRDG-02]
must_haves:
  truths:
    - "BRDG-01 / D-01 / D-03: Every focused conformance mode validates the lock's test target against one exact approved value, then launches the pinned SwiftPM admission suite as literal argv rather than interpreting repository data as shell code."
    - "BRDG-02 / D-11 / D-13: An altered, substituted, or shell-bearing test target is rejected before Swift or any injected command runs, so mutable evidence metadata cannot replace the focused denial/admission suite."
    - "D-02 / D-04 through D-12: The closure changes only the Accrue evidence runner and its regression test; Crosswake's validated bridge, literal command contract, route guards, delegate isolation, reply ownership, and pinned source revision remain unchanged."
    - "D-03 / D-14: The trusted-frame and full modes pass at the exact pinned Crosswake revision while the capability report and every capability remain feasibility_blocked, with no device-runtime or UI claim."
  artifacts:
    - path: "scripts/ci/verify_crosswake_host_commands.sh"
      provides: "Strict lock-target equality followed by a literal SwiftPM argv invocation for focused conformance modes"
    - path: "scripts/ci/test_verify_crosswake_host_commands.sh"
      provides: "Isolated regression proving approved argv execution and fail-closed rejection of substituted or shell-bearing lock targets"
  key_links:
    - from: ".planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json"
      to: "scripts/ci/verify_crosswake_host_commands.sh"
      via: "exact data equality check only; the JSON value never becomes executable syntax"
      pattern: "test_target|expected_test_target"
    - from: "scripts/ci/verify_crosswake_host_commands.sh"
      to: "Crosswake HostCommandAdmissionTests"
      via: "literal `swift test --package-path packages/crosswake-shell-core-ios --filter HostCommandAdmissionTests` argv after source and target validation"
      pattern: "HostCommandAdmissionTests"
    - from: "scripts/ci/test_verify_crosswake_host_commands.sh"
      to: "scripts/ci/verify_crosswake_host_commands.sh"
      via: "temporary exact-pin fixture with recording shims that observes exit status, argv, and absence of injected side effects"
      pattern: "trusted-frame|test_target"
---

<objective>
Close the sole remaining Phase 224 verification gap by making the exact-pin focused runner command-safe and proving that mutable lock data cannot execute or substitute the conformance suite per D-01, D-03, D-11, and D-13.

Purpose: Restore trust in the evidence boundary without changing the already verified Crosswake bridge implementation or widening Phase 224 scope.
Output: A literal-argv focused runner, an isolated rejection regression, and passing trusted-frame/full evidence with feasibility_blocked retained.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-REVIEW.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-05-SUMMARY.md
@.planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
@scripts/ci/verify_crosswake_host_commands.sh
@examples/crosswake_tracer/capability-report.json

Gap boundary: `224-VERIFICATION.md` is authoritative. BRDG-01 and BRDG-02 behavior in Crosswake is already verified at `fc5e399fcb46d78b610c81e13c644277f3fcf1c5`; only the mutable-data-to-shell execution boundary in the Accrue runner remains open. Do not re-pin or edit Crosswake for this closure.
</context>

## Artifacts this phase produces

- `scripts/ci/verify_crosswake_host_commands.sh`, with the lock's `test_target` treated only as data: exact approved-value validation followed by a hard-coded SwiftPM argv invocation for `tracer`, `admission`, `lifecycle`, and `trusted-frame`.
- `scripts/ci/test_verify_crosswake_host_commands.sh`, an isolated executable regression that proves the approved target launches the exact admission filter and altered, substituted, or shell-bearing targets fail before Swift or injected side effects run.
- Execution evidence in `224-06-SUMMARY.md` from the regression, exact-pin `trusted-frame`, exact-pin `full`, and blocked-status assertion; no capability-report status change.

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Keep the manifest/route-scoped delegate behind Crosswake's existing safe bridge validation boundary | 224-01..224-05 remain unchanged; 224-06 restores the trusted evidence runner for that boundary | COVERED |
| REQ | BRDG-01 | No bypass of protocol, version, route, origin, pack, or manifest validation | Task 1 re-runs the pinned trusted-frame and full suites through literal argv | COVERED |
| REQ | BRDG-02 | Invalid/failing host commands deny safely and cannot inject replies | Task 1 prevents mutable lock data from substituting the focused negative suite and re-runs it | COVERED |
| RESEARCH | — | Exact-source deterministic evidence with runtime status blocked | Task 1 validates the lock, runs the exact pinned suite, and retains feasibility_blocked | COVERED |
| CONTEXT | D-01/D-02/D-03 | Crosswake-owned exact pin; no local substitute; deterministic evidence only | Task 1 changes only the evidence runner/test and leaves the exact Crosswake revision and runtime status intact | COVERED |
| CONTEXT | D-04/D-05/D-06/D-07 | Closed command set and ordered manifest/route/origin admission | Preserved from 224-01..224-05 and exercised by trusted-frame/full | COVERED |
| CONTEXT | D-08/D-09/D-10/D-11 | Transport-free delegate, bounded payloads, epoch safety, bridge-owned denial | Preserved from 224-01..224-05 and exercised by trusted-frame/full | COVERED |
| CONTEXT | D-12/D-13 | Privacy-bounded operations and deterministic negative proof | Task 1 adds command-boundary negative proof and re-runs host-command negatives | COVERED |
| CONTEXT | D-14 | No end-user visual system | No UI or runtime artifact is introduced | COVERED |

Deferred ideas remain excluded: no Accrue dependency in Crosswake, generic commerce/plugin API, StoreKit or host UI/authentication work, additional commands, Android work, entitlement authority, simulator promotion, or physical-device claim appears in this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject mutable lock programs and execute the pinned focused suite as literal argv</name>
  <precondition>`/Users/jon/projects/crosswake-accrue-bridge` is clean at `fc5e399fcb46d78b610c81e13c644277f3fcf1c5`, and the current `source-gate` mode passes before the runner is edited.</precondition>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-REVIEW.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - scripts/ci/verify_crosswake_host_commands.sh
    - examples/crosswake_tracer/capability-report.json
  </read_first>
  <files>scripts/ci/verify_crosswake_host_commands.sh, scripts/ci/test_verify_crosswake_host_commands.sh</files>
  <behavior>
    - With the reviewed lock's exact target, a focused mode launches exactly `swift`, `test`, `--package-path`, `packages/crosswake-shell-core-ios`, `--filter`, `HostCommandAdmissionTests` as separate argv entries after the existing source gate passes.
    - A target containing a command separator and side-effect command exits with the runner's dedicated unexpected-target status before the Swift shim or side effect runs.
    - A syntactically harmless but different package path, filter, option, or suite also exits with the same status before Swift runs, proving the conformance suite cannot be substituted.
    - The regression operates entirely in a temporary fixture and never edits the checked-in lock, audit, evidence, capability report, or authorized Crosswake checkout.
    - The real trusted-frame and full modes still pass at the exact pinned source, and overall plus per-capability status remains feasibility_blocked.
  </behavior>
  <action>Start red by creating `scripts/ci/test_verify_crosswake_host_commands.sh`. The regression must build a disposable `mktemp -d` repository-shaped fixture, register cleanup with `trap`, copy the runner into that fixture, and generate the smallest internally consistent reviewed-patch lock/audit/source-gate inputs. Put recording `git` and `swift` shims first on the fixture-only `PATH`: the git shim supplies the sanitized remote, clean status, exact HEAD/base ancestry, and deterministic diff bytes required by the existing source gate; the swift shim records each argv element in an invocation log and creates a marker only when actually launched. Calculate fixture audit and diff digests from the fixture data rather than embedding stale values.

Exercise three cases without modifying repository files. First, the exact approved lock target must pass `trusted-frame`, invoke the Swift shim once, run from the fixture Crosswake root, and record precisely the expected admission-test argv. Second, replace only the fixture lock target with a value that appends a shell side effect; assert the runner returns the dedicated unexpected-target exit code, the Swift invocation count is unchanged, and the side-effect marker is absent. Third, replace it with a benign different filter or package path; assert the same rejection and no new invocation. Ensure assertion failures propagate under `set -euo pipefail`, and ensure the test itself checks exit codes explicitly rather than suppressing errors.

Then make the regression green in `verify_crosswake_host_commands.sh`: define one exact approved target string matching the reviewed lock; read `.test_target` as data; require byte-for-byte equality before every focused mode; and, only after equality succeeds, invoke `swift test --package-path packages/crosswake-shell-core-ios --filter HostCommandAdmissionTests` directly as literal argv from `CROSSWAKE_SOURCE_ROOT`. Assign a stable nonzero exit status and privacy-safe diagnostic for mismatch. Remove all dynamic command-string execution and do not split, expand, source, or otherwise interpret JSON-derived text. Keep `source-gate` and `full` behavior intact, including remote/clean-HEAD/base/diff/audit/evidence integrity and the full mode's literal complete suite, tracer consumer, and feasibility-blocked assertions.

Run the isolated regression first. Then run the real `trusted-frame` and `full` modes with `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge`, followed by the explicit capability-report assertion. Do not edit Crosswake, the source lock, the conformance evidence, the capability report, or phase runtime claims; this is a safely reversible runner/test correction that preserves D-01 through D-14 and closes only the authoritative verification gap.</action>
  <acceptance_criteria>The checked-in lock target is accepted only by exact equality and never becomes shell syntax. All focused modes execute the reviewed admission filter through literal argv. The isolated regression proves both a shell-bearing target and a benign substitute target are rejected before Swift or any injected side effect runs, while the exact target records the expected argv once. The real trusted-frame and full modes pass at Crosswake `fc5e399f`, BRDG-01/BRDG-02 coverage remains exercised, no bridge/host/UI code changes, and `overall_status` plus every capability status remains `feasibility_blocked`.</acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/test_verify_crosswake_host_commands.sh &amp;&amp; CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh trusted-frame &amp;&amp; CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh full &amp;&amp; jq -e '.overall_status == "feasibility_blocked" and all(.capabilities[]; .status == "feasibility_blocked")' examples/crosswake_tracer/capability-report.json</automated>
  </verify>
  <done>The exact-pinned focused evidence path is command-safe and non-substitutable, both real verification modes remain green, and deterministic bridge conformance still makes no device-runtime claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable repository lock → verification runner | `test_target` is untrusted data and must be compared only; it cannot select executable argv or shell syntax. |
| Verification runner → authorized Crosswake process | Only the runner's fixed SwiftPM argv may start after exact source and target validation. |
| Deterministic test output → capability report | Compile/unit success may support conformance only and cannot promote simulator, device, StoreKit, entitlement, or runtime readiness. |

## STRIDE Threat Register (ASVS L1; unresolved high severity blocks completion)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-224-22 | Tampering | lock-to-runner target selection | high | mitigate | Require byte-for-byte equality with the single reviewed target and invoke a separate hard-coded argv sequence; the lock value is never evaluated, expanded, or split. |
| T-224-23 | Elevation of privilege | focused runner process launch | high | mitigate | The regression supplies a shell-bearing target and proves rejection occurs before either the Swift shim or injected side effect can run. |
| T-224-24 | Spoofing | conformance suite identity | high | mitigate | The regression supplies a benign substitute filter/path and proves it cannot replace `HostCommandAdmissionTests`; exact argv is recorded for the accepted case. |
| T-224-25 | Repudiation | deterministic evidence result | medium | mitigate | The summary records the isolated regression plus real trusted-frame/full commands at the exact locked revision and blocked-status assertion. |
| T-224-26 | Information disclosure | mismatch diagnostics | low | accept | The runner emits only a stable unexpected-target message and status; it need not echo the untrusted target or repository-sensitive data. |
| T-224-SC | Tampering | package/source acquisition | high | mitigate | No package install or source update occurs; the existing clean remote/HEAD/base/diff/audit gate remains mandatory before the fixed SwiftPM command runs. |
</threat_model>

<verification>
1. Run `bash scripts/ci/test_verify_crosswake_host_commands.sh`; expect the exact argv case to pass and both altered-target cases to be rejected with no recorded Swift launch or injected marker.
2. Run `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh trusted-frame`; expect exact-source gating and all trusted-frame/admission negatives to pass.
3. Run `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh full`; expect the complete Crosswake suite, tracer consumer, evidence digests, and blocked-status checks to pass.
4. Confirm the Accrue diff contains only the runner, its new regression, this plan/summary, and normal planning-state bookkeeping; the authorized Crosswake checkout and capability report statuses are unchanged.
</verification>

<success_criteria>
- No mutable lock field is interpreted as a shell program or used to construct executable argv.
- Exact approved target metadata launches exactly the pinned `HostCommandAdmissionTests` filter; malicious and benign substitutes fail closed before process execution.
- The existing source, audit, diff, evidence, bridge-validation, and reply-ownership gates remain intact.
- Trusted-frame and full verification pass against clean Crosswake `fc5e399f`.
- The seam remains Crosswake-owned and host-local, with no Accrue dependency in Crosswake, generic commerce API, bridge-guard bypass, or device-runtime claim.
- The capability report retains `feasibility_blocked` overall and for every capability.
</success_criteria>

<output>
Create `.planning/phases/224-crosswake-host-command-bridge-seam/224-06-SUMMARY.md` when done.
</output>

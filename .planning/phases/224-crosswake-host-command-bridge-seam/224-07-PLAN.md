---
phase: 224-crosswake-host-command-bridge-seam
plan: 07
type: execute
wave: 7
depends_on: [224-06]
files_modified:
  - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
  - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift
  - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift
  - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
  - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
  - .planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md
autonomous: true
gap_closure: true
requirements: [BRDG-01, BRDG-02]
must_haves:
  truths:
    - "BRDG-01 — D-07: D-10: Every admitted invocation is bound to its validated route epoch and atomically claimed by route epoch plus correlation ID before `HostCommandDelegate.handle`; a duplicate admitted request cannot repeat the host side effect, while the same correlation ID is valid after route replacement creates a new epoch."
    - "BRDG-01 — D-04: D-06: Setup/install rejects malformed or duplicate host-command descriptors with actionable diagnostics through every production construction path; the ordinary public `CrosswakeShellConfig` initializer cannot accept host-command descriptors."
    - "BRDG-01 — D-07: Protocol, version, trusted frame/content world, exact origin, active route, installed pack, manifest capability, descriptor registration/version, and fixed-schema checks all remain before the invocation claim and delegate dispatch."
    - "BRDG-02 — D-08: D-10: D-11: Crosswake retains cancellation, stale-result suppression, and at-most-one terminal reply; the delegate still receives only normalized typed intent and never raw payload, WebKit, evaluator, callback, or reply transport."
    - "D-01: D-02: D-03: The repair exists only in the exact-pinned Crosswake lane, adds no Crosswake dependency on Accrue or generic commerce API, refreshes immutable audit/evidence bindings, and leaves overall plus every capability `feasibility_blocked` without a physical-device claim."
    - "D-05: D-09: D-12: D-14: The four-command literal allowlist, zero-field payload contract, privacy-safe telemetry, and no-UI boundary remain unchanged."
  artifacts:
    - path: "/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift"
      provides: "Atomic admitted-epoch/correlation invocation claim before host dispatch, with route-replacement retirement and existing terminalization preserved"
    - path: "/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift"
      provides: "Non-host public initializer plus the sole validated host-command-bearing construction path"
    - path: "/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift"
      provides: "Same-correlation, post-navigation reuse, malformed-version, duplicate-descriptor, and real channel/navigation regressions"
    - path: ".planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json"
      provides: "New immutable reviewed Crosswake patch revision, base-to-patch diff identity, and refreshed audit/evidence digests"
    - path: ".planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md"
      provides: "Exhaustive updated production/test symbol inventory and upstream-convergence record"
    - path: ".planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md"
      provides: "Deterministic-only closure evidence for both verifier blockers, with runtime limitations retained"
  key_links:
    - from: "BridgeChannel.evaluate(_:completion:)"
      to: "HostCommandDelegate.handle"
      via: "all existing admission checks followed by an atomic `(routeEpoch, correlationID)` claim"
      pattern: "claim.*HostCommand|correlationID|routeEpoch"
    - from: "BridgeChannel.update(session:transferCoordinator:)"
      to: "host-command invocation claims"
      via: "the route epoch advances and prior-epoch claim state is retired before the replacement session can admit work"
      pattern: "routeEpoch|removeAll"
    - from: "CrosswakeShellConfig.validating(hostCommandDescriptors:hostCommandDelegate:)"
      to: "BridgeChannel.init"
      via: "the only host-command-bearing config path validates literal membership, exact descriptor shape/version, and uniqueness before a channel can consume descriptors"
      pattern: "validating|hostCommandDescriptors"
    - from: "scripts/ci/verify_crosswake_host_commands.sh full"
      to: "Crosswake source lock, native suite, Accrue tracer, evidence digest, and capability report"
      via: "clean exact HEAD, ancestry, binary diff, audit/evidence digests, and blocked-status assertions"
      pattern: "source-gate|full|feasibility_blocked"
---

<objective>
Close the two remaining Phase 224 safety-boundary defects by claiming each admitted route-epoch/correlation before host work and making descriptor validation unavoidable on every host-command-bearing construction path, per D-01 through D-13.

Purpose: Prevent caller-controlled replay from duplicating host side effects and prevent malformed or ambiguous descriptor configuration from entering the production bridge, while preserving the already verified Crosswake admission, reply, privacy, ownership, and evidence boundaries.
Output: An exact-pinned Crosswake repair with deterministic duplicate/post-navigation and constructor-path regressions, plus refreshed immutable audit and conformance evidence that remains runtime-blocked.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-RESEARCH.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-PATTERNS.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-VALIDATION.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-06-SUMMARY.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
@.planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md
@.planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
@scripts/ci/verify_crosswake_host_commands.sh
@examples/crosswake_tracer/capability-report.json

Exact source root: `/Users/jon/projects/crosswake-accrue-bridge`. The starting reviewed patch is `fc5e399fcb46d78b610c81e13c644277f3fcf1c5` on the already authorized short-lived Alpha lane. `224-VERIFICATION.md` is authoritative for the two actionable gaps; no deferred item is part of this plan.

<interfaces>
From `BridgeChannel.swift`: `BridgeChannel.update(session:transferCoordinator:)` advances `routeEpoch`; `evaluate(_:completion:)` performs the ordered admission path and invokes `HostCommandDelegate.handle`; `terminalizeHostCommand(...)` rechecks the admitted session/epoch and currently claims only `request.correlationID` after the side effect.

From `CrosswakeShellConfig.swift`: the ordinary public initializer currently accepts `hostCommandDescriptors` and stores them unchanged; `CrosswakeShellConfig.validating(hostCommandDescriptors:hostCommandDelegate:) throws` contains the intended unsupported/malformed/duplicate checks but is optional.

From `HostCommandAdmissionTests.swift`: `makeChannel(...)` currently constructs the unchecked config, `makeRequest(...)` defaults correlation ID to `test-correlation`, and `NavigationAdvancingDelegate` provides a deterministic in-call navigation hook without sleeps.
</interfaces>
</context>

## Artifacts this phase produces

- A reviewed Crosswake commit in `/Users/jon/projects/crosswake-accrue-bridge` that changes only `BridgeChannel.swift`, `CrosswakeShellConfig.swift`, and `HostCommandAdmissionTests.swift` for these two closures.
- An atomic route-epoch/correlation invocation state transition before `HostCommandDelegate.handle`, with prior-epoch state retired when `BridgeChannel.update` replaces the route.
- A public config API in which the ordinary initializer cannot accept host-command descriptors and the throwing validating factory is the sole production construction path for descriptor-bearing configs.
- Deterministic tests for repeated admitted correlation, correlation reuse after navigation, malformed descriptor versions, duplicate same/conflicting versions, valid factory-to-channel admission, and navigation lifecycle.
- Refreshed `224-CROSSWAKE-SOURCE-AUDIT.md`, `crosswake-source-lock.json`, and `224-BRIDGE-CONFORMANCE-EVIDENCE.md`, bound to the new exact Crosswake revision while all capability statuses remain `feasibility_blocked`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Claim admitted epoch/correlation before the host side effect</name>
  <precondition>`/Users/jon/projects/crosswake-accrue-bridge` is clean at the patch revision in `crosswake-source-lock.json`, and `CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh source-gate` passes.</precondition>
  <reversibility rating="costly">D-10's invocation lifecycle is a shared bridge authority contract; changing its epoch and replay semantics later requires coordinated host, native-test, source-pin, and security-evidence updates.</reversibility>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift
  </read_first>
  <files>/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift, /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift</files>
  <behavior>
    - Test 1: two fully admitted requests with the same correlation ID in the same route epoch call the host delegate exactly once and produce at most one terminal reply.
    - Test 2: after `update(session:transferCoordinator:)` installs a replacement route/epoch, the same correlation ID is admitted once for the new route and does not inherit suppression from the prior epoch.
    - Test 3: a duplicate loses the claim before `HostCommandDelegate.handle`, including when two callers contend for the same epoch/correlation; the claim transition is synchronized rather than check-then-insert state.
    - Test 4: protocol/version, trusted sender/origin, route, pack, manifest, descriptor/version, and zero-field schema failures still occur before any claim or delegate call.
    - Test 5: navigation after an invocation claim still cancels/suppresses the old outcome and cannot deliver it to the replacement route.
  </behavior>
  <action>Start red in `HostCommandAdmissionTests` with an end-to-end same-correlation replay through `receive(_:from:completion:)`: use a valid trusted sender, manifest declaration, validated registration, real `BridgeChannel`, and a counting delegate, then send the identical admitted request twice and assert one delegate call plus at most one reply. Add a post-navigation case that first admits the correlation on `dashboard`, calls `update` with a `settings` session, then sends a request whose route IDs and trusted origin match `settings`; assert the same correlation invokes the delegate once in the replacement epoch. Add a deterministic contention case appropriate to the class's synchronization model so an unsynchronized check-then-insert cannot pass.

Replace the reply-only string set with private invocation lifecycle state keyed by an immutable `Hashable` pair of admitted `routeEpoch` and caller correlation ID. Synchronize the claim as one operation and perform it only after every existing D-07 guard (protocol/runtime, trusted frame/content world and exact origin, active route, installed packs, manifest capability, exact descriptor/registration version, and fixed request schema) has passed, but before attempt telemetry and `HostCommandDelegate.handle`. A failed claim must return before the delegate side effect and must not create a second terminal reply; emit only an existing privacy-safe suppression reason and never payload/configuration details. Keep terminalization Crosswake-owned: transition the already claimed invocation to terminal exactly once after the existing session/epoch recheck, and retain stale-reply suppression for throw, denial, success, and navigation. Make `update(session:transferCoordinator:)` advance the epoch and retire prior-epoch claim/terminal state under the same synchronization boundary before the replacement session is usable, so reuse in a later epoch is valid and state does not grow across navigation. Do not weaken or reorder any pre-dispatch guard, widen the four-command allowlist, add raw payload/reply access, or change entitlement authority per D-05/D-07/D-08/D-09/D-10/D-11/D-12/D-13.</action>
  <acceptance_criteria>Through the real trusted-sender and bridge evaluation path, identical admitted epoch/correlation requests invoke the delegate once and terminalize at most once; the claim is made atomically before host work; route replacement retires the old claim namespace; the same correlation succeeds once in the new epoch; navigation still suppresses an old outcome; and every existing pre-dispatch denial remains ahead of claim and dispatch.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios --filter HostCommandAdmissionTests</automated>
  </verify>
  <done>The verifier's route-epoch at-most-once truth is observable end to end: duplicate admitted work is suppressed before side effects, and post-navigation reuse is valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make descriptor validation unavoidable on the production config path</name>
  <precondition>Task 1's focused Crosswake suite passes and the checkout contains only its reviewed in-plan source/test changes.</precondition>
  <reversibility rating="costly">D-04/D-06 define a public host configuration contract; removing descriptor input from the ordinary initializer changes source-level host integration and must be carried consistently by the validating factory and tests.</reversibility>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-VERIFICATION.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CONTEXT.md
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
    - /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift
    - Every `CrosswakeShellConfig(` call site returned by `rg -n 'CrosswakeShellConfig\\(' /Users/jon/projects/crosswake-accrue-bridge -g '*.swift'`
  </read_first>
  <files>/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift, /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift</files>
  <behavior>
    - Test 1: the ordinary public initializer remains usable for configurations with no host-command descriptors and exposes no parameter that can install descriptors or a host-command delegate.
    - Test 2: the throwing host-command factory rejects a malformed descriptor version with an actionable diagnostic naming the affected command and malformed-version class.
    - Test 3: the factory rejects both identical duplicates and same-command descriptors carrying conflicting versions, with an actionable duplicate diagnostic before a channel is built.
    - Test 4: a valid four-command descriptor set constructed by the factory feeds the real `BridgeChannel`, admits the exact manifest/registration version, survives route navigation, and retains the epoch/correlation behavior from Task 1.
    - Test 5: all source-tree host-command-bearing `CrosswakeShellConfig` construction uses the validating factory; no test helper or example reinstates unchecked descriptors.
  </behavior>
  <action>Start red by replacing direct descriptor-bearing test construction with `try CrosswakeShellConfig.validating(...)` and adding distinct malformed-version, identical-duplicate, and conflicting-version duplicate cases. Diagnostics must be actionable: assert the stable error case and human-readable description identify the command and whether the descriptor is malformed or duplicate, without dumping descriptor collections or runtime allowlists. Exercise a valid factory result through the real `BridgeChannel.receive`, `update(session:transferCoordinator:)`, and replacement-route request path; this must be a production initializer/channel/navigation regression, not a unit test of the validator alone.

Refactor `CrosswakeShellConfig` so its ordinary nonthrowing public initializer cannot accept `hostCommandDescriptors` or `hostCommandDelegate` and always creates a non-host configuration. Keep descriptor-bearing initialization private to the type. Make `CrosswakeShellConfig.validating(hostCommandDescriptors:hostCommandDelegate:) throws` the sole public host-command-bearing construction path, validating every descriptor before it calls that private initializer. Preserve literal membership in `BridgeCommand.hostAccrueCommands`, exact command/capability equality, strict semantic version shape, and uniqueness by command; same-command duplicates must reject even when their versions differ. Update every actual descriptor-bearing source/test call site to use `try validating`; do not make the public initializer throwing for unrelated non-host callers, silently drop invalid descriptors, precondition-crash, or fall back to first-match behavior. Keep the registered capability projection and BridgeChannel manifest/registry/version intersection unchanged per D-04/D-05/D-06/D-07/D-13.</action>
  <acceptance_criteria>The public ordinary initializer cannot carry host-command descriptors or a host delegate; the sole public descriptor-bearing factory validates literal command/capability/version shape and command uniqueness before construction; malformed and identical/conflicting duplicate versions fail with actionable diagnostics; and a valid factory-built config traverses the real channel plus navigation path with Task 1's replay semantics intact.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios --filter HostCommandAdmissionTests &amp;&amp; swift test --package-path /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios</automated>
  </verify>
  <done>The verifier's setup-validation truth is observable through the production construction and channel path, and no descriptor-bearing bypass remains.</done>
</task>

<task type="auto">
  <name>Task 3: Review, commit, re-pin, and refresh deterministic-only closure evidence</name>
  <precondition>Both repaired Crosswake behaviors pass the focused and full native suites, the authorized checkout is otherwise clean on `chore/accrue-host-command-bridge`, and the immutable base remains `932b4f32bf087b8e4c0c36c3e54b1031839e867d`.</precondition>
  <read_first>
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md
    - .planning/phases/224-crosswake-host-command-bridge-seam/224-VALIDATION.md
    - scripts/ci/verify_crosswake_host_commands.sh
    - examples/crosswake_tracer/capability-report.json
    - The complete `git diff 932b4f32bf087b8e4c0c36c3e54b1031839e867d...HEAD` and changed-symbol inventory in the authorized Crosswake checkout
  </read_first>
  <files>/Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift, /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift, /Users/jon/projects/crosswake-accrue-bridge/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/HostCommandAdmissionTests.swift, .planning/phases/224-crosswake-host-command-bridge-seam/224-CROSSWAKE-SOURCE-AUDIT.md, .planning/phases/224-crosswake-host-command-bridge-seam/crosswake-source-lock.json, .planning/phases/224-crosswake-host-command-bridge-seam/224-BRIDGE-CONFORMANCE-EVIDENCE.md</files>
  <action>Review the Crosswake diff for only the two verifier closures and preserved controls, then commit it on the already authorized Alpha lane. Do not change the sanitized remote, delivery lane, immutable base, upstream-convergence meaning, test target, command allowlist, delegate surface, or runtime classification. Update `224-CROSSWAKE-SOURCE-AUDIT.md` with the new exact patch revision, base-to-patch range, binary diff identity, exhaustive three-file changed symbol/test inventory, both closed verifier findings, actual native commands/results, and the unchanged `alpha_fork_pending_upstream_review` status. Update `224-BRIDGE-CONFORMANCE-EVIDENCE.md` with deterministic evidence for the same-ID one-call result, post-navigation correlation reuse, public-initializer exclusion, malformed/duplicate factory diagnostics, and the retained admission/lifecycle/privacy boundaries; do not claim simulator, StoreKit, host integration, physical-device, or runtime proof.

Recompute the audit and evidence SHA-256 digests and update `crosswake-source-lock.json` atomically with the new `patch_revision`, `reviewed_diff_range`, `diff_identity`, `audit_sha256`, and `conformance_evidence_sha256`, preserving all other authority fields. Run the existing command-safe runner without altering its fixed `test_target`: first `source-gate`, then `trusted-frame`, then `full`; run the explicit capability-report assertion afterward. If the runner reveals an actual gap in the two repaired files or tests, fix and re-pin it in this same task; do not weaken digest/source checks or edit the capability status to make evidence pass. Retain D-01/D-02/D-03/D-08/D-09/D-12/D-14, Crosswake-only ownership, no Accrue dependency in Crosswake, no generic commerce surface, and `feasibility_blocked` throughout.</action>
  <acceptance_criteria>The authorized Crosswake checkout is clean at a new reviewed immutable commit descended from the unchanged base; lock, diff, audit, and evidence digests agree; the audit exhaustively names the two fixes and all changed symbols; focused and full native plus tracer/evidence gates pass; the report remains feasibility_blocked overall and per capability; and no physical-device/runtime proof is stated.</acceptance_criteria>
  <verify>
    <automated>CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh source-gate &amp;&amp; CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh trusted-frame &amp;&amp; CROSSWAKE_SOURCE_ROOT=/Users/jon/projects/crosswake-accrue-bridge bash scripts/ci/verify_crosswake_host_commands.sh full &amp;&amp; swift test --package-path examples/crosswake_tracer &amp;&amp; jq -e '.overall_status == "feasibility_blocked" and all(.capabilities[]; .status == "feasibility_blocked") and all(.capabilities[] | .evidence[]; (.kind != "crosswake_bridge_compile_unit") or (.location | contains("224-BRIDGE-CONFORMANCE-EVIDENCE.md")))' examples/crosswake_tracer/capability-report.json &amp;&amp; test -z "$(git -C /Users/jon/projects/crosswake-accrue-bridge status --porcelain)"</automated>
  </verify>
  <done>Both Phase 224 blockers are closed at one exact reviewed Crosswake revision with reproducible deterministic evidence and the physical-device/runtime gate unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| admitted WebKit request → host delegate | Caller-controlled correlation IDs cross into a side-effecting host boundary only after ordered admission and an atomic epoch-scoped replay claim. |
| public shell configuration → bridge authority | Descriptor arrays are untrusted configuration until literal membership, command/capability/version shape, and uniqueness are validated. |
| route replacement → invocation lifecycle state | Prior route authority, claims, and pending results must not suppress or target work in the replacement epoch. |
| reviewed Crosswake source → Accrue evidence | Source identity, diff, audit, test result, and evidence digest must agree before deterministic conformance is accepted. |

## STRIDE Threat Register (ASVS L1; high severity blocks completion)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-224-27 | Tampering | duplicate correlation dispatch | high | mitigate | Task 1 atomically claims `(routeEpoch, correlationID)` after all admission checks and before telemetry/delegate invocation; deterministic same-ID and contention tests require one host call. |
| T-224-28 | Denial of service | cross-epoch claim retention | high | mitigate | `update` advances the epoch and retires prior claim/terminal state under the same synchronization boundary; post-navigation reuse and cleanup are tested. |
| T-224-29 | Elevation of privilege | unchecked descriptor initializer | high | mitigate | Task 2 removes descriptor/delegate parameters from the ordinary public initializer and makes the throwing validated factory the sole descriptor-bearing path. |
| T-224-30 | Spoofing | ambiguous descriptor version | high | mitigate | Strict version-shape and duplicate-command checks reject malformed, identical, and conflicting-version descriptors with stable actionable diagnostics before channel construction. |
| T-224-31 | Information disclosure | replay/config diagnostics and telemetry | medium | mitigate | Reuse the bounded terminal/suppression vocabulary; diagnostics name only command and error class, while payloads, descriptor collections, stacks, identities, receipts, proofs, and credentials remain excluded. |
| T-224-32 | Repudiation | closure evidence | medium | mitigate | Audit and evidence bind the exact base/head, diff, changed symbols, commands, results, upstream-convergence status, and deterministic-only limitations. |
| T-224-SC | Tampering | exact external source pin | high | mitigate | No package install occurs; the command-safe runner verifies sanitized remote, clean exact HEAD, base ancestry, binary diff, audit/evidence digests, fixed test target, native suite, tracer, and blocked statuses. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Required item | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Manifest- and route-scoped host delegate remains behind Crosswake's safe bridge validation boundary | Tasks 1-3 | COVERED | Replay claim and config enforcement strengthen the existing single path; evidence is re-pinned. |
| REQ | BRDG-01 | Host declaration cannot bypass protocol/version/route/origin/pack/manifest validation | Tasks 1-3 | COVERED | Claim follows every guard; validated construction is mandatory; full suite re-runs. |
| REQ | BRDG-02 | Unsafe/failing host commands deny safely and cannot inject replies | Tasks 1, 3 | COVERED | Duplicate side effects/replies are suppressed and navigation terminalization remains stale-safe. |
| VERIFICATION | Gap 1 | Atomically claim admitted epoch/correlation before side effect; retire/isolate on navigation; same-ID and reuse tests | Task 1 | COVERED | Exact failed observable truth is a frontmatter must-have and end-to-end tracer. |
| VERIFICATION | Gap 2 | Enforce descriptor validation on every production path or exclude descriptors from public initializer; real-path malformed/duplicate tests | Task 2 | COVERED | Ordinary initializer loses host inputs; factory/channel/navigation regressions prove the live path. |
| CONTEXT | D-01..D-03 | Crosswake ownership, exact immutable lane, deterministic-only blocked evidence | Task 3 | COVERED | No local bridge; lock/audit/evidence refreshed; status stays blocked. |
| CONTEXT | D-04..D-07 | Manifest/registry intersection, four commands, mandatory config validation, ordered admission | Tasks 1-2 | COVERED | Constructor bypass closes; existing guard order is preserved. |
| CONTEXT | D-08..D-12 | Narrow delegate, zero-field privacy, epoch lifecycle, safe failure, bounded telemetry | Tasks 1-3 | COVERED | Replay state stays internal; no payload/reply widening; lifecycle and evidence tests re-run. |
| CONTEXT | D-13 | Deterministic denial, duplicate, navigation, and API-surface proof | Tasks 1-3 | COVERED | New exact regressions plus full native/evidence gate. |
| CONTEXT | D-14 | No end-user visual system | Tasks 1-3 | COVERED | No UI artifact or host behavior is introduced. |
| RESEARCH | source/evidence | Exact reviewed Crosswake source and deterministic evidence; no source substitution | Task 3 | COVERED | Existing authorized Alpha lane and fixed runner remain authoritative. |
| RESEARCH | architecture/security | Single validator/reply owner, manifest-registry intersection, epoch recheck, bounded data | Tasks 1-2 | COVERED | Repair occurs inside the existing Crosswake bridge/config owners. |
| CONTEXT | Deferred | Generic commerce/plugin API, StoreKit/host UI/auth/Accrue integration, device promotion, extra commands/Android | NONE | EXCLUDED | Explicitly deferred/out of phase; no task implements them. |

<verification>
Run the focused Crosswake host-command suite after Tasks 1 and 2. After committing and re-pinning Task 3, run the command-safe `source-gate`, `trusted-frame`, and `full` modes against `/Users/jon/projects/crosswake-accrue-bridge`, the Accrue tracer suite, the evidence-location/status assertion, and the clean-checkout assertion. Re-review the final base-to-patch diff to confirm only Crosswake's bridge/config/test owners changed and that no existing admission, allowlist, delegate, telemetry, or runtime-status guard was weakened.
</verification>

<success_criteria>
- Duplicate admitted requests with one route epoch and correlation invoke host work once, with an atomic claim before dispatch and at most one terminal reply.
- Route replacement retires prior claim state, suppresses stale old-route results, and admits the same correlation once in the new epoch.
- The ordinary public config initializer cannot accept host-command descriptors; every descriptor-bearing construction validates literal membership, exact shape/version, and uniqueness first.
- Malformed versions and identical/conflicting duplicate descriptors fail through the real production factory/channel/navigation path with actionable diagnostics.
- Protocol/version/frame/origin/route/pack/manifest/schema checks, four-command allowlist, narrow delegate, Crosswake reply ownership, cancellation, privacy telemetry, and no-Accrue/no-generic-API boundaries remain intact.
- Exact source lock, audit, evidence, native suite, tracer consumer, and report assertions agree at the new reviewed Crosswake revision.
- Overall and every capability remain `feasibility_blocked`; no simulator, StoreKit, host integration, physical-device, UI, entitlement, or runtime-readiness proof is claimed.
</success_criteria>

<output>
Create `.planning/phases/224-crosswake-host-command-bridge-seam/224-07-SUMMARY.md` when done.
</output>

---
phase: 225-required-lane-signal-repair
plan: 02
type: execute
wave: 2
depends_on: [225-01]
files_modified:
  - accrue_admin/e2e/admin-page-flow-phase191.spec.js
  - .github/workflows/ci.yml
  - scripts/ci/verify_phase192_ci_contract.sh
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "D-08/D-09/REL-02: the 5 × 2 × 21 Page 191 traversal is retained as bounded, independently named per-viewport Playwright tests with observed-time-derived per-case budgets."
    - "D-10: the required Admin lane remains single-worker with retries at zero; no sleep or global timeout inflation represents repair."
    - "D-11: Phase 192 report, test-results, and generated-evidence artifacts name paths that actually exist or are produced, and the generated-evidence upload fails visibly if its checked-in source disappears."
    - "D-06: `admin-hardening-guardrails`, release-gate topology, required/advisory support, assertions, screenshots, traces, and report behavior retain their established identity and semantics."
  artifacts:
    - path: "accrue_admin/e2e/admin-page-flow-phase191.spec.js"
      provides: "five independently reported viewport partitions retaining both themes and all 21 flows"
    - path: ".github/workflows/ci.yml"
      provides: "stable Phase 192 job with reachable report, test-results, and archived generated-evidence uploads"
    - path: "scripts/ci/verify_phase192_ci_contract.sh"
      provides: "static contract for stable job identity and real generated-evidence paths"
  key_links:
    - from: "accrue_admin/e2e/admin-page-flow-phase191.spec.js"
      to: "accrue_admin/e2e/phase191-page-flow-helpers.js"
      via: "PHASE191_VIEWPORTS and phase191PageFlows() drive exactly 5 × 21 coverage while PHASE191_THEMES drives both themes"
      pattern: "PHASE191_VIEWPORTS|PHASE191_THEMES|phase191PageFlows"
    - from: ".github/workflows/ci.yml"
      to: ".planning/milestones/v1.53-phases/192-idempotent-verification-sign-off"
      via: "phase192-generated-evidence upload uses the archived checked-in Phase 192 outputs"
      pattern: "v1.53-phases/192-idempotent-verification-sign-off"
    - from: "scripts/ci/verify_phase192_ci_contract.sh"
      to: ".github/workflows/ci.yml"
      via: "job_body admin-hardening-guardrails asserts exact artifact paths and always-run semantics"
      pattern: "admin-hardening-guardrails"
  assumptions:
    - id: "EDGE-REL-02-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: REL-02's specless edge was unclassified; partition equivalence and artifact availability are proven, but no SPEC-supplied edge-verification tier exists."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: A green required Admin result must not be manufactured by retrying, deleting coverage, inflating one global timeout, or suppressing first-failure evidence."
      category: transparency
      status: unresolved
      verification: null
---

<objective>
Repair the Admin Page 191 whole-test budget failure through native bounded Playwright partitions and make every promised Phase 192 artifact path truthful per D-08 through D-11.

Purpose: Restore a required Admin signal whose failures identify one bounded viewport case and retain trace-first evidence.
Output: Partitioned Page 191 test cases, reachable generated-evidence paths, and a synchronized CI contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
@.planning/phases/225-required-lane-signal-repair/225-RESEARCH.md
@.planning/phases/225-required-lane-signal-repair/225-PATTERNS.md
@.planning/phases/225-required-lane-signal-repair/225-01-SUMMARY.md
@.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-VERIFICATION.md
</context>

## Artifacts this phase produces

- `accrue_admin/e2e/admin-page-flow-phase191.spec.js` — new local constant `PHASE191_THEMES` and five generated test titles keyed by each existing `PHASE191_VIEWPORTS[*].name`; no production symbol or file.
- `.github/workflows/ci.yml` — corrected `phase192-generated-evidence` source paths under `.planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/`; no new job or check identity.
- `scripts/ci/verify_phase192_ci_contract.sh` — updated literal path expectations and missing-generated-evidence fail-closed assertion; no new public symbol.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Partition the Page 191 Cartesian traversal into per-viewport native tests</name>
  <reversibility rating="reversible">The partition changes one internal Playwright spec while preserving the same helpers and assertions.</reversibility>
  <read_first>
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js
    - accrue_admin/e2e/phase191-page-flow-helpers.js
    - accrue_admin/playwright.config.js
    - accrue_admin/e2e/admin-page-flow-phase200.spec.js
    - accrue_admin/package.json
    - scripts/ci/verify_phase192_admin_guardrails.sh
    - scripts/ci/verify_phase192_guardrail_contract.sh
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - .planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
  </read_first>
  <files>accrue_admin/e2e/admin-page-flow-phase191.spec.js</files>
  <behavior>
    - Coverage invariant: `PHASE191_VIEWPORTS.length === 5`, `PHASE191_THEMES.length === 2`, `phase191PageFlows().length === 21`, and their product is 210 full login/navigate/check cycles per existing traversal contract.
    - Reporting invariant: the responsive/scroll/copy contract produces one uniquely named Playwright test per viewport, so a timeout identifies `phone-320`, `phone-375`, `tablet-768`, `desktop-1024`, or `desktop-1440`.
    - Behavioral invariant: each partition resets/seeds its own fixtures and runs both themes, all 21 flows, and the original clip, focus, scroll, and generic-error-copy assertions.
  </behavior>
  <action>Start by adding the coverage invariant to the existing source-map/fixture contract and observe the current monolithic shape fail that expected independently reported partition count. Add local `PHASE191_THEMES = Object.freeze(["light", "dark"])`. Replace only the test beginning with `AX187-436 ... page flows avoid clipping` with a declaration loop over the existing five `PHASE191_VIEWPORTS`, emitting one native `test(...)` whose title ends in the viewport name. Inside each test, call `test.setTimeout(30_000)`: the trace shows the full 210-cycle body reaching ordinary work around 64 seconds, so the observed per-viewport share is about 13 seconds and 30 seconds retains more than 2× headroom without global inflation. Each case must run `reset(request)` and `seedPhase191Matrix(request)`, set only its assigned viewport, then iterate `PHASE191_THEMES` and every `phase191PageFlows()` entry through the unchanged `login`, `setPhase191Theme`, `assertNoHorizontalClip`, `assertNoBodyFocus`, `assertScrollReachable`, and `GENERIC_ERROR_COPY_PATTERN` assertions. Keep `workers: 1`, `fullyParallel: false`, trace retention, screenshot behavior, reporter, web server, package script, all other tests, selectors, viewports, themes, and flows unchanged per D-09/D-10. Do not add retries, sleeps, a serial wrapper, a CI matrix, a global timeout change, or reduced coverage.</action>
  <acceptance_criteria>Playwright lists five uniquely named responsive/scroll/copy tests for each selected browser project, each owns one viewport and a 30-second budget, and source assertions prove all five viewports × both themes × all 21 flows still execute the original assertion body. Configuration remains `workers: 1`, retries resolve to zero, and trace/screenshot/report settings are unchanged.</acceptance_criteria>
  <verify>
    <automated>cd accrue_admin &amp;&amp; npx playwright test e2e/admin-page-flow-phase191.spec.js --list --project=chromium-desktop --workers=1 | tee /tmp/phase225-pageflow-list.txt &amp;&amp; test "$(rg -c 'page flows avoid clipping.*(phone-320|phone-375|tablet-768|desktop-1024|desktop-1440)' /tmp/phase225-pageflow-list.txt)" -eq 5 &amp;&amp; npm run e2e:phase191</automated>
  </verify>
  <done>The trace-backed capacity failure is removed by five bounded native test units without losing a viewport, theme, flow, assertion, or first-failure artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Point Phase 192 generated evidence at real archived outputs and lock the contract</name>
  <reversibility rating="costly">The stable CI artifact contract and its static verifier must change together; undoing one without the other makes evidence silently unavailable.</reversibility>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/ci/verify_phase192_ci_contract.sh
    - scripts/ci/verify_phase192_guardrail_contract.sh
    - scripts/ci/verify_phase192_admin_guardrails.sh
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/final.cells.json
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/scorecard.delta.json
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/regressions.ndjson
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/artifacts.manifest.json
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-SCORECARD.md
    - .planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/192-VERIFICATION.md
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
  </read_first>
  <files>.github/workflows/ci.yml, scripts/ci/verify_phase192_ci_contract.sh</files>
  <behavior>
    - Static contract rejects the five obsolete `.planning/phases/192-idempotent-verification-sign-off/` paths and requires their five existing archived equivalents.
    - The `phase192-generated-evidence` step remains `if: always()` and fails visibly when an archived source is missing; report and test-results uploads retain their current always-run behavior.
    - Job ID/name, setup/command order, release matrix, and artifact names remain stable.
  </behavior>
  <action>First update `verify_phase192_ci_contract.sh` so its `admin-hardening-guardrails` job assertions require `final.cells.json`, `scorecard.delta.json`, `regressions.ndjson`, `artifacts.manifest.json`, and `192-SCORECARD.md` beneath the archived Phase 192 directory named in `read_first`, and reject the obsolete unarchived prefix inside that generated-evidence step. Add a source assertion that the generated-evidence upload uses `if-no-files-found: error`, making disappearance visible. Then update only the `Upload Phase 192 generated evidence` path block in `.github/workflows/ci.yml` to those five archived paths and set that step's missing-file policy to `error`. Preserve the existing job ID/name, `if: always()`, artifact name `phase192-generated-evidence`, Playwright report/test-results steps and names, command order, Node/BEAM/Postgres setup, `admin-hardening-guardrails`, all release-gate cells, `fail-fast: false`, and advisory Sigra semantics per D-06/D-11. Do not regenerate the historical 32+ MB evidence in CI, add a new job, change retention/topology/cache, or claim the historical scorecard is fresh Phase 225 repair proof.</action>
  <acceptance_criteria>All five generated-evidence paths exist in the checkout and are required by both workflow and static contract; the generated upload cannot silently succeed with no files; both Phase 192 contract scripts pass; the required Admin job identity, report/test-results artifacts, release topology, and required/advisory labels are byte-for-behavior preserved.</acceptance_criteria>
  <verify>
    <automated>for file in final.cells.json scorecard.delta.json regressions.ndjson artifacts.manifest.json 192-SCORECARD.md; do test -e ".planning/milestones/v1.53-phases/192-idempotent-verification-sign-off/$file" || exit 1; done &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_guardrail_contract.sh</automated>
  </verify>
  <done>The Phase 192 CI contract names real evidence, fails closed if it disappears, and retains the stable required Admin check and trace-first artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Playwright fixture/server → browser assertion case | Each bounded test consumes test-server state and must reset before evaluating UI assertions. |
| repository evidence paths → GitHub artifact service | Checked-in historical outputs and runtime Playwright outputs cross into externally retained Actions artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-225-05 | Tampering | Page 191 partition | high | mitigate | Source-level 5 × 2 × 21 invariant and unchanged assertion body prevent a faster green result from silently dropping coverage. |
| T-225-06 | Repudiation | Playwright result identity | medium | mitigate | Native uniquely named viewport tests retain reporter, trace, screenshot, and per-case timeout attribution. |
| T-225-07 | Information disclosure | Phase 192 artifacts | medium | mitigate | Upload only existing test/generated evidence already governed by the Phase 192 record; no raw content is copied into Phase 225 Markdown. |
| T-225-08 | Denial of service | whole-test timeout | high | mitigate | Five independently budgeted tests bound the unit of work while keeping `workers: 1` and zero retries. |
| T-225-SC | Tampering | npm/action dependencies | high | mitigate | No install or version change is planned; use the existing lockfile and already-pinned action references. |
</threat_model>

<verification>
Run the Page 191 spec with preserved output/report directories, then both Phase 192 static contracts. Confirm `git diff` shows no Playwright config, package script, runner order, release matrix, cache, job ID, or artifact-name changes.
</verification>

<success_criteria>
- Five bounded viewport tests retain 210 traversal cycles and every original assertion.
- Required Admin execution stays single-worker and zero-retry with trace/report/screenshot behavior intact.
- All Phase 192 artifact paths are truthful and missing generated evidence fails visibly.
- Stable release and Admin check identities and required/advisory semantics remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/225-required-lane-signal-repair/225-02-SUMMARY.md` when done.
</output>

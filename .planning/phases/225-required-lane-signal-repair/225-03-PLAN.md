---
phase: 225-required-lane-signal-repair
plan: 03
type: execute
wave: 3
depends_on: [225-01, 225-02]
files_modified:
  - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
autonomous: true
requirements: [REL-01, REL-02, REL-03]
must_haves:
  truths:
    - "D-12/REL-01: every active red signature maps to exactly one classified incident with targeted reproduction, negative control, and full local-suite evidence."
    - "D-12/REL-02: a fresh workflow_dispatch run against the repair commit—not a rerun—shows all three required release cells and `admin-hardening-guardrails` passing."
    - "D-12: fresh-run evidence preserves job/check identity and links the Phase 192 report/generated artifacts while static contracts retain first-failure test-results upload behavior."
    - "D-13/REL-03: the fresh proof lists Floor, Primary, and OpenTelemetry as required and records Sigra's actual `[advisory]` result separately."
  artifacts:
    - path: ".planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md"
      provides: "completed targeted, negative-control, full-local, and fresh repair-commit proof for both normalized incidents"
  key_links:
    - from: ".planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md"
      to: "GitHub Actions CI run"
      via: "recorded repair SHA, run ID/URL, exact required job names/URLs, advisory result, and artifact URLs"
      pattern: "Fresh repair SHA|Fresh repair run ID|admin-hardening-guardrails|Sigra"
    - from: "fresh Actions run"
      to: ".github/workflows/ci.yml"
      via: "workflow_dispatch on the pushed repair branch using unchanged stable job IDs"
      pattern: "workflow_dispatch"
  assumptions:
    - id: "EDGE-REL-03-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: REL-03's specless edge was unclassified; identical known signatures are intentionally one incident, but no broader SPEC-supplied normalization boundary exists."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: A rerun, advisory result, skipped lane, or artifact-upload step with no payload must not be represented as fresh required repair proof."
      category: transparency
      status: unresolved
      verification: null
---

<objective>
Close both incidents with targeted and full local proof, then bind the repaired signatures to one fresh repair-commit Actions run per D-12/D-13.

Purpose: Ensure the phase ends with causal evidence for the actual repaired commit rather than a local-only green or rerun.
Output: Completed `225-CI-INCIDENTS.md` evidence fields with required/advisory job and artifact links.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
@.planning/phases/225-required-lane-signal-repair/225-RESEARCH.md
@.planning/phases/225-required-lane-signal-repair/225-PATTERNS.md
@.planning/phases/225-required-lane-signal-repair/225-01-SUMMARY.md
@.planning/phases/225-required-lane-signal-repair/225-02-SUMMARY.md
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

- `.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md` — fills the existing incident record's targeted, negative-control, full-local, fresh SHA/run/job/artifact, and residual-status fields; no new file or code symbol.

## Planner contribution decisions

- External API coverage: the deterministic detector matched first-party “connect … webhook” prose, but the final scope adds no external API/SDK/service integration. `COVERAGE.md` records the reasoned no-integration declaration.
- Assumption delta: `no-change`; the primary noun stays a normalized required-lane failure-signature incident, so no promote/add-alongside action is needed.
- Schema push: deterministic schema-path detection returned no matches for the final file scope; no ORM command is injected.

## Multi-source coverage audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every active required release/Admin failure has trace-backed classification and repaired cause | 01-03 | COVERED | Incident tracer → two causal repairs → fresh Actions proof |
| REQ | REL-01 | Reproduce and classify each current signature | 01, 03 | COVERED | Exact commands, incident fields, targeted/full/fresh evidence |
| REQ | REL-02 | Required checks pass with assertions/artifacts retained | 02, 03 | COVERED | Bounded tests, truthful uploads, fresh required jobs |
| REQ | REL-03 | Same matrix-wide signature is one incident | 01, 03 | COVERED | One webhook incident, separate cell labels |
| CONTEXT | D-01 | One complete phase-local incident record | 01, 03 | COVERED | Created in tracer; completed after live proof |
| CONTEXT | D-02 | Durable index; raw evidence remains artifacts/privacy-safe | 01 | COVERED | Links only, no log dumps or sensitive values |
| CONTEXT | D-03 | Failure/classification/command/evidence first; exact voice | 01 | COVERED | Incident lead and README triage |
| CONTEXT | D-04 | Four release cells are one over-broad-observation incident | 01 | COVERED | One incident with affected-cell table |
| CONTEXT | D-05 | Event-owned webhook/job/ledger assertions and duplicate negative | 01 | COVERED | Identity-scoped ExUnit predicates |
| CONTEXT | D-06 | Preserve gate topology/identity; no retry/serialization/deletion/cache changes | 01-03 | COVERED | Static contracts and live job checks |
| CONTEXT | D-07 | Truthful rollback/co-presence test | 01 | COVERED | Success-only test is renamed after seam inspection |
| CONTEXT | D-08 | Trace-backed Admin capacity/topology classification | 01, 02 | COVERED | 210-cycle/60-second incident and repair |
| CONTEXT | D-09 | Bounded tests retain all coverage and evidence | 02 | COVERED | Per-viewport native tests |
| CONTEXT | D-10 | Zero retries; deterministic boundaries; no sleeps/global inflation | 02 | COVERED | Config preserved and source assertions |
| CONTEXT | D-11 | Preserve report/test-results; repair generated-evidence path | 02, 03 | COVERED | Archived paths + fail-closed contract + live artifact proof |
| CONTEXT | D-12 | Targeted, negative, full local, fresh repair-commit Actions proof | 03 | COVERED | Two explicit evidence tasks |
| CONTEXT | D-13 | Three required cells distinct from advisory Sigra | 01, 03 | COVERED | Static incident labels and live job classification |
| RESEARCH | — | Identity-scoped Ecto/Oban/ledger assertions | 01 | COVERED | No whole-table observation |
| RESEARCH | — | Native Playwright parameterization and coverage invariant | 02 | COVERED | Five named cases, 5 × 2 × 21 proof |
| RESEARCH | — | Truthful Phase 192 generated-evidence producer/path | 02 | COVERED | Existing archived outputs replace missing paths |
| RESEARCH | — | Local/full/fresh Actions validation architecture | 03 | COVERED | Command and GitHub evidence gates |

Deferred Phase 226 baselines/provider expansion/setup ownership, Phase 227 topology/cache/matrix optimization, Admin redesign/ratchet, and StoreKit/iPhone/Crosswake work are excluded exactly as scoped.

<tasks>

<task type="auto">
  <name>Task 1: Run targeted negative controls and full local suites, then record exact evidence</name>
  <precondition>Plans 225-01 and 225-02 summaries exist and their modified files are present in the current checkout.</precondition>
  <read_first>
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - .planning/phases/225-required-lane-signal-repair/225-01-SUMMARY.md
    - .planning/phases/225-required-lane-signal-repair/225-02-SUMMARY.md
    - accrue/test/accrue/webhook/ingest_test.exs
    - accrue_admin/e2e/admin-page-flow-phase191.spec.js
    - scripts/ci/verify_phase192_ci_contract.sh
    - scripts/ci/verify_phase192_guardrail_contract.sh
    - .github/workflows/ci.yml
  </read_first>
  <files>.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md</files>
  <action>From a clean dependency state, run the focused webhook file, both Phase 192 static contracts, the full `accrue` suite with warnings as errors, and the full Page 191 Playwright package script. Preserve Playwright's configured report and test-results directories on failure; if any command fails, leave both incident statuses open, link the local artifact path/error signature without copying sensitive output, and stop before dispatch. On success, update each incident's targeted reproduction, relevant negative-control proof, and full-local evidence with the exact command, UTC timestamp, current commit SHA, result, and concise mechanism proved. For the release incident, name the same-identity duplicate test and full ExUnit suite. For Admin, record five independently reported viewport partitions, both themes, all 21 flows, zero retries/single worker, full Page 191 result, static artifact-contract result, and the preserved local report/test-results locations. Do not invent numeric suite counts; record only observed output from this execution per D-12.</action>
  <acceptance_criteria>All four evidence commands exit zero in this execution; both incident sections contain exact command/SHA/timestamp/result rows for targeted, negative-control, and full-local proof; observed counts are copied from current output only; no incident is marked repaired if any required command failed.</acceptance_criteria>
  <verify>
    <automated>cd accrue &amp;&amp; mix test test/accrue/webhook/ingest_test.exs --warnings-as-errors &amp;&amp; mix test --warnings-as-errors &amp;&amp; cd .. &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_guardrail_contract.sh &amp;&amp; cd accrue_admin &amp;&amp; npm run e2e:phase191</automated>
  </verify>
  <done>Both repaired causes have targeted negative controls and full local-suite evidence tied to the current repair SHA, with failure artifacts preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch and verify a fresh repair-commit Actions run, then close the incident ledger</name>
  <precondition>`gh auth status` succeeds with Actions read/write and repository push access; the worktree is clean; Task 1's committed repair SHA can be pushed to a non-main branch.</precondition>
  <reversibility rating="reversible">A workflow dispatch and branch push are auditable and can be superseded without migration or published-contract breakage.</reversibility>
  <read_first>
    - .planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
    - .github/workflows/ci.yml
    - scripts/ci/verify_phase192_ci_contract.sh
    - scripts/ci/README.md
    - .planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
  </read_first>
  <files>.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md</files>
  <action>Capture the committed repair SHA before this evidence-only update and require a clean worktree. Use the current non-main branch, or create `gsd/phase-225-required-lane-signal-repair` at that SHA when the checkout is on `main`; push that branch explicitly to `origin`. Dispatch a new `ci.yml` run with `gh workflow run ci.yml --ref <branch>`—never use the rerun command—and locate the workflow_dispatch run whose `headSha` equals the captured repair SHA. Watch it to completion and fail closed unless all three release jobs whose names lack `[advisory]`—Floor, Primary dev target, and OpenTelemetry—conclude success and `Admin hardening guardrails (Phase 192)` concludes success. Record Sigra's separately labeled `[advisory]` job URL and actual conclusion without counting it among required proof per D-13. Query the run's artifacts and require/link `phase192-admin-playwright-report` and `phase192-generated-evidence`; also record the `Upload Phase 192 Playwright evidence` step result, noting that its preserved test-results path is a first-failure affordance and may have no payload on a clean run. Update both incidents with `Fresh repair SHA:`, `Fresh repair run ID:`, run URL, required job URLs/conclusions, advisory URL/conclusion, artifact URLs, exact topology/check-identity assertions, and residual owner/status. Mark an incident `repaired` only after its responsible required job is green on this fresh run and every D-12 field is present; otherwise leave it open with the exact failing signature and owner.</action>
  <acceptance_criteria>The recorded run is a workflow_dispatch event for the exact repair SHA and is not a rerun; exactly three non-advisory release matrix jobs and the stable Admin Phase 192 job conclude success; advisory Sigra is visibly separate; report/generated artifact URLs resolve; test-results upload semantics remain recorded; both incident statuses and residual fields reflect the actual run rather than assumed success.</acceptance_criteria>
  <verify>
    <automated>DOC=.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md &amp;&amp; RUN_ID=$(sed -n 's/^Fresh repair run ID: \([0-9][0-9]*\)$/\1/p' "$DOC" | head -1) &amp;&amp; REPAIR_SHA=$(sed -n 's/^Fresh repair SHA: \([0-9a-f][0-9a-f]*\)$/\1/p' "$DOC" | head -1) &amp;&amp; test -n "$RUN_ID" &amp;&amp; test -n "$REPAIR_SHA" &amp;&amp; gh run view "$RUN_ID" --json event,headSha,status,conclusion,jobs,url | jq -e --arg sha "$REPAIR_SHA" '.event == "workflow_dispatch" and .headSha == $sha and .status == "completed" and .conclusion == "success" and ([.jobs[] | select(.name | startswith("Release gate (")) | select((.name | contains("[advisory]")) | not)] | length == 3) and all(.jobs[] | select(.name | startswith("Release gate (")) | select((.name | contains("[advisory]")) | not); .conclusion == "success") and any(.jobs[]; .name == "Admin hardening guardrails (Phase 192)" and .conclusion == "success") and any(.jobs[]; (.name | contains("[advisory]")))' &amp;&amp; gh api "repos/{owner}/{repo}/actions/runs/$RUN_ID/artifacts" | jq -e '([.artifacts[].name] | index("phase192-admin-playwright-report") != null) and ([.artifacts[].name] | index("phase192-generated-evidence") != null)' &amp;&amp; test "$(rg -c '^## INC-225-' "$DOC")" -eq 2 &amp;&amp; test "$(rg -c '^Status: repaired$' "$DOC")" -eq 2</automated>
  </verify>
  <done>Both normalized incidents are closed by one fresh repair-commit run with three required release cells and the stable Admin job green, advisory Sigra separate, and artifact evidence linked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local committed repair → remote branch/workflow dispatch | The exact SHA and branch ref must remain bound so a different commit cannot supply green evidence. |
| GitHub run/job/artifact JSON → incident completion status | External CI metadata must be validated by event, SHA, stable job names, support label, conclusion, and artifact identity before closing incidents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-225-09 | Spoofing | fresh Actions proof | high | mitigate | Bind `workflow_dispatch` run `headSha` to the recorded committed repair SHA and reject rerun/other-event evidence. |
| T-225-10 | Tampering | required/advisory classification | high | mitigate | Machine-count exactly three release jobs without `[advisory]`; record the advisory job separately and never use it to satisfy required proof. |
| T-225-11 | Repudiation | incident closure | high | mitigate | Persist run/job/artifact URLs, exact SHA, UTC timestamps, conclusions, commands, and residual status in the causal index. |
| T-225-12 | Information disclosure | CI output capture | medium | mitigate | Record metadata and immutable URLs only; raw Playwright/server evidence stays in Actions artifacts per D-02. |
| T-225-SC | Tampering | GitHub CLI/dependencies | high | mitigate | Use the already-configured `gh` client and repository workflow; no package install or action-version change occurs. |
</threat_model>

<verification>
Re-run the automated GitHub metadata gate from the recorded run ID/SHA, resolve the recorded artifact URLs, and confirm the incident ledger has two repaired statuses only after all required jobs passed.
</verification>

<success_criteria>
- Targeted negative controls and full local suites are green for the recorded repair SHA.
- A new workflow_dispatch run—not a rerun—binds to that exact SHA.
- Three required release cells and the stable Admin Phase 192 job pass.
- Sigra stays visibly advisory and does not satisfy required proof.
- Both incident records link exact job/artifact evidence and retain meaningful failure diagnostics.
</success_criteria>

<output>
Create `.planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md` when done.
</output>

---
phase: 226-ci-baseline-proof-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/capture_ci_baseline.sh
  - scripts/ci/verify_ci_baseline_contract.sh
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02
must_haves:
  truths:
    - "BASE-01: A maintainer can reproduce a metadata-only record for Actions run 31322443304 without downloading logs, traces, payloads, environment values, or artifact archives."
    - "BASE-01: The initial record reports 2,380 seconds wall time, an approximately 39m36s required critical path, and queue delay measured in seconds, explicitly superseding the old 33-36 minute assumption."
    - "BASE-02: Every observed lane has separate policy, conclusion, and proof_state fields, and only a qualifying required success can be proved."
    - "BASE-02: The baseline records GitHub's effective ruleset and classic branch-protection required-check responses for main, including an explicit no-enforced-checks state rather than inferring protection from YAML."
  artifacts:
    - path: "scripts/ci/capture_ci_baseline.sh"
      provides: "Strict gh/jq metadata collector with live and fixture inputs"
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Fail-closed schema, taxonomy, privacy, and negative-control checks"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Canonical allowlisted baseline facts beginning with run 31322443304"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Human-readable initial measurement and privacy posture"
  key_links:
    - from: "scripts/ci/capture_ci_baseline.sh"
      to: "GitHub Actions run/job/artifact metadata APIs"
      via: "API-versioned read-only gh requests reduced immediately through jq allowlists"
      pattern: "X-GitHub-Api-Version: 2022-11-28"
    - from: "scripts/ci/verify_ci_baseline_contract.sh"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      via: "schema, privacy, duration, eligibility, and proof-state assertions"
      pattern: "proved|skipped|advisory|not-applicable"
---

<objective>
Establish the production-quality tracer from one real GitHub Actions run through an allowlisted collector, canonical JSON, readable baseline, and fail-closed contract.

Purpose: Prove BASE-01/BASE-02 end to end on the Phase 225 repair run before spending two more full CI dispatches.
Output: Metadata-only collector, contract, and initial one-run baseline that records the observed 39m36s critical path rather than the superseded estimate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md
@.planning/phases/225-required-lane-signal-repair/225-CI-INCIDENTS.md
@scripts/ci/annotation_sweep.sh
@scripts/ci/verify_phase192_ci_contract.sh
@.github/workflows/ci.yml

<interfaces>
Implement `scripts/ci/capture_ci_baseline.sh` with this stable CLI:
- `--run-id ID` may be repeated; at least one is required.
- `--output PATH` writes the canonical combined JSON atomically.
- `--fixture-dir PATH` replaces live API reads with sanitized run/jobs/artifacts fixture responses for contract self-tests.
- Live mode derives `owner/repo` from `GITHUB_REPOSITORY` or the git remote, validates numeric IDs, requires `gh` and `jq`, and uses only read endpoints.

Implement `scripts/ci/verify_ci_baseline_contract.sh` with `--self-test` for synthetic fixture/negative-control coverage and `--input PATH` to validate a collected JSON file; without `--input`, validate the canonical checked-in baseline.

The JSON owns facts; Markdown is a review rendering. Required enums are:
- policy: `required`, `advisory`, `conditional`
- proof_state: `proved`, `skipped`, `advisory`, `not-applicable`
- cache_state: `observed-hit`, `observed-miss`, `unknown`

The authoritative external required-check snapshot comes from `GET /repos/szTheory/accrue/rules/branches/main` plus `GET /repos/szTheory/accrue/branches/main/protection/required_status_checks`. Store only response state, check context, and app ID; `[]` plus a classic-protection 404 is the explicit `none-enforced` state.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace run 31322443304 from Actions metadata to a verified baseline</name>
  <reversibility rating="costly">Phase 227 will consume this schema, so changing it later requires coordinated collector, contract, JSON, and renderer edits.</reversibility>
  <files>scripts/ci/capture_ci_baseline.sh, scripts/ci/verify_ci_baseline_contract.sh, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <behavior>
    - Fixture and live paths emit the same allowlisted schema and preserve provider omissions as null.
    - Run 31322443304 records event workflow_dispatch, attempt 1, SHA ee940cf9e1f86b4d7c551b15ce113feb7f2a2997, 2,380 wall seconds, and the measured required-chain stages.
    - The readable baseline states that the approximately 39m36s critical path is staged release-gate through admin-drift-docs, host-integration, Playwright, and annotation sweep, while queue delay is only seconds.
  </behavior>
  <action>Start by making the contract fail against missing artifacts, then implement the strict Bash collector using the `annotation_sweep.sh` startup, temporary-directory cleanup, API-version, validation, and pagination patterns. Reduce every provider response immediately through an explicit jq field allowlist; derive run/job/step durations from timestamps, retain attempt and rerun facts, record artifact name/size/expiry only, and normalize root signatures without carrying message bodies. Snapshot GitHub-enforced checks from the effective branch-rules and classic required-status-check endpoints named in the interface; record response state plus allowlisted check context/app ID, and represent the observed empty-rules/classic-404 result as `none-enforced` rather than inferring protection from YAML comments or workflow success. Do not call log endpoints, artifact-download endpoints, check annotation payloads, or serialize environment data, raw event payloads, traces, reports, screenshots, server output, or any URL containing a query string. Populate the first canonical datum from run 31322443304 and render a concise Markdown table that names JSON as authority. Correct `226-VALIDATION.md` before wave sampling so its full command uses `--run-id 31322443304 --output "$tmp_file"` with a temporary output and then invokes the verifier as `--input "$tmp_file"`. Preserve Phase 225 job and artifact identities without changing `.github/workflows/ci.yml`. Commit this task atomically as `feat(226-01): add metadata-only CI baseline tracer`.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; tmp_file="$(mktemp)" &amp;&amp; trap 'rm -f "$tmp_file"' EXIT &amp;&amp; bash scripts/ci/capture_ci_baseline.sh --run-id 31322443304 --output "$tmp_file" &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh --input "$tmp_file" &amp;&amp; jq -e '.required_check_snapshot.enforcement_state == "none-enforced" and (.runs[] | select(.run.id == 31322443304 and .run.attempt == 1 and .run.wall_seconds == 2380))' "$tmp_file" &gt;/dev/null</automated>
  </verify>
  <done>The single real run is collected and rendered end to end, its contrary measured timing is explicit, the contract is green, and the task is committed independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce proof-state semantics and privacy negative controls</name>
  <files>scripts/ci/capture_ci_baseline.sh, scripts/ci/verify_ci_baseline_contract.sh, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <behavior>
    - A required successful job on an eligible first attempt maps to proved.
    - A skipped conclusion maps to skipped; a non-blocking lane maps to advisory even when successful; an event-excluded lane maps to not-applicable.
    - None of skipped, advisory, or not-applicable can satisfy aggregate release proof.
    - Synthetic fixtures containing secret/env fields, payload bodies, log/archive material, or query-bearing URLs fail closed.
  </behavior>
  <action>Expand the fixture-driven contract around the exact four-state vocabulary. Store workflow declaration, observed conclusion, and derived proof state separately; qualify `proved` only when policy is required, the run is eligible, attempt is 1, and conclusion is success. Add synthetic negative controls generated in a temporary directory so no unsafe sample data is committed. Assert artifact presence alone, workflow success, a green advisory cell, or a skipped failure-only upload cannot become release proof. Cover cache evidence conservatively with observed-hit, observed-miss, and unknown rather than inferring a hit from duration. Ensure the baseline privacy section enumerates the committed allowlist and Actions-owned raw-evidence boundary. Commit this task atomically as `test(226-01): lock proof and privacy semantics`.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; jq -e '[.runs[].jobs[].proof_state] | all(. == "proved" or . == "skipped" or . == "advisory" or . == "not-applicable")' .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &gt;/dev/null</automated>
  </verify>
  <done>All four states are mechanically exhaustive, non-proof states cannot satisfy release proof, privacy negative controls fail closed, and the task is committed independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions API → collector | Authenticated third-party JSON enters repository tooling and must be reduced before persistence. |
| Collector → checked-in baseline | Only allowlisted metadata may cross from transient responses into durable git artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-226-01 | Information Disclosure | capture_ci_baseline.sh output boundary | high | mitigate | Never request logs or archives; immediately allowlist fields; reject query-bearing URLs and secret/env/payload-shaped keys in the self-test. |
| T-226-02 | Tampering | proof-state derivation | medium | mitigate | Validate enums and derive proved only from required policy + eligible attempt-one success; contract all non-proof negative cases. |
| T-226-03 | Repudiation | run identity | medium | mitigate | Persist run ID, event, immutable SHA, attempt, timestamps, eligibility, and exclusion reason. |
| T-226-SC | Tampering | package supply chain | low | accept | No npm, pip, or cargo package is introduced or installed by this plan. |
</threat_model>

<verification>
Run `bash scripts/ci/verify_ci_baseline_contract.sh --self-test`, then perform the live read-only collection for run 31322443304 and compare its fixed identity/timing facts without committing the temporary output.
</verification>

<success_criteria>
- Run 31322443304 is a reproducible metadata-only tracer with exact identity and wall time.
- The approximately 39m36s critical chain and seconds-scale queue observation replace the 33-36 minute assumption.
- Proof and cache enums are closed and fail-closed.
- Privacy negative controls prove unsafe response shapes cannot enter the committed baseline.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md` when done.
</output>

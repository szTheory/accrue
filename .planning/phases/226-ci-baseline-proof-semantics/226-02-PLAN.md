---
phase: 226-ci-baseline-proof-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 226-01
files_modified:
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02
must_haves:
  truths:
    - "BASE-01: The canonical baseline contains three eligible green, first-attempt workflow_dispatch runs from the same current workflow shape."
    - "BASE-01: Every inspected but ineligible run remains visible with a concrete exclusion reason rather than disappearing from the cohort."
    - "BASE-01: Median, minimum, and maximum timing plus measured cache, Docker/browser setup, rerun, provider, and root-signature facts are derived only from the eligible cohort."
    - "BASE-02: Aggregate proof is computed from qualifying required proved lanes and cannot be satisfied by skipped, advisory, or not-applicable observations."
    - "Phase 227 cannot choose an optimization without citing a measured cohort field and affected critical-path stage from this baseline."
  artifacts:
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Canonical three-run cohort, exclusions, aggregates, and Phase 227 selection inputs"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Reviewable cohort tables, critical-path conclusion, proof state, and candidate ranking"
  key_links:
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      to: "scripts/ci/capture_ci_baseline.sh"
      via: "Two additional run IDs collected with the Plan 01 allowlisted schema"
      pattern: "eligible|exclusion_reason"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      to: "Phase 227 optimization selection"
      via: "Measured critical-path candidate table with exact JSON field references and run IDs"
      pattern: "Phase 227 selection gate"
---

<objective>
Expand the one-run tracer into the comparable three-run first-attempt cohort and publish the measured facts Phase 227 must use.

Purpose: Satisfy BASE-01/BASE-02 with real current-workflow-shape evidence rather than assumptions or rerun-biased timings.
Output: Three eligible dispatches, retained exclusions, aggregate timing/proof tables, and an evidence-bound optimization-selection contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@scripts/ci/capture_ci_baseline.sh
@scripts/ci/verify_ci_baseline_contract.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch and qualify two additional current-shape first-attempt runs</name>
  <precondition>`gh auth status` succeeds for `szTheory/accrue` and the authenticated principal can dispatch/read the CI workflow; if not, stop for the executor's dynamic authentication checkpoint without exposing credentials.</precondition>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json</files>
  <action>Resolve the exact committed HEAD containing Plan 01 artifacts and verify `.github/workflows/ci.yml` has the same stable job IDs, release matrix definitions, runner image family, and relevant lockfile hashes as the anchor shape. Use `gh workflow run ci.yml --ref` to start two distinct workflow_dispatch runs on that exact ref, capture their run IDs before polling, and wait for both terminal results. Do not use `gh run rerun`: every eligible datum must remain attempt 1. Collect each candidate with `capture_ci_baseline.sh`; require workflow_dispatch, current shape, attempt 1, green required lanes, and matching comparison fields. Retain every rejected candidate in the JSON with its run ID, observed root-signature ID when available, and a precise exclusion reason. A failed candidate is evidence, not a retry-masked proof: record it and halt for diagnosis instead of quietly replacing it. Commit the qualified run metadata atomically as `docs(226-02): record comparable CI dispatch cohort`.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; jq -e '([.runs[] | select(.eligibility.eligible == true)] | length) == 3 and ([.runs[] | select(.eligibility.eligible == true) | select(.run.event != "workflow_dispatch" or .run.attempt != 1)] | length) == 0' .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &gt;/dev/null</automated>
  </verify>
  <done>Exactly three eligible current-shape green attempt-one dispatches exist, all inspected exclusions remain explained, and the external-run evidence is committed independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate the cohort and bind Phase 227 to measured candidates</name>
  <reversibility rating="costly">Phase 227 must cite this cohort policy and candidate evidence, so changing eligibility or aggregation later requires re-collecting and re-reviewing the optimization basis.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <behavior>
    - Aggregates use only the three eligible attempt-one runs and retain per-run values beside median/min/max.
    - Queue, wall, critical-chain, job/step, cache, Docker, Node/npm, browser, Playwright, provider, rerun, and normalized root-signature observations remain distinguishable.
    - The selection table references exact JSON paths and run IDs; an unmeasured candidate is ineligible for Phase 227 selection.
  </behavior>
  <action>Compute cohort medians and ranges from provider timestamps without rounding away the per-run source facts. Render tables for eligibility/exclusions, wall/queue/critical-path stages, job/step durations, reruns, cache states, Docker and browser setup costs, provider policy/conclusion/proof state, artifact metadata, and normalized root signatures. Preserve the anchor's exact 2,380-second wall time and approximately 39m36s chain and state whether the two new runs confirm or contradict each observed conclusion; never reinstate the 33-36 minute assumption. Add a `Phase 227 selection gate` section and matching JSON object requiring the chosen optimization to cite eligible run IDs, exact measured fields, the affected critical-path stage, and baseline median/range. Rank only candidates supported by the cohort; distinguish repeated setup cost from actual critical-path cost. Re-run privacy negative controls after rendering. Commit this task atomically as `docs(226-02): publish measured three-run baseline`.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; jq -e '.cohort.eligible_count == 3 and (.aggregates.wall_seconds.median | numbers) and (.phase_227_selection_gate.required_evidence_fields | length &gt;= 4)' .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &gt;/dev/null &amp;&amp; rg -n '39m36s|Phase 227 selection gate|queue|excluded|proved|skipped|advisory|not-applicable' .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</automated>
  </verify>
  <done>The durable baseline contains the complete comparable cohort, all required measurements and proof distinctions, retained exclusions, and a mechanical measured-evidence gate for Phase 227.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor → GitHub workflow dispatch | A repository task causes two real external CI executions on an immutable reviewed ref. |
| Candidate runs → eligible cohort | Provider success must not bypass event/attempt/topology/runner/lockfile comparability checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-226-04 | Spoofing | cohort membership | medium | mitigate | Bind every datum to run ID, immutable SHA, event, attempt, workflow shape, runner family, matrix, and lock hashes. |
| T-226-05 | Repudiation | excluded/failed candidates | medium | mitigate | Preserve exclusions and normalized root-signature IDs; never erase failed candidates or convert reruns into fresh proof. |
| T-226-06 | Denial of Service | workflow dispatch | low | accept | Exactly two parallel dispatches are authorized for the cohort; halt rather than creating replacement traffic after a failure. |
| T-226-SC | Tampering | package supply chain | low | accept | The plan uses existing gh/jq and existing workflow dependencies; it adds no package install. |
</threat_model>

<verification>
The baseline contract must prove exactly three eligible attempt-one dispatches, preserve every exclusion, recompute aggregates from eligible members, and enforce the Phase 227 measured-selection fields.
</verification>

<success_criteria>
- Three comparable green workflow_dispatch runs form the cohort.
- Per-run and aggregate critical-path/setup/cache/provider facts are reviewable and privacy-safe.
- Exclusions and reasons are durable.
- Only qualifying required proved lanes satisfy release proof.
- Phase 227 selection is mechanically tied to measured cohort evidence.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md` when done.
</output>

---
phase: 226-ci-baseline-proof-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 226-02
files_modified:
  - .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md
  - scripts/ci/README.md
  - scripts/ci/verify_ci_baseline_contract.sh
  - .github/workflows/ci.yml
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - OWN-01
must_haves:
  truths:
    - "OWN-01: A host maintainer can identify CI versus host ownership for Node/npm, browser installation, Playwright, PostgreSQL, fixtures, ports, and server lifecycle."
    - "OWN-01: Each documented setup failure has a first diagnostic command, owner, expected signal, and next safe action using existing host scripts."
    - "BASE-02: The fail-closed baseline/proof contract runs inside the existing docs-contracts-shift-left job without creating, renaming, demoting, or rewiring any repository proof identity; GitHub-enforced checks remain whatever the captured provider-policy snapshot reports."
    - "Phase 225 required/advisory identities and Phase 192 artifacts remain byte-for-byte named and semantically stable."
  artifacts:
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md"
      provides: "CI/host responsibility matrix and command-first diagnostics"
    - path: "scripts/ci/README.md"
      provides: "Discoverable maintainer entry point for the baseline and setup runbook"
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Workflow topology, proof taxonomy, ownership, and privacy contract"
    - path: ".github/workflows/ci.yml"
      provides: "Existing shift-left job invocation only; no job topology change"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/verify_ci_baseline_contract.sh"
      via: "one added step in docs-contracts-shift-left"
      pattern: "bash scripts/ci/verify_ci_baseline_contract.sh"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md"
      to: "scripts/ci/accrue_host_uat.sh"
      via: "host full-gate and database readiness diagnostics"
      pattern: "accrue_host_uat.sh"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md"
      to: "scripts/ci/accrue_host_verify_browser.sh"
      via: "fixture, port, server, and Playwright lifecycle diagnostics"
      pattern: "accrue_host_verify_browser.sh"
---

<objective>
Make setup ownership actionable and bind the new proof/baseline semantics into the existing shift-left contract surface without topology drift or a false branch-protection claim.

Purpose: Close OWN-01 and keep BASE-01/BASE-02 durable after the planning artifacts are first published while preserving the distinction between repository proof taxonomy and provider-enforced checks.
Output: Ownership runbook, maintainer discoverability, workflow-semantic contract, and existing-job CI invocation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
@scripts/ci/accrue_host_uat.sh
@scripts/ci/accrue_host_verify_browser.sh
@scripts/ci/verify_phase192_ci_contract.sh
@scripts/ci/README.md
@examples/accrue_host/README.md
@examples/accrue_host/package.json
@.github/workflows/ci.yml

<interfaces>
Existing ownership entry points must be referenced, not replaced:
- Repo/CI full host gate: `bash scripts/ci/accrue_host_uat.sh`
- Host-local full gate: `cd examples/accrue_host &amp;&amp; mix verify.full`
- Host-local JS/browser bootstrap: `npm ci` then `npm run e2e:install`
- Browser lifecycle implementation: `scripts/ci/accrue_host_verify_browser.sh`

The only workflow edit is an added `run: bash scripts/ci/verify_ci_baseline_contract.sh` step under the existing `docs-contracts-shift-left` job.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish CI-versus-host setup ownership and command-first diagnostics</name>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md, scripts/ci/README.md</files>
  <action>Create an ownership matrix covering Node runtime, npm lock installation, Playwright package, Chromium/system dependencies, Docker versus Postgres service provisioning, database create/migrate, deterministic fixture creation, port selection, Phoenix server readiness, Playwright reuse/cleanup, and failure artifacts. For every surface state CI owner/action, host owner/action, first diagnostic command, expected signal, and next safe action. Use exact current commands from `ci.yml`, `examples/accrue_host/README.md`, `package.json`, `accrue_host_uat.sh`, and `accrue_host_verify_browser.sh`; link those owners instead of creating another bootstrap script. Explain that CI owns Node 22 and its browser/system-dependency install while the host maintainer owns local Node/npm/Chromium/Postgres/ports, and the existing scripts own fixture/server cleanup. Add concise BASE/OWN triage entries to `scripts/ci/README.md`. Commit this task atomically as `docs(226-03): document CI and host setup ownership`.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; rg -n 'Node|npm ci|e2e:install|Playwright|Chromium|Postgres|fixture|port|server|accrue_host_uat.sh|accrue_host_verify_browser.sh' .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md</automated>
  </verify>
  <done>Every required setup/lifecycle surface has one explicit CI/host owner and a runnable diagnostic path, discoverable from the CI README.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the baseline contract in the existing shift-left topology</name>
  <files>scripts/ci/verify_ci_baseline_contract.sh, .github/workflows/ci.yml, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <behavior>
    - The contract passes for the current job IDs, needs graph, matrix support labels, artifact names, ownership commands, and four proof states.
    - Removing or renaming a required/advisory identity, Phase 192 artifact, ownership command, privacy field guard, or Phase 227 measured-selection field makes the fixture/self-test fail.
    - The workflow diff adds one step to docs-contracts-shift-left and changes no job key, name, needs edge, continue-on-error policy, event condition, or artifact declaration.
  </behavior>
  <action>Extend the fail-closed verifier using the Phase 192 `job_body` pattern. Assert the stable job IDs and critical chain (`release-gate` → `admin-drift-docs` → `host-integration` → `playwright-e2e` → `annotation-sweep`), the three required release cells and separate Sigra advisory label, the exact `continue-on-error` policy, the four proof-state definitions, Phase 192 report/evidence/generated-evidence artifact names, metadata-only privacy boundary, complete ownership command set, three-run eligibility, Phase 227 measured-selection gate, and the authoritative effective-rules/classic-protection snapshot fields. Contract that an empty effective-rules response plus classic-protection 404 renders `none-enforced`; never relabel repository YAML semantics as GitHub branch protection. Add only one invocation step inside the existing non-scheduled `docs-contracts-shift-left` job. Capture the before/after workflow job-key/name/needs/continue-on-error/artifact census in the contract self-test so topology changes fail. After all checks pass, update `226-VALIDATION.md` to mark Wave 0 complete and Nyquist compliant with the actual commands. Commit this task atomically as `ci(226-03): gate baseline semantics without topology drift`.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; git diff --check &amp;&amp; rg -n 'bash scripts/ci/verify_ci_baseline_contract.sh' .github/workflows/ci.yml</automated>
  </verify>
  <done>The contract runs through the existing shift-left identity, all Phase 225/192 proof and artifact semantics remain stable, provider-enforced checks are reported only from the authoritative snapshot, validation is current, and the task is committed independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer docs → local host environment | Commands act on host-owned Node, browser, database, fixtures, ports, and server processes. |
| Baseline contract → repository CI proof topology | A documentation/contract change must not silently alter which jobs or artifacts constitute repository release evidence or misstate provider enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-226-07 | Elevation of Privilege | ownership runbook | medium | mitigate | Use credential-free diagnostics, never instruct secret output, and keep authenticated API work in the read-only collector. |
| T-226-08 | Tampering | ci.yml proof topology | high | mitigate | Contract stable job IDs, needs, support labels, continue-on-error policy, artifact names, and a one-step-only workflow diff. |
| T-226-09 | Information Disclosure | diagnostic documentation | medium | mitigate | Point raw logs/traces/server output to retained Actions or local transient paths; do not copy them into baseline artifacts. |
| T-226-SC | Tampering | package supply chain | low | accept | No package is introduced; the new CI step executes a repository-owned Bash contract only. |
</threat_model>

<verification>
Run the Phase 226 self-test and current Phase 192 contract, confirm the ownership needles, and inspect the workflow diff to prove only one step was added under `docs-contracts-shift-left`.
</verification>

<success_criteria>
- Host and CI ownership is explicit for all requested setup/lifecycle surfaces.
- Diagnostics start with exact safe commands and existing repository owners.
- The baseline/proof contract executes in the existing shift-left job, while external required-check enforcement is reported solely from the provider-policy snapshot.
- Required/advisory job identities, dependency topology, and Phase 192 artifacts are unchanged.
- Validation records the actual green automated gates.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Durable privacy-safe comparable baseline, setup ownership, provider proof state | 01-03 | COVERED | Tracer, real cohort, and durable CI/runbook binding. |
| REQ | BASE-01 | Timing, queue, jobs/steps, reruns, cache, Docker/browser, provider, signature across comparable runs | 01-02 | COVERED | Exact anchor plus three-run aggregates and exclusions. |
| REQ | BASE-02 | Required/skipped/advisory evidence cannot be confused | 01-03 | COVERED | Closed four-state vocabulary and workflow-policy contract. |
| REQ | OWN-01 | Node/browser/Playwright ownership and diagnostics | 03 | COVERED | Includes database, fixture, port, and server lifecycle. |
| RESEARCH | — | Metadata-only gh/jq collector with fixture mode and fail-closed privacy checks | 01 | COVERED | No new dependency or raw evidence archive. |
| RESEARCH | — | Three current-shape first-attempt dispatches; retain exclusions | 02 | COVERED | Reruns are ineligible and failed candidates remain visible. |
| RESEARCH | — | Dedicated verified remote cohort branch; provider head SHA and anchor workflow/lock blob equality | 02 | COVERED | Run IDs are discovered by before/after set difference and bound to the immutable snapshot. |
| RESEARCH | — | Effective ruleset plus classic branch-protection APIs are authoritative for externally enforced checks | 01, 03 | COVERED | Empty/404 is recorded explicitly; YAML remains repository proof taxonomy, not inferred protection. |
| RESEARCH | — | Preserve run 31322443304's 39m40s wall / ~39m36s chain and seconds-scale queue | 01-02 | COVERED | Contrary result is an invariant, not rounded back to the roadmap estimate. |
| RESEARCH | — | Bind Phase 227 candidate to measured critical-path/setup evidence | 02-03 | COVERED | JSON/Markdown selection gate is contract-checked. |
| RESEARCH | — | Existing shift-left job and stable proof/artifact topology | 03 | COVERED | One added step; no identity or needs change. |
| CONTEXT | — | No CONTEXT.md decisions supplied | — | N/A | User constraints are reflected above; no D-XX decisions exist. |

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md` when done.
</output>

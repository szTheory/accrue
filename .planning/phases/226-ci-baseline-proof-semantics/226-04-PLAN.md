---
phase: 226-ci-baseline-proof-semantics
plan: 04
type: execute
wave: 4
depends_on:
  - 226-03
files_modified:
  - scripts/ci/ci_baseline_workflow_policy.json
  - scripts/ci/capture_ci_baseline.sh
  - scripts/ci/verify_ci_baseline_contract.sh
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
autonomous: true
gap_closure: true
requirements:
  - BASE-01
  - BASE-02
  - OWN-01
must_haves:
  truths:
    - "BASE-01 / D-01 / D-02: The canonical baseline contains exactly three eligible green workflow_dispatch attempt-one runs, and every eligible run has a non-estimated numeric critical queue measurement."
    - "BASE-01 / D-03: Every eligible run has a normalized privacy-safe root_failure_signature containing only a versioned signature ID, category, and manifest lane identities."
    - "BASE-02 / D-05 / D-07: Every observed lane resolves through a checked-in workflow-policy manifest, unknown lane identities fail closed, and aggregate release proof is recomputed only from eligible required proved lanes."
    - "D-01: Complete anchor and cohort provenance is mandatory, including exact equality of the workflow blob OID and all six named lockfile blob OIDs."
    - "D-04: Cache evidence distinguishes an explicit cache-action hit from a setup bypass and does not promote a skipped setup step to observed-hit."
    - "OWN-01 / D-09 / D-10: The already-verified ownership matrix, command-first README route, and existing host/browser owner scripts remain contract-protected without new setup behavior."
  artifacts:
    - path: "scripts/ci/ci_baseline_workflow_policy.json"
      provides: "Versioned mapping from known GitHub job display identities to repository policy, proof eligibility, and critical-chain role"
    - path: "scripts/ci/capture_ci_baseline.sh"
      provides: "Manifest-driven metadata collector that derives per-run queue and privacy-safe failure-signature facts"
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Fail-closed cohort, proof, aggregate, provenance, privacy, and negative-control validation"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Repaired canonical three-run evidence with measured queue/signature facts and complete provenance"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Human rendering of the repaired facts and derivation rules"
  key_links:
    - from: "scripts/ci/capture_ci_baseline.sh"
      to: "scripts/ci/ci_baseline_workflow_policy.json"
      via: "classify_job resolves every provider job name to one known manifest identity and rejects ambiguity or absence"
      pattern: "classify_job|policy_manifest"
    - from: "scripts/ci/verify_ci_baseline_contract.sh"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      via: "validate_input and validate_aggregates recompute eligible cohort, required proof, queue, signature, and provenance invariants"
      pattern: "validate_input|validate_aggregates"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      via: "Markdown renders canonical JSON facts without adding estimates or raw provider evidence"
      pattern: "critical_queue_seconds|root_failure_signature"
---

<objective>
Close the three Phase 226 verification failures by making the comparable cohort, required-proof aggregate, queue/signature evidence, and anchor/cohort provenance deterministic and fail-closed.

Purpose: Phase 227 must consume measured evidence that cannot remain green after required facts, required proof, or immutable provenance are removed or altered.
Output: A versioned lane-policy manifest, repaired collector and canonical baseline, and mutation-tested contract that preserves the already-verified OWN-01 ownership interface.
</objective>

## Artifacts this phase produces

| Path | New or changed symbols / fields | Contract produced |
| --- | --- | --- |
| `scripts/ci/ci_baseline_workflow_policy.json` | `schema_version`, `workflow`, `lanes[]`, lane `identity`, `match`, `policy`, `required_for_release_proof`, `critical_chain_root` | Checked-in source of truth for known lane classification; zero-match and multi-match names are invalid. |
| `scripts/ci/capture_ci_baseline.sh` | `policy_manifest`, `load_policy_manifest`, `classify_job`, `derive_critical_queue`, `derive_root_failure_signature` | Manifest-driven capture of policy/proof state, measured queue, and normalized failure identity. |
| `scripts/ci/verify_ci_baseline_contract.sh` | `validate_policy_manifest`, expanded `validate_input`, new `validate_aggregates`, expanded self-test mutation set | Independent recomputation and fail-closed negative controls for all three verification gaps. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` | `cohort.anchor`, complete `cohort.remote_snapshot`, per-run `critical_queue_seconds`, `critical_queue_omission_reason`, `root_failure_signature`, `aggregates.queue_seconds`, derived `aggregates.proof` | Canonical, privacy-safe, three-run measurement and immutable provenance record. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` | Queue/signature cohort table, provenance equality table, aggregate-proof derivation explanation | Reviewable rendering of the JSON authority with no raw logs, traces, payloads, or estimates. |

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-01-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-03-SUMMARY.md
@scripts/ci/capture_ci_baseline.sh
@scripts/ci/verify_ci_baseline_contract.sh
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@.github/workflows/ci.yml
</context>

<assumptions>
- The spec-less BASE-01 and BASE-02 probes are resolved only through the explicit failures in `226-VERIFICATION.md`; they add no predicates beyond the locked D-01 through D-08 evidence contract.
- The OWN-01 adjacency, empty-input, and ordering probes do not describe the ownership matrix or command-routing interface. OWN-01 is preserved by regression assertions against the already-verified artifacts rather than by inventing collection semantics.
- The three recorded provider runs and their stored allowlisted timestamps are sufficient to repair queue/signature fields without dispatching a replacement run; if a required timestamp is absent, D-02 requires the executor to stop rather than estimate it.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make collection manifest-driven and emit queue/signature evidence</name>
  <reversibility rating="costly">D-05 and D-07 define a versioned interpretation boundary consumed by the checked-in baseline and Phase 227.</reversibility>
  <files>scripts/ci/ci_baseline_workflow_policy.json, scripts/ci/capture_ci_baseline.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` decisions D-02 through D-08.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` failed Truths 7 and 8.
    - `scripts/ci/capture_ci_baseline.sh` functions `policy_for_job` and `build_run`, including the current skipped-step cache inference.
    - `.github/workflows/ci.yml` job names, release matrix `support`, event conditions, and critical-chain roots.
  </read_first>
  <behavior>
    - Per D-07, each observed provider job name maps to exactly one versioned manifest lane; unknown or ambiguous identities terminate capture non-zero.
    - Per D-02, an eligible run receives a numeric `critical_queue_seconds` derived only from provider run/job timestamps and a null `critical_queue_omission_reason`; omitted provider timestamps produce null plus an allowed field-specific reason and make the run ineligible.
    - Per D-03, each run receives `root_failure_signature.id`, `.category`, and `.affected_jobs`; affected jobs are sorted manifest identities and the ID is derived deterministically from that allowlisted normalized tuple, with no log, trace, payload, or excerpt input.
    - Per D-04, `observed-hit` requires an explicit cache-action output. A skipped setup step is recorded as `inferred-setup-bypass`, while insufficient evidence remains `unknown`.
  </behavior>
  <action>Create `scripts/ci/ci_baseline_workflow_policy.json` as the checked-in versioned workflow snapshot named by D-07. Enumerate every current job display identity or bounded matrix-name matcher from `.github/workflows/ci.yml`; record its stable manifest identity, declared `required`/`advisory`/`conditional` policy, whether it is required for aggregate release proof, and whether it is a root used for critical queue measurement. Validate nonempty unique identities and reject overlapping matchers. In `capture_ci_baseline.sh`, replace `policy_for_job` and inline name heuristics with `policy_manifest`, `load_policy_manifest`, and `classify_job`; never default an unknown name to required or any proof state. Add `derive_critical_queue` to calculate the latest start of manifest-marked required critical-chain roots minus `run.created_at`, using provider timestamps only. Emit `critical_queue_omission_reason` as null when measured; when a timestamp is provider-omitted use a closed field-specific reason and set eligibility false per D-02. Add `derive_root_failure_signature` using only sorted manifest identities and normalized conclusions for failed, timed-out, or cancelled jobs: emit a versioned deterministic ID plus category and affected job identities, and emit an explicit no-failure category when no such lane exists. Do not consume job logs, step messages, artifact bodies, environment values, URLs with query strings, or test payloads per D-03/D-11/D-12. Correct cache derivation so a skipped create/setup step is not treated as an explicit hit per D-04. Keep read-only API calls, fixture mode, output atomics, stable workflow topology, and provider-snapshot semantics unchanged per D-06/D-08.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; tmp_file="$(mktemp)" &amp;&amp; trap 'rm -f "$tmp_file"' EXIT &amp;&amp; bash scripts/ci/capture_ci_baseline.sh --run-id 31322443304 --output "$tmp_file" &amp;&amp; jq -e '.runs[0] | (.run.critical_queue_seconds | type == "number") and .run.critical_queue_omission_reason == null and (.root_failure_signature.id | type == "string" and length &gt; 0) and (.root_failure_signature.category | type == "string" and length &gt; 0) and (.root_failure_signature.affected_jobs | type == "array")' "$tmp_file" &gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The manifest classifies all current workflow jobs exactly once and the fixture self-test rejects a synthetic unknown job identity.
    - Live read-only capture of run 31322443304 produces measured queue and normalized root-signature fields without raw evidence.
    - Cache state cannot be `observed-hit` solely because a setup/create step was skipped.
  </acceptance_criteria>
  <done>The collector emits D-02/D-03/D-04/D-07-compliant facts from one known policy manifest and fails closed on provider or workflow ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the canonical cohort, provenance, and derived aggregates</name>
  <reversibility rating="costly">D-01 fixes the cohort and blob-equality policy that Phase 227 cites as its measurement basis.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` failed Truths 7, 8, and 10.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` current `cohort`, `runs`, and `aggregates` objects.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md` immutable branch and eligible run IDs.
    - `scripts/ci/ci_baseline_workflow_policy.json` output of Task 1.
  </read_first>
  <behavior>
    - Exactly runs 31322443304, 31332551817, and 31344524124 form the D-01 eligible cohort; every one has measured queue and root-signature facts.
    - `cohort.anchor` and `cohort.remote_snapshot` each carry complete ref/SHA provenance, one `workflow_blob_oid`, and the same six exact lockfile keys and values.
    - Queue minimum/median/maximum and per-run rows are mechanically derived from the three eligible run measurements.
    - Aggregate proof names the eligible run IDs and required manifest lane identities, records required-proved lanes per run, and is true only when every manifest-required proof lane is `proved` in every eligible run per D-05.
  </behavior>
  <action>Regenerate the three canonical run records through the repaired collector where live read-only provider access is available; otherwise derive only from the already-stored allowlisted timestamps and halt if any required timestamp is absent. Add numeric `run.critical_queue_seconds`, null `critical_queue_omission_reason`, and the normalized `root_failure_signature` object to every eligible run. Add a complete `cohort.anchor` object for run 31322443304 with its provider ref/SHA and git-derived workflow plus six lockfile blob OIDs. Complete `cohort.remote_snapshot` with immutable branch/ref, SHA/remote SHA, workflow blob, and the identical lockfile map. The six required keys are `accrue/mix.lock`, `accrue_admin/mix.lock`, `accrue_admin/package-lock.json`, `examples/accrue_host/mix.lock`, `examples/accrue_host/package-lock.json`, and `examples/accrue_host/assets/package-lock.json`; do not accept extras as substitutes for a missing key. Rebuild queue and proof aggregates from eligible runs rather than copying booleans: include per-run measured queue rows and min/median/max; include eligible IDs, manifest-required lane identities, per-run required-proved identities/counts, and the derived all-required-lanes-proved result. Reclassify cache facts under D-04. Update `226-CI-BASELINE.md` from JSON authority with cohort queue/signature, provenance equality, and proof-derivation tables, keeping provider enforcement timestamp-scoped per D-06 and raw evidence Actions-owned per D-11.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; jq -e '([.runs[] | select(.run.eligible == true)] | length) == 3 and all(.runs[] | select(.run.eligible == true); (.run.critical_queue_seconds | type == "number") and .run.critical_queue_omission_reason == null and (.root_failure_signature.id | type == "string" and length &gt; 0)) and (.cohort.anchor.workflow_blob_oid == .cohort.remote_snapshot.workflow_blob_oid) and (.cohort.anchor.lockfile_blob_oids == .cohort.remote_snapshot.lockfile_blob_oids) and (.cohort.anchor.lockfile_blob_oids | keys | length) == 6 and (.aggregates.queue_seconds.per_run | length) == 3 and .aggregates.proof.all_required_lanes_proved == true' .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - All three eligible runs carry measured queue and privacy-safe root-signature facts.
    - Anchor and cohort provenance is complete and exactly equal for the workflow and all six named lockfiles.
    - Queue and proof aggregates can be reproduced from the eligible run records and policy manifest.
    - Markdown presents the same measured facts and does not add raw evidence or estimates.
  </acceptance_criteria>
  <done>The canonical JSON and Markdown close the BASE-01 evidence gap and supply complete D-01 provenance plus D-05-derived proof inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce fail-closed cohort, proof, and provenance mutations</name>
  <files>scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `scripts/ci/verify_ci_baseline_contract.sh` existing `validate_input`, `validate_repository_contract`, and `--self-test` mutations.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` behavioral spot-checks `ONE_RUN_ACCEPTED`, `NO_PROVED_ACCEPTED`, and `NO_QUEUE_ACCEPTED`.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` repaired Task 2 schema.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md` and `scripts/ci/README.md` for preserved OWN-01 assertions.
  </read_first>
  <behavior>
    - A one- or two-run cohort, an eligible non-dispatch/non-green/non-attempt-one run, missing measured queue, or missing/malformed root signature fails validation.
    - Changing every required proved lane to a non-proof state, removing a manifest-required lane, introducing an unknown lane, or changing any derived proof/queue aggregate fails validation.
    - Missing anchor/cohort ref/SHA provenance, workflow blob inequality, or deletion/change of any one of the six required lockfile entries fails validation.
    - Existing OWN-01 ownership commands and repository proof-topology assertions remain green and fail when mutated.
  </behavior>
  <action>Split the verifier responsibilities into `validate_policy_manifest`, expanded `validate_input`, and `validate_aggregates`. Require exactly three eligible D-01 runs and reject additional eligible records, while allowing explicitly excluded inspected runs only with a nonempty exclusion reason. For each eligible run require workflow_dispatch, attempt 1, completed success, numeric nonnegative measured queue, null omission reason, a closed normalized root-signature object, and complete manifest resolution for every lane. Recompute the expected required-proof set from `ci_baseline_workflow_policy.json`; compare exact eligible IDs, required lane identities, per-run proved identities/counts, and final aggregate truth rather than trusting stored booleans. Recompute queue per-run rows and min/median/max. Require complete anchor and remote-snapshot ref/SHA fields, exact 40-hex blob IDs, exact workflow equality, and exact equality over precisely the six D-01 lockfile paths. Extend `--self-test` with isolated temporary mutations for one-run cohort, null queue, deleted signature, all required proved lanes changed to skipped, removed required lane, unknown lane identity, inconsistent queue aggregate, inconsistent proof aggregate, missing anchor provenance, workflow blob mismatch, and a loop that separately deletes then changes each required lockfile OID. Every mutation must be observed to exit non-zero. Preserve the existing privacy, provider snapshot, CI topology, Phase 192 artifact, README, and ownership-matrix mutations so BASE-01/BASE-02 closure cannot regress OWN-01 or D-06/D-08/D-09/D-10/D-11/D-12.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Each verification-report bypass has a named negative mutation that exits non-zero.
    - Aggregate queue and proof values are independently recomputed, not accepted because fields are present.
    - Workflow and every lockfile equality invariant has an individual negative control.
    - The existing OWN-01 ownership route and stable CI/Phase 192 proof topology remain protected.
  </acceptance_criteria>
  <done>The contract fails closed for incomplete evidence, false proof, aggregate drift, and provenance drift while retaining all pre-existing verified safeguards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| GitHub Actions metadata → collector | Provider-controlled names, timestamps, conclusions, and refs cross into repository tooling. |
| Workflow-policy manifest → proof derivation | A checked-in interpretation artifact determines which observed lanes can satisfy release proof. |
| Canonical JSON → Phase 227 | Downstream optimization selection trusts cohort eligibility, measured aggregates, and immutable provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-226-10 | Tampering | cohort/proof aggregates | high | mitigate | Independently recompute exact eligible IDs, queue aggregates, required lane sets, per-run proved identities, and final truth; block on any mismatch. |
| T-226-11 | Information Disclosure | root failure signature | high | mitigate | Derive the signature only from versioned category, normalized manifest identities, and conclusions; retain raw logs/traces/payloads solely in Actions artifacts. |
| T-226-12 | Spoofing | provider job display identity | medium | mitigate | Resolve each name through a non-overlapping versioned manifest and reject unknown or ambiguous identities. |
| T-226-13 | Repudiation | anchor/cohort provenance | high | mitigate | Require complete ref/SHA fields and exact workflow plus six-lockfile blob equality, each backed by negative mutations. |
| T-226-14 | Denial of Service | malformed or omitted provider timestamps | low | mitigate | Validate timestamp arithmetic, preserve explicit omission reasons for diagnostics, and reject omitted measurements from the eligible cohort rather than estimating. |
| T-226-SC | Tampering | package supply chain | low | accept | The plan installs no npm, pip, or cargo package and uses existing Bash, jq, git, and gh tooling. |
</threat_model>

<verification>
Run the contract self-test, canonical validation, and Phase 192 topology/artifact contract. Confirm the self-test reports rejection of each cohort, queue, signature, proof, aggregate, workflow-blob, and per-lockfile mutation, then run `git diff --check`.
</verification>

<success_criteria>
- BASE-01 is closed by exactly three eligible runs with measured queue and privacy-safe root failure facts.
- BASE-02 is closed because only manifest-known, eligible, required successful lanes can satisfy a recomputed proof aggregate.
- Complete anchor/cohort provenance and exact workflow/six-lockfile equality are mandatory and mutation-tested.
- OWN-01 remains satisfied through preserved command-first ownership artifacts and regression assertions, with no unrelated ownership work.
- No CI identity, matrix breadth, dependency edge, cache topology, branch protection, package dependency, or product/UI behavior changes.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Durable privacy-safe comparable CI account distinguishing critical path, setup ownership, and provider proof | 226-04 | COVERED | Repairs missing evidence and fail-closed semantics while preserving ownership. |
| REQ | BASE-01 | Wall, queue, job/step, rerun, cache, setup, provider, and root-signature facts across comparable runs | 226-04 | COVERED | Queue/signature/provenance gaps are captured, rendered, and mutation-tested. |
| REQ | BASE-02 | Required, skipped, and advisory facts cannot be confused with release proof | 226-04 | COVERED | Manifest classification and exact aggregate recomputation fail closed. |
| REQ | OWN-01 | Host/CI Node, browser, and Playwright ownership/diagnostics | 226-04 | COVERED | Existing verified artifacts are explicitly preserved and regression-tested. |
| RESEARCH | — | Metadata-only gh/jq collector and raw-evidence boundary | 226-04 | COVERED | No logs, archives, payloads, environment values, or query URLs enter evidence. |
| RESEARCH | — | Comparable attempt-one three-run cohort with retained exclusions | 226-04 | COVERED | Exact cohort count and eligibility are enforced. |
| RESEARCH | — | Workflow plus six-lockfile blob equality | 226-04 | COVERED | Complete provenance and per-key negative controls are required. |
| RESEARCH | — | Provider snapshot is authoritative only at capture time | 226-04 | COVERED | Existing snapshot checks and D-06 wording remain unchanged. |
| CONTEXT | D-01 | Exactly three qualifying green dispatch runs, attempt one, with full provenance | 226-04 | COVERED | Tasks 2-3 repair and enforce the cohort. |
| CONTEXT | D-02 | Queue/signature/cache/setup facts required; provider omissions typed, never estimated | 226-04 | COVERED | Tasks 1-3 capture and reject missing eligible measurements. |
| CONTEXT | D-03 | Normalized privacy-safe root signature only | 226-04 | COVERED | Task 1 derives from manifest identities, never raw evidence. |
| CONTEXT | D-04 | Explicit cache output required for observed-hit | 226-04 | COVERED | Tasks 1-2 add inferred setup bypass and correct existing records. |
| CONTEXT | D-05 | Policy, conclusion, and proof state remain independent; aggregate is fail-closed | 226-04 | COVERED | Tasks 1 and 3 manifest-map and recompute proof. |
| CONTEXT | D-06 | Provider enforcement is a timestamped snapshot, not YAML inference | 226-04 | COVERED | Task 3 preserves provider-snapshot assertions. |
| CONTEXT | D-07 | Versioned checked-in workflow policy; unknown identities reject | 226-04 | COVERED | Task 1 creates the manifest and removes heuristics. |
| CONTEXT | D-08 | Only authenticated 404 is not-found; other provider failures block | 226-04 | COVERED | Task 1 preserves and Task 3 regression-tests existing provider error semantics. |
| CONTEXT | D-09 | `scripts/ci/README.md` remains the single triage home | 226-04 | COVERED | Task 3 preserves its existing contract; no competing guide is added. |
| CONTEXT | D-10 | Ownership matrix remains the maintainer interface | 226-04 | COVERED | Task 3 preserves exact commands and safe diagnostics. |
| CONTEXT | D-11 | Precise proof vocabulary and raw evidence stays in Actions | 226-04 | COVERED | Tasks 1-3 preserve vocabulary and privacy controls. |
| CONTEXT | D-12 | Repository-local metadata adapter; no application/product scope | 226-04 | COVERED | Only Bash/JSON/Markdown contract artifacts change. |

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-04-SUMMARY.md` when done.
</output>

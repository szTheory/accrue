---
phase: 226-ci-baseline-proof-semantics
plan: 06
type: execute
wave: 6
depends_on:
  - 226-05
files_modified:
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - BASE-01
  - BASE-02
  - OWN-01
must_haves:
  truths:
    - "BASE-01 / D-01 / D-02: The canonical JSON is regenerated from the corrected collector facts for exactly runs 31322443304, 31332551817, and 31344524124, with seconds-scale initial runner queue and separately measured staged-chain duration."
    - "BASE-01 / D-03 / D-11: Canonical JSON and Markdown contain only exact-schema metadata and conclusion-bearing normalized root signatures; raw logs, traces, payloads, secret values, screenshots, server output, and artifact bodies remain outside git."
    - "BASE-01 / D-06 / D-08: The provider section is regenerated only from successful effective-rules data and confirmed classic 404 semantics, remains timestamp-scoped, and cannot imply none-enforced from an error."
    - "BASE-02 / D-05: Canonical aggregate proof is recomputed from eligible required successful lanes, and every ineligible diagnostic record has zero proved lanes."
    - "BASE-01 / D-01: Workflow and all six lockfile blob identities, immutable ref/SHA provenance, cache/setup topology, rerun/exclusion facts, and complete job/artifact counts remain tied to the established comparable cohort."
    - "OWN-01 / D-09 / D-10 / D-12: Setup ownership artifacts, existing CI topology/required-check identities/matrix/cache topology, and Phase 192 artifact contracts remain unchanged and green."
  artifacts:
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Corrected authoritative three-run metadata cohort and derived aggregates"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Human rendering of corrected queue, staged-chain, proof, provider, cache/setup, and provenance facts"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Current executable validation commands and adversarial coverage map"
  key_links:
    - from: "scripts/ci/capture_ci_baseline.sh"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      via: "Read-only recollection of the fixed run IDs using the corrected record contract"
      pattern: "31322443304|31332551817|31344524124"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      via: "Markdown renders values from JSON authority without independent estimates"
      pattern: "runner_queue_seconds|staged_critical_chain_seconds|lane_conclusions"
    - from: "scripts/ci/verify_ci_baseline_contract.sh"
      to: ".github/workflows/ci.yml and Phase 192 artifacts"
      via: "Unchanged repository and Phase 192 regression contracts"
      pattern: "verify_phase192_ci_contract.sh"
---

<objective>
Regenerate and certify the canonical three-run JSON/Markdown solely from corrected metadata facts while preserving the established comparable cohort, setup ownership, stable CI topology, provider semantics, and Phase 192 evidence.

Purpose: Replace the misleading durable queue/proof/provider facts only after the repaired collector and exact contract are executable.
Output: Corrected canonical baseline JSON, matching Markdown rendering, and an updated validation map that proves all remaining gaps and negative controls are closed.
</objective>

## Artifacts this phase produces

| Path | New or changed symbols / fields | Contract produced |
| --- | --- | --- |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` | Corrected per-run queue/chain/proof/pagination/signature fields and eligible-only aggregates | Authoritative comparable metadata for Phase 227. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` | Corrected queue-versus-chain tables, provider proof wording, normalized signature summaries | Reviewable rendering sourced only from canonical JSON. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` | Current quick/full commands and gap-closure negative-control map | Durable execution recipe for record, cohort, privacy, provider, pagination, proof, timing, and Phase 192 gates. |

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-04-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-05-SUMMARY.md
@scripts/ci/ci_baseline_workflow_policy.json
@scripts/ci/capture_ci_baseline.sh
@scripts/ci/verify_ci_baseline_contract.sh
@scripts/ci/verify_phase192_ci_contract.sh
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md
@scripts/ci/README.md
@.github/workflows/ci.yml
</context>

<assumptions>
- The three fixed run IDs and immutable branch provenance remain the D-01 cohort; this plan performs read-only recollection and does not dispatch, rerun, delete, or replace provider runs.
- If read-only provider access is unavailable or any current response cannot satisfy the corrected completeness/status contract, execution stops rather than inventing or estimating facts.
- No one-way choice is introduced: canonical evidence can be regenerated from immutable provider metadata under the versioned contract, so the costly schema publication requires no decision checkpoint.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Recollect one fixed run and replace the canonical cohort from corrected facts</name>
  <precondition>`gh auth status` succeeds for read-only access to `szTheory/accrue`, and Plans 226-05 collector self-tests pass; no permission to dispatch workflows or mutate provider configuration is required.</precondition>
  <reversibility rating="costly">Phase 227 uses this published evidence as its selection input, so reverting requires regenerating and re-reviewing the coordinated JSON/Markdown pair.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` all decisions D-01 through D-12, especially D-02/D-03/D-05/D-06/D-08/D-12.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` five remaining gaps and hollow-semantics findings.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-02-SUMMARY.md` fixed cohort IDs and immutable branch.
    - `scripts/ci/capture_ci_baseline.sh` and `scripts/ci/verify_ci_baseline_contract.sh` completed Plan 05 interfaces.
    - Current canonical JSON/Markdown to preserve provenance, cache/setup topology, required/advisory identity, exclusions, and Phase 227 selection fields that remain factually valid.
  </read_first>
  <behavior>
    - A temporary recollection of run 31322443304 validates as a collector record before any canonical file changes.
    - Read-only recollection of the three fixed run IDs produces complete jobs/artifacts, confirmed provider response states, eligibility-qualified proofs, corrected runner queue, separate staged-chain duration, and normalized lane/conclusion signatures.
    - Canonical assembly preserves exact D-01 ref/SHA, workflow blob, six lockfile blobs, cohort IDs, required/advisory policy identities, cache/setup topology, rerun/exclusion facts, and Phase 227 selection gate.
    - Aggregates are regenerated only from corrected eligible records; no stored queue, proof boolean, signature ID, or provider absence fact is copied without revalidation.
    - Markdown reports seconds-scale initial queue separately from staged critical-chain duration and identifies JSON as authority.
  </behavior>
  <action>Retain the mapped Phase 187 analog convention from `.planning/milestones/v1.53-phases/187-audit-baseline/baseline.cells.json` and `.planning/milestones/v1.53-phases/187-audit-baseline/187-BASELINE.md`: the structured JSON is the canonical authority, while Markdown is derived rendering that must be regenerated from JSON whenever they differ and must not introduce independent facts or estimates. Begin with the production tracer: collect run 31322443304 to a temporary file and require the record-mode `--input` contract to pass. Then collect exactly 31322443304, 31332551817, and 31344524124 through authenticated read-only GETs; do not dispatch or rerun anything. Require each record to satisfy complete pagination, known manifest identity, D-01 event/attempt/conclusion eligibility, confirmed provider status handling, and exact recursive privacy schema. Assemble the canonical document from those corrected records plus the already-verified immutable cohort provenance and Phase 227 selection contract. Recompute runner queue, staged-chain, wall/setup/cache, root signature, job/artifact completeness, required proof, and min/median/max/per-run aggregates from the records and manifest; preserve an inspected ineligible run only with explicit exclusion and zero proved lanes. Regenerate provider snapshot wording only from successful effective-rules data and confirmed classic 404 semantics, including capture timestamp and no claim about current enforcement after that instant per D-06. Render `226-CI-BASELINE.md` directly from the corrected JSON facts: distinguish seconds-scale initial runner queue from the roughly staged release→host→Playwright chain, list normalized signature categories/pairs without raw evidence, show declared policy/conclusion/proof separately, and retain setup/cache/provenance/selection tables. Preserve D-09/D-10 ownership links. Never add logs, traces, payloads, environment values, artifact bodies, query-bearing URLs, product/UI content, or estimates.</action>
  <verify>
    <automated>tmp_file="$(mktemp)" &amp;&amp; trap 'rm -f "$tmp_file"' EXIT &amp;&amp; bash scripts/ci/capture_ci_baseline.sh --run-id 31322443304 --output "$tmp_file" &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh --input "$tmp_file" &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - The one-run live record passes record-mode `--input` before canonical replacement.
    - Canonical JSON contains exactly the established three eligible run IDs and preserves exact workflow/six-lockfile provenance.
    - Every eligible run has complete counts, seconds-scale initial runner queue, separate staged-chain duration, eligible-only proof, and normalized conclusion pairs/signature ID.
    - Provider none-enforced appears only with successful empty effective rules plus confirmed classic 404 and a capture timestamp.
    - Markdown matches JSON facts and contains no independent estimates or raw evidence.
  </acceptance_criteria>
  <done>The canonical durable baseline is rebuilt solely from corrected read-only metadata facts and renders the real queue/critical-chain/proof/provider state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Certify all gap negative controls and preserve CI/ownership/Phase 192 contracts</name>
  <reversibility rating="reversible">The validation map documents executable checks and can be updated locally as tests evolve without migrating evidence or changing public behavior.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` existing quick/full commands and per-task map.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` exact remaining gap/spot-check list.
    - `scripts/ci/verify_ci_baseline_contract.sh` completed named self-test cases from Plan 05.
    - `scripts/ci/verify_phase192_ci_contract.sh` complete stable artifact/topology contract.
    - `.github/workflows/ci.yml` existing single shift-left invocation and unchanged job topology.
  </read_first>
  <behavior>
    - The validation document's quick command runs deterministic self-tests; its full command recollects one fixed run to a temporary path, validates record mode, validates the canonical cohort, and runs Phase 192.
    - The coverage map names negative controls for exact schema/unknown secret-like key, every provider failure class, pagination incompleteness, collector/canonical mode separation, ineligible proved lanes, queue/chain conflation, and signature-pair mutation.
    - Canonical validation, self-tests, live record validation, Phase 192 contract, and diff hygiene all pass in the final tree.
    - `.github/workflows/ci.yml`, `scripts/ci/README.md`, `226-SETUP-OWNERSHIP.md`, and Phase 192 artifacts are not modified by this plan.
  </behavior>
  <action>Update `226-VALIDATION.md` to describe the corrected interfaces and executed gap-closure coverage. Set the quick command to the deterministic contract self-test. Set the full command to create/trap a temporary output, read-only collect run 31322443304, validate that collector record through `--input`, validate the canonical cohort, and execute `bash scripts/ci/verify_phase192_ci_contract.sh`. Add per-task rows for Plans 05 and 06 and enumerate the named adversarial fixtures/mutations for all five verification gaps. Explicitly retain the existing single `docs-contracts-shift-left` invocation and state that the plan does not modify workflow topology, required-check identity, release matrix, cache topology, setup ownership docs, or Phase 192 artifact identities. Execute the full suite and `git diff --check`; inspect `git diff -- .github/workflows/ci.yml scripts/ci/README.md .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md scripts/ci/verify_phase192_ci_contract.sh` and require it to be empty. Do not mark validation complete unless every high-severity threat mitigation and every named negative control is observed green.</action>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; git diff --check &amp;&amp; test -z "$(git diff -- .github/workflows/ci.yml scripts/ci/README.md .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md scripts/ci/verify_phase192_ci_contract.sh)"</automated>
  </verify>
  <acceptance_criteria>
    - Validation maps every remaining verification gap to at least one named failing adversarial case and a final positive gate.
    - Record-mode, canonical-mode, self-test, and Phase 192 commands are copyable and executable.
    - High-severity privacy, provider, proof, and timing threats have green automated mitigations.
    - Workflow topology, ownership docs, required identities, matrix/cache topology, and Phase 192 contract/artifacts have no diff.
  </acceptance_criteria>
  <done>The corrected baseline is fully certified without weakening existing OWN-01, CI topology, or Phase 192 evidence.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 posture and block threshold

Security enforcement is OWASP ASVS Level 1. Any unresolved high or critical threat blocks publication; evidence regeneration stops on missing provider access or incomplete/ambiguous facts.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Immutable provider run metadata → canonical JSON | Read-only external facts cross into a checked-in durable evidence artifact. |
| Canonical JSON → Markdown/Phase 227 | Human and downstream optimization decisions consume the published interpretation. |
| Existing CI/ownership/Phase 192 contracts → gap closure | Correcting evidence must not weaken unrelated stable proof or setup ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-226-21 | Information Disclosure | canonical JSON/Markdown | high | mitigate | Regenerate only from exact-schema collector records; self-test unknown keys and secret-like content; render no raw evidence. |
| T-226-22 | Tampering | canonical timing/proof aggregates | high | mitigate | Independently recompute queue, chain, signature, proof, and aggregates from fixed eligible records and manifest. |
| T-226-23 | Spoofing | provider enforcement snapshot | high | mitigate | Publish none-enforced only from successful empty rules plus confirmed classic 404 with timestamp. |
| T-226-24 | Repudiation | cohort provenance | high | mitigate | Preserve immutable run IDs/ref/SHA and exact workflow/six-lockfile blob equality under contract. |
| T-226-25 | Elevation of Privilege | live provider operation | medium | mitigate | Permit authenticated GET-only recollection; forbid dispatch, rerun, branch, rules, protection, and artifact-download operations. |
| T-226-26 | Tampering | stable CI/Phase 192 evidence | high | mitigate | Require Phase 192 verifier green and an empty diff for workflow, ownership, and Phase 192 contract files. |
| T-226-SC | Tampering | package supply chain | low | accept | No package-manager install or new dependency is introduced. |
</threat_model>

<verification>
Run the Plan 05 adversarial self-test, validate the corrected canonical baseline, perform the one-run read-only collector-to-`--input` tracer, execute `bash scripts/ci/verify_phase192_ci_contract.sh`, require no diff in stable workflow/ownership/Phase 192 files, and run `git diff --check`.
</verification>

<success_criteria>
- The checked-in JSON/Markdown pair is regenerated only from corrected facts for the established three-run cohort.
- Initial runner queue, staged critical-chain duration, provider state, proof eligibility, pagination completeness, cache/setup facts, and normalized signatures are accurately distinct.
- Every remaining verification gap has a deterministic adversarial negative control and a passing final gate.
- BASE-01 and BASE-02 are closeable without regressing OWN-01.
- Existing CI topology, required-check identities, matrix/cache topology, setup ownership, and Phase 192 artifacts remain intact.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Durable privacy-safe comparable account of critical path, setup ownership, and provider proof | 226-06 | COVERED | Publishes corrected authoritative facts and review rendering. |
| REQ | BASE-01 | Durable complete comparable baseline | 226-06 | COVERED | Regenerates JSON/Markdown from corrected three-run metadata. |
| REQ | BASE-02 | Required/skipped/advisory proof distinction | 226-06 | COVERED | Recomputes eligible-only lane and aggregate proof. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 226-06 | COVERED | Preserves ownership artifacts and exact commands with no diff. |
| RESEARCH | — | Three current-shape comparable runs and measured critical path | 226-06 | COVERED | Fixed cohort is recollected without dispatch/rerun. |
| RESEARCH | — | JSON authority with concise Markdown rendering | 226-06 | COVERED | Markdown is regenerated only from JSON facts. |
| RESEARCH | — | Phase 192 and stable topology preservation | 226-06 | COVERED | Phase 192 verifier and empty-diff gate are mandatory. |
| CONTEXT | D-01 | Exactly three qualifying attempt-one runs with provenance | 226-06 | COVERED | Preserves fixed IDs and exact blob identities. |
| CONTEXT | D-02 | Complete facts, typed omissions, no estimates | 226-06 | COVERED | Corrected queue/chain and complete pages are canonicalized. |
| CONTEXT | D-03 | Normalized privacy-safe signature | 226-06 | COVERED | Conclusion pairs and IDs are rendered without raw evidence. |
| CONTEXT | D-04 | Conservative cache facts | 226-06 | COVERED | Existing cache topology and corrected cache evidence are preserved. |
| CONTEXT | D-05 | Independent policy/conclusion/proof and fail-closed aggregate | 226-06 | COVERED | Eligible-only proof is recomputed from records/manifest. |
| CONTEXT | D-06 | Timestamped provider snapshot only | 226-06 | COVERED | Canonical wording is capture-time-scoped. |
| CONTEXT | D-07 | Versioned workflow-policy manifest | 226-06 | COVERED | Canonical jobs retain exact known identities. |
| CONTEXT | D-08 | Confirmed 404 only | 226-06 | COVERED | Canonical provider state comes through corrected status handling. |
| CONTEXT | D-09 | Single command-first triage home | 226-06 | COVERED | README is preserved unchanged. |
| CONTEXT | D-10 | Ownership matrix interface | 226-06 | COVERED | Ownership artifact is preserved unchanged. |
| CONTEXT | D-11 | Precise vocabulary and raw evidence remains in Actions | 226-06 | COVERED | JSON/Markdown stay metadata-only and copyable. |
| CONTEXT | D-12 | Bounded safe repository-local scope | 226-06 | COVERED | GET-only recollection; no topology/provider/product changes. |

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-06-SUMMARY.md` when done.
</output>

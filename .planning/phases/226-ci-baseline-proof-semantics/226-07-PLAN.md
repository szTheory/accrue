---
phase: 226-ci-baseline-proof-semantics
plan: "07"
type: execute
wave: 7
depends_on:
  - 226-06
files_modified:
  - scripts/ci/verify_ci_baseline_contract.sh
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - BASE-01
must_haves:
  truths:
    - "BASE-01 / D-02 / D-03 / D-11: The canonical three-run document is recursively validated against exact object-key, array-element, scalar-type, nullable-field, enum, and privacy rules; unknown or type-confused content cannot cross into durable evidence."
    - "BASE-01 / D-01: The accepted canonical document still contains exactly runs 31322443304, 31332551817, and 31344524124 with their existing workflow/six-lockfile provenance, queue, staged-chain, proof, provider, cache/setup, and normalized-signature facts unchanged."
    - "D-05 / D-06 / D-07 / D-08: Exact-schema tightening preserves the independent policy/conclusion/proof vocabulary, manifest-owned lane identities, and timestamp-scoped provider enforcement semantics."
    - "D-04 / D-09 / D-10 / D-12: Cache semantics, the command-first ownership interface, safe local scope, stable CI topology, and Phase 192 artifacts remain regression-protected."
  artifacts:
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Exact recursive canonical schema/privacy validator and fail-first canonical mutation suite"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Unmodified canonical three-run document proven to satisfy the exact schema"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executable canonical privacy mutation and regression coverage map"
  key_links:
    - from: "scripts/ci/verify_ci_baseline_contract.sh"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      via: "validate_input dispatches ci-baseline-canonical to validate_canonical_cohort, which applies the exact recursive schema before semantic aggregates"
      pattern: "validate_input|validate_canonical_cohort|exact"
    - from: "canonical adversarial mutations"
      to: "scripts/ci/verify_ci_baseline_contract.sh"
      via: "expect_canonical_invalid writes one mutation at a time and requires the public --input path to reject it"
      pattern: "expect_canonical_invalid|unknown-canonical|wrong-type-canonical"
    - from: "scripts/ci/verify_ci_baseline_contract.sh"
      to: "Phase 226/192 stable contracts"
      via: "canonical positive validation, existing self-tests, repository-contract checks, and verify_phase192_ci_contract.sh"
      pattern: "validate_repository_contract|verify_phase192_ci_contract.sh"
  assumptions:
    - id: "EDGE-BASE-01-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BASE-01's deterministic spec-less edge row was unclassified; this plan closes the explicit canonical exact-schema/privacy failure from 226-VERIFICATION.md but invents no additional edge semantics."
    - id: "EDGE-BASE-02-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BASE-02's deterministic spec-less edge row was unclassified; existing proof-state regressions are preserved, but no SPEC-supplied edge-verification tier exists."
    - id: "EDGE-OWN-01-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: OWN-01's deterministic spec-less edge row was unclassified; existing ownership-route regressions are preserved, but no SPEC-supplied edge-verification tier exists."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: The canonical baseline must not silently become a durable sink for unreviewed raw evidence, secret-like strings, or payload content under novel fields."
      category: privacy
      status: unresolved
      verification: null
---

<objective>
Close the final BASE-01 blocker by tracing a known-bad canonical mutation through the public validator to deterministic rejection, then prove the unchanged canonical three-run artifact and all established Phase 226/192 contracts still pass.

Purpose: Make the durable baseline's privacy boundary exact and recursive instead of relying on selected cohort invariants or known-key blacklists, per D-01, D-02, D-03, and D-11.
Output: A production canonical schema/privacy gate, adversarial canonical mutation coverage, and an updated executable validation map.
</objective>

## Artifacts this phase produces

| Path | New or modified symbols | Contract produced |
| --- | --- | --- |
| `scripts/ci/verify_ci_baseline_contract.sh` | `validate_canonical_cohort` recursive jq schema definitions; `reject_sensitive_strings`; `expect_canonical_invalid`; canonical mutation cases | Canonical input has exact keys/types at every durable object/array level, contains no recursively discoverable sensitive/raw-payload strings, and still satisfies the three-run semantic contract. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` | Plan 07 validation rows and canonical mutation coverage | The final BASE-01 gap has named fail-first mutations plus canonical, repository, and Phase 192 positive gates. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` | No content change; validation target only | Existing canonical facts are regression-proven under the exact schema. |

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-05-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-06-SUMMARY.md
@scripts/ci/verify_ci_baseline_contract.sh
@scripts/ci/ci_baseline_workflow_policy.json
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
@scripts/ci/verify_phase192_ci_contract.sh
@.github/workflows/ci.yml

No external API integration: this gap closure validates an existing stored metadata document locally and introduces no API call, SDK, credential, network prerequisite, or capability matrix. The assumption-delta detector reported `detected:false`, so no decision checkpoint is required. Generic credential leakage is security canon covered by the STRIDE register; the descriptor-less prohibition retained above is the product-specific durable-evidence privacy boundary and remains flagged-unverified rather than auto-dismissed.
</context>

<assumptions>
- Plans 226-01 through 226-06 and their summaries are completed historical evidence and remain untouched; Plan 07 changes only the validator and its validation map.
- The authoritative current failure is the sole `gaps:` entry in `226-VERIFICATION.md`; no deferred item is promoted into this plan.
- The canonical JSON's current empty `required_check_snapshot.rules`, `required_check_snapshot.classic_checks`, and `cohort.exclusions` arrays are exact facts of the fixed D-01 `none-enforced` three-run cohort. Their canonical schema must reject inserted objects rather than accept an undefined element shape.
- No one-way decision or schema migration is introduced. Tightening a repository-local versioned validator is reversible, and canonical content changes are neither required nor authorized.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a hostile canonical field through exact-schema privacy rejection</name>
  <reversibility rating="reversible">The change tightens a repository-local validator without altering the canonical artifact, provider state, public API, or stored product data.</reversibility>
  <files>scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` sole remaining gap and the failing canonical privacy spot-check.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` decisions D-01, D-02, D-03, D-05, D-06, D-07, D-08, and D-11.
    - `scripts/ci/verify_ci_baseline_contract.sh` functions `validate_collector_record`, `validate_canonical_cohort`, `validate_aggregates`, `validate_input`, and the current `--self-test` block.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` root and every nested object/array shape; treat it as the fixed canonical schema authority, not as permission to accept novel fields.
  </read_first>
  <behavior>
    - RED: copies of the canonical document with an unknown root `evidence` value or a nested job `evidence` value are demonstrably accepted before the fix and become rejected through `verify_ci_baseline_contract.sh --input FILE` after the fix.
    - The unchanged canonical document passes through `validate_input` → `validate_canonical_cohort` → `validate_aggregates` → `validate_repository_contract`.
    - Canonical validation uses exact sorted-key equality and explicit types for every root, object, object-array element, scalar array, nullable field, closed enum, timestamp, URL, SHA/OID, integer, nonnegative duration, and boolean.
    - Recursive string inspection rejects high-signal synthetic credential strings and raw request/response/event payload content anywhere in canonical input, even beneath a currently allowed string-bearing field; query-bearing URLs remain independently rejected.
  </behavior>
  <action>Start RED in the existing self-test by copying the canonical input to the temporary directory, applying `.evidence = "ghp_synthetic_secret_value"`, and proving the current public `--input` path accepts it before implementation; keep the failing assertion as the tracer and then make it green. Expand `validate_canonical_cohort` so its first boundary is a complete jq schema, using reusable definitions such as `exact($keys)`, `iso`, `num`, `integer`, `sha40`, `string_array`, `lockfile_map`, `step`, `job`, `artifact`, `lane_conclusion`, `signature`, `run`, `run_record`, `anchor`, `cohort_anchor`, `remote_snapshot`, `metric_row`, `metric`, `proof_row`, `proof`, `selection_candidate`, and `canonical`. Require the exact current root keys and recursively cover `policy_manifest`, `privacy`, `required_check_snapshot`, top-level `anchor`, `cohort` and both nested snapshots, all run/job/step/artifact/signature objects, all three aggregate timing objects, aggregate proof/per-run rows, and Phase 227 selection candidates. Require the current fixed-cohort empty rules/classic-check/exclusion arrays to remain empty rather than admitting objects with undefined schemas. Preserve the existing semantic checks and call `validate_aggregates` only after the exact schema passes.

Add a shell function `reject_sensitive_strings(candidate)` and invoke it for both collector and canonical inputs so privacy enforcement is recursive and shared. Its jq scan must traverse `.. | strings` and reject high-signal credential forms used by the adversarial suite (`ghp_`, `github_pat_`, Stripe-style `sk_live_`/`sk_test_`, bearer authorization material, and PEM private-key markers) plus raw request/response/event payload phrases; retain the existing query-bearing URL rejection as a separate check. Keep exact-key rejection primary: the root and nested `evidence` mutations must fail even when their values are harmless. Do not redact, normalize away, or silently drop rejected content. Do not change canonical JSON, collector output, provider calls, workflow policy, proof derivation, aggregate math, or repository topology. This directly implements D-02/D-03/D-11 while preserving D-01/D-05/D-06/D-07/D-08.</action>
  <acceptance_criteria>
    - The public `--input` path rejects an unknown canonical root key carrying `ghp_synthetic_secret_value` and an unknown canonical job key carrying a harmless value.
    - The unchanged checked-in canonical document passes the complete exact recursive schema before semantic validation.
    - Every nonempty object-array level in the canonical document has an exact element schema; scalar arrays have explicit element types; current intentionally empty canonical arrays reject inserted values.
    - Recursive string scanning rejects synthetic credential and raw-payload content anywhere in canonical input and retains query-URL rejection.
    - The three eligible run IDs, workflow/six-lockfile provenance, queue/chain facts, and required-proof aggregate remain byte-for-byte unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; git diff --exit-code -- .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json</automated>
  </verify>
  <done>A production canonical document still validates, while the same public path deterministically rejects hostile or novel content before semantic aggregation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust canonical mutations and certify Phase 226/192 regression preservation</name>
  <reversibility rating="reversible">The adversarial suite and validation map are local contract coverage and can be amended without changing published evidence semantics.</reversibility>
  <files>scripts/ci/verify_ci_baseline_contract.sh, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - `scripts/ci/verify_ci_baseline_contract.sh` Task 1 exact canonical definitions, `expect_invalid`, and repository-contract mutations.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` current quick/full commands, gap-closure coverage, and stable-contract list.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` canonical root/object/array levels used to select one mutation per schema boundary.
    - `scripts/ci/verify_phase192_ci_contract.sh` stable Phase 192 proof/artifact contract.
  </read_first>
  <behavior>
    - `expect_canonical_invalid` sends every mutated canonical copy through the public `--input` CLI and fails the self-test if any mutation is accepted.
    - Named unknown-key cases cover root; policy/privacy/provider/anchor/cohort/aggregate/selection objects; run-record, run, job, step, artifact, signature, lane-conclusion, metric-row, proof-row, and selection-candidate array elements; and inserted objects in intentionally empty canonical arrays.
    - Named wrong-type cases cover object, array, string, number/integer, boolean, nullability, enum, timestamp, URL, SHA/OID, duration, and scalar-array element boundaries.
    - Named recursive privacy cases put synthetic credential and raw-payload strings under both unknown and allowed string-bearing paths, proving schema and content defenses independently.
    - Existing collector/provider/pagination/eligibility/queue/signature mutations, canonical aggregate/provenance facts, repository ownership/topology checks, and Phase 192 verifier all remain green.
  </behavior>
  <action>Add `expect_canonical_invalid(label, jq_filter)` beside the existing collector-record mutation helper, sourcing every mutation from an untouched canonical copy and invoking `bash "$root_dir/scripts/ci/verify_ci_baseline_contract.sh" --input "$path"` so a subshell cannot bypass the public discriminator or repository contract. Build a compact table-driven mutation matrix rather than duplicating validator logic in tests. Include at least one unknown-key mutation at every listed canonical boundary, one inserted object for each intentionally empty canonical array, and wrong-type mutations spanning every jq predicate family. Add independent privacy mutations for an allowed string field containing `ghp_synthetic_secret_value`, another containing `synthetic raw payload`, and an unknown field containing harmless text; these distinguish recursive content rejection from exact-key rejection. Retain the existing collector-record mutation names and expected failures.

Update `226-VALIDATION.md` with Plan 07 rows and a canonical exact-schema/privacy section naming the root, nested-object, array-element, wrong-type, secret-like, raw-payload, and query-URL controls. Keep the quick command `bash scripts/ci/verify_ci_baseline_contract.sh --self-test`. Keep the full local positive gate as canonical contract plus `bash scripts/ci/verify_phase192_ci_contract.sh`; no authenticated collection is needed for this stored-document fix. Record that Plans 01-06 artifacts, canonical JSON/Markdown, workflow, policy manifest, collector, ownership docs, required identities, matrix/cache topology, and Phase 192 files must have no diff. Preserve D-04/D-05/D-06/D-07/D-08/D-09/D-10/D-12 through the existing regression checks; do not redesign the collector or regenerate evidence.</action>
  <acceptance_criteria>
    - Every canonical object/array boundary has a named unknown-key or invalid-element mutation that exits non-zero through `--input`.
    - Every canonical scalar/type family has a named wrong-type mutation that exits non-zero.
    - Synthetic secret-like and raw-payload strings fail recursively even when stored under otherwise allowed string fields.
    - `bash scripts/ci/verify_ci_baseline_contract.sh --self-test`, canonical validation, and `bash scripts/ci/verify_phase192_ci_contract.sh` all pass.
    - Only the verifier and validation map differ from the Plan 06 tree; canonical facts and all already verified Phase 226/192 behavior remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; git diff --check &amp;&amp; test -z "$(git diff -- .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md scripts/ci/capture_ci_baseline.sh scripts/ci/ci_baseline_workflow_policy.json .github/workflows/ci.yml scripts/ci/README.md .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md scripts/ci/verify_phase192_ci_contract.sh)"</automated>
  </verify>
  <done>The adversarial suite proves exact recursive canonical privacy enforcement and all previously verified Phase 226/192 contracts remain intact.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 posture and block threshold

Security enforcement uses OWASP ASVS Level 1. Every critical/high threat below is mitigated; any unresolved high-severity result blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Stored canonical JSON → validator | A durable repository document is untrusted input until exact schema, types, and recursive privacy checks pass. |
| Validator → Phase 227/maintainer interpretation | A green contract authorizes downstream use of timing/proof facts, so bypasses can persist or legitimize unsafe evidence. |
| Gap closure → stable Phase 226/192 contracts | Tightening privacy must not weaken provider, proof, ownership, topology, or artifact guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-226-27 | Information Disclosure | `validate_canonical_cohort` durable-input boundary | high | mitigate | Apply exact recursive key/type schemas before semantic checks and reject recursive secret-like/raw-payload strings plus query-bearing URLs. |
| T-226-28 | Tampering | Canonical nested objects and arrays | high | mitigate | Add public-CLI fail-first mutations for every root/nested/object-array boundary and for intentionally empty arrays. |
| T-226-29 | Spoofing | Canonical types, enums, IDs, timestamps, URLs, and OIDs | high | mitigate | Validate explicit scalar/container types, closed enums, formats, nullability, nonnegative durations, integers, and exact 40-hex identities. |
| T-226-30 | Tampering | Existing Phase 226/192 proof contracts | high | mitigate | Run existing self-tests, canonical/aggregate/repository validation, Phase 192 verifier, and protected-file empty-diff gates. |
| T-226-31 | Denial of Service | Recursive jq validation | low | accept | Input is one bounded checked-in metadata document; the contract performs one finite traversal and introduces no service endpoint. |
| T-226-SC | Tampering | Package supply chain | low | accept | No npm, pip, cargo, package install, or new dependency is introduced. |
</threat_model>

<verification>
Run the deterministic self-test, validate the unchanged canonical cohort through the public contract, run the Phase 192 verifier, require protected evidence/topology files to have no diff, and run `git diff --check`.
</verification>

<success_criteria>
- Canonical input is governed by an exact recursive schema, not selected invariant checks or a blacklist.
- Unknown root and nested keys, inserted object-array content, wrong types, synthetic secrets, raw-payload strings, and query URLs all fail through the public `--input` path.
- The canonical three-run IDs, provenance, queue/chain, provider, proof, cache/setup, signature, aggregate, and Phase 227 selection facts remain unchanged and green.
- Existing OWN-01 ownership routing, BASE-02 proof semantics, CI topology, and Phase 192 artifacts remain regression-protected.
- No external API integration, provider mutation, database/schema operation, package install, or implementation-file change outside the validator/validation map is introduced.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Durable privacy-safe comparable CI account usable for critical-path, ownership, and proof interpretation | 226-01–07 | COVERED | Plan 07 closes the last durable-document privacy bypass and preserves the prior measured account. |
| REQ | BASE-01 | Durable privacy-safe comparable baseline | 226-07 | COVERED | Exact recursive canonical schema/privacy validation and adversarial mutations close the authoritative verification gap. |
| REQ | BASE-02 | Required/skipped/advisory proof distinction | 226-01–06 | COVERED | Already verified; Plan 07 reruns its repository and canonical regression contracts without claiming new implementation ownership. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 226-03–06 | COVERED | Already verified; Plan 07 preserves the command-first runbook and owner-script contract unchanged. |
| RESEARCH | — | Metadata-only exact allowlist; raw evidence remains in Actions | 226-07 | COVERED | Exact canonical schema and recursive content rejection complete the research privacy boundary. |
| RESEARCH | — | Three comparable attempt-one runs and measured queue/critical path | 226-01–06 | COVERED | Canonical facts remain unchanged and must still pass aggregate validation. |
| RESEARCH | — | Stable proof/provider/ownership/topology semantics | 226-03–07 | COVERED | Existing contracts and protected-file empty-diff gates are mandatory. |
| CONTEXT | D-01 | Exactly three attempt-one runs with exact provenance | 226-07 | COVERED | Exact schema and positive canonical regression retain the fixed IDs and blob equality. |
| CONTEXT | D-02 | Complete typed facts, explicit omissions, no estimates | 226-07 | COVERED | Canonical types/nullability/omission fields are recursively exact. |
| CONTEXT | D-03 | Privacy-safe normalized signature only | 226-07 | COVERED | Signature and lane-conclusion schemas plus recursive string rejection exclude raw evidence. |
| CONTEXT | D-04 | Conservative cache semantics | 226-01–06 | COVERED | Unchanged canonical cache enums and existing mutation suite remain green. |
| CONTEXT | D-05 | Independent policy/conclusion/proof and fail-closed aggregate | 226-07 | COVERED | Exact job/proof schemas precede unchanged independent aggregate recomputation. |
| CONTEXT | D-06 | Timestamp-scoped provider snapshot | 226-07 | COVERED | Exact snapshot keys/types and current empty response arrays preserve the timestamped claim. |
| CONTEXT | D-07 | Versioned workflow-policy manifest | 226-07 | COVERED | Exact policy reference and unchanged manifest classification remain required. |
| CONTEXT | D-08 | Confirmed 404 only | 226-05–07 | COVERED | Existing status mutations remain; canonical snapshot fields are exact. |
| CONTEXT | D-09 | Single command-first triage home | 226-03–07 | COVERED | README remains protected and unchanged. |
| CONTEXT | D-10 | Ownership matrix interface | 226-03–07 | COVERED | Ownership artifact and exact commands remain protected and unchanged. |
| CONTEXT | D-11 | Precise vocabulary and raw evidence remains in Actions | 226-07 | COVERED | Recursive privacy enforcement prevents durable raw evidence under allowed or novel paths. |
| CONTEXT | D-12 | Safe repository-local scope | 226-07 | COVERED | Local validation only; no provider writes, bootstrap, topology, product, DB, or API changes. |

Deferred ideas remain excluded: Phase 227 owns matrix/cache/topology/branch-protection changes and the measured optimization. The three unclassified/unresolved edge rows and one descriptor-less prohibition are surfaced explicitly in `must_haves`; none is silently converted to a backstop, dismissed, or wired to a fabricated check.

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-07-SUMMARY.md` when done.
</output>

---
phase: 226-ci-baseline-proof-semantics
plan: "08"
type: execute
wave: 6
depends_on: ["226-07"]
files_modified:
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "The latest 20 unique successful first-attempt non-schedule full-CI runs with complete ordered release-gate → host-integration → Playwright paths form the primary measurement population even when exact topology fingerprints differ."
    - "Every selected observation retains its cohort fingerprint as a stratum; the report names the fingerprint distribution and reports per-stratum sensitivity so topology evolution is visible rather than silently erased."
    - "The primary p50/p95 uses one span per run from earliest successful release-gate start through latest successful Playwright shard completion; parallel shards are never summed."
    - "Fewer than 20 compatible complete paths, duplicate/rerun identities, missing stages, temporal-order violations, or non-full-CI provider-only runs fail the required critical-path gate."
    - "Privacy, provider proof states, setup ownership, host delegation, Playwright topology, workflow identity, and artifact contracts remain unchanged."
  artifacts:
    - path: "scripts/ci/render_ci_baseline.mjs"
      provides: "Compatible-path selection, fingerprint stratification, staged-path percentiles, and sensitivity rendering"
      exports: ["renderBaseline", "deriveStagedPathPercentiles"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Positive and negative controls for compatible selection, stratum disclosure, and byte-exact rendering"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      provides: "Privacy-safe 90-day live Actions snapshot containing at least 20 compatible complete paths"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Byte-reproducible primary percentile conclusion with fingerprint sensitivity"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Green Plan 06-08 gates, assertions, threats, and final Phase 226 sign-off"
  key_links:
    - from: "scripts/ci/render_ci_baseline.mjs"
      to: "run/job/cohort records"
      via: "latest-20 compatible complete-path selection followed by cohort-fingerprint stratification"
      pattern: "deriveStagedPathPercentiles|cohort_fingerprint|release-gate|host-integration|playwright-e2e"
    - from: "scripts/ci/verify_ci_baseline.mjs"
      to: "scripts/ci/render_ci_baseline.mjs"
      via: "fixtures and --require-critical-path call the same exported derivation used by Markdown"
      pattern: "require-critical-path|deriveStagedPathPercentiles"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      via: "two independent renders plus byte comparison"
      pattern: "Fingerprint strata|p50|p95|confirmed|contrary_measured_result"
  prohibitions:
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branch names, logs, secret-presence details, provider payloads, artifact contents, credentials, or sensitive runtime values."
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT mix schedule/provider-only executions into full-CI timing, count reruns or duplicate identities, omit topology strata, sum parallel shards, or manufacture percentiles below 20 complete observations."
    - requirement_id: BASE-02
      status: flagged-unverified
      statement: "MUST NOT promote non_run, skipped, stale, Fake-backed, zero-selected, or differently-SHA evidence into live-provider proof."
    - requirement_id: OWN-01
      status: flagged-unverified
      statement: "MUST NOT change host/CI setup ownership, host delegation, duplicate provisioning, Playwright workers/retries/artifacts, or required-check identity."
---

<objective>
Close Phase 226 using the sufficient live CI evidence that actually exists: 20 compatible complete staged paths, with exact fingerprints retained as visible sensitivity strata rather than used as an impossible all-or-nothing availability gate.

Purpose: Produce a truthful measured critical-path baseline without requiring 20 executions of one frozen workflow topology.
Output: Revised compatible-path derivation and fixtures, recollected frozen evidence, byte-reproducible Markdown, and the completed Phase 226 validation ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-07-SUMMARY.md
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/verify_provider_proof.mjs
@scripts/ci/verify_ci_setup_diagnostics.sh

Live read-only inspection on 2026-08-11 found 228 runs, 28 successful first-attempt full-CI runs, and 28 complete ordered staged paths across 12 fingerprints. The latest 20 span eight fingerprints and measured p50 2,083,000 ms / p95 2,602,000 ms. These observations prove evidence sufficiency but are not themselves durable evidence; this plan must recollect, validate, and freeze them through the canonical collector.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace exact-fingerprint readiness with compatible-path selection and visible sensitivity</name>
  <files>scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <action>Start RED by changing the deterministic fixture population so its latest 20 eligible runs span multiple cohort fingerprints while every run remains a unique successful first attempt with complete ordered release-gate, host-integration, and parallel Playwright jobs. Require exactly 20 selected observations, primary p50/p95 from one span per run, a deterministic fingerprint distribution, and per-stratum count/range or percentile sensitivity wherever the stratum has enough observations. Add precise negative controls for 19 compatible paths, duplicate original-run/SHA identity, rerun attempt, schedule/provider-only inclusion, missing release/host/Playwright, stage-order violation, and parallel-shard summation. Then revise deriveStagedPathPercentiles(records) to select the latest 20 compatible complete paths across fingerprints, retaining cohort_fingerprint on every observation and producing deterministic strata metadata. Update rendering and --require-critical-path validation to disclose the distribution and reject omitted or byte-inexact sensitivity output. Preserve schema-v1, privacy, queue-versus-DAG semantics, and all prior Plan 226-06/07 regressions.</action>
  <acceptance_criteria>
    - A deterministic multi-fingerprint population yields exactly 20 observations and non-null primary p50/p95.
    - Fingerprint counts sum to 20 and deterministic sensitivity output is present in Markdown.
    - Every eligibility, identity, stage, ordering, under-sample, and parallel-shard negative fails at a precise boundary.
    - Existing baseline fixture coverage remains green.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>The critical-path gate accepts sufficient compatible evidence while making topology variation explicit and testable.</done>
</task>

<task type="auto">
  <name>Task 2: Recollect, freeze, byte-compare, and complete the Phase 226 contract</name>
  <precondition>`gh auth status` and the read-only Actions workflow probe succeed; the temporary 90-day collection contains at least 20 compatible complete staged paths.</precondition>
  <checkpoint_protocol gate="blocking-human">Before any durable evidence write, halt on unavailable authentication/API access or fewer than 20 compatible complete paths. Report the exact failing probe or observed eligible/complete counts. Never fabricate evidence, print credentials, broaden the predicate beyond Task 1, or publish partial output.</checkpoint_protocol>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <action>Use task-specific temporary files under the phase directory with EXIT cleanup. Run the canonical read-only collector for szTheory/accrue ci.yml, 90 days, sample target 20. Validate schema/privacy plus --require-critical-path, render twice, byte-compare, and require at least 20 compatible complete paths with explicit fingerprint distribution and sensitivity. Atomically replace the frozen NDJSON and Markdown only after every temporary gate passes, then validate the committed pair again. Run baseline fixtures/artifacts, provider fixtures, the focused Accrue.Test.LiveProofFormatter test, setup diagnostics, and Phase 225 required-lane evidence. Update 226-VALIDATION.md with Plan 06/07/08 rows, the compatible-path and stratum-disclosure assertions, exact commands, threat coverage, and final dated sign-off only after all commands pass. Do not modify provider, formatter, setup, workflow, host, or Playwright implementation files.</action>
  <acceptance_criteria>
    - Frozen records come from the canonical 90-day read-only collector and pass schema/privacy validation.
    - At least 20 compatible complete paths produce a primary p50/p95 plus explicit fingerprint distribution and deterministic sensitivity.
    - Two renders are byte-identical and match the committed Markdown.
    - Provider, formatter, setup, and inherited Phase 225 contracts remain green unchanged.
    - 226-VALIDATION.md records the complete Plan 06-08 contract and final sign-off.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The frozen baseline truthfully measures the latest 20 compatible complete staged paths, discloses topology sensitivity, and every Phase 226 proof and ownership contract is green.</done>
</task>

</tasks>

<verification>
Run both task commands. Completion requires byte-reproducible live evidence with 20 compatible complete paths, visible fingerprint stratification, green provider/formatter/setup/inherited contracts, and a finalized validation ledger.
</verification>

<success_criteria>
- The exact-fingerprint availability deadlock is removed without hiding topology variation.
- The live 90-day snapshot supplies at least 20 validated complete staged paths.
- Frozen records and Markdown are privacy-safe, deterministic, and byte-consistent.
- BASE-01, BASE-02, and OWN-01 retain their safety prohibitions and executable proof.
</success_criteria>

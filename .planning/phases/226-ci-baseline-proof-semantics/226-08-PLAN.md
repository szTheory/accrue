---
phase: 226-ci-baseline-proof-semantics
plan: "08"
type: execute
wave: 8
depends_on:
  - 226-07
files_modified:
  - scripts/ci/verify_ci_baseline_contract.sh
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - BASE-01
  - BASE-02
  - OWN-01
must_haves:
  truths:
    - "BASE-01 — D-02: and D-07: Every collector or canonical job accepted through public --input validation matches exactly one lane in the checked-in workflow-policy manifest; fabricated and ambiguous names fail closed."
    - "BASE-02 — D-05: manifest_identity, policy, required_for_release_proof, initial_queue_root, staged_critical_chain_order, and proof_state are validated as manifest- and eligibility-derived facts rather than trusted candidate assertions."
    - "BASE-01 — D-01: D-03: D-04: D-06: D-08: and D-11: The existing three-run baseline retains its privacy-safe provenance, timing, cache, provider-snapshot, and normalized-signature facts under the repaired validator."
    - "OWN-01 — D-09: D-10: and D-12: Existing setup ownership, stable CI topology, required job identity, and Phase 192 artifact contracts remain unchanged and green."
  artifacts:
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Shared candidate-job policy predicate, derived proof validation, and public-CLI forgery mutations"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executable policy-binding and preservation coverage map"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Existing canonical cohort accepted only after semantic policy binding; modify only if the repaired validator proves regeneration is required"
  key_links:
    - from: "candidate collector/canonical jobs"
      to: "scripts/ci/ci_baseline_workflow_policy.json"
      via: "one shared jq semantic predicate resolves each job name to exactly one lane and compares all policy-owned fields"
      pattern: "candidate_job_semantics|manifest_identity|required_for_release_proof"
    - from: "matched policy lane plus candidate run eligibility/conclusion"
      to: "candidate job proof_state"
      via: "shared proof derivation runs before aggregate proof recomputation"
      pattern: "proof_state|eligible|conclusion"
    - from: "public --input mutation suite"
      to: "collector and canonical validators"
      via: "fabricated names and one-at-a-time policy/proof forgeries must exit non-zero through validate_input"
      pattern: "expect_invalid|expect_canonical_invalid|validate_input"
    - from: "scripts/ci/verify_ci_baseline_contract.sh"
      to: "Phase 226/192 stable contracts"
      via: "canonical validation, repository contract, Phase 192 verifier, and protected-file empty-diff gates"
      pattern: "validate_repository_contract|verify_phase192_ci_contract.sh"
  assumptions:
    - id: "EDGE-BASE-01-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BASE-01's spec-less fallback item is unclassified; Plan 226-08 closes the authoritative policy-binding failure without inventing unrelated edge semantics."
    - id: "EDGE-BASE-02-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BASE-02's spec-less fallback item is unclassified; Plan 226-08 covers the reproduced policy/proof forgery paths, but no SPEC-defined edge tier exists."
    - id: "EDGE-OWN-01-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: OWN-01's spec-less fallback item is unclassified; already-satisfied ownership behavior is preserved by regression gates rather than reopened."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: Candidate-controlled lane names or policy/proof fields must never mint release proof without an exact workflow-policy match and eligibility-derived proof state."
      status: unresolved
      verification: null
    - statement: "FLAGGED-UNVERIFIED: Repairing semantic binding must not widen the durable privacy boundary, ingest raw evidence, or weaken stable CI and Phase 192 proof contracts."
      status: unresolved
      verification: null
---

<objective>
Close the remaining BASE-01 and BASE-02 failures by binding both collector records and canonical cohorts to the versioned workflow-policy manifest through the public validator, while preserving already-satisfied OWN-01 and all durable evidence boundaries.

Purpose: Make accepted job identity, policy, and proof facts independently reproducible from trusted policy plus observed eligibility/conclusion instead of candidate-controlled assertions, per D-02, D-05, and D-07.
Output: One shared semantic predicate, public-CLI forgery controls for both document types, and an updated validation map.
</objective>

## Artifacts this phase produces

| Path | New or modified symbols | Contract produced |
| --- | --- | --- |
| `scripts/ci/verify_ci_baseline_contract.sh` | `candidate_job_semantics` (or equivalently named shared jq predicate); proof-state derivation; collector/canonical fabricated-name and forged-field mutation cases | Every accepted candidate job is uniquely policy-matched and all policy/proof fields equal derived values before aggregates are trusted. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` | Plan 08 policy-binding rows and preservation gates | BASE-01/BASE-02 closure has named positive and adversarial evidence; OWN-01 remains regression-protected. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` | No planned content change; validation target only | The checked-in baseline remains authoritative unless the repaired validator demonstrates that regeneration from the existing collector is necessary. |
| `.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md` | Planning-time reasoned no-external-API declaration | The deterministic API gate records that this local validator closure introduces no external capability surface. |

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-07-SUMMARY.md
@scripts/ci/verify_ci_baseline_contract.sh
@scripts/ci/capture_ci_baseline.sh
@scripts/ci/ci_baseline_workflow_policy.json
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@scripts/ci/verify_phase192_ci_contract.sh
@.github/workflows/ci.yml

No external API integration: the deterministic detector fired on phase prose, but this closure validates existing stored metadata locally and adds no provider call, SDK, credential, network prerequisite, or capability surface. `COVERAGE.md` carries the required reasoned declaration.
</context>

<assumptions>
- Plans 226-01 through 226-07 and their summaries are completed historical evidence and remain untouched; `226-VERIFICATION.md` is authoritative user-owned input and must not be overwritten.
- The two failed truths in the sole `gaps:` section describe one shared policy-binding defect; there are no deferred items to promote.
- The collector's current `classify_job` and proof-state expression are the behavior reference: exact-one regex lane match; skipped conclusion wins, then advisory, then conditional, then eligible required success, otherwise not-applicable.
- This plan carries forward D-05/D-07's already-locked `costly` proof/policy contract. It introduces no one-way data format, public API, schema migration, provider mutation, or package installation, so no decision checkpoint is required.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind one collector and canonical job end-to-end to trusted policy semantics</name>
  <reversibility rating="costly">D-05/D-07 make the published proof vocabulary and manifest interpretation boundary shared contracts for the checked-in evidence and Phase 227 consumers.</reversibility>
  <files>scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` authoritative BASE-01/BASE-02 failures, public mutation reproductions, and the sole actionable gaps.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` decisions D-01 through D-08 and D-11/D-12; policy, privacy, and safe-scope constraints are binding.
    - `scripts/ci/capture_ci_baseline.sh` `classify_job` and `build_run` lane/proof derivation; mirror these semantics without calling the collector or trusting its output.
    - `scripts/ci/ci_baseline_workflow_policy.json` exact lane keys, regex matching rules, and policy-owned values.
    - `scripts/ci/verify_ci_baseline_contract.sh` `validate_policy_manifest`, `validate_collector_record`, `validate_canonical_cohort`, `validate_aggregates`, `validate_input`, and self-test helpers.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` unchanged positive canonical target and current job/run nesting.
  </read_first>
  <behavior>
    - RED: a collector record and canonical copy whose first job name is fabricated currently pass public `--input`; retained tests make both fail after implementation.
    - Every candidate job name resolves against policy lane regexes to exactly one match; zero or multiple matches reject the containing document.
    - The candidate's manifest_identity, policy, required_for_release_proof, initial_queue_root, and staged_critical_chain_order equal the unique matched lane's values.
    - Expected proof_state is derived from the containing run's eligibility, the matched lane, and observed conclusion with precedence: skipped conclusion, advisory policy, conditional policy, eligible required success, then not-applicable.
    - Both collector-record and canonical-cohort public dispatch paths invoke the same predicate before signature and aggregate proof checks; the unchanged checked-in canonical document remains valid.
  </behavior>
  <action>Start with public-CLI failing tests for one fabricated collector job name and one fabricated canonical job name, using fresh copies of valid fixture/canonical documents. In `verify_ci_baseline_contract.sh`, define one reusable candidate-job semantic predicate that receives the candidate job, its containing run eligibility, and the validated policy manifest. Resolve the job name by evaluating every manifest lane regex and require exactly one result. From that result independently construct the expected `manifest_identity`, `policy`, `required_for_release_proof`, `initial_queue_root`, and `staged_critical_chain_order`. Derive expected `proof_state` from observed conclusion and run eligibility using the collector's precedence described in the behavior block. Require candidate values to equal the complete expected tuple.

Apply the predicate inside the recursive run/job traversal of both `validate_collector_record` and `validate_canonical_cohort`; do not leave either public `--input` discriminator on a shape-only path. Keep `validate_aggregates` after semantic binding so aggregate recomputation consumes facts already proven equal to policy derivation. Replace the collector validator's shallow inline job/artifact/signature checks with its already-declared complete predicates where needed so the shared semantic check cannot be bypassed through type-confused nested data. Do not duplicate the policy formula separately for collector and canonical documents, infer identities from aggregate fields, accept first-match behavior, normalize a fabricated name, or modify the policy manifest/collector/canonical evidence. This implements D-02/D-05/D-07 while preserving D-01/D-03/D-04/D-06/D-08/D-11/D-12.</action>
  <acceptance_criteria>
    - A fabricated job name exits non-zero through public `--input` for both `ci-baseline-collector-record` and `ci-baseline-canonical` documents.
    - Every accepted job has exactly one matching manifest lane and all five policy-owned fields equal that lane.
    - Every accepted proof_state equals the value derived from run eligibility, matched policy, required flag, and observed conclusion.
    - Collector and canonical validators call the same semantic predicate before signature or aggregate validation.
    - The unchanged checked-in canonical document and a valid collector fixture pass public validation.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh --input .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &amp;&amp; git diff --exit-code -- scripts/ci/ci_baseline_workflow_policy.json scripts/ci/capture_ci_baseline.sh .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</automated>
  </verify>
  <done>One shared fail-closed policy predicate governs every candidate job accepted through either public document mode, and the existing positive evidence still validates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every policy/proof forgery fails and certify preserved Phase 226/192 contracts</name>
  <reversibility rating="reversible">The adversarial matrix and validation documentation are local regression coverage with no external or stored-data migration cost.</reversibility>
  <files>scripts/ci/verify_ci_baseline_contract.sh, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - `scripts/ci/verify_ci_baseline_contract.sh` Task 1 semantic predicate, `expect_invalid`, `expect_canonical_invalid`, aggregate checks, and repository-contract checks.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` exact accepted-forgery spot-checks that this task must invert.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` existing Plan 01-07 controls, quick/full gates, and protected artifacts.
    - `scripts/ci/verify_phase192_ci_contract.sh` Phase 192 artifact and stable identity contract.
    - `.github/workflows/ci.yml` protected topology and single shift-left invocation.
  </read_first>
  <behavior>
    - Public mutation cases for collector and canonical documents independently reject zero-match fabricated names and every forged policy-owned field.
    - Mutating only manifest_identity, policy, required_for_release_proof, initial_queue_root, staged_critical_chain_order, or proof_state fails even when all other candidate fields remain valid.
    - Proof mutations cover successful eligible required lanes plus skipped, advisory, conditional, unsuccessful, and ineligible combinations, pinning derivation precedence rather than only enum shape.
    - Existing exact-schema/privacy, provider-status, pagination, queue/chain, signature, provenance, aggregate, topology, ownership, and Phase 192 controls remain green.
    - Checked-in canonical JSON/Markdown, collector, policy manifest, workflow, ownership docs, and Phase 192 verifier remain unchanged unless the repaired validator itself proves the canonical artifact invalid; any such result halts for evidence-backed regeneration rather than hand editing.
  </behavior>
  <action>Extend the table-driven self-test with paired collector/canonical mutations that always execute a fresh public `--input` process. Add named cases for a fabricated job name and one-at-a-time forgeries of `manifest_identity`, `policy`, `required_for_release_proof`, `initial_queue_root`, `staged_critical_chain_order`, and `proof_state`. Add proof-state cases representing eligible required success, skipped conclusion, advisory policy, conditional policy, unsuccessful required conclusion, and an ineligible run; each must establish that only the derived state passes. Include an ambiguous-name fixture only if the current manifest regexes can be safely exercised without changing production policy; otherwise the shared predicate's exact-one assertion plus the zero-match public mutations is the required ambiguity guard and should be documented as such.

Update `226-VALIDATION.md` with Plan 08 rows mapping the two verification gaps to collector and canonical fabricated-name controls, every policy-field forgery, proof derivation combinations, and the full positive regression command. Preserve and rerun Plan 07 recursive privacy/schema controls, the existing repository contract, and `verify_phase192_ci_contract.sh`. Treat the canonical baseline as checked-in evidence, not a test fixture to edit around the validator: regenerate it through the existing collector only if positive validation demonstrates a real mismatch, and require the same three run IDs/provenance plus a reviewed JSON/Markdown diff. Do not change workflow topology, required identities, matrix breadth, cache topology, ownership guidance, Phase 192 artifacts, provider calls, or privacy allowlists.</action>
  <acceptance_criteria>
    - Collector and canonical public validation each reject fabricated names and each of the six forged policy/proof fields with named self-test output.
    - Proof-state derivation tests distinguish skipped, advisory, conditional, eligible-required-success, unsuccessful, and ineligible cases.
    - Self-test, checked-in canonical validation, repository contract, and Phase 192 contract all pass in one automated command.
    - OWN-01 ownership routes, stable job identities, topology, privacy boundary, provider semantics, and Phase 192 artifacts remain unchanged.
    - Canonical JSON/Markdown are not regenerated unless the repaired validator supplies a concrete failing invariant; if regeneration occurs, it uses the existing collector and preserves D-01 cohort/provenance.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query check.api-coverage-verify-pre 226 | jq -e '.passed == true' &amp;&amp; git diff --check &amp;&amp; test -z "$(git diff -- scripts/ci/ci_baseline_workflow_policy.json scripts/ci/capture_ci_baseline.sh .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md .github/workflows/ci.yml scripts/ci/README.md .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md scripts/ci/verify_phase192_ci_contract.sh)"</automated>
  </verify>
  <done>Public validation rejects every reproduced identity/policy/proof forgery for both document types, while all satisfied Phase 226/192 guarantees remain green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 posture and block threshold

Security enforcement uses OWASP ASVS Level 1. Every critical/high threat is mitigated; any unresolved high-severity result blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Candidate metadata document → public validator | Collector and canonical JSON are untrusted until identity, policy, proof, schema, and privacy validation complete. |
| Workflow-policy manifest → candidate lane facts | The checked-in manifest is the trusted interpretation boundary; candidate copies of its fields are assertions, not authority. |
| Validated baseline → maintainer/Phase 227 | A green result authorizes release-proof and optimization decisions, so forged proof can cause downstream safety regressions. |
| Gap closure → stable Phase 226/192 contracts | Repair work must not weaken privacy, ownership, topology, required identities, or historical artifact evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-226-32 | Spoofing | Candidate job name and `manifest_identity` | high | mitigate | Resolve every name against all manifest regexes, require exactly one lane, and compare the derived identity in both public input modes. |
| T-226-33 | Tampering | Candidate policy-owned fields and proof state | high | mitigate | Derive the complete policy tuple and proof state from manifest plus eligibility/conclusion; add one-field public mutations for both document types. |
| T-226-34 | Elevation of Privilege | Release proof aggregate | high | mitigate | Run semantic binding before aggregate recomputation so candidate-controlled `proved`/required assertions cannot authorize release proof. |
| T-226-35 | Information Disclosure | Existing durable privacy boundary | high | mitigate | Retain exact recursive schema, sensitive-string/raw-payload rejection, query-URL checks, and unchanged canonical positive validation. |
| T-226-36 | Tampering | Phase 226/192 stable contracts | high | mitigate | Run repository, topology, ownership, canonical, and Phase 192 gates plus protected-file empty-diff checks. |
| T-226-37 | Denial of Service | Per-job manifest regex evaluation | low | accept | Inputs and the checked-in 18-lane manifest are bounded local documents; exhaustive exact-one matching is finite and not exposed as a service. |
| T-226-SC | Tampering | Package supply chain | low | accept | No npm, pip, cargo, package install, or new dependency is introduced. |
</threat_model>

<verification>
Run the public-CLI self-test matrix, validate the checked-in canonical cohort, run the Phase 192 verifier and API declaration gate, require protected evidence/topology files to have no diff, and run `git diff --check`. High-severity threat controls must all pass.
</verification>

<success_criteria>
- Both public document modes reject fabricated/ambiguous identities and candidate forgeries of every policy/proof field.
- Proof state is derived from trusted lane policy, run eligibility, required status, and observed conclusion before release aggregates are accepted.
- The existing canonical three-run baseline remains privacy-safe, comparable, and semantically trustworthy without hand-edited evidence.
- Already-satisfied OWN-01, stable CI topology, required identities, provider semantics, and Phase 192 artifacts remain unchanged and verified.
- No external API integration, provider mutation, package installation, database/schema operation, product/UI change, matrix/cache rewrite, branch-protection edit, or required-gate demotion is introduced.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Durable privacy-safe comparable CI account usable for critical-path, ownership, and provider-proof interpretation | 226-01–08 | COVERED | Plan 08 closes the remaining semantic trust boundary while preserving the measured account. |
| REQ | BASE-01 | Durable privacy-safe baseline with trustworthy observed facts | 226-08 | COVERED | Candidate jobs are uniquely manifest-bound in both public modes. |
| REQ | BASE-02 | Required/skipped/advisory/non-applicable proof cannot be forged | 226-08 | COVERED | All policy fields and proof state are independently derived and mutation-tested. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 226-03–08 | COVERED | Already satisfied; Plan 08 reruns and protects its existing contract without reopening scope. |
| RESEARCH | — | Versioned metadata-only baseline and exact comparable cohort | 226-01–08 | COVERED | Existing cohort/provenance remain authoritative; semantic validation is completed. |
| RESEARCH | — | Manifest-owned proof taxonomy and unknown-identity rejection | 226-08 | COVERED | Shared exact-one predicate binds both public document types. |
| RESEARCH | — | Privacy allowlist with raw diagnostics retained only as Actions artifacts | 226-07–08 | COVERED | Plan 07 controls remain mandatory and unchanged. |
| RESEARCH | — | Stable ownership, topology, provider, and Phase 192 contracts | 226-03–08 | COVERED | Positive regression and protected-file gates preserve them. |
| CONTEXT | D-01 | Exactly three attempt-one runs with workflow/six-lockfile provenance | 226-01–08 | COVERED | Positive canonical gate preserves cohort and provenance. |
| CONTEXT | D-02 | Complete typed facts, explicit omissions, no timing estimates | 226-01–08 | COVERED | Shared semantic binding adds independently validated lane facts without changing timing. |
| CONTEXT | D-03 | Privacy-safe normalized signature only | 226-01–08 | COVERED | Plan 07 privacy controls and signature mutations remain green. |
| CONTEXT | D-04 | Conservative cache semantics | 226-01–08 | COVERED | Existing cache derivation and enum controls remain unchanged. |
| CONTEXT | D-05 | Independent policy/conclusion/proof and fail-closed aggregate | 226-08 | COVERED | Candidate tuple/proof derivation precedes aggregate recomputation. |
| CONTEXT | D-06 | Timestamp-scoped provider snapshot | 226-01–08 | COVERED | Provider contract and canonical snapshot remain unchanged. |
| CONTEXT | D-07 | Versioned workflow-policy manifest; unknown identity rejection | 226-08 | COVERED | Exact-one shared predicate is the closure implementation. |
| CONTEXT | D-08 | Confirmed authenticated 404 only | 226-05–08 | COVERED | Existing provider-status mutations remain green. |
| CONTEXT | D-09 | Single command-first triage home | 226-03–08 | COVERED | README remains protected and unchanged. |
| CONTEXT | D-10 | Ownership matrix interface | 226-03–08 | COVERED | Ownership artifact/commands remain protected and unchanged. |
| CONTEXT | D-11 | Precise vocabulary; raw evidence stays in Actions | 226-03–08 | COVERED | Existing vocabulary/privacy gates remain required. |
| CONTEXT | D-12 | Safe repository-local scope | 226-08 | COVERED | Local validation only; no provider writes, bootstrap, topology, product, DB, or external API work. |

Deferred Phase 227 optimization/topology/cache/branch-protection work remains excluded. The three spec-less unclassified items and descriptor-less value/safety prohibitions remain explicit, unresolved, and flagged-unverified; none is silently dismissed or converted into fabricated coverage.

<api_coverage>
The deterministic detector fires on historical phase prose, but Plan 226-08 adds no external API integration. `.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md` records the reasoned no-integration declaration; capability rows would be fabricated and are therefore intentionally absent.
</api_coverage>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-08-SUMMARY.md` when done.
</output>

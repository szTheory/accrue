---
phase: 226-ci-baseline-proof-semantics
plan: "09"
type: execute
wave: 9
depends_on:
  - 226-08
files_modified:
  - scripts/ci/ci_baseline_workflow_policy.json
  - scripts/ci/capture_ci_baseline.sh
  - scripts/ci/verify_ci_baseline_contract.sh
autonomous: true
gap_closure: true
requirements:
  - BASE-01
  - BASE-02
  - OWN-01
must_haves:
  truths:
    - "BASE-01 — D-01: D-02: D-07: Collection and both public validation modes accept evidence only when the run workflow equals policy.workflow and every eligible run carries the complete policy-required proof identity set."
    - "BASE-02 — D-05: D-07: Every eligible run observes and proves every required_for_release_proof identity; removing one required lane through the public --input path fails even when all remaining lanes are valid and proved."
    - "BASE-01 — D-05: D-07: The complete live CI workflow topology, including all job IDs, expanded release and Playwright matrix display names, policy roles, and live-stripe conditional behavior, conforms exactly to the versioned policy manifest."
    - "BASE-01 — D-03: D-04: D-06: D-08: D-11: Existing normalized signatures, conservative cache facts, timestamped provider state, provider-error handling, and privacy boundaries remain unchanged and green."
    - "OWN-01 — D-09: D-10: D-12: The command-first ownership route, stable CI topology, required-check identities, matrix breadth, Phase 192 artifacts, and repository-local scope remain preserved."
  artifacts:
    - path: "scripts/ci/ci_baseline_workflow_policy.json"
      provides: "Versioned workflow identity, lane-proof, complete topology, matrix-expansion, and conditional-presence authority"
    - path: "scripts/ci/capture_ci_baseline.sh"
      provides: "Collector-side workflow identity rejection and updated policy-schema validation"
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Shared workflow/completeness predicates, complete workflow-to-policy conformance, and public-path drift controls"
  key_links:
    - from: "GitHub run metadata and candidate .run.workflow"
      to: "scripts/ci/ci_baseline_workflow_policy.json policy.workflow"
      via: "collector pre-reduction equality plus shared public-validator equality"
      pattern: "workflow|policy_manifest|validate_input"
    - from: "eligible candidate run jobs"
      to: "policy lanes where required_for_release_proof is true"
      via: "exact observed/proved identity-set derivation before aggregate validation"
      pattern: "required_for_release_proof|required_lane_identities|proof_state"
    - from: ".github/workflows/ci.yml"
      to: "policy workflow_topology"
      via: "normalized extraction of every job, name expansion, event condition, matrix support role, and conditional lane"
      pattern: "workflow_topology|job_id|expanded_display_names|applicable_events"
    - from: "self-test workflow copies and candidate copies"
      to: "public validator and repository contract"
      via: "policy-derived workflow, lane-removal, matrix, conditional, rename, removal, and addition mutations"
      pattern: "expect_invalid|expect_canonical_invalid|validate_repository_contract"
  assumptions:
    - id: "EDGE-BASE-01-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BASE-01's descriptor-less spec probe item remains unclassified; this plan defensibly covers workflow identity, complete topology, matrix expansion, and conditional-lane drift without inventing a different edge taxonomy."
    - id: "EDGE-BASE-02-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: BASE-02's descriptor-less spec probe item remains unclassified; this plan covers complete required-lane proof and public lane-removal controls, but no SPEC-defined edge tier exists."
    - id: "EDGE-OWN-01-UNCLASSIFIED"
      status: unresolved
      statement: "FLAGGED-UNVERIFIED: OWN-01's descriptor-less spec probe item remains unclassified; already-satisfied ownership behavior is protected by regression gates and is not reopened."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: Unrelated-workflow or incomplete-lane evidence must never be presented as release proof, even when every observed job is individually valid and successful."
      status: unresolved
      verification: null
    - statement: "FLAGGED-UNVERIFIED: Policy conformance must not silently erase advisory or conditional semantics, especially the periodic live-stripe lane."
      status: unresolved
      verification: null
    - statement: "FLAGGED-UNVERIFIED: Closing proof gaps must not ingest raw diagnostics, shift host-owned setup into CI, or imply that local host verification is externally enforced CI proof."
      status: unresolved
      verification: null
---

<objective>
Close the three authoritative integration gaps by binding every accepted record to the exact CI workflow, requiring complete eligible-run release proof, and failing closed when any part of the live workflow topology diverges from the versioned policy.

Purpose: Make the Phase 226 baseline safe for Phase 227 decisions because workflow origin, proof completeness, and policy freshness are independently demonstrated through public-path negative controls.
Output: An enriched policy manifest, collector workflow guard, shared validator completeness checks, full workflow-topology conformance, and adversarial mutations for every reproduced gap.
</objective>

## Artifacts this phase produces

| Path | New or modified symbols / sections | Contract produced |
| --- | --- | --- |
| `scripts/ci/ci_baseline_workflow_policy.json` | bumped `schema_version`; `workflow_topology`; per-job `job_id`, exact name source/expansions, event condition/applicability, matrix role and expected multiplicity | One checked-in authority describes the exact CI workflow and every live job/matrix/conditional identity without changing the workflow. |
| `scripts/ci/capture_ci_baseline.sh` | policy-schema gate; workflow-name equality in `build_run` before record construction | A run from any workflow other than `policy.workflow` cannot become collector output. |
| `scripts/ci/verify_ci_baseline_contract.sh` | `candidate_workflow_semantics`; `candidate_required_proof_completeness`; `extract_workflow_topology`; `validate_workflow_topology` (or equivalently named focused helpers); public mutation cases | Collector and canonical inputs prove workflow origin and complete eligible proof; repository validation proves the policy still matches all live topology. |
| `.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md` | no content change | Existing no-external-API declaration remains accurate because this plan adds no network capability, endpoint, SDK, credential, or provider mutation. |

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-REVIEW.md
@.planning/phases/226-ci-baseline-proof-semantics/226-08-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
@scripts/ci/ci_baseline_workflow_policy.json
@scripts/ci/capture_ci_baseline.sh
@scripts/ci/verify_ci_baseline_contract.sh
@scripts/ci/verify_phase192_ci_contract.sh
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@.github/workflows/ci.yml

No external API capability is added. The collector continues to use its existing read-only GitHub metadata surface; this plan changes only local validation of workflow identity, lane completeness, and checked-in workflow-policy conformance. Preserve `COVERAGE.md` unless the implementation actually introduces a new external capability.
</context>

<assumptions>
- Plans 226-01 through 226-08 and their summaries are completed historical evidence and remain untouched; the modified `226-VERIFICATION.md` is authoritative input and must not be edited.
- The three current `gaps:` entries are one sequential trust-boundary repair over shared policy/collector/verifier files; there are no deferred items to promote.
- D-05/D-07's proof vocabulary and versioned policy boundary are already locked and costly to undo. The added topology fields refine that same contract and do not create a new public API, persistence migration, provider write, or package dependency, so no decision checkpoint is required.
- API coverage does not change: all added tests use fixture or checked-in workflow/policy files, while existing live collector endpoints and permissions remain exactly as before.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact workflow origin and complete required proof through public paths</name>
  <reversibility rating="costly">D-05/D-07 make workflow identity and complete required proof part of the durable interpretation contract consumed by maintainers and Phase 227.</reversibility>
  <files>scripts/ci/capture_ci_baseline.sh, scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` gaps 1 and 2 plus the three failing public spot-checks for workflow identity and partial proof.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` D-01 through D-08 and D-11/D-12; workflow/proof/privacy decisions are binding.
    - `scripts/ci/ci_baseline_workflow_policy.json` top-level `workflow` and lanes with `required_for_release_proof`.
    - `scripts/ci/capture_ci_baseline.sh` `build_run`, policy validation, and fixture response construction.
    - `scripts/ci/verify_ci_baseline_contract.sh` `candidate_job_semantics`, `validate_collector_record`, `validate_canonical_cohort`, `validate_aggregates`, `validate_input`, and public self-test helpers.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` positive three-run cohort, including observed identity multiplicities.
  </read_first>
  <behavior>
    - RED: a fixture whose only changed run field is `name: Other workflow`, a collector document whose only changed run field is `.run.workflow`, and a canonical document with the same mutation all currently pass; retained tests make all three fail.
    - The collector compares provider `raw.name` with the loaded policy workflow before `build_run` can emit any job proof.
    - Both public document discriminators use one shared predicate requiring top-level policy workflow and every `.runs[].run.workflow` to equal the checked-in policy workflow.
    - For each eligible run, the unique observed identities marked `required_for_release_proof` equal the complete policy-required identity set, and the unique proved identities equal that same set.
    - Conditional/advisory lanes remain separately modeled and cannot satisfy required completeness; permitted conditional absence is explicit policy data rather than an accidental omission.
    - Removing one policy-selected required identity from a valid eligible collector record or canonical run fails through a fresh public `--input` process.
  </behavior>
  <action>Begin with the three workflow-name RED controls and paired collector/canonical required-lane-removal RED controls described in the behavior block. Construct the raw collector fixture by changing only the provider run name, then invoke `capture_ci_baseline.sh`; construct document mutations from valid positive inputs and invoke the public verifier process rather than a private jq helper.

In `capture_ci_baseline.sh`, load the policy workflow once and fail before record reduction when the provider run name differs. In `verify_ci_baseline_contract.sh`, add one shared workflow predicate used by both `validate_collector_record` and `validate_canonical_cohort`; compare the document's policy snapshot and every run workflow against the checked-in policy, not against each other alone. Add one shared eligible-run completeness predicate that derives the sorted unique required identity set from manifest lanes, derives observed and `proved` sets from each eligible run after `candidate_job_semantics`, and requires both candidate sets to equal policy. Keep non-eligible records diagnostic-only and prohibit any proved identity there per D-05.

Choose the removal target at test runtime from the policy's required lanes, then remove every job occurrence for that identity from a fresh valid document. Do not name a specific lane in the mutation, trust stored aggregate arrays as completeness evidence, count advisory/conditional identities as required proof, weaken the exactly-three cohort rule, or edit canonical evidence to satisfy the new validator. Keep D-01 provenance, D-02 typed omissions, D-03 privacy-safe signatures, D-04 cache semantics, D-06/D-08 provider snapshot behavior, and D-11/D-12 privacy/scope controls intact.</action>
  <acceptance_criteria>
    - Collector fixture capture rejects a provider run whose name differs from `policy.workflow` before writing an output record.
    - Public collector and canonical `--input` validation each reject a document whose run workflow differs from the checked-in policy.
    - Every eligible record's observed required identity set and proved required identity set exactly equal the complete policy-derived set.
    - Public collector and canonical validation each reject a policy-derived required-lane removal while the untouched positive documents pass.
    - Advisory, skipped, not-applicable, and explicitly conditional absence remain non-proof; ineligible records cannot contain proved lanes.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh --input .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &amp;&amp; git diff --exit-code -- .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md .github/workflows/ci.yml</automated>
  </verify>
  <done>Collector and both public validator modes authenticate the CI workflow and reject incomplete eligible release proof through policy-derived public negative controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the complete live workflow topology to the versioned policy</name>
  <reversibility rating="costly">D-07 makes the enriched topology manifest the trusted interpretation boundary; later workflow edits must update policy and its reviewed drift controls together.</reversibility>
  <files>scripts/ci/ci_baseline_workflow_policy.json, scripts/ci/capture_ci_baseline.sh, scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` gap 3 and the accepted non-policy-lane rename spot-check.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-REVIEW.md` CR-03 full topology, matrix support-tier, and conditional live-stripe expectations.
    - `.github/workflows/ci.yml` top-level workflow name, every `jobs:` entry, release-gate matrix/name/continue-on-error, Playwright shard matrix/name, and live-stripe `if:` condition.
    - `scripts/ci/ci_baseline_workflow_policy.json` all lane regexes and proof roles after Task 1.
    - `scripts/ci/capture_ci_baseline.sh` policy schema gate that must accept exactly the enriched manifest.
    - `scripts/ci/verify_ci_baseline_contract.sh` `validate_policy_manifest`, `job_body`, `validate_repository_contract`, and workflow-copy self-tests.
  </read_first>
  <behavior>
    - Policy records the exact workflow name and a complete topology entry for every live YAML job ID; no workflow job is omitted and no policy topology job is absent from YAML.
    - Each topology entry records the exact name source, event condition/applicability, expanded display-name multiset, lane-policy role, and expected multiplicity; release-gate expands to all four cells and Playwright expands to shards 1/3, 2/3, and 3/3.
    - Release support values and `continue-on-error` preserve three required cells plus the visibly advisory Sigra cell; live-stripe remains conditional for workflow_dispatch/schedule and non-proof.
    - `validate_workflow_topology` derives a normalized snapshot from the supplied `ci_file` and compares it to policy; unsupported or ambiguous matrix/name syntax fails rather than yielding partial topology.
    - Policy-derived mutations of a non-matrix lane, a matrix cell/support value, and the conditional live-stripe expression fail; removing or adding any job also fails.
    - Mutation targets and replacement values are selected from policy/topology data at runtime, so coverage is not limited to the six previously named jobs or a single example lane.
  </behavior>
  <action>Enrich `ci_baseline_workflow_policy.json` with a complete `workflow_topology` section and bump its schema version consistently in the collector, verifier, and stored policy snapshot rules. For every unique live YAML job, record `job_id`, the exact workflow name expression, normalized `if` expression or explicit unconditional marker, the complete expanded display-name list, expected instance count, and the policy lane identities produced by that job. Record matrix source values and support roles for `release-gate`, shard values for `playwright-e2e`, and event applicability for `live-stripe`. Cross-validate the topology section against `.lanes`: every lane belongs to one job, every expanded display name matches exactly one lane regex, required/advisory/conditional roles agree, and all required proof lanes are reachable.

In `verify_ci_baseline_contract.sh`, add an extractor that reads the supplied `ci_file` into a normalized topology document. Parse every two-space job key and its job-scoped `name`, `if`, matrix, and `continue-on-error` declarations; expand the repository's existing release include matrix and Playwright shard list into concrete display names. Reject duplicate jobs, unknown expansion forms, missing name/condition fields, and any partial parse. Compare exact workflow name, exact job-ID set, per-job normalized condition, expanded display-name multiset, multiplicity, and release support role to policy. Invoke this from `validate_repository_contract` before the older targeted chain/artifact/ownership checks, which remain defense in depth.

Extend self-test using fresh workflow copies. Select mutation targets from `workflow_topology` with jq: rename a policy-selected scalar job display name, remove a selected job block, append an unmanifested job block, alter one policy-selected release matrix support value, change one Playwright shard value, and alter the topology entry marked conditional. Each copy must fail `validate_repository_contract`; an untouched workflow copy must pass. Use structural job-block boundaries for edits and checks so comments outside the selected job cannot satisfy or invalidate a mutation. Do not alter `.github/workflows/ci.yml`, its matrix breadth, dependency graph, conditions, required identities, cache steps, branch protection, Phase 192 artifacts, or the checked-in baseline.</action>
  <acceptance_criteria>
    - Policy topology and live workflow have identical complete job-ID sets with no omitted or extra job.
    - All four release display identities/support roles, all three Playwright shard display names, every scalar display name, and live-stripe conditional behavior are independently derived and equal policy.
    - A policy-selected scalar rename, job removal, job addition, release support mutation, Playwright shard mutation, and conditional-expression mutation each fail repository validation.
    - Mutation target selection is policy-driven; no test's coverage depends on `phase18-tax-gate` or the prior six-job list.
    - The untouched workflow, canonical contract, Phase 192 contract, API coverage declaration, and all Task 1 public mutations pass together.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query check.api-coverage-verify-pre 226 | jq -e '.passed == true' &amp;&amp; git diff --check &amp;&amp; git diff --exit-code -- .github/workflows/ci.yml .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md scripts/ci/README.md scripts/ci/verify_phase192_ci_contract.sh</automated>
  </verify>
  <done>The complete live CI topology is policy-bound and drift-tested, including matrix expansions and conditional provider behavior, while every stable Phase 226/192 contract remains green.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 posture and block threshold

Security enforcement uses OWASP ASVS Level 1. Every critical/high threat is mitigated; any unresolved high-severity result blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| GitHub run metadata → collector | Provider workflow identity and job observations are untrusted until matched to checked-in policy. |
| Candidate JSON → public validator | Collector and canonical documents can assert false workflow/proof facts unless independently derived. |
| Live workflow YAML → policy manifest | Repository changes can stale the policy unless the complete topology is normalized and compared. |
| Validated baseline → Phase 227 | A green baseline authorizes optimization decisions, making false proof a downstream safety risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-226-38 | Spoofing | Run workflow identity | high | mitigate | Compare provider and candidate workflow names to policy.workflow in collector and both public validator modes; retain paired negative mutations. |
| T-226-39 | Elevation of Privilege | Eligible release proof | high | mitigate | Require observed and proved identity sets to equal the complete policy-required set before aggregate validation; remove a policy-selected lane in public negative controls. |
| T-226-40 | Tampering | Workflow-policy topology | high | mitigate | Normalize every live job, matrix expansion, role, and condition and compare exact sets/multisets to the versioned policy. |
| T-226-41 | Repudiation | Drift regression suite | medium | mitigate | Derive mutation targets from policy, execute them through public/repository entry points, and require each isolated workflow/document copy to fail. |
| T-226-42 | Information Disclosure | Durable baseline boundary | high | mitigate | Preserve recursive allowlists, secret/raw-payload/query-URL rejection, and checked-in evidence empty-diff gates. |
| T-226-43 | Denial of Service | Workflow parser | low | accept | The parser consumes one bounded checked-in workflow and fails on unsupported shapes; it is not network-facing or user-facing. |
| T-226-SC | Tampering | Package supply chain | low | accept | No npm, pip, cargo, package installation, or new dependency is introduced. |
</threat_model>

<verification>
Run the public self-test matrix, canonical validation, complete repository topology contract, Phase 192 verifier, and API declaration gate. Confirm the self-test names and rejects all workflow-identity, lane-removal, scalar-job, job-set, release-matrix, Playwright-matrix, and conditional-lane mutations. Require protected workflow/evidence/ownership artifacts to remain unchanged and every ASVS L1 high-severity mitigation to pass.
</verification>

<success_criteria>
- Collector and both public validation modes reject any run not produced by the exact policy workflow.
- Every eligible run observes and proves the complete policy-required identity set; public removal of any policy-selected required lane fails.
- The versioned policy and complete live workflow have identical job topology, matrix display names/support roles, and conditional live-stripe behavior.
- Topology mutations are policy-selected and cover scalar names, job removal/addition, release matrix support, Playwright breadth, and conditional behavior.
- Stable CI topology, required-check identities, matrices, branch protection, cache topology, Phase 192 artifacts, ownership guidance, privacy boundaries, and current evidence semantics remain unchanged.
- No external capability, provider mutation, package installation, database/schema work, product/UI change, or Phase 227 optimization is introduced.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Durable privacy-safe account that distinguishes critical path, setup ownership, and provider proof state | 226-01–09 | COVERED | Plan 09 closes workflow origin, proof completeness, and policy freshness while preserving the measured account. |
| REQ | BASE-01 | Trustworthy comparable-run facts and durable topology interpretation | 226-09 | COVERED | Exact workflow binding and complete workflow-policy conformance close both BASE-01 gaps. |
| REQ | BASE-02 | Required/skipped/advisory/non-applicable evidence cannot be confused | 226-09 | COVERED | Every eligible run must observe and prove the complete required identity set; conditional/advisory remain non-proof. |
| REQ | OWN-01 | Host/CI Node, browser, and Playwright ownership/diagnostics | 226-03–09 | COVERED | Existing satisfied ownership interface is protected by unchanged-file and Phase 192 regression gates. |
| RESEARCH | — | Metadata-only collector and exact workflow/policy identity | 226-01–09 | COVERED | Collector adds workflow equality without expanding provider access or durable fields. |
| RESEARCH | — | Manifest-owned proof taxonomy and fail-closed unknown/drift handling | 226-04–09 | COVERED | Policy owns complete proof and topology sets; unknown, missing, extra, or changed topology fails. |
| RESEARCH | — | Matrix required/advisory semantics and conditional live-stripe behavior | 226-09 | COVERED | Release/Playwright expansions and event condition are manifest-bound and mutation-tested. |
| RESEARCH | — | Privacy, provider, stable topology, ownership, and Phase 192 preservation | 226-01–09 | COVERED | Existing controls remain required and protected artifacts stay unchanged. |
| CONTEXT | D-01 | Exactly three qualifying attempt-one dispatch runs with full provenance | 226-01–09 | COVERED | Canonical positive gate and unchanged evidence/provenance remain mandatory. |
| CONTEXT | D-02 | Complete typed facts; explicit omissions; no timing estimates | 226-01–09 | COVERED | Workflow and required proof completeness are derived; existing omission/timing checks remain. |
| CONTEXT | D-03 | Privacy-safe normalized root signature only | 226-01–09 | COVERED | Signature and privacy gates remain unchanged and green. |
| CONTEXT | D-04 | Explicit cache evidence required for observed hit | 226-01–09 | COVERED | Cache derivation and canonical data are preserved. |
| CONTEXT | D-05 | Independent policy/conclusion/proof; aggregate fail-closed | 226-09 | COVERED | Complete required observed/proved sets gate aggregates. |
| CONTEXT | D-06 | Provider enforcement is timestamp-scoped, not inferred from YAML | 226-01–09 | COVERED | Topology validates repository taxonomy only; provider snapshot controls remain separate. |
| CONTEXT | D-07 | Versioned checked-in policy; unknown identities reject | 226-09 | COVERED | Policy expands to exact workflow topology and public workflow/proof identity checks. |
| CONTEXT | D-08 | Only confirmed authenticated 404 is not-found | 226-05–09 | COVERED | Existing provider-status mutations remain green and untouched. |
| CONTEXT | D-09 | README remains the single command-first triage home | 226-03–09 | COVERED | README is protected and no competing guide is added. |
| CONTEXT | D-10 | Ownership matrix remains the maintainer interface | 226-03–09 | COVERED | Ownership artifact and commands remain protected. |
| CONTEXT | D-11 | Exact proof vocabulary; raw evidence remains in Actions | 226-01–09 | COVERED | Vocabulary/privacy controls remain mandatory; no raw data path is added. |
| CONTEXT | D-12 | Safe repository-local Bash/gh/jq scope | 226-09 | COVERED | Only policy and local Bash/jq contract files change. |

Deferred Phase 227 optimization, topology/cache changes, and branch-protection work remain excluded. The three spec-less unclassified edge items and descriptor-less prohibitions remain explicit, unresolved, and flagged-unverified; none is silently dismissed or assigned a fabricated check descriptor.

<api_coverage>
Plan 226-09 does not alter the existing GitHub API capability surface. It adds no endpoint, method, credential, SDK, network prerequisite, or provider write. The current `COVERAGE.md` no-integration declaration remains accurate and is intentionally not rewritten.
</api_coverage>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-09-SUMMARY.md` when done.
</output>

---
phase: 226-ci-baseline-proof-semantics
plan: "11"
type: execute
wave: 11
depends_on:
  - 226-10
files_modified:
  - scripts/ci/capture_ci_baseline.sh
  - scripts/ci/verify_ci_baseline_contract.sh
  - scripts/ci/ci_baseline_canonical_schema.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
autonomous: true
gap_closure: true
requirements:
  - BASE-01
  - BASE-02
  - OWN-01
must_haves:
  truths:
    - "BASE-01 / D-02 / D-03 / D-11: Canonical validation composes the strict semantic predicate with the recursive type tree, requires all four durable privacy collection flags to be false, and rejects each true-flag mutation through public --input validation."
    - "BASE-01 / D-07: Canonical policy_manifest.schema_version and workflow exactly equal the checked-in policy, and deliberately refreshed canonical evidence carries the current policy fields."
    - "BASE-01 / D-07: Repository validation derives every release-gate and Playwright matrix-expanded display name from live .github/workflows/ci.yml and compares the exact multisets to policy.workflow_topology[].display_names."
    - "BASE-02 / D-01 / D-05 / D-06: Collector output records the provider run branch/ref, and only an attempt-one successful workflow_dispatch from main can become eligible or publish proved release lanes alongside the main provider snapshot."
    - "BASE-01 / BASE-02 / D-01 / D-07: Canonical shape is checked against a fixed-path versioned schema contract independent of candidate evidence, while exactly-three membership is checked against a fixed-path cohort binding produced only by deliberate live publication."
    - "BASE-01: Collector record IDs for runs, jobs, and artifacts are non-negative integers; fractional public mutations fail closed."
    - "OWN-01 / D-09 / D-10: Existing command-first ownership and host/browser diagnostic contracts remain green and unchanged while the evidence trust boundary is repaired."
  artifacts:
    - path: "scripts/ci/capture_ci_baseline.sh"
      provides: "Allowlisted run branch/ref plus main-scoped eligibility and proof derivation"
    - path: "scripts/ci/verify_ci_baseline_contract.sh"
      provides: "Composed canonical privacy/policy checks, exact live display-name derivation, branch/ref semantics, integer-ID schema, and public negative controls"
    - path: "scripts/ci/ci_baseline_canonical_schema.json"
      provides: "Versioned exact canonical and cohort-binding shape/type contract loaded from one repository-fixed path, never selected by candidate input"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json"
      provides: "Trusted exactly-three eligible run-ID and ref/SHA/workflow/lockfile provenance binding emitted by the deliberate live publication path"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json"
      provides: "Current-policy, privacy-safe, exactly-three-run main-scoped canonical evidence"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Human-readable provenance for the refreshed main-scoped cohort and retired phase-branch cohort"
  key_links:
    - from: "GitHub Actions run head_branch/ref"
      to: "run.eligible and jobs[].proof_state"
      via: "one shared main-scope predicate before proved-lane derivation"
      pattern: "head_branch|ref|main|eligible|proof_state"
    - from: "canonical policy_manifest and privacy"
      to: "checked-in policy, fixed canonical schema contract, and public validator result"
      via: "strict semantic predicate composed with candidate-independent recursive schema validation"
      pattern: "ci_baseline_canonical_schema|policy_manifest|privacy|type_tree"
    - from: ".github/workflows/ci.yml release and Playwright matrices"
      to: "policy.workflow_topology[].display_names"
      via: "normalized exact multiset derivation and comparison"
      pattern: "display_names|release-support|playwright-shards"
    - from: "three fresh main workflow_dispatch runs"
      to: "canonical cohort, aggregates, and Phase 227 selection inputs"
      via: "live-only publication creates a trusted cohort binding, validates temporary evidence against it, then publishes binding/JSON/Markdown together"
      pattern: "226-CI-BASELINE-COHORT|eligible_run_ids|aggregates|phase_227_selection_gate"
  prohibitions:
    - "[flagged/unverified] Durable evidence must not contain downloaded logs, artifact archives, environment values, raw payloads, query-bearing URLs, secrets, or diagnostic excerpts."
    - "[flagged/unverified] A phase-branch run must not be paired with the main provider snapshot and represented as eligible release proof."
    - "[flagged/unverified] Closure must not alter CI job topology, required-check identity, matrix breadth, branch protection, cache topology, Phase 192 artifacts, product behavior, or UI behavior."
---

<objective>
Close all four current verification blockers as one production-quality evidence-authentication slice: bind run scope, canonical privacy/policy freshness, and generated workflow display names before publishing refreshed proof.

Purpose: Restore a durable Phase 226 baseline that maintainers and Phase 227 can trust without confusing phase-branch observations, stale policy, or asserted privacy with main-scoped release proof.
Output: Main-scoped collector semantics, composed public canonical validation, exact matrix-display topology validation, deliberately regenerated canonical JSON/Markdown, and adversarial public controls.
</objective>

## Artifacts this phase produces

| Path | New or modified symbols | Contract produced |
| --- | --- | --- |
| `scripts/ci/capture_ci_baseline.sh` | `build_run` raw allowlist gains `head_branch` and normalized `ref`; `$base_eligible` gains exact `main` scope; exclusion reason names ref mismatch; output privacy allowlist names ref provenance | Provider ref is retained, and main-scoped provider enforcement cannot authorize proof for another branch. |
| `scripts/ci/verify_ci_baseline_contract.sh` | collector-only test mode; shared branch/ref eligibility predicate; fixed-path schema and cohort-binding loaders; composed strict canonical/type-tree predicate; checked-in policy field equality; integer run/job/artifact ID predicates; deliberate live publication mode; release/Playwright display-name derivation; named schema-selection, cohort-selection, privacy, stale-policy, topology, feature-branch, and fractional-ID mutations | Intermediate collector work no longer reads stale canonical evidence; public validation cannot accept candidate-selected schema or cohort IDs; repository topology validation fails closed at the reported gaps. |
| `scripts/ci/ci_baseline_canonical_schema.json` | versioned recursive key/type/nullability/enumeration contract for canonical evidence and the cohort binding | Canonical shape authority is immutable during a measurement refresh and independent of the candidate or currently published evidence. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json` | publication version, exactly three eligible run IDs, and per-run main ref/SHA/workflow/six-lockfile provenance | Fixed cohort membership is a trusted publication artifact rather than historical IDs embedded in validator code or values selected by candidate JSON. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` | refreshed `policy_manifest`; run `head_branch`/`ref`; new main-scoped `anchor`, `cohort`, `runs`, `aggregates`, and `phase_227_selection_gate` facts | Exactly three fresh qualifying main dispatches become the canonical comparable cohort only after the repaired validator accepts the complete document. |
| `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` | refreshed cohort/provenance/timing/proof tables and explicit retired-cohort note | Maintainers can see why phase-branch runs are not main release proof and can trace the current canonical evidence without raw diagnostics. |

No new workflow job, required-check identity, matrix cell, branch-protection rule, cache, dependency, API endpoint, database/schema object, product behavior, or UI symbol is produced. The two new JSON files are repository-local validation/publication contracts, not application schemas or external capabilities. `COVERAGE.md` remains authoritative: this plan adds no new network endpoint, SDK, credential, or external capability; it only retains an allowlisted field from the already-consumed Actions run response and uses the existing collector/`gh` refresh path.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-10-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/COVERAGE.md
@scripts/ci/capture_ci_baseline.sh
@scripts/ci/verify_ci_baseline_contract.sh
@scripts/ci/ci_baseline_workflow_policy.json
@.github/workflows/ci.yml
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
@scripts/ci/verify_phase192_ci_contract.sh

<interfaces>
- `capture_ci_baseline.sh::build_run` currently reduces the Actions run response into `{id, workflow, event, head_sha, attempt, status, conclusion, created_at, completed_at, url}` and computes `$base_eligible` before proof states.
- `candidate_job_semantics`, `candidate_workflow_semantics`, `candidate_required_proof_completeness`, and `candidate_derived_run_semantics` are shared candidate-authentication predicates used by public collector-record and canonical modes.
- `validate_canonical_cohort` currently defines a strict `canonical` predicate, redefines it as recursive type-tree equality against mutable `226-CI-BASELINE.json`, and calls `validate_aggregates` backed by `expected_ids`; the repaired validator must instead compose semantics with a fixed-path schema contract and a fixed-path cohort publication binding.
- `validate_workflow_topology` has matrix roles `release-support` and `playwright-shards`; policy already carries all expected expanded names, while the live workflow has four release include rows and shards `[1, 2, 3]`.
- `validate_input PATH` is the public boundary for both `ci-baseline-collector-record` and `ci-baseline-canonical`; it must load schema/cohort authority only from repository-fixed paths. A separate live-only publication command owns staged cohort creation and pre-publication validation.
</interfaces>
</context>

<assumptions>
- No phase SPEC or UI-SPEC exists. The spec-less edge probe left BASE-01, BASE-02, and OWN-01 unclassified/unresolved, so this plan treats the ROADMAP goal, REQUIREMENTS text, D-01 through D-12, and the current user-modified `226-VERIFICATION.md` gaps as the complete binding contract.
- Current verification's main-scope requirement is a changed assumption relative to research that used dedicated phase branches. For this closure, `main` is authoritative because provider enforcement is fetched from `/branches/main`; the eligible cohort is refreshed to exactly three new green attempt-one main dispatches under D-01, and the superseded phase-branch cohort remains recoverable in git history and is named in the Markdown provenance note rather than represented as release proof.
- The three new dispatches share one provider-reported main SHA and one workflow/six-lockfile blob snapshot. If any candidate fails, is rerun, changes SHA/ref, or is not comparable, retain its run URL as diagnostic evidence and halt; do not replace it or publish a partial cohort per D-01.
- OWN-01 is substantively satisfied, but remains listed because the spec-less probe could not classify it; existing ownership routes and Phase 192 checks are regression gates, not a request for new documentation.
- The fractional scalar-ID warning belongs in this closure because the collector-record schema is already being edited at the same public trust boundary; integer predicates and focused public mutations close it without changing the evidence vocabulary or phase boundary.
- The schema contract is stable across evidence refreshes and changes only with a deliberate schema-version change. The cohort binding changes with each deliberate publication, but candidate JSON cannot name, embed, override, or select either authority; ordinary `--input` always uses the checked-in fixed paths.
- No schema-relevant application file is modified, so no schema-push task applies. No package-manager install occurs, so no package-legitimacy checkpoint applies.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one run ref through collection and fail feature-branch proof closed</name>
  <reversibility rating="costly">D-01/D-05 eligibility and proof semantics are consumed by the checked-in cohort and Phase 227, so changing ref scope later requires coordinated evidence and validator regeneration.</reversibility>
  <files>scripts/ci/capture_ci_baseline.sh, scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` fourth gap, Truths 1/2/5, run-branch key-link failure, and feature-branch negative probe.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` D-01 exact cohort/provenance, D-05 fail-closed proof, D-06 provider-snapshot scope, D-07 policy authority, and D-11 vocabulary.
    - `scripts/ci/capture_ci_baseline.sh` `build_run`, raw run response allowlist, `$base_eligible`, proof-state mapping, exclusion reason, and `/branches/main` provider calls.
    - `scripts/ci/verify_ci_baseline_contract.sh` argument parsing, collector `run` predicate, `candidate_job_semantics`, completeness checks, semantic fixture builders, public mutation helpers, and every place `--self-test` currently reads the checked-in canonical evidence.
    - `scripts/ci/ci_baseline_workflow_policy.json` workflow and required/advisory/conditional lane roles.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` current run/proof shape; read as evidence to replace after Task 1 semantics pass, not as proof of correct ref scope.
  </read_first>
  <behavior>
    - A fixture run from `main` records both provider branch and normalized full ref, can become eligible when all existing timing/status predicates pass, and can mark successful required lanes `proved`.
    - The otherwise identical feature-branch fixture records its branch/ref but is ineligible, carries a branch-specific exclusion reason, and contains zero `proved` lanes even though event, attempt, status, and conclusion are otherwise eligible.
    - Collector-record public mode requires recorded branch/ref to be internally consistent and exact `main` scope before any eligible/proved state; canonical enforcement waits for Task 2's schema/binding refresh.
    - A collector-only self-test exercises fixture collection and public collector-record validation without loading, shape-checking, or semantically validating checked-in canonical evidence.
    - Main rules/classic-protection facts remain a timestamped provider snapshot per D-06 and are never inferred from workflow YAML or copied into a phase-branch proof claim.
  </behavior>
  <action>Begin with a RED fixture copied from the valid semantic collector fixture, adding provider `head_branch` for the positive main case and changing only that value for the negative feature-branch case. Extend the provider response shape check to require a nonempty string branch. In `build_run`, carry the allowlisted branch as `head_branch`, derive `ref` as `refs/heads/` plus that exact branch, and include exact main scope in `$base_eligible` before job proof states are derived. Make the ineligible reason distinguish ref-scope failure from event/attempt/status/conclusion failure.

Add one shared branch/ref semantic predicate for collector-record validation after its exact-schema check and before job proof/completeness validation. Require `head_branch == "main"`, `ref == "refs/heads/main"`, and exact consistency for eligible records; permit non-main values only on ineligible diagnostic records with zero proved lanes and a closed exclusion reason. Add the two fields to the collector-record schema and every collector positive fixture. Add `--self-test-collector` as a narrow deterministic mode that runs only collector fixtures and fresh-subprocess `--input` collector-record controls; it must not call `validate_canonical_cohort`, `validate_aggregates`, repository validation that opens canonical evidence, or the legacy full `--self-test` path. Exercise a feature-branch collector fixture and collector-record public mutation through this mode. Defer the canonical run schema, canonical branch predicate, and full self-test assertion until Task 2 can create the trusted schema/binding and refreshed canonical files in one task. Keep D-01 SHA/blob provenance, D-02 timing omissions, D-03 signature normalization, D-04 cache semantics, D-06/D-08 provider response states, and D-11 privacy intact.</action>
  <acceptance_criteria>
    - Collector output records exact `head_branch` and `ref` values from provider metadata without retaining the raw response.
    - Main is part of base eligibility before proof-state derivation; a successful attempt-one feature-branch fixture is ineligible and has zero proved lanes.
    - Public collector-record validation rejects a feature-branch record changed to eligible/proved and accepts the equivalent correctly ineligible diagnostic.
    - `--self-test-collector` passes without reading or validating either checked-in canonical evidence file, so Task 1 remains executable while those files retain the old shape and cohort.
    - Provider snapshot endpoints and their `main` scope remain unchanged; workflow YAML and branch protection have no diff.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test-collector &amp;&amp; git diff --exit-code -- .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md .github/workflows/ci.yml scripts/ci/ci_baseline_workflow_policy.json scripts/ci/verify_phase192_ci_contract.sh</automated>
  </verify>
  <done>One real metadata path carries provider ref through collection and validation, and feature-branch observations fail closed before main-scoped release proof can be published.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish trusted canonical contracts and publish a bound current main cohort</name>
  <precondition>`gh auth status` succeeds with Actions read/dispatch access to `szTheory/accrue`, and remote `main` can run `.github/workflows/ci.yml`; otherwise halt before dispatch or evidence edits.</precondition>
  <reversibility rating="costly">D-01 and D-07 make the refreshed cohort IDs, ref/SHA provenance, and current policy snapshot durable inputs to Phase 227; changing them later requires a deliberate remeasurement and publication cycle.</reversibility>
  <files>scripts/ci/verify_ci_baseline_contract.sh, scripts/ci/ci_baseline_canonical_schema.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` first and second gaps, canonical true-flag/stale-policy reproductions, and fractional-ID warning.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` D-01 cohort replacement rules, D-02 omissions, D-03/D-11 privacy boundary, D-05 proof states, D-06 snapshot wording, and D-07 versioned policy authority.
    - `scripts/ci/verify_ci_baseline_contract.sh` top-level `expected_ids`, `validate_aggregates`, `validate_canonical_cohort`, both `def canonical` declarations, `validate_input`, the Task 1 collector-only mode, and public self-test helpers.
    - `scripts/ci/capture_ci_baseline.sh` Task 1 branch/ref output and existing fixture/live collection CLI.
    - `scripts/ci/ci_baseline_workflow_policy.json` current `schema_version` and `workflow`.
    - `scripts/ci/ci_baseline_canonical_schema.json` new fixed-path schema authority to create from the documented canonical contract, not from a candidate evidence document.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json` new fixed-path publication binding to create only from three operator-fixed live run IDs after recollection.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json` complete canonical shape, historical cohort/provenance, aggregates, privacy object, and Phase 227 selection gate.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` human evidence tables and contrary critical-path result that regeneration must preserve or recompute from measurements.
  </read_first>
  <behavior>
    - The strict canonical object/semantic predicate and recursive schema predicate both execute; neither name shadows the other and neither derives shape from mutable canonical evidence.
    - A versioned checked-in schema contract at the repository-fixed path owns exact canonical and cohort-binding keys, scalar types, nullability, and enums; candidate JSON cannot embed or select a schema path/version/reference.
    - Canonical privacy requires `logs_downloaded`, `artifact_archives_downloaded`, `env_values_recorded`, and `raw_payloads_recorded` all equal false, and four isolated public true-flag mutations fail.
    - Canonical `policy_manifest.schema_version` and `workflow` exactly equal the checked-in policy; a stale-version public mutation fails.
    - Collector-record run/job/artifact IDs are integers, matching canonical scalar boundaries; isolated fractional-ID public mutations fail.
    - Exactly three fresh green attempt-one main dispatches at one main SHA and comparable workflow/six-lockfile blobs form the eligible cohort; one fixed-path binding records their IDs and provenance, and canonical cohort/aggregate/selection IDs must equal that binding exactly.
    - Ordinary public `--input` validation always uses the checked-in schema and cohort-binding paths; coherent mutation of all candidate-owned ID arrays still fails, and unknown schema/binding override arguments fail closed.
    - A live-only publication mode takes exactly three operator-fixed `--run-id` arguments, recollects them directly through the non-fixture collector, constructs the staged binding itself, constructs/validates temporary canonical JSON against that binding and the fixed schema, and renders Markdown before any checked-in publication file is replaced.
  </behavior>
  <action>Start RED controls against the public `--input` boundary: independently flip each of the four canonical privacy collection booleans, decrement the canonical policy schema version, set one collector-record run ID, job ID, and artifact ID to a fractional value, and coherently replace one eligible run ID everywhere candidate JSON controls it (`runs`, `cohort`, aggregate proof, and every Phase 227 selection row). Also prove that candidate-added schema/binding fields are rejected by exact keys and that `--input ... --schema ...`, `--cohort-binding ...`, or equivalent override arguments are rejected as unknown; no public candidate may choose its authority.

Create `scripts/ci/ci_baseline_canonical_schema.json` as the versioned trusted schema/type contract. Encode the exact canonical and cohort-binding recursive keys, scalar types, integer constraints, nullability, and enumerations required by current D-01/D-02/D-03/D-05/D-07/D-11 semantics. Load it only through a root-relative constant in the verifier; do not accept a path or schema version from candidate JSON, environment variables, or ordinary CLI arguments. Rename the strict canonical predicate so it cannot be overwritten, replace type-tree comparison to checked-in `226-CI-BASELINE.json` with comparison to this schema contract, and require both independent predicates. Bind canonical policy schema/workflow directly to the slurped checked-in workflow policy. Remove top-level `expected_ids` and every historical-ID assertion from aggregate/canonical validation. Instead, load the fixed checked-in `226-CI-BASELINE-COHORT.json` for ordinary `--input`, require its own schema-contract validity and exactly three unique positive integer IDs, require each bound record to carry `head_branch=main`, `ref=refs/heads/main`, head SHA, workflow blob OID, and all six named lockfile blob OIDs, and require exact ID/provenance equality across binding, `.cohort`, eligible `.runs`, aggregate proof/per-run rows, and every Phase 227 selection candidate. Tighten collector-record ID predicates to the existing non-negative integer helper without changing JSON number encoding.

After Task 1 and the new controls pass against fixtures, record the remote main SHA, workflow blob OID, and six D-01 lockfile blob OIDs. Dispatch exactly three fresh `workflow_dispatch` runs from that same `main` SHA, record every dispatched run URL immediately, and wait for all three without rerunning or replacing a failed candidate. Require provider `head_branch=main`, exact head SHA, attempt 1, success/completion, and exact blob equality before collection. If any condition fails, halt and leave the checked-in cohort binding, canonical JSON, and Markdown untouched.

Implement a distinct live-only `--prepare-publication --run-id ID --run-id ID --run-id ID --output-dir DIR` path. It must reject fixture mode, duplicates, any count other than three, and pre-existing/nonempty output destinations; invoke `capture_ci_baseline.sh` itself for exactly those operator-fixed IDs; validate the resulting collector record; derive the staged cohort binding from those collected provider facts rather than from candidate canonical fields; then build staged canonical JSON with current policy fields and Task 1 branch/ref fields, recomputing cohort IDs, provenance, all timing/proof aggregates, and Phase 227 selection inputs solely from the validated collector record. Validate the staged binding against the fixed schema, validate the staged canonical against that binding, render staged Markdown only from the validated staged JSON, and emit all three staged publication files only after every check succeeds. The ordinary `--input` path remains fixed to the checked-in binding; the live publication path is the sole narrowly scoped exception and never accepts an arbitrary schema or binding file.

After the prepare command succeeds, replace `226-CI-BASELINE-COHORT.json`, `226-CI-BASELINE.json`, and `226-CI-BASELINE.md` from the staged set as one rollback-safe publication operation; save same-directory backups, restore all three on any move or post-publication full-validator failure, and remove backups only after ordinary fixed-path `--input` plus full self-test pass. In Markdown, name the new main-scoped cohort and explain that the prior phase-branch cohort IDs are superseded for release proof but remain auditable in git history; do not copy logs, traces, artifact bodies, env values, payloads, or query-bearing URLs. Preserve the measured contrary-result wording if still supported, otherwise update it to the new exact measured result. Do not hand-edit IDs, binding facts, schema expectations, or aggregates merely to satisfy the validator.</action>
  <acceptance_criteria>
    - Each canonical privacy true-flag mutation and the stale-policy mutation exits non-zero through public validation; the untouched current-policy canonical document exits zero.
    - Collector-record fractional run/job/artifact IDs each exit non-zero, closing the warning without changing the schema vocabulary.
    - The fixed schema contract, not checked-in or candidate canonical evidence, supplies recursive shape/type authority; public candidates cannot select or override it.
    - Exactly three fresh main attempt-one successful dispatches share exact ref/SHA/workflow/six-lockfile provenance and are the only eligible/proved cohort members.
    - The fixed-path cohort binding contains those exactly three live-collected IDs/provenance facts, and coherent candidate-owned ID substitution fails against it without historical IDs embedded in validator code.
    - `226-CI-BASELINE.json` carries current policy schema/workflow plus branch/ref for each run, and staged binding, canonical aggregates, and Markdown all validate before publication.
    - `226-CI-BASELINE.md` is regenerated from the validated JSON, identifies the current cohort/proof scope, and records the retired phase-branch cohort without treating it as main release proof.
    - A failed collection, comparability check, schema/binding check, staged validator, move, or post-publication validator leaves or restores all three checked-in publication files unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh --input .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &amp;&amp; jq -e --slurpfile policy scripts/ci/ci_baseline_workflow_policy.json --slurpfile cohort .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json '(.policy_manifest == ($policy[0] | {schema_version,workflow})) and (.privacy.logs_downloaded == false) and (.privacy.artifact_archives_downloaded == false) and (.privacy.env_values_recorded == false) and (.privacy.raw_payloads_recorded == false) and ([.runs[] | select(.run.eligible) | .run.id] | sort == ($cohort[0].eligible_run_ids | sort)) and ([.runs[] | select(.run.eligible) | .run.head_branch] == ["main","main","main"]) and ([.runs[] | select(.run.eligible) | .run.ref] == ["refs/heads/main","refs/heads/main","refs/heads/main"])' .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &amp;&amp; ! rg -n '31322443304|31332551817|31344524124|expected[_]ids' scripts/ci/verify_ci_baseline_contract.sh</automated>
  </verify>
  <done>The checked-in account is current-policy, privacy-false, integer-typed, schema-authenticated independently of candidate evidence, and bound to exactly three deliberately published main-scoped runs before its cohort binding, JSON, and Markdown become authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive every matrix display name from live workflow topology</name>
  <reversibility rating="costly">D-07 makes generated display-name interpretation part of the trusted workflow-policy boundary; later YAML syntax changes require coordinated extractor, policy, and drift-control review.</reversibility>
  <files>scripts/ci/verify_ci_baseline_contract.sh</files>
  <read_first>
    - `.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md` third gap, no-op display-name loop, Floor-to-Foundation reproduction, and topology key-link failure.
    - `.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md` D-05 proof vocabulary, D-07 checked-in manifest authority, and the stable topology/matrix discretion boundary.
    - `.github/workflows/ci.yml` complete `release-gate` name/include matrix/support values and `playwright-e2e` name/shard strategy.
    - `scripts/ci/ci_baseline_workflow_policy.json` `workflow_topology` rows for `release-support`, `playwright-shards`, scalar, and conditional roles.
    - `scripts/ci/verify_ci_baseline_contract.sh` `job_body`, `validate_policy_manifest`, `validate_workflow_topology`, `validate_repository_contract`, workflow-copy mutation helpers, and Task 1/2 preservation tests.
    - `scripts/ci/ci_baseline_canonical_schema.json` and `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE-COHORT.json` Task 2 trust sources that the final full self-test must preserve and consume from fixed paths.
  </read_first>
  <behavior>
    - Repository validation parses every live release include row's compatibility, elixir, otp, sigra, opentelemetry, and support values, renders all four concrete job names using the live name expression, and compares the exact sorted multiset to policy display names.
    - Repository validation parses every live Playwright shard value, derives the denominator from the configured shard set/job-total semantics, renders every `n/N` display name, and compares the exact sorted multiset to policy display names.
    - Scalar and conditional topology rows continue to require their exact single display name, so every policy `display_names` entry is compared rather than merely iterated.
    - A release compatibility-value mutation and a Playwright shard-value mutation each fail first against isolated workflow copies; missing, duplicate, partial, or unsupported matrix syntax also fails closed.
    - The validator never changes live YAML, policy values, job identity, matrix breadth, support role, `continue-on-error`, condition, or dependency topology.
  </behavior>
  <action>Replace the `release-support` no-op display-name loop with deterministic extraction from the selected job body. Parse every existing include row as one closed tuple containing compatibility, elixir, otp, sigra, opentelemetry, and support; reject incomplete/duplicate keys or unsupported row syntax. Render the live job-name expression for each tuple, including the advisory suffix only for advisory support, sort it, and require exact multiset equality with the selected policy row's `display_names`. Retain the separate three-required/one-advisory and `continue-on-error` assertions as defense in depth.

For `playwright-shards`, parse the complete shard list, reject non-integer/duplicate/empty values, derive total from the list under the current strategy job-total contract, render every live display name, and compare the exact sorted multiset to policy. For scalar/conditional rows, assert policy display names are exactly the already-parsed live job name singleton. Add fail-first self-tests using isolated `ci_file` copies: change the policy-selected release compatibility value `Floor` to a distinct public value and change one Playwright shard value while preserving valid YAML. Each must fail specifically because the derived display-name multiset differs; keep existing job-set/name/condition/support mutations green. Do not edit `.github/workflows/ci.yml` or the policy to accommodate a mutated fixture.</action>
  <acceptance_criteria>
    - Every `workflow_topology[].display_names` value is reached by a live derivation and exact comparison; no role contains an empty/no-op loop.
    - All four release names, including the advisory suffix, derive from complete live matrix tuples and exactly equal policy.
    - All three Playwright shard names derive from the live shard configuration/job-total contract and exactly equal policy.
    - Isolated release compatibility and Playwright shard mutations each exit non-zero while untouched repository topology passes.
    - Full Phase 226 self-test, refreshed canonical validation, Phase 192 contract, API coverage declaration, and protected topology/ownership checks pass together.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_ci_baseline_contract.sh --self-test &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh --input .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.json &amp;&amp; bash scripts/ci/verify_ci_baseline_contract.sh &amp;&amp; bash scripts/ci/verify_phase192_ci_contract.sh &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query check.api-coverage-verify-pre 226 | jq -e '.passed == true' &amp;&amp; git diff --check &amp;&amp; git diff --exit-code -- .github/workflows/ci.yml scripts/ci/ci_baseline_workflow_policy.json scripts/ci/verify_phase192_ci_contract.sh scripts/ci/README.md .planning/phases/226-ci-baseline-proof-semantics/226-SETUP-OWNERSHIP.md</automated>
  </verify>
  <done>Repository validation derives and compares every policy display name from live scalar or matrix topology, and both reported matrix drift mutations fail closed without changing CI.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 posture and block threshold

Security enforcement uses OWASP ASVS Level 1. Every critical/high threat is mitigated; any unresolved high-severity result blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| GitHub run metadata → collector | Provider branch/SHA/status facts are untrusted until reduced through the exact allowlist and eligibility predicate. |
| Candidate JSON → public validator | A candidate can spoof privacy, policy freshness, ref scope, identifiers, or proof facts unless independently checked. |
| Fixed schema/cohort authority → public validator | Candidate-controlled paths, embedded contracts, or coherent ID rewrites could authorize fabricated evidence unless authority is repository-fixed and separately published. |
| Live workflow YAML → policy manifest | Matrix values can change rendered check identities while a stale policy appears structurally valid. |
| Validated canonical evidence → maintainer/Phase 227 | Published proof authorizes diagnosis and optimization selection, so stale or cross-scope evidence has downstream safety impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-226-49 | Spoofing | Canonical privacy claims | high | mitigate | Compose strict canonical semantics with recursive shape validation, require all four collection flags false, and run four public true-flag mutations. |
| T-226-50 | Tampering | Policy snapshot | high | mitigate | Compare canonical schema/workflow to the checked-in manifest and reject stale-version input before aggregates or publication. |
| T-226-51 | Tampering | Workflow topology display names | high | mitigate | Derive exact release and Playwright display-name multisets from live YAML and retain fail-first matrix mutations. |
| T-226-52 | Elevation of Privilege | Branch/protection proof scope | high | mitigate | Carry provider branch/ref, require main before eligibility/proved states, and reject an otherwise-successful feature-branch fixture. |
| T-226-53 | Repudiation | Evidence regeneration | high | mitigate | Record dispatched URLs immediately, require exact SHA/blob provenance, validate the complete staged publication set, use rollback-safe replacement with post-publication validation, and preserve retired cohort provenance in Markdown/git history. |
| T-226-56 | Spoofing | Candidate schema and cohort membership | high | mitigate | Load the versioned schema and ordinary cohort binding only from fixed repository paths; reject schema/binding fields and CLI overrides; require staged binding creation from exactly three operator-fixed IDs recollected live; reject coherent candidate ID substitution. |
| T-226-54 | Information Disclosure | Checked-in baseline | high | mitigate | Retain metadata-only allowlists, false collection flags, sensitive/query/raw-payload rejection, and Actions-owned diagnostic content. |
| T-226-55 | Denial of Service | Matrix parser and three-run refresh | low | accept | Inputs are bounded checked-in YAML and three explicit provider runs; unsupported syntax or failed candidates halt without publishing. |
| T-226-SC | Tampering | Package supply chain | low | accept | No npm, pip, cargo, package installation, or new dependency is introduced. |
</threat_model>

<verification>
Run Task 1's collector-only self-test before any canonical refresh. After Task 2 publication, run the public full self-test, refreshed canonical input, repository topology contract, Phase 192 verifier, and existing API coverage gate. Confirm named public mutations reject candidate-selected schema/binding authority, coherent cohort-ID substitution, all four privacy flags, stale policy version, feature-branch eligible/proved state, fractional run/job/artifact IDs, release compatibility drift, and Playwright shard drift. Confirm the schema source is independent of mutable evidence, the published binding contains exactly three main-scoped attempt-one successful comparable runs, and staged binding/JSON/Markdown validation precedes rollback-safe publication. Require empty diffs for workflow YAML, policy, branch-protection-facing files, ownership/README, and Phase 192 artifacts.
</verification>

<success_criteria>
- Canonical privacy and current-policy predicates execute together with recursive exact-shape validation.
- Recursive shape/type authority comes from the fixed versioned schema contract, and ordinary cohort membership comes from the separately published fixed-path binding; candidate input selects neither.
- Every privacy true flag and stale policy snapshot fails through the public input path.
- Collector and validators record branch/ref and prohibit feature-branch evidence from satisfying main-scoped release proof.
- Exactly three operator-fixed, live-recollected main runs with exact SHA/workflow/lockfile provenance replace the superseded eligible cohort through binding-first validation-before-publish regeneration, without historical-ID constants in validator code.
- Every policy display name is derived from live scalar/release/Playwright topology, and both matrix drift probes fail.
- Collector record run/job/artifact IDs reject fractional values.
- OWN-01 diagnostics, Phase 192 artifacts, stable CI identities/topology/matrix breadth, branch protection, caches, products, and UI do not regress.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Durable privacy-safe comparable-run account distinguishing critical path, setup ownership, and provider proof | 226-01–11 | COVERED | Plan 11 authenticates the remaining privacy, policy, topology, and ref-scope boundaries and republishes measured evidence. |
| REQ | BASE-01 | Durable privacy-safe timing/cache/setup/provider/signature baseline across comparable runs | 226-11 Tasks 1–3 | COVERED | Candidate-independent schema authority, separately bound main cohort regeneration, current-policy privacy validation, integer IDs, and live topology close all BASE-01 findings. |
| REQ | BASE-02 | Required/skipped/advisory evidence cannot be confused with release proof | 226-11 Tasks 1–2 | COVERED | Main scope is authenticated before eligible/proved state and cross-branch proof fails closed. |
| REQ | OWN-01 | Host/CI Node, browser, and Playwright ownership diagnostics | 226-03–11 regression gates | COVERED | Satisfied interface is preserved through unchanged ownership/README and Phase 192 checks; explicit due unresolved spec-less classification. |
| RESEARCH | — | Exactly three comparable first-attempt dispatches with explicit provenance | 226-11 Task 2 | COVERED | Changed main-scope assumption triggers a deliberate live-only three-run refresh whose fixed membership and exact SHA/blob provenance are published in a separate trusted binding. |
| RESEARCH | — | Metadata-only allowlist and raw evidence retained in Actions | 226-11 Tasks 1–2 | COVERED | Branch/ref is allowlisted metadata; four false privacy flags and sensitive-content gates remain mandatory. |
| RESEARCH | — | Versioned manifest owns proof taxonomy and workflow topology | 226-11 Tasks 2–3 | COVERED | Canonical policy fields equal current manifest and every generated display name is derived from live YAML. |
| RESEARCH | — | Provider enforcement and repository proof taxonomy remain distinct | 226-11 Tasks 1–3 | COVERED | Main provider snapshot is ref-bound; YAML validation does not claim external enforcement. |
| RESEARCH | — | Setup ownership and contrary measured critical-path result remain maintainer-legible | 226-03–11 | COVERED | Markdown is regenerated from measured facts while ownership artifacts are protected. |
| CONTEXT | D-01 | Exactly three qualifying green attempt-one dispatches with ref/SHA/workflow/lockfile provenance; no replacements | 226-11 Tasks 1–2 | COVERED | Three fresh main candidates are operator-fixed before waiting, recollected live by the publication command, and bound separately from candidate JSON; failure halts and the eligible cohort is not partially published. |
| CONTEXT | D-02 | Complete queue/signature/cache/setup facts with typed omissions and no estimates | 226-01–11 regression gates | COVERED | Regeneration uses collector facts and full existing derived-fact validation. |
| CONTEXT | D-03 | Privacy-safe normalized signature without raw diagnostics | 226-10–11 regression gates | COVERED | Existing derived signature and privacy controls remain green during refresh. |
| CONTEXT | D-04 | Conservative explicit cache semantics | 226-01–11 regression gates | COVERED | Collector algorithm and canonical derived validation are preserved. |
| CONTEXT | D-05 | Independent policy/conclusion/proof and fail-closed aggregate | 226-11 Tasks 1–2 | COVERED | Main scope precedes proof derivation; aggregates consume only validated eligible runs. |
| CONTEXT | D-06 | Timestamped provider snapshot is not workflow enforcement | 226-11 Tasks 1–3 | COVERED | Snapshot remains main-scoped provider data; topology remains repository taxonomy. |
| CONTEXT | D-07 | Versioned checked-in policy; unknown or drifted identities reject | 226-11 Tasks 2–3 | COVERED | Exact policy field binding, fixed versioned canonical schema authority, and complete display-name comparisons reject candidate-selected or stale/drifted authority. |
| CONTEXT | D-08 | Only confirmed authenticated 404 means not-found | 226-05–11 regression gates | COVERED | Existing provider-error self-tests remain mandatory and endpoints do not change. |
| CONTEXT | D-09 | README remains the single command-first triage home | 226-03–11 regression gates | COVERED | No new guide is created and README is protected. |
| CONTEXT | D-10 | Ownership matrix remains the maintainer interface | 226-03–11 regression gates | COVERED | Existing matrix and host scripts remain unchanged and verified. |
| CONTEXT | D-11 | Precise proof vocabulary and Actions-owned raw evidence | 226-11 Tasks 1–2 | COVERED | Main/retired scope is explicit and regeneration persists metadata only. |
| CONTEXT | D-12 | Bounded repository-local Bash/gh/jq scope; no app/API/schema/UI | 226-11 | COVERED | New files are repository-local evidence validation/publication contracts only; no application schema, endpoint, dependency, or destructive recovery is introduced. |

Deferred Phase 227 optimization and all workflow topology, required-check, matrix breadth, branch-protection, cache, product, and UI changes are excluded. The fractional-ID warning is intentionally included because it shares the edited public schema boundary; no source item remains unplanned.

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-11-SUMMARY.md` when done.
</output>

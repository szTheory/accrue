---
phase: 226-ci-baseline-proof-semantics
plan: "13"
type: execute
wave: 7
depends_on: ["226-12"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/ci_setup_diagnostic.sh
  - scripts/ci/accrue_host_uat.sh
  - scripts/ci/verify_ci_setup_diagnostics.sh
  - examples/accrue_host/README.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-08/D-09/D-10: Omitted provider evidence always normalizes to non_run, so an ordinary successful push or pull-request run cannot acquire proved status without an explicit proof record."
    - "D-05/D-07: Every declared dependent-job prerequisite has a known completion before DAG-wait arithmetic; incomplete dependency topology is rejected instead of becoming a null timing fact."
    - "D-14/D-16/D-19: The host wrapper preserves a lower-level setup diagnostic and uses an accurately named host-gate fallback only when the delegated gate emitted no setup fact."
    - "All baseline, critical-path, provider, formatter, setup, and inherited required-lane contracts remain green after the three verification gaps close."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Fail-closed provider default and complete-prerequisite validation for direct and live collection"
      exports: ["collectBaseline", "normalizeRun", "normalizeJob"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Regression controls for omitted provider state and unresolved prerequisite topology"
    - path: "scripts/ci/accrue_host_uat.sh"
      provides: "Lower-level setup-fact preservation and precise aggregate host-gate fallback"
    - path: "scripts/ci/ci_setup_diagnostic.sh"
      provides: "Stable host_gate_failure fallback classification for failures outside a narrower setup boundary"
    - path: "scripts/ci/verify_ci_setup_diagnostics.sh"
      provides: "Wrapper regressions for inner-fact preservation, generic host-gate fallback, and success"
    - path: "examples/accrue_host/README.md"
      provides: "Maintainer-facing aggregate host-gate diagnostic row"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed gap-closure assertions and complete Phase 226 validation command"
  key_links:
    - from: "scripts/ci/collect_ci_baseline.mjs normalizeRun"
      to: "schema-v1 run provider_state"
      via: "absence-safe normalization to the explicit no-proof state"
      pattern: "provider_state|non_run"
    - from: "scripts/ci/collect_ci_baseline.mjs normalizeJob/liveRuns"
      to: "job needs completion map"
      via: "one shared fail-closed prerequisite check before Math.max and record emission"
      pattern: "needs|completedByName|dag_wait_ms"
    - from: "scripts/ci/accrue_host_uat.sh"
      to: "scripts/ci/ci_setup_diagnostic.sh"
      via: "fact-file delta determines whether the wrapper preserves an inner code or emits host_gate_failure"
      pattern: "ACCRUE_CI_SETUP_FACTS|host_gate_failure"
  assumptions:
    - requirement_id: BASE-01
      category: unclassified
      status: unresolved
      flag: "Spec-less edge fallback could not classify BASE-01; the missing-prerequisite rejection and omitted-provider regression are explicit fail-closed assumptions, not an automatic backstop."
    - requirement_id: BASE-02
      category: unclassified
      status: unresolved
      flag: "Spec-less edge fallback could not classify BASE-02; this plan assumes that defaulting absent evidence to non_run is the required no-promotion boundary and keeps that assumption visible."
    - requirement_id: OWN-01
      category: unclassified
      status: unresolved
      flag: "Spec-less edge fallback could not classify OWN-01; this plan assumes fact-file delta plus an aggregate host_gate_failure code covers the wrapper boundary without inventing an inner cause."
  prohibitions: []
---

<objective>
Close the three current Phase 226 verification gaps by making baseline ingestion and host-wrapper diagnostics fail closed without changing the measured critical path, provider state machine, or proof topology.

Purpose: Restore trust in the durable evidence account: missing input must never become stronger proof, incomplete topology must never become a plausible null metric, and an aggregate host failure must never be mislabeled as a browser launch failure.
Output: Collector and host-wrapper corrections, deterministic negative controls, one precise aggregate diagnostic, reconciled host documentation, and updated validation evidence.

Decision preservation audit: D-01: the checked-in NDJSON/Markdown pack stays canonical; D-02: event/topology cohort separation stays intact; D-03: the rolling 90-day latest-20 rule and frozen snapshot stay intact; D-04: first-attempt and reliability grouping stay intact; D-05: DAG wait becomes fail-closed on incomplete prerequisites; D-06: the privacy allowlist stays intact; D-07: deterministic fixtures gain the two missing collector controls; D-08: provider state remains separate from raw conclusion; D-09: only explicit executed manifest-backed evidence may be proved; D-10: absent ordinary-lane evidence becomes non_run; D-11: cadence, grace, freshness, and SHA binding stay intact; D-12: redacted always-run provider records stay intact; D-13: Fake, required release, Sigra, and live Stripe boundaries stay intact; D-14: CI provisioning and host proof ownership stay intact; D-15: existing preflight and duplicate provisioning stay intact; D-16: the stable registry gains an accurately bounded aggregate host code; D-17: setup facts retain the privacy-safe field contract; D-18: workers, retries, artifacts, and accessibility proof stay intact; D-19: the wrapper reports fact, owner, command, and evidence without inventing a browser cause; D-20: the added README row uses literal measured language; D-21: conventional records and Markdown remain the evidence interface.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-12-SUMMARY.md

Spec-less edge fallback returned unresolved/unclassified rows for BASE-01, BASE-02, and OWN-01. All three remain explicit flagged assumptions in `must_haves.assumptions`; none is auto-backstopped or dropped.

Prohibition recall/precision result: no new bespoke descriptor-less prohibition survived. Privacy, proof-promotion, topology, and ownership constraints are already explicit locked decisions and ASVS threats rather than omitted values/safety/ethics constraints; canon security candidates remain owned by the threat model.

API coverage detector result: detected because the phase reads GitHub Actions REST. Existing `COVERAGE.md` already decides the complete read-only capability surface and remains valid; this gap plan adds no endpoint or SDK capability.

Assumption-delta detector result: `detected: false`; no identity-model checkpoint applies. Schema-push scan found no ORM schema path, so no database push task applies.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` defaults an absent `provider_state` to `non_run` for every event and rejects any declared dependency whose normalized identity has no valid completion.
- `scripts/ci/verify_ci_baseline.mjs` proves successful push and pull-request inputs without provider state remain `non_run`, and proves incomplete dependency topology fails before NDJSON emission.
- `scripts/ci/ci_setup_diagnostic.sh` defines `host_gate_failure` as the host-owned aggregate fallback for a delegated `mix verify.full` failure that emitted no narrower setup fact.
- `scripts/ci/accrue_host_uat.sh` snapshots the configured setup-fact destination before delegation, preserves any new inner diagnostic on failure, and emits `host_gate_failure` only when no new fact exists.
- `scripts/ci/verify_ci_setup_diagnostics.sh` executes inner-diagnostic, aggregate-fallback, and successful-delegation wrapper controls without a database, browser, or network.
- `examples/accrue_host/README.md` documents `host_gate_failure` with its owner, exact reproduction command, and evidence location.
- `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` records Plan 13 task assertions and the green full-phase command.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace absent proof and incomplete topology through the durable collector (D-05, D-07 through D-10)</name>
  <reversibility rating="reversible">Both changes tighten internal normalization behind the existing schema-v1 fields; undo is a local collector/verifier edit with no stored-data migration.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
  </read_first>
  <behavior>
    - A successful push without `provider_state` emits a run record with `provider_state: non_run`.
    - A successful pull-request without `provider_state` emits a run record with `provider_state: non_run`.
    - An explicitly supplied valid provider state is preserved, so selected provider proof still requires its existing explicit classifier output.
    - A dependent job naming one absent prerequisite throws a deterministic error naming the dependent job and unresolved prerequisite before any baseline output is accepted.
    - The live Actions path retains declared `workflowNeeds()` entries and subjects them to the same completion check instead of removing unavailable dependencies.
  </behavior>
  <action>Start RED in `cohortControls()` with successful push and pull-request copies of `successful_run` whose `provider_state` key is absent; assert both normalized run records are `non_run`, while an explicit valid state still round-trips. Add a missing-prerequisite control by changing the dependent fixture's `needs` to a stable identity not present in the run and require a precise rejection. Then change `normalizeRun()` so absence always means `non_run` per D-08/D-09/D-10, never an event-dependent proof promotion. Centralize prerequisite lookup in one pure fail-closed helper used by `normalizeJob()` and the live collection mapping: normalize every declared need, require a finite completion for each, require completion no later than dependent start, and only then calculate D-05 DAG wait from the latest completion. Remove the live path's unavailable-dependency pruning so incomplete topology reaches this validator and aborts collection. Keep schema-v1, cohort eligibility, explicit provider states, compatible-path selection, and frozen artifacts unchanged.</action>
  <acceptance_criteria>
    - Successful push and pull-request fixtures with no provider state normalize to `non_run`; neither can normalize to `proved`.
    - Explicit `proved`, `failed`, `misconfigured`, `blocked`, `skipped`, and `non_run` inputs retain the existing enum behavior.
    - A declared prerequisite absent from the completion map fails with both stable identities in the error; no job record with a null DAG wait is returned.
    - Live collection no longer discards a declared prerequisite before normalization.
    - Existing 20-path p50/p95, fingerprint-strata, timestamp, privacy, rerun, and provider controls remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path</automated>
  </verify>
  <done>Direct and live baseline inputs cannot manufacture provider proof or serialize incomplete dependency topology as an ordinary timing fact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve literal inner setup facts and classify only an unclassified aggregate host failure (D-14, D-16, D-17, D-19)</name>
  <reversibility rating="reversible">The added fixed code and wrapper branching are internal diagnostic behavior and can be changed through the existing registry without migration or check-identity changes.</reversibility>
  <files>scripts/ci/ci_setup_diagnostic.sh, scripts/ci/accrue_host_uat.sh, scripts/ci/verify_ci_setup_diagnostics.sh, examples/accrue_host/README.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - scripts/ci/ci_setup_diagnostic.sh
    - scripts/ci/accrue_host_uat.sh
    - scripts/ci/accrue_host_verify_browser.sh
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - examples/accrue_host/README.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
  </read_first>
  <behavior>
    - If delegated `mix verify.full` appends a valid narrower setup fact and fails, the wrapper preserves that fact and does not append a second aggregate or browser classification.
    - If delegation fails without appending a setup fact, the wrapper emits and renders `host_gate_failure`, owner `host`, the exact local reproduction command, and the host-integration log location.
    - A successful delegated gate emits no failure fact.
    - The wrapper returns the delegated nonzero status and continues to emit `FAILED_GATE=host-integration` for workflow compatibility.
  </behavior>
  <action>Start RED by extending `verify_ci_setup_diagnostics.sh` with a temporary PATH-provided `mix` executable and temporary `ACCRUE_CI_SETUP_FACTS` file. Cover three runs: a failing fake that appends a valid narrower fact, a failing fake that appends nothing, and a successful fake. Add fixed registry code `host_gate_failure` with host owner, `cd examples/accrue_host &amp;&amp; mix verify.full` as the narrow next command, and the host-integration command log as evidence. In the wrapper, record whether the configured fact file exists and its byte size or line count immediately before delegation; after failure, append/render `host_gate_failure` only when the file has no new complete record. When an inner fact was appended, leave it untouched and do not reinterpret the cause. Preserve delegated status, single delegation, `FAILED_GATE=host-integration`, optional unset fact destination, and every browser-script classification. Add the eighth registry row to the host README and append Plan 13's two automated rows plus the full green command to `226-VALIDATION.md` only after both task gates pass.</action>
  <acceptance_criteria>
    - The inner-fact fixture exits nonzero with exactly its narrower code retained and no wrapper-added browser or aggregate fact.
    - The no-inner-fact fixture exits nonzero with exactly one `host_gate_failure` fact whose owner, command, and evidence match the registry and README.
    - The success fixture exits zero and adds no failure record.
    - Wrapper verification proves one `mix verify.full` delegation and preserved `FAILED_GATE=host-integration` compatibility.
    - The full Phase 226 command passes before `226-VALIDATION.md` records Plan 13 green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/ci_setup_diagnostic.sh &amp;&amp; bash -n scripts/ci/accrue_host_uat.sh &amp;&amp; bash -n scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The host wrapper reports the closest literal setup boundary when one exists and an honest aggregate host-gate failure otherwise, without changing proof topology or evidence retention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Collector input → durable baseline | Missing or incomplete untrusted Actions metadata can otherwise become a stronger proof claim or misleading timing fact. |
| Actions job graph → DAG arithmetic | Heuristic declared dependencies cross into calculated wait evidence and must be complete before arithmetic. |
| Delegated host gate → wrapper diagnostics | Lower-level facts and arbitrary aggregate failures cross a classification boundary where the wrapper must not invent a cause. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-30 | Spoofing | `normalizeRun()` provider default | high | mitigate | Default absent evidence to `non_run` for every trigger and prove push/pull-request no-promotion controls per D-08 through D-10. |
| T-226-31 | Tampering | `normalizeJob()` / `liveRuns()` dependency topology | high | mitigate | Reject every unresolved or temporally invalid declared prerequisite before DAG arithmetic and exercise the live-path helper through deterministic fixtures per D-05/D-07. |
| T-226-32 | Spoofing | `accrue_host_uat.sh` fallback classification | high | mitigate | Compare fact-file state across delegation, preserve a new inner fact, and emit only the fixed aggregate code when no inner fact exists per D-14/D-16. |
| T-226-33 | Repudiation | wrapper failure evidence | medium | mitigate | Preserve delegated exit status, stable owner/code/command/evidence fields, and existing `FAILED_GATE` marker; fixture-test all branches. |
| T-226-34 | Information Disclosure | setup fact inspection | low | accept | Inspect only local file existence/size or bounded registry records; never print fact payloads, environment values, credentials, or provider data. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe account distinguishes critical path, setup ownership, and provider proof | 01-07, 12, 13 | COVERED | Plan 13 closes the remaining misleading-input and wrapper-classification paths. |
| REQ | BASE-01 | Trustworthy comparable timing and reliability facts | 01-02, 05-07, 12-13 | COVERED | Plan 13 rejects incomplete dependency topology. |
| REQ | BASE-02 | Non-run provider work cannot be mistaken for proof | 03, 05-07, 12-13 | COVERED | Plan 13 removes omitted-state proof promotion. |
| REQ | OWN-01 | Literal host/CI setup ownership and diagnostics | 04-05, 07, 12-13 | COVERED | Plan 13 preserves inner facts and adds an honest aggregate fallback. |
| RESEARCH | — | Separate raw facts, derived facts, and claims | 01, 03, 06, 13 | COVERED | Missing inputs now fail closed or remain explicit no-proof facts. |
| RESEARCH | — | Ownership-first setup diagnostics through one host proof path | 04-05, 13 | COVERED | No second browser path or topology change is introduced. |
| RESEARCH | — | Dependency-free deterministic fixture verification | 01, 03-04, 06-07, 12-13 | COVERED | New regressions use Node/Bash built-ins and fake local commands only. |
| CONTEXT | D-01–D-07 | Durable privacy-safe cohort and correct queue/DAG evidence | 01-02, 06-07, 12-13 | COVERED | D-05/D-07 missing-prerequisite gap closes in Task 1. |
| CONTEXT | D-08–D-13 | Explicit provider state, no conclusion-based promotion | 03, 05-07, 12-13 | COVERED | D-08/D-09/D-10 omitted-state gap closes in Task 1. |
| CONTEXT | D-14–D-18 | Stable ownership codes and preserved host/browser proof | 04-05, 07, 12-13 | COVERED | D-14/D-16 wrapper gap closes in Task 2. |
| CONTEXT | D-19–D-21 | Fact/state/owner/action evidence interface | 02, 05, 12-13 | COVERED | README and stable aggregate code keep the interface literal. |
| CONTEXT | Deferred | Optimization, caching, topology, matrix, branch protection, gate demotion, retry/test removal, external vendor, StoreKit/Admin work | — | EXCLUDED | No gap task implements deferred Phase 227 or unrelated work. |

<verification>
Run both task commands. Completion requires the two collector regressions, all three wrapper branches, the frozen byte-reproducible critical-path report, provider/formatter/setup contracts, and inherited Phase 225 evidence to pass together.
</verification>

<success_criteria>
- BASE-01 no longer accepts an unresolved prerequisite as a normal null DAG wait.
- BASE-02 no longer lets absent provider evidence normalize to `proved` on any ordinary full-CI trigger.
- OWN-01 reports a lower-level literal setup code when available and `host_gate_failure` only when the aggregate wrapper lacks a narrower fact.
- The three unresolved spec-less rows remain explicitly flagged; no edge is auto-backstopped and no bespoke prohibition is fabricated.
- No frozen evidence, workflow identity, required-check topology, cache/setup behavior, browser proof semantics, or deferred Phase 227 optimization is changed.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-13-SUMMARY.md` when done.
</output>

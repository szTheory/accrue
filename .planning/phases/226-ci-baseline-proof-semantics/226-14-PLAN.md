---
phase: 226-ci-baseline-proof-semantics
plan: "14"
type: execute
wave: 8
depends_on: ["226-13"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/provider_proof.mjs
  - scripts/ci/verify_provider_proof.mjs
  - .github/workflows/ci.yml
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-05/D-07: Live Actions collection resolves every declared prerequisite and observed job in one normalized display-identity namespace, then emits baseline records without unresolved prerequisites."
    - "D-08 through D-12: A newly completed manifest-backed proved result binds latest_proved_sha to the current proof SHA and latest_proved_at to the trusted manifest completion timestamp, so its visible freshness is accurate without prior --latest-proved input."
    - "The frozen critical-path report, all non-proved provider states, setup ownership diagnostics, required-check identity, and Phase 225 evidence remain green after both repairs."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "One stable identity namespace for workflowNeeds(), liveRuns(), completion lookup, and collectBaseline()"
      exports: ["liveRuns", "collectBaseline", "normalizeJob"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Current-display-name integration fixture proving the live mapping reaches emitted baseline records"
    - path: "scripts/ci/provider_proof.mjs"
      provides: "Current proved-record anchoring to manifest.finished_at and the current SHA"
      exports: ["classifyProviderProof", "deriveFreshness", "validateProviderManifest"]
    - path: "scripts/ci/verify_provider_proof.mjs"
      provides: "Freshly-proved fixture and static workflow finalization contract"
    - path: ".github/workflows/ci.yml"
      provides: "Existing always-run provider finalization path whose SHA and trusted manifest reach the proof record"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed Plan 14 assertions and full Phase 226 regression evidence"
  key_links:
    - from: "scripts/ci/collect_ci_baseline.mjs workflowNeeds()"
      to: "live Actions jobs indexed by completedByName"
      via: "the same normalized display identities, including admin-drift-and-docs and docs-contracts-shift-left"
      pattern: "workflowNeeds|completedByName|admin-drift-and-docs|docs-contracts-shift-left"
    - from: "scripts/ci/provider_proof.mjs classifyProviderProof()"
      to: "render_provider_summary.mjs freshness output"
      via: "proved records replace the previous anchor with current sha and validated manifest.finished_at before freshness is consumed"
      pattern: "latest_proved_sha|latest_proved_at|manifest.finished_at|stale"
    - from: ".github/workflows/ci.yml provider_proof_finalize"
      to: "scripts/ci/provider_proof.mjs"
      via: "current github.sha plus the suite-written manifest, with the resulting record passed to the always-run summary"
      pattern: "provider_proof_finalize|--sha|--manifest|provider_proof_summary"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists and the deterministic edge probe left BASE-01 unresolved/unclassified; this plan assumes that a current-display-name liveRuns/collectBaseline integration fixture closes the observed identity edge without dismissing the wider spec-less surface."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists and the deterministic edge probe left BASE-02 unresolved/unclassified; this plan assumes that current SHA plus validated manifest completion is the trusted freshness anchor for a newly proved record, while retaining every existing non-proved transition."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists and the deterministic edge probe left OWN-01 unresolved/unclassified; OWN-01 is preservation-only here and remains guarded by its focused setup diagnostic contract rather than being silently declared complete."
  prohibitions:
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branch names, logs, secret-presence details, provider payloads, artifact contents, credentials, or sensitive runtime values."
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT mix schedule/provider-only executions into full-CI timing, count reruns or duplicate identities, omit topology strata, sum parallel shards, or manufacture percentiles below 20 complete observations."
    - requirement_id: BASE-02
      status: flagged-unverified
      statement: "MUST NOT promote non_run, skipped, stale, Fake-backed, zero-selected, differently-SHA, invalid-manifest, or unsuccessful evidence into live-provider proof."
    - requirement_id: OWN-01
      status: flagged-unverified
      statement: "MUST NOT change host/CI setup ownership, host delegation, duplicate provisioning, Playwright workers/retries/artifacts, required-check identity, branch protection, matrix topology, or cache behavior."
---

<objective>
Close the two current Phase 226 verifier gaps while preserving the already-proved critical-path, setup-ownership, privacy, and provider-state contracts.

Purpose: Make the durable baseline collectable from current GitHub Actions job identities and make a newly successful provider proof visibly fresh at the moment it is finalized.
Output: Stable live job-identity mapping, a current-display-name collector integration fixture, current-proof freshness anchoring, workflow-contract coverage, and updated validation evidence.

Decision preservation audit: D-01 through D-04 keep the canonical evidence pack and comparable cohort unchanged; D-05/D-07 gain the missing production identity integration; D-06 privacy fields remain unchanged; D-08/D-09 keep policy, raw conclusion, and proof predicates independent; D-10 retains every non-proved transition; D-11 binds a current proved record to its own SHA and completion time; D-12 retains the redacted always-run record/summary path; D-13 keeps Fake/release/Sigra/live-Stripe boundaries; D-14 through D-21 remain preservation-only and are rerun through their existing contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-13-SUMMARY.md

Spec-less fallback result: the deterministic edge probe returned BASE-01, BASE-02, and OWN-01 as unresolved/unclassified. The three assumptions and four descriptor-less prohibitions in `must_haves` remain flagged and binding; none is auto-dismissed.

Assumption-delta detector result: `detected: false`; no identity-model checkpoint applies. This repairs the existing read-only GitHub metadata integration, so no new external API coverage matrix applies. No schema file or package install is involved, so no schema-push or package-legitimacy task applies.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` uses one normalized display-identity namespace across `workflowNeeds()`, observed Actions jobs, `completedByName`, `liveRuns()`, and `collectBaseline()`; no YAML job ID is looked up directly in a display-name index.
- `scripts/ci/verify_ci_baseline.mjs` gains an integration fixture containing current CI display names for Admin drift and docs, Host integration, and Docs contracts shift-left, and asserts that the live-shaped path emits run/job records with resolved prerequisite timing.
- `scripts/ci/provider_proof.mjs` makes a valid current `proved` record authoritative for `latest_proved_sha` and `latest_proved_at` using the current proof SHA and validated manifest `finished_at` before deriving visible freshness.
- `scripts/ci/verify_provider_proof.mjs` proves a newly finalized successful proof is fresh without prior `--latest-proved` input and rejects invalid, failed, zero-selected, skipped, non-run, or differently-SHA promotion.
- `.github/workflows/ci.yml` retains the stable `live-stripe` job and always-run step IDs while its static contract proves the finalizer receives current SHA and the trusted manifest consumed by the classifier.
- `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` records the two Plan 14 gates and the full green Phase 226 command only after execution evidence passes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace current Actions display identities through liveRuns and collectBaseline (D-05, D-07)</name>
  <reversibility rating="reversible">The namespace correction is internal to the collector and its fixture; undo is a local two-file edit with no evidence-schema migration.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - .github/workflows/ci.yml
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
  </read_first>
  <behavior>
    - A live-shaped run with display names `Admin drift and docs`, `Host integration (required deterministic gate)`, and `Docs contracts shift-left` resolves the host prerequisites in the same identity namespace used by the completion map.
    - `liveRuns()` followed by `collectBaseline()` emits the run and job records, including non-null host DAG wait derived from the latest prerequisite completion.
    - Removing either prerequisite, using an unknown identity, or placing its completion after host start fails closed with the existing precise unresolved/temporal diagnostic.
    - Existing current Playwright shard-name normalization and frozen critical-path calculations remain unchanged.
  </behavior>
  <action>Start RED by adding a dependency-injected live integration fixture that exercises the exported `liveRuns()` mapping without network access: provide run/job payloads shaped like the GitHub Actions API and use the current display names named in `<behavior>`, then feed the result into `collectBaseline()` and assert emitted host/job timing records contain no unresolved prerequisite. Include negative mutations for an absent prerequisite and a prerequisite completing after host start. Establish one stable display-identity namespace for both sides of the lookup: map the workflow dependency contract to the normalized identities actually produced from observed Actions display names, including `admin-drift-and-docs` and `docs-contracts-shift-left`, before `completedByName` lookup. Keep workflow YAML job IDs as workflow topology identifiers only; do not mix them into the observed display-name index. Preserve the Plan 13 fail-closed prerequisite validator, the current Playwright shard prefix, the privacy allowlist, and all duration/cohort behavior. If `liveRuns()` is not currently injectable, expose only the narrow fetch seam or mapper needed by the fixture; do not introduce a second collector path.</action>
  <acceptance_criteria>
    - The fixture uses the current CI display names and executes the production `liveRuns()` mapping plus `collectBaseline()` rather than reconstructing records by hand.
    - Host integration resolves both Admin drift/docs and docs-contract prerequisites and emits a finite non-negative `dag_wait_ms`.
    - Absent, unknown, and temporally impossible prerequisite mutations fail before record emission.
    - `workflowNeeds()`, observed job indexing, and prerequisite lookup use one documented normalized display-identity namespace.
    - Existing fixture and frozen critical-path gates remain green without modifying the frozen NDJSON or Markdown.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path</automated>
  </verify>
  <done>Current GitHub Actions display names traverse the production live collector into durable baseline records without unresolved prerequisites, while incomplete topology still fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind a newly proved provider record to its current SHA and trusted completion (D-08 through D-12)</name>
  <reversibility rating="costly">The proof record is consumed by the workflow summary and durable evidence readers; reverting the anchor requires coordinated classifier, verifier, workflow-contract, and validation changes, but no data migration.</reversibility>
  <files>scripts/ci/provider_proof.mjs, scripts/ci/verify_provider_proof.mjs, .github/workflows/ci.yml, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - scripts/ci/provider_proof.mjs
    - scripts/ci/render_provider_summary.mjs
    - scripts/ci/verify_provider_proof.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/provider-proof-cases.json
    - .github/workflows/ci.yml
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
  </read_first>
  <behavior>
    - A scheduled or workflow-dispatch result satisfying every existing proved predicate and supplying no prior latest proof emits `latest_proved_sha` equal to the current SHA and `latest_proved_at` equal to validated `manifest.finished_at`.
    - With `now` inside the documented cadence plus grace window, the newly proved record has `stale: false`, and the real summary renders fresh rather than stale.
    - Failed, misconfigured, blocked, skipped, and non_run results do not replace a supplied previous proved anchor with the current SHA/time.
    - Invalid or calendar-impossible manifest completion timestamps cannot become a freshness anchor.
    - The stable always-run workflow finalizer supplies current SHA and the suite-written manifest to the classifier, and the summary consumes that finalized record.
  </behavior>
  <action>Start RED in `verify_provider_proof.mjs` with a successful selected/configured/nonzero manifest fixture that omits prior `latest_proved_sha` and `latest_proved_at`, sets `now` inside the 24-hour cadence plus 48-hour grace, and asserts the returned record and rendered Markdown are current and fresh. Add controls proving every non-proved state preserves the previous anchor (or null when none exists), invalid manifest timestamps fail validation, and a differently-SHA prior record is not presented as proof for the current SHA. In `classifyProviderProof()`, retain `baseRecord()` for previous-proof freshness on all non-proved paths; only after the validated manifest and all D-09 success predicates yield `proved`, replace `latest_proved_sha` with `record.sha`, replace `latest_proved_at` with `manifest.finished_at`, and rederive `stale` from that trusted completion plus the existing cadence/grace and `now`. Do not infer proof or freshness from raw job conclusion alone. Strengthen `assertWorkflowContract()` so `provider_proof_finalize` must pass `${{ github.sha }}` and `$RUNNER_TEMP/accrue-provider-manifest.json`, write `$ACCRUE_PROVIDER_PROOF_RECORD`, run under `if: always()`, and precede the always-run summary that reads the same record. Change workflow implementation only if those already-required data links are absent; preserve job ID, trigger, policy, permissions, secrets boundary, artifact name, and all stable step IDs. After both task gates and the complete regression command pass, append Plan 14 task/assertion/threat rows and final green evidence to `226-VALIDATION.md`.</action>
  <acceptance_criteria>
    - A newly proved record without prior latest-proof input contains the current SHA, trusted manifest completion timestamp, and `stale: false` at current completion.
    - The actual provider summary labels that record fresh and retains literal proof state, reason, raw conclusion, counts, evidence, and next action.
    - No non-proved state, raw success alone, zero-selected suite, invalid manifest, or differently-SHA prior proof is promoted or rebound as current proof.
    - Static workflow verification proves current SHA plus trusted manifest flow through finalization to the always-run summary without changing workflow identity or permissions.
    - Baseline fixtures/frozen report, provider fixtures, formatter, setup diagnostics, and Phase 225 evidence all pass before validation is updated.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/provider_proof.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>A current manifest-backed provider proof is visibly fresh for its own SHA, all weaker states remain literal, and the complete Phase 226 contract stays green.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow topology → Actions display identities | Repository-declared prerequisite identities cross into untrusted live job metadata before DAG timing is calculated. |
| Provider manifest → proof record and summary | A suite-written manifest crosses from runtime evidence into a durable redacted proof claim and visible freshness state. |
| Workflow expressions → local finalizer | GitHub trigger/SHA/job status and runner-temp paths enter the classifier through shell arguments and must retain the existing read-only/secrets boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-35 | Tampering | `workflowNeeds()` / `completedByName` | high | mitigate | Use one normalized display-identity namespace and production-path positive/negative fixtures; reject unresolved or temporally impossible prerequisites before D-05 arithmetic. |
| T-226-36 | Spoofing | `classifyProviderProof()` freshness anchor | high | mitigate | Rebind only after all D-09 predicates pass, using current record SHA and validated manifest completion; fixture every non-proved and invalid-manifest path. |
| T-226-37 | Tampering | `provider_proof_finalize` workflow link | high | mitigate | Statically require current GitHub SHA, runner-temp manifest, finalized record, always-run ordering, exact read-only permissions, and unchanged stable step/job identities. |
| T-226-38 | Information Disclosure | provider record and summary | medium | mitigate | Retain the existing allowlisted record/renderer and full provider fixture gate; never serialize secrets, payloads, secret-presence facts, or raw provider output. |
| T-226-39 | Repudiation | Plan 14 validation evidence | medium | accept | Deterministic fixture and full-regression commands are recorded in the existing validation ledger; no new signing or external attestation is warranted for repository-local CI evidence at ASVS L1. |

No package-manager install or new third-party dependency is involved, so there is no new package-legitimacy/supply-chain decision in this plan.
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe account distinguishes actual critical path, setup ownership, and provider proof state | 01-07, 12-14 | COVERED | Plan 14 restores live collection and accurate current-proof freshness while retaining completed evidence/ownership behavior. |
| REQ | BASE-01 | Durable comparable baseline from current Actions metadata | 01-02, 05-07, 12-14 | COVERED | Task 1 closes the verified live identity mismatch with a production-path fixture. |
| REQ | BASE-02 | Required/skipped/advisory/non-run evidence visibly and accurately differs | 03, 05-07, 12-14 | COVERED | Task 2 closes the verified newly-proved stale rendering and preserves all weaker states. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics | 04-05, 07, 12-14 | COVERED | Already satisfied; Task 2 reruns the unchanged focused ownership contract. |
| RESEARCH | — | Dependency-free Node fixtures over sanctioned Actions metadata | 01, 03, 06-07, 12-14 | COVERED | Both repairs use Node built-ins and local injected fixtures; no new endpoint or dependency is added. |
| RESEARCH | — | Raw facts, derived facts, and claims remain separate | 01, 03, 06, 13-14 | COVERED | Job identity resolution precedes DAG arithmetic; proved classification precedes current freshness rebinding. |
| RESEARCH | — | Privacy allowlists and literal provider semantics | 01-03, 05-07, 12-14 | COVERED | Full existing negative controls rerun after both changes. |
| CONTEXT | D-01–D-07 | Canonical privacy-safe comparable baseline and correct DAG arithmetic | 01-02, 06-07, 12-14 | COVERED | Task 1 closes the live identity edge without changing schema/cohort/frozen evidence. |
| CONTEXT | D-08–D-13 | Independent provider proof/freshness and current-SHA truth | 03, 05-07, 12-14 | COVERED | Task 2 uses manifest completion only after the proved predicate succeeds. |
| CONTEXT | D-14–D-21 | Ownership, proof topology, artifacts, and maintainer interface | 04-05, 07, 12-14 | COVERED | Preservation-only full regression; no ownership or topology change. |
| CONTEXT | Deferred / out of scope | Topology optimization, duplicate setup removal, matrix/branch-protection/cache changes, test demotion/masking, StoreKit/iPhone/Crosswake, Admin ratchet | — | EXCLUDED | Plan 14 contains only the two verifier gaps and does not implement Phase 227 or unrelated work. |

<verification>
Run both task commands. Completion requires the current-display-name live collector fixture, newly-proved freshness fixture and rendered summary, workflow static contract, frozen critical-path report, all provider states, formatter, setup ownership diagnostics, and inherited Phase 225 evidence to pass together.
</verification>

<success_criteria>
- BASE-01 live collection emits durable records for current CI display names without unresolved prerequisites.
- BASE-02 renders a newly successful manifest-backed provider proof as fresh for its own SHA without prior latest-proof input.
- OWN-01 remains satisfied through the unchanged focused setup diagnostic contract.
- BASE-01, BASE-02, and OWN-01 remain visible flagged-unverified spec-less assumptions, and every descriptor-less prohibition remains binding.
- No frozen evidence, privacy schema, required-check identity, workflow topology, branch protection, matrix, cache, setup ownership, duplicate provisioning, Playwright semantics, or test strength changes.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-14-SUMMARY.md` when done.
</output>

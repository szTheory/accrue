---
phase: 226-ci-baseline-proof-semantics
plan: "15"
type: execute
wave: 9
depends_on: ["226-14"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-05/D-07: Production live collection resolves host-integration's docs prerequisite as the normalized identity of the exact workflow display name `Docs and bash contracts (shift-left)`, then emits finite non-negative DAG wait instead of aborting."
    - "D-07: The injected live-path fixture uses the exact `.github/workflows/ci.yml` display names and fails if the workflow name, production mapping, or fixture identity drifts apart again."
    - "D-01 through D-04 and D-06: The frozen privacy-safe cohort, critical-path derivation, schema, and byte-reproducible report remain unchanged and green after the identity correction."
    - "D-08 through D-21: Provider proof semantics, setup ownership, host delegation, workflow topology, and required evidence remain unchanged and green."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Exact production display-identity mapping for the docs prerequisite consumed by host-integration and host-docker timing"
      exports: ["liveRuns", "collectBaseline", "normalizeJob"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Workflow-coupled exact-name live fixture and fail-closed DAG regression controls"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed Plan 15 tracer and full-regression evidence"
  key_links:
    - from: ".github/workflows/ci.yml job `docs-contracts` display name"
      to: "scripts/ci/collect_ci_baseline.mjs workflowNeeds()"
      via: "exact display name normalization to docs-and-bash-contracts-shift-left"
      pattern: "Docs and bash contracts \(shift-left\)|docs-and-bash-contracts-shift-left"
    - from: "scripts/ci/verify_ci_baseline.mjs liveDisplayIdentityControls()"
      to: "scripts/ci/collect_ci_baseline.mjs liveRuns() and collectBaseline()"
      via: "injected API-shaped jobs using literal workflow display names"
      pattern: "liveDisplayIdentityControls|Docs and bash contracts"
    - from: "scripts/ci/verify_ci_baseline.mjs"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson and 226-CI-BASELINE.md"
      via: "unchanged frozen-record and required-critical-path regression gate"
      pattern: "require-critical-path"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists and the deterministic edge probe left BASE-01 unresolved/unclassified; this plan assumes exact workflow-display-name coupling plus production liveRuns()/collectBaseline() coverage closes the observed identity edge without resolving the wider spec-less surface."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists and the deterministic edge probe left BASE-02 unresolved/unclassified; BASE-02 is preservation-only and remains guarded by the exhaustive provider fixture and formatter contracts."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists and the deterministic edge probe left OWN-01 unresolved/unclassified; OWN-01 is preservation-only and remains guarded by the owner-first setup diagnostic contract."
  prohibitions:
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branch names, logs, secret-presence details, provider payloads, artifact contents, credentials, or sensitive runtime values."
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT weaken unresolved-prerequisite or temporal-order failures, mix YAML job IDs into the observed display-name index, or alter cohort/critical-path arithmetic to bypass the identity mismatch."
    - requirement_id: BASE-02
      status: flagged-unverified
      statement: "MUST NOT promote non_run, skipped, stale, Fake-backed, zero-selected, differently-SHA, invalid-manifest, or unsuccessful evidence into live-provider proof."
    - requirement_id: OWN-01
      status: flagged-unverified
      statement: "MUST NOT change host/CI setup ownership, host delegation, duplicate provisioning, Playwright workers/retries/artifacts, required-check identity, branch protection, matrix topology, or cache behavior."
---

<objective>
Close the remaining BASE-01 live-collection gap by binding the production prerequisite mapping and its regression fixture to the workflow's exact docs job display name per D-05 and D-07.

Purpose: Make current GitHub Actions data collectable without weakening fail-closed DAG timing, then prove BASE-02 and OWN-01 behavior did not regress.
Output: Correct exact-name mapping, workflow-coupled injected live regression coverage, and updated validation evidence.

Decision preservation audit: D-01, D-02, D-03, and D-04 retain the canonical evidence pack and compatible cohort; D-05 and D-07 receive the exact production identity repair; D-06 privacy stays unchanged; D-08, D-09, D-10, D-11, D-12, and D-13 remain preservation-only through provider/formatter regressions; D-14, D-15, D-16, D-17, and D-18 remain preservation-only through setup diagnostics; D-19, D-20, and D-21 retain the existing maintainer interface and evidence posture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-14-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs

Spec-less fallback: BASE-01, BASE-02, and OWN-01 remain flagged unresolved/unclassified assumptions. Descriptor-less adversarial recall retains only the phase-specific privacy, fail-closed DAG, provider-promotion, and ownership/topology prohibitions in `must_haves`; none is auto-resolved or auto-dismissed.

No external API integration is added: the repair stays inside the existing read-only GitHub Actions metadata collector and its injected local fixture, so no new external-API matrix applies. No package install or ORM schema file is in scope, so package-legitimacy and schema-push gates do not apply.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` maps the docs prerequisite to `docs-and-bash-contracts-shift-left`, the normalized form of the exact workflow display name `Docs and bash contracts (shift-left)`, for both existing consumers of that prerequisite.
- `scripts/ci/verify_ci_baseline.mjs` reads `.github/workflows/ci.yml`, asserts the `docs-contracts` job retains that exact display name, and drives the production `liveRuns()` → `collectBaseline()` path with the same literal name.
- The live regression asserts host-integration emits finite non-negative `dag_wait_ms`; removal, spelling drift, unresolved identity, and completion-after-dependent-start mutations continue to fail closed.
- `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` gains executed `226-15-01` and `226-15-02` rows and a truthful twenty-two-task sign-off after the complete regression command passes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the exact docs workflow display name through live collection (D-05, D-07)</name>
  <reversibility rating="reversible">This is an internal two-file identity correction with no schema, persisted-evidence, workflow, or public-contract migration.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-14-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
  </read_first>
  <behavior>
    - The verifier confirms the current `docs-contracts` workflow job display name is exactly `Docs and bash contracts (shift-left)` before exercising the live fixture.
    - API-shaped jobs named `Admin drift and docs`, `Docs and bash contracts (shift-left)`, and `Host integration (required deterministic gate)` traverse `liveRuns()` and `collectBaseline()` and emit host timing with finite non-negative `dag_wait_ms`.
    - The production prerequisite identity and completion-map key both normalize to `docs-and-bash-contracts-shift-left`.
    - Removing or misspelling the exact docs job, or completing it after host start, fails with the existing unresolved-prerequisite or temporal-order diagnostic before any record is emitted.
  </behavior>
  <action>Start RED by replacing the synthetic docs label in `liveDisplayIdentityControls()` with the literal display name from `.github/workflows/ci.yml` and adding a bounded static assertion keyed to the `docs-contracts` job block so a workflow rename cannot leave the fixture silently synthetic. Assert RED reproduces the production unresolved-prerequisite failure. Then centralize the normalized docs display identity in `collect_ci_baseline.mjs` and use `docs-and-bash-contracts-shift-left` for the existing host-integration and host-docker prerequisite mappings. Keep `workflowNeeds()`, observed-job indexing, `completedByName`, and `prerequisiteCompletions()` in normalized display-name space; retain YAML job IDs only for the verifier's bounded workflow-contract assertion. Update absent, spelling-drift, and temporal negative mutations to name the exact display label and expect the corrected normalized identity. Preserve the injected `fetchPages` seam, schema/privacy allowlist, current Playwright shard recognition, queue-versus-DAG arithmetic, cohort selection, and frozen evidence files.</action>
  <acceptance_criteria>
    - The fixture contains the exact literal `Docs and bash contracts (shift-left)` and statically proves it matches the current `docs-contracts` workflow block.
    - `liveRuns()` followed by `collectBaseline()` emits the host job with finite non-negative `dag_wait_ms` and no unresolved prerequisite for the exact production labels.
    - The corrected normalized identity is `docs-and-bash-contracts-shift-left` everywhere the docs prerequisite is matched.
    - Missing, drifted, and temporally impossible docs prerequisites fail closed with identity-specific diagnostics.
    - Frozen NDJSON and Markdown are not modified and still pass `--require-critical-path`.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path</automated>
  </verify>
  <done>The workflow's exact docs display name traverses the production live collector into durable host timing, while identity and temporal corruption still fail before record emission.</done>
</task>

<task type="auto">
  <name>Task 2: Record the closure and prove provider and ownership preservation (D-01 through D-21)</name>
  <reversibility rating="reversible">Validation-ledger rows describe reproducible repository-local commands and can be corrected without changing runtime contracts.</reversibility>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/verify_phase225_required_lane_evidence.sh
    - accrue/test/accrue/live_proof_formatter_test.exs
  </read_first>
  <action>After Task 1 passes, run the complete Phase 226 regression command covering the exact-name live collector, frozen critical-path report, exhaustive provider state/freshness fixtures, the real provider manifest formatter, owner-first setup diagnostics, and inherited Phase 225 evidence. Add validation rows `226-15-01` for the exact workflow-name tracer under BASE-01/T-226-40 and `226-15-02` for the full preservation contract under BASE-01, BASE-02, OWN-01/T-226-41/T-226-42. Record executed-green lifecycle text only after the corresponding command succeeds. Reconcile the existing stale eighteen-task sign-off with the actual twenty pre-Plan-15 rows, then approve twenty-two rows with zero behavior-unverified only after both new rows and the full suite are green. Leave the frozen evidence, provider implementation, workflow, setup scripts, host scripts, Playwright configuration, and documentation untouched.</action>
  <acceptance_criteria>
    - The full suite passes after the exact-name correction, including provider, formatter, setup, and Phase 225 gates.
    - Validation contains executed-green `226-15-01` and `226-15-02` rows with exact automated commands and threat references.
    - Validation sign-off states twenty-two executed task rows and zero behavior-unverified rows, matching the ledger count.
    - BASE-02 proof-state/freshness and OWN-01 owner/setup contracts are unchanged and green.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The exact-name closure is recorded against a green full Phase 226 contract, with BASE-02 and OWN-01 regression preservation explicit and the validation ledger internally consistent.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow YAML display identity → collector prerequisite contract | A repository-declared human display name must match the normalized identity used against untrusted live Actions job metadata. |
| Injected Actions-shaped fixture → production liveRuns()/collectBaseline() | Synthetic test payloads can create false confidence when they do not preserve literal production identities. |
| Identity correction → established Phase 226 proof contracts | A narrow baseline repair could accidentally weaken privacy, provider proof, setup ownership, or critical-path evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-40 | Tampering | `workflowNeeds()` / workflow display-name contract | high | mitigate | Block on a bounded workflow-block assertion, exact literal live fixture, production liveRuns()/collectBaseline() tracer, and unresolved/temporal negative controls before accepting DAG timing. |
| T-226-41 | Spoofing | injected live fixture | high | mitigate | Require the fixture's docs label to match the actual `docs-contracts` workflow display name and prove the corrected normalized identity reaches the completion map; synthetic aliases cannot satisfy the gate. |
| T-226-42 | Information Disclosure | full Phase 226 regression surface | high | mitigate | Block completion on unchanged schema/privacy fixtures and frozen-record validation, then rerun provider, formatter, setup, and inherited evidence contracts before validation sign-off. |
| T-226-43 | Repudiation | validation ledger | low | accept | Repository-local commands, task rows, and atomic commits provide sufficient traceability for this reversible internal mapping correction at ASVS L1. |

No package-manager install or third-party dependency is involved, so no package-legitimacy checkpoint or supply-chain threat row applies.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe account distinguishes actual critical path, setup ownership, and provider proof state | 01-07, 12-15 | COVERED | Plan 15 restores collection from current Actions identities and reruns the complete proof/ownership contract. |
| REQ | BASE-01 | Durable comparable baseline from current GitHub Actions metadata | 15 | COVERED | Exact production docs display identity traverses liveRuns()/collectBaseline() with fail-closed regression coverage. |
| REQ | BASE-02 | Required, skipped, advisory, and non-run provider evidence stays visibly distinct | 15 | COVERED | Preservation-only provider fixture plus real formatter gate reruns unchanged. |
| REQ | OWN-01 | Host maintainer can identify setup ownership and diagnostics | 15 | COVERED | Preservation-only owner-first setup diagnostic contract reruns unchanged. |
| RESEARCH | — | Dependency-free Node collector and deterministic injected Actions fixtures | 15 | COVERED | Existing Node built-ins and fetch seam are reused; no dependency or endpoint is added. |
| RESEARCH | — | Raw facts and derived DAG wait remain separate and fail closed | 15 | COVERED | Exact identity resolves before prerequisite completion and DAG arithmetic; missing/temporal controls remain blocking. |
| RESEARCH | — | Privacy-safe durable evidence and literal provider/setup semantics | 15 | COVERED | Frozen/schema, provider, formatter, and setup regression gates block completion. |
| CONTEXT | D-01–D-07 | Canonical baseline, comparable cohort, timing, privacy, and deterministic fixtures | 15 | COVERED | D-05/D-07 receive the exact-name correction; D-01–D-04/D-06 remain regression-preserved. |
| CONTEXT | D-08–D-13 | Independent provider policy/proof/freshness truth | 15 | COVERED | Preservation-only exhaustive provider and formatter gates. |
| CONTEXT | D-14–D-18 | Host/CI setup ownership, diagnostics, and Playwright proof | 15 | COVERED | Preservation-only setup diagnostic and inherited evidence gates. |
| CONTEXT | D-19–D-21 | Action-first, accessible, durable maintainer evidence | 15 | COVERED | Existing report/docs remain unchanged; corrected live data can reach the durable interface. |
| CONTEXT | Deferred / out of scope | Optimization, duplicate setup removal, caching/topology/matrix/branch-protection changes, test demotion/masking, external observability, StoreKit/iPhone/Crosswake, Admin ratchet | — | EXCLUDED | No task implements deferred Phase 227 or unrelated work. |

<verification>
Run both task commands. Completion requires the exact `.github/workflows/ci.yml` display name to traverse production live collection, all identity/temporal negative controls to fail closed, the frozen critical-path report to remain byte-valid, and every provider/formatter/setup/inherited evidence gate to pass before the twenty-two-row validation sign-off.
</verification>

<success_criteria>
- BASE-01 live collection resolves the actual `Docs and bash contracts (shift-left)` prerequisite and emits host DAG timing from current Actions-shaped data.
- The regression fixture cannot silently substitute a synthetic docs label because it is coupled to the current workflow block and production mapping.
- BASE-02 and OWN-01 remain explicit flagged-unverified spec-less assumptions and pass their unchanged focused regressions.
- All phase-specific privacy, DAG, provider-promotion, and ownership/topology prohibitions remain binding.
- Every ASVS L1 high-severity threat is mitigated by a blocking automated gate.
- No frozen evidence, workflow topology, schema, package dependency, provider behavior, setup ownership, or Playwright proof semantics change.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-15-SUMMARY.md` when done.
</output>

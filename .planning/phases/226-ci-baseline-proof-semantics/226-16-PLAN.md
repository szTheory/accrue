---
phase: 226-ci-baseline-proof-semantics
plan: "16"
type: execute
wave: 10
depends_on: ["226-15"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-02/D-04/D-07: A run attempt is normalized only from jobs returned by that exact attempt's Actions endpoint; an older-attempt job can never reach the current attempt's run, job, cohort, or critical-path records."
    - "D-02/D-06/D-07: Every retained live job receives an auditable privacy-safe runner class/image derived from the repository's trusted workflow `runs-on` contract, and unresolved or mismatched runner identity fails before cohort fingerprinting."
    - "D-02: Otherwise-identical runs on different runner classes/images produce different `cohort_fingerprint` values, so GitHub-hosted Linux, macOS, and self-hosted work cannot silently share a timing cohort."
    - "D-01/D-03/D-05: After collector repair, the canonical 90-day snapshot is recollected, validated, rendered twice, byte-compared, and accepted only if at least 20 unique successful first-attempt complete staged paths support the reported critical-path conclusion or the report records the contrary measured result."
    - "D-08 through D-21: Already-verified provider policy/proof/freshness, host/CI setup ownership, Playwright proof, stable workflow topology, artifacts, and maintainer guidance remain unchanged and green."
    - statement: "BASE-01 backstop: the recollected frozen evidence represents one attempt per run and a repository-declared runner cohort rather than an inferred hostedness label."
      verification: backstop
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Attempt-scoped Actions job retrieval and fail-closed workflow runner classification"
      exports: ["liveRuns", "collectBaseline", "cohortFingerprint", "normalizeJob"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "API-shaped all-attempt exclusion and runner-class/image fingerprint regression controls"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      provides: "Recollected canonical 90-day privacy-safe baseline produced only by the repaired collector"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Byte-reproducible critical-path report rendered from the repaired frozen records"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md"
      provides: "Executed Plan 16 integrity, recollection, and preservation evidence"
  key_links:
    - from: "Actions workflow run `run_attempt`"
      to: "scripts/ci/collect_ci_baseline.mjs liveRuns()"
      via: "attempt-specific `/actions/runs/{run_id}/attempts/{attempt_number}/jobs` request plus per-job attempt validation"
      pattern: "attempts/.*/jobs|run_attempt"
    - from: ".github/workflows/ci.yml `runs-on` declarations"
      to: "scripts/ci/collect_ci_baseline.mjs cohortFingerprint()"
      via: "bounded workflow runner resolver keyed by normalized observed display identity; unresolved identity rejects collection"
      pattern: "runs-on|runner_image|cohortFingerprint"
    - from: "scripts/ci/verify_ci_baseline.mjs live API fixture"
      to: "scripts/ci/collect_ci_baseline.mjs liveRuns() and collectBaseline()"
      via: "attempt-2 run with attempt-1 and attempt-2 job payloads, plus distinct runner contract controls"
      pattern: "filter=all|attempts/2/jobs|runner"
    - from: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      via: "repaired live collection, schema/critical-path verification, two independent renders, and byte comparison"
      pattern: "require-critical-path|cmp"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No Phase 226 SPEC exists; the deterministic edge probe did not classify the full comparable-run surface, so exact-attempt and workflow-declared-runner coverage closes the reproduced gap while the wider BASE-01 surface remains guarded by the complete fixture and frozen-record contracts."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No Phase 226 SPEC exists; BASE-02 is preservation-only in this gap plan and remains guarded by the exhaustive provider classifier, summary, and formatter checks."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "No Phase 226 SPEC exists; OWN-01 is preservation-only in this gap plan and remains guarded by the owner-first setup diagnostic and inherited host proof contracts."
  prohibitions:
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT persist actor identity, raw branch names, logs, secrets or secret-presence details, provider payloads, artifact contents, credentials, runner names, runner labels, or user data."
    - requirement_id: BASE-01
      status: flagged-unverified
      statement: "MUST NOT merge jobs across attempts, infer hostedness from runner_name, accept an unresolved runner mapping, mix runner classes/images into one fingerprint, broaden the comparable-path predicate, or accept a critical-path claim from pre-repair frozen evidence."
    - requirement_id: BASE-02
      status: flagged-unverified
      statement: "MUST NOT promote non_run, skipped, stale, Fake-backed, zero-selected, differently-SHA, invalid-manifest, or unsuccessful evidence into live-provider proof."
    - requirement_id: OWN-01
      status: flagged-unverified
      statement: "MUST NOT change host/CI setup ownership, duplicate provisioning, Playwright workers/retries/artifacts, required-check identity, workflow topology, matrix membership, branch protection, cache behavior, or test strength."
---

<objective>
Close the remaining BASE-01 evidence-integrity gaps by isolating live Actions jobs to their exact rerun attempt, sourcing runner class/image from the trusted workflow contract, and recollecting the frozen baseline only after both invariants pass production-path regressions (D-01 through D-07).

Purpose: Make the reported 33–36 minute staged critical path an actual comparable-run measurement rather than a reproducible rendering of attempt-mixed, falsely cohort-labeled input, while preserving verified BASE-02 and OWN-01 behavior (D-08 through D-21).
Output: Repaired live collector, API-shaped attempt/runner controls, recollected canonical NDJSON and Markdown, and updated validation evidence.

Decision preservation audit: D-01 keeps checked-in NDJSON plus derived Markdown canonical; D-02 receives auditable runner cohorting; D-03 keeps latest-20/90-day measurement; D-04 receives exact-attempt isolation; D-05 retains queue/DAG/setup arithmetic; D-06 retains the strict privacy boundary; D-07 receives production-shaped negative controls. D-08 through D-13 remain preservation-only through provider and formatter regressions. D-14 through D-18 remain preservation-only through setup and inherited proof regressions. D-19 through D-21 retain the existing action-first durable interface.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-15-SUMMARY.md
@.planning/phases/225-required-lane-signal-repair/225-CONTEXT.md
@.planning/phases/225-required-lane-signal-repair/225-03-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@.planning/phases/226-ci-baseline-proof-semantics/schema-v1.json

Spec-less fallback: BASE-01, BASE-02, and OWN-01 remain flagged unresolved/unclassified assumptions. The applicable BASE-01 backstop is structured in `must_haves.truths`; descriptor-less adversarial recall retains the phase-specific privacy, attempt-mixing, runner-fabrication, provider-promotion, and ownership/topology prohibitions. None is auto-resolved, auto-dismissed, or given a fabricated wired-check descriptor.

No package install, schema push, CI optimization, or external write integration is introduced. Collection continues through the existing read-only GitHub Actions metadata boundary, using the documented attempt-specific jobs resource and repository-declared workflow runner contract.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` requests jobs for the listed workflow run's exact positive `run_attempt`, rejects per-job attempt disagreement, and exposes no older-attempt job to `normalizeJob()`, `collectBaseline()`, `cohortFingerprint()`, reliability, or staged-path derivation.
- `scripts/ci/collect_ci_baseline.mjs` adds a bounded runner resolver whose output is the privacy-safe workflow-declared runner class/image (for example `github-hosted/ubuntu-24.04` or `github-hosted/macos-15`); unknown, ambiguous, or mismatched display identities fail before fingerprinting, and raw runner names/labels are never persisted.
- `scripts/ci/verify_ci_baseline.mjs` adds a live API-shaped attempt-2 regression with an attempt-1 stale job and an attempt-2 current job, proving the all-attempt response path is not requested and a mismatched per-job attempt cannot reach normalized records.
- `scripts/ci/verify_ci_baseline.mjs` adds workflow-coupled runner controls proving Linux, macOS, and self-hosted classifications are distinct, unresolved mappings fail closed, and otherwise-identical runs with different runner class/image inputs receive different fingerprints.
- `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson` is atomically replaced by a fresh 90-day read-only collection only after the repaired fixture and temporary record/critical-path gates pass.
- `.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md` is rendered twice from the repaired frozen records, byte-compared, and atomically replaced only when both renders match and the measured conclusion is supported.
- `.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md` gains executed `226-16-01` and `226-16-02` rows plus a truthful twenty-four-row sign-off only after the focused and full regression commands succeed.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one exact Actions attempt and workflow-declared runner cohort end to end (D-02, D-04, D-06, D-07)</name>
  <reversibility rating="reversible">The repair changes an internal read-only endpoint choice and bounded classification seam; it creates no public API, workflow-topology, or persisted-schema migration.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-15-SUMMARY.md
    - .github/workflows/ci.yml
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
  </read_first>
  <behavior>
    - An Actions run with `run_attempt: 2` requests `/actions/runs/{id}/attempts/2/jobs`; the injected all-attempt endpoint contains an attempt-1 stale job but is never used.
    - A job whose `run_attempt` differs from its parent run is rejected before normalization even when an injected or malformed attempt endpoint returns it.
    - Only attempt-2 job IDs, timings, conclusions, setup/cache facts, and dependencies reach `collectBaseline()` output and staged-path inputs.
    - The runner resolver derives class/image from the matching `.github/workflows/ci.yml` `runs-on` contract, never from `runner_name`; a self-hosted-looking runner name cannot override a workflow-declared GitHub-hosted image.
    - Unknown or ambiguous job-to-workflow runner mappings fail closed, while otherwise-identical `ubuntu-24.04`, `macos-15`, and controlled self-hosted classifications generate distinct cohort fingerprints.
    - Raw runner names, labels, groups, and API payloads remain outside normalized and persisted records.
  </behavior>
  <action>Start RED in `liveDisplayIdentityControls()` (or a narrowly named sibling) with a live API-shaped run-attempt-2 fixture. Make its fetch seam expose an all-attempt response containing an older failed/timing-distorting attempt-1 job and an attempt-specific response containing only current attempt-2 jobs; assert the production collector requests only the attempt-specific resource and that only current job IDs can reach normalized records. Add a negative response where the attempt-specific endpoint returns a job carrying attempt 1 and require an identity-specific rejection before `collectBaseline()`. Then change `liveRuns()` to require a positive run attempt, fetch the documented attempt-scoped jobs resource, and validate every returned job's attempt when the API supplies it. Preserve pagination, concurrency, existing exact display-name/DAG controls, and first-attempt percentile/reliability semantics.

In the same production tracer, replace the `runner_name` hostedness inference with a bounded `workflowRunnerImage()`/equivalent resolver sourced from the repository's workflow `runs-on` declarations and keyed by normalized observed job display identity. Return a privacy-safe class/image token that distinguishes GitHub-hosted Linux and macOS from self-hosted classifications; reject unknown or ambiguous mappings before `cohortFingerprint()`. Couple fixtures to representative `ci.yml` job blocks (`docs-contracts-shift-left` on `ubuntu-24.04` and `ios-offline-client` on `macos-15`), add an injected controlled self-hosted contract without persisting raw labels, and prove changing only resolved class/image changes the fingerprint. Keep runner names, labels, groups, actors, raw branches, logs, secrets, payloads, and artifacts outside the allowlisted durable record. Do not change schema-v1, timing arithmetic, cohort sample rules, workflow topology, provider/setup code, caches, or tests beyond this focused verifier.</action>
  <acceptance_criteria>
    - Production `liveRuns()` calls the exact attempt-specific jobs resource and never the all-attempt resource for a listed workflow run.
    - The API-shaped attempt-2 positive fixture emits only current-attempt jobs; an older attempt, absent/invalid parent attempt, or per-job mismatch fails before normalized record emission.
    - Runner class/image comes from a workflow-declared trusted mapping with a static workflow contract check; raw `runner_name` cannot decide hostedness or image.
    - Unknown/ambiguous runner mappings fail closed, and distinct Linux/macOS/self-hosted class-image inputs yield distinct cohort fingerprints.
    - The existing exact workflow display-name, DAG timing, privacy, provider-state, setup/cache, rerun, reliability, and critical-path fixture controls remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>The thinnest live path—from a run-attempt-2 API response through trusted runner resolution, normalization, cohort fingerprinting, and record emission—contains only current-attempt jobs and a real auditable runner class/image, with both corruptions blocked by production-path controls.</done>
</task>

<task type="auto">
  <name>Task 2: Recollect and byte-prove the repaired frozen baseline while preserving BASE-02 and OWN-01 (D-01, D-03, D-05, D-08 through D-21)</name>
  <reversibility rating="reversible">The canonical evidence files are regenerated from read-only metadata and remain reproducible from the collector; no external state or public contract is mutated.</reversibility>
  <precondition>`gh auth status` succeeds with read-only Actions access, the canonical workflow probe resolves every retained job's runner class/image, and a temporary repaired 90-day collection contains at least 20 unique successful first-attempt complete staged paths.</precondition>
  <files>.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md, .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_provider_proof.mjs
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/verify_phase225_required_lane_evidence.sh
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
    - accrue/test/accrue/live_proof_formatter_test.exs
  </read_first>
  <action>After Task 1 passes, create task-specific temporary files under the phase directory with cleanup on every exit. Verify read-only GitHub authentication and workflow access, run the canonical collector for `szTheory/accrue`, `ci.yml`, 90 days, and sample target 20, then validate the temporary NDJSON with `--require-critical-path`. Require every selected record to have passed exact-attempt and runner-resolution invariants, at least 20 compatible complete staged paths, visible fingerprint strata, and a conclusion that either supports the roughly 33–36 minute staged release-to-host-to-Playwright path or plainly records the contrary measurement. Render twice to separate temporary Markdown files, compare them byte-for-byte, and compare each to the same temporary records. Atomically replace the canonical NDJSON and Markdown only after all temporary gates succeed; on authentication, API, mapping, sample, render, or validation failure, retain the existing frozen files and report the precise blocker without weakening predicates or emitting partial evidence.

Validate the committed pair again, then run baseline fixtures, exhaustive provider proof fixtures, the real provider manifest formatter test, owner-first setup diagnostics, and inherited Phase 225 required-lane evidence. Update `226-VALIDATION.md` only after those commands pass: add `226-16-01` for exact-attempt plus runner-cohort integrity under BASE-01/T-226-44/T-226-45/T-226-46, add `226-16-02` for live recollection and preservation under BASE-01/BASE-02/OWN-01/T-226-47/T-226-48, and reconcile the sign-off to twenty-four executed rows with zero behavior-unverified rows. Do not alter the provider implementation, setup scripts, workflows, documentation, required/advisory identities, topology, cache behavior, or Playwright/test semantics.</action>
  <acceptance_criteria>
    - Recollection occurs only after the focused exact-attempt and runner-resolution fixture suite passes.
    - Temporary and committed NDJSON pass schema/privacy and `--require-critical-path` validation with at least 20 compatible complete first-attempt paths and auditable runner-separated fingerprints.
    - Two independent renders are byte-identical and the committed Markdown is byte-identical to a fresh render from the committed repaired records.
    - The critical-path statement is supported by the recollected repaired measurements or is replaced by an explicit contrary measured result; pre-repair evidence is not accepted as proof.
    - Provider classifier/summary fixtures, formatter test, owner-first setup diagnostics, and inherited required-lane evidence all pass unchanged.
    - Validation contains executed-green `226-16-01` and `226-16-02` rows and an internally consistent twenty-four-row, zero-behavior-unverified sign-off.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_provider_proof.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; (cd accrue &amp;&amp; mix test test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The canonical frozen evidence has been recollected through the repaired collector, byte-proves its measured critical-path conclusion, and leaves verified provider and setup ownership behavior unchanged.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow run attempt → Actions jobs resource | Untrusted remote metadata must be bound to one exact run attempt before any job reaches durable evidence. |
| Workflow `runs-on` contract → live runner metadata | Repository-declared runner class/image is trusted configuration; remote runner names and labels are untrusted operational metadata and cannot define a cohort. |
| Repaired collector → frozen evidence | Temporary live results become canonical checked-in evidence only after privacy, cohort, staged-path, and byte-reproduction gates. |
| Narrow BASE-01 repair → verified BASE-02/OWN-01 contracts | Baseline changes must not weaken provider proof state or host/CI ownership semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-44 | Tampering | `liveRuns()` jobs endpoint | high | mitigate | Request the exact run-attempt jobs resource, validate positive parent/per-job attempts, and block on an API-shaped negative proving an older attempt cannot reach normalized records. |
| T-226-45 | Spoofing | runner class/image resolver | high | mitigate | Resolve only through statically checked workflow `runs-on` contracts, ignore raw runner-name hostedness claims, and fail closed on unknown or ambiguous mappings. |
| T-226-46 | Information Disclosure | runner and job normalization | high | mitigate | Persist only the privacy-safe declared class/image contribution to the fingerprint; fixture rejection keeps raw names, labels, groups, actors, branches, logs, secrets, payloads, artifacts, and user data out. |
| T-226-47 | Tampering | canonical NDJSON/Markdown replacement | high | mitigate | Collect to temporary files, require repaired fixtures plus schema/critical-path validation and dual-render equality, then replace atomically and validate the committed pair again. |
| T-226-48 | Elevation of Privilege | read-only GitHub collection | medium | mitigate | Reuse existing read-only Actions authentication and permissions; no workflow mutation, token serialization, package install, or external write is introduced. |
| T-226-49 | Repudiation | validation ledger | low | accept | Exact repository-local commands, immutable run URLs in sanitized evidence, task rows, and atomic commits provide adequate traceability for this reversible ASVS L1 evidence repair. |

No package-manager install or third-party dependency is involved, so no package-legitimacy checkpoint or supply-chain threat row applies.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe account distinguishes the actual critical path, setup ownership, and provider proof state | 16 | COVERED | The plan repairs both invalid live inputs, recollects the baseline, and reruns verified provider/ownership contracts. |
| REQ | BASE-01 | Durable privacy-safe comparable timing, rerun, cache/setup, provider, and root-failure baseline | 16 | COVERED | Exact-attempt isolation, trusted runner cohorting, production-shaped negatives, and post-repair recollection close both failed truths. |
| REQ | BASE-02 | Required/skipped/advisory/non-run provider evidence remains visibly distinct | 16 | COVERED | Preservation-only provider classifier, summary, and formatter gates rerun unchanged. |
| REQ | OWN-01 | Host maintainer retains setup ownership and diagnostic guidance | 16 | COVERED | Preservation-only setup diagnostic and inherited host evidence gates rerun unchanged. |
| RESEARCH | — | Dependency-free read-only GitHub Actions collector | 16 | COVERED | Existing Node built-ins, `gh`, injected fetch seam, and official attempt-specific resource are reused; no dependency is installed. |
| RESEARCH | — | Fingerprinted cohorts use real runner image and separate raw/derived facts | 16 | COVERED | Workflow-declared runner classification feeds only the cohort input; remote runner labels do not become claims. |
| RESEARCH | — | Deterministic fixtures and byte-reproducible frozen evidence | 16 | COVERED | API-shaped attempt/runner controls precede temporary recollection, dual rendering, atomic replacement, and committed-pair verification. |
| CONTEXT | D-01–D-07 | Canonical evidence, comparable cohorts, latest-20/90-day sampling, attempt grouping, timing, privacy, validation | 16 | COVERED | D-02/D-04/D-07 receive direct repairs; D-01/D-03/D-05/D-06 are enforced during recollection and full regression. |
| CONTEXT | D-08–D-13 | Independent provider policy/proof/freshness truth | 16 | COVERED | Preservation-only exhaustive provider and real formatter verification. |
| CONTEXT | D-14–D-18 | Host/CI setup ownership, diagnostics, and Playwright proof | 16 | COVERED | Preservation-only setup diagnostic and inherited evidence verification. |
| CONTEXT | D-19–D-21 | Action-first, accessible, durable maintainer evidence | 16 | COVERED | Existing renderer/interface is regenerated from repaired data and byte-verified without a new dashboard. |
| CONTEXT | Deferred / out of scope | CI optimization, duplicate setup removal, browser caching, topology/matrix/branch-protection changes, gate demotion, test masking, external observability, StoreKit/iPhone/Crosswake, Admin ratchet | — | EXCLUDED | No task implements deferred Phase 227 or unrelated work. |

<verification>
Run both task commands in order. Completion requires the production-path exact-attempt/runner tracer to pass before any evidence write, followed by a fresh read-only 90-day collection, temporary and committed critical-path validation, dual-render byte equality, exhaustive provider/formatter/setup/inherited checks, and a truthful twenty-four-row validation sign-off.
</verification>

<success_criteria>
- BASE-01 live collection cannot merge an older rerun attempt into a current run and cannot fabricate runner hostedness/image from operational names.
- Distinct workflow-declared runner classes/images produce distinct cohort fingerprints, while unresolved classification stops collection.
- The frozen baseline and Markdown are recollected only after repair and byte-prove the measured critical-path conclusion or explicit contrary result.
- BASE-02 provider proof semantics and OWN-01 setup ownership remain verified without behavior or topology changes.
- BASE-01, BASE-02, and OWN-01 spec-less assumptions and every descriptor-less prohibition remain visible and binding.
- No CI optimization, topology change, matrix or branch-protection edit, cache rewrite, gate demotion, setup removal, retry masking, or test deletion occurs.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-16-SUMMARY.md` when done.
</output>

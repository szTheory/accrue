---
phase: 226-ci-baseline-proof-semantics
plan: "17"
type: execute
wave: 11
depends_on: ["226-16"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/accrue_host_uat.sh
  - scripts/ci/verify_ci_setup_diagnostics.sh
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-02/D-04/D-07: Every historical topology exception is admitted only for one immutable audited workflow revision, exact repository/event/prerequisite tuple, and exact job identity or explicitly enumerated matrix identity; an unknown pre-cutoff topology fails closed."
    - "D-02/D-07: DAG-needs and runner-contract resolution share one exact identity resolver; only workflow-declared identities and narrowly enumerated matrix expansions resolve, while arbitrary suffixes are rejected."
    - "D-14/D-16/D-17: An initial Postgres readiness failure emits and renders the stable host-owned `fixture_or_database` setup fact before exiting with `FAILED_GATE=host-integration`."
    - "D-08 through D-13: Provider policy, proof state, raw conclusion, evidence counts, SHA binding, and freshness remain independent and unchanged while BASE-01 and OWN-01 are repaired."
    - statement: "BASE-01 flagged assumption: immutable workflow-revision binding plus exact/narrow identity controls cover the reproduced admission gaps, while the wider spec-less comparable-run surface remains guarded by the complete baseline fixture suite."
      verification: backstop
    - statement: "BASE-02 flagged assumption: preservation through the exhaustive provider classifier, summary, and formatter checks is sufficient for this gap plan because the verification report found no provider regression."
      verification: backstop
    - statement: "OWN-01 flagged assumption: explicit initial-readiness coverage plus the existing eight-code registry fixtures cover the reproduced wrapper gap, while the wider spec-less ownership surface remains guarded by the setup contract suite."
      verification: backstop
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Immutable-revision-bound historical compatibility and exact workflow identity resolution"
      exports: ["liveRuns", "collectBaseline", "unresolvedPrerequisites", "workflowRunnerImage"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Production-path adversarial controls for unknown revisions, unknown historical topology, and spoofed suffixes"
    - path: "scripts/ci/accrue_host_uat.sh"
      provides: "Owner-first diagnostic path for initial Postgres readiness failure"
    - path: "scripts/ci/verify_ci_setup_diagnostics.sh"
      provides: "Shadowed-pg_isready regression proving setup fact, rendered guidance, exit status, and gate marker"
  key_links:
    - from: "immutable workflow contents at each run head SHA"
      to: "scripts/ci/collect_ci_baseline.mjs compatibilityRule()"
      via: "validated workflow revision/content digest in an audited compatibility row"
      pattern: "workflow_revision|compatibilityRule"
    - from: ".github/workflows/ci.yml job and matrix contracts"
      to: "workflowNeeds() and workflowRunnerImage()"
      via: "one exact-or-enumerated identity resolver used by both callers"
      pattern: "workflowNeeds|workflowRunnerImage"
    - from: "scripts/ci/accrue_host_uat.sh pg_isready status"
      to: "scripts/ci/ci_setup_diagnostic.sh"
      via: "emit and render fixture_or_database before FAILED_GATE and nonzero exit"
      pattern: "pg_isready|fixture_or_database|FAILED_GATE=host-integration"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists; the edge probe returned BASE-01 unresolved/unclassified, so this plan treats the reproduced revision/topology/identity attacks as explicit backstops rather than claiming full edge classification."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists; the edge probe returned BASE-02 unresolved/unclassified, and this gap plan preserves its already-green provider contract through exhaustive regressions."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "No plain Phase 226 SPEC exists; the edge probe returned OWN-01 unresolved/unclassified, so the initial-readiness negative control is an explicit backstop rather than a complete edge taxonomy."
  prohibitions:
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT turn the durable baseline into a raw-forensics or contributor-surveillance archive; actor identity, raw branches, runner names/labels, logs, secret-presence facts, provider payloads, artifact contents, credentials, and user data stay outside checked-in evidence."
    - requirement_id: BASE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT present an unknown workflow generation, spoofed job suffix, or unaudited historical exception as comparable trusted evidence."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT promote non_run, skipped, stale, Fake-backed, zero-selected, differently-SHA, invalid-manifest, or unsuccessful evidence into live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT hide an early host prerequisite failure, blame the CI boundary for host-owned database readiness, or weaken the canonical host/Playwright proof contract."
---

<objective>
Close the two production admission regressions and the initial host-readiness diagnostic regression identified after Plan 16 (D-01 through D-21).

Purpose: Ensure only audited workflow generations and legitimate exact/matrix identities enter comparable evidence, and ensure the host wrapper explains its earliest database failure before Phase 226 recollects the canonical baseline.
Output: Fail-closed collector identity/revision boundaries, adversarial baseline controls, explicit Postgres readiness handling, and a deterministic wrapper regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-RESEARCH.md
@.planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-16-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/accrue_host_uat.sh
@scripts/ci/ci_setup_diagnostic.sh
@scripts/ci/verify_ci_setup_diagnostics.sh

Spec-less fallback: BASE-01, BASE-02, and OWN-01 remain flagged-unverified assumptions. The prohibition recall retained only phase-specific privacy/transparency/ownership constraints. Canon-referral breadcrumbs, not minted prohibition rows: generic command injection, generic secret leakage, and Actions permission hardening remain owned by `$gsd-secure-phase` plus repository lint/static gates. No descriptor was fabricated for any flagged item.

Root-cause classification: Plan 16 had a plan/implementation boundary deficiency—its historical table was time/repository bounded but not immutable-revision bound, its shared identity matching admitted arbitrary suffixes, and its wrapper verification began after the earliest prerequisite. This plan repairs those admission/test boundaries rather than patching rendered symptoms.

No API capability is added or fabricated. Preserve the existing reasoned `COVERAGE.md` declaration: the collector continues using only the already-integrated read-only GitHub Actions metadata/workflow-content surfaces, while logs, artifact contents, and mutation capabilities remain opted out.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` binds each compatibility row to an immutable audited workflow revision/content digest plus exact repository, event, job identity, and missing prerequisite; unknown revisions/topologies are rejected.
- `scripts/ci/collect_ci_baseline.mjs` uses one exact identity resolver for DAG needs and runner contracts, with narrow enumerations for legitimate workflow matrix expansions and no general prefix admission.
- `scripts/ci/verify_ci_baseline.mjs` proves unknown pre-cutoff revisions, unknown historical jobs, malicious suffixes, and non-enumerated matrix suffixes fail before durable record emission.
- `scripts/ci/accrue_host_uat.sh` captures the initial `pg_isready` status explicitly and routes failure through the existing `fixture_or_database` registry entry before preserving the failure status.
- `scripts/ci/verify_ci_setup_diagnostics.sh` shadows `pg_isready` with a deterministic failure and proves the setup fact, owner-first rendering, exact repair command/evidence location, `FAILED_GATE=host-integration`, and absence of `mix verify.full` delegation.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one audited workflow revision and legitimate matrix job through trusted collection (D-01 through D-07)</name>
  <reversibility rating="reversible">This narrows internal read-only admission predicates and fixture seams without changing schema-v1, workflow topology, or a published API.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-16-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
    - .github/workflows/ci.yml
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .planning/phases/226-ci-baseline-proof-semantics/fixtures/ci-baseline-cases.json
  </read_first>
  <behavior>
    - A known historical run is accepted only when its immutable workflow revision/content digest, repository, event, exact normalized job identity, and exact absent prerequisite match one audited compatibility row.
    - The same date/repository/event/job with an unknown or mutated workflow revision is rejected, as is an unknown job under a known revision.
    - Exact non-matrix display identities resolve normally; Playwright shard identities resolve only for the workflow-declared enumerated shard set, and release matrix identities resolve only for the workflow-declared compatibility cells.
    - Appending an arbitrary suffix to host integration, release gate, admin guardrails, Docker smoke, or any matrix identity cannot acquire DAG needs or a runner class.
    - Accepted identity resolution feeds both `workflowNeeds()` and `workflowRunnerImage()` so their trust boundaries cannot drift.
  </behavior>
  <action>Start RED in the production-shaped live collector controls. Add attacks reproducing the verifier report: a pre-cutoff run with the right repository/event and a malicious host suffix, a known compatibility tuple with an unknown workflow revision, a known revision with an unknown topology, `Release gate attacker`, and Playwright suffixes outside the declared shard enumeration. Require all to fail before normalization/cohort output. Add positive controls for every audited historical compatibility row and the actual current matrix identities present in `.github/workflows/ci.yml`.

Replace the current date/repository/prefix compatibility lookup with an explicit immutable audit inventory. Each row must include a full immutable workflow revision or deterministic workflow-content digest, exact normalized job identity, exact prerequisite, allowed event, bounded era, and evidence reference. Populate `run.workflow_revision` on the live path from the workflow file at the run's immutable full `head_sha` using the existing read-only workflow-content capability; validate its format and bind the compatibility lookup to it. Never infer an exception from age alone. If the historical workflow content cannot be resolved or no exact row matches, leave the prerequisite unresolved and fail collection.

Introduce one `resolveWorkflowJobIdentity()`-style helper consumed by both `workflowNeeds()` and `workflowRunnerImage()`. Match non-matrix jobs exactly. For matrix jobs, generate or validate only the finite identities declared by the workflow contract: current release compatibility cells and Playwright shard values from the workflow matrix. Do not retain any generic `startsWith()` fallback. Keep raw display names, workflow content, runner names/labels, actors, raw branches, logs, secrets, payloads, artifact contents, and user data outside durable records. Preserve exact-attempt isolation, schema-v1 fields, queue/DAG arithmetic, sample selection, provider state, workflow topology, and all existing fixtures.</action>
  <acceptance_criteria>
    - Every historical exception carries and checks an immutable workflow revision/content digest plus exact job and prerequisite identities.
    - Unknown revision, unknown topology, malicious host suffix, release attacker suffix, and out-of-enumeration shard fixtures fail before record emission.
    - All audited historical rows and current legitimate matrix identities traverse `liveRuns()` → identity resolution → needs/runner resolution → `collectBaseline()` successfully.
    - No raw workflow contents, runner operational metadata, or newly forbidden sensitive field appears in schema-v1 output.
    - Existing exact-attempt, cohort, privacy, reliability, and critical-path fixtures remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>A legitimate current matrix job and one audited historical exception traverse the full production collector, while every unknown revision/topology or spoofed suffix fails closed before durable evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnose initial Postgres readiness failure before host delegation (D-14 through D-18)</name>
  <reversibility rating="reversible">The change adds an early branch to an internal shell wrapper and reuses the existing stable diagnostic registry without changing the host proof command.</reversibility>
  <files>scripts/ci/accrue_host_uat.sh, scripts/ci/verify_ci_setup_diagnostics.sh</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - scripts/ci/accrue_host_uat.sh
    - scripts/ci/ci_setup_diagnostic.sh
    - scripts/ci/verify_ci_setup_diagnostics.sh
    - scripts/ci/accrue_host_verify_browser.sh
    - examples/accrue_host/README.md
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - When `pg_isready` exists and exits nonzero, the wrapper emits exactly one `fixture_or_database` fact to `ACCRUE_CI_SETUP_FACTS`, renders `SETUP_CODE=fixture_or_database`, `OWNER=host`, its registry-owned next command/evidence location, prints `FAILED_GATE=host-integration`, and exits with the readiness status.
    - The initial readiness branch never invokes `mix verify.full` and never adds `host_gate_failure` on top of the narrower database fact.
    - Missing `pg_isready` preserves the existing optional preflight behavior; successful readiness still delegates exactly once and preserves the existing inner-fact/aggregate-fallback logic.
    - Diagnostic output and persisted facts contain no database host, port, user, password, database name, URL, environment value, or application data.
  </behavior>
  <action>Start RED by extending `assert_wrapper_diagnostic_contract()` with a shadow `pg_isready` executable that returns a distinctive nonzero status and a shadow `mix` executable that records any invocation. Run the real wrapper with a temporary `ACCRUE_CI_SETUP_FACTS`; assert one `fixture_or_database` row, literal owner-first render lines from the registry, the compatibility marker, preservation of the readiness status, zero `mix verify.full` calls, and no aggregate fact. Add privacy assertions using sensitive-looking PG environment values and prove none appears in output or facts.

In `accrue_host_uat.sh`, move the readiness call behind explicit status capture so `set -e` cannot terminate before diagnostics. On nonzero, call `ci_setup_diagnostic.sh emit fixture_or_database` with only fixed safe identity classes, then `render fixture_or_database`, print `FAILED_GATE=host-integration`, and exit with the captured status. Do not print the database connection arguments or environment values. Preserve the behavior when `pg_isready` is absent, the successful delegation path, the post-delegation fact-count/fallback logic, and the existing host-owned `mix verify.full` boundary.</action>
  <acceptance_criteria>
    - A failing shadowed `pg_isready` produces exactly one `fixture_or_database` fact and no `host_gate_failure` fact.
    - Output contains the exact registry-owned setup code, host owner, next command, evidence location, and `FAILED_GATE=host-integration`.
    - The wrapper returns the readiness status and does not invoke `mix verify.full` on this branch.
    - Sensitive-looking PG environment values appear in neither output nor facts.
    - Existing inner failure, aggregate failure, success, browser-boundary, workflow-summary, and registry fixtures remain green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/accrue_host_uat.sh &amp;&amp; bash -n scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures</automated>
  </verify>
  <done>An initial Postgres readiness failure is owner-first, privacy-safe, fact-backed, gate-marked, and returned without entering the host proof command.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical Actions metadata → compatibility inventory | Remote timestamps, names, events, and repository values cannot authorize a topology exception without an immutable audited workflow revision. |
| Workflow display identity → DAG/runner resolution | Untrusted display names may contain arbitrary suffixes; only exact or finite workflow-declared matrix identities can acquire trusted semantics. |
| Host environment → setup diagnostic | Database environment values cross an early failure path that must emit only fixed privacy-safe registry facts. |
| BASE-01/OWN-01 repair → verified BASE-02 behavior | Focused repairs must leave provider proof classification and existing workflow truth boundaries intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-226-50 | Spoofing | historical compatibility admission | high | mitigate | Bind each exception to a full immutable workflow revision/content digest and exact tuple; reject unknown revisions/topologies with production-path controls. |
| T-226-51 | Spoofing | shared workflow job resolver | high | mitigate | Use exact identities or finite workflow-derived matrix expansions and remove arbitrary prefix admission for both DAG and runner resolution. |
| T-226-52 | Tampering | cohort membership and DAG facts | high | mitigate | Fail before normalization when revision/identity resolution is absent or ambiguous; retain exact-attempt and frozen-record validators. |
| T-226-53 | Information Disclosure | readiness diagnostic | high | mitigate | Render fixed registry values and bounded fact identities only; adversarial PG environment values must appear in neither stderr nor NDJSON. |
| T-226-54 | Repudiation | host failure attribution | medium | mitigate | Emit exactly one stable `fixture_or_database` fact, owner, next command, evidence location, exit status, and gate marker before delegation. |
| T-226-55 | Denial of Service | workflow revision lookup | low | accept | Collection is bounded to the 90-day window and fails with a precise read-only error; it never weakens admission or writes partial canonical evidence. |

No package install or third-party dependency is introduced, so the package-legitimacy supply-chain row/checkpoint does not apply. No supported ORM schema path is modified, so schema-push detection does not add a task.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable privacy-safe evidence distinguishes critical path, setup ownership, and provider proof | 17, 18 | COVERED | Plan 17 repairs trust boundaries; Plan 18 recollects and proves the canonical account. |
| REQ | BASE-01 | Comparable timing/cache/provider/root-failure baseline | 17, 18 | COVERED | Exact revision/identity admission precedes fresh collection and byte proof. |
| REQ | BASE-02 | Required/skipped/advisory evidence differs | 17, 18 | COVERED | Preservation regressions remain exhaustive; Plan 18 reruns them with the final artifacts. |
| REQ | OWN-01 | Host/CI setup ownership and failure diagnostics | 17, 18 | COVERED | Initial Postgres failure receives a stable owner-first fact; final suite preserves all setup paths. |
| RESEARCH | — | Dependency-free read-only Actions collection | 17, 18 | COVERED | Existing Node built-ins and authenticated read-only workflow metadata/content are reused. |
| RESEARCH | — | Strict schema/privacy boundary and deterministic negative controls | 17, 18 | COVERED | Adversarial revision/identity/readiness controls precede frozen evidence replacement. |
| RESEARCH | — | Byte-reproducible frozen critical-path evidence | 18 | COVERED | Temporary collection, dual render, committed-pair validation, and contrary-result support are explicit. |
| CONTEXT | D-01–D-07 | Canonical comparable evidence and validation | 17, 18 | COVERED | Direct repair plus recollection. |
| CONTEXT | D-08–D-13 | Provider proof semantics | 17, 18 | COVERED | Preservation-only regression gates. |
| CONTEXT | D-14–D-18 | Host/CI ownership and browser proof | 17, 18 | COVERED | Direct readiness fix plus preservation gates. |
| CONTEXT | D-19–D-21 | Action-first accessible durable interface | 18 | COVERED | Existing renderer is regenerated and byte-checked from trusted data. |
| CONTEXT | Deferred ideas | Optimization, cache/topology/matrix/gate/test changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | No task implements deferred scope. |

<verification>
Run the baseline production-path fixtures and setup diagnostic suite. Both must prove their reproduced attacks fail before any canonical evidence write, while provider and inherited proof behavior stays untouched for Plan 18's full preservation gate.
</verification>

<success_criteria>
- Historical compatibility is revision-bound and exact; unknown historical topology is rejected.
- Workflow needs and runner resolution accept only exact or finitely enumerated matrix identities; spoofed suffixes are rejected.
- Initial Postgres readiness failure emits/renders one stable privacy-safe host diagnostic and the gate marker before exiting.
- BASE-01, BASE-02, and OWN-01 spec-less assumptions and descriptor-less prohibitions remain explicit and flagged-unverified.
- No provider, topology, cache, matrix, branch-protection, setup-ownership, Playwright, or test-strength contract is weakened.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-17-SUMMARY.md` when done.
</output>

---
phase: 226-ci-baseline-proof-semantics
plan: "21"
type: gap_closure
wave: 15
depends_on: ["226-20"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - scripts/ci/README.md
  - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-01/D-06/D-07/D-19/D-20: Every renderer and verifier invocation receives an independently supplied expected repository; snapshot.repository must equal that input before any run_url or job_url validation or Markdown construction."
    - "D-01/D-06/D-07/D-19/D-20: A forged snapshot plus matching valid-looking forged-repository run and job URLs fails through the production renderer CLI and leaves no report at the requested output path."
    - "D-01 through D-07: Canonical same-repository records, immutable run/job ID matching, historical workflow provenance, critical-path derivation, privacy filtering, and byte-deterministic rendering remain green without recollecting or rewriting the frozen baseline."
    - "D-08 through D-18: BASE-02 provider proof semantics and OWN-01 host/CI setup ownership are preservation-only assumptions in this closure and remain unchanged."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Immutable Actions URL and snapshot validation bound to an explicit repository trust input independent of persisted NDJSON"
      exports: ["createRepositoryValidationContext", "validateRecord"]
    - path: "scripts/ci/render_ci_baseline.mjs"
      provides: "Production record-set validation that consumes independently supplied repository context before interpolating evidence links"
      exports: ["renderBaseline"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Production-renderer negative controls for forged snapshot plus forged run/job evidence, URL-only forgery, omitted trust context, and same-repository success"
      exports: ["verifyFixtures"]
    - path: "scripts/ci/README.md"
      provides: "Maintainer verifier commands carrying explicit expected-repository trust input"
    - path: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"
      provides: "Regenerated deterministic report whose reproduction commands carry explicit repository trust input while measured evidence stays unchanged"
  key_links:
    - from: "CLI --expected-repository / collector --repo"
      to: "validateRecord(record, repository validation context)"
      via: "createRepositoryValidationContext validates an independent owner/repo input; validateRecord requires snapshot.repository equality before applying the same context to run_url and job_url"
      pattern: "expected-repository|createRepositoryValidationContext|snapshot.repository|validateRecord|immutableUrl"
    - from: "snapshot-less direct renderer and staged-path fixtures"
      to: "renderBaseline(records, validationContext) and deriveStagedPathPercentiles(records, validationContext)"
      via: "one frozen acme/accrue fixture context is passed explicitly at every existing direct call site; persisted snapshot content is never accepted as the source of trust"
      pattern: "createRepositoryValidationContext|renderBaseline|deriveStagedPathPercentiles"
    - from: "scripts/ci/verify_ci_baseline.mjs cross-repository fixture"
      to: "scripts/ci/render_ci_baseline.mjs --input/--out"
      via: "both URL-only substitutions and a forged snapshot with matching forged run/job URLs traverse the production CLI with --expected-repository acme/accrue and fail before writeFileSync creates the requested report"
      pattern: "spawnSync|render_ci_baseline|existsSync"
    - from: "renderer/verifier maintainer CLI call sites"
      to: "required independent repository context"
      via: "update renderer usage/main, verifier spawn and main, renderer Reproduce output, scripts/ci/README.md, and the regenerated canonical Markdown; source discovery found no .github workflow or package.json invocations"
      pattern: "--expected-repository"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "The spec-less edge probe returned BASE-01 unresolved/unclassified; this plan closes only the verifier-authoritative cross-repository false-evidence path and infers no additional baseline scope."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "The spec-less edge probe returned BASE-02 unresolved/unclassified; provider proof is already verified and is preserved without policy, classifier, formatter, workflow, or documentation changes."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "The spec-less edge probe returned OWN-01 unresolved/unclassified; setup ownership is already verified and is preserved without host wrapper, Playwright, provisioning, diagnostic, or documentation changes."
  prohibitions:
    - requirement_id: BASE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT render a run or job evidence link whose repository provenance is unrelated to the validated snapshot or its explicit immutable repository allowlist."
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT broaden the checked-in evidence surface to actors, raw branch names, logs, credentials, provider payloads, artifact contents, or user data while repairing repository provenance."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT reinterpret a raw job conclusion or non-run, skipped, blocked, misconfigured, failed, stale, Fake-backed, or different-SHA evidence as live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT transfer the host-owned Playwright proof contract to CI or change setup ownership, workers, retries, artifacts, provisioning, diagnostics, or canonical host commands in this closure."
---

<objective>
Bind every persisted GitHub Actions snapshot/run/job record to an independently supplied expected repository and prove the production renderer rejects a forged snapshot with matching forged-repository evidence without producing a report (D-01, D-06, D-07, D-19, D-20).

Purpose: Close the sole remaining BASE-01 verification blocker while preserving all already-verified historical-topology, deterministic baseline, provider-proof, and setup-ownership behavior.
Output: Explicit repository trust context across collector/renderer/verifier boundaries, updated CLI call sites, and production-renderer negative controls for forged snapshot plus forged run/job URLs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/README.md

Authoritative scope: `226-VERIFICATION.md` leaves one failed truth: syntactically valid Actions URLs from arbitrary repositories pass `validateRecord()` and are rendered. BASE-01 is implementation scope. BASE-02 and OWN-01 are already verified and remain explicit spec-less preservation assumptions. No external API integration, recollection, canonical NDJSON-fact rewrite, workflow change, or Phase 227 optimization belongs in this closure; only the generated Markdown reproduction commands change to carry the new trust argument.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` creates repository validation context only from the caller's explicit repository input and requires persisted snapshot.repository plus run/job URLs to match it.
- `scripts/ci/render_ci_baseline.mjs` requires `--expected-repository owner/repo` at the CLI and an explicit validation context in exported functions; it never derives trust from NDJSON.
- `scripts/ci/verify_ci_baseline.mjs` propagates the same explicit context through renderer, staged-path, critical-path, and production-CLI negative controls.
- `scripts/ci/README.md` and the renderer-generated reproduction block carry the required CLI option. Regenerate only `226-CI-BASELINE.md` so its command text stays byte-reproducible; the canonical NDJSON facts remain frozen and unchanged. Source discovery found no `.github/workflows/**` or `package.json` invocation to modify.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject cross-repository evidence through the production renderer (D-01, D-06, D-07, D-19, D-20)</name>
  <reversibility rating="reversible">The change tightens internal validation and fixture coverage, adds a required repository-provenance CLI argument, and updates generated reproduction text without changing schema-v1 fields, canonical NDJSON facts, or external product contracts.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs, scripts/ci/README.md, .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-PATTERNS.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - scripts/ci/verify_executable_uat_contract.mjs
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/README.md
  </read_first>
  <behavior>
    - "RED: with --expected-repository acme/accrue, changing snapshot.repository to attacker/forged and changing all run_url/job_url destinations to matching attacker/forged paths while retaining numeric IDs makes the production renderer CLI exit nonzero and create no output file."
    - "RED: with snapshot.repository still acme/accrue, URL-only foreign-repository substitutions for a run and a job independently exit nonzero and create no output file."
    - "GREEN: every canonical same-repository run/job URL remains accepted; existing URL-shape, run-ID, job-ID, newline, Markdown-link, timestamp, arithmetic, historical-topology, privacy, and critical-path controls remain green."
    - "Omitting --expected-repository at the renderer or verifier CLI, omitting function validationContext, or passing a malformed expected repository fails closed; snapshot.repository never supplies a fallback."
    - "GREEN: snapshot-bearing and snapshot-less acme/accrue fixture records remain accepted only when renderBaseline, deriveStagedPathPercentiles, and validateRecord receive the shared frozen acme/accrue validation context."
  </behavior>
  <action>Use `scripts/ci/verify_executable_uat_contract.mjs`, the `226-PATTERNS.md` assigned analog for all three modified scripts, as the implementation-shape contract. New or updated regression logic must remain dependency-free ESM: order `node:` built-in imports before repository-local imports, keep validation in small named helpers, and add no package dependency. Follow the analog's deterministic fixture lifecycle by creating forged-evidence inputs beneath one `fs.mkdtempSync()` directory and enclosing every write, production CLI invocation, and assertion in `try/finally` whose `fs.rmSync(temp, { recursive: true, force: true })` cleanup runs on both pass and failure. Preserve one `main()` argument dispatcher and one top-level failure boundary per executable: thrown validation/assertion failures cross that boundary, emit the script's stable `FAIL:` prefix, and set `process.exitCode = 1`; do not add nested process exits or nondeterministic cleanup paths.

Start RED in `verifyFixtures()` by extending the existing temporary forged-NDJSON production-path control, not by adding a parallel harness. Define a valid complete `snapshot + run + job` record set for `acme/accrue`. First create a simultaneous-forgery case: change `snapshot.repository` to `attacker/forged` and change every run_url and job_url to a syntactically valid `attacker/forged` Actions path while retaining the same numeric run/job IDs. Invoke the production renderer with `--expected-repository acme/accrue --input ... --out ...`; require nonzero status, require an expected-versus-snapshot repository mismatch error, and prove the output path does not exist. Retain separate URL-only run and job substitutions while the snapshot stays `acme/accrue`, the existing newline/link-closing case, and a same-repository green invocation. Every negative invocation must pass the independent expected repository except the explicit omitted-context case, which must also exit nonzero and leave no output.

Export `createRepositoryValidationContext({ expectedRepository })` from `collect_ci_baseline.mjs`. Require exactly one strict `owner/repository` string, canonicalize no aliases, and freeze both the copied repository value and returned branded/context object. It must not accept `snapshotRepository`, allowlist arrays, NDJSON records, URLs, Git remote state, actor/branch data, or process environment as trust sources. Change `validateRecord(record, validationContext)` so every record kind requires a constructor-produced context. Validate snapshot schema and require `record.repository === validationContext.expectedRepository` before validating any run/job record set. Extend `immutableUrl` to receive the context, parse after the current exact HTTPS/path constraints, compare the decoded owner/repository pair exactly with `expectedRepository`, and retain numeric run/job ID matching. Missing or structurally forged contexts fail with a repository-validation-context error.

Update the exported signatures to require `renderBaseline(records, validationContext)` and `deriveStagedPathPercentiles(records, validationContext)`. Each boundary validates the supplied context before iterating records and never constructs context from records. Pre-scan snapshots before validating any run/job URL: reject multiple snapshots; when one exists, validate it against the context first; snapshot-less fixture collections remain permitted only with explicit context. Pass the same object from `renderBaseline` into its internal staged-path call. In renderer `main()`, require exactly one snapshot plus `--expected-repository owner/repo`, validate both before calling `renderBaseline`, and fail on omitted/malformed input before output creation.

Enumerate and update every direct renderer/derive call in `verify_ci_baseline.mjs`. Import the constructor and `validateRecord`, define `ACME_FIXTURE_VALIDATION_CONTEXT` from `expectedRepository: "acme/accrue"`, and pass it to the `verifyFixtures()` render plus every `stagedPathControls()` call: initial records derive/render, contrary, missingHost, badOrderRecords, rerun, nineteen-run, multiFingerprintRecords derive/render, duplicateIdentity, and scheduledCompatible. Add a validation-context parameter to `verifyCriticalPath`. Require `--expected-repository` in verifier `main()` for both `--fixtures` and `--records`, construct the context only from that argument, validate the canonical snapshot equality, then pass it to `renderBaseline` and `verifyCriticalPath`. Add direct omitted-context assertions for `validateRecord`, `renderBaseline`, and `deriveStagedPathPercentiles`, plus same-repository green controls.

At collector serialization, require the existing `--repo owner/repo` for both live and local `--input`/`--fixtures` CLI modes, construct the context from that explicit option before serialization, require a live snapshot to match it, and pass it to every `validateRecord` call. Do not derive context from the generated snapshot, raw run/job URLs, or fixture content. Preserve the direct `collectBaseline()` API and all normalization behavior. Validation completes before collector `writeFileSync`.

Update every source CLI call site discovered: renderer usage/main; verifier's production-renderer `spawnSync`; verifier usage/main; the renderer's generated `## Reproduce` renderer and verifier commands; and both baseline verifier commands in `scripts/ci/README.md`. Use `acme/accrue` only for fixture commands and `szTheory/accrue` for canonical records. Regenerate `226-CI-BASELINE.md` from the unchanged NDJSON so only reproduction-command text changes and byte comparison remains authoritative. `rg` found no `.github/workflows/**` or `package.json` call site; assert that inventory remains absent rather than inventing one. Preserve schema-v1, canonical NDJSON facts, workflow topology, provider proof semantics, setup ownership, Playwright behavior, and Phase 227 optimization surfaces per D-01 through D-21.</action>
  <acceptance_criteria>
    - The task's `<read_first>` references both `226-PATTERNS.md` and its assigned analog, `scripts/ci/verify_executable_uat_contract.mjs`, before any of the three modified CI baseline scripts.
    - Static inspection of all three modified scripts confirms dependency-free ESM with `node:` built-ins before local imports and a single `main()`-level failure boundary that reports a stable `FAIL:` prefix and sets `process.exitCode = 1`.
    - `verifyFixtures()` keeps all forged-evidence temp files and production-renderer invocations inside the existing `fs.mkdtempSync()` `try/finally` lifecycle, with recursive forced cleanup exactly as the analog demonstrates; the fixture command passing confirms cleanup also survives every negative control.
    - A forged `attacker/forged` snapshot accompanied by matching valid-looking `attacker/forged` run and job URLs is rejected against `--expected-repository acme/accrue` by the production renderer CLI, which exits nonzero and leaves no output file.
    - Separate URL-only foreign-repository run and job substitutions remain rejected with no output; the unchanged same-repository snapshot/run/job set renders successfully.
    - The trusted repository originates only from explicit `--expected-repository` or collector `--repo` caller input; missing, malformed, or snapshot-derived trust context fails closed.
    - Every existing direct call in `verifyFixtures()` and `stagedPathControls()` passes `ACME_FIXTURE_VALIDATION_CONTEXT`; the canonical `--records` path constructs context from its explicit CLI option and requires its sole snapshot to match before `renderBaseline` or `verifyCriticalPath`.
    - Fixture assertions prove all three snapshot-less boundaries (`validateRecord`, `renderBaseline`, and `deriveStagedPathPercentiles`) reject absent context, and prove their updated same-repository direct calls accept the frozen `acme/accrue` context.
    - Collector serialization uses an explicit `--repo`-derived context in live and local modes; absent `--repo` or a live snapshot mismatch exits nonzero before output creation.
    - Renderer/verifier usage, verifier spawn, generated reproduction commands, and `scripts/ci/README.md` all pass the appropriate explicit repository; no workflow/package call site exists. The unchanged canonical NDJSON regenerates the checked-in Markdown byte-identically after its reproduction commands are updated.
    - BASE-01, BASE-02, and OWN-01 spec-less unresolved classifications and the descriptor-less must-NOT items remain visible in plan metadata rather than silently dropped.
  </acceptance_criteria>
  <verify>
    <automated>rg -Fq 'scripts/ci/verify_executable_uat_contract.mjs' .planning/phases/226-ci-baseline-proof-semantics/226-21-PLAN.md &amp;&amp; node -e 'const fs=require("node:fs"); for (const file of ["scripts/ci/collect_ci_baseline.mjs","scripts/ci/render_ci_baseline.mjs","scripts/ci/verify_ci_baseline.mjs"]) { const source=fs.readFileSync(file,"utf8"); const local=source.search(/^import .*from "\.\.?\//m); const builtIn=source.search(/^import .*from "node:/m); if (builtIn&lt;0 || (local&gt;=0 &amp;&amp; builtIn&gt;local) || !/\bfunction main\(/.test(source) || !/FAIL:/.test(source) || !/process\.exitCode\s*=\s*1/.test(source)) throw new Error(`${file}: analog ESM/failure-boundary contract missing`); } const verifier=fs.readFileSync("scripts/ci/verify_ci_baseline.mjs","utf8"); if (!/mkdtempSync/.test(verifier) || !/finally\s*\{[\s\S]*?rmSync\([^)]*\{\s*recursive:\s*true,\s*force:\s*true\s*\}/.test(verifier)) throw new Error("verify_ci_baseline.mjs: deterministic fixture cleanup contract missing"); for (const file of ["scripts/ci/render_ci_baseline.mjs","scripts/ci/verify_ci_baseline.mjs","scripts/ci/README.md",".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md"]) if (!fs.readFileSync(file,"utf8").includes("--expected-repository")) throw new Error(`${file}: explicit repository CLI context missing`);' &amp;&amp; node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh</automated>
  </verify>
  <done>Maintainers can render only evidence whose snapshot and URLs match an independently supplied repository context; simultaneous snapshot/run/job forgery, URL-only forgery, and omitted context fail before output creation, every CLI call site carries the context, and every previously verified Phase 226 contract stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted NDJSON → semantic validator | Repository-controlled or tampered records are untrusted until schema, arithmetic, identity, and repository provenance checks pass. |
| Caller CLI/API trust input → persisted NDJSON | The expected repository is supplied independently; untrusted snapshot.repository must match it before any persisted evidence can be accepted. |
| Semantic validator → Markdown renderer | Only snapshots and URLs bound to the independently supplied expected repository may become clickable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-226-21-01 | Spoofing | snapshot plus `run_url` / `job_url` provenance | high | mitigate | Bind snapshot and URL owner/repository to required caller-supplied context; production tests forge all persisted fields together and still block rendering. |
| T-226-21-02 | Tampering | persisted NDJSON → `renderBaseline()` | high | mitigate | Run complete record-set validation before Markdown construction and prove the production CLI leaves no output for foreign evidence. |
| T-226-21-03 | Information Disclosure | validation errors and fixtures | medium | mitigate | Report field/provenance failure without serializing credentials, actors, raw branches, payloads, logs, or artifact contents. |
| T-226-21-04 | Denial of Service | malformed repository trust context | low | accept | Fail closed with a bounded deterministic validation error; no long-running or remote operation is introduced. |
| T-226-21-SC | Tampering | package installs | high | mitigate | No package installation or dependency change is permitted by this plan. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Durable privacy-safe CI evidence distinguishes critical path, ownership, and provider proof | 21 | COVERED | Closes the final false-evidence path; already-verified ownership/provider truths are preservation gates. |
| REQ | BASE-01 | Durable privacy-safe comparable-run baseline | 21 | COVERED | Repository-bound evidence plus production negative controls close the authoritative blocker. |
| REQ | BASE-02 | Required, skipped, and advisory provider evidence remain distinct | 21 | COVERED | Explicit flagged assumption and unchanged provider fixture gate preserve verified behavior. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics remain legible | 21 | COVERED | Explicit flagged assumption and unchanged setup diagnostic gate preserve verified behavior. |
| RESEARCH | — | Versioned NDJSON → deterministic validated Markdown with privacy allowlist | 21 | COVERED | Existing validator/renderer path gains repository provenance; no new dependency or external API. |
| RESEARCH | — | Untrusted API/persisted data cannot write false Markdown evidence | 21 | COVERED | Independent trust input plus production renderer controls cover simultaneous snapshot/run/job forgery and URL-only tampering. |
| CONTEXT | D-01–D-07 | Canonical evidence, comparability, arithmetic, privacy, validation | 21 | COVERED | D-01/D-06/D-07 are repaired; D-02–D-05 stay protected by the complete baseline suite. |
| CONTEXT | D-08–D-13 | Provider policy/proof/freshness remain literal and independent | 21 | COVERED | Preservation-only; complete provider fixture gate remains required. |
| CONTEXT | D-14–D-18 | Host/CI setup ownership and Playwright proof stay unchanged | 21 | COVERED | Preservation-only; setup diagnostic gate remains required. |
| CONTEXT | D-19–D-21 | Truthful, privacy-safe, deterministic maintainer evidence | 21 | COVERED | Foreign evidence cannot become a Markdown claim; no dashboard or product UI is introduced. |
| CONTEXT | Deferred ideas | Optimization, caching, topology/matrix/gate changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | Explicitly outside Phase 226 and absent from tasks. |

<verification>
Run the dependency-free Node syntax checks, the complete baseline fixture suite with explicit fixture repository, byte-reproduction/critical-path validation with explicit canonical repository, the complete provider fixture suite, and owner-first setup diagnostics. Completion requires simultaneous forged snapshot/run/job evidence, both URL-only cases, and omitted-context cases to traverse production CLIs, exit nonzero for repository provenance, and leave no requested report.
</verification>

<success_criteria>
- Snapshot.repository and every run/job evidence URL are bound to an independently supplied expected repository before Markdown rendering.
- Simultaneously forged snapshot/run/job evidence, URL-only substitutions, and omitted context are rejected through production CLIs and produce no report; same-repository controls pass.
- The canonical NDJSON facts remain unchanged; regenerated Markdown is byte-reproducible with updated trust-bearing reproduction commands, and historical topology, critical-path, privacy, provider, and setup contracts stay green.
- No schema, workflow, package script, external API, dependency, provider-policy, setup-ownership, Playwright, or Phase 227 optimization scope changes.
- BASE-01, BASE-02, and OWN-01 unresolved spec-less assumptions and surviving must-NOT items remain explicit.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-21-SUMMARY.md` when done.
</output>

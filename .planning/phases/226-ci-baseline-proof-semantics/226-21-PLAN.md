---
phase: 226-ci-baseline-proof-semantics
plan: "21"
type: gap_closure
wave: 15
depends_on: ["226-20"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/render_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
autonomous: true
requirements: [BASE-01, BASE-02, OWN-01]
gap_closure: true
must_haves:
  truths:
    - "D-01/D-06/D-07/D-19/D-20: Every persisted run and job evidence URL is bound to the repository declared by its validated snapshot, or to an explicit immutable repository allowlist supplied by the same trusted validation context, before Markdown rendering begins."
    - "D-01/D-06/D-07/D-19/D-20: A syntactically valid GitHub Actions run or job URL for another repository fails through the production renderer CLI and leaves no report at the requested output path."
    - "D-01 through D-07: Canonical same-repository records, immutable run/job ID matching, historical workflow provenance, critical-path derivation, privacy filtering, and byte-deterministic rendering remain green without recollecting or rewriting the frozen baseline."
    - "D-08 through D-18: BASE-02 provider proof semantics and OWN-01 host/CI setup ownership are preservation-only assumptions in this closure and remain unchanged."
  artifacts:
    - path: "scripts/ci/collect_ci_baseline.mjs"
      provides: "Repository-aware immutable Actions URL validation that can bind run/job destinations to trusted snapshot provenance"
      exports: ["createRepositoryValidationContext", "validateRecord"]
    - path: "scripts/ci/render_ci_baseline.mjs"
      provides: "Production record-set validation that establishes the trusted repository context before interpolating evidence links"
      exports: ["renderBaseline"]
    - path: "scripts/ci/verify_ci_baseline.mjs"
      provides: "Production-renderer cross-repository negative control for both valid-looking run and job evidence URLs"
      exports: ["verifyFixtures"]
  key_links:
    - from: "snapshot.repository"
      to: "validateRecord(record, repository validation context)"
      via: "renderBaseline validates snapshot provenance first, then applies the resulting immutable repository allowlist to every run_url and job_url"
      pattern: "snapshot|repository|validateRecord|immutableUrl"
    - from: "snapshot-less direct renderer and staged-path fixtures"
      to: "renderBaseline(records, validationContext) and deriveStagedPathPercentiles(records, validationContext)"
      via: "one frozen acme/accrue fixture context is passed explicitly at every existing direct call site; calls with URL-bearing records and neither a snapshot nor explicit context fail closed"
      pattern: "createRepositoryValidationContext|renderBaseline|deriveStagedPathPercentiles"
    - from: "scripts/ci/verify_ci_baseline.mjs cross-repository fixture"
      to: "scripts/ci/render_ci_baseline.mjs --input/--out"
      via: "a valid-looking foreign repository URL traverses the production CLI and must fail before writeFileSync creates the requested report"
      pattern: "spawnSync|render_ci_baseline|existsSync"
  assumptions:
    - requirement_id: BASE-01
      status: flagged-unverified
      flag: "The spec-less edge probe returned BASE-01 unresolved/unclassified; this plan closes only the verifier-authoritative cross-repository false-evidence path and infers no additional baseline scope."
    - requirement_id: BASE-02
      status: flagged-unverified
      flag: "The spec-less edge probe returned BASE-02 unresolved/unclassified; provider proof is already verified and is preserved without policy, classifier, formatter, workflow, or documentation changes."
    - requirement_id: OWN-01
      status: flagged-unverified
      flag: "The spec-less edge probe returned OWN-01 unresolved/unclassified; setup ownership is already verified and is preserved without host wrapper, Playwright, provisioning, diagnostic, or documentation changes."
  prohibitions:
    - requirement_id: BASE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT render a run or job evidence link whose repository provenance is unrelated to the validated snapshot or its explicit immutable repository allowlist."
    - requirement_id: BASE-01
      category: privacy
      status: flagged-unverified
      statement: "MUST NOT broaden the checked-in evidence surface to actors, raw branch names, logs, credentials, provider payloads, artifact contents, or user data while repairing repository provenance."
    - requirement_id: BASE-02
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT reinterpret a raw job conclusion or non-run, skipped, blocked, misconfigured, failed, stale, Fake-backed, or different-SHA evidence as live-provider proof."
    - requirement_id: OWN-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT transfer the host-owned Playwright proof contract to CI or change setup ownership, workers, retries, artifacts, provisioning, diagnostics, or canonical host commands in this closure."
---

<objective>
Bind every persisted GitHub Actions run/job evidence URL to trusted snapshot repository provenance and prove the production renderer rejects valid-looking cross-repository evidence without producing a report (D-01, D-06, D-07, D-19, D-20).

Purpose: Close the sole remaining BASE-01 verification blocker while preserving all already-verified historical-topology, deterministic baseline, provider-proof, and setup-ownership behavior.
Output: Repository-aware record validation plus a production-renderer negative-control regression for foreign run and job URLs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CONTEXT.md
@.planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs

Authoritative scope: `226-VERIFICATION.md` leaves one failed truth: syntactically valid Actions URLs from arbitrary repositories pass `validateRecord()` and are rendered. BASE-01 is implementation scope. BASE-02 and OWN-01 are already verified and remain explicit spec-less preservation assumptions. No external API integration, collection, canonical evidence rewrite, workflow change, or Phase 227 optimization belongs in this closure.
</context>

## Artifacts this phase produces

- `scripts/ci/collect_ci_baseline.mjs` validates immutable run/job URLs against trusted repository provenance in addition to URL shape and numeric run/job identity.
- `scripts/ci/render_ci_baseline.mjs` derives or accepts one explicit immutable repository allowlist from the validated snapshot context and completes validation before producing Markdown.
- `scripts/ci/verify_ci_baseline.mjs` exercises the production renderer CLI with valid-looking foreign-repository run and job URLs and proves each invocation exits nonzero with no report.
- No canonical NDJSON/Markdown, workflow, provider, setup, documentation, or external API artifact changes in this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject cross-repository evidence through the production renderer (D-01, D-06, D-07, D-19, D-20)</name>
  <reversibility rating="reversible">The change tightens internal validation and fixture coverage without changing schema-v1 fields, CLI flags, persisted canonical evidence, or external contracts.</reversibility>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/render_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-20-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
  </read_first>
  <behavior>
    - "RED: starting from a valid fixture record set with a validated snapshot repository, replacing one run_url owner/repository while retaining its numeric run ID makes the production renderer CLI exit nonzero and create no output file."
    - "RED: the same valid-looking foreign repository substitution in one job_url, while retaining both numeric run and job IDs, also exits nonzero and creates no output file."
    - "GREEN: every canonical same-repository run/job URL remains accepted; existing URL-shape, run-ID, job-ID, newline, Markdown-link, timestamp, arithmetic, historical-topology, privacy, and critical-path controls remain green."
    - "A record set without one unambiguous trusted snapshot repository, or with a malformed explicit allowlist, fails closed rather than deriving trust from the untrusted run/job URL itself."
    - "GREEN: snapshot-less `acme/accrue` fixture records remain accepted only when `renderBaseline`, `deriveStagedPathPercentiles`, or `validateRecord` receives the shared frozen `acme/accrue` validation context; omitting that context rejects the same run/job records."
  </behavior>
  <action>Start RED in `verifyFixtures()` by extending the existing temporary forged-NDJSON production-path control, not by adding a parallel test harness. Build each case from the complete valid `snapshot + run + job` fixture set so repository trust comes from `snapshot.repository`. For the first case, replace a valid run record's evidence destination with `https://github.com/attacker/forged/actions/runs/{the same run_id}`. For the second, replace a valid job record's destination with `https://github.com/attacker/forged/actions/runs/{the same run_id}/job/{the same job_id}`. Invoke `render_ci_baseline.mjs --input ... --out ...` separately for each case, require a nonzero status, assert the error identifies repository provenance rather than URL syntax or numeric mismatch, and assert the requested report path does not exist. Retain the existing newline/link-closing negative control and the valid render assertion.

Then make repository provenance part of the existing validation path with this concrete API. Export `createRepositoryValidationContext({ snapshotRepository, allowedRepositories } = {})` from `collect_ci_baseline.mjs`; require exactly one source, accept either one already-validated `owner/repository` snapshot string or a non-empty array of explicit `owner/repository` strings, reject malformed/duplicate/ambiguous entries, copy/sort/freeze the repository array, and freeze the returned context. Change `validateRecord(record, validationContext = null)` so snapshot/cohort records retain their current validation, but every run/job record requires a context produced by that constructor and fails with a repository-validation-context error when it is absent or malformed. Extend `immutableUrl` to receive the context, parse the URL with the platform URL parser after the current exact HTTPS/path constraints, compare its decoded owner/repository pair exactly against the frozen allowlist, and continue enforcing matching run/job IDs. Do not infer allowed repositories from run_url/job_url, current Git remote state, actor/branch data, or mutable process environment.

Update the exported signatures to `renderBaseline(records, validationContext = null)` and `deriveStagedPathPercentiles(records, validationContext = null)`. At each boundary, resolve one context before `records.forEach`: when an explicit constructor-produced context is supplied, use it; otherwise validate that the record set contains exactly one snapshot and construct the context from that snapshot's `repository`. Zero or multiple snapshots with no explicit context, or any URL-bearing record with neither source, must fail closed. Pass the resolved context from `renderBaseline` into its internal `deriveStagedPathPercentiles` call so both paths enforce the same provenance and do not independently infer trust.

Enumerate and update every existing direct renderer/derive call in `verify_ci_baseline.mjs`. Define one module-level `ACME_FIXTURE_VALIDATION_CONTEXT` using `createRepositoryValidationContext({ allowedRepositories: Object.freeze(["acme/accrue"]) })`. Pass it to the `verifyFixtures()` `renderBaseline(records)` happy-path call and to every snapshot-less call in `stagedPathControls()`: the initial `records` derive/render pair; `contrary`; `missingHost`; `badOrderRecords`; `rerun`; the nineteen-run case; `multiFingerprintRecords` derive/render pair; `duplicateIdentity`; and `scheduledCompatible`. For `verifyCriticalPath(records, rendered)`, add a validation-context parameter and pass it to its direct derive call. In verifier `main()` for `--records`, derive one context from the canonical snapshot, pass it to both `renderBaseline` and `verifyCriticalPath`, and retain snapshot-origin coverage. Also add focused assertions that `renderBaseline(records)`, `deriveStagedPathPercentiles(records)`, and `validateRecord(records.find(kind run/job))` reject the snapshot-less fixture when no context is supplied, while the same-repository calls with `ACME_FIXTURE_VALIDATION_CONTEXT` succeed.

Update collector serialization at the existing `[snapshot, ...records, ...cohorts].map(validateRecord)` boundary: live collection constructs one context from the validated `snapshot.repository`; local `--input`/`--fixtures` serialization constructs it only from an explicitly supplied `--repo` value and fails closed before writing URL-bearing output when `--repo` is absent. Pass that context to every run/job `validateRecord` call while allowing snapshot/cohort validation to retain their non-URL checks. Preserve the current CLI flag names and the direct `collectBaseline()` API; do not add URL-derived fallback trust. Validation must complete before renderer Markdown construction or collector `writeFileSync`, preserving the property that foreign evidence creates no requested output. Do not change schema-v1, canonical baseline records or Markdown, workflow topology, provider proof semantics, setup ownership, Playwright behavior, or Phase 227 optimization surfaces per D-01 through D-21.</action>
  <acceptance_criteria>
    - A syntactically valid foreign-repository run URL with the expected numeric run ID is rejected for repository provenance by the production renderer CLI, which exits nonzero and leaves no output file.
    - A syntactically valid foreign-repository job URL with the expected numeric run/job IDs is rejected by the same production path with no output file.
    - The trusted repository originates only from the validated snapshot or an explicit immutable allowlist; missing, empty, malformed, or ambiguous trust context fails closed.
    - Every existing snapshot-less direct call in `verifyFixtures()` and `stagedPathControls()` passes `ACME_FIXTURE_VALIDATION_CONTEXT`; the canonical `--records` path passes a context derived from its sole snapshot to `renderBaseline` and `verifyCriticalPath`.
    - Fixture assertions prove all three snapshot-less boundaries (`validateRecord`, `renderBaseline`, and `deriveStagedPathPercentiles`) reject absent context, and prove their updated same-repository direct calls accept the frozen `acme/accrue` context.
    - Collector serialization passes a snapshot-derived context for live collection or an explicit `--repo`-derived context for local input; URL-bearing local serialization without `--repo` exits nonzero before output creation.
    - Canonical same-repository fixtures and the checked-in NDJSON/Markdown pair still validate and render byte-identically; all existing historical-topology, injection, semantic, critical-path, provider, and setup behavior is unchanged.
    - BASE-01, BASE-02, and OWN-01 spec-less unresolved classifications and the descriptor-less must-NOT items remain visible in plan metadata rather than silently dropped.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/render_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh</automated>
  </verify>
  <done>Maintainers can render only same-repository allowlisted Actions evidence; every snapshot-less direct fixture call supplies the explicit frozen context, absent context and valid-looking foreign run/job links fail before output creation, and every previously verified Phase 226 contract stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted NDJSON → semantic validator | Repository-controlled or tampered records are untrusted until schema, arithmetic, identity, and repository provenance checks pass. |
| Semantic validator → Markdown renderer | Only records bound to the trusted snapshot repository or explicit immutable allowlist may become clickable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-226-21-01 | Spoofing | `run_url` / `job_url` provenance | high | mitigate | Bind URL owner/repository to validated snapshot provenance or an explicit immutable allowlist and block rendering on mismatch. |
| T-226-21-02 | Tampering | persisted NDJSON → `renderBaseline()` | high | mitigate | Run complete record-set validation before Markdown construction and prove the production CLI leaves no output for foreign evidence. |
| T-226-21-03 | Information Disclosure | validation errors and fixtures | medium | mitigate | Report field/provenance failure without serializing credentials, actors, raw branches, payloads, logs, or artifact contents. |
| T-226-21-04 | Denial of Service | malformed repository trust context | low | accept | Fail closed with a bounded deterministic validation error; no long-running or remote operation is introduced. |
| T-226-21-SC | Tampering | package installs | high | mitigate | No package installation or dependency change is permitted by this plan. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Durable privacy-safe CI evidence distinguishes critical path, ownership, and provider proof | 21 | COVERED | Closes the final false-evidence path; already-verified ownership/provider truths are preservation gates. |
| REQ | BASE-01 | Durable privacy-safe comparable-run baseline | 21 | COVERED | Repository-bound evidence plus production negative controls close the authoritative blocker. |
| REQ | BASE-02 | Required, skipped, and advisory provider evidence remain distinct | 21 | COVERED | Explicit flagged assumption and unchanged provider fixture gate preserve verified behavior. |
| REQ | OWN-01 | Host/CI setup ownership and diagnostics remain legible | 21 | COVERED | Explicit flagged assumption and unchanged setup diagnostic gate preserve verified behavior. |
| RESEARCH | — | Versioned NDJSON → deterministic validated Markdown with privacy allowlist | 21 | COVERED | Existing validator/renderer path gains repository provenance; no new dependency or external API. |
| RESEARCH | — | Untrusted API/persisted data cannot write false Markdown evidence | 21 | COVERED | Production renderer negative controls exercise valid-looking cross-repository tampering. |
| CONTEXT | D-01–D-07 | Canonical evidence, comparability, arithmetic, privacy, validation | 21 | COVERED | D-01/D-06/D-07 are repaired; D-02–D-05 stay protected by the complete baseline suite. |
| CONTEXT | D-08–D-13 | Provider policy/proof/freshness remain literal and independent | 21 | COVERED | Preservation-only; complete provider fixture gate remains required. |
| CONTEXT | D-14–D-18 | Host/CI setup ownership and Playwright proof stay unchanged | 21 | COVERED | Preservation-only; setup diagnostic gate remains required. |
| CONTEXT | D-19–D-21 | Truthful, privacy-safe, deterministic maintainer evidence | 21 | COVERED | Foreign evidence cannot become a Markdown claim; no dashboard or product UI is introduced. |
| CONTEXT | Deferred ideas | Optimization, caching, topology/matrix/gate changes, external vendor, StoreKit/iPhone/Crosswake/Admin ratchet | — | EXCLUDED | Explicitly outside Phase 226 and absent from tasks. |

<verification>
Run the dependency-free Node syntax checks, the complete baseline fixture suite, byte-reproduction/critical-path validation against the unchanged canonical pair, the complete provider fixture suite, and owner-first setup diagnostics. Completion requires both valid-looking foreign URL cases to traverse the production renderer CLI, exit nonzero for repository provenance, and leave no requested report.
</verification>

<success_criteria>
- All run/job evidence URLs are bound to the snapshot repository or an explicit immutable repository allowlist before Markdown rendering.
- Valid-looking cross-repository run and job URLs are rejected through the production CLI and produce no report.
- The canonical baseline remains byte-reproducible and its historical topology, critical-path, privacy, provider, and setup contracts remain green.
- No schema, workflow, canonical evidence, documentation, external API, dependency, provider-policy, setup-ownership, Playwright, or Phase 227 optimization scope changes.
- BASE-01, BASE-02, and OWN-01 unresolved spec-less assumptions and surviving must-NOT items remain explicit.
</success_criteria>

<output>
Create `.planning/phases/226-ci-baseline-proof-semantics/226-21-SUMMARY.md` when done.
</output>

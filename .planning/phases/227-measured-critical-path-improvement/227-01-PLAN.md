---
phase: 227-measured-critical-path-improvement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/verify_ci_critical_path.mjs
  - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
  - .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json
  - .planning/phases/227-measured-critical-path-improvement/COVERAGE.md
autonomous: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "D-01/D-03: host-integration has exactly one prerequisite, docs-contracts-shift-left, while playwright-e2e still depends on host-integration and no other workflow edge changes."
    - "D-02/D-08: release/admin and host/browser remain independent required work, and the unchanged annotation-sweep fan-in is deterministically exercised by a negative fixture that records aggregate failure after independent host/browser completion."
    - "D-09: exact required job IDs/display names, release compatibility/support labels, provider policy/proof labels, artifact names, upload conditions, if-no-files-found values, and retention semantics are manifest-guarded."
    - "D-07: the three frozen Phase 226 files retain their recorded SHA-256 digests and are never written by Phase 227 tooling."
  artifacts:
    - path: "scripts/ci/verify_ci_critical_path.mjs"
      provides: "Dependency-free workflow/manifest/evidence verifier and fixture runner"
      exports: ["verifyWorkflowContract", "verifyComparisonEvidence", "renderCriticalPathComparison", "verifyFixtures"]
    - path: ".planning/phases/227-measured-critical-path-improvement/227-ci-contract.json"
      provides: "Versioned exact workflow identity, dependency, matrix/provider, artifact, and frozen-baseline manifest"
    - path: ".planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json"
      provides: "Sanitized positive and negative workflow/evidence controls"
    - path: ".github/workflows/ci.yml"
      provides: "The single authorized host-integration prerequisite deletion"
    - path: ".planning/phases/227-measured-critical-path-improvement/COVERAGE.md"
      provides: "Reasoned API-coverage detector override for reuse of the existing Actions collector"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".planning/phases/227-measured-critical-path-improvement/227-ci-contract.json"
      via: "verifyWorkflowContract compares the live workflow with the exact allowed graph and external evidence inventory"
      pattern: "host-integration|playwright-e2e|annotation-sweep|artifacts|release_matrix|provider"
    - from: "scripts/ci/verify_ci_critical_path.mjs"
      to: ".planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson"
      via: "read-only digest and before-metric validation"
      pattern: "sha256|2083|2602|frozen"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The spec-less edge probe classified PATH-01 as unresolved/unclassified; the plan assumes the locked staged-path definition and Phase 226 cohort are the complete optimization-selection boundary."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The spec-less edge probe classified PATH-02 as unresolved/unclassified; the plan assumes reducing dependency wait is sufficient only when immutable post-change evidence passes every locked proof-preservation gate."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The spec-less edge probe classified SAFE-01 as unresolved/unclassified; the plan assumes the exact manifest enumerates every externally meaningful identity and artifact contract without silently broadening optimization scope."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency remains unresolved: an observed median exactly 1,666 seconds is accepted, an observation exactly 2,602 seconds is not an exceedance, and exact-equal contract entries remain distinct rather than merged."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Empty/single/null evidence remains unresolved: null or fewer than three eligible first-attempt observations must fail closed as insufficient evidence and trigger the D-10 rollback path rather than produce a keep result."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Ordering remains unresolved: equal-valued observations must be retained individually and rendered in a deterministic stable order without changing median eligibility or silently deduplicating runs."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT describe correlation, whole-workflow variance, or a selectively excluded run as causal critical-path improvement."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT trade faster execution for missing, renamed, weakened, skipped, cancelled, or less recoverable release, host, browser, or provider proof."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT silently substitute a new check, label, condition, retention rule, or artifact for the stable external contract it is claimed to preserve."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT hide a deterministic failure through test deletion, retry, cancellation, branch-protection change, gate demotion, or fabricated rollback evidence."
---

<objective>
Prove and apply the one-edge critical-path vertical slice while locking every safety contract (D-01, D-02, D-03, D-07, D-08, D-09).

Purpose: Make the smallest production scheduling improvement executable end-to-end and fail closed before any live performance claim is attempted.
Output: Exact workflow/contract manifest, deterministic verifier and negative fixtures, one YAML-edge patch, and a preserved Phase 226 evidence boundary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-RESEARCH.md
@.planning/phases/227-measured-critical-path-improvement/227-PATTERNS.md
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
@.planning/phases/226-ci-baseline-proof-semantics/226-21-SUMMARY.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
@.planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
@.planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
@.github/workflows/ci.yml
@scripts/ci/collect_ci_baseline.mjs
@scripts/ci/render_ci_baseline.mjs
@scripts/ci/verify_ci_baseline.mjs
@scripts/ci/verify_provider_proof.mjs
@scripts/ci/README.md
</context>

## Artifacts this phase produces

- `scripts/ci/verify_ci_critical_path.mjs` exports `verifyWorkflowContract(workflowSource, contract)`, `verifyComparisonEvidence(records, contract, validationContext)`, `renderCriticalPathComparison(records, contract, validationContext)`, and `verifyFixtures()`; its CLI flags are `--fixtures`, `--require-negative-control`, `--verify-live-actions`, `--require-kept`, `--workflow`, `--contract`, `--evidence`, `--rendered`, and `--expected-repository`.
- `.planning/phases/227-measured-critical-path-improvement/227-ci-contract.json` defines schema version 1 and exact `jobs`, `needs`, `release_matrix`, `provider_labels`, `artifacts`, `frozen_inputs`, and `thresholds` fields.
- `.planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json` contains sanitized accepted, changed-edge, changed-identity, changed-artifact, rerun, incompatible-event/fingerprint, insufficient-sample, threshold-failure, and aggregate-failure cases.
- `.github/workflows/ci.yml` changes only `jobs.host-integration.needs` from `[admin-drift-docs, docs-contracts-shift-left]` to `[docs-contracts-shift-left]`; its exact inverse is restoring the former array.
- `.planning/phases/227-measured-critical-path-improvement/COVERAGE.md` records that no new external API integration exists.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the executable graph, evidence-contract, negative-control, and rollback vertical slice (D-02, D-07, D-08, D-09, D-10, D-11)</name>
  <reversibility rating="reversible">All new files are phase-local or an internal verifier and can be removed without migration; the manifest protects existing external contracts rather than creating a substitute.</reversibility>
  <files>scripts/ci/verify_ci_critical_path.mjs, .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json, .planning/phases/227-measured-critical-path-improvement/fixtures/ci-critical-path-cases.json</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-PATTERNS.md
    - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-21-SUMMARY.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson
    - .planning/phases/226-ci-baseline-proof-semantics/schema-v1.json
    - .github/workflows/ci.yml
    - scripts/ci/collect_ci_baseline.mjs
    - scripts/ci/render_ci_baseline.mjs
    - scripts/ci/verify_ci_baseline.mjs
    - scripts/ci/verify_provider_proof.mjs
  </read_first>
  <behavior>
    - "RED: the current two-prerequisite host graph fails the intended-graph fixture while the explicit inverse-graph fixture passes as the rollback contract."
    - "GREEN: the manifest accepts only the exact required job IDs/display names, complete annotation-sweep fan-in, required Floor/Primary/Primary+OpenTelemetry and advisory Sigra cells, literal provider policy/proof labels, and every existing upload name/condition/if-no-files-found/retention semantic."
    - "NEGATIVE: a removed/renamed job, changed display name, missing annotation upstream, changed matrix/support/provider label, weakened upload condition, changed artifact name, or changed retention setting fails."
    - "NEGATIVE: a rerun, incompatible event/fingerprint, fewer than three eligible runs, median above 1,666 seconds, or unsubstantiated observation above 2,602 seconds cannot produce keep=true."
    - "NEGATIVE: a fixture with an independent lane conclusion and retained artifacts proves host/browser completion plus annotation-sweep aggregate failure is represented, never inferred from local success."
    - "LIVE: repository-bound API fixtures reject mismatched repository/SHA/attempt/revision/job/annotation/artifact facts, and `--require-kept` exits nonzero for a fully verified rolled-back record so safe restoration cannot close PATH-02."
    - "FROZEN: SHA-256 digests remain 5fbde56db7ed6dac1677b7ef70841b79faecfb63598f5bddab4f8e8955e67350 for 226-CI-BASELINE.md, 768e67bfd957e2aae064706fc7a72c7b6b7b623b9d8daff3b99347cc171f5531 for 226-CI-BASELINE.ndjson, and 6d1933829a7eb9246a5aec078ce27a2bd817ab7b59a6ec57253bd314cccf9688 for schema-v1.json."
  </behavior>
  <action>Start with failing built-in-assert fixtures, then implement the dependency-free ESM verifier using only `node:` built-ins and the exported Phase 226 validation/collection/rendering helpers. Parse only the constrained workflow regions needed for exact contract comparison; do not add a YAML package or a second timing collector. Construct the manifest by inventorying the current workflow before the graph patch: job IDs and display names; exact `needs`; all four release matrix entries and support labels; provider policy/proof vocabulary; each `actions/upload-artifact@v7` name, enclosing `if`, `if-no-files-found`, and explicit-or-default retention behavior. Allow only an explicitly named Phase 227 evidence addition, never implicit extras.

Implement comparison validation with repository-bound immutable Actions URLs, workflow revision/cohort fingerprint, event class, attempt 1, successful eligible jobs, release start through latest Playwright shard completion, individual retained observations, range, median, exclusions, host DAG wait, whole-workflow context, static/negative-control results, and keep/rollback state. `--require-negative-control` validates a partial pack containing the controlled run without requiring the later three-run candidate cohort; full `--evidence` validation remains fail-closed on insufficient candidate samples. Add `--verify-live-actions`, reusing the existing GitHub collection/auth path, to fetch every recorded run, job, annotation, and artifact inventory and assert repository, SHA, attempt, workflow revision, conclusions, marker, condition-driven presence/absence, and immutable URL equality against NDJSON. The narrow anomaly rule accepts only an externally corroborated GitHub incident/status event or explicit runner/infrastructure failure evidence tied to the observation; ordinary runner variance and an unexplained slow run are not excludable. Validate the frozen file digests before any report calculation. Fixtures must prove positive and deliberately broken workflow, evidence, live-record mismatch, aggregate-failure, and inverse rollback contracts.</action>
  <acceptance_criteria>The fixture command exits zero only after proving the intended and inverse graphs, exact stable contract, frozen Phase 226 hashes, threshold arithmetic, visible exclusions, negative-control fan-in, and fail-closed malformed/changed cases; no package, action, standalone external integration, or Phase 226 file changes.</acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/verify_ci_critical_path.mjs &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue</automated>
  </verify>
  <done>The repository has one executable production-shaped verifier proving the permitted graph, exact external evidence contract, evidence eligibility/calculation, aggregate-failure negative control, frozen before state, and exact inverse rollback before the YAML patch lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply only the reversible host prerequisite deletion (D-01, D-02, D-03, D-09, D-11)</name>
  <reversibility rating="reversible">The single YAML edit is directly undone by restoring `needs: [admin-drift-docs, docs-contracts-shift-left]`; no schema, published API, feature switch, or migration is involved.</reversibility>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - scripts/ci/verify_ci_critical_path.mjs
    - .github/workflows/ci.yml
    - scripts/ci/README.md
  </read_first>
  <behavior>
    - "Before patch: verifier rejects the current graph as the candidate state and recognizes it as the exact inverse rollback state."
    - "After patch: host-integration has only docs-contracts-shift-left, playwright-e2e still has only host-integration, and annotation-sweep retains its complete fan-in."
    - "All stable job names, matrix/provider labels, artifact contracts, conditions, retries, workers, shards, and proof behavior remain byte-for-contract unchanged."
  </behavior>
  <action>Change only the `host-integration.needs` array per D-01. Do not edit its ID, display name, condition, runner, services, environment, steps, artifacts, or host UAT command. Preserve `playwright-e2e.needs: [host-integration]`, its three shards, `fail-fast: false`, one-worker environment, zero-retry Playwright configuration, failure artifact uploads, and the complete `annotation-sweep.needs` list per D-02/D-03. Run the exact-contract verifier and the complete Phase 225/226 local preservation suite. Record the inverse patch literally in the task summary; do not add a lasting feature switch.</action>
  <acceptance_criteria>The git diff for `.github/workflows/ci.yml` contains one dependency-array deletion only; the new verifier and inherited baseline/provider/setup/required-lane suites pass; the three Phase 226 frozen digests are unchanged.</acceptance_criteria>
  <verify>
    <automated>git diff --check -- .github/workflows/ci.yml &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The only production change is the D-01 dependency deletion, every protected contract passes locally, and the precise D-11 inverse is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow YAML → GitHub scheduler | A small dependency edit can alter execution, cancellation, and failure visibility across required jobs. |
| Workflow/manifest/fixtures → verifier | Repository files are untrusted until exact identity, graph, artifact, and threshold contracts pass. |
| Frozen Phase 226 evidence → Phase 227 comparison | Before-state files are read-only evidence and must not be rewritten or selectively interpreted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-01 | Tampering | `.github/workflows/ci.yml` graph and upload contracts | high | mitigate | Exact manifest comparison permits only the D-01 host prerequisite deletion and rejects every protected identity, edge, label, condition, retention, or artifact drift. |
| T-227-02 | Repudiation | aggregate failure and negative-control record | high | mitigate | Deterministic failed-lane fixture requires host/browser completion, retained evidence, and annotation-sweep aggregate failure as separate literal facts. |
| T-227-03 | Spoofing | Phase 226 before-state | high | mitigate | Pin and verify all three SHA-256 digests before comparison or rendering. |
| T-227-04 | Information Disclosure | fixtures and durable evidence | medium | mitigate | Reuse the Phase 226 privacy allowlist and repository-bound immutable URLs; exclude raw logs, actors, branches, secrets, payloads, and artifact contents. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No package or action installation is permitted; use Node built-ins and existing pinned workflow actions only. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 — Access-control rules are enforced at a trusted service layer | T-227-01, T-227-02 | Tasks 1-2 preserve the exact required-job/fan-in graph; `node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue` must pass. |
| V5.1.1 — Inputs are validated against an expected schema and contract | T-227-03, T-227-04 | Task 1 validates frozen hashes, repository-bound records, manifest fields, thresholds, anomaly corroboration, and negative fixtures with its automated command. |
| V14.2.1 — Components and dependencies come from trusted, pinned sources | T-227-SC | Task 1 uses only `node:` built-ins and Task 2 proves the workflow diff contains no package/action installation. |

**Blocking semantics:** Every high-severity threat is execution- and phase-closure-blocking until its mapped automated verification passes. A low-severity accepted threat never waives a failed high-severity mitigation.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster critical path with equally identifiable/recoverable proof | 01-03 | COVERED | Static tracer, live evidence, and keep/rollback closure form the full proof path. |
| REQ | PATH-01 | Exact measured path/cost, before evidence, rollback | 01, 02, 03 | COVERED | Manifest and frozen baseline establish selection; later plans record live result and rollback state. |
| REQ | PATH-02 | Reduce wait without proof removal | 01, 02, 03 | COVERED | One-edge patch plus three-run gate and exact contract preservation. |
| REQ | SAFE-01 | Stable check identity and artifacts | 01, 02 | COVERED | Exact manifest and live artifact observations. |
| REQ | SAFE-02 | Negative control and rollback without masking | 01, 02, 03 | COVERED | Fixture, controlled Actions run, and conditional exact inverse restoration. |
| RESEARCH | — | Dependency-free verifier, additive Phase 227 evidence, immutable Phase 226 input | 01-03 | COVERED | Uses prescribed files and existing exports; no new package. |
| CONTEXT | D-01–D-03 | One dependency edge; independent lanes; host→Playwright preserved | 01 | COVERED | Task 2 implements and Task 1 guards exact graph. |
| CONTEXT | D-04–D-07 | Three first attempts; staged metric; 20% bar; separate evidence | 02, 03 | COVERED | Live collection and final decision plans. |
| CONTEXT | D-08–D-11 | Two negative controls; exact contract; rollback triggers/inverse | 01-03 | COVERED | Local fixture, live control, manifest, and conditional restoration. |
| CONTEXT | Deferred ideas | Further graph/cache/matrix/test/branch-protection/product work | — | EXCLUDED | Explicitly absent from every task. |

<verification>
Run the new graph/contract/evidence fixtures, inspect the one-edge workflow diff, then run every inherited Phase 225/226 preservation command. Local success proves repository contracts only; it does not count as a post-change Actions performance observation or live aggregate-failure run.
</verification>

<success_criteria>
- The candidate graph is exactly D-01 through D-03 and its inverse is explicit.
- Stable IDs/display names, release/provider labels, artifacts, conditions, retention, host→Playwright, final fan-in, zero retries, and frozen Phase 226 hashes are protected by executable negative fixtures.
- No deferred graph, cache, matrix, branch-protection, test, product, schema, dependency, or external API work enters the diff.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-01-SUMMARY.md` when done.
</output>

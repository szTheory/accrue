---
phase: 227-measured-critical-path-improvement
plan: "03"
type: execute
wave: 3
depends_on: ["227-02"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
  - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
autonomous: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
user_setup:
  - service: github-actions
    why: "Conditional restoration run when the measured keep gate rejects the candidate"
    env_vars:
      - name: GH_TOKEN
        source: "Existing authenticated GitHub CLI session with Actions read/write access to szTheory/accrue"
must_haves:
  truths:
    - "D-06/D-10: keep is true only when exactly three eligible observations have median <=1,666 seconds, no unsubstantiated observation exceeds 2,602 seconds, every static/live negative control passes, and no required identity/artifact/proof/failure behavior regresses."
    - "D-10/D-11: any failed keep predicate restores exactly `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]`, reruns local controls, records a fresh successful first-attempt restoration run, then halts this phase as failed with PATH-02 unmet and escalates to a newly discussed/planned follow-up phase."
    - "The only successful terminal state is `decision: kept` with every keep predicate true, the one-edge candidate intact, rollback recorded as not triggered, and PATH-02 passed."
    - "PATH-01, PATH-02, SAFE-01, and SAFE-02 close only from combined automated local and repository-bound live evidence, never local proof alone; a safe rollback does not close this phase."
  artifacts:
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Final keep or rollback decision plus restoration evidence when triggered"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Final deterministic maintainer-facing result"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md"
      provides: "Executed requirement, threat, local, live, negative-control, and rollback proof"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md"
      provides: "Validated Nyquist map with executed outcomes"
  key_links:
    - from: "verifyComparisonEvidence keep predicates"
      to: ".github/workflows/ci.yml"
      via: "retain candidate or apply exact inverse dependency patch"
      pattern: "keep|rollback|host-integration|admin-drift-docs"
    - from: "local verifier and immutable Actions records"
      to: "227-VERIFICATION.md"
      via: "separate command/result and live-link evidence rows"
      pattern: "local|live|negative_control|restoration"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The unresolved PATH-01 edge remains explicit: a keep result proves only the locked staged-path metric for the recorded cohort, not universal future CI performance."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The unresolved PATH-02 edge remains explicit: retained proof equality is established for the manifest and observed runs, not for unenumerated future workflow additions."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The unresolved SAFE-01 edge remains explicit: branch-protection configuration itself is not queried or changed; stable identities are proven from workflow/live job contracts."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency/empty/ordering remain explicit in final evidence: equality boundaries are literal, insufficient evidence rolls back, and tied observations stay individually stable-ordered."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT publish `kept` or `improved` when any primary metric, eligibility, anomaly, frozen-input, or proof-preservation predicate is false or unknown."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT keep the candidate merely because whole-workflow wall time improved while the locked staged median failed."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT close stable-contract preservation from manifest text alone when live job/artifact evidence is missing."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT report rollback complete until the inverse graph, local controls, and fresh successful first-attempt restoration run are all recorded."
---

<objective>
Apply the measured keep-or-exact-rollback decision and seal the combined proof (D-06, D-07, D-10, D-11).

Purpose: Ensure the optimization survives only when it materially improves the locked path without weakening any proof; otherwise restore the verified prior graph and stop for a newly discussed/planned follow-up phase.
Output: A successful kept workflow with deterministic proof, or a verified safe-abort restoration record that leaves Phase 227 and PATH-02 explicitly incomplete.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-RESEARCH.md
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
@.planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
@scripts/ci/verify_ci_critical_path.mjs
@scripts/ci/README.md
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

- `.github/workflows/ci.yml` ends in exactly one of two verifier-recognized states: candidate `host-integration.needs: [docs-contracts-shift-left]` or restored `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]`.
- `.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson` replaces `decision_pending` with exactly one `decision` record (`kept` or `rolled_back`) and adds a `restoration_run` record only when rollback executes.
- `.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md` renders the final literal state, reason, calculations, controls, rollback state, and immutable evidence.
- `.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md` records requirement evidence and distinguishes automated/local commands from immutable live Actions observations.
- `.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md` becomes `status: validated`, `nyquist_compliant: true`, and records actual executed results for all planned rows.

<tasks>

<task type="auto">
  <name>Task 1: Enforce the candidate keep gate or execute the exact inverse restoration (D-06, D-10, D-11)</name>
  <precondition>Plan 02 evidence has exactly three eligible candidate records and one passing controlled-negative record, and `--verify-live-actions` passes for every immutable run/job/annotation/artifact fact.</precondition>
  <reversibility rating="reversible">Both permitted workflow states differ by one local YAML array entry and the exact inverse is executable; evidence records preserve why the transition occurred.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - scripts/ci/verify_ci_critical_path.mjs
    - .github/workflows/ci.yml
  </read_first>
  <action>Run the verifier’s final decision mode and enumerate every predicate in the resulting record: three eligible attempt-1 runs; same event class and compatible fingerprints; median at most 1,666 seconds; no observation above 2,602 seconds unless tied to an externally corroborated GitHub incident/status event or explicit runner/infrastructure failure evidence; frozen hashes; exact workflow manifest; local negative fixtures; passing live annotation control; no new deterministic failure; stable identities, artifacts, proof states, and failure propagation. Ordinary runner variance and an unexplained slow run remain included. Never use whole-workflow time to override the primary result.

If every predicate passes, leave the candidate edge intact, write `decision: kept`, record the inverse fixture as tested/not-triggered rollback evidence, regenerate Markdown, and require the final keep-mode verifier to pass. If any predicate fails or is unknown, apply only the D-11 inverse by restoring `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]`; rerun static/negative/inherited suites, push the restored SHA, trigger one fresh successful first-attempt run, and record immutable restoration run/job/artifact links. Write `decision: rolled_back` with every triggering predicate, regenerate/byte-verify the report, and prove the restoration through `--verify-live-actions`. Then make the final keep-mode verifier exit nonzero, stop execution before Task 2, leave PATH-02 and Phase 227 failed/incomplete, and route the work to `$gsd-discuss-phase` followed by `$gsd-plan-phase` for a new follow-up phase. Do not authorize or attempt another optimization in Phase 227 per D-03 and the deferred-ideas boundary.</action>
  <acceptance_criteria>The workflow and decision record agree exactly. The only passing terminal state is a kept candidate satisfying every locked predicate and PATH-02; any failed predicate produces the literal inverse graph, green local controls, fresh successful first-attempt restoration evidence, and an intentional nonzero safe-abort that prevents phase closure and escalates follow-up planning. No feature switch, third graph state, or second optimization exists.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --require-kept --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The task passes only for a demonstrably kept one-edge candidate with PATH-02 satisfied. A fully proven restoration is a safe abort: its intentional nonzero result halts Phase 227 and requires a newly discussed/planned follow-up phase.</done>
</task>

<task type="auto">
  <name>Task 2: Seal requirement, threat, negative-control, and rollback evidence (D-04 through D-11)</name>
  <precondition>Task 1 ended with `decision: kept`, its `--require-kept` command passed, and PATH-02 is supported by all automated local and live predicates.</precondition>
  <reversibility rating="reversible">Verification and validation documents report observed facts and can be corrected from immutable evidence without changing runtime contracts.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md, .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - scripts/ci/README.md
  </read_first>
  <action>Create `227-VERIFICATION.md` only for the kept branch, with `status: passed` and `behavior_unverified: 0`, and separate tables for automated/local commands and repository-bound live Actions evidence. Map PATH-01, PATH-02, SAFE-01, SAFE-02, D-01 through D-11, ASVS L1 mappings, and every high/medium threat to exact commands, records, immutable links, and pass status. Record the kept candidate graph literally, all three observations, range/median, host DAG wait, whole-workflow context, exclusions/corroboration, controlled failure, expected artifact presence/absence, frozen hashes, and rollback-not-triggered inverse-fixture result. Generate the automated UAT artifact and committed SUMMARY coverage required by CLAUDE.md with `human_judgment: false`.

Update `227-VALIDATION.md` from draft to validated only after every local row and required live row has current automated evidence. Add actual result text and immutable links, set `wave_0_complete: true`, and set `nyquist_compliant: true` only when no planned check is missing. Preserve unresolved spec-less assumptions and descriptor-less prohibitions as flagged; do not fabricate checks for them. This task is unreachable after rollback because Task 1 must stop and escalate instead.</action>
  <acceptance_criteria>Verification is passed with zero unverified behavior, traceable requirement-by-requirement, decision-by-decision, ASVS-control-by-control, and threat-by-threat; local/live proof is visibly distinct, the kept graph and decision match NDJSON, PATH-02 is passed, and validation plus automated UAT/SUMMARY metadata satisfy CLAUDE.md.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --require-kept --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; test -s .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md &amp;&amp; node -e 'const fs=require("node:fs"); const v=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md","utf8"); for (const token of ["status: passed","behavior_unverified: 0","PATH-01","PATH-02","SAFE-01","SAFE-02","Automated/local proof","Immutable live Actions evidence","D-11","ASVS L1","T-227-"]) if (!v.includes(token)) throw new Error(`verification missing ${token}`); const y=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md","utf8"); if (!/^status: validated$/m.test(y) || !/^nyquist_compliant: true$/m.test(y) || !/^wave_0_complete: true$/m.test(y)) throw new Error("validation sign-off incomplete");'</automated>
  </verify>
  <done>The final report proves the kept performance goal and all safety contracts, covers every requirement/decision/ASVS mapping/threat, records automated UAT with no human judgment, and leaves no local-vs-live evidence ambiguity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision engine → workflow state | Evidence predicates determine whether the candidate remains or the exact prior graph is restored. |
| Local/live evidence → verification claims | A green local suite cannot substitute for immutable scheduler, job, artifact, or restoration facts. |
| Verification metadata → milestone closure | Incorrect pass wording could close an unmet performance requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-10 | Tampering | keep/rollback predicate | high | mitigate | One verifier enumerates every locked threshold and proof predicate; unknown/false triggers the exact inverse. |
| T-227-11 | Repudiation | restoration claim | high | mitigate | Require inverse graph, local suites, fresh attempt-1 restoration SHA/run/jobs/artifacts before `rolled_back` is complete. |
| T-227-12 | Spoofing | verification status | high | mitigate | Cross-check workflow, NDJSON decision, generated Markdown, verification requirement rows, and validation metadata. |
| T-227-13 | Information Disclosure | final reports | medium | mitigate | Keep the Phase 226 sanitized allowlist; reference raw forensics through immutable Actions links only. |
| T-227-14 | Denial of Service | conditional restoration run | low | accept | At most one required fresh restoration run is planned; an automated auth precondition or infrastructure failure halts rather than looping. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No package or action installation is permitted. |

## ASVS L1 Control Mapping

| ASVS L1 Control | Applicable Threats | Task / Automated Verification |
|------------------|--------------------|-------------------------------|
| V4.1.1 — Access-control rules are enforced at a trusted service layer | T-227-10, T-227-11 | Task 1's `--verify-live-actions --require-kept` gate binds the decision to the exact workflow state; restoration proof is validated before the intentional safe-abort on a rejected candidate. |
| V5.1.1 — Inputs are validated against an expected schema and contract | T-227-10, T-227-12 | Tasks 1-2 cross-check every keep predicate, workflow/NDJSON/Markdown state, PATH-02 result, verification status, and validation metadata. |
| V5.2.1 — Untrusted data is sanitized before use or persistence | T-227-13 | Task 2 verifies the Phase 226 sanitized allowlist and retains raw forensics only behind immutable Actions links. |
| V14.2.1 — Components and dependencies come from trusted, pinned sources | T-227-SC | Both tasks add no package or action and run the existing pinned verifier suite. |

**Blocking semantics:** Every high-severity threat is execution- and phase-closure-blocking until its mapped automated verification passes. A low-severity accepted threat never waives a failed high-severity mitigation.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster path with equal proof | 03 | COVERED | The plan completes only when the keep gate proves it; safe restoration is an explicit failed/incomplete escalation branch. |
| REQ | PATH-01 | Path, cost, before evidence, rollback | 03 | COVERED | Final report contains all selection and rollback facts. |
| REQ | PATH-02 | Validated improvement without proof loss | 03 | COVERED | Kept only on full predicate pass; otherwise remains unmet and forces a newly discussed/planned follow-up phase. |
| REQ | SAFE-01 | Stable identity/artifacts | 03 | COVERED | Final local/live cross-check. |
| REQ | SAFE-02 | Negative control and rollback | 03 | COVERED | Live control plus conditional fresh restoration. |
| RESEARCH | — | Keep/rollback calculation and honest evidence boundary | 03 | COVERED | Prescriptive architecture is implemented. |
| CONTEXT | D-01–D-11 | Complete locked decision set | 01-03 | COVERED | Final task audits all decision IDs. |
| CONTEXT | Deferred ideas | Broader CI/product changes | — | EXCLUDED | No finalization task introduces them. |

<verification>
Run the complete local contract suite, validate the final evidence/report pair, and automatically inspect the repository-bound immutable live records. A successful phase requires `decision: kept`, `--require-kept`, and PATH-02. If rollback executes, first prove the exact restoration and fresh first-attempt run, then stop with an intentional failed verification and escalate; there is no rollback-only completion state.
</verification>

<success_criteria>
- The final workflow state is one of exactly two recognized graphs and agrees with the recorded decision.
- Successful completion requires every performance and safety predicate, `decision: kept`, and PATH-02 passed.
- Rollback requires the exact inverse, local controls, and fresh live restoration proof, then halts Phase 227 as failed/incomplete and routes to a newly discussed/planned follow-up phase.
- Verification distinguishes local automation from repository-bound live Actions evidence and cannot pass for an unmet PATH-02 goal.
- Validation, automated UAT, and SUMMARY coverage are authoritative only after all required evidence exists with `human_judgment: false`.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-03-SUMMARY.md` when done.
</output>

---
phase: 227-measured-critical-path-improvement
plan: "03"
type: execute
wave: 3
depends_on: ["227-02"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
  - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
autonomous: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
user_setup:
  - service: github-actions
    why: "Conditional restoration run when the measured keep gate rejects the candidate"
    env_vars:
      - name: GH_TOKEN
        source: "Existing authenticated GitHub CLI session with Actions read/write access to szTheory/accrue"
must_haves:
  truths:
    - "D-06/D-10: keep is true only when exactly three eligible observations have median <=1,666 seconds, no unsubstantiated observation exceeds 2,602 seconds, every static/live negative control passes, and no required identity/artifact/proof/failure behavior regresses."
    - "D-10/D-11: any failed keep predicate restores exactly `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]`, reruns local controls, and records a fresh successful first-attempt restoration run."
    - "If kept, the one-edge candidate remains and rollback is recorded as not triggered with its inverse fixture green; if rolled back, the report makes no performance-success claim."
    - "PATH-01, PATH-02, SAFE-01, and SAFE-02 close only from combined local and immutable live evidence, never local proof alone."
  artifacts:
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Final keep or rollback decision plus restoration evidence when triggered"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Final deterministic maintainer-facing result"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md"
      provides: "Executed requirement, threat, local, live, negative-control, and rollback proof"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md"
      provides: "Validated Nyquist map with executed outcomes"
  key_links:
    - from: "verifyComparisonEvidence keep predicates"
      to: ".github/workflows/ci.yml"
      via: "retain candidate or apply exact inverse dependency patch"
      pattern: "keep|rollback|host-integration|admin-drift-docs"
    - from: "local verifier and immutable Actions records"
      to: "227-VERIFICATION.md"
      via: "separate command/result and live-link evidence rows"
      pattern: "local|live|negative_control|restoration"
  assumptions:
    - requirement_id: PATH-01
      status: flagged-unverified
      flag: "The unresolved PATH-01 edge remains explicit: a keep result proves only the locked staged-path metric for the recorded cohort, not universal future CI performance."
    - requirement_id: PATH-02
      status: flagged-unverified
      flag: "The unresolved PATH-02 edge remains explicit: retained proof equality is established for the manifest and observed runs, not for unenumerated future workflow additions."
    - requirement_id: SAFE-01
      status: flagged-unverified
      flag: "The unresolved SAFE-01 edge remains explicit: branch-protection configuration itself is not queried or changed; stable identities are proven from workflow/live job contracts."
    - requirement_id: SAFE-02
      status: flagged-unverified
      flag: "Adjacency/empty/ordering remain explicit in final evidence: equality boundaries are literal, insufficient evidence rolls back, and tied observations stay individually stable-ordered."
  prohibitions:
    - requirement_id: PATH-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT publish `kept` or `improved` when any primary metric, eligibility, anomaly, frozen-input, or proof-preservation predicate is false or unknown."
    - requirement_id: PATH-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT keep the candidate merely because whole-workflow wall time improved while the locked staged median failed."
    - requirement_id: SAFE-01
      category: transparency
      status: flagged-unverified
      statement: "MUST NOT close stable-contract preservation from manifest text alone when live job/artifact evidence is missing."
    - requirement_id: SAFE-02
      category: safety
      status: flagged-unverified
      statement: "MUST NOT report rollback complete until the inverse graph, local controls, and fresh successful first-attempt restoration run are all recorded."
---

<objective>
Apply the measured keep-or-exact-rollback decision and seal the combined proof (D-06, D-07, D-10, D-11).

Purpose: Ensure the optimization survives only when it materially improves the locked path without weakening any proof, and otherwise restore the verified prior graph.
Output: Final workflow state, deterministic decision evidence, conditional restoration run, verification report, and validated test map.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-RESEARCH.md
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
@.planning/phases/227-measured-critical-path-improvement/227-02-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
@scripts/ci/verify_ci_critical_path.mjs
@scripts/ci/README.md
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

- `.github/workflows/ci.yml` ends in exactly one of two verifier-recognized states: candidate `host-integration.needs: [docs-contracts-shift-left]` or restored `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]`.
- `.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson` replaces `decision_pending` with exactly one `decision` record (`kept` or `rolled_back`) and adds a `restoration_run` record only when rollback executes.
- `.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md` renders the final literal state, reason, calculations, controls, rollback state, and immutable evidence.
- `.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md` records requirement evidence and distinguishes automated/local commands from immutable live Actions observations.
- `.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md` becomes `status: validated`, `nyquist_compliant: true`, and records actual executed results for all planned rows.

<tasks>

<task type="tracer">
  <name>Task 1: Enforce the candidate keep gate or execute the exact inverse restoration (D-06, D-10, D-11)</name>
  <precondition>Plan 02 evidence has exactly three eligible candidate records, one passing controlled-negative record, and maintainer approval of the immutable links.</precondition>
  <reversibility rating="reversible">Both permitted workflow states differ by one local YAML array entry and the exact inverse is executable; evidence records preserve why the transition occurred.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - scripts/ci/verify_ci_critical_path.mjs
    - .github/workflows/ci.yml
  </read_first>
  <action>Run the verifier’s final decision mode and enumerate every predicate in the resulting record: three eligible attempt-1 runs; same event class and compatible fingerprints; median at most 1,666 seconds; no observation above 2,602 seconds without admissible external substantiation; frozen hashes; exact workflow manifest; local negative fixtures; passing live annotation control; no new deterministic failure; stable identities, artifacts, proof states, and failure propagation. Never use whole-workflow time to override the primary result.

If every predicate passes, leave the candidate edge intact, write `decision: kept`, and record the inverse fixture as tested/not-triggered rollback evidence. If any predicate fails or is unknown, apply only the D-11 inverse by restoring `host-integration.needs: [admin-drift-docs, docs-contracts-shift-left]`; rerun static/negative/inherited suites, push the restored SHA, trigger one fresh successful first-attempt run, and record immutable restoration run/job/artifact links. Write `decision: rolled_back` with every triggering predicate and make no improvement claim. In both paths regenerate Markdown from NDJSON and byte-verify it.</action>
  <acceptance_criteria>The workflow and decision record agree exactly; a kept candidate passes every locked predicate, while any failure produces the literal inverse graph plus green local controls and a fresh successful first-attempt restoration run. No feature switch or third graph state exists.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository acme/accrue &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh</automated>
  </verify>
  <done>The repository is either demonstrably kept at the one-edge candidate or fully restored with fresh live proof; the decision is deterministic, immutable-link-backed, and byte-reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Seal requirement, threat, negative-control, and rollback evidence (D-04 through D-11)</name>
  <reversibility rating="reversible">Verification and validation documents report observed facts and can be corrected from immutable evidence without changing runtime contracts.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md, .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
    - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
    - .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json
    - .planning/phases/226-ci-baseline-proof-semantics/226-VERIFICATION.md
    - scripts/ci/README.md
  </read_first>
  <action>Create `227-VERIFICATION.md` with separate tables for automated/local commands and immutable live Actions evidence. Map PATH-01, PATH-02, SAFE-01, SAFE-02, D-01 through D-11, and every high/medium threat to exact commands, records, links, and pass/fail status. Record the candidate or restored graph literally, all three observations, range/median, host DAG wait, whole-workflow context, exclusions, controlled failure, expected artifact presence/absence, frozen hashes, and rollback result. Do not mark PATH-02 achieved if the final decision is rolled back; instead state honestly that the safety/rollback requirement passed while the performance candidate was rejected, so phase verification can report the resulting gap rather than manufacture success.

Update `227-VALIDATION.md` from draft to validated only after every local row and required live row has current evidence. Add actual result text and immutable links, set `wave_0_complete: true`, and set `nyquist_compliant: true` only when no planned check is missing. Preserve unresolved spec-less assumptions and descriptor-less prohibitions as flagged; do not fabricate checks for them.</action>
  <acceptance_criteria>Verification is traceable requirement-by-requirement and threat-by-threat, local/live proof is visibly distinct, the final graph/decision/rollback facts match NDJSON, validation metadata reflects actual execution, and a rejected candidate is never presented as PATH-02 success.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --expected-repository szTheory/accrue &amp;&amp; test -s .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md &amp;&amp; node -e 'const fs=require("node:fs"); const v=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md","utf8"); for (const token of ["PATH-01","PATH-02","SAFE-01","SAFE-02","Automated/local proof","Immutable live Actions evidence","D-11","T-227-"]) if (!v.includes(token)) throw new Error(`verification missing ${token}`); const y=fs.readFileSync(".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md","utf8"); if (!/^status: validated$/m.test(y) || !/^nyquist_compliant: true$/m.test(y) || !/^wave_0_complete: true$/m.test(y)) throw new Error("validation sign-off incomplete");'</automated>
  </verify>
  <done>The final report honestly proves or rejects the performance goal, proves safety and restoration behavior, covers every requirement/decision/threat, and leaves no local-vs-live evidence ambiguity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision engine → workflow state | Evidence predicates determine whether the candidate remains or the exact prior graph is restored. |
| Local/live evidence → verification claims | A green local suite cannot substitute for immutable scheduler, job, artifact, or restoration facts. |
| Verification metadata → milestone closure | Incorrect pass wording could close an unmet performance requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-10 | Tampering | keep/rollback predicate | high | mitigate | One verifier enumerates every locked threshold and proof predicate; unknown/false triggers the exact inverse. |
| T-227-11 | Repudiation | restoration claim | high | mitigate | Require inverse graph, local suites, fresh attempt-1 restoration SHA/run/jobs/artifacts before `rolled_back` is complete. |
| T-227-12 | Spoofing | verification status | high | mitigate | Cross-check workflow, NDJSON decision, generated Markdown, verification requirement rows, and validation metadata. |
| T-227-13 | Information Disclosure | final reports | medium | mitigate | Keep the Phase 226 sanitized allowlist; reference raw forensics through immutable Actions links only. |
| T-227-14 | Denial of Service | conditional restoration run | low | accept | At most one required fresh restoration run is planned; auth or infrastructure failure halts with an explicit checkpoint rather than looping. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No package or action installation is permitted. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster path with equal proof | 03 | COVERED | Final decision either proves it or honestly reports the unmet performance requirement after safe restoration. |
| REQ | PATH-01 | Path, cost, before evidence, rollback | 03 | COVERED | Final report contains all selection and rollback facts. |
| REQ | PATH-02 | Validated improvement without proof loss | 03 | COVERED | Kept only on full predicate pass; otherwise explicitly gaps rather than false closure. |
| REQ | SAFE-01 | Stable identity/artifacts | 03 | COVERED | Final local/live cross-check. |
| REQ | SAFE-02 | Negative control and rollback | 03 | COVERED | Live control plus conditional fresh restoration. |
| RESEARCH | — | Keep/rollback calculation and honest evidence boundary | 03 | COVERED | Prescriptive architecture is implemented. |
| CONTEXT | D-01–D-11 | Complete locked decision set | 01-03 | COVERED | Final task audits all decision IDs. |
| CONTEXT | Deferred ideas | Broader CI/product changes | — | EXCLUDED | No finalization task introduces them. |

<verification>
Run the complete local contract suite, validate the final evidence/report pair, and inspect the immutable live records. If rollback executes, completion additionally requires the fresh first-attempt restoration run; there is no local-only substitute.
</verification>

<success_criteria>
- The final workflow state is one of exactly two recognized graphs and agrees with the recorded decision.
- Keep requires every performance and safety predicate; rollback requires the exact inverse, local controls, and fresh live restoration proof.
- Verification distinguishes local automation from immutable Actions evidence and reports any unmet PATH-02 goal honestly.
- Validation is authoritative only after all required evidence exists.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-03-SUMMARY.md` when done.
</output>

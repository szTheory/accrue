---
phase: 227-measured-critical-path-improvement
plan: "08"
type: execute
wave: 8
depends_on: [227-07]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson
  - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
  - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
  - .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
  - scripts/ci/README.md
autonomous: true
gap_closure: true
requirements: [PATH-01, PATH-02, SAFE-01, SAFE-02]
user_setup:
  - service: github-actions
    why: "Create and read the separately authorized Phase 227 candidate and conditional restoration runs"
    env_vars:
      - name: GH_TOKEN
        source: "Existing authenticated GitHub CLI session with Actions read/write and temporary-ref access to szTheory/accrue"
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-04 through D-06/PATH-02: only exactly three qualifying, unique, attempt-1 observations at the Plan 227-07 candidate commit can produce kept, and their staged median/maximum rules must pass."
    - "D-08/D-09/SAFE-01/SAFE-02: the immutable negative control, exact live job-role/artifact vector, stable contracts, recursive privacy, and no-retry/no-replacement rules pass for every admitted run."
    - "D-10/D-11/D-21/D-22: the first nonqualifying or ambiguous candidate closes remaining slots and applies the exact inverse; only then may one restoration-only normal run be created, and it never counts toward PATH-02."
    - "Every accepted dispatch has a prior durable reservation and an immediate unique run-ID consumption record; resumption reconciles existing GitHub runs before any further dispatch."
    - "D-07/D-23: final report, validation, and verification can become green only after `--require-kept` passes against a genuine repository-bound kept terminal."
  artifacts:
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      provides: "Reservations, immediate consumption facts, complete candidate vectors, terminal kept or rollback result"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md"
      provides: "Byte-generated current fact and final comparison or rollback account"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md"
      provides: "Executed Nyquist map for the repaired local and newly bounded live path"
    - path: ".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md"
      provides: "Additive re-verification closing all three prior gaps only on kept evidence"
  key_links:
    - from: "227-07 candidate task commit"
      to: "GitHub Actions run IDs"
      via: "isolated preflight, authority activation, temporary ref, reservation, reconciliation, and immediate consumption binding"
      pattern: "gap_authority_activation|gap_run_reservation|gap_run_consumption"
    - from: ".planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson"
      to: ".planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md"
      via: "strict live require-kept gate and byte-generated report"
      pattern: "gap_decision.*kept|status: passed|PATH-02"
---

<objective>
Spend the smallest sufficient external budget, keep only a genuine measured improvement, and close Phase 227 from reproducible evidence (D-01 through D-23).

Purpose: Obtain the still-missing PATH-02 proof without duplicate dispatches, hidden failures, reopened historical authority, or a closure document that outruns terminal evidence.
Output: At most three candidate runs, at most one conditional restoration run, a kept-or-restored workflow, deterministic report, green validation, and passed re-verification only on kept.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/227-measured-critical-path-improvement/227-CONTEXT.md
@.planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md
@.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md
@.planning/phases/227-measured-critical-path-improvement/227-05-SUMMARY.md
@.planning/phases/227-measured-critical-path-improvement/227-07-SUMMARY.md
@.planning/debug/resolved/phase227-format-verifier-gap.md
@scripts/ci/preflight_phase227_candidate.sh
@scripts/ci/verify_ci_critical_path.mjs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the exact candidate through reservation, immediate consumption, live proof, and keep-or-inverse (D-01 through D-22)</name>
  <precondition>`gh auth status` succeeds for `szTheory/accrue`; 227-07-SUMMARY.md names a full candidate task-commit SHA; the new authority has zero reservations/consumptions and remote effects disabled; the current workflow is the exact candidate state.</precondition>
  <reversibility rating="costly">Created Actions runs and their immutable URLs cannot be erased, so each finite slot is durably reserved and bound; the workflow edge itself has an exact inverse.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson, .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-07-SUMMARY.md — exact candidate task commit and local outcomes.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — zero-consumption authority plus immutable history.
    - scripts/ci/preflight_phase227_candidate.sh — isolated exact-commit gate that must pass before activation.
    - scripts/ci/verify_ci_critical_path.mjs — reservation/consumption/live/decision validator.
    - .github/workflows/ci.yml — candidate graph and literal inverse target.
  </read_first>
  <behavior>
    - Before any push or dispatch, the detached candidate commit passes CI-pinned Accrue format, the focused repaired test, Node syntax/tests/fixtures, candidate workflow contract, and inherited local evidence contracts; an activation record binds the full SHA, tree, results, and timestamp.
    - One temporary candidate ref is created only if absent; on resume it is reused only when it points to the exact candidate SHA, and is never force-updated.
    - Each candidate slot is reserved and locally committed before dispatch. The executor queries matching runs for the ref/SHA/event after the reservation before calling dispatch; an existing unique unbound run is consumed instead of creating another.
    - After one accepted dispatch call, the executor waits only to identify the unique new run, appends/commits its consumption record immediately, and only then waits for terminal jobs/artifacts. Missing or multiple matches are an ambiguity failure, not permission to dispatch again.
    - Candidate runs are sequential. Any nonqualifying/ambiguous result closes unused candidate slots and triggers the exact inverse immediately. Only three complete qualifying vectors proceed to the locked median/maximum decision.
    - On candidate rejection, the exact inverse is committed and locally verified before a separately reserved restoration-only attempt. That single run uses normal manual input true, proves restoration only, and never becomes a performance observation.
    - Every temporary ref is removed after terminal binding/decision; all authority closes as kept, rollback_verified, or rollback_applied_unverified, with PATH-02 satisfied only by kept.
  </behavior>
  <action>Run the isolated preflight against the full candidate task-commit SHA before any remote mutation, then append and commit authority activation. Use a stable temporary candidate ref bound once to that SHA. For each of at most three slots, append and commit a reservation containing ordinal, nonce, earliest creation time, repository, SHA, ref, event/input tuple, and pre-dispatch run-ID snapshot. Reconcile GitHub before dispatch. If no matching unbound run exists, dispatch exactly once; poll only for unique identity, append and commit the consumed run ID immediately, then collect its terminal job/artifact vector. Never issue a second dispatch for the same reservation. Stop after the first invalid vector or ambiguity. On three valid vectors, evaluate the locked staged-path and anomaly predicates and append kept only if every safety predicate passes. Otherwise close candidate authority, remove its ref, apply/commit the literal inverse, rerun local controls, and use the same reserve/reconcile/consume protocol for at most one restoration-only default-true run. Append every classification and terminal state, render after each transition, and preserve all prior bytes and external URLs.</action>
  <acceptance_criteria>The ledger and GitHub agree on zero-to-three candidate attempts and zero-to-one conditional restoration attempt; each created run has exactly one reservation and immediate consumption binding; the workflow is candidate only for kept and restored for every other terminal.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-final-decision</automated>
  </verify>
  <fails_when>Exact-commit preflight is absent/red; a dispatch lacks a prior reservation; a reservation is dispatched twice; a created run is not immediately bound; GitHub/ledger counts differ; ambiguity is followed by dispatch; any invalid candidate is followed by another; ceilings are exceeded; a restoration is counted as candidate evidence; a ref remains; or workflow state disagrees with terminal state.</fails_when>
  <done>The newly authorized experiment is terminal with complete repository-bound accounting and either a genuine kept result or an exact, honestly reported restoration that leaves PATH-02 unmet.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the final maintainer report only from a genuine kept terminal (D-04 through D-11, D-15 through D-23)</name>
  <precondition>The latest terminal is `gap_decision: kept`; `--require-kept` passes live; exactly three admitted candidate observations exist; no restoration record exists for this authority; and the workflow is the exact candidate state.</precondition>
  <reversibility rating="reversible">The report is a deterministic projection of immutable sanitized evidence and can be regenerated without changing external state.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md, scripts/ci/README.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson — authoritative kept records, reservations, consumption facts, observations, and historical exclusions.
    - scripts/ci/verify_ci_critical_path.mjs — strict renderer and live kept gate.
    - .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md — frozen metric and maintainer-report grammar.
    - scripts/ci/README.md — current terminally closed guidance to replace with the verified current state.
  </read_first>
  <behavior>
    - The report begins with current fact, kept state, owner, and one exact verification command.
    - It shows the frozen 2,083-second median, 2,602-second p95, 1,666-second keep threshold, all three individual durations/URLs, range/median, DAG waits, workflow context, SHA/tree/revision/fingerprint, and exact sample count.
    - Required/advisory/provider/artifact vectors, negative control, every historical/new exclusion, all budget closures, reservation/consumption accounting, and the unused exact inverse remain visible.
    - Measurement provider state remains non_run and separate provider evidence remains literal; workflow success never implies provider proof.
    - Repeated rendering byte-matches and the README exposes verification commands but no remaining dispatch/rerun authority.
  </behavior>
  <action>Invoke the validated renderer; do not hand-author the comparison. Run offline evidence, byte, workflow, and live kept verification. Update the Phase 227 README section to name the kept state, exact reproduction commands, closed authority, and inverse procedure. Preserve the Phase 226 files, prior failure narrative, classifications, and all immutable links. Do not enter this task on any rollback, partial cohort, failed threshold, missing role/artifact, or non-kept terminal.</action>
  <acceptance_criteria>The report is byte-stable, privacy-safe, complete, and says kept only because the exact-three live verifier and every D-06/D-09 predicate pass.</acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-kept &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-workflow --workflow .github/workflows/ci.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-state candidate</automated>
  </verify>
  <fails_when>The terminal is not kept; any sample/vector/threshold/live check fails; rendering changes evidence; report bytes drift; provider proof is inferred; a historical failure disappears; or README authorizes another remote effect.</fails_when>
  <done>Maintainers have a reproducible, proof-honest account of the faster critical path and its still-executable inverse.</done>
</task>

<task type="auto">
  <name>Task 3: Close validation and re-verification only after kept proof (D-01 through D-23, PATH-01, PATH-02, SAFE-01, SAFE-02)</name>
  <precondition>Task 2 completed with live `--require-kept`, byte-render, and candidate-workflow checks green; the final report names exactly three admitted runs and PATH-02 satisfied.</precondition>
  <reversibility rating="reversible">Validation and verification are evidence documents that retain prior findings and add a dated re-verification result.</reversibility>
  <files>.planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md, .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md — stale draft map to replace with executed 227-04 through 227-08 coverage.
    - .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md — authoritative three-gap report whose history must remain visible.
    - .planning/phases/227-measured-critical-path-improvement/227-07-SUMMARY.md — exact local candidate and preflight controls.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — generated kept account and commands.
    - .planning/debug/resolved/phase227-format-verifier-gap.md — local blocker resolution evidence.
  </read_first>
  <behavior>
    - Validation maps every task in 227-04, 227-05, 227-07, and 227-08 to exact executed commands, requirements, threats, and pass results, with no pending/MISSING/manual-only row.
    - The full suite includes strict verifier tests/fixtures, exact-commit preflight evidence, live kept verification, byte rendering, candidate workflow state, frozen baseline, provider/setup/Phase 225 controls, CI-pinned Accrue format, and focused Accrue tests.
    - Validation becomes validated, wave_0_complete true, and nyquist_compliant true only after every required command exits zero.
    - Verification retains the original gaps-found account as history and adds a dated re-verification that closes all three listed gaps, scores every must-have verified, sets behavior_unverified to zero, and maps all four requirements plus D-01 through D-23.
    - A rollback, partial cohort, red command, missing live fact, or unverified behavior leaves both documents non-green and stops phase closure.
  </behavior>
  <action>Execute the complete local/live suite and record actual results without inventing counts. Update the validation task map for the additive plans and all applicable high/medium threats. Add a dated re-verification section that directly answers each prior gap: genuine exact-three improvement, fail-closed decision gate including kept-only semantics, and immutable fixture/report reproducibility. Preserve the earlier failure report rather than erasing it. Set passed/validated frontmatter only after live kept, exact candidate workflow, byte report, frozen baseline, provider/setup, formatter, and focused application commands all pass. Otherwise stop with the exact failing command and do not mark PATH-02 or the phase complete.</action>
  <acceptance_criteria>Validation and verification are current, fully automated, historically honest, and green for PATH-01, PATH-02, SAFE-01, SAFE-02, all three prior gaps, and every applicable threat with zero unverified behavior.</acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/verify_ci_critical_path.mjs &amp;&amp; node --test scripts/ci/verify_ci_critical_path.test.mjs &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --fixtures --workflow-fixture .planning/phases/227-measured-critical-path-improvement/fixtures/ci-workflow-restored-v2.yml --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json &amp;&amp; node scripts/ci/verify_ci_critical_path.mjs --verify-live-actions --evidence .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.ndjson --rendered .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md --contract .planning/phases/227-measured-critical-path-improvement/227-ci-contract.json --expected-repository szTheory/accrue --require-kept &amp;&amp; node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.ndjson --rendered .planning/phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md --require-critical-path --expected-repository szTheory/accrue &amp;&amp; node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures &amp;&amp; bash scripts/ci/verify_ci_setup_diagnostics.sh &amp;&amp; bash scripts/ci/verify_phase225_required_lane_evidence.sh &amp;&amp; (cd accrue &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 ASDF_ELIXIR_VERSION=1.19.5-otp-28 mix format --check-formatted) &amp;&amp; (cd accrue &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 ASDF_ELIXIR_VERSION=1.19.5-otp-28 mix test test/accrue/backend_automation_contract_test.exs test/accrue/docs/release_guidance_test.exs test/accrue/live_proof_formatter_test.exs --warnings-as-errors) &amp;&amp; rg -q '^status: validated$' .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md &amp;&amp; rg -q '^nyquist_compliant: true$' .planning/phases/227-measured-critical-path-improvement/227-VALIDATION.md &amp;&amp; rg -q '^status: passed$' .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md &amp;&amp; rg -q '^behavior_unverified: 0$' .planning/phases/227-measured-critical-path-improvement/227-VERIFICATION.md</automated>
  </verify>
  <fails_when>Any command fails or is omitted; kept-only verification is non-green; validation remains draft/pending; verification erases prior findings or retains an unresolved gap; any requirement/decision/threat lacks evidence; or closure is claimed from rollback.</fails_when>
  <done>The original three verification gaps are closed by genuine kept evidence, Phase 227 is Nyquist-valid with zero unverified behavior, and the milestone can consume PATH-02 without qualification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local authority/candidate → GitHub | Push, ref, and dispatch effects create immutable external state and must correspond to one reserved slot. |
| GitHub runs/jobs/artifacts → NDJSON | Remote facts are untrusted until repository, SHA, attempt, event, input, role, URL, artifact, and timing contracts agree. |
| NDJSON → report/validation/verification | Documentation must not turn a partial or rollback terminal into a completed improvement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-227-46 | Elevation of Privilege | candidate/restoration run creation | high | mitigate | Activation after exact-tree preflight plus finite authority, prior reservation, reconciliation, and immediate consumption binding govern every run. |
| T-227-47 | Repudiation | duplicate/ambiguous dispatch | high | mitigate | Pre-dispatch snapshots and resume reconciliation adopt one existing run; ambiguity closes authority and forbids another call. |
| T-227-48 | Spoofing | live proof vector | high | mitigate | Strict live verification binds repository, candidate SHA, attempt, event/input, revision, exact job roles/URLs, artifacts, and provider state. |
| T-227-49 | Tampering | keep/rollback decision | high | mitigate | Exact-three metric and all safety predicates gate kept; first invalid result closes slots and applies the literal inverse. |
| T-227-50 | Repudiation | restoration evidence | high | mitigate | A separate conditional reservation and default-true run can establish restoration only and is rejected as candidate evidence. |
| T-227-51 | Information Disclosure | durable live evidence | high | mitigate | Recursive schema/privacy validation precedes append, render, validation, and verification; raw logs and contents remain Actions-owned. |
| T-227-52 | Spoofing | phase completion | high | mitigate | Final artifacts require live `--require-kept`, candidate workflow state, byte report, full local suite, and explicit closure of each prior gap. |
| T-227-SC | Tampering | package/action installation | high | mitigate | No install or action change; existing pinned tools and dependencies only. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Demonstrably faster critical path with equal proof and recoverability | 07-08 | COVERED | Exact-tree preflight, bounded live cohort, kept-only closure. |
| REQ | PATH-01 | Path/edge/before evidence/rollback | 07-08 | COVERED | Candidate/inverse commits and generated report retain all facts. |
| REQ | PATH-02 | Validated improvement without proof removal | 08 | COVERED | Task 1 kept gate plus Tasks 2-3 closure. |
| REQ | SAFE-01 | Stable checks and artifacts | 07-08 | COVERED | Local exact manifest and live role/artifact reconciliation. |
| REQ | SAFE-02 | Negative control and rollback without masking | 07-08 | COVERED | Existing live control, fail-closed reservations, inverse, restoration-only proof. |
| RESEARCH | — | One-edge graph and frozen staged metric | 07-08 | COVERED | No alternate optimization or baseline mutation. |
| RESEARCH | — | Sanitized repository-bound evidence and deterministic reporting | 08 | COVERED | Immediate bindings, strict live validation, byte render. |
| CONTEXT | D-01-D-03 | One edge; independent lanes/fan-in preserved | 07-08 | COVERED | Candidate and inverse workflow states. |
| CONTEXT | D-04-D-07 | Exact three, thresholds, separate evidence | 08 | COVERED | Live kept terminal and generated report. |
| CONTEXT | D-08-D-11 | Controls, manifest, rollback triggers, fresh restoration | 07-08 | COVERED | Local/live controls and conditional restoration-only allowance. |
| CONTEXT | D-12-D-17 | Restore-first trigger/input/vector | 07-08 | COVERED | Repaired restored parent, manual-false candidates, complete vectors. |
| CONTEXT | D-18-D-22 | Immutable old attempts, finite new approved authority, classifications, literal states | 07-08 | COVERED | Separately named budget and strict transition/accounting rules. |
| CONTEXT | D-23 | Maintainer report grammar | 08 | COVERED | Tasks 1-2 render every state from ledger. |
| VERIFICATION | GAP-1 | Missing genuine three-run kept improvement | 08 | COVERED | Task 1. |
| VERIFICATION | GAP-2 | Decision gate must fail closed | 07-08 | COVERED | 07 Task 1 and 08 Task 1. |
| VERIFICATION | GAP-3 | Immutable fixtures and reproducible terminal renderer/validation | 07-08 | COVERED | 07 local gate; 08 Tasks 2-3. |
| CONTEXT | Deferred ideas | Other graph/cache/matrix/workflow/product/UI changes | — | EXCLUDED | No task implements them. |

<verification>
Prove the exact committed tree before activation, reconcile before each dispatch, bind every accepted run before waiting, and stop on the first ambiguity or nonqualifying result. Only a live `--require-kept` result unlocks report, validation, and verification closure.
</verification>

<success_criteria>
- No external run exists without one prior reservation and one immediate consumption record; no reservation can dispatch twice.
- Newly created attempts are limited to the three observations required by D-04 plus one genuinely conditional restoration-only proof.
- Kept requires exactly three qualifying first attempts at one candidate SHA and every timing, identity, artifact, provider, control, and privacy predicate.
- Rejection restores the exact edge and leaves PATH-02/phase closure explicitly unmet, with no further authority.
- A genuine kept terminal produces a byte-reproducible report, validated Nyquist map, and passed additive re-verification closing all three recorded gaps.
</success_criteria>

<output>
Create `.planning/phases/227-measured-critical-path-improvement/227-08-SUMMARY.md` when done.
</output>

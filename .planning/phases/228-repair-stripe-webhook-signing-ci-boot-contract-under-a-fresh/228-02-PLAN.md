---
phase: 228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh
plan: "02"
type: execute
wave: 2
depends_on: [228-01]
files_modified: []
autonomous: false
requirements: []
user_setup:
  - service: stripe-and-github
    why: Supply the exact Stripe test-mode endpoint signing secret to the existing live-stripe job without exposing it.
    env_vars:
      - name: STRIPE_WEBHOOK_SECRET
        source: Stripe Dashboard test-mode endpoint signing secret -> GitHub repository Actions secret of the same name
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Deterministic Plan 228-01 gates are green before external secret administration or dispatch authorization.
    - A maintainer confirms the exact test-mode endpoint secret is stored under STRIPE_WEBHOOK_SECRET without revealing its value.
    - Exactly one fresh first-attempt workflow_dispatch is explicitly authorized; no retry or replacement authority is implied.
  artifacts:
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-01-SUMMARY.md
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
  key_links:
    - Plan 228-01 deterministic success -> maintainer secret confirmation -> one-attempt authorization
  prohibitions:
    - No secret value is requested, displayed, copied into chat, or recorded.
    - No workflow is dispatched in this plan.
---

<objective>
Obtain the two pieces of external authority the agent cannot create: sanitized confirmation of correct endpoint-secret administration and approval for one fresh first-attempt run.

Purpose: Keep live evidence spending behind deterministic readiness and explicit human authority.
Output: Blocking checkpoint decisions only; no repository or provider mutation by the executor.

Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-01-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm exact test-mode endpoint secret administration</name>
  <read_first>
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-01-SUMMARY.md — deterministic gates must be green.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — readiness state must remain unconsumed.
  </read_first>
  <action>After independently confirming Plan 228-01 verification passed, ask the maintainer to retrieve the signing secret for the exact Stripe Dashboard test-mode endpoint used by this repository and store it as the GitHub Actions repository secret STRIPE_WEBHOOK_SECRET. Request only the statement that the correct endpoint secret is configured; do not request, inspect, echo, compare, or record its value or any endpoint identifier. A CLI forwarding secret, API key, or different endpoint secret does not satisfy this checkpoint.</action>
  <instructions>Run `node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/runtime_config_test.exs test/accrue/config_test.exs` first. The maintainer then stores the exact test-mode Dashboard endpoint signing secret in GitHub Actions as STRIPE_WEBHOOK_SECRET and reports only completion, never the value.</instructions>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/runtime_config_test.exs test/accrue/config_test.exs &amp;&amp; cd .. &amp;&amp; rg -n 'readiness_not_authorized|consumed: `false`' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md</automated>
    <human-check>Maintainer reports only `configured` after storing the exact test-mode Dashboard endpoint signing secret as STRIPE_WEBHOOK_SECRET; the response contains neither the secret value nor an endpoint identifier.</human-check>
  </verify>
  <verification>Automated readiness gates pass, and the maintainer supplies only the sanitized `configured` resume signal for the exact endpoint-secret setup.</verification>
  <resume-signal>Type `configured` after the exact test-mode endpoint signing secret is stored, or `decline` to stop.</resume-signal>
  <acceptance_criteria>Deterministic readiness remains green and the maintainer provides sanitized configuration confirmation only.</acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize exactly one fresh attempt-1 provider run</name>
  <reversibility rating="one-way">A GitHub Actions dispatch creates immutable external run evidence and consumes the sole Phase 228 budget; it cannot be undone.</reversibility>
  <read_first>
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — confirm consumed:false and the one-attempt ceiling.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — confirm Phase 227 authority is exhausted and remains historical only.
  </read_first>
  <decision>Whether to authorize exactly one new attempt-1 workflow_dispatch with run_live_stripe:true at the repaired committed SHA.</decision>
  <context>The run is immutable external evidence. Any created run consumes Phase 228's sole budget; an API rejection that creates no run does not. No retry, replacement, alternate transport, or Phase 227 recovery is included.</context>
  <action>Re-run the deterministic readiness gate, confirm the evidence record still has consumed:false, and confirm the Phase 227 evidence directory is unchanged. Present only the two declared options. Record the maintainer's exact option in the Plan 228-02 summary without dispatching a workflow: authorize-one-attempt grants Plan 228-03 authority for one attempt-1 workflow_dispatch with run_live_stripe:true at the repaired committed SHA; decline grants no dispatch authority and preserves the budget.</action>
  <options>
    <option id="authorize-one-attempt">
      <name>Authorize one attempt</name>
      <pros>Allows the repaired deterministic contract to obtain real provider evidence.</pros>
      <cons>Creates immutable external evidence and consumes the sole budget regardless of outcome.</cons>
    </option>
    <option id="decline">
      <name>Decline dispatch</name>
      <pros>Creates no external run and preserves the unconsumed budget.</pros>
      <cons>Leaves fresh provider proof unavailable and blocks Plan 228-03.</cons>
    </option>
  </options>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; rg -n 'consumed: `false`|first-attempt|additional dispatch authorized: `false`' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md &amp;&amp; git diff --exit-code -- .planning/phases/227-measured-critical-path-improvement</automated>
    <human-check>Maintainer selects exactly `authorize-one-attempt` or `decline`; no inferred approval, workflow dispatch, retry authority, or secret material is accepted.</human-check>
  </verify>
  <resume-signal>Select exactly `authorize-one-attempt` or `decline`.</resume-signal>
  <acceptance_criteria>A recorded explicit choice exists. Only authorize-one-attempt unlocks Plan 228-03; decline creates no run and leaves the evidence budget unconsumed.</acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new symbol, CLI flag, field, or path. It gates the existing `STRIPE_WEBHOOK_SECRET` administration contract and the `228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md` run-budget surface.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer -> Stripe/GitHub secret stores | External authority handles credential material outside repository and agent visibility. |
| Maintainer decision -> executor | One explicit authorization controls an irreversible external dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-228-05 | Information Disclosure | human secret-setup checkpoint | high | mitigate | Accept sanitized confirmation only and forbid value or endpoint-identity transmission. |
| T-228-06 | Spoofing | endpoint secret selection | high | mitigate | Require explicit confirmation of the exact test-mode Dashboard endpoint secret, excluding CLI and API credentials. |
| T-228-07 | Repudiation | dispatch authority | high | mitigate | Record an explicit binary decision and a one-attempt ceiling; no inferred or inherited authorization. |
| T-228-08 | Denial of Service | premature evidence spend | high | mitigate | Re-run deterministic gates before checkpoints and prohibit dispatch in this plan. |
</threat_model>

<verification>
Both deterministic suites remain green; the maintainer confirmation contains no secret material; the dispatch decision is explicit and bounded.
</verification>

<success_criteria>
Correct secret administration is confirmed safely and exactly one fresh attempt is either explicitly authorized or explicitly declined, with no dispatch yet created.
</success_criteria>

<output>
Create `.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-02-SUMMARY.md` when done.
</output>

---
phase: 228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh
plan: "03"
type: execute
wave: 3
depends_on: [228-02]
files_modified:
  - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
  - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md
autonomous: false
requirements: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The sole newly authorized first-attempt run targets the repaired committed SHA with workflow_dispatch and run_live_stripe:true.
    - The immutable result is recorded honestly whether proved, failed, misconfigured, blocked, or rejected without creating a run.
    - Provider proof requires successful raw conclusion, nonzero selected and passing tests, a valid manifest, and live-stripe-proof artifact presence.
  artifacts:
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md
  key_links:
    - Authorized repaired SHA -> workflow_dispatch run identity -> live-stripe job/proof artifact -> sanitized evidence record
  prohibitions:
    - No second run, retry, re-run, replacement ref dispatch, or borrowed Phase 227 authority is used.
    - No evidence result is called proved from raw green status alone or from zero selected tests.
---

<objective>
Spend the single fresh evidence budget once and seal the sanitized provider outcome without rewriting history.

Purpose: Establish whether the repaired boot contract restores real provider proof under the bounded Phase 228 authority.
Output: One immutable Actions identity and a complete sanitized Phase 228 evidence/validation record.

Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-01-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-02-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch and bind the sole fresh run identity</name>
  <precondition>228-02-SUMMARY.md records explicit authorize-one-attempt, STRIPE_WEBHOOK_SECRET setup confirmation, and the evidence record still says consumed:false.</precondition>
  <read_first>
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-02-SUMMARY.md — must contain explicit authorization and sanitized setup confirmation.
    - .github/workflows/ci.yml — dispatch input `run_live_stripe` and stable `live-stripe` job identity.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — transport analog for binding one attempt-1 run while preserving prior authority.
  </read_first>
  <files>.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md</files>
  <action>First rerun both deterministic Plan 228-01 commands and require a clean committed repaired SHA. Read-only check that no Phase 228 run identity is already bound. Use GitHub CLI/API to dispatch ci.yml exactly once with workflow_dispatch, run_live_stripe:true, and a ref resolving exactly to that repaired SHA; if GitHub cannot dispatch a literal SHA, stop for maintainer direction rather than create a temporary ref or alternate transport without fresh authority. Poll read-only until the new attempt-1 run is uniquely bound by repository, event, input, SHA, creation time, and run ID. Immediately mark budget consumed:true once a run exists, regardless of outcome. If the API rejects the request and creates no run, record the sanitized rejection class with consumed:false and stop; never retry automatically. Do not re-run jobs or dispatch a replacement.</action>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; cd accrue &amp;&amp; mix test test/accrue/runtime_config_test.exs test/accrue/config_test.exs &amp;&amp; cd .. &amp;&amp; ( RECORD=.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md; if rg -q 'run_id: `[0-9]+`' "$RECORD"; then test "$(rg -c 'run_id: `[0-9]+`' "$RECORD")" -eq 1 &amp;&amp; RUN_ID="$(rg -o 'run_id: `[0-9]+`' "$RECORD" | rg -o '[0-9]+')" &amp;&amp; gh run view "$RUN_ID" --json databaseId,event,headSha,attempt,status,conclusion,url &amp;&amp; rg -n 'attempt: `1`|consumed: `true`' "$RECORD"; else rg -n 'outcome: `api_rejected_no_run`|rejection_class: `(authorization|validation|rate_limit|transport)`|consumed: `false`|additional dispatch authorized: `false`' "$RECORD" &amp;&amp; ! rg -q 'run_id: `[0-9]+`' "$RECORD"; fi )</automated>
  </verify>
  <acceptance_criteria>At most one new run exists; it is attempt 1 at the exact repaired SHA with the required manual input, its immutable ID/URL is bound, and the budget state accurately reflects whether GitHub created a run.</acceptance_criteria>
  <done>The one explicitly authorized dispatch is uniquely bound or a no-run rejection is sealed, with no automatic second attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile proof artifact and seal the sanitized outcome</name>
  <precondition>The Phase 228 evidence record contains exactly one bound run ID with consumed:true, or a no-run API rejection with consumed:false.</precondition>
  <read_first>
    - scripts/ci/provider_proof.mjs — canonical proof_state, reason_code, manifest-count, and fail-closed semantics; do not modify.
    - scripts/ci/verify_provider_proof.mjs — existing empty-green and artifact contract fixtures; do not modify.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — fill the schema created in Plan 228-01.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-PATTERNS.md — Phase 227 role analog and sanitization fence.
  </read_first>
  <files>.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md, .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md</files>
  <action>Branch on the evidence-record outcome. For a created run, wait read-only for terminal status, inspect the stable live-stripe job and artifact inventory, download live-stripe-proof into a temporary directory, and validate the finalized record plus manifest using the existing provider-proof semantics. Fill every predeclared evidence field once. Classify proved only when raw conclusion is success, proof_state is proved, selected_count and passed_count are nonzero and equal with failed_count zero, skipped_count and failed_count are recorded, the manifest is valid, and live-stripe-proof is present; otherwise record the canonical failure/misconfiguration/blocked reason without claiming recovery. For a no-run rejection, record outcome api_rejected_no_run, omit run_id, keep consumed:false, choose only the sanitized rejection class authorization, validation, rate_limit, or transport, record that no retry occurred, and leave Phase 227 evidence immutable; do not invent run, job, artifact, manifest, conclusion, or proof fields. Update 228-VALIDATION.md status rows and sign-off for the branch actually reached. Preserve Phase 227 bytes, omit raw logs and sensitive identities, delete only temporary downloaded artifacts, and state that no further attempt is authorized.</action>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; ( RECORD=.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md; if rg -q 'run_id: `[0-9]+`' "$RECORD"; then test "$(rg -c 'run_id: `[0-9]+`' "$RECORD")" -eq 1 &amp;&amp; rg -n 'repaired_sha|attempt: `1`|raw_conclusion: `success`|proof_state: `proved`|reason_code|selected_count: `[1-9][0-9]*`|passed_count: `[1-9][0-9]*`|failed_count: `0`|skipped_count|manifest_present: `true`|live-stripe-proof.*present|consumed: `true`|additional dispatch authorized: `false`' "$RECORD"; else rg -n 'outcome: `api_rejected_no_run`|rejection_class: `(authorization|validation|rate_limit|transport)`|consumed: `false`|retry_performed: `false`|additional dispatch authorized: `false`' "$RECORD" &amp;&amp; ! rg -q 'run_id: `[0-9]+`' "$RECORD"; fi ) &amp;&amp; git diff --exit-code -- .planning/phases/227-measured-critical-path-improvement</automated>
    <human-check>Maintainer verifies the recorded URL and final classification correspond to the sole authorized run and approves no additional dispatch.</human-check>
  </verify>
  <acceptance_criteria>The record contains a sanitized, repository-bound terminal outcome; proved is used only for complete nonempty provider evidence; Phase 227 is byte-unchanged; and the Phase 228 run ceiling is closed.</acceptance_criteria>
  <done>The sole outcome is reconciled against canonical proof semantics, sanitized, immutable, and closed to further dispatch.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new symbol, CLI flag, field, or path. It fills the predeclared fields in `228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md` and updates the task/status surfaces in `228-VALIDATION.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor -> GitHub Actions dispatch | An explicit human decision permits one irreversible external mutation. |
| GitHub Actions -> downloaded proof artifact | External run metadata/artifacts must be repository-, SHA-, and run-bound before trust. |
| Proof artifact -> planning evidence | Sanitized durable evidence must preserve truth without leaking credentials or personal data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-228-09 | Repudiation | dispatch/run binding | high | mitigate | Bind repository, event, input, exact SHA, attempt, creation time, run ID, and immutable URL; consume budget when a run exists. |
| T-228-10 | Information Disclosure | logs/artifact/evidence record | high | mitigate | Inspect only required fields, keep downloads temporary, and persist no values, raw logs, payloads, actors, endpoints, or presence detail. |
| T-228-11 | Tampering | proof classification | high | mitigate | Use canonical provider record/manifest semantics and require nonzero counts plus artifact presence; do not infer proof from workflow green. |
| T-228-12 | Denial of Service | retry/transport behavior | high | mitigate | Dispatch once, stop on no-run rejection or terminal failure, and prohibit automatic retry, replacement, or alternate-ref transport. |
</threat_model>

<verification>
Reconcile the unique run and downloaded proof read-only, rerun deterministic fixtures, validate all evidence fields, and prove the Phase 227 directory has no diff.
</verification>

<success_criteria>
Exactly zero or one run is created according to explicit authority, the sole result is classified from complete canonical evidence, the budget is closed, and historical evidence remains immutable.
</success_criteria>

<output>
Create `.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-03-SUMMARY.md` when done.
</output>

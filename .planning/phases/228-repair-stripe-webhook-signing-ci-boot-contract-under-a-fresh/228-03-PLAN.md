---
phase: 228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh
plan: "03"
type: execute
wave: 3
depends_on: [228-02]
files_modified:
  - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
  - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md
autonomous: false
requirements: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The sole newly authorized first-attempt run targets the repaired committed SHA with workflow_dispatch and run_live_stripe:true.
    - The immutable result is recorded honestly whether proved, failed, misconfigured, blocked, or rejected without creating a run.
    - Provider proof requires successful raw conclusion, nonzero selected and passing tests, a valid manifest, and live-stripe-proof artifact presence.
    - proof_state skipped with reason_code intentional_bypass is impossible under the exact Phase 228 workflow and is a blocking contract violation, never an accepted terminal tuple.
  artifacts:
    - scripts/ci/verify_stripe_webhook_boot_evidence.mjs
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md
  key_links:
    - Authorized repaired SHA + bounded dispatch window -> authoritative verifier live binding -> immutable workflow/run/job/step identity -> sanitized evidence record
    - Authoritative verifier terminal mode -> GitHub run/job/artifact inventory + temporary provider record/manifest -> exhaustive durable-record tuple
  prohibitions:
    - No second run, retry, re-run, replacement ref dispatch, or borrowed Phase 227 authority is used.
    - No evidence result is called proved from raw green status alone or from zero selected tests.
    - No created-run evidence is accepted as skipped/intentional_bypass; encountering it blocks sealing the phase outcome.
---

<objective>
Spend the single fresh evidence budget once and seal the sanitized provider outcome without rewriting history.

Purpose: Establish whether the repaired boot contract restores real provider proof under the bounded Phase 228 authority.
Output: One immutable Actions identity and a complete sanitized Phase 228 evidence/validation record.

Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-01-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-02-SUMMARY.md
@.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md
@.planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch and bind the sole fresh run identity</name>
  <precondition>228-02-SUMMARY.md records explicit authorize-one-attempt, STRIPE_WEBHOOK_SECRET setup confirmation, and the evidence record still says consumed:false.</precondition>
  <read_first>
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-02-SUMMARY.md — must contain explicit authorization and sanitized setup confirmation.
    - .github/workflows/ci.yml — dispatch input `run_live_stripe` and stable `live-stripe` job identity.
    - scripts/ci/verify_stripe_webhook_boot_evidence.mjs — authoritative fixture-tested live binding and no-run verification; do not duplicate its assertions inline.
    - .planning/phases/227-measured-critical-path-improvement/227-CI-CRITICAL-PATH.md — transport analog for binding one attempt-1 run while preserving prior authority.
  </read_first>
  <files>.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md</files>
  <action>First rerun all deterministic Plan 228-01 commands and require a clean committed repaired SHA. Record UTC dispatch-at immediately before the single API request and observed-at after the response/poll, and retain both as the bounded creation window. Read-only check that no Phase 228 run identity is already bound. Use GitHub CLI/API to dispatch ci.yml exactly once with workflow_dispatch, run_live_stripe:true, and a ref resolving exactly to that repaired SHA; if GitHub cannot dispatch a literal SHA, stop for maintainer direction rather than create a temporary ref or alternate transport without fresh authority. Once a run exists, fill the durable record and invoke the authoritative verifier's `--verify-live-binding` mode. It must independently fetch the current repository, immutable run URL, workflow name exactly CI, event workflow_dispatch, exact SHA, attempt 1, creation time within the recorded window, and job inventory containing the stable display job `Stripe test-mode parity (mandatory periodic)`. Because schedule is excluded by the independently fetched event, existence/execution of that input-gated job is the proof of the true manual input; do not trust a markdown self-attestation. Require the named preflight, suite, finalizer, and upload steps with branch-appropriate terminal status/conclusion, and exact agreement between record and GitHub facts for identity, created_run:true, consumed:true, no retry, and closed authority. If the request is rejected with no created run, fill the complete sanitized dispatch-intent/rejection tuple and run the same mode so it independently proves no matching new run exists inside the bounded window; require created_run:false, consumed:false, empty run/proof-only fields, no retry, and closed authority. Never retry, rerun jobs, or dispatch a replacement.</action>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --verify-live-binding --record .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md --repository "$(gh repo view --json nameWithOwner --jq .nameWithOwner)" --sha "$(git rev-parse HEAD)" --dispatch-at "$(sed -n 's/^- dispatch_at: `\([^`]*\)`.*/\1/p' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md)" --observed-at "$(sed -n 's/^- observed_at: `\([^`]*\)`.*/\1/p' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md)"</automated>
  </verify>
  <acceptance_criteria>At most one new run exists; it is attempt 1 at the exact repaired SHA with the required manual input, its immutable ID/URL is bound, and the budget state accurately reflects whether GitHub created a run.</acceptance_criteria>
  <done>The one explicitly authorized dispatch is uniquely bound or a no-run rejection is sealed, with no automatic second attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile proof artifact and seal the sanitized outcome</name>
  <precondition>The Phase 228 evidence record contains exactly one bound run ID with consumed:true, or a no-run API rejection with consumed:false.</precondition>
  <read_first>
    - scripts/ci/provider_proof.mjs — canonical proof_state, reason_code, manifest-count, and fail-closed semantics; do not modify.
    - scripts/ci/verify_provider_proof.mjs — existing empty-green and artifact contract fixtures; do not modify.
    - scripts/ci/verify_stripe_webhook_boot_evidence.mjs — authoritative terminal reconciliation and exhaustive tuple contract; do not reimplement it inline.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md — fill the schema created in Plan 228-01.
    - .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-PATTERNS.md — Phase 227 role analog and sanitization fence.
  </read_first>
  <files>.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md, .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-VALIDATION.md</files>
  <action>Branch on the evidence-record outcome and fill every applicable field exactly once. For a created run, wait read-only for terminal status, then invoke the verifier's `--verify-terminal` mode. It must independently fetch run/job/artifact inventory, download `live-stripe-proof` into a newly created temporary directory, parse `accrue-provider-proof.json` and `accrue-provider-manifest.json` when present, compare both to GitHub and the durable record, and remove the temporary download before exit. Accept proved only for created_run:true, consumed:true, attempt 1, GitHub and stable job raw success, proof_state proved, reason_code complete_provider_evidence, positive selected_count equal to passed_count, zero failed_count and skipped_count, valid manifest totals/timestamps, successful finalizer, present artifact, no retry, and closed authority. Keep accepted created-run non-proved tuples limited to the exact reachable workflow outcomes: misconfigured/configuration_incomplete or manifest_invalid without a valid manifest; misconfigured/zero_selected_tests or unaccounted_skipped_tests with the corresponding valid manifest facts; blocked/job_cancelled, job_timed_out, job_action_required, or job_did_not_complete with exact raw conclusion; and failed/selected_assertions_failed with failure facts. Require their exact canonical reason, raw conclusion, counts, manifest, finalizer, and artifact semantics plus created_run:true, consumed:true, attempt 1, independently matching GitHub/job facts, and explicit absence of any proved claim. Treat proof_state skipped with reason_code intentional_bypass as a blocking contract violation: it is not an allowed Phase 228 terminal tuple because the mandatory-provider workflow exposes no bypass input, the workflow_dispatch path executes the run_live_stripe-gated live-stripe job, and the finalizer invocation passes neither a bypass flag nor reason. Stop without sealing validation if that impossible pair appears. For a no-run rejection, require the complete sanitized dispatch-intent and allowed rejection tuple, independently prove no matching run exists in the bounded window, and require every run/job/conclusion/count/manifest/artifact/proof-only field empty. Reject unknown, duplicate, or conflicting fields in all branches; retain privacy allowlists, no retry, closed authority, and Phase 227 immutability. Update validation for the actual branch only after this verifier passes.</action>
  <verify>
    <automated>node scripts/ci/verify_provider_proof.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --fixtures &amp;&amp; node scripts/ci/verify_stripe_webhook_boot_evidence.mjs --verify-terminal --record .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md --repository "$(gh repo view --json nameWithOwner --jq .nameWithOwner)" --sha "$(git rev-parse HEAD)" --dispatch-at "$(sed -n 's/^- dispatch_at: `\([^`]*\)`.*/\1/p' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md)" --observed-at "$(sed -n 's/^- observed_at: `\([^`]*\)`.*/\1/p' .planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md)" &amp;&amp; git diff --exit-code -- .planning/phases/227-measured-critical-path-improvement</automated>
    <human-check>Maintainer verifies the recorded URL and final classification correspond to the sole authorized run and approves no additional dispatch.</human-check>
  </verify>
  <acceptance_criteria>The record contains a sanitized, repository-bound terminal outcome; proved is used only for complete nonempty provider evidence; Phase 227 is byte-unchanged; and the Phase 228 run ceiling is closed.</acceptance_criteria>
  <done>The sole outcome is reconciled against canonical proof semantics, sanitized, immutable, and closed to further dispatch.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan consumes the Phase 228 Wave 1 verifier at `scripts/ci/verify_stripe_webhook_boot_evidence.mjs` through `--verify-live-binding` and `--verify-terminal`. It fills the predeclared fields in `228-STRIPE-WEBHOOK-BOOT-EVIDENCE.md` and updates the task/status surfaces in `228-VALIDATION.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor -> GitHub Actions dispatch | An explicit human decision permits one irreversible external mutation. |
| GitHub Actions -> downloaded proof artifact | External run metadata/artifacts must be repository-, SHA-, and run-bound before trust. |
| Proof artifact -> planning evidence | Sanitized durable evidence must preserve truth without leaking credentials or personal data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-228-09 | Repudiation | dispatch/run binding | high | mitigate | Bind repository, event, input, exact SHA, attempt, creation time, run ID, and immutable URL; consume budget when a run exists. |
| T-228-10 | Information Disclosure | logs/artifact/evidence record | high | mitigate | Inspect only required fields, keep downloads temporary, and persist no values, raw logs, payloads, actors, endpoints, or presence detail. |
| T-228-11 | Tampering | proof classification | high | mitigate | Use canonical provider record/manifest semantics and require nonzero counts plus artifact presence; do not infer proof from workflow green. |
| T-228-12 | Denial of Service | retry/transport behavior | high | mitigate | Dispatch once, stop on no-run rejection or terminal failure, and prohibit automatic retry, replacement, or alternate-ref transport. |
</threat_model>

<verification>
Reconcile the unique run and downloaded proof read-only, rerun deterministic fixtures, validate all evidence fields, and prove the Phase 227 directory has no diff.
</verification>

<success_criteria>
Exactly zero or one run is created according to explicit authority, the sole result is classified from complete canonical evidence, the budget is closed, and historical evidence remains immutable.
</success_criteria>

<output>
Create `.planning/phases/228-repair-stripe-webhook-signing-ci-boot-contract-under-a-fresh/228-03-SUMMARY.md` when done.
</output>

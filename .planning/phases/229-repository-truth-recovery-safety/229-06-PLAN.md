---
phase: 229-repository-truth-recovery-safety
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/collect_repository_inventory.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`--observe-remote` installs a real bounded `gh api` read adapter for exactly four allowed observations: remote main, all open PR heads, all `release/*` refs, and bounded recent Actions runs, per D-02 and D-08."
    - "Singleton remote main uses one full SHA; plural categories use deterministically ordered full-SHA arrays and distinguish confirmed zero results from unavailable observation, including stable secondary ordering when primary comparison keys are equal."
    - "The inventory contains every `git worktree list --porcelain` entry with SHA, branch/detached state, and per-worktree dirty state but no absolute path, plus actual bounded ship-window facts from `.planning/WINDOWS.md` or the supported GSD status authority (D-01, D-03)."
    - "The private recovery manifest repository must exactly equal `szTheory/accrue` before bundle, artifact, worktree, planning, or remote work begins, and all recovery inputs have explicit null/empty/single behavior (D-04)."
  prohibitions:
    - statement: "Unavailable evidence must not masquerade as observed evidence or provider proof (D-02, D-09)."
      status: flagged-unverified
    - statement: "The collector must not mutate GitHub, resolve ship windows, refresh refs, or cross into Phase 230 integration (D-08, D-10)."
      status: flagged-unverified
  artifacts:
    - path: scripts/ci/collect_repository_inventory.mjs
      provides: "Complete recovery-gated local inventory and bounded live GitHub observation adapter"
  key_links:
    - from: "collect_repository_inventory.mjs CLI --observe-remote"
      to: "GitHub REST API"
      via: "spawnSync('gh', ['api', fixed repository-bound endpoint]) with shell disabled and explicit bounds"
      pattern: 'spawnSync\(.*gh|GET /repos/szTheory/accrue'
    - from: scripts/ci/collect_repository_inventory.mjs
      to: .planning/WINDOWS.md
      via: "bounded parser or supported raw status command"
      pattern: 'WINDOWS\.md|windows status'
---

<objective>
Make the canonical collector report complete local repository truth and genuine, bounded GitHub read observations.

Purpose: Close CR-06 through CR-08 and the REPO-01/REPO-03 edge probes without weakening the recovery-before-observation barrier.
Output: A collector with real read-only API wiring, complete worktree/window collection, plural result preservation, and repository-bound recovery provenance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
@.planning/WINDOWS.md
@scripts/ci/collect_repository_inventory.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Observe the complete authorized GitHub surface with zero/one/many semantics</name>
  <files>scripts/ci/collect_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/collect_ci_baseline.mjs
    scripts/ci/ci_monitor.cjs
  </read_first>
  <behavior>
    - "The CLI's `--observe-remote` path invokes four fixed repository-bound GET endpoints through `gh api`; no adapter call occurs until manifest identity, anchored digest, bundle membership, encoded refs, and artifact attestation pass."
    - "Remote main rejects zero/multiple/null SHA shapes; open PRs, release branches, and recent Actions accept confirmed empty arrays and preserve every bounded result for zero, one, and many fixtures."
    - "Plural output order is deterministic by documented primary keys and full SHA/remote ID tie-breakers when primary values are equal."
    - "Authentication, rate-limit, network, timeout, malformed JSON/data, and overflow outcomes become distinct explicit unavailable facts without substituting cached/local values."
  </behavior>
  <action>Use `scripts/ci/collect_ci_baseline.mjs`, the mapped collector analog, for its dependency-free ESM CLI, explicit repository validation before the live command boundary, allowlisted record normalization, deterministic sorting, and stable top-level failure path. Add fixtureable `createGhApiReadAdapter` and bounded process invocation seams to the collector, plus embedded `node:test` cases in this same module so this Wave 1 plan verifies its behavior independently of Plan 229-07. Use `spawnSync` with `shell:false`, a fixed executable/argv split, explicit timeout/maxBuffer, and exact `szTheory/accrue` endpoints derived from the four INTEGRATE rows in `COVERAGE.md`. Bound pagination, per-page size, total items, and recent Actions time/run count; reject redirects or response shapes that escape the intended repository identity. Normalize remote main separately from plural categories so confirmed empty plural results remain `available: true` with `shas: []`, while null/invalid singleton and unavailable calls fail into a reasoned unavailable state. Preserve complete arrays and deterministic tie-breaking rather than retaining the first result. Keep Actions conclusion separate from provider proof per D-09, and audit adapter argv so no API mutation or workflow action is reachable per D-08.</action>
  <verify>
    <automated>node --check scripts/ci/collect_repository_inventory.mjs &amp;&amp; node --test scripts/ci/collect_repository_inventory.mjs</automated>
    <fails_when>syntax fails; `--observe-remote` makes zero adapter calls after a valid recovery barrier; any call is not a fixed repository-bound GET; empty plural data is unavailable; a plural SHA is dropped; equal-key permutations change output; or a failure is replaced with cached/local truth</fails_when>
  </verify>
  <acceptance_criteria>
    CR-06's missing CLI adapter is exercised through an injected subprocess seam and all four request argv are asserted.
    Exact fixtures cover null, empty, singleton, multiple, equal-key order, malformed/overflow, and each unavailable reason for all applicable remote categories.
    No mutation, publication, rerun/cancellation, PR change, ref/tag movement, or ship-window resolution capability appears in the adapter surface.
  </acceptance_criteria>
  <done>A maintainer invoking `--observe-remote` gets real bounded repository-bound observations, including every zero/one/many plural result, or honest unavailable evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collect every worktree and actual ship-window fact behind repository-bound recovery</name>
  <files>scripts/ci/collect_repository_inventory.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/WINDOWS.md
    .planning/STATE.md
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/collect_ci_baseline.mjs
  </read_first>
  <behavior>
    - "A temporary repository with multiple clean, dirty, branch, and detached worktrees emits one sanitized row per porcelain record in deterministic order, with no physical paths in public output."
    - "A non-empty Phase 229 window fixture emits every bounded window identity/state from the declared authority; a confirmed empty ledger emits an explicit empty array, while null/malformed/internally inconsistent authority fails closed."
    - "A private manifest for `other/repo`, a null manifest, or an empty ref set is rejected before bundle/adapter access; one valid ref is accepted and remains fully checked."
  </behavior>
  <action>Continue the mapped `scripts/ci/collect_ci_baseline.mjs` collector pattern: validate the fixed repository identity before collection, normalize each authority through a fixed allowlisted boundary, discard machine-specific paths and raw payloads, and deterministically sort the result. Pass the frozen validation context into `readTrustedRecoveryManifest` and require its allowlisted `repository` field to exactly match `expectedRepository` before dependent reads, closing CR-08. Replace the single current-checkout row with a bounded parser for every `git worktree list --porcelain` record; calculate dirty state by running the existing bounded Git helper against each worktree internally, then discard absolute paths before schema output. Replace the static ship-window array with bounded facts read from the declared `.planning/WINDOWS.md` authority or the supported raw GSD status command, preserving IDs/status without resolving or waiving any window. Add these cases to the module's embedded `node:test` suite and define explicit validation for empty, singleton, multiple, null, duplicate, and equal-sort-key category inputs. Preserve separately named local/cached/milestone/tag identities per D-03 and do not refresh refs or integrate histories per D-10.</action>
  <verify>
    <automated>node --test scripts/ci/collect_repository_inventory.mjs</automated>
    <fails_when>CR-07's multi-worktree or non-empty-window fixture is incomplete; absolute worktree paths leak; an empty ledger is ambiguous; malformed/null planning data passes; a foreign manifest reaches bundle/adapter access; or equal-key order is unstable</fails_when>
  </verify>
  <acceptance_criteria>
    CR-07 fixtures contain at least two worktrees with distinct dirty/detached states and a non-empty window set, and every expected row is present.
    CR-08's foreign-repository manifest is rejected before any bundle, artifact, worktree, planning, or remote adapter call.
    The REPO-01 unclassified probe has explicit behavior for named refs, all refs, artifacts, all worktrees, planning state, ship windows, remote main, PR heads, release branches, and Actions.
  </acceptance_criteria>
  <done>The collector covers every named repository-truth category from its real authority and cannot relabel a foreign recovery capsule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| private recovery manifest → collector | Private provenance must match the fixed repository before any other authority is queried. |
| GitHub/Git/GSD output → canonical schema | Untrusted external and local command output becomes committed evidence only after bounded normalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G06-01 | Spoofing | recovery manifest repository | high | mitigate | Compare the allowlisted manifest repository with the frozen validation context before dependent work. |
| T-229-G06-02 | Tampering | GitHub API adapter | high | mitigate | Fixed GET-only endpoint registry, shell-disabled argv, exact repository validation, and adapter-call assertions. |
| T-229-G06-03 | Denial of Service | pagination/process output | high | mitigate | Enforce page/item/time/buffer bounds and fail explicitly on overflow. |
| T-229-G06-04 | Information Disclosure | worktree/API evidence | high | mitigate | Drop physical paths and raw payloads; retain only allowlisted identities, SHAs, state, time, and request provenance. |
| T-229-G06-05 | Repudiation | empty/unavailable facts | medium | mitigate | Distinguish confirmed empty arrays from unavailable observation with deterministic typed states. |
</threat_model>

<verification>
Run the complete fixture suite with an adapter-call counter and inspect the Git diff to confirm only the collector changed and no remote mutation or local ref refresh command was introduced.
</verification>

<success_criteria>
CR-06 through CR-08 are closed; all five specless collection/ordering/empty-state expectations have concrete behavior where applicable; and the collector remains recovery-gated, read-only, bounded, deterministic, and privacy-safe.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/collect_repository_inventory.mjs`: exported bounded GitHub read adapter, singleton/plural remote schemas, all-worktree collector, ship-window authority reader, repository-bound manifest validation, and deterministic edge-case normalization.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-06-SUMMARY.md` when done.
</output>

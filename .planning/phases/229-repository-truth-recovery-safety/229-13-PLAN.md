---
phase: 229-repository-truth-recovery-safety
plan: 13
type: execute
wave: 2
depends_on:
  - 229-10
  - 229-11
  - 229-12
files_modified:
  - scripts/ci/verify_repository_inventory.mjs
  - scripts/ci/phase229_gap_closure.test.mjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CR-04 and CR-05: strict verification proves the live active ref/object exists and exactly reconciles both canonical preservation and non-preservation ref maps against independent recovery authorities."
    - "CR-06: canonical worktrees and ship windows exactly match direct Git porcelain and `.planning/WINDOWS.md` authorities, including empty, missing, extra, duplicate, dirty, detached, and changed-state cases."
    - "CR-07 and CR-08: each remote category has its exact producing request contract, and every forbidden absolute/path-like or control-bearing value is rejected before rendering."
    - "WR-03: a named adversarial suite replays CR-01 through CR-09 plus selected/viewed-run and wall-clock boundaries through public/process interfaces, so no self-consistent fabricated inventory or shallow source assertion can pass."
  artifacts:
    - path: scripts/ci/verify_repository_inventory.mjs
      provides: "Independent live/capsule/canonical authority reconciliation and field-specific provenance/privacy enforcement"
    - path: scripts/ci/phase229_gap_closure.test.mjs
      provides: "Complete named Phase 229 adversarial regression matrix"
  key_links:
    - from: scripts/ci/verify_repository_inventory.mjs
      to: "Git refs/worktrees, WINDOWS.md, private manifest, bundle heads, and encoded preservation refs"
      via: "strict exact-map/multiset reconciliation reads each independent authority rather than trusting canonical assertions"
      pattern: "symbolic-ref|cat-file|worktree list|WINDOWS|bundle list-heads"
    - from: scripts/ci/phase229_gap_closure.test.mjs
      to: "preservation, collector, verifier, and monitor public/process boundaries"
      via: "named counterexample fixtures cover every CR-01..CR-09 and WR-02 behavior"
      pattern: "CR-0[1-9]|WR-02"
---

<objective>
Make the strict verifier an independent authority check and lock every refreshed blocker behind adversarial regressions.

Purpose: Close CR-04 through CR-08 and WR-03 after the production boundaries from Plans 229-10 through 229-12 are corrected.
Output: Exact live/capsule/canonical reconciliation plus a complete named counterexample suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
@.planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
@.planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
@.planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
@.planning/phases/229-repository-truth-recovery-safety/229-07-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md
@.planning/phases/229-repository-truth-recovery-safety/229-12-SUMMARY.md
@scripts/ci/verify_repository_inventory.mjs
@scripts/ci/phase229_gap_closure.test.mjs
@scripts/ci/preserve_repository_state.sh
@scripts/ci/collect_repository_inventory.mjs
@scripts/ci/ci_monitor.cjs
@scripts/ci/verify_ci_baseline.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one fabricated active ref through every independent authority</name>
  <files>scripts/ci/verify_repository_inventory.mjs, scripts/ci/phase229_gap_closure.test.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-CONTEXT.md
    .planning/phases/229-repository-truth-recovery-safety/229-PATTERNS.md
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/WINDOWS.md
    scripts/ci/verify_repository_inventory.mjs
    scripts/ci/phase229_gap_closure.test.mjs
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/verify_ci_baseline.mjs
  </read_first>
  <reversibility rating="reversible">These checks strengthen a local verifier and fixture suite without changing the canonical schema's external write surface.</reversibility>
  <behavior>
    - "Replacing both the active canonical row and `milestone_branch` with a nonexistent full SHA fails after live symbolic-ref resolution and `git cat-file -e`; the actual current active object passes while its frozen object remains recoverable."
    - "Removing, adding, duplicating, or changing any canonical Phase 229 preservation row fails exact equality against the independently resolved encoded-ref map."
    - "Removing or inventing a ship window, omitting or inventing a worktree, or changing branch/detached/SHA/dirty state fails exact authority reconciliation."
  </behavior>
  <action>Begin with the exact CR-04, CR-05, and CR-06 false-positive variants from `229-VERIFICATION.md` as failing tests. In `assertStrictRecovery`, split `refs.all` into preservation and non-preservation maps: compare preservation rows exactly against independently resolved encoded refs, including missing/extra/duplicate/changed mappings; compare non-preservation rows against the private frozen map with the existing narrow active-branch continuity exception. Resolve the live active ref using `git symbolic-ref -q HEAD`, resolve `${activeRef}^{object}`, prove it exists with `git cat-file -e`, and require that live object to equal both the canonical active row and `refs.milestone_branch`; never accept a merely well-shaped SHA. Extend `assertCompleteCategories` with verifier-owned authority readers: directly parse `git worktree list --porcelain`, query each physical worktree status without persisting paths, parse the bounded `.planning/WINDOWS.md` table/counts, then compare canonical arrays as exact sorted multisets. Do not use `collectRepositoryInventory`, asserted booleans, non-empty arrays, or collector-normalization helpers as independent proof. Preserve D-04's frozen bundle/encoded-ref recovery and allow only the active execution branch to advance.</action>
  <verify>
    <automated>node --test scripts/ci/phase229_gap_closure.test.mjs &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-recovery --require-all-ref-recovery --require-complete-categories</automated>
    <fails_when>the all-`f` active object passes; canonical preservation rows can be removed or invented; empty/invented windows pass; a worktree is missing/extra/changed without failure; current valid authority fails; or collector output is used as its own verifier authority</fails_when>
  </verify>
  <acceptance_criteria>
    CR-04, CR-05, and CR-06 each have named failing mutations and a valid authority-backed control.
    The active branch exception requires the actual live object while retaining independent recovery of the frozen object.
    Canonical preservation refs, non-preservation refs, worktrees, and ship windows reconcile exactly, including multiplicity and deterministic ordering.
  </acceptance_criteria>
  <done>The strict verifier rejects fabricated or incomplete local repository truth instead of validating self-consistent canonical claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact remote provenance, comprehensive privacy, and the full adversarial matrix</name>
  <files>scripts/ci/verify_repository_inventory.mjs, scripts/ci/phase229_gap_closure.test.mjs</files>
  <read_first>
    .planning/phases/229-repository-truth-recovery-safety/229-VERIFICATION.md
    .planning/phases/229-repository-truth-recovery-safety/229-REVIEW.md
    .planning/phases/229-repository-truth-recovery-safety/COVERAGE.md
    .planning/phases/229-repository-truth-recovery-safety/229-10-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-11-SUMMARY.md
    .planning/phases/229-repository-truth-recovery-safety/229-12-SUMMARY.md
    scripts/ci/verify_repository_inventory.mjs
    scripts/ci/phase229_gap_closure.test.mjs
    scripts/ci/preserve_repository_state.sh
    scripts/ci/collect_repository_inventory.mjs
    scripts/ci/ci_monitor.cjs
  </read_first>
  <behavior>
    - "CR-07 rejects an unrelated same-repository endpoint and any missing, duplicated, skipped, reordered, foreign, or over-bound page request for its claimed category."
    - "CR-08 rejects POSIX `/var`, `/root`, `/opt`, `/private`, Windows drive, UNC, and `file:` URI locations plus every control character in fields where paths/private locations are forbidden."
    - "WR-03 names and executes CR-01 through CR-09 plus WR-02 boundaries through shell/CLI/process APIs, and each negative variant first proves the vulnerable shape before asserting corrected rejection."
  </behavior>
  <action>Replace the prefix-only provenance check with a category-specific exact contract: remote main has its one fixed request; PRs, release refs, and Actions use the complete ordered bounded request sequences implemented in Plan 229-11, with explicit page size/page continuity and terminal proof. Ensure unavailable facts retain the exact attempted sequence through the failure point without claiming SHAs. Replace the narrow platform-prefix privacy regex with field-specific validation: apply `path.posix.isAbsolute`, `path.win32.isAbsolute`, UNC and drive-relative detection, case-insensitive `file:` URI rejection, and all C0/DEL control rejection wherever a filesystem location is forbidden; keep normalized repository-relative artifact paths accepted only by their dedicated schema. Add the exact CR-07 `/issues` and CR-08 `/var/private/...` variants plus `/root`, `/opt`, `/private/tmp`, UNC, Windows-drive, drive-relative, file-URI, newline, carriage-return, tab, and DEL cases.

Expand `phase229_gap_closure.test.mjs` into the single named adversarial matrix required by WR-03. Exercise CR-01 mutation and CR-02 raw symlink bytes via the preservation process; CR-03 two-page/overflow via the injected GitHub adapter; CR-04 through CR-08 via freshly rendered strict variants and real fixture authorities; CR-09 via a delayed monitor subprocess; and WR-02 via selected/viewed ID and workflow mismatch. Each case must invoke the same public function, CLI, or subprocess boundary maintainers use, assert non-zero/rejection and absence of false PASS/partial evidence, clean only test-owned temporaries, and leave all workspace artifacts and existing external capsules untouched.</action>
  <verify>
    <automated>node --test scripts/ci/phase229_gap_closure.test.mjs &amp;&amp; node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-recovery --require-all-ref-recovery --require-typed-artifacts --require-complete-categories --require-edge-cases --require-command-provenance --require-privacy-controls --require-determinism</automated>
    <fails_when>an unrelated endpoint passes; any absolute/path-like/control variant reaches rendering; any CR-01..CR-09 or WR-02 named regression is absent or source-text-only; a vulnerable variant is not shown to reach its target boundary; test cleanup touches non-test paths; or the strict happy path regresses</fails_when>
  </verify>
  <acceptance_criteria>
    CR-07 and CR-08 exact reproduced variants fail the public strict command.
    WR-03 is closed by named behavioral tests for every refreshed critical boundary plus selected/viewed identity and wall-clock deadline.
    The suite proves successful, unavailable, and rejected states without fabricating remote evidence or exposing private locations.
  </acceptance_criteria>
  <done>Every canonical provenance/privacy claim is independently enforced and every refreshed blocker is locked behind executable adversarial coverage.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 Trust Boundaries

| Boundary | Description |
|---|---|
| canonical inventory → independent verifier | Self-consistent ref/category claims are untrusted until reconciled with live, planning, and private recovery authorities. |
| remote provenance/private strings → committed rendering | Requests must be capable of producing their category, and machine-specific paths/control data must be rejected. |
| regression fixtures → repository/capsule filesystem | Tests must isolate all mutation and cleanup to owned temporary paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-229-G13-01 | Spoofing | active/canonical refs | high | mitigate | Resolve the live active object, prove object existence, and exact-map both preservation and non-preservation rows. |
| T-229-G13-02 | Tampering | worktree/window categories | high | mitigate | Compare exact canonical multisets against direct Git porcelain and bounded planning-ledger authorities. |
| T-229-G13-03 | Repudiation | remote request provenance | high | mitigate | Validate exact category-specific singleton/page sequences and terminal-page semantics. |
| T-229-G13-04 | Information Disclosure | canonical strings/rendering | high | mitigate | Apply field-specific POSIX/Windows/UNC/file-URI/control rejection before render. |
| T-229-G13-05 | Elevation of Privilege | adversarial fixtures | medium | mitigate | Use shell-disabled argv and test-owned temporary directories; assert cleanup never targets workspace/capsule paths. |
</threat_model>

<verification>
Run the named adversarial suite and every strict verifier flag together. Confirm `git status --short` contains no changes outside the two plan-owned tracked files and pre-existing untracked/capsule hashes are unchanged.
</verification>

<success_criteria>
CR-04 through CR-08 and WR-03 are closed, all CR-01 through CR-09 plus WR-02 have executable regression evidence, and REPO-01/02/03 claims are independently authority-backed rather than self-asserted.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/verify_repository_inventory.mjs`: exact canonical ref/worktree/window/provenance/privacy authority verification.
- `scripts/ci/phase229_gap_closure.test.mjs`: named process/public-interface adversarial matrix for CR-01 through CR-09 and WR-02.

<output>
Create `.planning/phases/229-repository-truth-recovery-safety/229-13-SUMMARY.md` when done.
</output>

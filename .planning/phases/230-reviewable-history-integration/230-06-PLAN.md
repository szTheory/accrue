---
phase: 230-reviewable-history-integration
plan: 06
type: execute
wave: 5
depends_on: ["230-04", "230-05"]
files_modified:
  - scripts/ci/verify_phase230_archive_invariants.mjs
  - scripts/ci/README.md
  - .planning/phases/230-reviewable-history-integration/230-REF-EXCEPTIONS.json
  - .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json
  - .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md
autonomous: true
requirements: [INTG-01, INTG-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every hardcoded .planning/phases/<slug> literal in scripts/ci/** and .github/workflows/** either resolves on disk or is reachable via resolvePhaseEvidencePath; a literal naming a now-archived slug is a hard failure."
    - "The archive-path sweep is a standing fail-closed invariant that runs on the candidate, not a one-time manual check, so a literal introduced by the merge is caught at merge time."
    - "A code-only review branch exists whose non-.planning tree is identical to the candidate's: git diff --quiet <candidate> <review> -- . ':!.planning' exits 0."
    - "The review branch is declared in 230-REF-EXCEPTIONS.json with a retirement trigger, so the third ref is an evidenced decision rather than undeclared churn."
    - "Changed-file scope is recorded as three recomputed integers — total changed files, .planning-only changed files, and source changed files — so a reviewer knows how many files they actually read."
    - "scripts/ci/README.md carries a Phase 230 evidence row in the existing three-column shape with one runnable verify command."
    - "Items flagged for Phase 232 are recorded with their reasons and are not acted on in this phase."
  artifacts:
    - path: "scripts/ci/verify_phase230_archive_invariants.mjs"
      provides: "Standing fail-closed sweep over hardcoded phase-evidence path literals"
      exports: ["verifyArchiveInvariants"]
    - path: "scripts/ci/README.md"
      provides: "Contributor-facing evidence map including the Phase 230 row"
      contains: "230-INTEGRATION-DISPOSITION"
  key_links:
    - from: "scripts/ci/verify_phase230_archive_invariants.mjs"
      to: "scripts/ci/phase_evidence_path.mjs"
      via: "a literal that does not resolve on disk must be reachable through resolvePhaseEvidencePath"
      pattern: "resolvePhaseEvidencePath"
  prohibitions:
    - "Must not act on an item explicitly scoped to Phase 232 — record it with its reason and stop."
    - "Must not satisfy the archive-path sweep by deleting or weakening a check that names an evidence path; a stale literal is repaired by making it archive-aware."
    - "Must not push any ref or open any pull request in this plan."
    - "Must not embed adopter, customer, or personal identifiers, absolute filesystem paths, or $HOME in any committed evidence artifact."
---

<objective>
Turn the archive-resolution fix into a standing invariant, give the reviewer a 72-file read instead of a 283-file read, and record what Phase 232 inherits.

Purpose: D-22 observes that `origin/main` never touched the archive-resolution scripts, so the four closure commits survive the merge — but the merge is exactly when new hardcoded `.planning/phases/<slug>` literals can sneak in. A one-time fix does not survive the next merge; a fail-closed sweep does. D-04 observes that milestone-versus-merge-base is 283 files across 491 commits, of which 211 files and 260 commits are `.planning/`-only: the reviewer's real source surface is 72 files, and the 491-commit provenance branch answers a different question and should be linked rather than diffed.

Output: a standing archive-path sweep, a code-only review branch proved identical on source, recomputed scope integers, a Phase 230 row in the contributor evidence map, and recorded Phase-232 handoffs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-reviewable-history-integration/230-CONTEXT.md
@.planning/phases/230-reviewable-history-integration/230-PATTERNS.md
@.planning/phases/230-reviewable-history-integration/230-04-SUMMARY.md
@.planning/phases/230-reviewable-history-integration/230-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standing fail-closed archive-path invariant sweep</name>
  <files>scripts/ci/verify_phase230_archive_invariants.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/ci/phase_evidence_path.mjs (whole file, 59 lines — `resolvePhaseEvidencePath` checks `.planning/phases/<slug>/<artifact>` first, then falls back to `.planning/milestones/*-phases/<slug>/<artifact>`, and errors on zero or ambiguous matches)
    - scripts/ci/phase_evidence_path.test.mjs (the existing test conventions for that module)
    - .planning/v1.61-MILESTONE-AUDIT.md (the audit that produced closure commits F-01 through F-04 — the failures this sweep must prevent recurring)
    - .planning/phases/230-reviewable-history-integration/230-CONTEXT.md (D-22)
    - .github/workflows/ci.yml (the `docs-contracts-shift-left` job where new merge-blocking verifiers land)
  </read_first>
  <action>
Write `scripts/ci/verify_phase230_archive_invariants.mjs` as a standing, fail-closed sweep, following the `--fixtures` + `NODE_TEST_CONTEXT` + `fail()`-thrower conventions of the existing `scripts/ci/*.mjs` verifiers.

Scan every file under `scripts/ci/**` and `.github/workflows/**` for string literals matching the `.planning/phases/<slug>` shape. For each literal found, assert one of two things holds: the path resolves on disk as written, OR the literal is reachable through `resolvePhaseEvidencePath` from `scripts/ci/phase_evidence_path.mjs` (that is, the artifact is found in the archived location `.planning/milestones/*-phases/<slug>/`). A literal that satisfies neither is a hard failure naming the file, the line, and the literal.

A literal naming a slug that is ALREADY archived — present under `.planning/milestones/*-phases/` and absent under `.planning/phases/` — is a hard failure even if some path happens to exist, because that is exactly the F-01 through F-03 class the audit found: a check that silently reads the wrong thing after archiving. Repair such a literal by routing it through `resolvePhaseEvidencePath`; never by deleting the check.

Emit a recomputed integer count of scanned files and of literals found, and export `verifyArchiveInvariants` so the sweep is callable from a test as well as from the CLI. Add fixtures proving: a literal resolving on disk passes; a literal resolving only via the archived fallback passes; a literal resolving nowhere fails naming the file and line; and a literal naming an already-archived slug without going through `resolvePhaseEvidencePath` fails.

Register the sweep in `.github/workflows/ci.yml` under the `docs-contracts-shift-left` job so it is merge-blocking, following the invocation shape the neighbouring steps in that job already use. The sweep must run against the candidate's tree, which is how a literal introduced by the merge is caught at merge time rather than after archiving.
  </action>
  <acceptance_criteria>
    - `node scripts/ci/verify_phase230_archive_invariants.mjs --fixtures` prints a line containing `fixtures: PASS` and exits 0.
    - `node scripts/ci/verify_phase230_archive_invariants.mjs` run against the candidate's tree exits 0.
    - A fixture proves a `.planning/phases/<slug>` literal that resolves nowhere fails with a message naming the file and line number.
    - A fixture proves a literal naming an already-archived slug, not routed through `resolvePhaseEvidencePath`, fails.
    - A fixture proves a literal resolving only via the archived fallback passes.
    - The scanned-file count and literal count are integers in the output.
    - `.github/workflows/ci.yml` contains a `docs-contracts-shift-left` step invoking the sweep.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci/verify_phase230_archive_invariants.mjs &amp;&amp; node --test scripts/ci/verify_phase230_archive_invariants.mjs</automated>
    <fails_when>non-zero exit, or the node:test summary reports a non-zero "fail" count, or reports "tests 0"</fails_when>
    <automated>node scripts/ci/verify_phase230_archive_invariants.mjs --fixtures &amp;&amp; node scripts/ci/verify_phase230_archive_invariants.mjs</automated>
    <fails_when>non-zero exit, or the output names an unresolvable literal, or the scanned-file count is 0 (which would mean the sweep matched nothing)</fails_when>
  </verify>
  <done>A fail-closed, merge-blocking sweep asserts every phase-evidence path literal in `scripts/ci/**` and `.github/workflows/**` resolves or is archive-aware, with fixtures covering both passing and both failing shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Code-only review branch and recomputed changed-file scope</name>
  <files>.planning/phases/230-reviewable-history-integration/230-REF-EXCEPTIONS.json, scripts/ci/verify_integration_disposition.mjs, scripts/ci/verify_repository_inventory.mjs</files>
  <reversibility rating="reversible">A local review branch is a derived artifact rebuildable from the candidate at any time by the same deterministic construction; deleting it loses nothing.</reversibility>
  <read_first>
    - .planning/phases/230-reviewable-history-integration/230-REF-EXCEPTIONS.json (the declared-additions ledger created in Plan 230-01 — the new row must match its exact schema, including `retirement_trigger` and `published_elsewhere`)
    - scripts/ci/collect_integration_disposition.mjs (the `scope` section written in Plan 230-02 — the three recomputed integers this task confirms)
    - scripts/ci/verify_integration_disposition.mjs (the `--require-scope` flag to extend)
    - .planning/phases/230-reviewable-history-integration/230-CONTEXT.md (D-04, D-28, D-31, D-35)
  </read_first>
  <action>
Produce a code-only sibling review branch so the reviewer reads the real source surface rather than the provenance surface (D-04).

Construct it deterministically: create `refs/heads/review/v1.62-candidate-code-only` from `origin/main`, apply the candidate's tree for every path except `.planning/`, and commit once. The repository's `/gsd-pr-branch` tooling may be used instead provided the resulting branch satisfies the same assertion. The naming satisfies the locked convention — no adopter, customer, or personal names in ref names (D-35).

Prove the two are identical on source: `git diff --quiet <candidate> review/v1.62-candidate-code-only -- . ':!.planning'` must exit 0. That single assertion is what makes the shorter read trustworthy.

This is a third ref beyond the two D-31 calls unavoidable. Declare it in `230-REF-EXCEPTIONS.json` as an `owned` row with `published_elsewhere: "none"`, a reason naming D-04's 72-file review surface, and a `retirement_trigger` naming the Phase 232 integration-PR merge after which the branch must be removed. The two-sided exact-multiset assertion from Plan 230-01 then holds over three rows instead of two; update the literal integer row-count assertion accordingly. Do not push the branch (D-31: pushing mints a fourth ref, and INTG-03 requires verification before any PR targets `main`).

Confirm the three recomputed scope integers in `230-INTEGRATION-DISPOSITION.json` against the review branch: total changed files versus merge-base, `.planning/`-only changed files, and source changed files, plus total and `.planning/`-only commit counts. CONTEXT.md measured 283 files / 491 commits with 211 files and 260 commits `.planning/`-only, leaving 72 source files — re-measure rather than transcribe, and assert that the source-changed-file count equals the number of files in `git diff --name-only <merge-base> review/v1.62-candidate-code-only`. Extend `--require-scope` so that equality is a verifier assertion, not a note.

Record the link between the two branches in the rendered diagnostic: the provenance branch answers "how did this get here", the review branch answers "what source behavior changed" — link the former, diff the latter.
  </action>
  <acceptance_criteria>
    - `git diff --quiet integration/v1.62-candidate review/v1.62-candidate-code-only -- . ':!.planning'` exits 0.
    - `git rev-list --count review/v1.62-candidate-code-only ^origin/main` prints `1`.
    - `git ls-tree -r --name-only review/v1.62-candidate-code-only` lists no path beginning `.planning/` (capture the `ls-tree` output first, then count, so a failing `ls-tree` is not swallowed by the pipeline).
    - `230-REF-EXCEPTIONS.json` contains a row for `refs/heads/review/v1.62-candidate-code-only` with all seven fields and a non-empty `retirement_trigger`.
    - The verifier's literal ledger row-count integer equals the actual number of rows.
    - `--require-scope` asserts the recorded source-changed-file count equals the number of lines `git diff --name-only <merge-base> review/v1.62-candidate-code-only` emits, counted inside the verifier after capturing that command's exit status (never through a shell pipeline that would swallow a failing `git`), and a fixture proves a wrong count fails.
    - No ref was pushed: `git log origin/main..origin/main` and the remote ref set are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>git diff --quiet integration/v1.62-candidate review/v1.62-candidate-code-only -- . ':!.planning'</automated>
    <fails_when>non-zero exit, which occurs when any non-.planning path differs between the candidate and the review branch</fails_when>
    <automated>node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-typed-ref-continuity</automated>
    <fails_when>non-zero exit, or the output lacks a line containing "fixtures: PASS"</fails_when>
    <automated>node scripts/ci/verify_integration_disposition.mjs --records .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json --rendered .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-scope</automated>
    <fails_when>non-zero exit, or the output contains "STALE_BINDING", or the output lacks a line containing "PASS"</fails_when>
  </verify>
  <done>A declared, unpushed code-only review branch is byte-identical to the candidate on every non-`.planning` path, and the reviewer's source surface is a verifier-asserted recomputed integer.</done>
</task>

<task type="auto">
  <name>Task 3: Contributor evidence map row and recorded Phase-232 handoffs</name>
  <files>scripts/ci/README.md, scripts/ci/render_integration_disposition.mjs, .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json, .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md</files>
  <read_first>
    - scripts/ci/README.md (lines 1-40 — the existing three-column evidence tables, the `<!-- phase229-strict-verification:start -->` fenced-block convention, and the Phase 229 row shape to mirror)
    - .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md (the rendered artifact and its phase-230 splice markers from Plan 230-03)
    - .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.md (the rendered ledger from Plan 230-04)
    - release-please-config.json and .github/workflows/release-please.yml (the unset `commit-search-depth` flagged for Phase 232)
    - .planning/phases/230-reviewable-history-integration/230-CONTEXT.md (D-13, D-33, D-39, and the Specific Ideas section's Phase-232 flags)
  </read_first>
  <action>
Add one Phase 230 row to the evidence table in `scripts/ci/README.md`, in the existing three-column shape — artifact links, what it answers, one runnable verify command. Link `230-INTEGRATION-DISPOSITION.json`, `230-INTEGRATION-DISPOSITION.md`, `230-DISPOSITIONS.json`, and `230-DISPOSITIONS.md`. The "what it answers" column states what the merge decided on the reader's behalf and where an excluded commit went. The command column carries a single runnable `node scripts/ci/verify_integration_disposition.mjs` invocation with the `--require-*` flags. Follow the existing fenced-block convention for the full contract, as the Phase 229 section already does.

Record — do not act on — every Phase-232 handoff, each with its reason, in the rendered diagnostic's handoff section and in the plan SUMMARY:
- `origin/phase-226-baseline-5da8e6b88735` at `5da8e6b8` publishes part of the abandoned line and stays excluded; Phase 230 records it, Phase 232 acts (D-13).
- The untracked artifacts other than `.tool-versions` are hashed and recorded only; classification and removal are HYG-01 (D-33).
- `.planning/v1.61-v1.61-MILESTONE-AUDIT.md` appears to be a double-prefixed duplicate of the committed `.planning/v1.61-MILESTONE-AUDIT.md`; record it, do not fix it here.
- `commit-search-depth` is unset in the release-please workflow while roughly 492 commits sit against the default of 500, leaving almost no headroom; pin it in Phase 232 or the next release silently truncates.
- A 153-entry public CHANGELOG dominated by internal GSD and CI tooling commits is editorially wrong even though Release Please will produce a mechanically consistent PR; `RELEASING.md`'s "human polish belongs on the open release PR" clause is the sanctioned remedy.
- The worktree `dirty` boolean is currently pinned `true`; Phase 232 flipping the tree fully clean will flip it to `false` and fail strict verification (D-34).

Regenerate and commit `230-INTEGRATION-DISPOSITION.{json,md}` so the handoff section is part of the deterministic render and re-renders byte-identically.
  </action>
  <acceptance_criteria>
    - `scripts/ci/README.md` contains a table row linking all four Phase 230 artifacts with a runnable command in the third column.
    - The command in that row, run verbatim from the repository root, exits 0.
    - The rendered diagnostic contains a handoff section naming each of the six Phase-232 items above.
    - None of the six items was acted on: `origin/phase-226-baseline-5da8e6b88735` still exists on the remote, no untracked file other than `.tool-versions` was tracked or deleted, `.planning/v1.61-v1.61-MILESTONE-AUDIT.md` is unchanged, and `release-please-config.json` and `.github/workflows/release-please.yml` are unmodified by this plan.
    - Re-rendering the committed JSON produces output byte-identical to the committed Markdown.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/verify_integration_disposition.mjs --records .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.json --rendered .planning/phases/230-reviewable-history-integration/230-INTEGRATION-DISPOSITION.md --dispositions .planning/phases/230-reviewable-history-integration/230-DISPOSITIONS.json --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-ancestry --require-scope --require-hazard-universe --require-excluded-ledger --require-post-merge-scope --require-determinism</automated>
    <fails_when>non-zero exit, or the output contains "STALE_BINDING", or the output lacks a line containing "PASS"</fails_when>
    <automated>git diff --quiet <merge-commit>..integration/v1.62-candidate -- release-please-config.json .github/workflows/release-please.yml</automated>
    <fails_when>non-zero exit, which occurs when a commit on the candidate modified a release-please file this plan was required to leave for Phase 232</fails_when>
  </verify>
  <done>The contributor evidence map carries a working Phase 230 row, every Phase-232 handoff is recorded with its reason, nothing scoped to Phase 232 was acted on, and the diagnostic re-renders byte-identically.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `scripts/ci/verify_phase230_archive_invariants.mjs` — new; exports `verifyArchiveInvariants`; new `--fixtures` self-test
- New `docs-contracts-shift-left` step in `.github/workflows/ci.yml` invoking the sweep
- `refs/heads/review/v1.62-candidate-code-only` — new branch (unpushed, declared in the ref-exceptions ledger)
- New Phase 230 row in the `scripts/ci/README.md` evidence table
- New handoff section in `230-INTEGRATION-DISPOSITION.md`
- New JSON keys: `scanned_file_count`, `literal_count`, `handoffs`
</artifacts_this_phase_produces>

<threat_model>
ASVS level: **1**. Blocking severity: **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merged upstream content → phase-evidence path literals | The merge can introduce a hardcoded literal that silently reads the wrong artifact after archiving |
| Candidate tree → review branch construction | Repository content is copied into a second local ref |
| Evidence artifacts → contributor-facing README | Committed evidence becomes a documented, runnable contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-230-06-01 | Tampering | A `.planning/phases/<slug>` literal silently reading the wrong artifact after archiving | high | mitigate | Fail-closed standing sweep over `scripts/ci/**` and `.github/workflows/**`, merge-blocking in `docs-contracts-shift-left`; an already-archived slug not routed through `resolvePhaseEvidencePath` is a hard failure. |
| T-230-06-02 | Spoofing | A review branch that silently differs from the candidate on source | high | mitigate | `git diff --quiet <candidate> <review> -- . ':!.planning'` is the gate; the review branch is rebuilt deterministically from the candidate tree, never hand-edited. |
| T-230-06-03 | Repudiation | An undeclared third ref entering the repository | medium | mitigate | The review branch is declared in `230-REF-EXCEPTIONS.json` with a retirement trigger, and the two-sided exact-multiset assertion fails on any undeclared ref. |
| T-230-06-04 | Tampering | Repairing the sweep by deleting a check rather than making it archive-aware | high | mitigate | The prohibition is explicit and the acceptance criteria require the literal count to remain non-zero; a sweep that scans zero files fails. |
| T-230-06-05 | Information disclosure | README and rendered handoff section committed publicly | medium | mitigate | Allow-listed fields and `escape()` on rendered cells; no absolute paths, no `$HOME`, no actor names, no adopter identifiers; the already-public remote branch name adds no new exposure. |
| T-230-06-06 | Elevation of privilege | Acting on an item scoped to Phase 232 | medium | mitigate | A `git diff --quiet` assertion over the Phase-232-owned files fails when this plan touched any of them. |
| T-230-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
1. `node --test scripts/ci/verify_phase230_archive_invariants.mjs` reports a zero fail count and a non-zero test count.
2. The sweep exits 0 on the candidate with non-zero scanned-file and literal counts, and its fixtures cover both failing shapes.
3. `git diff --quiet <candidate> <review> -- . ':!.planning'` exits 0 and the review branch has exactly one commit beyond `origin/main` and no `.planning/` paths.
4. The full `--require-*` flag set passes on the committed evidence artifacts.
5. No Phase-232-owned file was modified.
</verification>

<success_criteria>
- Archive-aware phase-evidence resolution is a standing merge-blocking invariant, not a one-time fix.
- A reviewer can read 72 source files on a branch proved identical to the candidate on source.
- Changed-file scope is a verifier-asserted recomputed integer.
- Every Phase-232 handoff is recorded with its reason and none was acted on.
</success_criteria>

<output>
Create `.planning/phases/230-reviewable-history-integration/230-06-SUMMARY.md` when done. Record the sweep's scanned-file and literal counts, the review branch SHA, the three recomputed scope integers, and the six Phase-232 handoffs.
</output>

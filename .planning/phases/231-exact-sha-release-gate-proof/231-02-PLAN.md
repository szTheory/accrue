---
phase: 231-exact-sha-release-gate-proof
plan: 02
type: execute
wave: 2
depends_on: ["231-01"]
files_modified:
  - scripts/ci/collect_window_dispositions.mjs
  - scripts/ci/render_window_dispositions.mjs
  - scripts/ci/verify_window_dispositions.mjs
autonomous: true
requirements: [GATE-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The collector rejects any row whose `kind` is outside the closed set `unrun-verify`/`deviation` and any row whose `disposition` is outside the closed set `fixed`/`waived` (D-22)."
    - "The collector rejects any row whose `state` is `deferred`, `n/a`, or `green`, and rejects `state: proved` with no recorded exit code (D-29)."
    - "The verifier's 1:1 row-id join between the live `.planning/WINDOWS.md` non-`open` rows and the disposition rows is exact set equality in both directions, reported as a missing/extra/changed triple, never a non-empty check (D-26, D-30)."
    - "When a row id exists on exactly one side of the join, the verifier fails and names that id in the `missing` or `extra` list; when the id sets are exactly equal, the join succeeds — the equal-and-touching boundary is decided by set equality, not by count comparison."
    - "The empty case is defined and tested: zero non-`open` WINDOWS.md rows joined against zero disposition rows passes; a single row on each side passes; an absent or unreadable disposition file is a hard failure, never an empty-set pass."
    - "Output order is deterministic and stable: rows are emitted sorted by numeric `id` ascending, and rows comparing equal on any secondary key retain their id order, so re-rendering the committed JSON byte-equals the committed Markdown (D-31)."
    - "Every `waived` row carries a non-empty `owner`, `rationale`, and `release_impact`; a waive missing any of the three is a hard failure (GATE-03, D-22)."
    - "Timestamps in the record come from `git show -s --format=%cI` of the candidate, and no call to `Date.now()` reaches the emitted artifact (D-31)."
    - "All three modules resolve artifact paths through `resolvePhaseEvidencePath` so they keep working after milestone archiving (D-32)."
  artifacts:
    - scripts/ci/collect_window_dispositions.mjs
    - scripts/ci/render_window_dispositions.mjs
    - scripts/ci/verify_window_dispositions.mjs
  key_links:
    - "The verifier reads the WINDOWS.md side of the join through the same 10-column parse contract `verify_repository_inventory.mjs::directShipWindows` enforces — one parser contract, not two (D-25)."
    - "The renderer is a pure function of the validated JSON; the verifier re-renders and byte-compares against the committed Markdown (D-31)."
  prohibitions:
    - "MUST NOT widen `.planning/WINDOWS.md` beyond its existing 10 columns; rich fields live only in the sibling artifact (D-25, D-26)."
    - "MUST NOT implement a second, divergent WINDOWS.md parser; reuse the existing 10-column contract (D-25)."
    - "MUST NOT emit an aggregate boolean, a `green` state, or any `success` alias in the disposition record (D-29)."
    - "MUST NOT emit absolute paths, `$HOME`, actor names, adopter identifiers, or secrets into any committed evidence artifact (D-31)."
---

<objective>
Build the fourth `collect` / `render` / `verify` triad instance — the GATE-03 ship-window disposition artifact that carries owner, rationale, release impact, and current evidence per row id, joined 1:1 to `.planning/WINDOWS.md`.

Purpose: D-26 puts the rich GATE-03 fields in a sibling artifact because D-25 refuses to widen the terse ledger's hardcoded 10-column schema. This plan builds the machinery; Plan 05 authors the actual records.
Output: three new `scripts/ci/*.mjs` modules with hermetic `--fixtures` self-tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md
@.planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
@.planning/WINDOWS.md
</context>

<artifacts_this_phase_produces>
Created by this plan (new symbols — no prior definition exists in the repository):

- `scripts/ci/collect_window_dispositions.mjs` — exports `ROW_KINDS`, `ROW_DISPOSITIONS`, `WINDOW_ROW_FIELDS`, `DISPOSITION_FIELDS`, `validateWindowRow`, `validateWindowDispositions`, `readShipWindowRows`, `collectWindowDispositions`.
- `scripts/ci/render_window_dispositions.mjs` — exports `SPLICE_START`, `SPLICE_END`, `renderWindowDispositions`.
- `scripts/ci/verify_window_dispositions.mjs` — exports `verifyFixtures`.
- New CLI flags: on the collector `--repo`, `--expected-repository`, `--candidate`, `--records-in`, `--out`; on the renderer `--records`, `--out`; on the verifier `--fixtures`, `--repo`, `--records`, `--rendered`, `--expected-repository`, `--candidate`, `--require-row-join`, `--require-evidence-freshness`, `--require-waiver-completeness`, `--require-determinism`.
- New JSON field names: `schema_version`, `repository`, `candidate_object`, `observed_at`, `rows` (each row: `id`, `phase`, `kind`, `disposition`, `state`, `exit_code`, `owner`, `rationale`, `release_impact`, `current_evidence`, `evidence_command`, `resolved_at`).
- New splice markers: `<!-- phase231-window-dispositions:start -->` and `<!-- phase231-window-dispositions:end -->`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: Date.now -->
<!-- The literal `Date.now` is named in task actions because D-31 forbids it by name; the acceptance
     greps filter `//` comment lines, so a naming mention in prose cannot self-invalidate the gate. -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author collect_window_dispositions.mjs with closed enums and the WINDOWS.md row read</name>
  <files>scripts/ci/collect_window_dispositions.mjs</files>
  <read_first>
    - scripts/ci/collect_integration_disposition.mjs (the exact analog — `fail()`, `fields()`, `fullSha()`, `run()`, `HAZARD_CLASSES` closed-Set pattern, `parseArgs`, the `writeFileSync` with mode `0o600`, the `NODE_TEST_CONTEXT` split)
    - scripts/ci/verify_repository_inventory.mjs (the `directShipWindows` function around lines 523-547 — the authoritative 10-column WINDOWS.md parse contract and its frontmatter count cross-check)
    - scripts/ci/phase_evidence_path.mjs
    - .planning/WINDOWS.md (the live ledger: 10 rows, header string, frontmatter counts)
    - .planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
  </read_first>
  <behavior>
    - `readShipWindowRows` on the live ledger returns 10 rows with ids 1 through 10 and each row's `phase`, `kind`, `file`, `status`.
    - `readShipWindowRows` throws when the header line differs from the pinned 10-column header string.
    - `readShipWindowRows` throws when any row has a column count other than 10, when an id repeats, or when a frontmatter count disagrees with the parsed rows.
    - `validateWindowRow` rejects a `kind` outside `unrun-verify` and `deviation`.
    - `validateWindowRow` rejects a `disposition` outside `fixed` and `waived`, and specifically rejects the literal `open` as a disposition.
    - `validateWindowRow` rejects `state` values `deferred`, `n/a`, and `green`, and accepts only `proved`, `failed`, `skipped`, `advisory`, `non_run`.
    - `validateWindowRow` rejects `state: proved` when `exit_code` is null or absent.
    - `validateWindowRow` rejects `disposition: waived` when `owner`, `rationale`, or `release_impact` is empty or absent.
    - `validateWindowRow` rejects any field outside `WINDOW_ROW_FIELDS`, naming the forbidden key.
    - `validateWindowRow` rejects a `current_evidence` string containing an absolute path prefix, a `$HOME` reference, or a `/Users/` or `/home/` segment.
    - `collectWindowDispositions` emits `rows` sorted ascending by numeric `id`, and emits identical bytes on two consecutive runs over the same inputs.
    - `collectWindowDispositions` sets `observed_at` from the candidate commit's committer date, and the emitted record contains no timestamp derived from the wall clock.
    - Zero input rows plus zero non-`open` ledger rows is a valid empty record, not an error; one input row plus one non-`open` ledger row is valid.
  </behavior>
  <action>
Create `scripts/ci/collect_window_dispositions.mjs` cloning `collect_integration_disposition.mjs`'s structure verbatim: shebang, the same `node:` imports, the `SHA`/`REPOSITORY`/`ISO` regex constants, the single `fail(message)` throw path, the `fields(value, allowed, label)` allow-list helper, the `run(repo, args)` git wrapper using `spawnSync` with `shell: false`, and the paired `NODE_TEST_CONTEXT` guards at the bottom (one for the CLI `main()`, one for the `node:test` suite).

Declare these closed Sets, following the `HAZARD_CLASSES` precedent — an unknown value is a hard failure, never pass-through: `ROW_KINDS` holding `unrun-verify` and `deviation` (D-22's two ledger kinds); `ROW_DISPOSITIONS` holding `fixed` and `waived`; and reuse the Phase 226 lexicon `STATES` holding `proved`, `failed`, `skipped`, `advisory`, `non_run` with `REJECTED_STATES` holding `deferred`, `n/a`, `green` (D-29).

Declare `WINDOW_ROW_FIELDS` as a closed Set containing exactly: `id`, `phase`, `kind`, `disposition`, `state`, `exit_code`, `owner`, `rationale`, `release_impact`, `current_evidence`, `evidence_command`, `resolved_at`. Declare `DISPOSITION_FIELDS` as a closed Set containing exactly: `schema_version`, `repository`, `candidate_object`, `observed_at`, `rows`.

Implement `readShipWindowRows(repositoryRoot)` by transcribing the `directShipWindows` parse contract from `verify_repository_inventory.mjs` exactly — the same pinned header string, the same 512 KiB bounded-input guard, the same 10-column requirement, the same numeric-id and `open`/`waived`/`fixed` status validation, the same duplicate-id rejection, and the same four frontmatter count cross-checks. Return the richer per-row object (`id`, `phase`, `kind`, `file`, `line`, `description`, `status`, `reason`, `recorded_at`, `resolved_at`) rather than the `id:status` string form, but do not relax any check and do not accept an eleventh column. Add an explicit source comment naming `verify_repository_inventory.mjs` as the parser contract's origin so the two never drift silently.

Implement `validateWindowRow(row, label)` applying `fields()` against `WINDOW_ROW_FIELDS`, the three closed-enum checks, the proved-requires-exit-code rule, the waive-requires-owner-rationale-release-impact rule, and a sanitization check rejecting any string value matching an absolute-path or home-directory pattern (D-31).

Implement `collectWindowDispositions({ repo, expectedRepository, candidate, inputRows })`: validate `expectedRepository` against the `REPOSITORY` regex; resolve the candidate to a full 40-hex via `git rev-parse`; set `observed_at` from `git show -s --format=%cI <candidate>` and validate it against the `ISO` regex; never call `Date.now()` (D-31); sort `rows` ascending by numeric `id` with a stable comparator so equal-comparing rows keep id order; and return the object validated by `validateWindowDispositions`.

Implement the CLI `main()` reading `--repo`, `--expected-repository`, `--candidate`, `--records-in` (the hand-authored rich rows Plan 05 supplies), and `--out`, writing with `fs.writeFileSync(out, JSON.stringify(value, null, 2) + "\n", { mode: 0o600 })`.

Implement the `node:test` suite building a disposable repository and a synthetic WINDOWS.md under `fs.mkdtempSync`, covering every bullet in the behavior block including the zero-row and one-row cases and a two-consecutive-runs byte-equality assertion.
  </action>
  <verify>
    <automated>node --check scripts/ci/collect_window_dispositions.mjs &amp;&amp; node --test scripts/ci/collect_window_dispositions.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check`, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node --input-type=module -e "import('./scripts/ci/collect_window_dispositions.mjs').then(m =&gt; { const rows = m.readShipWindowRows(process.cwd()); if (rows.length !== 10) { console.error('expected 10 ledger rows, got ' + rows.length); process.exit(1); } })"</automated>
    <fails_when>non-zero exit, or stderr reporting a row count other than 10 against the live `.planning/WINDOWS.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' scripts/ci/collect_window_dispositions.mjs | grep -c 'Date.now'` prints `0`.
    - The module exports `ROW_KINDS`, `ROW_DISPOSITIONS`, `WINDOW_ROW_FIELDS`, `DISPOSITION_FIELDS`, `validateWindowRow`, `validateWindowDispositions`, `readShipWindowRows`, and `collectWindowDispositions`.
    - `ROW_KINDS` has exactly the two members `unrun-verify` and `deviation`; `ROW_DISPOSITIONS` has exactly the two members `fixed` and `waived`.
    - A test case asserts a throw for each of: unknown kind, unknown disposition, rejected state, proved without exit code, waived without owner, waived without rationale, waived without release impact, forbidden field, absolute path in a string value.
    - A test case asserts an 11-column synthetic ledger row throws.
    - A test case asserts byte-identical output across two consecutive collector runs on the same inputs.
  </acceptance_criteria>
  <done>The collector exists, parses the live ledger's ten rows through the pinned 10-column contract, enforces every closed enum and completeness rule with a negative control apiece, and emits deterministic sorted output with no wall-clock timestamp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author render_window_dispositions.mjs with maintainer-first section ordering</name>
  <files>scripts/ci/render_window_dispositions.mjs</files>
  <read_first>
    - scripts/ci/render_integration_disposition.mjs (the exact analog — `escape`, `order`, `section`, the `SPLICE_START`/`SPLICE_END` marker pair, and the closed-class bucketing that emits a zero-row section explicitly rather than omitting it)
    - scripts/ci/collect_window_dispositions.mjs (created in Task 1 — import its validators and closed Sets, never re-declare them)
    - .planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md (the Specific Ideas section: the maintainer's question is "can I ship this SHA, and what is still unproven?")
  </read_first>
  <behavior>
    - `renderWindowDispositions` is a pure function of the validated JSON: called twice with the same input it returns identical strings, and it performs no filesystem read and no wall-clock read.
    - The output leads with the `waived` section, then any `failed` state rows, then `skipped`, then collapses `fixed` rows last — so the first screenful answers what is still unproven.
    - Every declared bucket renders a heading even when it holds zero rows; an empty bucket is never silently omitted.
    - Row 1's lesson is preserved: the rendered row for id 1 shows its re-derived current evidence, not its original ledger description.
    - Markdown table cells escape pipe, backtick, backslash, and angle-bracket characters, and collapse any embedded newline to a single space.
    - The output is fenced by the `phase231-window-dispositions` start and end marker pair.
    - Rendering a record whose `rows` array is empty produces a well-formed document with every bucket heading present and an explicit zero-row statement, not an empty file.
  </behavior>
  <action>
Create `scripts/ci/render_window_dispositions.mjs` cloning `render_integration_disposition.mjs` verbatim in structure: the same `escape` implementation, the same `order` stable-sort helper, the same `section(title, ...)` shape, and imports of `validateWindowDispositions`, `ROW_KINDS`, and `ROW_DISPOSITIONS` from `./collect_window_dispositions.mjs` so no constant is duplicated.

Export `SPLICE_START` as the exact string `<!-- phase231-window-dispositions:start -->` and `SPLICE_END` as `<!-- phase231-window-dispositions:end -->`, mirroring the Phase 230 marker convention so Phase 232 can splice the block into the integration PR body without re-deriving it.

Export `renderWindowDispositions(record, opts)` as a pure function. Bucket rows by disposition and state in this display order, each bucket rendered even at zero rows: first `waived` (what the maintainer must accept), then rows whose `state` is `failed`, then rows whose `state` is `skipped`, `advisory`, or `non_run`, then `fixed` rows collapsed last. Within each bucket sort ascending by numeric `id` using the stable comparator, so rows comparing equal on the bucket key retain id order.

Render each row as a Markdown table row carrying `id`, `phase`, `kind`, `disposition`, `state`, `exit_code`, `owner`, `rationale`, `release_impact`, and `current_evidence`. Take `current_evidence` straight from the record — never fall back to the ledger's original `description`, which D-23 shows would close row 1 on a false premise.

Take every timestamp from the record's `observed_at`; make no call to `Date.now()` and no filesystem read beyond the input file the CLI passes in.

Implement the CLI `main()` reading `--records` and `--out` and writing with mode `0o600`, plus the `node:test` suite covering every behavior bullet including the empty-record case and a called-twice determinism assertion.
  </action>
  <verify>
    <automated>node --check scripts/ci/render_window_dispositions.mjs &amp;&amp; node --test scripts/ci/render_window_dispositions.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check`, or any `node:test` case reported as `not ok`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' scripts/ci/render_window_dispositions.mjs | grep -c 'Date.now'` prints `0`.
    - `grep -c 'phase231-window-dispositions:start' scripts/ci/render_window_dispositions.mjs` is at least 1.
    - The module imports `validateWindowDispositions` from `./collect_window_dispositions.mjs` and declares no second copy of `ROW_KINDS` or `ROW_DISPOSITIONS`.
    - A test case renders a record with zero rows and asserts every bucket heading is present in the output.
    - A test case renders the same record twice and asserts the two strings are identical.
    - A test case asserts that a row whose `current_evidence` differs from its ledger `description` renders the `current_evidence` text.
  </acceptance_criteria>
  <done>The renderer exists, is a pure deterministic function, leads with what is unproven, renders empty buckets explicitly, and is fenced by the phase231 splice markers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author verify_window_dispositions.mjs with the exact 1:1 row-id join</name>
  <files>scripts/ci/verify_window_dispositions.mjs</files>
  <read_first>
    - scripts/ci/verify_integration_disposition.mjs (the exact analog — `assertSameMultiset`, `exactMap`, `assertSameMap` with its missing/extra/changed triple, the strict-flag CLI with the visually distinct schema-only suffix, `verifyFixtures`, and the re-render byte-comparison determinism assertion)
    - scripts/ci/collect_window_dispositions.mjs and scripts/ci/render_window_dispositions.mjs (created in Tasks 1 and 2 — import their validators, constants, and render function)
    - scripts/ci/verify_repository_inventory.mjs (the `directShipWindows` parse the join's authority side must match)
    - .planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
  </read_first>
  <behavior>
    - `--require-row-join` passes when the set of non-`open` WINDOWS.md row ids exactly equals the set of disposition row ids.
    - `--require-row-join` fails when an id appears only in WINDOWS.md, naming it in the `missing` list of the triple.
    - `--require-row-join` fails when an id appears only in the disposition record, naming it in the `extra` list.
    - `--require-row-join` fails when an id appears on both sides but its ledger status disagrees with the record's disposition, naming it in the `changed` list.
    - `--require-row-join` fails on a duplicate id on either side rather than silently collapsing it.
    - `--require-row-join` passes on the empty case: zero non-`open` ledger rows joined against zero disposition rows.
    - `--require-row-join` passes on the single-row case and fails when the single ids differ.
    - `--require-row-join` fails when any WINDOWS.md row is still `open`, because an open row has no disposition partner and GATE-03 requires none remain.
    - `--require-evidence-freshness` fails when a row's `current_evidence` is byte-identical to that row's original ledger `description`, since a copied-forward description is not re-derived evidence.
    - `--require-waiver-completeness` fails when any `waived` row has an empty `owner`, `rationale`, or `release_impact`.
    - `--require-determinism` fails when re-rendering the committed JSON does not byte-equal the committed Markdown, printing the first differing byte offset.
    - A missing or unreadable disposition file is a hard failure with a message naming the path, never an empty-set pass.
    - An invocation with no strict flag prints a schema-only suffix distinct from the strict suffix.
  </behavior>
  <action>
Create `scripts/ci/verify_window_dispositions.mjs` cloning `verify_integration_disposition.mjs` verbatim in structure. Copy `assertSameMultiset`, `exactMap`, and `assertSameMap` identically — do not write new array comparisons with `.every()` or `.includes()` (D-30). Import `validateWindowDispositions`, `validateWindowRow`, `readShipWindowRows`, `ROW_KINDS`, and `ROW_DISPOSITIONS` from `./collect_window_dispositions.mjs`, and `renderWindowDispositions` from `./render_window_dispositions.mjs`.

Implement the 1:1 join exactly as the hazard-universe join is implemented in the analog: build one map from the live WINDOWS.md non-`open` rows keyed by numeric id with the ledger status as value, build a second map from the disposition rows keyed by numeric id with the disposition as value, then call `assertSameMap` in the direction that yields the full missing/extra/changed triple. Map ledger status `fixed` to disposition `fixed` and ledger status `waived` to disposition `waived`, so a status/disposition mismatch lands in `changed`.

Add an explicit assertion, separate from the join, that the live ledger's `open_count` frontmatter value is the literal integer 0 — an open row has no partner and must be reported as its own named failure rather than as a bare `missing` entry.

Implement `--require-evidence-freshness` comparing each row's `current_evidence` against that row's ledger `description` and failing on byte-identity (D-23).

Implement `--require-waiver-completeness` asserting non-empty `owner`, `rationale`, and `release_impact` on every `waived` row.

Implement `--require-determinism` by reading the committed JSON, calling `renderWindowDispositions`, and byte-comparing against the committed Markdown, reporting the first differing byte offset on mismatch (D-31).

Implement the strict-suffix pattern from the analog's `main()`: gather the requested strict flags sorted and print `window dispositions verification: PASS (verified: <flags>)`, or `PASS (schema-only: no strict flags supplied, no row-join or determinism check ran)` when none were supplied.

Implement `verifyFixtures()` building disposable repositories and synthetic ledgers under `fs.mkdtempSync` with one positive control plus a negative control for every behavior bullet above, including the id-only-on-one-side cases, the duplicate-id case, the zero-row case, the one-row case, the still-open-row case, and the missing-file case. Wire it to both `node --test` and the bare `--fixtures` flag, printing `window dispositions fixtures: PASS`.
  </action>
  <verify>
    <automated>node --check scripts/ci/verify_window_dispositions.mjs &amp;&amp; node --test scripts/ci/collect_window_dispositions.mjs scripts/ci/render_window_dispositions.mjs scripts/ci/verify_window_dispositions.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check`, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node scripts/ci/verify_window_dispositions.mjs --fixtures --expected-repository szTheory/accrue --require-row-join --require-evidence-freshness --require-waiver-completeness --require-determinism</automated>
    <fails_when>non-zero exit, a line beginning `window dispositions verify: FAIL:`, stdout missing `window dispositions fixtures: PASS`, or a PASS suffix containing `schema-only` despite the four strict flags being supplied</fails_when>
  </verify>
  <acceptance_criteria>
    - The module defines `assertSameMap` emitting a message containing all three of the substrings `missing=[`, `extra=[`, and `changed=[`.
    - A fixture asserts a throw whose message contains an id in the `missing` list when that id is only in the ledger.
    - A fixture asserts a throw whose message contains an id in the `extra` list when that id is only in the record.
    - A fixture asserts a throw when both sides hold the same id with disagreeing status, and the id appears in the `changed` list.
    - A fixture asserts a pass for the zero-row join and a pass for the one-row join.
    - A fixture asserts a throw when a ledger row remains `open`, with a message naming the open count.
    - A fixture asserts a throw when the disposition file path does not exist, with the path in the message.
    - Running the verifier with no strict flag prints a message containing the substring `schema-only`.
  </acceptance_criteria>
  <done>The verifier exists, its hermetic fixture suite passes with all four strict flags, and every adjacency, empty, ordering, freshness, waiver-completeness, and determinism boundary has a negative control that fails closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored rich rows to committed evidence | Free-text `owner`, `rationale`, `release_impact`, and `current_evidence` supplied by an operator cross into a file published on a public repository. |
| live ledger file to verifier authority | `.planning/WINDOWS.md` is read as the authority side of the join; a malformed or widened ledger must fail closed rather than degrade the join. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-07 | Information Disclosure | `231-WINDOW-DISPOSITIONS.json` free-text fields | high | mitigate | `validateWindowRow` rejects any string value matching an absolute-path or home-directory pattern, with a dedicated negative control (D-31). |
| T-231-08 | Repudiation | The row-id join | high | mitigate | `assertSameMap` recomputes exact set equality inside the verifier and reports a missing/extra/changed triple; no non-empty check is reachable (D-30). |
| T-231-09 | Spoofing | Copied-forward evidence | high | mitigate | `--require-evidence-freshness` fails on byte-identity between a row's `current_evidence` and its original ledger `description` (D-23). |
| T-231-10 | Tampering | `.planning/WINDOWS.md` schema | high | mitigate | The parse contract is transcribed from `directShipWindows` unchanged; an 11-column row throws, with a negative control (D-25). |
| T-231-11 | Repudiation | Vacuous pass on an empty record | high | mitigate | A missing or unreadable disposition file is a hard failure; `open_count` must be the literal integer 0; both have negative controls. |
| T-231-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. All three modules use only Node built-ins already available; no dependency is added to any manifest. |
</threat_model>

<verification>
- `node --check` and `node --test` pass for all three new modules.
- `node scripts/ci/verify_window_dispositions.mjs --fixtures` with all four strict flags prints `window dispositions fixtures: PASS`.
- `readShipWindowRows` returns exactly 10 rows against the live `.planning/WINDOWS.md`.
- No module reaches `Date.now()` outside a comment.
</verification>

<success_criteria>
The fourth collect/render/verify triad exists and is hermetically self-testing, so Plan 05 can author real records against machinery that already fails closed on every join, ordering, emptiness, freshness, and determinism boundary.
</success_criteria>

<output>
Create `.planning/phases/231-exact-sha-release-gate-proof/231-02-SUMMARY.md` when done.
</output>

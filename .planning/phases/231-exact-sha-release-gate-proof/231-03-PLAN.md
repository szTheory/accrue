---
phase: 231-exact-sha-release-gate-proof
plan: 03
type: execute
wave: 2
depends_on: ["231-01"]
files_modified:
  - scripts/ci/collect_gate01_cohort.mjs
  - scripts/ci/render_gate01_cohort.mjs
  - scripts/ci/verify_gate01_cohort.mjs
  - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json
  - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.md
autonomous: true
requirements: [GATE-01]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The declared merge-blocking cohort is read from the `ci.yml` header comment, not from a hand-maintained duplicate list, and the evidence contains exactly one row per declared job — asserted by exact set equality, never a non-empty check (D-09, D-30)."
    - "The declared list is cross-checked against `annotation-sweep`'s live `needs:` array; a job renamed, added, or removed on one side and not the other fails the gate (D-18)."
    - "Every cohort row carries a state from the closed set `proved`/`failed`/`skipped`/`advisory`/`non_run`, and every `proved` row carries a recorded integer exit code and the argv array of the command that produced it (D-29)."
    - "The cohort executes inside a scratch `git clone` of the local repository at the exact candidate SHA, with `deps/`, `_build/`, `priv/plts`, and built assets rebuilt from nothing — no cache is restored into the clone (D-12, D-13)."
    - "`git worktree list` in the subject repository has the same row count before and after the run (D-12)."
    - "`live-stripe`, `provider-proof-trigger`, `provider-proof-incident`, and `ios-offline-client` each get an explicit row with a recorded reason, classified outside the merge-blocking cohort rather than omitted (D-11)."
    - "`mix hex.publish --dry-run` and a bare `gh workflow run ci.yml` are each recorded `non_run` with the reason that they are not repository-declared local-CI-equivalent gates (D-10)."
    - "`annotation-sweep` is recorded `non_run` with the reason that it reads Actions run annotations through the API and has no scratch-clone-local form, and carries an explicit pointer that its proof is GATE-02's dispatch run."
    - "`admin-ui-ratchet-guardrails` is classified by reading its own `continue-on-error` flag in `ci.yml`, so a non-blocking advisory lane is never conflated with a required one."
    - "Re-rendering the committed JSON byte-equals the committed Markdown, and every timestamp comes from `git show -s --format=%cI` of the candidate (D-31)."
  artifacts:
    - scripts/ci/collect_gate01_cohort.mjs
    - scripts/ci/render_gate01_cohort.mjs
    - scripts/ci/verify_gate01_cohort.mjs
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.md
  key_links:
    - "The cohort authority is `.github/workflows/ci.yml`'s header comment parsed at collect time; the `annotation-sweep` `needs:` array is the independent cross-check (D-09, D-18)."
    - "A gate that runs and fails on its merits is a GATE-01 blocker recorded `failed`, not a still-open ship window — this is the evidence Plan 05 consumes to dispose the six `unrun-verify` rows (D-22)."
  prohibitions:
    - "MUST NOT restore or copy `deps/`, `_build/`, `priv/plts`, or built assets into the scratch clone from the subject repository or any cache (D-13)."
    - "MUST NOT use `git worktree add` for the cohort run (D-12)."
    - "MUST NOT hand-maintain a duplicate copy of the merge-blocking job list inside the new script; parse it from `ci.yml` (D-09)."
    - "MUST NOT record a state outside the closed lexicon, and MUST NOT record `proved` without an exit code (D-29)."
    - "MUST NOT emit absolute paths, `$HOME`, actor names, adopter identifiers, or secrets into the committed evidence (D-31)."
---

<objective>
Prove GATE-01: a fresh clean checkout of the exact candidate SHA runs the repository's own declared merge-blocking cohort, and every declared job lands exactly one row with a closed-vocabulary outcome and a recorded exit code.

Purpose: GATE-01's "complete" means complete with respect to what this repository's CI treats as required (D-09), not complete with respect to every lane Phase 230 happened to enumerate (D-10).
Output: a GATE-01 evidence triad and the committed JSON plus rendered Markdown.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md
@.planning/phases/231-exact-sha-release-gate-proof/231-RESEARCH.md
@.planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
@.github/workflows/ci.yml
</context>

<artifacts_this_phase_produces>
Created by this plan (new symbols — no prior definition exists in the repository):

- `scripts/ci/collect_gate01_cohort.mjs` — exports `COHORT_STATES`, `COHORT_ROW_FIELDS`, `GATE01_FIELDS`, `LANE_CLASSES`, `declaredMergeBlockingJobs`, `annotationSweepNeeds`, `validateCohortRow`, `validateGate01Evidence`, `collectGate01Cohort`.
- `scripts/ci/render_gate01_cohort.mjs` — exports `SPLICE_START`, `SPLICE_END`, `renderGate01Cohort`.
- `scripts/ci/verify_gate01_cohort.mjs` — exports `verifyFixtures`.
- New CLI flags: on the collector `--repo`, `--expected-repository`, `--candidate`, `--results-in`, `--out`; on the renderer `--records`, `--out`; on the verifier `--fixtures`, `--repo`, `--records`, `--rendered`, `--expected-repository`, `--candidate`, `--require-cohort-completeness`, `--require-declaration-drift`, `--require-exit-codes`, `--require-clean-checkout`, `--require-determinism`.
- New JSON field names: `schema_version`, `repository`, `candidate_object`, `observed_at`, `checkout` (with `method`, `source`, `caches_restored`, `worktree_rows_delta`), `rows` (each row: `job`, `lane_class`, `state`, `exit_code`, `argv`, `reason`, `deferred_to`, `duration_ms`).
- New evidence artifact paths: `.planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json` and `231-GATE-01-EVIDENCE.md`.
- New splice markers: `<!-- phase231-gate01-cohort:start -->` and `<!-- phase231-gate01-cohort:end -->`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: Date.now -->
<!-- The literal `Date.now` is named in task actions because D-31 forbids it by name; the acceptance
     greps filter `//` comment lines, so a naming mention in prose cannot self-invalidate the gate. -->

<!-- planner-discipline-allow: protection -->
<!-- planner-discipline-allow: rulesets -->
<!-- D-18 requires naming the branch-protection and rulesets endpoints in prose so the executor knows
     which reads are forbidden; the corresponding source assertions check for API request paths, not
     for these words appearing in plan prose. -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author collect_gate01_cohort.mjs and render_gate01_cohort.mjs</name>
  <files>scripts/ci/collect_gate01_cohort.mjs, scripts/ci/render_gate01_cohort.mjs</files>
  <read_first>
    - .github/workflows/ci.yml (the header comment's `Merge-blocking on pull_request:` list spanning several comment lines; the `annotation-sweep` job's `needs:` array; the `admin-ui-ratchet-guardrails` job's own `continue-on-error` flag; the `live-stripe` job's `if:` expression; the `ios-offline-client` job)
    - scripts/ci/collect_integration_disposition.mjs (the analog for closed enums, `fields()` allow-lists, argv-array recording, `writeFileSync` mode `0o600`, and the `NODE_TEST_CONTEXT` split)
    - scripts/ci/render_integration_disposition.mjs (the analog for deterministic bucketed rendering and splice markers)
    - scripts/ci/collect_ci_baseline.mjs (`workflowRunnerContracts` — an existing `ci.yml` parser to model the header/needs read on)
    - scripts/ci/phase_evidence_path.mjs
  </read_first>
  <behavior>
    - `declaredMergeBlockingJobs` parses the `ci.yml` header comment and returns exactly the thirteen declared job keys, in sorted order.
    - `declaredMergeBlockingJobs` throws when the header's merge-blocking block is absent or unparseable, rather than returning an empty list.
    - `annotationSweepNeeds` returns the twelve job keys in the live `needs:` array of the `annotation-sweep` job.
    - The declaration-drift check passes when the declared set minus `annotation-sweep` exactly equals the `needs:` set, and fails with a missing/extra triple when a job is renamed on one side only.
    - `validateCohortRow` rejects a `state` outside `proved`, `failed`, `skipped`, `advisory`, `non_run`, and specifically rejects `green`, `deferred`, and `n/a`.
    - `validateCohortRow` rejects `state: proved` with a null, absent, or non-integer `exit_code`.
    - `validateCohortRow` rejects `state: proved` with an empty or absent `argv` array, and rejects `argv` supplied as a string.
    - `validateCohortRow` rejects `state: skipped` or `non_run` with an empty `reason`.
    - `validateCohortRow` rejects a `lane_class` outside `merge-blocking`, `credential-gated`, `scheduler-only`, `pr-not-merge-blocking`, `not-a-declared-gate`.
    - `validateCohortRow` rejects any string value containing an absolute path, a home-directory segment, or a home-variable reference.
    - `collectGate01Cohort` fails when the row set does not exactly equal the declared cohort plus the declared out-of-cohort lanes, reporting a missing/extra triple.
    - `collectGate01Cohort` sets `observed_at` from the candidate's committer date and emits no wall-clock timestamp.
    - `renderGate01Cohort` is pure, leads with `failed` rows then `skipped`/`non_run` then collapses `proved` last, renders every bucket heading even at zero rows, and returns identical strings on repeated calls.
  </behavior>
  <action>
Create `scripts/ci/collect_gate01_cohort.mjs`, cloning `collect_integration_disposition.mjs`'s structure: shebang, `node:` imports, the `fail()` single-throw path, the `fields(value, allowed, label)` allow-list helper, `parseArgs`, the `writeFileSync` with mode `0o600`, and the paired `NODE_TEST_CONTEXT` guards.

Implement `declaredMergeBlockingJobs(source)` parsing the `ci.yml` header comment block that begins with the text `Merge-blocking on pull_request:` and continues across subsequent comment lines until a line that is not a continuation. Extract the backtick-quoted job keys. Assert the result contains the thirteen keys `release-manifest-ssot`, `docs-contracts-shift-left`, `release-gate`, `phase18-tax-gate`, `admin-drift-docs`, `admin-group-contracts`, `admin-hardening-guardrails`, `admin-phase200-guardrails`, `admin-ui-ratchet-guardrails`, `host-integration`, `playwright-e2e`, `host-docker-smoke`, `annotation-sweep` — assert them as a derived-then-compared set, so a future edit to `ci.yml` is detected rather than baked in. Throw on an absent or unparseable block; never return an empty list, because an empty required set passes vacuously (D-18).

Implement `annotationSweepNeeds(source)` reading the live `needs:` array of the `annotation-sweep` job. Implement the drift check as exact set equality between `declaredMergeBlockingJobs` minus `annotation-sweep` and `annotationSweepNeeds`, reusing the missing/extra/changed triple helper from `verify_integration_disposition.mjs` rather than an ad hoc comparison (D-30).

Declare `COHORT_STATES` as the Phase 226 lexicon closed Set (`proved`, `failed`, `skipped`, `advisory`, `non_run`) with the rejected set (`deferred`, `n/a`, `green`) checked first (D-29). Declare `LANE_CLASSES` as a closed Set holding `merge-blocking`, `credential-gated`, `scheduler-only`, `pr-not-merge-blocking`, `not-a-declared-gate`. Declare `COHORT_ROW_FIELDS` as a closed Set holding exactly `job`, `lane_class`, `state`, `exit_code`, `argv`, `reason`, `deferred_to`, `duration_ms`, and `GATE01_FIELDS` holding exactly `schema_version`, `repository`, `candidate_object`, `observed_at`, `checkout`, `rows`.

Implement `validateCohortRow` enforcing each rule in the behavior block, including the sanitization check (D-31) and the argv-must-be-an-array-of-strings rule.

Implement `collectGate01Cohort({ repo, expectedRepository, candidate, resultRows })`: resolve the candidate to a full 40-hex; set `observed_at` from `git show -s --format=%cI`; never call `Date.now()`; require the row set to exactly equal `declaredMergeBlockingJobs()` unioned with the fixed out-of-cohort lane list `live-stripe`, `provider-proof-trigger`, `provider-proof-incident`, `ios-offline-client`, `mix hex.publish --dry-run`, `gh workflow run ci.yml`; sort rows by job key ascending with a stable comparator; and populate `checkout` with `method` set to the string `git clone`, `source` set to the string `local repository` (never an absolute path), `caches_restored` set to the boolean false, and `worktree_rows_delta` set to the integer 0.

Then create `scripts/ci/render_gate01_cohort.mjs` cloning `render_integration_disposition.mjs`: import the validators and constants from the collector, export `SPLICE_START` as `<!-- phase231-gate01-cohort:start -->` and `SPLICE_END` as `<!-- phase231-gate01-cohort:end -->`, and export a pure `renderGate01Cohort(record)` that buckets rows `failed` first, then `skipped`/`non_run`/`advisory`, then `proved` collapsed last, renders every bucket heading even at zero rows, escapes Markdown cell characters, and makes no wall-clock or filesystem call.
  </action>
  <verify>
    <automated>node --check scripts/ci/collect_gate01_cohort.mjs &amp;&amp; node --check scripts/ci/render_gate01_cohort.mjs &amp;&amp; node --test scripts/ci/collect_gate01_cohort.mjs scripts/ci/render_gate01_cohort.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check` for either module, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node --input-type=module -e "import('./scripts/ci/collect_gate01_cohort.mjs').then(m =&gt; { const fs = require('node:fs'); const src = fs.readFileSync('.github/workflows/ci.yml','utf8'); const d = m.declaredMergeBlockingJobs(src); const n = m.annotationSweepNeeds(src); if (d.length !== 13) { console.error('declared cohort size ' + d.length); process.exit(1); } if (n.length !== 12) { console.error('needs size ' + n.length); process.exit(1); } })"</automated>
    <fails_when>non-zero exit, or stderr printing a declared cohort size other than 13 or a needs size other than 12 against the live `.github/workflows/ci.yml`</fails_when>
  </verify>
  <acceptance_criteria>
    - The collector contains no array literal enumerating the thirteen job keys as the source of truth; the declared list is derived by parsing `.github/workflows/ci.yml` and then compared against an expected set.
    - `COHORT_STATES` has exactly the five members `proved`, `failed`, `skipped`, `advisory`, `non_run`.
    - `LANE_CLASSES` has exactly the five members `merge-blocking`, `credential-gated`, `scheduler-only`, `pr-not-merge-blocking`, `not-a-declared-gate`.
    - A test case asserts a throw for each of: unknown state, rejected state, proved without an exit code, proved with an empty argv, argv supplied as a string, skipped without a reason, unknown lane class, absolute path in a string value, row set unequal to the declared cohort.
    - A test case asserts a throw when the header's merge-blocking block is removed from a synthetic `ci.yml`.
    - A test case renames one job in a synthetic `needs:` array and asserts the drift check throws with the renamed key in the triple.
    - The renderer returns identical strings across two calls on the same record, and renders every bucket heading on a zero-row record.
  </acceptance_criteria>
  <done>The collector derives the cohort from `ci.yml`, cross-checks it against the live `needs:` graph, enforces the closed lexicon with a negative control apiece, and the renderer is a pure deterministic function that leads with what failed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author verify_gate01_cohort.mjs with completeness, drift, and clean-checkout assertions</name>
  <files>scripts/ci/verify_gate01_cohort.mjs</files>
  <read_first>
    - scripts/ci/verify_integration_disposition.mjs (the analog — `assertSameMultiset`, `exactMap`, `assertSameMap`, the strict-flag CLI with the visually distinct schema-only suffix, `verifyFixtures`, the re-render byte comparison)
    - scripts/ci/collect_gate01_cohort.mjs and scripts/ci/render_gate01_cohort.mjs (created in Task 1)
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - `--require-cohort-completeness` passes when the record's job keys exactly equal the derived declared cohort plus the six declared out-of-cohort lanes, and fails with a missing/extra triple otherwise.
    - `--require-cohort-completeness` fails when a declared job has two rows rather than one.
    - `--require-declaration-drift` re-derives both the header list and the `needs:` array from the live `ci.yml` and fails when they disagree, naming the drifted key.
    - `--require-exit-codes` fails when any `proved` row lacks an integer exit code or an argv array, and fails when any `proved` row records a non-zero exit code.
    - `--require-clean-checkout` fails when `checkout.method` is not the string `git clone`, when `checkout.caches_restored` is not the boolean false, or when `checkout.worktree_rows_delta` is not the integer 0.
    - `--require-determinism` fails when re-rendering the committed JSON does not byte-equal the committed Markdown, printing the first differing byte offset.
    - An invocation with no strict flag prints a schema-only suffix distinct from the strict suffix.
    - A missing or unreadable records file is a hard failure naming the path, never a vacuous pass.
  </behavior>
  <action>
Create `scripts/ci/verify_gate01_cohort.mjs` cloning `verify_integration_disposition.mjs` verbatim in structure. Copy `assertSameMultiset`, `exactMap`, and `assertSameMap` identically rather than writing new comparisons (D-30). Import the validators, closed Sets, `declaredMergeBlockingJobs`, and `annotationSweepNeeds` from `./collect_gate01_cohort.mjs`, and `renderGate01Cohort` from `./render_gate01_cohort.mjs`.

Implement each strict flag exactly as described in the behavior block. For `--require-declaration-drift`, re-derive both sides from the live `.github/workflows/ci.yml` inside the verifier rather than trusting a value carried in the record — the record is compared against, never treated as, authority (D-30). Explicitly do not consult `gh api repos/szTheory/accrue/branches/main/protection` or `rulesets`: both are empty on this repository, so a protection-derived required set would pass vacuously (D-18).

Implement the strict-suffix pattern: print `gate01 cohort verification: PASS (verified: <sorted flags>)` or `PASS (schema-only: no strict flags supplied, no completeness or drift check ran)`.

Implement `verifyFixtures()` building disposable repositories and synthetic `ci.yml` files under `fs.mkdtempSync`, with one positive control plus a negative control for every behavior bullet, and wire it to both `node --test` and the bare `--fixtures` flag, printing `gate01 cohort fixtures: PASS`.
  </action>
  <verify>
    <automated>node --check scripts/ci/verify_gate01_cohort.mjs &amp;&amp; node --test scripts/ci/verify_gate01_cohort.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check`, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node scripts/ci/verify_gate01_cohort.mjs --fixtures --expected-repository szTheory/accrue --require-cohort-completeness --require-declaration-drift --require-exit-codes --require-clean-checkout --require-determinism</automated>
    <fails_when>non-zero exit, a line beginning `gate01 cohort verify: FAIL:`, stdout missing `gate01 cohort fixtures: PASS`, or a PASS suffix containing `schema-only` despite the five strict flags being supplied</fails_when>
  </verify>
  <acceptance_criteria>
    - The verifier issues no request to a GitHub branch-protection or rulesets endpoint — no reference to a protection or rulesets API path exists in the module.
    - The module defines `assertSameMap` emitting a message containing all three of the substrings `missing=[`, `extra=[`, and `changed=[`.
    - A fixture asserts a throw when a declared job is absent from the record, with that job key in the `missing` list.
    - A fixture asserts a throw when a job appears twice in the record.
    - A fixture asserts a throw when a synthetic `ci.yml` renames a job in only one of the two declarations.
    - A fixture asserts a throw when a `proved` row carries a non-zero exit code.
    - A fixture asserts a throw when `checkout.caches_restored` is the boolean true.
    - A fixture asserts a throw when the records file path does not exist, with the path in the message.
  </acceptance_criteria>
  <done>The verifier exists, its hermetic fixture suite passes with all five strict flags, the required set is derived from the in-repo declaration rather than from GitHub, and every completeness, drift, exit-code, checkout-purity, and determinism boundary has a negative control.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the declared cohort in a scratch clone at the candidate SHA and commit the evidence</name>
  <files>.planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json, .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/231-exact-sha-release-gate-proof/231-RESEARCH.md (the CI Cohort Enumeration table giving the exact local-equivalent command per job, and the Local Cost and Environment Prerequisites section)
    - .github/workflows/ci.yml (the per-job `run:` steps being reproduced locally)
    - scripts/ci/collect_gate01_cohort.mjs, render_gate01_cohort.mjs, verify_gate01_cohort.mjs (created in Tasks 1 and 2)
    - .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json (the candidate SHA this run binds to, produced by Plan 01)
  </read_first>
  <precondition>A Docker daemon is running and responds to `docker info` with exit 0 — `host-docker-smoke` is a declared merge-blocking job with no fallback, and the research environment found the daemon stopped. A PostgreSQL server accepts connections on port 5432 — `pg_isready` exits 0 — for the `release-gate`, `phase18-tax-gate`, `host-integration`, and `playwright-e2e` lanes.</precondition>
  <action>
Create a scratch clone in the session scratchpad with `git clone --no-hardlinks <local repo path> <scratch dir>` and check out the exact candidate SHA recorded in `231-ROLLBACK-POINT.json`. Do NOT use `git worktree add` — the repository inventory verifier pins an exact multiset of worktree rows and any added row fails it on the next recapture (D-12). Capture `git worktree list` row count in the subject repository before and after, and assert it unchanged.

Do not copy `deps/`, `_build/`, `priv/plts`, or any built asset directory into the clone, and do not point the clone at a shared build or PLT cache. Every dependency fetch, compile, Dialyzer PLT build, Chromium install, and asset build happens fresh inside the clone — D-13 makes "without ignored caches" true by construction rather than by assertion. Expect real cost: cold Dialyzer PLTs across three packages and four `release-gate` matrix cells, several independent Chromium downloads, and a `host-docker-smoke` boot poll that runs up to 900 seconds.

Confirm the clone's tracked `.tool-versions` carries the Elixir and Erlang lines before the first `mix` invocation; a clone lacking them fails immediately with a no-version-set error (231-RESEARCH.md Pitfall 2).

Run each declared merge-blocking job's local-equivalent command from the 231-RESEARCH.md CI Cohort Enumeration table, in the clone, recording for each: the argv array actually executed, the integer exit code, and the elapsed duration. Do not paraphrase the commands — transcribe them from the table, which itself transcribes the `ci.yml` `run:` steps.

Classify each row's `lane_class` and `state`:
- The twelve executable merge-blocking jobs get `lane_class: merge-blocking` and `state: proved` with exit code 0, or `state: failed` with the recorded non-zero exit code. A gate that runs and fails on its merits is a GATE-01 blocker recorded honestly, not a still-open ship window (D-22).
- `annotation-sweep` gets `lane_class: merge-blocking`, `state: non_run`, `reason` stating that it requires a workflow run id and an Actions API token and sweeps annotations of a remote run, so it has no scratch-clone-local form, and `deferred_to` set to the string `GATE-02`. Its proof is the dispatch run Plan 04 produces.
- `admin-ui-ratchet-guardrails`: read its own `continue-on-error` flag in `ci.yml` at the candidate SHA and record `state: advisory` if the flag is set, `proved`/`failed` otherwise. Do not conflate a non-blocking advisory lane with a required one.
- `live-stripe` gets `lane_class: credential-gated` and `state: skipped` with a reason naming the required Stripe test secrets and the job's own statement that it never runs on pull requests (D-11).
- `provider-proof-trigger` and `provider-proof-incident` get `lane_class: scheduler-only` and `state: non_run` with a reason naming their schedule/dispatch-only trigger conditions (D-11).
- `ios-offline-client` gets `lane_class: pr-not-merge-blocking` and `state: non_run` with the reason that it runs on pull requests but is deliberately absent from the `annotation-sweep` `needs:` list, placing it outside the declared merge-blocking contract (D-11).
- `mix hex.publish --dry-run` and `gh workflow run ci.yml` each get `lane_class: not-a-declared-gate` and `state: non_run` with the reason that they correspond to no CI job and are therefore not repository-declared local-CI-equivalent gates (D-10). Record them because the Phase 230 thirteen-lane table is a disposition record, not a scope proposal — they must be visibly dispositioned, not silently dropped.

Write the raw per-job results to a scratch input file, then run the collector to emit `.planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json` with mode `0o600`, and the renderer to emit `231-GATE-01-EVIDENCE.md`. Sanitize by construction: the `checkout.source` field records the string `local repository`, never the scratch directory path, and no absolute path, home-directory segment, actor name, or adopter identifier reaches either file (D-31).

Remove the scratch clone when done.
  </action>
  <verify>
    <automated>node scripts/ci/verify_gate01_cohort.mjs --repo . --records .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json --rendered .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.md --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-cohort-completeness --require-declaration-drift --require-clean-checkout --require-determinism</automated>
    <fails_when>non-zero exit, a line beginning `gate01 cohort verify: FAIL:`, a missing/extra triple naming any declared job, or a PASS suffix containing `schema-only`</fails_when>
    <automated>grep -nE '/Users/|/home/|\$HOME' .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.md; test $? -eq 1</automated>
    <fails_when>non-zero exit, meaning `grep` found an absolute path or a home-directory reference in a committed evidence artifact</fails_when>
    <automated>node --input-type=module -e "const fs=require('node:fs'); const r=JSON.parse(fs.readFileSync('.planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json','utf8')); const bad=r.rows.filter(x=&gt;x.state==='proved'&amp;&amp;!Number.isInteger(x.exit_code)); if(bad.length){console.error('proved rows without an integer exit code: '+bad.map(x=&gt;x.job).join(','));process.exit(1);} const sweep=r.rows.find(x=&gt;x.job==='annotation-sweep'); if(!sweep||sweep.deferred_to!=='GATE-02'){console.error('annotation-sweep row missing its GATE-02 pointer');process.exit(1);}"</automated>
    <fails_when>non-zero exit, or stderr naming a `proved` row with no integer exit code, or reporting the `annotation-sweep` row missing its GATE-02 pointer</fails_when>
  </verify>
  <acceptance_criteria>
    - `git worktree list` in the subject repository prints the same row count before and after the task.
    - The committed JSON contains exactly one row per declared merge-blocking job and one row per declared out-of-cohort lane, with no duplicates.
    - Every row's `state` is one of `proved`, `failed`, `skipped`, `advisory`, `non_run`.
    - Every `proved` row carries a non-empty `argv` array and an integer `exit_code`.
    - Every `skipped` and `non_run` row carries a non-empty `reason`.
    - `checkout.method` is the string `git clone`, `checkout.caches_restored` is the boolean false, and `checkout.worktree_rows_delta` is the integer 0.
    - The `admin-ui-ratchet-guardrails` row's state reflects that job's own `continue-on-error` flag as read from `ci.yml` at the candidate SHA.
    - `231-GATE-01-EVIDENCE.md` is fenced by the `phase231-gate01-cohort` start and end markers.
  </acceptance_criteria>
  <done>The declared merge-blocking cohort has run in a fresh cache-free clone at the exact candidate SHA, every declared job and every declared out-of-cohort lane has exactly one honestly classified row with a recorded exit code or reason, and the strict verifier passes on the committed evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subject repository to scratch clone | The clone must receive source only — no build outputs, PLTs, or caches may cross this boundary, or the "clean checkout" claim is false. |
| local command output to committed evidence | Command argv arrays, exit codes, and failure text cross from the developer machine into a file published on a public repository. |
| host services (Docker, PostgreSQL) to cohort run | Local daemons stand in for CI service containers; a stopped daemon must fail the gate, not silently skip it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-12 | Information Disclosure | `231-GATE-01-EVIDENCE.json` and `.md` | high | mitigate | `checkout.source` records a fixed string rather than the scratch path; `validateCohortRow` rejects absolute paths and home-directory segments; a grep verify command fails the task on any match (D-31). |
| T-231-13 | Repudiation | Cohort completeness | high | mitigate | The declared list is parsed from `ci.yml` and compared by exact set equality with a missing/extra triple; an unparseable header throws rather than returning an empty set (D-09, D-18, D-30). |
| T-231-14 | Spoofing | A cached build masquerading as a clean checkout | high | mitigate | `--require-clean-checkout` asserts `checkout.caches_restored` is the boolean false and `worktree_rows_delta` is 0; nothing is copied into the clone (D-13). |
| T-231-15 | Spoofing | `proved` without evidence | high | mitigate | `--require-exit-codes` rejects a `proved` row lacking an integer exit code or an argv array, and rejects a non-zero exit code on a `proved` row (D-29). |
| T-231-16 | Denial of Service | Local Docker network and container lifecycle | low | accept | `host-docker-smoke` creates a `proxy` network and brings a compose stack up and down on the developer machine; this mirrors CI and is torn down by the job's own `docker compose down` step. |
| T-231-SC | Tampering | npm/pip/cargo installs | high | mitigate | The clone runs `mix deps.get` and `npm ci` against the candidate's committed lockfiles only; no package is added and no manifest is edited, so no new dependency enters the supply chain. A lockfile modification during the run is a hard failure — `git status --porcelain` inside the clone must show no change to any `mix.lock` or `package-lock.json`. |
</threat_model>

<verification>
- `node --check` and `node --test` pass for all three new modules.
- The verifier's hermetic fixture suite passes with all five strict flags.
- The strict real-mode run against the committed evidence passes with four strict flags listed in its PASS suffix.
- `git worktree list` row count is unchanged.
- No absolute path or home-directory reference appears in either committed evidence file.
</verification>

<success_criteria>
A maintainer can read one rendered document and see, for the exact candidate SHA, every job this repository declares merge-blocking, whether it ran, what its exit code was, and for anything that did not run, exactly why.
</success_criteria>

<output>
Create `.planning/phases/231-exact-sha-release-gate-proof/231-03-SUMMARY.md` when done.
</output>
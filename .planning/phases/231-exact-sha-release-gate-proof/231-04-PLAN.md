---
phase: 231-exact-sha-release-gate-proof
plan: 04
type: execute
wave: 2
depends_on: ["231-01"]
files_modified:
  - scripts/ci/collect_ci_baseline.mjs
  - scripts/ci/verify_ci_baseline.mjs
  - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson
  - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.md
autonomous: false
requirements: [GATE-02]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The required-check set is derived from the in-repo `ci.yml` header declaration and asserted against the live job graph; the verifier never queries branch protection or rulesets, both of which are empty on this repository and would pass vacuously (D-18)."
    - "A job silently renamed, added, or removed between the header declaration and the live job graph fails the gate, naming the drifted key (D-18)."
    - "Every record carries `event_class` as a first-class field set to `workflow_dispatch`, so a dispatch proof can never be read as a pull-request proof (D-17)."
    - "Provider lanes keep the closed `PROVIDER_STATES` enum (`proved`, `failed`, `misconfigured`, `blocked`, `skipped`, `non_run`); no `success` or `green` alias is reachable and no aggregate boolean is emitted (D-19, D-29)."
    - "A record claiming `proved` without a recorded exit code or conclusion is a hard failure (D-21, D-29)."
    - "Exactly one branch, `integration/v1.62-candidate`, is pushed to `origin`; `main` is not pushed, no tag is moved, no force-push occurs, and no pull request is opened (D-14)."
    - "The workflow is triggered by `gh workflow run ci.yml --ref integration/v1.62-candidate`; pushing alone fires zero checks because the `push:` trigger is filtered to `branches: [main]` (D-15)."
    - "Observation polls to completion with a deadline and fails closed on a run that is queued, cancelled, or still in progress; absence of failure is never treated as success (D-20)."
    - "`live-stripe` is dispatched with `run_live_stripe` explicitly false and recorded `skipped` or `non_run` with a reason, which is honest and acceptable (D-21)."
    - "All records key on the candidate's full 40-hex `head_sha`; no run keyed to the superseded candidate `bab50d92` is reused (D-01)."
  artifacts:
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.md
  key_links:
    - "`gh workflow run` performs the one authorized mutation; `scripts/ci/ci_monitor.cjs watch` performs all observation — the monitor is read-only by construction and has no dispatch verb (231-RESEARCH.md Pitfall 3)."
    - "`collect_ci_baseline.mjs` is extended in place rather than forked; its `normalizeRun`/`normalizeJob`/`cohortFingerprint` already key on `head_sha` and derive the required job set from the live job graph (D-19)."
  prohibitions:
    - "MUST NOT push any ref other than `integration/v1.62-candidate`, MUST NOT push `main`, MUST NOT force-push, MUST NOT move or re-annotate a tag, and MUST NOT open a pull request (D-14, Phase 232 owns REL-04)."
    - "MUST NOT derive the required-check set from `branches/main/protection` or `rulesets` (D-18)."
    - "MUST NOT use `ci_monitor.cjs` to dispatch; it exposes only list, inspect, and watch (231-RESEARCH.md Pitfall 3)."
    - "MUST NOT treat a run that is queued, cancelled, or in progress as success, and MUST NOT treat a not-yet-propagated run as `non_run` (D-20, 231-RESEARCH.md Pitfall 4)."
    - "MUST NOT emit absolute paths, `$HOME`, actor names, adopter identifiers, or secrets into the committed evidence (D-31)."
---

<objective>
Prove GATE-02: dispatch `ci.yml` against the exact candidate SHA on `origin`, poll to completion, and record per-lane outcomes whose event class and proof states are explicit and unconflatable.

Purpose: branch protection declares nothing on this repository, so the required set must come from the in-repo declaration and be checked for drift (D-18). A `workflow_dispatch` run executes the identical merge-blocking job set a pull request would, while creating no PR object (D-16).
Output: an extended `collect_ci_baseline.mjs` / `verify_ci_baseline.mjs`, plus committed GATE-02 records and a rendered diagnostic.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md
@.planning/phases/231-exact-sha-release-gate-proof/231-RESEARCH.md
@.planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
@.github/workflows/ci.yml
</context>

<artifacts_this_phase_produces>
Created or extended by this plan:

- `scripts/ci/collect_ci_baseline.mjs` (extended in place, not forked) — new exports `declaredRequiredJobSet`, `liveRequiredJobSet`, `requiredJobSetDrift`; `RUN_INPUT_FIELDS` gains `event_class`, `required_job_set_declared`, and `required_job_set_observed`.
- `scripts/ci/verify_ci_baseline.mjs` (extended in place) — new CLI flags `--require-required-job-set`, `--require-event-class`, `--require-exit-codes`.
- New evidence artifact paths: `.planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson` and `231-GATE-02-EVIDENCE.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: protection -->
<!-- planner-discipline-allow: rulesets -->
<!-- D-18 requires naming the branch-protection and rulesets endpoints in prose so the executor knows
     which reads are forbidden; the corresponding source assertions check for API request paths, not
     for these words appearing in plan prose. -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend collect_ci_baseline.mjs and verify_ci_baseline.mjs for required-job-set drift and event class</name>
  <files>scripts/ci/collect_ci_baseline.mjs, scripts/ci/verify_ci_baseline.mjs</files>
  <read_first>
    - scripts/ci/collect_ci_baseline.mjs (the file being extended in place — `PROVIDER_STATES` at line 12, `RUN_INPUT_FIELDS` and `JOB_INPUT_FIELDS` at lines 18-19, `allowedFields` at lines 25-29, `eventClass` at line 64, `cohortFingerprint` at line 82 with its existing `required_job_set` derivation, `workflowRunnerContracts` at line 330)
    - scripts/ci/verify_ci_baseline.mjs (the `main()` at line 558, the existing `--require-critical-path` flag pattern, the `--fixtures` path)
    - scripts/ci/collect_gate01_cohort.mjs (Plan 03 Task 1 — if it already exists, import its `declaredMergeBlockingJobs` rather than writing a second header parser)
    - .github/workflows/ci.yml (the header comment declaration and the `annotation-sweep` `needs:` array)
    - .planning/milestones/v1.61-phases/226-ci-baseline-proof-semantics/226-CI-BASELINE.md (the proof-state lexicon this record set inherits)
  </read_first>
  <behavior>
    - `declaredRequiredJobSet` returns the thirteen job keys declared merge-blocking in the `ci.yml` header comment, sorted, and throws when the declaration block is absent rather than returning an empty set.
    - `liveRequiredJobSet` returns the job keys reachable from the live job graph for a non-schedule event, sorted.
    - `requiredJobSetDrift` returns an empty triple when the two sets agree, and a populated missing/extra triple naming the drifted key otherwise.
    - A run record validates only when `event_class` is present and is one of the values `eventClass` can produce; a record with `event_class` absent is rejected.
    - `RUN_INPUT_FIELDS` accepts `event_class`, `required_job_set_declared`, and `required_job_set_observed`, and still rejects any field outside the allow-list.
    - No value in `PROVIDER_STATES` equals `success` or `green`, and no code path maps a GitHub conclusion of `success` directly to a provider state without passing through the enum.
    - A record with `provider_state: proved` and no recorded conclusion or exit code is rejected.
    - The existing Phase 226 fixture cases continue to pass unchanged after the extension.
  </behavior>
  <action>
Extend `scripts/ci/collect_ci_baseline.mjs` in place — D-19 is explicit that this file is extended, never forked.

Add `event_class`, `required_job_set_declared`, and `required_job_set_observed` to `RUN_INPUT_FIELDS`, preserving the existing `allowedFields` rejection of anything outside the set. Populate `event_class` in `normalizeRun` from the existing `eventClass(run.event)` helper so a `workflow_dispatch` run is labelled as such at the record level and not merely inside `cohortFingerprint`'s hash (D-17).

Add `declaredRequiredJobSet(source)` parsing the `ci.yml` header comment's merge-blocking declaration. If Plan 03's `collect_gate01_cohort.mjs` already exports `declaredMergeBlockingJobs`, import and re-export it rather than writing a second parser. Add `liveRequiredJobSet(source)` deriving the same set from the live job graph via the existing `workflowRunnerContracts` reader plus the `annotation-sweep` `needs:` array. Add `requiredJobSetDrift(declared, live)` returning a missing/extra triple.

Explicitly do not add any code path that reads `repos/<owner>/<repo>/branches/main/protection` or `repos/<owner>/<repo>/rulesets`. Both return nothing on this repository, so a required set derived from them is empty and every check passes vacuously — silently wrong rather than merely unavailable (D-18). Add a source comment stating that reason at the definition site of `declaredRequiredJobSet`.

Reject `provider_state: proved` when the record carries neither a recorded conclusion nor a recorded exit code (D-21, D-29). Keep `PROVIDER_STATES` exactly as it is — six members, no `success` or `green` alias, no aggregate boolean added anywhere.

Then extend `scripts/ci/verify_ci_baseline.mjs` in place with three new flags following the existing `--require-critical-path` pattern: `--require-required-job-set` calling `requiredJobSetDrift` against the live `.github/workflows/ci.yml` and failing on any non-empty triple; `--require-event-class` failing when any run record's `event_class` is absent or differs from a value supplied via a new `--expect-event-class` option; and `--require-exit-codes` failing on any record with `provider_state: proved` and no recorded conclusion or exit code.

Add fixture cases for every behavior bullet to the existing Phase 226 fixture mechanism, and confirm the pre-existing fixture cases still pass.
  </action>
  <verify>
    <automated>node --check scripts/ci/collect_ci_baseline.mjs &amp;&amp; node --check scripts/ci/verify_ci_baseline.mjs &amp;&amp; node --test scripts/ci/collect_ci_baseline.mjs</automated>
    <fails_when>non-zero exit, a SyntaxError printed by `node --check`, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node scripts/ci/verify_ci_baseline.mjs --fixtures --expected-repository szTheory/accrue</automated>
    <fails_when>non-zero exit, or stdout missing the line `ci baseline fixtures: PASS` — a regression in the pre-existing Phase 226 fixture cases</fails_when>
    <automated>node --input-type=module -e "import('./scripts/ci/collect_ci_baseline.mjs').then(m =&gt; { const fs = require('node:fs'); const src = fs.readFileSync('.github/workflows/ci.yml','utf8'); const t = m.requiredJobSetDrift(m.declaredRequiredJobSet(src), m.liveRequiredJobSet(src)); const n = t.missing.length + t.extra.length; if (n !== 0) { console.error('required job set drift: missing=' + t.missing + ' extra=' + t.extra); process.exit(1); } })"</automated>
    <fails_when>non-zero exit, or stderr printing a non-empty missing or extra list — the header declaration has drifted from the live job graph</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither extended module contains a reference to a branch-protection or rulesets API path.
    - `PROVIDER_STATES` still has exactly the six members `proved`, `failed`, `misconfigured`, `blocked`, `skipped`, `non_run`.
    - `RUN_INPUT_FIELDS` contains `event_class`, `required_job_set_declared`, and `required_job_set_observed`, and a fixture asserts a throw on a field outside the allow-list.
    - A fixture asserts a throw when a run record omits `event_class`.
    - A fixture asserts a throw when `declaredRequiredJobSet` is given a `ci.yml` with the merge-blocking declaration removed.
    - A fixture asserts a populated triple when a synthetic `ci.yml` renames a job on only one side.
    - A fixture asserts a throw on `provider_state: proved` with no recorded conclusion or exit code.
    - `node scripts/ci/verify_ci_baseline.mjs --fixtures` still passes, proving no Phase 226 regression.
  </acceptance_criteria>
  <done>Both baseline modules are extended in place, the required set is derived from the in-repo declaration with a live-graph drift check, event class is a first-class record field, and the Phase 226 fixture suite still passes.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 2: Confirm the one-way public push of the candidate branch</name>
  <decision>Proceed with the one-way publication of the re-cut candidate SHA to the public repository `szTheory/accrue`, or stop the plan. The decision itself was already made by the maintainer in CONTEXT.md D-14; this gate re-binds that authorization to the exact SHA the re-cut produced, which did not exist when D-14 was recorded.</decision>
  <options>
    A. Go — push exactly `integration/v1.62-candidate` at the SHA shown, then dispatch `ci.yml` against it. This is the D-14 default and the only path that proves GATE-02 in this phase.
    B. No-go — stop the plan here. GATE-02 is then recorded honestly as `non_run` with the reason that the authorized publication did not occur; it is not a silent pass, and it moves to Phase 232 alongside REL-04.
    C. Go, but re-cut first — only if the live `git rev-parse integration/v1.62-candidate` no longer matches the SHA recorded in `231-ROLLBACK-POINT.json`, meaning something moved the ref after Plan 01. Return to Plan 01 rather than publishing an unverified object.
  </options>
  <resume-signal>The maintainer answers go or no-go, naming the option letter. On A, continue to Task 3 in the same session. On B, stop the plan and write the summary with GATE-02 recorded `non_run`. On C, stop and re-run Plan 01.</resume-signal>
  <read_first>
    - .planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md (D-14 — the explicit authorization already on record)
    - .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json (the exact SHA about to become public)
    - prompts/GSD-REPO-HYGIENE.md (the no-force-push and immutable-history posture)
  </read_first>
  <reversibility rating="one-way">D-14: pushing `integration/v1.62-candidate` to `origin` publishes the commit on the public repository `szTheory/accrue`, where it may be fetched, referenced, or indexed. Deleting the branch later does not unpublish the commit.</reversibility>
  <action>
This is an irreversible-external-operation gate, not an acceptance checkpoint. CLAUDE.md's post-218 policy forbids human verification as a completion gate but explicitly permits human interaction for irreversible external operations; nothing here is being accepted or judged, and `behavior_unverified` stays 0.

The decision itself is already made. D-14 records the maintainer's separate explicit authorization to push exactly one branch and dispatch the workflow. Do not re-litigate it. Present the concrete, now-measured facts and ask only for go or no-go before the irreversible step:

- The exact 40-hex candidate SHA that is about to become public, as measured live right now, and the fact that the re-cut changed it from the superseded `bab50d92`.
- That `origin` is `https://github.com/szTheory/accrue.git`, a public repository, authenticated as `szTheory`.
- That exactly one ref is pushed — no `main`, no tag, no force, no pull request.
- That deleting the branch afterwards does not unpublish the commit.

Record the confirmation, including the SHA it was given for, in the plan summary. If the answer is no-go, stop the plan here; GATE-02 cannot be proved without it, and that is an honest `non_run`, not a silent pass.
  </action>
  <acceptance_criteria>
    - The summary records the exact 40-hex SHA the authorization was given for, and that SHA equals the live `git rev-parse integration/v1.62-candidate` at the moment Task 3 runs.
    - The summary records that the authorization covers exactly one ref and excludes `main`, tags, force-push, and pull-request creation.
  </acceptance_criteria>
  <done>Go or no-go is recorded against the exact SHA, and the summary states the scope of what was authorized.</done>
</task>

<task type="auto">
  <name>Task 3: Push, dispatch, poll to completion, and commit the GATE-02 evidence</name>
  <files>.planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson, .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.md</files>
  <read_first>
    - scripts/ci/ci_monitor.cjs (the read-only command set and the `watch` option parsing — `--repo`, `--sha` requiring a full lowercase 40-hex, `--workflow`, `--timeout-seconds` capped at 3600, `--poll-seconds` capped at 300, and the exit codes 68 for deadline breach and 69 for unsuccessful completion)
    - scripts/ci/collect_ci_baseline.mjs and scripts/ci/verify_ci_baseline.mjs (extended in Task 1)
    - .github/workflows/ci.yml (the `workflow_dispatch` trigger with its `run_live_stripe` boolean input; the `if: github.event_name != 'schedule'` guard on every merge-blocking job; the `live-stripe` job's `if:` expression)
    - .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json
  </read_first>
  <precondition>`gh auth status` exits 0 and reports a token carrying the `workflow` scope; without it `gh workflow run` cannot dispatch.</precondition>
  <action>
Push exactly one ref: `git push origin integration/v1.62-candidate`. Do not push `main`, do not use a force flag, do not push or move any tag, and do not open a pull request — the PR is Phase 232's REL-04 (D-14).

Do not expect checks to appear from the push. The `push:` trigger is filtered to `branches: [main]`, so pushing the candidate branch fires zero checks; a plan that pushes and waits would wait forever (D-15). The dispatch is what produces check runs.

Dispatch with `gh workflow run ci.yml --ref integration/v1.62-candidate -f run_live_stripe=false`. Use `gh` directly: `scripts/ci/ci_monitor.cjs` is read-only by construction and exposes only list, inspect, and watch, with no dispatch verb (231-RESEARCH.md Pitfall 3). Setting `run_live_stripe` false means `live-stripe` does not run at all, which is honest and acceptable under D-21 and D-11, and avoids exercising Stripe credentials for a proof that does not require them.

Poll to completion with `node scripts/ci/ci_monitor.cjs watch --repo szTheory/accrue --sha <full 40-hex candidate> --workflow CI --timeout-seconds 3600 --poll-seconds 30`. The `--sha` option requires a full lowercase 40-hex; a 12-character abbreviation is rejected. Do not write a bespoke sleep loop — `watch` already implements absolute-deadline semantics, exiting 68 on deadline breach and 69 on unsuccessful completion, which is exactly D-20's fail-closed contract. Account for dispatch propagation delay: a run may not be visible immediately after `gh workflow run` returns, and a query that runs too soon must be retried by the watcher rather than recorded as `non_run` (231-RESEARCH.md Pitfall 4). Treat queued, in-progress, and cancelled as not-success.

Collect the run and job data with the extended `collect_ci_baseline.mjs`, keyed on the candidate's full 40-hex `head_sha`. Do not reuse any run keyed to the superseded candidate `bab50d92` — the re-cut voided those (D-01). Emit `.planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson` with mode `0o600`, then render `231-GATE-02-EVIDENCE.md` so the event class appears in the first screenful: state plainly that this is a `workflow_dispatch`-class proof, and that Phase 232's pull request will produce a second, distinct check-run set for the same SHA (D-17).

Assign every provider lane a state from the closed `PROVIDER_STATES` enum with no aggregate boolean and no `success` or `green` alias. Record `live-stripe` as `skipped` or `non_run` with a reason naming the false `run_live_stripe` input, and reject any record claiming `proved` without a recorded conclusion or exit code (D-21, D-29). A green Actions conclusion alone is not provider proof.

Sanitize by construction: no absolute path, home-directory reference, actor name, adopter identifier, or secret reaches either committed file (D-31).
  </action>
  <verify>
    <automated>REMOTE_CAND="$(git ls-remote --heads origin integration/v1.62-candidate)" &amp;&amp; REMOTE_MAIN="$(git ls-remote --heads origin main)" &amp;&amp; LOCAL_CAND="$(git rev-parse integration/v1.62-candidate)" &amp;&amp; LOCAL_MAIN="$(git rev-parse origin/main)" &amp;&amp; test "${REMOTE_CAND%%	*}" = "$LOCAL_CAND" &amp;&amp; test "${REMOTE_MAIN%%	*}" = "$LOCAL_MAIN"</automated>
    <fails_when>non-zero exit — either the remote candidate branch does not match the local candidate SHA, or `origin/main` moved, meaning a ref other than the one authorized was pushed</fails_when>
    <automated>node scripts/ci/verify_ci_baseline.mjs --records .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson --rendered .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.md --expected-repository szTheory/accrue --expect-event-class workflow_dispatch --require-required-job-set --require-event-class --require-exit-codes</automated>
    <fails_when>non-zero exit, a line beginning `ci baseline verify: FAIL:`, a non-empty required-job-set drift triple, or a record whose `event_class` is absent or not `workflow_dispatch`</fails_when>
    <automated>node --input-type=module -e "const fs=require('node:fs'); const sha=require('node:child_process').execFileSync('git',['rev-parse','integration/v1.62-candidate'],{encoding:'utf8'}).trim(); const lines=fs.readFileSync('.planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson','utf8').trim().split('\n').map(l=&gt;JSON.parse(l)); const wrong=lines.filter(r=&gt;r.sha&amp;&amp;r.sha!==sha); if(wrong.length){console.error('records bound to a foreign sha: '+wrong.length);process.exit(1);} const bad=lines.filter(r=&gt;r.provider_state==='proved'&amp;&amp;!r.conclusion); if(bad.length){console.error('proved records without a recorded conclusion: '+bad.length);process.exit(1);} if(!/success|green/i.test('x')&amp;&amp;lines.some(r=&gt;r.provider_state==='green'||r.provider_state==='success')){console.error('forbidden provider state alias present');process.exit(1);}"</automated>
    <fails_when>non-zero exit, or stderr reporting records bound to a foreign sha, `proved` records with no recorded conclusion, or a forbidden provider-state alias</fails_when>
    <automated>grep -nE '/Users/|/home/|\$HOME' .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.md; test $? -eq 1</automated>
    <fails_when>non-zero exit, meaning `grep` found an absolute path or a home-directory reference in a committed evidence artifact</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin` lists `integration/v1.62-candidate` at the local candidate SHA, and `origin/main` is unchanged from its pre-task value.
    - `gh pr list --repo szTheory/accrue --head integration/v1.62-candidate` returns no pull request.
    - `git ls-remote --tags origin` is byte-identical to its pre-task value.
    - Every record's `event_class` is the string `workflow_dispatch`.
    - The rendered Markdown states the event class within its first section, and names Phase 232's future pull-request-class proof as a distinct second set for the same SHA.
    - The `live-stripe` record's state is `skipped` or `non_run` with a non-empty reason naming the false dispatch input.
    - Every record whose `provider_state` is `proved` carries a recorded conclusion; no record carries an aggregate boolean field.
    - The polling step used `ci_monitor.cjs watch` and its recorded exit code is 0 — not 68 and not 69.
  </acceptance_criteria>
  <done>The candidate branch is on `origin` and only that branch, a `workflow_dispatch` run for the exact candidate SHA polled to completion under a deadline, and the committed records carry explicit event class and closed-enum provider states verified against the in-repo required-job declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository to public `origin` (`szTheory/accrue`) | One ref crosses into a public repository; the commit becomes permanently fetchable. |
| GitHub Actions run, executing with repository credentials | The dispatched workflow can access repository secrets; the `run_live_stripe` input controls whether Stripe credentials are exercised. |
| GitHub API response to committed evidence | Run and job metadata, including failure text, crosses into a committed file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-17 | Tampering | `git push origin` | high | mitigate | Exactly one explicit refspec; no force flag; a verify command asserts the remote candidate matches the local SHA, `origin/main` is unmoved, and the remote tag list is byte-identical. |
| T-231-18 | Information Disclosure | Publishing the candidate SHA on a public repository | medium | accept | Deliberate and separately authorized under D-14, re-confirmed at the exact SHA by the Task 2 gate; scope limited to one branch; no pull request is opened. |
| T-231-19 | Elevation of Privilege | Dispatched workflow accessing repository secrets | medium | mitigate | Dispatch with `run_live_stripe=false` so the Stripe credential lane does not run at all; the merge-blocking cohort needs no secret. |
| T-231-20 | Repudiation | A checker that passes vacuously on an empty required set | high | mitigate | The required set is derived from the in-repo `ci.yml` declaration with a drift check against the live job graph; no branch-protection or rulesets read exists in either module (D-18). |
| T-231-21 | Spoofing | A dispatch proof read later as a pull-request proof | high | mitigate | `event_class` is a validated first-class record field; `--require-event-class` fails when it is absent or unexpected; the rendered document states the class in its first section (D-17). |
| T-231-22 | Spoofing | Treating absence of failure as success | high | mitigate | `ci_monitor.cjs watch` fails closed with exit 68 on deadline breach and 69 on unsuccessful completion; queued, in-progress, and cancelled are not-success (D-20). |
| T-231-23 | Information Disclosure | Failure text in committed records | high | mitigate | `allowedFields` sanitization plus a grep verify command rejecting absolute paths and home-directory references (D-31). |
| T-231-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages locally. The dispatched CI run resolves the candidate's committed lockfiles on GitHub runners; no manifest is edited and no dependency is added. |
</threat_model>

<verification>
- `node --check` and `node --test` pass for both extended baseline modules, and the pre-existing Phase 226 fixture suite still passes.
- The live required-job-set drift triple is empty against `.github/workflows/ci.yml`.
- `git ls-remote` confirms exactly one new remote branch and an unmoved `origin/main` and tag list.
- The strict baseline verification passes with the event-class, required-job-set, and exit-code flags.
- No absolute path or home-directory reference appears in either committed evidence file.
</verification>

<success_criteria>
A maintainer can see, for the exact candidate SHA and no other, which required GitHub Actions jobs ran and how they concluded, under what event class, with every provider lane keeping a distinct honest proof state.
</success_criteria>

<output>
Create `.planning/phases/231-exact-sha-release-gate-proof/231-04-SUMMARY.md` when done, recording the dispatch run id, the run URL, the event class, and the SHA the Task 2 authorization was given for.
</output>
---
phase: 231-exact-sha-release-gate-proof
plan: 06
type: execute
wave: 4
depends_on: ["231-05"]
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/README.md
  - .planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every new Phase 231 verifier's hermetic self-tests run merge-blocking inside the `docs-contracts-shift-left` job, following the three-step and four-step shapes the Phase 229 and Phase 230 blocks already use (D-32)."
    - "No new CI step depends on `integration/v1.62-candidate` or any other local-only ref; a step that required one would fail on every GitHub runner."
    - "`scripts/ci/README.md` carries one evidence-table row per new Phase 231 artifact, each with the exact verify command that reproduces its proof (D-32)."
    - "A fresh `231-REPOSITORY-INVENTORY.json` is minted after all window-status edits, `.planning/STATE.md`, and the roadmap have settled — never before, and never by re-diffing the frozen 229 or 230 capsules (D-28)."
    - "The minted capsule bundle is `chmod 600` before publish, because `git bundle create` resets mode to 0644 (D-28)."
    - "The new `ci.yml` steps do not change the declared merge-blocking job list, so the required-job-set drift check Plan 04 added still reports an empty triple (D-18)."
  artifacts:
    - .planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json
  key_links:
    - "`docs-contracts-shift-left` is where new merge-blocking verifiers land; the Phase 230 archive-invariants block and the integration-disposition triad block are the two shapes to clone."
    - "The capsule is a point-in-time artifact: `ci.yml` deliberately does not run strict verification of a published capsule routinely, because it fails on any planning-doc update."
  prohibitions:
    - "MUST NOT add a CI step that reads `integration/v1.62-candidate`, `review/v1.62-candidate-code-only`, or any other unpushed local ref."
    - "MUST NOT wire strict verification of a published capsule into routine CI; only `--fixtures` self-tests belong there (D-27)."
    - "MUST NOT re-diff or modify the frozen 229 or 230 capsules while minting the new one (D-28)."
    - "MUST NOT add, rename, or remove a job in the `ci.yml` merge-blocking declaration or in `annotation-sweep`'s `needs:` array (D-18)."
    - "MUST NOT emit absolute paths, `$HOME`, actor names, adopter identifiers, or secrets into the capsule or the README (D-31)."
---

<objective>
Make the Phase 231 verifiers permanently merge-blocking, document every new evidence artifact for contributors, and mint the phase's capsule last.

Purpose: D-32 requires new artifacts to resolve through `phase_evidence_path.mjs` and to get a `scripts/ci/README.md` row; D-28 requires the capsule to be minted after all window-status edits and planning documents settle, or it invalidates itself on the next step.
Output: new `ci.yml` steps, new README rows, and `231-REPOSITORY-INVENTORY.json`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-exact-sha-release-gate-proof/231-CONTEXT.md
@.planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md
@.github/workflows/ci.yml
@scripts/ci/README.md
</context>

<artifacts_this_phase_produces>
Created by this plan:

- New `.github/workflows/ci.yml` step names inside the `docs-contracts-shift-left` job: `Recut candidate shape and ancestry contract (D-01, D-04, D-07)`, `GATE-01 cohort triad units and fixture contract (D-09, D-10, D-18)`, `Window dispositions triad units and fixture contract (D-22, D-26, D-30, D-31)`.
- New `scripts/ci/README.md` section `## Phase 231 exact-SHA release gate proof` with one evidence-table row per Phase 231 artifact.
- New evidence artifact path: `.planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json`, plus its sibling capsule bundle and manifest.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the Phase 231 verifiers into docs-contracts-shift-left</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the `docs-contracts-shift-left` job; specifically the `Phase-evidence archive-path sweep (D-22 standing invariant)` step and the `Integration disposition triad units and fixture contract (D-04, D-15, D-20, D-21, D-37)` step immediately after it, plus the comment above the latter explaining why local-only refs must not be depended on)
    - scripts/ci/verify_recut_candidate.mjs (Plan 01)
    - scripts/ci/collect_gate01_cohort.mjs, render_gate01_cohort.mjs, verify_gate01_cohort.mjs (Plan 03)
    - scripts/ci/collect_window_dispositions.mjs, render_window_dispositions.mjs, verify_window_dispositions.mjs (Plan 02)
    - .planning/phases/231-exact-sha-release-gate-proof/231-PATTERNS.md (the ci.yml wiring analog section)
  </read_first>
  <action>
Add three steps to the existing `docs-contracts-shift-left` job in `.github/workflows/ci.yml`, placed immediately after the `Integration disposition triad units and fixture contract` step, each using the folded-scalar multi-command shape that step already uses.

Step named `Recut candidate shape and ancestry contract (D-01, D-04, D-07)`: run `node --test scripts/ci/verify_recut_candidate.mjs` followed by `node scripts/ci/verify_recut_candidate.mjs --fixtures --expected-repository szTheory/accrue`, joined by the shell and-operator on continuation lines.

Step named `GATE-01 cohort triad units and fixture contract (D-09, D-10, D-18)`: run `node --test scripts/ci/collect_gate01_cohort.mjs`, `node --test scripts/ci/render_gate01_cohort.mjs`, `node --test scripts/ci/verify_gate01_cohort.mjs`, then `node scripts/ci/verify_gate01_cohort.mjs --fixtures --expected-repository szTheory/accrue --require-cohort-completeness --require-declaration-drift --require-exit-codes --require-clean-checkout --require-determinism`.

Step named `Window dispositions triad units and fixture contract (D-22, D-26, D-30, D-31)`: run `node --test scripts/ci/collect_window_dispositions.mjs`, `node --test scripts/ci/render_window_dispositions.mjs`, `node --test scripts/ci/verify_window_dispositions.mjs`, then `node scripts/ci/verify_window_dispositions.mjs --fixtures --expected-repository szTheory/accrue --require-row-join --require-evidence-freshness --require-waiver-completeness --require-determinism`.

Every command above is hermetic: each builds disposable fixture repositories in a temp directory and touches no network, no credential, and no ref outside them. Add a comment above the block, mirroring the wording of the existing Phase 230 comment, stating that these steps deliberately do not depend on `integration/v1.62-candidate` or `review/v1.62-candidate-code-only` because those refs are local-only and a step requiring them would fail on every GitHub runner. Note that the candidate branch does now exist on `origin` after Plan 04's push, but the code-only review branch never leaves the machine, and a CI step binding to a mutable integration branch would still be wrong.

Do not wire strict verification of any published capsule into routine CI. The existing comment above the Phase 229 block states the reason: it is a point-in-time check that fails on any planning-doc update, so it has no recurring CI value (D-27).

Do not add, rename, or remove any job. The merge-blocking declaration in the header comment and `annotation-sweep`'s `needs:` array must both stay exactly as they are, so Plan 04's drift check keeps reporting an empty triple (D-18).
  </action>
  <verify>
    <automated>node --input-type=module -e "const fs=require('node:fs'); const s=fs.readFileSync('.github/workflows/ci.yml','utf8'); for (const n of ['Recut candidate shape and ancestry contract','GATE-01 cohort triad units and fixture contract','Window dispositions triad units and fixture contract']) { if (!s.includes(n)) { console.error('missing ci.yml step: '+n); process.exit(1); } } for (const r of ['integration/v1.62-candidate','review/v1.62-candidate-code-only']) { for (const line of s.split('\n')) { if (line.includes(r) &amp;&amp; !line.trim().startsWith('#')) { console.error('ci.yml step references a local-only ref: '+r); process.exit(1); } } }"</automated>
    <fails_when>non-zero exit, or stderr naming a missing step title or a non-comment `ci.yml` line that references a local-only ref</fails_when>
    <automated>node --input-type=module -e "import('./scripts/ci/collect_ci_baseline.mjs').then(m =&gt; { const fs=require('node:fs'); const src=fs.readFileSync('.github/workflows/ci.yml','utf8'); const t=m.requiredJobSetDrift(m.declaredRequiredJobSet(src), m.liveRequiredJobSet(src)); if (t.missing.length + t.extra.length !== 0) { console.error('required job set drift introduced: missing='+t.missing+' extra='+t.extra); process.exit(1); } })"</automated>
    <fails_when>non-zero exit, or stderr printing a non-empty missing or extra list — the CI edit changed the declared job graph</fails_when>
    <automated>node --test scripts/ci/verify_recut_candidate.mjs scripts/ci/collect_gate01_cohort.mjs scripts/ci/render_gate01_cohort.mjs scripts/ci/verify_gate01_cohort.mjs scripts/ci/collect_window_dispositions.mjs scripts/ci/render_window_dispositions.mjs scripts/ci/verify_window_dispositions.mjs</automated>
    <fails_when>non-zero exit, or any `node:test` case reported as `not ok` — a module named in a new CI step does not actually pass its own unit suite</fails_when>
  </verify>
  <acceptance_criteria>
    - All three new step titles appear verbatim in `.github/workflows/ci.yml`, inside the `docs-contracts-shift-left` job.
    - No non-comment line in `ci.yml` references `integration/v1.62-candidate` or `review/v1.62-candidate-code-only`.
    - Comparing `.github/workflows/ci.yml` against its pre-task committed revision with an explicit range (`git diff <pre-task-sha>..HEAD -- .github/workflows/ci.yml`) shows only added steps and comments — the header merge-blocking declaration and `annotation-sweep`'s `needs:` array are byte-unchanged.
    - Each new step's command list runs the module's unit suite and then its `--fixtures` invocation, in that order.
    - No new step invokes strict verification of a published capsule.
  </acceptance_criteria>
  <done>The three Phase 231 verifier suites run merge-blocking in `docs-contracts-shift-left`, depend on no local-only ref, and leave the declared job graph unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add scripts/ci/README.md evidence rows for every Phase 231 artifact</name>
  <files>scripts/ci/README.md</files>
  <read_first>
    - scripts/ci/README.md (the `## Phase 230 reviewable history integration` section around lines 149-165 — the three-part shape of a prose paragraph, an evidence table row with the exact verify command, and the splice-marker note)
    - .planning/phases/231-exact-sha-release-gate-proof/231-ROLLBACK-POINT.json
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-01-EVIDENCE.json
    - .planning/phases/231-exact-sha-release-gate-proof/231-GATE-02-EVIDENCE.ndjson
    - .planning/phases/231-exact-sha-release-gate-proof/231-WINDOW-DISPOSITIONS.json
  </read_first>
  <action>
Add a `## Phase 231 exact-SHA release gate proof` section to `scripts/ci/README.md`, cloning the three-part shape of the Phase 230 section directly above it.

Open with a prose paragraph answering, in under a minute, the maintainer's actual question: can this SHA ship, and what is still unproven. State plainly that the candidate was re-cut because the prior candidate predated its own gating verifiers, and that the GATE-02 proof is `workflow_dispatch`-class, distinct from the pull-request-class proof Phase 232 will produce for the same SHA — that conflation is the single most likely misreading six months out (D-17).

Add one evidence-table row per artifact, each with the exact reproducing command:
- `231-ROLLBACK-POINT.json` — what the re-cut changed and how to undo it — `node scripts/ci/verify_recut_candidate.mjs --repo . --record ... --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-shape --require-ancestry --require-revert-proof --require-toolchain --require-supersession`.
- `231-GATE-01-EVIDENCE.json` and its rendered Markdown — which declared merge-blocking gates ran in a clean checkout at the candidate SHA and with what exit codes — `node scripts/ci/verify_gate01_cohort.mjs --repo . --records ... --rendered ... --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-cohort-completeness --require-declaration-drift --require-clean-checkout --require-determinism`.
- `231-GATE-02-EVIDENCE.ndjson` and its rendered Markdown — which required GitHub Actions jobs ran for the exact SHA, under which event class, with which provider states — `node scripts/ci/verify_ci_baseline.mjs --records ... --rendered ... --expected-repository szTheory/accrue --expect-event-class workflow_dispatch --require-required-job-set --require-event-class --require-exit-codes`.
- `231-WINDOW-DISPOSITIONS.json` and its rendered Markdown — whether every former ship window is fixed or waived with owner, rationale, release impact, and current evidence — `node scripts/ci/verify_window_dispositions.mjs --repo . --records ... --rendered ... --candidate integration/v1.62-candidate --expected-repository szTheory/accrue --require-row-join --require-evidence-freshness --require-waiver-completeness --require-determinism`.

Close with a splice-marker note in the same form as the Phase 230 one, naming the `phase231-window-dispositions` and `phase231-gate01-cohort` marker pairs so Phase 232 can splice the rendered blocks into the integration PR body without re-deriving them.

Replace each `...` above with the real repository-relative artifact path. Use no absolute path anywhere in the section (D-31).
  </action>
  <verify>
    <automated>node --input-type=module -e "const fs=require('node:fs'); const s=fs.readFileSync('scripts/ci/README.md','utf8'); for (const n of ['Phase 231 exact-SHA release gate proof','231-ROLLBACK-POINT.json','231-GATE-01-EVIDENCE.json','231-GATE-02-EVIDENCE.ndjson','231-WINDOW-DISPOSITIONS.json','phase231-window-dispositions','phase231-gate01-cohort','workflow_dispatch']) { if (!s.includes(n)) { console.error('README missing: '+n); process.exit(1); } } if (/\/Users\/|\/home\/[a-z]/.test(s)) { console.error('README contains an absolute home path'); process.exit(1); }"</automated>
    <fails_when>non-zero exit, or stderr naming a missing README needle or reporting an absolute home path in the file</fails_when>
    <automated>node --input-type=module -e "const fs=require('node:fs'); const s=fs.readFileSync('scripts/ci/README.md','utf8'); const sec=s.slice(s.indexOf('Phase 231 exact-SHA release gate proof')); const paths=[...sec.matchAll(/\.planning\/phases\/231-exact-sha-release-gate-proof\/[A-Za-z0-9._-]+/g)].map(m=&gt;m[0]); const missing=[...new Set(paths)].filter(p=&gt;!fs.existsSync(p)); if (missing.length) { console.error('README references nonexistent artifacts: '+missing.join(', ')); process.exit(1); }"</automated>
    <fails_when>non-zero exit, or stderr listing a README-referenced Phase 231 artifact path that does not exist on disk</fails_when>
  </verify>
  <acceptance_criteria>
    - The README section exists with one table row per Phase 231 evidence artifact, four rows total.
    - Each row's command is runnable as written against the real committed artifact paths.
    - The prose paragraph states both the re-cut cause and that the GATE-02 proof is `workflow_dispatch`-class and distinct from a pull-request-class proof.
    - Both splice-marker pairs are named.
    - Every referenced artifact path exists on disk and is repository-relative.
  </acceptance_criteria>
  <done>A contributor can find every Phase 231 artifact, know what question it answers, and reproduce its proof from the README alone.</done>
</task>

<task type="auto">
  <name>Task 3: Mint 231-REPOSITORY-INVENTORY.json last</name>
  <files>.planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json</files>
  <read_first>
    - scripts/ci/collect_repository_inventory.mjs (`readShipWindows` around line 402, used around line 451 — it recomputes ship windows fresh at collection time, which is why a collect-plus-verify pair is self-consistent by construction)
    - scripts/ci/preserve_repository_state.sh (capsule and bundle minting; the 0644-versus-0600 bug recorded from Phase 229 plan 19)
    - scripts/ci/verify_repository_inventory.mjs (`directShipWindows` and `assertCompleteCategories`)
    - .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json (the frozen capsule that must not be re-diffed)
    - .planning/STATE.md
  </read_first>
  <precondition>All Phase 231 window-status edits have landed, `.planning/STATE.md` and `.planning/ROADMAP.md` reflect the phase's outcome, and every preceding plan's SUMMARY is committed — the capsule is minted after these settle or it invalidates itself on the next GSD step (D-28).</precondition>
  <action>
Mint a fresh `231-REPOSITORY-INVENTORY.json` following the one-capsule-per-phase pattern. Run `collect_repository_inventory.mjs` against the current repository state and write the result to `.planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json`.

Do not touch or re-diff the frozen Phase 229 or Phase 230 capsules.
Before making the first edit in this task, capture the pre-edit commit into the environment: `export PHASE_BASE="$(git rev-parse HEAD)"`. The frozen-artifact verify command uses it as an explicit diff range, so a frozen file modified and then committed is still caught.
 Their strict re-verification against the now-changed `.planning/WINDOWS.md` will legitimately report differences; that is expected point-in-time behavior, not a regression to chase (D-27, D-28).

Mint the capsule bundle through `scripts/ci/preserve_repository_state.sh`, then `chmod 600` the bundle before publish — `git bundle create` resets mode to 0644, which is the exact bug recorded from Phase 229 plan 19 (D-28).

Record in the inventory that two known ref additions are maintainer-authorized fail-forward carryovers from Phase 230, plus the newly-pushed `origin/integration/v1.62-candidate` from Plan 04, so the next strict verification of a published capsule has a documented reason for its `extra` rows rather than an unexplained one.

Do not create a git worktree at any point (D-12), and do not include any absolute path, home-directory reference, actor name, or adopter identifier in the emitted record (D-31).
  </action>
  <verify>
    <automated>node --test scripts/ci/collect_repository_inventory.mjs scripts/ci/verify_repository_inventory.mjs</automated>
    <fails_when>non-zero exit, or any `node:test` case reported as `not ok`</fails_when>
    <automated>node scripts/ci/verify_repository_inventory.mjs --fixtures --expected-repository szTheory/accrue --require-complete-categories --require-edge-cases --require-all-ref-recovery --require-typed-artifacts --require-privacy-controls --require-determinism --require-command-provenance</automated>
    <fails_when>non-zero exit, or a line beginning with a FAIL prefix naming an incomplete category, a missing edge case, or a determinism mismatch</fails_when>
    <automated>test -f .planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json &amp;&amp; BASE="${PHASE_BASE:?capture PHASE_BASE=$(git rev-parse HEAD) before the first edit of this task}" &amp;&amp; git diff --quiet -- .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json &amp;&amp; git diff --quiet "$BASE"..HEAD -- .planning/phases/229-repository-truth-recovery-safety/229-REPOSITORY-INVENTORY.json .planning/phases/230-reviewable-history-integration/230-ROLLBACK-POINT.json</automated>
    <fails_when>non-zero exit — the new capsule was not written, or a frozen Phase 229 or Phase 230 artifact was modified while minting it</fails_when>
    <automated>grep -nE '/Users/|/home/[a-z]|\$HOME' .planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json; test $? -eq 1</automated>
    <fails_when>non-zero exit, meaning `grep` found an absolute path or a home-directory reference in the minted capsule record</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/231-exact-sha-release-gate-proof/231-REPOSITORY-INVENTORY.json` exists and validates against the repository-inventory schema.
    - The capsule bundle's file mode is `600`, not `644`.
    - `git worktree list` prints the same row count before and after the task.
    - The frozen Phase 229 and Phase 230 capsules and records are byte-unchanged.
    - The inventory's ref set documents the two Phase 230 fail-forward carryovers and the newly-pushed `origin/integration/v1.62-candidate`, each with a recorded reason.
    - No absolute path or home-directory reference appears in the minted record.
  </acceptance_criteria>
  <done>A fresh Phase 231 capsule is minted after all planning state settled, its bundle is mode 600, the frozen predecessors are untouched, and the known extra refs carry documented reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new CI steps to GitHub runners | Steps added here execute on shared runners; a step depending on a local-only ref, a credential, or network state would fail or leak. |
| repository state to capsule record | Ref names, worktree rows, and paths cross into a committed, publishable capsule. |
| contributor documentation to public repository | README commands are copied and run by others; a wrong path or an absolute path misleads them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-29 | Denial of Service | New `docs-contracts-shift-left` steps | high | mitigate | Every new command is a hermetic fixture suite touching no network, credential, or external ref; a verify command asserts no non-comment `ci.yml` line references a local-only ref. |
| T-231-30 | Information Disclosure | `231-REPOSITORY-INVENTORY.json` and the capsule bundle | high | mitigate | The inventory collector's existing privacy controls plus a grep verify command rejecting absolute paths and home-directory references; the bundle is `chmod 600` before publish (D-28, D-31). |
| T-231-31 | Tampering | The declared merge-blocking job graph | high | mitigate | A verify command re-runs the required-job-set drift check after the CI edit and fails on any non-empty triple (D-18). |
| T-231-32 | Repudiation | README commands that do not reproduce their proof | medium | mitigate | A verify command asserts every README-referenced Phase 231 artifact path exists on disk. |
| T-231-33 | Tampering | Frozen 229/230 capsules amended during the new mint | high | mitigate | An explicit-range `git diff --quiet "$PHASE_BASE"..HEAD` verify command, paired with a working-tree `git diff --quiet`, fails the task if any frozen artifact changed (D-27, D-28). |
| T-231-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. The new CI steps invoke only `node --test` and existing repository scripts; no manifest or lockfile is touched. |
</threat_model>

<verification>
- All three new `ci.yml` step titles are present, and no non-comment line references a local-only ref.
- The required-job-set drift triple is still empty after the CI edit.
- Every module named in a new CI step passes its own `node --test` suite.
- Every README-referenced Phase 231 artifact path exists on disk.
- The new capsule exists, its bundle is mode 600, and the frozen predecessors are byte-unchanged.
</verification>

<success_criteria>
The Phase 231 verifiers are permanently merge-blocking, every artifact is discoverable and reproducible from `scripts/ci/README.md`, and the phase's capsule is minted last against settled state.
</success_criteria>

<output>
Create `.planning/phases/231-exact-sha-release-gate-proof/231-06-SUMMARY.md` when done.
</output>

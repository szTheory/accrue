---
phase: 25-admin-ux-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md
  - .planning/phases/25-admin-ux-inventory/README.md
autonomous: true
requirements:
  - INV-01
must_haves:
  truths:
    - "INV-01 lists every shipping live_session route for reference host mount /billing with LiveView module column."
    - "Non-LiveView GET asset routes appear under a labeled subsection; dev-only live routes documented from router.ex with allow_live_reload note."
  artifacts:
    - path: ".planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md"
      provides: "Mechanically verifiable route matrix aligned to examples/accrue_host"
---

<objective>
Replace the INV-01 stub with a complete **route matrix** for `accrue_admin` as mounted in `examples/accrue_host`: shipping `live_session` paths, hashed **asset** `GET` routes (non-LiveView subsection), and **dev-only** `live/4` routes from source (reference config uses `allow_live_reload: false`, so these will not appear in `mix phx.routes` output).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/25-admin-ux-inventory/25-CONTEXT.md
@.planning/phases/25-admin-ux-inventory/25-RESEARCH.md
@.planning/phases/25-admin-ux-inventory/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill INV-01 tables from router + mix phx.routes</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/router.ex
    - examples/accrue_host/lib/accrue_host_web/router.ex
    - .planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md
    - .planning/phases/25-admin-ux-inventory/25-CONTEXT.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md</files>
  <action>
    1. Set **Snapshot** line to `YYYY-MM-DD @` plus output of `git rev-parse --short HEAD` at commit time; set **Production method** to: `cd examples/accrue_host && mix phx.routes` (document filter: admin routes under `/billing`).
    2. Set **Scope statement** explicitly: **Include** non-LiveView `GET /billing/assets/*` rows (module `AccrueAdmin.Assets`, actions `:brand`, `:css`, `:js`) in subsection **Non-LiveView (Plug → Assets)**; **Exclude** `WS /live/websocket` and `GET|POST /live/longpoll` from the product matrix (footnote that they are Phoenix LiveView transport, not admin pages).
    3. **Shipping table:** For each `live/4` inside `live_session :accrue_admin` **outside** the `if dev_routes?` block in `router.ex`, add one row: **Admin-relative path** (e.g. `/customers`), **LiveView module** (e.g. `AccrueAdmin.Live.CustomersLive`), **Notes** (e.g. `:index` / `:show`). Mirror **host-absolute** paths as `/billing...` in a column or header note per CONTEXT D-02.
    4. **Dev-only table:** List paths `/dev/clock`, `/dev/email-preview`, `/dev/webhook-fixtures`, `/dev/components`, `/dev/fake-inspect` with modules from `router.ex` lines 76–80; header must state they exist **only** when host passes `allow_live_reload: true` (and reference host uses `false`).
    5. **Host mount reference** table: Example host mount `/billing`; `allow_live_reload: false` (cite `examples/accrue_host/lib/accrue_host_web/router.ex` line ~90).
    6. Remove stub lines `_TBD_` and the `> **Stub.**` block once replaced.
  </action>
  <acceptance_criteria>
    - `25-INV-01-route-matrix.md` contains the literal substring `Snapshot:` followed by a date and `@`
    - `25-INV-01-route-matrix.md` contains `AccrueAdmin.Live.DashboardLive` and `AccrueAdmin.Live.WebhookLive`
    - `25-INV-01-route-matrix.md` contains `AccrueAdmin.Assets` and the word `Non-LiveView` or `non-LiveView`
    - `25-INV-01-route-matrix.md` contains `allow_live_reload` and the word `false` in the host reference context
    - `rg '_TBD_' .planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md` exits with status 1 (no matches)
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && mix phx.routes</automated>
  </verify>
  <done>INV-01 route matrix populated.</done>
</task>

<task type="auto">
  <name>Task 2: README status line for INV-01 progress</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/README.md
    - .planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/README.md</files>
  <action>
    Under the title, add or update a single line: `**INV-01:** complete (route matrix)` or equivalent once Task 1 is done; keep read order list accurate.
  </action>
  <acceptance_criteria>
    - `README.md` contains `INV-01` and either `complete` or `done` (case-insensitive)
  </acceptance_criteria>
  <verify>
    <automated>grep -iE 'INV-01.*(complete|done)' .planning/phases/25-admin-ux-inventory/README.md</automated>
  </verify>
  <done>README reflects INV-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs | Markdown must not embed live API keys or customer PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | I | INV-01 markdown | mitigate | Document public route paths only; no env secrets |
</threat_model>

<verification>
`rg '_TBD_' .planning/phases/25-admin-ux-inventory/25-INV-01-route-matrix.md` returns no matches; `cd examples/accrue_host && mix phx.routes` includes each shipping `/billing/` LiveView path listed in INV-01.
</verification>

<success_criteria>
INV-01 is no longer a stub; README notes INV-01 completion.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-ux-inventory/25-01-SUMMARY.md`
</output>

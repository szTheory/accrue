---
phase: 25-admin-ux-inventory
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
  - .planning/phases/25-admin-ux-inventory/README.md
autonomous: true
requirements:
  - INV-03
must_haves:
  truths:
    - "INV-03 clause rows cite 20/21 UI-SPEC headings with Status, Evidence, and Partial gap + target phase when applicable."
    - "Surface rollup reflects worst status with links to clause section."
  artifacts:
    - path: ".planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md"
      provides: "Traceability from specs to implementation evidence"
---

<objective>
Populate **INV-03** with the **two-level** alignment model from `25-CONTEXT.md` D-04: primary **clause rows** pointing into `20-UI-SPEC.md` and `21-UI-SPEC.md`, plus **surface rollup** (dashboard, money indexes, detail pages, webhooks, step-up).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/25-admin-ux-inventory/25-CONTEXT.md
@.planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md
@.planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
@.planning/phases/25-admin-ux-inventory/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract obligation rows from UI-SPECs</name>
  <read_first>
    - .planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md
    - .planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
    - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
    - .planning/phases/25-admin-ux-inventory/25-CONTEXT.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</files>
  <action>
    1. For each major **obligation** in 20-UI-SPEC and 21-UI-SPEC (use `##` / `###` headings as spec pointers, e.g. `21-UI-SPEC.md — ## Money index pages`), add one **clause row**: Spec pointer | Status (`Aligned` \| `Partial` \| `N/A`) | Scope tags | Evidence (test file path from `accrue_admin/test`, `examples/accrue_host/test`, or `examples/accrue_host/e2e`, or `—` with reason) | Owner (`maintainer` or `TBD`) | Gap + target phase (mandatory when Partial).
    2. Fill **Surface rollup** table: rows for Dashboard, Money indexes, Detail pages, Webhooks, Step-up — **Governing spec pointers** column lists which spec sections apply; **Worst status** is max severity of underlying clauses; **Link to clauses** references anchor text or row IDs you used in clause table.
    3. Set **Snapshot** line (same SHA/date as INV-01/02 when single commit).
    4. Remove `_TBD_` and stub banner.
  </action>
  <acceptance_criteria>
    - `25-INV-03-spec-alignment.md` contains the substring `20-UI-SPEC.md`
    - `25-INV-03-spec-alignment.md` contains the substring `21-UI-SPEC.md`
    - `25-INV-03-spec-alignment.md` contains at least one of: `Aligned`, `Partial`, `N/A` as status values
    - `rg '_TBD_' .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md` exits with status 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md && test -f .planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md</automated>
  </verify>
  <done>INV-03 clause and rollup tables populated.</done>
</task>

<task type="auto">
  <name>Task 2: README INV-03 + phase status</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/README.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/README.md</files>
  <action>
    Mark INV-03 complete; if all INV-01..03 are complete, set top **Status:** line to `INV deliverables complete` or similar.
  </action>
  <acceptance_criteria>
    - `grep -iE 'INV-03.*(complete|done)' .planning/phases/25-admin-ux-inventory/README.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -iE 'INV-03.*(complete|done)' .planning/phases/25-admin-ux-inventory/README.md</automated>
  </verify>
  <done>README reflects INV-03.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | I | INV-03 | mitigate | Evidence column lists repo-relative test paths only |
</threat_model>

<verification>
No `_TBD_` in INV-03; at least 5 clause rows with distinct spec pointers; rollup table has all five surfaces from stub (Dashboard, Money indexes, Detail pages, Webhooks, Step-up) with non-placeholder Worst status.
</verification>

<success_criteria>
INV-03 enables Phase 26–29 to grep obligations without opening full UI-SPECs first.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-ux-inventory/25-03-SUMMARY.md`
</output>

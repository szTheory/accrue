---
phase: 26-hierarchy-and-pattern-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/live/customers_live.ex
  - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
  - accrue_admin/lib/accrue_admin/live/invoices_live.ex
  - accrue_admin/lib/accrue_admin/live/charges_live.ex
  - accrue_admin/test/accrue_admin/live/customers_live_test.exs
  - accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs
  - accrue_admin/test/accrue_admin/live/invoices_live_test.exs
  - accrue_admin/test/accrue_admin/live/charges_live_test.exs
  - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Four money index LiveViews share list-row signal placement and chip typography per 26-UI-SPEC and 21-UI-SPEC."
    - "ExUnit tests assert structure or stable class needles for row signals on each index."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/customers_live.ex"
      provides: "Reference-aligned billing signal row"
---

<objective>
Deliver **UX-01**: normalize **money index** list rows (`CustomersLive`, `SubscriptionsLive`, `InvoicesLive`, `ChargesLive`) for column/signal placement and **14px semibold chip** scale on touched signals, matching `26-UI-SPEC.md` § List rows and Phase 21 two-signal rules.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-hierarchy-and-pattern-alignment/26-CONTEXT.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-RESEARCH.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-UI-SPEC.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customers index — chip typography + signal cluster</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/test/accrue_admin/live/customers_live_test.exs
    - .planning/phases/26-hierarchy-and-pattern-alignment/26-UI-SPEC.md
    - .planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/customers_live.ex, accrue_admin/test/accrue_admin/live/customers_live_test.exs</files>
  <action>
    1. In `billing_signals_cell/1`, replace chip classes so **neither** chip uses `ax-text-12` on touched surfaces; use the **label** chip pattern defined in `theme.css` / existing admin utilities (e.g. align with `26-UI-SPEC.md` typography table: 14px semibold for list signal chips — cite the exact `class=` string you choose, such as `ax-chip ax-text-label` or equivalent existing utility **already present** in `theme.css` or shared CSS; if none exists, add a **semantic** utility in `theme.css` first then reference it here).
    2. Ensure the two chips remain in one trailing cluster with **two** `span` elements (ownership + tax) and preserve existing text content from `BillingPresentation` helpers.
    3. Extend `customers_live_test.exs` with at least one assertion that rendered HTML **does not** contain the substring `ax-text-12` inside the billing signals region **or** documents the replacement class explicitly via `assert html =~ "ax-chip` for the new token (pick one verifiable needle).
  </action>
  <acceptance_criteria>
    - `rg "ax-text-12" accrue_admin/lib/accrue_admin/live/customers_live.ex` exits with status 1 OR only appears outside `billing_signals_cell` (document in task done if exception)
    - `mix test test/accrue_admin/live/customers_live_test.exs` exits 0
    - `customers_live.ex` still contains literal substring `billing_signals_cell`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/customers_live_test.exs</automated>
  </verify>
  <done>Customers index row signals aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Subscriptions index — parity with customers row pattern</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/subscriptions_live.ex, accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs</files>
  <action>
    1. Locate the render function or cell helper that emits **tax/ownership (or equivalent) list signals** for subscription rows.
    2. Apply the **same** chip class pattern used after Task 1 in `customers_live.ex` (copy the exact `class=` attribute string for chips).
    3. Add or extend `subscriptions_live_test.exs` so a `live/2` test renders the index and asserts HTML contains the new chip class substring and still contains a stable existing heading such as `Subscriptions` or `ax-eyebrow` text from the template.
  </action>
  <acceptance_criteria>
    - `mix test test/accrue_admin/live/subscriptions_live_test.exs` exits 0
    - `subscriptions_live.ex` contains the same chip `class=` substring as `customers_live.ex` for signal chips (grep both files for that substring; both match)
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscriptions_live_test.exs</automated>
  </verify>
  <done>Subscriptions index signals aligned.</done>
</task>

<task type="auto">
  <name>Task 3: Invoices + Charges indexes — parity</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/invoices_live.ex
    - accrue_admin/lib/accrue_admin/live/charges_live.ex
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/test/accrue_admin/live/invoices_live_test.exs
    - accrue_admin/test/accrue_admin/live/charges_live_test.exs
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/invoices_live.ex, accrue_admin/lib/accrue_admin/live/charges_live.ex, accrue_admin/test/accrue_admin/live/invoices_live_test.exs, accrue_admin/test/accrue_admin/live/charges_live_test.exs</files>
  <action>
    1. For each file, align signal chip markup with `customers_live.ex` (same `class=` pattern as Task 1).
    2. If a LiveView has no dual-signal column, add a test comment in the test file explaining **N/A** and assert single outer `ax-page` instead; otherwise add chip-class assertion like subscriptions.
    3. Run both test files successfully.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs test/accrue_admin/live/charges_live_test.exs` exits 0
    - `rg "ax-text-12" accrue_admin/lib/accrue_admin/live/invoices_live.ex accrue_admin/lib/accrue_admin/live/charges_live.ex` exits 1 OR only matches outside list-signal cells (if match remains, file must contain comment `# UX-01: ax-text-12 intentional` with rationale)
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs test/accrue_admin/live/charges_live_test.exs</automated>
  </verify>
  <done>Invoices and charges indexes aligned.</done>
</task>

<task type="auto">
  <name>Task 4: INV-03 — mark money index UX-01 evidence</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
    - .planning/phases/26-hierarchy-and-pattern-alignment/26-CONTEXT.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</files>
  <action>
    1. In `25-INV-03-spec-alignment.md`, update rows covering **money index list shells** (customers/subscriptions/invoices/charges) to **Aligned** (or keep **Partial** with explicit next action) and add **Evidence** bullets pointing to the four `*_live.ex` paths and the test file paths touched in Tasks 1–3.
    2. Do not remove clause IDs; append a dated note `2026-04-20 Phase 26 plan 01` in the evidence cell or adjacent list per existing table style.
  </action>
  <acceptance_criteria>
    - `25-INV-03-spec-alignment.md` contains the substring `Phase 26` or `26-01`
    - `25-INV-03-spec-alignment.md` contains at least one path under `accrue_admin/lib/accrue_admin/live/` with `customers` or `CustomersLive`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 26|26-01" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</automated>
  </verify>
  <done>INV-03 traceability updated for UX-01 wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTML | Templates must not echo raw webhook bodies or secrets into new debug markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | I | List row HTML | mitigate | Keep escaping via `Phoenix.HTML.html_escape` / `raw` only on already-escaped fragments; do not bypass escaping |

## Security Notes

- No auth or routing changes in this plan; markup-only. Preserve existing `BillingPresentation` escaping patterns.
</threat_model>

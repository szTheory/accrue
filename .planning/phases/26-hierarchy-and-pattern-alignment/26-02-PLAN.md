---
phase: 26-hierarchy-and-pattern-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - accrue_admin/lib/accrue_admin/live/customer_live.ex
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/lib/accrue_admin/live/invoice_live.ex
  - accrue_admin/lib/accrue_admin/live/charge_live.ex
  - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
  - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
  - accrue_admin/test/accrue_admin/live/charge_live_test.exs
  - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "Money detail LiveViews use exactly one top-level ax-page section inside AppShell; KPI blocks use ax-kpi-grid; no inner ax-page used as card chrome."
    - "Tests prove single ax-page or equivalent spec from 26-UI-SPEC on each detail screen."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      provides: "Canonical fix for nested ax-page (if present)"
---

<objective>
Deliver **UX-02**: align **money detail** LiveViews with `26-UI-SPEC.md` nesting — one outer `ax-page`, KPI region via `ax-kpi-grid` / `KpiCard`, remove inner `ax-page` / duplicate `ax-page-header` stacks that violate Phase 20 rules.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-hierarchy-and-pattern-alignment/26-CONTEXT.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-RESEARCH.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-UI-SPEC.md
@.planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subscription detail — remove inner ax-page chrome</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
    - .planning/phases/26-hierarchy-and-pattern-alignment/26-UI-SPEC.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex, accrue_admin/test/accrue_admin/live/subscription_live_test.exs</files>
  <action>
    1. In `render/1`, locate the **inner** `~s(<div class="ax-page">` or `<div class="ax-page">` block nested under the primary `<section class="ax-page">` (approximately lines 172–180 region per current tree — re-read file for exact line numbers).
    2. Replace that inner wrapper with **`ax-card`** (or a neutral `div` with **no** `ax-page` class) so **only one** `ax-page` remains in the LiveView render tree inside `AppShell`.
    3. Collapse duplicate `ax-page-header` fragments where they only existed to mimic page chrome; preserve visible headings with `ax-heading` / existing typography classes per `26-UI-SPEC.md`.
    4. Add to `subscription_live_test.exs` a test that mounts a successful detail view and asserts `Regex.scan(~r/class=\"ax-page\"/, html) |> length() == 1` **or** uses `LazyHTML` to count elements with attribute `class` containing `ax-page` equal to 1 (choose one approach and document in assertion comment).
  </action>
  <acceptance_criteria>
    - `mix test test/accrue_admin/live/subscription_live_test.exs` exits 0
    - `rg -c 'class=\"ax-page\"' accrue_admin/lib/accrue_admin/live/subscription_live.ex` outputs `1` OR `2` only if second occurrence is in a comment string (must not be duplicate live markup — prefer exactly **1** occurrence of `class="ax-page"` in HEEx)
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs</automated>
  </verify>
  <done>Subscription detail hierarchy normalized.</done>
</task>

<task type="auto">
  <name>Task 2: Customer + Invoice + Charge detail — audit and fix nesting</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex
    - accrue_admin/lib/accrue_admin/live/charge_live.ex
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/test/accrue_admin/live/customer_live_test.exs
    - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
    - accrue_admin/test/accrue_admin/live/charge_live_test.exs
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/customer_live.ex, accrue_admin/lib/accrue_admin/live/invoice_live.ex, accrue_admin/lib/accrue_admin/live/charge_live.ex, accrue_admin/test/accrue_admin/live/customer_live_test.exs, accrue_admin/test/accrue_admin/live/invoice_live_test.exs, accrue_admin/test/accrue_admin/live/charge_live_test.exs</files>
  <action>
    1. For each of the three modules, run `rg 'class=\"ax-page\"' <file>`; if more than **one** HEEx occurrence exists, apply the same structural fix pattern as Task 1 (inner chrome → `ax-card` / neutral wrapper).
    2. If a file already has exactly one `ax-page`, add a **comment-only** note in the test file `# UX-02: single ax-page verified {date}` and a regression test that counts `ax-page` == 1 using the same technique as Task 1.
    3. Ensure KPI sections remain **`ax-kpi-grid`** where the screen shows numeric summaries at top.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs` exits 0
    - Each of the three `*_live.ex` files contains at most **one** literal `class="ax-page"` string in active HEEx (count via `rg -n 'class=\"ax-page\"' FILE` — max 1 line per file)
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs</automated>
  </verify>
  <done>Customer, invoice, charge detail hierarchies verified.</done>
</task>

<task type="auto">
  <name>Task 3: INV-03 — money detail UX-02 evidence</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</files>
  <action>
    Update clause rows for **money detail** surfaces to **Aligned** (or documented Partial) with evidence paths to the four detail LiveViews and tests; include marker `26-02` in the note text.
  </action>
  <acceptance_criteria>
    - `rg "26-02" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md` finds at least one match
    - File still contains substring `21-UI-SPEC` or `20-UI-SPEC` (preserve upstream traceability headings)
  </acceptance_criteria>
  <verify>
    <automated>rg "26-02" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</automated>
  </verify>
  <done>INV-03 updated for UX-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step-up / destructive actions | Markup changes must not alter event names or `StepUp` wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T | Action modals | mitigate | Do not rename `handle_event` strings; DOM-only refactors |

## Security Notes

- Preserve `StepUpAuthModal` and `@destructive_actions` behavior; visual hierarchy only.
</threat_model>

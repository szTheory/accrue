---
phase: 26-hierarchy-and-pattern-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/lib/accrue_admin/live/webhook_live.ex
  - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
  - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "Webhook list and detail tables use the same body/label typography tokens as money index tables (`ax-body`, shared table spacing)."
    - "Tests assert stable class needles or compare class sets against a money index reference string documented in test."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      provides: "Webhook index density aligned to money lists"
---

<objective>
Deliver **UX-03**: align **webhook** index + detail with money-list **typographic and density rhythm** per `26-UI-SPEC.md` § Webhooks and § Typography.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-hierarchy-and-pattern-alignment/26-UI-SPEC.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-RESEARCH.md
@.planning/phases/21-admin-and-host-ux-proof/21-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Webhooks index — table typography parity</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/webhooks_live.ex, accrue_admin/test/accrue_admin/live/webhooks_live_test.exs</files>
  <action>
    1. Compare `DataTable` column definitions and **table cell** wrapper classes in `webhooks_live.ex` to `customers_live.ex` (reference: body text uses `ax-body` on descriptions; table headers use label scale per existing money tables).
    2. Remove webhook-specific **smaller-than-default** body classes (e.g. any `text-xs`, `ax-text-12`, or custom `font-size` on table cells) unless required for density with documented exception — prefer `ax-body` for cell copy and `ax-label` / existing header classes for column headers matching customers.
    3. Extend `webhooks_live_test.exs` with `assert html =~ "ax-body"` **or** assert absence of a removed orphan class (e.g. if you remove `ax-text-12` from table body, `refute html =~ "ax-text-12"` for the table region — pick a **grep-verifiable** rule tied to your edit).
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/webhooks_live_test.exs` exits 0
    - `webhooks_live.ex` contains substring `DataTable` OR `data_table` (preserve table component usage)
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhooks_live_test.exs</automated>
  </verify>
  <done>Webhooks index typography aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook detail — typography parity</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex
    - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/webhook_live.ex, accrue_admin/test/accrue_admin/live/webhook_live_test.exs</files>
  <action>
    1. Align KPI grid and inspector sections to reuse the same **heading/body** classes as invoice detail for equivalent regions (e.g. `ax-heading`, `ax-body`, `ax-kpi-grid`).
    2. Ensure **no** new 12px chip scale on touched surfaces; chips use the same pattern as post–plan-01 money indexes if chips appear.
    3. Update `webhook_live_test.exs` with at least one new assertion for a class string you introduce or normalize (must be substring-verifiable).
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs` exits 0
    - `webhook_live.ex` contains substring `ax-kpi-grid`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs</automated>
  </verify>
  <done>Webhook detail typography aligned.</done>
</task>

<task type="auto">
  <name>Task 3: INV-03 — webhooks UX-03 evidence</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</files>
  <action>
    Update webhook-related clause rows to **Aligned** with evidence pointing to `webhooks_live.ex`, `webhook_live.ex`, and tests; include marker `26-03`.
  </action>
  <acceptance_criteria>
    - `rg "26-03" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md` exits 0
    - Same file contains `WebhooksLive` or `webhooks_live.ex`
  </acceptance_criteria>
  <verify>
    <automated>rg "26-03|WebhooksLive|webhooks_live" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</automated>
  </verify>
  <done>INV-03 updated for UX-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook payload display | Do not log or expand raw secret material in new markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | I | Webhook HTML | mitigate | Keep existing redaction / viewer components unchanged |

## Security Notes

- CSS/class changes only; no changes to `Webhooks.detail` authorization.
</threat_model>

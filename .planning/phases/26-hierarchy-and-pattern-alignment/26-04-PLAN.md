---
phase: 26-hierarchy-and-pattern-alignment
plan: 04
type: execute
wave: 4
depends_on:
  - 26-03
files_modified:
  - accrue_admin/assets/css/theme.css
  - .planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions.md
  - .planning/REQUIREMENTS.md
  - accrue_admin/guides/admin_ui.md
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - "New color on Phase 26–touched HEEx/CSS flows through semantic variables in theme.css or a documented row in 26-theme-exceptions.md."
    - "REQUIREMENTS.md UX-01..UX-04 checkboxes updated to reflect shipped alignment (at minimum UX-04 scope for this plan’s token pass)."
  artifacts:
    - path: ".planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions.md"
      provides: "Exception registry for literals"
---

<objective>
Deliver **UX-04**: **theme token discipline** on all files touched by plans **26-01..26-03** — eliminate ad hoc `#RRGGBB` / non-semantic literals where feasible; register unavoidable exceptions in `26-theme-exceptions.md`; document adopters in `guides/admin_ui.md`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-hierarchy-and-pattern-alignment/26-CONTEXT.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions.md
@.planning/phases/26-hierarchy-and-pattern-alignment/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory literals on touched LiveViews + CSS</name>
  <read_first>
    - accrue_admin/assets/css/theme.css
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
    - accrue_admin/lib/accrue_admin/live/invoices_live.ex
    - accrue_admin/lib/accrue_admin/live/charges_live.ex
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex
    - accrue_admin/lib/accrue_admin/live/charge_live.ex
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex
  </read_first>
  <files>accrue_admin/assets/css/theme.css, .planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions.md</files>
  <action>
    1. Run `rg '#[0-9A-Fa-f]{3,8}' accrue_admin/lib/accrue_admin/live/{customers,subscriptions,invoices,charges,customer,subscription,invoice,charge,webhooks,webhook}_live.ex` and list each match (3–8 hex digits after `#`).
    2. For each **new or remaining** literal in those paths after prior plans, either: (a) replace with `var(--ax-…)` or existing semantic utility class, **or** (b) add a row to `26-theme-exceptions.md` table with columns Location / Selector / Rationale / Link filled (Link may be `—`).
    3. If you introduce a **new** semantic token, add it to `theme.css` with a comment `/* Phase 26 UX-04 */` above the block.
  </action>
  <acceptance_criteria>
    - `26-theme-exceptions.md` contains the substring `Phase 26` or `UX-04` in at least one table row **or** documents `*No exceptions*` remains true with a dated note under the table if zero literals remain
    - `theme.css` contains substring `Phase 26` **or** file has no edits and task done comment explains no new tokens were needed
  </acceptance_criteria>
  <verify>
    <automated>rg "Phase 26|UX-04" .planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions.md accrue_admin/assets/css/theme.css</automated>
  </verify>
  <done>Literal inventory and registry updated.</done>
</task>

<task type="auto">
  <name>Task 2: Admin UI guide — pointer to exception registry</name>
  <read_first>
    - accrue_admin/guides/admin_ui.md
    - .planning/phases/26-hierarchy-and-pattern-alignment/26-CONTEXT.md
  </read_first>
  <files>accrue_admin/guides/admin_ui.md</files>
  <action>
    Add a short subsection **Theming and exceptions** (or extend existing theming section) with 2–4 sentences stating: semantic tokens live in `assets/css/theme.css`; non-token literals used in admin HEEx must be listed in `.planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions.md` during Phase 26 alignment work.
  </action>
  <acceptance_criteria>
    - `accrue_admin/guides/admin_ui.md` contains the literal path substring `26-theme-exceptions.md`
    - Same file contains substring `theme.css`
  </acceptance_criteria>
  <verify>
    <automated>rg "26-theme-exceptions.md|theme.css" accrue_admin/guides/admin_ui.md</automated>
  </verify>
  <done>Guide updated.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md — UX checkboxes for Phase 26</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/26-hierarchy-and-pattern-alignment/26-CONTEXT.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    1. After confirming plans **26-01..26-03** are implemented and this plan’s token pass is complete, set markdown checkboxes for **UX-01, UX-02, UX-03, UX-04** from `[ ]` to `[x]` in the bullet list at the top of `REQUIREMENTS.md` **only if** each requirement’s acceptance is met; if any item is intentionally deferred, leave `[ ]` and add `<!-- deferred: reason -->` on the same line (max one line per REQ).
    2. Update the traceability table rows for UX-01..04 **Status** column from `Pending` to `Complete` when all four are satisfied.
  </action>
  <acceptance_criteria>
    - `REQUIREMENTS.md` contains at least one `[x]` in the UX-01..UX-04 bullet block after execution OR contains `deferred:` comment with explicit reason
    - `rg "UX-01" .planning/REQUIREMENTS.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg "UX-0[1-4]" .planning/REQUIREMENTS.md | head -20</automated>
  </verify>
  <done>Requirements checkboxes reflect phase exit state.</done>
</task>

<task type="auto">
  <name>Task 4: Full accrue_admin test suite</name>
  <read_first>
    - accrue_admin/mix.exs
  </read_first>
  <files></files>
  <action>
    Run `cd accrue_admin && mix test` and fix any regressions from token or guide edits (if none, no file changes).
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test</automated>
  </verify>
  <done>Package suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs | Do not paste secrets into `26-theme-exceptions.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | I | Theme docs | mitigate | Document only CSS paths and public variable names |

## Security Notes

- No runtime config changes; CSS and markdown only.
</threat_model>

---
phase: 27-microcopy-and-operator-strings
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01-PLAN.md"
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/copy/locked.ex
  - accrue_admin/lib/accrue_admin/live/customer_live.ex
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/lib/accrue_admin/live/invoice_live.ex
  - accrue_admin/lib/accrue_admin/live/charge_live.ex
  - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
  - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
  - accrue_admin/test/accrue_admin/live/charge_live_test.exs
  - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
autonomous: true
requirements:
  - COPY-02
  - COPY-03
must_haves:
  truths:
    - "Money detail LiveViews source operator flash strings from `AccrueAdmin.Copy` / `AccrueAdmin.Copy.Locked` — no duplicate literals for the same semantic message."
    - "`Copy.Locked.owner_access_denied/0` returns exactly: You don't have access to billing for this organization."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy/locked.ex"
      provides: "Shared denial literal for scoped detail mounts"
---

<objective>
Deliver **COPY-02** and extend **COPY-03** for **money detail** surfaces: consolidate `@owner_access_denied` and repeated operator flash strings into `AccrueAdmin.Copy` / `AccrueAdmin.Copy.Locked`, migrate `CustomerLive`, `SubscriptionLive`, `InvoiceLive`, and `ChargeLive` flash + inline empty-copy paragraphs to those functions (verbatim where already spec-stable; Tier A tone for customer detail empty lines per CONTEXT D-02), and extend ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-microcopy-and-operator-strings/27-CONTEXT.md
@.planning/phases/27-microcopy-and-operator-strings/27-RESEARCH.md
@.planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Copy.Locked + detail Copy functions</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex
    - accrue_admin/lib/accrue_admin/live/charge_live.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy/locked.ex, accrue_admin/lib/accrue_admin/copy.ex</files>
  <action>
    1. Create `accrue_admin/lib/accrue_admin/copy/locked.ex` with `def owner_access_denied/0` returning exactly `You don't have access to billing for this organization.` (including trailing period).
    2. In `copy.ex`, add **verbatim** detail helpers (exact English below):
       - `subscription_select_action_warning/0` → `"Select an action before confirming."`
       - `subscription_action_recorded_info/0` → `"Subscription action recorded."`
       - `invoice_select_action_warning/0` → `"Select an invoice action before confirming."`
       - `invoice_pdf_open_info/0` → `"Open PDF now uses the shared invoice render path."`
       - `invoice_action_recorded_info/0` → `"Invoice action recorded."`
       - `payment_processor_action_warning/1` — takes `payment_intent` (or any term); returns `"Processor requires action: " <> inspect(payment_intent)` (same prefix and spacing as current LiveViews).
       - `charge_prepare_refund_warning/0` → `"Prepare a refund before confirming."`
       - `charge_refund_created_info/0` → `"Refund created with fee-aware fields from the billing facade."`
       - `customer_detail_no_subscriptions/0` → `"No subscriptions for this customer yet."`
       - `customer_detail_no_invoices/0` → `"No invoices for this customer yet."`
    3. Run `mix format` on touched files.
  </action>
  <acceptance_criteria>
    - `test -f accrue_admin/lib/accrue_admin/copy/locked.ex` exits 0
    - `rg "def owner_access_denied" accrue_admin/lib/accrue_admin/copy/locked.ex` exits 0
    - `rg "No invoices projected yet" accrue_admin/lib/accrue_admin/copy.ex` exits 1
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Copy helpers for money detail exist.</done>
</task>

<task type="auto">
  <name>Task 2: SubscriptionLive + CustomerLive — Locked + flashes + empty lines</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/copy/locked.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex, accrue_admin/lib/accrue_admin/live/customer_live.ex</files>
  <action>
    1. Remove `@owner_access_denied` module attribute from `subscription_live.ex`; replace `put_flash(:error, @owner_access_denied)` with `put_flash(:error, AccrueAdmin.Copy.Locked.owner_access_denied())` (add `alias AccrueAdmin.Copy.Locked` as needed).
    2. Replace `push_flash(socket, :warning, "Select an action before confirming.")` with `push_flash(socket, :warning, AccrueAdmin.Copy.subscription_select_action_warning())`.
    3. Replace `push_flash(:info, "Subscription action recorded.")` with `Copy.subscription_action_recorded_info()` (after `alias AccrueAdmin.Copy`).
    4. Replace `push_flash(socket, :warning, "Processor requires action: #{inspect(payment_intent)}")` with `push_flash(socket, :warning, AccrueAdmin.Copy.payment_processor_action_warning(payment_intent))` in `subscription_live.ex` wherever that literal pattern appears.
    5. In `customer_live.ex`, replace `put_flash(:error, @owner_access_denied)` with `Copy.Locked.owner_access_denied()` and remove `@owner_access_denied` attribute.
    6. Replace inline HEEx copy `No subscriptions for this customer.` with `Copy.customer_detail_no_subscriptions()` and `No invoices projected yet.` with `Copy.customer_detail_no_invoices()` (use function call form `<%= AccrueAdmin.Copy.customer_detail_no_subscriptions() %>` or local alias — consistent with file style).
  </action>
  <acceptance_criteria>
    - `rg "@owner_access_denied" accrue_admin/lib/accrue_admin/live/subscription_live.ex accrue_admin/lib/accrue_admin/live/customer_live.ex` exits 1
    - `rg "No invoices projected yet" accrue_admin/lib/accrue_admin/live/customer_live.ex` exits 1
    - `rg "Select an action before confirming" accrue_admin/lib/accrue_admin/live/subscription_live.ex` exits 1
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Customer + subscription detail copy centralized.</done>
</task>

<task type="auto">
  <name>Task 3: InvoiceLive + ChargeLive — flash migration</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex
    - accrue_admin/lib/accrue_admin/live/charge_live.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/invoice_live.ex, accrue_admin/lib/accrue_admin/live/charge_live.ex</files>
  <action>
    1. In `invoice_live.ex`, replace flash string literals listed in plan 02 Task 1 with the corresponding `AccrueAdmin.Copy.*` functions (`invoice_select_action_warning/0`, `invoice_pdf_open_info/0`, `invoice_action_recorded_info/0`, `payment_processor_action_warning/1`).
    2. In `charge_live.ex`, replace `"Prepare a refund before confirming."` and `"Refund created with fee-aware fields from the billing facade."` with `Copy.charge_prepare_refund_warning/0` and `Copy.charge_refund_created_info/0`.
    3. Leave `inspect(reason)` / `inspect(payment_intent)` **error** paths structurally unchanged except where replaced by Task 1 helpers; do not introduce `raw/1`.
  </action>
  <acceptance_criteria>
    - `rg "Select an invoice action before confirming" accrue_admin/lib/accrue_admin/live/invoice_live.ex` exits 1
    - `rg "Prepare a refund before confirming" accrue_admin/lib/accrue_admin/live/charge_live.ex` exits 1
    - `rg "Refund created with fee-aware" accrue_admin/lib/accrue_admin/live/charge_live.ex` exits 1
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Invoice and charge detail flashes use Copy.</done>
</task>

<task type="auto">
  <name>Task 4: ExUnit coverage for detail copy</name>
  <read_first>
    - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
    - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
    - accrue_admin/test/accrue_admin/live/charge_live_test.exs
    - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/subscription_live_test.exs, accrue_admin/test/accrue_admin/live/invoice_live_test.exs, accrue_admin/test/accrue_admin/live/charge_live_test.exs, accrue_admin/test/accrue_admin/live/customer_live_test.exs</files>
  <action>
    1. Add at least **one** new assertion per touched LiveView file that proves a `Copy` or `Copy.Locked` string appears in rendered HTML **or** in `flash` after a simulated event (pick the lowest-friction pattern already used in each test file).
    2. For `customer_live_test.exs`, assert rendered HTML contains `No invoices for this customer yet.` after the HEEx change.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs test/accrue_admin/live/customer_live_test.exs` exits 0
    - `customer_live_test.exs` contains substring `No invoices for this customer yet.`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs test/accrue_admin/live/customer_live_test.exs</automated>
  </verify>
  <done>Tests pin Copy-backed detail strings.</done>
</task>

<task type="auto">
  <name>Task 5: INV-03 — money detail COPY evidence</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</files>
  <action>
    1. Append **Phase 27 plan 02** evidence to rows covering **money detail** operator copy (subscription/invoice/charge/customer detail) pointing to `copy.ex`, `copy/locked.ex`, the four `*_live.ex` files, and the four test files.
  </action>
  <acceptance_criteria>
    - `rg "27-02" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg -n "27-02" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</automated>
  </verify>
  <done>INV-03 updated for money detail copy wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin flash channel | Must not expose raw secrets or full webhook payloads in new copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | I | Error flashes using `inspect/1` | accept (baseline) | Keep existing `inspect(reason)` for true failures; do not expand logged detail in this plan |

## Security Notes

- `payment_processor_action_warning/1` preserves existing `inspect(payment_intent)` behavior — no broader exposure than today.
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/charge_live_test.exs test/accrue_admin/live/customer_live_test.exs`
</verification>

<success_criteria>
- COPY-02: money detail operator flashes and customer detail empty lines coordinated through `Copy` / `Copy.Locked` in one pass per this plan.
- COPY-03: tests assert stable integration surface for new helpers.
- INV-03 evidence updated for money detail rows.
</success_criteria>

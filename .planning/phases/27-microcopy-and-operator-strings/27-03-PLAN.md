---
phase: 27-microcopy-and-operator-strings
plan: 03
type: execute
wave: 3
depends_on:
  - "27-02-PLAN.md"
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/copy/locked.ex
  - accrue_admin/lib/accrue_admin/live/webhook_live.ex
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  - accrue_admin/README.md
  - accrue_admin/CHANGELOG.md
  - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
must_haves:
  truths:
    - "Webhook replay / denial / confirmation strings remain character-identical to pre-migration literals (Playwright + ExUnit contracts)."
    - "Webhooks index empty state uses Tier A voice without DLQ in the primary title."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy/locked.ex"
      provides: "Locked webhook replay literals (UI-SPEC / E2E contract)"
---

<objective>
Finish **COPY-01..COPY-03** for **webhook operator surfaces**: centralize locked replay/denial/success strings from `WebhookLive` and bulk-replay strings from `WebhooksLive` into `AccrueAdmin.Copy` / `AccrueAdmin.Copy.Locked` **verbatim**, refresh webhooks **index** empty-state copy (Tier A, no DLQ headline), document **copy tiers** in `accrue_admin/README.md`, add **`### Host-visible copy (accrue_admin)`** notes to `CHANGELOG.md`, update **INV-03** evidence, and keep **`accrue_admin` + host** tests green for unchanged literal assertions.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-microcopy-and-operator-strings/27-CONTEXT.md
@.planning/phases/27-microcopy-and-operator-strings/27-RESEARCH.md
@.planning/phases/20-organization-billing-with-sigra/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy + Locked — webhooks index + locked literals</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
    - accrue_admin/lib/accrue_admin/copy/locked.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy/locked.ex, accrue_admin/lib/accrue_admin/copy.ex</files>
  <action>
    1. Extend `AccrueAdmin.Copy` with:
       - `webhooks_index_empty_title/0` → `"No webhook deliveries for this organization yet"`
       - `webhooks_index_empty_copy/0` → `"Stripe events appear here after they are recorded for this organization. If you expected deliveries, check filters or confirm your endpoint is receiving traffic."`
       - `webhooks_bulk_replay_confirm_question/1` — takes integer `count`; returns **exactly** `"Replay #{count} failed or dead webhook rows for the active organization?"` (same wording and punctuation as current `bulk_replay_confirmation/1` in `webhooks_live.ex`, including the trailing `?`).
       - `webhooks_bulk_no_rows_warning/0` → `"No failed or dead-lettered webhook rows match the current filters."` (verbatim from `webhooks_live.ex` today).
    2. Extend `AccrueAdmin.Copy.Locked` with **verbatim** string functions mirroring current `webhook_live.ex` module attributes and `single_replay_confirmation/0`:
       - `ambiguous_replay_blocked/0` → `"Ownership couldn't be verified for this webhook. Replay is unavailable until the linked billing owner is resolved."`
       - `replay_success_organization/0` → `"Replay requested for the active organization."`
       - `replay_success_global_webhook/0` → `"Webhook replay requested."`
       - `replay_blocked/0` → `"Replay is blocked because this webhook isn't linked to a billable row in the active organization."`
       - `single_replay_confirmation/0` → `"Replay webhook for the active organization?"`
       - `bulk_replay_success_organization/0` → `"Replay requested for the active organization."` (same string as replay org success — one function may delegate to the other to avoid drift).
       - `bulk_replay_success_global/0` → `"Bulk replay requested"`
    3. Do **not** change English text of the strings above vs current source — grep host and package tests for these literals before editing.
  </action>
  <acceptance_criteria>
    - `rg "def single_replay_confirmation" accrue_admin/lib/accrue_admin/copy/locked.ex` exits 0
    - `rg "Replay webhook for the active organization\\?" accrue_admin/lib/accrue_admin/copy/locked.ex` exits 0
    - `rg "DLQ" accrue_admin/lib/accrue_admin/copy.ex` exits 1
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Locked + webhooks index copy helpers added.</done>
</task>

<task type="auto">
  <name>Task 2: WebhookLive — module attributes to Copy.Locked</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex
    - accrue_admin/lib/accrue_admin/copy/locked.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/webhook_live.ex</files>
  <action>
    1. Remove module attributes `@owner_access_denied`, `@ambiguous_replay_blocked`, `@replay_success`, `@global_replay_success`, `@replay_blocked` from `webhook_live.ex`.
    2. Replace every use with `AccrueAdmin.Copy.Locked` function calls (`owner_access_denied/0`, `ambiguous_replay_blocked/0`, `replay_success_organization/0`, `replay_success_global_webhook/0`, `replay_blocked/0`) per the mapping implied by current `@` names.
    3. Replace `single_replay_confirmation/0` private function body to delegate to `Copy.Locked.single_replay_confirmation/0`.
    4. Update `replay_success/1` private to return `Locked.replay_success_organization/0` when `%{mode: :organization}` and `Locked.replay_success_global_webhook/0` otherwise (preserve current branching).
  </action>
  <acceptance_criteria>
    - `rg "^\\s*@owner_access_denied|^\\s*@replay_blocked|^\\s*@replay_success" accrue_admin/lib/accrue_admin/live/webhook_live.ex` exits 1
    - `rg "Copy.Locked" accrue_admin/lib/accrue_admin/live/webhook_live.ex` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Webhook detail uses Locked for contract strings.</done>
</task>

<task type="auto">
  <name>Task 3: WebhooksLive — empty table + bulk replay messages</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/copy/locked.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/webhooks_live.ex</files>
  <action>
    1. Remove `@bulk_replay_success` and `@global_bulk_replay_success` attributes; use `Copy.Locked.bulk_replay_success_organization/0` and `Copy.Locked.bulk_replay_success_global/0` in `bulk_replay_success/1`.
    2. Replace `empty_title` / `empty_copy` on `DataTable` with `Copy.webhooks_index_empty_title/0` and `Copy.webhooks_index_empty_copy/0`.
    3. Replace inline flash string `"No failed or dead-lettered webhook rows match the current filters."` with `Copy.webhooks_bulk_no_rows_warning/0`.
    4. Replace inline replay-blocked flash with `Copy.Locked.replay_blocked/0`.
    5. Replace `bulk_replay_confirmation/1` body to delegate to `Copy.webhooks_bulk_replay_confirm_question/1`.
  </action>
  <acceptance_criteria>
    - `rg "No webhook rows matched" accrue_admin/lib/accrue_admin/live/webhooks_live.ex` exits 1
    - `rg "Adjust the DLQ filters" accrue_admin/lib/accrue_admin/live/webhooks_live.ex` exits 1
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Webhooks index copy migrated.</done>
</task>

<task type="auto">
  <name>Task 4: Tests — webhook LiveViews + host replay spec spot-check</name>
  <read_first>
    - accrue_admin/test/accrue_admin/live/webhook_live_test.exs
    - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
    - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/webhook_live_test.exs, accrue_admin/test/accrue_admin/live/webhooks_live_test.exs</files>
  <action>
    1. Run `cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs test/accrue_admin/live/webhooks_live_test.exs` and fix failures caused by import/alias only — **do not** change expected English literals in assertions unless the implementation accidentally drifted; if drifted, fix implementation to match Locked strings.
    2. If any assertion compared against removed `@` constants, switch to `AccrueAdmin.Copy.Locked.*` return values.
    3. Optional host check (if executor has Node): `cd examples/accrue_host && npx playwright test phase13-canonical-demo.spec.js -g "Replay webhook"` — run only when Playwright deps already installed; if not installed, skip with comment in SUMMARY.md later.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs test/accrue_admin/live/webhooks_live_test.exs` exits 0
    - `webhook_live_test.exs` still contains literal `Replay webhook for the active organization?`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs test/accrue_admin/live/webhooks_live_test.exs</automated>
  </verify>
  <done>Webhook copy migration is test-locked.</done>
</task>

<task type="auto">
  <name>Task 5: Contributor docs — copy tiers</name>
  <read_first>
    - accrue_admin/README.md
    - .planning/phases/27-microcopy-and-operator-strings/27-CONTEXT.md
  </read_first>
  <files>accrue_admin/README.md</files>
  <action>
    1. Add a short section **Operator copy (tiers)** to `accrue_admin/README.md` describing Tier A (host contract / `AccrueAdmin.Copy`), Tier B (library demo / `ComponentKitchenLive`), Tier C (dev-only) per CONTEXT D-03 — max ~15 lines.
  </action>
  <acceptance_criteria>
    - `rg "Operator copy" accrue_admin/README.md` exits 0
    - `rg "Tier A" accrue_admin/README.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Operator copy|Tier A" accrue_admin/README.md</automated>
  </verify>
  <done>README documents copy tiers.</done>
</task>

<task type="auto">
  <name>Task 6: CHANGELOG — host-visible copy subsection</name>
  <read_first>
    - accrue_admin/CHANGELOG.md
  </read_first>
  <files>accrue_admin/CHANGELOG.md</files>
  <action>
    1. Under the topmost version section (add `## [Unreleased]` at top if the project uses Keep-a-Changelog unreleased entries; otherwise append under current `0.2.0` **Unreleased**-style heading per repo convention), add markdown heading exactly `### Host-visible copy (accrue_admin)` and one bullet summarizing Phase 27 webhook + `Copy` consolidation for integrators grepping releases.
  </action>
  <acceptance_criteria>
    - `rg "### Host-visible copy \\(accrue_admin\\)" accrue_admin/CHANGELOG.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg "### Host-visible copy" accrue_admin/CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG calls out host-visible copy.</done>
</task>

<task type="auto">
  <name>Task 7: INV-03 — webhook COPY evidence</name>
  <read_first>
    - .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md
  </read_first>
  <files>.planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</files>
  <action>
    1. Append **Phase 27 plan 03** evidence for **webhook list/detail/replay** rows: paths `webhook_live.ex`, `webhooks_live.ex`, `copy/locked.ex`, `copy.ex`, and both test files.
  </action>
  <acceptance_criteria>
    - `rg "27-03" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg -n "27-03" .planning/phases/25-admin-ux-inventory/25-INV-03-spec-alignment.md</automated>
  </verify>
  <done>INV-03 webhook copy evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replay confirmation | Copy changes must not weaken replay gate semantics — strings are legal/UX contract, not authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | T | Locked replay strings | mitigate | Byte-for-byte parity with previous literals; tests unchanged |

## Security Notes

- Authorization remains in `Webhooks` / `DLQ` contexts — this plan moves literals only.
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/webhook_live_test.exs test/accrue_admin/live/webhooks_live_test.exs`
- `cd accrue_admin && mix test` (full package) before merge if CI runs full suite
</verification>

<success_criteria>
- Webhook operator strings centralized without breaking E2E/ExUnit literal contracts.
- Webhooks index empty state matches Tier A guidance (no DLQ in headline).
- COPY-03 satisfied: README + CHANGELOG document how hosts should treat copy changes.
- INV-03 evidence updated for webhook surfaces.
</success_criteria>

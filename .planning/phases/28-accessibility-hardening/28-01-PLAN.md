---
phase: 28-accessibility-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/step_up.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
  - accrue_admin/lib/accrue_admin/live/charge_live.ex
  - accrue_admin/lib/accrue_admin/live/invoice_live.ex
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/test/accrue_admin/live/step_up_test.exs
  - accrue_admin/test/accrue_admin/live/charge_live_test.exs
  - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
  - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
autonomous: true
requirements:
  - A11Y-01
must_haves:
  truths:
    - "Step-up dialog uses Phoenix.LiveView.JS push_focus/pop_focus (or equivalent documented stack) on open/close."
    - "Submit control visible label is the Copy-backed string `Verify identity` (exact characters)."
    - "Escape dismisses pending step-up when cancellable without executing the deferred continuation."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex"
      provides: "Dialog semantics + focus hooks + Escape wiring surface"
    - path: "accrue_admin/lib/accrue_admin/step_up.ex"
      provides: "Dismiss API for cancel paths"
---

<objective>
Close **A11Y-01** for the shared step-up surface: predictable **focus** on open/restore on close, **Escape** cancels a pending challenge, submit control reads **Verify identity** from `AccrueAdmin.Copy`, and tests prove open/close/Escape behavior per `28-CONTEXT.md` D-01 and `28-UI-SPEC.md`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/28-accessibility-hardening/28-CONTEXT.md
@.planning/phases/28-accessibility-hardening/28-UI-SPEC.md
@.planning/phases/28-accessibility-hardening/28-RESEARCH.md
@.planning/phases/28-accessibility-hardening/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy + StepUp dismiss API</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/step_up.ex
    - .planning/phases/28-accessibility-hardening/28-CONTEXT.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/lib/accrue_admin/step_up.ex</files>
  <action>
    1. Add `def step_up_submit_label/0`, returning the **exact** string `Verify identity` (no surrounding whitespace).
    2. In `AccrueAdmin.StepUp`, add public `dismiss_challenge/1` accepting a `%Phoenix.LiveView.Socket{}` that returns `socket` with **the same assigns** as after a successful `clear_pending/1` (pending false, challenge nil, continuation nil, step_up_error nil) **without** calling the stored continuation and **without** new `Events.record/1` audit rows for this dismiss path (document in `@moduledoc` that Escape/cancel is intentionally unaudited vs verify outcomes).
    3. Reuse or refactor the private `clear_pending/1` helper so `dismiss_challenge/1` cannot drift from successful clear semantics.
  </action>
  <acceptance_criteria>
    - `rg "def step_up_submit_label" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "Verify identity" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "def dismiss_challenge" accrue_admin/lib/accrue_admin/step_up.ex` exits 0
    - `rg "defp clear_pending" accrue_admin/lib/accrue_admin/step_up.ex` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Copy label + dismiss API landed.</done>
</task>

<task type="auto">
  <name>Task 2: StepUpAuthModal — JS focus stack + label + Escape hook surface</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
    - accrue_admin/lib/accrue_admin/copy.ex
    - https://hexdocs.pm/phoenix_live_view/Phoenix.LiveView.JS.html (focus helpers)
  </read_first>
  <files>accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex</files>
  <action>
    1. At top of module add `import Phoenix.LiveView.JS` (or fully qualify calls — pick one style and keep consistent).
    2. On the outer `section` that renders when `@pending` is true, set **stable** `id="accrue-admin-step-up-dialog"`.
    3. Add `phx-mounted={JS.push_focus() |> JS.focus_first(to: "#accrue-admin-step-up-dialog")}` so initial focus runs **only** when the dialog subtree mounts (not on unrelated events).
    4. Add `phx-remove={JS.pop_focus()}` on that same `section` so closing restores prior focus.
    5. Replace submit button body `Verify` with `<%= AccrueAdmin.Copy.step_up_submit_label() %>` (add `alias AccrueAdmin.Copy` if idiomatic for this file).
    6. Add a **second** control: `type="button"` **Cancel** (or **Close**) with `phx-click="step_up_dismiss"` and `class="ax-button ax-button-ghost"` placed before the submit in reading order; label text **exact** `Cancel` (ASCII C-a-n-c-e-l).
    7. Document in `@moduledoc` that hosts should register optional tab-cycle hook (name reserved in CONTEXT) when they need full trap — do **not** import Alpine.
  </action>
  <acceptance_criteria>
    - `rg "phx-mounted=" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 0
    - `rg "phx-remove=" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 0
    - `rg "step_up_submit_label" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 0
    - `rg ">Verify</" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 1
    - `rg "phx-click=\"step_up_dismiss\"" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Modal markup matches A11Y-01 shell decisions.</done>
</task>

<task type="auto">
  <name>Task 3: LiveViews — dismiss + Escape + pop_focus on success</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/charge_live.ex
    - accrue_admin/lib/accrue_admin/live/invoice_live.ex
    - accrue_admin/lib/accrue_admin/live/subscription_live.ex
    - accrue_admin/lib/accrue_admin/step_up.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/charge_live.ex, accrue_admin/lib/accrue_admin/live/invoice_live.ex, accrue_admin/lib/accrue_admin/live/subscription_live.ex</files>
  <action>
    1. On the **first** `<section class="ax-page">` inside each file’s `AppShell.app_shell` inner block (the section that currently opens the page body), add `phx-window-keydown="step_up_escape"` plus the correct `phx-key` / filter attributes per your locked `phoenix_live_view` Hexdocs so **only** Escape invokes this handler.
    2. Implement `handle_event("step_up_escape", params, socket)` (or the exact event name arity Phoenix generates) so that when `socket.assigns[:step_up_pending] == true`, the socket becomes `StepUp.dismiss_challenge(socket)`; otherwise unchanged.
    3. Implement `handle_event("step_up_dismiss", _params, socket)` with the same pending-guarded dismiss behavior.
    4. In the existing `handle_event("step_up_submit", ...)` **success** branch (where verification succeeds and modal closes), pipe the returned socket through `Phoenix.LiveView.JS` execution is **not** possible from Elixir — instead append `push_event` is wrong. **Required approach:** on successful submit path, return `{:noreply, socket}` only after server assigns already hide modal; rely on `phx-remove={JS.pop_focus()}` from Task 2 when `pending` flips false. If success path today returns `{:noreply, socket}` without removing dialog in same render cycle, ensure `step_up_pending` becomes false in that same `handle_event` return so `phx-remove` fires.
    5. Do not attach `JS.focus_first` to `handle_event("step_up_submit", ...)` on validation error (failed code) paths.
  </action>
  <acceptance_criteria>
    - `rg "phx-window-keydown=\"step_up_escape\"" accrue_admin/lib/accrue_admin/live/charge_live.ex` exits 0
    - `rg "phx-window-keydown=\"step_up_escape\"" accrue_admin/lib/accrue_admin/live/invoice_live.ex` exits 0
    - `rg "phx-window-keydown=\"step_up_escape\"" accrue_admin/lib/accrue_admin/live/subscription_live.ex` exits 0
    - `rg "step_up_dismiss" accrue_admin/lib/accrue_admin/live/charge_live.ex accrue_admin/lib/accrue_admin/live/invoice_live.ex accrue_admin/lib/accrue_admin/live/subscription_live.ex` exits 0
    - `cd accrue_admin && mix compile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>All three money LiveViews route Escape/Cancel to dismiss.</done>
</task>

<task type="auto">
  <name>Task 4: LiveViewTest coverage for focus stack + Escape + label</name>
  <read_first>
    - accrue_admin/test/accrue_admin/live/charge_live_test.exs
    - accrue_admin/test/accrue_admin/live/step_up_test.exs
    - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/charge_live_test.exs, accrue_admin/test/accrue_admin/live/invoice_live_test.exs, accrue_admin/test/accrue_admin/live/subscription_live_test.exs, accrue_admin/test/accrue_admin/live/step_up_test.exs</files>
  <action>
    1. Extend **one** primary LiveView test (prefer `charge_live_test.exs`) to assert rendered HTML contains substring `Verify identity` when step-up is pending.
    2. Add test that simulates `step_up_dismiss` or `step_up_escape` (use `render_click` / `render_keydown` per LiveViewTest API for your Phoenix version) and asserts `step_up_pending` surface disappears (e.g. `role="dialog"` absent or `step-up-title` absent).
    3. Ensure existing step-up submit tests still pass with updated button label (update selectors from text `Verify` to `Verify identity` where they matched literal link text).
  </action>
  <acceptance_criteria>
    - `rg "Verify identity" accrue_admin/test/accrue_admin/live/charge_live_test.exs` exits 0
    - `cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/step_up_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/step_up_test.exs</automated>
  </verify>
  <done>Automated proof for A11Y-01 operator flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTML | Step-up strings are developer-authored; challenge `message` from host auth remains HEEx-escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | I | `Copy.step_up_submit_label/0` | mitigate | Literal return; no interpolation of user input |
| T-28-02 | E | `dismiss_challenge/1` | mitigate | Does not run deferred destructive continuation; document no audit row |

## Security Notes

- Do not echo raw TOTP/password values in new flash or debug text.
- Escape/Cancel must not call `StepUp.verify/2` success path or stored continuation.
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/charge_live_test.exs test/accrue_admin/live/invoice_live_test.exs test/accrue_admin/live/subscription_live_test.exs test/accrue_admin/live/step_up_test.exs`
- `cd accrue_admin && mix compile`
</verification>

<success_criteria>
- A11Y-01: Modal exposes dialog semantics, moves/restores focus via JS stack, Escape + Cancel dismiss without continuation, visible submit label reads **Verify identity**.
- Tests cover open chrome + dismiss paths without flaking on unrelated key events.
</success_criteria>

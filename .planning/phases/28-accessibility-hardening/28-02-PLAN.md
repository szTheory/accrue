---
phase: 28-accessibility-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/components/data_table.ex
  - accrue_admin/lib/accrue_admin/live/customers_live.ex
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/test/accrue_admin/components/data_table_test.exs
  - accrue_admin/test/accrue_admin/live/customers_live_test.exs
  - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
autonomous: true
requirements:
  - A11Y-02
must_haves:
  truths:
    - "Desktop `DataTable` grid renders `<caption class=\"ax-visually-hidden\">` when `table_caption` assign is non-nil."
    - "Customers and webhooks indexes pass captions whose text matches each page `<h2 class=\"ax-display\">` exactly."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      provides: "Optional table caption on desktop `<table>` only"
---

<objective>
Close **A11Y-02**: machine-readable **table names** for the **customers** and **webhooks** desktop grids via native `<caption>` aligned with page titles in `AccrueAdmin.Copy`, without regressing mobile card layout.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/28-accessibility-hardening/28-CONTEXT.md
@.planning/phases/28-accessibility-hardening/28-UI-SPEC.md
@.planning/phases/28-accessibility-hardening/28-RESEARCH.md
@.planning/phases/28-accessibility-hardening/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy captions + visually hidden utility</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
    - accrue_admin/assets/css/app.css
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/assets/css/app.css</files>
  <action>
    1. Add `def customers_index_table_caption/0` returning the **exact** string `Searchable customer projections` (must match `customers_live.ex` `<h2 class="ax-display">` inner text character-for-character).
    2. Add `def webhooks_index_table_caption/0` returning the **exact** string `Replay, inspect, and trace webhook delivery` (must match `webhooks_live.ex` `<h2 class="ax-display">` inner text character-for-character).
    3. In `accrue_admin/assets/css/app.css`, append utility class **`.ax-visually-hidden`** using the classic clip pattern:
       - `position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;`
       - (If `clip` is deprecated in your stylelint rules, use `clip-path: inset(50%)` instead **and** document the substitution in the commit message — but prefer the literal block above if lint-clean.)
  </action>
  <acceptance_criteria>
    - `rg "def customers_index_table_caption" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "def webhooks_index_table_caption" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "Searchable customer projections" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "Replay, inspect, and trace webhook delivery" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "\\.ax-visually-hidden" accrue_admin/assets/css/app.css` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Caption strings + CSS utility exist.</done>
</task>

<task type="auto">
  <name>Task 2: DataTable optional caption (desktop table only)</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/components/data_table.ex
    - .planning/phases/28-accessibility-hardening/28-CONTEXT.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/components/data_table.ex</files>
  <action>
    1. In `update/2`, after the existing `assign(assigns)` (or equivalent merge of incoming assigns into socket), ensure socket assigns include `:table_caption` using `Map.get(assigns, :table_caption)` defaulting to `nil` when the parent omits it (match how other optional assigns like `:card_title` are handled in this module).
    2. Inside the desktop-only `<table class="ax-data-table-grid">` (the branch at ~line 168), immediately after `<table ...>`, conditionally render:
       `<caption :if={@table_caption} class="ax-visually-hidden"><%= @table_caption %></caption>`
    3. Do **not** render `<caption>` inside the mobile card / `<dl>` layout.
  </action>
  <acceptance_criteria>
    - `rg "<caption" accrue_admin/lib/accrue_admin/components/data_table.ex` exits 0
    - `rg "table_caption" accrue_admin/lib/accrue_admin/components/data_table.ex` exits 0
    - `rg "ax-visually-hidden" accrue_admin/lib/accrue_admin/components/data_table.ex` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs</automated>
  </verify>
  <done>DataTable supports optional caption.</done>
</task>

<task type="auto">
  <name>Task 3: Wire customers + webhooks LiveViews</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/customers_live.ex
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/customers_live.ex, accrue_admin/lib/accrue_admin/live/webhooks_live.ex</files>
  <action>
    1. On `CustomersLive` `live_component` for `DataTable`, add `table_caption={Copy.customers_index_table_caption()}`.
    2. On `WebhooksLive` `live_component` for `DataTable`, add `table_caption={Copy.webhooks_index_table_caption()}`.
    3. Confirm no other `DataTable` instances in this phase receive `table_caption` (per CONTEXT — only customers + webhooks).
  </action>
  <acceptance_criteria>
    - `rg "table_caption={Copy.customers_index_table_caption" accrue_admin/lib/accrue_admin/live/customers_live.ex` exits 0
    - `rg "table_caption={Copy.webhooks_index_table_caption" accrue_admin/lib/accrue_admin/live/webhooks_live.ex` exits 0
    - `rg -l "table_caption=" accrue_admin/lib/accrue_admin/live | wc -l` outputs `2`
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Indexes pass captions.</done>
</task>

<task type="auto">
  <name>Task 4: Tests assert caption markup</name>
  <read_first>
    - accrue_admin/test/accrue_admin/components/data_table_test.exs
    - accrue_admin/test/accrue_admin/live/customers_live_test.exs
    - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  </read_first>
  <files>accrue_admin/test/accrue_admin/components/data_table_test.exs, accrue_admin/test/accrue_admin/live/customers_live_test.exs, accrue_admin/test/accrue_admin/live/webhooks_live_test.exs</files>
  <action>
    1. Extend `data_table_test.exs` to mount/render a case with `table_caption` set and assert HTML contains `<caption` and the caption substring and `ax-visually-hidden`.
    2. Extend `customers_live_test.exs` and `webhooks_live_test.exs` (or add minimal tests if missing) to assert rendered index contains `<caption` with the respective full caption string inside the grid table context.
  </action>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs test/accrue_admin/live/customers_live_test.exs test/accrue_admin/live/webhooks_live_test.exs` exits 0
    - `rg "<caption" accrue_admin/test/accrue_admin/components/data_table_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs test/accrue_admin/live/customers_live_test.exs test/accrue_admin/live/webhooks_live_test.exs</automated>
  </verify>
  <done>Regression harness for captions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTML | Captions are static `Copy` strings; table body cells still escape row values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | I | Table captions | mitigate | No user-controlled caption text in this phase |

## Security Notes

- Do not switch table cells to `raw/1` while touching `DataTable`.
</threat_model>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs test/accrue_admin/live/customers_live_test.exs test/accrue_admin/live/webhooks_live_test.exs`
</verification>

<success_criteria>
- A11Y-02: Customers + webhooks desktop tables expose programmatic names via `<caption>` aligned with page `<h2>` text; other routes unchanged.
</success_criteria>

---
phase: 28-accessibility-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
  - 28-02
files_modified:
  - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  - .planning/phases/28-accessibility-hardening/28-VERIFICATION.md
autonomous: true
requirements:
  - A11Y-03
  - A11Y-04
must_haves:
  truths:
    - "Host Playwright spec runs `@axe-core/playwright` after `waitForLiveView` on customers index for both light and dark theme selections."
    - "Assertions fail only when axe reports violations with `impact` of `critical` or `serious`."
  artifacts:
    - path: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      provides: "CI-mounted admin a11y gate"
    - path: ".planning/phases/28-accessibility-hardening/28-VERIFICATION.md"
      provides: "Manual contrast gap checklist + commands"
---

<objective>
Close **A11Y-04** (automated axe on mounted admin) and **A11Y-03** (light + dark contrast signal) by extending the **VERIFY-01** host harness: one Playwright spec that logs in, reaches **customers index** with LiveView connected, forces **light** then **dark** via `data-theme-target` buttons, runs axe with **serious + critical** thresholds, and documents residual manual contrast gaps per `28-CONTEXT.md` D-04.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/28-accessibility-hardening/28-CONTEXT.md
@.planning/phases/28-accessibility-hardening/28-UI-SPEC.md
@.planning/phases/28-accessibility-hardening/28-RESEARCH.md
@examples/accrue_host/e2e/verify01-admin-mounted.spec.js
@examples/accrue_host/e2e/support/fixture.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verify01-admin-a11y Playwright spec</name>
  <read_first>
    - examples/accrue_host/e2e/verify01-admin-mounted.spec.js
    - examples/accrue_host/e2e/support/fixture.js
    - examples/accrue_host/package.json
  </read_first>
  <files>examples/accrue_host/e2e/verify01-admin-a11y.spec.js</files>
  <action>
    1. Create `examples/accrue_host/e2e/verify01-admin-a11y.spec.js` using `@playwright/test` and **exactly** this import style for axe: `const AxeBuilder = require("@axe-core/playwright").default;` (adjust only if package export differs — verify with `node -e "console.log(require('@axe-core/playwright'))"` in `examples/accrue_host`).
    2. Reuse `readFixture`, `reseedFixture`, `login`, `waitForLiveView` from `./support/fixture.js` identical to `verify01-admin-mounted.spec.js`.
    3. Implement **async helper** `async function scanAxe(page)` that runs `new AxeBuilder({ page }).analyze()` and returns violations filtered to `v.impact === "critical" || v.impact === "serious"`.
    4. Primary `test(...)`: after navigation to customers index URL (copy path construction from `verify01-admin-mounted.spec.js` lines ~20–22), call `await waitForLiveView(page)`.
    5. For **light** theme: click `page.locator('button[data-theme-target="light"]')`, `await expect(...).toBeVisible()` if needed, `await waitForLiveView(page)`, then run `scanAxe` and `expect(violations, JSON.stringify(violations, null, 2)).toEqual([])`.
    6. For **dark** theme: click `page.locator('button[data-theme-target="dark"]')`, `await waitForLiveView(page)`, repeat axe assert.
    7. If Topbar theme buttons are not visible on that route, document blocker in `28-VERIFICATION.md` and fail the task — do **not** silently skip theme forcing.
  </action>
  <acceptance_criteria>
    - File `examples/accrue_host/e2e/verify01-admin-a11y.spec.js` exists
    - `rg "AxeBuilder" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` exits 0
    - `rg "data-theme-target=\"light\"" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` exits 0
    - `rg "data-theme-target=\"dark\"" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` exits 0
    - `cd examples/accrue_host && npm run e2e -- e2e/verify01-admin-a11y.spec.js` exits 0 (executor must have Playwright browsers installed)
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npm run e2e -- e2e/verify01-admin-a11y.spec.js</automated>
  </verify>
  <done>Automated axe gate on mounted customers index.</done>
</task>

<task type="auto">
  <name>Task 2: Phase verification doc (manual gaps + commands)</name>
  <read_first>
    - .planning/phases/28-accessibility-hardening/28-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/28-accessibility-hardening/28-VERIFICATION.md</files>
  <action>
    1. Create `.planning/phases/28-accessibility-hardening/28-VERIFICATION.md` with sections: **Automated** (exact npm command from Task 1), **Manual contrast gaps** (bullet list mirroring `28-CONTEXT.md` D-04: gradients, overlays, hover/focus/disabled, Phase 26 theme-exception paths, host `accent_hex`), **Focus mechanism** (reference `JS.push_focus` / `pop_focus` + `step_up_dismiss` from Plan 01), **Table captions** (reference `table_caption` + `Copy` from Plan 02).
    2. Link requirement IDs **A11Y-01..04** explicitly in a small coverage table.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/28-accessibility-hardening/28-VERIFICATION.md` exits 0
    - `rg "A11Y-0" .planning/phases/28-accessibility-hardening/28-VERIFICATION.md` exits 0
    - `rg "verify01-admin-a11y" .planning/phases/28-accessibility-hardening/28-VERIFICATION.md` exits 0
    - `rg "gradients" .planning/phases/28-accessibility-hardening/28-VERIFICATION.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/28-accessibility-hardening/28-VERIFICATION.md && rg "A11Y-01" .planning/phases/28-accessibility-hardening/28-VERIFICATION.md</automated>
  </verify>
  <done>Operator-facing verification map exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host browser | Tests run against seeded fixture data only; no production credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | I | Playwright spec | mitigate | Uses fixture password from env file generated by seed script — do not echo password in `console.log` |

## Security Notes

- Axe failure attachments must not include session cookies or auth headers in saved artifacts beyond Playwright defaults.
</threat_model>

<verification>
- `cd examples/accrue_host && npm run e2e -- e2e/verify01-admin-a11y.spec.js`
- `test -f .planning/phases/28-accessibility-hardening/28-VERIFICATION.md`
</verification>

<success_criteria>
- A11Y-04: CI-relevant host package runs at least one axe-enabled test on mounted admin customers path.
- A11Y-03: Same spec exercises **light** and **dark** theme buttons before axe; manual gap list recorded for under-axe surfaces.
</success_criteria>

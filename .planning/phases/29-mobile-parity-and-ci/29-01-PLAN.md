---
phase: 29-mobile-parity-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/e2e/support/overflow.js
  - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
autonomous: true
requirements:
  - MOB-01
must_haves:
  truths:
    - "`expectNoHorizontalOverflow` is defined once in `e2e/support/overflow.js` and imported by `phase13-canonical-demo.spec.js`."
    - "Overflow check still uses `document.documentElement.scrollWidth` vs `window.innerWidth` with **+1** px tolerance."
  artifacts:
    - path: "examples/accrue_host/e2e/support/overflow.js"
      provides: "Shared overflow assertion for MOB specs and phase13"
---

<objective>
Extract **`expectNoHorizontalOverflow`** (and **`expectVisibleInViewport`** if still required by phase13 after refactor) into **`examples/accrue_host/e2e/support/overflow.js`**, update **`phase13-canonical-demo.spec.js`** to import from that module, and keep Phase 13 behavior unchanged so MOB work can reuse the helper without copy-paste drift (`29-CONTEXT.md` code_context).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/29-mobile-parity-and-ci/29-CONTEXT.md
@.planning/phases/29-mobile-parity-and-ci/29-RESEARCH.md
@.planning/phases/29-mobile-parity-and-ci/29-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overflow support module</name>
  <read_first>
    - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
    - examples/accrue_host/e2e/support/fixture.js
  </read_first>
  <files>examples/accrue_host/e2e/support/overflow.js</files>
  <action>
    1. Create **`examples/accrue_host/e2e/support/overflow.js`** exporting **`expectNoHorizontalOverflow(page, label)`** with the **same** implementation as today’s lines 38–47 in `phase13-canonical-demo.spec.js` (use `@ts-check` and JSDoc `@param {import('@playwright/test').Page} page` matching other support files).
    2. If **`expectVisibleInViewport`** is still used by `assertResponsiveState` / phase13-only helpers in that file, export it from **`overflow.js`** as well (move the full function body from phase13, unchanged semantics).
    3. **`module.exports = { expectNoHorizontalOverflow, … }`** — export only what phase13 still needs.
  </action>
  <acceptance_criteria>
    - `test -f examples/accrue_host/e2e/support/overflow.js` exits 0
    - `rg "expectNoHorizontalOverflow" examples/accrue_host/e2e/support/overflow.js` exits 0
    - `rg "scrollWidth" examples/accrue_host/e2e/support/overflow.js` exits 0
    - `rg "viewportWidth \+ 1" examples/accrue_host/e2e/support/overflow.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && node -e "require('./e2e/support/overflow.js')"</automated>
  </verify>
  <done>Support module exports overflow helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire phase13 to import overflow helpers</name>
  <read_first>
    - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
    - examples/accrue_host/e2e/support/overflow.js
  </read_first>
  <files>examples/accrue_host/e2e/phase13-canonical-demo.spec.js</files>
  <action>
    1. Add **`const { expectNoHorizontalOverflow, … } = require("./support/overflow.js");`** (adjust destructuring to match exports).
    2. Remove the **in-file** duplicate definitions of **`expectNoHorizontalOverflow`** / **`expectVisibleInViewport`** that were moved to support (keep **`assertResponsiveState`** in phase13 if it still composes them, or move **`assertResponsiveState`** into support only if it is still a single consumer — prefer **minimal diff**: leave **`assertResponsiveState`** in phase13 importing the primitives).
    3. Do **not** change test titles, routes, or non-overflow assertion logic.
  </action>
  <acceptance_criteria>
    - `rg "function expectNoHorizontalOverflow" examples/accrue_host/e2e/phase13-canonical-demo.spec.js` exits 1
    - `rg "require\\(\"./support/overflow.js\"\\)" examples/accrue_host/e2e/phase13-canonical-demo.spec.js` exits 0
    - `cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js --project=chromium-desktop` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js --project=chromium-desktop</automated>
  </verify>
  <done>Phase 13 uses shared overflow module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only JS | Support module runs in Playwright Node context; no user-controlled strings in overflow math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | — | `overflow.js` | accept | No secrets; assertion-only |

## Security Notes

- Do not log fixture contents or page HTML in new `console.log` debugging.
</threat_model>

<verification>
- `cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js --project=chromium-desktop`
</verification>

<success_criteria>
- Phase 13 desktop run passes after extraction.
- MOB-01 consumers can `require("./support/overflow.js")` from new specs.
</success_criteria>

## PLANNING COMPLETE

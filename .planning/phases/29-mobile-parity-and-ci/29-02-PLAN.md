---
phase: 29-mobile-parity-and-ci
plan: 02
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - accrue_admin/assets/js/app.js
  - accrue_admin/assets/js/hooks/accrue_shell_nav.js
  - accrue_admin/assets/css/app.css
  - accrue_admin/priv/static/assets/app.js
  - accrue_admin/priv/static/assets/app.css
  - examples/accrue_host/e2e/verify01-admin-mobile.spec.js
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - MOB-03
must_haves:
  truths:
    - "Mobile shell menu opens primary nav links on Pixel 5 (`chromium-mobile`) via working toggle + CSS overlay."
    - "`verify01-admin-mobile.spec.js` uses `test.skip(testInfo.project.name !== \"chromium-mobile\", …)` for substantive MOB tests."
    - "Customers index + customer detail both pass `expectNoHorizontalOverflow` after `waitForLiveView` on real mobile project only."
  artifacts:
    - path: "examples/accrue_host/e2e/verify01-admin-mobile.spec.js"
      provides: "MOB-01/MOB-02/MOB-03 Playwright proof on chromium-mobile"
---

<objective>
Implement **working mobile shell navigation** (toggle + Escape + overlay CSS) in **`accrue_admin`** assets, rebuild **`priv/static`**, and add **`examples/accrue_host/e2e/verify01-admin-mobile.spec.js`** with **`@mobile`** tagging plus **chromium-mobile-only** overflow + nav + customers→detail journey per **`29-CONTEXT.md` D-01–D-04** (closes **MOB-01..MOB-03**).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/29-mobile-parity-and-ci/29-CONTEXT.md
@.planning/phases/29-mobile-parity-and-ci/29-RESEARCH.md
@.planning/phases/29-mobile-parity-and-ci/29-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell nav — JS toggle + Escape</name>
  <read_first>
    - accrue_admin/assets/js/app.js
    - accrue_admin/assets/js/hooks/accrue_theme.js
    - accrue_admin/lib/accrue_admin/components/topbar.ex
    - accrue_admin/lib/accrue_admin/components/app_shell.ex
  </read_first>
  <files>accrue_admin/assets/js/hooks/accrue_shell_nav.js, accrue_admin/assets/js/app.js</files>
  <action>
    1. Add **`accrue_admin/assets/js/hooks/accrue_shell_nav.js`** exporting **`initShellNav()`** that:
       - Registers a **capture-phase** `click` listener on `document` (or `document.body`): if **`event.target.closest("[data-sidebar-toggle='true']")`**, then **`event.preventDefault()`** and **`document.documentElement.classList.toggle("ax-shell-nav-open")`** (use the string **`ax-shell-nav-open`** exactly on **`document.documentElement`**).
       - Registers `keydown` on `document`: if **`event.key === "Escape"`** and **`document.documentElement.classList.contains("ax-shell-nav-open")`**, remove the class.
    2. In **`accrue_admin/assets/js/app.js`**, inside the existing **`ready(() => { … })`** block after theme/clipboard inits, call **`initShellNav()`** (import from `./hooks/accrue_shell_nav`).
    3. Optionally close the drawer when a **sidebar nav anchor** inside **`.ax-sidebar a.ax-sidebar-link`** is clicked (toggle class off) — recommended for “sane state” after navigation.
  </action>
  <acceptance_criteria>
    - `test -f accrue_admin/assets/js/hooks/accrue_shell_nav.js` exits 0
    - `rg "ax-shell-nav-open" accrue_admin/assets/js/hooks/accrue_shell_nav.js` exits 0
    - `rg "initShellNav" accrue_admin/assets/js/app.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Client bundle wires Menu + Escape for shell nav.</done>
</task>

<task type="auto">
  <name>Task 2: Shell nav — CSS overlay below lg breakpoint</name>
  <read_first>
    - accrue_admin/assets/css/app.css
    - accrue_admin/lib/accrue_admin/components/sidebar.ex
  </read_first>
  <files>accrue_admin/assets/css/app.css</files>
  <action>
    1. Below the existing **`@media (min-width: 1024px)`** sidebar rules, add a **max-width: 1023px** (or equivalent **`(max-width: 1023.98px)`**) block so that when **`html.accrue-admin.ax-shell-nav-open`**:
       - **`.ax-sidebar`** is **visible** (`display` **not** `none` — use **`flex`** or **`block`**) as a **fixed** overlay with **`inset: 0 auto 0 0`**, reasonable **`width`** (e.g. **`min(18rem, 85vw)`**), **`z-index`** above **`.ax-shell-main`** but below dev toolbar if conflict — prefer **`z-index: 40`** on sidebar overlay and ensure **`.ax-shell-main`** stays readable or dimmed lightly optional.
       - Add **`overflow-y: auto`** on sidebar for long nav lists.
    2. Ensure **default** (no class) at &lt;1024px keeps **`.ax-sidebar { display: none }`** behavior unchanged from today.
    3. At **`min-width: 1024px`**, **`ax-shell-nav-open`** must **not** break desktop layout — strip or neutralize open-state when desktop (e.g. inside max-width block only) so class is harmless on wide screens.
  </action>
  <acceptance_criteria>
    - `rg "ax-shell-nav-open" accrue_admin/assets/css/app.css` exits 0
    - `rg "1023" accrue_admin/assets/css/app.css` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>CSS exposes sidebar overlay when open class set on mobile widths.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild accrue_admin priv/static assets</name>
  <read_first>
    - accrue_admin/mix.exs
    - examples/accrue_host/README.md
  </read_first>
  <files>accrue_admin/priv/static/assets/app.js, accrue_admin/priv/static/assets/app.css</files>
  <action>
    1. Run **`cd accrue_admin && mix accrue_admin.assets.build`** (or documented equivalent) so **`priv/static/assets/app.js`** and **`app.css`** reflect the new shell nav behavior.
    2. Commit updated **checked-in** static bundles if that is the repo norm (host README says commit updated `priv/static` after admin asset changes).
  </action>
  <acceptance_criteria>
    - `rg "ax-shell-nav-open" accrue_admin/priv/static/assets/app.js` exits 0 **or** (if minifier inlined without readable string) `rg "sidebar-toggle" accrue_admin/priv/static/assets/app.js` exits 0 — **and** `rg "ax-shell-nav-open" accrue_admin/priv/static/assets/app.css` exits 0
    - `rg "ax-shell-nav-open" accrue_admin/priv/static/assets/app.css` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile</automated>
  </verify>
  <done>Built static artifacts include shell nav changes.</done>
</task>

<task type="auto">
  <name>Task 4: Playwright MOB spec — overflow, nav, customers journey</name>
  <read_first>
    - examples/accrue_host/e2e/support/overflow.js
    - examples/accrue_host/e2e/support/fixture.js
    - examples/accrue_host/e2e/verify01-admin-mounted.spec.js
    - scripts/ci/accrue_host_seed_e2e.exs
    - accrue_admin/lib/accrue_admin/components/app_shell.ex
  </read_first>
  <files>examples/accrue_host/e2e/verify01-admin-mobile.spec.js</files>
  <action>
    1. Create **`examples/accrue_host/e2e/verify01-admin-mobile.spec.js`** with **`// @ts-check`** and **`test.describe("@mobile mounted admin shell and customers", () => { … })`**.
    2. At the **start of each substantive test** (or once in `beforeEach`), add **`test.skip(testInfo.project.name !== "chromium-mobile", "MOB layout proofs require real Pixel 5 project")`** using Playwright’s **`testInfo`** parameter.
    3. **Fixture:** Prefer **no `reseedFixture()`** when **`process.env.ACCRUE_HOST_SKIP_PLAYWRIGHT_GLOBAL_SEED === "1"`** (match pattern from other VERIFY specs); otherwise call **`reseedFixture()`** once per describe if required for local dev parity — document branch in a one-line comment.
    4. **MOB-01 overflow:** After reaching **`/billing/customers?org=…`** (fixture `admin_org_alpha_slug`) with **`waitForLiveView`**, call **`expectNoHorizontalOverflow(page, "customers index")`** from **`./support/overflow.js`**. Then **`page.goto`** **`/billing/customers/${fixture.admin_denial_customer_id}?org=${encodeURIComponent(fixture.admin_org_alpha_slug)}`**, **`waitForLiveView`**, run **`expectNoHorizontalOverflow(page, "customer detail")`**.
    5. **MOB-02 nav:** On customers index, assert **`expectNoHorizontalOverflow`** already run; click **`page.getByRole("button", { name: "Menu" })`**; assert **`await expect(page.getByRole("link", { name: "Dashboard" })).toBeVisible()`**, **`Customers`**, **`Subscriptions`** (three links); press **`page.keyboard.press("Escape")`**; assert **`document.documentElement`** does **not** have class **`ax-shell-nav-open`** via **`page.evaluate(() => !document.documentElement.classList.contains("ax-shell-nav-open"))`** expecting **true**.
    6. **MOB-03 journey:** Combine login path from **`verify01-admin-mounted.spec.js`** (**`readFixture`**, **`login(page, fixture, fixture.admin_email)`**, **Go to billing**, **`waitForLiveView`**, org switcher button **`button[data-organization-slug="${fixture.admin_org_alpha_slug}"]`**, **`waitForLiveView`**, customers URL) then **detail** navigation as in step 4; include **`scrollIntoViewIfNeeded`** on the org switcher or customers table row before click if needed for hit targets.
    7. Keep **no assertions** that depend on **`chromium-mobile-tagged`** for layout parity.
  </action>
  <acceptance_criteria>
    - `test -f examples/accrue_host/e2e/verify01-admin-mobile.spec.js` exits 0
    - `rg "@mobile mounted admin" examples/accrue_host/e2e/verify01-admin-mobile.spec.js` exits 0
    - `rg "chromium-mobile" examples/accrue_host/e2e/verify01-admin-mobile.spec.js` exits 0
    - `rg "expectNoHorizontalOverflow" examples/accrue_host/e2e/verify01-admin-mobile.spec.js` exits 0
    - `rg "ax-shell-nav-open" examples/accrue_host/e2e/verify01-admin-mobile.spec.js` exits 0
    - `cd examples/accrue_host && npx playwright test e2e/verify01-admin-mobile.spec.js --project=chromium-mobile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/accrue_host && npx playwright test e2e/verify01-admin-mobile.spec.js --project=chromium-mobile</automated>
  </verify>
  <done>MOB Playwright file green on real mobile project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM | Toggle class affects layout only; no billing mutations |
| Fixture-driven URLs | Customer ids come from seed JSON, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | T | `initShellNav` click handler | mitigate | `preventDefault` only on toggle button match; no `eval` |
| T-29-02-02 | E | Escape handler | accept | Removes local UI class only |

## Security Notes

- Do not post fixture passwords or CSRF tokens to `console.log` in new specs.
</threat_model>

<verification>
- `cd examples/accrue_host && npx playwright test e2e/verify01-admin-mobile.spec.js --project=chromium-mobile`
- `cd examples/accrue_host && npm run e2e` (when CI parity required before merge)
</verification>

<success_criteria>
- `chromium-mobile` passes new MOB spec.
- Desktop + tagged projects skip substantive MOB tests without failing the suite.
</success_criteria>

## PLANNING COMPLETE

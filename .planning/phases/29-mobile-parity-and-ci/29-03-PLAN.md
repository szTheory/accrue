---
phase: 29-mobile-parity-and-ci
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - examples/accrue_host/README.md
autonomous: true
requirements:
  - MOB-02
must_haves:
  truths:
    - "README contains subsection title **exactly** `### Mounted admin — mobile shell` under the VERIFY-01 area."
    - "Subsection documents single scroll owner, menu/drawer pattern, touch targets, `?org=` coherence, and host z-index / double-chrome cautions per D-02."
  artifacts:
    - path: "examples/accrue_host/README.md"
      provides: "Operator-facing mobile shell contract for mounted admin"
---

<objective>
Add the **“Mounted admin — mobile shell”** documentation subsection to **`examples/accrue_host/README.md`** next to VERIFY-01 guidance so MOB-02’s human contract matches automation (`29-CONTEXT.md` D-02).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/29-mobile-parity-and-ci/29-CONTEXT.md
@.planning/phases/29-mobile-parity-and-ci/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — Mounted admin — mobile shell</name>
  <read_first>
    - examples/accrue_host/README.md
    - .planning/phases/29-mobile-parity-and-ci/29-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <action>
    1. Under **`## VERIFY-01 (Phase 21)`** (after the opening paragraphs, before or after the numbered seed steps — pick the spot that reads naturally), insert a new subsection with heading **`### Mounted admin — mobile shell`** (exact heading string).
    2. Body bullets (plain Markdown) must cover, at minimum:
       - **Single scroll owner** — content scrolls inside shell main, not competing with host chrome.
       - **Narrow width** — primary destinations live behind the **Menu** control (`getByRole("button", { name: "Menu" })`) until opened; **no hover-only** primary nav.
       - **`?org=`** — links in the shell preserve active org the same way as desktop (`AppShell` `nav_href` behavior).
       - **Touch targets** — call out need to `scrollIntoViewIfNeeded` for dense tables on Pixel 5 in Playwright.
       - **Host embedding cautions** — warn about **double chrome**, **`overflow: hidden`** on host wrappers, and **z-index** stacking that can hide the drawer (Filament/ActiveAdmin lesson, per CONTEXT).
    3. Cross-link: mention **`e2e/verify01-admin-mobile.spec.js`** runs substantive checks on **`chromium-mobile`** only; **`chromium-mobile-tagged`** is not responsive proof.
  </action>
  <acceptance_criteria>
    - `rg "### Mounted admin — mobile shell" examples/accrue_host/README.md` exits 0
    - `rg "verify01-admin-mobile.spec.js" examples/accrue_host/README.md` exits 0
    - `rg "chromium-mobile-tagged" examples/accrue_host/README.md` exits 0
    - `rg "Menu" examples/accrue_host/README.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -F "### Mounted admin — mobile shell" examples/accrue_host/README.md</automated>
  </verify>
  <done>README mobile shell subsection landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public README | Prose only; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | — | README | accept | Documentation only |

## Security Notes

- Do not embed live Stripe keys or production URLs in examples.
</threat_model>

<verification>
- `grep -F "### Mounted admin — mobile shell" examples/accrue_host/README.md`
</verification>

<success_criteria>
- Maintainers can find mobile shell expectations adjacent to VERIFY-01.
</success_criteria>

## PLANNING COMPLETE

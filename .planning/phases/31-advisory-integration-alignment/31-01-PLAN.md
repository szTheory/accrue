---
phase: 31-advisory-integration-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_verify01_readme_contract.sh
  - examples/accrue_host/README.md
  - examples/accrue_host/package.json
autonomous: true
requirements:
  - INV-03
  - MOB-01
  - MOB-03
  - A11Y-03
must_haves:
  truths:
    - "`verify_verify01_readme_contract.sh` requires a **stable** anchor for the mobile VERIFY-01 spec path **`e2e/verify01-admin-mobile.spec.js`** (or successor filename updated in the same commit)."
    - "`examples/accrue_host/package.json` defines **`e2e:mobile`** as `env -u NO_COLOR playwright test e2e/verify01-admin-mobile.spec.js`."
    - "README VERIFY-01 prose mentions **`npm run e2e:mobile`** as the focused mobile lane (alongside `e2e:a11y`)."
  artifacts:
    - path: "scripts/ci/verify_verify01_readme_contract.sh"
      provides: "CI-enforced README parity including mobile VERIFY-01 anchor"
    - path: "examples/accrue_host/package.json"
      provides: "Maintainer shortcut mirroring e2e:a11y shape"
---

<objective>
Extend the **VERIFY-01 README ↔ CI contract** for **mobile** parity (`31-CONTEXT.md` D-01, D-02), add **`npm run e2e:mobile`** to `examples/accrue_host/package.json`, and align README prose so evaluators see **a11y + mobile** shortcuts symmetrically—without bloating substring tests beyond Tier A + optional Tier B.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/31-advisory-integration-alignment/31-CONTEXT.md
@.planning/phases/31-advisory-integration-alignment/31-RESEARCH.md
@.planning/phases/31-advisory-integration-alignment/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier A — script anchors for mobile VERIFY-01</name>
  <read_first>
    - scripts/ci/verify_verify01_readme_contract.sh
    - examples/accrue_host/README.md
  </read_first>
  <files>scripts/ci/verify_verify01_readme_contract.sh</files>
  <action>
    1. After the existing `require_substring "e2e/verify01-admin-a11y.spec.js"` line, add **`require_substring "e2e/verify01-admin-mobile.spec.js"`** with label **`"mobile VERIFY-01 spec path"`**.
    2. Add **`require_substring "### Mounted admin — mobile shell"`** with label **`"VERIFY-01 mobile shell subsection heading"`** (exact heading text must match `README.md` line starting with `### Mounted admin`).
    3. Do **not** remove or weaken the existing **`sk_live`** `awk` negation block (lines 32–47).
  </action>
  <acceptance_criteria>
    - `rg 'require_substring \"e2e/verify01-admin-mobile.spec.js\"' scripts/ci/verify_verify01_readme_contract.sh` exits 0
    - `rg 'Mounted admin — mobile shell' scripts/ci/verify_verify01_readme_contract.sh` exits 0
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>Tier A mobile anchors enforced in contract script.</done>
</task>

<task type="auto">
  <name>Task 2: Tier B (optional) — README-listed verify01 spec paths exist</name>
  <read_first>
    - scripts/ci/verify_verify01_readme_contract.sh
    - examples/accrue_host/README.md
  </read_first>
  <files>scripts/ci/verify_verify01_readme_contract.sh</files>
  <action>
    1. Append a bash loop **after** all `require_substring` calls and **before** the `awk` `sk_live` block that:
       - extracts path-like tokens matching **`e2e/verify01-*.spec.js`** from **`README.md`** (use `grep -oE 'e2e/verify01-[a-zA-Z0-9_-]+\\.spec\\.js' examples/accrue_host/README.md | sort -u`);
       - for each token, **`test -f "${repo_root}/examples/accrue_host/${token}"`** — on miss, print **`verify_verify01_readme_contract: README references missing spec file: …`** to stderr and **`exit 1`**.
    2. Keep **`repo_root`** resolution consistent with the script’s existing `repo_root=…` line.
  </action>
  <acceptance_criteria>
    - `rg "grep -oE" scripts/ci/verify_verify01_readme_contract.sh` exits 0
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>Filesystem-backed guard for README verify01 spec paths.</done>
</task>

<task type="auto">
  <name>Task 3: package.json — add e2e:mobile</name>
  <read_first>
    - examples/accrue_host/package.json
  </read_first>
  <files>examples/accrue_host/package.json</files>
  <action>
    1. Inside **`scripts`**, add **`"e2e:mobile": "env -u NO_COLOR playwright test e2e/verify01-admin-mobile.spec.js"`** (exact key and command string).
    2. Preserve JSON validity and existing script keys (`e2e`, `e2e:a11y`, …).
  </action>
  <acceptance_criteria>
    - `node -e 'const p=require("./examples/accrue_host/package.json"); if(p.scripts["e2e:mobile"]!=="env -u NO_COLOR playwright test e2e/verify01-admin-mobile.spec.js") process.exit(1)'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const p=require("./examples/accrue_host/package.json"); if(p.scripts["e2e:mobile"]!=="env -u NO_COLOR playwright test e2e/verify01-admin-mobile.spec.js") process.exit(1)'</automated>
  </verify>
  <done>e2e:mobile shortcut added.</done>
</task>

<task type="auto">
  <name>Task 4: README — document e2e:mobile next to e2e:a11y</name>
  <read_first>
    - examples/accrue_host/README.md
    - scripts/ci/verify_verify01_readme_contract.sh
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <action>
    1. In **`## VERIFY-01 (Phase 21)`**, within ~5 lines of the existing **`npm run e2e:a11y`** sentence, add a parallel sentence: **`npm run e2e:mobile`** runs **`e2e/verify01-admin-mobile.spec.js`** on **`chromium-mobile`** (wording may vary but must include exact substrings **`npm run e2e:mobile`** and **`e2e/verify01-admin-mobile.spec.js`** and **`chromium-mobile`**).
    2. If not already present in VERIFY-01 section, add **one** short note that **`env -u NO_COLOR`** is POSIX-oriented and Windows contributors may run `npx playwright test …` directly (keep to one sentence).
  </action>
  <acceptance_criteria>
    - `rg "npm run e2e:mobile" examples/accrue_host/README.md` exits 0
    - `rg "e2e/verify01-admin-mobile.spec.js" examples/accrue_host/README.md` exits 0
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>README matches enforced contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / local bash | Contract script reads repo files only; no network |
| README | Maintainer-authored; script prevents accidental deletion of mobile proof pointers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | README drift | mitigate | `require_substring` + optional file-exists loop |
| T-31-01-02 | — | npm scripts | accept | No secrets in script strings |

## Security Notes

- Do not relax the **`sk_live`** negation in VERIFY-01.
</threat_model>

<verification>
- `bash scripts/ci/verify_verify01_readme_contract.sh`
- `cd examples/accrue_host && npm run e2e:mobile` (when Playwright browsers installed)
</verification>

<success_criteria>
- Contract script passes on `main` after edits.
- Mobile shortcut exists and is documented beside `e2e:a11y`.
</success_criteria>

## PLANNING COMPLETE

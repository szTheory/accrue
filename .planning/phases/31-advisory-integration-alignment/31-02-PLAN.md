---
phase: 31-advisory-integration-alignment
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
autonomous: true
requirements:
  - COPY-02
  - COPY-03
  - UX-01
must_haves:
  truths:
    - "**`StepUpAuthModal`** renders operator-visible chrome (`Sensitive action`, **`Step-up required`**, default body, **`Cancel`**) via **`AccrueAdmin.Copy`** functions (not inline English literals in the `~H` template except where impossible)."
    - **`Copy.step_up_submit_label/0`** remains the submit control text (**`Verify identity`**) — already wired; do not rename without updating **`accrue_admin/e2e/phase7-uat.spec.js`** if it asserts submit text (today it does not assert submit label text).
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy.ex"
      provides: "New step-up chrome accessors"
    - path: "accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex"
      provides: "HEEx uses Copy for step-up strings"
---

<objective>
Close the audit finding **mix of Copy-backed and inline strings** in **`step_up_auth_modal.ex`** by routing **eyebrow, title, Cancel label, and default challenge body** through **`AccrueAdmin.Copy`** (`31-CONTEXT.md` D-03, audit §integration step-up).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/31-advisory-integration-alignment/31-CONTEXT.md
@.planning/phases/31-advisory-integration-alignment/31-RESEARCH.md
@.planning/phases/28-accessibility-hardening/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Copy functions for step-up chrome</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex</files>
  <action>
    1. In **`AccrueAdmin.Copy`**, add **public** functions (exact return strings):
       - **`step_up_eyebrow/0`** → **`"Sensitive action"`**
       - **`step_up_title/0`** → **`"Step-up required"`**
       - **`step_up_default_challenge_message/0`** → **`"Confirm your identity to continue."`**
       - **`step_up_cancel_label/0`** → **`"Cancel"`**
    2. Place them **adjacent** to existing **`step_up_submit_label/0`** (after line 73 region) with one-line `@doc` comments if needed for consistency with the module.
  </action>
  <acceptance_criteria>
    - `rg "def step_up_title" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "Step-up required" accrue_admin/lib/accrue_admin/copy.ex` exits 0
    - `rg "Confirm your identity to continue" accrue_admin/lib/accrue_admin/copy.ex` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Copy API exposes step-up strings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire StepUpAuthModal to Copy</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex</files>
  <action>
    1. Replace inline **`Sensitive action`**, **`Step-up required`**, **`Cancel`**, and the default **`Confirm your identity to continue.`** string in the `~H` template with **`Copy.step_up_eyebrow()`**, **`Copy.step_up_title()`**, **`Copy.step_up_cancel_label()`**, and **`Map.get(@challenge || %{}, :message) || Copy.step_up_default_challenge_message()`** respectively.
    2. Keep **`Copy.step_up_submit_label()`** for the submit control.
    3. Do **not** change **`input_placeholder/1`** private logic except if a literal duplicates **Copy** — placeholders stay in the private functions unless audit-required (out of scope unless identical to new Copy strings).
  </action>
  <acceptance_criteria>
    - `rg "Sensitive action" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 1
    - `rg "Step-up required" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 1
    - `rg "Copy\\.step_up_title" accrue_admin/lib/accrue_admin/components/step_up_auth_modal.ex` exits 0
    - `cd accrue_admin && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Modal is Copy-backed for listed chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI copy | Static operator strings; no user input echoed into new Copy functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | — | Copy strings | accept | No security claims added |

## Security Notes

- Do not log challenge maps in new debug code.
</threat_model>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors`
- `cd accrue_admin && mix test test/accrue_admin/live/step_up_test.exs` (if present and green in CI matrix)
</verification>

<success_criteria>
- Step-up modal has no inline English for the four routed strings.
- Compiler clean on `accrue_admin`.
</success_criteria>

## PLANNING COMPLETE

---
phase: 31-advisory-integration-alignment
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - accrue_admin/e2e/phase7-uat.spec.js
  - .github/workflows/accrue_admin_browser.yml
  - accrue_admin/README.md
autonomous: true
requirements:
  - COPY-03
  - INV-01
  - INV-03
must_haves:
  truths:
    - "**`accrue_admin/e2e/phase7-uat.spec.js`** no longer asserts long **verbatim** **`Copy.Locked`** success strings where **`data-role`** / **dialog structure** + **role/name** affordances already prove the same UX outcome (per `31-CONTEXT.md` D-04)."
    - **`.github/workflows/accrue_admin_browser.yml`** includes a YAML comment (first line after `name:` block or above `jobs:`) stating **`examples/accrue_host` VERIFY-01 Playwright is the merge-blocking browser truth** for mounted admin proofs."
    - **`accrue_admin/README.md` `## Browser UAT`** section links evaluators to **`examples/accrue_host/README.md`** **`## VERIFY-01`** for canonical mounted proofs."
  artifacts:
    - path: "accrue_admin/e2e/phase7-uat.spec.js"
      provides: "Reduced duplicate literal coupling vs host VERIFY-01"
---

<objective>
Reduce **dual Playwright literal drift** between **`accrue_admin` fixture UAT** and **host VERIFY-01** by tightening admin specs to **structural / role-first** assertions, and document **`accrue_admin_browser.yml`** as **fast package smoke** with **host VERIFY-01** as the **normative** merge-blocking path (`31-CONTEXT.md` D-04, D-05).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/31-advisory-integration-alignment/31-CONTEXT.md
@.planning/phases/21-admin-and-host-ux-proof/21-CONTEXT.md
@.planning/phases/27-microcopy-and-operator-strings/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor phase7-uat — webhook replay flow</name>
  <read_first>
    - accrue_admin/e2e/phase7-uat.spec.js
    - accrue_admin/lib/accrue_admin/live/webhook_live.ex
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  </read_first>
  <files>accrue_admin/e2e/phase7-uat.spec.js</files>
  <action>
    1. In **`operator can replay one webhook…`**, after **`getByRole("button", { name: "Replay webhook" }).click()`**, replace **`expect(page.getByText("Replay webhook for the active organization?"))`** with **`await expect(page.locator("[data-role='replay-confirm']")).toBeVisible({ timeout: 15_000 })`** (see **`webhook_live.ex`** `data-role="replay-confirm"`).
    2. Keep the existing **`locator("[data-role='confirm-replay']").click()`** line; it must run **after** the replay-confirm panel is visible.
    3. Replace the **`.or()`** success **`getByText`** pair with **one** assertion: **`await expect(page.getByText(/Replay requested|Webhook replay requested/)).toBeVisible({ timeout: 15_000 })`**.
  </action>
  <acceptance_criteria>
    - `rg "Replay webhook for the active organization\\?" accrue_admin/e2e/phase7-uat.spec.js` exits 1
    - `rg "Replay requested for the active organization\\." accrue_admin/e2e/phase7-uat.spec.js` exits 1
    - `rg "Webhook replay requested\\." accrue_admin/e2e/phase7-uat.spec.js` exits 1
    - `rg "data-role='replay-confirm'" accrue_admin/e2e/phase7-uat.spec.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && npm run e2e</automated>
  </verify>
  <done>Single-replay path uses structural + regex success.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor phase7-uat — bulk replay confirmation</name>
  <read_first>
    - accrue_admin/e2e/phase7-uat.spec.js
    - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  </read_first>
  <files>accrue_admin/e2e/phase7-uat.spec.js</files>
  <action>
    1. Replace **`toContainText("Replay 1 failed or dead webhook rows for the active organization?")`** on **`[data-role='bulk-replay-confirm']`** with **`await expect(page.locator("[data-role='bulk-replay-confirm']")).toBeVisible()`** plus **`await expect(page.locator("[data-role='bulk-replay-confirm']")).toContainText(/failed or dead webhook rows for the active organization/)`** (regex **without** hard-coding the **`1`** digit so seed drift does not break the spec).
    2. Keep the subsequent **`confirm-bulk-replay`** click and **counts** assertion unchanged.
  </action>
  <acceptance_criteria>
    - `rg "Replay 1 failed or dead webhook rows" accrue_admin/e2e/phase7-uat.spec.js` exits 1
    - `rg "failed or dead webhook rows for the active organization" accrue_admin/e2e/phase7-uat.spec.js` exits 0
    - `cd accrue_admin && npm run e2e` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd accrue_admin && npm run e2e</automated>
  </verify>
  <done>Bulk replay assert no longer hard-codes full Locked string + count.</done>
</task>

<task type="auto">
  <name>Task 3: Workflow maintainer comment</name>
  <read_first>
    - .github/workflows/accrue_admin_browser.yml
  </read_first>
  <files>.github/workflows/accrue_admin_browser.yml</files>
  <action>
    1. Insert a YAML comment line immediately under **`name: AccrueAdmin Browser UAT`** (or as the first line in the file if YAML position disallows — **prefer** top-of-file **`#`** comment block **before** `name:`) containing the exact substring **`examples/accrue_host`** and **`VERIFY-01`** and the phrase **`merge-blocking`** explaining host owns canonical mounted browser proofs.
  </action>
  <acceptance_criteria>
    - `rg "examples/accrue_host" .github/workflows/accrue_admin_browser.yml` exits 0
    - `rg "VERIFY-01" .github/workflows/accrue_admin_browser.yml` exits 0
    - `rg "merge-blocking" .github/workflows/accrue_admin_browser.yml` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/accrue_admin_browser.yml')); print('ok')"</automated>
  </verify>
  <done>Workflow documents host-first truth.</done>
</task>

<task type="auto">
  <name>Task 4: README Browser UAT — pointer to host VERIFY-01</name>
  <read_first>
    - accrue_admin/README.md
    - examples/accrue_host/README.md
  </read_first>
  <files>accrue_admin/README.md</files>
  <action>
    1. Under **`## Browser UAT`**, add a short paragraph containing **all** of: **`VERIFY-01`**, **`examples/accrue_host/README.md`**, and the sentence meaning **host Playwright is canonical for merge-blocking mounted admin proofs** (exact English flexible).
  </action>
  <acceptance_criteria>
    - `rg "VERIFY-01" accrue_admin/README.md` exits 0
    - `rg "examples/accrue_host/README.md" accrue_admin/README.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f accrue_admin/README.md</automated>
  </verify>
  <done>README steers evaluators to host VERIFY-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture Playwright | Runs against `__e2e__` test endpoints only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | — | Weaker assertions | accept | Host VERIFY-01 retains literal contracts |

## Security Notes

- Comments and README must not suggest disabling webhook signature verification.
</threat_model>

<verification>
- `cd accrue_admin && npm run e2e`
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/accrue_admin_browser.yml'))"`
</verification>

<success_criteria>
- Admin browser suite green with reduced verbatim Locked duplication.
- Workflow + README document host-first VERIFY-01 posture.
</success_criteria>

## PLANNING COMPLETE

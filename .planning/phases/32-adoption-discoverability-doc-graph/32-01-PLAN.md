---
phase: 32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/README.md
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/verify_verify01_readme_contract.sh
autonomous: true
requirements:
  - ADOPT-02
---

<objective>
Restructure `examples/accrue_host/README.md` so one top-level **## Proof and verification** section is the executable SSOT: opening paragraph states the approved merge-blocking one-liner verbatim, **### Verification modes** carries the former “Verification modes” body, and **### VERIFY-01 (Phase 21)** carries the VERIFY-01 checklist (with heading levels adjusted so nested content stays valid). Update doc-contract scripts in the same change so CI stays green.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-doc-01: Mis-stating which command is merge-blocking (`mix verify` vs `mix verify.full`) | High | Paste the approved one-liner from `32-CONTEXT.md` verbatim in the H2 lede; keep `host-integration` id unchanged. |
| T-doc-02: CI doc gates drift after heading rename | Medium | Edit `verify_package_docs.sh` and `verify_verify01_readme_contract.sh` in the same PR as README; run both scripts before commit. |
| T-doc-03: sk_live guidance regression | Medium | Preserve awk negation logic; if VERIFY-01 heading depth changes, update awk `^## VERIFY-01` to `^#{2,3} VERIFY-01` so the `sk_live` scan still covers the VERIFY-01 block. |
</threat_model>

<tasks>

<task id="32-01-01" type="execute">
<read_first>
- examples/accrue_host/README.md
- .planning/phases/32-adoption-discoverability-doc-graph/32-CONTEXT.md
- scripts/ci/verify_package_docs.sh
- scripts/ci/verify_verify01_readme_contract.sh
</read_first>
<action>
1. In `examples/accrue_host/README.md`, **after** the existing `## Seeded history` section and **before** the current `## Verification modes` heading, insert a new top-level section:

   - Title (exact): `## Proof and verification`
   - First paragraph (exact string, single paragraph or two short sentences, no wording drift):

     `Pull requests are merge-blocked on GitHub Actions job `host-integration`, which runs the same contract as `cd examples/accrue_host && mix verify.full`; use `mix verify` for a faster bounded Fake slice that is not CI-complete.`

   - Demote the former `## Verification modes` block to a subsection titled exactly `### Verification modes` — keep the bullet semantics; remove duplicate merge-blocking sentences that are now fully covered by the lede (at most one forward-reference sentence allowed).

2. Demote the former `## VERIFY-01 (Phase 21)` block to `### VERIFY-01 (Phase 21)` nested under `## Proof and verification`. Re-indent child headings under VERIFY-01 by one level (each former `###` becomes `####`, each former `####` becomes `#####`, etc.) so Markdown hierarchy stays valid. Preserve all command blocks, paths, and CI identifiers.

3. Keep `## Visual walkthrough (Fake-backed)` and later sections **below** the new Proof section unless CONTEXT explicitly required moving them; they must remain clearly labeled non-blocking relative to the lede (add one clarifying sentence in the Visual walkthrough intro if needed).

4. In `scripts/ci/verify_package_docs.sh`, replace the invariant `require_fixed ... "## Verification modes"` with **both**:
   - `require_fixed` for `## Proof and verification`
   - `require_fixed` for `### Verification modes`

5. In `scripts/ci/verify_verify01_readme_contract.sh`:
   - Change the awk start pattern from `/^## VERIFY-01/` to `/^#{2,3} VERIFY-01/` so `sk_live` scanning works when VERIFY-01 is `###`.
   - Replace `require_substring "### Mounted admin — mobile shell"` with `require_substring "Mounted admin — mobile shell"` so the mobile shell contract survives heading depth changes.
</action>
<acceptance_criteria>
- `grep -Fq "## Proof and verification" examples/accrue_host/README.md` exits 0.
- `grep -Fq "### Verification modes" examples/accrue_host/README.md` exits 0.
- `grep -Fq "### VERIFY-01" examples/accrue_host/README.md` exits 0.
- `grep -Fq "Pull requests are merge-blocked on GitHub Actions job \`host-integration\`" examples/accrue_host/README.md` exits 0 (backticks as in file).
- `grep -Fq "not CI-complete" examples/accrue_host/README.md` exits 0.
- `grep -Fq "require_fixed \"\$ROOT_DIR/examples/accrue_host/README.md\" \"## Proof and verification\"" scripts/ci/verify_package_docs.sh` exits 0.
- `grep -Fq "### Verification modes" scripts/ci/verify_package_docs.sh` exits 0 (inside require_fixed line).
- `grep -Eq '\^#\{2,3\} VERIFY-01' scripts/ci/verify_verify01_readme_contract.sh` exits 0.
- `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root.
- `bash scripts/ci/verify_verify01_readme_contract.sh` exits 0 from repo root.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_verify01_readme_contract.sh</verify>
</task>

</tasks>

<verification>
From repository root after all tasks: `bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_verify01_readme_contract.sh` (must print OK lines and exit 0).
</verification>

<must_haves>
- Host README has a single authoritative **Proof and verification** section containing the approved merge-blocking one-liner and VERIFY-01 material without contradicting it.
- Doc-contract scripts assert the new structure; CI would fail if headings drift.
</must_haves>

<success_criteria>
- Both doc-contract scripts pass on a clean checkout after the change.
- `host-integration`, `mix verify.full`, and `mix verify` roles are never inverted in prose.
</success_criteria>

---
phase: 32
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - ADOPT-01
---

<objective>
Add an above-the-fold **Proof path (VERIFY-01)** block to the repository root `README.md` so evaluators get merge-blocking Fake-first instructions in ≤5 lines, with **exactly one** primary deep link to the host `## Proof and verification` anchor, without duplicating the Playwright matrix or VERIFY-01 checklist on the root.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-doc-01: Root implies `mix verify` is CI-complete | High | Block must include the substring `not CI-complete` or the full approved one-liner from plan 01 (same wording as host lede). |
| T-doc-04: Broken anchor / >2 hops to runnable commands | Medium | Link target must match the actual host heading text from plan 01 (`#proof-and-verification` for `## Proof and verification` — verify fragment after edit). |
</threat_model>

<tasks>

<task id="32-02-01" type="execute">
<read_first>
- README.md
- examples/accrue_host/README.md
- .planning/phases/32-adoption-discoverability-doc-graph/32-CONTEXT.md
- scripts/ci/verify_package_docs.sh
</read_first>
<action>
1. Open `examples/accrue_host/README.md` and confirm the `## Proof and verification` heading text is exactly `## Proof and verification` (from plan 01). Compute the GitHub fragment id (`#proof-and-verification`). If the renderer slug differs, use the correct fragment (verify by matching GitHub’s algorithm: lowercase, spaces → `-`, remove `&`).

2. In **repository root** `README.md`, immediately **after** the one-line product description (`Accrue owns the billing engine…`) and **before** `## Start here`, insert a short block:

   - Markdown heading (exact): `## Proof path (VERIFY-01)`
   - Body: ≤5 lines including: Fake-first default, `cd examples/accrue_host`, merge-blocking command `mix verify.full`, explicit statement that `mix verify` is faster but **not CI-complete**, and **one** markdown link to `examples/accrue_host/README.md` with the fragment from step 1 (e.g. `[merge-blocking proof details](examples/accrue_host/README.md#proof-and-verification)`).

3. Do **not** remove or replace the existing `## Start here` hub; the proof block complements it. Optionally add one bullet under `## Start here` pointing to `## Proof path (VERIFY-01)` **only if** it does not increase hop count ambiguity — prefer a single clear entry path.

4. Extend `scripts/ci/verify_package_docs.sh` with **two** new `require_fixed` calls on `$ROOT_DIR/README.md`:
   - The substring `## Proof path (VERIFY-01)`
   - The substring `proof-and-verification` inside the host README link (path + fragment as implemented in step 2)
</action>
<acceptance_criteria>
- `grep -Fq "## Proof path (VERIFY-01)" README.md` exits 0.
- `grep -Fq "cd examples/accrue_host" README.md` exits 0 in the Proof path section (file-level check: `grep -n "Proof path" -A20 README.md | grep -Fq "cd examples/accrue_host"` exits 0).
- `grep -Fq "mix verify.full" README.md` exits 0.
- `grep -Fq "not CI-complete" README.md` exits 0.
- `grep -Fq "examples/accrue_host/README.md#" README.md` exits 0.
- `grep -Fq "proof-and-verification" README.md` exits 0.
- `grep -Fq 'require_fixed "$ROOT_DIR/README.md" "## Proof path (VERIFY-01)"' scripts/ci/verify_package_docs.sh` exits 0.
- `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh</verify>
</task>

</tasks>

<verification>
`bash scripts/ci/verify_package_docs.sh` from repository root exits 0.
</verification>

<must_haves>
- Root README surfaces merge-blocking proof in one glance block with a single canonical link into the host Proof section.
- Doc contract locks the new root strings so they cannot drift silently.
</must_haves>

<success_criteria>
- From `README.md`, a reader reaches explicit `mix verify` / `mix verify.full` / Playwright pointers within two hops (root block → host Proof section → commands).
</success_criteria>

---
phase: 32
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - accrue/guides/testing.md
  - accrue/guides/first_hour.md
  - guides/testing-live-stripe.md
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - ADOPT-03
---

<objective>
Align package and repo guides with the **two-layer SSOT**: guides teach lanes and philosophy while linking to `examples/accrue_host/README.md#proof-and-verification` for runnable commands. Add the **approved merge-blocking one-liner** (identical to host lede) near the top of `accrue/guides/testing.md` without introducing a second contradictory command matrix. Extend doc-contract coverage so the one-liner cannot drift.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-doc-01: Guides contradict host on merge-blocking | High | Use the exact same one-liner string as in `32-01-PLAN.md` / host README; link to host fragment; do not restate npm matrix on root. |
| T-doc-05: Stripe advisory read as merge-blocking | Medium | `guides/testing-live-stripe.md` keeps advisory framing; add cross-link to host Proof section if missing. |
</threat_model>

<tasks>

<task id="32-03-01" type="execute">
<read_first>
- accrue/guides/testing.md
- accrue/guides/first_hour.md
- guides/testing-live-stripe.md
- examples/accrue_host/README.md
- .planning/phases/32-adoption-discoverability-doc-graph/32-CONTEXT.md
- scripts/ci/verify_package_docs.sh
</read_first>
<action>
1. **accrue/guides/testing.md** — Immediately after the top-level title `# Testing Accrue Billing Flows` and before `## Fake-first Phoenix scenario`, insert a short callout paragraph (no new heading required) containing **exactly** this sentence (verbatim, including backticks):

   `Pull requests are merge-blocked on GitHub Actions job `host-integration`, which runs the same contract as `cd examples/accrue_host && mix verify.full`; use `mix verify` for a faster bounded Fake slice that is not CI-complete.`

   Add a second sentence with a markdown link using the same relative style as other links in this guide, pointing to `../../examples/accrue_host/README.md#proof-and-verification` (Runnable VERIFY-01 commands and Playwright entry points).

2. **accrue/guides/first_hour.md** — In the section that discusses verification / `mix verify` (existing content around “focused tutorial proof suite”), add **one** sentence that points readers to the host Proof section for the authoritative command matrix: include a markdown link `../../examples/accrue_host/README.md#proof-and-verification` (or the repo’s established relative pattern — match how existing links to the host README are written in that file). Do **not** add a second npm/CI matrix.

3. **guides/testing-live-stripe.md** — After the opening paragraphs (before or within “What the live-Stripe suite covers”), add a single sentence: live-Stripe / `live-stripe` job is **not** the PR merge-blocking lane; link to `examples/accrue_host/README.md#proof-and-verification` using relative path from repo root: `examples/accrue_host/README.md#proof-and-verification`.

4. **scripts/ci/verify_package_docs.sh** — Add `require_fixed` for the approved one-liner sentence (choose **one** canonical file to pin first, preferably `accrue/guides/testing.md` since it is the philosophy hub) OR a unique 32-character substring long enough to be stable, e.g. require_fixed on:

   `Pull requests are merge-blocked on GitHub Actions job \`host-integration\``

   on both `accrue/guides/testing.md` and `README.md` if not already asserted for root (if root already contains it from plan 02, only add to guides that must carry it — **minimum** add to `accrue/guides/testing.md`).

5. If `verify_package_docs.sh` already loops guides for other invariants, extend the loop or add explicit `require_fixed` lines consistent with existing style.
</action>
<acceptance_criteria>
- `grep -Fq "Pull requests are merge-blocked on GitHub Actions job \`host-integration\`" accrue/guides/testing.md` exits 0.
- `grep -Fq "not CI-complete" accrue/guides/testing.md` exits 0.
- `grep -Fq "examples/accrue_host/README.md#proof-and-verification" accrue/guides/testing.md` exits 0 **or** `grep -Fq "../../examples/accrue_host/README.md#proof-and-verification" accrue/guides/testing.md` exits 0 (at least one).
- `grep -Fq "proof-and-verification" accrue/guides/first_hour.md` exits 0.
- `grep -Fq "proof-and-verification" guides/testing-live-stripe.md` exits 0.
- `grep -Fq "examples/accrue_host/README.md#proof-and-verification" guides/testing-live-stripe.md` exits 0.
- `grep -Fq "host-integration" scripts/ci/verify_package_docs.sh` already true; additionally `grep -Fq "Pull requests are merge-blocked" scripts/ci/verify_package_docs.sh` exits 0 after edit.
- `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh</verify>
</task>

</tasks>

<verification>
`bash scripts/ci/verify_package_docs.sh` from repository root exits 0.
</verification>

<must_haves>
- High-traffic guides open with or immediately reference the same merge-blocking one-liner as the host Proof section.
- No guide introduces a competing “primary CI command” story; deep links land on `#proof-and-verification`.
</must_haves>

<success_criteria>
- Cross-links among `testing.md`, `first_hour.md`, and `testing-live-stripe.md` agree with the host Proof H2 on what blocks merge vs advisory lanes.
</success_criteria>

---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/first_hour.md
  - accrue/test/accrue/docs/first_hour_guide_test.exs
autonomous: true
requirements:
  - ADOPT-04
---

<objective>
Teach installer **rerun semantics** in the First Hour path by linking to the locked public contract in `upgrade.md`, without breaking the manifest-backed doc order test.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-doc-01: Readers infer reruns always overwrite host edits | High | Link explicitly to `upgrade.md#installer-rerun-behavior` and use one sentence quoting “pristine” vs “user-edited” semantics aligned with `upgrade.md`. |
| T-doc-02: `first_hour_guide_test` order regression | Medium | Append new `##` section **after** the last `assert_order!` marker (`mix verify.full` occurrence used in the chain) or extend `assert_order!` list in the same commit. |
</threat_model>

<tasks>

<task id="33-01-01" type="execute">
<read_first>
- accrue/guides/first_hour.md
- accrue/guides/upgrade.md
- accrue/test/accrue/docs/first_hour_guide_test.exs
- .planning/phases/33-installer-host-contracts-ci-clarity/33-CONTEXT.md
</read_first>
<action>
1. At the **end** of `accrue/guides/first_hour.md` (after the `## 3. Focused verification` body), append a new section titled exactly `## 4. Rerunning mix accrue.install`.

2. In that section, write 2–4 sentences that state: reruns refresh **pristine** generated files; **user-edited** generated files stay skipped; unmarked files stay skipped unless forcing; optional `--write-conflicts` writes reviewable artifacts — **same meaning** as bullets under `## Installer rerun behavior` in `accrue/guides/upgrade.md`, without inventing new flags.

3. Include a Markdown link using **exact** link text and href (relative to `accrue/guides/`): `[Upgrade guide — Installer rerun behavior](upgrade.md#installer-rerun-behavior)` (the heading slug must match GitHub-style anchor for `## Installer rerun behavior`).

4. In `accrue/test/accrue/docs/first_hour_guide_test.exs`, add `assert guide =~ "upgrade.md#installer-rerun-behavior"` inside the existing `"first hour guide preserves the manifest-backed host boundary contract"` test (or a focused new assertion in that test module).

5. Do **not** insert the new `## 4` section **before** the `Seeded history` / `Focused verification` markers used by `assert_order!` — only append after section 3 content.
</action>
<acceptance_criteria>
- `grep -Fq "## 4. Rerunning mix accrue.install" accrue/guides/first_hour.md` exits 0.
- `grep -Fq "upgrade.md#installer-rerun-behavior" accrue/guides/first_hour.md` exits 0.
- `grep -Fq "upgrade.md#installer-rerun-behavior" accrue/test/accrue/docs/first_hour_guide_test.exs` exits 0.
- `cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs --warnings-as-errors` exits 0.
- `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh && cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs --warnings-as-errors</verify>
</task>

</tasks>

<verification>
`bash scripts/ci/verify_package_docs.sh && cd accrue && mix test test/accrue/docs/first_hour_guide_test.exs --warnings-as-errors`
</verification>

<must_haves>
- First Hour links to the upgrade guide’s installer rerun anchor with correct semantics.
- Doc order test stays green.
</must_haves>

<success_criteria>
- ADOPT-04: Documented rerun story matches the existing upgrade guide + installer tests for the facets we surface in First Hour.
</success_criteria>

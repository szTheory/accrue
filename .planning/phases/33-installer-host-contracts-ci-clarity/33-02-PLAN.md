---
phase: 33
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - ADOPT-05
---

<objective>
Harden **doc drift detection** for Phase 33 installer/discoverability strings so renamed guides or moved anchors fail in CI, using existing bash + ExUnit gates.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-doc-02: New First Hour link rots silently | Medium | Add `require_fixed` in `verify_package_docs.sh` for the exact `upgrade.md#installer-rerun-behavior` substring in `accrue/guides/first_hour.md`. |
| T-doc-03: Troubleshooting loses install preflight hook | Medium | Add `require_fixed` for `mix accrue.install --check` in `accrue/guides/troubleshooting.md` via `verify_package_docs.sh`. |
</threat_model>

<tasks>

<task id="33-02-01" type="execute">
<read_first>
- scripts/ci/verify_package_docs.sh
- accrue/guides/first_hour.md
- accrue/guides/troubleshooting.md
- .planning/phases/33-installer-host-contracts-ci-clarity/33-RESEARCH.md
</read_first>
<action>
1. In `scripts/ci/verify_package_docs.sh`, after the existing `require_fixed` lines for `accrue/guides/first_hour.md` (or nearby first-hour invariants), add:

   `require_fixed "$ROOT_DIR/accrue/guides/first_hour.md" "upgrade.md#installer-rerun-behavior"`

2. In the same file, if there is not already a line requiring `mix accrue.install --check` inside `accrue/guides/troubleshooting.md`, add:

   `require_fixed "$ROOT_DIR/accrue/guides/troubleshooting.md" "mix accrue.install --check"`

   (Search the script first; if already present, skip this line and note it in the commit message.)
</action>
<acceptance_criteria>
- `grep -Fq 'require_fixed "$ROOT_DIR/accrue/guides/first_hour.md" "upgrade.md#installer-rerun-behavior"' scripts/ci/verify_package_docs.sh` exits 0.
- `grep -Fq 'require_fixed "$ROOT_DIR/accrue/guides/troubleshooting.md" "mix accrue.install --check"' scripts/ci/verify_package_docs.sh` exits 0 **or** `grep -Fq "mix accrue.install --check" scripts/ci/verify_package_docs.sh` exits 0 with a `require_fixed` targeting `troubleshooting.md` (executor documents which variant applied).
- `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh</verify>
</task>

</tasks>

<verification>
`bash scripts/ci/verify_package_docs.sh`
</verification>

<must_haves>
- CI doc gate fails if the First Hour → upgrade rerun anchor string disappears.
</must_haves>

<success_criteria>
- ADOPT-05: Broken anchors or accidental removal of the rerun cross-link are caught by automated gates.
</success_criteria>

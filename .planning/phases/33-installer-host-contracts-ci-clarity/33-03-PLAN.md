---
phase: 33
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - guides/testing-live-stripe.md
  - README.md
autonomous: true
requirements:
  - ADOPT-06
---

<objective>
Preserve **stable GitHub Actions job ids** while making merge-blocking vs **advisory** Stripe test-mode lanes impossible to misread in workflow comments and top-level docs.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-ci-01: Contributors rename `jobs:` keys | High | Do **not** change YAML job keys (`release-gate`, `host-integration`, `live-stripe`, …); only edit `name:` display strings and comments. |
| T-ci-02: Docs imply `live-stripe` blocks merges | High | Keep explicit “not merge-blocking” / “advisory” language next to `live-stripe` references; link to host Proof section where helpful. |
</threat_model>

<tasks>

<task id="33-03-01" type="execute">
<read_first>
- .github/workflows/ci.yml
- guides/testing-live-stripe.md
- README.md
- .planning/phases/33-installer-host-contracts-ci-clarity/33-CONTEXT.md
</read_first>
<action>
1. In `.github/workflows/ci.yml`, at the **top** of the file after the existing `name: CI` (within first ~40 lines), add a short YAML comment block listing **job id keys** that external docs and `act` rely on: `release-gate`, `phase18-tax-gate`, `admin-drift-docs`, `host-integration`, `annotation-sweep`, `live-stripe`. State explicitly which ids are **merge-blocking on pull_request** vs **advisory** (`live-stripe` only on `workflow_dispatch` + `schedule`).

2. In `guides/testing-live-stripe.md`, ensure the paragraph that discusses PR merge-blocking points to **job id** `host-integration` (backticks) and reiterates `live-stripe` is **not** the PR merge-blocking lane. If this is already present verbatim, add **one** clarifying sentence max (no duplicate matrices).

3. In repository root `README.md`, within the **Proof path (VERIFY-01)** section (or adjacent adoption bullets), add **one** sentence: merge-blocking jobs include `host-integration`; Stripe test-mode parity uses job id `live-stripe` on manual/cron runs and is advisory. Use backticks around job ids.

4. Do not modify `scripts/ci/annotation_sweep.sh` job list in this task unless you find a factual mismatch with `annotation-sweep` job’s `needs:` graph — if mismatch exists, fix the **comment** above the sweep step in `ci.yml` first and document in commit body.
</action>
<acceptance_criteria>
- `grep -Fq "release-gate" .github/workflows/ci.yml` exits 0 (pre-existing; must remain true).
- `grep -Fq "host-integration" .github/workflows/ci.yml` exits 0.
- `grep -Fq "live-stripe" .github/workflows/ci.yml` exits 0.
- `grep -Fq "job id" .github/workflows/ci.yml` exits 0 OR `grep -Fq "Job id" .github/workflows/ci.yml` exits 0 (case-insensitive ok via two greps: run `grep -Eiq 'job id|merge-blocking|advisory' .github/workflows/ci.yml` and it must exit 0 after edits).
- `grep -Fq "\`host-integration\`" README.md` exits 0.
- `grep -Fq "\`live-stripe\`" README.md` exits 0.
- `grep -Fq "advisory" README.md` exits 0.
- `bash scripts/ci/verify_package_docs.sh` exits 0 from repo root.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh</verify>
</task>

</tasks>

<verification>
`bash scripts/ci/verify_package_docs.sh`
</verification>

<must_haves>
- Job ids in YAML keys unchanged; prose clarifies blocking vs advisory lanes.
- Root README names `host-integration` and `live-stripe` with advisory framing.
</must_haves>

<success_criteria>
- ADOPT-06: CI + guides + root README keep unambiguous merge-blocking vs advisory language without renaming stable job ids.
</success_criteria>

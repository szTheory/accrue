# Phase 34 — Patterns

Follow **Phase 20/21 UI-SPEC** for cards, grids, and hierarchy. Prefer **flat canonical URLs** + `handle_params/3` for drill context over session stacks. Centralize **nav order and labels** in one module (e.g. `AccrueAdmin.Nav`) with optional feature gating for surfaces not present in every install.

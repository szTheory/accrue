# Phase 34 — Research

**Status:** Stub — extend during `/gsd-plan-phase` or `/gsd-research-phase`.

**Pointers:** Stripe Dashboard “related objects” patterns; Filament/Nova resource consistency vs billing timelines; Accrue `AccrueAdmin.Router` + `live_session` conventions (named session, mount path).

See project research log: `.planning/research/SUMMARY.md` when updated for v1.7 operator slice.

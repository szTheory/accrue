---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/live/dashboard_live.ex
autonomous: true
requirements:
  - OPS-04
  - OPS-05
---

<objective>
Route **all operator-visible English** on `DashboardLive` through **`AccrueAdmin.Copy`** and keep markup on existing **`ax-*`** primitives so OPS-05 is satisfied for the home KPI surface; OPS-04 satisfied by not introducing non-token styling in HEEx (CSS changes only if a gap is found during implementation).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-copy-01: Drift between HEEx and tests | Medium | Follow-up plan 35-02 switches ExUnit/Playwright assertions to `Copy` / shared JS mirror; this plan leaves zero raw dashboard marketing copy in `dashboard_live.ex`. |
| T-ui-01: Token regression | Low | Only `class={...}` patterns already used elsewhere; any new rule goes into `accrue_admin/assets/css/app.css` using existing CSS variables — if truly impossible, add one-line exception note in `35-02-PLAN.md` must_haves. |
| T-a11y-01: Empty `aria_label` on linked KPI | Low | Preserve Phase 34 rule: every `KpiCard` with `href` receives `aria_label` from `Copy.dashboard_kpi_*_aria_label/0`. |
</threat_model>

<tasks>

<task id="35-01-01" type="execute">
<read_first>
- accrue_admin/lib/accrue_admin/copy.ex
- accrue_admin/lib/accrue_admin/live/dashboard_live.ex
- .planning/phases/35-summary-surfaces-test-literal-hygiene/35-UI-SPEC.md
</read_first>
<action>
Append **public** `dashboard_*` functions to `AccrueAdmin.Copy` returning **exact** strings currently rendered in `DashboardLive` for:

1. Breadcrumb sole crumb label `"Dashboard"`.
2. Page chrome: eyebrow `"Billing health"`, `<h2>` `"Local billing projections at a glance"`, and the full `<p class="ax-body ax-page-copy">` paragraph starting with `Dashboard KPIs are sourced from`.
3. Section `aria-label` values: `"Billing KPI summary"`, `"Dashboard activity"`.
4. Four KPI **labels** (`Customers`, `Active subscriptions`, `Open invoice balance`, `Webhook backlog`) and their four `<:meta>` slot strings (`Total local customer records`, `Canonical active + trialing predicates`, `Remaining amount due from local invoice projections`, `Failed + dead webhook rows waiting for operator attention`).
5. Four KPI **`aria_label`** strings already passed to `KpiCard` (`Open customers list`, `Open subscriptions list`, `Open invoices list`, `Open webhooks list`).
6. Static fragments used inside **delta** assigns: suffix `" canceling"` (concat after integer), `" open invoices"`, `" events in 24h"` — expose as `dashboard_kpi_active_subscriptions_canceling_suffix/0`, `dashboard_kpi_open_invoice_delta_suffix/0`, `dashboard_kpi_webhook_events_suffix/0`.
7. Activity cards: eyebrows + headings (`Event ledger`, `Recent local activity`, `Webhook health`, `Projection pipeline`).
8. `Timeline` attrs: `label` / `empty_label` for events column (`Recent event ledger rows`, `No local events recorded yet`) and webhooks column (`Recent webhook processing rows`, `No webhook rows recorded yet`).

Use `@doc` only where it clarifies host-contract intent; keep names grep-stable (`dashboard_` prefix).
</action>
<acceptance_criteria>
- `grep -c "def dashboard_" accrue_admin/lib/accrue_admin/copy.ex` returns an integer **≥ 18**.
- `grep -E "Billing health|Local billing projections at a glance|Total local customer records" accrue_admin/lib/accrue_admin/live/dashboard_live.ex` exits **1** (no matches — literals removed from LiveView).
</acceptance_criteria>
<verify>cd accrue_admin && mix compile --warnings-as-errors</verify>
</task>

<task id="35-01-02" type="execute">
<read_first>
- accrue_admin/lib/accrue_admin/copy.ex (after task 35-01-01)
- accrue_admin/lib/accrue_admin/live/dashboard_live.ex
</read_first>
<action>
In `AccrueAdmin.Live.DashboardLive`:

1. Add `alias AccrueAdmin.Copy`.
2. Replace every string covered in task 35-01-01 with `Copy.function_name()` inside HEEx (including `Breadcrumbs.breadcrumbs items={[%{label: Copy.dashboard_breadcrumb_home()}]}` pattern).
3. For dynamic deltas, keep `Integer.to_string/1` and `format_minor/2` in the LiveView but compose suffixes only from `Copy.dashboard_kpi_*_suffix/0` functions.

Do **not** change query logic (`dashboard_stats/0`, `recent_events/0`, `webhook_health/0`).
</action>
<acceptance_criteria>
- `grep -Fq "alias AccrueAdmin.Copy" accrue_admin/lib/accrue_admin/live/dashboard_live.ex` exits 0.
- `grep -Fq "Copy.dashboard_display_headline" accrue_admin/lib/accrue_admin/live/dashboard_live.ex` exits 0.
- `grep -Fq "<:meta><%= Copy." accrue_admin/lib/accrue_admin/live/dashboard_live.ex` exits 0 **or** equivalent `<%= Copy.dashboard_kpi_` appears inside each meta slot (grep `Copy.dashboard_kpi_` returns ≥ 4 lines).
</acceptance_criteria>
<verify>cd accrue_admin && mix compile --warnings-as-errors</verify>
</task>

</tasks>

<verification>
`cd accrue_admin && mix compile --warnings-as-errors`
</verification>

<must_haves>
- Dashboard HEEx contains **no** hard-coded operator English for chrome, KPI labels, meta, aria-labels, timeline labels, or static delta suffixes.
- `AccrueAdmin.Copy` owns the canonical strings for those surfaces.
</must_haves>

<success_criteria>
- OPS-04: Dashboard remains on semantic `ax-*` layout; any unavoidable CSS exception is deferred to plan 35-02 notes if discovered during compile.
- OPS-05: New polish on the dashboard routes through `AccrueAdmin.Copy` per REQUIREMENTS.md.
</success_criteria>

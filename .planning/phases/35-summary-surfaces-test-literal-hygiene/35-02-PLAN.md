---
phase: 35
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01-PLAN.md
files_modified:
  - accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
  - accrue_admin/test/accrue_admin/live/auth_hook_test.exs
  - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
  - examples/accrue_host/e2e/support/copy_dashboard.js
  - accrue_admin/e2e/phase7-uat.spec.js
  - examples/accrue_host/e2e/phase13-canonical-demo.spec.js
  - scripts/ci/accrue_host_browser_smoke.cjs
autonomous: true
requirements:
  - OPS-04
  - OPS-05
---

<objective>
Eliminate **divergent literals** in ExUnit, host package tests, Playwright, and CI smoke by binding assertions to **`AccrueAdmin.Copy`** (Elixir) and a **single JS mirror** for Node runners, after plan 35-01 centralizes dashboard strings.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-test-01: JS/Elixir SSOT drift | High | One new module `examples/accrue_host/e2e/support/copy_dashboard.js` exporting **exact** headline (and any other string those tests assert); PR acceptance greps both files for the same substring count; ExUnit uses `Copy.*()` only. |
| T-test-02: Host test cannot see Copy | Low | `examples/accrue_host` already depends on `accrue_admin`; `alias AccrueAdmin.Copy` in `admin_mount_test.exs`. |
| T-ci-01: Smoke script path break | Medium | Use `path.join(__dirname, ...)`-style relative require from `scripts/ci/` to the new support file; run `node scripts/ci/accrue_host_browser_smoke.cjs` locally if env allows, else `grep` require path exists. |
</threat_model>

<tasks>

<task id="35-02-01" type="execute">
<read_first>
- accrue_admin/lib/accrue_admin/copy.ex
- accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
- accrue_admin/test/accrue_admin/live/auth_hook_test.exs
</read_first>
<action>
1. In `dashboard_live_test.exs`, add `alias AccrueAdmin.Copy` and replace every `assert html =~ "..."` dashboard literal that still duplicates `Copy` (headline, KPI labels such as `Customers`, `Open invoice balance`, currency output `$42.50` may stay as formatted money, webhook type `invoice.payment_failed`, **href** paths, and aria snippets like `Open customers list`) with `Copy.*()` calls **except** dynamic fixture-derived values (`customer.updated`, processor ids) which are not copy SSOT.

2. In `auth_hook_test.exs`, replace the headline assertion with `assert html =~ Copy.dashboard_display_headline()`.

Minimum: headline + all KPI card labels + four `Open … list` aria strings must come from `Copy`.
</action>
<acceptance_criteria>
- `grep -Fq "alias AccrueAdmin.Copy" accrue_admin/test/accrue_admin/live/dashboard_live_test.exs` exits 0.
- `grep -Fq "Copy.dashboard_display_headline()" accrue_admin/test/accrue_admin/live/dashboard_live_test.exs` exits 0.
- `grep "Local billing projections at a glance" accrue_admin/test/accrue_admin/live/dashboard_live_test.exs` exits **1**.
- `grep -Fq "Copy.dashboard_display_headline()" accrue_admin/test/accrue_admin/live/auth_hook_test.exs` exits 0.
</acceptance_criteria>
<verify>cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs test/accrue_admin/live/auth_hook_test.exs --warnings-as-errors</verify>
</task>

<task id="35-02-02" type="execute">
<read_first>
- examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
- accrue_admin/lib/accrue_admin/copy.ex
</read_first>
<action>
In `admin_mount_test.exs`, `alias AccrueAdmin.Copy` (if not already present) and change the mount assertion to `assert html =~ Copy.dashboard_display_headline()` instead of the raw string literal.
</action>
<acceptance_criteria>
- `grep "Local billing projections at a glance" examples/accrue_host/test/accrue_host_web/admin_mount_test.exs` exits **1**.
- `grep -Fq "Copy.dashboard_display_headline()" examples/accrue_host/test/accrue_host_web/admin_mount_test.exs` exits 0.
</acceptance_criteria>
<verify>cd examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs --warnings-as-errors</verify>
</task>

<task id="35-02-03" type="execute">
<read_first>
- accrue_admin/lib/accrue_admin/copy.ex (confirm `dashboard_display_headline/0` name)
- examples/accrue_host/e2e/phase13-canonical-demo.spec.js
- accrue_admin/e2e/phase7-uat.spec.js
- scripts/ci/accrue_host_browser_smoke.cjs
</read_first>
<action>
1. Create **`examples/accrue_host/e2e/support/copy_dashboard.js`** exporting **CommonJS** `module.exports = { ... }` with **byte-identical** values to these `AccrueAdmin.Copy` functions (add keys only for strings Playwright asserts):
   - `DASHBOARD_DISPLAY_HEADLINE` ↔ `dashboard_display_headline/0`
   - `DASHBOARD_BREADCRUMB_HOME` ↔ `dashboard_breadcrumb_home/0` (breadcrumb / heading text `"Dashboard"`)
   - `DASHBOARD_KPI_OPEN_INVOICE_BALANCE_LABEL` ↔ `dashboard_kpi_open_invoice_balance_label/0`
   - `DASHBOARD_KPI_WEBHOOK_BACKLOG_LABEL` ↔ `dashboard_kpi_webhook_backlog_label/0`

   File header comment: `// SYNC: accrue_admin/lib/accrue_admin/copy.ex — dashboard_* functions`.

2. In `examples/accrue_host/e2e/phase13-canonical-demo.spec.js`, add `const { DASHBOARD_DISPLAY_HEADLINE, ... } = require("./support/copy_dashboard.js");` and replace both `getByText("Local billing projections at a glance")` usages with `getByText(DASHBOARD_DISPLAY_HEADLINE)`.

3. In `scripts/ci/accrue_host_browser_smoke.cjs`, `require` the same support file using a relative path from `scripts/ci/` (`../../examples/accrue_host/e2e/support/copy_dashboard.js`) and swap the dashboard headline literal for `DASHBOARD_DISPLAY_HEADLINE`.

4. In `accrue_admin/e2e/phase7-uat.spec.js`, `require("../../examples/accrue_host/e2e/support/copy_dashboard.js")` (path from `accrue_admin/e2e/` reaches monorepo `examples/accrue_host/...`) and replace literals for: heading `"Dashboard"`, headline, `"Open invoice balance"`, `"Webhook backlog"` with the matching exported constants (`getByRole("heading", { name: DASHBOARD_BREADCRUMB_HOME })`, etc.).
</action>
<acceptance_criteria>
- `test -f examples/accrue_host/e2e/support/copy_dashboard.js` exits 0.
- `grep -F "Local billing projections at a glance" examples/accrue_host/e2e/phase13-canonical-demo.spec.js` exits **1**.
- `grep -F "DASHBOARD_DISPLAY_HEADLINE" examples/accrue_host/e2e/phase13-canonical-demo.spec.js` exits 0.
- `grep -F "copy_dashboard.js" scripts/ci/accrue_host_browser_smoke.cjs` exits 0.
- `grep -F "copy_dashboard.js" accrue_admin/e2e/phase7-uat.spec.js` exits 0.
- `grep -E "Open invoice balance|Webhook backlog|Local billing projections" accrue_admin/e2e/phase7-uat.spec.js` exits **1** (no raw dashboard copy literals left in that spec).
</acceptance_criteria>
<verify>cd examples/accrue_host && npx playwright test e2e/phase13-canonical-demo.spec.js --project=chromium-desktop</verify>
</task>

</tasks>

<verification>
`cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs test/accrue_admin/live/auth_hook_test.exs --warnings-as-errors`
`cd examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs --warnings-as-errors`
</verification>

<must_haves>
- No test or CI file contains a bare duplicate of `dashboard_display_headline` text except `copy.ex` and `copy_dashboard.js`.
- ExUnit dashboard coverage references `AccrueAdmin.Copy` for static operator strings.
</must_haves>

<success_criteria>
- OPS-05: Playwright and LiveView tests do not embed a second unsynchronized source for the dashboard headline; other asserted dashboard chrome uses `Copy` in Elixir.
</success_criteria>

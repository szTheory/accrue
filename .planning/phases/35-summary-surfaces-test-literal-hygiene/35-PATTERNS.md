# Phase 35 — Patterns

**Strings:** `AccrueAdmin.Copy` at LiveView boundary; dumb function components receive `label`/`value` data, not raw English in HEEx.

**Tokens:** semantic variables only; exceptions listed in phase notes + registry doc.

**Tests:** assert copy via same module or role-based selectors; avoid hard-coding duplicate literals in Playwright.

# Phase 35 — Research

**Status:** Stub.

**Pointers:** Gettext-in-library vs `Copy` module hybrid; accessible selectors (`getByRole`) vs stable `data-role` for KPI grids; token-only KPI visuals without new charting dependencies.

---
phase: 36
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - CONTRIBUTING.md
  - scripts/ci/README.md
  - scripts/ci/verify_package_docs.sh
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-02
  - ADOPT-03
  - ADOPT-04
  - ADOPT-05
  - ADOPT-06
---

<objective>
Give contributors an **ADOPT requirement → verifier / test entrypoint** map (per `v1.7-MILESTONE-AUDIT.md` integration gap), link it from `CONTRIBUTING.md`, and add **triage prefixes** on `verify_package_docs.sh` failures so mega-script output names the relevant **ADOPT** row where practical without rewriting every assertion.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-doc-03: Stale map vs scripts | Medium | README must reference **file paths that exist** (`scripts/ci/verify_package_docs.sh`, `scripts/ci/verify_verify01_readme_contract.sh`, `.github/workflows/ci.yml`) and phase dirs `.planning/phases/32-*` / `33-*` for VERIFICATION owners; run `bash scripts/ci/verify_package_docs.sh` after edits. |
| T-doc-04: Misleading REQ blame on unrelated failures | Low | Only prefix messages inside `verify_package_docs.sh`’s existing `fail()` helper or at **cluster headers** (comment blocks); do not change assertion semantics. |
</threat_model>

<tasks>

<task id="36-02-01" type="execute">
<read_first>
- CONTRIBUTING.md
- scripts/ci/verify_package_docs.sh
- scripts/ci/verify_verify01_readme_contract.sh
- `.planning/milestones/v1.7-MILESTONE-AUDIT.md`
- .planning/phases/32-adoption-discoverability-doc-graph/32-VERIFICATION.md
- .planning/phases/33-installer-host-contracts-ci-clarity/33-VERIFICATION.md
- accrue/test/accrue/docs/package_docs_verifier_test.exs
</read_first>
<action>
1. Create **new** `scripts/ci/README.md` with:
   - H1: `# scripts/ci — contributor map`
   - Section `## ADOPT gates (v1.7 adoption milestone)` containing a Markdown table with columns: `REQ-ID`, `Primary script(s) or artifact`, `Package ExUnit (if any)`, `Phase VERIFICATION owner`.
   - Rows for **ADOPT-01** through **ADOPT-06** filled from `32-VERIFICATION.md` / `33-VERIFICATION.md` evidence columns (scripts named explicitly; include `package_docs_verifier_test.exs` only if that file actually asserts strings for that REQ).
   - Section `## When package docs verification fails` explaining stderr lines are prefixed by `[verify_package_docs]` and pointing readers to the triage bullets for ADOPT mapping.

2. In `CONTRIBUTING.md`, after the paragraph ending with “Keep webhook secrets…” (still under **Running the release gate locally** or immediately following that section if clearer), add a new subsection titled exactly `## Adoption (ADOPT) CI contracts` with 2–4 sentences that link to `scripts/ci/README.md` using a **relative** Markdown link from repo root: `[scripts/ci/README.md](scripts/ci/README.md)`.

3. In `scripts/ci/verify_package_docs.sh`, update only the `fail()` helper so every stderr line begins with the literal substring `[verify_package_docs]` followed by the existing human-readable message (keep `exit 1`). Do **not** change `require_fixed` / `require_regex` signatures.

4. In `scripts/ci/README.md`, add subsection `### Triage: verify_package_docs.sh` listing **five or more** bullet lines of the form ``- `ADOPT-0X` — typical failure substring / file pair`` mapping common `require_fixed` targets (README, host README, first_hour, RELEASING, workflows) to ADOPT rows per `32-VERIFICATION.md` / `33-VERIFICATION.md`.

5. Run from repo root: `bash scripts/ci/verify_package_docs.sh` and ensure exit code 0.
</action>
<acceptance_criteria>
- `test -f scripts/ci/README.md` exits 0.
- `grep -Fq "## Adoption (ADOPT) CI contracts" CONTRIBUTING.md` exits 0.
- `grep -Fq "](scripts/ci/README.md)" CONTRIBUTING.md` exits 0.
- `grep -Fq "ADOPT-01" scripts/ci/README.md && grep -Fq "ADOPT-06" scripts/ci/README.md` exits 0.
- `grep -Fq 'echo "[verify_package_docs]' scripts/ci/verify_package_docs.sh || grep -Fq "[verify_package_docs]" scripts/ci/verify_package_docs.sh` exits 0.
- `grep -c "ADOPT-0" scripts/ci/README.md` exits 0 with stdout **greater than or equal to** `5`.
- `bash scripts/ci/verify_package_docs.sh` from repo root exits 0.
</acceptance_criteria>
<verify>bash scripts/ci/verify_package_docs.sh</verify>
</task>

</tasks>

<verification>
`bash scripts/ci/verify_package_docs.sh`
</verification>

<must_haves>
- New `scripts/ci/README.md` is the SSOT table for ADOPT ↔ verifier ownership.
- `CONTRIBUTING.md` links maintainers into that map.
- `verify_package_docs.sh` failures are easier to triage via `[verify_package_docs]` + ADOPT hints.
</must_haves>

<success_criteria>
- ROADMAP Phase 36 success criterion **2** satisfied: contributor-facing map + labeled failures where practical.
</success_criteria>

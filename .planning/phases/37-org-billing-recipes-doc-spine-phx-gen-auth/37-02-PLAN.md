---
phase: 37-org-billing-recipes-doc-spine-phx-gen-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/auth_adapters.md
  - accrue/guides/sigra_integration.md
  - accrue/test/accrue/docs/community_auth_test.exs
  - accrue/test/accrue/docs/sigra_integration_guide_test.exs
autonomous: true
requirements: [ORG-05, ORG-06]
must_haves:
  truths:
    - "auth_adapters.md immediately frames Sigra as optional and points readers at the non-Sigra org billing spine without moving the full narrative into the adapter reference."
    - "sigra_integration.md surfaces a not-using-Sigra escape hatch before dependency instructions so installers never think Sigra is mandatory."
  artifacts:
    - path: "accrue/guides/auth_adapters.md"
      provides: "Choosing an adapter / without Sigra block + spine link."
    - path: "accrue/guides/sigra_integration.md"
      provides: "Top callout + link to auth_adapters + organization spine."
    - path: "accrue/test/accrue/docs/sigra_integration_guide_test.exs"
      provides: "Contract for Sigra guide callout ordering."
  key_links:
    - from: "accrue/guides/auth_adapters.md"
      to: "accrue/guides/organization_billing.md"
      via: "markdown link"
      pattern: "organization_billing\\.md"
---

<objective>
Update **auth** and **Sigra** entry guides so non-Sigra org billing is the obvious default path (CONTEXT D-04–D-06, D-08) while preserving `auth_adapters.md` as the `Accrue.Auth` contract SSOT.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/37-org-billing-recipes-doc-spine-phx-gen-auth/37-CONTEXT.md
@accrue/guides/organization_billing.md
@accrue/guides/auth_adapters.md
@accrue/guides/sigra_integration.md
@accrue/test/accrue/docs/community_auth_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth_adapters.md non-Sigra framing</name>
  <read_first>
    accrue/guides/auth_adapters.md
    accrue/guides/organization_billing.md
  </read_first>
  <files>accrue/guides/auth_adapters.md</files>
  <acceptance_criteria>
    - `rg -n 'Choosing an adapter|without Sigra|optional' accrue/guides/auth_adapters.md` matches within the first **40** lines of the file (opening framing).
    - `rg -n 'organization_billing\\.md' accrue/guides/auth_adapters.md` finds a markdown link to the spine.
    - `rg -n 'Accrue\\.Integrations\\.Sigra' accrue/guides/auth_adapters.md` still appears in the Sigra example section (do not remove adapter documentation).
  </acceptance_criteria>
  <action>Immediately after the opening paragraph that ends with “without knowing how the host signs users in.” (before `## Required callbacks`), insert a new subsection titled **## Choosing an adapter (Sigra is optional)** with 2–4 sentences: Accrue is auth-agnostic; when `:sigra` is already a dependency you may use `Accrue.Integrations.Sigra` and `guides/sigra_integration.md`; otherwise implement `Accrue.Auth` (link to existing PhxGenAuth/Pow/Assent sections below). Add one bullet or sentence with a markdown link `[Organization billing (session → billable)](organization_billing.md)` for the non-Sigra org-shaped path. Do **not** move the ORG-03 narrative into this file beyond this short hub text (D-04).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/community_auth_test.exs</automated>
  </verify>
  <done>Auth adapter guide advertises the spine without duplicating it.</done>
</task>

<task type="auto">
  <name>Task 2: Extend community_auth_test for new anchors</name>
  <read_first>
    accrue/test/accrue/docs/community_auth_test.exs
    accrue/guides/auth_adapters.md
  </read_first>
  <files>accrue/test/accrue/docs/community_auth_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'Choosing an adapter|organization_billing' accrue/test/accrue/docs/community_auth_test.exs` finds new assertions.
  </acceptance_criteria>
  <action>Add one test (or extend an existing test) that asserts `File.read!("guides/auth_adapters.md")` contains the literal substrings `Choosing an adapter` and `organization_billing.md`.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/community_auth_test.exs</automated>
  </verify>
  <done>Auth guide hub strings are executable-locked.</done>
</task>

<task type="auto">
  <name>Task 3: sigra_integration.md callout + contract test</name>
  <read_first>
    accrue/guides/sigra_integration.md
    accrue/guides/auth_adapters.md
  </read_first>
  <files>accrue/guides/sigra_integration.md, accrue/test/accrue/docs/sigra_integration_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'Not using Sigra|### Not using Sigra' accrue/guides/sigra_integration.md` finds the callout.
    - The contract test in `sigra_integration_guide_test.exs` asserts the index of `Not using Sigra` is less than the index of `## Add the dependency` in the guide file (documented in test failure message if violated).
    - `rg -n 'auth_adapters\\.md|organization_billing\\.md' accrue/guides/sigra_integration.md` finds both links in the callout region (first 35 lines).
    - `test -f accrue/test/accrue/docs/sigra_integration_guide_test.exs` succeeds.
    - `rg -n 'defmodule Accrue\\.Docs\\.SigraIntegrationGuideTest' accrue/test/accrue/docs/sigra_integration_guide_test.exs` finds module.
  </acceptance_criteria>
  <action>Insert a short callout block at the top of `sigra_integration.md` **after** the existing intro paragraph that ends with “published `Accrue.Auth` callbacks.” and **before** `## Add the dependency`: title `### Not using Sigra?`, 3–5 sentences stating this guide assumes Sigra is already chosen; if not, read `guides/auth_adapters.md` first and use `[Organization billing (session → billable)](organization_billing.md)` for the non-Sigra org path; do not reference `Accrue.Integrations.Sigra` without the dependency. Create `accrue/test/accrue/docs/sigra_integration_guide_test.exs` that reads `guides/sigra_integration.md` and asserts it contains `Not using Sigra`, `auth_adapters.md`, and `organization_billing.md`, and that the file position of `Not using Sigra` is before `## Add the dependency` (compare `String.split/2` indices in Elixir).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/sigra_integration_guide_test.exs</automated>
  </verify>
  <done>Sigra guide has a tested escape hatch ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra guide → dependency install | Reader must not install Sigra by mistake when org path is intended |
| Auth guide → adapter code | PhxGenAuth module remains canonical implementation detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Security Misconfiguration | Sigra guide ordering | mitigate | Callout before `## Add the dependency` + contract test on string indices. |
| T-37-02-02 | Information Disclosure | Cross-links | mitigate | Link only to in-repo guides; no live tokens. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/docs/community_auth_test.exs`
- `cd accrue && mix test test/accrue/docs/sigra_integration_guide_test.exs`
- `cd accrue && MIX_ENV=test mix docs`
</verification>

<success_criteria>
- Both guides cross-link the spine; tests enforce anchors.
</success_criteria>

## PLANNING COMPLETE

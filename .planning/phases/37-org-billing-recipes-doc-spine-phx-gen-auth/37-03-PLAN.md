---
phase: 37-org-billing-recipes-doc-spine-phx-gen-auth
plan: 03
type: execute
wave: 2
depends_on: ["37-01-PLAN.md", "37-02-PLAN.md"]
files_modified:
  - accrue/lib/mix/tasks/accrue.install.ex
  - accrue/README.md
  - accrue/guides/quickstart.md
  - accrue/guides/finance-handoff.md
  - accrue/test/accrue/docs/organization_billing_guide_test.exs
autonomous: true
requirements: [ORG-05, ORG-06]
must_haves:
  truths:
    - "mix accrue.install non-Sigra scrollback points at guides/auth_adapters.md and guides/organization_billing.md with stable copy."
    - "README and quickstart list the org billing spine alongside existing entry guides (D-03)."
  artifacts:
    - path: "accrue/lib/mix/tasks/accrue.install.ex"
      provides: "Installer pointer lines in print_auth_guidance/1."
    - path: "accrue/README.md"
      provides: "Start here bullet for organization billing."
    - path: "accrue/guides/quickstart.md"
      provides: "Focused guides index entry."
---

<objective>
Wire **discoverability** (installer + README + quickstart + light finance-handoff pointer) and extend the spine contract test to cover installer/README literals (CONTEXT D-07, D-03, roadmap success criterion 1).
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/37-org-billing-recipes-doc-spine-phx-gen-auth/37-CONTEXT.md
@accrue/lib/mix/tasks/accrue.install.ex
@accrue/README.md
@accrue/guides/quickstart.md
@accrue/guides/finance-handoff.md
@accrue/test/accrue/docs/organization_billing_guide_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer print_auth_guidance pointers</name>
  <read_first>
    accrue/lib/mix/tasks/accrue.install.ex
  </read_first>
  <files>accrue/lib/mix/tasks/accrue.install.ex</files>
  <acceptance_criteria>
    - `rg -n 'guides/auth_adapters\\.md' accrue/lib/mix/tasks/accrue.install.ex` finds the literal inside `print_auth_guidance` non-Sigra clause (the `defp print_auth_guidance\\(_project\\)` function body).
    - `rg -n 'guides/organization_billing\\.md' accrue/lib/mix/tasks/accrue.install.ex` finds the literal in the same function body.
    - `rg -n 'report\\(' accrue/lib/mix/tasks/accrue.install.ex` still only uses `report/1` for user-visible strings (no secret values added).
  </acceptance_criteria>
  <action>In `defp print_auth_guidance(_project)`, after the existing `report("Community auth adapters can implement Accrue.Auth")` line, append **two** new `report/1` lines with these **exact** string literals (including paths): `report("Non-Sigra org billing walkthrough: guides/organization_billing.md")` and `report("Accrue.Auth adapter reference: guides/auth_adapters.md")`.</action>
  <verify>
    <automated>cd accrue && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Installer emits stable paths for org billing + auth reference.</done>
</task>

<task type="auto">
  <name>Task 2: README + quickstart discoverability</name>
  <read_first>
    accrue/README.md
    accrue/guides/quickstart.md
  </read_first>
  <files>accrue/README.md, accrue/guides/quickstart.md</files>
  <acceptance_criteria>
    - `rg -n 'organization_billing\\.md' accrue/README.md` finds a markdown link under `## Start here` (or bullet list in that section).
    - `rg -n 'organization_billing\\.md' accrue/guides/quickstart.md` finds a bullet under `## Focused guides` (or equivalent index section).
  </acceptance_criteria>
  <action>Add one bullet to `accrue/README.md` in the **Start here** list: link text `Organization billing (non-Sigra)` pointing to `guides/organization_billing.md` with one clause that this is the session→billable org path. Add one bullet to `accrue/guides/quickstart.md` under **Focused guides** with the same link.</action>
  <verify>
    <automated>rg -n 'organization_billing' accrue/README.md accrue/guides/quickstart.md</automated>
  </verify>
  <done>Top-level indexes surface the spine.</done>
</task>

<task type="auto">
  <name>Task 3: finance-handoff cross-link</name>
  <read_first>
    accrue/guides/finance-handoff.md
  </read_first>
  <files>accrue/guides/finance-handoff.md</files>
  <acceptance_criteria>
    - `rg -n 'organization_billing\\.md' accrue/guides/finance-handoff.md` finds one markdown link before the first `## Stripe Revenue Recognition` heading.
  </acceptance_criteria>
  <action>After the paragraph ending with “parallel ledger.” (line ~9) and **before** `## Stripe Revenue Recognition`, insert a single short paragraph: “For **which host row** owns the Stripe Customer (user vs organization) and how session maps to billable, see [Organization billing (session → billable)](organization_billing.md).”</action>
  <verify>
    <automated>rg -n 'organization_billing' accrue/guides/finance-handoff.md</automated>
  </verify>
  <done>Finance guide points readers at billable ownership narrative.</done>
</task>

<task type="auto">
  <name>Task 4: Extend organization_billing_guide_test for installer + README</name>
  <read_first>
    accrue/test/accrue/docs/organization_billing_guide_test.exs
    accrue/lib/mix/tasks/accrue.install.ex
    accrue/README.md
  </read_first>
  <files>accrue/test/accrue/docs/organization_billing_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'accrue\\.install\\.ex|README\\.md' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds `File.read!` paths or `@installer` constants pointing at those files.
    - `rg -n 'guides/organization_billing\\.md' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds assertion on README content.
  </acceptance_criteria>
  <action>Add two tests. (1) Define `@installer Path.expand(Path.join([__DIR__, "..", "..", "..", "lib", "mix", "tasks", "accrue.install.ex"]))` — `__DIR__` is `accrue/test/accrue/docs`, so three `..` segments reach the `accrue/` app root. `File.read!(@installer)` must include both literal substrings `guides/organization_billing.md` and `guides/auth_adapters.md` inside the `defp print_auth_guidance(_project)` function body (grep the read string in Elixir with `String.contains?/2` twice). (2) Define `@readme Path.expand(Path.join([__DIR__, "..", "..", "..", "README.md"]))` and assert `File.read!(@readme)` contains `organization_billing.md`.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs</automated>
  </verify>
  <done>Discoverability strings are regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installer stdout | Must never print secrets; only paths and existing guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Information Disclosure | Installer messages | mitigate | Only add static path strings already public in repo. |
| T-37-03-02 | Tampering | N/A | accept | — |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs`
- `cd accrue && mix test test/accrue/docs/community_auth_test.exs test/accrue/docs/sigra_integration_guide_test.exs`
- `cd accrue && MIX_ENV=test mix docs`
</verification>

<success_criteria>
- ROADMAP success criterion “linked from auth_adapters and/or sigra_integration” remains true with tests.
- Installer non-Sigra branch names both guides explicitly.
</success_criteria>

## PLANNING COMPLETE

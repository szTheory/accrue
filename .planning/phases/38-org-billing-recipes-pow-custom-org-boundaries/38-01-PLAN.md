---
phase: 38-org-billing-recipes-pow-custom-org-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/organization_billing.md
  - accrue/guides/auth_adapters.md
  - accrue/test/accrue/docs/organization_billing_guide_test.exs
autonomous: true
requirements: [ORG-07]
must_haves:
  truths:
    - "Pow recipe is version-agnostic at the Accrue boundary: Pow.Plug identity + explicit membership-gated organization + MyApp.Auth.Pow adapter pointer."
    - "Honest community-maintenance / upgrade posture appears as prose, not dependency evangelism."
  artifacts:
    - path: "accrue/guides/organization_billing.md"
      provides: "ORG-07 H2 + numbered checklist mirroring phx.gen.auth shape."
    - path: "accrue/guides/auth_adapters.md"
      provides: "Pow section links into the spine Pow checklist."
    - path: "accrue/test/accrue/docs/organization_billing_guide_test.exs"
      provides: "ORG-07 literal anchors cannot be deleted silently."
  key_links:
    - from: "accrue/guides/auth_adapters.md"
      to: "accrue/guides/organization_billing.md"
      via: "markdown link"
      pattern: "organization_billing\\.md"
---

<objective>
Ship **ORG-07** by extending `organization_billing.md` with a **Pow-oriented checklist** (membership-gated org + `MyApp.Auth.Pow` + upgrade notes), cross-linking from `auth_adapters.md`, and locking literals in `organization_billing_guide_test.exs`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-CONTEXT.md
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-RESEARCH.md
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-VALIDATION.md
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@accrue/guides/organization_billing.md
@accrue/guides/auth_adapters.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pow-oriented checklist to organization_billing.md</name>
  <read_first>
    accrue/guides/organization_billing.md
    .planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-CONTEXT.md
  </read_first>
  <files>accrue/guides/organization_billing.md</files>
  <acceptance_criteria>
    - `rg -n '^## Pow-oriented checklist \\(ORG-07\\)' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'Pow\\.Plug\\.current_user' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'MyApp\\.Auth\\.Pow' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'config :accrue, :auth_adapter, MyApp\\.Auth\\.Pow' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'fetch_current_organization' accrue/guides/organization_billing.md` returns ≥1 match inside the Pow section context (file-level grep is sufficient if section is single contiguous block).
    - `rg -n 'community-maintained|community maintained|upgrade' accrue/guides/organization_billing.md` matches at least one line documenting maintenance/upgrade honesty.
    - `rg -n '^### ' accrue/guides/organization_billing.md` shows new H3 headings under the Pow H2 (≥3 H3 lines between Pow H2 and next H2).
  </acceptance_criteria>
  <action>Insert a new section **immediately before** the existing `## User-as-billable (bounded aside)` heading. Add H2 **exactly** `## Pow-oriented checklist (ORG-07)`. Under it, add **at least** these H3 headings in order: `### Identity with Pow`, `### Active organization and membership`, `### Billable row and host facade`, `### Accrue.Auth configuration`, `### Maintenance and upgrades`. Content requirements: (1) state identity comes from `Pow.Plug.current_user/1`; (2) restate that **active organization** still requires the same **membership-gated** `fetch_current_organization` pattern as the phx.gen.auth mainline — Pow does not infer org tenancy; (3) `use Accrue.Billable` on `MyApp.Accounts.Organization` for org-shaped billing; (4) host `MyApp.Billing` org flows pass **`Organization`**; (5) `config :accrue, :auth_adapter, MyApp.Auth.Pow` with explicit instruction to copy **`MyApp.Auth.Pow`** module body from [`auth_adapters.md`](auth_adapters.md); (6) one short paragraph on **community maintenance** + pinning `pow` deps + re-verifying Plug integration after upgrades (no specific version numbers). Remove or rewrite the single sentence in `## User-as-billable (bounded aside)` that says Pow is deferred to Phase 38 if it would contradict the new section (Pow depth now exists — replace with forward pointer only to **ORG-08** / custom org if needed).</action>
  <verify>
    <automated>cd accrue && MIX_ENV=test mix docs</automated>
  </verify>
  <done>Pow checklist shipped in the org billing spine with ORG-07 heading and contract-aligned content.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link Pow adapter section to spine</name>
  <read_first>
    accrue/guides/auth_adapters.md
    accrue/guides/organization_billing.md
  </read_first>
  <files>accrue/guides/auth_adapters.md</files>
  <acceptance_criteria>
    - `rg -n '## MyApp\\.Auth\\.Pow' accrue/guides/auth_adapters.md` finds the section.
    - `rg -n 'organization_billing\\.md' accrue/guides/auth_adapters.md` finds a markdown link **on or after** the `## MyApp.Auth.Pow` heading line and **before** the next `## ` H2 heading (`## MyApp.Auth.Assent`).
    - The linked text or surrounding sentence includes the substring `Pow-oriented` OR `ORG-07` (prove intentional cross-nav).
  </acceptance_criteria>
  <action>Under `## MyApp.Auth.Pow` in `auth_adapters.md`, add **one short paragraph** after the intro sentence (before the fenced `defmodule MyApp.Auth.Pow` code block) containing a relative markdown link to `organization_billing.md` and explicitly naming **active organization** + **ORG-07** so readers know where the checklist lives.</action>
  <verify>
    <automated>cd accrue && MIX_ENV=test mix docs</automated>
  </verify>
  <done>Pow adapter SSOT points at the new spine section.</done>
</task>

<task type="auto">
  <name>Task 3: Extend organization_billing_guide_test for ORG-07</name>
  <read_first>
    accrue/test/accrue/docs/organization_billing_guide_test.exs
    accrue/guides/organization_billing.md
  </read_first>
  <files>accrue/test/accrue/docs/organization_billing_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'ORG-07' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds the substring in the needle list or assertion.
    - `rg -n 'MyApp\\.Auth\\.Pow' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds the substring.
    - `rg -n 'Pow\\.Plug\\.current_user' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds the substring.
    - `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs` exits 0.
  </acceptance_criteria>
  <action>Append to the existing `for needle <- [` list in `test "organization billing guide keeps mandatory org billing anchors"` these exact string needles: `"ORG-07"`, `"MyApp.Auth.Pow"`, `"Pow.Plug.current_user"` (preserve list syntax commas).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs</automated>
  </verify>
  <done>ORG-07 anchors are executable-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader → host security posture | Pow checklist must not imply org scope is automatic from Pow |
| Doc → tests | Tests lock only public doc literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Elevation of Privilege | Pow org guidance | mitigate | Explicit membership gate + `fetch_current_organization` restatement in checklist |
| T-38-01-02 | Information Disclosure | auth_adapters link | accept | Public docs only |

## OWASP ASVS L1 Mapping

| ASVS | Control | Coverage |
|------|---------|----------|
| V2 | Authentication | Pow identity boundary documented; org is separate gate |

## Residual Risks

| Risk ID | Description | Severity | Accepted By | Expiry / Revisit |
|---------|-------------|----------|-------------|------------------|
| R-38-01-01 | ExDoc anchor drift for deep links | low | Phase owner | next doc refactor |

## Blocklist / Never-Do

- Do not claim Pow supplies organization tenancy.
- Do not add `:pow` as an Accrue package dependency.

</threat_model>

<verification>
1. `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs`
2. `cd accrue && MIX_ENV=test mix docs`
3. `rg -n '^## Pow-oriented checklist \\(ORG-07\\)' accrue/guides/organization_billing.md`
</verification>

<success_criteria>
- ORG-07 acceptance in `.planning/REQUIREMENTS.md` is satisfied by committed docs + tests after checkbox flip at phase close.
- No new Accrue Hex dependencies.
</success_criteria>

---
phase: 38-org-billing-recipes-pow-custom-org-boundaries
plan: 02
type: execute
wave: 2
depends_on: ["38-01-PLAN.md"]
files_modified:
  - accrue/guides/organization_billing.md
  - accrue/test/accrue/docs/organization_billing_guide_test.exs
autonomous: true
requirements: [ORG-08]
must_haves:
  truths:
    - "Custom org recipe names LiveView admin, context queries, webhook replay, and export scoping with explicit ORG-03 anti-patterns."
    - "`Accrue.Auth` actor alignment for privileged tooling is addressed in prose."
  artifacts:
    - path: "accrue/guides/organization_billing.md"
      provides: "ORG-08 H2 + anti-pattern / obligation tables."
    - path: "accrue/test/accrue/docs/organization_billing_guide_test.exs"
      provides: "ORG-08 literal anchors."
  key_links:
    - from: "accrue/guides/organization_billing.md"
      to: ".planning/milestones/v1.3-REQUIREMENTS.md"
      via: "ORG-03 anchor"
      pattern: "v1\\.3-REQUIREMENTS|ORG-03"
---

<objective>
Ship **ORG-08** by adding a **custom organization model** section to `organization_billing.md` with ORG-03-aligned **anti-patterns** and **host obligations** across public, admin, webhook replay, and export paths, plus contract-test needles.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-CONTEXT.md
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-RESEARCH.md
@.planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-PATTERNS.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@accrue/guides/organization_billing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Custom organization model (ORG-08) section</name>
  <read_first>
    accrue/guides/organization_billing.md
    .planning/phases/38-org-billing-recipes-pow-custom-org-boundaries/38-CONTEXT.md
    .planning/milestones/v1.3-REQUIREMENTS.md
  </read_first>
  <files>accrue/guides/organization_billing.md</files>
  <acceptance_criteria>
    - `rg -n '^## Custom organization model \\(ORG-08\\)' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'webhook replay' accrue/guides/organization_billing.md` returns ≥1 match in the file after the ORG-08 H2 line (use `rg -n` visually; grep file suffices if unique).
    - `rg -n 'LiveView|on_mount' accrue/guides/organization_billing.md` matches at least one line mentioning LiveView scoping (`LiveView` OR `on_mount`).
    - `rg -n 'actor_id' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'Anti-pattern' accrue/guides/organization_billing.md` returns ≥1 match (table title or row label).
    - `rg -n '\\| public \\|' accrue/guides/organization_billing.md` OR `rg -n 'public.*admin.*webhook' accrue/guides/organization_billing.md` confirms path-class vocabulary ties to ORG-03 (all four literals **public**, **admin**, **webhook replay**, **export** appear as plain text in the ORG-08 section — verify with `rg -n 'public|admin|webhook replay|export' accrue/guides/organization_billing.md` counting ≥4 distinct path-class mentions near ORG-08).
    - `rg -n 'ORG-08' accrue/guides/organization_billing.md` returns ≥1 match.
  </acceptance_criteria>
  <action>Insert a new H2 **immediately before** `## User-as-billable (bounded aside)` **unless** Plan 01 already inserted Pow before that heading — in that case, insert **after** the entire `## Pow-oriented checklist (ORG-07)` section and **still before** `## User-as-billable (bounded aside)`. H2 title must be exactly `## Custom organization model (ORG-08)`. Include: (1) short paragraph that custom tenancy keys (subdomain, header, alternate session keys) must still resolve to a **membership-verified** `Organization`; (2) a **markdown table** with columns exactly: `Anti-pattern` | `ORG-03 path class` | `Why it violates` | `Host obligation` with **≥4 rows** where `ORG-03 path class` cells are exactly one of: `public`, `admin`, `webhook replay`, `export` (each appears ≥1 time); (3) subsection **LiveView admin** mandating org scope from session/`on_mount` membership check, not **only** `live_action` params; (4) subsection **Context functions** mandating first-arg scope or explicit org id from verified session; (5) subsection **Webhook replay** tying handler queries to event metadata billable scope and warning against global `Repo.all`; (6) subsection **`Accrue.Auth` actor alignment** explaining `actor_id/1` must match the real acting principal for audit — no silent superuser widening. Link ORG-03 canonical text using the existing repo pattern URL to `v1.3-REQUIREMENTS.md` or relative path consistent with the ORG-03 table in the same file.</action>
  <verify>
    <automated>cd accrue && MIX_ENV=test mix docs</automated>
  </verify>
  <done>ORG-08 narrative and anti-pattern catalog exist in spine.</done>
</task>

<task type="auto">
  <name>Task 2: Extend organization_billing_guide_test for ORG-08</name>
  <read_first>
    accrue/test/accrue/docs/organization_billing_guide_test.exs
    accrue/guides/organization_billing.md
  </read_first>
  <files>accrue/test/accrue/docs/organization_billing_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'ORG-08' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds the substring in the needle list.
    - `rg -n 'Custom organization model' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds the substring.
    - `rg -n 'Anti-pattern' accrue/test/accrue/docs/organization_billing_guide_test.exs` finds the substring.
    - `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs` exits 0.
  </acceptance_criteria>
  <action>Append to the existing `for needle <- [` list these exact string needles: `"ORG-08"`, `"Custom organization model"`, `"Anti-pattern"`.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs</automated>
  </verify>
  <done>ORG-08 anchors are executable-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader → ORG-03 | Anti-pattern table must map to path classes |
| Replay tooling | Doc warns on elevated access patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T-38-02-01 | Elevation of Privilege | Custom org doc | mitigate | Explicit anti-patterns for param-only org selection + global queries |
| T-38-02-02 | Repudiation | actor_id guidance | mitigate | State audit principal alignment |

## OWASP ASVS L1 Mapping

| ASVS | Control | Coverage |
|------|---------|----------|
| V4 | Access control | Context + LV scoping obligations spelled |

## Residual Risks

| Risk ID | Description | Severity | Accepted By | Expiry / Revisit |
|---------|-------------|----------|-------------|------------------|
| R-38-02-01 | Host-specific replay frameworks vary | low | Docs only | Phase 39 proof row |

## Blocklist / Never-Do

- Do not endorse `live_action` org id without membership verification.
- Do not suggest global billing queries for replay handlers.

</threat_model>

<verification>
1. `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs`
2. `cd accrue && MIX_ENV=test mix docs`
3. `rg -n '^## Custom organization model \\(ORG-08\\)' accrue/guides/organization_billing.md`
</verification>

<success_criteria>
- ORG-08 acceptance in `.planning/REQUIREMENTS.md` is satisfied by committed docs + tests at phase close.
- ORG-03 cross-links remain valid.
</success_criteria>

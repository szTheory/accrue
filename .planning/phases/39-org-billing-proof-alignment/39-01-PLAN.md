---
phase: 39-org-billing-proof-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/docs/adoption-proof-matrix.md
autonomous: true
requirements: [ORG-09]
must_haves:
  truths:
    - "ORG-09 primary archetype is phx.gen.auth + membership-gated Organization billable per organization_billing.md — not Sigra-free host claim."
    - "Pow/custom lanes are advisory-by-default in the matrix; VERIFY-01 stays a single bounded browser lane."
  artifacts:
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "ORG-09 section with merge-blocking primary row + honesty caveat + advisory recipe rows + script path literal."
  key_links:
    - from: "examples/accrue_host/docs/adoption-proof-matrix.md"
      to: "accrue/guides/organization_billing.md"
      via: "relative markdown link"
      pattern: "organization_billing\\.md"
---

<objective>
Extend **`examples/accrue_host/docs/adoption-proof-matrix.md`** with a dedicated **ORG-09** section: merge-blocking primary non-Sigra org archetype row, explicit **Sigra demo honesty** caveat, advisory framing for ORG-07/ORG-08, and the **literal** path `scripts/ci/verify_adoption_proof_matrix.sh` so the follow-on verifier has stable needles.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/39-org-billing-proof-alignment/39-CONTEXT.md
@.planning/phases/39-org-billing-proof-alignment/39-RESEARCH.md
@.planning/phases/39-org-billing-proof-alignment/39-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@examples/accrue_host/docs/adoption-proof-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ORG-09 section with stable headings and tables</name>
  <read_first>
    examples/accrue_host/docs/adoption-proof-matrix.md
    .planning/phases/39-org-billing-proof-alignment/39-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <acceptance_criteria>
    - `rg -n '^## Organization billing proof \\(ORG-09\\)' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n '^### Primary archetype \\(merge-blocking\\)' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n '^### Recipe lanes \\(advisory by default\\)' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'scripts/ci/verify_adoption_proof_matrix\\.sh' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'phx\\.gen\\.auth' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'use Accrue\\.Billable' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'non-Sigra' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'Sigra' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match AND the same file contains the substring `demo` OR `implementation detail` (prove honesty sentence): `rg -n 'demo|implementation detail' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'ORG-07' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'ORG-08' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match.
    - `rg -n 'organization_billing\\.md' examples/accrue_host/docs/adoption-proof-matrix.md` returns ≥1 match with a relative path containing `accrue/guides` OR `../../../../accrue/guides/organization_billing.md` (either spelling proves spine link).
  </acceptance_criteria>
  <action>Insert a new markdown section **immediately before** the existing `## Advisory: Stripe test mode (network)` heading (after the **Caveat** paragraph that ends the blocking discussion). Use these **exact** headings: `## Organization billing proof (ORG-09)`, `### Primary archetype (merge-blocking)`, `### Recipe lanes (advisory by default)`. Under **Primary archetype**, add a **three-column** markdown table (`| Concern | Proof | Where |`) with **exactly one** data row whose **Concern** cell states the canonical **non-Sigra** mainline: **phx.gen.auth + membership-gated `Organization` billable** aligned with ORG-05/ORG-06 language from CONTEXT; **Proof** cell must name **`scripts/ci/verify_adoption_proof_matrix.sh`** and `host-integration`; **Where** cell must link to `accrue/guides/organization_billing.md` using a **relative** repo path from this file (`../../../../accrue/guides/organization_billing.md`). Add a short paragraph (2–4 sentences) between the new H2 and the first H3 that: (1) defines **non-Sigra** as identity + billable resolution per the guide (`Accrue.Auth`, `Accrue.Billable`, host facade); (2) explicitly states the **example host may still use Sigra** as a **demo** / **implementation detail** without implying the whole repository is Sigra-free. Under **Recipe lanes**, add a **three-column** table with **two** rows: one for **Pow (ORG-07)** with the literal token **ORG-07** in the row, one for **custom organization (ORG-08)** with the literal token **ORG-08**; both rows must say they stay **advisory** and must **not** add a parallel VERIFY-01 Playwright lane.</action>
  <verify>
    <automated>rg -n '^## Organization billing proof \\(ORG-09\\)' examples/accrue_host/docs/adoption-proof-matrix.md</automated>
  </verify>
  <done>Matrix SSOT contains ORG-09 blocking vs advisory semantics and script literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader → tenancy assumptions | Matrix must not imply Sigra-free default host |
| Doc → CI | Literals are public; bash verifier only reads markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Information Disclosure | honesty paragraph | mitigate | Explicit Sigra / demo wording |
| T-39-01-02 | Tampering | matrix rows | mitigate | Follow-on script locks substrings |

## OWASP ASVS L1 Mapping

| ASVS | Control | Coverage |
|------|---------|----------|
| V14 | Configuration | Advisory vs blocking vocabulary preserved |

## Residual Risks

| Risk ID | Description | Severity | Accepted By | Expiry / Revisit |
|---------|-------------|----------|-------------|------------------|
| R-39-01-01 | Anchor slug drift if hosting platform changes | low | Phase owner | next doc refactor |

## Blocklist / Never-Do

- Do not redefine VERIFY-01 as multi-browser per auth vendor.
- Do not mark Pow/custom lanes merge-blocking without a new executable gate approved in a future phase.

</threat_model>

<verification>
1. `rg -n '^## Organization billing proof \\(ORG-09\\)' examples/accrue_host/docs/adoption-proof-matrix.md`
2. `rg -n 'scripts/ci/verify_adoption_proof_matrix\\.sh' examples/accrue_host/docs/adoption-proof-matrix.md`
</verification>

<success_criteria>
- ORG-09 acceptance text in REQUIREMENTS is satisfiable from the matrix doc after verifier lands in Plan 02.
- VERIFY-01 scope remains verbally narrow in the new section.
</success_criteria>

---
phase: 39-org-billing-proof-alignment
plan: 02
type: execute
wave: 2
depends_on: ["39-01-PLAN.md"]
files_modified:
  - scripts/ci/verify_adoption_proof_matrix.sh
  - .github/workflows/ci.yml
  - scripts/ci/README.md
autonomous: true
requirements: [ORG-09]
must_haves:
  truths:
    - "verify_adoption_proof_matrix.sh is the merge-blocking machine owner for ORG-09 matrix literals."
    - "host-integration runs the script adjacent to verify_verify01_readme_contract.sh."
  artifacts:
    - path: "scripts/ci/verify_adoption_proof_matrix.sh"
      provides: "Fails CI when ORG-09 needles drift."
    - path: "scripts/ci/README.md"
      provides: "ORG gates subsection + ORG-09 table row + triage."
---

<objective>
Add **`scripts/ci/verify_adoption_proof_matrix.sh`**, wire it into **`host-integration`**, and document ownership + triage under **`scripts/ci/README.md`** in a new **`## ORG gates (v1.8 org billing proof)`** section per CONTEXT **D-14–D-18**.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/39-org-billing-proof-alignment/39-CONTEXT.md
@.planning/phases/39-org-billing-proof-alignment/39-RESEARCH.md
@.planning/phases/39-org-billing-proof-alignment/39-PATTERNS.md
@scripts/ci/verify_verify01_readme_contract.sh
@.github/workflows/ci.yml
@scripts/ci/README.md
@examples/accrue_host/docs/adoption-proof-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify_adoption_proof_matrix.sh</name>
  <read_first>
    scripts/ci/verify_verify01_readme_contract.sh
    examples/accrue_host/docs/adoption-proof-matrix.md
  </read_first>
  <files>scripts/ci/verify_adoption_proof_matrix.sh</files>
  <acceptance_criteria>
    - `test -f scripts/ci/verify_adoption_proof_matrix.sh` exits 0.
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits 0 from repo root on a tree where Plan 01 is merged.
    - Every stderr line from intentional failure mode includes prefix `verify_adoption_proof_matrix:` (prove by reading script: `rg -n 'verify_adoption_proof_matrix:' scripts/ci/verify_adoption_proof_matrix.sh` returns ≥2 matches for echo lines).
    - Script uses `set -euo pipefail` and computes `repo_root` the same way as `verify_verify01_readme_contract.sh` (dirname `../..` from `scripts/ci`).
    - Script `require_substring` checks (or equivalent `grep -Fq`) **all** strings from Plan 01 acceptance list that apply to the matrix file: `## Organization billing proof (ORG-09)`, `### Primary archetype (merge-blocking)`, `### Recipe lanes (advisory by default)`, `scripts/ci/verify_adoption_proof_matrix.sh`, `phx.gen.auth`, `use Accrue.Billable`, `non-Sigra`, `ORG-07`, `ORG-08`.
    - Final stdout line is exactly `verify_adoption_proof_matrix: OK` (match `verify_verify01` style).
  </acceptance_criteria>
  <action>Create `scripts/ci/verify_adoption_proof_matrix.sh` as a bash script mirroring the structure of `verify_verify01_readme_contract.sh`: `#!/usr/bin/env bash`, `set -euo pipefail`, `repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"`, `matrix="${repo_root}/examples/accrue_host/docs/adoption-proof-matrix.md"`, fail if missing. Implement a `require_substring "NEEDLE" "LABEL"` helper that prints **`verify_adoption_proof_matrix: ...`** to stderr on failure. Call it for each required substring listed in acceptance_criteria. Success path: echo **`verify_adoption_proof_matrix: OK`** to stdout. Make the script executable in git (`chmod +x`).</action>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh</automated>
  </verify>
  <done>ORG-09 matrix literals are machine-enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Run script in host-integration job</name>
  <read_first>
    .github/workflows/ci.yml
    scripts/ci/verify_adoption_proof_matrix.sh
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <acceptance_criteria>
    - `rg -n 'verify_adoption_proof_matrix\\.sh' .github/workflows/ci.yml` finds the path inside the `host-integration` job (file contains both `host-integration:` header region and the substring — use line context: step must appear after the `host-integration:` job key and before the next top-level job at column 0 if any).
    - The workflow step runs `bash scripts/ci/verify_adoption_proof_matrix.sh` (or equivalent `run: bash ...`).
  </acceptance_criteria>
  <action>In `.github/workflows/ci.yml`, inside **`host-integration`**, add a new step **immediately after** the existing `VERIFY-01 README contract (shift-left)` step that runs `bash scripts/ci/verify_adoption_proof_matrix.sh`. Name the step **`ORG-09 adoption proof matrix literals`**.</action>
  <verify>
    <automated>rg -n 'ORG-09 adoption proof matrix|verify_adoption_proof_matrix' .github/workflows/ci.yml</automated>
  </verify>
  <done>CI blocks on matrix drift in the same job family as VERIFY-01.</done>
</task>

<task type="auto">
  <name>Task 3: Document ORG gates in scripts/ci/README.md</name>
  <read_first>
    scripts/ci/README.md
    .planning/phases/39-org-billing-proof-alignment/39-CONTEXT.md
  </read_first>
  <files>scripts/ci/README.md</files>
  <acceptance_criteria>
    - `rg -n '^## ORG gates \\(v1\\.8 org billing proof\\)' scripts/ci/README.md` returns ≥1 match.
    - `rg -n '\\| ORG-09 \\|' scripts/ci/README.md` returns ≥1 table row line containing ORG-09.
    - Table row lists `scripts/ci/verify_adoption_proof_matrix.sh` in the **Primary script(s)** column cell (same line or continued row text).
    - `rg -n '39-VERIFICATION\\.md|39-org-billing-proof-alignment/39-VERIFICATION\\.md' scripts/ci/README.md` returns ≥1 match for phase VERIFICATION owner column.
    - `rg -n 'verify_adoption_proof_matrix' scripts/ci/README.md` finds a triage subsection header or bullet list containing the script name (e.g. `### Triage: verify_adoption_proof_matrix.sh`).
  </acceptance_criteria>
  <action>Append a new top-level section **`## ORG gates (v1.8 org billing proof)`** after the ADOPT section (before `## When package docs verification fails` OR insert between ADOPT and triage if clearer). Include a **four-column** markdown table matching ADOPT style: **REQ-ID | Primary script(s) or artifact | Package ExUnit (if any) | Phase VERIFICATION owner**. Add **one row** for **`ORG-09`** listing `scripts/ci/verify_adoption_proof_matrix.sh` as primary; mention `organization_billing_guide_test.exs` path in Package column if Plan 03 lands needles; owner path **`.planning/phases/39-org-billing-proof-alignment/39-VERIFICATION.md`**. Add **`### Triage: verify_adoption_proof_matrix.sh`** with bullets mapping stderr prefix **`verify_adoption_proof_matrix:`** to ORG-09.</action>
  <verify>
    <automated>rg -n '^## ORG gates' scripts/ci/README.md</automated>
  </verify>
  <done>Contributor map lists ORG-09 with triage DX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script → filesystem | Script reads only the matrix path under repo_root |
| Logs | stderr prefix only; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Tampering | matrix doc | mitigate | Substring gate fails closed |

## OWASP ASVS L1 Mapping

| ASVS | Control | Coverage |
|------|---------|----------|
| V14 | Secure deployment | CI step is deterministic bash |

## Residual Risks

| Risk ID | Description | Severity | Accepted By | Expiry / Revisit |
|---------|-------------|----------|-------------|------------------|
| R-39-02-01 | Bash false positive on editorial reword | low | Phase owner | if needles too brittle |

## Blocklist / Never-Do

- Do not embed Stripe secrets or host `.env` reads in the verifier.
- Do not move full matrix taxonomy into `verify_verify01_readme_contract.sh`.

</threat_model>

<verification>
1. `bash scripts/ci/verify_adoption_proof_matrix.sh`
2. `rg -n 'verify_adoption_proof_matrix' .github/workflows/ci.yml`
3. `rg -n '^## ORG gates' scripts/ci/README.md`
</verification>

<success_criteria>
- ORG-09 has a named merge-blocking verifier in CI and in contributor docs.
</success_criteria>

---
phase: 39-org-billing-proof-alignment
plan: 03
type: execute
wave: 2
depends_on: ["39-01-PLAN.md"]
files_modified:
  - accrue/guides/organization_billing.md
  - accrue/test/accrue/docs/organization_billing_guide_test.exs
  - accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs
autonomous: true
requirements: [ORG-09]
must_haves:
  truths:
    - "Guide literals cross-link to adoption-proof-matrix ORG-09 section; ExUnit guards removals."
    - "Optional ExUnit mirrors Phase 31 pattern invoking bash matrix verifier from accrue package tests."
  artifacts:
    - path: "accrue/test/accrue/docs/organization_billing_guide_test.exs"
      provides: "ORG-09 / matrix path needles."
    - path: "accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs"
      provides: "Bash verifier smoke from accrue CI slice."
---

<objective>
Lock **ORG-09** cross-navigation between **`accrue/guides/organization_billing.md`** and the host **`adoption-proof-matrix.md`**, extend **`organization_billing_guide_test.exs`** needles, and add a **small ExUnit** that shells **`scripts/ci/verify_adoption_proof_matrix.sh`** (Phase 31 pattern) so package CI catches accidental script deletion.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/39-org-billing-proof-alignment/39-CONTEXT.md
@.planning/phases/39-org-billing-proof-alignment/39-RESEARCH.md
@accrue/test/accrue/phase_31_nyquist_validation_test.exs
@accrue/test/accrue/docs/organization_billing_guide_test.exs
@accrue/guides/organization_billing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ORG-09 cross-link block to organization_billing.md</name>
  <read_first>
    accrue/guides/organization_billing.md
    examples/accrue_host/docs/adoption-proof-matrix.md
  </read_first>
  <files>accrue/guides/organization_billing.md</files>
  <acceptance_criteria>
    - `rg -n '^## Adoption proof matrix \\(ORG-09\\)' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'examples/accrue_host/docs/adoption-proof-matrix\\.md' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'verify_adoption_proof_matrix\\.sh' accrue/guides/organization_billing.md` returns ≥1 match.
    - `rg -n 'Organization billing proof \\(ORG-09\\)' accrue/guides/organization_billing.md` returns ≥1 match (anchor text matching matrix heading).
  </acceptance_criteria>
  <action>Insert a new section **immediately after** the title `# Organization billing` intro paragraph (after the paragraph ending with **ORG-03 obligations** and before `## Who this guide is for`). Heading must be **exactly** `## Adoption proof matrix (ORG-09)`. Body: 2–5 sentences that (1) point readers to `examples/accrue_host/docs/adoption-proof-matrix.md` for the **blocking vs advisory** map; (2) name the **Organization billing proof (ORG-09)** subsection as the ORG-09 entry point; (3) mention **`scripts/ci/verify_adoption_proof_matrix.sh`** as the merge-blocking literal checker for matrix content; (4) repeat the honesty constraint that **non-Sigra** here is about **Accrue.Auth / Accrue.Billable** contracts, not a promise that the demo host is Sigra-free.</action>
  <verify>
    <automated>cd accrue && MIX_ENV=test mix docs</automated>
  </verify>
  <done>Spine documents ORG-09 navigation and verifier name.</done>
</task>

<task type="auto">
  <name>Task 2: Extend organization_billing_guide_test needles</name>
  <read_first>
    accrue/test/accrue/docs/organization_billing_guide_test.exs
    accrue/guides/organization_billing.md
  </read_first>
  <files>accrue/test/accrue/docs/organization_billing_guide_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'ORG-09' accrue/test/accrue/docs/organization_billing_guide_test.exs` returns ≥1 match.
    - `rg -n 'adoption-proof-matrix\\.md' accrue/test/accrue/docs/organization_billing_guide_test.exs` returns ≥1 match.
    - `rg -n 'verify_adoption_proof_matrix' accrue/test/accrue/docs/organization_billing_guide_test.exs` returns ≥1 match.
    - `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs` exits 0.
  </acceptance_criteria>
  <action>In `test "organization billing guide keeps mandatory org billing anchors"`, append these exact needles to the `for needle <- [` list: `"ORG-09"`, `"examples/accrue_host/docs/adoption-proof-matrix.md"`, `"verify_adoption_proof_matrix.sh"`, `"## Adoption proof matrix (ORG-09)"`.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs</automated>
  </verify>
  <done>ORG-09 literals are ExUnit-guarded in the accrue package.</done>
</task>

<task type="auto">
  <name>Task 3: Add organization_billing_org09_matrix_test.exs bash smoke</name>
  <read_first>
    accrue/test/accrue/phase_31_nyquist_validation_test.exs
    scripts/ci/verify_adoption_proof_matrix.sh
  </read_first>
  <files>accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs</files>
  <acceptance_criteria>
    - File `accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs` exists and defines `use ExUnit.Case, async: true`.
    - Test invokes `System.cmd("bash", [script], cd: root, stderr_to_stdout: true)` where `root` is `Path.expand("../../../..", __DIR__)` and `script` is `Path.join(root, "scripts/ci/verify_adoption_proof_matrix.sh")` (monorepo root; one level deeper than `phase_31_nyquist_validation_test.exs`).
    - Assertion: exit status `0` and output contains `verify_adoption_proof_matrix: OK`.
    - `cd accrue && mix test test/accrue/docs/organization_billing_org09_matrix_test.exs` exits 0.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs` following `Accrue.Phase31NyquistValidationTest` structure: private `repo_root/0` as `Path.expand("../../../..", __DIR__)` — from `accrue/test/accrue/docs/` four `..` segments reach the monorepo root that contains `scripts/ci/`. One test **`ORG-09 adoption proof matrix script passes`** that asserts script exists and `bash` invocation returns OK output.</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/docs/organization_billing_org09_matrix_test.exs</automated>
  </verify>
  <done>Accrue package CI exercises the matrix bash gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExUnit → bash | Tests only run repo-owned script; no network |
| Guide → reader | Public documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03-01 | Denial of Service | System.cmd | accept | Test runs in CI only; short script |

## OWASP ASVS L1 Mapping

| ASVS | Control | Coverage |
|------|---------|----------|
| V14 | Configuration | Verifies script presence + success |

## Residual Risks

| Risk ID | Description | Severity | Accepted By | Expiry / Revisit |
|---------|-------------|----------|-------------|------------------|
| R-39-03-01 | `repo_root` path depth wrong vs Phase 31 | low | Executor | use four `..` from `test/accrue/docs`; verify with `Path.join(root, "scripts/ci/verify_adoption_proof_matrix.sh")` exists |

## Blocklist / Never-Do

- Do not shell arbitrary user input.
- Do not mark advisory lanes merge-blocking from tests.

</threat_model>

<verification>
1. `cd accrue && mix test test/accrue/docs/organization_billing_guide_test.exs test/accrue/docs/organization_billing_org09_matrix_test.exs`
2. `cd accrue && MIX_ENV=test mix docs`
</verification>

<success_criteria>
- ORG-09 cross-links are executable-guarded in `accrue` without adding Playwright lanes.
</success_criteria>

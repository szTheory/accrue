---
phase: 40-telemetry-catalog-guide-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/telemetry.md
autonomous: true
requirements: [OBS-01, OBS-03, OBS-04]
must_haves:
  truths:
    - "guides/telemetry.md uses an evergreen ops catalog heading (no SemVer in that heading)"
    - "Ops markdown table includes a Primary owner column per 40-CONTEXT D-10"
    - "Guide header states Hex vs main divergence in 2–4 lines per D-16"
    - "Footer line documents last reconciliation with v1.9 gap audit §1 (date placeholder acceptable until PR merges)"
  artifacts:
    - path: "accrue/guides/telemetry.md"
      provides: "Authoritative ops catalog + OBS-03 firehose policy + runbook column"
      contains: "Ops event catalog"
  key_links:
    - from: "accrue/guides/telemetry.md"
      to: ".planning/research/v1.9-TELEMETRY-GAP-AUDIT.md"
      via: "OBS-04 reconciliation footer"
      pattern: "gap audit"
---

<objective>
Harden `accrue/guides/telemetry.md` as the operator-facing **single ops catalog** (OBS-01): evergreen heading (D-15), version/publish contract in the guide intro (D-16), **Primary owner** column on the ops table (D-10), tighten **firehose vs ops** prose without enumerating full billing surface (OBS-03, D-13), and add the **reconciliation footer** stub for OBS-04 (D-06) pointing at gap audit §1 — use literal placeholder text `PR_NUMBER_PENDING` for the PR fragment until the merge PR exists, then replace during execution.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-telemetry-catalog-guide-truth/40-CONTEXT.md
@.planning/phases/40-telemetry-catalog-guide-truth/40-RESEARCH.md
@.planning/research/v1.9-TELEMETRY-GAP-AUDIT.md
@accrue/guides/telemetry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evergreen ops heading + guide header contract</name>
  <read_first>
    .planning/phases/40-telemetry-catalog-guide-truth/40-CONTEXT.md
    accrue/guides/telemetry.md
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n '^## Ops event catalog' accrue/guides/telemetry.md` returns at least one match.
    - `rg -n '## Ops events in v1\\.0' accrue/guides/telemetry.md` returns zero matches.
    - `rg -n 'Hex|hex\\.docs|main branch|published' accrue/guides/telemetry.md` finds at least three case-insensitive matches in the first 40 lines of the file (header contract per D-16).
  </acceptance_criteria>
  <action>Rename the section currently titled `## Ops events in v1.0` to exactly `## Ops event catalog (\`[:accrue, :ops, :*]\`)` (backticks as shown). Insert a short **Doc contract** paragraph immediately after the opening `# Telemetry & Observability` section (before `## Namespace split`) with 2–4 sentences: published package telemetry contracts; `main` may add events ahead of Hex; operators should prefer the version-matched guide for their `mix.exs` lock — use concrete wording, not placeholders except none required for URLs beyond existing Hex links.</action>
  <verify>
    <automated>bash -lc 'rg -n "^## Ops event catalog" accrue/guides/telemetry.md && ! rg -n "## Ops events in v1\\.0" accrue/guides/telemetry.md'</automated>
  </verify>
  <done>Ops catalog section title is evergreen; reader sees version/source contract at top.</done>
</task>

<task type="auto">
  <name>Task 2: Ops table — Primary owner column + row alignment</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/lib/accrue/telemetry/ops.ex
    accrue/lib/accrue/webhook/connect_handler.ex
    accrue/lib/accrue/workers/mailer.ex
    accrue/lib/accrue/events.ex
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n '\\| Primary owner \\|' accrue/guides/telemetry.md` finds the table header row.
    - `rg -n 'Accrue\\.Webhook\\.ConnectHandler|Accrue\\.Workers\\.Mailer|Accrue\\.Events|Accrue\\.Telemetry\\.Ops|webhook' accrue/guides/telemetry.md` finds at least one Primary-owner cell reference for Connect, PDF, ledger, and DLQ class rows combined (grep any three of these tokens in the table body).
    - Every ops row from the v1.9 audit §1 table remains: `rg -n ':pdf_adapter_unavailable|:events_upcast_failed|:connect_account_deauthorized|:connect_capability_lost|:connect_payout_failed' accrue/guides/telemetry.md` returns ≥1 match each.
  </acceptance_criteria>
  <action>Add a **Primary owner** column to the ops events markdown table. For each existing row, populate with one stable anchor per 40-CONTEXT D-10 — examples: `Accrue.Telemetry.Ops` for `Ops.emit/3` events; `Accrue.Webhook.ConnectHandler` for Connect rows; `Accrue.Workers.Mailer` for PDF adapter unavailable; `Accrue.Events` for ledger upcast; `Accrue.Webhooks.DLQ` / pruner / default_handler as appropriate for DLQ rows. Keep measurements/metadata columns unchanged unless code contradicts (then fix to match emit sites in same task).</action>
  <verify>
    <automated>rg -n '\\| Primary owner \\|' accrue/guides/telemetry.md</automated>
  </verify>
  <done>Ops table is contract-first and traceable to owning modules per CONTEXT.</done>
</task>

<task type="auto">
  <name>Task 3: OBS-03 firehose subsection + reconciliation footer</name>
  <read_first>
    accrue/guides/telemetry.md
    .planning/phases/40-telemetry-catalog-guide-truth/40-CONTEXT.md
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n 'diagnostics|paging|firehose|high cardinality' accrue/guides/telemetry.md` returns ≥4 matches in the Namespace split / firehose area.
    - `rg -n 'Last reconciled with v1\\.9 gap audit' accrue/guides/telemetry.md` finds exactly one line (or contiguous wrapped sentence) containing that substring and the literal `PR_NUMBER_PENDING`.
    - `rg -n 'v1\\.9-TELEMETRY-GAP-AUDIT' accrue/guides/telemetry.md` finds a relative link `.planning/research/v1.9-TELEMETRY-GAP-AUDIT.md` or plain path text acceptable to Markdown linters in-repo.
  </acceptance_criteria>
  <action>Tighten `### Firehose and diagnostic events` (or equivalent) to restate: billing spans use `Accrue.Telemetry.span/3`; ops paging stays on `[:accrue, :ops, :*]`; do **not** add a full billing action list. Append a short footer paragraph after the main ops table (or before `## Span naming`) containing: **Last reconciled with v1.9 gap audit §1:** `2026-04-21` (adjust to execution date) **— PR #PR_NUMBER_PENDING** and one sentence that the audit §1 inventory is reflected in the table above.</action>
  <verify>
    <automated>rg -n 'Last reconciled with v1\.9 gap audit' accrue/guides/telemetry.md</automated>
  </verify>
  <done>OBS-03 clarity preserved; OBS-04 footer trail exists for operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public guide readers | Markdown is world-readable — must not introduce live secrets, customer data, or Stripe keys |
| Ops catalog | Mis-stated metadata keys could mis-route alerts — table must match code contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Information Disclosure | `guides/telemetry.md` | mitigate | Do not add env var names with example values, tokens, or webhook secrets; keep examples synthetic. |
| T-40-01-02 | Tampering | ops table | mitigate | Align measurements/metadata text with existing emit sites; executor reads emit modules in `read_first`. |
| T-40-01-03 | Repudiation | reconciliation footer | mitigate | Reference dated gap audit §1 + PR placeholder for audit trail. |
</threat_model>

<verification>
- `rg -n '^## Ops event catalog' accrue/guides/telemetry.md`
- `rg -n '\\| Primary owner \\|' accrue/guides/telemetry.md`
- `rg -n 'Last reconciled with v1\\.9 gap audit' accrue/guides/telemetry.md`
</verification>

<success_criteria>
- OBS-01 table is complete for all §1 ops events with Primary owner column.
- OBS-03 firehose vs ops split remains explicit and non-enumerative for billing.
- OBS-04 has a dated footer with `PR_NUMBER_PENDING` ready for final substitution post-merge.
</success_criteria>

<output>
After completion, create `.planning/phases/40-telemetry-catalog-guide-truth/40-01-SUMMARY.md`
</output>

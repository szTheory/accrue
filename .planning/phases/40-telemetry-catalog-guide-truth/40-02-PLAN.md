---
phase: 40-telemetry-catalog-guide-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/telemetry.ex
  - accrue/guides/telemetry.md
autonomous: true
requirements: [OBS-01, OBS-03]
must_haves:
  truths:
    - "Accrue.Telemetry moduledoc domain list matches guide OTel domain vocabulary (including checkout and billing_portal when still accurate)"
    - "Guide OTel examples do not label non-span signals as OTel span names without an explicit callout"
    - "Guide includes verified example accrue.billing.meter_event.report_usage and cross-links ops meter_reporting_failed"
  artifacts:
    - path: "accrue/lib/accrue/telemetry.ex"
      provides: "Aligned domain vocabulary for span/3"
      contains: ":billing"
    - path: "accrue/guides/telemetry.md"
      provides: "Honest OTel section"
      contains: "Illustrative"
  key_links:
    - from: "accrue/guides/telemetry.md"
      to: "accrue/test/accrue/telemetry/billing_span_coverage_test.exs"
      via: "non-exhaustive billing span inventory pointer"
      pattern: "billing_span_coverage"
---

<objective>
Reconcile **OpenTelemetry / span naming documentation** with code (OBS-01 narrative + audit §4): fix **misleading OTel examples** (ops-only signals such as DLQ replay), add **verified** `accrue.billing.meter_event.report_usage`, add an explicit **non-exhaustiveness** callout pointing at `billing_span_coverage_test.exs`, and align **`Accrue.Telemetry` moduledoc** domain vocabulary with the guide (`checkout`, `billing_portal`, `:storage` if used anywhere in `span/3` paths — verify with `rg 'Accrue\\.Telemetry\\.span\\(\\[:accrue' accrue/lib` before editing).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-telemetry-catalog-guide-truth/40-CONTEXT.md
@.planning/phases/40-telemetry-catalog-guide-truth/40-RESEARCH.md
@accrue/lib/accrue/telemetry.ex
@accrue/lib/accrue/telemetry/otel.ex
@accrue/guides/telemetry.md
@accrue/test/accrue/telemetry/billing_span_coverage_test.exs
@accrue/lib/accrue/billing.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory span domains from code, then patch Telemetry moduledoc</name>
  <read_first>
    accrue/lib/accrue/telemetry.ex
    accrue/guides/telemetry.md
  </read_first>
  <files>accrue/lib/accrue/telemetry.ex</files>
  <acceptance_criteria>
    - `rg -n ':checkout|:billing_portal|:storage' accrue/lib/accrue/telemetry.ex` reflects every domain atom actually used in `Accrue.Telemetry.span([:accrue, :DOMAIN` first positions under `accrue/lib` (executor pre-runs `rg -n 'Accrue\\.Telemetry\\.span\\(\\[:accrue' accrue/lib | head -n 50` and copies domain set into moduledoc list).
    - `rg -n '## Event naming' accrue/lib/accrue/telemetry.ex` precedes the updated domain bullet list.
    - Moduledoc still documents 4-level `[:accrue, :domain, :resource, :action, :phase]` pattern with at least two concrete `[:accrue, :billing,` examples.
  </acceptance_criteria>
  <action>Run `rg -n 'Accrue\\.Telemetry\\.span\\(\\[:accrue' accrue/lib` and derive the **complete domain atom set** used at position 2 of the event list. Update `@moduledoc` in `accrue/lib/accrue/telemetry.ex` **Event naming** section so the prose list matches reality (include `:checkout`, `:billing_portal`, `:storage` only if present in code). If guide lists a domain with **zero** span call sites, remove it from the guide in Task 2 rather than documenting dead vocabulary.</action>
  <verify>
    <automated>cd accrue && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Telemetry moduledoc no longer contradicts `span/3` usage in lib.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite guide OTel examples for honesty + meter cross-link</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/lib/accrue/telemetry/otel.ex
    accrue/lib/accrue/billing.ex
    accrue/test/accrue/telemetry/billing_span_coverage_test.exs
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n 'accrue\\.webhooks\\.dlq\\.replay' accrue/guides/telemetry.md` returns zero matches **OR** the line is inside a clearly labeled **non-OTel** callout (e.g. contains the substring `NOT an OTel span name` on the same bullet or immediately prior line).
    - `rg -n 'accrue\\.billing\\.meter_event\\.report_usage' accrue/guides/telemetry.md` finds at least one bullet or code line with that exact dotted string.
    - `rg -n 'meter_reporting_failed' accrue/guides/telemetry.md` finds a line within 15 lines of the `meter_event.report_usage` example that references ops cross-link (substring `meter_reporting_failed` suffices).
    - `rg -n 'billing_span_coverage_test' accrue/guides/telemetry.md` finds a Markdown link or backtick path to `test/accrue/telemetry/billing_span_coverage_test.exs`.
    - `rg -n 'Illustrative|non-exhaustive|not exhaustive' accrue/guides/telemetry.md` finds ≥1 match in the OTel section.
  </acceptance_criteria>
  <action>In `## Span naming conventions (OpenTelemetry)` (or successor title): (1) Keep dot-join algorithm description aligned with `Accrue.Telemetry.OTel.span_name/1`. (2) Remove or relabel **`accrue.webhooks.dlq.replay`** as ops/DLQ signal, **not** an OTel span from `span/3`. (3) Add **`accrue.billing.meter_event.report_usage`** as a verified billing example; cross-link to ops `[:accrue, :ops, :meter_reporting_failed]` for failure path. (4) Add a short **Illustrative examples** callout stating non-exhaustiveness and pointing to `accrue/test/accrue/telemetry/billing_span_coverage_test.exs` for billing span coverage discipline. (5) Align domain bullet list under `The domain layer is one of` with the code-derived set from Task 1.</action>
  <verify>
    <automated>rg -n 'accrue\.billing\.meter_event\.report_usage|billing_span_coverage_test|Illustrative' accrue/guides/telemetry.md</automated>
  </verify>
  <done>OTel section matches D-14; operators are not mis-trained on DLQ replay as a span.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OTel attribute allowlist | `otel.ex` enforces PII-safe attrs — docs must not encourage prohibited keys |
| Public guide | Same as Plan 01 — no secrets in examples |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Information Disclosure | OTel examples | mitigate | Use only documented allowlisted attribute names; no raw Stripe objects in examples. |
| T-40-02-02 | Security Misconfiguration | span naming docs | mitigate | Align examples with `Telemetry.OTel` + `span/3` so hosts configure tracers on real span names. |
</threat_model>

<verification>
- `cd accrue && mix compile --warnings-as-errors`
- `rg -n 'Span naming conventions|OpenTelemetry' accrue/guides/telemetry.md`
- `rg -n 'accrue\\.billing\\.meter_event\\.report_usage' accrue/guides/telemetry.md`
</verification>

<success_criteria>
- Telemetry moduledoc and guide domain vocabulary match code.
- OTel examples are verified or explicitly disclaimed; meter reporting example + ops failure cross-link present.
</success_criteria>

<output>
After completion, create `.planning/phases/40-telemetry-catalog-guide-truth/40-02-SUMMARY.md`
</output>

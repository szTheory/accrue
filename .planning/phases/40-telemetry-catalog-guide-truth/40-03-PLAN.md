---
phase: 40-telemetry-catalog-guide-truth
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - accrue/test/accrue/telemetry/ops_event_contract_test.exs
  - .planning/research/v1.9-TELEMETRY-GAP-AUDIT.md
  - accrue/guides/telemetry.md
  - accrue/CHANGELOG.md
autonomous: true
requirements: [OBS-01, OBS-04]
must_haves:
  truths:
    - "ExUnit contract locks ops catalog against silent code drift"
    - "Gap audit §1 carries SUPERSEDED pointer to the guide reconciliation"
    - "CHANGELOG records user-visible telemetry doc contract under Unreleased"
  artifacts:
    - path: "accrue/test/accrue/telemetry/ops_event_contract_test.exs"
      provides: "Ops allowlist vs lib vs guide"
      contains: "ops_event_contract"
    - path: ".planning/research/v1.9-TELEMETRY-GAP-AUDIT.md"
      provides: "Supersession trail"
      contains: "SUPERSEDED"
  key_links:
    - from: "accrue/test/accrue/telemetry/ops_event_contract_test.exs"
      to: "accrue/guides/telemetry.md"
      via: "table row presence"
      pattern: "telemetry.md"
---

<objective>
Ship **OBS-04** closure and **OBS-01** machine verification: add an ExUnit **ops event contract** that (a) enumerates the canonical `[:accrue, :ops | suffix]` inventory, (b) asserts every literal ops emit in `accrue/lib` is in that inventory, (c) asserts each inventory event has a **table row** in `guides/telemetry.md`. Mark the v1.9 gap audit **SUPERSEDED** with date + PR. Replace `PR_NUMBER_PENDING` in the guide footer with the real PR number. Add **`accrue/CHANGELOG.md`** Unreleased bullet under documentation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-telemetry-catalog-guide-truth/40-CONTEXT.md
@.planning/phases/40-telemetry-catalog-guide-truth/40-RESEARCH.md
@.planning/phases/40-telemetry-catalog-guide-truth/40-PATTERNS.md
@.planning/research/v1.9-TELEMETRY-GAP-AUDIT.md
@accrue/guides/telemetry.md
@accrue/lib/accrue/telemetry/ops.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ops_event_contract_test.exs</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/lib/accrue/telemetry/ops.ex
    accrue/lib/accrue/workers/mailer.ex
    accrue/lib/accrue/events.ex
    accrue/lib/accrue/webhook/default_handler.ex
    accrue/lib/accrue/webhook/connect_handler.ex
    accrue/lib/accrue/webhooks/dlq.ex
    accrue/lib/accrue/webhook/pruner.ex
    accrue/lib/accrue/billing/meter_event_actions.ex
    accrue/lib/accrue/jobs/meter_events_reconciler.ex
    accrue/test/accrue/telemetry/billing_span_coverage_test.exs
  </read_first>
  <files>accrue/test/accrue/telemetry/ops_event_contract_test.exs</files>
  <acceptance_criteria>
    - File `accrue/test/accrue/telemetry/ops_event_contract_test.exs` exists and defines `defmodule Accrue.Telemetry.OpsEventContractTest`.
    - `rg -n '@expected_ops_events' accrue/test/accrue/telemetry/ops_event_contract_test.exs` finds a module attribute listing **exactly** these 13 canonical suffix forms as atoms or lists: `:revenue_loss`, `:dunning_exhaustion`, `:incomplete_expired`, `:charge_failed`, `:meter_reporting_failed`, `[:webhook_dlq, :dead_lettered]`, `[:webhook_dlq, :replay]`, `[:webhook_dlq, :prune]`, `:pdf_adapter_unavailable`, `:events_upcast_failed`, `:connect_account_deauthorized`, `:connect_capability_lost`, `:connect_payout_failed`.
    - Test prints remediation text containing both strings `guides/telemetry.md` and `ops_event_contract_test.exs` on intentional failure paths (assert message or `flunk` string).
    - `cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs` exits 0.
  </acceptance_criteria>
  <action>Create `accrue/test/accrue/telemetry/ops_event_contract_test.exs`. Implement: (1) `@expected_ops_events` as the list of full event names `[:accrue, :ops | suffix]` built from the 13 suffixes above. (2) **Guide coverage:** read `accrue/guides/telemetry.md` and assert every expected event appears as a backtick-wrapped literal in the ops catalog table body (use string contains check for each `inspect(event)`-style or hand-built `` `[:accrue, :ops, ...]` `` substring as used in the guide). (3) **Code coverage:** recursively read `accrue/lib/**/*.ex` files, skip `accrue/lib/accrue/telemetry/ops.ex` moduledoc block if needed, and `assert [] == undeclared_emits` where undeclared emits are regex matches of `[:accrue, :ops` multi-line tuples not matching any expected event **after** normalizing whitespace — **or** simpler approved approach: `Enum.each` known emit files from RESEARCH and grep each file content for `[:accrue, :ops` and assert each matched event is in `@expected_ops_events`. (4) Inverse: every `@expected_ops_events` must appear at least once in lib **or** be documented as reserved — for Phase 40 all 13 must appear in lib (they do today).</action>
  <verify>
    <automated>cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs</automated>
  </verify>
  <done>OBS-01 has machine enforcement; new ops emits cannot merge without guide+allowlist update.</done>
</task>

<task type="auto">
  <name>Task 2: SUPERSEDED block in gap audit + guide PR footer</name>
  <read_first>
    .planning/research/v1.9-TELEMETRY-GAP-AUDIT.md
    accrue/guides/telemetry.md
  </read_first>
  <files>.planning/research/v1.9-TELEMETRY-GAP-AUDIT.md, accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n '## SUPERSEDED|SUPERSEDED — Phase 40' .planning/research/v1.9-TELEMETRY-GAP-AUDIT.md` finds a heading or bold line containing `SUPERSEDED` and the substring `Phase 40`.
    - The SUPERSEDED section body contains the relative path `accrue/guides/telemetry.md` and a date `2026-` (year of reconciliation).
    - `rg -n 'PR_NUMBER_PENDING' accrue/guides/telemetry.md` returns **zero** matches after substitution with the real GitHub PR number for the telemetry doc work (use literal `#NNN` once known).
  </acceptance_criteria>
  <action>Append to `.planning/research/v1.9-TELEMETRY-GAP-AUDIT.md` (end of file, before final horizontal rule if present): a `## SUPERSEDED — inventory §1 (Phase 40)` section stating §1 ops inventory is reflected in `accrue/guides/telemetry.md` ops catalog as of the merge date, with **PR #NNN** link. In `accrue/guides/telemetry.md`, replace `PR_NUMBER_PENDING` with the same `#NNN`.</action>
  <verify>
    <automated>rg -n SUPERSEDED .planning/research/v1.9-TELEMETRY-GAP-AUDIT.md</automated>
  </verify>
  <done>OBS-04 maintainer trail exists; guide footer matches merged PR.</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG Unreleased entry</name>
  <read_first>
    accrue/CHANGELOG.md
  </read_first>
  <files>accrue/CHANGELOG.md</files>
  <acceptance_criteria>
    - `rg -n 'telemetry|Ops event|observability' accrue/CHANGELOG.md` finds at least one match under `## Unreleased`.
    - `CHANGELOG.md` keeps standard Keep a Changelog section ordering if already present.
  </acceptance_criteria>
  <action>Under `## Unreleased` in `accrue/CHANGELOG.md`, add a **Documentation** bullet: telemetry guide ops catalog evergreen heading, Primary owner column, OTel example corrections, and ops event contract test — phrase in user-facing language (no internal REQ ids required).</action>
  <verify>
    <automated>bash -lc 'rg -n "## Unreleased" -A20 accrue/CHANGELOG.md | head -n 25'</automated>
  </verify>
  <done>Release hygiene signal for operators consuming Hex package notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract test reads `accrue/lib` | Must not execute compiled code from disk — file read only |
| Planning research doc | SUPERSEDED annotation is non-executable markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Denial of Service | recursive lib read in test | mitigate | Limit to `accrue/lib` tree only; test marked `async: false` if filesystem contention; keep file count bounded. |
| T-40-03-02 | Tampering | allowlist | mitigate | Two-way check against lib emits so attackers cannot document fake ops events without code. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs`
- `cd accrue && mix test test/accrue/telemetry/`
- `rg -n SUPERSEDED .planning/research/v1.9-TELEMETRY-GAP-AUDIT.md`
</verification>

<success_criteria>
- Contract test green; gap audit superseded; guide footer real PR#; CHANGELOG documents the work.
</success_criteria>

<output>
After completion, create `.planning/phases/40-telemetry-catalog-guide-truth/40-03-SUMMARY.md`
</output>

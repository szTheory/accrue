---
phase: 41-host-metrics-wiring-cross-domain-example
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/test/support/telemetry_ops_inventory.ex
  - accrue/test/accrue/telemetry/ops_event_contract_test.exs
  - accrue/test/accrue/telemetry/metrics_ops_parity_test.exs
autonomous: true
requirements: [TEL-01]
must_haves:
  truths:
    - "`Accrue.TestSupport.TelemetryOpsInventory` in test/support holds canonical ops tuple list used by both contract tests"
    - "ExUnit asserts every allowlisted ops tuple matches at least one Telemetry.Metrics default's event_name"
    - "Intentional metric gaps (if any) are explicit with reason + guide anchor in parity test module"
  artifacts:
    - path: "accrue/test/accrue/telemetry/metrics_ops_parity_test.exs"
      provides: "TEL-01 machine-verifiable parity gate"
      contains: "TelemetryOpsInventory"
    - path: "accrue/test/support/telemetry_ops_inventory.ex"
      provides: "Shared ops tuple source (D-02)"
      contains: "expected_ops_events"
---

<objective>
Close **TEL-01** with a machine-verifiable contract: every canonical `[:accrue, :ops | _]` tuple from the Phase 40 ops inventory is backed by a `Telemetry.Metrics` definition in `Accrue.Telemetry.Metrics.defaults/0` (assert on struct **`event_name`**, not string heuristics). Refactor `OpsEventContractTest` to consume a **single** `test/support` module for `@expected_ops_events` / `@not_wired_first_party_emits` so lists cannot drift (41-CONTEXT D-01–D-03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-host-metrics-wiring-cross-domain-example/41-CONTEXT.md
@.planning/phases/41-host-metrics-wiring-cross-domain-example/41-RESEARCH.md
@accrue/lib/accrue/telemetry/metrics.ex
@accrue/test/accrue/telemetry/ops_event_contract_test.exs
@accrue/test/accrue/telemetry/metrics_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpsInventory support module</name>
  <read_first>
    accrue/test/accrue/telemetry/ops_event_contract_test.exs
    accrue/mix.exs
  </read_first>
  <files>accrue/test/support/telemetry_ops_inventory.ex</files>
  <acceptance_criteria>
    - File path exists: `accrue/test/support/telemetry_ops_inventory.ex`.
    - `rg -n 'def expected_ops_events' accrue/test/support/telemetry_ops_inventory.ex` returns ≥1 match.
    - `rg -n 'def not_wired_first_party_emits' accrue/test/support/telemetry_ops_inventory.ex` returns ≥1 match.
    - Module name is exactly `Accrue.TestSupport.TelemetryOpsInventory` (grep `defmodule Accrue.TestSupport.TelemetryOpsInventory`).
  </acceptance_criteria>
  <action>Create `Accrue.TestSupport.TelemetryOpsInventory` in `accrue/test/support/telemetry_ops_inventory.ex` (flat `test/support` path matches existing `billing_case.ex` style). Move the literal list currently in `OpsEventContractTest` `@expected_ops_events` into `def expected_ops_events/0` returning the same **13** tuples in the same order as today's attribute. Move `@not_wired_first_party_emits` into `def not_wired_first_party_emits/0` returning `MapSet.new([...])` with the **same** three tuples as the current test. Add `@moduledoc false`.</action>
  <verify>
    <automated>bash -lc 'test -f accrue/test/support/telemetry_ops_inventory.ex && rg -q "defmodule Accrue.TestSupport.TelemetryOpsInventory" accrue/test/support/telemetry_ops_inventory.ex'</automated>
  </verify>
  <done>Single support module owns ops tuple inventory for tests.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor OpsEventContractTest to use OpsInventory</name>
  <read_first>
    accrue/test/accrue/telemetry/ops_event_contract_test.exs
    accrue/test/support/telemetry_ops_inventory.ex
  </read_first>
  <files>accrue/test/accrue/telemetry/ops_event_contract_test.exs</files>
  <acceptance_criteria>
    - `rg -n '@expected_ops_events' accrue/test/accrue/telemetry/ops_event_contract_test.exs` returns zero matches.
    - `rg -n 'TelemetryOpsInventory' accrue/test/accrue/telemetry/ops_event_contract_test.exs` returns ≥1 match.
    - `cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs` exits 0.
  </acceptance_criteria>
  <action>Replace module attributes with `alias Accrue.TestSupport.TelemetryOpsInventory` and call `TelemetryOpsInventory.expected_ops_events/0` / `TelemetryOpsInventory.not_wired_first_party_emits/0` everywhere the old attributes were referenced. Keep all assertion logic and remediation strings unchanged aside from list source.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs'</automated>
  </verify>
  <done>Ops contract test shares inventory with new parity test surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add MetricsOpsParityTest</name>
  <read_first>
    accrue/lib/accrue/telemetry/metrics.ex
    accrue/test/accrue/telemetry/metrics_test.exs
    accrue/test/support/telemetry_ops_inventory.ex
    .planning/phases/41-host-metrics-wiring-cross-domain-example/41-CONTEXT.md
  </read_first>
  <files>accrue/test/accrue/telemetry/metrics_ops_parity_test.exs</files>
  <acceptance_criteria>
    - File `accrue/test/accrue/telemetry/metrics_ops_parity_test.exs` exists.
    - Test module name matches `Accrue.Telemetry.MetricsOpsParityTest` (grep `defmodule Accrue.Telemetry.MetricsOpsParityTest`).
    - `rg -n 'event_name' accrue/test/accrue/telemetry/metrics_ops_parity_test.exs` returns ≥2 matches.
    - `cd accrue && mix test test/accrue/telemetry/metrics_ops_parity_test.exs` exits 0.
    - If no intentional omissions: `rg -n '@tel_metric_omissions' accrue/test/accrue/telemetry/metrics_ops_parity_test.exs` may be absent OR attribute is empty list with comment; if omissions exist, module attribute `@tel_metric_omissions` is a keyword list or map with `:reason` and `:guide_anchor` per D-03.
  </acceptance_criteria>
  <action>Add `accrue/test/accrue/telemetry/metrics_ops_parity_test.exs` using `use ExUnit.Case, async: true`. For each tuple from `Accrue.TestSupport.TelemetryOpsInventory.expected_ops_events/0`, assert there exists a metric in `Accrue.Telemetry.Metrics.defaults()` whose **`.event_name`** equals that tuple (use `Enum.any?/2`). If `:telemetry_metrics` is not loaded (defensive guard matching D-04), **skip** the test module's examples with `@moduletag :skip` is discouraged — instead use `if Code.ensure_loaded?(Telemetry.Metrics)` inside a single test that flunks with message pointing at `mix.exs` optional dep, **or** assert `function_exported?(Accrue.Telemetry.Metrics, :defaults, 0)` and that first call returns a non-empty list (match existing `MetricsTest` assumption). Add module doc one line: parity is the TEL-01 gate. If all ops tuples match, omit `@tel_metric_omissions`; otherwise define with explicit reasons per D-03.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/telemetry/metrics_ops_parity_test.exs'</automated>
  </verify>
  <done>TEL-01 has ExUnit parity beyond narrative docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only inventory | Ops tuples are public catalog data — no secrets |
| CI signal | False negatives break release hygiene — parity logic must use `event_name` equality only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | TelemetryOpsInventory list | mitigate | Contract test still asserts guide literals + lib scan; inventory edits require dual test pass. |
| T-41-01-02 | Information Disclosure | Failure messages | mitigate | Remediation strings reference only repo-relative paths already used in Phase 40 tests. |
</threat_model>

<verification>
- `cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs test/accrue/telemetry/metrics_ops_parity_test.exs test/accrue/telemetry/metrics_test.exs`
</verification>

<success_criteria>
- Shared `Accrue.TestSupport.TelemetryOpsInventory` module is the only source for canonical ops tuples in tests.
- Parity test fails if an ops tuple is added to inventory without a matching `defaults/0` metric `event_name`.
</success_criteria>

<output>
After completion, create `.planning/phases/41-host-metrics-wiring-cross-domain-example/41-01-SUMMARY.md`
</output>

---
phase: 41-host-metrics-wiring-cross-domain-example
plan: 02
type: execute
wave: 2
depends_on: ["41-01-PLAN.md"]
files_modified:
  - accrue/guides/telemetry.md
  - examples/accrue_host/lib/accrue_host_web/telemetry.ex
  - examples/accrue_host/lib/accrue_host/accrue_ops_telemetry.ex
  - examples/accrue_host/lib/accrue_host/application.ex
  - examples/accrue_host/mix.exs
  - examples/accrue_host/README.md
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "guides/telemetry.md gains a short cross-domain subsection linking to catalog rows (no second ops table)"
    - "accrue_host metrics/0 appends Accrue.Telemetry.Metrics.defaults()"
    - "Example host attaches once to one high-signal ops event via public API only"
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/accrue_ops_telemetry.ex"
      provides: "Production-shaped attach-once subscriber"
      contains: ":telemetry.attach"
    - path: "accrue/guides/telemetry.md"
      provides: "OBS-02 copy-paste narrative"
      contains: "Cross-domain"
---

<objective>
Deliver **OBS-02**: host-copyable, **public-API-only** cross-domain subscription story in `accrue/guides/telemetry.md` plus a **compile-checked** mirror in `examples/accrue_host` (D-07–D-17). Fix **`AccrueHostWeb.Telemetry.metrics/0`** to append `++ Accrue.Telemetry.Metrics.defaults()` (D-14). Add one attach-at-startup module for **one** `[:accrue, :ops, …]` event (default narrative: **`[:accrue, :ops, :webhook_dlq, :dead_lettered]`** per 41-RESEARCH) with maintenance contract comment pointing at guide anchor (D-08, D-10, D-13).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-host-metrics-wiring-cross-domain-example/41-CONTEXT.md
@accrue/guides/telemetry.md
@examples/accrue_host/lib/accrue_host_web/telemetry.ex
@examples/accrue_host/lib/accrue_host/application.ex
@examples/accrue_host/README.md
@examples/accrue_host/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guide — cross-domain subsection + OTel callout</name>
  <read_first>
    accrue/guides/telemetry.md
    .planning/phases/41-host-metrics-wiring-cross-domain-example/41-CONTEXT.md
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n '^## Cross-domain host subscription' accrue/guides/telemetry.md` returns exactly one match (H2 title as written).
    - `rg -n ':telemetry\\.attach' accrue/guides/telemetry.md` returns ≥1 match in the new section body.
    - `rg -n 'accrue_host' accrue/guides/telemetry.md` returns ≥1 match referencing the example app path or module name.
    - `rg -n 'billing.*:stop|high.cardinality|metric tags' accrue/guides/telemetry.md` returns ≥2 matches covering billing span caution (D-12) OR explicit prose "do not" / "never" for unbounded IDs as tags.
    - `rg -n 'OpenTelemetry|OTel-only|skip `Telemetry.Metrics`' accrue/guides/telemetry.md` has ≥1 match for OTel-only escape (D-17) — adjust wording but keep meaning.
  </acceptance_criteria>
  <action>Add H2 `## Cross-domain host subscription` after the ops catalog / host-metrics area (pick the least disruptive insertion point before any appendices). Include: (1) snippet using **only** public modules `Accrue.Telemetry`, `Accrue.Telemetry.Metrics`, `Accrue.Telemetry.Ops` docs cross-links as prose permits; (2) **one** `:telemetry.attach/4` example whose handler increments/logs on **`[:accrue, :ops, :webhook_dlq, :dead_lettered]`**; (3) link text pointing readers to the **existing** ops catalog table for full tuple list — **do not** duplicate the markdown table. Add optional short subsubsection with **one** billing span handler showing **`:stop` and `:exception` only** and explicit warning: no customer/subscription IDs as metric tags (D-12). Add one short paragraph for OTel-only teams (attach-only, skip `Telemetry.Metrics` recipe) per D-17.</action>
  <verify>
    <automated>bash -lc 'rg -n "^## Cross-domain host subscription" accrue/guides/telemetry.md'</automated>
  </verify>
  <done>OBS-02 narrative lives beside Phase 40 catalog without forked truth.</done>
</task>

<task type="auto">
  <name>Task 2: Example host — metrics defaults + Ops subscriber module</name>
  <read_first>
    examples/accrue_host/lib/accrue_host_web/telemetry.ex
    examples/accrue_host/lib/accrue_host/application.ex
    accrue/lib/accrue/telemetry/metrics.ex
    accrue/guides/telemetry.md
  </read_first>
  <files>examples/accrue_host/lib/accrue_host_web/telemetry.ex, examples/accrue_host/lib/accrue_host/accrue_ops_telemetry.ex, examples/accrue_host/lib/accrue_host/application.ex</files>
  <acceptance_criteria>
    - `rg -n 'Accrue\\.Telemetry\\.Metrics\\.defaults' examples/accrue_host/lib/accrue_host_web/telemetry.ex` returns ≥1 match inside `metrics/0` list pipeline (use `++` with host metrics list).
    - New file exists: `examples/accrue_host/lib/accrue_host/accrue_ops_telemetry.ex` defining a `child_spec/1` or `start_link/1` module that calls `:telemetry.attach/4` for `[:accrue, :ops, :webhook_dlq, :dead_lettered]` and detaches on terminate (or documents permanent attach for example scope).
    - `rg -n 'AccrueOpsTelemetry|accrue_ops_telemetry' examples/accrue_host/lib/accrue_host/application.ex` returns ≥1 match adding the child **after** `AccrueHostWeb.Telemetry` in the children list.
    - `rg -n 'Mirrors|guides/telemetry\\.md' examples/accrue_host/lib/accrue_host/accrue_ops_telemetry.ex` returns ≥1 maintenance contract comment (D-08).
    - `cd examples/accrue_host && mix compile` exits 0.
  </acceptance_criteria>
  <action>In `metrics/0`, append host Phoenix/Repo metrics with `++ Accrue.Telemetry.Metrics.defaults()` exactly as `Accrue.Telemetry.Metrics` moduledoc shows. Keep reporter child commented with clarifying comment (D-15 pointer). Implement `AccrueHost.AccrueOpsTelemetry` (filename snake_case) as a small GenServer or `Agent`-less simple `Supervisor` child: on `start_link`, attach id `accrue-host-ops-dlq-dead-lettered` (or similar stable string); on `terminate`, `:telemetry.detach/1`. Handler may `Logger.info/1` with **low-cardinality** metadata only. Wire child in `Application.start/2` per acceptance order. If `telemetry_metrics` version in `examples/accrue_host/mix.exs` is not `~> 1.1`, bump to match guide direction when file is touched (D-15).</action>
  <verify>
    <automated>bash -lc 'cd examples/accrue_host && mix compile'</automated>
  </verify>
  <done>Example host is a credible clone of the guide recipe.</done>
</task>

<task type="auto">
  <name>Task 3: Example host README deep link</name>
  <read_first>
    examples/accrue_host/README.md
    accrue/guides/telemetry.md
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <acceptance_criteria>
    - `rg -n 'telemetry\\.md#|Cross-domain host subscription' examples/accrue_host/README.md` returns ≥1 match with Markdown link to `../../accrue/guides/telemetry.md` or repo-relative equivalent used elsewhere in README.
  </acceptance_criteria>
  <action>Add one bullet or short subsection under observability / telemetry pointing maintainers to the guide anchor for the cross-domain example and `AccrueOpsTelemetry` module (D-08).</action>
  <verify>
    <automated>rg -n 'telemetry\.md' examples/accrue_host/README.md</automated>
  </verify>
  <done>Discoverability path from host to guide is explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example host | Educational code — must not log secrets or PII |
| Guide readers | Public Markdown — examples must stay synthetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Information Disclosure | Logger example | mitigate | Log only counts or static atoms; never log full metadata maps from billing. |
| T-41-02-02 | Elevation | Telemetry attach | mitigate | Attach ID is fixed string; detach on shutdown to avoid duplicate handlers on hot reload in dev (document if simplified). |
</threat_model>

<verification>
- `cd examples/accrue_host && mix compile`
- `rg -n '^## Cross-domain host subscription' accrue/guides/telemetry.md`
- `rg -n 'Accrue\\.Telemetry\\.Metrics\\.defaults' examples/accrue_host/lib/accrue_host_web/telemetry.ex`
</verification>

<success_criteria>
- Guide subsection exists with attach snippet + catalog link.
- Example host compiles with defaults merged and one ops attach module running under supervision.
</success_criteria>

<output>
After completion, create `.planning/phases/41-host-metrics-wiring-cross-domain-example/41-02-SUMMARY.md`
</output>

---
phase: 41-host-metrics-wiring-cross-domain-example
plan: 03
type: execute
wave: 3
depends_on: ["41-02-PLAN.md"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TEL-01, OBS-02]
must_haves:
  truths:
    - "REQUIREMENTS.md checkboxes for TEL-01 and OBS-02 reflect shipped Phase 41 work"
    - "Traceability table rows for OBS-01..OBS-04 / TEL-01 / OBS-02 / RUN-01 match actual phase completions through 41"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "D-18 traceability hygiene"
      contains: "Phase 41"
---

<objective>
Reconcile `.planning/REQUIREMENTS.md` per **D-18**: when Phase 41 implementation is done, **TEL-01** and **OBS-02** checkboxes and the **Traceability** table must reflect closure (and **OBS-01 / OBS-03 / OBS-04** rows must show **Phase 40 / Complete** if not already aligned post–Phase 40 ship). Fix any accidental line wraps that split markdown list items (observed around OBS/TEL lines).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-host-metrics-wiring-cross-domain-example/41-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS hygiene + traceability table</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <acceptance_criteria>
    - `rg -n '^- \\[x\\] \\*\\*OBS-02' .planning/REQUIREMENTS.md` finds OBS-02 marked complete (lowercase x).
    - `rg -n '^- \\[x\\] \\*\\*TEL-01' .planning/REQUIREMENTS.md` finds TEL-01 marked complete.
    - Traceability table contains row `| OBS-01 | Phase 40 |` with Status column **Complete** (case-insensitive) or `✓` / `Shipped` consistent with repo style.
    - Traceability row for TEL-01 shows `Phase 41` and **Complete** (or equivalent) status.
    - Traceability row for OBS-02 shows `Phase 41` and **Complete** (or equivalent) status.
    - No broken markdown where `**OBS-01` splits across lines without closing `**` on same bullet — `rg -n '\\*\\*OBS-01$' .planning/REQUIREMENTS.md` returns zero matches.
  </acceptance_criteria>
  <action>Edit `.planning/REQUIREMENTS.md`: (1) Ensure OBS-02 and TEL-01 bullets are single-line markdown with proper `**` closures. (2) Set checkboxes to `[x]` for OBS-02 and TEL-01 once Phase 41 deliverables exist (this task runs last in phase). (3) Update **Traceability** table: OBS-01, OBS-03, OBS-04 → Phase **40**, Status **Complete** (if still Pending from template drift); TEL-01 and OBS-02 → Phase **41**, Status **Complete**. Leave RUN-01 as Phase 42 Pending.</action>
  <verify>
    <automated>bash -lc 'rg -n "Traceability|TEL-01|OBS-02" .planning/REQUIREMENTS.md | head -40'</automated>
  </verify>
  <done>Published REQ state matches shipped telemetry + host work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs | Internal accuracy — affects contributor trust only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Repudiation | Traceability table | mitigate | Phase numbers must match ROADMAP.md authoritative mapping. |
</threat_model>

<verification>
- `rg -n 'OBS-02|TEL-01' .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- Readers see consistent checkbox + table state for v1.9 observability requirements through Phase 41.
</success_criteria>

<output>
After completion, create `.planning/phases/41-host-metrics-wiring-cross-domain-example/41-03-SUMMARY.md`
</output>

---
phase: 42-operator-runbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/operator-runbooks.md
autonomous: true
requirements: [RUN-01]
must_haves:
  truths:
    - "New guide accrue/guides/operator-runbooks.md exists and is linked from guides/telemetry.md in wave 2"
    - "Oban appendix lists every Accrue Oban.Worker queue name that appears in accrue/lib today"
    - "Four mini-playbooks exist for webhook_dlq dead_lettered, events_upcast_failed, meter_reporting_failed, revenue_loss per 42-CONTEXT D-09"
  artifacts:
    - path: "accrue/guides/operator-runbooks.md"
      provides: "RUN-01 procedural depth: Oban topology + playbooks + Stripe verification pattern"
      contains: "Oban queue topology"
  key_links:
    - from: "accrue/guides/operator-runbooks.md"
      to: "accrue/guides/telemetry.md"
      via: "Ops catalog SSOT"
      pattern: "no competing ops table"
---

<objective>
Add **`accrue/guides/operator-runbooks.md`** as the procedural companion to **`guides/telemetry.md`** (42-CONTEXT D-01, D-02, D-05, D-08–D-13): host-owned Oban disclaimer, **Oban queue topology** appendix aligned to current `use Oban.Worker` queues in `accrue/lib`, **Stripe two-layer** verification guidance (Accrue identifiers + Stripe docs / Dashboard paths, no finance-of-record tone), and **numbered mini-playbooks (3–7 steps)** for the four high-risk ops classes in D-09. Do **not** duplicate the ops event catalog table from Phase 40.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-operator-runbooks/42-CONTEXT.md
@.planning/REQUIREMENTS.md
@accrue/lib/accrue/telemetry/ops.ex
@accrue/lib/accrue/webhook/dispatch_worker.ex
@accrue/lib/accrue/workers/mailer.ex
@accrue/lib/accrue/jobs/meter_events_reconciler.ex
@accrue/lib/accrue/jobs/dunning_sweeper.ex
@accrue/lib/accrue/jobs/reconcile_refund_fees.ex
@accrue/lib/accrue/jobs/reconcile_charge_fees.ex
@accrue/lib/accrue/jobs/detect_expiring_cards.ex
@accrue/lib/accrue/webhook/pruner.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold operator-runbooks.md + Oban topology appendix</name>
  <read_first>
    .planning/phases/42-operator-runbooks/42-CONTEXT.md
    accrue/guides/telemetry.md
    accrue/lib/accrue/webhook/dispatch_worker.ex
    accrue/lib/accrue/workers/mailer.ex
    accrue/lib/accrue/jobs/meter_events_reconciler.ex
    accrue/lib/accrue/jobs/dunning_sweeper.ex
    accrue/lib/accrue/jobs/reconcile_refund_fees.ex
    accrue/lib/accrue/jobs/reconcile_charge_fees.ex
    accrue/lib/accrue/jobs/detect_expiring_cards.ex
    accrue/lib/accrue/webhook/pruner.ex
  </read_first>
  <files>accrue/guides/operator-runbooks.md</files>
  <acceptance_criteria>
    - `test -f accrue/guides/operator-runbooks.md` exits 0.
    - `rg -n '^# Operator runbooks' accrue/guides/operator-runbooks.md` returns ≥1 match.
    - `rg -n 'telemetry\\.md|guides/telemetry' accrue/guides/operator-runbooks.md` returns ≥1 match (link to catalog SSOT).
    - `rg -n '^## Oban queue topology' accrue/guides/operator-runbooks.md` returns exactly one match.
    - `rg -n ':accrue_webhooks|:accrue_mailers|:accrue_meters|:accrue_dunning|:accrue_reconcilers|:accrue_scheduled|:accrue_maintenance' accrue/guides/operator-runbooks.md` finds all seven queue atoms as prose or table cells.
    - `rg -n 'DispatchWorker|Accrue\\.Workers\\.Mailer|MeterEventsReconciler|DunningSweeper|ReconcileRefundFees|ReconcileChargeFees|DetectExpiringCards|Accrue\\.Webhook\\.Pruner' accrue/guides/operator-runbooks.md` finds at least six of these module name substrings (queue → worker mapping).
    - `rg -n 'host|Oban' accrue/guides/operator-runbooks.md` finds ≥3 matches in the first 80 lines conveying that **the host application configures and starts Oban** (not Accrue).
  </acceptance_criteria>
  <action>Create **`accrue/guides/operator-runbooks.md`** with: (1) H1 `# Operator runbooks`; (2) opening paragraphs stating scope (**RUN-01**), that **`guides/telemetry.md`** owns the **ops event catalog** (no second catalog), and library vs host boundaries; (3) section **`## Oban queue topology`** containing a **markdown table**: columns **Queue (default name)** | **Worker module** | **Role / when to look** | **Typical symptoms** | **Safe first checks** — rows MUST cover exactly these queue defaults from code: `:accrue_webhooks` → `Accrue.Webhook.DispatchWorker`; `:accrue_mailers` → `Accrue.Workers.Mailer`; `:accrue_meters` → `Accrue.Jobs.MeterEventsReconciler`; `:accrue_dunning` → `Accrue.Jobs.DunningSweeper`; `:accrue_reconcilers` → `Accrue.Jobs.ReconcileChargeFees` and `Accrue.Jobs.ReconcileRefundFees` (one row each or combined with both modules named); `:accrue_scheduled` → `Accrue.Jobs.DetectExpiringCards`; `:accrue_maintenance` → `Accrue.Webhook.Pruner`. Prepend a sentence that **queue names are host-configurable** — Accrue documents these defaults (D-05).</action>
  <verify>
    <automated>bash -lc 'test -f accrue/guides/operator-runbooks.md && rg -n "^## Oban queue topology" accrue/guides/operator-runbooks.md'</automated>
  </verify>
  <done>Canonical procedural doc exists with drift-resistant Oban appendix.</done>
</task>

<task type="auto">
  <name>Task 2: Stripe verification pattern + four mini-playbooks</name>
  <read_first>
    accrue/guides/operator-runbooks.md
    .planning/phases/42-operator-runbooks/42-CONTEXT.md
    accrue/lib/accrue/telemetry/ops.ex
  </read_first>
  <files>accrue/guides/operator-runbooks.md</files>
  <acceptance_criteria>
    - `rg -n '^## Stripe verification pattern' accrue/guides/operator-runbooks.md` returns exactly one match.
    - `rg -n 'https://stripe\\.com/docs/' accrue/guides/operator-runbooks.md` returns ≥2 matches (canonical docs URLs, not dashboard deep links only).
    - `rg -n '^## Mini-playbook:' accrue/guides/operator-runbooks.md` returns exactly four lines (four H2 playbooks).
    - Each playbook H2 immediately followed by an ordered list: `rg -A6 '^## Mini-playbook: \\[:accrue, :ops, :webhook_dlq, :dead_lettered\\]' accrue/guides/operator-runbooks.md` shows lines starting with `1.` through at least `3.` (minimum three numbered steps per playbook; target 3–7).
    - Same structure for playbooks titled with event tuples **exactly**: `[:accrue, :ops, :events_upcast_failed]`, `[:accrue, :ops, :meter_reporting_failed]`, `[:accrue, :ops, :revenue_loss]` (use these strings in the H2 titles so telemetry links can grep them).
    - `rg -ni 'accrue is (your |the )?(system of record|ledger of record|general ledger)' accrue/guides/operator-runbooks.md` returns zero matches (D-13 anti-patterns).
  </acceptance_criteria>
  <action>Append sections: **`## Stripe verification pattern`** explaining the two layers from D-11–D-12 (local rows / telemetry / FKs vs Stripe resource + docs link + functional Dashboard path language). Then append **four** sections with H2 titles exactly:

`## Mini-playbook: [:accrue, :ops, :webhook_dlq, :dead_lettered]`

`## Mini-playbook: [:accrue, :ops, :events_upcast_failed]`

`## Mini-playbook: [:accrue, :ops, :meter_reporting_failed]`

`## Mini-playbook: [:accrue, :ops, :revenue_loss]`

Each with **3–7 numbered steps** covering safe order: confirm scope (dry-run where applicable), inspect Accrue tables or admin tools named in `telemetry.md` runbook row, enqueue/Oban checks pointing at **## Oban queue topology**, then Stripe verification via docs/dashboard — **no** raw secret logging instructions.</action>
  <verify>
    <automated>bash -lc 'rg -c "^## Mini-playbook:" accrue/guides/operator-runbooks.md | awk "{exit !(\$1==4)}"'</automated>
  </verify>
  <done>RUN-01 high-risk paths have ordered procedures; Stripe boundary explicit.</done>
</task>

<task type="auto">
  <name>Task 3: RUN-01 coverage footer + See also</name>
  <read_first>
    accrue/guides/operator-runbooks.md
    accrue/guides/telemetry.md
  </read_first>
  <files>accrue/guides/operator-runbooks.md</files>
  <acceptance_criteria>
    - `rg -n '^## See also' accrue/guides/operator-runbooks.md` returns ≥1 match.
    - `rg -n 'Accrue\\.Telemetry\\.Ops' accrue/guides/operator-runbooks.md` returns ≥1 match.
    - `rg -n ':connect_account_deauthorized|:connect_payout_failed|:dunning_exhaustion|:charge_failed|:incomplete_expired|:pdf_adapter_unavailable' accrue/guides/operator-runbooks.md` returns ≥5 distinct ops tuple substrings (pointer list for remaining RUN-01 classes that stay one-liners in telemetry).
    - `rg -n 'Operator runbooks \\(first actions\\)' accrue/guides/operator-runbooks.md` returns ≥1 match (explicit pointer to the compact table section title in telemetry.md).
  </acceptance_criteria>
  <action>Add **`## RUN-01 coverage`** (short bullet list): link readers to the **`## Operator runbooks (first actions)`** table in `telemetry.md` for one-line first actions for **every** ops class including Connect, dunning exhaustion, charge_failed, incomplete_expired, pdf_adapter_unavailable, replay/prune — clarify that **this file** adds depth for the four mini-playbooks only. Finish with **`## See also`** linking to `telemetry.md`, `Accrue.Telemetry.Ops` module doc path `lib/accrue/telemetry/ops.ex`, and Hex docs path pattern `https://hexdocs.pm/accrue/` without inventing version-specific URLs beyond that pattern.</action>
  <verify>
    <automated>rg -n '^## See also' accrue/guides/operator-runbooks.md</automated>
  </verify>
  <done>Operators see full RUN-01 surface area map back to telemetry compact table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public guide | World-readable — no live secrets, API keys, or customer PII in examples |
| Operational instructions | Mis-ordered replay could worsen billing posture — playbooks must emphasize safe checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Information Disclosure | runbook examples | mitigate | Never instruct copying full webhook metadata or card numbers into tickets |
| T-42-01-02 | Elevation of privilege | replay / DLQ steps | mitigate | State dry-run / admin-gated actions; defer destructive deletes to explicit maintenance playbooks |
| T-42-01-03 | Repudiation | Stripe handoff | mitigate | Point to Stripe docs + Dashboard for finance truth; Accrue state is operational not accounting SSOT |
</threat_model>

<verification>
- `rg -n '^## Oban queue topology' accrue/guides/operator-runbooks.md`
- `rg -n '^## Mini-playbook:' accrue/guides/operator-runbooks.md`
- `! rg -ni 'accrue is (your |the )?(system of record|ledger of record)' accrue/guides/operator-runbooks.md`
</verification>

<success_criteria>
- RUN-01 procedural home exists with Oban topology + four playbooks + Stripe pattern + telemetry cross-links.
- No competing ops catalog table; catalog remains SSOT in telemetry.md.
</success_criteria>

<output>
After completion, create `.planning/phases/42-operator-runbooks/42-01-SUMMARY.md`
</output>

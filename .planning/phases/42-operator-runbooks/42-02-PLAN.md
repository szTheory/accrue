---
phase: 42-operator-runbooks
plan: 02
type: execute
wave: 2
depends_on: ["42-01-PLAN.md"]
files_modified:
  - accrue/guides/telemetry.md
autonomous: true
requirements: [RUN-01]
must_haves:
  truths:
    - "guides/telemetry.md Operator runbooks section has a preface linking operator-runbooks.md before the table"
    - "The four D-09 playbook rows include a stable pointer to ## Oban queue topology in operator-runbooks.md"
  artifacts:
    - path: "accrue/guides/telemetry.md"
      provides: "Fast-scan table + deep link to procedural guide"
      contains: "operator-runbooks.md"
  key_links:
    - from: "accrue/guides/telemetry.md"
      to: "accrue/guides/operator-runbooks.md"
      via: "preface + row anchors"
      pattern: "hybrid pointers D-06"
---

<objective>
Update **`accrue/guides/telemetry.md`** per 42-CONTEXT D-03 and D-06: insert a **short preface** immediately before **`## Operator runbooks (first actions)`** that points to **`operator-runbooks.md`** for Oban topology, mini-playbooks, and expanded Stripe checks — without duplicating the appendix. Augment the **four** table rows for `webhook_dlq` dead_lettered, `events_upcast_failed`, `meter_reporting_failed`, and `revenue_loss` with **one extra clause each** that links to the stable anchor **`#oban-queue-topology`** on the new guide (Markdown auto-anchor for `## Oban queue topology`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-operator-runbooks/42-CONTEXT.md
@accrue/guides/telemetry.md
@accrue/guides/operator-runbooks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preface before Operator runbooks section</name>
  <read_first>
    .planning/phases/42-operator-runbooks/42-CONTEXT.md
    accrue/guides/telemetry.md
    accrue/guides/operator-runbooks.md
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n '^## Operator runbooks \\(first actions\\)' accrue/guides/telemetry.md` returns exactly one match.
    - The file contains `operator-runbooks.md` as a relative link target from `guides/` (either `[text](operator-runbooks.md)` or `` `operator-runbooks.md` `` in prose) — verify: `rg -n 'operator-runbooks\\.md' accrue/guides/telemetry.md` returns ≥1 match.
    - Immediately **above** the `## Operator runbooks` heading, `rg -B12 '^## Operator runbooks \\(first actions\\)' accrue/guides/telemetry.md` output includes the substring `operator-runbooks` (preface in the 12 lines before the heading).
    - Preface is **≤8 lines** of prose + optional blank line (measure: lines between previous section content and `## Operator runbooks` — executor may count in comment).
  </acceptance_criteria>
  <action>Insert a concise preface block **immediately before** the existing line `## Operator runbooks (first actions)` stating: compact one-liners stay in this table; **ordered triage**, **Oban queue reference**, and **expanded Stripe verification** live in **[Operator runbooks](operator-runbooks.md)** (use that link text). Mention **#oban-queue-topology** once in plain text as the anchor for queue details. Do **not** remove or relocate the existing **Use this as a starting point** paragraph — integrate or merge so the section still reads as one coherent intro (you may fold that sentence into the preface if cleaner).</action>
  <verify>
    <automated>bash -lc 'rg -n "operator-runbooks\\.md" accrue/guides/telemetry.md && rg -n "^## Operator runbooks" accrue/guides/telemetry.md'</automated>
  </verify>
  <done>Readers get a stable deep link before the fast-scan table.</done>
</task>

<task type="auto">
  <name>Task 2: Hybrid row pointers for four playbook ops events</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/guides/operator-runbooks.md
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - Table row for dead-lettered contains `operator-runbooks.md#oban-queue-topology` as substring: `rg -n 'webhook_dlq, :dead_lettered' accrue/guides/telemetry.md` line includes that substring.
    - Same for `events_upcast_failed` row and substring `operator-runbooks.md#oban-queue-topology`.
    - Same for `meter_reporting_failed` row.
    - Same for `revenue_loss` row.
    - No other table rows gain a second long paragraph (only a short clause + link for those four) — spot-check: `rg -n 'operator-runbooks\\.md#oban-queue-topology' accrue/guides/telemetry.md` returns **exactly four** matches.
  </acceptance_criteria>
  <action>In the **Operator runbooks** markdown table, extend **only** the **Suggested first actions** cell for these four ops events: `[:accrue, :ops, :webhook_dlq, :dead_lettered]`, `[:accrue, :ops, :events_upcast_failed]`, `[:accrue, :ops, :meter_reporting_failed]`, `[:accrue, :ops, :revenue_loss]` — append one short clause + markdown link `(Oban defaults: [queue topology](operator-runbooks.md#oban-queue-topology))` or equivalent ≤120 characters added per cell so the row stays one line in source if possible. Preserve existing cell advice; append with semicolon or em-dash.</action>
  <verify>
    <automated>bash -lc 'test $(rg -o "operator-runbooks\\.md#oban-queue-topology" accrue/guides/telemetry.md | wc -l | tr -d " ") -eq 4'</automated>
  </verify>
  <done>Hybrid pointers D-06 satisfied without duplicating Oban matrix in telemetry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telemetry.md | Widely read — links must stay relative and correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | broken relative link | mitigate | Link target file must exist after plan 01; use exact `#oban-queue-topology` anchor |
</threat_model>

<verification>
- `rg -n 'operator-runbooks\\.md' accrue/guides/telemetry.md`
- `rg -c 'operator-runbooks\\.md#oban-queue-topology' accrue/guides/telemetry.md` outputs `4`
</verification>

<success_criteria>
- Telemetry runbook table remains the fast-scan SSOT with deep links to procedural guide.
- Four high-risk rows carry Oban topology anchor per D-06.
</success_criteria>

<output>
After completion, create `.planning/phases/42-operator-runbooks/42-02-SUMMARY.md`
</output>

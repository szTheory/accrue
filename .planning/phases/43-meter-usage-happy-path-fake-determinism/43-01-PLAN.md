---
phase: 43-meter-usage-happy-path-fake-determinism
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing.ex
autonomous: true
requirements: [MTR-01]
must_haves:
  truths:
    - "`Accrue.Billing.report_usage/3` and `report_usage!/3` carry ExDoc listing every NimbleOptions key from MeterEventActions.@report_usage_schema"
    - "Option semantics in ExDoc match defaults and types in @report_usage_schema (single SSOT per 43-CONTEXT D-07)"
  artifacts:
    - path: "accrue/lib/accrue/billing.ex"
      provides: "MTR-01 documented public API for opts"
      contains: "report_usage"
  key_links:
    - from: "accrue/lib/accrue/billing.ex"
      to: "accrue/lib/accrue/billing/meter_event_actions.ex"
      via: "@report_usage_schema"
      pattern: "no second conflicting defaults"
---

<threat_model>
**ASVS L1.** Documentation must not embed live Stripe secret keys or real customer PII. Examples for `:payload` use only synthetic keys (e.g. `%{\"dimension\" => \"seats\"}`). No encouragement to log full `opts` in production.
</threat_model>

<objective>
Ship **MTR-01**: ExDoc on **`Accrue.Billing.report_usage/3`** and **`report_usage!/3`** as the **single published options reference** (43-CONTEXT D-07), aligned verbatim with `Accrue.Billing.MeterEventActions` `@report_usage_schema` for `:value`, `:timestamp`, `:identifier`, `:operation_id`, and `:payload` — including defaults, allowed types, and one-line semantics for Fake vs Stripe host configuration (pointer only; no PROC-08).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-meter-usage-happy-path-fake-determinism/43-CONTEXT.md
@.planning/REQUIREMENTS.md
@accrue/lib/accrue/billing/meter_event_actions.ex
@accrue/lib/accrue/billing.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExDoc for report_usage/3</name>
  <read_first>
    accrue/lib/accrue/billing.ex
    accrue/lib/accrue/billing/meter_event_actions.ex
    .planning/phases/43-meter-usage-happy-path-fake-determinism/43-CONTEXT.md
  </read_first>
  <files>accrue/lib/accrue/billing.ex</files>
  <acceptance_criteria>
    - `rg -n '@doc' accrue/lib/accrue/billing.ex | rg report_usage` returns at least one match whose following `@doc` block appears **immediately above** `def report_usage(` (no other `def` between `@doc` and `def report_usage`).
    - That `@doc` fenced or markdown section lists **all five** option atoms as prose or bullet keys: `:value`, `:timestamp`, `:identifier`, `:operation_id`, `:payload`.
    - `rg ':value' accrue/lib/accrue/billing.ex` finds text stating **default 1** for usage count (matching `@report_usage_schema` default).
    - `rg ':timestamp' accrue/lib/accrue/billing.ex` documents that `nil` uses current UTC normalization behavior (reference “see MeterEventActions” or equivalent explicit sentence).
    - `rg ':operation_id' accrue/lib/accrue/billing.ex` documents dedupe / identifier derivation relationship in one sentence (must mention `identifier` uniqueness at audit layer or `operation_id` override).
    - `mix compile --warnings-as-errors` in `accrue/` exits 0 after the edit.
  </acceptance_criteria>
  <action>Add a **`@doc`** immediately above **`def report_usage(customer, event_name, opts \\\\ [])`** in `accrue/lib/accrue/billing.ex`. Content requirements: (1) first line is a clear one-liner; (2) **`## Options`** subsection enumerating **exactly** the keys from `@report_usage_schema` with types/defaults matching `meter_event_actions.ex` lines 36–41; (3) short **## Fake / test mode** paragraph stating hosts can use `Accrue.Processor.Fake` without network (cross-link to `Accrue.Test.setup_fake_processor/1` in backticks, no new guide duplication); (4) state invalid opts raise `NimbleOptions.ValidationError` via `validate!` (name the exception module). Do **not** copy implementation of `derive_identifier/4` into prose — only behavior hosts rely on.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>MTR-01 ExDoc SSOT for report_usage/3.</done>
</task>

<task type="auto">
  <name>Task 2: ExDoc for report_usage!/3</name>
  <read_first>
    accrue/lib/accrue/billing.ex
    accrue/lib/accrue/billing/meter_event_actions.ex
  </read_first>
  <files>accrue/lib/accrue/billing.ex</files>
  <acceptance_criteria>
    - `rg -n 'def report_usage!' accrue/lib/accrue/billing.ex` shows an `@doc` block **immediately above** `def report_usage!` (within 15 lines above, with only whitespace/comments between).
    - That `@doc` contains the substring `report_usage/3` (delegation reference).
    - `rg 'raise' accrue/lib/accrue/billing.ex` matches the `report_usage!` @doc region for **both** `NimbleOptions.ValidationError` (bad opts) and **not-found / APIError** outcomes consistent with `MeterEventActions.report_usage!/3` (string mention of `Accrue.APIError` or "404" / `resource_missing`).
    - `mix compile --warnings-as-errors` in `accrue/` exits 0.
  </acceptance_criteria>
  <action>Add **`@doc`** above **`def report_usage!(customer, event_name, opts \\\\ [])`**: raising variant of `report_usage/3`; link readers to the **`report_usage/3`** doc for the full options table (`See \`report_usage/3\``); document raise on `{:error, _}` from bang implementation (match actual `MeterEventActions.report_usage!/3` behavior — string vs exception).</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>MTR-01 ExDoc for bang variant.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix compile --warnings-as-errors`
- `mix test test/accrue/billing/meter_event_actions_test.exs` (no behavior change expected; guards regressions)
</verification>

<success_criteria>
- MTR-01 satisfied for public API documentation without guide duplication.
- Compiler clean on `accrue` package.
</success_criteria>

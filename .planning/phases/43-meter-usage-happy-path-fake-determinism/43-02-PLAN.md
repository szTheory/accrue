---
phase: 43-meter-usage-happy-path-fake-determinism
plan: "02"
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - accrue/guides/testing.md
autonomous: true
requirements: [MTR-01, MTR-02]
must_haves:
  truths:
    - "guides/testing.md contains a dedicated subsection for Fake meter usage without duplicating the full NimbleOptions table"
    - "Fragment references Accrue.Billing.report_usage/3 HexDocs as options SSOT"
  artifacts:
    - path: "accrue/guides/testing.md"
      provides: "MTR-01 pointer + MTR-02 assertion patterns for hosts"
      contains: "meter"
  key_links:
    - from: "accrue/guides/testing.md"
      to: "accrue/lib/accrue/billing.ex"
      via: "report_usage/3 ExDoc"
      pattern: "deep link to source doc path or HexDocs wording"
---

<threat_model>
**ASVS L1.** Guide examples use Fake customer ids (`cus_fake_*`) only. No copy-paste of secret keys. If showing `:payload`, use non-sensitive synthetic maps.
</threat_model>

<objective>
Add the **focused testing fragment** to **`accrue/guides/testing.md`** (43-CONTEXT D-08) **after Plan 03** lands: happy-path outline for `Accrue.Billing.report_usage/3` on **Fake**, **`Accrue.Test.setup_fake_processor/1`**, assertion order (**Repo row first**, then **`Accrue.Test.meter_events_for/1`** for processor-shaped payloads), **determinism levers** (`operation_id`, `timestamp`, avoid wall-clock in golden tests), and **deep link** text to ExDoc for options — **without** duplicating the NimbleOptions table (D-08).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-meter-usage-happy-path-fake-determinism/43-CONTEXT.md
@accrue/guides/testing.md
@accrue/lib/accrue/test.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usage metering (Fake) subsection in testing.md</name>
  <read_first>
    accrue/guides/testing.md
    .planning/phases/43-meter-usage-happy-path-fake-determinism/43-CONTEXT.md
    accrue/lib/accrue/test.ex
  </read_first>
  <files>accrue/guides/testing.md</files>
  <acceptance_criteria>
    - `rg -ni 'usage meter|meter usage|report_usage' accrue/guides/testing.md` returns ≥1 section heading (H2 or H3) containing **meter** or **usage** in the title.
    - `rg -n 'Accrue\\.Test\\.setup_fake_processor' accrue/guides/testing.md` returns ≥1 match in the new section.
    - `rg -n 'Accrue\\.Billing\\.report_usage' accrue/guides/testing.md` returns ≥1 match.
    - `rg -n 'Accrue\\.Test\\.meter_events_for' accrue/guides/testing.md` returns ≥1 match.
    - `rg -n 'accrue_meter_events|MeterEvent' accrue/guides/testing.md` returns ≥1 match explaining **pending → reported** happy path in plain language.
    - `rg -n 'operation_id|:timestamp|timestamp:' accrue/guides/testing.md` returns ≥2 matches in the new section (determinism levers per D-04–D-06).
    - `rg -n 'NimbleOptions|options table|full option' accrue/guides/testing.md` in the new section: either **zero** NimbleOptions table **or** an explicit sentence that **full keys/defaults live in ExDoc** on `Accrue.Billing.report_usage/3` (must contain substring `report_usage/3`).
    - `rg -ni 'guides/metering\\.md' accrue/guides/testing.md` returns **zero** matches (do not add deferred guide per D-09).
    - `rg -n 'telemetry\\.md|meter_reporting_failed' accrue/guides/testing.md` returns at most **one** sentence + link forward (D-09 ceiling); if present, must be single-line pointer, not a new ops table.
  </acceptance_criteria>
  <action>Insert a new section (suggested H2 **`## Usage metering (Fake)`** or H3 under an existing billing testing chapter — match the file’s heading style). Content: (1) one sentence on Fake vs Live Stripe; (2) `Accrue.Test.setup_fake_processor/1` in `setup`; (3) call `Accrue.Billing.report_usage/3` with explicit `timestamp:` for stable tests; (4) assert **`Accrue.Billing.MeterEvent`** via Repo (`stripe_status == \"reported\"`, `identifier`, `value`) as the primary contract; (5) document **`Accrue.Test.meter_events_for/1`** for Fake payload shape (must match function added in Plan 03); (6) link full opts to **`Accrue.Billing.report_usage/3`** ExDoc (`lib/accrue/billing.ex` or HexDocs “Accrue.Billing”).</action>
  <verify>
    <automated>bash -lc 'rg -n "Usage metering|report_usage" accrue/guides/testing.md | head'</automated>
  </verify>
  <done>MTR-01/MTR-02 host-facing testing fragment shipped.</done>
</task>

</tasks>

<verification>
- All `<acceptance_criteria>` greps in this plan pass after Plan 03 merge.
</verification>

<success_criteria>
- `guides/testing.md` gives a copy-pasteable Fake metering path without duplicating ExDoc options tables.
- No `guides/metering.md` reference introduced.
</success_criteria>

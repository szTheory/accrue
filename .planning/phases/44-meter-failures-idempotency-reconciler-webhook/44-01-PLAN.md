---
phase: 44-meter-failures-idempotency-reconciler-webhook
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/meter_events.ex
  - accrue/lib/accrue/billing/meter_event_actions.ex
  - accrue/lib/accrue/billing.ex
  - accrue/test/accrue/billing/meter_event_actions_test.exs
  - accrue/guides/telemetry.md
  - accrue/lib/accrue/telemetry/metrics.ex
  - accrue/guides/operator-runbooks.md
  - accrue/CHANGELOG.md
autonomous: true
requirements: [MTR-04]
must_haves:
  truths:
    - "`[:accrue, :ops, :meter_reporting_failed]` emits at most once per durable transition of a meter row into terminal `failed` from the sync path; idempotent `report_usage/3` replay does not duplicate the event"
    - "Telemetry metadata uses `source: :sync` (not `:inline`) on the synchronous reporting failure path"
    - "`report_usage!/3` returns `%MeterEvent{stripe_status: \"failed\"}` when the non-bang API would return `{:ok, that_row}` on idempotent replay after failure"
  artifacts:
    - path: "accrue/lib/accrue/billing/meter_events.ex"
      provides: "Shared guarded failure transition + optional telemetry hook used by sync/reconciler/webhook"
      contains: "mark_failed"
    - path: "accrue/lib/accrue/billing/meter_event_actions.ex"
      provides: "MTR-04 sync semantics + skip processor when row already terminal"
      contains: "report_usage"
  key_links:
    - from: "accrue/lib/accrue/billing/meter_event_actions.ex"
      to: "accrue/lib/accrue/billing/meter_events.ex"
      via: "failure choke point"
      pattern: "single telemetry owner for terminal failed"
---

<threat_model>
**ASVS L1.** Do not log raw Stripe error bodies containing PII. `inspect(err)` in telemetry metadata is acceptable only for structured `Accrue.APIError`-style maps already sanitized by existing `MeterEvent.failed_changeset/2` patterns; preserve redaction conventions from Phase 4 Plan 02. No secrets in ExDoc examples.
</threat_model>

<objective>
Deliver **MTR-04** plus **D-01..D-07** from `44-CONTEXT.md`: rename ops `source` **`:inline` → `:sync`**, centralize “mark `failed` + emit `meter_reporting_failed` only on first qualifying transition” in **`Accrue.Billing.MeterEvents`**, implement **idempotent read-your-writes** return contract on **`report_usage/3`** / **`report_usage!/3`**, extend **`Accrue.Billing`** ExDoc for error vs row semantics, and align **`guides/telemetry.md`**, **`Accrue.Telemetry.Metrics`** tag docs, **`operator-runbooks.md`** mini-playbook wording, and **`CHANGELOG.md`** (Telemetry subsection) with the contract change.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-meter-failures-idempotency-reconciler-webhook/44-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/v1.10-METERING-SPIKE.md
@accrue/lib/accrue/billing/meter_event_actions.ex
@accrue/lib/accrue/billing/meter_events.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeterEvents failure choke (guarded transition + telemetry)</name>
  <read_first>
    accrue/lib/accrue/billing/meter_events.ex
    accrue/lib/accrue/billing/meter_event.ex
    accrue/lib/accrue/billing/meter_event_actions.ex
    .planning/phases/44-meter-failures-idempotency-reconciler-webhook/44-CONTEXT.md
  </read_first>
  <files>accrue/lib/accrue/billing/meter_events.ex</files>
  <acceptance_criteria>
    - New or extended public function(s) on `Accrue.Billing.MeterEvents` perform **failed** transition from **`stripe_status == "pending"`** (and any other states explicitly allowed in CONTEXT D-04) using an **Ecto update with `WHERE` guard** (or `Repo.update` on a struct loaded with `lock: :dirty` / stale check) such that **no-op** updates return a distinguishable result **without** emitting telemetry.
    - A single helper is invoked for sync failures **from** `MeterEventActions` by the end of this plan — `rg emit_failure_telemetry accrue/lib/accrue/billing/meter_event_actions.ex` returns **no** matches (logic lives on `MeterEvents`).
    - The new `MeterEvents` API is **documented `@doc`** with parameters **`source`** (`:sync` | `:reconciler` | `:webhook`) so Plans **02–03** can call it without new private hacks.
    - `mix format` on touched files; `cd accrue && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>Implement **D-04** choke point on `MeterEvents`: add something like `mark_failed_with_telemetry/3` taking `%MeterEvent{}` or id, **sanitized** error payload, and `source` atom (`:sync` | `:reconciler` | `:webhook`). Use **`MeterEvent.failed_changeset/2`** for field writes. Use **`Repo.update_all`** with `where: stripe_status == ^\"pending\"` (adjust if CONTEXT requires including one additional transitional state — document in `@doc`) returning `{count, _}`. If `count == 1`, execute `[:accrue, :ops, :meter_reporting_failed]` with metadata **`source`** as passed, **`meter_event_id`**, **`event_name`**, and **`error`** as today’s shape. If `count == 0`, **skip telemetry** (idempotent replay / race). Export only the API surface needed by `MeterEventActions` in this plan; reconciler/webhook can be wired in Plans 02–03.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>MeterEvents central failure transition.</done>
</task>

<task type="auto">
  <name>Task 2: MeterEventActions — :sync, idempotent returns, delegate telemetry</name>
  <read_first>
    accrue/lib/accrue/billing/meter_event_actions.ex
    accrue/lib/accrue/billing/meter_events.ex
    accrue/test/accrue/billing/meter_event_actions_test.exs
  </read_first>
  <files>accrue/lib/accrue/billing/meter_event_actions.ex</files>
  <acceptance_criteria>
    - `rg :inline accrue/lib/accrue/billing/meter_event_actions.ex` returns **no** matches.
    - After `Fake.scripted_response(:report_meter_event, {:error, err})`, first `Billing.report_usage/3` returns `{:error, err}` **and** repo row has `stripe_status == \"failed\"` **and** exactly **one** telemetry message received by a test handler with **`source: :sync`** (replace L113 assertion `:inline` → `:sync`).
    - Second `Billing.report_usage/3` with **same** `customer`, `event_name`, `value`, `timestamp`, and `operation_id` as first call returns **`{:ok, %MeterEvent{stripe_status: \"failed\"}}`** (not `{:error, _}`) and test process receives **no second** `{:fail, ...}` message (use `refute_receive` with 100ms timeout or `assert_receive` max count pattern).
    - `Billing.report_usage!/3` on that idempotent replay returns the **failed** struct **without** raising.
    - `cd accrue && mix test test/accrue/billing/meter_event_actions_test.exs` exits 0.
  </acceptance_criteria>
  <action>In `report_usage/3` after `insert_pending/6`: if existing row has `stripe_status` in **`[\"reported\", \"failed\"]`**, **skip** `Processor.report_meter_event/1` and return **`{:ok, row}`** immediately (D-05). If `pending`, run processor; on `{:error, err}` call **`MeterEvents`** choke from Task 1 with **`source: :sync`** instead of manual `failed_changeset` + `emit_failure_telemetry(..., :inline)`; return **`{:error, err}`** only when this **call** performed the transition attempt that surfaced the error (preserve D-07: if choke returns no transition because row already failed **from a prior call in the same test**, follow idempotent **`{:ok, failed}`** rule). Update moduledoc lines 17–18: replace “reconciler” vs “inline” wording with **“sync request path vs reconciler vs webhook”** per **D-01**.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/billing/meter_event_actions_test.exs'</automated>
  </verify>
  <done>MTR-04 sync path behavior locked in tests.</done>
</task>

<task type="auto">
  <name>Task 3: Billing ExDoc — error tuple vs persisted row (D-07)</name>
  <read_first>
    accrue/lib/accrue/billing.ex
    accrue/lib/accrue/billing/meter_event_actions.ex
  </read_first>
  <files>accrue/lib/accrue/billing.ex</files>
  <acceptance_criteria>
    - `rg -n 'report_usage' accrue/lib/accrue/billing.ex` shows `@doc` (or `@doc false` replacement with public wrapper doc) containing a short paragraph that states: **`{:error, _}`** means the **call** could not advance durable state as requested; hosts should **inspect the `MeterEvent` row** (`stripe_status`, `stripe_error`) after retries when using idempotent keys.
    - `mix compile --warnings-as-errors` passes.
  </acceptance_criteria>
  <action>Add **one** concise paragraph to the public **`report_usage/3`** (and bang variant if documented) `@doc` in `accrue/lib/accrue/billing.ex` per **D-07** — no duplication of full options table from Phase 43.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>ExDoc alignment.</done>
</task>

<task type="auto">
  <name>Task 4: Docs + CHANGELOG — :sync vocabulary (D-01, D-02)</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/guides/operator-runbooks.md
    accrue/lib/accrue/telemetry/metrics.ex
    accrue/CHANGELOG.md
  </read_first>
  <files>accrue/guides/telemetry.md,accrue/guides/operator-runbooks.md,accrue/lib/accrue/telemetry/metrics.ex,accrue/CHANGELOG.md</files>
  <acceptance_criteria>
    - `rg ':inline' accrue/guides/telemetry.md accrue/guides/operator-runbooks.md accrue/lib/accrue/telemetry/metrics.ex` returns **no** matches for meter `source` documentation (file-wide `:inline` unrelated to meters may remain elsewhere — if any false positive, restrict grep to meter rows only).
    - `CHANGELOG.md` **Unreleased** section contains a **Telemetry** bullet explicitly stating **`meter_reporting_failed` metadata `source`** value **`:inline` renamed to `:sync`** for sync request path failures.
    - `rg 'meter_reporting_failed' accrue/guides/telemetry.md` shows the catalog row still lists **`source`** as **`:sync | :reconciler | :webhook`** (order may vary).
  </acceptance_criteria>
  <action>Update meter ops catalog row and mini-playbook bullets to **`:sync`**. In `Accrue.Telemetry.Metrics` `@moduledoc` (if present) or adjacent comment for `accrue.ops.meter_reporting_failed.count`, document tag **`:source`** closed set **`:sync | :reconciler | :webhook`**. Append CHANGELOG bullet under **Unreleased** per **D-02**.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs'</automated>
  </verify>
  <done>Telemetry contract documented.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix compile --warnings-as-errors`
- `cd accrue && mix test test/accrue/billing/meter_event_actions_test.exs`
- `cd accrue && mix test test/accrue/telemetry/ops_event_contract_test.exs` (if present; else `mix test test/accrue/telemetry/`)
</verification>

<success_criteria>
- **MTR-04** satisfied: Fake sync failure, exactly one ops emit with **`source: :sync`**, no duplicate on idempotent retry; bang variant correct on replay.
- Guides and CHANGELOG consistent with **D-01..D-02**.
</success_criteria>

---
phase: 44-meter-failures-idempotency-reconciler-webhook
plan: "02"
type: execute
wave: 2
depends_on: ["44-01-PLAN.md"]
files_modified:
  - accrue/lib/accrue/jobs/meter_events_reconciler.ex
  - accrue/test/accrue/jobs/meter_events_reconciler_test.exs
autonomous: true
requirements: [MTR-05]
must_haves:
  truths:
    - "Stuck `pending` rows past grace window are retried by `MeterEventsReconciler.reconcile/0`"
    - "Processor `{:error, _}` from reconciler emits at most one `meter_reporting_failed` with `source: :reconciler` per qualifying transition"
  artifacts:
    - path: "accrue/lib/accrue/jobs/meter_events_reconciler.ex"
      provides: "Reconciler uses MeterEvents failure choke"
      contains: "reconcile"
  key_links:
    - from: "accrue/lib/accrue/jobs/meter_events_reconciler.ex"
      to: "accrue/lib/accrue/billing/meter_events.ex"
      via: "mark_failed_with_telemetry"
      pattern: "no duplicate inline :telemetry.execute"
---

<threat_model>
**ASVS L1.** Reconciler logs and telemetry must not include Stripe secret keys. Use existing `inspect(err)` patterns only on structured API errors already returned by Fake/Stripe adapter.
</threat_model>

<objective>
Deliver **MTR-05** per **44-CONTEXT D-08, D-09**: wire **`Accrue.Jobs.MeterEventsReconciler`** failure path through the **Plan 01** `MeterEvents` guarded choke so **`meter_reporting_failed`** uses **`source: :reconciler`** exactly once per durable transition; add **Repo-first** tests with **backdated `inserted_at`** (no process kill, no sleep) plus optional narrow telemetry attach.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-meter-failures-idempotency-reconciler-webhook/44-CONTEXT.md
@accrue/lib/accrue/jobs/meter_events_reconciler.ex
@accrue/lib/accrue/billing/meter_events.ex
@accrue/test/accrue/jobs/meter_events_reconciler_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciler delegates failure to MeterEvents choke</name>
  <read_first>
    accrue/lib/accrue/jobs/meter_events_reconciler.ex
    accrue/lib/accrue/billing/meter_events.ex
    accrue/lib/accrue/billing/meter_event.ex
  </read_first>
  <files>accrue/lib/accrue/jobs/meter_events_reconciler.ex</files>
  <acceptance_criteria>
    - `rg ':telemetry.execute' accrue/lib/accrue/jobs/meter_events_reconciler.ex` returns **no** match for **`[:accrue, :ops, :meter_reporting_failed]`** (all failure telemetry routed through `MeterEvents`).
    - `MeterEventsReconciler` `@moduledoc` no longer says **“inline vs deferred”** for failure source — use **sync / reconciler / webhook** vocabulary per **D-01**.
    - `cd accrue && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>Replace the `{:error, err}` branch’s manual `failed_changeset` + `:telemetry.execute` block with a call to the **Plan 01** `MeterEvents` API, passing **`source: :reconciler`** and the same error object shape used on the sync path. Preserve success path (`reported_changeset`) unchanged.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Reconciler uses shared choke.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciler tests — backdated pending + telemetry (D-08, D-09)</name>
  <read_first>
    accrue/test/accrue/jobs/meter_events_reconciler_test.exs
    accrue/test/accrue/billing/meter_event_actions_test.exs
    accrue/test/support/billing_case.ex
  </read_first>
  <files>accrue/test/accrue/jobs/meter_events_reconciler_test.exs</files>
  <acceptance_criteria>
    - Test inserts `%MeterEvent{stripe_status: \"pending\"}` (via same factories/helpers as existing reconciler tests or `Repo.insert` on changeset), then runs **`Repo.update_all`** to set **`inserted_at`** to **at least 120 seconds** in the past (relative to `DateTime.utc_now()` or `Accrue.Clock.utc_now/0` if tests use clock stub).
    - **`MeterEventsReconciler.reconcile/0`** returns `{:ok, _}` and row ends **`failed`** when Fake scripts `{:error, _}` for `:report_meter_event`.
    - **At most one** `:telemetry.attach` handler in that test receives **`%{source: :reconciler}`** and **`%{count: 1}`** (use `assert_received` once; no duplicate).
    - `cd accrue && mix test test/accrue/jobs/meter_events_reconciler_test.exs` exits 0.
    - Test module uses **`use Accrue.BillingCase, async: false`** per **D-09**.
  </acceptance_criteria>
  <action>Extend `meter_events_reconciler_test.exs`: add failure-path test meeting D-08/D-09. Reuse **`Fake.scripted_response/2`**. If an existing test already covers failure telemetry inline, **rewrite** it to assert **`source: :reconciler`** and Repo state instead of duplicating scenarios.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/jobs/meter_events_reconciler_test.exs'</automated>
  </verify>
  <done>MTR-05 test contract.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix test test/accrue/jobs/meter_events_reconciler_test.exs`
- `cd accrue && mix test test/accrue/billing/meter_event_actions_test.exs`
</verification>

<success_criteria>
- **MTR-05** satisfied with deterministic tests and reconciler moduledoc aligned to vocabulary.
</success_criteria>

---
phase: 44-meter-failures-idempotency-reconciler-webhook
plan: "03"
type: execute
wave: 2
depends_on: ["44-01-PLAN.md"]
files_modified:
  - accrue/lib/accrue/webhook/dispatch_worker.ex
  - accrue/lib/accrue/webhook/default_handler.ex
  - accrue/lib/accrue/billing/meter_events.ex
  - accrue/test/accrue/webhook/handlers/billing_meter_error_report_test.exs
  - accrue/test/accrue/webhook/dispatch_worker_test.exs
autonomous: true
requirements: [MTR-06]
must_haves:
  truths:
    - "`DispatchWorker`-driven `DefaultHandler.handle_event/3` receives full Stripe meter error object in `ctx` for identifier extraction"
    - "Webhook persistence + `meter_reporting_failed` uses `source: :webhook` at most once per qualifying `failed` transition (D-12)"
  artifacts:
    - path: "accrue/lib/accrue/webhook/dispatch_worker.ex"
      provides: "ctx carries embedded object map"
      contains: "ctx"
    - path: "accrue/lib/accrue/webhook/default_handler.ex"
      provides: "MTR-06 handle_event clauses for meter error types"
      contains: "billing.meter.error_report_triggered"
  key_links:
    - from: "accrue/lib/accrue/webhook/default_handler.ex"
      to: "accrue/lib/accrue/billing/meter_events.ex"
      via: "reduce_meter_error_report"
      pattern: "same reducer as handle/1"
---

<threat_model>
**ASVS L1.** Persisted webhook `data` may contain sensitive fields — pass **only** the embedded meter object into handler `ctx`, not the full signing payload. Do not add new `Logger.info` that dumps full `ctx`. Structured metadata for telemetry must stay Stripe-reference-shaped (codes/messages), not raw PII.
</threat_model>

<objective>
Deliver **MTR-06** plus **D-10..D-13** from `44-CONTEXT.md`: fix **production** `DispatchWorker` → **`handle_event/3`** path by enriching **`ctx`** with persisted **`data["object"]`**, add explicit **`handle_event`** clauses for **`billing.meter.error_report_triggered`** and **`v1.billing.meter.error_report_triggered`** that invoke **`reduce_meter_error_report/2`** with the embedded object; extend **`MeterEvents.mark_failed_by_identifier/2`** (or companion) to use the **Plan 01** guarded choke for **transition-only** telemetry (**D-12**); add tests proving **DispatchWorker-shaped** context (**D-11**); optionally enrich webhook telemetry metadata per **D-13** when trivial.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-meter-failures-idempotency-reconciler-webhook/44-CONTEXT.md
@accrue/lib/accrue/webhook/dispatch_worker.ex
@accrue/lib/accrue/webhook/default_handler.ex
@accrue/lib/accrue/webhook/event.ex
@accrue/lib/accrue/billing/meter_events.ex
@accrue/test/accrue/webhook/handlers/billing_meter_error_report_test.exs
@accrue/test/accrue/webhook/dispatch_worker_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: DispatchWorker ctx — embedded meter object</name>
  <read_first>
    accrue/lib/accrue/webhook/dispatch_worker.ex
    accrue/lib/accrue/webhook/webhook_event.ex
    accrue/lib/accrue/webhook/event.ex
  </read_first>
  <files>accrue/lib/accrue/webhook/dispatch_worker.ex</files>
  <acceptance_criteria>
    - `ctx` map passed to `safe_handle/3` includes atom or string key **`:meter_error_object`** or **`\"meter_error_object\"`** (pick **one** convention and document in `@moduledoc` for DispatchWorker) whose value is the **raw embedded Stripe object** map extracted from persisted webhook row **`row.data`** using string keys consistent with JSON storage: path **`[\"data\", \"object\"]`** (defensive `get_in` with nil fallback to `%{}` acceptable if type unknown).
    - `mix compile --warnings-as-errors` passes.
  </acceptance_criteria>
  <action>After `Event.from_webhook_event(row)`, build `ctx` with existing keys plus **`meter_error_object: get_in(row.data, [\"data\", \"object\"])`** (normalize key style to match handler expectations). Do **not** break Connect vs platform branch.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>ctx enrichment.</done>
</task>

<task type="auto">
  <name>Task 2: DefaultHandler — handle_event clauses + reduce_meter_error_report choke</name>
  <read_first>
    accrue/lib/accrue/webhook/default_handler.ex
    accrue/lib/accrue/billing/meter_events.ex
  </read_first>
  <files>accrue/lib/accrue/webhook/default_handler.ex,accrue/lib/accrue/billing/meter_events.ex</files>
  <acceptance_criteria>
    - Two **`def handle_event(\"billing.meter.error_report_triggered\", ...)`** and **`def handle_event(\"v1.billing.meter.error_report_triggered\", ...)`** clauses exist **above** the broad `def handle_event(type, %Accrue.Webhook.Event{} = event, _ctx)` clause (or pattern match is unambiguous), each calling into **`reduce_meter_error_report/2`** with **`event.processor_event_id`** (or equivalent evt id) and **object from `ctx`**.
    - `reduce_meter_error_report/2` no longer calls `:telemetry.execute` directly when row transition is a no-op — delegated to **`MeterEvents`** guarded API with **`source: :webhook`** so duplicate webhook delivery does **not** increment ops counter (**D-12**).
    - `cd accrue && mix test test/accrue/webhook/handlers/billing_meter_error_report_test.exs` exits 0.
  </acceptance_criteria>
  <action>Implement **D-10**: clauses take `%Accrue.Webhook.Event{}` + `ctx` map, read embedded object via agreed `ctx` key, pass to **`reduce_meter_error_report(evt_id, obj)`**. Refactor **`MeterEvents.mark_failed_by_identifier/2`** to use guarded transition helper from Plan 01, returning **`{:ok, row, :transitioned | :noop}`** or equivalent so **`reduce_meter_error_report`** emits telemetry **only on transitioned** to `failed`. Add **optional** `Logger.info` on noop redelivery per **D-12** if low-noise. Per **D-13**, add **`error_code`** / **`message`** keys to webhook telemetry metadata when derivable from `stripe_error` map without scope creep.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/webhook/handlers/billing_meter_error_report_test.exs'</automated>
  </verify>
  <done>Webhook reducer + persistence aligned.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — unversioned event + DispatchWorker-shaped handle_event</name>
  <read_first>
    accrue/test/accrue/webhook/handlers/billing_meter_error_report_test.exs
    accrue/test/accrue/webhook/dispatch_worker_test.exs
    accrue/test/support/webhook_case.ex
  </read_first>
  <files>accrue/test/accrue/webhook/handlers/billing_meter_error_report_test.exs,accrue/test/accrue/webhook/dispatch_worker_test.exs</files>
  <acceptance_criteria>
    - At least one test exercises **`billing.meter.error_report_triggered`** (unversioned string) end-to-end through **`DefaultHandler`** (existing helper or new).
    - At least one test calls **`DefaultHandler.handle_event(type, %Accrue.Webhook.Event{}, ctx)`** where **`ctx`** includes the **`meter_error_object`** (or chosen key) shaped like Stripe’s nested payload containing **`identifier`** (reuse fixtures from `billing_meter_error_report_test.exs` or `StripeFixtures` per CONTEXT).
    - `cd accrue && mix test test/accrue/webhook/handlers/billing_meter_error_report_test.exs accrue/test/accrue/webhook/dispatch_worker_test.exs` exits 0.
  </acceptance_criteria>
  <action>Add/adjust tests per **D-11**. If `dispatch_worker_test.exs` lacks meter coverage, add minimal test: build **`%WebhookEvent{}`** row in DB (or use factory), invoke **`DispatchWorker.perform/1`** with `Oban.Job` args, assert **`DefaultHandler`** marks meter row failed when fixture object present — **or** unit-test `handle_event` with synthetic `ctx` if full worker test is prohibitive (document choice in test moduledoc).</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/webhook/handlers/billing_meter_error_report_test.exs test/accrue/webhook/dispatch_worker_test.exs'</automated>
  </verify>
  <done>MTR-06 verification.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix test test/accrue/webhook/handlers/billing_meter_error_report_test.exs test/accrue/webhook/dispatch_worker_test.exs`
- `cd accrue && mix test test/accrue/billing/meter_event_actions_test.exs test/accrue/jobs/meter_events_reconciler_test.exs`
</verification>

<success_criteria>
- **MTR-06** satisfied: both event type strings covered; production dispatch path proven; webhook dedupe telemetry behavior matches **D-12**.
</success_criteria>

---
phase: 45-docs-telemetry-runbook-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/metering.md
  - accrue/lib/accrue/billing.ex
autonomous: true
requirements: [MTR-07]
must_haves:
  truths:
    - "`guides/metering.md` explains public `Accrue.Billing.report_usage/*` vs internal `MeterEvent` / outbox lifecycle vs processor `report_meter_event/1` without duplicating the NimbleOptions table from ExDoc"
    - "Metering ops narrative defers to `guides/telemetry.md` (at most one sentence + link) for `meter_reporting_failed`"
  artifacts:
    - path: "accrue/guides/metering.md"
      provides: "MTR-07 host-facing architecture map"
      contains: "report_usage"
  key_links:
    - from: "accrue/guides/metering.md"
      to: "accrue/guides/telemetry.md"
      via: "operator / failure telemetry"
      pattern: "single hop to ops SSOT"
---

<threat_model>
**ASVS L1.** Doc examples must use Fake / synthetic identifiers only (`cus_…` test ids from Stripe test docs are acceptable; no live API keys, no webhook signing secrets, no raw customer emails). Do not paste production-shaped JSON bodies.
</threat_model>

<objective>
Ship **MTR-07**: add thin **`accrue/guides/metering.md`** per **45-CONTEXT D-01..D-04** and optionally add a **single** ExDoc cross-link from **`Accrue.Billing.report_usage/3`** (`@report_usage_doc`) to that guide — without copying **`@report_usage_schema`** option tables.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-docs-telemetry-runbook-alignment/45-CONTEXT.md
@.planning/REQUIREMENTS.md
@accrue/lib/accrue/billing.ex
@accrue/guides/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guides/metering.md (thin spine)</name>
  <read_first>
    .planning/phases/45-docs-telemetry-runbook-alignment/45-CONTEXT.md
    accrue/guides/testing.md
    accrue/guides/telemetry.md
    accrue/lib/accrue/billing/meter_event_actions.ex
  </read_first>
  <files>accrue/guides/metering.md</files>
  <acceptance_criteria>
    - File `accrue/guides/metering.md` exists and `rg -n '^# ' accrue/guides/metering.md` shows exactly **one** top-level H1.
    - `rg -n 'report_usage' accrue/guides/metering.md` returns at least **one** line stating hosts call **`Accrue.Billing.report_usage`** (or `report_usage/3`) for metered usage.
    - `rg -n 'NimbleOptions|@report_usage_schema|## Options' accrue/guides/metering.md` returns **no** matches (options stay in ExDoc only).
    - `rg -n 'MeterEvent|outbox|pending' accrue/guides/metering.md` has ≥ **1** match explaining **internal** row lifecycle at concept level (words need not be exact but intent readable).
    - `rg -n 'Accrue\\.Processor|report_meter_event|Fake' accrue/guides/metering.md` has ≥ **1** match tying reporting to the **processor** seam and tests/Fake.
    - `rg -n 'PROC-08|second processor' accrue/guides/metering.md` has ≥ **1** match explicitly scoping **PROC-08** out of the v1.10 narrative (per **D-02**).
    - `rg -n '\\| Event \\|' accrue/guides/metering.md` returns **no** matches (no duplicate ops markdown **table** mimicking `telemetry.md`).
    - `rg -n 'telemetry\\.md' accrue/guides/metering.md` returns ≥ **1** match (link to ops / failure path).
    - `cd accrue && mix docs` exits **0**.
  </acceptance_criteria>
  <action>Create **`accrue/guides/metering.md`** with this **minimum** outline (headings may be reworded; keep substance):
    1. H1 such as **"Usage metering"** + one intro sentence: this guide is the **architecture map**; ExDoc on **`Accrue.Billing.report_usage/3`** owns option keys.
    2. **Public API** — `Accrue.Billing.report_usage/3` and `report_usage!/3`; sentence pointing to ExDoc **`## Options`** (do not inline the schema).
    3. **Internal persistence** — transactional insert / `pending` → `reported` / `failed` at **concept** level; name **`Accrue.Billing.MeterEvent`** and outbox; link module paths as plain code spans (`Accrue.Billing.MeterEventActions`, `Accrue.Billing.MeterEvents`) — no API dump.
    4. **Processor** — configured **`Accrue.Processor`** implements **`report_meter_event/1`**; Fake for tests; Stripe when host configures real processor.
    5. **Out of scope** — one explicit sentence: **PROC-08** / additional processors are not covered beyond the seam.
    6. **When metering fails (ops)** — **at most two sentences** + markdown link **`telemetry.md`** (and optionally **`operator-runbooks.md`**) — **no** ops event table, no second catalog (**D-03**).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue && mix docs'</automated>
  </verify>
  <done>Metering guide created.</done>
</task>

<task type="auto">
  <name>Task 2: Optional ExDoc cross-link on report_usage</name>
  <read_first>
    accrue/lib/accrue/billing.ex
    accrue/guides/metering.md
  </read_first>
  <files>accrue/lib/accrue/billing.ex</files>
  <acceptance_criteria>
    - `rg -n 'metering\\.md' accrue/lib/accrue/billing.ex` returns ≥ **1** match inside **`@report_usage_doc`** (the heredoc assigned before `@doc @report_usage_doc`).
    - The inserted sentence is **≤ 25 words** and appears in the **`## Error tuples vs persisted rows`** section **or** immediately after **`## Fake / test mode`** (pick one placement only — not both).
    - `cd accrue && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>Append **one** short sentence to **`@report_usage_doc`** in **`accrue/lib/accrue/billing.ex`**: point readers to **`guides/metering.md`** for the public vs internal vs processor map. Use the repo-relative path string **`guides/metering.md`** (same style as other guide references in the codebase). If the sentence would duplicate the new guide’s intro, instead add a single **"See also `guides/metering.md`."** line after **## Fake / test mode** only.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>ExDoc cross-link added.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix docs`
- `cd accrue && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- **MTR-07** satisfied: published **`guides/metering.md`** meets CONTEXT **D-01..D-03** (thin spine, no options table, no ops catalog fork).
- Optional **D-04** fallback **not** taken unless executor documents skip in SUMMARY with PM approval — default is ship the guide.
</success_criteria>

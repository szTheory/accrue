---
phase: 45-docs-telemetry-runbook-alignment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/telemetry.md
autonomous: true
requirements: [MTR-08]
must_haves:
  truths:
    - "`guides/telemetry.md` documents that `[:accrue, :ops, :meter_reporting_failed]` reflects a durable terminal `failed` transition (not per-retry noise) and lists causal origins for `:sync`, `:reconciler`, `:webhook` under the catalog SSOT"
    - "The ops catalog row for `meter_reporting_failed` remains unchanged in meaning (measurements/metadata/owners columns) — only additive prose below the table"
  artifacts:
    - path: "accrue/guides/telemetry.md"
      provides: "Semantics subsection + stable anchor"
      contains: "meter_reporting_failed"
  key_links:
    - from: "accrue/guides/operator-runbooks.md"
      to: "accrue/guides/telemetry.md"
      via: "meter-reporting-semantics anchor"
      pattern: "procedure → contract"
---

<threat_model>
**ASVS L1.** Do not add example webhook payloads, secrets, or customer-identifying fields to the new prose. Keep pointers module-level (`Accrue.Billing.MeterEventActions`, etc.).
</threat_model>

<objective>
Extend **`accrue/guides/telemetry.md`** with a compact **“Semantics & sources”** block for **`[:accrue, :ops, :meter_reporting_failed]`** per **45-CONTEXT D-05..D-07**: preserve the single catalog row; clarify **durable terminal failure** vs retries; document **`:sync`**, **`:reconciler`**, **`:webhook`** in **three bullets**; add HTML anchor **`{#meter-reporting-semantics}`** for runbook deep links.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-docs-telemetry-runbook-alignment/45-CONTEXT.md
@accrue/lib/accrue/billing/meter_events.ex
@accrue/guides/telemetry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert semantics subsection + anchor under ops catalog</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/lib/accrue/billing/meter_events.ex
    .planning/phases/44-meter-failures-idempotency-reconciler-webhook/44-CONTEXT.md
  </read_first>
  <files>accrue/guides/telemetry.md</files>
  <acceptance_criteria>
    - `rg -n 'meter-reporting-semantics' accrue/guides/telemetry.md` returns **≥ 1** match (HTML anchor `{#meter-reporting-semantics}` adjacent to the new heading).
    - `rg -n 'Meter reporting failures|meter reporting failures|Semantics' accrue/guides/telemetry.md` matches a new **H2** heading (wording flexible) placed **after** the line containing **`\\| \\[:accrue, :ops, :meter_reporting_failed\\]`** in the ops catalog table **and** before the paragraph that begins **`Connect ops rows above`** (exact prefix grep).
    - `rg -ni 'durable|terminal' accrue/guides/telemetry.md` matches **≥ 1** occurrence inside the **20 lines** following that new H2 (operator-language sentence per **D-06**).
    - `rg -n ':sync' accrue/guides/telemetry.md` and `rg -n ':reconciler' accrue/guides/telemetry.md` and `rg -n ':webhook' accrue/guides/telemetry.md` each return **≥ 1** match in the **30 lines** following the new H2 (three bullets or equivalent list items).
    - `rg -n 'MeterEventActions|MeterEventsReconciler|DefaultHandler|DispatchWorker' accrue/guides/telemetry.md` has **≥ 3** total matches in the file after edit (distribution across bullets is fine).
    - `rg -n '\\| Event \\|' accrue/guides/telemetry.md` returns **exactly 1** line matching that header pattern (no **second** ops catalog table introduced).
    - `cd accrue && mix docs` exits **0**.
  </acceptance_criteria>
  <action>Immediately **after** the ops catalog markdown table (after the **`meter_reporting_failed`** row, before the paragraph starting **`Connect ops rows above`**) insert:
    - Raw HTML anchor line: `<a id="meter-reporting-semantics"></a>` **immediately before** a new **`## ...`** heading (title should include **Semantics** or **sources** for grep stability).
    - **One short paragraph** in operator voice: **`meter_reporting_failed`** fires on the **first durable** transition of a meter row into terminal **`failed`** (align with **`Accrue.Billing.MeterEvents`** guarded update); **not** once per HTTP retry, webhook redelivery, or idempotent replay (**D-06** / Phase 44 **D-03**).
    - **Three bullets** — **`:sync`**, **`:reconciler`**, **`:webhook`** — each **one line** of causal origin + pointer to implementation area (`Accrue.Billing.MeterEventActions`, `Accrue.Jobs.MeterEventsReconciler`, `Accrue.Webhook.DefaultHandler` / `Accrue.Webhook.DispatchWorker` path). **Do not** add a full metadata key matrix (**D-07**).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue && mix docs'</automated>
  </verify>
  <done>Telemetry semantics documented.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix docs`
- `rg -n 'meter_reporting_failed' accrue/guides/telemetry.md | head`
</verification>

<success_criteria>
- **MTR-08** (telemetry half): operators can read **when** and **why** `meter_reporting_failed` fires and how **`source`** maps to code paths — without contradicting the v1.9 catalog row.
</success_criteria>

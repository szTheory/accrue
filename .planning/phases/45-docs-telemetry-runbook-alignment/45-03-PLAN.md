---
phase: 45-docs-telemetry-runbook-alignment
plan: "03"
type: execute
wave: 2
depends_on:
  - "45-01-PLAN.md"
  - "45-02-PLAN.md"
files_modified:
  - accrue/guides/operator-runbooks.md
autonomous: true
requirements: [MTR-08]
must_haves:
  truths:
    - "`operator-runbooks.md` meter mini-playbook adds source-aware ordered branches that elaborate (not duplicate) the catalog row, with deep link to `telemetry.md#meter-reporting-semantics`"
    - "No second `| Event |` ops catalog table appears in runbooks"
  artifacts:
    - path: "accrue/guides/operator-runbooks.md"
      provides: "RUN-01 meter playbook depth"
      contains: "meter_reporting_failed"
  key_links:
    - from: "accrue/guides/operator-runbooks.md"
      to: "accrue/guides/telemetry.md"
      via: "#meter-reporting-semantics"
      pattern: "alert → contract → procedure"
---

<threat_model>
**ASVS L1.** Runbook steps must stay identifier-only for triage (no raw webhook JSON, no secrets). Reinforce Grafana/alert annotations link to **telemetry contract** first (**D-10**).
</threat_model>

<objective>
Extend **`accrue/guides/operator-runbooks.md`** **mini-playbook:** **`[:accrue, :ops, :meter_reporting_failed]`** with **source-aware ordered branches** (**`:sync`** vs **`:reconciler`** vs **`:webhook`**) per **45-CONTEXT D-08..D-10**, deep-linking to **`telemetry.md#meter-reporting-semantics`** instead of redefining the ops tuple.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-docs-telemetry-runbook-alignment/45-CONTEXT.md
@accrue/guides/operator-runbooks.md
@accrue/guides/telemetry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend meter_reporting_failed mini-playbook</name>
  <read_first>
    accrue/guides/operator-runbooks.md
    accrue/guides/telemetry.md
    .planning/phases/42-operator-runbooks/42-CONTEXT.md
  </read_first>
  <files>accrue/guides/operator-runbooks.md</files>
  <acceptance_criteria>
    - `rg -n 'meter-reporting-semantics' accrue/guides/operator-runbooks.md` returns **≥ 1** match linking to **`telemetry.md#meter-reporting-semantics`** (relative link from `guides/`).
    - Under the existing **`## Mini-playbook: \\[:accrue, :ops, :meter_reporting_failed\\]`** heading, the doc contains a **subheading or bold run-in** for each of **`:sync`**, **`:reconciler`**, **`:webhook`** source branches (grep `:sync`, `:reconciler`, `:webhook` within **80 lines** after the mini-playbook H2 — each appears ≥1).
    - Each branch has **≥ 2 numbered ordered steps** (digits `1.` pattern) specific to that source (not generic duplicates across all three).
    - `rg -n '^\\| Event \\|' accrue/guides/operator-runbooks.md` returns **no** matches (no ops catalog table in runbooks).
    - `cd accrue && mix docs` exits **0**.
  </acceptance_criteria>
  <action>Under **`## Mini-playbook: [:accrue, :ops, :meter_reporting_failed]`** in **`accrue/guides/operator-runbooks.md`**:
    1. Keep existing opening steps that apply to all sources if still accurate; then add **three** clearly labeled branches (**sync request path**, **reconciler**, **webhook / meter error report**).
    2. For **each** branch: **ordered** triage steps (Oban queue **`:accrue_meters`** for reconciler; request-path logs / same txn for sync; webhook ingest + **`DispatchWorker`** pointer for webhook — align with Phase 44 wiring narrative).
    3. Add one line per branch (or shared preamble) mandating: read **contract** details at **`telemetry.md#meter-reporting-semantics`** before tuning alerts (**D-10**).
    4. Do **not** paste the ops catalog markdown table; do not restate measurements/metadata columns beyond short reminders already in playbook tone.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue && mix docs'</automated>
  </verify>
  <done>Runbook branches added.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix docs`
- `rg 'meter-reporting-semantics' accrue/guides/operator-runbooks.md`
</verification>

<success_criteria>
- **MTR-08** (runbook half): operators triaging **`meter_reporting_failed`** have **source-specific** procedures that **compose with** (not fork) **`guides/telemetry.md`**.
</success_criteria>

---
phase: 45-docs-telemetry-runbook-alignment
plan: "04"
type: execute
wave: 2
depends_on:
  - "45-01-PLAN.md"
  - "45-02-PLAN.md"
  - "45-03-PLAN.md"
files_modified:
  - accrue/guides/testing.md
  - accrue/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "If executed, optional cross-links reduce hop count from testing/README to metering + telemetry without duplicating bodies (CONTEXT D-11..D-12)"
  artifacts:
    - path: "accrue/guides/testing.md"
      provides: "optional metering guide hop"
      contains: "metering.md"
    - path: "accrue/README.md"
      provides: "optional README TOC line"
      contains: "guides/testing.md"
  key_links: []
---

<threat_model>
**ASVS L1.** README/testing edits: no secrets; links only to in-repo guides.
</threat_model>

<objective>
Optional stretch (**45-CONTEXT D-11..D-12**): add **minimal** reciprocal navigation between **`guides/testing.md`**, **`guides/metering.md`**, **`guides/telemetry.md`**, and **at most one** **`README.md`** line — only if doing so does not bloat README into a full guide index.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-docs-telemetry-runbook-alignment/45-CONTEXT.md
@accrue/guides/testing.md
@accrue/README.md
@accrue/guides/metering.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: testing.md — link to metering spine</name>
  <read_first>
    accrue/guides/testing.md
    accrue/guides/metering.md
  </read_first>
  <files>accrue/guides/testing.md</files>
  <acceptance_criteria>
    - `rg -n 'metering\\.md' accrue/guides/testing.md` returns **≥ 1** match in the **"Usage metering (Fake)"** section (heading matched by `rg -n 'Usage metering' accrue/guides/testing.md`).
    - The new text is **≤ 2 sentences** and does not duplicate the NimbleOptions / ExDoc option list.
    - `cd accrue && mix docs` exits **0**.
  </acceptance_criteria>
  <action>In **`accrue/guides/testing.md`** within **## Usage metering (Fake)**, after the paragraph that mentions **`guides/telemetry.md`** for failures, add **one** sentence linking **`guides/metering.md`** for the **public vs internal vs processor** map (**D-12**). If that would repeat the new guide’s intro verbatim, instead append a short **"See `guides/metering.md`."** clause to the existing failure-path sentence.</action>
  <verify>
    <automated>bash -lc 'cd accrue && mix docs'</automated>
  </verify>
  <done>Testing guide link added.</done>
</task>

<task type="auto">
  <name>Task 2: README.md — optional one-liner</name>
  <read_first>
    accrue/README.md
    accrue/guides/testing.md
  </read_first>
  <files>accrue/README.md</files>
  <acceptance_criteria>
    - Either **`rg -n 'guides/testing\\.md' accrue/README.md`** returns **≥ 1** new match **or** executor records **skip** in SUMMARY with reason "README already has equivalent link" (then README must already contain **`guides/testing.md`** via `rg`).
    - If modified: **≤ 1** new markdown bullet or sentence under an existing **Docs** / **Guides** / **Documentation** section — **no** new section with more than **3** links.
    - `cd accrue && mix docs` exits **0** (README is an extra; compile not strictly required if untouched).
  </acceptance_criteria>
  <action>If **`accrue/README.md`** lacks any pointer to **`guides/testing.md`**, add **one** line under the most appropriate existing list/paragraph pointing hosts to **`guides/testing.md`** for verification posture (**D-12**). If such a link already exists, **skip** this task and note in phase SUMMARY.</action>
  <verify>
    <automated>bash -lc 'cd accrue && (test -f accrue/README.md && rg -q "guides/testing.md" accrue/README.md && echo README_OK) || true; cd accrue && mix docs'</automated>
  </verify>
  <done>README stretch evaluated.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix docs`
</verification>

<success_criteria>
- Optional evaluator-facing link graph improved per **D-11..D-12** without comprehensive README↔all-guides expansion.
</success_criteria>

---
phase: 46-release-train-hex-publish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release-pr-automation.yml
  - RELEASING.md
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "Release Please PRs are not merged by unattended `pull_request` auto-merge; maintainer uses explicit merge or `workflow_dispatch` after review (CONTEXT D-06, D-09)"
    - "`RELEASING.md` describes the same policy as `.github/workflows/release-pr-automation.yml`"
  artifacts:
    - path: ".github/workflows/release-pr-automation.yml"
      provides: "Human-gated merge contract for billing releases"
    - path: "RELEASING.md"
      provides: "REL-01 maintainer path narrative aligned with workflows"
  key_links:
    - from: "RELEASING.md"
      to: "scripts/ci/gh_merge_release_pr.sh"
      via: "optional merge helper after review"
      pattern: "explicit operator action"
---

<threat_model>
**ASVS L1.** Do not add secrets, PAT literals, or `HEX_API_KEY` values to YAML or markdown. Workflow tokens remain `${{ secrets.* }}` / `${{ github.token }}` only. Comments must not suggest pasting API keys into repo files.
</threat_model>

<objective>
Close the **D-06 / D-09** gap: **`release-pr-automation.yml` must not queue auto-merge on `pull_request` for `release-please--*`** (unattended ship). Update **`RELEASING.md`** so **REL-01** readers see one story: human-reviewed merge, optional **`workflow_dispatch`**, and **`scripts/ci/gh_merge_release_pr.sh`**. Add a short **partial Hex publish recovery** subsection (bullets: retry admin → revert window for mistaken core → retire + forward-fix) with link **`https://hex.pm/docs/faq`** only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-release-train-hex-publish/46-CONTEXT.md
@.planning/REQUIREMENTS.md
@.github/workflows/release-pr-automation.yml
@RELEASING.md
@scripts/ci/gh_merge_release_pr.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Human-gate Release PR automation</name>
  <read_first>
    .github/workflows/release-pr-automation.yml
    .planning/phases/46-release-train-hex-publish/46-CONTEXT.md
  </read_first>
  <files>.github/workflows/release-pr-automation.yml</files>
  <acceptance_criteria>
    - `rg -n '^on:' -A20 .github/workflows/release-pr-automation.yml` shows **`workflow_dispatch` only** under `on:` (no `pull_request:` block remains).
    - File header comment (first 25 lines) states **maintainer-owned merge** for Release Please PRs and that **`workflow_dispatch`** is used to queue merge **after** explicit maintainer intent.
    - `rg -n 'pull_request' .github/workflows/release-pr-automation.yml` returns **no** matches.
    - YAML still defines job **`queue-merge-when-green`** with **`if:`** condition that is **only** `github.event_name == 'workflow_dispatch'` (no `pull_request` branch).
    - **`Resolve PR number`** step writes **`number=${{ github.event.inputs.pr_number }}`** to **`GITHUB_OUTPUT`** without an **`else`** branch reading **`github.event.pull_request`**.
    - **`concurrency.group`** uses only **`github.event.inputs.pr_number`** (no **`github.event.pull_request.number`**).
  </acceptance_criteria>
  <action>Remove the entire **`on: pull_request`** stanza from **`.github/workflows/release-pr-automation.yml`**. Keep **`workflow_dispatch`** with input **`pr_number`** as the **only** trigger. Rewrite the top file comment to: (1) billing-adjacent releases require **human merge or explicit dispatch** after checklist; (2) **`gh pr merge --merge --auto`** runs only when a maintainer dispatches the workflow with a PR number they chose; (3) optional pointer to **`scripts/ci/gh_merge_release_pr.sh`** for discovering the RP PR number. Set job **`if:`** to **`github.event_name == 'workflow_dispatch'`** only. Simplify **`Resolve PR number`** to always echo **`inputs.pr_number`**. Set **`concurrency.group`** to **`release-pr-automation-${{ github.event.inputs.pr_number }}`**.</action>
  <verify>
    <automated>bash -lc 'rg -q "^on:" .github/workflows/release-pr-automation.yml'</automated>
  </verify>
  <done>Release PR workflow is dispatch-only.</done>
</task>

<task type="auto">
  <name>Task 2: Align RELEASING.md with human-gate policy</name>
  <read_first>
    RELEASING.md
    .github/workflows/release-pr-automation.yml
  </read_first>
  <files>RELEASING.md</files>
  <acceptance_criteria>
    - `rg -n 'Auto-merge when CI is green' RELEASING.md` returns **no** matches OR the same heading is rewritten to **`Optional: queue merge after maintainer review`** and the body says auto-merge runs **only** via **`workflow_dispatch`** (not on PR open/sync).
    - `rg -n 'release-pr-automation' RELEASING.md` has ≥ **1** line stating **`workflow_dispatch`** + PR number (exact phrase **`workflow_dispatch`** appears).
    - `rg -n 'Merge the reviewed release PR' RELEASING.md` has ≥ **1** match (preserve human-first ordering in bootstrap steps).
    - `rg -n 'https://hex\\.pm/docs/faq' RELEASING.md` returns ≥ **1** match inside a new subsection whose heading contains **`Partial`** AND **`Hex`** (case-insensitive: use `rg -ni`).
  </acceptance_criteria>
  <action>Edit **`RELEASING.md`**: (1) Replace the **“Auto-merge when CI is green”** subsection so it matches the updated workflow (no automatic subscribe on `pull_request`). (2) In **“Same-day `1.0.0` bootstrap”** step 4, remove the parenthetical that says maintainers may rely on auto-merge on PR events; instead say: run **Actions → Release PR automation → Run workflow** with the PR number **after** checklist sign-off, **or** merge manually. (3) Add **`## Partial Hex publish recovery`** (or equivalent) with 4–6 bullets covering: retry **`accrue_admin`** publish for same version; **`mix hex.publish --revert`** only for mistaken **`accrue`** inside Hex’s short window; otherwise **`mix hex.retire`** + new paired version; link **`https://hex.pm/docs/faq`**. Keep **REL-03** narrative scope out (defer to Phase 47) — this section is **ops recovery** only.</action>
  <verify>
    <automated>bash -lc 'rg -n "workflow_dispatch" RELEASING.md | head -1'</automated>
  </verify>
  <done>Runbook matches workflow.</done>
</task>

</tasks>

<verification>
- `rg -n 'pull_request' .github/workflows/release-pr-automation.yml` → no output
- `rg -ni 'partial.*hex|hex.*partial' RELEASING.md`
</verification>

<success_criteria>
- **REL-01:** Maintainer reading **`RELEASING.md`** + **`.github/workflows/release-please.yml`** is not misled into thinking Release Please PRs auto-merge on green **without** an explicit maintainer step.
</success_criteria>

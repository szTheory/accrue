---
phase: 46-release-train-hex-publish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_release_manifest_alignment.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "`.release-please-manifest.json` versions for `accrue` and `accrue_admin` equal each `@version` in the respective `mix.exs` on every PR CI run"
  artifacts:
    - path: "scripts/ci/verify_release_manifest_alignment.sh"
      provides: "Deterministic REL-02 SSOT preflight"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking job wiring"
  key_links:
    - from: "scripts/ci/verify_release_manifest_alignment.sh"
      to: ".release-please-manifest.json"
      via: "jq version keys"
      pattern: "manifest ↔ mix.exs"
---

<threat_model>
**ASVS L1.** Script must only read repository files (no network, no secret env). Do not echo file contents to logs beyond failing diff messages.
</threat_model>

<objective>
Add **`scripts/ci/verify_release_manifest_alignment.sh`** and a fast blocking CI job **`release-manifest-ssot`** so **REL-02** drift (manifest vs **`mix.exs` `@version`**) fails before merge. Wire **`annotation-sweep`** to depend on the new job and include it in **`annotation_sweep.sh`** arguments. Update the **`ci.yml` header comment** job list to mention **`release-manifest-ssot`** as merge-blocking.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-release-train-hex-publish/46-CONTEXT.md
@.planning/phases/46-release-train-hex-publish/46-PATTERNS.md
@.release-please-manifest.json
@accrue/mix.exs
@accrue_admin/mix.exs
@.github/workflows/ci.yml
@scripts/ci/verify_package_docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verify_release_manifest_alignment.sh</name>
  <read_first>
    scripts/ci/verify_package_docs.sh
    .release-please-manifest.json
    accrue/mix.exs
    accrue_admin/mix.exs
  </read_first>
  <files>scripts/ci/verify_release_manifest_alignment.sh</files>
  <acceptance_criteria>
    - File **`scripts/ci/verify_release_manifest_alignment.sh`** exists, starts with **`#!/usr/bin/env bash`** and **`set -euo pipefail`**.
    - `chmod +x scripts/ci/verify_release_manifest_alignment.sh` is recorded in git (`git ls-files -s scripts/ci/verify_release_manifest_alignment.sh` shows mode **100755**).
    - Script extracts **`@version`** from **`accrue/mix.exs`** and **`accrue_admin/mix.exs`** using the same **`sed`** pattern as **`verify_package_docs.sh`**: `sed -n 's/^  @version "\\([^"]*\\)"/\\1/p'`.
    - Script requires **`jq`** and fails if manifest key missing or semver string empty.
    - `bash scripts/ci/verify_release_manifest_alignment.sh` exits **0** on current **`main`** checkout.
  </acceptance_criteria>
  <action>Create **`scripts/ci/verify_release_manifest_alignment.sh`** that: (1) reads **`.release-please-manifest.json`** keys **`accrue`** and **`accrue_admin`**; (2) parses both **`mix.exs`** `@version` lines; (3) fails with stderr message if either pair mismatches; (4) fails if the two manifest versions differ (lockstep violation on `main`); (5) prints **`OK:`** plus the shared version on success. Use **`fail()`** helper style from **`verify_package_docs.sh`** if convenient.</action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_release_manifest_alignment.sh'</automated>
  </verify>
  <done>SSOT script added.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release-manifest-ssot job into CI</name>
  <read_first>
    .github/workflows/ci.yml
    scripts/ci/verify_release_manifest_alignment.sh
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <acceptance_criteria>
    - `rg -n '^  release-manifest-ssot:' .github/workflows/ci.yml` returns **≥ 1** line.
    - Job runs on **`ubuntu-24.04`**, has **`if: github.event_name != 'schedule'`**, contains **`actions/checkout@v6`** and **`run: bash scripts/ci/verify_release_manifest_alignment.sh`**.
    - `annotation-sweep` job **`needs:`** list includes **`release-manifest-ssot`** alongside existing jobs.
    - `annotation_sweep.sh` invocation line includes the literal token **`release-manifest-ssot`** as an argument.
    - Top-of-file **`ci.yml` comment** (first 15 lines) mentions **`release-manifest-ssot`** in the merge-blocking bullet list.
  </acceptance_criteria>
  <action>Insert new job **`release-manifest-ssot`** immediately after the **`jobs:`** key (before **`release-gate`**) so it runs early. Add **`release-manifest-ssot`** to **`annotation-sweep.needs`** and extend the **`bash scripts/ci/annotation_sweep.sh`** argument list. Update header comments to document merge-blocking inclusion.</action>
  <verify>
    <automated>bash -lc 'grep -A200 "^jobs:" .github/workflows/ci.yml | head -80'</automated>
  </verify>
  <done>CI wired.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_release_manifest_alignment.sh`
- `rg -n 'release-manifest-ssot' .github/workflows/ci.yml`
</verification>

<success_criteria>
- **REL-02:** Manifest and both package **`@version`** lines cannot silently diverge on a green PR.
</success_criteria>

---
phase: 46-release-train-hex-publish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md
autonomous: true
requirements: [REL-04]
must_haves:
  truths:
    - "`46-VERIFICATION.md` defines the evidence index card (D-12) so a ship fills REL-04 with links + commands, not log dumps"
  artifacts:
    - path: ".planning/phases/46-release-train-hex-publish/46-VERIFICATION.md"
      provides: "Phase verification template for tags + Hex + PR anchors"
  key_links:
    - from: "46-VERIFICATION.md"
      to: "RELEASING.md"
      via: "maintainer checklist cross-ref"
      pattern: "runbook ↔ evidence"
---

<threat_model>
**ASVS L1.** Template uses placeholders only (`REPLACE`, `TODO`, or `_ _ _`). No live API keys, webhook secrets, or customer data. Hex commands are documented as local maintainer runs, not CI secrets.
</threat_model>

<objective>
Create **`.planning/phases/46-release-train-hex-publish/46-VERIFICATION.md`** as a **fill-in index card** implementing **46-CONTEXT D-11 / D-12**: ordered evidence blocks for **REL-01**, **REL-02**, **REL-04** with placeholders for PR URL, merge SHA, tag names, CI run links, changelog paths, and **`mix hex.info`** command blocks.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-release-train-hex-publish/46-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 46-VERIFICATION.md template</name>
  <read_first>
    .planning/phases/46-release-train-hex-publish/46-CONTEXT.md
  </read_first>
  <files>.planning/phases/46-release-train-hex-publish/46-VERIFICATION.md</files>
  <acceptance_criteria>
    - File **`.planning/phases/46-release-train-hex-publish/46-VERIFICATION.md`** exists.
    - `rg -c '^## [0-9]+\\.' .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md` prints a count **≥ 9** (matches **D-12** ordered blocks 1–9).
    - `rg -n 'REL-01|REL-02|REL-04' .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md` has ≥ **3** matches total covering each ID at least once.
    - `rg -n 'mix hex\\.info accrue' .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md` returns ≥ **1** line in a fenced **`bash`** code block.
    - `rg -n 'accrue-v|accrue_admin-v' .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md` returns ≥ **1** line documenting tag naming.
    - `rg -n 'REPLACE_|TODO|___' .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md` returns ≥ **1** match (explicit placeholders for executor fill-in).
  </acceptance_criteria>
  <action>Write **`46-VERIFICATION.md`** with: H1 **“Phase 46 verification”**; intro citing **D-12**; **nine** numbered **`## N.`** sections mirroring **46-CONTEXT D-12** order: (1) phase + REL IDs; (2) Release Please PR + merge SHA placeholders; (3) tags **`accrue-v{VERSION}`** / **`accrue_admin-v{VERSION}`**; (4) **`mix hex.info`** commands in a **`bash`** fence; (5) changelog path anchors; (6) CI run links naming **`release-manifest-ssot`** + **`release-gate`** + **`release-please.yml`** workflow; (7) minimal consumer smoke (**`mix new`** / path deps example — keep generic, no secrets); (8) version coupling paragraph (`{:accrue, "~> X"}`); (9) support posture bullets linking **`SECURITY.md`**. Use **`REPLACE_ME`** style tokens for URLs/SHAs. </action>
  <verify>
    <automated>bash -lc 'test -f .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md && wc -l .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md | awk "{print \\$1 >= 40}"'</automated>
  </verify>
  <done>Verification template authored.</done>
</task>

</tasks>

<verification>
- `wc -l .planning/phases/46-release-train-hex-publish/46-VERIFICATION.md` ≥ 40 lines
</verification>

<success_criteria>
- **REL-04:** Template exists so first real train captures **tags + Hex** evidence without inventing structure at ship time.
</success_criteria>

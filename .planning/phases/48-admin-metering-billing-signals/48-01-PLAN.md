---
phase: 48-admin-metering-billing-signals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/live/dashboard_live.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
autonomous: true
requirements: [ADM-01]
must_haves:
  truths:
    - "Admin home shows one new KPI for COUNT of `accrue_meter_events` rows with `stripe_status == \"failed\"`, placed first in `ax-kpi-grid`"
    - "Deep link is `ScopedPath.build(mount, \"/events\", owner_scope)` with copy that does not claim the events list is filtered to meter failures"
    - "All new operator strings live in `AccrueAdmin.Copy` under `dashboard_meter_reporting_failures_*` names (exact prefix per tasks)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/dashboard_live.ex"
      provides: "ADM-01 metering signal on entry path"
      contains: "MeterEvent"
    - path: "accrue_admin/lib/accrue_admin/copy.ex"
      provides: "SSOT copy for new KPI"
      contains: "dashboard_meter_reporting_failures"
  key_links:
    - from: "new KPI card"
      to: "EventsLive index"
      via: "href /events"
      pattern: "honest ledger navigation"
---

<threat_model>
**ASVS L1.** Dashboard aggregate is a numeric count only — no new logging of `stripe_error` maps, Stripe IDs in operator copy beyond what existing cards already do, and no embedding of raw customer email in new strings. Test fixtures use synthetic `stripe_customer_id` / `identifier` values consistent with other `MeterEvent` tests (`cus_…`, `accrue_mev_…` style).
</threat_model>

<objective>
Deliver **ADM-01**: one **credible metering-adjacent** KPI on **`AccrueAdmin.Live.DashboardLive`** counting **terminal failed** `MeterEvent` rows, with **honest** navigation to **`/events`** and **`AccrueAdmin.Copy`** SSOT per **48-CONTEXT.md** and **48-UI-SPEC.md**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-admin-metering-billing-signals/48-CONTEXT.md
@.planning/phases/48-admin-metering-billing-signals/48-UI-SPEC.md
@.planning/phases/48-admin-metering-billing-signals/48-RESEARCH.md
@.planning/phases/48-admin-metering-billing-signals/48-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dashboard_stats with failed MeterEvent count</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/dashboard_live.ex
    accrue/lib/accrue/billing/meter_event.ex
    .planning/phases/48-admin-metering-billing-signals/48-CONTEXT.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex</files>
  <acceptance_criteria>
    - `rg -n 'alias Accrue\\.Billing' accrue_admin/lib/accrue_admin/live/dashboard_live.ex` includes **`MeterEvent`** alongside existing `Customer`, `Invoice`, etc.
    - `rg -n 'failed_meter_event_count' accrue_admin/lib/accrue_admin/live/dashboard_live.ex` returns ≥ **1** match as a **key** in the **`dashboard_stats/0`** return map.
    - `rg -n 'MeterEvent' accrue_admin/lib/accrue_admin/live/dashboard_live.ex` shows a **`where`** clause matching **`stripe_status == \"failed\"`** (string form as stored in schema) before **`Repo\\.aggregate\\(:count, :id\\)`**.
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    In **`accrue_admin/lib/accrue_admin/live/dashboard_live.ex`**:
    1. Add **`alias Accrue.Billing.MeterEvent`** to the existing **`Accrue.Billing`** alias line (or a dedicated alias line immediately below imports).
    2. In **`dashboard_stats/0`**, add **`failed_meter_event_count:`** computed as **`Repo.aggregate`** on **`from(m in MeterEvent, where: m.stripe_status == \"failed\")`** — use the same **`import Ecto.Query`** / **`from`** style as **`blocked_webhook_count`**.
    3. Merge the key into the returned **`%{...}`** map alongside **`customer_count`**, **`blocked_webhook_count`**, etc.
    **Do not** add `pending` count KPI. **Defer D-03** optional 24h delta query unless you add a second map key **`failed_meter_event_recent_count`** only when implementation also adds matching Copy + UI in the same commit (default: **omit** 24h secondary for Phase 48).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Failed meter-event aggregate available on dashboard assigns pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Add AccrueAdmin.Copy helpers for the meter KPI</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex
    .planning/phases/48-admin-metering-billing-signals/48-UI-SPEC.md
    .planning/phases/48-admin-metering-billing-signals/48-CONTEXT.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex</files>
  <acceptance_criteria>
    - `rg -n 'def dashboard_meter_reporting_failures_label' accrue_admin/lib/accrue_admin/copy.ex` returns **≥ 1** line.
    - `rg -n 'def dashboard_meter_reporting_failures_meta' accrue_admin/lib/accrue_admin/copy.ex` returns **≥ 1** line.
    - `rg -n 'def dashboard_meter_reporting_failures_aria_label' accrue_admin/lib/accrue_admin/copy.ex` returns **≥ 1** line.
    - **`meta`** function return string contains the substring **`accrue_meter_events`** OR **`meter event`** (documents data source honesty per UI-SPEC).
    - **`aria_label`** return string contains **`event`** (lowercase) and **does not** contain the substring **`filtered`**.
    - `rg -n 'Webhook backlog|webhook backlog' accrue_admin/lib/accrue_admin/copy.ex` shows **no new lines** editing existing webhook KPI keys (meter strings must use the new **`dashboard_meter_reporting_failures_*`** prefix only).
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    Append functions under the existing dashboard section comment in **`accrue_admin/lib/accrue_admin/copy.ex`** using this **exact function name prefix:** **`dashboard_meter_reporting_failures_`**:
    - **`dashboard_meter_reporting_failures_label/0`** — names the aggregate (terminal **failed** meter-event rows / meter reporting failures), **not** vague **“Usage”** alone.
    - **`dashboard_meter_reporting_failures_meta/0`** — one sentence: counts **`accrue_meter_events`** rows in **`stripe_status=\"failed\"`** (operator-readable).
    - **`dashboard_meter_reporting_failures_aria_label/0`** — action + destination, e.g. opening **billing event ledger** at **`/events`**, **without** claiming the list is pre-filtered to meter rows.
    Optional fourth function **`dashboard_meter_reporting_failures_recent_suffix/0`** only if Task 1 shipped a 24h secondary stat and the LiveView renders a delta line.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Copy SSOT for meter KPI added.</done>
</task>

<task type="auto">
  <name>Task 3: Insert first KpiCard + mount assigns</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/dashboard_live.ex
    accrue_admin/lib/accrue_admin/components/kpi_card.ex
    accrue_admin/lib/accrue_admin/scoped_path.ex
    .planning/phases/48-admin-metering-billing-signals/48-UI-SPEC.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/dashboard_live.ex</files>
  <acceptance_criteria>
    - In **`dashboard_live.ex` `render/1`**, the first **`KpiCard.kpi_card`** after **`<section class=\"ax-kpi-grid\"`** uses **`Copy.dashboard_meter_reporting_failures_label()`** for **`label`**.
    - **`value=`** is **`Integer.to_string(@stats.failed_meter_event_count)`** (exact assign key name **`failed_meter_event_count`**).
    - **`href=`** is **`ScopedPath.build(@admin_mount_path, \"/events\", @current_owner_scope)`** (literal **`\"/events\"`** segment).
    - **`aria_label=`** uses **`Copy.dashboard_meter_reporting_failures_aria_label()`** only (no string literal for aria text).
    - **`delta_tone`** is **`if(@stats.failed_meter_event_count > 0, do: \"amber\", else: \"moss\")`** when a delta slot is rendered; if no delta, omit **`delta`** and **`delta_tone`** attrs entirely.
    - `rg -n 'dashboard_meter_reporting_failures_' accrue_admin/lib/accrue_admin/live/dashboard_live.ex` shows **≥ 3** **`Copy.`** references and **zero** new freeform English strings inside **`~H\"\"\"`** for this card (numeric **`Integer.to_string`** allowed).
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    In **`render/1`**, insert the new **`KpiCard`** as the **first** child inside **`section.ax-kpi-grid`**, **before** the existing Customers card. Reuse the webhook card’s linked pattern (**`href`**, **`aria_label`**, optional **`delta`** + **`delta_tone`**) per **48-UI-SPEC**. If optional 24h stat exists from Task 1, wire **`delta`** using **`Integer.to_string(@stats.failed_meter_event_recent_count)`** + **`Copy.dashboard_meter_reporting_failures_recent_suffix()`**; otherwise ship **value + meta only**.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Meter KPI visible first in grid.</done>
</task>

<task type="auto">
  <name>Task 4: Tests — fixture + LiveView assertions</name>
  <read_first>
    accrue_admin/test/accrue_admin/live/dashboard_live_test.exs
    accrue/test/accrue/webhook/dispatch_worker_test.exs
    accrue/lib/accrue/billing/meter_event.ex
    accrue_admin/test/support/test_repo.ex
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/dashboard_live_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'MeterEvent' accrue_admin/test/accrue_admin/live/dashboard_live_test.exs` returns ≥ **1** line (alias or module reference).
    - Test inserts **≥ 1** row with **`stripe_status`** **`\"failed\"`** persisted via **`TestRepo`** (or **`MeterEvent.failed_changeset/2`** after insert).
    - `assert html =~ Copy.dashboard_meter_reporting_failures_label()` appears in **`dashboard_live_test.exs`**.
    - `assert html =~ ~s(href=\"/billing/events\")` **or** equivalent org-scoped pattern **`href=\"/billing/events?`** — choose the assertion that matches **`ScopedPath`** for default scope in this test session.
    - `assert html =~ Copy.dashboard_meter_reporting_failures_aria_label()` passes.
    - `cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs` exits **0**.
  </acceptance_criteria>
  <action>
    Extend **`setup`** in **`accrue_admin/test/accrue_admin/live/dashboard_live_test.exs`**:
    1. Build a **`MeterEvent`** row tied to the existing **`customer`** from **`Factory.customer`** (reuse **`customer.id`** as **`customer_id`**).
    2. Use **`MeterEvent.pending_changeset/1`** with required fields: **`stripe_customer_id`**, **`event_name`**, **`value`**, **`identifier`**, **`occurred_at`** — use a **globally unique** **`identifier`** string containing **`dashboard_live_test_meter`**.
    3. Insert via **`TestRepo.insert!`**, then transition to failed via **`MeterEvent.failed_changeset(row, %Accrue.APIError{code: \"test\", http_status: 400, message: \"x\"})`** + **`TestRepo.update!`** (or equivalent single insert with **`put_change(:stripe_status, \"failed\")`** if changeset pipeline is simpler — final DB state must be **`failed`**).
    Extend **`test \"renders KPI cards\"`** (or add a focused test) to assert the new **`Copy`** strings and **`/billing/events`** href as above. Assert the rendered **integer** for the KPI equals the **non-zero** failure count when one failed row exists.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs'</automated>
  </verify>
  <done>ADM-01 covered by LiveView test.</done>
</task>

<task type="auto">
  <name>Task 5 (conditional): ax-kpi-grid breakpoints for five cards</name>
  <read_first>
    accrue_admin/assets/css/app.css
    .planning/phases/48-admin-metering-billing-signals/48-UI-SPEC.md
  </read_first>
  <files>accrue_admin/assets/css/app.css</files>
  <acceptance_criteria>
    - If **skipped**: **`git diff --name-only`** for this commit does not include **`app.css`** — document skip in SUMMARY as **“5-tile grid acceptable without CSS change”**.
    - If **executed**: only rules under selectors matching **`\\.ax-kpi-grid`** are modified ( **`rg -n 'ax-kpi-grid' accrue_admin/assets/css/app.css`** shows changed lines inside those blocks); **no** new layout component classes.
    - `cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs` still exits **0**.
  </acceptance_criteria>
  <action>
    After Tasks **1–4** pass, visually smoke-test five KPI tiles at **768px** and **1280px** width. If the grid overflows or wraps worse than before, adjust **`grid-template-columns`** / **`gap`** **only** inside existing **`.ax-kpi-grid`** media-query blocks in **`accrue_admin/assets/css/app.css`**. If layout is acceptable, **skip** this task with no **`app.css`** diff.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs'</automated>
  </verify>
  <done>Responsive grid acceptable for five KPIs.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors`
- `cd accrue_admin && mix test test/accrue_admin/live/dashboard_live_test.exs`
- `cd accrue_admin && mix test` (full package suite before phase close)
</verification>

<success_criteria>
- **ADM-01** met: operator sees a **metering-credible** failed **`MeterEvent`** count on admin home with **honest** **`/events`** navigation and **`AccrueAdmin.Copy`** SSOT.
- **48-CONTEXT** D-01..D-14 respected: no **`pending`** panic KPI, no fake **`/webhooks`** link for this aggregate, no VERIFY-01 policy change, no new UI kits.
</success_criteria>

---
phase: 49-drill-flows-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
  - accrue_admin/lib/accrue_admin/copy.ex
autonomous: true
requirements: [ADM-02]
must_haves:
  truths:
    - "SubscriptionLive breadcrumbs match InvoiceLive parity: Dashboard → Subscriptions → linked Customer → terminal subscription label"
    - "Every new breadcrumb and Related href uses ScopedPath.build/3 or /4 — no subscription_id= query on invoices or charges"
    - "Related billing card has at most five links; new operator strings live only under AccrueAdmin.Copy prefix subscription_drill_"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      provides: "ADM-02 drill continuity on subscription detail"
      contains: "ScopedPath"
    - path: "accrue_admin/lib/accrue_admin/copy.ex"
      provides: "SSOT copy for related region + link labels"
      contains: "subscription_drill_"
  key_links:
    - from: "Subscription detail"
      to: "Customer detail"
      via: "breadcrumb + related card"
      pattern: "ScopedPath customer route"
    - from: "Subscription detail"
      to: "Invoices index filtered by customer_id"
      via: "ScopedPath + query map"
      pattern: "honest AccrueAdmin.Queries.Invoices filter"
---

<threat_model>
**ASVS L1.** Links only reuse **already-loaded** `subscription.customer` — no new logging of processor tokens in copy. **`customer_id`** query params are UUIDs already visible in admin URLs elsewhere; do not add **subscription processor_id** into query strings. Step-up / destructive flows unchanged — do not weaken **`StepUp`** gates.
</threat_model>

<objective>
Ship **ADM-02** on **`SubscriptionLive`**: **InvoiceLive-parity** breadcrumbs (**customer** segment + **`ScopedPath`**) and one **≤5-link** **“Related billing”** **`ax-card`** using **only** query shapes supported by **`AccrueAdmin.Queries.Invoices`** / **`Charges`** (**`customer_id`**), plus **Copy** SSOT per **049-CONTEXT** and **049-UI-SPEC**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-drill-flows-navigation/049-CONTEXT.md
@.planning/phases/49-drill-flows-navigation/049-UI-SPEC.md
@.planning/phases/49-drill-flows-navigation/049-RESEARCH.md
@.planning/phases/49-drill-flows-navigation/049-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Copy helpers for subscription drill</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex
    .planning/phases/49-drill-flows-navigation/049-UI-SPEC.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex</files>
  <acceptance_criteria>
    - `rg -n 'def subscription_drill_related_card_title' accrue_admin/lib/accrue_admin/copy.ex` returns ≥ **1** line.
    - `rg -n 'def subscription_drill_related_region_aria_label' accrue_admin/lib/accrue_admin/copy.ex` returns ≥ **1** line.
    - `rg -n 'def subscription_drill_link_customer' accrue_admin/lib/accrue_admin/copy.ex` returns ≥ **1** line.
    - `rg -n 'def subscription_drill_link_invoices_for_customer' accrue_admin/lib/accrue_admin/copy.ex` returns ≥ **1** line.
    - `rg -n 'def subscription_drill_link_charges_for_customer' accrue_admin/lib/accrue_admin/copy.ex` returns ≥ **1** line.
    - `rg -n 'def subscription_drill_link_events_index' accrue_admin/lib/accrue_admin/copy.ex` returns ≥ **1** line.
    - `rg -n 'subscription_drill_' accrue_admin/lib/accrue_admin/copy.ex | wc -l` is **≥ 6** (at least the six functions above).
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    In **`accrue_admin/lib/accrue_admin/copy.ex`**, add **`subscription_drill_*`** functions only (no other prefix):
    - **`subscription_drill_related_card_title/0`** — short region title (**“Related billing”** or equivalent per **049-UI-SPEC**).
    - **`subscription_drill_related_region_aria_label/0`** — `aria-label` for the **`nav`** or **`section`** wrapping the link list.
    - **`subscription_drill_link_customer/0`**, **`subscription_drill_link_invoices_for_customer/0`**, **`subscription_drill_link_charges_for_customer/0`**, **`subscription_drill_link_events_index/0`** — anchor text for each link; **must not** claim subscription-scoped filtering for invoices/charges/events beyond **`customer_id`** honesty rules in **049-RESEARCH.md**.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Copy SSOT for drill region added.</done>
</task>

<task type="auto">
  <name>Task 2: Breadcrumbs — ScopedPath + customer segment</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/subscription_live.ex
    accrue_admin/lib/accrue_admin/live/invoice_live.ex
    accrue_admin/lib/accrue_admin/scoped_path.ex
    .planning/phases/49-drill-flows-navigation/049-PATTERNS.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex</files>
  <acceptance_criteria>
    - `rg -n 'alias AccrueAdmin.ScopedPath' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 1** line.
    - `rg -n 'ScopedPath.build\\(@admin_mount_path, \"/customers/' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 1** line.
    - `rg -n 'ScopedPath.build\\(@admin_mount_path' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 6** lines after implementation (three breadcrumb **`href`**s + three related **`href`**s minimum).
    - `rg -n 'label: \"Subscriptions\"' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 1** line inside the **`Breadcrumbs.breadcrumbs`** `items=` list (same function’s **`render/1`** HEEx).
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    In **`subscription_live.ex`**:
    1. Add **`alias AccrueAdmin.ScopedPath`** next to other **`AccrueAdmin.*`** aliases.
    2. Add **`defp customer_label(%Accrue.Billing.Customer{} = customer)`** — copy the **body** from **`invoice_live.ex` `customer_label/1`** (name → email → id fallback) verbatim or factor-safe duplicate per **049-PATTERNS.md**.
    3. Replace breadcrumb **`items`** with: **Dashboard** `ScopedPath.build(@admin_mount_path, "", @current_owner_scope)`; **Subscriptions** `ScopedPath.build(@admin_mount_path, "/subscriptions", @current_owner_scope)`; **Customer** `%{label: customer_label(@customer), href: ScopedPath.build(@admin_mount_path, "/customers/#{@customer.id}", @current_owner_scope)}`; terminal **`%{label: ...}`** without **`href`** using existing subscription label (`processor_id` or `id`).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Breadcrumb parity implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Related billing ax-card (≤5 links)</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/subscription_live.ex
    accrue_admin/lib/accrue_admin/copy.ex
    .planning/phases/49-drill-flows-navigation/049-RESEARCH.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex</files>
  <acceptance_criteria>
    - `rg -n 'subscription_drill_' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 4** lines (title + ≥3 `Copy.` usages in HEEx).
    - `rg -n 'customer_id' accrue_admin/lib/accrue_admin/live/subscription_live.ex` shows **`%{\"customer_id\"` or `%{'customer_id'`** inside **`ScopedPath.build`** for **both** invoices and charges links (exact key **`customer_id`**).
    - `rg -n 'subscription_id=' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **0** matches.
    - `rg -n 'Related billing|subscription_drill_related' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 1** line (region visibility).
    - `rg -n 'aria-label=\\{Copy\\.subscription_drill_related_region_aria_label\\(\\)\\}' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **≥ 1** line.
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    After **`ax-kpi-grid`** (and before **`TaxOwnershipCard`**), insert **`article.ax-card`** containing:
    - Header: **`Copy.subscription_drill_related_card_title()`** via **`ax-eyebrow`/`ax-heading`** pattern consistent with neighboring cards.
    - **`nav`** (or **`section`**) with **`aria-label={Copy.subscription_drill_related_region_aria_label()}`** and an **unordered list** of **≤5** **`<.link navigate={...}>`** or **`<a href=...>`** entries:
      1. **Customer** → `ScopedPath.build(@admin_mount_path, "/customers/#{@customer.id}", @current_owner_scope)`
      2. **Invoices** → `ScopedPath.build(@admin_mount_path, "/invoices", @current_owner_scope, %{"customer_id" => @customer.id})`
      3. **Charges** → `ScopedPath.build(@admin_mount_path, "/charges", @current_owner_scope, %{"customer_id" => @customer.id})`
      4. **Events** → `ScopedPath.build(@admin_mount_path, "/events", @current_owner_scope)` (**no** fake filters)
    - Link text from **`Copy.subscription_drill_link_*`** functions only.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Related billing region shipped.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors` — **0** exit.
- `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs` — **green** after **`049-02-PLAN.md`** extends tests (runner may execute plans in order; if this plan runs alone first, compile-only gate above still applies).
</verification>

<success_criteria>
- **ADM-02** drill continuity demonstrable on **`SubscriptionLive`** HTML (breadcrumbs + related links).
- **D-07** honesty: **no** `subscription_id=` query invented on list targets.
</success_criteria>

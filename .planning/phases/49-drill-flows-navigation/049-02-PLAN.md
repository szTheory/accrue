---
phase: 49-drill-flows-navigation
plan: "02"
type: execute
wave: 2
depends_on: ["049-01-PLAN.md"]
files_modified:
  - accrue_admin/test/accrue_admin/live/subscription_live_test.exs
  - examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
  - accrue_admin/README.md
autonomous: true
requirements: [ADM-02, ADM-03]
must_haves:
  truths:
    - "subscription_live_test asserts breadcrumb hrefs include /customers/ and org query when org session is used"
    - "Host admin_mount_test performs live/2 against /billing/subscriptions/:id for a Factory subscription visible to billing admin session"
    - "README either unchanged with explicit grep proof OR gains one clarifying sentence about sidebar vs router ordering per D-09"
  artifacts:
    - path: "accrue_admin/test/accrue_admin/live/subscription_live_test.exs"
      provides: "LiveViewTest proof for drill hrefs"
      contains: "customers"
    - path: "examples/accrue_host/test/accrue_host_web/admin_mount_test.exs"
      provides: "Mounted-router wiring regression guard"
      contains: "subscriptions"
  key_links:
    - from: "Host integration test"
      to: "SubscriptionLive"
      via: "/billing/subscriptions/:id"
      pattern: "mounted admin parity"
---

<threat_model>
**ASVS L1.** Tests use **`Accrue.Test.Factory`** with synthetic emails (`*.example.com` / factory pattern) — no live Stripe identifiers. Host test reuses existing **billing admin** session pattern — do not broaden **`admin_organization_ids`** beyond fixture needs.
</threat_model>

<objective>
Prove **ADM-02** with **`Phoenix.LiveViewTest`** + **one** **`examples/accrue_host`** **`live/2`** drill, and close **ADM-03** documentation drift risk with a **minimal** **`accrue_admin/README.md`** update **only** if required for contributor clarity.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-drill-flows-navigation/049-CONTEXT.md
@.planning/phases/49-drill-flows-navigation/049-VALIDATION.md
@.planning/phases/49-drill-flows-navigation/049-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveViewTest — breadcrumb + related hrefs</name>
  <read_first>
    accrue_admin/test/accrue_admin/live/subscription_live_test.exs
    accrue_admin/lib/accrue_admin/live/subscription_live.ex
    accrue_admin/test/support/live_case.ex
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/subscription_live_test.exs</files>
  <acceptance_criteria>
    - File contains **`assert html =~`** or **`has_element?`** checks for **`/customers/`** substring tied to **`subscription.customer.id`** (use **`subscription.id`** / **`customer.id`** interpolation — grep **`/customers/\#\{`** or equivalent **≥ 1**).
    - `rg -n 'subscription_drill_' accrue_admin/test/accrue_admin/live/subscription_live_test.exs` returns **≥ 1** line OR **`Related`** substring assertion exists (**`rg -n 'Related|subscription_drill' accrue_admin/test/accrue_admin/live/subscription_live_test.exs` ≥ 1**).
    - `rg -n 'customer_id' accrue_admin/test/accrue_admin/live/subscription_live_test.exs` returns **≥ 1** line asserting **`/invoices`** or query contains **`customer_id`**.
    - `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs` exits **0**.
  </acceptance_criteria>
  <action>
    Extend **`subscription_live_test.exs`**:
    1. **`live/2`** + **`render/1`** (existing pattern) after setup subscription.
    2. Assert rendered HTML includes **customer** breadcrumb **`href`** containing **`/customers/<id>`** (use **`subscription.customer_id`** from setup).
    3. Assert **invoices** related link includes **`customer_id=`** query for that customer (string contains **`customer_id=`** and customer id).
    4. Assert **charges** related link includes **`customer_id=`** (same).
    5. Optionally assert **events** link is **`/events`** without **`subject_id`**. 
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs'</automated>
  </verify>
  <done>Admin-package LiveView tests cover drill hrefs.</done>
</task>

<task type="auto">
  <name>Task 2: Host mounted-router smoke — subscription detail</name>
  <read_first>
    examples/accrue_host/test/accrue_host_web/admin_mount_test.exs
    accrue/lib/accrue/test/factory.ex
    examples/accrue_host/config/test.exs
  </read_first>
  <files>examples/accrue_host/test/accrue_host_web/admin_mount_test.exs</files>
  <acceptance_criteria>
    - New **`test`** block contains **`live(conn, \"/billing/subscriptions/`** (grep returns **≥ 1**).
    - Same test **`assert`** html contains subscription **`processor_id`** **or** stable substring from **`Copy.subscription_drill_*`** **or** customer email used in factory attrs (**≥ 1** visibility assertion).
    - `cd examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs` exits **0**.
  </acceptance_criteria>
  <action>
    Add **one** test to **`admin_mount_test.exs`** mirroring the **billing admin + org session** setup from **`signed-in billing admins can mount /billing`**, then:
    1. Insert **`%{customer: customer, subscription: subscription} = Accrue.Test.Factory.active_subscription(%{owner_type: \"Organization\", owner_id: organization.id, email: \"phase49-drill@example.com\"})`** (use **`organization`** from **`organization_fixture`** tied to billing admin user — same pattern as existing test for org ids).
    2. `live(conn, "/billing/subscriptions/#{subscription.id}")` → **`{:ok, view, html}`**.
    3. Assert **`html`** includes **`customer.id`** in a **`/customers/`** path **or** includes the related-card copy key rendered from **`Copy`** (choose one deterministic substring).
  </action>
  <verify>
    <automated>bash -lc 'cd examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs'</automated>
  </verify>
  <done>Host-mounted subscription drill covered.</done>
</task>

<task type="auto">
  <name>Task 3: README — router vs sidebar clarity (ADM-03)</name>
  <read_first>
    accrue_admin/README.md
    .planning/phases/49-drill-flows-navigation/049-CONTEXT.md
  </read_first>
  <files>accrue_admin/README.md</files>
  <acceptance_criteria>
    - **Either** `rg -n 'sidebar|Sidebar|navigation order|router order' accrue_admin/README.md` returns **≥ 1** match after edit **OR** file **`README.md`** in diff has **zero** lines changed — if zero lines changed, this task’s **`<done>`** must still be reachable by documenting in **`049-02-SUMMARY.md`** later that README was verified unchanged (**executor**: add a **HTML comment**-equivalent is N/A for markdown — prefer **one** sentence added).
    - If a sentence is added: it must include both substrings **`router`** and **`sidebar`** (case-insensitive) per **D-09** clarity goal — `rg -in 'router' accrue_admin/README.md` and `rg -in 'sidebar' accrue_admin/README.md` both **≥ 1** after edit.
    - `rg -n 'Admin routes' accrue_admin/README.md` still returns **≥ 1** (section not removed).
  </acceptance_criteria>
  <action>
    Open **`accrue_admin/README.md`** **Admin routes** / navigation section:
    - If **no** contributor confusion risk: add **one** sentence explicitly stating **sidebar order is curated** and **README lists router declaration order** — both words **router** and **sidebar** must appear.
    - If that sentence already exists from prior work: **skip** edit and mark task done with **no file change** (ensure acceptance path: at least one **`rg`** hit already satisfies second bullet).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "Admin routes" accrue_admin/README.md'</automated>
  </verify>
  <done>ADM-03 documentation posture addressed.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs` — **0** exit.
- `cd examples/accrue_host && mix test test/accrue_host_web/admin_mount_test.exs` — **0** exit.
</verification>

<success_criteria>
- **ADM-02** has **automated** href-level proof at **library** and **host** layers.
- **ADM-03** README/router contributor story is **explicit** or verified **pre-existing**.
</success_criteria>

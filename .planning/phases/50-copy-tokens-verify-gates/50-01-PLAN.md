---
phase: 50-copy-tokens-verify-gates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/guides/theme-exceptions.md
  - examples/accrue_host/docs/verify01-v112-admin-paths.md
  - CONTRIBUTING.md
  - .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md
autonomous: true
requirements: [ADM-05, ADM-06]
must_haves:
  truths:
    - "theme-exceptions.md exists with table columns: slug, location, deviation, rationale, future_token, status, phase_ref"
    - "verify01-v112-admin-paths.md lists at least dashboard entry, customers index, subscriptions index, subscription detail paths"
    - "CONTRIBUTING.md contains explicit PR bullet for theme exception register updates"
  artifacts:
    - path: "accrue_admin/guides/theme-exceptions.md"
      provides: "ADM-05 SSOT register"
      contains: "slug"
    - path: "examples/accrue_host/docs/verify01-v112-admin-paths.md"
      provides: "ADM-06 inventory seed"
      contains: "/billing/"
  key_links:
    - from: "50-VERIFICATION.md"
      to: "theme-exceptions + inventory"
      via: "markdown links"
      pattern: "gate doc"
---

<threat_model>
**ASVS L1.** This plan adds **documentation only** — no new runtime endpoints. Do **not** paste live Stripe IDs, customer PII, or webhook secrets into **`theme-exceptions.md`** or path inventory; use **placeholders** or **route templates** only (`/billing/customers`, `:id` patterns).
</threat_model>

<objective>
Land **ADM-05** scaffolding (**theme exception register** + **PR checklist**) and **ADM-06** scaffolding (**checked-in mounted-path inventory** + **`50-VERIFICATION.md`** links) so later plans can assert completeness against stable files (**50-CONTEXT D-10–D-15, D-11**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-copy-tokens-verify-gates/50-CONTEXT.md
@.planning/phases/50-copy-tokens-verify-gates/50-UI-SPEC.md
@.planning/phases/50-copy-tokens-verify-gates/50-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme exception register</name>
  <read_first>
    .planning/phases/50-copy-tokens-verify-gates/50-CONTEXT.md
    accrue_admin/README.md
  </read_first>
  <files>accrue_admin/guides/theme-exceptions.md</files>
  <acceptance_criteria>
    - `test -f accrue_admin/guides/theme-exceptions.md` exits **0**.
    - `rg -n '^## ' accrue_admin/guides/theme-exceptions.md` includes a line matching `## Register` OR `## Theme exceptions` (≥ **1** match).
    - `rg -n '\\| slug \\|' accrue_admin/guides/theme-exceptions.md` returns **≥ 1** line (header row contains **slug**).
    - `rg -n 'location' accrue_admin/guides/theme-exceptions.md` returns **≥ 1** line in the table header area.
  </acceptance_criteria>
  <action>
    Create **`accrue_admin/guides/theme-exceptions.md`** with:
    1. Title **`# Accrue Admin — theme & layout exceptions`** and short intro referencing **UX-04** intent and **Phase 50**.
    2. A **Markdown table** with columns exactly: **`slug`**, **`location`**, **`deviation`**, **`rationale`**, **`future_token`**, **`status`**, **`phase_ref`**.
    3. Include **≥ 1** example row (can be **“none yet”** placeholder row with **`status: clear`** if no real exceptions) — table must not be empty.
  </action>
  <verify>
    <automated>bash -lc 'test -f accrue_admin/guides/theme-exceptions.md && rg -n "\\| slug \\|" accrue_admin/guides/theme-exceptions.md'</automated>
  </verify>
  <done>Exception register file exists.</done>
</task>

<task type="auto">
  <name>Task 2: ADM-06 path inventory (v1.12 union seed)</name>
  <read_first>
    .planning/phases/48-admin-metering-billing-signals/48-CONTEXT.md
    .planning/phases/49-drill-flows-navigation/049-CONTEXT.md
    examples/accrue_host/README.md
  </read_first>
  <files>examples/accrue_host/docs/verify01-v112-admin-paths.md</files>
  <acceptance_criteria>
    - `test -f examples/accrue_host/docs/verify01-v112-admin-paths.md` exits **0**.
    - `rg -n '/billing/customers' examples/accrue_host/docs/verify01-v112-admin-paths.md` returns **≥ 1** line.
    - `rg -n '/billing/subscriptions' examples/accrue_host/docs/verify01-v112-admin-paths.md` returns **≥ 1** line.
    - `rg -n 'Dashboard|dashboard' examples/accrue_host/docs/verify01-v112-admin-paths.md` returns **≥ 1** line (admin **home** / entry path wording).
    - File contains a **bulleted or numbered** list of **≥ 4** distinct path patterns or URLs.
  </acceptance_criteria>
  <action>
    Create **`examples/accrue_host/docs/verify01-v112-admin-paths.md`** documenting the **union** of v1.12 materially touched **mounted** admin URLs (templates welcome): include **customers index**, **subscriptions index**, **subscription detail** (`/billing/subscriptions/:id`), and **dashboard home** path used after **“Go to billing”** per **48/49** artifacts. Use **`org=`** query convention notes if org-scoped routes require it.
  </action>
  <verify>
    <automated>bash -lc 'test -f examples/accrue_host/docs/verify01-v112-admin-paths.md && test "$(rg -c /billing/ examples/accrue_host/docs/verify01-v112-admin-paths.md)" -ge 3'</automated>
  </verify>
  <done>Inventory seed documented.</done>
</task>

<task type="auto">
  <name>Task 3: CONTRIBUTING + 50-VERIFICATION cross-links</name>
  <read_first>
    CONTRIBUTING.md
    .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md
  </read_first>
  <files>CONTRIBUTING.md</files>
  <acceptance_criteria>
    - `rg -n 'theme-exceptions\\.md' CONTRIBUTING.md` returns **≥ 1** line.
    - `rg -n 'verify01-v112-admin-paths' .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md` returns **≥ 1** line OR **`examples/accrue_host/docs/verify01-v112-admin-paths.md`** appears in **`50-VERIFICATION.md`** via `rg -n 'verify01-v112' .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md` **≥ 1**.
    - `rg -n 'PR checklist|pull request|theme exception' CONTRIBUTING.md` returns **≥ 1** line (case-insensitive ok).
  </acceptance_criteria>
  <action>
    1. Append a **short subsection** to root **`CONTRIBUTING.md`** (e.g. **`## Accrue Admin UI hygiene (v1.12+)`**) with a bullet: if a PR introduces a **non-token** color/layout exception in **`accrue_admin`**, add a row to **`accrue_admin/guides/theme-exceptions.md`** (**D-13**).
    2. Update **`.planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md`** so the **Canonical artifacts** table or body references **`examples/accrue_host/docs/verify01-v112-admin-paths.md`** by **repo-relative** path.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "theme-exceptions" CONTRIBUTING.md'</automated>
  </verify>
  <done>Contributor gate documented.</done>
</task>

</tasks>

<verification>
- `rg -n 'theme-exceptions' CONTRIBUTING.md` — **≥ 1** match.
- `test -f accrue_admin/guides/theme-exceptions.md` — **0** exit.
- `test -f examples/accrue_host/docs/verify01-v112-admin-paths.md` — **0** exit.
</verification>

<success_criteria>
- **ADM-05** register file exists and is discoverable under **`accrue_admin/guides/`**.
- **ADM-06** inventory file exists and lists the **v1.12** mounted surfaces explicitly (**D-15**).
</success_criteria>

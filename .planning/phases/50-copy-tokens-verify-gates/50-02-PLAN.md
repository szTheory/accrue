---
phase: 50-copy-tokens-verify-gates
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/copy/subscription.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/live/subscription_live.ex
autonomous: true
requirements: [ADM-04]
must_haves:
  truths:
    - "SubscriptionLive breadcrumb items use Copy.dashboard_breadcrumb_home/0 and new Copy subscription breadcrumb helpers — no raw label: \"Dashboard\" / \"Subscriptions\""
    - "AccrueAdmin.Copy.Subscription module exists; copy.ex defdelegates the new subscription surface helpers"
    - "@proration_options labels are sourced from AccrueAdmin.Copy functions (no inline English in module attribute for those three labels)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy/subscription.ex"
      provides: "Subscription detail copy SSOT"
      contains: "def subscription_"
    - path: "accrue_admin/lib/accrue_admin/live/subscription_live.ex"
      provides: "ADM-04 enforcement on primary v1.12 drill surface"
      contains: "Copy."
  key_links:
    - from: "Copy.Subscription"
      to: "SubscriptionLive HEEx"
      via: "Copy.subscription_*()"
      pattern: "defdelegate"
---

<threat_model>
**ASVS L1.** Copy strings must **not** embed **live** `processor_id`, **card** PANs, or **webhook signing** material. Keep **step-up** / **`StepUp`** flows unchanged — only **label** / **helper text** moves. **`Copy.Locked`** stays reserved for verbatim legal-ish text (**50-CONTEXT D-06**) — do **not** route normal labels through **`Locked`**.
</threat_model>

<objective>
Ship **ADM-04** on the **highest-churn v1.12 surface** (`**SubscriptionLive**`): introduce **`AccrueAdmin.Copy.Subscription`** behind **`defdelegate`**, migrate **breadcrumb**, **KPI chrome**, **proration option labels**, and **primary action** button text to **`AccrueAdmin.Copy`**, preserving **`Copy.subscription_drill_*`** already on **`Copy`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-copy-tokens-verify-gates/50-CONTEXT.md
@.planning/phases/50-copy-tokens-verify-gates/50-UI-SPEC.md
@.planning/phases/50-copy-tokens-verify-gates/50-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AccrueAdmin.Copy.Subscription</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex
    accrue_admin/lib/accrue_admin/live/subscription_live.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy/subscription.ex</files>
  <acceptance_criteria>
    - `test -f accrue_admin/lib/accrue_admin/copy/subscription.ex` exits **0**.
    - `rg -n 'def subscription_breadcrumb_subscriptions' accrue_admin/lib/accrue_admin/copy/subscription.ex` returns **≥ 1** line.
    - `rg -n 'def subscription_detail_eyebrow' accrue_admin/lib/accrue_admin/copy/subscription.ex` returns **≥ 1** line.
    - `rg -n 'def subscription_kpi_section_aria_label' accrue_admin/lib/accrue_admin/copy/subscription.ex` returns **≥ 1** line.
    - `rg -n 'def subscription_proration_' accrue_admin/lib/accrue_admin/copy/subscription.ex | wc -l` prints **≥ 3** (one per proration option label).
    - `rg -c '^  def subscription_' accrue_admin/lib/accrue_admin/copy/subscription.ex` prints **≥ 8**.
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    Add **`accrue_admin/lib/accrue_admin/copy/subscription.ex`** defining **`AccrueAdmin.Copy.Subscription`** with **public** `def` functions (snake_case) returning English operator strings for **at minimum**:
    - **`subscription_breadcrumb_subscriptions/0`** → **`"Subscriptions"`** (exact string).
    - **`subscription_detail_eyebrow/0`** → replaces HEEx **`Subscription detail`** eyebrow.
    - **`subscription_kpi_section_aria_label/0`** → replaces **`aria-label="Subscription lifecycle summary"`**.
    - **`subscription_proration_create/0`**, **`subscription_proration_none/0`**, **`subscription_proration_always_invoice/0`** → strings currently **`Create prorations`**, **`No proration`**, **`Always invoice`**.
    - Add **`subscription_*` helpers** for KPI labels (**`Status`**, **`Canonical predicates`**, **`Timeline rows`**) and the **primary destructive / resume** button labels (**`Cancel now`**, **`Cancel at period end`**, **`Resume`**, **`Swap plan`**, **`Pause collection`**, **`Create comp replacement`**) so **`rg -c 'def subscription_' accrue_admin/lib/accrue_admin/copy/subscription.ex`** prints **≥ 8**.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Copy.Subscription module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: defdelegate from AccrueAdmin.Copy</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex
    accrue_admin/lib/accrue_admin/copy/subscription.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex</files>
  <acceptance_criteria>
    - `rg -n 'defdelegate' accrue_admin/lib/accrue_admin/copy.ex` returns **≥ 3** lines targeting **`AccrueAdmin.Copy.Subscription`**.
    - `rg -n 'alias AccrueAdmin.Copy.Subscription' accrue_admin/lib/accrue_admin/copy.ex` returns **≥ 1** line OR **`Copy.Subscription`** reference in **`defdelegate`** uses full module name.
    - `cd accrue_admin && mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <action>
    In **`accrue_admin/lib/accrue_admin/copy.ex`**, **`alias AccrueAdmin.Copy.Subscription`** and add **`defdelegate`** entries for **every** public function added in **`Copy.Subscription`** so call sites continue **`AccrueAdmin.Copy.*`** only.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Delegates wired.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate SubscriptionLive literals to Copy</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/subscription_live.ex
    accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/subscription_live.ex</files>
  <acceptance_criteria>
    - `rg -n 'label: \"Dashboard\"' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **0** lines.
    - `rg -n 'label: \"Subscriptions\"' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **0** lines.
    - `rg -n 'Subscription lifecycle summary' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **0** lines.
    - `rg -n 'Create prorations|No proration|Always invoice' accrue_admin/lib/accrue_admin/live/subscription_live.ex` returns **0** lines.
    - `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs` exits **0**.
  </acceptance_criteria>
  <action>
    1. Replace breadcrumb **`items`** labels: first item uses **`Copy.dashboard_breadcrumb_home/0`**; second uses **`Copy.subscription_breadcrumb_subscriptions/0`** (via delegate).
    2. Replace **`aria-label="Subscription lifecycle summary"`** with **`Copy.subscription_kpi_section_aria_label/0()`**.
    3. Remove the **`@proration_options`** module attribute **if** it embeds English — assign **`proration_options`** in **`mount/3`** (or a **`defp`**) using **`Copy`** for **`label`** text only; keep **`value`** strings **`create_prorations`**, **`none`**, **`always_invoice`** unchanged (**Stripe-ish codes**, not translatable copy).
    4. Migrate **remaining obvious** operator literals in the **`render/1`** HEEx and **`assign_shell`** (**`page_title`**) to **`Copy`** helpers added in Task 1 where not yet covered.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs'</automated>
  </verify>
  <done>ADM-04 migration tranche complete.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix compile --warnings-as-errors` — **0** exit.
- `cd accrue_admin && mix test test/accrue_admin/live/subscription_live_test.exs` — **0** exit.
</verification>

<success_criteria>
- **ADM-04** materially improved on **`SubscriptionLive`** with **scalable** `**Copy.Subscription**` module.
- **No** regression in **subscription drill** tests.
</success_criteria>

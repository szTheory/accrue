---
phase: 50-copy-tokens-verify-gates
plan: "03"
type: execute
wave: 2
depends_on: ["50-01-PLAN.md", "50-02-PLAN.md"]
files_modified:
  - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
  - scripts/ci/accrue_host_verify_browser.sh
  - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  - examples/accrue_host/README.md
  - .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md
autonomous: true
requirements: [ADM-04, ADM-06]
must_haves:
  truths:
    - "mix accrue_admin.export_copy_strings writes JSON consumed by VERIFY-01 specs"
    - "accrue_host_verify_browser.sh runs the export before npm e2e"
    - "At least one Playwright assertion uses imported copy JSON (not a hand-duplicated English literal for that key)"
  artifacts:
    - path: "accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex"
      provides: "D-23 anti-drift generator"
      contains: "def run"
    - path: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      provides: "ADM-06 + D-23 binding"
      contains: "copy_strings"
  key_links:
    - from: "Elixir Copy"
      to: "Playwright"
      via: "generated JSON"
      pattern: "export task"
---

<threat_model>
**ASVS L1.** The export allowlist must include **only** operator-safe strings already public on **`AccrueAdmin.Copy`** — **never** export env-derived secrets, **`Copy.Locked`** legal blocks unless explicitly reviewed, or **host-specific** credentials. **`ACCRUE_HOST_ALLOW_GENERATED_DRIFT`** (already in **`accrue_host_verify_browser.sh`** ecosystem) must **not** be repurposed to skip JSON generation — add a **separate** flag only if truly needed, documented in **`examples/accrue_host/README.md`**.
</threat_model>

<objective>
Close **D-23** with a **`mix accrue_admin.export_copy_strings`** pipeline wired into **`scripts/ci/accrue_host_verify_browser.sh`**, extend **VERIFY-01** specs to cover **≥ 1** additional inventory path from **`verify01-v112-admin-paths.md`** using **Copy-derived** strings, and record the **anti-drift** gate in **`50-VERIFICATION.md`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-copy-tokens-verify-gates/50-CONTEXT.md
@.planning/phases/50-copy-tokens-verify-gates/50-UI-SPEC.md
@.planning/phases/50-copy-tokens-verify-gates/50-RESEARCH.md
@examples/accrue_host/docs/verify01-v112-admin-paths.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mix task export_copy_strings</name>
  <read_first>
    accrue_admin/lib/mix/tasks/accrue_admin.assets.build.ex
    accrue_admin/lib/accrue_admin/copy.ex
    accrue_admin/lib/accrue_admin/copy/subscription.ex
  </read_first>
  <files>accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex</files>
  <acceptance_criteria>
    - `test -f accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex` exits **0**.
    - `cd accrue_admin && mix help accrue_admin.export_copy_strings` shows **≥ 3** lines of help text (task registered).
    - `cd accrue_admin && mix accrue_admin.export_copy_strings --out /tmp/accrue-copy-test.json` exits **0**.
    - `jq -e '.subscription_drill_link_customer' /tmp/accrue-copy-test.json` exits **0** (key present — exact key name **`subscription_drill_link_customer`** as snake_case atom string in JSON).
    - `rm -f /tmp/accrue-copy-test.json` — optional cleanup.
  </acceptance_criteria>
  <action>
    Implement **`Mix.Tasks.AccrueAdmin.ExportCopyStrings`** (`mix accrue_admin.export_copy_strings`):
    - **`--out PATH`** (required): write **UTF-8 JSON** map **`%{ "function_name" => "returned string" }`** using **0-arity** **`AccrueAdmin.Copy`** / delegated functions only.
    - **Allowlist** inside the task module (explicit list of atoms) including **at minimum** every **`subscription_drill_*`** function on **`Copy`** and **≥ 3** **`subscription_*`** functions added in **`50-02`** (e.g. breadcrumb / proration labels) — **no** `Code.all_loaded` blind sweep.
    - JSON keys must be **string** form of function names (**`subscription_drill_link_customer`**, not **`Elixir.AccrueAdmin.Copy.subscription_drill_link_customer/0`**).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix accrue_admin.export_copy_strings --out /tmp/accrue-copy-test.json && jq -e . /tmp/accrue-copy-test.json'</automated>
  </verify>
  <done>Export task ships.</done>
</task>

<task type="auto">
  <name>Task 2: CI + README wiring</name>
  <read_first>
    scripts/ci/accrue_host_verify_browser.sh
    examples/accrue_host/README.md
  </read_first>
  <files>scripts/ci/accrue_host_verify_browser.sh</files>
  <acceptance_criteria>
    - `rg -n 'export_copy_strings' scripts/ci/accrue_host_verify_browser.sh` returns **≥ 1** line.
    - `rg -n 'export_copy_strings|copy_strings\\.json' examples/accrue_host/README.md` returns **≥ 1** line.
    - Shell snippet runs **`mix accrue_admin.export_copy_strings`** with **`cd "$repo_root/accrue_admin"`** (or equivalent) **before** **`npm run e2e`** in the same script file.
  </acceptance_criteria>
  <action>
    1. After the existing **`mix accrue_admin.assets.build`** subshell in **`scripts/ci/accrue_host_verify_browser.sh`**, invoke **`mix accrue_admin.export_copy_strings --out "$repo_root/examples/accrue_host/e2e/generated/copy_strings.json"`** from **`accrue_admin`** cwd (create **`e2e/generated/`** if missing — `mkdir -p`).
    2. Document in **`examples/accrue_host/README.md`** under VERIFY-01 / e2e section: contributors must run **`mix accrue_admin.export_copy_strings --out e2e/generated/copy_strings.json`** from **`accrue_admin/`** when changing **`Copy`** allowlist keys, and CI regenerates each run.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "export_copy_strings" scripts/ci/accrue_host_verify_browser.sh'</automated>
  </verify>
  <done>Pipeline wired.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright uses generated JSON + extra inventory path</name>
  <read_first>
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    examples/accrue_host/e2e/support/fixture.js
    examples/accrue_host/docs/verify01-v112-admin-paths.md
  </read_first>
  <files>examples/accrue_host/e2e/verify01-admin-a11y.spec.js</files>
  <acceptance_criteria>
    - `rg -n 'copy_strings\\.json|generated/copy_strings' examples/accrue_host/e2e/verify01-admin-a11y.spec.js` returns **≥ 1** line.
    - `rg -n 'require\\(.*copy_strings' examples/accrue_host/e2e/verify01-admin-a11y.spec.js` returns **≥ 1** line **OR** `readFileSync` / `fs.readFileSync` loads **`e2e/generated/copy_strings.json`**.
    - `rg -n 'test\\(' examples/accrue_host/e2e/verify01-admin-a11y.spec.js | wc -l` is **≥ 2** (at least **one** new **`test(`** block beyond the existing customers test — count may be higher).
    - `rg -n 'subscriptions' examples/accrue_host/e2e/verify01-admin-a11y.spec.js` returns **≥ 1** line referencing **`/billing/subscriptions`** navigation or URL.
  </acceptance_criteria>
  <action>
    1. At top of **`verify01-admin-a11y.spec.js`**, load **`./generated/copy_strings.json`** into **`const copyStrings`** (path relative to **`e2e/`**).
    2. Add one **`test(`** (desktop-only **`test.skip`** parity with existing customers test) that: **`login`** → **`Go to billing`** → org picker → navigate to **`/billing/subscriptions?org=...`** (reuse fixture slug pattern from customers test) → **`waitForLiveView`** → assert **`expect(page.getByRole('heading', { name: copyStrings.subscriptions_index_empty_title })).toBeVisible()`** (or equivalent **`getByRole`/`getByText`** if page structure uses **`<h2>`** vs **`<h1>`** — adjust locator to match DOM) **without** duplicating the English literal **`No subscriptions for this organization yet`** in the spec source.
    3. Call **`scanAxe(page)`** once after readiness and **`expect(violations).toEqual([])`** same as customers test.
    4. Ensure **`mix accrue_admin.export_copy_strings`** allowlist includes **`subscriptions_index_empty_title`** (0-arity on **`AccrueAdmin.Copy`**).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "subscriptions_index_empty_title|copy_strings" examples/accrue_host/e2e/verify01-admin-a11y.spec.js'</automated>
  </verify>
  <done>Playwright bound to Copy export.</done>
</task>

<task type="auto">
  <name>Task 4: Close 50-VERIFICATION anti-drift checklist</name>
  <read_first>
    .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md
  </read_first>
  <files>.planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md</files>
  <acceptance_criteria>
    - `rg -n 'export_copy_strings|copy_strings\\.json|D-23' .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md` returns **≥ 2** lines.
  </acceptance_criteria>
  <action>
    Append subsection **## Anti-drift (ADM-04 + ADM-06)** stating: **`mix accrue_admin.export_copy_strings`** is merge-blocking via **`accrue_host_verify_browser.sh`**; Playwright must **`require`/`readFileSync`** **`e2e/generated/copy_strings.json`** for at least **one** assertion per **D-23**.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "D-23|export_copy_strings" .planning/phases/50-copy-tokens-verify-gates/50-VERIFICATION.md'</automated>
  </verify>
  <done>Verification doc closed.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix accrue_admin.export_copy_strings --out ../examples/accrue_host/e2e/generated/copy_strings.json` — **0** exit (from monorepo layout).
- `cd examples/accrue_host && npm run e2e` — **0** exit (local; CI equivalent **`mix verify.full`**).
</verification>

<success_criteria>
- **D-23** satisfied: **JS tests** consume **Elixir-exported** Copy strings.
- **ADM-06** extended: **≥ 1** additional **mounted** path covered with **axe** in merge-blocking style.
</success_criteria>

---
phase: 51-integrator-golden-path-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/first_hour.md
  - examples/accrue_host/README.md
  - accrue/guides/quickstart.md
autonomous: true
requirements: [INT-01]
must_haves:
  truths:
    - "first_hour.md and examples/accrue_host/README.md both name Hex vs monorepo vs read-only entry paths and join the same ordered integrator spine"
    - "quickstart.md stays a thin hub and only adds routing into First Hour / host README — no second tutorial body"
  artifacts:
    - path: "accrue/guides/first_hour.md"
      provides: "Package-facing spine + capsules"
      contains: "Capsule"
    - path: "examples/accrue_host/README.md"
      provides: "Host-facing spine + capsules"
      contains: "Capsule"
    - path: "accrue/guides/quickstart.md"
      provides: "Hub index"
      contains: "First Hour"
  key_links:
    - from: "first_hour.md"
      to: "examples/accrue_host/README.md"
      via: "relative link to host README"
      pattern: "canonical host path"
---

<threat_model>
**ASVS L1.** Documentation only. Do **not** add real Stripe secrets, `sk_live`, webhook signing secrets, or customer PII. Use existing placeholders (`whsec_test_host`, `System.get_env("STRIPE_WEBHOOK_SECRET", "whsec_test_host")`, etc.).
</threat_model>

<objective>
Ship **INT-01**: one coherent **clone → install → Fake subscription → proof** narrative with **Step 0 entry capsules (H/M/R)** and a **strict single spine** shared between **`accrue/guides/first_hour.md`** and **`examples/accrue_host/README.md`**; keep **`accrue/guides/quickstart.md`** as a **thin index** only (**51-CONTEXT D-01–D-04**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
@.planning/phases/51-integrator-golden-path-docs/51-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry capsules + spine header in First Hour</name>
  <read_first>
    .planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
    accrue/guides/first_hour.md
    examples/accrue_host/README.md
  </read_first>
  <files>accrue/guides/first_hour.md</files>
  <acceptance_criteria>
    - `rg -n 'Capsule H|Capsule M|Capsule R' accrue/guides/first_hour.md` returns **≥ 3** lines (all three capsule labels present).
    - `rg -n '## How to enter this guide|## Entry capsules|### Capsule' accrue/guides/first_hour.md` returns **≥ 1** line (a dedicated heading introducing capsules).
    - `rg -n 'examples/accrue_host/README\\.md#proof-and-verification|examples/accrue_host/README.md#proof-and-verification' accrue/guides/first_hour.md` returns **≥ 1** line (deep link to host VERIFY section remains reachable from First Hour body).
    - `rg -ni 'sk_live' accrue/guides/first_hour.md` returns **0** lines (no live secret encouragement).
  </acceptance_criteria>
  <action>
    In **`accrue/guides/first_hour.md`**, immediately **after** the opening intro paragraphs (before **`## 1. First run`**), insert a short section (suggested title **`## How to enter this guide`**) that defines three capsules and how each joins the **same** ordered spine:
    - **Capsule H — Hex consumer:** existing Phoenix app → start at **deps** + **`mix accrue.install`** then continue the numbered spine (runtime → migrations → Oban → webhooks → admin → proof).
    - **Capsule M — Monorepo clone:** `cd examples/accrue_host` → **`mix setup`** / server path → same **subscription → webhook → admin → `mix verify`** story as the host README.
    - **Capsule R — Evaluate / read-only:** shortest path: clone → `cd examples/accrue_host` → **`mix verify`** (and optionally **`mix verify.full`**) with pointer into the full spine / **#proof-and-verification** when readers need CI detail.
    Add one explicit sentence: edits to the **public integrator story** stay aligned with **`examples/accrue_host/README.md`** (same PR when changing step order or command vocabulary per **D-02**).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "Capsule H|Capsule M|Capsule R" accrue/guides/first_hour.md | wc -l | awk "{exit !(\$1>=3)}"'</automated>
  </verify>
  <done>First Hour exposes capsules and spine contract.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror capsules + spine contract in host README</name>
  <read_first>
    accrue/guides/first_hour.md
    examples/accrue_host/README.md
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <acceptance_criteria>
    - `rg -n 'Capsule H|Capsule M|Capsule R' examples/accrue_host/README.md` returns **≥ 3** lines.
    - `rg -n '## How to enter this example|## Entry capsules|### Capsule' examples/accrue_host/README.md` returns **≥ 1** line.
    - `rg -n 'accrue/guides/first_hour\\.md' examples/accrue_host/README.md` returns **≥ 1** line (bidirectional pointer to First Hour preserved or strengthened).
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0** from repo root after edits (do **not** remove substrings the script requires).
  </acceptance_criteria>
  <action>
    In **`examples/accrue_host/README.md`**, before **`## First run`**, add the **mirror** capsule section (same **H/M/R** meanings and join points as First Hour). Keep **`## First run`** numbered story intact; only add orientation prose + cross-link to **`../../accrue/guides/first_hour.md`** for package-facing wording.
    Preserve all **VERIFY-01** / Playwright / `host-integration` prose required by **`scripts/ci/verify_verify01_readme_contract.sh`**.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>Host README mirrors capsules; VERIFY contract still green.</done>
</task>

<task type="auto">
  <name>Task 3: Quickstart hub — capsule routing only</name>
  <read_first>
    accrue/guides/quickstart.md
    accrue/guides/first_hour.md
  </read_first>
  <files>accrue/guides/quickstart.md</files>
  <acceptance_criteria>
    - `wc -l accrue/guides/quickstart.md` prints a line count **≤ 80** (stay thin — hub only).
    - `rg -n 'Capsule|First Hour|first_hour\\.md' accrue/guides/quickstart.md` returns **≥ 2** lines.
    - `rg -n '^## ' accrue/guides/quickstart.md` returns **≤ 6** lines (no large new top-level sections).
  </acceptance_criteria>
  <action>
    Under **`## Start with First Hour`** (or equivalent), add **≤ 4 sentences** telling readers to pick a **capsule** in **First Hour** (Hex vs monorepo vs read-only eval). Do **not** duplicate the install/webhook tutorial body here.
  </action>
  <verify>
    <automated>bash -lc 'python3 -c "import pathlib; p=pathlib.Path(\"accrue/guides/quickstart.md\"); exit(len(p.read_text().splitlines()) > 80)"'</automated>
  </verify>
  <done>Quickstart routes; does not fork the spine.</done>
</task>

</tasks>

## PLANNING COMPLETE

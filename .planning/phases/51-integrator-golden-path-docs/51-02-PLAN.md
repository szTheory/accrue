---
phase: 51-integrator-golden-path-docs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements: [INT-02]
must_haves:
  truths:
    - "Root README keeps VERIFY-01 two-hop intent: intent block + single deep link to host #proof-and-verification"
    - "CONTRIBUTING.md bridges Layer A (release gate) vs Layer B/C (host proof + PR contract) with one deep link to the host README proof section"
  artifacts:
    - path: "README.md"
      provides: "Repo-root VERIFY discoverability"
      contains: "proof-and-verification"
    - path: "CONTRIBUTING.md"
      provides: "Contributor vs host proof bridge"
      contains: "Layer"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "examples/accrue_host/README.md#proof-and-verification"
      via: "markdown anchor link"
      pattern: "D-08"
---

<threat_model>
**ASVS L1.** Prose only. Do not add credentials, live Stripe keys, or copy/pasteable webhook secrets. Keep **merge-blocking vs advisory** language aligned with **`.github/workflows/ci.yml`** job ids (**`host-integration`**, **`live-stripe`**) — verify with `rg host-integration README.md` after edits.
</threat_model>

<objective>
Ship **INT-02**: **VERIFY-01** and CI lanes stay **discoverable within two hops** from repo root; add **Layer A / B / C** vocabulary where integrators look before opening a PR (**51-CONTEXT D-06, D-08–D-12**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
@.planning/phases/51-integrator-golden-path-docs/51-RESEARCH.md
@scripts/ci/README.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root README — scannable CI-equivalent line</name>
  <read_first>
    README.md
    .planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
    scripts/ci/verify_verify01_readme_contract.sh
  </read_first>
  <files>README.md</files>
  <acceptance_criteria>
    - `rg -n '## Proof path' README.md` returns **≥ 1** line.
    - `rg -n 'examples/accrue_host/README\\.md#proof-and-verification' README.md` returns **≥ 1** line.
    - `rg -n '^```bash' README.md` returns **≥ 1** line **after** the first `## Proof path` heading (a fenced block exists in the Proof path section for scanner-friendly copy/paste).
    - `rg -n 'cd examples/accrue_host' README.md` returns **≥ 1** line inside a fenced `bash` block in or immediately after the Proof path section.
    - `rg -n 'mix verify\\.full' README.md` returns **≥ 1** line in that same fenced block or the immediately following fenced block under Proof path.
  </acceptance_criteria>
  <action>
    In **`README.md`**, under **`## Proof path (VERIFY-01)`**, after the paragraph that names **`host-integration`** and contrasts **`mix verify`** vs **`mix verify.full`**, insert a **single** fenced **`bash`** block (≤ 5 lines) whose primary command is exactly:
    `cd examples/accrue_host && mix verify.full`
    Optional: prefix with **one comment line** (`# CI-equivalent local gate`) — no duplicate of the long matrix from the host README.
    Preserve the existing **markdown link** on the next line(s) to **`examples/accrue_host/README.md#proof-and-verification`**.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>Root Proof path has a fenced CI-equivalent line; VERIFY script still passes.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING — Host proof (VERIFY-01) bridge</name>
  <read_first>
    CONTRIBUTING.md
    README.md
    examples/accrue_host/README.md
  </read_first>
  <files>CONTRIBUTING.md</files>
  <acceptance_criteria>
    - `rg -n 'examples/accrue_host/README\\.md#proof-and-verification' CONTRIBUTING.md` returns **≥ 1** line.
    - `rg -n 'Layer A|Layer B|Layer C' CONTRIBUTING.md` returns **≥ 3** lines cumulative OR **≥ 1** line listing all three layer names in one sentence/bullet block (use **exact** substrings **Layer A**, **Layer B**, **Layer C**).
    - `rg -n 'host proof|VERIFY-01|verify-01' CONTRIBUTING.md` returns **≥ 1** line (case-insensitive ok via separate ripgrep flags in manual check; for automation use `rg -ni 'verify-01|host proof' CONTRIBUTING.md` returns ≥ 1).
    - `rg -n 'scripts/ci/README\\.md' CONTRIBUTING.md` returns **≥ 1** line (retain triage pointer next to new bridge).
  </acceptance_criteria>
  <action>
    Append a short subsection (suggested title **`## Host proof (VERIFY-01)`**) after **`## Adoption (ADOPT) CI contracts`** (or merge into that section as final bullets if flow reads better) containing:
    1. **One** bullet for **Layer A** — per-package **`mix test` / Credo / Dialyzer** release gate (already documented above).
    2. **One** bullet for **Layer B** — `cd examples/accrue_host` → **`mix verify`** (fast Fake slice) vs **`mix verify.full`** (CI-equivalent host stack).
    3. **One** bullet for **Layer C** — PR **`host-integration`** merge contract **≠** running **`mix verify.full`** alone; point to **`scripts/ci/README.md`** for script/lane triage and to **host README** for VERIFY-01 detail.
    Include a **repo-relative** markdown link to **`examples/accrue_host/README.md#proof-and-verification`**.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "examples/accrue_host/README.md#proof-and-verification" CONTRIBUTING.md'</automated>
  </verify>
  <done>CONTRIBUTING bridges contributor vs host proof layers.</done>
</task>

</tasks>

## PLANNING COMPLETE

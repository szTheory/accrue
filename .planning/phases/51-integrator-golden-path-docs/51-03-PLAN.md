---
phase: 51-integrator-golden-path-docs
plan: "03"
type: execute
wave: 2
depends_on: ["51-01-PLAN.md", "51-02-PLAN.md"]
files_modified:
  - accrue/guides/troubleshooting.md
  - accrue/guides/webhooks.md
  - accrue/guides/first_hour.md
  - examples/accrue_host/README.md
autonomous: true
requirements: [INT-03]
must_haves:
  truths:
    - "troubleshooting.md documents kebab-case anchor convention aligned with ACCRUE-DX codes"
    - "webhooks.md links to the stable ACCRUE-DX-WEBHOOK-RAW-BODY troubleshooting section"
    - "first_hour.md and host README carry at most short failure callouts + deep links — no second full matrix"
  artifacts:
    - path: "accrue/guides/troubleshooting.md"
      provides: "Anchor SSOT note"
      contains: "kebab"
    - path: "accrue/guides/webhooks.md"
      provides: "Raw-body failure deep link"
      contains: "accrue-dx-webhook-raw-body"
  key_links:
    - from: "webhooks.md"
      to: "troubleshooting.md#accrue-dx-webhook-raw-body"
      via: "relative .md link"
      pattern: "D-13"
---

<threat_model>
**ASVS L1.** Documentation only. Link targets must stay on **repo-relative** paths and **existing** `{#anchor}` headings — do not invent new `ACCRUE-DX-*` codes without updating **`accrue/lib/accrue/setup_diagnostic.ex`** in a different phase.
</threat_model>

<objective>
Ship **INT-03**: first-run failure classes (**webhook signing + raw body ordering**, **secrets**, **`mix accrue.install` rerun / conflicts**) stay **linkable** from narrative surfaces with **stable anchors** per **51-CONTEXT D-13–D-17** (hybrid SSOT with **`troubleshooting.md`**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
@.planning/phases/51-integrator-golden-path-docs/51-RESEARCH.md
@accrue/lib/accrue/setup_diagnostic.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Troubleshooting — anchor convention (document once)</name>
  <read_first>
    accrue/guides/troubleshooting.md
    accrue/lib/accrue/setup_diagnostic.ex
    .planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
  </read_first>
  <files>accrue/guides/troubleshooting.md</files>
  <acceptance_criteria>
    - `rg -n 'kebab-case|kebab case|fragment' accrue/guides/troubleshooting.md` returns **≥ 1** line in the intro area (first **40** lines of file: use `head -n 40` combined with `rg` in manual verification; automation: `head -n 40 accrue/guides/troubleshooting.md | rg -n 'kebab|fragment|anchor'` returns ≥ 1).
    - `rg -n '#accrue-dx-webhook-raw-body' accrue/guides/troubleshooting.md` returns **≥ 1** line (existing section preserved).
    - `rg -n '\\{#accrue-dx-webhook-raw-body\\}' accrue/guides/troubleshooting.md` returns **≥ 1** line (heading anchor syntax present for that diagnostic).
  </acceptance_criteria>
  <action>
    After the opening paragraph of **`accrue/guides/troubleshooting.md`** (before the matrix table), insert a short note (**≤ 6 sentences**) explaining: section anchors use **lowercase kebab-case** derived from the **`ACCRUE-DX-*`** code (example: **`ACCRUE-DX-WEBHOOK-RAW-BODY`** → **`#accrue-dx-webhook-raw-body`**). Mention that **`Accrue.SetupDiagnostic`** doc paths should stay consistent with these fragments.
  </action>
  <verify>
    <automated>bash -lc 'head -n 45 accrue/guides/troubleshooting.md | rg -n "kebab|fragment|anchor|#" | head -n 1 | grep -q .'</automated>
  </verify>
  <done>Anchor convention documented at troubleshooting SSOT.</done>
</task>

<task type="auto">
  <name>Task 2: Webhooks guide — deep link for signature / raw-body failures</name>
  <read_first>
    accrue/guides/webhooks.md
    accrue/guides/troubleshooting.md
  </read_first>
  <files>accrue/guides/webhooks.md</files>
  <acceptance_criteria>
    - `rg -n 'troubleshooting\\.md#accrue-dx-webhook-raw-body' accrue/guides/webhooks.md` returns **≥ 1** line.
    - `rg -n 'ACCRUE-DX-WEBHOOK-RAW-BODY' accrue/guides/webhooks.md` returns **≥ 1** line.
  </acceptance_criteria>
  <action>
    Under **`## Signature failures and generic HTTP failures`** in **`accrue/guides/webhooks.md`**, add **≤ 3 sentences** plus a markdown link to **`troubleshooting.md#accrue-dx-webhook-raw-body`** (repo-relative from `accrue/guides/`). Name the diagnostic code **`ACCRUE-DX-WEBHOOK-RAW-BODY`** explicitly once.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "troubleshooting.md#accrue-dx-webhook-raw-body" accrue/guides/webhooks.md'</automated>
  </verify>
  <done>Webhooks failure surface links to SSOT matrix row.</done>
</task>

<task type="auto">
  <name>Task 3: First Hour — short troubleshooting pointers (INT-03 classes)</name>
  <read_first>
    accrue/guides/first_hour.md
    accrue/guides/troubleshooting.md
    accrue/guides/upgrade.md
  </read_first>
  <files>accrue/guides/first_hour.md</files>
  <acceptance_criteria>
    - `rg -n 'troubleshooting\\.md#' accrue/guides/first_hour.md` returns **≥ 3** lines (at least three distinct anchor links into troubleshooting).
    - Links include **`#accrue-dx-webhook-raw-body`** and **`#accrue-dx-webhook-secret-missing`** (or the exact subsection anchor spelling used in `troubleshooting.md` for webhook secret — verify file uses **`#accrue-dx-webhook-secret-missing`**; adjust acceptance to match actual heading slug).
    - `rg -n 'upgrade\\.md#installer-rerun-behavior' accrue/guides/first_hour.md` returns **≥ 1** line OR existing equivalent **`upgrade.md`** installer rerun link remains **and** a new explicit **`#installer-rerun-behavior`** link is added if missing.
  </acceptance_criteria>
  <action>
    Add **≤ 8 lines** total of **“When this fails”** callouts (can be one blockquote or bullet list) placed after the **router raw-body example** block and before **`### Prove the first subscription`**, linking to:
    - **`troubleshooting.md#accrue-dx-webhook-raw-body`** (parser / raw body ordering),
    - **`troubleshooting.md#accrue-dx-webhook-secret-missing`** (or the correct existing slug for webhook secret issues — read **`troubleshooting.md`** headings first),
    - **`upgrade.md#installer-rerun-behavior`** for installer reruns / conflicts (per **D-16**).
    Do **not** duplicate full troubleshooting bodies.
  </action>
  <verify>
    <automated>bash -lc 'test "$(rg -c "troubleshooting\\.md#" accrue/guides/first_hour.md)" -ge 3'</automated>
  </verify>
  <done>First Hour carries bounded failure pointers.</done>
</task>

<task type="auto">
  <name>Task 4: Host README — first-run troubleshooting scent</name>
  <read_first>
    examples/accrue_host/README.md
    accrue/guides/troubleshooting.md
  </read_first>
  <files>examples/accrue_host/README.md</files>
  <acceptance_criteria>
    - `rg -n 'troubleshooting\\.md#' examples/accrue_host/README.md` returns **≥ 2** lines.
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
  </acceptance_criteria>
  <action>
    In **`examples/accrue_host/README.md`**, within **`## First run`** (after step **2** webhook bullet or end of step list), add **≤ 5 lines** pointing to **`../../accrue/guides/troubleshooting.md`** for **`ACCRUE-DX-*`** codes (name **≥ 2** concrete codes relevant to webhooks/secrets). Link using repo-relative path from the example host directory.
    Do not move VERIFY-01 authority out of **`#proof-and-verification`**.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_verify01_readme_contract.sh</automated>
  </verify>
  <done>Host README surfaces DX/troubleshooting scent without duplicating SSOT.</done>
</task>

</tasks>

## PLANNING COMPLETE

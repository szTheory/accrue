---
phase: 52-integrator-proof-package-alignment-auxiliary-copy-part-1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - examples/accrue_host/docs/evaluator-walkthrough-script.md
  - examples/accrue_host/README.md
  - scripts/ci/verify_adoption_proof_matrix.sh
  - scripts/ci/verify_verify01_readme_contract.sh
autonomous: true
requirements: [INT-04]
must_haves:
  truths:
    - "adoption-proof-matrix.md stays consistent with README VERIFY-01 commands and CI job ids touched by INT-01..INT-03"
    - "verify_verify01_readme_contract.sh and verify_adoption_proof_matrix.sh exit 0 after changes"
    - "evaluator-walkthrough-script.md edited only when D-06 triggers (commands, CI claims, artifact names, blocking vs advisory script lines)"
  artifacts:
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "ORG-09 semantic proof map"
      contains: "ORG-09"
    - path: "examples/accrue_host/README.md"
      provides: "VERIFY-01 executable SSOT"
      contains: "VERIFY-01"
  key_links:
    - from: "52-CONTEXT.md"
      to: "D-05 D-06 D-07 D-08"
      via: "locked decisions"
      pattern: "context"
---

<threat_model>
**ASVS L1.** Doc-only work: **do not** embed **live** Stripe secrets (`sk_live`, raw **webhook signing secrets**), customer emails, or production URLs in **`adoption-proof-matrix.md`**, **`evaluator-walkthrough-script.md`**, or **`README.md`**. Use **`whsec_test_*`**, **Fake** processor wording, and **template routes** (`/billing/...`) only. When describing CI, use **exact job ids** from **`.github/workflows/ci.yml`** — no invented job names.
</threat_model>

<objective>
Satisfy **INT-04**: keep **`adoption-proof-matrix.md`** and dependent **README** / **walkthrough** artifacts **evidence-true** vs **golden path + CI lanes** from **INT-01..INT-03**, extending **`verify_adoption_proof_matrix.sh`** / **`verify_verify01_readme_contract.sh`** when new stable literals are introduced (**52-CONTEXT D-05–D-08**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-CONTEXT.md
@.planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-UI-SPEC.md
@.planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-RESEARCH.md
@.planning/phases/51-integrator-golden-path-docs/51-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit matrix vs README + CI (INT-04 baseline)</name>
  <read_first>
    examples/accrue_host/docs/adoption-proof-matrix.md
    examples/accrue_host/README.md
    .github/workflows/ci.yml
    scripts/ci/verify_verify01_readme_contract.sh
    scripts/ci/verify_adoption_proof_matrix.sh
    .planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <acceptance_criteria>
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0** before and after this task wave (re-run after edits in Task 2).
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0** after any matrix edit in Task 2.
    - `rg -n 'host-integration' examples/accrue_host/README.md` returns **≥ 1** line.
    - `rg -n 'docs/adoption-proof-matrix\\.md' examples/accrue_host/README.md` returns **≥ 1** line.
  </acceptance_criteria>
  <action>
    1. Read **`examples/accrue_host/README.md`** **VERIFY-01** section and list **every** command string, script path, and **GitHub Actions job id** the README asserts as merge-blocking or advisory.
    2. Read **`.github/workflows/ci.yml`** and confirm **each job id** referenced in the README/matrix **exists** (exact string match for ids cited as SSOT).
    3. Read **`adoption-proof-matrix.md`** and fix **factual drift** only (wrong job names, wrong script paths, stale lane descriptions per **52-CONTEXT D-07**). Do **not** rewrite prose for style.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && bash scripts/ci/verify_verify01_readme_contract.sh'</automated>
  </verify>
  <done>Matrix and README factual alignment documented in commit message body (1–3 bullets).</done>
</task>

<task type="auto">
  <name>Task 2: Extend verify_adoption_proof_matrix needles (if matrix gained literals)</name>
  <read_first>
    scripts/ci/verify_adoption_proof_matrix.sh
    examples/accrue_host/docs/adoption-proof-matrix.md
  </read_first>
  <files>scripts/ci/verify_adoption_proof_matrix.sh</files>
  <acceptance_criteria>
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.
    - If **Task 1** added **new** stable substrings to **`adoption-proof-matrix.md`** that must never drift (new section heading, new job id, new script path), then **`verify_adoption_proof_matrix.sh`** contains a **new** `require_substring "…"` line for **each** such substring (grep count of `require_substring` in script is **≥** previous count).
    - If **no** new stable literals were added, script diff may be **empty** — then `git diff --name-only scripts/ci/verify_adoption_proof_matrix.sh` is empty **OR** only comment changes.
  </acceptance_criteria>
  <action>
    For **each** new **machine-checkable** literal introduced into **`adoption-proof-matrix.md`**, add **`require_substring "<literal>" "<label>"`** to **`scripts/ci/verify_adoption_proof_matrix.sh`** mirroring existing **`require_substring`** style (lines **22–30** pattern). Skip subjective prose.
  </action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_adoption_proof_matrix.sh'</automated>
  </verify>
  <done>Matrix gate green.</done>
</task>

<task type="auto">
  <name>Task 3: README contract extension (only if README literals changed)</name>
  <read_first>
    scripts/ci/verify_verify01_readme_contract.sh
    examples/accrue_host/README.md
  </read_first>
  <files>scripts/ci/verify_verify01_readme_contract.sh</files>
  <acceptance_criteria>
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
    - If **`examples/accrue_host/README.md`** gained a **new** referenced **`e2e/verify01-*.spec.js`** path, that file **exists** on disk (`test -f "examples/accrue_host/${path}"` for each new path).
    - If no README VERIFY-01 literal changes: `git diff --name-only scripts/ci/verify_verify01_readme_contract.sh` may be empty.
  </acceptance_criteria>
  <action>
    If **Task 1** changed **VERIFY-01** literals (new Playwright spec path, new **`mix`** target string, new **`cd examples/accrue_host`** block): extend **`verify_verify01_readme_contract.sh`** with additional **`require_substring`** calls **or** ensure the existing **`while read spec_path`** loop still covers every **`e2e/verify01-*.spec.js`** mention extracted by **`grep -oE`** in the script.
  </action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_verify01_readme_contract.sh'</automated>
  </verify>
  <done>README contract gate green.</done>
</task>

<task type="auto">
  <name>Task 4: Evaluator walkthrough (D-06 gated)</name>
  <read_first>
    examples/accrue_host/docs/evaluator-walkthrough-script.md
    examples/accrue_host/README.md
    examples/accrue_host/docs/adoption-proof-matrix.md
    .planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/docs/evaluator-walkthrough-script.md</files>
  <acceptance_criteria>
    - **Either** `git diff --name-only examples/accrue_host/docs/evaluator-walkthrough-script.md` is **empty** (no D-06 trigger — document **skip** in commit message) **OR** the diff updates **only** lines tied to **commands**, **CI equivalence**, **artifact file names**, or **blocking vs advisory** evaluator speech per **52-CONTEXT D-06**.
    - If file changed: `rg -n 'mix verify|mix verify\\.full|playwright|host-integration|adoption-proof-matrix' examples/accrue_host/docs/evaluator-walkthrough-script.md` returns **≥ 3** distinct match lines (script remains anchored to executable proof).
  </acceptance_criteria>
  <action>
    Apply **52-CONTEXT D-06**: update **`evaluator-walkthrough-script.md`** **only if** Task 1–3 altered (or immediately follow-up fixed) any **command**, **CI job id equivalence claim**, **artifact path**, or **blocking vs advisory** language that the walkthrough tells an evaluator to say or show. Otherwise **leave file unchanged** and note **D-06 skip** in commit message.
  </action>
  <verify>
    <automated>bash -lc 'test -f examples/accrue_host/docs/evaluator-walkthrough-script.md'</automated>
  </verify>
  <done>Walkthrough policy satisfied.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_verify01_readme_contract.sh` — exit **0**.
- `bash scripts/ci/verify_adoption_proof_matrix.sh` — exit **0**.
- `rg -n 'Layer B|Layer C|host-integration' examples/accrue_host/docs/adoption-proof-matrix.md` shows **no** line implying **Layer B == Layer C** unless explicitly qualified as **distinct** (manual read + fix if found).
</verification>

<success_criteria>
- **INT-04** checklist items in **`.planning/REQUIREMENTS.md`** are satisfied for matrix + README + walkthrough (where triggered) honesty.
</success_criteria>

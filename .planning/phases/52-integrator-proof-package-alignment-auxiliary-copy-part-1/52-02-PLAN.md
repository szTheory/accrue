---
phase: 52-integrator-proof-package-alignment-auxiliary-copy-part-1
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/README.md
  - accrue_admin/README.md
  - accrue/guides/first_hour.md
  - README.md
  - accrue/mix.exs
  - accrue_admin/mix.exs
autonomous: true
requirements: [INT-05]
must_haves:
  truths:
    - "Published SemVer in consumer install snippets matches @version parsed from accrue/mix.exs and accrue_admin/mix.exs (verify_package_docs enforces)"
    - "README-on-main vs Hex footgun called out on GitHub-facing READMEs per D-11"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "version + install fence CI"
      contains: "extract_version"
    - path: "accrue/mix.exs"
      provides: "numeric SSOT"
      contains: "@version"
  key_links:
    - from: "52-CONTEXT.md"
      to: "D-09 D-10 D-11 D-12"
      via: "INT-05"
      pattern: "context"
---

<threat_model>
**ASVS L1.** Doc edits: **never** add real **`STRIPE_SECRET_KEY`**, **`HEX_API_KEY`**, or live webhook secrets to **README** / **first_hour** / **`RELEASING`**. Keep **`System.get_env`** placeholders and **`whsec_test`** patterns consistent with existing guides. **`verify_package_docs.sh`** must not echo secret values — only version strings already public in **`mix.exs`**.
</threat_model>

<objective>
Satisfy **INT-05**: extend **`verify_package_docs.sh`** so **every** Phase-52-touched **install-adjacent** surface stays aligned with **`@version`** in **`accrue/mix.exs`** / **`accrue_admin/mix.exs`**; add **Hex vs `main`** clarity banners per **D-11** where missing (**52-CONTEXT**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-CONTEXT.md
@.planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verify_package_docs for touched paths</name>
  <read_first>
    scripts/ci/verify_package_docs.sh
    accrue/mix.exs
    accrue_admin/mix.exs
  </read_first>
  <files>scripts/ci/verify_package_docs.sh</files>
  <acceptance_criteria>
    - `bash scripts/ci/verify_package_docs.sh` exits **0** **before** edits (baseline) **and** **after** edits (final).
    - After edits: script contains **≥ 1** new **`require_fixed`** or **`require_regex`** line compared to **`main`** baseline **when** any markdown edit introduced new stable literals; preserve `accrue_version=$(extract_version` — `grep -Fq 'accrue_version=$(extract_version' scripts/ci/verify_package_docs.sh` exits **0**.
  </acceptance_criteria>
  <action>
    1. Run **`bash scripts/ci/verify_package_docs.sh`** once on a clean tree (must exit **0**).
    2. For **each** markdown file edited for **INT-05** (**`accrue/README.md`**, **`accrue_admin/README.md`**, **`accrue/guides/first_hour.md`**, root **`README.md`**, etc.), add **`require_fixed`** / **`require_regex`** lines for **each** new **verbatim** install or Hex banner substring so Release Please bumps surface CI failures (**52-CONTEXT D-10**). Use **exact** copied substrings.
  </action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_package_docs.sh'</automated>
  </verify>
  <done>Gate extended.</done>
</task>

<task type="auto">
  <name>Task 2: Hex vs main banners (D-11)</name>
  <read_first>
    accrue/README.md
    accrue_admin/README.md
    README.md
    .planning/phases/52-integrator-proof-package-alignment-auxiliary-copy-part-1/52-CONTEXT.md
  </read_first>
  <files>accrue/README.md accrue_admin/README.md README.md</files>
  <acceptance_criteria>
    - At **least one** of **`accrue/README.md`**, **`accrue_admin/README.md`**, or root **`README.md`** contains the substring **`Hex`** AND **`main`** in **≤ 400** chars of each other (`rg -Uzo '(?s)Hex.{0,400}main|main.{0,400}Hex' accrue/README.md accrue_admin/README.md README.md` returns **≥ 1** match across the three files **OR** a clearly equivalent banner using **`hex.pm`** URL + **branch** wording — if already present, `rg -n 'hex\\.pm|Hex\\.pm' accrue/README.md accrue_admin/README.md` returns **≥ 1** line).
    - `bash scripts/ci/verify_package_docs.sh` exits **0** after banner text (extend **Task 1** `require_fixed` needles for any new banner literals).
  </acceptance_criteria>
  <action>
    Add a **short** callout (1–3 sentences) near the **install** snippet on **GitHub-facing** READMEs: dependency lines on **`main`** reflect **`@version` on this branch**; **published** docs/versions → **https://hex.pm/** packages / **HexDocs** links (**52-CONTEXT D-11**). Do **not** introduce a second numeric version SSOT — prose points to **`mix.exs`** / Hex.
  </action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_package_docs.sh'</automated>
  </verify>
  <done>Banners landed.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh` — exit **0**.
- `accrue_version=$(sed -n 's/^  @version "\\([^"]*\\)"/\\1/p' accrue/mix.exs | head -1)` and `grep -Fq "{:accrue, \"~> $accrue_version\"" accrue/README.md` — exit **0** (dynamic check in shell).
</verification>

<success_criteria>
- **INT-05** satisfied: **`verify_package_docs`** green; install lines aligned with **`@version`**; Hex clarity improved.
</success_criteria>

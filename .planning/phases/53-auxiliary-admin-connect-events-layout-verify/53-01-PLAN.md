---
phase: 53-auxiliary-admin-connect-events-layout-verify
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/copy/connect.ex
  - accrue_admin/lib/accrue_admin/copy/billing_event.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
  - accrue_admin/lib/accrue_admin/live/connect_account_live.ex
  - accrue_admin/lib/accrue_admin/live/events_live.ex
  - accrue_admin/guides/theme-exceptions.md
  - accrue_admin/test/accrue_admin/copy/connect_test.exs
  - accrue_admin/test/accrue_admin/copy/billing_event_test.exs
autonomous: true
requirements: [AUX-03, AUX-04, AUX-05]
must_haves:
  truths:
    - "No new raw English operator phrases remain on materially touched HEEx in ConnectAccountsLive, ConnectAccountLive, EventsLive — grep resolves to AccrueAdmin.Copy.*"
    - "Connect and billing events Copy use D-10 prefixes (connect_accounts_*, connect_account_*, billing_events_*)"
    - "Destructive deauthorize UI is not added (D-06)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy/connect.ex"
      provides: "AUX-03 Connect copy"
      contains: "defmodule AccrueAdmin.Copy.Connect"
    - path: "accrue_admin/lib/accrue_admin/copy/billing_event.ex"
      provides: "AUX-04 events copy"
      contains: "defmodule AccrueAdmin.Copy.BillingEvent"
  key_links:
    - from: "53-UI-SPEC.md"
      to: "Copy table + ax-* rules"
      via: "implementation targets"
      pattern: "ui-spec"
---

<threat_model>
**ASVS L1.** Copy modules and LiveView templates must not embed **live** Stripe secrets, webhook signing secrets, or customer PII. Use **Fake**-aligned wording only. Flash and error strings are **operator guidance**, not channels for host-injected HTML — keep `Phoenix.HTML` safe defaults (no `raw/1` on untrusted input). **Do not** add a **deauthorize** / hard-delete **LiveView event** or production button (D-06 — elevation-of-trust / least-surprise).
</threat_model>

<objective>
Ship **AUX-03**, **AUX-04**, **AUX-05**: **`AccrueAdmin.Copy.Connect`** + **`Copy.BillingEvent`** with **`defdelegate`** on **`AccrueAdmin.Copy`**; migrate **Connect** + **events** LiveViews to Copy-backed strings and **`ax-*` / `--ax-*`** token discipline; update **`theme-exceptions.md`** only for **documented** real deviations.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-CONTEXT.md
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-UI-SPEC.md
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-RESEARCH.md
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Copy.Connect and Copy.BillingEvent modules</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy/coupon.ex
    accrue_admin/lib/accrue_admin/copy/promotion_code.ex
    .planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-UI-SPEC.md
    .planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-CONTEXT.md
  </read_first>
  <files>
    accrue_admin/lib/accrue_admin/copy/connect.ex
    accrue_admin/lib/accrue_admin/copy/billing_event.ex
  </files>
  <acceptance_criteria>
    - `accrue_admin/lib/accrue_admin/copy/connect.ex` exists and first line of module is `defmodule AccrueAdmin.Copy.Connect do`.
    - `accrue_admin/lib/accrue_admin/copy/billing_event.ex` exists and first line is `defmodule AccrueAdmin.Copy.BillingEvent do`.
    - **`connect.ex`** returns **exact** UI-SPEC literals for: primary CTA **`Save platform fee override`**; secondary **`Apply filters`**; Connect index empty heading **`No connected accounts yet`** and body starting **`Stripe projections will appear here`**; generic error copy starting **`This Connect view failed to load`**.
    - **`billing_event.ex`** returns **exact** UI-SPEC literals: Events empty heading **`No billing events matched`** and body starting **`Loosen filters or trigger`**.
    - Flash string **`Platform fee override saved.`** is returned only from **`AccrueAdmin.Copy`** (e.g. **`connect_account_flash_override_saved/0`**) with **character-for-character** match to prior LiveView behavior.
    - `rg "defp " accrue_admin/lib/accrue_admin/copy/connect.ex accrue_admin/lib/accrue_admin/copy/billing_event.ex` returns **no** lines (public API only, mirroring coupon modules — adjust if sibling modules use `defp` for helpers; if helpers exist, they must remain **private** and not bypass Copy facade).
  </acceptance_criteria>
  <action>
    1. Create **`accrue_admin/lib/accrue_admin/copy/connect.ex`** implementing **all** Connect list/detail operator strings currently hard-coded in **`connect_accounts_live.ex`** and **`connect_account_live.ex`**, using prefixes **`connect_accounts_*`** (index) and **`connect_account_*`** (detail) per **53-CONTEXT D-10**.
    2. Create **`accrue_admin/lib/accrue_admin/copy/billing_event.ex`** implementing **events** index strings (eyebrow, heading, body, KPI labels/meta, table column labels, empty states, filter labels) using prefix **`billing_events_*`** per **D-10**.
    3. Map **every** English string visible in those three LiveView **`render/1`** templates (including breadcrumb labels **Connect**, **Events**, **Dashboard** if they appear as literals) into named functions — **Dashboard** may stay aligned with other lives if already centralized; if other lives use **`AccrueAdmin.Copy`** for **Dashboard**, delegate; otherwise add **`connect_accounts_breadcrumb_dashboard`** only if required for grep cleanliness on touched files.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue_admin" && mix compile'</automated>
  </verify>
  <done>Copy modules compile.</done>
</task>

<task type="auto">
  <name>Task 2: Facade defdelegates on AccrueAdmin.Copy</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex
    accrue_admin/lib/accrue_admin/copy/connect.ex
    accrue_admin/lib/accrue_admin/copy/billing_event.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy.ex</files>
  <acceptance_criteria>
    - `accrue_admin/lib/accrue_admin/copy.ex` contains `alias AccrueAdmin.Copy.Connect` and `alias AccrueAdmin.Copy.BillingEvent`.
    - Count of `defdelegate` lines targeting **Connect** or **BillingEvent** is **≥ 12** (substantive surface — adjust upward if more functions added in Task 1).
    - `cd accrue_admin && mix compile` exits **0**.
  </acceptance_criteria>
  <action>
    Add **`alias`** lines after existing Copy aliases and add **`defdelegate ... , to: Connect`** / **`to: BillingEvent`** for **each** public 0-arity function defined in Task 1 so **`AccrueAdmin.Copy.function_name/0`** is callable from HEEx.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue_admin" && mix compile'</automated>
  </verify>
  <done>Facade wired.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Connect + Events LiveViews to Copy + tokens</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
    accrue_admin/lib/accrue_admin/live/connect_account_live.ex
    accrue_admin/lib/accrue_admin/live/events_live.ex
    accrue_admin/lib/accrue_admin/live/coupons_live.ex
    accrue_admin/lib/accrue_admin/copy.ex
    .planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-UI-SPEC.md
  </read_first>
  <files>
    accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
    accrue_admin/lib/accrue_admin/live/connect_account_live.ex
    accrue_admin/lib/accrue_admin/live/events_live.ex
  </files>
  <acceptance_criteria>
    - `rg -c "AccrueAdmin\\.Copy\\." accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex` outputs a count **≥ 5**.
    - `rg -c "AccrueAdmin\\.Copy\\." accrue_admin/lib/accrue_admin/live/connect_account_live.ex` outputs a count **≥ 8**.
    - `rg -c "AccrueAdmin\\.Copy\\." accrue_admin/lib/accrue_admin/live/events_live.ex` outputs a count **≥ 5**.
    - `rg "handle_event\\(\"deauthorize|>Deauthorize<|Deauthorize Connect" accrue_admin/lib/accrue_admin/live/connect_account_live.ex` exits **1** (no matches).
    - Fee override submit control in **`connect_account_live.ex`** still uses **`type="submit"`** (or **`phx-submit`**) and retains **`ax-*`** / primary button class pattern — `rg "save_override|override" accrue_admin/lib/accrue_admin/live/connect_account_live.ex` returns **≥ 1** line.
  </acceptance_criteria>
  <action>
    Replace hard-coded operator strings in **`~H"""`** with **`AccrueAdmin.Copy.*()`** calls (including **`KpiCard`** labels/meta, table column labels, empty states, **`aria-label`** values).
    Replace **`activity_feed_*`** / similar helper functions in **`EventsLive`** if they only returned English — either move strings into **`Copy.BillingEvent`** and keep thin assigns, or have helpers call **`AccrueAdmin.Copy`**.
    Align breadcrumb **`%{label: ...}`** with **`AccrueAdmin.Copy.connect_accounts_breadcrumb_connect()`** (or equivalent) for **Connect** / **Events** labels.
    For **`connect_account_live.ex`** flash messages in **`handle_event`**, use **`AccrueAdmin.Copy.connect_account_flash_override_saved()`** (exact string per UI-SPEC / existing behavior).
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue_admin" && mix test'</automated>
  </verify>
  <done>LiveViews compile and tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Theme exceptions register (AUX-05)</name>
  <read_first>
    accrue_admin/guides/theme-exceptions.md
    accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
    accrue_admin/lib/accrue_admin/live/connect_account_live.ex
    accrue_admin/lib/accrue_admin/live/events_live.ex
  </read_first>
  <files>accrue_admin/guides/theme-exceptions.md</files>
  <acceptance_criteria>
    - `rg -n "phase_ref.*53|Phase 53" accrue_admin/guides/theme-exceptions.md` returns **≥ 1** line (either a table row with phase **53** or a **Phase 53** sign-off note).
    - The literal **`none-yet`** no longer appears in **`theme-exceptions.md`** **OR** a new real exception row was added and **`none-yet`** row removed per register hygiene.
  </acceptance_criteria>
  <action>
    Audit the three LiveViews for **hard-coded hex colors**, **inline `style=`**, or **non-`ax-*` layout classes** on **v1.13-touched** auxiliary rows. For each **unavoidable** deviation, add a **`theme-exceptions.md`** row (**`phase_ref` `53`**) and a **short HTML comment** pointer in HEEx. If **no** deviations: add a **Phase 53** reviewer note stating **no token bypasses** and remove the **`none-yet`** placeholder row.
  </action>
  <verify>
    <automated>bash -lc 'test -f accrue_admin/guides/theme-exceptions.md'</automated>
  </verify>
  <done>Theme register honest.</done>
</task>

<task type="auto">
  <name>Task 5: ExUnit for new Copy modules</name>
  <read_first>
    accrue_admin/test/accrue_admin/copy/coupon_test.exs
    accrue_admin/lib/accrue_admin/copy/connect.ex
    accrue_admin/lib/accrue_admin/copy/billing_event.ex
  </read_first>
  <files>
    accrue_admin/test/accrue_admin/copy/connect_test.exs
    accrue_admin/test/accrue_admin/copy/billing_event_test.exs
  </files>
  <acceptance_criteria>
    - Files **`accrue_admin/test/accrue_admin/copy/connect_test.exs`** and **`billing_event_test.exs`** exist.
    - `cd accrue_admin && mix test test/accrue_admin/copy/connect_test.exs test/accrue_admin/copy/billing_event_test.exs` exits **0**.
    - At least **one** assertion per file checks **exact string equality** against a **UI-SPEC** target (e.g. **`No connected accounts yet`**).
  </acceptance_criteria>
  <action>
    Add ** doctest-style or plain `assert AccrueAdmin.Copy.connect_accounts_*`** tests mirroring **`coupon_test.exs`** structure for **≥ 3** functions per module covering **empty titles**, **headlines**, and **one** KPI label each.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue_admin" && mix test test/accrue_admin/copy/connect_test.exs test/accrue_admin/copy/billing_event_test.exs'</automated>
  </verify>
  <done>Copy regression tests landed.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix test` — exit **0**.
- `cd accrue_admin && mix compile --warnings-as-errors` — exit **0** if project already uses this flag in CI; otherwise `mix compile`.
- Spot: **`rg "Deauthorize|deauthorize" accrue_admin/lib/accrue_admin/live/connect_account_live.ex`** — must show **no** new interactive deauthorize **button** (existing word in copy/comments only if unavoidable — prefer zero).
</verification>

<success_criteria>
- **AUX-03**, **AUX-04**, **AUX-05** satisfied for **Connect** + **events** Copy, tokens, and documented exceptions per **53-UI-SPEC** and **53-CONTEXT** (through end of plan **01**; **AUX-06** is plan **02**).
</success_criteria>

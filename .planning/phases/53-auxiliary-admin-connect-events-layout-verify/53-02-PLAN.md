---
phase: 53-auxiliary-admin-connect-events-layout-verify
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
  - examples/accrue_host/e2e/generated/copy_strings.json
  - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  - examples/accrue_host/docs/verify01-v112-admin-paths.md
  - scripts/ci/verify_verify01_readme_contract.sh
autonomous: true
requirements: [AUX-06]
must_haves:
  truths:
    - "Merge-blocking Playwright + axe covers /connect, /connect/:id, /events plus one coupon and one promotion-code journey (53-CONTEXT D-03)"
    - "Playwright asserts SSOT strings via copyStrings.* after export allowlist (D-12)"
    - "verify01-v112-admin-paths.md maps blocking specs to AUX requirements (D-04)"
  artifacts:
    - path: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      provides: "VERIFY-01 axe coverage"
      contains: "scanAxe"
    - path: "examples/accrue_host/docs/verify01-v112-admin-paths.md"
      provides: "ADM-06 inventory extension"
      contains: "AUX-"
  key_links:
    - from: "53-CONTEXT.md"
      to: "D-01 D-03 D-04 D-12 D-13"
      via: "VERIFY scope"
      pattern: "context"
---

<threat_model>
**ASVS L1.** Playwright tests use **fixture-backed** auth only (`readFixture`, `reseedFixture`); **never** commit **`sk_live`**, webhook secrets, or real customer emails. **`copy_strings.json`** is generated operator copy — no PII. Do **not** weaken axe to **moderate** or below for merge-blocking scans — keep **serious** + **critical** filter per **Phase 50**.
</threat_model>

<objective>
Ship **AUX-06**: extend **`export_copy_strings`** allowlist + committed **`copy_strings.json`**; add **merge-blocking** **`verify01-admin-a11y.spec.js`** flows for **`/connect`**, **`/connect/:id`**, **`/events`**, **one coupon path**, **one promotion-code path**; extend **ADM-06** inventory doc with **AUX** mapping (**D-04**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-CONTEXT.md
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-UI-SPEC.md
@.planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-RESEARCH.md
@.planning/phases/50-copy-tokens-verify-gates/50-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright flows + allowlist + export JSON (single atomic slice)</name>
  <read_first>
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    examples/accrue_host/e2e/support/fixture.js
    accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
    accrue_admin/lib/accrue_admin/copy/connect.ex
    accrue_admin/lib/accrue_admin/copy/billing_event.ex
    .planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-CONTEXT.md
  </read_first>
  <files>
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
    examples/accrue_host/e2e/generated/copy_strings.json
  </files>
  <acceptance_criteria>
    - `rg -o "copyStrings\\.([a-z0-9_]+)" examples/accrue_host/e2e/verify01-admin-a11y.spec.js | sed 's/copyStrings\\.//' | sort -u | while read k; do rg -q "$k" accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex || exit 1; done` exits **0** (every **`copyStrings`** key is in **`@allowlist`**).
    - `rg -ni "connect" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` matches a **`test.describe`** line (e.g. contains **`connect`**).
    - `rg -c "^test\\(" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` is **≥ 7** (baseline **2** on **`main`** at phase planning time + **≥ 5** new **`test(`** blocks for Connect/events/coupon/promo/auxiliary coverage).
    - Each added test: **`test.skip`** for **`chromium-mobile`** / **`chromium-mobile-tagged`** when theme toggle is desktop-only (mirror lines **21–24**).
    - `rg "networkidle" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` returns **no** matches **OR** only matches outside lines touched by this phase (prefer **zero** total).
    - `rg "scanAxe" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` count **≥** number of **`test(`** blocks that navigate to **`/connect`**, **`/events`**, **`/coupons`**, or **`/promotion-codes`** (each such flow ends with axe).
    - `rg -o "copyStrings\\.[a-z0-9_]+" examples/accrue_host/e2e/verify01-admin-a11y.spec.js | sort -u | wc -l` returns integer **≥ 8** (aggregate growth for SSOT assertions).
  </acceptance_criteria>
  <action>
    **Order (mandatory):** (A) land **`verify01-admin-a11y.spec.js`** edits with **`copyStrings.<key>`** references matching **`AccrueAdmin.Copy`** 0-arity names from **plan 01**; (B) extend **`@allowlist`** with **every** extracted key + coupon/promo keys used; (C) run **`mix accrue_admin.export_copy_strings --out ../examples/accrue_host/e2e/generated/copy_strings.json`**; (D) run **`bash scripts/ci/accrue_host_verify_browser.sh`**.
    1. **Connect index:** after org scope selection, **`page.goto`** **`/billing/connect?org=...`** (same mount prefix as **`/billing/customers`** tests), **`waitForLiveView`**, assert **`copyStrings`** for a **connect_accounts_*** string **or** KPI region, **`scanAxe`**.
    2. **Connect detail:** open first row link **or** fixture-backed id; assert fee override CTA via **`copyStrings`**; **`scanAxe`**.
    3. **Events index:** **`/billing/events?org=...`**, **`copyStrings`** for **billing_events_*** empty or header, **`scanAxe`**.
    4. **Coupon minimal journey** + **5. Promotion-code minimal journey** per **53-CONTEXT D-03**.
    6. **Light + dark** for **Connect index** only if required to match **customers** axe stability pattern.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && bash scripts/ci/accrue_host_verify_browser.sh'</automated>
  </verify>
  <done>VERIFY-01 browser lane green.</done>
</task>

<task type="auto">
  <name>Task 2: ADM-06 inventory — paths + AUX mapping</name>
  <read_first>
    examples/accrue_host/docs/verify01-v112-admin-paths.md
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    .planning/phases/53-auxiliary-admin-connect-events-layout-verify/53-CONTEXT.md
  </read_first>
  <files>examples/accrue_host/docs/verify01-v112-admin-paths.md</files>
  <acceptance_criteria>
    - `rg -n "AUX-" examples/accrue_host/docs/verify01-v112-admin-paths.md` returns **≥ 4** lines.
    - File contains **`/connect`**, **`/events`**, **`/coupons`**, **`/promotion-codes`** as path fragments.
    - `rg -n "verify01-admin-a11y" examples/accrue_host/docs/verify01-v112-admin-paths.md` returns **≥ 1** line.
  </acceptance_criteria>
  <action>
    Append **## v1.13 auxiliary (Phase 53)** with path list and a **mapping table**: **`test.describe` title** (exact strings from **`verify01-admin-a11y.spec.js`**), **mounted path template**, **`AUX-03`..`AUX-06`** ids satisfied.
  </action>
  <verify>
    <automated>bash -lc 'test -f examples/accrue_host/docs/verify01-v112-admin-paths.md'</automated>
  </verify>
  <done>Inventory extended.</done>
</task>

<task type="auto">
  <name>Task 3: README VERIFY-01 contract script (only if README literals change)</name>
  <read_first>
    examples/accrue_host/README.md
    scripts/ci/verify_verify01_readme_contract.sh
  </read_first>
  <files>scripts/ci/verify_verify01_readme_contract.sh</files>
  <acceptance_criteria>
    - **If** **`examples/accrue_host/README.md`** was **not** edited: `git diff --name-only examples/accrue_host/README.md` is empty and **`verify_verify01_readme_contract.sh`** diff may be empty.
    - **If** README was edited to cite **new** spec filenames: script updated so `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0** unconditionally after task.
  </acceptance_criteria>
  <action>
    Only if README gained/changed **`e2e/verify01-*.spec.js`** references: extend **`verify_verify01_readme_contract.sh`** per **Phase 52** pattern. Otherwise **no-op** with explicit **skip** note in commit.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && bash scripts/ci/verify_verify01_readme_contract.sh'</automated>
  </verify>
  <done>README contract satisfied.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/accrue_host_verify_browser.sh` — exit **0** from repo root.
- `bash scripts/ci/verify_verify01_readme_contract.sh` — exit **0**.
- `cd accrue_admin && mix test` — exit **0**.
</verification>

<success_criteria>
- **AUX-06** satisfied: materially touched auxiliary mounted paths have **Playwright + axe** coverage and **inventory mapping** per **53-CONTEXT** and **53-UI-SPEC**.
</success_criteria>

---
phase: 54-core-admin-inventory-first-burn-down
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/guides/core-admin-parity.md
  - accrue_admin/mix.exs
  - accrue_admin/guides/admin_ui.md
  - accrue_admin/README.md
autonomous: true
requirements: [ADM-07]
must_haves:
  truths:
    - "core-admin-parity.md is listed in accrue_admin mix.exs extras"
    - "Matrix includes eleven core mounted surfaces from router.ex (excluding coupons, promotion-codes, connect, events)"
    - "VERIFY-01 lane column honestly marks merge-blocking vs planned Phase 55 where specs absent"
  artifacts:
    - path: "accrue_admin/guides/core-admin-parity.md"
      provides: "ADM-07 SSOT"
      contains: "Route"
    - path: "accrue_admin/mix.exs"
      provides: "Hexdocs discoverability"
      contains: "core-admin-parity"
  key_links:
    - from: "admin_ui.md"
      to: "core-admin-parity.md"
      via: "markdown relative link"
      pattern: "Guides cross-link"
---

<threat_model>
**ASVS L1.** Documentation-only plan. Do **not** embed **live Stripe IDs**, **customer emails**, **webhook signing secrets**, or **host production URLs** in **`core-admin-parity.md`**. Use **route templates** (`/invoices/:id`) and **module names** only.
</threat_model>

<objective>
Ship **ADM-07**: publish **`accrue_admin/guides/core-admin-parity.md`** as the canonical **core admin vs Copy / `ax-*` / VERIFY-01** matrix (**54-CONTEXT D-01–D-05, D-16–D-18**), register it in **ExDoc extras**, and link from **`guides/admin_ui.md`** + **`README.md`** for discoverability.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-core-admin-inventory-first-burn-down/54-CONTEXT.md
@.planning/phases/54-core-admin-inventory-first-burn-down/54-UI-SPEC.md
@.planning/phases/54-core-admin-inventory-first-burn-down/54-RESEARCH.md
@.planning/REQUIREMENTS.md
@accrue_admin/lib/accrue_admin/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author core-admin-parity.md (11-row matrix)</name>
  <read_first>
    .planning/phases/54-core-admin-inventory-first-burn-down/54-CONTEXT.md
    accrue_admin/lib/accrue_admin/router.ex
    accrue_admin/guides/theme-exceptions.md
  </read_first>
  <files>accrue_admin/guides/core-admin-parity.md</files>
  <acceptance_criteria>
    - `test -f accrue_admin/guides/core-admin-parity.md` exits **0**.
    - `rg -c '^\\| `/customers' accrue_admin/guides/core-admin-parity.md` returns a number **≥ 1** and `rg -c '^\\| `/invoices' accrue_admin/guides/core-admin-parity.md` returns **≥ 2** (index + show rows in the matrix).
    - `rg -n 'InvoicesLive|InvoiceLive|DashboardLive|CustomersLive|CustomerLive|SubscriptionsLive|SubscriptionLive|ChargesLive|ChargeLive|WebhooksLive|WebhookLive' accrue_admin/guides/core-admin-parity.md` returns **≥ 11** matches total across the file.
    - `rg -n 'VERIFY-01 lane|VERIFY-01' accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line.
    - `rg -n 'planned — Phase 55 \\(ADM-09\\)|planned — Phase 55' accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line (honest **planned** posture for not-yet-specced core flows).
    - `rg -n 'Copy posture|AccrueAdmin\\.Copy' accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line.
    - `rg -n 'ax-\\*|token' accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line (token discipline column or prose).
    - `rg -n 'Excluded' accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line (explicit exclusion section for v1.13 auxiliary routes — **D-18**).
  </acceptance_criteria>
  <action>
    Create **`accrue_admin/guides/core-admin-parity.md`** with:
    1. Title and intro stating this file is the **ADM-07** SSOT and links **54-CONTEXT** decisions **D-01–D-05**.
    2. A **Markdown table** with **minimum** columns: **Route (relative to mount)**, **LiveView module + action**, **`AccrueAdmin.Copy` posture**, **`ax-*` / token posture**, **Named VERIFY flow id** (use `—` until ADM-09), **`VERIFY-01 lane`**, **Severity** — per **54-CONTEXT D-02**.
    3. Insert **exactly 11** data rows for: `/` Dashboard `:index`; `/customers` Customers `:index`; `/customers/:id` Customer `:show`; `/subscriptions` Subscriptions `:index`; `/subscriptions/:id` Subscription `:show`; `/invoices` Invoices `:index`; `/invoices/:id` Invoice `:show`; `/charges` Charges `:index`; `/charges/:id` Charge `:show`; `/webhooks` Webhooks `:index`; `/webhooks/:id` Webhook `:show` — modules **`AccrueAdmin.Live.*`** names must match **`router.ex`**.
    4. Under **Excluded (v1.13 auxiliary)**, list **`/coupons*`**, **`/promotion-codes*`**, **`/connect*`**, **`/events`** as **out of core matrix** (**D-18**).
    5. Populate **Copy/token** cells with an **honest** maintainer audit (grep HEEx for raw English vs `Copy.` usage). Where audit is incomplete, use explicit **`needs audit`** with **P2** severity rather than guessing **`clean`**.
  </action>
  <verify>
    <automated>bash -lc 'test -f accrue_admin/guides/core-admin-parity.md && test "$(rg -c AccrueAdmin.Live accrue_admin/guides/core-admin-parity.md)" -ge 11'</automated>
  </verify>
  <done>ADM-07 matrix file exists with router-faithful rows.</done>
</task>

<task type="auto">
  <name>Task 2: Register guide in mix.exs ExDoc extras</name>
  <read_first>
    accrue_admin/mix.exs
    accrue_admin/guides/core-admin-parity.md
  </read_first>
  <files>accrue_admin/mix.exs</files>
  <acceptance_criteria>
    - `rg -n 'guides/core-admin-parity\\.md' accrue_admin/mix.exs` returns **≥ 1** line inside **`extras:`** list.
    - `rg -n 'core-admin-parity' accrue_admin/mix.exs` returns **≥ 2** lines (extras + **`groups_for_extras`** entry under **Guides** group).
  </acceptance_criteria>
  <action>
    Update **`accrue_admin/mix.exs`** `docs/0`:
    1. Append **`"guides/core-admin-parity.md"`** to **`extras:`** alongside **`README.md`** and **`guides/admin_ui.md`**.
    2. Add the same path to **`groups_for_extras`** under the **`Guides:`** list so ExDoc sidebar groups it (**54-CONTEXT D-01**).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "guides/core-admin-parity" accrue_admin/mix.exs'</automated>
  </verify>
  <done>Hex consumers see the parity guide in docs.</done>
</task>

<task type="auto">
  <name>Task 3: Discoverability links (admin_ui + README)</name>
  <read_first>
    accrue_admin/guides/admin_ui.md
    accrue_admin/README.md
  </read_first>
  <files>accrue_admin/guides/admin_ui.md
accrue_admin/README.md</files>
  <acceptance_criteria>
    - `rg -n 'core-admin-parity\\.md' accrue_admin/guides/admin_ui.md` returns **≥ 1** line.
    - `rg -n 'core-admin-parity|parity' accrue_admin/README.md` returns **≥ 1** line (policy-level pointer — **54-CONTEXT D-04**).
  </acceptance_criteria>
  <action>
    1. Add a short subsection to **`accrue_admin/guides/admin_ui.md`** (e.g. **`## Core operator surface parity (checklist)`**) with a relative link **`[core-admin-parity.md](core-admin-parity.md)`** describing **ADM-07**.
    2. Add **one bullet or sentence** under an existing appropriate heading in **`accrue_admin/README.md`** pointing maintainers to **`guides/core-admin-parity.md`** for the **v1.14** inventory.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "core-admin-parity" accrue_admin/guides/admin_ui.md accrue_admin/README.md'</automated>
  </verify>
  <done>Parity guide linked from primary entry docs.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix compile` — **0** exit (docs/mix edits must compile).
- `rg -n 'guides/core-admin-parity' accrue_admin/mix.exs` — **≥ 1** match.
- `test -f accrue_admin/guides/core-admin-parity.md` — **0** exit.
</verification>

<success_criteria>
- **ADM-07** satisfied: published, **Hex-listed** guide maps **all 11** core surfaces vs Copy, tokens, VERIFY posture (**REQUIREMENTS.md**).
- No auxiliary v1.13 routes appear as **core** matrix rows.
</success_criteria>

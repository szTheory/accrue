---
phase: 54-core-admin-inventory-first-burn-down
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/copy/invoice.ex
  - accrue_admin/lib/accrue_admin/live/invoices_live.ex
  - accrue_admin/lib/accrue_admin/live/invoice_live.ex
  - accrue_admin/test/accrue_admin/live/invoices_live_test.exs
  - accrue_admin/test/accrue_admin/live/invoice_live_test.exs
  - accrue_admin/guides/core-admin-parity.md
  - accrue_admin/guides/theme-exceptions.md
autonomous: true
requirements: [ADM-08]
must_haves:
  truths:
    - "Invoice index + detail operator chrome uses AccrueAdmin.Copy (or copy/invoice.ex defdelegates) for migrated P0 strings"
    - "ExUnit invoice LiveView tests assert Copy functions for migrated literals — no duplicate English for those strings"
    - "core-admin-parity.md invoice rows updated to reflect post-burn-down Copy/token posture"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/live/invoices_live.ex"
      provides: "ADM-08 index closure"
      contains: "Copy."
    - path: "accrue_admin/lib/accrue_admin/live/invoice_live.ex"
      provides: "ADM-08 detail closure"
      contains: "Copy."
  key_links:
    - from: "core-admin-parity.md"
      to: "invoice LiveViews"
      via: "matrix severity + posture cells"
      pattern: "inventory truth"
---

<threat_model>
**ASVS L1.** UI copy changes must **not** log **invoice PDF binaries**, **full card numbers**, or **step-up secrets**. When migrating strings, preserve **redaction** behavior in **`InvoiceLive`** tests (**`refute html =~ \"123 Private Lane\"`**) — do not replace guarded assertions with Copy that re-introduces PII. **Step-up** copy remains **non-enumerable** for secrets.
</threat_model>

<objective>
Close **ADM-08** **P0** gaps on the **locked invoice anchor** (`/invoices`, `/invoices/:id`) per **54-CONTEXT D-06–D-15**: route operator-visible literals through **`AccrueAdmin.Copy`**, align touched **`ax-*` / token** markup, update **materially touched** **ExUnit** tests to track Copy, refresh **`core-admin-parity.md`** invoice rows, and register **real** token exceptions only when required.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-core-admin-inventory-first-burn-down/54-CONTEXT.md
@.planning/phases/54-core-admin-inventory-first-burn-down/54-UI-SPEC.md
@.planning/phases/50-copy-tokens-verify-gates/50-CONTEXT.md
@accrue_admin/lib/accrue_admin/live/invoices_live.ex
@accrue_admin/lib/accrue_admin/live/invoice_live.ex
@accrue_admin/lib/accrue_admin/copy.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Copy surface for invoice chrome</name>
  <read_first>
    accrue_admin/lib/accrue_admin/copy.ex
    accrue_admin/lib/accrue_admin/live/invoices_live.ex
    accrue_admin/lib/accrue_admin/live/invoice_live.ex
    .planning/phases/54-core-admin-inventory-first-burn-down/54-CONTEXT.md
  </read_first>
  <files>accrue_admin/lib/accrue_admin/copy/invoice.ex
accrue_admin/lib/accrue_admin/copy.ex</files>
  <acceptance_criteria>
    - **Either** `test -f accrue_admin/lib/accrue_admin/copy/invoice.ex` exits **0** **or** `rg -n 'def invoices_' accrue_admin/lib/accrue_admin/copy.ex` returns **≥ 5** new function lines after change — **one** consolidation strategy only (submodule **preferred** if &gt; ~15 new strings).
    - `rg -n 'defdelegate' accrue_admin/lib/accrue_admin/copy.ex` matches a line containing **`Invoice`** when submodule path used.
    - `mix format --check-formatted` passes under **`accrue_admin/`** after edits (run `mix format` if needed).
  </acceptance_criteria>
  <action>
    1. Inventory **operator-visible English** in **`InvoicesLive.render/1`** and **`InvoiceLive`** `~H"""` blocks (breadcrumbs, headings, KPI labels/meta, table column headers, filter labels, button labels, section titles **excluding** dynamic customer/money values).
    2. Add **`AccrueAdmin.Copy.Invoice`** (new file) with **one function per string** (or small grouped helpers), **`@moduledoc false`**, and **`defdelegate`** from **`AccrueAdmin.Copy`** for public entrypoints (**54-CONTEXT** + **Phase 50 D-07**).
    3. Keep **existing** `Copy.invoices_index_empty_*` / invoice warnings **working** — either **move** to **`Copy.Invoice`** with **delegate** or **leave** if already optimal; **avoid duplicate** definitions.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile'</automated>
  </verify>
  <done>Copy API covers invoice P0 literals.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate InvoicesLive HEEx + assigns to Copy</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/invoices_live.ex
    accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/invoices_live.ex</files>
  <acceptance_criteria>
    - `rg -n 'Collections and invoice review' accrue_admin/lib/accrue_admin/live/invoices_live.ex` returns **0** lines (string must not remain raw in HEEx after migration).
    - `rg -n 'assign\\(:page_title,' accrue_admin/lib/accrue_admin/live/invoices_live.ex` returns **≥ 1** line containing **`Copy.`** in the same line or the assign uses a **`Copy.`** call on the continuation line in the same `assign_shell` function block.
    - `cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs` exits **0**.
  </acceptance_criteria>
  <action>
    Replace **P0** literals identified in Task 1 in **`InvoicesLive`** with **`Copy...()`** calls (HEEx `<% %>` attribute interpolation where needed). Update **`assign(:page_title, ...)`** to Copy-backed title. Keep **empty state** on existing **`Copy.invoices_index_empty_*`** if still correct.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs'</automated>
  </verify>
  <done>Invoice index uses Copy for migrated chrome.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate InvoiceLive HEEx + flash strings</name>
  <read_first>
    accrue_admin/lib/accrue_admin/live/invoice_live.ex
    accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>accrue_admin/lib/accrue_admin/live/invoice_live.ex</files>
  <acceptance_criteria>
    - `cd accrue_admin && mix test test/accrue_admin/live/invoice_live_test.exs` exits **0**.
    - `rg -n 'Open PDF|Tax &' accrue_admin/lib/accrue_admin/live/invoice_live.ex` returns **0** lines **OR** every match is inside **`<%#` / comment** only — **prefer 0** raw operator English matches for strings migrated in **54-CONTEXT D-10**.
  </acceptance_criteria>
  <action>
    Migrate **operator-visible** literals in **`InvoiceLive`** `~H"""` sections to **`Copy.Invoice.*`** (or **`Copy.*`** delegates) per Task 1 inventory — **including** button labels and section headers currently asserted in **`invoice_live_test.exs`**.
    **Do not** weaken **PII redaction** tests (**`refute html =~ \"123 Private Lane\"`**, **`refute html =~ \"Invoice payload\"`**).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/invoice_live_test.exs'</automated>
  </verify>
  <done>Invoice detail uses Copy for migrated chrome.</done>
</task>

<task type="auto">
  <name>Task 4: Update ExUnit tests + parity matrix rows</name>
  <read_first>
    accrue_admin/test/accrue_admin/live/invoices_live_test.exs
    accrue_admin/test/accrue_admin/live/invoice_live_test.exs
    accrue_admin/guides/core-admin-parity.md
  </read_first>
  <files>accrue_admin/test/accrue_admin/live/invoices_live_test.exs
accrue_admin/test/accrue_admin/live/invoice_live_test.exs
accrue_admin/guides/core-admin-parity.md</files>
  <acceptance_criteria>
    - `rg -n 'Copy\\.Invoice\\.|Copy\\.invoices_|Copy\\.invoice_' accrue_admin/test/accrue_admin/live/invoices_live_test.exs` returns **≥ 1** line (tests reference **`Copy`** for at least one migrated invoice index assertion).
    - `rg -n 'Copy\\.Invoice\\.|Copy\\.invoice_' accrue_admin/test/accrue_admin/live/invoice_live_test.exs` returns **≥ 3** lines (detail tests track **`Copy`** for migrated chrome).
    - `rg -n 'Collections and invoice review' accrue_admin/test/accrue_admin/live/invoices_live_test.exs` returns **0** lines **unless** the substring appears only inside a **comment** line.
    - `rg -n '/invoices' accrue_admin/guides/core-admin-parity.md` returns **≥ 2** lines in **table rows** (index + show).
    - `rg -n 'clean|P0 cleared|ADM-08' accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line updating **invoice** row posture after burn-down.
  </acceptance_criteria>
  <action>
    1. Update **`invoices_live_test.exs`** and **`invoice_live_test.exs`** so assertions reference **`Copy.Invoice.*`** / **`Copy.*`** for all strings migrated in Tasks 2–3 (**54-CONTEXT D-10**).
    2. Update **`core-admin-parity.md`** **`/invoices`** and **`/invoices/:id`** rows: **`AccrueAdmin.Copy` posture** → honest **`clean`** (or **`clean — ADM-08`**) if P0 closed; **token** column updated if exceptions added; **severity** lowered where appropriate.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs test/accrue_admin/live/invoice_live_test.exs'</automated>
  </verify>
  <done>Tests + inventory reflect burn-down.</done>
</task>

<task type="auto">
  <name>Task 5: Token exceptions register (only if honest deviation remains)</name>
  <read_first>
    accrue_admin/guides/theme-exceptions.md
    accrue_admin/lib/accrue_admin/live/invoices_live.ex
    accrue_admin/lib/accrue_admin/live/invoice_live.ex
  </read_first>
  <files>accrue_admin/guides/theme-exceptions.md</files>
  <acceptance_criteria>
    - **If no new exception:** `rg -n 'Phase 54|ADM-08' accrue_admin/guides/theme-exceptions.md` may return **0** lines — task completes with **no file change**; document **“N/A — no new rows”** in **`54-SUMMARY.md`** later.
    - **If new exception:** `rg -n 'ADM-08|Phase 54' accrue_admin/guides/theme-exceptions.md` returns **≥ 1** line with **`phase_ref`**-style reference.
  </acceptance_criteria>
  <action>
    If Tasks 2–4 introduce **intentional** non-token styling that **cannot** be resolved without product redesign, add **one** **`theme-exceptions.md`** row (**slug**, **location**, **deviation**, **rationale**, **future_token**, **status**, **`phase_ref: Phase 54`**). Otherwise **skip** (**54-CONTEXT D-10** prefers fixes).
  </action>
  <verify>
    <automated>bash -lc 'cd accrue_admin && mix compile'</automated>
  </verify>
  <done>Exception register stays honest.</done>
</task>

</tasks>

<verification>
- `cd accrue_admin && mix test test/accrue_admin/live/invoices_live_test.exs test/accrue_admin/live/invoice_live_test.exs` — **0** exit.
- `cd accrue_admin && mix compile` — **0** exit.
- `rg -n 'Collections and invoice review' accrue_admin/lib/accrue_admin/live/invoices_live.ex` — **0** lines.
</verification>

<success_criteria>
- **ADM-08** satisfied: **P0** gaps on **invoice list + detail** closed per **REQUIREMENTS.md** and **54-CONTEXT D-10–D-14**.
- **`core-admin-parity.md`** invoice rows truthfully show post-**ADM-08** posture.
</success_criteria>

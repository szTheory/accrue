---
phase: 55-core-admin-verify-theme-copy-ci
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/accrue_host_seed_e2e.exs
  - scripts/ci/verify_e2e_fixture_jq.sh
  - examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
  - examples/accrue_host/e2e/generated/copy_strings.json
autonomous: true
requirements: [ADM-09, ADM-11]
must_haves:
  truths:
    - "Merge-blocking Playwright + axe covers ADM-08 invoice index and detail with canonical flow ids core-admin-invoices-index and core-admin-invoices-detail (55-CONTEXT D-01, D-03, D-04)"
    - "Every copyStrings key used in new/edited VERIFY lines is on export_copy_strings @allowlist; JSON regenerated (55-CONTEXT D-12–D-14)"
    - "Fixture exposes invoice_id for deterministic /billing/invoices/:id navigation (55-RESEARCH seed gap)"
  artifacts:
    - path: "examples/accrue_host/e2e/verify01-admin-a11y.spec.js"
      provides: "VERIFY-01 invoice anchor"
      contains: "core-admin-invoices-index"
    - path: "examples/accrue_host/e2e/generated/copy_strings.json"
      provides: "Exported Copy strings"
      contains: "invoices_index_headline"
  key_links:
    - from: "55-CONTEXT.md"
      to: "D-01 D-03 D-07 D-12"
      via: "VERIFY + copy"
      pattern: "context"
---

<threat_model>
**ASVS L1.** Playwright continues **fixture-backed** auth (`readFixture`, `reseedFixture`, `login`) — never embed **`sk_live`**, real customer PII, or webhook signing secrets in specs or JSON. **`copy_strings.json`** holds **operator UI strings** only. **axe** merge-blocking filter stays **serious** + **critical** only — do not widen to moderate/minor to “green” CI. **Forbidden:** `try/catch` around primary PDF / navigation expectations that masks failures (**55-CONTEXT D-09**).
</threat_model>

<objective>
Add **deterministic host seed + fixture `invoice_id`**, extend **`verify01-admin-a11y.spec.js`** with **tagged** merge-blocking **Playwright + axe** flows for **`/billing/invoices`** and **`/billing/invoices/:id`**, assert **PDF-related controls** are **visible + enabled** per **D-07**, extend **`export_copy_strings`** allowlist, regenerate **`copy_strings.json`**, and prove **`bash scripts/ci/accrue_host_verify_browser.sh`** is green (**ADM-09**, **ADM-11**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-core-admin-verify-theme-copy-ci/55-CONTEXT.md
@.planning/phases/55-core-admin-verify-theme-copy-ci/55-UI-SPEC.md
@.planning/phases/55-core-admin-verify-theme-copy-ci/55-RESEARCH.md
@.planning/phases/55-core-admin-verify-theme-copy-ci/55-PATTERNS.md
@.planning/phases/54-core-admin-inventory-first-burn-down/54-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E seed — minimal Accrue.Billing.Invoice + fixture invoice_id</name>
  <read_first>
    scripts/ci/accrue_host_seed_e2e.exs
    accrue/lib/accrue/billing/invoice.ex
    accrue/test/support/billing_case.ex
    scripts/ci/verify_e2e_fixture_jq.sh
  </read_first>
  <files>
    scripts/ci/accrue_host_seed_e2e.exs
    scripts/ci/verify_e2e_fixture_jq.sh
  </files>
  <acceptance_criteria>
    - `rg -n "invoice_id" scripts/ci/accrue_host_seed_e2e.exs` returns **≥ 1** line inside the **`fixture = %\{}`** map (string key **`"invoice_id"`** emitted to JSON).
    - `rg -n "defp insert_fixture" scripts/ci/accrue_host_seed_e2e.exs` shows a new **`insert_fixture_invoice!`** (or equivalently named) helper **or** inline `Repo.insert!` for **`Accrue.Billing.Invoice`** scoped to the **existing fixture `customer`** / **`subscription`** rows already created in `run!/1`.
    - `rg -n "invoice" scripts/ci/accrue_host_seed_e2e.exs` includes **`cleanup_fixture_footprint!`** logic that deletes fixture invoices (e.g. match **`processor_id`** prefix like **`inv_host_browser_%`** or documented stable token — must be **idempotent** across reruns).
    - `scripts/ci/verify_e2e_fixture_jq.sh` **jq** program includes **`(.invoice_id | type) == "string"`** conjunct so **`accrue_host_verify_browser.sh`** keeps passing its **`verify_e2e_fixture_jq.sh "$fixture_file"`** step after seed changes.
    - `MIX_ENV=test mix run scripts/ci/accrue_host_seed_e2e.exs` with **`ACCRUE_HOST_E2E_FIXTURE`** pointing at a temp file completes without crash (manual smoke acceptable in CI via existing script).
  </acceptance_criteria>
  <action>
    1. Insert **one** **`Accrue.Billing.Invoice`** row for the seeded **`customer`** (and **`subscription`** if the schema requires **`subscription_id`**) using **`processor: "fake"`** and a unique **`processor_id`** such as **`inv_host_browser_replay`** (adjust if collision with cleanup — pick **one** stable id documented in seed comments).
    2. Add **`invoice_id: invoice.id`** (binary UUID string form from Ecto) to the **`fixture`** map written by **`write_fixture!/2`**.
    3. Extend **`cleanup_fixture_footprint!`** to remove invoices created for this fixture using the same **`processor` / `processor_id`** contract as other fixture tables so parallel CI does not accumulate rows.
    4. Update **`verify_e2e_fixture_jq.sh`** so **`invoice_id`** is required and typed as **string** (UUID).
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/examples/accrue_host" && tmp=$(mktemp) && ACCRUE_HOST_E2E_FIXTURE="$tmp" MIX_ENV=test mix run ../../scripts/ci/accrue_host_seed_e2e.exs >/dev/null && bash ../../scripts/ci/verify_e2e_fixture_jq.sh "$tmp" && jq -e ".invoice_id | length > 10" "$tmp" >/dev/null'</automated>
  </verify>
  <done>Fixture contract includes invoice_id.</done>
</task>

<task type="auto">
  <name>Task 2: verify01 Playwright + allowlist + copy_strings.json</name>
  <read_first>
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    examples/accrue_host/e2e/support/fixture.js
    accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
    accrue_admin/lib/accrue_admin/copy.ex
    .planning/phases/55-core-admin-verify-theme-copy-ci/55-UI-SPEC.md
  </read_first>
  <files>
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
    examples/accrue_host/e2e/generated/copy_strings.json
  </files>
  <acceptance_criteria>
    - `rg "networkidle" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` returns **no** matches.
    - `rg -n "/billing/invoices" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` returns **≥ 2** lines (index + detail navigations).
    - `rg "core-admin-invoices-index" examples/accrue_host/e2e/verify01-admin-a11y.spec.js` matches **≥ 1** line and **`core-admin-invoices-detail`** matches **≥ 1** line (prefer **`tag:`** in **`test.describe`** options per **Playwright 1.57**).
    - **Index flow:** after org scope selection, `page.goto` **`/billing/invoices?org=${encodeURIComponent(fixture.admin_org_alpha_slug)}`**, **`waitForLiveView`**, assert **`copyStrings.invoices_index_headline`** (or **`invoices_page_title_index`**) visibility, run **light + dark** **`scanAxe`** with the same **theme button** pattern as the **customers** test (`data-theme-target="light"` / `"dark"`).
    - **Detail flow:** `page.goto` **`/billing/invoices/${fixture.invoice_id}?org=...`**, **`waitForLiveView`**, assert **`copyStrings.invoice_detail_eyebrow`** visible, assert **PDF primary control** **`getByRole('link', { name: copyStrings.invoice_open_pdf_button })` OR `getByRole('button', { name: ... })`** matches actual **`InvoiceLive`** markup — whichever role is correct after **`read_first` inspection** — with **`.toBeEnabled()`** after visibility.
    - `rg -o "copyStrings\\.([a-z0-9_]+)" examples/accrue_host/e2e/verify01-admin-a11y.spec.js | sed "s/copyStrings\\.//" | sort -u | while read k; do rg -q "$k" accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex || exit 1; done` exits **0** from repo root.
    - `jq -e 'type == "object"' examples/accrue_host/e2e/generated/copy_strings.json` exits **0** (valid JSON object).
    - `python3 -c "import pathlib; b=pathlib.Path('examples/accrue_host/e2e/generated/copy_strings.json').read_bytes(); assert b.endswith(b'\\n')"` exits **0** (trailing newline for stable diffs).
  </acceptance_criteria>
  <action>
    **Mandatory order:** (A) land **`verify01-admin-a11y.spec.js`** with **two** top-level **`test.describe`** blocks — **`core-admin-invoices-index`** and **`core-admin-invoices-detail`** — each containing desktop-only skips mirroring lines **21–24** of the existing customers test; (B) add **`copyStrings.<function_name>`** assertions only for **0-arity** `AccrueAdmin.Copy` delegates listed in **`copy.ex`**; (C) extend **`@allowlist`** with **every** new key; (D) run **`cd accrue_admin && mix accrue_admin.export_copy_strings --out ../examples/accrue_host/e2e/generated/copy_strings.json`**; (E) run **`bash scripts/ci/accrue_host_verify_browser.sh`** from repo root.
    **PDF tier:** implement **D-07** (visible + enabled). Do **not** add **`waitForEvent('download')`** or **`popup`** unless you have verified **determinism** on Fake host — if omitted, add a **short code comment** citing **D-08** optional layer (no `try/catch` pass).
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && bash scripts/ci/accrue_host_verify_browser.sh'</automated>
  </verify>
  <done>ADM-09 + ADM-11 slice green on browser CI script.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/accrue_host_verify_browser.sh` — exit **0** from repo root.
- `bash scripts/ci/verify_e2e_fixture_jq.sh` on a fresh seed temp fixture — exit **0** with **`invoice_id`** present.
- `cd accrue_admin && mix test` — exit **0** (export task / Copy unchanged in behavior aside from allowlist).
</verification>

<success_criteria>
- **ADM-09:** Merge-blocking VERIFY-01 covers **invoice index + detail** with canonical ids **`core-admin-invoices-index`** and **`core-admin-invoices-detail`**.
- **ADM-11:** **`export_copy_strings`** allowlist and committed **`copy_strings.json`** include **only** the keys required for those specs; ordering stable.
</success_criteria>

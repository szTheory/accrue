---
phase: 55-core-admin-verify-theme-copy-ci
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - accrue_admin/guides/core-admin-parity.md
  - examples/accrue_host/docs/verify01-v112-admin-paths.md
  - accrue_admin/guides/admin_ui.md
  - accrue_admin/guides/theme-exceptions.md
  - scripts/ci/verify_core_admin_invoice_verify_ids.sh
autonomous: true
requirements: [ADM-09, ADM-10, ADM-11]
must_haves:
  truths:
    - "core-admin-parity.md invoice rows show Named VERIFY flow ids core-admin-invoices-index and core-admin-invoices-detail and VERIFY-01 lane merge-blocking in the same change-set as merge-blocking specs (already landed in plan 01) — this plan verifies doc parity + traceability (55-CONTEXT D-04)"
    - "admin_ui.md links to package theme-exceptions.md — no stale .planning/phases/26-theme-exceptions path (55-CONTEXT D-19)"
    - "theme-exceptions.md honestly records deviations or an explicit Phase 55 audit note when none (55-CONTEXT D-17, D-18)"
  artifacts:
    - path: "accrue_admin/guides/core-admin-parity.md"
      provides: "ADM-07 matrix"
      contains: "core-admin-invoices-index"
    - path: "examples/accrue_host/docs/verify01-v112-admin-paths.md"
      provides: "VERIFY path map"
      contains: "invoices"
  key_links:
    - from: "55-UI-SPEC.md"
      to: "parity + registry"
      pattern: "spec"
---

<threat_model>
**ASVS L1.** Doc-only plan: **no** secrets in markdown. **Drift script** must be **read-only** (`grep`/`rg`/`jq` on tracked files) — no network, no credential access. Do not instruct contributors to disable VERIFY gates.
</threat_model>

<objective>
Sync **`core-admin-parity.md`** invoice matrix with shipped VERIFY ids, extend **`verify01-v112-admin-paths.md`** for **ADM-09** traceability, fix **`admin_ui.md`** contributor link to **`theme-exceptions.md`**, keep **`theme-exceptions.md`** honest, and add an optional **CI drift guard** so matrix **Named VERIFY flow ids** for merge-blocking invoice rows cannot disappear from **`verify01-admin-a11y.spec.js`** without failing CI (**55-CONTEXT D-05**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-core-admin-verify-theme-copy-ci/55-CONTEXT.md
@.planning/phases/55-core-admin-verify-theme-copy-ci/55-RESEARCH.md
@examples/accrue_host/e2e/verify01-admin-a11y.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity matrix + VERIFY path map</name>
  <read_first>
    accrue_admin/guides/core-admin-parity.md
    examples/accrue_host/docs/verify01-v112-admin-paths.md
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
  </read_first>
  <files>
    accrue_admin/guides/core-admin-parity.md
    examples/accrue_host/docs/verify01-v112-admin-paths.md
  </files>
  <acceptance_criteria>
    - `rg -nF "\`/invoices\`" accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line and the **same file** contains **`core-admin-invoices-index`** on a line that also contains **`/invoices`** (table row coherence — use `rg -n "invoices" accrue_admin/guides/core-admin-parity.md` and confirm **`core-admin-invoices-index`** appears).
    - `rg -nF "\`/invoices/:id\`" accrue_admin/guides/core-admin-parity.md` returns **≥ 1** line and **`core-admin-invoices-detail`** appears in **`accrue_admin/guides/core-admin-parity.md`**.
    - `rg "merge-blocking" accrue_admin/guides/core-admin-parity.md` matches **≥ 1** line in the **`/invoices`** / **`/invoices/:id`** table region (human spot: both invoice rows’ **`VERIFY-01 lane`** cells must read **`merge-blocking`**, not **`planned — Phase 55 (ADM-09)`**).
    - `examples/accrue_host/docs/verify01-v112-admin-paths.md` contains a **`##`** heading that includes **`Phase 55`** or **`v1.14`** and lists **`/billing/invoices?org=<slug>`** and **`/billing/invoices/:id?org=<slug>`** as path fragments.
    - The same doc file contains a **markdown table** row mapping **`test.describe` titles** from **`verify01-admin-a11y.spec.js`** that include **`invoice`** (case-insensitive) to **`ADM-09`** (literal substring **`ADM-09`** appears in the table region — `rg -n "ADM-09" examples/accrue_host/docs/verify01-v112-admin-paths.md` returns **≥ 1** line).
  </acceptance_criteria>
  <action>
    1. Update the **`/invoices`** and **`/invoices/:id`** rows: **`Named VERIFY flow id`** → **`core-admin-invoices-index`** / **`core-admin-invoices-detail`**; **`VERIFY-01 lane`** → **`merge-blocking`** (must match **plan 01** shipped tests).
    2. Append a **Phase 55 — ADM-08 invoice anchor** subsection to **`verify01-v112-admin-paths.md`** mirroring the **Phase 53** table pattern: exact **`test.describe` strings**, path templates, **`ADM-09`** column.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && test -f accrue_admin/guides/core-admin-parity.md && test -f examples/accrue_host/docs/verify01-v112-admin-paths.md'</automated>
  </verify>
  <done>Parity + path map aligned.</done>
</task>

<task type="auto">
  <name>Task 2: admin_ui.md + theme-exceptions.md hygiene</name>
  <read_first>
    accrue_admin/guides/admin_ui.md
    accrue_admin/guides/theme-exceptions.md
    .planning/phases/55-core-admin-verify-theme-copy-ci/55-CONTEXT.md
  </read_first>
  <files>
    accrue_admin/guides/admin_ui.md
    accrue_admin/guides/theme-exceptions.md
  </files>
  <acceptance_criteria>
    - `rg "\.planning/phases/26-hierarchy-and-pattern-alignment/26-theme-exceptions" accrue_admin/guides/admin_ui.md` returns **no** matches.
    - `rg -F "guides/theme-exceptions.md" accrue_admin/guides/admin_ui.md` returns **≥ 1** line (relative link acceptable: **`theme-exceptions.md`** or **`./theme-exceptions.md`** inside **`guides/`**).
    - `rg -n "Phase 55" accrue_admin/guides/theme-exceptions.md` returns **≥ 1** line (required: add **`## Phase 55` reviewer note** — *no new token exceptions for invoice VERIFY wiring* **or** document new slugged rows with **`phase_ref`** containing **`55`**).
  </acceptance_criteria>
  <action>
    Replace stale **`.planning/...26-theme-exceptions.md`** hyperlink targets with **`theme-exceptions.md`** per **D-19**. Audit **`InvoiceLive` / `InvoicesLive`** touched in plan **01** for intentional non-token styling; either add a **register row** (**slug, location, deviation, rationale, future_token, status, phase_ref**) or add the **explicit “no new exceptions”** reviewer note under **`## Register`** (same style as **Phase 53** note). **Always** add a **`## Phase 55`** (or **`### Phase 55`**) heading so the grep acceptance passes.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && rg -q "Phase 55" accrue_admin/guides/theme-exceptions.md'</automated>
  </verify>
  <done>ADM-10 SSOT links + register honest.</done>
</task>

<task type="auto">
  <name>Task 3: Optional matrix↔spec drift guard (D-05)</name>
  <read_first>
    accrue_admin/guides/core-admin-parity.md
    examples/accrue_host/e2e/verify01-admin-a11y.spec.js
    .github/workflows/ci.yml
  </read_first>
  <files>
    scripts/ci/verify_core_admin_invoice_verify_ids.sh
  </files>
  <acceptance_criteria>
    - File **`scripts/ci/verify_core_admin_invoice_verify_ids.sh`** exists, is **executable** (`chmod +x` in git), starts with **`#!/usr/bin/env bash`** and **`set -euo pipefail`**.
    - Running **`bash scripts/ci/verify_core_admin_invoice_verify_ids.sh`** from repo root exits **0** when **`core-admin-invoices-index`** and **`core-admin-invoices-detail`** appear in **`examples/accrue_host/e2e/verify01-admin-a11y.spec.js`**.
    - **If** root **`.github/workflows/ci.yml`** (or the job file that already runs **`accrue_host_verify_browser.sh`**) is edited: the workflow contains **either** a new step **`bash scripts/ci/verify_core_admin_invoice_verify_ids.sh`** **or** a code comment **`# Phase 55: drift guard deferred`** with a **TODO** linking **55-CONTEXT D-05** — pick **one**; acceptance requires **script exists + passes** unconditionally.
  </acceptance_criteria>
  <action>
    Implement a **read-only** bash script that fails if either canonical id string is missing from **`verify01-admin-a11y.spec.js`** (simple **`grep -F`** is sufficient for this phase). Optionally invoke it from the **same CI job** as browser VERIFY **after** specs are present — if wiring is high-churn, **ship script only** and document invocation in **`examples/accrue_host/README.md`** under VERIFY-01 (grep README for **`verify01`** section before editing).
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && bash scripts/ci/verify_core_admin_invoice_verify_ids.sh'</automated>
  </verify>
  <done>Drift guard available.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_core_admin_invoice_verify_ids.sh` — exit **0** (after creation).
- `rg "26-theme-exceptions" accrue_admin/guides/admin_ui.md` — **no** matches.
- Spot-check: invoice rows in **`core-admin-parity.md`** show **`merge-blocking`** for VERIFY lane.
</verification>

<success_criteria>
- **ADM-09:** Parity matrix + host VERIFY doc reflect **named flow ids** and **merge-blocking** lane for invoices; optional **D-05** script prevents silent spec↔matrix drift.
- **ADM-10:** **`admin_ui.md`** points consumers at **`guides/theme-exceptions.md`**; register contains **Phase 55** honesty note **or** new slugged rows.
- **ADM-11:** No change required if plan **01** already satisfied allowlist — this plan must **not** regenerate **`copy_strings.json`** without a corresponding **Copy** / spec delta.
</success_criteria>

---
phase: 56-billing-stripe-depth-telemetry-truth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/lib/accrue/billing/payment_method_actions.ex
  - accrue/lib/accrue/billing.ex
  - accrue/test/accrue/billing/payment_method_list_test.exs
autonomous: true
requirements: [BIL-01]
must_haves:
  truths:
    - "`Accrue.Billing.list_payment_methods/2` and `list_payment_methods!/2` delegate through `span_billing(:payment_method, :list, …)` to `PaymentMethodActions` (56-CONTEXT D-01, D-03, D-14)"
    - "`Processor.list_payment_methods/2` is invoked with `%{customer: customer.processor_id}` plus optional validated list filters without PROC-08 / new processor callbacks"
    - "Fake-backed ExUnit proves at least one attached PM appears in list results (56-CONTEXT D-05)"
  artifacts:
    - path: "accrue/lib/accrue/billing.ex"
      provides: "Public list PM API + telemetry span"
      contains: "payment_method, :list"
    - path: "accrue/lib/accrue/billing/payment_method_actions.ex"
      provides: "List implementation"
      contains: "list_payment_methods"
    - path: "accrue/test/accrue/billing/payment_method_list_test.exs"
      provides: "BIL-01 regression"
      contains: "list_payment_methods"
  key_links:
    - from: "56-CONTEXT.md"
      to: "D-01 D-02 D-03 D-05 D-14 D-18"
      via: "Billing list PM"
      pattern: "context"
---

<threat_model>
**ASVS L1.** Read-only call path must not introduce **high-cardinality** or **PII-heavy** telemetry metadata beyond existing `billing_metadata/4` (`customer_id`, `processor`, `operation`). Do **not** `Logger.info/1` full payment method payloads or card numbers. Tests use **`Accrue.Processor.Fake`** only — no **`sk_live`** strings, no real Stripe objects in committed fixtures. **`list_payment_methods!`** must raise only on **`{:error, _}`** from the processor using the same exception style as sibling `PaymentMethodActions` bang helpers.
</threat_model>

<objective>
Implement **BIL-01**: public **`Accrue.Billing.list_payment_methods/2`** and **`list_payment_methods!/2`** with **`span_billing(:payment_method, :list, …)`**, **`PaymentMethodActions`** implementation calling existing **`Processor.list_payment_methods/2`**, optional **`NimbleOptions`** for Stripe list filters (**56-CONTEXT D-02**), and **Fake-backed** **`payment_method_list_test.exs`** proving the happy path.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-billing-stripe-depth-telemetry-truth/56-CONTEXT.md
@.planning/phases/56-billing-stripe-depth-telemetry-truth/56-RESEARCH.md
@.planning/phases/56-billing-stripe-depth-telemetry-truth/56-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaymentMethodActions — list + options schema</name>
  <read_first>
    accrue/lib/accrue/billing/payment_method_actions.ex
    accrue/lib/accrue/processor.ex
    accrue/lib/accrue/processor/stripe.ex
    accrue/lib/accrue/processor/fake.ex
    accrue/lib/accrue/billing/meter_event_actions.ex
  </read_first>
  <files>
    accrue/lib/accrue/billing/payment_method_actions.ex
  </files>
  <acceptance_criteria>
    - `rg -n "def list_payment_methods" accrue/lib/accrue/billing/payment_method_actions.ex` returns **≥ 2** lines (non-bang and bang).
    - `rg -n "list_payment_methods" accrue/lib/accrue/billing/payment_method_actions.ex` includes **`Processor.__impl__().list_payment_methods`** (or **`Processor\.__impl__`** with call to **`list_payment_methods`**).
    - `rg -n "processor_id" accrue/lib/accrue/billing/payment_method_actions.ex` shows **`customer.processor_id`** passed into the processor params map for **`%Accrue.Billing.Customer{}`** input.
    - If **`NimbleOptions`** is used: **`@list_payment_methods_schema`** (or equivalent name) is **`validate!`**-d in the **`/2`** function before the processor call; schema documents **only** supported keys (at minimum allow **`[]`** — empty opts must remain valid).
    - `mix compile` in **`accrue`** exits **0**.
  </acceptance_criteria>
  <action>
    Add **`list_payment_methods(%Customer{} = customer, opts \\\\ [])`** returning **`Processor.list_payment_methods(%{customer: customer.processor_id} merged_with_optional_filters, sanitized_opts)`** where **`merged_with_optional_filters`** follows Stripe **`PaymentMethod.list`** semantics (read **`LatticeStripe`** / **`stripe.ex`** for how keys are stringified). Add **`list_payment_methods!/2`** mirroring **`attach_payment_method!/3`** error-raising style. Extend **`@moduledoc`** with a bullet for the read surface. If list filters are deferred, document **`opts`** as reserved and pass **`[]`** to the processor **`opts`** argument — still satisfy **D-02** by naming the extension point in **`@moduledoc`** table.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue" && mix compile'</automated>
  </verify>
  <done>Action module lists PMs via processor.</done>
</task>

<task type="auto">
  <name>Task 2: Accrue.Billing façade + span</name>
  <read_first>
    accrue/lib/accrue/billing.ex
    accrue/lib/accrue/billing/payment_method_actions.ex
  </read_first>
  <files>
    accrue/lib/accrue/billing.ex
  </files>
  <acceptance_criteria>
    - `rg -n "def list_payment_methods" accrue/lib/accrue/billing.ex` matches **≥ 2** lines (**`/2`** and **`!/2`**).
    - `rg -n "payment_method, :list" accrue/lib/accrue/billing.ex` matches **≥ 2** lines (tuple appears inside both public functions).
    - `rg -n "PaymentMethodActions.list_payment_methods" accrue/lib/accrue/billing.ex` exits **0**.
    - Each new public function has **`@doc`** mentioning delegation to **`PaymentMethodActions`** and includes **`@doc since: \"0.3.1\"`** (or the current **`@version`** string from **`accrue/mix.exs`** if it differs at implementation time).
    - `mix compile` in **`accrue`** exits **0**.
  </acceptance_criteria>
  <action>
    Insert **`list_payment_methods/2`** and **`list_payment_methods!/2`** immediately after the existing **`set_default_payment_method!`** pair in the **PaymentMethod surface** section, each wrapping **`PaymentMethodActions.list_payment_methods`** / **`!`** inside **`span_billing(:payment_method, :list, customer, opts, fn -> … end)`** mirroring **`:attach`**. Add **`@doc`** describing read-only semantics (processor truth, not local projection cache).
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue" && mix compile'</automated>
  </verify>
  <done>Billing public API emits billing span.</done>
</task>

<task type="auto">
  <name>Task 3: ExUnit — Billing list happy path + span coverage</name>
  <read_first>
    accrue/test/support/billing_case.ex
    accrue/test/accrue/processor/fake_phase3_test.exs
    accrue/test/accrue/telemetry/billing_span_coverage_test.exs
    accrue/lib/accrue/billing.ex
  </read_first>
  <files>
    accrue/test/accrue/billing/payment_method_list_test.exs
  </files>
  <acceptance_criteria>
    - File **`accrue/test/accrue/billing/payment_method_list_test.exs`** exists and **`use Accrue.BillingCase`** (or the project-standard billing test harness).
    - Test calls **`Accrue.Billing.list_payment_methods`** (not only **`Processor.Fake`** directly) and asserts **`{:ok, %{data: list}}`** (or the actual return shape after **`read_first`**) with **`length(list) >= 1`** after attach.
    - `cd accrue && mix test test/accrue/billing/payment_method_list_test.exs test/accrue/telemetry/billing_span_coverage_test.exs` exits **0**.
    - `rg -n "Process\\.sleep" accrue/test/accrue/billing/payment_method_list_test.exs` returns **no** matches (**56-CONTEXT D-08**).
  </acceptance_criteria>
  <action>
    Create **`payment_method_list_test.exs`**: setup customer + payment method + **`Billing.attach_payment_method`**, then assert **`Billing.list_payment_methods(customer, [])`** returns the attached PM in **`data`**. Reset Fake per **`BillingCase`** conventions. Ensure test module name follows **`Accrue.Billing.*Test`** pattern used by sibling billing tests.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue" && mix test test/accrue/billing/payment_method_list_test.exs test/accrue/telemetry/billing_span_coverage_test.exs'</automated>
  </verify>
  <done>BIL-01 proved on Fake + span inventory green.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix test test/accrue/billing/payment_method_list_test.exs test/accrue/telemetry/billing_span_coverage_test.exs` — exit **0**.
- `cd accrue && mix compile --warnings-as-errors` — exit **0** if the repo already uses this in CI; otherwise **`mix compile`**.
</verification>

<success_criteria>
- **BIL-01:** One bounded **`Accrue.Billing`** expansion ships with **Fake** regressions and **no** **PROC-08** / **FIN-03** creep.
- **`billing_span_coverage_test.exs`** passes with new public functions.
</success_criteria>

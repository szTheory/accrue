---
phase: 56-billing-stripe-depth-telemetry-truth
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - accrue/guides/telemetry.md
  - accrue/CHANGELOG.md
  - accrue/priv/accrue/templates/install/billing.ex.eex
  - accrue/guides/first_hour.md
autonomous: true
requirements: [BIL-02]
must_haves:
  truths:
    - "`guides/telemetry.md` documents the **`[:accrue, :billing, :payment_method, :list]`** span with the same dotted naming style as adjacent billing examples (56-CONTEXT D-10, Phase 40 verified-examples rule)"
    - "`CHANGELOG.md` **Unreleased** records host-facing **`list_payment_methods`** capability under **Features** or **Billing** (56-CONTEXT D-13)"
    - "Generated host **`billing.ex.eex`** exposes **`list_payment_methods`** delegation so **`MyApp.Billing`** stays the supported seam (56-CONTEXT D-17)"
  artifacts:
    - path: "accrue/guides/telemetry.md"
      provides: "Telemetry catalog truth"
      contains: "payment_method"
    - path: "accrue/CHANGELOG.md"
      provides: "Release notes"
      contains: "list_payment_method"
    - path: "accrue/priv/accrue/templates/install/billing.ex.eex"
      provides: "Installer host facade"
      contains: "list_payment_methods"
  key_links:
    - from: "56-CONTEXT.md"
      to: "D-10 D-11 D-13 D-17"
      via: "Docs + host template"
      pattern: "context"
---

<threat_model>
**ASVS L1.** Doc edits must **not** embed live API keys or sample **`sk_live_`** prefixes. Telemetry prose stays **low-cardinality** — describe event names and suggested subscriptions, not “log everything from metadata.” **`operator-runbooks.md`** is **out of scope** unless Plan **01** introduced a new **`[:accrue, :ops, :*]`** tuple (**56-CONTEXT D-11** default: no runbook edits).
</threat_model>

<objective>
Complete **BIL-02**: align **`guides/telemetry.md`** with the new billing span, update **`CHANGELOG.md`**, extend **`billing.ex.eex`** for installer hosts, and add a **single** First Hour cross-link only if it reduces evaluator confusion (**56-CONTEXT** optional stretch).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-billing-stripe-depth-telemetry-truth/56-CONTEXT.md
@.planning/phases/56-billing-stripe-depth-telemetry-truth/56-RESEARCH.md
@.planning/phases/40-telemetry-catalog-guide-truth/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: guides/telemetry.md — billing firehose line</name>
  <read_first>
    accrue/guides/telemetry.md
    accrue/lib/accrue/billing.ex
  </read_first>
  <files>
    accrue/guides/telemetry.md
  </files>
  <acceptance_criteria>
    - `rg -n "payment_method" accrue/guides/telemetry.md` matches **≥ 1** line in the **billing span examples** / illustrative list region (same subsection style as **`meter_event.report_usage`**).
    - The added line includes the substring **`list`** adjacent to **`payment_method`** documentation intent (e.g. **`accrue.billing.payment_method.list`** or documented **`[:accrue, :billing, :payment_method, :list]`** — must match actual **`span_billing`** resource/action pair from **`billing.ex`** after Plan **01**).
    - `rg -n "operator-runbooks" accrue/guides/telemetry.md` change count is **0** unless Plan **01** added an **ops** tuple (default: **0** edits to runbook links).
  </acceptance_criteria>
  <action>
    Add **one** verified billing span bullet for **`payment_method` / `list`** next to existing non-exhaustive examples; keep **ops** catalog untouched unless new ops events exist.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && rg -n "payment_method" accrue/guides/telemetry.md | head -n 5'</automated>
  </verify>
  <done>Telemetry guide matches code.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG + install template (+ optional first_hour)</name>
  <read_first>
    accrue/CHANGELOG.md
    accrue/priv/accrue/templates/install/billing.ex.eex
    accrue/guides/first_hour.md
    accrue/README.md
  </read_first>
  <files>
    accrue/CHANGELOG.md
    accrue/priv/accrue/templates/install/billing.ex.eex
  </files>
  <acceptance_criteria>
    - `rg -n "list_payment_method" accrue/CHANGELOG.md` matches **≥ 1** line under **`## Unreleased`**.
    - `rg -n "def list_payment_methods" accrue/priv/accrue/templates/install/billing.ex.eex` matches **≥ 1** line delegating to **`Billing.list_payment_methods`**.
    - `rg -n "def list_payment_methods!" accrue/priv/accrue/templates/install/billing.ex.eex` matches **≥ 1** line delegating to **`Billing.list_payment_methods!`**.
    - If **`first_hour.md`** is edited: `rg -n "list_payment_methods" accrue/guides/first_hour.md` is **≥ 1**; if **not** edited, skip and note **“no first_hour change”** in **`SUMMARY.md`** deviations later.
  </acceptance_criteria>
  <action>
    Append an **Unreleased** bullet describing **`Accrue.Billing.list_payment_methods`** for hosts. Extend **`billing.ex.eex`** with **`list_payment_methods/2`** and **`list_payment_methods!/2`** delegating to **`Accrue.Billing`** with a short **`@doc`** comment (“host may add policy / preloads”). Optionally add **one** sentence to **`first_hour.md`** pointing hosts at the new read API **only** if it fits the existing “public surface” paragraph without scope creep.
  </action>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)/accrue" && mix compile'</automated>
  </verify>
  <done>BIL-02 host + release narrative updated.</done>
</task>

</tasks>

<verification>
- `cd accrue && mix test test/accrue/telemetry/billing_span_coverage_test.exs` — exit **0** (telemetry doc changes must not break build; regression guard).
- `rg -n "list_payment_method" accrue/CHANGELOG.md accrue/guides/telemetry.md` — each file **≥ 1** match from repo root.
</verification>

<success_criteria>
- **BIL-02:** Telemetry catalog / operator narrative for new/changed billing signals matches shipped code; **CHANGELOG** records the capability; installer template exposes host delegation.
</success_criteria>

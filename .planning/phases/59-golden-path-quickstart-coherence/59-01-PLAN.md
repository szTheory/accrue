---
phase: 59-golden-path-quickstart-coherence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/guides/first_hour.md
  - accrue/guides/quickstart.md
  - CONTRIBUTING.md
autonomous: true
requirements: [INT-06]
must_haves:
  truths:
    - "First Hour contains one bounded Trust boundary block linking auth_adapters, organization_billing, and host README without duplicating the Sigra essay"
    - "Quickstart remains a thin hub and points to auth_adapters for production Accrue.Auth"
    - "CONTRIBUTING documents the ordered bash preflight trio for golden-path doc edits"
  artifacts:
    - path: "accrue/guides/first_hour.md"
      provides: "Trust boundary + capsule clarity"
      contains: "Trust boundary"
    - path: "accrue/guides/quickstart.md"
      provides: "Hub + auth routing"
      contains: "auth_adapters"
    - path: "CONTRIBUTING.md"
      provides: "Contributor preflight"
      contains: "verify_verify01_readme_contract"
  key_links:
    - from: "first_hour.md"
      to: "auth_adapters.md"
      via: "relative markdown link"
      pattern: "D-06"
---

<threat_model>
**ASVS L1.** Documentation only. Do **not** paste live Stripe keys, production webhook secrets, or PII. Keep existing placeholders (`whsec_test_host`, env lookups in fenced examples).
</threat_model>

<objective>
Ship **INT-06** narrative slice: **59-CONTEXT D-06–D-09**, **D-13** — one **Trust boundary** block in **First Hour**, light **Capsule M** Sigra framing, **quickstart** pointer to **`auth_adapters.md`**, and **CONTRIBUTING** preflight trio for editors touching the golden-path doc set.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-golden-path-quickstart-coherence/59-CONTEXT.md
@.planning/phases/59-golden-path-quickstart-coherence/59-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: First Hour — Trust boundary block</name>
  <read_first>
    .planning/phases/59-golden-path-quickstart-coherence/59-CONTEXT.md
    accrue/guides/first_hour.md
    examples/accrue_host/README.md
    accrue/guides/auth_adapters.md
  </read_first>
  <files>accrue/guides/first_hour.md</files>
  <acceptance_criteria>
    - `rg -n '^### Trust boundary' accrue/guides/first_hour.md` returns **≥ 1** line.
    - `rg -n 'auth_adapters\\.md' accrue/guides/first_hour.md` returns **≥ 1** line.
    - `rg -n 'organization_billing\\.md' accrue/guides/first_hour.md` returns **≥ 1** line.
    - `rg -n 'examples/accrue_host/README\\.md' accrue/guides/first_hour.md` returns **≥ 1** line (pointer to demo SSOT remains).
    - `rg -ni 'Sigra.*required|required.*Sigra' accrue/guides/first_hour.md` returns **0** lines (forbidden pattern per **D-09**).
  </acceptance_criteria>
  <action>
    Insert a new subsection **`### Trust boundary (production vs demo)`** immediately **after** the three **Capsule H/M/R** subsections and **before** **`## 1. First run`**. Content (**≤ 8 sentences** total) must state: production uses host-owned **`Accrue.Auth`**; link **`[Auth adapters](auth_adapters.md)`**; Sigra is **optional** / demo uses it for deterministic org billing + CI; non-Sigra hosts follow **Capsule H** + link **`[Organization billing (non-Sigra)](organization_billing.md)`**; defer demo **`mix.exs`** specifics to **`examples/accrue_host/README.md`** via one relative link matching existing repo layout (`../../examples/accrue_host/README.md` from `accrue/guides/`).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "^### Trust boundary" accrue/guides/first_hour.md | head -n1 | grep -q .'</automated>
  </verify>
  <done>Trust boundary block present with required links.</done>
</task>

<task type="auto">
  <name>Task 2: First Hour — Capsule M Sigra one-liner</name>
  <read_first>
    accrue/guides/first_hour.md
    .planning/phases/59-golden-path-quickstart-coherence/59-CONTEXT.md
  </read_first>
  <files>accrue/guides/first_hour.md</files>
  <acceptance_criteria>
    - `rg -n '### Capsule M' accrue/guides/first_hour.md` returns **≥ 1** line.
    - In the **Capsule M** subsection only: `rg -n 'Sigra' accrue/guides/first_hour.md` returns **≥ 1** line after edit (word **Sigra** appears in or immediately after that subsection — verify by reading file: the line with **Sigra** must occur **after** the line matching `### Capsule M` and **before** the line matching `### Capsule R`; use `awk '/### Capsule M/,/### Capsule R/' accrue/guides/first_hour.md | rg -n Sigra | head -n1 | grep -q .` as automated check).
  </acceptance_criteria>
  <action>
    Under **`### Capsule M — Monorepo clone`**, append **exactly one sentence** clarifying that **Sigra** is wired in the **checked-in demo** for convenience/reproducibility, **not** a requirement to ship Accrue in production — must not duplicate the full host README blockquote.
  </action>
  <verify>
    <automated>bash -lc 'awk "/### Capsule M/,/### Capsule R/" accrue/guides/first_hour.md | rg -n Sigra | head -n1 | grep -q .'</automated>
  </verify>
  <done>Capsule M clarifies Sigra as demo convenience.</done>
</task>

<task type="auto">
  <name>Task 3: Quickstart — production auth pointer</name>
  <read_first>
    accrue/guides/quickstart.md
    accrue/guides/first_hour.md
    .planning/phases/59-golden-path-quickstart-coherence/59-CONTEXT.md
  </read_first>
  <files>accrue/guides/quickstart.md</files>
  <acceptance_criteria>
    - `rg -n 'auth_adapters\\.md' accrue/guides/quickstart.md` returns **≥ 1** line.
    - `wc -l accrue/guides/quickstart.md` prints a line count **≤ 45** (stay thin — hub only; slight growth allowed vs **51-03** ceiling if file was shorter).
    - `rg -n 'defp deps' accrue/guides/quickstart.md` returns **0** lines (no second install spine).
  </acceptance_criteria>
  <action>
    In **`## Focused guides`** (or equivalent hub list), add **one bullet** linking **`[Auth adapters](auth_adapters.md)`** with **≤ 18 words** describing production **`Accrue.Auth`** choices. Do **not** add a second tutorial body or fenced **`mix.exs`** install block.
  </action>
  <verify>
    <automated>bash -lc '! rg -q "defp deps" accrue/guides/quickstart.md'</automated>
  </verify>
  <done>Quickstart routes to auth contract.</done>
</task>

<task type="auto">
  <name>Task 4: CONTRIBUTING — doc-contract preflight trio</name>
  <read_first>
    CONTRIBUTING.md
    .planning/phases/59-golden-path-quickstart-coherence/59-CONTEXT.md
    scripts/ci/README.md
  </read_first>
  <files>CONTRIBUTING.md</files>
  <acceptance_criteria>
    - `rg -n 'bash scripts/ci/verify_package_docs\\.sh' CONTRIBUTING.md` returns **≥ 1** line.
    - `rg -n 'verify_verify01_readme_contract\\.sh' CONTRIBUTING.md` returns **≥ 1** line.
    - `rg -n 'verify_adoption_proof_matrix\\.sh' CONTRIBUTING.md` returns **≥ 1** line.
    - The file contains the **substring** `verify_package_docs.sh &&` on one line or the three commands appear in **numbered order** in a fenced `bash` block (verify: `rg -n "verify_package_docs" -n CONTRIBUTING.md` line number is **less than** line number of first `verify_verify01` occurrence, and that is **less than** first `verify_adoption_proof` occurrence — use `bash -lc '...'` comparing line numbers if needed).
  </acceptance_criteria>
  <action>
    Near the existing **`scripts/ci/`** / **`verify_package_docs`** guidance (**~ line 75**), add a short note (**≤ 6 sentences**) plus a **fenced `bash` block** with the **exact ordered trio** from **59-CONTEXT D-11**:
    `bash scripts/ci/verify_package_docs.sh && \`
    `bash scripts/ci/verify_verify01_readme_contract.sh && \`
    `bash scripts/ci/verify_adoption_proof_matrix.sh`
    State explicitly this is the **minimum local preflight** before PRs that touch **First Hour**, **host README**, **quickstart**, or **`verify_package_docs`** needles — it does **not** replace **`host-integration`**.
  </action>
  <verify>
    <automated>bash -lc 'grep -n verify_package_docs CONTRIBUTING.md | head -1; grep -n verify_verify01 CONTRIBUTING.md | head -1; grep -n verify_adoption_proof CONTRIBUTING.md | head -1'</automated>
  </verify>
  <done>CONTRIBUTING lists ordered preflight commands.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits **0** (markdown-only edits should not break existing needles).
- `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
- `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.
</verification>

<success_criteria>
- All four tasks meet **acceptance_criteria** with grep/automation as written.
- No forbidden “Sigra required for production” phrasing in **First Hour** or **quickstart**.
</success_criteria>

## PLANNING COMPLETE

---
phase: 59-golden-path-quickstart-coherence
plan: "02"
type: execute
wave: 2
depends_on: ["59-01-PLAN.md"]
files_modified:
  - scripts/ci/verify_package_docs.sh
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [INT-06]
must_haves:
  truths:
    - "verify_package_docs.sh enforces quickstart hub invariants aligned with INT-06 and 59-CONTEXT D-12/D-18"
    - "PackageDocsVerifierTest stays green and tmp-dir negative tests cover quickstart drift"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "Extended doc contract"
      contains: "quickstart.md"
    - path: "accrue/test/accrue/docs/package_docs_verifier_test.exs"
      provides: "ExUnit gate"
      contains: "package docs verifier"
  key_links:
    - from: "verify_package_docs.sh"
      to: "accrue/guides/quickstart.md"
      via: "require_fixed paths"
      pattern: "D-12"
---

<threat_model>
**ASVS L1.** Script edits only affect **static doc verification** — no new network calls, no secrets in repo. Test fixtures must keep using **`whsec_test_host`** / existing placeholders only.
</threat_model>

<objective>
Ship **INT-06** automation slice: extend **`verify_package_docs.sh`** + **`PackageDocsVerifierTest`** so **`accrue/guides/quickstart.md`** and **structural** golden-path invariants (**capsule headings**, **no second spine** in quickstart) are merge-blocking like other package docs (**59-CONTEXT D-12**, **D-18**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-golden-path-quickstart-coherence/59-CONTEXT.md
@.planning/phases/59-golden-path-quickstart-coherence/59-RESEARCH.md
@.planning/phases/59-golden-path-quickstart-coherence/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify_package_docs.sh — quickstart + structural needles</name>
  <read_first>
    scripts/ci/verify_package_docs.sh
    accrue/guides/quickstart.md
    accrue/guides/first_hour.md
    examples/accrue_host/README.md
  </read_first>
  <files>scripts/ci/verify_package_docs.sh</files>
  <acceptance_criteria>
    - `rg -n 'accrue/guides/quickstart\\.md' scripts/ci/verify_package_docs.sh` returns **≥ 3** lines (file path referenced in **≥ 3** distinct checks — use `rg -c` ≥ 3 or count `require_` lines containing `quickstart.md`).
    - `rg -n 'require_.*quickstart' scripts/ci/verify_package_docs.sh` returns **≥ 2** lines.
    - `rg -n 'auth_adapters' scripts/ci/verify_package_docs.sh` returns **≥ 1** line (**after** Plan **01** added the link to quickstart — this task adds `require_fixed` for that substring).
    - `rg -n 'require_absent|defp deps' scripts/ci/verify_package_docs.sh` shows at least one check preventing **`defp deps`** in **`quickstart.md`** (literal `defp deps` in `require_absent_regex` or `grep -Fq` negated).
    - Script still ends with the existing success **`echo`** lines (append quickstart to the second echo if you extend messaging).
  </acceptance_criteria>
  <action>
    After **`accrue_version`** / **`accrue_admin_version`** variables are defined (reuse them), add **`require_fixed`** calls for **`$ROOT_DIR/accrue/guides/quickstart.md`** including at minimum:
    - `[First Hour](first_hour.md)` (hub entry).
    - **`Capsule`** vocabulary (same word as current quickstart uses — match exact casing from file).
    - **`auth_adapters.md`** (post–Plan-01 anchor).
    Add **`require_absent_regex`** on **`quickstart.md`** for **`defp deps`** (forbids second **`mix.exs`** tutorial spine).
    Add **structural** checks for **`first_hour.md`**: `require_fixed` for **`### Capsule H`**, **`### Capsule M`**, **`### Capsule R`** in that relative order (three separate `require_fixed` lines are acceptable if order is already guaranteed by narrative).
    Add **`require_fixed`** for **`examples/accrue_host/README.md`** for **`### Capsule H`**, **`### Capsule M`**, **`### Capsule R`** if not already present in script; if already satisfied by other checks, add explicit `require_fixed` only for any **missing** capsule heading literal.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>Package docs script covers quickstart + capsule structure.</done>
</task>

<task type="auto">
  <name>Task 2: PackageDocsVerifierTest — fixtures + assertions</name>
  <read_first>
    accrue/test/accrue/docs/package_docs_verifier_test.exs
    scripts/ci/verify_package_docs.sh
  </read_first>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <acceptance_criteria>
    - `mix test accrue/test/accrue/docs/package_docs_verifier_test.exs` exits **0**.
    - If any test builds a **`tmp_dir`** fixture tree that runs the verifier script, that tree **`File.cp!`** includes **`accrue/guides/quickstart.md`** whenever **`ROOT_DIR`** is set for a full-repo positive run path inside that test (verify: `rg -n 'quickstart' accrue/test/accrue/docs/package_docs_verifier_test.exs` returns **≥ 1** line after edits).
    - Positive test **`assert output =~`** includes **`quickstart`** (substring) in **`test "package docs verifier succeeds"`** or a dedicated assertion line.
  </acceptance_criteria>
  <action>
    Update **`copy_fixture!`** / **`File.cp`** lists in **`tmp_dir`** tests so **`accrue/guides/quickstart.md`** is present whenever the bash script under **`ROOT_DIR`** would read it. Extend the happy-path stdout assertions to mention **`quickstart`** (mirror existing **First run** / **host-integration** style checks). Add **one** negative test that removes **`auth_adapters`** link from a **temp quickstart** and asserts **non-zero** exit + **`[verify_package_docs]`** in output **if** you add a hard **`require_fixed`** for that substring; skip new negative test if redundant.
  </action>
  <verify>
    <automated>mix test accrue/test/accrue/docs/package_docs_verifier_test.exs</automated>
  </verify>
  <done>ExUnit reflects new script contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Full doc-contract gate sweep</name>
  <read_first>
    scripts/ci/verify_verify01_readme_contract.sh
    scripts/ci/verify_adoption_proof_matrix.sh
  </read_first>
  <files></files>
  <acceptance_criteria>
    - `bash scripts/ci/verify_package_docs.sh` exits **0**.
    - `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.
  </acceptance_criteria>
  <action>
    From repository root, run the **three scripts above in order** (same order as **59-CONTEXT D-11**). Fix any failures by adjusting **docs** or **scripts** — do not weaken unrelated assertions.
  </action>
  <verify>
    <automated>bash -lc 'bash scripts/ci/verify_package_docs.sh && bash scripts/ci/verify_verify01_readme_contract.sh && bash scripts/ci/verify_adoption_proof_matrix.sh'</automated>
  </verify>
  <done>INT-06 merge-blocking doc scripts green.</done>
</task>

</tasks>

<verification>
- `mix test accrue/test/accrue/docs/package_docs_verifier_test.exs`
- Full bash trio as in Task 3.
</verification>

<success_criteria>
- **`verify_package_docs.sh`** fails on deliberate quickstart hub violations (missing **First Hour** link, **`defp deps`**, missing **`auth_adapters`** link).
- All automated commands in **acceptance_criteria** exit **0** on a clean tree after implementation.
</success_criteria>

## PLANNING COMPLETE

---
phase: 60-adoption-proof-ci-ownership-map
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/accrue_host/docs/adoption-proof-matrix.md
  - examples/accrue_host/docs/evaluator-walkthrough-script.md
autonomous: true
requirements: [INT-07]
must_haves:
  truths:
    - "Matrix and walkthrough stay honest about Fake merge-blocking vs Stripe advisory and Layer B vs C"
    - "Thin v1.15 trust signals present with pointers to First Hour and host README (no essay duplication)"
  artifacts:
    - path: "examples/accrue_host/docs/adoption-proof-matrix.md"
      provides: "Claim catalog + trust boundary stubs"
      contains: "Trust and versioning"
    - path: "examples/accrue_host/docs/evaluator-walkthrough-script.md"
      provides: "Evaluator checklist aligned to matrix"
      contains: "adoption-proof-matrix"
  key_links:
    - from: "evaluator-walkthrough-script.md"
      to: "adoption-proof-matrix.md"
      via: "same-directory relative link"
---

<threat_model>
**ASVS L1.** Documentation only. Do **not** add real API keys, webhook secrets, or customer PII. Use existing placeholder patterns from surrounding docs.
</threat_model>

<objective>
Refresh **`adoption-proof-matrix.md`** and **`evaluator-walkthrough-script.md`** so **INT-07** holds: proof lanes and **v1.15** trust semantics stay **scanner-true**, with **traceability** from walkthrough to matrix (**60-CONTEXT D-04–D-08**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-adoption-proof-ci-ownership-map/60-CONTEXT.md
@.planning/phases/60-adoption-proof-ci-ownership-map/60-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Matrix — trust / versioning stub (v1.15+)</name>
  <read_first>
    examples/accrue_host/docs/adoption-proof-matrix.md
    scripts/ci/verify_adoption_proof_matrix.sh
    accrue/guides/first_hour.md
    examples/accrue_host/README.md
  </read_first>
  <files>examples/accrue_host/docs/adoption-proof-matrix.md</files>
  <acceptance_criteria>
    - `rg -n '^### Trust and versioning' examples/accrue_host/docs/adoption-proof-matrix.md` returns **≥ 1** line.
    - `rg -n 'first_hour\\.md' examples/accrue_host/docs/adoption-proof-matrix.md` returns **≥ 1** line.
    - `rg -n 'README\\.md' examples/accrue_host/docs/adoption-proof-matrix.md` returns **≥ 1** line (host README pointer).
    - `rg -ni 'Hex|hex\\.pm' examples/accrue_host/docs/adoption-proof-matrix.md` returns **≥ 1** line.
    - `rg -F 'non-Sigra' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0** with **≥ 1** match.
    - `rg -F 'host-integration' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0** with **≥ 1** match.
    - `rg -Fi 'advisory' examples/accrue_host/docs/adoption-proof-matrix.md` exits **0** with **≥ 1** match.
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0** after edits.
  </acceptance_criteria>
  <action>
    Insert a new subsection **`### Trust and versioning (v1.15+)`** immediately **before** the existing **`## Evaluator narrative`** heading (keep that heading verbatim). Content (**≤ 6 short bullets**): (1) **Hex**-published SemVer in package **`mix.exs`** is authoritative for dependency pins; (2) **`.planning/`** milestone labels (`v1.16`, …) are **planning** artifacts, not substitute install pins; (3) **Demo / optional** adapters (**Sigra** in the checked-in host) are **host wiring**, not a global production requirement — align with **`non-Sigra`** ORG-09 framing already in this file; (4) link **`first_hour.md`** using the **same relative path prefix** as the existing **`organization_billing.md`** markdown link in the **`### Primary archetype (merge-blocking)`** table (swap filename only); (5) link the example host README as **`../README.md`**. Do **not** remove or rename existing headings enforced by **`verify_adoption_proof_matrix.sh`**.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh</automated>
  </verify>
  <done>Matrix includes trust/versioning stub and verifier stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Walkthrough — matrix traceability + lane honesty</name>
  <read_first>
    examples/accrue_host/docs/evaluator-walkthrough-script.md
    examples/accrue_host/docs/adoption-proof-matrix.md
  </read_first>
  <files>examples/accrue_host/docs/evaluator-walkthrough-script.md</files>
  <acceptance_criteria>
    - `rg -n 'adoption-proof-matrix\\.md' examples/accrue_host/docs/evaluator-walkthrough-script.md` returns **≥ 1** line.
    - `rg -n 'Trust and versioning|### Trust' examples/accrue_host/docs/evaluator-walkthrough-script.md` returns **≥ 1** line (explicit pointer to matrix subsection title or anchor text).
    - Section **`## A.`** still exists: `rg -n '^## A\\.' examples/accrue_host/docs/evaluator-walkthrough-script.md` returns **≥ 1** line.
    - `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0** (walkthrough must not break matrix literals — matrix unchanged in this task; script still passes).
  </acceptance_criteria>
  <action>
    Under **`## A. Clone and trust gate`**, after the existing step list (or as final numbered step in that section), add **one** bullet instructing evaluators to open **`adoption-proof-matrix.md`** and read **`### Trust and versioning (v1.15+)`** before claiming install / milestone / demo-Sigra facts on camera. In **`## E. What to say about “real Stripe”`**, add **≤ 2 sentences** restating that **Stripe test mode** remains **advisory** per matrix **`## Advisory: Stripe test mode`** — no wording that implies Stripe is merge-blocking for contributors.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_adoption_proof_matrix.sh</automated>
  </verify>
  <done>Walkthrough traces to matrix trust section.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits **0**.
- `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
- `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits **0** (from monorepo root).
</verification>

<success_criteria>
- Both tasks meet **acceptance_criteria**.
- No new **merge-blocking** claims for Stripe or Sigra that contradict **`adoption-proof-matrix.md`** layering tables.
</success_criteria>

## PLANNING COMPLETE

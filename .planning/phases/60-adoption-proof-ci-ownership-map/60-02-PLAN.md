---
phase: 60-adoption-proof-ci-ownership-map
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/README.md
  - CONTRIBUTING.md
autonomous: true
requirements: [INT-07]
must_haves:
  truths:
    - "`scripts/ci/README.md` gains an INT (v1.16) gate table matching ADOPT/ORG column schema plus explicit delta scope note"
    - "CONTRIBUTING still routes maintainers to `scripts/ci/README.md` when CI map rows change"
  artifacts:
    - path: "scripts/ci/README.md"
      provides: "INT verifier ownership for v1.16 delta"
      contains: "INT gates"
    - path: "CONTRIBUTING.md"
      provides: "Contributor routing"
      contains: "scripts/ci/README"
---

<threat_model>
**ASVS L1.** Documentation only. No secrets in tables; script paths only.
</threat_model>

<objective>
Extend **`scripts/ci/README.md`** with an **INT / v1.16** contributor map section per **60-CONTEXT D-01–D-03, D-09–D-10**, and align **`CONTRIBUTING.md`** cross-pointer if the CI map navigation needs a one-line INT mention (**INT-07**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-adoption-proof-ci-ownership-map/60-CONTEXT.md
@.planning/phases/60-adoption-proof-ci-ownership-map/60-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/ci/README — INT gates section + scope note</name>
  <read_first>
    scripts/ci/README.md
    .planning/phases/60-adoption-proof-ci-ownership-map/60-CONTEXT.md
    .github/workflows/ci.yml
  </read_first>
  <files>scripts/ci/README.md</files>
  <acceptance_criteria>
    - `rg -n '^## INT gates' scripts/ci/README.md` returns **≥ 1** line (exact heading **`## INT gates (v1.16 integrator + proof continuity)`** OR **`## INT gates`** as prefix — verify with `rg -n '^## INT gates'` **≥ 1**).
    - Table header row contains all three column labels **exactly**: ``| REQ-ID |``, ``| Primary script(s) or artifact |``, ``| Package ExUnit (if any) |``, ``| Phase VERIFICATION owner |`` (verify: `rg -F 'REQ-ID' scripts/ci/README.md` and `rg -F 'Phase VERIFICATION owner' scripts/ci/README.md` each **≥ 1**).
    - **≥ 2** data rows where first column contains **`INT-0`**: `rg -n 'INT-0[67]' scripts/ci/README.md` returns **≥ 2** lines (covers **INT-06** and **INT-07** requirement IDs).
    - Scope note present: `rg -ni 'delta|v1\\.16|branch protection|ci\\.yml' scripts/ci/README.md` returns **≥ 3** matches across the INT section (paragraph or bullet under the new heading — use `rg -ni` on file).
    - `rg -n '59-VERIFICATION|60-VERIFICATION' scripts/ci/README.md` returns **≥ 1** line (markdown link or path text to phase verification files).
  </acceptance_criteria>
  <action>
    Insert **`## INT gates (v1.16 integrator + proof continuity)`** **after** the **`## ORG gates`** table and **before** the existing **`### Triage: verify_adoption_proof_matrix.sh`** heading (preserve that triage block). Add: (1) **≤ 4 sentences** scope note stating rows are **delta-maintained** for merge-blocking checks **touched by phases 59–61**; normative required-job set remains **`.github/workflows/ci.yml`** plus branch protection. (2) Markdown table **matching ADOPT/ORG columns** with rows:
    - **INT-06** — primary artifacts **`accrue/guides/first_hour.md`**, **`examples/accrue_host/README.md`**, **`accrue/guides/quickstart.md`**, **`CONTRIBUTING.md`**; scripts **`verify_package_docs.sh`**, **`verify_verify01_readme_contract.sh`**, **`verify_adoption_proof_matrix.sh`**; ExUnit **`accrue/test/accrue/docs/package_docs_verifier_test.exs`**; owner **`.planning/phases/59-golden-path-quickstart-coherence/59-VERIFICATION.md`**.
    - **INT-07** — artifacts **`examples/accrue_host/docs/adoption-proof-matrix.md`**, **`examples/accrue_host/docs/evaluator-walkthrough-script.md`**; scripts **`verify_adoption_proof_matrix.sh`** (and **`verify_package_docs.sh`** only if pins touch matrix paths); ExUnit **`accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs`** when ORG-09 matrix literals change (else **—**); owner **`.planning/phases/60-adoption-proof-ci-ownership-map/60-VERIFICATION.md`**.
    Do **not** add **INT-08/INT-09** full rows (Phase **61**); optional one-line sentence under table: “**INT-08/INT-09** — Phase **61**.”
  </action>
  <verify>
    <automated>bash -lc 'rg -q "^## INT gates" scripts/ci/README.md'</automated>
  </verify>
  <done>INT registry section present with INT-06 and INT-07 rows.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING — pointer to INT CI map section</name>
  <read_first>
    CONTRIBUTING.md
    scripts/ci/README.md
  </read_first>
  <files>CONTRIBUTING.md</files>
  <acceptance_criteria>
    - `rg -n 'scripts/ci/README\\.md' CONTRIBUTING.md` returns **≥ 1** line.
    - `rg -ni 'INT gates|INT-07|v1\\.16' CONTRIBUTING.md` returns **≥ 1** line (new or updated sentence routing integrator-milestone doc editors to the INT section).
  </acceptance_criteria>
  <action>
    In the **`scripts/ci/`** contributor guidance block (near existing **`verify_package_docs`** / preflight text), add **≤ 2 sentences** telling maintainers that **v1.16 integrator + proof** (**INT-06..INT-09**) verifier ownership lives under **`## INT gates`** in **`scripts/ci/README.md`** — without duplicating the full INT table in **CONTRIBUTING**.
  </action>
  <verify>
    <automated>bash -lc 'rg -ni "INT gates|INT-07|v1\\.16" CONTRIBUTING.md | head -1 | grep -q .'</automated>
  </verify>
  <done>CONTRIBUTING references INT section in CI README.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits **0**.
- `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
- `bash scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits **0** (from monorepo root).
</verification>

<success_criteria>
- INT table matches ADOPT/ORG column schema and includes **INT-06** + **INT-07** with correct verifier paths.
- **CONTRIBUTING** mentions INT / **`scripts/ci/README.md`** routing without duplicating the registry.
</success_criteria>

## PLANNING COMPLETE

---
phase: 61-root-verify-hops-hex-doc-ssot
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - scripts/ci/verify_package_docs.sh
  - scripts/ci/README.md
  - accrue/test/accrue/docs/package_docs_verifier_test.exs
autonomous: true
requirements: [INT-08]
must_haves:
  truths:
    - "Root README keeps VERIFY-01 / host-integration discoverable within ADOPT-01 hop intent; verifiers and README change together when the contract moves"
    - "Contributor map lists INT-08 with owning scripts and ExUnit, not a Phase 61 placeholder line"
    - "verify_package_docs encodes at least one additional root README invariant tied to the deep link into host proof if not already present"
  artifacts:
    - path: "scripts/ci/verify_package_docs.sh"
      provides: "INT-08 / package doc needles"
      contains: "README.md"
    - path: "scripts/ci/README.md"
      provides: "INT-08 registry row"
      contains: "INT-08"
  key_links:
    - from: "README.md"
      to: "examples/accrue_host/README.md"
      via: "#proof-and-verification"
      pattern: "D-01"
---

<threat_model>
**ASVS L1.** Documentation and CI scripts only. Do **not** add live secrets, real Stripe keys, or webhook signing material. Preserve existing `sk_live` negation semantics in host README when editing cross-links.
</threat_model>

<objective>
Close **INT-08**: root **`README.md`** proof-path IA stays within **ADOPT-01** hop intent after **v1.15** growth; **`verify_package_docs.sh`** owns root + cross-package pins; **`verify_verify01_readme_contract.sh`** keeps VERIFY-01 depth; **`scripts/ci/README.md`** gains a concrete **INT-08** row (replacing the placeholder); optional **D-07** overlap is audited and reduced only where **release-gate** self-sufficiency is preserved (**61-CONTEXT** D-04–D-07).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
@.planning/phases/61-root-verify-hops-hex-doc-ssot/61-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document verifier ownership above host README pins</name>
  <read_first>
    scripts/ci/verify_package_docs.sh
    scripts/ci/verify_verify01_readme_contract.sh
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>scripts/ci/verify_package_docs.sh</files>
  <acceptance_criteria>
    - `rg -n 'verify_verify01_readme_contract' scripts/ci/verify_package_docs.sh` returns **≥ 1** line in a **comment** within **20 lines above** the first `require_fixed "$host_readme_md"` (or immediately above the `host_readme_md=` assignment block if needles follow).
    - Comment text includes both substrings **`release-gate`** and **`host-integration`** (ASCII case as written).
    - **`rg -n 'INT-08' scripts/ci/verify_package_docs.sh`** returns **≥ 1** line inside that same contiguous **`#`** comment block (Bash comment lines only, before first non-comment line of the block).
  </acceptance_criteria>
  <action>
    Insert a **`#`** comment block **immediately before** the first cluster of **`require_fixed "$host_readme_md"`** lines in **`verify_package_docs.sh`** (before line **115** in the current file layout). Summarize **61-CONTEXT D-04 / D-05 / D-07** in **≤ 8 lines**: structural host README pins here support **`release-gate`** via **`package_docs_verifier_test.exs`**; **VERIFY-01 section semantics + dynamic Playwright inventory + `sk_live` guard** stay in **`verify_verify01_readme_contract.sh`** (**`host-integration`** first step). State that **intentional overlap** (e.g. **`mix verify.full`**) may remain so **`release-gate`** does not depend on **`host-integration`**.
  </action>
  <verify>
    <automated>bash -lc 'awk "/host_readme_md=/{flag=1} flag&&/require_fixed.*host_readme/{print NR; exit}" scripts/ci/verify_package_docs.sh | head -1'</automated>
  </verify>
  <done>Ownership comment block present.</done>
</task>

<task type="auto">
  <name>Task 2: Optional dedupe — host README needles owned by verify_verify01</name>
  <read_first>
    scripts/ci/verify_package_docs.sh
    scripts/ci/verify_verify01_readme_contract.sh
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>scripts/ci/verify_package_docs.sh</files>
  <acceptance_criteria>
    - **`bash scripts/ci/verify_package_docs.sh`** exits **0**.
    - **`rg -n '^# D-07 audit:' scripts/ci/verify_package_docs.sh`** returns **≥ 1** line.
  </acceptance_criteria>
  <action>
    Compare **`require_fixed "$host_readme_md"`** needles against **`verify_verify01_readme_contract.sh`** **`require_substring`** list for **`examples/accrue_host/README.md`**. Remove **at most one** redundant **`require_fixed`** line **only** if the substring is **purely** VERIFY-01 prose already enforced by **`verify_verify01`** **and** **`verify_package_docs.sh`** still retains **distinct structural pins** for **`## Proof and verification`**, **`### Verification modes`**, **`## First run`**, **`## Seeded history`**, **`mix setup`**, **`mix phx.server`**, **`/webhooks/stripe`**, **`/billing`**, **`mix verify`**, **`mix verify.full`**, **`bash scripts/ci/accrue_host_uat.sh`** (if removing a line would drop coverage of any of these literals, **do not remove**). Append a **`# D-07 audit:`** comment line documenting **`removed …`** or **`no removals; release-gate retains full host structural pins`** inside the Task 1 comment block.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>Dedupe pass complete with green verifier.</done>
</task>

<task type="auto">
  <name>Task 3: Root README — lock deep-link line in verify_package_docs</name>
  <read_first>
    README.md
    scripts/ci/verify_package_docs.sh
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>scripts/ci/verify_package_docs.sh</files>
  <acceptance_criteria>
    - **`README.md`** contains the exact substring **`Merge-blocking proof`** and **`examples/accrue_host/README.md#proof-and-verification`** on the **same** markdown link line (line **22** today — if line shifts, **`rg -n 'Merge-blocking proof.*proof-and-verification' README.md`** returns **≥ 1** line).
    - **`scripts/ci/verify_package_docs.sh`** contains a **`require_fixed "$ROOT_DIR/README.md"`** line whose second argument is the **full sentence substring** starting with **`Merge-blocking proof`** through **`proof-and-verification).`** (copy verbatim from **`README.md`** after any edit).
  </acceptance_criteria>
  <action>
    If **`README.md`** link line already matches the long merge-blocking anchor sentence, add **`require_fixed "$ROOT_DIR/README.md" "<exact substring from README>"`** using the **verbatim** single-line string (escape quotes for bash as needed). If **`README.md`** drifts, rewrite the link line to restore **D-01** hybrid: one line deep-linking host **`#proof-and-verification`** with **VERIFY-01** vocabulary, **then** add the matching **`require_fixed`**.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "Merge-blocking proof" README.md && rg -n "Merge-blocking proof" scripts/ci/verify_package_docs.sh'</automated>
  </verify>
  <done>Root README deep link is verifier-pinned.</done>
</task>

<task type="auto">
  <name>Task 4: scripts/ci/README.md — INT-08 registry row</name>
  <read_first>
    scripts/ci/README.md
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>scripts/ci/README.md</files>
  <acceptance_criteria>
    - **`rg -n '\\*\\*INT-08/INT-09\\*\\*' scripts/ci/README.md`** returns **0** lines (placeholder removed).
    - **`rg -n '\\| INT-08 \\|' scripts/ci/README.md`** returns **≥ 1** line (markdown table row).
    - INT-08 row’s **Primary script(s)** cell mentions **`verify_package_docs.sh`** and **`verify_verify01_readme_contract.sh`** (order free).
    - INT-08 row’s **Package ExUnit** cell contains **`package_docs_verifier_test.exs`**.
    - INT-08 row’s **Phase VERIFICATION owner** cell equals **`.planning/phases/61-root-verify-hops-hex-doc-ssot/61-VERIFICATION.md`** (exact path string).
  </acceptance_criteria>
  <action>
    Replace the standalone markdown line **`**INT-08/INT-09** — Phase **61**.`** with a **new table row** for **INT-08** inserted **after** the INT-07 row (still inside the INT gates table). Use the same **column semantics** as INT-06/07 rows. **Do not** add INT-09 here (that is **61-02**).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "INT-08" scripts/ci/README.md | head -3'</automated>
  </verify>
  <done>INT-08 contributor map row present.</done>
</task>

<task type="auto">
  <name>Task 5: ExUnit — align package_docs verifier test with script output</name>
  <read_first>
    accrue/test/accrue/docs/package_docs_verifier_test.exs
    scripts/ci/verify_package_docs.sh
  </read_first>
  <files>accrue/test/accrue/docs/package_docs_verifier_test.exs</files>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits **0**.
    - If Task 1 added new **`echo`** summary lines to **`verify_package_docs.sh`**, the test file asserts them with **`assert output =~`**; if no echo changes, this task is a **no-op** but must still leave tests green.
  </acceptance_criteria>
  <action>
    Run **`mix test test/accrue/docs/package_docs_verifier_test.exs`** from **`accrue/`**. Update assertions only if **`verify_package_docs.sh`** trailing **`echo`** strings changed.
  </action>
  <verify>
    <automated>bash -lc 'cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs'</automated>
  </verify>
  <done>ExUnit package docs tests green.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits **0**.
- `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
- `cd accrue && mix test test/accrue/docs/package_docs_verifier_test.exs` exits **0**.
</verification>

<success_criteria>
- **INT-08** table row exists in **`scripts/ci/README.md`**; placeholder **INT-08/INT-09** line removed.
- Root proof path remains discoverable; new **`require_fixed`** pins the merge-blocking deep-link sentence.
- Verifier ownership comment + optional safe dedupe complete without breaking **`release-gate`** semantics.
</success_criteria>

## PLANNING COMPLETE

---
phase: 61-root-verify-hops-hex-doc-ssot
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - scripts/ci/README.md
  - CONTRIBUTING.md
autonomous: true
requirements: [INT-09]
must_haves:
  truths:
    - "Planning docs do not imply Hex == main without the dual-track explanation; stale v1.16 'Next' pointers are fixed"
    - "Contributor map lists INT-09 with the same registry columns as other INT rows"
    - "CONTRIBUTING states the sharp edge: after a version bump on main, Hex-only mix deps.get may fail until publish — use path:/git ref or wait"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "v1.16 milestone front-matter honesty"
      contains: "Hex"
    - path: "CONTRIBUTING.md"
      provides: "Contributor sharp-edge note"
      contains: "Hex"
  key_links:
    - from: "MILESTONES.md"
      to: "PROJECT.md"
      via: "Current State"
      pattern: "D-08"
---

<threat_model>
**ASVS L1.** Planning and contributor markdown only. No secrets; do not embed API keys in examples.
</threat_model>

<objective>
Close **INT-09**: **`.planning/PROJECT.md`** / **`.planning/MILESTONES.md`** stay **HYG-honest** about **workspace `@version`** vs **public Hex**; **`scripts/ci/README.md`** registers **INT-09**; **CONTRIBUTING** documents the **pre-publish deps window** once (**61-CONTEXT** D-08–D-12, no collapsed SSOT).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
@.planning/phases/61-root-verify-hops-hex-doc-ssot/61-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MILESTONES.md — v1.16 Hex mirror + stale Next fix</name>
  <read_first>
    .planning/MILESTONES.md
    .planning/PROJECT.md
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>.planning/MILESTONES.md</files>
  <acceptance_criteria>
    - **`rg -n '/gsd-discuss-phase 59' .planning/MILESTONES.md`** returns **0** lines.
    - **`rg -n 'https://hex\\.pm/packages/accrue' .planning/MILESTONES.md`** returns **≥ 1** line in the **v1.16** section (first **80** lines of file is acceptable search window: `head -n 80 .planning/MILESTONES.md | rg -n 'https://hex\\.pm/packages/accrue'` returns **≥ 1**).
    - **`rg -n '@version' .planning/MILESTONES.md`** returns **≥ 1** line in the **v1.16** section (use **`head -n 25 .planning/MILESTONES.md | rg '@version'`** as proxy for header block).
  </acceptance_criteria>
  <action>
    In **`## v1.16 Integrator + proof continuity`**, delete the stale **`**Next:**`** line pointing to discuss/plan **59**. Replace with **two short lines**: (1) **Workspace vs Hex** — **`mix.exs` `@version` on the branch under test** may read **ahead** of **last published** on Hex; link **`.planning/PROJECT.md`** § Current State for the canonical pair. (2) **Next** — **`/gsd-execute-phase 61`** (after plans land) or equivalent maintainer-facing pointer consistent with **STATE.md**. Append **Markdown links** to **`https://hex.pm/packages/accrue`** and **`https://hex.pm/packages/accrue_admin`** once in this section.
  </action>
  <verify>
    <automated>bash -lc '! rg -q "/gsd-discuss-phase 59" .planning/MILESTONES.md'</automated>
  </verify>
  <done>v1.16 milestone header is current.</done>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md — explicit two-authority lines (D-09)</name>
  <read_first>
    .planning/PROJECT.md
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>.planning/PROJECT.md</files>
  <acceptance_criteria>
    - **`rg -n 'Public Hex \\(last published\\):' .planning/PROJECT.md`** returns **≥ 1** line.
    - **`rg -n 'workspace.*@version|@version.*workspace' .planning/PROJECT.md`** returns **≥ 1** line (case-insensitive OK: use **`rg -ni 'workspace.*@version' .planning/PROJECT.md`**).
    - **`rg -n 'https://hex\\.pm/packages/accrue' .planning/PROJECT.md`** returns **≥ 1** line in **§ Current State** (file line range **45–60** acceptable: **`sed -n '45,60p' .planning/PROJECT.md' | rg -c 'https://hex\\.pm/packages/accrue'`** prints **≥ 1**).
  </acceptance_criteria>
  <action>
    Under **`## Current State`** (or adjacent **v1.16** intro if cleaner), ensure a **two-line** (or **two-bullet**) pattern states: (A) **install literals / `{:accrue, "~> …"}`** follow **`mix.exs` `@version`** on the branch (**`verify_package_docs`** enforces); (B) **“Public Hex (last published)”** tracks registry reality and updates on **publish** / **HYG**, not random doc commits. If already present verbatim, adjust only for clarity **without** deleting the dual-track story (**D-10**).
  </action>
  <verify>
    <automated>bash -lc 'rg -n "Public Hex" .planning/PROJECT.md | head -1'</automated>
  </verify>
  <done>PROJECT Current State reflects INT-09.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/ci/README.md — INT-09 registry row</name>
  <read_first>
    scripts/ci/README.md
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>scripts/ci/README.md</files>
  <acceptance_criteria>
    - **`rg -n '\\| INT-09 \\|' scripts/ci/README.md`** returns **≥ 1** line.
    - INT-09 row mentions **`verify_package_docs.sh`** in the primary scripts cell.
    - INT-09 row mentions **`.planning/PROJECT.md`** or **`.planning/MILESTONES.md`** (either or both).
    - INT-09 row’s **Phase VERIFICATION owner** cell equals **`.planning/phases/61-root-verify-hops-hex-doc-ssot/61-VERIFICATION.md`**.
  </acceptance_criteria>
  <action>
    Add a new INT gates table row for **INT-09** immediately **after** the **INT-08** row added by **61-01**. Primary scripts: **`verify_package_docs.sh`** as the enforcer for **`first_hour`**, **`accrue/README.md`**, **`accrue_admin/README.md`** version pins; planning files are **manual** mirror updates — state that in the cell. ExUnit: **`package_docs_verifier_test.exs`**.
  </action>
  <verify>
    <automated>bash -lc 'rg -n "INT-09" scripts/ci/README.md'</automated>
  </verify>
  <done>INT-09 contributor map row present.</done>
</task>

<task type="auto">
  <name>Task 4: CONTRIBUTING.md — pre-publish deps sharp edge (D-12)</name>
  <read_first>
    CONTRIBUTING.md
    accrue/guides/first_hour.md
    .planning/phases/61-root-verify-hops-hex-doc-ssot/61-CONTEXT.md
  </read_first>
  <files>CONTRIBUTING.md</files>
  <acceptance_criteria>
    - **`rg -n 'path:' CONTRIBUTING.md`** returns **≥ 1** line in the paragraph or bullet you add (literal **`path:`** substring for **`path:`** deps).
    - Same **50-line window** around the new text (use **`rg -n -C3 'path:' CONTRIBUTING.md`**) contains **at least one** of **`git ref`**, **`git `**, or **`wait for publish`** (ASCII case-insensitive: **`rg -ni 'git ref|wait for publish' CONTRIBUTING.md`** after edit).
    - **`rg -ni 'Hex.*deps|deps.*Hex' CONTRIBUTING.md`** returns **≥ 1** line.
  </acceptance_criteria>
  <action>
    Near existing **dependency / docs preflight** guidance (search **`verify_package_docs`** in **`CONTRIBUTING.md`**), add **≤ 3 sentences** explaining: after **`@version`** bumps on **`main`** **before** a Hex publish, **`mix deps.get` from Hex-only** pins may fail — use **`path:`** / **git** dependency **or** wait for publish. Do not contradict **`first_hour`** banner semantics.
  </action>
  <verify>
    <automated>bash -lc 'rg -ni "path:" CONTRIBUTING.md | head -1'</automated>
  </verify>
  <done>Sharp edge documented for contributors.</done>
</task>

<task type="auto">
  <name>Task 5: first_hour.md — verify INT-09 banner (no-op if already satisfied)</name>
  <read_first>
    accrue/guides/first_hour.md
    scripts/ci/verify_package_docs.sh
  </read_first>
  <files>accrue/guides/first_hour.md</files>
  <acceptance_criteria>
    - **`rg -n 'Hex vs' accrue/guides/first_hour.md`** returns **≥ 1** line inside a blockquote (line starts with **`>`** and contains **`Hex vs`** — verify with **`rg -n '^>.*Hex vs' accrue/guides/first_hour.md`** returning **≥ 1**).
    - **`bash scripts/ci/verify_package_docs.sh`** exits **0**.
  </acceptance_criteria>
  <action>
    Confirm **`first_hour.md`** still contains the **`> **Hex vs `main`:**`** banner and **`{:accrue, \"~> …\"}`** pins driven by **`verify_package_docs`**. If the banner is missing or contradicted, restore per **Phase 59** patterns; otherwise **no prose change** required.
  </action>
  <verify>
    <automated>bash scripts/ci/verify_package_docs.sh</automated>
  </verify>
  <done>First Hour remains verifier-aligned.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/verify_package_docs.sh` exits **0** (planning-only edits should not break existing needles).
- `bash scripts/ci/verify_verify01_readme_contract.sh` exits **0**.
</verification>

<success_criteria>
- **INT-09** row exists in **`scripts/ci/README.md`** alongside **INT-08**.
- **MILESTONES** v1.16 header has no stale **59** pointers; includes Hex links + `@version` honesty.
- **CONTRIBUTING** documents the **pre-publish** deps edge with **`path:`** / **git** / **wait** vocabulary.
</success_criteria>

## PLANNING COMPLETE

---
phase: 63-p0-integrator-verify-docs
plan: 03
type: execute
wave: 2
depends_on: ["63-01-PLAN.md", "63-02-PLAN.md"]
files_modified:
  - .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INT-10]
security_enforcement: enabled
tags: [INT-10, verification, governance, friction-inventory]
must_haves:
  truths:
    - "63-VERIFICATION.md exists with traceability rows for v1.17-P0-001, v1.17-P0-002, and INT-10 rollup"
    - "Friction inventory P0 rows show closure (or signed downgrade per D-03b) without breaking verify_v1_17_friction_research_contract.sh"
    - "REQUIREMENTS.md INT-10 checkbox checked when evidence complete"
  artifacts:
    - path: ".planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md"
      provides: "Phase 63 verification / traceability SSOT"
      contains: "v1.17-P0-001"
      min_lines: 40
---

<objective>
Finish **INT-10** governance: author **`63-VERIFICATION.md`**, update **`.planning/research/v1.17-FRICTION-INVENTORY.md`** rows **v1.17-P0-001** / **v1.17-P0-002** with disposition, and tick **`.planning/REQUIREMENTS.md`** **INT-10** when merge-blocking proof is recorded per **63-CONTEXT.md** D-03 / D-04.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-63-02 | Maintainer notes in inventory paste tokens or secrets | medium | Notes cite PR numbers, commit SHAs, verifier names — no `sk_live`, no `whsec_` production values |
| T-63-05 | Accidental relaxation of friction contract needles | high | After every inventory edit run `verify_v1_17_friction_research_contract.sh`; never remove P0 rows without milestone-wide **REQUIREMENTS** / **ROADMAP** coordination per D-03c |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/63-p0-integrator-verify-docs/63-CONTEXT.md
@.planning/phases/63-p0-integrator-verify-docs/63-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<task type="auto">
  <name>Task 1: Author 63-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/63-p0-integrator-verify-docs/63-CONTEXT.md (D-04)
    - .planning/phases/62-friction-triage-north-star/62-VERIFICATION.md
    - .planning/research/v1.17-FRICTION-INVENTORY.md (Backlog — INT-10)
  </read_first>
  <files>
    - .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md
  </files>
  <action>
    Create **`.planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`** with:

    1. **Title** and short **scope** paragraph pointing to **`### Backlog — INT-10 (Phase 63)`** in **`v1.17-FRICTION-INVENTORY.md`** (relative link).

    2. A **Markdown table** with columns: **Row ID** | **Acceptance one-liner** | **Merge-blocking proof** | **CI vs manual** | **Closure status**.

    3. **One data row each** for **`v1.17-P0-001`**, **`v1.17-P0-002`**, plus a **rollup** row for **INT-10**.

    4. **Merge-blocking proof** cells must name **concrete commands** (e.g. `bash scripts/ci/verify_package_docs.sh`, `bash scripts/ci/accrue_host_uat.sh`, relevant `mix test ...` paths) tied to work from **63-01** / **63-02**.

    5. Keep total document **lean** (D-04a): no full **`ci.yml`** transcription.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && test -f .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`
    - `rg -qF 'v1.17-P0-001' .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`
    - `rg -qF 'v1.17-P0-002' .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`
    - `rg -qF 'INT-10' .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`
    - `rg -qF 'verify_package_docs.sh' .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`
    - `rg -qF 'v1.17-FRICTION-INVENTORY.md' .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`
  </acceptance_criteria>
  <done>63-VERIFICATION.md authored with traceability table.</done>
</task>

<task type="auto">
  <name>Task 2: Update friction inventory P0 rows (closure notes)</name>
  <read_first>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
    - scripts/ci/verify_v1_17_friction_research_contract.sh
    - .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md (after Task 1)
  </read_first>
  <files>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
  </files>
  <action>
    For table rows **`v1.17-P0-001`** and **`v1.17-P0-002`**:

    1. Set **`status`** column from **`open`** to **`closed`** for each row once evidence is merged (inventory uses lowercase **`open`** / **`closed`** in the status column).

    2. Append to **`notes`** a **maintainer-signed** one- or two-line summary (date **2026-04-23** or execution date) citing **what shipped**: First Hour/README work (**63-01**) and/or host log prefixes (**63-02**), and pointer to **`63-VERIFICATION.md`**.

    3. **Do not** change column count, remove **`| INT-10 |`**, remove **`| →63 |`**, alter **`### Backlog — INT-10`**, or change the **four-row** inventory layout.

    4. If **downgrading** instead of closing (only if truly warranted): follow **D-03b** mini-ADR in **`notes`** — otherwise prefer **closed** after successful implementation.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_v1_17_friction_research_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_v1_17_friction_research_contract.sh` exits 0
    - `rg -qF '| v1.17-P0-001 |' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `rg -qF '| v1.17-P0-002 |' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `grep -F '| v1.17-P0-001 |' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -q '| INT-10 |' && grep -F '| v1.17-P0-001 |' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -q '| →63 |'`
    - `grep -F '| v1.17-P0-002 |' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -q '| INT-10 |' && grep -F '| v1.17-P0-002 |' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -q '| →63 |'`
    - `grep -F '| v1.17-P0-001 |' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -q '| closed |'`
    - `grep -F '| v1.17-P0-002 |' .planning/research/v1.17-FRICTION-INVENTORY.md | rg -q '| closed |'`
  </acceptance_criteria>
  <done>Inventory rows closed with signed notes; contract script green.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md — INT-10 checkbox</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    When **63-01**, **63-02**, and **Task 1–2** of this plan are **actually satisfied** in the branch (docs + scripts + inventory + **63-VERIFICATION.md**):

    Change the **INT-10** line from `- [ ]` to `- [x]` in **`.planning/REQUIREMENTS.md`**.

    If work is split across PRs, **do not** check the box until the **final** PR merges the inventory closure — executor must respect real repo state.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg '\*\*INT-10\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '^- \[x\] \*\*INT-10\*\*:' .planning/REQUIREMENTS.md` after full phase completion
    - If checkbox intentionally remains open mid-flight: commit message must state **deferral** — prefer **not** leaving partial state; gate this task on prior tasks’ green verifiers
  </acceptance_criteria>
  <done>INT-10 requirement checked when evidence landed.</done>
</task>

<task type="auto">
  <name>Task 4: Friction contract ExUnit smoke</name>
  <read_first>
    - accrue/test/accrue/docs/v1_17_friction_research_contract_test.exs
  </read_first>
  <files>
    - accrue/test/accrue/docs/v1_17_friction_research_contract_test.exs
  </files>
  <action>
    Run tests. Update **only if** inventory anchors or script paths changed in a way that requires assertion updates (unlikely for notes-only edits).

    Otherwise **no file change** — verification is `mix test` green.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` exits 0
  </acceptance_criteria>
  <done>Contract test green.</done>
</task>

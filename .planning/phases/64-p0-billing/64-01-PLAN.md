---
phase: 64-p0-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/64-p0-billing/64-VERIFICATION.md
  - .planning/research/v1.17-FRICTION-INVENTORY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [BIL-03]
security_enforcement: enabled
tags: [billing, BIL-03, v1.17, friction-inventory, certification]
must_haves:
  truths:
    - "`64-VERIFICATION.md` matches the **63** verification **family** (scope + anchor + table + proof column) for **BIL-03** only"
    - "`### Backlog — BIL-03` gains a **maintainer-signed** certification line pointing at `64-VERIFICATION.md` without touching the **FRG-01** four-row pipe table"
    - "`bash scripts/ci/verify_v1_17_friction_research_contract.sh` and `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` exit 0 after edits"
  artifacts:
    - path: ".planning/phases/64-p0-billing/64-VERIFICATION.md"
      provides: "Phase 64 merge-facing verification SSOT"
      contains: "### Backlog — BIL-03"
      min_lines: 12
---

<objective>
Close **BIL-03** on the **empty billing P0 queue** path: ship **`64-VERIFICATION.md`**, sign the **inventory** **`### Backlog — BIL-03`** subsection per **64-CONTEXT.md** **D-01a**/**D-02a**, and flip **`REQUIREMENTS.md`** + **`STATE.md`** to reflect **BIL-03** satisfied **without** **`Accrue.Billing` / Fake / CHANGELOG / telemetry** churn (**D-03**).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-64-01 | Stripe or `whsec_*` shaped literals pasted into `.planning/` prose | high | Inventory edits are certification text only; do not paste secrets; grep self-check stays clean |
| T-64-02 | Accidental edit to **FRG-01** pipe table breaks `verify_v1_17_friction_research_contract.sh` (expects **4** data rows, **2** P0) | high | Restrict edits to **`### Backlog — BIL-03 (Phase 64)`** bullets only; run friction script after save |
| T-64-03 | Drift between **ROADMAP** anchor and inventory heading | medium | Do not rename `### Backlog — BIL-03 (Phase 64)` heading; script greps substring `### Backlog — BIL-03` |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/64-p0-billing/64-CONTEXT.md
@.planning/phases/64-p0-billing/64-RESEARCH.md
@.planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md
@.planning/research/v1.17-FRICTION-INVENTORY.md
</context>

<task type="auto">
  <name>Task 1: Add `64-VERIFICATION.md` (63-family)</name>
  <read_first>
    - .planning/phases/63-p0-integrator-verify-docs/63-VERIFICATION.md
    - .planning/phases/64-p0-billing/64-CONTEXT.md (D-01b, D-02a)
    - .planning/research/v1.17-FRICTION-INVENTORY.md (only the `### Backlog — BIL-03` heading for anchor text)
  </read_first>
  <files>
    - .planning/phases/64-p0-billing/64-VERIFICATION.md
  </files>
  <action>
    Create **`.planning/phases/64-p0-billing/64-VERIFICATION.md`** with this exact structure (adjust only `verified` date string when closing later if needed):

    1. YAML frontmatter (first line `---`, then):
       - `status: pending`
       - `phase: 64-p0-billing`
       - `verified: ""`
       - closing `---`

    2. H1: `# Phase 64 — P0 billing verification`

    3. One scope paragraph starting with **`Scope:`** stating closure of **BIL-03** for **v1.17** using the inventory section at Markdown link **`[### Backlog — BIL-03 (Phase 64)](../../research/v1.17-FRICTION-INVENTORY.md#backlog--bil-03-phase-64)`** (same relative link style as **63-VERIFICATION.md**).

    4. A Markdown pipe **table** with header row exactly:
       `| Row ID | Acceptance one-liner | Merge-blocking proof | CI vs manual | Closure status |`
       and separator row under it.

    5. Exactly **one** data row for rollup **BIL-03**:
       - **Row ID:** `BIL-03`
       - **Acceptance one-liner:** States **both** branches: (a) if billing-tagged **P0** rows exist they ship bounded **Accrue.Billing**/**Fake**/**telemetry**/**CHANGELOG** per **BIL-03**; (b) this milestone **no such rows** — inventory documents empty queue + maintainer certification (**reference `64-VERIFICATION.md` + inventory** in the text).
       - **Merge-blocking proof:** `` `bash scripts/ci/verify_v1_17_friction_research_contract.sh`; `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` `` (same commands as **63-VERIFICATION.md** rollup row pattern).
       - **CI vs manual:** `CI + local manual`
       - **Closure status:** `closed`

    6. **Do not** add a second taxonomy or extra tables; keep file lean per **64-CONTEXT.md** **D-01b**.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && test -f .planning/phases/64-p0-billing/64-VERIFICATION.md && rg -qF '| BIL-03 |' .planning/phases/64-p0-billing/64-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/64-p0-billing/64-VERIFICATION.md`
    - `rg -qF '# Phase 64 — P0 billing verification' .planning/phases/64-p0-billing/64-VERIFICATION.md`
    - `rg -qF 'research/v1.17-FRICTION-INVENTORY.md#backlog--bil-03-phase-64' .planning/phases/64-p0-billing/64-VERIFICATION.md`
    - `rg -qF '| Row ID | Acceptance one-liner | Merge-blocking proof | CI vs manual | Closure status |' .planning/phases/64-p0-billing/64-VERIFICATION.md`
    - `rg -qF '| BIL-03 |' .planning/phases/64-p0-billing/64-VERIFICATION.md`
    - `rg -q 'verify_v1_17_friction_research_contract' .planning/phases/64-p0-billing/64-VERIFICATION.md`
    - `rg -q 'v1_17_friction_research_contract_test' .planning/phases/64-p0-billing/64-VERIFICATION.md`
  </acceptance_criteria>
  <done>Verification SSOT file exists and matches 63-family expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory — signed BIL-03 certification (subsection only)</name>
  <read_first>
    - .planning/research/v1.17-FRICTION-INVENTORY.md (full file — avoid touching FRG-01 table)
    - .planning/phases/64-p0-billing/64-CONTEXT.md (D-01a)
    - scripts/ci/verify_v1_17_friction_research_contract.sh (row-count expectations)
  </read_first>
  <files>
    - .planning/research/v1.17-FRICTION-INVENTORY.md
  </files>
  <action>
    Under **`### Backlog — BIL-03 (Phase 64)`**, **replace** the existing single italic bullet with **two** lines:

    1. A bold maintainer line exactly starting **`**Maintainer (2026-04-23):**`** stating that the **BIL-03** execution slice has **zero** billing **P0** rows for v1.17, and that closure evidence lives in **`.planning/phases/64-p0-billing/64-VERIFICATION.md`** (use that full repo-relative path in backticks).

    2. A second line: `- *(no P0 rows — queue empty for **→64** work this milestone)*`

    **Hard rule:** Do **not** add, remove, or edit any row inside the **FRG-01** pipe table (`| v1.17-P0-001 |` …). Do **not** add new **`v1.17-P0-*`** IDs for “fake” billing rows.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_v1_17_friction_research_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/verify_v1_17_friction_research_contract.sh` exits 0 from repo root
    - `test "$(grep -cE '^\| v1.17-P[012]-[0-9]{3} \|' .planning/research/v1.17-FRICTION-INVENTORY.md)" = 4`
    - `test "$(grep -cE '^\| v1.17-P0-' .planning/research/v1.17-FRICTION-INVENTORY.md)" = 2`
    - `rg -qF '**Maintainer (2026-04-23):**' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `rg -qF '.planning/phases/64-p0-billing/64-VERIFICATION.md' .planning/research/v1.17-FRICTION-INVENTORY.md`
    - `rg -n '### Backlog — BIL-03' .planning/research/v1.17-FRICTION-INVENTORY.md | head -1` exits 0
  </acceptance_criteria>
  <done>Inventory signed; friction contract still green.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS — mark BIL-03 satisfied</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/64-p0-billing/64-VERIFICATION.md (after Task 1)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    1. In **`### Billing (BIL)`**, change the **BIL-03** checklist item from `- [ ]` to `- [x]` (same line text otherwise).

    2. In the **Traceability** table, change the **BIL-03** row’s **Status** column from **`Pending`** to **`Complete`** (keep **Phase** column **`64`**).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "^- \\[x\\] \\*\\*BIL-03\\*\\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '^- \\[x\\] \\*\\*BIL-03\\*\\*' .planning/REQUIREMENTS.md`
    - `rg -q '^\\| BIL-03 \\| 64 \\| Complete \\|' .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>REQUIREMENTS reflects BIL-03 closure.</done>
</task>

<task type="auto">
  <name>Task 4: STATE — planning handoff for Phase 64</name>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/64-p0-billing/64-01-PLAN.md
  </read_first>
  <files>
    - .planning/STATE.md
  </files>
  <action>
    Update **`.planning/STATE.md`** narrative (YAML + body) minimally:

    1. **`last_activity`**: set to a line mentioning **`/gsd-execute-phase 64`** as next (or “plans ready”) **2026-04-23**.

    2. **`Current focus`** line: state **Phase 64** plans exist (**`64-01-PLAN.md`**) and **next** is **`/gsd-execute-phase 64`**.

    3. **`Current Position` / `Status` / `Last activity` / `Next:`** bullets: align with “discuss + plan complete; execute pending” — remove stale “ready for `/gsd-plan-phase 64`” wording.

    4. **`Current Planning Artifacts`** for **64**: list **`64-RESEARCH.md`**, **`64-VALIDATION.md`**, **`64-01-PLAN.md`**, and **`64-VERIFICATION.md`** (after Task 1).

    Do **not** change **`progress:`** YAML numbers unless you are sure counts match repo policy; prefer body-only updates if uncertain.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "64-01-PLAN|/gsd-execute-phase 64" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '64-01-PLAN\\.md' .planning/STATE.md`
    - `rg -q '/gsd-execute-phase 64' .planning/STATE.md`
    - `rg -q '64-VERIFICATION\\.md' .planning/STATE.md`
  </acceptance_criteria>
  <done>STATE points operators at execute-phase.</done>
</task>

<verification>
  <automated>
    cd /Users/jon/projects/accrue && bash scripts/ci/verify_v1_17_friction_research_contract.sh && cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs
  </automated>
</verification>

<success_criteria>
  - `64-VERIFICATION.md` exists with **BIL-03** rollup row and inventory anchor link
  - Inventory **BIL-03** subsection contains **Maintainer (2026-04-23)** line + `64-VERIFICATION.md` path without FRG-01 table churn
  - **`REQUIREMENTS.md`**: **BIL-03** checked and traceability **Complete**
  - Friction script + **`v1_17_friction_research_contract_test.exs`** green
</success_criteria>

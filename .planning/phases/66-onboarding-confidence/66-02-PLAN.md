---
phase: 66-onboarding-confidence
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - .planning/STATE.md
  - scripts/ci/verify_v1_17_friction_research_contract.sh
  - scripts/ci/README.md
  - accrue/test/accrue/docs/v1_17_friction_research_contract_test.exs
  - .planning/phases/66-onboarding-confidence/66-VERIFICATION.md
autonomous: true
requirements: [UAT-02, UAT-04]
security_enforcement: enabled
tags: [state, uat-04, friction-script, v1.18]
must_haves:
  truths:
    - "STATE.md deferred Phase 62 UAT gap is cleared or explicitly signed with rationale per ROADMAP success criterion 2"
    - "verify_v1_17_friction_research_contract.sh exits non-zero if .planning/milestones/v1.17-REQUIREMENTS.md is missing (after change)"
    - "66-VERIFICATION.md UAT-04 row cites the new invariant"
  artifacts:
    - path: "scripts/ci/verify_v1_17_friction_research_contract.sh"
      provides: "UAT-04 mechanical archive presence"
      contains: "v1.17-REQUIREMENTS.md"
      min_lines: 1
---

<objective>
Close **STATE.md** narrative gap for deferred UAT (**UAT-02** alignment with pointers) and add a **thin mechanical** check for **UAT-04** archive presence in **`verify_v1_17_friction_research_contract.sh`** with ExUnit mirror update, then refresh **66-VERIFICATION.md** **UAT-04** row proof strings.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-66-01 | Script change weakens friction contract (false greens) | high | Add only `test -f` (or bash `[[ -f ... ]]`) with clear stderr message; keep existing checks intact; run full script before commit |
| T-66-03 | STATE.md edit drops unrelated milestone links | low | `rg` diff — only touch **Deferred Items** / Phase 62 UAT paragraph sections identified in **66-CONTEXT.md** |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/66-onboarding-confidence/66-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<task type="auto">
  <name>Task 1: STATE.md — Phase 62 UAT deferral closure</name>
  <read_first>
    - .planning/STATE.md
    - .planning/ROADMAP.md (Phase 66 success criteria)
    - .planning/phases/66-onboarding-confidence/66-CONTEXT.md
  </read_first>
  <files>
    - .planning/STATE.md
  </files>
  <action>
    1. Locate the **Deferred Items** (or equivalent) section that still frames **Phase 62** human UAT as open for v1.18.

    2. Either: **(a)** remove/replace with a line that UAT closure is tracked in **`66-VERIFICATION.md`** and Phase **66** execution, with date **2026-04-23**; or **(b)** if work is not done yet, keep deferral but add explicit pointer to **`66-VERIFICATION.md`** and phase **66** — choose **(a)** only when plans **66-01..03** are actually executed and verification rows are ready to close.

    3. Preserve **`Friction inventory (FRG-01):`** and **`North star`** pointer lines — **UAT-02** must still resolve to **`.planning/research/v1.17-FRICTION-INVENTORY.md`** substring after edit (`rg` proof).

    4. Update **Status / Next** lines at top of **STATE.md** to reflect **Phase 66** execution progress without contradicting **PROJECT.md** shipped FRG/INT/BIL/ADM claims (**UAT-04** semantic).
  </action>
  <verify>
    <automated>rg -n "v1\\.17-FRICTION-INVENTORY" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -Fq ".planning/research/v1.17-FRICTION-INVENTORY.md" .planning/STATE.md`
    - `rg -n "66-VERIFICATION|Phase 66|phase 66" .planning/STATE.md` exits 0
  </acceptance_criteria>
  <done>STATE reflects Phase 66 closure path; friction inventory pointer intact.</done>
</task>

<task type="auto">
  <name>Task 2: UAT-04 — archive file gate in friction script</name>
  <read_first>
    - scripts/ci/verify_v1_17_friction_research_contract.sh
    - accrue/test/accrue/docs/v1_17_friction_research_contract_test.exs
    - .planning/phases/66-onboarding-confidence/66-RESEARCH.md (§3 UAT-04 gap)
  </read_first>
  <files>
    - scripts/ci/verify_v1_17_friction_research_contract.sh
    - accrue/test/accrue/docs/v1_17_friction_research_contract_test.exs
  </files>
  <action>
    1. In **`scripts/ci/verify_v1_17_friction_research_contract.sh`**, after repo-root resolution and **before** final success exit, add a check: if **`.planning/milestones/v1.17-REQUIREMENTS.md`** is not a regular file, print **`ERROR:`** line naming the missing path and exit **1**.

    2. Keep stderr/stdout final success token **`verify_v1_17_friction_research_contract: OK`** unchanged when all checks pass.

    3. If **ExUnit** test only asserts exit 0, no change needed; if it snapshots stderr, update expected substring.

    4. Update **`scripts/ci/README.md`** INT / friction row with one clause that archive file presence is now in this script (if a row exists — optional single sentence).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && bash scripts/ci/verify_v1_17_friction_research_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -Fq "v1.17-REQUIREMENTS.md" scripts/ci/verify_v1_17_friction_research_contract.sh`
    - `bash scripts/ci/verify_v1_17_friction_research_contract.sh` exits 0 from repo root
    - `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` exits 0
  </acceptance_criteria>
  <done>UAT-04 mechanical guard shipped; ExUnit green.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh 66-VERIFICATION UAT-04 row</name>
  <read_first>
    - .planning/phases/66-onboarding-confidence/66-VERIFICATION.md
  </read_first>
  <files>
    - .planning/phases/66-onboarding-confidence/66-VERIFICATION.md
  </files>
  <action>
    In the **UAT-04** row, set **Merge-blocking proof** to include **`bash scripts/ci/verify_v1_17_friction_research_contract.sh`** (now implies archive exists). Set **Automation** to cite **`docs-contracts-shift-left`** + step name **`v1.17 friction + north-star SSOT contract`** (grep **`.github/workflows/ci.yml`** for exact string).
  </action>
  <verify>
    <automated>rg -n "UAT-04" .planning/phases/66-onboarding-confidence/66-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -A2 'UAT-04' .planning/phases/66-onboarding-confidence/66-VERIFICATION.md | rg -Fq 'verify_v1_17_friction_research_contract.sh'` exits 0
  </acceptance_criteria>
  <done>Verification row matches new script behavior.</done>
</task>

<verification>
- `bash scripts/ci/verify_v1_17_friction_research_contract.sh` exits 0
- `cd accrue && mix test test/accrue/docs/v1_17_friction_research_contract_test.exs` exits 0
</verification>

<success_criteria>
- **UAT-04** has mechanical CI proof via friction script
- **STATE.md** updated per ROADMAP criterion 2 intent
</success_criteria>

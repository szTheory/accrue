---
phase: 67-proof-contracts
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PRF-01, PRF-02]
tags: [proof-contracts, verify_adoption_proof_matrix, v1.19]
---

# Plan 67-01 — Proof contracts

## Objective

Implement **PRF-01** and **PRF-02**: extend **`verify_adoption_proof_matrix.sh`** (and add or tighten **ExUnit** coverage only if needed) so **ORG-09** / layering / archetype headings in the matrix cannot drift from merge-blocking needles; add contributor triage text to **`scripts/ci/README.md`**.

## Tasks

1. **Inventory** — List every `require_substring` in **`verify_adoption_proof_matrix.sh`** and map each to a stable anchor in **`adoption-proof-matrix.md`** (heading, table cell id, or explicit glossary line). Flag redundant or overly brittle needles.
2. **PRF-01** — Add high-signal needles for any **ORG-09** / archetype / matrix table rows called out in **v1.17-P1-001** that are not yet enforced (e.g. stable row ids or section anchors agreed in discuss-phase). Prefer **short literal** checks that fail with actionable stderr. If **ExUnit** duplicates matrix strings, consolidate or cross-check so one edit updates both.
3. **PRF-02** — In **`scripts/ci/README.md`**, under the **`verify_adoption_proof_matrix.sh`** / adoption-proof triage area: document **matrix SSOT**, **script as regression harness**, and **single change set** rule; link **`examples/accrue_host/docs/adoption-proof-matrix.md`**.
4. **Verify** — Run `bash scripts/ci/verify_adoption_proof_matrix.sh` and relevant `mix test` for touched **`accrue`** doc tests; ensure **`docs-contracts-shift-left`** contract still passes locally.

## Non-goals

- Publishing Hex (**Phase 68**).
- Rewriting the matrix for editorial polish without a failing contract driving the change.

## Completion

- Update **`67-VERIFICATION.md`** (create if missing) with **PRF-01** / **PRF-02** rows and proof commands.
- Check **`REQUIREMENTS.md`** traceability for Phase **67** when closing.

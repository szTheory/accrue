---
phase: 67-proof-contracts
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PRF-01, PRF-02]
autonomous: true
files_modified:
  - scripts/ci/verify_adoption_proof_matrix.sh
  - scripts/ci/README.md
  - .planning/phases/67-proof-contracts/67-VERIFICATION.md
tags: [proof-contracts, verify_adoption_proof_matrix, v1.19, PRF-01, PRF-02]
---

# Plan 67-01 — Proof contracts (matrix ↔ verifier ↔ triage)

<threat_model>
  <summary>ASVS L1–aligned; phase is docs + CI bash gates only. No PII or secrets in scope.</summary>
  <threat id="T-67-01">False CI confidence — matrix prose drops merge-blocking Layer C semantics while bash needles still pass.</threat>
  <mitigation>Add stable <code>require_substring</code> needles for every Layer C script basename the matrix promises (including <code>verify_core_admin_invoice_verify_ids.sh</code>); run script + ExUnit before merge.</mitigation>
  <threat id="T-67-02">Contributor confusion — stderr prefix or job id unclear; triage doc omits SSOT path or co-update rule.</threat>
  <mitigation>README triage names full matrix path, <code>docs-contracts-shift-left</code>, <code>verify_adoption_proof_matrix:</code> prefix, and same-PR matrix + script + literal-bearing ExUnit rule with link to matrix.</mitigation>
  <threat id="T-67-03">Accidental dual maintenance — duplicate needle lists in Elixir and bash diverge (<code>v1.17-P1-001</code> class).</threat>
  <mitigation>Keep needles only in <code>verify_adoption_proof_matrix.sh</code>; <code>OrganizationBillingOrg09MatrixTest</code> stays a thin shell-out harness unless bash cannot express a future invariant.</mitigation>
</threat_model>

## Objective

Close **PRF-01** and **PRF-02** for milestone **v1.19**: tighten merge-blocking **taxonomy / archetype / row-id** alignment between **`examples/accrue_host/docs/adoption-proof-matrix.md`** and **`scripts/ci/verify_adoption_proof_matrix.sh`**, and extend **`scripts/ci/README.md`** triage so contributors know the SSOT path, CI job home, stderr prefix, and **single change set** (matrix + script + any ExUnit that embeds matrix literals). Update **`67-VERIFICATION.md`** with machine-cited proof commands.

## Tasks

<task id="67-01-01">
  <read_first>
    <file>examples/accrue_host/docs/adoption-proof-matrix.md</file>
    <file>scripts/ci/verify_adoption_proof_matrix.sh</file>
    <file>.planning/phases/67-proof-contracts/67-RESEARCH.md</file>
  </read_first>
  <action>
    In **`scripts/ci/verify_adoption_proof_matrix.sh`**, after the existing Layer C script needles (immediately after the line that requires **`verify_verify01_readme_contract.sh`**), add **one** new **`require_substring`** call:

    - **Needle:** `verify_core_admin_invoice_verify_ids.sh` (exact basename string as in the matrix Layer C sentence).
    - **Label:** `Layer C verify_core_admin_invoice_verify_ids script name in matrix` (or equivalent short human label consistent with neighboring labels).

    Do **not** reorder existing checks unless required for readability; preserve **`verify_adoption_proof_matrix:`** stderr prefix pattern on failures.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'require_substring "verify_core_admin_invoice_verify_ids\\.sh"' scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.</criterion>
    <criterion>Command `bash scripts/ci/verify_adoption_proof_matrix.sh` run from repository root exits **0** and stdout contains exact substring **`verify_adoption_proof_matrix: OK`**.</criterion>
  </acceptance_criteria>
</task>

<task id="67-01-02">
  <read_first>
    <file>examples/accrue_host/docs/adoption-proof-matrix.md</file>
    <file>scripts/ci/verify_adoption_proof_matrix.sh</file>
    <file>.planning/phases/67-proof-contracts/67-CONTEXT.md</file>
  </read_first>
  <action>
    Still in **`scripts/ci/verify_adoption_proof_matrix.sh`**, add **`require_substring`** lines so the **primary archetype** row’s guide cross-ref taxonomy cannot silently disappear:

    - **Needle:** `ORG-05` — **Label:** e.g. `ORG-05 taxonomy token in matrix` (must remain unique among labels in this file).
    - **Needle:** `ORG-06` — **Label:** e.g. `ORG-06 taxonomy token in matrix`.

    Place these adjacent to the existing **`ORG-07`** / **`ORG-08`** checks (before or after that pair—pick the order that reads top-to-bottom vs matrix). If either substring is already enforced by a broader needle (it is not today), skip duplicate—**must** end with exactly **one** `require_substring` line per needle above.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'require_substring "ORG-05"' scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.</criterion>
    <criterion>Command `rg -n 'require_substring "ORG-06"' scripts/ci/verify_adoption_proof_matrix.sh` exits **0**.</criterion>
    <criterion>Command `bash scripts/ci/verify_adoption_proof_matrix.sh` from repository root exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="67-01-03">
  <read_first>
    <file>scripts/ci/README.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/67-proof-contracts/67-CONTEXT.md</file>
  </read_first>
  <action>
    Under **`### Triage: verify_adoption_proof_matrix.sh`** in **`scripts/ci/README.md`**, expand the bullet list (keep existing stderr-prefix guidance) so **all** of the following appear as **visible prose** (not only inside table cells):

    1. **Full SSOT path** as a literal path string: **`examples/accrue_host/docs/adoption-proof-matrix.md`** (the exact characters, so `rg -F` finds it).
    2. **CI job id** literal: **`docs-contracts-shift-left`** (spell exactly as in `.github/workflows/ci.yml`).
    3. **Co-update rule** in explicit words: any intentional change to adoption-proof matrix taxonomy / archetype / row-level text that affects needles **must** land in the **same PR / commit** as updates to **`scripts/ci/verify_adoption_proof_matrix.sh`** and to **any ExUnit test file that embeds matrix literals** (name **`accrue/test/accrue/docs/organization_billing_org09_matrix_test.exs`** as the thin harness that shells out—note it does not duplicate needles today).
    4. **Markdown link** to the matrix for click-through from GitHub: use repo-relative link syntax pointing at **`examples/accrue_host/docs/adoption-proof-matrix.md`** (from **`scripts/ci/README.md`** the path is **`../../examples/accrue_host/docs/adoption-proof-matrix.md`** — e.g. `[adoption-proof-matrix.md](../../examples/accrue_host/docs/adoption-proof-matrix.md)`).

    Optional but encouraged: one short clause that **`verify_adoption_proof_matrix.sh`** is the **canonical substring list** (needles live here, not duplicated in Elixir).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -Fq 'examples/accrue_host/docs/adoption-proof-matrix.md' scripts/ci/README.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'docs-contracts-shift-left' scripts/ci/README.md` exits **0** (scoped: entire file is acceptable).</criterion>
    <criterion>Command `rg -Fq 'organization_billing_org09_matrix_test.exs' scripts/ci/README.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'verify_adoption_proof_matrix.sh' scripts/ci/README.md` exits **0**.</criterion>
    <criterion>Command `rg -n '\\]\\([^)]*adoption-proof-matrix\\.md' scripts/ci/README.md` exits **0** (at least one Markdown inline link whose URL contains **`adoption-proof-matrix.md`**).</criterion>
  </acceptance_criteria>
</task>

<task id="67-01-04">
  <read_first>
    <file>.planning/phases/67-proof-contracts/67-VERIFICATION.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>
    Edit **`.planning/phases/67-proof-contracts/67-VERIFICATION.md`**:

    1. Keep YAML frontmatter **`phase: 67-proof-contracts`**; set **`status`** to **`complete`** only when executor has finished all tasks **67-01-01**–**67-01-03** and verification commands pass—otherwise leave **`pending`** until closeout.

    2. Replace **Merge-blocking proof** TBD text for **PRF-01** with exact commands:
       - **`bash scripts/ci/verify_adoption_proof_matrix.sh`**
       - **`cd accrue && mix test test/accrue/docs/organization_billing_org09_matrix_test.exs`** (executor runs from repo root; document as two-step or single `sh -c` — literal substring **`organization_billing_org09_matrix_test.exs`** must appear).

    3. For **PRF-02**, cite **`scripts/ci/README.md`** subsection anchor: file path plus grep discovery **`### Triage: verify_adoption_proof_matrix.sh`** (literal heading text in file).

    4. Set **Closure** column to **`complete`** for both rows when evidence exists; until then **`pending`**.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -Fq 'bash scripts/ci/verify_adoption_proof_matrix.sh' .planning/phases/67-proof-contracts/67-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'organization_billing_org09_matrix_test.exs' .planning/phases/67-proof-contracts/67-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq '### Triage: verify_adoption_proof_matrix.sh' .planning/phases/67-proof-contracts/67-VERIFICATION.md` exits **0**.</criterion>
    <criterion>Command `rg -Fq 'scripts/ci/README.md' .planning/phases/67-proof-contracts/67-VERIFICATION.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

Run from repository root **`/Users/jon/projects/accrue`** (or any clone; paths below assume root):

```bash
bash scripts/ci/verify_adoption_proof_matrix.sh
```

```bash
cd accrue && mix test test/accrue/docs/organization_billing_org09_matrix_test.exs
```

Optional full doc-contracts parity (matches CI job **`docs-contracts-shift-left`** ordering context—run if other shift-left scripts were touched; not required if only matrix/script/README/verification changed and host workflows untouched):

```bash
bash scripts/ci/verify_package_docs.sh
```

Executor must re-run the **acceptance_criteria** `rg` / `bash` checks in tasks **67-01-01**–**67-01-04** and confirm exit code **0** each.

## must_haves

- **PRF-01:** **`verify_adoption_proof_matrix.sh`** asserts presence in the matrix of **Layer C** basename **`verify_core_admin_invoice_verify_ids.sh`** plus **ORG-05** / **ORG-06** taxonomy tokens alongside existing ORG-09 / layering / archetype needles; merge-blocking script exits **0** on current **`main`** matrix.
- **PRF-02:** **`scripts/ci/README.md`** triage subsection explicitly names **`examples/accrue_host/docs/adoption-proof-matrix.md`**, job id **`docs-contracts-shift-left`**, co-update rule covering **matrix + script + ExUnit literals same change set**, links to matrix, and references **`organization_billing_org09_matrix_test.exs`**.
- **No dual needle lists in Elixir** for this gate—bash remains sole substring owner per **67-CONTEXT.md** D-01.
- **`67-VERIFICATION.md`** lists concrete **`bash` / `mix test`** commands for **PRF-01** and pointer text for **PRF-02** closure evidence.

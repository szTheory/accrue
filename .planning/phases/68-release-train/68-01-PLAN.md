---
phase: 68-release-train
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REL-01, REL-02]
autonomous: true
files_modified:
  - RELEASING.md
tags: [release-train, v1.19, REL-01, REL-02]
---

# Plan 68-01 — Runbook fidelity (REL-01) + changelog ship boundary (REL-02)

<threat_model>
  <summary>ASVS L1; documentation-only edits. No new secrets, tokens, or workflow credential surfaces.</summary>
  <threat id="T-68-01">Contributor or maintainer follows wrong merge/publish story and skips gates or reverses good core on admin failure.</threat>
  <mitigation>Anchor prose to literal workflow job names and <code>needs:</code> edges from <code>release-please.yml</code>; restate partial-publish recovery already in <code>RELEASING.md</code> without contradicting D-03.</mitigation>
  <threat id="T-68-02">Stale “Last verified” metadata claims automation alignment that drifted.</threat>
  <mitigation>Bump verification date when touching runbook for this phase.</mitigation>
  <threat id="T-68-03">Changelog “Unreleased” gap at ship boundary — integrators read wrong upgrade surface.</threat>
  <mitigation>Add explicit ship-boundary subsection referencing both package changelog paths and Release Please ownership.</mitigation>
</threat_model>

## Objective

Satisfy **REL-01** and **REL-02** preparation in-repo: **`RELEASING.md`** must state the **default human merge** path, cite **ordered Hex publish** from **`release-please.yml`**, and document **changelog / Unreleased** expectations at the **Release Please** ship boundary. Registry and tag proof after publish are **Plan 68-02** (**REL-03**) + maintainer steps.

## Tasks

<task id="68-01-01">
  <read_first>
    <file>RELEASING.md</file>
    <file>.github/workflows/release-please.yml</file>
    <file>.planning/phases/68-release-train/68-CONTEXT.md</file>
  </read_first>
  <action>
    In **`RELEASING.md`**, inside the subsection **`### Release Please + Hex (linked automation)`** (after the existing bullet that says **`accrue_admin` publishes only after the `accrue` publish job succeeds**), add **one** new bullet (Markdown `-` line) containing **all** of the following exact substrings:

    1. **`publish-accrue-admin`**
    2. **`needs: [release, publish-accrue]`**
    3. **`release-please.yml`**

    Wording should state that this **`needs:`** edge is what enforces **`accrue` before `accrue_admin`** for linked publishes (paraphrase OK; substrings above must appear verbatim on that bullet line or split across continuation lines **without** breaking the substrings).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'publish-accrue-admin' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'needs: \[release, publish-accrue\]' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -n 'release-please.yml' RELEASING.md` exits **0** (allowed: multiple matches; at least one inside **`### Release Please + Hex (linked automation)`** section).</criterion>
  </acceptance_criteria>
</task>

<task id="68-01-02">
  <read_first>
    <file>RELEASING.md</file>
    <file>.planning/phases/68-release-train/68-CONTEXT.md</file>
  </read_first>
  <action>
    In **`RELEASING.md`**, under **`## Routine pre-1.0 linked releases (Release Please + Hex)`**, edit **step 4** (the step beginning **`4. Merge after checklist sign-off`**):

    - Insert as the **first sentence** of that step (before the existing **`or`** clause) the exact sentence:
      **`**Default (primary path):** merge the combined Release Please PR manually on GitHub when required checks are green.`**
    - Preserve the remainder of step 4 (the **`or` after review dispatch** branch and `scripts/ci/gh_merge_release_pr.sh` mention) unchanged except for grammar/punctuation needed to join sentences.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '**Default (primary path):** merge the combined Release Please PR manually on GitHub when required checks are green.' RELEASING.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

<task id="68-01-03">
  <read_first>
    <file>RELEASING.md</file>
    <file>release-please-config.json</file>
    <file>.planning/phases/68-release-train/68-CONTEXT.md</file>
  </read_first>
  <action>
    Append a **new** second-level Markdown heading and body to **`RELEASING.md`** placed **immediately before** the existing heading **`## Release verification lanes`**:

    1. Heading text **exactly:** `## Changelog ship boundary (Release Please)`
    2. Under it, add **at least three** `-` bullet lines covering, in plain language:
       - **Release Please** is the single writer for **`@version` bumps** in **`accrue/mix.exs`** and **`accrue_admin/mix.exs`** and for **new numbered** sections in **`accrue/CHANGELOG.md`** and **`accrue_admin/CHANGELOG.md`** (both paths must appear as literal substrings).
       - At the **merge/tag/publish boundary**, **`## Unreleased`** / **`## [Unreleased]`** must not be the only home for work that already appears under the shipped version section (describe the freeze/drain expectation from **`68-CONTEXT.md` D-04** — paraphrase OK).
       - Human polish belongs on the **open release PR** or in **conventional commit bodies**, not as a competing manual version block on **`main`**.
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -F '## Changelog ship boundary (Release Please)' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'accrue/CHANGELOG.md' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -F 'accrue_admin/CHANGELOG.md' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -F '## Release verification lanes' RELEASING.md` exits **0** (heading must still exist after insertion).</criterion>
  </acceptance_criteria>
</task>

<task id="68-01-04">
  <read_first>
    <file>RELEASING.md</file>
  </read_first>
  <action>
    In **`RELEASING.md`**, update the **“Last verified against … on … (UTC)”** paragraph near the top (currently references **`2026-04-22`**) so the date reads **`2026-04-23`** and the listed paths remain **`release-please-config.json`**, **`.release-please-manifest.json`**, and **`.github/workflows/release-please.yml`** (same three path substrings must still appear in that paragraph).
  </action>
  <acceptance_criteria>
    <criterion>Command `rg -n 'Last verified against' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -F '2026-04-23' RELEASING.md` exits **0**.</criterion>
    <criterion>Command `rg -F '.release-please-manifest.json' RELEASING.md` exits **0**.</criterion>
  </acceptance_criteria>
</task>

## Verification

From repository root:

```bash
cd accrue && mix test --warnings-as-errors
```

```bash
bash scripts/ci/verify_package_docs.sh
```

Re-run every **`acceptance_criteria`** `rg` command in tasks **68-01-01**–**68-01-04** and confirm exit code **0**.

## must_haves

- **REL-01:** **`RELEASING.md`** explicitly documents default **manual merge**, optional **`workflow_dispatch`** merge path, and cites **`publish-accrue-admin`** + **`needs: [release, publish-accrue]`** + **`release-please.yml`** for **core-before-admin** Hex ordering.
- **REL-02:** New **`## Changelog ship boundary (Release Please)`** section names both changelog paths and states **Unreleased** / ship-boundary expectations consistent with **`68-CONTEXT.md` D-04**.
- **Last verified** metadata updated to **2026-04-23** while retaining the three automation file references.

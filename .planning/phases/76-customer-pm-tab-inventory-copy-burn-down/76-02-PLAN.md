---
phase: 76-customer-pm-tab-inventory-copy-burn-down
plan: 02
type: execute
wave: 2
depends_on: ["76-01-PLAN.md"]
files_modified:
  - accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/live/customer_live.ex
  - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  - examples/accrue_host/e2e/generated/copy_strings.json
  - .planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-VERIFICATION.md
autonomous: true
requirements: [ADM-14]
security_enforcement: enabled
tags: [admin, ADM-14, v1.24, Copy, LiveView]
must_haves:
  truths:
    - "New submodule **`AccrueAdmin.Copy.CustomerPaymentMethods`** (or chosen equivalent) holds PM-tab strings; **`AccrueAdmin.Copy`** exposes them via **`defdelegate`** per CONTEXT **D-04**–**D-05**"
    - "**Only** the **`\"payment_methods\"`** branch in **`customer_live.ex`** gains new Copy calls — no KPI/charges/subscription literal edits per **D-08**"
    - "**`customer_live_test.exs`** asserts PM-tab output using **`AccrueAdmin.Copy.*`** functions — no new raw duplicate English string for the same operator text per **D-11**"
    - "`76-VERIFICATION.md` inventory table **Copy-backed?** column updated to **yes** for migrated strings"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex"
      provides: "Tier A strings for customer PM tab"
      contains: "def customer_payment_methods"
      min_lines: 8
---

<objective>
Ship **ADM-14**: route customer **`payment_methods`** tab operator strings through **`AccrueAdmin.Copy`** (new submodule + facade), keep **`ax-*`** structure intact, extend **ExUnit** for the tab, refresh **`copy_strings.json`** when export surface changes, and reconcile **`76-VERIFICATION.md`** copy flags.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-76-10 | Scope creep: editing literals outside **`\"payment_methods\"`** branch | high | Code review + `git diff` bounded to named line region; reject unrelated tab edits |
| T-76-11 | XSS via copy functions | low | Copy returns plain strings; HEEx remains attribute-safe; no `raw/1` |
| T-76-12 | Drift between ExUnit expected text and Copy source | medium | Tests call the same **`Copy`** functions rendered in templates |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-CONTEXT.md
@.planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-PATTERNS.md
@.planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-VERIFICATION.md
</context>

<task type="auto">
  <name>Task 1: Add `Copy.CustomerPaymentMethods` + facade delegates</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/copy.ex
    - accrue_admin/lib/accrue_admin/copy/subscription.ex
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
  </read_first>
  <files>
    - accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </files>
  <action>
    1. Add **`accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex`** with `@moduledoc false` and **public** functions (names are implementer choice, but must cover these exact operator strings today):
       - Card section title equivalent to current **"Payment methods"**
       - Empty state equivalent to current **"No payment methods on file."**
       - Fallback label equivalent to current **"Payment method"** (single payment method row when brand/type absent)
       - If the mask **"·••••"** remains a separate literal in HEEx, either (a) expose a **`Copy`** function returning exactly **`"·••••"`** or (b) keep the mask literal in HEEx only — pick one approach and use it consistently in Task 2.

    2. In **`copy.ex`**: `alias AccrueAdmin.Copy.CustomerPaymentMethods` and add **`defdelegate`** lines for each new public function, grouped near other customer-related delegates if a group exists, else after **`Coupon`** / **`Connect`** block alphabetically.

    3. Do **not** add functions for deferred KPI/charges copy.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && test -f accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex && rg -q 'defdelegate' accrue_admin/lib/accrue_admin/copy.ex && rg -q 'CustomerPaymentMethods' accrue_admin/lib/accrue_admin/copy.ex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f accrue_admin/lib/accrue_admin/copy/customer_payment_methods.ex`
    - `rg -q 'defdelegate.*CustomerPaymentMethods' accrue_admin/lib/accrue_admin/copy.ex`
    - `mix compile` succeeds when run as: `cd accrue_admin && mix compile`
  </acceptance_criteria>
  <done>Copy submodule and facade compile.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `CustomerLive` payment_methods branch only</name>
  <read_first>
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
  </files>
  <action>
    In **`customer_live.ex`**, inside the **`&lt;% \"payment_methods\" -&gt;`** branch **only**, replace literals introduced in Plan **76-01** inventory with calls to **`Copy.*`** (via the new **`defdelegate`** names from Task 1). Preserve **`class=`** attributes and DOM structure. Do **not** edit other `case @tab` branches or KPI/meta strings in this task.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && mix compile</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '\"payment_methods\"' accrue_admin/lib/accrue_admin/live/customer_live.ex | head -1` exits 0
    - `rg -q 'Copy\\.' accrue_admin/lib/accrue_admin/live/customer_live.ex`
    - For each deferred string from **76-VERIFICATION.md** cross-tab section (**"No charges projected yet."**, subscriptions KPI delta, etc.), `rg -n 'No charges projected yet.' accrue_admin/lib/accrue_admin/live/customer_live.ex` still finds the line **unchanged** (literal still present at pre-phase location)
  </acceptance_criteria>
  <done>PM tab uses Copy; deferred literals untouched.</done>
</task>

<task type="auto">
  <name>Task 3: ExUnit — payment_methods tab + Copy alignment</name>
  <read_first>
    - accrue_admin/test/accrue_admin/live/customer_live_test.exs
    - accrue_admin/lib/accrue_admin/live/customer_live.ex
    - accrue_admin/lib/accrue_admin/copy.ex
  </read_first>
  <files>
    - accrue_admin/test/accrue_admin/live/customer_live_test.exs
  </files>
  <action>
    1. Add a test that mounts **`/billing/customers/#{customer.id}?tab=payment_methods`** using the existing fixture customer (has a Visa **4242** payment method from setup).

    2. Assert the rendered HTML includes text from **`AccrueAdmin.Copy`** for the **card title** and shows **last4** / brand-derived content from fixture data (use existing assigns — do not hardcode a second full English sentence that duplicates a **`Copy`** return value).

    3. Add a second test **or** setup variant for **empty** payment methods list (insert a bare customer without **`PaymentMethod`** rows) asserting the empty-state string using **`Copy`** function only (same pattern as **`Copy.customer_detail_no_invoices()`** test).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue/accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'tab=payment_methods' accrue_admin/test/accrue_admin/live/customer_live_test.exs`
    - `cd accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs` exits **0**
  </acceptance_criteria>
  <done>ExUnit covers PM tab with Copy-sourced expectations.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate `copy_strings.json` + refresh verification flags</name>
  <read_first>
    - accrue_admin/lib/mix/tasks/accrue_admin.export_copy_strings.ex
    - .planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-VERIFICATION.md
  </read_first>
  <files>
    - examples/accrue_host/e2e/generated/copy_strings.json
    - .planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-VERIFICATION.md
  </files>
  <action>
    1. Run: **`cd accrue_admin && mix accrue_admin.export_copy_strings --out ../examples/accrue_host/e2e/generated/copy_strings.json`**

    2. Update **`76-VERIFICATION.md`** inventory table: set **Copy-backed?** to **yes** for every string migrated in Task 2.

    3. If **`mix format`** is standard in this repo for touched Elixir files, run it on modified **`accrue_admin`** paths only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && test -f examples/accrue_host/e2e/generated/copy_strings.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f examples/accrue_host/e2e/generated/copy_strings.json`
    - `rg -q '| yes |' .planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-VERIFICATION.md` OR `rg -qF 'yes' .planning/phases/76-customer-pm-tab-inventory-copy-burn-down/76-VERIFICATION.md` in the Copy-backed column context (manual spot-check if table uses **Yes** capitalization — normalize to match existing verification docs style)
    - `cd accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs` still exits **0**
  </acceptance_criteria>
  <done>Export JSON and verification doc aligned.</done>
</task>

<verification>
  <automated>cd /Users/jon/projects/accrue/accrue_admin && mix test test/accrue_admin/live/customer_live_test.exs</automated>
</verification>

<success_criteria>
  - PM-tab strings centralized in **`AccrueAdmin.Copy.CustomerPaymentMethods`** + delegates
  - **`customer_live.ex`** **`payment_methods`** branch only changed for literals
  - ExUnit proves tab rendering; **`copy_strings.json`** regenerated when export list changes
  - **`76-VERIFICATION.md`** marks migrated rows Copy-backed
</success_criteria>

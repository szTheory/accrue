---
phase: 78-billing-portal-on-accrue-billing-telemetry-truth
plan: 02
type: execute
wave: 1
depends_on: ["78-01-PLAN.md"]
files_modified:
  - accrue/guides/telemetry.md
  - accrue/guides/operator-runbooks.md
  - accrue/CHANGELOG.md
autonomous: true
requirements: [BIL-05]
security_enforcement: enabled
tags: [BIL-05, telemetry, docs, changelog, runbook]
must_haves:
  truths:
    - "`guides/telemetry.md` documents `[:accrue, :billing, :billing_portal, :create]` and OTel name `accrue.billing.billing_portal.create`"
    - "Payment method attach/detach/set_default billing spans are documented consistently with `billing_span_coverage_test.exs`"
    - "`CHANGELOG.md` under `accrue` records the facade + docs"
  artifacts:
    - path: "accrue/guides/telemetry.md"
      provides: "Operator-facing billing portal span catalog"
      contains: "billing_portal"
      min_lines: 1
---

<objective>
Close **BIL-05**: update **`accrue/guides/telemetry.md`** and **`accrue/guides/operator-runbooks.md`** for the new billing-portal span and payment-method span inventory; extend **`accrue/CHANGELOG.md`**; optional **First Hour** pointer only if executor finds an obvious gap (otherwise skip per requirement optional clause).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-78-04 | Docs show example portal URLs in copy-paste snippets | medium | Use placeholder `https://example.com/return` only; never real Stripe portal URLs |
| T-78-05 | Runbook suggests logging session structs | low | Text says correlate via **`customer_id` / `operation` / `processor`** only |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/78-billing-portal-on-accrue-billing-telemetry-truth/78-RESEARCH.md
</context>

<task type="auto">
  <name>Task 1: telemetry.md — billing portal + payment_method spans</name>
  <read_first>
    - accrue/guides/telemetry.md
    - accrue/test/accrue/telemetry/billing_span_coverage_test.exs
    - accrue/lib/accrue/billing.ex
  </read_first>
  <files>
    - accrue/guides/telemetry.md
  </files>
  <action>
    In the **Span naming conventions (OpenTelemetry)** illustrative list (near **`accrue.billing.payment_method.list`**):

    1. Add bullet **`accrue.billing.billing_portal.create`** with prose that the telemetry tuple is **`[:accrue, :billing, :billing_portal, :create]`** from **`Accrue.Billing.create_billing_portal_session/2`** (Phase 78 / BIL-04).

    2. Add three bullets (or one grouped bullet) for **`accrue.billing.payment_method.attach`**, **`.detach`**, **`.set_default`** citing **`Accrue.Billing.attach_payment_method/3`**, **`detach_payment_method/2`**, **`set_default_payment_method/3`** and the exact **`[:accrue, :billing, :payment_method, …]`** tuples matching **`billing.ex`**.

    3. Update **Last reconciled** footnote line to **`2026-04-24 — Phase 78 BIL-05`** (or append that phrase to existing date line if project style prefers cumulative audit trail).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "billing_portal" accrue/guides/telemetry.md && rg -n "payment_method.attach" accrue/guides/telemetry.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -nF "[:accrue, :billing, :billing_portal, :create]" accrue/guides/telemetry.md`
    - `rg -nF "[:accrue, :billing, :payment_method, :attach]" accrue/guides/telemetry.md`
    - `rg -nF "[:accrue, :billing, :payment_method, :detach]" accrue/guides/telemetry.md`
    - `rg -nF "[:accrue, :billing, :payment_method, :set_default]" accrue/guides/telemetry.md`
  </acceptance_criteria>
  <done>Telemetry guide matches code + span coverage intent.</done>
</task>

<task type="auto">
  <name>Task 2: operator-runbooks.md cross-link</name>
  <read_first>
    - accrue/guides/operator-runbooks.md
    - accrue/guides/telemetry.md
  </read_first>
  <files>
    - accrue/guides/operator-runbooks.md
  </files>
  <action>
    In **`operator-runbooks.md`**, under the **Accrue layer** triage bullet list (first ~40 lines), add **one sentence** with a **relative markdown link** to **`telemetry.md`**: when investigating **customer billing portal** failures, correlate **`[:accrue, :billing, :billing_portal, :create]`** **`:exception`** / latency with **`accrue.customer.id`** / **`operation_id`** — **do not** log **`%Accrue.BillingPortal.Session{}`** inspect output in tickets.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "billing_portal" accrue/guides/operator-runbooks.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -nF "telemetry.md" accrue/guides/operator-runbooks.md`
    - `rg -nF ":billing_portal" accrue/guides/operator-runbooks.md`
  </acceptance_criteria>
  <done>Runbook points operators at span contract.</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG.md</name>
  <read_first>
    - accrue/CHANGELOG.md
  </read_first>
  <files>
    - accrue/CHANGELOG.md
  </files>
  <action>
    Under **`Unreleased`** (or create that heading per keep-a-changelog style already in file), add a **### Added** bullet: **`Accrue.Billing.create_billing_portal_session/2`** (+ **`!`**) delegating to **`Accrue.BillingPortal.Session`** with **`:billing_portal`** telemetry; mention **docs** in **`guides/telemetry.md`**.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/accrue && rg -n "create_billing_portal_session" accrue/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "create_billing_portal_session" accrue/CHANGELOG.md`
    - `CHANGELOG.md` contains **`Unreleased`** OR the file's established equivalent section header unchanged but new entry is under the topmost pending-release section
  </acceptance_criteria>
  <done>Package changelog records the feature.</done>
</task>

<verification>
- `rg -n "billing_portal" accrue/guides/telemetry.md accrue/guides/operator-runbooks.md accrue/CHANGELOG.md`
</verification>

<success_criteria>
- BIL-05 doc + changelog bullets satisfied
- No new full URLs except **`https://example.com`** placeholders if needed
</success_criteria>

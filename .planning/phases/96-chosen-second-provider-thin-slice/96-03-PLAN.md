---
phase: 96-chosen-second-provider-thin-slice
plan: 03
type: execute
wave: 2
depends_on:
  - 96-01
files_modified:
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/lib/accrue_host/braintree.ex
  - examples/accrue_host/assets/js/braintree_vault_acquisition.js
  - examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
autonomous: true
requirements:
  - PROC-12
user_setup:
  - service: braintree
    why: "Run the advisory real-provider proof in examples/accrue_host"
    env_vars:
      - name: BRAINTREE_MERCHANT_ID
        source: "Braintree sandbox account credentials"
      - name: BRAINTREE_PUBLIC_KEY
        source: "Braintree sandbox account credentials"
      - name: BRAINTREE_PRIVATE_KEY
        source: "Braintree sandbox account credentials"
    dashboard_config:
      - task: "Create a sandbox recurring billing plan id used by the host proof path"
        location: "Braintree Control Panel -> Plans"
must_haves:
  truths:
    - "The canonical real-provider proof surface is examples/accrue_host, not the installer-generated temp app."
    - "The host owns Braintree browser/vault acquisition and forwards only the narrow handoff reference to Accrue."
    - "The Braintree provider-backed host lane stays advisory and subordinate to the Fake-first merge-blocking proof model."
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/braintree.ex"
      provides: "Host-owned Braintree preparation seam"
    - path: "examples/accrue_host/assets/js/braintree_vault_acquisition.js"
      provides: "Host-owned browser acquisition logic kept outside core Accrue"
    - path: "examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs"
      provides: "Advisory provider-backed host proof coverage"
  key_links:
    - from: "examples/accrue_host/lib/accrue_host/braintree.ex"
      to: "examples/accrue_host/lib/accrue_host/billing.ex"
      via: "vault-acquisition handoff reference"
      pattern: "subscribe_with_vault_reference|client_token_for"
    - from: "examples/accrue_host/lib/accrue_host/billing.ex"
      to: "Accrue.Billing.subscribe/3"
      via: "generic host billing facade"
      pattern: "Billing.subscribe"
---

<objective>
Create the canonical example-host proof surface for the Braintree slice while keeping the installer generic and thin.

Purpose: Satisfy D-08..D-10 by proving the real provider path only in `examples/accrue_host`, with host-owned vault acquisition and advisory execution posture.
Output: A host-owned Braintree seam, an advisory Braintree host test lane, and facade-template guards that keep the installer from becoming a second proof track.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-CONTEXT.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-RESEARCH.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-PATTERNS.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md
@examples/accrue_host/lib/accrue_host/billing.ex
@examples/accrue_host/test/accrue_host/billing_facade_test.exs
@accrue/priv/accrue/templates/install/billing.ex.eex

<interfaces>
From `examples/accrue_host/lib/accrue_host/billing.ex`:

```elixir
def subscribe(billable, price_id, opts \\ []) do
  Billing.subscribe(billable, price_id, opts)
end
```

From `accrue/priv/accrue/templates/install/billing.ex.eex`:

```elixir
def subscribe(billable, price_id, opts \\ []) do
  Billing.subscribe(billable, price_id, opts)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a host-owned Braintree preparation seam and narrow handoff helper</name>
  <read_first>
    - examples/accrue_host/lib/accrue_host/billing.ex
    - .planning/phases/96-chosen-second-provider-thin-slice/96-CONTEXT.md
    - .planning/phases/96-chosen-second-provider-thin-slice/96-RESEARCH.md
    - .planning/phases/96-chosen-second-provider-thin-slice/96-PATTERNS.md
  </read_first>
  <files>examples/accrue_host/lib/accrue_host/billing.ex, examples/accrue_host/lib/accrue_host/braintree.ex, examples/accrue_host/assets/js/braintree_vault_acquisition.js</files>
  <acceptance_criteria>
    - `rg -n 'def client_token_for\\(|def subscribe_with_vault_reference\\(' examples/accrue_host/lib/accrue_host/braintree.ex examples/accrue_host/lib/accrue_host/billing.ex` returns the host-owned Braintree seam.
    - `rg -n 'vault_acquisition.*reference' examples/accrue_host/lib/accrue_host/billing.ex` returns the exact narrow handoff shape forwarded to `Accrue.Billing.subscribe/3`.
    - `rg -n 'clientToken|vault acquisition|reference' examples/accrue_host/assets/js/braintree_vault_acquisition.js` returns host-owned browser tokenization code, not core-library code.
  </acceptance_criteria>
  <action>Create `examples/accrue_host/lib/accrue_host/braintree.ex` as the only host-owned Braintree seam per D-01 and D-08. It should expose a concrete `client_token_for/1` helper and any minimal server-side config helpers needed for the example host. If a convenience wrapper is needed, add one host-only helper such as `subscribe_with_vault_reference/4` beside `AccrueHost.Billing.subscribe/3`, and have it call `Accrue.Billing.subscribe/3` with the exact `payment_method: %{vault_acquisition: %{reference: reference}}` shape from Plan 01 per D-06. Add `examples/accrue_host/assets/js/braintree_vault_acquisition.js` for the browser-side acquisition story so no Braintree JS or PCI-sensitive flow leaks into core Accrue.</action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs</automated>
  </verify>
  <done>The example host owns the Braintree browser/server handoff and passes only the narrow vault reference into the generic billing facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an advisory example-host Braintree proof lane and keep the installer generic</name>
  <read_first>
    - examples/accrue_host/test/accrue_host/billing_facade_test.exs
    - accrue/priv/accrue/templates/install/billing.ex.eex
    - .planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md
    - guides/testing-live-stripe.md
  </read_first>
  <files>examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs, examples/accrue_host/test/accrue_host/billing_facade_test.exs</files>
  <acceptance_criteria>
    - `rg -n '@moduletag :live_braintree|System.get_env\\(\"BRAINTREE_' examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs` returns an env-gated advisory provider lane.
    - `rg -n 'refute .*Braintree|Billing.subscribe\\(billable, price_id, opts\\)' examples/accrue_host/test/accrue_host/billing_facade_test.exs` shows the installer/facade genericity guard.
    - `rg -n 'vault_acquisition.*reference|processor: .*braintree' examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs` returns the public-facade proof assertions.
  </acceptance_criteria>
  <action>Add `examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs` as the canonical advisory provider-backed proof lane. Gate it with a tag such as `:live_braintree` and explicit `BRAINTREE_*` env checks so the test skips cleanly when sandbox credentials are absent, preserving D-10 and the Fake-first merge-blocking posture. Extend `billing_facade_test.exs` with a negative guard that the generated installer-facing billing template remains generic: no `Braintree`-specific helper names or browser jargon, and the generic `Billing.subscribe(billable, price_id, opts)` line still exists per D-09.</action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs test/accrue_host/braintree_subscribe_test.exs</automated>
  </verify>
  <done>`examples/accrue_host` contains the only real-provider Braintree proof lane, it is explicitly advisory, and the installer/generic host facade remains thin instead of becoming a second proof surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `examples/accrue_host` | Untrusted client-side acquisition data crosses into the host app |
| host helper -> `Accrue.Billing.subscribe/3` | Host code forwards the narrow vault reference into the library facade |
| advisory proof lane -> CI/local execution | External credentials must not silently become merge-blocking or leak into logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | I | `examples/accrue_host/assets/js/braintree_vault_acquisition.js` | mitigate | Keep tokenization host-owned and forward only the narrow `vault_acquisition.reference` value; do not log or persist raw browser payment artifacts. |
| T-96-08 | E | `examples/accrue_host/lib/accrue_host/billing.ex` | mitigate | Preserve the generic Accrue facade boundary and keep Braintree-specific helpers confined to the host seam instead of widening the installer or public core API. |
| T-96-09 | R | advisory proof lane | mitigate | Gate provider-backed tests behind explicit `BRAINTREE_*` env checks and keep Fake as the required CI lane so missing credentials cannot create ambiguous partial failures. |
</threat_model>

<verification>
Run the host-focused tests from `96-VALIDATION.md`; if sandbox credentials are present, rerun the advisory Braintree test with those env vars set and capture the result in the plan summary.
</verification>

<success_criteria>
The repo has one canonical Braintree proof surface in `examples/accrue_host`, with host-owned vault acquisition and no second full Braintree lane in installer output.
</success_criteria>

<output>
After completion, create `.planning/phases/96-chosen-second-provider-thin-slice/96-03-SUMMARY.md`
</output>

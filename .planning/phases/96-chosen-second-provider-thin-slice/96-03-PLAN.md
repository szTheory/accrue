---
phase: 96-chosen-second-provider-thin-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 96-01
  - 96-02
files_modified:
  - examples/accrue_host/lib/accrue_host/billing.ex
  - examples/accrue_host/lib/accrue_host/braintree.ex
  - examples/accrue_host/assets/js/braintree_vault_acquisition.js
  - examples/accrue_host/assets/js/app.js
  - examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex
  - examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs
  - examples/accrue_host/test/accrue_host/billing_facade_test.exs
autonomous: false
requirements:
  - PROC-12
user_setup:
  - service: braintree
    why: "Run the required real-provider proof in examples/accrue_host before Phase 96 can close"
    env_vars:
      - name: BRAINTREE_MERCHANT_ID
        source: "Braintree sandbox account credentials"
      - name: BRAINTREE_PUBLIC_KEY
        source: "Braintree sandbox account credentials"
      - name: BRAINTREE_PRIVATE_KEY
        source: "Braintree sandbox account credentials"
      - name: BRAINTREE_SANDBOX_PLAN_ID
        source: "Recurring Braintree sandbox plan created for the Phase 96 host proof flow"
    dashboard_config:
      - task: "Create a sandbox recurring billing plan id used by the host proof path"
        location: "Braintree Control Panel -> Plans"
must_haves:
  truths:
    - "The canonical real-provider proof surface is `examples/accrue_host`, not the installer-generated temp app."
    - "The host owns Braintree browser/vault acquisition and forwards only the narrow handoff reference to Accrue."
    - "The Braintree browser asset is explicitly loaded by the example-host billing proof path instead of existing as an unreferenced support file."
    - "Real-provider proof evidence is captured only after the webhook/lifecycle slice from Plan 02 exists."
  artifacts:
    - path: "examples/accrue_host/lib/accrue_host/braintree.ex"
      provides: "Host-owned Braintree preparation seam"
    - path: "examples/accrue_host/assets/js/braintree_vault_acquisition.js"
      provides: "Host-owned browser acquisition logic kept outside core Accrue"
    - path: "examples/accrue_host/assets/js/app.js"
      provides: "Host asset entrypoint that imports the Braintree acquisition module"
    - path: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      provides: "Canonical proof UI wired to the host-owned Braintree acquisition hook"
    - path: "examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs"
      provides: "Credentialed provider-backed host proof coverage"
  key_links:
    - from: "examples/accrue_host/assets/js/braintree_vault_acquisition.js"
      to: "examples/accrue_host/assets/js/app.js"
      via: "explicit import into the host asset bundle"
      pattern: "import .*braintree_vault_acquisition"
    - from: "examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex"
      to: "examples/accrue_host/assets/js/braintree_vault_acquisition.js"
      via: "proof-page data attributes or LiveView hook"
      pattern: "phx-hook=|data-braintree"
    - from: "examples/accrue_host/lib/accrue_host/braintree.ex"
      to: "examples/accrue_host/lib/accrue_host/billing.ex"
      via: "vault-acquisition handoff reference"
      pattern: "subscribe_with_vault_reference|client_token_for"
    - from: "examples/accrue_host/lib/accrue_host/billing.ex"
      to: "Accrue.Billing.subscribe/3"
      via: "generic host billing facade"
      pattern: "Billing.subscribe"
---

<objective>
Create the canonical example-host proof surface for the Braintree slice while keeping the installer generic and thin.

Purpose: Satisfy D-08..D-10 by proving the real provider path only in `examples/accrue_host`, with host-owned vault acquisition, explicit proof-page wiring, and one required credentialed Braintree run after the webhook/lifecycle slice is complete.
Output: A host-owned Braintree seam, an explicitly wired host proof path, a blocking Braintree host test lane, and facade-template guards that keep the installer from becoming a second proof track.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-CONTEXT.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-RESEARCH.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-PATTERNS.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-VALIDATION.md
@.planning/phases/96-chosen-second-provider-thin-slice/96-02-PLAN.md
@examples/accrue_host/lib/accrue_host/billing.ex
@examples/accrue_host/test/accrue_host/billing_facade_test.exs
@accrue/priv/accrue/templates/install/billing.ex.eex

<interfaces>
From `examples/accrue_host/lib/accrue_host/billing.ex`:

```elixir
def subscribe(billable, price_id, opts \\ []) do
  Billing.subscribe(billable, price_id, opts)
end
```

From `accrue/priv/accrue/templates/install/billing.ex.eex`:

```elixir
def subscribe(billable, price_id, opts \\ []) do
  Billing.subscribe(billable, price_id, opts)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a host-owned Braintree preparation seam, proof-page wiring, and narrow handoff helper</name>
  <files>examples/accrue_host/lib/accrue_host/billing.ex, examples/accrue_host/lib/accrue_host/braintree.ex, examples/accrue_host/assets/js/braintree_vault_acquisition.js, examples/accrue_host/assets/js/app.js, examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex</files>
  <behavior>
    - Test 1: the host seam exposes `client_token_for/1` plus one narrow wrapper that forwards only `payment_method.vault_acquisition.reference` per D-01, D-02, and D-08.
    - Test 2: the example-host proof page loads the Braintree acquisition asset explicitly through `assets/js/app.js` and one LiveView hook/data seam.
  </behavior>
  <action>Create `examples/accrue_host/lib/accrue_host/braintree.ex` as the only host-owned Braintree seam per D-01 and D-08. It should expose a concrete `client_token_for/1` helper plus any minimal server-side config helpers needed for the example host. If a convenience wrapper is needed, add one host-only helper such as `subscribe_with_vault_reference/4` beside `AccrueHost.Billing.subscribe/3`, and have it call `Accrue.Billing.subscribe/3` with the exact `payment_method: %{vault_acquisition: %{reference: reference}}` shape from Plan 01. Add `examples/accrue_host/assets/js/braintree_vault_acquisition.js` for the browser-side acquisition story so no Braintree JS or PCI-sensitive flow leaks into core Accrue, then wire that asset into the actual proof path by importing it from `examples/accrue_host/assets/js/app.js` and attaching it to the canonical billing proof UI in `examples/accrue_host/lib/accrue_host_web/live/subscription_live.ex` via one explicit hook/data-attribute seam. The proof page must also surface the sandbox `BRAINTREE_SANDBOX_PLAN_ID` path honestly by passing that plan id through the existing `price_id` argument per the resolved research mapping.</action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs && rg -n 'import .*braintree_vault_acquisition' assets/js/app.js && rg -n 'phx-hook=|data-braintree|vault acquisition' lib/accrue_host_web/live/subscription_live.ex</automated>
  </verify>
  <done>The example host owns the Braintree browser/server handoff, the acquisition asset is loaded by the proof page, and only the narrow vault reference crosses into the generic billing facade.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provide Braintree sandbox credentials and a recurring sandbox plan id</name>
  <files>shell environment variables only: BRAINTREE_MERCHANT_ID, BRAINTREE_PUBLIC_KEY, BRAINTREE_PRIVATE_KEY, BRAINTREE_SANDBOX_PLAN_ID</files>
  <what-built>Task 1 creates the host-owned seam and proof-page wiring, but `PROC-12` is not complete until one real Braintree host proof runs successfully with credentials.</what-built>
  <action>Pause execution until the human supplies the required Braintree sandbox credentials and recurring sandbox plan id in the shell that will run the live proof command. This is a true human-only setup dependency because Claude cannot create the sandbox account secrets or dashboard plan on the user's behalf.</action>
  <how-to-verify>
    1. Export `BRAINTREE_MERCHANT_ID`, `BRAINTREE_PUBLIC_KEY`, `BRAINTREE_PRIVATE_KEY`, and `BRAINTREE_SANDBOX_PLAN_ID` into the executor shell.
    2. Confirm `BRAINTREE_SANDBOX_PLAN_ID` refers to a recurring Braintree sandbox plan that can be used by the example-host proof flow.
    3. Do not resume until those values are present in the shell that will run `cd examples/accrue_host && mix test test/accrue_host/braintree_subscribe_test.exs --include live_braintree`.
  </how-to-verify>
  <verify>
    <automated>test -n "$BRAINTREE_MERCHANT_ID" && test -n "$BRAINTREE_PUBLIC_KEY" && test -n "$BRAINTREE_PRIVATE_KEY" && test -n "$BRAINTREE_SANDBOX_PLAN_ID"</automated>
  </verify>
  <done>The executor shell contains all four required Braintree values and the recurring sandbox plan id exists for the host proof flow.</done>
  <resume-signal>Type `credentials ready` after the env vars are exported in the executor shell and the recurring sandbox plan id exists.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the blocking example-host Braintree proof lane and keep the installer generic</name>
  <files>examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs, examples/accrue_host/test/accrue_host/billing_facade_test.exs</files>
  <behavior>
    - Test 1: the credentialed proof requires `BRAINTREE_*` env vars and `BRAINTREE_SANDBOX_PLAN_ID`, drives the host path, and proves persisted `processor: :braintree` state through the public facade.
    - Test 2: the installer-facing host facade remains generic and contains no Braintree-specific helper names or browser jargon per D-09.
  </behavior>
  <action>Add `examples/accrue_host/test/accrue_host/braintree_subscribe_test.exs` as the canonical real-provider proof lane for Phase 96. Tag it with `:live_braintree`, but do not let it skip silently when credentials are absent: after Task 2, it must require `BRAINTREE_MERCHANT_ID`, `BRAINTREE_PUBLIC_KEY`, `BRAINTREE_PRIVATE_KEY`, and `BRAINTREE_SANDBOX_PLAN_ID` explicitly and fail with a clear setup error if the executor tries to run it without them. The test should drive the host-owned proof path created in Task 1, pass the recurring sandbox plan id through the existing `price_id` public argument, assert the exact `payment_method: %{vault_acquisition: %{reference: reference}}` handoff shape, and prove the resulting subscription persists through the public facade with `processor: :braintree`. Extend `billing_facade_test.exs` with a negative guard that the generated installer-facing billing template remains generic: no `Braintree`-specific helper names or browser jargon, and the generic `Billing.subscribe(billable, price_id, opts)` line still exists per D-09. The task is not complete until the executor reruns the credentialed command successfully and records the command, absolute date, sandbox plan-id source, and pass result in `96-03-SUMMARY.md` as execution evidence.</action>
  <verify>
    <automated>cd examples/accrue_host && mix test test/accrue_host/billing_facade_test.exs test/accrue_host/braintree_subscribe_test.exs --include live_braintree</automated>
  </verify>
  <done>`examples/accrue_host` contains the only real-provider Braintree proof lane, the installer/generic host facade remains thin instead of becoming a second proof surface, and Phase 96 has one captured successful credentialed Braintree run as proof evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `examples/accrue_host` | Untrusted client-side acquisition data crosses into the host app |
| host helper -> `Accrue.Billing.subscribe/3` | Host code forwards the narrow vault reference into the library facade |
| advisory proof lane -> CI/local execution | External credentials must not silently become merge-blocking or leak into logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | I | `examples/accrue_host/assets/js/braintree_vault_acquisition.js` | mitigate | Keep tokenization host-owned and forward only the narrow `vault_acquisition.reference` value; do not log or persist raw browser payment artifacts. |
| T-96-08 | E | `examples/accrue_host/lib/accrue_host/billing.ex` | mitigate | Preserve the generic Accrue facade boundary and keep Braintree-specific helpers confined to the host seam instead of widening the installer or public core API. |
| T-96-09 | R | real-provider proof lane | mitigate | Use a blocking credential/setup checkpoint plus a required post-checkpoint live test run so missing Braintree credentials or plan ids cannot be misreported as Phase 96 completion. |
</threat_model>

<verification>
Run the host-focused command from `96-VALIDATION.md` after Task 1. Only capture `live_braintree` proof evidence after Plan 02 is complete and the credential checkpoint has been satisfied.
</verification>

<success_criteria>
The repo has one canonical Braintree proof surface in `examples/accrue_host`, with host-owned vault acquisition, explicit proof-page wiring, no second full Braintree lane in installer output, and one captured successful credentialed Braintree host run after the lifecycle slice exists.
</success_criteria>

<output>
After completion, create `.planning/phases/96-chosen-second-provider-thin-slice/96-03-SUMMARY.md`
</output>

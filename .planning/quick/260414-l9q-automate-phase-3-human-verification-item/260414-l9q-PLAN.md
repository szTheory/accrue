---
phase: quick-260414-l9q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue/test/test_helper.exs
  - accrue/mix.exs
  - accrue/test/accrue/billing/charge_3ds_test.exs
  - accrue/test/accrue/billing/proration_roundtrip_test.exs
  - accrue/test/accrue/webhook/default_handler_out_of_order_test.exs
  - accrue/test/live_stripe/charge_3ds_live_test.exs
  - accrue/test/live_stripe/proration_fidelity_live_test.exs
  - accrue/config/runtime.exs
  - .github/workflows/ci.yml
  - .gitignore
  - accrue/.secrets.example
  - guides/testing-live-stripe.md
  - .planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md
autonomous: true
requirements: [PHASE3-HUMAN-UAT-1, PHASE3-HUMAN-UAT-2, PHASE3-HUMAN-UAT-3]

must_haves:
  truths:
    - "mix test excludes :live_stripe and :slow tags by default"
    - "mix test.live alias runs only :live_stripe tagged tests"
    - "Fake-asserted charge_3ds_test proves Billing.charge/3 returns {:ok, :requires_action, _} and a charge.succeeded webhook transitions the Charge row to :succeeded"
    - "default_handler_out_of_order_test.exs covers all 3 legs: older-skipped, reverse-order-newer-wins, watermark + Processor.fetch/2 refetch"
    - "proration_roundtrip_test proves preview_upcoming_invoice line items match swap_plan committed invoice line-for-line under the Fake processor"
    - "live-stripe fidelity suite exists under accrue/test/live_stripe/ and skips unless STRIPE_TEST_SECRET_KEY is set"
    - "CI live-stripe job runs on workflow_dispatch and schedule with continue-on-error: true, not blocking PR test job"
    - "03-HUMAN-UAT.md frontmatter status is resolved and all 3 items report result: automated"
  artifacts:
    - path: accrue/test/accrue/billing/charge_3ds_test.exs
      provides: "Fake-asserted 3DS charge + webhook drive test"
    - path: accrue/test/accrue/billing/proration_roundtrip_test.exs
      provides: "Fake-asserted preview-vs-committed proration comparison"
    - path: accrue/test/live_stripe/charge_3ds_live_test.exs
      provides: "Live-Stripe 3DS fidelity test, gated on STRIPE_TEST_SECRET_KEY"
    - path: accrue/test/live_stripe/proration_fidelity_live_test.exs
      provides: "Live-Stripe proration fidelity test, gated on STRIPE_TEST_SECRET_KEY"
    - path: accrue/.secrets.example
      provides: "Template for act --secret-file"
    - path: guides/testing-live-stripe.md
      provides: "Documentation for mix test.live + act workflow"
  key_links:
    - from: accrue/test/test_helper.exs
      to: accrue/mix.exs
      via: "ExUnit.configure exclude ↔ test.live alias --only inversion"
      pattern: "exclude:.*live_stripe"
    - from: accrue/config/runtime.exs
      to: accrue/test/live_stripe/
      via: "STRIPE_TEST_SECRET_KEY env var opt-in swaps Processor.Fake → Processor.Stripe"
      pattern: "STRIPE_TEST_SECRET_KEY"
    - from: .github/workflows/ci.yml
      to: accrue/mix.exs
      via: "live-stripe job calls mix test.live"
      pattern: "test\\.live"
---

<objective>
Automate all 3 items in `.planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md` per the approved plan `tender-foraging-flamingo`. Split each into Fake-asserted correctness (runs every PR) and live-Stripe fidelity (gated, scheduled). Flip HUMAN-UAT from `status: partial` to `status: resolved`.

Purpose: Eliminate the last 3 human-in-the-loop verification items from Phase 3 so the library can ship without manual browser testing.
Output: 6 task deliverables: tag infra, 3DS Fake test, out-of-order audit, proration Fake test, live-Stripe suite + runtime opt-in, CI wiring + HUMAN-UAT flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/jon/.claude/plans/tender-foraging-flamingo.md
@.planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md
@CLAUDE.md

<interfaces>
Key functions/files to reuse (do NOT rewrite):

- `Accrue.Processor.Fake.scripted_response/2` — accrue/lib/accrue/processor/fake.ex:163
- `Accrue.Processor.Fake.create_charge/2` — accrue/lib/accrue/processor/fake.ex:378 (emits `status: :requires_action` at lines 1240–1246)
- `Accrue.WebhookFixtures.signed_event/2` — accrue/test/support/webhook_fixtures.ex:32
- `Accrue.WebhookFixtures.signed_event_of_type/2` — accrue/test/support/webhook_fixtures.ex:84
- `Accrue.BillingCase` — Ecto sandbox + Fake reset + operation_id seed
- `Accrue.ConnCase` — raw-body `Plug.Test` conn for webhook POSTs
- Template: `accrue/test/accrue/webhook/default_handler_phase3_test.exs` — BillingCase + Fake.transition/3 shape
- Existing `.github/workflows/ci.yml` `test` job lines 10–55 — copy postgres service + setup-beam steps
- `accrue/mix.exs` aliases/0 at lines 86–95 — only has `"test.all"` today
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag infrastructure (test_helper exclude + mix test.live alias)</name>
  <files>accrue/test/test_helper.exs, accrue/mix.exs</files>
  <read_first>
    - accrue/test/test_helper.exs (line 32 is currently `ExUnit.start()` with no configure)
    - accrue/mix.exs lines 86–95 (aliases/0 — currently only has `"test.all"`)
  </read_first>
  <action>
    1. In `accrue/test/test_helper.exs`, insert `ExUnit.configure(exclude: [:live_stripe, :slow])` on the line immediately before `ExUnit.start()`.
    2. In `accrue/mix.exs` aliases/0, add an entry: `"test.live": ["test --only live_stripe"]`. Keep existing `"test.all"` intact. Preserve trailing comma/syntax.
    3. Run `mix compile --warnings-as-errors` from `accrue/` to verify no syntax breakage.
  </action>
  <acceptance_criteria>
    <automated>cd accrue &amp;&amp; mix compile --warnings-as-errors &amp;&amp; grep -q 'ExUnit.configure(exclude: \[:live_stripe, :slow\])' test/test_helper.exs &amp;&amp; grep -q '"test.live":' mix.exs</automated>
  </acceptance_criteria>
  <done>test_helper.exs excludes both tags; `mix test.live` alias resolves to `test --only live_stripe`; project compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fake-asserted 3DS charge + webhook drive test (Items 1A + 1B)</name>
  <files>accrue/test/accrue/billing/charge_3ds_test.exs</files>
  <read_first>
    - accrue/lib/accrue/processor/fake.ex (lines 163, 378, 1240–1246 for scripted_response and requires_action shape)
    - accrue/test/support/webhook_fixtures.ex (lines 32, 84)
    - accrue/test/accrue/webhook/default_handler_phase3_test.exs (template for BillingCase + webhook POST shape)
  </read_first>
  <behavior>
    - Test 1A: `Billing.charge/3` against a Fake-scripted requires_action response returns `{:ok, :requires_action, %{payment_intent: _, client_secret: _}}`
    - Test 1B: POSTing a signed `charge.succeeded` event for the same charge transitions the local `Accrue.Billing.Charge` row to `:succeeded`
  </behavior>
  <action>
    Create `accrue/test/accrue/billing/charge_3ds_test.exs`. Use `Accrue.BillingCase` (async: false if webhook plug needs conn) and `Accrue.ConnCase`. Reference the phase3 webhook test for setup shape.

    Test 1A: call `Accrue.Processor.Fake.scripted_response(:create_charge, fn _ -> {:ok, %{id: "ch_3ds_xxx", status: :requires_action, payment_intent: "pi_xxx", client_secret: "pi_xxx_secret_yyy", ...minimum fields create_charge returns...}} end)`. Then call `Accrue.Billing.charge(customer, amount, opts)` and assert the result is `{:ok, :requires_action, %{payment_intent: pi, client_secret: cs}}` where both are non-nil strings.

    Test 1B: synthesize a signed `charge.succeeded` event via `Accrue.WebhookFixtures.signed_event_of_type("charge.succeeded", data: %{object: %{id: charge.processor_id, status: "succeeded"}})`. POST the raw body + signature header through the webhook plug (copy conn setup from default_handler_phase3_test.exs). Assert the `Accrue.Billing.Charge` row reloaded via `Repo.reload/1` has `status: :succeeded`.

    Do NOT complete real 3DS — Fake processor is sufficient.
  </action>
  <acceptance_criteria>
    <automated>cd accrue &amp;&amp; mix test test/accrue/billing/charge_3ds_test.exs --max-failures 1</automated>
  </acceptance_criteria>
  <done>charge_3ds_test.exs exists, both 1A and 1B test cases pass, no :live_stripe tag.</done>
</task>

<task type="auto">
  <name>Task 3: Audit out-of-order webhook test and add reverse-order leg if missing</name>
  <files>accrue/test/accrue/webhook/default_handler_out_of_order_test.exs</files>
  <read_first>
    - accrue/test/accrue/webhook/default_handler_out_of_order_test.exs (existing 2 cases: older-skipped, tie-on-equal-timestamps)
    - accrue/lib/accrue/webhook/default_handler.ex (for Processor.fetch/2 dispatch pattern, watermark columns `last_stripe_event_ts` / `last_stripe_event_id`)
  </read_first>
  <action>
    1. Read the existing file and inventory which of these 3 legs are covered:
       (a) Older event delivered after newer: stale skipped with `:stale_event` telemetry and no refetch.
       (b) Newer event delivered AFTER an older one that already updated state, but delivered in reverse creation-order (i.e., received newer first, then the lagging older): newer wins, older is skipped.
       (c) Watermark columns `last_stripe_event_ts` + `last_stripe_event_id` update correctly AND DefaultHandler refetches via `Processor.__impl__().fetch(type, id)` rather than trusting payload snapshot.
    2. If leg (b) — the reverse-order delivery — is missing, add a new `test "..."` block in the same file that:
       - Seeds a Subscription at version N.
       - Constructs two `customer.subscription.updated` events via WebhookFixtures with different `created` timestamps (T1 &lt; T2).
       - Delivers T2 first, then T1.
       - Asserts T2's data won (via Processor.fetch, not payload), T1 was skipped with `:stale_event` telemetry, watermark equals T2.
    3. If all 3 legs already covered, leave the file unchanged and document the audit in a comment at the top of the file: `# Audit 2026-04-14: 3-leg coverage confirmed for Phase 3 HUMAN-UAT Item 2`.
    4. Run the test file to verify.
  </action>
  <acceptance_criteria>
    <automated>cd accrue &amp;&amp; mix test test/accrue/webhook/default_handler_out_of_order_test.exs --max-failures 1</automated>
  </acceptance_criteria>
  <done>File covers all 3 legs (older skipped, reverse-order newer wins, watermark + refetch); tests pass; either audit comment added or new test case merged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Fake-asserted proration round-trip test (Item 3A)</name>
  <files>accrue/test/accrue/billing/proration_roundtrip_test.exs</files>
  <read_first>
    - accrue/test/accrue/billing/upcoming_invoice_test.exs (existing preview shape)
    - accrue/test/accrue/billing/swap_plan_test.exs (existing swap shape)
    - accrue/lib/accrue/billing.ex (preview_upcoming_invoice/2 and swap_plan/3 signatures)
  </read_first>
  <behavior>
    - preview_upcoming_invoice line items numerically match the committed invoice line items from swap_plan with proration: :create_prorations, line-for-line (amount, description, period).
  </behavior>
  <action>
    Create `accrue/test/accrue/billing/proration_roundtrip_test.exs` using `Accrue.BillingCase`.

    Setup: create a customer + subscription on an existing test price (price_A).
    Step 1: call `Accrue.Billing.preview_upcoming_invoice(subscription, swap_to: price_B)` and capture `line_items` (list).
    Step 2: call `Accrue.Billing.swap_plan(subscription, price_B, proration: :create_prorations)`.
    Step 3: fetch the resulting committed invoice (most recent Invoice for the customer, or return value from swap_plan if it returns one).
    Step 4: assert `length(preview_items) == length(committed_items)` and for each pair assert `amount`, `description`, and `period.start` / `period.end` match exactly.

    Use Fake processor (no tag — runs on every PR). Fake should already produce deterministic proration line items; if not, script `Processor.Fake.scripted_response/2` for both `preview_invoice` and `update_subscription` to return identical line-items data.
  </action>
  <acceptance_criteria>
    <automated>cd accrue &amp;&amp; mix test test/accrue/billing/proration_roundtrip_test.exs --max-failures 1</automated>
  </acceptance_criteria>
  <done>File exists, preview-vs-committed comparison passes line-for-line under Fake, no :live_stripe tag.</done>
</task>

<task type="auto">
  <name>Task 5: Live-Stripe fidelity suite + runtime.exs opt-in</name>
  <files>accrue/test/live_stripe/charge_3ds_live_test.exs, accrue/test/live_stripe/proration_fidelity_live_test.exs, accrue/config/runtime.exs</files>
  <read_first>
    - accrue/config/runtime.exs (existing structure — single Stripe-secret opt-in only, no broader rework)
    - accrue/config/test.exs (confirm default processor stays Fake)
    - Task 2 + 4 artifacts for test shape to mirror
  </read_first>
  <action>
    1. Create `accrue/test/live_stripe/charge_3ds_live_test.exs`:
       - `@moduletag :live_stripe`
       - `@moduletag timeout: 30_000`
       - In `setup_all`: if `System.get_env("STRIPE_TEST_SECRET_KEY")` is nil, return `{:ok, skip: true}` and use a `@tag :skip` or early-return guard in each test.
       - Configure `Accrue.Processor.Stripe` as the processor for this module (via `Application.put_env` in setup_all, restore in on_exit).
       - Create a real Stripe customer via the processor, attach PM via Stripe's test token `pm_card_threeDSecure2Required`, call `Accrue.Billing.charge/3`, assert `{:ok, :requires_action, _}`. Do NOT complete 3DS.

    2. Create `accrue/test/live_stripe/proration_fidelity_live_test.exs`:
       - Same `@moduletag :live_stripe`, same 30s timeout, same env-var gating.
       - Create real Stripe customer + subscription with a real test price, call `preview_upcoming_invoice/2`, call `swap_plan/3`, fetch the real committed invoice, compare preview line items against it numerically.

    3. Modify `accrue/config/runtime.exs`: add a guarded block (only when `config_env() == :test` AND `System.get_env("STRIPE_TEST_SECRET_KEY")` is set) that configures `config :accrue, :processor, Accrue.Processor.Stripe` and `config :lattice_stripe, :api_key, System.fetch_env!("STRIPE_TEST_SECRET_KEY")`. Otherwise leave defaults untouched. Do NOT touch `config/test.exs`.
  </action>
  <acceptance_criteria>
    <automated>cd accrue &amp;&amp; mix compile --warnings-as-errors &amp;&amp; test -f test/live_stripe/charge_3ds_live_test.exs &amp;&amp; test -f test/live_stripe/proration_fidelity_live_test.exs &amp;&amp; grep -q '@moduletag :live_stripe' test/live_stripe/charge_3ds_live_test.exs &amp;&amp; grep -q '@moduletag :live_stripe' test/live_stripe/proration_fidelity_live_test.exs &amp;&amp; grep -q 'STRIPE_TEST_SECRET_KEY' config/runtime.exs &amp;&amp; mix test --exclude live_stripe --max-failures 1</automated>
  </acceptance_criteria>
  <done>Both live tests exist and are tagged :live_stripe; runtime.exs has the env-gated Stripe opt-in; default `mix test` run still passes with live tests skipped; no live tests execute without STRIPE_TEST_SECRET_KEY.</done>
</task>

<task type="auto">
  <name>Task 6: CI wiring + local act support + HUMAN-UAT flip to resolved</name>
  <files>.github/workflows/ci.yml, .gitignore, accrue/.secrets.example, guides/testing-live-stripe.md, .planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md</files>
  <read_first>
    - .github/workflows/ci.yml (lines 3–7 for `on:`; lines 10–55 for test job postgres service + setup-beam to copy)
    - .gitignore (to pick insertion point)
    - guides/ directory listing (to decide where testing-live-stripe.md lands)
    - .planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md (current frontmatter status: partial; 3 items result: [pending])
  </read_first>
  <action>
    1. `.github/workflows/ci.yml`:
       - In top-level `on:` block, add `workflow_dispatch:` and `schedule:` with `- cron: '0 6 * * *'`.
       - Add a new job `live-stripe:` alongside existing `test:` job:
         - `if: github.event_name == 'workflow_dispatch' || github.event_name == 'schedule'`
         - `continue-on-error: true`
         - Same postgres service block as `test` job
         - `env: STRIPE_TEST_SECRET_KEY: ${{ secrets.STRIPE_TEST_SECRET_KEY }}`
         - Steps: actions/checkout → erlef/setup-beam (Elixir 1.18, OTP 27 — match existing test job versions) → `mix deps.get` → `cd accrue &amp;&amp; mix ecto.create &amp;&amp; mix ecto.migrate` → `cd accrue &amp;&amp; mix test.live`
       - Leave existing `test:` job untouched.

    2. `.gitignore`: add `.secrets` on a new line (avoid dupes).

    3. Create `accrue/.secrets.example` with single line: `STRIPE_TEST_SECRET_KEY=sk_test_replace_me`.

    4. Create `guides/testing-live-stripe.md` (or wherever the existing guides/ structure dictates — inspect first). Document:
       - `mix test` (default — live tests excluded)
       - `STRIPE_TEST_SECRET_KEY=sk_test_... mix test.live` (local live run)
       - `act workflow_dispatch -W .github/workflows/ci.yml -j live-stripe --secret-file .secrets` (local act run)
       - Note that `.secrets` must be copied from `accrue/.secrets.example` and never committed.

    5. Update `.planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md`:
       - Frontmatter: `status: partial` → `status: resolved`; update `updated:` to today.
       - Item 1 `result:` → `automated — accrue/test/accrue/billing/charge_3ds_test.exs (Fake) + accrue/test/live_stripe/charge_3ds_live_test.exs (fidelity)`
       - Item 2 `result:` → `automated — accrue/test/accrue/webhook/default_handler_out_of_order_test.exs (3-leg coverage)`
       - Item 3 `result:` → `automated — accrue/test/accrue/billing/proration_roundtrip_test.exs (Fake) + accrue/test/live_stripe/proration_fidelity_live_test.exs (fidelity)`
       - Update summary: `passed: 3`, `pending: 0`.
  </action>
  <acceptance_criteria>
    <automated>grep -q 'workflow_dispatch:' .github/workflows/ci.yml &amp;&amp; grep -q 'schedule:' .github/workflows/ci.yml &amp;&amp; grep -q 'live-stripe:' .github/workflows/ci.yml &amp;&amp; grep -q 'continue-on-error: true' .github/workflows/ci.yml &amp;&amp; grep -q '^\.secrets$' .gitignore &amp;&amp; test -f accrue/.secrets.example &amp;&amp; test -f guides/testing-live-stripe.md &amp;&amp; grep -q '^status: resolved$' .planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md &amp;&amp; ! grep -q 'result: \[pending\]' .planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md</automated>
  </acceptance_criteria>
  <done>CI has workflow_dispatch + schedule + live-stripe job with continue-on-error; .secrets gitignored; .secrets.example exists; guide exists; HUMAN-UAT.md frontmatter resolved with all 3 items automated.</done>
</task>

</tasks>

<verification>
1. `cd accrue && mix test` — full default suite passes; charge_3ds_test, proration_roundtrip_test, and default_handler_out_of_order_test all green; live tests skipped via tag exclusion; test count ≈ +4 over baseline of 376.
2. `cd accrue && mix test.live` — without STRIPE_TEST_SECRET_KEY, both live tests skip cleanly; with the env var set, they run against Stripe test mode and pass.
3. `grep '^status:' .planning/phases/03-core-subscription-lifecycle/03-HUMAN-UAT.md` → `status: resolved`.
4. `.github/workflows/ci.yml` parses as valid YAML; manual `gh workflow run CI` invocation (post-merge) executes the `live-stripe` job with continue-on-error without blocking the `test` job.
5. Optional local: `act workflow_dispatch -W .github/workflows/ci.yml -j live-stripe --secret-file .secrets` runs green in Docker.
</verification>

<success_criteria>
- All 6 tasks complete; every acceptance_criteria command exits 0.
- No human-in-the-loop items remain for Phase 3 library verification.
- Fake-asserted tests run on every PR; live-Stripe tests run on schedule/dispatch only.
- HUMAN-UAT.md frontmatter flipped to `status: resolved`.
- No changes to Phase 3 production code; test + CI + config only.
- Out of scope (do NOT touch): Phase 3 code review findings CR-01/02/03, broader runtime.exs rework, headless 3DS browser completion, Bypass, Stripe CLI triggers.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-l9q-automate-phase-3-human-verification-item/260414-l9q-SUMMARY.md` summarizing which files changed and linking to the 6 task deliverables.
</output>

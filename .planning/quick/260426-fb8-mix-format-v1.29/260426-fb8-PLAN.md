---
quick_id: 260426-fb8
slug: mix-format-v1.29
type: quick
description: "Fix mix format violations introduced by v1.29 Mailglass merge"
date: 2026-04-26
mode: quick
---

# Quick Task 260426-fb8: Mix format auto-fix for v1.29 Mailglass commits

## Objective

Phase 1 of the post-v1.29 main-CI recovery. Auto-fix mix format violations introduced across 20 files in `accrue/` by the v1.29 Mailglass commits (`2cc6e0b`, `38e130f`, `1a6a51a`). Independent of the Mailglass-on-Hex blocker.

## Scope

- Run `mix format` from inside `accrue/` and `accrue_admin/`.
- Verify both packages clean via `mix format --check-formatted`.
- Verify compile + targeted tests pass.
- Open PR titled "fix: mix format auto-fix for v1.29 Mailglass commits" against main.

## Out of scope

- `mix.exs` deps (Mailglass path → Hex switch is its own milestone).
- `.github/workflows/*` — no CI workarounds.
- `README.md` — no changes needed.
- `.planning/*` — only this task directory.

## Tasks

1. Create branch `fix/mix-format-v1.29` from `main`.
2. Run `mix format` in both packages.
3. Verify `mix format --check-formatted` exits 0 in both.
4. Verify `mix compile --warnings-as-errors` clean.
5. Verify `mix test test/accrue/emails/ test/accrue/invoices/components_test.exs test/accrue/webhook/default_handler_mailer_dispatch_test.exs` passes.
6. Commit + push + open PR.

## Verification

- PR CI shows `Accrue format` job GREEN across all 5 Release gate matrix cells.
- Compile-dependent gates (Browser UAT, Phase 18 Stripe Tax gate, test/dialyzer/credo portions) stay RED — that's expected until Mailglass is on Hex.

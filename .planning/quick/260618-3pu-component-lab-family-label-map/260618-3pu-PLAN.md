---
quick_id: 260618-3pu
slug: component-lab-family-label-map
status: complete
date: 2026-06-18
---

# Quick Task 260618-3pu: Component-lab family-label map

## Description

Replace the `String.upcase(family)` component-lab family headers in
`accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex` (which render
"BUTTON", "FORM-FIELD", etc.) with a `family → label` map using the approved
Phase-189 UI-SPEC copywriting labels. Surfaced as a WARNING in `189-UI-REVIEW.md`
and tracked as a Phase 189 follow-up todo in STATE.md.

## Task

**Files:** `accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex`

**Action:**
1. Replace `<%= String.upcase(family) %>` (line 187) with `<%= family_label(family) %>`.
2. Add a private `family_label/1` helper that maps each of the 16 registry
   families (those with `applicable_states`) to its UI-SPEC `####` section label,
   with a humanizing fallback (`"-" → " "`, capitalize) for any future/unmapped
   family so a new registry entry can never regress to an unstyled token.

**Label source of truth:** the `####` component headers in
`.planning/phases/189-primitive-form-components-component-lab/189-UI-SPEC.md`:

| family | label |
|--------|-------|
| button | Button |
| input | Input |
| textarea | Textarea |
| checkbox | Checkbox |
| radio | Radio |
| toggle | Toggle switch |
| select | Select |
| form-field | Form field |
| status | Status badge |
| icon | Icon |
| money | Money |
| json-viewer | JSON viewer |
| spinner | Loading |
| tooltip | Tooltip |
| inline-id | Inline code / ID |
| empty-state | Empty state |

**Verify:** `mix compile` is clean; the rendered eyebrow shows "Toggle switch"
not "TOGGLE"; all 16 families have an explicit mapping.

**Done:** kitchen family headers read as UI-SPEC copywriting labels.

---
phase: 260620-ps2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/components/timeline.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/lib/accrue_admin/dev/component_registry.ex
  - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
  - accrue_admin/test/accrue_admin/components/display_components_test.exs
  - accrue_admin/priv/static/accrue_admin.css
autonomous: true
requirements: [QUICK-260620-ps2]

must_haves:
  truths:
    - "The timeline renders a continuous vertical rail threading the nodes (drawn only between consecutive items, never above the first or below the last)."
    - "Each item's status renders via the shared StatusBadge (ax-status-badge), tinted to the same computed tone as its node dot."
    - "Each item's timestamp renders in a semantic <time> element with tabular-nums."
    - "The empty state renders a dedicated calm ax-timeline-empty block (no longer borrowing ax-filter-chip-empty)."
    - "All 4 call sites (dashboard recent_events + webhook_health, webhook attempt/derived, subscription timeline) render unchanged — zero data-contract changes."
    - "The component lab (/billing/dev/components) shows a 'Timeline' family with a populated multi-tone specimen and an empty-state specimen."
    - "The committed priv/static/accrue_admin.css bundle contains the new rail rule and is regenerated from source."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/timeline.ex"
      provides: "Restyled timeline component (StatusBadge reuse, <time>, dedicated empty state)"
      contains: "StatusBadge"
    - path: "accrue_admin/assets/css/app.css"
      provides: "Rail, calmer node, threaded-feed entry, timestamp, empty-state CSS (timeline-scoped, tokens-only)"
      contains: "ax-timeline-item:not(:last-child)"
    - path: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      provides: "timeline family registry entry"
      contains: "ax-timeline ax-timeline-item"
    - path: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      provides: "timeline specimen renderers + family_label"
      contains: "Timeline.timeline"
    - path: "accrue_admin/priv/static/accrue_admin.css"
      provides: "Rebuilt committed CSS bundle"
      contains: "ax-timeline-item:not(:last-child)"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/timeline.ex"
      to: "accrue_admin/lib/accrue_admin/components/status_badge.ex"
      via: "StatusBadge.status_badge with tone={tone(item)} passthrough"
      pattern: "StatusBadge.status_badge"
    - from: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      to: "accrue_admin/lib/accrue_admin/components/timeline.ex"
      via: "do_render_specimen renders <Timeline.timeline items={...} />"
      pattern: "Timeline.timeline"
---

<objective>
Elevate `AccrueAdmin.Components.Timeline` into a proper, on-brand, design-system timeline — a continuous vertical rail threading calm "threaded feed" entries, with status rendered through the shared `StatusBadge` (tone passed through so node + badge agree), semantic tabular timestamps, a dedicated empty state, and a `timeline` family registered in the in-app component lab.

This is a faithful transcription of an already user-approved plan (`/Users/jon/.claude/plans/i-just-got-an-ethereal-harbor.md`). The design decisions are LOCKED — do NOT re-explore the design space.

Purpose: The "Recent local activity" block on `/admin` (and 3 other surfaces) reads as a card-in-card boxy stack, not a threaded sequence — it has floating tone-dots beside heavy elevated cards with no connecting rail. The brand book names "timelines / event streams / state transitions" as Accrue's visual language, so the most on-brand fix is also the most correct one.

Output: Restyled + enriched Timeline component, timeline-scoped CSS additions, a registered `timeline` component-lab family, extended tests, and the rebuilt committed `priv/static/accrue_admin.css` bundle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/Users/jon/.claude/plans/i-just-got-an-ethereal-harbor.md
@accrue_admin/lib/accrue_admin/components/timeline.ex
@accrue_admin/lib/accrue_admin/components/status_badge.ex
@accrue_admin/test/accrue_admin/components/display_components_test.exs

# Hard constraints (from CLAUDE.md + approved plan):
# - GSD enforcement: all edits go through this plan.
# - The admin serves the COMMITTED priv/static/accrue_admin.css bundle, NOT source app.css.
#   Editing app.css ships NOTHING until `mix accrue_admin.assets.build` regenerates the bundle
#   AND it is committed. This is the most-missed step (Phase 189 shipped dead CSS by skipping it).
# - StatusBadge is SHARED — DO NOT MODIFY it. Pass a computed `tone` so the node dot + badge agree.
# - CSS grouped-selector care: `.ax-timeline-card` is grouped with `.ax-kpi-card` (border+background,
#   app.css:968-980); `.ax-timeline-status` is grouped with kpi/json selectors (app.css:1063-1071).
#   DO NOT mutate those shared groups — add timeline-scoped overrides AFTER them. Tokens only, NO hex.
# - Data contract is IDENTICAL — ZERO call-site changes (shared by dashboard_live, webhook_live,
#   subscription_live). We only restyle/enrich the component + register it in the lab.
# - Do NOT touch examples/accrue_host/mix.lock or .planning/research/.cache/ — leave out of all commits.
# - No new deps, no host changes, per-event-type icons DEFERRED.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle + enrich the Timeline component (StatusBadge, &lt;time&gt;, dedicated empty state)</name>
  <files>accrue_admin/lib/accrue_admin/components/timeline.ex</files>
  <action>
Recon first: run `rg -n 'ax-timeline' accrue_admin/test accrue_admin/assets accrue_admin/lib` to confirm shared selectors before touching anything.

Edit `timeline.ex` (the component being elevated) with the following changes, keeping the item data contract IDENTICAL (no attr changes, no Map key changes — all 4 call sites must keep working untouched):

1. Add `alias AccrueAdmin.Components.StatusBadge` near the top of the module (after `use Phoenix.Component`).

2. Replace the bespoke status span:
   `<span :if={Map.get(item, :status)} class="ax-timeline-status"><%= humanize(Map.get(item, :status)) %></span>`
   with the shared badge, KEEPING the `:if` guard (nil-actor rows on the dashboard rely on it):
   `<StatusBadge.status_badge :if={Map.get(item, :status)} status={Map.get(item, :status)} tone={tone(item)} />`
   StatusBadge's `tone` attr is `default: nil` and maps to `ax-status-badge-<tone>`; the timeline's `tone/1` returns one of moss/cobalt/amber/slate/ink — all valid badge tones — so node dot and badge stay in agreement.

3. Wrap the timestamp in a semantic `<time>` element. Replace:
   `<p :if={Map.get(item, :at)} class="ax-body ax-muted"><%= Map.get(item, :at) %></p>`
   with:
   `<time :if={Map.get(item, :at)} class="ax-timeline-time"><%= Map.get(item, :at) %></time>`
   (No `datetime` attribute — call-site values are display strings, not ISO timestamps; machine value is out of scope.)

4. Replace the empty-state borrow. Change:
   `<p :if={@items == []} class="ax-body ax-filter-chip-empty"><%= @empty_label %></p>`
   to a dedicated, calm, muted, centered block:
   `<p :if={@items == []} class="ax-body ax-timeline-empty"><%= @empty_label %></p>`

5. Keep `tone/1` exactly as-is (it now also feeds StatusBadge). The `humanize/1` private function's status path is now owned by StatusBadge — VERIFY (via grep within the module) that nothing else in `timeline.ex` still calls `humanize/1`; if and only if it becomes fully unused, remove it to avoid an unused-function warning. If any other path still uses it, leave it.

6. Keep the `<details>`/`<pre>` "Inspect details" inspector VERBATIM (a test pins the literal "Inspect details" summary). Keep the semantic `<ol>`/`<li>` ordered list and the `aria-hidden="true"` dots.

NOTE: the literal string `ax-filter-chip-empty` must NOT remain in this component after the edit (it is being replaced by `ax-timeline-empty`).
  </action>
  <verify>
    <automated>cd accrue_admin && mix compile --warnings-as-errors 2>&1 | grep -i 'warning\|error' | grep -i 'timeline' ; test $? -ne 0 && echo "no timeline warnings" ; grep -q 'StatusBadge.status_badge' lib/accrue_admin/components/timeline.ex && grep -q 'ax-timeline-time' lib/accrue_admin/components/timeline.ex && grep -q 'ax-timeline-empty' lib/accrue_admin/components/timeline.ex && ! grep -q 'ax-filter-chip-empty' lib/accrue_admin/components/timeline.ex && echo OK</automated>
  </verify>
  <done>timeline.ex aliases StatusBadge, renders status via `StatusBadge.status_badge` (with `:if` guard + `tone={tone(item)}`), renders the timestamp in `<time class="ax-timeline-time">`, renders the empty state via `ax-timeline-empty`, keeps the "Inspect details" `<details>`/`<pre>` inspector and the `<ol>`/`<li>` + aria-hidden dots, and no longer references `ax-filter-chip-empty`. Compiles with no new warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Add timeline-scoped CSS (rail, calmer node, threaded-feed entry, timestamp, empty)</name>
  <files>accrue_admin/assets/css/app.css</files>
  <action>
⚠ Grouped-selector care: `.ax-timeline-card` is grouped with `.ax-kpi-card` (border+background at app.css:968-980) and `.ax-timeline-status` is grouped with kpi/json selectors (app.css:1063-1071). DO NOT mutate those shared groups (it would break KPI cards / JSON tree). Instead, add timeline-scoped OVERRIDE rules placed AFTER the existing timeline block (which currently ends around `.ax-timeline-meta`, app.css:1455-1459). Tokens ONLY — no hardcoded hex.

Add the following timeline-scoped rules (after the existing `.ax-timeline-*` block):

(a) RAIL — draw a 1px line only BETWEEN consecutive nodes so it never overshoots the first/last:
`.ax-timeline-item:not(:last-child)::before { content: ""; position: absolute; ... }`
The line is `var(--ax-border-strong)`, 1px wide, positioned at the node-column center, running from the node center down through the inter-item gap to the next node, with `z-index` BEHIND the dot (dot must paint over it). Tune top/height against the existing `.ax-timeline-item` grid (`grid-template-columns: auto minmax(0,1fr)`, `gap: var(--ax-space-md)`), the `.ax-timeline-list` `gap: var(--ax-space-lg)`, and the dot's `width/height: 0.875rem` + `margin-top: 0.5rem` (which you are shrinking in (b)). The rail must read as visibly quiet.

(b) NODE — shrink the dot and give it a surface-colored ring so the rail reads as passing behind it. Override `.ax-timeline-dot`: width/height ~`0.6875rem` (from 0.875rem), and `box-shadow: 0 0 0 3px var(--ax-elevated)` (the surface the dot sits on). KEEP the existing `.ax-timeline-dot-{moss,cobalt,amber,slate,ink}` tone fills untouched. Adjust `margin-top` if the smaller dot needs realignment to the title baseline.

(c) ENTRY (Threaded feed — LOCKED style) — override `.ax-timeline-card` to borderless, transparent background, lighter padding (`var(--ax-space-sm)` / `var(--ax-space-md)`), and `border-radius: var(--ax-radius-md)`. Add `:hover` → `background: var(--ax-interactive-hover)` (or `var(--ax-sunken)` if hover token reads too strong) with `transition: var(--ax-transition-colors)`. This also fixes the card-in-card nesting on the dashboard (rows already sit inside the dashboard `ax-card`). Do this with a timeline-scoped `.ax-timeline-card { ... }` rule placed AFTER the shared group so it overrides border/background/padding/radius without editing the grouped selector.

(d) TIMESTAMP:
`.ax-timeline-time { font-variant-numeric: tabular-nums; color: var(--ax-muted); font-size: var(--ax-type-sm); }`

(e) EMPTY:
`.ax-timeline-empty { ... }` — a calm neutral block: centered text, `color: var(--ax-muted)`, comfortable padding (token spacing). No border/pill.

(f) DARK MODE — confirm the rail + hover surface read on `[data-theme="dark"]`. The chosen tokens (`--ax-border-strong`, `--ax-elevated`, `--ax-interactive-hover`/`--ax-sunken`, `--ax-muted`) should auto-adapt; add explicit `[data-theme="dark"]` overrides ONLY if a token does not auto-adapt.

(g) REDUCED MOTION — rail and nodes are static (no animation). The hover transition uses `var(--ax-transition-colors)`, which already collapses under the existing `prefers-reduced-motion` motion-token handling — do not add a separate animation.
  </action>
  <verify>
    <automated>cd accrue_admin && grep -q 'ax-timeline-item:not(:last-child)' assets/css/app.css && grep -q 'ax-timeline-time' assets/css/app.css && grep -q 'ax-timeline-empty' assets/css/app.css && ! grep -nE '#[0-9a-fA-F]{3,6}' <(awk '/ax-timeline/{print}' assets/css/app.css) && echo OK</automated>
  </verify>
  <done>app.css contains a timeline-scoped rail rule (`.ax-timeline-item:not(:last-child)::before`), a shrunk node with `box-shadow ... var(--ax-elevated)` ring, a borderless/transparent threaded-feed `.ax-timeline-card` override with a token-based `:hover` surface + `--ax-transition-colors`, `.ax-timeline-time` (tabular-nums, muted, `--ax-type-sm`), and `.ax-timeline-empty` (calm muted block). The shared `.ax-kpi-card` / `.ax-timeline-status` / json-tree grouped selectors are unmodified. All values use tokens (no hardcoded hex in any timeline rule). Dark-mode + reduced-motion confirmed via tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Register timeline in the component lab, extend tests, rebuild + commit the CSS bundle</name>
  <files>accrue_admin/lib/accrue_admin/dev/component_registry.ex, accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex, accrue_admin/test/accrue_admin/components/display_components_test.exs, accrue_admin/priv/static/accrue_admin.css</files>
  <action>
1) REGISTRY — `component_registry.ex`. Add a `"timeline"` family entry to the `entries/0` list (mirror the composite `theme-picker`/`tabs`/`filter_chip_bar` entries):
   - `family: "timeline"`
   - `variant: "threaded"`
   - `ax_class: "ax-timeline ax-timeline-item"` (base + a variant class the DEFAULT specimen actually renders — the render-coverage guardrail requires two space-separated tokens AND that the default specimen is NON-EMPTY so `ax-timeline-item` actually appears).
   - `tokens: ["--ax-border-strong", "--ax-elevated", "--ax-success", "--ax-accent", "--ax-warning", "--ax-muted", "--ax-primary", "--ax-radius-lg", "--ax-space-lg", "--ax-transition-colors"]`
   - `applicable_states: ["default", "hover", "empty"]`
   - `na_states:` provide entries with reasons for the remaining states in the taxonomy (mirror the theme-picker na_states shape: selected, focus, active, pressed, disabled, loading, error, overflow — give each a one-line reason appropriate to a read-only event feed; e.g. focus → "details summary focus is a live keyboard state, not a static specimen", overflow → "long bodies wrap via overflow-wrap, not a distinct specimen").
   - `specimens:` two — a populated multi-tone timeline (label e.g. "Multi-tone feed") and an empty state (label e.g. "Empty"). Match the specimen map shape used by neighboring composite families (e.g. `%{label: ..., props: %{...}, content: nil}`); the kitchen renderer (below) is what actually materializes them, so props can be minimal.

2) KITCHEN — `component_kitchen_live.ex`:
   - Add `Timeline` to the `alias AccrueAdmin.Components.{...}` block (alphabetical neighborhood near `Toggle`/`ThemePicker`).
   - Add `defp family_label("timeline"), do: "Timeline"` alongside the other `family_label/1` clauses.
   - Add two `do_render_specimen` clauses (place them near the other composite renderers, before the fallback clause):
     - `defp do_render_specimen("timeline", "empty", _specimen, _theme)` → renders `<Timeline.timeline items={[]} />` (the empty-state path).
     - `defp do_render_specimen("timeline", _state, _specimen, _theme)` → renders `<Timeline.timeline items={sample} />` where `sample` is a small multi-tone literal list exercising several tones + a status + a timestamp + a body + a details block, e.g.:
       `[%{title: "Payment succeeded", at: "Apr 15, 2026 09:14", status: :succeeded, body: "Invoice in_123 paid"}, %{title: "Webhook retrying", at: "Apr 15, 2026 09:12", status: :retrying, body: "payment_intent.payment_failed", details: "{\"attempt\": 2}"}, %{title: "Subscription canceled", at: "Apr 14, 2026 17:02", status: :canceled, meta: "Ends at period end"}]`
       (assign `sample` in the function body before the `~H` sigil; include `__changed__: %{}` in `assigns` per the existing renderer pattern). The default specimen MUST be non-empty so `ax-timeline-item` renders (render-coverage guardrail).

3) TESTS — extend the existing `describe "Timeline"` block in `display_components_test.exs` (around line 291). KEEP the existing assertions green (escaping `&lt;script&gt;`, the literal "Inspect details", "Moved to DLQ", "Next retry requires manual action", "Webhook received", "April 15, 2026"). ADD structural assertions to the existing test (or a new test in the same describe):
   - an `ax-timeline-item` appears once per item (e.g. assert the count of `ax-timeline-item` occurrences equals the number of items, or simply assert `html =~ "ax-timeline-item"`);
   - status renders via the shared badge: `assert html =~ "ax-status-badge"`;
   - a semantic time element: `assert html =~ "<time"` (and/or `assert html =~ "ax-timeline-time"`).
   NOTE: the existing fixture statuses `:queued`/`:dlq` map (via `tone/1`) to cobalt/amber — both valid StatusBadge tones — so the badge renders cleanly. Do NOT change the existing fixture data.

4) REBUILD THE COMMITTED BUNDLE (REQUIRED — the admin serves the committed bundle, not source app.css):
   `cd accrue_admin && mix accrue_admin.assets.build`
   This regenerates `priv/static/accrue_admin.css` from the edited `app.css`. The rebuilt bundle MUST contain the new rail rule.

Do NOT stage/commit `examples/accrue_host/mix.lock` or `.planning/research/.cache/`.
  </action>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.assets.build && mix test test/accrue_admin/components/display_components_test.exs test/accrue_admin/live/dashboard_live_test.exs test/accrue_admin/live/webhook_live_test.exs test/accrue_admin/live/subscription_live_test.exs 2>&1 | tail -5 && [ "$(grep -c 'ax-timeline' priv/static/accrue_admin.css)" -ge 1 ] && grep -q 'ax-timeline-item:not(:last-child)' priv/static/accrue_admin.css && grep -q 'family_label("timeline")' lib/accrue_admin/dev/component_kitchen_live.ex && grep -q '"ax-timeline ax-timeline-item"' lib/accrue_admin/dev/component_registry.ex && echo OK</automated>
  </verify>
  <done>The `timeline` family is registered in `component_registry.ex` (ax_class `"ax-timeline ax-timeline-item"`, the 10 listed tokens, `applicable_states ["default","hover","empty"]`, na_states with reasons, populated + empty specimens). The kitchen aliases `Timeline`, has `family_label("timeline")`, and renders both an empty and a non-empty multi-tone `<Timeline.timeline>` specimen. The Timeline test describe asserts per-item `ax-timeline-item`, `ax-status-badge`, and a `<time` element while keeping all prior assertions green. `priv/static/accrue_admin.css` is rebuilt and contains `ax-timeline-item:not(:last-child)`. The 4 named live test suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a presentational restyle of an existing admin component with an identical data contract. No new untrusted input crosses any boundary; no auth, persistence, or network surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ps2-01 | Tampering (XSS) | Timeline item rendering (title/body/details/meta) | mitigate | Continue rendering all item fields through HEEx `<%= %>` auto-escaping (the existing `&lt;script&gt;` escaping test stays green); no `raw/1`, no `Phoenix.HTML.raw`, no inner_block injection introduced. |
| T-ps2-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps; `mix accrue_admin.assets.build` uses already-installed tooling). No legitimacy gate required. |
</threat_model>

<verification>
1. `cd accrue_admin && mix accrue_admin.assets.build && mix test` — all green, especially:
   `test/accrue_admin/components/display_components_test.exs`, `test/accrue_admin/live/dashboard_live_test.exs`, `test/accrue_admin/live/webhook_live_test.exs`, `test/accrue_admin/live/subscription_live_test.exs`.
2. `grep -c ax-timeline priv/static/accrue_admin.css` ≥ 1 AND the rail rule `ax-timeline-item:not(:last-child)` is present in the BUILT bundle (not just source).
3. Component-lab render-coverage + axe-a11y guardrails stay green; the default `timeline` specimen renders `ax-timeline-item` (non-empty).
4. No hardcoded hex in any timeline CSS rule; shared `.ax-kpi-card` / `.ax-timeline-status` / json-tree grouped selectors unmodified.
5. `examples/accrue_host/mix.lock` and `.planning/research/.cache/` are NOT in the commit.

(Visual spot-check at `http://accrue.localhost/admin` after rebuild — one continuous rail threading the nodes, tinted status badges, tabular timestamps, clean light/dark, and the other 3 surfaces still rendering — is recommended but not a blocking automated gate.)
</verification>

<success_criteria>
- Timeline renders a continuous vertical rail (between consecutive nodes only), calmer ringed nodes, borderless threaded-feed entries with a token-based hover surface, shared StatusBadge status (tone matches node), tabular `<time>` timestamps, and a dedicated `ax-timeline-empty` empty state.
- All 4 call sites render unchanged (zero data-contract changes); the 4 named live test suites pass.
- `timeline` family is visible in `/billing/dev/components` with populated + empty specimens; lab coverage + a11y guardrails green.
- `priv/static/accrue_admin.css` is rebuilt and committed, containing the new rail rule.
- StatusBadge is unmodified; shared grouped CSS selectors are unmodified; tokens-only (no hex).
</success_criteria>

<output>
Create `.planning/quick/260620-ps2-elevate-the-timeline-component-into-a-pr/260620-ps2-SUMMARY.md` when done.
</output>

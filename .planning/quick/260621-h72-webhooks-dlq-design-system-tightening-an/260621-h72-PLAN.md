---
phase: quick-260621-h72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/assets/css/app.css
  - accrue_admin/assets/css/theme.css
  - accrue_admin/lib/accrue_admin/components/kpi_card.ex
  - accrue_admin/lib/accrue_admin/components/data_table.ex
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/lib/accrue_admin/queries/webhooks.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/copy/locked.ex
  - accrue_admin/lib/accrue_admin/dev/component_registry.ex
  - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
  - accrue_admin/test/accrue_admin/components/data_table_test.exs
  - accrue_admin/test/accrue_admin/live/webhooks_live_test.exs
  - accrue_admin/priv/static/accrue_admin.css
  - accrue_admin/priv/static/accrue_admin.js
autonomous: true
requirements: [QUICK-260621-h72]

must_haves:
  truths:
    - "On load in dark mode, no blue UA focus box paints on the main content region; keyboard skip-link still lands focus on main."
    - "KPI card secondary stat (e.g. '1 dead-lettered') never wraps mid-word and the muted meta caption sits on its own full-width line below it."
    - "On /admin/webhooks an operator can check specific rows and click a primary 'Retry selected' button; a plain-language confirm then retries exactly those selected ids and records an audit event of the selected count."
    - "The 8 non-webhooks list pages no longer render selection checkboxes or a selection bar."
    - "DataTable footer reads 'Showing N events' / 'Showing N results' (plural-aware), not 'N rows loaded'."
    - "Webhooks Type filter offers autocomplete of real distinct event types; Status filter shows counts with zero-count options disabled; Live mode is an All/Live/Test segmented control."
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      provides: "Selection emit to parent + plural-aware footer + selectable default false + :datalist/:segmented filter clauses"
      contains: "data_table_bulk_action"
    - path: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      provides: "Selection-driven retry handle_info, inline confirm, plain copy; DLQ bulk-replay card removed"
      contains: "data_table_bulk_action"
    - path: "accrue_admin/lib/accrue_admin/queries/webhooks.ex"
      provides: "distinct_types/1 and status_counts/1 aggregate queries (owner-scoped via scope_rows)"
      contains: "def distinct_types"
    - path: "accrue_admin/assets/css/theme.css"
      provides: "--ax-accent-contrast token defined in light + dark blocks"
      contains: "--ax-accent-contrast"
    - path: "accrue_admin/priv/static/accrue_admin.css"
      provides: "Rebuilt committed CSS bundle containing .ax-segmented and .ax-kpi-meta"
      contains: "ax-segmented"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      to: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      via: "bulk-action button sends {:data_table_bulk_action, event, ids} to parent self()"
      pattern: "data_table_bulk_action"
    - from: "accrue_admin/lib/accrue_admin/live/webhooks_live.ex"
      to: "accrue_admin/lib/accrue_admin/queries/webhooks.ex"
      via: "filter field-spec options populated by distinct_types/status_counts"
      pattern: "Webhooks\\.(distinct_types|status_counts)"
---

<objective>
Faithfully translate the approved plan (Webhooks DLQ page: design-system tightening + selection-driven retry) into executable GSD form. Five reusable design-system fixes plus a rebuilt static bundle, landing as six atomic commits in order: (1) blue-border CSS, (2) KpiCard footer + `--ax-accent-contrast` token, (3) DataTable selection-emit + footer copy + `selectable` default flip, (4) webhooks selection-driven retry + plain microcopy, (5) filter overhaul + new admin queries + component-lab registration, (6) rebuilt `priv/static` bundle.

Purpose: Fix five firsthand interaction/design defects on `/admin/webhooks?status=dead` by improving SHARED primitives (KpiCard, DataTable, filter system, copy) so the dividends reach all 9 list pages — not a one-off page patch.

Output: Updated shared components + admin-only queries + copy + component-lab registration + tests + committed rebuilt static bundle. No core `accrue` API changes, no host changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/Users/jon/.claude/plans/i-just-got-an-ethereal-harbor.md

@accrue_admin/lib/accrue_admin/components/kpi_card.ex
@accrue_admin/lib/accrue_admin/components/data_table.ex
@accrue_admin/lib/accrue_admin/live/webhooks_live.ex
@accrue_admin/lib/accrue_admin/queries/webhooks.ex
@accrue_admin/lib/accrue_admin/copy.ex
@accrue_admin/lib/accrue_admin/copy/locked.ex
@accrue_admin/lib/accrue_admin/dev/component_registry.ex
@accrue_admin/assets/css/app.css
@accrue_admin/assets/css/theme.css

KEY FACTS (verified during planning — honor exactly):
- accrue_admin serves the COMMITTED priv/static/accrue_admin.{css,js} bundle, NOT source app.css/app.js. After ANY app.css/app.js/theme.css change you MUST run `cd accrue_admin && mix accrue_admin.assets.build` and commit both priv/static files. assets_test.exs asserts the committed md5 hash — Task 6 is the dedicated rebuild commit.
- DataTable is a LiveComponent: `selected_ids` is a private MapSet (data_table.ex:28) and is currently NEVER emitted to the parent — that is why selection is dead on all 9 pages. The bulk-action button must `send(self(), {:data_table_bulk_action, event, MapSet.to_list(selected_ids)})` so the PARENT LiveView handles the side effect via handle_info.
- `assign_new(:selectable, fn -> true end)` at data_table.ex:37 must flip to `false`; webhooks opts in with `selectable={true}`. The other 8 pages then drop orphaned checkboxes with no per-page edits.
- New aggregate queries (`distinct_types/1`, `status_counts/1`) go in queries/webhooks.ex, admin-only, NO core accrue change. They MUST respect owner_scope by post-fetch `scope_rows/2` — match the established post-fetch pattern in that file (do not refactor to SQL-pushdown).
- StatusBadge is shared — DO NOT modify it. Do NOT weaken CSP. Do NOT touch examples/accrue_host/mix.lock or .planning/research/.cache/. Do NOT update ROADMAP.md.
- ComponentRegistry entries require `ax_class` to be exactly a base+variant two-class string; the default specimen MUST render the variant class non-empty (render-coverage guardrail). New `segmented` family ax_class = "ax-segmented ax-segmented-option" (mirror the existing theme-picker entry at component_registry.ex:923).
- filter_input/1 pattern-matches on field.type (data_table.ex:307 :select / :320 :checkbox / :335 text fallback). Add :datalist and :segmented as NEW additive clauses; extend the :select clause to honor a `disabled` option key + render a count in the label. option_value/option_label/option_selected? helpers handle tuples AND maps already.
- CSS anchors: `.ax-shell-content` block app.css:912; `.ax-kpi-delta` app.css:1059 (min-height 1.75rem, padding 0.125rem 0.6875rem, has `overflow-wrap: anywhere`); theme-picker pattern app.css:850. `--ax-accent-contrast` is consumed at app.css:593/1734/2721 but UNDEFINED in theme.css (silently falls back to white).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill the blue UA focus box on .ax-shell-content load (CSS, commit 1)</name>
  <files>accrue_admin/assets/css/app.css</files>
  <action>Near the `.ax-shell-content` block (app.css:912), add a rule that suppresses the UA outline ONLY on programmatic (non-keyboard) focus, preserving the ring for genuine keyboard focus: `.ax-shell-content:focus:not(:focus-visible) { outline: none; }` with a short comment explaining that `<main id="main-content" tabindex="-1">` is programmatically focused on mount/nav by `connection_state.js` `ensurePageFocus()` (AT-cursor move), is never keyboard-tabbable, and in dark mode the UA outline reads as a bright blue box via `--ax-focus-ring`. Tokens/standard CSS only. Do NOT remove the `ensurePageFocus()` JS. This is the ONLY change in commit 1 (do not rebuild the bundle yet — Task 6 rebuilds once).</action>
  <verify>
    <automated>cd accrue_admin && grep -q 'ax-shell-content:focus:not(:focus-visible)' assets/css/app.css && echo OK</automated>
  </verify>
  <done>Source app.css contains the `.ax-shell-content:focus:not(:focus-visible) { outline: none; }` rule; `ensurePageFocus()` JS untouched. Committed as atomic commit 1.</done>
</task>

<task type="auto">
  <name>Task 2: KpiCard footer redesign + define --ax-accent-contrast token (commit 2)</name>
  <files>accrue_admin/assets/css/app.css, accrue_admin/assets/css/theme.css, accrue_admin/lib/accrue_admin/components/kpi_card.ex</files>
  <action>Three coordinated changes, one commit:
1. CSS `.ax-kpi-delta` (app.css:1059): add `white-space: nowrap;`, tighten `padding` to `0.0625rem 0.5rem;`, lower `min-height` to ~`1.5rem` so secondary stats like "1 dead-lettered" never break. Keep the tone backgrounds. (The existing `overflow-wrap: anywhere` allows wrapping — `white-space: nowrap` overrides it; you may drop `overflow-wrap` from this selector since nowrap is intended.)
2. CSS `.ax-kpi-card-footer`: keep `flex-direction: column`, `align-items: flex-start`, `gap: var(--ax-space-sm)`; drop any `justify-content: space-between` that pulls pill and caption apart so the meta caption renders full-width on its own line below the pill. Add a `.ax-kpi-meta` selector: `color: var(--ax-muted); font-size: var(--ax-type-sm);` (use the existing muted/type-sm token names — confirm them in theme.css; if `--ax-type-sm` is not the token name, use the established small-type token).
3. kpi_card.ex (line 55): wrap `render_slot(@meta)` in `<p class="ax-kpi-meta"><%= render_slot(@meta) %></p>` so the caption is a real stylable element. Keep the existing `<span :if={@delta} class={["ax-kpi-delta", ...]}>` pill. The public data contract (label/value/delta/delta_tone/:meta slot) stays unchanged so all 9 call sites keep working untouched.
4. BUG FREEBIE: define `--ax-accent-contrast: #ffffff;` in BOTH the light and dark token blocks of theme.css (it is consumed by `.ax-button-primary` at app.css:593 but undefined, silently falling back to white — so this is zero visual change, just correctness). Locate the light/dark `:root`/`[data-theme]` token blocks in theme.css and add the var to each.
Do NOT rebuild the bundle here — Task 6 rebuilds once.</action>
  <verify>
    <automated>cd accrue_admin && grep -q 'white-space: nowrap' assets/css/app.css && grep -q 'ax-kpi-meta' assets/css/app.css && grep -q 'ax-kpi-meta' lib/accrue_admin/components/kpi_card.ex && grep -c -- '--ax-accent-contrast:' assets/css/theme.css</automated>
  </verify>
  <done>`.ax-kpi-delta` has `white-space: nowrap` + tightened padding/min-height; `.ax-kpi-meta` styled selector exists; kpi_card.ex wraps the meta slot in `<p class="ax-kpi-meta">`; `--ax-accent-contrast: #ffffff;` defined in both theme.css token blocks (grep count >= 2); all 9 KpiCard call sites still compile. Committed as atomic commit 2.</done>
</task>

<task type="auto">
  <name>Task 3: Emit selection from shared DataTable + plural footer + selectable default flip (commit 3)</name>
  <files>accrue_admin/lib/accrue_admin/components/data_table.ex, accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/test/accrue_admin/components/data_table_test.exs</files>
  <action>Make the shared DataTable emit its selection so a parent can act on it:
1. New optional attrs assigned via `assign_new`: `bulk_action_label` (default `nil`), `bulk_action_event` (default `nil`), `row_label` (default `{"result", "results"}`).
2. In the selection bar (`data-role="selection-bar"`, render ~line 193): when `@selectable` and `@bulk_action_event` is set and `MapSet.size(@selected_ids) > 0`, render a PRIMARY button (`ax-button ax-button-primary`) labeled `@bulk_action_label`. Add a `handle_event("bulk-action", _params, socket)` clause that does NOT mutate the component — it notifies the parent: `send(self(), {:data_table_bulk_action, socket.assigns.bulk_action_event, MapSet.to_list(socket.assigns.selected_ids)})` then `{:noreply, socket}`. Wire the button with `phx-click="bulk-action" phx-target={@myself}`.
3. Flip the default: change `assign_new(:selectable, fn -> true end)` (data_table.ex:37) to `fn -> false end`.
4. Footer copy (data_table.ex:285): replace the hardcoded `"#{length(@rows)} rows loaded"` with `Copy.data_table_row_count(length(@rows), @row_label)`. Add `Copy.data_table_row_count/2` in copy.ex: takes a count and a `{singular, plural}` tuple, returns `"Showing 1 event"` / `"Showing 3 events"` (plural-aware on count == 1).
5. Keep the `"N selected"` selection-count copy as-is (correct now that the action is real).
Tests (data_table_test.exs): pass `selectable={true}` in every test that asserts selection UI (default is now false); add a test that clicking the bulk-action button emits `{:data_table_bulk_action, event, ids}` to the parent test process; update footer-copy assertions from "N rows loaded" to "Showing N <label>".
Do NOT rebuild the bundle here.</action>
  <verify>
    <automated>cd accrue_admin && grep -q 'data_table_bulk_action' lib/accrue_admin/components/data_table.ex && grep -q 'assign_new(:selectable, fn -> false end)' lib/accrue_admin/components/data_table.ex && grep -q 'def data_table_row_count' lib/accrue_admin/copy.ex && mix test test/accrue_admin/components/data_table_test.exs 2>&1 | tail -3</automated>
  </verify>
  <done>DataTable emits `{:data_table_bulk_action, event, ids}` to its parent on bulk-action click; `selectable` defaults to `false`; footer uses `Copy.data_table_row_count/2` (plural-aware "Showing N events"); data_table_test green with the selectable={true} updates and the new emit test. Committed as atomic commit 3.</done>
</task>

<task type="auto">
  <name>Task 4: Webhooks page — selection-driven retry + plain microcopy (commit 4)</name>
  <files>accrue_admin/lib/accrue_admin/live/webhooks_live.ex, accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/lib/accrue_admin/copy/locked.ex, accrue_admin/test/accrue_admin/live/webhooks_live_test.exs</files>
  <action>Replace the filter-driven DLQ replay with a selection-driven flow:
1. DataTable call (webhooks_live.ex:214): add `selectable={true}`, `row_label={{"event", "events"}}`, `bulk_action_label={Copy.webhooks_retry_selected_label()}`, `bulk_action_event="retry_selected"`.
2. DELETE the "DLQ bulk replay" card entirely (the `<section class="ax-card">` block at lines 170-212: eyebrow "DLQ bulk replay" + h3 + body + the "Replay filtered DLQ rows" secondary button + the inline `@pending_bulk_replay` confirm section). Also delete the filter-based handlers `prepare_bulk_replay` (line 51) and `cancel_bulk_replay`/`confirm_bulk_replay` (lines 62/66) — they are replaced below.
3. Add `handle_info({:data_table_bulk_action, "retry_selected", ids}, socket)`: set `pending_bulk_replay = %{ids: ids, count: length(ids)}`. Render an inline confirm card (retrying is consequential — keep a confirm step) with a PRIMARY Confirm (`ax-button-primary`, now the screen's primary action) and a ghost Cancel. Re-add `cancel_bulk_replay` (clears `pending_bulk_replay`) and a `confirm_retry_selected` handler.
4. Confirm path: reuse `replay_scoped_rows/1` (the per-id `DLQ.requeue` reducer, keep it) but seed from the SELECTED ids instead of `DLQ.list(filter)`. Validate each selected id is in owner scope via `Webhooks.detail/2` before requeue (mirror the existing `replay_scope/2` owner filter — `match?({:ok, _}, Webhooks.detail(id, owner_scope))`). On success refresh `summary` via `webhook_summary/1` and flash success. Keep the audit event (`record_bulk_replay` → `Events.record` + `Auth.log_audit`) but record the SELECTED count + ids instead of the filter (update `record_bulk_replay` to accept ids/count; subject_id can be the joined selected ids or a count summary — keep `subject_type: "WebhookBatch"`; store `"ids"` and `"count"` in the event `data`).
5. Empty-state helper above the table (one short line, plain language): "Events that failed every automatic retry land here. Filter, select the ones to re-run, then Retry selected." Render it as `<p class="ax-body ...">` above the `.live_component` (or via the DataTable empty copy already set — but this helper line is always shown above the table).
6. MICROCOPY — move all strings into Copy / Copy.Locked, JTBD-plain, NO "DLQ"/"cap"/"intent"/"slice"/"dead-letter"/"audit event" jargon. Add to copy.ex: `webhooks_retry_selected_label/0` → "Retry selected"; `webhooks_retry_confirm_question/2` (count + owner_scope opts) → "Retry N webhook events? They failed every automatic retry — retrying runs them through processing again." (plural-aware on count); a no-selection guard string and a success flash string ("Retrying N events…", plural-aware). REMOVE now-orphaned filter-replay strings after grepping for remaining references: `webhooks_bulk_replay_confirm_question/1,2` and `webhooks_bulk_no_rows_warning/0` (copy.ex:655-665) — grep the whole accrue_admin tree first; only delete if no other caller remains. Keep `Copy.Locked` replay-scope strings (`replay_blocked`, `bulk_replay_success_*`) if still referenced by the scoped-retry path.
Tests (webhooks_live_test.exs): replace filter-driven replay assertions with selection-driven — select rows, click Retry selected, confirm, assert exactly those ids retried and the audit records the selected count; assert the new plain microcopy (and that old jargon strings are gone).
Do NOT rebuild the bundle here.</action>
  <verify>
    <automated>cd accrue_admin && grep -q 'data_table_bulk_action.*retry_selected' lib/accrue_admin/live/webhooks_live.ex && grep -q 'def webhooks_retry_selected_label' lib/accrue_admin/copy.ex && grep -q 'def webhooks_retry_confirm_question' lib/accrue_admin/copy.ex && ! grep -q 'DLQ bulk replay' lib/accrue_admin/live/webhooks_live.ex && mix test test/accrue_admin/live/webhooks_live_test.exs 2>&1 | tail -3</automated>
  </verify>
  <done>Webhooks page is selection-driven: handle_info routes `{:data_table_bulk_action, "retry_selected", ids}` to an inline plain-language confirm → retries exactly those owner-scoped ids → refreshes summary + records an audit event of the selected count. The "DLQ bulk replay" card and filter-based handlers are gone; no jargon copy remains; webhooks_live_test green. Committed as atomic commit 4.</done>
</task>

<task type="auto">
  <name>Task 5: Filter overhaul + new admin queries + component-lab registration (commit 5)</name>
  <files>accrue_admin/lib/accrue_admin/components/data_table.ex, accrue_admin/lib/accrue_admin/queries/webhooks.ex, accrue_admin/lib/accrue_admin/live/webhooks_live.ex, accrue_admin/lib/accrue_admin/dev/component_registry.ex, accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex, accrue_admin/assets/css/app.css, accrue_admin/test/accrue_admin/components/data_table_test.exs</files>
  <action>Three filter upgrades (all additive — the 8 other pages are unaffected), new owner-scoped queries, and lab registration:
1. NEW admin queries in queries/webhooks.ex (admin-only, owner-scoped via the existing post-fetch `scope_rows/2` — match that pattern, do NOT SQL-pushdown):
   - `distinct_types(owner_scope)`: fetch WebhookEvent rows, `scope_rows`, then map `.type`, `Enum.uniq`, `Enum.sort` → list of distinct type strings. (Post-fetch distinct keeps owner scoping correct, consistent with `count/2` and `bulk_replay_count/2` in this module.)
   - `status_counts(owner_scope)`: fetch rows, `scope_rows`, then `Enum.frequencies_by(& &1.status)` → map of status → count.
2. data_table.ex filter_input/1 — add NEW pattern clauses (do not touch the existing :select/:checkbox/text clauses except the :select extension in 2b):
   - `:datalist` clause: renders a text `<input list={list_id} ...>` + a `<datalist id={list_id}>` of `<option value=…>` for each field option (native autocomplete, no JS, CSP-safe, still free-typeable). Use a deterministic list id derived from `@id`.
   - 2b. Extend the existing `:select` clause to honor a `disabled` option key (render `disabled={...}` on the `<option>`) and to render a count inside the label when options are maps carrying a count/label. Use the existing `option_value`/`option_label`/`option_selected?` helpers (they already handle maps + tuples). Do NOT disable the currently-selected value even at zero count (so the active filter stays selectable).
   - `:segmented` clause: renders a small radiogroup styled like the theme-picker segmented control — reuse the `.ax-theme-picker*` visual pattern as NEW shared `.ax-segmented*` classes. Radio inputs submit normally in the GET form (no JS needed for submission; arrow-key roving optional, mirror the theme-picker if added — but if it adds JS, that JS rebuild rides in Task 6). Field renders the field's options as segments.
3. CSS in app.css — add `.ax-segmented` and `.ax-segmented-option` (+ active/hover/focus states) modeled on the `.ax-theme-picker*` block (app.css:850), tokens only. (Bundle rebuild is Task 6.)
4. webhooks_live.ex filter_fields (line 235): change Type to `%{id: :type, label: "Type", type: :datalist, options: Webhooks.distinct_types(@current_owner_scope)}`; change Status options to maps `%{value: ..., label: "Dead (N)", disabled: count == 0}` built from `Webhooks.status_counts(@current_owner_scope)` (don't disable the active value at zero); change Live mode to `%{id: :livemode, label: "Live mode", type: :segmented, options: [{"", "All"}, {"true", "Live"}, {"false", "Test"}]}`.
5. Component lab: register the new `segmented` filter family in component_registry.ex (two-class `ax_class` exactly `"ax-segmented ax-segmented-option"`, default + active states, a specimen whose default rendering contains the variant class `ax-segmented-option` to satisfy the render-coverage guardrail — mirror the theme-picker entry at component_registry.ex:923). Wire any specimen rendering needed in component_kitchen_live.ex following the existing family pattern. Re-render the existing `kpi-card` family specimen with the tightened footer (no class renames, so the registry guardrails stay green).
Tests (data_table_test.exs): assert the :datalist input + datalist render, the :select count-in-label + disabled-on-zero (and active value NOT disabled at zero), and the :segmented radiogroup render. Add/keep query coverage for distinct_types/status_counts owner scoping if the queries test module is the natural home (otherwise assert via the live test).
Do NOT rebuild the bundle here.</action>
  <verify>
    <automated>cd accrue_admin && grep -q 'def distinct_types' lib/accrue_admin/queries/webhooks.ex && grep -q 'def status_counts' lib/accrue_admin/queries/webhooks.ex && grep -q ':datalist' lib/accrue_admin/components/data_table.ex && grep -q ':segmented' lib/accrue_admin/components/data_table.ex && grep -q 'ax-segmented' assets/css/app.css && grep -q 'ax-segmented ax-segmented-option' lib/accrue_admin/dev/component_registry.ex && mix test test/accrue_admin/components/data_table_test.exs 2>&1 | tail -3</automated>
  </verify>
  <done>distinct_types/1 + status_counts/1 owner-scoped queries exist; filter_input/1 has additive :datalist + :segmented clauses and the extended :select (count + disabled-on-zero, active value never disabled); `.ax-segmented*` CSS added; webhooks filters use datalist Type / counted Status / segmented Live mode; `segmented` family registered in the component lab passing the render-coverage guardrail; data_table_test green. Committed as atomic commit 5.</done>
</task>

<task type="auto">
  <name>Task 6: Rebuild and commit the static asset bundle + full verify (commit 6)</name>
  <files>accrue_admin/priv/static/accrue_admin.css, accrue_admin/priv/static/accrue_admin.js</files>
  <action>Tasks 1/2/5 changed source CSS (and possibly app.js if the segmented control added roving-key JS). The admin serves the COMMITTED `priv/static/accrue_admin.{css,js}` bundle, not source. Run `cd accrue_admin && mix accrue_admin.assets.build` to regenerate both bundles, then commit `priv/static/accrue_admin.css` AND `priv/static/accrue_admin.js`. `assets_test.exs` asserts the committed md5 hash — it will fail until the rebuilt bundles are committed. This is the dedicated final atomic commit (commit 6); no source changes ride in it.
Then run the full gate: `mix compile --warnings-as-errors` clean and `mix test` all green (especially data_table, webhooks_live, display_components / kpi_card, app_shell/navigation, assets_test, and the component-lab render-coverage guardrails).</action>
  <verify>
    <automated>cd accrue_admin && mix accrue_admin.assets.build && grep -q 'ax-segmented' priv/static/accrue_admin.css && grep -q 'ax-kpi-meta' priv/static/accrue_admin.css && mix compile --warnings-as-errors && mix test 2>&1 | tail -5</automated>
  </verify>
  <done>Rebuilt `priv/static/accrue_admin.{css,js}` committed; built CSS contains `.ax-segmented` and `.ax-kpi-meta`; `mix compile --warnings-as-errors` clean; full `mix test` green including assets_test (committed md5) and component-lab guardrails. Committed as atomic commit 6.</done>
</task>

</tasks>

<verification>
1. `cd accrue_admin && mix accrue_admin.assets.build && mix compile --warnings-as-errors` clean; `mix test` all green (esp. `data_table`, `webhooks_live`, `display_components`/`kpi_card`, `app_shell`/navigation, `assets_test`, component-lab render-coverage guardrails).
2. Visual at `http://accrue.localhost/admin/webhooks?status=dead` (hot-reloads after the bundle rebuild):
   - No blue box on `.ax-shell-content` at load in dark mode; keyboard skip-link still lands focus on main.
   - KPI cards: tidy stacked footer, pill never wraps, muted caption on its own line; light + dark.
   - Filters: Type type-ahead suggests real event types; Status shows "Dead (N)" with zero-count options disabled (active value never disabled); Live mode is an All/Live/Test segmented toggle.
   - Select rows → primary "Retry selected" → plain-language confirm → exactly those events retried, summary refreshes, audit event recorded; no jargon anywhere; footer reads "Showing N events".
   - Other 8 list pages: no checkboxes/selection bar; footers read "Showing N results".
3. `grep` built CSS: `.ax-segmented` present, `.ax-kpi-meta` present, `--ax-accent-contrast` defined in theme.css.
</verification>

<success_criteria>
- Six atomic commits land in the specified order (blue-border CSS → KpiCard+token → DataTable selection-emit+footer+default → webhooks selection-driven retry+copy → filter overhaul+queries+lab → rebuilt bundle).
- No core `accrue` API change; no host change; ROADMAP.md, CSP, StatusBadge, examples/accrue_host/mix.lock, and .planning/research/.cache/ all untouched.
- `mix compile --warnings-as-errors` clean and full `mix test` green.
- All six must_have truths observable on `/admin/webhooks?status=dead` and across the other 8 list pages.
</success_criteria>

<output>
Create `.planning/quick/260621-h72-webhooks-dlq-design-system-tightening-an/260621-h72-SUMMARY.md` when done.
</output>
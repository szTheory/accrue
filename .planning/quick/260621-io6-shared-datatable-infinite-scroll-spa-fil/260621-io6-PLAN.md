---
phase: quick-260621-io6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_admin/lib/accrue_admin/components/data_table.ex
  - accrue_admin/lib/accrue_admin/data_table_nav.ex
  - accrue_admin/lib/accrue_admin/live/subscriptions_live.ex
  - accrue_admin/lib/accrue_admin/live/customers_live.ex
  - accrue_admin/lib/accrue_admin/live/invoices_live.ex
  - accrue_admin/lib/accrue_admin/live/charges_live.ex
  - accrue_admin/lib/accrue_admin/live/coupons_live.ex
  - accrue_admin/lib/accrue_admin/live/promotion_codes_live.ex
  - accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex
  - accrue_admin/lib/accrue_admin/live/events_live.ex
  - accrue_admin/lib/accrue_admin/live/webhooks_live.ex
  - accrue_admin/lib/accrue_admin/components/id_badge.ex
  - accrue_admin/lib/accrue_admin/components/icon.ex
  - accrue_admin/lib/accrue_admin/dev/component_registry.ex
  - accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
  - accrue_admin/lib/accrue_admin/copy.ex
  - accrue_admin/lib/accrue_admin/queries/customers.ex
  - accrue_admin/assets/css/app.css
  - accrue_admin/assets/js/hooks/clipboard.js
  - accrue_admin/assets/js/app.js
  - accrue_admin/priv/static/accrue_admin.css
  - accrue_admin/priv/static/accrue_admin.js
autonomous: true
requirements: [QUICK-260621-io6-B]

must_haves:
  truths:
    - "Scrolling to the bottom of any of the 9 admin list pages auto-loads the next page while a Load more button remains for no-JS/a11y and past the dom_limit cap"
    - "Applying or clearing a filter on any list page updates the URL via push_patch with no full page reload or flash"
    - "The Apply and Clear filter actions are visually grouped and no longer sit flush against the field above them"
    - "An admin can click an ID chip on the customers list to copy the customer processor id to the clipboard"
    - "The customers page reads as an operator find-and-open surface: heading Customers, plain description, lean columns (Customer, Payment method, copyable ID), an owner-type dropdown filter, and no always-Off Billing signals column"
    - "The committed priv/static bundle reflects the new id-badge and filter-spacing CSS AND the new registered Clipboard JS hook so the assets md5 test passes for both css and js"
    - "Clicking an ID chip on an infinite-scroll-appended or filter-re-rendered row still copies the id (the Clipboard hook binds per-chip on mount, not once at first paint)"
    - "Applying or clearing a filter in organization mode keeps the org scope (org=<slug> survives every apply/change/clear with no double-? URL corruption)"
  artifacts:
    - path: "accrue_admin/lib/accrue_admin/data_table_nav.ex"
      provides: "Shared push_patch filter navigation helper for all list LiveViews"
      contains: "def patch_with_filters"
    - path: "accrue_admin/lib/accrue_admin/components/id_badge.ex"
      provides: "Click-to-copy monospace ID chip using the registered Clipboard LiveView hook"
      contains: "data-clipboard-text"
    - path: "accrue_admin/assets/js/hooks/clipboard.js"
      provides: "Registered Clipboard LiveView hook (survives DataTable row re-render/append)"
      contains: "export const Clipboard"
    - path: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      provides: "Shared list primitive with viewport sentinel, SPA filter form, grouped actions"
      contains: "phx-viewport-bottom"
    - path: "accrue_admin/lib/accrue_admin/queries/customers.ex"
      provides: "Owner-scoped distinct owner_type options for the customers filter"
      contains: "def distinct_owner_types"
    - path: "accrue_admin/priv/static/accrue_admin.css"
      provides: "Rebuilt committed CSS bundle containing ax-id-badge"
      contains: "ax-id-badge"
  key_links:
    - from: "accrue_admin/lib/accrue_admin/components/data_table.ex"
      to: "accrue_admin/lib/accrue_admin/data_table_nav.ex"
      via: "filter form phx-change/phx-submit emits data_table_filter to the parent LiveView which calls DataTableNav.patch_with_filters"
      pattern: "data_table_filter"
    - from: "accrue_admin/lib/accrue_admin/components/id_badge.ex"
      to: "accrue_admin/assets/js/hooks/clipboard.js"
      via: "phx-hook=Clipboard + unique DOM id + data-clipboard-text binds the registered Clipboard hook per chip (survives infinite-scroll/filter re-render)"
      pattern: "phx-hook=\"Clipboard\""
    - from: "accrue_admin/assets/js/hooks/clipboard.js"
      to: "accrue_admin/assets/js/app.js"
      via: "Clipboard hook is imported and registered in the LiveSocket hooks map"
      pattern: "Clipboard"
    - from: "accrue_admin/lib/accrue_admin/live/customers_live.ex"
      to: "accrue_admin/lib/accrue_admin/components/id_badge.ex"
      via: "customers ID column renders IdBadge.id_badge for the processor id"
      pattern: "IdBadge"
    - from: "accrue_admin/lib/accrue_admin/dev/component_registry.ex"
      to: "accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex"
      via: "id-badge registry entry rendered by an explicit do_render_specimen clause so the variant class appears in /dev/components"
      pattern: "id-badge"
---

<objective>
Part B of the approved ethereal-harbor plan: upgrade the shared admin DataTable UX
(infinite scroll + SPA/push_patch filters + filter-toolbar spacing) so all 9 admin list
pages benefit, add a new shared click-to-copy ID chip component, and redesign the
/admin/customers page from a raw-DB dump into an operator find-and-open surface.

This is accrue_admin-only. No core accrue API changes. No host/seed changes (that is
Part C). No StatusBadge changes. No CSP weakening: viewport/datalist are native, and the
click-to-copy uses a first-party Clipboard LiveView hook (in-repo JS, no inline/eval, no new
CSP directives) — the JS bundle changes intentionally and is rebuilt+committed in Task 4.
Do NOT update ROADMAP.md. Do NOT touch
examples/accrue_host/mix.lock or .planning/research/.cache/.

Purpose: the customers list (and every list page) becomes scannable and instant; technical
IDs become copy-on-click chips instead of prominent columns.
Output: shared DataTable + nav helper changes, new IdBadge component + copy icon + CSS +
component-lab entry, customers page redesign, rebuilt committed bundle, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The approved plan — Part B is the source of truth
@/Users/jon/.claude/plans/i-just-got-an-ethereal-harbor.md

# Shared list primitive (the form lives here, must drive a PARENT push_patch)
@accrue_admin/lib/accrue_admin/components/data_table.ex

# Customers page being redesigned
@accrue_admin/lib/accrue_admin/live/customers_live.ex
@accrue_admin/lib/accrue_admin/queries/customers.ex

# Pattern to mirror for distinct_owner_types
@accrue_admin/lib/accrue_admin/queries/webhooks.ex

# Clipboard control — register a real LiveView hook here (survives DataTable re-render)
@accrue_admin/assets/js/hooks/clipboard.js

# LiveSocket hooks registration — add Clipboard to the hooks map
@accrue_admin/assets/js/app.js

# Icon component (+ copy glyph), copy strings, component lab + drift test, CSS + tokens
@accrue_admin/lib/accrue_admin/components/icon.ex
@accrue_admin/lib/accrue_admin/components/inline_id.ex
@accrue_admin/lib/accrue_admin/dev/component_registry.ex
@accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared DataTable — infinite scroll + SPA push_patch filters + filter-toolbar spacing</name>
  <files>accrue_admin/lib/accrue_admin/data_table_nav.ex, accrue_admin/lib/accrue_admin/components/data_table.ex, accrue_admin/lib/accrue_admin/live/subscriptions_live.ex, accrue_admin/lib/accrue_admin/live/customers_live.ex, accrue_admin/lib/accrue_admin/live/invoices_live.ex, accrue_admin/lib/accrue_admin/live/charges_live.ex, accrue_admin/lib/accrue_admin/live/coupons_live.ex, accrue_admin/lib/accrue_admin/live/promotion_codes_live.ex, accrue_admin/lib/accrue_admin/live/connect_accounts_live.ex, accrue_admin/lib/accrue_admin/live/events_live.ex, accrue_admin/lib/accrue_admin/live/webhooks_live.ex, accrue_admin/assets/css/app.css</files>
  <action>
Four coordinated edits driving the shared list UX. Do NOT touch selection markup (selectable/selected_ids are owned by a prior task) and do NOT modify StatusBadge.

(1) New shared helper module AccrueAdmin.DataTableNav (lib/accrue_admin/data_table_nav.ex). Public patch_with_filters(socket, path, params): the path passed in is socket.assigns.table_path, which in organization mode ALREADY carries an existing query string (scoped_path/3 appends "?org=<slug>" — see customers_live.ex/subscriptions_live.ex scoped_path/3). The helper MUST MERGE the new filter params into that existing query rather than naively appending a second "?" (a naive path <> "?" <> URI.encode_query(params) would produce /billing/customers?org=acme?q=foo — a corrupting second "?" that drops the org param and breaks owner-scoping on every apply/change/clear). Implementation: %URI{} = uri = URI.parse(path); decode the existing query with URI.decode_query(uri.query || %{}{} -> use URI.decode_query(uri.query || "") which returns a map); MERGE the filter params over it (filter params win for keys they set); DROP blank/nil values from the MERGED map (reject nil and empty-string values so cleared fields disappear from the URL); re-encode the merged map with URI.encode_query/1; re-attach as a SINGLE query via %{uri | query: encoded} (set query to nil when the merged map is empty so the path has no trailing "?"), then URI.to_string/1; finally Phoenix.LiveView.push_patch(socket, to: that_string). The pre-existing org (and any other) query param MUST survive every apply/change/clear. Keep it dependency-light (alias Phoenix.LiveView).

(2) DataTable (components/data_table.ex) — three render changes; the load-more handler is unchanged:
  - Infinite-scroll sentinel: inside the existing footer block (the one guarded by Enum.empty?(@rows)), AFTER the Load more button (KEEP the button), add a sentinel that loads the next page on scroll but ONLY while a next cursor exists AND we are under the dom cap. Use the module-qualified JS helper: a div with :if={@next_cursor && length(@rows) < @dom_limit}, attribute phx-viewport-bottom={Phoenix.LiveView.JS.push("load-more", target: @myself)}, plus data-role="viewport-sentinel" and aria-hidden="true". Reuses the existing handle_event("load-more", ...) verbatim. The Load more button stays meaningful past the dom_limit cap and with JS disabled.
  - SPA Clear links: change the two clear-filters anchors from full-reload anchors to LiveView patches. The header clear link (currently an a tag with href={@path} class ax-button ax-button-ghost) and the in-empty-state clear link (currently an a tag with :if={any_filter_active?(@filter_params)} href={@path}) both become a patch link of the form .link patch={@path} keeping the same class and data-phase191-focus/data-role attributes. The link/1 component is available via use Phoenix.LiveComponent. IMPORTANT (org-scope preservation): @path here is table_path, which retains "?org=<slug>" in organization mode. Clear MUST patch @path AS-IS (the org-only query path) — it must NOT strip to a bare mount path, or Clear would drop owner-scoping. Clearing filters means "return to the unfiltered but still org-scoped view": patch the existing table_path verbatim. Likewise the empty-filter branch (when patch_with_filters' merged map has no filter params left) patches table_path as-is, retaining ?org=.
  - SPA filter form: change the filter form from action={@path} method="get" to a LiveView-driven form that reaches the PARENT (no phx-target). Set phx-change="data_table_filter" and phx-submit="data_table_filter" and remove the action and method attributes. Add phx-debounce="300" to the free-text inputs (the default filter_input/1 text clause and the datalist text-input clause) so typing does not patch on every keystroke; select/segmented/datalist selection still fires change immediately. Keep the hidden passthrough inputs and the existing field markup. The submit button stays (it is the no-JS/explicit Apply path and inherits the form phx-submit).
  - Filter-toolbar spacing markup: wrap the Apply submit button and the Clear link together in a single actions container div with class ax-data-table-filter-actions so they group at the end of the toolbar.

(3) Per-page handler (×9): each list LiveView gets a small handle_event("data_table_filter", params, socket) that strips LiveView form-noise keys ("_target", "_csrf_token") from params and delegates: {:noreply, AccrueAdmin.DataTableNav.patch_with_filters(socket, socket.assigns.table_path, params)}. (W2 seam note: this task spans 12 files but is one cohesive SPA-contract commit — the shared helper + DataTable render changes are the contract, and the 9 per-page one-line handlers are the mechanical fan-out. If this balloons during execution, split along the seam helper+DataTable / 9-handlers, but default to keeping it as one commit.) Add it to all 9 list LiveViews: subscriptions, customers, invoices, charges, coupons, promotion_codes, connect_accounts, events, webhooks. Place it next to each existing handle_params. The form field names (from field_param/1, e.g. q, owner_type, has_default_payment_method) already mirror the prior GET form, so each page decode_filter/1 consumes them unchanged. Patch socket.assigns.table_path (NOT current_path) so the canonical default-view redirect logic on subscriptions/invoices/charges/events is unaffected.

(4) CSS spacing (assets/css/app.css). Near the existing .ax-data-table-filters toolbar rules, give the toolbar a coherent gap and align-items: end so the submit button no longer sits flush against the field above it, and add .ax-data-table-filter-actions { display: flex; gap: var(--ax-space-sm); align-items: center; } using spacing tokens only. Use existing --ax-space-* tokens; no hard-coded pixels, no inline styles, no CSP-affecting constructs. Do NOT rebuild the bundle here — Task 4 owns the single rebuild after all CSS lands.
  </action>
  <verify>cd accrue_admin && mix compile --warnings-as-errors and: grep -c phx-viewport-bottom lib/accrue_admin/components/data_table.ex (>=1), grep -c patch_with_filters lib/accrue_admin/data_table_nav.ex (>=1), and the count of files under lib/accrue_admin/live/ containing handle_event("data_table_filter" equals 9. (The DataTableNav merge-into-existing-query unit test is authored in Task 5.)</verify>
  <done>DataTableNav.patch_with_filters/3 exists and MERGES filter params into the path's existing query (no double "?"; pre-existing ?org= survives), dropping blanks, and push_patches the result. DataTable renders a viewport-bottom sentinel gated on next_cursor && under dom_limit, keeps the Load more button, both Clear links are patch links to table_path as-is (org scope retained), and the filter form drives the parent via data_table_filter with debounced text inputs. All 9 list LiveViews delegate data_table_filter to the helper. CSS gives the toolbar coherent spacing and a grouped actions container. Compiles warnings-as-errors. No selection or StatusBadge changes.</done>
</task>

<task type="auto">
  <name>Task 2: Shared click-to-copy ID badge component + :copy icon + CSS + component-lab entry</name>
  <files>accrue_admin/lib/accrue_admin/components/id_badge.ex, accrue_admin/lib/accrue_admin/components/icon.ex, accrue_admin/assets/js/hooks/clipboard.js, accrue_admin/assets/js/app.js, accrue_admin/assets/css/app.css, accrue_admin/lib/accrue_admin/dev/component_registry.ex, accrue_admin/lib/accrue_admin/dev/component_kitchen_live.ex</files>
  <action>
(0) Register a REAL Clipboard LiveView hook (this is the fix for the dynamic-row copy bug). The current initClipboardControls() (hooks/clipboard.js) binds only [data-clipboard-text] elements present at FIRST paint via ready(); it does NOT re-bind rows appended on infinite-scroll load-more or re-rendered on filter push_patch. IdBadge chips live inside DataTable rows, so a one-shot binder leaves dynamically-rendered chips dead, and an unregistered phx-hook="Clipboard" on a connected socket logs an "unknown hook" error. Fix:
  - In hooks/clipboard.js, ADD and export a named LiveView hook object `export const Clipboard = { mounted() { ... } }`. Each chip element gets its own hook instance, so the binding survives re-render/append (LiveView re-mounts the hook on freshly-rendered elements). The mounted() attaches a click listener on this.el that reads this.el.dataset.clipboardText, guards on navigator.clipboard?.writeText, awaits navigator.clipboard.writeText(text), and sets this.el.dataset.copied = "true" to drive the CSS [data-copied="true"] feedback (mirror the existing one-shot behavior). Keep initClipboardControls() in the same file (do not delete it) so json_viewer's existing copy button keeps working.
  - In app.js, IMPORT Clipboard from "./hooks/clipboard" and ADD it to the LiveSocket hooks map: hooks: { CommandPalette, ConnectionState, FocusTrap, SidebarCollapse, Clipboard }. Leave the existing initClipboardControls() call in ready() untouched (json_viewer still relies on it). This DOES change the JS bundle — that is intended (see Task 4).
  - json_viewer.ex consistency (low-risk, optional but preferred): if migrating json_viewer's copy button to phx-hook="Clipboard" (with a DOM id + data-clipboard-text) is trivial, do it for consistency and then the one-shot initClipboardControls() can be considered legacy; if it is NOT trivial or would touch unrelated markup, LEAVE json_viewer untouched and keep initClipboardControls() as-is. The NEW IdBadge MUST use the registered Clipboard hook regardless.

(1) New component AccrueAdmin.Components.IdBadge (lib/accrue_admin/components/id_badge.ex), use Phoenix.Component. Function id_badge/1 with attrs: id (optional DOM id; phx-hook REQUIRES a unique DOM id — default to a stable derived id such as "ax-id-badge-" <> @id_value when not supplied, or require the caller to pass id; the list usage in Task 3 MUST pass a unique id per row, e.g. derived from the row processor_id), id_value (required string), label (optional accessible label, default nil), class (optional, default nil), and a :global rest. It renders a single clickable button (type="button") with a unique DOM id and carrying the two-class pattern class={["ax-id-badge", "ax-id-badge-default", @class]} so the registry render-coverage guardrail (base + variant) is satisfied. The button uses the REGISTERED Clipboard LiveView hook: set phx-hook="Clipboard" and data-clipboard-text={@id_value} (the hook's mounted() writes data-clipboard-text to navigator.clipboard and sets data-copied="true" on success, and survives DataTable row re-render/append). Inside: a truncated monospace id span (mono via the --ax-font-mono / .ax-type-code-xs tokens — apply an .ax-id-badge-text class, not inline font) showing @id_value with title={@id_value} for full-text access, and an Icon.icon name={:copy} size="sm" copy affordance. Provide an aria-label (use @label or a default like "Copy " <> @id_value). This component is distinct from the display-only InlineId (ax-inline-id) — IdBadge is interactive/click-to-copy.

(2) Icon (components/icon.ex): add :copy to the @names list and a paths(:copy) clause with the Heroicons v2 outline document-duplicate (clipboard/copy) path data. Keep the existing literal-SVG style. Update the @names list so the value enumeration includes :copy.

(3) CSS (assets/css/app.css): add .ax-id-badge and .ax-id-badge-default (and a .ax-id-badge-text truncation helper) using tokens only — monospace via --ax-font-mono, small code type via --ax-type-code-xs, truncation (overflow hidden, text-overflow ellipsis, white-space nowrap) under a max-width, a quiet hover affordance, and a copied-feedback rule keyed on the attribute the clipboard hook sets (a [data-copied="true"] selector, e.g. surfacing a subtle confirmation). Borrow the theme-picker/segmented chip visual language for the resting/hover treatment. No inline styles, no new tokens, no CSP-affecting constructs.

(4) Component lab: register an id-badge family so the lab and its drift test cover it.
  - component_registry.ex: add ONE entry with family "id-badge", variant "default", ax_class "ax-id-badge ax-id-badge-default" (two-class: base + variant), tokens listing the design tokens the CSS consumes (e.g. --ax-font-mono, --ax-type-code-xs, plus the hover/border tokens used), applicable_states ["default"] (it is a click-to-copy control; treat copy success as a transient JS state, mark interactive states n/a with reasons mirroring the existing inline-id/icon entries), and at least one specimen with a real id_value (e.g. cus_phase191_host_1). The default specimen MUST render the variant class ax-id-badge-default (the drift test splits ax_class and asserts the variant token appears in the page HTML).
  - component_kitchen_live.ex: add an explicit do_render_specimen("id-badge", _state, _specimen, _theme) clause that renders IdBadge.id_badge id_value="cus_phase191_host_1" (alias IdBadge in the kitchen module alias block). This is REQUIRED — the catch-all clause only emits placeholder text and would NOT contain ax-id-badge-default, failing the render-coverage drift test. Add a family_label("id-badge") -> "ID badge" clause.
  </action>
  <verify>cd accrue_admin && mix compile --warnings-as-errors and: grep -c data-clipboard-text lib/accrue_admin/components/id_badge.ex (>=1), grep -c 'phx-hook="Clipboard"' lib/accrue_admin/components/id_badge.ex (>=1), grep -c 'export const Clipboard' assets/js/hooks/clipboard.js (>=1), grep -c 'Clipboard' assets/js/app.js (>=2, i.e. import + hooks map), grep -c 'paths(:copy)' lib/accrue_admin/components/icon.ex (>=1), grep -c 'family: "id-badge"' lib/accrue_admin/dev/component_registry.ex (>=1), grep -c 'do_render_specimen("id-badge"' lib/accrue_admin/dev/component_kitchen_live.ex (>=1). Then mix test test/accrue_admin/dev/component_registry_test.exs passes (render-coverage drift).</verify>
  <done>A registered Clipboard LiveView hook exists in hooks/clipboard.js (exported) and is wired into the app.js LiveSocket hooks map, so copy works on dynamically-rendered/appended DataTable rows (no "unknown hook" error). IdBadge.id_badge/1 renders a monospace truncated id with a unique DOM id, phx-hook="Clipboard" + data-clipboard-text and an :copy icon. The :copy icon is in @names with path data. .ax-id-badge* CSS exists (tokens only, truncate + hover + data-copied feedback). The id-badge registry entry uses a base+variant ax_class and its default specimen renders ax-id-badge-default; an explicit kitchen render clause makes the variant class appear at /dev/components and the drift test passes. initClipboardControls() remains for json_viewer (not broken).</done>
</task>

<task type="auto">
  <name>Task 3: Customers page redesign — microcopy, lean columns, owner-type filter, KPIs, distinct_owner_types</name>
  <files>accrue_admin/lib/accrue_admin/queries/customers.ex, accrue_admin/lib/accrue_admin/copy.ex, accrue_admin/lib/accrue_admin/live/customers_live.ex</files>
  <action>
(1) queries/customers.ex: add distinct_owner_types(owner_scope) mirroring Queries.Webhooks.distinct_types/1 but owner-scoped via the existing scope_query/2 (push the scope through SQL since customers are SQL-scopable, unlike webhooks): select distinct customer.owner_type from the owner-scoped Customer query, reject nils, Enum.uniq, Enum.sort. Returns a sorted list of owner_type strings visible in the active scope. Do NOT change the existing list/2 projection.

(2) copy.ex: move customers microcopy into Copy functions (brand voice: calm, exact, no jargon). Replace the page heading source from "Searchable customer projections" to "Customers" and the description to the plain string "Everyone you bill through Accrue for this organization. Search by name, email, or customer ID." Add/repoint customers_* Copy functions for the heading and description (and adjust customers_index_table_caption away from the jargon phrase to a plain caption like "Customers"). Keep the existing customers_index_empty_* functions.

(3) customers_live.ex:
  - Heading + description: render Copy-sourced heading "Customers" and the plain description (replace the inline "Searchable customer projections" h1 and the projection-jargon paragraph).
  - KPIs: drop the "Owner types / Distinct host billable types" KpiCard. Keep "Customers" (customer_count) and "With payment method" (with_default_payment_method_count). Only add a third KPI if a cheap real metric already exists in customer_summary — do NOT invent a metric or add an N+1 query; two clean KPIs is acceptable. Remove the now-unused owner_type_count from customer_summary/1 if no longer referenced (keep the function compiling cleanly).
  - Columns (lean find-and-open set): Customer (name + email stacked via the existing customer_link render, augmented to show email under the name), Payment method (relabel "Default PM" -> "Payment method"; keep the default_payment_method_label render but soften to "On file" / "Missing" rather than leaking the raw id in the prominent column), and a click-to-copy ID chip rendering IdBadge.id_badge for the customer processor_id (or id when processor_id is nil). Pass a UNIQUE DOM id per row (phx-hook requires it) — derive it from the row id, e.g. id={"ax-id-badge-" <> to_string(customer.id)} — so each chip mounts its own Clipboard hook instance and copy works on infinite-scroll-appended rows. DROP the prominent :owner_id and :processor_id columns and the "Billing signals" column from the LIST (signals reads tax fields the projection never selects, so it always shows Off — it belongs on the detail page). Update card_fields similarly to the lean set; remove the billing_signals_cell usage from the list/cards. If owner_type stays useful after seeds, render it as a small chip column, else drop it (default: drop from the prominent list to keep it lean). Remove the now-dead billing_signals_cell/1 if nothing else references it, to keep warnings-as-errors clean.
  - Filter: change the owner_type filter field from free-text to a derived dropdown. Compute the options from Customers.distinct_owner_types(@current_owner_scope) (call in mount or handle_params and assign, do NOT call in render on every patch if it would re-query excessively — assign once in mount is fine for the active scope) and pass them as a :select (or :datalist) filter field with type and options. Keep the q (Search) and has_default_payment_method fields. Alias AccrueAdmin.Components.IdBadge.
  </action>
  <verify>cd accrue_admin && mix compile --warnings-as-errors and: grep -c distinct_owner_types lib/accrue_admin/queries/customers.ex (>=1), grep -c IdBadge lib/accrue_admin/live/customers_live.ex (>=1), and confirm the jargon phrase is gone from the live module via: the customers_live.ex file no longer contains the literal projections heading. Then mix test test/accrue_admin/live/customers_live_test.exs (after Task 5 updates it).</verify>
  <done>Customers page shows heading "Customers", a plain Copy-sourced description, two (or two-plus-cheap) KPIs without the Owner-types KPI, a lean column set (Customer name+email, Payment method, copyable ID chip) with no owner_id/processor_id/Billing-signals columns, and an owner_type dropdown sourced from distinct_owner_types(owner_scope). Compiles warnings-as-errors with no dead-function warnings.</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild and commit the static asset bundle</name>
  <files>accrue_admin/priv/static/accrue_admin.css, accrue_admin/priv/static/accrue_admin.js</files>
  <action>
After Tasks 1-3 land their CSS/JS/icon changes, rebuild the committed bundle that accrue_admin actually serves: cd accrue_admin && mix accrue_admin.assets.build. This regenerates priv/static/accrue_admin.css (now containing .ax-id-badge and the filter-spacing rules) AND priv/static/accrue_admin.js (now containing the registered Clipboard hook wired into the LiveSocket hooks map — Task 2 step 0 changed hooks/clipboard.js and app.js, so the JS bundle content DOES change this time). Commit BOTH priv/static/accrue_admin.css and priv/static/accrue_admin.js (assets_test.exs asserts the md5 of each committed bundle matches a fresh build; the new JS is covered by the JS md5). Do NOT hand-edit the generated files. Do NOT touch examples/accrue_host or any mix.lock.
  </action>
  <verify>cd accrue_admin && mix accrue_admin.assets.build then grep -c ax-id-badge priv/static/accrue_admin.css (>=1), grep -c Clipboard priv/static/accrue_admin.js (>=1), then mix test test/accrue_admin/assets_test.exs passes (committed-bundle md5 matches a fresh build for BOTH css and js).</verify>
  <done>priv/static/accrue_admin.css contains .ax-id-badge and the new filter-spacing rules; priv/static/accrue_admin.js contains the registered Clipboard hook; both committed bundle files match a fresh asset build; assets_test.exs passes. No host or mix.lock changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Tests — DataTable SPA/infinite-scroll contract, IdBadge, customers redesign, per-page filter smoke, lab guardrail</name>
  <files>accrue_admin/test/accrue_admin/data_table_nav_test.exs, accrue_admin/test/accrue_admin/components/data_table_test.exs, accrue_admin/test/accrue_admin/components/id_badge_test.exs, accrue_admin/test/accrue_admin/live/customers_live_test.exs, accrue_admin/test/accrue_admin/live/subscriptions_live_test.exs, accrue_admin/test/accrue_admin/dev/component_registry_test.exs</files>
  <behavior>
    - DataTableNav.patch_with_filters MERGES filter params into a path's EXISTING query: given a path already carrying "?org=acme" and params %{"q" => "foo"}, the patched URL contains BOTH org=acme and q=foo with exactly ONE "?" (no /customers?org=acme?q=foo double-? corruption); blank/nil param values are dropped; passing only-blank params against an org-scoped path yields the org-only path (org param preserved, no trailing "?"); a path with no existing query plus real params yields a single "?query".
    - DataTable renders a phx-viewport-bottom sentinel (data-role="viewport-sentinel") when next_cursor is present AND row count < dom_limit; the sentinel is absent when next_cursor is nil; the Load more button is still present when next_cursor is set.
    - DataTable Clear is a patch link (no full-page GET form anchor); the filter form carries phx-change/phx-submit="data_table_filter" and NO phx-target (parent-targeted), text inputs carry phx-debounce.
    - A parent list page receiving the data_table_filter event push_patches to a URL carrying the submitted filter params (customers + at least one more page, e.g. subscriptions); in organization mode the patched URL still carries org=<slug> alongside the filter params (org scope survives apply); the Clear action push_patches back to the org-scoped table_path (org preserved, filters gone).
    - IdBadge renders a monospace id, a title with the full id, phx-hook="Clipboard", and data-clipboard-text equal to the id_value; class carries ax-id-badge and ax-id-badge-default.
    - customers_live: heading is "Customers" (not the old projections jargon), the plain description renders, the Owner-types KPI is gone, the Payment-method column header reads "Payment method", the Billing-signals column is absent, the owner_type filter renders as a select/datalist with derived options, and an IdBadge id chip renders for a seeded customer.
    - component_registry_test render-coverage already asserts every variant (including ax-id-badge-default) appears at /dev/components — extend or rely on the existing loop.
  </behavior>
  <action>
Update the existing tests to the NEW SPA/infinite-scroll contract and add the new coverage. IMPORTANT: existing data_table_test.exs and the list live tests assert the OLD method=get filter form, anchor-based Clear, and Load-more-only pagination — rewrite those assertions to the new contract rather than leaving stale expectations.
  - data_table_nav_test.exs (new): unit-test DataTableNav.patch_with_filters merge behavior. Build a minimal %Phoenix.LiveView.Socket{} (or use Phoenix.LiveViewTest helpers) and assert the resulting push_patch :to path: (a) merges %{"q"=>"foo"} into "/billing/customers?org=acme" → exactly one "?", contains org=acme AND q=foo; (b) drops blank/nil values; (c) only-blank params against "/billing/customers?org=acme" → "/billing/customers?org=acme" (no double "?", org retained); (d) real params against a query-less path → single "?query". This is the regression guard for the double-? org-corruption blocker.
  - data_table_test.exs: replace GET-form/anchor-Clear assertions; assert the viewport sentinel renders only when next_cursor && under dom_limit, that Load more persists, that Clear is a patch link, and that the filter form is parent-targeted (phx-change/phx-submit data_table_filter, no phx-target, debounce on text inputs).
  - id_badge_test.exs (new): render_component the IdBadge and assert mono id, title, phx-hook Clipboard, data-clipboard-text, and the two ax-id-badge classes.
  - customers_live_test.exs: update to new copy/headings/lean columns/payment-method label/owner-type dropdown/dropped-signals-column; add an IdBadge presence assertion. Update any assertion referencing the old jargon strings, the owner_id/processor_id columns, or the Owner-types KPI.
  - subscriptions_live_test.exs (or another existing list-page test): add a smoke test that triggering the data_table_filter event push_patches the URL with the filter params (proves the shared parent contract for a second page beyond customers).
  - component_registry_test.exs: confirm the existing render-coverage loop now covers id-badge (no change needed if the loop iterates ComponentRegistry.entries()); add an explicit assertion that ax-id-badge-default appears at /dev/components if a targeted guardrail is preferred.
  Watch for other list/live tests that assert old filter/pagination behavior and update them too (grep the test tree for method=get filter-form and Load-more-only assertions on list pages).
  </action>
  <verify>cd accrue_admin && mix test test/accrue_admin/components/data_table_test.exs test/accrue_admin/components/id_badge_test.exs test/accrue_admin/live/customers_live_test.exs test/accrue_admin/live/subscriptions_live_test.exs test/accrue_admin/dev/component_registry_test.exs — all pass. Then a full cd accrue_admin && mix test (dodge the flaky PdfTest with --seed 0 if needed) is green.</verify>
  <done>All five named test files pass against the new contract, the per-page filter smoke proves the shared push_patch contract on customers + a second page, the IdBadge and id-badge lab guardrail are covered, and the broader accrue_admin suite is green (no stale OLD-contract assertions remain).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> LiveView | filter params arrive from the client via phx-change/phx-submit and the patched URL query string |
| client clipboard | data-clipboard-text is read by the registered Clipboard LiveView hook (per-element mounted()) and written to navigator.clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-io6-01 | Tampering | filter params via data_table_filter / URL query | mitigate | params flow through each page existing decode_filter/1 + owner-scoped queries (scope_query/2 / scope_rows/2); no new trust surface — handle_params already re-queries with the same validation as the prior GET form |
| T-io6-02 | Information disclosure | owner-scoped customer rows | accept | distinct_owner_types and the list both run under the active OwnerScope; scoping is unchanged from the existing list path |
| T-io6-03 | Information disclosure | clipboard copy of processor/customer id | accept | IDs shown are already rendered on the page today (just relocated into a chip); copying a visible id introduces no new exposure |
| T-io6-04 | Elevation/CSP | viewport/clipboard/datalist + registered Clipboard hook | mitigate | viewport/datalist are native; the Clipboard hook is first-party in-repo JS (no inline styles, no eval, no remote code) registered in the LiveSocket hooks map. The JS bundle DOES change (new hook export + app.js wiring) — that change is authored in-repo and the assets_test.exs JS md5 guards the committed bundle against drift from a fresh build. CSP unchanged (no new inline/eval). W3 reconciled: the JS md5 covers the intentional JS change. |
| T-io6-SC | Tampering | npm/mix installs | accept | no new dependencies added (first-party Clipboard hook, Heroicons literal SVG, existing tokens — all in-repo, no package installs) |
</threat_model>

<verification>
- cd accrue_admin && mix compile --warnings-as-errors is clean (no dead-function or unused-alias warnings after the customers refactor).
- cd accrue_admin && mix accrue_admin.assets.build then mix test test/accrue_admin/assets_test.exs passes (committed bundle md5).
- Full cd accrue_admin && mix test is green (dodge flaky PdfTest with --seed 0 if it appears). The DataTable, IdBadge, customers_live, a second list-page filter smoke, and component-registry drift tests pass against the new SPA/infinite-scroll contract.
- grep priv/static/accrue_admin.css contains ax-id-badge.
- No changes under examples/accrue_host, no mix.lock changes, no ROADMAP.md change, no .planning/research/.cache/ change, StatusBadge untouched.
</verification>

<success_criteria>
- All 9 admin list pages: scrolling auto-loads the next page (Load more still works / no-JS), filter apply + Clear are instant via push_patch (no full reload/flash), and the Apply/Clear actions are grouped and no longer flush against the field above.
- A reusable click-to-copy IdBadge component exists, using a REGISTERED Clipboard LiveView hook that binds per-chip and survives infinite-scroll/filter re-render (the JS bundle changes intentionally and is committed), ships a :copy icon and .ax-id-badge CSS, and is registered + render-covered in the component lab.
- /admin/customers reads as a lean operator surface: heading Customers, plain description, two KPIs (no Owner-types), Customer (name+email) / Payment method / copyable ID columns, no Billing-signals column, owner-type dropdown from distinct_owner_types.
- Committed bundle rebuilt and committed; assets md5 test green; full accrue_admin suite green.
</success_criteria>

<output>
Create `.planning/quick/260621-io6-shared-datatable-infinite-scroll-spa-fil/260621-io6-SUMMARY.md` when done
</output>

---
quick_id: 260718-g6b
title: Click-to-copy demo credentials on the host login page
status: complete
created: 2026-07-18
---

# Quick Task 260718-g6b: Click-to-copy demo credentials on the host login page

## Goal
Make the seeded demo credentials on `/users/log-in` (examples/accrue_host) easy to
grab: each persona's email and the shared password become click-to-copy chips
(password repeated per card for proximity), with a clean daisyUI toast on copy and
**zero layout reflow**. Deliverable is copy-to-clipboard (not form-autofill).

Full approved design: `~/.claude/plans/i-m-in-users-jon-projects-accrue-current-refactored-willow.md`.

## Tasks
1. **Port + enhance the Clipboard hook** — NEW `examples/accrue_host/assets/js/hooks/clipboard.js`
   - action: `navigator.clipboard.writeText` (guarded); set transient `data-copied` (~1200ms) for icon swap; `showCopyToast()` clones the hidden `#copy-toast-template` into fixed `#copy-toast-root`, auto-dismiss ~1600ms with fade/slide.
   - verify: served `/assets/js/app.js` contains `showCopyToast`.
   - done: clicking a chip copies + toasts.
2. **Register the hook** — `examples/accrue_host/assets/js/app.js`
   - action: `import {Clipboard}`; add to the `LiveSocket` hooks map.
3. **Rework persona cards + toast host** — `examples/accrue_host/lib/accrue_host_web/live/user_live/login.ex`
   - action: `Enum.with_index` loop for unique ids; two `copy-chip` `<button>`s (Email + Password) per card with `phx-hook="Clipboard"`, `data-clipboard-text`, `data-copy-label`, `aria-label`, and a copy→check icon swap; muted "Tap any field to copy" hint replacing the single top password badge; add once `#copy-toast-root` (daisyUI `toast toast-top toast-end`) + hidden `#copy-toast-template` (daisyUI `alert alert-success`).
   - verify: rendered page has 8 `phx-hook="Clipboard"` chips + toast root + template.
4. **Chip/toast CSS** — `examples/accrue_host/assets/css/app.css`
   - action: icon-swap keyed on `.copy-chip[data-copied="true"]`; copied outline via **inset box-shadow** (no reflow); `.copy-toast` leave transition.

## Constraints
- Touch only the four files above; leave the ~10 pre-existing dirty/untracked files untouched.
- `AccrueHost.DemoBrand` (`personas/0`, `demo_password/0`) unchanged — single source of truth.
- Host builds its own assets at dev time (Tailwind v4 + daisyUI); no committed-bundle rebuild.

## Verification
Playwright with clipboard permissions granted, driving the live page: email & password
copy to clipboard, toast shows "Copied email address" and auto-dismisses, `data-copied`
+ check-icon flip, Enter-key copies (keyboard a11y), and **no reflow** (chip bounding box +
page height unchanged on copy).

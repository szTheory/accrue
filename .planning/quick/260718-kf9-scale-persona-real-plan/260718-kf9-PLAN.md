---
quick_id: 260718-kf9
title: Align the "Scale Customer" persona with the real Scale plan (fix orphan price_premium)
status: complete
created: 2026-07-18
---

# Quick Task 260718-kf9: Scale Customer → real Scale plan

## Goal
On `/app/billing`, the enterprise "Scale Customer" persona showed "Plan: price_premium" (raw id) and no
plan card highlighted. Root cause: the seed subscribed it to `price_premium`, absent from the catalog
(`Plans` = price_basic/price_pro/price_metered → Launch/Studio/Scale). `price_premium` was also wired to
the `:advanced_reports` entitlement, so the fix moves both to the real Scale plan the persona is named after.

## Changes
1. `priv/repo/seeds/hero_accounts.exs` — enterprise subscribes to `"price_metered"` (Scale) instead of
   `"price_premium"`; dropped the stale placeholder comments.
2. `config/config.exs` — the `:advanced_reports` entitlement plan `premium: [price_ids: ["price_premium"]]`
   → `scale: [price_ids: ["price_metered"]]` (the `:premium` key was referenced nowhere else).
3. `test/accrue_host_web/live/entitlements_guard_test.exs` — the two entitled subscriptions `price_premium`
   → `price_metered` (non-entitled `price_basic` case unchanged).
4. `make reset` to reseed the running demo onto Scale.

## Verification
- `rg price_premium examples/accrue_host` → no hits.
- `mix test .../entitlements_guard_test.exs` → 3/0.
- Playwright as enterprise@ (Scale Customer): current subscription reads **"Plan Scale"** (no raw id),
  the **Scale** card shows **"Current plan"**, Launch/Studio show "Available".
- Entitlement preserved: `/app/reports/advanced` renders "Cohort performance / …advanced cohort reporting
  through its active plan." (enterprise on Scale is entitled; not redirected).

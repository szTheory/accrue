---
phase: quick-260719-ix8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - accrue/README.md
  - accrue/guides/architecture.md
  - accrue/guides/code-walkthrough.md
  - accrue/mix.exs
  - accrue/priv/ex_doc/accrue-mark.svg
  - accrue/priv/ex_doc/favicon.svg
  - accrue/test/accrue/docs/architecture_code_walkthrough_test.exs
autonomous: true
requirements:
  - QUICK-260719-ix8

must_haves:
  truths:
    - "A senior Elixir/Phoenix engineer can learn Accrue outside-in from a direct-subscribe command through processor authority, local durable projections, webhook convergence, entitlements, and sibling-package ownership."
    - "The code walkthrough deepens the same route with exactly 18 parseable, source-faithful Elixir excerpts and an early warning that internals shown for understanding are not promised public API."
    - "The architecture guide contains exactly four accessible Mermaid diagrams: whole system, direct subscribe, webhook convergence, and data/package authority."
    - "HexDocs renders Mermaid 11.16.0 only in HTML, with strict security, `exdoc:loaded` rerendering, and source-code fallback preserved until rendering succeeds; EPUB receives no hook."
    - "Mermaid follows the active ExDoc light/dark theme, rerenders after a live theme toggle, and uses a subtle theme-native surface instead of a forced light canvas."
    - "Generated HexDocs and the Hex package include an adaptive Accrue logo and favicon that remain legible in light and dark browser chrome."
    - "Both guides ship in the core Hex package, are discoverable from the root and core READMEs, link to one another, and use module references rather than repository paths or source-line URLs."
    - "The guides candidly distinguish processor truth from local projections and record the two verified queued-webhook caveats instead of claiming stronger behavior than the current implementation provides."
    - "All edits and commits are scoped to this task; the pre-existing Admin, workflow, lockfile, example-host, prompt, and planning changes remain untouched."
  artifacts:
    - path: "accrue/guides/architecture.md"
      provides: "Outside-in architectural narrative with four accessible Mermaid diagrams and five short Elixir examples"
      contains: "Journey 1: The application asks billing to change"
    - path: "accrue/guides/code-walkthrough.md"
      provides: "Inside-out direct-subscribe and webhook walkthrough with exactly 18 parseable Elixir excerpts"
      contains: "internal modules"
    - path: "accrue/mix.exs"
      provides: "Pinned strict HTML-only Mermaid body hook with navigation/theme rerender, branded ExDoc assets, and readable failure fallback"
      contains: "mermaid@11.16.0"
    - path: "accrue/priv/ex_doc/favicon.svg"
      provides: "Adaptive Accrue browser-tab mark included by the package's priv whitelist"
      contains: "prefers-color-scheme: dark"
    - path: "accrue/test/accrue/docs/architecture_code_walkthrough_test.exs"
      provides: "Behavior-oriented contracts for guide discovery, structure, parsing, source drift, cross-links, and Mermaid safety"
      contains: "Code.string_to_quoted"
  key_links:
    - from: "README.md"
      to: "accrue/guides/architecture.md"
      via: "Start here architecture link"
      pattern: "accrue/guides/architecture\\.md"
    - from: "accrue/README.md"
      to: "accrue/guides/code-walkthrough.md"
      via: "Core-package Start here code-walkthrough link"
      pattern: "guides/code-walkthrough\\.md"
    - from: "accrue/guides/architecture.md"
      to: "accrue/guides/code-walkthrough.md"
      via: "Mutual guide navigation using relative HexDocs-extra links"
      pattern: "code-walkthrough\\.md"
    - from: "accrue/mix.exs"
      to: "accrue/guides/architecture.md"
      via: "ExDoc extras wildcard plus before_closing_body_tag renders fenced mermaid blocks"
      pattern: "before_closing_body_tag"
    - from: "accrue/test/accrue/docs/architecture_code_walkthrough_test.exs"
      to: "accrue/lib/accrue/billing/subscription_actions.ex"
      via: "Stable direct-subscribe idempotency anchor must remain in source and walkthrough"
      pattern: "Idempotency\\.key\\(:create_subscription"
    - from: "accrue/test/accrue/docs/architecture_code_walkthrough_test.exs"
      to: "accrue/lib/accrue/webhook/default_handler.ex"
      via: "Stable canonical-refetch anchor must remain in source and walkthrough"
      pattern: "fetch\\(:subscription"
---

<objective>
Author the core package's durable architecture learning path: an outside-in architecture guide and
an inside-out code walkthrough for senior Elixir/Phoenix engineers. The approved plan is LOCKED.
Use direct subscription creation as the primary outbound route, and teach the inbound webhook route
that converges processor state into local projections and entitlements.

Purpose: Give maintainers and adopters one coherent conceptual spine for understanding ownership,
authority, durability, retries, and safe change without turning the guides into a directory tour or
feature catalog.

Output: two HexDocs guides, secure Mermaid rendering, README discovery links, focused documentation
contracts, generated-doc/package proof, and browser evidence. No runtime/public API behavior change,
version bump, release, publish, or unrelated formatting.
</objective>

<execution_context>
@/Users/jon/.agents/gsd-core/workflows/execute-plan.md
@/Users/jon/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@prompts/ARCHITECTURE-CODE-WALKTHROUGH-DNA.md

The user-approved proposed plan is authoritative. Preserve every pre-existing dirty file. Before
editing, record `git status --short`; never use broad staging, `git add -A`, or a formatter over
sibling packages. The prompt is already ignored by `.git/info/exclude`; do not change ignore rules.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the paired guides and wire the reading path</name>
  <files>
    accrue/guides/architecture.md
    accrue/guides/code-walkthrough.md
    README.md
    accrue/README.md
  </files>
  <action>
Create `architecture.md` as the outside-in guide with an opening promise followed by these exact
ordered H2 sections: `Accrue in one picture`, `Vocabulary for the trip`,
`Journey 1: The application asks billing to change`,
`Journey 2: The processor reports what happened`, `The data model carries the architecture`,
`Cross-cutting mechanics`, `How the sibling packages fit`, `Module atlas`,
`Code-reading routes`, `Changing Accrue safely`, and `Where to go next`.

Teach the direct-subscribe route from the adopter-owned facade through `Accrue.Billing`, customer
normalization, capability gating, operation ID/idempotency, the configured processor adapter, and the
transactional local subscription/item/event projection. Follow it with the inbound route from raw
signature verification and durable deduplicated `WebhookEvent` evidence through Oban dispatch,
default reduction, canonical processor refetch, transactional projections/audit, and fail-closed local
entitlements. Explain host ownership of Repo, Oban, routes, auth, facade, and product policy; core
ownership of reusable billing semantics; Admin as an operator/control-plane consumer; Portal as a
customer integration surface; and Stripe/Braintree authority for processor-side money movement.
In `Cross-cutting mechanics`, tie Ecto, Swoosh, ChromicPDF, telemetry, supervision, and optional
integrations briefly to those two journeys: state what each dependency provides, what Accrue owns
around it, and what lifecycle/runtime responsibility remains with the host. Do not turn this section
into a dependency catalog or detach it from the values, work, and evidence moving through the routes.

Add exactly four Mermaid fences, each with `accTitle` and `accDescr`: (1) host/core/processor whole
system with Admin and Portal around core, (2) direct-subscribe sequence, (3) webhook-ingest and
convergence sequence, and (4) data-authority/package-ownership relationship. Keep the happy-path
production architecture primary and make diagrams readable without color. Add exactly five short,
parseable Elixir examples for the host facade call, processor boundary, transactional projection,
webhook/watermark value shape, and entitlement gate. Use tables only for exact module/ownership maps.

Create `code-walkthrough.md` as the inside-out companion. State immediately that it starts where the
architecture guide stops and that internal modules/private functions are explanatory rather than
promised public API. Include exactly 18 fenced Elixir excerpts, normally 8-35 lines, copied from current
source, parseable with `Code.string_to_quoted/1`, and using `# ...` at every deliberate cut. Keep this
locked order: (1) host `AccrueHost.Billing.subscribe/3`; (2) `Accrue.Application.start/2`;
(3) `Accrue.Config.validate_at_boot!/0`; (4) public `Accrue.Billing.subscribe/3`;
(5) billable-to-customer normalization; (6) capability gate, operation ID, idempotency key, processor
call, and transaction; (7) `Accrue.Processor` behaviour/runtime seam; (8) Stripe direct-create adapter;
(9) `SubscriptionProjection.decompose/1`; (10) subscription lifecycle predicate/watermarks;
(11) Webhook Plug verification; (12) ingest transaction/deduplication; (13) lean persisted event;
(14) worker ordering/retries/user-handler isolation; (15) canonical refetch/stale reducer;
(16) local entitlement resolution; (17) duplicate-ingest test; (18) out-of-order reducer test.
Do not excerpt Fake or Braintree implementations; explain Fake as an executable boundary and mention
Braintree only where its real boundary matters. End with module sequences and the question answered
for subscribe, webhook, entitlements, dunning, invoice/PDF, audit, Admin, and Portal routes.

Both guides must use documented module/function references and relative guide links, never repository
paths, GitHub blob/tree URLs, or source-line anchors. Cross-link them near the beginning and end and
link outward to existing webhook, lifecycle, entitlement, testing, production, metering, dunning,
PDF/email, Admin, and Portal material instead of duplicating task instructions.

Include a candid current-state note: queued events construct normalized `created_at` from persisted
`received_at`, while the raw/Fake path can carry provider-created timestamps, so canonical refetch is
the queued path's primary convergence guarantee; and Braintree receipt can persist, but
`Accrue.Webhook.Event` currently omits `"braintree"` from its bounded processor conversion map, so
queued Braintree dispatch fails before its handler. State that direct Braintree subscription creation
is separate and unaffected. Do not fix either runtime issue here.

Add concise Architecture and Code Walkthrough entries at the start of the existing `Start here`
reading maps in the root and core READMEs. Do not edit Admin/Portal READMEs and do not add a changelog
entry: `accrue/CHANGELOG.md` has no Unreleased convention.
  </action>
  <verify>
From the repository root, use `rg`/a small read-only script to assert both guides exist, the root and
core READMEs link to both, architecture H2s occur in the locked order, architecture has exactly four
`mermaid` fences/four `accTitle`/four `accDescr`, walkthrough has exactly 18 `elixir` fences, and the
guides link to one another. Cold-read both Markdown files top-to-bottom for a single consistent story,
defined terminology, unambiguous processor/local authority, and no directory-tour prose.
  </verify>
  <done>
The two guides teach the same direct-subscribe/webhook system from opposite directions, expose the
verified caveats, provide the locked diagrams/excerpts and next-reading routes, and are discoverable
from both intended README entry points without touching sibling documentation or the changelog.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add safe Mermaid rendering and maintainability contracts</name>
  <files>
    accrue/mix.exs
    accrue/priv/ex_doc/accrue-mark.svg
    accrue/priv/ex_doc/favicon.svg
    accrue/test/accrue/docs/architecture_code_walkthrough_test.exs
  </files>
  <action>
Extend the existing ExDoc configuration with `before_closing_body_tag: &before_closing_body_tag/1`.
Keep it private and docs-only. Return `""` for `:epub` and other non-HTML formats. For `:html`, emit a
module script that imports exactly
`https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.esm.min.mjs`, initializes Mermaid with
`startOnLoad: false` and `securityLevel: "strict"`, and listens for every `exdoc:loaded` event.

On each event, discover unrendered `pre > code.mermaid` blocks. Select Mermaid's `dark` or `default`
theme from ExDoc's current `body.dark` state, observe live theme-class changes, and rerender existing
diagrams from their retained source without flashing a forced light canvas. Preserve each original `<pre>` until
`mermaid.render(unique_id, code.textContent)` resolves; only then replace it with a responsive diagram
wrapper containing the returned SVG. Use a per-node in-progress marker plus unique IDs so initial and
Swup/ExDoc navigation cannot double-render. Clear the marker and leave the readable source block intact
on import/render failure, logging one non-fatal warning. Call returned bind functions if present. Add
minimal inline CSS for a centered transparent diagram surface with a layered light-mode shadow and
a subtle dark-mode white ring, max-width SVG, and horizontal overflow. Emit none of the script/style
for EPUB.

Configure ExDoc with an adaptive Accrue mark and favicon under `priv/ex_doc/`, based on the canonical
brand mark. Both SVGs must switch their dark bars to the light brand neutral under
`prefers-color-scheme: dark`; the package already ships `priv`, so no package whitelist expansion is
needed.

Add one focused ExUnit contract module. Read the guides and both READMEs, obtain the docs config via
`Mix.Project.config()[:docs]`, and invoke its body callback directly. Give failures actionable messages.
Assert:

- wildcard extras include both guides and both READMEs expose the intended links;
- architecture essential H2 headings occur in order, diagram counts are exactly 4/4/4, architecture
  has exactly five Elixir fences, and every extracted architecture block parses with
  `Code.string_to_quoted/1`;
- walkthrough Elixir fence count is in `12..18` and every extracted block parses with
  `Code.string_to_quoted/1` (the guide itself is locked to exactly 18);
- mutual guide links exist;
- neither guide exposes GitHub blob/tree links, `#L` anchors, or prose/code-reading interfaces using
  `lib/`, `test/`, `.planning/`, `examples/`, or package filesystem paths;
- stable anchors occur in both walkthrough and current source: the host
  `Billing.subscribe(billable, price_id, opts)` delegation, subscription-action
  `Idempotency.key(:create_subscription, customer.id, op_id)`, Plug call to
  `Accrue.Webhook.Ingest.run`, default-handler canonical `fetch(:subscription, stripe_id)`, and local
  entitlement `Query.entitling()`;
- HTML hook contains the exact Mermaid pin, strict security, disabled startup, `exdoc:loaded`, live
  ExDoc theme detection/rerendering, an explicit render-before-replace sequence, and failure
  preservation; logo/favicon config points to existing adaptive SVG assets; EPUB callback is empty.

Keep tests resilient to prose edits: freeze essential structure, counts, links, safety behavior, and a
small anchor set only. Do not change scripts/CI; the new contract belongs to the normal core suite.
  </action>
  <verify>
Run from `accrue/`:

`mix test test/accrue/docs/architecture_code_walkthrough_test.exs`

`mix format --check-formatted mix.exs test/accrue/docs/architecture_code_walkthrough_test.exs`

Then run `MIX_ENV=dev mix docs --warnings-as-errors` and inspect generated source to confirm HTML pages
retain the Mermaid code fallback while EPUB contains no Mermaid loader URL. The focused contract must
report exactly five parseable architecture Elixir blocks as well as all 18 parseable walkthrough
blocks.
  </verify>
  <done>
The docs hook renders pinned strict Mermaid safely across ExDoc navigation without sacrificing failure
fallback or EPUB output, and focused contracts catch structural, security, parsing, discovery, and
high-value source drift.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove tests, package contents, and generated-page behavior</name>
  <files>
    README.md
    accrue/README.md
    accrue/guides/architecture.md
    accrue/guides/code-walkthrough.md
    accrue/mix.exs
    accrue/test/accrue/docs/architecture_code_walkthrough_test.exs
  </files>
  <action>
Run final quality gates without modifying unrelated source. First run the focused contract and core
journey tests, then the complete core suite. Build docs with warnings as errors, run the monorepo
package-docs contract, and build (but never publish) the Hex archive. Inspect the archive contents for
both new guides.

Use the `agent-browser` skill in named sessions with local-file access. In the normal session allow
`cdn.jsdelivr.net`, open `accrue/doc/architecture.html`, wait for four rendered SVG diagrams, inspect a
full-page snapshot/screenshot and console, navigate via the guide cross-link to the walkthrough, then
back through ExDoc navigation and confirm all diagrams rerender once with no raw Mermaid source left
after success. Check light and dark presentation, heading order, code width, accessible diagram labels,
and link behavior. In a fresh fallback session block/omit jsDelivr, open the architecture page, and
confirm all four original Mermaid code blocks remain visible/readable when the module import cannot
load. Close both browser sessions. Finally run `open doc/architecture.html` and
`open doc/code-walkthrough.html` from `accrue/` as the prompt requests.

Re-run `git status --short` and compare with the pre-task snapshot. Stage/commit only the six planned
files plus this quick task's GSD artifacts. Do not push, publish to Hex, release, change versions,
rewrite `examples/accrue_host/mix.lock`, or stage any pre-existing work.
  </action>
  <verify>
Run these commands from `accrue/` unless noted:

1. `mix test test/accrue/docs/architecture_code_walkthrough_test.exs`
2. `mix test test/accrue/billing/subscription_actions_test.exs test/accrue/webhook/ingest_test.exs test/accrue/webhook/default_handler_out_of_order_test.exs test/accrue/entitlements/local_map_test.exs test/accrue/entitlements_test.exs test/accrue/events/immutability_test.exs`
3. `mix test --warnings-as-errors`
4. `mix format --check-formatted mix.exs test/accrue/docs/architecture_code_walkthrough_test.exs`
5. `mix format --check-formatted`
6. `MIX_ENV=dev mix docs --warnings-as-errors`
7. From the repository root: `bash scripts/ci/verify_package_docs.sh`
8. `mix hex.build`, then inspect the generated tarball entries for `guides/architecture.md` and
   `guides/code-walkthrough.md`.

The known baseline full-format failure is the untouched
`test/accrue/processor/fake_test.exs`; do not format it. Scoped formatting must pass. Report the full
format result exactly and fail the task if any changed file introduces an additional formatting error.
Browser acceptance requires four rendered diagrams online, readable four-block fallback offline,
working cross-navigation/rerender, legible light/dark pages, and no unexpected page/console errors.
  </verify>
  <done>
Contracts and proportional/full tests pass; scoped formatting passes; any full-format failure is only
the unchanged baseline file; docs build warning-free; the package contains both guides; browser review
proves normal rendering, fallback, navigation, and readability; final diff contains no unrelated work.
  </done>
</task>

</tasks>

<verification>
The executor must preserve command output in the quick-task summary, including test counts, docs build,
Hex archive evidence, agent-browser observations, scoped/full formatting distinction, and the two
documented implementation/code mismatches. No completion claim is valid without generated-page and
package-content evidence.
</verification>

<success_criteria>
- Both guides are included in HexDocs/Hex package and discovered from the root/core reading paths.
- Architecture has the locked narrative, five examples, and four accessible diagrams.
- Walkthrough has exactly 18 parseable, source-faithful excerpts and clearly marks internal APIs.
- Mermaid is pinned to 11.16.0, strict, HTML-only, rerenders on `exdoc:loaded`, and preserves fallback.
- Documentation contracts, core tests, docs build, package verification, and browser review pass.
- Queued webhook timestamp/Braintree processor-map caveats are explicit; runtime behavior is unchanged.
- No unrelated dirty file is altered, staged, committed, formatted, pushed, published, or released.
</success_criteria>

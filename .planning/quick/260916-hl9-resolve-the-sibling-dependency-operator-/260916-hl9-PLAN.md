---
phase: quick-260916-hl9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accrue_portal/mix.exs
  - CLAUDE.md
  - scripts/ci/verify_release_manifest_alignment.sh
autonomous: true
requirements: [HL9-01]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Both sibling packages declare their core dep with the same-minor operator, interpolating @version.
    - CLAUDE.md's accrue_admin dependency row states the real contract instead of an exact-pin contract.
    - CI fails the build if either sibling mix.exs drifts off the interpolated same-minor form.
  artifacts:
    - accrue_portal/mix.exs (Hex-release branch of accrue_dep/0)
    - CLAUDE.md (:accrue row of the accrue_admin technology table)
    - scripts/ci/verify_release_manifest_alignment.sh (new operator assertion)
  key_links:
    - scripts/ci/verify_release_manifest_alignment.sh is already merge-blocking via the release-manifest-ssot job at .github/workflows/ci.yml:278, so the new assertion is enforced on every PR with no workflow edit.
    - accrue_portal/mix.exs dev branch stays {:accrue, path: "../accrue"} so monorepo compilation is unaffected.
---

<objective>
Make the sibling dependency operator consistent and machine-enforced: `accrue_portal` moves off an exact pin to the same-minor operator already used by `accrue_admin`, CLAUDE.md is corrected to describe that real contract, and the existing merge-blocking release-manifest script gains an assertion so the two can never diverge again silently.

Purpose: an exact pin in a published Hex library constrains every adopter's dependency graph, and under release-please `linked-versions` it buys no guarantee the same-minor operator does not already provide. The divergence went unnoticed because no gate asserted anything about the operator.
Output: one changed dependency declaration, one corrected doc row, one strengthened CI assertion.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@accrue_portal/mix.exs
@accrue_admin/mix.exs
@scripts/ci/verify_release_manifest_alignment.sh

Ground facts observed live at planning time (2026-09-16):
- `accrue_admin/mix.exs:117` already reads the interpolated same-minor form inside the `ACCRUE_ADMIN_HEX_RELEASE == "1"` branch of `accrue_dep/0`.
- `accrue_portal/mix.exs:79` reads the exact-pin form inside the `ACCRUE_PORTAL_HEX_RELEASE == "1"` branch of `accrue_dep/0`; line 81 is the dev path branch.
- Both packages are at `@version "1.4.0"`, as is `accrue/mix.exs:4`.
- `examples/accrue_host/mix.exs` already consumes all three packages with the same-minor operator, interpolating a version read from the sibling `mix.exs`.
- `scripts/ci/verify_release_manifest_alignment.sh` is 48 lines, has a `fail()` helper writing `[verify_release_manifest_alignment] ...` to stderr and exiting 1, and ends with a single `OK:` line.
- That script is wired merge-blocking at `.github/workflows/ci.yml:278`; no workflow change is needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize accrue_portal on the same-minor sibling operator</name>
  <files>accrue_portal/mix.exs</files>
  <action>In `defp accrue_dep`, inside the branch guarded by `System.get_env("ACCRUE_PORTAL_HEX_RELEASE") == "1"`, replace the exact-pin operator in the `{:accrue, ...}` tuple with the same-minor operator `~>`, keeping the `#{@version}` interpolation exactly as it is. The resulting tuple must be byte-identical in shape to `accrue_admin/mix.exs:117`. Leave the `else` branch (`{:accrue, path: "../accrue"}`) untouched, and change nothing else in the file — not `@version`, not `package/0`, not `docs/0`.</action>
  <verify>
    <automated>cd accrue_portal && mix compile</automated>
  </verify>
  <done>`accrue_portal/mix.exs` declares `{:accrue, "~> #{@version}"}` in its Hex-release branch, the dev path branch is unchanged, and `cd accrue_portal && mix compile` succeeds (proving the path-dep dev branch is unaffected).</done>
</task>

<task type="auto">
  <name>Task 2: Correct the CLAUDE.md accrue dependency row</name>
  <files>CLAUDE.md</files>
  <action>In the `### Core Technologies — accrue_admin package` table, edit ONLY the row whose Technology cell is `:accrue` (currently CLAUDE.md:52). Change its Version Constraint cell from the exact-pin form to the same-minor form `~> <same version>`. Rewrite the Rationale cell so it states the actual contract: sibling dep via `path:` in dev (monorepo) and a same-minor constraint in published releases; release-please linked-versions releases all three packages at the same version, so the minor is locked in lockstep while patch releases of the core are allowed to flow to adopters; enforced in CI by `scripts/ci/verify_release_manifest_alignment.sh`. Drop the sentence asserting an exact version match per release. Keep the table's four-column structure, its pipe alignment style, and its terse reference tone. Do not touch any other row, table, or section of CLAUDE.md.</action>
  <verify>
    <automated>test "$(grep -c '`~> <same version>`' CLAUDE.md)" -eq 1</automated>
  </verify>
  <done>The `:accrue` row reads `~> <same version>` with a rationale describing same-minor lockstep plus permitted patch flow and naming the CI enforcer; the diff against CLAUDE.md touches exactly that one table row and nothing else.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the sibling operator in the merge-blocking release manifest script</name>
  <files>scripts/ci/verify_release_manifest_alignment.sh</files>
  <action>Extend `scripts/ci/verify_release_manifest_alignment.sh`, after the existing manifest/@version lockstep assertions and before the final `OK:` echo, with an assertion over BOTH `accrue_admin/mix.exs` and `accrue_portal/mix.exs`. For each file: select the non-comment lines declaring a version-constrained accrue dependency (lines containing the text `{:accrue, "` with lines whose first non-whitespace character is `#` excluded — do NOT strip inline `#`, because the required form itself contains `#{@version}`); require exactly one such line; and require its whitespace-trimmed content to equal the interpolated same-minor form using the module attribute rather than any literal version. Compare against a single shell variable holding the expected form so the expected string appears once. A hardcoded version literal in place of the attribute must fail this comparison, since a literal silently stops tracking the attribute; note that requirement in a leading comment for the block. Use the script's existing `fail()` helper and its `[verify_release_manifest_alignment] ...` message style; each failure message must name the offending file, what was found, and the expected form. Also handle the zero-match and multi-match cases with their own failure messages. Update the closing `OK:` line so it additionally reports that both sibling packages declare the interpolated same-minor accrue constraint. Do not add new external tool dependencies beyond what the script already uses (`jq`, `sed`, coreutils, grep).

Then prove the new gate is non-vacuous with two negative controls. These are required, not optional — this repo has a documented history of gates that assert nothing. Control A: temporarily restore the exact-pin operator in `accrue_portal/mix.exs`'s Hex-release branch, run `bash scripts/ci/verify_release_manifest_alignment.sh`, confirm it exits non-zero with the new message naming that file, then restore via `git checkout -- accrue_portal/mix.exs` (or the equivalent targeted revert if the file has other staged work). Control B: temporarily replace the `#{@version}` interpolation in `accrue_admin/mix.exs`'s Hex-release branch with a hardcoded `1.4.0` literal (still using `~>`), run the script again, confirm it exits non-zero with the new message naming that file, then restore. After both controls, confirm `git status --porcelain accrue_admin/mix.exs accrue_portal/mix.exs` shows only the Task 1 change and re-run the script to confirm exit 0. Capture and quote the verbatim stderr output of both failing runs in the SUMMARY.</action>
  <verify>
    <automated>bash scripts/ci/verify_release_manifest_alignment.sh &amp;&amp; git diff --quiet accrue_admin/mix.exs</automated>
  </verify>
  <done>`bash scripts/ci/verify_release_manifest_alignment.sh` exits 0 and prints an `OK:` line naming both the manifest/@version alignment and the verified sibling constraint form. Both negative controls produced a non-zero exit with a distinct, file-naming failure message; both files were restored (`accrue_admin/mix.exs` shows no diff); both failure transcripts are quoted verbatim in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → published Hex tarball | The dependency constraint in `mix.exs` crosses into adopters' dependency resolution and constrains their whole graph. |
| PR author → merge-blocking CI | The new assertion is the only machine check standing between a drifted sibling constraint and a published release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-hl9-01 | Tampering | `accrue_portal/mix.exs` dev branch | medium | mitigate | Task 1 restricts the edit to the `ACCRUE_PORTAL_HEX_RELEASE == "1"` branch only; `mix compile` in the monorepo proves the `path:` dev branch still resolves. |
| T-hl9-02 | Spoofing (vacuous gate) | `scripts/ci/verify_release_manifest_alignment.sh` | high | mitigate | Task 4's two mandatory negative controls (exact-pin form and hardcoded-literal form) prove the assertion rejects both drift shapes; transcripts quoted in the SUMMARY. |
| T-hl9-03 | Tampering | grep-based source assertion | medium | mitigate | Comment lines are excluded by leading-`#` filter only (never inline `#`, which would destroy `#{@version}`); exactly-one-match is required so an added decoy line fails rather than passes. |
| T-hl9-04 | Information disclosure | `~>` widens the accepted core range | low | accept | `~> 1.4.0` still means `>= 1.4.0 and < 1.5.0`, so a sibling can never pair with a core that changed schema in a new minor; patch flow is the intended behavior and matches `examples/accrue_host/mix.exs`. |
| T-hl9-05 | Elevation of privilege | Repudiation of doc/code divergence | low | mitigate | CLAUDE.md now names the CI enforcer, so the doc and the gate cite each other. |
| T-hl9-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this task; no new dependency of any kind is added. |
</threat_model>

<verification>
- `bash scripts/ci/verify_release_manifest_alignment.sh` exits 0 and prints an informative `OK:` line covering both manifest alignment and the sibling constraint form.
- Negative control A (exact-pin restored in `accrue_portal/mix.exs`): script exits non-zero, message names that file. Restored afterward.
- Negative control B (hardcoded version literal in `accrue_admin/mix.exs`): script exits non-zero, message names that file. Restored afterward.
- `cd accrue_portal && mix compile` succeeds.
- The CLAUDE.md diff touches exactly one line (the `:accrue` table row).
</verification>

<success_criteria>
- `accrue_portal/mix.exs` and `accrue_admin/mix.exs` both declare `{:accrue, "~> #{@version}"}` in their Hex-release branches, with dev `path:` branches untouched.
- CLAUDE.md's `:accrue` row describes same-minor lockstep with permitted patch flow and names the CI enforcer.
- The merge-blocking release-manifest script fails on either drift shape, proven by quoted transcripts.
- No other file, row, or workflow is modified.
</success_criteria>

<output>
Create `.planning/quick/260916-hl9-resolve-the-sibling-dependency-operator-/260916-hl9-SUMMARY.md` when done, including the two verbatim negative-control failure transcripts.
</output>

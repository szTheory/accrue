# v1.17 — Friction inventory and scoped backlog (FRG-01 + FRG-03)

**Purpose:** Single **evidence SSOT** for ranked friction (**FRG-01**) and execution backlog routing (**FRG-03**). **North star + stop rules** live in **`v1.17-north-star.md`** (FRG-02) — do not duplicate that essay here; link upward when context is needed.

**Invariant:** Do not rely on **`.planning/phases/*`** as the **only** copy of this inventory — `phases.clear` may remove working trees. This file under **`research/`** is the durable home until milestone archive.

---

## FRG-01 — Ranked friction inventory

### Priority bar (triage P0 vs CI)

Use **two axes** on every row (avoids “CI green so no pain” and “everything is P0”):

| Axis | Values | Meaning |
|------|--------|---------|
| **`ci_contract`** | `merge_blocking` · `advisory` · `n/a` | Relationship to **VERIFY-01**, **host-integration**, **`verify_package_docs`**, or other named checks. |
| **`integrator_impact`** | `blocks_golden_path` · `blocks_secondary_path` · `no_workaround` · `workaround_documented` | Effect on the documented first-hour / host path. |

**Triage P0** = (**`blocks_golden_path`** OR **`no_workaround`** for a documented integrator obligation) **and** impact touches **core adoption**, **correctness**, or **trust** for billing — **or** row documents a **`merge_blocking`** failure on `main`.

**CI-only red** without integrator story → fix immediately; may be **`merge_blocking`** without being a long-lived **FRG-03** row (close when green).

**Sources:** Every row needs **≥1** evidence link from: verifier name + script path, CI log, issue/PR URL, doc path + anchor, adoption matrix row id. Mark `source_tier`: **`primary`** | **`mixed`** | **`secondary`** (secondary allowed only with a **promotion hypothesis**).

### Inventory table (add rows below)

| `id` | `priority` | `title` | `ci_contract` | `integrator_impact` | `workaround` | `sources` | `source_tier` | `req` | `tags` | `frg03_disposition` | `status` | `notes` |
|------|------------|---------|---------------|---------------------|--------------|-----------|---------------|-------|--------|---------------------|----------|---------|
| v1.17-P0-001 | P0 | Hex / workspace `@version` vs published Hex vs First Hour `~>` pins still stall pre-1.0 upgrades | `merge_blocking` | `blocks_golden_path` | Run `bash scripts/ci/verify_package_docs.sh`; mirror First Hour § Install with workspace `mix.exs` | `accrue/guides/first_hour.md` (deps + “Hex vs main”); `scripts/ci/README.md` (INT-09); `scripts/ci/verify_package_docs.sh` | `primary` | INT-10 | docs,verify | →63 | closed | Maintainer (2026-04-23): INT-10 should keep golden-path pins provably aligned with the published SemVer train without re-reading three SSOTs. Closed 2026-04-23: First Hour + package README three-fact framing (Phase 63-01); see `.planning/milestones/v1.17-phases/63-p0-integrator-verify-docs/63-VERIFICATION.md`. |
| v1.17-P0-002 | P0 | `host-integration` bundles multiple verifiers; first CI failure does not name which contract broke | `merge_blocking` | `no_workaround` | Use `scripts/ci/README.md` triage tables + host README **#proof-and-verification** | `examples/accrue_host/README.md` (Proof and verification); `scripts/ci/README.md` (INT gates + triage); `.github/workflows/ci.yml` (`host-integration`) | `primary` | INT-10 | verify,docs | →63 | closed | Maintainer (2026-04-23): Phase 63 may add a single ordered “first failing gate” pointer in contributor-facing docs after this row is accepted. Closed 2026-04-23: `[host-integration]` phase slugs + `FAILED_GATE=host-integration` in `accrue_host_uat.sh` and triage subsection in `scripts/ci/README.md` (Phase 63-02); see `63-VERIFICATION.md`. |
| v1.17-P1-001 | P1 | Adoption proof matrix vs `verify_adoption_proof_matrix.sh` needles drift on taxonomy edits | `merge_blocking` | `blocks_secondary_path` | `scripts/ci/README.md` § “Triage: verify_adoption_proof_matrix.sh” | `examples/accrue_host/docs/adoption-proof-matrix.md`; `scripts/ci/verify_adoption_proof_matrix.sh` | `primary` | INT-10 | adopt,verify | downgraded | closed | **Closed 2026-04-24 (v1.19 + v1.20 inventory):** **PRF-01..02** shipped merge-blocking needle alignment + contributor same-PR co-update rule (Phase **67** / **`.planning/milestones/v1.19-phases/67-proof-contracts/67-VERIFICATION.md`**); **PRD-02** cross-links preserve discoverability. Reopen as **new sourced row** only if matrix/script/ExUnit drift recurs on `main`. |
| v1.17-P1-002 | P1 | Post–linked-Hex-publish drift: workspace `@version`, First Hour pins, adoption matrix needles, or `.planning/` public-Hex callouts disagree with registry reality | `merge_blocking` | `blocks_secondary_path` | Same-PR discipline per **`scripts/ci/README.md`**; run **`verify_package_docs.sh`** + **`verify_adoption_proof_matrix.sh`** locally or via CI | **`RELEASING.md`**; **`scripts/ci/verify_package_docs.sh`**; **`scripts/ci/verify_adoption_proof_matrix.sh`**; **`.planning/PROJECT.md`** (Current State); **Hex** package pages for **`accrue` / `accrue_admin`** | `primary` | INT-10 | publish,verify | →75 | closed | **Closed 2026-04-24 (v1.23 Phase 75):** **`@version` 0.3.1** matches published **Hex** pair; **`docs-contracts-shift-left`** bash gates green (including **`verify_v1_17_friction_research_contract.sh`** row-count bump for this row); evidence **`.planning/milestones/v1.23-phases/75-post-publish-contract-alignment/75-VERIFICATION.md`**. **`package_docs_verifier_test`** remains **CI / release-gate** (requires **TestRepo**). |
| v1.17-P2-001 | P2 | First Hour cross-links span many files — easy to miss same-PR spine updates when editing one capsule | `advisory` | `workaround_documented` | First Hour “How to enter” + host README capsule parity callout | `accrue/guides/first_hour.md` (How to enter); `examples/accrue_host/README.md` (How to enter) | `mixed` | INT-10 | docs | downgraded | closed | **Closed 2026-04-23 (v1.21 INT-11):** contributor **same-PR capsule parity** checklist in **`scripts/ci/README.md`** § “First Hour + host README capsule parity”; cross-links from **First Hour** → **`maturity-and-maintenance.md`**. Reopen only if VERIFY-01 / **`verify_package_docs`** proves capsule drift on **`main`**. |

**`frg03_disposition`:** `→63` | `→64` | `→65` | `not_v1.17` (requires **signed rationale** in `notes`) | `downgraded` (P0→P1+ with reason).

**`id` format:** Namespace **`v1.17-`**, band (**P0** / **P1** / **P2**), hyphen, three-digit **`NNN`** — **stable**; do not renumber; append new IDs.

### v1.20 evidence refresh (maintainer pass — 2026-04-24)

**INV-01 / INV-02 (see `.planning/REQUIREMENTS.md`):** **`v1.17-P1-001`** row closed above with **v1.19** verification pointers. **No additional sourced P0/P1 rows** were added in this pass — post-**0.3.1** integrator contracts are green on the evidence checked (**`verify_adoption_proof_matrix.sh`**, **`verify_package_docs.sh`**, **First Hour** / host README spine).

**Revisit trigger:** next **linked Hex publish** for **`accrue` / `accrue_admin`**, or an intentional **adoption proof matrix** taxonomy / Layer C script rename that touches **ORG-** / **INT-** needles without a same-PR verifier update — **routed** to **`v1.17-P1-002`** (**v1.23** Phase **75**, **`.planning/REQUIREMENTS.md`** **PPX-01..04**).

### v1.25 INV-03 maintainer pass (2026-04-24)

**INV-03** (**`.planning/REQUIREMENTS.md`**): post–**v1.24** maintainer pass on this inventory. **No new sourced P1/P2 friction rows** were appended — ranked evidence on **`main`** at reviewed merge **`149736d0b1523f7ac8982da84cd14f49a5deebbd`** did not clear the **FRG-01** bar for a net-new row versus a dated certification (per **`.planning/milestones/v1.25-phases/079-friction-inventory-maintainer-pass/079-CONTEXT.md`** D-01 / **FRG-02** **S1** / **S5**). Falsifiable evidence for the named verifier bundle (**`verify_v1_17_friction_research_contract.sh`**, **`verify_package_docs.sh`**, **`verify_adoption_proof_matrix.sh`**, **VERIFY-01** via **`verify_verify01_readme_contract.sh`**, and **GitHub Actions** **`host-integration`**) is recorded in **`.planning/milestones/v1.25-phases/079-friction-inventory-maintainer-pass/079-VERIFICATION.md`** on the same documentation change-set as this subsection.

**Revisit trigger:**

- Next **linked Hex publish** for **`accrue` / `accrue_admin`** (re-open **INV-03** evidence against the new registry reality — same family as **`v1.17-P1-002`**).
- A **merge-blocking** failure on **`main`** in **`host-integration`** or **`docs-contracts-shift-left`** that documents a **new** integrator stall not captured in an existing row’s **`notes`** / reopen trigger.
- Intentional **adoption proof matrix** taxonomy or verifier rename without same-PR **`verify_adoption_proof_matrix.sh`** co-update.

### v1.26 INV-04 maintainer pass (2026-04-24)

**INV-04** (**`.planning/REQUIREMENTS.md`**): post–**INT-13** (Phase **82**) maintainer pass on this inventory after **`Accrue.Billing.create_billing_portal_session/2`** landed on the First Hour + adoption proof matrix spine. **No new sourced P1/P2 friction rows** were appended — ranked evidence on the reviewed tree at merge **`d1f121c75bb21fb2dd53c9ac9315bd83c26a438e`** did not clear the **FRG-01** bar for a net-new row versus a dated certification (per **`.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-CONTEXT.md`** D-01 / **FRG-02** **S1** / **S5**). Falsifiable evidence for the named verifier bundle (**`verify_v1_17_friction_research_contract.sh`**, **`verify_package_docs.sh`**, **`verify_adoption_proof_matrix.sh`**, **VERIFY-01** via **`verify_verify01_readme_contract.sh`**, **GitHub Actions** **`docs-contracts-shift-left`**, and **`host-integration`**) is recorded in **`.planning/milestones/v1.26-phases/083-friction-inventory-post-touch/083-VERIFICATION.md`** on the same documentation change-set as this subsection.

**Revisit trigger:**

- Next **linked Hex publish** for **`accrue` / `accrue_admin`** — re-open **INV-04** evidence against registry reality (same family as **`v1.17-P1-002`** / **v1.23** Phase **75**).
- **INT-13** drift: **`create_billing_portal_session`** or **billing portal** needles disappear from **First Hour** / host README **§ proof-and-verification** / adoption matrix without a sourced row update when **`main`** shows a new integrator stall.
- **First Hour** cross-links, adoption matrix taxonomy, or **`verify_adoption_proof_matrix.sh`** needles change without same-PR verifier co-update.
- A **merge-blocking** failure in **`docs-contracts-shift-left`** or **`host-integration`** on **`main`** that documents a new stall not captured in existing **`notes`** / reopen triggers.

### v1.27 INV-05 maintainer pass (2026-04-24)

**INV-05** (**`.planning/REQUIREMENTS.md`**): post–**CLS-01..03** (Phase **84**) maintainer pass on this inventory after **pre-1.0 closure narrative** landed on repo root **`README.md`**, **`accrue/README.md`**, **`RELEASING.md`**, and **`accrue/guides/upgrade.md`**. **No new sourced P1/P2 friction rows** were appended — ranked evidence on the reviewed tree at commit **`a47a2ba9db27d6ce7a3c2e88a2577b195503be6a`** did not clear the **FRG-01** bar for a net-new row versus a dated certification (per **FRG-02** **S1** / **S5**). Falsifiable evidence for the named verifier bundle (**`verify_v1_17_friction_research_contract.sh`**, **`verify_package_docs.sh`**, **`verify_adoption_proof_matrix.sh`**, **VERIFY-01** via **`verify_verify01_readme_contract.sh`**, **`verify_production_readiness_discoverability.sh`**, **GitHub Actions** **`docs-contracts-shift-left`**, and **`host-integration`**) is recorded in **`.planning/milestones/v1.27-phases/85-friction-inventory-post-closure/085-VERIFICATION.md`** on the same documentation change-set as this subsection.

**Revisit trigger:**

- Next **linked Hex publish** for **`accrue` / `accrue_admin`** — re-open **INV-05** evidence against registry reality (same family as **`v1.17-P1-002`** / **v1.23** Phase **75**).
- **CLS** drift: **Maintenance posture** / **wrap-up semantics** removed or contradicted without a sourced row update when **`main`** shows a new integrator stall.
- A **merge-blocking** failure in **`docs-contracts-shift-left`** or **`host-integration`** on **`main`** that documents a new stall not captured in existing **`notes`** / reopen triggers.

### v1.28 INV-06 maintainer pass (2026-04-24)

**INV-06** (**`.planning/REQUIREMENTS.md`**): post–**PPX-05..08** (**Phase 86**) / **v1.28** registry + **`.planning/`** mirror work maintainer pass **(b)** on this inventory. **No new sourced P1/P2 friction rows** were appended — ranked evidence on the reviewed baseline at merge **`aa3df3cad0262b3760a9f9a65a56d177eb6bc047`** (used to pin **`.github/workflows/ci.yml`** **`docs-contracts-shift-left`** membership) did not clear the **FRG-01** bar for a net-new row versus a dated certification (per **FRG-02** **S1** / **S5**). Falsifiable methodology + transcript pointer is **`.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md`** on the same documentation change-set as this subsection.

**Evidence pointer:** **`.planning/milestones/v1.28-phases/087-friction-inventory-post-publish/087-VERIFICATION.md`** — **Reviewed merge SHA** **`aa3df3cad0262b3760a9f9a65a56d177eb6bc047`**.

**Revisit trigger:**

- Next **linked Hex publish** for **`accrue` / `accrue_admin`** — re-open **INV-06** evidence against registry reality (same family as **`v1.17-P1-002`** / **v1.23** Phase **75**).
- **Adoption proof matrix** / Layer **C** rename without same-PR **`verify_adoption_proof_matrix.sh`** co-update.
- A **merge-blocking** failure on **`main`** in **`host-integration`** or **`docs-contracts-shift-left`** that documents a **new** integrator stall not captured in an existing row’s **`notes`** / reopen trigger.
- **INT-13**-class facade / **First Hour** / adoption matrix needle drift on **`main`** without a sourced row update when evidence shows a new stall.
- **PPX-08**-class **`.planning/`** public-Hex / last-published callout mismatch vs **actual** registry versions for shipped **`accrue` / `accrue_admin`** — re-open **INV-06** evidence and fix mirrors per **`.planning/REQUIREMENTS.md` PPX-08`** without requiring a new publish.

---

## FRG-03 — Scoped backlog for Phases 63–65

**Rule:** A row appears in the **execution slice** for **63 / 64 / 65** iff **`priority`** = **P0** (triage bar) **and** **`req`** is **INT-10** / **BIL-03** / **ADM-12** respectively (or split if multi-axis — pick **primary** `req`).

**Out of v1.17:** `frg03_disposition` = **`not_v1.17`** with explicit rationale and future owner — still listed in **FRG-01** (no silent scope).

### Backlog — INT-10 (Phase 63)

- **`v1.17-P0-001`** — pins / Hex honesty (**→63**)
- **`v1.17-P0-002`** — VERIFY / CI triage discoverability (**→63**)

### Backlog — INT-10 (v1.23 Phase 75)

- **`v1.17-P1-002`** — post–Hex-publish contract alignment (**→75**, **closed** **2026-04-24** — see **`75-VERIFICATION.md`**)

### Backlog — BIL-03 (Phase 64)

**Maintainer (2026-04-23):** The **BIL-03** execution slice has **zero** billing **P0** rows for v1.17; closure evidence lives in **`.planning/milestones/v1.17-phases/64-p0-billing/64-VERIFICATION.md`**.
- *(no P0 rows — queue empty for **→64** work this milestone)*

### Backlog — ADM-12 (Phase 65)

**Maintainer (2026-04-23):** The **ADM-12** execution slice has **zero** admin **P0** rows for v1.17; closure evidence lives in **`.planning/milestones/v1.17-phases/65-p0-admin-operator/65-VERIFICATION.md`**.
- *(no P0 rows — queue empty for **→65** work this milestone)*

**`.planning/ROADMAP.md`** may add **one pointer line** per phase to the relevant anchor here — **do not duplicate** row bodies in ROADMAP.

---

## Deferred / future requirements

Rows that belong to **PROC-08**, **FIN-03**, or other future milestones: keep in inventory with **`not_v1.17`** and link **`.planning/PROJECT.md`** non-goals.

### `not_v1.17` — pure-Elixir PDF adapter exploration

- **`PDF-FUTURE-001`** (`not_v1.17`, `disposition: deferred`): Evaluate a pure-Elixir PDF rendering backend as a possible default/blessed `Accrue.PDF` adapter, replacing the ChromicPDF (Chrome/Chromium dep) and Gotenberg (sidecar) options for hosts that want to drop the external-process dependency entirely. Candidate: an unpublished WIP Elixir PDF library by the lead author (Prawn-equivalent shape). Benefits if it matures: simpler DX/devops (no Chrome on host, no sidecar service), smaller container images, fewer moving parts. Open questions: rendering fidelity vs ChromicPDF, performance vs Chromium, fonts/CJK coverage. Cross-link: `accrue/guides/pdf.md` § Future / experimental adapters. **Revisit triggers:** the candidate library reaches a published `0.1.0` on Hex; OR a new `Accrue.PDF` adapter scope opens (post-v1.0 or new milestone). Until either trigger fires, ChromicPDF stays the v1.0 default.

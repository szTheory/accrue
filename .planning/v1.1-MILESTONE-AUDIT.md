---
milestone: v1.1
milestone_name: Stabilization + Adoption
audited: 2026-04-16T19:40:00Z
status: gaps_found
scope:
  phases:
    - 10-host-app-dogfood-harness
    - 11-ci-user-facing-integration-gate
  note: "Audit scoped to completed active milestone directories returned by gsd-tools phases list. Planned future roadmap entries 12-15 remain outside this completed-scope audit."
scores:
  requirements: 14/14
  phases: 2/2
  integration: 13/14
  flows: 3/4
  nyquist: 1/2
gaps:
  requirements: []
  integration:
    - id: INT-001
      status: partial
      affected_requirements: [HOST-06, HOST-07, CI-04]
      phase: "10 -> 11"
      evidence: "Phase 11 Playwright creates/cancels a subscription for fixture.normal_email, but admin replay uses a separately seeded fixture.webhook_id tied to billing-history@example.test."
      remediation: "Create a gap-closure phase/plan that carries browser-created billing activity into the admin replay target, or explicitly reclassify the two adjacent flows as the accepted milestone boundary."
  flows:
    - id: FLOW-001
      status: partial
      name: "user billing -> admin replay -> CI artifact path"
      breaks_at: "handoff from browser-created subscription to admin replay target"
      evidence: "Checkout/cancel, seeded admin replay, and failure artifact upload are wired; the replay does not target webhook/event data generated from the same browser-created billing activity."
tech_debt:
  - phase: 10-host-app-dogfood-harness
    items:
      - "10-VALIDATION.md is not currently Nyquist-clean: frontmatter has nyquist_compliant: true but wave_0_complete: false, task rows remain pending, and sign-off remains pending."
  - phase: 11-ci-user-facing-integration-gate
    items:
      - "host-integration installs npm/Chromium before accrue_host_uat.sh repeats npm ci and npm run e2e:install."
      - "scripts/ci/accrue_host_browser_smoke.cjs remains in the repo as a legacy raw smoke runner even though the canonical blocking path is Playwright."
nyquist:
  compliant_phases: [11]
  partial_phases: [10]
  missing_phases: []
  overall: partial
---

# v1.1 Milestone Audit

## Scope

This audit used `gsd-tools phases list`, which returned the completed active phase directories:

| Phase | Status | Verification | Validation |
|-------|--------|--------------|------------|
| 10-host-app-dogfood-harness | complete | verified | partial |
| 11-ci-user-facing-integration-gate | complete | passed | compliant |

The roadmap also lists planned future v1.1 phases 12-15. Those requirements remain pending in `REQUIREMENTS.md` and are not counted as completed-scope failures in this audit because their phase directories and plans have not been created yet.

## Requirements Coverage

Completed-scope requirements are fully satisfied by the three-source cross-check:

| Requirement Set | Requirements | REQUIREMENTS.md | SUMMARY frontmatter | VERIFICATION.md | Final |
|-----------------|--------------|-----------------|---------------------|-----------------|-------|
| Host App Dogfood | HOST-01..HOST-08 | Complete | Listed by Phase 10 summaries | Satisfied in 10-VERIFICATION.md | 8/8 satisfied |
| CI Integration Gate | CI-01..CI-06 | Complete | Listed by Phase 11 summaries | Satisfied in 11-VERIFICATION.md | 6/6 satisfied |

No orphaned completed-scope requirements were found.

## Phase Verification

| Phase | Verification Status | Score | Critical Gaps |
|-------|---------------------|-------|---------------|
| 10 | verified | 5/5 must-haves | none |
| 11 | passed | 10/10 must-haves | none |

## Integration Check

The integration checker verified these major cross-phase connections:

| Flow / Surface | Status | Evidence |
|----------------|--------|----------|
| Clean checkout -> installer rerun -> compile/tests -> browser gate -> CI | wired | `scripts/ci/accrue_host_uat.sh` is called by `.github/workflows/ci.yml` host-integration. |
| `/app/billing` signed-in user flow | wired | Phase 10 LiveView tests and Phase 11 Playwright both exercise the route. |
| `/webhooks/stripe` signed ingest flow | wired | Phase 10 webhook ingest test verifies signed payload, dispatch, and events. |
| `/billing` admin mount and replay | wired | Phase 10 admin tests and Phase 11 Playwright exercise protected admin routes. |
| Browser failure artifacts | wired | Playwright retains report/traces/screenshots; `ci.yml` uploads report, test-results, and server log on failure. |

### Integration Gap

| ID | Affected Requirements | Status | Detail | Remediation |
|----|-----------------------|--------|--------|-------------|
| INT-001 | HOST-06, HOST-07, CI-04 | partial | Phase 11 proves browser checkout/cancel and admin replay in one job, but the admin replay uses a separately seeded webhook row rather than the browser-created subscription. | Add a gap-closure plan that carries the browser-created billing activity into admin replay, or explicitly document the two adjacent flows as the intended milestone boundary. |

## Broken / Partial Flows

| Flow | Status | Break Point | Notes |
|------|--------|-------------|-------|
| user billing -> admin replay -> CI artifact path | partial | Browser-created subscription is not the replay target | Checkout/cancel, seeded replay, and artifacts all work independently; the handoff is not continuous. |

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 10 | exists | partial | `$gsd-validate-phase 10` |
| 11 | exists | true | none |

Phase 10 validation is the main documentation-quality gap: the file says `nyquist_compliant: true`, but `wave_0_complete: false`, all per-task rows are still pending, and sign-off remains pending. Phase 11 validation is verified and all rows are green.

## Tech Debt

| Phase | Item |
|-------|------|
| 10 | Validation strategy needs cleanup/retroactive audit so it matches the already-passed phase verification and UAT automation. |
| 11 | CI host-integration duplicates browser dependency installation that the shell script also performs. |
| 11 | Legacy raw browser smoke script remains in `scripts/ci/accrue_host_browser_smoke.cjs` and may drift from the canonical Playwright gate. |

## Result

**Status:** gaps_found

The completed-scope requirements are satisfied, and both phases have passed phase verification. The milestone should not be archived yet because cross-phase integration has one partial E2E handoff and Phase 10 has partial Nyquist validation metadata.

Recommended next action:

```bash
$gsd-plan-milestone-gaps
```

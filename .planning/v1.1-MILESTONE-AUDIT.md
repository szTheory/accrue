---
milestone: v1.1
milestone_name: Stabilization + Adoption
audited: 2026-04-16T19:47:59Z
status: gaps_found
scope:
  phases:
    - 10-host-app-dogfood-harness
    - 11-ci-user-facing-integration-gate
  note: "Audit scoped to completed active milestone directories returned by gsd-tools phases list. Planned future roadmap entries 12-15 remain outside this completed-scope audit."
scores:
  requirements: 14/14
  phases: 2/2
  integration: 13/14
  flows: 3/4
  nyquist: 2/2
gaps:
  requirements: []
  integration:
    - id: INT-001
      status: partial
      affected_requirements: [HOST-08, CI-02, CI-03]
      phase: "10 -> 11"
      evidence: "The canonical host gate passes because scripts/ci/accrue_host_uat.sh resets the test database before focused host proofs. The integration checker found direct focused host-flow test runs can collide on deterministic processor ids when run outside that wrapper-managed state hygiene."
      remediation: "Make focused host-flow tests hermetic without relying on a wrapper-level database reset, or document that these proofs are only supported through the canonical host UAT wrapper."
  flows:
    - id: FLOW-001
      status: partial
      name: "direct focused host-flow proof rerun"
      breaks_at: "state isolation before subscription/webhook/admin replay proofs"
      evidence: "Directly running the subscription, webhook ingest, and admin replay proof files together can hit accrue_subscriptions_processor_processor_id_index collisions on previously seeded deterministic records; the full UAT wrapper resets the database and passes."
tech_debt:
  - phase: 11-ci-user-facing-integration-gate
    items:
      - "scripts/ci/accrue_host_browser_smoke.cjs remains in the repo as a legacy raw smoke runner even though the canonical blocking path is Playwright."
      - "Some lower-level exercised paths can still emit no-operation-id warnings even though the main billing UI passes explicit operation ids."
nyquist:
  compliant_phases: [10, 11]
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# v1.1 Milestone Audit

## Scope

This audit used `gsd-tools phases list`, which returned the completed active phase directories:

| Phase | Status | Verification | Validation |
|-------|--------|--------------|------------|
| 10-host-app-dogfood-harness | complete | verified | compliant |
| 11-ci-user-facing-integration-gate | complete | passed | compliant |

The roadmap also lists planned future v1.1 phases 12-15. Those requirements remain pending in `REQUIREMENTS.md` and are not counted as completed-scope failures in this audit because their phase directories and plans have not been created yet.

## Requirements Coverage

Completed-scope requirements are fully satisfied by the three-source cross-check:

| Requirement Set | Requirements | REQUIREMENTS.md | SUMMARY frontmatter | VERIFICATION.md | Final |
|-----------------|--------------|-----------------|---------------------|-----------------|-------|
| Host App Dogfood | HOST-01..HOST-08 | Complete | Listed by Phase 10 summaries | Satisfied in 10-VERIFICATION.md | 8/8 satisfied |
| CI Integration Gate | CI-01..CI-06 | Complete | Listed by Phase 11 summaries | Satisfied in 11-VERIFICATION.md | 6/6 satisfied |

No orphaned completed-scope requirements were found.

## Phase Verification

| Phase | Verification Status | Score | Critical Gaps |
|-------|---------------------|-------|---------------|
| 10 | verified | 5/5 must-haves | none |
| 11 | passed | 10/10 must-haves | none |

## Integration Check

The integration checker verified these major cross-phase connections:

| Flow / Surface | Status | Evidence |
|----------------|--------|----------|
| Clean checkout -> installer rerun -> compile/tests -> browser gate -> CI | wired | `scripts/ci/accrue_host_uat.sh` is called by `.github/workflows/ci.yml` host-integration. |
| `/app/billing` signed-in user flow | wired | Phase 10 LiveView tests and Phase 11 Playwright both exercise start/cancel through the generated host billing facade. |
| `/webhooks/stripe` signed ingest flow | wired | Phase 10 webhook ingest test verifies signed payload, duplicate suppression, normal dispatch, and persisted host event output. |
| `/billing` admin mount and replay | wired | Phase 10 admin tests and Phase 11 Playwright exercise protected admin routes, webhook replay, and audit event evidence. |
| Browser failure artifacts | wired | Playwright retains report/traces/screenshots; `ci.yml` uploads report, test-results, and server log on failure. |
| Direct focused host-flow proof rerun | partial | Direct focused test invocation can collide on deterministic processor ids unless the wrapper gate resets the test database first. |

### Integration Gap

| ID | Affected Requirements | Status | Detail | Remediation |
|----|-----------------------|--------|--------|-------------|
| INT-001 | HOST-08, CI-02, CI-03 | partial | Focused host-flow proof files are not hermetic when run directly against a dirty test database; deterministic subscription/webhook/admin replay records can collide. | Make the focused host-flow tests self-isolating, or explicitly define the wrapper reset in `scripts/ci/accrue_host_uat.sh` as the only supported invocation path for those proofs. |

## Broken / Partial Flows

| Flow | Status | Break Point | Notes |
|------|--------|-------------|-------|
| direct focused host-flow proof rerun | partial | state isolation before subscription/webhook/admin replay proofs | The full host UAT wrapper passes end to end; direct ad hoc focused runs can fail with unique-index collisions if prior seeded records remain. |

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 10 | exists | true | none |
| 11 | exists | true | none |

Phase 10 validation was refreshed on 2026-04-16. Both completed phases now have `nyquist_compliant: true`, `wave_0_complete: true`, and green task-level validation rows.

## Tech Debt

| Phase | Item |
|-------|------|
| 11 | Legacy raw browser smoke script remains in `scripts/ci/accrue_host_browser_smoke.cjs` and may drift from the canonical Playwright gate. |
| 11 | Some lower-level exercised paths can still emit no-operation-id warnings even though the main billing UI passes explicit operation ids. |

## Verification Executed During Audit

The Phase 10 validation refresh passed:

- `cd examples/accrue_host && MIX_ENV=test mix ecto.drop --quiet || true && MIX_ENV=test mix ecto.create --quiet && MIX_ENV=test mix ecto.migrate --quiet && mix compile --warnings-as-errors && mix test --max-cases 1 test/install_boundary_test.exs test/accrue_host/billing_facade_test.exs test/accrue_host_web/subscription_flow_test.exs test/accrue_host_web/webhook_ingest_test.exs test/accrue_host_web/admin_mount_test.exs test/accrue_host_web/admin_webhook_replay_test.exs && mix test --max-cases 1`
- `bash scripts/ci/accrue_host_uat.sh`

The integration checker also reported:

- `CI=true bash scripts/ci/accrue_host_uat.sh` passed end to end.
- `bash -n scripts/ci/accrue_host_uat.sh && bash -n scripts/ci/annotation_sweep.sh` passed.
- `cd examples/accrue_host && npx playwright test --list` discovered the single blocking browser spec.

## Result

**Status:** gaps_found

The completed-scope requirements are satisfied, both phases have passed phase verification, and both completed phases are now Nyquist-compliant. The milestone should not be archived yet because one integration gap remains: focused host-flow tests are not fully hermetic outside the canonical UAT wrapper's database reset.

Recommended next action:

```bash
$gsd-plan-milestone-gaps
```

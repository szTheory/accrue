---
milestone: v1.39
name: "Entitlements / Plan-Gating"
audited: 2026-05-24T00:00:00Z
status: tech_debt
scores:
  requirements: 12/12
  phases: 5/5
  integration: 9.5/10
  flows: 3/3
gaps:
  requirements: []   # No unsatisfied, partial, or orphaned requirements
  integration:
    - from: "Phase 127"
      to: "(no consumer)"
      export: "Accrue.Entitlements.StripeSync.summary_for_customer/1"
      severity: warning
      requirement: ENT-10
      note: "Read-only observational seam is exported but has zero callers (production, admin, or test). Intentionally inert by design — its forward consumer (an admin 'what Stripe last reported' view) is deferred, and ENT-10 explicitly defers paginated reads to lattice_stripe >= 1.2. Not wired into the gate path BY DESIGN (the isolation invariant). Confirm it is meant to ship inert in v1.39."
  flows: []          # All 3 milestone flows trace end-to-end
tech_debt:
  - phase: 123-config-core-gate-api-foundation
    items:
      - "WR-02 (deferred): subject_id remains in the OTel allowlist pending the cardinality decision."
      - "IN-01/IN-02/IN-03 (deferred): accepted follow-ups from 123-REVIEW.md, non-blocking."
  - phase: 124-enforcement-surfaces-plug-liveview-guards
    items:
      - "WR-02 (open): non-%Plug.Conn{} plug fallback re-resolves — resolve-once not guaranteed for undocumented non-conn containers (maintainability only; documented path holds)."
      - "WR-03 (open): {status, body} deny accepts out-of-HTTP-range integers; misconfig -> per-request 500 on deny path (does NOT fail open; robustness only)."
  - phase: 125-provider-honesty-lifecycle-truth
    items:
      - "4 review warnings: telemetry-reason accuracy on freshly-:past_due nil-past_due_since rows; partial-:dunning config raise; string-keyed-map twin drift; negative-guard regex missing uppercase/digit tokens. None affect any allow/deny decision (all fail-closed)."
  - phase: 126-admin-surface-docs-jtbd-spine
    items:
      - "D-12 accepted deviation: ROADMAP SC#3 literally names 'First Hour' as a spine to reference the entitlements guide; the verifier-pinned First Hour numbered spine was deliberately left untouched, with discoverability satisfied via README 'Start here' + quickstart pointers. Override recorded in 126-VERIFICATION.md frontmatter."
      - "IN-02 (deferred): export_copy_strings silent allowlist drop (pre-existing, cosmetic)."
      - "IN-03 (deferred): StatusBadge acronym humanize (pre-existing, cosmetic)."
  - phase: 127-optional-stripe-native-sync-isolated-off-by-default
    items:
      - "WR-05 + IN-01..04 tracked in .planning/todos/pending/2026-05-24-ent10-advisory-cache-followups.md: concurrent same-customer delivery raises Ecto.StaleEntryError (self-healing via Oban retry but noisy); processor column hardcodes 'stripe' under Fake; livemode absent->false fidelity; stale fixture moduledoc; no default summary_synced counter."
nyquist:
  compliant_phases: [126, 127]
  partial_phases: [123, 124, 125]
  missing_phases: []
  overall: partial
baseline_notes:
  - "Full `mix test` carries documented baseline noise unrelated to v1.39: per project memory the 6 PackageDocsVerifierTest failures were RESOLVED in Phase 126, and a flaky PdfTest remains (dodge with --seed 0). The 94+ phase-relevant entitlements tests + 4 properties pass cleanly with --warnings-as-errors."
---

# Milestone v1.39 — Entitlements / Plan-Gating — Audit Report

**Audited:** 2026-05-24
**Phases:** 123–127 (5 phases, all complete)
**Status:** ⚡ **tech_debt** — definition of done achieved, zero blockers, accumulated non-critical debt + partial Nyquist coverage worth a review before completion.

## Definition of Done

> Close the last open step of the canonical SaaS loop — let a Phoenix dev *gate features and access on what a customer has paid for*, first-party, with the same provider-honest + telemetry + admin + docs rigor as the rest of Accrue. The headline JTBD ships with **no new tables and no Stripe dependency**, and the optional Stripe-native sync is isolated to the final phase so it never blocks the core value.

**Verdict: ACHIEVED.** The headline JTBD (local-first, fail-closed plan/feature gating with no new tables and no Stripe dependency) is live and property-pinned across both enforcement surfaces. The optional Stripe-native sync is provably isolated off the core gate path (default `:disabled`, merge-blocking isolation gate green). The critical-path isolation design held: Phases 123→126 delivered full value before any Stripe-specific work in Phase 127.

## Requirements Coverage (3-source cross-reference)

12/12 requirements **satisfied**. Cross-referenced against (1) each phase VERIFICATION.md Requirements Coverage table and (2) the REQUIREMENTS.md traceability table. Source (3), SUMMARY.md `requirements_completed` frontmatter, is **N/A for this project** — Accrue's SUMMARY schema carries `requires`/`provides`/`affects` dependency frontmatter, not a requirements field; absence is a schema difference, not a verification gap. The two authoritative sources agree on all 12, and the integration checker independently confirmed each REQ-ID's wiring path.

| REQ-ID | Phase | VERIFICATION | REQUIREMENTS.md | Integration | Final |
|--------|-------|--------------|-----------------|-------------|-------|
| ENT-01 | 123 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-02 | 123 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-03 | 123 | ✓ SATISFIED | `[x]` Complete | WIRED (property-pinned) | **satisfied** |
| ENT-04 | 123 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-05 | 123 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-06 | 124 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-07 | 124 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-08 | 125 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-09 | 125 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-10 | 127 | ✓ SATISFIED | `[x]` Complete | PARTIAL (write+isolation wired; read seam inert by design) | **satisfied** |
| ENT-11 | 126 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |
| ENT-12 | 126 | ✓ SATISFIED | `[x]` Complete | WIRED | **satisfied** |

**Orphan detection:** None. Every REQ-ID in the traceability table appears in exactly one phase's VERIFICATION.md (no orphans, no double-mapping).

**FAIL gate:** Not triggered — zero `unsatisfied` requirements.

## Phase Verifications

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 123 Config + Core Gate API Foundation | passed | 5/5 | Prior REVIEW BLOCKER (CR-01 non-stringable `:id`) fixed + regression-pinned. |
| 124 Enforcement Surfaces — Plug + LiveView Guards | passed | 4/4 | REVIEW BLOCKER (CR-01 LiveView resolve-once nil) fixed in aecd640. WR-02/WR-03 open (non-fail-open). |
| 125 Provider Honesty + Lifecycle Truth | passed | 4/4 | Paused fail-OPEN gap closed in live read path. 4 review warnings (none affect decisions). |
| 126 Admin Surface + Docs / JTBD Spine | passed | 4/4 | One accepted deviation (D-12 First Hour spine). REVIEW BLOCKER (render crash) fixed. |
| 127 Optional Stripe-Native Sync (isolated) | passed | 4/4 | Isolation invariant verified. WR-05 + INFO tracked in pending todo. |

All 5 phases carry a VERIFICATION.md with `status: passed`. No unverified phases.

## Cross-Phase Integration (score 9.5/10)

All five critical seams trace end-to-end through actual source; both packages compile clean across the monorepo boundary; three merge-blocking static gates pass (LiveView-runtime-free, processor support-matrix drift, sync isolation).

1. **Gate API ← config** — WIRED. The 4 `Accrue.*` delegates resolve through `Accrue.Config.entitlements/0` + `Resolver.__impl__().resolve/2`. No dangling delegates.
2. **Guards → gate API** — WIRED. `Accrue.Entitlements.Guard` (Plug + LiveView surfaces) delegates to the Phase 123 fail-closed contract with resolve-once (`:accrue_billable` stash), not a parallel reimplementation.
3. **Resolver/lifecycle truth → gate path** — WIRED. `Query.entitling/1` is in the live `local_map` read path; the paused fail-OPEN closure is real, not just defined.
4. **Admin → read API** — WIRED across the monorepo boundary. `accrue_admin` CustomerLive `entitlements` tab → `Accrue.Entitlements.Admin.resolve_for_customer/1` → SSOT `fold_active/1`.
5. **Phase 127 isolation invariant (load-bearing)** — VERIFIED. With `stripe_native_sync: :disabled`, the 8 gate-path files contain ZERO references to the cache; `verify_entitlement_sync_isolation.sh` green; the webhook checks the config gate FIRST and early-returns before any Repo call; runtime test asserts zero queries against `accrue_entitlement_summaries` + byte-for-byte Phase-126 parity.

**One loose end (warning, not blocker):** `Accrue.Entitlements.StripeSync.summary_for_customer/1` is exported with zero consumers — an intentionally-inert observational seam awaiting a deferred admin view / `lattice_stripe ≥ 1.2`. By design (it must NOT be on the gate path), but confirm it is meant to ship inert in v1.39.

## E2E Flows (3/3 complete)

- **Headline JTBD (local-first, no Stripe):** COMPLETE — config → active subscription → `entitled?` true; unentitled denied at both Plug and LiveView; fail-closed on error/nil/unmapped. Property-pinned.
- **Operator visibility:** COMPLETE — admin entitlements tab renders resolved entitlements + surfaces unmapped-plan drift; three render states tested.
- **Optional Stripe sync (enabled):** write/upsert/monotonic/isolation legs COMPLETE and tested; the observational read leg dead-ends (no consumer reads the cache back), the single reason flows score is 9/10 not 10/10.

## Nyquist Coverage (partial)

| Phase | VALIDATION.md | nyquist_compliant | Status | Suggested action |
|-------|---------------|-------------------|--------|------------------|
| 123 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 123` |
| 124 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 124` |
| 125 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 125` |
| 126 | exists (validated) | true | COMPLIANT | — |
| 127 | exists (validated) | true | COMPLIANT | — |

Discovery only — no validation was auto-run. Phases 123–125 have VALIDATION.md scaffolds left in `draft` (wave_0 not complete); their goals are otherwise fully verified by VERIFICATION.md + the phase test suites. Running `/gsd:validate-phase` on each would close the Nyquist coverage gap retroactively.

## Tech Debt Summary

No critical blockers. Accumulated non-critical debt spans 5 phases; all of it is documented and non-fail-open. The Phase 127 items are already captured in `.planning/todos/pending/2026-05-24-ent10-advisory-cache-followups.md`. See the `tech_debt` block in the frontmatter for the per-phase itemization. Highest-value items to consider before/after completing:

- **127 WR-05** — concurrent same-customer summary delivery raises `Ecto.StaleEntryError`; prefer a DB-level upsert (`on_conflict`/`conflict_target: :customer_id`) so deliveries serialize instead of crash-and-retry.
- **124 WR-03** — out-of-HTTP-range `{status, body}` deny status → per-request 500 on a misconfigured deny path (does not fail open, but worth a boot-time range guard).
- **Nyquist** — 3 phases (123/124/125) sit at PARTIAL; validate-phase to close.

## Conclusion

Milestone v1.39 **achieved its definition of done.** All 12 requirements satisfied, all 5 phases verified passed, cross-phase integration and all 3 E2E flows complete, and the load-bearing Phase 127 isolation invariant proven. Status is **tech_debt** (not `passed`) solely to surface the accumulated review follow-ups and the partial Nyquist coverage for a decision before archiving — none of it blocks completion.

---
milestone: v1.40
scope: phase-128-only (scoped mid-flight audit — 1 of 5 phases complete)
audited: 2026-05-25T04:25:13Z
status: passed
note: >
  This is a SCOPED audit of Phase 128 only, run mid-milestone at the user's
  request. It is NOT the full v1.40 milestone definition-of-done audit — that
  must wait until Phases 129–132 are built. When the milestone completes, run
  /gsd:audit-milestone v1.40 again for the full 5-phase DoD audit, which will
  produce v1.40-v1.40-MILESTONE-AUDIT.md.
scores:
  requirements: 4/4   # in-scope (DUN-01, DUN-02, DUN-04, DUN-05)
  phases: 1/1         # in-scope (Phase 128)
  integration: 10/10
  flows: 4/4
nyquist:
  compliant_phases: ["128"]
  partial_phases: []
  missing_phases: []
  overall: compliant
security:
  phase_128: { status: verified, threats_open: 0, asvs_level: 2 }
gaps:
  requirements: []   # no unsatisfied / partial / orphaned in-scope requirements
  integration: []
  flows: []
out_of_scope:        # NOT gaps — unbuilt later phases, deliberately excluded
  - phase: 129
    requirements: [DUN-06, DUN-07, DUN-08]
    note: "Customer + operator surfaces + observability — not started"
  - phase: 130
    requirements: [DUN-09, DUN-10]
    note: "Provider honesty + Fake-lane proof + example-host wiring — not started"
  - phase: 131
    requirements: [DUN-03]
    note: "Optional Chimeway engine adapter (isolated) — not started"
  - phase: 132
    requirements: [PROOF-03]
    note: "Entitlements adopter-proof demo — not started"
tech_debt:
  - phase: 128-campaign-engine-foundation-idempotency-must-fix
    items:
      - "IN-02: hardcoded \"Acme Billing\" brand fallback in production email modules — only surfaces if a host's branding resolution returns nil at delivery (recommended follow-up)."
      - "IN-03: non-total email_type/1 crashes the worker on a custom step key — unreachable on the shipped default journey (:reminder/:action_required/:final_notice), but a host using a custom step key could hit it (recommended follow-up)."
      - "IN-01: duplicated email scaffolding across the two new step-email modules (cosmetic)."
      - "IN-04: deferred index on dunning_campaign_started_at — acceptable at current scale; revisit if cancel-on-recovery cancel_all_jobs queries become hot."
---

# v1.40 Phase 128 — Scoped Audit Report

**Milestone:** v1.40 — Dunning depth / notification journeys (Phases 128–132)
**Scope of this audit:** Phase 128 only (the milestone is 1 of 5 phases complete)
**Audited:** 2026-05-25T04:25:13Z
**Status:** ✅ **passed** (in-scope contract fully met; 4 minor, acknowledged tech-debt items)

---

## Why this is a scoped audit

`/gsd-audit-milestone` was invoked against the current milestone (v1.40), but v1.40
is only **20% built** — Phase 128 is complete and verified; Phases 129–132 have not
been started. A full milestone definition-of-done audit is premature. At the user's
direction, this audit verifies **only the completed Phase 128 scope** and treats the
unbuilt phases as out-of-scope (not gaps).

The full v1.40 milestone DoD audit should be run once Phases 129–132 are built and
verified; it will produce `v1.40-v1.40-MILESTONE-AUDIT.md`.

---

## Requirements Coverage (3-source cross-reference)

In-scope requirements for Phase 128: **DUN-01, DUN-02, DUN-04, DUN-05**.

| REQ-ID | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | → Final |
|--------|-----------------|---------------------|-----------------|---------|
| DUN-01 | ✓ SATISFIED (passed) | 128-01 `requirements-completed: [DUN-01]`; 128-04 `tags: […DUN-01]` | `[x]` Complete | **satisfied** |
| DUN-02 | ✓ SATISFIED (passed) | 128-03 `[DUN-02]`; 128-05 `[DUN-02, DUN-05]` | `[x]` Complete | **satisfied** |
| DUN-04 | ✓ SATISFIED (passed) | 128-04 carries `DUN-04` in `tags` (not the `requirements-completed` field) | `[x]` Complete | **satisfied** |
| DUN-05 | ✓ SATISFIED (passed) | 128-02 `[DUN-05]`; 128-05 `[DUN-02, DUN-05]` | `[x]` Complete | **satisfied** |

- **Orphan check:** none. REQUIREMENTS.md maps exactly DUN-01/02/04/05 to Phase 128; all are claimed by plans and verified. DUN-03 / DUN-06..10 / PROOF-03 are correctly mapped to later phases (129–132), not Phase 128.
- **Minor inconsistency (not a gap):** Plan 04's SUMMARY records its REQ-IDs (DUN-04, DUN-01) in the `tags:` field rather than the `requirements-completed:` field used by the other plans. VERIFICATION evidence and the `[x]` traceability make satisfaction conclusive; this is a frontmatter-field-naming nit worth normalizing in future plans.

**Result: 4/4 in-scope requirements satisfied.**

---

## Phase Verification (in scope)

| Phase | VERIFICATION status | Score | Gaps | Deferred |
|-------|---------------------|-------|------|----------|
| 128 Campaign Engine Foundation + Idempotency Must-Fix | ✅ passed | 4/4 ROADMAP SC · 21/21 plan must-haves | none | none |

All four ROADMAP success criteria verified with code evidence; behavioral spot-checks
green (compile clean; 8 phase test files = 6 properties + 75 tests, 0 failures; full
suite 56 properties + 1555 tests, 0 failures).

---

## Cross-System Integration (gsd-integration-checker)

**Verdict: INTEGRATION SOUND — 10/10, 4/4 E2E flows complete.**

Because Phase 128 is the first phase of the milestone, the meaningful integration
question is whether the new dunning engine wires into the **pre-existing** Accrue
systems (config, Subscription schema, webhook DefaultHandler, Mailglass mailer, Oban)
through real entry points. It does:

| E2E Flow | Status |
|----------|--------|
| Failed-payment campaign start (real `DefaultHandler.handle/1` → atomic elector → day-0 `DunningStep` enqueue) | ✅ complete |
| Step delivery + chain (`perform` → cancel-guard → `Mailer.deliver/2` → `Campaign.next_step/3` → enqueue next w/ same anchor) | ✅ complete |
| Idempotency / dedup (enqueue `unique` on top-level `invoice_id` + `idempotency_key/2` backstop; no regression for other types) | ✅ complete |
| Cancel-on-recovery (in-transaction anchor-clear + post-commit `Oban.cancel_all_jobs` keyed on `campaign_started_at`; stale-recovery isolation) | ✅ complete |

- No orphaned exports, no missing connections.
- Config invalidity (incl. `last_step.after_days > grace_days`) fails loud at boot via `maybe_validate_boot_setup!/1`.
- Coherent with the pre-existing `DunningSweeper` (grace→terminal) — its suite passes unchanged; the D-15 REPLACE gate is coherent in both directions (enabled ⇒ standalone skipped; disabled ⇒ standalone fires deduped).
- Integration checker re-ran tests: phase files green at `--seed 0`; regression sweep 246 tests + 2 properties, 0 failures; `dunning_sweeper_test.exs` 7 tests, 0 failures.

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 128 | exists | true | true | **COMPLIANT** |

13 per-task verification rows, all `✅ green`; 0 gaps; retroactive validation audit (2026-05-25) confirmed every requirement has automated verification. No `/gsd:validate-phase` needed.

---

## Security

Phase 128 `128-SECURITY.md`: **verified**, `threats_open: 0`, ASVS L2. Each plan-time
`<threat_model>` mitigation was confirmed present in implemented code (trust boundaries:
host config→boot, webhook retry→enqueue, reducer→DB anchor race, Oban JSONB args,
recovery→cancel query). No retroactive STRIDE needed.

---

## Tech Debt (non-blocking, acknowledged)

The 4 REVIEW Info findings were intentionally left open as out-of-scope for the
critical-warning fix pass and do **not** block the phase goal:

| Ref | Item | Consequence | Recommendation |
|-----|------|-------------|----------------|
| IN-02 | Hardcoded `"Acme Billing"` brand fallback in production email modules | Only surfaces if host branding resolves to nil at delivery | Follow-up phase |
| IN-03 | Non-total `email_type/1` crashes worker on a custom step key | Unreachable on the shipped default journey; reachable only with a custom step key | Follow-up phase |
| IN-01 | Duplicated email scaffolding across the two new step-email modules | Cosmetic | Optional cleanup |
| IN-04 | Deferred index on `dunning_campaign_started_at` | Acceptable at current scale | Revisit if cancel queries get hot |

IN-02 and IN-03 are the most consequential and are the natural candidates to fold into
a later v1.40 phase (e.g. alongside Phase 129/130 work touching the email surfaces).

---

## Bottom Line

Phase 128 — the correctness-first foundation of v1.40 — fully meets its contract:
config-driven cadence (DUN-01), durable Oban campaign (DUN-02), the idempotency
must-fix (DUN-04), and cancel-on-recovery keying (DUN-05) are all satisfied, soundly
integrated into the existing system through real entry points, Nyquist-compliant, and
security-verified. The milestone can safely proceed to Phase 129.

**This does not certify v1.40's definition of done** — 4 of 5 phases remain. Re-run
`/gsd:audit-milestone v1.40` after Phase 132 for the full milestone DoD audit.

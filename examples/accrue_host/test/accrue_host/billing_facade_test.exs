defmodule AccrueHost.BillingFacadeTest do
  use ExUnit.Case, async: true

  @moduletag :phase10

  test "plan 10-04 replaces this scaffold with the executable billing facade proof" do
    assert "Plan 10-04 replaces this scaffold with the executable billing facade proof." =~
             "10-04"
  end
end

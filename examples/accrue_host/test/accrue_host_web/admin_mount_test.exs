defmodule AccrueHostWeb.AdminMountTest do
  use ExUnit.Case, async: true

  @moduletag :phase10

  test "plan 10-07 replaces this scaffold with the executable admin mount proof" do
    assert "Plan 10-07 replaces this scaffold with the executable admin mount proof." =~
             "10-07"
  end
end

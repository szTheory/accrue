defmodule AccrueHostWeb.AdminWebhookReplayTest do
  use ExUnit.Case, async: true

  @moduletag :phase10

  test "plan 10-07 replaces this scaffold with the executable admin replay proof" do
    assert "Plan 10-07 replaces this scaffold with the executable admin replay proof." =~
             "10-07"
  end
end

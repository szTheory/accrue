defmodule AccrueHostWeb.SubscriptionFlowTest do
  use ExUnit.Case, async: true

  @moduletag :phase10

  test "plan 10-05 replaces this scaffold with the executable subscription flow proof" do
    assert "Plan 10-05 replaces this scaffold with the executable subscription flow proof." =~
             "10-05"
  end
end

defmodule AccrueHostWeb.WebhookIngestTest do
  use ExUnit.Case, async: true

  @moduletag :phase10

  test "plan 10-06 replaces this scaffold with the executable webhook ingest proof" do
    assert "Plan 10-06 replaces this scaffold with the executable webhook ingest proof." =~
             "10-06"
  end
end
